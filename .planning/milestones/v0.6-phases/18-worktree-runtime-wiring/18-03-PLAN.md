---
phase: 18
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - src/triggers/dispatcher.js
  - src/orchestrator/launch.js
  - test/dispatcher.test.js
  - test/orchestrator-launch-isolation.test.js
  - test/gsd-concurrency.test.js
autonomous: true
requirements: [WT-01, WT-03]
user_setup: []

must_haves:
  truths:
    - "Si `<projectPath>/.bg-shell/<sessionId>/` ya existe al dispatch, el dispatcher devuelve `{ action: 'worktree_collision', code: 'worktree_exists', detail: <worktreePath> }` y emite stderr `[kodo:dispatch] worktree_collision — <task.ref> blocked by existing worktree at <worktreePath>` ANTES de invocar `launchWorkItem` (D-05 fail-fast canonical, D-05b shape paralelo a `gsd_locked`)."
    - "El check de colisión usa `existsSync(computeWorktreePath(projectPath, gsdSessionId))` para sesiones GSD (donde sessionId se genera ANTES del lock per pattern actual líneas 119–138 dispatcher) y `existsSync(computeWorktreePath(projectPath, randomUUID()))` para sesiones no-GSD donde el sessionId también se genera early-bird (NUEVO: dispatcher genera sessionId en wave 3 también para no-GSD para poder check colisión)."
    - "El check ocurre DESPUÉS del lock acquire GSD (líneas 121–140 dispatcher) y ANTES de la sección `4. Session-already-active guard` (línea 263). En modo GSD, si check falla → release del lock (no leak) y return `worktree_collision`. En modo no-GSD, return directo."
    - "INVARIANTE LOCK PER-REPO (SC#3): `acquireGsdLockFn(projectPath, ...)` línea 130 sigue recibiendo `projectPath` (repo principal), JAMÁS `worktreePath`. Verificable con grep cross-source: `grep -c \"acquireGsdLock(worktree\\|acquireGsdLockFn(worktree\\|releaseGsdLock(worktree\\|releaseGsdLockFn(worktree\" src/` == 0 (nunca lock sobre worktree)."
    - "Coalescencia preservada (WT-03): dos `dispatchTrigger` concurrentes sobre la misma task sobre el mismo repo → la primera adquiere lock + worktree, la segunda recibe `gsd_locked` (Phase 8 GSD-10 semantics intactas). El test de coalescencia en `test/gsd-concurrency.test.js` se extiende para verificar que (a) sesión 1 cablea `--worktree` y (b) sesión 2 NO llega a check de worktree (lock la rechaza antes)."
    - "EXCLUSIÓN ORCHESTRATOR (D-06): `src/orchestrator/launch.js#launchOrchestrator` NO contiene `--worktree` en su comando claude (líneas 83–89 actuales). Verificable con `grep -c -- '--worktree' src/orchestrator/launch.js` == 0. Comentario in-file documenta el motivo: cwd=repo Phase 999.1 D-05 constraint para auto-cargar `.claude/skills/kodo-orchestrate/skill.md`."
    - "El return type de `dispatchTrigger` se extiende: `'launched'|'ignored'|'already_active'|'stale_relaunch'|'cleaned'|'gsd_locked'|'resolver_failed'|'worktree_collision'` (D-05b shape paralelo, action string explícito)."
    - "Sesiones no-GSD también corren con `--worktree` (D-06b — heredan WT-01 de Plan 02 vía launchWorkItem) PERO no adquieren lock (heredan v0.5 — comportamiento sin cambios). Dos no-GSD sobre el mismo repo pueden correr en paralelo cada una en su worktree (resuelve incidencia 28/04 ROMAN-113…118)."
  artifacts:
    - path: "src/triggers/dispatcher.js"
      provides: "Canonical worktree_collision action; check fail-fast pre-launch; lock invariant preservado; release-on-collision para no leak"
      contains: "worktree_collision"
      min_lines: 340
    - path: "src/orchestrator/launch.js"
      provides: "Comentario in-file documentando exclusión D-06 (cwd=repo Phase 999.1)"
      contains: "Phase 18 D-06"
      min_lines: 100
    - path: "test/dispatcher.test.js"
      provides: "Asserts: (a) worktree_collision shape, (b) lock release on collision, (c) action en return type union"
      contains: "worktree_collision"
      min_lines: 1
    - path: "test/orchestrator-launch-isolation.test.js"
      provides: "Source-hygiene: launchOrchestrator NUNCA contiene --worktree (D-06 invariante grep cross-file con stripComments)"
      contains: "--worktree"
      min_lines: 30
    - path: "test/gsd-concurrency.test.js"
      provides: "Coalescencia 2-tasks-1-repo preservada con --worktree cableado en sesión 1"
      contains: "worktree_path"
      min_lines: 1
  key_links:
    - from: "src/triggers/dispatcher.js"
      to: "src/session/state.js#computeWorktreePath"
      via: "named import"
      pattern: "import\\s+\\{[^}]*\\bcomputeWorktreePath\\b[^}]*\\}\\s+from\\s+['\"]\\.\\./session/state\\.js['\"]"
    - from: "src/triggers/dispatcher.js#dispatchTrigger"
      to: "node:fs#existsSync"
      via: "import existsSync; existsSync(worktreePath) check"
      pattern: "existsSync\\(\\s*[a-zA-Z_]*[wW]orktree[a-zA-Z_]*\\s*\\)"
    - from: "src/triggers/dispatcher.js#worktree-collision-branch"
      to: "stderr canonical message"
      via: "console.log('[kodo:dispatch] worktree_collision — ...')"
      pattern: "\\[kodo:dispatch\\]\\s+worktree_collision"
    - from: "src/triggers/dispatcher.js#worktree-collision-branch"
      to: "releaseGsdLockFn (lock NO leak)"
      via: "release before return when GSD path acquired lock"
      pattern: "releaseGsdLockFn\\([^)]+\\)[^;]*;[\\s\\S]{0,300}return\\s*\\{\\s*action:\\s*'worktree_collision'"
    - from: "src/orchestrator/launch.js#launchOrchestrator"
      to: "explicit absence of --worktree"
      via: "claudeCmd template line 83-89 sin --worktree (Phase 999.1 D-05 cwd=repo)"
      pattern: "(?!.*--worktree).*"
    - from: "test/orchestrator-launch-isolation.test.js"
      to: "src/orchestrator/launch.js"
      via: "readFileSync + stripComments + assert.ok(!stripped.includes('--worktree'))"
      pattern: "stripComments\\("
---

<objective>
Cerrar Phase 18 con tres invariantes críticos:

1. **Canonical error fail-fast** (D-05, D-05b): si el path del worktree ya existe, el dispatcher aborta ANTES de invocar `launchWorkItem`/`cmux`, devolviendo `{ action: 'worktree_collision', code: 'worktree_exists', detail: <worktreePath> }`. Patrón paralelo a `gsd_locked` (Phase 8 D-19) y `resolver_failed` (Phase 9 D-13). Si la colisión ocurre tras adquirir el lock GSD → release del lock para no leak.

2. **Lock invariant preservado SC#3 (WT-03)**: `acquireGsdLockFn(projectPath, ...)` JAMÁS recibe `worktreePath`. Verificable con grep cross-source. Test integration `gsd-concurrency.test.js` extiende los escenarios coalesce con la nueva forma del cmd (`--worktree` cableado en sesión 1) y confirma que la sesión 2 sigue rebotando con `gsd_locked` (no llega a check de worktree).

3. **Exclusión `launchOrchestrator` D-06**: `src/orchestrator/launch.js` NO emite `--worktree` en su cmd claude. Comentario explícito en el archivo documenta el motivo (Phase 999.1 D-05 cwd=repo constraint para auto-cargar skill local). Test source-hygiene `test/orchestrator-launch-isolation.test.js` blinda con grep + stripComments (patrón Phase 16 LOG-13 `dispatcher-isolation.test.js`).

**Decisión del planner sobre code/action strings (D-05b Claude's Discretion):**
- `action: 'worktree_collision'` — paralelo semántico a `gsd_locked` (otra forma de "no podemos lanzar porque algo ya existe").
- `code: 'worktree_exists'` — describe la causa concreta (vs. `gsd_locked` que NO usa code, va con `holder`; el resolver usa `code: 'no-match' | 'roadmap-missing' | 'multi-match'`).
- `detail: <worktreePath>` — el path absoluto que colisiona (paralelo a `resolver_failed` `detail`).
- Stderr: `[kodo:dispatch] worktree_collision — <task.ref> blocked by existing worktree at <worktreePath>` (mismo shape que `[kodo:dispatch] gsd_locked — ${task.ref} blocked by lock on ${gsdProjectPath}` línea 136).
- Exit code: NO se introduce nuevo CLI exit code; el dispatcher es interno (su return shape lo consumen el server y `kodo launch`). El handler downstream decide el exit code; en este plan no se modifica `src/cli.js` ni `src/server.js`.

**Decisión sobre check para sesiones no-GSD (D-05 + D-06b):**
Hoy el dispatcher genera `gsdSessionId` SOLO si `gsdMode` es truthy (línea 119–139). Para sesiones no-GSD, el sessionId se genera DENTRO de `launchWorkItem` (línea 206 manager.js: `const sessionId = opts.sessionId || randomUUID()`). Si queremos check fail-fast PRE-launch para no-GSD, necesitamos generar el sessionId también en el dispatcher para no-GSD.

**Estrategia escogida (mínima fricción):**
- Para sesiones GSD: usar `gsdSessionId` ya disponible (líneas 119–139).
- Para sesiones no-GSD: generar `randomUUID()` en el dispatcher justo antes del check, y threadearlo via `opts.sessionId` a `launchWorkItem` (mismo mecanismo que ya usa GSD por CR-01 fix). Esto unifica el ownership del sessionId en el dispatcher para ambos modos.

Purpose: cubrir WT-01 (verificación de exclusión orchestrator) + WT-03 (lock invariant + canonical error coordinado).
Output: `dispatcher.js` con +1 import (`computeWorktreePath`, `existsSync`), +1 randomUUID gen para no-GSD, +1 branch `worktree_collision` con release-on-collision; `launch.js` con comentario D-06; 3 tests (dispatcher + isolation + concurrency).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md
@.planning/phases/18-worktree-runtime-wiring/18-01-PLAN.md
@.planning/phases/18-worktree-runtime-wiring/18-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Source of truth — read BEFORE editing
@src/triggers/dispatcher.js
@src/orchestrator/launch.js
@src/session/state.js
@src/session/manager.js
@src/gsd/lock.js
@test/dispatcher.test.js
@test/gsd-concurrency.test.js
@test/dispatcher-isolation.test.js

<interfaces>
<!-- Dispatcher actual (líneas relevantes) — NO cambia signature de dispatchTrigger: -->

```javascript
// Línea 39: return type discriminated union (extender con 'worktree_collision')
@returns {Promise<{ action: 'launched'|'ignored'|'already_active'|'stale_relaunch'|'cleaned'|'gsd_locked'|'resolver_failed'|'worktree_collision', session?, holder?, code?, detail? }>}

// Líneas 119–139: lock acquire GSD (NO TOCAR la signature; verificar pasa projectPath, no worktreePath)
let gsdSessionId = null;
let gsdProjectPath = null;
if (gsdMode) {
  try { gsdProjectPath = resolveProjectPathFn(task); } catch { gsdProjectPath = null; }
  if (gsdProjectPath) {
    gsdSessionId = randomUUID();
    const lockResult = acquireGsdLockFn(gsdProjectPath, { session_id: gsdSessionId, ... });
    if (!lockResult.acquired) return { action: 'gsd_locked', holder: lockResult.holder };
  }
}

// NUEVO bloque (entre línea 140 y línea 142, ANTES del switch del resolver):
// Phase 18 D-05: fail-fast canonical worktree_collision check.
// Para sesiones no-GSD, generamos sessionId AQUÍ (early-bird) para poder
// computar worktree path PRE-launch. Threaded a launchWorkItem via opts.sessionId
// (mismo mecanismo que GSD por CR-01).
let dispatchSessionId = gsdSessionId;
let dispatchProjectPath = gsdProjectPath;
if (!gsdMode) {
  try { dispatchProjectPath = resolveProjectPathFn(task); } catch { dispatchProjectPath = null; }
  if (dispatchProjectPath) dispatchSessionId = randomUUID();
}
if (dispatchSessionId && dispatchProjectPath) {
  const worktreePath = computeWorktreePath(dispatchProjectPath, dispatchSessionId);
  if (existsSync(worktreePath)) {
    // Release lock if GSD acquired one (no leak)
    if (gsdSessionId && gsdProjectPath) {
      try { releaseGsdLockFn(gsdProjectPath, gsdSessionId); } catch {}
    }
    console.log(`[kodo:dispatch] worktree_collision — ${task.ref} blocked by existing worktree at ${worktreePath}`);
    return { action: 'worktree_collision', code: 'worktree_exists', detail: worktreePath };
  }
}

// Líneas 282–292 / 313–322 — launchOpts: añadir threading de sessionId también para no-GSD
const launchOpts = {
  model: opts.model ?? kodoConfig.model,
  flags: [...(opts.flags || []), ...kodoConfig.flags],
  ...(dispatchSessionId ? { sessionId: dispatchSessionId } : {}),  // ← cambiado de gsdSessionId a dispatchSessionId
  ...(gsdPhaseId ? { phase_id: gsdPhaseId } : {}),
  ...(gsdBrief ? { brief: gsdBrief } : {}),
};
```

<!-- Orchestrator actual (lines 78-89) — NO mutación al cmd, SÍ comentario D-06: -->

```javascript
// Líneas 83–89 actuales:
const claudeCmd = [
  'claude',
  '--model', config.claude.default_model,
  '--session-id', sessionId,
  ...config.claude.flags,
  `'${escapedPrompt}'`,
].join(' ');

// AÑADIR comentario JUSTO ANTES (después de línea 82 que dice `const sessionId = randomUUID();`):
// Phase 18 D-06: launchOrchestrator queda EXCLUIDO de --worktree. El orchestrator
// necesita cwd = repo para que Claude Code auto-cargue
// `.claude/skills/kodo-orchestrate/skill.md` (Phase 999.1 D-05 constraint
// registrado en PROJECT.md §Constraints). Si añadiéramos --worktree aquí, la
// session arrancaría en `<repo>/.bg-shell/<uuid>/` donde NO existe la skill,
// regresando al fallback degradado de src/orchestrator/prompt.md.
// Source-hygiene blindado por test/orchestrator-launch-isolation.test.js.
```

<!-- Source-hygiene helper reusable (de test/dispatcher-isolation.test.js líneas 22–28): -->

```javascript
function stripComments(src) {
  return src
    .replace(/\/\*[\s\S]*?\*\//g, '')
    .split('\n')
    .filter((line) => !line.trim().startsWith('//') && !line.trim().startsWith('*'))
    .join('\n');
}
```

<!-- Lock invariant grep (cross-source) — patrón a usar en el test: -->

```javascript
// Verifica que NUNCA se pasa worktreePath al lock. Cross-file scan.
const sourceFiles = ['src/triggers/dispatcher.js', 'src/session/manager.js', 'src/hooks/stop.js'];
for (const f of sourceFiles) {
  const src = readFileSync(f, 'utf-8');
  const stripped = stripComments(src);
  assert.ok(!/acquireGsdLockFn?\([^)]*worktree/i.test(stripped), `${f} pasa worktree* a acquireGsdLock`);
  assert.ok(!/releaseGsdLockFn?\([^)]*worktree/i.test(stripped), `${f} pasa worktree* a releaseGsdLock`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical error worktree_collision en dispatcher + threading sessionId no-GSD</name>
  <files>src/triggers/dispatcher.js, test/dispatcher.test.js</files>

  <read_first>
    - src/triggers/dispatcher.js (estado completo — confirmar líneas 119–139 lock GSD, líneas 280–325 launchOpts dual stale_relaunch+launched, return type línea 39, imports líneas 1–12)
    - src/session/state.js (Plan 01 — confirmar `computeWorktreePath` exportado)
    - src/session/manager.js (Plan 02 — confirmar que `launchWorkItem` ya consume `opts.sessionId` cuando se le pasa, no genera uno nuevo en ese caso — línea 206 `opts.sessionId || randomUUID()`)
    - src/gsd/lock.js (verificar signature `acquireGsdLock(projectPath, sessionInfo)` línea 103 — invariante WT-03)
    - test/dispatcher.test.js (estado actual — patrones de stub para deps `acquireGsdLockFn`, `releaseGsdLockFn`, `launchWorkItemFn`; cómo invoca `dispatchTrigger`; cómo asserta sobre return shape)
    - test/dispatcher-isolation.test.js (helper `stripComments` reusable; patrón source-hygiene a clonar en orchestrator-launch-isolation.test.js)
    - .planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md §decisions D-05, D-05b, D-06, D-06b
  </read_first>

  <behavior>
    - Test 1 (worktree_collision shape — GSD): mock `acquireGsdLockFn` → acquired:true, mock `existsSync` → true para el path computado, dispatch GSD task → return shape exacto `{ action: 'worktree_collision', code: 'worktree_exists', detail: '<repo>/.bg-shell/<uuid>' }`. NO se invoca `launchWorkItemFn`.
    - Test 2 (worktree_collision shape — no-GSD): mock `existsSync` → true, dispatch task SIN flags GSD → mismo shape. NO `acquireGsdLockFn` invocado (no-GSD heredado v0.5). NO `launchWorkItemFn`.
    - Test 3 (lock release on collision — GSD): mock `acquireGsdLockFn` → acquired:true, mock `existsSync` → true → verificar que `releaseGsdLockFn(gsdProjectPath, gsdSessionId)` se invoca EXACTAMENTE 1 vez con los args correctos ANTES del return. Lock no leak.
    - Test 4 (no collision happy path GSD): `existsSync` → false → flow continúa al resolver y luego a `launchWorkItemFn(taskRef, { sessionId: gsdSessionId, ... })`. El `sessionId` threaded es el `gsdSessionId` (no se genera uno nuevo).
    - Test 5 (no collision happy path no-GSD): `existsSync` → false, sin GSD → `launchWorkItemFn(taskRef, { sessionId: <uuid generado en dispatcher>, ... })`. El threaded `sessionId` es nuevo (NO undefined).
    - Test 6 (return type union extendido): el JSDoc del `@returns` de `dispatchTrigger` (línea 39) incluye `'worktree_collision'`. Test introspectivo opcional o grep sobre el archivo.
    - Test 7 (stderr canonical bytes): el mensaje stderr es exactamente `[kodo:dispatch] worktree_collision — KL-42 blocked by existing worktree at /repo/.bg-shell/<uuid>` para `task.ref='KL-42'` y path computed. Capture stderr via stub de `console.log` (el dispatcher usa `console.log`, no `console.error` — paralelo a línea 136 `gsd_locked`).
    - Test 8 (existsSync no llamado cuando resolveProjectPath falla): si `resolveProjectPathFn` throws, el path no se computa, no hay collision check, flow sigue normal (heredado de v0.5: launch fallará luego con su propio error). Comportamiento gracioso.
  </behavior>

  <action>
    1. **Modificar imports** en `src/triggers/dispatcher.js` líneas 1–12. Añadir `existsSync` desde `node:fs` (NUEVO import) y `computeWorktreePath` desde `'../session/state.js'`:

       ```javascript
       // Top of file (líneas 1-12 actuales):
       // @ts-check
       import { randomUUID } from 'node:crypto';
       import { existsSync } from 'node:fs';                    // ← NUEVO (Phase 18 D-05)
       import { getProvider } from '../providers/registry.js';
       import { loadConfig, loadProjects } from '../config.js';
       import { parseKodoLabels, getGsdMode } from '../labels.js';
       import { listSessions, removeSession } from '../session/state.js';
       import { computeWorktreePath } from '../session/state.js'; // ← NUEVO (puede combinarse con la línea anterior en un solo import)
       import { launchWorkItem, resolveProjectPath } from '../session/manager.js';
       import { acquireGsdLock, releaseGsdLock } from '../gsd/lock.js';
       import * as cmux from '../cmux/client.js';
       import { resolvePhase } from '../gsd/resolver.js';
       import { buildBriefFromTask, isBriefEmpty } from '../gsd/brief.js';
       import { EVENTS } from '../logger-events.js';
       ```

       Preferencia: combinar imports de `'../session/state.js'` en una sola línea: `import { listSessions, removeSession, computeWorktreePath } from '../session/state.js';`.

    2. **Extender JSDoc return type** línea 39 (añadir `'worktree_collision'` al union):

       ```javascript
       // ANTES:
       * @returns {Promise<{ action: 'launched'|'ignored'|'already_active'|'stale_relaunch'|'cleaned'|'gsd_locked'|'resolver_failed', session?: object, holder?: object, code?: string, detail?: string }>}

       // DESPUÉS:
       * @returns {Promise<{ action: 'launched'|'ignored'|'already_active'|'stale_relaunch'|'cleaned'|'gsd_locked'|'resolver_failed'|'worktree_collision', session?: object, holder?: object, code?: string, detail?: string }>}
       ```

    3. **Añadir el bloque collision check** entre el lock acquire (línea ~140 actual, tras el `if (!lockResult.acquired) return { action: 'gsd_locked', ... }`) y la sección 3c del resolver (línea ~142). El bloque genera sessionId early-bird también para no-GSD y unifica el `dispatchSessionId`/`dispatchProjectPath` para los launchOpts downstream:

       ```javascript
       // ─────────────────────────────────────────────────────────────────────
       // Phase 18 D-05, D-05b, D-06b: fail-fast worktree_collision canonical
       // error. Single source of truth para el path: computeWorktreePath de
       // state.js (Plan 01). Patrón paralelo a gsd_locked (Phase 8 D-19) y
       // resolver_failed (Phase 9 D-13): action explícito + return early.
       //
       // Para sesiones GSD: gsdSessionId ya está generado tras lock acquire.
       // Para sesiones no-GSD (D-06b): generamos sessionId aquí early-bird
       // para poder check colisión PRE-launch. Threaded a launchWorkItem via
       // opts.sessionId (mismo mecanismo que GSD por CR-01 fix).
       //
       // INVARIANTE WT-03: el lock per-repo (Phase 8 GSD-10) sigue siendo
       // sobre projectPath, JAMÁS sobre worktreePath. acquireGsdLockFn arriba
       // (línea 130) NO se modifica.
       // ─────────────────────────────────────────────────────────────────────
       let dispatchSessionId = gsdSessionId;
       let dispatchProjectPath = gsdProjectPath;
       if (!gsdMode) {
         try { dispatchProjectPath = resolveProjectPathFn(task); } catch { dispatchProjectPath = null; }
         if (dispatchProjectPath) dispatchSessionId = randomUUID();
       }
       if (dispatchSessionId && dispatchProjectPath) {
         const worktreePath = computeWorktreePath(dispatchProjectPath, dispatchSessionId);
         if (existsSync(worktreePath)) {
           // Release lock if GSD acquired one (no leak — Phase 8 D-09 idempotent)
           if (gsdSessionId && gsdProjectPath) {
             try { releaseGsdLockFn(gsdProjectPath, gsdSessionId); } catch {
               // silent — release is idempotent (Phase 8 D-09)
             }
           }
           console.log(`[kodo:dispatch] worktree_collision — ${task.ref} blocked by existing worktree at ${worktreePath}`);
           return { action: 'worktree_collision', code: 'worktree_exists', detail: worktreePath };
         }
       }
       ```

    4. **Modificar los `launchOpts` en ambos paths (stale_relaunch línea 282–292 + launched línea 313–322)** para usar `dispatchSessionId` en vez de `gsdSessionId`:

       ```javascript
       // ANTES (líneas 287 y 318 — ambas):
       ...(gsdSessionId ? { sessionId: gsdSessionId } : {}),

       // DESPUÉS (ambas):
       // Phase 18: thread dispatchSessionId (puede ser GSD o no-GSD generado en
       // collision-check block arriba). launchWorkItem línea 206 lo consume vía
       // `opts.sessionId || randomUUID()` — si está presente lo usa verbatim.
       ...(dispatchSessionId ? { sessionId: dispatchSessionId } : {}),
       ```

       NOTA: hay también código en líneas 296–303 (catch block) y líneas 327–335 (catch block) que hace release del lock con `gsdSessionId` y `gsdProjectPath` — NO TOCAR esos catch blocks. Mantienen WT-03 invariante (release del lock per-repo en error path).

    5. **Tests en `test/dispatcher.test.js`** — añadir un nuevo `describe('Phase 18 — worktree collision')` con los 8 tests del bloque `<behavior>`. Patrón base (clonar de tests existentes para `gsd_locked` y `resolver_failed`):

       ```javascript
       describe('Phase 18 — worktree_collision', () => {
         it('returns worktree_collision when path exists (GSD)', async () => {
           // Mock existsSync directamente requiere intercept en el módulo.
           // Estrategia DI: NO existe deps slot para existsSync hoy. Dos opciones:
           // (A) añadir deps.existsSyncFn al typedef DispatchDeps + parametrizar.
           // (B) testar via tmpdir real: crear `.bg-shell/<uuid>/` antes del dispatch.
           //
           // Preferencia: (B) — alineado con Phase 16 LOG-13..15 patrón
           // tmpdir+HOME override (sin mockear node:fs). Setup:
           //   const tmpRepo = mkdtempSync(join(tmpdir(), 'kodo-collision-'));
           //   const fakeUuid = 'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee';
           //   mkdirSync(join(tmpRepo, '.bg-shell', fakeUuid), { recursive: true });
           //
           // Pero `randomUUID()` se llama internamente — necesitamos mockearlo
           // o interceptar. Para simplicidad, mockear via deps (introducir
           // deps.computeWorktreePathFn?: para retornar siempre el path
           // pre-creado). Alternativa: tests stub directos sin deps adicionales,
           // si el equipo prefiere no extender DispatchDeps:
           //
           // TEST OPTION C — añadir deps.existsSyncFn: parametrizable:
           //   const result = await dispatchTrigger(event, {}, {
           //     ...stdDeps,
           //     existsSyncFn: () => true,
           //   });
           //   assert.equal(result.action, 'worktree_collision');
           //   assert.equal(result.code, 'worktree_exists');
           //   assert.match(result.detail, /\.bg-shell\/[a-f0-9-]+$/);
           //
           // RECOMENDACIÓN: extender DispatchDeps con existsSyncFn? para test
           // hygiene (precedente: la mayoría de IO en dispatch ya está parametrizado).
         });

         it('releases lock on collision (GSD path) — no leak', async () => {
           const releaseSpy = mock.fn();
           // ... configurar acquireGsdLockFn → acquired:true, existsSyncFn → true
           // ... dispatch
           // assert.equal(releaseSpy.mock.callCount(), 1);
           // assert.equal(releaseSpy.mock.calls[0].arguments[0], '/test/repo'); // projectPath
           // assert.match(releaseSpy.mock.calls[0].arguments[1], /^[a-f0-9-]+$/); // sessionId UUID
         });

         it('returns worktree_collision for non-GSD task (D-06b)', async () => {
           // Sin labels GSD — sigue corriendo collision check porque dispatcher
           // genera sessionId early-bird incluso para no-GSD.
         });

         it('does NOT call launchWorkItem on collision', async () => {
           const launchSpy = mock.fn();
           // ... existsSyncFn → true, dispatch
           // assert.equal(launchSpy.mock.callCount(), 0);
         });

         it('threads dispatchSessionId to launchWorkItem when no collision', async () => {
           const launchSpy = mock.fn(() => ({ session_id: 'unused' }));
           // ... existsSyncFn → false, dispatch GSD task
           // const opts = launchSpy.mock.calls[0].arguments[1];
           // assert.match(opts.sessionId, /^[a-f0-9-]+$/);
           // assert.equal(opts.sessionId.length, 36); // UUID v4 length
         });

         it('threads dispatchSessionId to launchWorkItem for non-GSD too (Phase 18)', async () => {
           // Sesión no-GSD ahora también recibe sessionId del dispatcher
           // (cambio de comportamiento: antes no-GSD generaba sessionId DENTRO
           // de launchWorkItem; ahora se genera en dispatcher para colision check).
         });

         it('emits stderr canonical bytes [kodo:dispatch] worktree_collision — ...', async () => {
           // Capturar console.log via spy; aserir el shape exacto.
         });

         it('skips collision check when resolveProjectPath throws (graceful — heredado v0.5)', async () => {
           // Si resolveProjectPath throws → dispatchProjectPath null → no check
           // → flow continúa, launchWorkItem fallará luego con su propio error.
         });
       });
       ```

       **DECISIÓN OPERATIVA del executor:** elegir TEST OPTION C (extender `DispatchDeps` con `existsSyncFn?: (path: string) => boolean`) por consistencia con el resto del módulo. Añadir la deps key + default en línea ~47 del dispatcher:

       ```javascript
       const existsSyncFn = deps.existsSyncFn || existsSync;
       ```

       Y usar `existsSyncFn(worktreePath)` en el bloque collision-check en lugar de `existsSync(worktreePath)`. Esto extiende el typedef `DispatchDeps` con una entrada más, paralelo a `acquireGsdLockFn`/`releaseGsdLockFn`.

    6. **NO TOCAR**:
       - Líneas 142–261 (resolver branch + EVENTS emits) — el resolver corre DESPUÉS del collision check; comportamiento intacto.
       - Líneas 263–308 (Session-already-active guard + stale_relaunch) — solo cambia el spread `dispatchSessionId` vs `gsdSessionId` en launchOpts.
       - Líneas 310–339 (launch + catch block) — solo cambia el spread.
       - `inFlight` Set (línea 15) — semántica intacta.
       - El catch block de release-on-launch-error (líneas 296–303 y 327–335) — sigue usando `gsdSessionId`/`gsdProjectPath` (no `dispatchSessionId`/`dispatchProjectPath`) porque el catch SOLO releasea si fue GSD el que adquirió lock.
  </action>

  <verify>
    <automated>node --test test/dispatcher.test.js test/gsd-concurrency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "worktree_collision" src/triggers/dispatcher.js` >= 3 (1 typedef return + 1 console.log + 1 return obj)
    - `grep -c "worktree_exists" src/triggers/dispatcher.js` == 1 (solo el code en el return obj)
    - `grep -c "computeWorktreePath" src/triggers/dispatcher.js` >= 2 (1 import + 1 invocación)
    - `grep -c "existsSync" src/triggers/dispatcher.js` >= 2 (1 import + 1 default deps fallback)
    - Stderr canonical exact: la string `[kodo:dispatch] worktree_collision — ` aparece exactamente 1 vez en el código (D-05b shape paralelo `gsd_locked`).
    - Lock release on collision verificable: regex multilinea `releaseGsdLockFn[\s\S]{0,200}return\\s*\\{\\s*action:\\s*'worktree_collision'` matches al menos 1 vez en el archivo.
    - INVARIANTE WT-03 cross-source (lock NO toca worktree): `grep -E "acquireGsdLockFn?\\(\\s*[a-zA-Z_]*[wW]orktree|releaseGsdLockFn?\\(\\s*[a-zA-Z_]*[wW]orktree" src/triggers/dispatcher.js src/session/manager.js src/hooks/stop.js` retorna 0 matches.
    - `node --test test/dispatcher.test.js` exits 0 (incluyendo nuevos describe Phase 18)
    - `node --test test/gsd-concurrency.test.js` exits 0 (coalesce sigue funcionando con `--worktree` cableado)
    - `npm test` exits 0 (suite global)
  </acceptance_criteria>

  <done>
    Dispatcher emite `worktree_collision` canonical (action + code + detail + stderr) ANTES de invocar launchWorkItem; lock se libera on-collision sin leak; lock invariant WT-03 preservado verificable con grep cross-source; tests cubren 8 escenarios.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comentario D-06 + source-hygiene test orchestrator-launch-isolation</name>
  <files>src/orchestrator/launch.js, test/orchestrator-launch-isolation.test.js</files>

  <read_first>
    - src/orchestrator/launch.js (estado completo — confirmar líneas 78–89 donde se construye claudeCmd, líneas 79–82 generación sessionId, línea 73 cwd)
    - test/dispatcher-isolation.test.js (helper `stripComments` líneas 22–28 reusable)
    - test/format-isolation.test.js (patrón source-hygiene cross-file con grep + walker reusable; nosotros NO necesitamos walker, solo grep en 1 archivo)
    - .planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md §decisions D-06
    - .planning/PROJECT.md §Constraints (línea 136 — Orchestrator se lanza desde cwd = repo kodo)
  </read_first>

  <behavior>
    - Test 1 (source-hygiene `--worktree` ausente): `readFileSync(src/orchestrator/launch.js)` → `stripComments(src)` → assert.ok(!stripped.includes('--worktree')). El comentario in-file SÍ menciona `--worktree` pero queda fuera del stripped. La defensa-en-profundidad bloquea regresión futura si alguien añade el flag.
    - Test 2 (comentario D-06 presente): el archivo SÍ contiene la frase `Phase 18 D-06` en algún comentario (block o //). Verifica que la decisión está documentada en código.
    - Test 3 (cwd=repo invariant): `cwd: process.cwd()` línea 73 sigue intacto (no `cwd: worktreePath` ni similar). Grep: `grep -c "cwd: process.cwd()" src/orchestrator/launch.js` == 1.
  </behavior>

  <action>
    1. **Añadir comentario in-file** en `src/orchestrator/launch.js` justo ANTES de la construcción de `claudeCmd` (línea 83 actual). Ubicación exacta: después de `const sessionId = randomUUID();` (línea 79 actual) y antes de `const prompt = ...` (línea 80) o, alternativamente, justo encima del array `claudeCmd = [...]`. Preferencia: encima del array, donde el lector busca la composición del cmd:

       ```javascript
       // Líneas 78-82 actuales (tras notify y notify color):
       // Build Claude command with orchestrator prompt + context
       const sessionId = randomUUID();
       const prompt = `${basePrompt}\n\n## Situación actual\n\n${contextSummary}`;
       const escapedPrompt = prompt.replace(/'/g, "'\\''");

       // ─────────────────────────────────────────────────────────────────────
       // Phase 18 D-06: launchOrchestrator EXCLUIDO de --worktree.
       //
       // El orchestrator necesita cwd = repo kodo (línea 73 arriba: `cwd:
       // process.cwd()`) para que Claude Code auto-cargue
       // `.claude/skills/kodo-orchestrate/skill.md` (Phase 999.1 D-05/D-06
       // constraint registrado en PROJECT.md §Constraints).
       //
       // Si se añadiera --worktree aquí, la sesión arrancaría en
       // <repo>/.bg-shell/<uuid>/ donde NO existe la skill, regresando al
       // fallback degradado de src/orchestrator/prompt.md.
       //
       // Source-hygiene blindado por test/orchestrator-launch-isolation.test.js
       // que grep-asserta `--worktree` ausente del código (los comentarios sí
       // pueden mencionarlo — el test usa stripComments).
       //
       // Las sesiones de TRABAJO (launchWorkItem) sí van con --worktree (Plan 02
       // WT-01 + D-06b). Solo el orchestrator queda exento.
       // ─────────────────────────────────────────────────────────────────────
       const claudeCmd = [
         'claude',
         '--model', config.claude.default_model,
         '--session-id', sessionId,
         ...config.claude.flags,
         `'${escapedPrompt}'`,
       ].join(' ');
       ```

       NO modificar el array `claudeCmd` — sigue idéntico al estado actual.

    2. **Crear `test/orchestrator-launch-isolation.test.js`** (archivo nuevo). Plantilla basada en `test/dispatcher-isolation.test.js`:

       ```javascript
       // @ts-check
       //
       // Phase 18 D-06 source-hygiene guard.
       // Asegura que src/orchestrator/launch.js NO emite --worktree en su cmd
       // claude. El comentario in-file sí puede mencionarlo (stripComments).
       //
       // Driver: launchOrchestrator necesita cwd=repo para auto-cargar
       // .claude/skills/kodo-orchestrate/skill.md (Phase 999.1 D-05).

       import { describe, it } from 'node:test';
       import assert from 'node:assert/strict';
       import { readFileSync } from 'node:fs';
       import { join, dirname } from 'node:path';
       import { fileURLToPath } from 'node:url';

       const __dirname = dirname(fileURLToPath(import.meta.url));
       const SRC = join(__dirname, '..', 'src', 'orchestrator', 'launch.js');

       /**
        * Strip block-comments (slash-star-star-slash) and line-comments (//)
        * + JSDoc continuation lines starting with *. Preserves runtime tokens.
        * @param {string} src
        */
       function stripComments(src) {
         return src
           .replace(/\/\*[\s\S]*?\*\//g, '')
           .split('\n')
           .filter((line) => !line.trim().startsWith('//') && !line.trim().startsWith('*'))
           .join('\n');
       }

       describe('orchestrator/launch.js — Phase 18 D-06 source-hygiene', () => {
         it('NEVER emits --worktree in runtime code (only in comments)', () => {
           const source = readFileSync(SRC, 'utf-8');
           const stripped = stripComments(source);
           assert.ok(
             !stripped.includes('--worktree'),
             'src/orchestrator/launch.js MUST NOT include --worktree in runtime code (Phase 18 D-06: cwd=repo for skill auto-load — Phase 999.1 D-05 constraint)',
           );
         });

         it('preserves cwd: process.cwd() (orchestrator opens in repo)', () => {
           const source = readFileSync(SRC, 'utf-8');
           assert.ok(
             /cwd:\s*process\.cwd\(\)/.test(source),
             'src/orchestrator/launch.js MUST set cwd: process.cwd() (Phase 999.1 D-05 — auto-load .claude/skills/kodo-orchestrate/skill.md)',
           );
         });

         it('documents Phase 18 D-06 exclusion in a comment', () => {
           const source = readFileSync(SRC, 'utf-8');
           assert.ok(
             /Phase 18 D-06/.test(source),
             'src/orchestrator/launch.js MUST document the D-06 exclusion as in-file comment for future maintainers',
           );
         });
       });
       ```
  </action>

  <verify>
    <automated>node --test test/orchestrator-launch-isolation.test.js test/orchestrator-gsd.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c -- "--worktree" src/orchestrator/launch.js` >= 1 (en el comentario; el test source-hygiene confirma 0 en runtime tras stripComments)
    - `grep -c "Phase 18 D-06" src/orchestrator/launch.js` >= 1 (decisión documentada)
    - `grep -c "cwd: process.cwd()" src/orchestrator/launch.js` == 1 (intacto Phase 999.1 D-05)
    - `node --test test/orchestrator-launch-isolation.test.js` exits 0 (3 asserts)
    - `node --test test/orchestrator-gsd.test.js` exits 0 (sin regresiones — el orchestrator sigue arrancando igual)
    - El array `claudeCmd` (líneas ~83-89) NO se modifica byte (golden bytes para la sesión orchestrator).
  </acceptance_criteria>

  <done>
    `src/orchestrator/launch.js` documenta la exclusión D-06 con comentario explícito y test source-hygiene `test/orchestrator-launch-isolation.test.js` blinda con grep + stripComments. Anti-regresión activa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender test/gsd-concurrency.test.js con coalesce + worktree (SC#3 integration)</name>
  <files>test/gsd-concurrency.test.js</files>

  <read_first>
    - test/gsd-concurrency.test.js (estado completo — confirmar setup tmpdir+HOME override existente, aserts sobre `gsd_locked` action, escenarios de 2 dispatches concurrentes)
    - src/triggers/dispatcher.js (post Task 1 — confirmar shape de return values)
    - src/session/state.js (post Plan 01 — confirmar `computeWorktreePath` para asertar paths esperados)
    - .planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md §SC#3 (lock vive en repo principal, NO worktree)
  </read_first>

  <behavior>
    - Test 1 (coalesce + worktree cableado): dos `dispatchTrigger` concurrentes sobre el MISMO repo → primero acquired (lock + sin colisión worktree porque path no existe) → segundo recibe `gsd_locked` ANTES de check de worktree (el lock rebote es por GSD-10, no por worktree). Aserts:
        - Primer dispatch: `action === 'launched'` o `action === 'worktree_collision'` (depende del orden de invocación; el primero que llega al check de existsSync NO encuentra path, el segundo TAMPOCO porque el primero NO crea el dir físicamente — el dir lo crea claude --worktree después de cmux.send). Por tanto en este test integration con stubs, AMBOS pasarían el check de worktree si no fuera por el lock.
        - Segundo dispatch: `action === 'gsd_locked'` con `holder` populated. Lock rebote PRECEDE collision check (orden en dispatcher: lock acquire línea 130 → collision check NUEVO línea ~141 → resolver línea 152). Verificar el orden de operaciones via stubs counters.
    - Test 2 (lock invariant WT-03 cross-call): después de los 2 dispatches concurrentes, leer el lock file `<projectPath>/.planning/.kodo.lock` y verificar:
        - El path es exactamente `<projectPath>/.planning/.kodo.lock` (NO `<worktreePath>/.planning/.kodo.lock`).
        - El contenido tiene `session_id` igual al gsdSessionId del primer dispatch (NO uno generado en collision-check para no-GSD, NO uno relacionado al worktree path).
    - Test 3 (lock release no toca worktree): tras release del primer dispatch (simulado por stop hook test), grep en el código verificable que `releaseGsdLock(projectPath, sessionId)` y NO `releaseGsdLock(worktreePath, sessionId)`.
    - Test 4 (no-GSD paralelo en mismo repo — D-06b): dos dispatches no-GSD sobre el mismo repo NO generan colisión de lock (no se adquiere lock en no-GSD, heredado v0.5). Cada uno computa su propio worktree path con UUID distinto, ambos pasan check `existsSync` → ambos llegan a `launchWorkItem`. Resuelve incidencia 28/04 ROMAN-113…118 a nivel de invariante.
  </behavior>

  <action>
    1. **Leer y entender** `test/gsd-concurrency.test.js` actual. Identificar:
       - Setup tmpdir + HOME override.
       - Patrón de stubs para `getProviderFn`, `acquireGsdLockFn`, `launchWorkItemFn`.
       - Escenarios coalesce existentes (probablemente 1-2 tests sobre `gsd_locked`).

    2. **Añadir un nuevo `describe('Phase 18 — coalesce con worktree cableado')`** con los 4 tests del bloque `<behavior>`. Reusar el setup tmpdir existente. Patrón:

       ```javascript
       describe('Phase 18 — coalesce con worktree cableado (WT-03 SC#3)', () => {
         let tmpRepo;
         beforeEach(() => {
           tmpRepo = mkdtempSync(join(tmpdir(), 'kodo-coalesce-wt-'));
           // .planning/ se crea automáticamente por acquireGsdLock (Pitfall 4 Phase 8)
         });
         afterEach(() => {
           rmSync(tmpRepo, { recursive: true, force: true });
         });

         it('GSD coalesce: second dispatch gets gsd_locked BEFORE worktree check (lock precedes collision in code path)', async () => {
           const stdDeps = makeStdDeps({ projectPath: tmpRepo });
           const event = makeEvent({ taskRef: 'KL-1', flags: ['gsd'] });

           const result1 = await dispatchTrigger(event, {}, stdDeps);
           assert.equal(result1.action, 'launched');

           // Segundo dispatch sobre la misma task — debería rebotar por lock, no por worktree.
           // (El primer dispatch dejó el lock acquired. El check de worktree del segundo
           // NUNCA se ejecuta porque el lock rebote retorna antes.)
           const result2 = await dispatchTrigger(event, {}, stdDeps);
           assert.equal(result2.action, 'gsd_locked');
           assert.ok(result2.holder, 'gsd_locked debe incluir holder');
           assert.equal(result2.holder.task_ref, 'KL-1');
         });

         it('lock file vive en projectPath/.planning/.kodo.lock, NO en worktree (WT-03 SC#3)', async () => {
           const stdDeps = makeStdDeps({ projectPath: tmpRepo });
           const event = makeEvent({ taskRef: 'KL-2', flags: ['gsd'] });

           await dispatchTrigger(event, {}, stdDeps);

           const lockPath = join(tmpRepo, '.planning', '.kodo.lock');
           assert.ok(existsSync(lockPath), `Lock debe vivir en ${lockPath}`);

           const lockContent = JSON.parse(readFileSync(lockPath, 'utf-8'));
           assert.match(lockContent.session_id, /^[a-f0-9-]+$/);

           // Confirmar que NO hay lock dentro del worktree path
           const worktreePath = join(tmpRepo, '.bg-shell', lockContent.session_id);
           const wrongLockPath = join(worktreePath, '.planning', '.kodo.lock');
           assert.ok(!existsSync(wrongLockPath), 'Lock NUNCA debe vivir dentro del worktree');
         });

         it('no-GSD parallel sobre mismo repo: ambos dispatches launchean (D-06b)', async () => {
           // Driver: incidencia 28/04 ROMAN-113…118. Worktrees distintos por
           // session-id permiten paralelismo sin contención (cada uno aislado).
           const stdDeps = makeStdDeps({ projectPath: tmpRepo });
           const event1 = makeEvent({ taskRef: 'KL-3' }); // sin flags GSD
           const event2 = makeEvent({ taskRef: 'KL-4' });

           const r1 = await dispatchTrigger(event1, {}, stdDeps);
           const r2 = await dispatchTrigger(event2, {}, stdDeps);

           assert.equal(r1.action, 'launched');
           assert.equal(r2.action, 'launched');
           // Ambas sesiones tienen sessionId distinto (worktree paths distintos)
           assert.notEqual(r1.session.session_id, r2.session.session_id);
         });

         it('lock invariant cross-callsite: NUNCA se pasa worktreePath a acquireGsdLock', () => {
           // Source-hygiene cross-source. Patrón Phase 16 LOG-13 dispatcher-isolation.
           const sources = [
             join(__dirname, '..', 'src', 'triggers', 'dispatcher.js'),
             join(__dirname, '..', 'src', 'session', 'manager.js'),
             join(__dirname, '..', 'src', 'hooks', 'stop.js'),
           ];
           function stripComments(src) {
             return src
               .replace(/\/\*[\s\S]*?\*\//g, '')
               .split('\n')
               .filter((line) => !line.trim().startsWith('//') && !line.trim().startsWith('*'))
               .join('\n');
           }
           for (const f of sources) {
             const src = readFileSync(f, 'utf-8');
             const stripped = stripComments(src);
             assert.ok(
               !/acquireGsdLockFn?\s*\([^)]*worktree/i.test(stripped),
               `${f} pasa worktree* a acquireGsdLock — VIOLA WT-03 SC#3`,
             );
             assert.ok(
               !/releaseGsdLockFn?\s*\([^)]*worktree/i.test(stripped),
               `${f} pasa worktree* a releaseGsdLock — VIOLA WT-03 SC#3`,
             );
           }
         });
       });
       ```

       Las funciones helper (`makeStdDeps`, `makeEvent`) son las del test existente — reutilizar; si no existen con ese nombre, adaptar a las helpers efectivas del archivo.

    3. **NO TOCAR** los tests existentes en `gsd-concurrency.test.js` — solo AÑADIR un nuevo `describe`. Si los tests existentes empiezan a fallar tras cambios de Plan 01/02/03 anteriores, eso es REGRESIÓN y el executor debe escalar (no cambiar los tests para que pasen).
  </action>

  <verify>
    <automated>node --test test/gsd-concurrency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/gsd-concurrency.test.js` exits 0 con los 4 nuevos asserts pasando
    - Tests existentes en gsd-concurrency.test.js siguen verdes (sin regresión)
    - El test "lock invariant cross-callsite" recorre 3 archivos source y NO falsa positivos por comentarios (stripComments funciona)
    - `npm test` exits 0 (suite global)
  </acceptance_criteria>

  <done>
    `test/gsd-concurrency.test.js` extendido con 4 asserts integration cubriendo: coalesce con worktree, lock vive en repo, no-GSD paralelo (driver incidencia ROMAN-113), invariante cross-callsite. SC#3 doblemente blindado (grep + integration).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → `existsSync(worktreePath)` | El worktreePath se computa internamente desde `dispatchProjectPath` (config humano) + `randomUUID()`. Sin input externo de red. existsSync es read-only — no muta filesystem. |
| dispatcher → `releaseGsdLockFn` on collision | El release se llama solo si `gsdSessionId` y `gsdProjectPath` truthy (es decir, solo si lock fue acquired). Idempotente por D-09 Phase 8. Sin riesgo de release del lock de otra sesión. |
| `launchOrchestrator` → cmd claude | Cmd construido con `randomUUID()` sessionId y `escapedPrompt`. Ningún input externo se interpola sin escape. |
| Lock file path ↔ worktree path | INVARIANTE WT-03: nunca cruzan. Lock vive en `<projectPath>/.planning/.kodo.lock` (Phase 8 D-05). Worktree vive en `<projectPath>/.bg-shell/<sessionId>/` (Phase 18 D-01). Misma raíz `projectPath`, distinto subdir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering / Privilege Escalation | callsite pasa accidentalmente `worktreePath` a `acquireGsdLockFn` → lock vive dentro del worktree → coalescencia rota (cada worktree = lock distinto = paralelismo no controlado) | mitigate | Test `lock invariant cross-callsite` (Task 3) escanea dispatcher/manager/stop con grep + stripComments. Falla loud si futura PR introduce `acquireGsdLockFn(worktree...` o `releaseGsdLockFn(worktree...`. SC#3 invariante blindado. |
| T-18-06 | Bypass | desarrollador añade `--worktree` a `launchOrchestrator` → orchestrator arranca en worktree → skill `kodo-orchestrate` no se carga → fallback degradado silencioso | mitigate | Test `orchestrator-launch-isolation.test.js` con grep + stripComments. Bloquea PRs que añaden `--worktree` en runtime. Comentario in-file (Task 2) educa al lector ANTES de que escriba el cambio. |
| T-18-07 | TOCTOU | Entre `existsSync(worktreePath)` y `cmux.send(claudeCmd)`, otro proceso podría crear el dir | accept | Window de TOCTOU es ms-scale; el `inFlight` Set del dispatcher cubre dispatches concurrentes del propio kodo. Un proceso EXTERNO creando `.bg-shell/<random-uuid>/` justo en ese intervalo es astronómicamente improbable (UUIDs v4 son 122 bits de entropía). Si pasa, claude --worktree fallará con su propio error (que el agente verá en cmux). NO se introduce filesystem-level locking aquí — sería overkill. |
| T-18-08 | Information Disclosure | `worktree_collision` detail leakea path absoluto via stderr/return | accept | El path contiene `<projectPath>` (config humano, no secreto) + UUID (random, no identificable). Mismo modelo de threat que `gsd_locked` ya emite con `holder.task_ref`. Sin nueva superficie. |
| T-18-09 | DoS | Atacante pre-crea `<projectPath>/.bg-shell/<uuid>/` para todos los UUIDs posibles → todas las dispatches devuelven `worktree_collision` | accept | UUIDs v4 son 122 bits — espacio inadressable. Atacante necesitaría acceso al filesystem (ya tiene control total — threat fuera del modelo de kodo). Personal-use tool sin multi-tenant. |
| T-18-10 | Repudiation | Si check `existsSync` falla con `EACCES` o similar, exception silente | mitigate | `existsSync` retorna `false` ante errores de permisos según docs Node.js (no throw). Si en algún OS exótico throws, el dispatch caerá en catch outer del server (línea 326-334) que liberará el lock GSD (no leak). Comportamiento gracioso heredado. |
</threat_model>

<verification>
- `npm test` exits 0 (suite global completa) — sin regresiones.
- Smoke manual (post-execute, opcional si setup local disponible):
  - Lanzar 2 sesiones GSD concurrentes contra el mismo repo. Verificar que la segunda recibe `gsd_locked` (cmux/stderr de kodo). Lock file `<repo>/.planning/.kodo.lock` existe con `session_id` de la primera.
  - Pre-crear `<repo>/.bg-shell/test-uuid/` y forzar dispatch con sessionId conocido (test fixture). Verificar que retorna `worktree_collision` y stderr exacto.
  - Lanzar `kodo orchestrator` desde `~/dev/klab/kodo` y verificar que la sesión NO arranca en `.bg-shell/<uuid>/` — debe arrancar en repo y auto-cargar la skill.
- Cross-grep WT-03: `grep -rE "acquireGsdLockFn?\s*\(\s*[a-zA-Z_]*[wW]orktree|releaseGsdLockFn?\s*\(\s*[a-zA-Z_]*[wW]orktree" src/` retorna 0 matches.
- Cross-grep D-06: `grep -c -- "--worktree" src/orchestrator/launch.js` >= 1 (comentario) PERO con stripComments == 0 (Test 1 Task 2).
</verification>

<success_criteria>
- **WT-01 (validation completion):** `--worktree` cableado en TODAS las sesiones de `launchWorkItem` (Plan 02) Y excluido de `launchOrchestrator` (Plan 03 Task 2). Verificable cross-source.
- **WT-03 (SC#3 invariant):** lock per-repo nunca recibe worktreePath. Doblemente blindado: (a) Task 3 grep cross-callsite, (b) Task 3 integration test 2-dispatches-1-repo coalesce.
- **D-05/D-05b canonical error:** `worktree_collision` action + code + detail + stderr deterministic. Lock release on collision (no leak).
- **D-06 documentation + enforcement:** comentario in-file `Phase 18 D-06` + test source-hygiene `orchestrator-launch-isolation.test.js`.
- **D-06b:** sesiones no-GSD también con worktree (heredado vía Plan 02), sin lock per-repo. Test 4 Task 3 confirma paralelismo no-GSD funcional sobre mismo repo.
- **Sin regresiones:** suite global verde. Phase 8 GSD-10 invariantes intactos. Phase 12 QUICK-07 golden bytes intactos.
</success_criteria>

<output>
Crear `.planning/phases/18-worktree-runtime-wiring/18-03-SUMMARY.md` documentando: (a) líneas exactas modificadas en dispatcher.js (collision block + threading dispatchSessionId), (b) `DispatchDeps.existsSyncFn` añadido, (c) decisiones del planner sobre code/action strings (`worktree_collision`/`worktree_exists`), (d) confirmación cross-grep WT-03 (lock NUNCA toca worktree), (e) confirmación D-06 (orchestrator NO --worktree).
</output>
