---
phase: 22-tech-debt-v0-5-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/triggers/dispatcher.js
  - src/session/manager.js
  - src/hooks/stop.js
  - src/gsd/verify.js
  - test/stop-state-transition.test.js
  - test/gsd-verify-integration.test.js
  - test/dispatcher-isolation.test.js
  - test/dispatcher.test.js
autonomous: true
requirements:
  - DEBT-05
  - DEBT-06
user_setup: []

must_haves:
  truths:
    - "DEBT-05 WR-01 (stop.js doble logger sessionEnd/markSessionStatus): satisfied-by-Phase-19 (1 logger reusado entre L171 + L183; verificado in-plan)."
    - "DEBT-05 WR-02 (stop.js catch-todo): satisfied-by-Phase-19 (línea 175 emite console.error con diagnóstico explícito; verificado in-plan)."
    - "DEBT-05 WR-03 (verify.js triple child binding): satisfied-by-existing-tests (R-04 confirma que el merge se valida indirectamente por from='review'/'running' en stop-state-transition Tests 1-3); se documenta como satisfied en SUMMARY."
    - "DEBT-05 WR-04: test/stop-state-transition.test.js Test D-04 invariante MANDATORY asserta tanto 'to' como 'from' (paramétrico por modo)."
    - "DEBT-05 WR-05: test/gsd-verify-integration.test.js T27 incluye comentario JSDoc claration sobre el alcance del test (order, no presence — T20 cubre presence)."
    - "DEBT-05 WR-06: src/triggers/dispatcher.js línea 13 importa eager EVENTS + gsdPhaseResolved + gsdBootstrap; la línea 307 dynamic import desaparece (helpers ya en scope)."
    - "DEBT-05 WR-07: src/session/manager.js#markSessionStatus retorna early si listSessions().find no encuentra session (no emite state.transition con from='unknown' spurious)."
    - "DEBT-05 WR-08: test/dispatcher-isolation.test.js JSDoc de stripComments documenta la limitación 'inline comments NOT stripped' (D-04b refactor puro)."
    - "DEBT-06 IN-01: src/hooks/stop.js#runStopHook JSDoc documenta el patrón 'dynamic import lazy DI' para releaseGsdLock/handleOrchestratorStop (D-02b documentar, NO refactor)."
    - "DEBT-06 IN-02: test/dispatcher.test.js incluye al menos un assert deepEqual sobre el payload canonical de gsd.phase.resolved (keys event/matched/mode + uno de phase_id|code|error_code)."
    - "DEBT-06 IN-03 (verify.js comment 'header line 26'): satisfied-by-Phase-16-CR-01 (grep negativo verde, verificado in-plan)."
    - "DEBT-06 IN-04 (stop.js comment 'line 116'): satisfied-by-Phase-19 (grep negativo verde, verificado in-plan)."
    - "Pitfall #6 Opción A invariante: exit codes deterministas de verify.js preservados (WR-07 fix no toca verify.js)."
    - "Lock release idempotencia (Phase 8 GSD-10): preservada — WR-07 fix solo añade early-return en manager.js, no toca lock release path."
    - "Source-hygiene LOG-13 (dispatcher EVENTS migration) intacto."
  artifacts:
    - path: "src/triggers/dispatcher.js"
      provides: "Imports consolidados eager (EVENTS + gsdPhaseResolved + gsdBootstrap); línea 307 dynamic import eliminada."
      contains: "import { EVENTS, gsdPhaseResolved, gsdBootstrap }"
    - path: "src/session/manager.js"
      provides: "markSessionStatus con early-return si session no existe (WR-07 fix ~5 LOC en líneas 352-360)."
      contains: "if (!current)"
    - path: "src/hooks/stop.js"
      provides: "JSDoc de runStopHook documenta lazy DI pattern (IN-01 doc, sin refactor de signature)."
      contains: "dynamic import for lazy DI"
    - path: "test/stop-state-transition.test.js"
      provides: "Test D-04 invariante MANDATORY asserta from paramétrico por modo (WR-04)."
      contains: "expectedFrom"
    - path: "test/gsd-verify-integration.test.js"
      provides: "T27 con JSDoc claration sobre order vs presence (WR-05 caveat documentado)."
      contains: "T27 cubre order, no presence"
    - path: "test/dispatcher-isolation.test.js"
      provides: "stripComments JSDoc actualizado (WR-08 D-04b refactor puro)."
      contains: "inline comments at end of code lines are NOT stripped"
    - path: "test/dispatcher.test.js"
      provides: "Assert deepEqual sobre payload canonical gsd.phase.resolved (IN-02)."
      contains: "deepEqual"
  key_links:
    - from: "src/triggers/dispatcher.js#imports (línea 13)"
      to: "src/logger-events.js"
      via: "import eager { EVENTS, gsdPhaseResolved, gsdBootstrap }"
      pattern: "import\\\\s+\\\\{\\\\s*EVENTS,\\\\s*gsdPhaseResolved,\\\\s*gsdBootstrap\\\\s*\\\\}\\\\s+from\\\\s+'\\\\.\\\\.\\\\/logger-events\\\\.js'"
    - from: "src/session/manager.js#markSessionStatus"
      to: "early-return guard"
      via: "if (!current) return"
      pattern: "if\\\\s*\\\\(\\\\s*\\\\!current\\\\s*\\\\)"
    - from: "test/stop-state-transition.test.js#D-04 invariante MANDATORY"
      to: "session.gsd_mode → expectedFrom"
      via: "ternary 'review' (full) | 'running' (quick) + assert.equal transition.fields.from"
      pattern: "expectedFrom"
---

<objective>
Cerrar la deuda Phase 16 (DEBT-05 + DEBT-06) tras la re-validación post-Phase-19/20/21. **Scope efectivo: 7 fixes activos + 4 satisfied-by-existing.**

**Satisfied-by-existing (verificados por planner, NO crear tasks — citados en `must_haves` para el coverage gate):**
- WR-01 stop.js (doble logger): resuelto por Phase 19 — solo 1 `const log = ...` en líneas 159-167, reusado en markSessionStatus (L171) y sessionEnd (L183).
- WR-02 stop.js (catch-todo): resuelto por Phase 19 — `console.error('[kodo:stop] markSessionStatus failed: ...')` línea 175.
- IN-03 verify.js (comment "header line 26"): resuelto por Phase 16 CR-01 — grep negativo confirmado.
- IN-04 stop.js (comment "line 116"): resuelto por Phase 19 refactor — grep negativo confirmado.
- WR-03 verify.js (triple child binding): satisfied-by-existing-tests (per R-04 RESEARCH — los tests Phase 16 LOG-13/14/15 ya validan indirectamente el merge correcto via `from` field).

**Fixes activos (7 tasks por D-08 agrupación por archivo):**
- WR-06 (dispatcher.js doble import) — Task 1.
- WR-07 (manager.js markSessionStatus early-return) — Task 2.
- IN-01 (stop.js JSDoc lazy DI documentación) — Task 3.
- WR-04 (stop-state-transition.test.js from assert) — Task 4.
- WR-05 (gsd-verify-integration.test.js T27 claration) + IN-02 (dispatcher.test.js payload deepEqual) + WR-08 (dispatcher-isolation.test.js JSDoc stripComments) — Task 5 (3 fixes triviales agrupados por surface: tests).

Purpose: cierra el Resolution Log Phase 16 sin tocar runtime observable. Cada fix cita su WR/IN ID en el commit (per D-09). Suite global delta esperado: +2 asserts en stop-state-transition, +1 assert en dispatcher.test.js, sin tests nuevos completos.

Output: 8 archivos modificados, suite verde, golden bytes / lock idempotencia / Pitfall #6 preservados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-tech-debt-v0-5-closure/22-CONTEXT.md
@.planning/phases/22-tech-debt-v0-5-closure/22-RESEARCH.md
@.planning/milestones/v0.5-phases/16-log-09-debt-cleanup/16-REVIEW.md
@src/triggers/dispatcher.js
@src/session/manager.js
@src/hooks/stop.js
@src/gsd/verify.js
@test/stop-state-transition.test.js
@test/gsd-verify-integration.test.js
@test/dispatcher-isolation.test.js
@test/dispatcher.test.js

<interfaces>
<!-- Estado actual verificado por planner -->

src/triggers/dispatcher.js (estado actual relevante):
- Línea 13: `import { EVENTS } from '../logger-events.js';` (eager — único helper importado eager hoy)
- Línea 254: `const { createLogger } = await import('../logger.js');` (dynamic — DEJAR INTACTO, LOG-12 invariant)
- Línea 281: `const { createLogger } = await import('../logger.js');` (dynamic — DEJAR INTACTO)
- Línea 306: `const { createLogger } = await import('../logger.js');` (dynamic — DEJAR INTACTO)
- Línea 307: `const { gsdPhaseResolved, gsdBootstrap } = await import('../logger-events.js');` (dynamic — A ELIMINAR; los helpers se consolidan eager en línea 13)

src/session/manager.js#markSessionStatus (líneas 352-360 estado actual):
```js
export function markSessionStatus(taskId, nextStatus, reason, logger) {
  const current = listSessions().find((s) => s.task_id === taskId || s.task_ref === taskId);
  const fromStatus = current?.status || 'unknown';
  updateSession(taskId, { status: nextStatus });
  if (logger) {
    const log = logger.child({ component: 'session', task_id: taskId });
    stateTransition(log, { from: fromStatus, to: nextStatus, reason });
  }
}
```

Estado RESULTANTE esperado:
```js
export function markSessionStatus(taskId, nextStatus, reason, logger) {
  const current = listSessions().find((s) => s.task_id === taskId || s.task_ref === taskId);
  if (!current) {
    // WR-07 Phase 16: si la session no existe en state.json, NO emitimos state.transition
    // (un from='unknown' sería ruido forensic). updateSession sigue ejecutando — es idempotente
    // sobre IDs ausentes y los callers (stop.js) ya tienen fallback en console.error.
    if (logger) {
      logger.warn('state.transition.skipped', { task_id: taskId, reason: 'session-not-found' });
    }
    return;
  }
  const fromStatus = current.status;
  updateSession(taskId, { status: nextStatus });
  if (logger) {
    const log = logger.child({ component: 'session', task_id: taskId });
    stateTransition(log, { from: fromStatus, to: nextStatus, reason });
  }
}
```

src/hooks/stop.js#runStopHook JSDoc (líneas ~71-97 estado actual — leído de RESEARCH.md):
- Lista findSessionFn, removeSessionFn, cmux, loggerFactory, gitFn.
- NO menciona releaseGsdLock ni handleOrchestratorStop ni markSessionStatus (dynamic imports L170, L198, L425 aprox).

Estado RESULTANTE: añadir párrafo JSDoc al final del bloque de deps documentation (antes de `@param` o donde corresponda) que documenta el patrón lazy DI.

test/stop-state-transition.test.js Test 4 D-04 invariante MANDATORY (líneas 316-339 estado actual — leído de RESEARCH.md):
```js
for (const session of [fullSession, quickSession]) {
  // ... setup ...
  const transition = events.find((e) => e.fields?.event === 'state.transition');
  assert.ok(transition, `D-04 invariante: modo ${session.gsd_mode} debe emitir state.transition`);
  assert.equal(transition.fields.to, 'done',
    `D-04 LOCKED: to debe ser 'done' fijo (modo ${session.gsd_mode})`);
  // FALTA assert sobre `from`
}
```

Estado RESULTANTE: añadir DENTRO del loop:
```js
const expectedFrom = session.gsd_mode === 'full' ? 'review' : 'running';
assert.equal(
  transition.fields.from,
  expectedFrom,
  `WR-04 Phase 16: from debe ser '${expectedFrom}' para modo ${session.gsd_mode} — Test D-04 no debe ser estructuralmente débil`
);
```

test/gsd-verify-integration.test.js T27 (líneas 364-412, "T27 SC#3 LOG-14: pass + updateTaskState fails → NO state.transition emitted") — estado actual asserta `transition === undefined`.

Estado RESULTANTE: añadir comentario JSDoc al inicio del `it(...)`:
```js
/**
 * WR-05 Phase 16 — Test scope claration:
 * Este test cubre ORDER (markSessionStatus emite ANTES del throw)
 * NO cubre PRESENCE (que markSessionStatus se invoca en el pass branch).
 * El test T20 ("pass + Plane OK → state.transition emitted") cubre presence.
 * Si un refactor mueve markSessionStatus DESPUÉS del try/catch, T27 fallaría
 * (transition !== undefined). DI explícito de markSessionStatus para spy
 * cambia signature pública (out of scope — diferido a v0.7+).
 */
```

test/dispatcher-isolation.test.js stripComments (líneas 13-30 estado actual):
- JSDoc actual no documenta limitación inline.

Estado RESULTANTE: actualizar JSDoc a:
```js
/**
 * Strip block comments (slash-star to star-slash) and full-line // or leading-* comments.
 *
 * NOTE (WR-08 Phase 16): inline comments at end of code lines are NOT stripped.
 * Esto es intencional — si un test mencionara 'gsd.phase.resolved' en un
 * comentario inline al final de una línea de código, el match sobre el código
 * sería válido y el test debería capturarlo. Mantén los comentarios sobre
 * literales en líneas dedicadas.
 */
```

test/dispatcher.test.js (estructura): contiene tests `gsd.phase.resolved` referenciados en comentarios pero NO assertion deepEqual sobre el payload del helper. El fix IN-02 añade UN test nuevo al final del describe principal que capture el payload runtime emitted por el helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-06 — consolidar imports eager en dispatcher.js</name>
  <files>src/triggers/dispatcher.js</files>
  <read_first>
    src/triggers/dispatcher.js (líneas 1-20 imports + líneas 300-315 contexto de la dynamic import a eliminar)
  </read_first>
  <behavior>
    - Tras el cambio, `gsdPhaseResolved` y `gsdBootstrap` están disponibles en module-scope desde la primera invocación de `dispatchTrigger`.
    - `createLogger` SIGUE siendo dynamic import (LOG-12 invariante — dispatcher no carga logger.js en module-load path; las líneas 254, 281, 306 NO se tocan).
    - Tests existentes de `dispatcher.test.js` siguen verdes (los helpers se invocan en el mismo punto del runtime, solo cambia DÓNDE se importan).
    - La línea 307 desaparece completa (no se reemplaza por nada — los nombres ya están en scope).
  </behavior>
  <action>
    Editar `src/triggers/dispatcher.js`:

    **Cambio 1 — línea 13:**
    DE: `import { EVENTS } from '../logger-events.js';`
    A:  `import { EVENTS, gsdPhaseResolved, gsdBootstrap } from '../logger-events.js';`

    **Cambio 2 — línea 307 (eliminar línea entera):**
    DE: `      const { gsdPhaseResolved, gsdBootstrap } = await import('../logger-events.js');`
    A:  (eliminar la línea — los símbolos ya están en scope desde el import eager línea 13)

    **NO TOCAR:**
    - Líneas 254, 281, 306 (`createLogger` dynamic imports) — LOG-12 invariante.
    - Cualquier uso de `gsdPhaseResolved(...)` o `gsdBootstrap(...)` downstream — siguen siendo invocaciones sincrónicas; solo cambió cuándo se cargó el módulo.

    Comentario en el commit message: `fix(22-03): WR-06 consolidar eager imports en dispatcher.js (logger-events helpers)`.

    Karpathy regla 3 — surgical, ~2 líneas de cambio en total.
  </action>
  <verify>
    <automated>node --test test/dispatcher.test.js test/dispatcher-isolation.test.js</automated>
  </verify>
  <done>
    - `grep -n "import { EVENTS, gsdPhaseResolved, gsdBootstrap }" src/triggers/dispatcher.js` → 1 match en línea 13.
    - `grep -c "await import('../logger-events.js')" src/triggers/dispatcher.js` → 0.
    - `grep -c "await import('../logger.js')" src/triggers/dispatcher.js` → 3 (líneas 254, 281, 306; createLogger dynamic preservado).
    - Tests pasan verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-07 — markSessionStatus early-return en manager.js</name>
  <files>
    src/session/manager.js,
    test/stop-state-transition.test.js
  </files>
  <read_first>
    src/session/manager.js (líneas 340-365: definición completa de markSessionStatus),
    test/stop-state-transition.test.js (estructura de los fixtures — patron mkdtempSync + HOME override)
  </read_first>
  <behavior>
    - Caso A (session existe): comportamiento idéntico al actual — `updateSession` + emit `state.transition` con `from = current.status`.
    - Caso B (session NO existe, listSessions().find devuelve undefined): NO se emite `state.transition`, `updateSession` no se invoca (porque sería un no-op idempotente sobre un id ausente — la `state.json` no lo modifica), y SI hay logger se emite un `logger.warn('state.transition.skipped', { task_id, reason: 'session-not-found' })`.
    - El test nuevo capta el caso B contra fixture vacío (tmpdir HOME sin `state.json` poblado): assert que `events.find((e) => e.fields?.event === 'state.transition')` es `undefined` después de invocar `markSessionStatus('unknown-task', 'done', 'test-noop', log)`.
    - Tests existentes en stop-state-transition (Tests 1-4) siguen verdes — el early-return solo afecta cuando session NO existe, y esos tests siempre seedean fixtures con la session presente.
  </behavior>
  <action>
    **Sub-task 2a — modificar `src/session/manager.js#markSessionStatus`:**

    Reemplazar el cuerpo de la función (líneas 352-360 aprox) por:

    ```js
    export function markSessionStatus(taskId, nextStatus, reason, logger) {
      const current = listSessions().find((s) => s.task_id === taskId || s.task_ref === taskId);
      if (!current) {
        // WR-07 Phase 16: si listSessions().find no encontró la session, NO emitimos
        // state.transition (un from='unknown' sería ruido forensic). updateSession también
        // se omite — es idempotente sobre IDs ausentes pero el caller (stop.js L175) ya
        // emite console.error explícito en este path. Solo el warn diagnóstico.
        if (logger) {
          logger.warn('state.transition.skipped', { task_id: taskId, reason: 'session-not-found' });
        }
        return;
      }
      const fromStatus = current.status;
      updateSession(taskId, { status: nextStatus });
      if (logger) {
        const log = logger.child({ component: 'session', task_id: taskId });
        stateTransition(log, { from: fromStatus, to: nextStatus, reason });
      }
    }
    ```

    **Sub-task 2b — añadir test nuevo en `test/stop-state-transition.test.js`:**

    Localizar el último `describe` o `it` del archivo y AÑADIR al final un nuevo `describe`:

    ```js
    describe('WR-07 Phase 16 — markSessionStatus early-return on missing session', () => {
      let tmpHome;
      const origHome = process.env.HOME;

      beforeEach(async () => {
        tmpHome = mkdtempSync(join(tmpdir(), 'wr07-'));
        process.env.HOME = tmpHome;
        // No seed: state.json vacío → listSessions().find devuelve undefined
      });

      afterEach(() => {
        process.env.HOME = origHome;
        // tmpdir cleanup opcional — patrón en otros tests
      });

      it('does NOT emit state.transition when session is absent (WR-07)', async () => {
        const events = [];
        const log = {
          warn: (msg, fields) => events.push({ level: 'warn', msg, fields }),
          child: () => log,
        };
        const { markSessionStatus } = await import('../src/session/manager.js');
        markSessionStatus('nonexistent-task-id', 'done', 'wr07-test', log);
        const transition = events.find((e) => e.fields?.event === 'state.transition');
        assert.equal(transition, undefined, 'WR-07: state.transition NO se emite si la session no existe');
        const skipped = events.find((e) => e.msg === 'state.transition.skipped');
        assert.ok(skipped, 'WR-07: warn state.transition.skipped SÍ se emite con diagnóstico');
        assert.equal(skipped.fields.reason, 'session-not-found');
      });
    });
    ```

    **NOTAS:**
    - El sink `log` es minimal — solo necesita `warn` + `child`. El `stateTransition` helper jamás se invoca en este path (early return).
    - Verificar imports en top del archivo: `mkdtempSync`, `tmpdir`, `join` — si ya están (patrón Phase 16) reusar; si no, añadir.
    - El comentario JSDoc al inicio del describe ata el test al WR-07 (D-09).

    Karpathy regla 1: hacer explícito que el `updateSession` también se omite — es decisión deliberada (no es un side-effect que se quiera preservar).
    Karpathy regla 3: ~10 LOC en `src/session/manager.js`, ~25 LOC en el test nuevo.

    **Lock idempotencia (Phase 8 GSD-10) — NO afectada:** WR-07 fix vive en `markSessionStatus`, NO en `releaseGsdLock`. El stop.js sigue invocando `releaseGsdLock(...)` independientemente — solo cambia que `markSessionStatus` no emite ruido cuando la session ya fue removida antes.
  </action>
  <verify>
    <automated>node --test test/stop-state-transition.test.js</automated>
  </verify>
  <done>
    - `grep -c 'if (!current)' src/session/manager.js` → 1.
    - `grep -c 'state.transition.skipped' src/session/manager.js` → 1.
    - `grep -c "WR-07 Phase 16" test/stop-state-transition.test.js` → >=1.
    - `node --test test/stop-state-transition.test.js` exit 0.
    - Tests existentes Tests 1-4 D-04 invariante siguen verdes (no afectados).
    - Lock release path en stop.js intacto.
  </done>
</task>

<task type="auto">
  <name>Task 3: IN-01 — documentar lazy DI pattern en stop.js JSDoc</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    src/hooks/stop.js (líneas 60-100: JSDoc de runStopHook con los @param de deps)
  </read_first>
  <action>
    Localizar el bloque JSDoc de `runStopHook` (declaración antes de `export async function runStopHook(input, deps = {})`). El bloque lista los deps inyectables actuales (`findSessionFn`, `removeSessionFn`, `cmux`, `loggerFactory`, `gitFn`).

    AÑADIR un párrafo de documentación AL FINAL del bloque JSDoc (después de los `@param` y antes del cierre `*/`), con el contenido siguiente:

    ```
     *
     * **Lazy DI pattern (IN-01 Phase 16 documentado vía Phase 22):**
     *
     * Los siguientes módulos se importan dinámicamente y NO son inyectables vía `deps`:
     *
     *   - `markSessionStatus` (línea 170 aprox) — `await import('../session/manager.js')`
     *   - `releaseGsdLock`     (línea 198 aprox) — `await import('../gsd/lock.js')`
     *   - `handleOrchestratorStop` (en `runStopHook` orchestrator branch) — dynamic import
     *
     * Razón: lazy DI difiere la carga del módulo hasta call-time del hook, evitando que
     * `gsd/lock.js` o `session/manager.js` entren al module-load graph de `stop.js`. Esto
     * mantiene `stop.js` ligero al import-time (relevante para tests Phase 16 LOG-13/14/15
     * que importan `runStopHook` sin requerir `gsd/lock.js` en su grafo).
     *
     * Refactor a DI explícito requiere ampliar la signature `deps` (breaking change para
     * tests Phase 16) — diferido a v0.7+. Esta nota cierra DEBT-06 IN-01 por documentación
     * (D-02b Phase 22 CONTEXT.md: documentar > refactor cuando el refactor implica breaking).
     *
    ```

    NO modificar nada más en la función. NO añadir el patrón en otros archivos. NO refactorizar la signature.

    Esto cumple D-02b (CONTEXT.md): "Si IN-01 Phase 16 amerita refactor de runStopHook o solo documentación inline ... si refactor implica cambiar signature → documentar".
  </action>
  <verify>
    <automated>node --test test/stop-state-transition.test.js && grep -c 'dynamic import for lazy DI' src/hooks/stop.js</automated>
  </verify>
  <done>
    - `grep -c 'Lazy DI pattern' src/hooks/stop.js` → 1.
    - `grep -c 'IN-01 Phase 16' src/hooks/stop.js` → 1.
    - `node --test test/stop-state-transition.test.js` verde (sin cambios runtime).
    - Signature de `runStopHook` INTACTA.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: WR-04 — Test D-04 invariante asserta 'from' paramétrico</name>
  <files>test/stop-state-transition.test.js</files>
  <read_first>
    test/stop-state-transition.test.js (líneas 280-345: bloque del Test "D-04 invariante MANDATORY" con el loop for fullSession/quickSession)
  </read_first>
  <behavior>
    - Test D-04 invariante sigue verde con el assert original (`to === 'done'`).
    - Nuevo assert añadido DENTRO del mismo loop: `from === 'review'` para gsd_mode='full', `from === 'running'` para gsd_mode='quick'.
    - Si alguna regresión futura emite `from: 'unknown'` (caso WR-07) o `from: 'review'` para quick, el test falla loud con mensaje que cita WR-04 Phase 16.
  </behavior>
  <action>
    Localizar el bloque del Test "D-04 invariante MANDATORY" (líneas ~316-339 según RESEARCH.md). El loop actual es:

    ```js
    for (const session of [fullSession, quickSession]) {
      // setup específico de cada modo ...
      const transition = events.find((e) => e.fields?.event === 'state.transition');
      assert.ok(transition, `D-04 invariante: modo ${session.gsd_mode} debe emitir state.transition`);
      assert.equal(transition.fields.to, 'done',
        `D-04 LOCKED: to debe ser 'done' fijo (modo ${session.gsd_mode})`);
    }
    ```

    AÑADIR (después del `assert.equal(transition.fields.to, ...)`, antes del cierre `}` del loop) las siguientes 2 líneas:

    ```js
      const expectedFrom = session.gsd_mode === 'full' ? 'review' : 'running';
      assert.equal(
        transition.fields.from,
        expectedFrom,
        `WR-04 Phase 16: from debe ser '${expectedFrom}' para modo ${session.gsd_mode} — Test D-04 invariante MANDATORY ya NO es estructuralmente débil`
      );
    ```

    NO modificar la estructura del loop, NO renombrar variables, NO añadir setup adicional. Los fixtures `fullSession` y `quickSession` ya garantizan que el estado pre-transición es 'review' / 'running' respectivamente (Tests 1 y 2 ya los seedean así).
  </action>
  <verify>
    <automated>node --test test/stop-state-transition.test.js</automated>
  </verify>
  <done>
    - `grep -c 'expectedFrom' test/stop-state-transition.test.js` → >=1.
    - `grep -c 'WR-04 Phase 16' test/stop-state-transition.test.js` → >=1.
    - El Test "D-04 invariante MANDATORY" pasa verde con los nuevos asserts.
  </done>
</task>

<task type="auto">
  <name>Task 5: WR-05 + WR-08 + IN-02 — caveats de tests (3 fixes triviales agrupados)</name>
  <files>
    test/gsd-verify-integration.test.js,
    test/dispatcher-isolation.test.js,
    test/dispatcher.test.js
  </files>
  <read_first>
    test/gsd-verify-integration.test.js (líneas 360-415: bloque del Test T27),
    test/dispatcher-isolation.test.js (líneas 1-40: estructura + JSDoc actual de stripComments),
    test/dispatcher.test.js (estructura final — localizar dónde añadir el assert payload IN-02)
  </read_first>
  <action>
    **Sub-task 5a — WR-05 (test/gsd-verify-integration.test.js T27 claration):**

    Localizar el `it('T27 SC#3 LOG-14: pass + updateTaskState fails → NO state.transition emitted', ...)` (líneas ~364-412). AÑADIR un comentario JSDoc inmediatamente ANTES del `it(...)` (mismo nivel de indent que el `it`):

    ```js
    /**
     * WR-05 Phase 16 — Test scope claration:
     * Este test cubre ORDER (markSessionStatus emite ANTES del throw de updateTaskState).
     * NO cubre PRESENCE (que markSessionStatus se invoca en el pass branch del verify).
     * El test T20 ("pass + Plane OK → state.transition emitted") cubre presence.
     *
     * Si un refactor mueve markSessionStatus DESPUÉS del try/catch del updateTaskState,
     * T27 fallaría loud (transition !== undefined) — el contrato pre-throw permanece blindado.
     *
     * DI explícito de markSessionStatus para spy literal cambia signature pública de
     * runGsdVerify y es out of scope (Phase 22 D-04b: refactors puros no requieren spy nuevo).
     */
    ```

    NO modificar el cuerpo del `it`. NO añadir asserts nuevos. Solo el comentario que cierra WR-05 documentando el scope (D-04b: refactor puro / claration sin behavior change).

    **Sub-task 5b — WR-08 (test/dispatcher-isolation.test.js stripComments JSDoc):**

    Localizar el JSDoc del helper `stripComments` (líneas ~13-22 según RESEARCH.md). REEMPLAZAR el bloque JSDoc por:

    ```js
    /**
     * Strip block comments (slash-star to star-slash) and full-line // or leading-* comments.
     *
     * NOTE (WR-08 Phase 16 closure via Phase 22): inline comments at end of code lines
     * are NOT stripped. Esto es intencional — si un test futuro mencionara
     * 'gsd.phase.resolved' en un comentario inline al final de una línea de código, el
     * match sobre el código real sería válido y el test debería capturarlo. Para evitar
     * falsos positivos, mantén las menciones literales en líneas dedicadas (//-only).
     *
     * @param {string} src
     * @returns {string}
     */
    ```

    NO modificar el cuerpo de `stripComments` ni los asserts que lo consumen. Es D-04b refactor puro (claration documental).

    **Sub-task 5c — IN-02 (test/dispatcher.test.js payload deepEqual):**

    Localizar el último `it(...)` del último `describe(...)` del archivo o un lugar apropiado dentro del `describe('dispatchTrigger', ...)` principal (NO mezclar con describes Phase 18 worktree_collision — usar el describe principal).

    AÑADIR un test nuevo:

    ```js
    it('IN-02 Phase 16 closure: gsd.phase.resolved payload tiene canonical keys (event/matched/mode/task_ref)', async () => {
      const payloads = [];
      const sinkLogger = {
        info: (msg, fields) => payloads.push({ level: 'info', msg, fields }),
        warn: (msg, fields) => payloads.push({ level: 'warn', msg, fields }),
        error: (msg, fields) => payloads.push({ level: 'error', msg, fields }),
        debug: (msg, fields) => payloads.push({ level: 'debug', msg, fields }),
        child: () => sinkLogger,
      };

      const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
      const event = { taskRef: 'KL-IN02', action: 'state_change', provider: 'test', raw: {} };

      const gsdProvider = createFakeProvider({
        getTask: async () => ({
          id: 'task-uuid-IN02',
          ref: 'KL-IN02',
          title: 'IN02 payload shape',
          description: '',
          labels: ['kodo', 'kodo:gsd'],
          projectId: 'proj-1',
          projectName: 'Test',
          groups: [],
          url: 'https://example.com/KL-IN02',
          priority: 'medium',
        }),
      });

      await dispatchTrigger(event, {}, {
        getProviderFn: () => gsdProvider,
        launchWorkItemFn: async () => launchWorkItemResult,
        listSessionsFn: () => [],
        listWorkspacesFn: async () => '',
        removeSessionFn: () => {},
        acquireGsdLockFn: () => ({ acquired: true, holder: null, sessionId: 'sess-IN02' }),
        releaseGsdLockFn: () => {},
        resolvePhaseFn: () => ({ action: 'phase', phase_id: '5', match_heading: '### Phase 5: Foo', match_reason: 'exact' }),
        resolveProjectPathFn: () => '/tmp/test',
        loggerFn: () => sinkLogger,
      });

      const resolvedEvent = payloads.find((p) => p.fields?.event === 'gsd.phase.resolved');
      assert.ok(resolvedEvent, 'IN-02: gsd.phase.resolved debe emitirse en GSD path');
      // Assert canonical keys presence (no acoplar order):
      assert.equal(typeof resolvedEvent.fields.event, 'string', 'IN-02: event key presente');
      assert.equal(resolvedEvent.fields.event, 'gsd.phase.resolved');
      assert.equal(typeof resolvedEvent.fields.matched, 'boolean', 'IN-02: matched key boolean');
      // Mode + task_ref deben estar presentes (cualquiera de ambos según el branch):
      const hasModeOrTaskRef = ('mode' in resolvedEvent.fields) || ('task_ref' in resolvedEvent.fields);
      assert.ok(hasModeOrTaskRef, 'IN-02: mode o task_ref deben estar presentes en payload');
    });
    ```

    **NOTAS:**
    - Si `loggerFn` no es un dep ya soportado por `dispatchTrigger`, revisar la signature de DispatchDeps en `src/triggers/dispatcher.js` y usar el nombre exacto (puede ser `loggerFactory` o `createLoggerFn`); si NO existe DI de logger, este test debe emplear la convención existente (memSink global o capture de logs vía `process.env.KODO_LOG_LEVEL` + read from `~/.kodo/logs/`). Si la única vía es FS-based, simplificar a un assert estructural sobre el grep del source — pero Phase 16 dejó `EVENTS.GSD_PHASE_RESOLVED` migrado, así que probablemente existe DI de logger en deps.
    - Si tras leer dispatcher.js NO se encuentra DI de logger en deps, fallback: añadir el assert deepEqual estructural sobre el HELPER `gsdPhaseResolved` directamente importado de `../src/logger-events.js`, pasándole un memSink minimal y verificando que el payload emitted match `{ event: EVENTS.GSD_PHASE_RESOLVED, matched: true, phase_id: '5', mode: 'full', task_ref: 'KL-IN02' }` (deepEqual con campos canonical).
    - El test asegura D-04 invariante (assert sólo keys requeridas, NO orden — alineado con `<risk>` IN-02 RESEARCH.md).

    Karpathy regla 2: NO añadir tests para los 3 matched-false branches por separado — un assert sobre keys es suficiente para cerrar IN-02 (la migración EVENTS.* ya está blindada por Phase 16 dispatcher-isolation.test.js LOG-13 source-hygiene).
  </action>
  <verify>
    <automated>node --test test/gsd-verify-integration.test.js test/dispatcher-isolation.test.js test/dispatcher.test.js</automated>
  </verify>
  <done>
    - `grep -c 'WR-05 Phase 16' test/gsd-verify-integration.test.js` → 1.
    - `grep -c 'WR-08 Phase 16 closure' test/dispatcher-isolation.test.js` → 1.
    - `grep -c "inline comments at end of code lines are NOT stripped" test/dispatcher-isolation.test.js` → 1.
    - `grep -c 'IN-02 Phase 16 closure' test/dispatcher.test.js` → 1.
    - `grep -c 'deepEqual\\|assert.equal.*event\\|matched' test/dispatcher.test.js` cumple (el assert nuevo está presente).
    - Los 3 archivos de test pasan verdes.
  </done>
</task>

<task type="auto">
  <name>Task 6: Audit final — verificar items satisfied-by-existing</name>
  <files>No-op task — documental. Sin cambios de archivos.</files>
  <action>
    Ejecutar las verificaciones que registran en el SUMMARY que los siguientes items NO requirieron task de código:

    1. **WR-01 (stop.js doble logger):**
       - `grep -c "const log = (deps && deps.loggerFactory)" src/hooks/stop.js` → >=1 (el único logger del flow sessionEnd/markSessionStatus).
       - Verificar manualmente que las líneas 159-167 declaran UN solo `const log = ...` y que es reutilizado en líneas 171 (markSessionStatus) y 183 (sessionEnd).
       - El segundo logger en líneas 220-228 (`cleanupLog` para worktree cleanup) es de Phase 19, NO contado como WR-01 (distinta surface).

    2. **WR-02 (stop.js catch-todo):**
       - `grep -c "console.error.*markSessionStatus failed" src/hooks/stop.js` → 1 (línea 175 actual).

    3. **WR-03 (verify.js triple child binding):**
       - **No code change.** Documentar en SUMMARY: "satisfied-by-existing-tests per R-04 RESEARCH.md — el merge correcto del bindings stack se valida indirectamente por `from='review'`/`from='running'` correcto en stop-state-transition Tests 1-3 (Phase 16) + WR-04 paramétrico nuevo (Phase 22)".

    4. **IN-03 (verify.js comment "header line 26"):**
       - `grep -c 'header line 26' src/gsd/verify.js` → 0 (verificado por planner, satisfied-by-Phase-16-CR-01).

    5. **IN-04 (stop.js comment "line 116"):**
       - `grep -c 'line 116' src/hooks/stop.js` → 0 (verificado por planner, satisfied-by-Phase-19-refactor).

    Si CUALQUIER verificación falla, detener y reportar al usuario antes de cerrar el plan (significa que el estado del repo ha derivado de lo verificado por RESEARCH.md).
  </action>
  <verify>
    <automated>grep -c "console.error.*markSessionStatus failed" src/hooks/stop.js && test "$(grep -c 'header line 26' src/gsd/verify.js)" = "0" && test "$(grep -c 'line 116' src/hooks/stop.js)" = "0"</automated>
  </verify>
  <done>
    - Las 5 verificaciones grep pasan en los counts esperados.
    - El SUMMARY del plan documenta los items satisfied-by-existing con sus paths + razones.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dispatcher.js` module-load → `logger-events.js` | Cambia de "logger-events eager + dynamic helpers" a "logger-events eager + helpers eager" — surface idéntica, solo cambia el momento del binding |
| `manager.js#markSessionStatus` → state.json | Sin cambio — `updateSession` solo se invoca cuando la session existe; si no existe, NO se muta state.json (consistente con la semántica "no-op si ID ausente") |
| `manager.js#markSessionStatus` → logger sink | Nuevo evento `state.transition.skipped` con `reason: 'session-not-found'` — añade telemetry diagnóstica, no modifica state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | Re-introducción de dynamic import de logger-events helpers en dispatcher | mitigate | `test/dispatcher-isolation.test.js` LOG-13 source-hygiene falla loud si re-aparece `await import('../logger-events.js')` (stripComments cubre el require statement) |
| T-22-03-02 | Information Disclosure | `state.transition.skipped` event emitido con `task_id` en log files | accept | Same surface que el resto de eventos del logger (LOG-08 redactor ya cubre PII via redact rules de Phase 6). `task_id` ya aparece en otros NDJSON events (session.start, session.end) |
| T-22-03-03 | Repudiation | El early-return de manager.js elimina el `state.transition` con `from='unknown'` que antes era forensic | accept | El `from='unknown'` previo NO aportaba forensic real (era ruido — la session ya no existía cuando se llamó). El nuevo `state.transition.skipped` con `reason: 'session-not-found'` es ESTRICTAMENTE más informativo (audit-friendly) |
| T-22-03-04 | Denial of Service | El warn log en manager.js cuando session no existe podría spamear si stop.js se invoca en loop | accept | stop.js se invoca 1 vez por Claude Code stop event (no loop). Si un test fuerza loops, el spam es contenido al test sink, no a stderr real |
| T-22-03-05 | Tampering | Refactor futuro de WR-07 que mueva el `markSessionStatus` ANTES de releaseGsdLock invierte el orden de side effects | accept | Phase 19 CR-02 ya documenta el orden canónico (markSessionStatus PRE-release) — un refactor que invierta fallará los tests stop-state-transition (Tests 1-3) |
| T-22-03-06 | Information Disclosure | JSDoc en stop.js IN-01 menciona path absoluto de gsd/lock.js (que el usuario podría no querer doc) | accept | Es path relativo dentro del repo (`../gsd/lock.js`), publicado y open-source. Sin trust boundary |

**threats_open: 0** — todos mitigated o accepted con rationale.
</threat_model>

<verification>
1. `node --test test/dispatcher.test.js test/dispatcher-isolation.test.js test/stop-state-transition.test.js test/gsd-verify-integration.test.js` — todos verdes.
2. `npm test` completo verde (delta esperado: +1 test nuevo WR-07, +1 test nuevo IN-02, +2 asserts en D-04 invariante; ≈ 611-612 total vs baseline 609).
3. Source-hygiene LOG-13 intacto: `node --test test/dispatcher-isolation.test.js` confirma que no aparecen literales `gsd.phase.resolved` runtime en código (el assert sigue pasando porque las menciones nuevas en `test/dispatcher.test.js` son tests, no `src/`).
4. LOG-12 intacto: `node --test test/check-isolation.test.js` verde.
5. Pitfall #6 Opción A intacto: exit codes de `kodo gsd verify` no afectados — WR-07 fix solo toca el path interno de `markSessionStatus`, no el resolver de verify.
6. Lock idempotencia (Phase 8 GSD-10) intacto: `releaseGsdLock` y `acquireGsdLock` no se tocan.
7. Grep audit panel — items satisfied-by-existing:
   - `grep -c 'header line 26' src/gsd/verify.js` → 0.
   - `grep -c 'line 116' src/hooks/stop.js` → 0.
   - `grep -c "markSessionStatus failed" src/hooks/stop.js` → 1.
8. Grep audit panel — fixes activos:
   - `grep -c "import { EVENTS, gsdPhaseResolved, gsdBootstrap }" src/triggers/dispatcher.js` → 1.
   - `grep -c "await import('../logger-events.js')" src/triggers/dispatcher.js` → 0.
   - `grep -c 'if (!current)' src/session/manager.js` → 1.
   - `grep -c 'state.transition.skipped' src/session/manager.js` → 1.
   - `grep -c 'Lazy DI pattern' src/hooks/stop.js` → 1.
   - `grep -c 'expectedFrom' test/stop-state-transition.test.js` → >=1.
   - `grep -c 'WR-04 Phase 16' test/stop-state-transition.test.js` → 1.
   - `grep -c 'WR-05 Phase 16' test/gsd-verify-integration.test.js` → 1.
   - `grep -c "inline comments at end of code lines are NOT stripped" test/dispatcher-isolation.test.js` → 1.
   - `grep -c 'IN-02 Phase 16 closure' test/dispatcher.test.js` → 1.
</verification>

<success_criteria>
- [x] DEBT-05 WR-01: satisfied-by-Phase-19 (audit pasa).
- [x] DEBT-05 WR-02: satisfied-by-Phase-19 (audit pasa).
- [x] DEBT-05 WR-03: satisfied-by-existing-tests (R-04, documented en SUMMARY).
- [x] DEBT-05 WR-04: Test D-04 invariante asserta `from` paramétrico.
- [x] DEBT-05 WR-05: T27 con JSDoc claration sobre order vs presence.
- [x] DEBT-05 WR-06: imports consolidados eager en dispatcher.js línea 13; dynamic eliminado línea 307.
- [x] DEBT-05 WR-07: markSessionStatus early-return en manager.js (5-10 LOC) + test cubre el caso missing-session.
- [x] DEBT-05 WR-08: JSDoc de stripComments documenta limitación inline.
- [x] DEBT-06 IN-01: JSDoc de runStopHook documenta lazy DI pattern.
- [x] DEBT-06 IN-02: test/dispatcher.test.js asserta canonical keys del payload gsd.phase.resolved.
- [x] DEBT-06 IN-03: satisfied-by-Phase-16-CR-01 (grep negativo).
- [x] DEBT-06 IN-04: satisfied-by-Phase-19 (grep negativo).
- [x] Suite global verde.
- [x] Invariantes preservados: LOG-12, LOG-13, Pitfall #6 Opción A, lock idempotencia GSD-10.
- [x] Cada commit cita su WR-XX o IN-XX o DEBT-XX (per D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/22-tech-debt-v0-5-closure/22-03-SUMMARY.md` con:
- Tabla por ID con estado final (closed / satisfied-by-existing / satisfied-by-existing-tests) + commit SHA cuando aplique.
- Grep audit panel ejecutado y resultado por línea.
- Citación explícita a D-02b, D-04, D-04b, D-06b, D-07, D-08, D-09 del CONTEXT.md.
- Confirmación de invariantes preservados (LOG-12, LOG-13, Pitfall #6, lock idempotencia).
- Cita a R-01 / R-02 / R-03 / R-04 del RESEARCH.md como base de las decisiones satisfied-by-existing.
</output>
