---
phase: 22-tech-debt-v0-5-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger.js
  - src/cli/format.js
  - test/logger-exports.test.js
  - test/format-isolation.test.js
autonomous: true
requirements:
  - DEBT-04
user_setup: []

must_haves:
  truths:
    - "DEBT-04: src/logger.js NO exporta ANSI_RESET ni COLOR_BY_LEVEL (y NO contiene ANSI_GRAY/CYAN/YELLOW como const)."
    - "DEBT-04 resguardo runtime: ANSI_RESET y ANSI_RED siguen vivos COMO const PRIVADOS — la línea 312 writeNdjson error path sigue formateando con ellos (defense-in-depth invariante)."
    - "test/logger-exports.test.js NO testea exports retirados; el resto del suite Phase 6/7 sigue verde."
    - "src/cli/format.js comentarios línea 101 y 159 NO citan COLOR_BY_LEVEL por nombre."
    - "test/format-isolation.test.js incluye un guard nuevo que asserta el grep negativo sobre src/logger.js."
    - "Suite global verde (~609 tests menos 2-3 eliminados de logger-exports + 1 nuevo guard ≈ delta ±2)."
    - "LOG-12 guard intacto: kodo check sigue sin cargar src/logger.js transitivamente."
  artifacts:
    - path: "src/logger.js"
      provides: "ANSI_RESET + ANSI_RED como const PRIVADAS (no exportadas); COLOR_BY_LEVEL/GRAY/CYAN/YELLOW eliminadas."
      contains: "const ANSI_RESET"
    - path: "test/logger-exports.test.js"
      provides: "Suite sin tests sobre exports retirados; describe label refleja surface restante (formatLine)."
      contains: "formatLine"
    - path: "src/cli/format.js"
      provides: "Comentarios actualizados (líneas 101 y 159) — citan 'mapeo equivalente al logger NDJSON pre-Phase-15, ya no expuesto'."
      contains: "pre-Phase-15"
    - path: "test/format-isolation.test.js"
      provides: "Guard DEBT-04 source-hygiene: src/logger.js no exporta ANSI_* ni COLOR_BY_LEVEL."
      contains: "DEBT-04 source-hygiene"
  key_links:
    - from: "src/logger.js#writeNdjson error path"
      to: "ANSI_RESET + ANSI_RED const privadas"
      via: "process.stderr.write template literal con ANSI codes (línea 312 actual)"
      pattern: "ANSI_RED.*write failed.*ANSI_RESET"
    - from: "test/format-isolation.test.js#DEBT-04 guard"
      to: "src/logger.js source"
      via: "readFileSync + regex negativo"
      pattern: "export\\\\s\\+const\\\\s\\+ANSI_"
---

<objective>
Cerrar la deuda Phase 15 IN-01 (DEBT-04): retirar los `ANSI_*` + `COLOR_BY_LEVEL` exports de `src/logger.js` que ya no tienen consumers externos en `src/`. Es un único surface (1 plan, 1 task agrupada por archivo per D-08).

**Crítico (validado por researcher):**
- `ANSI_RESET` + `ANSI_RED` se siguen consumiendo internamente en `src/logger.js:312` (writeNdjson error path) → SOLO se retira el `export`, las const se mantienen privadas.
- `ANSI_GRAY/CYAN/YELLOW` + `COLOR_BY_LEVEL` se eliminan completos (cero consumers, incluso internos tras retirar `COLOR_BY_LEVEL`).
- `test/logger-exports.test.js` líneas 12-15 (`exports ANSI_RESET as a string`) y 17-24 (`exports COLOR_BY_LEVEL frozen map`) se eliminan; el resto del archivo (tests de `formatLine`) se mantiene.
- Comentarios en `src/cli/format.js:101` y `src/cli/format.js:159` que CITAN `COLOR_BY_LEVEL` por nombre se actualizan (sin perder semántica documental).
- Guard nuevo en `test/format-isolation.test.js` que asserta `src/logger.js` no exporta los símbolos (anti-regresión D-05d).

Purpose: cerrar el último item residual de Phase 15 (IN-01) declarado retirable por la `<canonical_refs>` de Phase 15 REVIEW.md, sin tocar el comportamiento runtime ni romper LOG-12 / color isolation invariantes.

Output: 4 archivos modificados, suite verde, golden bytes intactos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-tech-debt-v0-5-closure/22-CONTEXT.md
@.planning/phases/22-tech-debt-v0-5-closure/22-RESEARCH.md
@.planning/milestones/v0.5-phases/15-cli-polish-wiring/15-REVIEW.md
@src/logger.js
@src/cli/format.js
@test/logger-exports.test.js
@test/format-isolation.test.js

<interfaces>
<!-- Estado actual verificado por planner -->

src/logger.js líneas 41-53 (estado actual):
```js
// Línea 42 — EXPORTED (consumido internamente línea 312)
export const ANSI_RESET = '\x1b[0m';
// Líneas 43-46 — PRIVATE (consumidas SOLO por COLOR_BY_LEVEL)
const ANSI_GRAY = '\x1b[90m';
const ANSI_CYAN = '\x1b[36m';
const ANSI_YELLOW = '\x1b[33m';
const ANSI_RED = '\x1b[31m';   // también consumido línea 312
// Líneas 48-53 — EXPORTED (sin consumers externos en src/)
export const COLOR_BY_LEVEL = Object.freeze({
  debug: ANSI_GRAY,
  info: ANSI_CYAN,
  warn: ANSI_YELLOW,
  error: ANSI_RED,
});
```

src/logger.js línea 312 (writeNdjson error path — INTACTO):
```js
process.stderr.write(`${ANSI_RED}[kodo:logger] write failed: ${msg}${ANSI_RESET}\n`);
```

Estado RESULTANTE esperado en src/logger.js líneas 41-44:
```js
// ANSI escape codes (privadas; consumidas por el error path de writeNdjson, línea 312).
const ANSI_RESET = '\x1b[0m';
const ANSI_RED = '\x1b[31m';
```

src/cli/format.js:101 (JSDoc, estado actual):
```
*   - Level chips: debug/info/warn/error (mapeo idéntico a logger.js COLOR_BY_LEVEL).
```
Estado RESULTANTE:
```
*   - Level chips: debug/info/warn/error (mapeo equivalente al logger NDJSON pre-Phase-15, ya no expuesto).
```

src/cli/format.js:159 (comentario inline, estado actual):
```
    // Level chips (D-03, mapping mirrors src/logger.js:37 COLOR_BY_LEVEL).
```
Estado RESULTANTE:
```
    // Level chips (D-03, mapping mirrors el mapeo interno legacy del logger NDJSON pre-Phase-15).
```

test/logger-exports.test.js shape ESPERADO post-edit:
- describe label línea ~11: `'LOG-05/06/07 exports: formatLine'` (sin `COLOR_BY_LEVEL` ni `ANSI_RESET`).
- `it('exports ANSI_RESET as a string', ...)` líneas 12-15 → ELIMINADO.
- `it('exports COLOR_BY_LEVEL frozen map with 4 levels', ...)` líneas 17-24 → ELIMINADO.
- `it('exports formatLine(record, { useColor }) as pure function', ...)` MANTENIDO.
- `it('formatLine with useColor=true wraps level with ANSI codes ...', ...)` MANTENIDO — pero la línea ~57 `assert.equal(typeof mod.ANSI_RESET, 'string')` se ELIMINA si existe (assertion sobre export retirado).
- `it('formatLine omits component when record has no component field', ...)` MANTENIDO.

test/format-isolation.test.js — añadir nuevo describe al final del archivo o junto al walker LOG-12 existente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DEBT-04 — retirar ANSI_*/COLOR_BY_LEVEL exports + ajustar tests + comentarios</name>
  <files>
    src/logger.js,
    src/cli/format.js,
    test/logger-exports.test.js,
    test/format-isolation.test.js
  </files>
  <read_first>
    src/logger.js (líneas 40-55 + línea 312),
    src/cli/format.js (líneas 95-110 + 155-165),
    test/logger-exports.test.js (todo el archivo, ~70 líneas),
    test/format-isolation.test.js (estructura para localizar dónde añadir el guard)
  </read_first>
  <behavior>
    - El test guard nuevo en `test/format-isolation.test.js` FALLA inicialmente (RED) si se ejecuta antes de editar `src/logger.js` (porque `export const ANSI_RESET` sigue presente).
    - Tras editar `src/logger.js`, el guard pasa (GREEN).
    - Los tests existentes de `formatLine` en `test/logger-exports.test.js` siguen verdes — `formatLine` no cambia, solo cambia el shape exportado del módulo.
    - `npm test` completo: delta esperado ≈ -2 tests (eliminados de logger-exports) + 1 guard nuevo (DEBT-04 source-hygiene); resto intacto.
    - Runtime: `writeNdjson` error path (línea 312) sigue formateando con ANSI codes — las const privadas siguen accesibles.
  </behavior>
  <action>
    Ejecutar las 4 ediciones en este orden (el orden importa solo para que el guard `test/format-isolation.test.js` RED→GREEN sea observable):

    **Paso 1 — Añadir guard nuevo en `test/format-isolation.test.js` (TDD RED):**

    Localizar el bloque `describe(...)` existente (LOG-12 walker) y AÑADIR al final del archivo (después del último `});` del describe principal, antes del EOF) un nuevo describe:

    ```js
    describe('DEBT-04 source-hygiene: ANSI exports retired (Phase 15 IN-01 closed via Phase 22)', () => {
      it('src/logger.js no exporta ANSI_RESET ni COLOR_BY_LEVEL', () => {
        const src = readFileSync(join(SRC, 'logger.js'), 'utf-8');
        assert.equal(
          /export\s+const\s+ANSI_/m.test(src),
          false,
          'ANSI_* must not be exported from logger.js after Phase 22 (DEBT-04)'
        );
        assert.equal(
          /export\s+const\s+COLOR_BY_LEVEL/m.test(src),
          false,
          'COLOR_BY_LEVEL must not be exported from logger.js after Phase 22 (DEBT-04)'
        );
      });
    });
    ```

    Verificar que los imports en el top del archivo (`readFileSync`, `join`, `SRC`, `assert`, `describe`, `it`) ya están — si `readFileSync` no está, añadirlo al import de `node:fs` existente. `SRC` debería ser ya una constante apuntando a `<repo>/src` (revisar el patrón del walker LOG-12 vigente; si nombre distinto, usar el equivalente).

    Correr `node --test test/format-isolation.test.js` — DEBE FALLAR con el mensaje "ANSI_* must not be exported" (TDD RED esperado).

    **Paso 2 — Editar `src/logger.js` (GREEN):**

    Reemplazar líneas 41-53 (bloque de `ANSI_*` + `COLOR_BY_LEVEL`) por:

    ```js
    // ANSI escape codes — privadas; consumidas por el writeNdjson error path (línea ~312).
    // Phase 22 DEBT-04 retiró COLOR_BY_LEVEL y los exports ANSI_* (Phase 15 IN-01 closed).
    const ANSI_RESET = '\x1b[0m';
    const ANSI_RED = '\x1b[31m';
    ```

    Líneas a eliminar: `ANSI_GRAY`, `ANSI_CYAN`, `ANSI_YELLOW` (cero consumidores tras retirar `COLOR_BY_LEVEL`), y el bloque entero de `export const COLOR_BY_LEVEL = Object.freeze({...});`.

    Línea 42 `export const ANSI_RESET` → `const ANSI_RESET` (quitar `export`).
    Línea 46 `const ANSI_RED` — mantener (ya es `const`, no se exporta).

    **VERIFICACIÓN OBLIGATORIA antes de continuar:**
    - `grep -n 'ANSI_GRAY\\|ANSI_CYAN\\|ANSI_YELLOW' src/logger.js` → 0 matches.
    - `grep -n 'COLOR_BY_LEVEL' src/logger.js` → 0 matches.
    - `grep -n 'ANSI_RED\\|ANSI_RESET' src/logger.js` → debe imprimir las 2 declaraciones + 2 usos en línea ~312 (4 matches totales esperados).

    Correr `node --test test/format-isolation.test.js` — el guard nuevo debe pasar GREEN.

    **Paso 3 — Editar `test/logger-exports.test.js`:**

    Localizar y aplicar:
    - Línea ~11 (`describe(...)` label): cambiar `'LOG-05/06/07 exports: formatLine + COLOR_BY_LEVEL + ANSI_RESET'` → `'LOG-05/06/07 exports: formatLine (Phase 22 DEBT-04 retiró ANSI_RESET + COLOR_BY_LEVEL)'`.
    - Líneas 12-15 (`it('exports ANSI_RESET as a string', ...)`): ELIMINAR el bloque completo.
    - Líneas 17-24 (`it('exports COLOR_BY_LEVEL frozen map with 4 levels', ...)`): ELIMINAR el bloque completo.
    - Líneas ~40-58 (`it('formatLine with useColor=true wraps level with ANSI codes ...', ...)`):
      - MANTENER el test (testea `formatLine`, no exports).
      - Si una de las assertions de ese test es `assert.equal(typeof mod.ANSI_RESET, 'string')` o equivalente, ELIMINARLA — el resto del test mide `formatLine` output que sigue intacto.
      - Si el test parsea el output buscando `\x1b[0m` (reset literal en la string formateada), MANTENERLO — `formatLine` sigue emitiendo el reset internamente.
    - Líneas restantes (`it('exports formatLine ...')`, `it('formatLine omits component ...')`): MANTENER.

    **Paso 4 — Editar `src/cli/format.js` (comentarios):**

    - Línea 101 (JSDoc): cambiar texto exacto:
      - DE: `*   - Level chips: debug/info/warn/error (mapeo idéntico a logger.js COLOR_BY_LEVEL).`
      - A:  `*   - Level chips: debug/info/warn/error (mapeo equivalente al logger NDJSON pre-Phase-15, ya no expuesto).`
    - Línea 159 (inline comment): cambiar texto exacto:
      - DE: `    // Level chips (D-03, mapping mirrors src/logger.js:37 COLOR_BY_LEVEL).`
      - A:  `    // Level chips (D-03, mapping mirrors el mapeo interno legacy del logger NDJSON pre-Phase-15).`

    Ambos cumplen D-07/D-07b: cita por contenido, no por offset, y NO referencian un símbolo eliminado.

    **Paso 5 — Verificación final:**

    Correr `npm test`. Esperado: suite verde, delta de tests: -2 (logger-exports eliminados) + 1 (guard DEBT-04) = -1 neto vs baseline.

    Karpathy regla 2: simplicidad — NO mover `ANSI_RED`/`ANSI_RESET` a scope local de `writeNdjson`. Dejarlas module-scoped privadas es estructuralmente más simple y mantiene compatibilidad si otro error path interno futuro las necesita.

    Karpathy regla 3: cambios quirúrgicos — NO refactorizar nada adyacente (función `formatLine`, `writeNdjson`, otros tests). NO añadir comentarios decorativos.
  </action>
  <verify>
    <automated>node --test test/format-isolation.test.js test/logger-exports.test.js</automated>
  </verify>
  <done>
    - `grep -E '^export\\s+const\\s+ANSI_' src/logger.js` → 0 matches.
    - `grep -E '^export\\s+const\\s+COLOR_BY_LEVEL' src/logger.js` → 0 matches.
    - `grep -c 'ANSI_RED\\|ANSI_RESET' src/logger.js` → 4 (2 declaraciones + 2 usos en línea 312).
    - `grep -c 'COLOR_BY_LEVEL' src/logger.js` → 0.
    - `grep -c 'COLOR_BY_LEVEL' src/cli/format.js` → 0 (los 2 comentarios actualizados).
    - `grep -c 'pre-Phase-15' src/cli/format.js` → 2.
    - `grep -c 'DEBT-04 source-hygiene' test/format-isolation.test.js` → 1.
    - `grep -c 'exports ANSI_RESET as a string' test/logger-exports.test.js` → 0.
    - `grep -c 'exports COLOR_BY_LEVEL' test/logger-exports.test.js` → 0.
    - `npm test` exit 0.
    - LOG-12 guard intacto (test/check-isolation.test.js sigue verde).
    - Color isolation intacto (test/format-isolation.test.js LOG-12 walker original sigue verde).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/logger.js` module exports → resto del repo | Surface reduction (menos exports = menos surface) — no nuevas entries |
| `src/logger.js#writeNdjson` error path → stderr | Sin cambios — sigue emitiendo `ANSI_RED ... ANSI_RESET` con las mismas const ahora privadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | Re-introducción accidental de `export const ANSI_*` en future PR | mitigate | `test/format-isolation.test.js` DEBT-04 guard fallaría loud con grep regex `/export\\s+const\\s+ANSI_/m` |
| T-22-02-02 | Tampering | Otro módulo en `src/` añade `import { ANSI_RESET } from './logger.js'` (re-introduce dependencia retirada) | accept | El import fallaría loud en runtime/CI (`SyntaxError` o `undefined`), no es silent breakage. CLAUDE.md regla 1 prohibe `--no-verify` |
| T-22-02-03 | Information Disclosure | `writeNdjson` error path emite ANSI escapes en TTY de usuarios con NO_COLOR | accept | Defense-in-depth invariante Phase 15 IN-02 (deferred) — escapes pre-existentes, no nuevos. Documented en `src/logger.js:308-312` comment block (existing) |
| T-22-02-04 | Repudiation | Comentarios en format.js sin referencia explícita al ID retirado podrían ofuscar audit history | accept | Los comentarios nuevos citan "pre-Phase-15, ya no expuesto" — futuras lecturas saben que el símbolo desapareció en Phase 15 → 22 |

**threats_open: 0** — todos mitigated o accepted con rationale.
</threat_model>

<verification>
1. `node --test test/format-isolation.test.js test/logger-exports.test.js` verde.
2. `npm test` completo verde (delta esperado: -1 test neto vs baseline).
3. `grep` panel (per `<done>` de Task 1) — todos los matches en los counts esperados.
4. Verificación LOG-12 invariante: `node --test test/check-isolation.test.js` sigue verde (kodo check sin imports transitivos a logger.js).
5. Verificación color isolation: `node --test test/format-isolation.test.js` sigue verde (picocolors single-source intacto).
</verification>

<success_criteria>
- [x] `src/logger.js`: sin `export const ANSI_*`, sin `export const COLOR_BY_LEVEL`, sin `ANSI_GRAY/CYAN/YELLOW`.
- [x] `src/logger.js:312` writeNdjson error path sigue formateando con `ANSI_RED`+`ANSI_RESET` (privadas).
- [x] `test/logger-exports.test.js`: 2 tests sobre exports retirados eliminados; describe label refleja surface restante.
- [x] `src/cli/format.js:101` y `:159`: comentarios actualizados (sin citar `COLOR_BY_LEVEL` por nombre).
- [x] `test/format-isolation.test.js`: guard DEBT-04 source-hygiene presente y verde.
- [x] Suite global verde.
- [x] LOG-12 + color isolation invariantes intactos.
- [x] Commit cita `DEBT-04 Phase 15 IN-01` (per D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/22-tech-debt-v0-5-closure/22-02-SUMMARY.md` con:
- DEBT-04 status: closed (commit SHA + paths modificados).
- Constantes preservadas como privadas (ANSI_RED + ANSI_RESET para writeNdjson).
- Constantes eliminadas (ANSI_GRAY/CYAN/YELLOW + COLOR_BY_LEVEL).
- Tests eliminados (2 en logger-exports).
- Tests añadidos (1 guard DEBT-04 en format-isolation).
- Comentarios actualizados (2 en format.js).
- Cita a D-05/D-05b/D-05c/D-05d/D-05e/D-07 del CONTEXT.md.
</output>
