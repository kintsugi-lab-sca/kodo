---
phase: 19-worktree-cleanup-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - src/hooks/stop.js
  - test/stop-worktree-cleanup.test.js
  - test/stop.test.js
autonomous: true
requirements: []
tags:
  - worktree-cleanup
  - stop-hook
  - phase-19
  - gap-closure
user_setup: []

must_haves:
  truths:
    - "CR-02 fix: markSessionStatus('done', 'session-stop', log) se ejecuta FUERA del bloque if (session.gsd); todas las sesiones (GSD + no-GSD) transitan a 'done' ANTES de sessionEnd + removeSession"
    - "CR-02 fix: sessionEnd emite status: 'done' (no session.status pre-removal) — observable NDJSON refleja el estado terminal real para sesiones no-GSD"
    - "CR-02 fix: el try/catch alrededor de markSessionStatus usa console.error (no comentario silent) per WR-03 — markSessionStatus muta state.json y un fallo merece diagnóstico explícito"
    - "CR-03 fix: la pre-check de Pitfall #1 usa lstatSync(target) en try/catch en lugar de existsSync(target); ENOENT mantiene <wt>.dirty canónico, cualquier otro stat (file, dir, symlink colgante, symlink vivo, EACCES) fuerza la variante <wt>.dirty-<Date.now()>"
    - "CR-03 fix: dos tests nuevos en stop-worktree-cleanup.test.js cubren (a) symlink colgante apuntando a path inexistente y (b) archivo regular en la ruta .dirty — ambos terminan en variante suffixed"
    - "Fail-open preservado: ningún cambio rompe el contrato 'runStopHook nunca crashea' — outer try/catch top-level intacto; los try/catch internos siguen su política existente"
    - "CR-01 deferido por override D-07 (VERIFICATION.md overrides_applied: 1, accepted_by: alex) — fuera de scope 19-03; NO se tocan src/session/state.js findSession ni el orden cleanup/nudge/removeSession en stop.js"
    - "handleOrchestratorStop intacto (D-05 satisfied-by-design preservado de 19-02)"
  artifacts:
    - path: "src/hooks/stop.js"
      provides: "markSessionStatus relocated fuera de if (session.gsd) + lstatSync replace existsSync en pre-check dirty target"
      contains: "lstatSync"
    - path: "test/stop-worktree-cleanup.test.js"
      provides: "2 tests nuevos para CR-03 (symlink colgante + archivo regular) — ambos verifican variante .dirty-<ts>"
      contains: "DANGLING SYMLINK"
    - path: "test/stop.test.js"
      provides: "Source-hygiene asserts CR-02 (markSessionStatus fuera de if session.gsd) + CR-03 (lstatSync presente, existsSync(target) NO presente en el bloque dirty)"
      contains: "Phase 19 CR-02"
  key_links:
    - from: "src/hooks/stop.js"
      to: "src/session/manager.js"
      via: "markSessionStatus(task_id, 'done', 'session-stop', log) — invocación fuera del bloque if (session.gsd)"
      pattern: "markSessionStatus\\(session\\.task_id,\\s*'done',\\s*'session-stop'"
    - from: "src/hooks/stop.js"
      to: "node:fs"
      via: "lstatSync(target) en try/catch ENOENT — reemplaza existsSync(target) per CR-03"
      pattern: "lstatSync\\(target\\)"
    - from: "test/stop-worktree-cleanup.test.js"
      to: "src/hooks/stop.js"
      via: "runStopHook + fixtures con symlink colgante / archivo regular en <wt>.dirty"
      pattern: "DANGLING SYMLINK|REGULAR FILE"
    - from: "test/stop.test.js"
      to: "src/hooks/stop.js"
      via: "source-hygiene asserts CR-02 + CR-03"
      pattern: "Phase 19 CR-(02|03)"
---

<objective>
Cerrar dos WARNINGs del code review de Phase 19 (`19-REVIEW.md` CR-02 y CR-03) sin reabrir el override de CR-01 (deferido a Phase 21+ por D-07 + VERIFICATION.md overrides_applied).

Purpose:
- **CR-02** — `markSessionStatus('done')` solo se ejecuta para sesiones GSD. Las sesiones no-GSD se archivan a `state.history` con un `status` stale (típicamente `'review'`) y `sessionEnd` emite `status: session.status` pre-removal. El observable NDJSON contradice la realidad. Fix: mover `markSessionStatus` FUERA del `if (session.gsd)` para que TODAS las sesiones transiten a `'done'` antes de `sessionEnd` + `removeSession`. Además, sustituir el `try { ... } catch { /* silent */ }` por `console.error` (WR-03): `markSessionStatus` es mutación de `state.json`, no logger op.
- **CR-03** — `existsSync(target)` sigue symlinks por defecto en Node. Un symlink colgante en `<wt>.dirty` apuntando a un worktree ya borrado devuelve `false`; el código intenta `git worktree move` directo y git falla confusamente. Fix: reemplazar `existsSync(target)` por `lstatSync(target)` en try/catch — ENOENT ⇒ target libre, cualquier otro stat (file, dir, symlink colgante, symlink vivo, EACCES) ⇒ variante `<wt>.dirty-<Date.now()>`.

Output:
- `src/hooks/stop.js` con `markSessionStatus` relocated antes de `sessionEnd` y fuera del `if (session.gsd)`; `lstatSync` en lugar de `existsSync` para el dirty target pre-check.
- `test/stop-worktree-cleanup.test.js` con 2 tests nuevos cubriendo symlink colgante + archivo regular en la ruta `.dirty`.
- `test/stop.test.js` con 2 source-hygiene asserts (CR-02 + CR-03).

NOTA: CR-01 (orden cleanup/removeSession antes del nudge → orchestrator-led verify roto) NO se toca. `VERIFICATION.md` lo overridea con `overrides_applied: 1`, `accepted_by: alex`, `deferred_to: "Phase 21+"`. Cualquier cambio en `src/session/state.js findSession` o reordenamiento de `cleanup/removeSession/notify orchestrator` está FUERA del scope de 19-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md
@.planning/phases/19-worktree-cleanup-integration/19-VERIFICATION.md
@.planning/phases/19-worktree-cleanup-integration/19-REVIEW.md
@.planning/phases/19-worktree-cleanup-integration/19-02-SUMMARY.md
@src/hooks/stop.js
@test/stop-worktree-cleanup.test.js
@test/stop.test.js

<interfaces>
<!-- ESTADO ACTUAL del bloque markSessionStatus (src/hooks/stop.js:178-208) — POST 19-02.
     Está DENTRO de `if (session.gsd)` y usa try { ... } catch { /* silent */ }. -->

```javascript
// stop.js:178-208 (current, BEFORE Task 1):
// Release GSD lock if applicable (D-09: idempotent, verifies session_id)
if (session.gsd) {
  // Phase 16 LOG-15 (D-04, D-08): mark session 'done' BEFORE releaseGsdLock so
  // the state.transition event captures the terminal status while the session
  // record still exists. Mirrors the session.end pattern at line 116 (emit
  // BEFORE mutation). Status is fixed 'done' for both modes (full + quick) —
  // stop.js is a mechanical hook and does NOT infer mode (D-04, D-07).
  // Reason canónico: 'session-stop:lock-released' (D-06).
  try {
    const log = (deps && deps.loggerFactory)
      ? deps.loggerFactory({ session_id: session.session_id, task_id: session.task_id })
      : await (async () => {
          const { createLogger } = await import('../logger.js');
          return createLogger({
            sessionId: session.session_id,
            minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
          }).child({ component: 'hook', task_id: session.task_id });
        })();
    const { markSessionStatus } = await import('../session/manager.js');
    markSessionStatus(session.task_id, 'done', 'session-stop:lock-released', log);
  } catch {
    // silent — never block lock release on logger failure (mirrors session.end pattern line 116)
  }

  try {
    const { releaseGsdLock } = await import('../gsd/lock.js');
    releaseGsdLock(session.project_path, session.session_id);
  } catch (err) {
    console.error(`[kodo:stop] Error releasing GSD lock: ${err.message}`);
  }
}
```

<!-- ESTADO ACTUAL del sessionEnd block (src/hooks/stop.js:163-176) — POST 19-02.
     Emite status: session.status (stale). Debe pasar a status: 'done' tras el relocate. -->

```javascript
// stop.js:163-176 (current, BEFORE Task 1):
try {
  const log = (deps && deps.loggerFactory)
    ? deps.loggerFactory({ session_id: session.session_id, task_id: session.task_id })
    : await (async () => {
        const { createLogger } = await import('../logger.js');
        return createLogger({
          sessionId: session.session_id,
          minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
        }).child({ component: 'hook', task_id: session.task_id });
      })();
  const { sessionEnd } = await import('../logger-events.js');
  sessionEnd(log, {
    session_id: session.session_id,
    task_id: session.task_id,
    status: session.status,             // ← CR-02: stale value pre-mark
    ended_at: new Date().toISOString(),
  });
} catch {
  // silent — never crash Claude Code
}
```

<!-- ESTADO ACTUAL del pre-check Pitfall #1 (src/hooks/stop.js:304-306) — POST 19-02.
     existsSync sigue symlinks → dangling symlinks evaden la mitigación. -->

```javascript
// stop.js:299-307 (current, BEFORE Task 2):
} else if (isDirty === true) {
  // 3b. DIRTY path: move-aside to <wt>.dirty (D-02); branch PRESERVADA.
  // Pitfall #1 mitigation: pre-check con existsSync — si el target canónico
  // ya existe, generar variante `<wt>.dirty-<timestamp>` para no acabar
  // metiendo el worktree DENTRO del directorio colisionante.
  let target = `${wt}.dirty`;
  if (existsSync(target)) {              // ← CR-03: follows symlinks
    target = `${wt}.dirty-${Date.now()}`;
  }
```

<!-- ESTADO ACTUAL del destructuring de node:fs (src/hooks/stop.js:219) — POST 19-02. -->

```javascript
// stop.js:219 (current):
const { existsSync, renameSync } = await import('node:fs');
```

Tras Task 2 debe quedar:
```javascript
const { lstatSync, renameSync } = await import('node:fs');
// existsSync ya NO se importa — no se usa fuera de este bloque dirty.
```
</interfaces>

<scope_boundary>
<!-- HARD BOUNDARY — items prohibidos en este plan: -->

- ❌ NO modificar `src/session/state.js` (findSession scan de state.history es CR-01, overrideado).
- ❌ NO reordenar `cleanup` / `removeSessionFn(id)` / `notify orchestrator` en `stop.js` (CR-01 architectural fix, overrideado).
- ❌ NO modificar `buildStopNudgeText` (stop.js:50) ni la lógica del nudge.
- ❌ NO modificar `handleOrchestratorStop` (preserva `cwd: KODO_ROOT`, D-05).
- ❌ NO modificar `src/gsd/verify.js` (WT-06 closed by 19-02).
- ❌ NO modificar `src/logger-events.js` (Plan 19-01 closed).
- ❌ NO añadir nuevos helpers en logger-events.js, nuevos eventos, ni schemas.
- ❌ NO renombrar la dep `gitFn` ni añadir más deps al `runStopHook` signature.
- ❌ NO modificar `.claude/skills/kodo-orchestrate/skill.md`.

Justificación: VERIFICATION.md `overrides_applied: 1` + `accepted_by: alex` + `deferred_to: "Phase 21+"` cierra CR-01 como deuda explícita. Este plan es **gap closure surgical**: 1 línea movida (markSessionStatus), 1 línea reemplazada (existsSync → lstatSync), 1 swallow → console.error, 2 tests nuevos, 2 source-hygiene asserts.
</scope_boundary>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-02 — Mover markSessionStatus FUERA del if (session.gsd) y propagar status: 'done' al sessionEnd</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    - src/hooks/stop.js líneas 160-210 completas (sessionEnd block + if session.gsd block + markSessionStatus + releaseGsdLock)
    - .planning/phases/19-worktree-cleanup-integration/19-REVIEW.md §"CR-02" (fix block verbatim)
    - .planning/phases/19-worktree-cleanup-integration/19-REVIEW.md §"WR-03" (console.error en lugar de silent — markSessionStatus muta state.json)
    - .planning/phases/19-worktree-cleanup-integration/19-VERIFICATION.md frontmatter (gap CR-02 + missing: "Mover markSessionStatus FUERA del bloque if (session.gsd)")
  </read_first>
  <behavior>
    Tras el cambio:
    1. `markSessionStatus(session.task_id, 'done', 'session-stop', log)` se ejecuta para TODAS las sesiones (GSD y no-GSD), DESPUÉS de adquirir el logger y ANTES de `sessionEnd`.
    2. `sessionEnd` recibe `status: 'done'` literal (no `session.status` pre-removal).
    3. Para sesiones con `session.gsd === true`, el flujo restante (`releaseGsdLock`) sigue dentro de `if (session.gsd)` — solo `markSessionStatus` se relocaliza.
    4. El catch que rodea `markSessionStatus` usa `console.error(\`[kodo:stop] markSessionStatus failed: ${err.message}\`)` (WR-03) — NO el comentario `/* silent */` actual.
    5. La razón canónica del mark cambia de `'session-stop:lock-released'` a `'session-stop'` (el mark ahora ocurre PRE-lock-release, no post; la razón debe reflejarlo). REVIEW.md CR-02 fix block usa `'session-stop'` literal.
    6. El logger compartido para mark + sessionEnd se construye UNA sola vez (no duplicar la factory) — economía y consistencia con el pattern de 19-02.
  </behavior>
  <action>
    Editar `src/hooks/stop.js` en torno a las líneas 163-208. Reestructurar como sigue (este bloque sustituye el `sessionEnd` actual línea 163-176 + la mitad superior del `if (session.gsd)` línea 178-200; la parte de `releaseGsdLock` línea 202-207 queda IDÉNTICA dentro de su `if (session.gsd)`):

    ```javascript
    // ANTES del cambio (líneas 163-208 condensadas):
    //   try { ... sessionEnd(log, { ..., status: session.status, ... }) } catch {}
    //   if (session.gsd) {
    //     try { ... markSessionStatus(session.task_id, 'done', 'session-stop:lock-released', log) } catch { /* silent */ }
    //     try { ... releaseGsdLock(...) } catch (err) { console.error(...) }
    //   }

    // DESPUÉS del cambio:

    // Phase 19 CR-02 fix: markSessionStatus aplica a TODAS las sesiones (GSD + no-GSD)
    // y debe ejecutarse ANTES de sessionEnd para que el observable NDJSON refleje el
    // estado terminal real. REVIEW.md CR-02 + WR-03 mandatan console.error (no silent)
    // porque markSessionStatus muta state.json — un fallo merece diagnóstico explícito.
    const log = (deps && deps.loggerFactory)
      ? deps.loggerFactory({ session_id: session.session_id, task_id: session.task_id })
      : await (async () => {
          const { createLogger } = await import('../logger.js');
          return createLogger({
            sessionId: session.session_id,
            minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
          }).child({ component: 'hook', task_id: session.task_id });
        })();

    try {
      const { markSessionStatus } = await import('../session/manager.js');
      markSessionStatus(session.task_id, 'done', 'session-stop', log);
    } catch (err) {
      // WR-03: state.json mutation failure merits explicit diagnostic (NOT silent).
      // Still fail-open — runStopHook never crashes Claude Code.
      console.error(`[kodo:stop] markSessionStatus failed: ${/** @type {Error} */ (err).message}`);
    }

    try {
      const { sessionEnd } = await import('../logger-events.js');
      sessionEnd(log, {
        session_id: session.session_id,
        task_id: session.task_id,
        status: 'done',                         // CR-02: terminal status, no session.status pre-removal
        ended_at: new Date().toISOString(),
      });
    } catch {
      // silent — never crash Claude Code (logger fail-open per Phase 16 LOG-15)
    }

    // Release GSD lock if applicable (D-09: idempotent, verifies session_id).
    // Phase 19 CR-02: markSessionStatus ya corrió ANTES de este bloque para
    // todas las sesiones; aquí solo queda el lock release para sesiones GSD.
    if (session.gsd) {
      try {
        const { releaseGsdLock } = await import('../gsd/lock.js');
        releaseGsdLock(session.project_path, session.session_id);
      } catch (err) {
        console.error(`[kodo:stop] Error releasing GSD lock: ${err.message}`);
      }
    }
    ```

    Notas críticas:
    - **NO tocar** el bloque cleanup (líneas 210-356) — sigue idéntico tras `if (session.gsd) { releaseGsdLock }` y antes de `removeSessionFn(id)`.
    - **NO tocar** `handleOrchestratorStop` (D-05 — `cwd: KODO_ROOT` preservado).
    - **NO reordenar** `cleanup` / `removeSessionFn(id)` / `notify orchestrator` (CR-01 overrideado).
    - El logger `log` ahora se declara UNA vez al nivel donde antes vivía sessionEnd; markSessionStatus + sessionEnd lo comparten. El bloque cleanup más abajo sigue construyendo su `cleanupLog` separado (no tocar — pattern 19-02 preserved).
    - Comentario antiguo de Phase 16 LOG-15 ("D-04, D-08") referenciando "BEFORE releaseGsdLock" sigue válido en spirit (mark ocurre antes del lock release) — actualizar el comentario para reflejar que ahora aplica a TODAS las sesiones.
    - Si el ESLint del repo se queja por `const log` re-declarado en otros bloques, renombrar el local de cleanup (`cleanupLog`) o el de aquí (`stopLog`) — preferir mantener `log` aquí y `cleanupLog` abajo (consistente con 19-02-SUMMARY).
  </action>
  <verify>
    <automated>node --test test/stop-worktree-cleanup.test.js test/stop.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "markSessionStatus\(session\.task_id,\s*'done',\s*'session-stop'" src/hooks/stop.js` matches (razón canónica actualizada a 'session-stop').
    - `grep -c "markSessionStatus" src/hooks/stop.js` == 1 (única invocación; el comentario referencia puede o no quedar — lo crítico es que no haya 2 sitios productivos).
    - `grep -E "console\.error\(\`\[kodo:stop\] markSessionStatus failed" src/hooks/stop.js` matches (WR-03 fix — console.error en lugar de silent).
    - `awk '/markSessionStatus\(session\.task_id/{m=NR} /^    if \(session\.gsd\) \{/{g=NR} END{exit !(m < g || g == 0)}' src/hooks/stop.js` exit 0 — `markSessionStatus` aparece ANTES del bloque `if (session.gsd) {` que contiene `releaseGsdLock` (verifica que NO está dentro).
    - `grep -E "status:\s*'done'," src/hooks/stop.js` matches al menos 1 vez dentro del bloque sessionEnd (CR-02 fix).
    - `grep -c "status: session.status" src/hooks/stop.js` == 0 (la propagación stale fue eliminada).
    - `node --test test/stop-worktree-cleanup.test.js` exit 0 — los 8 tests existentes de 19-02 siguen pasando (no regresión).
    - `node --test test/stop.test.js` exit 0 — los asserts existentes siguen verdes; los 2 nuevos (Task 3) se añaden en su task.
    - `grep -q "cwd: KODO_ROOT" src/hooks/stop.js` (handleOrchestratorStop intacto, D-05).
    - `grep -c "removeSessionFn(id)" src/hooks/stop.js` ≥ 1 y NO precedido por reordenamiento — verificar manualmente que sigue en su sitio actual línea 358, despues del bloque cleanup (CR-01 NO tocado).
  </acceptance_criteria>
  <done>markSessionStatus relocated fuera de `if (session.gsd)`, ejecuta para todas las sesiones con razón `'session-stop'` y catch `console.error` (WR-03). `sessionEnd` emite `status: 'done'` literal. `releaseGsdLock` sigue dentro de `if (session.gsd)`. `cleanup` y `removeSessionFn(id)` intactos en su orden actual (CR-01 NO tocado, override D-07 honrado). Tests existentes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-03 — Reemplazar existsSync(target) por lstatSync(target) en pre-check Pitfall #1</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    - src/hooks/stop.js líneas 299-340 (bloque DIRTY: pre-check + worktree move + fallback)
    - src/hooks/stop.js línea 219 (destructuring de node:fs — existsSync se importa allí)
    - .planning/phases/19-worktree-cleanup-integration/19-REVIEW.md §"CR-03" (fix block verbatim con lstatSync + try/catch ENOENT)
    - .planning/phases/19-worktree-cleanup-integration/19-VERIFICATION.md frontmatter (gap CR-03 + missing: "Sustituir existsSync por lstatSync con try/catch ENOENT")
  </read_first>
  <behavior>
    Tras el cambio:
    1. El destructuring de `node:fs` importa `lstatSync` en lugar de `existsSync` (renameSync se conserva).
    2. La pre-check del dirty target (línea ~304-307 actual) usa `try { lstatSync(target) } catch { ... }`:
       - Si `lstatSync` NO lanza (target existe como file/dir/symlink vivo/symlink colgante) → `target = \`${wt}.dirty-${Date.now()}\``.
       - Si `lstatSync` lanza con `err.code === 'ENOENT'` → mantener `target = \`${wt}.dirty\`` (canónico libre).
       - Si `lstatSync` lanza con cualquier otro código (EACCES, ELOOP, etc.) → `target = \`${wt}.dirty-${Date.now()}\`` (defensivo: si no podemos statar, no asumimos libre).
    3. `lstatSync` NO sigue symlinks (a diferencia de `existsSync` que sí los sigue), así que un symlink colgante apuntando a un path inexistente devuelve un stat válido del symlink en sí mismo y dispara la variante suffixed correctamente.
    4. Comportamiento happy-path (target NO existe) idéntico al previo: `<wt>.dirty` se usa tal cual.
    5. Comportamiento existing-dir (test TARGET COLLISION de 19-02) idéntico: variante `<wt>.dirty-<ts>`.
    6. Nuevo comportamiento defensivo (Tasks 3 los blindan): symlink colgante + archivo regular fuerzan variante suffixed.
  </behavior>
  <action>
    Editar `src/hooks/stop.js` en dos sitios:

    **1. Línea 219 — actualizar el destructuring de node:fs:**

    ```javascript
    // ANTES:
    const { existsSync, renameSync } = await import('node:fs');

    // DESPUÉS:
    const { lstatSync, renameSync } = await import('node:fs');
    ```

    **2. Líneas 299-307 — reemplazar el if (existsSync) por try { lstatSync } catch:**

    ```javascript
    // ANTES (líneas 299-307):
    } else if (isDirty === true) {
      // 3b. DIRTY path: move-aside to <wt>.dirty (D-02); branch PRESERVADA.
      // Pitfall #1 mitigation: pre-check con existsSync — si el target canónico
      // ya existe, generar variante `<wt>.dirty-<timestamp>` para no acabar
      // metiendo el worktree DENTRO del directorio colisionante.
      let target = `${wt}.dirty`;
      if (existsSync(target)) {
        target = `${wt}.dirty-${Date.now()}`;
      }

    // DESPUÉS (líneas 299-313 aprox tras el cambio):
    } else if (isDirty === true) {
      // 3b. DIRTY path: move-aside to <wt>.dirty (D-02); branch PRESERVADA.
      // Pitfall #1 mitigation (Phase 19 CR-03): lstatSync en try/catch detecta
      // archivos regulares, dirs, symlinks vivos Y symlinks colgantes (existsSync
      // los seguía y devolvía false → evadía la pre-check). Solo ENOENT mantiene
      // el target canónico; cualquier otro error o stat exitoso fuerza la variante
      // suffixed para evitar que `git worktree move` falle confusamente.
      let target = `${wt}.dirty`;
      try {
        lstatSync(target);
        // Target existe como cualquier cosa (file, dir, symlink vivo o colgante)
        // → forzar variante con timestamp.
        target = `${wt}.dirty-${Date.now()}`;
      } catch (err) {
        if (/** @type {NodeJS.ErrnoException} */ (err).code !== 'ENOENT') {
          // EACCES, ELOOP u otro: defensivo, no asumimos libre.
          target = `${wt}.dirty-${Date.now()}`;
        }
        // ENOENT: target libre, mantener `<wt>.dirty` canónico.
      }
    ```

    Notas críticas:
    - El resto del bloque DIRTY (try/catch alrededor de `git worktree move`, fallback `renameSync + git worktree repair`, emisión de `worktreeCleanupDirty`) queda IDÉNTICO. Solo el pre-check de líneas 304-307 cambia.
    - El comentario actualizado debe referenciar explícitamente Phase 19 CR-03 (no solo Pitfall #1) para que el source-hygiene assert del Task 4 pueda referenciarlo.
    - NO añadir `existsSync` a otros sitios — no se usa fuera de este bloque dirty. Verificar con `grep -c existsSync src/hooks/stop.js` que el count baja a 0 tras el cambio.
    - El error annotation `/** @type {NodeJS.ErrnoException} */ (err)` mantiene la coherencia de JSDoc con el resto de catches del bloque cleanup (que ya usan `/** @type {Error} */ (err).message`).
  </action>
  <verify>
    <automated>node --test test/stop-worktree-cleanup.test.js test/stop.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "const \{ lstatSync, renameSync \} = await import\('node:fs'\)" src/hooks/stop.js` matches.
    - `grep -c "lstatSync\(target\)" src/hooks/stop.js` ≥ 1.
    - `grep -c "existsSync" src/hooks/stop.js` == 0 (eliminado completamente del archivo — verificar que no quedan otros usos).
    - `grep -E "err\.code\s*!==\s*'ENOENT'" src/hooks/stop.js` matches (defensa contra EACCES / ELOOP).
    - `grep -q "Phase 19 CR-03" src/hooks/stop.js` (comentario referencia la decisión).
    - El test TARGET COLLISION existente (test/stop-worktree-cleanup.test.js:153) sigue verde — `mkdirSync(dirty)` crea un directorio real que `lstatSync` detecta sin lanzar, y el `target` toma la variante suffixed igual que antes.
    - `node --test test/stop-worktree-cleanup.test.js` exit 0 — 8/8 tests existentes pasan (CLEAN, DIRTY, ERROR, COLLISION, LEGACY, BRANCH-D-FAIL, E2E CLEAN, E2E DIRTY).
    - `node --test test/stop.test.js` exit 0 — los 3 source-hygiene asserts existentes (D-05/D-07/D-08) siguen verdes.
  </acceptance_criteria>
  <done>`existsSync(target)` reemplazado por `lstatSync(target)` en try/catch con discriminación ENOENT. Symlinks colgantes ya no evaden la pre-check. Tests existentes verdes (regression-free). Tasks 3 añade los tests dedicados de symlink colgante + archivo regular.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests CR-03 — Añadir 2 tests para symlink colgante y archivo regular en .dirty</name>
  <files>test/stop-worktree-cleanup.test.js</files>
  <read_first>
    - test/stop-worktree-cleanup.test.js líneas 153-188 (test TARGET COLLISION existente — analog exacto, copiar shape)
    - test/stop-worktree-cleanup.test.js líneas 1-60 (imports + helpers makeMemLogger, makeSession, makeStubCmux, makeGitFnStub)
    - src/hooks/stop.js bloque DIRTY tras Task 2 (lstatSync pre-check)
  </read_first>
  <behavior>
    Dos `it()` nuevos al final del describe `'Phase 19 WT-04: worktree cleanup — unit (gitFn stub)'`:

    1. **DANGLING SYMLINK**: crear `<wt>.dirty` como symlink apuntando a un path inexistente (`<tmpBase>/nonexistent-target`). Ejecutar el hook con session dirty. Verificar que `git worktree move` recibió un target `<wt>.dirty-<digits>` (NO el `.dirty` canónico). Esto valida CR-03: pre-Task-2 `existsSync` seguía el symlink y devolvía false (target queda como `.dirty` canónico, git falla confusamente); post-Task-2 `lstatSync` ve el symlink en sí y dispara la variante suffixed.

    2. **REGULAR FILE**: crear `<wt>.dirty` como archivo regular (no directorio) con `writeFileSync`. Ejecutar el hook con session dirty. Verificar que `git worktree move` recibió un target `<wt>.dirty-<digits>`. Pre-Task-2 `existsSync` devolvía true → variante suffixed (este path ya pasaba); el test sirve como red contra regresiones si alguien revierte a `existsSync` con bug.

    Ambos tests usan el mismo shape que TARGET COLLISION (líneas 153-188): `mkdtempSync` + helpers + try/finally con `rmSync`.
  </behavior>
  <action>
    Añadir al final del describe `'Phase 19 WT-04: worktree cleanup — unit (gitFn stub)'` (antes del cierre del describe, antes del describe E2E), los siguientes 2 tests:

    ```javascript
    it('DANGLING SYMLINK: <wt>.dirty is a symlink to nonexistent path → suffixed (Pitfall #1 / CR-03)', async () => {
      const tmpBase = mkdtempSync(join(tmpdir(), 'kodo-dangling-'));
      const wt = join(tmpBase, 'wt');
      const dirty = `${wt}.dirty`;
      const nonexistent = join(tmpBase, 'nonexistent-target');
      mkdirSync(wt, { recursive: true });
      // Symlink colgante: <wt>.dirty → <tmpBase>/nonexistent-target (que NO existe).
      // existsSync(dirty) → false (sigue el symlink); lstatSync(dirty) → stat
      // exitoso del symlink en sí. CR-03 fix: dispara variante suffixed.
      const { symlinkSync } = await import('node:fs');
      symlinkSync(nonexistent, dirty);
      const session = makeSession({ worktree_path: wt });
      const { logger, events } = makeMemLogger();
      const { gitFn, calls } = makeGitFnStub((cwd, args) => {
        if (args.includes('--show-current')) return 'sess-x';
        if (args.includes('--porcelain')) return 'M something\n'; // dirty
        return '';
      });
      try {
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );
        const moveCall = calls.find((c) => c.args[0] === 'worktree' && c.args[1] === 'move');
        assert.ok(moveCall, 'must call worktree move');
        const target = moveCall.args[3] ?? moveCall.args[moveCall.args.length - 1];
        assert.notEqual(target, dirty, 'target must NOT be the colliding symlink path');
        assert.ok(
          target.startsWith(`${wt}.dirty-`),
          `target must use suffixed variant, got: ${target}`,
        );
        const dirtyEv = events.find((e) => e.fields?.event === 'worktree.cleanup.dirty');
        assert.equal(dirtyEv?.fields?.moved_to, target);
      } finally {
        rmSync(tmpBase, { recursive: true, force: true });
      }
    });

    it('REGULAR FILE: <wt>.dirty is a plain file (not dir) → suffixed (Pitfall #1 / CR-03)', async () => {
      const tmpBase = mkdtempSync(join(tmpdir(), 'kodo-regfile-'));
      const wt = join(tmpBase, 'wt');
      const dirty = `${wt}.dirty`;
      mkdirSync(wt, { recursive: true });
      writeFileSync(dirty, 'pre-existing file blocking the dirty target');
      const session = makeSession({ worktree_path: wt });
      const { logger, events } = makeMemLogger();
      const { gitFn, calls } = makeGitFnStub((cwd, args) => {
        if (args.includes('--show-current')) return 'sess-x';
        if (args.includes('--porcelain')) return 'M something\n'; // dirty
        return '';
      });
      try {
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );
        const moveCall = calls.find((c) => c.args[0] === 'worktree' && c.args[1] === 'move');
        assert.ok(moveCall, 'must call worktree move');
        const target = moveCall.args[3] ?? moveCall.args[moveCall.args.length - 1];
        assert.notEqual(target, dirty, 'target must NOT be the colliding regular file path');
        assert.ok(
          target.startsWith(`${wt}.dirty-`),
          `target must use suffixed variant, got: ${target}`,
        );
        const dirtyEv = events.find((e) => e.fields?.event === 'worktree.cleanup.dirty');
        assert.equal(dirtyEv?.fields?.moved_to, target);
      } finally {
        rmSync(tmpBase, { recursive: true, force: true });
      }
    });
    ```

    Notas críticas:
    - El test DANGLING SYMLINK importa `symlinkSync` dinámicamente con `await import('node:fs')` dentro del test (no top-level) — minimiza el blast radius si Windows/CI no soporta symlinks; el test puede skiparse o fallar limpiamente.
    - El test REGULAR FILE usa `writeFileSync` que ya está importado al top del archivo (verificar antes de añadir).
    - Ambos tests asertan via `target.startsWith(\`${wt}.dirty-\`)` — patrón sugerido por REVIEW IN-03 (más robusto que regex con escape de slashes).
    - Si `writeFileSync` no estuviera importado, añadirlo al import existente: `import { mkdtempSync, rmSync, mkdirSync, writeFileSync, appendFileSync, existsSync } from 'node:fs';` (ya está per la línea 9 del archivo).
  </action>
  <verify>
    <automated>node --test test/stop-worktree-cleanup.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "DANGLING SYMLINK" test/stop-worktree-cleanup.test.js` (test 1 añadido).
    - `grep -q "REGULAR FILE" test/stop-worktree-cleanup.test.js` (test 2 añadido).
    - `grep -q "symlinkSync" test/stop-worktree-cleanup.test.js` (uso de symlink colgante).
    - `grep -c "^  it(" test/stop-worktree-cleanup.test.js` ≥ 9 (8 originales de 19-02 + 2 nuevos; CLEAN/DIRTY/ERROR/COLLISION/LEGACY/BRANCH-D + 2 nuevos + 2 E2E = 10; el grep cuenta los unit, excluye los del describe E2E si están en otro `  it(` indent — ajustar el conteo según indent real, mínimo 8 nuevos+existentes unit).
    - `node --test test/stop-worktree-cleanup.test.js` exit 0 — todos los tests pasan (10/10 unit + 2 E2E = 12 escenarios; el conteo exacto depende de cuántos `it()` haya tras el merge).
    - Cada test nuevo verifica `target.startsWith(\`${wt}.dirty-\`)` (patrón consistente con REVIEW IN-03).
    - El test TARGET COLLISION existente sigue verde (no se modifica).
  </acceptance_criteria>
  <done>2 tests nuevos cubren CR-03: symlink colgante y archivo regular en la ruta `.dirty` ambos disparan la variante suffixed `.dirty-<ts>`. Suite verde. Pattern consistente con TARGET COLLISION existente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Source-hygiene asserts CR-02 + CR-03 en test/stop.test.js (living documentation)</name>
  <files>test/stop.test.js</files>
  <read_first>
    - test/stop.test.js completo (especialmente el describe `'stop.js source hygiene'` y los 3 asserts existentes Phase 19 D-05/D-07/D-08 añadidos en 19-02)
    - src/hooks/stop.js (resultado tras Tasks 1 + 2 — los markers que los grep tests buscan deben existir)
  </read_first>
  <behavior>
    Dos source-hygiene asserts nuevos:

    1. **Phase 19 CR-02**: `markSessionStatus(session.task_id, 'done', 'session-stop', log)` aparece ANTES de la línea `if (session.gsd) {` que envuelve `releaseGsdLock`. Esto blinda que el relocate no se revierta accidentalmente.

    2. **Phase 19 CR-03**: el source contiene `lstatSync(target)` y NO contiene `existsSync(target)` ni `if (existsSync(\`${wt}.dirty\`))`. Garantiza que el pre-check usa lstatSync con discriminación ENOENT en lugar del existsSync que sigue symlinks.
  </behavior>
  <action>
    Añadir 2 `it()` al final del describe `'stop.js source hygiene'` en `test/stop.test.js`, justo después de los 3 asserts existentes (Phase 19 D-05/D-07/D-08 de 19-02):

    ```javascript
    it('Phase 19 CR-02: markSessionStatus is invoked OUTSIDE the if (session.gsd) block (applies to all sessions)', () => {
      const source = readFileSync(STOP_SOURCE_PATH, 'utf-8');
      // El mark debe aparecer en el source y NO estar precedido inmediatamente por
      // `if (session.gsd) {` sin un cierre intermedio. Estrategia simple: el mark
      // debe aparecer ANTES de la línea que contiene `if (session.gsd) {` que
      // envuelve `releaseGsdLock`.
      const markIdx = source.indexOf("markSessionStatus(session.task_id, 'done', 'session-stop'");
      assert.ok(markIdx > 0, 'must find markSessionStatus with new reason session-stop (Phase 19 CR-02)');
      // Buscar el `if (session.gsd) {` que contiene `releaseGsdLock`. Patrón robusto:
      // captura el bloque if (session.gsd) seguido (en pocas líneas) de releaseGsdLock.
      const ifGsdRegex = /if\s*\(session\.gsd\)\s*\{[\s\S]{0,500}releaseGsdLock/;
      const ifMatch = ifGsdRegex.exec(source);
      assert.ok(ifMatch, 'must find if (session.gsd) { ... releaseGsdLock block');
      assert.ok(
        markIdx < ifMatch.index,
        'markSessionStatus must be invoked BEFORE the if (session.gsd) { releaseGsdLock } block (Phase 19 CR-02)',
      );
      // Sanity: la cadena con la razón antigua ya NO debe aparecer (relocate completo).
      assert.equal(
        source.indexOf("'session-stop:lock-released'"),
        -1,
        "old reason 'session-stop:lock-released' must be removed after CR-02 relocate",
      );
    });

    it('Phase 19 CR-03: dirty target pre-check uses lstatSync (NOT existsSync) — symlink-safe', () => {
      const source = readFileSync(STOP_SOURCE_PATH, 'utf-8');
      assert.ok(
        source.includes('lstatSync(target)'),
        'pre-check must use lstatSync(target) per Phase 19 CR-03 (symlink-safe)',
      );
      // existsSync NO debe aparecer en el archivo — el único uso era el pre-check
      // que se sustituyó por lstatSync (CR-03). Si reaparece, regresión.
      assert.equal(
        source.indexOf('existsSync'),
        -1,
        'existsSync must NOT appear in stop.js after CR-03 fix (symlink-following risk)',
      );
      // Sanity: el comentario referencia la decisión.
      assert.ok(
        /Phase 19 CR-03/.test(source),
        'comment must reference Phase 19 CR-03 for traceability',
      );
    });
    ```

    Notas críticas:
    - `STOP_SOURCE_PATH` y `readFileSync` ya están importados al top del archivo (los 3 asserts de 19-02 los usan). NO duplicar imports.
    - NO modificar los 3 asserts existentes Phase 19 D-05/D-07/D-08 (siguen siendo source-of-truth de 19-02).
    - NO modificar el test legacy `'releases lock before removeSession (order matters)'` (Phase 16 LOG-15 invariant).
    - El primer assert usa `markSessionStatus(session.task_id, 'done', 'session-stop'` literal — debe matchear exactamente la cadena que escribió Task 1.
    - El segundo assert verifica AUSENCIA de `existsSync` en el archivo entero — si en el futuro alguien añade `existsSync` para otro propósito, el assert fallará y forzará revisar si la mitigación CR-03 sigue intacta.
  </action>
  <verify>
    <automated>node --test test/stop.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 19 CR-02: markSessionStatus is invoked OUTSIDE" test/stop.test.js` (assert 1 añadido).
    - `grep -q "Phase 19 CR-03: dirty target pre-check uses lstatSync" test/stop.test.js` (assert 2 añadido).
    - `grep -q "releases lock before removeSession" test/stop.test.js` (test legacy intacto).
    - `grep -q "Phase 19 D-07" test/stop.test.js` (asserts de 19-02 preservados).
    - `grep -q "Phase 19 D-08" test/stop.test.js`.
    - `grep -q "Phase 19 D-05" test/stop.test.js`.
    - `node --test test/stop.test.js` exit 0 — los asserts existentes + 2 nuevos pasan (asume Tasks 1 y 2 ejecutados; este task es el último).
  </acceptance_criteria>
  <done>2 source-hygiene asserts nuevos blindan CR-02 (relocate de markSessionStatus) y CR-03 (lstatSync replace existsSync). Pasan tras Tasks 1+2. Living documentation cableada para evitar regresiones futuras.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Este plan no introduce nueva superficie de ataque. CR-02 cambia el orden de mutación de `state.json` (que ya estaba autorizada para el stop hook) y CR-03 mejora la pre-check de filesystem (más defensiva, no menos). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | `markSessionStatus` post-relocate | accept | El relocate no cambia las credenciales ni los paths que el mark puede escribir. La mutación seguía ya autorizada en 19-02 (dentro del `if (session.gsd)` con misma firma `(task_id, status, reason, log)`). Riesgo idéntico al baseline. |
| T-19-03-02 | Denial of Service | `lstatSync(target)` en stop hook | accept | `lstatSync` es synchronous y rápido (μs sobre paths locales). Caso patológico: filesystem stalled (NFS down). El try/catch ya cubre cualquier excepción y dispara el fallback timestamp variant. No degrada disponibilidad respecto a `existsSync` previo. |
| T-19-03-03 | Information Disclosure | `console.error` con `err.message` de `markSessionStatus` (WR-03 fix) | mitigate | El mensaje de error puede contener fragmentos de paths del filesystem del usuario (ej. `EACCES: permission denied, open '/home/.kodo/state.json'`). Mitigación: el log va a `stderr` del child process del hook, capturado por cmux. No se exfiltra a Plane ni a logs públicos. El operador del repo es el único consumer. Mismo riesgo que el `console.error` ya existente para `releaseGsdLock` (línea 206 actual). |
</threat_model>

<verification>
- `node --test test/stop-worktree-cleanup.test.js` exit 0 — 10+ tests pasan (8 originales de 19-02 + 2 nuevos CR-03: DANGLING SYMLINK + REGULAR FILE).
- `node --test test/stop.test.js` exit 0 — los 4 asserts source-hygiene existentes (`releases lock before removeSession` + Phase 19 D-05/D-07/D-08) + 2 nuevos (Phase 19 CR-02 + CR-03) pasan.
- `node --test test/gsd-verify-integration.test.js` exit 0 — sin regresión (no se toca verify.js).
- `node --test test/logger-events.test.js` exit 0 — sin regresión (no se toca logger-events.js).
- `node --test test/skill-auto-commit.test.js` exit 0 — sin regresión (handleOrchestratorStop intacto, D-05).
- `npm test` (full suite) verde — sin regresiones globales.
- Source-hygiene grep manual:
  - `grep -c "markSessionStatus" src/hooks/stop.js` == 1 (una sola invocación productiva).
  - `grep -c "session-stop:lock-released" src/hooks/stop.js` == 0 (razón antigua eliminada).
  - `grep -c "status: session.status" src/hooks/stop.js` == 0 (propagación stale eliminada de sessionEnd).
  - `grep -c "lstatSync(target)" src/hooks/stop.js` ≥ 1.
  - `grep -c "existsSync" src/hooks/stop.js` == 0.
  - `grep -c "Phase 19 CR-03" src/hooks/stop.js` ≥ 1 (comentario traceability).
  - `awk '/markSessionStatus\(session\.task_id/{m=NR} /^    if \(session\.gsd\) \{/{g=NR; exit} END{exit !(m < g)}' src/hooks/stop.js` exit 0 (mark ANTES del if gsd).
- CR-01 override preservation check:
  - `grep -c "findSession" src/session/state.js` debe coincidir con el valor pre-19-03 (no debe haber cambios).
  - Orden en stop.js: el cleanup block (líneas ~210-356) sigue precediendo a `removeSessionFn(id)` (línea ~358) que sigue precediendo al `notify orchestrator` (línea ~362-371) — IDÉNTICO a 19-02. NO reordenado.
</verification>

<success_criteria>
1. **CR-02 cerrado**: `markSessionStatus(session.task_id, 'done', 'session-stop', log)` se ejecuta para TODAS las sesiones (GSD + no-GSD) ANTES de `sessionEnd`; `sessionEnd` emite `status: 'done'` literal (no `session.status`). El catch del mark usa `console.error` (WR-03 fix). El observable NDJSON ahora refleja el estado terminal real para sesiones no-GSD.
2. **CR-03 cerrado**: `lstatSync(target)` en try/catch ENOENT reemplaza `existsSync(target)`. Symlinks colgantes ya no evaden la pre-check de Pitfall #1. 2 tests dedicados (DANGLING SYMLINK + REGULAR FILE) blindan ambos casos patológicos. El test TARGET COLLISION existente sigue verde (regression-free).
3. **CR-01 override honrado**: cero cambios en `src/session/state.js`, en `buildStopNudgeText`, o en el orden cleanup/removeSession/notify. La deuda queda explícita y deferida a Phase 21+ per VERIFICATION.md overrides_applied + CONTEXT.md §D-07.
4. **Fail-open preservado**: `runStopHook` sigue sin crashear bajo ningún path. El outer try/catch top-level intacto. Los catches internos siguen su política existente; el único upgrade es `silent → console.error` en el catch del mark (WR-03).
5. **handleOrchestratorStop intacto** (D-05): `cwd: KODO_ROOT` preservado. Source-hygiene assert existente de 19-02 sigue verde.
6. **No regresiones**: `npm test` verde end-to-end.
7. **2 source-hygiene asserts nuevos** (Phase 19 CR-02 + CR-03) blindan el fix contra regresiones futuras.
</success_criteria>

<output>
After completion, create `.planning/phases/19-worktree-cleanup-integration/19-03-SUMMARY.md` con:
- Gaps cerrados (CR-02 + CR-03) con referencia al fix block de REVIEW.md que se siguió verbatim.
- Override CR-01 referenciado (VERIFICATION.md overrides_applied: 1, accepted_by: alex, deferred_to: Phase 21+).
- Sitios exactos modificados en src/hooks/stop.js (líneas resultantes post-cambio para markSessionStatus relocate + lstatSync replace).
- Tests añadidos: 2 en stop-worktree-cleanup.test.js (DANGLING SYMLINK + REGULAR FILE) + 2 en stop.test.js (Phase 19 CR-02 + CR-03 source-hygiene).
- Resultados de `npm test` (count de tests + estado).
- Confirmación explícita de que NO se tocaron: src/session/state.js, buildStopNudgeText, orden cleanup/removeSession/notify, handleOrchestratorStop, src/gsd/verify.js, src/logger-events.js.
</output>
