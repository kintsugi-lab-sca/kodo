---
phase: 19-worktree-cleanup-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger-events.js
  - test/logger-events.test.js
autonomous: true
requirements:
  - WT-04
tags:
  - logger-events
  - ndjson
  - phase-19
user_setup: []

must_haves:
  truths:
    - "EVENTS frozen object exporta tres strings nuevos: worktree.cleanup.ok, worktree.cleanup.dirty, worktree.cleanup.error (D-10)"
    - "Helper worktreeCleanupOk(logger, fields) escribe NDJSON a nivel info con campos {session_id, worktree_path, branch_deleted} (D-08, D-10)"
    - "Helper worktreeCleanupDirty(logger, fields) escribe NDJSON a nivel warn con campos {session_id, worktree_path, moved_to} (D-02, D-10)"
    - "Helper worktreeCleanupError(logger, fields) escribe NDJSON a nivel error con campos {session_id, worktree_path, phase, reason} donde phase ∈ {status, remove, move, branch, prune} (D-03, D-10)"
    - "El test de inventario (Object.values(EVENTS).sort()) refleja 11 tipos incluyendo los 3 nuevos"
  artifacts:
    - path: "src/logger-events.js"
      provides: "EVENTS extendido + 3 helpers tipados worktreeCleanupOk/Dirty/Error"
      contains: "WORKTREE_CLEANUP_OK"
    - path: "test/logger-events.test.js"
      provides: "Contract tests para los 3 helpers (level + event + shape) + inventory test actualizado"
      contains: "worktreeCleanupOk"
  key_links:
    - from: "src/logger-events.js"
      to: "EVENTS frozen object"
      via: "WORKTREE_CLEANUP_OK / DIRTY / ERROR keys"
      pattern: "WORKTREE_CLEANUP_(OK|DIRTY|ERROR):\\s*'worktree\\.cleanup\\."
    - from: "test/logger-events.test.js"
      to: "src/logger-events.js"
      via: "import { worktreeCleanupOk, worktreeCleanupDirty, worktreeCleanupError, EVENTS }"
      pattern: "worktreeCleanup(Ok|Dirty|Error)"
---

<objective>
Extender `src/logger-events.js` con 3 helpers NDJSON tipados (`worktreeCleanupOk`, `worktreeCleanupDirty`, `worktreeCleanupError`) y 3 nuevos strings en el `EVENTS` frozen object, siguiendo el patrón verbatim de `sessionEnd` (info), `orchestratorReview` (warn) y `planeApiCallFailed` (error). Cubrir contract tests en `test/logger-events.test.js`.

Purpose: Phase 19 D-10 fija la observabilidad del cleanup del worktree como eventos tipados antes de cablear el cleanup en `stop.js` (Plan 02). Aislar el scaffolding (puro, sin I/O ni git ops) permite que Plan 02 importe los helpers sin re-definirlos y que los tests del cleanup en Plan 02 aserten contra eventos ya validados.

Output:
- `src/logger-events.js` con `WORKTREE_CLEANUP_OK/DIRTY/ERROR` en `EVENTS` + 3 helpers exportados.
- `test/logger-events.test.js` con 3 `it()` nuevos (level + shape) + inventory test actualizado a 11 tipos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md
@.planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md
@.planning/phases/19-worktree-cleanup-integration/19-VALIDATION.md
@src/logger-events.js
@test/logger-events.test.js

<interfaces>
<!-- EVENTS frozen object actual (src/logger-events.js:30-39) -->

```javascript
export const EVENTS = Object.freeze({
  SESSION_START:          'session.start',
  SESSION_END:            'session.end',
  STATE_TRANSITION:       'state.transition',
  ORCHESTRATOR_REVIEW:    'orchestrator.review',
  GSD_PHASE_RESOLVED:     'gsd.phase.resolved',
  GSD_BOOTSTRAP:          'gsd.bootstrap',
  PLANE_API_CALL:         'plane.api.call',
  PLANE_API_CALL_FAILED:  'plane.api.call.failed',
});
```

<!-- Helper canónico nivel info (src/logger-events.js:94-111, sessionEnd) -->

```javascript
export function sessionEnd(logger, fields) {
  logger.info(EVENTS.SESSION_END, {
    event: EVENTS.SESSION_END,
    session_id: fields.session_id,
    task_id: fields.task_id,
    status: fields.status,
    ended_at: fields.ended_at,
  });
}
```

<!-- Helper canónico nivel warn condicional (src/logger-events.js:126-139, orchestratorReview) -->

```javascript
export function orchestratorReview(logger, fields) {
  const level = fields.verdict === 'approved' ? 'info' : 'warn';
  logger[level](EVENTS.ORCHESTRATOR_REVIEW, {
    event: EVENTS.ORCHESTRATOR_REVIEW,
    phase_id: fields.phase_id,
    verdict: fields.verdict,
    reason: fields.reason,
  });
}
```

<!-- Helper canónico nivel error (src/logger-events.js:189-204, planeApiCallFailed) -->

```javascript
export function planeApiCallFailed(logger, fields) {
  logger.error(EVENTS.PLANE_API_CALL_FAILED, {
    event: EVENTS.PLANE_API_CALL_FAILED,
    step: fields.step,
    error: fields.error,
  });
}
```

<!-- Level mapping Phase 19 D-10:
     - .ok → logger.info (espejo de sessionEnd)
     - .dirty → logger.warn (espejo de orchestratorReview blocked)
     - .error → logger.error (espejo de planeApiCallFailed) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender EVENTS frozen object con los 3 strings worktree.cleanup.*</name>
  <files>src/logger-events.js, test/logger-events.test.js</files>
  <read_first>
    - src/logger-events.js (líneas 1-50, especialmente el `@type` JSDoc 22-30 y el frozen object 30-39)
    - test/logger-events.test.js (especialmente el inventory test que enumera Object.values(EVENTS).sort())
    - .planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md §3 "src/logger-events.js" (Analog #1)
    - .planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md §"Eventos NDJSON del cleanup" (D-10)
  </read_first>
  <behavior>
    - Test 1: `EVENTS.WORKTREE_CLEANUP_OK === 'worktree.cleanup.ok'`
    - Test 2: `EVENTS.WORKTREE_CLEANUP_DIRTY === 'worktree.cleanup.dirty'`
    - Test 3: `EVENTS.WORKTREE_CLEANUP_ERROR === 'worktree.cleanup.error'`
    - Test 4: `Object.values(EVENTS).sort()` produce exactamente 11 strings incluyendo los 3 nuevos
    - Test 5: `Object.isFrozen(EVENTS) === true` (preservado)
  </behavior>
  <action>
    1. Abrir `src/logger-events.js`.

    2. Actualizar el `@type` JSDoc (líneas ~22-30) añadiendo las 3 propiedades nuevas al final, antes del cierre `}>`:

    ```javascript
    /** @type {Readonly<{
     *   SESSION_START: 'session.start',
     *   SESSION_END: 'session.end',
     *   STATE_TRANSITION: 'state.transition',
     *   ORCHESTRATOR_REVIEW: 'orchestrator.review',
     *   GSD_PHASE_RESOLVED: 'gsd.phase.resolved',
     *   GSD_BOOTSTRAP: 'gsd.bootstrap',
     *   PLANE_API_CALL: 'plane.api.call',
     *   PLANE_API_CALL_FAILED: 'plane.api.call.failed',
     *   WORKTREE_CLEANUP_OK: 'worktree.cleanup.ok',
     *   WORKTREE_CLEANUP_DIRTY: 'worktree.cleanup.dirty',
     *   WORKTREE_CLEANUP_ERROR: 'worktree.cleanup.error',
     * }>} */
    ```

    3. Extender el `EVENTS = Object.freeze({...})` (líneas 30-39) añadiendo las 3 entradas al final:

    ```javascript
    export const EVENTS = Object.freeze({
      SESSION_START:           'session.start',
      SESSION_END:             'session.end',
      STATE_TRANSITION:        'state.transition',
      ORCHESTRATOR_REVIEW:     'orchestrator.review',
      GSD_PHASE_RESOLVED:      'gsd.phase.resolved',
      GSD_BOOTSTRAP:           'gsd.bootstrap',
      PLANE_API_CALL:          'plane.api.call',
      PLANE_API_CALL_FAILED:   'plane.api.call.failed',
      WORKTREE_CLEANUP_OK:     'worktree.cleanup.ok',
      WORKTREE_CLEANUP_DIRTY:  'worktree.cleanup.dirty',
      WORKTREE_CLEANUP_ERROR:  'worktree.cleanup.error',
    });
    ```

    4. Actualizar el comentario del header (líneas ~3-8) — la cabecera dice "Taxonomía cerrada de 7 eventos" (en realidad ya tiene 8, ahora 11). Cambiar a:

    ```javascript
    // src/logger-events.js — Taxonomía cerrada de 11 eventos de ciclo de vida.
    //
    // Contrato fijo por ROADMAP §Phase 7 + extensiones v0.3 (LOG-09)
    // + Phase 19 (worktree cleanup):
    //   session.start, session.end, state.transition, orchestrator.review,
    //   gsd.phase.resolved, gsd.bootstrap, plane.api.call, plane.api.call.failed,
    //   worktree.cleanup.ok, worktree.cleanup.dirty, worktree.cleanup.error
    ```

    5. Abrir `test/logger-events.test.js`. Localizar el inventory test (typically `it('EVENTS exposes exactly N event types', ...)` o equivalente cerca de líneas 45-58) y actualizar el array esperado a la lista ordenada de 11 strings:

    ```javascript
    const types = Object.values(EVENTS).sort();
    assert.deepEqual(types, [
      'gsd.bootstrap',
      'gsd.phase.resolved',
      'orchestrator.review',
      'plane.api.call',
      'plane.api.call.failed',
      'session.end',
      'session.start',
      'state.transition',
      'worktree.cleanup.dirty',
      'worktree.cleanup.error',
      'worktree.cleanup.ok',
    ]);
    ```

    Si el `describe` heading menciona la cifra "8 helpers" (línea ~44), renombrar a:

    ```javascript
    describe('logger-events taxonomy (Phase 7 LOG-09 + Phase 19 worktree cleanup)', () => {
    ```

    6. Mantener el `Object.isFrozen(EVENTS) === true` test si existe; si no existe, NO añadirlo (fuera de scope).
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | grep -E "(pass |fail |tests )" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "WORKTREE_CLEANUP_OK:\s*'worktree\.cleanup\.ok'" src/logger-events.js` (entrada presente en frozen object)
    - `grep -q "WORKTREE_CLEANUP_DIRTY:\s*'worktree\.cleanup\.dirty'" src/logger-events.js`
    - `grep -q "WORKTREE_CLEANUP_ERROR:\s*'worktree\.cleanup\.error'" src/logger-events.js`
    - `grep -q "'worktree\.cleanup\.ok'" test/logger-events.test.js` (inventory test menciona el string)
    - `grep -q "'worktree\.cleanup\.dirty'" test/logger-events.test.js`
    - `grep -q "'worktree\.cleanup\.error'" test/logger-events.test.js`
    - `node --test test/logger-events.test.js` exit code 0
    - `node -e "import('./src/logger-events.js').then(m => { if (!Object.isFrozen(m.EVENTS)) process.exit(1); if (Object.values(m.EVENTS).length !== 11) process.exit(2); })"` exit code 0
  </acceptance_criteria>
  <done>EVENTS contiene 11 strings inmutables. Inventory test esperado actualizado a 11. `node --test test/logger-events.test.js` pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir helpers worktreeCleanupOk/Dirty/Error + tests de contrato</name>
  <files>src/logger-events.js, test/logger-events.test.js</files>
  <read_first>
    - src/logger-events.js (líneas 94-111 `sessionEnd`, 126-139 `orchestratorReview`, 189-204 `planeApiCallFailed` — analogs verbatim)
    - test/logger-events.test.js (líneas ~102-114 `sessionEnd` test, ~207-216 `planeApiCallFailed` test con `assert.equal(line.level, 'error')`)
    - .planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md §3 "src/logger-events.js" (Analog #2/#3/#4 + Diff sugerido líneas 244-292)
    - .planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md §"Eventos NDJSON del cleanup" (D-10, level mapping)
  </read_first>
  <behavior>
    - Test 1 (ok): `worktreeCleanupOk(log, { session_id: 'sess-x', worktree_path: '/tmp/wt', branch_deleted: true })` produce una línea NDJSON con `level === 'info'`, `event === 'worktree.cleanup.ok'`, `branch_deleted === true`.
    - Test 2 (dirty): `worktreeCleanupDirty(log, { session_id: 'sess-x', worktree_path: '/tmp/wt', moved_to: '/tmp/wt.dirty' })` produce línea NDJSON con `level === 'warn'`, `event === 'worktree.cleanup.dirty'`, `moved_to === '/tmp/wt.dirty'`.
    - Test 3 (error): `worktreeCleanupError(log, { session_id: 'sess-x', worktree_path: '/tmp/wt', phase: 'remove', reason: 'EBUSY' })` produce línea NDJSON con `level === 'error'`, `event === 'worktree.cleanup.error'`, `phase === 'remove'`, `reason === 'EBUSY'`.
    - Cada test usa el mismo `createLogger` + `readAllLines(logPathFor(sessionId)).pop()` que los tests existentes.
  </behavior>
  <action>
    1. Añadir al final de `src/logger-events.js` (después del último helper) los 3 helpers VERBATIM:

    ```javascript
    /**
     * Worktree cleanup OK — emitted (info) after a clean worktree was
     * successfully removed and (optionally) its branch deleted (Phase 19 D-08).
     *
     * @param {Logger} logger
     * @param {{ session_id: string, worktree_path: string, branch_deleted: boolean }} fields
     */
    export function worktreeCleanupOk(logger, fields) {
      logger.info(EVENTS.WORKTREE_CLEANUP_OK, {
        event: EVENTS.WORKTREE_CLEANUP_OK,
        session_id: fields.session_id,
        worktree_path: fields.worktree_path,
        branch_deleted: fields.branch_deleted,
      });
    }

    /**
     * Worktree cleanup DIRTY — emitted (warn) when the worktree had uncommitted
     * changes and was moved aside to `<path>.dirty` for human review (Phase 19 D-02).
     *
     * @param {Logger} logger
     * @param {{ session_id: string, worktree_path: string, moved_to: string }} fields
     */
    export function worktreeCleanupDirty(logger, fields) {
      logger.warn(EVENTS.WORKTREE_CLEANUP_DIRTY, {
        event: EVENTS.WORKTREE_CLEANUP_DIRTY,
        session_id: fields.session_id,
        worktree_path: fields.worktree_path,
        moved_to: fields.moved_to,
      });
    }

    /**
     * Worktree cleanup ERROR — emitted (error) when a cleanup step failed
     * unexpectedly (FS error, git lock, race). The stop hook continues
     * fail-open after this event (Phase 19 D-03).
     *
     * @param {Logger} logger
     * @param {{
     *   session_id: string,
     *   worktree_path: string,
     *   phase: 'status' | 'remove' | 'move' | 'branch' | 'prune',
     *   reason: string,
     * }} fields
     */
    export function worktreeCleanupError(logger, fields) {
      logger.error(EVENTS.WORKTREE_CLEANUP_ERROR, {
        event: EVENTS.WORKTREE_CLEANUP_ERROR,
        session_id: fields.session_id,
        worktree_path: fields.worktree_path,
        phase: fields.phase,
        reason: fields.reason,
      });
    }
    ```

    2. Añadir al final del describe block en `test/logger-events.test.js` los 3 `it()` (importando los 3 nuevos helpers desde `../src/logger-events.js` en el bloque de imports existente):

    ```javascript
    it('worktreeCleanupOk emits event=worktree.cleanup.ok at info level', () => {
      const sessionId = 'sess-ev-wtok';
      const log = createLogger({ sessionId, minLevel: 'info' });
      worktreeCleanupOk(log, {
        session_id: sessionId,
        worktree_path: '/tmp/wt',
        branch_deleted: true,
      });
      const line = readAllLines(logPathFor(sessionId)).pop();
      assert.equal(line.event, EVENTS.WORKTREE_CLEANUP_OK);
      assert.equal(line.level, 'info');
      assert.equal(line.session_id, sessionId);
      assert.equal(line.worktree_path, '/tmp/wt');
      assert.equal(line.branch_deleted, true);
    });

    it('worktreeCleanupDirty emits event=worktree.cleanup.dirty at warn level with moved_to', () => {
      const sessionId = 'sess-ev-wtdirty';
      const log = createLogger({ sessionId, minLevel: 'info' });
      worktreeCleanupDirty(log, {
        session_id: sessionId,
        worktree_path: '/tmp/wt',
        moved_to: '/tmp/wt.dirty',
      });
      const line = readAllLines(logPathFor(sessionId)).pop();
      assert.equal(line.event, EVENTS.WORKTREE_CLEANUP_DIRTY);
      assert.equal(line.level, 'warn');
      assert.equal(line.session_id, sessionId);
      assert.equal(line.worktree_path, '/tmp/wt');
      assert.equal(line.moved_to, '/tmp/wt.dirty');
    });

    it('worktreeCleanupError emits event=worktree.cleanup.error at error level with phase+reason', () => {
      const sessionId = 'sess-ev-wterr';
      const log = createLogger({ sessionId, minLevel: 'info' });
      worktreeCleanupError(log, {
        session_id: sessionId,
        worktree_path: '/tmp/wt',
        phase: 'remove',
        reason: 'EBUSY: rmdir failed',
      });
      const line = readAllLines(logPathFor(sessionId)).pop();
      assert.equal(line.event, EVENTS.WORKTREE_CLEANUP_ERROR);
      assert.equal(line.level, 'error');
      assert.equal(line.session_id, sessionId);
      assert.equal(line.worktree_path, '/tmp/wt');
      assert.equal(line.phase, 'remove');
      assert.equal(line.reason, 'EBUSY: rmdir failed');
    });
    ```

    3. Asegurar que la import line al inicio de `test/logger-events.test.js` incluye `worktreeCleanupOk, worktreeCleanupDirty, worktreeCleanupError` junto a los helpers ya importados.

    4. No tocar otros tests existentes. No modificar el shape de los helpers existentes.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function worktreeCleanupOk" src/logger-events.js`
    - `grep -q "export function worktreeCleanupDirty" src/logger-events.js`
    - `grep -q "export function worktreeCleanupError" src/logger-events.js`
    - `grep -q "logger.info(EVENTS.WORKTREE_CLEANUP_OK" src/logger-events.js` (level=info)
    - `grep -q "logger.warn(EVENTS.WORKTREE_CLEANUP_DIRTY" src/logger-events.js` (level=warn)
    - `grep -q "logger.error(EVENTS.WORKTREE_CLEANUP_ERROR" src/logger-events.js` (level=error)
    - `grep -q "worktreeCleanupOk emits event=worktree.cleanup.ok at info level" test/logger-events.test.js`
    - `grep -q "worktreeCleanupDirty emits event=worktree.cleanup.dirty at warn level" test/logger-events.test.js`
    - `grep -q "worktreeCleanupError emits event=worktree.cleanup.error at error level" test/logger-events.test.js`
    - `node --test test/logger-events.test.js` exit code 0
    - `npm test -- --test-name-pattern="worktreeCleanup"` (o equivalente) demuestra los 3 tests pasando
  </acceptance_criteria>
  <done>3 helpers exportados con shape + level correctos. 3 it() pasan. `node --test test/logger-events.test.js` exit 0. Sin regresión en tests existentes del archivo.</done>
</task>

</tasks>

<verification>
- `node --test test/logger-events.test.js` exit 0 con todos los tests pasando (existentes + 4 nuevos: inventory + 3 helpers).
- `node -e "const m = await import('./src/logger-events.js'); console.log(Object.values(m.EVENTS).length === 11 && typeof m.worktreeCleanupOk === 'function' && typeof m.worktreeCleanupDirty === 'function' && typeof m.worktreeCleanupError === 'function')"` imprime `true`.
- `npm test` (full suite) no introduce regresiones — la suite sigue verde.
- LOG-12 invariant preservado: `grep -r "logger-events" src/check.js src/cli/format.js 2>/dev/null` debe devolver 0 matches (el archivo no debe ser importado por el grafo de `kodo check`).
</verification>

<success_criteria>
1. EVENTS exporta 11 strings incluyendo los 3 nuevos (frozen + ordenados consistentemente).
2. 3 helpers exportados con level mapping correcto (info / warn / error) y shape exacto declarado en D-10.
3. Tests de contrato pasan asertando event string, level y campos específicos.
4. Sin modificaciones a `stop.js` (eso es Plan 02 — depends_on [01]).
5. LOG-12 invariant intacto (logger-events.js sigue sin ser importado por `src/check.js` o `src/cli/format.js`).
</success_criteria>

<output>
After completion, create `.planning/phases/19-worktree-cleanup-integration/19-01-SUMMARY.md` con:
- Decisiones aplicadas (D-10 → 3 helpers + 3 EVENTS strings)
- Shape exacto de cada helper (firma + payload)
- Tests añadidos (4 nuevos: inventory updated + 3 helpers)
- Confirmación de invariant LOG-12 preservado
- Handoff a Plan 02: helpers listos para importar desde `src/hooks/stop.js`
</output>
