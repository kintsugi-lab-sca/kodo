---
phase: 21-skill-sync-cli-auto-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skill/sync.js
  - src/cli/skill-sync.js
  - src/cli.js
  - src/logger-events.js
  - test/skill-sync.test.js
autonomous: true
requirements: [SKILL-01, SKILL-04]
tags: [skill-sync, cli, sha256, walker, exit-codes]

must_haves:
  truths:
    - "T-21.01-01 (D-02): syncSkill detecta drift por hash SHA-256 archivo por archivo (no mtime)"
    - "T-21.01-02 (D-04): syncSkill detecta lstat.isSymbolicLink() y reemplaza el symlink con dir real via rmSync+mkdirSync"
    - "T-21.01-03 (D-05): sin --prune, archivos foráneos en dest se PRESERVAN; con --prune se BORRAN con console.warn por cada uno"
    - "T-21.01-04 (D-07): kodo skill sync exit 0 ok / 0 noop / 1 fs error / 2 not a kodo repo con stderr canonical"
    - "T-21.01-05 (D-06b): salida TTY coloreada via createFormatter; --json byte-deterministic single-line"
    - "T-21.01-06 (D-09): EVENTS.SKILL_SYNC_AUTO + EVENTS.SKILL_SYNC_AUTO_ERROR añadidos a logger-events.js junto con helpers skillSyncAuto/skillSyncAutoError"
    - "T-21.01-07 (D-08): syncSkill es función pura — NO emite eventos NDJSON; caller decide"
    - "T-21.01-08 (D-01): syncSkill opera SOLO sobre el par fijo `<repo>/.claude/skills/kodo-orchestrate/` → `~/.claude/skills/kodo-orchestrate/`; no acepta path arbitrario en Phase 21 (otras skills NO son responsabilidad de kodo)"
  artifacts:
    - path: "src/skill/sync.js"
      provides: "Módulo único de sincronización canonical→home con walker manual + SHA-256"
      exports: ["syncSkill"]
      min_lines: 80
    - path: "src/cli/skill-sync.js"
      provides: "Handler CLI con DI completa + exit codes deterministas"
      exports: ["runSkillSyncCli"]
      min_lines: 50
    - path: "src/cli.js"
      provides: "Commander subgrupo `kodo skill sync [--prune] [--json]`"
      contains: "program.command('skill')"
    - path: "src/logger-events.js"
      provides: "2 helpers tipados skillSyncAuto + skillSyncAutoError + 2 keys en EVENTS"
      contains: "SKILL_SYNC_AUTO"
    - path: "test/skill-sync.test.js"
      provides: "4 escenarios SKILL-04 spawnSync + escenario symlink legacy + source-hygiene grep"
      min_lines: 150
  key_links:
    - from: "src/cli.js"
      to: "src/cli/skill-sync.js"
      via: "lazy dynamic import in .action()"
      pattern: "await import\\(['\\\"]\\./cli/skill-sync\\.js['\\\"]\\)"
    - from: "src/cli/skill-sync.js"
      to: "src/skill/sync.js"
      via: "static import { syncSkill }"
      pattern: "from\\s+['\\\"]\\.\\./skill/sync\\.js['\\\"]"
    - from: "src/cli/skill-sync.js"
      to: "src/cli/format.js"
      via: "createFormatter (color isolation — picocolors NO direct import)"
      pattern: "from\\s+['\\\"]\\./format\\.js['\\\"]"
---

<objective>
Implementar el núcleo de Phase 21: módulo único `src/skill/sync.js` (función pura `syncSkill`), handler CLI `src/cli/skill-sync.js`, wiring Commander en `src/cli.js`, helpers tipados en `src/logger-events.js`, y el test file con 4 escenarios SKILL-04 + symlink + source-hygiene.

Purpose: Materializar SKILL-01 (CLI manual diff-aware con --prune opt-in) + SKILL-04 (exit codes deterministas 0/1/2 con stderr canonical) — la fundación que SKILL-02 auto-sync consumirá en Plan 02.

Output: 4 archivos nuevos + 2 modificados, suite verde con ~10 escenarios nuevos cubriendo D-01..D-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-CONTEXT.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-RESEARCH.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md

# Source canonical (read-only — Phase 21 NUNCA la modifica)
@.claude/skills/kodo-orchestrate/skill.md

# Analogs directos (Pattern Map §File Classification)
@src/cli/gsd-verify.js
@src/cli.js
@src/logger-events.js
@src/cli/format.js
@src/hooks/stop.js
@test/skill-auto-commit.test.js
@test/dispatcher-isolation.test.js
@test/format-isolation.test.js

<interfaces>
<!-- Contratos consumidos desde el codebase existente. Extraídos de las lecturas
     de las líneas 1-275 de cli.js y 1-267 de logger-events.js (Read-once).
     NO re-explorar el árbol — los executores tienen los signatures aquí. -->

From src/cli/format.js (createFormatter factory — Phase 14 D-01):
```js
// Returns object with bound methods: ok/fail/green/yellow/red/blue/etc + formatRow/formatTable/visibleWidth.
// useColor calculado eager: NO_COLOR > FORCE_COLOR > stream.isTTY.
export function createFormatter(stream, env = process.env): Formatter
```

From src/logger-events.js (EVENTS frozen object — líneas 35-47):
```js
export const EVENTS = Object.freeze({
  SESSION_START: 'session.start',
  // ... existing 11 keys ...
  WORKTREE_CLEANUP_OK: 'worktree.cleanup.ok',
  WORKTREE_CLEANUP_DIRTY: 'worktree.cleanup.dirty',
  WORKTREE_CLEANUP_ERROR: 'worktree.cleanup.error',
});
// Phase 21 añade 2 keys más al final del Object.freeze: SKILL_SYNC_AUTO + SKILL_SYNC_AUTO_ERROR.
```

From src/logger-events.js (helper pattern — líneas 215-228, worktreeCleanupOk como molde literal):
```js
export function worktreeCleanupOk(logger, fields) {
  logger.info(EVENTS.WORKTREE_CLEANUP_OK, {
    event: EVENTS.WORKTREE_CLEANUP_OK,
    session_id: fields.session_id,
    worktree_path: fields.worktree_path,
    branch_deleted: fields.branch_deleted,
  });
}
```

From src/cli.js (Commander subgrupo `kodo gsd` — líneas 241-274, molde a copiar):
```js
const gsd = program.command('gsd').description('GSD subcommands (inspect resolver, etc.)');
gsd.command('inspect <task-id>').description(...).option('--json', ...).action(async (taskId, opts) => {
  try {
    await ensureConfig();  // <- NO en `kodo skill sync` (Open Question 1: gate D-07 sustituye)
    const { runGsdInspect } = await import('./cli/gsd-inspect.js');
    const code = await runGsdInspect({ taskId, json: opts.json || false });
    process.exit(code);
  } catch (err) {
    console.error(`Error: ${err.message}`);
    process.exit(1);
  }
});
```

From src/cli/gsd-verify.js (handler DI shape — líneas 17-85, molde para runSkillSyncCli):
```js
export async function runGsdVerifyCli(opts, deps = {}) {
  const write = deps.writeFn || ((s) => process.stdout.write(s));
  const err = deps.errFn || ((s) => process.stderr.write(s));
  const runVerifyFn = deps.runVerifyFn || runGsdVerify;
  const fmt = (deps.formatterFn || (() => createFormatter(process.stdout)))();
  // ... handler body
}
```

From src/cli.js insertion point (línea 275 — DESPUÉS de gsd verify, ANTES de program.parse()):
```js
gsd
  .command('verify <session-id>')
  .description(...)
  // ... líneas 261-273 cierran aquí en línea 274.
program.parse();  // línea 276
// ─── PHASE 21 INSERTION GOES BETWEEN líneas 274 y 276 ───
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/skill/sync.js (módulo puro syncSkill — D-01, D-02, D-04, D-05, D-08) + test/skill-sync.test.js scaffolding</name>
  <files>src/skill/sync.js, test/skill-sync.test.js</files>
  <read_first>
    - .planning/phases/21-skill-sync-cli-auto-sync/21-RESEARCH.md (§Module Structure líneas 69-141 — API + walker internals)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md (§`src/skill/sync.js` líneas 21-118 — DI signature + symlink-detection + fail-soft try/catch + KODO_ROOT-style)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-CONTEXT.md (§Implementation Decisions D-01..D-05 + D-08 — scope/diff/symlink/prune/SoSoT)
    - src/hooks/stop.js (líneas 205-363 — patrón fail-soft outer try/catch + lstatSync wrapper Phase 19 D-02)
    - src/gsd/verify.js (líneas 1-100 — DI signature `runGsdVerify(opts, deps)` con typedef + JSDoc shape)
    - test/skill-auto-commit.test.js (líneas 22-99 — makeFixture + spawnSync + HOME override + timeout: 10000 + NO_COLOR=1)
    - test/dispatcher-isolation.test.js (líneas 24-63 — stripComments + grep importers para source-hygiene)
  </read_first>
  <behavior>
    syncSkill({ source, dest, prune?, logger? }) — función pura, sin emit de eventos:
    - Test 1 (happy path first-sync): source con 2 archivos (`skill.md` + `subdir/extra.md`); dest no existe — return `{ status: 'ok', files_changed: 2, symlink_replaced: undefined }`; ambos archivos copiados byte-idénticos.
    - Test 2 (noop sin drift): segundo run sobre dest ya sync — return `{ status: 'noop', files_changed: 0 }`; mtimes inalterados (proof de skip por hash match).
    - Test 3 (single-file diff): modificar `skill.md` source entre runs — return `{ status: 'ok', files_changed: 1 }`; `subdir/extra.md` NO retocado.
    - Test 4 (symlink legacy → replaced): dest = symlinkSync('/nonexistent/path', dest) ANTES de syncSkill — return `{ status: 'ok', symlink_replaced: true, files_changed: 2 }`; lstatSync(dest).isDirectory() === true post-run.
    - Test 5 (prune=false default): añadir `~/.claude/skills/kodo-orchestrate/foreign.md` que NO existe en source; sin --prune → archivo preservado; `files_pruned` undefined.
    - Test 6 (prune=true): mismo escenario + `prune: true` → archivo borrado; `files_pruned: 1`; `console.warn` capturado contiene literal `[kodo skill sync --prune] removing foreign: foreign.md`.
    - Test 7 (source no existe): source dir vacío sin skill.md → return `{ status: 'error', error: matches /source skill not found/ }`.
    - Test 8 (recursivo): source con 3 niveles `skill.md` + `subdir/a.md` + `subdir/nested/b.md`; primer run copia los 3.
    - 4 escenarios SKILL-04 spawnSync + symlink CLI + source-hygiene grep van en Task 2 (test file ya creado, escenarios añadidos al mismo file).
  </behavior>
  <action>
**Paso 1.A — Crear `src/skill/sync.js` con header + imports + typedefs.**

Header literal (mirror de Pattern §Header lines 27-44):

```javascript
// @ts-check
//
// src/skill/sync.js — Phase 21 D-08: módulo único de sincronización canonical → home.
//
// Responsabilidades:
//   1. Detectar drift por hash SHA-256 archivo por archivo (D-02 — no mtime).
//   2. Detectar y reemplazar symlink legacy en dest (D-04).
//   3. Copiar archivos cambiados; preservar foráneos salvo opts.prune (D-05).
//   4. Return { status, files_changed, files_pruned?, symlink_replaced?, error? }.
//      NO emite eventos (caller decide — D-08 SoSoT).
//
// Invariantes:
//   - lstatSync (NO statSync) detecta symlink sin seguirlo (Phase 19 D-02 patrón).
//   - rmSync(symlinkPath, { force: true }) borra solo el link, no el target.
//   - syncSkill es función pura testeable: NO emite eventos; el caller los emite (D-08).
//   - Walker manual recursivo (NO fs.cp) para control fino sobre diff hash + prune list.

import { createHash } from 'node:crypto';
import {
  readFileSync, writeFileSync, readdirSync, mkdirSync,
  lstatSync, rmSync, existsSync,
} from 'node:fs';
import { join, dirname, relative } from 'node:path';
```

Typedefs literales (mirror de Pattern §DI signature):

```javascript
/**
 * @typedef {{
 *   source: string,
 *   dest: string,
 *   prune?: boolean,
 *   logger?: import('../logger.js').Logger,
 * }} SyncSkillOpts
 *
 * @typedef {{
 *   status: 'ok' | 'noop' | 'error',
 *   files_changed: number,
 *   files_pruned?: number,
 *   symlink_replaced?: boolean,
 *   error?: string,
 * }} SyncSkillResult
 */
```

**Paso 1.B — Implementar `syncSkill(opts)` con outer try/catch + walker manual.**

Estructura interna en orden de ejecución (RESEARCH §Internals walker manual recomendado, líneas 105-127):

```javascript
/**
 * @param {SyncSkillOpts} opts
 * @returns {SyncSkillResult}
 */
export function syncSkill(opts) {
  const { source, dest, prune = false } = opts;
  let filesChanged = 0;
  let filesPruned = 0;
  let symlinkReplaced = false;

  try {
    // 1. Validar source: skill.md DEBE existir (D-07 traducción a 'error').
    if (!existsSync(join(source, 'skill.md'))) {
      return { status: 'error', files_changed: 0, error: 'source skill not found' };
    }

    // 2. D-04: detectar y reemplazar symlink legacy (lstatSync NO sigue el link).
    try {
      const st = lstatSync(dest);
      if (st.isSymbolicLink()) {
        rmSync(dest, { force: true }); // borra solo el link, no el target
        mkdirSync(dest, { recursive: true });
        symlinkReplaced = true;
      }
    } catch (err) {
      // ENOENT: dest no existe — mkdirSync más abajo lo crea. Otros errores: caen al mkdirSync siguiente.
      if (/** @type {NodeJS.ErrnoException} */ (err).code !== 'ENOENT') {
        // fall-through silencioso (defense in depth)
      }
    }

    // 3. Asegurar dest existe (idempotente).
    mkdirSync(dest, { recursive: true });

    // 4. Walker recursivo del source → array de rel paths (regular files only).
    const sourceFiles = walkFiles(source);

    // 5. Hash + diff por archivo. Recolectamos el Set para reusar en prune.
    /** @type {Set<string>} */
    const sourceSet = new Set(sourceFiles);
    for (const relPath of sourceFiles) {
      const srcAbs = join(source, relPath);
      const destAbs = join(dest, relPath);
      const srcContent = readFileSync(srcAbs);
      const srcHash = createHash('sha256').update(srcContent).digest('hex');

      let needsCopy = false;
      if (!existsSync(destAbs)) {
        needsCopy = true;
      } else {
        const destHash = createHash('sha256').update(readFileSync(destAbs)).digest('hex');
        if (destHash !== srcHash) needsCopy = true;
      }

      if (needsCopy) {
        mkdirSync(dirname(destAbs), { recursive: true });
        writeFileSync(destAbs, srcContent); // reuso srcContent — evita doble I/O
        filesChanged += 1;
      }
    }

    // 6. D-05 prune (opt-in destructivo, default false).
    if (prune === true) {
      const destFiles = walkFiles(dest);
      for (const relPath of destFiles) {
        if (!sourceSet.has(relPath)) {
          console.warn(`[kodo skill sync --prune] removing foreign: ${relPath}`);
          rmSync(join(dest, relPath), { force: true });
          filesPruned += 1;
        }
      }
    }

    // 7. Status: ok si hubo cambios o symlink replaced; noop si no.
    const status = (filesChanged > 0 || symlinkReplaced) ? 'ok' : 'noop';
    /** @type {SyncSkillResult} */
    const result = { status, files_changed: filesChanged };
    if (prune === true) result.files_pruned = filesPruned;
    if (symlinkReplaced) result.symlink_replaced = true;
    return result;
  } catch (err) {
    return {
      status: 'error',
      files_changed: filesChanged,
      error: /** @type {Error} */ (err).message,
    };
  }
}

/**
 * Walker recursivo de archivos regulares. Returns relative paths from `root`.
 * @param {string} root
 * @returns {string[]}
 */
function walkFiles(root) {
  /** @type {string[]} */
  const out = [];
  function recurse(currentAbs) {
    const entries = readdirSync(currentAbs, { withFileTypes: true });
    for (const e of entries) {
      const abs = join(currentAbs, e.name);
      if (e.isDirectory()) {
        recurse(abs);
      } else if (e.isFile()) {
        out.push(relative(root, abs));
      }
      // Symlinks dentro del árbol: ignorados (defense — la skill canonical no los tiene).
    }
  }
  if (existsSync(root)) recurse(root);
  return out.sort(); // determinismo (D-02 — hash + orden estable)
}
```

**Paso 1.C — Crear `test/skill-sync.test.js` con 8 tests unitarios in-process** (Task 2 añadirá los spawnSync escenarios SKILL-04).

Imports + constants + makeFixture (mirror exacto de test/skill-auto-commit.test.js:22-73):

```javascript
// @ts-check
import { describe, it, afterEach } from 'node:test';
import assert from 'node:assert/strict';
import {
  mkdtempSync, rmSync, mkdirSync, writeFileSync, readFileSync,
  lstatSync, symlinkSync, chmodSync, existsSync,
} from 'node:fs';
import { tmpdir } from 'node:os';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { syncSkill } from '../src/skill/sync.js';

const __dirname = dirname(fileURLToPath(import.meta.url));
const REPO = resolve(__dirname, '..');

function makeFixture() {
  const tmpHome = mkdtempSync(join(tmpdir(), 'kodo-skill-sync-home-'));
  const tmpRepo = mkdtempSync(join(tmpdir(), 'kodo-skill-sync-repo-'));
  const skillDir = join(tmpRepo, '.claude', 'skills', 'kodo-orchestrate');
  mkdirSync(skillDir, { recursive: true });
  writeFileSync(join(skillDir, 'skill.md'), '# kodo:orchestrate\n\nCanonical body v1.\n', 'utf-8');
  mkdirSync(join(skillDir, 'subdir'), { recursive: true });
  writeFileSync(join(skillDir, 'subdir', 'extra.md'), 'extra content\n', 'utf-8');
  return { tmpHome, tmpRepo, skillDir };
}

function destOf(tmpHome) {
  return join(tmpHome, '.claude', 'skills', 'kodo-orchestrate');
}
```

Implementar los 8 tests con `describe('syncSkill (unit)', () => {...})` cubriendo:
- Test 1: happy path first-sync — assert `result.status === 'ok'`, `result.files_changed === 2`, `existsSync(join(dest,'skill.md')) === true`, `readFileSync(join(dest,'subdir/extra.md'), 'utf-8') === 'extra content\n'`.
- Test 2: noop sin drift — primer run + segundo run; `second.status === 'noop'`, `second.files_changed === 0`.
- Test 3: single-file diff — primer run + modificar `skill.md` source + segundo run; `second.status === 'ok'`, `second.files_changed === 1`.
- Test 4: symlink legacy → replaced — `symlinkSync('/nonexistent/path/to/old', dest)` PRE-syncSkill; assert `result.symlink_replaced === true`, `lstatSync(dest).isDirectory() === true`, `lstatSync(dest).isSymbolicLink() === false`.
- Test 5: prune=false default — añadir `foreign.md` en dest tras primer run; segundo run sin prune; assert `existsSync(join(dest,'foreign.md')) === true`, `result.files_pruned === undefined`.
- Test 6: prune=true — mismo escenario + capturar console.warn (override temporal); assert `result.files_pruned === 1`, `existsSync(join(dest,'foreign.md')) === false`, warn capturado matches `/\[kodo skill sync --prune\] removing foreign: foreign\.md/`.
- Test 7: source sin skill.md — `tmpRepo` con dir vacío sin skill.md → `result.status === 'error'`, `result.error.match(/source skill not found/)`.
- Test 8: walker recursivo 3 niveles — sembrar `subdir/nested/b.md` adicional; `result.files_changed === 3` en primer run.

afterEach cleanup (mirror de test/skill-auto-commit.test.js:107-115):
```javascript
let _tmpHome, _tmpRepo;
afterEach(() => {
  if (_tmpHome) { try { chmodSync(destOf(_tmpHome), 0o755); } catch {}; rmSync(_tmpHome, { recursive: true, force: true }); }
  if (_tmpRepo) rmSync(_tmpRepo, { recursive: true, force: true });
  _tmpHome = undefined; _tmpRepo = undefined;
});
```
  </action>
  <verify>
    <automated>node --test test/skill-sync.test.js 2>&1 | tee /tmp/p21-01-task1.log; grep -E "(^# pass|^# fail|^# tests)" /tmp/p21-01-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/skill-sync.test.js` → al menos 8 tests pass, 0 fail.
    - `grep -c "^export function syncSkill" src/skill/sync.js` → exactamente 1.
    - `grep -c "^export function walkFiles" src/skill/sync.js` → 0 (helper privado, sin export).
    - `grep -E "from 'node:crypto'" src/skill/sync.js` → 1 match (`createHash`).
    - `grep -E "lstatSync" src/skill/sync.js` → al menos 1 match (D-04 symlink detection).
    - `grep -E "rmSync.*force.*true" src/skill/sync.js` → al menos 1 match (D-04 + D-05 prune).
    - `grep -E "from\\s+['\\\"]node:" src/skill/sync.js | wc -l` → 3 (crypto, fs, path).
    - No leak de picocolors: `grep -c "picocolors" src/skill/sync.js` → 0.
  </acceptance_criteria>
  <done>
    src/skill/sync.js exporta syncSkill function pura con outer try/catch + walker manual + symlink detection + prune opt-in; test/skill-sync.test.js 8 unit tests pasando; sin dependencias nuevas; D-01..D-05 + D-08 implementados como código.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/cli/skill-sync.js handler (D-06, D-07) + wiring Commander en src/cli.js + extender test/skill-sync.test.js con 4 escenarios SKILL-04 spawnSync + symlink CLI + source-hygiene grep</name>
  <files>src/cli/skill-sync.js, src/cli.js, test/skill-sync.test.js</files>
  <read_first>
    - .planning/phases/21-skill-sync-cli-auto-sync/21-RESEARCH.md (§CLI Subcommand Wiring líneas 211-321 + §stderr Canonical Messages líneas 322-342)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md (§`src/cli/skill-sync.js` líneas 121-222 + §`src/cli.js` líneas 226-263 — molde literal)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-CONTEXT.md (§D-06 + D-07 — subgrupo + exit codes)
    - src/cli/gsd-verify.js (líneas 17-85 — handler DI shape literal a copiar)
    - src/cli/gsd-inspect.js (renderHuman + JSON branch — analog secundario)
    - src/cli.js (líneas 241-274 — Commander subgrupo `kodo gsd` molde literal a copiar)
    - src/cli/format.js (factory createFormatter — fmt.ok/yellow/green/red signature)
  </read_first>
  <behavior>
    runSkillSyncCli({ prune, json }, deps) — handler con DI completa:
    - Test CLI 1 (SKILL-04 #1 ok): spawnSync `bin/kodo skill sync` desde tmpRepo con skill canonical; assert `status === 0`, stdout matches `/Synced 2 files? to /`, dest tiene `skill.md` + `subdir/extra.md` byte-idénticos.
    - Test CLI 2 (SKILL-04 #2 noop): primer run exitoso + segundo run; segundo `status === 0`, stdout matches `/No drift/`.
    - Test CLI 3 (SKILL-04 #3 fs error): primer run + chmod dest 0o500 (read-only) + segundo run modificando source; `status === 1`, stderr matches `/^Error: filesystem error: /`. AfterEach restaura permisos.
    - Test CLI 4 (SKILL-04 #4 not a kodo repo): spawn desde dir vacío sin `.claude/skills/kodo-orchestrate/skill.md`; `status === 2`, stderr exacto: `Error: not a kodo repository (no .claude/skills/kodo-orchestrate/skill.md found)\n`.
    - Test CLI 5 (D-04 symlink CLI): dest = symlink obsoleto pre-spawn; `status === 0`, stdout contiene `Legacy symlink replaced`, dest post-spawn es directorio real.
    - Test CLI 6 (--json bytes): spawn con `--json`; stdout matches `/^\{"status":"ok","files_changed":2\}\n$/` — single-line, sin pretty-print, sin ANSI.
    - Test CLI 7 (--prune): sembrar `foreign.md` en dest + spawn con `--prune`; stdout contiene `Pruned 1 foreign file`; stderr contiene `[kodo skill sync --prune] removing foreign: foreign.md`.
    - Test CLI 8 (source-hygiene D-08b): readFileSync src/cli/skill-sync.js → stripComments → assert matches `/from\s+['"]\.\.\/skill\/sync\.js['"]/`; readFileSync src/cli/skill-sync.js → assert 0 matches `/picocolors/` (color isolation).
  </behavior>
  <action>
**Paso 2.A — Crear `src/cli/skill-sync.js` con handler completo.**

Archivo literal (mirror de Pattern §`src/cli/skill-sync.js` líneas 127-222):

```javascript
// @ts-check
//
// src/cli/skill-sync.js — Action handler de `kodo skill sync`.
//
// Responsabilidades (CONTEXT §D-06, D-07, D-08):
//   1. Gate: ¿cwd es un repo kodo? (exit 2 + stderr canonical D-07).
//   2. Invocar syncSkill (lógica vive en src/skill/sync.js — D-08 SoSoT).
//   3. Render: human (default) coloreado via createFormatter, o JSON (--json).
//   4. Exit codes: 0 (ok/noop) — 1 (fs error) — 2 (no kodo repo).
//
// Color isolation invariante (Phase 14 D-07): este archivo NUNCA importa
// picocolors directamente — solo createFormatter. Blindado por test/format-isolation.test.js.

import { join } from 'node:path';
import { homedir } from 'node:os';
import { existsSync } from 'node:fs';
import { syncSkill } from '../skill/sync.js';
import { createFormatter } from './format.js';

/**
 * @typedef {{ prune?: boolean, json?: boolean }} RunSkillSyncCliOpts
 *
 * @typedef {{
 *   syncFn?: typeof syncSkill,
 *   writeFn?: (s: string) => void,
 *   errFn?: (s: string) => void,
 *   formatterFn?: () => import('./format.js').Formatter,
 *   cwdFn?: () => string,
 * }} RunSkillSyncCliDeps
 */

/**
 * @param {RunSkillSyncCliOpts} opts
 * @param {RunSkillSyncCliDeps} [deps]
 * @returns {Promise<number>} exit code per D-07 (0 ok/noop, 1 fs error, 2 no kodo repo).
 */
export async function runSkillSyncCli(opts, deps = {}) {
  const write = deps.writeFn || ((s) => process.stdout.write(s));
  const err = deps.errFn || ((s) => process.stderr.write(s));
  const syncFn = deps.syncFn || syncSkill;
  const cwd = deps.cwdFn ? deps.cwdFn() : process.cwd();
  const fmt = (deps.formatterFn || (() => createFormatter(process.stdout)))();

  const source = join(cwd, '.claude', 'skills', 'kodo-orchestrate');
  const dest = join(homedir(), '.claude', 'skills', 'kodo-orchestrate');

  // Gate D-07 exit 2: stderr canonical message exacto.
  if (!existsSync(join(source, 'skill.md'))) {
    err('Error: not a kodo repository (no .claude/skills/kodo-orchestrate/skill.md found)\n');
    return 2;
  }

  /** @type {import('../skill/sync.js').SyncSkillResult} */
  let result;
  try {
    result = syncFn({ source, dest, prune: opts.prune === true });
  } catch (e) {
    err(`Error: filesystem error: ${/** @type {Error} */ (e).message}\n`);
    return 1;
  }
  if (result.status === 'error') {
    err(`Error: filesystem error: ${result.error || 'unknown'}\n`);
    return 1;
  }

  if (opts.json === true) {
    // D-06b: single-line JSON byte-deterministic (LOG-12 + DX-06 invariante).
    /** @type {Record<string, any>} */
    const payload = {
      status: result.status,
      files_changed: result.files_changed,
    };
    if (opts.prune === true) payload.files_pruned = result.files_pruned ?? 0;
    if (result.symlink_replaced === true) payload.symlink_replaced = true;
    write(JSON.stringify(payload) + '\n');
  } else {
    renderHuman(result, dest, write, fmt);
  }
  return 0;
}

/**
 * @private
 * @param {import('../skill/sync.js').SyncSkillResult} result
 * @param {string} dest
 * @param {(s: string) => void} write
 * @param {import('./format.js').Formatter} fmt
 */
function renderHuman(result, dest, write, fmt) {
  if (result.symlink_replaced === true) {
    write(`${fmt.yellow('⚠')} Legacy symlink replaced at ${dest}\n`);
  }
  if (result.status === 'noop') {
    write(`${fmt.ok('No drift')} — ${dest} up to date\n`);
  } else {
    const n = result.files_changed;
    write(`${fmt.ok(`Synced ${n} file${n === 1 ? '' : 's'} to ${dest}`)}\n`);
  }
  if (result.files_pruned !== undefined && result.files_pruned > 0) {
    write(`${fmt.yellow(`Pruned ${result.files_pruned} foreign file${result.files_pruned === 1 ? '' : 's'}`)}\n`);
  }
}
```

NOTA sobre `fmt.ok`: si el factory `createFormatter` no expone `fmt.ok` directamente (mirror gsd-verify.js usa `fmt.green('pass')`), inspeccionar `src/cli/format.js` en read_first y usar el método correcto (ej. `fmt.green` para verde). El test 6 (--json) bypassea esto.

**Paso 2.B — Wire Commander en `src/cli.js` línea 275** (DESPUÉS del cierre del `gsd.command('verify')` línea 274, ANTES de `program.parse()` línea 276).

Bloque a INSERTAR (mirror exacto de Pattern §`src/cli.js` líneas 236-256):

```javascript
// --- kodo skill <subcommand> ---
const skill = program.command('skill').description('Skill management subcommands (sync, etc.)');

skill
  .command('sync')
  .description('Sync canonical skill <repo>/.claude/skills/kodo-orchestrate/ → ~/.claude/skills/kodo-orchestrate/')
  .option('--prune', 'Remove foreign files in home that are not in repo (destructive; opt-in)')
  .option('--json', 'Emit structured result as JSON (scriptable)')
  .action(async (opts) => {
    try {
      // NOTE: NO `ensureConfig()` — kodo skill sync no requiere provider configurado
      // (RESEARCH §Open Question 1; gate D-07 exit 2 sustituye al check de config).
      const { runSkillSyncCli } = await import('./cli/skill-sync.js');
      const code = await runSkillSyncCli({ prune: opts.prune || false, json: opts.json || false });
      process.exit(code);
    } catch (err) {
      console.error(`Error: ${err.message}`);
      process.exit(1);
    }
  });
```

**Paso 2.C — Extender `test/skill-sync.test.js` con 8 escenarios CLI** (añadir un segundo `describe('runSkillSyncCli (integration spawnSync)', () => { ... })` debajo del primero).

Imports adicionales al top:
```javascript
import { spawnSync } from 'node:child_process';
const KODO_BIN = join(REPO, 'bin', 'kodo');

function runCli({ tmpHome, tmpRepo, args = [], cwd }) {
  return spawnSync(
    process.execPath,
    [KODO_BIN, 'skill', 'sync', ...args],
    {
      cwd: cwd ?? tmpRepo,
      env: { ...process.env, HOME: tmpHome, NO_COLOR: '1' },
      encoding: 'utf-8',
      timeout: 10000,
    },
  );
}
```

Implementar 8 tests CLI (Test CLI 1..8 listados en `<behavior>`). Casos clave con asserts exactos:

```javascript
it('SKILL-04 #4: not a kodo repo → exit 2 + stderr canonical exacto', () => {
  const emptyCwd = mkdtempSync(join(tmpdir(), 'kodo-not-a-repo-'));
  try {
    ({ tmpHome: _tmpHome } = makeFixture());
    const result = runCli({ tmpHome: _tmpHome, tmpRepo: emptyCwd, cwd: emptyCwd });
    assert.equal(result.status, 2);
    assert.equal(
      result.stderr,
      'Error: not a kodo repository (no .claude/skills/kodo-orchestrate/skill.md found)\n',
    );
  } finally {
    rmSync(emptyCwd, { recursive: true, force: true });
  }
});

it('SKILL-04 --json: byte-deterministic single-line', () => {
  ({ tmpHome: _tmpHome, tmpRepo: _tmpRepo } = makeFixture());
  const result = runCli({ tmpHome: _tmpHome, tmpRepo: _tmpRepo, args: ['--json'] });
  assert.equal(result.status, 0);
  assert.match(result.stdout, /^\{"status":"ok","files_changed":2\}\n$/);
  // No ANSI escapes leak (LOG-12 + DX-06).
  assert.equal(/\x1b\[/.test(result.stdout), false);
});

it('D-08b source-hygiene: syncSkill solo importado desde CLI handler + (Plan 02 cubre launch.js)', () => {
  const cliHandler = readFileSync(join(REPO, 'src', 'cli', 'skill-sync.js'), 'utf-8');
  // stripComments verbatim de test/dispatcher-isolation.test.js:24-30
  function stripComments(src) {
    return src.replace(/\/\*[\s\S]*?\*\//g, '')
      .split('\n').filter((l) => !l.trim().startsWith('//') && !l.trim().startsWith('*'))
      .join('\n');
  }
  assert.match(stripComments(cliHandler), /from\s+['"]\.\.\/skill\/sync\.js['"]/);
  // Color isolation: NO picocolors leak.
  assert.equal(/from\s+['"]picocolors['"]/.test(stripComments(cliHandler)), false);
});
```

Los 8 tests CLI suben el total del file a ~16 (8 unit + 8 CLI).

**Verificar `fmt.ok` vs `fmt.green`:** abrir `src/cli/format.js` durante read_first; si el factory NO exporta `fmt.ok`, usar `fmt.green` en `renderHuman` (Pattern §`src/cli/skill-sync.js` línea 213 muestra `fmt.ok` pero gsd-verify.js usa `fmt.green('pass')` — preferencia: usar el método que ya exista en format.js sin añadir nuevos).
  </action>
  <verify>
    <automated>node --test test/skill-sync.test.js 2>&1 | tail -25 | tee /tmp/p21-01-task2.log; node /Users/alex/dev/klab/kodo/bin/kodo skill sync --help 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/skill-sync.test.js` → ≥16 tests pass, 0 fail (8 unit + 8 CLI).
    - `node bin/kodo skill sync --help` exit 0, salida contiene `--prune` y `--json`.
    - `grep -c "program.command('skill')" src/cli.js` → exactamente 1.
    - `grep -c "import { runSkillSyncCli }" src/cli.js` → 0 (lazy dynamic import dentro del action, NO top-level).
    - `grep -E "await import\\(['\\\"]\\./cli/skill-sync\\.js['\\\"]\\)" src/cli.js` → 1 match.
    - `grep -c "from '../skill/sync.js'" src/cli/skill-sync.js` → exactamente 1.
    - `grep -c "picocolors" src/cli/skill-sync.js` → 0 (color isolation).
    - `grep -E "Error: not a kodo repository \\(no \\.claude/skills/kodo-orchestrate/skill\\.md found\\)" src/cli/skill-sync.js` → 1 match.
    - `grep -E "Error: filesystem error:" src/cli/skill-sync.js | grep -v '^#' | wc -l` → ≥1.
    - `npm test 2>&1 | tail -5 | grep -E "fail 0|0 fail"` → suite global verde (no regresión).
  </acceptance_criteria>
  <done>
    `kodo skill sync` ejecutable end-to-end con D-06 subgrupo Commander, D-07 4 exit codes con stderr canonical, D-06b dual TTY/JSON output sin ANSI leak, D-08 lazy import + DI completa, single-source-of-color verde, 16 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Añadir EVENTS.SKILL_SYNC_AUTO + EVENTS.SKILL_SYNC_AUTO_ERROR + helpers skillSyncAuto/skillSyncAutoError en src/logger-events.js (D-09)</name>
  <files>src/logger-events.js, test/logger-events.test.js</files>
  <read_first>
    - .planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md (§`src/logger-events.js` líneas 265-332 — insertion point + helper shape literal)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-CONTEXT.md (§D-09 — 2 helpers tipados, NO skill.sync.manual)
    - src/logger-events.js (LECTURA COMPLETA — header lines 1-17, EVENTS frozen lines 22-47, worktreeCleanupOk/Dirty/Error lines 214-267 como molde literal)
    - test/logger-events.test.js (LECTURA COMPLETA — ver patrón de asserts sobre EVENTS keys + helpers)
  </read_first>
  <behavior>
    EVENTS object congelado extendido con 2 keys + 2 helpers tipados:
    - Test A: `EVENTS.SKILL_SYNC_AUTO === 'skill.sync.auto'`.
    - Test B: `EVENTS.SKILL_SYNC_AUTO_ERROR === 'skill.sync.auto.error'`.
    - Test C: `skillSyncAuto(logger, { source, dest, files_changed: 3 })` → logger.info llamado con event='skill.sync.auto' + payload `{ event, source, dest, files_changed: 3 }`.
    - Test D: `skillSyncAutoError(logger, { source, dest, error: 'EACCES' })` → logger.error llamado con event='skill.sync.auto.error' + payload `{ event, source, dest, error: 'EACCES' }`.
    - Test E: NO existe `EVENTS.SKILL_SYNC_AUTO_NOOP` ni helper `skillSyncAutoNoop` (D-03b — sin .noop event para evitar ruido).
    - Test F: EVENTS sigue siendo `Object.isFrozen(EVENTS) === true`.
  </behavior>
  <action>
**Paso 3.A — Extender `EVENTS` frozen object** (líneas 35-47 actuales).

ANTES (líneas 44-47):
```javascript
  WORKTREE_CLEANUP_OK:     'worktree.cleanup.ok',
  WORKTREE_CLEANUP_DIRTY:  'worktree.cleanup.dirty',
  WORKTREE_CLEANUP_ERROR:  'worktree.cleanup.error',
});
```

DESPUÉS:
```javascript
  WORKTREE_CLEANUP_OK:     'worktree.cleanup.ok',
  WORKTREE_CLEANUP_DIRTY:  'worktree.cleanup.dirty',
  WORKTREE_CLEANUP_ERROR:  'worktree.cleanup.error',
  SKILL_SYNC_AUTO:         'skill.sync.auto',
  SKILL_SYNC_AUTO_ERROR:   'skill.sync.auto.error',
});
```

**Paso 3.B — Actualizar el `@type` JSDoc** (líneas 22-34 actuales) añadiendo las 2 nuevas keys al final del shape:

```javascript
/** @type {Readonly<{
 *   SESSION_START: 'session.start',
 *   SESSION_END: 'session.end',
 *   STATE_TRANSITION: 'state.transition',
 *   ORCHESTRATOR_REVIEW: 'orchestrator.review',
 *   GSD_PHASE_RESOLVED: 'gsd.phase.resolved',
 *   GSD_BOOTSTRAP: 'gsd.bootstrap',
 *   PLANE_API_CALL: 'plane.api.call',
 *   PLANE_API_CALL_FAILED: 'plane.api.call.failed',
 *   WORKTREE_CLEANUP_OK: 'worktree.cleanup.ok',
 *   WORKTREE_CLEANUP_DIRTY: 'worktree.cleanup.dirty',
 *   WORKTREE_CLEANUP_ERROR: 'worktree.cleanup.error',
 *   SKILL_SYNC_AUTO: 'skill.sync.auto',
 *   SKILL_SYNC_AUTO_ERROR: 'skill.sync.auto.error',
 * }>} */
```

**Paso 3.C — Actualizar el header comment** (líneas 4-9) añadiendo Phase 21 a la enumeración:

ANTES (líneas 4-9):
```javascript
// Contrato fijo por ROADMAP §Phase 7 + extensiones v0.3 (LOG-09)
// + Phase 19 (worktree cleanup):
//   session.start, session.end, state.transition, orchestrator.review,
//   gsd.phase.resolved, gsd.bootstrap, plane.api.call, plane.api.call.failed,
//   worktree.cleanup.ok, worktree.cleanup.dirty, worktree.cleanup.error
```

DESPUÉS:
```javascript
// Contrato fijo por ROADMAP §Phase 7 + extensiones v0.3 (LOG-09)
// + Phase 19 (worktree cleanup) + Phase 21 (skill sync):
//   session.start, session.end, state.transition, orchestrator.review,
//   gsd.phase.resolved, gsd.bootstrap, plane.api.call, plane.api.call.failed,
//   worktree.cleanup.ok, worktree.cleanup.dirty, worktree.cleanup.error,
//   skill.sync.auto, skill.sync.auto.error
```

Y actualizar el JSDoc de file-header línea 3 si dice "11 eventos":
```javascript
// src/logger-events.js — Taxonomía cerrada de 13 eventos de ciclo de vida.
```

**Paso 3.D — Añadir helpers `skillSyncAuto` + `skillSyncAutoError`** al final del archivo (después de la línea 267 donde cierra `worktreeCleanupError`). Mirror literal de Pattern §`src/logger-events.js` líneas 281-316:

```javascript
/**
 * Skill sync AUTO ok — emitted (info) when launchOrchestrator auto-syncs
 * the canonical skill from repo → home (Phase 21 D-03b). `files_changed` is
 * the count of files actually copied this run (may be 0 if drift was resolved
 * via symlink-replace alone — caller normalizes that case).
 *
 * @param {Logger} logger
 * @param {{ source: string, dest: string, files_changed: number }} fields
 */
export function skillSyncAuto(logger, fields) {
  logger.info(EVENTS.SKILL_SYNC_AUTO, {
    event: EVENTS.SKILL_SYNC_AUTO,
    source: fields.source,
    dest: fields.dest,
    files_changed: fields.files_changed,
  });
}

/**
 * Skill sync AUTO error — emitted (error) when the auto-sync in launchOrchestrator
 * failed (FS error, permissions, etc). The orchestrator continues fail-open
 * (Phase 21 D-03 — mismo principio que worktree cleanup Phase 19 D-03).
 *
 * @param {Logger} logger
 * @param {{ source: string, dest: string, error: string }} fields
 */
export function skillSyncAutoError(logger, fields) {
  logger.error(EVENTS.SKILL_SYNC_AUTO_ERROR, {
    event: EVENTS.SKILL_SYNC_AUTO_ERROR,
    source: fields.source,
    dest: fields.dest,
    error: fields.error,
  });
}
```

**Paso 3.E — Extender `test/logger-events.test.js`** (si existe) con 6 tests:

- Test A: `assert.equal(EVENTS.SKILL_SYNC_AUTO, 'skill.sync.auto')`.
- Test B: `assert.equal(EVENTS.SKILL_SYNC_AUTO_ERROR, 'skill.sync.auto.error')`.
- Test C: mock logger; `skillSyncAuto(mock, { source: '/a', dest: '/b', files_changed: 3 })`; assert mock.info called con `('skill.sync.auto', { event: 'skill.sync.auto', source: '/a', dest: '/b', files_changed: 3 })`.
- Test D: mock logger; `skillSyncAutoError(mock, { source: '/a', dest: '/b', error: 'EACCES' })`; assert mock.error called con `('skill.sync.auto.error', { event: ..., source: '/a', dest: '/b', error: 'EACCES' })`.
- Test E: `assert.equal(EVENTS.SKILL_SYNC_AUTO_NOOP, undefined)` (D-03b — explicit guard).
- Test F: `assert.equal(Object.isFrozen(EVENTS), true)`.

Si `test/logger-events.test.js` no existe, leer el archivo en read_first; si no, crear el file con los 6 tests siguiendo el patrón de imports de `test/logger-redaction.test.js`.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | tail -15; grep -c "SKILL_SYNC_AUTO" src/logger-events.js; grep -c "export function skillSyncAuto" src/logger-events.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/logger-events.test.js` → todos los tests pre-existentes pass + ≥6 nuevos pass.
    - `grep -c "SKILL_SYNC_AUTO:" src/logger-events.js` → exactamente 1 (la key en EVENTS).
    - `grep -c "SKILL_SYNC_AUTO_ERROR:" src/logger-events.js` → exactamente 1.
    - `grep -cE "^export function skillSyncAuto" src/logger-events.js` → exactamente 2 (Auto + AutoError).
    - `grep -c "skill.sync.auto.noop" src/logger-events.js` → 0 (D-03b — sin .noop event).
    - `node -e "import('./src/logger-events.js').then(m => { console.log(Object.isFrozen(m.EVENTS), m.EVENTS.SKILL_SYNC_AUTO); })"` → outputs `true skill.sync.auto`.
    - `npm test 2>&1 | tail -5` → suite verde, sin regresiones.
  </acceptance_criteria>
  <done>
    EVENTS extendido con 2 keys + 2 helpers tipados análogos a worktreeCleanupOk/Error; D-09 implementado; tests verdes; Object.freeze invariante preservado; sin event `.noop` por D-03b.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → CLI process | El operador invoca `kodo skill sync` desde un cwd que controla; cwd determina `source` (lectura local). |
| CLI process → FS user home | `homedir()` derivado de `process.env.HOME`; el proceso escribe en `~/.claude/skills/kodo-orchestrate/` propiedad del usuario. |
| syncSkill → readFileSync (source) | Lectura de archivos en el repo kodo (skill canonical); inputs son code-versionado. |
| syncSkill → writeFileSync (dest) | Escritura en home del usuario; bytes provienen del repo (verified content). |
| Symlink legacy at dest | Atacante teórico podría sustituir el symlink con uno apuntando a `/etc/passwd`; mitigado: `rmSync(symlink)` borra solo el link, no el target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.01-01 | Tampering | syncSkill writeFileSync | accept | El contenido viene del repo (code-versionado git); no es untrusted input. Walker manual no sigue symlinks dentro del árbol → no se puede sembrar archivo externo via symlink en source. |
| T-21.01-02 | Tampering | Symlink replacement | mitigate | `lstatSync` NO sigue el link + `rmSync(symlinkPath, { force: true })` borra solo el link, NUNCA toca el target. Atacante que sustituya `~/.claude/skills/kodo-orchestrate` por un symlink a `/etc/passwd` NO ve `/etc/passwd` borrado — sólo el link desaparece. |
| T-21.01-03 | Information Disclosure | Hash computation | accept | SHA-256 sobre contenido local; no exfiltration; no network calls; hash queda en memoria del proceso y descartado al exit. |
| T-21.01-04 | Denial of Service | Walker recursivo en source | accept | Source es la skill canonical ~50KB / ~5-10 archivos; walker síncrono completa en <5ms. No accepta arg para arbitrary path en Phase 21 (D-01 — paths fijos). |
| T-21.01-05 | Denial of Service | Walker recursivo en dest (con --prune) | accept | Dest es controlado por el usuario; si el usuario tiene 10K archivos en `~/.claude/skills/kodo-orchestrate/`, el walker los lista — pero esto es self-inflicted. `--prune` es opt-in informado (D-05b). |
| T-21.01-06 | Elevation of Privilege | rmSync con prune en home | mitigate | `--prune` es opt-in explícito (D-05); cada borrado precede `console.warn` con path literal (D-05b). El operador VE qué se borra antes de borrarse. Auto-sync NUNCA hace prune (D-05c). |
| T-21.01-07 | Repudiation | Sin audit log del CLI manual | accept | El CLI imprime stdout con conteo + `--json` para scripts; el operador puede pipear a archivo. Auto-sync emite NDJSON event (Plan 02). Manual no requiere log dedicated (D-09 explícito — sin `skill.sync.manual` event). |
| T-21.01-08 | Spoofing | Process env HOME override | accept | Tests legítimos lo usan (mismo patrón Phase 999.1 D-16); operador con HOME malicioso es self-DoS no exploit. |

**threats_open: 0** — todos los threats tienen disposition (3 mitigate, 5 accept con rationale explícito).
</threat_model>

<verification>
- `node --test test/skill-sync.test.js test/logger-events.test.js` → todos verdes (8 unit + 8 CLI + 6 events = 22 tests).
- `node bin/kodo skill sync --help` → exit 0, output contiene `--prune` y `--json`.
- `cd /tmp && mkdir -p kodo-not-a-repo && cd kodo-not-a-repo && node /Users/alex/dev/klab/kodo/bin/kodo skill sync; echo "exit=$?"` → stderr canonical + exit=2.
- `npm test 2>&1 | tail -5` → suite global verde (no regresión cross-phase).
- `grep -c "picocolors" src/skill/sync.js src/cli/skill-sync.js` → 0 (color isolation invariante Phase 14 D-07).
- `grep -c "skill.sync.auto" src/orchestrator/launch.js` → 0 (auto-sync se cabela en Plan 02 — Plan 01 SÓLO crea infra).
</verification>

<success_criteria>
- SKILL-01 satisfied: `kodo skill sync` copia diff archivos repo → home, preserva foráneos sin `--prune` (D-05), borra con `--prune` opt-in (D-05b). Verificable por 4 escenarios CLI + 6 unit syncSkill.
- SKILL-04 satisfied: 4 exit codes deterministas con stderr canonical (D-07) cubiertos por 4 tests spawnSync.
- D-04 satisfied: symlink legacy detectado y reemplazado en primera ejecución; idempotente en segunda.
- D-08 satisfied: `syncSkill` es función pura testeable, NO emite eventos (caller decide); CLI handler lo consume con DI.
- D-09 satisfied: 2 helpers tipados + 2 keys en EVENTS; NO se añade `skill.sync.manual` ni `.noop`.
- Color isolation Phase 14 D-07 preservado: `picocolors` NO importado desde nuevos archivos.
- LOG-12 + DX-06 invariantes: `--json` byte-deterministic single-line sin ANSI.
- Suite global verde (≥570 tests pass — baseline 567 + ~22 nuevos = ~589).
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-sync-cli-auto-sync/21-01-SUMMARY.md` documentando:
- Files created: src/skill/sync.js, src/cli/skill-sync.js, test/skill-sync.test.js
- Files modified: src/cli.js (+~17 LOC subgroup), src/logger-events.js (+~30 LOC: 2 EVENTS keys + 2 helpers + header update), test/logger-events.test.js (+~30 LOC)
- Decisions implemented: D-01 (paths fijos), D-02 (SHA-256), D-04 (symlink replace), D-05 (prune opt-in), D-06 (Commander subgroup + dual TTY/JSON), D-07 (4 exit codes), D-08 (syncSkill puro + DI), D-09 (2 helpers tipados)
- Test counts: 8 syncSkill unit + 8 CLI spawnSync + 6 logger-events = 22 nuevos tests
- Suite global delta (567 baseline → 589 target)
- Plan 02 dependencies satisfied: `syncSkill` + `skillSyncAuto/Error` + `KODO_ROOT_FOR_SKILL` pattern reference disponibles.
</output>
