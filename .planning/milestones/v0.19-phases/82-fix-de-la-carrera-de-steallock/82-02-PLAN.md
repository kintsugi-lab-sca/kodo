---
phase: 82-fix-de-la-carrera-de-steallock
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - .planning/debug/resolved/gsd-lock-race-cr01.md
  - .planning/STATE.md
autonomous: true
requirements: [LOCK-02, LOCK-03]
must_haves:
  truths:
    # Probe item 2 (LOCK-02, unclassified → covered)
    - "El harness `gsd-lock-race` (CR-01, byte-idéntico D-07) pasa verde ≥50× bajo carga paralela con 0 fallos y con el assert `exactly one` intacto — el repro original fallaba ~48% en esas condiciones; el verde determinista valida la garantía real sin enmascarar (LOCK-02)."
    - "La suite completa (~2364 tests, `npm test`) sigue verde tras el fix, sin regresión en `src/gsd/lock.js` ni en sus consumidores (`doctor.decideLock`, dispatch, release) — success criterion 3."
    # LOCK-03 documental closure
    - "R-81-01 y la debug session `gsd-lock-race-cr01` figuran formalmente cerradas: el fichero movido a `.planning/debug/resolved/` con su Outcome actualizado (fecha, mecanismo del fix, commit) y la fila «Carrera real confirmada en `stealLock`» de STATE.md §Deferred Items marcada cerrada (LOCK-03)."
  prohibitions:
    - statement: "NO modificar `test/gsd-lock-race.test.js` ni `test/helpers/lock-race-child.mjs` para lograr el verde del loop de estrés — byte-idénticos (D-07); un verde obtenido enmascarando es el TOP THREAT (T-82-02)."
      status: flagged-unverified
    - statement: "NO documentar el cierre (D-09/D-10) antes de que el loop de estrés ≥50× esté verde — un cierre sin evidencia de determinismo es doc-drift (el tipo que HYG-08/DEBT-02 vinieron a erradicar)."
      status: flagged-unverified
  artifacts:
    - ".planning/debug/resolved/gsd-lock-race-cr01.md — debug session movida a resolved/ con Outcome actualizado (D-09)"
    - ".planning/STATE.md — fila §Deferred Items «Carrera real confirmada en stealLock» cerrada (D-10)"
  key_links:
    - "El loop de estrés reejecuta el MISMO harness CR-01 del Plan 82-01 bajo carga paralela — evidencia de determinismo, no un test nuevo"
    - "El Outcome de la debug session cita el commit del fix del Plan 82-01 (mecanismo guard+rename-in-place)"
---

<objective>
Evidenciar el verde DETERMINISTA del fix (LOCK-02) y cerrar formalmente la deuda documental (LOCK-03). Reejecutar el harness CR-01 byte-idéntico ≥50× bajo carga paralela con 0 fallos (el repro original fallaba ~48%), correr la suite completa sin regresión, y cerrar los artefactos: mover la debug session a `.planning/debug/resolved/` con Outcome actualizado (D-09) y marcar cerrada la fila de STATE.md §Deferred Items (D-10).

Purpose: probar que la garantía es real y estable (no una pasada afortunada) y saldar R-81-01 + la debug session `gsd-lock-race-cr01` con la resolución documentada — cierra el workstream sin doc-drift.
Output: evidencia de estrés documentada en el SUMMARY, `gsd-lock-race-cr01.md` en `resolved/` con Outcome, fila STATE.md cerrada.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-fix-de-la-carrera-de-steallock/82-CONTEXT.md
@.planning/phases/82-fix-de-la-carrera-de-steallock/82-RESEARCH.md
@.planning/debug/gsd-lock-race-cr01.md
@.planning/phases/82-fix-de-la-carrera-de-steallock/82-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evidencia de verde determinista bajo estrés (LOCK-02) + suite completa</name>
  <files>(sin cambios de código — produce evidencia documentada en el SUMMARY)</files>
  <read_first>
    - `test/gsd-lock-race.test.js` — el harness CR-01 (`raceGsdStealDeadHolder` N=2/N=5) que se reejecuta bajo carga; confirmar que sigue byte-idéntico (`git diff --quiet` limpio) ANTES de correr el loop.
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-RESEARCH.md` §"Open Questions" #2 (N del loop: ≥50 iteraciones, réplica de las condiciones 13/50 y 19/40 del repro) y §"Validation Architecture" (Sampling Rate).
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-VALIDATION.md` §"Manual-Only Verifications" (el comando del loop y el 0-fallos esperado).
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-01-SUMMARY.md` — para citar el commit/mecanismo del fix en la evidencia.
  </read_first>
  <action>
    Producir la evidencia de determinismo de LOCK-02 en dos pasos, sin tocar ningún fichero de test (D-07):

    1. Loop de estrés del harness CR-01: ejecutar `node --test test/gsd-lock-race.test.js` en un bucle de N≥50 iteraciones, idealmente con la suite corriendo en paralelo o bajo carga (replica las condiciones del repro original que medía ~48% de fallos: 13/50 y 19/40). Contar iteraciones con fallo — el criterio de aceptación es 0 fallos. Registrar el comando exacto, el N usado, la carga concurrente aplicada y el conteo (0/N) como evidencia en el SUMMARY. Ejemplo de bucle: iterar 50 veces `node --test test/gsd-lock-race.test.js`, capturar el exit code de cada pasada y sumar los no-cero. El N exacto (≥50) es discreción del implementador según el research.

    2. Suite completa: ejecutar `npm test` (≈2364 tests) y confirmar verde total — sin regresión en `src/gsd/lock.js` ni en consumidores (`doctor.decideLock`, dispatch, release). Registrar el conteo total pass/fail en el SUMMARY.

    Antes de ambos, confirmar `git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs` limpio (harness byte-idéntico). Si el loop muestra CUALQUIER fallo, NO enmascarar (prohibido D-07): parar, reportar el interleaving observado y devolver al fix (Plan 82-01). Un fallo residual del segundo mecanismo benigno (`raceGsdChildren` hold-expiry por spawn-jitter, 1/40 documentado en CONTEXT §Deferred) se distingue del bug de producto CR-01 y se trata como issue de harness aparte, jamás debilitando el assert.
  </action>
  <verify>
    <automated>git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs && fails=0; for i in $(seq 1 50); do node --test test/gsd-lock-race.test.js >/dev/null 2>&1 || fails=$((fails+1)); done; echo "stress fails: $fails/50"; test "$fails" -eq 0 && npm test >/dev/null 2>&1 && echo "full suite green"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs` limpio antes de correr (harness byte-idéntico, D-07).
    - El loop de N≥50 iteraciones de `node --test test/gsd-lock-race.test.js` produce 0 fallos; el comando, N y conteo (0/N) quedan documentados en el SUMMARY.
    - `npm test` verde total (≈2364 tests, 0 fail); el conteo pass/fail queda documentado en el SUMMARY.
    - El assert `exactly one` de los casos CR-01 permanece intacto (no `.skip`, no retries, no timeouts subidos).
  </acceptance_criteria>
  <done>El fix es determinista: ≥50 pasadas del harness CR-01 bajo carga con 0 fallos y la suite completa verde, con el harness byte-idéntico y la evidencia registrada.</done>
</task>

<task type="auto">
  <name>Task 2: Cierre documental — debug session → resolved/ (D-09) + fila STATE.md (D-10)</name>
  <files>.planning/debug/resolved/gsd-lock-race-cr01.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/debug/gsd-lock-race-cr01.md` (fichero completo) — la debug session a mover; su sección `## Outcome` (final del fichero) describe el estado "flaky-red a propósito, lock.js intacto" que hay que actualizar a "resuelto".
    - `.planning/STATE.md` §"Deferred Items" — la fila «**Carrera real confirmada en `stealLock`**» (categoría Concurrencia, columna Estado = "**Programado → v0.19 Phase 82** (LOCK-01..03)") y el párrafo introductorio de la sección (menciona "la carrera de `stealLock` → **Phase 82**") a marcar cerrados.
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-CONTEXT.md` §D-09/D-10 — el contrato exacto del cierre documental.
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-01-SUMMARY.md` — el commit SHA y el mecanismo del fix a citar en el Outcome.
  </read_first>
  <action>
    Cerrar formalmente los artefactos de R-81-01 y la debug session (LOCK-03):

    1. D-09 — mover `.planning/debug/gsd-lock-race-cr01.md` a `.planning/debug/resolved/gsd-lock-race-cr01.md` con `git mv` (el subdirectorio `resolved/` ya existe, confirmado). Actualizar su sección `## Outcome` añadiendo la resolución: fecha de cierre (2026-07-24 o la real), el mecanismo del fix (steal-guard `O_EXCL` + reemplazo in-place atómico `renameSync(tmp→lockPath)` que elimina la ventana briefly-empty por construcción, D-01/D-02), el commit SHA del fix (del Plan 82-01), y que el test CR-01 pasa verde determinista ≥50× bajo carga (evidencia del Task 1). NO borrar el diagnóstico previo — es la traza de la causa raíz; sólo AÑADIR el desenlace resuelto.

    2. D-10 — en `.planning/STATE.md` §"Deferred Items", marcar cerrada la fila «**Carrera real confirmada en `stealLock`**»: cambiar la columna Estado de "**Programado → v0.19 Phase 82** (LOCK-01..03)" a una marca de cierre (p. ej. "✅ **Cerrada** — fix real en Phase 82 (commit `<sha>`): ventana move-aside eliminada por construcción; CR-01 verde determinista"). Actualizar coherentemente el párrafo introductorio de la sección que decía "la carrera de `stealLock` → **Phase 82** ... se cerrarán al completar sus fases" para reflejar que la de `stealLock` YA está cerrada. NO tocar las otras filas de Deferred Items (WR-01/02, Nyquist, etc. — son de otras fases).

    Nota: STATE.md §Current Position / Session Continuity / progress las actualiza el flujo execute/verify, no este plan — este task se ciñe a la fila de Deferred Items (D-10) y al Outcome de la debug session (D-09).
  </action>
  <verify>
    <automated>test -f .planning/debug/resolved/gsd-lock-race-cr01.md && test ! -f .planning/debug/gsd-lock-race-cr01.md && grep -qiE 'cerrad|resuel' .planning/debug/resolved/gsd-lock-race-cr01.md && node -e "const s=require('node:fs').readFileSync('.planning/STATE.md','utf8'); const m=s.match(/Carrera real confirmada en .stealLock.[^|]*\|([^|]*)\|/); if(m && /cerrad|✅/i.test(m[1])) process.exit(0); console.error('fila Deferred no marcada cerrada'); process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/debug/resolved/gsd-lock-race-cr01.md` existe y `.planning/debug/gsd-lock-race-cr01.md` ya NO existe (movido con `git mv`, historial preservado).
    - La sección `## Outcome` del fichero movido contiene la resolución: fecha, mecanismo (guard `O_EXCL` + reemplazo in-place atómico), commit del fix, y verde determinista del CR-01 — sin borrar el diagnóstico de causa raíz previo.
    - En `.planning/STATE.md`, la fila §Deferred Items «Carrera real confirmada en `stealLock`» tiene su columna Estado marcada cerrada (contiene `cerrada`/`✅`); el resto de filas intactas.
    - El párrafo introductorio de §Deferred Items refleja que la de `stealLock` ya está cerrada (no "programado").
  </acceptance_criteria>
  <done>La debug session `gsd-lock-race-cr01` vive en `resolved/` con su Outcome resuelto y la fila de STATE.md §Deferred Items figura cerrada con el mecanismo y commit del fix — R-81-01 saldada (LOCK-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-harness ↔ garantía | La integridad de la garantía LOCK-01 depende de que el harness CR-01 y su assert `exactly one` no se debiliten; el loop de estrés es la evidencia de que el verde es real, no enmascarado |
| documentación ↔ estado real | El cierre documental (Deferred Items, debug session) debe reflejar el estado verificado del código, no una intención — doc-drift es el riesgo (HYG-08/DEBT-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-82-02 | Tampering (integridad de la garantía) | `test/gsd-lock-race.test.js` + loop de estrés | high | mitigate | Harness byte-idéntico verificado (`git diff --quiet`) ANTES del loop; assert `exactly one` intacto; verde ≥50× bajo carga con 0 fallos es la evidencia. Enmascarar está prohibido (prohibitions). |
| T-82-05 | Repudiation (doc-drift) | STATE.md §Deferred Items + debug session Outcome | medium | mitigate | El cierre documental sólo se escribe DESPUÉS del verde determinista (Task 1 gate); el Outcome cita commit + mecanismo verificables; la traza de causa raíz se preserva (no se borra). |
| T-82-SC | Tampering | npm/pip/cargo installs | low | accept | Sin instalaciones esta fase — invariante cero-deps. Sin superficie de supply-chain; nada que verificar. |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (surfaced, no silent drops)

- El loop de estrés a N=50 replica las condiciones del repro original (~48% de fallos). Si el entorno de ejecución tiene MENOS carga/paralelismo que el diagnóstico (12 cores, load avg ~5-8), 50 pasadas verdes son evidencia necesaria pero podrían no reproducir el peor caso; mitigación: correr bajo la suite en paralelo (o carga equivalente) como pide el research, y subir N por discreción si el entorno es holgado.

> No-silent-drop: el probe item 2 (LOCK-02) queda representado como `must_haves.truths` en este plan; los items 1/3/4/5/6 quedan en el Plan 82-01. 6 items del edge-probe == 6 truths authored + 0 flagged (equality global entre ambos planes).
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces

Este plan NO crea símbolos de código nuevos. Produce:
- Evidencia de estrés (texto en el SUMMARY: comando, N, conteo 0/N, conteo de la suite).
- `.planning/debug/resolved/gsd-lock-race-cr01.md` — fichero MOVIDO (no nuevo) con sección Outcome ampliada.
- `.planning/STATE.md` — fila §Deferred Items editada (cierre).

Sin exports, sin CLI flags, sin ficheros de producción, sin deps.
</artifacts_produced>

<verification>
- `git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs` → limpio (D-07).
- Loop ≥50× de `node --test test/gsd-lock-race.test.js` → 0 fallos (documentado).
- `npm test` → suite completa verde (documentado).
- `.planning/debug/resolved/gsd-lock-race-cr01.md` existe; el original ya no.
- La fila STATE.md §Deferred Items de `stealLock` figura cerrada; resto intacto.
</verification>

<success_criteria>
- LOCK-02: el harness CR-01 pasa verde ≥50× bajo carga con 0 fallos + suite completa verde, con el assert intacto y el harness byte-idéntico.
- LOCK-03: R-81-01 y la debug session `gsd-lock-race-cr01` cerradas formalmente (fichero en `resolved/` con Outcome; fila STATE.md marcada cerrada) — sin doc-drift.
</success_criteria>

<output>
Create `.planning/phases/82-fix-de-la-carrera-de-steallock/82-02-SUMMARY.md` when done
</output>
