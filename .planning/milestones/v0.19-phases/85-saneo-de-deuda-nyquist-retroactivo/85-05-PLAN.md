---
phase: 85-saneo-de-deuda-nyquist-retroactivo
plan: 05
type: execute
wave: 2
depends_on: ["85-01", "85-02", "85-03", "85-04"]
files_modified:
  - .planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEBT-05, DEBT-06, DEBT-07, NYQ-01, NYQ-02]

must_haves:
  truths:
    - "Las 4 filas de `STATE.md` §Deferred Items que esta fase cierra quedan marcadas como cerradas con su resolución escrita y la ref del requisito que las salda: 81-REVIEW WR-01/WR-02 → DEBT-05/06 (salda R-81-02) · los 3 warnings de 80-REVIEW → DEBT-07 · Nyquist draft de 79/80/81 → NYQ-01 · Nyquist draft de 69/71/72 → NYQ-02."
    - "La fila de `STATE.md` del **format-isolation transitivo** sigue ABIERTA. Dice literalmente «candidato natural de la Phase 85» y un agente diligente la cerraría; D-18 la deja fuera del boundary y el registro documenta que se evaluó y se difirió, con su trigger (medir primero el radio de ficheros del dashboard que se pondrían rojos)."
    - "`deferred-items.md` de la fase registra cada ítem no cerrado con su razón **y su trigger real**; un ítem sin trigger es una intención, no un diferido, así que la columna nunca queda vacía."
    - "OQ-1 queda registrado: el mismo comentario de premisa falsa que DEBT-07/WR-03 corrige en `test/check-isolation.test.js:14,33` vive verbatim en `test/format-isolation.test.js:14,33`, y **no** se corrige aquí (D-18). Se registra JUNTO al ítem de format-isolation transitivo para que ambos se corrijan a la vez: arreglar solo el comentario dejaría un guard que sigue sin cubrir el caso que su comentario ya no niega."
    - "Toda mutación de `STATE.md` pasa por `gsd-tools query state.*`; ninguna por `Write` o `Edit` directo. `STATE.md` es un artefacto gestionado y la edición directa lo desincroniza."
    - "Los 5 requisitos de la fase (DEBT-05, DEBT-06, DEBT-07, NYQ-01, NYQ-02) quedan marcados como completos en `REQUIREMENTS.md` vía `gsd-tools query requirements.mark-complete`, no por edición manual del checkbox."
  artifacts:
    - "`.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md` — **el único fichero nuevo de toda la fase**"
    - "`.planning/STATE.md` — 4 filas de §Deferred Items cerradas con su resolución"
    - "`.planning/REQUIREMENTS.md` — los 5 checkboxes de DEBT-05/06/07 y NYQ-01/02 marcados"
  key_links:
    - "`deferred-items.md` de la fase → `STATE.md` §Deferred Items: la fila abierta de format-isolation apunta al registro de la fase, y viceversa; sin ese enlace el próximo audit la lee como olvido"
    - "`gsd-tools query state.*` → `STATE.md`: el único writer admitido del artefacto gestionado"
    - "`85-01`/`85-02`/`85-03`/`85-04` → este plan: no se cierra ninguna fila cuyo trabajo no esté hecho y verificado"
  prohibitions:
    - "NO se cierra la fila de `STATE.md` del format-isolation transitivo. Su texto invita a hacerlo («candidato natural de la Phase 85») y D-18 la deja explícitamente fuera del boundary: la razón declarada es que no se ha medido el radio de ficheros del dashboard que se pondrían rojos, y una fase declarada mecánica no es el sitio para descubrirlo."
    - "NO se editan `.planning/STATE.md` ni `.planning/REQUIREMENTS.md` con `Write` o `Edit` directo. Son artefactos gestionados; la mutación va por `gsd-tools query`."
    - "NO se cierra ninguna fila cuyo trabajo no esté verificado en el SUMMARY del plan correspondiente. Una fila cerrada sin artefacto que la respalde es exactamente el mismo fallo que un `nyquist_compliant: true` sin cita."
    - "NO se reescriben los `MILESTONE-AUDIT.md` archivados de v0.16 ni de v0.18 (D-15): son snapshots de lo que era cierto al cerrar su milestone. El cierre se registra en el `VALIDATION.md` de cada fase y en la fila de `STATE.md`, no reescribiendo la historia."
---

<objective>
Cerrar la contabilidad de la fase: registrar lo que **no** se cierra (con su razón y su trigger) y marcar como cerrado lo que **sí**.

Purpose: el único riesgo residual de esta fase es documental — una fila de `STATE.md` sin cerrar, o una cerrada sin que su artefacto lo respalde. Ambos extremos envenenan el siguiente audit. Este plan es la pasada que los evita, y es también donde se registra la deuda adyacente que la fase decidió no tocar para que no se lea como olvido.

Output: el `deferred-items.md` de la fase (el **único** fichero nuevo de todo el barrido), 4 filas de `STATE.md` §Deferred Items cerradas y los 5 checkboxes de `REQUIREMENTS.md` marcados.

Decisiones que este plan implementa y registra: **D-02** (los hallazgos del grep de auditoría fuera de `state.js` que no fueran one-liner, si los hubo), **D-10** (`IN-01` de 80-REVIEW no entra: es *info*, no warning), **D-15** (los `MILESTONE-AUDIT.md` archivados no se reescriben; el cierre se registra aquí y en cada `VALIDATION.md`), **D-18** (`format-isolation` transitivo fuera, y su fila de `STATE.md` **sigue abierta**), **D-19** (`src/gsd/lock.js` / R-82-01 fuera; el formato de línea del inbox y `src/server.js` tampoco se abren), **D-20** (`.planning/codebase/TESTING.md` no se refresca aquí).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-CONTEXT.md
</context>

## Artifacts this phase produces

| Artefacto | Naturaleza |
|---|---|
| `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md` | **El único fichero nuevo de toda la Phase 85** |
| 4 filas cerradas en `.planning/STATE.md` §Deferred Items | mutación vía `gsd-tools`, no edición directa |
| 5 checkboxes marcados en `.planning/REQUIREMENTS.md` | mutación vía `gsd-tools requirements mark-complete` |

**Cero código, cero tests, cero deps.**

<tasks>

<task type="auto">
  <name>Task 1 (85-05-01): `deferred-items.md` de la fase — lo que NO se cierra, con su trigger</name>
  <files>.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md</files>
  <read_first>
    - `.planning/phases/84-superficies-de-captura-skill-sync-conteo-ambient/deferred-items.md` — **el molde exacto**: cabecera, tabla de 4 columnas (`Ítem | Qué se difiere | Por qué no aquí | Trigger`), sección de nota de riesgo cuando hace falta, y sección final «Ajenos por construcción»
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-CONTEXT.md` `&lt;deferred&gt;` — la lista canónica de ítems diferidos con sus triggers ya redactados
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-RESEARCH.md` §Open Questions OQ-1 — el comentario de premisa falsa duplicado en `test/format-isolation.test.js:14,33`, con la recomendación conservadora ya resuelta
    - Los SUMMARY de `85-01`, `85-02`, `85-03` y `85-04` — para recoger cualquier hallazgo que hayan producido (en particular el resultado del grep de auditoría D-02 del plan 85-01)
  </read_first>
  <action>
Crear `deferred-items.md` en el directorio de la fase, con el molde de `84/deferred-items.md`: cabecera de dos frases que declare que un ítem sin trigger es una intención y no un diferido, y tabla de 4 columnas `Ítem | Qué se difiere | Por qué no aquí | Trigger`.

Filas obligatorias:

1. **`format-isolation` transitivo (D-18)** — endurecer `test/format-isolation.test.js` para que el guard de color-isolation siga imports transitivos y no solo directos. Razón: no es ninguno de los 5 requisitos y el boundary del roadmap es fijo; además **no se ha medido** el radio de ficheros del dashboard que se pondrían rojos. Trigger: medir ese radio primero, fuera de una fase declarada mecánica. Anotar además que el plan `85-02` produjo un patrón de source-grep reutilizable (`stripComments` + regex constante sobre la salida del walker) y que **deliberadamente no se aplicó aquí**: que compartan patrón no los hace el mismo trabajo.

2. **OQ-1 — comentario de premisa falsa duplicado** — la misma frase que `85-02` retiró de `test/check-isolation.test.js:14,33` vive verbatim en `test/format-isolation.test.js:14,33`. Razón de no tocarlo: D-18 declara ese fichero no modificado, y corregir solo el comentario dejaría un guard que sigue sin cubrir el caso que su comentario ya no niega — peor que la situación actual. Trigger: **el mismo** que la fila 1; se corrigen juntos. Dejar el enlace entre ambas filas explícito.

3. **`IN-01` de 80-REVIEW (D-10)** — doble `scan` por pase; el conteo de advisories proviene de otro snapshot que las acciones ejecutadas. Razón: clasificado *info* en el propio review, y el criterio literal de DEBT-07 dice «los 3 warnings». Trigger: que el conteo de advisories se contradiga con lo aplicado en un caso real.

4. **Refresco de `.planning/codebase/TESTING.md` (D-20)** — desfasado desde 2026-04-07: describe 2 ficheros de test frente a 110 reales y 2586 tests. Vale para el framework, no como inventario. Trigger: el próximo `/gsd-map-codebase` o `/gsd-docs-update`, o la apertura del siguiente milestone.

5. **Hallazgos del grep de auditoría D-02 que no fueran one-liner**, con su path — si el SUMMARY de `85-01` no registró ninguno, escribir la fila declarando que la auditoría **se cerró vacía**, citando el alcance exacto del grep. Es una afirmación citable, no una omisión.

Sección final **«Ajenos por construcción — registrados en sus fases, no re-listados aquí»**, con al menos: **R-82-01** (carrera de 2.º orden de `stealLock` con holder VIVO — esta fase no abre `src/gsd/lock.js`, D-19), el formato de línea del inbox (congelado en 83) y `src/server.js` (D-19), y **D-08 / D-08b** de Phase 84 (rename de `skill.md` y auto-sync multi-skill del orquestador), que siguen con sus triggers en `84/deferred-items.md`.

Prosa en español, sin emoji.
  </action>
  <verify>
    <automated>P=.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md; test -f "$P" && grep -c 'Trigger' "$P" | grep -qvx 0 && grep -c 'format-isolation' "$P" | grep -qvx 0 && grep -c 'IN-01' "$P" | grep -qvx 0 && grep -c 'TESTING.md' "$P" | grep -qvx 0 && grep -c 'R-82-01' "$P" | grep -qvx 0 && grep -c 'D-02' "$P" | grep -qvx 0 && echo "deferred-items OK"</automated>
  </verify>
  <acceptance_criteria>
    - El fichero existe en `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md`
    - La tabla tiene las 4 columnas del molde de 84 y **5 filas** como mínimo: format-isolation transitivo, OQ-1, IN-01, TESTING.md, hallazgos del grep D-02
    - **Ninguna celda de la columna `Trigger` está vacía**: el número de filas de la tabla coincide con el número de triggers escritos
    - `grep -c 'format-isolation.test.js:14' <path>` devuelve `>= 1` — OQ-1 registra las líneas exactas
    - La fila de OQ-1 declara explícitamente que su trigger es el mismo que el de format-isolation transitivo y que se corrigen juntos
    - La sección «Ajenos por construcción» nombra `R-82-01`, `src/gsd/lock.js`, `src/server.js` y los diferidos `D-08` / `D-08b` de Phase 84
    - La fila del grep D-02 refleja el resultado real registrado en el SUMMARY de `85-01` (hallazgos con su path, o cierre vacío con el alcance citado)
  </acceptance_criteria>
  <done>La deuda adyacente que la Phase 85 decidió no tocar queda registrada con razón y trigger, de modo que el próximo audit la lea como decisión y no como olvido. En particular, el par format-isolation transitivo + OQ-1 queda enlazado para corregirse a la vez.</done>
</task>

<task type="auto">
  <name>Task 2 (85-05-02): cerrar las 4 filas de `STATE.md` y marcar los 5 requisitos</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/STATE.md` §Deferred Items — las filas exactas a cerrar y, crucialmente, la que **NO** se cierra (la de «Higiene de tests», que incluye el format-isolation transitivo con el texto «candidato natural de la Phase 85»)
    - `.planning/REQUIREMENTS.md` §Saneo de deuda (DEBT) y §Nyquist retroactivo (NYQ) — los 5 checkboxes
    - Los SUMMARY de `85-01`, `85-02`, `85-03` y `85-04` — la evidencia que respalda cada cierre
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md` — el resultado de la Task 1, al que la fila abierta debe poder apuntar
  </read_first>
  <action>
Cerrar en `.planning/STATE.md` §Deferred Items **exactamente cuatro** filas, cada una con su resolución escrita y la ref del requisito que la salda:

1. La fila de **Doc/consistencia** (81-REVIEW WR-01 typedef stale · WR-02 `deriveAnyNext` sin colapso) → cerrada por **DEBT-05/06**, con la nota de que salda **R-81-02**, citando el plan `85-01`.
2. La fila de **Observabilidad** (3 warnings de 80-REVIEW) → cerrada por **DEBT-07**, indicando que los tres se **RESOLVIERON** — ninguno re-aceptado — y que `IN-01` queda diferido por ser *info* (D-10), con puntero al `deferred-items.md` de la fase.
3. La fila de **Nyquist** de 79/80/81 → cerrada por **NYQ-01**, citando el plan `85-03`.
4. La fila de **Nyquist** de 69/71/72 → cerrada por **NYQ-02**, citando el plan `85-04`.

**La mutación va por `gsd-tools query state.*`**, nunca por `Write` ni `Edit` directo sobre `STATE.md`: es un artefacto gestionado y la edición directa lo desincroniza. Los subcomandos disponibles incluyen `update` y `patch`; invocar `gsd-tools query state` sin argumentos lista la superficie exacta, y cada subcomando sin argumentos declara lo que requiere.

**La fila de «Higiene de tests» NO se cierra.** Contiene el format-isolation transitivo con el texto «candidato natural de la Phase 85», que invita a cerrarla; D-18 la deja explícitamente fuera del boundary. Actualizarla, si acaso, **solo** para apuntar al `deferred-items.md` de esta fase — sigue abierta, con su trigger intacto. La fila de «Evidencia en vivo» (round-trip real de `--fix` de 79 y convergencia contra cmux vivo de 80) **tampoco** se cierra: los planes `85-03` y `85-04` la contabilizaron como manual-only, que no es lo mismo que resolverla.

Después, marcar los 5 requisitos como completos con `gsd-tools query requirements.mark-complete DEBT-05,DEBT-06,DEBT-07,NYQ-01,NYQ-02` (o la forma exacta que el comando declare al invocarse sin argumentos). Sin edición manual de los checkboxes de `REQUIREMENTS.md`.

Ejecutar `gsd-tools query state.validate` al terminar para comprobar que el artefacto queda consistente.
  </action>
  <verify>
    <automated>grep -c 'DEBT-05' .planning/STATE.md | grep -qvx 0 && grep -c 'NYQ-01' .planning/STATE.md | grep -qvx 0 && grep -c 'NYQ-02' .planning/STATE.md | grep -qvx 0 && grep -c '\[x\] \*\*DEBT-05\*\*' .planning/REQUIREMENTS.md | grep -qx 1 && grep -c '\[x\] \*\*NYQ-02\*\*' .planning/REQUIREMENTS.md | grep -qx 1 && grep -c 'format-isolation transitivo' .planning/STATE.md | grep -qvx 0 && echo "cierre OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*DEBT-05\*\*' .planning/REQUIREMENTS.md`, `...DEBT-06...`, `...DEBT-07...`, `...NYQ-01...` y `...NYQ-02...` devuelven `1` cada uno
    - `grep -c '\[ \] \*\*DEBT-0' .planning/REQUIREMENTS.md` devuelve `0` y `grep -c '\[ \] \*\*NYQ-0' .planning/REQUIREMENTS.md` devuelve `0`
    - `STATE.md` §Deferred Items marca como cerradas las 4 filas, cada una nombrando el requisito que la salda (`DEBT-05`/`DEBT-06`, `DEBT-07`, `NYQ-01`, `NYQ-02`) y el plan que lo hizo
    - La fila de DEBT-07 declara que **los 3 warnings se resolvieron** y que `IN-01` queda diferido por ser *info*
    - **La fila del format-isolation transitivo sigue presente y ABIERTA**: `grep -c 'format-isolation transitivo' .planning/STATE.md` devuelve `>= 1` y su celda de estado no dice cerrada/resuelta
    - **La fila de «Evidencia en vivo» sigue abierta**: `grep -c 'Evidencia en vivo' .planning/STATE.md` devuelve `>= 1`
    - `gsd-tools query state.validate` sale sin errores
    - `git log -1 --stat` muestra que `STATE.md` y `REQUIREMENTS.md` cambiaron en un commit `docs(85): ...`, y el historial de la sesión no contiene ninguna llamada a `Write` o `Edit` sobre esos dos ficheros
  </acceptance_criteria>
  <done>Las 4 filas que esta fase salda están cerradas con su resolución y su ref de requisito; las 2 que no salda siguen abiertas con su trigger; los 5 requisitos están marcados. Todo por `gsd-tools`, sin escritura directa sobre artefactos gestionados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(sin frontera de red ni de proceso)* | El plan escribe un Markdown nuevo y muta dos artefactos gestionados del propio repo vía `gsd-tools`. Cero código, cero endpoints, cero instalaciones. |
| Agente de cierre → artefactos gestionados (`STATE.md`, `REQUIREMENTS.md`) | El vector real es de **integridad de bookkeeping**: cerrar de más (una fila fuera del boundary) o de menos (una fila saldada que queda abierta). Ambos envenenan el siguiente audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-05-01 | Repudiation | Fila de `STATE.md` cerrada sin artefacto que la respalde | high | mitigate | Prohibición explícita + `depends_on` sobre los cuatro planes de wave 1: no se cierra ninguna fila cuyo trabajo no esté verificado en su SUMMARY. Cada cierre nombra el plan que lo hizo. |
| T-85-05-02 | Tampering | Fila del format-isolation transitivo (su texto invita a cerrarla) | medium | mitigate | Prohibición explícita + criterio de aceptación: `grep -c 'format-isolation transitivo' .planning/STATE.md` ≥ 1 y su celda de estado no dice cerrada. Registro cruzado en el `deferred-items.md` de la fase con su trigger. |
| T-85-05-03 | Tampering | `STATE.md` / `REQUIREMENTS.md` editados fuera del writer gestionado | medium | mitigate | Prohibición explícita + criterio de aceptación sobre el historial: la mutación va por `gsd-tools query state.*` y `requirements.mark-complete`, y `state.validate` cierra la tarea. La edición directa desincroniza el artefacto. |
| T-85-05-04 | Repudiation | `deferred-items.md` con ítems sin trigger | low | mitigate | Criterio de aceptación: ninguna celda de la columna `Trigger` vacía. Un ítem sin trigger es una intención, no un diferido — convención heredada del molde de `84/deferred-items.md`. |
| T-85-05-SC | Tampering | Supply chain (npm) | low | accept | **Cero instalaciones.** Constraint LOCKED «cero deps npm nuevas»; `85-RESEARCH.md` §Package Legitimacy Audit devuelve tabla vacía. No procede checkpoint de legitimidad. |
</threat_model>

<verification>
- `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/deferred-items.md` existe, con 5 filas mínimo y ninguna sin trigger
- `STATE.md` §Deferred Items: 4 filas cerradas con resolución y ref de requisito; la de format-isolation transitivo y la de «Evidencia en vivo» siguen abiertas
- `REQUIREMENTS.md`: los 5 checkboxes de DEBT-05/06/07 y NYQ-01/02 marcados
- `gsd-tools query state.validate` sin errores
- `npm test` — la suite sigue verde (este plan no toca código, pero se comprueba antes del cierre de fase)
</verification>

<success_criteria>
1. La deuda que la fase decide no tocar está registrada con razón y trigger, enlazando OQ-1 con el format-isolation transitivo para que se corrijan juntos.
2. Las 4 filas de `STATE.md` que la fase salda están cerradas, cada una con la ref del requisito y del plan que lo hizo.
3. La fila del format-isolation transitivo y la de «Evidencia en vivo» siguen abiertas — cerrarlas habría sido falsear el registro.
4. Los 5 requisitos de la fase están marcados como completos vía `gsd-tools`.
5. Ningún artefacto gestionado se editó con `Write` o `Edit` directo.
</success_criteria>

<output>
Create `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-05-SUMMARY.md` when done.
</output>
