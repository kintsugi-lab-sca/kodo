---
phase: 85-saneo-de-deuda-nyquist-retroactivo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/state.js
  - src/cli/dashboard/select.js
  - test/dashboard-select.test.js
autonomous: true
requirements: [DEBT-05, DEBT-06]

must_haves:
  truths:
    - "El comentario del typedef `TaskHandoff` enuncia los tres estados por PRESENCIA del campo `next` — string no vacío SOBRESCRIBE, `null` explícito BORRA, campo AUSENTE PRESERVA — y cada uno es trazable a su test existente: `test/state/handoff-state.test.js:307` (OVERWRITE), `:265` (CLEAR), `:288` (PRESERVE)."
    - "DEBT-05 es doc-only: no introduce ninguna regla nueva de longitud, igualdad ni normalización del `next`; el truncado a 200 de Phase 74 queda intacto y no se re-enuncia en el typedef."
    - "El grep de auditoría de D-02 (`ausente/null` · `NO borra` · `no borra el previo`) sobre `src/`, `README.md`, `.planning/codebase/` y `.claude/skills/` devuelve CERO hits stale tras el saneo, y su resultado queda escrito en el SUMMARY con los patrones y el alcance exactos."
    - "Un `next` de solo whitespace (`'   '`, `'\\n\\t'`, `' \\r\\n '`) hace que `deriveAnyNext` devuelva `false` y la columna `next` del dashboard se omita por completo — cabecera incluida, sin placeholder ni celda vacía. El test que lo asevera se escribe RED ANTES del fix, en el bloque LIVE-05 de `test/dashboard-select.test.js:471` (D-06)."
    - "Los 8 asserts LIVE-05 existentes (`test/dashboard-select.test.js:473-497`) siguen verdes SIN tocarlos; si alguno necesita cambiar, la delegación está mal hecha (D-06)."
    - "El contrato de overflow/texto largo de la celda `next` no se modifica: `nextCell` (`src/cli/dashboard/format.js:264`) sigue colapsando `/\\s+/g` + `trim` (DEBT-03) y `SessionTable` sigue truncando a `COLS.next = 40` con `truncate: true`. Esta fase no ensancha ni estrecha ese contrato."
    - "`deriveAnyNext` conserva su contrato de D-05: devuelve un `boolean` estructural, se computa sobre el set SIN filtrar (`enriched`, no `filtered`) y es never-throws para no-string. `src/cli/dashboard/App.js:820` no se toca."
    - "`src/cli/dashboard/select.js` gana su primer import de runtime (`./format.js`, cuyo único import es `node:path`) sin introducir `picocolors` bajo `src/cli/dashboard/**`: el guard §TUI-04 de `test/format-isolation.test.js` sigue verde y NO se modifica (D-18)."
  artifacts:
    - "`src/session/state.js` — comentario del campo `next` del typedef `TaskHandoff` reescrito al contrato tres-estados por PRESENCIA"
    - "`src/cli/dashboard/select.js` — `import { nextCell } from './format.js';` + `deriveAnyNext` delegando + docblock actualizado"
    - "`test/dashboard-select.test.js` — un `it()` nuevo dentro del `describe` LIVE-05 con los 3 casos de solo-whitespace y el caso mixto"
  key_links:
    - "`deriveAnyNext` (`select.js`) → `nextCell` (`format.js`): única fuente de verdad del colapso de whitespace; la incoherencia de 81-REVIEW WR-02 deja de ser posible por construcción"
    - "typedef `TaskHandoff` (`state.js:~53`) ↔ merge tres-estados implementado (`state.js:449-454`): la descripción vuelve a coincidir con la implementación"
    - "`src/cli/dashboard/App.js:820` → `deriveAnyNext(enriched)`: llamada intacta, set SIN filtrar preservado (Pitfall 4 de Phase 75)"
  prohibitions:
    - "Ningún assert existente se debilita, se estrecha ni se reescribe para que la suite pase en verde. Los 8 casos LIVE-05 de `test/dashboard-select.test.js:473-497` son invariantes bajo la delegación (verificado en RESEARCH §2d); si alguno se pone rojo, el fix está mal y se corrige el fix, nunca el assert."
    - "No se reescribe prosa de artefactos archivados que son snapshots históricos. La auditoría de D-02 se limita a `src/` y a la documentación VIVA; las fases archivadas siguen describiendo lo que era cierto cuando se escribieron."
---

<objective>
Saldar las dos filas de deuda documental y de coherencia que 81-REVIEW dejó abiertas: **DEBT-05** (el typedef `TaskHandoff` describe la semántica PRE-DEBT-01 y por tanto miente al siguiente implementador) y **DEBT-06** (`deriveAnyNext` decide la presencia de la columna `next` con una regla distinta a la que la pinta, de modo que un `next` de solo-whitespace enciende una columna que se renderiza vacía).

Purpose: la deuda de DEBT-05 no es «un comentario desfasado», es una trampa activa — quien lo lea hoy escribirá código correcto para el contrato de Phase 74 e incorrecto para el vigente, y el typedef le habrá dado permiso. DEBT-06 elimina la divergencia entre los dos lectores del mismo dato haciendo que el que decide **consuma** al que pinta (D-04), en vez de duplicar su regla.

Output: `src/session/state.js` con el typedef saneado (doc-only, cero cambio de comportamiento, D-01), `test/dashboard-select.test.js` con el caso RED de D-06, y `src/cli/dashboard/select.js` delegando en `nextCell` (D-04).

Decisiones que este plan implementa: **D-01** (saneo doc-only acotado a `state.js`), **D-02** (grep de auditoría documentado en el SUMMARY), **D-03** (cero tests nuevos para DEBT-05 — la evidencia es la cita), **D-04** (delegación en `nextCell`), **D-05** (contrato de `deriveAnyNext` intacto), **D-06** (test RED antes del fix). Fence respetado: **D-18** — `test/format-isolation.test.js` **no** se modifica.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-CONTEXT.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-UI-SPEC.md
</context>

## Supuestos declarados del planner

- **A1 — DEBT-06 quedó `unclassified` en el edge probe determinista.** No es un vacío de cobertura: el requisito es un fix de **coherencia entre dos lectores del mismo dato**, y su cobertura real la fija D-06 — el test RED escrito ANTES del fix (3 casos de solo-whitespace + 1 caso mixto) más los 8 asserts LIVE-05 existentes intactos como red de no-regresión. Se registra aquí como nudge de revisión manual, no como gap resuelto automáticamente.

## Artifacts this phase produces

Este plan **no crea ningún fichero nuevo**. Todo es modificación in-place. Los símbolos y bloques que genuinamente añade:

| Artefacto | Fichero | Naturaleza |
|---|---|---|
| `import { nextCell } from './format.js';` | `src/cli/dashboard/select.js` | **primer import de runtime** del módulo (hoy tiene cero) |
| 1 `it()` nuevo dentro del `describe` LIVE-05 | `test/dashboard-select.test.js` | 4 asserts (3 whitespace-only → `false`, 1 mixto → `true`) |
| Comentario reescrito del campo `next` del typedef `TaskHandoff` | `src/session/state.js` | prosa JSDoc — **cero símbolos nuevos, cero cambio de comportamiento** |
| Predicado reescrito de `deriveAnyNext` + su docblock | `src/cli/dashboard/select.js` | misma firma, misma export, mismo contrato de retorno |

**Cero funciones nuevas, cero ficheros nuevos, cero exports nuevos, cero deps.**

<tasks>

<task type="auto">
  <name>Task 1 (85-01-01): DEBT-05 — el typedef `TaskHandoff` enuncia el contrato tres-estados por PRESENCIA</name>
  <files>src/session/state.js</files>
  <read_first>
    - `src/session/state.js:44-64` — el bloque de typedefs; el campo a corregir es el comentario de `next` dentro de `TaskHandoff` (hoy una sola línea larga)
    - `src/session/state.js:405-410` — la tabla ya CORRECTA del contrato (`non-empty string → OVERWRITE` / `null` (explicit) → CLEAR / `field ABSENT/undefined → PRESERVE`). **El texto nuevo debe ser coherente con esta tabla, no inventar formulación propia**
    - `src/session/state.js:425-430` — el `@param entry` y el `@returns` de `upsertTaskHandoff`: RESEARCH §4b los verificó YA CORRECTOS. **No se tocan**
    - `src/session/state.js:449-454` — el merge tres-estados implementado, con su comentario `DEBT-01 three-state merge, discriminated by PRESENCE`. Es la fuente de verdad que el typedef debe describir
    - `test/state/handoff-state.test.js:265,288,307` — CLEAR / PRESERVE / OVERWRITE: la evidencia citable de DEBT-05 (D-03)
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §No Analog Found
  </read_first>
  <action>
Reescribir **solo** el comentario del campo `next` del typedef `TaskHandoff` en `src/session/state.js` (D-01, doc-only). Cero cambios de código, cero cambios de comportamiento, cero tests nuevos (D-03).

El texto nuevo debe enunciar el contrato tres-estados **discriminado por PRESENCIA del campo**, usando los mismos tres verbos que la tabla de `:405-410` — OVERWRITE, CLEAR, PRESERVE — y referenciar esa tabla como fuente de verdad. Las tres transiciones a enunciar: un `next` string no vacío SOBRESCRIBE el previo · un `next` con valor `null` explícito BORRA el previo · el campo AUSENTE del entry PRESERVA el previo.

Retirar de esa línea, en bloque, la advertencia entera que hoy la ocupa: describía el contrato de Phase 74/75 (semántica pre-DEBT-01) y quedó falsa cuando Phase 81 cambió el merge. Incluye retirar la etiqueta de warning de `81-REVIEW` con la que ese aviso se abre — ese warning ya no es vigente, es justo el que esta fase cierra.

**Conservar** la última frase de la línea, que sigue siendo cierta y es información útil: `null` en el estado persistido significa «ninguna sesión de esta tarea ha dejado nunca un `NEXT:`», no «el último cierre no lo traía».

**No re-enunciar** ninguna regla de longitud, igualdad ni normalización: el truncado a 200 de Phase 74 (D-02 de aquella fase) queda intacto y **fuera** del texto nuevo. DEBT-05 no introduce contrato nuevo, solo describe el vigente.

Añadir al final del comentario una referencia cruzada a los tres tests que congelan el contrato: `test/state/handoff-state.test.js:265` (CLEAR), `:288` (PRESERVE), `:307` (OVERWRITE) — así el siguiente lector tiene la evidencia a un salto de distancia (D-03).

Después, ejecutar el grep de auditoría de **D-02** sobre `src/`, `README.md`, `.planning/codebase/` y `.claude/skills/` con los patrones de la advertencia retirada, y **escribir su resultado en el SUMMARY** (patrones exactos, alcance exacto, hits). RESEARCH §4a lo ejecutó y encontró un único hit stale en todo el repo — el que esta tarea corrige. Si aparece algún hit adicional: se corrige **solo si es one-liner**; si exige contexto, se registra con su path para el `deferred-items.md` de la fase (lo consolida el plan 85-05). Si sale limpio, escribirlo así en el SUMMARY: la fila «Hallazgos fuera de `state.js`» del `&lt;deferred&gt;` del CONTEXT **se cierra vacía**, y eso es una afirmación citable, no una omisión.
  </action>
  <verify>
    <automated>node --test test/state/handoff-state.test.js && grep -c 'ausente/null' src/session/state.js | grep -qx 0 && grep -c 'NO borra el previo' src/session/state.js | grep -qx 0 && sed -n '44,64p' src/session/state.js | grep -q 'PRESENCIA' && echo "DEBT-05 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/state/handoff-state.test.js` pasa sin que se haya tocado ni un byte de ese fichero (`git diff --exit-code test/state/handoff-state.test.js` sale 0) — el cambio es doc-only (D-03)
    - `git diff --stat src/session/state.js` muestra un cambio acotado al bloque de typedefs (líneas ~44-64); `git diff src/session/state.js` no contiene ninguna línea de código ejecutable
    - `grep -c 'ausente/null' src/session/state.js` devuelve `0` (gate deliberadamente sobre el fichero sin filtrar: el objetivo ES un comentario)
    - `grep -c 'NO borra el previo' src/session/state.js` devuelve `0`
    - `sed -n '44,64p' src/session/state.js | grep -c 'WR-02'` devuelve `0` — la etiqueta del warning retirado ya no aparece en la región del typedef
    - `sed -n '44,64p' src/session/state.js | grep -c 'PRESENCIA'` devuelve `>= 1`
    - `sed -n '44,64p' src/session/state.js` contiene los tres verbos del contrato (OVERWRITE, CLEAR, PRESERVE) y la ref `handoff-state.test.js`
    - `git diff --exit-code src/session/state.js -- ` restringido a las líneas 400-460 está vacío: `:405-410`, `:425-430` y `:449-454` **no** se han tocado
    - El SUMMARY contiene una sección con el grep de auditoría D-02: patrones usados, alcance (`src/`, `README.md`, `.planning/codebase/`, `.claude/skills/`), número de hits y clasificación de cada uno
  </acceptance_criteria>
  <done>El typedef `TaskHandoff` describe el contrato tres-estados por presencia del campo, coherente con la tabla de `:405-410` y con el merge de `:449-454`, con las tres transiciones trazables a `test/state/handoff-state.test.js:265/288/307`. La suite de handoff sigue verde sin haber tocado ningún test. El grep D-02 está documentado en el SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (85-01-02): DEBT-06 RED — el caso de solo-whitespace ANTES del fix</name>
  <files>test/dashboard-select.test.js</files>
  <read_first>
    - `test/dashboard-select.test.js:460-500` — el `describe` LIVE-05 completo y sus 5 `it()` existentes (8 asserts). **Se lee para imitar el estilo, NO para modificarlo**
    - `src/cli/dashboard/select.js:245-262` — `deriveAnyNext` a HEAD y su docblock (contrato de D-05: flag estructural, set SIN filtrar, never-throws)
    - `src/cli/dashboard/format.js:250-268` — `nextCell` y su docblock: el colapso `/\s+/g` + `trim`, la semántica que el test congela
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §4 — el estilo de assert del bloque (one-liner `assert.equal(deriveAnyNext(<literal inline>), <bool>)`, sin fixture externo)
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-UI-SPEC.md` §S-1 — la tabla de contrato de presencia
  </read_first>
  <behavior>
    - `deriveAnyNext([{ next: '   ' }])` → `false` (hoy `true` — RED)
    - `deriveAnyNext([{ next: '\n\t' }])` → `false` (hoy `true` — RED)
    - `deriveAnyNext([{ next: ' \r\n ' }])` → `false` (hoy `true` — RED)
    - `deriveAnyNext([{ next: '   ' }, { next: 'algo' }])` → `true` (una fila con contenido real sigue encendiendo la columna aunque otra sea whitespace)
  </behavior>
  <action>
Añadir **un** `it()` nuevo **dentro** del `describe` LIVE-05 existente de `test/dashboard-select.test.js` (el que arranca en `:471`), después del último `it()` del bloque. Los 5 `it()` existentes y sus 8 asserts **no se tocan ni se reformatean** — son la red de no-regresión de D-06.

Naming coherente con el bloque, que usa `'Test N: …'` o `'<REF>: <descripción>'`: usar la forma con ref, `'DEBT-06: un next de solo-whitespace NO enciende la columna (coherente con nextCell)'`.

Cuerpo: los 4 asserts del bloque `&lt;behavior&gt;` de arriba, en ese orden, con el estilo one-liner del bloque (`assert.equal(deriveAnyNext(<literal inline>), <bool>)`, literales inline, sin fixture externo). Un comentario de una línea que declare la naturaleza RED: hoy la longitud del string crudo es mayor que cero mientras `nextCell` devuelve cadena vacía — ésa es exactamente la incoherencia que denuncia el segundo warning de `81-REVIEW`.

El tercer caso (`' \r\n '`) es un extra recomendado por RESEARCH §2e: `nextCell` lo colapsa vía `/\s+/g` y refuerza la coherencia con `stripControlChars` (`src/cli/format.js:73`, que elimina `\r`). El cuarto caso (mixto) es el que impide que el fix se «pase de frenada» y apague la columna cuando sí hay contenido real.

**Esta tarea se commitea con el test EN ROJO.** Es el punto entero de D-06: el test dicta la forma del arreglo, no al revés. No tocar `src/cli/dashboard/select.js` en esta tarea.
  </action>
  <verify>
    <automated>if node --test test/dashboard-select.test.js; then echo "FALLO: el caso DEBT-06 NO esta RED — revisa que el test asevere el comportamiento nuevo"; exit 1; else echo "RED confirmado (DEBT-06)"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-select.test.js` termina con **exit code distinto de 0** y su salida nombra el `it()` `DEBT-06` entre los fallidos
    - `git diff test/dashboard-select.test.js` añade exactamente un `it()` y **cero líneas eliminadas o modificadas** en el rango `:460-500` preexistente
    - `grep -c "DEBT-06" test/dashboard-select.test.js` devuelve `>= 1`
    - El `it()` nuevo contiene los 4 asserts: 3 con resultado esperado `false` (`'   '`, `'\n\t'`, `' \r\n '`) y 1 con resultado esperado `true` (caso mixto whitespace + contenido)
    - `git diff --exit-code src/cli/dashboard/select.js` sale 0 — el fix todavía no existe
  </acceptance_criteria>
  <done>El bloque LIVE-05 tiene un `it()` que falla hoy con los 3 casos de solo-whitespace, más el caso mixto que impide un fix demasiado agresivo. Los 8 asserts previos están intactos. El commit registra el estado RED.</done>
</task>

<task type="auto">
  <name>Task 3 (85-01-03): DEBT-06 GREEN — `deriveAnyNext` delega en `nextCell`</name>
  <files>src/cli/dashboard/select.js</files>
  <read_first>
    - `src/cli/dashboard/select.js:20-35` — la cabecera de comentarios, incluido el bloque de **color-isolation** (invariante D-12 de Phase 34) y el `@typedef {import('./format.js').EnrichedSession}`. El import nuevo va **después** del bloque de comentarios y **antes** del `@typedef`, que ya referencia ese mismo módulo
    - `src/cli/dashboard/select.js:245-262` — `deriveAnyNext`, su docblock de 12 líneas y el hermano `deriveAnyProgress` (`:241-243`) como molde de one-liner
    - `src/cli/dashboard/format.js:20-30` — el comentario de color-isolation y `:25`, el ÚNICO import del módulo (`node:path`): es la razón por la que esta delegación es segura
    - `src/cli/dashboard/format.js:250-268` — `nextCell`: colapso `/\s+/g` + `trim`, never-throws para no-string
    - `test/format-isolation.test.js:200-221` — el guard §TUI-04, que comprueba **cada fichero** de `src/cli/dashboard/` por separado. **NO se modifica** (D-18); se lee para entender por qué el import es seguro
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §5
  </read_first>
  <action>
Aplicar la delegación de **D-04** en `src/cli/dashboard/select.js`:

1. Añadir el primer import de runtime del módulo: `import { nextCell } from './format.js';`, colocado después del bloque de comentarios de cabecera y antes del `@typedef` que ya referencia `./format.js`. Acompañarlo de un comentario de dos o tres líneas que declare **por qué** este import es seguro: `format.js` es puro (su único import es `node:path`), está cubierto por el mismo guard §TUI-04 que este fichero, y no hay ciclo porque `format.js` no importa `select.js`.

2. Sustituir el cuerpo de `deriveAnyNext` por `return rows.some((r) => nextCell(r).length > 0);`. El predicado actual — el guard de `typeof` combinado con la longitud del string crudo — desaparece entero: `nextCell` ya es never-throws para no-string, así que el contrato de D-05 (never-throws) se preserva sin guard propio.

3. Actualizar el docblock de `deriveAnyNext`. Lo que **se conserva intacto**: el párrafo CRÍTICO de Pitfall 4 (se computa sobre el set SIN filtrar, `enriched` y no `filtered`, para que la columna no parpadee al teclear `/`), la firma `@param`, y el hecho de ser espejo literal de `deriveAnyProgress`. Lo que **cambia**: la línea de contrato deja de prometer longitud sobre el string crudo y pasa a enunciar «un `next` que colapsa a vacío NO enciende la columna», nombrando a `nextCell` como la **única fuente de verdad** del colapso y citando DEBT-06 como la razón del cambio.

4. **No tocar** el comentario de color-isolation de `:26-27` — es la invariante que el guard §TUI-04 custodia.

Fences de esta tarea: `src/cli/dashboard/format.js` **no se modifica** · `src/cli/dashboard/App.js` **no se toca** (D-05) · `test/format-isolation.test.js` **no se modifica** (D-18) · los asserts de `test/dashboard-select.test.js:473-497` **no se tocan**. Si alguno de esos 8 asserts se pone rojo, la delegación está mal hecha: se corrige la delegación, jamás el assert (RESEARCH §2d verificó empíricamente que los 8 son invariantes bajo el cambio).
  </action>
  <verify>
    <automated>node --test test/dashboard-select.test.js test/dashboard-format.test.js test/format-isolation.test.js && grep -cF "import { nextCell } from './format.js';" src/cli/dashboard/select.js | grep -qx 1 && grep -c 'r.next.length' src/cli/dashboard/select.js | grep -qx 0 && echo "DEBT-06 GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-select.test.js` pasa con exit code 0, incluido el `it()` `DEBT-06` que la Task 2 dejó en rojo
    - `grep -cF "import { nextCell } from './format.js';" src/cli/dashboard/select.js` devuelve exactamente `1`
    - `grep -cF 'nextCell(r).length > 0' src/cli/dashboard/select.js` devuelve exactamente `1`
    - `grep -c 'r.next.length' src/cli/dashboard/select.js` devuelve `0` — el predicado sobre el string crudo ha desaparecido
    - `grep -c 'picocolors' src/cli/dashboard/select.js` devuelve `0` y `node --test test/format-isolation.test.js` pasa (§TUI-04 verde)
    - `git diff --exit-code test/dashboard-select.test.js` sale 0 respecto al commit de la Task 2 — ningún assert existente se ha modificado para greenear
    - `git diff --exit-code src/cli/dashboard/format.js src/cli/dashboard/App.js test/format-isolation.test.js` sale 0 (D-05 y D-18)
    - `grep -c 'nextCell' src/cli/dashboard/select.js` devuelve `>= 3` (import + predicado + docblock)
    - `npm test` sigue en 2586 tests · 0 fail (baseline `84-VERIFICATION.md:94`, tolerando el delta de los tests nuevos de esta fase)
  </acceptance_criteria>
  <done>`deriveAnyNext` decide la presencia de la columna con la MISMA regla que la pinta. La columna `next` deja de aparecer vacía cuando todos los `next` colapsan a cadena vacía. Los 8 asserts LIVE-05 previos siguen verdes sin tocarlos, `format-isolation` sigue verde y sin modificar, y `App.js` no se ha abierto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `state.json` (disco) → `deriveAnyNext` / `nextCell` | Dato persistido de forma arbitraria (lo escribe el hook de cierre de sesión) que cruza a la capa derive del TUI. Único punto de esta tarea donde entra dato no controlado. |
| *(sin frontera de red)* | El plan no abre `src/server.js`, no añade endpoints, no parsea entrada del operador ni instala nada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-01-01 | Denial of Service | `nextCell` (`src/cli/dashboard/format.js:264`) — regex `/\s+/g` aplicada a un `next` de disco | low | accept | Regex **CONSTANTE**, sin alternancia anidada ni backtracking catastrófico, y el input está truncado a 200 caracteres en escritura (Phase 74 D-02). El plan **no** introduce ninguna regex nueva ni compila ninguna desde input. |
| T-85-01-02 | Tampering | `src/cli/dashboard/select.js` — primer import de runtime del módulo | low | mitigate | El módulo importado (`format.js`) es puro: único import `node:path`. El guard §TUI-04 de `test/format-isolation.test.js` comprueba **cada fichero** de `src/cli/dashboard/` por separado, así que la invariante de color-isolation sigue custodiada sin ampliar el radio (verificado en RESEARCH §2b). Criterio de aceptación explícito: `grep -c picocolors` = 0 y `format-isolation` verde. |
| T-85-01-03 | Repudiation | Los 8 asserts LIVE-05 de `test/dashboard-select.test.js` | medium | mitigate | Prohibición explícita en `must_haves.prohibitions` + criterio de aceptación `git diff --exit-code` sobre el fichero de test entre la Task 2 y la Task 3: la delegación no puede greenearse tocando la red de no-regresión. |
| T-85-01-04 | Information Disclosure | Comentario JSDoc de `state.js` | low | accept | El texto nuevo describe un contrato de merge; no incorpora paths del operador, refs de workspace ni valores de `state.json`. |
| T-85-01-SC | Tampering | Supply chain (npm) | low | accept | **Cero instalaciones.** Constraint LOCKED «cero deps npm nuevas»; `85-RESEARCH.md` §Package Legitimacy Audit devuelve tabla vacía (0 paquetes, 0 `[SLOP]`, 0 `[SUS]`). No procede checkpoint de legitimidad. |
</threat_model>

<verification>
- `node --test test/dashboard-select.test.js` — verde (incluye el `it()` DEBT-06)
- `node --test test/dashboard-format.test.js` — verde (semántica de `nextCell` sin cambios)
- `node --test test/format-isolation.test.js` — verde y el fichero **sin modificar** (D-18)
- `node --test test/state/handoff-state.test.js` — verde y el fichero **sin modificar** (D-03)
- `npm test` — suite completa sin regresiones respecto al baseline de 2586 tests · 0 fail
- `git diff --exit-code src/cli/dashboard/App.js src/cli/dashboard/format.js test/format-isolation.test.js test/state/handoff-state.test.js` — sale 0
- SUMMARY con la sección del grep de auditoría D-02 (patrones, alcance, hits, clasificación)
</verification>

<success_criteria>
1. El typedef `TaskHandoff` documenta el contrato tres-estados por PRESENCIA del campo `next`, con los tres verbos de la tabla de `state.js:405-410` y la cita a `test/state/handoff-state.test.js:265/288/307` (DEBT-05, cierra 81-REVIEW WR-01).
2. `deriveAnyNext` delega en `nextCell`: un `next` de solo-whitespace ya no enciende la columna (DEBT-06, cierra 81-REVIEW WR-02; con DEBT-05 salda R-81-02).
3. El test RED de D-06 existe, está commiteado en rojo antes del fix y pasa a verde con la delegación.
4. Los 8 asserts LIVE-05 previos, `format-isolation.test.js`, `handoff-state.test.js`, `App.js` y `format.js` quedan sin tocar.
5. El grep de auditoría D-02 está ejecutado y documentado en el SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-01-SUMMARY.md` when done.
</output>
