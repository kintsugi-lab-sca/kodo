---
phase: 65-daemon-lifecycle-foundation
plan: 04
type: execute
wave: 3
depends_on: [65-03]
files_modified:
  - src/cli.js
  - test/daemon/daemon-run-integration.test.js
autonomous: true
requirements: [UP-04]

must_haves:
  truths:
    - "kodo daemon run is registered as a hidden commander subcommand under a daemon group"
    - "the daemon run action awaits runDaemon() and does NOT call process.exit (runDaemon blocks/owns exit)"
    - "spawning `node bin/kodo daemon run` (HOME-isolated, KODO_DEV=1) writes ~/.kodo/kodo.pid and the child stays alive (foreground blocks, does not self-detach)"
    - "sending SIGTERM to that child makes it exit 0 within 5s and removes ~/.kodo/kodo.pid"
  artifacts:
    - src/cli.js
    - test/daemon/daemon-run-integration.test.js
  key_links:
    - "cli.js lazy-imports runDaemon from ./daemon/run.js inside the action (mirror the polling group at cli.js:443)"
    - "the integration test is the process-level proof of UP-04 (foreground supervisable, clean SIGTERM); it exercises the full Wave 1→3 stack end to end"
---

<objective>
Wire the internal `kodo daemon run` entrypoint (D-02) that Phase 66 will orchestrate (detached via `kodo up`, direct via launchd/`brew services`) and prove it end to end. `daemon run` is a hidden commander subcommand whose action simply awaits `runDaemon()` (Plan 03) — no `process.exit` in the action, because the foreground funnel owns the exit. A process-level integration test spawns the real binary, confirms it blocks in the foreground (writes `kodo.pid`, stays alive), and shuts down cleanly on SIGTERM.

Purpose: This closes UP-04 with the one behavior that only a real child process can prove — that the daemon blocks in the foreground (never self-detaches) and tears down cleanly on SIGTERM. Test-first (Task 1 writes the failing spawn test; Task 2 wires the command to make it pass) keeps the wiring honest.
Output: `kodo daemon run` (hidden) in `src/cli.js` + the child-spawn integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-daemon-lifecycle-foundation/65-CONTEXT.md
@.planning/phases/65-daemon-lifecycle-foundation/65-RESEARCH.md

@src/cli.js
@test/cli/polling.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the process-level daemon-run integration test (RED until wired)</name>
  <files>test/daemon/daemon-run-integration.test.js</files>
  <read_first>
    - test/cli/polling.test.js:12-27 (spawn/spawnSync HOME-isolated molde: absolute KODO_BIN = join(REPO,'bin','kodo'), NO_COLOR, timeout)
    - src/daemon/run.js (Plan 03 — runDaemon blocks forever, writes kodo.pid via writePidFile(...,'kodo'), cleans up on SIGTERM)
    - src/cli/polling-daemon.js (getPidPath('kodo') to resolve the expected PID path in the isolated HOME)
    - src/server.js:405 (KODO_DEV=1 bypasses the webhook-secret gate so the composed server can start in the test)
  </read_first>
  <action>
    Write test/daemon/daemon-run-integration.test.js as a process-level test (mirror the spawn HOME-isolation molde of test/cli/polling.test.js:12-27). Set up an isolated HOME (mkdtempSync) with a minimal config.json whose provider resolves offline so startServer({managed}) succeeds (a provider whose init() does not require the network — the only registered providers are plane + github; pick the one that inits offline in this repo and document the choice in a comment). `spawn(process.execPath, [KODO_BIN, 'daemon', 'run'], { env: { ...process.env, HOME: tmpHome, KODO_DEV: '1' }, stdio: ... })` — KODO_DEV=1 clears the managed misconfig gate (server.js:405). Poll up to ~3s for getPidPath('kodo') (i.e. tmpHome/.kodo/kodo.pid) to appear; assert it exists (foreground funnel wrote it). Then assert the child is STILL ALIVE after a short delay (foreground blocks — it did NOT self-detach/exit): the spawned child's `exitCode`/`signalCode` are still null. Then `child.kill('SIGTERM')`; wait for the 'exit' event with a bounded timeout ≤5s; assert exit code 0 (or SIGTERM-clean) AND that kodo.pid was removed. Ensure teardown always kills the child and rmSync the tmp HOME even on assertion failure. Under CURRENT code (daemon run not yet wired) this test fails — that is the expected RED for Task 2.
  </action>
  <verify>
    <automated>node --test test/daemon/daemon-run-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test spawns bin/kodo daemon run HOME-isolated, polls for kodo.pid, asserts child stays alive (foreground), SIGTERM → exit 0 ≤5s + kodo.pid removed
    - Test is hermetic (isolated HOME, always kills child + cleans tmp)
  </acceptance_criteria>
  <done>Integration test spawns bin/kodo daemon run, asserts kodo.pid written + child stays alive + SIGTERM→exit 0 ≤5s + kodo.pid removed (RED until wired).</done>
</task>

<task type="auto">
  <name>Task 2: Register kodo daemon run (hidden) wired to runDaemon</name>
  <files>src/cli.js</files>
  <read_first>
    - src/cli.js:65-84 (kodo start / stop wiring — action shape, lazy import)
    - src/cli.js:442-497 (polling group: program.command('polling') + subcommands + lazy dynamic import in the action)
    - src/daemon/run.js (Plan 03 — export runDaemon)
  </read_first>
  <action>
    Per D-02: register an internal `daemon` command group and a hidden `run` subcommand, mirroring the polling group shape at cli.js:442-497. Add `const daemon = program.command('daemon').description('Internal daemon lifecycle');` then `daemon.command('run', { hidden: true }).description('Run the composed daemon (server + polling) in the foreground').action(async () => { const { runDaemon } = await import('./daemon/run.js'); await runDaemon(); });` — commander 13.1.0 supports the `{ hidden: true }` options-object form (verified). The action MUST simply await runDaemon and NOT call process.exit — runDaemon blocks forever and is the single owner of the exit (D-05); a process.exit in the action would defeat the foreground funnel. Use the lazy `await import('./daemon/run.js')` pattern (mirror the polling handlers) so the parent CLI does not pay the daemon import cost for unrelated commands. Do NOT add `kodo up`, detach/spawn, or stop/status here — those are Phase 66; this phase exposes only the internal foreground entrypoint. Keep the change surgical and additive; do not touch the start/stop/polling wiring.
  </action>
  <verify>
    <automated>node --test test/daemon/daemon-run-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `kodo daemon run` is a hidden subcommand that awaits runDaemon (no process.exit in the action)
    - The Task-1 integration test is now GREEN (foreground blocks, kodo.pid written, clean SIGTERM exit 0, kodo.pid removed)
  </acceptance_criteria>
  <done>kodo daemon run (hidden) awaits runDaemon (no process.exit); integration test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → daemon entrypoint | the hidden internal entrypoint must not self-detach or PATH-lookup |
| supervisor (launchd/brew, Phase 66) → daemon run | the process must block in the foreground to be supervisable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-11 | Elevation of Privilege | kodo daemon run entrypoint | low | mitigate | Foreground-only this phase (no spawn/PATH lookup in the action); detach with process.execPath + absolute KODO_BIN is Phase 66; hidden subcommand reduces accidental direct operator invocation |
| T-65-12 | Denial of Service | daemon run self-detach (crash-loop risk) | medium | mitigate | Action awaits runDaemon which blocks via await new Promise(()=>{}); no spawn/unref here — the integration test asserts the child stays alive in the foreground (prevents the launchd crash-loop of Pitfall 6, load-bearing for Phase 66) |
| T-65-13 | Denial of Service | Windows non-POSIX detach/SIGTERM | low | accept | Phase 65 foreground path is cross-platform; the detach/launchd Windows guard is Phase 66 (documented deferral) |
| T-65-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit = N/A; zero-new-deps invariant LOCKED) |
</threat_model>

<verification>
- `node --test test/daemon/daemon-run-integration.test.js` green (UP-04 process-level proof)
- `npm test` green (full suite — includes the phase-gate integration test and no-regression of polling/stop suites)
</verification>

<success_criteria>
- `kodo daemon run` (hidden) starts the composed daemon in a foreground process that blocks and does not self-detach (SC1, UP-04)
- SIGTERM shuts it down cleanly: exit 0 ≤5s and `~/.kodo/kodo.pid` removed (SC1, UP-04)
- No `kodo up`/detach/stop/status introduced (correctly deferred to Phase 66)
</success_criteria>

<output>
Create `.planning/phases/65-daemon-lifecycle-foundation/65-04-SUMMARY.md` when done.
</output>
