---
phase: 66-kodo-up-stop-status-unificados-homebrew
plan: 04
type: execute
wave: 3
depends_on: [66-03]
files_modified: []
autonomous: false
requirements: [UP-01, UP-02, DIST-01, DIST-02, DIST-03]

user_setup:
  - service: homebrew-tap
    why: "Publicar la fórmula in-tree en el tap para el ciclo real de brew services (DIST-01/02)"
    dashboard_config:
      - task: "Crear el repo tap (recomendado kintsugi-lab/homebrew-kodo; el prefijo 'homebrew-' es obligatorio) y publicar packaging/homebrew/Formula/kodo.rb como Formula/kodo.rb; crear la release/tag y rellenar url+sha256"
        location: "GitHub → nuevo repo homebrew-kodo + release del tag de kodo"

must_haves:
  truths:
    - "El ciclo real `brew install kodo` → `brew services start kodo` → `brew services list`=started funciona en macOS con la fórmula del tap (DIST-01, DIST-02)."
    - "El plist renderizado invoca `kodo daemon run` (foreground supervisado) — verificado runtime que NO entra en crash-loop (Pitfall 6)."
    - "`kodo up` end-to-end en terminal real: dashboard se engancha y, al cerrar (q/Ctrl-C), el daemon PERSISTE (UP-01, UP-02)."
  artifacts:
    - "Registro del resultado del spike en el 66-04-SUMMARY.md (checks pasados/fallados, arquitectura probada, ajustes de fórmula si A1 falla)."
  key_links:
    - "opt_bin absoluto resuelve Apple Silicon /opt/homebrew vs Intel /usr/local; el shebang node bajo el PATH mínimo de launchd (A1) es el riesgo abierto a confirmar."
---

<objective>
Validar manualmente el único riesgo NO unit-testable de Phase 66: el ciclo real de `brew services` en macOS y el `kodo up` end-to-end en un TTY real. Todo el código (`up`/`stop`/`status`) y la fórmula ya están implementados y con unit tests verdes (Plans 66-01/02/03); este plan es el GATE MANUAL OBLIGATORIO (D-07) que cierra Pilar 1.

Purpose: launchd bajo `brew services` (PATH mínimo, opt_bin por arquitectura, RunAtLoad, crash-restart, lectura de ~/.kodo/.env) NO es unit-testable — `brew audit` ni siquiera detecta el foreground-trap (Pitfall 6). La única validación posible es una instalación real. En `--auto` este checkpoint se PAUSA para el operador (NO se auto-aprueba — es una validación de despliegue real).

Output: 66-04-SUMMARY.md con el resultado de los 9 chequeos + cualquier ajuste requerido en la fórmula (p.ej. añadir `EnvironmentVariables.PATH` si el shebang node no resuelve — open question A1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-kodo-up-stop-status-unificados-homebrew/66-CONTEXT.md
@.planning/phases/66-kodo-up-stop-status-unificados-homebrew/66-RESEARCH.md
@.planning/phases/66-kodo-up-stop-status-unificados-homebrew/66-VALIDATION.md
@packaging/homebrew/Formula/kodo.rb
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: GATE MANUAL — spike real de brew services + kodo up E2E (D-07)</name>
  <action>
    PAUSA para el operador — checkpoint NO auto-aprobable (D-07: en `--auto` se pausa, jamás se marca pasado automáticamente). El operador ejecuta manualmente los 9 checks enumerados en <how-to-verify> (ciclo real de `brew services` en macOS + `kodo up` end-to-end en TTY real), aplica cualquier ajuste de fórmula que exija A1 (CHECK 4), y registra el resultado en 66-04-SUMMARY.md antes de dar la fase por cerrada. No hay trabajo de código automatizable aquí — todo lo automatizable ya está verde en 66-01/02/03.
  </action>
  <what-built>
    Pilar 1 completo y con unit tests verdes: `kodo up` (orquestador detached + attach dashboard + persistencia, Plan 66-01), `kodo stop`/`status` daemon-first con `--json` (Plan 66-02), wiring en cli.js + la fórmula `packaging/homebrew/Formula/kodo.rb` con `service do` → `kodo daemon run` (Plan 66-03). Falta la única validación no automatizable: el ciclo real de `brew services` en macOS y el `kodo up` end-to-end en un TTY real.
  </what-built>
  <how-to-verify>
    Requisitos previos (setup del tap): publica `packaging/homebrew/Formula/kodo.rb` como `Formula/kodo.rb` en el repo tap `kintsugi-lab/homebrew-kodo` (confirma el owner), crea la release/tag de kodo y rellena `url` + `sha256` reales en la fórmula. Requiere Homebrew ≥4.x en una máquina macOS (blocker si ausente).

    CHECK 1 — install: `brew tap kintsugi-lab/kodo && brew install kodo` completa sin error (resuelve `depends_on node`, corre `npm install *std_npm_args`, crea el symlink `bin/kodo`).
    CHECK 2 — opt_bin por arquitectura: `readlink $(brew --prefix)/opt/kodo/bin/kodo` apunta al symlink correcto (Apple Silicon `/opt/homebrew` vs Intel `/usr/local`). Anota la arquitectura probada (Pitfall 4/9).
    CHECK 3 — service start: `brew services start kodo` → `brew services list` muestra `kodo` como `started` (NO `error`, NO parpadeo `started`↔`stopped` cada ~10s). Un parpadeo delataría el foreground-trap (Pitfall 6).
    CHECK 4 — node bajo launchd (open question A1): revisa `$(brew --prefix)/var/log/kodo.log` — NO debe haber "env: node: No such file or directory" ni "spawn node ENOENT". Si falla, añade `EnvironmentVariables { "PATH" => "#{Formula["node"].opt_bin}:#{ENV["PATH"]}" }` (o invoca node absoluto) a la fórmula y repite desde CHECK 1 (Pitfall 2).
    CHECK 5 — .env leído bajo launchd: con `~/.kodo/.env` y `~/.kodo/config.json` VÁLIDOS (config completa, NO first-run — el first-run limpio se aborda en Phase 68), el daemon arranca y sirve; confirma en el log que obtiene sus secretos (Pitfall 7).
    CHECK 6 — RunAtLoad tras relogin: cierra sesión / reinicia → el daemon vuelve a arrancar automáticamente al login.
    CHECK 7 — crash-restart: `kill` el pid del daemon → launchd lo reinicia (keep_alive).
    CHECK 8 — service stop: `brew services stop kodo` → `brew services list` = `stopped` limpio; el daemon deja de correr.
    CHECK 9 — `kodo up` E2E (sin brew, TTY real): `kodo up` → aparece el dashboard → pulsa `q` → `kodo status` confirma que el daemon SIGUE vivo (persistencia, UP-02) → `kodo stop` lo tumba. Repite `kodo up` estando el daemon ya vivo → adjunta sin doble-spawn (idempotencia, UP-03).

    Registra en 66-04-SUMMARY.md: qué checks pasaron, la arquitectura probada, y cualquier cambio aplicado a la fórmula (especialmente el resultado de A1 en CHECK 4).
  </how-to-verify>
  <resume-signal>Escribe "approved" si los 9 checks pasan (anota la arquitectura), o describe qué check falló y el ajuste aplicado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launchd → daemon (runtime real) | el supervisor del OS arranca el binario; PATH mínimo, HOME del user agent |
| tarball de release → máquina del usuario | instalación real desde el tap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-07 | Denial of Service | crash-loop launchd (foreground trap) | high | mitigate | CHECK 3/4 validan runtime que el plist (`daemon run`) NO parpadea ni crash-loopea |
| T-66-09 | Elevation of Privilege | resolución de node bajo PATH mínimo (A1) | medium | mitigate | CHECK 4 verifica el log; si falla, se añade EnvironmentVariables.PATH a la fórmula |
| T-66-10 | Tampering | split-brain de ~/.kodo bajo `sudo brew services` (HOME=/var/root) | medium | mitigate | Instalar como user LaunchAgent (SIN sudo); CHECK 5/6 validan homedir() correcto |
| T-66-SC | Tampering (supply chain) | tarball instalado | high | mitigate | sha256 real en la fórmula + `brew audit --new --strict` antes de publicar el tap |
</threat_model>

<verification>
- Los 9 checks manuales aprobados por el operador (D-07). En `--auto` el checkpoint PAUSA — no se auto-aprueba.
- Cualquier ajuste de la fórmula (A1) se aplica a `packaging/homebrew/Formula/kodo.rb` y se re-verifica desde CHECK 1.
- `npm test` verde como pre-requisito (los Plans 66-01/02/03 ya lo garantizan) antes de correr el spike.
</verification>

<success_criteria>
- Ciclo real `brew install`→`services start`→`list`=started→relogin→crash-restart→`stop` verde en macOS (DIST-01, DIST-02).
- El plist invoca `kodo daemon run` y NO crash-loopea (Pitfall 6 cerrado en runtime).
- `kodo up` E2E: dashboard engancha y el daemon persiste al cerrar (UP-01, UP-02); idempotencia confirmada (UP-03).
- Pilar 1 shippable: `brew install kodo && kodo up` deja kodo funcionando.
</success_criteria>

<output>
Create `.planning/phases/66-kodo-up-stop-status-unificados-homebrew/66-04-SUMMARY.md` when done (incluye el registro de los 9 checks + arquitectura probada + ajustes de fórmula).
</output>
