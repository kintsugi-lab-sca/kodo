---
phase: 68-dashboard-setup-mode-cfgf-03-first-run
plan: 03
type: execute
wave: 3
depends_on: ["68-02"]
files_modified:
  - src/cli.js
  - test/cli/config-writers.test.js
  - test/config-env-writer.test.js
autonomous: false
requirements: [SETUP-05]
must_haves:
  truths:
    - "El wizard kodo config (interactiveConfig) escribe config/proyectos SOLO vía saveConfig/saveProjects (los mismos escritores que el dashboard, SETUP-05/D-10)"
    - "El wizard sigue comprobando PRESENCIA de la key vía getProviderApiKey, sin capturar su valor (cli.js:613-620, D-11)"
    - "Un test de source-hygiene/DI blinda el invariante single-writer: ningún camino escribe config/.env fuera de saveConfig/saveProjects/writeEnvVar"
    - "El grep de higiene PERSIST-04 de Phase 67 sigue verde tras añadir el modo setup (los 5 sinks intactos)"
  artifacts:
    - "src/cli.js — interactiveConfig verificado/rewired: únicos escritores compartidos, sin captura del valor de la key"
    - "test/cli/config-writers.test.js — source-hygiene/DI del invariante single-writer (SETUP-05)"
    - "test/config-env-writer.test.js — re-verificación del boundary de 5 sinks tras el modo setup"
  key_links:
    - "saveConfig/saveProjects/writeEnvVar son los únicos escritores compartidos por dashboard y wizard (una fontanería, varios consumidores)"
    - "El valor de la key entra EXCLUSIVAMENTE por el campo enmascarado del dashboard (D-11); el wizard nunca lo captura"
  prohibitions:
    - "interactiveConfig NO hace rl.question sobre el VALOR de la API key (eco del secreto en terminal — Pitfall 11)"
    - "Ningún camino escribe config.json o .env fuera de saveConfig/saveProjects/writeEnvVar"
    - "El wizard NO shell-out del secreto (sin execFile/spawn con el valor en argv)"
    - "El modo setup añadido en 68-02 NO regresiona el grep de higiene de los 5 sinks de Phase 67"
---

<objective>
Cerrar SETUP-05 formalmente y blindar el boundary de seguridad de la fase. (1) Rewire mínimo/quirúrgico de `interactiveConfig` (`kodo config`, cli.js:576-680): garantizar que el camino headless y el TUI convergen en los MISMOS escritores (`saveConfig`/`saveProjects`/`writeEnvVar`) y que el wizard NO captura el valor de la key (D-10/D-11). (2) Un test de source-hygiene/DI que fije el invariante single-writer. (3) Re-verificar que el grep de higiene PERSIST-04 de Phase 67 sigue verde tras añadir el modo setup (68-02). (4) GATE MANUAL LOCKED: UAT en máquina limpia.

Purpose: SETUP-05 es "una fontanería, varios consumidores" — sin este blindaje el wizard y el dashboard podrían divergir en cómo escriben, reintroduciendo un vector de fuga del secreto. El rewire es casi un no-op de verificación (el wizard YA usa saveConfig/saveProjects y YA no captura el valor, cli.js:613-620); el trabajo real es el test que lo mantiene verdadero + el gate humano del ciclo real de first-run.
Output: interactiveConfig verificado, test de single-writer, re-verificación de los 5 sinks, y el checkpoint humano del ciclo limpio.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-CONTEXT.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-RESEARCH.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-PATTERNS.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-VALIDATION.md
@src/cli.js
@src/config.js
@test/config-env-writer.test.js
@test/config.test.js
</context>

## Artifacts this phase produces (para excluir en source-grounding)

Símbolos NUEVOS en este plan:
- `test/cli/config-writers.test.js` — test de source-hygiene/DI del invariante single-writer (nuevo).
Reuso/verificación (sin símbolos nuevos): `interactiveConfig` (cli.js:576, ya usa `saveConfig`/`saveProjects`/`getProviderApiKey`), `saveConfig`/`saveProjects`/`writeEnvVar` (config.js), el grep de higiene de los 5 sinks (test/config-env-writer.test.js, Phase 67).

<tasks>

<task type="auto">
  <name>Task 1: rewire/verificación de interactiveConfig (SETUP-05) + test source-hygiene single-writer</name>
  <files>src/cli.js, test/cli/config-writers.test.js</files>
  <read_first>
    - `src/cli.js` — `interactiveConfig` (cli.js:576-680): lazy import compartido `const { loadConfig, saveConfig, loadProjects, saveProjects, getProviderApiKey } = await import('./config.js')` (cli.js:579), lista canónica `const availableProviders = ['plane', 'github']` (cli.js:588), la comprobación de PRESENCIA de la key SIN capturar el valor (cli.js:613-620: `getProviderApiKey` + mensaje si falta, `rl.close()`), `saveConfig(config)` (cli.js:653), `saveProjects` (cli.js:777/781), `ensureConfig` (cli.js:561-574).
    - `test/config.test.js` — molde de test de source-hygiene por lectura de fuente + grep de violaciones (config.test.js:95-110, patrón `readFileSync` → strip comments → assert `violations` vacío).
    - `test/config-env-writer.test.js` — molde de blindaje source-level (config-env-writer.test.js:320-346: lee el cuerpo de una función y asevera `doesNotMatch` de vectores de shell-out/console).
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-PATTERNS.md` §cli.js (rewire casi no-op de verificación).
  </read_first>
  <action>
    Rewire quirúrgico de `interactiveConfig` (cli.js:576-680): VERIFICAR (y ajustar solo si diverge) que (a) toda escritura de config/proyectos pasa por `saveConfig`/`saveProjects` importados de `config.js` (cli.js:579) — no hay `writeFileSync`/`writeFileAtomic` directo sobre `config.json`/`.env` en el wizard; (b) el wizard NO hace `rl.question` sobre el VALOR de la API key (mantiene la comprobación de PRESENCIA vía `getProviderApiKey` de cli.js:613-620, D-10/D-11 — evita el eco del secreto en terminal, Pitfall 11); (c) la lista de providers es la canónica `['plane','github']` (cli.js:588), la misma que el selector del dashboard (D-05). Si algo diverge (p. ej. un writer directo), corregirlo para converger en los escritores compartidos; si ya cumple (esperado — Assumption A4), el rewire es un no-op documentado. Crear `test/cli/config-writers.test.js` (molde de source-hygiene de config.test.js:95-110 + config-env-writer.test.js:320-346): leer `src/cli.js` y asseverar, sobre el cuerpo de `interactiveConfig`, que (1) NO contiene escritura directa a `config.json`/`.env` (sin `writeFileSync`/`writeFileAtomic`/`renameSync` sobre esos paths — todo va por saveConfig/saveProjects/writeEnvVar); (2) NO hace `rl.question` que capture el valor de la key (grep de que la línea de la key es solo presencia, no input del valor); (3) NO importa `child_process` para el secreto (sin shell-out del valor). Los asserts usan `doesNotMatch` sobre el fragmento de fuente (molde existente), con mensajes de violación descriptivos.
  </action>
  <acceptance_criteria>
    - `src/cli.js` `interactiveConfig` escribe config/proyectos solo vía `saveConfig`/`saveProjects` (source assertion: sin writers directos de `config.json`/`.env`).
    - `interactiveConfig` mantiene la comprobación de presencia de la key (getProviderApiKey) sin capturar el valor (source assertion sobre cli.js:613-620).
    - `test/cli/config-writers.test.js` existe y asevera el invariante single-writer + no-captura-del-valor + no-shell-out.
    - `node --test test/cli/config-writers.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/cli/config-writers.test.js</automated>
  </verify>
  <done>interactiveConfig converge en saveConfig/saveProjects/writeEnvVar como únicos escritores y no captura el valor de la key; test/cli/config-writers.test.js verde blindando el invariante single-writer (SETUP-05/D-10/D-11).</done>
</task>

<task type="auto">
  <name>Task 2: re-verificación del boundary PERSIST-04 (5 sinks) tras el modo setup</name>
  <files>test/config-env-writer.test.js</files>
  <read_first>
    - `test/config-env-writer.test.js` — el bloque de higiene de Phase 67 (config-env-writer.test.js:320-346): lee el cuerpo de `writeEnvVar` y asevera `doesNotMatch` de `child_process`/`execFile|spawn|exec`/`console.` (los vectores de fuga del secreto).
    - `src/cli/dashboard/App.js` y `src/cli/dashboard/SessionTable.js` — las rutas de render del modo setup añadidas en 68-02 (paso apikey: máscara `'•'`, `setBuffer('')` al guardar/cancelar) — el nuevo código que este test debe cubrir para los sinks de render.
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-VALIDATION.md` §Per-Task Verification Map — fila PERSIST-04 (held-out grep, re-verificar).
  </read_first>
  <action>
    Re-verificar y, si hace falta, EXTENDER el grep de higiene held-out de Phase 67 para que cubra las rutas del modo setup añadidas en 68-02, sin debilitar los asserts existentes. Confirmar que el valor de la API key no alcanza los 5 sinks (argv/console/logger/config.json/overlay-snapshot): (a) `writeEnvVar` sigue siendo in-proceso (los asserts de config-env-writer.test.js:339-346 sobre su cuerpo permanecen verdes); (b) añadir una aserción source-level sobre las rutas del paso apikey del modo setup (App.js/SessionTable.js) de que el render usa la máscara `'•'.repeat` y que el buffer del secreto se limpia (`setBuffer('')`) al guardar/cancelar — es decir, ningún `console.*`/logger recibe el valor, y el render nunca pinta el buffer raw en el modo setup. Mantener los mensajes de violación descriptivos (molde existente). El objetivo es que este held-out siga siendo la red de seguridad de PERSIST-04 tras ampliar la superficie con el modo setup. NO tocar `writeEnvVar` (el escritor ya está blindado en Phase 67).
    Nota de disciplina: describir los tokens de sink por concepto en los mensajes del test; evitar incrustar el nombre literal de la env var del secreto o su valor en cualquier cadena.
  </action>
  <acceptance_criteria>
    - `test/config-env-writer.test.js` sigue aseverando el boundary de 5 sinks y ahora cubre (source-level) las rutas de render del modo setup (máscara `'•'` + limpieza del buffer).
    - Los asserts de Phase 67 sobre `writeEnvVar` (in-proceso, sin shell-out, sin console del valor) permanecen verdes.
    - `node --test test/config-env-writer.test.js` exits 0.
    - `npm test` (suite completa) verde — el modo setup no regresiona ninguna higiene.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/config-env-writer.test.js</automated>
  </verify>
  <done>El grep de higiene PERSIST-04 (5 sinks) sigue verde y cubre las rutas del modo setup; el valor del secreto no alcanza argv/console/logger/config.json/overlay tras 68-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: GATE MANUAL — UAT del ciclo first-run en máquina limpia (LOCKED)</name>
  <action>
    PAUSA para el operador — checkpoint NO auto-aprobable (GATE MANUAL LOCKED del roadmap; en `--auto` se pausa, jamás se marca pasado automáticamente). No hay trabajo de código automatizable aquí: todo lo unit/integration ya está verde en 68-01/68-02/68-03 Tasks 1-2. El operador ejecuta manualmente los 6 pasos enumerados en <how-to-verify> (ciclo real first-run en HOME limpio + TTY real + 2º arranque con KODO_DEV=1 para sortear el webhook secret, D-12) y registra el resultado en 68-03-SUMMARY.md antes de dar la fase por cerrada.
  </action>
  <what-built>
    El ciclo completo de onboarding dashboard-first: `needsSetup()` + rama pre-spawn de `runUp` (68-01), el modo setup de 4 pasos en el dashboard (68-02) y la fontanería única de escritura + higiene del secreto (68-03). Todo verificado por unit/integration, pero el ciclo real (TTY + disco limpio + spawn del daemon + webhook secret) NO es unit-testable — es el GATE MANUAL LOCKED del roadmap.
  </what-built>
  <how-to-verify>
    En un HOME/entorno temporal LIMPIO (sin `~/.kodo/config.json` ni `~/.kodo/.env`):
    1. Ejecutar `kodo up` en una terminal REAL (TTY). VERIFICAR: sirve el dashboard en modo setup (pantalla guiada "configuración inicial de kodo") SIN NINGÚN `exit(1)` — no arranca el daemon en este primer arranque (D-02). El proceso no crashea.
    2. Completar los 4 pasos: seleccionar provider `plane` → teclear `base_url` → teclear `workspace_slug` → teclear la API key en el campo enmascarado (debe verse SOLO `•` por carácter, nunca el valor). VERIFICAR: al guardar la key aparece el aviso honesto de reinicio (`config guardada — reinicia kodo (kodo up) para aplicar`) + la nota del webhook secret (D-08/D-12).
    3. Inspeccionar el disco: `~/.kodo/config.json` contiene provider/base_url/workspace_slug; `~/.kodo/.env` existe con permisos `0600` y contiene la key (NO en config.json). VERIFICAR: el valor de la key NO aparece en el scrollback de la terminal, ni en `~/.kodo/logs`, ni en `ps`.
    4. Transición honesta (Pitfall 15/D-09): el indicador de presencia de la key se refleja al instante (leído del estado fresco, no de una caché mentirosa).
    5. Segundo arranque con `KODO_DEV=1 kodo up` (o `--insecure`) para sortear el webhook secret ausente (D-12 — el webhook secret está FUERA del guiado): VERIFICAR que ahora `needsSetup()` es false, el daemon arranca y el dashboard muestra la tabla viva. (Sin `KODO_DEV=1`, documentar honestamente que el daemon requiere `KODO_WEBHOOK_SECRET_PLANE` por fuera — NO es un fallo del setup.)
    6. Caso non-TTY (D-13): `kodo up | cat` NO debe colgar ni crashear el first-run; degrada con un mensaje que remite a `kodo config`.
  </how-to-verify>
  <resume-signal>Escribe "approved" si los 6 pasos pasan, o describe el fallo (qué paso, qué se observó vs. lo esperado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard headless (`kodo config`, readline) → filesystem | El wizard escribe config/proyectos. Debe converger en los mismos escritores que el dashboard; nunca capturar el valor del secreto (eco en terminal). |
| todas las superficies de escritura → ~/.kodo | `saveConfig`/`saveProjects`/`writeEnvVar` como únicos escritores; cualquier otro camino de escritura es un vector de divergencia/fuga. |
| render del modo setup (68-02) → sinks de salida | El valor del secreto no puede alcanzar argv/console/logger/config.json/overlay-snapshot (los 5 sinks). |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-08 | Information Disclosure | `interactiveConfig` (eco del secreto) | high | mitigate | El wizard comprueba PRESENCIA (getProviderApiKey), NO captura el valor con `rl.question` (D-10/D-11). Acceptance: test/cli/config-writers.test.js (no-captura + no-shell-out). |
| T-68-09 | Information Disclosure | 5 sinks tras el modo setup | high | mitigate | Grep de higiene held-out re-verificado (máscara `'•'`, buffer limpiado, writeEnvVar in-proceso). Acceptance: test/config-env-writer.test.js verde + `npm test`. |
| T-68-10 | Tampering | escritores divergentes (config/.env) | medium | mitigate | Invariante single-writer blindado por source-hygiene. Acceptance: test/cli/config-writers.test.js. |
| T-68-11 | Denial of Service | daemon crash-loop por webhook secret ausente (Pitfall 1) | medium | accept | Fuera del guiado (D-12): el webhook secret se configura por fuera; UAT usa `KODO_DEV=1`/`--insecure`. Documentado honestamente en el aviso de reinicio; verificado en el GATE MANUAL paso 5. |

Nota de supply-chain: sin `npm/pip/cargo install` en la fase (invariante LOCKED). No aplica `T-68-SC`.
</threat_model>

<verification>
- `node --test test/cli/config-writers.test.js test/config-env-writer.test.js` verde + `npm test` (suite completa) verde.
- GATE MANUAL: los 6 pasos del checkpoint humano pasan (approved).
- Source assertions: interactiveConfig converge en los 3 escritores y no captura el valor de la key.
</verification>

<success_criteria>
- SETUP-05 cerrado: wizard y dashboard convergen en saveConfig/saveProjects/writeEnvVar; el wizard no captura el valor de la key (D-10/D-11).
- El boundary PERSIST-04 (5 sinks) sigue verde tras el modo setup.
- GATE MANUAL LOCKED aprobado: `kodo up` en máquina limpia sirve el setup sin exit(1), transición honesta, 2º arranque con KODO_DEV=1 → tabla viva.
</success_criteria>

<output>
Create `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-03-SUMMARY.md` when done
</output>
