---
phase: 79-sidebar-doctor
plan: 03
type: execute
wave: 3
depends_on: ["79-02"]
files_modified:
  - src/cli/sidebar-doctor.js
  - src/cli.js
  - test/cli/sidebar-doctor-cli.test.js
autonomous: false
requirements: [SDR-01, SDR-06]
must_haves:
  truths:
    - "src/cli/sidebar-doctor.js exporta runSidebarDoctor(opts, deps) — calco de runGsdDoctor (gsd-doctor.js:53-84): 1) SIEMPRE scan primero, 2) exitCode = report.hasActions ? 1 : 0 calculado ANTES del render, 3) execute(deps,{fix:true}) solo bajo opts.fix, 4) render"
    - "SDR-06: con opts.json el output es byte-determinista — JSON.stringify(report, null, 2) sin pasar por el formatter, idéntico TTY/no-TTY, cero ANSI; bajo --fix el result de execute se mergea bajo `executed`"
    - "SDR-06: exit code es hasActions ? 1 : 0 tanto en dry-run como en --fix; report.protected NUNCA afecta el exit (espejo D-09 de gsd-doctor)"
    - "SDR-01: el render humano (dry-run) agrupa las 3 categorías (missing_group/loose_workspace/empty_group) con la acción exacta por item, en orden D-09, vía createFormatter inyectado (cero picocolors/ANSI inline — color isolation)"
    - "src/cli/sidebar-doctor.js importa scan/execute de ../cmux/sidebar-doctor.js, NUNCA de ../cmux/client.js (RESEARCH §Nota de aislamiento)"
    - "src/cli.js registra `program.command('sidebar')` + subcomando `doctor` con --fix y --json, SIN ensureConfig (el doctor no toca provider — espejo del comentario cli.js:466-468), importando runSidebarDoctor dinámicamente y haciendo process.exit(code)"
    - "SDR-01 idempotency: correr el dry-run (--json) dos veces sobre el mismo estado produce bytes idénticos (report read-only serializado deterministamente)"
  artifacts:
    - "src/cli/sidebar-doctor.js (nuevo) — runSidebarDoctor + renderHuman por categoría"
    - "src/cli.js con el namespace `kodo sidebar doctor` registrado"
    - "test/cli/sidebar-doctor-cli.test.js (nuevo) — dry-run/--fix/--json/exit codes con DI hermético (calco de test/gsd-doctor-cli.test.js)"
  key_links:
    - "runSidebarDoctor -> scan/execute de sidebar-doctor.js: el CLI es un shell fino; toda la política vive en el módulo puro (Plan 02)"
    - "el exit code deriva SIEMPRE de scan().hasActions, no de execute — un --fix con errores parciales igual sale 1 si scan detectó acciones"
  prohibitions:
    - statement: "MUST NOT llamar ensureConfig() en el registro del comando sidebar — el doctor lee state.json/projects.json/cmux y no toca ningún provider; añadir ensureConfig acoplaría el carril doctor al provider configurado (rompe el 0-provider de SDR-03)"
      status: unverified
    - statement: "MUST NOT emitir ANSI/color en la rama --json ni construir el payload JSON a través del formatter — el --json es byte-determinista idéntico TTY/no-TTY (SDR-06/DX-06)"
      status: unverified
---

<objective>
La mitad CLI: `src/cli/sidebar-doctor.js` con `runSidebarDoctor` (calco literal de `runGsdDoctor`) y el registro `kodo sidebar doctor [--fix] [--json]` en `src/cli.js` (espejo del namespace `gsd`, sin `ensureConfig`). El ritual de dos pasos "doctor mira / doctor --fix arregla", exit `hasActions ? 1 : 0`, `--json` byte-determinista. Cierra con un checkpoint humano que confirma en vivo la convergencia real del sidebar (los verbos mutantes de cmux no se ejecutan en tests — manual-only).

Purpose: Expone el motor del Plan 02 como comando espejo de `gsd doctor` (SDR-06) y da al operador el dry-run seguro por defecto. El checkpoint valida empíricamente los supuestos A1/A2/A5 (semántica de create/add/set-anchor no ejercitada en unit) contra el sidebar real antes de declarar cerrada la fase.
Output: `src/cli/sidebar-doctor.js`, registro en `src/cli.js`, `test/cli/sidebar-doctor-cli.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/79-sidebar-doctor/79-CONTEXT.md
@.planning/phases/79-sidebar-doctor/79-RESEARCH.md
@.planning/phases/79-sidebar-doctor/79-PATTERNS.md

# Analogs de solo lectura (calco literal)
@src/cli/gsd-doctor.js
@test/gsd-doctor-cli.test.js
</context>

## Artifacts this phase produces

Ver `79-01-PLAN.md §Artifacts this phase produces`. Símbolos nuevos de ESTE plan: `runSidebarDoctor` (export de src/cli/sidebar-doctor.js), el comando `kodo sidebar` + subcomando `doctor` con flags `--fix`/`--json` en cli.js, y `test/cli/sidebar-doctor-cli.test.js`. Consume (no crea) `scan`/`execute` del Plan 02.

## Supuestos señalados (edge probe spec-less — sin drops silenciosos)

- **SDR-06 (unclassified):** el edge probe no clasificó una categoría de borde para "CLI espejo, --json byte-determinista, exit deterministas". Supuesto del planner: se cubre con los tests de --json TTY/no-TTY idéntico y exit hasActions?1:0 (truths arriba, calco de gsd-doctor-cli.test.js). No es un drop: queda trazado aquí.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runSidebarDoctor handler + render humano + CLI test</name>
  <files>src/cli/sidebar-doctor.js, test/cli/sidebar-doctor-cli.test.js</files>
  <read_first>
    - src/cli/gsd-doctor.js (calco literal: runGsdDoctor :53-84, renderHuman :106-141, renderCategory :154-163; el único cambio es report.hasGarbage -> report.hasActions y las 3 categorías del sidebar)
    - test/gsd-doctor-cli.test.js (calco del estilo DI hermético: makeStdoutStub, nocolorFormatter, cleanReport/garbageReport, tests de exit/dry-run-vs-fix/--json/render)
    - src/cmux/sidebar-doctor.js (scan/execute + shape de SidebarReport/SidebarResult del Plan 02)
    - src/cli/format.js (createFormatter — el color sale SOLO de aquí, inyectado)
  </read_first>
  <behavior>
    - Test: report limpio (hasActions:false) -> runSidebarDoctor devuelve 0 y renderiza verdict "clean"/limpio
    - Test: report con acciones (hasActions:true) -> devuelve 1 y renderiza las categorías con acción por item
    - Test: dry-run (fix:false) NUNCA llama executeFn
    - Test: --fix llama executeFn exactamente una vez DESPUÉS de scan (orden ['scan','execute']); exit sigue derivando de scan().hasActions
    - Test: --json produce JSON parseable = report serializado; bajo --fix mergea el result bajo `executed`; sin framing humano
    - Test: --json idéntico byte-a-byte con isTTY:true vs isTTY:false, cero secuencias ANSI
    - Test: protected presente pero sin acciones -> exit 0 (protected no afecta el exit)
  </behavior>
  <action>
    Crear `src/cli/sidebar-doctor.js` con `// @ts-check`, importando `scan as realScan, execute as realExecute` de `../cmux/sidebar-doctor.js` y `createFormatter` de `./format.js`. NUNCA importar de `../cmux/client.js`.
    `runSidebarDoctor(opts, deps)` calco de runGsdDoctor: `write`/`err`/`fmt`/`scanFn`/`executeFn` inyectables con defaults; 1) `const report = scanFn(deps)`; 2) `const exitCode = report.hasActions ? 1 : 0` (ANTES del render); 3) si `opts.fix` -> `result = await executeFn(deps,{fix:true})`; 4) si `opts.json` -> `write(JSON.stringify(opts.fix ? {...report, executed:result} : report, null, 2) + '\n')` (byte-determinista, SIN formatter), else `renderHuman(...)`; return exitCode.
    `renderHuman` calco de gsd-doctor.js:106: cabecera `kodo sidebar doctor` (+ ` --fix`/` (dry-run)`), tres `renderCategory` para missing_group / loose_workspace / empty_group con la acción exacta por item en orden D-09 (missing -> "create + add + set-anchor", loose -> "add", empty -> "ungroup"), resumen `protected`, verdict (hasActions -> "drift found, run --fix" / else "clean"), y bajo --fix la sección "executed" con created/added/ungrouped + errores por stderr. Todo el color vía `fmt.*` inyectado — cero picocolors/ANSI inline (color isolation).
    Crear `test/cli/sidebar-doctor-cli.test.js` calcando `test/gsd-doctor-cli.test.js` con los 7 <behavior> + un test source-hygiene de que el handler no importa picocolors.
  </action>
  <verify>
    <automated>node --test test/cli/sidebar-doctor-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Los 7 tests de <behavior> pasan
    - `runSidebarDoctor({}, {scanFn: cleanReport})` devuelve 0; con report con acciones devuelve 1
    - --json byte-idéntico TTY vs no-TTY (assert.equal de los dos buffers) y sin `\x1b[`
    - Source assertion: `src/cli/sidebar-doctor.js` no contiene `picocolors` ni importa `../cmux/client.js`
    - El exit code deriva de scan().hasActions incluso bajo --fix con errores
  </acceptance_criteria>
  <done>runSidebarDoctor es espejo fiel de runGsdDoctor con 3 categorías del sidebar; --json byte-determinista; exit deterministas; color aislado; CLI test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Registro `kodo sidebar doctor` en cli.js (sin ensureConfig)</name>
  <files>src/cli.js</files>
  <read_first>
    - src/cli.js:424-476 (bloque namespace `gsd` — `program.command('gsd')` + subcomando `doctor` :459-476, incluido el comentario "NO ensureConfig" :466-468)
    - src/cli/sidebar-doctor.js (el handler runSidebarDoctor de la Task 1)
    - .planning/phases/79-sidebar-doctor/79-PATTERNS.md §src/cli.js (registro namespace)
  </read_first>
  <action>
    Registrar en `src/cli.js` un namespace nuevo espejo de `gsd`: `const sidebar = program.command('sidebar').description('cmux sidebar hygiene (workspace groups)')`. Añadir `sidebar.command('doctor')` con `.description(...)` (menciona detectar dry-run y arreglar --fix la deriva de grupos: faltantes/disueltos, workspaces sueltos, grupos vacíos), `.option('--fix', ...)`, `.option('--json', ...)`, y `.action(async (opts) => { ... })` que — SIN `ensureConfig()` (espejo del comentario cli.js:466-468: el doctor no toca provider) — importe dinámicamente `runSidebarDoctor` de `./cli/sidebar-doctor.js`, lo invoque con `{ fix: opts.fix||false, json: opts.json||false }` y haga `process.exit(code)`; catch -> `console.error` + `process.exit(1)`.
    Colocar el bloque cerca del namespace `gsd` para consistencia. NO añadir ensureConfig. NO tocar otros comandos.
  </action>
  <verify>
    <automated>node src/cli.js sidebar doctor --json > /dev/null 2>&1; test $? -le 1 && node --test test/cli/sidebar-doctor-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node src/cli.js sidebar doctor --json` imprime un JSON parseable y sale con 0 o 1 (no crash; exit determinista aunque cmux/state falten -> fail-open -> hasActions=false -> 0)
    - `node src/cli.js sidebar doctor --help` lista --fix y --json
    - El action NO llama ensureConfig (source assertion sobre el bloque)
    - Los demás comandos de cli.js siguen registrados (ningún diff fuera del bloque nuevo)
  </acceptance_criteria>
  <done>`kodo sidebar doctor [--fix] [--json]` registrado y ejecutable end-to-end, sin ensureConfig, con exit determinista.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — convergencia real del sidebar en vivo (SDR-05, A1/A2/A5)</name>
  <action>Pausar la ejecución autónoma y ceder al operador para verificar en vivo el sidebar cmux según <how-to-verify>. No continuar hasta recibir "approved" o una discrepancia.</action>
  <what-built>
    `kodo sidebar doctor` completo: dry-run por defecto (lista la deriva de grupos clasificada) y `--fix` (ejecuta el allowlist no-destructivo create/add/set-anchor/ungroup). Todo el motor está cubierto por unit tests con DI; lo que NO se ejercita en tests es la mutación REAL del sidebar cmux (los verbos mutantes no se corren en unit — manual-only, VALIDATION.md) ni los supuestos A1 (¿`create --json` devuelve el ref?), A2 (¿`add` mueve un workspace de otro grupo?) y A5 (¿los verbos mutan bajo el daemon headless?).
  </what-built>
  <how-to-verify>
    1. Dry-run seguro (no muta nada): `node src/cli.js sidebar doctor` y `node src/cli.js sidebar doctor --json`. Confirmar que lista las categorías coherentes con el estado real (comparar contra `cmux workspace-group list --json`) y que el exit es 1 si hay acciones / 0 si limpio.
    2. Si hay ≥1 sesión kodo suelta (workspace sin su grupo esperado): `node src/cli.js sidebar doctor --fix`, luego `cmux workspace-group list --json` y confirmar que el workspace aparece ahora en `member_workspace_refs` del grupo esperado (SDR-05 en vivo).
    3. (Opcional, verifica A1) tras un `--fix` que cree un grupo, confirmar que el grupo quedó bien anclado (set-anchor al miembro más longevo) y sin duplicados.
    4. Confirmar que NINGÚN workspace del operador (no-kodo) fue movido ni re-anclado (D-04).
  </how-to-verify>
  <resume-signal>Escribe "approved" si el dry-run es coherente y (si se probó --fix) la convergencia es correcta sin tocar workspaces del operador; o describe la discrepancia (p.ej. A1/A2/A5 se comportan distinto a lo asumido) para ajustar execute().</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> CLI | `kodo sidebar doctor --fix` es el único opt-in de mutación; dry-run por defecto |
| runSidebarDoctor -> cmux (vía execute) | El `--fix` propaga a los verbos del allowlist del Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-02 | Denial of Service / Destruction | `--fix` como opt-in de mutación | high | mitigate | dry-run por defecto; `--fix` solo emite el allowlist no-destructivo (Plan 02 + guard Plan 01); checkpoint humano confirma que no toca workspaces del operador |
| T-79-07 | Information Disclosure | fuga de datos en el render/--json | low | mitigate | El report solo lleva refs/nombres de grupo (no contenido de usuario); --json byte-determinista sin ANSI; sin secretos en el payload |
| T-79-06 | Denial of Service | CLI cuelga si cmux no responde | low | mitigate | run() timeout 15s (Plan 01) -> fail-open per item; exit determinista aunque cmux falte |
| T-79-SC | Tampering | supply chain (npm) | low | accept | Cero deps npm nuevas; nada que instalar |
</threat_model>

<verification>
- `node --test test/cli/sidebar-doctor-cli.test.js` verde (todos los <behavior> + source-hygiene)
- `node src/cli.js sidebar doctor --json` sale 0/1 con JSON parseable; `--help` lista --fix/--json
- --json byte-idéntico TTY/no-TTY; exit hasActions?1:0
- `npm test` (suite completa, ~2309 tests) verde antes de `/gsd-verify-work`
- Checkpoint humano: convergencia real en la GUI cmux confirmada (SDR-05) sin tocar workspaces del operador
</verification>

<success_criteria>
- `kodo sidebar doctor` es espejo de `gsd doctor` (dry-run/--fix/--json/exit deterministas) — SDR-06
- El dry-run lista la deriva clasificada en orden D-09 sin ejecutar nada — SDR-01
- Sin ensureConfig (0-provider preservado)
- El operador confirma en vivo la convergencia no-destructiva
</success_criteria>

<output>
Create `.planning/phases/79-sidebar-doctor/79-03-SUMMARY.md` when done
</output>
