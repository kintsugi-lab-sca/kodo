---
phase: 42-dismiss-tui-read-write-server-amplification
plan: 03
type: execute
wave: 2
depends_on: ["42-01", "42-02"]
files_modified:
  - test/server-dismiss-e2e.test.js
  - .planning/STATE.md
autonomous: false
requirements: [DISMISS-01, DISMISS-02, DISMISS-03, DISMISS-04]

must_haves:
  truths:
    - "A real DELETE /sessions/{id} over a booted server (or its request-callback under test) returns 200 + {ok,removed,actions} for a dead session and 409 for a live one — proving the wiring of Plan 01 + Plan 02 contracts"
    - "The destructive double-d flow has been verified by a human against a real dead session (worktree+lock+state actually removed, footer shows the correct result)"
    - "STATE.md records the conscious break of the v0.9 'TUI read-only' invariant (observability → management)"
  artifacts:
    - path: "test/server-dismiss-e2e.test.js"
      provides: "end-to-end wiring test of the amplified DELETE handler against the real dismiss module"
      contains: "DELETE"
    - path: ".planning/STATE.md"
      provides: "documented identity change of the dashboard surface + invariant update"
      contains: "read-write"
  key_links:
    - from: "test/server-dismiss-e2e.test.js"
      to: "createDismissHandler + mapDismissResult"
      via: "feed server body into the consumer"
      pattern: "actions"
---

<objective>
Close Phase 42 with an end-to-end wiring assertion and the mandatory human UAT for a destructive mutation, then record the conscious break of the v0.9 "TUI read-only" invariant in STATE.md.

This is wave 2 — it depends on Plan 01 (server amplification) and Plan 02 (TUI read-write) both landing. The unit/integration layers are covered inside those plans; this plan adds the thin seam test that proves the two halves agree on the HTTP contract, and gates on a human verifying the real destructive flow (mirroring how v0.9 closed Phases 37/38 by manual UAT — RESEARCH A2, CONTEXT §specifics, ROADMAP §"Fase de alto riesgo").

Purpose: A destructive double-`d` that removes a real worktree+lock+state entry cannot be fully proven by deterministic tests alone (the operator's confidence in production is the gate). The automated seam test catches contract drift between the server body shape and the client's `mapDismissResult` consumption; the human UAT confirms the end-to-end behavior against a genuine dead session.

Output: `test/server-dismiss-e2e.test.js` (wiring seam), a human-verify checkpoint with exact UAT steps, and a STATE.md entry documenting the surface identity change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-dismiss-tui-read-write-server-amplification/42-CONTEXT.md
@.planning/phases/42-dismiss-tui-read-write-server-amplification/42-RESEARCH.md
@.planning/phases/42-dismiss-tui-read-write-server-amplification/42-01-SUMMARY.md
@.planning/phases/42-dismiss-tui-read-write-server-amplification/42-02-SUMMARY.md

<interfaces>
<!-- The two contracts being proven to agree -->
Server (Plan 01) emits: 200 {ok:true, removed, actions:[{type,result}]} | 409 {ok:false, error:'alive'}
TUI (Plan 02) consumes via dismissSession → mapDismissResult: actions[] result ∈ {removed,pruned,moved-dirty,kept,error} → {kind,color}
The seam: every `result` value the server can emit MUST be a value mapDismissResult handles, and vice-versa.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end wiring seam test (server body ↔ client consumption agree)</name>
  <files>test/server-dismiss-e2e.test.js</files>
  <read_first>
    - .planning/phases/42-dismiss-tui-read-write-server-amplification/42-01-SUMMARY.md (the exact exported createDismissHandler signature + the final actions[] result vocabulary Plan 01 emits)
    - .planning/phases/42-dismiss-tui-read-write-server-amplification/42-02-SUMMARY.md (the exact mapDismissResult discriminant + which result values it branches on)
    - src/server/dismiss.js (createDismissHandler, translateToActions — the real emitter)
    - src/cli/dashboard/select.js (mapDismissResult — the real consumer)
    - test/server/provider-state.test.js (node:test + node:assert/strict molde, fake deps)
  </read_first>
  <action>
    Create `test/server-dismiss-e2e.test.js` (node:test + node:assert/strict) that proves the two halves agree WITHOUT a flaky live HTTP boot. Drive `createDismissHandler` with a fake `loadState` (a dead session) and a fake `executeFn` returning representative DoctorResult counters for each scenario: (a) clean removal (worktree removed + state removed) → assert `status===200`, `body.actions` contains `{type:'worktree',result:'removed'}` and `{type:'state',result:'removed'}`, then feed `body` to `mapDismissResult` and assert `kind==='ok'`/green; (b) dirty worktree (`worktrees.moved:1`) → server emits `moved-dirty`, mapDismissResult → `kind==='dirty'`/yellow; (c) fail-open sub-error (`errors:[{category:'lock',...}]`) → server emits a `result:'error'`, mapDismissResult → `kind==='warn'`/yellow; (d) live session (fake loadState `alive:true`) → `status===409`, body `{ok:false,error:'alive'}`, mapDismissResult → `kind==='error'`/red reason 'alive'. Add a vocabulary guard: assert every distinct `result` string the translator can emit is a value mapDismissResult branches on (drift canary — if Plan 01 adds a result value Plan 02 doesn't handle, this fails).
  </action>
  <verify>
    <automated>node --test test/server-dismiss-e2e.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test/server-dismiss-e2e.test.js` imports both `createDismissHandler` (server) and `mapDismissResult` (client) and feeds the server body into the consumer.
    - Behavior: the clean / dirty / warn / 409 scenarios each produce the expected `mapDismissResult` kind+color.
    - Behavior: a vocabulary assertion fails if the server can emit a `result` value the client mapper does not handle.
    - Test: `node --test test/server-dismiss-e2e.test.js` exits 0 and `npm test` exits 0 (full suite green across both halves).
  </acceptance_criteria>
  <done>The server emitter and TUI consumer are proven to agree on the actions[] vocabulary end-to-end, deterministically, with the full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — destructive double-d against a real dead session</name>
  <files>(no files — human verification of the shipped destructive flow)</files>
  <read_first>
    - .planning/phases/42-dismiss-tui-read-write-server-amplification/42-UI-SPEC.md §"Interaction Contract" + §"Copywriting Contract" (the literal footer copy the operator should see)
    - .planning/phases/42-dismiss-tui-read-write-server-amplification/42-01-SUMMARY.md and 42-02-SUMMARY.md (what shipped, the exact commands to boot)
  </read_first>
  <action>
    Built: the `d` (dismiss) capability — arming an inline confirm over a dead row, a second `d` dispatching DELETE /sessions/{id} which delegates to doctor.execute({taskId, fix:true}) to remove the real worktree + lock + state.json entry, with a defense-in-depth 409 alive guard and a distinguishable footer (success / .dirty partial / error). First read-write capability in the dashboard — a genuinely destructive mutation, so a human verifies it end to end.

    UAT STEPS (the executor presents these and pauses for the operator):
    Prerequisite: have (or create) at least one DEAD session in state.json (alive===false) — e.g. a stale ROMAN-* session whose workspace is gone. Confirm its worktree under `.bg-shell/<sessionId>` and any per-repo `.kodo.lock` exist.
    1. Start the kodo server (normal boot) and open the dashboard (`kodo dashboard`).
    2. Navigate the cursor (↑/↓) to the dead row. Confirm the footer hint shows `· d dismiss`.
    3. Press `d` ONCE. Expected: footer shows `dismiss <task_ref>? press d again · Esc cancel` (cyan). The table keeps polling beneath (NOT frozen).
    4. Press `Esc`. Expected: returns to list, no DELETE sent, nothing removed. Re-arm with `d`.
    5. Press any non-`d` key (e.g. `x`) while armed. Expected: cancels, no DELETE sent.
    6. Re-arm with `d`, then press `d` AGAIN to confirm. Expected: within ~2.5s the dead row disappears (natural poll) and the footer shows `dismissed <task_ref>` (green) — or `dismissed <task_ref> — worktree preserved (.dirty)` (yellow) if the worktree was dirty.
    7. Verify on disk: the worktree directory is gone (or moved to `<path>.dirty` if it had uncommitted work), the per-repo lock is gone (if its PID was dead), and the session entry is no longer in `state.json` `sessions` (archived in `history`).
    8. Guard check: navigate to a LIVE row (alive===true) and press `d`. Expected: red footer `[!] session is alive — only dead sessions can be dismissed`, NO confirm, NOTHING removed.
    9. (Optional race check) If you can keep a session live and force a DELETE, confirm it returns 409 — the server-side guard. Otherwise step 8 is sufficient.

    Resume signal: type "approved" if all steps behave as described, or describe the discrepancy (which step, observed vs expected).
  </action>
  <verify>
    <human-check>Operator confirms steps 1-8 behave as specified: arm/confirm/cancel state machine, real dead-session removal (worktree+lock+state), `.dirty` preservation when applicable, and the live-row guard rejection. Operator types "approved" or reports the discrepancy.</human-check>
  </verify>
  <done>A human has exercised the destructive double-`d` flow against a real dead session and confirmed: the arm/confirm/Esc/any-key state machine, the real worktree+lock+state removal, the `.dirty` preservation path, and the live-row guard — and typed "approved".</done>
</task>

<task type="auto">
  <name>Task 3: Record the TUI read-only → read-write invariant break in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md §"Critical Invariants to Preserve" (the "TUI read-only, cero endpoints nuevos" bullet that this phase consciously breaks)
    - .planning/STATE.md §"Accumulated Context" (the Decisions sub-sections format for prior plans)
  </read_first>
  <action>
    Update `.planning/STATE.md`: in the "Critical Invariants to Preserve" section, update the "TUI read-only, cero endpoints nuevos" bullet to record that Phase 42 SHIPPED the conscious break — the dashboard is now read-WRITE for the dismiss-of-dead-sessions case, still adding zero endpoints (DELETE /sessions/{id} amplified, not new). Note the surface identity change (observability → management). Add a short "Decisions (Plan 42-01/42-02)" entry under Accumulated Context capturing: the server-side 409 alive guard as the authoritative TOCTOU re-check (D-07/D-08); the `dismiss.js` pure-DI extraction mirroring Phase 40 provider-state; `fix:true` locked; the `mode:'confirm'` double-d state machine; never-throws `dismissSession`. Keep it surgical — do not rewrite unrelated sections.
  </action>
  <verify>
    <automated>grep -c "read-write\|read-WRITE" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/STATE.md` "Critical Invariants" section records Phase 42 as the shipped break of "TUI read-only" (now read-write for dismiss, zero new endpoints).
    - Source: an Accumulated Context entry for Plan 42-01/42-02 captures the 409 TOCTOU guard, the dismiss.js extraction, fix:true, and the mode:'confirm' machine.
    - Verify: `grep -c "read-write\|read-WRITE" .planning/STATE.md` ≥ 1.
  </acceptance_criteria>
  <done>STATE.md documents the conscious read-only → read-write surface change and the Phase 42 implementation decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator → production destructive dismiss | The UAT is the final gate that a real worktree/lock/state removal behaves correctly before the operator trusts it in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Tampering | server body shape drifts from TUI consumption (a result value the footer can't map) | mitigate | The e2e seam test's vocabulary assertion fails on any actions[] `result` the server emits but mapDismissResult doesn't branch on. |
| T-42-12 | Tampering (data loss) | a real dirty worktree silently deleted instead of preserved | mitigate | Human UAT step 6/7 confirms dirty → `.dirty` preservation and the yellow footer; not provable by mocks alone. |
| T-42-13 | Repudiation | the conscious invariant break goes undocumented | mitigate | STATE.md entry (Task 3) records the surface identity change for future-milestone planners. |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node --test test/server-dismiss-e2e.test.js` — server body ↔ TUI consumer agree across clean/dirty/warn/409 (DISMISS-01..04 seam).
- `npm test` — full suite green across both halves.
- Human UAT (Task 2) — destructive double-`d` against a real dead session, the live-row guard, and the `.dirty` preservation all behave as specified.
- `grep -c "read-write\|read-WRITE" .planning/STATE.md` ≥ 1.
</verification>

<success_criteria>
- The amplified DELETE handler and the TUI consumer are proven to agree on the actions[] contract end-to-end (deterministic seam test).
- A human has verified the real destructive flow: dead-session removal, the double-`d`/Esc/any-key state machine, the live-row guard, and `.dirty` preservation (DISMISS-01..04).
- STATE.md records the conscious break of the v0.9 "TUI read-only" invariant.
</success_criteria>

<output>
Create `.planning/phases/42-dismiss-tui-read-write-server-amplification/42-03-SUMMARY.md` when done.
</output>
