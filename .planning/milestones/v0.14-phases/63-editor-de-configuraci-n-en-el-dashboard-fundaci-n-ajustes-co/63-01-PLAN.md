---
phase: 63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config-validate.js
  - src/config.js
  - test/config-validate.test.js
  - test/config-atomic.test.js
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - PERSIST-01
  - PERSIST-04
  - PERSIST-05
must_haves:
  truths:
    - "Un valor inválido (entero no-positivo, modelo fuera de {opus,sonnet,haiku}, color cmux desconocido, string vacío/espacios) es rechazado con {ok:false,error} y nunca llega a escribirse (CFG-05, D-07)"
    - "El config.json previo queda intacto byte-a-byte si la escritura o la serialización falla — nunca un fichero a medias (PERSIST-05, D-08)"
    - "saveConfig preserva el formato exacto JSON.stringify(cfg,null,2)+'\\n' y la migración de schema previa (PERSIST-01)"
    - "La lista de campos editables excluye por construcción api_key_env / base_url / workspace_slug / provider activo (PERSIST-04, D-11)"
  artifacts:
    - path: "src/config-validate.js"
      provides: "Validadores puros never-throws + registro getEditableFields + getByPath/setByPath puros"
      exports: ["validatePositiveInt", "validateModel", "validateNonEmpty", "validateCmuxColor", "validateField", "getEditableFields", "getByPath", "setByPath", "CMUX_COLORS", "MODELS"]
    - path: "src/config.js"
      provides: "writeFileAtomic temp+rename + saveConfig/saveProjects refactorizados sobre él"
      contains: "writeFileAtomic"
    - path: "test/config-validate.test.js"
      provides: "Tabla válido/inválido por campo + exclusión de campos de secreto"
    - path: "test/config-atomic.test.js"
      provides: "temp+rename, formato byte-exacto, original intacto en fallo"
  key_links:
    - from: "src/config.js"
      to: "src/config-validate.js"
      via: "App.js (Plan 02) importa validateField/getEditableFields/getByPath/setByPath; saveConfig usa writeFileAtomic"
      pattern: "writeFileAtomic|getEditableFields"
---

<objective>
Construir la **fundación pura y determinista** del editor de configuración: el módulo de validadores `src/config-validate.js` (CFG-05/D-06/D-07) y la escritura atómica no-corruptiva `writeFileAtomic` en `src/config.js` (PERSIST-01/05/D-08). Carril 100% local, sin ink, sin red, 0 tokens.

Purpose: estos dos módulos son las piezas de menor riesgo y máxima testabilidad del milestone; se construyen y se prueban en aislamiento (sin la TUI) para que el editor UI (Plan 02) las consuma ya verificadas, y para que el editor de proyectos de Phase 64 reuse la misma escritura atómica.
Output: `src/config-validate.js` (nuevo), `writeFileAtomic` + `saveConfig`/`saveProjects` refactorizados en `src/config.js`, y sus tests unitarios.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-CONTEXT.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-RESEARCH.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 01)

- **Módulo nuevo:** `src/config-validate.js` (puro, `// @ts-check`, never-throws).
- **Funciones nuevas:** `validatePositiveInt(raw)`, `validateModel(raw)`, `validateNonEmpty(raw)`, `validateCmuxColor(raw)`, `validateField(field, raw)` (dispatcher por `field.kind`), `getByPath(obj, dotted)`, `setByPath(obj, dotted, value)` (puro, opera sobre el clon que recibe), `getEditableFields(config)` (registro resuelto contra el provider activo).
- **Constantes/sets nuevos:** `MODELS` (`{opus, sonnet, haiku}`), `CMUX_COLORS` (16 colores nombrados de cmux).
- **Helper nuevo en `src/config.js`:** `writeFileAtomic(path, data)` (temp+rename, recibe `path` por parámetro — DI puro).
- **Tests nuevos:** `test/config-validate.test.js`, `test/config-atomic.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo de validadores puros + registro de campos editables (src/config-validate.js)</name>
  <files>src/config-validate.js, test/config-validate.test.js</files>
  <read_first>
    - `src/config.js` (DEFAULT_CONFIG — shape de claude/states/server/cmux; default `opus`, `max_parallel:3`, colores `Amber`/`Green`/`Crimson`/`Blue`; provider activo `config.provider`)
    - `src/cli.js` (líneas ~500-520: `setNestedValue` — MODELO conceptual del split-by-dot; NO se reusa: muta su input, aquí se hace variante PURA)
    - `.planning/phases/63-.../63-RESEARCH.md` (§Code Examples "Validador puro never-throws"; §Architecture Pattern 4; Pitfall 6 sobre default_model)
    - `test/config.test.js` (molde de test unitario `node:test` del repo)
  </read_first>
  <behavior>
    - validatePositiveInt: `'5'`→{ok:true,value:5}; `'0'`/`'-1'`/`'3.5'`/`'abc'`/`''`→{ok:false,error}; recorta espacios (`' 7 '`→7).
    - validateModel: `'opus'`/`'sonnet'`/`'haiku'`→ok; `'gpt'`/`'claude-opus-4'`/`''`→{ok:false} (set estricto, D-07/Pitfall 6 — documentar el límite en el JSDoc).
    - validateNonEmpty: `'In Progress'`→ok; `''`/`'   '`→{ok:false}; recorta.
    - validateCmuxColor: `'Amber'`/`'Crimson'`→ok; `'amber'` (minúscula)/`'#FF0000'`/`'Banana'`→{ok:false} (solo los 16 nombrados, v1).
    - validateField(field, raw): despacha por `field.kind` ('positiveInt'|'model'|'nonEmpty'|'cmuxColor') al validador correspondiente.
    - getByPath/setByPath: dot-walk puro; setByPath sobre un clon NO toca el objeto original a otra referencia.
    - getEditableFields(config): devuelve EXACTAMENTE 11 descriptores (NINGUNO de secreto), con los paths de states resueltos contra `config.provider`.
  </behavior>
  <action>
Crear `src/config-validate.js` con cabecera `// @ts-check` y comentarios densos en español (estilo del repo). Implementar el contrato D-06: validadores PUROS, no-I/O, never-throws, que devuelven `{ok:true, value}` o `{ok:false, error}` (error en español, copy estable). Exportar:

1. Sets de validación verificados (D-07, §RESEARCH State of the Art): `MODELS = new Set(['opus','sonnet','haiku'])` y `CMUX_COLORS = new Set(['Red','Crimson','Orange','Amber','Olive','Green','Teal','Aqua','Blue','Navy','Indigo','Purple','Magenta','Rose','Brown','Charcoal'])` (16 nombrados, v1 sin hex — discreción del planner resuelta: solo nombrados).
2. `validatePositiveInt(raw)`: trim, exige `/^\d+$/` sobre el string corto (anti-ReDoS), `Number(s)` entero `>= 1`. Cubre `max_parallel`, `idle_threshold_min`, `stuck_threshold_min` (CFG-01/CFG-03).
3. `validateModel(raw)`: trim + `MODELS.has(s)` (CFG-01). Documentar en JSDoc el límite del set estricto (un id completo `claude-*` manual se rechazaría — A2/Pitfall 6, decisión v1 aceptada).
4. `validateNonEmpty(raw)`: trim + longitud `> 0` (CFG-02 — `states.trigger/review/done`).
5. `validateCmuxColor(raw)`: trim + `CMUX_COLORS.has(s)` (CFG-04).
6. `getByPath(obj, dotted)` y `setByPath(obj, dotted, value)`: split por `.` y walk; setByPath MUTA el objeto recibido (el consumidor pasa siempre un `structuredClone`, ver Plan 02) y nunca crea referencias compartidas con `DEFAULT_CONFIG`. NO reusar `setNestedValue` de cli.js (muta su input por contrato CLI distinto); replicar solo la lógica split-by-dot como variante pura.
7. `getEditableFields(config)`: devuelve el REGISTRO de los 11 campos editables (D-11/PERSIST-04 — lista restringida EXPLÍCITAMENTE), cada uno `{path, label, kind}`:
   - `claude.default_model` (kind 'model'), `claude.max_parallel` (kind 'positiveInt')
   - `providers.${config.provider}.states.trigger` / `.review` / `.done` (kind 'nonEmpty') — path resuelto contra el provider ACTIVO (discreción: solo el activo, A3)
   - `server.idle_threshold_min` / `server.stuck_threshold_min` (kind 'positiveInt')
   - `cmux.colors.running` / `.done` / `.error` / `.review` (kind 'cmuxColor')
   NINGÚN descriptor de `api_key_env`, `base_url`, `workspace_slug` ni `provider` (D-11 — las keys viven solo en `~/.kodo/.env`).
8. `validateField(field, raw)`: switch sobre `field.kind` → validador correspondiente.

Crear `test/config-validate.test.js` (molde `test/config.test.js`, `node:test` + `node:assert/strict`) PRIMERO con la tabla RED, luego implementar hasta GREEN: casos válidos e inválidos por validador incluyendo límites (0, negativos, decimales, no-numérico, color en minúscula, hex, modelo fuera de set, string vacío y solo-espacios); aserción de que `getEditableFields(DEFAULT_CONFIG)` devuelve 11 entradas y que NINGÚN `path` contiene `api_key_env`/`base_url`/`workspace_slug` (blindaje PERSIST-04); aserción de que `setByPath` sobre un clon no muta una segunda referencia.
  </action>
  <verify>
    <automated>node --test test/config-validate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/config-validate.test.js` exits 0.
    - `src/config-validate.js` exporta `validatePositiveInt`, `validateModel`, `validateNonEmpty`, `validateCmuxColor`, `validateField`, `getByPath`, `setByPath`, `getEditableFields`, `MODELS`, `CMUX_COLORS`.
    - El test asierta que `getEditableFields(...)` no incluye ningún path con `api_key_env`, `base_url` ni `workspace_slug` (PERSIST-04/D-11).
    - Ningún validador lanza ante input arbitrario (never-throws / D-06).
  </acceptance_criteria>
  <done>Los validadores rechazan todo valor inválido por campo con mensaje en español y aceptan los válidos; el registro de campos editables está restringido a los 11 campos no-secretos (CFG-01..05, PERSIST-04, D-06/D-07/D-11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escritura atómica no-corruptiva en src/config.js (writeFileAtomic + saveConfig/saveProjects)</name>
  <files>src/config.js, test/config-atomic.test.js</files>
  <read_first>
    - `src/config.js` (líneas 6-8 KODO_DIR/CONFIG_PATH/PROJECTS_PATH cacheados al import; 136-140 `saveConfig`; 153-157 `saveProjects`; 117-119 `migrateConfigIfNeeded` writeFileSync directo — NO tocar la migración)
    - `.planning/phases/63-.../63-RESEARCH.md` (§Architecture Pattern 2 "Escritura atómica temp+rename"; Pitfall 4 cross-fs EXDEV; §Validation "Nota de aislamiento de tests")
    - `.planning/phases/63-.../63-VALIDATION.md` (Wave 0: `config-atomic.test.js` — helper recibe `path` por parámetro, DI puro)
  </read_first>
  <behavior>
    - writeFileAtomic(path, data): escribe a `path+'.tmp'` en el MISMO dir y luego `renameSync(tmp, path)`; si el write previo falla, `path` no se modifica.
    - saveConfig(cfg): produce EXACTAMENTE `JSON.stringify(cfg,null,2)+'\n'` (formato preservado, PERSIST-01).
    - Fallo simulado de escritura → el contenido previo de config.json queda intacto (PERSIST-05).
    - El `.tmp` se crea en el mismo directorio que el destino (nunca os.tmpdir() — evita EXDEV).
  </behavior>
  <action>
Introducir el helper interno `writeFileAtomic(path, data)` en `src/config.js` (D-08, §RESEARCH Pattern 2): escribe `data` a `path + '.tmp'` con `writeFileSync` y luego `renameSync(path+'.tmp', path)`. El `.tmp` SIEMPRE en el mismo directorio que el destino (rename atómico intra-filesystem; cross-fs lanza EXDEV — Pitfall 4). Recibe `path` como PARÁMETRO (DI puro) para que el test lo ejercite contra un tmpdir sin depender de `KODO_DIR` (que `config.js` cachea al import — fuga de aislamiento conocida, obs. 21811/22683). fsync omitido en v1 (A1 — rename solo basta para crash-safety de proceso; documentarlo en el JSDoc).

Refactorizar `saveConfig` y `saveProjects` (cambio quirúrgico): preservar firma y formato actuales (`JSON.stringify(..., null, 2) + '\n'`), solo sustituir el `writeFileSync(PATH, data)` por `writeFileAtomic(PATH, data)`. NO tocar `migrateConfigIfNeeded` ni `loadConfig`. NO exportar `writeFileAtomic` salvo que el test lo requiera (si se exporta, marcarlo como helper interno en el JSDoc).

Crear `test/config-atomic.test.js` PRIMERO en RED, luego GREEN: (a) `writeFileAtomic(tmpfile, data)` produce el contenido exacto; (b) formato byte-exacto de `saveConfig` preservado (escribir DEFAULT_CONFIG a un path de prueba vía el helper y comparar contra `JSON.stringify(DEFAULT_CONFIG,null,2)+'\n'`); (c) simular fallo de write (p.ej. path cuyo `.tmp` no se puede crear, o stub de fs) → el fichero destino previo queda intacto; (d) confirmar que el `.tmp` se crea junto al destino, no en otro filesystem. Usar un `mkdtempSync(join(tmpdir(),'kodo-atomic-'))` como dir de trabajo y pasar paths explícitos al helper (DI puro, sin tocar HOME).
  </action>
  <verify>
    <automated>node --test test/config-atomic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/config-atomic.test.js` exits 0.
    - `src/config.js` contiene `writeFileAtomic` y `saveConfig`/`saveProjects` lo usan (no quedan `writeFileSync(CONFIG_PATH...)`/`writeFileSync(PROJECTS_PATH...)` directos en esas dos funciones).
    - El test demuestra que, ante fallo de escritura, el fichero previo se preserva intacto (PERSIST-05).
    - `npm test` sigue verde (sin regresión en `test/config.test.js` ni en el resto de la suite).
  </acceptance_criteria>
  <done>saveConfig/saveProjects escriben de forma atómica temp+rename; el config.json/projects.json previo nunca queda a medias; el formato y la migración de schema se preservan (PERSIST-01/05, D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valor de buffer → validador | string crudo del operador entra a las funciones puras antes de tocar disco |
| objeto config → filesystem | `saveConfig` escribe `~/.kodo/config.json` (proceso del usuario, permisos del usuario) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Tampering | validateField / config-validate.js | high | mitigate | Validación pura pre-escritura: un valor inválido nunca alcanza saveConfig (CFG-05/D-07) |
| T-63-02 | Tampering/DoS | writeFileAtomic / config.js | high | mitigate | temp+rename atómico: original intacto si el write falla (PERSIST-05/D-08) |
| T-63-04 | Tampering/Injection | validateModel → claude --model | medium | mitigate | Set estricto {opus,sonnet,haiku} + el valor cruza por execFile argv literal (sin shell) en launch.js |
| T-63-05 | DoS | validatePositiveInt | low | mitigate | `Set.has` + `/^\d+$/` acotada sobre input corto; nunca regex compilada desde input (anti-ReDoS) |
| T-63-06 | Tampering | writeFileAtomic (.tmp placement) | medium | mitigate | `.tmp` en el mismo dir que el destino → rename atómico intra-fs; nunca os.tmpdir() (EXDEV) |
| T-63-SC | Tampering | npm installs | low | accept | No se instalan paquetes nuevos esta fase (D-01 in-house, package.json intacto) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/config-validate.test.js` y `node --test test/config-atomic.test.js` verdes.
- `npm test` verde (sin regresión).
- `src/config-validate.js` no importa `node:fs` ni ink ni picocolors (módulo puro — preserva color-isolation y 0-I/O).
</verification>

<success_criteria>
Los validadores cubren CFG-01..05 y rechazan todo inválido never-throws; la escritura es atómica y no-corruptiva (PERSIST-01/05); el registro de campos excluye los secretos por construcción (PERSIST-04). Todo verificado en aislamiento, sin la TUI.
</success_criteria>

<output>
Create `.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-01-SUMMARY.md` when done
</output>
