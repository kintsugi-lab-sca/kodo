---
phase: 63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co
plan: 03
type: execute
wave: 3
depends_on:
  - 63-02
files_modified:
  - src/cli/dashboard/index.js
autonomous: false
requirements:
  - PERSIST-02
  - PERSIST-04
  - UX-04
must_haves:
  truths:
    - "El editor escribe a ~/.kodo/config.json importando saveConfig directo en el proceso ink — sin endpoint nuevo en src/server.js (PERSIST-02, D-09)"
    - "index.js inyecta loadConfigFn/onSaveConfig en <App> espejo de onAdopt/onDerive (cableado DI)"
    - "onSaveConfig es never-throws: un fallo de escritura devuelve {ok:false,error} y el panel ink sigue montado (UX-04, D-12)"
    - "El dashboard real abre el editor con `e`, guarda un ajuste y muestra el aviso de reinicio (UX-01/PERSIST-03 end-to-end)"
  artifacts:
    - path: "src/cli/dashboard/index.js"
      provides: "cableado DI loadConfigFn/onSaveConfig en createElement(App,...) + wrapper never-throws sobre saveConfig"
      contains: "onSaveConfig"
  key_links:
    - from: "src/cli/dashboard/index.js"
      to: "src/config.js"
      via: "lazy import de loadConfig/saveConfig; saveConfig (ya atómico, Plan 01) escrito local"
      pattern: "loadConfig|saveConfig"
    - from: "src/cli/dashboard/index.js"
      to: "src/cli/dashboard/App.js"
      via: "createElement(App, { loadConfigFn, onSaveConfig, ... })"
      pattern: "loadConfigFn|onSaveConfig"
---

<objective>
Cablear el editor al proceso REAL del dashboard: `index.js` inyecta `loadConfigFn`/`onSaveConfig` en `<App>` (espejo de `onAdopt`/`onDerive`), importando `loadConfig`/`saveConfig` (ya atómico) DIRECTO en el proceso ink — sin shell-out, sin endpoint nuevo en `src/server.js` (D-09/PERSIST-02). Cierra el carril end-to-end y verifica con un checkpoint humano sobre el dashboard real (las dos verificaciones manual-only de 63-VALIDATION.md).

Purpose: el Plan 02 dejó el editor funcionando con DI fakes; este plan lo conecta a la config real y confirma que el invariante "cero endpoints desde v0.10" se preserva.
Output: `index.js` modificado + confirmación humana del flujo real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-CONTEXT.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-RESEARCH.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 03)

- **Cableado DI nuevo en `src/cli/dashboard/index.js`:** props `loadConfigFn` (= `() => loadConfig()`) y `onSaveConfig` (wrapper never-throws sobre `saveConfig`) pasadas a `createElement(App, {...})`.
- **NINGÚN cambio en `src/server.js`** (invariante "cero endpoints desde v0.10" — verificado por `git diff --quiet src/server.js`).

<tasks>

<task type="auto">
  <name>Task 1: Cableado DI de loadConfigFn/onSaveConfig en index.js</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - `src/cli/dashboard/index.js` (líneas 100-101 lazy import de loadConfig; 148-149 import de loadProjects + `const projects`; 166-198 `createElement(App, {...})` con onFocus/onOpen/onAdopt/onDerive/projects — MOLDE exacto de inyección DI)
    - `src/config.js` (saveConfig ya atómico tras Plan 01; loadConfig never-throws)
    - `.planning/phases/63-.../63-RESEARCH.md` (§Code Examples "Cableado DI en index.js")
    - `src/cli/dashboard/App.js` (props loadConfigFn/onSaveConfig añadidas en Plan 02 — confirmar nombres exactos)
  </read_first>
  <action>
Modificar `src/cli/dashboard/index.js` (cambio quirúrgico, espejo de `onAdopt`/`onDerive`):

1. Añadir `saveConfig` al lazy import existente de `../../config.js` (hoy importa `loadConfig` en línea 100 y `loadProjects` en 148; añadir `saveConfig` a uno de esos `await import('../../config.js')` — NO crear un import nuevo).
2. En `createElement(App, { ... })` (líneas 166-198), añadir dos props:
   - `loadConfigFn: () => loadConfig()` — App lo llama al pulsar `e` para tomar el snapshot (lo deep-clona internamente, Plan 02).
   - `onSaveConfig: async (cfg) => { try { saveConfig(cfg); return { ok: true }; } catch (e) { return { ok: false, error: String(e?.message ?? e) }; } }` — wrapper never-throws (D-12/UX-04). `saveConfig` ya es atómico (Plan 01) → escritura local no-corruptiva, directa al filesystem, SIN shell-out y SIN endpoint (D-09/PERSIST-02).
3. NO tocar `src/server.js`. NO añadir ningún `POST /config` (invariante "cero endpoints desde v0.10"). NO shell-out a `kodo config` (D-09: el dashboard ya corre en Node con acceso a `src/config.js`; importar `saveConfig` es más simple y determinista).

Documentar en comentario denso (estilo del repo) por qué es import directo y no shell-out (D-09: contraste con la tecla `a` de Phase 62 que sí shelleó `kodo adopt` por su lógica 0-token compleja; aquí `saveConfig` es función pura trivial).
  </action>
  <verify>
    <automated>node --test test/dashboard-config.test.js && git diff --quiet src/server.js && echo "server.js intacto (cero endpoints)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/index.js` contiene `loadConfigFn` y `onSaveConfig` en el `createElement(App, ...)`.
    - `onSaveConfig` envuelve `saveConfig` en try/catch y devuelve `{ok:...}` (never-throws, UX-04/D-12).
    - `git diff --quiet src/server.js` exits 0 (server.js sin cambios — PERSIST-02, invariante cero-endpoints).
    - `node --test test/dashboard-config.test.js` exits 0 (el contrato DI que index cablea coincide con el que App consume).
    - `npm test` verde completo.
  </acceptance_criteria>
  <done>index.js inyecta loadConfigFn/onSaveConfig en el dashboard real importando saveConfig directo (atómico, local, sin endpoint ni shell-out); server.js queda intacto (PERSIST-02, UX-04, D-09/D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — verificación humana del editor end-to-end</name>
  <action>Pausa para verificación humana del flujo real (no autónoma). El executor presenta los pasos de `<how-to-verify>` y espera la señal de reanudación; no hay cambios de código en este task.</action>
  <what-built>
El editor de ajustes comunes end-to-end: tecla `e` abre el overlay sobre el dashboard real, edición con cursor en ink, validación pre-escritura, guardado atómico a `~/.kodo/config.json` y aviso de reinicio. Cubre las dos verificaciones manual-only de 63-VALIDATION.md (render visual del cursor bajo terminal real; que reiniciar el daemon aplique el cambio).
  </what-built>
  <how-to-verify>
1. Arranca el server si no corre: `kodo server` (en otra terminal). Lanza el dashboard en una terminal REAL (TTY, no pipe): `kodo dashboard`.
2. Pulsa `e` → debe abrirse el overlay de config con la lista de campos y sus valores actuales, SIN salir del dashboard. Confirma que NO aparece ninguna API key / api_key_env / base_url (PERSIST-04).
3. Navega con ↑/↓ hasta `claude.max_parallel`, pulsa Enter → entra a edición con el valor precargado y el cursor visible. Teclea, usa backspace y ←/→ para editar a mitad de string (UX-02). Confirma que el cursor se ve (bloque invertido).
4. Deja un valor inválido (p.ej. `0` o `abc`) y pulsa Enter → footer rojo con el mensaje, el archivo NO se escribe, y puedes seguir editando sin "perder" la siguiente tecla (Pitfall 2).
5. Corrige a un válido (p.ej. `4`) y pulsa Enter → aparece el aviso de reinicio (footer verde/ámbar) y vuelves a la lista de campos.
6. Pulsa Esc para cerrar el overlay → vuelves a la tabla con la MISMA sesión seleccionada que antes de abrir (UX-03).
7. Verifica la persistencia: `cat ~/.kodo/config.json` → `claude.max_parallel` es `4` y el JSON está completo y bien formado (no a medias). Confirma que existe respaldo de no-corrupción: el fichero es válido.
8. (Manual-only PERSIST-03) Reinicia `kodo server` y confirma que el nuevo `max_parallel` se aplica (sin hot-reload — el aviso de reinicio era necesario).
  </how-to-verify>
  <resume-signal>Escribe "approved" si los 8 pasos pasan, o describe el primer paso que falle (incluye el frame/contenido observado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proceso ink → filesystem | onSaveConfig escribe ~/.kodo/config.json directo (sin red, sin server, sin shell) |
| dashboard ↔ server.js | el editor NO cruza este límite — escritura 100% local (invariante cero-endpoints) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-07 | Elevation of Privilege / Tampering | nuevo endpoint en server.js | high | mitigate | NO se añade endpoint: escritura local vía import directo de saveConfig (D-09); `git diff --quiet src/server.js` lo verifica (PERSIST-02) |
| T-63-10 | DoS | onSaveConfig wrapper | medium | mitigate | try/catch never-throws: un fallo de escritura devuelve {ok:false} y el panel ink sigue montado (UX-04/D-12) |
| T-63-03 | Information Disclosure | flujo real del overlay | high | mitigate | Checkpoint humano confirma que ninguna API key es visible/editable end-to-end (PERSIST-04) |
| T-63-SC | Tampering | npm installs | low | accept | Cero dependencias nuevas (D-01 in-house) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/dashboard-config.test.js` verde y `git diff --quiet src/server.js` (cero endpoints).
- `npm test` verde completo.
- Checkpoint humano: 8 pasos del flujo real aprobados (incluye las dos verificaciones manual-only de 63-VALIDATION.md).
</verification>

<success_criteria>
El editor de ajustes comunes funciona end-to-end sobre el dashboard real: abre con `e`, edita con cursor, valida, guarda atómico y local a `~/.kodo/config.json` sin endpoint nuevo, avisa de reiniciar, preserva la selección y nunca muestra API keys (PERSIST-02/03/04, UX-01..04; D-09/D-12).
</success_criteria>

<output>
Create `.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-03-SUMMARY.md` when done
</output>
