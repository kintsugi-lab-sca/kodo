---
phase: 63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co
plan: 02
type: execute
wave: 2
depends_on:
  - 63-01
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-config.test.js
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - PERSIST-03
  - PERSIST-04
must_haves:
  truths:
    - "El operador pulsa `e` en mode:'list' y se abre el overlay de config (mode:'config') sin salir del dashboard (UX-01, D-02)"
    - "El operador edita un valor con cursor, backspace/delete y ←/→ y confirma con Enter (UX-02, D-01)"
    - "Esc en mode:'config' cierra y vuelve a list con la MISMA sesión seleccionada por task_id (UX-03, D-05)"
    - "Un valor inválido pinta el footer rojo desde un estado dedicado (configEditError, NO focusError), NO escribe, y sigue en config-edit (CFG-05-UI, D-05/Pitfall 2)"
    - "Tras guardar con éxito aparece el aviso de reinicio (footer verde/ámbar) (PERSIST-03, D-10)"
    - "Ningún campo de API key / base_url / workspace_slug es visible ni editable en el overlay (PERSIST-04, D-11)"
    - "Una escritura fallida deja el panel ink montado (never-throws) y el footer rojo (UX-04, D-12)"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "modes config/config-edit, text-input buffer+cursor, handler `e`, validación/guardado, copy constants CONFIG_*"
      contains: "config-edit"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "render del overlay de config (lista de campos + valor read-only + cursor) y footer de validación/guardado"
      contains: "renderConfigOverlay"
    - path: "test/dashboard-config.test.js"
      provides: "cobertura integration: e abre, edición, inválido→rojo no-escribe, guardar→reinicio, Esc preserva selección, write-fail→panel montado"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/config-validate.js"
      via: "import getEditableFields/validateField/getByPath/setByPath del Plan 01"
      pattern: "getEditableFields|validateField"
    - from: "src/cli/dashboard/App.js"
      to: "onSaveConfig (prop DI)"
      via: "Enter de config-edit llama onSaveConfig(structuredClone aplicado)"
      pattern: "onSaveConfig"
    - from: "src/cli/dashboard/SessionTable.js"
      to: "src/cli/dashboard/App.js"
      via: "consume props mode/buffer/cursor/fieldCursor/configEditError/configSnapshot + constantes CONFIG_*"
      pattern: "buffer|configEditError"
---

<objective>
Construir el **editor UI** (la 2ª ruptura consciente de "TUI read-only"): la máquina de modos `config`/`config-edit` (D-03), el text-input controlado in-house con cursor visible (D-01/UX-02), el handler de la tecla `e` (D-02/UX-01), la navegación two-level (D-05), la validación derivada de un estado dedicado (Pitfall 2/D-05) y el guardado con aviso de reinicio (D-10/PERSIST-03), probado end-to-end con `test/dashboard-config.test.js`. Toda la edición opera sobre un `structuredClone` del snapshot congelado (Pitfall 1/D-04).

Purpose: es la pieza de UX genuinamente nueva del milestone (candidata `/gsd-ui-phase`); ensambla moldes ya maduros del dashboard (filter-mode, overlaySnapshot, adoptCursor, focusError/footerColor) con el único componente nuevo — el cursor de texto.
Output: `App.js` + `SessionTable.js` modificados y `test/dashboard-config.test.js`. El cableado del proceso real (index.js) lo hace el Plan 03; este plan opera con DI fakes (`loadConfigFn`/`onSaveConfig`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-CONTEXT.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-RESEARCH.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-VALIDATION.md
@src/config-validate.js
</context>

## Artifacts this phase produces (Plan 02)

- **Nuevos `mode`:** `'config'` (lista de campos navegable, valor read-only) y `'config-edit'` (editando con el text-input).
- **Nuevo handler de tecla:** `e` en la rama `mode === 'list'` de `useInput` (abre el overlay, snapshot vía `loadConfigFn`).
- **Nuevo estado en App.js:** `buffer` (string), `cursor` (number), `fieldCursor` (number), `configEditError` (string|null, estado DEDICADO — no focusError), `configSnapshot` (clon congelado).
- **Nuevas props DI en App:** `loadConfigFn` (lee el snapshot al abrir), `onSaveConfig` (escribe never-throws). Default fakes para tests.
- **Nuevas constantes de copy EXPORTADAS** en App.js (molde OVERLAY_*/DISMISS_*): `CONFIG_SAVED_RESTART`, `CONFIG_SAVE_FAILED`, `CONFIG_OVERLAY_TITLE` (y las etiquetas de campo si se centralizan).
- **Nueva función de render** en SessionTable.js: `renderConfigOverlay(...)`.
- **Test nuevo:** `test/dashboard-config.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Test RED de integración del editor (test/dashboard-config.test.js) — fija el contrato</name>
  <files>test/dashboard-config.test.js</files>
  <read_first>
    - `test/dashboard-overlay.test.js` (MOLDE: makeFakeClock/injectProps/render/stdin.write/lastFrame/flushTick + `unmount()` en finally; códigos de tecla `\r` Enter, `\x1b` Esc, `\x7f` backspace, `\x1b[D`/`\x1b[C` ←/→, `\x1b[A`/`\x1b[B` ↑/↓)
    - `.planning/phases/63-.../63-RESEARCH.md` (§Code Examples "Test hermético del overlay"; §Validation Per-Task map UX-01..04/CFG-05-UI/PERSIST-03)
    - `src/cli/dashboard/App.js` (props actuales de App líneas 274-290; render de SessionTable 1015-1039 — qué props se pasan)
  </read_first>
  <action>
Crear `test/dashboard-config.test.js` en RED (debe FALLAR: importa props/constantes/modos aún inexistentes). Reusar VERBATIM el harness de `test/dashboard-overlay.test.js` (makeFakeClock, injectProps, render, drain), extendiendo `injectProps` para inyectar `loadConfigFn` (devuelve un config fake con valores conocidos: `claude.max_parallel:3`, `claude.default_model:'opus'`, colores por defecto) y `onSaveConfig` (spy async never-throws que registra el config recibido y devuelve `{ok:true}`; en los casos de fallo devuelve `{ok:false,error}`). Importar de `../src/cli/dashboard/App.js` las constantes nuevas `CONFIG_SAVED_RESTART`, `CONFIG_SAVE_FAILED`, `CONFIG_OVERLAY_TITLE`.

Cubrir (cada test con `unmount()` en finally):
1. **UX-01/D-02:** tras `flushTick`, `stdin.write('e')` → `lastFrame()` contiene `CONFIG_OVERLAY_TITLE` y la lista de campos (al menos `default_model`, `max_parallel`).
2. **UX-02/D-01:** `'e'` → `'\r'` (entra a config-edit del primer campo, precarga valor) → teclear `'8'`, `'\x7f'` (backspace), `'\x1b[D'` (←), insertar char en medio → el frame refleja el contenido editado del buffer (asertar por CONTENIDO, no por bytes ANSI del cursor — Pitfall 5).
3. **CFG-05-UI:** editar `max_parallel` a un inválido (p.ej. `'0'`), `'\r'` → el frame muestra el mensaje de error de validación (rojo), `onSaveConfig` NO fue llamado, y el modo sigue siendo config-edit (otra tecla NO se pierde — Pitfall 2).
4. **PERSIST-03/D-10:** editar a un válido, `'\r'` → `onSaveConfig` llamado una vez con el valor nuevo aplicado; `lastFrame()` matchea `/reinicia|reiniciar/i` (CONFIG_SAVED_RESTART).
5. **UX-03/D-05:** estando en mode:'config', `'\x1b'` (Esc) → vuelve a la tabla y `selectedTaskId` se preserva (la misma fila sigue marcada — asertar contra el ref/fila previa).
6. **UX-04/D-12:** con `onSaveConfig` devolviendo `{ok:false}`, editar válido + `'\r'` → footer rojo (CONFIG_SAVE_FAILED), el panel sigue renderizando la tabla (no crash, no excepción), `lastFrame()` no vacío.

El test es el CONTRATO: bloquea los nombres de props DI (`loadConfigFn`/`onSaveConfig`), las constantes (`CONFIG_*`) y los modos. Tasks 2 y 3 lo llevan a GREEN.
  </action>
  <verify>
    <automated>node --test test/dashboard-config.test.js 2>&1 | grep -q "fail\|Error\|not ok" && echo "RED OK (esperado: el test falla antes de implementar)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-config.test.js` existe y, ejecutado ANTES de Tasks 2/3, FALLA (RED) por props/constantes/modos inexistentes.
    - El test reusa el harness de `test/dashboard-overlay.test.js` (render + stdin.write + lastFrame + unmount en finally).
    - El test inyecta `loadConfigFn` y `onSaveConfig` por DI (no toca `~/.kodo/` real).
    - Cubre los 6 comportamientos UX-01..04 / CFG-05-UI / PERSIST-03.
  </acceptance_criteria>
  <done>El contrato de interacción del editor está fijado como test RED ejecutable (Nyquist Wave 0 para la UI); Tasks 2/3 lo vuelven verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Máquina de modos + text-input + validación/guardado en App.js</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - `src/cli/dashboard/App.js` (líneas 274-290 props/destructuring; 300-366 useState incl. focusError/footerColor/overlaySnapshot/adoptCursor/selectedTaskId/mode; 489 `sel = resolveSelection`; 502-518 clear-on-any-input + orden de gating; 645-728 ramas confirm/filter — molde de inserción; 729-753 filter-mode = esqueleto del text-input; 755-763 rama list + handler `/`; 98-223 constantes de copy EXPORTADAS OVERLAY_*/DISMISS_*; 1008-1042 render de SessionTable + props)
    - `src/config-validate.js` (getEditableFields/validateField/getByPath/setByPath — Plan 01)
    - `.planning/phases/63-.../63-RESEARCH.md` (§Patterns 1/3/4; §Anti-Patterns; Pitfalls 1/2/3)
    - `.planning/milestones/v0.13-phases/62-.../62-CONTEXT.md` (cómo se integró el sub-modo deriving/confirm sin romper los existentes)
  </read_first>
  <behavior>
    - `e` en mode:'list' → `configSnapshot = structuredClone(loadConfigFn())`, `fieldCursor=0`, `setMode('config')` (Pitfall 1: deep-clone obligatorio).
    - mode:'config': ↑/↓ mueven fieldCursor con clamp [0, getEditableFields(snapshot).length-1] sin wrap; Enter precarga `getByPath(snapshot, field.path)` en buffer + cursor al final → mode:'config-edit'; Esc → mode:'list' SIN tocar selectedTaskId.
    - mode:'config-edit': char imprimible inserta en `cursor`; backspace/delete borra el char anterior a cursor; ←/→ mueven cursor con clamp; Esc → mode:'config' sin guardar; Enter → validateField; inválido → setConfigEditError + sigue en config-edit; válido → setByPath(structuredClone(snapshot), path, value) + await onSaveConfig + footer de reinicio + mode:'config'.
    - configEditError es estado DEDICADO (no focusError) → la siguiente tecla NO lo consume (Pitfall 2).
  </behavior>
  <action>
Modificar `src/cli/dashboard/App.js` (cambio quirúrgico, preservar todo lo existente):

1. **Props DI (D-09):** añadir `loadConfigFn` y `onSaveConfig` al destructuring de `App({...})` (líneas 274-290), con defaults inertes para tests del módulo (`loadConfigFn = () => DEFAULT_CONFIG-like`, `onSaveConfig = async () => ({ ok: true })`). Documentar en JSDoc espejo de `onAdopt`/`onDerive`.
2. **Estado nuevo (useState, junto a los existentes):** `buffer` (''), `cursor` (0), `fieldCursor` (0), `configEditError` (null — estado DEDICADO, Pitfall 2/D-05), `configSnapshot` (null — clon congelado, D-04).
3. **Constantes de copy EXPORTADAS** (molde líneas 98-223): `CONFIG_OVERLAY_TITLE` (título del overlay), `CONFIG_SAVED_RESTART` (aviso "reinicia server/daemon para aplicar", PERSIST-03/D-10), `CONFIG_SAVE_FAILED` (fallo de escritura, UX-04/D-12). Copy estable en español para que el test y SessionTable las importen.
4. **Handler `e`** en la rama `mode === 'list'` (junto a `c`/`l`/`p`, línea ~764): `setConfigSnapshot(structuredClone(loadConfigFn()))` (Pitfall 1 — deep-clone OBLIGATORIO: `loadConfig` sin fichero devuelve `{...DEFAULT_CONFIG}` con spread superficial), `setFieldCursor(0)`, `setConfigEditError(null)`, `setMode('config')`. NO tocar `selectedTaskId` (UX-03 gratis).
5. **Rama `mode === 'config'`** insertada ENTRE el bloque `confirm` (línea 728) y `filter` (729), espejo del orden D-03 ("antes del mode-gate de filtro"): Esc → `setMode('list')` (selectedTaskId intacto, UX-03); ↑/↓ → `setFieldCursor` clamp `Math.max(0, i-1)`/`Math.min(fields.length-1, i+1)` sin wrap (molde adoptCursor); Enter → `field = getEditableFields(configSnapshot)[fieldCursor]`, `setBuffer(String(getByPath(configSnapshot, field.path)))`, `setCursor(buffer.length)`, `setConfigEditError(null)`, `setMode('config-edit')`. Tragar el resto de teclas.
6. **Rama `mode === 'config-edit'`** (§RESEARCH Pattern 1): Esc → `setMode('config')` (cancela sin guardar, D-05); ←/→ → clamp cursor; backspace||delete → borra char anterior a cursor (tratar AMBOS juntos, Pitfall 3); char imprimible (`input && !key.ctrl && !key.meta`) → inserta en `cursor` (NO append ciego — Pattern 1); Enter → `res = validateField(field, buffer)`; si `!res.ok` → `setConfigEditError(res.error)` y `return` (sigue en config-edit, NO escribe — CFG-05/D-05); si ok → `next = structuredClone(configSnapshot)`, `setByPath(next, field.path, res.value)`, `await onSaveConfig(next)`; si el resultado es ok → `setConfigSnapshot(next)`, `setFocusError(CONFIG_SAVED_RESTART)` + `setFooterColor('yellow')` (footer transitorio AQUÍ sí focusError — transitorio y ya de vuelta en config, D-10), `setConfigEditError(null)`, `setMode('config')`; si falla → `setConfigEditError(CONFIG_SAVE_FAILED)` (config.json previo intacto, never-throws, UX-04/D-12). Envolver la llamada en try/catch defensivo aunque onSaveConfig sea never-throws.
7. **Render:** pasar a `SessionTable` (líneas 1015-1039) las props nuevas `buffer`, `cursor`, `fieldCursor`, `configEditError`, `configSnapshot`, y la lista `getEditableFields(configSnapshot)` (o que SessionTable la derive). Actualizar la línea de ayuda del footer (línea 1041) añadiendo `e config`.

NO añadir endpoint, NO shell-out, NO tocar el poll ni `resolveSelection`. El clear-on-any-input (línea 510) se conserva intacto — por eso el error de validación va en `configEditError`, no en `focusError` (Pitfall 2/4).
  </action>
  <verify>
    <automated>node --test test/dashboard-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `App.js` contiene `mode === 'config'` y `mode === 'config-edit'`.
    - `App.js` exporta `CONFIG_SAVED_RESTART`, `CONFIG_SAVE_FAILED`, `CONFIG_OVERLAY_TITLE`.
    - El handler `e` usa `structuredClone(loadConfigFn())` (deep-clone, Pitfall 1) — verificable: el frame de config no contamina aperturas posteriores.
    - El error de validación se almacena en `configEditError`, NO en `focusError` (Pitfall 2).
    - Tras este task, los tests de comportamiento de edición/validación/guardado de `test/dashboard-config.test.js` que no dependen del render del overlay pasan; el resto pasa tras Task 3.
    - `npm test` no regresiona los tests existentes de App.js (filter/overlay/select/dismiss/adopt).
  </acceptance_criteria>
  <done>La máquina de modos config/config-edit, el text-input con cursor y la validación/guardado están implementados en App.js sin romper los modos existentes; la edición opera sobre un deep-clone y el error de validación no se pierde (UX-01/02/03, CFG-05, PERSIST-03/04, UX-04; D-01..05/10/11/12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render del overlay de config + cursor + footer en SessionTable.js</name>
  <files>src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - `src/cli/dashboard/SessionTable.js` (líneas 27-43 import de constantes desde App.js; 62 COLS; 133-190 `renderOverlay`/`renderAdoptPicker` — molde de render de overlay; 309-336 firma del componente + early-return `if (mode === 'overlay')`; 367-418 footer + footerColor + confirm prompt derivado de mode)
    - `src/cli/dashboard/App.js` (constantes CONFIG_* exportadas por Task 2; props nuevas buffer/cursor/fieldCursor/configEditError/configSnapshot)
    - `src/config-validate.js` (getEditableFields — para la lista de campos y sus labels)
    - `.planning/phases/63-.../63-RESEARCH.md` (§Pattern 1 "Render del cursor"; Pitfall 5 ANSI del inverse)
  </read_first>
  <behavior>
    - mode:'config' → renderConfigOverlay muestra CONFIG_OVERLAY_TITLE + la lista de los 11 campos editables con su valor actual (read-only), con el campo en fieldCursor resaltado.
    - mode:'config-edit' → el campo seleccionado muestra el buffer con el char bajo cursor invertido (`<Text inverse>`); si cursor===buffer.length, bloque al final.
    - configEditError != null → footer rojo con el mensaje (derivado de mode==='config-edit', NO de focusError).
    - El overlay NUNCA muestra api_key_env/base_url/workspace_slug (la lista viene de getEditableFields, restringida por construcción — PERSIST-04).
    - Todo color vía props de `<Text>` (color-isolation; el cursor con `inverse`, no picocolors).
  </behavior>
  <action>
Modificar `src/cli/dashboard/SessionTable.js` (cambio quirúrgico):

1. **Imports:** añadir al import desde `./App.js` (líneas 27-43) las constantes `CONFIG_OVERLAY_TITLE`, `CONFIG_SAVED_RESTART`, `CONFIG_SAVE_FAILED`; importar `getEditableFields`/`getByPath` de `../../config-validate.js`.
2. **Props nuevas** en la firma del componente (líneas 309-336): `buffer`, `cursor`, `fieldCursor`, `configEditError`, `configSnapshot` (defaults seguros).
3. **`renderConfigOverlay(snapshot, fieldCursor, mode, buffer, cursor, configEditError)`** (molde `renderOverlay`/`renderAdoptPicker`): cabecera `CONFIG_OVERLAY_TITLE`; lista de `getEditableFields(snapshot)` — cada fila `label` + valor actual (`getByPath(snapshot, path)`) read-only; la fila en `fieldCursor` resaltada (bold/inverse vía `<Text>`); agrupación visual por sección (claude / states / server / cmux — discreción del planner). En `mode==='config-edit'`, la fila activa renderiza el text-input: `left = buffer.slice(0,cursor)`, `under = buffer[cursor] ?? ' '`, `right = buffer.slice(cursor+1)`, montando `h(Text,null, left, h(Text,{inverse:true}, under), right)` (Pattern 1 — cursor por `inverse`, color-isolation intacta). Si `configEditError != null`, una línea de footer en rojo con el mensaje (derivado de mode, Pitfall 2/4).
4. **Early-return:** añadir, junto al `if (mode === 'overlay' && overlaySnapshot)` (línea 337), un `if ((mode === 'config' || mode === 'config-edit') && configSnapshot) return renderConfigOverlay(...)`.
5. **Footer de guardado:** el aviso de reinicio tras guardar llega vía `focusError`/`footerColor` (ya manejado por el footer transitorio existente, líneas 367-373) — no requiere código nuevo aquí salvo confirmar que CONFIG_SAVED_RESTART se pinta verde/ámbar.

Asertar (vía el test GREEN del Task 1) por CONTENIDO de texto, no por bytes ANSI del cursor (Pitfall 5). NO usar picocolors (color-isolation — el cursor es `<Text inverse>`).
  </action>
  <verify>
    <automated>node --test test/dashboard-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-config.test.js` exits 0 (los 6 comportamientos en verde).
    - `SessionTable.js` contiene `renderConfigOverlay` y un early-return para `mode === 'config'`/`'config-edit'`.
    - El overlay renderiza solo los campos de `getEditableFields` (ningún campo de secreto visible — PERSIST-04).
    - El cursor se pinta con `<Text inverse>` (no picocolors); `npm test format-isolation` sigue verde (color-isolation intacta).
    - `npm test` verde completo.
  </acceptance_criteria>
  <done>El overlay de config renderiza la lista de campos editables con su valor, el cursor de texto visible y el footer de validación/guardado, sin exponer secretos y sin romper color-isolation (UX-01/02/04, PERSIST-04; D-01/11/12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teclado del operador → useInput | toda la entrada de edición cruza por `useInput` (única superficie de input de la TUI) |
| snapshot de config → render | el overlay muestra valores de config; las API keys NUNCA entran al overlay (lista restringida) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-03 | Information Disclosure | renderConfigOverlay / getEditableFields | high | mitigate | La lista de campos editables excluye api_key_env/base_url/workspace_slug por construcción (D-11); el overlay solo itera getEditableFields — keys nunca renderizadas (PERSIST-04) |
| T-63-08 | DoS | useInput config-edit (clear-on-any-input) | medium | mitigate | configEditError es estado dedicado (no focusError) → la edición no pierde teclas; never-throws mantiene el panel montado (UX-04/Pitfall 2) |
| T-63-09 | Tampering | structuredClone del snapshot | medium | mitigate | Deep-clone obligatorio antes de mutar: evita contaminar DEFAULT_CONFIG del módulo (Pitfall 1) |
| T-63-SC | Tampering | npm installs | low | accept | Cero dependencias nuevas (D-01 in-house) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/dashboard-config.test.js` verde (6 comportamientos UX-01..04/CFG-05-UI/PERSIST-03).
- `npm test` verde completo (sin regresión en filter/overlay/select/dismiss/adopt/format-isolation).
- Inspección: `App.js` no introduce import de picocolors; el cursor es `<Text inverse>`.
</verification>

<success_criteria>
El operador abre el editor con `e`, navega los campos, edita con cursor/backspace/←→, un inválido se rechaza con footer rojo sin escribir y sin perder teclas, un válido guarda y muestra el aviso de reinicio, Esc preserva la selección por task_id, y un fallo de escritura deja el panel montado — todo sin exponer API keys (UX-01..04, CFG-05, PERSIST-03/04, D-01..05/10/11/12).
</success_criteria>

<output>
Create `.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-02-SUMMARY.md` when done
</output>
