---
phase: 64
plan: 04
type: execute
wave: 4
depends_on:
  - 64-02
  - 64-03
files_modified:
  - src/cli/dashboard/index.js
autonomous: false
requirements:
  - PROJ-01
  - PROJ-04
  - PROJ-05
must_haves:
  truths:
    - "index.js inyecta listProjectsFn/listModulesFn/loadProjectsFn/saveProjectsFn en <App> espejo de onAdopt/onSaveConfig (cableado DI, D-08/PERSIST-02)"
    - "listProjectsFn es never-throws y devuelve discriminado {ok:true,projects}|{ok:false,error}, envolviendo la construcción del provider/PlaneClient (que LANZA sin API key) Y la llamada de red (PROJ-05, Pattern 2/Pitfall 1)"
    - "listModulesFn se wira CONDICIONAL: provider plane → new PlaneClient(...).listModules; github/otros → modules:[] (asimetría documentada: listModules solo en PlaneClient, no en el contrato TaskProvider)"
    - "loadProjectsFn/saveProjectsFn importan loadProjects/saveProjects de config.js DIRECTO en el proceso ink — sin endpoint nuevo en src/server.js (PERSIST-02, invariante cero-endpoints)"
    - "El dashboard real abre con `m`, lista en vivo, mapea/edita/quita/módulos, guarda atómico y avisa de reinicio; provider caído / sin key → projects-error + retry sin crashear ni corromper projects.json (end-to-end PROJ-01/04/05)"
  artifacts:
    - path: "src/cli/dashboard/index.js"
      provides: "cableado DI de los 4 *Fn en createElement(App,...): listProjectsFn (wrapper never-throws cubre construcción+red), listModulesFn (condicional plane/github), loadProjectsFn, saveProjectsFn"
      contains: "listProjectsFn"
  key_links:
    - from: "src/cli/dashboard/index.js"
      to: "src/providers/registry.js"
      via: "initRegistry()+getProvider(config.provider).listProjects() dentro del wrapper never-throws"
      pattern: "getProvider|initRegistry"
    - from: "src/cli/dashboard/index.js"
      to: "src/providers/plane/client.js"
      via: "new PlaneClient(...).listModules(projectId) (solo si provider==='plane')"
      pattern: "PlaneClient"
    - from: "src/cli/dashboard/index.js"
      to: "src/config.js"
      via: "lazy import de loadProjects/saveProjects (saveProjects ya atómico, Phase 63)"
      pattern: "loadProjects|saveProjects"
    - from: "src/cli/dashboard/index.js"
      to: "src/cli/dashboard/App.js"
      via: "createElement(App, { listProjectsFn, listModulesFn, loadProjectsFn, saveProjectsFn })"
      pattern: "listProjectsFn|saveProjectsFn"
---

<objective>
Cablear el editor de proyectos al proceso REAL del dashboard: `index.js` inyecta `listProjectsFn` (wrapper never-throws discriminado que cubre construcción del provider/PlaneClient + la llamada de red — PROJ-05/Pattern 2), `listModulesFn` (condicional por provider: plane via PlaneClient directo, github no-op — Pattern 4/asimetría), `loadProjectsFn`/`saveProjectsFn` (import directo de `config.js`, ya atómico) en `<App>`, espejo de `onAdopt`/`onSaveConfig`. Cierra el carril end-to-end sin endpoint nuevo en `server.js` (D-08/PERSIST-02) y verifica con un checkpoint humano sobre el dashboard real (las 4 verificaciones manual-only de 64-VALIDATION.md).

Purpose: los Planes 02/03 dejaron el editor funcionando con DI fakes; este plan lo conecta al provider real y a `projects.json`, materializando el carril de mayor riesgo del milestone (la única llamada de red de v0.14) con su degradación, y confirma que el invariante "cero endpoints desde v0.10" se preserva.
Output: `index.js` modificado + confirmación humana del flujo real. NO toca App.js/SessionTable.js (ya completos) ni server.js (invariante).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-CONTEXT.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 04)

- **Cableado DI nuevo en `src/cli/dashboard/index.js`:** 4 props pasadas a `createElement(App, {...})`:
  - `listProjectsFn` = wrapper never-throws: `try { initRegistry(); const provider = getProvider(providerName); const projects = await provider.listProjects(); return {ok:true,projects} } catch (e) { return {ok:false, error:String(e?.message ?? e)} }` (cubre construcción + red — Pitfall 1).
  - `listModulesFn` = condicional: si `providerName==='plane'` → wrapper que construye `new PlaneClient({baseUrl, apiKey: process.env[api_key_env], workspaceSlug})` y llama `.listModules(projectId)`, devolviendo `{ok:true,modules}|{ok:false,error}`; else → `async () => ({ ok:true, modules:[] })`.
  - `loadProjectsFn` = `() => loadProjects()`.
  - `saveProjectsFn` = `(m) => saveProjects(m)` (saveProjects síncrono y atómico — Phase 63).
- **`saveProjects` añadido al lazy import existente** de `../../config.js` (hoy importa `loadProjects` en ~línea 154; añadir `saveProjects`, NO crear import nuevo).
- **NINGÚN cambio en `src/server.js`** (invariante cero-endpoints — verificado por `git diff --quiet src/server.js`).

<tasks>

<task type="auto">
  <name>Task 1: Cableado DI de los 4 *Fn de proyectos en index.js</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - `src/cli/dashboard/index.js` (líneas 96-106 lazy import de loadConfig/saveConfig; 154-155 `loadProjects` + `const projects`; 172-219 `createElement(App, {...})` con onAdopt/onDerive/loadConfigFn/onSaveConfig — MOLDE exacto de inyección DI + wrapper never-throws de onSaveConfig)
    - `src/providers/registry.js` (initRegistry/getProvider ~96-125 — molde de obtención del provider, espejo de `cli.js:652`/`adopt.js:124`)
    - `src/providers/plane/client.js` (constructor LANZA sin apiKey ~13-15; `listModules` ~151-154; el provider plane lee config.providers.plane base_url/api_key_env/workspace_slug)
    - `src/providers/github/provider.js` (listProjects = config.repos, sin módulos ~224 — por qué listModulesFn es no-op para github)
    - `src/config.js` (loadProjects/saveProjects ~173-187 — saveProjects ya atómico vía writeFileAtomic, Phase 63)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Pattern 2 wrapper never-throws; §Pattern 4 listModules condicional; §Code Examples "Cableado DI en index.js"; Pitfall 1 PlaneClient constructor lanza; A4 discriminado vs fail-open)
    - `src/cli/dashboard/App.js` (props listProjectsFn/listModulesFn/loadProjectsFn/saveProjectsFn añadidas en Plan 02/03 — confirmar nombres EXACTOS antes de cablear)
  </read_first>
  <action>
Modificar `src/cli/dashboard/index.js` (cambio quirúrgico, espejo de `onSaveConfig`/`onAdopt`):

1. **Imports:** añadir `saveProjects` al lazy import existente de `loadProjects` (~línea 154: `const { loadProjects, saveProjects } = await import('../../config.js')`). Lazy import de `initRegistry`/`getProvider` desde `../../providers/registry.js` (espejo de `cli.js:652`). Resolver `const providerName = loadConfig().provider` (loadConfig ya importado/cacheado arriba).
2. **`listProjectsFn`** (RESEARCH Pattern 2 — wrapper never-throws DISCRIMINADO, A4): `async () => { try { await initRegistry(); const provider = getProvider(providerName); const projects = await provider.listProjects(); return { ok: true, projects }; } catch (e) { return { ok: false, error: String(e?.message ?? e) }; } }`. CRÍTICO: el try/catch DEBE cubrir la construcción del provider/PlaneClient (que LANZA sin API key — `client.js:13-15`, Pitfall 1) Y la llamada de red. NO fail-open a `[]` (no distinguiría 0-proyectos de error — PROJ-05).
3. **`listModulesFn`** (RESEARCH Pattern 4 — condicional por provider, asimetría `listModules` solo en PlaneClient): si `providerName === 'plane'` → leer `const planeCfg = loadConfig().providers.plane` y construir un wrapper never-throws que `import('../../providers/plane/client.js')`, `new PlaneClient({ baseUrl: planeCfg.base_url, apiKey: process.env[planeCfg.api_key_env], workspaceSlug: planeCfg.workspace_slug })` (constructor LANZA sin key → capturado), `await client.listModules(projectId)`, return `{ ok:true, modules }`; catch → `{ ok:false, error }`. Si `providerName !== 'plane'` → `async () => ({ ok: true, modules: [] })` (github sin módulos — `provider.js:224`). Documentar en comentario denso por qué es wiring condicional y NO provider-agnostic uniforme: `listModules` NO está en el contrato `TaskProvider` (`interface.js`), vive solo en `PlaneClient` (`client.js:151`); ampliar el contrato tocaría github y `getProvider` — excepción consciente (RESEARCH A1).
4. **`loadProjectsFn`/`saveProjectsFn`** en `createElement(App, {...})` (~172-219, junto a loadConfigFn/onSaveConfig): `loadProjectsFn: () => loadProjects()`, `saveProjectsFn: (m) => saveProjects(m)` (saveProjects síncrono y atómico — Phase 63 → escritura local no-corruptiva, directa al filesystem, SIN red/server/shell, PERSIST-01/05). Añadir también `listProjectsFn` y `listModulesFn` al mismo objeto de props.
5. NO tocar `src/server.js`. NO añadir ningún endpoint (invariante "cero endpoints desde v0.10"). NO shell-out — el dashboard ya corre en Node con acceso a `src/config.js`/`registry.js`; importar directo es más simple y determinista (D-08, espejo del editor de config de Phase 63).

Documentar en comentario denso (estilo del repo) la asimetría de `listModules` y por qué `listProjectsFn` envuelve construcción+red (Pitfall 1: un proyecto sin key crashearía el handler si solo se envolviera la llamada).
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js && git diff --quiet src/server.js && echo "server.js intacto (cero endpoints)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/index.js` contiene `listProjectsFn`, `listModulesFn`, `loadProjectsFn`, `saveProjectsFn` en el `createElement(App, ...)`.
    - `listProjectsFn` envuelve `initRegistry`+`getProvider`+`listProjects` en try/catch y devuelve discriminado `{ok}` (never-throws, cubre construcción+red — PROJ-05/Pitfall 1).
    - `listModulesFn` es condicional: plane construye PlaneClient y llama listModules; github/otros devuelven `modules:[]`.
    - `saveProjectsFn`/`loadProjectsFn` importan `saveProjects`/`loadProjects` de `config.js` (sin endpoint, sin shell-out).
    - `git diff --quiet src/server.js` exits 0 (server.js sin cambios — PERSIST-02, invariante cero-endpoints).
    - `node --test test/dashboard-projects.test.js` exits 0 (el contrato DI que index cablea coincide con el que App consume) y `npm test` verde completo.
  </acceptance_criteria>
  <done>index.js inyecta los 4 *Fn del editor de proyectos en el dashboard real: listProjectsFn never-throws (construcción+red), listModulesFn condicional plane/github, load/saveProjects directo (atómico, local, sin endpoint ni shell-out); server.js intacto (PROJ-01/04/05, PERSIST-02, D-08).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — verificación humana del editor de proyectos end-to-end</name>
  <action>Pausa para verificación humana del flujo real (no autónoma). El executor presenta los pasos de `<how-to-verify>` y espera la señal de reanudación; no hay cambios de código en este task.</action>
  <read_first>
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md` (§Manual-Only Verifications — las 4 verificaciones que requieren el dashboard/provider real)
  </read_first>
  <what-built>
El editor de proyectos end-to-end: tecla `m` abre el overlay sobre el dashboard real, lista en vivo del provider con estado de mapeo, edición de ruta con cursor en ink validada (debe existir), quitar mapeo, sub-overlay de módulos, persistencia atómica a `~/.kodo/projects.json` preservando la forma dual, aviso de reinicio, y degradación con gracia si el provider cae. Cubre las 4 verificaciones manual-only de 64-VALIDATION.md (lista real del provider; degradación real con provider caído; aviso de reinicio efectivo; render visual del cursor/sub-overlay en terminal real).
  </what-built>
  <how-to-verify>
1. Arranca el server si no corre: `kodo server` (otra terminal). Lanza el dashboard en una terminal REAL (TTY, no pipe): `kodo dashboard`.
2. **(PROJ-01)** Pulsa `m` → debe abrirse el overlay de proyectos: un breve "cargando", luego la lista REAL de proyectos del provider (Plane/GitHub) con su estado de mapeo (`[ruta]` o `[sin mapear]`), SIN salir del dashboard. Confirma que NO aparece ninguna API key / api_key_env / base_url (PERSIST-04).
3. **(PROJ-02)** Navega con ↑/↓ a un proyecto sin mapear, pulsa Enter → entra a edición con el cursor visible. Teclea una ruta que EXISTE (un directorio real), usa backspace y ←/→ a mitad de string, pulsa Enter → aviso de reinicio (footer) y vuelves a la lista; la fila ahora muestra la ruta.
4. **(PROJ-02 inválido)** Edita una ruta a algo que NO existe y pulsa Enter → footer rojo, el archivo NO se escribe, sigues editando sin "perder" la siguiente tecla.
5. **(PROJ-03)** Sobre una fila mapeada pulsa la tecla de quitar (`x`) → la fila vuelve a `[sin mapear]` + aviso de reinicio.
6. **(PROJ-04)** Sobre un proyecto de Plane pulsa la tecla de módulos (`m`) → sub-overlay de módulos; mapea la ruta de un módulo a un dir real → se guarda. (Si el provider es GitHub o el proyecto no tiene módulos → footer "este provider no tiene módulos", sin crash.)
7. **Persistencia:** `cat ~/.kodo/projects.json` → la entrada editada tiene la forma correcta (string o `{default, modules}` con el módulo mapeado), el JSON está completo y bien formado (no a medias). Confirma que las entradas de OTROS proyectos no se tocaron.
8. **(PROJ-05 degradación)** Provoca un fallo del provider: detén el acceso (sin conexión) o invalida temporalmente la API key, relanza el dashboard y pulsa `m` → debe aparecer `projects-error` (panel rojo) con la opción de reintentar (`r`) o salir (`Esc`); el dashboard NO crashea (panel ink montado) y `~/.kodo/projects.json` queda INTACTO (no se escribió nada en el carril de error).
9. **(PERSIST-03 manual-only)** Reinicia `kodo server` y confirma que el nuevo mapeo se aplica (sin hot-reload — el aviso de reinicio era necesario).
  </how-to-verify>
  <resume-signal>Escribe "approved" si los 9 pasos pasan, o describe el primer paso que falle (incluye el frame/contenido observado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proceso ink → provider (red) | listProjectsFn/listModulesFn hacen fetch HTTPS al Plane API (X-API-Key, AbortSignal.timeout) — único cruce de red de v0.14 |
| proceso ink → filesystem | saveProjectsFn escribe ~/.kodo/projects.json directo (sin red, sin server, sin shell) |
| dashboard ↔ server.js | el editor NO cruza este límite — escritura 100% local (invariante cero-endpoints) |
| API key del provider → construcción del cliente | la key se LEE de process.env[api_key_env] para construir PlaneClient; NUNCA se renderiza ni edita |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-14 | DoS | listProjectsFn / listModulesFn wrappers | high | mitigate | try/catch cubre construcción (PlaneClient lanza sin key — Pitfall 1) + red; un fallo devuelve {ok:false} y el panel ink sigue montado (PROJ-05) |
| T-64-15 | Elevation of Privilege / Tampering | nuevo endpoint en server.js | high | mitigate | NO se añade endpoint: lectura vía provider, escritura vía import directo de saveProjects (D-08); `git diff --quiet src/server.js` lo verifica (PERSIST-02) |
| T-64-16 | Information Disclosure | API key en la construcción del cliente | high | mitigate | La key se lee de process.env[api_key_env] solo para construir PlaneClient; jamás se pasa al snapshot, render ni se escribe (PERSIST-04); checkpoint humano confirma que no es visible |
| T-64-17 | Tampering | saveProjectsFn (escritura no-corruptiva) | high | mitigate | saveProjects ya atómico (writeFileAtomic, Phase 63); el carril de error de fetch ni siquiera escribe → projects.json intacto (PERSIST-05/PROJ-05) |
| T-64-SC | Tampering | npm installs | low | accept | Cero dependencias nuevas (D-09 — package.json intacto); sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/dashboard-projects.test.js` verde y `git diff --quiet src/server.js` (cero endpoints).
- `npm test` verde completo.
- Checkpoint humano: 9 pasos del flujo real aprobados (incluye las 4 verificaciones manual-only de 64-VALIDATION.md: lista real, degradación real, aviso de reinicio, render del cursor/módulos).
</verification>

<success_criteria>
El editor de proyectos funciona end-to-end sobre el dashboard real: abre con `m`, lista en vivo del provider, edita/quita/mapea-módulos con validación, guarda atómico y local a `~/.kodo/projects.json` sin endpoint nuevo, avisa de reiniciar, preserva la forma dual y nunca muestra API keys; un provider caído degrada a projects-error con retry sin crash ni corrupción (PROJ-01/04/05, PERSIST-02/03/04/05; D-08).
</success_criteria>

<output>
Create `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-04-SUMMARY.md` when done
</output>
