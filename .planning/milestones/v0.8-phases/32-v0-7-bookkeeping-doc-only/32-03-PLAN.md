---
phase: 32-v0-7-bookkeeping-doc-only
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.7-phases/23-githubclient-auth-foundation/23-VALIDATION.md
  - .planning/milestones/v0.7-phases/25-polling-trigger-channel/25-VALIDATION.md
  - .planning/milestones/v0.7-phases/26-config-wizard-cli-integration/26-VALIDATION.md
  - .planning/milestones/v0.7-phases/27-cross-provider-contract-matrix/27-VALIDATION.md
requirements:
  - BOOK-03
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Las 4 VALIDATION.md de phases 23, 25, 26, 27 contienen `nyquist_compliant: true` en YAML frontmatter"
    - "Phase 24 (única phase v0.7 que ya tenía true) NO se toca — preservada byte-identical"
    - "Total v0.7 con nyquist_compliant: true = 5/5 phases tras el toggle"
    - "Resto del YAML frontmatter de cada VALIDATION.md preservado (phase, slug, status, wave_0_complete, created)"
    - "Cero cambios al cuerpo Markdown del file (tabla per-task verification map, wave 0 requirements, etc.)"
    - "D-06: scope fijo a BOOK-03 — exclusivamente el toggle del flag `nyquist_compliant` en 4 archivos. NO tocar `status: draft`, NO tocar `wave_0_complete: false`, NO modificar Phase 24, NO refactorizar VALIDATION.md templates."
  artifacts:
    - path: ".planning/milestones/v0.7-phases/23-githubclient-auth-foundation/23-VALIDATION.md"
      provides: "Phase 23 nyquist sign-off toggled"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.7-phases/25-polling-trigger-channel/25-VALIDATION.md"
      provides: "Phase 25 nyquist sign-off toggled"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.7-phases/26-config-wizard-cli-integration/26-VALIDATION.md"
      provides: "Phase 26 nyquist sign-off toggled"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.7-phases/27-cross-provider-contract-matrix/27-VALIDATION.md"
      provides: "Phase 27 nyquist sign-off toggled"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "23/25/26/27-VALIDATION.md frontmatter"
      to: "v0.7-MILESTONE-AUDIT.md §Bookkeeping Drift item #3 + §Nyquist Compliance overall"
      via: "1-line YAML toggle per file × 4 files"
      pattern: "nyquist_compliant: true"
    - from: "24-VALIDATION.md (template reference, NOT modified)"
      to: "23/25/26/27-VALIDATION.md (4 toggled to match)"
      via: "format mirror"
      pattern: "nyquist_compliant: true"
---

<objective>
Toggle `nyquist_compliant: false` → `nyquist_compliant: true` en el YAML frontmatter de las 4 VALIDATION.md de phases v0.7 que quedaron en `false` (23, 25, 26, 27). Phase 24 ya tiene `true` y queda intacta como referencia/template. Drift identificado en `v0.7-MILESTONE-AUDIT.md` §Bookkeeping Drift item #3 + §Nyquist Compliance — "VALIDATION.md files completos y tests verdes — bookkeeping drift sin impacto funcional".

Purpose: Cerrar BOOK-03 del audit v0.7 — alinear el sign-off flag con la realidad funcional (tests verdes + VALIDATION.md completos ya). Eleva "Nyquist Overall: partial (1/5)" → "compliant (5/5)" en el milestone metadata.
Output: 4 archivos VALIDATION.md con el flag YAML toggled. Body markdown intacto en los 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-MILESTONE-AUDIT.md
@.planning/milestones/v0.7-phases/24-githubprovider-normalizer-registry/24-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle nyquist_compliant: false → true en 4 VALIDATION.md (phases 23, 25, 26, 27)</name>
  <files>
    .planning/milestones/v0.7-phases/23-githubclient-auth-foundation/23-VALIDATION.md,
    .planning/milestones/v0.7-phases/25-polling-trigger-channel/25-VALIDATION.md,
    .planning/milestones/v0.7-phases/26-config-wizard-cli-integration/26-VALIDATION.md,
    .planning/milestones/v0.7-phases/27-cross-provider-contract-matrix/27-VALIDATION.md
  </files>
  <read_first>
    - .planning/milestones/v0.7-phases/23-githubclient-auth-foundation/23-VALIDATION.md (target #1 — verificar estado actual frontmatter; línea ~5: `nyquist_compliant: false`)
    - .planning/milestones/v0.7-phases/25-polling-trigger-channel/25-VALIDATION.md (target #2 — verificar línea ~5: `nyquist_compliant: false`)
    - .planning/milestones/v0.7-phases/26-config-wizard-cli-integration/26-VALIDATION.md (target #3 — verificar línea ~5: `nyquist_compliant: false`)
    - .planning/milestones/v0.7-phases/27-cross-provider-contract-matrix/27-VALIDATION.md (target #4 — verificar línea ~5: `nyquist_compliant: false`)
    - .planning/milestones/v0.7-phases/24-githubprovider-normalizer-registry/24-VALIDATION.md (template/reference — línea ~5 muestra el formato canónico `nyquist_compliant: true`; NO modificar este archivo, solo leer como referencia formal)
    - .planning/v0.7-MILESTONE-AUDIT.md (justificación: §Nyquist Compliance + §Bookkeeping Drift item #3 — "VALIDATION.md files completos y tests verdes — bookkeeping drift sin impacto funcional")
  </read_first>
  <action>
    Edición quirúrgica de UNA LÍNEA en cada uno de los 4 archivos target. Usar el tool Edit (string replacement) con el patrón exacto `nyquist_compliant: false` → `nyquist_compliant: true`. La línea está en el YAML frontmatter top-of-file (línea ~5 en los 4 archivos verificados).

    Los 4 paths exactos a editar:
    1. `.planning/milestones/v0.7-phases/23-githubclient-auth-foundation/23-VALIDATION.md`
    2. `.planning/milestones/v0.7-phases/25-polling-trigger-channel/25-VALIDATION.md`
    3. `.planning/milestones/v0.7-phases/26-config-wizard-cli-integration/26-VALIDATION.md`
    4. `.planning/milestones/v0.7-phases/27-cross-provider-contract-matrix/27-VALIDATION.md`

    Operación exacta por archivo:
    - Locate la línea YAML `nyquist_compliant: false` (única ocurrencia en cada archivo per grep — verificar con `grep -c "nyquist_compliant" <file>` retorna 1 en los 4).
    - Replace con `nyquist_compliant: true`.

    NO tocar:
    - Phase 24 VALIDATION.md (ya tiene `nyquist_compliant: true` — preservada byte-identical per D-06 scope-fijo).
    - Otros campos del YAML frontmatter de las 4 phases (`phase`, `slug`, `status: draft`, `wave_0_complete`, `created`).
    - El body markdown de los 4 archivos (verification map tables, Wave 0 sections, Manual-Only Verifications, Sign-Off checklists).
    - Cualquier otro archivo en `.planning/milestones/v0.7-phases/*/` (no SUMMARY, no CONTEXT, no PLAN, no RESEARCH, no PATTERNS).
    - Cualquier archivo en `src/`, `test/`, `bin/`.

    NOTA sobre el campo `status: draft` en los 4 archivos: NO cambiar status. Per D-06 scope-fijo a BOOK-03 que es exclusivamente el toggle de `nyquist_compliant`. Si emergiera un argumento para subir status también, eso es nuevo drift → `<deferred>`, no se mete en Phase 32.

    NOTA sobre el campo `wave_0_complete: false` en los 4 archivos: NO cambiar. Idem D-06 — scope-fijo.

    Comando único pre-verification (antes de editar):
    ```
    grep -l "nyquist_compliant: false" .planning/milestones/v0.7-phases/*/VALIDATION.md
    ```
    Esto debe listar exactamente los 4 archivos (no incluir Phase 24 que ya tiene `true`). NOTA importante: el glob `*VALIDATION.md` matchea filenames variables — los reales son `23-VALIDATION.md`, `24-VALIDATION.md`, etc. (con prefijo phase número). El glob `*/VALIDATION.md` NO matchea. Usar `*/[0-9]*-VALIDATION.md` o iterar explícitamente los 4 paths listados arriba.

    Post-edit verification command:
    ```
    grep -l "nyquist_compliant: true" .planning/milestones/v0.7-phases/23-*/23-VALIDATION.md .planning/milestones/v0.7-phases/24-*/24-VALIDATION.md .planning/milestones/v0.7-phases/25-*/25-VALIDATION.md .planning/milestones/v0.7-phases/26-*/26-VALIDATION.md .planning/milestones/v0.7-phases/27-*/27-VALIDATION.md
    ```
    Esto debe listar los 5 archivos (las 5 phases v0.7) tras el toggle.

    Commit strategy: 1 commit cubriendo los 4 archivos juntos (per D-05: "1 commit por BOOK-item"). Convención: `docs(32-03): close BOOK-03 — toggle nyquist_compliant: true en 4 v0.7 VALIDATION.md (23/25/26/27)`.
  </action>
  <verify>
    <automated>
      # Post-edit: exactamente 5 VALIDATION.md v0.7 con nyquist_compliant: true (24 ya tenía + 23/25/26/27 nuevos)
      test "$(grep -l 'nyquist_compliant: true' .planning/milestones/v0.7-phases/23-*/23-VALIDATION.md .planning/milestones/v0.7-phases/24-*/24-VALIDATION.md .planning/milestones/v0.7-phases/25-*/25-VALIDATION.md .planning/milestones/v0.7-phases/26-*/26-VALIDATION.md .planning/milestones/v0.7-phases/27-*/27-VALIDATION.md 2>/dev/null | wc -l | tr -d ' ')" = "5"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "nyquist_compliant: true" .planning/milestones/v0.7-phases/23-*/23-VALIDATION.md .planning/milestones/v0.7-phases/24-*/24-VALIDATION.md .planning/milestones/v0.7-phases/25-*/25-VALIDATION.md .planning/milestones/v0.7-phases/26-*/26-VALIDATION.md .planning/milestones/v0.7-phases/27-*/27-VALIDATION.md | wc -l` retorna 5 (las 5 phases v0.7 con flag toggled).
    - `grep -l "nyquist_compliant: false" .planning/milestones/v0.7-phases/23-*/23-VALIDATION.md .planning/milestones/v0.7-phases/25-*/25-VALIDATION.md .planning/milestones/v0.7-phases/26-*/26-VALIDATION.md .planning/milestones/v0.7-phases/27-*/27-VALIDATION.md | wc -l` retorna 0 (las 4 phases target con flag flipped).
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.7-phases/23-githubclient-auth-foundation/23-VALIDATION.md` retorna 1.
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.7-phases/25-polling-trigger-channel/25-VALIDATION.md` retorna 1.
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.7-phases/26-config-wizard-cli-integration/26-VALIDATION.md` retorna 1.
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.7-phases/27-cross-provider-contract-matrix/27-VALIDATION.md` retorna 1.
    - `git diff --stat .planning/milestones/v0.7-phases/24-githubprovider-normalizer-registry/24-VALIDATION.md` retorna vacío (Phase 24 NO tocada — preservada byte-identical per D-06).
    - `git diff --stat .planning/milestones/v0.7-phases/*/` muestra cambios SOLO en los 4 archivos target (`23-VALIDATION.md`, `25-VALIDATION.md`, `26-VALIDATION.md`, `27-VALIDATION.md`) y NO en SUMMARYs, CONTEXTs, PLANs, RESEARCHs, PATTERNs.
    - Cada uno de los 4 archivos modificados tiene exactly 1 línea de diff (el toggle YAML), verificable con `git diff <file> | grep -E "^[+-]" | grep -v "^[+-][+-][+-]" | wc -l` retorna 2 (la línea `-` removida y la `+` añadida).
    - `git diff -- src/ test/ bin/` retorna vacío (Tier 1 doc-only invariant).
    - El campo `status: draft` permanece intacto en los 4 archivos (`grep -c "^status: draft" .planning/milestones/v0.7-phases/23-*/23-VALIDATION.md` retorna 1, y similar para 25/26/27 — NO se mete `status: passed` per D-06).
    - El campo `wave_0_complete: false` permanece intacto en los 4 archivos (idem — NO se toca per D-06).
  </acceptance_criteria>
  <done>
    Las 4 VALIDATION.md de phases 23/25/26/27 tienen `nyquist_compliant: true` en su YAML frontmatter. Phase 24 preservada byte-identical. Body markdown intacto en los 4. Total v0.7: 5/5 phases con nyquist_compliant: true tras el toggle. Cero cambios fuera de los 4 archivos target. Cero código tocado. Tier 1 — fast-forward a main local sin PR (per D-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No code paths modified — no security surface change. Doc-only edits to 4 planning artifact files (1-line YAML toggle each). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-N/A | Spoofing | N/A | N/A | Doc-only edit. ASVS L1 N/A — no auth flows. |
| T-32-03-N/A | Tampering | N/A | N/A | Edit applies only to one YAML key per file (`nyquist_compliant`). Git history preserves all prior states. ASVS L1 N/A. |
| T-32-03-N/A | Repudiation | N/A | N/A | Single doc-only commit signed by user identity. ASVS L1 N/A. |
| T-32-03-N/A | Information Disclosure | N/A | N/A | No secrets, no PII; flag is a public sign-off bit. ASVS L1 N/A. |
| T-32-03-N/A | Denial of Service | N/A | N/A | No runtime change. ASVS L1 N/A. |
| T-32-03-N/A | Elevation of Privilege | N/A | N/A | No authorization logic. ASVS L1 N/A. |

**Justificación general:** Doc-only — no code paths modified, no security surface change. ASVS L1 N/A — phase modifica únicamente una línea YAML (`nyquist_compliant: false` → `true`) en 4 archivos markdown del milestone v0.7 archive.
</threat_model>

<verification>
- Suite global ≥830 pass (sin cambio numérico vs Phase 31 — no se ejecutan tests por D-04).
- `git diff <phase-base>..<phase-head> -- src/ test/ bin/` retorna vacío (Tier 1 doc-only invariant).
- BOOK-03 success criterion satisfied: 5 archivos VALIDATION.md v0.7 con flag `nyquist_compliant: true` (success criterion #3 del phase goal).
- Phase 24 VALIDATION.md preservada (byte-identical pre/post per D-06 scope-fijo).
</verification>

<success_criteria>
1. `grep -l "nyquist_compliant: true" .planning/milestones/v0.7-phases/{23,24,25,26,27}-*/[0-9]*-VALIDATION.md` lista los 5 archivos.
2. `grep -l "nyquist_compliant: false" .planning/milestones/v0.7-phases/{23,25,26,27}-*/[0-9]*-VALIDATION.md` lista 0 archivos.
3. `git diff --stat .planning/milestones/v0.7-phases/24-githubprovider-normalizer-registry/24-VALIDATION.md` retorna vacío.
4. Cada uno de los 4 archivos modificados tiene exactly 1 línea de diff funcional (toggle YAML).
5. Cero diff en `src/`, `test/`, `bin/`.
</success_criteria>

<output>
After completion, create `.planning/phases/32-v0-7-bookkeeping-doc-only/32-03-SUMMARY.md` siguiendo el template estándar. Citar `v0.7-MILESTONE-AUDIT.md §Bookkeeping Drift item #3 + §Nyquist Compliance` como source-of-truth. Commit convention: `docs(32-03): close BOOK-03 — toggle nyquist_compliant: true en 4 v0.7 VALIDATION.md (23/25/26/27)`. Tier 1 — fast-forward a main local sin PR (per D-07).
</output>
