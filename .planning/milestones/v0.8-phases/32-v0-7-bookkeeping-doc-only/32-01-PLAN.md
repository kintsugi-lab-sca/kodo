---
phase: 32-v0-7-bookkeeping-doc-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.7-REQUIREMENTS.md
requirements:
  - BOOK-01
autonomous: true
user_setup: []

must_haves:
  truths:
    - "v0.7-REQUIREMENTS.md traceability table tiene 16/16 IDs marcados Complete (post-state, invariante del cierre BOOK-01)"
    - "Cero IDs `pending` quedan en la traceability table"
    - "Los 8 IDs cubiertos por la reconciliación documental del audit son exactamente: GH-01, GH-02, GH-03, GH-04, GH-05, CFG-01, CFG-02, TEST-01 (per v0.7-MILESTONE-AUDIT.md §Bookkeeping Drift item #1)"
    - "Tabla preserva su estructura (3 columnas REQ-ID | Phase | Status) y el resto de los 8 IDs ya-Complete intactos"
    - "Validación idempotente: BOOK-01 acepta dos branches válidos — (A) tabla ya reconciliada al inicio (0 pending) → commit funcional no-op, solo SUMMARY; (B) drift remanente (≥1 pending) → reconcile inline + commit funcional + SUMMARY. Acceptance criteria idénticos en ambos casos (post-state 16/16)."
    - "D-06: scope fijo a BOOK-01 — cero ediciones fuera de la traceability table de v0.7-REQUIREMENTS.md (no auditar otras phases, no refactorizar la tabla, no tocar prosa adyacente)."
  artifacts:
    - path: ".planning/milestones/v0.7-REQUIREMENTS.md"
      provides: "Traceability table v0.7 con 16/16 Complete"
      contains: "| GH-01    | 23       | Complete |"
  key_links:
    - from: "v0.7-REQUIREMENTS.md traceability table"
      to: "v0.7-MILESTONE-AUDIT.md §Bookkeeping Drift item #1"
      via: "doc-only reconciliation / validation (idempotent)"
      pattern: "Complete"
---

<objective>
Validar idempotentemente que la traceability table de `.planning/milestones/v0.7-REQUIREMENTS.md` está reconciliada (16/16 IDs `Complete`, 0 `pending`) — drift identificado en `v0.7-MILESTONE-AUDIT.md` §Bookkeeping Drift item #1. Wire-up funcional ya verificado empíricamente en el integration check del audit (16/16 wires WIRED, 5/5 phases complete, 777 tests pass). Si por drift posterior aparecen IDs `pending` en la tabla al momento de ejecución, reconciliar inline a `Complete`. Caso happy actual (verificado 2026-05-21): tabla ya muestra 16 Complete / 0 pending entre audit (2026-05-14) y planning (2026-05-21) — BOOK-01 se cierra como validación retro-defensiva con commit solo del SUMMARY. Cero código tocado en ambos branches.

Purpose: Cerrar BOOK-01 del audit v0.7 — alinear el sign-off documental con la realidad funcional ya validada, y dejar registro defensivo de que la tabla cumple el invariante 16/16 al cierre del milestone v0.8.
Output: `.planning/milestones/v0.7-REQUIREMENTS.md` con 16/16 IDs `Complete` en la traceability table (post-state garantizado independiente del branch ejecutado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-MILESTONE-AUDIT.md
@.planning/milestones/v0.7-REQUIREMENTS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validar idempotentemente la traceability table v0.7-REQUIREMENTS.md (16/16 Complete, 0 pending); reconcile inline si drift remanente</name>
  <files>.planning/milestones/v0.7-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v0.7-REQUIREMENTS.md (ver el estado actual de la tabla — líneas ~88-105; determinar branch A vs B)
    - .planning/v0.7-MILESTONE-AUDIT.md (source of truth — §Requirements Coverage 16/16 tabla muestra todos los wires WIRED; §Bookkeeping Drift enumera los 8 IDs exactos cubiertos por la reconciliación documental: GH-01..05, CFG-01, CFG-02, TEST-01)
  </read_first>
  <action>
    **Step 1 — Branch detection (mandatory pre-edit grep):**

    Ejecutar `grep -c "| pending |" .planning/milestones/v0.7-REQUIREMENTS.md` ANTES de cualquier edit.

    - **Caso A (happy path — validación retro-defensiva):** retorno `0`. La tabla ya está reconciliada (estado verificado 2026-05-21: drift cerrado en algún commit entre el audit del 2026-05-14 y este plan). **NO ejecutar edits al archivo. NO crear commit funcional.** Único commit del BOOK-item = el del SUMMARY.md al cierre (`docs(32-01): close BOOK-01 SUMMARY — validate v0.7 REQUIREMENTS traceability already reconciled (16/16, 0 pending)`). Documentar en el SUMMARY que la tabla ya estaba al estado target al inicio, citando el grep como evidencia.

    - **Caso B (drift remanente):** retorno `≥ 1`. Para cada fila con `| pending |`, ejecutar Edit (string replacement) flipping la celda a `| Complete |`, manteniendo la alineación de columnas del Markdown table. Commit funcional con mensaje `docs(32-01): close BOOK-01 — reconcile {N} IDs pending → Complete (16/16)` donde `{N}` es el conteo exacto del grep inicial; luego commit del SUMMARY.md normal.

    **Step 2 — IDs cubiertos por el alcance documental de BOOK-01** (referencia per v0.7-MILESTONE-AUDIT.md §Bookkeeping Drift item #1, independiente del branch que se ejecute):

    - GH-01 (Phase 23)
    - GH-02 (Phase 24)
    - GH-03 (Phase 24)
    - GH-04 (Phase 24)
    - GH-05 (Phase 24)
    - CFG-01 (Phase 26)
    - CFG-02 (Phase 26)
    - TEST-01 (Phase 24)

    **Step 3 — Scope guards (ambos branches):**

    NO tocar (per D-06 scope-fijo):
    - Las otras 8 filas (POLL-01..04, CFG-03, CFG-04, TEST-02, TEST-03) que ya están `Complete`.
    - La sección `## Coverage` debajo de la tabla.
    - El header `**Status:** ✅ SHIPPED 2026-05-14 — all 16 requirements satisfied.` (ya correcto).
    - El footer `*Created: 2026-05-13 ...` (ya correcto).
    - Cualquier otro archivo (D-06 scope-fijo).

    No actualizar timestamp `*Last updated:` en este archivo — el archive milestone v0.7 ya está sellado por fecha de ship 2026-05-14.

    **Step 4 — Commit convention summary:**

    - Branch A (no-op funcional): solo `docs(32-01): close BOOK-01 SUMMARY — validate v0.7 REQUIREMENTS traceability already reconciled (16/16, 0 pending)`.
    - Branch B (reconcile real): `docs(32-01): close BOOK-01 — reconcile {N} IDs pending → Complete (16/16)` + commit SUMMARY normal.

    Acceptance criteria abajo son IDÉNTICOS en ambos branches — solo validan el post-state.
  </action>
  <verify>
    <automated>grep -c "| pending |" .planning/milestones/v0.7-REQUIREMENTS.md  # debe retornar 0 (post-state, ambos branches)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| pending |" .planning/milestones/v0.7-REQUIREMENTS.md` retorna `0` (cero filas con status pending — post-state garantizado en ambos branches).
    - `grep -c "| Complete |" .planning/milestones/v0.7-REQUIREMENTS.md` retorna `16` (las 16 filas de la traceability table marcadas Complete; NOTA: solo cuenta lo que está en la tabla con el patrón exacto `| Complete |` — el header `## Traceability` y prosa no matchean este patrón).
    - `grep -nE "^\| GH-0[1-5]" .planning/milestones/v0.7-REQUIREMENTS.md | grep -c "Complete"` retorna `5` (GH-01..05 todos Complete).
    - `grep -nE "^\| CFG-0[12] " .planning/milestones/v0.7-REQUIREMENTS.md | grep -c "Complete"` retorna `2` (CFG-01 y CFG-02 Complete).
    - `grep -nE "^\| TEST-01 " .planning/milestones/v0.7-REQUIREMENTS.md | grep -c "Complete"` retorna `1` (TEST-01 Complete).
    - En branch B (si aplica): `git diff --stat .planning/milestones/v0.7-REQUIREMENTS.md` muestra ediciones SOLO en este archivo (no leak a otros archivos). En branch A: `git diff --stat .planning/milestones/v0.7-REQUIREMENTS.md` retorna vacío (no-op funcional).
    - `git diff -- src/ test/ bin/` retorna vacío (cero líneas de código tocadas — Tier 1 doc-only invariant — ambos branches).
    - En branch B: `git diff .planning/milestones/v0.7-REQUIREMENTS.md` NO modifica las filas de POLL-01, POLL-02, POLL-03, POLL-04, CFG-03, CFG-04, TEST-02, TEST-03 (ya estaban Complete).
  </acceptance_criteria>
  <done>
    Traceability table de v0.7-REQUIREMENTS.md tiene 16/16 IDs Complete con cero `pending` (post-state). Doc-only. Cambios scoped exclusivamente al archivo target en branch B (o cero cambios en branch A). Source-of-truth audit doc citado en commit message + SUMMARY. Branch ejecutado (A o B) documentado en el SUMMARY con el resultado del grep pre-edit como evidencia.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No code paths modified — no security surface change. Doc-only edits to planning artifacts (markdown table). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-N/A | Spoofing | N/A | N/A | Doc-only edit to `.planning/milestones/v0.7-REQUIREMENTS.md`. ASVS L1 N/A — no auth flows, no input validation, no session handling. |
| T-32-01-N/A | Tampering | N/A | N/A | Edit applies only to markdown table cells `pending` → `Complete` (branch B) o no-op (branch A). Git history preserves all prior states. ASVS L1 N/A. |
| T-32-01-N/A | Repudiation | N/A | N/A | Single doc-only commit (branch A: SUMMARY only; branch B: functional + SUMMARY) signed by user identity in git config. ASVS L1 N/A. |
| T-32-01-N/A | Information Disclosure | N/A | N/A | No secrets, no PII, no credentials in the file. ASVS L1 N/A. |
| T-32-01-N/A | Denial of Service | N/A | N/A | No runtime change, no resource consumption. ASVS L1 N/A. |
| T-32-01-N/A | Elevation of Privilege | N/A | N/A | No authorization logic affected. ASVS L1 N/A. |

**Justificación general:** Doc-only — no code paths modified, no security surface change. ASVS L1 N/A — phase modifica únicamente celdas de una tabla markdown en `.planning/milestones/v0.7-REQUIREMENTS.md` (o ninguna celda, en branch A).
</threat_model>

<verification>
- Suite global ≥830 pass (sin cambio numérico vs Phase 31 — no se ejecutan tests nuevos por D-04).
- `git diff <phase-base>..<phase-head> -- src/ test/ bin/` retorna vacío (Tier 1 doc-only invariant).
- BOOK-01 success criterion satisfied: tabla con 16/16 Complete (success criterion #1 del phase goal) — post-state idéntico en ambos branches.
</verification>

<success_criteria>
1. `grep -c "| pending |" .planning/milestones/v0.7-REQUIREMENTS.md` retorna 0 (post-state).
2. `grep -c "| Complete |" .planning/milestones/v0.7-REQUIREMENTS.md` retorna 16 (post-state).
3. Único archivo posiblemente modificado: `.planning/milestones/v0.7-REQUIREMENTS.md` (branch B) o ninguno (branch A).
4. Cero diff en `src/`, `test/`, `bin/` (ambos branches).
5. Branch ejecutado documentado en SUMMARY con grep evidencia.
</success_criteria>

<output>
After completion, create `.planning/phases/32-v0-7-bookkeeping-doc-only/32-01-SUMMARY.md` siguiendo el template estándar. Citar `v0.7-MILESTONE-AUDIT.md §Bookkeeping Drift item #1` como source-of-truth. Documentar explícitamente cuál branch fue ejecutado (A no-op funcional / B reconcile inline) con el resultado del grep pre-edit como evidencia.

Commit convention (un branch único depende del estado real al ejecutar):
- **Branch A (validación retro-defensiva, no-op funcional):** commit único `docs(32-01): close BOOK-01 SUMMARY — validate v0.7 REQUIREMENTS traceability already reconciled (16/16, 0 pending)`.
- **Branch B (reconcile real):** commit funcional `docs(32-01): close BOOK-01 — reconcile {N} IDs pending → Complete (16/16)` + commit SUMMARY normal.

Tier 1 — fast-forward a main local sin PR (per D-07).
</output>
