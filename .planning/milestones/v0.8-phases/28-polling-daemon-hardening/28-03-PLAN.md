---
phase: 28-polling-daemon-hardening
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - src/cli/polling-logfile.js
  - src/cli/polling.js
  - src/triggers/polling.js
  - test/cli/polling-logfile.test.js
  - test/cli/polling-verbose.test.js
autonomous: true
requirements:
  - DAEMON-02
requirements_addressed:
  - DAEMON-02
tags: [daemon, logfile, crash-diagnosis, retention, fd-redirect]

must_haves:
  truths:
    - "Operador encuentra el stack trace post-crash via `cat ~/.kodo/logs/polling-YYYY-MM-DD.log` — archivo existe con permisos 0o600"
    - "Daemon escribe stdout/stderr crudo al logfile via fd redirect en el spawn padre (D-13) — cero código de captura en el hijo"
    - "`polling-YYYY-MM-DD.log` filename usa fecha local del día de arranque (D-14), NO rolls mid-process"
    - "Retención 7 días aplicada al arrancar el daemon (D-15): scan + unlink fail-open sobre archivos con `mtime > 7 días`"
    - "Directorio `~/.kodo/logs/` se crea con mode 0o700 si no existe (D-16)"
    - "Daemon `--verbose` escribe NDJSON summary lines al logfile (D-17), separados del NDJSON sink raíz (D-18 separation of concerns)"
    - "Fd leak protegido: si `spawn` throws después de `openSync`, el fd se cierra explícitamente con `closeSync` en el catch (T-28-14 mitigation)"
    - "Crash simulation via env var `KODO_TEST_FORCE_THROW` (guarded por `NODE_ENV=test`) permite test integration determinístico — fd redirect captura el throw post-arranque del hijo"
  artifacts:
    - path: "src/cli/polling-logfile.js"
      provides: "resolveLogfilePath + ensureLogsDir + sweepRetention (3 pure FS-I/O primitivas)"
      contains: "export function resolveLogfilePath"
    - path: "src/cli/polling.js"
      provides: "fd redirect en daemon spawn + pre-flight retention sweep + try/catch wrap del fd"
      contains: "openSync"
    - path: "src/triggers/polling.js"
      provides: "Test seam KODO_TEST_FORCE_THROW guarded por NODE_ENV=test (4-line opt-in para integration test del daemon crash)"
      contains: "KODO_TEST_FORCE_THROW"
    - path: "test/cli/polling-logfile.test.js"
      provides: "Unit tests HOME-isolated para 3 primitivas del logfile module"
      contains: "resolveLogfilePath"
    - path: "test/cli/polling-verbose.test.js"
      provides: "Caso integration daemon crash → logfile contiene stack trace (extiende file existente del Plan 28-02)"
      contains: "stack trace"
  key_links:
    - from: "src/cli/polling.js#runPollingStartCli (daemon branch)"
      to: "src/cli/polling-logfile.js (resolveLogfilePath, ensureLogsDir, sweepRetention)"
      via: "Import + openSync(path, 'a', 0o600) + stdio: ['ignore', logFd, logFd]"
      pattern: "openSync\\(.*0o600"
    - from: "Daemon child stdout/stderr"
      to: "~/.kodo/logs/polling-YYYY-MM-DD.log"
      via: "Kernel fd redirect via spawn detached"
      pattern: "stdio:.*logFd"
    - from: "src/cli/polling-logfile.js#sweepRetention"
      to: "unlinkSync de archivos con mtime > 7 días"
      via: "readdirSync + statSync + unlinkSync (fail-open per archivo)"
      pattern: "mtimeMs"
    - from: "src/triggers/polling.js (test seam)"
      to: "Throw forzado post-arranque cuando KODO_TEST_FORCE_THROW + NODE_ENV=test"
      via: "Guard inline en processRepo"
      pattern: "KODO_TEST_FORCE_THROW"
---

<objective>
Cerrar el v0.7 tech debt DAEMON-02 (T-26-DIAG silent daemon crash): el daemon actual spawneaba con `stdio: 'ignore'`, perdiendo cualquier stack trace o output de crash. Esta phase añade captura via **fd redirect en el spawn del padre** (D-13) — robust contra SIGSEGV/OOM/throw fuera del event loop. Filename `polling-YYYY-MM-DD.log` con permisos 0o600, retention 7 días aplicada al arrancar (cleanup pasivo).

Purpose: Operador puede diagnosticar daemon crashes mediante `cat ~/.kodo/logs/polling-*.log` post-mortem, cerrando definitivamente T-26-DIAG.
Output: Nuevo módulo `src/cli/polling-logfile.js` con 3 primitivas FS-I/O puras, fd redirect en `runPollingStartCli` daemon branch (con try/catch wrap del fd para T-28-14), pre-flight retention sweep, test seam `KODO_TEST_FORCE_THROW` para crash determinístico, test unit logfile + test integration daemon crash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-polling-daemon-hardening/28-CONTEXT.md
@.planning/phases/28-polling-daemon-hardening/28-PATTERNS.md
@.planning/phases/28-polling-daemon-hardening/28-01-SUMMARY.md
@.planning/phases/28-polling-daemon-hardening/28-02-SUMMARY.md

@src/cli/polling.js
@src/cli/polling-daemon.js
@src/triggers/polling.js
@test/cli/polling-daemon.test.js
@test/cli/polling.test.js

<interfaces>
<!-- Contracts touched by this plan, extracted from existing codebase. -->

From src/cli/polling-daemon.js (template — lazy path resolver pattern):
- `getPidPath() → string` (lines 51-53): `return join(homedir(), '.kodo', 'polling.pid');` — homedir() called per invocation (Pitfall #11 HOME-isolated tests).
- `writePidFile({pid, started_at, repos})` (lines 75-90 approx): atomic write tmp+chmod 0o600 PRE-rename pattern.
- `removePidFile()`, `loadStateCache` etc. — fail-open patterns.

From src/cli/polling.js (current daemon spawn, lines 245-252):
```
const KODO_BIN = resolveKodoBin();
const child = spawn(
  process.execPath,
  [KODO_BIN, 'polling', 'start', '--no-daemon'],
  { detached: true, stdio: 'ignore', env: process.env },
);
child.unref();
```
- `stdio: 'ignore'` discards stdout/stderr — root cause of T-26-DIAG.
- After Plan 28-02: args include `...(opts.verbose ? ['--verbose'] : [])` — preserve this.

From src/triggers/polling.js#processRepo (líneas 223-385):
- Entry of the try block (línea 240) — punto natural para insertar test seam guard antes del network call.

From test/cli/polling-daemon.test.js (template for HOME-isolated unit tests):
- `mkdtempSync(join(tmpdir(), 'kodo-pid-write-'))` + `process.env.HOME = _tmpHome`.
- `await import(\`../../src/cli/polling-daemon.js?write-test-${Date.now()}\`)` — ESM cache bust per test.
- `afterEach` restaura HOME + `rmSync({recursive: true, force: true})`.

D-LOCKED references:
- D-13: fd redirect via `openSync(logfilePath, 'a', 0o600)` + `stdio: ['ignore', logFd, logFd]`.
- D-14: filename `polling-YYYY-MM-DD.log` con fecha local del día de arranque. NO roll mid-process.
- D-15: retention 7 días al arrancar, fail-open per archivo.
- D-16: `mkdirSync(dir, {recursive: true, mode: 0o700})` para `~/.kodo/logs/`.
- D-17: logfile content = stdout/stderr crudo (NDJSON summary lines + Node stack traces + console.error fallback).
- D-18: NDJSON sink raíz NO se toca — separation of concerns.

**Decisión sobre crash simulation (resuelve blocker checker #3)**:
Tras inspección del flow real:
- Opción A original (config inválida con `owner: null`) **NO viable**: la validación de config (`runPollingStartCli` D-14 línea 367 cli.js) ocurre EN EL PADRE pre-spawn → abortaría con exit code antes de crear el logfile. AC#2 ROADMAP (cat logfile contiene stack trace) NO se verificaría.
- **Opción B (elegida)**: env var `KODO_TEST_FORCE_THROW` guarded por `NODE_ENV=test`. Es un test seam mínimo (4 líneas) en `src/triggers/polling.js#processRepo`. El throw ocurre POST-spawn, dentro del hijo, donde el fd redirect está activo y captura el stack trace. La guard `NODE_ENV=test` garantiza que el seam NUNCA se activa en producción (defensa en profundidad — incluso si el operador pone la env var por accidente).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nuevo módulo src/cli/polling-logfile.js + unit tests HOME-isolated</name>
  <files>src/cli/polling-logfile.js, test/cli/polling-logfile.test.js</files>
  <read_first>
    - src/cli/polling-daemon.js (verbatim mirror del patrón lazy resolver + ensure dir + fail-open)
    - test/cli/polling-daemon.test.js (pattern HOME-isolated: mkdtempSync, env.HOME, ESM cache bust via dynamic import query, afterEach restore)
  </read_first>
  <behavior>
    - Test 1 (D-14 filename): `resolveLogfilePath({now: () => new Date('2026-05-18T12:00:00')})` retorna path terminado en `polling-2026-05-18.log` (fecha LOCAL — `Date('2026-05-18T12:00:00')` sin Z para evitar UTC shift).
    - Test 2 (D-16 dir mode): `ensureLogsDir()` crea `~/.kodo/logs/` con `statSync(dir).mode & 0o777 === 0o700`.
    - Test 3 (D-15 sweep): pre-poblar 3 archivos antiguos (mtime >7 días) + 2 recientes. Llamar `sweepRetention()`. Assert solo los antiguos fueron borrados.
    - Test 4 (D-15 fail-open dir ausente): sin `~/.kodo/logs/`, `sweepRetention()` no throw.
    - Test 5 (sweep filtro): `random.log` con mtime antiguo NO es borrado.
    - Test 6 (lazy resolver): cambiar `process.env.HOME` entre 2 invocaciones — las 2 paths reflejan HOME distinto (no cached).
  </behavior>
  <action>
    Crear `src/cli/polling-logfile.js`:
    1. Header `@ts-check` + bloque de comentario explicativo (mirror estilo `polling-daemon.js` líneas 1-25): propósito (Phase 28 D-13..D-16), 3 primitivas exportadas, Color isolation guarantee (cero stdout writes), LOG-12 invariant (cero import de logger).
    2. Imports: `import { openSync, readdirSync, statSync, unlinkSync, mkdirSync } from 'node:fs';` + `import { homedir } from 'node:os';` + `import { join } from 'node:path';`. NO importar logger ni picocolors.
    3. `export function resolveLogfilePath(opts = {})`:
       - JSDoc: lazy path resolver, recomputa en cada llamada (Pitfall #11). Filename `polling-YYYY-MM-DD.log` con fecha LOCAL al momento de la llamada (D-14). NO roll mid-process.
       - Body: `const now = opts.now ? opts.now() : new Date(); const y = now.getFullYear(); const m = String(now.getMonth() + 1).padStart(2, '0'); const d = String(now.getDate()).padStart(2, '0'); return join(homedir(), '.kodo', 'logs', \`polling-${y}-${m}-${d}.log\`);`
    4. `export function ensureLogsDir()`:
       - JSDoc: idempotente, crea `~/.kodo/logs/` con mode 0o700 (D-16).
       - Body: `mkdirSync(join(homedir(), '.kodo', 'logs'), { recursive: true, mode: 0o700 });`
    5. `export function sweepRetention(opts = {})`:
       - JSDoc: D-15 cleanup pasivo. Fail-open per archivo. Default retención 7 días.
       - Body:
         - `const dir = join(homedir(), '.kodo', 'logs');`
         - `const retentionDays = opts.retentionDays ?? 7;`
         - `const nowMs = opts.now ? opts.now().getTime() : Date.now();`
         - `const cutoffMs = nowMs - retentionDays * 86_400_000;`
         - `let entries; try { entries = readdirSync(dir); } catch { return; }` (D-15 fail-open).
         - `for (const name of entries) { if (!name.startsWith('polling-') || !name.endsWith('.log')) continue; const full = join(dir, name); try { const st = statSync(full); if (st.mtimeMs < cutoffMs) unlinkSync(full); } catch { /* fail-open per file */ } }`
    6. NO export `chmodSync` o `openSync` — esos los usa el caller. Separation of concerns.

    Crear `test/cli/polling-logfile.test.js`: pattern mirror de `test/cli/polling-daemon.test.js` (estilo + HOME isolation + ESM cache bust). Casos a implementar: los 6 listados en `<behavior>`.
  </action>
  <verify>
    <automated>node --test test/cli/polling-logfile.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/polling-logfile.js` existe con 3 exports nombrados: `resolveLogfilePath`, `ensureLogsDir`, `sweepRetention`.
    - `grep "console\\.\\|picocolors\\|require.*logger" src/cli/polling-logfile.js` retorna 0 matches (color isolation + LOG-12 invariants).
    - `grep "homedir()" src/cli/polling-logfile.js` retorna ≥ 3 matches (lazy per-function).
    - `test/cli/polling-logfile.test.js` existe con ≥ 6 casos.
    - `node --test test/cli/polling-logfile.test.js` exit 0.
  </acceptance_criteria>
  <done>
    Módulo `polling-logfile.js` implementado con 3 primitivas FS-I/O puras. Tests unit HOME-isolated cubren D-14, D-15, D-16, y la propiedad lazy del resolver.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fd redirect en runPollingStartCli daemon branch + try/catch wrap (T-28-14 fd leak) + retention sweep pre-flight</name>
  <files>src/cli/polling.js</files>
  <read_first>
    - src/cli/polling.js líneas 1-30 (imports), 204-265 (runPollingStartCli completo, daemon branch líneas 228-262)
    - src/cli/polling-logfile.js (creado en Task 1 — los 3 exports a importar)
  </read_first>
  <behavior>
    - Test 1 (D-13): cuando `runPollingStartCli` toma la branch daemon, llama a `openSync(logfilePath, 'a', 0o600)` y pasa el fd al `spawn` con `stdio: ['ignore', logFd, logFd]`.
    - Test 2 (D-15 pre-flight): orden: `ensureLogsDir` → `sweepRetention` → PID check → `openSync logfile` → `spawn`.
    - Test 3 (D-15 fail-open): si `sweepRetention` throws, `runPollingStartCli` continúa al spawn sin abortar.
    - Test 4 (verbose propagation preservada): `--verbose` propagado en args spawn (Plan 28-02).
    - Test 5 (D-16 logfile mode 0o600): post-spawn, `statSync(logfilePath).mode & 0o777 === 0o600`.
    - Test 6 (**T-28-14 fd leak**): si `spawn()` throws (e.g., ENOENT del binary), `closeSync(logFd)` se llama en el catch ANTES de re-throw → no fd leak.
  </behavior>
  <action>
    En `src/cli/polling.js`:
    1. **Imports** — añadir cerca de los imports existentes:
       - `import { openSync, closeSync } from 'node:fs';` (verificar primero si `openSync`/`closeSync` ya están; ambos necesarios para T-28-14).
       - `import { resolveLogfilePath, ensureLogsDir, sweepRetention } from './polling-logfile.js';`
    2. En `runPollingStartCli(opts, deps = {})` (línea 204), branch daemon (líneas 228-262): antes del bloque spawn (líneas 245-252), insertar IN-ORDER:
       - `ensureLogsDir();` (D-16).
       - `try { sweepRetention(); } catch { /* D-15 fail-open */ }`.
       - PID check existente preserva su posición.
       - `const logfilePath = resolveLogfilePath();` (D-14).
       - `const logFd = openSync(logfilePath, 'a', 0o600);` (D-13 + D-16).
    3. **Modificar el `spawn(...)` con try/catch wrap (T-28-14 fix)**:

       Reemplazar:
       ```
       const child = spawn(
         process.execPath,
         [KODO_BIN, 'polling', 'start', '--no-daemon', ...(opts.verbose ? ['--verbose'] : [])],
         { detached: true, stdio: 'ignore', env: process.env },
       );
       child.unref();
       ```

       Por (T-28-14 mitigation explícito):
       ```
       let child;
       try {
         child = spawn(
           process.execPath,
           [KODO_BIN, 'polling', 'start', '--no-daemon', ...(opts.verbose ? ['--verbose'] : [])],
           { detached: true, stdio: ['ignore', logFd, logFd], env: process.env },
         );
         child.unref();
       } catch (spawnErr) {
         // T-28-14: si spawn throws (ENOENT, EAGAIN), cerrar logFd antes de re-throw para evitar fd leak.
         try { closeSync(logFd); } catch { /* defensive */ }
         throw spawnErr;
       }
       ```

       Cambios clave:
       - `stdio: 'ignore'` → `stdio: ['ignore', logFd, logFd]` (D-13).
       - Wrap try/catch `closeSync(logFd)` (T-28-14 mitigation).
       - `child.unref()` DENTRO del try (no se ejecuta sobre undefined).
       - Preservar propagación `--verbose` (Plan 28-02).

    4. Comentarios inline:
       - En `openSync`: `// D-13/D-16 Phase 28: fd redirect captura stdout/stderr crudo del hijo a logfile mode 0o600.`
       - En `stdio`: `// D-13: stdio[1]=stdout, stdio[2]=stderr → mismo fd preserva interleaving cronológico.`
    5. NO añadir `process.on('uncaughtException')` en el hijo. NO cambiar path foreground.
  </action>
  <verify>
    <automated>node --test test/cli/polling.test.js 2>&1 | tail -30; node -e "const fs = require('fs'); const src = fs.readFileSync('src/cli/polling.js', 'utf8'); const checks = { openSync: src.includes('openSync'), closeSync: src.includes('closeSync(logFd)'), sweepRetention: src.includes('sweepRetention'), ensureLogsDir: src.includes('ensureLogsDir'), fdRedirect: /stdio:\s*\[\s*'ignore'\s*,\s*logFd\s*,\s*logFd\s*\]/.test(src), mode0o600: src.includes('0o600'), tryCatchSpawn: /try\s*\{[\s\S]*?spawn\([\s\S]*?\}\s*catch/.test(src) }; console.log(JSON.stringify(checks, null, 2)); process.exit(Object.values(checks).every(Boolean) ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "openSync.*'a', 0o600" src/cli/polling.js` retorna 1 match.
    - `grep "stdio:.*logFd.*logFd" src/cli/polling.js` retorna 1 match.
    - `grep "closeSync(logFd)" src/cli/polling.js` retorna ≥ 1 match (**T-28-14 mitigation verificable**).
    - `grep "ensureLogsDir\|sweepRetention\|resolveLogfilePath" src/cli/polling.js` retorna ≥ 4 matches (import + 3 invocaciones).
    - Orden secuencial: `ensureLogsDir()` → `sweepRetention()` (con try/catch) → `openSync` → `spawn` (verificado por inspección manual del diff o regex over source).
    - `grep "stdio: 'ignore'" src/cli/polling.js` retorna 0 matches (cambio efectivo).
    - Tests pre-existentes de `test/cli/polling.test.js` siguen verdes.
    - `process.on('uncaughtException')` NO añadido.
  </acceptance_criteria>
  <done>
    `runPollingStartCli` daemon branch hace fd redirect del stdout/stderr al logfile con mode 0o600. Pre-flight retention sweep aplicado fail-open. fd leak protegido por try/catch wrap con `closeSync` (T-28-14). Verbose propagation del Plan 28-02 preservada.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test seam KODO_TEST_FORCE_THROW guarded por NODE_ENV=test en src/triggers/polling.js</name>
  <files>src/triggers/polling.js</files>
  <read_first>
    - src/triggers/polling.js líneas 223-245 (processRepo entry — donde insertar el guard).
  </read_first>
  <behavior>
    - Test 1: cuando `NODE_ENV !== 'test'`, la env var `KODO_TEST_FORCE_THROW` se ignora completamente — el flow normal continúa sin throw.
    - Test 2: cuando `NODE_ENV === 'test'` Y `KODO_TEST_FORCE_THROW === 'true'`, `processRepo` throws un Error con mensaje `'KODO_TEST_FORCE_THROW: test-induced crash'` ANTES de cualquier network call o I/O.
    - Test 3: sin `KODO_TEST_FORCE_THROW`, el flow normal pasa sin tocar el guard (caso producción).
  </behavior>
  <action>
    En `src/triggers/polling.js#processRepo` (líneas 223-245):

    Insertar el siguiente bloque AL INICIO del cuerpo de la función `processRepo`, antes del `const key = ${owner}/${repo};` (línea 235):

    ```
    // Phase 28 D-13 test seam (Plan 28-03 Task 3): integration test del daemon
    // crash usa esta env var para forzar throw post-spawn del hijo, permitiendo
    // que el fd redirect (D-13) capture el stack trace en el logfile. Doble
    // guard NODE_ENV=test para que NUNCA se active en producción incluso si el
    // operador la define accidentalmente.
    if (process.env.NODE_ENV === 'test' && process.env.KODO_TEST_FORCE_THROW === 'true') {
      throw new Error('KODO_TEST_FORCE_THROW: test-induced crash');
    }
    ```

    Posición exacta: dentro de `async function processRepo({...})`, como **primera línea ejecutable** del body. NO dentro del `while`. NO dentro del `try`. NO añadir handler `process.on('uncaughtException')` (deferred ideas explícito).

    El throw propagará up del processRepo → tick closure → `Promise.resolve().then(tick)` kick-off (línea 459) → uncaught rejection del proceso del hijo → exit code != 0 → stderr crudo (incluyendo stack trace) → fd redirect (D-13) → logfile.

    NO añadir esta env var a documentación pública (README, --help, etc.). Es un test seam interno; el JSDoc inline + el path testing/ son la documentación.
  </action>
  <verify>
    <automated>node --test test/triggers/polling.test.js 2>&1 | tail -20; grep -c "KODO_TEST_FORCE_THROW" src/triggers/polling.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "KODO_TEST_FORCE_THROW" src/triggers/polling.js` retorna ≥ 2 matches (env var + comentario inline).
    - `grep "NODE_ENV === 'test'" src/triggers/polling.js` retorna ≥ 1 match (guard explícito).
    - El bloque está al INICIO de `processRepo`, antes de `const key = ...`.
    - Tests pre-existentes de `test/triggers/polling.test.js` siguen verdes (`NODE_ENV !== 'test'` o `KODO_TEST_FORCE_THROW` unset).
    - Si los tests existentes corren con `NODE_ENV=test`, no se activa salvo que también se set `KODO_TEST_FORCE_THROW=true` — defensa en profundidad por dos env vars.
    - `node --test test/triggers/polling.test.js` exit 0.
  </acceptance_criteria>
  <done>
    Test seam `KODO_TEST_FORCE_THROW` insertado en `processRepo` con guard doble `NODE_ENV=test && KODO_TEST_FORCE_THROW='true'`. Cero impacto en producción. Permite a Task 4 simular crash determinístico capturado por fd redirect.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test integration daemon crash → logfile contiene stack trace + permisos 0o600 + D-17/D-18 separation</name>
  <files>test/cli/polling-verbose.test.js</files>
  <read_first>
    - test/cli/polling-verbose.test.js (creado en Plan 28-02 — extender, NO recrear)
    - test/cli/polling.test.js (pattern integration spawn, líneas 179-185 + makeFixture líneas 87-110)
    - src/cli/polling-logfile.js (export `resolveLogfilePath` para reusar en el test)
    - src/triggers/polling.js (Task 3 — env var seam `KODO_TEST_FORCE_THROW`)
  </read_first>
  <behavior>
    - Test 1 (AC#2 ROADMAP): spawn `kodo polling start` como daemon (sin `--no-daemon`) con HOME-isolated config + fake GitHub token + env vars `NODE_ENV=test` y `KODO_TEST_FORCE_THROW=true`. El hijo arranca exitosamente, processRepo throws en el primer tick, fd redirect captura. Wait ≤3s. `readFileSync(resolveLogfilePath())` contiene stack trace (regex `at ` o `Error:`).
    - Test 2 (D-16 permisos): post-spawn, `statSync(logfilePath).mode & 0o777 === 0o600`.
    - Test 3 (D-14 filename): post-spawn, `logfilePath` matches `polling-YYYY-MM-DD.log`.
    - Test 4 (D-17 verbose escribe al logfile): spawn daemon con `--verbose` SIN `KODO_TEST_FORCE_THROW`, wait ≥1 tick, `grep "polling.tick.summary" logfile` retorna ≥ 1 match.
    - Test 5 (D-18 separation of concerns): logfile daemon (`~/.kodo/logs/polling-*.log`) es path DISTINTO del sink NDJSON raíz (`~/.kodo/logs/<session>.ndjson`). Ambos pueden coexistir.
  </behavior>
  <action>
    En `test/cli/polling-verbose.test.js` (extender el file de Plan 28-02 — añadir nuevos `test()` casos sin tocar los existentes):

    1. **Imports adicionales**: `import { readFileSync, statSync, existsSync } from 'node:fs';` + `import { resolveLogfilePath } from '../../src/cli/polling-logfile.js';`. Usar dynamic import si el file usa ESM cache bust para HOME-isolation.

    2. **Caso "daemon crash → logfile contiene stack trace + mode 0o600"**:
       - HOME-isolated setup (mkdtempSync + env.HOME + makeFixture con fake GITHUB_TOKEN + 1 repo válido).
       - Spawn:
         ```
         const child = spawn(process.execPath, [KODO_BIN, 'polling', 'start'], {
           env: {
             ...process.env,
             HOME: tmpHome,
             GITHUB_TOKEN: 'fake_token_for_test',
             NO_COLOR: '1',
             NODE_ENV: 'test',
             KODO_TEST_FORCE_THROW: 'true',  // Task 3 seam — fuerza throw post-spawn
           },
         });
         ```
       - Comentario inline en el test: "Opción B explícita (resuelve blocker checker #3): la env var `KODO_TEST_FORCE_THROW` (Task 3) forza throw POST-spawn del hijo dentro de `processRepo`. El fd redirect (D-13) captura el stack trace en el logfile. La env var requiere `NODE_ENV=test` como doble guard — NUNCA se activa en producción."
       - Wait ≤3s: `await new Promise(r => setTimeout(r, 2500));`.
       - `const logfilePath = resolveLogfilePath();` (lazy — usa HOME del tmpHome via `homedir()`).
       - `assert.equal(existsSync(logfilePath), true, 'logfile creado por fd redirect padre');`.
       - `const content = readFileSync(logfilePath, 'utf8'); assert.match(content, /KODO_TEST_FORCE_THROW.*test-induced crash/, 'logfile contiene mensaje del throw');`.
       - `assert.match(content, /at .*processRepo|Error:/, 'logfile contiene stack trace de Node');`.
       - `assert.equal(statSync(logfilePath).mode & 0o777, 0o600, 'logfile mode 0o600 D-16');`.
       - `assert.match(logfilePath, /polling-\d{4}-\d{2}-\d{2}\.log$/, 'D-14 filename format');`.
       - Cleanup: `try { child.kill('SIGTERM'); } catch {}` + rmSync tmpHome.

    3. **Caso "daemon --verbose escribe NDJSON al logfile"**:
       - HOME-isolated setup, SIN `KODO_TEST_FORCE_THROW`.
       - Spawn `kodo polling start --verbose` (sin `--no-daemon`).
       - Wait ≤4s para ≥1 tick (poll_interval mínimo configurado en makeFixture).
       - `child.kill('SIGTERM');` + esperar exit.
       - `const content = readFileSync(resolveLogfilePath(), 'utf8'); assert.match(content, /"event":\s*"polling\.tick\.summary"|polling\.tick\.summary/, 'NDJSON summary line en logfile');`.

    4. **Caso "D-18 separation of concerns"**:
       - HOME-isolated setup, sin crash.
       - Spawn daemon `kodo polling start` (sin verbose, sin crash). Wait ≤3s. Kill.
       - `assert.equal(existsSync(resolveLogfilePath()), true, 'logfile daemon existe');`.
       - El sink NDJSON raíz path es `join(_tmpHome, '.kodo', 'logs', 'polling.ndjson')` (sessionId 'polling' → file `<sessionId>.ndjson` per src/logger.js convention). Si el sink raíz fue creado (verificar `existsSync`), asserts:
         - Path NDJSON raíz ≠ logfile daemon path.
         - El logfile daemon NO contiene los eventos de level=debug (que sí van al sink raíz, separation D-18).

    5. **NO añadir handler de SIGTERM o cleanup forzado en el `afterEach`** — usar el cleanup del Plan 28-02 ya wireado.
  </action>
  <verify>
    <automated>node --test test/cli/polling-verbose.test.js 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - ≥3 nuevos `test()` cases añadidos en `test/cli/polling-verbose.test.js` (crash, verbose-NDJSON-logfile, D-18 separation).
    - Caso "daemon crash → stack trace" valida: file existe + match regex stack trace + match `KODO_TEST_FORCE_THROW` + mode 0o600 + filename format `polling-YYYY-MM-DD.log`.
    - `node --test test/cli/polling-verbose.test.js` exit 0; tests Plan 28-02 (foreground verbose) siguen verdes.
    - El test NO requiere acceso a la red real — todo el spawn corre con `GITHUB_TOKEN=fake_token_for_test` y el seam fuerza throw antes del network call.
    - El test NO modifica el `~/.kodo/` real del operador — HOME-isolated estricto.
    - Cleanup borra el tmpHome.
  </acceptance_criteria>
  <done>
    Test integration valida AC#2 ROADMAP: daemon crash → logfile existe con stack trace + mode 0o600 + filename `polling-YYYY-MM-DD.log`. Crash simulation via env var Task 3 (`KODO_TEST_FORCE_THROW` + `NODE_ENV=test`) garantiza determinismo. Caso `--verbose` daemon valida D-17 (NDJSON summary al logfile). Caso D-18 valida separation vs NDJSON sink raíz.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Padre `runPollingStartCli` → `openSync` → kernel fd | Path bajo `~/.kodo/logs/` (HOME-isolated). Mode 0o600 evita lectura por otros usuarios. |
| Child stdout/stderr → fd inherited via `spawn detached` | Kernel propaga fd al hijo via dup; child no abre el archivo directamente. |
| `sweepRetention` → `readdirSync` + `unlinkSync` | Bound al subdirectorio `~/.kodo/logs/` con filtro `polling-*.log`. NUNCA toca paths fuera. |
| Daemon crash → stdout/stderr → logfile | Texto crudo del proceso. Puede contener stack traces (paths absolutos del sistema), pero no secrets del entorno. |
| `KODO_TEST_FORCE_THROW` env var → processRepo throw | Test seam con doble guard `NODE_ENV=test` — defensa en profundidad contra activación accidental en producción. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | S (Spoofing) | Symlink attack en `~/.kodo/logs/polling-YYYY-MM-DD.log` | mitigate | `~/.kodo/logs/` se crea con mode 0o700 (D-16), restringiendo write access al owner. Path traversal bloqueado por uso de `join(homedir(), '.kodo', 'logs', ...)` con filename literal de fecha (no input usuario). |
| T-28-11 | T (Tampering) | TOCTOU en `sweepRetention` (statSync → unlinkSync) | mitigate | Wrap try/catch per archivo. Fail-open per archivo evita que un archivo "fantasma" detenga el sweep del resto. |
| T-28-12 | I (Information disclosure) | Stack trace en logfile expone paths absolutos | accept | Mode 0o600 restringe lectura al owner. |
| T-28-13 | D (DoS) | Logfile crece sin bound si daemon corre días sin restart | mitigate | D-14 trade-off: NO roll mid-process. Retención 7 días al arrancar (D-15) limita bloat histórico. |
| **T-28-14** | E (Elevation of privilege) | **fd leak post-openSync si spawn falla** | **mitigate** | **Wrap explícito `try { spawn(); child.unref(); } catch (e) { try { closeSync(logFd); } catch {} ; throw e; }` en `src/cli/polling.js` (Task 2). Acceptance criteria: `grep "closeSync(logFd)" src/cli/polling.js` retorna ≥1 match.** |
| T-28-15 | T (Tampering) | sweepRetention puede borrar logfile actual si clock skew | mitigate | El sweep usa `mtimeMs` (no parse del filename). El logfile activo tendrá `mtime` reciente → no cae en cutoff. |
| T-28-16 | E (Elevation of privilege) | Test seam `KODO_TEST_FORCE_THROW` activado en producción | mitigate | Doble guard `NODE_ENV === 'test' && KODO_TEST_FORCE_THROW === 'true'`. El seam es un throw, no una elevación real, pero el doble guard previene cualquier activación accidental. Documentación restringida al JSDoc inline (no expuesto en --help). |

Block on: T-28-10 (symlink), **T-28-14 (fd leak)** — alto severity ante errores de runtime.
</threat_model>

<verification>
- `kodo polling start` (branch daemon) crea `~/.kodo/logs/polling-YYYY-MM-DD.log` con mode 0o600 — verificable con `ls -la ~/.kodo/logs/`.
- Stdout/stderr del child redirigidos al logfile via fd redirect del padre — robust contra SIGSEGV/throw fuera del event loop.
- `~/.kodo/logs/` se crea con mode 0o700 al arrancar.
- Retención 7 días al arrancar: cleanup pasivo fail-open per archivo.
- **T-28-14 mitigation verificable**: `grep "closeSync(logFd)" src/cli/polling.js` retorna ≥1 match en el catch del spawn.
- Crash simulation determinístico: `KODO_TEST_FORCE_THROW=true` + `NODE_ENV=test` fuerza throw post-spawn capturado por fd redirect.
- Suite global ≥784 pass (≥781 post-28-02 + ≥3 nuevos: 6 tests unit logfile + ≥3 integration logfile/crash).
- 0 regresiones en tests Phase 25/26.
</verification>

<success_criteria>
1. ROADMAP Success Criterion 2: operador encuentra stack trace en `~/.kodo/logs/polling-YYYY-MM-DD.log` post-crash; archivo existe con mode 0o600.
2. Daemon `--verbose` escribe NDJSON summary lines al logfile (D-17), separados del NDJSON sink raíz (D-18 separation of concerns).
3. Retención 7 días al arrancar: cleanup pasivo fail-open per archivo.
4. fd redirect en padre: cero código de captura en el hijo (D-13 robustez ante crashes catastróficos).
5. T-28-14 mitigation: fd leak protegido por try/catch wrap explícito con `closeSync`.
6. ROADMAP Success Criterion 4: suite global ≥783 alcanzado o excedido.
</success_criteria>

<output>
After completion, create `.planning/phases/28-polling-daemon-hardening/28-03-SUMMARY.md`
</output>
