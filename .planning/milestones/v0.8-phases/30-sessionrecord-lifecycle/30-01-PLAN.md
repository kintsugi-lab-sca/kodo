---
phase: 30-sessionrecord-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/state.js
  - test/session/find-session.test.js
autonomous: true
requirements:
  - LIFE-01
requirements_addressed:
  - LIFE-01
tags:
  - session-lifecycle
  - state-store
  - history-scan
must_haves:
  truths:
    - "Operador ejecuta `kodo gsd verify <session-id>` para sesión archivada (en state.history, ausente de state.sessions) y recibe el SessionRecord histórico — NO el error 'session not found'"
    - "D-01: findSession retorna tagged discriminated union `{id, session, source: 'sessions' | 'history'}` — callers existentes que solo usan `r.session` siguen funcionando"
    - "D-02: cuando una entry aparece en sessions Y en history (degenerate window de removeSession), priority sessions"
    - "D-03: para history entries, `id = session.task_id` (sintetizado del propio record porque history es array sin key real); el shape `{id, session}` se preserva para compatibilidad con verify.js#84"
    - "D-04: las 3 lookup keys actuales (`sessionId`, `workspaceRef`, `cwd`) operan idénticas sobre history (mismo shape gracias a removeSession que hace `{...removed, ended_at: ISO}`)"
    - "D-10: tests viven en subdirectorio nuevo `test/session/` — `find-session.test.js` con 4 escenarios (en sessions, en history, en ambos = priority sessions, en ninguno = not found)"
    - "D-11: convención existente del proyecto es flat (`test/session-start.test.js`, `test/session-of-resolver.test.js`); Phase 30 introduce el subdirectorio porque ROADMAP SC#3 lo lockea explícitamente; tests existentes flat NO se mueven"
    - "D-13: Phase 30 cierra CR-01 Phase 19 deferred (findSession debe escanear state.history) en STATE.md v0.7 deferred section"
    - "D-14: suite global post-Phase-30 ≥825 pass + 0 fail (floor explícito en SC#4); baseline post-Phase-29 = 873 pass + 1 skip, Phase 30 añade ~7 tests netos"
    - "`kodo logs --session-of <task-id>` sigue funcionando para sesiones archivadas vía step-2 NDJSON head-line scan (sobrevive a removeSession porque los NDJSON files son independientes de state.sessions)"
  artifacts:
    - path: "src/session/state.js"
      provides: "findSession extendido — scan sessions + history con tagged return shape"
      contains: "source: 'sessions'"
      contains_also: "source: 'history'"
    - path: "test/session/find-session.test.js"
      provides: "4 escenarios LIFE-01 con HOME-isolation scaffold + manual state.json seed para priority test"
      exports: "describe block 'LIFE-01 — findSession scans history'"
      min_lines: 120
  key_links:
    - from: "src/session/state.js findSession"
      to: "state.history array"
      via: "loadState().history defensive Array.isArray guard (mismo patrón que listHistory line 144)"
      pattern: "Array\\.isArray\\(.*history\\)"
    - from: "test/session/find-session.test.js"
      to: "src/session/state.js"
      via: "dynamic import POST-HOME override (KODO_DIR cache)"
      pattern: "await import\\('.*state\\.js'\\)"
    - from: "src/gsd/verify.js#84 findSession caller"
      to: "extended findSession"
      via: "non-breaking: solo lee `r.session`, ignora `source` field (D-01 aditivo)"
      pattern: "r\\.session"
---

<objective>
Extender `findSession` en `src/session/state.js` para escanear tanto `state.sessions` (activas) como `state.history` (terminadas, FIFO 50-slot) y retornar un tagged discriminated union `{id, session, source: 'sessions' | 'history'}`.

Cierra CR-01 Phase 19 deferred. Driver real: ROMAN-132 (2026-05-15) confirmó state.json desync — `state.sessions = {}` mientras sesión seguía viva en cmux.

Purpose: Resolver SC#1 del ROADMAP. `kodo gsd verify <session-id>` y `kodo logs --session-of <task-id>` deben funcionar incluso después del stop hook (que llama removeSession y archiva en history).

Output: `src/session/state.js` con findSession extendido + `test/session/find-session.test.js` con 4 escenarios verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sessionrecord-lifecycle/30-CONTEXT.md
@.planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md
@src/session/state.js
@test/session-of-resolver.test.js

<interfaces>
<!-- Contracts extraídos del código fuente para evitar exploración del executor -->

From src/session/state.js (current state):

```javascript
/**
 * @typedef {{
 *   workspace_ref: string,
 *   session_id: string,
 *   task_id: string,
 *   task_ref: string,
 *   provider: string,
 *   project_id: string,
 *   summary: string,
 *   status: 'running'|'done'|'error'|'review',
 *   started_at: string,
 *   project_path: string,
 *   // ... optional fields
 * }} Session
 *
 * @typedef {{ schema_version: number, sessions: Record<string, Session> }} State
 */
// NOTA: el typedef NO documenta `history` field — discretionary cleanup opcional.

export function loadState(): State
// Retorna {schema_version, sessions} — NO inicializa history. Caller defensive guard.

export function listHistory(): Array<Session & { ended_at: string }>
// Pattern: const history = Array.isArray(state.history) ? state.history : [];

export function removeSession(taskId, logger):
// Archiva en state.history.unshift({...removed, ended_at: ISO}) ANTES de delete state.sessions[taskId]
// Garantiza: history entries tienen session_id, workspace_ref, project_path, task_id (mismo shape)

// Current findSession (lines 180-194) — to be EXTENDED:
export function findSession(query: { sessionId?, cwd?, workspaceRef? })
  : { id: string, session: Session } | null
```

From src/gsd/verify.js (line 83-86, non-breaking caller):
```javascript
const findSessionFn =
  deps.findSessionFn ||
  ((q) => {
    const r = findSession(q);
    return r ? r.session : undefined;  // solo lee .session — source addition no rompe
  });
```

From src/hooks/session-start.js (line 203, non-breaking caller):
```javascript
const result = findSession({ sessionId, cwd });
if (!result) process.exit(0);
const { session } = result;  // solo destructura .session — source addition no rompe
```

From test/session-of-resolver.test.js (HOME-isolation scaffold lines 101-134):
```javascript
import { mkdtempSync, rmSync, mkdirSync, writeFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
// CRITICAL: process.env.HOME = tmpHome ANTES del dynamic import (KODO_DIR computa en load-time)
process.env.HOME = tmpHome;
mkdirSync(join(tmpHome, '.kodo'), { recursive: true });
const stateMod = await import('../../src/session/state.js');
```

History entry shape from removeSession (state.js line 132-135):
```
{...removed, ended_at: '<ISO>'}
// NOTA: campo es `ended_at`, NO `archived_at`. CONTEXT.md D-04 dice `archived_at`
//       pero la fuente es autoritativa — usar `ended_at` byte-exact.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender findSession para escanear state.history con tagged return shape</name>
  <files>src/session/state.js</files>
  <read_first>
    - src/session/state.js (líneas 1-197 completas — pequeño, único pass)
    - .planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md sección "src/session/state.js — extend findSession (LIFE-01)" (líneas 24-115)
    - .planning/phases/30-sessionrecord-lifecycle/30-CONTEXT.md decisiones D-01..D-04
  </read_first>
  <behavior>
    - Test 1: findSession con sessionId presente solo en state.sessions retorna `{id, session, source: 'sessions'}`
    - Test 2: findSession con sessionId presente solo en state.history retorna `{id: session.task_id, session, source: 'history'}` (D-03: id sintetizado)
    - Test 3 (priority D-02): findSession con sessionId presente en AMBOS retorna source 'sessions' (window degenerate)
    - Test 4: findSession con sessionId ausente retorna null
    - Test 5: findSession por workspaceRef opera idéntico sobre history (D-04)
    - Test 6: findSession por cwd opera idéntico sobre history (D-04)
    - Test 7: Legacy state.json sin `history` array no crashea (defensive Array.isArray guard, mismo patrón listHistory line 144)
  </behavior>
  <action>
    Refactor de `findSession` (líneas 180-194 de state.js). Mantener firma `findSession(query)` con type `{sessionId?, cwd?, workspaceRef?}`.

    Estructura del nuevo body (sin código fenced — descripción directiva):

    1. `const state = loadState();` (single call — preserva pattern actual). Then `const sessions = state.sessions;` y `const history = Array.isArray(state.history) ? state.history : [];` (defensive guard copiado de listHistory).
    2. **Priority sessions (D-02)**: Primero scan completo de `sessions`. Si match por sessionId → return `{id, session, source: 'sessions'}`. Si no, scan por workspaceRef/cwd → return `{id, session, source: 'sessions'}`.
    3. Después scan de `history` array. Para cada entry (D-03: synthetic id = `session.task_id`):
       - Si query.sessionId && entry.session_id === query.sessionId → return `{id: entry.task_id, session: entry, source: 'history'}`
       - Si query.workspaceRef && entry.workspace_ref === query.workspaceRef → return `{id: entry.task_id, session: entry, source: 'history'}`
       - Si query.cwd && entry.project_path === query.cwd → return `{id: entry.task_id, session: entry, source: 'history'}`
    4. `return null;` (sin match).

    **Opcional (Claude's Discretion)**: extraer helper privado `findInBucket(bucket, query, source)` si el body se vuelve ilegible. Bucket puede ser `Object.entries(sessions)` o `history.map(s => [s.task_id, s])`. NO requiere decisión externa.

    **JSDoc updates (Claude's Discretion)**: actualizar JSDoc de `findSession` para documentar:
    - El nuevo `source` field en el return type (`{id: string, session: Session, source: 'sessions' | 'history'} | null`)
    - Priority sessions over history (D-02)
    - Lookup keys idénticas en ambos buckets (D-04)

    **Discretionary**: actualizar el JSDoc `State` typedef (línea 32) para añadir `history?: Array<Session & {ended_at: string}>` — marcado como Claude's Discretion en pitfall #3.

    **NO tocar**: `addSession`, `removeSession`, `updateSession`, `listSessions`, `listHistory`. Solo extender `findSession`.

    Cambios per D-01..D-04 + CR-01 Phase 19 closure.
  </action>
  <verify>
    <automated>node --test test/session/find-session.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/session/state.js` contiene exactamente una definición de `findSession` que itera tanto `state.sessions` como `state.history`
    - `grep -n "source:" src/session/state.js` retorna ≥2 líneas dentro del cuerpo de findSession (una por bucket)
    - `grep -n "Array.isArray.*history" src/session/state.js` retorna ≥1 match dentro de findSession (defensive guard)
    - `node --test test/session/find-session.test.js` exits 0 con 4 escenarios pasando
    - `node --test test/session-start.test.js test/session-of-resolver.test.js test/gsd-verify-integration.test.js` exits 0 (callers existentes no rompen)
    - `npm test` total pass ≥ 825 + 0 fail (D-14 floor)
    - JSDoc del `findSession` documenta el nuevo `source` field (verificable con `grep -B2 "export function findSession" src/session/state.js | grep "source"`)
  </acceptance_criteria>
  <done>
    findSession extendido funciona idénticamente para callers existentes (verify.js#84 + session-start.js#203 + stop.js#132 solo leen `.session`); nuevos callers pueden discriminar por `source` field; 4 escenarios test GREEN; suite global ≥825 pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test/session/find-session.test.js con 4 escenarios LIFE-01 + HOME-isolation scaffold</name>
  <files>test/session/find-session.test.js</files>
  <read_first>
    - test/session-of-resolver.test.js (líneas 1-180 completas — analog HOME-isolation scaffold + SessionRecord construction pattern)
    - test/state.test.js (líneas 1-90 — basic state-store test structure analog)
    - .planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md sección "test/session/find-session.test.js (NEW — 4 scenarios LIFE-01)" (líneas 283-371)
    - src/session/state.js líneas 113-141 (addSession + removeSession para preparar fixtures)
  </read_first>
  <behavior>
    - Escenario 1 (D-01 source 'sessions'): addSession(taskId, session) + findSession({sessionId}) → assert deepEqual `{id: taskId, session, source: 'sessions'}`
    - Escenario 2 (D-01 source 'history'): addSession + removeSession + findSession({sessionId}) → assert match con `source: 'history'` y `id === session.task_id` (D-03 synthesis)
    - Escenario 3 (D-02 priority): manual seed state.json con misma session_id en ambos buckets + findSession({sessionId}) → assert `source: 'sessions'`
    - Escenario 4 (null path): findSession({sessionId: 'never-existed'}) sobre state limpio → assert `result === null`
  </behavior>
  <action>
    Crear NEW file `test/session/find-session.test.js`. Mkdir `test/session/` si no existe (D-11: subdirectorio nuevo lockado por SC#3 ROADMAP).

    Estructura del file (sin código fenced):

    1. **Imports**: `describe, it, before, after, afterEach` de `node:test`. `assert` de `node:assert/strict`. `mkdtempSync, rmSync, mkdirSync, writeFileSync` de `node:fs`. `tmpdir` de `node:os`. `join` de `node:path`.

    2. **HOME-isolation scaffold** (copiar verbatim el patrón de test/session-of-resolver.test.js líneas 101-134):
       - `let tmpHome, origHome;`
       - `let findSession, addSession, removeSession;` (dynamic-imported POST-HOME)
       - `before(async () => {...})`: guarda `origHome`, crea `tmpHome` con `mkdtempSync(join(tmpdir(), 'kodo-life01-'))`, set `process.env.HOME = tmpHome`, mkdir `.kodo/`, **DYNAMIC IMPORT** `await import('../../src/session/state.js')` y desestructura las 3 funciones. La ruta relativa es `../../` porque el test vive en subdirectorio `test/session/` (D-11).
       - `after(() => {...})`: restore HOME, rmSync tmpHome con `recursive: true, force: true`.
       - `afterEach(() => {...})`: rewrite state.json a `{schema_version: 2, sessions: {}}` (sin history — verifica defensive guard).

    3. **describe block prefix** (D-10 convention): `'LIFE-01 — findSession scans history'`.

    4. **4 it() escenarios** con prefijos descriptivos:
       - `it('returns {source: "sessions"} when session is active', ...)`: construye SessionRecord (ver shape abajo), addSession + findSession por sessionId → `assert.deepEqual(result, {id: taskId, session: {...}, source: 'sessions'})` (usar assert.equal sobre `result.source` + `assert.equal(result.id, taskId)` + assert structural sobre `result.session.session_id`).
       - `it('returns {source: "history"} when session was removed', ...)`: addSession + removeSession + findSession por sessionId → assert `result.source === 'history'` + `result.id === session.task_id` (D-03 synthesis) + `result.session.ended_at` presente (heredado de removeSession).
       - `it('priorities sessions over history when entry exists in both', ...)`: manual `writeFileSync(STATE_PATH, JSON.stringify({schema_version: 2, sessions: {'task-X': {session_id: 'shared-id', task_id: 'task-X', /* full shape */}}, history: [{session_id: 'shared-id', task_id: 'task-X', ended_at: '...', /* full shape */}]}))` + findSession({sessionId: 'shared-id'}) → assert `result.source === 'sessions'`.
       - `it('returns null when session is in neither bucket', ...)`: estado limpio (gracias a afterEach) + findSession({sessionId: 'never-existed'}) → `assert.equal(result, null)`.

    5. **SessionRecord construction shape** (copiar de session-of-resolver.test.js líneas 142-154):
       ```
       session_id, task_id, task_ref, gsd: false, status: 'running',
       provider: 'plane', project_id, project_path: tmpHome,
       workspace_ref, started_at: new Date().toISOString(), summary
       ```

    **NO añadir** assertions sobre `workspaceRef` o `cwd` lookups en history — esos quedan cubiertos por D-04 (mismo shape, misma lógica) y la regression tests de session-start.test.js + session-of-resolver.test.js. Mantener el file enfocado a los 4 escenarios SC#3.

    Prefijo describe `'LIFE-01 — '` per established pattern (Phase 29 REPORT-NN, QUICK-08).
  </action>
  <verify>
    <automated>node --test test/session/find-session.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `test/session/find-session.test.js` existe y tiene ≥120 líneas
    - `grep -c "it('" test/session/find-session.test.js` retorna 4 (4 escenarios)
    - `grep "describe('LIFE-01" test/session/find-session.test.js` matchea exactamente una vez
    - `grep "source: 'sessions'" test/session/find-session.test.js` y `grep "source: 'history'" test/session/find-session.test.js` retornan ≥1 match cada uno
    - `node --test test/session/find-session.test.js` exits 0 con 4 tests pasando
    - `npm test` total pass ≥ 825 con 0 fail (D-14 floor)
    - Test discovery confirmado: `find test -name '*.test.js' -type f | grep -c "find-session.test.js"` retorna 1
  </acceptance_criteria>
  <done>
    4 escenarios test GREEN; HOME-isolation funciona (sin contaminar `~/.kodo/`); manual seed scenario 3 demuestra D-02 priority; afterEach reset garantiza aislamiento entre tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificación empírica session-lookup.js step-2 cubre archived sessions (pitfall #1 PATTERNS.md)</name>
  <files>test/session/find-session.test.js</files>
  <read_first>
    - src/logs/session-lookup.js (líneas 1-90 completas)
    - .planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md sección "Pitfalls & Gotchas" pitfall #1 (líneas 494-497)
  </read_first>
  <action>
    **Smoke verification — NO añadir test nuevo si la suite existente ya lo cubre.**

    El pitfall #1 dice: `src/logs/session-lookup.js` step-1 (line 33) itera `state.sessions` directamente, NO via `findSession`. SC#1 ROADMAP lockea AMBOS:
    - `kodo gsd verify <session-id>` (resuelto por Task 1+2 — usa findSession extendido)
    - `kodo logs --session-of <task-id>` (depende de session-lookup.js)

    **Hipótesis del pattern-mapper**: step-2 (head-line scan over `~/.kodo/logs/*.ndjson`) cubre archived sessions porque los NDJSON files sobreviven a `removeSession` (son independientes de `state.sessions`).

    **Verificación quirúrgica**:

    1. **Read test/logs-session-of.test.js completo** (single pass). Buscar si existe ya un escenario que:
       - Arranque una sesión (escribiendo `~/.kodo/logs/<session-id>.ndjson` con head-line `{event: 'session.start', task_id, timestamp}`)
       - Llame removeSession en state.json (o equivalente)
       - Invoque `resolveSessionIdFromTaskId(taskId)` y assert que step-2 resuelva el session_id correctamente

    2. **Si la cobertura YA existe** (esperado): no hacer nada. Documentar en `<done>` con cita de línea de test/logs-session-of.test.js.

    3. **Si la cobertura NO existe** (no esperado): añadir UN it() adicional a `test/session/find-session.test.js` bajo un describe separado `'LIFE-01 sanity — session-lookup step-2 covers archived'` que:
       - mkdir `~/.kodo/logs/` en tmpHome
       - escribe un NDJSON file con head-line `{"event":"session.start","task_id":"task-archived","timestamp":"2026-05-20T00:00:00Z"}`
       - NO añade a state.sessions (simula sesión ya cerrada que dejó NDJSON pero no entry en state)
       - dynamic-imports `resolveSessionIdFromTaskId` de `../../src/logs/session-lookup.js`
       - assert que retorna el session_id correcto (parseado del filename)

    **NO refactorizar `session-lookup.js`** (pitfall #1 recomienda Option A — leave untouched). Solo verificación.

    **NO añadir** scope-creep refactorizando `session-lookup.js` step-1 para usar findSession extendido. Esto es Option B del pitfall y rompe el lookup-key contract (step-1 matchea por task_id|task_ref, findSession matchea por sessionId|workspaceRef|cwd).
  </action>
  <verify>
    <automated>node --test test/logs-session-of.test.js test/session/find-session.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El executor documenta en su SUMMARY si añadió un escenario nuevo o si la cobertura existente es suficiente
    - Si añadió escenario: `node --test test/session/find-session.test.js` exits 0 con 5+ tests
    - Si no añadió: documenta cita exacta (file:line) del test existente que cubre archived NDJSON resolution
    - `src/logs/session-lookup.js` queda intacto (verificable: `git diff src/logs/session-lookup.js` retorna vacío)
    - `npm test` total pass ≥ 825 + 0 fail
  </acceptance_criteria>
  <done>
    SC#1 del ROADMAP queda verificado para ambas CLIs (`kodo gsd verify` via findSession extension; `kodo logs --session-of` via session-lookup step-2). Pitfall #1 cerrado documentalmente en el SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 30 modifica funciones in-process puras sin nuevas superficies externas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Spoofing | `findSession` history scan | N/A | No nueva auth surface — function ya operaba sobre state.json local owned por user; misma trust zone que pre-refactor |
| T-30-02 | Tampering | `state.history` array | accept | state.json es local owned 0600 (heredado de Phase 28 daemon-02 hardening); history items son consultados read-only desde findSession (no muta el array). Mutaciones siguen restringidas a addSession/removeSession (out of scope) |
| T-30-03 | Repudiation | history audit trail | mitigate | history mantiene `ended_at: ISO` timestamp (preservado por removeSession line 134) — findSession exposure incrementa visibilidad sin alterar el audit log |
| T-30-04 | Information disclosure | tagged source field | N/A | `source: 'sessions' \| 'history'` no expone PII nueva — discriminator ya derivable del bucket consulted |
| T-30-05 | Denial of service | findSession iteration cost | accept | history cap 50 entries (state.js line 136 slice(0, 50)) — O(50) lookup trivial. No DoS surface |
| T-30-06 | Elevation of privilege | callers verify.js/session-start.js | N/A | Callers existentes solo leen `r.session` — addition de `source` field es additive, no abre nuevo control flow |

**Net assessment**: Phase 30-01 surface es refactor in-process puro sobre estado local. Most STRIDE dimensions N/A o accept. Zero net-new attack surface.
</threat_model>

<verification>
- Suite global ≥825 pass + 0 fail (D-14 floor)
- `node --test test/session/find-session.test.js` GREEN con ≥4 tests
- `node --test test/session-start.test.js test/gsd-verify-integration.test.js test/stop-state-transition.test.js test/logs-session-of.test.js` GREEN (callers existentes intactos)
- `grep -n "source: 'sessions'\\|source: 'history'" src/session/state.js` retorna ≥2 líneas
- `grep -n "Array.isArray.*history" src/session/state.js` retorna ≥1 línea dentro de findSession body
- `git diff src/logs/session-lookup.js` retorna vacío (Option A pitfall #1)
</verification>

<success_criteria>
SC#1 ROADMAP (`kodo gsd verify` + `kodo logs --session-of` archived sessions): satisfied via findSession extension + session-lookup step-2 NDJSON scan.
SC#3 (4 escenarios test): satisfied por test/session/find-session.test.js.
SC#4 suite ≥825 + CR-01 Phase 19 closed: closure documentada en SUMMARY + STATE.md update (post-phase).
</success_criteria>

<output>
After completion, create `.planning/phases/30-sessionrecord-lifecycle/30-01-SUMMARY.md` documentando:
- LIFE-01 entrega: findSession extendido (sessions + history) con tagged shape
- Test scenarios añadidos (4 mínimo, posible 5 si Task 3 añadió el sanity)
- Pitfall #1 resolution: Option A (untouched session-lookup.js) o Option B (extendido) — con cita del test existente o nuevo
- CR-01 Phase 19 closure note
- Suite global delta (pre vs post)
</output>
