---
phase: 30-sessionrecord-lifecycle
plan: 04
type: execute
wave: 3
depends_on: [30-01]
gap_closure: true
gap_source: HUMAN-UAT Test #2 empirical failure (task_ref LIKEN-113 → "No session found")
files_modified:
  - src/logs/session-lookup.js
  - test/logs-session-of.test.js
autonomous: true
requirements:
  - LIFE-01
requirements_addressed:
  - LIFE-01
tags:
  - session-lookup
  - history-scan
  - sc1-closure
  - gap-closure
must_haves:
  truths:
    - "Step-1 de resolveSessionIdFromTaskId escanea state.sessions Y state.history con misma lógica (task_id || task_ref match)"
    - "Priority sessions sobre history (mismo idiom LIFE-01 D-02)"
    - "Comportamiento idéntico CLI: `kodo logs --session-of LIKEN-113` resuelve tanto sesión viva como archivada"
    - "Suite global ≥882 pass + 0 fail (no degradación vs Wave 2)"
    - "Test añade escenario: sesión archivada en state.history matcheable por task_ref humano"
  artifacts:
    - path: "src/logs/session-lookup.js"
      provides: "Step-1 dual-scan (sessions + history) con priority sessions"
      contains: "state.history"
    - path: "test/logs-session-of.test.js"
      provides: "Nuevo escenario: history-only session resuelta por task_ref"
      contains: "history"
  key_links:
    - from: "src/logs/session-lookup.js resolveSessionIdFromTaskId step-1"
      to: "state.history array"
      via: "loadState().history defensive Array.isArray guard (idéntico patrón LIFE-01 src/session/state.js#213)"
      pattern: "Array\\.isArray\\(.*history\\)"
---

<objective>
Cerrar el gap empírico descubierto en HUMAN-UAT Test #2: `kodo logs --session-of LIKEN-113` falla para sesiones archivadas porque `session-lookup.js` step-1 solo escanea `state.sessions` (no `state.history`). Step-2 (NDJSON head-line scan) solo matchea por `task_id` UUID — no por `task_ref` humano. Resultado: operador con task_ref humano no puede recuperar logs de sesión cerrada — ROADMAP SC#1 Truth 2 literalmente incumplido.

Fix: extender step-1 para escanear ambos buckets (sessions y history) con la misma lógica `task_id || task_ref`. Mismo idiom defensivo que LIFE-01 (`Array.isArray(state.history) ? ... : []`).

Output: `src/logs/session-lookup.js` con step-1 dual-scan + test que cubre el escenario history-only + task_ref humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-sessionrecord-lifecycle/30-HUMAN-UAT.md
@src/logs/session-lookup.js
@src/session/state.js
@test/logs-session-of.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test RED — sesión archivada matcheable por task_ref</name>
  <files>test/logs-session-of.test.js</files>
  <read_first>
    - test/logs-session-of.test.js (líneas 1-150 — patrón existente HOME-isolation + fixtures)
    - src/session/state.js#113-141 (addSession + removeSession para preparar sesión archivada)
  </read_first>
  <behavior>
    - Nuevo escenario: `addSession(taskId, session)` → `removeSession(taskId)` (sesión ahora en history) → `resolveSessionIdFromTaskId(session.task_ref)` debe retornar `session.session_id`.
    - Pre-fix: el test FALLA porque step-1 solo escanea state.sessions y step-2 solo matchea por task_id UUID — task_ref humano no resuelve.
    - Post-fix Task 2: el test PASA.
  </behavior>
  <action>
    Añadir un nuevo `it()` block en test/logs-session-of.test.js bajo un describe nuevo o existente. Estructura sugerida:

    1. Reusar el scaffold HOME-isolation existente del file.
    2. Construir SessionRecord con task_ref tipo `'GAP-30'` (no reusar LIKEN-113 para evitar colisión con fixtures reales).
    3. addSession(task_id, session) — sesión activa.
    4. removeSession(task_id) — sesión ahora en history.
    5. Verificar estado: `loadState().sessions === {}` y `loadState().history` tiene la entry.
    6. Llamar `await resolveSessionIdFromTaskId('GAP-30')` (pasando el task_ref humano).
    7. Assertion: `assert.equal(result, session.session_id)`.

    Describe block sugerido: `'session-lookup step-1 — history scan (LIFE-01 closure)'` o equivalente que ubique el test contextualmente.

    NO modificar tests existentes — solo añadir el nuevo escenario.
  </action>
  <verify>
    <automated>node --test test/logs-session-of.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El nuevo test FALLA pre-fix (RED) con expected = session_id, actual = null
    - El test PASA post-fix (Task 2 GREEN)
    - Tests existentes en el file siguen pasando
  </acceptance_criteria>
  <done>
    Test RED escrito que demuestra el gap empírico encontrado en HUMAN-UAT.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender step-1 a state.history en session-lookup.js</name>
  <files>src/logs/session-lookup.js</files>
  <read_first>
    - src/logs/session-lookup.js (líneas 1-90 completas)
    - src/session/state.js#208-253 (findSession LIFE-01 — analog dual-scan pattern)
  </read_first>
  <behavior>
    - Pre-fix: step-1 solo itera Object.values(sessions). Sesión en history → no match → cae a step-2 → step-2 solo matchea por UUID → null.
    - Post-fix: step-1 itera primero sessions (priority), luego history. Match por task_id O task_ref en ambos buckets.
  </behavior>
  <action>
    Refactor de `resolveSessionIdFromTaskId` en `src/logs/session-lookup.js` líneas 29-39:

    Estructura del nuevo step-1 (sin código fenced — descripción directiva):

    1. `const state = loadState();`
    2. `const sessions = state.sessions || {};` (preservado).
    3. `const history = Array.isArray(state.history) ? state.history : [];` (defensive guard idéntico a LIFE-01 findSession).
    4. **Priority sessions (LIFE-01 D-02 idiom)**: iterar `Object.values(sessions)`. Para cada `sess`, si `sess.task_id === taskId || sess.task_ref === taskId` → return `sess.session_id`.
    5. Después iterar `history` array. Para cada `entry`, mismo check `entry.task_id === taskId || entry.task_ref === taskId` → return `entry.session_id`.
    6. Si ningún match en ambos buckets, fall through a step-2 (NDJSON scan, sin cambios).

    Opcional (Claude's Discretion): extraer helper privado `findInBucket(bucket, taskId)` si el body se vuelve ilegible. Bucket puede ser `Object.values(sessions)` o `history`. No requiere decisión externa.

    **JSDoc updates (Claude's Discretion):** mencionar en el comentario header del file que step-1 ahora escanea ambos buckets (sessions + history), análogo a LIFE-01 findSession.

    **NO tocar** step-2 (NDJSON scan) — preserve para sesiones huérfanas (en logs pero no en state.json, e.g. orphan logs tras crash o legacy cleanup).
  </action>
  <verify>
    <automated>node --test test/logs-session-of.test.js test/session-of-resolver.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Array.isArray.*history" src/logs/session-lookup.js` retorna ≥1
    - El test RED de Task 1 pasa a GREEN
    - `node --test test/logs-session-of.test.js` exits 0
    - `node --test test/session-of-resolver.test.js` exits 0 (regression — sesión activa sigue resolviendo correctamente)
    - `npm test` total ≥882 pass + 0 fail (no degradación vs Wave 2)
    - Empirical re-test: `node bin/kodo logs --session-of LIKEN-113` retorna logs (no "No session found") — verificación manual post-fix por el orchestrator.
  </acceptance_criteria>
  <done>
    SC#1 Truth 2 cerrado byte-exact: `kodo logs --session-of <task-ref>` opera idéntico para sesiones vivas Y archivadas.
  </done>
</task>

</tasks>

<verification>
- `npm test` GREEN ≥882 pass + 0 fail
- `node bin/kodo logs --session-of LIKEN-113` retorna logs reales (no "No session found") — re-test empírico
- `grep -c "Array.isArray.*history" src/logs/session-lookup.js` retorna ≥1
- Tests existentes (logs-session-of.test.js + session-of-resolver.test.js) GREEN sin cambios
</verification>

<success_criteria>
SC#1 Truth 2 ROADMAP cumplido byte-exact (comportamiento idéntico CLI entre sesiones vivas y archivadas para `kodo logs --session-of <task-ref>`).
</success_criteria>

<output>
After completion, create `.planning/phases/30-sessionrecord-lifecycle/30-04-SUMMARY.md` documentando:
- HUMAN-UAT Test #2 gap closure: step-1 dual-scan implementado
- Test RED→GREEN cycle
- Re-test empírico CLI: `kodo logs --session-of LIKEN-113` ahora funciona
- Suite global delta
</output>
