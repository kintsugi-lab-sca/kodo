---
phase: 29-gsd-provider-reporting-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 29-03
  - 29-01
files_modified:
  - src/orchestrator/prompt.md
  - test/orchestrator-gsd.test.js
  - .planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md
autonomous: true
requirements:
  - REPORT-04
  - REPORT-06
user_setup: []

must_haves:
  truths:
    - "Operador con `workflow.report_to_provider: true` lanza una sesión GSD y verifica que el prompt renderizado contiene la prosa ES de Sub-issue reporting entre markers, cubriendo los 6 conceptos canónicos: just-in-time creation, label `kodo:gsd-child` (via constante), comentarios plan-by-plan, lifecycle abstracto, append-only `NUNCA delete-issue`, HARD STEP pre-transición."
    - "Prosa provider-agnostic via `{{provider_name}}` placeholder — paréntesis pragmático canónico `(en {{provider_name}}: In Progress / In Review / Done)` presente byte-exact."
    - "Logs literales byte-exact: `[kodo:reporting] MCP failure on phase N: <error>` (con colon, sin em-dash) y `[kodo:reporting] Provider MCP lacks sub-issue capability — reporting disabled` (con em-dash U+2014)."
    - "Cherry-pick traceability completa: 9 SHAs aplicados a lo largo de los 4 plans, todos con trailer `(cherry picked from commit <sha>)` o nota `[manual reapply of <sha>]` en commit messages."
    - "Cuando `workflow.report_to_provider` está en `false`/`undefined`/missing, el prompt rendered NO contiene ningún string del bloque reporting (RA1..RA6 anti-leak asserts)."
    - "Cross-phase coupling alive: `KODO_LABEL_GSD_CHILD === 'kodo:gsd-child'` (defensive runtime check)."
  artifacts:
    - path: "src/orchestrator/prompt.md"
      provides: "Prosa ES de ~65 líneas dentro de los markers reemplazando el placeholder de 29-03"
      contains: "Sub-issue reporting"
    - path: "test/orchestrator-gsd.test.js"
      provides: "Append 2 describes: `REPORT-04..08 — Sub-issue reporting block content` (15 tests RC1..RC15) + `REPORT-03 — Sub-issue reporting block ABSENT when flag=false` (6 tests RA1..RA6)"
      contains: "RC1"
    - path: ".planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md"
      provides: "Phase-level VERIFICATION.md cubriendo los 5 SC observables del ROADMAP + reconciliación REPORT-01..06 traceability (D-21 + REPORT-06 satisfacción) + 9 SHAs audit trail"
      contains: "Phase 29 Verification"
  key_links:
    - from: "test/orchestrator-gsd.test.js"
      to: "src/labels.js#KODO_LABEL_GSD_CHILD"
      via: "cross-phase coupling import (Plan 29-01 → Plan 29-04)"
      pattern: "import \\{ KODO_LABEL_GSD_CHILD \\} from '\\.\\./src/labels\\.js'"
    - from: "test/orchestrator-gsd.test.js"
      to: "src/orchestrator/launch.js#applyReportingGate"
      via: "block extraction via indexOf BEGIN/END"
      pattern: "applyReportingGate\\(resolvePromptTemplate"
    - from: "src/orchestrator/prompt.md (prosa)"
      to: "src/labels.js#KODO_LABEL_GSD_CHILD"
      via: "RC1 cross-phase coupling assert"
      pattern: "kodo:gsd-child"
---

<objective>
Reemplazar el placeholder de 29-03 con la prosa ES de ~65 líneas que constituye el contenido real del bloque "Sub-issue reporting" + cargar los 21 tests heredados (RC1..RC15 content asserts + RA1..RA6 anti-leak asserts) que blindan ese contenido contra drift futuro. Cerrar phase con VERIFICATION.md phase-level cubriendo los 5 SC del ROADMAP + final suite gate explícito.

Purpose: cumplir REPORT-04 (sub-issue reporting prose) + REPORT-06 transversalmente (cherry-pick traceability de los 9 SHAs + planning artifacts regenerados). Cerrar T-29-04 content drift threat con asserts byte-level sobre cada concepto canónico. Aceptar explícitamente T-29-05 (prompt injection upstream) como riesgo de diseño instruction-driven.

Output: 1 archivo `src/` modificado (manual reapply de `d030547` por colisión de prompt.md drift) + 1 archivo `test/` appended (cherry-pick `81c848c` carga RC + RA en `test/orchestrator-gsd.test.js`) + 1 VERIFICATION.md nuevo + 1 final suite gate task. Suite global gate: **≥818 pass + 0 fail + 1 skip preservado** (D-22 floor SC#5 canonical assertion; D-22 target real ≥844 si baseline acumulada lo soporta).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PENDING-INTEGRATIONS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md
@.planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md
@.planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-VALIDATION.md
@.planning/phases/29-gsd-provider-reporting-integration/29-01-PLAN.md
@.planning/phases/29-gsd-provider-reporting-integration/29-02-PLAN.md
@.planning/phases/29-gsd-provider-reporting-integration/29-03-PLAN.md

<interfaces>
<!-- Outputs that downstream verification will rely on -->

Prosa ES en `src/orchestrator/prompt.md` entre markers (reemplaza placeholder de 29-03):
- 6 conceptos canónicos: just-in-time creation, label `kodo:gsd-child` (mención literal aparece exactamente 2 veces en el bloque — Crear + Dedup), comentarios plan-by-plan, lifecycle abstracto, append-only `NUNCA delete-issue`, HARD STEP pre-transición.
- Provider-agnostic via `{{provider_name}}` placeholder; paréntesis pragmático byte-exact `(en {{provider_name}}: In Progress / In Review / Done)` sustituido por `resolvePromptTemplate` antes del gate.
- Logs literales byte-exact en el bloque (RC10/RC11).
- Body fields literales `Goal:`, `PLAN dir:`, `Plans:` (RC13).
- PM7 invariant: NO frases inglesas dentro del bloque.

Tests RC1..RC15 + RA1..RA6 en `test/orchestrator-gsd.test.js` — 21 tests que assertean cada concepto + las 6 ausencias cuando flag=false.

VERIFICATION.md phase-level con shape paralelo a Phase 28 VERIFICATION.md (5 SC del ROADMAP × verdict + assertion source + 9 SHAs audit trail).

Final suite gate (Task 4): `npm test` pass count ≥818 absolute floor (D-22 SC#5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Manual reapply de `d030547` (prosa ES de ~65 líneas reemplaza placeholder en prompt.md)</name>
  <files>src/orchestrator/prompt.md</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Drift Inventory" §prompt.md (severity SEVERE — manual reapply obligatorio por Phase 999.1 rewrite) + §"Open Questions (RESOLVED)" Q1 (squash vs separated — preservar separated por audit trail)
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 4" §"What to Add" → "Plan 29-04 reemplaza placeholder con prosa ES de 65 líneas" + §"Invariants the implementation MUST preserve" (la lista completa de invariantes byte-level)
    - src/orchestrator/prompt.md (estado post-29-03: contiene markers + heading + placeholder `_Section body added in plan 29-04._` — placeholder con numeración Phase 29, NO `15-02`)
    - git show gsd-provider-reporting:src/orchestrator/prompt.md (HEAD del branch — contiene la prosa ES final completa entre markers, ~65 líneas)
    - git show d030547 -- src/orchestrator/prompt.md (diff que muestra placeholder → prosa)
    - CONTEXT D-15 (6 conceptos canónicos provider-agnostic via {{provider_name}}) + D-16 (RC1..RC15 + RA1..RA6 anti-leak)
  </read_first>
  <action>
NO usar `git cherry-pick d030547` — depende del placeholder de `7c28c06` que se aplicó en 29-03 con manual reapply, y el patch context divergió. RESEARCH §"Drift Inventory" §prompt.md instruye manual reapply (D-24-2).

Manual reapply:

1. Inspeccionar `git show gsd-provider-reporting:src/orchestrator/prompt.md` y extraer el bloque completo entre `<!-- BEGIN reporting -->` y `<!-- END reporting -->` (incluyendo los markers). En HEAD del branch este bloque contiene la prosa ES final de ~65 líneas con los 6 conceptos canónicos.

2. En `src/orchestrator/prompt.md` actual (post-29-03), localizar el bloque completo `<!-- BEGIN reporting -->...<!-- END reporting -->` (que ahora contiene solo el placeholder `_Section body added in plan 29-04._`) y reemplazarlo enteramente por el bloque extraído del branch HEAD en paso 1.

3. Verificar invariantes byte-level (PATTERNS §"Touch File 4 → Invariants"):
   - Markers siguen únicos (1 BEGIN + 1 END).
   - Heading `## Sub-issue reporting` ENTRE markers (SR3).
   - `indexOf(BEGIN) > indexOf('## Sesiones GSD')` (SR2 topology).
   - Provider-agnostic via `{{provider_name}}` placeholder.
   - Paréntesis pragmático canónico byte-exact: `(en {{provider_name}}: In Progress / In Review / Done)` (ISSUE-29-010 invariant).
   - `kodo:gsd-child` aparece exactamente 2 veces en el bloque (RC1 + RC14 dedup mention).
   - `HARD STEP` capitalizado + `NUNCA` capitalizado cerca de `delete-issue` (≤200 chars distance) — RC7.
   - Log literals byte-exact:
     - `[kodo:reporting] MCP failure on phase N: <error>` con colon, sin em-dash (RC10).
     - `[kodo:reporting] Provider MCP lacks sub-issue capability — reporting disabled` con em-dash U+2014 (RC11).
   - Body fields: `Goal:`, `PLAN dir:`, `Plans:` presentes (RC13 D-12).
   - Lifecycle vocabulary: `in progress`, `done`, `verified` (RC5) + `cancelled` (RC8) presentes.
   - Quick clarification: en quick sessions NO crear sub-issue (RC12).
   - Literal canónico: `Plan = comentario. Phase = sub-issue.` (RC4).
   - PM7 invariant: NO `you must` / `please` / `execute your` dentro del bloque (SR6 / RC15).
   - Placeholder eliminado: `grep -c "Section body added in plan 29-04" src/orchestrator/prompt.md` retorna 0 post-reapply.

4. Crear commit con message: `feat(29-04): replace placeholder with full ES prose for sub-issue reporting [manual reapply of d030547, prose inserted in 29-04 over the placeholder from 29-03]`.

Validación post-write:
- Conteo de líneas: el archivo total debe haber crecido ~65 líneas (39 baseline + ~7 placeholder/markers de 29-03 + ~58 delta prose).
- `grep -c 'kodo:gsd-child' src/orchestrator/prompt.md` ≥1 (la mención dentro del bloque) — confirmar que NO viola REPORT-05 source-hygiene porque (a) `prompt.md` no es `.js` así que el walker de 29-01 lo excluye por extension filter, (b) el contenido es prosa instructiva, no código.

**Acceptance shell pipeline notes:**
- El check de provider-agnostic NO se hace via fragile grep `(^|[^{])Plane([^_]|$)` (ISSUE-29-007 removed). La invariante "no inline Plane/GitHub/ClickUp fuera del paréntesis pragmático" queda cubierta por los RC tests de Task 2 (`test/orchestrator-gsd.test.js`), no por shell asserts en este verify.
- El paréntesis pragmático canónico SÍ se asserta byte-exact con `grep -Eq "\\(en \\{\\{provider_name\\}\\}: In Progress / In Review / Done\\)"` (ISSUE-29-010 byte-test).
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -q "manual reapply of d030547" && [ "$(grep -c 'BEGIN reporting' src/orchestrator/prompt.md)" = "1" ] && [ "$(grep -c 'END reporting' src/orchestrator/prompt.md)" = "1" ] && grep -q "HARD STEP" src/orchestrator/prompt.md && grep -q "NUNCA" src/orchestrator/prompt.md && grep -q "delete-issue" src/orchestrator/prompt.md && grep -Eq "Plan = comentario\. Phase = sub-issue\." src/orchestrator/prompt.md && grep -q "\[kodo:reporting\] MCP failure on phase N:" src/orchestrator/prompt.md && ! grep -q "Section body added in plan 29-04" src/orchestrator/prompt.md && grep -Eq "\(en \{\{provider_name\}\}: In Progress / In Review / Done\)" src/orchestrator/prompt.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene `manual reapply of d030547`.
    - Placeholder eliminado: `grep -c "Section body added in plan 29-04" src/orchestrator/prompt.md` retorna 0 (ISSUE-29-001 inverted-logic fix: verify usa `! grep -q ...` que falla con exit 1 si el placeholder permanece).
    - Conceptos canónicos presentes:
      - `grep -c 'HARD STEP' src/orchestrator/prompt.md` ≥1.
      - `grep -c 'NUNCA' src/orchestrator/prompt.md` ≥1.
      - `grep -c 'delete-issue' src/orchestrator/prompt.md` ≥1.
      - `grep -Ec 'Plan = comentario\. Phase = sub-issue\.' src/orchestrator/prompt.md` ≥1 (RC4).
      - `grep -c 'in progress' src/orchestrator/prompt.md` ≥1 (RC5 lifecycle).
      - `grep -c 'cancelled' src/orchestrator/prompt.md` ≥1 (RC8).
      - `grep -Ec '\[kodo:reporting\] MCP failure on phase N:' src/orchestrator/prompt.md` ≥1 (RC10 log literal).
      - `grep -Ec '\[kodo:reporting\] Provider MCP lacks sub-issue capability' src/orchestrator/prompt.md` ≥1 (RC11 log literal).
    - **Paréntesis pragmático canónico byte-exact (ISSUE-29-010):** `grep -Eq "\(en \{\{provider_name\}\}: In Progress / In Review / Done\)" src/orchestrator/prompt.md` exit 0.
    - **Provider-agnostic enforcement (ISSUE-29-007):** la antigua aserción shell `grep -E '(^|[^{])Plane([^_]|$)' ...` queda **deferred al test layer** — RC tests en `test/orchestrator-gsd.test.js` cargados en Task 2 cubren la invariante. NO se incluye shell pipeline frágil en este verify.
    - Markers únicos preservados; topology preservada (Sesiones GSD antes de BEGIN reporting).
    - Suite ≥818 pass (floor SC#5 D-22 canonical assertion) sin nuevos tests aún — los tests entran en Task 2; 0 fail; 1 skip preservado.
  </acceptance_criteria>
  <done>Prosa ES de ~65 líneas insertada entre markers reemplazando placeholder; commit message documenta manual reapply de `d030547`; todos los conceptos canónicos + log literals + paréntesis pragmático byte-exact presentes; placeholder original eliminado (ISSUE-29-001 verify ahora fail-fast si placeholder permanece); suite ≥818 pass + 0 fail + 1 skip preservado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cherry-pick `81c848c` (RC1..RC15 + RA1..RA6 — 21 tests append a test/orchestrator-gsd.test.js)</name>
  <files>test/orchestrator-gsd.test.js</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Test File Mapping" (`test/orchestrator-gsd.test.js` existe, 6 describes existentes incluyendo QUICK-08 source hygiene line 239-271) + §"Pitfall 4" (verificación de no-overlap con tests pre-existentes — ya confirmado safe)
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 10 Sub-touch C: test/orchestrator-gsd.test.js (append RC1..RC15 + RA1..RA6)" (descripción completa de los 21 tests + setup compartido con `applyReportingGate` + `resolvePromptTemplate` + `KODO_LABEL_GSD_CHILD` import)
    - test/orchestrator-gsd.test.js (estado actual main: 271 líneas, 6 describes existentes incluyendo `prompt.md — sección GSD renderizada` lines 6-44 + `QUICK-08 — launch.js source hygiene` lines 239-271)
    - git show 81c848c -- test/orchestrator-gsd.test.js
    - src/orchestrator/prompt.md (post-Task-1: contiene la prosa ES — RC1..RC15 corren sobre este contenido vía `applyReportingGate(resolvePromptTemplate(raw, {provider:'plane'}), true)`)
    - src/labels.js (post-29-01: export `KODO_LABEL_GSD_CHILD` — RC1 cross-phase coupling lo importa)
    - CONTEXT D-16 (RC1..RC15 + RA1..RA6 anti-leak)
  </read_first>
  <action>
Aplicar `git cherry-pick 81c848c` sobre HEAD post-Task-1. Drift severity Clean per RESEARCH §"Test File Mapping" — append puro a archivo de 271 líneas con 6 describes existentes.

Contenido esperado del cherry-pick (PATTERNS §"Touch File 10 Sub-touch C → What to Add"):

Imports añadidos (top of file o agrupados con existentes):
- `import { applyReportingGate, resolvePromptTemplate } from '../src/orchestrator/launch.js';`
- `import { KODO_LABEL_GSD_CHILD } from '../src/labels.js';` (cross-phase coupling RC1 — depende de Plan 29-01)

2 describes appended al final del archivo:

1. `describe('REPORT-04..08 — Sub-issue reporting block content', () => { ... })` con 15 tests RC1..RC15:
   - Setup: `const raw = readFileSync('src/orchestrator/prompt.md', 'utf-8'); const resolved = applyReportingGate(resolvePromptTemplate(raw, { provider: 'plane' }), true); const beginIdx = resolved.indexOf('<!-- BEGIN reporting -->'); const endIdx = resolved.indexOf('<!-- END reporting -->'); const block = resolved.substring(beginIdx, endIdx);`
   - RC1: sanity `KODO_LABEL_GSD_CHILD === 'kodo:gsd-child'` + `block.includes(KODO_LABEL_GSD_CHILD)`. Cross-phase coupling con Plan 29-01.
   - RC2..RC15: assertions sobre `block` (provider-agnostic / lifecycle / NUNCA-delete-issue / HARD STEP / log literals / body fields / quick clarification / dedup / PM7 sub-scoped).

2. `describe('REPORT-03 — Sub-issue reporting block ABSENT when flag=false', () => { ... })` con 6 tests RA1..RA6:
   - Setup: `const stripped = applyReportingGate(resolvePromptTemplate(raw, { provider: 'plane' }), false);`
   - RA1: `!stripped.includes('Sub-issue reporting')`.
   - RA2: `!stripped.includes('kodo:gsd-child')`.
   - RA3: `!/\bNUNCA\b/.test(stripped)`.
   - RA4: `!/HARD STEP/.test(stripped)`.
   - RA5: `!stripped.includes('[kodo:reporting]')`.
   - RA6: `stripped.includes('## Sesiones GSD')` + `kodo gsd verify <session-id>` + `Sesiones quick` (preservation invariant).

Si Git produce conflict (improbable per RESEARCH drift Clean), manual append per CONTEXT D-24-1 (trivial whitespace en final del archivo).

**Defensive guard (ISSUE-29-011 cross-phase coupling check):**

Antes de declarar Task 2 verde, ejecutar runtime check que `KODO_LABEL_GSD_CHILD` siga en HEAD desde Plan 29-01 con su valor canónico:

`node -e "import('./src/labels.js').then(m => process.exit(m.KODO_LABEL_GSD_CHILD === 'kodo:gsd-child' ? 0 : 1))"`

Si exit ≠ 0, abortar el cherry-pick — significa que algún plan posterior accidentalmente revirtió o mutó la constante de 29-01. Plan 29-04 NO debe avanzar con cross-phase coupling roto.

Validación post-cherry-pick:
- `npm test -- test/orchestrator-gsd.test.js` exit 0; +21 tests vs baseline.
- Los 21 tests pasan porque (a) prosa ES está en place (Task 1), (b) markers + helper en place (29-03), (c) `KODO_LABEL_GSD_CHILD` exportado (29-01 — verificado por el defensive guard).
- 0 regresiones en los 6 describes existentes.
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -Eq "(cherry picked from commit 81c848c|cherry-picked from 81c848c)" && grep -q "KODO_LABEL_GSD_CHILD" test/orchestrator-gsd.test.js && grep -q "REPORT-04" test/orchestrator-gsd.test.js && grep -Eqi "RA[1-6]" test/orchestrator-gsd.test.js && grep -Eqi "RC[1-9]|RC1[0-5]" test/orchestrator-gsd.test.js && npm test -- test/orchestrator-gsd.test.js 2>&1 | tail -20 | grep -Eq "pass [0-9]+" && node -e "import('./src/labels.js').then(m => process.exit(m.KODO_LABEL_GSD_CHILD === 'kodo:gsd-child' ? 0 : 1))"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene `cherry picked from commit 81c848c`.
    - `grep -c "KODO_LABEL_GSD_CHILD" test/orchestrator-gsd.test.js` ≥1 (cross-phase import RC1).
    - `grep -c "applyReportingGate" test/orchestrator-gsd.test.js` ≥1 (setup compartido).
    - 15 `it(` blocks con prefix `RC` presentes: `grep -cE "it\('RC[0-9]" test/orchestrator-gsd.test.js` ≥15.
    - 6 `it(` blocks con prefix `RA` presentes: `grep -cE "it\('RA[1-6]" test/orchestrator-gsd.test.js` ≥6.
    - **ISSUE-29-011 defensive guard:** `node -e "import('./src/labels.js').then(m => process.exit(m.KODO_LABEL_GSD_CHILD === 'kodo:gsd-child' ? 0 : 1))"` exit 0 — Plan 29-01 SHA sigue en HEAD con valor canónico.
    - `npm test -- test/orchestrator-gsd.test.js` exit 0; +21 tests vs baseline pre-29-04.
    - Suite ≥818 pass + 0 fail + 1 skip preservado (D-22 floor SC#5 canonical assertion per ISSUE-29-009 — evita arithmetic drift entre plans).
    - 0 regresiones.
  </acceptance_criteria>
  <done>Cherry-pick `81c848c` aplicado; 21 tests (15 RC + 6 RA) verdes en `test/orchestrator-gsd.test.js`; cross-phase coupling con Plan 29-01 (`KODO_LABEL_GSD_CHILD` import + runtime guard) operacional; suite ≥818 pass + 0 fail + 1 skip preservado; 0 regresiones.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear VERIFICATION.md phase-level (REPORT-06 satisfacción transversal)</name>
  <files>.planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 29: GSD Provider Reporting Integration" → Success Criteria (5 items observables)
    - .planning/phases/28-polling-daemon-hardening/VERIFICATION.md (shape canónico v0.8 — referente estructural per CONTEXT D-21)
    - .planning/REQUIREMENTS.md §REPORT (acceptance criteria REPORT-01..06)
    - .planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md §D-19/D-20/D-21 (planning artifacts regen instructions)
    - .planning/phases/29-gsd-provider-reporting-integration/29-VALIDATION.md (per-task verification map)
    - 29-01-PLAN.md, 29-02-PLAN.md, 29-03-PLAN.md, 29-04-PLAN.md (este archivo) + sus SUMMARYs (si ya escritos)
  </read_first>
  <action>
Crear `.planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md` phase-level con shape paralelo a `.planning/phases/28-polling-daemon-hardening/VERIFICATION.md` (CONTEXT D-21). Contenido mínimo (organizado en secciones):

1. **Frontmatter YAML**: `phase: 29`, `slug: gsd-provider-reporting-integration`, `verdict: pass` (o el verdict que aplique post-suite), `verified: <YYYY-MM-DD>`, `nyquist_compliant: true`.

2. **Sección "Goal observable"**: copiar literal el Goal de ROADMAP Phase 29 (líneas 39-40).

3. **Sección "Success Criteria"**: para cada uno de los 5 SC del ROADMAP, una entry con:
   - Restatement literal del SC.
   - Verdict: pass/fail.
   - Assertion source: ruta + identifier exacto del test o comando que lo verifica.
   - Evidencia: salida del comando o referencia al SUMMARY del plan que cierra el SC.
   - Mapeo a los SC:
     - SC#1 (anti-recursión observable): `test/dispatcher.test.js` describe `REPORT-01 — kodo:gsd-child anti-recursion filter` (6 behavior tests) + log line literal en código + `awk` estructural `filterIdx < forceIdx`. Asociado a 29-01.
     - SC#2 (opt-in observable): SR1..SR6 en `test/prompt.test.js` (29-03) + RC1..RC15 / RA1..RA6 en `test/orchestrator-gsd.test.js` (29-04) + manual UAT en VALIDATION.md.
     - SC#3 (source-hygiene): `test/labels-hygiene.test.js` (29-01) + `test/config.test.js` source-hygiene describe (29-02) + manual smoke `grep -rE "'kodo:gsd-child'" src/ | grep -v src/labels.js`.
     - SC#4 (cherry-pick traceability + planning regen): `git log --grep="cherry picked from commit\|manual reapply of" --since="2026-05-20" | wc -l` ≥9 + presencia de 4 PLAN.md + 4 SUMMARY.md + VERIFICATION.md + VALIDATION.md.
     - SC#5 (suite ≥818 pass — D-22 canonical floor): `npm test` output count, gate explícito en Task 4 de este plan.

4. **Sección "REPORT-01..06 traceability reconciliation"**: tabla mapeando cada REQ-ID a su plan + tests + assertion command. Verifica que los 6 REQ-IDs estén COVERED.

5. **Sección "Cherry-pick audit trail"**: lista de los 9 SHAs con su plan asociado y método (literal cherry-pick vs manual reapply per D-24/D-25):
   - 29-01: `5a41d8f` (literal), `cbd8f9c` (literal con minor import resolve)
   - 29-02: `e1f82c9` (manual reapply — insertion above `getDefaultGithubProviderConfig`)
   - 29-03: `7c28c06` (manual reapply — Phase 999.1 rewrite, placeholder renumbered `15-02` → `29-04`), `5feb578` (literal o 3-hunk manual), `38c7a2e` (literal), `4d67312` (literal)
   - 29-04: `d030547` (manual reapply — depends on 29-03 placeholder), `81c848c` (literal)

6. **Sección "Deviations from cherry-pick literal"** (per D-25 — toda desviación documentada): para cada manual reapply, listar archivo + razón + commit hash final en main.

7. **Sección "0 regresiones + 0 nuevos skips"**: confirmar 1 skip pre-existente preservado.

8. **Phase complete signoff**: bullet final con la línea `**Phase 29 complete:** <fecha> — <suma de tests> pass + 1 skip + 0 fail; 9 SHAs integrados; 4 PLAN.md + 4 SUMMARY.md + VERIFICATION.md + VALIDATION.md committed.`

Commit message sugerido: `docs(29): phase-level VERIFICATION.md — 5 SC verified + REPORT-01..06 traceability + 9 SHAs audit trail`.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md && grep -q "Phase 29" .planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md && grep -Eq "REPORT-0[1-6]" .planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md && [ "$(grep -Ec '5a41d8f|cbd8f9c|e1f82c9|7c28c06|5feb578|38c7a2e|d030547|4d67312|81c848c' .planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md)" -ge 9 ] && grep -Eq "SC.?1|Success Criter" .planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `.planning/phases/29-gsd-provider-reporting-integration/VERIFICATION.md` existe.
    - Frontmatter YAML con `phase: 29`, `verdict`, `verified`, `nyquist_compliant: true`.
    - Los 5 SC del ROADMAP están enumerados con verdict + assertion source.
    - Los 6 REQ-IDs REPORT-01..06 están listados en sección traceability con tests asociados.
    - Los 9 SHAs están listados en sección audit trail con método (literal vs manual reapply).
    - Cherry-pick traceability comando documentado: `git log --grep="cherry picked from commit\|manual reapply of" --since="2026-05-20" | wc -l` retorna ≥9 (lo cual cierra SC#4).
    - 0 regresiones + 0 nuevos skips + 1 skip pre-existente confirmado.
  </acceptance_criteria>
  <done>VERIFICATION.md phase-level creado con shape canónico v0.8; 5 SC con verdict; REPORT-01..06 traceability reconciliada; 9 SHAs audit trail documentado; deviations per D-25 listadas; phase signoff con conteo final de suite.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Phase 29 final suite gate (ISSUE-29-003 SC#5 floor enforcement)</name>
  <files>(none — runtime gate; no file modifications)</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md §D-22 (target real ≥844; floor mínimo ≥818)
    - .planning/ROADMAP.md §"Phase 29" → Success Criteria SC#5 (suite ≥818 pass)
    - .planning/phases/29-gsd-provider-reporting-integration/29-VALIDATION.md §"Per-Task Verification Map" task 29-04-04
  </read_first>
  <action>
Phase-gate runtime task que ejecuta el suite completo y extrae el pass count programáticamente. Este task absorbe la antigua tarea meta `29-05-02 (suite count)` consolidando el gate en el cierre del plan 29-04 (per VALIDATION.md update).

Pasos:

1. Ejecutar suite completa: `npm test`.
2. Extraer pass count del footer estándar de `node:test` (formato canónico: footer con `# pass <N>`).
3. Comparar contra el floor absolute D-22 SC#5: `PASS_COUNT ≥ 818`.
4. Si el conteo es ≥844 (target real D-22), registrar evidencia adicional en `29-04-SUMMARY.md` post-task para marcar "target met" — opcional pero útil para audit.
5. Si pass count < 818 → suite gate FAIL → Plan 29-04 no se cierra hasta diagnosticar (CONTEXT D-23: detener y diagnosticar antes de avanzar).
6. Verificar también: 0 fail + 1 skip preservado (no nuevos skips).

NO se modifica ningún archivo de producción en este task. Es un runtime check final.
  </action>
  <verify>
    <automated>PASS_COUNT=$(npm test 2>&1 | grep -E "^# pass" | grep -oE "[0-9]+" | head -1); test -n "$PASS_COUNT" && test "$PASS_COUNT" -ge 818 && echo "OK suite ≥818 (got $PASS_COUNT)" || (echo "FAIL — pass count $PASS_COUNT < 818 floor" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit 0.
    - Pass count extraído del footer `# pass <N>` (node:test canonical output) ≥ 818 (floor SC#5 D-22 canonical assertion per ISSUE-29-009).
    - Ideally ≥844 (target real D-22) — if achieved, document in `29-04-SUMMARY.md` as "target met".
    - 0 fail, 1 skip preservado (no nuevos skips).
    - Si el suite gate falla, NO se progresa al siguiente paso; Plan 29-04 queda blocked hasta diagnóstico (CONTEXT D-23).
  </acceptance_criteria>
  <done>Suite final ejecutado; pass count programático extraído del footer node:test; floor SC#5 D-22 (≥818) verified; 0 fail + 1 skip preservado; phase 29 gate cerrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/orchestrator/prompt.md` (prosa ES) → agente Claude vía cmux | Frontier de instrucciones: la prosa instruye al agente a crear sub-issues con label `kodo:gsd-child` via su MCP. Si el contenido drift (typo, traducción accidental, removal del HARD STEP), el agente puede crear sub-issues malformados o omitir validaciones. |
| `task.description_markdown` (user-controlled) → agente crea sub-issue body con contenido derivado | Frontier de prompt injection upstream: contenido user-controlled del task padre puede contener instrucciones que el agente incorpore al sub-issue body. Out of scope para este plan (instruction-driven fail-open by design). |
| Cross-phase coupling Plan 29-01 → Plan 29-04 | Frontier de import: `test/orchestrator-gsd.test.js` importa `KODO_LABEL_GSD_CHILD` desde `src/labels.js`. Si Plan 29-01 SHA queda revertido pero Plan 29-04 SHA permanece, los tests rompen en suite — defensive coupling reforzado con runtime guard Task 2 (ISSUE-29-011). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering (content drift in reporting prose) | `src/orchestrator/prompt.md` bloque entre markers | mitigate | 21 tests RC1..RC15 + RA1..RA6 byte-level. RC1 cross-phase coupling con `KODO_LABEL_GSD_CHILD` constante (rompe si Phase 14 D-09 cambia el value). RC10/RC11 byte-exact log literals (em-dash vs colon). RC7 HARD STEP ↔ NUNCA delete-issue proximity ≤200 chars. PM7 sub-scoped (RC15). Paréntesis pragmático byte-test (ISSUE-29-010). **MEDIUM severity** — drift sin tests permitiría agente crear sub-issues malformados sin CI rojo. |
| T-29-05 | Spoofing (prompt injection en sub-issue body via task.description_markdown user-controlled) | Agente Claude crea sub-issue body desde task padre | accept | **Accepted risk by design** (CONTEXT §"Specifics" + instruction-driven principle). Out of scope: fail-open implica que el agente confía en su propio criterio para no inyectar contenido malicioso derivado de la task. kodo NO valida formato/contenido del sub-issue. Si emerge demanda real (incident-driven), futura phase puede añadir validator sub-issue → CONTEXT §"Deferred Ideas". Documentado en VERIFICATION.md sección "Accepted Risks by Design". |
| T-29-PA | Tampering (provider-agnostic violation) | `src/orchestrator/prompt.md` prosa | mitigate | Tests assertean: (a) `{{provider_name}}` placeholder presente (RC subset), (b) NO menciones inline `Plane`/`GitHub`/`ClickUp` fuera del paréntesis pragmático (RC subset, ya NO en shell pipeline frágil — ISSUE-29-007 fix), (c) paréntesis canónico byte-exact (Task 1 verify ISSUE-29-010), (d) `resolvePromptTemplate` sustituye antes del gate (LH3 en 29-03 verifica composition order). **MEDIUM severity**. |
| T-29-COVERAGE | Information Disclosure (REPORT-06 traceability gap) | Audit trail post-phase | mitigate | VERIFICATION.md Task 3 lista los 9 SHAs + método; `git log --grep` comando documentado; 4 PLAN.md + 4 SUMMARY.md presentes; VALIDATION.md ya escrito. **LOW severity** (documental). |
| T-29-CROSSREV | Tampering (Plan 29-01 SHA reverted while 29-04 SHA permanece) | Cross-phase coupling import | mitigate | Task 2 defensive guard (ISSUE-29-011): `node -e "import('./src/labels.js').then(m => process.exit(m.KODO_LABEL_GSD_CHILD === 'kodo:gsd-child' ? 0 : 1))"`. Si la constante de 29-01 falta o cambió, exit ≠ 0 y Plan 29-04 verify FAIL. **MEDIUM severity** — protege la cadena cross-phase. |

</threat_model>

<verification>
- Suite global `npm test` exit 0 con **≥818 pass + 0 fail + 1 skip preservado** (D-22 floor SC#5 canonical assertion per ISSUE-29-009; target real D-22 ≥844 si baseline lo soporta — sobrecumplimiento documentado en SUMMARY).
- Cherry-pick traceability completa: `git log --grep="cherry picked from commit\|manual reapply of" --since="2026-05-20" | wc -l` retorna ≥9.
- Prosa byte-exact: greps individuales para HARD STEP, NUNCA, delete-issue, log literals (em-dash en RC11, colon en RC10), body fields (Goal:, PLAN dir:, Plans:), lifecycle (in progress, done, verified, cancelled), Plan = comentario. Phase = sub-issue.
- Paréntesis pragmático byte-exact (ISSUE-29-010): `grep -Eq "\(en \{\{provider_name\}\}: In Progress / In Review / Done\)" src/orchestrator/prompt.md`.
- Placeholder removal verified (ISSUE-29-001 inverted-logic fix): `! grep -q "Section body added in plan 29-04" src/orchestrator/prompt.md` exit 0.
- Cross-phase coupling operacional + guard (ISSUE-29-011): `node -e "import('./src/labels.js').then(m => process.exit(m.KODO_LABEL_GSD_CHILD === 'kodo:gsd-child' ? 0 : 1))"` exit 0; `test/orchestrator-gsd.test.js` importa la constante.
- VERIFICATION.md phase-level presente con 5 SC + REPORT-01..06 traceability + 9 SHAs audit trail.
- Provider-agnostic: enforcement movido al test layer (RC tests) per ISSUE-29-007 — NO shell pipeline frágil en task verify.
- Final suite gate Task 4: pass count ≥818 programáticamente extraído.
</verification>

<success_criteria>
- 2 SHAs aplicados (1 manual reapply + 1 cherry-pick literal): `d030547` (manual), `81c848c` (literal).
- VERIFICATION.md phase-level creado.
- 21 tests heredados verdes (15 RC + 6 RA).
- **Suite ≥818 pass + 0 fail + 1 skip preservado** (D-22 floor SC#5 canonical assertion per ISSUE-29-009 — la antigua aritmética cumulativa `≥874` queda como commentary informativo en `must_haves`, no como gate).
- Los 6 conceptos canónicos REPORT-04 presentes en prosa ES (validado por RC1..RC15).
- Paréntesis pragmático byte-exact preserved (ISSUE-29-010).
- Cross-phase coupling Plan 29-01 → Plan 29-04 defensive (ISSUE-29-011 runtime guard).
- Los 9 SHAs de PENDING-INTEGRATIONS.md aplicados across los 4 plans (validado por SC#4 / VERIFICATION.md).
- 0 regresiones, 0 nuevos skips, 1 skip pre-existente preservado.
- REPORT-06 satisfecho transversalmente: 4 PLAN.md + 4 SUMMARY.md + VERIFICATION.md + VALIDATION.md presentes; cherry-pick audit trail completo.
- Final suite gate Task 4 cerrado con pass count programático.
</success_criteria>

<output>
After completion, create `.planning/phases/29-gsd-provider-reporting-integration/29-04-SUMMARY.md` documentando:
- SHAs aplicados (1 manual reapply + 1 cherry-pick) con commit hashes finales en main.
- Conflictos: 1 severo (prompt.md prose replace, manual reapply); 1 clean (test/orchestrator-gsd.test.js append).
- Final suite gate (Task 4) result: actual pass count + comparación contra floor ≥818 y target ≥844.
- Confirmación que VERIFICATION.md phase-level (Task 3) cubre los 5 SC del ROADMAP + REPORT-01..06 traceability + 9 SHAs audit trail.
- Phase 29 complete: total tests post-phase, fecha de signoff, archivos planning generados.
- Notas para futura: T-29-05 prompt injection upstream (sub-issue body desde task.description_markdown) queda ACCEPTED RISK BY DESIGN — si emerge incident-driven demand, abrir nueva phase v0.9+ con validator (CONTEXT Deferred Ideas).
</output>

## Decisions Addressed (CONTEXT.md citations)

- D-01: cherry-pick orden cronológico — aplica `d030547` → `81c848c` (últimos 2 SHAs de los 9).
- D-02: cherry-pick sobre main post-Phase-28.
- D-03: cherry-pick plan-by-plan.
- D-04: 4-plan decomposition — este es 29-04 (REPORT-04 + REPORT-06 transversal).
- D-05: PLAN + SUMMARY propios + VERIFICATION.md phase-level único (Task 3).
- D-16: tests RC1..RC15 + RA1..RA6 content + anti-leak asserts.
- D-19: PLAN antes / SUMMARY después.
- D-20: branch PLANs consultables.
- D-22: target suite ≥844 (real); floor ≥818 (letra SC#5). Task 4 gate programático.
- D-23: verificación incremental `npm test`.
- D-24: conflict resolution policy (severe drift en `prompt.md` — manual reapply de prosa 65 líneas).
- D-25: documentar desvíos en SUMMARY.md (prosa reflowed; `{{provider_name}}` substitution verificada en `resolvePromptTemplate`).
</output>
