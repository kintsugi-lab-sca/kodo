---
phase: 29-gsd-provider-reporting-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/config.js
  - test/config.test.js
autonomous: true
requirements:
  - REPORT-02
user_setup: []

must_haves:
  truths:
    - "Operador con `workflow.report_to_provider: true` en `~/.kodo/config.json` ve `isReportToProviderEnabled()` retornar `true`; con `false` / `\"true\"` / `1` / `undefined` / missing key recibe `false`."
    - "`DEFAULT_CONFIG` exportado desde `src/config.js` NO contiene la key `workflow` — operador opta-in editando el JSON manualmente (D-09 anti-mutation invariant)."
    - "Ningún `src/**/*.js` accede `.report_to_provider` directo fuera de `src/config.js`."
  artifacts:
    - path: "src/config.js"
      provides: "`export function isReportToProviderEnabled(_loadConfig = loadConfig)` strict equality `=== true` + DI opcional"
      contains: "isReportToProviderEnabled"
    - path: "test/config.test.js"
      provides: "10 tests cubriendo 5-state matrix + DEFAULT_CONFIG anti-mutation + source-hygiene multi-archivo recursivo"
      contains: "REPORT-02"
  key_links:
    - from: "src/config.js#isReportToProviderEnabled"
      to: "src/config.js#loadConfig"
      via: "DI seam con default real"
      pattern: "_loadConfig\\s*=\\s*loadConfig"
    - from: "test/config.test.js"
      to: "src/config.js#DEFAULT_CONFIG"
      via: "import + hasOwnProperty check"
      pattern: "hasOwnProperty\\.call\\(DEFAULT_CONFIG, 'workflow'\\)"
---

<objective>
Helper opt-in `isReportToProviderEnabled` con strict equality `=== true` + DI opcional + DEFAULT_CONFIG sin key `workflow` (anti-mutation invariant). Pre-requisito hard para 29-03: el wire-up `applyReportingGate(..., isReportToProviderEnabled())` en `src/orchestrator/launch.js` requiere que el helper exista y se importe.

Purpose: cumplir REPORT-02 + bloquear cualquier truthy coercion accidental (`"true"`, `1`, missing key, JSON corrupto fallback). Cierra el T-29-02 threat (config opt-in bypass) con tests que aseveran cada estado de la matriz 5-states.

Output: 1 archivo `src/` modificado (cherry-pick `e1f82c9` con manual conflict resolution — el slot original colisiona con `getDefaultGithubProviderConfig` añadido por Phase 26) + 1 archivo `test/` nuevo (`test/config.test.js`, 10 tests). Suite global esperada post-plan: 826 post-29-01 + 10 nuevos = **≥836 pass**, 0 nuevos skips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PENDING-INTEGRATIONS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md
@.planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md
@.planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-VALIDATION.md
@.planning/phases/29-gsd-provider-reporting-integration/29-01-PLAN.md

<interfaces>
<!-- Contract that 29-03 launch.js will consume -->

Export añadido a `src/config.js`:
- `export function isReportToProviderEnabled(_loadConfig?: () => typeof DEFAULT_CONFIG): boolean` — DI opcional con default a `loadConfig` real. Fail-closed: cualquier valor que no sea booleano `true` retorna `false`.

Insertion site:
- ENTRE `getPlaneApiKey` (línea actual ~174) Y `getDefaultGithubProviderConfig` (Phase 26, línea actual ~192). RESEARCH §"Drift Inventory" §config recommendation.

Invariante consumido por 29-03 test LH1:
- `src/orchestrator/launch.js` importará `isReportToProviderEnabled` desde `'../config.js'`. Si no se exporta correctamente, todo Plan 03 falla.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cherry-pick `e1f82c9` con manual conflict resolution (helper + 10 tests)</name>
  <files>src/config.js, test/config.test.js</files>
  <read_first>
    - .planning/PENDING-INTEGRATIONS.md (orden cronológico — `e1f82c9` es el 3er SHA después de `5a41d8f` + `cbd8f9c`)
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Drift Inventory" §config (severity Moderate — Phase 26 `getDefaultGithubProviderConfig` ocupa el slot)
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 3: src/config.js" + §"Touch File 9: test/config.test.js" (excerpt completo del helper + 10 tests)
    - src/config.js (estado actual main: `getProviderApiKey` lines 160-174 + `getPlaneApiKey` + `getDefaultGithubProviderConfig` lines 192-200 — punto de inserción ANTES de Phase 26 helper)
    - git show e1f82c9 -- src/config.js test/config.test.js (excerpt literal — el diff espera el slot que Phase 26 ahora ocupa)
    - CONTEXT D-09 (strict equality `=== true` + optional chaining + fail-closed), D-10 (DI opcional `_loadConfig`), D-11 (5-state matrix + anti-mutation invariant + source-hygiene multi-archivo)
    - 29-01-SUMMARY.md (verificar que Plan 29-01 cerró verde y baseline está en ≥826 pass)
  </read_first>
  <action>
Intentar `git cherry-pick e1f82c9` sobre HEAD post-Plan-29-01. Per 29-RESEARCH.md §"Drift Inventory" §config, conflict severity es **Moderate**: el patch original esperaba el slot entre `getPlaneApiKey` y el `export { ... }` final (línea base 174), pero Phase 26 (commit reciente) insertó `getDefaultGithubProviderConfig` ahí (líneas 192-200 actuales).

**Estrategia de resolución (CONTEXT D-24-2 conflict semántico — manual reapply):**

1. Si Git produce conflict marker en `src/config.js`, abortar con `git cherry-pick --abort`.
2. Aplicar manual reapply: extraer el contenido del helper de `git show e1f82c9 -- src/config.js` y insertarlo **ANTES** de la function `getDefaultGithubProviderConfig` (no después). Esta es la decisión documentada en RESEARCH §"Drift Inventory" §config Recommended Resolution.
3. `test/config.test.js` es archivo nuevo — sin conflict — aplicar directamente del diff (`git show e1f82c9 -- test/config.test.js > test/config.test.js`).
4. Crear commit con message: `feat(29-02): isReportToProviderEnabled + tests [cherry-picked from e1f82c9, manual resolution: insertion point shifted above getDefaultGithubProviderConfig (Phase 26)]`.

Contenido del helper a insertar en `src/config.js` (excerpt literal en PATTERNS §"Touch File 3 → What to Add", líneas 219-241 del archivo PATTERNS):
- JSDoc multi-line documentando los 6 estados que retornan `false` (missing workflow, missing key, explicit false, string "true", number 1, JSON corruption fallback) + D-03 anti-mutation reference.
- Function signature: `export function isReportToProviderEnabled(_loadConfig = loadConfig)`.
- Body: `const config = _loadConfig(); return config?.workflow?.report_to_provider === true;`.

Contenido de `test/config.test.js` (archivo nuevo, excerpt literal en PATTERNS §"Touch File 9 → What to Write", líneas 843-930):
- 3 describes:
  - `REPORT-02 — isReportToProviderEnabled` con 8 tests: baseline (DEFAULT_CONFIG), JSON corrupto fallback, providers sin workflow, `workflow:{}` vacía, `workflow.report_to_provider:false` explícito, `workflow.report_to_provider:"true"` string, `workflow.report_to_provider:1` number, `workflow.report_to_provider:true` (único true). Factory `fakeLoad(overrides)` evita filesystem touching (Pitfall 3).
  - `REPORT-02 — DEFAULT_CONFIG anti-mutation (D-03)` con 1 test: `hasOwnProperty.call(DEFAULT_CONFIG, 'workflow') === false`.
  - `REPORT-02 — source hygiene (D-05): .report_to_provider only inside src/config.js` con 1 test: walker recursivo en `src/`, excluye `src/config.js`, `stripComments` 3-capas, `assert.deepEqual(violations, [])`.

Validación post-write:
- `node -c src/config.js` debe pasar (sintaxis).
- `node -e "import('./src/config.js').then(m => console.log(typeof m.isReportToProviderEnabled))"` debe imprimir `function`.
- Insertion order: helper ANTES de `getDefaultGithubProviderConfig` (PATTERNS Touch File 3 invariant).
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -E "(cherry picked from commit e1f82c9|cherry-picked from e1f82c9)" &amp;&amp; grep -q "export function isReportToProviderEnabled" src/config.js &amp;&amp; grep -q "_loadConfig\s*=\s*loadConfig" src/config.js &amp;&amp; awk '/export function isReportToProviderEnabled/{a=NR} /export function getDefaultGithubProviderConfig/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;a&lt;b)?0:1}' src/config.js &amp;&amp; node -c src/config.js &amp;&amp; npm test -- test/config.test.js 2>&amp;1 | tail -10 | grep -E "pass [0-9]+"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene `cherry picked from commit e1f82c9` o `cherry-picked from e1f82c9, manual resolution`.
    - `grep -c "export function isReportToProviderEnabled" src/config.js` retorna 1.
    - `grep -c "_loadConfig\\s*=\\s*loadConfig" src/config.js` retorna 1 (DI seam pattern).
    - `grep -c "=== true" src/config.js` ≥1 dentro del body de `isReportToProviderEnabled` (strict equality).
    - Estructural insertion order: `awk` confirma que `isReportToProviderEnabled` aparece ANTES de `getDefaultGithubProviderConfig` en `src/config.js` (NR-line numbers a<b).
    - Anti-mutation invariant: `node -e "import('./src/config.js').then(m => console.log(Object.prototype.hasOwnProperty.call(m.DEFAULT_CONFIG, 'workflow')))"` imprime `false`.
    - Archivo `test/config.test.js` existe.
    - `npm test -- test/config.test.js` exit 0; ≥10 tests pasando (8 matrix + 1 anti-mutation + 1 source-hygiene).
    - Suite completa `npm test` exit 0; ≥836 pass total (826 post-29-01 + 10 nuevos).
    - Source-hygiene walker reporta `violations === []` (esperado porque post-Plan-29-02 SOLO `src/config.js` toca `.report_to_provider`).
  </acceptance_criteria>
  <done>Cherry-pick `e1f82c9` aplicado con manual reapply documentado en commit message; helper `isReportToProviderEnabled` exportado desde `src/config.js` con DI opcional + strict equality + insertion site ANTES de `getDefaultGithubProviderConfig`; `test/config.test.js` creado con 10 tests verdes; DEFAULT_CONFIG NO contiene key `workflow`; suite global ≥836 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.kodo/config.json` (filesystem) → `loadConfig()` | Entrada untrusted-ish: el operador edita el JSON a mano; un typo (`"true"` string en lugar de `true` boolean) NO debe activar el flag. JSON corruption produce fallback a `DEFAULT_CONFIG` (líneas 126-128 src/config.js). |
| `isReportToProviderEnabled()` → consumer (`launchOrchestrator` en 29-03) | Frontier de gating: si retorna `true` por mistake (truthy coercion), el agente recibe instrucciones de crear sub-issues; combinado con T-29-01 mitigation OK, pero un operador que NO opt-in vería comportamiento inesperado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | Spoofing (config opt-in bypass via truthy coercion) | `isReportToProviderEnabled` body | mitigate | Strict equality `=== true` (CONTEXT D-09). Tests REPORT-02 #6 (`"true"` string → false) + #7 (`1` number → false) lo verifican byte-level. Optional chaining bloquea undefined/null sin throw. **MEDIUM severity** — falla solo en sentido fail-open accidental (no security crítico, pero rompe la promesa opt-in del feature). |
| T-29-MUT | Tampering (DEFAULT_CONFIG drift) | `src/config.js#DEFAULT_CONFIG` | mitigate | Anti-mutation invariant test `Object.prototype.hasOwnProperty.call(DEFAULT_CONFIG, 'workflow') === false`. Si una futura phase intenta añadir `workflow:{}` al DEFAULT_CONFIG por conveniencia, CI rojo inmediato. **LOW severity** (cosmético: rompería D-09 documentation, no afecta runtime). |
| T-29-DRIFT | Tampering (consumer drift `.report_to_provider` inline) | `src/**/*.js` salvo `src/config.js` | mitigate | Walker source-hygiene (test #10) bloquea acceso directo desde otros archivos. Defensa en profundidad: si Phase 30+ olvida usar el helper, CI rojo. **MEDIUM severity.** |

</threat_model>

<verification>
- Suite global `npm test` exit 0 con ≥836 pass, 0 fail, 1 skip pre-existente.
- Cherry-pick traceability: `git log --oneline -1 | grep -E "(cherry picked from commit e1f82c9|cherry-picked from e1f82c9)"` retorna 1 match.
- Helper estructural: `isReportToProviderEnabled` aparece ANTES de `getDefaultGithubProviderConfig` en `src/config.js`.
- Anti-mutation: `node -e ...` verifica `DEFAULT_CONFIG.workflow === undefined`.
- Source hygiene smoke: `grep -rE "\\.report_to_provider\\b" src/ | grep -v src/config.js` retorna vacío.
- Strict equality verificada: `grep "=== true" src/config.js` retorna ≥1 match en líneas del helper.
</verification>

<success_criteria>
- 1 SHA aplicado (cherry-pick literal o manual reapply documentado): `e1f82c9`.
- 1 archivo nuevo creado: `test/config.test.js`.
- 10 tests heredados verdes; **suite ≥818 pass + 0 fail + 1 skip preservado** (D-22 floor SC#5 canonical assertion per ISSUE-29-009 — cumulative delta `+10` queda como commentary informativo en must_haves, no como gate).
- Helper `isReportToProviderEnabled` listo para ser importado por `src/orchestrator/launch.js` en Plan 29-03.
- DEFAULT_CONFIG sin key `workflow` (anti-mutation invariant assertado).
- 0 regresiones, 0 nuevos skips.
</success_criteria>

<output>
After completion, create `.planning/phases/29-gsd-provider-reporting-integration/29-02-SUMMARY.md` documentando:
- SHA aplicado (1 cherry-pick) con commit hash final en main.
- Conflicto resuelto: insertion point shifted above `getDefaultGithubProviderConfig` (manual resolution).
- Delta de tests: ≥826 → ≥836 (+10).
- Nota crítica para 29-03: `isReportToProviderEnabled` exportado desde `src/config.js` — listo para import en `src/orchestrator/launch.js`.
- Source-hygiene baseline post-29-02: 0 violations en `src/` excluyendo `src/config.js`.
</output>

## Decisions Addressed (CONTEXT.md citations)

- D-01: cherry-pick orden cronológico — aplica `e1f82c9` (tercer SHA).
- D-02: cherry-pick sobre main post-Phase-28.
- D-03: cherry-pick plan-by-plan.
- D-04: 4-plan decomposition — este es 29-02 (REPORT-02).
- D-05: PLAN + SUMMARY propios.
- D-10: DI opcional `_loadConfig = loadConfig` evita filesystem touching.
- D-11: matriz de 5 estados (true / "true" / 1 / undefined / missing) + DEFAULT_CONFIG anti-mutation + source-hygiene multi-archivo.
- D-19: PLAN antes / SUMMARY después.
- D-20: branch PLANs consultables.
- D-23: verificación incremental `npm test`.
- D-24: conflict resolution policy (moderate severity por Phase 26 `getDefaultGithubProviderConfig` slot collision — manual reapply).
- D-25: documentar desvío del cherry-pick literal en SUMMARY.md.
