---
phase: 15
plan: 05
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - test/format-isolation.test.js
autonomous: true
requirements: [DX-01, DX-02, DX-03, DX-04, DX-05]
user_setup: []

must_haves:
  truths:
    - "`test/format-isolation.test.js` extiende sus describes con un nuevo bloque que asserta que los 5 callsites Phase 15 (`src/logger.js`, `src/logs/reader.js`, `src/check.js`, `src/cli/gsd-inspect.js`, `src/cli/gsd-verify.js`) SI importan `src/cli/format.js` (positive: cableado verificado)."
    - "El mismo bloque asserta que NINGUNO de los 5 callsites importa `picocolors` directamente (negative: D-07 single-source preservado tras el cableado)."
    - "Los describes existentes (LOG-12 extension walker + Single source of color picocolors grep) NO se alteran y siguen verdes."
    - "Wave 2: este plan depende explícitamente de 15-01..15-04 — el orchestrator NO lo ejecuta hasta que los 4 anteriores hayan completado, garantizando que los 5 imports nuevos existen al correr el test."
  artifacts:
    - path: "test/format-isolation.test.js"
      provides: "Aserciones positivas de import + anti-leak picocolors para los 5 callsites Phase 15"
      contains: "PHASE_15_CALLSITES"
  key_links:
    - from: "test/format-isolation.test.js (nuevo describe)"
      to: "los 5 callsites Phase 15"
      via: "regex import sobre cada archivo + grep picocolors"
      pattern: "PHASE_15_CALLSITES"
---

<objective>
Cerrar el invariant **single-source-of-color** y verificar el **cableado completo Phase 15** extendiendo `test/format-isolation.test.js` con un nuevo describe block. Dos asserts:

1. **Positive**: los 5 archivos cableados en Phase 15 (Plans 15-01..15-04) SI importan `src/cli/format.js`. Confirma que el cableado se hizo en todos los callsites previstos. Si un Plan futuro elimina el import por error, el test falla.

2. **Negative**: ninguno de esos 5 archivos importa `picocolors` directamente. D-07 single-source de color preservado tras el cableado. Si un developer en el futuro añade `import pc from 'picocolors'` en cualquiera de esos archivos, el test falla.

**Wave 2 (`depends_on: [15-01, 15-02, 15-03, 15-04]`)**: este plan se ejecuta SÓLO tras completarse Plans 15-01..15-04. Garantiza que los 5 imports existen cuando el test corre — sin race conditions con paralelización.

**No ejecutar antes de tiempo**: si se corre `node --test test/format-isolation.test.js` con sólo Plans 15-01..15-03 completados, el assert positivo fallará para los archivos de Plan 15-04 (gsd-verify.js). Esto es deliberado: el test detecta el cableado incompleto. El orchestrator respeta el `depends_on` y no lo dispara antes.

Purpose: Cerrar el invariant single-source-of-color y certificar el cableado de los 5 callsites. Cubre todos los SCs Phase 15 transversalmente — un fallo en este plan indica que algún Plan 15-01..15-04 dejó algo sin cablear.
Output: `test/format-isolation.test.js` extendido con 1 describe block adicional (2 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cli-polish-wiring/15-CONTEXT.md
@.planning/phases/15-cli-polish-wiring/15-PATTERNS.md

# Source of truth — read BEFORE editing
@test/format-isolation.test.js
@src/cli/format.js
@.planning/phases/15-cli-polish-wiring/15-01-SUMMARY.md
@.planning/phases/15-cli-polish-wiring/15-02-SUMMARY.md
@.planning/phases/15-cli-polish-wiring/15-03-SUMMARY.md
@.planning/phases/15-cli-polish-wiring/15-04-SUMMARY.md

<interfaces>
<!-- Estructura del test actual (Phase 14) — reusar el mismo helper extractImports/walkImports si existe. -->

`test/format-isolation.test.js` ya tiene 2 describes (Phase 14):
- "LOG-12 extension: src/cli/format.js isolation (D-06)" — walker que asegura que format.js NO importa logger.js transitivamente.
- "Single source of color (D-07, D-08): picocolors imports" — grep que asegura que SOLO src/cli/format.js importa picocolors.

Ambos describes NO se modifican. El nuevo describe se añade al final del archivo.

Helpers ya disponibles (Phase 14):
- `walkImports(entry, visited)` — devuelve un Set de paths transitivos.
- `extractImports(file)` (si existe) — devuelve los specifiers importados desde un archivo.
- Constantes `SRC` (path absoluto a `src/`), `REPO` (path absoluto al repo root).

Phase 15 callsites esperados tras Plans 15-01..15-04:
| Archivo                       | Path import esperado          |
|-------------------------------|-------------------------------|
| `src/logger.js`               | `from './cli/format.js'`      |
| `src/logs/reader.js`          | `from '../cli/format.js'`     |
| `src/check.js`                | `from './cli/format.js'`      |
| `src/cli/gsd-inspect.js`      | `from './format.js'`          |
| `src/cli/gsd-verify.js`       | `from './format.js'`          |

El regex `from\s+['"](\.\.?\/)+(cli\/)?format\.js['"]` cubre los 3 patrones de path relativos.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir describe Phase 15 cableado a test/format-isolation.test.js</name>
  <files>test/format-isolation.test.js</files>

  <read_first>
    - test/format-isolation.test.js (estado actual completo — describes existentes en líneas 73, 98)
    - src/logger.js (verificar el import actual de format.js tras Plan 15-01)
    - src/logs/reader.js (verificar el import tras Plan 15-01)
    - src/check.js (verificar el import tras Plan 15-02)
    - src/cli/gsd-inspect.js (verificar el import tras Plan 15-03)
    - src/cli/gsd-verify.js (verificar el import tras Plan 15-04)
    - .planning/phases/15-cli-polish-wiring/15-04-SUMMARY.md (confirmar que Plan 15-04 completó — sin esto, los asserts positivos fallarán)
  </read_first>

  <behavior>
    - Test 1 (positive: 5 archivos cableados importan format.js): para cada archivo en `PHASE_15_CALLSITES`, leer el contenido y assert que matchea `from\s+['"](\.\.?\/)+(cli\/)?format\.js['"]`. Si alguno falta, el test falla con un mensaje listando los archivos sin import.
    - Test 2 (negative: ningún Phase 15 callsite importa picocolors directamente): para cada archivo en `PHASE_15_CALLSITES`, leer el contenido y assert que NO matchea `from\s+['"]picocolors['"]`. Si alguno leak-ea, el test falla con un mensaje listando los leakers.
    - Tests pre-existentes (LOG-12 walker + single-source picocolors): siguen verdes — no se tocan.
  </behavior>

  <action>
    1. **Inspeccionar el archivo actual** para entender:
       - Qué imports/helpers están disponibles a nivel módulo (`readFileSync`, `join`, `relative`, `SRC`, `REPO`, `walkImports`, `extractImports`).
       - El estilo del describe block (e.g. `describe('...', () => { it('...', () => { ... }) })`).
       - Si `extractImports` o equivalente ya existe — reusar.

    2. **Añadir un nuevo describe block al final del archivo** (después del último describe de single-source picocolors, line 98+):
       ```javascript
       describe('Phase 15 cableado: callsites importan format.js (positive) + no picocolors leak (negative)', () => {
         /** @type {readonly string[]} */
         const PHASE_15_CALLSITES = Object.freeze([
           'src/logger.js',
           'src/logs/reader.js',
           'src/check.js',
           'src/cli/gsd-inspect.js',
           'src/cli/gsd-verify.js',
         ]);

         it('Phase 15 callsites import src/cli/format.js (cableado verificado tras Plans 15-01..15-04)', () => {
           const missingImports = [];
           for (const rel of PHASE_15_CALLSITES) {
             const full = join(REPO, rel);
             const src = readFileSync(full, 'utf-8');
             // Match relative imports a format.js — paths varían según ubicación del archivo:
             //   - desde src/logger.js → './cli/format.js'
             //   - desde src/logs/reader.js → '../cli/format.js'
             //   - desde src/check.js → './cli/format.js'
             //   - desde src/cli/gsd-inspect.js → './format.js'
             //   - desde src/cli/gsd-verify.js → './format.js'
             const re = /from\s+['"](\.\.?\/)+(cli\/)?format\.js['"]/;
             if (!re.test(src)) {
               missingImports.push(rel);
             }
           }
           assert.deepEqual(
             missingImports,
             [],
             `Phase 15 callsites missing the format.js import:\n  ${missingImports.join('\n  ')}\n` +
             `Each callsite must import { createFormatter } and/or { _resolveUseColor } from src/cli/format.js.`,
           );
         });

         it('Phase 15 callsites do NOT import picocolors directly (D-07 single-source preserved)', () => {
           const leakers = [];
           for (const rel of PHASE_15_CALLSITES) {
             const full = join(REPO, rel);
             const src = readFileSync(full, 'utf-8');
             if (/from\s+['"]picocolors['"]/.test(src)) {
               leakers.push(rel);
             }
           }
           assert.deepEqual(
             leakers,
             [],
             `picocolors must be imported only via src/cli/format.js (D-07 single-source).\n` +
             `Leakers detected:\n  ${leakers.join('\n  ')}`,
           );
         });
       });
       ```

       **Si `extractImports` existe** y devuelve un array de specifiers, usar:
       ```javascript
       const specs = extractImports(full);
       const hasFormatImport = specs.some((s) => /(\.\.?\/)+(cli\/)?format\.js$/.test(s));
       if (!hasFormatImport) missingImports.push(rel);
       // ...
       const hasPicocolors = specs.includes('picocolors');
       if (hasPicocolors) leakers.push(rel);
       ```
       Esto es más robusto que el regex inline; usar la opción que el archivo ya soporte.

    3. **NO TOCAR**:
       - El describe "LOG-12 extension" (línea 73) — sigue verde porque format.js no importa logger.js.
       - El describe "Single source of color" (línea 98) — sigue verde porque sólo src/cli/format.js importa picocolors.
       - Los helpers `walkImports`, `extractImports` (si existen) — reusar tal cual.
       - Cualquier import a nivel módulo del archivo de tests.

    4. **Confirmar pre-condiciones de ejecución**: el `depends_on: [15-01, 15-02, 15-03, 15-04]` del frontmatter garantiza que los 5 imports ya existen cuando este test corre. Si por algún motivo se ejecuta antes (e.g. operador manual sin orchestrator), el test falla apropiadamente con la lista de archivos sin cablear — diagnóstico claro.
  </action>

  <verify>
    <automated>node --test test/format-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Phase 15 cableado" test/format-isolation.test.js` >= 1
    - `grep -c "PHASE_15_CALLSITES" test/format-isolation.test.js` >= 1
    - `grep -c "src/logger.js" test/format-isolation.test.js` >= 1
    - `grep -c "src/logs/reader.js" test/format-isolation.test.js` >= 1
    - `grep -c "src/check.js" test/format-isolation.test.js` >= 1
    - `grep -c "src/cli/gsd-inspect.js" test/format-isolation.test.js` >= 1
    - `grep -c "src/cli/gsd-verify.js" test/format-isolation.test.js` >= 1
    - `grep -cE "missingImports|leakers" test/format-isolation.test.js` >= 2
    - `node --test test/format-isolation.test.js` exit 0 — TODOS los tests verdes (LOG-12 extension + Single source picocolors + Phase 15 cableado).
  </acceptance_criteria>

  <done>
    `test/format-isolation.test.js` extendido con 1 describe Phase 15 (2 tests: positive cableado + negative anti-leak picocolors). Suite verde tras Plans 15-01..15-04. Single-source-of-color invariant cerrado.
  </done>
</task>

</tasks>

<verification>
- `node --test test/format-isolation.test.js` exit 0.
- `node --test` global (suite completa) sigue verde.
- Los 5 callsites Phase 15 confirmados como cableados a format.js sin leak directo de picocolors.
</verification>

<success_criteria>
Single-source-of-color y cableado completo Phase 15 verificados por test source-hygiene. Indirectamente cubre los 5 SCs ROADMAP §15 — si algún Plan 15-01..15-04 olvida un cableado, este plan lo detecta.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/15-cli-polish-wiring/15-05-SUMMARY.md` con: archivos modificados, describe añadido, los 5 callsites verificados como cableados, anti-leak picocolors confirmado.
</output>
