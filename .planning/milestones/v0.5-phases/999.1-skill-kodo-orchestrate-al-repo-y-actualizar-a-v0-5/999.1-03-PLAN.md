---
phase: 999.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/stop.js
autonomous: true
requirements: [D-14, D-15, D-16]
must_haves:
  truths:
    - "SKILL_PATH apunta al archivo real `.claude/skills/kodo-orchestrate/skill.md` del repo"
    - "handleOrchestratorStop ejecuta `git status --porcelain .claude/skills/` (no `skills/`)"
    - "handleOrchestratorStop ejecuta `git add .claude/skills/` (no `git add skills/`) cuando commitea"
    - "El mensaje del commit conserva el formato `skill: orchestrator learnings <date>`"
    - "El JSDoc de handleOrchestratorStop documenta el path actualizado `.claude/skills/` (M6: ya no `skills/` a secas)"
    - "KODO_ROOT honra la env var KODO_ROOT si está set, para que Plan 04 pueda ejercitar el hook contra un repo aislado sin tocar el repo real del desarrollador"
    - "El comportamiento productivo es idéntico cuando KODO_ROOT no está set en el entorno (fallback a join(__dirname, '..', '..'))"
    - "El resto del flujo de stop.js (runStopHook, buildStopNudgeText, releaseGsdLock, markSessionStatus PRE-release) NO se modifica"
    - "La detección de orchestrator session sigue funcionando vía cwd === KODO_ROOT o cwd.startsWith(KODO_ROOT + '/')"
  artifacts:
    - path: "src/hooks/stop.js"
      provides: "Stop hook con auto-commit re-activado contra `.claude/skills/` + KODO_ROOT overridable por env (para tests Plan 04)"
      contains: "join(KODO_ROOT, '.claude', 'skills'"
  key_links:
    - from: "src/hooks/stop.js#SKILL_PATH"
      to: ".claude/skills/kodo-orchestrate/skill.md"
      via: "join(KODO_ROOT, '.claude', 'skills', 'kodo-orchestrate', 'skill.md')"
      pattern: "join\\(KODO_ROOT,\\s*'\\.claude',\\s*'skills'"
    - from: "src/hooks/stop.js#handleOrchestratorStop"
      to: "git workdir .claude/skills/"
      via: "execSync('git status --porcelain .claude/skills/') con cwd=KODO_ROOT"
      pattern: "git status --porcelain \\.claude/skills/"
    - from: "src/hooks/stop.js#KODO_ROOT"
      to: "process.env.KODO_ROOT (override testability)"
      via: "process.env.KODO_ROOT || join(__dirname, '..', '..')"
      pattern: "process\\.env\\.KODO_ROOT"
---

<objective>
Fixear `src/hooks/stop.js` (D-14/D-15) para que el path de la skill y los argumentos de
`git status`/`git add` en `handleOrchestratorStop` apunten al directorio real
`.claude/skills/`. En la misma edición:

1. Actualizar el JSDoc de `handleOrchestratorStop` para que cite `.claude/skills/` en vez
   de `skills/` (M6 obligatorio, no opcional).
2. Hacer `KODO_ROOT` overridable por `process.env.KODO_ROOT` (línea aditiva, fallback al
   cálculo existente) para que el spawnSync de Plan 04 (D-16) pueda ejercitar el hook
   contra un repo aislado sin contaminar el repo real del desarrollador (CR-02 Phase 16).

Purpose: D-14/D-15 — restaurar el contrato auto-commit que la skill promete (`el commit es
automático`). D-16 — el env override es la única forma limpia de honrar el patrón canónico
spawnSync + tmpdir/HOME (Phase 16 LOG-15 + Phase 17 UAT-03) sin refactorizar la firma de
`handleOrchestratorStop`. El override es aditivo: en producción `process.env.KODO_ROOT` no
está set y el comportamiento es idéntico al pre-Plan 03.

Output: `src/hooks/stop.js` con:
- 3 paths fixed (`.claude/skills/`)
- 1 línea aditiva en la constante `KODO_ROOT` (`process.env.KODO_ROOT ||` prefix)
- 1 línea actualizada en el JSDoc de `handleOrchestratorStop`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md

<interfaces>
<!-- Bloques a fixear — el resto del archivo NO se modifica. -->

From src/hooks/stop.js (estado actual, líneas relevantes):

L19–21 (KODO_ROOT + SKILL_PATH):
```javascript
const __dirname = dirname(fileURLToPath(import.meta.url));
const KODO_ROOT = join(__dirname, '..', '..');
const SKILL_PATH = join(KODO_ROOT, 'skills', 'kodo-orchestrate', 'skill.md');
```

→ Ediciones:
- L20: `const KODO_ROOT = process.env.KODO_ROOT || join(__dirname, '..', '..');`
- L21: `const SKILL_PATH = join(KODO_ROOT, '.claude', 'skills', 'kodo-orchestrate', 'skill.md');`

L234–237 (JSDoc encima de la función — el comentario actual):
```javascript
/**
 * Called when the orchestrator session ends.
 * Auto-commits any pending changes in skills/ to preserve learnings.
 */
async function handleOrchestratorStop() {
```

→ Edición M6 obligatoria: el texto del JSDoc debe decir `.claude/skills/` (no `skills/`).

L243 + L255 (los dos execSync dentro de handleOrchestratorStop): paths git fixados a
`.claude/skills/`. El `cwd: KODO_ROOT` queda como está — ahora respeta el env override
automáticamente porque KODO_ROOT mismo lo respeta.

**Justificación del env override (para auditoría):**
El refactor aditivo es minúsculo: una expresión OR antes de la llamada a `join`. En
ejecución productiva, `process.env.KODO_ROOT` no está set, así que `KODO_ROOT` se
computa exactamente igual que antes. Para tests (Plan 04 D-16), el spawnSync inyecta
`env: { KODO_ROOT: tmpRepo, HOME: tmpHome, ... }` y el código resuelve KODO_ROOT al
tmpdir, ejercitando el flujo completo sobre un git workdir aislado. Esto honra D-16
("patrón source-hygiene + spawnSync usado en Phases 16–17") sin tocar la firma de la
función ni introducir parámetros opcionales en su API. Análogo: `KODO_DIR` en
`src/session/state.js` ya respeta `homedir()` que es overridable vía `HOME`.

Nota: la función está nombrada `handleOrchestratorStop` en el código aunque el
CONTEXT.md la referencia como `handleSkillAutoCommit`. NO renombrar — el caller en L118
(`await handleOrchestratorStop()`) depende del nombre actual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix path + env override + JSDoc en src/hooks/stop.js</name>
  <files>src/hooks/stop.js</files>

  <read_first>
    - src/hooks/stop.js (archivo completo — confirmar exactamente qué 5 líneas se tocan, y que el resto del flujo runStopHook/buildStopNudgeText/releaseGsdLock/markSessionStatus NO necesita ningún ajuste)
    - .planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md (D-14, D-15, D-16)
  </read_first>

  <action>
    Aplicar EXACTAMENTE 5 ediciones quirúrgicas en `src/hooks/stop.js`, sin tocar nada más:

    **Edit 1 — L20 (KODO_ROOT con env override):**
    Reemplazar:
    ```javascript
    const KODO_ROOT = join(__dirname, '..', '..');
    ```
    Por:
    ```javascript
    const KODO_ROOT = process.env.KODO_ROOT || join(__dirname, '..', '..');
    ```

    **Edit 2 — L21 (SKILL_PATH):**
    Reemplazar:
    ```javascript
    const SKILL_PATH = join(KODO_ROOT, 'skills', 'kodo-orchestrate', 'skill.md');
    ```
    Por:
    ```javascript
    const SKILL_PATH = join(KODO_ROOT, '.claude', 'skills', 'kodo-orchestrate', 'skill.md');
    ```

    **Edit 3 — JSDoc encima de handleOrchestratorStop (~L234–237) — M6 obligatorio:**
    Reemplazar:
    ```javascript
    /**
     * Called when the orchestrator session ends.
     * Auto-commits any pending changes in skills/ to preserve learnings.
     */
    ```
    Por:
    ```javascript
    /**
     * Called when the orchestrator session ends.
     * Auto-commits any pending changes in .claude/skills/ to preserve learnings.
     */
    ```

    **Edit 4 — dentro de handleOrchestratorStop (~L243), git status:**
    Reemplazar:
    ```javascript
        const status = execSync('git status --porcelain skills/', {
    ```
    Por:
    ```javascript
        const status = execSync('git status --porcelain .claude/skills/', {
    ```

    **Edit 5 — dentro de handleOrchestratorStop (~L255), git add + commit:**
    Reemplazar:
    ```javascript
        execSync(`git add skills/ && git commit -m "skill: orchestrator learnings ${date}"`, {
    ```
    Por:
    ```javascript
        execSync(`git add .claude/skills/ && git commit -m "skill: orchestrator learnings ${date}"`, {
    ```

    Reglas duras:
    - **No renombrar** `handleOrchestratorStop` ni cambiar la firma (sigue siendo
      `async function handleOrchestratorStop()` sin parámetros). El caller en L118
      depende del nombre actual.
    - **No exportar** la función (sigue siendo internal scope al módulo).
    - **No reordenar** ni refactorizar otros bloques. Esto es un fix de 5 líneas;
      cualquier cambio adicional sale del scope D-14/D-15/D-16.
    - **No tocar** runStopHook, buildStopNudgeText, readStdin, main(), ni los
      bloques `markSessionStatus PRE-release` (Phase 16 LOG-15) ni `releaseGsdLock`.
    - **No tocar** el mensaje de commit ni el formato `${date}` (Plan 4 hace
      asserts contra el literal `skill: orchestrator learnings`).
    - **No tocar** `cwd: KODO_ROOT` en los dos `execSync` — KODO_ROOT ya respeta
      el env override gracias a Edit 1.
  </action>

  <acceptance_criteria>
    - `grep -q "const KODO_ROOT = process\\.env\\.KODO_ROOT || join(__dirname, '\\.\\.', '\\.\\.')" src/hooks/stop.js` (Edit 1)
    - `grep -q "join(KODO_ROOT, '\\.claude', 'skills', 'kodo-orchestrate', 'skill\\.md')" src/hooks/stop.js` (Edit 2)
    - `! grep -q "join(KODO_ROOT, 'skills', 'kodo-orchestrate'" src/hooks/stop.js` (path antiguo eliminado)
    - `grep -q "Auto-commits any pending changes in \\.claude/skills/" src/hooks/stop.js` (Edit 3: M6 JSDoc actualizado)
    - `! grep -qE 'pending changes in skills/' src/hooks/stop.js` (JSDoc antiguo eliminado)
    - `grep -q "git status --porcelain \\.claude/skills/" src/hooks/stop.js` (Edit 4)
    - `! grep -q "git status --porcelain skills/" src/hooks/stop.js` (path git antiguo eliminado)
    - `grep -q "git add \\.claude/skills/" src/hooks/stop.js` (Edit 5)
    - `! grep -qE "git add skills/[^.]" src/hooks/stop.js` (path git antiguo eliminado — regex evita falso positivo con `.claude/skills/`)
    - `grep -q "skill: orchestrator learnings \\\${date}" src/hooks/stop.js` (mensaje de commit preservado)
    - `grep -q "async function handleOrchestratorStop()" src/hooks/stop.js` (función NO renombrada y SIN parámetros — la firma sigue siendo cero-args, NO exportada)
    - `! grep -q "export async function handleOrchestratorStop" src/hooks/stop.js` (NO se exporta — confirma que no hubo refactor de API)
    - `grep -q "await handleOrchestratorStop()" src/hooks/stop.js` (caller intacto)
    - `node --check src/hooks/stop.js` (sintaxis válida)
    - `node --test test/stop.test.js test/stop-state-transition.test.js` exits 0 (regression: el resto del flujo sigue verde, sin contaminar state real porque ningún test instala KODO_ROOT env)
  </acceptance_criteria>

  <verify>
    <automated>bash -c "F=src/hooks/stop.js; grep -q \"const KODO_ROOT = process\\.env\\.KODO_ROOT || join(__dirname, '\\.\\.', '\\.\\.')\" \"\$F\" && grep -q \"join(KODO_ROOT, '\\.claude', 'skills', 'kodo-orchestrate', 'skill\\.md')\" \"\$F\" && ! grep -q \"join(KODO_ROOT, 'skills', 'kodo-orchestrate'\" \"\$F\" && grep -q 'Auto-commits any pending changes in \\.claude/skills/' \"\$F\" && ! grep -qE 'pending changes in skills/' \"\$F\" && grep -q 'git status --porcelain \\.claude/skills/' \"\$F\" && ! grep -q 'git status --porcelain skills/' \"\$F\" && grep -q 'git add \\.claude/skills/' \"\$F\" && grep -q 'async function handleOrchestratorStop()' \"\$F\" && ! grep -q 'export async function handleOrchestratorStop' \"\$F\" && grep -q 'await handleOrchestratorStop()' \"\$F\" && node --check \"\$F\" && node --test test/stop.test.js test/stop-state-transition.test.js"</automated>
  </verify>

  <done>
    `src/hooks/stop.js` con las 5 ediciones aplicadas exactamente; KODO_ROOT honra
    `process.env.KODO_ROOT` con fallback al cálculo original; SKILL_PATH y los dos
    comandos git apuntan a `.claude/skills/`; el JSDoc cita la ruta actualizada; la
    función NO se renombra ni se exporta; sintaxis válida; tests existentes del hook
    Stop siguen pasando (sin contaminar state real porque ningún test pre-existente
    instala la env var).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stop hook → git workdir | execSync ejecuta git con cwd derivado de KODO_ROOT |
| Stop hook → process.env.KODO_ROOT | Nueva superficie de configuración por env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1.03-01 | Tampering / Blast radius | `git status --porcelain .claude/skills/` | mitigate | El filtro por path `.claude/skills/` ya estaba en el código original; el fix no amplía el blast radius. La inspección sigue restringida a un único subdirectorio del workdir. |
| T-999.1.03-02 | Spoofing / accidental commit | `git add .claude/skills/` con cwd cooked | mitigate | execSync se invoca con `cwd: KODO_ROOT`. KODO_ROOT ahora respeta env override (Edit 1) pero el comportamiento productivo es idéntico cuando la env var no está set. Plan 04 cubre con tests aislados (mkdtempSync + KODO_ROOT inyectado) que el comportamiento es correcto en aislamiento. |
| T-999.1.03-03 | Tampering / env injection en producción | `process.env.KODO_ROOT` malicioso | accept | Riesgo bajo: el orchestrator se ejecuta bajo la cuenta del usuario, no en un contexto multi-tenant; un atacante que pueda setear env vars en la shell del usuario ya tiene capacidad arbitraria. El env override sigue el mismo modelo que `KODO_DIR` (vía HOME), aceptado en Phase 6 LOG-12 sin objeción. |
| T-999.1.03-04 | Repudiation | commit sin GPG sign | accept | Mismo régimen que el código original (D-15 explícito). Plan 04 desactiva `commit.gpgsign` localmente en su tmpdir para CI determinismo; producción honra el gitconfig del usuario. |
</threat_model>

<verification>
- Las 5 ediciones quirúrgicas presentes
- Función `handleOrchestratorStop` NO renombrada, NO exportada, firma sin parámetros
- Caller `await handleOrchestratorStop()` intacto
- JSDoc actualizado (M6)
- `node --check` y tests existentes siguen verdes
</verification>

<success_criteria>
- Fix D-14 aplicado: SKILL_PATH + git status + git add apuntan a `.claude/skills/`
- Auto-commit re-habilitado (validado por Plan 4)
- JSDoc cita el path actualizado (M6)
- KODO_ROOT overridable por env para testability (D-16 prerequisite)
- Comportamiento productivo idéntico (env var no set → fallback al cálculo original)
- Tests existentes de stop.js no regresan
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-03-SUMMARY.md`.
</output>
