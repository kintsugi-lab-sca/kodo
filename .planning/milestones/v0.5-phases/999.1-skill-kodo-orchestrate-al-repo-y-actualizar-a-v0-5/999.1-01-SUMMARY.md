---
phase: 999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5
plan: 01
subsystem: docs
tags: [skill, orchestrator, claude-code, provider-agnostic, gsd, cli, diagnostics]

# Dependency graph
requires:
  - phase: 999.1
    provides: "CONTEXT.md decisiones D-01..D-17 (boundary skill ↔ prompt, provider-agnostic, 4 flujos diagnóstico, mapping vía projects.json, mecanismo auto-update)"
provides:
  - "Skill canonical .claude/skills/kodo-orchestrate/skill.md, provider-agnostic v0.5"
  - "Documentación operativa de los 3 tags del orchestrator ([GSD phase N], [GSD bootstrap], [GSD quick]) y del modo no-GSD"
  - "Cheat-sheet CLI de diagnóstico (kodo logs --session-of, kodo gsd inspect, kodo gsd verify, locks per-repo) con exit codes deterministas"
  - "Instrucción canónica `cat ~/.kodo/config.json` para derivar provider y namespace mcp__<provider>__*"
  - "Instrucción canónica `cat ~/.kodo/projects.json` como única fuente del mapping projectId → path"
  - "Mecanismo de auto-update preservado con sección 'Lecciones aprendidas' vacía (placeholder D-13)"
affects: [999.1-02-prompt-simplify, 999.1-03-stop-hook-fix, 999.1-04-stop-hook-tests, 999.1-05-cleanup, orchestrator-runtime]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Skill markdown sin frontmatter (Claude Code lee cuerpo crudo, sin render)"
    - "Provider-agnostic: namespace mcp__<provider>__* derivado de ~/.kodo/config.json"
    - "Idioma split (ES docs humanos / EN slash commands del agente)"

key-files:
  created:
    - ".claude/skills/kodo-orchestrate/skill.md"
  modified: []

key-decisions:
  - "Skill vive en .claude/skills/kodo-orchestrate/skill.md (D-01); auto-cargada por Claude Code cuando cwd=repo"
  - "Cero referencias a Plane o tools MCP concretas en el cuerpo (D-07/D-09); namespace genérico mcp__<provider>__*"
  - "Primer paso operativo: cat ~/.kodo/config.json para derivar provider (D-08)"
  - "Mapping de proyectos siempre vía cat ~/.kodo/projects.json — sin IDs hardcoded (D-17)"
  - "4 flujos D-10 documentados (stuck / lock / inspect / verify) con exit codes deterministas"
  - "Lecciones aprendidas arranca vacía con placeholder; las 4 entradas 2026-04-07 NO se trasladan (D-12/D-13)"
  - "Cross-reference explícita a src/orchestrator/prompt.md y src/hooks/stop.js en cabecera y sección auto-commit"

patterns-established:
  - "Skill canonical en repo + prompt.md como fallback degradado provider-specific (D-01/D-02/D-03)"
  - "Diagnóstico task-oriented (síntoma → comando) con exit codes documentados"
  - "Lecciones aprendidas con mecanismo de auto-update preservado pero contenido fresco"

requirements-completed: [D-01, D-03, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-15, D-17]

# Metrics
duration: 2min
completed: 2026-05-11
---

# Phase 999.1 Plan 01: Skill kodo-orchestrate al repo Summary

**Migración y reescritura provider-agnostic del skill kodo-orchestrate a `.claude/skills/kodo-orchestrate/skill.md`, con 3 tags GSD literales, 4 flujos diagnóstico CLI con exit codes deterministas, mapping vía `~/.kodo/projects.json` y mecanismo auto-update preservado pero limpio.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-11T05:52:57Z
- **Completed:** 2026-05-11T05:54:58Z
- **Tasks:** 1/1
- **Files modified:** 1 (creado)

## Accomplishments

- Skill canonical creada en repo (`.claude/skills/kodo-orchestrate/skill.md`, 177 líneas) que Claude Code auto-carga cuando `cwd = ~/dev/klab/kodo`.
- Cuerpo 100% provider-agnostic: sin `Plane` como nombre propio, sin `mcp__plane__*`/`mcp__github__*`/`mcp__clickup__*`, sin placeholders literales `{{...}}`, sin IDs hardcoded de proyectos.
- Los 3 tags del orchestrator (`[GSD phase N]`, `[GSD bootstrap]`, `[GSD quick]`) documentados literalmente como discriminador de modo, alineados con `buildContextSummary` en `src/orchestrator/launch.js`.
- 4 flujos de diagnóstico D-10 cubiertos: sesión stuck (`cmux read-screen` + `kodo logs --follow --session-of`), lock no liberado (`ls ~/.kodo/locks/` + JSON PID+TTL), verdict dudoso (`kodo gsd inspect`, exit codes 0/1/2), Review (`kodo gsd verify`, exit codes 0/1/2 + 64..78).
- Contrato auto-commit re-honrado en el texto: la skill apunta a `src/hooks/stop.js` / `handleSkillAutoCommit` y a `.claude/skills/` como path que el hook vigilará tras Plan 3.

## Task Commits

1. **Task 1: Crear `.claude/skills/kodo-orchestrate/skill.md` provider-agnostic v0.5** — `9a8f8c4` (feat)

_(Plan metadata commit se hace por el orquestador de la wave tras agregar SUMMARY.md.)_

## Files Created/Modified

- `.claude/skills/kodo-orchestrate/skill.md` — Skill canonical orquestador kodo (provider-agnostic, 7 secciones, 177 líneas)

## Decisions Made

Ninguna decisión nueva: el plan dictaba estructura literal sección por sección y reglas duras de redacción (D-07..D-17). El executor solo materializó esas decisiones — ningún trade-off discrecional surgió.

Detalle sobre la redacción:
- La sección §"Reglas de operación" preserva el detalle textual de selección de modelo (`Opus / Sonnet con kodo:sonnet / Haiku con kodo:haiku`) por ser la regla práctica más usada del orquestador (sugerencia del CONTEXT.md "Claude's Discretion").
- El cross-reference a `prompt.md` se redactó como párrafo corto al inicio (no como sección dedicada) para mantener legibilidad sin inflar la tabla de contenidos.
- La nota sobre IDs eliminados se dejó como paréntesis en §"Mapeo de proyectos" en vez de sección propia — el plan pedía "NOTA EXPLÍCITA" pero un párrafo dentro de la sección es suficiente para que un lector entienda por qué no hay IDs concretos (verificable: el grep gate `! grep -qE '(LIKEN|ROMAN|TENDERIO)'` pasa).

## Deviations from Plan

None — plan ejecutado exactamente como estaba escrito. Todos los 22 grep gates del bloque `acceptance_criteria` pasan; el one-liner del `<verify>` también devuelve PASS; longitud final 177 líneas (rango 80–250 OK).

## Issues Encountered

None.

## User Setup Required

None — la skill se auto-carga cuando Claude Code arranca con `cwd = repo` (D-05). No requiere registrar nada ni instalar dependencias.

## Next Phase Readiness

- **Plan 02 (simplify `src/orchestrator/prompt.md`)** puede arrancar: la skill ahora es canonical y `prompt.md` puede reducirse a un render mínimo provider-specific con cross-reference de regreso. Ningún bloqueador.
- **Plan 03 (fix `src/hooks/stop.js#handleSkillAutoCommit` D-14)** puede arrancar en paralelo: el path nuevo (`.claude/skills/kodo-orchestrate/skill.md`) ya existe en disco para que los tests del Plan 04 lo encuentren tras el fix.
- **Plan 05 (eliminar `~/.claude/skills/kodo-orchestrate/skill.md`)** queda pendiente hasta que Plans 02–04 hayan validado el nuevo flujo end-to-end.

## Self-Check: PASSED

- `.claude/skills/kodo-orchestrate/skill.md` existe (verificado con `test -f`).
- Commit `9a8f8c4` presente en `git log` de la branch worktree-agent-a67bc5216cd34ceac.
- 22/22 grep gates de `acceptance_criteria` pasan; one-liner de `<verify>` devuelve PASS.
- Longitud 177 líneas dentro del rango 80–250.

---
*Phase: 999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5*
*Plan: 01*
*Completed: 2026-05-11*
