---
phase: 999.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/prompt.md
autonomous: true
requirements: [D-02, D-03]
must_haves:
  truths:
    - "src/orchestrator/prompt.md sigue siendo válido como prompt mínimo provider-specific (fallback degradado cuando cwd ≠ repo)"
    - "Los tres placeholders {{provider_name}}, {{mcp_tool}}, {{provider}} se preservan y siguen consumidos por resolvePromptTemplate sin tocar src/orchestrator/launch.js"
    - "El prompt contiene una cabecera que cross-referencia .claude/skills/kodo-orchestrate/skill.md como fuente canonical extendida"
    - "El loop de supervisión mínimo (leer estado → revisar sesiones → actuar → repetir) sobrevive como fallback operativo"
    - "Las instrucciones GSD básicas (cuándo correr verify, qué significa [GSD quick]) sobreviven en forma compacta"
    - "El prompt no nombra a NINGÚN provider concreto (Plane, GitHub, ClickUp) ni cita tools MCP provider-specific — todo via placeholders"
  artifacts:
    - path: "src/orchestrator/prompt.md"
      provides: "Render mínimo provider-specific consumido por resolvePromptTemplate en launch.js L16–31"
      contains: "{{provider_name}}"
  key_links:
    - from: "src/orchestrator/prompt.md"
      to: "src/orchestrator/launch.js#resolvePromptTemplate"
      via: "placeholders {{provider_name}} / {{mcp_tool}} / {{provider}}"
      pattern: "\\{\\{provider"
    - from: "src/orchestrator/prompt.md"
      to: ".claude/skills/kodo-orchestrate/skill.md"
      via: "cross-reference en cabecera (D-03)"
      pattern: "\\.claude/skills/kodo-orchestrate/skill\\.md"
---

<objective>
Simplificar `src/orchestrator/prompt.md` a un render mínimo provider-specific:
mantener los 3 placeholders `{{provider_name}}` / `{{mcp_tool}}` / `{{provider}}` que
`resolvePromptTemplate` ya sustituye al spawn, y añadir cross-reference a la skill canonical.

Purpose: La skill (Plan 1) es la fuente canonical extendida del comportamiento orquestador.
`prompt.md` debe seguir vivo como **fallback degradado** cuando el orchestrator se lanza con
`cwd ≠ repo` y Claude Code no auto-carga la skill (D-02/D-05). El render mínimo evita duplicación
de contenido entre prompt.md y skill.md, y reduce el risk surface de drift.

Output: `src/orchestrator/prompt.md` reducido a un render mínimo (~30–60 líneas), con todos los
placeholders intactos, cross-ref a la skill, y el loop de supervisión esencial preservado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md

<interfaces>
<!-- Contrato del renderer (NO se modifica). El prompt.md DEBE seguir conteniendo
     los tres placeholders para que resolvePromptTemplate produzca un render válido. -->

From src/orchestrator/launch.js L16–31:
```javascript
export function resolvePromptTemplate(template, config) {
  const providerName = config.provider.charAt(0).toUpperCase() + config.provider.slice(1);
  const mcpTool = `${providerName} MCP server`;

  return template
    .replaceAll('{{provider_name}}', providerName)  // ej: "Plane"
    .replaceAll('{{provider}}', config.provider)     // ej: "plane"
    .replaceAll('{{mcp_tool}}', mcpTool);            // ej: "Plane MCP server"
}
```

Workspace cwd (justifica el fallback degradado D-05):
- launch.js L70–73: `await cmux.newWorkspace({ name: 'kodo-orchestrator', cwd: process.cwd() })`
- Si el usuario invoca `kodo orchestrator` desde fuera del repo kodo, el cmux workspace hereda
  ese cwd y Claude Code NO encuentra `.claude/skills/kodo-orchestrate/skill.md`.
- En ese caso, prompt.md es la ÚNICA fuente de instrucciones — debe seguir siendo operativo
  por sí solo (no un stub que delegue todo a la skill).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir src/orchestrator/prompt.md como render mínimo provider-specific con cross-ref a la skill</name>
  <files>src/orchestrator/prompt.md</files>

  <read_first>
    - src/orchestrator/prompt.md (estado actual, 91 líneas — preservar los placeholders y el núcleo del loop, recortar lo que ya está en la skill)
    - src/orchestrator/launch.js L16–31 (`resolvePromptTemplate` — contrato de placeholders que NO debe romperse)
    - src/orchestrator/launch.js L33–101 (`launchOrchestrator` — para confirmar que el cwd se hereda de `process.cwd()` y justificar D-05)
    - .claude/skills/kodo-orchestrate/skill.md (escrito por Plan 1 — para asegurar que el cross-ref apunta a la ruta correcta; nota: si este Plan corre en paralelo a Plan 1 en Wave 1, el executor puede asumir esa ruta literal porque ya está fijada en el contexto)
    - .planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md (D-02, D-03)
  </read_first>

  <action>
    Sobrescribir `src/orchestrator/prompt.md` con la siguiente estructura compacta
    (idioma español; slash commands del agente en inglés cuando aparezcan):

    ---
    # PRIMERA LÍNEA — placeholder render preservado

    Mantener el opener equivalente a:
    `Eres el orquestador de kodo. Tu trabajo es supervisar y coordinar las sesiones
    de Claude Code que están trabajando en tareas de {{provider_name}}.`

    # BLOQUE DE CROSS-REFERENCE (D-03) — nuevo

    Párrafo corto inmediatamente debajo del opener:

    > **Fuente canonical extendida**: la skill `.claude/skills/kodo-orchestrate/skill.md`
    > del repo `kodo` contiene el comportamiento completo del orquestador (proceso
    > de inicio, mapeo de proyectos, 4 flujos de diagnóstico, sesiones GSD full y quick,
    > mecanismo de auto-update). Cuando este prompt se lanza con `cwd = ~/dev/klab/kodo`
    > (recomendado), Claude Code la auto-carga. Si arrancas con otro cwd, este prompt
    > es la fuente degradada; pide al usuario que arranque desde el repo o consulta la
    > skill manualmente.

    # SECCIÓN: Contexto mínimo

    3 bullets compactos que preserven los placeholders:
    - Workspace cmux dedicado `kodo-orchestrator`
    - Acceso a `{{provider_name}}` via `{{mcp_tool}}`
    - Estado en `~/.kodo/state.json`

    # SECCIÓN: Loop de supervisión (núcleo del fallback degradado)

    Lista numerada de 6–8 pasos, formato compacto pero completo:
    1. `cat ~/.kodo/state.json` — ver sesiones corriendo
    2. Por cada sesión: `cmux read-screen --workspace <ref> --lines 15`
    3. Evaluar progreso / idle / errores
    4. Actuar si necesario (nudge via `cmux send`, escalar)
    5. Revisar tareas en Review (ver §"Sesiones GSD" más abajo si aplica)
    6. Lanzar nuevas tareas si hay slots (`kodo launch <task-ref>`, máximo 3)
    7. Si nada pendiente → `[kodo:idle]` y esperar
    8. Si recibes un nudge del hook Stop → ronda inmediata

    # SECCIÓN: Reglas mínimas

    Bullets:
    - Máximo 3 sesiones simultáneas
    - Opus por defecto; Sonnet si `kodo:sonnet`, Haiku si `kodo:haiku`
    - Solo lanzar tareas con label `kodo`
    - Prioridad: urgencia > impacto > esfuerzo
    - Para `{{provider_name}}`, usar el `{{mcp_tool}}` disponible en tu sesión

    # SECCIÓN: Sesiones GSD (compacta — la skill tiene el detalle)

    Texto breve (no más de ~15 líneas):
    - Las sesiones con `gsd: true` siguen un flujo de fase. Cuando entran a Review:
      ejecuta `kodo gsd verify <session-id>`. El CLI postea el comentario y
      transiciona el work item. Exit codes deterministas (0=pass, 1/2=fail,
      64..78=usage).
    - `[GSD quick]` (sesiones de `kodo:gsd-quick`) son one-shot: **NO ejecutes
      `kodo gsd verify`** — revísalas manualmente como cualquier sesión no-GSD.
    - Para dudas previas al verify: `kodo gsd inspect <task-id>` (dry-run del resolver).
    - **No dupliques el gate** en comentarios manuales al provider — el CLI es la
      única fuente para `gsd verify`.

    # CIERRE

    No incluir las secciones "Comandos disponibles" detalladas ni "Estado actual"
    estructural — la skill las cubre. El prompt mínimo debe terminar después de la
    sección GSD.

    ---

    Reglas duras:
    - PRESERVAR los placeholders literales `{{provider_name}}`, `{{mcp_tool}}`,
      `{{provider}}` exactamente como aparecen (cada uno DEBE aparecer al menos
      una vez). Son consumidos por `resolvePromptTemplate` en launch.js L16–31.
    - El cross-ref DEBE citar la ruta literal `.claude/skills/kodo-orchestrate/skill.md`.
    - Idioma del cuerpo: español. Slash commands (`kodo gsd verify`, `cmux send`)
      literales.
    - El prompt resultante debe quedar entre ~30 y ~80 líneas (compacto pero
      auto-suficiente como fallback degradado, D-02).
    - PROHIBIDO citar Plane, GitHub Issues, ClickUp ni ningún provider concreto.
      Todo provider name fluye por el placeholder `{{provider_name}}`. Si en el
      futuro se añade un provider, no debe haber que tocar prompt.md (forward-compat).
    - PROHIBIDO citar tools MCP provider-specific (`mcp__plane__*`,
      `mcp__github__*`, `mcp__clickup__*`). La referencia genérica al MCP fluye
      por `{{mcp_tool}}`.
  </action>

  <acceptance_criteria>
    - `test -f src/orchestrator/prompt.md` (archivo existe)
    - `grep -c '{{provider_name}}' src/orchestrator/prompt.md | awk '{exit !($1 >= 1)}'` (placeholder presente)
    - `grep -c '{{mcp_tool}}' src/orchestrator/prompt.md | awk '{exit !($1 >= 1)}'` (placeholder presente)
    - `grep -c '{{provider}}' src/orchestrator/prompt.md | awk '{exit !($1 >= 1)}'` (placeholder presente — nota: `{{provider_name}}` contiene la subcadena `{{provider`, así que se cuenta el literal completo `{{provider}}`)
    - `grep -q '.claude/skills/kodo-orchestrate/skill.md' src/orchestrator/prompt.md` (cross-ref D-03)
    - `grep -q 'kodo gsd verify' src/orchestrator/prompt.md` (loop GSD preservado)
    - `grep -q '\[GSD quick\]' src/orchestrator/prompt.md` (modo quick documentado)
    - `grep -q 'cat ~/.kodo/state.json' src/orchestrator/prompt.md` (paso 1 del loop)
    - `grep -q 'cmux read-screen' src/orchestrator/prompt.md` (paso 2 del loop)
    - `grep -q '\[kodo:idle\]' src/orchestrator/prompt.md` (terminación del loop)
    - `! grep -qE '\bPlane\b' src/orchestrator/prompt.md` (sin "Plane" hardcoded — debe usar placeholders)
    - `! grep -qE '\bGitHub\b' src/orchestrator/prompt.md` (sin "GitHub" hardcoded)
    - `! grep -qE '\bClickUp\b' src/orchestrator/prompt.md` (sin "ClickUp" hardcoded)
    - `! grep -qE 'mcp__(github|clickup|plane)__' src/orchestrator/prompt.md` (M4: sin tools MCP provider-specific de NINGÚN provider conocido — forward-compat v0.6+)
    - `wc -l src/orchestrator/prompt.md | awk '{exit !($1 >= 30 && $1 <= 80)}'` (longitud entre 30 y 80 líneas)
    - `node --input-type=module -e "import('./src/orchestrator/launch.js').then(m => { const t = require('fs').readFileSync('./src/orchestrator/prompt.md','utf-8'); const out = m.resolvePromptTemplate(t, {provider:'plane'}); if(out.match(/\\{\\{[^}]+\\}\\}/)) { console.error('placeholder no sustituido:', out.match(/\\{\\{[^}]+\\}\\}/g)); process.exit(1); } })"` (render con provider=plane no deja placeholders sin sustituir — invariante de launch.js)
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'F=src/orchestrator/prompt.md; test -f "$F" && grep -q "{{provider_name}}" "$F" && grep -q "{{mcp_tool}}" "$F" && grep -q "{{provider}}" "$F" && grep -q ".claude/skills/kodo-orchestrate/skill.md" "$F" && grep -q "kodo gsd verify" "$F" && grep -q "\[GSD quick\]" "$F" && grep -q "\[kodo:idle\]" "$F" && ! grep -qE "\bPlane\b" "$F" && ! grep -qE "\bGitHub\b" "$F" && ! grep -qE "\bClickUp\b" "$F" && ! grep -qE "mcp__(github|clickup|plane)__" "$F" && [ $(wc -l < "$F") -ge 30 ] && [ $(wc -l < "$F") -le 80 ] && node --input-type=module -e "import(\"./src/orchestrator/launch.js\").then(m => { const t = require(\"fs\").readFileSync(\"./src/orchestrator/prompt.md\",\"utf-8\"); const out = m.resolvePromptTemplate(t, {provider:\"plane\"}); if(out.match(/\\{\\{[^}]+\\}\\}/)) process.exit(1); })"'</automated>
  </verify>

  <done>
    `src/orchestrator/prompt.md` queda como render mínimo (~30–80 líneas), con los
    3 placeholders preservados, cross-ref a la skill canonical en cabecera, loop
    GSD compacto, sin nombres de provider concretos (Plane/GitHub/ClickUp) ni tools
    MCP provider-specific, y `resolvePromptTemplate` produce un render sin
    placeholders residuales.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt.md → resolvePromptTemplate | Cambios al template afectan TODOS los launches del orchestrator |
| prompt.md → Claude Code agent (degraded path) | Cuando cwd ≠ repo, este es el único set de instrucciones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1.02-01 | Tampering | placeholders del template | mitigate | Acceptance criteria fuerzan `grep` por los 3 placeholders + ejecución real de `resolvePromptTemplate` con `provider:'plane'` confirmando que no quedan `{{...}}` residuales tras el render. Si el executor elimina un placeholder por accidente, el test node falla. |
| T-999.1.02-02 | Denial of Service | fallback degradado vacío | mitigate | Acceptance criterion `wc -l >= 30` impide reducir el prompt a un stub. Grep gates por `kodo gsd verify`, `[GSD quick]`, `[kodo:idle]`, `cmux read-screen` aseguran que el loop esencial sobrevive (D-02). |
| T-999.1.02-03 | Information Disclosure / Forward-compat | provider name hardcoded | mitigate | Multi-grep negativo (`\bPlane\b`, `\bGitHub\b`, `\bClickUp\b`, `mcp__(github\|clickup\|plane)__`) evita reintroducir nombres concretos que el render placeholder ya cubre (Phase 5 D-22). Forward-compat para adapters v0.6+. |
</threat_model>

<verification>
- prompt.md compacto, con los 3 placeholders intactos
- Cross-ref a la skill presente
- Loop de supervisión + GSD compacto operativos
- `resolvePromptTemplate(t, {provider:'plane'})` no devuelve placeholders residuales
- Cero nombres de provider concretos (Plane/GitHub/ClickUp) ni tools MCP provider-specific
</verification>

<success_criteria>
- prompt.md reducido a render mínimo provider-specific
- Cross-reference a la skill canonical
- Auto-suficiente como fallback degradado (D-02)
- Contrato con launch.js intacto
- Forward-compat para v0.6+ adapters
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-02-SUMMARY.md`.
</output>
