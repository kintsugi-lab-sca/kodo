---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/triggers/dispatcher.js
  - test/dispatcher-isolation.test.js
autonomous: true
requirements: [LOG-13]
user_setup: []

must_haves:
  truths:
    - "`src/triggers/dispatcher.js` no contiene literal `'gsd.phase.resolved'` ni `\"gsd.phase.resolved\"` en código non-comment — los 4 callsites runtime (líneas 183, 184, 210, 211) usan `EVENTS.GSD_PHASE_RESOLVED` (D-01, D-02)."
    - "Las 4 menciones del literal en COMENTARIOS de dispatcher.js (líneas 171, 173, 203, 228 — documentación histórica D-14 Phase 9) se preservan intactas; el grep es comment-aware (D-12)."
    - "`dispatcher.js` añade `import { EVENTS } from '../logger-events.js'` al top (eager — sin coste runtime, single-source si la constante cambia) (D-02)."
    - "Los helpers `gsdPhaseResolved`/`gsdBootstrap` en `logger-events.js` NO se modifican y NO se invocan desde dispatcher.js — sus shapes fijos no cubren las variantes con `matched:false`/`code`/`tolerated`/`error_code`/`detail` (D-03)."
    - "El shape inline del payload (todas las variantes con campos extra) se preserva byte-a-byte; el `event:` key dentro del payload también migra a `EVENTS.GSD_PHASE_RESOLVED` (D-02 single-source)."
    - "Existe `test/dispatcher-isolation.test.js` con 3 asserts (D-13): no-literal `'gsd.phase.resolved'`, no-literal `'gsd.bootstrap'`, positive-import de EVENTS desde `'../logger-events.js'`."
    - "El test source-hygiene LOG-12 (`test/check-isolation.test.js`) sigue verde — `logger-events.js` no entra al grafo de `check.js`."
  artifacts:
    - path: "src/triggers/dispatcher.js"
      provides: "Migración EVENTS.* completa en los 4 literales runtime + import eager de EVENTS"
      contains: "EVENTS.GSD_PHASE_RESOLVED"
      min_lines: 330
    - path: "test/dispatcher-isolation.test.js"
      provides: "Source-hygiene guard comment-aware (3 asserts: 2 negative literal-absence + 1 positive import)"
      contains: "stripComments"
      min_lines: 60
  key_links:
    - from: "src/triggers/dispatcher.js"
      to: "src/logger-events.js"
      via: "import { EVENTS } from '../logger-events.js'"
      pattern: "import\\s+\\{[^}]*\\bEVENTS\\b[^}]*\\}\\s+from\\s+['\"]\\.\\.\\/logger-events\\.js['\"]"
    - from: "src/triggers/dispatcher.js#info-noMatch-tolerated"
      to: "EVENTS.GSD_PHASE_RESOLVED"
      via: "log.info(EVENTS.GSD_PHASE_RESOLVED, { event: EVENTS.GSD_PHASE_RESOLVED, ... })"
      pattern: "log\\.info\\(EVENTS\\.GSD_PHASE_RESOLVED"
    - from: "src/triggers/dispatcher.js#warn-failClosed"
      to: "EVENTS.GSD_PHASE_RESOLVED"
      via: "log.warn(EVENTS.GSD_PHASE_RESOLVED, { event: EVENTS.GSD_PHASE_RESOLVED, ... })"
      pattern: "log\\.warn\\(EVENTS\\.GSD_PHASE_RESOLVED"
    - from: "test/dispatcher-isolation.test.js"
      to: "src/triggers/dispatcher.js"
      via: "readFileSync + stripComments + assert.ok(!stripped.includes(...))"
      pattern: "stripComments\\("
---

<objective>
Cerrar LOG-13: migrar los 4 literales runtime `'gsd.phase.resolved'` en `src/triggers/dispatcher.js` (líneas 183, 184, 210, 211) a la constante `EVENTS.GSD_PHASE_RESOLVED`, añadir el import eager de `EVENTS` desde `'../logger-events.js'`, y crear el test source-hygiene `test/dispatcher-isolation.test.js` con 3 asserts comment-aware (D-12, D-13).

**Importante (corrección a CONTEXT.md verificada por pattern-mapper):** SOLO 4 literales `'gsd.phase.resolved'` existen en runtime — CERO literales `'gsd.bootstrap'` (la rama matched-true línea 251 ya usa el helper `gsdBootstrap()` importado dinámicamente). Este plan migra 4 sustituciones, no 5. El test sigue afirmando ambos strings ausentes en código no-comment como guardia anti-regresión.

**Estrategia (D-01):** sustitución literal → constante 1-a-1, **sin** usar los helpers `gsdPhaseResolved()`/`gsdBootstrap()` (D-03 — sus shapes fijos `{phase_id, match_heading, mode}` no cubren los campos `matched:false`, `code`, `tolerated`, `error_code`, `detail`, `task_ref` que las variantes de dispatcher emiten). Forzar el helper exigiría modificar el helper (rompe callers happy-path) o crear helpers nuevos (overkill — explícitamente deferred en CONTEXT línea 124-126).

Purpose: cubrir SC#1 ROADMAP §Phase 16 (taxonomía cerrada — single-source de los strings canónicos) + REQ LOG-13.
Output: `src/triggers/dispatcher.js` modificado (1 import + 4 sustituciones runtime, comentarios intactos) y `test/dispatcher-isolation.test.js` nuevo con 3 asserts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md
@.planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md

# Source of truth — read BEFORE editing
@src/triggers/dispatcher.js
@src/logger-events.js
@test/check-isolation.test.js
@test/stop.test.js

<interfaces>
<!-- API que este plan consume desde src/logger-events.js (read-only — NO se modifica). -->

From src/logger-events.js (named exports):
```javascript
export const EVENTS = Object.freeze({
  // ...
  STATE_TRANSITION: 'state.transition',
  GSD_PHASE_RESOLVED: 'gsd.phase.resolved',
  GSD_BOOTSTRAP: 'gsd.bootstrap',
  // ...
});

// Helpers existentes — NO se usan desde dispatcher.js (D-03):
export function gsdPhaseResolved(logger, fields): void
// Acepta SOLO {phase_id, match_heading, mode} — shape canónico matched-true
// dispatcher.js emite shapes heterogéneos con matched:false / code / tolerated
// / error_code / detail / task_ref — incompatibles. Quedan disponibles para
// otros callers (matched-true happy path) pero dispatcher mantiene literal→constante.

export function gsdBootstrap(logger, fields): void
// Idem — happy path matched-true. Dispatcher line 251 ya lo usa via dynamic import.
```

Líneas exactas a migrar en `src/triggers/dispatcher.js` (verificadas por pattern-mapper 2026-05-06):
- Línea 183: `log.info('gsd.phase.resolved', {`            → `log.info(EVENTS.GSD_PHASE_RESOLVED, {`
- Línea 184: `  event: 'gsd.phase.resolved',`              → `  event: EVENTS.GSD_PHASE_RESOLVED,`
- Línea 210: `log.warn('gsd.phase.resolved', {`            → `log.warn(EVENTS.GSD_PHASE_RESOLVED, {`
- Línea 211: `  event: 'gsd.phase.resolved',`              → `  event: EVENTS.GSD_PHASE_RESOLVED,`

Líneas que NO se modifican (comentarios documentando contrato histórico — D-12 grep comment-aware los respeta):
- Línea 171: `// gsd.phase.resolved {matched:false, code:'no-match', tolerated:true,`
- Línea 173: `// Dispatcher remains the single source of gsd.phase.resolved (D-14 Phase 9).`
- Línea 203: `// D-14: emit gsd.phase.resolved with matched:false for forensic logging.`
- Línea 228: `// D-14: emit matched-true gsd.phase.resolved (phase branch) or gsd.bootstrap (bootstrap branch).`

Patrón reutilizable a copiar (comment-aware filter — analog: `test/stop.test.js:62-67`):
```javascript
function stripComments(src) {
  return src
    .replace(/\/\*[\s\S]*?\*\//g, '')
    .split('\n')
    .filter((line) => !line.trim().startsWith('//') && !line.trim().startsWith('*'))
    .join('\n');
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar 4 literales runtime + import EVENTS en src/triggers/dispatcher.js</name>
  <files>src/triggers/dispatcher.js</files>

  <read_first>
    - src/triggers/dispatcher.js (estado actual completo — confirmar líneas 183, 184, 210, 211 antes de editar; el contenido alrededor puede haber cambiado vs. PATTERNS.md si hubo commits intermedios)
    - src/logger-events.js (confirmar que `EVENTS.GSD_PHASE_RESOLVED` es export named y vale `'gsd.phase.resolved'`)
    - .planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md sección "1. `src/triggers/dispatcher.js` — EVENTS.* migration" (líneas 39-207)
    - .planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md decisions D-01, D-02, D-03
  </read_first>

  <behavior>
    - Test 1 (post-edit, runtime invariante): `node --test test/dispatcher.test.js` sigue verde — el shape de los eventos `gsd.phase.resolved` NO cambia (mismo `event:` key, mismos campos). Si los tests de dispatcher asertan sobre el primer arg de `log.info`/`log.warn` por literal `'gsd.phase.resolved'`, deben seguir pasando porque `EVENTS.GSD_PHASE_RESOLVED === 'gsd.phase.resolved'` (la constante resuelve al mismo string).
    - Test 2 (import añadido): el archivo contiene `import { EVENTS } from '../logger-events.js'` exactamente una vez en la cabecera (no duplicado), con `EVENTS` como named import.
    - Test 3 (sustitución completa): `grep -c "EVENTS.GSD_PHASE_RESOLVED" src/triggers/dispatcher.js` >= 4 (las 4 líneas 183, 184, 210, 211 migradas). Las 4 líneas siguen viviendo dentro de los try-blocks emisores (D-01: shape inline preservado, no se mueven a helper).
    - Test 4 (literal eliminado en código): tras `stripComments` el archivo NO contiene `'gsd.phase.resolved'` ni `"gsd.phase.resolved"`. Los comentarios sí los mencionan (líneas 171, 173, 203, 228) y se respetan.
    - Test 5 (literal `'gsd.bootstrap'` ausente): tras `stripComments` el archivo NO contiene `'gsd.bootstrap'` ni `"gsd.bootstrap"`. Hoy ya no aparece en runtime — la helper `gsdBootstrap()` importada dinámicamente en línea ~231 sigue siendo el único path. El test lo afirma defensivamente para bloquear regresión futura.
    - Test 6 (LOG-12 invariante): `node --test test/check-isolation.test.js` exit 0 — `logger-events.js` no se importa transitivamente desde `check.js`.
  </behavior>

  <action>
    1. **Localizar las 4 líneas runtime** con grep (confirmar offset actual antes de editar — los números pueden haber cambiado por commits posteriores al pattern-mapping):
       ```bash
       grep -n "'gsd.phase.resolved'\|\"gsd.phase.resolved\"" src/triggers/dispatcher.js
       ```
       Esperado: 4 matches en código (no en comentarios). Si hay más, **DETENERSE y leer** la sección completa antes de editar — algo cambió desde 2026-05-06.

    2. **Añadir el import eager** al top del archivo (D-02). Insertar tras `import { resolvePhase } from '../gsd/resolver.js';` (línea 9) o tras `import { buildBriefFromTask, isBriefEmpty } from '../gsd/brief.js';` (línea 10) — el orden exacto no importa, pero debe quedar AGRUPADO con los demás imports `from '../...'` para mantener la convención del archivo:
       ```javascript
       import { EVENTS } from '../logger-events.js';
       ```

       LOG-12 invariante: `logger-events.js` solo importa `node:os` y `node:path`, sin side effects ni transitivamente `logger.js`. El walker de `test/check-isolation.test.js` no lo señala.

    3. **Migrar el callsite INFO (no-match tolerated)** — buscar el bloque `try { const { createLogger } = await import('../logger.js'); ... log.info('gsd.phase.resolved', { event: 'gsd.phase.resolved', matched: false, code: 'no-match', tolerated: true, mode: 'quick', task_ref: task.ref, }); } catch { ... }`. Sustituir AMBOS `'gsd.phase.resolved'` (primer arg de `log.info` Y `event:` key del payload) por `EVENTS.GSD_PHASE_RESOLVED`:

       ```javascript
       // ANTES (líneas 183-194 actuales):
       log.info('gsd.phase.resolved', {
         event: 'gsd.phase.resolved',
         matched: false,
         code: 'no-match',
         tolerated: true,
         mode: 'quick',
         task_ref: task.ref,
       });

       // DESPUÉS:
       log.info(EVENTS.GSD_PHASE_RESOLVED, {
         event: EVENTS.GSD_PHASE_RESOLVED,
         matched: false,
         code: 'no-match',
         tolerated: true,
         mode: 'quick',
         task_ref: task.ref,
       });
       ```

       NO TOCAR el resto del bloque: el try/catch silencioso (`catch {}` con comentario `silent — never block dispatch on logger failure`), el child logger, ni los campos del payload. Solo los dos strings.

    4. **Migrar el callsite WARN (fail-closed)** — mismo patrón sobre el bloque `log.warn('gsd.phase.resolved', { event: 'gsd.phase.resolved', matched: false, error_code: ..., detail: ..., task_ref: ..., mode: gsdMode, })`:

       ```javascript
       // ANTES (líneas 210-217 actuales):
       log.warn('gsd.phase.resolved', {
         event: 'gsd.phase.resolved',
         matched: false,
         error_code: resolverVerdict.code,
         detail: resolverVerdict.detail,
         task_ref: task.ref,
         mode: gsdMode,  // D-07 schema homogeneity: warn fail-closed also distinguishes mode
       });

       // DESPUÉS:
       log.warn(EVENTS.GSD_PHASE_RESOLVED, {
         event: EVENTS.GSD_PHASE_RESOLVED,
         matched: false,
         error_code: resolverVerdict.code,
         detail: resolverVerdict.detail,
         task_ref: task.ref,
         mode: gsdMode,  // D-07 schema homogeneity: warn fail-closed also distinguishes mode
       });
       ```

       Preservar el comentario inline `// D-07 schema homogeneity: ...`.

    5. **NO TOCAR los siguientes elementos de `src/triggers/dispatcher.js`**:
       - Los 4 comentarios documentando el contrato histórico D-14 (líneas 171, 173, 203, 228 — el grep comment-aware los respeta y son referencia útil para el lector).
       - El branch matched-true (línea ~231-256) que ya usa el helper `gsdBootstrap()` via dynamic import (`const { gsdBootstrap } = await import('../logger-events.js');`). NO se cambia a `EVENTS.GSD_BOOTSTRAP` directo — el helper sigue siendo el patrón canónico para shapes happy-path (D-03).
       - El branch matched-true de `gsdPhaseResolved()` (línea ~241) — mismo razonamiento (D-03).
       - Los `try { const { createLogger } = await import('../logger.js'); ... }` lazy imports — patrón establecido para no penalizar startup del trigger handler.
       - El comentario "silent — never block dispatch/return on logger failure" en cada catch.
       - Los demás imports del archivo (líneas 1-11). El nuevo import se INSERTA, no reemplaza nada.

    6. **Verificación post-edit (mental, antes de correr tests)**:
       - `grep -c "EVENTS.GSD_PHASE_RESOLVED" src/triggers/dispatcher.js` debe dar **4** (los 4 sites runtime).
       - `grep -c "'gsd.phase.resolved'" src/triggers/dispatcher.js` debe dar exactamente **4** (ahora SOLO en comentarios — líneas 171, 173, 203, 228).
       - `grep -c "import { EVENTS } from '../logger-events.js'" src/triggers/dispatcher.js` debe dar **1**.
  </action>

  <verify>
    <automated>node --test test/dispatcher.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { EVENTS } from '../logger-events.js'" src/triggers/dispatcher.js` === 1
    - `grep -c "EVENTS.GSD_PHASE_RESOLVED" src/triggers/dispatcher.js` >= 4
    - Comment-aware grep: filtrando líneas que empiezan con `//` o `*`, el archivo NO contiene `'gsd.phase.resolved'` ni `"gsd.phase.resolved"`. Verificable inline:
      ```bash
      grep -v '^\s*//' src/triggers/dispatcher.js | grep -v '^\s*\*' | grep -c "'gsd.phase.resolved'\|\"gsd.phase.resolved\""
      ```
      debe dar `0`.
    - Comment-aware grep: idem para `'gsd.bootstrap'` / `"gsd.bootstrap"` debe dar `0` (ya hoy es 0, el invariante no cambia).
    - `node --test test/dispatcher.test.js` exit 0 (los tests existentes que asertan sobre el shape del evento siguen verdes — `EVENTS.GSD_PHASE_RESOLVED === 'gsd.phase.resolved'` por valor).
    - `node --test test/check-isolation.test.js` exit 0 (LOG-12 invariante preservado — `logger-events.js` no entra al grafo de `check.js`).
  </acceptance_criteria>

  <done>
    Los 4 literales runtime en dispatcher.js se sustituyeron por `EVENTS.GSD_PHASE_RESOLVED`. El import eager de `EVENTS` está en cabecera. Los comentarios documentando el contrato histórico siguen intactos. `node --test test/dispatcher.test.js` y `test/check-isolation.test.js` pasan sin fallos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test/dispatcher-isolation.test.js con 3 asserts comment-aware (D-12, D-13)</name>
  <files>test/dispatcher-isolation.test.js</files>

  <read_first>
    - test/check-isolation.test.js (estado completo — naming + estructura imports + path setup; copiar el bloque __dirname/REPO/SRC verbatim)
    - test/stop.test.js líneas 60-72 (helper `stripComments` que se eleva a función nombrada en este test)
    - test/format-isolation.test.js líneas ~141-163 (positive import assert pattern — assert.match con regex que captura specifier relativo concreto)
    - src/triggers/dispatcher.js (post-edit Task 1 — el archivo cuyo source escanea el test)
    - .planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md sección "4. `test/dispatcher-isolation.test.js` (NEW) — D-12, D-13" (líneas 458-562)
    - .planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md decisions D-12, D-13
  </read_first>

  <behavior>
    - Test 1 (no-literal `'gsd.phase.resolved'` en código): tras `stripComments(source)`, `stripped.includes("'gsd.phase.resolved'") === false` Y `stripped.includes('"gsd.phase.resolved"') === false`. Mensaje: 'src/triggers/dispatcher.js must not contain literal "gsd.phase.resolved" in code (use EVENTS.GSD_PHASE_RESOLVED). Comments documenting the historical contract are allowed.'
    - Test 2 (no-literal `'gsd.bootstrap'` en código): idem para ambas variantes single/double quote. Hoy pasa trivialmente (ya 0 ocurrencias en runtime). El test es defensivo — bloquea regresión futura si alguien re-introduce un `log.info('gsd.bootstrap', ...)` literal.
    - Test 3 (positive import EVENTS): regex match `/import\s+\{[^}]*\bEVENTS\b[^}]*\}\s+from\s+['"]\.\.\/logger-events\.js['"]/` contra el source crudo (NO stripped — el import vive en la cabecera, fuera de comentarios). Acepta named imports con extras, e.g. `import { EVENTS, gsdBootstrap } from '../logger-events.js'`. Mensaje: 'dispatcher.js must import { EVENTS } from "../logger-events.js" (Phase 16 D-02 wiring)'.
    - Test 4 (regression — el helper stripComments funciona): incluir un mini self-test que verifica que `stripComments("// hello\nlive\n* doc\nrun")` retorna `"live\nrun"` (filtra líneas que empiezan con `//` o con `*` indented). Esto es opcional pero deja el contrato del helper en el archivo.
  </behavior>

  <action>
    1. **Crear `test/dispatcher-isolation.test.js`** con la estructura siguiente. Mirroring de `test/check-isolation.test.js` (naming + setup) + helper de `test/stop.test.js:62-67`:

       ```javascript
       import { describe, it } from 'node:test';
       import assert from 'node:assert/strict';
       import { readFileSync } from 'node:fs';
       import { fileURLToPath } from 'node:url';
       import { dirname, resolve, join } from 'node:path';

       const __dirname = dirname(fileURLToPath(import.meta.url));
       const REPO = resolve(__dirname, '..');
       const SRC = join(REPO, 'src');
       const DISPATCHER_PATH = join(SRC, 'triggers', 'dispatcher.js');

       /**
        * Strip block comments + line comments + JSDoc continuation lines.
        * Used by source-hygiene tests that want to assert literal-absence in CODE
        * (not in documentation prose). Comments in dispatcher.js documenting the
        * historical contract D-14 (Phase 9) mention 'gsd.phase.resolved' literally
        * and must be tolerated — this helper filters them out.
        *
        * Mirrors test/stop.test.js:62-67 (Phase 13 source-hygiene).
        *
        * @param {string} src
        * @returns {string} the source with all comment lines removed
        */
       function stripComments(src) {
         return src
           .replace(/\/\*[\s\S]*?\*\//g, '')
           .split('\n')
           .filter((line) => !line.trim().startsWith('//') && !line.trim().startsWith('*'))
           .join('\n');
       }

       describe('LOG-13: dispatcher source hygiene (Phase 16 SC#1)', () => {
         it('does not contain literal "gsd.phase.resolved" in non-comment code (uses EVENTS.GSD_PHASE_RESOLVED)', () => {
           const source = readFileSync(DISPATCHER_PATH, 'utf-8');
           const stripped = stripComments(source);
           assert.ok(
             !stripped.includes("'gsd.phase.resolved'") && !stripped.includes('"gsd.phase.resolved"'),
             'src/triggers/dispatcher.js must not contain literal "gsd.phase.resolved" in code ' +
               '(use EVENTS.GSD_PHASE_RESOLVED). Comments documenting the historical contract are allowed.',
           );
         });

         it('does not contain literal "gsd.bootstrap" in non-comment code (uses EVENTS.GSD_BOOTSTRAP or gsdBootstrap helper)', () => {
           const source = readFileSync(DISPATCHER_PATH, 'utf-8');
           const stripped = stripComments(source);
           assert.ok(
             !stripped.includes("'gsd.bootstrap'") && !stripped.includes('"gsd.bootstrap"'),
             'src/triggers/dispatcher.js must not contain literal "gsd.bootstrap" in code ' +
               '(use EVENTS.GSD_BOOTSTRAP or the gsdBootstrap helper).',
           );
         });

         it('imports EVENTS from logger-events.js (forces wiring — Phase 16 D-02)', () => {
           const source = readFileSync(DISPATCHER_PATH, 'utf-8');
           // Path relative from src/triggers/dispatcher.js to src/logger-events.js es '../logger-events.js'.
           // Acepta la forma `import { EVENTS } from '...'` o `import { EVENTS, ... } from '...'`
           // (named import, posiblemente con extras como gsdBootstrap).
           assert.match(
             source,
             /import\s+\{[^}]*\bEVENTS\b[^}]*\}\s+from\s+['"]\.\.\/logger-events\.js['"]/,
             'dispatcher.js must import { EVENTS } from "../logger-events.js" (Phase 16 D-02 wiring)',
           );
         });
       });
       ```

    2. **NO replicar el walker LOG-12 ni asserts transitivos** (anti-pattern explícito en CONTEXT línea 124-126). Single-file scan basta — dispatcher.js no introduce indirecciones (no hay `const EVT = 'gsd.phase.resolved'` que escape al grep). Si en el futuro alguien añade `const EVT = 'gsd.phase.resolved'; log.info(EVT, ...)`, los tests 1/2 detectan el RHS del const en el non-comment code y fallan — comportamiento deseado.

    3. **Convención de naming**: el archivo se llama `test/dispatcher-isolation.test.js` paralelo a `test/check-isolation.test.js` y `test/format-isolation.test.js`. El sufijo `-isolation` indica source-hygiene contra un archivo concreto.
  </action>

  <verify>
    <automated>node --test test/dispatcher-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - El archivo `test/dispatcher-isolation.test.js` existe.
    - `grep -c "stripComments" test/dispatcher-isolation.test.js` >= 2 (definición + 2 usos en los tests 1 y 2).
    - `grep -c "DISPATCHER_PATH" test/dispatcher-isolation.test.js` >= 4 (constante + 3 lecturas en los tests).
    - `grep -c "'gsd.phase.resolved'" test/dispatcher-isolation.test.js` >= 1 (assert content — el test PUEDE contener el string literal entre asserts y mensajes de error; lo crítico es que el subject `dispatcher.js` no lo tenga en código).
    - El test contiene EXACTAMENTE 3 `it(...)` blocks dentro del describe (no más, no menos).
    - `node --test test/dispatcher-isolation.test.js` exit 0 — los 3 asserts pasan tras Task 1 completada.
    - El test FALLA si Task 1 no se completó: si dispatcher.js sigue con literales runtime, el test 1 explota con el mensaje 'src/triggers/dispatcher.js must not contain literal "gsd.phase.resolved" in code'. Verificable revirtiendo Task 1 mentalmente.
  </acceptance_criteria>

  <done>
    `test/dispatcher-isolation.test.js` existe con 3 asserts comment-aware (2 negative literal-absence + 1 positive import). `node --test test/dispatcher-isolation.test.js` pasa. Bloquea regresión de los literales runtime y exige el import eager de EVENTS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger handler → logger | dispatcher.js emite eventos NDJSON al logger desde un handler invocado por el server. Falla del logger NO debe bloquear el dispatch. |
| source-hygiene test → src/triggers/dispatcher.js | el test lee el archivo del subject y assert-a contra su contenido. Read-only — no mutación. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | T (Tampering) | dispatcher.js EVENTS import | mitigate | Single-source via `EVENTS.GSD_PHASE_RESOLVED`: si alguien renombra el evento canónico en `logger-events.js`, dispatcher.js cambia automáticamente — drift imposible (D-02). Test 3 de dispatcher-isolation enforces el import. |
| T-16-02 | I (Information disclosure) | NDJSON event payloads | accept | Los payloads existentes ya pasaron review en Phase 9; este plan solo migra el primer arg + `event:` key, NO añade ni quita campos. Sin cambio de superficie de información. |
| T-16-03 | D (Denial of Service) | logger failure blocking dispatch | mitigate | El try/catch silencioso (`catch {}` con comentario "silent — never block dispatch on logger failure") ya existe en los emisores afectados. Plan NO toca los catch — patrón preservado byte-a-byte. |
| T-16-04 | R (Repudiation) | regression del literal | mitigate | `test/dispatcher-isolation.test.js` con 3 asserts comment-aware bloquea anyone re-añadiendo `log.info('gsd.phase.resolved', ...)` directo. El test corre en `node --test` y se ejecuta en CI (parte del suite global). |
| T-16-05 | T (Tampering) | LOG-12 guard regression | mitigate | El nuevo import `from '../logger-events.js'` añade un nodo al grafo de dispatcher.js, pero `logger-events.js` no entra al grafo de `check.js` (verificado por `test/check-isolation.test.js`, que sigue verde — acceptance criteria explícita). Phase 14 ya verificó que `format-isolation.test.js` pasa por una vía distinta — no relevante aquí. |

**Notas**: ninguna amenaza HIGH. Phase 16 es debt cleanup interno con superficie de cambio mínima (1 import + 4 sustituciones de string + 1 test nuevo de 3 asserts). Sin nuevos endpoints, sin nuevos consumidores externos.
</threat_model>

<verification>
- `node --test test/dispatcher.test.js test/dispatcher-isolation.test.js test/check-isolation.test.js` exit 0 (subset crítico).
- `node --test` global pasa sin nuevos fallos respecto a la baseline Phase 15 (414+ tests, todos verdes).
- Manual sanity: abrir `src/triggers/dispatcher.js` y confirmar que el import `EVENTS` está en cabecera y que las 4 ocurrencias de la constante están en los 4 emisores correctos (líneas ~183, ~184, ~210, ~211 — los offsets pueden haber cambiado).
- Manual sanity: los comentarios D-14 (líneas 171, 173, 203, 228) siguen mencionando los strings literales — confirma que el comment-aware filter funciona y que la documentación histórica no se perdió.
</verification>

<success_criteria>
LOG-13 cubierto. SC#1 ROADMAP §Phase 16: dispatcher.js usa `EVENTS.GSD_PHASE_RESOLVED` en los 4 callsites runtime; un test source-hygiene grep contra `src/triggers/dispatcher.js` falla si los literales reaparecen en código no-comment.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/16-log-09-debt-cleanup/16-01-SUMMARY.md` con: archivos modificados, líneas de los 4 emisores migrados (offset real verificado por grep), import añadido, los 4 comentarios D-14 preservados, el nuevo test pasa, LOG-12 sigue verde.
</output>
