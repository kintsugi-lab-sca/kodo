---
phase: 16
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/stop.js
  - test/stop-state-transition.test.js
autonomous: true
requirements: [LOG-15]
user_setup: []

must_haves:
  truths:
    - "`src/hooks/stop.js` invoca `markSessionStatus(session.task_id, 'done', 'session-stop:lock-released', log)` DENTRO de la rama `if (session.gsd) { ... }`, ANTES del try que importa `releaseGsdLock` (D-04, D-07, D-08 — orden PRE-release, mirroring del patrón session.end línea 116)."
    - "El estado terminal es **fixed `'done'`** para ambos modos (full + quick) — stop.js es hook mecánico, NO infiere modo, NO consulta verdict, NO deriva del transcript (D-04). Si verify.js dejó la sesión en `'review'` (full happy path), state.transition emite `from='review' to='done'` (D-05). Si stop dispara antes que verify (quick), emite `from='running' to='done'`. Ambas válidas (D-05)."
    - "El reason `'session-stop:lock-released'` (D-06) es informativo y consistente con el formato de otros eventos del repo (e.g. `'gate-passed'`, `'plane-unreachable'`)."
    - "El bloque `markSessionStatus` se envuelve en try/catch silencioso (`catch {}` con comentario `silent — never block lock release on logger failure (mirrors session.end pattern line 116)`) — un fallo defensivo del logger NO impide el releaseGsdLock que viene después (D-09)."
    - "La rama no-GSD (implícita por estar fuera del `if (session.gsd)`) sigue sin emitir `state.transition` — `markSessionStatus` NO se invoca para sesiones no-GSD (D-07). El behavior pre-Phase 16 byte-a-byte se preserva."
    - "Phase 11 D-08 preservado: el block `if (session.gsd)` sigue cubriendo ambos modos (full + quick). markSessionStatus se invoca UNA vez en ambos casos."
    - "**W-2: el evento `sessionEnd` (líneas 116-134 actuales de stop.js) se preserva en su sitio durante el refactor — sigue emitiéndose ANTES de `removeSession` y dentro del cuerpo de `runStopHook`**. Patrón Phase 13 'emit BEFORE mutation' intacto."
    - "`main()` se refactoriza light a `runStopHook(input, deps)` exportable + `main()` wrapper que parsea stdin y llama `runStopHook(input)`. **W-4: deps efectivos enumerados explícitamente (ver Task 1 step 3) — `findSessionFn`, `removeSessionFn`, `cmux`, `loggerFactory`. El refactor preserva la lógica idéntica — solo añade DI por deps para tests unitarios. NO viola D-04 (no infiere modo) — solo cambia la estructura de invocación.**"
    - "El test file nuevo `test/stop-state-transition.test.js` cubre 3 escenarios SC#5 + 1 invariante D-04 mandatory (N-2): (1) full mode session.status='review' + lock release → emits state.transition from=review to=done; (2) quick mode session.status='running' + lock release → emits state.transition from=running to=done; (3) non-GSD session.gsd=false → NO emite state.transition (sanity: removeSession sí ejecuta); (4) D-04 invariante mandatory — ambos modos emiten to='done' fijo."
    - "Phase 13 D-09/D-10 anti-inline `session.gsd_mode` invariante preservado: stop.js Phase 16 NO accede a `session.gsd_mode` ni a `getSessionMode(session)` para decidir nada — solo a `session.task_id`, `session.session_id`, `session.status`. `test/stop.test.js` source-hygiene tests siguen verdes."
    - "**D-05: state.transition acepta from='review' to='done' cuando full pasó por verify primero (D-05 LOCKED). El test 1 (full mode) valida exactamente esa transición.**"
    - "**D-06: el reason `'session-stop:lock-released'` es el string canónico exacto del campo reason en state.transition (D-06 LOCKED). Los tests 1 y 2 asertan literalmente sobre este string.**"
  artifacts:
    - path: "src/hooks/stop.js"
      provides: "markSessionStatus PRE-release dentro de if (session.gsd) + try/catch silencioso + runStopHook export para tests + sessionEnd preservado"
      contains: "markSessionStatus(session.task_id, 'done', 'session-stop:lock-released'"
      min_lines: 215
    - path: "test/stop-state-transition.test.js"
      provides: "4 tests SC#5 (full / quick / no-GSD / D-04 invariante mandatory) con memSink fake logger + fixture session sintético"
      contains: "runStopHook"
      min_lines: 130
  key_links:
    - from: "src/hooks/stop.js#if (session.gsd) block"
      to: "src/session/manager.js#markSessionStatus"
      via: "lazy dynamic import + invocación PRE-releaseGsdLock"
      pattern: "markSessionStatus\\(session\\.task_id,\\s*'done',\\s*'session-stop:lock-released'"
    - from: "src/hooks/stop.js"
      to: "exportable runStopHook(input, deps)"
      via: "extract body of main() preserving lógica idéntica + DI por findSessionFn/removeSessionFn/cmux/loggerFactory"
      pattern: "export\\s+async\\s+function\\s+runStopHook"
    - from: "test/stop-state-transition.test.js"
      to: "runStopHook + memSink fake logger"
      via: "DI via deps.loggerFactory + deps.findSessionFn/removeSessionFn + fixture state.json sintético"
      pattern: "runStopHook\\("
    - from: "src/hooks/stop.js#try/catch silent"
      to: "session.end pattern line 116"
      via: "comentario 'mirrors session.end pattern line 116' enforce-able por test source-hygiene si se desea"
      pattern: "silent — never block lock release on logger failure"
    - from: "src/hooks/stop.js#runStopHook body"
      to: "sessionEnd emit (W-2 invariant)"
      via: "sessionEnd(log, ...) preservado ANTES de removeSession dentro del cuerpo de runStopHook"
      pattern: "sessionEnd\\(log"
---

<objective>
Cerrar LOG-15: cablear `markSessionStatus(session.task_id, 'done', 'session-stop:lock-released', log)` en `src/hooks/stop.js` dentro de la rama `if (session.gsd) { ... }`, ANTES del try que invoca `releaseGsdLock` (D-08 — patrón "emit BEFORE mutation"). Refactor light de `main()` a `runStopHook(input, deps)` exportable para permitir tests unitarios SC#5 con memSink fake logger sin spawn de child process.

**Estrategia (D-04, D-05, D-06, D-07, D-08, D-09):**
- Estado terminal **fixed `'done'`** para ambos modos (D-04 — stop.js es hook mecánico, no infiere).
- Solo dentro de `if (session.gsd)` (D-07 — la rama no-GSD del switch sigue sin state.transition).
- PRE-release del lock (D-08 — mirror de session.end línea 116 que ya está antes de removeSession).
- Try/catch silencioso (D-09 — never block lock release on logger failure).
- Acepta `from='review' to='done'` cuando full pasó por verify primero (D-05).
- Reason canónico `'session-stop:lock-released'` (D-06).

**Decisión de testing (Discretion CONTEXT línea 154-156):** opción (a) light refactor — extraer el body de `main()` a `runStopHook(input, deps = {})` exportable, dejando `main()` como wrapper que parsea stdin. Justificación:
- (a) usa el patrón DI ya establecido en `runGsdVerify(opts, deps)` (verify.js Phase 10) — coherente con el codebase.
- (a) no viola D-04 ("hook mecánico, no infiere modo") — D-04 es comportamiento, no estructura. La función refactorizada hace EXACTAMENTE lo mismo que la actual.
- (b) spawn child process sería ~3x más lento + frágil con timing del fs.watchFile + setup de NDJSON file fixture + cleanup. Desproporcionado para 4 asserts.
- El refactor es light: ~15 líneas de wrapping, signatura `(input, deps)`, defaults vía `deps.findSessionFn || findSession` etc. Test path único; runtime path inalterado.

Purpose: cubrir SC#4 + SC#5 ROADMAP §Phase 16 (state.transition emit cuando lock release + cobertura full/quick/no-GSD).
Output: `src/hooks/stop.js` (1 import lazy + ~12 líneas insertadas en `if (session.gsd)` + ~15 líneas de wrapping para `runStopHook` export — DI completa por W-4); `test/stop-state-transition.test.js` (4 tests con memSink + fixture session sintético — N-2 hace mandatory el test 4 D-04 invariante).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md
@.planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md

# Source of truth — read BEFORE editing
@src/hooks/stop.js
@src/session/manager.js
@src/logger-events.js
@test/stop.test.js
@test/gsd-verify-integration.test.js

<interfaces>
<!-- API que este plan consume + estructura del refactor light. -->

From src/session/manager.js (mismo helper que Plan 02):
```javascript
export function markSessionStatus(taskId, nextStatus, reason, logger): void
// Emite state.transition vía logger.child({component:'session', task_id}).info(EVENTS.STATE_TRANSITION, {...})
// Tipos válidos para nextStatus: 'running' | 'done' | 'error' | 'review' | 'interrupted'
// from se lee del session record actual ANTES de mutar
```

From src/hooks/stop.js (estado actual relevante — patrón a mirror, líneas 116-134):
```javascript
// Emit typed session.end event BEFORE removeSession so the logger
// captures the transition while the session record still exists.
// Silent-failure: never crash Claude Code stop hook.
try {
  const { createLogger } = await import('../logger.js');
  const { sessionEnd } = await import('../logger-events.js');
  const log = createLogger({
    sessionId: session.session_id,
    minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
  }).child({ component: 'hook', task_id: session.task_id });
  sessionEnd(log, { /* ... */ });
} catch {
  // silent — never crash Claude Code
}
```

**Deps efectivos del refactor (W-4 enumeration — derivado de lectura completa de stop.js#main() líneas 71-165):**

Side-effects que `main()` ejecuta:
1. `findSession({sessionId, cwd})` — line 79 — lookup de la sesión en state.json. **Mandatory para tests** (los tests SC#5 inyectan stub para fixture sintético).
2. `cmux.setColor({workspace, color})` — line 100 — cambia color del workspace cmux. **Opcional para tests** (los tests podrían tolerar el real client si está disponible, pero por aislamiento es preferible stub-ear). El catch local ya silencia errores.
3. `cmux.notify({title, body, workspace})` — line 109 — notificación del workspace. Catch local. **Opcional para tests**.
4. `createLogger` (lazy import via `'../logger.js'`) + `sessionEnd` (lazy import via `'../logger-events.js'`) — lines 116-134 — sessionEnd typed event. **W-2: este bloque DEBE preservarse en su sitio dentro de `runStopHook` — viaja con el cuerpo refactorizado y queda ANTES de `removeSession`**.
5. `createLogger` + `markSessionStatus` (lazy imports) — **NUEVO Phase 16** — bloque añadido en step 2 de Task 1.
6. `releaseGsdLock` (lazy import via `'../gsd/lock.js'`) — line 139 — release del lock per-repo. Catch local con console.error. **Opcional para tests** (los tests SC#5 no asertan sobre el lock — solo sobre state.transition).
7. `removeSession(id)` — line 146 — borra la entry de state.json. **Mandatory para tests** (el test 3 no-GSD assert-a que removeSession sí se ejecuta — sanity).
8. `cmux.listWorkspaces()` + `cmux.send(...)` — lines 151-158 — orchestrator notify (silent catch). **Opcional para tests**.
9. `handleOrchestratorStop()` — line 89 — solo se invoca cuando session no encontrada Y cwd es kodo repo. Branch lateral, no se ejerce en los tests SC#5.

**DI strategy (W-4):** el refactor inyecta:
- `deps.findSessionFn || findSession` — mandatory para los 4 tests SC#5
- `deps.removeSessionFn || removeSession` — mandatory para sanity assert del test no-GSD
- `deps.cmux || cmux` — sí inyectable: el flow no-GSD usa `cmux.setColor/notify` ANTES del `if (session.gsd)`, y el flow GSD también lo invoca. Si los tests no quieren el real cmux client (sí lo evitamos por aislamiento), el stub mock evita conexiones a `cmuxd`. Default: importar `cmux` desde `../cmux/client.js` como hoy.
- `deps.loggerFactory` — mandatory para que markSessionStatus + sessionEnd usen el memSink y los tests puedan capturar eventos.

**No es necesario inyectar:** `releaseGsdLock` (lazy import dentro del try local; los tests no asertan sobre lock release, y el catch local silencia errores), `createLogger` (consumido vía `loggerFactory` cuando se inyecta).

Estructura actual del block `if (session.gsd)` (líneas ~137-144):
```javascript
if (session.gsd) {
  try {
    const { releaseGsdLock } = await import('../gsd/lock.js');
    releaseGsdLock(session.project_path, session.session_id);
  } catch (err) {
    console.error(`[kodo:stop] Error releasing GSD lock: ${err.message}`);
  }
}

removeSession(id);
```

Estructura objetivo tras Task 1:
```javascript
if (session.gsd) {
  // <-- INSERCIÓN: markSessionStatus PRE-release con try/catch silencioso -->
  try {
    const { releaseGsdLock } = await import('../gsd/lock.js');
    releaseGsdLock(session.project_path, session.session_id);
  } catch (err) {
    console.error(`[kodo:stop] Error releasing GSD lock: ${err.message}`);
  }
}
removeSessionFn(id);
```

Patrón fake logger (mismo que Plan 02 — copy verbatim de gsd-verify-integration.test.js:73-83):
```javascript
function makeLogger() {
  const events = [];
  const logger = {
    info: (m, f) => events.push({ level: 'info', msg: m, fields: f }),
    warn: (m, f) => events.push({ level: 'warn', msg: m, fields: f }),
    error: (m, f) => events.push({ level: 'error', msg: m, fields: f }),
    debug: (m, f) => events.push({ level: 'debug', msg: m, fields: f }),
    child: () => logger,
  };
  return { logger, events };
}
```

Refactor light shape objetivo de `main()` (Discretion opción a, W-4 expanded):
```javascript
/**
 * Test-friendly entry point for the stop hook.
 * Pure function over (input, deps) — used by tests with memSink loggerFactory.
 *
 * @param {{session_id: string, cwd?: string, transcript_path?: string}} input
 * @param {{
 *   findSessionFn?: typeof findSession,
 *   removeSessionFn?: typeof removeSession,
 *   cmux?: typeof cmux,
 *   loggerFactory?: (binding: {session_id: string, task_id: string}) => any,
 * }} [deps]
 */
export async function runStopHook(input, deps = {}) {
  const findSessionFn = deps.findSessionFn || findSession;
  const removeSessionFn = deps.removeSessionFn || removeSession;
  const cmuxClient = deps.cmux || cmux;
  // loggerFactory: si null/undefined, los bloques try-block hacen lazy import del default.
  // ... resto del cuerpo actual de main() recibiendo deps
}

async function main() {
  const input = JSON.parse(await readStdin());
  await runStopHook(input);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insertar markSessionStatus PRE-release + refactor light a runStopHook (src/hooks/stop.js)</name>
  <files>src/hooks/stop.js</files>

  <read_first>
    - src/hooks/stop.js (estado actual completo — confirmar líneas 116-134 patrón session.end y líneas 137-147 rama if(session.gsd); los offsets pueden haber cambiado)
    - src/session/manager.js (firma y comportamiento de markSessionStatus, especialmente el lookup interno por task_id)
    - test/stop.test.js (estado actual — entender qué tests source-hygiene Phase 13 D-09/D-10 corren contra stop.js para asegurar que NO se rompen)
    - .planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md sección "3. `src/hooks/stop.js` — markSessionStatus PRE-release" (líneas 335-453)
    - .planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md decisions D-04, D-05, D-06, D-07, D-08, D-09
  </read_first>

  <behavior>
    - Test 1 (post-edit, runtime invariante): la rama `if (session.gsd)` mantiene el orden `markSessionStatus → releaseGsdLock → (fuera del if) removeSessionFn`. El behavior runtime para Claude Code stop hook NO se altera (excepto el evento extra emitido al NDJSON).
    - Test 2 (D-04 fixed `'done'`): el segundo argumento literal `'done'` está hardcoded — no se deriva de `session.gsd_mode`, no se infiere del transcript, no consulta verdict. Verificable con grep.
    - Test 3 (D-07 solo en GSD branch): `markSessionStatus` se invoca EXCLUSIVAMENTE dentro del `if (session.gsd) { ... }` block. Verificable con grep multilinea + lectura manual: el call NO existe fuera del if.
    - Test 4 (D-08 PRE-release): la línea `markSessionStatus(...)` aparece ANTES de `await import('../gsd/lock.js')` / `releaseGsdLock(...)`. Mirroring de session.end línea 116 que ya está antes de removeSession.
    - Test 5 (D-09 try/catch silencioso): el call está envuelto en `try { ... } catch {}` con el comentario explícito mencionando "mirrors session.end pattern line 116". Si markSessionStatus o el logger lanzan, el catch silencia y el flujo continúa al releaseGsdLock.
    - Test 6 (Phase 13 D-09/D-10 invariante): `test/stop.test.js` source-hygiene tests siguen verdes. La inserción NO accede a `session.gsd_mode` ni invoca `getSessionMode(session)` — solo a `session.task_id` (anti-inline check Phase 13 sigue cumpliéndose).
    - Test 7 (refactor light — runStopHook export con DI completo W-4): `runStopHook` se exporta. `main()` se reduce a wrapper de stdin parse + invocación. La lógica del cuerpo se preserva idéntica — el refactor extrae deps via parámetros opcionales: `findSessionFn`, `removeSessionFn`, `cmux`, `loggerFactory`.
    - Test 8 (Phase 11 D-08 invariante): el block `if (session.gsd)` sigue cubriendo ambos modos (full + quick). El refactor no introduce un switch sobre modo.
    - **Test 9 (W-2 sessionEnd preservado)**: el bloque `sessionEnd(log, {...})` (líneas ~116-134 actuales) viaja al cuerpo de `runStopHook` SIN reordenar. Sigue ANTES de `removeSessionFn(id)` y dentro de su propio try/catch silencioso.
  </behavior>

  <action>
    1. **Localizar las líneas relevantes** con grep:
       ```bash
       grep -n "if (session.gsd)" src/hooks/stop.js
       grep -n "releaseGsdLock" src/hooks/stop.js
       grep -n "removeSession(id)" src/hooks/stop.js
       grep -n "session.end\|sessionEnd" src/hooks/stop.js
       grep -n "async function main" src/hooks/stop.js
       grep -n "findSession\|cmux\." src/hooks/stop.js
       ```
       Esperado: 1 match para `if (session.gsd)` ~línea 138, 1-2 para `releaseGsdLock` (~140 dentro del block + 1 import lazy), 1 para `removeSession(id)` ~línea 146, 1 para sessionEnd pattern ~línea 126, 1 para `async function main` ~línea 71, varias para findSession + cmux.

    2. **Insertar el bloque markSessionStatus** dentro del `if (session.gsd)`, ANTES del try que importa `releaseGsdLock`:

       ```javascript
       // ANTES (líneas ~137-147 actuales):
       // Release GSD lock if applicable (D-09 Phase 8: idempotent, verifies session_id)
       if (session.gsd) {
         try {
           const { releaseGsdLock } = await import('../gsd/lock.js');
           releaseGsdLock(session.project_path, session.session_id);
         } catch (err) {
           console.error(`[kodo:stop] Error releasing GSD lock: ${err.message}`);
         }
       }

       removeSession(id);

       // DESPUÉS:
       // Release GSD lock if applicable (D-09 Phase 8: idempotent, verifies session_id)
       if (session.gsd) {
         // Phase 16 LOG-15 (D-04, D-08): mark session 'done' BEFORE releaseGsdLock so
         // the state.transition event captures the terminal status while the session
         // record still exists. Mirrors the session.end pattern at line 116 (emit
         // BEFORE mutation). Status is fixed 'done' for both modes (full + quick) —
         // stop.js is a mechanical hook and does NOT infer mode (D-04, D-07).
         // Reason canónico: 'session-stop:lock-released' (D-06).
         try {
           const log = (deps && deps.loggerFactory)
             ? deps.loggerFactory({ session_id: session.session_id, task_id: session.task_id })
             : await (async () => {
                 const { createLogger } = await import('../logger.js');
                 return createLogger({
                   sessionId: session.session_id,
                   minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
                 }).child({ component: 'hook', task_id: session.task_id });
               })();
           const { markSessionStatus } = await import('../session/manager.js');
           markSessionStatus(session.task_id, 'done', 'session-stop:lock-released', log);
         } catch {
           // silent — never block lock release on logger failure (mirrors session.end pattern line 116)
         }

         try {
           const { releaseGsdLock } = await import('../gsd/lock.js');
           releaseGsdLock(session.project_path, session.session_id);
         } catch (err) {
           console.error(`[kodo:stop] Error releasing GSD lock: ${err.message}`);
         }
       }

       removeSessionFn(id);
       ```

       **Notas**:
       - El `deps.loggerFactory` permite a los tests inyectar el memSink fake logger. En runtime productivo, `deps` no se pasa → `deps && deps.loggerFactory` es falsy → fallback al lazy import + child del default. Cero churn en producción.
       - El comentario explícito cita "mirrors the session.end pattern at line 116" — patrón D-09 textual.
       - El try/catch envuelve TANTO el createLogger COMO el markSessionStatus — un fallo en cualquiera de los dos cae al silent catch.

    3. **Refactor light de `main()` a `runStopHook(input, deps)` con DI completa (W-4 enumeration)**. Localizar el cuerpo actual de `main()` (~línea 71-165). Renombrar la función y añadir parámetros con todos los deps efectivos enumerados:

       ```javascript
       // ANTES (línea ~71):
       async function main() {
         try {
           const input = JSON.parse(await readStdin());
           const sessionId = input.session_id;
           const cwd = input.cwd || process.cwd();
           // ... resto del cuerpo
         } catch (err) {
           // ...
         }
       }

       // DESPUÉS (W-4: deps completos enumerados):
       /**
        * Test-friendly entry point for the stop hook.
        * Pure-ish function over (input, deps) — used by tests with memSink loggerFactory.
        * Production callers should use main() which parses stdin first.
        *
        * @param {{session_id: string, cwd?: string, transcript_path?: string}} input
        * @param {{
        *   findSessionFn?: typeof findSession,        // mandatory para tests (lookup en fixture)
        *   removeSessionFn?: typeof removeSession,    // mandatory para test no-GSD (sanity removeSession ejecuta)
        *   cmux?: typeof cmux,                        // mandatory para aislamiento — el flow no-GSD invoca cmux.setColor/notify ANTES del if (session.gsd); sin stub los tests intentarían conectar a cmuxd real
        *   loggerFactory?: (binding: {session_id: string, task_id: string}) => any,  // mandatory para captura de events.transition + sessionEnd
        * }} [deps]
        * @returns {Promise<void>}
        */
       export async function runStopHook(input, deps = {}) {
         // W-4: defaults vía OR — runtime productivo usa los imports estáticos
         const findSessionFn = deps.findSessionFn || findSession;
         const removeSessionFn = deps.removeSessionFn || removeSession;
         const cmuxClient = deps.cmux || cmux;
         try {
           const sessionId = input.session_id;
           const cwd = input.cwd || process.cwd();

           // ... resto del cuerpo, sustituyendo:
           //   findSession(...)        → findSessionFn(...)
           //   removeSession(...)      → removeSessionFn(...)
           //   cmux.setColor(...)      → cmuxClient.setColor(...)
           //   cmux.notify(...)        → cmuxClient.notify(...)
           //   cmux.listWorkspaces()   → cmuxClient.listWorkspaces()
           //   cmux.send(...)          → cmuxClient.send(...)

           // El bloque sessionEnd (líneas 116-134 actuales) usa loggerFactory si está
           // presente, igual que el bloque markSessionStatus de step 2:
           //   const log = (deps && deps.loggerFactory)
           //     ? deps.loggerFactory({ session_id: session.session_id, task_id: session.task_id })
           //     : await (async () => { const { createLogger } = await import('../logger.js'); ... })();
           //   const { sessionEnd } = await import('../logger-events.js');
           //   sessionEnd(log, { ... });

         } catch (err) {
           console.error(`[kodo] Stop hook error: ${err.message}`);
         }
       }

       async function main() {
         const input = JSON.parse(await readStdin());
         await runStopHook(input);
       }
       ```

       **W-4 enumeration justification (per dep):**
       - `findSessionFn`: **mandatory** — los 4 tests SC#5 inyectan stub que devuelve la session sintética del fixture. Sin esto los tests poblarían `~/.kodo/state.json` real (frágil).
       - `removeSessionFn`: **mandatory** — el test 3 (no-GSD) assert-a `removeSessionCalls === [session_id]` como sanity de que el flujo no se truncó.
       - `cmux`: **mandatory para aislamiento** — `main()` invoca `cmux.setColor` (line 100), `cmux.notify` (line 109), `cmux.listWorkspaces` (line 151), `cmux.send` (line 154) tanto en flow no-GSD como GSD. Sin stub los tests intentarían conexión a `cmuxd` real (timeout, falla CI). Los tests pasan `cmux: { setColor: async () => {}, notify: async () => {}, listWorkspaces: async () => '', send: async () => {} }`.
       - `loggerFactory`: **mandatory** — captura tanto `state.transition` (markSessionStatus) como `session.end` (sessionEnd). Single factory para ambos eventos en el memSink.

       **No inyectados (justificación):**
       - `releaseGsdLock`: lazy import dentro del try local; los tests no asertan sobre lock release. El catch local con `console.error` no bloquea el flujo. Si el lazy import falla en test (no debería — el módulo existe), el catch silencia.
       - `createLogger`: cuando `loggerFactory` se inyecta, el lazy import nunca se ejecuta. El default productivo sigue siendo el lazy import del módulo real.
       - `handleOrchestratorStop`: branch lateral que se ejerce solo cuando `findSession` devuelve null Y cwd es kodo repo. Los 4 tests SC#5 NO ejercen este path (todos pasan session válida vía findSessionFn). No requiere DI.

    4. **W-2 invariante — sessionEnd preservation**: durante el refactor, el bloque `sessionEnd(log, {...})` actual de líneas 116-134 viaja con el cuerpo a `runStopHook` SIN reordenar. Sigue ANTES de `removeSessionFn(id)`. Solo se modifica la creación del logger (usar `deps.loggerFactory` si presente) y la invocación de cmux (`cmuxClient` en lugar de `cmux`).

       Patrón "emit BEFORE mutation" (Phase 13 invariante): sessionEnd línea ~126 < removeSession línea ~146. **El refactor preserva ese orden literal**.

    5. **Preservar el resto del archivo intacto**:
       - Los `export function buildStopNudgeText(...)` que ya existen (línea ~41-56) — sin cambios.
       - El `readStdin()` helper, los typedefs JSDoc, los imports existentes.
       - El bloque `if (isMainEntry)` al final del archivo (~línea 207) — sigue invocando `main()` cuando se ejecuta como CLI.
       - El patrón `session.end` en líneas 116-134 — el cuerpo viaja al refactor pero NO se reordena ni elimina (W-2).
       - `handleOrchestratorStop` (líneas 167-202) — sin cambios; sigue siendo invocado desde el branch `findSessionFn` null + isOrchestratorSession.

    6. **NO TOCAR**:
       - `session.gsd_mode` access — Phase 13 D-09/D-10 anti-inline invariante. La inserción solo lee `session.task_id`, `session.session_id`, `session.project_path`, `session.gsd`, `session.status` (vía markSessionStatus internal lookup). NO toca `session.gsd_mode` (o cualquier mode-derivation).
       - Los `console.error` para errores de releaseGsdLock — patrón establecido en el archivo.
       - El comentario `// Release GSD lock if applicable (D-09 Phase 8: idempotent, verifies session_id)` — sigue siendo correcto.

    7. **Verificación post-edit (mental)**:
       - `grep -c "markSessionStatus" src/hooks/stop.js` debe dar **1** invocación runtime (más 1 en el dynamic import = total 2 ocurrencias del string).
       - `grep -c "'done'" src/hooks/stop.js` debe incluir el literal de `markSessionStatus(... 'done' ...)`.
       - `grep -c "session-stop:lock-released" src/hooks/stop.js` debe dar **1**.
       - `grep -c "export async function runStopHook" src/hooks/stop.js` debe dar **1**.
       - `grep -c "if (session.gsd)" src/hooks/stop.js` debe dar **1** (la rama no se duplicó).
       - `grep -c "session.gsd_mode" src/hooks/stop.js` debe dar **0** (Phase 13 invariante).
       - `grep -c "sessionEnd(log" src/hooks/stop.js` debe dar **1** (W-2 preservado).
  </action>

  <verify>
    <automated>node --test test/stop.test.js test/manager.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export async function runStopHook" src/hooks/stop.js` === 1
    - `grep -c "markSessionStatus(session.task_id, 'done', 'session-stop:lock-released'" src/hooks/stop.js` === 1
    - `grep -c "session-stop:lock-released" src/hooks/stop.js` === 1
    - `grep -c "silent — never block lock release on logger failure" src/hooks/stop.js` === 1
    - `grep -c "session.gsd_mode" src/hooks/stop.js` === 0 (Phase 13 D-09/D-10 invariante preservado)
    - `grep -c "if (session.gsd)" src/hooks/stop.js` === 1 (la rama no se duplicó)
    - **B-2 (a) Inspección sintáctica multilinea — confirma try/catch syntactic block alrededor de markSessionStatus**:
      ```bash
      grep -B1 -A2 "markSessionStatus(session.task_id, 'done'" src/hooks/stop.js | grep -cE "^\s*try \{|^\s*\} catch \{"
      ```
      debe dar `>= 2` (al menos un `try {` y un `} catch {` en la ventana de ±2 líneas alrededor de la invocación).
    - **B-2 (b) Inspección de placement — el bloque try/catch está dentro del body de `if (session.gsd)`**:
      ```bash
      awk '/if \(session\.gsd\)/,/^  \}/' src/hooks/stop.js | grep -c "markSessionStatus"
      ```
      debe dar `>= 1` (la invocación está dentro del if-block, no fuera).

      Alternativa de comprobación (assertion sobre indentación): la línea de `markSessionStatus(session.task_id, 'done',` tiene indentación >= 6 espacios (consistente con interior de un `if` nested en main()/runStopHook). Verificable con:
      ```bash
      grep -E "^      markSessionStatus\(session\.task_id, 'done'" src/hooks/stop.js | wc -l
      ```
      debe dar `>= 1`.
    - El `markSessionStatus(...)` aparece ANTES (en orden de líneas) de la línea con `releaseGsdLock(`. Verificable con:
      ```bash
      grep -n "markSessionStatus(session.task_id" src/hooks/stop.js
      grep -n "releaseGsdLock(session.project_path" src/hooks/stop.js
      ```
      el primer match debe tener número de línea menor que el segundo.
    - **W-2 (a) sessionEnd preservation — invocación intacta**:
      ```bash
      grep -c "sessionEnd(log" src/hooks/stop.js
      ```
      debe dar `=== 1` (preservado, no duplicado).
    - **W-2 (b) sessionEnd ANTES de removeSessionFn(id) — orden 'emit BEFORE mutation' Phase 13 preservado**:
      ```bash
      grep -n "sessionEnd(log" src/hooks/stop.js
      grep -n "removeSessionFn(id)\|removeSession(id)" src/hooks/stop.js
      ```
      el match de `sessionEnd` debe tener número de línea menor que el de `removeSessionFn(id)`. Verificación awk alternativa:
      ```bash
      awk '/sessionEnd\(log/{a=NR} /removeSessionFn?\(id\)/{b=NR} END {print (a<b)?"OK":"FAIL"}' src/hooks/stop.js
      ```
      debe imprimir `OK`.
    - **W-4 enumeration verifiable — los 4 deps están enumerados en la firma**:
      ```bash
      grep -E "deps\.findSessionFn|deps\.removeSessionFn|deps\.cmux|deps\.loggerFactory" src/hooks/stop.js | wc -l
      ```
      debe dar `>= 4` (los 4 deps efectivos referenciados).
    - `node --test test/stop.test.js` exit 0 — Phase 13 source-hygiene tests siguen verdes (no se introdujo `session.gsd_mode` access).
    - `node --test test/manager.test.js` exit 0 — markSessionStatus existing tests siguen verdes.
    - `node --test test/check-isolation.test.js` exit 0 — LOG-12 invariante (manager.js ya estaba en el grafo de stop.js indirectamente; el nuevo import no añade nodos prohibidos).
  </acceptance_criteria>

  <done>
    `src/hooks/stop.js` invoca `markSessionStatus(session.task_id, 'done', 'session-stop:lock-released', log)` PRE-release dentro del `if (session.gsd)` block, envuelto en try/catch silencioso (B-2: try/catch syntactic + placement DENTRO de if (session.gsd) verificado). `main()` refactor light a `runStopHook(input, deps)` exportable con DI completa (W-4: findSessionFn, removeSessionFn, cmux, loggerFactory enumerados). sessionEnd preservado ANTES de removeSession (W-2). Phase 11 D-08, Phase 13 D-09/D-10 invariantes preservados.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test/stop-state-transition.test.js con 4 escenarios SC#5 (full / quick / no-GSD + D-04 invariante mandatory)</name>
  <files>test/stop-state-transition.test.js</files>

  <read_first>
    - src/hooks/stop.js (post-edit Task 1 — confirmar export `runStopHook` y firma con findSessionFn/removeSessionFn/cmux/loggerFactory)
    - src/session/manager.js (estructura del payload state.transition emitido)
    - test/gsd-verify-integration.test.js (patrón makeLogger + child self-return — copy verbatim)
    - test/manager.test.js (estado actual — entender qué fixtures de session/state.json se usan en otros tests para reutilizar el setup)
    - .planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md sección "6. Coverage tests stop.js" (líneas 668-748)
    - .planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md decisions D-04, D-05, D-06, D-07 + sección Specifics
  </read_first>

  <behavior>
    - Test 1 (full mode — D-05): fixture state.json con `{gsd: true, gsd_mode: 'full', status: 'review', task_id: 't-1', session_id: 's-1', project_path: '/tmp/x'}`. Tras `runStopHook({session_id: 's-1'}, {findSessionFn, removeSessionFn, cmux: cmuxStub, loggerFactory})`, asertar:
      - `events.find(e => e.fields?.event === 'state.transition')` truthy.
      - `transition.fields.from === 'review'` (D-05: full ya pasó por verify, que dejó la sesión en review).
      - `transition.fields.to === 'done'` (D-04 fixed).
      - `transition.fields.reason === 'session-stop:lock-released'` (D-06).
    - Test 2 (quick mode): fixture state.json con `{gsd: true, gsd_mode: 'quick', status: 'running', task_id: 't-2', session_id: 's-2', project_path: '/tmp/y'}`. Asertar:
      - `transition.fields.from === 'running'` (quick no pasa por verify).
      - `transition.fields.to === 'done'`.
      - `transition.fields.reason === 'session-stop:lock-released'`.
    - Test 3 (non-GSD): fixture state.json con `{gsd: false, status: 'running', task_id: 't-3', session_id: 's-3', project_path: '/tmp/z'}`. Tras `runStopHook(...)`, asertar:
      - `events.find(e => e.fields?.event === 'state.transition') === undefined`.
      - Sanity: `removeSessionFn` se ejecutó (el spy registra la llamada con `id === 's-3'`). Confirma que el flujo no se truncó — solo no se emitió state.transition.
    - **Test 4 (D-04 invariante — N-2 MANDATORY)**: test parametrizado o explícito sobre los 2 modos (full + quick) que afirma `to === 'done'` SIEMPRE. **N-2 enforce: este test es MANDATORY**, no opcional. D-04 es decisión LOCKED en CONTEXT.md y el test cierra el drift futuro: si un implementer infiere modo y emite `'review'` para quick (violando D-04), este test cae con assertion message específico citando D-04.
  </behavior>

  <action>
    1. **Crear `test/stop-state-transition.test.js`** con la estructura siguiente. Setup mínimo (state.json sintético en tmpdir):

       ```javascript
       import { describe, it, beforeEach, afterEach } from 'node:test';
       import assert from 'node:assert/strict';
       import { mkdtempSync, rmSync, writeFileSync, mkdirSync, existsSync } from 'node:fs';
       import { tmpdir } from 'node:os';
       import { join } from 'node:path';

       /**
        * Fake logger memSink — same pattern as test/gsd-verify-integration.test.js:73-83.
        * child() returns the same logger so events array survives .child(...) calls.
        */
       function makeLogger() {
         const events = [];
         const logger = {
           info: (m, f) => events.push({ level: 'info', msg: m, fields: f }),
           warn: (m, f) => events.push({ level: 'warn', msg: m, fields: f }),
           error: (m, f) => events.push({ level: 'error', msg: m, fields: f }),
           debug: (m, f) => events.push({ level: 'debug', msg: m, fields: f }),
           child: () => logger,
         };
         return { logger, events };
       }

       /**
        * Cmux stub — evita conexión a cmuxd real durante tests (W-4).
        * El flow de stop.js invoca setColor + notify + listWorkspaces + send tanto en
        * GSD como no-GSD. Sin stub los tests intentarían conectar al daemon real.
        */
       function makeCmuxStub() {
         const calls = [];
         return {
           stub: {
             setColor: async (args) => { calls.push({ method: 'setColor', args }); },
             notify: async (args) => { calls.push({ method: 'notify', args }); },
             listWorkspaces: async () => { calls.push({ method: 'listWorkspaces' }); return ''; },
             send: async (args) => { calls.push({ method: 'send', args }); },
           },
           calls,
         };
       }

       describe('SC#5 LOG-15: stop hook state.transition coverage', () => {

         it('full mode: session.status="review" + lock release → emits state.transition from=review to=done (D-05)', async () => {
           const session = {
             session_id: 's-full-1',
             task_id: 't-full-1',
             task_ref: 'KL-full-1',
             gsd: true,
             gsd_mode: 'full',
             status: 'review',
             project_path: '/tmp/repo-full',
             provider: 'plane',
             workspace_ref: 'workspace:1',
             summary: 'test session full',
           };
           const { logger, events } = makeLogger();
           const { stub: cmuxStub } = makeCmuxStub();
           const findSessionFn = ({ sessionId }) =>
             sessionId === session.session_id ? { id: session.session_id, session } : null;
           const removeSessionCalls = [];
           const removeSessionFn = (id) => removeSessionCalls.push(id);

           const { runStopHook } = await import('../src/hooks/stop.js');
           await runStopHook(
             { session_id: session.session_id, cwd: '/tmp/repo-full' },
             {
               findSessionFn,
               removeSessionFn,
               cmux: cmuxStub,
               loggerFactory: () => logger,
             },
           );

           const transition = events.find((e) => e.fields?.event === 'state.transition');
           assert.ok(transition, 'full mode debe emitir state.transition');
           assert.equal(transition.fields.from, 'review', 'D-05: from debe ser el status previo (review post-verify)');
           assert.equal(transition.fields.to, 'done', 'D-04: to fixed a "done"');
           assert.equal(transition.fields.reason, 'session-stop:lock-released', 'D-06: reason canónico');
           assert.deepEqual(removeSessionCalls, [session.session_id], 'removeSession se ejecutó (sanity)');
         });

         it('quick mode: session.status="running" + lock release → emits state.transition from=running to=done', async () => {
           const session = {
             session_id: 's-quick-1',
             task_id: 't-quick-1',
             task_ref: 'KL-quick-1',
             gsd: true,
             gsd_mode: 'quick',
             status: 'running',
             project_path: '/tmp/repo-quick',
             provider: 'plane',
             workspace_ref: 'workspace:2',
             summary: 'test session quick',
           };
           const { logger, events } = makeLogger();
           const { stub: cmuxStub } = makeCmuxStub();
           const findSessionFn = ({ sessionId }) =>
             sessionId === session.session_id ? { id: session.session_id, session } : null;
           const removeSessionCalls = [];
           const removeSessionFn = (id) => removeSessionCalls.push(id);

           const { runStopHook } = await import('../src/hooks/stop.js');
           await runStopHook(
             { session_id: session.session_id, cwd: '/tmp/repo-quick' },
             { findSessionFn, removeSessionFn, cmux: cmuxStub, loggerFactory: () => logger },
           );

           const transition = events.find((e) => e.fields?.event === 'state.transition');
           assert.ok(transition, 'quick mode debe emitir state.transition');
           assert.equal(transition.fields.from, 'running', 'from debe ser el status previo (running — quick no pasa por verify)');
           assert.equal(transition.fields.to, 'done', 'D-04: to fixed a "done" (mismo para ambos modos)');
           assert.equal(transition.fields.reason, 'session-stop:lock-released', 'D-06: reason canónico');
         });

         it('non-GSD: session.gsd=false → does NOT emit state.transition (D-07)', async () => {
           const session = {
             session_id: 's-nogsd-1',
             task_id: 't-nogsd-1',
             task_ref: 'KL-nogsd-1',
             gsd: false,
             status: 'running',
             project_path: '/tmp/repo-nogsd',
             provider: 'plane',
             workspace_ref: 'workspace:3',
             summary: 'test session no-gsd',
           };
           const { logger, events } = makeLogger();
           const { stub: cmuxStub } = makeCmuxStub();
           const findSessionFn = ({ sessionId }) =>
             sessionId === session.session_id ? { id: session.session_id, session } : null;
           const removeSessionCalls = [];
           const removeSessionFn = (id) => removeSessionCalls.push(id);

           const { runStopHook } = await import('../src/hooks/stop.js');
           await runStopHook(
             { session_id: session.session_id, cwd: '/tmp/repo-nogsd' },
             { findSessionFn, removeSessionFn, cmux: cmuxStub, loggerFactory: () => logger },
           );

           const transition = events.find((e) => e.fields?.event === 'state.transition');
           assert.equal(transition, undefined, 'D-07: non-GSD no debe emitir state.transition (solo dentro de if (session.gsd))');
           assert.deepEqual(removeSessionCalls, [session.session_id], 'removeSession sí se ejecuta (la sesión se limpia normal — solo el state.transition no aparece)');
         });

         // N-2 MANDATORY: D-04 es LOCKED. Este test cierra drift futuro — si un
         // implementer infiere modo y emite 'review' para quick (violando D-04),
         // este test cae con assertion message específico citando D-04.
         it('D-04 invariante MANDATORY: ambos modos full y quick emiten to="done" (no se infiere modo)', async () => {
           const fullSession = {
             session_id: 's-d04-full', task_id: 't-d04-full', task_ref: 'KL-d04-full',
             gsd: true, gsd_mode: 'full', status: 'review', project_path: '/tmp/d04-full',
             provider: 'plane', workspace_ref: 'workspace:4', summary: 'd04 full',
           };
           const quickSession = {
             session_id: 's-d04-quick', task_id: 't-d04-quick', task_ref: 'KL-d04-quick',
             gsd: true, gsd_mode: 'quick', status: 'running', project_path: '/tmp/d04-quick',
             provider: 'plane', workspace_ref: 'workspace:5', summary: 'd04 quick',
           };

           const { runStopHook } = await import('../src/hooks/stop.js');

           for (const session of [fullSession, quickSession]) {
             const { logger, events } = makeLogger();
             const { stub: cmuxStub } = makeCmuxStub();
             const findSessionFn = ({ sessionId }) =>
               sessionId === session.session_id ? { id: session.session_id, session } : null;
             const removeSessionFn = () => {};
             await runStopHook(
               { session_id: session.session_id, cwd: session.project_path },
               { findSessionFn, removeSessionFn, cmux: cmuxStub, loggerFactory: () => logger },
             );
             const transition = events.find((e) => e.fields?.event === 'state.transition');
             assert.ok(transition, `D-04 invariante: modo ${session.gsd_mode} debe emitir state.transition`);
             assert.equal(transition.fields.to, 'done',
               `D-04 LOCKED: to debe ser 'done' fijo (modo ${session.gsd_mode}) — D-04 prohíbe inferir modo`);
           }
         });
       });
       ```

    2. **Verificar que `runStopHook` acepta `loggerFactory`** correctamente — el stub debe devolver el logger (no requiere crear un child más, porque el helper interno ya hace `.child(...)`). Si `markSessionStatus` invoca `logger.child({component:'session', task_id})`, el test memSink hace `child: () => logger` (self-return) y los eventos siguen capturándose.

    3. **W-4 garantiza la inyección completa**: por Task 1 step 3 los 4 deps (`findSessionFn`, `removeSessionFn`, `cmux`, `loggerFactory`) están enumerados en la firma de `runStopHook`. Los tests pueden inyectar todos sin fallback a env vars o KODO_DIR real. **Eliminada la cláusula de fallback** ("Si findSessionFn / removeSessionFn no se inyectan en el refactor de Task 1...") — Task 1 garantiza la inyección.

    4. **N-2 MANDATORY — Test 4 D-04 invariante NO es opcional**. D-04 es decisión LOCKED en CONTEXT.md. Permitir omitir el test introduce drift futuro: un implementer podría inferir modo en una refactorización posterior y la única garantía contra esa regresión sería el assert explícito de Test 4 con assertion message citando D-04. **Eliminada la cláusula** "Si parece redundante con tests 1+2, OMITIRLO".

       Justificación de no-redundancia: Tests 1 y 2 asertan `to='done'` cada uno aisladamente, pero Test 4 lo afirma DENTRO de un loop sobre los 2 modos con un único punto de fallo y assertion message específico ("D-04 LOCKED: ...prohíbe inferir modo"). Si Test 4 cae, el mensaje identifica el problema con precisión D-04, sin scroll. Tests 1+2 caerían pero el operador tendría que correlacionar dos fallos para inferir el patrón D-04.

    5. **NO TOCAR** `test/stop.test.js` existente. Los source-hygiene + buildStopNudgeText tests siguen ahí, sin modificación. Phase 16 introduce un comportamiento diferente (state.transition emit) que merece su propio archivo (separación de concerns — pattern-mapper sección 6.0).
  </action>

  <verify>
    <automated>node --test test/stop-state-transition.test.js test/stop.test.js test/manager.test.js</automated>
  </verify>

  <acceptance_criteria>
    - El archivo `test/stop-state-transition.test.js` existe.
    - `grep -c "runStopHook" test/stop-state-transition.test.js` >= 5 (1 import dinámico * 4 tests + invocaciones por test = ≥5)
    - `grep -c "fields?.event === 'state.transition'" test/stop-state-transition.test.js` >= 4 (full, quick, no-GSD, D-04 invariante MANDATORY)
    - `grep -c "to: 'done'" test/stop-state-transition.test.js` >= 1 O `grep -c "to, 'done'" test/stop-state-transition.test.js` >= 1 (assert sobre el campo fixed)
    - `grep -c "session-stop:lock-released" test/stop-state-transition.test.js` >= 2 (asserts en full + quick)
    - `grep -c "from, 'review'" test/stop-state-transition.test.js` >= 1 (full mode — D-05)
    - `grep -c "from, 'running'" test/stop-state-transition.test.js` >= 1 (quick mode)
    - `grep -c "transition, undefined" test/stop-state-transition.test.js` >= 1 (no-GSD assert)
    - **N-2 enforce — Test 4 D-04 invariante MANDATORY presente**:
      ```bash
      grep -c "D-04" test/stop-state-transition.test.js
      ```
      debe dar `>= 2` (al menos 1 mención en test name + 1 en assertion message del Test 4 mandatory).
    - El test contiene EXACTAMENTE 4 `it(...)` blocks dentro del describe (full, quick, no-GSD, D-04 invariante). N-2: el 4to NO es opcional.
    - **W-4 verify — los 4 deps se inyectan en cada test**:
      ```bash
      grep -cE "findSessionFn|removeSessionFn|cmux:|loggerFactory" test/stop-state-transition.test.js
      ```
      debe dar `>= 12` (4 deps × 3 tests con DI completo + Test 4 reusa los mismos = 12+ ocurrencias).
    - `node --test test/stop-state-transition.test.js` exit 0.
    - `node --test test/stop.test.js` exit 0 — tests Phase 13 source-hygiene siguen verdes.
    - `node --test test/manager.test.js` exit 0 — markSessionStatus tests siguen verdes.
  </acceptance_criteria>

  <done>
    `test/stop-state-transition.test.js` cubre 4 escenarios SC#5 (full review→done D-05, quick running→done, no-GSD no-emit D-07, D-04 invariante MANDATORY) con memSink fake logger y DI completo (findSessionFn, removeSessionFn, cmux stub, loggerFactory — W-4). Test 4 D-04 invariante mandatory por N-2. Tests existentes siguen verdes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code stop hook → stop.js | input via stdin (untrusted shape — JSON.parse puede lanzar). El hook actual ya envuelve en try/catch top-level. |
| stop.js → manager.js (markSessionStatus) | invocación interna que muta state.json. La mutación es la PRIMARY action; emitir state.transition es side-effect. |
| stop.js → gsd/lock.js (releaseGsdLock) | mutación filesystem (release del lock per-repo). Idempotente (Phase 8). |
| stop.js → cmux client | network call al daemon cmux. Catches locales silencian timeouts. Tests inyectan stub. |
| stop.js → logger NDJSON | append-only write con redacción. Fallo del logger NO debe bloquear el lock release (D-09 explícito). |
| runStopHook (export) → tests | nueva surface pública añadida por el refactor light. Solo accesible vía import desde test files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | D (Denial of Service) | logger failure blocking lock release | mitigate | D-09: try/catch silencioso envuelve `markSessionStatus + createLogger`. Si cualquiera lanza, el catch silencia y el control flow continúa al `releaseGsdLock` (siguiente try). El comentario `silent — never block lock release on logger failure (mirrors session.end pattern line 116)` enforce-able por test source-hygiene futuro si hace falta. B-2: try/catch syntactic + placement DENTRO de if (session.gsd) verificado por acceptance criteria. |
| T-16-13 | T (Tampering) | state.transition con `to` incorrecto | mitigate | D-04 fixed `'done'` literal — no se deriva de session.gsd_mode. Test 4 SC#5 (N-2 MANDATORY) asserts ambos modos (full + quick) emiten `to='done'`. Si alguien refactoriza inferiendo modo y emitiendo `'review'` para quick, el test cae con mensaje específico citando D-04 LOCKED. |
| T-16-14 | E (Elevation of privilege) | markSessionStatus invocado en rama no-GSD | mitigate | D-07: la invocación está EXCLUSIVAMENTE dentro de `if (session.gsd)`. Test 3 SC#5 (non-GSD) asserts `state.transition === undefined`. Si alguien mueve la línea fuera del if, el test cae. B-2 acceptance criteria asegura placement dentro del if-block via awk. |
| T-16-15 | R (Repudiation) | Phase 13 D-09/D-10 anti-inline regression | mitigate | El acceptance criteria explícita (`grep -c "session.gsd_mode" src/hooks/stop.js === 0`) bloquea regresión. Phase 16 inserción solo lee `session.task_id`/`session_id`/`project_path`/`gsd` — NO toca `gsd_mode` ni `getSessionMode(session)`. `test/stop.test.js` source-hygiene tests siguen verdes. |
| T-16-16 | T (Tampering) | LOG-12 guard regression | mitigate | Lazy dynamic import de `manager.js` (igual patrón que session.end → logger-events.js). `manager.js` no entra al grafo de `check.js`. `test/check-isolation.test.js` sigue verde. |
| T-16-17 | I (Information disclosure) | runStopHook export expone interno | accept | El export es para tests unitarios (mismo patrón que `runGsdVerify` en verify.js). No expone secrets ni privilegio adicional al runtime productivo (que sigue invocando `main()`). El refactor preserva la lógica idéntica — sin nueva superficie de información. W-4: los 4 deps enumerados son funciones internas ya importadas, no leak adicional. |
| T-16-18 | D (Denial of Service) | hook stop crash on unexpected input | accept | El control flow superior (`async function main`) ya tenía try/catch top-level que silencia errores y exit 0. El refactor preserva esa garantía: `main()` parsea stdin, si falla cae al catch top-level. `runStopHook(input, deps)` asume que input ya está parseado. |
| T-16-19 | T (Tampering) | sessionEnd event drop tras refactor (W-2) | mitigate | Acceptance criteria W-2 (a) verifica `grep -c "sessionEnd(log" === 1` (preservado, no duplicado). Acceptance criteria W-2 (b) verifica orden literal sessionEnd ANTES de removeSessionFn(id) (patrón "emit BEFORE mutation" Phase 13). |

**Notas**: T-16-12 y T-16-13 son las amenazas funcionales más críticas — ambas mitigadas por tests directos en Task 2 + acceptance criteria B-2 en Task 1. T-16-15 y T-16-19 son las amenazas de regresión cross-phase (Phase 13 D-09/D-10 + Phase 13 'emit BEFORE mutation') — mitigadas por acceptance criteria explícita.
</threat_model>

<verification>
- `node --test test/stop-state-transition.test.js test/stop.test.js test/manager.test.js test/check-isolation.test.js` exit 0.
- `node --test` global pasa sin nuevos fallos respecto a la baseline Phase 15 (414+ tests).
- Manual sanity: `kodo` ejecutado en una sesión gsd:true full que llega a stop debe producir un `state.transition` en el NDJSON: `grep "state.transition" ~/.kodo/logs/<session-id>.log` con `from=review to=done reason=session-stop:lock-released`.
- Manual sanity: `kodo` ejecutado en una sesión no-GSD (gsd:false) NO produce `state.transition` en el NDJSON.
- Manual sanity: el lock per-repo en `~/.kodo/locks/` se libera (lockfile eliminado) — el behavior de release no se rompió por la inserción.
- Manual sanity: el evento `session.end` sigue apareciendo en el NDJSON antes que cualquier `state.transition` (W-2 invariante).
</verification>

<success_criteria>
LOG-15 cubierto. SC#4 ROADMAP §Phase 16: `markSessionStatus` invocado en `stop.js#if (session.gsd)` PRE-release; `state.transition` emitido al estado terminal `'done'`; orden documentado y enforced (B-2 try/catch syntactic + placement). SC#5 ROADMAP §Phase 16: cadena quick + full + no-GSD verificada — full review→done (D-05), quick running→done, no-GSD sin state.transition (D-07), D-04 invariante MANDATORY (N-2); los 414+ tests siguen verdes; nuevos tests cubren los 4 escenarios con DI completo (W-4). sessionEnd preservado (W-2).
</success_criteria>

<output>
Tras completar, crear `.planning/phases/16-log-09-debt-cleanup/16-03-SUMMARY.md` con: archivos modificados, líneas tocadas, refactor light runStopHook documentado con DI completa (W-4: findSessionFn, removeSessionFn, cmux, loggerFactory), orden PRE-release (markSessionStatus línea N < releaseGsdLock línea M; B-2 try/catch syntactic + placement), 4 escenarios SC#5 verdes (incluyendo D-04 invariante MANDATORY por N-2), sessionEnd preservado (W-2: invocación intacta + orden BEFORE removeSession), Phase 13 D-09/D-10 invariante preservado (grep `session.gsd_mode` === 0), LOG-12 sigue verde.
</output>
</output>
