---
phase: 16
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gsd/verify.js
  - test/gsd-verify-integration.test.js
autonomous: true
requirements: [LOG-14]
user_setup: []

must_haves:
  truths:
    - "`src/gsd/verify.js#finalize` invoca `markSessionStatus(session.task_id, 'review', 'gate-passed', log)` SOLO cuando `verdict.action === 'pass'` Y `addComment` OK Y `updateTaskState` OK (D-10, D-11)."
    - "El `markSessionStatus` se inserta DENTRO del try-OK path de `updateTaskState` (después de `transitioned = true`, antes del `} catch (err)`), de modo que un fallo de `updateTaskState` cae al catch ANTES de invocar `markSessionStatus` — no se emite `state.transition` en ese path (D-11, SC#3)."
    - "Las ramas `verdict.action === 'fail'` (3 sub-variants: soft `gaps-found`, soft `must-haves-incomplete`, hard `status-failed`), `'missing'`, `'malformed'` y errores intermedios de Plane (`getTask` fail, `addComment` fail, `updateTaskState` fail) NO invocan `markSessionStatus` y NO emiten `state.transition` — un test de integración verifica las 6+ ramas con `events.find(e => e.fields?.event === 'state.transition') === undefined` (D-11, SC#3). **B-1: NO colapsar las 3 sub-variants de fail en un único test parametrizado** — se exigen al menos 2 sub-variants explícitas (1 soft + 1 hard) para preservar la distinción soft-fail / hard-fail que ROADMAP §Phase 16 SC#3 demanda literalmente."
    - "`verify.js` añade `import { markSessionStatus } from '../session/manager.js'` (eager, sibling import — verify.js es CLI-invoked, no hook con startup-budget; el patrón eager está alineado con los otros imports de verify.js)."
    - "El logger threading existente se reusa: `markSessionStatus` recibe el `log` ya child-bound (component='session', task_id) que vive en el scope de `finalize` — no se crea un logger nuevo (D-Discretion CONTEXT línea 145)."
    - "El `reason` `'gate-passed'` espeja exactamente el verdict legacy mapping documentado en la cabecera de `verify.js` línea 26 — consistencia textual = trazabilidad cross-event (orchestrator.review reason 'gate-passed' Y state.transition reason 'gate-passed' en el mismo branch)."
    - "El campo `from` del `state.transition` proviene del `session.status` previo (lectura interna de `markSessionStatus` en `manager.js:300`); el campo `to` es `'review'` literal. El test de integración assert-a ambos campos canónicos."
    - "Phase 10 D-11/D-12 preservados: `updateTaskState` SOLO en pass + addComment OK; orden enforced (pass → addComment → updateTaskState → markSessionStatus → orchestratorReview). Phase 16 D-11 inserta `markSessionStatus` SIN reordenar la cadena."
  artifacts:
    - path: "src/gsd/verify.js"
      provides: "markSessionStatus invocado en pass branch tras updateTaskState OK + import desde manager.js"
      contains: "markSessionStatus(session.task_id, 'review', 'gate-passed'"
      min_lines: 405
    - path: "test/gsd-verify-integration.test.js"
      provides: "Asserts SC#2 positive (pass branch emite state.transition with from/to/reason canónicos) + SC#3 negative en 4 verdict (incluyendo soft + hard fail explícitos) + 2 ramas error Plane (B-1: 6+ negativos)"
      contains: "events.find((e) => e.fields?.event === 'state.transition')"
  key_links:
    - from: "src/gsd/verify.js#finalize pass branch"
      to: "src/session/manager.js#markSessionStatus"
      via: "import + invocación dentro del try-OK de updateTaskState"
      pattern: "markSessionStatus\\(session\\.task_id,\\s*'review',\\s*'gate-passed',\\s*log\\)"
    - from: "src/session/manager.js#markSessionStatus"
      to: "stateTransition emit via logger"
      via: "logger.child({component: 'session', task_id}).info(EVENTS.STATE_TRANSITION, {...})"
      pattern: "stateTransition"
    - from: "test/gsd-verify-integration.test.js"
      to: "memSink fake logger"
      via: "events array captura logger.info calls; child() devuelve same logger (self-return)"
      pattern: "child:\\s*\\(\\)\\s*=>\\s*logger"
    - from: "test/gsd-verify-integration.test.js#SC#3 negative tests"
      to: "events.filter(e => e.fields?.event === 'state.transition')"
      via: "filter discriminator estable por payload key (NO por msg)"
      pattern: "fields\\?\\.event === 'state.transition'"
---

<objective>
Cerrar LOG-14: cablear `markSessionStatus(session.task_id, 'review', 'gate-passed', log)` en `src/gsd/verify.js#finalize` exactamente en el punto donde la cadena `pass + addComment OK + updateTaskState OK` ya está enforced (Phase 10 D-11/D-12). El helper `markSessionStatus` existe en `src/session/manager.js:299` y emite `state.transition` con `from`/`to` reales cuando recibe un logger.

**Estrategia (D-10, D-11):** inserción de UNA línea entre `transitioned = true` y el `} catch (err)` que cierra el try de `updateTaskState`. La invocación va DENTRO del try-OK path — si `updateTaskState` lanza, el throw cae al catch ANTES de invocar `markSessionStatus`, garantizando que `state.transition` NO se emite en el path de fallo (D-11, SC#3 documenta este orden exacto).

**Cobertura de tests SC#3 (B-1: ≥6 negative + 1 positive = ≥7 asserts state.transition):**
- Pass + Plane OK → emite `state.transition` (positive, SC#2).
- Fail soft `gaps-found` → NO emite (sub-variant explícita — soft-fail).
- Fail hard `status-failed` → NO emite (sub-variant explícita — hard-fail; **B-1 mandatory: distingue soft de hard**).
- (Opcional adicional: Fail soft `must-haves-incomplete` — si parametrizar es trivial, incluir; si no, los 2 sub-variants soft+hard explícitos bastan para SC#3 literal).
- Missing (VERIFICATION.md no encontrado) → NO emite.
- Malformed (frontmatter inválido) → NO emite.
- Pass + getTask fail (Plane unreachable) → NO emite (no entra al `if (task)` block).
- Pass + addComment fail → NO emite (`commented = false` impide entrar al if del updateTaskState).
- Pass + updateTaskState fail → NO emite (markSessionStatus está DENTRO del try; el throw aborta antes).

**B-1 enforcement:** se prohíbe colapsar `gaps-found`/`must-haves-incomplete`/`status-failed` en un único test parametrizado con 3 fixtures que pierda la distinción soft-fail vs hard-fail. Se exigen al menos 2 tests separados de fail (uno soft, uno hard) o un test parametrizado que mantenga las dos sub-variants como casos diferenciados con assertion message distintivo. ROADMAP §Phase 16 SC#3 demanda esa distinción literalmente.

Purpose: cubrir SC#2 + SC#3 ROADMAP §Phase 16 (state.transition emit en runtime + regression test que enforce el orden de la cadena).
Output: `src/gsd/verify.js` (1 import + 1 línea + 1 comentario explicativo) y `test/gsd-verify-integration.test.js` (1 assert positive + 6 asserts negative: 2 fail sub-variants + missing + malformed + getTask-fail + updateTaskState-fail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md
@.planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md

# Source of truth — read BEFORE editing
@src/gsd/verify.js
@src/session/manager.js
@src/logger-events.js
@test/gsd-verify-integration.test.js

<interfaces>
<!-- API que este plan consume desde src/session/manager.js + src/logger-events.js (read-only). -->

From src/session/manager.js (line 299, named export):
```javascript
/**
 * Mark session status — atomic state transition with optional state.transition event emission.
 * Looks up session by task_id (exact match on s.task_id || s.task_ref).
 * Emits 'state.transition' via logger with {from, to, reason} when logger is provided.
 *
 * @param {string} taskId  — session.task_id (UUID estable). Convención del repo: pasar task_id, no task_ref.
 * @param {'running'|'done'|'error'|'review'|'interrupted'} nextStatus
 * @param {string} reason  — informativo, sin formato fijo (e.g. 'gate-passed', 'session-stop:lock-released')
 * @param {Logger} logger  — child logger preferentemente; el helper hace .child({component:'session', task_id}) internamente
 * @returns {void} (return value ignored upstream — solo side-effect)
 */
export function markSessionStatus(taskId, nextStatus, reason, logger): void
```

Comportamiento clave:
- Si logger se pasa, emite `logger.info(EVENTS.STATE_TRANSITION, {event: 'state.transition', from: <prev>, to: <next>, reason})` vía helper `stateTransition()`.
- Si logger NO se pasa, mutación silenciosa de state.json — NO emite evento.
- `from` se lee del session record actual ANTES de mutar; `to` es el `nextStatus` literal.

From src/gsd/verify.js (estado actual relevante — líneas 218-246 dentro de finalize):
```javascript
if (task) {
  try {
    await provider.addComment(task, markdown);
    commented = true;
  } catch (err) {
    planeApiCallFailed(log, { step: 'addComment', error: err.message });
  }

  if (verdict.action === 'pass' && commented) {
    const config = loadConfigFn();
    const providerName = session.provider || config.provider;
    const providerCfg = (config.providers && config.providers[providerName]) || {};
    const reviewState = (providerCfg.states && providerCfg.states.review) || 'In review';
    try {
      await provider.updateTaskState(task, reviewState);
      transitioned = true;
      // <-- INSERCIÓN AQUÍ -->
    } catch (err) {
      planeApiCallFailed(log, { step: 'updateTaskState', error: err.message });
    }
  }
}
```

`session.task_id` está en scope (lo persiste `findSessionFn`); `log` también en scope (es el child logger creado al entrar a `runGsdVerify`/`finalize`).

Patrón fake logger / event spy (analog: `test/gsd-verify-integration.test.js:73-83`):
```javascript
function makeLogger() {
  const events = [];
  const logger = {
    info: (m, f) => events.push({ level: 'info', msg: m, fields: f }),
    warn: (m, f) => events.push({ level: 'warn', msg: m, fields: f }),
    error: (m, f) => events.push({ level: 'error', msg: m, fields: f }),
    debug: (m, f) => events.push({ level: 'debug', msg: m, fields: f }),
    child: () => logger,  // self-return preserva events array a través de .child(...)
  };
  return { logger, events };
}
```

Filter discriminator (Pattern D — pattern-mapper sección Shared Patterns):
```javascript
const transition = events.find((e) => e.fields?.event === 'state.transition');
// NO filtrar por e.msg — el msg puede mutar tras refactor del logger.
// El payload `event:` key es contractual (Phase 7 D-10) y stable.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insertar markSessionStatus en pass branch de src/gsd/verify.js</name>
  <files>src/gsd/verify.js</files>

  <read_first>
    - src/gsd/verify.js (estado actual completo — confirmar que líneas 218-246 mantienen el shape descrito en interfaces; los offsets pueden haber cambiado)
    - src/session/manager.js (firma + comportamiento de `markSessionStatus`, especialmente que recibe `taskId` como string y emite `state.transition` solo con logger)
    - src/logger-events.js (constante `EVENTS.STATE_TRANSITION = 'state.transition'` y helper `stateTransition`)
    - .planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md sección "2. `src/gsd/verify.js` — markSessionStatus integration" (líneas 211-332)
    - .planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md decisions D-10, D-11
  </read_first>

  <behavior>
    - Test 1 (post-edit, runtime invariante): la cadena `pass + addComment OK + updateTaskState OK` sigue ejecutándose en el mismo orden — ningún side-effect existente cambia. `transitioned = true` precede a `markSessionStatus`. `orchestratorReview` se invoca DESPUÉS del bloque if, NO se altera.
    - Test 2 (markSessionStatus invocado solo en happy path): grep contra `src/gsd/verify.js` confirma exactamente UN `markSessionStatus(...)` runtime, dentro del bloque `if (verdict.action === 'pass' && commented)` y dentro del try de `updateTaskState`. Si grep encuentra >1 invocación, hay regresión.
    - Test 3 (taskId correcto): `session.task_id` (NO `session.task_ref`) se pasa como primer argumento. `manager.js:300` acepta ambos pero la convención del repo es task_id (UUID estable).
    - Test 4 (logger threading): el `log` que se pasa a `markSessionStatus` es el mismo que ya está en scope de `finalize` (no se crea un nuevo logger, no se invoca `createLogger` de nuevo).
    - Test 5 (verdict legacy mapping intacto): el `reason` `'gate-passed'` espeja exactamente la línea 26 de la cabecera de verify.js (`pass + side-effects OK → 'approved' (reason: 'gate-passed')`).
    - Test 6 (orden literal): la línea `markSessionStatus(...)` aparece DESPUÉS de `transitioned = true` y ANTES de `} catch (err)`. Esto es lo que garantiza SC#3 (un fallo de `updateTaskState` aborta antes de invocar markSessionStatus).
  </behavior>

  <action>
    1. **Localizar el bloque `if (verdict.action === 'pass' && commented)`** con grep:
       ```bash
       grep -n "if (verdict.action === 'pass' && commented)" src/gsd/verify.js
       ```
       Esperado: 1 match cerca de la línea 230. Si hay 0 o >1, **DETENERSE y leer** la sección entera — algo cambió.

    2. **Añadir el import eager** al top del archivo. Insertar tras la última línea de imports `from '..'` existentes (típicamente tras `import { createLogger } from '../logger.js';` línea 39):
       ```javascript
       import { markSessionStatus } from '../session/manager.js';
       ```

       LOG-12 / LOG-12-extension: `manager.js` ya está fuera del grafo de `check.js` (verify.js consume manager.js indirectamente desde Phase 11). El nuevo import NO añade dependencias prohibidas. NO requiere extensión de tests source-hygiene.

    3. **Insertar la invocación de `markSessionStatus`** dentro del try-OK de `updateTaskState`, DESPUÉS de `transitioned = true` y ANTES del `} catch (err) {`:

       ```javascript
       // ANTES (líneas 230-244 actuales):
       if (verdict.action === 'pass' && commented) {
         const config = loadConfigFn();
         const providerName = session.provider || config.provider;
         const providerCfg = (config.providers && config.providers[providerName]) || {};
         const reviewState = (providerCfg.states && providerCfg.states.review) || 'In review';
         try {
           await provider.updateTaskState(task, reviewState);
           transitioned = true;
         } catch (err) {
           planeApiCallFailed(log, {
             step: 'updateTaskState',
             error: /** @type {Error} */ (err).message,
           });
         }
       }

       // DESPUÉS:
       if (verdict.action === 'pass' && commented) {
         const config = loadConfigFn();
         const providerName = session.provider || config.provider;
         const providerCfg = (config.providers && config.providers[providerName]) || {};
         const reviewState = (providerCfg.states && providerCfg.states.review) || 'In review';
         try {
           await provider.updateTaskState(task, reviewState);
           transitioned = true;
           // Phase 16 LOG-14 (D-11): mark session 'review' SOLO cuando pass + addComment OK
           // + updateTaskState OK. El reason 'gate-passed' espeja el verdict legacy mapping
           // del header (line 26) y el orchestratorReview emitido abajo. markSessionStatus
           // emite state.transition con from/to reales vía logger. SC#3: las ramas fail/
           // missing/malformed y errores Plane NO emiten state.transition (verificado por
           // test/gsd-verify-integration.test.js Task 2).
           markSessionStatus(session.task_id, 'review', 'gate-passed', log);
         } catch (err) {
           planeApiCallFailed(log, {
             step: 'updateTaskState',
             error: /** @type {Error} */ (err).message,
           });
         }
       }
       ```

       **Crítico para SC#3**: la invocación va DENTRO del try, después de `transitioned = true`. Si `updateTaskState` lanza, el throw aborta antes de `markSessionStatus`. Si alguien refactoriza moviéndola DESPUÉS del try (afuera), el test SC#3 cae (`pass + updateTaskState fail` empezaría a emitir `state.transition`, violando D-11).

    4. **NO TOCAR los siguientes elementos de `src/gsd/verify.js`**:
       - El control flow de `finalize` antes y después del bloque modificado (getTask, addComment, los 4 mapeos a legacy verdict, `orchestratorReview`, los logs `planeApiCallFailed`).
       - `renderComment` y los 4 sub-renderers (`renderPassComment`, `renderFailComment`, `renderMissingComment`, `renderMalformedComment`).
       - `findSession`, `phasesRoot` discovery, `parseVerificationFrontmatter`, `computeVerdict`.
       - Los WR-01/WR-02 paths (líneas 107-115, 134-148, 161-170 — siguen invocando `finalize` con verdict missing/malformed; markSessionStatus NO se ejecuta en ese path porque `verdict.action !== 'pass'`).
       - El return shape de `finalize` (Phase 15 ya añadió `plane.comment_body`; Phase 16 NO toca el return).
       - Las firmas de `runGsdVerify(opts, deps)` y `finalize`.
       - Los demás imports.

    5. **Verificación post-edit (mental)**:
       - `grep -c "markSessionStatus" src/gsd/verify.js` debe dar exactamente **2** (1 import + 1 invocación). Si da más, hay regresión (alguien duplicó la inserción).
       - `grep -c "markSessionStatus(session.task_id, 'review', 'gate-passed', log)" src/gsd/verify.js` debe dar exactamente **1**.
       - El `import { markSessionStatus } from '../session/manager.js'` está en la cabecera.
       - Las líneas alrededor del bloque modificado mantienen su shape (las del `if (verdict.action === 'pass' && commented)` block).
  </action>

  <verify>
    <automated>node --test test/gsd-verify-integration.test.js test/gsd-verification.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { markSessionStatus } from '../session/manager.js'" src/gsd/verify.js` === 1
    - `grep -c "markSessionStatus(session.task_id, 'review', 'gate-passed', log)" src/gsd/verify.js` === 1
    - `grep -c "markSessionStatus" src/gsd/verify.js` === 2 (import + invocación — ni más ni menos)
    - La invocación vive dentro del try de `updateTaskState`, después de `transitioned = true`, antes de `} catch (err)`. **W-3: usar window de 12 líneas para tolerar el comentario explicativo de 6 líneas que precede la invocación**:
      ```bash
      grep -A 12 "transitioned = true;" src/gsd/verify.js | grep -c "markSessionStatus"
      ```
      debe dar `1` (la línea está dentro de las 12 siguientes a `transitioned = true`).
    - El `verdict.action === 'pass' && commented` block es el ÚNICO que contiene `markSessionStatus` (grep multilinea o lectura manual confirma que no hay otras invocaciones en `finalize` ni en otros functions del archivo).
    - `node --test test/gsd-verify-integration.test.js` exit 0 — tests existentes verdes; los nuevos asserts vienen en Task 2.
    - `node --test test/gsd-verification.test.js` exit 0 — el parser/computeVerdict no se toca.
  </acceptance_criteria>

  <done>
    `src/gsd/verify.js` invoca `markSessionStatus(session.task_id, 'review', 'gate-passed', log)` exactamente una vez, dentro del try-OK de `updateTaskState`, después de `transitioned = true`. Import eager añadido. Tests existentes verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender test/gsd-verify-integration.test.js con SC#2 positive + 6 asserts negative SC#3 (B-1)</name>
  <files>test/gsd-verify-integration.test.js</files>

  <read_first>
    - test/gsd-verify-integration.test.js (estado actual completo — confirmar T20 pass, T21 fail/gaps-found, T22 malformed, T23 missing y la presencia de `makeLogger` / `makeDeps` / `makeProviderMock`)
    - src/gsd/verify.js (post-edit Task 1 — confirmar que la invocación de markSessionStatus está en su sitio)
    - src/session/manager.js (entender el shape exacto del payload `state.transition` emitido por `markSessionStatus`: campos `from`, `to`, `reason` + el `event:` key)
    - .planning/phases/16-log-09-debt-cleanup/16-PATTERNS.md sección "5. Regression tests verify.js" (líneas 565-666)
    - .planning/phases/16-log-09-debt-cleanup/16-CONTEXT.md sección Specifics (parametrización 4+2 ramas)
  </read_first>

  <behavior>
    **B-1: las 3 sub-variants de fail NO se colapsan en un solo test parametrizado.** Mínimo 2 tests separados que distinguen soft-fail (`gaps-found` o `must-haves-incomplete`) vs hard-fail (`status-failed`). Se prohíbe la opción "1 parameterized test con 3 fixtures suficiente" — se pierde la distinción que ROADMAP §Phase 16 SC#3 demanda literalmente.

    Las 6 ramas negativas que el plan exige cubrir:
    1. **Fail soft** (verdict.action === 'fail', sub-reason `gaps-found` o `must-haves-incomplete`)
    2. **Fail hard** (verdict.action === 'fail', sub-reason `status-failed`)
    3. **Missing** (VERIFICATION.md no encontrado)
    4. **Malformed** (frontmatter inválido)
    5. **Pass + getTask fail** (Plane unreachable)
    6. **Pass + updateTaskState fail** (markSessionStatus dentro del try; throw aborta)

    Todas asertan `events.find((e) => e.fields?.event === 'state.transition') === undefined`.

    Plus 1 positive (SC#2): pass + Plane OK → emite state.transition con `to='review'`, `reason='gate-passed'`, `from` no vacío.

    Total esperado: 1 positive + 6 negative = ≥7 asserts referenciando `fields?.event === 'state.transition'`.

    - Test 1 (SC#2 positive — pass branch emite state.transition): T20 (pass) extendido. Tras `await runGsdVerify(...)`, asertar:
      - `events.find((e) => e.fields?.event === 'state.transition')` truthy.
      - `transition.level === 'info'`.
      - `transition.fields.to === 'review'`.
      - `transition.fields.reason === 'gate-passed'`.
      - `transition.fields.from` es el `session.status` previo del fixture (típicamente `'running'` — verificar con el fixture real).
    - Test 2 (SC#3 negative — fail/soft NO emite): T21 extendido o nuevo. Sub-reason `gaps-found` (o `must-haves-incomplete` si el fixture lo permite). Aserto: `events.find((e) => e.fields?.event === 'state.transition') === undefined`. Assertion message debe mencionar explícitamente "soft-fail".
    - Test 3 (SC#3 negative — fail/hard NO emite): nuevo test mandatory por B-1. Sub-reason `status-failed`. Aserto: idem. Assertion message debe mencionar explícitamente "hard-fail" para distinguirlo del Test 2.
    - Test 4 (SC#3 negative — missing NO emite): T23 extendido. Mismo aserto.
    - Test 5 (SC#3 negative — malformed NO emite): T22 extendido. Mismo aserto.
    - Test 6 (SC#3 negative — pass + getTask fail NO emite): nuevo test. Provider mock con `getTask: async () => { throw new Error('Plane unreachable'); }`. Aserto: `state.transition` ausente. Sanity: `events.find(e => e.fields?.event === 'plane.api.call.failed' && e.fields.step === 'getTask')` truthy (confirma que la rama de error sí se ejerció).
    - Test 7 (SC#3 negative — pass + updateTaskState fail NO emite): nuevo test. Provider mock con `updateTaskState: async () => { throw new Error('Plane state transition rejected'); }`. Aserto: `state.transition` ausente — porque markSessionStatus está DENTRO del try y nunca se ejecuta. Sanity: `events.find(e => e.fields?.event === 'plane.api.call.failed' && e.fields.step === 'updateTaskState')` truthy.
    - Test 8 (idempotencia / no-double-emit, opcional): correr `runGsdVerify` dos veces con el mismo fixture pass — la segunda invocación SÍ emite `state.transition` de nuevo (no hay deduplicación). Documenta que no asumimos "emite una sola vez por sesión global".
  </behavior>

  <action>
    1. **Reusar `makeLogger` existente** (líneas 73-83 del archivo actual). El helper ya cumple los requisitos (4 niveles + child self-return). NO duplicar — referencia desde los nuevos tests.

    2. **Extender T20 (pass branch)**. Localizar el test que postea pass; tras los asserts existentes sobre `commented`/`transitioned`/Plane, añadir el bloque positive:
       ```javascript
       // Phase 16 LOG-14 SC#2: pass branch emite state.transition
       const transition = events.find((e) => e.fields?.event === 'state.transition');
       assert.ok(transition, 'pass + Plane OK debe emitir state.transition');
       assert.equal(transition.level, 'info');
       assert.equal(transition.fields.to, 'review');
       assert.equal(transition.fields.reason, 'gate-passed');
       // from depende del fixture session.status — el test confirma que el campo existe y NO está vacío
       assert.ok(typeof transition.fields.from === 'string' && transition.fields.from.length > 0);
       ```

    3. **Extender T22, T23** (malformed/missing). En cada test, después de los asserts existentes, añadir:
       ```javascript
       // Phase 16 LOG-14 SC#3: <branch> NO emite state.transition
       const transition = events.find((e) => e.fields?.event === 'state.transition');
       assert.equal(transition, undefined, '<branch> branch must NOT emit state.transition');
       ```
       Sustituir `<branch>` por el nombre del verdict (`'malformed'`, `'missing'`).

    4. **B-1 (CRÍTICO): cubrir las 3 sub-variantes de fail con AL MENOS 2 tests separados** (un soft + un hard). **PROHIBIDO** colapsar las 3 en un único test parametrizado que pierda la distinción soft-fail vs hard-fail. Opciones aceptables:

       **Opción A (recomendada — 2 tests separados):**
       ```javascript
       it('SC#3 LOG-14: fail soft (gaps-found) → NO state.transition emitted', async () => {
         // VERIFICATION.md fixture con must-haves-incomplete o gaps en checklist (soft-fail)
         writeFileSync(/* ... fixture sub-reason 'gaps-found' o 'must-haves-incomplete' ... */);
         // ... resto del setup estándar ...
         await runGsdVerify({ sessionId: 'sess-int' }, deps);
         const transition = events.find((e) => e.fields?.event === 'state.transition');
         assert.equal(transition, undefined,
           'soft-fail (gaps-found / must-haves-incomplete) must NOT emit state.transition — verdict.action !== "pass"');
       });

       it('SC#3 LOG-14: fail hard (status-failed) → NO state.transition emitted', async () => {
         // VERIFICATION.md fixture con status: failed (hard-fail explícito)
         writeFileSync(/* ... fixture sub-reason 'status-failed' ... */);
         // ... resto del setup estándar ...
         await runGsdVerify({ sessionId: 'sess-int' }, deps);
         const transition = events.find((e) => e.fields?.event === 'state.transition');
         assert.equal(transition, undefined,
           'hard-fail (status-failed) must NOT emit state.transition — verdict.action !== "pass"');
       });
       ```

       **Opción B (test parametrizado con 2+ casos preservando distinción):**
       ```javascript
       const failVariants = [
         { name: 'soft (gaps-found)',       fixture: /* ... */, kind: 'soft' },
         { name: 'hard (status-failed)',    fixture: /* ... */, kind: 'hard' },
         // Opcional 3a: { name: 'soft (must-haves-incomplete)', fixture: /* ... */, kind: 'soft' },
       ];
       for (const v of failVariants) {
         it(`SC#3 LOG-14: fail ${v.name} → NO state.transition emitted`, async () => {
           writeFileSync(/* v.fixture */);
           // ... setup ...
           await runGsdVerify({ sessionId: 'sess-int' }, deps);
           const transition = events.find((e) => e.fields?.event === 'state.transition');
           assert.equal(transition, undefined,
             `${v.kind}-fail (${v.name}) must NOT emit state.transition`);
         });
       }
       ```
       Mínimo 2 entries (1 soft + 1 hard); 3 entries (2 soft variants + 1 hard) si los fixtures están disponibles.

       **Opción C (PROHIBIDA por B-1):** un único test `it('all fail variants → no state.transition', () => { for (fixture of [...]) { ... } })` que itera sin generar tests independientes. El parametrizado debe generar `it(...)` blocks separados (Opción B), no un único `it` con loop interno.

    5. **Añadir 2 tests nuevos para errores Plane** (SC#3 — getTask fail + updateTaskState fail). Patrón exacto en PATTERNS.md líneas 627-657:

       ```javascript
       it('SC#3 LOG-14: pass + getTask fails → NO state.transition emitted', async () => {
         // VERIFICATION.md fixture pass válido
         writeFileSync(/* ... */);
         const session = makeSession();
         const { deps, calls, events } = makeDeps(session);
         // Override provider para lanzar en getTask:
         const originalGetProviderFn = deps.getProviderFn;
         deps.getProviderFn = async () => ({
           getTask: async () => { throw new Error('Plane unreachable'); },
           addComment: async () => {},
           updateTaskState: async () => {},
         });
         await runGsdVerify({ sessionId: 'sess-int' }, deps);
         const transition = events.find((e) => e.fields?.event === 'state.transition');
         assert.equal(transition, undefined,
           'pass + getTask fail must NOT emit state.transition (markSessionStatus skipped — no entra al if (task) block)');
         // Sanity: planeApiCallFailed sí se emitió
         const apiFailed = events.find((e) =>
           e.fields?.event === 'plane.api.call.failed' && e.fields.step === 'getTask');
         assert.ok(apiFailed, 'planeApiCallFailed should fire on getTask error');
       });

       it('SC#3 LOG-14: pass + updateTaskState fails → NO state.transition emitted', async () => {
         writeFileSync(/* ... */);
         const session = makeSession();
         const { deps, calls, events } = makeDeps(session);
         deps.getProviderFn = async () => ({
           getTask: async (ref) => ({ ref, /* ... */ }),
           addComment: async () => {},
           updateTaskState: async () => { throw new Error('Plane state transition rejected'); },
         });
         await runGsdVerify({ sessionId: 'sess-int' }, deps);
         const transition = events.find((e) => e.fields?.event === 'state.transition');
         assert.equal(transition, undefined,
           'pass + updateTaskState fail must NOT emit state.transition (markSessionStatus is INSIDE the updateTaskState try; throw aborts before invocation — D-11 order)');
         // Sanity: planeApiCallFailed sí se emitió
         const apiFailed = events.find((e) =>
           e.fields?.event === 'plane.api.call.failed' && e.fields.step === 'updateTaskState');
         assert.ok(apiFailed, 'planeApiCallFailed should fire on updateTaskState error');
       });
       ```

       **Importante para Test 7 (updateTaskState fail)**: el assertion message documenta el orden exacto del cableado (Task 1 D-11). Si alguien refactoriza moviendo `markSessionStatus` afuera del try, este test cae con el mensaje correcto, identificando el problema específicamente.

    6. **Test 8 idempotencia** (opcional pero defensivo): correr `runGsdVerify` dos veces con la misma session/fixture pass. La segunda invocación produce nuevos eventos en el mismo `events` array (asumiendo memSink compartido). Aserto: `events.filter(e => e.fields?.event === 'state.transition').length === 2`. Documenta que no hay deduplicación a nivel verify.js — si en el futuro se quiere hacer idempotente, este test caería y obligaría a la decisión consciente.

       **Discretion**: si el test parece redundante con la cobertura existente, OMITIRLO. No es SC#3 mandatorio.

    7. **NO TOCAR**:
       - El helper `makeLogger`, `makeDeps`, `makeProviderMock` existentes (reuse, no rewrite).
       - Los fixtures de VERIFICATION.md existentes — los nuevos tests pueden compartirlos.
       - Tests de `gsd-verification.test.js` (parser puro — sin cambio).
       - Otros tests no relacionados con LOG-14.
  </action>

  <verify>
    <automated>node --test test/gsd-verify-integration.test.js test/gsd-verification.test.js test/manager.test.js</automated>
  </verify>

  <acceptance_criteria>
    - **B-1 enforce: ≥7 referencias a `fields?.event === 'state.transition'`** en el archivo (1 positive + 6 negative: 2 fail variants minimum + missing + malformed + getTask-fail + updateTaskState-fail). Si se incluye un 3er fail variant (must-haves-incomplete), serían ≥8:
      ```bash
      grep -c "fields?.event === 'state.transition'" test/gsd-verify-integration.test.js
      ```
      debe dar `>= 7`.
    - **B-1 enforce: distinción soft-fail / hard-fail explícita en el archivo**:
      ```bash
      grep -cE "soft-fail|gaps-found|must-haves-incomplete" test/gsd-verify-integration.test.js
      ```
      debe dar `>= 1` (al menos una sub-variant soft mencionada en assertion messages o test names).
      ```bash
      grep -cE "hard-fail|status-failed" test/gsd-verify-integration.test.js
      ```
      debe dar `>= 1` (al menos una sub-variant hard mencionada en assertion messages o test names).
    - `grep -c "assert.ok(transition" test/gsd-verify-integration.test.js` >= 1 (positive assert SC#2)
    - `grep -c "assert.equal(transition, undefined" test/gsd-verify-integration.test.js` >= 6 (6 negative asserts SC#3 — 2 fail variants minimum + malformed + missing + getTask-fail + updateTaskState-fail)
    - `grep -c "to: 'review'" test/gsd-verify-integration.test.js` >= 1 (positive assert verifica `to`)
    - `grep -c "reason: 'gate-passed'" test/gsd-verify-integration.test.js` >= 1 O `grep -c "reason, 'gate-passed'" >= 1` (positive assert verifica `reason`)
    - Los 2 nuevos tests `SC#3 LOG-14: pass + getTask fails` y `SC#3 LOG-14: pass + updateTaskState fails` están presentes (grep por `'pass + getTask fails'` y `'pass + updateTaskState fails'`).
    - `node --test test/gsd-verify-integration.test.js` exit 0 — todos los tests verdes (existentes + nuevos).
    - El test `pass + updateTaskState fails` es el centinela de orden: si Task 1 movió `markSessionStatus` afuera del try, este test FALLA. Verificable mentalmente revirtiendo Task 1.
  </acceptance_criteria>

  <done>
    `test/gsd-verify-integration.test.js` cubre SC#2 (positive pass-branch) y SC#3 (B-1: ≥6 asserts negative incluyendo 2 fail sub-variants explícitas soft+hard, missing, malformed, getTask-fail, updateTaskState-fail). El test `pass + updateTaskState fails` documenta y enforce el orden exacto del cableado D-11. `node --test` global pasa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify.js → manager.js (markSessionStatus) | invocación interna del runtime. `manager.js` muta el state.json y emite `state.transition` vía logger child. |
| manager.js → state.json (filesystem) | mutation atómica del session record. Falla del fs ya manejada en helper (Phase 6). |
| logger child → NDJSON file | append-only write con redacción aplicada upstream (Phase 6). Falla del logger NO debe bloquear el verify pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | T (Tampering) | state.transition contract drift | mitigate | El test SC#2 positive verifica los 3 campos canónicos del NDJSON (`from`, `to`, `reason`). Si alguien renombra o re-shapea, los asserts caen. Filter por `e.fields?.event === 'state.transition'` (NO por `e.msg`) preserva el contrato a través de logger refactors (Pattern D pattern-mapper). |
| T-16-07 | I (Information disclosure) | state.transition payload | accept | Los campos `from`/`to`/`reason` no contienen PII (status enum + reason string informativo). El logger ya tiene redacción aplicada en Phase 6 para session_id/task_id si fuera necesario. Sin nueva superficie de información. |
| T-16-08 | E (Elevation of privilege) | markSessionStatus invoked en rama equivocada | mitigate | SC#3 regression test cubre las 6 ramas no-pass que deben NO invocar markSessionStatus, con distinción explícita soft-fail vs hard-fail (B-1). Si alguien refactoriza moviendo la línea fuera del try-OK, el test `pass + updateTaskState fails` cae con mensaje específico. El test es la red de seguridad para D-11 enforcement. |
| T-16-09 | D (Denial of Service) | logger failure blocking verify pipeline | accept | `markSessionStatus` recibe el `log` del scope y NO está envuelto en try/catch silencioso (a diferencia de stop.js Plan 03 D-09). Razón: si markSessionStatus falla en verify, el verdict ya tuvo side-effects Plane (comment + state transition); fallar early es preferible a un state desincronizado entre Plane y kodo. Aceptado: el control flow de `runGsdVerify` propaga la excepción al CLI handler que la maps a exit 1. |
| T-16-10 | T (Tampering) | LOG-12 guard regression | mitigate | El nuevo import `from '../session/manager.js'` añade un nodo al grafo de verify.js. `manager.js` ya estaba en el grafo (verify.js ya consume otras exports de manager.js). Sin cambio en el grafo de `check.js` — `test/check-isolation.test.js` sigue verde. |
| T-16-11 | R (Repudiation) | Phase 10 D-11/D-12 order regression | mitigate | Phase 10 D-11 enforce `updateTaskState SOLO en pass + addComment OK`. Phase 16 inserta `markSessionStatus` SIN reordenar — la invocación está DESPUÉS de `updateTaskState` exitosa, manteniendo el orden Phase 10. Test `pass + updateTaskState fails` en Task 2 verifica que markSessionStatus respeta el orden. |

**Notas**: T-16-09 es la única decisión de diseño no-trivial (NO envolver en try/catch). Diferencia explicita con Plan 03 D-09 (stop.js SÍ envuelve por D-09 — "never block lock release"). Razón: stop.js es hook mecánico tras todo el flujo; verify.js está en medio del pipeline con side-effects Plane ya aplicados. Fallar pronto es más seguro.
</threat_model>

<verification>
- `node --test test/gsd-verify-integration.test.js test/gsd-verification.test.js test/manager.test.js` exit 0.
- `node --test` global pasa sin nuevos fallos respecto a la baseline Phase 15.
- Manual: `kodo gsd verify <session-id>` con session pass + Plane reachable produce un evento `state.transition` en el NDJSON (`grep "state.transition" ~/.kodo/logs/<session-id>.log` debe dar 1 línea con `from`/`to`/`reason`).
- Manual: `kodo gsd verify <session-id>` con session fail (VERIFICATION.md con gaps) NO produce evento `state.transition` en el NDJSON.
- `node --test test/check-isolation.test.js` exit 0 (LOG-12 invariante preservado).
</verification>

<success_criteria>
LOG-14 cubierto. SC#2 ROADMAP §Phase 16: `markSessionStatus` invocado en pass branch tras updateTaskState OK; `state.transition` con `from`/`to` reales emitido. SC#3 ROADMAP §Phase 16: ramas fail (soft + hard explícitos por B-1)/missing/malformed/getTask-fail/updateTaskState-fail/addComment-fail NO emiten `state.transition` (verificado por ≥6 asserts negative).
</success_criteria>

<output>
Tras completar, crear `.planning/phases/16-log-09-debt-cleanup/16-02-SUMMARY.md` con: archivos modificados, líneas tocadas, orden enforced (markSessionStatus DENTRO del try, después de transitioned=true), 6+ asserts SC#3 (incluyendo 2 fail sub-variants explícitas) + 1 assert SC#2, helpers reusados (makeLogger, makeDeps), ningún test existente rotó, LOG-12 sigue verde.
</output>
</output>
