---
phase: 17-phase-7-uat-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/logs-follow-integration.test.js
autonomous: true
requirements: [UAT-01]
tags: [uat, integration-test, logs-follow, subprocess]

must_haves:
  truths:
    - "Spawn child de `bin/kodo logs <session-id> --follow` con HOME aislado emite el dump-0 (archivo vacío) y luego cada batch NDJSON conforme se appendea (tail real, no fake)"
    - "Tres batches escritos a ~250ms cada uno con sentinels seq:1, seq:2, seq:3 aparecen en stdout del child en ese mismo orden estricto"
    - "El watcher cierra limpiamente con SIGINT en <2s y exit code 0 (D-06: cubre SC#1 cleanup limpio sin handles abiertos)"
    - "El tmpHome se crea con mkdtempSync y se elimina en after() (rmSync recursive force) — D-02 isolation, no contamina ~/.kodo del desarrollador"
    - "El test no usa fixtures estáticos pre-grabados — la progresión temporal del orden seq:1 antes de batch-2 escrito demuestra tail real (D-05)"
  artifacts:
    - path: "test/logs-follow-integration.test.js"
      provides: "Integration test UAT-01: spawn bin/kodo logs --follow + appendFileSync progressivo + awaitLine + SIGINT cleanup"
      contains: "spawn(process.execPath"
      contains_alt: "FOLLOW_INTERVAL_MS"
      min_lines: 100
  key_links:
    - from: "test/logs-follow-integration.test.js"
      to: "bin/kodo"
      via: "spawn(process.execPath, [KODO_BIN, 'logs', sessionId, '--follow'], { env: { ...process.env, HOME: tmpHome } })"
      pattern: "spawn.*KODO_BIN.*--follow"
    - from: "test/logs-follow-integration.test.js"
      to: "<tmpHome>/.kodo/logs/<session-id>.ndjson"
      via: "appendFileSync 3 batches"
      pattern: "appendFileSync.*test\\.batch"
    - from: "test/logs-follow-integration.test.js"
      to: "child.kill('SIGINT')"
      via: "exit code 0 within 2s"
      pattern: "kill\\('SIGINT'\\)"
---

<objective>
Convertir el UAT-01 humano de Phase 7 (`07-HUMAN-UAT.md` test #1) en integration test automatizado que spawnea `bin/kodo logs <session-id> --follow` como child process real, escribe progresivamente NDJSON al archivo objetivo, verifica con orden estricto que el child emite las líneas según se añaden (tail real, no fake), y cierra el watcher limpiamente vía SIGINT al terminar.

Purpose: Cierra SC#1 de Phase 17 (ROADMAP §Phase 17). Sustituye el TODO humano de live `--follow` con cobertura programática que rompe si el path real `followFile` (`src/logs/follow.js:42-93`) regresa.

Output: `test/logs-follow-integration.test.js` con 1 test `it()` que cubre dump-0 + tail real + SIGINT cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-phase-7-uat-automation/17-CONTEXT.md

<!-- Source-of-truth files exercised por el test (read-only, NO modificar) -->
@src/logs/follow.js
@src/cli.js

<!-- Patrón canónico que el test debe replicar (D-02 + CR-02 Phase 16) -->
@test/stop-state-transition.test.js
@test/version-smoke.test.js

<interfaces>
<!-- Contratos relevantes extraídos del codebase. El executor NO debe explorar más. -->

From src/logs/follow.js:
```js
// Polling-based tail con buffer de partial-line. Watcher real, no mock.
export const FOLLOW_INTERVAL_MS = 200; // poll del fs.watchFile
export function followFile(filePath, onLine) { /* dump-0 + tail + SIGINT handler */ }
// SIGINT handler interno: unwatchFile + process.exit(0) (líneas 67-70).
```

From src/cli.js (líneas 213-239):
```js
program.command('logs [session-id]')
  .option('-f, --follow', 'Tail live output (like tail -f)')
  .action(async (sessionId, opts) => { /* delega a runLogs() */ });
```

From test/version-smoke.test.js (precedente bin/kodo spawn):
```js
const KODO_BIN = join(REPO, 'bin', 'kodo');
spawnSync(process.execPath, [KODO_BIN, '--version'], { ... });
```

From test/stop-state-transition.test.js (D-02 HOME override pattern):
```js
tmpHome = mkdtempSync(join(tmpdir(), 'kodo-test-stop-state-'));
process.env.HOME = tmpHome;  // antes de imports dinámicos
mkdirSync(join(tmpHome, '.kodo'), { recursive: true });
// after(): rmSync(tmpHome, { recursive: true, force: true });
```

D-XX decisions del CONTEXT.md aplicables a este plan:
- D-01: spawn(process.execPath, [...], opts) — subprocess real, NO import directo de followFile.
- D-02: HOME override via env del child + mkdtempSync 'kodo-uat-' + cleanup en after(). Mismo filosofía que CR-02 Phase 16 pero adaptado a child process.
- D-03: NO introducir KODO_DIR público ni cwd override.
- D-04: 3 batches con setInterval ~250ms (>= FOLLOW_INTERVAL_MS=200ms). Cada batch appendFileSync con sentinel `{event:'test.batch', seq:1|2|3, timestamp:'<ISO>'}`.
- D-05: awaitLine(stream, sentinel, timeoutMs) helper inline. Acumula chunks de stdout, resuelve al ver sentinel, rechaza con timeout. Asserts orden ESTRICTO: await seq:1 ANTES de escribir batch 2, etc.
- D-06: child.kill('SIGINT') + await on('exit') con timeout duro 2s. Assert exit code 0.
- D-07: pre-crear archivo NDJSON vacío con writeFileSync(logFile, '') ANTES del spawn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear test/logs-follow-integration.test.js con dump-0 + tail real + SIGINT cleanup</name>
  <files>test/logs-follow-integration.test.js</files>
  <read_first>
    - test/logs-follow-integration.test.js (confirmar ausencia o estado actual)
    - .planning/phases/17-phase-7-uat-automation/17-CONTEXT.md (D-01..D-07)
    - test/stop-state-transition.test.js (patrón mkdtempSync + HOME override + after cleanup)
    - test/version-smoke.test.js (patrón spawn bin/kodo desde el runner)
    - src/logs/follow.js (contrato: FOLLOW_INTERVAL_MS=200, SIGINT → unwatchFile + exit(0))
    - src/cli.js líneas 213-239 (subcomando logs --follow)
  </read_first>
  <action>
Crear el archivo `test/logs-follow-integration.test.js` con 1 test `it()` que valida UAT-01 end-to-end. Estructura concreta:

1. Imports: `describe, it, before, after` from `node:test`; `assert` strict; `spawn` from `node:child_process`; `mkdtempSync, rmSync, writeFileSync, appendFileSync, mkdirSync` from `node:fs`; `tmpdir` from `node:os`; `join, resolve, dirname` from `node:path`; `fileURLToPath` from `node:url`.

2. Constantes: `__dirname = dirname(fileURLToPath(import.meta.url))`; `REPO = resolve(__dirname, '..')`; `KODO_BIN = join(REPO, 'bin', 'kodo')`. Mismo patrón que `test/version-smoke.test.js`.

3. Helper inline `awaitLine(stream, predicate, timeoutMs)` (D-05): acumula chunks de stdout en un buffer string, parte por '\n', y resuelve cuando alguna línea satisface `predicate(line)`. Rechaza con `Error('awaitLine timeout: ' + descripción)` si pasa `timeoutMs`. Limpia el listener al resolver/rechazar para no leakear handles. Acepta `predicate` como función para que el test pase un parser JSON tolerante (try/catch) que matche por `seq`.

4. Helper inline `waitForExit(child, timeoutMs)` (D-06): retorna Promise que resuelve con `{code, signal}` cuando el child emite 'exit', rechaza con `Error('waitForExit timeout')` si pasa el timeout.

5. Setup `describe('UAT-01 SC#1: kodo logs --follow real tail + SIGINT cleanup', ...)`:
   - `before`: `tmpHome = mkdtempSync(join(tmpdir(), 'kodo-uat-follow-'))`, `mkdirSync(join(tmpHome, '.kodo', 'logs'), { recursive: true })`. Guardar `origHome = process.env.HOME`. (NO override del HOME global del test runner — el test pasa HOME al child env directamente, D-02.)
   - `after`: `rmSync(tmpHome, { recursive: true, force: true })`. Restaurar HOME global no necesario porque no se tocó.

6. El test `it('dumps empty file then tails 3 progressive batches in strict order and exits cleanly on SIGINT (D-04..D-07)', async () => { ... })`:

   a. `const sessionId = 'uat-follow-' + process.pid + '-' + Date.now();` (id único por test run para evitar colisiones).
   b. `const logFile = join(tmpHome, '.kodo', 'logs', sessionId + '.ndjson');`
   c. **D-07**: pre-crear archivo NDJSON vacío: `writeFileSync(logFile, '')`. Esto asegura el path "existe pero vacío" (dump-0 + tail), NO el path "waiting for session log to appear" (deferred).
   d. **D-01 + D-02**: spawn child:
      ```js
      const child = spawn(
        process.execPath,
        [KODO_BIN, 'logs', sessionId, '--follow'],
        {
          cwd: REPO,
          env: { ...process.env, HOME: tmpHome },
          stdio: ['ignore', 'pipe', 'pipe'],
        }
      );
      ```
   e. Capturar stderr en un buffer (debugging local, NO assertar contenido — discretion del CONTEXT). `let stderr = ''; child.stderr.on('data', (b) => { stderr += b.toString('utf8'); });`
   f. **D-04 + D-05** progresión + asserts orden estricto. Helper `appendBatch(seq)`:
      ```js
      const sentinel = JSON.stringify({ event: 'test.batch', seq, timestamp: new Date().toISOString() });
      appendFileSync(logFile, sentinel + '\n');
      ```
      Predicate parser: `(line) => { try { const r = JSON.parse(line); return r.event === 'test.batch' && r.seq === expectedSeq; } catch { return false; } }`.

      Pequeña espera inicial 350ms para que el child arranque y haya hecho dump-0 + esté en watch loop (FOLLOW_INTERVAL_MS=200, +50ms margen). Implementar como `await new Promise((r) => setTimeout(r, 350));` — única espera fija, justo por encima del poll interval del watcher (D-04 menciona 250ms ≥ poll, este 350ms es startup buffer, no progression interval).

      Luego en bucle estricto:
      ```js
      for (const seq of [1, 2, 3]) {
        appendBatch(seq);
        await awaitLine(child.stdout, (line) => { try { const r = JSON.parse(line); return r.event === 'test.batch' && r.seq === seq; } catch { return false; } }, 2000);
        if (seq < 3) await new Promise((r) => setTimeout(r, 250));  // D-04: ≥ FOLLOW_INTERVAL_MS para evitar coalescencia
      }
      ```

      **Importante**: la prueba de tail real es que `awaitLine(seq=1)` se cumpla ANTES de que `appendBatch(2)` se haya ejecutado — si la implementación bufferera todo y emitiera al final, el ordering temporal lo demostraría falso (assert 1 ya habría fallado por timeout). NO necesitas asserts adicionales sobre timing.

   g. **D-06** SIGINT cleanup: `child.kill('SIGINT'); const { code } = await waitForExit(child, 2000); assert.equal(code, 0, 'SIGINT debe convertir en exit 0 limpio en <2s — D-06 cleanup sin handles abiertos. stderr=' + stderr);`

7. NO mockear `unwatchFile` ni inspeccionar handles abiertos. El timeout duro de 2s en `waitForExit` ES el assert de cleanup limpio (si quedan handles, exit no se completa).

8. NO usar `--json` (D-07 Phase 14 no aplica — UAT-01 no toca el flag). NO override de `FOLLOW_INTERVAL_MS` por env (D-04 prohibido — superficie test-only).

9. Coherencia con stop-state-transition.test.js patron: si añades try/finally para garantizar cleanup ante throws en el test body, no hace falta — el `after()` global recoge tmpHome igualmente, y el child queda muerto con SIGINT/SIGKILL del test runner.
  </action>
  <verify>
    <automated>node --test test/logs-follow-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/logs-follow-integration.test.js` exists.
    - File contains `spawn(process.execPath` (D-01: subprocess real).
    - File contains `mkdtempSync` AND `'kodo-uat-` (D-02: tmpHome aislado).
    - File contains `HOME: tmpHome` (D-02: HOME override en env del child).
    - File contains `KODO_BIN` AND `'logs'` AND `'--follow'` (target subcommand).
    - File contains `appendFileSync` AND `'test.batch'` (D-04: sentinels propios).
    - File contains `seq: 1` AND `seq: 2` AND `seq: 3` (D-04: 3 batches).
    - File contains `awaitLine` (D-05: helper de orden estricto inline).
    - File contains `kill('SIGINT')` (D-06: cleanup vía señal real).
    - File contains `'exit'` (D-06: await child exit).
    - File contains `2000` (D-06: timeout duro 2s para SIGINT cleanup).
    - File contains `writeFileSync(` AND `, '')` (D-07: pre-crear archivo vacío) — grep tolerante a espaciado.
    - File does NOT contain `FOLLOW_INTERVAL_MS=` env override (D-04 prohibido) — grep `process\.env\.FOLLOW_INTERVAL_MS\s*=` returns 0.
    - File does NOT contain `import.*followFile` (D-01: NO import directo del módulo bajo test) — grep `from.*src/logs/follow` returns 0.
    - `node --test test/logs-follow-integration.test.js` exits 0.
    - `node --test` (full suite) exits 0 — sin regresiones.
    - El test es deterministic en run repetido: `for i in 1 2 3; do node --test test/logs-follow-integration.test.js || exit 1; done` exits 0.
  </acceptance_criteria>
  <done>UAT-01 automatizado: 1 test it() que valida dump-0 + tail real progresivo en orden estricto + SIGINT cleanup en <2s con exit 0. Suite global verde sin regresiones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner ↔ child kodo | El test spawnea un proceso hijo; código del child puede leakear handles/fds si SIGINT no es honrado. |
| child ↔ filesystem (tmpHome) | El child escribe a `<tmpHome>/.kodo/logs/<sid>.ndjson` con HOME override; un fallo de override expondría `~/.kodo` real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Denial of Service | child watcher cleanup | mitigate | `waitForExit(child, 2000)` con timeout duro; si SIGINT no completa → test falla con mensaje explícito (D-06). Imposibilita merge de regresión que leakea fd/watcher. |
| T-17-01-02 | Information Disclosure | HOME override scope | mitigate | El test pasa `HOME: tmpHome` en el env del child (D-02). NO modifica `process.env.HOME` global del runner — child y subsecuentes tests nunca cruzan boundary al `~/.kodo` del usuario. |
| T-17-01-03 | Tampering | leaked tmpdir | mitigate | `after()` global llama `rmSync(tmpHome, { recursive: true, force: true })`. Si el test crashea entre setup y after, Node ejecuta after por contrato del test runner; OS-level cleanup respalda con `/tmp` purge. |
| T-17-01-04 | Repudiation | flaky timing-dependent assert | mitigate | Buffer 350ms de startup + 250ms entre batches (≥ FOLLOW_INTERVAL_MS=200) sigue D-04 explícitamente. Timeouts internos `awaitLine(2000)` y `waitForExit(2000)` mucho mayores que el período de poll, eliminando race conditions deterministicamente bajo carga normal. |
| T-17-01-05 | Spoofing | sentinel collision con eventos canónicos | accept | `event:'test.batch'` no aparece en `EVENTS.*` (verificado contra src/logger-events.js: SESSION_START, SESSION_END, STATE_TRANSITION, ORCHESTRATOR_REVIEW, GSD_PHASE_RESOLVED, GSD_BOOTSTRAP, PLANE_API_CALL, PLANE_API_CALL_FAILED). No hay riesgo de match accidental. |

Severity gate: 0 high, 0 medium-high. All mitigated o accepted con justificación. Plan procede.
</threat_model>

<verification>
- `node --test test/logs-follow-integration.test.js` exit 0.
- Full suite verde: `node --test` exit 0.
- Grep checks de acceptance_criteria todos green.
- 3 ejecuciones consecutivas verdes (deterministicidad).
</verification>

<success_criteria>
- 1 archivo nuevo: `test/logs-follow-integration.test.js`.
- 1 test `it()` cubre SC#1 de Phase 17 (tail real + cleanup limpio).
- Sin regresiones en suite existente.
- Sin nuevas dependencias externas (solo stdlib + node:test).
- Sin `--test-only`, sin sleeps > FOLLOW_INTERVAL_MS exceptuando el startup buffer (350ms) que sigue D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/17-phase-7-uat-automation/17-01-SUMMARY.md` con:
- file created (path)
- D-decisions covered (D-01..D-07)
- exit-code observado del SIGINT cleanup
- duración aproximada del test (sanity timing)
- regresiones detectadas (debe ser ninguna)
</output>
