---
phase: 17-phase-7-uat-automation
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified: []
autonomous: true
requirements: [UAT-01, UAT-02, UAT-03]
tags: [verification, suite-green, sc5, no-regression, source-hygiene]

must_haves:
  truths:
    - "`node --test` end-to-end (full suite) exits 0 — los 3 nuevos tests pasan junto a los 414+ existentes (SC#5 de Phase 17)"
    - "Los 3 nuevos archivos test NO contienen `--test-only` ni `it.only` (SC#5: sin nuevos --test-only)"
    - "Los 3 nuevos archivos test NO contienen sleeps fijos > 350ms exceptuando el startup buffer documentado (SC#5: sin sleeps mayores que el watcher poll de fs.watchFile salvo justificados)"
    - "Los 3 nuevos archivos test NO añaden imports externos no presentes en `package.json` antes de Phase 17 (SC#5: sin dependencias externas más allá de las ya existentes)"
    - "Los 3 archivos test ejecutados consecutivamente 3 veces son verdes (deterministicidad cross-run)"
  artifacts: []
  key_links:
    - from: "node --test (full suite)"
      to: "test/*.test.js (414+ tests + 3 nuevos UAT)"
      via: "test runner ejecuta todos los archivos test/*.test.js"
      pattern: "node --test"
---

<objective>
Verificar SC#5 de Phase 17: la suite global pasa (`node --test`) y los 3 nuevos tests forman parte de los pasados, sin nuevos `--test-only`, sin sleeps mayores que el watcher poll de fs.watchFile (FOLLOW_INTERVAL_MS=200ms) excepto los explícitamente justificados, y sin nuevas dependencias externas más allá de las ya presentes en `package.json`.

Purpose: Cierra SC#5. Es el verification gate final que firma que Phase 17 no introdujo regresiones, no abrió superficie test-only oculta, y no escaló deps externas.

Output: `17-05-SUMMARY.md` con evidencia (output del test runner + grep checks). NO modifica ningún archivo de código o doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-phase-7-uat-automation/17-CONTEXT.md

<!-- Los 3 archivos test creados en plans 01/02/03 — read state para audit -->
@test/logs-follow-integration.test.js
@test/session-start-event.test.js
@test/session-of-resolver.test.js

<!-- package.json para audit de dependencias (no debe haber crecido) -->
@package.json

<interfaces>
<!-- Comandos de verificación a ejecutar. -->

```bash
# Suite global verde:
node --test

# 3 ejecuciones consecutivas de los 3 nuevos archivos para validar deterministicidad:
for f in test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js; do
  for i in 1 2 3; do
    node --test "$f" || { echo "FAIL: $f run $i"; exit 1; }
  done
done

# Source-hygiene: no --test-only ni it.only
grep -E '\.only\(|--test-only' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js
# (debe retornar empty con exit 1)

# No sleeps fijos > 350ms (startup buffer único justificado en plan 01):
grep -E 'setTimeout\([^,]+,\s*[5-9][0-9][0-9]\)|setTimeout\([^,]+,\s*[0-9]{4,}\)' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js
# (debe retornar empty O solo matches dentro de timeouts de awaitLine/waitForExit que son LIMITES, no waits)

# Dependencias externas — package.json no creció en deps runtime:
git diff origin/main -- package.json
# (debe ser vacío en la sección "dependencies" — Phase 14 ya añadió picocolors)
```

D-XX decisions del CONTEXT.md aplicables:
- SC#5 verbatim de ROADMAP §Phase 17: "La suite global pasa (`node --test`) y los 3 nuevos tests forman parte de los pasados — sin nuevos `--test-only`, sin sleeps mayores que el watcher poll de fs.watchFile, sin dependencias externas más allá de las ya existentes."
- D-04 (de plan 01): el startup buffer de 350ms en logs-follow-integration es el único sleep > 200ms justificado (FOLLOW_INTERVAL_MS=200 + 50ms margen de startup). Otros sleeps en los 3 archivos deben ser ≤ 250ms (entre batches) o ser timeouts (awaitLine 2000ms, waitForExit 2000ms — son LÍMITES, no waits).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ejecutar full suite y los 3 archivos UAT individualmente — capture evidencia</name>
  <files></files>
  <read_first>
    - test/logs-follow-integration.test.js (confirmar existe, creado por plan 01)
    - test/session-start-event.test.js (confirmar existe, creado por plan 02)
    - test/session-of-resolver.test.js (confirmar existe, creado por plan 03)
    - .planning/phases/17-phase-7-uat-automation/17-CONTEXT.md (SC#5 verbatim)
    - package.json (audit de deps)
  </read_first>
  <action>
Ejecutar las verificaciones de SC#5 desde la raíz del repo y capturar evidencia para `17-05-SUMMARY.md`. Pasos:

1. **Suite global verde**:
   ```bash
   node --test 2>&1 | tail -50
   ```
   Capturar el resumen final del runner (líneas con `# pass`, `# fail`, `# tests`, `# skipped`). Sí `# fail` > 0 → SC#5 falla → escalar al user.

2. **3 ejecuciones consecutivas de los 3 nuevos archivos** (deterministicidad cross-run):
   ```bash
   for f in test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js; do
     for i in 1 2 3; do
       node --test "$f" 1>/dev/null 2>&1 || { echo "FAIL: $f run $i"; exit 1; }
     done
   done
   echo "All 9 runs (3 files × 3) passed deterministically"
   ```
   Si alguna run falla, capturar el output completo del run problemático (`node --test "$f"` sin redirección) y escalar — un test flaky es regresión que bloquea Phase 17 closure.

3. **Audit source-hygiene de los 3 archivos test**:
   ```bash
   # No --test-only ni .only(:
   grep -nE '\.only\(|--test-only' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js
   # Exit code 1 (no matches) = PASS. Exit code 0 (matches found) = FAIL → SC#5 violado.

   # No sleeps fijos > 400ms exceptuando el startup buffer documentado de 350ms en plan 01:
   grep -nE 'setTimeout\(\s*[^,]+,\s*[4-9][0-9]{2}\b|setTimeout\(\s*[^,]+,\s*[0-9]{4,}\b' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js
   # Lo único permitido: matches dentro de definiciones de timeoutMs/awaitLine/waitForExit (son LÍMITES, no waits actuales).
   # Si aparece un setTimeout con valor hardcoded > 400ms FUERA de un timeout helper → SC#5 violado.
   # Reportar matches encontrados y clasificarlos como (a) timeout LIMITE = OK, (b) wait fijo = FAIL.
   ```

4. **Audit de dependencias** (sin nuevas deps runtime):
   ```bash
   # package.json no debe haber crecido respecto a la línea base post-Phase 16.
   # La línea base ya tenía picocolors (Phase 14) — los 3 plans nuevos NO deben añadir nada.
   git log --oneline -- package.json | head -5
   # Si el último commit de package.json es post-Phase 16 (commits de plan 01/02/03), CHECK manual del diff.
   git diff HEAD~10 -- package.json | grep -E '^\+\s+"[^"]+":' | head
   # Si el grep produce líneas que añaden dependencies (no devDependencies), SC#5 violado.
   # PASS: ningún diff o solo diffs de version bumps mecánicos sin nuevos paquetes.
   ```

5. **Audit de imports externos en los 3 archivos test**:
   ```bash
   # Solo imports permitidos: 'node:*' (stdlib) y '../src/*' (codebase interno).
   grep -nE "^import .* from '" test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js | grep -vE "'node:|'\\.\\./src/" | grep -v "// "
   # Empty output = PASS. Cualquier match = nuevo paquete externo → SC#5 violado.
   ```

6. **Producir `17-05-SUMMARY.md`** con TODA la evidencia capturada en los pasos 1-5: output del runner (resumen), counts de tests pass/fail/skip, output de cada grep audit, fecha-hora del run. Estructura sugerida (el ejecutor adapta):
   ```markdown
   # Phase 17 Plan 05 — SC#5 Suite Green Check SUMMARY

   ## Step 1: Full suite
   - Command: `node --test`
   - Result: <output del tail -50 con # pass / # fail / # tests / # skipped>
   - Verdict: PASS / FAIL

   ## Step 2: Deterministicidad (3 files × 3 runs = 9 runs)
   - All 9 runs passed: YES / NO
   - If NO: which file/run failed + captured output

   ## Step 3: Source-hygiene grep (no .only, no --test-only)
   - Matches: <empty / lista>
   - Verdict: PASS / FAIL

   ## Step 4: Sleep audit (no fixed sleeps > 400ms except justified)
   - Matches: <empty / clasificados>
   - Verdict: PASS / FAIL

   ## Step 5: Dependency audit
   - package.json delta: <empty / lista de packages añadidos>
   - Verdict: PASS / FAIL

   ## Step 6: External imports audit
   - Matches: <empty / lista>
   - Verdict: PASS / FAIL

   ## Final Verdict
   - Phase 17 SC#5: PASS / FAIL
   - Si FAIL: razón específica + plan de remediación

   ## Stats
   - Total tests in suite: <N>
   - New tests added in Phase 17: <expect ≥ 6 = 1 it() in 17-01 + 1 it() in 17-02 + 4 it() in 17-03>
   - Suite duration: <segundos>
   ```

7. NO modificar ningún archivo de test/ ni src/. Si SC#5 falla, escalar al user con el output específico — la remediación pertenece a un gap-closure plan separado o a un retorno al plan 01/02/03 según el origen de la regresión.
  </action>
  <verify>
    <automated>node --test 2>&1 | tail -20 | grep -E '# fail [0-9]+' | grep -qvE '# fail 0$' && exit 1; echo "FULL SUITE PASS"; for f in test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js; do for i in 1 2 3; do node --test "$f" >/dev/null 2>&1 || { echo "FAIL: $f run $i"; exit 1; }; done; done; echo "DETERMINISTIC PASS"; ! grep -E '\.only\(|--test-only' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js && echo "HYGIENE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test` (full suite) exits 0.
    - Output del runner contiene `# fail 0` (zero failed tests).
    - 9 runs deterministicos (3 files × 3 iter) todos verdes — exit 0 cada uno.
    - `grep -E '\.only\(|--test-only' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js` returns empty (exit 1, sin matches).
    - `grep -E 'setTimeout\(\s*[^,]+,\s*[4-9][0-9]{2}\b|setTimeout\(\s*[^,]+,\s*[0-9]{4,}\b' test/logs-follow-integration.test.js test/session-start-event.test.js test/session-of-resolver.test.js` o bien retorna empty, o todos los matches están dentro de definiciones de timeout (segundo argumento de awaitLine/waitForExit/spawnSync timeout — son límites, no waits).
    - `git diff origin/main -- package.json` no añade líneas nuevas en `"dependencies": { ... }` (devDependencies está fuera de scope SC#5).
    - Imports en los 3 archivos test: solo `node:*` y `'../src/*'` (audit step 5 returns empty).
    - `17-05-SUMMARY.md` creado con evidencia completa de los 6 pasos.
    - Total tests count en la suite ha CRECIDO desde la línea base post-Phase 16 (414/415 + 1 skip) — al menos +6 tests nuevos (1 + 1 + 4 minimum según plans 01/02/03).
  </acceptance_criteria>
  <done>SC#5 verificado y firmado: full suite verde, 9 runs deterministicos verdes, no .only, no sleeps fijos injustificados, no nuevas deps externas, imports internos solo. 17-05-SUMMARY.md con evidencia completa para audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner ↔ filesystem | Ejecutar la suite escribe a HOMEs aislados; los 3 plans 01/02/03 ya cubrieron isolation. |
| ejecutor ↔ git | Audit de package.json depende de `git diff origin/main` — si origin/main no está fetcheado o el branch divergió, el audit falla por motivo no relacionado con SC#5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Repudiation | flaky test bajo carga | mitigate | 9 runs (3 files × 3 iter) capturan flakiness deterministicamente. Si UNA run falla, el SUMMARY documenta cual + output completo, escalando al user antes de marcar Phase 17 closed. |
| T-17-05-02 | Tampering | hidden --test-only en otros archivos | accept | El audit grep cubre solo los 3 nuevos archivos. Si otros archivos tienen `--test-only` pre-existentes (no introducidos por Phase 17), eso es responsabilidad de su milestone. SC#5 dice "sin nuevos --test-only" — el "nuevos" se restringe a Phase 17. |
| T-17-05-03 | Denial of Service | suite duration creep | accept | SC#5 no acota duración. Phase 17 añade ~10s de tests subprocess (3 spawns × ~3s cada uno). Aceptable; si emerge regression sub-second-budget, gap-closure separado. |
| T-17-05-04 | Tampering | git diff false positive en deps audit | mitigate | Si `git diff origin/main -- package.json` falla por divergencia de branch, fallback a `git log --oneline -- package.json` + manual inspect. El SUMMARY documenta el método usado. |

Severity gate: 0 high. T-17-05-01 es load-bearing — 9 runs deterministicos es el gate principal contra flakiness. Plan procede.
</threat_model>

<verification>
- Full suite verde: `node --test` exit 0.
- 9 runs deterministicos verdes.
- 4 audits source-hygiene/sleeps/deps/imports todos PASS.
- `17-05-SUMMARY.md` creado con evidencia.
- Si CUALQUIERA falla, ejecutor NO marca Phase 17 closed — escala al user.
</verification>

<success_criteria>
- 0 archivos modificados (este plan es verification-only, no produce código).
- 1 archivo nuevo: `17-05-SUMMARY.md` (artefacto de evidencia).
- SC#5 firmado con outputs reales del test runner y greps.
- Sin regresiones detectadas — si emergen, escalar antes de cerrar.
</success_criteria>

<output>
After completion, create `.planning/phases/17-phase-7-uat-automation/17-05-SUMMARY.md` con la estructura del paso 6 del action: output del runner, deterministicidad, audits, final verdict, stats. Este SUMMARY es el cierre de Phase 17 — debe ser self-contained suficiente para que un revisor humano confirme SC#5 sin re-ejecutar.
</output>
