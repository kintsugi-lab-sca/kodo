---
phase: 11-quick-mode-recognition-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/manager.js
autonomous: true
requirements:
  - QUICK-03
  - QUICK-04
tags:
  - session
  - manager
  - skip-perms
  - persistence

must_haves:
  truths:
    - "Una task con label kodo:gsd-quick produce un Session record con gsd:true y gsd_mode:'quick' en state.json"
    - "Una task con label kodo:gsd produce un Session record con gsd:true y gsd_mode:'full'"
    - "Una task con ambos labels (kodo:gsd + kodo:gsd-quick) produce gsd_mode:'quick' (precedencia D-01 vía getGsdMode)"
    - "Una task sin labels GSD produce un Session record sin campo gsd ni gsd_mode (conditional spread, no `gsd: false`)"
    - "El comando claude generado para kodo:gsd-quick contiene --dangerously-skip-permissions (parity con kodo:gsd)"
    - "El comando claude generado para kodo:gsd contiene --dangerously-skip-permissions (sin regresión vs commit 004995c)"
    - "El comando claude generado para una task sin GSD (ni yolo) NO contiene --dangerously-skip-permissions"
    - "El comando claude generado para kodo:yolo contiene --dangerously-skip-permissions (sin regresión)"
    - "buildSessionFromTask sigue siendo función pura: misma firma, sin nuevos parámetros"
  artifacts:
    - path: "src/session/manager.js"
      provides: "buildSessionFromTask persiste gsd_mode + buildClaudeCommand deriva skipPerms via getGsdMode"
      contains: "getGsdMode(flags)"
      contains_alt: "gsd_mode: gsdMode"
  key_links:
    - from: "src/session/manager.js"
      to: "src/labels.js getGsdMode"
      via: "import named"
      pattern: "import \\{ parseKodoLabels, getGsdMode \\} from '\\.\\.\\/labels\\.js'"
    - from: "src/session/manager.js buildSessionFromTask"
      to: "state.json Session record"
      via: "conditional spread con gsd_mode"
      pattern: "\\.\\.\\.\\(gsdMode \\? \\{ gsd: true, gsd_mode: gsdMode \\} : \\{\\}\\)"
    - from: "src/session/manager.js buildClaudeCommand"
      to: "claude CLI argv"
      via: "skipPerms flag computation"
      pattern: "kodoFlags\\.includes\\('yolo'\\) \\|\\| getGsdMode\\(kodoFlags\\) !== null"
---

<objective>
Cerrar el data-plane de QUICK-03 y QUICK-04 dentro del session manager: hacer que `buildSessionFromTask` derive el modo GSD localmente vía `getGsdMode(flags)` y persista `gsd_mode` junto a `gsd:true` con un único spread atómico (D-03/D-04), y refactorizar `buildClaudeCommand` para que `skipPerms` se derive del helper en vez del literal `flags.includes('gsd')` — extiende el contrato del commit `004995c` a `kodo:gsd-quick` con un solo punto de cambio futuro (D-01/D-02).

Purpose: una task `kodo:gsd-quick` debe arrancar (a) con `gsd:true` y `gsd_mode:'quick'` en `state.json` para que Phase 12 hooks puedan ramificar; (b) con `--dangerously-skip-permissions` para que `/gsd-quick` corra autónomo. Hoy ambos faltan — el manager mira `flags.includes('gsd')` literal y `gsd-quick` no lo activa.
Output: `src/session/manager.js` modificado en dos puntos (línea 5 import, línea 27 buildSessionFromTask, línea 41-43 spread, línea 254 buildClaudeCommand, línea 262 skipPerms). Sin cambios de firma pública.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md
@.planning/phases/11-quick-mode-recognition-persistence/11-PATTERNS.md

@src/session/manager.js
@src/labels.js
@src/session/state.js
@src/triggers/dispatcher.js

<interfaces>
<!-- Helper exported by src/labels.js (consumed unchanged by this plan): -->

```javascript
// src/labels.js:53
export function getGsdMode(flags) {
  if (!Array.isArray(flags)) return null;
  if (flags.includes('gsd-quick')) return 'quick';
  if (flags.includes('gsd')) return 'full';
  return null;
}
```

<!-- Session typedef in src/session/state.js:11-32 — gsd_mode field already declared in WIP: -->

```javascript
/**
 * @typedef {{
 *   ...
 *   gsd?: boolean,             // Phase 8 D-10
 *   gsd_mode?: 'full'|'quick', // Phase 11 (this plan persists it)
 *   phase_id?: string,
 *   brief?: string,
 *   ...
 * }} Session
 */
```

<!-- buildSessionFromTask current signature (this plan does NOT change it — D-03): -->

```javascript
// src/session/manager.js:27 (signature stays identical, body extended)
export function buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief })
```

<!-- buildClaudeCommand current signature (this plan does NOT change it): -->

```javascript
// src/session/manager.js:254 (private function, signature stays identical)
function buildClaudeCommand(config, sessionId, task, description, modelOverride, kodoFlags = [], moduleName = null)
```

<!-- Existing dispatcher consumer (analog reference for the helper-derives-locally pattern): -->

```javascript
// src/triggers/dispatcher.js:74 (already in WIP — pattern to mirror)
const gsdMode = getGsdMode(kodoConfig.flags);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Persistir gsd_mode en buildSessionFromTask (QUICK-03 / D-03 / D-04)</name>
  <files>src/session/manager.js</files>
  <read_first>
    - src/session/manager.js (todo el archivo — necesitas ver el patrón de spread condicional en líneas 41-48 para `gsd`, `phase_id`, `brief`; el spread nuevo lo reemplaza al gsd existente y reusa la misma forma)
    - src/labels.js (líneas 40-58 — confirmar firma exacta de `getGsdMode(flags)` y su retorno tipado `'full'|'quick'|null`)
    - src/session/state.js (líneas 11-32 — confirmar que `gsd_mode?: 'full'|'quick'` ya está en el typedef WIP, línea 26)
    - src/triggers/dispatcher.js (líneas 5 y 70-74 — analog "derive locally con getGsdMode" que este plan replica en manager)
    - .planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md (`<decisions>` D-03 "buildSessionFromTask deriva el modo localmente vía getGsdMode" + D-04 "Forma del spread: ...(gsdMode ? { gsd: true, gsd_mode: gsdMode } : {})")
    - .planning/phases/11-quick-mode-recognition-persistence/11-PATTERNS.md (§2 "src/session/manager.js:43" — pattern a copiar verbatim, incluyendo el comentario explicativo)
  </read_first>
  <behavior>
    - Test 1: buildSessionFromTask({...mínimo, flags:['gsd-quick']}) ⇒ session.gsd === true && session.gsd_mode === 'quick'
    - Test 2: buildSessionFromTask({...mínimo, flags:['gsd']}) ⇒ session.gsd === true && session.gsd_mode === 'full'
    - Test 3: buildSessionFromTask({...mínimo, flags:['gsd','gsd-quick']}) ⇒ session.gsd_mode === 'quick' (precedencia)
    - Test 4: buildSessionFromTask({...mínimo, flags:[]}) ⇒ !('gsd' in session) && !('gsd_mode' in session) (campos omitidos por el spread, no `gsd:false`)
    - Test 5: buildSessionFromTask({...mínimo, flags: undefined}) ⇒ no `gsd`, no `gsd_mode` (defensivo via getGsdMode)
    - Test 6: La firma de buildSessionFromTask sigue aceptando exactamente los 8 params actuales: { task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief }. NO se añade `gsdMode` como param.
    (Tests viven en Phase 13 QUICK-08; este plan solo asegura los comportamientos.)
  </behavior>
  <action>
**Paso 1: Ampliar el import de `../labels.js` en línea 5.**

Línea 5 actual:
```javascript
import { parseKodoLabels } from '../labels.js';
```

Reemplazar por:
```javascript
import { parseKodoLabels, getGsdMode } from '../labels.js';
```

**Paso 2: Derivar `gsdMode` localmente al inicio del cuerpo de `buildSessionFromTask`.**

La firma de `buildSessionFromTask` (línea 27) NO se toca. Se añade UNA línea al inicio del cuerpo, ANTES del `return {`. La función actualmente empieza así:

```javascript
export function buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief }) {
  return {
    workspace_ref: workspaceRef,
    ...
```

Insertar entre la apertura `{` y `return {`:

```javascript
export function buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief }) {
  // Phase 11 (D-03): GSD execution mode derived locally from flags. Single source
  // of truth: `flags`. The signature does NOT grow — gsdMode is a local derivation,
  // mirroring the dispatcher pattern at src/triggers/dispatcher.js:74.
  const gsdMode = getGsdMode(flags);
  return {
    workspace_ref: workspaceRef,
    ...
```

**Paso 3: Reemplazar el spread `gsd:true` actual (línea 41-43) por el spread atómico `gsd:true + gsd_mode`.**

Líneas 41-43 actuales:
```javascript
    // D-12: GSD flag propagated from labels through dispatcher → launchWorkItem.
    // Field is omitted entirely when flags do not include 'gsd' (treated as falsy/non-GSD).
    ...(flags?.includes('gsd') ? { gsd: true } : {}),
```

Reemplazar las TRES líneas (comentario + comentario + spread) por:
```javascript
    // Phase 11 (D-03/D-04): GSD mode derived locally from flags via getGsdMode.
    // When set, gsd_mode is ALWAYS persisted alongside gsd:true (no missing-mode
    // shape post-v0.4). Legacy sessions with gsd:true and no gsd_mode are read
    // as 'full' by getSessionMode (D-08). 'kodo:gsd-quick' wins over 'kodo:gsd'
    // (precedence centralized in getGsdMode — single point of change for new modes).
    ...(gsdMode ? { gsd: true, gsd_mode: gsdMode } : {}),
```

**Paso 4: NO TOCAR las dos líneas siguientes** (`...(phaseId ? { phase_id: phaseId } : {})` y `...(brief ? { brief } : {})`). Permanecen idénticas — `phase_id` ya viene del dispatcher correctamente discardeado en modo quick (ver `src/triggers/dispatcher.js:153-160`, donde `gsdPhaseId` solo se setea si `gsdMode === 'full'`).

**Por qué este shape (D-04 razonamiento):**
- Persistir siempre `gsd_mode` junto a `gsd:true` post-Phase 11 garantiza que cualquier nueva sesión tenga el campo. Sólo las sesiones legacy (pre-v0.4 ya en `state.json`) tienen `gsd:true` sin `gsd_mode` — y el helper `getSessionMode` (Plan 01) las lee como `'full'`.
- El spread atómico evita estados inconsistentes (no puede haber `gsd:true` sin `gsd_mode` o viceversa en sesiones nuevas).
- `gsdMode ? ... : ...` reemplaza `flags?.includes('gsd')` y es funcionalmente más correcto: detecta `gsd-quick` además de `gsd` (el chequeo viejo no detectaba quick).
- Idempotencia: `getGsdMode` ya devuelve `null` para flags inválidos/undefined, así que el `?` defensivo de `flags?.includes` ya no es necesario — `getGsdMode` lo absorbe (línea 54: `if (!Array.isArray(flags)) return null;`).

**Validación de no-regresión:** una task que antes pasaba `flags:['gsd']` recibía `{gsd:true}` en el record. Ahora recibe `{gsd:true, gsd_mode:'full'}`. La compatibilidad de lectura está cubierta por `getSessionMode` (D-08): cualquier consumer que lea `session.gsd` sigue funcionando; los nuevos consumers (Phase 12) leerán `getSessionMode(session)` y obtendrán `'full'` o `'quick'` consistentemente.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node -e "const { buildSessionFromTask } = await import('./src/session/manager.js'); const base = { task:{id:'t1', ref:'KL-1', title:'T', projectId:'p1', url:'u', projectName:'N'}, providerName:'plane', projectPath:'/x', workspaceRef:'w:1', sessionId:'s1' }; const q = buildSessionFromTask({...base, flags:['gsd-quick']}); console.assert(q.gsd === true && q.gsd_mode === 'quick', 'quick fail: '+JSON.stringify(q)); const f = buildSessionFromTask({...base, flags:['gsd']}); console.assert(f.gsd === true && f.gsd_mode === 'full', 'full fail: '+JSON.stringify(f)); const both = buildSessionFromTask({...base, flags:['gsd','gsd-quick']}); console.assert(both.gsd_mode === 'quick', 'precedence fail: '+JSON.stringify(both)); const none = buildSessionFromTask({...base, flags:[]}); console.assert(!('gsd' in none) && !('gsd_mode' in none), 'omit fail: '+JSON.stringify(none)); const undef = buildSessionFromTask({...base, flags:undefined}); console.assert(!('gsd' in undef) && !('gsd_mode' in undef), 'undef fail: '+JSON.stringify(undef)); console.log('OK Task1');" --input-type=module</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \{ parseKodoLabels, getGsdMode \} from '\.\./labels\.js'" src/session/manager.js` matches (import ampliado)
    - `grep -c "from '../labels.js'" src/session/manager.js` returns `1` (un solo import line, no duplicado)
    - `grep "const gsdMode = getGsdMode(flags);" src/session/manager.js` matches (derivación local en buildSessionFromTask)
    - `grep "...(gsdMode ? { gsd: true, gsd_mode: gsdMode } : {})" src/session/manager.js` matches (spread atómico — usar grep -F para tratar literalmente)
    - `grep -c "flags?.includes('gsd')" src/session/manager.js` returns `0` (literal antiguo eliminado del archivo en buildSessionFromTask)
    - `grep -c "...(phaseId ? { phase_id: phaseId } : {})" src/session/manager.js` returns `1` (línea hermana intacta — usar grep -F)
    - `grep -c "...(brief ? { brief } : {})" src/session/manager.js` returns `1` (línea hermana intacta — usar grep -F)
    - `grep "buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief })" src/session/manager.js` matches (firma intacta — D-03 explícito: no se añade `gsdMode` al destructure)
    - `node --check src/session/manager.js` exits 0 (syntax OK)
    - El comando `<verify>` automated imprime `OK Task1` y exits 0
    - `node --test test/manager.test.js` exits 0 (tests existentes siguen pasando — Phase 13 añadirá los nuevos casos quick)
  </acceptance_criteria>
  <done>
    `buildSessionFromTask` deriva `gsdMode` localmente vía `getGsdMode(flags)` y persiste `gsd_mode` con valor explícito (`'full'` o `'quick'`) junto a `gsd:true` en cada nueva sesión GSD. Tasks `kodo:gsd-quick` (sólo o junto a `kodo:gsd`) producen `gsd_mode:'quick'`. Tasks sin labels GSD producen records sin `gsd` ni `gsd_mode` (campos omitidos por el spread). La firma pública de `buildSessionFromTask` no cambia. Tests existentes siguen verdes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor skipPerms a getGsdMode en buildClaudeCommand (QUICK-04 / D-01 / D-02)</name>
  <files>src/session/manager.js</files>
  <read_first>
    - src/session/manager.js (líneas 254-266 — la firma + cuerpo actual de `buildClaudeCommand`, en particular líneas 259-263 con el comentario y el cómputo de `skipPerms`)
    - src/triggers/dispatcher.js (líneas 70-74 — analog "derive locally con getGsdMode" idéntico al refactor que aplica este task)
    - commit 004995c (`feat(session): kodo:gsd implies --dangerously-skip-permissions`) — vía `git show 004995c -- src/session/manager.js` para ver el contrato original que se está extendiendo a quick. **Para esta tarea no es necesario abrir el commit — el contrato está documentado en CONTEXT.md y el código actual es la fuente de verdad**.
    - .planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md (`<decisions>` D-01 "skipPerms desde getGsdMode" + D-02 "comentario generalizado" + `<specifics>` "preservar el orden actual: yolo primero")
    - .planning/phases/11-quick-mode-recognition-persistence/11-PATTERNS.md (§3 "src/session/manager.js:262 — refactor skipPerms" — bloque exacto a escribir, incluyendo el comentario nuevo en español)
  </read_first>
  <behavior>
    - Test 1: buildClaudeCommand con kodoFlags=['gsd-quick'] genera string que incluye '--dangerously-skip-permissions'
    - Test 2: buildClaudeCommand con kodoFlags=['gsd'] genera string que incluye '--dangerously-skip-permissions' (no regresión)
    - Test 3: buildClaudeCommand con kodoFlags=['yolo'] genera string que incluye '--dangerously-skip-permissions' (no regresión)
    - Test 4: buildClaudeCommand con kodoFlags=['yolo','gsd'] genera string que incluye '--dangerously-skip-permissions' (no regresión, short-circuit ||)
    - Test 5: buildClaudeCommand con kodoFlags=[] NO incluye '--dangerously-skip-permissions'
    - Test 6: buildClaudeCommand con kodoFlags=['review'] (kodo flag no relacionado con GSD ni yolo) NO incluye '--dangerously-skip-permissions'
    (Tests viven en Phase 13 QUICK-08; este plan solo asegura los comportamientos.)
  </behavior>
  <action>
**Modificar UNICAMENTE las líneas 259-263 dentro de `buildClaudeCommand`** (la firma de `buildClaudeCommand` línea 254 NO se toca; el bloque `cliFlags` línea 263 NO se toca; el `return` línea 265 NO se toca).

Líneas 259-263 actuales:
```javascript
  // GSD sessions run `/gsd-execute-phase` autonomously; pedir confirmación
  // por cada tool call rompe la automatización. Por diseño, kodo:gsd implica
  // skip-permissions tal y como hace kodo:yolo explícito.
  const skipPerms = kodoFlags.includes('yolo') || kodoFlags.includes('gsd');
  const cliFlags = skipPerms ? '--dangerously-skip-permissions' : '';
```

Reemplazar EL COMENTARIO (3 líneas) Y la línea de `skipPerms` por:
```javascript
  // Las sesiones GSD (full y quick) corren slash commands autónomos; pedir
  // confirmación por tool call rompe la automatización. Cualquier modo GSD
  // implica skip-permissions, igual que kodo:yolo explícito. Un solo punto
  // de cambio: añadir un nuevo modo a getGsdMode() basta (D-01/D-02 Phase 11).
  const skipPerms = kodoFlags.includes('yolo') || getGsdMode(kodoFlags) !== null;
  const cliFlags = skipPerms ? '--dangerously-skip-permissions' : '';
```

**Por qué `getGsdMode(kodoFlags) !== null` y no `Boolean(getGsdMode(kodoFlags))`:**
- `getGsdMode` devuelve `'full' | 'quick' | null`. Ambos strings son truthy y `null` es falsy, así que `Boolean(...)` también funcionaría — pero `!== null` documenta la intención: "cualquier modo GSD reconocido (no la ausencia)". Si el helper en el futuro añade un modo `'maintenance'` que sigue queriendo skip-perms, `!== null` sigue cubriendo. Si alguien (mal) le añade un retorno `''` falsy, `Boolean(...)` colapsaría — `!== null` fallaría visiblemente. Inclinarse a la versión que falla rápido si la firma del helper cambia.

**Por qué `yolo` primero (orden invariante de `<specifics>`):**
- `yolo` es intención explícita del humano (puso el label `kodo:yolo` a propósito); GSD es intención implícita del modo. El short-circuit `||` evalúa `yolo` primero — si está, ni siquiera llamamos `getGsdMode`. Trazabilidad humana coherente: el comentario "yolo primero" se mantiene cierto.
- Coste de la doble llamada `getGsdMode` (una en `buildSessionFromTask` Task 1, otra aquí en `buildClaudeCommand`) es despreciable (~µs sobre un array de ~3 strings). Es el trade-off que documenta D-03: "prefer derivar localmente en cada módulo a threadear el modo por la firma".

**Import:** `getGsdMode` ya se importó en Task 1 (línea 5 modificada). Si Task 1 no se ejecutó antes que este, ESTE TASK debe ejecutarse en una sola unidad atómica — ambos tasks tocan el mismo archivo, idealmente se aplican en el mismo commit. (Ver `<task_ordering>` abajo.)

**No hacer:**
- NO inline el resultado en el ternario (`const cliFlags = (kodoFlags.includes('yolo') || getGsdMode(kodoFlags) !== null) ? '--dangerously-skip-permissions' : ''`). Mantener `skipPerms` como variable nombrada — sirve de documentation y facilita el debugging con un breakpoint/log.
- NO eliminar `cliFlags` ni el ternario. Otros tests pueden depender del literal exacto del flag.
- NO tocar la línea 265 (`return …`). El template literal está fino tal como está.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node -e "
import { resolveTaskAndLaunchContext } from './src/session/manager.js';
// Use module-internal access via re-import: buildClaudeCommand is private, so test via grep + node --check.
import { readFileSync } from 'node:fs';
const src = readFileSync('./src/session/manager.js', 'utf8');
const re = /const skipPerms = kodoFlags\.includes\('yolo'\) \|\| getGsdMode\(kodoFlags\) !== null;/;
console.assert(re.test(src), 'skipPerms refactor missing');
console.assert(!/kodoFlags\.includes\('gsd'\)/.test(src), 'old literal still present in skipPerms');
console.log('OK Task2 grep checks');
" --input-type=module</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "const skipPerms = kodoFlags.includes('yolo') || getGsdMode(kodoFlags) !== null;" src/session/manager.js` matches exactly one line
    - `grep -c "kodoFlags.includes('gsd')" src/session/manager.js` returns `0` (literal viejo eliminado — buildSessionFromTask también lo eliminó en Task 1)
    - `grep -F "const cliFlags = skipPerms ? '--dangerously-skip-permissions' : '';" src/session/manager.js` matches exactly one line (línea siguiente intacta)
    - `grep "Las sesiones GSD (full y quick) corren slash commands autónomos" src/session/manager.js` matches (comentario nuevo en ES presente)
    - `grep "function buildClaudeCommand(config, sessionId, task, description, modelOverride, kodoFlags = \[\], moduleName = null)" src/session/manager.js` matches (firma intacta)
    - `node --check src/session/manager.js` exits 0 (syntax OK)
    - El comando `<verify>` automated imprime `OK Task2 grep checks` y exits 0
    - Verificación dinámica via integration: `node -e "import('./src/session/manager.js').then(() => console.log('module loads'))"  --input-type=module` exits 0 (el archivo se importa sin lanzar)
    - `node --test test/manager.test.js` exits 0 (tests existentes siguen pasando)
  </acceptance_criteria>
  <done>
    `buildClaudeCommand` deriva `skipPerms` desde `getGsdMode(kodoFlags) !== null` (cualquier modo GSD activa skip-perms automáticamente) en lugar del literal `kodoFlags.includes('gsd')`. Una task `kodo:gsd-quick` ahora hereda `--dangerously-skip-permissions` por el mismo mecanismo que `kodo:gsd` (commit 004995c). `kodo:yolo` sigue funcionando idénticamente (short-circuit `||`, orden invariante `<specifics>`). El comentario en español refleja D-02 (no lista literales de label, generaliza a "cualquier modo GSD"). Un futuro modo añadido a `getGsdMode` heredará skip-perms sin tocar este archivo.
  </done>
</task>

</tasks>

<task_ordering>
Task 1 y Task 2 tocan el mismo archivo (`src/session/manager.js`) y comparten el import de `getGsdMode` (Task 1 añade el import; Task 2 lo usa). Aplicar en orden: **Task 1 antes que Task 2**. Idealmente, ambos tasks se commitean juntos (un solo commit `feat(session): persist gsd_mode and unify skipPerms via getGsdMode`) — el archivo no debe quedar en un estado intermedio donde se importe `getGsdMode` pero no se use, ni donde `getGsdMode` se use sin importarse.
</task_ordering>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persistence (Session record → state.json) | Campo `gsd_mode` se persiste en disco; readers downstream confían en el enum `'full'|'quick'`. No hay datos externos no-trusted. |
| Argv (kodoFlags → `--dangerously-skip-permissions`) | Flag CLI muy potente (skip-perms desactiva el sandbox de Claude Code). La fuente del flag es siempre el label de Plane parseado por `parseKodoLabels`, no input arbitrario del usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Elevation of Privilege | `buildClaudeCommand` skipPerms expansion to gsd-quick | accept | El contrato `kodo:gsd ⇒ skip-perms` ya existe en producción desde commit 004995c y está cubierto por el modelo de confianza Phase 8 (label de Plane = autorización del operador). Ampliar a `kodo:gsd-quick` no introduce nuevo vector — quien puede crear el label `kodo:gsd-quick` ya puede crear `kodo:gsd` (es el mismo workspace de Plane). El operador que añade el label es el mismo humano que tiene acceso al cmux/Claude. |
| T-11-04 | Tampering | `getGsdMode(kodoFlags) !== null` short-circuit con `||` | mitigate | Orden `yolo` primero, `getGsdMode` después es invariante documentada (`<specifics>` Phase 11). Un cambio que rompa el orden (e.g., `getGsdMode(...) !== null \|\| includes('yolo')`) seguiría siendo funcionalmente correcto pero rompería la trazabilidad humana del comentario. Acceptance criterion grepea el literal exacto para anclar el orden. |
| T-11-05 | Information Disclosure | Persistir `gsd_mode` en state.json | accept | El valor es un enum constante (`'full'|'quick'`), no PII. `state.json` ya contiene `task_id`, `task_ref`, `summary`, `project_path` — añadir un enum más no amplía la superficie de información sensible. |
| T-11-06 | Denial of Service | Spread condicional `gsdMode ? ... : {}` con `gsdMode = null` | mitigate | El spread `...({})` es un no-op JS estándar; nunca lanza, nunca corrompe el record. El guard `getGsdMode(flags)` con flags inválidos devuelve `null` (línea 54 de labels.js). Resultado: input malformado produce un Session record sin `gsd`/`gsd_mode`, comportamiento idéntico a una task non-GSD. |
</threat_model>

<verification>
- `node --check src/session/manager.js` exits 0
- `node --test test/manager.test.js` exits 0 (sin regresión)
- `node --test test/labels.test.js` exits 0 (Plan 01 sigue cubierto)
- `node --test test/dispatcher.test.js` exits 0 (dispatcher consume `buildSessionFromTask` indirectamente vía `launchWorkItem` mockeado; los tests existentes verifican que la firma siga compatible)
- `grep -F "...(gsdMode ? { gsd: true, gsd_mode: gsdMode } : {})" src/session/manager.js` returns 1 match
- `grep -F "const skipPerms = kodoFlags.includes('yolo') || getGsdMode(kodoFlags) !== null;" src/session/manager.js` returns 1 match
- `grep -c "kodoFlags.includes('gsd')" src/session/manager.js` returns 0 (literal viejo eliminado)
- `grep -c "flags?.includes('gsd')" src/session/manager.js` returns 0 (literal viejo eliminado)
- Suite completa `node --test` exits 0 con 0 fallos vs el estado pre-plan (las pruebas existentes deben seguir verdes; los casos quick-específicos llegan en Phase 13)
</verification>

<success_criteria>
- Una task con label `kodo:gsd-quick` produce `Session.gsd === true && Session.gsd_mode === 'quick'` al persistirse en `state.json`
- Una task con label `kodo:gsd` produce `Session.gsd === true && Session.gsd_mode === 'full'`
- Una task con ambos labels produce `gsd_mode: 'quick'` (precedencia centralizada en `getGsdMode`)
- Una task sin labels GSD produce un Session record sin `gsd` ni `gsd_mode` (campos omitidos)
- El comando claude para `kodo:gsd-quick` incluye `--dangerously-skip-permissions`
- El comando claude para `kodo:gsd` sigue incluyendo `--dangerously-skip-permissions` (no regresión)
- El comando claude para `kodo:yolo` sigue incluyendo `--dangerously-skip-permissions` (no regresión)
- La firma pública de `buildSessionFromTask` y `buildClaudeCommand` no cambia
- No quedan literales `flags?.includes('gsd')` ni `kodoFlags.includes('gsd')` en el archivo
</success_criteria>

<output>
After completion, create `.planning/phases/11-quick-mode-recognition-persistence/11-02-SUMMARY.md` con:
- Diff resumido de las 3 zonas modificadas (import línea 5, derivación + spread líneas 27-50, skipPerms líneas 259-263)
- Confirmación del comando de validación dinámica usado (e.g., `node -e ...` con los 5 casos de fixtures)
- Hand-off para Phase 12: "hooks pueden leer `session.gsd_mode` directamente desde state.json o usar `getSessionMode(session)` (Plan 01) para aplicar el fallback legacy `'full'`"
- Hand-off para Plan 03 (Wave 1 paralelo): "manager ya persiste `gsd_mode`; el dispatcher añadirá `mode` a la telemetría — los dos cierres de la cadena se observan independientemente"
</output>
