---
phase: 09-phase-resolver-bootstrap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/state.js
  - src/gsd/brief.js
  - test/gsd-brief.test.js
autonomous: true
requirements: [GSD-08]
must_haves:
  truths:
    - "`Session` typedef acepta campo opcional `brief?: string` (groundwork para D-09)"
    - "`buildBriefFromTask` renderiza el brief exacto D-10 (título + URL + descripción)"
    - "`buildBriefFromTask` degrada elegantemente cuando `task.description` está vacía (`(no description provided)`)"
    - "`buildBriefFromTask` omite la línea `**Source:**` si `task.url` está ausente"
  artifacts:
    - path: "src/session/state.js"
      provides: "Session typedef extendido con brief?: string"
      contains: "brief?: string"
    - path: "src/gsd/brief.js"
      provides: "buildBriefFromTask helper puro (D-10 format)"
      contains: "export function buildBriefFromTask"
    - path: "test/gsd-brief.test.js"
      provides: "Unit tests cubriendo los 4 casos de D-10 + D-12"
      contains: "describe('buildBriefFromTask'"
  key_links:
    - from: "src/session/state.js (Session typedef)"
      to: "src/gsd/brief.js (consumer via session.brief)"
      via: "field aditivo opcional"
      pattern: "brief\\?:\\s*string"
---

<objective>
Extender el typedef `Session` con el campo opcional `brief?: string` (groundwork para D-09 "persistir en Session record") y crear el helper puro `src/gsd/brief.js` con `buildBriefFromTask(task)` que renderiza el bloque exacto definido en CONTEXT D-10. Satisface la preparación para GSD-08 sin tocar dispatcher/hook todavía (eso vive en el plan 09-04).

Purpose: Separar el groundwork de typedef + helper puro (Wave 1, sin deps) del wiring (Wave 2, que depende del resolver). Permite que el plan 09-04 consuma tanto el typedef como el helper sin bloqueo.

Output: `src/session/state.js` con `brief?: string` añadido al Session typedef, `src/gsd/brief.js` nuevo con buildBriefFromTask + JSDoc typedef para el input, `test/gsd-brief.test.js` con tests unitarios.

**Refinamiento crítico aplicado (pattern-mapper #4):** El CONTEXT D-09 dejaba abierto si `brief` vive en el Session record o se pasa transitorio. Este plan adopta la recomendación del pattern-mapper: **persistirlo en el record**. El hook SessionStart ya lee el record vía `findSession()`; cualquier canal alternativo (env vars, temp files) añade mecanismo sin beneficio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md
@.planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md
@.planning/phases/08-gsd-label-session-plumbing/08-CONTEXT.md

<interfaces>
<!-- Session typedef actual (src/session/state.js:11-27). phase_id ya existe de Phase 8 D-11. -->
<!-- Este plan solo añade una línea más: brief?: string. -->

From src/session/state.js:
```javascript
/**
 * @typedef {{
 *   workspace_ref: string,
 *   session_id: string,
 *   task_id: string,
 *   task_ref: string,
 *   provider: string,
 *   project_id: string,
 *   summary: string,
 *   status: 'running'|'done'|'error'|'review',
 *   started_at: string,
 *   project_path: string,
 *   task_url?: string,
 *   project_name?: string,
 *   gsd?: boolean,             // Phase 8: GSD mode flag (D-10).
 *   phase_id?: string,         // Phase 9 prep (D-11): resolved phase identifier.
 * }} Session
 */
```

<!-- TaskItem (input de buildBriefFromTask): campos expuestos por plane provider -->
Available fields on TaskItem (from src/interface.js + src/providers/plane/provider.js):
- `ref: string` — e.g. "KL-42"
- `title: string`
- `url?: string` — optional
- `description?: string | null` — optional
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir `brief?: string` al typedef `Session` en `src/session/state.js`</name>
  <files>src/session/state.js</files>
  <read_first>
    - src/session/state.js (completo — necesitas ver el Session typedef actual con phase_id de Phase 8)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/session/state.js (MODIFIED — typedef only)")
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md (D-09 decisión)
  </read_first>
  <action>
Editar `src/session/state.js` y modificar ÚNICAMENTE el bloque `@typedef Session` (alrededor de la línea 26, justo después de `phase_id?: string,`). NO tocar `migrateState`, `loadState`, `saveState`, `addSession`, ni ningún otro símbolo.

**Cambio exacto — añadir UNA línea tras `phase_id?: string`:**

Antes:
```javascript
 *   gsd?: boolean,             // Phase 8: GSD mode flag (D-10). Falsy/missing == non-GSD.
 *   phase_id?: string,         // Phase 9 prep (D-11): resolved phase identifier (not populated in Phase 8).
 * }} Session
```

Después:
```javascript
 *   gsd?: boolean,             // Phase 8: GSD mode flag (D-10). Falsy/missing == non-GSD.
 *   phase_id?: string,         // Phase 9 (D-11): resolved phase identifier. Populated by dispatcher when match succeeds.
 *   brief?: string,            // Phase 9 (D-09, pattern-mapper #4): bootstrap brief rendered by buildBriefFromTask. Persisted so hook SessionStart can read it via findSession(). Only set when resolver returns action='bootstrap'.
 * }} Session
```

**Razón de NO migración:** schema v2 es flexible (campos opcionales aditivos). No requiere bump de `schema_version` ni código de migración. Sesiones existentes sin `brief` se comportan idénticamente al estado previo.

**Invariantes a preservar:**
- `schema_version` sigue siendo 2 (no bump).
- `loadState`/`saveState`/`migrateState` no se tocan.
- JSDoc JSdoc del typedef mantiene comentarios de Phase 8 (gsd, phase_id).
  </action>
  <verify>
    <automated>grep -n "brief?: string" src/session/state.js && node --check src/session/state.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "brief?: string" src/session/state.js` devuelve exactamente una coincidencia, dentro del bloque `@typedef` de Session
    - `grep -n "schema_version" src/session/state.js` sigue mostrando el valor `2` (no se bumpeó)
    - `grep -nE "phase_id\\?: string" src/session/state.js` sigue devolviendo la línea de Phase 8 (no se reemplazó)
    - `node --check src/session/state.js` sale con código 0
    - `node --test test/state.test.js test/migration.test.js` sigue pasando (no hubo cambio de runtime)
  </acceptance_criteria>
  <done>Session typedef incluye `brief?: string` como campo opcional aditivo. Ningún test existente se rompe. No se introduce migración.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear `src/gsd/brief.js` (buildBriefFromTask) + test `test/gsd-brief.test.js`</name>
  <files>src/gsd/brief.js, test/gsd-brief.test.js</files>
  <read_first>
    - src/labels.js (analog: module header + pure transform + JSDoc shape)
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md (D-10 formato exacto, D-12 edge case descripción vacía)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "Brief format (D-10)" con snippet literal)
  </read_first>
  <behavior>
    - Test 1: task con title+url+description completa produce 6 líneas: header + blank + **Task:** + **Source:** + blank + description
    - Test 2: task sin `url` omite la línea **Source:** completamente (no deja línea vacía donde iba)
    - Test 3: task con `description` null produce `(no description provided)` como cuerpo (D-12)
    - Test 4: task con `description` string vacío (`''` o solo whitespace) también produce `(no description provided)` (D-12)
    - Test 5: el resultado empieza exactamente con `## Project Brief\n` (heading H2)
    - Test 6: `**Task:** KL-42 — Build X` (formato exacto: `ref` + em-dash + `title`)
  </behavior>
  <action>
**Archivo 1: `src/gsd/brief.js`** — crear con este contenido exacto:

```javascript
// @ts-check

/**
 * Build the bootstrap brief block per CONTEXT §D-10.
 *
 * Rendered format:
 *   ## Project Brief
 *
 *   **Task:** <ref> — <title>
 *   **Source:** <url>         ← omitted if url missing
 *
 *   <description or "(no description provided)">
 *
 * D-12 fallback: when description is null, undefined, '', or whitespace-only,
 * the body becomes "(no description provided)" and the caller's event emitter
 * can flag `brief_empty: true` to surface it in gsd.bootstrap log entries.
 *
 * Pure function — no I/O, no redaction. The caller is responsible for not
 * passing untrusted descriptions into logs if they may contain secrets.
 *
 * @param {{ ref: string, title: string, url?: string, description?: string | null }} task
 * @returns {string}
 */
export function buildBriefFromTask(task) {
  const body = task.description && task.description.trim()
    ? task.description
    : '(no description provided)';

  const lines = [
    '## Project Brief',
    '',
    `**Task:** ${task.ref} — ${task.title}`,
  ];
  if (task.url) {
    lines.push(`**Source:** ${task.url}`);
  }
  lines.push('', body);

  return lines.join('\n');
}

/**
 * True when `task.description` is effectively empty (null/undefined/whitespace-only).
 * Used by the dispatcher to set `brief_empty: true` on the gsd.bootstrap event (D-12).
 *
 * @param {{ description?: string | null }} task
 * @returns {boolean}
 */
export function isBriefEmpty(task) {
  return !task.description || !task.description.trim();
}
```

**Archivo 2: `test/gsd-brief.test.js`** — crear con este contenido exacto:

```javascript
// @ts-check
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { buildBriefFromTask, isBriefEmpty } from '../src/gsd/brief.js';

describe('buildBriefFromTask', () => {
  it('renders full brief with title, url and description', () => {
    const out = buildBriefFromTask({
      ref: 'KL-42',
      title: 'Build phase resolver',
      url: 'https://plane.example.com/tasks/KL-42',
      description: 'Implements GSD-03.',
    });
    assert.equal(out, [
      '## Project Brief',
      '',
      '**Task:** KL-42 — Build phase resolver',
      '**Source:** https://plane.example.com/tasks/KL-42',
      '',
      'Implements GSD-03.',
    ].join('\n'));
  });

  it('omits **Source:** line when url is missing', () => {
    const out = buildBriefFromTask({
      ref: 'KL-1',
      title: 'No URL',
      description: 'Body text',
    });
    assert.ok(!out.includes('**Source:**'), 'Source line should be absent');
    // And the blank-line separator before body is still present once
    assert.equal(out, [
      '## Project Brief',
      '',
      '**Task:** KL-1 — No URL',
      '',
      'Body text',
    ].join('\n'));
  });

  it('uses "(no description provided)" when description is null (D-12)', () => {
    const out = buildBriefFromTask({
      ref: 'KL-2',
      title: 'Null desc',
      url: 'https://x/2',
      description: null,
    });
    assert.ok(out.endsWith('(no description provided)'));
  });

  it('uses "(no description provided)" when description is whitespace-only (D-12)', () => {
    const out = buildBriefFromTask({
      ref: 'KL-3',
      title: 'Blank desc',
      description: '   \n\t ',
    });
    assert.ok(out.endsWith('(no description provided)'));
  });

  it('uses "(no description provided)" when description is missing', () => {
    const out = buildBriefFromTask({ ref: 'KL-4', title: 'No desc' });
    assert.ok(out.endsWith('(no description provided)'));
  });

  it('starts with the H2 heading', () => {
    const out = buildBriefFromTask({ ref: 'R', title: 'T' });
    assert.ok(out.startsWith('## Project Brief\n'));
  });
});

describe('isBriefEmpty', () => {
  it('returns true for null/undefined/empty/whitespace description', () => {
    assert.equal(isBriefEmpty({ description: null }), true);
    assert.equal(isBriefEmpty({ description: undefined }), true);
    assert.equal(isBriefEmpty({ description: '' }), true);
    assert.equal(isBriefEmpty({ description: '   \n\t' }), true);
    assert.equal(isBriefEmpty({}), true);
  });

  it('returns false for non-empty description', () => {
    assert.equal(isBriefEmpty({ description: 'hello' }), false);
    assert.equal(isBriefEmpty({ description: ' x ' }), false);
  });
});
```

Convenciones obligatorias: `// @ts-check` header, `node:test` flat describe/it, `node:assert/strict`.
  </action>
  <verify>
    <automated>node --check src/gsd/brief.js && node --test test/gsd-brief.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/gsd/brief.js` existe, empieza con `// @ts-check`
    - `grep -n "export function buildBriefFromTask" src/gsd/brief.js` devuelve 1 coincidencia
    - `grep -n "export function isBriefEmpty" src/gsd/brief.js` devuelve 1 coincidencia
    - `grep -E "import.*(node:fs|node:path|'fs'|'path')" src/gsd/brief.js` devuelve 0 coincidencias (módulo puro)
    - `node --test test/gsd-brief.test.js` sale con código 0 y ejecuta ≥8 tests (6 en buildBriefFromTask + 2 en isBriefEmpty)
    - Todos los tests pasan sin fixtures de disco
  </acceptance_criteria>
  <done>`src/gsd/brief.js` creado como módulo puro con `buildBriefFromTask` + `isBriefEmpty`. Tests unitarios validan formato D-10 exacto y degradación D-12. Sin I/O.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task.description (externa Plane) → brief rendering | La descripción de la tarea puede contener texto arbitrario proveniente de Plane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | brief rendering | accept | `buildBriefFromTask` renderiza `task.description` verbatim. Si la tarea contiene secretos (tokens, credenciales), se persistirán en `session.brief` y en el NDJSON de la sesión. Mitigación ya existente: `src/logger.js` aplica redactor de secretos conocidos (Phase 6) antes de cualquier escritura. El brief como string opaco no escapa de ese canal. CLI `gsd inspect` previene fuga adicional porque el preview se imprime solo en TTY local. |
| T-09-02-02 | Tampering | Session typedef extension | mitigate | Campo `brief?` es opcional y aditivo. `loadState`/`saveState` no validan campos desconocidos — cualquier valor inyectado en state.json es reemplazado la próxima vez que dispatcher escribe el record. No hay prototype pollution: el typedef es doc-only (JSDoc). |
| T-09-02-03 | Denial of Service | brief length explosion | accept | `task.description` sin límite duro. Plane impone límites razonables (~5000 chars). Si se detecta un caso patológico, añadir truncate en dispatcher (fuera de alcance de este plan). |
</threat_model>

<verification>
- `grep -n "brief?: string" src/session/state.js` ≥ 1 coincidencia en el typedef
- `node --test test/gsd-brief.test.js` pasa con 0 fallos
- `node --test test/state.test.js test/migration.test.js` sigue verde (no se rompió nada en state.js)
- `grep -E "import.*(node:fs|node:path)" src/gsd/brief.js` devuelve 0 (pureza preservada)
</verification>

<success_criteria>
- `Session` typedef en `src/session/state.js` incluye `brief?: string` (una línea aditiva)
- `src/gsd/brief.js` existe con `buildBriefFromTask` (formato D-10 exacto) + `isBriefEmpty`
- `test/gsd-brief.test.js` con ≥8 tests cubriendo: brief completo, sin URL, descripción null/empty/whitespace, H2 heading, isBriefEmpty
- Ningún test anterior se rompe (state.test.js, migration.test.js siguen pasando)
- Sin migration bump, sin cambios de runtime en state.js
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-resolver-bootstrap/09-02-SUMMARY.md`
</output>
