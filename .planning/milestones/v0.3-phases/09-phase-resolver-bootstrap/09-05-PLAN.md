---
phase: 09-phase-resolver-bootstrap
plan: 05
type: execute
wave: 3
depends_on: [09-04]
files_modified:
  - src/cli/gsd-inspect.js
  - src/cli.js
  - test/gsd-inspect-cli.test.js
autonomous: true
requirements: [GSD-02, GSD-03, GSD-08, GSD-09]
must_haves:
  truths:
    - "`kodo gsd inspect <task-id>` imprime el verdict humano del resolver SIN adquirir lock, tocar state, ni cmux (D-18 dry-run estricto)"
    - "`kodo gsd inspect <task-id> --json` emite JSON estructurado (verdict + metadata: task ref, project_path, brief)"
    - "Exit code: 0 si verdict.action ∈ {'phase', 'bootstrap'}; 1 si verdict.action === 'error' (D-19)"
    - "El inspect CLI usa la MISMA `resolvePhase` que el dispatcher (D-04 — una sola fuente de verdad)"
    - "El output humano muestra 4 secciones: task resolution, .planning presence, verdict, preview de buildGsdContext"
    - "`kodo gsd` (subcommand group) está registrado en cli.js siguiendo el patrón de `kodo logs` (Phase 7)"
  artifacts:
    - path: "src/cli/gsd-inspect.js"
      provides: "runGsdInspect({taskId, json}, deps?) — action handler con DI opcional"
      contains: "export async function runGsdInspect"
    - path: "src/cli.js"
      provides: "`program.command('gsd')` group + `.command('inspect <task-id>')` subcommand"
      contains: ".command('inspect <task-id>')"
    - path: "test/gsd-inspect-cli.test.js"
      provides: "Tests DI cubriendo los 3 action types + --json + exit codes + D-18 invariant"
      contains: "describe('runGsdInspect'"
  key_links:
    - from: "src/cli.js (inspect action)"
      to: "src/cli/gsd-inspect.js (runGsdInspect)"
      via: "dynamic import dentro del .action()"
      pattern: "import\\('./cli/gsd-inspect\\.js'\\)"
    - from: "src/cli/gsd-inspect.js"
      to: "src/gsd/resolver.js (resolvePhase)"
      via: "MISMA resolvePhase que dispatcher (D-04)"
      pattern: "resolvePhase"
    - from: "src/cli/gsd-inspect.js"
      to: "D-18 invariant"
      via: "NO imports de acquireGsdLock/releaseGsdLock/addSession/cmux"
      pattern: "NO_IMPORT (acquireGsdLock|addSession|cmux)"
---

<objective>
Implementar `kodo gsd inspect <task-id>` como subcomando read-only que ejecuta la misma `resolvePhase` que el dispatcher y reporta el verdict en formato humano o JSON. Satisface los 4 criterios de éxito del ROADMAP Phase 9 consolidados: reporta qué haría el resolver para una task sin arrancar sesión, expone el preview del brief/contexto GSD, y sirve como herramienta forense para debugging de dispatches fallidos.

Purpose: `kodo gsd inspect` es la interfaz humana para los 4 requirements de Phase 9 en un solo comando. Un operador puede validar GSD-02 (detecta bootstrap), GSD-03 (match 1:1), GSD-08 (brief rendering) y GSD-09 (inferencia de phase_id) sin tocar Plane ni el estado local.

**Refinamiento crítico aplicado (pattern-mapper #5):** El CONTEXT decía "inline en cli.js O archivo dedicado". Este plan adopta la recomendación del pattern-mapper: **archivo dedicado `src/cli/gsd-inspect.js`**. Razón: cli.js ya tiene 469 líneas, e inline la lógica de inspect (≈100 líneas con render humano + JSON + preview de buildGsdContext) lo vuelve mucho más difícil de mantener. Phase 7 `kodo logs` ya demuestra que cli.js es thin registrar y el handler vive en su propio módulo.

Output: archivo nuevo `src/cli/gsd-inspect.js` (~100 líneas), edición de `src/cli.js` para registrar el subcommand group + inspect, y `test/gsd-inspect-cli.test.js` con tests DI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md
@.planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md
@.planning/phases/09-phase-resolver-bootstrap/09-03-SUMMARY.md
@.planning/phases/09-phase-resolver-bootstrap/09-04-SUMMARY.md

<interfaces>
<!-- Todo lo que consume el inspect ya existe tras los planes anteriores -->

From src/gsd/resolver.js (09-03):
```javascript
export function resolvePhase({ projectPath, task }): ResolveResult
// Same function the dispatcher calls — D-04 invariant.
```

From src/gsd/brief.js (09-02):
```javascript
export function buildBriefFromTask(task): string
```

From src/hooks/session-start.js (09-04 extended):
```javascript
export function buildGsdContext(session, opts?: { brief?: string }): string
```

From src/session/manager.js (09-04 extended):
```javascript
export function resolveProjectPath(task, projects): string
```

From src/providers/registry.js:
```javascript
export function getProvider(name?: string): TaskProvider
export async function initRegistry(): Promise<void>
```

From src/config.js:
```javascript
export function loadConfig(): Config
export function loadProjects(): Record<string, string | { default?: string, modules?: Record<string,string> }>
```

<!-- Patrón a copiar: cli.js:213-239 — subcommand logs de Phase 7 -->
From src/cli.js:213-239:
```javascript
program
  .command('logs [session-id]')
  .description('Inspect a session log (dump, tail, filter)')
  .option('--json', 'Emit raw NDJSON (pipe-friendly)')
  .action(async (sessionId, opts) => {
    try {
      const { runLogs } = await import('./logs/reader.js');
      await runLogs({ sessionId, json: opts.json || false, /* ... */ });
    } catch (err) {
      console.error(`Error: ${err.message}`);
      process.exit(1);
    }
  });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear `src/cli/gsd-inspect.js` con `runGsdInspect(opts, deps?)`</name>
  <files>src/cli/gsd-inspect.js</files>
  <read_first>
    - src/logs/reader.js (analog: CLI action handler con signature `runLogs(opts)`)
    - src/gsd/resolver.js (para saber la shape del ResolveResult)
    - src/gsd/brief.js (para buildBriefFromTask)
    - src/hooks/session-start.js (para buildGsdContext — exportado desde ahí)
    - src/session/manager.js (para resolveProjectPath)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/cli/gsd-inspect.js")
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md (D-16 a D-19)
  </read_first>
  <action>
Crear primero el directorio si no existe: `mkdir -p src/cli`.

Luego crear `src/cli/gsd-inspect.js` con este contenido exacto:

```javascript
// @ts-check
//
// src/cli/gsd-inspect.js — Action handler de `kodo gsd inspect <task-id>`.
//
// Responsabilidades (CONTEXT §D-16..D-19):
//   1. Resolver task via provider (igual shape que dispatcher pero sin launch).
//   2. Resolver projectPath via resolveProjectPath.
//   3. Llamar resolvePhase() — MISMA función que el dispatcher (D-04 invariant).
//   4. Renderizar preview de buildGsdContext con session sintético.
//   5. Emitir human-readable (default) o JSON (--json, D-17).
//   6. Exit code 0 si phase|bootstrap, 1 si error (D-19).
//
// Dry-run estricto (D-18): NO lock, NO state, NO cmux. Pure read-only.
// Esta invariante está protegida por un test dedicado en test/gsd-inspect-cli.test.js
// que aserta que `acquireGsdLockFn`, `addSession`, y cualquier llamada a cmux
// nunca son invocadas durante una ejecución de runGsdInspect.

import { existsSync } from 'node:fs';
import { join } from 'node:path';
import { resolvePhase } from '../gsd/resolver.js';
import { buildBriefFromTask } from '../gsd/brief.js';
import { buildGsdContext } from '../hooks/session-start.js';
import { resolveProjectPath } from '../session/manager.js';
import { loadProjects } from '../config.js';
import { initRegistry, getProvider } from '../providers/registry.js';

/**
 * @typedef {{ taskId: string, json?: boolean }} RunGsdInspectOpts
 *
 * @typedef {{
 *   getProviderFn?: () => import('../interface.js').TaskProvider,
 *   resolveProjectPathFn?: (task: any) => string,
 *   resolvePhaseFn?: (params: { projectPath: string, task: any }) => import('../gsd/resolver.js').ResolveResult,
 *   writeFn?: (s: string) => void,
 *   errFn?: (s: string) => void,
 * }} RunGsdInspectDeps
 */

/**
 * Dry-run of the GSD phase resolver for a single task. Read-only.
 *
 * @param {RunGsdInspectOpts} opts
 * @param {RunGsdInspectDeps} [deps]
 * @returns {Promise<number>} exit code (0 success, 1 resolver error, 2 fetch failure)
 */
export async function runGsdInspect(opts, deps = {}) {
  const write = deps.writeFn || ((s) => process.stdout.write(s));
  const err = deps.errFn || ((s) => process.stderr.write(s));

  let getProviderFn = deps.getProviderFn;
  if (!getProviderFn) {
    await initRegistry();
    getProviderFn = () => getProvider();
  }
  const resolveProjectPathFn = deps.resolveProjectPathFn
    || ((task) => resolveProjectPath(task, loadProjects()));
  const resolvePhaseFn = deps.resolvePhaseFn || resolvePhase;

  // 1. Fetch task — best-effort; on fetch failure, exit 2 with message.
  let task;
  try {
    const provider = getProviderFn();
    task = await provider.getTask(opts.taskId);
  } catch (e) {
    err(`Error fetching task ${opts.taskId}: ${/** @type {Error} */ (e).message}\n`);
    return 2;
  }

  // 2. Resolve project path (may throw if task's project has no mapping).
  let projectPath;
  try {
    projectPath = resolveProjectPathFn(task);
  } catch (e) {
    err(`Error resolving project path: ${/** @type {Error} */ (e).message}\n`);
    return 2;
  }

  // 3. Call resolver — MISMA función que el dispatcher (D-04).
  const verdict = resolvePhaseFn({ projectPath, task });

  // 4. Detect presence of .planning/PROJECT.md for the human report section 2.
  const hasPlanning = existsSync(join(projectPath, '.planning', 'PROJECT.md'));

  // 5. Build the brief (only meaningful for bootstrap verdicts, but we always
  //    compute it so JSON mode can include it verbatim for operator inspection).
  const brief = verdict.action === 'bootstrap' ? buildBriefFromTask(task) : null;

  // 6. Render: --json (D-17) or human (D-16).
  if (opts.json) {
    write(JSON.stringify({
      task: { ref: task.ref, title: task.title, labels: task.labels },
      project_path: projectPath,
      has_planning_dir: hasPlanning,
      verdict,
      brief,
    }, null, 2) + '\n');
  } else {
    renderHuman({ task, projectPath, hasPlanning, verdict, brief, write });
  }

  // 7. Exit code (D-19).
  return verdict.action === 'error' ? 1 : 0;
}

/**
 * Render the human-readable 4-section report per D-16.
 * @private
 */
function renderHuman({ task, projectPath, hasPlanning, verdict, brief, write }) {
  // Section 1: task resolution
  write(`Task:         ${task.ref} — ${task.title}\n`);
  write(`Labels:       [${(task.labels || []).join(', ')}]\n`);
  write(`Project path: ${projectPath}\n\n`);

  // Section 2: .planning/PROJECT.md presence
  write(`.planning/PROJECT.md: ${hasPlanning ? 'present' : 'MISSING'}\n\n`);

  // Section 3: verdict — exhaustive switch per D-02
  write('Verdict:\n');
  switch (verdict.action) {
    case 'phase':
      write(`  action:        phase\n`);
      write(`  phase_id:      ${verdict.phase_id}\n`);
      write(`  match_heading: ${verdict.match_heading}\n`);
      write(`  match_reason:  ${verdict.match_reason}\n`);
      break;
    case 'bootstrap':
      write(`  action:        bootstrap (${verdict.reason})\n`);
      break;
    case 'error':
      write(`  action:        error\n`);
      write(`  code:          ${verdict.code}\n`);
      if (verdict.detail) write(`  detail:        ${verdict.detail}\n`);
      if (verdict.matches) write(`  matches:       ${verdict.matches.join(', ')}\n`);
      break;
  }
  write('\n');

  // Section 4: preview of buildGsdContext with a synthetic session
  const syntheticSession = /** @type {any} */ ({
    task_ref: task.ref,
    summary: task.title,
    project_path: projectPath,
    session_id: '<dry-run-preview>',
    task_id: task.id,
    project_id: task.projectId,
    gsd: true,
    // Only include phase_id when the verdict matched a phase.
    ...(verdict.action === 'phase' ? { phase_id: verdict.phase_id } : {}),
  });
  const preview = buildGsdContext(syntheticSession, { brief });

  write('─── buildGsdContext preview ───\n');
  write(preview);
  if (!preview.endsWith('\n')) write('\n');
  write('───────────────────────────────\n');
}
```

**Invariantes a preservar (D-18 strict dry-run):**
- NO importar `acquireGsdLock` / `releaseGsdLock` (grep debe devolver 0).
- NO importar `addSession` / `updateSession` / `removeSession` de state.js (grep debe devolver 0).
- NO importar nada de `cmux/` (grep debe devolver 0).
- NO llamar `launchWorkItem` ni `dispatchTrigger`.
- Handler es `async` — usa await para `initRegistry()` + `provider.getTask()`.

**Correctness:** el `syntheticSession` pasa `gsd: true` y (solo en el caso phase) `phase_id`. Sin phase_id → rama bootstrap de `buildGsdContext` se activa, renderizando el brief primero (D-11) — que es exactamente el preview que el operador necesita ver.
  </action>
  <verify>
    <automated>node --check src/cli/gsd-inspect.js && ! grep -E "import.*(acquireGsdLock|releaseGsdLock|addSession|updateSession|cmux/client)" src/cli/gsd-inspect.js</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `src/cli/gsd-inspect.js` existe con `// @ts-check` en línea 1
    - `grep -n "export async function runGsdInspect" src/cli/gsd-inspect.js` devuelve 1 coincidencia
    - `grep -nE "import.*resolvePhase.*'\\.\\./gsd/resolver\\.js'" src/cli/gsd-inspect.js` devuelve 1 coincidencia (D-04 invariant)
    - `grep -nE "import.*(acquireGsdLock|releaseGsdLock|addSession|updateSession|removeSession)" src/cli/gsd-inspect.js` devuelve 0 coincidencias (D-18 invariant)
    - `grep -nE "import.*cmux" src/cli/gsd-inspect.js` devuelve 0 coincidencias (D-18 invariant)
    - `grep -n "verdict.action === 'error'" src/cli/gsd-inspect.js` devuelve 1 coincidencia (exit code logic per D-19)
    - `grep -n "opts.json" src/cli/gsd-inspect.js` devuelve ≥1 coincidencia (formato JSON)
    - `node --check src/cli/gsd-inspect.js` sale con código 0
  </acceptance_criteria>
  <done>`src/cli/gsd-inspect.js` implementa runGsdInspect con DI opcional (testeable sin red/fs), llama MISMA resolvePhase que dispatcher, render humano 4 secciones + JSON mode + exit codes 0/1/2. D-18 invariant: cero imports de lock/state/cmux.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar `kodo gsd inspect` en `src/cli.js`</name>
  <files>src/cli.js</files>
  <read_first>
    - src/cli.js (líneas 210-242 — el subcommand `kodo logs` como analog exacto)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/cli.js (MODIFIED)")
  </read_first>
  <action>
Añadir al final de la sección de subcomandos de `src/cli.js` (inmediatamente después del bloque `// --- kodo logs ---` que termina en la línea ~239, ANTES de `program.parse();`), este nuevo bloque:

```javascript
// --- kodo gsd <subcommand> ---
const gsd = program.command('gsd').description('GSD subcommands (inspect resolver, etc.)');

gsd
  .command('inspect <task-id>')
  .description('Dry-run the phase resolver for a task (read-only, no lock/state/cmux)')
  .option('--json', 'Emit structured verdict as JSON (scriptable)')
  .action(async (taskId, opts) => {
    try {
      await ensureConfig();
      const { runGsdInspect } = await import('./cli/gsd-inspect.js');
      const code = await runGsdInspect({ taskId, json: opts.json || false });
      process.exit(code);
    } catch (err) {
      console.error(`Error: ${err.message}`);
      process.exit(1);
    }
  });
```

**Ubicación exacta:** justo antes de la línea `program.parse();` (≈ línea 241 actual). El bloque debe estar DESPUÉS de `// --- kodo logs ---` y ANTES de `program.parse();`.

**Consistencia con patrón de Phase 7:**
- Dynamic import del handler (performance — cli.js no arrastra el resolver en cold start).
- try/catch con `console.error` + `process.exit(1)` para fallos inesperados.
- `ensureConfig()` antes de invocar el handler (el provider.getTask requiere config cargada).

**Invariantes:** NO tocar ningún otro subcomando existente. NO añadir flags extra al `kodo gsd inspect` (solo `--json` por D-17). NO añadir un alias.
  </action>
  <verify>
    <automated>grep -n ".command('gsd')" src/cli.js && grep -n ".command('inspect <task-id>')" src/cli.js && node --check src/cli.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n ".command('gsd')" src/cli.js` devuelve 1 coincidencia
    - `grep -n ".command('inspect <task-id>')" src/cli.js` devuelve 1 coincidencia
    - `grep -n "import('./cli/gsd-inspect.js')" src/cli.js` devuelve 1 coincidencia (dynamic import)
    - `grep -n "runGsdInspect" src/cli.js` devuelve 1 coincidencia
    - `grep -n "await ensureConfig()" src/cli.js` devuelve ≥2 coincidencias (una original + la añadida en el inspect action)
    - `node --check src/cli.js` sale con código 0
    - `node bin/kodo gsd --help 2>&1 | grep -q "inspect"` (o equivalente — el subcomando es descubrible por commander)
    - La línea `program.parse();` sigue siendo la última antes de los helpers — no se movió
  </acceptance_criteria>
  <done>Subcomando `kodo gsd inspect <task-id>` registrado con flag `--json`. Dynamic import del handler. Sigue el patrón literal de `kodo logs`. Commander lo descubre en `--help`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear `test/gsd-inspect-cli.test.js` con tests DI cubriendo D-18 invariant</name>
  <files>test/gsd-inspect-cli.test.js</files>
  <read_first>
    - test/dispatcher.test.js (para el patrón de DI con deps objeto — líneas ~300-394)
    - src/cli/gsd-inspect.js (recién creado — para confirmar la signature de deps)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "test/gsd-inspect-cli.test.js")
  </read_first>
  <action>
Crear `test/gsd-inspect-cli.test.js` con este contenido:

```javascript
// @ts-check
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { runGsdInspect } from '../src/cli/gsd-inspect.js';

/**
 * Build a StdoutStub that captures written strings.
 */
function makeStdoutStub() {
  let buf = '';
  return {
    write: (s) => { buf += s; },
    get: () => buf,
  };
}

const taskPhaseMatch = {
  id: 'task-1',
  ref: 'KL-42',
  title: 'Phase Resolver + Bootstrap',
  labels: ['kodo', 'kodo:gsd'],
  url: 'https://plane.example.com/KL-42',
  description: 'Implements GSD-03.',
  projectId: 'proj-1',
};

const taskBootstrap = {
  id: 'task-2',
  ref: 'KL-1',
  title: 'Bootstrap me',
  labels: ['kodo', 'kodo:gsd'],
  description: 'Fresh project brief',
  projectId: 'proj-1',
};

const taskNoMatch = {
  id: 'task-3',
  ref: 'KL-99',
  title: 'No such phase exists',
  labels: ['kodo', 'kodo:gsd'],
  description: null,
  projectId: 'proj-1',
};

describe('runGsdInspect — read-only dry-run (D-18 invariant)', () => {
  it('exits 0 and prints phase_id when verdict is phase (human mode)', async () => {
    const stdout = makeStdoutStub();
    const stderr = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({ init: async () => {}, getTask: async () => taskPhaseMatch }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({
        action: 'phase',
        phase_id: '9',
        match_heading: '### Phase 9: Phase Resolver + Bootstrap',
        match_reason: 'exact title match (normalized)',
      }),
      writeFn: stdout.write,
      errFn: stderr.write,
    };
    const code = await runGsdInspect({ taskId: 'KL-42' }, deps);
    assert.equal(code, 0);
    const out = stdout.get();
    assert.ok(out.includes('Task:         KL-42'), 'task line present');
    assert.ok(out.includes('phase_id:      9'), 'phase_id present');
    assert.ok(out.includes('match_heading:'), 'match_heading present');
    assert.ok(out.includes('buildGsdContext preview'), 'preview section present');
    assert.ok(out.includes('/gsd-plan-phase 9'), 'preview includes phase command');
  });

  it('exits 0 and prints bootstrap verdict with brief preview', async () => {
    const stdout = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({ init: async () => {}, getTask: async () => taskBootstrap }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({ action: 'bootstrap', reason: 'no-planning-dir' }),
      writeFn: stdout.write,
      errFn: () => {},
    };
    const code = await runGsdInspect({ taskId: 'KL-1' }, deps);
    assert.equal(code, 0);
    const out = stdout.get();
    assert.ok(out.includes('action:        bootstrap'), 'bootstrap action printed');
    assert.ok(out.includes('## Project Brief'), 'brief heading rendered in preview');
    assert.ok(out.includes('**Task:** KL-1 — Bootstrap me'), 'brief task line rendered');
    assert.ok(out.includes('/gsd-new-project'), 'bootstrap command in preview');
  });

  it('exits 1 when verdict is error (no-match)', async () => {
    const stdout = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({ init: async () => {}, getTask: async () => taskNoMatch }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({ action: 'error', code: 'no-match' }),
      writeFn: stdout.write,
      errFn: () => {},
    };
    const code = await runGsdInspect({ taskId: 'KL-99' }, deps);
    assert.equal(code, 1);
    const out = stdout.get();
    assert.ok(out.includes('action:        error'));
    assert.ok(out.includes('code:          no-match'));
  });

  it('exits 1 on multi-match with the list of matches', async () => {
    const stdout = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({ init: async () => {}, getTask: async () => taskNoMatch }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({
        action: 'error',
        code: 'multi-match',
        matches: ['Phase 1: Foo', 'Phase 2: Foo'],
      }),
      writeFn: stdout.write,
      errFn: () => {},
    };
    const code = await runGsdInspect({ taskId: 'KL-99' }, deps);
    assert.equal(code, 1);
    const out = stdout.get();
    assert.ok(out.includes('code:          multi-match'));
    assert.ok(out.includes('Phase 1: Foo, Phase 2: Foo'), 'matches list printed');
  });

  it('--json emits structured verdict + metadata (D-17)', async () => {
    const stdout = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({ init: async () => {}, getTask: async () => taskPhaseMatch }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({ action: 'phase', phase_id: '9', match_heading: 'x', match_reason: 'y' }),
      writeFn: stdout.write,
      errFn: () => {},
    };
    const code = await runGsdInspect({ taskId: 'KL-42', json: true }, deps);
    assert.equal(code, 0);
    const json = JSON.parse(stdout.get());
    assert.equal(json.task.ref, 'KL-42');
    assert.equal(json.project_path, '/tmp/fake-project');
    assert.equal(json.verdict.action, 'phase');
    assert.equal(json.verdict.phase_id, '9');
    // brief is null for phase verdicts (only rendered on bootstrap)
    assert.equal(json.brief, null);
    // Output must NOT contain the human-readable header lines
    assert.ok(!stdout.get().includes('Task:         '), 'human header should be absent in JSON mode');
  });

  it('--json emits non-null brief when verdict is bootstrap', async () => {
    const stdout = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({ init: async () => {}, getTask: async () => taskBootstrap }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({ action: 'bootstrap', reason: 'no-planning-dir' }),
      writeFn: stdout.write,
      errFn: () => {},
    };
    await runGsdInspect({ taskId: 'KL-1', json: true }, deps);
    const json = JSON.parse(stdout.get());
    assert.equal(json.verdict.action, 'bootstrap');
    assert.ok(json.brief && json.brief.startsWith('## Project Brief'));
  });

  it('exits 2 on provider fetch failure (task not found)', async () => {
    const stdout = makeStdoutStub();
    const stderr = makeStdoutStub();
    const deps = {
      getProviderFn: () => ({
        init: async () => {},
        getTask: async () => { throw new Error('Task KL-0 not found'); },
      }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      resolvePhaseFn: () => ({ action: 'phase', phase_id: '1', match_heading: 'x', match_reason: 'y' }),
      writeFn: stdout.write,
      errFn: stderr.write,
    };
    const code = await runGsdInspect({ taskId: 'KL-0' }, deps);
    assert.equal(code, 2);
    assert.ok(stderr.get().includes('Task KL-0 not found'));
  });

  it('D-18 invariant: never invokes acquireGsdLock, addSession, or cmux', async () => {
    // This is the ANTI-REGRESSION test for D-18 (dry-run strict).
    // We inspect the imports of gsd-inspect.js — if anyone adds an import of
    // state mutation or cmux, this test fails.
    const { readFileSync } = await import('node:fs');
    const src = readFileSync(new URL('../src/cli/gsd-inspect.js', import.meta.url), 'utf-8');
    assert.ok(!/import.*acquireGsdLock/.test(src), 'must not import acquireGsdLock');
    assert.ok(!/import.*releaseGsdLock/.test(src), 'must not import releaseGsdLock');
    assert.ok(!/import.*addSession/.test(src), 'must not import addSession');
    assert.ok(!/import.*removeSession/.test(src), 'must not import removeSession');
    assert.ok(!/import.*updateSession/.test(src), 'must not import updateSession');
    assert.ok(!/import.*cmux/.test(src), 'must not import anything from cmux/');
    assert.ok(!/launchWorkItem/.test(src), 'must not reference launchWorkItem');
  });

  it('D-04 invariant: imports the SAME resolvePhase used by the dispatcher', async () => {
    // runGsdInspect MUST import resolvePhase from src/gsd/resolver.js (not an
    // inline copy, not a different module). This is a static assertion on the
    // inspect handler's source.
    const { readFileSync } = await import('node:fs');
    const src = readFileSync(new URL('../src/cli/gsd-inspect.js', import.meta.url), 'utf-8');
    assert.match(src, /import\s*\{\s*resolvePhase\s*\}\s*from\s*['"]\.\.\/gsd\/resolver\.js['"]/);
  });
});
```

Estos tests usan DI total: no tocan red (provider mockado), no tocan fs real (projectPath mockado, resolver mockado), no invocan initRegistry real. Los 2 últimos tests son assertions estáticas sobre el archivo fuente — son anti-regresión para los invariantes D-18 y D-04.
  </action>
  <verify>
    <automated>node --test test/gsd-inspect-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/gsd-inspect-cli.test.js` existe
    - `node --test test/gsd-inspect-cli.test.js` sale con código 0 y ejecuta ≥9 tests
    - Cada uno de los 3 action types (phase, bootstrap, error) tiene al menos un test
    - Hay un test explícito para `--json` mode (D-17)
    - Hay un test explícito para `code=2` (provider fetch failure)
    - Hay un test ANTI-REGRESIÓN "D-18 invariant" que grep-ea el source y falla si alguien añade import de lock/state/cmux
    - Hay un test ANTI-REGRESIÓN "D-04 invariant" que asserta que `resolvePhase` se importa de `../gsd/resolver.js`
    - Todos los tests son DI-puros (cero tocar disco real o red)
  </acceptance_criteria>
  <done>Tests cubren 9+ casos: phase match, bootstrap, no-match, multi-match, --json phase, --json bootstrap, fetch failure (exit 2), y 2 invariantes estáticos (D-18 no-side-effects, D-04 misma-resolvePhase). Todos pasan sin red/fs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taskId (CLI arg) → provider.getTask | argumento de línea de comandos no validado — pasa directo al provider |
| task.title/description → stdout render | contenido externo se escribe a la TTY del operador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information Disclosure | stdout render of task.description in preview | accept | El preview incluye `buildGsdContext` con el brief renderizado, que a su vez puede contener `task.description`. Si la descripción tiene secretos, se imprimen en la TTY local del operador. Mitigación: `kodo gsd inspect` es un comando CLI local invocado deliberadamente por el operador; no se escribe a log files a menos que él lo redirija. D-18 garantiza que NO se escribe al NDJSON de ninguna sesión. Si el operador redirige a file, es responsabilidad del operador. |
| T-09-05-02 | Command Injection | taskId como arg de CLI | mitigate | `taskId` pasa a `provider.getTask()` que internamente hace llamada HTTP a Plane con `taskId` como parte del path URL. Plane client normaliza el ref (ver `src/providers/plane/client.js`). No se ejecuta shell con `taskId`. No se construye regex con `taskId`. Superficie mínima. |
| T-09-05-03 | Denial of Service | provider.getTask failure | mitigate | El handler captura el throw y devuelve exit code 2 con mensaje a stderr. No loop, no retry infinito. |
| T-09-05-04 | Tampering | synthetic session in preview | mitigate | `syntheticSession` es un object literal local; no se persiste, no se pasa a addSession, no sale del scope de `runGsdInspect`. Cualquier inyección en el preview queda contenida en el render. |
| T-09-05-05 | Elevation of Privilege | D-18 violation risk | mitigate | Test anti-regresión `'D-18 invariant'` aserta que el source de `gsd-inspect.js` no importa funciones de estado ni cmux. Cualquier PR futuro que añada side effects romperá ese test. |
</threat_model>

<verification>
- `node --test test/gsd-inspect-cli.test.js` pasa todos los tests
- `node --test` con toda la suite (`node --test test/*.test.js`) pasa sin regresiones
- `grep -E "(acquireGsdLock|releaseGsdLock|addSession|cmux/)" src/cli/gsd-inspect.js` devuelve 0 líneas (D-18)
- `grep -nE "import\\s*\\{\\s*resolvePhase\\s*\\}\\s*from\\s*['\"]\\.\\./gsd/resolver\\.js['\"]" src/cli/gsd-inspect.js` devuelve 1 coincidencia (D-04)
- `node bin/kodo gsd --help 2>&1 | grep -q inspect` (subcomando descubrible)
- `node --check src/cli/gsd-inspect.js src/cli.js` ambos salen con código 0
</verification>

<success_criteria>
- `kodo gsd inspect <task-id>` imprime reporte humano de 4 secciones (task, .planning presence, verdict, preview)
- `kodo gsd inspect <task-id> --json` emite JSON scriptable con task + project_path + has_planning_dir + verdict + brief
- Exit codes respetados: 0 para phase/bootstrap, 1 para error, 2 para fetch failure
- D-04 invariant: el inspect usa la MISMA `resolvePhase` que el dispatcher (test estático lo protege)
- D-18 invariant: el inspect es dry-run puro — NO lock, NO state, NO cmux (test estático lo protege)
- Subcommand descubrible via `kodo gsd --help`
- Los 4 requirements de Phase 9 (GSD-02, GSD-03, GSD-08, GSD-09) tienen cobertura forense end-to-end vía este comando
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-resolver-bootstrap/09-05-SUMMARY.md`
</output>
