---
phase: 09-phase-resolver-bootstrap
plan: 04
type: execute
wave: 2
depends_on: [09-02, 09-03]
files_modified:
  - src/triggers/dispatcher.js
  - src/session/manager.js
  - src/hooks/session-start.js
  - test/dispatcher.test.js
  - test/gsd-context.test.js
  - test/session-start.test.js
autonomous: true
requirements: [GSD-02, GSD-08, GSD-09]
must_haves:
  truths:
    - "Para una task GSD con `.planning/PROJECT.md` ausente, el dispatcher resuelve `{ action: 'bootstrap' }`, thread-ea `brief` al launchOpts, y launch procede (no libera lock)"
    - "Para una task GSD con match de fase, el dispatcher thread-ea `phase_id` al launchOpts y launch procede"
    - "Para una task GSD con resolver error (`no-match`/`multi-match`/`roadmap-missing`), el dispatcher libera el GSD lock y retorna `{ action: 'resolver_failed', code, detail }`"
    - "El resolver corre ANTES del session-already-active guard (pattern-mapper #2: stale relaunches también reciben phase_id threaded)"
    - "`buildSessionFromTask` persiste `phase_id` y `brief` en el Session record cuando vienen en params (D-09 persistente)"
    - "`buildGsdContext(session)` renderiza el brief del record (si presente) ANTES de los comandos (D-11 order)"
    - "El hook `session-start.js` YA NO emite `gsd.phase.resolved` (pattern-mapper #3: migrado al dispatcher, evita duplicados)"
    - "El hook sigue emitiendo `gsd.bootstrap` SOLO cuando `session.gsd && !session.phase_id`"
    - "El evento `gsd.phase.resolved` se emite desde el dispatcher en ambos outcomes: `matched: true, phase_id` O `matched: false, error_code, detail`"
  artifacts:
    - path: "src/triggers/dispatcher.js"
      provides: "Guard chain: lock → resolver → session-active (pattern-mapper #2 order)"
      contains: "action: 'resolver_failed'"
    - path: "src/session/manager.js"
      provides: "buildSessionFromTask acepta phaseId + brief; launchWorkItem threads opts.phase_id/opts.brief"
      contains: "phaseId ? { phase_id: phaseId } : {}"
    - path: "src/hooks/session-start.js"
      provides: "buildGsdContext(session, {brief}) render + hook cleanup (ya no emite gsd.phase.resolved)"
      contains: "buildGsdContext"
  key_links:
    - from: "dispatcher.js (resolvePhaseFn call)"
      to: "src/gsd/resolver.js (resolvePhase)"
      via: "DI con default fallback a import directo"
      pattern: "resolvePhaseFn.*resolvePhase"
    - from: "dispatcher.js (launchOpts)"
      to: "manager.js (launchWorkItem)"
      via: "threading de phase_id + brief via opts"
      pattern: "opts\\.(phase_id|brief)"
    - from: "manager.js (buildSessionFromTask)"
      to: "state.js (Session record)"
      via: "conditional spread de campos opcionales"
      pattern: "phaseId \\? \\{ phase_id: phaseId \\} : \\{\\}"
    - from: "session-start.js (buildGsdContext)"
      to: "session record brief field"
      via: "session.brief persistido leído vía findSession"
      pattern: "session\\.brief"
---

<objective>
Cablear el resolver en el dispatcher, thread-ear `phase_id` + `brief` desde el dispatcher hasta el Session record vía `launchWorkItem` → `buildSessionFromTask`, extender `buildGsdContext` para renderizar el brief antes de los comandos, y eliminar la emisión duplicada de `gsd.phase.resolved` del hook.

Purpose: Este es el plan que hace que todo funcione end-to-end. Satisface GSD-02 (bootstrap trigger), GSD-08 (brief inyectado como contexto inicial) y GSD-09 (inferencia automática de phase_id).

**Refinamientos críticos aplicados:**
- **Pattern-mapper #2 (guard order):** El resolver se ejecuta INMEDIATAMENTE después del GSD lock acquisition y ANTES del session-already-active guard. Razón: stale relaunches (dispatcher.js:148-172) también necesitan phase_id+brief threaded — si el resolver vive después de ese check, las relaunches no los reciben.
- **Pattern-mapper #3 (hook cleanup):** El hook actual emite `gsd.phase.resolved` en líneas 180-181. Cuando el dispatcher empiece a emitirlo, ese emit DEBE removerse — si no, cada dispatch produce 2 entries NDJSON del mismo evento y `kodo logs --event gsd.phase.resolved` se vuelve ruidoso.
- **Pattern-mapper #4 (brief persistence):** `brief` se persiste en el Session record (plan 09-02 añadió el typedef). El hook lo lee vía `findSession()` — no necesita canal lateral.

Output: 3 archivos src/ modificados + 3 test files modificados/extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md
@.planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md
@.planning/phases/09-phase-resolver-bootstrap/09-01-SUMMARY.md
@.planning/phases/09-phase-resolver-bootstrap/09-02-SUMMARY.md
@.planning/phases/09-phase-resolver-bootstrap/09-03-SUMMARY.md
@.planning/phases/08-gsd-label-session-plumbing/08-CONTEXT.md

<interfaces>
<!-- Dependencias ya existentes, firmas actuales -->

From src/gsd/resolver.js (09-03):
```javascript
export function resolvePhase({ projectPath, task }): ResolveResult
// ResolveResult = { action: 'phase', phase_id, match_heading, match_reason }
//               | { action: 'bootstrap', reason: 'no-planning-dir' }
//               | { action: 'error', code: 'no-match'|'multi-match'|'roadmap-missing', detail?, matches? }
```

From src/gsd/brief.js (09-02):
```javascript
export function buildBriefFromTask(task): string
export function isBriefEmpty(task): boolean
```

From src/session/state.js (09-02 typedef):
```javascript
/** @typedef {{
 *   ..., gsd?: boolean, phase_id?: string, brief?: string,
 * }} Session */
```

From src/logger-events.js (Phase 7, ya existentes):
```javascript
export function gsdPhaseResolved(logger, { phase_id, match_heading }): void
export function gsdBootstrap(logger, { project_path }): void
```

From src/triggers/dispatcher.js (current state):
```javascript
// Line 35: return type union needs 'resolver_failed' added
// Line 43: acquireGsdLockFn DI with default
// Line 119-128: GSD lock acquisition block (exists)
// Line 131: session-already-active guard (line number may shift)
// Lines 150-156 and 177-183: launchOpts construction (two sites — stale + fresh)
```

From src/session/manager.js (current state):
```javascript
// Line 25: buildSessionFromTask signature (needs phaseId, brief)
// Line 132: launchWorkItem signature (needs phase_id, brief in opts)
// Line 184: sessionId = opts.sessionId || randomUUID() (Phase 8 CR-01 fix — model)
// Line 193-200: buildSessionFromTask call (thread new fields here)
```

From src/hooks/session-start.js (current state):
```javascript
// Line 77: buildGsdContext(session) — needs second opts param with brief
// Line 144-146: context = session.gsd ? buildGsdContext(session) : buildSessionContext
// Line 171-189: GSD event emission block — 179-181 gsdPhaseResolved must be REMOVED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender `buildSessionFromTask` + `launchWorkItem` en `src/session/manager.js`</name>
  <files>src/session/manager.js</files>
  <read_first>
    - src/session/manager.js (líneas 15-50 typedef + buildSessionFromTask, líneas 125-205 launchWorkItem)
    - src/session/state.js (Session typedef — confirmar que `phase_id?` y `brief?` ya están allí tras 09-02)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/session/manager.js (MODIFIED — aditivo)")
    - .planning/phases/08-gsd-label-session-plumbing/08-CONTEXT.md (D-12 — patrón buildSessionFromTask original)
  </read_first>
  <action>
Hacer DOS ediciones aditivas a `src/session/manager.js`:

**Edit 1: Extender `buildSessionFromTask` (firma + retorno).**

Busca el JSDoc y firma actual (≈ líneas 15-25):
```javascript
/**
 * Build the session record saved to state from a resolved TaskItem.
 * Pure function — no I/O.
 *
 * @param {{
 *   task: import('../interface.js').TaskItem,
 *   providerName: string,
 *   projectPath: string,
 *   workspaceRef: string,
 *   sessionId: string,
 *   flags?: string[],
 * }} params
 * @returns {import('./state.js').Session}
 */
export function buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags }) {
```

Reemplaza por (AÑADE `phaseId?` y `brief?` al typedef y al destructure, mantén todo lo demás intacto):
```javascript
/**
 * Build the session record saved to state from a resolved TaskItem.
 * Pure function — no I/O.
 *
 * @param {{
 *   task: import('../interface.js').TaskItem,
 *   providerName: string,
 *   projectPath: string,
 *   workspaceRef: string,
 *   sessionId: string,
 *   flags?: string[],
 *   phaseId?: string,  // Phase 9 D-03: resolved phase id threaded from dispatcher (action === 'phase').
 *   brief?: string,    // Phase 9 D-09: bootstrap brief (only set when resolver returned 'bootstrap').
 * }} params
 * @returns {import('./state.js').Session}
 */
export function buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief }) {
```

Luego, en el cuerpo (después de la línea actual `...(flags?.includes('gsd') ? { gsd: true } : {})`), AÑADE dos líneas con el mismo patrón de conditional-spread:

Antes:
```javascript
  return {
    workspace_ref: workspaceRef,
    session_id: sessionId,
    // ... existing fields ...
    // D-12: GSD flag propagated from labels through dispatcher → launchWorkItem.
    // Field is omitted entirely when flags do not include 'gsd' (treated as falsy/non-GSD).
    ...(flags?.includes('gsd') ? { gsd: true } : {}),
  };
}
```

Después:
```javascript
  return {
    workspace_ref: workspaceRef,
    session_id: sessionId,
    // ... existing fields (no cambian) ...
    // D-12: GSD flag propagated from labels through dispatcher → launchWorkItem.
    // Field is omitted entirely when flags do not include 'gsd' (treated as falsy/non-GSD).
    ...(flags?.includes('gsd') ? { gsd: true } : {}),
    // Phase 9: phase_id and brief threaded from dispatcher after resolvePhase().
    // Both optional — only present on GSD sessions where the resolver produced
    // `action: 'phase'` (phaseId) or `action: 'bootstrap'` (brief). Never both.
    ...(phaseId ? { phase_id: phaseId } : {}),
    ...(brief ? { brief } : {}),
  };
}
```

**Edit 2: Extender `launchWorkItem` (JSDoc + signature + threading).**

Busca (≈ líneas 123-132):
```javascript
/**
 * Launch a Claude Code session for a provider-backed task.
 *
 * @param {string} identifier e.g. "KL-42"
 * @param {{ model?: string|null, flags?: string[], sessionId?: string }} [opts]
 *   If `opts.sessionId` is provided (e.g. from the GSD dispatcher which acquires
 *   the repo lock before calling), it is used verbatim as the session_id. Otherwise
 *   a fresh randomUUID() is generated (backwards-compatible for non-GSD paths).
 */
export async function launchWorkItem(identifier, opts = {}) {
```

Reemplaza por:
```javascript
/**
 * Launch a Claude Code session for a provider-backed task.
 *
 * @param {string} identifier e.g. "KL-42"
 * @param {{
 *   model?: string|null,
 *   flags?: string[],
 *   sessionId?: string,
 *   phase_id?: string,  // Phase 9: threaded from dispatcher when resolver returned 'phase'.
 *   brief?: string,     // Phase 9: threaded from dispatcher when resolver returned 'bootstrap'.
 * }} [opts]
 *   If `opts.sessionId` is provided (e.g. from the GSD dispatcher which acquires
 *   the repo lock before calling), it is used verbatim as the session_id. Otherwise
 *   a fresh randomUUID() is generated (backwards-compatible for non-GSD paths).
 *   `phase_id` and `brief` are persisted on the Session record for the hook
 *   SessionStart to consume via findSession().
 */
export async function launchWorkItem(identifier, opts = {}) {
```

Luego, en la llamada a `buildSessionFromTask` (≈ líneas 193-200), AÑADE las dos líneas de threading. Busca:
```javascript
  const session = buildSessionFromTask({
    task,
    providerName: config.provider,
    projectPath,
    workspaceRef,
    sessionId,
    flags: combinedFlags,
  });
```

Reemplaza por:
```javascript
  const session = buildSessionFromTask({
    task,
    providerName: config.provider,
    projectPath,
    workspaceRef,
    sessionId,
    flags: combinedFlags,
    // Phase 9: resolver outputs threaded by dispatcher via opts. Conditional
    // spread in buildSessionFromTask omits the fields when undefined — keeps
    // Session records clean for non-GSD paths.
    phaseId: opts.phase_id,
    brief: opts.brief,
  });
```

**Invariantes:** NO tocar `buildClaudeCommand`, `resolveProjectPath`, `markSessionStatus`, ni nada de cmux/colors. Sólo aditivo.
  </action>
  <verify>
    <automated>grep -n "phaseId?" src/session/manager.js && grep -n "phase_id: opts.phase_id" src/session/manager.js && node --check src/session/manager.js && node --test test/manager.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "phaseId\\?:\\s*string" src/session/manager.js` devuelve 1 coincidencia (en el typedef de buildSessionFromTask)
    - `grep -nE "brief\\?:\\s*string" src/session/manager.js` devuelve ≥1 coincidencia (en los dos typedefs: buildSessionFromTask y launchWorkItem opts)
    - `grep -nE "\\.\\.\\.\\(phaseId \\?" src/session/manager.js` devuelve 1 coincidencia (conditional spread)
    - `grep -nE "\\.\\.\\.\\(brief \\?" src/session/manager.js` devuelve 1 coincidencia
    - `grep -n "phaseId: opts.phase_id" src/session/manager.js` devuelve 1 coincidencia
    - `node --check src/session/manager.js` sale con código 0
    - `node --test test/manager.test.js` sigue pasando sin modificaciones a los tests existentes (aditivo retrocompatible)
    - La signature original de `launchWorkItem(identifier, opts)` se preserva (opts sigue siendo el segundo arg)
  </acceptance_criteria>
  <done>manager.js acepta y propaga `phaseId` + `brief` desde `launchWorkItem.opts` → `buildSessionFromTask` → Session record. Tests existentes no se rompen. Los nuevos campos son opcionales y se omiten cuando no vienen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear resolver en `src/triggers/dispatcher.js` con guard order correcto + emitir eventos</name>
  <files>src/triggers/dispatcher.js, test/dispatcher.test.js</files>
  <read_first>
    - src/triggers/dispatcher.js (completo — 199 líneas; necesitas entender el guard chain actual)
    - src/gsd/resolver.js (creado en 09-03)
    - src/gsd/brief.js (creado en 09-02)
    - test/dispatcher.test.js (para entender el DI pattern existente con acquireGsdLockFn)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/triggers/dispatcher.js (MODIFIED)")
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md (D-03, D-13, D-14)
  </read_first>
  <action>
Hacer CUATRO ediciones a `src/triggers/dispatcher.js`. El orden del guard chain es **CRÍTICO** (pattern-mapper #2).

**Edit 1: Añadir imports al top (línea 10, tras el import de cmux):**

Añade DOS imports nuevos tras `import * as cmux from '../cmux/client.js';`:
```javascript
import { resolvePhase } from '../gsd/resolver.js';
import { buildBriefFromTask, isBriefEmpty } from '../gsd/brief.js';
```

**Edit 2: Extender el typedef `DispatchDeps` (líneas 15-25) con `resolvePhaseFn`:**

Busca:
```javascript
/**
 * @typedef {{
 *   getProviderFn?: (name?: string) => import('../interface.js').TaskProvider,
 *   launchWorkItemFn?: (ref: string, opts: object) => Promise<any>,
 *   listSessionsFn?: () => any[],
 *   listWorkspacesFn?: () => Promise<string>,
 *   removeSessionFn?: (id: string) => void,
 *   acquireGsdLockFn?: (projectPath: string, sessionInfo: {session_id: string, task_id: string, task_ref: string}) => {acquired: boolean, holder?: object},
 *   releaseGsdLockFn?: (projectPath: string, sessionId: string) => void,
 *   resolveProjectPathFn?: (task: import('../interface.js').TaskItem, projects: Record<string, any>) => string,
 * }} DispatchDeps
 */
```

Añade una línea más antes del cierre `}`:
```javascript
 *   resolvePhaseFn?: (params: { projectPath: string, task: object }) => import('../gsd/resolver.js').ResolveResult,
```

**Edit 3: Añadir el default para `resolvePhaseFn` en la inicialización de deps (líneas 37-45):**

Tras la línea `const resolveProjectPathFn = deps.resolveProjectPathFn || ...;`, añade:
```javascript
  const resolvePhaseFn = deps.resolvePhaseFn || resolvePhase;
```

**Edit 4 (CRÍTICO — guard order): Insertar el call al resolver INMEDIATAMENTE después de `acquireGsdLockFn` (línea 128 cierra el `if (gsdProjectPath)`) Y ANTES del `// 4. Session-already-active guard` block.**

El lugar exacto de inserción es entre la línea donde termina el bloque `if (gsdProjectPath) { ... acquireGsdLockFn ... }` (≈ línea 129) y el comentario `// 4. Session-already-active guard (checks persisted state)` (≈ línea 131).

Ampliar también el `@returns` del dispatchTrigger (línea 35) para incluir el nuevo action:
```javascript
 * @returns {Promise<{ action: 'launched'|'ignored'|'already_active'|'stale_relaunch'|'cleaned'|'gsd_locked'|'resolver_failed', session?: object, holder?: object, code?: string, detail?: string }>}
```

Insertar este bloque DESPUÉS del `}` de cierre del `if (gsdProjectPath) {` (que termina en línea 129) y ANTES del comentario `// 4. Session-already-active guard`:

```javascript
  // 3c. GSD phase resolution (Phase 9, D-03). Runs AFTER lock acquisition and
  // BEFORE the session-already-active guard so that stale relaunches also
  // receive phase_id + brief threaded (pattern-mapper refinement #2 — if this
  // moved below the already-active check, relaunches of stale sessions would
  // miss the resolver output).
  //
  // Fail-closed (D-13): error verdicts release the lock and return early.
  let gsdPhaseId = null;
  let gsdBrief = null;
  let resolverVerdict = null;
  if (kodoConfig.flags.includes('gsd') && gsdProjectPath) {
    resolverVerdict = resolvePhaseFn({ projectPath: gsdProjectPath, task });
    switch (resolverVerdict.action) {
      case 'phase':
        gsdPhaseId = resolverVerdict.phase_id;
        break;
      case 'bootstrap':
        gsdBrief = buildBriefFromTask(task);
        break;
      case 'error':
        // D-13: fail-closed. Release lock, emit forensic event, return early.
        if (gsdSessionId && gsdProjectPath) {
          try { releaseGsdLockFn(gsdProjectPath, gsdSessionId); } catch {
            // silent — lock.js release is idempotent
          }
        }
        // D-14: emit gsd.phase.resolved with matched:false for forensic logging.
        try {
          const { createLogger } = await import('../logger.js');
          const log = createLogger({
            sessionId: gsdSessionId || 'dispatch',
            minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
          }).child({ component: 'dispatcher', task_id: task.id });
          log.warn('gsd.phase.resolved', {
            event: 'gsd.phase.resolved',
            matched: false,
            error_code: resolverVerdict.code,
            detail: resolverVerdict.detail,
            task_ref: task.ref,
          });
        } catch {
          // silent — never block the return on logger failure
        }
        console.log(`[kodo:dispatch] resolver_failed — ${task.ref}: ${resolverVerdict.code}${resolverVerdict.detail ? ' (' + resolverVerdict.detail + ')' : ''}`);
        return {
          action: 'resolver_failed',
          code: resolverVerdict.code,
          detail: resolverVerdict.detail,
        };
    }
    // D-14: emit matched-true gsd.phase.resolved (phase branch) or gsd.bootstrap (bootstrap branch).
    try {
      const { createLogger } = await import('../logger.js');
      const { gsdPhaseResolved, gsdBootstrap } = await import('../logger-events.js');
      const log = createLogger({
        sessionId: gsdSessionId,
        minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
      }).child({ component: 'dispatcher', task_id: task.id });
      if (resolverVerdict.action === 'phase') {
        gsdPhaseResolved(log, {
          phase_id: resolverVerdict.phase_id,
          match_heading: resolverVerdict.match_heading,
        });
      } else if (resolverVerdict.action === 'bootstrap') {
        // Include brief_empty flag per D-12 for operator visibility.
        log.info('gsd.bootstrap', {
          event: 'gsd.bootstrap',
          project_path: gsdProjectPath,
          brief_empty: isBriefEmpty(task),
        });
      }
    } catch {
      // silent — never crash dispatch on logger failure
    }
  }

```

**Edit 5: Thread `phase_id` y `brief` en AMBOS `launchOpts` construction sites (stale relaunch + fresh launch).**

Sitio 1 — stale relaunch (actualmente líneas 150-156):
```javascript
      const launchOpts = {
        model: opts.model ?? kodoConfig.model,
        flags: [...(opts.flags || []), ...kodoConfig.flags],
        // Thread GSD sessionId so launchWorkItem uses the UUID stamped in the
        // lock file (fix CR-01). Omitted for non-GSD paths.
        ...(gsdSessionId ? { sessionId: gsdSessionId } : {}),
      };
```

Reemplazar por:
```javascript
      const launchOpts = {
        model: opts.model ?? kodoConfig.model,
        flags: [...(opts.flags || []), ...kodoConfig.flags],
        // Thread GSD sessionId so launchWorkItem uses the UUID stamped in the
        // lock file (fix CR-01). Omitted for non-GSD paths.
        ...(gsdSessionId ? { sessionId: gsdSessionId } : {}),
        // Phase 9: thread phase_id (match) or brief (bootstrap) so Session
        // record persists them for the hook SessionStart to render.
        ...(gsdPhaseId ? { phase_id: gsdPhaseId } : {}),
        ...(gsdBrief ? { brief: gsdBrief } : {}),
      };
```

Sitio 2 — fresh launch (actualmente líneas 177-183): mismo cambio, añade los dos `...(gsdPhaseId ? ...)` y `...(gsdBrief ? ...)` spreads con el mismo comentario.

**Edit 6: Actualizar `test/dispatcher.test.js` con 4 tests nuevos.**

Añade al final de `test/dispatcher.test.js` (antes del cierre del último `describe`) un nuevo bloque describe:

```javascript
describe('dispatchTrigger — Phase 9 resolver integration', () => {
  const baseTask = {
    id: 'task-uuid-1',
    ref: 'KL-42',
    title: 'Phase Resolver + Bootstrap',
    labels: ['kodo', 'kodo:gsd'],
    state: 'In Progress',
    projectId: 'proj-1',
  };
  const baseEvent = { provider: 'plane', taskRef: 'KL-42' };

  function makeDeps({ verdict, acquireResult = { acquired: true }, launchResult = { session_id: 'sess-1' } }) {
    let releaseCalled = false;
    let launchCalledWith = null;
    return {
      getProviderFn: () => ({ getTask: async () => baseTask, init: async () => {} }),
      resolveProjectPathFn: () => '/tmp/fake-project',
      acquireGsdLockFn: () => acquireResult,
      releaseGsdLockFn: () => { releaseCalled = true; },
      resolvePhaseFn: () => verdict,
      listSessionsFn: () => [],
      listWorkspacesFn: async () => '',
      removeSessionFn: () => {},
      launchWorkItemFn: async (_ref, opts) => { launchCalledWith = opts; return launchResult; },
      _inspect: () => ({ releaseCalled, launchCalledWith }),
    };
  }

  it('threads phase_id to launchOpts when resolver returns action=phase', async () => {
    const deps = makeDeps({
      verdict: { action: 'phase', phase_id: '9', match_heading: '### Phase 9: Phase Resolver + Bootstrap', match_reason: 'exact' },
    });
    const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
    const result = await dispatchTrigger(baseEvent, {}, deps);
    assert.equal(result.action, 'launched');
    const { launchCalledWith } = deps._inspect();
    assert.equal(launchCalledWith.phase_id, '9');
    assert.equal(launchCalledWith.brief, undefined);
  });

  it('threads brief to launchOpts when resolver returns action=bootstrap', async () => {
    const deps = makeDeps({
      verdict: { action: 'bootstrap', reason: 'no-planning-dir' },
    });
    const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
    await dispatchTrigger(baseEvent, {}, deps);
    const { launchCalledWith } = deps._inspect();
    assert.ok(launchCalledWith.brief, 'brief should be present');
    assert.ok(launchCalledWith.brief.startsWith('## Project Brief'));
    assert.equal(launchCalledWith.phase_id, undefined);
  });

  it('releases lock and returns resolver_failed on action=error (no-match)', async () => {
    const deps = makeDeps({
      verdict: { action: 'error', code: 'no-match' },
    });
    const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
    const result = await dispatchTrigger(baseEvent, {}, deps);
    assert.equal(result.action, 'resolver_failed');
    assert.equal(result.code, 'no-match');
    const { releaseCalled, launchCalledWith } = deps._inspect();
    assert.equal(releaseCalled, true, 'lock must be released on resolver error (D-13)');
    assert.equal(launchCalledWith, null, 'launch must NOT be called on resolver error');
  });

  it('releases lock and returns resolver_failed on multi-match with detail', async () => {
    const deps = makeDeps({
      verdict: { action: 'error', code: 'multi-match', matches: ['Phase 1: Foo', 'Phase 2: Foo'] },
    });
    const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
    const result = await dispatchTrigger(baseEvent, {}, deps);
    assert.equal(result.action, 'resolver_failed');
    assert.equal(result.code, 'multi-match');
    const { releaseCalled } = deps._inspect();
    assert.equal(releaseCalled, true);
  });

  it('resolver runs BEFORE session-already-active guard (pattern-mapper #2)', async () => {
    // Simulate an existing session AND a successful resolver match. The
    // existing-session path should still thread phase_id through launchOpts
    // (stale_relaunch). Use listSessionsFn returning a session whose workspace
    // is no longer present (stale) to force stale_relaunch path.
    const deps = makeDeps({
      verdict: { action: 'phase', phase_id: '9', match_heading: '### Phase 9', match_reason: 'x' },
    });
    deps.listSessionsFn = () => [{ task_id: 'task-uuid-1', workspace_ref: 'workspace:gone' }];
    deps.listWorkspacesFn = async () => ''; // empty workspace list → stale
    const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
    const result = await dispatchTrigger(baseEvent, {}, deps);
    assert.equal(result.action, 'stale_relaunch');
    const { launchCalledWith } = deps._inspect();
    assert.equal(launchCalledWith.phase_id, '9', 'stale relaunch must still receive phase_id');
  });

  it('does NOT call resolver for non-GSD tasks', async () => {
    const nonGsdTask = { ...baseTask, labels: ['kodo'] /* no kodo:gsd */ };
    let resolverCalled = false;
    const deps = makeDeps({ verdict: { action: 'phase', phase_id: '9', match_heading: 'x', match_reason: 'x' } });
    deps.getProviderFn = () => ({ getTask: async () => nonGsdTask, init: async () => {} });
    deps.resolvePhaseFn = () => { resolverCalled = true; return { action: 'phase', phase_id: '9', match_heading: 'x', match_reason: 'x' }; };
    const { dispatchTrigger } = await import('../src/triggers/dispatcher.js');
    await dispatchTrigger(baseEvent, {}, deps);
    assert.equal(resolverCalled, false, 'resolver must not run for non-GSD tasks');
  });
});
```

Usa las importaciones que ya existen en el archivo (describe/it/assert de `node:test`).

**Invariantes:** NO tocar los guards existentes (labels, terminal-state, inFlight, GSD lock) — sólo INSERTAR el resolver tras el GSD lock. El `await import` dinámico del logger sigue el pattern best-effort ya establecido en session-start.js.
  </action>
  <verify>
    <automated>grep -n "action: 'resolver_failed'" src/triggers/dispatcher.js && grep -n "resolvePhaseFn" src/triggers/dispatcher.js && node --check src/triggers/dispatcher.js && node --test test/dispatcher.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "import \\{ resolvePhase \\} from '\\.\\./gsd/resolver\\.js'" src/triggers/dispatcher.js` devuelve 1 coincidencia
    - `grep -n "buildBriefFromTask" src/triggers/dispatcher.js` devuelve ≥1 coincidencia
    - `grep -n "resolvePhaseFn" src/triggers/dispatcher.js` devuelve ≥3 coincidencias (typedef, default, invocación)
    - `grep -n "action: 'resolver_failed'" src/triggers/dispatcher.js` devuelve 1 coincidencia
    - `grep -nE "\\.\\.\\.\\(gsdPhaseId \\?" src/triggers/dispatcher.js` devuelve 2 coincidencias (stale + fresh launchOpts)
    - `grep -nE "\\.\\.\\.\\(gsdBrief \\?" src/triggers/dispatcher.js` devuelve 2 coincidencias
    - Guard order: la línea del resolver (`resolvePhaseFn({`) aparece ANTES (número de línea menor) que `// 4. Session-already-active guard` en `grep -n`
    - `node --check src/triggers/dispatcher.js` sale con código 0
    - `node --test test/dispatcher.test.js` pasa TODOS los tests (existentes + 6 nuevos del bloque "Phase 9 resolver integration")
    - Ningún test existente se rompe: la firma de dispatchTrigger es retrocompatible (deps sigue siendo opcional; nuevos DI fns tienen defaults)
  </acceptance_criteria>
  <done>Dispatcher llama resolver tras acquire lock y antes de already-active guard, thread-ea phase_id + brief a AMBOS launchOpts sites, libera lock en error, emite gsdPhaseResolved/gsdBootstrap, retorna `resolver_failed` con code+detail. Tests cubren los 6 casos: phase, bootstrap, no-match, multi-match, stale-relaunch, non-GSD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender `buildGsdContext` con brief + eliminar emit duplicado en hook (pattern-mapper #3)</name>
  <files>src/hooks/session-start.js, test/gsd-context.test.js, test/session-start.test.js</files>
  <read_first>
    - src/hooks/session-start.js (completo — líneas 77-117 buildGsdContext, líneas 144-146 caller, líneas 170-189 event emission)
    - test/gsd-context.test.js (para entender cómo están testeadas las ramas phase/bootstrap actualmente)
    - test/session-start.test.js (para entender fixtures de hook)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/hooks/session-start.js (MODIFIED)")
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md (D-09, D-10, D-11 orden brief-first)
  </read_first>
  <action>
TRES ediciones a `src/hooks/session-start.js`.

**Edit 1: Extender signature + cuerpo de `buildGsdContext` (líneas 69-117).**

Busca la firma actual:
```javascript
/**
 * Build GSD-mode context injected into Claude Code sessions.
 * Replaces buildSessionContext entirely for GSD sessions (per D-03).
 * Pure: no I/O, no globals — fully testable.
 *
 * @param {import('../session/state.js').Session} session
 * @returns {string}
 */
export function buildGsdContext(session) {
```

Reemplaza por:
```javascript
/**
 * Build GSD-mode context injected into Claude Code sessions.
 * Replaces buildSessionContext entirely for GSD sessions (per D-03).
 * Pure: no I/O, no globals — fully testable.
 *
 * Phase 9 extension (D-09, D-11): accepts `opts.brief` (pre-rendered bootstrap
 * brief from buildBriefFromTask) and renders it FIRST, then the bootstrap
 * command. Phase 8 behavior unchanged for sessions that already have phase_id.
 *
 * @param {import('../session/state.js').Session} session
 * @param {{ brief?: string }} [opts] - Phase 9: bootstrap brief to render before commands (D-11 order).
 * @returns {string}
 */
export function buildGsdContext(session, opts = {}) {
```

Luego, en la rama `else` (bootstrap, alrededor de líneas 103-113), INSERTA el render del brief ANTES del mensaje "No `.planning/` directory detected...". Busca:
```javascript
  } else {
    // No phase — bootstrap mode (D-01 fallback)
    lines.push(
      'No `.planning/` directory detected or no phase resolved for this task.',
      '',
      'Run the bootstrap command:',
      '',
      '1. `/gsd-new-project`',
      '',
      'This will initialize the project planning structure using the task description as brief.',
    );
  }
```

Reemplaza por:
```javascript
  } else {
    // No phase — bootstrap mode (D-01 fallback).
    // D-11: brief FIRST, commands AFTER. Claude reads the brief, then executes
    // the bootstrap command. If brief is absent (legacy sessions or non-GSD
    // bootstrap paths), skip the brief block entirely — never render a blank section.
    if (opts.brief) {
      lines.push(opts.brief, '');
    }
    lines.push(
      'No `.planning/` directory detected or no phase resolved for this task.',
      '',
      'Run the bootstrap command:',
      '',
      '1. `/gsd-new-project`',
      '',
      'This will initialize the project planning structure using the task description as brief.',
    );
  }
```

**Edit 2: Actualizar el caller en `main()` (≈ líneas 144-146) para pasar el brief del record.**

Busca:
```javascript
    const context = session.gsd
      ? buildGsdContext(session)
      : buildSessionContext(session, loadConfig());
```

Reemplaza por:
```javascript
    // Phase 9: thread session.brief into buildGsdContext — it was persisted by
    // the dispatcher via buildSessionFromTask when resolver returned 'bootstrap'.
    const context = session.gsd
      ? buildGsdContext(session, { brief: session.brief })
      : buildSessionContext(session, loadConfig());
```

**Edit 3 (CRÍTICO — pattern-mapper #3): Eliminar el emit duplicado de `gsd.phase.resolved` del hook.**

Busca el bloque actual (≈ líneas 170-189):
```javascript
    // Emit GSD-specific event (best-effort, silent on failure)
    if (session.gsd) {
      try {
        const { createLogger } = await import('../logger.js');
        const log = createLogger({
          sessionId: session.session_id,
          minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
        }).child({ component: 'hook', task_id: session.task_id });

        if (session.phase_id) {
          const { gsdPhaseResolved } = await import('../logger-events.js');
          gsdPhaseResolved(log, { phase_id: session.phase_id, match_heading: session.summary });
        } else {
          const { gsdBootstrap } = await import('../logger-events.js');
          gsdBootstrap(log, { project_path: session.project_path });
        }
      } catch {
        // silent — never crash Claude Code
      }
    }
```

Reemplaza por (solo emite `gsd.bootstrap` cuando NO hay phase_id — la rama `gsd.phase.resolved` se eliminó porque el dispatcher ya la emite en Phase 9; mantenerla duplicaría entries):
```javascript
    // Phase 9 (pattern-mapper refinement #3): gsd.phase.resolved is emitted by
    // the DISPATCHER now (single source of truth), not the hook. The hook only
    // emits gsd.bootstrap for bootstrap sessions (no phase_id). If we kept the
    // phase-resolved emit here, every GSD dispatch would produce TWO matching
    // NDJSON entries — `kodo logs --event gsd.phase.resolved` would double-count.
    if (session.gsd && !session.phase_id) {
      try {
        const { createLogger } = await import('../logger.js');
        const { gsdBootstrap } = await import('../logger-events.js');
        const log = createLogger({
          sessionId: session.session_id,
          minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
        }).child({ component: 'hook', task_id: session.task_id });
        gsdBootstrap(log, { project_path: session.project_path });
      } catch {
        // silent — never crash Claude Code
      }
    }
```

**Edit 4: Añadir tests en `test/gsd-context.test.js` para la nueva signature + ausencia de duplicate emit.**

Primero lee `test/gsd-context.test.js` para ver su estructura actual. Añade al final del describe principal estos 3 tests adicionales:

```javascript
  it('renders brief BEFORE the bootstrap command (D-11 order)', () => {
    const session = /** @type {any} */ ({
      task_ref: 'KL-42', summary: 'Build phase resolver',
      project_path: '/tmp/p', session_id: 'sess-1', task_id: 't-1', project_id: 'p-1',
      gsd: true,
      // phase_id intentionally omitted → bootstrap branch
    });
    const out = buildGsdContext(session, { brief: '## Project Brief\n\n**Task:** KL-42 — Build phase resolver\n\nBody' });
    const briefIdx = out.indexOf('## Project Brief');
    const cmdIdx = out.indexOf('/gsd-new-project');
    assert.ok(briefIdx >= 0, 'brief heading should be present');
    assert.ok(cmdIdx >= 0, 'bootstrap command should be present');
    assert.ok(briefIdx < cmdIdx, `brief must appear before command (brief@${briefIdx}, cmd@${cmdIdx})`);
  });

  it('renders bootstrap branch without brief when opts.brief is absent (backward-compatible)', () => {
    const session = /** @type {any} */ ({
      task_ref: 'K-1', summary: 's', project_path: '/p', session_id: 'x', task_id: 'y', project_id: 'z',
      gsd: true,
    });
    const out = buildGsdContext(session); // no opts
    assert.ok(!out.includes('## Project Brief'), 'brief block should be absent');
    assert.ok(out.includes('/gsd-new-project'), 'bootstrap command still rendered');
  });

  it('ignores opts.brief when session.phase_id is present (phase branch unchanged)', () => {
    const session = /** @type {any} */ ({
      task_ref: 'K-1', summary: 's', project_path: '/p', session_id: 'x', task_id: 'y', project_id: 'z',
      gsd: true, phase_id: '9',
    });
    const out = buildGsdContext(session, { brief: '## Project Brief\n\nShould NOT appear' });
    assert.ok(!out.includes('## Project Brief'), 'brief should NOT be rendered on phase branch');
    assert.ok(out.includes('/gsd-plan-phase 9'), 'phase commands still render');
  });
```

Asegúrate de tener `buildGsdContext` importado en ese test file.

**Edit 5: Verificar/actualizar `test/session-start.test.js` para asertar que el hook YA NO emite `gsd.phase.resolved`.**

Si `test/session-start.test.js` tiene un test que asserta `gsdPhaseResolved` fue llamado desde el hook, reemplázalo por un test que aserte:
1. Cuando `session.phase_id` está presente, el hook NO emite `gsd.phase.resolved` (el dispatcher ya lo hizo).
2. Cuando `session.gsd && !session.phase_id`, el hook SÍ emite `gsd.bootstrap`.
3. Cuando `!session.gsd`, el hook no emite ninguno de los dos.

Usa el patrón de capture de logs ya presente en el archivo (in-memory sink o tmp file).

Si NO existen tests previos de los emits en `test/session-start.test.js`, añade este test mínimo nuevo (adapta al patrón ya usado en ese archivo para invocar el hook):

```javascript
  it('does NOT emit gsd.phase.resolved from hook (moved to dispatcher per Phase 9)', async () => {
    // This assertion guards against regression. The dispatcher is now the single
    // source for gsd.phase.resolved emission. Duplicating from the hook would
    // make kodo logs --event gsd.phase.resolved double-count every GSD session.
    // Invocar el hook con un session payload que tenga gsd=true + phase_id='9'
    // y aseverar que el NDJSON capturado NO contiene event=gsd.phase.resolved.
    // (Detalles de invocación del hook: usa el mismo helper que ya usan los
    // tests existentes en este archivo.)
  });
```

Completa el cuerpo del test usando el helper de invocación de hook ya presente en el archivo (mkdtemp, spawn del hook, capturar stdout/stderr, parse NDJSON). Si no hay un helper existente, revisa `test/session-start.test.js` para el patrón exacto y adáptalo.
  </action>
  <verify>
    <automated>grep -n "buildGsdContext(session, opts = {})" src/hooks/session-start.js && ! grep -nE "gsdPhaseResolved\\(log" src/hooks/session-start.js && node --check src/hooks/session-start.js && node --test test/gsd-context.test.js test/session-start.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "buildGsdContext(session, opts" src/hooks/session-start.js` devuelve 1 coincidencia (signature extendida)
    - `grep -n "opts.brief" src/hooks/session-start.js` devuelve ≥1 coincidencia (render condicional)
    - **`grep -nE "gsdPhaseResolved\\(log" src/hooks/session-start.js` devuelve 0 coincidencias** (emit duplicado ELIMINADO — pattern-mapper #3)
    - `grep -n "gsdBootstrap(log" src/hooks/session-start.js` devuelve ≥1 coincidencia (emit bootstrap preservado)
    - `grep -n "session.gsd && !session.phase_id" src/hooks/session-start.js` devuelve 1 coincidencia (guard explícito)
    - `grep -n "{ brief: session.brief }" src/hooks/session-start.js` devuelve 1 coincidencia (caller threads brief del record)
    - `node --check src/hooks/session-start.js` sale con código 0
    - `node --test test/gsd-context.test.js` pasa todos los tests (existentes + 3 nuevos)
    - `node --test test/session-start.test.js` pasa todos los tests, incluido el que aserta NO emit de `gsd.phase.resolved`
  </acceptance_criteria>
  <done>buildGsdContext acepta `{brief}` y lo renderiza antes de los comandos en bootstrap branch. Caller pasa `session.brief` del record persistido. Hook YA NO emite `gsd.phase.resolved` (eliminado — el dispatcher es fuente única). Sigue emitiendo `gsd.bootstrap` para sesiones bootstrap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task (external Plane) → dispatcher resolver call | title/description llegan del provider; el resolver ya trata title como string opaco y brief como string opaco |
| Session record (disk) → hook buildGsdContext | brief se persiste en state.json; hook lo lee y lo inyecta en additionalContext de Claude Code |
| dispatcher ↔ logger emit | logger invocation es best-effort con try/catch silencioso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing | phase_id threading | mitigate | `gsdPhaseId` solo se setea desde `resolverVerdict.phase_id` en rama `action==='phase'` del switch. No hay fallback ni coerción — si el resolver no devuelve phase, phase_id queda null y no se thread-ea. |
| T-09-04-02 | Denial of Service | dispatcher blocking on logger | mitigate | El emit del dispatcher usa `await import` dentro de try/catch silencioso (pattern de session-start.js:151-168). Si el logger falla, el dispatch continúa. |
| T-09-04-03 | Information Disclosure | brief en additionalContext | accept | `brief` contiene `task.description` — si la descripción tiene tokens/secretos, se inyecta al prompt de Claude. Mitigación existente: `src/logger.js` redactor redacta secretos conocidos antes de NDJSON. El prompt de Claude (`additionalContext`) NO pasa por el redactor — el operador Plane es responsable de no pegar secretos en descripciones de tareas GSD. Fuera del modelo de amenaza v0.3 (operador de confianza). |
| T-09-04-04 | Tampering | resolver_failed short-circuit | mitigate | El return early del error path ocurre ANTES de `inFlight.add(task.id)` y ANTES de cualquier launch — no hay estado parcial que tampear. Lock release es idempotente. |
| T-09-04-05 | Elevation of Privilege | guard chain order | mitigate | Pattern-mapper #2 corrige el orden: resolver corre ANTES del session-active guard. Test dedicado en Task 2 aserta que stale relaunches también reciben phase_id threaded. |
</threat_model>

<verification>
- `node --test` con TODOS los tests (`node --test test/*.test.js`) pasa sin fallos nuevos — asegura que no se rompió nada de Phase 6/7/8
- Grep de emit duplicado: `grep -rn "gsdPhaseResolved(log" src/` devuelve exactamente 1 coincidencia (en `src/triggers/dispatcher.js`, NO en `src/hooks/session-start.js`)
- `node --check src/triggers/dispatcher.js src/session/manager.js src/hooks/session-start.js` todos salen con código 0
- Verificación manual del guard order: abrir dispatcher.js y confirmar que el bloque "3c. GSD phase resolution" aparece entre "3b. GSD repo lock guard" y "4. Session-already-active guard"
</verification>

<success_criteria>
- GSD-02 satisfecho: task GSD con PROJECT.md ausente → brief inyectado + launch procede (no fail)
- GSD-08 satisfecho: brief renderizado en additionalContext antes del comando `/gsd-new-project` (D-11 order)
- GSD-09 satisfecho: task GSD con título matching heading → phase_id inferido sin config explícita, thread-eado al Session record
- Fail-closed: resolver error → lock release + return `resolver_failed`
- Pattern-mapper #2 aplicado: resolver corre antes del session-active guard (stale relaunches también reciben phase_id)
- Pattern-mapper #3 aplicado: hook YA NO emite `gsd.phase.resolved` (eliminado para evitar duplicados); dispatcher es fuente única
- Pattern-mapper #4 aplicado: brief persistido en Session record, leído por hook vía findSession()
- Tests cubren los 6 flows: phase, bootstrap, no-match, multi-match, stale-relaunch, non-GSD + los 3 tests de buildGsdContext + el test anti-regresión del emit duplicado
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-resolver-bootstrap/09-04-SUMMARY.md`
</output>
