---
phase: 06-structured-logger-foundation
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/logger-noop.js
  - src/logger.js
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04]
tags: [logging, factory, ndjson]

must_haves:
  truths:
    - "Existe `createLogger({ sessionId, minLevel })` que devuelve un logger con `debug/info/warn/error/child`"
    - "El logger escribe NDJSON una línea por evento a `~/.kodo/logs/<session-id>.ndjson` con `timestamp`, `level`, `component`, `msg`, `session_id` y contexto top-level"
    - "`warn` y `error` se espejan a stderr en pretty-print; ninguna línea stderr empieza con `{`"
    - "Niveles por debajo de `minLevel` son descartados (no se escriben ni a disco ni a stderr)"
    - "`noopLogger` existe en `src/logger-noop.js` con zero imports (no `node:*`, no relativos)"
    - "Fallos de I/O (ENOSPC/EACCES) emiten UN SOLO warning pretty-print a stderr y no lanzan excepción"
  artifacts:
    - path: "src/logger-noop.js"
      provides: "No-op logger stub, zero imports, re-exportable desde logger.js"
      exports: ["noopLogger"]
    - path: "src/logger.js"
      provides: "Factory NDJSON + pretty-print stderr + closure-captured state"
      exports: ["createLogger", "noopLogger", "LEVELS", "LEVEL_NAMES"]
  key_links:
    - from: "src/logger.js"
      to: "src/config.js"
      via: "import { KODO_DIR } from './config.js'"
      pattern: "from ['\"]\\.\\/config\\.js['\"]"
    - from: "src/logger.js"
      to: "~/.kodo/logs/<session>.ndjson"
      via: "appendFileSync con JSON.stringify(record) + '\\n'"
      pattern: "appendFileSync\\("
    - from: "src/logger.js"
      to: "process.stderr"
      via: "writeSync(2, ...) para pretty-print"
      pattern: "writeSync\\(2"
---

<objective>
Implementar el factory `createLogger` con NDJSON sink a disco y pretty-print mirror a stderr, más el no-op stub independiente. Cubre LOG-01 (niveles + config), LOG-02 (NDJSON shape), LOG-03 (per-session file + child bindings) y LOG-04 (pretty-print sin duplicar JSON). Redacción de secretos (LOG-08) se suma en Plan 03.

Purpose: Establecer el contrato y la mecánica de escritura del logger con ergonomía pino-like, reutilizando `KODO_DIR` de `src/config.js`, sin añadir dependencias. El no-op vive en archivo separado (`src/logger-noop.js`, zero imports) para resolver Open Question Q1 del RESEARCH: módulos compartidos que quieran un logger default usable desde el path del vigilante pueden importar `noopLogger` desde el stub sin arrastrar `src/logger.js` al grafo.

Output: `src/logger.js` (~200 LoC) + `src/logger-noop.js` (~10 LoC). Los tests `test/logger.test.js` pasan a GREEN para LOG-01..LOG-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-structured-logger-foundation/06-CONTEXT.md
@.planning/phases/06-structured-logger-foundation/06-RESEARCH.md
@.planning/phases/06-structured-logger-foundation/06-PATTERNS.md
@.planning/phases/06-structured-logger-foundation/06-01-SUMMARY.md
@src/config.js
@src/providers/plane/provider.js
@src/session/state.js
@src/check.js
@test/logger.test.js
@test/helpers/logger-fixtures.js

<interfaces>
<!-- Contratos que este plan crea. Los consumidores futuros (Phase 7-10) los importarán. -->

`src/logger.js` exports públicos:

```javascript
/**
 * @typedef {'debug'|'info'|'warn'|'error'} LogLevel
 * @typedef {{ sessionId: string, minLevel?: LogLevel }} LoggerOpts
 * @typedef {{
 *   debug(msg: string, ctx?: object): void,
 *   info(msg: string, ctx?: object): void,
 *   warn(msg: string, ctx?: object): void,
 *   error(msg: string, ctx?: object): void,
 *   child(bindings: object): Logger,
 * }} Logger
 */
export function createLogger(opts: LoggerOpts): Logger;
export const LEVELS: Readonly<{ debug: 10, info: 20, warn: 30, error: 40 }>;
export const LEVEL_NAMES: readonly ['debug', 'info', 'warn', 'error'];
export { noopLogger } from './logger-noop.js';
```

`src/logger-noop.js` export:
```javascript
export const noopLogger: Logger; // todos los métodos no-op, child() retorna el mismo noopLogger
```

Reutilizado desde src/config.js (ya existente):
```javascript
export const KODO_DIR = join(homedir(), '.kodo'); // en config.js línea 6
```
</interfaces>

<conventions_from_codebase>
- `// @ts-check` en línea 1 de todo archivo `src/*.js` (verificado: config.js, check.js, state.js, registry.js).
- Imports bare con prefijo `node:` (`node:fs`, `node:path`); relativos con `.js` explícito.
- Factory con closure-captured state, no `class`, no `this` (patrón `provider.js`).
- Named exports only; nunca `export default`.
- Strings con comillas simples. 2 espacios de indentación.
- Constantes ANSI `UPPER_SNAKE_CASE`. Prefijo mensaje stderr: `[kodo:logger]`.
- `JSDoc @param`/`@returns` obligatorio en exports públicos.
- Keys de payload en `snake_case` (`session_id`, `plane_task_id`, `phase_id`).
- Comentarios de usuario visible en español (`[kodo:logger] write failed: ...` acepta mezcla).
</conventions_from_codebase>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Crear src/logger-noop.js (zero-import stub)</name>
  <files>src/logger-noop.js</files>
  <read_first>
    - .planning/phases/06-structured-logger-foundation/06-PATTERNS.md líneas 123-155 (no-op pattern)
    - .planning/phases/06-structured-logger-foundation/06-RESEARCH.md líneas 323-334 (Pattern 4)
    - src/providers/registry.js (archivo pequeño con estado cerrado — comparable)
    - test/check-isolation.test.js (test que valida zero imports)
  </read_first>
  <behavior>
    - `noopLogger.debug()`, `.info()`, `.warn()`, `.error()` no lanzan, no escriben nada, retornan `undefined`
    - `noopLogger.child({ anything })` retorna el MISMO `noopLogger` (referencia idéntica) — no se crea objeto nuevo
    - `Object.isFrozen(noopLogger) === true`
    - El archivo NO contiene ningún `import` (verificable por grep)
  </behavior>
  <action>
  Crear archivo EXACTAMENTE con esta estructura (no añadir imports de ningún tipo):

  ```javascript
  // @ts-check
  //
  // No-op logger stub. MUST have zero imports (not even node: builtins, not
  // relative files) so that src/check.js — o cualquier módulo en el camino del
  // vigilante — puede importarlo sin arrastrar src/logger.js al grafo.
  //
  // Guardián runtime: test/check-isolation.test.js asserta que logger-noop.js
  // no contiene imports.
  //

  /**
   * @typedef {{
   *   debug(msg: string, ctx?: object): void,
   *   info(msg: string, ctx?: object): void,
   *   warn(msg: string, ctx?: object): void,
   *   error(msg: string, ctx?: object): void,
   *   child(bindings: object): NoopLogger,
   * }} NoopLogger
   */

  /** @type {NoopLogger} */
  export const noopLogger = Object.freeze({
    debug() {},
    info() {},
    warn() {},
    error() {},
    child() { return noopLogger; },
  });
  ```

  No añadir nada más. El archivo debe ser ~25 LoC incluyendo comentarios.
  </action>
  <acceptance_criteria>
    - `test -f src/logger-noop.js`
    - `grep -E "^\\s*import\\s" src/logger-noop.js` NO devuelve output (zero imports verificable)
    - `node -e "import('./src/logger-noop.js').then(m =&gt; { if(typeof m.noopLogger.child !== 'function') process.exit(1); if(m.noopLogger.child({}) !== m.noopLogger) process.exit(2); if(!Object.isFrozen(m.noopLogger)) process.exit(3); })"` exit 0
    - `node -c src/logger-noop.js` exit 0
    - El archivo contiene `// @ts-check` en la línea 1
  </acceptance_criteria>
  <verify>
    <automated>node -c src/logger-noop.js &amp;&amp; ! grep -E "^\\s*import\\s" src/logger-noop.js &amp;&amp; node --input-type=module -e "import('./src/logger-noop.js').then(m=&gt;{if(m.noopLogger.child({}) !== m.noopLogger) process.exit(1); if(!Object.isFrozen(m.noopLogger)) process.exit(2);})"</automated>
  </verify>
  <done>`src/logger-noop.js` existe, tiene cero imports, `noopLogger` está congelado y `.child()` retorna la misma referencia.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Crear src/logger.js — factory + NDJSON sink + pretty-print</name>
  <files>src/logger.js</files>
  <read_first>
    - src/config.js (reutilizar `KODO_DIR`; seguir convención `// @ts-check`, JSDoc, `mkdirSync({recursive: true})`)
    - src/providers/plane/provider.js (factory con closure pattern — primer analog)
    - src/session/state.js (typedef pattern + snake_case keys en datos serializados)
    - src/check.js líneas 15-17, 42-50 (ANSI constantes + prefijo `[kodo:<sub>]` + swallow+stderr)
    - src/logger-noop.js (recién creado en Task 2.1 — re-export)
    - .planning/phases/06-structured-logger-foundation/06-RESEARCH.md líneas 192-321 (Patterns 1-3)
    - .planning/phases/06-structured-logger-foundation/06-PATTERNS.md líneas 24-155 (pattern assignments)
    - test/logger.test.js (contrato que debe cumplir)
  </read_first>
  <behavior>
    - `createLogger({ sessionId: 's1', minLevel: 'info' }).info('hi', { x: 1 })` escribe UNA línea NDJSON a `$HOME/.kodo/logs/s1.ndjson` con campos: `timestamp` (ISO), `level: 'info'`, `msg: 'hi'`, `session_id: 's1'`, `x: 1`
    - `createLogger({...}).debug(...)` con `minLevel: 'info'` NO escribe nada
    - `root.child({ component: 'plane.client', plane_task_id: 'KL-1' }).info('x')` produce línea con `component` y `plane_task_id` mergeados
    - `log.warn(...)` y `log.error(...)` emiten a `process.stderr` en formato `HH:MM:SS LEVEL component msg`, nunca una línea que empiece con `{`
    - `log.info(...)` emite a stderr solo si `process.stderr.isTTY` y `minLevel <= info`
    - `log.debug(...)` emite a stderr solo si `process.stderr.isTTY` y `minLevel === 'debug'`
    - Si el `appendFileSync` lanza (p.ej. tras `rmSync` del dir), se emite UN warning `[kodo:logger] write failed: ...` por sesión y NO se lanza excepción
    - `minLevel` inválido (`'trace'`, `'fatal'`, `'warning'`) lanza error en el factory
    - `noopLogger` se re-exporta desde este módulo
  </behavior>
  <action>
  Crear `src/logger.js` siguiendo literalmente RESEARCH Pattern 1-3 combinados con PATTERNS.md. Estructura:

  ```javascript
  // @ts-check
  //
  // src/logger.js — NDJSON structured logger con pretty-print stderr mirror.
  //
  // Responsabilidades:
  //   1. Factory createLogger({ sessionId, minLevel }) con child bindings (estilo pino).
  //   2. Sink NDJSON a ~/.kodo/logs/<session>.ndjson via appendFileSync.
  //   3. Sink pretty-print a stderr para warn/error (e info/debug en TTY con minLevel).
  //   4. Manejo swallow+stderr de I/O failures (nunca throw).
  //
  // Redacción de secretos (LOG-08) se añade en Plan 03.
  // Aislamiento del vigilante (LOG-12): src/check.js NO importa este archivo — ver
  // test/check-isolation.test.js.
  //

  import { appendFileSync, mkdirSync, writeSync } from 'node:fs';
  import { join } from 'node:path';
  import { KODO_DIR } from './config.js';

  // Re-export for conveniencia — los consumidores pueden hacer
  // `import { noopLogger } from './logger.js'` o directamente desde './logger-noop.js'.
  export { noopLogger } from './logger-noop.js';

  /** @type {Readonly<{ debug: 10, info: 20, warn: 30, error: 40 }>} */
  export const LEVELS = Object.freeze({ debug: 10, info: 20, warn: 30, error: 40 });

  /** @type {readonly ['debug','info','warn','error']} */
  export const LEVEL_NAMES = Object.freeze(['debug', 'info', 'warn', 'error']);

  // ANSI escape codes (mismas convenciones que src/check.js).
  const ANSI_RESET = '\x1b[0m';
  const ANSI_GRAY = '\x1b[90m';
  const ANSI_CYAN = '\x1b[36m';
  const ANSI_YELLOW = '\x1b[33m';
  const ANSI_RED = '\x1b[31m';

  const COLOR_BY_LEVEL = Object.freeze({
    debug: ANSI_GRAY,
    info: ANSI_CYAN,
    warn: ANSI_YELLOW,
    error: ANSI_RED,
  });

  /**
   * @typedef {'debug'|'info'|'warn'|'error'} LogLevel
   * @typedef {{ sessionId: string, minLevel?: LogLevel }} LoggerOpts
   * @typedef {{
   *   debug(msg: string, ctx?: object): void,
   *   info(msg: string, ctx?: object): void,
   *   warn(msg: string, ctx?: object): void,
   *   error(msg: string, ctx?: object): void,
   *   child(bindings: object): Logger,
   * }} Logger
   */

  /**
   * Crea un logger raíz con session_id bindeado.
   *
   * @param {LoggerOpts} opts
   * @returns {Logger}
   */
  export function createLogger({ sessionId, minLevel = 'info' }) {
    if (typeof sessionId !== 'string' || sessionId.length === 0) {
      throw new Error('[kodo:logger] sessionId is required');
    }
    if (!(minLevel in LEVELS)) {
      throw new Error(`[kodo:logger] invalid minLevel: ${minLevel}`);
    }

    const logDir = join(KODO_DIR, 'logs');
    mkdirSync(logDir, { recursive: true });
    const filePath = join(logDir, `${sessionId}.ndjson`);
    const minLevelNum = LEVELS[minLevel];
    const useColor = Boolean(process.stderr.isTTY) && !process.env.NO_COLOR;
    let writeFailedWarned = false;

    return makeNode({ session_id: sessionId });

    /**
     * @param {object} boundFields
     * @returns {Logger}
     */
    function makeNode(boundFields) {
      /** @type {Logger} */
      const node = /** @type {any} */ ({
        child(extra) { return makeNode({ ...boundFields, ...extra }); },
      });
      for (const name of LEVEL_NAMES) {
        node[name] = (msg, ctx) => emit(name, msg, ctx, boundFields);
      }
      return node;
    }

    /**
     * @param {LogLevel} level
     * @param {string} msg
     * @param {object | undefined} ctx
     * @param {object} boundFields
     */
    function emit(level, msg, ctx, boundFields) {
      if (LEVELS[level] < minLevelNum) return;
      const record = {
        timestamp: new Date().toISOString(),
        level,
        ...boundFields,
        msg: String(msg),
        ...(ctx ?? {}),
      };
      writeNdjson(record);
      maybeMirrorToStderr(level, record);
    }

    /** @param {object} record */
    function writeNdjson(record) {
      try {
        appendFileSync(filePath, JSON.stringify(record) + '\n');
      } catch (err) {
        if (!writeFailedWarned) {
          const msg = err instanceof Error ? err.message : String(err);
          writeSync(2, `${ANSI_RED}[kodo:logger] write failed: ${msg}${ANSI_RESET}\n`);
          writeFailedWarned = true;
        }
      }
    }

    /**
     * @param {LogLevel} level
     * @param {object} record
     */
    function maybeMirrorToStderr(level, record) {
      const isTTY = Boolean(process.stderr.isTTY);
      const mirror =
        level === 'error' ||
        level === 'warn' ||
        (level === 'info' && isTTY && minLevelNum <= LEVELS.info) ||
        (level === 'debug' && isTTY && minLevelNum <= LEVELS.debug);
      if (!mirror) return;

      const time = /** @type {string} */ (record.timestamp).slice(11, 19); // HH:MM:SS
      const c = useColor ? COLOR_BY_LEVEL[level] : '';
      const r = useColor ? ANSI_RESET : '';
      const comp = record.component ? ` ${record.component}` : '';
      const ctxStr = formatCtxInline(record);
      writeSync(2, `${time} ${c}${level.toUpperCase()}${r}${comp} ${record.msg}${ctxStr}\n`);
    }

    /**
     * Formatea campos extra de contexto inline como `+k=v k2=v2`.
     * Excluye los base fields (timestamp, level, msg, session_id, component, plane_task_id, phase_id).
     * @param {object} record
     * @returns {string}
     */
    function formatCtxInline(record) {
      const BASE = new Set(['timestamp', 'level', 'msg', 'session_id', 'component', 'plane_task_id', 'phase_id']);
      const parts = [];
      for (const [k, v] of Object.entries(record)) {
        if (BASE.has(k)) continue;
        if (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean') {
          parts.push(`${k}=${v}`);
        } else {
          parts.push(`${k}=${JSON.stringify(v)}`);
        }
      }
      return parts.length ? ` +${parts.join(' ')}` : '';
    }
  }
  ```

  **Puntos clave de conformidad:**
  - `// @ts-check` línea 1 (patrón del codebase).
  - `import { KODO_DIR } from './config.js'` — NO duplicar `homedir() + '.kodo'`.
  - `mkdirSync(logDir, { recursive: true })` directo (idempotente, sin `existsSync` previo — preferencia RESEARCH).
  - Factory sin `this`, objeto literal retornado, helpers como `function name()` (hoisted).
  - Constantes ANSI en UPPER_SNAKE_CASE (patrón check.js).
  - `writeSync(2, ...)` en lugar de `process.stderr.write` o `console.error` — determinista, no monkey-patcheable más allá del mock de node:test.
  - `writeFailedWarned` como `let` en closure — un warning por sesión.
  - Re-export de `noopLogger` al top.

  **No hacer en este plan:**
  - NO implementar redactor — Plan 03 lo añade.
  - NO parsear CLI args ni env vars — `src/cli.js` en fase siguiente compone `minLevel`.
  - NO manejar `Error` instances especial (serialización de `err.stack`) — dejar al consumidor pasar `{ err: { message, stack } }` (documentado en RESEARCH Pitfall 1); Plan 03 puede añadir defensive handling si es low-cost.

  Ejecutar tests para confirmar GREEN en LOG-01..LOG-04 (y RED esperado en LOG-08 redaction hasta Plan 03):
  `node --test test/logger.test.js`
  </action>
  <acceptance_criteria>
    - `test -f src/logger.js` y línea 1 es `// @ts-check`
    - Grep `import\\s.*from\\s['"]\\.\\/config\\.js['"]` encuentra match
    - Grep `appendFileSync\\(` encuentra ≥1 match
    - Grep `writeSync\\(2` encuentra ≥1 match
    - Grep `export\\s+function\\s+createLogger` encuentra exactamente 1 match
    - Grep `export\\s+\\{\\s*noopLogger\\s*\\}\\s*from\\s*['"]\\.\\/logger-noop\\.js['"]` encuentra match
    - `node --test test/logger.test.js` pasa en verde los 4 describe blocks (LOG-01..LOG-04)
    - `node --test test/check-isolation.test.js` SIGUE en verde (logger.js no se importó desde check.js)
    - `node --test test/startup-budget.test.js` SIGUE en verde (no se degradó el arranque)
    - Grep `^\\s*throw\\s` en `writeNdjson` subfunction NO encuentra match (el logger nunca lanza por I/O)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/logger.test.js test/check-isolation.test.js test/startup-budget.test.js</automated>
  </verify>
  <done>`src/logger.js` implementa createLogger con NDJSON+stderr, re-exporta `noopLogger`, pasa los 4 describe blocks de LOG-01..LOG-04, y no rompe los tests de LOG-12 ya verdes de Wave 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → logger API | `msg` y `ctx` pueden venir de fuentes semi-confiables (cuerpos Plane, payloads webhook). Riesgo de log-injection vía `\n` en `msg`. |
| Logger → disk | `appendFileSync` a `~/.kodo/logs/` — escribible por el user, confidencial de facto |
| Logger → stderr | `writeSync(2, ...)` hereda TTY/pipe del padre; en CI puede terminar en logs públicos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-02-01 | Tampering (log injection) | `emit()` → `JSON.stringify(record)` | mitigate | `JSON.stringify` escapa `\n`, `\r`, `\t` y comillas automáticamente; `msg` se coacciona con `String(msg)` antes de inclusión |
| T-6-02-02 | Information Disclosure | `writeNdjson` persiste todo el ctx | mitigate (parcial) | Plan 03 añade redactor antes de `JSON.stringify`; este plan documenta que ctx NO debe pasarse con secretos hasta Plan 03 |
| T-6-02-03 | DoS (gigantic ctx) | Caller pasa objeto circular o enorme | mitigate | `JSON.stringify` lanza en ciclos → atrapado por try/catch en `writeNdjson` → un warning pretty a stderr, sesión continúa |
| T-6-02-04 | DoS (log flooding) | Loop de `log.info` sin throttle | accept | Out of scope v0.3; futuro LOG-F4 podría añadir rate-limit |
| T-6-02-05 | Information Disclosure | `~/.kodo/logs/` permisos | mitigate (débil) | Hereda umask del user (típicamente 0022 → 0644 file). RESEARCH Security Domain V4 recomienda `chmod 0600` — diferido a Plan 03 junto con redacción si es trivial, si no a follow-up |
| T-6-02-06 | Tampering (prototype pollution via ctx con `__proto__`) | Object spread `{...ctx}` | mitigate | El spread crea un objeto nuevo sin copiar `__proto__` enumerable; `JSON.stringify` no sigue `__proto__`. Test explícito recomendado en Plan 03 al tocar el walker del redactor |
| T-6-02-07 | Tampering (symlink en `~/.kodo/logs/`) | `mkdirSync({ recursive: true })` sigue symlinks | accept | Out of scope v0.3; atacante con access a `$HOME` ya ganó; documentado en RESEARCH |
</threat_model>

<verification>
- `node --test test/logger.test.js` → 4 describe blocks pasan (LOG-01, LOG-02, LOG-03, LOG-04)
- `node --test test/check-isolation.test.js` → sigue en verde
- `node --test test/startup-budget.test.js` → sigue en verde
- `node --test test/logger-redaction.test.js` → sigue en RED (esperado hasta Plan 03)
- `node -e "const l=(await import('./src/logger.js')).createLogger({sessionId:'x', minLevel:'info'}); l.debug('dropped'); l.info('kept');"` — no lanza
- Grep `throw` en `src/logger.js` no aparece dentro de `writeNdjson` ni `maybeMirrorToStderr` (solo en validación de `sessionId`/`minLevel` al inicio)
</verification>

<success_criteria>
- `src/logger.js` existe, cumple todas las reglas de convention (@ts-check, KODO_DIR reuse, factory closure, JSDoc, named exports)
- `src/logger-noop.js` existe y tiene zero imports
- Los 4 describe blocks de LOG-01..LOG-04 pasan en verde
- `test/check-isolation.test.js` y `test/startup-budget.test.js` siguen verdes
- El logger nunca lanza por I/O — test de error path (filesystem bloqueado) emite warning y continúa
- `noopLogger` se re-exporta desde `logger.js` para ergonomía
</success_criteria>

<output>
After completion, create `.planning/phases/06-structured-logger-foundation/06-02-SUMMARY.md` documenting LoC total, test results (LOG-01..LOG-04 green), and notes sobre cualquier desviación de los patterns (p.ej. si `formatCtxInline` se simplificó).
</output>
