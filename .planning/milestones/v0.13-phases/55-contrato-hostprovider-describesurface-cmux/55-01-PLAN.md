---
phase: 55-contrato-hostprovider-describesurface-cmux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/cmux/surface-resume-show.json
  - test/fixtures/cmux/surface-tree.json
  - src/host/interface.js
  - src/host/cmux.js
  - test/host/contract.test.js
autonomous: true
requirements: [DETECT-01]

must_haves:
  truths:
    - "D-01: listAgentSurfaces() es enumeración → array (NO describeSurface(ref)); CmuxHost lo expone como función"
    - "D-01: listAgentSurfaces() enumera en 2 pasos (tree --all --json → fan-out surface resume show --surface <ref>) y devuelve AgentSurface[]"
    - "D-02: el shape devuelto es camelCase {workspaceRef, cwd, sessionId, kind}, alineado con la firma de adoptSession"
    - "D-02: sessionId proviene literalmente de resume_binding.checkpoint_id; cwd ← resume_binding.cwd; kind ← resume_binding.kind; workspaceRef ← workspace_ref"
    - "D-03: el método es opcional typeof-detected, FUERA de HOST_METHODS (que sigue congelado en 4); validateHost intacto"
    - "D-04: la salida real de cmux 0.64.16 (NO 0.64.15) queda fixture-lockeada en surface-resume-show.json y asertada campo a campo vía el run DI"
    - "D-05: never-throws fail-open — tree exec/parse falla → [] sin throw; el try/catch del fan-out va DENTRO del bucle (fila-a-fila): cleared:true / sin resume_binding / source!=agent-hook se omiten, un resume show individual que falla omite esa surface sin romper el array"
    - "D-06: el set-difference contra state.json se difiere al consumidor (Phase 56), keyeado por sessionId/cwd estable (nunca workspaceRef); adopt.js/reconcile.js permanecen host-agnósticos (regla transversal LOCKED)"
    - "El walker cmux-isolation.test.js sigue verde sin cambios"
  artifacts:
    - path: "test/fixtures/cmux/surface-resume-show.json"
      provides: "Salida cruda real de cmux 0.64.16 surface resume show: ≥1 adoptable + cleared:true + sin resume_binding + source!=agent-hook"
      contains: "checkpoint_id"
    - path: "test/fixtures/cmux/surface-tree.json"
      provides: "Salida cruda de cmux tree --all --json (paso-1 de enumeración)"
      contains: "surface_ref"
    - path: "src/host/interface.js"
      provides: "@typedef AgentSurface (camelCase); HOST_METHODS sin cambios"
      contains: "AgentSurface"
    - path: "src/host/cmux.js"
      provides: "listAgentSurfaces() + helper puro normalizeSurface(raw)"
      contains: "listAgentSurfaces"
    - path: "test/host/contract.test.js"
      provides: "Golden asserts campo a campo + casos fail-open; routing argv para tree y surface resume show"
      contains: "listAgentSurfaces"
  key_links:
    - from: "src/host/cmux.js listAgentSurfaces()"
      to: "run DI (tree + surface resume show)"
      via: "run(['tree','--all','--json','--id-format','both']) y run(['surface','resume','show','--json','--surface',ref])"
      pattern: "surface.*resume.*show"
    - from: "src/host/cmux.js normalizeSurface()"
      to: "AgentSurface shape"
      via: "resume_binding.checkpoint_id → sessionId, resume_binding.cwd → cwd, resume_binding.kind → kind, workspace_ref → workspaceRef"
      pattern: "checkpoint_id"
    - from: "test/host/contract.test.js fakeExecFromFixtures + instantiateHost run"
      to: "test/fixtures/cmux/surface-resume-show.json"
      via: "argv routing includes('surface resume show') / includes('tree')"
      pattern: "surface-resume-show"
---

<objective>
Añadir `listAgentSurfaces()` al CmuxHost (`src/host/cmux.js`) como método **opcional typeof-detected** (FUERA de `HOST_METHODS`, congelado en 4), que enumera las sesiones-agente ad-hoc de cmux mediante una **enumeración de dos pasos** y las devuelve como datos host-agnósticos `{ workspaceRef, cwd, sessionId, kind }`. Implementa DETECT-01 íntegro.

Purpose: es el **seam del host** de la dirección inversa `sesión → tarea`. Phase 56 (tecla `a` del dashboard) lo consumirá vía `typeof host.listAgentSurfaces === 'function'` + un set-difference contra `state.json`. Mantiene la regla transversal LOCKED: todo lo cmux-específico vive en `src/host/cmux.js`; `adopt.js`/`reconcile.js` permanecen host-agnósticos.

Output: fixtures golden de cmux 0.64.16 (`surface-resume-show.json` + `surface-tree.json`), `@typedef AgentSurface` en `interface.js`, `listAgentSurfaces()` + helper puro `normalizeSurface(raw)` en `cmux.js`, y un nuevo `describe` en `contract.test.js` con asserts campo a campo + casos fail-open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/55-contrato-hostprovider-describesurface-cmux/55-CONTEXT.md
@.planning/phases/55-contrato-hostprovider-describesurface-cmux/55-RESEARCH.md
@.planning/phases/55-contrato-hostprovider-describesurface-cmux/55-PATTERNS.md
@.planning/phases/55-contrato-hostprovider-describesurface-cmux/55-VALIDATION.md
@.planning/research/CMUX-CAPABILITIES.md

<interfaces>
<!-- Contratos clave que el executor necesita. Extraídos del codebase. Usar directamente — no explorar. -->

El `run` DI de createCmuxHost (src/host/cmux.js:24-43) — REUSAR sin tocar:
  function makeRun(execSync, binary) => async (args) => execSync(binary, args, {encoding:'utf-8', timeout: 5000})
  const run = opts.run || makeRun(opts.execSync || execFileSync, binary);
  const logger = opts.logger;   // NUNCA importar src/logger.js (LOG-12). Se lee como logger?.warn?.(...).

Molde never-throws a espejar (listWorkspaces, src/host/cmux.js:62-118):
  try { raw = await run([...]) } catch (err) { logger?.warn?.('host.X.fail', {code: err?.code||'EXEC_ERROR', detail: String(err?.message||'').trim(), duration_ms: Date.now()-started}); return []; }
  try { parsed = JSON.parse(raw) } catch (err) { logger?.warn?.('host.X.fail', {code:'PARSE_ERROR', ...}); return []; }
  ...map... ; logger?.info?.('host.X.ok', {count, duration_ms}); return result;

Return shape actual de createCmuxHost (src/host/cmux.js:178):
  return { listWorkspaces, selectWorkspace, isAlive, needsInput, _legacy };
  // NUEVO: { listWorkspaces, selectWorkspace, isAlive, needsInput, listAgentSurfaces, _legacy };  // _legacy NO se toca

HOST_METHODS (src/host/interface.js:34-39) — NO CAMBIA, sigue en 4:
  export const HOST_METHODS = Object.freeze(['listWorkspaces','selectWorkspace','isAlive','needsInput']);

Firma de adoptSession (src/adopt.js:163-166) — el shape de salida DEBE encajar sin transformación:
  adoptSession({ provider, providerName, workspaceRef, cwd, sessionId, projectId, ... }, deps)

Shape REAL de `surface resume show --json` adoptable (cmux 0.64.16, RESEARCH §Code Examples):
  { cleared:false, pane_ref:"pane:1", resume_binding:{ checkpoint_id:"c1c3ed6d-...", cwd:"/Users/alex/dev/klab/kodo", kind:"claude", name:"Claude Code", source:"agent-hook", updated_at:1781624696.83 }, surface_ref:"surface:1", window_ref:"window:1", workspace_ref:"workspace:1" }
  // sessionId ← resume_binding.checkpoint_id (== session_id de Claude Code, P0)
  // Una surface CERRADA pasada a --surface → exit 0 con "Error: not_found: Surface not found" o JSON inválido → fila-a-fila skip.

Shape REAL de `tree --all --json --id-format both` (cmux 0.64.16):
  { active:{...}, caller:{...}, windows:[{ ref:"window:1", workspaces:[{ ref:"workspace:1", title:"KODO DEV", panes:[{ ref:"pane:1", surface_refs:["surface:1","surface:2"], surfaces:[{ref:"surface:1", type:"terminal", title:"⠂ Claude Code", tty:"ttys005"}, ...] }] }] }] }
  // tree NO trae kind/source/cleared — el title "Claude Code" es cosmético. El único discriminador fiable es llamar resume show y filtrar por source==agent-hook.

Router de fixtures actual (test/host/contract.test.js) — HAY DOS routers a extender:
  1. fakeExecFromFixtures() líneas 35-49 (callback/sync style) — usado por opts.exec
  2. instantiateHost('cmux').run async líneas 70-84 — usado por opts.run
  Ambos enrutan por `const argv = (args||[]).join(' ')` y comparan con includes(...).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmux binary → host adapter | stdout NO confiable del proceso externo cmux cruza hacia datos de descubrimiento |
| argv → execFile | el `--surface <ref>` proviene de la salida JSON de cmux (paso-1), no de input de usuario; cruza hacia el proceso hijo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | parseo de stdout cmux en `listAgentSurfaces`/`normalizeSurface` | mitigate | never-throws D-05: `JSON.parse` en try/catch; `normalizeSurface` valida `typeof checkpoint_id === 'string'` y `typeof cwd === 'string'`, devuelve `null` ante shapes inesperados. JSON corrupto del tree → return `[]`; de un resume show individual → skip fila. |
| T-55-02 | Denial of Service | el bucle de fan-out de `resume show` | mitigate | try/catch DENTRO del bucle (D-05 fila-a-fila): un `not_found`/parse error individual hace `continue`, jamás throw. Solo el fallo del paso-1 (`tree`) retorna `[]`. El panel ink del consumer (Phase 56) permanece montado. timeout 5s ya en el `run` DI. |
| T-55-03 | Elevation/Tampering | command injection vía argv en exec | accept | `execFile`/`execFileSync` con array argv literal (NO shell, NO interpolación) — patrón existente de `src/host/cmux.js`. El `--surface <ref>` viene del JSON de cmux, no de input externo. Bajo riesgo: sin shell, sin string concat. |
| T-55-04 | Information disclosure | el `cwd` devuelto (path absoluto) fluye al consumer | accept | Este método NO usa el `cwd` — solo lo devuelve como dato. El consumer downstream (Phase 56) lo pasa por `sanitizeAdoptionData` (BIDIR-08) antes de cualquier uso/POST. Fuera de scope de esta fase. |
| T-55-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — esta fase NO instala paquetes (solo builtins de Node + fixture JSON). RESEARCH §Package Legitimacy Audit: sin superficie de slopsquatting. |

Block on high-severity. Todos los threats aquí son low (descubrimiento read-only, never-throws, sin shell, sin endpoints, sin persistencia).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — fixtures golden de cmux 0.64.16 + @typedef AgentSurface</name>
  <files>test/fixtures/cmux/surface-resume-show.json, test/fixtures/cmux/surface-tree.json, src/host/interface.js</files>
  <read_first>
    - test/fixtures/cmux/list-workspaces.json (molde de fixture cruda a replicar)
    - .planning/phases/55-contrato-hostprovider-describesurface-cmux/55-RESEARCH.md §"Code Examples" (shapes reales 0.64.16 + comando exacto de captura) y §"Wave 0 Gaps"
    - .planning/phases/55-contrato-hostprovider-describesurface-cmux/55-PATTERNS.md §"surface-resume-show.json (CREATE)"
    - src/host/interface.js (el @typedef WorkspaceInfo a espejar, líneas 14-24; HOST_METHODS líneas 34-39 NO cambia)
  </read_first>
  <action>
    Capturar la salida CRUDA REAL del binario cmux 0.64.16 (build 96), NO inventar JSON. Correr `cmux --version` y confirmar `0.64.16 (96)`. Si el binario no resuelve o no hay sesión claude viva, usar los shapes verificados de RESEARCH §"Code Examples" verbatim como base y anotar la limitación.

    Crear `test/fixtures/cmux/surface-tree.json` con la salida de `cmux tree --all --json --id-format both` (shape con `windows[].workspaces[].panes[].surface_refs[]` + `surfaces[]`). Debe contener al menos 2 surface_refs (p. ej. `surface:1` claude y `surface:2` no-claude) para alimentar el paso-1 de enumeración.

    Crear `test/fixtures/cmux/surface-resume-show.json` como un MAPA `surfaceRef → showOutput` (objeto por ref, NO un array — `surface resume show` devuelve UN objeto por surface, Pitfall 1). Debe incluir CUATRO casos para ejercer D-05 en el mismo test:
      (1) adoptable: `cleared:false`, `resume_binding.source:"agent-hook"`, `kind:"claude"`, `checkpoint_id` UUID string, `cwd` absoluto string;
      (2) `cleared:true`;
      (3) sin `resume_binding` (clave ausente);
      (4) `source:"environment"` (o `"tmux"`) ≠ `agent-hook`.
    Anotar la versión `cmux 0.64.16 (96)` en un campo/comentario sidecar o en el nombre de un caso (Pitfall 2: NUNCA documentar 0.64.15).

    En `src/host/interface.js`: añadir el JSDoc `@typedef {Object} AgentSurface` con las 4 props camelCase: `workspaceRef` (string), `cwd` (string, ← resume_binding.cwd), `sessionId` (string, ← resume_binding.checkpoint_id == session_id de Claude Code), `kind` (string, ← resume_binding.kind; el consumer filtra, NO este método). Espejo de estilo del `@typedef WorkspaceInfo` pero camelCase consciente (D-02). NO tocar `HOST_METHODS`, `validateHost`, `createNullHost`, ni `getHost` en esta task.
  </action>
  <verify>
    <automated>node -e "const m=require('/Users/alex/dev/klab/kodo/test/fixtures/cmux/surface-resume-show.json'); const vals=Object.values(m); if(!vals.some(v=>v&&v.resume_binding&&v.resume_binding.source==='agent-hook'&&v.cleared===false)) throw new Error('falta caso adoptable'); if(!vals.some(v=>v&&v.cleared===true)) throw new Error('falta cleared:true'); if(!vals.some(v=>v&&!v.resume_binding)) throw new Error('falta sin resume_binding'); if(!vals.some(v=>v&&v.resume_binding&&v.resume_binding.source!=='agent-hook')) throw new Error('falta source!=agent-hook'); console.log('fixture OK')"</automated>
    <automated>node -e "const t=require('/Users/alex/dev/klab/kodo/test/fixtures/cmux/surface-tree.json'); JSON.stringify(t); console.log('tree fixture parseable')"</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/cmux/surface-resume-show.json` existe, es JSON válido, mapa por surfaceRef con ≥1 caso adoptable (`source==agent-hook` ∧ `cleared==false`) + casos `cleared:true`, sin `resume_binding`, `source!=agent-hook`.
    - El caso adoptable contiene `resume_binding.checkpoint_id` (UUID string), `resume_binding.cwd` (path absoluto string), `resume_binding.kind:"claude"`, `workspace_ref`.
    - `test/fixtures/cmux/surface-tree.json` existe, JSON válido, con ≥2 surface_refs bajo `windows[].workspaces[].panes[]`.
    - La versión `0.64.16` queda anotada en la fixture (NO 0.64.15).
    - `src/host/interface.js` contiene `@typedef {Object} AgentSurface` con las 4 props camelCase; `grep -c 'AgentSurface' src/host/interface.js` ≥ 1.
    - `HOST_METHODS` sigue siendo exactamente `['listWorkspaces','selectWorkspace','isAlive','needsInput']` (sin cambios).
  </acceptance_criteria>
  <done>Ambas fixtures existen y parsean; los 4 casos D-05 presentes; `@typedef AgentSurface` añadido a interface.js; HOST_METHODS intacto en 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: listAgentSurfaces() + normalizeSurface(raw) en src/host/cmux.js</name>
  <files>src/host/cmux.js, test/host/contract.test.js</files>
  <behavior>
    - normalizeSurface(adoptable) → { workspaceRef, cwd, sessionId, kind } con sessionId === resume_binding.checkpoint_id, cwd === resume_binding.cwd, kind === resume_binding.kind.
    - normalizeSurface({cleared:true,...}) → null.
    - normalizeSurface({...sin resume_binding}) → null.
    - normalizeSurface({resume_binding:{source:'environment',...}}) → null.
    - normalizeSurface con checkpoint_id no-string o cwd no-string → null.
    - listAgentSurfaces() con tree fixture + resume show fixtures → array solo con surfaces adoptables (las inválidas omitidas).
    - listAgentSurfaces() cuando el run del paso-1 (tree) throws → [] (fail-open), nunca lanza.
    - listAgentSurfaces() cuando un resume show individual throws → esa surface se omite, el resto del array sobrevive (fila-a-fila), nunca lanza.
  </behavior>
  <read_first>
    - src/host/cmux.js (el molde completo de listWorkspaces líneas 62-118; el run DI líneas 24-43; el return shape línea 178; el bloque _legacy NO se toca)
    - .planning/phases/55-contrato-hostprovider-describesurface-cmux/55-RESEARCH.md §"Pattern 1" y §"Pattern 2" (esqueleto verificado de la enumeración 2-pasos + normalizeSurface)
    - .planning/phases/55-contrato-hostprovider-describesurface-cmux/55-PATTERNS.md §"src/host/cmux.js — listAgentSurfaces()"
    - test/host/contract.test.js (los DOS routers de fixtures: fakeExecFromFixtures líneas 35-49 y instantiateHost run líneas 70-84)
  </read_first>
  <action>
    En `src/host/cmux.js`, definir un helper puro a SCOPE DE MÓDULO (fuera de createCmuxHost, sin I/O) `normalizeSurface(raw)`: devuelve `null` si `!raw || raw.cleared === true`; si no hay `raw.resume_binding`; si `b.source !== 'agent-hook'`; o si `typeof b.checkpoint_id !== 'string' || typeof b.cwd !== 'string'`. En el caso válido devuelve `{ workspaceRef: raw.workspace_ref, cwd: b.cwd, sessionId: b.checkpoint_id, kind: b.kind }` (mapeo D-02). NO filtrar por `kind=='claude'` aquí (D-05 — el consumer decide).

    Definir un helper de extracción del paso-1 (puro o inline) `extractSurfaceRefs(treeJson)` que recorre `windows[].workspaces[].panes[].surface_refs[]` (o `panes[].surfaces[].ref`) y devuelve la lista de refs de surface vivas a consultar. Defensivo ante claves ausentes (devuelve `[]` si el shape no encaja).

    Dentro de `createCmuxHost`, añadir la función async `listAgentSurfaces()` espejando el molde never-throws de `listWorkspaces`:
      Paso 1 (enumerar): `treeRaw = await run(['tree','--all','--json','--id-format','both'])` en try/catch — exec error → `logger?.warn?.('host.list_agent_surfaces.fail', {code: err?.code||'EXEC_ERROR', detail, duration_ms})` + `return []`. Luego `JSON.parse(treeRaw)` en un segundo try/catch → `code:'PARSE_ERROR'` + `return []`. Extraer refs con `extractSurfaceRefs`.
      Paso 2 (fan-out): `for (const ref of refs)`: try `raw = JSON.parse(await run(['surface','resume','show','--json','--surface',ref]))`; catch → `continue` (D-05 fila-a-fila, NO return — el try/catch va DENTRO del bucle, Pitfall 3). `const s = normalizeSurface(raw); if (s) out.push(s);`.
      Final: `logger?.info?.('host.list_agent_surfaces.ok', {count: out.length, duration_ms: Date.now()-started}); return out;`.

    Añadir un comentario JSDoc sobre la función documentando: "OPTIONAL (NOT in HOST_METHODS — FROZEN at 4). Detected via `typeof host.listAgentSurfaces === 'function'`." y referenciando DETECT-01.

    Añadir `listAgentSurfaces` al objeto retornado: `return { listWorkspaces, selectWorkspace, isAlive, needsInput, listAgentSurfaces, _legacy };`. NO tocar `_legacy`. NO importar `src/logger.js` (LOG-12). NO tocar `adopt.js`/`reconcile.js`.

    En `test/host/contract.test.js`, extender AMBOS routers de fixtures (fakeExecFromFixtures Y el run async de instantiateHost) con ramas: `argv.includes('tree')` → sirve TREE_FIXTURE; `argv.includes('surface resume show')` → sirve el showOutput correcto según el `--surface <ref>` presente en el argv (enrutar por ref). Cargar las fixtures nuevas con `readFileSync` al tope del archivo (molde líneas 14-17). Estas extensiones de test pueden hacerse en esta task junto al código que verifican.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/host/contract.test.js 2>&1 | tail -20</automated>
    <automated>cd /Users/alex/dev/klab/kodo && grep -v '^//' src/host/cmux.js | grep -c 'listAgentSurfaces'</automated>
  </verify>
  <acceptance_criteria>
    - `src/host/cmux.js` contiene `function normalizeSurface` a scope de módulo y `async function listAgentSurfaces` dentro de createCmuxHost.
    - El objeto retornado por createCmuxHost incluye `listAgentSurfaces` (return línea ~178 actualizada); `_legacy` sin cambios.
    - El try/catch del paso-2 está DENTRO del bucle `for` (verificable leyendo: `continue` en el catch, no `return`).
    - `normalizeSurface` devuelve `{workspaceRef, cwd, sessionId, kind}` con `sessionId === resume_binding.checkpoint_id` y `null` en los 4 casos inválidos.
    - `src/host/cmux.js` NO importa `src/logger.js` (`grep -c "logger.js" src/host/cmux.js` == 0).
    - `node --test test/host/contract.test.js` exits 0 (suite del archivo verde).
    - `grep -v '^//' src/host/cmux.js | grep -c 'listAgentSurfaces'` ≥ 2 (definición + return).
  </acceptance_criteria>
  <done>`listAgentSurfaces` + `normalizeSurface` implementados con enumeración 2-pasos never-throws; try/catch fila-a-fila dentro del bucle; método en el return; routers de fixtures extendidos; `node --test test/host/contract.test.js` verde.</done>
</task>

<task type="auto">
  <name>Task 3: Golden asserts campo a campo + casos fail-open + verificar walker + suite completa</name>
  <files>test/host/contract.test.js</files>
  <read_first>
    - test/host/contract.test.js (el molde del describe golden 'CmuxHost — derivación needs_input' líneas 142-171; el assert HOST_METHODS líneas 87-94 que NO debe romperse)
    - test/host/cmux-isolation.test.js (el walker SC#5 verify-only — NO modificar)
    - .planning/phases/55-contrato-hostprovider-describesurface-cmux/55-PATTERNS.md §"test/host/contract.test.js (MODIFY)" (el molde del nuevo describe)
    - .planning/phases/55-contrato-hostprovider-describesurface-cmux/55-VALIDATION.md (sampling + per-task map)
  </read_first>
  <action>
    En `test/host/contract.test.js`, añadir un nuevo `describe('CmuxHost — listAgentSurfaces (DETECT-01)', ...)` espejando el molde del describe golden existente (líneas 142-171), con estos tests:
      (1) "retorna AgentSurface[] con {workspaceRef,cwd,sessionId,kind}": llama `await host.listAgentSurfaces()`, assert `Array.isArray`, y campo a campo en el caso adoptable que `sessionId === <checkpoint_id de la fixture>`, `cwd === <cwd de la fixture>`, `kind === 'claude'`, `typeof workspaceRef === 'string'`.
      (2) "omite cleared:true / sin resume_binding / source!=agent-hook (D-05)": assert que ninguna surface inválida aparece en el array (length == número de adoptables en la fixture; ninguna con kind no-agente).
      (3) "tree falla → [] (fail-open D-05)": instanciar un host cmux con un `run` que throws cuando `argv.includes('tree')`; assert `deepEqual(await host.listAgentSurfaces(), [])` y que no lanza.
      (4) "un resume show individual falla → se omite esa surface, no rompe el array (D-05 fila-a-fila)": `run` que devuelve el tree fixture pero throws para UN `--surface <ref>` específico y sirve el resto; assert que el array contiene las surfaces restantes válidas (no vacío, no throw).

    Añadir el test de typeof-detection según la decisión de NullHost (Claude's Discretion): dado que `createNullHost` NO stubea el método (decisión: dejarlo ausente para documentar la rama de degradación del consumer), añadir `test('null host no implementa listAgentSurfaces (rama degradación)', () => assert.equal(typeof getHost('null').listAgentSurfaces, 'function' /* esperado: !== */))` — concretamente asertar `typeof getHost('null').listAgentSurfaces !== 'function'`. Esto documenta el call site de Phase 56.

    NO modificar `test/host/cmux-isolation.test.js`. NO añadir el método a `HOST_METHODS`. Verificar que el assert existente "HOST_METHODS es exactamente los 4 métodos" sigue verde (confirma D-03).

    Correr la suite completa del proyecto al final para confirmar cero regresiones.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/host/ 2>&1 | tail -25</automated>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/host/cmux-isolation.test.js 2>&1 | grep -E "pass|fail" | tail -3</automated>
    <automated>cd /Users/alex/dev/klab/kodo && node --test $(find test -name '*.test.js' -type f) 2>&1 | grep -E "^# (tests|pass|fail)" </automated>
  </verify>
  <acceptance_criteria>
    - El nuevo `describe('CmuxHost — listAgentSurfaces (DETECT-01)')` existe con ≥4 tests: shape campo a campo, omisión D-05, tree→[], fila-a-fila skip.
    - Hay un assert campo a campo de que `sessionId` proviene de `resume_binding.checkpoint_id` (el valor de la fixture).
    - Existe el test de typeof-detection: `typeof getHost('null').listAgentSurfaces !== 'function'`.
    - El assert "HOST_METHODS es exactamente los 4 métodos" sigue verde.
    - `node --test test/host/` exits 0.
    - `node --test test/host/cmux-isolation.test.js` reporta `fail 0` (walker verde sin cambios).
    - Suite completa (`node --test $(find test -name '*.test.js')`) reporta `# fail 0`.
  </acceptance_criteria>
  <done>Nuevo describe golden con asserts campo a campo + 3 casos fail-open + test de degradación typeof; walker SC#5 verde sin tocar; HOST_METHODS intacto; suite completa con `# fail 0`.</done>
</task>

</tasks>

<verification>
- `node --test test/host/` verde (contract matrix + golden listAgentSurfaces + walker).
- Suite completa del proyecto: `node --test $(find test -name '*.test.js' -type f)` → `# fail 0`.
- `grep -c 'AgentSurface' src/host/interface.js` ≥ 1 (typedef añadido).
- `grep -v '^//' src/host/cmux.js | grep -c 'listAgentSurfaces'` ≥ 2 (definición + return).
- `HOST_METHODS` sigue congelado en 4 (assert del contract test).
- `src/host/cmux.js` NO importa `src/logger.js` (LOG-12).
- `adopt.js`/`reconcile.js` NO modificados (regla transversal LOCKED).
- Fixtures anotadas con cmux `0.64.16` (NO 0.64.15).
</verification>

<success_criteria>
1. `listAgentSurfaces()` existe en `src/host/cmux.js`, FUERA de `HOST_METHODS`, detectable por `typeof` (SC#1 del roadmap).
2. Devuelve por surface `{ workspaceRef, cwd, sessionId (= resume_binding.checkpoint_id), kind }` mediante enumeración 2-pasos (`tree` → fan-out `surface resume show`) (SC#2 + corrección de premisa de RESEARCH).
3. La salida real de cmux 0.64.16 está fixture-lockeada y asertada campo a campo vía el `run` DI (SC#3 + DETECT-01(a)).
4. Modos fail-open manejados: `cleared:true` / sin `resume_binding` / `source!=agent-hook` se omiten fila-a-fila; tree falla → `[]`; never-throws (SC#4 + DETECT-01(b)).
5. Regla transversal preservada: todo lo cmux vive en `src/host/cmux.js`; el walker SC#5 verde; `adopt.js`/`reconcile.js` host-agnósticos (SC#5 + DETECT-01(c)).
</success_criteria>

<output>
Create `.planning/phases/55-contrato-hostprovider-describesurface-cmux/55-01-SUMMARY.md` when done
</output>
