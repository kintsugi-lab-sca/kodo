---
phase: 60-enriquecimiento-de-tareas-adoptadas-por-el-orquestador
plan: 01
status: complete
requirements: [BIDIR-F2]
subsystem: cli, orchestrator-skill
created: 2026-06-19
---

# Phase 60 Plan 01: Enriquecimiento de tareas adoptadas

## Objetivo

Una tarea adoptada acaba con información real (título inteligente — ya de Phase 57 — Y una descripción-resumen). Dos caminos: **at-adopt** (la tarea nace rellena vía `kodo adopt --description`) y **backfill** (tareas ya adoptadas se enriquecen con un comentario-resumen vía `addComment`). El LLM vive SOLO en el orquestador (prosa del skill); los consumidores CLI son deterministas 0-token.

## Decisión LOCKED (operador, 2026-06-19)
Backfill vía **`addComment`** (FROZEN-9), NO `updateTask`. Cero superficie de provider nueva.

## Tasks

- [x] **T-60-01-1 — CLI consumidor `kodo comment <ref>`** (`src/cli/comment.js` + registro en `src/cli.js`)
  - Thin handler espejo de `runAdoptCli`/`runGsdInspect`: argv → validación → resolver provider → `getTask(ref)` → sanear body (BIDIR-08) → `addComment(task, body)` → render.
  - Reusa `sanitizeAdoptionData` (`src/adopt.js`) como backstop determinista ANTES del POST.
  - Exit codes: 0 ok · 1 INVALID_INPUT (ref/body vacío) · 2 FETCH_FAILED/POST_FAILED (transient).
  - Color isolation (createFormatter de format.js); cero acceso a state/lock/cmux (dry-write sobre el provider).
  - NO añade métodos al contrato (reusa `addComment` FROZEN-9).

- [x] **T-60-01-2 — Tests `test/comment-cli.test.js`** (12 tests, espejo de adopt-cli)
  - INVALID_INPUT (ref/body vacío) → exit 1, provider nunca tocado.
  - Happy path → getTask + addComment llamados, exit 0, render ok.
  - Sanitización BIDIR-08 (rutas/home redactados antes del POST) + uso del sanitizer real por defecto.
  - getTask falla → FETCH_FAILED exit 2 (addComment NO llamado); addComment falla → POST_FAILED exit 2.
  - --json byte-determinista, parseable, sin ANSI con formatter TTY inyectado.
  - Wiring estático en src/cli.js (command + import + runCommentCli + --body).

- [x] **T-60-01-3 — Prosa del skill `kodo-orchestrate`** (`.claude/skills/kodo-orchestrate/skill.md` + espejo `src/orchestrator/prompt.md`)
  - §"Adopción asistida": paso 2b (derivar resumen-descripción, nunca verbatim, BIDIR-08) + §6 habilita `--description` (antes "OMITE --description, diferido").
  - Nueva sub-sección §"Backfill: enriquecer una tarea YA adoptada (`kodo comment`)": flujo numerado + mandato shell-seguro (single-quote en ref+body) + exit codes.
  - Mandato shell-seguro de Phase 57 extendido a `--description`/`--body` (free-text, fail-closed sobre `'`).

## Verificación

Ver `60-VERIFICATION.md`. Suite completa: 1489 pass / 0 fail / 1 skip. Walkers format/cmux-isolation 8/8.
</content>
