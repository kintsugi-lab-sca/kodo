---
phase: 35-datos-cliente-http-polling
plan: 03
type: execute
wave: 3
depends_on: [35-01, 35-02]
files_modified:
  - src/cli/dashboard/App.js
  - test/dashboard-status-line.test.js
autonomous: true
requirements: [TUI-06]
must_haves:
  truths:
    - "App refresca desde GET /status y muestra una status line viva: ● live + N sessions cuando conecta"
    - "Si el server cae a mitad, App conserva el último count (keep-last-good) y muestra ⚠ server caído + (last update Ns ago, retrying…)"
    - "Si el server está caído al arrancar (sin dato bueno), App muestra waiting for server sin contador"
    - "Una respuesta JSON corrupta se trata como poll fallido (mismo path que ECONNREFUSED), nunca como crash del render"
    - "Implementa D-02 (status line viva, no headless), D-06 (dos estados de degradación: waiting for server sin dato bueno vs stale/retrying con keep-last-good) y D-08 (la edad 'last update Ns ago' se recalcula en cada intento de poll, sin timer de 1s)"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "Status line viva: connection + N sessions + banner de degradación (D-01); cablea usePoll(fetchStatus)"
      contains: "usePoll"
    - path: "test/dashboard-status-line.test.js"
      provides: "Tests keep-last-good (succeed×2-then-throw) + dos estados (waiting/live) vía ink-testing-library"
      contains: "server caído"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "fetchStatus (client.js) vía usePoll"
      via: "usePoll envuelve fetchStatus(baseUrl, fetchFn, signal)"
      pattern: "usePoll\\("
    - from: "src/cli/dashboard/App.js"
      to: "ink <Text color>"
      via: "color de connection state SOLO de ink (cero picocolors)"
      pattern: "color:"
---

<objective>
Reemplazar el placeholder estático `starting…` de `App.js` (Phase 34) por una **status line viva**
(D-01): indicador de conexión (`● live` / `⚠ server caído`), contador `N sessions`, y banner de
degradación con edad (`last update Ns ago, retrying…`). Cablea `usePoll(fetchStatus, …)` (Planes
01+02) y mantiene el estado de **keep-last-good** + connection + edad en React, con **dos estados
de degradación** (D-06): `waiting for server` (arranque sin dato bueno) vs `stale, retrying`
(server cayó a mitad → conservar el último count). Cierra TUI-06 de forma observable.

Purpose: La capa de presentación del slice de datos — hace observable la resiliencia (keep-last-good,
server caído, JSON corrupto → poll fallido). Preserva color-isolation, lifecycle `q`→exit y el
markup `React.createElement` plano de Phase 34.
Output: `src/cli/dashboard/App.js` (MODIFICADO) + `test/dashboard-status-line.test.js` (NUEVO).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-datos-cliente-http-polling/35-CONTEXT.md
@.planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md
@.planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md
@.planning/phases/35-datos-cliente-http-polling/35-01-SUMMARY.md
@.planning/phases/35-datos-cliente-http-polling/35-02-SUMMARY.md

<interfaces>
<!-- App.js firma actual (Phase 34): export default function App({ baseUrl }) — NO cambia; baseUrl llega resuelto de index.js. -->
<!-- Markup base a preservar (src/cli/dashboard/App.js:49-55): Box column borderStyle round paddingX 1; -->
<!--   banner Text bold "kodo dashboard" (conservar), nodo central (← REEMPLAZA starting…), footer Text dimColor "q quit" (conservar). -->
<!-- Lifecycle a preservar (App.js:36-47): useApp().exit() en `q`; useInput gateado por useStdin().isRawModeSupported. -->

<!-- Contrato de fetchStatus (Plan 01): fetchStatus(baseUrl, fetchFn=globalThis.fetch, signal) → { ok:true, data } | { ok:false, error } -->
<!--   data.count (== sessions.length) es "N sessions" (D-01; VERIFICADO src/server.js:399). -->
<!-- Contrato de usePoll (Plan 02): usePoll(fn, onResult, deps, opts); opts inyecta clock/schedule/baseMs/maxMs. -->
<!--   onResult(result) lo llama App por tick para actualizar connection/keep-last-good. -->

<!-- Derivación de estado de render (D-06, Pattern 3 RESEARCH:248-271): -->
<!--   never had good     → 'waiting for server' (sin contador) -->
<!--   had good + !connected → 'stale'  → ⚠ server caído + "N sessions (last update Ns ago, retrying…)" (keep-last-good) -->
<!--   connected          → 'live'      → ● live + "N sessions" -->
<!-- Edad (D-08): se recalcula en cada intento de poll (lastAttemptAt - lastGoodAt), NO con timer de 1s. -->
<!-- Inyectar fetchFn + clock fakes en App vía props (igual que baseUrl en test/dashboard-render.test.js:40) para render hermético. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Wave 0 de la status line (keep-last-good + dos estados) vía ink-testing-library</name>
  <files>test/dashboard-status-line.test.js</files>
  <read_first>
    - test/dashboard-render.test.js (harness completo: render/lastFrame/frames/stdin; constraint ink-testing-library@4 sin waitUntilExit, líneas 21-77; inyección de baseUrl como prop línea 40)
    - .planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md (test keep-last-good líneas 369-385; observabilidad Nyquist líneas 466-473)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (sección test/dashboard-status-line.test.js, líneas 251-272)
    - .planning/phases/35-datos-cliente-http-polling/35-CONTEXT.md (mockup D-01 líneas 36-50; Specific Ideas líneas 178-188)
  </read_first>
  <behavior>
    - keep-last-good (TUI-06 load-bearing): `fetchFn` succeed×2 (devuelve `{ sessions:[{},{},{}], count:3 }`) luego throw (ECONNREFUSED) → tras avanzar el poll, `lastFrame()` SIGUE conteniendo "3 sessions" (NO blanqueado) Y contiene "server caído"/"retrying" (estado stale)
    - dos estados — waiting (D-06): `fetchFn` falla desde el primer tick (sin dato bueno) → `lastFrame()` contiene "waiting for server" y NO contiene un contador de sessions
    - dos estados — live: `fetchFn` resuelve ok → `lastFrame()` contiene "● live" y "N sessions" (N = data.count)
    - JSON corrupto = poll fallido (TUI-06/D-07): `fetchFn` con `json()` que lanza → mismo path que ECONNREFUSED (status line stale, keep-last-good si ya había dato), NUNCA crash del render (el árbol ink sobrevive: `lastFrame()` sigue devolviendo un frame)
  </behavior>
  <action>
    Crear `test/dashboard-status-line.test.js` con `import { render } from 'ink-testing-library'`,
    `import { createElement } from 'react'`, `import App from '../src/cli/dashboard/App.js'`, runner
    `node:test` + `node:assert/strict` (patrón de `test/dashboard-render.test.js:32-46`). Inyectar
    `fetchFn` y un clock/schedule fake en `App` vía props (igual que `baseUrl` ya se inyecta en
    `test/dashboard-render.test.js:40`) para que el render sea hermético — sin red ni timers reales.

    Construir los fakes de fetch por escenario del <behavior> (un `fetchFn` con contador interno para
    succeed×2-then-throw; uno que siempre falla; uno que siempre devuelve ok; uno con `json()` que lanza).
    Usar `frames`/`lastFrame()` para las aserciones (NO `waitUntilExit()` — no existe en el harness@4).
    Avanzar el loop disparando manualmente los callbacks del fake schedule (determinista). Aserciones con
    `assert.match(lastFrame(), /…/)`: keep-last-good (`/3 sessions/` Y `/server caído|retrying/`), waiting
    (`/waiting for server/`), live (`/● live/` Y `/3 sessions/`), JSON corrupto (el frame sigue existiendo
    y muestra estado stale, sin throw). Estado Wave 0: ROJO hasta que Task 2 modifique `App.js` para
    aceptar las props de inyección y renderizar la status line.
  </action>
  <verify>
    <automated>node --test test/dashboard-status-line.test.js 2>&1 | grep -qE "fail|not ok" && echo "RED-as-expected (App status line not yet implemented)" || echo "check"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-status-line.test.js` existe e importa `App` desde `../src/cli/dashboard/App.js` + `render` de `ink-testing-library`
    - Contiene un `it(...)` para cada uno de: keep-last-good, waiting, live, JSON-corrupto (`grep -c "it(" test/dashboard-status-line.test.js` ≥ 4)
    - El test keep-last-good asserta que `lastFrame()` conserva `3 sessions` Y muestra `server caído`/`retrying`
    - NO usa `waitUntilExit()` (`grep -c "waitUntilExit" test/dashboard-status-line.test.js` == 0)
    - Inyecta `fetchFn`/clock fakes en `App` vía props (sin red ni timers reales: `grep -c "globalThis.fetch" test/dashboard-status-line.test.js` == 0)
    - Antes de Task 2 el test está ROJO
  </acceptance_criteria>
  <done>El test cubre keep-last-good (succeed×2-then-throw), waiting/live y JSON corrupto vía ink-testing-library con props inyectadas; ROJO hasta que App rinda la status line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear usePoll + keep-last-good + status line en App.js hasta verde</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - src/cli/dashboard/App.js (archivo COMPLETO actual, 56 líneas: imports 25-26, useInput/lifecycle 36-47, markup 49-55 — preservar todo salvo el nodo central)
    - src/cli/dashboard/client.js (de Plan 01 — fetchStatus a envolver)
    - src/cli/dashboard/usePoll.js (de Plan 02 — el hook a consumir; su firma/opts)
    - .planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md (Pattern 3 keep-last-good líneas 248-271; D-01/D-06/D-08)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (sección App.js líneas 136-173; color pattern líneas 162-164)
    - test/dashboard-status-line.test.js (el test de Task 1 — contrato a satisfacer)
    - test/dashboard-render.test.js (los asserts de Phase 34 que NO deben romperse: banner, q→exit)
  </read_first>
  <action>
    Modificar `src/cli/dashboard/App.js`. Extender imports: añadir `useState`/`useEffect` de `'react'`
    (preservar `Box, Text, useApp, useInput, useStdin` de ink y `createElement`). Importar
    `{ fetchStatus } from './client.js'` y `{ usePoll } from './usePoll.js'`. Actualizar el bloque de
    comentario-cabecera para reflejar Phase 35 (status line viva en vez del placeholder D-02 de Phase 34).

    Extender la firma `App({ baseUrl })` para aceptar props de inyección opcionales (Discretion del markup):
    `App({ baseUrl, fetchFn, ...clockOpts })` o equivalente, de modo que los tests inyecten `fetchFn`/clock
    fakes (default a `globalThis.fetch` y el clock real en runtime). NO cambiar el nombre `baseUrl`.

    Estado React (Discretion Open Question 2 — keep-last-good vive en App): `lastGoodCount`, `lastGoodAt`,
    `connected`, `lastError`, `lastAttemptAt`. Definir `onResult(result)`: en `result.ok` →
    `setLastGoodCount(result.data.count ?? result.data.sessions.length)`, `setLastGoodAt(now)`,
    `setConnected(true)`, `setLastError(null)`; en `!result.ok` → `setConnected(false)`,
    `setLastError(result.error)` y **NO** tocar `lastGoodCount`/`lastGoodAt` (keep-last-good, D-06/Pitfall 5);
    siempre `setLastAttemptAt(now)` (la edad se recalcula por poll, D-08 — NO un timer de 1s). Cablear
    `usePoll((signal) => fetchStatus(baseUrl, fetchFn, signal), onResult, [baseUrl], clockOpts)`.

    Derivar el estado de render (puro, D-06): si nunca hubo dato bueno (`lastGoodAt == null`) y no conectado
    → `'waiting for server'` (mostrar "waiting for server", sin contador); si hubo dato y `!connected` →
    `'stale'` → `⚠ server caído` (Text color yellow/dimColor) + `N sessions (last update Ns ago, retrying…)`
    donde N = `lastGoodCount` y Ns = `Math.round((lastAttemptAt - lastGoodAt)/1000)`; si `connected` →
    `'live'` → `● live` (Text color green) + `N sessions`. Reemplazar SOLO el nodo central del markup
    (`src/cli/dashboard/App.js:53`, el `starting…`) por la status line; CONSERVAR el banner `kodo dashboard`
    (línea 52) y el footer `q quit` (línea 54) y el lifecycle `q`→exit/useInput gateado (36-47).

    Color SOLO de props `<Text color>` de ink (verde `● live`, yellow/dimColor `⚠ server caído`) — JAMÁS
    `import` de `picocolors` ni del helper de color del CLI clásico (invariante D-12, verificado por el walker).
    Markup vía `React.createElement` plano (no JSX, no build step).
  </action>
  <verify>
    <automated>node --test test/dashboard-status-line.test.js test/dashboard-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/App.js` consume `usePoll` y `fetchStatus` (`grep -q "usePoll(" src/cli/dashboard/App.js` y `grep -q "fetchStatus" src/cli/dashboard/App.js`)
    - El `onResult` en fallo NO setea `lastGoodCount`/`lastGoodAt` (keep-last-good) — verificable en el test keep-last-good (count conservado)
    - El markup ya NO contiene el placeholder `starting…` (`grep -c "starting…" src/cli/dashboard/App.js` == 0) y conserva `kodo dashboard` + `q quit`
    - La status line muestra `● live` / `⚠ server caído` / `waiting for server` según el estado derivado
    - Color SOLO de props `<Text>`: ningún `import` de `picocolors` (`grep -v '^//' src/cli/dashboard/App.js | grep -c picocolors` == 0)
    - El lifecycle `q`→`exit()` y `useInput` gateado por `isRawModeSupported` se preservan (`grep -q "exit()" src/cli/dashboard/App.js`)
    - `node --test test/dashboard-status-line.test.js` pasa (keep-last-good, waiting, live, JSON corrupto) — GREEN
    - `node --test test/dashboard-render.test.js` sigue verde (los asserts de Phase 34 NO se rompen; nota: el assert "starting…" de render.test ya no aplica — si rompe, el implementador debe actualizar ese assert al nuevo nodo central de forma mínima y justificada, NO eliminar la cobertura de banner/q-exit)
    - `node --test test/format-isolation.test.js` verde
  </acceptance_criteria>
  <done>App rinde la status line viva con keep-last-good + dos estados de degradación, cablea usePoll(fetchStatus); status-line + render + color-isolation verdes; sin picocolors; lifecycle q→exit preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/usePoll → App (render) | App renderiza el resultado del poll. En esta fase SOLO renderiza el **contador `N sessions`** (un número) + copy estático — NO texto libre de tareas. El riesgo de inyección ANSI (texto untrusted de `task_ref`/`repo`) NO aplica aquí: aparece en Phase 36 (tabla) y 38 (comments/logs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Denial of Service (crash del render) | `onResult` / status line ante poll fallido | mitigate | El cliente never-throws (`{ok:false}`, Plan 01) + keep-last-good en App: JSON corrupto / ECONNREFUSED / HTTP no-ok se tratan todos como poll fallido (D-07), el árbol ink NUNCA recibe un throw. Cubierto por el test "JSON corrupto = poll fallido" (el frame sobrevive). |
| T-35-06 | Tampering / Spoofing (UI, inyección ANSI) | render de texto untrusted | accept (esta fase) | Phase 35 solo renderiza el contador numérico `N sessions`, no texto libre de tareas — no hay superficie de inyección ANSI aquí. ink `<Text>` no interpreta escapes embebidos como control por default. **Documentado para Phase 36/38** (donde se renderiza `task_ref`/`repo`/comments). `[CITED: RESEARCH §Security Domain líneas 495-500]` |
| T-35-07 | Information Disclosure | `lastError` en pantalla | accept | El `error` mostrado es copy unificado ("server caído"/"retrying…", D-07), NO el `err.message` crudo del endpoint localhost; el `baseUrl` no se expone en el banner de degradación. Superficie local read-only. |
| T-35-SC | Tampering | npm installs | accept | Esta fase NO instala paquetes. Ver RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- `node --test test/dashboard-status-line.test.js` — keep-last-good, waiting/live, JSON corrupto verdes (TUI-06 observable)
- `node --test test/dashboard-render.test.js` — chrome + q→exit de Phase 34 preservados
- `node --test test/format-isolation.test.js` — color-isolation verde (App.js sin picocolors)
- `npm test` — suite completa verde antes del phase gate
</verification>

<success_criteria>
- La status line muestra `● live` + `N sessions` cuando conecta; `⚠ server caído` + `N sessions (last update Ns ago, retrying…)` cuando el server cae a mitad (keep-last-good); `waiting for server` al arrancar sin dato bueno (D-06).
- JSON corrupto / ECONNREFUSED / HTTP no-ok → poll fallido, nunca crash del render (TUI-06).
- Color SOLO de ink, lifecycle `q`→exit preservado, sin `picocolors`.
- Suites status-line + render + color-isolation verdes.
</success_criteria>

<output>
Crear `.planning/phases/35-datos-cliente-http-polling/35-03-SUMMARY.md` al terminar.
</output>
