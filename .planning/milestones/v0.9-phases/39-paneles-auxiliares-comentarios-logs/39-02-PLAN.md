---
phase: 39-paneles-auxiliares-comentarios-logs
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-overlay.test.js
autonomous: true
requirements: [TUI-15, TUI-16]

must_haves:
  truths:
    - "El operador pulsa `c` sobre la fila seleccionada y ve los comentarios de esa tarea (resueltos por task_id); 404/vacío/error muestran mensajes distintos; Esc vuelve al mismo cursor"
    - "El operador pulsa `l` y ve las líneas de log que coinciden por grep substring (task_ref/workspace_ref); no-match muestra mensaje claro; Esc vuelve al mismo cursor"
    - "El overlay de logs lleva una etiqueta honesta en el header: grep de buffer compartido, may include other sessions (SC#3)"
    - "El overlay es un snapshot congelado al abrir: el polling de la tabla sigue por debajo (keep-last-good intacto) pero el contenido del overlay no salta mientras el operador lee"
    - "En modo overlay, ↑/↓ scrollean el contenido (NO navegan filas); Esc cierra y restaura mode:'list' con selectedTaskId intacto"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "mode:'overlay' sub-mode + overlayKind + scrollOffset + overlaySnapshot + handlers c/l/Esc/scroll + literal-stable overlay copy constants"
      contains: "mode === 'overlay'"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "overlay chrome (header + scrollable body + footer hints + honesty label + empty/error copy)"
      contains: "mode === 'overlay'"
    - path: "test/dashboard-overlay.test.js"
      provides: "ink render tests: c abre comments, l abre logs, Esc cierra+restaura cursor, ↑↓ scroll, snapshot congelado, copy D-07, honesty label D-04"
  key_links:
    - from: "src/cli/dashboard/App.js (handler c/l)"
      to: "fetchComments / fetchLogs / grepLogs (Plan 39-01)"
      via: "await + freeze into overlaySnapshot"
      pattern: "fetchComments|fetchLogs|grepLogs"
    - from: "src/cli/dashboard/App.js (Esc en overlay)"
      to: "selectedTaskId (untouched) -> resolveSelection re-deriva el mismo row"
      via: "setMode('list') sin tocar selectedTaskId"
      pattern: "setMode\\('list'\\)"
---

<objective>
Implementar los dos overlays a pantalla completa sobre la fila seleccionada: `c` (comentarios por
task_id) y `l` (logs por grep substring), como un TERCER `mode:'overlay'` en el `useInput`
mode-gated de App.js, con chrome de overlay en SessionTable.js (header + body scrollable + footer +
etiqueta de honestidad D-04). Cierra TUI-15 y TUI-16.

Purpose: Es la capa de presentación/interacción que consume las tres funciones del Plan 39-01.
Las decisiones load-bearing: SNAPSHOT congelado (D-05, el texto no salta bajo el lector mientras el
poll sigue por debajo), SUB-MODO de scroll (D-06, ↑/↓ scrollean en vez de navegar filas), Esc
preserva el cursor GRATIS (selectedTaskId nunca se toca -> resolveSelection re-deriva la misma fila),
y la ETIQUETA HONESTA del overlay de logs (D-04, no es cosmética).

Output: App.js con mode:'overlay' + estado + handlers, SessionTable.js con render de overlay, suite
de overlay verde. Esto satisface SC#1, SC#2, SC#3 del ROADMAP (SC#4 lo cubrió el Plan 39-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-paneles-auxiliares-comentarios-logs/39-CONTEXT.md
@.planning/phases/39-paneles-auxiliares-comentarios-logs/39-PATTERNS.md
@.planning/phases/39-paneles-auxiliares-comentarios-logs/39-01-SUMMARY.md

<interfaces>
<!-- Contratos exactos del codebase + del Plan 39-01. El executor NO explora. -->

Del Plan 39-01 (ya implementadas en Wave 1):
- `fetchComments(baseUrl, taskId, fetchFn, signal)` -> `{ok:true, data:{comments}}` |
  `{ok:false, error, code:'not-found'|'http'|'network', status?}`. comments:[] es ok (vacío no es error).
- `fetchLogs(baseUrl, fetchFn, signal)` -> `{ok:true, data:{logs}}` | `{ok:false, error}`.
- `grepLogs(logs, session)` -> `Array<{ts,level,msg}>` filtradas por substring OR de task_ref/workspace_ref.

App.js — estado actual (lines 153-162) a EXTENDER:
```
const [sessions, setSessions] = useState([]);
const [selectedTaskId, setSelectedTaskId] = useState(null);
const [mode, setMode] = useState(/* 'list' | 'filter' */ ('list'));
const [query, setQuery] = useState('');
const prevIndexRef = useRef(0);
```

App.js — pipeline de derivación (lines 198-204): `sorted -> filtered -> sel = resolveSelection(...)`.
La fila seleccionada se lee con `const row = sel.index >= 0 ? filtered[sel.index] : null;` (lines 284-285).

App.js — useInput callback YA es `async` (line 214) y ya hace `await onFocus(...)` (line 296). El
clear-on-any-input de focusError/hostError corre ANTES del mode-gate (lines 221-229) — preservar ese orden.

App.js — fetchFn ya es prop de App (line 116, default globalThis.fetch); baseUrl ya es prop (line 99).
NO hace falta tocar index.js — fetchComments/fetchLogs usan baseUrl+fetchFn que App ya tiene.

App.js — constantes literal-estables exportadas (lines 72-87, FOCUS_ERR_*/HOST_ERR_*) — patrón a imitar.

SessionTable.js — filterLine/errorLine condicional (lines 138-159) + `cell({width,text,color,dim,bold,truncate})`
helper (lines 49-59). STATE_BADGES/colors via `<Text color="...">` string names (format.js lines 121-126).

Test harness — test/dashboard-table.test.js lines 44-110: `makeFakeClock` (intercepta el setTimeout
recursivo de usePoll -> NO cuelga), `injectProps(clock, fetchFn)`, `drain()` (doble setImmediate),
`okResponse`. Conducir teclado con `stdin.write(...)`: ↑='\x1b[A' ↓='\x1b[B' Esc='\x1b' 'c'/'l' literal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mode:'overlay' en App.js — estado, handlers c/l/Esc/scroll, snapshot congelado</name>
  <files>src/cli/dashboard/App.js, test/dashboard-overlay.test.js</files>
  <read_first>
    - src/cli/dashboard/App.js (el archivo a modificar; estado lines 153-162, useInput lines 213-313, render lines 330-353, constantes lines 72-87)
    - src/cli/dashboard/client.js (fetchComments/fetchLogs del Plan 39-01)
    - src/cli/dashboard/select.js (grepLogs del Plan 39-01; resolveSelection lines 74-80)
    - test/dashboard-table.test.js (harness makeFakeClock/injectProps/drain/okResponse — reusar VERBATIM)
  </read_first>
  <behavior>
    - `c` en mode:'list' con una fila seleccionada -> await fetchComments(baseUrl, row.task_id, fetchFn)
      -> congela el resultado en overlaySnapshot, setMode('overlay'), setOverlayKind('comments'), setScrollOffset(0).
    - `l` en mode:'list' con una fila seleccionada -> await fetchLogs(baseUrl, fetchFn) -> si ok, corre
      grepLogs(data.logs, {task_ref: row.task_ref, workspace_ref: row.workspace_ref}) -> congela las líneas
      matcheadas en overlaySnapshot; si !ok, congela el estado de error. setMode('overlay'), setOverlayKind('logs').
    - En mode:'overlay': ↑ -> setScrollOffset(o => Math.max(0, o-1)); ↓ -> setScrollOffset(o => o+1 con clamp
      al máximo derivado del tamaño del snapshot); Esc -> setMode('list') + setOverlayKind(null) (NO toca
      selectedTaskId -> cursor preservado); cualquier otra tecla se traga (early return) — D-05/D-06.
    - El poll de la tabla SIGUE corriendo bajo el overlay (NO se detiene usePoll); overlaySnapshot NO se
      re-escribe por onResult -> el contenido del overlay no salta (D-05).
    - Esc en mode:'list' sigue ignorado salvo cuando hay overlay (la rama overlay lo consume primero).
  </behavior>
  <action>
    Modificar `src/cli/dashboard/App.js`. NO añadir un segundo useInput; extender el existente.

    1) Constantes literal-estables EXPORTADAS (imitar FOCUS_ERR_* lines 72-87, para que los tests las
       importen sin duplicar strings — D-07):
       - `OVERLAY_COMMENTS_EMPTY = 'no comments yet'`
       - `OVERLAY_COMMENTS_NOT_FOUND = 'task not found'`
       - `OVERLAY_COMMENTS_ERROR = 'error fetching comments'`
       - `OVERLAY_LOGS_EMPTY = 'no log lines match this session'`
       - `OVERLAY_LOGS_ERROR = 'error fetching logs'`
       - `OVERLAY_LOGS_LABEL = 'grep of shared buffer — may include other sessions'` (D-04, SC#3 — load-bearing).

    2) Importar `fetchComments`, `fetchLogs` de './client.js' (junto a fetchStatus) y `grepLogs` de
       './select.js' (junto a los imports existentes de select.js).

    3) Estado nuevo (mirror de las useState lines 153-162):
       - Ensanchar el tipo de `mode` a `'list' | 'filter' | 'overlay'`.
       - `const [overlayKind, setOverlayKind] = useState(/* 'comments'|'logs'|null */ (null));`
       - `const [scrollOffset, setScrollOffset] = useState(0);`
       - `const [overlaySnapshot, setOverlaySnapshot] = useState(null);` — objeto FROZEN al abrir. Forma
         sugerida: `{ kind:'comments'|'logs', taskRef, status:'ok'|'empty'|'not-found'|'error', lines: string[] }`
         donde `lines` son strings ya proyectadas (comentarios como texto, o `msg` de cada log entry).

    4) En el useInput callback (que ya es async): añadir la RAMA overlay como guard al TOPE del mode-gate,
       DESPUÉS del clear-on-any-input de focusError/hostError (lines 221-229) pero ANTES de `mode==='filter'`:
       ```
       if (mode === 'overlay') {
         if (key.escape) { setMode('list'); setOverlayKind(null); return; }
         if (key.upArrow) { setScrollOffset((o) => Math.max(0, o - 1)); return; }
         if (key.downArrow) { setScrollOffset((o) => o + 1); return; }   // clamp en el render (Task 2) o vs lines.length
         return; // tragar el resto mientras se lee
       }
       ```
       (El clamp superior del scroll puede vivir aquí contra `overlaySnapshot.lines.length` o en el render —
       Claude's Discretion; lo importante es no pasar de la última línea.)

    5) En el bloque `mode==='list'` añadir los handlers `c` y `l` (imitar los handlers `'q'`/`'/'` lines 257-264
       y el handler async de Enter lines 276-309 que ya `await`a):
       - `if (input === 'c') { const row = sel.index >= 0 ? filtered[sel.index] : null; if (!row) return;
         const res = await fetchComments(baseUrl, row.task_id, fetchFn); ...congelar snapshot...; return; }`
         Mapear res a snapshot: `res.ok && comments.length>0` -> status:'ok', lines = comentarios proyectados;
         `res.ok && comments.length===0` -> status:'empty'; `!res.ok && res.code==='not-found'` -> status:'not-found';
         `!res.ok` (otro) -> status:'error'. Luego setOverlayKind('comments'), setScrollOffset(0), setMode('overlay').
       - `if (input === 'l') { const row = ...; if (!row) return; const res = await fetchLogs(baseUrl, fetchFn);
         if (res.ok) { const matched = grepLogs(res.data.logs, {task_ref: row.task_ref, workspace_ref: row.workspace_ref});
         status = matched.length ? 'ok' : 'empty'; lines = matched.map(e => proyección de {ts,level,msg}); }
         else { status='error'; lines=[]; } ...congelar snapshot, setOverlayKind('logs'), setScrollOffset(0), setMode('overlay'); }`
       - D-06 cursor GRATIS: NO tocar selectedTaskId en open ni en close. resolveSelection re-deriva la misma
         fila al volver.

    6) En el render (lines 330-353): pasar las props del overlay a SessionTable (Task 2 las consume):
       `mode, overlayKind, scrollOffset, overlaySnapshot` (junto a las props ya existentes). Mirror de cómo
       focusError/hostError se threadean (lines 348-349). El footer de hints inferior (line 352) puede
       permanecer; SessionTable decide qué renderiza según mode.

    7) Tests (Wave 0 RED primero): crear `test/dashboard-overlay.test.js` reusando makeFakeClock/injectProps/
       drain/okResponse de dashboard-table.test.js. fetchFn fake debe responder distinto según la URL
       (`/status` -> sesiones, `/comments/<id>` -> comentarios, `/logs` -> buffer). Casos:
       - `c` abre overlay de comentarios (lastFrame contiene el task_ref o un comentario).
       - `c` sobre tarea 404 -> lastFrame contiene OVERLAY_COMMENTS_NOT_FOUND.
       - `c` sin comentarios -> OVERLAY_COMMENTS_EMPTY.
       - `l` abre overlay de logs con la etiqueta OVERLAY_LOGS_LABEL visible (D-04/SC#3).
       - `l` sin matches -> OVERLAY_LOGS_EMPTY.
       - Esc cierra el overlay y vuelve a la tabla con el MISMO cursor (gutter en la misma fila).
       - snapshot congelado: tras abrir, un flushTick del poll con datos distintos NO cambia el contenido del overlay.
       DISCIPLINA: cada test que renderiza App DEBE capturar el handle de render y llamar `unmount()` en un
       `finally` (el harness usa fake clock que intercepta el setTimeout de usePoll, pero unmount es el
       cinturón de seguridad contra cuelgues del runner — lección de fases recientes).
  </action>
  <verify>
    <automated>node --test test/dashboard-overlay.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mode === 'overlay'" src/cli/dashboard/App.js` matches.
    - `grep -n "fetchComments\|fetchLogs\|grepLogs" src/cli/dashboard/App.js` matches las tres.
    - `grep -cE "export const OVERLAY_" src/cli/dashboard/App.js` >= 6 (constantes literal-estables exportadas).
    - `grep -n "new RegExp\|picocolors" src/cli/dashboard/App.js` returns nothing.
    - `node --test test/dashboard-overlay.test.js` exits 0 con: c abre comments, 404->not found, empty->no comments yet, l abre logs+label, no-match->no log lines, Esc restaura cursor, snapshot congelado.
    - `node --test test/dashboard-table.test.js` exits 0 (navegación/filtro de Phase 36 intactos — no regresión).
  </acceptance_criteria>
  <done>mode:'overlay' enruta c/l/Esc/scroll, snapshot congelado bajo el poll vivo, Esc preserva el cursor, suite de overlay verde y la de tabla sin regresión.</done>
</task>

<task type="auto">
  <name>Task 2: overlay chrome en SessionTable.js — header + body scrollable + footer + etiqueta honesta</name>
  <files>src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js (el archivo a modificar; cell lines 49-59, filterLine/errorLine lines 138-159, render lines 161-231)
    - src/cli/dashboard/App.js (las props overlayKind/scrollOffset/overlaySnapshot + constantes OVERLAY_* del Task 1)
    - src/cli/dashboard/format.js lines 121-126 (STATE_BADGES color names para el color via <Text color>)
  </read_first>
  <action>
    Modificar `src/cli/dashboard/SessionTable.js` para renderizar el overlay a PANTALLA COMPLETA cuando
    `mode === 'overlay'` (D-01: "ocupa el área de la tabla"). Patrón elegido: early-return de una rama
    overlay al TOPE de SessionTable ANTES del render de la tabla (mantiene SessionTable como único punto
    de render; alternativa de componente sibling Overlay.js permitida pero añade un archivo — preferir
    early-return salvo que el executor lo juzgue más limpio separarlo, en cuyo caso el nuevo archivo copia
    los mismos patrones cell/filterLine y queda cubierto por format-isolation walker).

    Añadir props a la firma de SessionTable: `mode` (ya existe), `overlayKind=null`, `scrollOffset=0`,
    `overlaySnapshot=null`. Importar las constantes OVERLAY_* de App.js (o recibirlas como copy ya
    resuelta — Discretion; preferir importar para matar drift code/test).

    Rama overlay (early-return cuando `mode === 'overlay' && overlaySnapshot`):
    - HEADER (Box marginBottom/row): título según kind. Comments -> `comments · <taskRef>` en color 'cyan'
      bold. Logs -> `logs · <taskRef>` bold SEGUIDO de la ETIQUETA HONESTA en línea propia con
      OVERLAY_LOGS_LABEL en color 'yellow' o dimColor (D-04/SC#3 — OBLIGATORIA, load-bearing, no cosmética).
    - BODY: según overlaySnapshot.status:
      - 'ok' -> render de `overlaySnapshot.lines` en un `Box{flexDirection:'column'}` (mirror del body de
        la tabla line 229), SLICEADO por scrollOffset contra una altura de viewport (tamaño del viewport =
        Discretion; p.ej. 15-20 líneas). Usar el helper `cell` o un `<Text>` simple por línea; para logs,
        proyectar `{ts,level,msg}` ya viene como string desde App (Task 1).
      - 'empty' (comments) -> OVERLAY_COMMENTS_EMPTY dimColor.
      - 'empty' (logs) -> OVERLAY_LOGS_EMPTY dimColor.
      - 'not-found' -> OVERLAY_COMMENTS_NOT_FOUND (color 'red' o dim).
      - 'error' -> OVERLAY_COMMENTS_ERROR / OVERLAY_LOGS_ERROR según kind (color 'red').
      (mirror del `emptyCopy` lines 169-177 — una sola línea dim/red).
    - FOOTER (Box marginTop:1): hint `↑↓ scroll · Esc close` dimColor.
    - COLOR-ISOLATION (D-12): todo color via `<Text color="...">` string names de ink (cyan/yellow/red/
      dimColor). CERO picocolors, CERO import de src/cli/format.js de color, CERO ANSI inline. El walker
      test/format-isolation.test.js cubre este archivo automáticamente.

    NO tocar la rama de tabla existente (mode list/filter) — el overlay es un early-return adicional. Las
    ramas de estados vacíos de la tabla (lines 161-178) y el render de filas (lines 193-231) quedan intactas.
  </action>
  <verify>
    <automated>node --test test/dashboard-overlay.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mode === 'overlay'" src/cli/dashboard/SessionTable.js` matches.
    - `grep -n "scrollOffset" src/cli/dashboard/SessionTable.js` matches (el body se sliceа por scroll).
    - El overlay de logs renderiza la etiqueta honesta (OVERLAY_LOGS_LABEL) — verificado por el test del Task 1.
    - `node --test test/dashboard-overlay.test.js` exits 0 (header/body/footer/etiqueta/empty/error/scroll renderizados).
    - `node --test test/format-isolation.test.js` exits 0 (cero picocolors en SessionTable.js).
    - `node --test test/dashboard-table.test.js` exits 0 (la tabla list/filter sin regresión).
  </acceptance_criteria>
  <done>SessionTable renderiza el overlay full-screen (header + body scrollable + footer + etiqueta honesta D-04) con color SOLO de ink; tabla normal sin regresión.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard TUI -> server localhost | Render de datos leídos de `/comments`/`/logs` (vía Plan 39-01). El render no muta nada en el server. |
| log buffer compartido -> overlay visible | El overlay de logs muestra líneas de un buffer compartido sin session_id; puede incluir líneas de otras sesiones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Information Disclosure | overlay de logs sobre buffer compartido | accept + disclose | El grep best-effort puede mostrar líneas de otras sesiones (no hay session_id por línea). NO se oculta: la ETIQUETA HONESTA OVERLAY_LOGS_LABEL ("may include other sessions") en el header lo declara explícitamente (D-04/SC#3, acceptance criteria Task 2). Riesgo bajo: logs locales del propio operador. |
| T-39-04 | Denial of Service (render) | overlaySnapshot con datos del server (comentarios/logs largos) | mitigate | Snapshot congelado + viewport sliceado por scrollOffset -> el render no intenta pintar miles de líneas de golpe (slice acotado por la altura del viewport). El poll sigue bajo el overlay sin re-escribir el snapshot (D-05) -> no hay thrash de re-render. |
| T-39-05 | Tampering/render-injection | strings de comentarios/logs renderizados como `<Text>` | accept | ink renderiza texto plano en TUI; no hay interpretación HTML/markup. Secuencias ANSI embebidas en un msg de log son un riesgo teórico menor (logs del propio server local), no mitigado en v1 — fuera del scope read-only del milestone. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 39 NO añade dependencias (ink/react ya instalados, fetch built-in). Cero install tasks. |
</threat_model>

<verification>
- `node --test test/dashboard-overlay.test.js` verde (c/l abren, copy D-07 distinta por caso, etiqueta D-04, Esc restaura cursor, snapshot congelado, scroll).
- `node --test test/dashboard-table.test.js` verde (navegación/filtro Phase 36 sin regresión).
- `node --test test/format-isolation.test.js` verde (cero picocolors en App.js/SessionTable.js).
- (gate de fase) `node --test` de la suite completa de dashboard verde.
</verification>

<success_criteria>
- SC#1: `c` muestra comentarios resueltos por task_id; 404/vacío/error distintos; Esc vuelve al mismo cursor.
- SC#2: `l` muestra líneas grep substring (task_ref/workspace_ref); no-match claro; Esc vuelve al mismo cursor.
- SC#3: el overlay de logs lleva la etiqueta honesta de buffer compartido ("may include other sessions").
- Snapshot congelado (D-05): el poll sigue por debajo pero el contenido del overlay no salta.
- Sub-modo de scroll (D-06): ↑/↓ scrollean en overlay; Esc cierra sin tocar selectedTaskId.
</success_criteria>

<output>
Create `.planning/phases/39-paneles-auxiliares-comentarios-logs/39-02-SUMMARY.md` when done.
</output>
