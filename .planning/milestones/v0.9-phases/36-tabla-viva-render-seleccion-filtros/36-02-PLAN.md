---
phase: 36-tabla-viva-render-seleccion-filtros
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-table.test.js
autonomous: true
requirements: [TUI-07, TUI-09, TUI-10, TUI-11]
must_haves:
  truths:
    - "App renders a columnar table of active sessions with task_ref · repo · phase/mode · status · age, built from ink <Box>/<Text> only — never formatTable/formatRow or picocolors (TUI-07, D-01) — with fixed-width columns + ellipsis truncation via React.createElement plano (D-02)"
    - "Rows are ordered stably DESC by started_at (newest first) and do not jump between polls (TUI-09)"
    - "The status cell is colored by status+alive via ink <Text color>, and the zombie running+!alive shows the textual '(zombie)' marker (TUI-10)"
    - "The header shows the live indicator (reused from Phase 35 connection state, not reinvented) plus per-status counters incl. zombie counted apart (TUI-11, D-10)"
    - "Empty (poll-ok, 0 sessions) shows 'no active sessions'; the Phase 35 'waiting for server' / 'server caído' state takes precedence above all (TUI-11, D-12)"
    - "Selected row shows inverse highlight + a '› ' gutter; React key is task_id (D-05, Pitfall 7)"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "Table-rendering root: stores sessions + selectedTaskId; runs sort→filter→resolveSelection pipeline; reuses Phase 35 connection state for the header live indicator"
      contains: "rowCells"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "ink presentational table: header counters, column header, rows with fixed widths + semantic color + selected-row treatment, empty-state branches"
      exports: ["default"]
    - path: "test/dashboard-table.test.js"
      provides: "ink-testing-library render tests: columns, DESC order, zombie marker, counters, empty states (reuses makeFakeClock/injectProps/drain/okResponse)"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/select.js + format.js"
      via: "import { sortSessions, applyFilter, parseFilter, resolveSelection, countByStatus } + { rowCells, statusColor }"
      pattern: "from './select.js'|from './format.js'"
    - from: "src/cli/dashboard/App.js onResult"
      to: "data.sessions"
      via: "setSessions on ok; untouched on !ok (keep-last-good)"
      pattern: "setSessions"
    - from: "src/cli/dashboard/SessionTable.js"
      to: "ink <Text color>"
      via: "statusColor name string applied to the status cell; never picocolors"
      pattern: "color"
---

<objective>
Replace the Phase 35 status-line body in `App.js` with a live columnar session table. App stores the raw `sessions` array (keep-last-good on poll failure) and runs the obligatory derive pipeline `sortSessions → applyFilter → resolveSelection` every render. A presentational `SessionTable` component renders the header (live indicator reused from Phase 35 + per-status counters), the dim column-header row, the fixed-width data rows (semantic color on the status cell, zombie textual marker, selected-row inverse + `›` gutter), and the empty-state branches.

Purpose: This is the central presentation layer (TUI-07/09/10/11) — the visible table the operator reads. Interaction wiring (↑/↓ nav, `/` filter) lands in Plan 03; this plan establishes initial selection (first row) and renders the table from the data already flowing via `usePoll`.

Output: modified `App.js`, new `SessionTable.js`, and `test/dashboard-table.test.js` (ink render assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-tabla-viva-render-seleccion-filtros/36-CONTEXT.md
@.planning/phases/36-tabla-viva-render-seleccion-filtros/36-RESEARCH.md
@.planning/phases/36-tabla-viva-render-seleccion-filtros/36-PATTERNS.md
@.planning/phases/36-tabla-viva-render-seleccion-filtros/36-UI-SPEC.md
@.planning/phases/36-tabla-viva-render-seleccion-filtros/36-01-SUMMARY.md

<interfaces>
<!-- Pure derive layer from Plan 01 (src/cli/dashboard/select.js + format.js) — consume directly, do not reimplement. -->
From src/cli/dashboard/select.js:
  sortSessions(rows): Session[]                          // copy, DESC by started_at, task_id tiebreak
  applyFilter(rows, parsed, deriveRepo): Session[]       // AND, String.includes
  parseFilter(query): { repo, status, text }
  resolveSelection(rows, selectedTaskId, prevIndex): { index, taskId }
  countByStatus(rows): { running, review, done, error, zombie }
From src/cli/dashboard/format.js:
  deriveRepo(session): string
  formatAge(elapsedMin): string
  phaseMode(session): string
  statusColor(status, alive): { color?: string, dim?: boolean }   // ink color NAME, never ANSI
  statusLabel(status, alive): string                              // 'running (zombie)' for zombie
  rowCells(session): { task_ref, repo, phasemode, status, age }

<!-- Existing App.js connection state to REUSE for the header live indicator (D-10). Do NOT reinvent. -->
App.js (src/cli/dashboard/App.js, current):
  state: connected, lastGoodCount, lastGoodAt, lastAttemptAt   // the three-branch live/stale/waiting logic at lines 130-149
  onResult({ok,data}) at lines 101-117                          // ok → set live state; !ok → keep-last-good
  root render at lines 151-157: <Box flexDirection="column" borderStyle="round" paddingX={1}> + bold banner + body + dim footer
  useInput gated by useStdin().isRawModeSupported (lines 81-88) — only handles 'q' today

<!-- Fixed column widths (UI-SPEC lines 49-65). status NOT truncated ('(zombie)' is load-bearing). -->
COLS = { gutter: 2, task_ref: 10, repo: 18, phasemode: 11, status: 18, age: 7 }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED render tests for the table (TUI-07/09/10/11)</name>
  <files>test/dashboard-table.test.js</files>
  <read_first>
    - test/dashboard-status-line.test.js (the hermetic ink-testing-library harness to reuse VERBATIM: `makeFakeClock` lines 39-76, `injectProps` lines 82-92, `drain` lines 99-101, `okResponse` lines 104-106, and the `assert.match(lastFrame(), …)` assertion style lines 119-139)
    - .planning/phases/36-tabla-viva-render-seleccion-filtros/36-PATTERNS.md lines 287-315 (the table render test sketch + the D-03 fixture)
    - .planning/phases/36-tabla-viva-render-seleccion-filtros/36-RESEARCH.md lines 515-537 (hermetic render test pattern + harness facts: ink@4 has no waitUntilExit)
    - .planning/phases/36-tabla-viva-render-seleccion-filtros/36-UI-SPEC.md lines 143-197 (table layout, header counters, copywriting/empty-state precedence)
    - src/cli/dashboard/App.js (current render to be extended — confirm props shape for injection)
  </read_first>
  <behavior>
    test/dashboard-table.test.js (renders `App` via ink-testing-library, injecting fetchFn + fake clock as injectProps does):
    - Columns render (TUI-07): with the two-session FIXTURE (one running+alive GSD `kodo`/`36/full`/`5m`, one running+!alive non-GSD `/x/foo`→'foo'/`—`/`1h3m`), `lastFrame()` matches `KL-1`, `kodo`, `36/full`, `5m`, `KL-2`, `foo`, `1h3m`, and a `—` for the non-GSD phase/mode.
    - Zombie marker (TUI-10/D-09): `lastFrame()` matches `running \(zombie\)` for the running+!alive session.
    - Header counters (TUI-11/D-11): `lastFrame()` matches `2 running · 1 zombie` semantics — assert it contains `running` count and a separate `zombie` count (e.g. regex `/1 zombie/` and `/1 running/` given the fixture has one of each). Live indicator `● live` present after an ok poll (reused Phase 35 state).
    - Stable DESC order (TUI-09): the session with the newer `started_at` (KL-1 @10:00) renders ABOVE the older (KL-2 @09:00) — assert KL-1's index in `lastFrame()` is less than KL-2's index.
    - Empty state (TUI-11/D-12a): with a fixture of `{count:0, sessions:[]}` after an ok poll, `lastFrame()` matches `no active sessions` and does NOT match `no sessions match`.
    - Degraded precedence (D-12): a fetchFn that throws from the first tick keeps showing `waiting for server` and does NOT show `no active sessions`.
    - Initial selection (D-07): with ≥1 session, the first row (newest) shows the `› ` gutter; assert `lastFrame()` contains `› KL-1` (gutter on the initially-selected newest row).
  </behavior>
  <action>
    Create `test/dashboard-table.test.js` RED FIRST. Copy `makeFakeClock`/`injectProps`/`drain`/`okResponse` verbatim from test/dashboard-status-line.test.js (do not reinvent — same hermetic harness, no real timers, no network). Header comment block: `// @ts-check` first line, "Phase 36 Plan 02 Wave 0 (TUI-07/09/10/11)". Use the D-03 FIXTURE from PATTERNS.md lines 300-303 (two sessions: GSD running+alive, non-GSD zombie running+!alive). Drive the poll with `await drain()` for the kick-off tick exactly as dashboard-status-line.test.js does. Write assertions per the behavior block using `assert.match(lastFrame(), /…/, msg)` with the actual frame interpolated in the message. These will fail RED because App.js still renders the Phase 35 status line (no columns, no gutter). Run and confirm RED. Commit RED: `test(36-02): add Wave 0 RED render tests for session table (TUI-07/09/10/11)`.
  </action>
  <verify>
    <automated>node --test test/dashboard-table.test.js 2>&1 | grep -qE 'fail|not ok' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-table.test.js` exists, starts with `// @ts-check`, and imports `render` from `ink-testing-library` + `App` from `../src/cli/dashboard/App.js`.
    - `node --test test/dashboard-table.test.js` exits non-zero (RED) against the current Phase 35 App.js.
    - The file contains the D-03 two-session fixture (a `running`+`alive` GSD session and a `running`+`alive:false` non-GSD zombie).
    - `grep -c 'makeFakeClock' test/dashboard-table.test.js` ≥ 1 (harness reused, not reinvented).
    - A commit exists with message matching `test(36-02): add Wave 0 RED render tests`.
  </acceptance_criteria>
  <done>RED render test committed, failing because the table render does not exist yet; the D-03 fixture and reused hermetic harness are in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the live session table in App.js + SessionTable.js until GREEN (TUI-07/09/10/11, D-01/D-03/D-08/D-09/D-10/D-11/D-12)</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/App.js (the file being modified — preserve connection state lines 92-117, the useInput guard lines 81-88, and the root <Box borderStyle="round" paddingX={1}> wrapper lines 151-157; REPLACE only the statusNode body lines 130-155)
    - .planning/phases/36-tabla-viva-render-seleccion-filtros/36-PATTERNS.md lines 41-251 (App.js extension pattern, the obligatory derive pipeline order, and the SessionTable Row/cell pattern with exact COLS widths)
    - .planning/phases/36-tabla-viva-render-seleccion-filtros/36-UI-SPEC.md lines 122-197 (selected-row treatment, table layout ASCII, header counters, copywriting precedence)
    - src/cli/dashboard/select.js + src/cli/dashboard/format.js (the Plan 01 helpers to consume — confirm exact export names/signatures)
    - test/dashboard-table.test.js (the RED contract from Task 1 — implement exactly to satisfy it)
  </read_first>
  <action>
    In `App.js`: add `setSessions`/`sessions` state (`useState([])`) and a `selectedTaskId` state (`useState(null)`). In the existing `onResult` (lines 101-117), on `ok` additionally call `setSessions(result.data.sessions ?? [])`; on `!ok` leave `sessions` untouched (keep-last-good — same discipline as lastGoodCount). Import `{ sortSessions, applyFilter, parseFilter, resolveSelection, countByStatus }` from `./select.js` and `{ rowCells, statusColor, deriveRepo }` from `./format.js`. In render, run the OBLIGATORY pipeline in order: `const sorted = sortSessions(sessions)` → `const filtered = applyFilter(sorted, parseFilter(''), deriveRepo)` (query is empty in this plan — Plan 03 wires the live query; pass `''` for now so the pipeline shape is final) → `const sel = resolveSelection(filtered, selectedTaskId, 0)`. Set initial selection (D-07): in a `useEffect` keyed on `sel.taskId`, if `selectedTaskId !== sel.taskId` call `setSelectedTaskId(sel.taskId)` (write-back so the first row is selected when data arrives and the cursor never points at an absent id). Compute `const counts = countByStatus(filtered)`. Keep the existing three-branch connection logic (live/stale/waiting) — pass `connected`/`lastGoodCount`/`lastGoodAt`/`lastAttemptAt`/`now` to SessionTable as the header live indicator (D-10), do not duplicate it. Replace the `statusNode` body with `createElement(SessionTable, { rows: filtered, selectedIndex: sel.index, counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt, now })`. Keep the root `<Box borderStyle="round" paddingX={1}>`, the bold `kodo dashboard` banner, and update the footer to `↑↓ move · / filter · q quit` (UI-SPEC line 185). Create `src/cli/dashboard/SessionTable.js` (ink, `// @ts-check`, `createElement as h` from react, `{ Box, Text }` from ink, default export): render order — (1) header row: live indicator (`● live` green when connected; `⚠ server caído` yellow + `N sessions (last update Ns ago, retrying…)` when `lastGoodAt != null` && !connected with age `(lastAttemptAt - lastGoodAt)/1000`; `waiting for server` dim otherwise — port the EXACT three branches from App.js lines 130-149) followed by the compact counters string from `counts` joined by ` · ` omitting zero counts and counting zombie apart (e.g. `2 running · 1 zombie`); (2) empty-state precedence (D-12): if !connected && lastGoodAt==null → only the `waiting for server` indicator (no table); else if connected && rows.length===0 → `no active sessions`; (the `no sessions match` branch will be reachable in Plan 03 when a query exists — render it when rows.length===0 but a non-empty query was applied; in this plan rows.length===0 always means 0 sessions, so render `no active sessions`); (3) dim column-header row `task_ref repo phase/mode status age` padded to COLS widths; (4) the rows: map `rows` to a `Row` keyed by `task_id` (NEVER index — Pitfall 7), each row a `<Box flexDirection="row">` with a gutter cell (width 2: `'› '` when its index === selectedIndex else `'  '`), then cells from `rowCells(session)` in fixed-width `<Box width>` wrappers: task_ref/repo/phasemode with `<Text wrap="truncate-end" inverse={selected}>`, the status cell with `<Text {...statusColor(session.status, session.alive)} inverse={selected}>` (semantic color name + dim, NOT truncated — `(zombie)` must survive), age cell `<Text inverse={selected}>`. COLS = { gutter:2, task_ref:10, repo:18, phasemode:11, status:18, age:7 } (UI-SPEC lines 51-58). Color ONLY via ink `<Text>` props — NEVER import picocolors or src/cli/format.js (D-08). Run test/dashboard-table.test.js until GREEN, then the full suite. Commit GREEN: `feat(36-02): live session table render (App.js + SessionTable.js)`.
  </action>
  <verify>
    <automated>node --test test/dashboard-table.test.js test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-table.test.js` exits 0 (columns, DESC order, zombie marker, counters, empty state, initial-selection gutter all green).
    - `node --test test/format-isolation.test.js` exits 0 (SessionTable.js + App.js import zero picocolors).
    - `grep -REn "picocolors|cli/format'" src/cli/dashboard/App.js src/cli/dashboard/SessionTable.js` returns nothing.
    - `src/cli/dashboard/App.js` imports from `./select.js` and `./format.js` (grep `from './select.js'` and `from './format.js'` both match) and calls `setSessions` in onResult.
    - `src/cli/dashboard/SessionTable.js` has a default export and uses `task_id` as the React key (grep `key:` near the row map references `task_id`, not an index).
    - `grep -v '^//' src/cli/dashboard/App.js | grep -c "selectedIndex\|sel.index"` ≥ 1 (selection index is derived, not stored as an index).
    - `npm test` full suite green.
    - A commit exists with message matching `feat(36-02): live session table render`.
  </acceptance_criteria>
  <done>App renders the live columnar table from the poll stream with DESC stable order, semantic status color + zombie marker, header live indicator + per-status counters, dual empty-state precedence, and an initially-selected first row with the `›` gutter; color-isolation and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `/status` JSON → ink render | Server-provided string fields (`task_ref`, `summary`, `project_name`, `phase_id`) are rendered as `<Text>` children. Read-only; no auth, no mutation, no network egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Spoofing / Tampering | server-provided string fields rendered in SessionTable rows | mitigate (assumption documented) | ink `<Text>` renders children as literal text and does its own ANSI for styling — it does NOT interpret embedded CSI/escape sequences from `summary`/`task_ref`/`project_name` as control codes (documented assumption, RESEARCH Security Domain). No `console.log` of payloads — ink owns the screen (Information Disclosure backstop). Defense-in-depth CSI stripping in `rowCells` is OPTIONAL/discretionary and LOW-priority (task data originates from the operator's own provider); if added it lives in Plan 01's `rowCells`, color-isolation unaffected. |
| T-36-03 | Tampering | color rendering path | mitigate | Color exclusively via ink `<Text color>` name strings (from `statusColor`); zero `picocolors`, zero `src/cli/format.js` import. Enforced by `test/format-isolation.test.js` (walker) + grep gate in acceptance criteria. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — ink/react/ink-testing-library already present (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `node --test test/dashboard-table.test.js` — green (columns, order, zombie marker, counters, empty state, initial selection).
- `node --test test/format-isolation.test.js` — green (color-isolation preserved with the new render files).
- `npm test` — full suite green.
</verification>

<success_criteria>
- The operator sees a columnar table `task_ref · repo · phase/mode · status · age` (TUI-07) ordered stably DESC by started_at (TUI-09).
- The status cell is colored by status+alive and the zombie shows `(zombie)` textually (TUI-10).
- The header shows the reused live indicator + per-status counters with zombie counted apart; empty list shows `no active sessions` with the Phase 35 degraded state taking precedence (TUI-11, D-12).
- Color-isolation walker and full suite green.
</success_criteria>

<output>
Create `.planning/phases/36-tabla-viva-render-seleccion-filtros/36-02-SUMMARY.md` when done.
</output>
