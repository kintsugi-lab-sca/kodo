---
phase: "39.1"
slug: cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-10
---

# Phase 39.1 — Validation Strategy

> Per-phase validation contract reconstruido retroactivamente (backfill Nyquist Phase 47, NYQ-02).
> Cobertura **citada** de `39.1-VERIFICATION.md` (passed 14/14 must-haves, 2026-06-03).
> **Sin re-ejecutar la suite** — cada dimensión referencia el resultado empírico ya registrado (D-03).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` + `node:assert/strict` · `ink-testing-library` para la TUI |
| **Config file** | none — runner nativo, sin config externa |
| **Quick run command** | `node --test test/dashboard-table.test.js test/dashboard-format.test.js` |
| **Full suite command** | `npm test` (`node --test $(find test -name '*.test.js' -type f)`) |
| **Evidencia citada** | `39.1-VERIFICATION.md` (2026-06-03, status passed, score 14/14) |

---

## Sampling Rate

- **Evidencia primaria:** `39.1-VERIFICATION.md` — 14/14 observable truths + 8/8 artifacts + 6/6 key-links verificados; suite confirmatoria **1073 pass / 0 fail / 1 skip**.
- **Política Nyquist (backfill):** la cobertura ES la cita a la evidencia preexistente; no se re-corre la suite (D-03 / D-05).

---

## Per-Task Verification Map (dimensión → cobertura citada)

| Requirement | Plan | Dimensión / Behavior | Test Type | Automated Command | Evidencia citada (`39.1-VERIFICATION.md`) | Status |
|-------------|------|----------------------|-----------|-------------------|--------------------------------------------|--------|
| TUI-17 (blocker) | 39.1-01 | Wiring host muerto eliminado del dashboard ink (sin prop `host`, sin `HOST_ERR_*`/`hostError`) | unit (grep + suite) | `node --test test/dashboard-table.test.js` | Truths #1/#2 ✓ VERIFIED (`getHost\|host/interface`=0 en index.js; `HOST_ERR\|hostError`=0 en App.js); table 33/33 verde | ✅ green |
| TUI-13 | 39.1-02 | `alive` autoritativo: `GET /status` NO sobreescribe el campo escrito por reconcileTick (single-writer) | unit (grep + flow) | `npm test` | Truths #5/#6 ✓ VERIFIED (`workspaceList\|cmux.listWorkspaces`=0 en server.js; enrich `{...s, elapsed_min}` pasa-through; `elapsed_min`=4) | ✅ green |
| TUI-14 | 39.1-02 | Guard de Enter consume `alive` autoritativo (`row.alive === false`), sin divergencia con host | unit | `node --test test/dashboard/app-focus.test.js` | Truth #4 ✓ VERIFIED (`row.alive === false` guard=1; alive del server = fuente única) | ✅ green |
| TUI-10 | 39.1-03 | `statusColor` v3-aware: colorea idle/needs-input (status v2 null) leyendo STATE_BADGES; pura, precedencia v2 intacta | unit | `node --test test/dashboard-format.test.js` | Truths #8/#9/#10 ✓ VERIFIED (`statusColor(null,true,'idle')`→yellow; `STATE_BADGES[state`=3; `('running',true,'idle')`→green); format 16/16 verde | ✅ green |
| TUI-15 | 39.1-04/05 | Overlay distingue `unsupported` vs `empty`; `GET /comments` retorna `{comments, supported}`; `OVERLAY_COMMENTS_UNSUPPORTED` | unit (server + ink) | `node --test test/dashboard-table.test.js` | Truths #11/#12 ✓ VERIFIED (`JSON.stringify({comments,supported})`=1; `OVERLAY_COMMENTS_UNSUPPORTED`='comments not supported by this provider'; `snap.status==='unsupported'`) | ✅ green |
| TUI-16 | 39.1-05 | Bookkeeping verificable: TUI-15/16 `[x]` en REQUIREMENTS.md; columna Phase corregida a Phase 39 | doc/grep | `grep -c "^- \[x\] \*\*TUI-16\*\*" .planning/REQUIREMENTS.md` | Truths #13/#14 ✓ VERIFIED (TUI-15/16 `[x]`; `Pending`=0; `\| Phase 39 \|`=2) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky / manual-only*

---

## Wave 0 Requirements

Infraestructura existente cubre todos los requirements. Fase de cierre de gaps del audit v0.9 — sin nuevos frameworks. `node --check` exit 0 en todos los archivos modificados; `format-isolation` walker intacto. Sin Wave 0.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Evidencia |
|----------|-------------|------------|-----------|
| _Ninguno._ | — | — | `39.1-VERIFICATION.md` §Human Verification Required: "Ninguna. Todos los criterios de éxito son verificables programáticamente." |

---

## Validation Sign-Off

- [x] Cada requirement (TUI-17/13/14/10/15/16) mapeado a ≥1 cita de evidencia real en `39.1-VERIFICATION.md`
- [x] Sampling continuity: cobertura automatizada verde (table 33/33, format 16/16, suite 1073 pass)
- [x] Wave 0 covers all MISSING references (ninguna — fase de cierre, infra existente)
- [x] No watch-mode flags
- [x] Ninguna fase declarada N/A — evidencia empírica real citada
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-10 (backfill Phase 47, NYQ-02)

---

## Reconstruction Audit 2026-06-10 (Phase 47 NYQ-02)

| Metric | Count |
|--------|-------|
| Requirements audited | 6 (TUI-17, TUI-13, TUI-14, TUI-10, TUI-15, TUI-16) |
| COVERED (automated unit + grep) | 6 |
| PARTIAL | 0 |
| MISSING | 0 |
| Manual-only | 0 (todo verificable programáticamente per `39.1-VERIFICATION.md`) |
| Tests citados (no re-corridos) | 1073 pass / 0 fail / 1 skip (suite completa; table 33 + format 16) |

**Nota Nyquist:** La fase salda la deuda del audit v0.9 (elimina código muerto del wiring host, unifica la fuente única de `alive` en reconcileTick, hace `statusColor` v3-aware, distingue unsupported vs empty en el overlay). Los 14 must-haves están VERIFIED con evidencia de código + test en `39.1-VERIFICATION.md` (passed 14/14). **Sin re-ejecutar la suite** — cobertura citada de `39.1-VERIFICATION.md`. Fase declarada **nyquist-compliant**.
