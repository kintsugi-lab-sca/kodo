---
phase: 39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta
plan: 01
subsystem: cli-dashboard
tags: [tui, dead-code-removal, host-wiring, alive-source-of-truth]
requires: []
provides:
  - "dashboard ink sin rastro de host/hostError/HOST_ERR_* (TUI-17 cerrado por eliminación)"
affects:
  - src/cli/dashboard/index.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-table.test.js
tech-stack:
  added: []
  patterns:
    - "Cierre de blocker por eliminación de código muerto (no por cableado): cuando el espejo de un patrón vivo nunca se conecta, se borra en lugar de completarse"
key-files:
  created: []
  modified:
    - src/cli/dashboard/index.js
    - src/cli/dashboard/App.js
    - src/cli/dashboard/SessionTable.js
    - test/dashboard-table.test.js
decisions:
  - "D-01: cerrar TUI-17 eliminando el wiring host muerto, NO cableando consulta host directa"
  - "D-03: el camino del server (reconcileLoop → state.json → GET /status → tabla) es la única fuente de idle/needs-input; cumple la promesa funcional de Phase 38"
  - "D-06: guard Enter fail-open `row.alive === false` preservado literalmente (sin cambio a `!row.alive`)"
metrics:
  duration: ~7min
  tasks: 3
  files: 4
  completed: 2026-06-03
requirements: [TUI-17]
---

# Phase 39.1 Plan 01: Eliminación del wiring host muerto en el dashboard ink — Summary

Cierre del BLOCKER TUI-17 por eliminación: se borró todo el rastro de `host`/`hostError`/`HOST_ERR_*` (código muerto de Phase 38 cuya prop se descartaba y cuyos errores nunca se emitían) sin introducir consulta host directa desde el dashboard.

## Qué se hizo

- **Task 1 (index.js):** eliminado el bloque de construcción del WorkspaceHost (`import getHost` + `const host = getHost('cmux', ...)`) y la prop `host` del `createElement(App)`. Preservados `cmuxBin`, `onFocus` (Phase 37) y el alt-screen/SIGTERM/waitUntilExit (Phase 34/36). `../../host/interface.js` ya no se importa en este archivo.
- **Task 2 (App.js):** eliminadas las constantes exportadas `HOST_ERR_UNAVAILABLE`/`HOST_ERR_TIMEOUT`, el estado `[hostError, setHostError]`, su rama de dismiss en `useInput`, y la prop `hostError` pasada a `SessionTable`. Preservado el espejo vivo `focusError`/`setFocusError`/`FOCUS_ERR_*` y el guard de Enter D-06 (`row.alive === false`, fail-open deliberado).
- **Task 3 (SessionTable.js + test):** eliminado el parámetro `hostError` de la firma del componente y simplificado `footerError = focusError ?? hostError` → `footerError = focusError`. En el test se quitó el import de `HOST_ERR_*` (que habría importado `undefined`) y el `describe('Phase 38 SC#3: HOST_ERR_* constantes literal-stable')` completo.

## D-03 (documentación obligatoria del cierre)

El blocker TUI-17 se cierra **por eliminación**, no por cableado. El camino del server (`startReconcileLoop` → `state.json` → `GET /status` → tabla) ya entrega idle/needs-input al operador y **cumple la promesa funcional de Phase 38**. Cablear la consulta host directa desde el dashboard crearía una segunda fuente de verdad para `alive` — exactamente la divergencia que TUI-13/14 (Plan 02) corrige. Se mantiene una única fuente de verdad.

## Hecho registrado (D-02 / output spec)

`SessionTable.js` **NO importaba** `HOST_ERR_*` — su bloque de imports solo trae `OVERLAY_*`/`OVERLAY_VIEWPORT`. Solo consumía la prop `hostError`. Por tanto no hubo import que quitar en SessionTable.js; el único import de `HOST_ERR_*` vivía en `test/dashboard-table.test.js` (desde App.js).

## Verificación

- `node --check` en index.js / App.js / SessionTable.js: exit 0.
- `grep -rn "HOST_ERR\|hostError" src/cli/dashboard/`: 0 líneas.
- `grep -rn "host/interface" src/cli/dashboard/`: 0 líneas.
- `grep -c "row.alive === false" src/cli/dashboard/App.js`: 1 (guard D-06 intacto).
- `node --test test/dashboard-table.test.js`: 30 pass, 0 fail.
- `npm test` (suite completa): 1065 pass, 0 fail, 1 skipped (preexistente, no relacionado). Cero regresión.

## Deviations from Plan

None - plan ejecutado exactamente como fue escrito.

Notas menores (dentro de la acción descrita, no desviaciones): se actualizaron tres comentarios que referenciaban `HOST_ERR_*`/host-error a entidades ya inexistentes — el comentario "mismo patrón que FOCUS_ERR_*/HOST_ERR_*" en App.js (ahora solo `FOCUS_ERR_*`), el comentario del bloque footer-error en SessionTable.js (eliminado junto al `?? hostError`), y el comentario de la sección del test (quitada la mención `HOST_ERR_*`). El plan instruía explícitamente actualizar los comentarios adyacentes.

## Known Stubs

None — cambio puramente sustractivo, sin valores hardcodeados ni placeholders introducidos.

## Threat Flags

None — el cambio es puramente sustractivo (T-39.1-01: accept). Reduce superficie al eliminar un import dinámico de host; no añade red, auth ni datos.

## Commits

- `0dc43fe` refactor(39.1-01): eliminar wiring de host muerto en index.js
- `95ed811` refactor(39.1-01): eliminar hostError/HOST_ERR_* muertos en App.js
- `f8dcf52` refactor(39.1-01): quitar prop hostError de SessionTable y reconciliar test

## Self-Check: PASSED
- FOUND: .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-01-SUMMARY.md
- FOUND commit 0dc43fe / 95ed811 / f8dcf52
