---
phase: 39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server.js
autonomous: true
requirements: [TUI-13, TUI-14]
must_haves:
  truths:
    - "GET /status NO sobreescribe el campo `alive` autoritativo escrito por reconcileTick en state.json"
    - "El cómputo de `elapsed_min` se preserva en el enrich de /status"
    - "displayStatus (vista HTML web) deriva dead/idle del mismo `alive` autoritativo, sin recomputar alive"
    - "Decisiones cubiertas: D-04 (GET /status deja de sobreescribir `alive`; elimina el legacy `workspaceList.includes`), D-05 (displayStatus consume el mismo `alive` autoritativo, no recomputado)"
  artifacts:
    - path: "src/server.js"
      provides: "Handler GET /status sin override legacy de alive; elapsed_min preservado"
      contains: "elapsed_min"
  key_links:
    - from: "src/session/reconcile.js (reconcileTick)"
      to: "GET /status enriched sessions"
      via: "alive ya en state.json, no recomputado"
      pattern: "elapsed_min"
---

<objective>
Cerrar TUI-13/14 (D-04/D-05): establecer una FUENTE ÚNICA de verdad para el campo `alive`. Hoy `GET /status` sobreescribe el `alive` que `reconcileTick` ya computó (vía `WorkspaceHost.listWorkspaces() --json`, escrito en state.json) con un check legacy `workspaceList.includes(s.workspace_ref)` sobre el texto plano de `cmux.listWorkspaces()`. Ese doble cómputo puede divergir → el guard de Enter (`row.alive === false`, App.js) podría permitir focus a un workspace que el host ya marcó muerto.

Purpose: el valor autoritativo es el de reconcileTick (reconcile.js:203 / state.js:123). El server (startReconcileLoop) es el ÚNICO escritor de state.json. Dejar de pisar `alive` en /status elimina la divergencia (invariante "una sola fuente de verdad para alive").

Output: handler `GET /status` en `src/server.js` sin el override legacy de `alive`; `elapsed_min` preservado; `workspaceList`/`cmux.listWorkspaces()` eliminadas si quedan sin uso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-CONTEXT.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md
@.planning/v0.9-MILESTONE-AUDIT.md

<interfaces>
<!-- Fuente autoritativa de alive (NO TOCAR — solo leer para entender el invariante): -->
- src/session/reconcile.js:203 → alive = effectiveState in {running, idle, needs-input}
- src/session/state.js:123     → alive = state in {running, idle, needs-input} (migrateStateV2toV3)
- startReconcileLoop corre en el proceso server (server.js:506-511): único escritor de state.json.

<!-- Estado actual del enrich a modificar (server.js:376-383): -->
  let workspaceList = '';
  try { workspaceList = await cmux.listWorkspaces(); } catch {}
  const enriched = sessions.map((s) => ({
    ...s,
    alive: workspaceList.includes(s.workspace_ref),   // ← D-04: ELIMINAR override legacy
    elapsed_min: Math.floor((Date.now() - new Date(s.started_at).getTime()) / 60000),
  }));

<!-- USO de `cmux` en el resto del archivo (verificado por grep — NO eliminar el import de cmux):
     server.js:485 cmux.rename(...)  · server.js:486 cmux.setColor(...)
     workspaceList / cmux.listWorkspaces() SOLO aparecen en 377-378 → eliminables tras D-04. -->

<!-- displayStatus (server.js:197-200) es JS de NAVEGADOR embebido en template string (NO Node server-side):
  function renderSession(s) {
    var displayStatus = s.status;
    if (!s.alive && s.status === 'running') displayStatus = 'dead';
    if (s.alive && s.status === 'running' && s.elapsed_min > 30) displayStatus = 'idle';
  D-05 se satisface AUTOMÁTICAMENTE al aplicar D-04 (displayStatus solo DERIVA de s.alive, no lo recomputa). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar el override legacy de `alive` en GET /status (D-04)</name>
  <files>src/server.js</files>
  <read_first>
    - src/server.js (leer líneas 360-412 — handler GET /status completo, incluyendo el enrich y el res.end)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección server.js D-04: fuente autoritativa confirmada)
  </read_first>
  <action>
    En el bloque "Enrich sessions with live workspace health" (server.js:376-383): eliminar la línea `alive: workspaceList.includes(s.workspace_ref),` del objeto del `.map`. El objeto enriquecido debe quedar como `{ ...s, elapsed_min: Math.floor((Date.now() - new Date(s.started_at).getTime()) / 60000) }` — `...s` ya trae el `alive` autoritativo que reconcileTick escribió en state.json. Tras quitar el override, las líneas `let workspaceList = '';` y `try { workspaceList = await cmux.listWorkspaces(); } catch {}` (377-378) quedan sin uso → eliminarlas también (cambio quirúrgico). Verificar con `grep -n "workspaceList" src/server.js` que no haya otro consumidor antes de borrar. NO eliminar el import/handle de `cmux`: sigue usándose en `cmux.rename`/`cmux.setColor` (485-486). Actualizar el comentario "Enrich sessions with live workspace health" para reflejar que ahora solo se computa `elapsed_min` (el `alive` viene de state.json vía reconcileTick).
  </action>
  <acceptance_criteria>
    - `grep -c "workspaceList" src/server.js` devuelve 0
    - `grep -c "cmux.listWorkspaces" src/server.js` devuelve 0
    - `grep -c "elapsed_min" src/server.js` es ≥ 1 (cómputo de elapsed_min preservado)
    - `grep -c "cmux.rename\|cmux.setColor" src/server.js` es ≥ 2 (import de cmux preservado, sigue usándose)
    - `node --check src/server.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/server.js && test "$(grep -c 'workspaceList\|cmux.listWorkspaces' src/server.js)" = "0" && test "$(grep -c 'elapsed_min' src/server.js)" -ge "1"</automated>
  </verify>
  <done>GET /status no recomputa `alive` (lo toma de state.json vía `...s`); elapsed_min preservado; workspaceList/listWorkspaces() eliminadas; cmux.rename/setColor intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar displayStatus consume el alive autoritativo (D-05/D-09 — verificación, no edición obligatoria)</name>
  <files>src/server.js</files>
  <read_first>
    - src/server.js (leer líneas 167-210 — toggleComments + renderSession/displayStatus, JS de navegador embebido)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección D-05 "ATENCIÓN CRÍTICA: clasificación de superficie")
  </read_first>
  <action>
    VERIFICAR (no necesariamente editar) que `displayStatus` (server.js:197-200) lee `s.alive` y `s.status`/`s.elapsed_min` del objeto JSON de /status y NO recomputa `alive` por su cuenta. Esto es JS de NAVEGADOR embebido como template string (la vista HTML del dashboard web), no código Node server-side. Tras la Task 1 (D-04), el `s.alive` que ve `displayStatus` ES el autoritativo, así que la derivación `dead`/`idle` es coherente sin tocar este código. Registrar en el SUMMARY: (a) que `displayStatus` solo DERIVA `dead`/`idle` de `s.alive`/`s.status`, no recalcula alive → D-05 satisfecho por D-04; (b) el tradeoff de que `displayStatus` reimplementa la heurística `idle` (`elapsed_min > 30`) en el cliente HTML, divergente del estado v3 del server — esto es la vista HTML legacy (web), NO el overlay ink del `kodo dashboard`; dejar NOTA (mismo criterio defensivo que D-09) sin alinearla en esta fase salvo que se confirme superficie viva crítica. NO editar el template string salvo que la verificación revele que `displayStatus` lee un campo legacy distinto de `s.alive` (no es el caso esperado).
  </action>
  <acceptance_criteria>
    - El SUMMARY documenta que `displayStatus` (server.js:197-200) deriva `dead`/`idle` de `s.alive`/`s.status` sin recomputar alive → D-05 satisfecho por D-04
    - `grep -c "displayStatus = 'idle'" src/server.js` es exactamente 1 (heurística cliente sin cambio, salvo decisión explícita)
    - El SUMMARY registra la NOTA D-09: la vista HTML web reimplementa la heurística idle (elapsed_min>30) divergente del estado v3; fuera del alcance prioritario (overlay ink) salvo confirmación de superficie viva
    - `node --check src/server.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/server.js && test "$(grep -c "displayStatus = 'idle'" src/server.js)" = "1"</automated>
  </verify>
  <done>Verificado y documentado en SUMMARY: displayStatus consume el alive autoritativo (D-05 satisfecho por D-04); NOTA D-09 registrada sobre la divergencia de la vista HTML web.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP (TUI/web) → GET /status | respuesta JSON local; sin nueva entrada de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-02 | Tampering | campo `alive` en respuesta de /status | accept (mejora) | El cambio ELIMINA un segundo cómputo de `alive` (string-includes legacy) en favor de la fuente autoritativa de reconcileTick. Reduce la superficie de divergencia/estado inconsistente; no añade entrada de usuario ni endpoint. Cambio sustractivo byte-compatible (el campo `alive` sigue presente, solo cambia su origen al autoritativo). |
| T-39.1-SC | Tampering | npm/pip/cargo installs | n/a | Sin instalaciones de paquetes (cero deps nuevas). |
</threat_model>

<verification>
- `grep -rn "workspaceList\|cmux.listWorkspaces" src/server.js` → 0 líneas
- La respuesta de `GET /status` sigue conteniendo `alive` por sesión (ahora desde state.json) y `elapsed_min` (computado) — byte-compatible para clientes
- Suite completa sin regresión: `npm test` verde (en particular cualquier test de server/status)
</verification>

<success_criteria>
- TUI-13/14 cerrado: `alive` tiene una sola fuente de verdad (reconcileTick → state.json → /status pasa-through vía `...s`)
- El guard de Enter del dashboard (`row.alive === false`) ahora consume el `alive` autoritativo (sin divergencia con el host)
- `elapsed_min` preservado; `cmux.rename`/`cmux.setColor` intactos
- SUMMARY documenta D-05 (displayStatus coherente) y la NOTA D-09 (vista HTML web)
</success_criteria>

<output>
Create `.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-02-SUMMARY.md` when done.
</output>
