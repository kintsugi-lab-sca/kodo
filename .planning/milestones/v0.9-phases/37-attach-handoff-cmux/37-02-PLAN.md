---
phase: 37-attach-handoff-cmux
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard/app-focus.test.js
autonomous: true
requirements: [TUI-13, TUI-14]
must_haves:
  truths:
    - "Render `<App>` con fixture de 1 sesión `alive:false` + simulación de Enter (`stdin.write('\\r')`) → `onFocus` NUNCA es llamado (counter==0) y `lastFrame()` contiene la string literal `[!] workspace gone (alive=false) — press any key` (TUI-14 criterio #2, D-02/D-05)"
    - "Render `<App>` con fixture de 1 sesión `alive:true` + simulación de Enter + `onFocus` que resuelve `{ok:true}` → `onFocus` invocada exactamente UNA vez con `row.workspace_ref` literal y `lastFrame()` NO contiene `[!]` (TUI-13 criterio #1, D-06)"
    - "Render `<App>` con fixture alive:false + Enter (footer-error visible) + cualquier tecla (`stdin.write('x')`) → `focusError===null` siguiente frame, footer normal `↑↓ move · / filter · q quit` restaurado, `onFocus` SIGUE sin llamarse (D-04 clear-on-any-input)"
    - "El render del mensaje rojo viene de `<Text color=\"red\">` de ink (color-isolation D-12 Phase 34); cero `picocolors`, cero ANSI inline en SessionTable.js o App.js"
    - "NO-PICOCOLORS: ninguno de App.js / SessionTable.js / test/dashboard/app-focus.test.js importa `picocolors` (cubierto por walker test/format-isolation.test.js + grep negativo)"
    - "Los 3 mensajes literal-estables D-05 viven como constantes exportadas en App.js (o un módulo de constantes) — tests los importan SIN duplicar string, eliminando drift: `FOCUS_ERR_ZOMBIE = '[!] workspace gone (alive=false) — press any key'`, `FOCUS_ERR_ENOENT = '[!] cmux not found in PATH — press any key'`, `FOCUS_ERR_FAILED_FN = (n) => \\`[!] cmux focus failed (code ${n}) — press any key\\``"
    - "El handler de Enter vive EN el bloque `mode === 'list'` existente (líneas 198-219), NO en el bloque `mode === 'filter'` ni fuera del `useInput` — preserva la reserva D-15 Phase 36 de Esc modal"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "Prop onFocus + useState focusError + Enter handler con guard alive===false + clear-on-any-input + render condicional"
      contains: "FOCUS_ERR_ZOMBIE"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "Prop focusError + render condicional del footer-error rojo (espejo de filterLine líneas 142-148)"
      contains: "focusError"
    - path: "test/dashboard/app-focus.test.js"
      provides: "3 escenarios Wave 0 integration: alive===false guard, ok path, clear-on-any-input"
      min_lines: 80
  key_links:
    - from: "src/cli/dashboard/App.js (Enter handler)"
      to: "props.onFocus (consumido en Plan 03 con runFocus)"
      via: "await onFocus?.(row.workspace_ref)"
      pattern: "onFocus\\?\\.\\(.*workspace_ref"
    - from: "src/cli/dashboard/App.js (useInput head)"
      to: "src/cli/dashboard/App.js (setFocusError(null))"
      via: "if (focusError != null) { setFocusError(null); return; }"
      pattern: "focusError\\s*!=\\s*null"
    - from: "src/cli/dashboard/SessionTable.js (errorLine)"
      to: "Text color='red'"
      via: "render condicional de prop focusError"
      pattern: "color:\\s*['\\\"]red['\\\"]"
---

<objective>
Wirear el handler de `Enter` en `App.js` con guard `alive===false` (D-02), el estado local `focusError` (D-06), el clear-on-any-input (D-04), los 3 mensajes literal-estables (D-05) izados a constantes exportadas, y la prop `focusError` propagada a `SessionTable.js` con el render condicional del footer rojo (espejo del patrón `filterLine` líneas 142-148). Cubrir con 3 tests integration `ink-testing-library` (Wave 0) que asertan los 3 contratos load-bearing: guard alive===false (NO `onFocus` + footer rojo con texto literal), happy path (`onFocus` invocada UNA vez con `workspace_ref`), clear-on-any-input.

Purpose: Aislar toda la UX del focus en la capa React (componente + interacción), independiente del wiring de `runDashboard` (Plan 03). Cubre TUI-13 criterio #1 (Enter llama a `onFocus` con el ref correcto) y TUI-14 criterio #2 (guard alive===false sin invocar) + parte del criterio #3 (footer-error render del mensaje canónico — los mensajes de ENOENT / failed quedan testeados estructuralmente vía el mapeo en el handler; el path runtime real se cierra en Plan 03 + UAT).

Output: 2 archivos modificados (App.js + SessionTable.js), 1 archivo test nuevo (test/dashboard/app-focus.test.js) con 3 tests verdes; 3 constantes exportadas para mensajes literal-estables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-attach-handoff-cmux/37-CONTEXT.md
@.planning/phases/37-attach-handoff-cmux/37-PATTERNS.md
@.planning/phases/37-attach-handoff-cmux/37-VALIDATION.md
@.planning/phases/37-attach-handoff-cmux/37-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From `src/cli/dashboard/App.js:89-99` — Current props signature (extend with `onFocus`):
```js
export default function App({
  baseUrl,
  fetchFn,
  now = Date.now,
  schedule,
  cancel,
  scheduleTimeout,
  cancelTimeout,
  baseMs,
  maxMs,
}) {
```

From `src/cli/dashboard/App.js:109` — `lastError` useState pattern to mirror for `focusError`:
```js
const [lastError, setLastError] = useState(/** @type {string | null} */ (null));
```

From `src/cli/dashboard/App.js:160-166` — Derivation pipeline (sel.row IS available):
```js
const sorted = sortSessions(sessions);
const filtered = applyFilter(sorted, parseFilter(query), deriveRepo);
const sel = resolveSelection(filtered, selectedTaskId, prevIndexRef.current);
```

From `src/cli/dashboard/select.js:74-80` — `resolveSelection` shape (sel does NOT expose `.row` — must use `filtered[sel.index]`):
```js
export function resolveSelection(rows, selectedTaskId, prevIndex = 0) {
  if (rows.length === 0) return { index: -1, taskId: null };
  const idx = rows.findIndex((r) => r.task_id === selectedTaskId);
  if (idx !== -1) return { index: idx, taskId: selectedTaskId };
  const clamped = Math.max(0, Math.min(prevIndex, rows.length - 1));
  return { index: clamped, taskId: rows[clamped].task_id ?? null };
}
```

**CRITICAL:** sel returns `{ index, taskId }` only — NO `sel.row`. Enter handler must use `const row = sel.index >= 0 ? filtered[sel.index] : null;` (PATTERNS.md línea 261 lo prefigura).

From `src/cli/dashboard/App.js:170-221` — useInput mode-gated structure (insert clear-on-any-input AT TOP, insert Enter handler in `mode === 'list'` branch):
```js
useInput(
  (input, key) => {
    if (mode === 'filter') {
      if (key.escape) { setQuery(''); setMode('list'); return; }
      if (key.return) { setMode('list'); return; }
      // ... backspace/delete + char append
      return;
    }
    // mode === 'list'
    if (input === 'q') { exit(); return; }
    if (input === '/') { setMode('filter'); return; }
    if (key.upArrow) { /* ... */ return; }
    if (key.downArrow) { /* ... */ return; }
    // key.escape: DELIBERADAMENTE ignorado en modo lista (reservado Phase 38 — D-11/D-15).
  },
  { isActive: isRawModeSupported },
);
```

From `src/cli/dashboard/App.js:238-259` — Render JSX-less; SessionTable invocation site to extend with `focusError`:
```js
return createElement(
  Box,
  { flexDirection: 'column', borderStyle: 'round', paddingX: 1 },
  createElement(Text, { bold: true }, 'kodo dashboard'),
  createElement(
    Box,
    { marginY: 1, paddingX: 1 },
    createElement(SessionTable, {
      rows: filtered,
      selectedIndex: sel.index,
      counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt,
      mode, query, hasQuery,
    }),
  ),
  createElement(Text, { dimColor: true }, '↑↓ move · / filter · q quit'),
);
```

From `src/cli/dashboard/SessionTable.js:119-148` — Props signature + filterLine analog to mirror as `errorLine`:
```js
export default function SessionTable({
  rows, selectedIndex, counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt,
  hasQuery = false, mode = 'list', query = '',
}) {
  // ...
  const filterLine =
    mode === 'filter'
      ? h(Box, { marginTop: 1 }, h(Text, null, `/ ${query}▏`))
      : null;
```

From `src/cli/dashboard/SessionTable.js:155-167, 204-210` — Three render branches that include `filterLine` (extend each with errorLine precedence):
```js
if (!connected && lastGoodAt == null) {
  return h(Box, { flexDirection: 'column' }, header, filterLine);
}
if (rows.length === 0) {
  // ...
  return h(Box, { flexDirection: 'column' }, header, h(Box, ...), filterLine);
}
// final return
return h(Box, { flexDirection: 'column' }, header, h(Box, ...columnHeader + dataRows), filterLine);
```

From `test/dashboard-render.test.js:34-77` — Primary analog for app-focus.test.js (ink-testing-library + render + stdin + lastFrame + tick):
```js
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { render } from 'ink-testing-library';
import { createElement } from 'react';
import App from '../src/cli/dashboard/App.js';

const NEVER_FETCH = () => new Promise(() => {});

describe('TUI-01: dashboard chrome (D-01)', () => {
  it('renders banner + waiting placeholder + q quit footer', () => {
    const { lastFrame } = render(
      createElement(App, { baseUrl: 'http://localhost:9090', fetchFn: NEVER_FETCH }),
    );
    // ...
  });

  it('q triggers clean exit', async () => {
    const tick = () => new Promise((r) => setTimeout(r, 80));
    // stdin.write('x'); await tick(); ...
  });
});
```

ADAPT for app-focus.test.js:
- Use `tick = () => new Promise(r => setTimeout(r, 80))` literal — 80ms is load-bearing (anything shorter is flaky in CI).
- Build a `fakeFetch` that returns `{ok:true, status:200, json: async () => ({ sessions: [...], count: N, pending: [] })}`.
- Inject `onFocus` spy that increments a counter and returns `{ok:true}` for happy-path test, returns nothing (void) for guard tests where it should NOT be called.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test/dashboard/app-focus.test.js (Wave 0 RED — 3 escenarios integration)</name>
  <files>test/dashboard/app-focus.test.js</files>
  <read_first>
    - .planning/phases/37-attach-handoff-cmux/37-PATTERNS.md (líneas 430-528: §`test/dashboard/app-focus.test.js` con los 3 escenarios verbatim + tick pattern + fakeFetch)
    - .planning/phases/37-attach-handoff-cmux/37-VALIDATION.md (líneas 38-67: rows del Per-Task Verification Map para app-focus.test.js)
    - test/dashboard-render.test.js (líneas 1-77: patrón canónico render + stdin + lastFrame + tick + NEVER_FETCH — copiar la forma)
    - test/dashboard-table.test.js (referencia para fixtures de SessionRecord completo con `alive`/`status`/`task_id`/`workspace_ref`)
    - src/cli/dashboard/App.js (líneas 89-99: props actuales; necesario para saber qué props inyectar; líneas 109, 122-124 para los 3 useStates a respetar)
  </read_first>
  <behavior>
    - Test 1 (`alive=false guard: Enter sobre fila zombie NO llama onFocus y muestra footer rojo`): fakeFetch retorna 1 sesión `alive:false`; counter `focusCalls=0`; tras render + tick(80ms) + `stdin.write('\\r')` + tick(80ms): asserta `focusCalls === 0` y `lastFrame()` contiene `[!] workspace gone (alive=false) — press any key`.
    - Test 2 (`ok path: Enter sobre fila alive llama onFocus con workspace_ref literal y NO renderiza error`): fakeFetch retorna 1 sesión `alive:true, workspace_ref:'workspace:9'`; `onFocus` spy retorna `{ok:true}`; tras Enter: asserta `focusCalls === 1`, `capturedRef === 'workspace:9'`, `lastFrame()` NO contiene `[!]`.
    - Test 3 (`clear-on-any-input: con focusError visible, cualquier tecla lo limpia`): repite Test 1 setup, después escribe `stdin.write('x')` + tick(80ms); asserta `lastFrame()` ya NO contiene `workspace gone` y SÍ contiene el footer normal `↑↓ move · / filter · q quit`. `focusCalls` debe seguir en 0 (la tecla 'x' solo consumió el clear, no propaga al handler de Enter).
  </behavior>
  <action>
    Crear `test/dashboard/app-focus.test.js` desde cero. Estructura literal:

    1. Header comment `// @ts-check` + bloque explicativo: Phase 37 Plan 02 Wave 0 RED — cubre TUI-14 criterio #2 (alive=false guard) + TUI-13 criterio #1 (happy path call) + D-04 (clear-on-any-input). Patrón ink-testing-library espejo de test/dashboard-render.test.js.

    2. Imports:
       ```
       import { describe, it } from 'node:test';
       import assert from 'node:assert/strict';
       import { render } from 'ink-testing-library';
       import { createElement } from 'react';
       import App, { FOCUS_ERR_ZOMBIE } from '../../src/cli/dashboard/App.js';
       ```
       (Importar `FOCUS_ERR_ZOMBIE` constante para asertar literal sin duplicar string. Si el executor decide no exportar la constante, usar regex `/workspace gone \\(alive=false\\) — press any key/` literal.)

    3. Helpers compartidos al top del file:
       ```
       const tick = () => new Promise(r => setTimeout(r, 80));

       function makeFetch(sessions) {
         return async () => ({
           ok: true, status: 200,
           json: async () => ({ sessions, count: sessions.length, pending: [] }),
         });
       }

       function sessionFixture(overrides = {}) {
         return {
           task_id: 'T-1', task_ref: 'KL-99', workspace_ref: 'workspace:9',
           status: 'running', alive: true,
           phase_id: 'p', gsd_mode: 'gsd',
           project_name: 'kodo', project_path: '/x/kodo',
           summary: 'test session',
           started_at: new Date().toISOString(),
           ...overrides,
         };
       }
       ```
       (Los campos extra son load-bearing — sortSessions/applyFilter/rowCells los leen; si faltan, el render explota silenciosamente o muestra '—'.)

    4. `describe('Phase 37 Plan 02: Enter handler + alive guard + clear-on-any-input', () => { ... })` con 3 `it(...)`:

       **Test 1 — alive=false guard:**
       ```
       it('alive=false guard: Enter sobre fila zombie NO llama onFocus y muestra footer rojo', async () => {
         let focusCalls = 0;
         const onFocus = async () => { focusCalls++; return { ok: true }; };
         const fakeFetch = makeFetch([sessionFixture({ task_id: 'Z-1', alive: false })]);

         const { stdin, lastFrame } = render(createElement(App, {
           baseUrl: 'http://localhost:9090', fetchFn: fakeFetch, onFocus,
         }));

         await tick(); // primer poll resuelve + render
         stdin.write('\\r'); // Enter
         await tick();

         assert.equal(focusCalls, 0, 'onFocus NUNCA debe llamarse con alive:false (D-02 guard)');
         assert.match(lastFrame(), /workspace gone \\(alive=false\\) — press any key/);
       });
       ```

       **Test 2 — happy path con onFocus({ok:true}):**
       ```
       it('ok path: Enter sobre fila alive llama onFocus con workspace_ref literal y NO renderiza error', async () => {
         let focusCalls = 0;
         let capturedRef = null;
         const onFocus = async (ref) => { focusCalls++; capturedRef = ref; return { ok: true }; };
         const fakeFetch = makeFetch([sessionFixture({ task_id: 'A-1', alive: true, workspace_ref: 'workspace:9' })]);

         const { stdin, lastFrame } = render(createElement(App, {
           baseUrl: 'http://localhost:9090', fetchFn: fakeFetch, onFocus,
         }));

         await tick();
         stdin.write('\\r');
         await tick();

         assert.equal(focusCalls, 1, 'onFocus invocada exactamente una vez');
         assert.equal(capturedRef, 'workspace:9', 'workspace_ref propagado literal');
         assert.doesNotMatch(lastFrame(), /\\[!\\]/, 'cero footer-error en ok path');
       });
       ```

       **Test 3 — clear-on-any-input:**
       ```
       it('clear-on-any-input: cualquier tecla limpia focusError y restaura footer normal (D-04)', async () => {
         let focusCalls = 0;
         const onFocus = async () => { focusCalls++; return { ok: true }; };
         const fakeFetch = makeFetch([sessionFixture({ task_id: 'Z-1', alive: false })]);

         const { stdin, lastFrame } = render(createElement(App, {
           baseUrl: 'http://localhost:9090', fetchFn: fakeFetch, onFocus,
         }));

         await tick();
         stdin.write('\\r'); // Enter sobre zombie → footer rojo
         await tick();
         assert.match(lastFrame(), /workspace gone/, 'pre-condition: footer-error visible');

         stdin.write('x'); // cualquier tecla → consume y limpia
         await tick();

         assert.doesNotMatch(lastFrame(), /workspace gone/, 'focusError limpiado');
         assert.match(lastFrame(), /↑↓ move · \\/ filter · q quit/, 'footer normal restaurado');
         assert.equal(focusCalls, 0, 'la tecla x NO propaga al handler de Enter, solo limpia');
       });
       ```

    5. NO importar `picocolors`, NO importar `node:child_process` — color-isolation + estructural.

    6. NO `await` los renders con timer real superior a 100ms — el tick=80ms es load-bearing (cualquier valor menor es flakey, mayor es lento).

    Tras crear el archivo, los tests fallarán RED porque App.js aún no tiene la prop `onFocus`, el handler ni la constante exportada. Task 2 + Task 3 los ponen GREEN.
  </action>
  <verify>
    <automated>node --test test/dashboard/app-focus.test.js 2>&amp;1 | head -40 # En estado RED, fallará — verificará al menos que el archivo es ejecutable como test</automated>
  </verify>
  <done>
    - Archivo `test/dashboard/app-focus.test.js` existe con 3 `it(...)` dentro de 1 `describe(...)`.
    - Header comment documenta Phase 37 Plan 02 Wave 0 RED.
    - `tick = () => new Promise(r => setTimeout(r, 80))` presente literal (80ms).
    - `sessionFixture` helper presente con todos los campos load-bearing (`task_id`, `task_ref`, `workspace_ref`, `status`, `alive`, `phase_id`, `gsd_mode`, `project_name`, `project_path`, `summary`, `started_at`).
    - Asserts usan regex literal o constante importada para el mensaje `workspace gone (alive=false) — press any key`.
    - Test 3 asserta tanto el clear (`doesNotMatch /workspace gone/`) como la restauración del footer normal (`match /↑↓ move · \\/ filter · q quit/`) y que `focusCalls === 0` (la tecla x no propaga).
    - `node --test test/dashboard/app-focus.test.js` ejecuta los tests (en RED por ahora — esperado).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender App.js con onFocus prop + focusError state + Enter handler + constantes</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - src/cli/dashboard/App.js COMPLETO (líneas 1-260: necesario para localizar todos los sitios de inserción sin reescribir nada — props, useStates, useInput, render)
    - .planning/phases/37-attach-handoff-cmux/37-PATTERNS.md (líneas 164-262: §`src/cli/dashboard/App.js` con los 6 cambios exactos: prop signature, focusError useState, clear-on-any-input head, Enter handler, async callback discussion, prop pass to SessionTable)
    - .planning/phases/37-attach-handoff-cmux/37-CONTEXT.md (líneas 40-50: D-04 + D-05 mensajes literal; líneas 71-78: Claude's Discretion sobre async/await en useInput)
    - src/cli/dashboard/select.js (líneas 74-80: resolveSelection retorna `{index, taskId}` SIN `.row` — el handler debe usar `filtered[sel.index]`)
    - test/dashboard/app-focus.test.js (recién creado en Task 1 — define los contratos a satisfacer)
  </read_first>
  <behavior>
    - Tests 1-3 de app-focus.test.js pasan (alive=false guard, ok path, clear-on-any-input).
    - El handler de Enter respeta el mode-gating: solo activo en `mode === 'list'`, jamás interfiere con `mode === 'filter'` ni con la reserva D-15 de Esc.
    - Los 3 mensajes literal-estables están izados como constantes exportadas para que tests los asserten sin duplicar strings.
  </behavior>
  <action>
    Editar `src/cli/dashboard/App.js` con 6 cambios precisos (NO reescribir el archivo — usar Edit puntual por bloque):

    **Cambio 1 — Constantes exportadas (al top del módulo, después de los imports líneas 54-66):**
    Insertar:
    ```
    // Mensajes literal-estables D-05 (Phase 34 NON_TTY_MSG pattern). Tests del Plan 02/03 los
    // importan en lugar de duplicar strings — elimina drift entre código y assert.
    export const FOCUS_ERR_ZOMBIE = '[!] workspace gone (alive=false) — press any key';
    export const FOCUS_ERR_ENOENT = '[!] cmux not found in PATH — press any key';
    export const focusErrFailed = (code) => `[!] cmux focus failed (code ${code}) — press any key`;
    ```

    **Cambio 2 — Añadir `onFocus` a la firma de props (línea 89-99):**
    Cambiar:
    ```js
    export default function App({
      baseUrl, fetchFn, now = Date.now,
      schedule, cancel, scheduleTimeout, cancelTimeout, baseMs, maxMs,
    }) {
    ```
    a:
    ```js
    export default function App({
      baseUrl, fetchFn, now = Date.now,
      schedule, cancel, scheduleTimeout, cancelTimeout, baseMs, maxMs,
      onFocus, // Phase 37 D-01: async (workspace_ref) => Promise<FocusResult>
    }) {
    ```

    **Cambio 3 — Añadir `focusError` useState junto a los otros useStates (alrededor de línea 110, después de `lastError`):**
    Insertar:
    ```js
    // Phase 37 D-06: estado local del footer-error. `null` cuando no hay error pendiente.
    // Set en el Enter handler (D-02 pre-flight zombie, o D-06 post-runFocus). Cleared por
    // cualquier tecla en useInput (D-04).
    const [focusError, setFocusError] = useState(/** @type {string | null} */ (null));
    ```

    **Cambio 4 — Insertar clear-on-any-input al INICIO del callback de useInput (línea 171, ANTES del bloque `if (mode === 'filter')`):**
    Modificar la firma del callback de `useInput((input, key) => {` a `useInput(async (input, key) => {` (D-Claude's-Discretion recomendado async para `await onFocus`). Justo después de la apertura, antes del `if (mode === 'filter')`, insertar:
    ```js
    // Phase 37 D-04: cualquier tecla limpia focusError ANTES de procesar el resto.
    // La tecla se CONSUME (no propaga a Enter / q / etc.) — el operador hace "dismiss" del
    // error y vuelve a interactuar con un keystroke separado. No choca con D-15 Phase 36 Esc
    // modal porque el dismiss aplica antes del mode-gate.
    if (focusError != null) {
      setFocusError(null);
      return;
    }
    ```

    **Cambio 5 — Insertar Enter handler en bloque `mode === 'list'` (después de las ramas `q`/`/`/`upArrow`/`downArrow`, ANTES del comentario `// key.escape: DELIBERADAMENTE ignorado`, línea ~218):**
    Insertar:
    ```js
    if (key.return) {
      // Phase 37 D-02 + D-06: zombie pre-flight + invocación never-throws de onFocus.
      // sel retorna { index, taskId } solamente (no .row) — leemos la fila del array filtrado.
      const row = sel.index >= 0 ? filtered[sel.index] : null;
      if (!row) return; // lista vacía: no-op
      if (row.alive === false) {
        // D-02: cero invocación de cmux. La marca textual `(zombie)` ya pinta el estado
        // (Phase 36 D-09); este mensaje confirma el rechazo en el footer.
        setFocusError(FOCUS_ERR_ZOMBIE);
        return;
      }
      // D-06: runFocus es never-throws (Plan 01 D-01) — siempre resuelve con discriminado.
      const result = await onFocus?.(row.workspace_ref);
      if (result && !result.ok) {
        if (result.code === 'ENOENT') {
          setFocusError(FOCUS_ERR_ENOENT);
        } else {
          // NON_ZERO_EXIT (detail = code numérico) o SPAWN_ERROR (detail = message).
          const n = result.detail ?? 'unknown';
          setFocusError(focusErrFailed(n));
        }
      }
      return;
    }
    ```

    **Cambio 6 — Propagar `focusError` a SessionTable en el render (línea 245-256):**
    Cambiar:
    ```js
    createElement(SessionTable, {
      rows: filtered, selectedIndex: sel.index,
      counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt,
      mode, query, hasQuery,
    }),
    ```
    a:
    ```js
    createElement(SessionTable, {
      rows: filtered, selectedIndex: sel.index,
      counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt,
      mode, query, hasQuery,
      focusError, // Phase 37 D-04: render condicional del footer-error rojo en SessionTable
    }),
    ```

    **Verificación:** Tras los 6 cambios, ejecutar `node --test test/dashboard/app-focus.test.js`. Test 1 (alive guard) debería pasar en cuanto SessionTable renderice `focusError` rojo (Task 3); Tests 2 (ok path) y 3 (clear-on-any-input) deberían pasar ya con solo Cambios 1-5 (no necesitan SessionTable extendido).

    NO añadir `await` espurio. NO cambiar las ramas existentes `q`/`/`/`upArrow`/`downArrow`. NO tocar las ramas de `mode === 'filter'`. NO mover el comentario `// key.escape: DELIBERADAMENTE ignorado`. NO añadir try/catch alrededor de `await onFocus` — runFocus es never-throws por contrato Plan 01.
  </action>
  <verify>
    <automated>node --test test/dashboard/app-focus.test.js -t "ok path" -t "clear-on-any-input" 2>&amp;1 | tail -20 # Tests 2 y 3 deben pasar tras este task (Test 1 requiere también el cambio de SessionTable de Task 3)</automated>
  </verify>
  <done>
    - 3 constantes exportadas presentes en App.js: `grep -cE "export const FOCUS_ERR_ZOMBIE|export const FOCUS_ERR_ENOENT|export const focusErrFailed" src/cli/dashboard/App.js` == 3.
    - Prop `onFocus` añadida a la firma de props.
    - `useState` de `focusError` presente: `grep -c "focusError" src/cli/dashboard/App.js` ≥ 5 (state, set en zombie, set en ENOENT, set en failed, clear en useInput head, prop pass).
    - Handler de Enter dentro del bloque `mode === 'list'`: `grep -c "key.return" src/cli/dashboard/App.js` ≥ 1.
    - useInput callback es `async`: `grep -cE "useInput\\(\\s*async" src/cli/dashboard/App.js` ≥ 1.
    - `await onFocus?.(row.workspace_ref)` presente: `grep -c "await onFocus" src/cli/dashboard/App.js` ≥ 1.
    - SessionTable recibe la prop `focusError`: `grep -c "focusError," src/cli/dashboard/App.js` ≥ 2.
    - Tests 2 y 3 (ok path, clear-on-any-input) pasan: `node --test test/dashboard/app-focus.test.js -t "ok path"` y `-t "clear-on-any-input"` exit 0.
    - `grep -c "picocolors" src/cli/dashboard/App.js` == 0.
    - Suite completa de App.js no regresiona (q sigue funcionando, filter sigue funcionando, etc.): `node --test test/dashboard-render.test.js test/dashboard-table.test.js test/dashboard-filter.test.js` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender SessionTable.js con prop focusError + render condicional del footer-error rojo</name>
  <files>src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js COMPLETO (líneas 1-211: necesario para localizar los 3 returns y el filterLine actual)
    - .planning/phases/37-attach-handoff-cmux/37-PATTERNS.md (líneas 265-305: §`src/cli/dashboard/SessionTable.js` con los 3 cambios literales: firma, errorLine, precedencia)
    - .planning/phases/37-attach-handoff-cmux/37-CONTEXT.md (líneas 40-42: D-04 — render condicional, color-isolation D-12)
    - test/dashboard/app-focus.test.js (Test 1 asserta el render del mensaje rojo — define el contrato)
  </read_first>
  <behavior>
    - Test 1 de app-focus.test.js pasa (alive=false guard + footer rojo visible).
    - El errorLine reemplaza al filterLine cuando `focusError != null` (precedencia: error gana). Cuando `focusError == null`, comportamiento original intacto.
    - El color rojo viene de `<Text color="red">` literal — cero `picocolors`, cero ANSI escapes.
  </behavior>
  <action>
    Editar `src/cli/dashboard/SessionTable.js` con 3 cambios precisos (Edit puntual):

    **Cambio 1 — Añadir `focusError` a la firma de props (líneas 119-130):**
    Cambiar:
    ```js
    export default function SessionTable({
      rows, selectedIndex, counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt,
      hasQuery = false, mode = 'list', query = '',
    }) {
    ```
    a:
    ```js
    export default function SessionTable({
      rows, selectedIndex, counts, connected, lastGoodCount, lastGoodAt, lastAttemptAt,
      hasQuery = false, mode = 'list', query = '',
      focusError = null, // Phase 37 D-04: string del footer-error rojo (o null cuando no hay error pendiente)
    }) {
    ```

    Y actualizar el bloque JSDoc encima (líneas 104-117) añadiendo:
    ```js
    * @param {string|null} [props.focusError] - Phase 37 D-04: si != null, sustituye el footer
    *   (filterLine y/o `↑↓ move · / filter · q quit`) por el mensaje rojo del error. Color SOLO
    *   vía `<Text color="red">` (color-isolation D-12 Phase 34).
    ```

    **Cambio 2 — Construir `errorLine` con el patrón espejo de `filterLine` (después del bloque de `filterLine` líneas 145-148, antes del bloque de precedencia de estados vacíos línea 150):**
    Insertar:
    ```js
    // Phase 37 D-04: errorLine es el render condicional del footer-error rojo. Espejo exacto
    // del patrón filterLine arriba. Color del rojo vía <Text color="red"> ink (color-isolation
    // D-12 Phase 34, cero picocolors).
    const errorLine =
      focusError != null
        ? h(Box, { marginTop: 1 }, h(Text, { color: 'red' }, focusError))
        : null;
    ```

    **Cambio 3 — Aplicar precedencia (error gana a filter) en los 3 returns:**

    En los 3 sitios donde aparece `filterLine` como prop posicional, reemplazar por `(errorLine ?? filterLine)`:

    Sitio 1 (línea 156, branch `!connected && lastGoodAt == null`):
    ```js
    return h(Box, { flexDirection: 'column' }, header, (errorLine ?? filterLine));
    ```

    Sitio 2 (línea 165, branch `rows.length === 0`):
    ```js
    return h(
      Box, { flexDirection: 'column' },
      header,
      h(Box, { marginTop: 1 }, h(Text, { dimColor: true }, emptyCopy)),
      (errorLine ?? filterLine),
    );
    ```

    Sitio 3 (línea 209, return final con data rows):
    ```js
    return h(
      Box, { flexDirection: 'column' },
      header,
      h(Box, { marginTop: 1, flexDirection: 'column' }, columnHeader, ...dataRows),
      (errorLine ?? filterLine),
    );
    ```

    **Verificación:** Tras los 3 cambios, ejecutar `node --test test/dashboard/app-focus.test.js`. Los 3 tests deben pasar GREEN (Test 1 requería este cambio; Tests 2 y 3 ya pasaban tras Task 2).

    NO cambiar el resto de la tabla (`countsLabel`, `LiveIndicator`, `cell`, `columnHeader`, `dataRows`). NO mover los 3 returns. NO tocar `filterLine` original — el precedence operator `??` deja la lógica original intacta cuando `focusError == null`.
  </action>
  <verify>
    <automated>node --test test/dashboard/app-focus.test.js</automated>
  </verify>
  <done>
    - SessionTable acepta prop `focusError`: `grep -c "focusError" src/cli/dashboard/SessionTable.js` ≥ 3 (firma, errorLine const, JSDoc).
    - `errorLine` construido con `<Text color="red">`: `grep -c "color: 'red'\\|color: \\\"red\\\"" src/cli/dashboard/SessionTable.js` ≥ 1.
    - Precedencia `(errorLine ?? filterLine)` presente en los 3 returns: `grep -c "errorLine ?? filterLine" src/cli/dashboard/SessionTable.js` == 3.
    - `grep -c "picocolors" src/cli/dashboard/SessionTable.js` == 0 (NO-PICOCOLORS).
    - 3 tests de app-focus.test.js GREEN: `node --test test/dashboard/app-focus.test.js` exit 0 con 3/3 pass.
    - Suite de table no regresiona: `node --test test/dashboard-table.test.js` exit 0.
    - Walker color-isolation no detecta leakers: `node --test test/format-isolation.test.js` exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React tree (App.js) → onFocus prop | `row.workspace_ref` cruza al callback inyectado en Plan 03 |
| User input (`stdin.write`) → useInput | Keystrokes consumidos por el handler de Enter / clear-on-any-input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | Mensaje del footer-error rojo | mitigate | Mensajes izados a constantes exportadas (`FOCUS_ERR_*`); tests asertan literal; cero string-interpolation con valores externos salvo el `code` numérico de `focusErrFailed(n)`. El `n` viene de `result.detail` de runFocus (Plan 01) que solo puede ser número o 'unknown'. |
| T-37-05 | Information Disclosure | focusError state across renders | accept | `focusError` vive en useState local del componente App; cero persistencia, cero log, cero cross-instance leak. Limpiado por cualquier tecla siguiente. |
| T-37-06 | Repudiation | Acción de focus invocada por error | mitigate | Pre-flight zombie guard (D-02): `alive === false` cortocircuita ANTES de tocar onFocus, eliminando la posibilidad de invocación accidental sobre workspaces muertos. |
</threat_model>

<verification>
**Plan-level checks:**

1. `node --test test/dashboard/app-focus.test.js` → exit 0, 3/3 pass.
2. `node --test test/dashboard-render.test.js test/dashboard-table.test.js test/dashboard-filter.test.js test/dashboard-non-tty.test.js test/dashboard-baseurl.test.js test/dashboard-altscreen.test.js test/dashboard-status-line.test.js` → exit 0 (cero regresiones en suites de Phase 34-36).
3. `node --test test/format-isolation.test.js` → exit 0 (walker cubre App.js y SessionTable.js sin leakers).
4. `grep -cE "FOCUS_ERR_ZOMBIE|FOCUS_ERR_ENOENT|focusErrFailed" src/cli/dashboard/App.js` ≥ 3 (constantes exportadas D-05).
5. `grep -c "await onFocus" src/cli/dashboard/App.js` ≥ 1 (handler invoca el callback inyectado).
6. `grep -c "errorLine ?? filterLine" src/cli/dashboard/SessionTable.js` == 3 (precedencia en los 3 returns).
7. `grep -c "stdio\\|spawn" src/cli/dashboard/App.js src/cli/dashboard/SessionTable.js` == 0 (nada de proceso lifecycle en la capa React).

**Cross-cutting invariants asserted by this plan:**
- NO-PICOCOLORS (via walker + grep en los 3 archivos modificados).
- LITERAL-STABLE MESSAGES (constantes exportadas + tests usan import o regex literal).
- NO-ALT-SCREEN-MUTATION (App.js / SessionTable.js no escriben `\\x1b[?1049h/l` — verificable por grep).
- NO-SIGNAL-HANDLER-MUTATION (App.js / SessionTable.js no llaman `process.on` / `process.once` / `process.removeListener` — verificable por grep).
</verification>

<success_criteria>
- [ ] `test/dashboard/app-focus.test.js` existe con 3 tests bajo 1 describe.
- [ ] 3 tests pasan GREEN: `node --test test/dashboard/app-focus.test.js` exit 0.
- [ ] `App.js` exporta `FOCUS_ERR_ZOMBIE`, `FOCUS_ERR_ENOENT`, `focusErrFailed(code)` literal-estables.
- [ ] `App.js` acepta prop `onFocus` y tiene useState para `focusError`.
- [ ] El handler de `key.return` vive dentro del bloque `mode === 'list'`, después de las ramas existentes, antes del comentario `// key.escape: ...`.
- [ ] El clear-on-any-input vive al INICIO del callback de useInput (antes del mode-gate).
- [ ] `SessionTable.js` acepta prop `focusError` y construye `errorLine` con `<Text color="red">`.
- [ ] La precedencia `(errorLine ?? filterLine)` aplica en los 3 returns de SessionTable.
- [ ] Cero regresión en otras suites de dashboard (Phases 34-36).
- [ ] Walker `format-isolation` no detecta nuevos leakers de picocolors.
- [ ] `App.js` / `SessionTable.js` no contienen `process.on`, `process.removeListener`, `\\x1b[?1049`, `spawn`, ni `stdio` — preserva NO-ALT-SCREEN-MUTATION y NO-SIGNAL-HANDLER-MUTATION (cross-cutting invariantes Phase 37 VALIDATION.md).
</success_criteria>

<output>
Crear `.planning/phases/37-attach-handoff-cmux/37-02-SUMMARY.md` al completar con:
- Hash de commit final.
- Comandos exactos que verifican green: `node --test test/dashboard/app-focus.test.js` + `node --test test/dashboard-table.test.js test/dashboard-render.test.js test/dashboard-filter.test.js`.
- Decisiones tomadas en Claude's Discretion (`async useInput` vs `.then()`, ubicación del render del footer — confirmada SessionTable per recomendación PATTERNS.md).
- Cualquier divergencia respecto al plan documentada con justificación.
</output>
</content>
</invoke>