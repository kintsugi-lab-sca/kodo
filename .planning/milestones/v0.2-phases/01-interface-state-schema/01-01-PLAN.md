---
phase: 01-interface-state-schema
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface.js
  - src/session/state.js
  - test/interface.test.js
autonomous: true
requirements:
  - INTF-01
  - INTF-02
  - INTF-03

must_haves:
  truths:
    - "src/interface.js existe y exporta las constantes VALID_PRIORITIES y TASK_PROVIDER_METHODS"
    - "TaskProvider typedef tiene exactamente 8 métodos (init + 7)"
    - "TaskItem typedef tiene todos los campos acordados en CONTEXT.md (id, ref, title, description, labels, projectId, projectName, groups, url, priority)"
    - "TriggerEvent typedef tiene los 4 campos: taskRef, action, provider, raw"
    - "La typedef Session en state.js usa task_id/task_ref/provider (no plane_id/plane_identifier)"
    - "Los tests de importación en test/interface.test.js pasan"
  artifacts:
    - path: "src/interface.js"
      provides: "Typedefs JSDoc para TaskProvider, TaskItem, TriggerEvent + constantes de ayuda"
      exports: ["VALID_PRIORITIES", "TASK_PROVIDER_METHODS"]
    - path: "src/session/state.js"
      provides: "Typedef Session actualizada con campos provider-agnostic"
    - path: "test/interface.test.js"
      provides: "Tests de smoke para importaciones y constantes de interface.js"
  key_links:
    - from: "src/interface.js"
      to: "src/session/state.js"
      via: "Typedef Session referencia los contratos definidos en interface.js"
      pattern: "task_id.*task_ref.*provider"
---

<objective>
Definir los contratos de datos provider-agnostic de kodo v0.2 mediante JSDoc typedefs.

Purpose: Establecer la interfaz `TaskProvider` (8 métodos), `TaskItem` (shape canónica), y `TriggerEvent` antes de que Phase 2 implemente el adapter de Plane. Sin estos contratos claros, Phase 2 no tiene contra qué programar.

Output: `src/interface.js` con typedefs + constantes de ayuda, `src/session/state.js` con typedef Session actualizada, `test/interface.test.js` con smoke tests.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-interface-state-schema/01-CONTEXT.md
@.planning/phases/01-interface-state-schema/01-RESEARCH.md

<interfaces>
<!-- Estado actual del codebase — leer antes de implementar -->

Desde src/session/state.js (typedef a reemplazar):
```javascript
/**
 * @typedef {{
 *   workspace_ref: string,
 *   session_id: string,
 *   plane_id: string,         // RENOMBRAR a task_id
 *   plane_identifier: string, // RENOMBRAR a task_ref
 *   project_id: string,
 *   summary: string,
 *   status: 'running'|'done'|'error'|'review',
 *   started_at: string,
 *   project_path: string,
 * }} Session
 */
```

Desde src/config.js (referencia de DEFAULT_CONFIG actual — no modificar en este plan):
```javascript
const DEFAULT_CONFIG = {
  plane: {
    base_url: 'https://tasks.kintsugi-lab.com',
    api_key_env: 'PLANE_API_KEY',
    workspace_slug: 'k-lab',
    projects: [],
    trigger_state: 'In Progress',
    done_state: 'Done',
    review_state: 'In review',
  },
  // cmux, claude, server sin cambios
};
```

Desde test/state.test.js (patrón de testing existente a seguir):
```javascript
import { describe, it, beforeEach } from 'node:test';
import assert from 'node:assert/strict';
// Tests usan node:test built-in — mismo patrón para interface.test.js
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/interface.js con typedefs JSDoc y constantes de ayuda</name>
  <files>src/interface.js, test/interface.test.js</files>
  <behavior>
    - Test 1: TASK_PROVIDER_METHODS es un array con exactamente 8 strings: ['init', 'getTask', 'updateTaskState', 'addComment', 'listPendingTasks', 'parseTriggerEvent', 'verifySignature', 'resolveRef']
    - Test 2: VALID_PRIORITIES es un array con exactamente 5 strings: ['urgent', 'high', 'medium', 'low', 'none']
    - Test 3: El módulo src/interface.js importa sin errores (smoke test de importación)
  </behavior>
  <action>
    Escribir los tests en test/interface.test.js primero (RED), luego implementar src/interface.js (GREEN).

    **test/interface.test.js** — estructura:
    ```javascript
    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';
    import { TASK_PROVIDER_METHODS, VALID_PRIORITIES } from '../src/interface.js';

    describe('interface contracts', () => {
      it('TASK_PROVIDER_METHODS tiene los 8 métodos exactos', () => {
        const expected = ['init', 'getTask', 'updateTaskState', 'addComment',
          'listPendingTasks', 'parseTriggerEvent', 'verifySignature', 'resolveRef'];
        assert.deepEqual(TASK_PROVIDER_METHODS, expected);
      });

      it('VALID_PRIORITIES tiene los 5 valores normalizados', () => {
        assert.deepEqual(VALID_PRIORITIES, ['urgent', 'high', 'medium', 'low', 'none']);
      });
    });
    ```

    **src/interface.js** — estructura completa:
    - `// @ts-check` al inicio
    - `@typedef` para `TaskItem` con campos exactos del CONTEXT.md:
      `{ id: string, ref: string, title: string, description: string, labels: string[], projectId: string, projectName: string, groups: string[], url: string, priority: 'urgent'|'high'|'medium'|'low'|'none'|null }`
    - `@typedef` para `TriggerEvent` con campos: `{ taskRef: string, action: string, provider: string, raw: object }`
    - `@typedef` para `TaskProvider` usando object literal con todas las funciones tipadas:
      `init: () => Promise<void>`, `getTask: (ref: string) => Promise<TaskItem>`, `updateTaskState: (task: TaskItem, stateName: string) => Promise<void>`, `addComment: (task: TaskItem, markdownText: string) => Promise<void>`, `listPendingTasks: () => Promise<TaskItem[]>`, `parseTriggerEvent: (rawPayload: object) => TriggerEvent|null`, `verifySignature: (rawBody: string, headers: object) => boolean`, `resolveRef: (humanRef: string) => Promise<string>`
    - Exportar constante `TASK_PROVIDER_METHODS` como array frozen de los 8 nombres de método en el mismo orden que el typedef
    - Exportar constante `VALID_PRIORITIES` como array frozen: `['urgent', 'high', 'medium', 'low', 'none']`
    - NO usar `@callback` — usar `@typedef` con object literal (patrón del codebase)
    - NO exportar funciones de runtime — solo constantes y typedefs (los typedefs son solo para el editor)
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/interface.test.js</automated>
  </verify>
  <done>3 tests pasan. src/interface.js existe con los 3 typedefs y 2 constantes exportadas. TASK_PROVIDER_METHODS tiene exactamente 8 métodos. VALID_PRIORITIES tiene 5 valores.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar typedef Session en src/session/state.js al nuevo schema provider-agnostic</name>
  <files>src/session/state.js</files>
  <action>
    Actualizar SOLO la typedef `Session` y la typedef `State` en `src/session/state.js`. NO cambiar ninguna función pública (loadState, saveState, addSession, removeSession, updateSession, getSession, listSessions, findSession). Los consumers (stop.js, manager.js, etc.) seguirán usando los nombres viejos — esto genera warnings de @ts-check esperados que se resuelven en Phase 3.

    **Cambios exactos en la typedef Session:**
    - `plane_id: string` → `task_id: string` (comentario: `// UUID del task en el provider activo`)
    - `plane_identifier: string` → `task_ref: string` (comentario: `// Referencia humana: "KL-42", "#42"`)
    - Añadir campo nuevo: `provider: string` (comentario: `// "plane", "github", etc.`)

    **Cambios en typedef State:**
    - Añadir `schema_version: number` al typedef State: `{ schema_version: number, sessions: Record<string, Session> }`

    **Actualizar parámetro de addSession:** El parámetro `planeId` en `addSession(planeId, session)` y funciones similares se puede renombrar a `taskId` para coherencia, pero esto es cosmético — la firma pública no cambia.

    NOTA: La migración real del JSON (backup + escritura) se implementa en Plan 02. Este task solo actualiza los typedefs.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/interface.test.js && node --test test/state.test.js</automated>
  </verify>
  <done>Los tests existentes de state.test.js siguen pasando. El typedef Session tiene task_id, task_ref, provider. El typedef State tiene schema_version.</done>
</task>

</tasks>

<verification>
Tests verdes:
- `node --test test/interface.test.js` — 3 tests pasan
- `node --test test/state.test.js` — todos los tests existentes siguen pasando

Revisión manual de src/interface.js:
- Los 3 typedefs están presentes (TaskItem, TriggerEvent, TaskProvider)
- TASK_PROVIDER_METHODS.length === 8
- VALID_PRIORITIES.length === 5
- Ningún campo es específico de Plane (sin plane_id, sin description_html)
</verification>

<success_criteria>
- src/interface.js existe con TaskProvider (8 métodos), TaskItem (10 campos), TriggerEvent (4 campos)
- Los typedefs no contienen ningún campo específico de Plane
- TASK_PROVIDER_METHODS y VALID_PRIORITIES son arrays frozen exportados
- La typedef Session en state.js usa task_id/task_ref/provider
- La typedef State tiene schema_version: number
- Todos los tests pasan: `node --test test/interface.test.js && node --test test/state.test.js`
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-interface-state-schema/01-01-SUMMARY.md` con lo implementado.
</output>
