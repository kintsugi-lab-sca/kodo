---
phase: 51-backfill-nyquist-v0-11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VALIDATION.md
  - .planning/milestones/v0.11-phases/45-inyecci-n-de-plan-ligero-universal/45-VALIDATION.md
  - .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VALIDATION.md
  - .planning/STATE.md
autonomous: true
requirements: [NYQ-03]

must_haves:
  truths:
    - "Phases 44, 45 y 46 (v0.11) tienen VALIDATION.md con nyquist_compliant: true citando su evidencia existente (VERIFICATION.md + HUMAN-UAT donde existe) sin re-ejecutar la suite (Success Criteria 1)"
    - "Ningún VALIDATION.md objetivo conserva status: draft ni nyquist_compliant: false; los 3 stubs quedan reemplazados in-place (Success Criteria 2)"
    - "STATE.md ## Deferred Items refleja las 3 filas nyquist (44/45/46) como saldadas referenciando Phase 51 (NYQ-03); la línea intro pasa a tiempo pasado/cerrado (Success Criteria 2)"
    - "git diff -- src/ test/ bin/ queda vacío (D-05: Tier 1 doc-only — invariante duro) (Success Criteria 3)"
    - "D-01: 1 solo plan secuencial Wave 1 (Task 1 = 3 UPDATE in-place de VALIDATION, Task 2 = reconciliación STATE.md) — evita contención en el STATE.md compartido"
    - "D-02: 3 UPDATE in-place (los 3 drafts existentes → compliant preservando estructura) — NO NEW (difiere de Phase 47, que mezclaba NEW+UPDATE)"
    - "D-03: cada VALIDATION.md cita su evidencia más fuerte (44/45 → VERIFICATION.md passed; 46 → VERIFICATION.md passed + 46-HUMAN-UAT.md 2/2) sin re-ejecutar la suite; ninguna fase marcada N/A"
    - "D-04: la reconciliación de STATE.md toca solo las 3 filas nyquist (44/45/46); las filas frontmatter, verification y code (WARNING-01) quedan intactas — son deuda distinta fuera de NYQ-03"
  artifacts:
    - path: ".planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VALIDATION.md"
      provides: "VALIDATION Phase 44 (UPDATE draft→compliant), cita 44-VERIFICATION.md (passed 10/10, PLAN-01/PLAN-02/TUI-18/TUI-19)"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.11-phases/45-inyecci-n-de-plan-ligero-universal/45-VALIDATION.md"
      provides: "VALIDATION Phase 45 (UPDATE draft→compliant), cita 45-VERIFICATION.md (passed 7/7, PLAN-03)"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VALIDATION.md"
      provides: "VALIDATION Phase 46 (UPDATE draft→compliant), cita 46-VERIFICATION.md (passed 6/6, PLAN-04) + 46-HUMAN-UAT.md (2/2 pass, sesión ROMAN-173)"
      contains: "nyquist_compliant: true"
    - path: ".planning/STATE.md"
      provides: "Deferred Items reconciliado: 3 filas nyquist (44/45/46) saldadas + intro en pasado"
      contains: "## Deferred Items"
  key_links:
    - from: "51-01 task 1 (NYQ-03)"
      to: ".../44-VERIFICATION.md, .../45-VERIFICATION.md, .../46-VERIFICATION.md, .../46-HUMAN-UAT.md"
      via: "tabla Per-Task Verification Map citando fichero+resultado real (no re-corrida)"
      pattern: "VERIFICATION|HUMAN-UAT"
    - from: "51-01 task 2"
      to: ".planning/STATE.md ## Deferred Items"
      via: "toggle de las 3 filas nyquist a saldado + intro a pasado; filas frontmatter/verification/code intactas"
      pattern: "nyquist"
---

<objective>
Saldar la deuda Nyquist heredada de v0.11 actualizando in-place los 3 `VALIDATION.md` draft de las Phases 44/45/46 a **citation-based** (`nyquist_compliant: true`) y reconciliar `STATE.md ## Deferred Items`. Espejo estructural directo de v0.11 Phase 47 / v0.8 Phase 33 Bloque B: cada `VALIDATION.md` cita la evidencia empírica ya existente (VERIFICATION.md + HUMAN-UAT donde existe) como cobertura por-dimensión, **sin re-ejecutar la suite**.

Diferencia con Phase 47: las 3 fases objetivo YA tienen un `VALIDATION.md` draft, luego son **3 UPDATE in-place** (no archivos NEW) — togglear flags + poblar la tabla de citación + preservar el resto del documento. Más simple que Phase 47.

Purpose: Cerrar el último item de deuda de proceso de v0.11 dejando trazabilidad nyquist completa de las fases shipped (NYQ-03). Doc-only Tier 1, independiente del resto del milestone v0.12: no bloquea ni es bloqueada.

Output: 3 `VALIDATION.md` actualizados in-place (44/45/46) y `STATE.md` reconciliado. Cero cambios en `src/`, `test/`, `bin/` (D-05, Tier 1 doc-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Precedente estructural DIRECTO — replicar forma, NO reinventar formato
@.planning/milestones/v0.11-phases/47-backfill-de-deuda-nyquist/47-01-PLAN.md
@.planning/milestones/v0.11-phases/47-backfill-de-deuda-nyquist/47-CONTEXT.md

# Template de estructura VALIDATION.md citation-based ya-compliant (v0.10, mismo milestone-family)
@.planning/milestones/v0.10-phases/40-provider-state-contrato-providers-enrichment/40-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NYQ-03 — UPDATE in-place 44/45/46-VALIDATION.md (draft → compliant, citation-based)</name>
  <files>.planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VALIDATION.md, .planning/milestones/v0.11-phases/45-inyecci-n-de-plan-ligero-universal/45-VALIDATION.md, .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VALIDATION.md</files>
  <read_first>
  - .planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VALIDATION.md — el draft EXISTENTE (status: draft, nyquist_compliant: false). UPDATE in-place: preservar toda su estructura (Test Infrastructure, Sampling, Per-Task Verification Map, Wave 0, Manual-Only, Sign-Off); solo togglear flags y poblar la tabla con citas reales.
  - .planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VERIFICATION.md — evidencia primaria de Phase 44 (status: passed, score 10/10). Cubre PLAN-01, PLAN-02, TUI-18, TUI-19. Behavioral Spot-Checks: dashboard-plan.test.js 15 pass, dashboard-overlay.test.js 16 pass, dashboard-select.test.js 32 pass, dashboard-table.test.js 41 pass, format-isolation.test.js 8 pass, full suite 1245 pass / 1 skip / 0 fail.
  - .planning/milestones/v0.11-phases/45-inyecci-n-de-plan-ligero-universal/45-VALIDATION.md — el draft EXISTENTE (status: draft, nyquist_compliant: false). UPDATE in-place igual que 44.
  - .planning/milestones/v0.11-phases/45-inyecci-n-de-plan-ligero-universal/45-VERIFICATION.md — evidencia primaria de Phase 45 (status: passed, score 7/7). Cubre PLAN-03. Behavioral Spot-Checks: 58 tests target (session-start.test.js + gsd-context.test.js) pass 58 / fail 0, full suite npm test 1252 pass / 1 skip / 0 fail.
  - .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VALIDATION.md — el draft EXISTENTE (status: draft, nyquist_compliant: false; este es el más esquelético — placeholders {N}/{command}). UPDATE in-place: poblar la tabla y togglear flags preservando el esqueleto de secciones.
  - .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VERIFICATION.md — evidencia primaria automated de Phase 46 (status: passed, score 6/6). Cubre PLAN-04. Behavioral Spot-Checks: dashboard-plan.test.js 21 pass / 0 fail, dashboard-overlay.test.js 18 pass / 0 fail, format-isolation.test.js 8 pass / 0 fail.
  - .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-HUMAN-UAT.md — evidencia de verificación humana/visual de Phase 46 (status: complete, 2/2 pass, 0 issues; sesión real ROMAN-173 quick/non-GSD; copy dim confirmado). Cita complementaria para la dimensión de verificación manual/visual de PLAN-04.
  - .planning/milestones/v0.11-phases/47-backfill-de-deuda-nyquist/47-01-PLAN.md — PLANTILLA estructural del backfill (Task 2 hace exactamente UPDATE in-place de drafts). Replicar la forma de la acción y de los acceptance_criteria.
  - .planning/milestones/v0.10-phases/40-provider-state-contrato-providers-enrichment/40-VALIDATION.md — ejemplo ya-compliant de la estructura citation-based (frontmatter + Per-Task Verification Map + Sign-Off). Referencia de forma para el poblado de la tabla.
  </read_first>
  <action>
  UPDATE in-place los 3 `VALIDATION.md` draft. En CADA uno: en el frontmatter, cambiar `status: draft` → `status: approved` y `nyquist_compliant: false` → `nyquist_compliant: true`; rellenar la tabla Per-Task Verification Map (hoy con filas TBD/⬜ pending/placeholders) con las citas reales de evidencia (fichero + resultado por dimensión); marcar el bloque Validation Sign-Off (incluida la casilla `nyquist_compliant: true set in frontmatter`) y cambiar `**Approval:** pending` → `**Approval:** approved`. Preservar TODO el resto del documento (Test Infrastructure, Sampling Rate, Wave 0 Requirements, Manual-Only Verifications) — solo togglear flags y poblar citación. Añadir una nota explícita de reconstrucción retroactiva que declare "cobertura citada de la evidencia existente, sin re-ejecutar la suite". NO re-correr tests.

  Citas concretas por fase (cada Success Criteria de la fase mapeado a ≥1 cita de evidencia real):
  - 44-VALIDATION.md: poblar las filas PLAN-01, PLAN-02, TUI-18, TUI-19 citando `44-VERIFICATION.md` (passed, score 10/10) con los resultados reales de sus Behavioral Spot-Checks (p. ej. `dashboard-plan.test.js` 15 pass/0 fail, `dashboard-overlay.test.js` 16 pass/0 fail, `dashboard-table.test.js` 41 pass/0 fail, `format-isolation.test.js` 8 pass/0 fail, full suite 1245 pass/1 skip/0 fail). No hay HUMAN-UAT para 44 — citar solo VERIFICATION.
  - 45-VALIDATION.md: poblar la(s) fila(s) PLAN-03 citando `45-VERIFICATION.md` (passed, score 7/7) con los resultados reales (target suite `session-start.test.js`+`gsd-context.test.js` 58 pass/0 fail, full suite `npm test` 1252 pass/1 skip/0 fail). No hay HUMAN-UAT para 45 — citar solo VERIFICATION.
  - 46-VALIDATION.md: poblar la fila PLAN-04 (reemplazando los placeholders `{N}`/`{command}`/`{behavior}`) citando `46-VERIFICATION.md` (passed, score 6/6) como evidencia primaria automated (p. ej. `dashboard-plan.test.js` 21 pass/0 fail, `dashboard-overlay.test.js` 18 pass/0 fail, `format-isolation.test.js` 8 pass/0 fail) Y `46-HUMAN-UAT.md` (2/2 pass, sesión ROMAN-173) como evidencia de la dimensión de verificación manual/visual (snapshot congelado + copy dim no-rojo). Rellenar los campos placeholder restantes (Estimated runtime, quick/full command) a partir de los valores reales del VERIFICATION.

  Ninguna fase marcada N/A — las 3 tienen evidencia empírica real. No tocar nada bajo `src/`, `test/`, `bin/`. No editar `REQUIREMENTS.md` (NYQ-03 se auto-marca al cierre del plan vía roadmap.update-plan-progress — verificar, no editar a mano).
  </action>
  <verify>
    <automated>for f in .planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VALIDATION.md .planning/milestones/v0.11-phases/45-inyecci-n-de-plan-ligero-universal/45-VALIDATION.md .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VALIDATION.md; do test -f "$f" || { echo "MISSING $f"; exit 1; }; grep -q '^nyquist_compliant: true' "$f" || { echo "NOT COMPLIANT $f"; exit 1; }; grep -q '^nyquist_compliant: false' "$f" && { echo "STILL DRAFT $f"; exit 1; }; grep -q 'VERIFICATION' "$f" || { echo "NO CITATION $f"; exit 1; }; done; grep -q 'HUMAN-UAT' .planning/milestones/v0.11-phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-VALIDATION.md || { echo "46 missing UAT citation"; exit 1; }; git diff --quiet -- src/ test/ bin/ || { echo "TIER1 VIOLATION: code touched"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
  - Los 3 `VALIDATION.md` (44/45/46) existen en sus rutas exactas, contienen `nyquist_compliant: true` en frontmatter, y ya NO contienen `nyquist_compliant: false` ni `status: draft`.
  - 44 cita `44-VERIFICATION.md` (PLAN-01/PLAN-02/TUI-18/TUI-19); 45 cita `45-VERIFICATION.md` (PLAN-03); 46 cita `46-VERIFICATION.md` (PLAN-04, automated) + `46-HUMAN-UAT.md` (verificación humana/visual). Ninguna fase marcada N/A.
  - Cada VALIDATION conserva sus secciones no-nyquist preexistentes (Test Infrastructure, Sampling, Wave 0, Manual-Only); en 46 los placeholders `{N}`/`{command}`/`{behavior}` quedan reemplazados por valores reales.
  - Las citas referencian ficheros de evidencia reales por nombre con su resultado (p. ej. "passed 10/10", "21 pass/0 fail"); ninguna sección instruye re-correr tests.
  - `git diff -- src/ test/ bin/` permanece vacío.
  </acceptance_criteria>
  <done>NYQ-03 Success Criteria 1 cubierto: 44/45/46 togglean a citation-based compliant in-place, citando su evidencia VERIFICATION/HUMAN-UAT existente sin re-ejecutar la suite.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliar STATE.md ## Deferred Items (3 filas nyquist + intro)</name>
  <files>.planning/STATE.md</files>
  <read_first>
  - .planning/STATE.md — específicamente la sección `## Deferred Items` (líneas ~63-74). Localizar las 3 filas con categoría `nyquist` (Phase 44/45/46, todas `PARTIAL → Phase 51 (NYQ-03)`) y la línea intro "La deuda Nyquist de v0.11 (Phases 44/45/46) se salda en **Phase 51** (NYQ-03, citation-based)".
  - .planning/ROADMAP.md §Phase 51 Success Criteria 2 — define exactamente qué reconciliar (stubs reemplazados + STATE.md reconciliado).
  - .planning/milestones/v0.11-phases/47-backfill-de-deuda-nyquist/47-01-PLAN.md — Task 3 es la plantilla exacta de esta reconciliación (toggle de las filas nyquist + intro a pasado, dejando intactas las filas de deuda distinta).
  </read_first>
  <action>
  Editar SOLO `.planning/STATE.md`. En la sección `## Deferred Items`:
  1. Actualizar las 3 filas de categoría `nyquist` (Phase 44/45/46) cambiando su columna de Estado de `PARTIAL → Phase 51 (NYQ-03)` a saldado/compliant (p. ej. "✓ saldado Phase 51 (NYQ-03)") — reflejando que sus `VALIDATION.md` ya están `nyquist_compliant: true`.
  2. Reescribir la línea intro de la sección a tiempo pasado/cerrado (de "La deuda Nyquist de v0.11 (Phases 44/45/46) se salda en **Phase 51** (NYQ-03, citation-based)" a "La deuda Nyquist de v0.11 (Phases 44/45/46) quedó saldada en **Phase 51** (NYQ-03, citation-based)").
  3. DEJAR INTACTAS (D-04) las filas de categoría `frontmatter` (`requirements_completed: []` cosmético de 46-01/47-01), `verification` (covered-by-UAT 37/38 de v0.9) y `code` (WARNING-01 ciclo ESM App.js↔SessionTable.js) — son deuda distinta, fuera de NYQ-03. No tocar ninguna otra sección de STATE.md (Current Position, Accumulated Context, Session Continuity, etc. — las actualiza el cierre del plan/fase, no esta tarea de reconciliación).

  No tocar `src/`, `test/`, `bin/`.
  </action>
  <verify>
    <automated>! grep -E 'nyquist.*(PARTIAL|MISSING)' .planning/STATE.md && grep -q 'frontmatter' .planning/STATE.md && grep -q 'WARNING-01' .planning/STATE.md && grep -q 'covered-by-UAT' .planning/STATE.md && git diff --quiet -- src/ test/ bin/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
  - Ninguna de las 3 filas `nyquist` de `## Deferred Items` (44/45/46) contiene ya `PARTIAL` ni `MISSING`; todas reflejan saldado/compliant referenciando Phase 51 (NYQ-03).
  - La línea intro de `## Deferred Items` está en tiempo pasado/cerrado.
  - Las filas `frontmatter`, `verification` (covered-by-UAT) y `code` (WARNING-01) siguen presentes y sin modificar.
  - `git diff -- src/ test/ bin/` permanece vacío.
  </acceptance_criteria>
  <done>STATE.md ## Deferred Items reconciliado: 3 filas nyquist (44/45/46) saldadas + intro en pasado; deuda distinta (frontmatter/verification/code) intacta (Success Criteria 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

**N/A — doc-only phase.** Esta fase no introduce código, inputs, endpoints, parsing, ni superficies de autenticación. El único cambio es la edición de ficheros `.md` bajo `.planning/` (D-05, Tier 1 doc-only). No cruza ninguna frontera de confianza nueva: no hay client→API, no hay datos no confiables, no hay ejecución.

## STRIDE Threat Register

**N/A — sin superficie de amenaza.** Cero código (`git diff -- src/ test/ bin/` debe quedar vacío), cero dependencias nuevas (sin npm/pip/cargo install → la Package Legitimacy Gate no aplica), cero inputs, cero endpoints. No hay componente sobre el que registrar amenazas S/T/R/I/D/E. La disciplina Tier 1 doc-only es en sí la mitigación: el gate `git diff -- src/ test/ bin/` vacío garantiza que no se introduce código explotable.
</threat_model>

<verification>
Checks de fase (todos automated, doc-only):

1. Los 3 `VALIDATION.md` (44/45/46) existen en sus rutas exactas y todos tienen `nyquist_compliant: true` (ver verify de Task 1).
2. Ningún `VALIDATION.md` objetivo conserva `nyquist_compliant: false` ni `status: draft`.
3. Cada VALIDATION cita su evidencia real: 44/45 → VERIFICATION; 46 → VERIFICATION + HUMAN-UAT.
4. STATE.md `## Deferred Items`: ninguna fila nyquist con PARTIAL/MISSING; filas frontmatter/verification/code intactas.
5. **Invariante duro D-05:** `git diff -- src/ test/ bin/` queda vacío (cero código, cero tests netos).

**Nota Nyquist (Dimension 8 — test/validation requirements):** N/A para esta fase. Es bookkeeping doc-only sobre fases ya shipped y verificadas (44/45/46, todas con VERIFICATION passed); NO se crean ni re-ejecutan tests. La cobertura de cada VALIDATION.md ES la cita a la evidencia empírica preexistente (VERIFICATION.md / HUMAN-UAT), no una nueva corrida de la suite. El plan-checker debe tratar la creación de tasks de test como inaplicable aquí.
</verification>

<success_criteria>
- [ ] Phases 44, 45 y 46 tienen `VALIDATION.md` citation-based con `nyquist_compliant: true` citando evidencia existente (ROADMAP Success Criteria 1 / NYQ-03).
- [ ] Los 3 stubs `draft` / `nyquist_compliant: false` quedan reemplazados in-place y STATE.md `## Deferred Items` reconciliado (ROADMAP Success Criteria 2).
- [ ] `git diff -- src/ test/ bin/` vacío (ROADMAP Success Criteria 3 / D-05 Tier 1 doc-only).
- [ ] Ninguna fase declarada N/A; cada una cita su evidencia más fuerte disponible (D-03).
</success_criteria>

<output>
Create `.planning/phases/51-backfill-nyquist-v0-11/51-01-SUMMARY.md` when done.
Commit convention (D-05): `docs(51-01): backfill nyquist VALIDATION.md NYQ-03 (44/45/46 draft→compliant) + STATE reconciliation`.
</output>
</content>
</invoke>
