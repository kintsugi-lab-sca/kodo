---
phase: 50.1-live-progress-via-gsd-state
verified: 2026-06-15T08:10:00Z
status: human_needed
score: 8/8 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Abrir el dashboard con una sesión GSD activa en curso (execute-phase corriendo un plan)"
    expected: "La columna `prog` muestra `N/M` donde N=completed_phases y M=total_phases del STATE.md del worktree, y pasa a `✓` cuando N===M"
    why_human: "La lectura es síncrona sobre el filesystem del worktree real; no hay servidor ni mock que permita ejercitarlo sin una sesión GSD viva"
  - test: "Fallo transitorio: eliminar o hacer inaccesible el STATE.md mientras el dashboard está abierto"
    expected: "La columna muestra el último N/M conocido (keep-last-good), no '?'; al restaurar el archivo vuelve a mostrar el valor actualizado"
    why_human: "El keep-last-good en progressLastGoodRef.current es estado en memoria del proceso ink — requiere interacción manual para verificar"
  - test: "Fila no-GSD (row.gsd !== true): sesión lanzada sin /gsd"
    expected: "La columna `prog` muestra '—' (no-progress), sin intentar leer ningún STATE.md"
    why_human: "Requiere tener activas sesiones mixtas (GSD y no-GSD) simultáneamente para observar la discriminación visual"
---

# Phase 50.1: Verificación — Corrección de la fuente del progreso vivo (STATE.md)

**Phase Goal:** El dashboard muestra `N/M` fases por sesión GSD leyendo el bloque `progress:` del `STATE.md` del worktree real, reusando el display de Phase 50 intacto y demotando el hook 50-02 que leía la superficie equivocada (`~/.claude/tasks/`).
**Verified:** 2026-06-15T08:10:00Z
**Status:** human_needed (8/8 must-haves verificados en codebase; 3 checks de comportamiento en vivo requieren sesión real)
**Re-verification:** No — verificación inicial

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidencia |
|---|-------|--------|-----------|
| 1 | **DG-01**: `readGsdProgress` deriva N/M = completed_phases/total_phases (fases, no tareas), con `completed: true` cuando n===m | VERIFIED | `src/cli/dashboard/progress.js:96-98`: `m=total_phases`, `n=completed_phases??0`, `completed: m>0 && n===m`. Test 1 (3/5→false), Test 2 (5/5→true) pasan. |
| 2 | **DG-02**: la fuente es el bloque `progress:` del STATE.md del worktree GSD; mini-parser regex constante sin dep YAML; no lee `~/.claude/tasks/` ni `~/.kodo/progress/` | VERIFIED | `join(worktreeBase, '.planning', 'STATE.md')` (línea 90). `FRONTMATTER_RE` y regexes por key son constantes literales (líneas 30, 59). `grep homedir/\.kodo/claude.*tasks` → 0 resultados. Sin deps YAML añadidos (`package.json` sin cambios). |
| 3 | **DG-03**: solo filas con `row.gsd === true` se enriquecen con progreso; no-GSD → `{status:'no-progress'}` → '—' | VERIFIED | `App.js:353`: `if (row?.gsd !== true) return { ...row, progress: { status: 'no-progress' } }` — gate explícito, primera línea del enrich. |
| 4 | **DG-04**: el enrich deriva la ruta con `computeRealWorktreePath(project_path, session_id)`, nunca confía en `row.worktree_path` | VERIFIED | `App.js:354-367`: `const projectPath = row.project_path; const sessionId = row.session_id;` + `computeRealWorktreePath(projectPath, sessionId)`. Los únicos 2 hits de `row.worktree_path` en App.js son en comentarios que documentan que NO se usa. `computeRealWorktreePath` devuelve `<projectPath>/.claude/worktrees/<sessionId>` (state.js:176). |
| 5 | **DG-05**: columna `prog`, 4 estados (`progCell`), condicionalidad (`deriveAnyProgress`) y cero-color reusados INTACTOS — solo cambia la fuente del enrich | VERIFIED | `format.js`, `select.js`, `SessionTable.js` sin modificaciones en esta fase. `deriveAnyProgress` importado y llamado intacto en App.js:386. Color isolation confirmada — sin `picocolors` en ningún módulo del dashboard. |
| 6 | **DG-06**: enrich CLIENT-SIDE en App.js; CERO cambios en `src/server.js`; cero endpoints; cero deps | VERIFIED | `git diff --quiet src/server.js` → CLEAN. `git diff --quiet package.json` → CLEAN. El enrich vive completamente en el render de App.js. |
| 7 | **DG-07**: STATE.md ausente/parcial → no-progress; corrupto/EACCES → error; never-throws | VERIFIED | Tests 3 (parcial sin total_phases→no-progress), 4 (parcial con total_phases→ok), 5 (ENOENT→no-progress), 6a (EACCES→error), 6b (corrupto→error) pasan. Bloque try/catch en progress.js:99-103 con mapeo exhaustivo. |
| 8 | **DG-08**: `task-progress.js` eliminado + registro TaskCreated/TaskCompleted removido de `install.js` + tests del hook eliminados/reescritos | VERIFIED | `src/hooks/task-progress.js` → NO EXISTE. `test/hooks/task-progress.test.js` → NO EXISTE. `grep -c TaskCreated\|TaskCompleted install.js` (sin comentarios) → 0. `install.test.js` Test 5 aserta que tras `installHooks` no existe entrada kodo en TaskCreated/TaskCompleted. |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Esperado | Status | Detalle |
|----------|----------|--------|---------|
| `src/cli/dashboard/progress.js` | `readGsdProgress` + `parseProgressBlock` | VERIFIED | 104 líneas sustantivas. Exporta `readGsdProgress` y `parseProgressBlock`. Shim `readProgress` eliminado (0 consumidores). |
| `src/session/state.js` | `computeRealWorktreePath` aditivo, sin tocar `computeWorktreePath` | VERIFIED | Líneas 157-177: helper nuevo. `computeWorktreePath` en línea 153 intacta (`join(projectPath, '.bg-shell', sessionId)`). Test en state.test.js aserta `notEqual` entre ambas funciones. |
| `src/cli/dashboard/App.js` | enrich repuntado a `readGsdProgress` + `computeRealWorktreePath`, gate `row.gsd` | VERIFIED | Importa `readGsdProgress` (línea 71) y `computeRealWorktreePath` (línea 72). Bloque enrich completo en líneas 350-379. |
| `src/hooks/install.js` | Solo registra SessionStart/Stop; sin Task* | VERIFIED | `installHooks` registra únicamente `SessionStart` y `Stop`. `uninstallHooks` itera solo `['SessionStart', 'Stop']`. Sin referencias a `task-progress.js`. |
| `test/dashboard-progress.test.js` | 8 casos del contrato STATE.md con DI `readFileFn` | VERIFIED | 116 líneas. 8 tests: ok-en-progreso, ok-completado, parcial-sin-total, parcial-con-total-sin-completed, ENOENT, EACCES, corrupto, determinismo. |
| `test/state.test.js` | `describe('computeRealWorktreePath')` | VERIFIED | Describe en líneas 119-146 con 4 casos incluyendo assert de separación `.bg-shell` vs `.claude/worktrees`. |
| `test/hooks/install.test.js` | Cubre SessionStart/Stop + assert ausencia Task* | VERIFIED | Test 5 (DG-08 demote) aserta explícitamente que `TaskCreated`/`TaskCompleted` tienen 0 entradas kodo tras `installHooks`. |
| `src/hooks/task-progress.js` | ELIMINADO | VERIFIED | No existe en el filesystem. |
| `test/hooks/task-progress.test.js` | ELIMINADO | VERIFIED | No existe en el filesystem. |

---

### Key Link Verification

| From | To | Via | Status | Detalle |
|------|----|----|--------|---------|
| `src/cli/dashboard/App.js` | `<worktree>/.planning/STATE.md` | `computeRealWorktreePath(project_path, session_id)` → `readGsdProgress(base, {})` | WIRED | App.js:367-368: `const base = computeRealWorktreePath(projectPath, sessionId); const res = readGsdProgress(base, {})` |
| `src/cli/dashboard/App.js` | `computeRealWorktreePath` + `readGsdProgress` | import + enrich client-side gateado por `row.gsd === true` | WIRED | 2 usos de `readGsdProgress`, 3 de `computeRealWorktreePath` confirmados por grep |
| `src/cli/dashboard/App.js` | `progCell`/`deriveAnyProgress` | `session.progress` consumido por columna `prog` (reuso intacto) | WIRED | `deriveAnyProgress(enriched)` en línea 386; `progress` asignado a cada fila en el enrich |
| `src/hooks/install.js` | `settings.json hooks` | `addHook` solo SessionStart/Stop | WIRED | `addHook(settings.hooks, 'SessionStart', ...)` y `addHook(settings.hooks, 'Stop', ...)` — sin `TaskCreated`/`TaskCompleted` |
| `readGsdProgress` | `parseProgressBlock` | llamada interna en progress.js:91 | WIRED | `const block = parseProgressBlock(md)` |

---

### Data-Flow Trace (Level 4)

| Artifact | Variable de datos | Fuente | Produce datos reales | Status |
|----------|-------------------|--------|----------------------|--------|
| `App.js` enrich | `row.progress` | `readGsdProgress(computeRealWorktreePath(row.project_path, row.session_id))` | Si el STATE.md existe con `total_phases` → sí (datos del disco del worktree GSD real) | FLOWING (condicional a sesión GSD activa en vivo) |
| `readGsdProgress` | `block` | `readFileSync(join(worktreeBase, '.planning', 'STATE.md'))` parseado con `parseProgressBlock` | Extrae `total_phases`/`completed_phases` del frontmatter real | FLOWING |

Nota: el flujo completo de datos depende de que exista un worktree GSD real con STATE.md escrito por GSD. En ausencia de sesión activa → `no-progress` → '—' (comportamiento correcto per-spec, no stub).

---

### Behavioral Spot-Checks

| Behavior | Comando | Resultado | Status |
|----------|---------|-----------|--------|
| Suite completa verde | `npm test` | 1307 pass / 0 fail / 1 skip (pre-existente) — 1308 tests totales | PASS |
| `task-progress.js` eliminado | `test ! -f src/hooks/task-progress.js` | exit 0 | PASS |
| `task-progress.test.js` eliminado | `test ! -f test/hooks/task-progress.test.js` | exit 0 | PASS |
| sin TaskCreated/TaskCompleted en install.js | `grep -c 'TaskCreated\|TaskCompleted' install.js` (sin comentarios) | 0 | PASS |
| server.js intacto | `git diff --quiet src/server.js` | exit 0 (CLEAN) | PASS |
| package.json intacto | `git diff --quiet package.json` | exit 0 (CLEAN) | PASS |
| `computeWorktreePath` intacta | `grep -q "return join(projectPath, '.bg-shell'" src/session/state.js` | OK | PASS |
| `readGsdProgress` en App.js (>= 1) | `grep -c "readGsdProgress" App.js` | 2 | PASS |
| `computeRealWorktreePath` en App.js (>= 1) | `grep -c "computeRealWorktreePath" App.js` | 3 | PASS |
| shim `readProgress` eliminado | `grep -rn "readProgress\b" src/ test/` | 0 resultados | PASS |
| sin fuente HOME-relative en progress.js | `grep -c "homedir\|\.kodo\|claude.*tasks" progress.js` | 0 | PASS |
| color isolation dashboard | sin `import.*picocolors` en módulos del dashboard | 0 resultados en `src/cli/dashboard/` | PASS |

---

### Requirements Coverage

| Requirement | Plan | Descripción | Status | Evidencia |
|-------------|------|-------------|--------|-----------|
| PROG-02 | 50.1-01 | Captura y persistencia del progreso de sesión a superficie kodo-controlada | SATISFIED (re-interpretado) | PROG-02 requería originalmente persistir en `~/.kodo/` vía hooks. La re-interpretación de Phase 50.1 (basada en el veredicto PROG-01 sobre la viabilidad del surface correcto) es que la captura es por lectura directa del STATE.md — el dato canónico que GSD ya produce. `readGsdProgress` lo consume sin persistencia intermedia (correcta por diseño: el STATE.md ES la persistencia). |
| PROG-03 | 50.1-02 | Dashboard muestra progreso por sesión (N/M) leyendo el artefacto filesystem | SATISFIED | App.js enrich lee `readGsdProgress` → `progCell` → columna `prog`. Display intacto (DG-05). |

**Nota sobre PROG-02 y la trazabilidad:** REQUIREMENTS.md define PROG-02 como "persiste el progreso a un archivo kodo-controlado bajo `~/.kodo/`". Phase 50.1 re-direcciona a leer el STATE.md del worktree GSD directamente (sin archivo kodo-controlado intermedio). Esta desviación del literal de PROG-02 es intencional y documentada — el CONTEXT.md de la fase justifica que `~/.kodo/progress/` estaba vacía porque GSD usa `Agent`, no `TaskCreate`. La fuente canónica es el STATE.md de GSD. La desviación está dentro del espíritu de PROG-02 (el dato está disponible y correlacionado por sesión) pero difiere de su letra.

---

### Warnings del Code Review (50.1-REVIEW.md) — Evaluación de impacto en el objetivo

| ID | Descripción | Severidad asignada | Impacto en el objetivo de fase |
|----|-------------|-------------------|-------------------------------|
| WR-01 | `parseProgressBlock` no acota al bloque `progress:` — coincide con keys de allowlist bajo cualquier padre en el frontmatter | WARNING | **No bloquea el objetivo actual.** El STATE.md de GSD solo tiene `total_phases`/`completed_phases` bajo `progress:` hoy. El riesgo es un bug silencioso si GSD añade otro bloque YAML con esas keys — latente, no actual. |
| WR-02 | `n > m` no se clampea — `completed_phases > total_phases` produce N/M imposible (e.g. 5/3) | WARNING | **No bloquea el objetivo actual.** El STATE.md es GSD-mantenido; una inconsistencia así no debería ocurrir. Robustez defensiva deseable pero no requisito de PROG-02/03. |
| WR-03 | `deriveAnyProgress` es estructuralmente siempre-true — la columna `prog` nunca se oculta (pre-existente de Phase 50, trasladado) | WARNING | **Pre-existente, no introducido.** La columna `prog` siempre visible no impide mostrar N/M correctamente. Cosmético. |

Ninguno de los 3 warnings se eleva a BLOCKER: el objetivo de fase (mostrar N/M desde STATE.md, demotar hook 50-02, reusar display Phase 50) se cumple plenamente con las advertencias presentes.

---

### Anti-Patterns Found

| Archivo | Línea | Patrón | Severidad | Impacto |
|---------|-------|--------|-----------|---------|
| — | — | Sin TBD/FIXME/XXX en archivos modificados | — | — |
| — | — | Sin stubs de render (return null/placeholder) | — | — |
| — | — | Sin dependencias especulativas | — | — |

Sin anti-patrones bloqueantes detectados.

---

### Human Verification Required

#### 1. Columna `prog` con sesión GSD activa en vivo

**Test:** Lanzar una sesión GSD (`/gsd <task>`) con un plan de varias fases. Abrir el dashboard (`kodo`). Observar la columna `prog`.
**Expected:** La columna muestra `N/M` donde N = `completed_phases` y M = `total_phases` del bloque `progress:` del STATE.md del worktree de la sesión (e.g. `2/5`). Al completar todas las fases, muestra `✓`.
**Why human:** La lectura es síncrona contra el filesystem del worktree real. No existe un mock de sesión GSD activa ejecutable sin levantar una sesión real.

#### 2. Keep-last-good bajo fallo transitorio

**Test:** Con una sesión GSD activa mostrando N/M, hacer inaccesible temporalmente el STATE.md del worktree (e.g. `chmod 000` o renombrar). Observar la columna durante el poll interval.
**Expected:** La columna mantiene el último N/M conocido (no cae a '?') mientras dura el fallo. Al restaurar el archivo (`chmod 644`), vuelve a mostrar el valor actualizado.
**Why human:** El `progressLastGoodRef.current` es estado en memoria del proceso ink — el ciclo de poll → error → last-good → restauración requiere interacción manual y observación visual.

#### 3. Discriminación GSD vs no-GSD en filas mixtas

**Test:** Con sesiones activas mixtas (una GSD, una no-GSD), abrir el dashboard.
**Expected:** La fila GSD muestra `N/M`; la fila no-GSD muestra '—' en la columna `prog`.
**Why human:** Requiere tener activas simultáneamente sesiones de ambos tipos para observar la discriminación visual del gate `row.gsd === true`.

---

### Gaps Summary

Sin gaps bloqueantes. Los 8 must-haves están verificados en el codebase con evidencia directa (lectura de código + grep + suite de tests verde). Los 3 warnings del code review son deuda de robustez, no fallos del objetivo de fase. Los 3 items de verificación humana son checks de comportamiento en vivo que no pueden comprobarse con grep/tests unitarios.

---

_Verified: 2026-06-15T08:10:00Z_
_Verifier: Claude (gsd-verifier)_
