---
status: passed
phase: 50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog
source: [50.1-VERIFICATION.md]
started: 2026-06-15T05:58:00Z
updated: 2026-06-23T16:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Columna `prog` en vivo desde STATE.md
expected: Con una sesión GSD viva (`/gsd <task>` con varias fases en su ROADMAP), el dashboard muestra la columna `prog` con el `N/M` real derivado del bloque `progress:` del `STATE.md` del worktree (`completed_phases/total_phases`), y `N/M✓` cuando `completed_phases === total_phases`. La fuente es el STATE.md del worktree real (`.claude/worktrees/<sid>`), NO `~/.claude/tasks/` ni `~/.kodo/progress/`.
result: [passed]

### 2. Keep-last-good en fallo transiente
expected: Con una sesión GSD reportando `N/M`, al hacer el `STATE.md` del worktree temporalmente inaccesible (p. ej. permisos), la columna mantiene el último `N/M` conocido (keep-last-good keyed por `session_id`) en vez de parpadear a `?`; al restaurar el acceso, el valor se actualiza. Sin último valor bueno previo, el fallo transiente muestra `?`.
result: [passed]

### 3. Gate GSD vs no-GSD
expected: Con sesiones mixtas activas (GSD y no-GSD), solo las filas con `gsd === true` muestran `N/M`; las no-GSD muestran `—`. La columna nunca crashea el panel (never-throws end-to-end).
result: [passed]

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
