---
phase: 50-live-progress-display-condicional-solo-si-phase-49-viable
plan: 03
type: execute
wave: 2
depends_on: ["50-02"]
files_modified:
  - src/cli/dashboard/progress.js
  - src/cli/dashboard/select.js
  - src/cli/dashboard/format.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-progress.test.js
  - test/dashboard-select.test.js
  - test/dashboard-format.test.js
autonomous: true
requirements: ["PROG-03"]
user_setup: []

must_haves:
  truths:
    - "El dashboard muestra el progreso N/M por sesión leyendo SOLO el artefacto kodo ~/.kodo/progress/<task_id>.json filesystem-style (mold readLightPlan), CLIENT-SIDE en App.js, nunca los internals de Claude Code"
    - "CERO endpoints nuevos y CERO cambios en src/server.js — el progreso NO se enriquece en GET /status (D-08, Success Criteria 2)"
    - "La columna prog es condicional ambient (deriveAnyProgress sobre el set SIN filtrar, espejo de deriveAnyGsd) — aparece solo si alguna sesión reporta progreso, no parpadea bajo filtro / (D-06)"
    - "La celda muestra formato N/M crudo + ✓ al completar, sin color (D-07) y 4 estados honestos: N/M (en progreso), N/M✓ (completado), — (sin progreso/ENOENT), ? (fallo transiente) con keep-last-good en memoria (D-09)"
    - "Cero-color: la celda nunca recibe color, solo dim — color-isolation intacta (cubierta por el walker format-isolation)"
    - "El artefacto ~/.kodo/progress/<task_id>.json persiste con el N/M final congelado al morir/dismiss/completar la sesión — sin limpieza nueva, simétrico con ~/.kodo/plans/ (D-10)"
  artifacts:
    - path: "src/cli/dashboard/progress.js"
      provides: "readProgress(taskId, deps) never-throws → { status:'ok'|'no-progress'|'error', n?, m?, completed? } (mold readLightPlan)"
      contains: "readProgress"
      min_lines: 15
    - path: "src/cli/dashboard/select.js"
      provides: "deriveAnyProgress(rows) — flag estructural sobre set sin filtrar (mold deriveAnyGsd)"
      contains: "deriveAnyProgress"
    - path: "src/cli/dashboard/format.js"
      provides: "progCell(session) → { text, dim } (mold taskCell) + prog en rowCells"
      contains: "progCell"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "Columna condicional prog entre status y task (COLS.prog + cabecera + celda)"
      contains: "anyProgress"
    - path: "src/cli/dashboard/App.js"
      provides: "Enrich client-side de session.progress vía readProgress + keep-last-good + wiring anyProgress a SessionTable"
      contains: "deriveAnyProgress"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/progress.js (readProgress)"
      via: "enrich client-side por fila en el render (mold readPlan App.js:544), CERO server.js"
      pattern: "readProgress"
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/SessionTable.js"
      via: "prop anyProgress = deriveAnyProgress(sorted)"
      pattern: "anyProgress"
    - from: "src/cli/dashboard/format.js (progCell)"
      to: "session.progress"
      via: "rowCells proyecta prog: progCell(session)"
      pattern: "progCell\\(session\\)"
    - from: "src/cli/dashboard/SessionTable.js"
      to: "~/.kodo/progress/<task_id>.json (vía session.progress enriquecido)"
      via: "celda condicional cell({ width: COLS.prog, ... truncate:true })"
      pattern: "COLS.prog"
---

<objective>
Mostrar el progreso vivo `N/M` por sesión en el dashboard TUI, leyendo SOLO el artefacto kodo `~/.kodo/progress/<task_id>.json` filesystem-style (mold de `readLightPlan`), enriquecido **CLIENT-SIDE en App.js** (NUNCA server-side), en una columna condicional `prog` con 4 estados degradados honestos y cero-color.

Purpose: cierra PROG-03 (display + estados degradados). Es el consumidor del seam byte-idéntico productor↔consumidor (el productor es el hook del Plan 02). Espejo barato de DOS precedentes shipped: seam plan-ligero v0.11 (lectura del artefacto) + columna no-color `provider_state` v0.10 (presentación degradada) + `deriveAnyGsd` v0.11 (condicionalidad).

Output: `progress.js` (nuevo, readProgress), `select.js` (deriveAnyProgress), `format.js` (progCell + rowCells), `App.js` (enrich client-side + keep-last-good + wiring), `SessionTable.js` (columna), + tests.

**INVARIANTE LOAD-BEARING (D-08 — CORRECCIÓN al PATTERNS.md):** PATTERNS.md (líneas 12-18, 214-234, 250-252) sugiere enriquecer `progress` server-side en `src/server.js` GET /status (donde vive `provider_state`). **ESTO SE RECHAZA.** D-08 + Success Criteria 2 mandan lectura CLIENT-SIDE, mold de `readLightPlan` invocado en `App.js:544` (`readPlan(row, deps)`), CERO cambios en `src/server.js`, CERO endpoints nuevos. Verificado: `provider_state` es server-side porque requiere `await providerStateResolver.resolve(s)` (async/red); `progress` es lectura filesystem SÍNCRONA never-throws → encaja client-side como `readPlan`. El executor NO debe tocar `src/server.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Decisiones de display + contrato visual + patrones
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-PATTERNS.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-UI-SPEC.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-VALIDATION.md

# ══════════════════════════════════════════════════════════════════════════════
# ⚠⚠⚠ ADVERTENCIA DE ALTO CONTRASTE — LEER ANTES DE TOCAR NADA ⚠⚠⚠
# ══════════════════════════════════════════════════════════════════════════════
# Los documentos @PATTERNS.md y @RESEARCH.md fueron CORREGIDOS, pero si encuentras
# CUALQUIER texto residual que dirija a enriquecer `progress` en `src/server.js`
# (p. ej. §File Classification listando server.js, o §Enriquecimiento read-only en
# GET /status, o "mold del enrich de provider_state"), **IGNÓRALO POR COMPLETO.**
#
# D-08 (LOCKED) PROHÍBE tocar `src/server.js`. La lectura del artefacto es
# CLIENT-SIDE en `App.js` (~línea 544), mold del helper `readPlan(row, deps)`
# síncrono never-throws — NUNCA en el payload de GET /status.
#
# `provider_state` SÍ es server-side (requiere `await ...resolve()`/red); `progress`
# NO (es lectura filesystem síncrona). NO copies el patrón de provider_state.
#
# CRITERIO DURO DE ACEPTACIÓN: `git diff --quiet src/server.js` DEBE dar 0 cambios.
# Si abres server.js, has violado D-08.
# ══════════════════════════════════════════════════════════════════════════════

# Analogs verbatim (read_first de cada tarea apunta a líneas concretas)
@src/cli/dashboard/plan.js
@src/cli/dashboard/select.js
@src/cli/dashboard/format.js
@src/cli/dashboard/SessionTable.js
@src/cli/dashboard/App.js

<interfaces>
<!-- Contratos extraídos del codebase. Úsalos directamente — no explores. -->

readLightPlan (plan.js:65-78) — mold EXACTO de readProgress:
  DI: `const readFileFn = deps.readFileFn || ((p) => readFileSync(p, 'utf-8'));`
  Root inyectable (HOME isolation): `deps.kodoProgressDir || join((deps.homedirFn || homedir)(), '.kodo', 'progress')`
  Mapeo de error: err?.code === 'ENOENT' → 'no-progress'; otro (EACCES/JSON corrupto) → 'error'.
  Anti-ReDoS guard del taskId (plan.js:120-121): String.includes('/')/'\\'/'..' — el CALLER valida antes.

deriveAnyGsd (select.js:217) — mold EXACTO de deriveAnyProgress:
  export function deriveAnyGsd(rows) { return rows.some((r) => r.phase_id != null); }
  CRÍTICO (docstring select.js:209-213): el caller lo computa sobre el set SIN filtrar (sorted), NO filtered.

taskCell (format.js:214-221) — mold EXACTO de progCell:
  reason 'unsupported' → { text:'—', dim:true }; 'fetch-failed' → { text:'?', dim:true };
  ok → { text: raw, dim:false }. Devuelve { text, dim } plano, CERO color propio.

rowCells (format.js:232-241) — añadir prog:
  retorna { task_ref, repo, phasemode, status, task, age } → añadir prog: progCell(session) ENTRE task y age.

COLS (SessionTable.js:54) — añadir prog ENTRE status y task:
  const COLS = { gutter:2, state:18, task_ref:10, repo:18, phasemode:11, status:18, prog:7, task:12, age:7 };

Patrón columna condicional en SessionTable (cabecera 320-327, celda 368-379):
  ...(anyGsd ? [h(Box,{width:COLS.phasemode}, h(Text,{dimColor:true},'phase/mode'))] : []),  // cabecera
  ...(anyGsd ? [cell({ width:COLS.phasemode, text:..., dim:..., bold:selected, truncate:true })] : []),  // celda
  cell({ width:COLS.task, text:cells.task.text, dim:cells.task.dim, bold:selected, truncate:true }),  // celda task no-color

Wiring en App.js: deriveAnyGsd(sorted) en línea 331; anyGsd pasado a SessionTable en línea 694.
readPlan se invoca CLIENT-SIDE en App.js:544 (síncrono never-throws en el handler). progress se enriquece análogamente — NUNCA en server.js (D-08).

Forma del artefacto (productor Plan 02): { n, m, completed, updated_at }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: progress.js (readProgress) + deriveAnyProgress en select.js — leaves puras</name>
  <files>src/cli/dashboard/progress.js, src/cli/dashboard/select.js, test/dashboard-progress.test.js, test/dashboard-select.test.js</files>
  <read_first>
    - src/cli/dashboard/plan.js (líneas 41-45 imports leaf-isolation; 65-78 readLightPlan VERBATIM — DI, discriminante de status, mapeo ENOENT/error; 120-121 guard anti-traversal)
    - src/cli/dashboard/select.js (líneas 203-219 deriveAnyGsd: docstring del invariante "set SIN filtrar" + cuerpo)
    - test/dashboard-plan.test.js (mold de los tests puros de readPlan: DI readFileFn/kodoPlansDir/homedirFn, sin disco real)
    - test/dashboard-select.test.js (mold de los tests de deriveAnyGsd)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md (líneas 165-198 Patterns 2 y 3 verbatim)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md (D-08 anti-ReDoS reusar guard; D-09 estados degradados)
  </read_first>
  <behavior>
    - readProgress Test 1 (ok): artefacto `{ n:1, m:3, completed:false }` legible → `{ status:'ok', n:1, m:3, completed:false }`.
    - readProgress Test 2 (completado): `{ n:3, m:3, completed:true }` → `{ status:'ok', n:3, m:3, completed:true }`.
    - readProgress Test 3 (no-progress): ENOENT (sin artefacto) → `{ status:'no-progress' }`.
    - readProgress Test 4 (error): readFile lanza EACCES o JSON corrupto → `{ status:'error' }` (never-throws).
    - readProgress Test 5 (HOME isolation): `kodoProgressDir` inyectado → lee de ahí, no del HOME real.
    - deriveAnyProgress Test 1: alguna fila con `progress != null` → true.
    - deriveAnyProgress Test 2: ninguna fila con progress → false.
    - deriveAnyProgress Test 3: `progress` undefined/null en todas → false (guard `!= null`, igual que deriveAnyGsd).
  </behavior>
  <action>
    **Crear `src/cli/dashboard/progress.js`** clonando la FORMA de `readLightPlan` (plan.js:65-78). NO reusar literalmente (el artefacto es JSON con campos, no markdown). El cuerpo está en RESEARCH.md líneas 165-181 (Pattern 2).
    - Imports leaf-isolation (mold plan.js:41-45): `readFileSync` de `node:fs`, `join` de `node:path`, `homedir` de `node:os`. NO importar `src/config.js`.
    - `export function readProgress(taskId, deps = {})`:
      - DI: `const readFileFn = deps.readFileFn || ((p) => readFileSync(p, 'utf-8'));`
      - Root: `const progDir = deps.kodoProgressDir || join((deps.homedirFn || homedir)(), '.kodo', 'progress');`
      - Ruta byte-idéntica al productor (Plan 02): `join(progDir, \`${taskId}.json\`)`.
      - try: parsear JSON → `{ status:'ok', n:o.n, m:o.m, completed:!!o.completed }`.
      - catch: `err?.code === 'ENOENT'` → `{ status:'no-progress' }`; otro → `{ status:'error' }`.
    - **Anti-ReDoS guard del taskId:** REUSAR el de plan.js (String.includes, NO regex). El CALLER (App.js enrich) valida el taskId ANTES de llamar, exactamente como `readPlan` valida antes de `readLightPlan` (plan.js:117-123). Documentar en el JSDoc que el caller garantiza el guard. (Opcional defensivo: un guard interno también, sin coste, si el executor lo prefiere — pero el contrato es caller-validates.)

    **Añadir `deriveAnyProgress` a `src/cli/dashboard/select.js`** (NO a un `derive.js` — ese archivo NO EXISTE; PATTERNS.md lo corrige). Espejo literal de `deriveAnyGsd` (línea 217):
    - `export function deriveAnyProgress(rows) { return rows.some((r) => r.progress != null); }`
    - Replicar el docstring del invariante CRÍTICO de `deriveAnyGsd` (líneas 209-213): el caller lo computa sobre el set SIN filtrar (`sorted`), NO `filtered` — la columna es ESTRUCTURAL, no debe parpadear bajo query `/` (Pitfall 5 = Pitfall 4 de Phase 44).

    Crear `test/dashboard-progress.test.js` (mold de `test/dashboard-plan.test.js` — puros, DI, sin disco real): 5 behaviors de readProgress.
    Extender `test/dashboard-select.test.js` con los 3 behaviors de deriveAnyProgress.

    NO importar picocolors. NO regex sobre input.
  </action>
  <verify>
    <automated>node --test test/dashboard-progress.test.js test/dashboard-select.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-progress.test.js test/dashboard-select.test.js` exits 0.
    - `grep -c "function readProgress" src/cli/dashboard/progress.js` >= 1.
    - `grep -c "function deriveAnyProgress" src/cli/dashboard/select.js` >= 1.
    - readProgress mapea ENOENT→'no-progress', otro→'error', contenido→'ok' (Tests 1-4 verdes).
    - `grep -c "picocolors\|require('picocolors')" src/cli/dashboard/progress.js` == 0 (leaf-isolation).
    - readProgress NO importa `src/config.js` (leaf-isolation): `grep -c "config" src/cli/dashboard/progress.js` == 0.
  </acceptance_criteria>
  <done>readProgress es un consumidor never-throws con discriminante ok/no-progress/error, HOME-isolable por DI, sin regex. deriveAnyProgress es el espejo de deriveAnyGsd con el invariante "set sin filtrar" documentado. Los 8 tests pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: progCell + prog en rowCells (format.js) — celda no-color con 4 estados</name>
  <files>src/cli/dashboard/format.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/dashboard/format.js (líneas 200-221 taskCell: degradados —/? + ok verbatim, { text, dim } plano cero-color; 223-241 rowCells: shape de retorno + JSDoc; 22-23 NO importa picocolors)
    - test/dashboard-format.test.js (mold de los tests de taskCell/rowCells)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-UI-SPEC.md (los 4 estados de celda LOCKED: N/M, N/M✓, —, ?; cero-color; reserva de ancho para ✓)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md (líneas 184-198 Pattern 3 progCell verbatim)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md (D-07 formato N/M + ✓ + cero color; D-09 estados degradados)
  </read_first>
  <behavior>
    - progCell Test 1 (en progreso): `session.progress = { status:'ok', n:1, m:3, completed:false }` → `{ text:'1/3', dim:false }`.
    - progCell Test 2 (completado): `{ status:'ok', n:3, m:3, completed:true }` → `{ text:'3/3✓', dim:false }`.
    - progCell Test 3 (sin progreso): `progress` ausente o `{ status:'no-progress' }` → `{ text:'—', dim:true }`.
    - progCell Test 4 (fallo transiente): `{ status:'error' }` → `{ text:'?', dim:true }`.
    - rowCells Test: el objeto retornado incluye `prog: { text, dim }` (forma de progCell) en la posición entre `task` y `age`.
  </behavior>
  <action>
    **Añadir `progCell` a `src/cli/dashboard/format.js`** clonando la forma de `taskCell` (líneas 214-221). El cuerpo está en RESEARCH.md líneas 192-198 (Pattern 3). Lee `session.progress` (el objeto enriquecido client-side en App.js — Tarea 3):
    - `if (!p || p.status === 'no-progress') return { text: '—', dim: true };`  (estado #3, espejo taskCell `unsupported`)
    - `if (p.status === 'error') return { text: '?', dim: true };`  (estado #4, espejo taskCell `fetch-failed`; el keep-last-good lo gestiona App.js, no progCell)
    - ok: `const suffix = p.completed ? '✓' : ''; return { text: \`${p.n}/${p.m}${suffix}\`, dim: false };`  (estados #1/#2)
    - Devolver `{ text, dim }` PLANO — CERO color propio (color-isolation D-12; el dim lo mapea ink a dimColor).

    **Extender `rowCells`** (líneas 232-241): añadir `prog: progCell(session)` ENTRE `task` y `age`. Actualizar el JSDoc `@returns` (línea 230) añadiendo `prog: { text: string, dim: boolean }`.

    Extender `test/dashboard-format.test.js` con los 5 behaviors (4 de progCell + 1 de rowCells). NO importar picocolors (el walker `format-isolation.test.js` lo cubre automáticamente).
  </action>
  <verify>
    <automated>node --test test/dashboard-format.test.js test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-format.test.js test/format-isolation.test.js` exits 0.
    - `grep -c "function progCell\|progCell =" src/cli/dashboard/format.js` >= 1.
    - `grep -c "prog:" src/cli/dashboard/format.js` >= 1 (prog en rowCells).
    - `format-isolation.test.js` verde (progCell no rompe color-isolation — cero picocolors/ANSI en la celda).
    - Los 4 estados de progCell verdes (Tests 1-4): N/M, N/M✓, —, ?.
  </acceptance_criteria>
  <done>progCell formatea los 4 estados LOCKED ({text,dim} plano cero-color, espejo de taskCell) y rowCells incluye prog entre task y age. color-isolation intacta. Los 5 tests pasan.</done>
</task>

<task type="auto">
  <name>Task 3: Enrich client-side en App.js (readProgress + keep-last-good) + columna prog en SessionTable</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/App.js (líneas 322-337 pipeline de derivación: sortSessions→sorted, deriveAnyGsd(sorted) línea 331, filtered/sel/counts; 531-551 handler `p`/readPlan CLIENT-SIDE síncrono never-throws — EL MOLD del enrich; 680-704 wiring de SessionTable con anyGsd línea 694)
    - src/cli/dashboard/SessionTable.js (línea 54 COLS; 70-80 helper cell; 230-241 firma de props/anyGsd; 312-328 columnHeader con cabecera condicional phasemode líneas 320-323; 330-382 dataRows con celda condicional phasemode 368-370 y celda task no-color 379)
    - src/cli/dashboard/progress.js (readProgress — creado en Tarea 1, el caller que valida el taskId)
    - src/cli/dashboard/select.js (deriveAnyProgress — creado en Tarea 1)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-UI-SPEC.md (ubicación COLS entre status y task; ancho 6-7 + reserva ✓; keep-last-good vive en App.js NO en progCell; truncate anti-DoS)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-PATTERNS.md (CORRECCIÓN: enrich client-side, NO server.js — leer la nota crítica del planner)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md (D-06 columna condicional ambient; D-08 client-side cero endpoints; D-09 keep-last-good)
  </read_first>
  <action>
    **A) Enrich client-side en App.js (mold del handler `p`/readPlan, NO server.js — D-08):**

    En el pipeline de derivación (zona líneas 322-337, donde ya vive `sorted`/`deriveAnyGsd(sorted)`):
    - Importar `readProgress` de `./progress.js` y `deriveAnyProgress` de `./select.js`.
    - **Enriquecer cada fila con `progress`** leyendo `readProgress(row.task_id, {})` (síncrono, never-throws — mold del `readPlan(row, ...)` de App.js:544, que también es lectura filesystem síncrona en el render). Construir las filas enriquecidas a partir de `sorted` (o `sessions`) ANTES de `deriveAnyProgress`/`filtered`, de modo que `row.progress` esté presente para `deriveAnyProgress`, `applyFilter`, y `rowCells`.
    - **Keep-last-good (D-09):** mantener en un `useRef` (mapa por `task_id`) el último `{ n, m, completed }` con `status === 'ok'`. Al enriquecer:
      - `status === 'ok'` → guardar en el ref y exponer `row.progress = { status:'ok', n, m, completed }`.
      - `status === 'error'` → si hay un last-good para ese task_id, exponer `row.progress = { status:'ok', ...lastGood }` (el último N/M sobrevive; progCell pinta el N/M, NO el `?`). Si NO hay last-good → exponer `{ status:'error' }` (progCell pinta `?`).
      - `status === 'no-progress'` → `row.progress = { status:'no-progress' }` (progCell pinta `—`). (Decisión de discreción: un `no-progress` NO borra el last-good si la sesión ya completó — pero por simetría con D-10/persistencia post-mortem, el artefacto persiste, así que no-progress real solo ocurre cuando nunca hubo artefacto; tratarlo como `—`.)
    - `const anyProgress = deriveAnyProgress(<filas enriquecidas no filtradas>);` — sobre el set SIN filtrar (igual que `deriveAnyGsd(sorted)`, Pitfall 5). Computar ANTES de `applyFilter`.
    - Pasar `anyProgress` a `SessionTable` junto a `anyGsd` (zona línea 694).

    **CERO cambios en `src/server.js`.** El executor NO debe abrir server.js. Verificable por git.

    Mantener la pureza del render: `readProgress` es never-throws (sin try/catch necesario en App.js, igual que `readPlan`). El `useRef` del keep-last-good no dispara re-render (es memoria entre polls).

    **B) Columna `prog` en SessionTable.js (mold phasemode condicional + task no-color):**
    - `COLS` (línea 54): añadir `prog: 7` ENTRE `status` y `task` → `{ ..., status:18, prog:7, task:12, age:7 }`. Ancho 7 (reserva el sufijo `✓` para el peor caso `12/15✓` = 6 chars + padding; alinea con el estilo par/legible de COLS — discreción planner, UI-SPEC recomienda 6-7). Header literal `prog` (4 chars, cabe).
    - Prop `anyProgress` en la firma de `SessionTable` (junto a `anyGsd`) con default `false` (retro-compat: oculta la columna si no se pasa).
    - Cabecera condicional (mold phasemode líneas 320-323), insertar ENTRE `status` (324) y `task` (326): `...(anyProgress ? [h(Box,{width:COLS.prog}, h(Text,{dimColor:true},'prog'))] : []),`
    - Celda de datos condicional (mold celda task no-color línea 379 + wrapper condicional phasemode 368-370), insertar ENTRE la celda `status` (374) y la celda `task` (379): `...(anyProgress ? [cell({ width:COLS.prog, text:cells.prog.text, dim:cells.prog.dim, bold:selected, truncate:true })] : []),`. `truncate:true` → ellipsis nativo `…` = anti-DoS T-43-03 (un n/m absurdo se trunca, no desborda).

    El orden de columnas resultante: `… status → prog → task → age` (UI-SPEC). NO right-align (consistencia COLS). Cero color en la celda prog.
  </action>
  <verify>
    <automated>node --test test/dashboard/app-open.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` verde (cero fail — suite completa, sin regresiones).
    - `grep -c "readProgress" src/cli/dashboard/App.js` >= 1 (enrich client-side).
    - `grep -c "deriveAnyProgress\|anyProgress" src/cli/dashboard/App.js` >= 1 (flag + wiring).
    - `grep -c "anyProgress" src/cli/dashboard/SessionTable.js` >= 2 (prop + cabecera + celda).
    - `grep -c "prog" src/cli/dashboard/SessionTable.js` >= 1 (COLS.prog).
    - **`git diff --quiet src/server.js` == 0 cambios (D-08 LOCKED: cero endpoints, cero enrich server-side).**
    - keep-last-good implementado en App.js (un useRef por task_id; status error con last-good pinta N/M, sin él pinta ?).
    - `deriveAnyProgress` computado sobre el set SIN filtrar (no `filtered`) — la columna no parpadea bajo `/`.
  </acceptance_criteria>
  <done>App.js enriquece session.progress CLIENT-SIDE vía readProgress (mold readPlan, never-throws) con keep-last-good, computa anyProgress sobre el set sin filtrar y lo pasa a SessionTable; la columna prog aparece entre status y task con los 4 estados y truncado anti-DoS. CERO cambios en server.js. npm test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.kodo/progress/<task_id>.json → readProgress (App.js client-side) | artefacto JSON on-disk (posiblemente corrupto) cruza al render del TUI |
| session.task_id → ruta del artefacto | el taskId (UUID kodo, key de state.sessions) construye la ruta de lectura |
| session.progress (n/m) → celda prog | valores numéricos (posiblemente absurdos) renderizados en una columna de ancho fijo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-redos | DoS | guard del taskId en readProgress | mitigate | REUSAR el guard String.includes de readLightPlan (NO regex, mold plan.js:120-121); el caller (App.js) valida; s.task_id es UUID kodo (seguro por construcción) |
| T-50-traversal | Tampering | ruta `~/.kodo/progress/<taskId>.json` | mitigate | Ruta construida con root FIJO `join(homedir(),'.kodo','progress')`; taskId validado anti-`/`/`\\`/`..` |
| T-50-cell-dos | DoS | celda prog con n/m gigantes | mitigate | `truncate:true` (→ wrap:'truncate-end', ellipsis nativo) en la celda — mold T-43-03 provider_state; un n/m absurdo se trunca, no desborda la tabla |
| T-50-json-crash | DoS | JSON corrupto del artefacto crashea el dashboard | mitigate | readProgress never-throws: JSON.parse en try/catch → status 'error' → progCell '?' + keep-last-good |
| T-50-endpoint | Tampering / scope | tentación de enriquecer en server.js | mitigate | INVARIANTE D-08: cero cambios en server.js, cero endpoints; enrich client-side. `git diff --quiet src/server.js` en acceptance |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — cero paquetes nuevos (research §Package Legitimacy Audit). Node builtins + ink/react ya presentes |
</threat_model>

<verification>
- `node --test test/dashboard-progress.test.js test/dashboard-select.test.js test/dashboard-format.test.js test/format-isolation.test.js` verde.
- `npm test` sin regresiones (cero fail).
- `git diff --quiet src/server.js` (D-08: cero cambios server-side, cero endpoints nuevos).
- La columna prog es condicional (deriveAnyProgress sobre set sin filtrar), 4 estados honestos, cero-color, truncado anti-DoS.
- readProgress nunca lee `~/.claude/` — solo el artefacto kodo.
</verification>

<success_criteria>
- PROG-03 cumplido: el dashboard muestra N/M por sesión leyendo SOLO el artefacto kodo filesystem-style (client-side, mold readLightPlan), cero endpoints nuevos, con los 4 estados degradados honestos y keep-last-good.
- D-08 honrado: lectura client-side en App.js, CERO cambios en server.js.
- Color-isolation intacta (cubierta por el walker); columna condicional no parpadea bajo filtro.
</success_criteria>

<output>
Create `.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-03-SUMMARY.md` when done.
</output>
