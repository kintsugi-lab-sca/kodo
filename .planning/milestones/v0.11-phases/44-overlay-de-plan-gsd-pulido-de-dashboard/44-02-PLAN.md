---
phase: 44-overlay-de-plan-gsd-pulido-de-dashboard
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/cli/dashboard/select.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-select.test.js
  - test/dashboard-table.test.js
autonomous: true
requirements: [TUI-18, TUI-19]
user_setup: []

must_haves:
  truths:
    - "TUI-18/D-08: when no active session has phase_id (anyGsd false), the `phase/mode` column is not rendered and its width is reclaimed; it reappears automatically when a GSD session enters"
    - "TUI-18/D-08: anyGsd is derived purely (React-free) over the unfiltered `sorted` set — it does NOT flicker when the operator types a `/` filter"
    - "TUI-19/D-09: a zombie row (running + alive===false) shows a `(zombie)` mark in the `state` cell, colored red from the existing statusColor — not only the header counter"
    - "TUI-19: COLS.state widened 16→18 so `▶ running (zombie)` survives un-truncated"
    - "D-09/D-12: the zombie red comes from statusColor(status, alive, state); zero new color, zero picocolors"
    - "TUI-19: the header zombie counter (countsLabel) is kept — the per-row mark is additive, not a replacement"
  artifacts:
    - path: "src/cli/dashboard/select.js"
      provides: "Pure deriveAnyGsd(rows) → boolean"
      exports: ["deriveAnyGsd"]
    - path: "src/cli/dashboard/App.js"
      provides: "anyGsd computed over `sorted` and threaded as a SessionTable prop"
      contains: "deriveAnyGsd"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "Conditional phasemode column drop + per-row (zombie) state-cell mark + COLS.state=18"
      contains: "anyGsd"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/select.js"
      via: "const anyGsd = deriveAnyGsd(sorted) — over sorted, NOT filtered (D-08)"
      pattern: "deriveAnyGsd\\(sorted\\)"
    - from: "src/cli/dashboard/SessionTable.js"
      to: "src/cli/dashboard/format.js"
      via: "statusColor(status, alive, state).color for the zombie red (already v3-aware)"
      pattern: "statusColor\\("
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/SessionTable.js"
      via: "anyGsd passed in the SessionTable prop bag"
      pattern: "anyGsd"
---

<objective>
Two dashboard polish items, both confined to the table-body/column regions of `SessionTable.js` (kept in ONE plan to avoid intra-file conflict with the overlay edits of 44-01, and because TUI-18 and TUI-19 share the same file body). TUI-18: a pure `deriveAnyGsd(rows)` in `select.js`, computed over the unfiltered `sorted` set in `App.js`, threaded into `SessionTable` to conditionally drop the `phase/mode` column (header + every data cell) when no active session is GSD. TUI-19: a per-row `(zombie)` mark in the `state` cell, colored red from the already-v3-aware `statusColor`, with `COLS.state` widened 16→18 so the mark is not truncated.

Purpose: pulido from v0.10 dogfooding — reclaim horizontal space when no GSD sessions are active, and show *which* row is a zombie (not just the header count).
Output: `select.js`/`App.js`/`SessionTable.js` edits (table regions only), extended `test/dashboard-select.test.js` and `test/dashboard-table.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-CONTEXT.md
@.planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-RESEARCH.md
@.planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-PATTERNS.md
@.planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-UI-SPEC.md
@.planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Verified against current source 2026-06-09. -->

deriveAnyGsd contract (NEW — src/cli/dashboard/select.js):
  deriveAnyGsd(rows) => boolean   // rows.some((r) => r.phase_id != null); pure, React-free, NO regex/color

select.js existing pure derives (analog shape — src/cli/dashboard/select.js):
  sortSessions(rows) (:44), resolveSelection (:78), parseFilter (:109), applyFilter (:157), countByStatus (:193)

App.js derive pipeline (src/cli/dashboard/App.js):
  const sorted = sortSessions(sessions);  (:265)
  const filtered = applyFilter(sorted, parseFilter(query), deriveRepo);  (:266)
  → anyGsd MUST derive from `sorted` (filter-insensitive, D-08), NOT `filtered`
  SessionTable prop bag (:556-573) — thread anyGsd next to overlayKind/scrollOffset

SessionTable.js (src/cli/dashboard/SessionTable.js):
  COLS (:48) = { gutter:2, state:16, task_ref:10, repo:18, phasemode:11, status:18, task:12, age:7 }  → state:16 → 18
  prop destructure (:199-216) — add `anyGsd`
  phase/mode column HEADER (:291): h(Box, { width: COLS.phasemode }, h(Text, { dimColor: true }, 'phase/mode'))
  phase/mode DATA cell (:323): cell({ width: COLS.phasemode, text: cells.phasemode, dim: cells.phasemode === NO_GSD_LABEL, ... })
  state DATA cell IIFE (:315-317): const badge = stateBadge(...); cell({ width: COLS.state, text, color: badge.color, bold: selected, truncate: false })
  statusColor already imported (:25); used at :302 (const sc); returns {color:'red'} for running+!alive (format.js:108-116)

format.js statusColor (src/cli/dashboard/format.js:108-116):
  statusColor(status, alive, state) — already v3-aware, returns {color:'red'} for running && !alive. NO edit needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveAnyGsd pure helper + thread over `sorted`</name>
  <files>src/cli/dashboard/select.js, src/cli/dashboard/App.js, test/dashboard-select.test.js</files>
  <read_first>
    - src/cli/dashboard/select.js:14-23 (derive-layer doc conventions) and :193-201 (countByStatus — analog pure-fn shape)
    - src/cli/dashboard/App.js:263-268 (sorted/filtered derive pipeline — anyGsd derives from `sorted`)
    - src/cli/dashboard/App.js:556-573 (SessionTable prop bag — how derives are threaded)
    - test/dashboard-select.test.js:14-30 (import block + node:test harness) and :269-280 (countByStatus truth-table example)
    - .planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-RESEARCH.md:304-318 (Pattern 3) and :395-399 (Pitfall 4 derive-over-filtered flicker)
  </read_first>
  <behavior>
    - Test: deriveAnyGsd([{phase_id:'36'},{phase_id:null}]) === true (some row has phase_id)
    - Test: deriveAnyGsd([{phase_id:null},{}]) === false (no row has phase_id)
    - Test: deriveAnyGsd([]) === false
    - Test: deriveAnyGsd is computed over the unfiltered set — a fixture where filtering would remove the GSD rows still returns true when derived from the full list (documents the D-08 "structural, not filter-sensitive" contract)
  </behavior>
  <action>In `src/cli/dashboard/select.js`, add and export `deriveAnyGsd(rows)` returning `rows.some((r) => r.phase_id != null)` — a single pure React-free fn, no regex, no color, mirroring the `countByStatus` shape and JSDoc convention. In `App.js`, after `const sorted = sortSessions(sessions);` (:265), add `const anyGsd = deriveAnyGsd(sorted);` (import `deriveAnyGsd` from './select.js' next to the existing select imports). CRITICAL (Pitfall 4 / D-08): derive from `sorted`, NOT `filtered` — otherwise typing a `/` filter that empties GSD rows would make the column flicker. Pass `anyGsd` into the `createElement(SessionTable, {...})` prop bag (:556-573), mirroring how `overlayKind`/`scrollOffset` are threaded. Extend `test/dashboard-select.test.js` with the truth-table cases above (direct import of `deriveAnyGsd`).</action>
  <verify>
    <automated>node --test test/dashboard-select.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function deriveAnyGsd" src/cli/dashboard/select.js` returns 1
    - `grep -c "deriveAnyGsd(sorted)" src/cli/dashboard/App.js` returns 1 (derived from sorted, NOT filtered)
    - `grep -c "new RegExp" src/cli/dashboard/select.js` returns 0
    - `node --test test/dashboard-select.test.js` exits 0 including the deriveAnyGsd truth table
    - `test/dashboard-select.test.js` contains a derive-before-filter case: a fixture whose GSD rows would be removed by an active filter but, passed as the FULL unfiltered set to `deriveAnyGsd`, still returns `true` — mechanically proving D-08 derivation order (not just the `deriveAnyGsd(sorted)` grep)
    - `grep -c "anyGsd" src/cli/dashboard/App.js` returns at least 2 (compute + prop thread)
  </acceptance_criteria>
  <done>deriveAnyGsd is a pure exported fn; App.js derives anyGsd from the unfiltered sorted set and threads it to SessionTable; truth-table tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional phasemode column drop + per-row (zombie) mark + COLS.state widen</name>
  <files>src/cli/dashboard/SessionTable.js, test/dashboard-table.test.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js:38-48 (COLS block + width-tuning comment convention) — COLS.state 16→18
    - src/cli/dashboard/SessionTable.js:199-216 (prop destructure — add anyGsd + its JSDoc)
    - src/cli/dashboard/SessionTable.js:283-325 (header row incl. phase/mode :291, state cell IIFE :315-317, phasemode data cell :323, statusColor at :302)
    - src/cli/dashboard/format.js:108-116 (statusColor — already returns {color:'red'} for running+!alive; NO edit)
    - test/dashboard-table.test.js:7-35 (test doc: existing `running (zombie)` header assertion + KL-2 non-GSD zombie fixture) and :104-124 (drain/fixture harness)
    - .planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-RESEARCH.md:320-339 (Pattern 4 zombie cell) and :389-393 (Pitfall 3 truncation)
  </read_first>
  <action>Four edits in `SessionTable.js`, all in the table-body/column region (NOT the renderOverlay region owned by 44-01). (1) COLS widen: change `state: 16` → `state: 18` in the COLS object (:48) so `▶ running (zombie)` (~18 cells) survives un-truncated (Pitfall 3); extend the adjacent width-tuning comment in the file's convention if present. (2) Add `anyGsd` to the prop destructure (:199-216) with a JSDoc line describing it as the structural GSD-presence flag (TUI-18). (3) TUI-18 column drop: guard BOTH the `phase/mode` header `<Box>` (:291) AND the per-row phasemode data `cell(...)` (:323) behind `anyGsd` — when `anyGsd === false`, do NOT emit either element (e.g. conditionally include via `...(anyGsd ? [<element>] : [])` spread, or a falsy filter, matching the surrounding array-build style). Dropping the element reclaims its 11 cells for free — ink's `flexDirection:'row'` shifts siblings left; do NOT do width arithmetic (RESEARCH Pattern 3; A3). (4) TUI-19 zombie mark: in the `state` cell IIFE (:315-317), compute `const isZombie = (session.status === 'running' || session.state === 'running') && session.alive === false;`; build the badge text as today, then when `isZombie` append ` (zombie)` to the text (additive, NO_COLOR-safe) and set `color = statusColor(session.status ?? '', session.alive, session.state).color` (already 'red' — zero new color, zero picocolors, statusColor already imported at :25). Keep `truncate: false`. Do NOT touch the header counter (`countsLabel`/`countByStatus`) — the per-row mark is additive (D-09). Extend `test/dashboard-table.test.js`: (a) assert that with no GSD rows in the fixture there is NO `phase/mode` header and width is reclaimed, and with ≥1 GSD row the header is present; (b) assert the per-row `(zombie)` mark appears in the `state` cell for a running+!alive row (the existing KL-2 non-GSD zombie fixture is a candidate) AND that the header zombie counter is unchanged; (c) update any byte-stable width assertions affected by `COLS.state` 16→18.</action>
  <verify>
    <automated>node --test test/dashboard-table.test.js</automated>
    <automated>node --test test/format-isolation.test.js</automated>
    <automated>node --test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "state: 18" src/cli/dashboard/SessionTable.js` returns 1 (COLS.state widened)
    - `grep -c "anyGsd" src/cli/dashboard/SessionTable.js` returns at least 3 (destructure + header guard + data-cell guard)
    - `grep -c "(zombie)" src/cli/dashboard/SessionTable.js` returns 1 (additive per-row mark)
    - `grep -c "statusColor(session.status" src/cli/dashboard/SessionTable.js` returns at least 1 in the state-cell zombie branch (red from existing fn, no new color)
    - `grep -c picocolors src/cli/dashboard/SessionTable.js` returns 0
    - `node --test test/dashboard-table.test.js` exits 0 including column-hide, per-row `(zombie)`, and updated width assertions
    - `node --test` exits 0 (full suite green; WARNING-01 cycle still resolves)
  </acceptance_criteria>
  <done>The phase/mode column is dropped (width reclaimed) when anyGsd is false and reappears with a GSD row; a zombie row shows a red `(zombie)` mark in the state cell sourced from statusColor; COLS.state is 18; header counter untouched; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session row → render | `phase_id`/`status`/`alive`/`state` from GET /status drive a pure derivation and a cell projection; no untrusted-input sink, no filesystem, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Denial of Service | regex on row fields in deriveAnyGsd / state cell | mitigate | `deriveAnyGsd` uses `r.phase_id != null` (no string matching); the zombie branch uses equality comparisons only — zero `new RegExp` (D-13) |
| T-44-07 | Tampering | new color literal bypassing color isolation | mitigate | the zombie red is read from the existing `statusColor` (format.js); zero new color literal, zero picocolors under src/cli/dashboard/ (D-12). Asserted by test/format-isolation.test.js |
| T-44-08 | Information Disclosure | column drop hiding a GSD session that is actually active | accept | `anyGsd` is derived over the full unfiltered `sorted` set, so the column is present whenever ANY active session is GSD; it only drops when there is genuinely no GSD session to show (D-08) |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase; no install task, no slopcheck checkpoint required |

No network/auth/path threats apply: this plan touches only the pure derive layer and ink render (no endpoints, no filesystem, no input sink).
</threat_model>

<verification>
- `node --test test/dashboard-select.test.js` — deriveAnyGsd truth table over unfiltered rows (TUI-18)
- `node --test test/dashboard-table.test.js` — column-hide render + per-row `(zombie)` mark + COLS.state width (TUI-18/TUI-19)
- `node --test test/format-isolation.test.js` — zero picocolors, zombie red from statusColor (D-12/D-09)
- `node --test` — full suite green, WARNING-01 cycle still resolves
</verification>

<success_criteria>
- `phase/mode` column not rendered (width reclaimed) when anyGsd is false; reappears when a GSD session enters; derived over `sorted`, filter-insensitive (TUI-18, D-08)
- zombie row shows a red `(zombie)` mark in the `state` cell from the existing statusColor; header counter kept (TUI-19, D-09)
- COLS.state widened 16→18 so the mark is not truncated (TUI-19, Pitfall 3)
- zero new color, zero picocolors, zero RegExp on row fields (D-12/D-13)
</success_criteria>

<output>
Create `.planning/phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-02-SUMMARY.md` when done
</output>
