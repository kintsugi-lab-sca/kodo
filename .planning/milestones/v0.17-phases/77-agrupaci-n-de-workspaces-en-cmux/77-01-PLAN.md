---
phase: 77-agrupaci-n-de-workspaces-en-cmux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cmux/client.js
  - src/host/cmux.js
  - test/cmux/client-args.test.js
autonomous: true
requirements: [GRP-01, GRP-03, GRP-04]

must_haves:
  truths:
    - "`cmux new-workspace` recibe `--group <ref>` en su argv exactamente cuando `opts.group` está presente; sin `opts.group` el argv es byte-idéntico al actual (GRP-01)"
    - "`buildNewWorkspaceArgs(opts)` es una función pura y exportada: mismo input → mismo argv determinista, con orden `new-workspace`,`--name`,`--cwd`,`--command`,`--group`"
    - "`listWorkspaceGroups()` ejecuta `workspace-group list --json` vía `run()` y devuelve el stdout crudo — no parsea JSON (D-05)"
    - "`run()` rejecta ante error/timeout de cmux, y esa promesa rechazada es lo que da gratis la capa 1 de fail-open al caller (GRP-03)"
    - "`host._legacy.listWorkspaceGroups` es un passthrough lazy-import fiel a `client.js`; el walker `cmux-isolation.test.js` sigue verde y `HOST_METHODS` sigue congelado en 4 (D-06)"
  prohibitions:
    - "kodo no añade a `client.js` ningún subcomando de gestión de grupos de cmux — de la familia workspace-group solo se añade `list` (read-only); create / rename / delete / ungroup / add quedan fuera (GRP-04)"
    - "`HOST_METHODS` permanece congelado en 4; el método nuevo vive SOLO dentro de `_legacy`, nunca en el contrato ni en el `return` de los 4 métodos"
    - "cero dependencias npm nuevas: todo con `node:child_process` (execFile) ya presente en `client.js`"
    - "el parseo del JSON de `workspace-group list` NO vive en `client.js` (D-05): el passthrough devuelve stdout crudo"
    - "cmux sigue confinado a `src/host/` + `src/cmux/`: no se introduce ninguna ejecución de cmux fuera de esos dos árboles"
  artifacts:
    - src/cmux/client.js
    - src/host/cmux.js
    - test/cmux/client-args.test.js
  key_links:
    - "host._legacy.listWorkspaceGroups → import('../cmux/client.js').listWorkspaceGroups (lazy import; único import de cmux/client permitido por el walker)"
    - "buildNewWorkspaceArgs es el punto donde `opts.group` se traduce a `--group <ref>` en el argv de execFile — argv array, sin shell (V5/Tampering)"
---

<objective>
Añadir la fontanería cmux para agrupar workspaces: el passthrough read-only `listWorkspaceGroups()`
en `src/cmux/client.js`, su espejo en `host._legacy`, y el flag `--group` en `newWorkspace` extraído a
una función pura `buildNewWorkspaceArgs(opts)` testeable directamente. Es la capa de I/O sobre la que
la Plan 02 cablea la resolución y el fail-open.

Purpose: dar al launch un punto único read-only para leer grupos (nunca gestionarlos, GRP-04) y una
construcción de argv determinista y testeada para el `--group` (GRP-01), respetando el confinamiento
de cmux a `src/host/`+`src/cmux/` (walker) y `HOST_METHODS` congelado en 4 (D-06).
Output: `client.js` con `buildNewWorkspaceArgs` + `--group` + `listWorkspaceGroups`; `host/cmux.js` con
`_legacy.listWorkspaceGroups`; una suite pura nueva `test/cmux/client-args.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-CONTEXT.md
@.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-RESEARCH.md
@.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-PATTERNS.md
@src/cmux/client.js
@src/host/cmux.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: client.js — buildNewWorkspaceArgs pura con --group + passthrough listWorkspaceGroups</name>

  <read_first>
    - `src/cmux/client.js` — el fichero que se modifica: `run()` (`:14-26`, execFile argv array + timeout 15s + reject en err), `newWorkspace` (`:32-40`, patrón `--cwd`/`--command`), `listWorkspaces` (`:79-81`, analog exacto del passthrough)
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-PATTERNS.md` §`src/cmux/client.js` — el bloque que dicta calcar `listWorkspaces` y el push de `--group`
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-RESEARCH.md` §Pattern 4 y §Validation Architecture Wave 0 Gaps (la recomendación de extraer `buildNewWorkspaceArgs`)
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-CONTEXT.md` §decisions — D-05 (client no parsea JSON), D-09 (sin version-check)
  </read_first>

  <files>src/cmux/client.js, test/cmux/client-args.test.js</files>

  <behavior>
    - `buildNewWorkspaceArgs({ name: 'X' })` devuelve exactamente `['new-workspace', '--name', 'X']`.
    - `buildNewWorkspaceArgs({ name: 'X', cwd: '/p' })` devuelve `['new-workspace', '--name', 'X', '--cwd', '/p']`.
    - `buildNewWorkspaceArgs({ name: 'X', cwd: '/p', command: 'claude' })` añade `'--command', 'claude'` en ese orden, tras `--cwd`.
    - `buildNewWorkspaceArgs({ name: 'X', group: 'workspace_group:1' })` incluye `'--group', 'workspace_group:1'` como dos elementos de array consecutivos.
    - `buildNewWorkspaceArgs({ name: 'X' })` (sin `group`) NO contiene el token `'--group'` en ninguna posición del array — argv byte-idéntico al comportamiento previo.
    - `buildNewWorkspaceArgs({ name: 'X', cwd: '/p', command: 'c', group: 'workspace_group:2' })` produce el orden completo `new-workspace, --name, X, --cwd, /p, --command, c, --group, workspace_group:2`.
    - `buildNewWorkspaceArgs({ name: 'X', group: '' })` (grupo falsy) NO incluye `--group` (el push está guardado por truthiness, igual que `--cwd`/`--command`).
    - El array devuelto es plano de strings (cada elemento es `typeof === 'string'`) — apto para `execFile` sin shell.
  </behavior>

  <action>
    Refactorizar `newWorkspace` en `src/cmux/client.js` para que construya su argv mediante una nueva
    función pura exportada `buildNewWorkspaceArgs(opts)`, y añadir el passthrough `listWorkspaceGroups`.

    1. Exportar `buildNewWorkspaceArgs(opts)` (función pura, sin I/O): parte de `['new-workspace', '--name', opts.name]`
       y hace `push` condicional de `'--cwd', opts.cwd` cuando `opts.cwd` es truthy, `'--command', opts.command`
       cuando `opts.command` es truthy, y `'--group', opts.group` cuando `opts.group` es truthy. El orden es
       `--cwd`, `--command`, `--group`. Devuelve el array. Es el mismo idiom de push condicional que ya usa
       `newWorkspace` hoy — solo se extrae a función para darle un test directo (el `run()` con `execFile` no es
       inyectable, RESEARCH §Wave 0 Gaps).
    2. Reescribir `newWorkspace(opts)` para que haga `const args = buildNewWorkspaceArgs(opts)` y luego el
       `run(args)` + la extracción de ref con el regex `/(workspace:\d+)/` EXISTENTE (no tocar el regex — tolera
       el aviso legacy de `new-workspace`, verificado en ROADMAP §Phase 77). Actualizar el JSDoc `@param` para
       añadir `group?: string` al shape de `opts`.
    3. Añadir `export async function listWorkspaceGroups()` que devuelve `run(['workspace-group', 'list', '--json'])`
       — calcado literal de `listWorkspaces` (`:79-81`), devuelve el stdout crudo. NO parsea JSON (D-05): el
       parseo defensivo vive en la función pura de la Plan 02. Añadir un JSDoc `@returns {Promise<string>}` que
       aclare que es JSON crudo de `workspace-group list`.

    De la familia de subcomandos de grupos de cmux, este task añade ÚNICAMENTE el verbo `list` (read-only). No se
    introduce ningún verbo de gestión de grupos (GRP-04).

    Seguridad (V5 / Tampering, T-77-01): el argv se pasa a `execFile` como array; el ref de grupo viaja como
    elemento de array, jamás interpolado en un string de shell. No introducir `exec`/`shell`/plantilla de string.

    Crear `test/cmux/client-args.test.js` (fichero nuevo, `node:test` + `node:assert/strict`, importa
    `buildNewWorkspaceArgs` de `../../src/cmux/client.js`, sin FS ni cmux real) con los casos de `<behavior>`.
    Escribir los tests primero, verlos fallar, luego implementar.
  </action>

  <verify>
    <automated>node --test test/cmux/client-args.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/cmux/client-args.test.js` exits 0 y cubre todos los casos de `<behavior>`.
    - Behavior assertion: `buildNewWorkspaceArgs({ name:'X', group:'workspace_group:1' })` contiene la subsecuencia `['--group','workspace_group:1']` y `buildNewWorkspaceArgs({ name:'X' })` no contiene `'--group'`.
    - Behavior assertion: el orden relativo de flags es siempre `--name` → `--cwd` → `--command` → `--group`.
    - Source assertion: `src/cmux/client.js` exporta `buildNewWorkspaceArgs` y `newWorkspace` lo invoca (`newWorkspace` ya no construye el array inline).
    - Source assertion: `src/cmux/client.js` exporta `listWorkspaceGroups` y su cuerpo es `return run(['workspace-group', 'list', '--json'])` sin ninguna llamada a `JSON.parse` (D-05).
    - Source assertion: el regex `/(workspace:\d+)/` de `newWorkspace` permanece intacto.
  </acceptance_criteria>

  <done>`client.js` construye el argv de `new-workspace` (incluido `--group` opcional) vía una función pura testeada, y expone el passthrough read-only `listWorkspaceGroups` sin parsear JSON.</done>
</task>

<task type="auto">
  <name>Task 2: host/cmux.js — espejo _legacy.listWorkspaceGroups (walker verde, HOST_METHODS intacto)</name>

  <read_first>
    - `src/host/cmux.js:357-388` — el objeto `_legacy` (cada método es un lazy-import passthrough fiel) y el `return { ...4 métodos..., _legacy }` que NO se toca
    - `src/host/interface.js` — `HOST_METHODS` congelado en 4; el contrato que NO cambia
    - `test/host/cmux-isolation.test.js:59-79` — el walker que exige que solo `src/host/`+`src/cmux/` importen `cmux/client.js` (regex `/\/cmux\/client/` sobre imports de `src/session/`, etc.)
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-PATTERNS.md` §`src/host/cmux.js` — el analog exacto (`listWorkspaces` passthrough) a calcar
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-CONTEXT.md` §decisions — D-06
  </read_first>

  <files>src/host/cmux.js</files>

  <action>
    Añadir un método `listWorkspaceGroups` dentro del objeto `_legacy` de `src/host/cmux.js` (antes del
    cierre `}` del objeto `_legacy`, junto al espejo de `listWorkspaces` en `:382-385`). El cuerpo es el
    passthrough lazy-import fiel: `return (await import('../cmux/client.js')).listWorkspaceGroups();`, con un
    JSDoc `@returns {Promise<string>}` que aclare que es JSON crudo de `workspace-group list`.

    Invariantes que NO se tocan: `HOST_METHODS` (congelado en 4), el `return { listWorkspaces, selectWorkspace,
    isAlive, needsInput, listAgentSurfaces, _legacy }` de `:388` (el nuevo método vive SOLO en `_legacy`, nunca
    en el contrato de 4), y el confinamiento de cmux — el único import de `cmux/client.js` sigue estando en este
    fichero, así que el walker sigue verde.
  </action>

  <verify>
    <automated>node --test test/host/cmux-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/host/cmux-isolation.test.js` exits 0 (el walker sigue verde: ningún fichero de `src/session/` importa `cmux/client.js`).
    - Source assertion: `src/host/cmux.js` contiene un método `listWorkspaceGroups` dentro del objeto `_legacy` cuyo cuerpo hace `import('../cmux/client.js')` y llama `.listWorkspaceGroups()`.
    - Source assertion: el `return` de `getHost('cmux')` sigue exponiendo exactamente los 4 métodos de contrato + `_legacy` (el método nuevo NO aparece en la lista del `return`, solo en `_legacy`).
    - Source assertion: `src/host/interface.js` `HOST_METHODS` no cambia de longitud (sigue en 4) — el diff de esta fase no toca `interface.js`.
  </acceptance_criteria>

  <done>`host._legacy.listWorkspaceGroups` existe como passthrough fiel; el contrato de 4 métodos y el walker de aislamiento permanecen intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kodo (`client.js`) → cmux binary | kodo ejecuta el binario cmux vía `execFile` con argv array; el ref de grupo cruza aquí como argumento |
| cmux binary → kodo | el stdout JSON de `workspace-group list` vuelve a kodo (parseado en Plan 02, no aquí) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-01 | Tampering | `buildNewWorkspaceArgs` / `run` (argv de `new-workspace --group`) | high | mitigate | argv array vía `execFile` (nunca `exec`/shell); el ref viaja como elemento de array — superficie de inyección nula. Verificado: `client.js` usa `execFile`. |
| T-77-04 | Denial of Service | `run(['workspace-group','list','--json'])` (cmux headless/cuelgue) | low | mitigate | control existente reutilizado: `run()` tiene timeout 15s (`client.js:17`); expira → reject → capa 1 fail-open en el caller (Plan 02). Cero llamadas nuevas en el reconcile loop (D-12). |
</threat_model>

<verification>
- `node --test test/cmux/client-args.test.js test/host/cmux-isolation.test.js` verde tras cada task.
- Tras la wave: `npm test` verde (baseline ~2132 tests, 1 skipped). Si aparece el flake conocido `test/gsd-lock-race.test.js` «concurrent dead-holder steal (CR-01)», anotarlo y seguir — NO tocarlo (fuera de alcance).
- Commits atómicos por task; `git add` de ficheros específicos (nunca `git add .`/`-A`).
</verification>

<success_criteria>
- `client.js` emite `--group <ref>` en el argv de `new-workspace` exactamente cuando `opts.group` es truthy, verificado por test directo de `buildNewWorkspaceArgs` (GRP-01).
- `listWorkspaceGroups()` existe como passthrough read-only que devuelve stdout crudo, disponible vía `host._legacy` (GRP-03/GRP-04, base de la capa 1).
- El walker de aislamiento y `HOST_METHODS` (4) permanecen verdes/intactos (D-06).
- De la familia workspace-group solo se añadió `list` — ningún verbo de gestión de grupos (GRP-04).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

Símbolos nuevos que esta fase crea (para excluirlos del source-grounding de drift review):

- `buildNewWorkspaceArgs(opts)` — función pura exportada en `src/cmux/client.js` (nueva export).
- `listWorkspaceGroups()` — export nuevo en `src/cmux/client.js` (passthrough read-only de `workspace-group list --json`).
- `--group <ref>` — flag nuevo en el argv de `new-workspace` (vía `buildNewWorkspaceArgs`).
- `_legacy.listWorkspaceGroups` — método nuevo en el objeto `_legacy` de `getHost('cmux')` en `src/host/cmux.js`.
- `test/cmux/client-args.test.js` — fichero de test nuevo.
</artifacts_produced>

<output>
Create `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-01-SUMMARY.md` when done
</output>
