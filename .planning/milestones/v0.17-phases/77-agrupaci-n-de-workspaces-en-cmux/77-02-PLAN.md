---
phase: 77-agrupaci-n-de-workspaces-en-cmux
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - src/session/manager.js
  - test/session/group-resolve.test.js
  - test/manager.test.js
autonomous: true
requirements: [GRP-01, GRP-02, GRP-03, GRP-04]

must_haves:
  truths:
    - "Al lanzar una tarea cuyo grupo existe en la sidebar, el `new-workspace` sale con el `--group <ref>` resuelto y el workspace queda como miembro del grupo — comprobable en `workspace-group list --json` → `member_workspace_refs` (GRP-01)"
    - "`deriveExpectedGroupName`: proyecto flat o path resuelto == `entry.default` → identifier a secas (`KODO`); módulo con path propio distinto del default → `IDENTIFIER/Módulo` (`ROMAN/FVF`); los F0..F6 de SCP (todos == default) colapsan al identifier (GRP-02)"
    - "El identifier se deriva de `task.ref` sin plumbear config a la función pura: Plane `IDENT-<seq>` → `IDENT` (strip trailing `-<dígitos>`); GitHub `owner/repo#n` → basename antes de `#` (GRP-02)"
    - "Empate: dos grupos que normalizan al mismo `name` → `resolveWorkspaceGroup` toma el PRIMERO de la lista `groups`; determinista y estable (D-03 · edge GRP-01 adjacency/ordering)"
    - "Lista de grupos vacía, `groups` ausente, o JSON malformado → `resolveWorkspaceGroup` devuelve null → la sesión se lanza sin `--group`; never-throws (edge GRP-01 empty · GRP-03 capa 1)"
    - "`workspace-group list` falla (cmux < 0.64.19, daemon headless, socket roto) → el try/catch englobante deja `groupRef = null` → se omite el flag y la sesión se lanza exactamente como hoy (edge GRP-03 empty · capa 1)"
    - "Grupo borrado entre `list` y `new-workspace` (TOCTOU): el `new-workspace --group` falla (ref inválido = fatal exit=1) → UN reintento sin `--group` que sí crea el workspace (D-10 · edge GRP-03 adjacency · capa 2)"
    - "El reintento ocurre EXACTAMENTE una vez y solo cuando el fallo fue con `--group` presente; un fallo del reintento sin grupo propaga como hoy (edge GRP-03 ordering · D-10)"
    - "La resolución añade como mucho UNA llamada cmux por lanzamiento (~50ms) y cero llamadas en el reconcile loop (D-12)"
  prohibitions:
    - "kodo no ejecuta ningún verbo de gestión de grupos de cmux — de la familia workspace-group solo se usa `list` (read-only); create / rename / delete / ungroup / add quedan fuera (GRP-04)"
    - "ningún ref `workspace_group:N` se persiste en `state.json` ni en config: `buildSessionFromTask` no gana campos de grupo; el ref se pasa a `newWorkspace` y se descarta (GRP-04, defensa Phase 43 — resolver en fresco, no persistir)"
    - "`manager.js` no importa `src/cmux/client.js`: toda ejecución cmux pasa por `host._legacy` (walker `cmux-isolation.test.js`)"
    - "no se toca `buildSessionFromTask`, `setColor`, `addSession`, `send` ni `notify` — el orden de efectos del launch se preserva; el grupo es cosmético sobre la carga útil"
    - "no se tocan hooks, `state.json`, `src/server.js` ni el reconcile loop; cero endpoints nuevos; cero dependencias npm nuevas"
    - "sin version-check de cmux: el soporte se deriva del éxito/fallo de la propia llamada (D-09)"
    - "el `console.log` de degradación lleva SOLO identifier/ref/motivo, nunca el título de la tarea ni el nombre de grupo con contenido libre (D-11)"
  artifacts:
    - src/session/manager.js
    - test/session/group-resolve.test.js
    - test/manager.test.js
  key_links:
    - "launchWorkItem → host._legacy.listWorkspaceGroups() → JSON.parse (try/catch) → resolveWorkspaceGroup → newWorkspaceWithGroupFallback → host._legacy.newWorkspace({ group })"
    - "deriveExpectedGroupName consume resolveProjectPath (path resuelto) y deriveModuleName — reusa sus outputs, no re-lee projects.json (GRP-02)"
    - "newWorkspaceWithGroupFallback recibe `host._legacy.newWorkspace` inyectado → el retry D-10 tiene dientes reales en test (no solo source-hygiene)"
---

<objective>
Añadir las dos funciones puras (derivar el nombre esperado de grupo, resolver nombre→ref) y el helper
de reintento con dientes (`newWorkspaceWithGroupFallback`) a `src/session/manager.js`, y cablearlas en el
seam de `launchWorkItem` con fail-open en dos capas. La lógica arriesgada (el CONTRATO de matching) vive
en funciones puras que SÍ se ejecutan en test; el cableado se verifica por source-hygiene (analog
`manager.test.js:769-782`) porque `launchWorkItem` hace I/O real de cmux/provider y no corre en test.

Purpose: hacer que cada sesión de tarea aterrice en el grupo cmux de su path resuelto (GRP-01/GRP-02)
sin poder perder nunca una sesión por culpa de la agrupación (GRP-03, fail-open total) y sin gestionar
ni persistir grupos (GRP-04). Depende de Plan 01 (`host._legacy.listWorkspaceGroups` + `--group`).
Output: `manager.js` con 3 funciones nuevas + cableado; `test/session/group-resolve.test.js` (unit puro);
asserts nuevos de source-hygiene en `test/manager.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-CONTEXT.md
@.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-RESEARCH.md
@.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-PATTERNS.md
@src/session/manager.js
@src/providers/plane/normalize.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: manager.js — deriveExpectedGroupName + resolveWorkspaceGroup + newWorkspaceWithGroupFallback (funciones puras) y su suite unit</name>

  <read_first>
    - `src/session/manager.js:79-115` — `resolveProjectPath` (compara módulo vs `entry.default`) y `deriveModuleName` (`task.groups[0] || null`): los analogs de estilo y el output que consumen las funciones nuevas
    - `src/host/cmux.js:57-140` — `normalizeSurface`/`buildTitleMap`: el analog defensivo never-throws (`Array.isArray` guard, type-check por campo, shape inesperado → vacío/null) a calcar en `resolveWorkspaceGroup`
    - `src/providers/plane/normalize.js:66` — `ref = ${projectIdentifier}-${sequence_id}` (de dónde sale el identifier); confirmar el shape de `task.ref`
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-RESEARCH.md` §Pattern 1, §Pattern 2, §Pattern 3 (código canónico de las 3 funciones), §Common Pitfalls 1-4, y el shape live de `workspace-group list --json` (`Kodo`/`SCRIBBA`/`SCP-CMRi`)
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-CONTEXT.md` §decisions — D-01, D-02, D-03, D-07, D-08, D-10, D-11
    - `test/session/max-parallel-alive.test.js` — el analog de test que importa una función pura de `manager.js` y la ejerce sin FS/HOME
  </read_first>

  <files>src/session/manager.js, test/session/group-resolve.test.js</files>

  <behavior>
    - `deriveExpectedGroupName` con `entry` flat string (`'/klab/kodo'`) y `task.ref='KODO-9'` → `'KODO'` (D-01, proyecto flat).
    - `deriveExpectedGroupName` con `entry` objeto, `resolvedPath === entry.default` y `task` con módulo → `'KODO'` (path == default colapsa al identifier; caso F0..F6 de SCP).
    - `deriveExpectedGroupName` con `entry` objeto, módulo `'FVF'` y `resolvedPath !== entry.default` → `'ROMAN/FVF'` (D-01, módulo con path propio; `task.ref='ROMAN-3'`).
    - `deriveExpectedGroupName` con módulo presente pero que cayó al default (`resolvedPath === entry.default`) → identifier a secas, no `IDENTIFIER/Módulo` (robustez GRP-02).
    - Identifier Plane: `task.ref='SCP-42'` → `'SCP'` (strip trailing `-<dígitos>`, no corta de más — A2).
    - Identifier GitHub: `task.ref='acme/x#7'` → `'x'` (basename antes de `#`) — `[ASSUMED A1]`, bajo riesgo, fail-open cubre el no-match.
    - `task.ref` degenerado (`''`, `'   '` solo-whitespace, ausente/`undefined`, o no-string) → `deriveExpectedGroupName` devuelve `null` directamente, SIN lanzar y SIN emitir un nombre bogus (nada de `'undefined'`/`''` como grupo); y ese `null` piped por `resolveWorkspaceGroup(fixtureLive, <null>)` → `null` (fail-open end-to-end: sin ref válido no hay grupo — edge GRP-02 degenerate, contrato de entrada malformada probado, no solo safe-by-construction).
    - `resolveWorkspaceGroup(fixtureLive, 'KODO')` → `'workspace_group:1'` (match case-insensitive+trim contra `name:'Kodo'`, D-03).
    - `resolveWorkspaceGroup(fixtureLive, 'SCRIBBA')` → `'workspace_group:2'`.
    - `resolveWorkspaceGroup(fixtureLive, 'SCP')` → `null` (Pitfall 1: `scp` ≠ `scp-cmri`, el grupo del operador no matchea — fail-open esperado y correcto).
    - `resolveWorkspaceGroup` con dos grupos que normalizan al mismo nombre → el ref del PRIMERO de la lista (D-03 empate, determinista/estable).
    - `resolveWorkspaceGroup(null, 'X')`, `resolveWorkspaceGroup({}, 'X')`, `resolveWorkspaceGroup({groups: 'no-array'}, 'X')`, `resolveWorkspaceGroup({groups: [{name:5}]}, 'X')` → todos `null`, sin lanzar (D-07 never-throws · edge GRP-01 empty/GRP-03 capa 1).
    - `resolveWorkspaceGroup` con un `name` que solo difiere en mayúsculas/espacios/forma Unicode (`' kodo '`, `'KODO'`) → matchea igual (norm = NFC+lowercase+trim).
    - `newWorkspaceWithGroupFallback(fn, base, null)` → llama `fn(base)` UNA vez, sin `group` (sin grupo → como hoy).
    - `newWorkspaceWithGroupFallback(fn, base, 'workspace_group:1')` con `fn` que resuelve → llama `fn` UNA vez con `{ ...base, group:'workspace_group:1' }` y devuelve su valor.
    - `newWorkspaceWithGroupFallback(fn, base, 'workspace_group:1')` con `fn` que RECHAZA al primer intento → segundo intento `fn(base)` SIN `group`, y emite exactamente UNA línea de log vía el `log` inyectado (D-10 capa 2 + D-11).
    - En ese caso de retry, `fn` se invoca exactamente 2 veces: la 1ª con `group`, la 2ª sin `group` (edge GRP-03 ordering: el retry es único).
    - Si el segundo intento (sin grupo) también rechaza, `newWorkspaceWithGroupFallback` propaga el error (no lo captura).
    - El `log` inyectado recibe una cadena que contiene `group_skipped` y el ref, y NO contiene el título de la tarea (D-11).
  </behavior>

  <action>
    Añadir a `src/session/manager.js`, junto a `deriveModuleName` (`:106-115`), tres funciones exportadas
    (mismo estilo: pura, JSDoc `@param`/`@returns`). Código canónico en RESEARCH §Pattern 1/2/3 — replicarlo,
    no reinventar.

    1. `deriveExpectedGroupName(task, entry, resolvedPath)` (D-01/D-02/D-08): **guarda de entrada degenerada como
       primera línea** — si `task.ref` no es un string no-vacío (ausente, `undefined`, no-string, o solo whitespace
       tras `trim`) → `return null` de inmediato. NO derives un nombre bogus (`'undefined'`, `''`) sobre input
       malformado: `null` propaga limpio a `resolveWorkspaceGroup` (que no matchea `null` contra ningún `name` →
       devuelve `null`) y la sesión se lanza sin `--group` (fail-open). Esto blinda el `replace(/-\d+$/,'')` de
       correr sobre `undefined`. Con `ref` válido, deriva el identifier humano de
       `task.ref` — si `ref` incluye `#` es GitHub (basename antes de `#`), si no es Plane (`replace(/-\d+$/,'')`).
       Calcula `moduleName = deriveModuleName(task)`. Si `entry` es flat string, o no hay módulo, o
       `resolvedPath === entry.default` → devuelve el identifier a secas. Si hay módulo Y `resolvedPath !== entry.default`
       → devuelve `` `${identifier}/${moduleName}` ``. Comparar contra `resolvedPath === entry.default` implementa
       GRP-02 literal (path resuelto decide default-vs-módulo). NO re-leer projects.json — consume el output que
       `resolveProjectPath` ya computó (Don't Hand-Roll).
    2. `resolveWorkspaceGroup(groupsJson, expectedName)` (D-03/D-07): función pura defensiva calcada de
       `buildTitleMap`. Define `norm = (s) => String(s).normalize('NFC').toLowerCase().trim()`. Guard
       `if (!groupsJson || !Array.isArray(groupsJson.groups)) return null`. Itera; devuelve `g.ref` del PRIMER
       `g` con `typeof g.name === 'string' && typeof g.ref === 'string' && norm(g.name) === norm(expectedName)`.
       Si ninguno matchea → `null`. Nunca lanza.
    3. `newWorkspaceWithGroupFallback(newWorkspaceFn, baseOpts, group, log = console.log)` (D-10/D-11): si
       `!group` → `return newWorkspaceFn(baseOpts)`. Si hay grupo → `try { return await newWorkspaceFn({ ...baseOpts, group }); }`
       `catch { log('[kodo] group_skipped — retry_sin_grupo ' + group); return newWorkspaceFn(baseOpts); }`.
       El `newWorkspaceFn` es inyectable (para test); el `log` inyectable con default `console.log`. El log lleva
       SOLO el ref/motivo, nunca contenido de usuario (D-11, precedente `worktree_skipped_nongit` `:311-313`).

    Discreción resuelta por RESEARCH: mantener las 3 funciones en `manager.js` (misma casa, mismo estilo, D-08),
    NO crear un módulo nuevo. Normalización del match = NFC + lowercase + trim (cubre `Traça Web`).

    Crear `test/session/group-resolve.test.js` (fichero nuevo, `node:test` + `node:assert/strict`, importa las 3
    funciones de `../../src/session/manager.js`, sin FS ni cmux real) con un fixture inline del shape live de los
    3 grupos reales (`Kodo`/`SCRIBBA`/`SCP-CMRi`, RESEARCH §Pattern 2) y todos los casos de `<behavior>`. El caso
    de retry (D-10) usa un `newWorkspaceFn` stub que rechaza la 1ª vez y un `log` stub que cuenta invocaciones —
    esto le da dientes reales al fail-open capa 2. Escribir los tests primero, verlos fallar, luego implementar.
  </action>

  <verify>
    <automated>node --test test/session/group-resolve.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/session/group-resolve.test.js` exits 0 y cubre todos los casos de `<behavior>`.
    - Behavior assertion: `resolveWorkspaceGroup(fixtureLive, 'KODO') === 'workspace_group:1'` y `resolveWorkspaceGroup(fixtureLive, 'SCP') === null` (Pitfall 1 verificado en test).
    - Behavior assertion: `resolveWorkspaceGroup` devuelve `null` (sin lanzar) para `null`, `{}`, `{groups:'x'}` y grupos con `name` no-string (never-throws).
    - Behavior assertion: en el caso de retry, el `newWorkspaceFn` stub se invoca 2 veces (1ª con `group`, 2ª sin) y el `log` stub se invoca exactamente 1 vez con una cadena que contiene `group_skipped`.
    - Behavior assertion: `deriveExpectedGroupName` devuelve `'ROMAN/FVF'` para módulo con path propio y `'KODO'` para flat/path==default.
    - Behavior assertion (degenerado): `deriveExpectedGroupName` con `task.ref` `''` / whitespace / `undefined` / no-string devuelve `null` sin lanzar, y `resolveWorkspaceGroup(fixtureLive, null)` devuelve `null` (fail-open del contrato de entrada malformada probado end-to-end).
    - Source assertion: las 3 funciones están exportadas desde `src/session/manager.js` (no en un módulo aparte).
  </acceptance_criteria>

  <done>Las 3 funciones puras existen en `manager.js`, testeadas contra el fixture live; el retry D-10 tiene dientes reales vía `newWorkspaceFn` inyectado.</done>
</task>

<task type="auto">
  <name>Task 2: manager.js — cablear la resolución de grupo en launchWorkItem + source-hygiene en manager.test.js</name>

  <read_first>
    - `src/session/manager.js:225-283` — el seam de `launchWorkItem`: `projects = loadProjects()` (`:243`), `resolveTaskAndLaunchContext` (`:244-251`), `projectPath` (`:258`), `getHost('cmux')` (`:276`), construcción de `workspaceName` (`:278-279`), la llamada `host._legacy.newWorkspace({ name, cwd: projectPath })` (`:280-283`)
    - `src/session/manager.js:285-354` — lo que viene DESPUÉS y NO se toca: `setColor`, `buildSessionFromTask`, `addSession`, `host._legacy.send`
    - `src/session/manager.js:311-313` — el precedente exacto del `console.log` de degradación de una línea (`worktree_skipped_nongit`)
    - `test/manager.test.js:9` (const `MANAGER_SOURCE_PATH`) y `:696-782` — los asserts regex-sobre-source a calcar (patrón `readFileSync` + `assert.ok(/regex/.test(source))`)
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-RESEARCH.md` §Code Examples (cableado canónico entre `:276` y `:283`) y §Validation Architecture (por qué el cableado se verifica por source-hygiene: `launchWorkItem` hace I/O real y no corre en test)
    - `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-CONTEXT.md` §decisions — D-11, D-12, D-13; §GRP-04
  </read_first>

  <files>src/session/manager.js, test/manager.test.js</files>

  <action>
    Cablear la resolución de grupo en `launchWorkItem`, entre `getHost('cmux')` (`:276`) y la construcción de
    `workspaceName` (`:278`). Código canónico en RESEARCH §Code Examples. Concretamente:

    1. Tras `const host = getHost('cmux')`, resolver el ref con la capa 1 englobante en try/catch:
       `const entry = projects[task.projectId];` (el mapa `projects` ya está cargado en `:243`).
       `let groupRef = null;` luego `try { const expectedName = deriveExpectedGroupName(task, entry, projectPath);`
       `const raw = await host._legacy.listWorkspaceGroups(); groupRef = resolveWorkspaceGroup(JSON.parse(raw), expectedName); }`
       `catch { console.log('[kodo] group_skipped — resolucion_fallo'); }`. Esta es la ÚNICA llamada cmux nueva
       por lanzamiento (D-12) y va SIEMPRE por `host._legacy` (nunca `import` de `cmux/client.js` — walker).
       El `console.log` lleva solo el motivo, sin contenido de usuario (D-11).
    2. Sustituir la llamada `host._legacy.newWorkspace({ name: workspaceName, cwd: projectPath })` (`:280-283`) por
       `const workspaceRef = await newWorkspaceWithGroupFallback(host._legacy.newWorkspace, { name: workspaceName, cwd: projectPath }, groupRef);`.
       Mantener `cwd: projectPath` LITERAL (invariante Phase 18 D-04 — el worktree lo materializa claude, no cambia).
    3. NO tocar nada de `:285` en adelante: `setColor`, `buildSessionFromTask` (que NO gana campos de grupo — GRP-04),
       `addSession`, `send`, `notify`. El `groupRef` se usa solo en el `newWorkspace` y se descarta; nunca se persiste.
       Alcance D-13: esto es solo `launchWorkItem` (sesiones de tarea); el orquestador (`orchestrator/launch.js`) no
       se toca.

    Añadir a `test/manager.test.js` asserts de source-hygiene nuevos (patrón `readFileSync(MANAGER_SOURCE_PATH)` +
    `assert.ok(/.../.test(source))`, reusando la const `MANAGER_SOURCE_PATH` de `:9`):
    - positivo: el source invoca `host._legacy.listWorkspaceGroups()` y usa `newWorkspaceWithGroupFallback(...)` con
      `host._legacy.newWorkspace` como primer argumento y `groupRef` como tercero.
    - positivo: se preserva `cwd: projectPath` en la construcción del `newWorkspace` (D-04 no regresiona).
    - negativo GRP-04 (membresía): el source de `manager.js` NO contiene ninguna invocación de verbo de gestión de
      grupos de cmux — de la familia workspace-group, solo `list` es admisible. Escribir el assert como un regex
      negativo que rechace los verbos de gestión (create / rename / delete / add / ungroup) precedidos por
      `workspace-group`; ver el patrón de regex negativo exacto en el analog `:778-781`. El regex vive SOLO en el
      test (`test/manager.test.js`), nunca como comentario en `manager.js`.
    - negativo GRP-04 (persistencia): `buildSessionFromTask` no gana ningún campo cuyo nombre contenga `group`
      (assert sobre el bloque de `buildSessionFromTask` en el source, o sobre el shape de Session).
    - estructural: `manager.js` no importa `cmux/client.js` (puede delegarse al walker existente
      `cmux-isolation.test.js`, que ya cubre `src/session/`; no duplicar si el walker basta).
  </action>

  <verify>
    <automated>node --test test/manager.test.js test/host/cmux-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/manager.test.js test/host/cmux-isolation.test.js` exits 0.
    - Source assertion: `src/session/manager.js` contiene una llamada `host._legacy.listWorkspaceGroups()` dentro de `launchWorkItem`, envuelta en `try/catch`.
    - Source assertion: `src/session/manager.js` invoca `newWorkspaceWithGroupFallback(host._legacy.newWorkspace, { ... cwd: projectPath }, groupRef)` — sustituye la llamada directa a `newWorkspace`.
    - Source assertion (GRP-04): un grep de verbos de gestión de grupos precedidos por `workspace-group` (create/rename/delete/add/ungroup) sobre `src/session/manager.js` da 0 coincidencias; solo `list` aparece.
    - Source assertion (GRP-04): `buildSessionFromTask` no introduce ningún campo cuyo nombre contenga `group`.
    - Behavior assertion: el walker `cmux-isolation.test.js` sigue verde (manager no importa `cmux/client.js`).
  </acceptance_criteria>

  <done>El launch de tareas resuelve y pasa `--group` con fail-open en dos capas; el cableado y las prohibiciones GRP-04 quedan blindados por source-hygiene, y nada de grupo se persiste.</done>
</task>

</tasks>

<unresolved_edge_probes>
## Unresolved edge probes (flagged assumptions — no silenciadas)

Dos filas del reporte de cobertura de bordes quedaron `unclassified` (el auto-clasificador no supo
categorizarlas); se dejan como suposiciones explícitas, cubiertas conductualmente por los requisitos:

- **GRP-02 (unclassified):** el probe genérico no aplica limpio. La conducta está fijada por D-01/D-02
  (derivación por path resuelto, compuesto obligatorio) y cubierta por los truths de `deriveExpectedGroupName`.
  Suposición residual `[ASSUMED A1]`: para GitHub el identifier = repo basename; el operador solo usa grupos
  Plane hoy, fail-open cubre cualquier no-match. Riesgo bajo.
- **GRP-04 (unclassified):** el probe genérico no aplica limpio. La conducta (no crear/renombrar/borrar,
  no persistir refs) está cubierta por el bloque `prohibitions` y por los asserts source-hygiene del Task 2.
  Sin suposición residual: es una restricción negativa verificable estáticamente.

Nota de operación (Pitfall 1, RESEARCH): con los datos reales de hoy, la aceptación de GRP-02 solo cubre
`KODO` y `SCRIBBA`. El grupo `SCP-CMRi` NO matchea el identifier `SCP` → las tareas de SCP se lanzan sin
grupo (fail-open correcto). Para agruparlas, el operador renombra el grupo a `SCP` — es acción de operador,
NO cambio de código. El plan no intenta "arreglarlo".
</unresolved_edge_probes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider remoto → kodo | `task.ref`/`task.title` vienen del provider (Plane/GitHub); alimentan la derivación del nombre y el log |
| cmux binary → kodo | el JSON de `workspace-group list` se parsea en `resolveWorkspaceGroup` (función pura defensiva) |
| kodo → cmux binary | el ref de grupo cruza como argumento de `new-workspace --group` (argv array, vía Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-02 | Tampering / DoS | `resolveWorkspaceGroup(JSON.parse(raw), ...)` (JSON malformado/hostil de cmux) | medium | mitigate | `JSON.parse` dentro del try/catch englobante (capa 1); `resolveWorkspaceGroup` es defensiva: `Array.isArray` guard + type-check por campo → `null`, never-throws (calca `normalizeSurface`). |
| T-77-03 | Information Disclosure | `console.log('[kodo] group_skipped ...')` | low | mitigate | D-11: el log lleva SOLO el ref/motivo (`group_skipped — <motivo>`), nunca `task.title` ni el nombre de grupo del operador con contenido libre. Verificado por el behavior assert del Task 1 (el `log` no contiene el título). |
| T-77-01 | Tampering | ref de grupo → `new-workspace --group` | high | mitigate | heredada de Plan 01: argv array vía `execFile`, sin shell; superficie de inyección nula. Aquí el ref proviene de `resolveWorkspaceGroup` (siempre un `workspace_group:N` de la lista de cmux, nunca input libre). |
| T-77-04 | Denial of Service | `host._legacy.listWorkspaceGroups()` en el launch | low | accept | control existente: `run()` timeout 15s → reject → capa 1. Máx 1 llamada extra por lanzamiento (~50ms), cero en reconcile (D-12). Aceptado: el coste marginal es conocido y acotado. |
</threat_model>

<verification>
- Tras cada task: `node --test <fichero tocado>` (<10s). Task 1 → `test/session/group-resolve.test.js`;
  Task 2 → `test/manager.test.js test/host/cmux-isolation.test.js`.
- Tras la wave: `npm test` verde (baseline ~2132 tests, 1 skipped). Rellenar el Per-Task Verification Map de
  `77-VALIDATION.md` y poner `nyquist_compliant: true` si el mapa queda completo.
- Flake conocido FUERA de alcance: `test/gsd-lock-race.test.js` «concurrent dead-holder steal (CR-01)» — si
  aparece, anotarlo y seguir, NO tocarlo.
- PROHIBIDO en tests: crear/borrar/renombrar grupos o workspaces contra la app cmux REAL del operador. Todo con
  DI/stubs/fixtures (el JSON real está capturado en RESEARCH §Pattern 2).
- Commits atómicos por task; `git add` de ficheros específicos (nunca `git add .`/`-A`).
</verification>

<success_criteria>
- Una tarea con grupo coincidente sale con `--group <ref>` resuelto en fresco por lanzamiento (GRP-01/GRP-02).
- Sin match / con la resolución fallando / con cmux viejo → la sesión se lanza exactamente como hoy; con el grupo
  borrado a mitad, UN reintento sin grupo la salva (GRP-03, fail-open en 2 capas).
- Cero refs `workspace_group:N` persistidos; cero verbos de gestión de grupos; `buildSessionFromTask` sin campos
  nuevos (GRP-04).
- Una sola llamada cmux extra por lanzamiento, cero en reconcile; `manager.js` sin import de `cmux/client.js` (D-12, walker).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Símbolos nuevos que esta fase crea (para excluirlos del source-grounding de drift review):

- `deriveExpectedGroupName(task, entry, resolvedPath)` — función pura exportada nueva en `src/session/manager.js`.
- `resolveWorkspaceGroup(groupsJson, expectedName)` — función pura exportada nueva en `src/session/manager.js`.
- `newWorkspaceWithGroupFallback(newWorkspaceFn, baseOpts, group, log)` — helper exportado nuevo en `src/session/manager.js`.
- Cableado nuevo en `launchWorkItem` (`src/session/manager.js`): resolución de `groupRef` + uso de `newWorkspaceWithGroupFallback`.
- `test/session/group-resolve.test.js` — fichero de test nuevo.
- Asserts source-hygiene nuevos en `test/manager.test.js` (fichero existente).

Consume (no crea) de Plan 01: `host._legacy.listWorkspaceGroups`, `--group` en `newWorkspace`.
</artifacts_produced>

<output>
Create `.planning/phases/77-agrupaci-n-de-workspaces-en-cmux/77-02-SUMMARY.md` when done
</output>
