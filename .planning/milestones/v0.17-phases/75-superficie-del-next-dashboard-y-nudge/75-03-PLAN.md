---
phase: 75-superficie-del-next-dashboard-y-nudge
plan: 03
type: execute
wave: 2
depends_on: [75-01]
files_modified:
  - src/session/handoff.js
  - src/cli/dashboard/markdown.js
  - src/cli/dashboard/plan.js
  - src/cli/dashboard/SessionTable.js
  - src/cli/dashboard/App.js
  - test/session/handoff.test.js
  - test/dashboard-markdown.test.js
  - test/dashboard-overlay.test.js
autonomous: true
requirements: [LIVE-06]

must_haves:
  truths:
    - "Desde la fila de una sesión no-GSD (phaseId == null), el operador abre el markdown completo del plan ligero renderizado y de SOLO LECTURA (no editable) con la tecla p existente; Esc vuelve a la lista preservando el cursor por task_id (LIVE-06, D-07, SC2)."
    - "El render markdown es line-based best-effort (headings bold/cyan, líneas **Label:** bold, bullets, code fences dim) — NO un parser CommonMark completo; solo aplica al carril light-plan, jamás a la rama GSD (D-05, RESEARCH Pitfall 3)."
    - "Las filas GSD siguen abriendo su overlay de plan GSD EXACTAMENTE igual que hoy: la rama GSD se pinta con <Text> plano byte-idéntico; el handoff no se surface en esa rama aunque esté en disco (D-02 LOCKED intacto, LIVE-06 SC3, test de no-regresión explícito)."
    - "El marcador HTML <!-- kodo:handoff … --> es INVISIBLE en el render del plan ligero — stripHandoffMarker lo elimina de los headings usando el conocimiento del contrato de handoff.js (dueño único), no una regex ad-hoc divergente (D-06, deuda de 74 D-01 saldada)."
    - "stripHandoffMarker es string-only (indexOf/slice, cero regex — anti-ReDoS T-74-09) y handoff.js conserva CERO imports tras añadirlo (guard test/check-isolation.test.js sigue verde)."
    - "Las líneas del plan ligero (contenido LLM) pasan por stripControlChars en el mini-renderer antes de proyectarse al terminal — neutraliza OSC-52/CSI/C1 (V5 Input Validation, T-75-02)."
    - "Con el plan ausente o ilegible, el overlay light-plan mantiene sus copias existentes (no-light-plan / error) intactas; el mini-renderer solo actúa sobre el estado 'ok' (SC5, UI-SPEC error)."
    - statement: "El overlay de plan ligero muy largo (handoffs acumulados) se pinta como snapshot sin scroll propio del mini-renderer; el scroll/paginación está fuera de scope (diferido v0.18, M21). El render best-effort (headings/labels/bullets/fences, NO CommonMark) es suficiente para el UAT de LIVE-06; si el UAT humano exige tablas/links/nested, es fricción a confirmar con el operador (RESEARCH A3, UI-SPEC long-text backstop)."
      verification: backstop
  artifacts:
    - "src/session/handoff.js — stripHandoffMarker exportada (string-only, cero imports)"
    - "src/cli/dashboard/markdown.js — nuevo módulo con el mini-renderer line-based renderMarkdownLines"
    - "src/cli/dashboard/plan.js — discriminante render ('markdown' en el carril light 'ok', 'plain' en GSD)"
    - "src/cli/dashboard/SessionTable.js — renderOverlay aplica el mini-renderer SOLO si snap.render === 'markdown'"
    - "src/cli/dashboard/App.js — el snapshot del overlay p threadea render: res.render"
    - "test/dashboard-markdown.test.js — nuevo (Wave 0), cubre el mini-renderer"
  key_links:
    - "readPlan (plan.js) fija render:'markdown' SOLO en la rama light 'ok' → App.js:1820 threadea render al snapshot → SessionTable renderOverlay ramifica: markdown → mini-renderer; plain/ausente (GSD) → <Text> plano byte-idéntico"
    - "El mini-renderer delega el strip del marcador en stripHandoffMarker de handoff.js (dueño único del contrato, no regex ad-hoc)"
  prohibitions:
    - statement: "NUNCA aplicar el mini-renderer a la rama GSD ni surface el handoff en el overlay GSD — rompe D-02 LOCKED y SC3; la rama GSD queda byte-idéntica (test de no-regresión explícito)."
      status: unverified
    - statement: "NUNCA strippear/parsear el marcador con una regex ad-hoc en el dashboard — el marcador tiene un solo dueño (handoff.js, D-06/D-13); stripHandoffMarker usa indexOf/slice (anti-ReDoS)."
      status: unverified
    - statement: "NUNCA hacer el plan editable desde la TUI — el overlay es read-only (snapshot congelado); esta fase no añade edición (REQUIREMENTS §Out of Scope)."
      status: unverified
---

<objective>
Superficie LIVE-06: desde la fila de una sesión no-GSD (`phaseId == null`), el operador abre el markdown
completo del plan ligero renderizado (headings, labels, bullets, code fences) y de solo lectura, con el
marcador `<!-- kodo:handoff … -->` invisible; `Esc` vuelve preservando el cursor. Las filas GSD siguen
abriendo su overlay GSD byte-idéntico (D-02 intacto).

Purpose: saldar las dos deudas que la Phase 74 dejó para esta fase — el render legible del plan ligero
(hoy `md.split('\n')` plano) y la promesa de invisibilidad del marcador — reutilizando el overlay `p`
existente, un mini-renderer in-house (cero deps) y `stripHandoffMarker` en el dueño único del contrato.

Output: `stripHandoffMarker` en handoff.js, el módulo `markdown.js` (nuevo), el discriminante `render` en
`readPlan`, el gate en `renderOverlay` y el threading del flag al snapshot en App.js.
</objective>

<artifacts_produced>
Símbolos nuevos/modificados que crea este plan (para el pase de source-grounding del review):
- `stripHandoffMarker(line)` — NUEVO export en `src/session/handoff.js` (string-only, cero imports).
- `renderMarkdownLines(lines)` (y el helper interno `renderMarkdownLine`) — NUEVO fichero `src/cli/dashboard/markdown.js`.
- Campo `render` ('markdown' | 'plain') en el PlanResult que devuelve `readPlan`/`readLightPlan` — `src/cli/dashboard/plan.js`.
- Rama por `snap.render === 'markdown'` en `renderOverlay` — `src/cli/dashboard/SessionTable.js`.
- `render: res.render` en el objeto `overlaySnapshot` del handler `p` — `src/cli/dashboard/App.js`.
- `test/dashboard-markdown.test.js` — NUEVO fichero de test (Wave 0).
</artifacts_produced>

<planner_assumptions>
Suposición flagged, sin resolver (edge LIVE-06 unclassified → surface, no drop): la fidelidad del render
markdown es best-effort line-based (headings/bold/bullets/fences), NO CommonMark. El UAT de
`/gsd-verify-work` confirma que es suficiente para LIVE-06; si el operador exige tablas/links/nested
rendering es fricción a confirmar (RESEARCH assumption A3, UI-SPEC ## UI Considerations backstop row).
Está codificada como el must_haves.truths backstop de este plan (verification: backstop → abstiene a
human_needed si el verificador no puede confirmarla).
</planner_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-CONTEXT.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-RESEARCH.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-PATTERNS.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-UI-SPEC.md
@src/session/handoff.js
@src/cli/dashboard/plan.js
@src/cli/dashboard/SessionTable.js
@src/cli/dashboard/App.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stripHandoffMarker en handoff.js (hoja pura, string-only)</name>
  <files>src/session/handoff.js, test/session/handoff.test.js</files>
  <read_first>
    - src/session/handoff.js (MARKER_OPEN :30 = '<!-- kodo:handoff', MARKER_CLOSE :32 = '-->'; el estilo string-only del módulo, docstring de cero imports; buildHandoffBlock :159-163 donde se compone el marcador; findSessionBlock :200-213 que usa indexOf/slice sobre el marcador — el patrón a replicar)
    - test/check-isolation.test.js (:160-175 — el guard que exige que handoff.js tenga CERO imports «para que Phase 75 pueda importar su parser desde el leaf del dashboard sin arrastrar el grafo»)
    - test/session/handoff.test.js (estructura de describe/it existente para añadir el bloque de stripHandoffMarker)
  </read_first>
  <behavior>
    - stripHandoffMarker('## Handoff 2026-07-17 <!-- kodo:handoff v=1 session=abc author=auto at=... -->') → '## Handoff 2026-07-17' (marcador eliminado, trimEnd).
    - stripHandoffMarker de una línea SIN marcador → la línea intacta (idéntica).
    - stripHandoffMarker de un marcador sin cerrar (open sin close) → la línea intacta (conservador, no toca).
    - stripHandoffMarker(no-string) → '' (never-throws).
    - Cero regex: solo indexOf/slice (anti-ReDoS). handoff.js conserva CERO imports.
  </behavior>
  <action>
    Añadir `export function stripHandoffMarker(line)` en `handoff.js`, junto a los constantes MARKER_OPEN/
    MARKER_CLOSE existentes (:30-32). Implementar SOLO con `indexOf`/`slice` (cero regex — anti-ReDoS
    T-74-09; espejo del estilo de findSessionBlock :210-213): si `line` no es string → devolver ''; buscar
    MARKER_OPEN con indexOf; si no está → devolver line intacta; localizar MARKER_CLOSE en el resto; si no
    está (marcador sin cerrar) → devolver line intacta (conservador); si está → devolver `(line.slice(0,
    open) + after).trimEnd()`. EXPORTAR el helper, NO los constantes crudos (encapsula el formato, D-06/
    D-13 — dueño único, RESEARCH Pitfall 2). El módulo DEBE seguir con CERO imports (ni node: builtins) —
    string-only puro. Extender `test/session/handoff.test.js` con un describe para stripHandoffMarker
    cubriendo los 5 casos de behavior. Escribir los asserts primero (fallan por símbolo ausente).
  </action>
  <verify>
    <automated>node --test test/session/handoff.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/session/handoff.test.js` pasa con los 5 casos de stripHandoffMarker.
    - `node --test test/check-isolation.test.js` sigue verde (handoff.js conserva CERO imports).
    - `grep -n "export function stripHandoffMarker" src/session/handoff.js` devuelve una línea.
    - `grep -n "indexOf" src/session/handoff.js` muestra que stripHandoffMarker localiza el marcador con indexOf/slice (no regex); la garantía de hoja pura (cero imports, sin regex arrastrada) la fuerza test/check-isolation.test.js — ya en el verify.
  </acceptance_criteria>
  <done>stripHandoffMarker elimina el marcador con indexOf/slice, es never-throws, exporta el helper (no los constantes) y handoff.js sigue siendo una hoja de cero imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mini-renderer markdown line-based (nuevo módulo markdown.js)</name>
  <files>src/cli/dashboard/markdown.js, test/dashboard-markdown.test.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js (renderOverlay :177-243 — cómo se pintan hoy las líneas del body 'ok' con `h(Text, {key}, line)`; el estilo de color por nombre ink)
    - src/cli/dashboard/format.js (docstring de color-isolation :205-209 — color SOLO vía props ink, cero picocolors/ANSI; el guard test/format-isolation.test.js escanea src/cli/dashboard/**)
    - src/session/handoff.js (stripHandoffMarker — creado en Task 1; se importa aquí)
    - src/cli/format.js (stripControlChars :80 — el saneo a aplicar por línea)
    - src/cli/dashboard/App.js (:72 — el patrón de import de stripControlChars desde '../format.js')
  </read_first>
  <behavior>
    - renderMarkdownLines(lines) → un array de ReactElement (<Text>), uno por línea, en el MISMO orden y con keys estables por índice absoluto.
    - Línea '# ...' o '## ...' → <Text bold color='cyan'> con el marcador handoff strippeado (stripHandoffMarker).
    - Línea que empieza por '**' (p. ej. **Label:**) → <Text bold>.
    - Línea que empieza por '- ' o '* ' → <Text> plano (bullet best-effort).
    - Líneas dentro de un bloque de code fence (entre delimitadores ```), incluida la línea delimitadora → <Text dimColor>.
    - Cualquier otra línea → <Text> plano.
    - Cada línea se sanea con stripControlChars antes de renderizarse (V5 / T-75-02).
    - El toggle de fence se acumula recorriendo las líneas desde 0 (estado correcto aunque luego se corte por scroll).
    - Color EXCLUSIVAMENTE por props ink (bold/dimColor/color) — cero picocolors, cero ANSI inline.
  </behavior>
  <action>
    Crear `src/cli/dashboard/markdown.js` que exporte `renderMarkdownLines(lines)` devolviendo
    `ReactElement[]` (uno por línea). Importar `Text` de 'ink', `createElement as h` de 'react',
    `stripHandoffMarker` de '../../session/handoff.js' y `stripControlChars` de '../format.js'. Recorrer las
    líneas con un acumulador local `inFence` (bool) que alterna cuando una línea (tras trim) empieza por la
    triple comilla invertida; la línea delimitadora y las de dentro del fence se pintan `dimColor`.
    Para cada línea: sanear con `stripControlChars`, luego mapear a estilo ink best-effort: heading (`# `/
    `## `) → `{ bold:true, color:'cyan' }` aplicando `stripHandoffMarker` al texto (D-06); `**` inicial →
    `{ bold:true }`; bullet (`- `/`* `) → sin atributo; resto → sin atributo. Key = índice absoluto. NO
    CommonMark (best-effort, D-05). Color SOLO por props ink (color-isolation; el guard format-isolation
    escanea este directorio) — jamás picocolors ni ANSI.
    Crear `test/dashboard-markdown.test.js` (Wave 0) verificando, sobre el árbol de elementos devuelto
    (props de cada <Text>): heading bold+cyan y con el marcador strippeado; **Label:** bold; bullet plano;
    líneas de fence dimColor con el toggle correcto; saneo de una línea con caracteres de control; longitud
    del array == número de líneas. Escribir los asserts primero (fallan por módulo ausente).
  </action>
  <verify>
    <automated>node --test test/dashboard-markdown.test.js test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-markdown.test.js` pasa (heading strippeado+bold+cyan, label bold, bullet plano, fence dim con toggle, saneo, longitud del array).
    - `node --test test/format-isolation.test.js` sigue verde — es EL guard de color-isolation que escanea los imports de src/cli/dashboard/** (garantiza que markdown.js no importa el color raw; el color sale de props ink).
    - `grep -n "stripHandoffMarker\|stripControlChars" src/cli/dashboard/markdown.js` muestra ambos imports/usos.
    - `grep -n "from 'ink'" src/cli/dashboard/markdown.js` muestra que el color se aplica vía Text de ink (props color/bold/dimColor).
  </acceptance_criteria>
  <done>renderMarkdownLines mapea líneas a <Text> con estilo best-effort, strippea el marcador en headings, sanea el contenido LLM, mantiene el toggle de fence y usa color solo por props ink.</done>
</task>

<task type="auto">
  <name>Task 3: Discriminante render + gate del mini-renderer en el overlay (plan.js, SessionTable.js, App.js)</name>
  <files>src/cli/dashboard/plan.js, src/cli/dashboard/SessionTable.js, src/cli/dashboard/App.js, test/dashboard-overlay.test.js</files>
  <read_first>
    - src/cli/dashboard/plan.js (readLightPlan :65-78 — la rama light 'ok' que debe tagearse render:'markdown'; readPlan :91-189 — las ramas GSD 'ok' :188 que deben tagearse render:'plain'; los estados no-'ok' no necesitan render)
    - src/cli/dashboard/SessionTable.js (renderOverlay :177-243 — el body 'ok' :198-205 donde ramificar por snap.render; la firma del snapshot :172; renderOverlay se invoca en :852)
    - src/cli/dashboard/markdown.js (renderMarkdownLines — creado en Task 2)
    - src/cli/dashboard/App.js (handler p :1805-1824 — donde se construye el snapshot con setOverlaySnapshot :1820; readPlan se llama :1818)
    - test/dashboard-overlay.test.js (los tests de overlay existentes — patrón render(App)+stdin para simular la tecla p; base para el test de no-regresión GSD y el de render light)
  </read_first>
  <action>
    En `plan.js`: añadir un campo `render` al PlanResult. En la rama del plan ligero, cuando `readLightPlan`
    devuelve status 'ok', el resultado debe llevar `render: 'markdown'` (el carril light es el ÚNICO que se
    renderiza — D-07/Pitfall 3). En las ramas GSD (el `return { status:'ok', lines }` final :188 y demás),
    llevar `render: 'plain'`. Los estados no-'ok' (no-phase/no-plan/no-light-plan/error) pueden llevar
    'plain' o dejarlo ausente — solo el body 'ok' consume el flag. Actualizar el typedef PlanResult
    (:48) con el campo `render`. NO cambiar la lógica de prioridad GSD (D-02 LOCKED).
    En `App.js` handler `p` (:1820): añadir `render: res.render` al objeto pasado a `setOverlaySnapshot`
    (verbatim del resultado de readPlan). NO tocar nada más del handler (mode:'overlay', cursor por task_id,
    scrollOffset 0 se preservan).
    En `SessionTable.js` `renderOverlay` (:198-205, body 'ok'): ramificar por `snap.render`. Cuando
    `snap.render === 'markdown'`, renderizar el body con el mini-renderer: `const elements =
    renderMarkdownLines(snap.lines)` y luego slicear ESOS elementos por scrollOffset contra OVERLAY_VIEWPORT
    (para preservar el estado de fence — renderMarkdownLines recorre desde 0 y devuelve el array completo;
    se slicea el array de elementos, no las líneas). Cuando `snap.render !== 'markdown'` (GSD, 'plain', o
    ausente) → el body queda EXACTAMENTE como hoy (`snap.lines.slice(...).map((line,i) => h(Text,{key},
    line))`) — byte-idéntico (SC3, D-02). Importar renderMarkdownLines de './markdown.js'.
    Extender `test/dashboard-overlay.test.js`: (a) NO-REGRESIÓN GSD explícita (SC3) — abrir el overlay de
    plan de una fila GSD produce el mismo frame que antes del mini-renderer (render:'plain' → <Text> plano);
    (b) abrir el overlay de una fila light (phaseId==null) con un plan que contiene un heading con marcador
    → el frame muestra el heading SIN el marcador y con el resto de líneas; (c) Esc vuelve preservando el
    cursor por task_id (mecánica existente, sin regresión).
  </action>
  <verify>
    <automated>node --test test/dashboard-overlay.test.js test/dashboard-plan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-overlay.test.js` pasa: no-regresión GSD (frame byte-idéntico), render light con marcador invisible, Esc preserva cursor.
    - `node --test test/dashboard-plan.test.js` pasa: readLightPlan 'ok' lleva render:'markdown', las ramas GSD 'ok' llevan render:'plain'.
    - `grep -n "render:" src/cli/dashboard/plan.js` muestra el flag en las ramas; `grep -n "snap.render\|renderMarkdownLines" src/cli/dashboard/SessionTable.js` muestra el gate; `grep -n "render: res.render" src/cli/dashboard/App.js` muestra el threading.
    - `git diff --stat src/server.js` VACÍO (cero endpoints nuevos).
    - `node --test test/format-isolation.test.js` verde.
    - `npm test` verde salvo el flaky preexistente CR-01.
  </acceptance_criteria>
  <done>El overlay del plan ligero se renderiza con el mini-renderer (marcador invisible) solo en la rama phaseId==null; la rama GSD queda byte-idéntica; Esc preserva el cursor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.kodo/plans/<task_id>.md (contenido LLM) → mini-renderer → terminal | Las líneas del plan son contenido escrito por un LLM; cruzan al render del terminal. |
| snapshot del overlay (kind:'plan') → renderOverlay | El discriminante render decide si aplica el mini-renderer (light) o el <Text> plano (GSD). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-02 | Tampering | renderMarkdownLines (markdown.js) | high | mitigate | Cada línea del plan (contenido LLM) pasa por `stripControlChars` antes de proyectarse — neutraliza OSC-52/CSI/C1 (terminal escape injection). |
| T-75-03 | Denial of Service | stripHandoffMarker (handoff.js) | medium | mitigate | indexOf/slice, cero regex — sin vector ReDoS (T-74-09); guard test/check-isolation cero imports. |
| T-75-08 | Tampering | gate render en renderOverlay | medium | mitigate | El mini-renderer se aplica SOLO cuando snap.render==='markdown' (carril light); la rama GSD queda byte-idéntica y el handoff no se surface ahí (D-02 LOCKED, SC3). |
| T-75-09 | Denial of Service | overlay light-plan / cadena de render | medium | mitigate | never-throws: readLightPlan colapsa ENOENT→no-light-plan / error; el mini-renderer no lanza sobre líneas arbitrarias (SC5). |
| T-75-SC | Tampering | instalaciones npm/pip/cargo | low | accept | Esta fase instala CERO paquetes (el mini-renderer es in-house para no añadir marked/ink-markdown) — sin superficie supply-chain. |
</threat_model>

<verification>
- `node --test test/session/handoff.test.js test/check-isolation.test.js test/dashboard-markdown.test.js test/dashboard-overlay.test.js test/dashboard-plan.test.js` — verde.
- `node --test test/format-isolation.test.js` — verde (color-isolation con markdown.js).
- Test de NO-REGRESIÓN GSD explícito verde: el overlay de plan GSD produce frame byte-idéntico (SC3).
- `git diff --stat src/server.js package.json package-lock.json` — VACÍO.
- `npm test` — verde salvo el flaky preexistente CR-01.
</verification>

<success_criteria>
- Desde una fila no-GSD el operador abre el plan ligero renderizado y read-only; Esc preserva el cursor (LIVE-06 / SC2).
- Las filas GSD abren su overlay GSD byte-idéntico; el handoff no se surface ahí (D-02 intacto, SC3).
- El marcador kodo:handoff es invisible en el render (D-06, deuda de 74 saldada).
- El contenido LLM del plan se sanea antes de renderizarse (T-75-02); handoff.js sigue siendo hoja de cero imports.
</success_criteria>

<output>
Crear `.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-03-SUMMARY.md` al terminar.
</output>
