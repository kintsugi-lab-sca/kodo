---
phase: 74-handoff-acumulativo-al-cierre
plan: 04
type: execute
wave: 2
depends_on: ["74-01", "74-02"]
files_modified:
  - src/hooks/session-end.js
  - test/hooks/session-end-handoff.test.js
  - test/hooks/session-end.test.js
autonomous: true
requirements: [LIVE-01, LIVE-03, LIVE-04]

must_haves:
  truths:
    - "Al cerrar una sesión, `~/.kodo/plans/<task_id>.md` gana un bloque `## Handoff <fecha>` — y el operador lo encuentra ahí al abrir el fichero (LIVE-01, SC#1)"
    - "El bloque aterriza ANTES del cleanup terminal destructivo (removeSession + worktree + promptFile) — orden observable (LIVE-01, SC#1)"
    - "Si el LLM no escribió handoff, el fichero gana igualmente un bloque mecánico distinguible con el sufijo `— automático` y sin `NEXT:` (LIVE-03, SC#3)"
    - "Si el plan no existe, se crea con cabecera mínima y se appendea: el handoff es universal, cubre también las ramas GSD full y bootstrap (D-09)"
    - "Tras el cierre, `state.json` tiene el puntero al plan y el `NEXT:` de una línea (LIVE-04)"
    - "Un handoff que falla (plan ilegible, lock ocupado) no crashea Claude Code, no bloquea el cierre y no altera `backstop → setColor → notify` (SC#5)"
  artifacts:
    - src/hooks/session-end.js
    - test/hooks/session-end-handoff.test.js
  key_links:
    - "El seam es `session-end.js:97`: tras la construcción del `log` (:88-96, que D-06 necesita) y ANTES de runReviewBackstop (:104)"
    - "`fn` de withFileLock debe ser 100% SÍNCRONO — `:227` no espera una Promise y el finally liberaría el lock antes de que la escritura aterrice"
    - "El try/catch propio es estructural, no cosmético: withFileLock no tiene catch y acquireLock rethrow todo lo que no sea EEXIST"
    - "`plansDir`/`stateWriterFn` inyectables — sin ellos, `test/hooks/session-end.test.js` (que no aísla HOME) escribiría en el `~/.kodo` REAL del operador"
---

<objective>
Insertar el bloque de handoff en `runSessionEndHook` (D-07): leer el plan, decidir autoría (D-04),
appendear el bloque mecánico si hace falta (D-03), crear el fichero si no existe (D-09), todo bajo
`withFileLock` sobre `<plan>.md.lock` (D-08), y persistir el puntero + el `NEXT:` en `state.tasks`
(D-05/D-06).

Purpose: éste es el plan que hace verdadero el criterio de éxito 1 del ROADMAP y el invariante de
STATE.md — *«el handoff se escribe en disco para TODA sesión»*. Los Planes 01-03 preparan el contrato,
la persistencia y la instrucción optimista; aquí es donde el dato aterriza.

**El hook queda como I/O + orquestación** (D-13): el formato entero vive en `src/session/handoff.js`,
que es una hoja pura. Meter el I/O ahí lo degradaría de hoja a nodo con fs y rompería a la Phase 75
(RESEARCH §Pitfall 1).

Output: `writeHandoff` exportado + el seam cableado en `:97` + su suite DI + el cierre de una fuga de
HOME en la suite preexistente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-PATTERNS.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-01-SUMMARY.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Símbolos y ficheros NUEVOS creados por la Phase 74 (ninguno existe hoy — no son deriva):

**Plan 01** — `src/session/handoff.js`, exports: `HANDOFF_REASONS`, `normalizeReason`,
`sanitizeInline`, `isSafeTaskId`, `buildPlanHeader`, `buildHandoffBlock`, `findSessionBlock`,
`hasSessionHandoff`, `extractNext`. Fichero nuevo: `test/session/handoff.test.js`.

**Plan 02** — `upsertTaskHandoff` en `src/session/state.js`; clave `tasks?` en el typedef `State`.
Fichero nuevo: `test/state/handoff-state.test.js`.

**Plan 03** — sin símbolos nuevos (dos strings de instrucción en `src/hooks/session-start.js`).

**Plan 04 (este plan)** — `writeHandoff`, export nuevo de `src/hooks/session-end.js`. Claves nuevas en
el shape `deps` de `runSessionEndHook`: `plansDir`, `fs`, `stateWriterFn`, `now`. Fichero nuevo:
`test/hooks/session-end-handoff.test.js`.

**Plan 05** — modo `--kind handoff` en `test/helpers/lock-race-child.mjs`. Fichero nuevo:
`test/state/handoff-concurrency.test.js`.

**Formato del marcador de handoff (D-01)** — heading + marcador HTML en la MISMA línea:
`## Handoff <YYYY-MM-DD HH:MM> <!-- kodo:handoff v=1 session=<session_id> author=<llm|auto> at=<ISO-8601 UTC> -->`
El bloque mecánico añade el sufijo ` — automático` al heading, antes del marcador.

**Shape de `state.tasks` (D-05)** — `state.tasks[task_id] = { plan_path: string, next: string|null, updated_at: string }`.

**Fichero nuevo en disco** — `~/.kodo/plans/<task_id>.md.lock`, el lockfile advisory de D-08.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `writeHandoff` — RMW del plan bajo withFileLock + persistencia</name>

  <read_first>
    - `src/hooks/session-end.js:85-129` — el seam (`:97`) y el bloque backstop (`:98-129`), que es el analog LITERAL de D-07: comentario que cita la decisión + try/catch propio además del outer never-throws
    - `src/hooks/session-end.js:47-63` — el shape `deps` de `runSessionEndHook` y el patrón `deps.X || <default real>`
    - `src/session/state-lock.js:201-231` — `withFileLock`: `try { return {ok:true, value: fn()} } finally { releaseLock(...) }` — **sin catch**; y `:73`/`:81` (`acquireLock`): `mkdirSync` puede lanzar y todo error que no sea EEXIST se re-lanza
    - `src/session/state.js:266-281` (`saveState`) — el patrón de tmp+rename con nombre ÚNICO por escritor (pid + randomUUID, fix WR-02): el que se copia
    - `src/config.js:135-146` (`writeFileAtomic`) — la TRAMPA: tmp de nombre FIJO. Leerlo para entender por qué NO se usa
    - `src/session/handoff.js` — el contrato del Plan 01
    - `src/cli/dashboard/plan.js:65-78` — `readLightPlan`, el consumidor aguas abajo: la ruta debe ser byte-idéntica a la suya
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md` §Pitfall 3 y §Pitfall 4
  </read_first>

  <files>src/hooks/session-end.js, test/hooks/session-end-handoff.test.js</files>

  <behavior>
    - Plan ausente (D-09): `writeHandoff` crea `<plansDir>/<task_id>.md` con la cabecera mínima de `buildPlanHeader` seguida del bloque mecánico. El fichero existe tras la llamada.
    - Plan existente sin marcador de esta sesión (LIVE-03): el fichero gana el bloque mecánico appendeado al final; el contenido previo queda **íntegro byte a byte** (el prefijo del fichero resultante es idéntico al contenido original).
    - Plan existente CON marcador de esta sesión (D-04): el fichero NO cambia — el LLM ya escribió, no se appendea nada. El contenido tras la llamada es idéntico byte a byte al de antes.
    - **Caso crítico D-04 / LIVE-02+LIVE-03 combinados:** plan con el bloque de una sesión ANTERIOR + esta sesión sin escribir → SÍ se appendea el mecánico, y el fichero acaba con DOS bloques. El detector es scoped, no por conteo.
    - Acumulación (LIVE-02): dos llamadas secuenciales con `session_id` distintos dejan DOS bloques y el primero íntegro.
    - Persistencia (LIVE-04): tras la llamada, el `stateWriterFn` inyectado recibió `(task_id, {plan_path, next, updated_at})` con `plan_path` apuntando al fichero escrito.
    - Cuando se appendea el mecánico, el `next` que llega al `stateWriterFn` es `null` (el bloque mecánico no tiene `NEXT:`, D-03).
    - Cuando el LLM ya escribió con un `**NEXT:**`, el `next` que llega al `stateWriterFn` es ese valor extraído y truncado a 200 (D-02).
    - Guard de contención (T-74-01): con `task_id` = `'../../evil'`, NO se crea ningún fichero en ningún sitio, se emite un `warn`, y `stateWriterFn` NO se llama.
    - SC#5 — plan ilegible: con un `fs` stub cuyo `readFileSync` lanza un error con `code: 'EACCES'`, `writeHandoff` propaga (el caller lo captura); el test asserta que el hook completo (Task 2) sobrevive.
    - SC#5 — lock ocupado: con el lockfile ya presente y un pid vivo, `writeHandoff` emite un `warn`, NO llama al `stateWriterFn`, y NO lanza.
    - Determinismo: con un `now` fijo inyectado, dos ejecuciones sobre el mismo estado inicial producen ficheros byte-idénticos.
    - Higiene del tmp: tras una escritura correcta no queda ningún fichero `.tmp.` en `plansDir`.
  </behavior>

  <action>
    Añadir a `src/hooks/session-end.js` el export `writeHandoff({ session, input, log }, deps = {})`.

    **Declararlo como función SÍNCRONA** — sin la palabra clave de función asíncrona y sin esperas
    dentro. No es estilo: `withFileLock` ejecuta `fn()` dentro de un `try/finally` que libera el lock
    en el `finally` (`state-lock.js:226-230`) y **no distingue una Promise**. Un `fn` asíncrono
    devolvería `{ok:true, value: Promise}` y el lock se liberaría **antes de que la escritura aterrice**
    — la sección crítica no protegería nada y el *lost update* que D-08 existe para evitar volvería
    (RESEARCH §Pitfall 4). Precedente idéntico ya comentado en el repo: `reconcile.js:357-359` y el
    `sleepSync` con `Atomics.wait` de `state-lock.js:39-48`.

    Resolver los deps al principio con el patrón del fichero (`deps.X || <default real>`):
    `plansDir` (default `join(KODO_DIR, 'plans')`, importando `KODO_DIR` de `../config.js` como ya hace
    `session-start.js`), `fs` (default: el namespace de `node:fs`), `stateWriterFn` (default:
    `upsertTaskHandoff` del Plan 02, importado de `../session/state.js`, de donde el fichero ya importa)
    y `now` (default `() => new Date()`).

    **Por qué `plansDir`/`stateWriterFn`/`fs` son inyectables y no opcional-de-lujo (RESEARCH §Pitfall 6):**
    `test/hooks/session-end.test.js` **no aísla HOME** — no lo necesita hoy porque inyecta
    `findSessionFn`/`removeSessionFn` y nunca toca el fs. Con el handoff dentro, sin DI, esa suite
    escribiría en el `~/.kodo/plans` y el `state.json` **REALES del operador** cada vez que se corre
    `npm test`. El Task 3 cierra esa fuga; la DI es lo que la hace cerrable.

    Flujo, en este orden:
    1. Guard de contención: si `isSafeTaskId(session.task_id)` es falso → `log.warn` con
       `{task_id}` y **return**. Nada de fichero, nada de state. Mitigación T-74-01: D-09 hace del hook
       un ESCRITOR, así que este guard impide CREAR ficheros fuera de `~/.kodo/plans/`.
    2. Construir la ruta: `join(plansDir, taskId + '.md')`. **Construida, jamás derivada** — byte-idéntica
       a la del productor (`session-start.js`) y a la del consumidor (`plan.js:69`). El lock va en
       `<planPath>.lock` (es decir, `<task_id>.md.lock`).
    3. Asegurar que `plansDir` existe (recursive), **fuera** de la sección crítica.
    4. `withFileLock(lockPath, fn, { logger: log })` — pasar el logger inyectado en `opts` para que el
       `lock.timeout` salga por él y no por `console.warn` (`state-lock.js:449-453`).
       Dentro de `fn`, todo síncrono:
       a. Leer el plan si existe; si no, partir de `buildPlanHeader({taskRef: session.task_ref, summary: session.summary})` (D-09).
       b. `findSessionBlock(md, session.session_id)` (D-04). Si devuelve un bloque → el LLM ya escribió:
          **no appendear nada, no escribir el fichero**, devolver `{ planPath, next: extractNext(block) }`.
       c. Si no: componer el markdown appendeando `buildHandoffBlock({sessionId: session.session_id, reason: input.reason, status: session.status, at: now()})` al final, con una línea en blanco de
          separación y asegurando que el contenido previo queda **íntegro** (nunca reescribirlo).
       d. Escribir con tmp+rename copiando el patrón de `saveState:266-281`: nombre de tmp **único por
          escritor** (`planPath + '.tmp.' + process.pid + '.' + randomUUID()`), `writeFileSync` → `renameSync`,
          y en el catch limpiar el tmp perdido con `rmSync({force:true})` antes de re-lanzar.
          **NO usar `writeFileAtomic` de `config.js:136`**: usa un tmp de nombre FIJO (`path + '.tmp'`),
          exactamente lo que el fix WR-02 corrigió en `saveState` porque dos escritores concurrentes lo
          comparten y se pisan bytes parciales; bajo el lock sería seguro, pero el lock es **robable tras
          el TTL de 10 s** (`state-lock.js:36`), así que la garantía no es absoluta. Y además importarlo
          acoplaría a `config.js`.
          Devolver `{ planPath, next: null }`.
    5. Si `withFileLock` devuelve `{ok:false}` → `log.warn` con `{task_id, reason}` y **return** sin
       escribir state (D-06: el lock-timeout jamás bloquea el cierre).
    6. Llamar al `stateWriterFn(taskId, { plan_path, next, updated_at: now().toISOString() }, log)` (D-05).

    **Campos de log: solo `{task_id, reason}`.** Nunca el `next` ni el `summary` — los logs no llevan
    contenido de usuario (precedente T-71-18). Mitigación T-74-08.

    **OQ2 del research, ya resuelta — no re-litigar.** La pregunta era si el `<plan>.md.lock` nuevo
    confunde a `detectHungLocks` de `doctor.js`. Verificado en código: `detectHungLocks`
    (`doctor.js:341-356`) itera `collectLockProjects` (los `project_path` de `state.sessions`/`history`)
    y lee `join(projectPath, LOCK_FILE)` con `LOCK_FILE = '.planning/.kodo.lock'` (`gsd/lock.js:53`).
    **Solo escanea locks GSD dentro de los directorios de proyecto; jamás `~/.kodo/plans/`.** No hay
    nada que cambiar en `doctor.js` y no hace falta un task para ello.

    Escribir primero los casos de `<behavior>` en `test/hooks/session-end-handoff.test.js` (fichero
    nuevo), verlos fallar, y luego implementar. Para los casos de fichero real usar un `plansDir`
    apuntando a un `mkdtempSync` — **jamás el HOME real**; para el caso EACCES usar un `fs` stub que
    lanza.
  </action>

  <verify>
    <automated>node --test test/hooks/session-end-handoff.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/hooks/session-end-handoff.test.js` exits 0 y cubre todos los casos de `<behavior>`.
    - Behavior assertion: `writeHandoff.constructor.name` es `'Function'` y no `'AsyncFunction'` — la función es síncrona, que es lo que hace que la sección crítica de `withFileLock` proteja algo (RESEARCH §Pitfall 4).
    - Source assertion: `grep -v '^\s*//' src/hooks/session-end.js | grep -c 'writeFileAtomic'` == 0 — se copia el patrón de `saveState`, no la trampa del tmp fijo. El filtro de comentarios es deliberado: el nombre de la trampa puede citarse en un comentario que justifique por qué NO se usa, pero no puede aparecer en código.
    <!-- planner-discipline-allow: writeFileAtomic -->
    - Source assertion: el único símbolo importado de `../config.js` es `KODO_DIR` — `grep -n "from '../config.js'" src/hooks/session-end.js` no muestra ningún otro import.
    - Source assertion: `grep -c 'export function writeHandoff' src/hooks/session-end.js` == 1.
    - Source assertion: `grep -v '^\s*//' src/hooks/session-end.js | grep -c "'.tmp.' + process.pid"` >= 1 (tmp único por escritor, patrón WR-02).
    - Behavior assertion: con `task_id` = `'../../evil'`, `readdirSync(plansDir)` sigue vacío, no existe ningún fichero fuera de `plansDir`, y el `stateWriterFn` no se llamó (T-74-01).
    - Behavior assertion: en el caso «marcador de esta sesión presente», el contenido del fichero antes y después es idéntico byte a byte.
    - Behavior assertion: en el caso «bloque de sesión anterior presente», el fichero acaba con dos bloques y el primero íntegro (D-04).
    - CLI: `ls ~/.kodo/plans/ 2>/dev/null | wc -l` no cambia al correr el test — el HOME real intacto.
  </acceptance_criteria>

  <done>`writeHandoff` existe, es síncrona, hace el RMW bajo `withFileLock` con tmp+rename de nombre único, decide autoría con el detector scoped de D-04, crea el plan si falta, y persiste el puntero + `NEXT:`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear el seam `:97` con try/catch propio y probar el orden de efectos</name>

  <read_first>
    - `src/hooks/session-end.js:85-104` — el seam EXACTO: `session` ya desestructurada en `:85`, `log` construido en `:88-96`, `runReviewBackstop` en `:104`. El hueco es la línea 97
    - `src/hooks/session-end.js:98-129` — el bloque backstop: la forma del comentario + try/catch propio a copiar verbatim
    - `src/hooks/session-end.js:159` (`performTerminalCleanup`) y `:175`/`:184`/`:193` (setColor/notify/nudge) — lo que va DESPUÉS y no se reordena
    - `src/hooks/terminal-cleanup.js` — la secuencia destructiva: worktree → removePromptFile → removeSession
    - `test/hooks/session-end.test.js:16-62` — `makeLogger`, `makeCmuxStub` (con su array `calls`) y `makeSession`: los tres helpers a copiar
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md` §decisions — D-07 y su corrección post-research
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md` §Pitfall 3
  </read_first>

  <files>src/hooks/session-end.js, test/hooks/session-end-handoff.test.js</files>

  <behavior>
    - Cierre completo (LIVE-01): tras `runSessionEndHook` con `plansDir` apuntando a un tmpdir, el fichero del plan existe y contiene un bloque `## Handoff`.
    - **Orden observable (LIVE-01, SC#1):** en un array `calls` compartido por todos los stubs, el índice de la escritura del handoff es MENOR que el de `removeSession`. El handoff aterriza antes del cleanup destructivo.
    - **Orden observable (SC#5):** en ese mismo array, `removeSession` va antes de `setColor`, y `setColor` antes de `notify` — el trío LOCKED sigue en su sitio tras la inserción.
    - SC#5 — plan ilegible: con un `fs` stub cuyo `readFileSync` lanza `EACCES`, `runSessionEndHook` **completa sin lanzar**; `removeSession`, `setColor` y `notify` se llamaron igualmente; se registró un mensaje de error de handoff.
    - SC#5 — lock ocupado: con el lockfile presente y un pid vivo, `runSessionEndHook` completa sin lanzar, se registró un `warn`, y el cleanup + el trío cosmético ocurrieron igualmente.
    - D-09 universal: una sesión GSD (`gsd: true`, sin plan previo en disco) también gana su fichero de plan y su bloque — el handoff no depende de la rama que produjo (o no) el plan.
    - Guard de idempotencia intacto: con `findSessionFn` devolviendo `null`, o con `source: 'history'`, NO se escribe ningún handoff (el bloque va después de los guards).
    - `stateWriterFn` inyectado recibe la entrada de `state.tasks` exactamente una vez por cierre.
  </behavior>

  <action>
    Insertar la llamada a `writeHandoff` en `runSessionEndHook`, en el hueco de la **línea 97**: tras la
    construcción del `log` (`:88-96`) y antes del bloque `// ── Review backstop` (`:98`).

    **Por qué exactamente ahí y no «tras los guards» (corrección post-research de D-07):** el bloque
    necesita `log` para el `warn` del lock-timeout de D-06, y `log` no existe hasta `:96`. Sigue estando
    después de los guards de idempotencia (`:72-83`) y antes del backstop, así que el razonamiento de
    D-07 se mantiene intacto: el handoff es una escritura a **disco** (barata, sin red) y es el dato más
    valioso de la fase — si el backstop se atasca en red, el handoff ya aterrizó. Y queda 62 líneas por
    delante de `performTerminalCleanup` (`:159`), como exige LIVE-01. **No altera el orden LOCKED
    `backstop → setColor → notify`** (D-08 v0.16 Phase 71): se inserta ANTES del trío, no lo reordena.

    Envolver la llamada en un **try/catch propio** además del outer never-throws, copiando la forma del
    backstop (`:104-129`): un `console.error` con el prefijo `[kodo:session-end]` en el catch.
    **El try/catch es estructural, no cosmético** (RESEARCH §Pitfall 3): el contrato documentado de
    `withFileLock` dice «never throws», pero eso aplica **solo al agotamiento de reintentos**. En el
    código real `acquireLock` hace `mkdirSync` (`state-lock.js:73`, puede lanzar) y re-lanza
    explícitamente todo error que no sea EEXIST (`:81`); y `withFileLock` ejecuta `fn()` en un
    `try/finally` **sin catch** (`:226-230`), así que un `fn` que lanza propaga. Sin este catch, un
    `EACCES`/`EROFS` crashearía el hook y bloquearía el cierre — violando el criterio de éxito 5. Es el
    mismo motivo por el que el backstop lo tiene.

    Escribir un comentario de bloque en el estilo del fichero que cite D-07, D-08 (el orden que no se
    toca), LIVE-01 (por delante del cleanup destructivo) y la razón del try/catch propio.

    Pasar `deps` tal cual a `writeHandoff` — así `plansDir`/`fs`/`stateWriterFn`/`now` fluyen desde el
    caller sin duplicar el desempaquetado.

    Extender el JSDoc del shape `deps` de `runSessionEndHook` (`:47-63`) con las cuatro claves nuevas.

    Extender `test/hooks/session-end-handoff.test.js` con los casos de `<behavior>` de este task. La
    mecánica del orden observable: un ÚNICO array `calls` compartido, al que empujan el `fs` inyectado
    (en su `renameSync`), el `removeSessionFn`, y los métodos del cmux stub (`setColor`, `notify`).
    Copiar `makeLogger`/`makeCmuxStub`/`makeSession` de `test/hooks/session-end.test.js:16-62`,
    adaptando `makeCmuxStub` para que acepte el array compartido en vez de crear el suyo.
  </action>

  <verify>
    <automated>node --test test/hooks/session-end-handoff.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/hooks/session-end-handoff.test.js` exits 0 y cubre todos los casos de `<behavior>`.
    - Behavior assertion: `calls.findIndex(c => c.fn === 'handoff-write') < calls.findIndex(c => c.fn === 'removeSession')` (LIVE-01, SC#1 — el handoff antes del cleanup destructivo).
    - Behavior assertion: en el mismo array, `removeSession` < `setColor` < `notify` (el trío LOCKED intacto, SC#5).
    - Behavior assertion: con el `fs` stub que lanza `EACCES`, `runSessionEndHook` resuelve sin rechazar y `removeSession`/`setColor`/`notify` siguen presentes en `calls`.
    - Source assertion: `grep -n 'writeHandoff' src/hooks/session-end.js` muestra la llamada situada entre la construcción del `log` y el comentario del Review backstop.
    - Source assertion: el JSDoc de `deps` lista las cuatro claves nuevas — `grep -c 'plansDir\|stateWriterFn' src/hooks/session-end.js` >= 2.
    - Behavior assertion: con `findSessionFn` devolviendo `null` o `source: 'history'`, `readdirSync(plansDir)` queda vacío (los guards de idempotencia siguen mandando).
  </acceptance_criteria>

  <done>El bloque de handoff está cableado en `:97` con su try/catch propio; el orden «handoff → cleanup destructivo → setColor → notify» está asserted y el hook sigue never-throw.</done>
</task>

<task type="auto">
  <name>Task 3: Cerrar la fuga de HOME en la suite preexistente del hook</name>

  <read_first>
    - `test/hooks/session-end.test.js` — el fichero completo: cada invocación de `runSessionEndHook` y los deps que inyecta hoy
    - `src/hooks/session-end.js` — el `writeHandoff` cableado en los Tasks 1 y 2
    - `src/config.js:11` — `join(homedir(), '.kodo')` evaluado en module-load: la razón de la fuga
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md` §Pitfall 6
  </read_first>

  <files>test/hooks/session-end.test.js</files>

  <action>
    Inyectar `plansDir` (un `mkdtempSync` por fichero, limpiado en `after`) y `stateWriterFn` (un spy
    no-op) en **todas** las invocaciones de `runSessionEndHook` de `test/hooks/session-end.test.js`.

    **Por qué esto es obligatorio y no cosmético.** Este fichero **no aísla HOME** — hoy no lo necesita,
    porque inyecta `findSessionFn`/`removeSessionFn` y nunca toca el fs. Con el bloque de handoff
    cableado en `:97`, cada uno de sus casos pasa a ejecutar `writeHandoff`, cuyos defaults resuelven
    a `join(KODO_DIR, 'plans')` y a `upsertTaskHandoff` — es decir, **al `~/.kodo/plans` y al
    `state.json` REALES del operador**, en cada `npm test`. La fuga es silenciosa: los tests seguirían
    verdes mientras ensucian el HOME de quien los corre. `config.js:11` evalúa `homedir()` en
    module-load, así que poner `process.env.HOME` no la cierra desde un fichero con imports estáticos:
    la única salida limpia es la DI que los Tasks 1 y 2 añadieron.

    Añadir además un caso de regresión que fije el orden LOCKED de D-08 con el bloque nuevo insertado:
    sobre el array `calls` del `makeCmuxStub` ya presente, más un push desde `removeSessionFn`, assertar
    la secuencia `removeSession` → `setColor` → `notify`. Es la regresión que vigila la mitad de D-07
    que dice qué **no** cambia: insertar antes del trío no lo reordena.

    **No modificar las asserts de los casos existentes** — solo añadir deps a sus invocaciones. Si algún
    caso existente cambia de resultado al inyectar los deps, es un hallazgo real: pararse y reportarlo
    en el SUMMARY en vez de ajustar la assert.

    Añadir al `after` del fichero una comprobación de higiene: el tmpdir se borra.
  </action>

  <verify>
    <automated>node --test test/hooks/session-end.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/hooks/session-end.test.js` exits 0 con todas las asserts preexistentes sin modificar.
    - Source assertion: el número de invocaciones de `runSessionEndHook` en el fichero es igual al número de las que pasan `plansDir` — `grep -c 'runSessionEndHook(' test/hooks/session-end.test.js` == `grep -c 'plansDir' test/hooks/session-end.test.js` (cada invocación aislada).
    - Behavior assertion: el caso de regresión asserta `removeSession` → `setColor` → `notify` en el array `calls` compartido (orden LOCKED D-08 intacto).
    - CLI (la prueba de la fuga): con `npm test` recién corrido, `ls ~/.kodo/plans/ 2>/dev/null | wc -l` devuelve el mismo valor que antes de correrlo, y `git status --porcelain` no muestra ficheros nuevos. Ninguna entrada nueva aparece en el `state.json` real.
  </acceptance_criteria>

  <done>La suite preexistente del hook está aislada del HOME real y vigila el orden LOCKED con el bloque de handoff insertado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code stdin → hook | `input.reason` llega por stdin y acaba interpolado en el markdown del bloque mecánico |
| Provider remoto → filesystem | `session.task_id` gobierna la ruta de un fichero que D-09 ahora **crea**; `session.summary`/`task_ref` se interpolan en su cabecera |
| Hook → `~/.kodo/plans/` | El hook pasa de lector a **escritor** de un directorio compartido |
| Hook → `state.json` | Fichero compartido con daemon, server y reconcile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01 | Tampering | `writeHandoff` — construcción de la ruta desde `session.task_id` | high | mitigate | `isSafeTaskId` (Plan 01, `String.includes` sobre `/`, `\`, `..`, nunca RegExp) como PRIMERA sentencia; falso → `warn` + return sin tocar el fs. Ruta **construida** con `join(plansDir, taskId + '.md')`, jamás derivada. D-09 sube el riesgo respecto al lector de Phase 46: ahora impide **crear** ficheros fuera del root. Asserted en Task 1 |
| T-74-02 | Tampering | `buildHandoffBlock` ← `input.reason` (stdin no confiable) | medium | mitigate | `normalizeReason` (Plan 01) valida el enum CERRADO antes de interpolar; desconocido → `other` (D-03, precedente T-71-12) |
| T-74-03 | Tampering | `buildPlanHeader` ← `session.summary`/`task_ref` (provider) | medium | mitigate | `sanitizeInline` (Plan 01) colapsa CR/LF y trunca → no se puede forjar un marcador; `findSessionBlock` solo mira líneas de heading `## Handoff `. Ni summary ni task_ref se interpolan en la **ruta** |
| T-74-04 | Tampering | RMW del plan (dos cierres simultáneos de la misma tarea) | high | mitigate | `withFileLock` sobre `<plan>.md.lock` (D-08) — un temp+rename por sí solo NO evita el *lost update* de un leer→appendear→escribir. `fn` 100% **síncrono** (Pitfall 4) o el `finally` liberaría el lock antes de que la escritura aterrice. Escritura final tmp+rename con nombre único pid+randomUUID (patrón `saveState:273`, fix WR-02), **nunca** `writeFileAtomic` (tmp fijo). Verificación cross-process en el Plan 05 |
| T-74-05 | Tampering | `state.json` | high | mitigate | Toda la escritura vía `stateWriterFn` → `upsertTaskHandoff` → `withStateLock` (Plan 02). Toca solo `state.tasks`; jamás `alive` (D-04) |
| T-74-06 | DoS | Lock ocupado (plan o state) | medium | mitigate | `{ok:false}` → `log.warn` → return. Peor caso ~160 ms por lock (`state-lock.js:34-36`); dos locks → ~320 ms en un hook de cierre. El cierre **jamás** se bloquea (D-06). Asserted en Tasks 1 y 2 |
| T-74-07 | DoS | `writeHandoff` lanzando (EACCES, EROFS, lock robado) bloquea el cierre de Claude Code | high | mitigate | try/catch **propio** en el seam `:97` además del outer never-throws — estructural: `acquireLock:73` puede lanzar en `mkdirSync`, `:81` re-lanza todo lo que no sea EEXIST, y `withFileLock:226-230` es `try/finally` **sin catch**. Asserted en Task 2 (stub que lanza EACCES → el cierre completa) |
| T-74-08 | Information Disclosure | Campos del logger del hook | low | mitigate | Los logs llevan solo `{task_id, reason}`; nunca el `next` ni el `summary` (precedente T-71-18: el backstop loguea solo `{session_id, task_id, state}`) |
| T-74-14 | Tampering | Lock robado por TTL durante una escritura larga | low | mitigate | `fn` síncrono y corto (una RMW de un markdown pequeño); TTL de 10 s ≫ su duración (`state-lock.js:36`). El tmp único por escritor limita el daño incluso si se robara |
| T-74-15 | Information Disclosure | La suite de tests escribiendo en el `~/.kodo` real del operador | medium | mitigate | `plansDir`/`stateWriterFn` inyectables + Task 3, que los inyecta en todas las invocaciones de la suite preexistente. Verificado por criterio CLI: `npm test` no altera `~/.kodo/plans/` |
| T-74-SC | Tampering | npm/pip/cargo installs | high | mitigate | Esta fase instala CERO paquetes (`74-RESEARCH.md` §Package Legitimacy Audit: *"No aplica"*; `withFileLock` y `withStateLock` son **imports** de módulos internos). Sin tasks de install → sin checkpoint de legitimidad. Verificable: `git diff --stat package.json package-lock.json` vacío |
</threat_model>

<verification>
- `node --test test/hooks/session-end-handoff.test.js` exits 0
- `node --test test/hooks/session-end.test.js` exits 0
- `npm test` — suite completa verde (baseline 2027 tests, cero regresiones)
- `git diff --stat package.json package-lock.json` — vacío
- Higiene del HOME: tras `npm test`, `~/.kodo/plans/` y `~/.kodo/state.json` sin cambios
</verification>

<success_criteria>
- `writeHandoff` existe, es síncrona, y está cableada en `session-end.js:97` con try/catch propio
- El handoff aterriza antes de `removeSession` (asserted sobre un array de llamadas compartido)
- El orden `removeSession → setColor → notify` intacto (asserted)
- Plan ausente → se crea con cabecera mínima + bloque (D-09, universal)
- Marcador de esta sesión presente → cero cambios en el fichero (D-04)
- Bloque de sesión anterior presente → sí se appendea (D-04, el caso crítico)
- Plan ilegible o lock ocupado → warn, sin throw, el cierre completa (SC#5)
- `state.tasks[task_id]` poblado con `{plan_path, next, updated_at}` (LIVE-04)
- `npm test` no toca el `~/.kodo` real del operador
- Cero dependencias npm nuevas
</success_criteria>

<output>
Create `.planning/phases/74-handoff-acumulativo-al-cierre/74-04-SUMMARY.md` when done
</output>
