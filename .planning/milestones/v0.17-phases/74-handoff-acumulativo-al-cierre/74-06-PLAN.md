---
phase: 74-handoff-acumulativo-al-cierre
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/session/state.js
  - test/state/handoff-state.test.js
autonomous: true
requirements: [LIVE-04]

must_haves:
  truths:
    - "Un `NEXT:` real registrado por una sesión anterior SOBREVIVE a un cierre mecánico posterior de la misma tarea: la SIGUIENTE sesión de esa tarea lo encuentra en `state.tasks[task_id].next` (el must_have que declaró el Plan 02 y que el código rompió — WR-02)"
    - "Un `NEXT:` nuevo y NO nulo sigue pisando al anterior: el upsert no se convierte en append-only ni en un campo de solo-escritura-una-vez"
    - "Sin entrada previa, un `next` ausente o `null` sigue persistiendo como `null` — la Phase 75 nunca lee `undefined` ni una clave ausente"
    - "`plan_path` y `updated_at` siguen siendo overwrite incondicional: el cierre mecánico SÍ ocurrió y el puntero debe fecharlo"
    - "Los invariantes del Plan 02 siguen en pie: escritura bajo `withStateLock`, el mutator no toca `alive`, sin bump de `schema_version`, fail-safe ante lock-timeout sin telemetría de éxito falsa (WR-01)"
  artifacts:
    - src/session/state.js
    - test/state/handoff-state.test.js
  key_links:
    - "El `prev` se lee del parámetro `state` DENTRO del mutator — nunca de un `loadState()` propio fuera del lock: `withStateLock` carga fresco dentro del lock y ésa es la única razón por la que el merge no es un TOCTOU"
    - "La asimetría es semántica, no defensiva: D-02 define `null` como «ausente», no como «no hay siguiente paso». Ausente no debe pisar a presente — y sólo `next` tiene esa ambigüedad"
    - "La rama mecánica de `writeHandoff` (`session-end.js:365-366`) NO se toca: pasar `next: null` es LIVE-03 funcionando por diseño. El defecto está en el receptor, no en el emisor"
---

<objective>
Cerrar el único gap bloqueante de la Phase 74 (LIVE-04, WR-02): `upsertTaskHandoff` reemplaza la
entrada entera de `state.tasks[taskId]` con `next: entry.next ?? null`, así que un cierre mecánico
posterior borra el `NEXT:` real que dejó una sesión anterior de la misma tarea.

Purpose: la secuencia que rompe es **ordinaria, no exótica** — sesión 1 deja un `NEXT:` real, sesión 2
cierra sin que el LLM escriba handoff (exactamente el caso que el backstop de LIVE-03 existe para
cubrir) y el `NEXT:` desaparece de `state.json` aunque siga intacto byte a byte en el fichero de plan.
Contradice el must_have que el propio Plan 02 declaró (*«el dato sobrevive a `removeSession`... así que
la SIGUIENTE sesión de la misma tarea lo encuentra»*) y vacía el propósito de la fase: la Phase 75
pintaría una celda NEXT vacía con un NEXT real en disco. Reproducido end-to-end por el verifier, no
hipotético.

Output: `upsertTaskHandoff` preserva el `next` previo cuando el entrante es ausente/null + un caso de
regresión que lo vigila.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-VERIFICATION.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-REVIEW.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Este plan **no crea símbolos ni ficheros nuevos**. Es una corrección quirúrgica sobre artefactos que la
Phase 74 ya produjo:

**Modificado** — `upsertTaskHandoff` (`src/session/state.js:408-429`, creado por el Plan 02): cambia
únicamente la resolución del campo `next` dentro del mutator. Firma, valor de retorno
(`{ok:true}|{ok:false, reason:'lock-timeout'}`), telemetría y el resto de la función quedan idénticos.

**Modificado** — `test/state/handoff-state.test.js` (creado por el Plan 02): +2 casos de regresión.
Los 14 casos existentes se conservan sin editar.

**Shape de `state.tasks` (D-05) — SIN cambios**: `state.tasks[task_id] = { plan_path: string, next:
string|null, updated_at: string }`, clave top-level aditiva y opcional, sin bump de `schema_version`.
Lo que cambia es **cómo se calcula `next`** en una escritura, no el shape que la Phase 75 leerá.

**Fuera de alcance explícito** (warnings no bloqueantes de `74-REVIEW.md`, deliberadamente diferidos —
NO tocar en este plan): WR-01 (`sessionId` sin validar en el marcador), WR-03 (`migrateStateV2toV3`
descarta `tasks`), WR-04 (`state.tasks` sin cap — D-12 decidió «sin poda en v0.17»), WR-05
(`session-start.js` sin `isSafeTaskId`). Y `src/hooks/session-end.js` **no se modifica**.
</artifacts_this_phase_produces>

<tasks>

<!--
  planner-discipline: el grep negativo del Task 2 está scopeado a las líneas de CÓDIGO añadidas a
  `src/session/state.js` (que no debe cargar el estado por su cuenta: el `prev` sale del `state` del
  mutator). El literal aparece legítimamente en el Task 1, que describe cómo LEE el test —
  `test/state/handoff-state.test.js`, otro fichero, fuera del scope de ese grep.
-->
<!-- planner-discipline-allow: loadState -->

<task type="auto">
  <name>Task 1: Caso de regresión en rojo — el `NEXT:` previo sobrevive al cierre mecánico</name>

  <read_first>
    - `test/state/handoff-state.test.js` COMPLETO — la suite a extender. Su cabecera `:1-26` documenta
      las dos trampas obligatorias (aislamiento de HOME vía `process.env.HOME` ANTES del import
      DINÁMICO de `state.js`, porque `config.js:11` evalúa `join(homedir(), '.kodo')` en module-load; y
      la siembra v3 explícita, Pitfall 5 del research). Los helpers ya existen: `seedV3():43`,
      `writeSeed(extra):48`, `spyLogger():56`, y el par `beforeEach`/`afterEach` que resiembra `:88-89`
    - `test/state/handoff-state.test.js:112-131` — el caso `upserts: a second call for the same
      task_id replaces, never appends`: el vecino semántico del caso nuevo y el que delimita hasta
      dónde llega la preservación
    - `test/state/handoff-state.test.js:179-197` — los dos casos que hoy fijan `next: null`/omitido →
      `null`. Ambos corren sobre semilla limpia (sin entrada previa), así que el fix NO los rompe:
      confírmalo, no los edites
    - `src/session/state.js:408-429` (`upsertTaskHandoff`) — la firma exacta que se ejerce
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-VERIFICATION.md` — frontmatter `gaps:`
      (el contrato) y la fila «WR-02 reproduction» de §Behavioral Spot-Checks (el guion exacto que el
      verifier ejecutó: `addSession` → `next` real → `next: null`)
  </read_first>

  <files>test/state/handoff-state.test.js</files>

  <action>
    Añade a la suite existente DOS casos de regresión — no crees fichero nuevo, no toques los 14 casos
    ya presentes, no reordenes el fichero. Van juntos: el primero prueba el gap, el segundo prueba que
    el fix no se pasa de frenada.

    **Caso 1 — el gap (WR-02).** Nómbralo de forma que se lea el invariante, no la implementación (p.
    ej. `un NEXT: real sobrevive a un cierre mecánico posterior de la misma tarea (WR-02)`). Secuencia,
    espejando el guion que el verifier reprodujo: `upsertTaskHandoff` sobre un `task_id` con un
    `next` real y un `updated_at` fijo → segunda llamada al MISMO `task_id` con `next: null`, un
    `updated_at` posterior y el mismo `plan_path` (así es como llega la rama mecánica de
    `writeHandoff:366`). Assertea sobre `loadState().tasks[taskId]`: que `next` sigue siendo el valor
    real de la primera llamada, y que `updated_at` SÍ avanzó al de la segunda — las dos mitades de la
    asimetría en un mismo assert. Comenta POR QUÉ (no qué): D-02 define `null` como «ausente», y un
    cierre mecánico no dice «esta tarea no tiene siguiente paso», dice «esta sesión no tuvo nada que
    decir»; el `NEXT:` real sigue en el fichero de plan, así que borrarlo de `state.json` es
    divergencia entre las dos mitades del mismo dato.

    **Caso 2 — el límite.** Un `next` nuevo y NO nulo SIGUE pisando al previo. Sin este caso, el fix
    del Task 2 podría degenerar en «el primer NEXT gana para siempre» y la suite seguiría verde:
    ninguno de los 14 casos existentes distingue «preserva si ausente» de «nunca sobrescribe». Es el
    caso que le da teeth al fix por el otro lado.

    Usa los helpers existentes tal cual (`writeSeed()` en `beforeEach` ya deja la semilla v3 limpia);
    no introduzcas import estático de `state.js` ni fixtures nuevas.

    Este task deja la suite en ROJO a propósito: el Caso 1 falla contra el código actual — es la prueba
    de que el caso tiene teeth y no es decorativo (precedente del Plan 02: «un test verde no prueba
    nada sobre lo que vigila»). El Caso 2 debe pasar ya hoy.
  </action>

  <acceptance_criteria>
    - `node --test test/state/handoff-state.test.js` sale con código NO-CERO y su resumen reporta
      exactamente `# fail 1`: el único que falla es el Caso 1 (WR-02). Los 14 preexistentes + el Caso 2
      siguen verdes (`# pass 15`).
    - El fallo del Caso 1 es el esperado: el assert de `next` recibe `null` donde espera el valor real
      (no un `TypeError`, no un fallo de setup).
    - `grep -c "from '../../src/session/state.js'" test/state/handoff-state.test.js` == 0 — el
      aislamiento de HOME sigue intacto (import dinámico post-`process.env.HOME`).
    - `git diff test/state/handoff-state.test.js | grep -c '^-'` == 0 líneas borradas de los casos
      existentes (el diff es puramente aditivo; sólo cuentan como `-` las cabeceras de hunk, que
      empiezan por `---`).
    - `ls ~/.kodo/plans` y `~/.kodo/state.json` sin cambios tras el run: el HOME real del operador no
      se toca (el Plan 04 reprodujo una fuga real de esta familia de tests).
  </acceptance_criteria>

  <verify>
    <automated>node --test test/state/handoff-state.test.js; test $? -ne 0</automated>
  </verify>

  <done>El Caso 1 falla reproduciendo el gap de LIVE-04 en la suite; el Caso 2 pasa. La red está puesta antes que el fix.</done>
</task>

<task type="auto">
  <name>Task 2: `upsertTaskHandoff` preserva el `next` previo cuando el entrante es ausente</name>

  <read_first>
    - `src/session/state.js:408-429` (`upsertTaskHandoff`) — la función a corregir; el cambio vive
      DENTRO del mutator y en ninguna otra parte
    - `src/session/state.js:324-330` (`withStateLock`) — el contrato que hace correcto el merge: el
      `state` que recibe el mutator viene de un `loadState()` FRESCO ejecutado ya dentro del lock
    - `src/session/state.js:338-355` (`addSession`) — la forma canónica a no romper: `withStateLock` →
      `if (!r.ok)` → `logger.warn` → `return r` → telemetría de éxito gated DESPUÉS del guard
    - `src/session/state.js:51-61` — el typedef `TaskHandoff` y la línea `tasks?:` del typedef `State`:
      es donde se documenta la semántica del campo
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-REVIEW.md` §WR-02 — el fix esbozado y su
      razonamiento («absent should not overwrite present»)
    - `src/hooks/session-end.js:334-338` y `:365-366` — los DOS emisores de `next` (bloque del LLM vía
      `extractNext`, que también puede dar `null`; y la rama mecánica, `null` por diseño). Léelos para
      confirmar que el fix va en el receptor: **no los modifiques**
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md` §decisions — D-02 (`null` =
      «ausente»), D-03 (el bloque mecánico no lleva `NEXT:`), D-05/D-06
  </read_first>

  <files>src/session/state.js</files>

  <action>
    Dentro del mutator de `upsertTaskHandoff`, tras el guard defensivo `if (!state.tasks) state.tasks =
    {};`, captura la entrada previa desde el parámetro `state` del mutator y resuelve `next` con
    fallback a ella cuando el entrante sea ausente o `null`, colapsando a `null` si tampoco había
    previa (el partido de `??` que esboza WR-02: entrante → previo → `null`). Nombra la variable de
    forma que se lea el rol (`prev` / `previa`), no el tipo.

    **La sutileza que hace correcto el fix, y que el executor debe respetar explícitamente:** `prev`
    sale del parámetro `state` del mutator, JAMÁS de una carga propia del estado hecha desde dentro de
    la función. `withStateLock` ya carga fresco dentro del lock adquirido (`:324-330`) — ésa es la
    clave anti-clobber de toda la fase. Releer la entrada previa por fuera del lock reintroduciría por
    la puerta de atrás el lost-update que T-74-04 y el test cross-process del Plan 05 existen para
    prohibir: dos cierres concurrentes de la misma tarea partirían del mismo `prev` viejo y uno
    resucitaría un `NEXT:` ya superado.

    **`plan_path` y `updated_at` NO se mergean — siguen siendo overwrite incondicional.** Razón, y va
    en el comentario porque es la pregunta que este cambio invita a hacerse mal:
    - `plan_path` lo CONSTRUYE el caller determinísticamente desde el `task_id`
      (`session-end.js:307`, `join(plansDir, taskId + '.md')`), nunca lo deriva del input del provider:
      para un mismo `task_id` el valor entrante es siempre el mismo string. Un fallback no tendría
      entrada alcanzable que preservar, y enmascararía a un caller que pasara `undefined` (un bug que
      debe salir a flote, no heredar un valor viejo).
    - `updated_at` significa «cuándo se escribió por última vez este puntero», y el cierre mecánico
      **sí ocurrió**: su bloque aterrizó en el fichero de plan. Preservar el timestamp previo mentiría
      sobre la última escritura y dejaría a la Phase 75 pintando una tarea como más rancia de lo que
      es. Su `??` actual es un DEFAULT de generación (`entry.updated_at ?? new Date().toISOString()`),
      no un merge con lo previo: no lo toques.
    - `next` es el ÚNICO campo donde la ausencia del entrante es semánticamente distinta del valor
      `null` persistido (D-02: `null` = «ausente»). De ahí la asimetría — no es una excepción, es la
      única aplicación del principio.

    Cierra el comentario nombrando el escenario concreto que se protege (un cierre mecánico de D-03
    detrás de un `NEXT:` real de una sesión anterior), no la mecánica del `??`.

    Actualiza además la doc del campo `next` en el typedef `TaskHandoff` (`:51-55`) y/o el JSDoc de
    `upsertTaskHandoff` para que declare la semántica nueva: un `next` ausente/null **no borra** el
    previo; para pisarlo hay que pasar uno nuevo no nulo. Hoy el typedef sólo explica por qué existe la
    clave; el siguiente lector (Phase 75, que la consume) necesita saber que el valor puede provenir de
    un cierre ANTERIOR al `updated_at` que ve.

    No toques nada más de la función: firma, orden de las claves del objeto, guard `if (!r.ok)` →
    `warn` → `return r`, telemetría gated tras el guard (WR-01: jamás reclamar un éxito que no
    persistió) y `return r` final quedan idénticos. `state.js` sigue importando sólo `logger-noop`
    (LOG-12). Cero dependencias npm.
  </action>

  <acceptance_criteria>
    - `node --test test/state/handoff-state.test.js` sale 0 con `# pass 16 · # fail 0` — el Caso 1 del
      Task 1 pasa a verde sin haber sido editado.
    - `git diff test/state/handoff-state.test.js` está VACÍO en este task: el fix es de producción; si
      el test tuvo que cambiar para pasar, el test no valía y hay que parar y decirlo.
    - `git diff --stat src/hooks/session-end.js` vacío — la rama mecánica no se toca (es LIVE-03 por
      diseño).
    - `git diff src/session/state.js | grep -v '^[-+][[:space:]]*\*' | grep -v '^[-+][[:space:]]*//' | grep -c '^[-+].*schema_version'` == 0
      — ninguna línea de CÓDIGO toca el schema (grep filtrado: el typedef lo menciona en prosa;
      precedente del Plan 02, que dejó registrada esta misma trampa de criterio).
    - `git diff src/session/state.js | grep '^+' | grep -v '^+[[:space:]]*\*' | grep -v '^+[[:space:]]*//' | grep -c 'loadState'` == 0
      — ninguna línea de CÓDIGO añadida carga el estado por su cuenta: el `prev` sale del `state` del
      mutator, dentro del lock (grep filtrado de comentarios: la prosa del JSDoc sí puede nombrar la
      primitiva para explicar por qué).
    - `node --test test/state/handoff-concurrency.test.js` sigue verde: el merge no reintroduce
      lost-update bajo carrera cross-process (T-74-04).
    - `node --test test/hooks/session-end-handoff.test.js` sigue verde: el seam inyecta `stateWriterFn`
      stub, así que el contrato del caller no cambió.
    - `node --test test/check-isolation.test.js` verde — LOG-12 intacto.
    - `npm test` → **0 fail, 1 skipped**, y el total sube exactamente en los 2 casos añadidos por el
      Task 1 (2130 → 2132). Ningún caso preexistente se rompe ni desaparece.
    - `git diff --stat package.json package-lock.json` vacío — cero dependencias npm nuevas.
  </acceptance_criteria>

  <verify>
    <automated>node --test test/state/handoff-state.test.js test/state/handoff-concurrency.test.js test/hooks/session-end-handoff.test.js test/check-isolation.test.js</automated>
  </verify>

  <done>Un `NEXT:` real registrado por una sesión anterior sobrevive a un cierre mecánico posterior de la misma tarea; un `NEXT:` nuevo no nulo sigue pisándolo; la suite completa verde con 0 fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Escritores concurrentes (hook `SessionEnd` + `reconcileTick` + server) → `state.json` en disco | Frontera **preexistente**, no nueva: ya la cruzaba `upsertTaskHandoff` desde el Plan 02, y ya está mediada por `withStateLock`. Este cambio no añade superficie — no introduce input nuevo, ni parseo nuevo, ni interpolación, ni I/O nuevo: reordena la resolución de un campo dentro de un mutator que ya corría dentro del lock. |

**Evaluación honesta:** este fix **no altera el perfil de amenaza de la fase**. No hay dato nuevo de
provider ni de LLM entrando al sistema: el `next` que ahora puede sobrevivir es exactamente el mismo
string que `upsertTaskHandoff` ya había validado, truncado (200 chars, D-02, en `extractNext`) y
persistido en la escritura anterior. Las amenazas relevantes (T-74-04 lost update, T-74-05 tampering de
`state.json`, T-74-08 disclosure vía logs) ya tienen mitigación viva del Plan 02 y siguen intactas. La
única entrada nueva del registro (T-74-16) es la que este cambio concreto podría introducir si se
implementa mal — no un relleno.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-16 | Tampering (lost update / TOCTOU) | `upsertTaskHandoff` — lectura del `prev` | medium | mitigate | El `prev` se lee del parámetro `state` del mutator, que `withStateLock` obtiene de una carga fresca YA dentro del lock (`state.js:324-330`). Releerlo por fuera del lock haría que dos cierres concurrentes de la misma tarea partieran del mismo `prev` viejo y uno resucitara un `NEXT:` superado. Acceptance criteria del Task 2: el grep filtrado que prohíbe cargar el estado por cuenta propia en las líneas de código añadidas + `test/state/handoff-concurrency.test.js` verde (prueba cross-process con teeth verificadas por mutación, Plan 05). |
| T-74-05 | Tampering | `state.json` | high | mitigate | **Ya mitigada, sin cambios.** Escritura exclusiva vía `withStateLock`; el mutator sigue tocando sólo `state.tasks`, nunca `alive` (`reconcileTick` sigue su único escritor). Vigilado por el caso existente `does not introduce or modify any 'alive' key` (`handoff-state.test.js:292`), que este plan no edita. |
| T-74-08 | Information Disclosure | Telemetría de `upsertTaskHandoff` | medium | accept (ya mitigada) | **Sin cambios.** La telemetría sigue siendo `{task_id, reason}`; el `next` preservado nunca entra en los logs. El caso existente `never logs the 'next' content` (`:261`) sigue vigilándolo. El fix no añade ninguna llamada al logger. |
| T-74-SC | Tampering | npm installs | high | mitigate | Cero instalaciones en este plan: el cambio es una expresión dentro de una función existente. Acceptance criteria: `git diff --stat package.json package-lock.json` vacío. Sin paquetes → sin `[ASSUMED]`/`[SUS]` → sin checkpoint de legitimidad. |
</threat_model>

<verification>
1. `node --test test/state/handoff-state.test.js` → 16/16 pass, exit 0.
2. `npm test` → 2132 total, 0 fail, 1 skipped (baseline 2130 + los 2 casos nuevos).
3. `git diff --stat src/hooks/session-end.js package.json package-lock.json` → vacío.
4. `git diff --stat` final: exactamente 2 ficheros (`src/session/state.js`, `test/state/handoff-state.test.js`).
5. Reproducción del guion del verifier (§Behavioral Spot-Checks de `74-VERIFICATION.md`) bajo HOME
   aislado: `addSession` → `upsertTaskHandoff(t1, {next:'desplegar el fix'})` →
   `upsertTaskHandoff(t1, {next:null})` → `loadState().tasks.t1.next` sigue siendo `'desplegar el fix'`
   (antes: `null`). Es el mismo escenario que el Caso 1 automatiza; correrlo a mano cierra el bucle con
   la evidencia que abrió el gap.
6. `~/.kodo` real intacto tras `npm test`: sin clave `tasks` nueva, sin residuo `.lock`/`.tmp.`.
</verification>

<success_criteria>
- LIVE-04 verdadero en sus DOS mitades: la de locking/concurrencia (ya verificada) y la de «`state.json`
  refleja el `NEXT:`», que hoy es falsa en una secuencia de dos sesiones ordinaria.
- Un `NEXT:` real de una sesión anterior sobrevive a un cierre mecánico posterior de la misma tarea —
  el must_have que el Plan 02 declaró queda cierto en el código, no sólo en la prosa.
- Un `NEXT:` nuevo no nulo sigue pisando al previo (el fix no degenera en «el primero gana»).
- Sin entrada previa, `next` ausente/null sigue persistiendo como `null` — la Phase 75 nunca ve
  `undefined`.
- Los invariantes LOCKED siguen en pie: `withStateLock`, `reconcileTick` único escritor de `alive`,
  never-throw, sin bump de schema, LOG-12, cero dependencias npm.
- Los 4 warnings fuera de alcance (WR-01, WR-03, WR-04, WR-05) siguen SIN tocar.
</success_criteria>

<output>
Create `.planning/phases/74-handoff-acumulativo-al-cierre/74-06-SUMMARY.md` when done
</output>
