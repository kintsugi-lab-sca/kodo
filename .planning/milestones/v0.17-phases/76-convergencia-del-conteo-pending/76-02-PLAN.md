---
phase: 76-convergencia-del-conteo-pending
plan: 02
type: execute
wave: 2
depends_on: [76-01]
files_modified:
  - src/server.js
  - src/check.js
  - test/server/status-pending.test.js
  - test/check.test.js
  - test/check-isolation.test.js
autonomous: true
requirements: [ORCH-05, ORCH-06]

must_haves:
  truths:
    - "Con el provider sano, /status (server.js) y kodo check (check.js) derivan pending/pending_count del MISMO módulo src/tasks/pending.js, no de dos caminos de lectura inline distintos (ORCH-05, D-01)."
    - "/status expone pending_stale: boolean y pending_fetched_at: string|null SIEMPRE presentes, en la rama sana y en la de fallo (D-05, ORCH-06)."
    - "pending_count === pending.length en ambas ramas del payload de /status (Pitfall 4)."
    - "Con el provider caído tras expirar el TTL, /status marca pending_stale:true en vez de servir el último dato como fresco — la rama defectuosa que devolvía la caché en el catch sin comprobar TTL desaparece (ORCH-06)."
    - "kodo check produce output sano byte-idéntico (líneas /N pending/, color amarillo) y la línea roja 'Error checking tasks: <msg>' intacta en error (D-07) — los Tests 1-5 de check.test.js siguen verdes."
    - "El HTML del dashboard web marca visualmente la stat «Candidatas» cuando pending_stale es true (D-06)."
    - { statement: "Cero endpoints nuevos en src/server.js: el conjunto de rutas registradas (pathname === ...) no cambia respecto a antes del refactor.", verification: backstop }
    - { statement: "Cero dependencias npm nuevas: package.json dependencies permanece {commander, ink, picocolors, react}.", verification: backstop }
  artifacts:
    - test/server/status-pending.test.js
  key_links:
    - "server.js startServer() instancia createPendingResolver({listPendingTasksFn: () => provider.listPendingTasks(), ttlMs: PENDING_CACHE_TTL_MS, now: Date.now}) junto al providerStateResolver (~línea 509)."
    - "check.js consume fetchFreshPending(() => provider.listPendingTasks()) dentro de su try/catch existente; test/check-isolation.test.js afirma que pending.js es alcanzable desde el grafo de check.js (convergencia probada) sin arrastrar deps prohibidas."
  prohibitions:
    - "La rama de error de /status NO devuelve el último dato de caché sin comprobar TTL — todo dato de fallo va etiquetado stale:true (ORCH-06)."
    - "pending_count NO colapsa a null en error — conserva el shape numérico que consume el HTML (server.js:370) (D-04)."
    - "La staleness NO se señala solo por console.warn — es un campo (pending_stale) en la respuesta de /status (ORCH-06)."
    - "server.js y check.js NO re-implementan la lógica de fetch/frescura inline — vive solo en src/tasks/pending.js (D-09)."
    - "El output sano de kodo check NO cambia ni un byte respecto a hoy (D-07); la línea roja de error conserva err.message real."
---

<objective>
Cablear los dos consumidores del carril pending al módulo compartido `src/tasks/pending.js` (creado en el Plan 01), cerrando ORCH-05 (convergencia de código) y ORCH-06 (fallo etiquetado, no servido como fresco):

- `server.js` (/status): reemplazar el bloque inline defectuoso (caché module-level + catch que devolvía el último dato sin comprobar TTL) por `createPendingResolver({...}).resolve()`; añadir los campos aditivos `pending_stale`/`pending_fetched_at` al payload vía `buildPendingStatusFields` (D-05); marcar la staleness en el HTML del dashboard web (D-06).
- `check.js` (checkPendingTasks): consumir `fetchFreshPending` en modo crudo, conservando su try/catch y la línea roja byte-idéntica (D-07).
- Cerrar el hueco de cobertura (D-08) con el contrato de `/status`, y el guard source-hygiene de convergencia (D-09): `check.js` alcanza `pending.js` en su grafo sin arrastrar deps prohibidas (LOG-12 sigue verde).

Purpose: Hacer observable la convergencia y la frescura honesta sin endpoints nuevos ni bus de invalidación (fuera de alcance). El comportamiento cambia; el `pendingCache` no se rediseña.
Output: `server.js` y `check.js` modificados, `test/server/status-pending.test.js` nuevo, `test/check.test.js` y `test/check-isolation.test.js` ampliados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-convergencia-del-conteo-pending/76-CONTEXT.md
@.planning/phases/76-convergencia-del-conteo-pending/76-RESEARCH.md
@.planning/phases/76-convergencia-del-conteo-pending/76-PATTERNS.md
@.planning/phases/76-convergencia-del-conteo-pending/76-01-PLAN.md
</context>

<artifacts_this_phase_produces>
Símbolos NUEVOS creados por este plan (la revisión de convergencia los excluye de la verificación de drift):

- `test/server/status-pending.test.js` — fichero de test de contrato nuevo.
- `/status` (server.js) gana dos campos de respuesta NUEVOS: `pending_stale: boolean` y `pending_fetched_at: string|null`.
- `pendingResolver` — const local en `startServer()` (server.js), instancia de `createPendingResolver` del Plan 01.
- Clases CSS `.stale` / `.stale-tag` nuevas en el `<style>` de `dashboardHtml(token)` (server.js) + marcador condicional en la stat «Candidatas».
- Bloque `it('kodo check alcanza src/tasks/pending.js ...')` nuevo en `test/check-isolation.test.js` (aserción positiva de convergencia).

Consumo de símbolos del Plan 01 (NO nuevos): `fetchFreshPending`, `createPendingResolver`, `buildPendingStatusFields` de `src/tasks/pending.js`.
</artifacts_this_phase_produces>

<flagged_assumptions>
El reporte de cobertura de edge (spec-less probe fallback) clasificó **ORCH-05 y ORCH-06 como `unclassified → unresolved`** (no hay SPEC.md). NO se descartan en silencio: los `must_haves` de arriba se derivaron a mano de los Success Criteria de la Phase 76 (ROADMAP.md) y de las decisiones LOCKED D-01..D-09 (CONTEXT.md). El ejecutor debe respetar:

- **A1 (shape de tarea):** el `.map` del payload preserva `{ref, title, url, state, projectName}` verbatim (verificado en server.js:651).
- **A2 (método de contrato):** ambos caminos usan `provider.listPendingTasks()` (FROZEN).
- **Nota de paralelismo:** Phases 74/75 son ortogonales (hooks/planes vs carril pending) y ya están mayormente cerradas; si el ejecutor encuentra que server.js divergió, releer el rango real antes de editar (usar `<read_first>`), no asumir números de línea fijos.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cablear /status al resolver + campos aditivos + marcador HTML + contrato</name>
  <files>src/server.js, test/server/status-pending.test.js</files>
  <read_first>
    - src/server.js líneas 20-22 (PENDING_CACHE_TTL_MS + `let pendingCache`), ~484-509 (wiring del providerStateResolver — el patrón espejo EXACTO), 588-663 (handler /status: bloque inline defectuoso + payload), 77-147 (dashboardHtml: `<style>` en :126-128 y token interpolation), 360-374 (el JS cliente `refresh()` que pinta `#stats`, stat «Candidatas» en :370)
    - src/tasks/pending.js (creado en Plan 01: `createPendingResolver`, `buildPendingStatusFields`)
    - test/server/provider-state.test.js (analog de estilo de test para el seam; role-match para status-pending.test.js)
    - .planning/phases/76-convergencia-del-conteo-pending/76-PATTERNS.md (secciones «wiring en startServer», «/status handler», «HTML stat Candidatas»)
  </read_first>
  <action>
    En `src/server.js`:
    1. Import: añadir `import { createPendingResolver, buildPendingStatusFields } from './tasks/pending.js';` junto a los imports existentes (el import de `createProviderStateResolver` está en :9).
    2. Limpieza module-level: eliminar `let pendingCache = { data: [], ts: 0 };` (:22) — la caché pasa a vivir en el closure del resolver. CONSERVAR `const PENDING_CACHE_TTL_MS = 30 * 1000;` (:21) como la ÚNICA fuente del número (D-03).
    3. Wiring en `startServer()`: junto al `providerStateResolver` (~:504-509) instanciar `const pendingResolver = createPendingResolver({ listPendingTasksFn: () => provider.listPendingTasks(), ttlMs: PENDING_CACHE_TTL_MS, now: Date.now });` — mismo shape que el sibling, reusando el literal por parámetro (no second number).
    4. Handler /status (:588-601): reemplazar todo el bloque `let pending = []; if (Date.now() - pendingCache.ts < ...) {...} else { try {...} catch {...} }` por `const pendingResult = await pendingResolver.resolve(); if (pendingResult.stale) console.warn('[kodo] listPendingTasks stale — serving last-known-good');`. El resolver nunca lanza; el warn genérico (sin payload ni err.message) lo emite el caller (D-02, Pitfall 1).
    5. Payload (:648-662): construir los campos pending vía `const pendingFields = buildPendingStatusFields(pendingResult);` y expandirlos en el objeto `res.end(JSON.stringify({ sessions: enriched, count: enriched.length, ...pendingFields, history: fullHistory.slice(0, 10), metrics: {...}, uptime: process.uptime() }))`. `pendingFields` aporta `pending`, `pending_count`, `pending_stale`, `pending_fetched_at`. Eliminar el `pending: pending.map(...)` y `pending_count: pending.length` inline anteriores (ahora vienen del shaper). Los bloques `enriched`/`metrics`/`history` NO se tocan.
    6. HTML dashboard (dashboardHtml): en el `<style>` (junto a `.stat-val`, :127) añadir reglas `.stat-val.stale` (p.ej. atenuado/opacity) y `.stale-tag` (sufijo pequeño). En el JS cliente `refresh()` (:370), condicionar la stat «Candidatas» a `data.pending_stale`: añadir la clase `stale` al `.stat-val` y un sufijo `<span class="stale-tag">?</span>` cuando `data.pending_stale` sea true. El estilo/redacción exactos son Claude's Discretion (D-06), mínimo indicador; NO tocar la TUI ink (no consume pending).

    Crear `test/server/status-pending.test.js`: contrato de `/status` testeando el seam resolver→payload sin arrancar HTTP (mismo motivo por el que provider-state se extrajo — el handler no tiene harness). Importar `buildPendingStatusFields` de `../../src/tasks/pending.js`. Asertar:
    - Rama fresca: `buildPendingStatusFields({tasks:[{ref:'KL-1',title:'t',url:'u',state:'s',projectName:'p'}], fetched_at:'2026-07-17T00:00:00.000Z', stale:false})` → `pending_stale===false`, `pending_fetched_at==='2026-07-17T00:00:00.000Z'`, `pending_count===1`, `pending.length===1`, y `pending_count === pending.length`.
    - Rama stale/cold: input `{tasks:[], fetched_at:null, stale:true}` → `pending_stale===true`, `pending_fetched_at===null`, `pending_count===0`, `pending_count === pending.length`.
    - Ambos campos SIEMPRE presentes (usar `assert.ok('pending_stale' in fields)` y `'pending_fetched_at' in fields`).
    - Source-guard de convergencia (D-09): `readFileSync('src/server.js')` y `assert.match` que importa de `./tasks/pending.js` y que llama `pendingResolver.resolve()` y `buildPendingStatusFields(` — probando que server.js consume el módulo, no lógica inline.
  </action>
  <verify>
    <automated>node --test test/server/status-pending.test.js && node -e "import('./src/server.js').then(()=>console.log('server.js imports OK')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/server.js` importa `createPendingResolver`/`buildPendingStatusFields` de `./tasks/pending.js` y ya NO declara `let pendingCache` a nivel de módulo; `PENDING_CACHE_TTL_MS` sigue siendo el único literal del TTL.
    - El handler /status llama `await pendingResolver.resolve()` y emite `console.warn` solo cuando `stale`; el payload expande `buildPendingStatusFields(...)` con `pending`, `pending_count`, `pending_stale`, `pending_fetched_at`.
    - `dashboardHtml` gana CSS `.stale`/`.stale-tag` y el JS cliente marca la stat «Candidatas» cuando `data.pending_stale`.
    - `test/server/status-pending.test.js` verde: ambas ramas, ambos campos presentes, `pending_count === pending.length`, y source-guard de convergencia.
    - `import('./src/server.js')` no lanza (el módulo carga tras el refactor).
  </acceptance_criteria>
  <done>/status converge al resolver compartido, expone frescura discriminada, marca staleness en el HTML, y el contrato está verde sin endpoints nuevos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear check.js a fetchFreshPending (byte-idéntico) + regresión + guard de convergencia</name>
  <files>src/check.js, test/check.test.js, test/check-isolation.test.js</files>
  <read_first>
    - src/check.js líneas 1-52 (imports + `checkPendingTasks`: el punto DI existente `getProviderFn`, el try/catch :34-49 y la línea roja :45-49 que D-07 protege)
    - src/tasks/pending.js (Plan 01: `fetchFreshPending`)
    - test/check.test.js líneas 1-56 (createFakeProvider, BASE_CONFIG) + Tests 1-5 (:36-206) que deben seguir verdes byte-idénticos (D-07)
    - test/check-isolation.test.js líneas 40-52 (`walkImports`), 75-147 (aserciones de grafo de check.js), 160-177 (bloque handoff a imitar)
  </read_first>
  <behavior>
    - Camino sano: con pending > 0 y slots disponibles, `checkPendingTasks` sigue produciendo `/N pending/` en amarillo y el reason en español (Tests 1, 5a, 5d) — byte-idéntico.
    - Camino de error: con `listPendingTasks` que lanza, sigue produciendo `Error checking tasks: <err.message>` en rojo (Tests 3, 4, 5b, 5d-error) — la `err.message` real conservada.
    - Convergencia: `check.js` ahora importa `./tasks/pending.js`; su grafo lo alcanza SIN arrastrar logger.js / github/provider.js / normalize.js / polling.js.
  </behavior>
  <action>
    En `src/check.js`:
    1. Añadir `import { fetchFreshPending } from './tasks/pending.js';` junto a los imports existentes (:9-14).
    2. En `checkPendingTasks`, sustituir la línea `const pending = await provider.listPendingTasks();` (:37) por `const pending = await fetchFreshPending(() => provider.listPendingTasks());` (convergencia ORCH-05). CONSUMIR el fetch CRUDO (no el resolver) — `fetchFreshPending` propaga el throw, así el try/catch existente (:34-49) y su línea roja quedan byte-idénticos (D-07, Pitfall 2). NO tocar nada más de check.js.

    En `test/check.test.js`: los Tests 1-5 existentes deben seguir verdes sin cambios. Añadir un caso que pruebe explícitamente que `checkPendingTasks` ahora rutea por `fetchFreshPending` y sigue produciendo la misma línea `/N pending/` (reusar `createFakeProvider` con `listPendingTasks` que devuelve 2 items y `getProviderFn: () => provider`), y un caso de error que confirme que la `err.message` real (`network down`) sigue apareciendo en la línea roja.

    En `test/check-isolation.test.js`: añadir un `it(...)` que afirme la CONVERGENCIA — `walkImports(join(SRC, 'check.js'))` INCLUYE `join(SRC, 'tasks', 'pending.js')` (aserción positiva: check.js realmente consume el módulo compartido, D-09). Reutilizar el `walkImports` existente. Las aserciones de grafo preexistentes (no logger.js / github / polling) ya cubren transitivamente que pending.js no arrastra deps prohibidas (es hoja); confirmarlo dejándolas correr — no duplicarlas.
  </action>
  <verify>
    <automated>node --test test/check.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/check.js` importa `fetchFreshPending` de `./tasks/pending.js` y `checkPendingTasks` lo usa en lugar de la llamada cruda a `provider.listPendingTasks()`; el try/catch y la línea roja quedan intactos.
    - Los Tests 1-5 de `test/check.test.js` siguen verdes (output sano byte-idéntico + línea roja con `err.message` real, D-07).
    - `test/check-isolation.test.js` tiene una aserción positiva de que `src/tasks/pending.js` está en el grafo de `check.js`, y las aserciones LOG-12 preexistentes (no logger.js/github/polling) siguen verdes.
    - `node --test test/check.test.js test/check-isolation.test.js` verde.
  </acceptance_criteria>
  <done>check.js consume el fetch compartido con output byte-idéntico; la convergencia queda probada por el grafo de imports sin romper LOG-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → GET /status | Cruza tras el bearer default-deny (server.js:570) — SIN cambios; el refactor no toca auth. |
| provider externo (Plane/GitHub) → server/check | La lista `pending` cruza aquí; el fallo del fetch se colapsa a `stale:true`, no propaga payload al cliente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-02 | Tampering/Repudiation | /status sirviendo dato caducado como fresco | medium | mitigate | ES el fix de ORCH-06: `pending_stale:true` + `pending_fetched_at` del último éxito hacen la staleness dato en la respuesta; el operador/orquestador distingue «0 real» de «no se pudo saber». Contrato blindado por status-pending.test.js. |
| T-76-01 | Information Disclosure | `console.warn` de fallo en server.js | low | mitigate | El warn es genérico («stale — serving last-known-good»): NO incluye payload del provider ni `err.message`. El resolver no expone el error. PERSIST-04 intacto (bearer/API key jamás en /status). |
| T-76-03 | Information Disclosure | `pending_fetched_at` como canal de timing lateral | low | accept | Insignificante — /status ya está tras bearer; el timestamp de un fetch interno no filtra nada explotable. |
| T-76-SC | Tampering | Instalación npm/pip/cargo | low | accept | N/A — cero paquetes nuevos (constraint LOCKED «cero dependencias npm nuevas»); todo built-in. Backstop must_have verifica que package.json dependencies no cambia. |
</threat_model>

<verification>
- `node --test test/server/status-pending.test.js test/check.test.js test/check-isolation.test.js` verde.
- `import('./src/server.js')` carga sin lanzar.
- Gate de fase: `npm test` (suite completa ~2027 tests) verde antes de `/gsd-verify-work`.
- Highest severity del threat model = medium (T-76-02), mitigado → sin bloqueo (security_block_on=high).
- Backstop: revisar manualmente/por grep que no se añadieron rutas nuevas en server.js ni entradas en package.json dependencies.
</verification>

<success_criteria>
- ORCH-05: /status y kodo check derivan pending/pending_count del mismo `src/tasks/pending.js`; convergencia probada por el grafo de imports (D-09).
- ORCH-06: /status expone `pending_stale`/`pending_fetched_at`; con provider caído tras TTL, marca stale en vez de servir el último dato como fresco; la rama de catch defectuosa desaparece.
- D-07: kodo check output sano byte-idéntico; línea roja con err.message real.
- D-06: HTML del dashboard marca la staleness.
- Cero endpoints nuevos, cero deps npm nuevas.
</success_criteria>

<output>
Create `.planning/phases/76-convergencia-del-conteo-pending/76-02-SUMMARY.md` when done
</output>
