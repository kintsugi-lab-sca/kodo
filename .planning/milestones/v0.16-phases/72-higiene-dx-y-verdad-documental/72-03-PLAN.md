---
phase: 72-higiene-dx-y-verdad-documental
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/labels.js
  - src/gsd/verification.js
  - src/gsd/verify.js
  - src/gsd/roadmap.js
  - src/providers/plane/client.js
  - src/hooks/install.js
  - src/providers/registry.js
  - test/labels.test.js
  - test/gsd-verification.test.js
  - test/gsd-roadmap.test.js
  - test/plane-provider.test.js
  - test/registry.test.js
  - test/hooks/install.test.js
autonomous: true
requirements: [HYG-06]
must_haves:
  truths:
    - "Una tarea con label `kodo:opus` resuelve `model === 'opus'` (no cae a `flags`)."
    - "El cliente Plane lee su config desde `config.providers.plane.*`, no del schema v1 `config.plane.*` eliminado."
    - "El gate de must_haves de verificación usa `!==` (99/3 se rechaza, no pasa)."
    - "El match de VERIFICATION.md por fase no depende del zero-pad de 2 dígitos."
    - "Un identificador tipo `K2-42` (letra+dígito antes del guion) resuelve; el parser de roadmap acepta guion, en-dash y em-dash."
    - "El uninstall/install de hooks matchea por ruta canónica del comando, no por substring `'kodo'`."
    - "El factory de GitHub sin bloque `github` en config da el mensaje canónico, no un `TypeError` críptico."
  artifacts:
    - "src/labels.js (whitelist opus/sonnet/haiku)"
    - "src/gsd/verification.js (must_haves `!==`; YAML inline `#` manejado)"
    - "src/gsd/verify.js (match de fase desacoplado del pad-2)"
    - "src/gsd/roadmap.js (regex de header acepta [-–—])"
    - "src/providers/plane/client.js (config.providers.plane.*; regex de identificador; throttle epoch-vs-delta o diferido; 409 estrecho)"
    - "src/hooks/install.js (match por ruta canónica)"
    - "src/providers/registry.js (factory GitHub con guard y mensaje canónico)"
  key_links:
    - "Cada hallazgo es un micro-diff de 1-5 líneas con su test (Pattern 1) — D-01 fija el contrato REQUIREMENTS HYG-06"
    - "B12 se descompone en 4 micro-diffs independientes; si alguno excede ~5 líneas se DIFIERE con nota en el SUMMARY (D-02)"
---

<objective>
Aplicar el batch de BAJAS mecánicas de HYG-06 como micro-diffs quirúrgicos (1-5 líneas) con un test por hallazgo (Pattern 1). El contrato exacto es **REQUIREMENTS.md HYG-06 (D-01)**: `B1, B2, B3, B4, B8, B9, B12 + M12`. La PROPUESTA omitía B2/B8 — REQUIREMENTS los añade y gana. **D-03:** el RESEARCH ya re-verificó cada file:line contra HEAD (2026-07-13) — todos reproducen; 3 file:line quedaron desplazados y están corregidos en la tabla del RESEARCH (M3/B8/M4). B6/B10 ya estaban cerrados por Phase 69 y NO son de este scope (Deferred).

**B12 «Otros» (D-02):** grab-bag de 4 sub-items independientes → 4 micro-diffs. Si alguno excede el presupuesto de ~5 líneas del criterio de éxito, se **difiere con nota en el SUMMARY** (no se fuerza). El candidato principal a diferir es B12b (throttle epoch-vs-delta, Open Question #2/A4).

Purpose: saldar la deuda mecánica dispersa por proveedores, GSD, labels y hooks sin cambiar ninguna capacidad.
Output: 11 micro-diffs (o los que quepan; los diferidos con nota) + tests por subsistema. Los tres tasks agrupan por fichero/subsistema para minimizar cambios de contexto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-CONTEXT.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-PATTERNS.md
@.compound/AUDITORIA-ADVERSARIAL-2026-07-03.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: B1 (labels) + B3/B12a (verification) + B4 (verify) + M12 (roadmap)</name>
  <files>src/labels.js, src/gsd/verification.js, src/gsd/verify.js, src/gsd/roadmap.js, test/labels.test.js, test/gsd-verification.test.js, test/gsd-roadmap.test.js</files>
  <read_first>
    - src/labels.js (whitelist en :29 `['sonnet', 'haiku'].includes(tag)`)
    - src/gsd/verification.js (comparación must_haves en :214 `must_haves_verified < must_haves_total`; parser YAML naive `body.split('\n')` en :118)
    - src/gsd/verify.js (match acoplado al pad-2: `padStart(2,'0')` :136-137, `startsWith(\`${padded}-\`)` :177, helper :463)
    - src/gsd/roadmap.js (regex de header en :31 `(?::\s*|\s+-\s+)`)
    - test/labels.test.js, test/gsd-verification.test.js, test/gsd-roadmap.test.js (convención de test por subsistema)
    - .planning/phases/72-.../72-RESEARCH.md (§HYG-06 tabla + Code Example M12; §Re-verificación B3/B4/B12a)
    - .compound/AUDITORIA-ADVERSARIAL-2026-07-03.md (definiciones canónicas B1/B3/B4/B12a/M12)
  </read_first>
  <action>
    **B1:** en `src/labels.js:29`, añade `'opus'` a la whitelist de modelos (`['opus', 'sonnet', 'haiku']`) para que un label `kodo:opus` resuelva `result.model = 'opus'` en vez de caer a `flags` (latente-activo: `default_model` ya es `'opus'`).

    **B3:** en `src/gsd/verification.js:214`, cambia la condición `must_haves_verified < must_haves_total` por `must_haves_verified !== must_haves_total`, de modo que un `verified > total` (p.ej. 99/3) también se rechace (reason `must-haves-incomplete`).

    **B12a (D-02, sub-item de B12):** en el parser de frontmatter YAML de `verification.js:118` (`body.split('\n')`), maneja los comentarios inline `#` en valores (strip del `#...` inline antes de parsear el valor). Si el fix supera ~5 líneas, difiérelo con nota en el SUMMARY (D-02) — pero prioriza un strip simple y acotado.

    **B4:** en `src/gsd/verify.js` (:136-137, :177, :463), desacopla el match del descubrimiento de VERIFICATION.md del zero-pad fijo de 2 dígitos — el prefijo de fase debe casar tanto `9-` como `09-` (o el pad real del directorio) sin asumir 2 dígitos. Mantén el comportamiento actual para fases ya padded a 2 dígitos.

    **M12:** en `src/gsd/roadmap.js:31`, amplía la regex de header para aceptar guion ASCII, en-dash y em-dash como separador: el grupo `(?::\s*|\s+-\s+)` pasa a `(?::\s*|\s+[-–—]\s+)` (Code Example del RESEARCH). Así `## Phase 6 — Foundation` (em-dash) matchea.

    Añade/actualiza un test por hallazgo en el fichero de test del subsistema correspondiente (B1 en test/labels.test.js; B3/B12a en test/gsd-verification.test.js; M12 en test/gsd-roadmap.test.js; B4 en el fichero de verify o gsd-verification según cobertura). Convención `node:test` describe/it + `assert/strict`.
  </action>
  <verify>
    <automated>node --test test/labels.test.js test/gsd-verification.test.js test/gsd-roadmap.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `parseKodoLabels([{name:'kodo:opus'}])` devuelve `model === 'opus'` (test B1).
    - El gate de must_haves con `verified=99, total=3` produce reason `must-haves-incomplete` (test B3, condición `!==`).
    - Un frontmatter con un comentario `#` inline en un valor se parsea correctamente O el sub-item B12a queda diferido con nota explícita en el SUMMARY (D-02).
    - El descubrimiento de VERIFICATION.md casa una fase de 1 dígito sin depender del pad-2 (test B4).
    - `## Phase 6 — Foundation` (em-dash) matchea el parser de roadmap; `## Phase 6 - Foundation` (guion ASCII) sigue matcheando (test M12).
    - `node --test test/labels.test.js test/gsd-verification.test.js test/gsd-roadmap.test.js` pasa.
  </acceptance_criteria>
  <done>B1, B3, B4, B12a y M12 aplicados (o B12a diferido con nota); tests de labels/verification/roadmap verdes.</done>
</task>

<task type="auto">
  <name>Task 2: B2 + B8 + B12b + B12c — proveedor Plane (client.js)</name>
  <files>src/providers/plane/client.js, test/plane-provider.test.js</files>
  <read_first>
    - src/providers/plane/client.js (schema v1: `config.plane.base_url` :8, `config.plane.workspace_slug` :10, `config.plane.api_key_env` :14; throttle: `_rateReset*1000-Date.now()` :37 y `x-ratelimit-reset` :57-59; identificador: `resolveIdentifier` regex `/^([A-Z]+)-(\d+)$/i` :289-290; 409: `isNameConflict409` :263-264 con `|| msg.includes('labels/')`)
    - src/config.js (`migrateConfig` :113-138 — la forma canónica v2 `providers.plane.{base_url,web_url,api_key_env,workspace_slug,states}`)
    - test/plane-provider.test.js (cobertura existente del cliente)
    - .planning/phases/72-.../72-RESEARCH.md (§HYG-06 tabla B2/B8/B12b/B12c; Pitfall #2 — re-grep `parseRef`; Pitfall #6 — B12b candidato a diferir; Open Question #2)
    - .compound/AUDITORIA-ADVERSARIAL-2026-07-03.md (definiciones canónicas B2/B8/B12)
  </read_first>
  <action>
    **B2:** en `src/providers/plane/client.js` :8, :10, :14, sustituye las lecturas del schema v1 `config.plane.base_url`/`config.plane.workspace_slug`/`config.plane.api_key_env` por el schema v2 `config.providers.plane.base_url`/`...workspace_slug`/`...api_key_env` (tras la migración v1→v2, `config.plane.*` es `undefined`). Re-grep por si hay más lecturas de `config.plane.` en el fichero y migra todas.

    **B8 (Pitfall #2):** primero `grep -nE "\\^\\(\\[A-Za?-z\\]" src/providers/plane/client.js` (o busca todas las regex de identificador `parseRef`/`resolveIdentifier`) para localizar TODOS los sitios. Sustituye la regex `/^([A-Z]+)-(\d+)$/i` (:290) por `/^([A-Za-z][A-Za-z0-9]*)-(\d+)$/` para que un identificador con dígito interno (`K2-42`) resuelva — el `/i` no salva `K2-42` porque `[A-Za-z]+` no puede contener el `2`. Aplica el fix a CADA sitio encontrado.

    **B12c (D-02):** en `isNameConflict409` (:263-264), estrecha el predicado quitando el `|| msg.includes('labels/')` — dejar solo el match por `msg.includes('already exists')` (además del `Plane API 409`), para no tragarse CUALQUIER 409 cuyo path contenga `labels/`.

    **B12b (D-02, candidato a diferir — Pitfall #6 / Open Question #2):** el throttle (:37) asume que `x-ratelimit-reset` (:57-59) es epoch-segundos; si Plane self-hosted devuelve un delta, `_rateReset*1000-Date.now()` da basura. Si puedes detectar barato epoch-vs-delta (p.ej. un valor pequeño = delta en segundos; un valor grande = epoch) en ≤5 líneas, aplícalo; si NO se puede confirmar el formato barato, **difiere B12b con nota explícita en el SUMMARY** (D-02) en vez de adivinar.

    Añade tests en `test/plane-provider.test.js`: B2 → el cliente construido con una config v2 lee base_url/workspace_slug/api_key_env de `providers.plane`; B8 → `resolveIdentifier('K2-42')` resuelve (no null); B12c → un 409 con `labels/` en el path pero SIN `already exists` NO se trata como name-conflict (re-lanza); B12b → test solo si se aplica (si se difiere, nota en SUMMARY, sin test).
  </action>
  <verify>
    <automated>node --test test/plane-provider.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El cliente Plane lee su config de `config.providers.plane.*`; `grep -c "config.plane\." src/providers/plane/client.js` == 0.
    - `resolveIdentifier('K2-42')` devuelve un match no nulo (test B8); todas las regex de identificador del fichero quedan alineadas.
    - `isNameConflict409` ya no trata como conflicto un 409 con `labels/` en el path sin `already exists` (test B12c).
    - B12b aplicado con test, O diferido con nota explícita en el SUMMARY citando Open Question #2 (D-02).
    - `node --test test/plane-provider.test.js` pasa.
  </acceptance_criteria>
  <done>B2, B8, B12c aplicados y B12b aplicado-o-diferido-con-nota; tests del proveedor Plane verdes.</done>
</task>

<task type="auto">
  <name>Task 3: B9 (install) + B12d (registry)</name>
  <files>src/hooks/install.js, src/providers/registry.js, test/hooks/install.test.js, test/registry.test.js</files>
  <read_first>
    - src/hooks/install.js (match por substring: uninstall `h.command?.includes('kodo')` :82; install exists-check `hook.command?.includes('kodo')` :111; los comandos canónicos se construyen en :20-23 como `node "<kodoRoot>/src/hooks/<hook>.js"`)
    - src/providers/registry.js (factory github :60-73: `config.providers?.github` :66 → `createGitHubProvider(github)` :71 con `undefined` → TypeError críptico; bloque github ya en try/catch separado :53-75)
    - test/hooks/install.test.js, test/registry.test.js (cobertura existente)
    - .planning/phases/72-.../72-RESEARCH.md (§HYG-06 tabla B9/B12d; B12d analog)
    - .compound/AUDITORIA-ADVERSARIAL-2026-07-03.md (definiciones canónicas B9/B12)
  </read_first>
  <action>
    **B9:** en `src/hooks/install.js` (:82 uninstall y :111 install exists-check), sustituye el match por substring `'kodo'` por un match por RUTA canónica del comando del hook (compara contra el patrón canónico `node "<kodoRoot>/src/hooks/...js"` construido en :20-23, o al menos por el segmento de ruta `/src/hooks/` + el nombre del hook, en vez del substring genérico `'kodo'` que matchearía cualquier comando que mencione kodo). Mantén el comportamiento para los hooks legítimos de kodo ya instalados.

    **B12d (D-02):** en `src/providers/registry.js` (:66-71), añade un guard: si `config.providers?.github` es `undefined`/falsy en el momento de invocar el factory, lanza/propaga el MENSAJE CANÓNICO (p.ej. "config de GitHub ausente: define providers.github …") en vez de pasar `undefined` a `createGitHubProvider` y producir un `TypeError` críptico. No cambies el fail-isolation del bloque github (try/catch separado, Pitfall #6 Phase 24).

    Tests: B9 → un `settings.json` con un hook cuyo comando menciona "kodo" pero NO es un hook de kodo (ruta distinta) no se elimina/confunde; un hook canónico de kodo SÍ se detecta (test en test/hooks/install.test.js). B12d → invocar el factory github sin bloque `providers.github` produce el mensaje canónico, no un `TypeError` crudo (test en test/registry.test.js).
  </action>
  <verify>
    <automated>node --test test/hooks/install.test.js test/registry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El match de install/uninstall usa la ruta canónica del hook, no el substring `'kodo'` (verificable por test con un comando "kodo" ajeno que NO debe matchear).
    - El factory github sin `config.providers.github` produce el mensaje canónico (assert de mensaje), no un `TypeError` sobre propiedad de `undefined` (test B12d).
    - `node --test test/hooks/install.test.js test/registry.test.js` pasa.
  </acceptance_criteria>
  <done>B9 y B12d aplicados; tests de install y registry verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Respuestas de la API Plane → cliente | Headers/errores (rate-limit, 409) parseados por el cliente |
| config.json → proveedores/factory | El schema de config alimenta el cliente Plane y el factory de GitHub |
| settings.json de Claude Code → install/uninstall | El match decide qué hooks toca kodo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-08 | Tampering | `isNameConflict409` demasiado amplio (B12c) | low | mitigate | Estrechar el predicado a `already exists` — no tragarse 409 no relacionados |
| T-72-09 | Denial of Service | throttle epoch-vs-delta (B12b) | low | mitigate | Detectar formato o diferir con nota (D-02) — evita esperas basura ante header ambiguo |
| T-72-10 | Tampering | match por substring en install (B9) | low | mitigate | Match por ruta canónica — no confundir hooks ajenos que mencionen "kodo" |
| T-72-11 | Denial of Service | factory github sin config (B12d) | low | mitigate | Guard + mensaje canónico — no `TypeError` críptico que oculte la causa |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | Esta fase NO instala paquetes (invariante: cero nuevas deps npm) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/labels.test.js test/gsd-verification.test.js test/gsd-roadmap.test.js test/plane-provider.test.js test/hooks/install.test.js test/registry.test.js` verde.
- Suite completa `npm test` verde antes del merge de wave.
- Cualquier sub-item diferido (candidato: B12b) documentado con nota explícita en `72-03-SUMMARY.md` (D-02).
</verification>

<success_criteria>
- Batch de BAJAS mecánicas (B1, B2, B3, B4, B8, B9, B12 + M12) aplicado en diffs de 1-5 líneas; los que excedan el presupuesto, diferidos con nota (criterio de éxito #4 de la fase).
</success_criteria>

## Artifacts this plan produces

**Símbolos/comportamientos AÑADIDOS/CAMBIADOS:**
- Whitelist de modelo con `opus` (src/labels.js).
- Lecturas `config.providers.plane.*` (src/providers/plane/client.js) reemplazando `config.plane.*`.
- Comparación `!==` en el gate de must_haves (src/gsd/verification.js) + manejo de `#` inline en el parser YAML.
- Match de VERIFICATION.md desacoplado del pad-2 (src/gsd/verify.js).
- Regex de identificador `^([A-Za-z][A-Za-z0-9]*)-(\d+)$` (src/providers/plane/client.js).
- Regex de header de roadmap con `[-–—]` (src/gsd/roadmap.js).
- `isNameConflict409` estrechado (src/providers/plane/client.js).
- Match por ruta canónica en install/uninstall (src/hooks/install.js).
- Guard + mensaje canónico en el factory github (src/providers/registry.js).
- (Condicional) detección epoch-vs-delta del throttle (src/providers/plane/client.js) — o diferido con nota.

**Diferibles (D-02):** B12b (throttle epoch-vs-delta) si excede ~5 líneas / formato de header no confirmable barato.

<output>
Create `.planning/phases/72-higiene-dx-y-verdad-documental/72-03-SUMMARY.md` when done
</output>
