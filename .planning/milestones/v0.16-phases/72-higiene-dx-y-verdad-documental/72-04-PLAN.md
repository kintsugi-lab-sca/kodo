---
phase: 72-higiene-dx-y-verdad-documental
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/format.js
  - src/cli/dashboard/App.js
  - test/dashboard-format.test.js
autonomous: true
requirements: [HYG-07]
must_haves:
  truths:
    - "Un comentario de Plane que contiene una secuencia OSC-52 (`\\x1b]52;c;…\\x07`) se renderiza en el dashboard SIN los bytes de control — no puede inyectar en el terminal del operador."
    - "El strip elimina TODO `\\x1b` y los bytes de control C0/C1 (salvo `\\n`/`\\t`), no solo el CSI que ya cubría format.js."
    - "El texto normal (letras/acentos/espacios) del comentario se conserva intacto."
  artifacts:
    - "src/cli/format.js (helper exportado `stripControlChars`)"
    - "src/cli/dashboard/App.js (proyección de comentarios pasa por stripControlChars, :1696-1699)"
    - "test/dashboard-format.test.js (payload OSC-52 + texto normal)"
  key_links:
    - "El helper NO extiende el regex CSI de format.js:57 (solo cubre CSI, no OSC) — es un strip amplio nuevo (Don't-Hand-Roll)"
    - "El punto de inyección real es la proyección `comments.map` de App.js:1696-1699, no SessionTable.js (que renderiza líneas ya proyectadas)"
---

<objective>
Cerrar M4/HYG-07: el contenido externo no confiable (comentarios de Plane) llega al render del dashboard Ink sin sanear, así que una secuencia de escape ANSI/OSC embebida en un comentario puede inyectar en el terminal del operador (p.ej. OSC-52 = escritura al portapapeles). El regex CSI existente (`format.js:57`, `\x1b\[[\d;]*[A-Za-z]`) solo cubre CSI — NO el vector OSC (`\x1b]`). Se añade un helper de strip amplio y se cablea en el ÚNICO punto de entrada del contenido externo: la proyección de comentarios en `App.js:1696-1699` (SessionTable.js:234 solo renderiza líneas ya proyectadas — no es el sitio).

Purpose: neutralizar la inyección de terminal desde contenido de proveedor (STRIDE Tampering) — rebajada de prioridad tras cerrar la red en Ola 1, pero sigue siendo material de seguridad.
Output: `stripControlChars` exportado desde format.js + cableado en la proyección de comentarios + test con payload OSC-52.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-CONTEXT.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: helper `stripControlChars` en format.js (test-first)</name>
  <files>src/cli/format.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/format.js (el regex CSI de :57 `\x1b\[[\d;]*[A-Za-z]` — cubre solo CSI; el helper nuevo es SEPARADO, no una extensión de este regex; invariante color-isolation: picocolors solo desde este módulo, el helper es puro sin color)
    - test/dashboard-format.test.js (convención de test del módulo format)
    - .planning/phases/72-.../72-RESEARCH.md (§HYG-07 Code Example + Don't-Hand-Roll; §Security Domain OSC-52)
  </read_first>
  <behavior>
    - Test 1: `stripControlChars('hola')` === 'hola' (texto normal intacto, incluidos acentos/espacios).
    - Test 2: un payload con OSC-52 (`\x1b]52;c;AAAA\x07`) queda sin ningún `\x1b` ni el terminador — solo el texto visible restante.
    - Test 3: un payload con CSI (`\x1b[31mrojo\x1b[0m`) queda como `rojo` (sin los bytes de control).
    - Test 4: se conservan `\n` y `\t`; se eliminan los demás C0/C1 y `\x7f`.
  </behavior>
  <action>
    Añade a `src/cli/format.js` una función pura exportada `stripControlChars(s)` que devuelva `String(s)` con TODOS los `\x1b` (ESC) y los bytes de control C0/C1 eliminados, PRESERVANDO `\n` y `\t`. Rango a eliminar: `\x00-\x08`, `\x0b`, `\x0c`, `\x0e-\x1f`, `\x7f` (y por tanto `\x1b`, que cae en `\x0e-\x1f`). NO extiendas el regex CSI de :57 (solo cubre CSI; el vector es OSC `\x1b]`) — es un helper nuevo e independiente (Don't-Hand-Roll del RESEARCH). Nunca lanza sobre input no-string (coacciona con `String(s)`).

    Escribe primero los tests (RED) en `test/dashboard-format.test.js` según el bloque `<behavior>`, luego implementa hasta verde (GREEN).
  </action>
  <verify>
    <automated>node --test test/dashboard-format.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `stripControlChars('\x1b]52;c;AAAA\x07x')` no contiene `\x1b` ni `\x07` (comprobable con `.includes('\x1b') === false`).
    - `stripControlChars('\x1b[31mrojo\x1b[0m')` === 'rojo'.
    - `stripControlChars('a\nb\tc')` === 'a\nb\tc' (preserva `\n`/`\t`).
    - `stripControlChars('café con leña')` === 'café con leña' (texto normal intacto).
    - `stripControlChars` es exportada por `src/cli/format.js` y no importa picocolors.
    - `node --test test/dashboard-format.test.js` pasa.
  </acceptance_criteria>
  <done>`stripControlChars` existe, es pura, elimina ESC/OSC/C0/C1 (preservando `\n`/`\t`) y conserva texto normal. Tests RED→GREEN verdes.</done>
</task>

<task type="auto">
  <name>Task 2: cablear stripControlChars en la proyección de comentarios (App.js)</name>
  <files>src/cli/dashboard/App.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/dashboard/App.js (proyección de comentarios en :1696-1699: `lines = comments.map((c) => { const body = c.body ?? c.text ?? c.message; if (body == null) return JSON.stringify(c); return c.author ? \`${c.author}: ${body}\` : String(body); })`)
    - src/cli/format.js (el `stripControlChars` recién exportado por la Task 1)
    - .planning/phases/72-.../72-PATTERNS.md (§HYG-07 — punto de inyección exacto y qué NO tocar)
  </read_first>
  <action>
    En `src/cli/dashboard/App.js`, importa `stripControlChars` desde `../format.js` (ajusta la ruta relativa real desde `dashboard/App.js`) y envuelve con él CADA rama de la proyección de comentarios (:1696-1699): el `JSON.stringify(c)` de respaldo, y la línea `${c.author}: ${body}` / `String(body)`. Es el ÚNICO punto de entrada del contenido externo; NO toques `SessionTable.js` (renderiza líneas ya saneadas). No cambies la lógica de `status`/`empty`/shape — solo envuelve los strings proyectados.

    Añade un test (en test/dashboard-format.test.js o el dashboard test correspondiente) que verifique que la proyección de un comentario cuyo `body` lleva OSC-52 produce una línea sin bytes de control (invoca la función de proyección si está expuesta, o replica el map con el helper para asertar el saneo aplicado en el punto correcto).
  </action>
  <verify>
    <automated>node --test test/dashboard-format.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `App.js` importa `stripControlChars` desde el módulo format y lo aplica en las tres ramas de la proyección de comentarios (:1696-1699); `grep -c "stripControlChars" src/cli/dashboard/App.js` ≥ 1.
    - Un comentario con `body` = `'\x1b]52;c;AAAA\x07evil'` proyecta una línea sin `\x1b`/`\x07`.
    - `SessionTable.js` no se modificó.
    - `node --test test/dashboard-format.test.js` pasa; `npm test` (suite de dashboard) no regresiona el render.
  </acceptance_criteria>
  <done>El contenido externo de comentarios pasa por `stripControlChars` en su punto de entrada del render; una carga OSC-52 no puede inyectar en el terminal. Tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comentarios de Plane (proveedor) → render Ink del dashboard | Contenido externo NO confiable escrito por terceros en el gestor de tareas, proyectado a `<Text>` en el terminal del operador |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-12 | Tampering | proyección de comentarios (M4/HYG-07) | high | mitigate | `stripControlChars` elimina ESC/OSC/C0/C1 antes del `<Text>` en el único punto de entrada (App.js:1696-1699) — neutraliza OSC-52 (portapapeles) y otras inyecciones de terminal |
| T-72-13 | Information Disclosure | fallback `JSON.stringify(c)` de shapes raras | low | mitigate | El fallback también pasa por el strip — ninguna rama de la proyección escapa al saneo |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | Esta fase NO instala paquetes (invariante: cero nuevas deps npm) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/dashboard-format.test.js` verde.
- Suite completa `npm test` verde antes del merge de wave (confirma que el render del dashboard no regresiona).
</verification>

<success_criteria>
- El dashboard hace strip de `\x1b` (incluido OSC) en el contenido externo (comentarios) — criterio de éxito #3 de la fase, parte HYG-07.
</success_criteria>

## Artifacts this plan produces

**Símbolos AÑADIDOS:**
- `stripControlChars(s)` — helper puro exportado desde `src/cli/format.js` (elimina ESC/OSC/C0/C1, preserva `\n`/`\t`).
- Cableado de `stripControlChars` en la proyección de comentarios de `src/cli/dashboard/App.js` (:1696-1699).
- Tests OSC-52 en `test/dashboard-format.test.js`.

**Sin cambios (conservado):** el regex CSI de `format.js:57`; `SessionTable.js`.

<output>
Create `.planning/phases/72-higiene-dx-y-verdad-documental/72-04-SUMMARY.md` when done
</output>
