---
phase: 72-higiene-dx-y-verdad-documental
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.js
  - src/cli/up.js
  - src/session/health.js
  - src/config.js
  - test/config-hardening.test.js
  - test/cli/health-wait.test.js
autonomous: true
requirements: [HYG-02, HYG-03, HYG-05]
must_haves:
  truths:
    - "`kodo up --help` ya no lista `--url`; `runUp` se invoca sin argumentos y ningún camino lee `deps.url`."
    - "`startHealthLoop`/`stopHealthLoop`/`runHealthCheck` ya no existen; `checkHealth`/`actOnHealth` siguen exportadas e intactas (las usa check.js)."
    - "`kodo config --set __proto__.x=y` (o constructor/prototype) se RECHAZA con error, no se sanea ni escribe."
    - "`kodo config --set token=a=b=c` guarda el valor completo `a=b=c` (no se trunca en el primer `=`); `--map-project id:/ruta:con:dos-puntos` preserva la ruta completa."
    - "Una config parcial o inválida (p.ej. `max_parallel:-5`) se deep-mergea sobre DEFAULT_CONFIG y los valores inválidos caen al default con warn NDJSON — loadConfig NUNCA lanza."
    - "Un `.env` con `KEY=\"valor\"` carga `valor` sin las comillas emparejadas."
    - "Un fichero persistido vía writeFileAtomic cuyo contenido incluye una clave `*_secret` queda con permisos 0600."
  artifacts:
    - "src/cli.js (option `--url` de `up` eliminada; setNestedValue con rechazo de claves prohibidas; parsing de `--set`/`--map-project` con indexOf+slice)"
    - "src/cli/up.js (runUp sin lectura de deps.url)"
    - "src/session/health.js (loop de health eliminado; checkHealth/actOnHealth conservadas)"
    - "src/config.js (loadEnvFile con strip de comillas; loadConfig con deep-merge+validación warn-and-fallback; writeFileAtomic con chmod 0600 condicional a *_secret)"
    - "test/config-hardening.test.js (M3, M14, B5, B7, M5)"
  key_links:
    - "loadConfig reutiliza los validadores de src/config-validate.js (D-10) — NO duplica validación"
    - "M5 espeja la técnica chmod-pre-rename de writeEnvVar (config.js:448-453); writeEnvVar ya es 0600 y NO se toca"
    - "El `--url` de `kodo dashboard` (cli.js:381, runDashboard lo lee en :386) NO se toca — solo el de `up`"
---

<objective>
Batch de bajo riesgo que posee `src/cli.js`, `src/cli/up.js`, `src/session/health.js` y `src/config.js`: dos borrados de código muerto (HYG-02, HYG-03) y el endurecimiento del pipeline de config (HYG-05). Se agrupan porque HYG-02 (option `--url` de `up`) y HYG-05 (M3/M14 en `cli.js`) tocan el MISMO fichero — un solo plan lo posee y evita conflictos de wave.

- **HYG-02 (A9):** el flag `kodo up --url` es código muerto — `runUp` computa `baseUrl` vía `resolveBaseUrlFn` y jamás lee `deps.url`. Decisión 2026-07-05: **borrar, no cablear**. Quitar la option (cli.js:75) y el arg `{ url: opts.url }` (cli.js:83), y la lectura muerta en up.js. NO tocar el `--url` de `kodo dashboard` (cli.js:381).
- **HYG-03 (M18):** `startHealthLoop`/`stopHealthLoop`/`runHealthCheck` + el `healthInterval` module-level (health.js:150-184) son dead code (cero importadores). Borrar SOLO eso — `checkHealth`/`actOnHealth` del MISMO fichero los usa `check.js`, NO borrar el fichero.
- **HYG-05:** batch de endurecimiento de config (V5/V12/V14 ASVS): M3 (rechazar `__proto__|constructor|prototype`), M14 (`split`→`indexOf`+`slice` en `--set`/`--map-project`), B5 (strip de comillas en el parser `.env`), B7 (deep-merge+validación warn-and-fallback en `loadConfig`, reutilizando `config-validate.js`), M5 (chmod 0600 en el camino writeFileAtomic si el contenido lleva `*_secret`).

Purpose: quitar superficie muerta y cerrar prototype pollution, truncado de valores, permisos de secreto y config parcial maliciosa — todo con la filosofía never-throws del daemon.
Output: cli.js/up.js/health.js/config.js endurecidos y limpios; `test/config-hardening.test.js` nuevo cubriendo M3/M14/B5/B7/M5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-CONTEXT.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYG-02 + HYG-03 — borrar `up --url` y el loop de health</name>
  <files>src/cli.js, src/cli/up.js, src/session/health.js, test/cli/health-wait.test.js</files>
  <read_first>
    - src/cli.js (la definición del comando `up`: option `--url` en :75, action que llama `runUp({ url: opts.url })` en :83; y el comando `dashboard` con SU PROPIO `--url` en :381 que NO se toca)
    - src/cli/up.js (`runUp(deps = {})` en :151; computa `baseUrl` vía `resolveBaseUrlFn` en :172/:190 y nunca lee `deps.url`)
    - src/session/health.js (dead: `healthInterval` :150-151, `startHealthLoop` :157-165, `stopHealthLoop` :167-172, `runHealthCheck` :174-184; VIVAS: `checkHealth` :22-100, `actOnHealth` :106-129, `detectIdle` :136-148)
    - src/check.js (importa checkHealth/actOnHealth — confirmar que NO importa las funciones borradas)
    - test/cli/health-wait.test.js (cobertura existente de health)
    - .planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md (§HYG-02, §HYG-03, Anti-Patterns)
  </read_first>
  <action>
    **HYG-02:** en `src/cli.js`, elimina la línea `.option('--url <baseUrl>', ...)` del comando `up` (:75) y cambia la llamada `await runUp({ url: opts.url })` (:83) por `await runUp()`. NO toques la option `--url` del comando `dashboard` (:381) ni su reenvío a `runDashboard` (:386) — es un flag vivo. En `src/cli/up.js`, elimina cualquier lectura/destructuring de `deps.url` (si existe) y la mención en el comentario :78-79 que dice que `--url` se reenvía; `runUp` sigue resolviendo `baseUrl` config-driven vía `resolveBaseUrlFn`.

    **HYG-03:** en `src/session/health.js`, borra `startHealthLoop`, `stopHealthLoop`, `runHealthCheck` y la variable module-level `healthInterval` (bloque :150-184). CONSERVA intactas `checkHealth`, `actOnHealth` y `detectIdle`. No dejes un comentario huérfano que nombre las funciones borradas (documenta por concepto si hace falta). Verifica con grep que ningún otro módulo importaba las funciones borradas antes de quitarlas.

    Verificación de ausencia por IMPORT (no grep de fichero, para no colisionar con el `--url` vivo del dashboard): añade/actualiza un test que importe `src/session/health.js` y asserte `typeof health.startHealthLoop === 'undefined'` (idem stopHealthLoop/runHealthCheck) y que `typeof health.checkHealth === 'function'` y `typeof health.actOnHealth === 'function'`. Para HYG-02, añade un test que construya el comando `up` (o inspeccione sus `.options`) y asserte que ninguna option tiene el flag `--url`, y que el comando `dashboard` SÍ lo conserva.
  </action>
  <verify>
    <automated>node --test test/cli/health-wait.test.js && node -e "import('./src/session/health.js').then(h=>{if(h.startHealthLoop!==undefined||h.stopHealthLoop!==undefined||h.runHealthCheck!==undefined)process.exit(1);if(typeof h.checkHealth!=='function'||typeof h.actOnHealth!=='function')process.exit(1);})"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./src/session/health.js')..."` (arriba) sale 0: las tres funciones del loop son `undefined`, `checkHealth`/`actOnHealth` siguen siendo funciones.
    - El comando `up` no expone la option `--url`; el comando `dashboard` SÍ la conserva (assert de test o inspección de `.options`).
    - `runUp` ya no lee ninguna propiedad `url` de sus deps (verificable por test de comportamiento: pasar `{ url: 'x' }` a `runUp` no cambia el `baseUrl` resuelto, que sigue siendo config-driven vía `resolveBaseUrlFn`).
    - `node --test test/cli/health-wait.test.js` pasa (checkHealth/actOnHealth intactas).
    - `npm test` no reporta importadores rotos de las funciones borradas.
  </acceptance_criteria>
  <done>`kodo up --url` no existe (dashboard --url intacto); el loop de health muerto está borrado y checkHealth/actOnHealth siguen operativas. Tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: HYG-05 (cli.js) — M3 rechazar prototype pollution + M14 split→indexOf</name>
  <files>src/cli.js, test/config-hardening.test.js</files>
  <read_first>
    - src/cli.js (`setNestedValue` def en :551-559 + callsite :42; parsing de `--set` con `opts.set.split('=')` en :36; parsing de `--map-project` con `opts.mapProject.split(':')` en :49)
    - src/config-validate.js (`setByPath` :149 y la convención de validación pura/never-throws — analog para el estilo del guard)
    - .planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md (§HYG-05 Code Examples M3 y M14; Pitfall #1 — la ubicación real de M14 es cli.js:36,49, NO el parser .env; D-11)
  </read_first>
  <action>
    **M3 (D-11):** en `setNestedValue` (cli.js:551), antes de caminar las keys, rechaza `__proto__`, `constructor` y `prototype`: define un `Set` de claves prohibidas y lanza un `Error` explícito (mensaje tipo "Clave de config prohibida: <k>") si alguna key del path coincide. **Rechazar, no escapar/sanear** (Anti-Pattern del RESEARCH). El rechazo cubre el path completo (todas las keys tras `split('.')`).

    **M14 (Pitfall #1 — sitio real cli.js:36,49):** sustituye `const [key, value] = opts.set.split('=')` (:36) por parsing con `indexOf('=')` + `slice`: `key` = todo antes del primer `=`, `value` = TODO lo que sigue al primer `=` (preservando `=` internos; `value` indefinido si no hay `=`). Aplica el mismo idiom con `indexOf(':')` a `opts.mapProject` (:49) para preservar rutas con `:`. Es el mismo idiom que ya usa `loadEnvFile` (config.js:19-22) — NO tocar el parser `.env` para M14 (ya usa indexOf; su fix es B5 en la Task 3).

    Cubre ambos en `test/config-hardening.test.js` (crear el fichero si no existe, convención `node:test` describe/it + `assert/strict`, espejo de test/config-validate.test.js): M3 → `setNestedValue` con un path que contiene `__proto__`/`constructor`/`prototype` lanza y NO muta el objeto ni contamina `Object.prototype`; M14 → parsear `token=a=b=c` da `value === 'a=b=c'`, y una ruta `id:/a:b:c` da `localPath === '/a:b:c'`.
  </action>
  <verify>
    <automated>node --test test/config-hardening.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `setNestedValue(obj, '__proto__.polluted', 'x')` lanza; tras el intento, `({}).polluted === undefined` (sin prototype pollution) y `obj` no fue mutado.
    - Parsear `--set token=a=b=c` produce key `token` y value `a=b=c` (verificable por test).
    - Parsear `--map-project PROJ:/home/a:b` preserva `/home/a:b` como ruta.
    - `node --test test/config-hardening.test.js` pasa (bloques M3 y M14).
    - El parser `.env` (`loadEnvFile`) NO se modificó en esta task (M14 vive en cli.js, no en config.js).
  </acceptance_criteria>
  <done>`kodo config --set` rechaza claves de prototype pollution y ya no trunca valores con `=`/`:` internos. Tests M3/M14 verdes.</done>
</task>

<task type="auto">
  <name>Task 3: HYG-05 (config.js) — B5 strip comillas + B7 deep-merge/validate + M5 chmod 0600</name>
  <files>src/config.js, test/config-hardening.test.js</files>
  <read_first>
    - src/config.js (parser `loadEnvFile` :12-28; `writeFileAtomic` :100-104 sin chmod; `loadConfig` :161-169 que devuelve `migrateConfigIfNeeded(parsed)` verbatim; `writeEnvVar` :412-458 que YA hace chmod 0600 pre-rename en :451-453; `DEFAULT_CONFIG` :32+)
    - src/config-validate.js (validadores exportados: `validatePositiveInt` :50, `validateModel` :68, `validateNonEmpty` :82, `validateCmuxColor` :96, `validateField` :110, `getByPath`/`setByPath`)
    - .planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md (§HYG-05 Code Examples B5; Don't-Hand-Roll para B7 y M5; D-10; §Re-verificación M5 — el audit apunta a config.js:100-104, D-11 lo etiqueta como ".env")
  </read_first>
  <action>
    **B5 (D-11):** en `loadEnvFile` (config.js:22), tras `const value = trimmed.slice(eq + 1).trim()`, añade un strip conservador de comillas emparejadas: si `value.length >= 2` y empieza y termina por la MISMA comilla (`"` o `'`), quítalas (`value.slice(1, -1)`). Solo pares emparejados en inicio Y fin — no tocar comillas sueltas internas.

    **B7 (D-10, warn-and-fallback NUNCA crash):** en `loadConfig` (config.js:161-169), tras parsear y migrar, deep-mergea la config parseada sobre `DEFAULT_CONFIG` (defaults como base, valores del usuario encima) y valida los campos conocidos reutilizando `src/config-validate.js` (NO duplicar validación): un valor inválido (p.ej. `max_parallel:-5`) cae al default con un warn NDJSON (usa el logger existente / patrón warn-and-continue; wording a discreción). Conserva el `try/catch` externo fail-open que ya devuelve `{ ...DEFAULT_CONFIG }` ante JSON corrupto (:167-168). El deep-merge debe preservar sub-objetos (p.ej. `providers.plane.states`) sin pisar ramas ausentes en el parcial.

    **M5 (audit config.js:100-104; D-11 nuance):** endurece `writeFileAtomic` para que, cuando el contenido serializado contenga una clave que matchee `*_secret` (sufijo `_secret`), el fichero temporal se cree/chmodee a 0600 ANTES del `renameSync` — espejo de la técnica de `writeEnvVar` (:451-453: `writeFileSync(tmp,{mode:0o600})` → `chmodSync(tmp,0o600)` → `renameSync`). Cuando el contenido NO lleva `*_secret`, el comportamiento de permisos queda como hoy. NO modifiques `writeEnvVar` (ya es 0600 incondicional). Documenta por qué (defensa en profundidad del boundary PERSIST-04: config.json no debería llevar secretos, pero si los lleva no puede quedar world-readable).

    Amplía `test/config-hardening.test.js` con: B5 → un `.env` con `KEY="valor con espacios"` carga `valor con espacios` sin comillas (usa un tmp .env + re-import o el parser expuesto); B7 → una config parcial con `max_parallel:-5` produce un config con `max_parallel` en el default y las ramas ausentes rellenas desde DEFAULT_CONFIG, sin lanzar; M5 → `writeFileAtomic(tmpPath, JSON.stringify({api_key_secret:'x'}))` deja el fichero en modo 0600 (`statSync(tmpPath).mode & 0o777 === 0o600`), y un contenido sin `*_secret` no fuerza 0600.
  </action>
  <verify>
    <automated>node --test test/config-hardening.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Un `.env` con valor entre comillas emparejadas carga el valor sin comillas (test B5).
    - `loadConfig()` sobre una config parcial/ inválida devuelve un objeto con TODAS las ramas de DEFAULT_CONFIG presentes y los valores inválidos en su default; nunca lanza (test B7).
    - `loadConfig()` reutiliza `config-validate.js` (import presente en config.js; sin validador nuevo duplicado).
    - `writeFileAtomic` deja 0600 cuando el contenido incluye una clave `*_secret`, y respeta el modo por defecto en caso contrario (test M5).
    - `writeEnvVar` NO fue modificada.
    - `node --test test/config-hardening.test.js` pasa (bloques B5, B7, M5, además de M3/M14 de la Task 2).
  </acceptance_criteria>
  <done>El pipeline de config rechaza config parcial peligrosa con fallback validado, hace strip de comillas en el `.env` y protege 0600 los ficheros con secreto. Tests B5/B7/M5 verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (`--set`/`--map-project`) → config object | Input del operador que muta `config.json` vía setNestedValue |
| `.env` / `config.json` en disco → proceso | Ficheros parseados a `process.env`/config; pueden contener secretos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-04 | Tampering | `setNestedValue` (M3) | high | mitigate | Rechazar `__proto__`/`constructor`/`prototype` (nunca escapar) — corta prototype pollution vía `config --set` |
| T-72-05 | Information Disclosure | `writeFileAtomic` con secreto (M5) | high | mitigate | chmod 0600 pre-rename cuando el contenido lleva `*_secret` (espejo writeEnvVar) — boundary PERSIST-04 |
| T-72-06 | Denial of Service | `loadConfig` con config parcial (B7) | medium | mitigate | deep-merge sobre DEFAULT_CONFIG + validación warn-and-fallback vía config-validate.js — nunca crash |
| T-72-07 | Tampering | parsing `--set`/`.env` (M14/B5) | low | mitigate | indexOf+slice preserva `=`/`:` internos; strip conservador de comillas solo pares emparejados |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | Esta fase NO instala paquetes (invariante: cero nuevas deps npm) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/config-hardening.test.js test/cli/health-wait.test.js` verde.
- Suite completa `npm test` verde antes del merge de wave (confirma que ningún importador de las funciones borradas quedó roto y que loadConfig endurecido no rompe consumidores).
</verification>

<success_criteria>
- `kodo up --url` eliminado (dashboard `--url` intacto) y loop de health muerto borrado (criterio de éxito #2 de la fase, parte HYG-02/HYG-03).
- Batch de endurecimiento de config aplicado: M3 rechaza prototype pollution, M14 preserva valores, B5 strip de comillas, B7 deep-merge/validación, M5 chmod 0600 (criterio de éxito #3 de la fase, parte HYG-05).
</success_criteria>

## Artifacts this plan produces

**Símbolos/comportamientos AÑADIDOS:**
- `Set` de claves prohibidas + rechazo en `setNestedValue` (src/cli.js) — corta prototype pollution.
- Parsing `indexOf('=')`/`indexOf(':')`+`slice` en `--set`/`--map-project` (src/cli.js).
- Strip de comillas emparejadas en `loadEnvFile` (src/config.js).
- Deep-merge sobre `DEFAULT_CONFIG` + validación warn-and-fallback en `loadConfig` (src/config.js), cableando `src/config-validate.js`.
- chmod 0600 condicional (contenido con `*_secret`) en `writeFileAtomic` (src/config.js).
- `test/config-hardening.test.js` (nuevo) cubriendo M3, M14, B5, B7, M5.

**Símbolos/comportamientos ELIMINADOS:**
- Option `--url` del comando `up` (src/cli.js) + arg `{ url: opts.url }` + lectura muerta en `runUp` (src/cli/up.js).
- `startHealthLoop`, `stopHealthLoop`, `runHealthCheck`, `healthInterval` (src/session/health.js).

**Sin cambios (conservado):** `checkHealth`/`actOnHealth`/`detectIdle` (health.js); `--url` de `kodo dashboard` (cli.js); `writeEnvVar` (ya 0600).

<output>
Create `.planning/phases/72-higiene-dx-y-verdad-documental/72-02-SUMMARY.md` when done
</output>
