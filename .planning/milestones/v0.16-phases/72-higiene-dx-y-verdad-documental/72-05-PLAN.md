---
phase: 72-higiene-dx-y-verdad-documental
plan: 05
type: execute
wave: 2
depends_on: ["72-01", "72-02"]
files_modified:
  - README.md
autonomous: false
requirements: [HYG-08]
must_haves:
  truths:
    - "El README describe la transición a «In Review» como un backstop mecánico en `SessionEnd` (no un efecto per-turn del stop hook)."
    - "El README refleja que el auto-commit del stop hook ocurre solo en la sesión orquestadora (gate `KODO_ORCHESTRATOR`)."
    - "El README no promete features borradas (`kodo up --url`, loop de health cada 60s) si no las promete ya."
    - "Cada claim tocado corresponde a un cambio de comportamiento POST-72; los claims ya verdaderos NO se reescriben (delta, no rewrite)."
  artifacts:
    - "README.md (claims reconciliados con el estado POST-72)"
  key_links:
    - "Corre en Wave 2, DESPUÉS de HYG-04 (Plan 01) y de los borrados HYG-02/HYG-03 (Plan 02) — D-05"
    - "Es pasada DELTA de verificación, no reescritura — el README fue reescrito y auditado el 2026-07-10 (commit cb98a6d) — D-04"
---

<objective>
Pasada DELTA de verdad documental (HYG-08). El README fue reescrito y auditado contra el CLI real el 2026-07-10 (commit `cb98a6d`), así que **NO es una reescritura (D-04)** — se verifica cada claim contra el estado POST-72 y se toca SOLO lo que quede falso. Va en el ÚLTIMO plan/wave (**D-05**) porque HYG-04 (Plan 01) mueve a `SessionEnd` el comportamiento que el README describe («al cerrar, el stop hook mueve a In Review y comenta»), y HYG-02/HYG-03 (Plan 02) borran `up --url` y el loop de health.

Claims candidatos a quedar falsos POST-72 (verificar cada uno):
- Diagrama de flujo (~:26-29): «stop hook → Plane → In Review, notifica orquestador» — la transición a In Review es ahora el backstop mecánico de `SessionEnd` (DELIV-04) y el nudge/efectos se movieron a `SessionEnd` (HYG-04).
- «Al cerrar — el stop hook postea un comentario de cierre y mueve la tarea a In Review» (~:266): ahora el backstop de `SessionEnd` transiciona a In Review y comenta «cierre automático»; el stop hook ya no lo hace per-turn.
- Tabla de arquitectura (~:279): «Stop (In Review, comentario de cierre)» — debe reflejar que `SessionEnd` maneja In Review/cleanup y `Stop` solo el estado ligero.
- «el stop hook auto-commitea los cambios» (~:258): ahora gated a la sesión orquestadora (`KODO_ORCHESTRATOR`), pathspec `.claude/skills/kodo-orchestrate/`.
- Ausencia de promesas de features borradas: `kodo up --url`, «health check cada 60s» (verificar que no aparecen; si no aparecen, no hay cambio — anotar en el SUMMARY).

Purpose: que la documentación no mienta sobre el comportamiento de cierre tras el reparto Stop↔SessionEnd.
Output: README.md con los claims falsos corregidos (delta mínimo) y un checkpoint humano que confirma la verdad de cada claim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-CONTEXT.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYG-08 — pasada DELTA del README contra el estado POST-72</name>
  <files>README.md</files>
  <read_first>
    - README.md (claims de cierre: diagrama :14-33, «Al cerrar» :265-267, tabla de arquitectura :271-282, auto-commit del orquestador :256-259)
    - src/hooks/stop.js (estado POST-Plan-01: gate + pathspec; efectos de cierre ya NO están aquí)
    - src/hooks/session-end.js (estado POST-Plan-01: backstop DELIV-04 + efectos setColor/notify/nudge tras el cleanup)
    - src/session/health.js (estado POST-Plan-02: sin loop de health)
    - src/cli.js (estado POST-Plan-02: sin `up --url`)
    - .planning/phases/72-.../72-01-SUMMARY.md y 72-02-SUMMARY.md (qué cambió exactamente en Plans 01/02)
    - .planning/phases/72-.../72-RESEARCH.md (§HYG-08 checklist; §State of the Art — old vs current)
  </read_first>
  <action>
    Verifica cada claim del objetivo contra el CÓDIGO POST-72 (tras Plans 01 y 02, ya mergeados en Wave 1). Para cada claim que quede FALSO, aplica el delta mínimo con `Edit` (reemplazo acotado — NUNCA reescribir el fichero entero):
    - Diagrama de flujo: la caja «stop hook → Plane → In Review» debe reflejar que el cierre real (`/exit`) dispara `SessionEnd`, que ejecuta el backstop mecánico a In Review y los efectos (color/notify/nudge). Mantén el estilo ASCII existente; cambio mínimo.
    - «Al cerrar» (:266): reformula para que la transición a In Review + comentario «cierre automático» sean del backstop de `SessionEnd` (DELIV-04), no del stop hook per-turn.
    - Tabla de arquitectura (:279): la fila `src/hooks/` debe mencionar `SessionEnd` (In Review backstop + cleanup terminal) y `Stop` (estado ligero: idle/lock/color diferido). Añade `src/daemon/`/`src/triggers/` solo si ya están (no inventar).
    - Auto-commit (:258): aclara que el stop hook auto-commitea la skill SOLO en la sesión orquestadora (marcador `KODO_ORCHESTRATOR`), con pathspec `.claude/skills/kodo-orchestrate/`.
    - `kodo up --url` y «health check cada 60s»: confirma que NO se prometen; si aparecen, quítalos; si no aparecen, no cambies nada y anótalo.

    NO reescribas secciones que ya sean verdaderas POST-72 (D-04). NO añadas capacidades nuevas ni prometas trabajo futuro. Registra en el SUMMARY, claim por claim, cuáles resultaron ya-verdaderos (sin cambio) y cuáles se corrigieron.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('README.md','utf8'); if(/up\s+--url/.test(s)) {console.error('README aún menciona up --url'); process.exit(1);} if(/health\s+check\s+cada\s+60|cada\s+60s/i.test(s)) {console.error('README aún promete health loop 60s'); process.exit(1);} process.exit(0);"</automated>
  </verify>
  <acceptance_criteria>
    - El README no contiene `up --url` ni una promesa de «health check cada 60s» (el comando `node -e` de verify sale 0).
    - El claim de «Al cerrar» describe el backstop de `SessionEnd` (In Review + «cierre automático»), no un efecto per-turn del stop hook.
    - La fila `src/hooks/` de la tabla de arquitectura menciona `SessionEnd` para el ciclo de cierre (In Review/cleanup) y `Stop` para el estado ligero.
    - El claim de auto-commit refleja el gate `KODO_ORCHESTRATOR` y el pathspec de la skill.
    - Los cambios son ediciones acotadas (delta), no una reescritura del fichero (verificable en el diff: solo las líneas de los claims falsos cambian).
    - El SUMMARY lista, claim por claim, verdadero-sin-cambio vs corregido.
  </acceptance_criteria>
  <done>El README refleja el comportamiento de cierre POST-72 (SessionEnd/backstop, gate de auto-commit) y no promete features borradas; solo los claims falsos cambiaron.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — verificación humana de los claims del README POST-72</name>
  <action>Bloqueante: el humano contrasta cada claim tocado del README contra el comportamiento real POST-72 (Plans 01/02 ya mergeados) y aprueba, o señala qué claim sigue falso. No hay automatización adicional en este paso — es una confirmación de verdad documental.</action>
  <what-built>Pasada DELTA del README: se reconciliaron los claims de cierre (In Review vía SessionEnd/backstop, auto-commit gated por KODO_ORCHESTRATOR) y se confirmó la ausencia de promesas de features borradas (`up --url`, loop de health).</what-built>
  <how-to-verify>
    1. `git diff README.md` — confirma que SOLO cambiaron las líneas de los claims falsos (delta, no reescritura).
    2. Lee la sección «Al cerrar» y la tabla de arquitectura: la transición a «In Review» y el comentario «cierre automático» deben atribuirse a `SessionEnd`/backstop, no al stop hook per-turn.
    3. Lee el claim de auto-commit del orquestador: debe mencionar que solo ocurre en la sesión orquestadora (marcador `KODO_ORCHESTRATOR`) con pathspec `.claude/skills/kodo-orchestrate/`.
    4. Contrasta cada claim tocado con el comportamiento real (Plans 01/02 ya mergeados) — que ninguno haya quedado mintiendo en la otra dirección.
    5. Confirma que no se prometen `kodo up --url` ni un «health check cada 60s».
  </how-to-verify>
  <resume-signal>Escribe "approved" si cada claim del README es verdadero POST-72, o describe qué claim sigue siendo falso/incompleto.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README (documentación) → operador | No hay superficie de ejecución; el único riesgo es documentación engañosa que induzca un mal modelo mental (no explotable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-14 | Repudiation / (documental) | README claims de cierre | low | mitigate | Pasada DELTA + checkpoint humano — el README no describe un comportamiento de cierre que ya no existe |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | Esta fase NO instala paquetes (invariante: cero nuevas deps npm) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- El comando `node -e` de la Task 1 sale 0 (sin `up --url` ni promesa de health-loop).
- Checkpoint humano `approved`: cada claim del README es verdadero contra el estado POST-72.
- Suite completa `npm test` verde (esta fase no toca código en este plan, pero se corre como gate de cierre de fase).
</verification>

<success_criteria>
- El README refleja la realidad POST-72: cierre vía SessionEnd/backstop, auto-commit gated, sin features borradas (criterio de éxito #5 de la fase, HYG-08).
</success_criteria>

## Artifacts this plan produces

**Cambios (documentación):**
- README.md: claims de cierre reconciliados (In Review vía `SessionEnd`/backstop; auto-commit gated por `KODO_ORCHESTRATOR` + pathspec de la skill); confirmación de ausencia de `up --url` y del loop de health.

**Sin símbolos de código nuevos** — este plan solo toca documentación (delta).

<output>
Create `.planning/phases/72-higiene-dx-y-verdad-documental/72-05-SUMMARY.md` when done
</output>
