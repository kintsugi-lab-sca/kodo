---
phase: 72-higiene-dx-y-verdad-documental
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/stop.js
  - src/orchestrator/launch.js
  - src/hooks/session-end.js
  - test/hooks/stop-idempotency.test.js
  - test/hooks/session-end.test.js
  - test/stop.test.js
  - test/stop-state-transition.test.js
autonomous: true
requirements: [HYG-01, HYG-04]
must_haves:
  truths:
    - "Una sesión normal en el repo kodo (sin KODO_ORCHESTRATOR=1) NO auto-commitea al terminar un turno — skip silencioso con log."
    - "La sesión orquestadora auto-commitea SOLO el subdirectorio .claude/skills/kodo-orchestrate/ (pathspec en el add Y en el commit)."
    - "El coloreado a review, la notify de cierre y el nudge al orquestador ocurren al cierre REAL de la sesión (SessionEnd), no al final de cada turno (Stop)."
    - "El backstop de transición de estado (DELIV-04) sigue corriendo ANTES que los efectos cosméticos de HYG-04."
  artifacts:
    - "src/hooks/stop.js (gate KODO_ORCHESTRATOR + pathspec kodo-orchestrate/; efectos setColor/notify/nudge eliminados de runStopHook)"
    - "src/orchestrator/launch.js (prefijo KODO_ORCHESTRATOR=1 en el claudeCmd de la rama new-workspace)"
    - "src/hooks/session-end.js (efectos setColor/notify/nudge insertados tras performTerminalCleanup, cada uno never-throws)"
  key_links:
    - "launch.js inyecta la env var en el command string → stop.js la lee en handleOrchestratorStop (gate D-06)"
    - "HYG-04 inserta los efectos DESPUÉS de runReviewBackstop (D-08) — nunca antes; el comentario reservado en session-end.js:89-94 marca el sitio"
    - "buildStopNudgeText permanece exportado en stop.js; session-end.js lo importa para el nudge movido (Pitfall #3)"
---

<objective>
Cerrar los dos hallazgos de la Ola 4 que tocan los hooks de cierre: HYG-01 (gate del auto-commit del orquestador) y HYG-04 (mover los efectos de cierre de `Stop` a `SessionEnd`). Ambos modifican `src/hooks/stop.js`, por eso viven en un solo plan que posee ese fichero de punta a punta.

- **HYG-01 (A8):** hoy `handleOrchestratorStop` (stop.js:265-299) auto-commitea `.claude/skills/` **entero** en CUALQUIER sesión cuyo cwd sea el repo kodo, sin gate y sin pathspec en el commit → arrastra lo que el dev tuviera staged (commits fantasma por turno). Se añade el gate `KODO_ORCHESTRATOR=1` (D-06, cubre TODO el bloque add+commit; sin la variable → skip silencioso con log), se restringe el pathspec al subdirectorio `.claude/skills/kodo-orchestrate/` en AMBOS pasos (D-07), y se inyecta la variable en `launchOrchestrator` (D-07) prefijando el command string enviado por `cmux.send` (no hay `spawn` — Pattern 3 del PATTERNS).
- **HYG-04 (M19):** los tres efectos de cierre (`setColor` review, `notify` cerrada, `nudge` al orquestador) disparan hoy en `runStopHook` al final de CADA turno. Se mueven a `runSessionEndHook`, DESPUÉS del backstop de DELIV-04 (D-08), cada uno con su propio try/catch (never-throws individual). `Stop` conserva lo que NO es efecto de cierre: guards de idempotencia, `markSessionStatus('idle')` y `releaseGsdLock` (D-09).

Purpose: eliminar los commits fantasma y hacer que los efectos cosméticos reflejen el cierre real, sin romper los guardrails de test existentes (`stop-idempotency`, `session-end`, `stop`, `stop-state-transition`).
Output: stop.js con gate+pathspec y sin efectos de cierre; launch.js con la env var inyectada; session-end.js con los efectos tras el cleanup terminal; los 4 tests guardrail actualizados en el mismo commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-CONTEXT.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md
@.planning/phases/72-higiene-dx-y-verdad-documental/72-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYG-01 — gate KODO_ORCHESTRATOR + pathspec en stop.js, inyección en launch.js</name>
  <files>src/hooks/stop.js, src/orchestrator/launch.js, test/hooks/stop-idempotency.test.js</files>
  <read_first>
    - src/hooks/stop.js (estado actual completo; `handleOrchestratorStop` en :265-299, el add es `.claude/skills/` entero y el commit NO lleva pathspec en :285; la ruta que llega aquí es el branch "no session found + cwd es KODO_ROOT" en :131-138)
    - src/orchestrator/launch.js (el `claudeCmd` array-join en :250-256, enviado por `cmux.send` en :258 — NO hay child_process/spawn; `persistOrchestratorRef` en :31-37 ya escribe ~/.kodo/orchestrator.json)
    - test/hooks/stop-idempotency.test.js (el cmux stub y los asserts de calls; base para el nuevo test del gate)
    - .planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md (§HYG-01, Pattern 3, Open Question #1 / A2 — propagación de la env var)
  </read_first>
  <action>
    En `src/orchestrator/launch.js`, dentro de la rama new-workspace, prefija la asignación de entorno inline `KODO_ORCHESTRATOR=1` como PRIMER elemento del array `claudeCmd` (antes de `'claude'`) que se une con `' '` y se manda por `cmux.send` en :258 (D-07). Es una asignación de entorno del shell delante del comando, no una opción de `claude`. NO tocar la rama "existing" (:197-204) — el nudge de refresco no lanza `claude` nuevo. NO alterar el `+ '\\n'` del `cmux.send`.

    En `src/hooks/stop.js`, dentro de `handleOrchestratorStop` (:265-299), añade al PRINCIPIO del cuerpo (antes del `git status --porcelain`) un gate: si `process.env.KODO_ORCHESTRATOR` no es la cadena `'1'`, emite un `console.error` explicando que no es sesión orquestadora y `return` sin commitear (D-06: el gate cubre TODO el bloque add+commit; skip silencioso con log, NO error). Espejo del early-return-con-log de session-end.js:63-66.

    En el `execSync` del auto-commit (:285), restringe el pathspec al subdirectorio de la skill en AMBOS pasos: el `git ... add` y el `git ... commit` deben llevar `-- .claude/skills/kodo-orchestrate/` (literal del criterio de éxito #1). Conserva `-c commit.gpgsign=false` y el mensaje `skill: orchestrator learnings ${date}`. Ajusta también el `git status --porcelain` (:270) para consultar el mismo subdirectorio `.claude/skills/kodo-orchestrate/` en vez de `.claude/skills/`, para que el "no changes to commit" sea coherente con el pathspec del commit.

    NO dejes en stop.js/launch.js ningún comentario que reproduzca verbatim la variable como texto suelto que confunda al lector; documenta el gate por concepto (marcador de sesión orquestadora).

    Sobre A2 (propagación shell→hook, confianza MEDIA): la env var se inyecta por el prefijo del command string; el modo de fallo si el shell del workspace no exporta el prefijo es SEGURO por diseño (D-06: sin la variable → skip, cero commits fantasma). La confirmación empírica en cmux+claude reales se registra en la verificación de fase (`<verification>`); NO construyas el fallback de marcador-en-fichero en este plan salvo que la verificación empírica falle (queda como candidato a gap-closure con `persistOrchestratorRef`, que ya existe).

    Extiende `test/hooks/stop-idempotency.test.js` (o añade un bloque describe nuevo) con un test que invoque `handleOrchestratorStop` indirectamente vía `runStopHook` con un input sin sesión tracked y `cwd = KODO_ROOT`: con `process.env.KODO_ORCHESTRATOR = '1'` alcanza el path de commit (usa un `execSyncFn`/git stub o un repo tmp para no commitear de verdad — sigue el patrón de aislamiento del fichero), y sin la variable NO alcanza el commit (skip). Restaura `process.env` en `afterEach`.
  </action>
  <verify>
    <automated>node --test test/hooks/stop-idempotency.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/hooks/stop-idempotency.test.js` pasa (incluye el nuevo test del gate: con `KODO_ORCHESTRATOR=1` se alcanza el commit; sin la variable, skip).
    - `grep -c 'KODO_ORCHESTRATOR=1' src/orchestrator/launch.js` ≥ 1 (la env var se inyecta en el command string).
    - `grep -F -- '.claude/skills/kodo-orchestrate/' src/hooks/stop.js` aparece en el `add` Y en el `commit` (pathspec completo en ambos pasos).
    - Comportamiento: en una sesión normal del repo kodo (sin la env var), `handleOrchestratorStop` retorna sin ejecutar `git commit` (skip silencioso con log en stderr).
    - No se toca la rama "existing" de `launchOrchestrator` (:197-204) ni el `--url`/`spawn` de ningún otro sitio.
  </acceptance_criteria>
  <done>El auto-commit del stop hook solo corre con `KODO_ORCHESTRATOR=1` presente y limita el pathspec a `.claude/skills/kodo-orchestrate/` en add y commit; la variable se inyecta al lanzar el workspace orquestador. Test verde.</done>
</task>

<task type="auto">
  <name>Task 2: HYG-04 — mover setColor/notify/nudge de Stop a SessionEnd (tras el backstop)</name>
  <files>src/hooks/stop.js, src/hooks/session-end.js, test/hooks/session-end.test.js, test/stop.test.js, test/stop-state-transition.test.js</files>
  <read_first>
    - src/hooks/stop.js (los tres efectos a mover: setColor :156-163, notify :165-171, nudge send :234-243; lo que Stop CONSERVA: markSessionStatus :189-207 y releaseGsdLock :216-223; `buildStopNudgeText` exportada :41-56)
    - src/hooks/session-end.js (el slot reservado tras el backstop: comentario :89-94, `runReviewBackstop` :95-120, `performTerminalCleanup` :150-156; `deps` shape :42-50; hoy NO importa cmux)
    - test/hooks/session-end.test.js (asserts del backstop DELIV-04; base para los nuevos asserts de efectos)
    - test/stop.test.js (source-hygiene: :35 "no toca estado de Plane"; :204-257 tests de `buildStopNudgeText` que importan la función DESDE stop.js — debe seguir exportada ahí)
    - test/stop-state-transition.test.js (asserts sobre el orden/estado en Stop)
    - .planning/phases/72-higiene-dx-y-verdad-documental/72-RESEARCH.md (§HYG-04 diagrama, Pitfalls #3/#4/#5, D-08/D-09)
  </read_first>
  <action>
    Elimina de `runStopHook` (stop.js) los tres bloques de efecto de cierre: `setColor(review)` (:156-163), `notify("… cerrada")` (:165-171) y el bloque de nudge al orquestador que usa `buildStopNudgeText` (:234-243). Conserva intactos, en el mismo orden: los guards de idempotencia (:129-149), el bloque `markSessionStatus(... 'idle' ...)` (:189-207) y el `releaseGsdLock` bajo `if (session.gsd)` (:216-223) (D-09). `buildStopNudgeText` PERMANECE exportada en stop.js (Pitfall #3 — la importan tests) — solo se mueve su CALLSITE.

    En `src/hooks/session-end.js`, importa `cmux` (`import * as cmux from '../cmux/client.js'`), `colorForStatus` (`../cmux/colors.js`) y `buildStopNudgeText` (`./stop.js`); añade `cmux` a `deps` con default lazy (`const cmuxClient = deps.cmux || cmux`) siguiendo el patrón DI de stop.js:120. Inserta los tres efectos DESPUÉS de `performTerminalCleanup` (:156), al FINAL de `runSessionEndHook`, cada uno en su propio try/catch (never-throws individual, D-08): setColor(review) sobre `session.workspace_ref`, notify de cierre con `session.task_ref`/`session.summary`, y el nudge (resolver el ref del orquestador vía `cmuxClient.listWorkspaces()` + el mismo match `/(workspace:\d+)\s+kodo-orchestrator/`, o reutiliza `findOrchestratorRef` de orchestrator/launch.js si está disponible sin acoplar de más; enviar `buildStopNudgeText(session)`). El orden LOCKED (D-08): guards → runReviewBackstop → session.end event → releaseGsdLock backstop → performTerminalCleanup → efectos HYG-04. Ningún efecto puede abortar la transición de estado ni los demás efectos.

    Actualiza los guardrail tests en el MISMO commit (Pitfall #4):
    - `test/hooks/session-end.test.js`: añade asserts de que setColor/notify/send ocurren en SessionEnd, DESPUÉS del backstop, y que un efecto que lanza NO aborta el resto ni el cleanup (never-throws). Inyecta el cmux stub vía `deps.cmux`.
    - `test/hooks/stop-idempotency.test.js` y `test/stop-state-transition.test.js`: ajusta cualquier assert que esperara setColor/notify/send en la PRIMERA invocación de Stop — Stop ya no los emite. Los asserts de "segunda invocación no llama setColor/send" siguen válidos (ahora la primera tampoco).
    - `test/stop.test.js`: los tests de `buildStopNudgeText` importada desde stop.js deben seguir verdes (la función no se movió).
  </action>
  <verify>
    <automated>node --test test/hooks/session-end.test.js test/hooks/stop-idempotency.test.js test/stop.test.js test/stop-state-transition.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/hooks/session-end.test.js test/hooks/stop-idempotency.test.js test/stop.test.js test/stop-state-transition.test.js` pasa.
    - Comportamiento: `runSessionEndHook` invoca setColor, notify y send (nudge) tras `performTerminalCleanup`; un stub de setColor que lanza no impide notify/send ni el cleanup.
    - Comportamiento: `runStopHook` (primera invocación, sesión viva) NO invoca setColor/notify/send; SÍ invoca `markSessionStatus('idle')` y (si gsd) `releaseGsdLock`.
    - `buildStopNudgeText` sigue exportada por `src/hooks/stop.js` (los tests de :204-257 de test/stop.test.js pasan sin cambiar el import).
    - Los efectos en session-end.js van DESPUÉS del `runReviewBackstop` (verificable por el orden del código y/o un assert de secuencia de llamadas en el test).
  </acceptance_criteria>
  <done>Los efectos de cierre (color review, notify, nudge) se disparan una sola vez al cierre real en SessionEnd, tras el backstop DELIV-04, cada uno never-throws; Stop solo deja el estado ligero. Los 4 tests guardrail verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → git (auto-commit) | El hook construye y ejecuta comandos git en el repo del dev; input no confiable = el árbol staged del dev |
| launchOrchestrator → shell del workspace cmux → hook `claude` | La env var marcadora cruza como prefijo del command string enviado por `cmux.send` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Tampering / Repudiation | `handleOrchestratorStop` auto-commit | high | mitigate | Gate `KODO_ORCHESTRATOR=1` cubre add+commit (D-06) + pathspec `-- .claude/skills/kodo-orchestrate/` en ambos pasos (D-07) — cero commits fantasma que arrastren staged ajeno |
| T-72-02 | Tampering | env var como marcador de rol | low | accept | No es secreto; no persiste en disco/`/status`/logs. Modo de fallo de propagación es seguro (sin var → skip). Confirmación empírica en `<verification>` |
| T-72-03 | Denial of Service | efectos cmux en SessionEnd | low | mitigate | Cada efecto en su try/catch (D-08); un fallo de cmux nunca aborta la transición de estado ni el cleanup terminal |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | Esta fase NO instala paquetes (invariante cross-milestone: cero nuevas deps npm) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/hooks/stop-idempotency.test.js test/hooks/session-end.test.js test/stop.test.js test/stop-state-transition.test.js` verde.
- Suite completa `npm test` verde antes del merge de wave.
- **Confirmación empírica (Open Question #1 / A2, human-check en dogfooding):** lanzar el orquestador real (`kodo orchestrate`), verificar que `process.env.KODO_ORCHESTRATOR === '1'` llega al proceso del hook (el auto-commit dispara SOLO en la sesión orquestadora; una sesión normal del repo ya no genera commits fantasma). Si la propagación falla → gap-closure con el marcador-en-fichero vía `persistOrchestratorRef`.
</verification>

<success_criteria>
- El stop hook solo auto-commitea con `KODO_ORCHESTRATOR=1` y pathspec `.claude/skills/kodo-orchestrate/` en add y commit (criterio de éxito #1 de la fase).
- Color review, notify y nudge se disparan en `SessionEnd`, no en `Stop`, DESPUÉS del backstop (criterio de éxito #2 de la fase, parte HYG-04).
- Ningún guardrail test roto.
</success_criteria>

## Artifacts this plan produces

**Símbolos/comportamientos AÑADIDOS:**
- Gate `KODO_ORCHESTRATOR === '1'` en `handleOrchestratorStop` (src/hooks/stop.js) — marcador de sesión orquestadora.
- Prefijo de entorno `KODO_ORCHESTRATOR=1` en el `claudeCmd` de `launchOrchestrator` (src/orchestrator/launch.js).
- Pathspec `-- .claude/skills/kodo-orchestrate/` en el `git add` y el `git commit` del auto-commit.
- Efectos de cierre (setColor/notify/nudge) en `runSessionEndHook` (src/hooks/session-end.js) + `cmux` como nueva `deps` inyectable de ese hook + imports de `cmux`/`colorForStatus`/`buildStopNudgeText`.

**Símbolos/comportamientos ELIMINADOS:**
- Los tres bloques de efecto de cierre (setColor :156-163, notify :165-171, nudge :234-243) de `runStopHook` (src/hooks/stop.js).
- El add de `.claude/skills/` entero sin pathspec (reemplazado por el subdirectorio de la skill).

**Sin cambios (conservado):** `buildStopNudgeText` exportada en stop.js; `markSessionStatus('idle')` y `releaseGsdLock` en Stop; el orden del backstop DELIV-04 en SessionEnd.

<output>
Create `.planning/phases/72-higiene-dx-y-verdad-documental/72-01-SUMMARY.md` when done
</output>
