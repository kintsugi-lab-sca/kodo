---
phase: 70-concurrencia-y-ciclo-de-vida-de-procesos
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - src/session/state.js
  - src/session/reconcile.js
  - src/server.js
  - test/state/state-writers-concurrency.test.js
autonomous: true
requirements: [CONC-01]

must_haves:
  truths:
    - "addSession / updateSession / removeSession run inside withStateLock: the state is RE-READ fresh under the lock before mutating, then saved (D-02)."
    - "N concurrent processes each mutating a DIFFERENT session in state.json lose no writes — the final state contains every session (no last-write-wins clobber)."
    - "runReconcileTick's saveState participates in the same lock WITHOUT holding the lock across host async I/O (pgrep/listWorkspaces) — snapshot outside, apply inside (Pitfall 1)."
    - "The false 'sole writer of state.json' comment in server.js is corrected to the truth (N writers coordinated by withStateLock) in this same commit (D-04)."
  artifacts:
    - path: "src/session/state.js"
      provides: "withStateLock(mutator) + the 3 mutators wrapped in it"
      contains: "withStateLock"
    - path: "test/state/state-writers-concurrency.test.js"
      provides: "Real N-process writer race asserting zero lost writes"
      contains: "sessions"
  key_links:
    - from: "src/session/state.js withStateLock"
      to: "src/session/state-lock.js"
      via: "import { withFileLock } — the primitive from Plan 01"
      pattern: "withFileLock"
    - from: "src/session/reconcile.js runReconcileTick"
      to: "src/session/state.js withStateLock"
      via: "apply pure reconcileTick derivation inside the lock; host snapshot taken outside"
      pattern: "reconcileTick"
  prohibitions:
    - "MUST NOT hold the lock across runReconcileTick's `await host.listWorkspaces()` or pgrep calls (serializes host poll, trips TTL/steal) — Pitfall 1."
    - "MUST NOT change reconcile's state machine or make any writer other than reconcileTick write the `alive` field (invariant: reconcileTick is the sole writer of `alive`)."
    - "MUST NOT leave the literal sole-writer claim in server.js (verify the corrected comment names withStateLock)."
---

<objective>
Apply `withStateLock(fn)` (D-02) over the complete set of `state.json` write points so cross-process mutations stop clobbering each other (audit T1), and correct the false single-writer comment in `server.js` (D-04) in the same commit.

Purpose: The ~13 cross-process call-sites all funnel through 3 mutators in `state.js` plus reconcile's saveState. Wrapping those 4 points in the Plan-01 primitive coordinates all of them at once — load→mutate→save under an O_EXCL lock with a fresh re-read (the anti-clobber key).
Output: Coordinated state writes proven by a real N-process writer race with zero lost writes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-CONTEXT.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-RESEARCH.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-PATTERNS.md

# The primitive this plan consumes (Plan 01 output):
@src/session/state-lock.js
# The writers + reconcile to wrap:
@src/session/state.js
@src/session/reconcile.js
</context>

## Artifacts this phase produces (Plan 02 scope)

New symbols introduced by THIS plan (exclude from drift verification):
- `src/session/state.js`: new export `withStateLock(mutator)` + a `STATE_LOCK_PATH` constant; `addSession`/`updateSession`/`removeSession` bodies re-routed through it.
- `src/session/reconcile.js`: `runReconcileTick` now applies its derivation via `withStateLock` (snapshot-outside / apply-inside).
- New test file: `test/state/state-writers-concurrency.test.js`.
- `src/server.js`: corrected comment (no new symbol).

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing N-process writer race test (RED)</name>
  <files>test/state/state-writers-concurrency.test.js, test/helpers/lock-race-child.mjs</files>
  <read_first>
    - test/state/save-state-atomic.test.js (HOME-isolation + dynamic import AFTER HOME set)
    - test/gsd-concurrency.test.js (mkdtemp sandbox + real child spawn)
    - src/session/state.js:264-315 (addSession/removeSession/updateSession current shape)
  </read_first>
  <action>
    Create test/state/state-writers-concurrency.test.js RED-first. Sandbox HOME via mkdtempSync; write a seed state.json (empty sessions) into the isolated ~/.kodo. Extend test/helpers/lock-race-child.mjs with a `--kind writer` mode: it dynamic-imports ../../src/session/state.js AFTER HOME is set (via env passed from parent) and calls `addSession('task-'+idx, {session_id:'s'+idx, status:'running'})` for its assigned index, waiting on the `go` barrier first.

    Spawn N=10 writer children, each adding a DIFFERENT task id, all racing against the same isolated state.json. After all children close, load the final state and assert `Object.keys(state.sessions).length === 10` and every `task-<i>` is present — i.e. NO writer's addSession was clobbered by a concurrent load→mutate→save. Today (mutators load OUTSIDE any lock) this loses writes → the test is RED until Task 2.

    Also add one focused unit assertion: `withStateLock` re-reads state under the lock (spy/inject to prove loadState is called INSIDE the acquired lock, not before).
  </action>
  <verify>
    <automated>node --test test/state/state-writers-concurrency.test.js 2>&1 | grep -Eq 'fail|not ok' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Test spawns real child processes against one isolated state.json and asserts zero lost writes across 10 concurrent addSession calls.
    - It is RED now (lost writes) and will go GREEN after Task 2.
    - No touch of the real ~/.kodo (HOME isolated).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: withStateLock(mutator) + wrap the 3 mutators + fix server.js comment (CONC-01, D-02, D-04)</name>
  <files>src/session/state.js, src/server.js</files>
  <read_first>
    - src/session/state-lock.js (withFileLock signature from Plan 01)
    - src/session/state.js:231-315 (loadState, saveState, addSession, removeSession, updateSession)
    - src/server.js:842 (the comment to correct — grep for it, do NOT trust a line number)
  </read_first>
  <behavior>
    - withStateLock(fn) acquires the state lock, calls loadState() FRESH inside the lock, runs fn(state), saveState(result ?? state), releases in finally; on lock-timeout returns the primitive's {ok:false} and emits a warn (no throw, no partial write).
    - addSession/updateSession/removeSession produce identical results to before under no contention; under contention they never clobber a concurrent writer.
    - The 10-writer race test passes.
  </behavior>
  <action>
    (a) In src/session/state.js, define `const STATE_LOCK_PATH = STATE_PATH + '.lock'` (one global lock for the single state file — RESEARCH Open Question 3) and import `withFileLock` from './state-lock.js'. Export `withStateLock(mutator)`: `return withFileLock(STATE_LOCK_PATH, () => { const state = loadState(); const next = mutator(state); saveState(next ?? state); })`. The RE-READ of loadState() inside the callback is the anti-clobber key (D-02) — do not read state before the lock.

    (b) Rewrite addSession/removeSession/updateSession (state.js:264-315) so each performs its mutation via withStateLock. Move the existing body into the mutator callback operating on the freshly-loaded `state`: addSession → `withStateLock(state => { state.sessions[taskId] = session; })`; removeSession → delete under the lock; updateSession → find+merge under the lock. Preserve the existing logger calls and return shapes. Keep saveState (state.js:242, tmp+rename) UNCHANGED as the write step inside the lock.

    (c) Correct the false comment in src/server.js at the line grep locates ("... el ÚNICO escritor de state.json ..."). Replace it with the truth: that state.json now has multiple coordinated writers serialized by `withStateLock` (the O_EXCL state lock), and the dashboard remains a read-only HTTP client. Name `withStateLock` explicitly in the new comment so the corrected truth is greppable. Do this in THIS commit (D-04).
  </action>
  <verify>
    <automated>node --test test/state/state-writers-concurrency.test.js && grep -c 'withStateLock' src/server.js | grep -qv '^0$' && echo COMMENT-OK</automated>
  </verify>
  <acceptance_criteria>
    - withStateLock exported from state.js and imported into the 3 mutators; loadState is called INSIDE the lock (test proves it).
    - 10-process writer race GREEN: all 10 sessions present, zero lost writes.
    - server.js comment names withStateLock and no longer claims a single writer (grep: corrected text present).
    - Existing state.js tests (migration, save-state-atomic) still green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route runReconcileTick's save through withStateLock without holding across async I/O (CONC-01, Pitfall 1)</name>
  <files>src/session/reconcile.js, test/session/reconcile-lock.test.js</files>
  <read_first>
    - src/session/reconcile.js:308-353 (runReconcileTick: host snapshot → loadState → process_alive refresh → reconcileTick pure → saveState at :351)
    - src/session/reconcile.js:117 (reconcileTick is pure, takes state as arg), :250-259 (applyLiveFields — sole writer of `alive`)
    - 70-RESEARCH.md Pitfall 1 (do NOT hold lock across async host I/O)
  </read_first>
  <behavior>
    - runReconcileTick still calls `await host.listWorkspaces()` and pgrep OUTSIDE any lock (host snapshot unchanged).
    - The pure derivation + save is applied INSIDE withStateLock, re-reading state fresh so a concurrent hook write is not clobbered.
    - reconcileTick remains the sole writer of `alive`; no state-machine change.
    - A test proves the lock is NOT held during the host snapshot (e.g. host.listWorkspaces resolves while the state lock is free) and IS held during the save.
  </behavior>
  <action>
    Refactor runReconcileTick (reconcile.js:308-353) to the snapshot-outside / apply-inside pattern:
    1. Keep the `try { liveRefs = await host.listWorkspaces() } catch {...}` block exactly as-is (outside the lock).
    2. Replace the `let state = loadState(); ...process_alive refresh...; const {state:newState} = reconcileTick(...); if (newState !== state) saveState(newState);` tail with a `withStateLock(state => { ...re-derive on the fresh state... return derived; })` call. Inside the callback: run the SAME process_alive refresh loop and `reconcileTick(state, liveRefs, {debounceStore, tick, now: now(), logger})` on the freshly-loaded `state`, and return `newState` when it differs (return the original when unchanged so saveState is a no-op-equivalent — or short-circuit: only mutate when newState !== state). The derivation is pure and already takes state as an argument (reconcile.js:117), so re-running it on the fresh state under the lock is safe and idempotent.
    3. Keep the events return value (`return events`) from the computed tick. Compute `events` from the derivation performed inside the lock.
    4. Do NOT change applyLiveFields or deriveTarget — `alive` stays reconcileTick-only.

    Add test/session/reconcile-lock.test.js: inject a host whose listWorkspaces asserts the state lock file does NOT exist at snapshot time (proving the lock isn't held during async I/O), and a saveState spy proving the save ran while the lock was held. Assert reconcile still transitions a dead session (existing behavior preserved).
  </action>
  <verify>
    <automated>node --test test/session/reconcile-lock.test.js test/session/reconcile.test.js 2>/dev/null || node --test test/session/reconcile-lock.test.js</automated>
  </verify>
  <acceptance_criteria>
    - runReconcileTick applies its save via withStateLock; grep confirms no `await` inside the withStateLock callback.
    - New reconcile-lock test proves lock free during host snapshot, held during save.
    - Existing reconcile suite still green (no state-machine regression; `alive` still reconcile-only).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| independent process ↔ state.json | hooks / CLI / dispatcher / reconcile all mutate one shared state.json concurrently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-04 | Tampering | state.json concurrent writers | high | mitigate | withStateLock re-reads state fresh under an O_EXCL lock before mutate+save (D-02). Proven by 10-process no-lost-write race. |
| T-70-05 | Denial of Service | lock held across reconcile async host I/O | medium | mitigate | Snapshot host OUTSIDE the lock; apply pure derivation INSIDE (Pitfall 1). Reconcile tick keeps the lock only for the sub-ms save. |
| T-70-SC | Tampering | npm installs | n/a | accept | Zero new deps (milestone invariant). |
</threat_model>

<verification>
- `node --test test/state/state-writers-concurrency.test.js test/session/reconcile-lock.test.js` green; non-flaky over 3 runs.
- `grep 'withStateLock' src/server.js` shows the corrected comment; no remaining single-writer claim.
- `npm test` full suite green.
</verification>

<success_criteria>
- CONC-01: the 3 state.js mutators + reconcile's save all go through withStateLock; N concurrent writers lose no writes; the false server.js comment is corrected in the same commit (D-04).
</success_criteria>

<output>
Create `.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-02-SUMMARY.md` when done. Note the STATE_LOCK_PATH value and confirm reconcile keeps `alive` as its sole write.
</output>
