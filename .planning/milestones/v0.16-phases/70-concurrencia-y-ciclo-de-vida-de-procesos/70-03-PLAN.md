---
phase: 70-concurrencia-y-ciclo-de-vida-de-procesos
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/manager.js
  - src/daemon/run.js
  - src/daemon/lifecycle.js
  - src/config.js
  - test/session/max-parallel-alive.test.js
  - test/daemon/run.test.js
  - test/daemon/lifecycle.test.js
  - test/config-migration-atomic.test.js
autonomous: true
requirements: [CONC-03, CONC-04, CONC-05, CONC-07]

must_haves:
  truths:
    - "A zombie session (reconcile marked alive:false because its cmux TAB died) no longer occupies a max_parallel slot — the gate counts only status==='running' AND alive!==false (D-05)."
    - "teardown deletes ~/.kodo/kodo.pid ONLY when payload.pid === process.pid — a process never removes another daemon's PID file (D-09)."
    - "The daemon PID stays written pre-bind (D-10 REVISED — 66-07 fix preserved); a failed boot still leaves no lying PID because the fail-path teardown(1) removes it."
    - "Before the SIGKILL fallback in stopDaemon, payload.started_at is compared to the real process start via `ps -o lstart=` (LC_ALL=C); on mismatch the kill is ABORTED; if ps is absent/unparseable, degrade safe (do not kill, warn) (D-11)."
    - "migrateConfigIfNeeded persists the migrated config via writeFileAtomic (tmp+rename) — a crash mid-migration cannot truncate config.json (D-14)."
  artifacts:
    - path: "src/session/manager.js"
      provides: "max_parallel gate filtering by alive"
      contains: "alive"
    - path: "src/daemon/lifecycle.js"
      provides: "anti-PID-reuse guard before SIGKILL"
      contains: "lstart"
    - path: "test/config-migration-atomic.test.js"
      provides: "HOME-isolated proof migration is atomic (no .tmp residue, no truncation)"
  key_links:
    - from: "src/session/manager.js gate"
      to: "reconcile-written `alive` field"
      via: "gate READS alive (never writes it); zombie with alive:false is skipped"
      pattern: "alive !== false"
    - from: "src/daemon/lifecycle.js stopDaemon"
      to: "ps -o lstart="
      via: "execFileSync('ps',[...],{env:{...,LC_ALL:'C'}}) compared to payload.started_at before SIGKILL"
      pattern: "lstart"
  prohibitions:
    - "MUST NOT move the daemon PID write to post-bind (regresses gap-closure 66-07 cold-spawn timeout) — keep pre-bind, document that fail-path teardown guarantees no lying PID (D-10 REVISED)."
    - "MUST NOT write the `alive` field from the manager gate — the gate only reads it (reconcile stays sole writer)."
    - "MUST NOT SIGKILL when the start-time check is unverifiable or mismatched — degrade safe, warn (D-11)."
    - "The zombie test MUST drive TAB death (host stops reporting the workspace_ref), NOT just kill -9 the process (D-06b / Pitfall 2)."
---

<objective>
Harden the process/PID/config lifecycle with four surgical guards that do NOT depend on the new lock primitive (all Wave 1): a zombie frees its `max_parallel` slot (CONC-03/D-05), a process deletes only its own PID file (CONC-04/D-09), a recycled PID is never SIGKILLed (CONC-05/D-11), and config migration is atomic (CONC-07/D-14).

Purpose: These close audit A4/A5/A6/M16 — the most damaging capacity leak (zombie holding a slot up to 30 days) plus the "kill/delete the wrong process" and "truncated config" hazards. Each is a small diff with a focused test.
Output: Defensive lifecycle guards, each proven by an automated test; the daemon PID ordering (66-07) explicitly preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-CONTEXT.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-RESEARCH.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-PATTERNS.md

@src/daemon/run.js
@src/daemon/lifecycle.js
@src/config.js
</context>

## Artifacts this phase produces (Plan 03 scope)

New symbols / modified behavior introduced by THIS plan (exclude from drift verification):
- `src/session/manager.js`: max_parallel gate at :178 gains `&& s.alive !== false` (or an `isSchedulable(s)` helper — Claude's discretion).
- `src/daemon/run.js`: teardown reads the PID payload and removes it only if `payload.pid === process.pid`; a comment documents the pre-bind write is preserved (D-10 REVISED).
- `src/daemon/lifecycle.js`: new internal `processStartMatches(pid, startedAtISO, toleranceMs)` guarding the SIGKILL in stopDaemon.
- `src/config.js`: migrateConfigIfNeeded now calls the existing `writeFileAtomic`.
- New tests: `test/session/max-parallel-alive.test.js`, `test/config-migration-atomic.test.js`; extended `test/daemon/run.test.js`, `test/daemon/lifecycle.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: teardown deletes only its OWN kodo.pid; preserve pre-bind PID write (CONC-04, D-09, D-10 REVISED)</name>
  <files>src/daemon/run.js, test/daemon/run.test.js</files>
  <read_first>
    - src/daemon/run.js:115-123 (teardown — removePidFileFn('kodo') unconditional), :142-145 (pre-bind writePidFile), :152-166 (fail-path teardown(1))
    - src/cli/polling-daemon.js:119-129 (readPidFile shape-check {pid, started_at})
    - 70-RESEARCH.md Pitfall 3 (D-10 vs 66-07 — CRITICAL) and §Assumptions A1
  </read_first>
  <behavior>
    - teardown removes ~/.kodo/kodo.pid when the payload's pid === process.pid.
    - teardown does NOT remove the PID file when it belongs to a different pid (foreign daemon) — no-op, no throw.
    - The PID write stays BEFORE `await startServerFn` (pre-bind); a boot that throws still ends with no kodo.pid (fail-path teardown(1) removes it).
  </behavior>
  <action>
    (a) In src/daemon/run.js teardown (lines 115-123), replace the unconditional `try { removePidFileFn('kodo'); } catch {}` with an ownership-guarded removal: read the current PID payload (inject a `_readPidFile` dep defaulting to readPidFile from polling-daemon.js, same DI style as `_writePidFile`/`_removePidFile` already used), and only call `removePidFileFn('kodo')` when `payload && payload.pid === process.pid`. Wrap in try/catch so teardown never throws (idempotent, matches existing style). Note: in the normal daemon the payload.pid IS process.pid (it wrote it), so behavior is unchanged for the happy path; the guard prevents a second/foreign process from deleting a live daemon's PID.

    (b) Do NOT move the writePidFile at run.js:142 (D-10 REVISED). Keep it pre-bind. UPDATE the surrounding comment to state explicitly: the "no lying PID on bind failure" invariant (audit A5) is guaranteed by the fail-path teardown(1) removing the PID — NOT by post-bind ordering — and that moving it post-bind would regress gap-closure 66-07 (cold-spawn `kodo up` PID-wait timeout). Keep the existing 66-07 rationale intact.

    (c) Extend test/daemon/run.test.js: inject `_readPidFile` returning a payload with a FOREIGN pid and assert `_removePidFile` is NOT called on teardown; inject one with `pid === process.pid` and assert it IS called. Add a test where `_startServer` rejects (bind fail) and assert teardown(1) still calls removePidFile (PID cleaned) — proving the pre-bind write leaves no lying PID.
  </action>
  <verify>
    <automated>node --test test/daemon/run.test.js</automated>
  </verify>
  <acceptance_criteria>
    - teardown removes kodo.pid only when payload.pid === process.pid (foreign pid → no removal); tests prove both branches.
    - writePidFile remains pre-bind (grep: the write precedes `await startServerFn`); comment documents fail-path cleanup as the A5 guarantee.
    - Bind-failure test: teardown(1) removes the PID (no stale kodo.pid).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: anti-PID-reuse — compare ps -o lstart= before SIGKILL (CONC-05, D-11)</name>
  <files>src/daemon/lifecycle.js, test/daemon/lifecycle.test.js</files>
  <read_first>
    - src/daemon/lifecycle.js:163-195 (stopDaemon: SIGTERM → 5s isAlive loop → SIGKILL at :184 → removePid)
    - 70-RESEARCH.md §Code Examples (processStartMatches reference, lines ~375-407) and Pitfall 4 (locale — force LC_ALL=C, ~5-10s tolerance, resolution 1s)
  </read_first>
  <behavior>
    - When the real process start (ps -o lstart=) is within tolerance of payload.started_at → SIGKILL proceeds (our process).
    - When it mismatches beyond tolerance (PID recycled) → SIGKILL is SKIPPED, a warn is logged, stopDaemon still returns cleanly.
    - When ps is absent / exits non-zero / output unparseable (NaN) → SIGKILL is SKIPPED (degrade safe), warn logged.
    - SIGTERM path and the ESRCH/stale cleanup are unchanged.
  </behavior>
  <action>
    Add an internal helper `processStartMatches(pid, payloadStartedAtISO, { toleranceMs = 8000, _exec } = {})` in lifecycle.js: run `_exec` (default `execFileSync`) as `('ps', ['-o','lstart=','-p', String(pid)], { encoding:'utf8', env: { ...process.env, LC_ALL: 'C' } })`, trim, `Date.parse` it; on any throw return `{ verifiable:false }`; if either parsed date is not finite return `{ verifiable:false }`; else return `{ verifiable:true, match: Math.abs(real - claimed) <= toleranceMs }`. LC_ALL=C stabilizes the locale-dependent format (Pitfall 4); tolerance ~8s absorbs lstart's 1s resolution + the started_at write skew (Claude's discretion on the exact value).

    In stopDaemon, guard the SIGKILL at line 184: before `kill(payload.pid, 'SIGKILL')`, compute `const chk = processStartMatches(payload.pid, payload.started_at, { _exec: deps._exec })`. If `chk.verifiable && !chk.match` → log warn `daemon.sigkill.aborted` { pid, reason:'pid-reuse-suspected' } and SKIP the kill. If `!chk.verifiable` → log warn `daemon.sigkill.unverifiable` { pid } and SKIP the kill (degrade safe — do NOT kill by default, D-11). Only when `chk.verifiable && chk.match` do the SIGKILL. Add an injectable `_exec` dep (default execFileSync) to deps for testing, mirroring the existing `_kill`/`_isPidAlive` DI. Keep `removePid(name)` after the block as today.

    Extend test/daemon/lifecycle.test.js with injected `_exec`: (1) match within tolerance → `_kill` called with SIGKILL; (2) mismatch (real start hours off) → SIGKILL NOT called, warn emitted; (3) `_exec` throws (ps absent) → SIGKILL NOT called, warn emitted. Keep the process alive past the SIGTERM window via `_isPidAlive` so the SIGKILL branch is reached.
  </action>
  <verify>
    <automated>node --test test/daemon/lifecycle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - processStartMatches forces LC_ALL=C and degrades safe on throw/NaN.
    - Three tests prove: match→SIGKILL, mismatch→abort+warn, ps-absent→abort+warn.
    - SIGTERM path and ESRCH stale-cleanup unchanged; full lifecycle suite green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: zombie frees its slot (gate filters alive) + atomic config migration (CONC-03/D-05/D-06b, CONC-07/D-14)</name>
  <files>src/session/manager.js, src/config.js, test/session/max-parallel-alive.test.js, test/config-migration-atomic.test.js</files>
  <read_first>
    - src/session/manager.js:178 (`listSessions().filter((s) => s.status === 'running')`)
    - src/session/reconcile.js:250-259 (applyLiveFields — alive derived from TAB liveness, sole writer)
    - src/config.js:100-104 (writeFileAtomic), :146-153 (migrateConfigIfNeeded — direct writeFileSync at :150)
    - test/state/save-state-atomic.test.js (HOME-isolation + dynamic import pattern for the config test)
    - 70-RESEARCH.md Pitfall 2 (tab-alive vs process-alive — the zombie test MUST drive TAB death)
  </read_first>
  <behavior>
    - The max_parallel gate counts a session only when status==='running' AND alive !== false; a session reconcile marked alive:false does not consume a slot.
    - `alive !== false` (not `=== true`) so legacy sessions without the field still count (no regression).
    - migrateConfigIfNeeded writes the migrated config via writeFileAtomic; after a simulated crash between tmp write and rename, config.json is intact (untouched original), no .tmp residue leaks as config.
  </behavior>
  <action>
    (a) src/session/manager.js:178 (CONC-03/D-05): change the gate to `listSessions().filter((s) => s.status === 'running' && s.alive !== false)`. Optionally extract an `isSchedulable(s)` helper (Claude's discretion) — but per D-06 this is PURELY a change to the gate filter: no new field, no reconcile state-machine change, and the gate only READS `alive`, never writes it (reconcile stays sole writer). Update the error message's `active` list accordingly (it already maps over the filtered array).

    (b) src/config.js:150 (CONC-07/D-14): replace the direct `writeFileSync(CONFIG_PATH, JSON.stringify(newConfig, null, 2) + '\n')` in migrateConfigIfNeeded with `writeFileAtomic(CONFIG_PATH, JSON.stringify(newConfig, null, 2) + '\n')` (the tmp+rename helper already at config.js:100). One-line swap. Leave the `.bak` write as-is (the risk is a truncated config.json, not the backup).

    (c) test/session/max-parallel-alive.test.js (CONC-03): unit test the gate. Seed listSessions (inject or HOME-isolated state) with sessions: 2 running+alive:true, 1 running+alive:false (the zombie), max_parallel=2. Assert launchWorkItem does NOT throw "Max parallel reached" because the zombie is excluded (only 2 alive count == limit? use 1 running+alive:true + 1 running+alive:false with max_parallel=2 and assert a launch is admitted). CRITICAL (D-06b/Pitfall 2): the zombie's alive:false must be produced the REAL way — either seed the session record with alive:false directly, or drive reconcile with a host fixture whose listWorkspaces OMITS the zombie's workspace_ref so reconcile derives alive:false. Document in the test that a bare `kill -9` on the process (tab still alive) would NOT flip alive — the gate correctly still counts that case.

    (d) test/config-migration-atomic.test.js (CONC-07): HOME-isolated (mkdtempSync + dynamic import AFTER HOME set). Write a v1 config (no `providers` key) to config.json, call loadConfig() (triggers migrateConfigIfNeeded), assert the result has `providers`, config.json is valid JSON (not truncated), config.json.bak exists, and no orphan `config.json.tmp` remains. Optionally inject a failing rename to assert the original config.json is untouched on mid-migration failure.
  </action>
  <verify>
    <automated>node --test test/session/max-parallel-alive.test.js test/config-migration-atomic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Gate filters `status==='running' && alive !== false`; zombie (alive:false) frees its slot; legacy no-alive sessions still count.
    - The zombie test drives alive:false via TAB-death semantics (host omits the ref) or a directly-seeded alive:false record — documented per D-06b, NOT a bare process kill.
    - migrateConfigIfNeeded uses writeFileAtomic; migration test proves atomicity (valid JSON, no .tmp residue).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kodo process ↔ OS process table | stop/teardown act on PIDs that the OS may recycle; the PID file is a claim about a process that may no longer be ours. |
| migration ↔ config.json on disk | a crash mid-migration can corrupt the single config file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-06 | Denial of Service | stopDaemon SIGKILL vs recycled PID | high | mitigate | Compare `ps -o lstart=` (LC_ALL=C) to payload.started_at before SIGKILL; abort on mismatch; degrade safe when unverifiable (CONC-05/D-11). |
| T-70-07 | Denial of Service | teardown removing a foreign daemon's PID file | high | mitigate | Remove kodo.pid only if `payload.pid === process.pid` (CONC-04/D-09). |
| T-70-08 | Tampering / Integrity | truncated config.json on crash | medium | mitigate | migrateConfigIfNeeded via writeFileAtomic tmp+rename (CONC-07/D-14). |
| T-70-09 | Denial of Service (capacity) | zombie holding a max_parallel slot | high | mitigate | Gate filters by `alive !== false` so a reconcile-dead session frees its slot (CONC-03/D-05). |
| T-70-SC | Tampering | npm installs | n/a | accept | Zero new deps (milestone invariant). |
</threat_model>

<verification>
- `node --test test/daemon/run.test.js test/daemon/lifecycle.test.js test/session/max-parallel-alive.test.js test/config-migration-atomic.test.js` all green.
- `grep -n "alive !== false" src/session/manager.js` present; `grep -n "writeFileAtomic" src/config.js` present in migrateConfigIfNeeded; `grep -n "LC_ALL" src/daemon/lifecycle.js` present.
- writePidFile in run.js still precedes `await startServerFn` (pre-bind preserved).
- `npm test` full suite green.
</verification>

<success_criteria>
- CONC-03: zombie (alive:false via TAB death) frees its max_parallel slot; gate reads alive, reconcile stays sole writer.
- CONC-04: teardown removes only its own kodo.pid; PID stays pre-bind with fail-path cleanup documented (D-10 REVISED — 66-07 preserved).
- CONC-05: ps -o lstart= compared before SIGKILL; mismatch/unverifiable → abort + warn.
- CONC-07: migrateConfigIfNeeded atomic via writeFileAtomic.
</success_criteria>

<output>
Create `.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-03-SUMMARY.md` when done. Record the SIGKILL tolerance value chosen and confirm the pre-bind PID ordering was NOT changed.
</output>
