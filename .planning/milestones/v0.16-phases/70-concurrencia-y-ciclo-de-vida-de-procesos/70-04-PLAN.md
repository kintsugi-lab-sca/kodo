---
phase: 70-concurrencia-y-ciclo-de-vida-de-procesos
plan: 04
type: execute
wave: 2
depends_on: ["70-01", "70-03"]
files_modified:
  - src/daemon/lifecycle.js
  - src/triggers/dispatcher.js
  - src/gsd/doctor.js
  - test/daemon/polling-start-race.test.js
  - test/dispatcher-dedup-crossproc.test.js
  - test/helpers/lock-race-child.mjs
autonomous: false
requirements: [CONC-06, CONC-08, CONC-09]

must_haves:
  truths:
    - "Two concurrent `polling start` (startDaemon) against the same PID name start exactly ONE daemon — the second sees the O_EXCL start-lock held, reports already-starting/started, and exits clean (D-12)."
    - "Two processes dispatching the SAME non-GSD task_id result in exactly ONE launch — the second acquires no per-task_id lock and returns already_active (cross-process mirror of the in-process inFlight guard) (D-13)."
    - "The real on-disk location of live GSD worktrees is verified and documented; if code analysis shows doctor.js scans a dead directory (.bg-shell) while worktrees live in .claude/worktrees, the discrepancy is corrected or explicitly documented as deferred (D-15/CONC-09)."
  artifacts:
    - path: "src/daemon/lifecycle.js"
      provides: "O_EXCL start-lock around the polling-start spawn decision"
      contains: "withFileLock"
    - path: "src/triggers/dispatcher.js"
      provides: "per-task_id cross-process dedup lock on the non-GSD lane"
      contains: "dispatch-"
    - path: "test/daemon/polling-start-race.test.js"
      provides: "real 2-process polling-start race → one daemon"
    - path: "test/dispatcher-dedup-crossproc.test.js"
      provides: "real 2-process non-GSD dispatch race → one launch"
  key_links:
    - from: "src/daemon/lifecycle.js startDaemon"
      to: "src/session/state-lock.js withFileLock"
      via: "acquire O_EXCL start-lock around pre-flight+spawn; second EEXIST → already-starting"
      pattern: "withFileLock|acquireLock"
    - from: "src/triggers/dispatcher.js non-GSD lane"
      to: "src/session/state-lock.js acquireLock"
      via: "per-task_id lock ~/.kodo/locks/dispatch-<task_id>.lock; acquire before launch, release in finally"
      pattern: "dispatch-"
  prohibitions:
    - "MUST NOT add a new lockfile implementation — reuse the Plan 01 primitive (state-lock.js)."
    - "MUST NOT touch the GSD lane's per-repo lock (acquireGsdLock stays on projectPath, WT-03 invariant) — the new dedup lock is ONLY for the non-GSD lane, keyed by task_id."
    - "CONC-09 MUST NOT silently 'fix' the worktree path by inference — the correction requires empirical confirmation (live GSD session) or is documented as deferred with the code analysis delivered (D-15)."
---

<objective>
Wire the two remaining consumers of the Plan-01 lock primitive — a `polling start` O_EXCL lock so two concurrent starts never spawn two daemons (CONC-06/D-12), and a per-`task_id` cross-process dedup lock on the non-GSD dispatch lane (CONC-08/D-13) — and close the long-standing worktree-location discrepancy empirically (CONC-09/D-15).

Purpose: CONC-06/08 close audit M20/M17 (check-then-spawn and in-process-only dedup) by reusing the shared primitive rather than duplicating lock logic. CONC-09 resolves M13 (doctor scans `.bg-shell` while real GSD worktrees live in `.claude/worktrees`) with verification, not inference.
Output: Both consumers proven by real 2-process race tests; the worktree truth documented (and corrected if analysis is conclusive), with a deferrable human confirmation on a live GSD session.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-CONTEXT.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-RESEARCH.md
@.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-PATTERNS.md

# The primitive this plan consumes (Plan 01 output):
@src/session/state-lock.js
@src/daemon/lifecycle.js
@src/triggers/dispatcher.js
@src/gsd/doctor.js
</context>

## Artifacts this phase produces (Plan 04 scope)

New symbols / modified behavior introduced by THIS plan (exclude from drift verification):
- `src/daemon/lifecycle.js`: startDaemon acquires an O_EXCL start-lock (path e.g. `~/.kodo/{daemon-name}.start.lock`) around the pre-flight+spawn; a new discriminated result field (e.g. `alreadyStarting:true`) for the loser.
- `src/triggers/dispatcher.js`: non-GSD lane acquires a per-task_id lock via the primitive (path `~/.kodo/locks/dispatch-<task_id>.lock`), released in the existing finally; injectable via deps for tests.
- `src/gsd/doctor.js`: possibly updated to scan `computeRealWorktreePath` (`.claude/worktrees`) IF empirical/code analysis is conclusive; otherwise a documented note.
- New tests: `test/daemon/polling-start-race.test.js`, `test/dispatcher-dedup-crossproc.test.js`; extended `test/helpers/lock-race-child.mjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: O_EXCL lock around polling start — two starts, one daemon (CONC-06, D-12)</name>
  <files>src/daemon/lifecycle.js, test/daemon/polling-start-race.test.js, test/helpers/lock-race-child.mjs</files>
  <read_first>
    - src/daemon/lifecycle.js:86-145 (startDaemon: Windows-refuse → pre-flight readPid+isAlive at :110-118 → detached spawn at :120 → bounded wait at :135-140)
    - src/session/state-lock.js (acquireLock/releaseLock/withFileLock from Plan 01)
    - 70-RESEARCH.md Pattern 6 (the check-then-spawn TOCTOU)
  </read_first>
  <behavior>
    - Two concurrent startDaemon(name) with no daemon running: exactly one performs the detached spawn; the other observes the start-lock held and returns a clean "already starting" result (no second spawn, no second PID file).
    - When a daemon is already running (pre-flight sees alive), behavior is unchanged (already-running short-circuit before/inside the lock).
    - The start-lock is released after the spawn decision resolves (finally), and is stealable if a prior starter died mid-spawn (primitive's steal-if-dead).
  </behavior>
  <action>
    Wrap the spawn decision in startDaemon (lifecycle.js:110-140) with the Plan-01 primitive. Import `acquireLock`/`releaseLock` (or `withFileLock`) from '../session/state-lock.js'. Define a start-lock path derived from the daemon name, e.g. `join(KODO_DIR, name + '.start.lock')` (reuse the existing kodo-dir resolution used for PID files in polling-daemon.js).

    Flow: after the Windows-refuse guard, acquire the start-lock (short retries). If acquisition FAILS (another starter holds it), return a clean discriminated result `{ ok: true, alreadyStarting: true }` — do NOT spawn. If acquired, run the EXISTING pre-flight (readPid+isAlive → alreadyRunning short-circuit; stale → removePid) and the detached spawn + bounded wait INSIDE the lock, then releaseLock in a finally. This closes the window where two starts both see "not alive" and both spawn. Keep the injectable deps (`_spawn`/`_isPidAlive`/`_readPidFile`/`_removePidFile`) and add an injectable lock path or lock fns so the test can point at an isolated dir.

    Add test/daemon/polling-start-race.test.js: extend lock-race-child.mjs with a `--kind polling` mode that calls startDaemon against an isolated KODO_DIR with a trivial fake daemon argv (a node one-liner that writes a PID file and sleeps, or an injected `_spawn` that records invocation). Spawn 2 (and 5) children with the `go` barrier; assert exactly one actually spawned the daemon (exactly one PID file created / exactly one non-alreadyStarting result). Sandbox + cleanup; assert on the aggregate, not which wins.
  </action>
  <verify>
    <automated>node --test test/daemon/polling-start-race.test.js test/daemon/lifecycle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - startDaemon acquires the O_EXCL start-lock before the spawn decision and releases in finally; reuses state-lock.js (no new lock logic).
    - Race test: 2 (and 5) concurrent starts → exactly one daemon spawned.
    - Already-running short-circuit and Windows-refuse behavior unchanged; lifecycle suite green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: per-task_id cross-process dedup on the non-GSD dispatch lane (CONC-08, D-13)</name>
  <files>src/triggers/dispatcher.js, test/dispatcher-dedup-crossproc.test.js, test/helpers/lock-race-child.mjs</files>
  <read_first>
    - src/triggers/dispatcher.js:16 (inFlight Set — in-process only), :138-141 (in-process guard), :150-169 (GSD lane lock — do NOT touch), :201-211 (non-GSD dispatchSessionId), :423-459 (non-GSD lane: inFlight.add → launchWorkItemFn at :443 → finally inFlight.delete at :458)
    - src/session/state-lock.js (acquireLock/releaseLock from Plan 01)
    - 70-RESEARCH.md Pattern 8 (non-GSD lane never acquires the GSD lock)
  </read_first>
  <behavior>
    - Two processes dispatching the same non-GSD task_id: exactly one proceeds to launchWorkItem; the other returns `{ action: 'already_active' }` (or a distinct cross-process code) without launching.
    - The per-task_id lock is released after launch completes/fails (existing finally at :458).
    - The GSD lane is unaffected (still uses acquireGsdLock on projectPath; WT-03 invariant intact).
    - The in-process inFlight guard (:138) is preserved as the fast local path; the file lock is the cross-process backstop.
  </behavior>
  <action>
    In the NON-GSD lane only (dispatcher.js:423-459), add a per-task_id file lock via the Plan-01 primitive. Inject `acquireLockFn`/`releaseLockFn` through `deps` (default to acquireLock/releaseLock from '../session/state-lock.js'), mirroring the existing `acquireGsdLockFn`/`releaseGsdLockFn` DI. Lock path: `join(KODO_DIR, 'locks', 'dispatch-' + task.id + '.lock')` (ensure the `locks/` dir exists; reuse kodo-dir resolution). 

    Flow: right before `inFlight.add(task.id)` at :423 (the non-GSD branch), attempt `const held = acquireLockFn(dispatchLockPath)`. If it returns null (another process is dispatching this task_id), return `{ action: 'already_active' }` WITHOUT launching (cross-process mirror of the :138 in-process guard). If acquired, proceed with inFlight.add + launchWorkItemFn, and in the EXISTING finally at :458 (alongside inFlight.delete) call `releaseLockFn(dispatchLockPath, held.token)`. Keep the GSD lane (:150-169, and its finally at :417) completely unchanged.

    Add test/dispatcher-dedup-crossproc.test.js: extend lock-race-child.mjs with a `--kind dispatch` mode (or spawn 2 real processes) that invoke the dispatcher's non-GSD path against the same task_id in an isolated KODO_DIR, with launchWorkItemFn stubbed to record a launch marker file. Use the `go` barrier; assert exactly one launch marker exists (one actual dispatch), the other returns already_active. Sandbox + cleanup; aggregate assertion.
  </action>
  <verify>
    <automated>node --test test/dispatcher-dedup-crossproc.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Non-GSD lane acquires a per-task_id lock (path contains `dispatch-<task_id>`) before launch and releases in the existing finally; GSD lane untouched.
    - Cross-process race: 2 processes, same non-GSD task_id → exactly one launch; the other returns already_active.
    - Existing dispatcher tests still green (in-process inFlight guard preserved).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: verify + document the real worktree location; correct doctor if conclusive (CONC-09, D-15)</name>
  <files>src/gsd/doctor.js, .planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-WORKTREE-VERIFICATION.md</files>
  <read_first>
    - src/session/state.js:154-181 (computeWorktreePath `.bg-shell` vs computeRealWorktreePath `.claude/worktrees`)
    - src/gsd/doctor.js:250-268 (detectOrphanWorktrees — scans `.bg-shell`)
    - 70-RESEARCH.md Pattern 9 + Pitfall 5 (the live discrepancy) and CONTEXT.md D-15
  </read_first>
  <action>
    (a) Code analysis (autonomous): grep the codebase for every consumer of `computeWorktreePath` (.bg-shell) vs `computeRealWorktreePath` (.claude/worktrees) — reconcile, doctor, plan-reader, manager, dispatcher collision-check. Determine from the code which directory Claude Code actually creates worktrees in for a live GSD session (the `--worktree` / cmux path) and which directory each consumer assumes. Record the findings in 70-WORKTREE-VERIFICATION.md: the two path helpers, every consumer and which path it uses, and the concrete discrepancy (doctor scanning `.bg-shell` while real worktrees are in `.claude/worktrees`).

    (b) Correction decision: IF the code analysis is conclusive that `.bg-shell` is dead and `.claude/worktrees` is the live path, correct doctor.js's detectOrphanWorktrees (and any other confirmed-wrong consumer) to use `computeRealWorktreePath`, and add/extend a test asserting doctor scans the real path. IF the analysis is NOT conclusive without a live session, do the minimal safe change: add a code comment at the discrepancy site (doctor.js and computeWorktreePath) documenting the two paths and the open question, and leave the scan unchanged pending the empirical confirmation in the checkpoint below. Do NOT change the path by inference (prohibition).

    (c) Write 70-WORKTREE-VERIFICATION.md with: the analysis, the decision taken (corrected vs documented-deferred), and the exact steps a human runs to confirm on a live GSD session (`git worktree list` in a repo with a running kodo GSD session; compare against both helper outputs).
  </action>
  <verify>
    <automated>test -f .planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-WORKTREE-VERIFICATION.md && grep -Eq 'claude/worktrees|bg-shell' .planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-WORKTREE-VERIFICATION.md && echo DOC-OK</automated>
  </verify>
  <acceptance_criteria>
    - 70-WORKTREE-VERIFICATION.md exists documenting both path helpers, every consumer, and the discrepancy.
    - Either doctor.js is corrected to the real path (with a test) OR a documented deferral with an explanatory comment at the discrepancy site — never a silent inference-based path change.
    - If doctor.js was changed, `node --test` on the doctor test file is green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human confirmation of the real worktree path on a live GSD session (deferrable)</name>
  <action>Pause for the operator to confirm, against a live GSD session, which directory holds the real worktree (per 70-WORKTREE-VERIFICATION.md). This step is manual because it needs a running GSD session; it is deferrable per D-15 (same precedent as the Phase 50.1 progress display).</action>
  <what-built>
    Code analysis of the worktree-path discrepancy is complete and documented in 70-WORKTREE-VERIFICATION.md (Task 3). The empirical confirmation on a LIVE GSD session is the only part that cannot be automated.
  </what-built>
  <how-to-verify>
    1. Start a real kodo GSD session against a repo (a task labeled kodo:gsd), let it create its worktree.
    2. In that repo run: `git worktree list`.
    3. Compare the listed worktree path against the two helpers documented in 70-WORKTREE-VERIFICATION.md (`.bg-shell/<sid>` vs `.claude/worktrees/<sid>`).
    4. Confirm which directory is real, and whether doctor.js (as left by Task 3) scans the correct one.
    If a live GSD session cannot be mounted right now, reply "defer" — per D-15 the human signature is deferred exactly like the Phase 50.1 progress display, and the code analysis already delivered stands.
  </how-to-verify>
  <resume-signal>Type "approved" (confirms the documented path is correct), describe a correction needed, or "defer" to defer the live-session confirmation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent starters ↔ single daemon | two `polling start` invocations race to spawn the one daemon. |
| concurrent dispatchers ↔ one task | two processes (rapid webhooks / poll + webhook) may dispatch the same non-GSD task nearly simultaneously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-10 | Elevation / Tampering | startDaemon check-then-spawn TOCTOU | high | mitigate | O_EXCL start-lock around pre-flight+spawn; second EEXIST → already-starting, no second daemon (CONC-06/D-12). Proven by 2-process race. |
| T-70-11 | Tampering | duplicate non-GSD dispatch of one task | medium | mitigate | Per-task_id lock via the shared primitive on the non-GSD lane; second → already_active (CONC-08/D-13). GSD lane's per-repo lock untouched (WT-03). |
| T-70-12 | Information disclosure (stale) | doctor cleaning the wrong worktree dir | low | mitigate | Empirical verification + documented correction/deferral of the `.bg-shell` vs `.claude/worktrees` path (CONC-09/D-15). |
| T-70-SC | Tampering | npm installs | n/a | accept | Zero new deps (milestone invariant). |
</threat_model>

<verification>
- `node --test test/daemon/polling-start-race.test.js test/dispatcher-dedup-crossproc.test.js` green; non-flaky over 3 runs.
- 70-WORKTREE-VERIFICATION.md exists and documents the discrepancy + decision.
- `npm test` full suite green.
</verification>

<success_criteria>
- CONC-06: two concurrent polling starts → one daemon (O_EXCL start-lock, primitive reused).
- CONC-08: two processes, same non-GSD task_id → one launch (per-task_id lock; GSD lane untouched).
- CONC-09: real worktree location verified/documented; doctor corrected if conclusive, else deferred with analysis delivered + human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/70-concurrencia-y-ciclo-de-vida-de-procesos/70-04-SUMMARY.md` when done. Record the start-lock and dispatch-lock paths, and the CONC-09 decision (corrected vs deferred).
</output>
