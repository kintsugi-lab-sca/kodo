---
phase: 23
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/providers/github/client.js
  - test/providers/github/client.test.js
  - test/fixtures/github/issue.json
  - test/fixtures/github/issues-list.json
  - test/fixtures/github/issues-list-304.json
  - test/fixtures/github/rate-limit-low.json
  - test/fixtures/github/rate-limit-exceeded.json
  - test/fixtures/github/unauthorized-401.json
  - test/fixtures/github/forbidden-403.json
  - test/fixtures/github/not-found-404.json
  - test/fixtures/github/comment-created.json
  - test/fixtures/github/labels-list.json
autonomous: true
requirements:
  - GH-01
goal: >-
  Crear `GitHubClient` (clase con 5 métodos: `getIssue`, `listIssues`, `addComment`, `updateIssue`,
  `listLabels`) en `src/providers/github/client.js`. Mirror estructural de `PlaneClient` con tres
  divergencias justificadas: (a) CERO retry interno (D-11), (b) envelope `{status, items, etag,
  rate_limit_remaining}` solo en `listIssues` con soporte 304 sin throw (D-19), (c) `fetch` inyectable
  por constructor (D-06). Consume helpers de Plan 23-01. Verificado por 12 tests offline en
  `test/providers/github/client.test.js` con 10 fixtures JSON. Preserva invariantes LOG-12 y color
  isolation.
must_haves:
  truths:
    - "Importar { GitHubClient } desde src/providers/github/client.js no lanza"
    - "Constructor sin token y sin getProviderApiKey('github') resultado → throw 'GitHub token not found. Set GITHUB_TOKEN env var.'"
    - "client.getIssue('owner','repo',42) devuelve raw GitHub issue payload (number, title, body, labels, state)"
    - "client.listIssues('o','r') 200 path devuelve {status:200, items:Array, etag:string|undefined, rate_limit_remaining:number|undefined}"
    - "client.listIssues('o','r',{etag:'W/\"abc\"'}) 304 path devuelve {status:304, items:[], etag, rate_limit_remaining} SIN throw"
    - "client.addComment('o','r',42,'hello') POSTea body markdown {body:'hello'} y devuelve raw comment 201"
    - "client.updateIssue('o','r',42,{state:'closed'}) PATCHea con el body y devuelve raw issue"
    - "client.listLabels('o','r') devuelve array de labels raw"
    - "Respuesta 429 → throw Error con .code === 'rate_limit_exceeded', .status === 429, .retryAfter parseado"
    - "Respuesta 401 → throw Error con .code === 'unauthorized'"
    - "Respuesta 404 → throw Error con .code === 'not_found'"
    - "Respuesta 403 con X-RateLimit-Remaining: 0 → .code === 'rate_limit_exceeded' (no 'forbidden')"
    - "Header X-RateLimit-Remaining < 100 → logger spy recibe nivel 'warn'; >= 100 → 'info'"
    - "Headers enviados incluyen 'Authorization: token <PAT>', 'Accept: application/vnd.github+json', 'X-GitHub-Api-Version: 2022-11-28', 'User-Agent: kodo/0.7.x'"
    - "listIssues({etag}) envía If-None-Match: <etag> al fetch"
    - "Cero llamadas a globalThis.fetch en la suite (todos los tests inyectan fetch)"
    - "test/check-isolation.test.js sigue verde — src/check.js NO importa src/providers/github/client.js"
  artifacts:
    - path: "src/providers/github/client.js"
      provides: "class GitHubClient con 5 métodos públicos + request privado + parseRetryAfter + mapErrorCode"
      contains: "class GitHubClient"
      min_lines: 130
    - path: "test/providers/github/client.test.js"
      provides: "12 tests cubriendo Per-Task Verification Map de 23-VALIDATION.md (rows 23-02-01..12)"
      contains: "describe('GitHubClient'"
      min_lines: 200
    - path: "test/fixtures/github/issue.json"
      provides: "Raw GET /repos/{o}/{r}/issues/{n} 200 payload"
      contains: "node_id"
    - path: "test/fixtures/github/issues-list.json"
      provides: "Raw GET /repos/{o}/{r}/issues 200 array (issue + PR para Pitfall #2 doc)"
      contains: "pull_request"
    - path: "test/fixtures/github/comment-created.json"
      provides: "Raw POST .../comments 201 payload"
      contains: "author_association"
    - path: "test/fixtures/github/labels-list.json"
      provides: "Raw GET .../labels 200 array"
      contains: "0e8a16"
    - path: "test/fixtures/github/rate-limit-exceeded.json"
      provides: "Body de 429 GitHub: {message: 'API rate limit exceeded ...'}"
      contains: "rate limit exceeded"
    - path: "test/fixtures/github/unauthorized-401.json"
      provides: "Body de 401: {message:'Bad credentials', documentation_url}"
      contains: "Bad credentials"
    - path: "test/fixtures/github/not-found-404.json"
      provides: "Body de 404: {message:'Not Found', documentation_url}"
      contains: "Not Found"
    - path: "test/fixtures/github/forbidden-403.json"
      provides: "Body de 403 secondary rate-limit"
      contains: "abuse"
    - path: "test/fixtures/github/rate-limit-low.json"
      provides: "Issue payload + scenario header con X-RateLimit-Remaining: 50 (warn path)"
      contains: "node_id"
    - path: "test/fixtures/github/issues-list-304.json"
      provides: "Placeholder JSON para 304 scenario shape symmetry"
      contains: "{}"
  key_links:
    - from: "src/providers/github/client.js (request)"
      to: "src/logger-events.js (githubApiCall/githubApiCallFailed)"
      via: "dynamic await import('../../logger-events.js') con try/catch silent"
      pattern: "await import\\(['\"]\\.\\.\\/\\.\\.\\/logger-events\\.js"
    - from: "src/providers/github/client.js (constructor)"
      to: "src/config.js (getProviderApiKey)"
      via: "import { getProviderApiKey } from '../../config.js'"
      pattern: "getProviderApiKey\\(['\"]github['\"]"
    - from: "src/providers/github/client.js (listIssues 304 branch)"
      to: "envelope return"
      via: "if (res.status === 304) return { status: 304, items: [], etag, rate_limit_remaining }"
      pattern: "status:\\s*304"
    - from: "test/providers/github/client.test.js"
      to: "test/fixtures/github/*.json"
      via: "import fixture from '../../fixtures/github/<name>.json' with { type: 'json' }"
      pattern: "with\\s*\\{\\s*type:\\s*['\"]json['\"]"
---

<objective>
Implementar el cliente HTTP REST `GitHubClient` que hablará con `https://api.github.com` con PAT,
rate-limit awareness, ETag/304 conditional fetch y error mapping a códigos canónicos. Es **toda
la implementación productiva** de Phase 23. Plan 23-01 ya extendió la taxonomía NDJSON; este plan
crea el productor.

**Purpose:**
- Producir la capa de transporte HTTP aislada (no conoce `TaskProvider`, no conoce `TaskItem`, no
  conoce dispatcher) que Phase 24 consumirá para construir `GitHubProvider` y Phase 25 para el
  polling loop.
- Suplir las 4 Success Criteria de Phase 23 (ROADMAP §Phase 23):
  - **SC#1** — clase exportada con los 5 métodos + auth `Authorization: token <PAT>` leído via
    `getProviderApiKey('github')`.
  - **SC#2** — emite `github.api.call` con level `warn` cuando `X-RateLimit-Remaining < 100`;
    rechaza con error canonical `rate_limit_exceeded` en `429`.
  - **SC#3** — `listIssues({etag})` devuelve `{status:304, items:[], etag, rate_limit_remaining}`
    sin throw.
  - **SC#4** — 12 tests offline con 10 fixtures `test/fixtures/github/*.json` (≥ 8 requerido).

**Output:**
- `src/providers/github/client.js` (NUEVO) — clase + private `request()` + 2 helpers internos
  (`parseRetryAfter`, `mapErrorCode`).
- `test/providers/github/client.test.js` (NUEVO) — 12 tests cubriendo las 12 filas del
  Per-Task Verification Map en `23-VALIDATION.md`.
- `test/fixtures/github/` (NUEVO directorio) — 10 fixtures JSON redactadas.

**Per CONTEXT.md (D-01..D-39):** TODAS las decisiones lockeadas se respetan. Las 3 divergencias
respecto a `PlaneClient` están justificadas:
- **D-06** (`fetch` inyectable) — mejora de testability sin global mutation.
- **D-11** (cero retry) — Phase 25 polling es la capa de retry; double-retry sería antipattern.
- **D-19** (envelope 304 en `listIssues`) — contract crítico entre Phase 23 ↔ Phase 25.

**NOT in this plan:** `GitHubProvider` (Phase 24), normalizer a `TaskItem` (Phase 24),
registry wiring (Phase 24), polling loop (Phase 25), config wizard (Phase 26).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-githubclient-auth-foundation/23-CONTEXT.md
@.planning/phases/23-githubclient-auth-foundation/23-RESEARCH.md
@.planning/phases/23-githubclient-auth-foundation/23-PATTERNS.md
@.planning/phases/23-githubclient-auth-foundation/23-VALIDATION.md
@.planning/phases/23-githubclient-auth-foundation/23-01-SUMMARY.md
@src/providers/plane/client.js
@src/config.js
@test/plane-provider.test.js
@test/normalize.test.js

<interfaces>
<!-- Contratos extraídos del codebase y del Plan 23-01. El executor NO debe explorar más. -->

Desde `src/config.js` (exportado, useable directly):
```js
/**
 * @param {string} [providerName] - Provider name. Defaults to config.provider.
 * @returns {string|undefined}
 */
export function getProviderApiKey(providerName);

// Behavior: lee `config.providers[providerName].api_key_env` y devuelve `process.env[<name>]`.
// Si el config NO tiene la clave `providers.github` (caso v0.6 antes de CFG-02 Phase 26),
// devuelve undefined → el constructor de GitHubClient lanza con el mensaje canónico.
// Para tests: NUNCA invocar — siempre inyectar `opts.token: 'ghp_test'`.
```

Desde `src/logger-events.js` (tras Plan 23-01 — DEPENDENCIA UPSTREAM):
```js
export const EVENTS = Object.freeze({
  /* ... 13 existentes ... */
  GITHUB_API_CALL:         'github.api.call',
  GITHUB_API_CALL_FAILED:  'github.api.call.failed',
});

/**
 * Switch info/warn por `rate_limit_remaining < 100` (D-16).
 * @param {Logger} logger
 * @param {{ method, path, status, duration_ms, rate_limit_remaining }} fields
 */
export function githubApiCall(logger, fields);

/**
 * Siempre error level. Shape `{method, path, status, error}` (no `step`).
 * @param {Logger} logger
 * @param {{ method, path, status, error }} fields
 */
export function githubApiCallFailed(logger, fields);
```

Desde `src/providers/plane/client.js` (TEMPLATE — pattern de `request()` private, líneas 23-92):
```js
// El cliente:
// - usa `new URL(...)` + `url.searchParams.set(k, v)` para query params.
// - parsea X-RateLimit-Remaining/Reset:
//     const remaining = res.headers.get('x-ratelimit-remaining');
//     if (remaining !== null) this._rateRemaining = parseInt(remaining, 10);
// - emite logger.<level>(EVENTS.X, {...}) via dynamic await import('../../logger-events.js')
//   envuelto en try/catch silent (líneas 76-88).
// - throw new Error(`Plane API ${res.status}: ${path} — ${text}`) en !res.ok (línea 70-72).
//
// LÍNEAS A NO PORTAR (D-11 + D-29):
// - 31-67 (loop retry + maxRetries + 429 backoff + proactive throttle + console.warn) — GitHubClient
//   hace UN SOLO fetch por request, sin retry, sin proactive throttle, sin console.warn.
```

Desde `test/plane-provider.test.js` líneas 62-77 (PATRÓN A NO PORTAR):
```js
// PROHIBIDO replicar: globalThis.fetch = … restore en finally.
// El cliente GitHub ACEPTA `opts.fetch` y los tests SIEMPRE inyectan vía constructor.
```

Desde `test/normalize.test.js` líneas 12-14 (PATRÓN A USAR — JSON fixture import):
```js
import workItemFixture from './fixtures/plane-workitem.json' with { type: 'json' };
// Path desde test/providers/github/client.test.js: '../../fixtures/github/<name>.json'
```

Desde `23-VALIDATION.md §Per-Task Verification Map` (las 12 filas que estos tests cubren):
- 23-02-01: constructor throws sin token
- 23-02-02: getIssue retorna raw payload (id, number, body, labels, state)
- 23-02-03: listIssues 200 path retorna envelope
- 23-02-04: listIssues 304 path retorna envelope SIN throw
- 23-02-05: 429 → Error.code='rate_limit_exceeded', .status=429, .retryAfter
- 23-02-06: rate_limit_remaining < 100 → logger spy recibe level='warn'
- 23-02-07: addComment envía POST con markdown body intact
- 23-02-08: updateIssue envía PATCH con body
- 23-02-09: listLabels retorna array raw
- 23-02-10: 401/403/404/410/422/5xx → mapping table-driven a .code canonical
- 23-02-11: 9+ fixtures cargan; cero llamadas live `globalThis.fetch` (static-check via `grep -c globalThis.fetch test/providers/github/client.test.js === 0` **+ runtime guard**: top-of-file `globalThis.fetch = () => { throw new Error('live fetch leak: test must inject fetch via constructor opts'); };` con restore en `after()` — falla loud si cualquier test invoca fetch global)
- 23-02-12: check-isolation sigue verde (LOG-12)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear directorio test/fixtures/github/ con 10 fixtures JSON redactadas</name>
  <files>
    test/fixtures/github/issue.json,
    test/fixtures/github/issues-list.json,
    test/fixtures/github/issues-list-304.json,
    test/fixtures/github/rate-limit-low.json,
    test/fixtures/github/rate-limit-exceeded.json,
    test/fixtures/github/unauthorized-401.json,
    test/fixtures/github/forbidden-403.json,
    test/fixtures/github/not-found-404.json,
    test/fixtures/github/comment-created.json,
    test/fixtures/github/labels-list.json
  </files>
  <read_first>
    - Leer `23-RESEARCH.md §Testing Strategy` (líneas 496-543) — shapes documentadas.
    - Leer `23-RESEARCH.md §GitHub REST API Reference` (líneas 123-251) — campos esperados por endpoint.
    - Convenciones de redacción D-34: `owner` = `kodo-test`, `repo` = `fixture-repo`, IDs = enteros pequeños,
      URLs `https://github.com/kodo-test/fixture-repo/...`, ZERO PATs o info personal.
  </read_first>
  <behavior>
    - Los 10 fixtures cargables vía `import x from '../../fixtures/github/<name>.json' with { type: 'json' }`.
    - Cada fixture es un objeto/array JSON válido — NO incluye headers (los headers van en el
      `scenario` object dentro del test).
    - Shapes coinciden con respuestas reales documentadas en `23-RESEARCH.md`.
  </behavior>
  <action>
    Crear el directorio `test/fixtures/github/` y los 10 archivos siguientes con el contenido exacto:

    **`test/fixtures/github/issue.json`** (GET /repos/{o}/{r}/issues/{n} → 200):
    ```json
    {
      "id": 1,
      "node_id": "I_kwTEST001",
      "number": 42,
      "title": "Test issue",
      "body": "Issue body markdown",
      "labels": [
        { "id": 1, "node_id": "LA_TEST001", "name": "kodo", "color": "0e8a16", "default": false, "description": null }
      ],
      "state": "open",
      "state_reason": null,
      "html_url": "https://github.com/kodo-test/fixture-repo/issues/42",
      "pull_request": null,
      "assignees": [],
      "user": { "login": "kodo-test", "id": 1 },
      "created_at": "2026-05-14T07:00:00Z",
      "updated_at": "2026-05-14T08:00:00Z",
      "locked": false,
      "comments": 0
    }
    ```

    **`test/fixtures/github/issues-list.json`** (GET /repos/.../issues → array de 3 elementos: 2 issues + 1 PR — Pitfall #2 doc):
    ```json
    [
      {
        "id": 1,
        "node_id": "I_kwTEST001",
        "number": 42,
        "title": "Test issue 1",
        "body": "Body 1",
        "labels": [
          { "id": 1, "node_id": "LA_TEST001", "name": "kodo", "color": "0e8a16", "default": false, "description": null }
        ],
        "state": "open",
        "state_reason": null,
        "html_url": "https://github.com/kodo-test/fixture-repo/issues/42",
        "pull_request": null,
        "assignees": [],
        "user": { "login": "kodo-test", "id": 1 },
        "created_at": "2026-05-14T07:00:00Z",
        "updated_at": "2026-05-14T08:00:00Z",
        "locked": false,
        "comments": 0
      },
      {
        "id": 2,
        "node_id": "I_kwTEST002",
        "number": 43,
        "title": "Test issue 2",
        "body": null,
        "labels": [],
        "state": "open",
        "state_reason": null,
        "html_url": "https://github.com/kodo-test/fixture-repo/issues/43",
        "pull_request": null,
        "assignees": [],
        "user": { "login": "kodo-test", "id": 1 },
        "created_at": "2026-05-14T07:30:00Z",
        "updated_at": "2026-05-14T08:30:00Z",
        "locked": false,
        "comments": 1
      },
      {
        "id": 3,
        "node_id": "PR_kwTEST003",
        "number": 44,
        "title": "Test PR (should be filtered by Phase 24)",
        "body": "PR body",
        "labels": [],
        "state": "open",
        "state_reason": null,
        "html_url": "https://github.com/kodo-test/fixture-repo/pull/44",
        "pull_request": { "url": "https://api.github.com/repos/kodo-test/fixture-repo/pulls/44", "html_url": "https://github.com/kodo-test/fixture-repo/pull/44" },
        "assignees": [],
        "user": { "login": "kodo-test", "id": 1 },
        "created_at": "2026-05-14T08:00:00Z",
        "updated_at": "2026-05-14T09:00:00Z",
        "locked": false,
        "comments": 0
      }
    ]
    ```

    **`test/fixtures/github/issues-list-304.json`** (placeholder; 304 no tiene body — solo existe para shape symmetry del scenario):
    ```json
    {}
    ```

    **`test/fixtures/github/rate-limit-low.json`** (igual a `issue.json` pero el test inyecta `X-RateLimit-Remaining: 50`; el body es el mismo para reutilizar shape):
    ```json
    {
      "id": 1,
      "node_id": "I_kwTEST001",
      "number": 42,
      "title": "Test issue (low rate limit scenario)",
      "body": "Issue body",
      "labels": [],
      "state": "open",
      "state_reason": null,
      "html_url": "https://github.com/kodo-test/fixture-repo/issues/42",
      "pull_request": null,
      "assignees": [],
      "user": { "login": "kodo-test", "id": 1 },
      "created_at": "2026-05-14T07:00:00Z",
      "updated_at": "2026-05-14T08:00:00Z",
      "locked": false,
      "comments": 0
    }
    ```

    **`test/fixtures/github/rate-limit-exceeded.json`** (body de 429 — body real de GitHub):
    ```json
    {
      "message": "API rate limit exceeded for user ID 1.",
      "documentation_url": "https://docs.github.com/rest/overview/rate-limits-for-the-rest-api"
    }
    ```

    **`test/fixtures/github/unauthorized-401.json`**:
    ```json
    {
      "message": "Bad credentials",
      "documentation_url": "https://docs.github.com/rest"
    }
    ```

    **`test/fixtures/github/forbidden-403.json`** (secondary rate-limit / abuse):
    ```json
    {
      "message": "You have exceeded a secondary rate limit. Please wait a few minutes before you try again. If you reach out to GitHub Support for help, please include the request ID ABCD:1234:5678.",
      "documentation_url": "https://docs.github.com/rest/overview/resources-in-the-rest-api#secondary-rate-limits"
    }
    ```

    **`test/fixtures/github/not-found-404.json`**:
    ```json
    {
      "message": "Not Found",
      "documentation_url": "https://docs.github.com/rest"
    }
    ```

    **`test/fixtures/github/comment-created.json`** (POST .../comments → 201):
    ```json
    {
      "id": 123,
      "node_id": "IC_kwTESTCOMMENT001",
      "url": "https://api.github.com/repos/kodo-test/fixture-repo/issues/comments/123",
      "html_url": "https://github.com/kodo-test/fixture-repo/issues/42#issuecomment-123",
      "body": "Test comment markdown",
      "user": { "login": "kodo-test", "id": 1 },
      "created_at": "2026-05-14T08:00:00Z",
      "updated_at": "2026-05-14T08:00:00Z",
      "author_association": "OWNER",
      "reactions": { "total_count": 0, "+1": 0, "-1": 0, "laugh": 0, "hooray": 0, "confused": 0, "heart": 0, "rocket": 0, "eyes": 0 }
    }
    ```

    **`test/fixtures/github/labels-list.json`** (GET .../labels → array):
    ```json
    [
      { "id": 1, "node_id": "LA_TEST001", "url": "https://api.github.com/repos/kodo-test/fixture-repo/labels/kodo", "name": "kodo", "description": "Auto-launched by kodo", "color": "0e8a16", "default": false },
      { "id": 2, "node_id": "LA_TEST002", "url": "https://api.github.com/repos/kodo-test/fixture-repo/labels/bug", "name": "bug", "description": null, "color": "d73a4a", "default": true },
      { "id": 3, "node_id": "LA_TEST003", "url": "https://api.github.com/repos/kodo-test/fixture-repo/labels/priority%3Ahigh", "name": "priority:high", "description": null, "color": "ff0000", "default": false }
    ]
    ```

    Validar que cada archivo es JSON parseable:
    ```bash
    for f in test/fixtures/github/*.json; do node -e "JSON.parse(require('node:fs').readFileSync('$f','utf8'))" || echo "INVALID: $f"; done
    ```
  </action>
  <verify>
    <automated>ls test/fixtures/github/*.json | wc -l && for f in test/fixtures/github/*.json; do node -e "JSON.parse(require('node:fs').readFileSync('$f','utf8'))" 2>&1 || echo "BROKEN: $f"; done | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls test/fixtures/github/*.json | wc -l` === 10.
    - `for f in test/fixtures/github/*.json; do node -e "JSON.parse(require('node:fs').readFileSync('$f','utf8'))"; done` exit 0 en todos.
    - `grep -l "ghp_\|github_pat_" test/fixtures/github/*.json` devuelve 0 archivos (ZERO PATs en fixtures).
    - `grep -l "kodo-test" test/fixtures/github/*.json | wc -l` ≥ 7 (redacción consistente).
    - `grep "pull_request" test/fixtures/github/issues-list.json` devuelve la línea del PR (Pitfall #2 doc).
  </acceptance_criteria>
  <done>10 fixtures JSON parseables creadas, redactadas con `kodo-test`/`fixture-repo`, sin PATs ni info sensible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test/providers/github/client.test.js skeleton con makeFetch + makeSpyLogger helpers</name>
  <files>test/providers/github/client.test.js</files>
  <read_first>
    - Leer `23-PATTERNS.md §test/providers/github/client.test.js` para los helpers `makeFetch`, `makeSpyFetch`, `makeSpyLogger`.
    - Leer `test/normalize.test.js` líneas 1-30 (header + import-with-json pattern).
    - Confirmar que `test/providers/github/` NO existe (`ls test/providers/` actualmente da
      "No such file or directory"). Esta task crea el directorio implícitamente al escribir el archivo.
    - **NO crear** un módulo separado `test/providers/github/__helpers/fake-fetch.js` — YAGNI; los
      helpers viven inline en el archivo de test (per VALIDATION.md Wave 0 "or inline in test file").
  </read_first>
  <behavior>
    - El archivo es importable por `node --test test/providers/github/client.test.js` y compila
      (sintaxis válida, imports resueltos).
    - `describe('GitHubClient', ...)` presente con stub vacío o con tests `it.todo(...)` placeholders
      (Task 3 + Task 4 llenan los tests reales).
    - `makeFetch(scenario)` función disponible en el módulo (top-level, no exportada).
    - `makeSpyFetch(scenario)` disponible — wraps makeFetch + captura `calls`.
    - `makeSpyLogger()` disponible — captura `records`.
    - **NO** llama `globalThis.fetch` en ninguna parte.
  </behavior>
  <action>
    Crear `test/providers/github/client.test.js` con el siguiente contenido EXACTO:

    ```js
    // @ts-check
    /**
     * Phase 23 GH-01 — GitHubClient unit tests.
     *
     * Estrategia: fetch inyectado via constructor (D-06) + fixtures JSON offline (D-33/D-34/D-35).
     * Zero llamadas a `globalThis.fetch` — el constructor de cada test recibe `makeFetch(scenario)`.
     *
     * Las 12 filas del Per-Task Verification Map (23-VALIDATION.md) están implementadas como
     * `it(...)` discretos. Cobertura: SC#1 (5 métodos + constructor), SC#2 (rate-limit warn +
     * 429 mapping), SC#3 (304 envelope sin throw), SC#4 (≥ 8 tests offline).
     */

    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';

    import { GitHubClient } from '../../../src/providers/github/client.js';

    import issueFixture from '../../fixtures/github/issue.json' with { type: 'json' };
    import issuesListFixture from '../../fixtures/github/issues-list.json' with { type: 'json' };
    import rateLimitLowFixture from '../../fixtures/github/rate-limit-low.json' with { type: 'json' };
    import rateLimitExceededFixture from '../../fixtures/github/rate-limit-exceeded.json' with { type: 'json' };
    import unauthorizedFixture from '../../fixtures/github/unauthorized-401.json' with { type: 'json' };
    import forbiddenFixture from '../../fixtures/github/forbidden-403.json' with { type: 'json' };
    import notFoundFixture from '../../fixtures/github/not-found-404.json' with { type: 'json' };
    import commentCreatedFixture from '../../fixtures/github/comment-created.json' with { type: 'json' };
    import labelsListFixture from '../../fixtures/github/labels-list.json' with { type: 'json' };

    /**
     * Construye un `fetch` fake que devuelve un `Response`-like object con
     * solo lo que `request()` realmente consume (status, ok, headers.get, json, text).
     *
     * @param {{ status: number, body?: any, headers?: Record<string,string> }} scenario
     * @returns {typeof fetch}
     */
    function makeFetch(scenario) {
      // @ts-ignore — el shape mínimo es suficiente para lo que el cliente lee.
      return async (_url, _init) => ({
        status: scenario.status,
        ok: scenario.status >= 200 && scenario.status < 300,
        headers: {
          get(name) {
            return scenario.headers?.[name.toLowerCase()] ?? null;
          },
        },
        async json() {
          if (scenario.status === 304) throw new Error('No body for 304');
          return scenario.body;
        },
        async text() {
          return scenario.body ? JSON.stringify(scenario.body) : '';
        },
      });
    }

    /**
     * Variant de `makeFetch` que captura cada call (url + init) en un array.
     * Útil para asserts de headers/body/URL.
     *
     * @param {{ status: number, body?: any, headers?: Record<string,string> }} scenario
     * @returns {{ fetch: typeof fetch, calls: Array<{ url: string, init: any }> }}
     */
    function makeSpyFetch(scenario) {
      const calls = [];
      const inner = makeFetch(scenario);
      const fakeFetch = async (url, init) => {
        calls.push({ url: url.toString(), init });
        return inner(url, init);
      };
      // @ts-ignore — fakeFetch matches the shape we care about.
      return { fetch: fakeFetch, calls };
    }

    /**
     * Mock minimal de `Logger` (compatible con la shape que esperan
     * `githubApiCall` / `githubApiCallFailed`: `.info`/`.warn`/`.error`).
     * Captura cada call en `records` con el `level` derivado del método invocado.
     */
    function makeSpyLogger() {
      const records = [];
      return {
        records,
        info: (event, fields) => records.push({ level: 'info', event, ...fields }),
        warn: (event, fields) => records.push({ level: 'warn', event, ...fields }),
        error: (event, fields) => records.push({ level: 'error', event, ...fields }),
      };
    }

    describe('GitHubClient', () => {
      // Task 3 + Task 4 añaden los 12 tests aquí.
    });
    ```

    Crear directorio (implícito al escribir el archivo). Si Write falla con "directory not found",
    crear primero `test/providers/github/` con `mkdir -p`.
  </action>
  <verify>
    <automated>node --check test/providers/github/client.test.js && echo "SYNTAX_OK" && node --test test/providers/github/client.test.js 2>&1 | tail -5</automated>
    <!-- Espera: SYNTAX_OK + suite vacía verde ("# pass 0 / # fail 0"). El test falla intencionalmente -->
    <!-- una vez Task 3 carga `GitHubClient` antes de que Task 5 lo cree — orden de waves importa. -->
  </verify>
  <acceptance_criteria>
    - `test/providers/github/client.test.js` existe y es archivo (no directorio).
    - `node --check test/providers/github/client.test.js` exit 0.
    - `grep -c "^function makeFetch" test/providers/github/client.test.js` === 1.
    - `grep -c "^function makeSpyFetch" test/providers/github/client.test.js` === 1.
    - `grep -c "^function makeSpyLogger" test/providers/github/client.test.js` === 1.
    - `grep -c "describe('GitHubClient'" test/providers/github/client.test.js` === 1.
    - `grep "globalThis.fetch" test/providers/github/client.test.js` devuelve 0 líneas (zero global mutation).
  </acceptance_criteria>
  <done>Test skeleton creado, helpers `makeFetch`/`makeSpyFetch`/`makeSpyLogger` definidos inline, imports de los 9 fixtures listos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear src/providers/github/client.js — clase GitHubClient + request privado + 2 helpers</name>
  <files>src/providers/github/client.js</files>
  <read_first>
    - **Re-leer** `src/providers/plane/client.js` completo (Read en context) para confirmar el patrón
      de constructor (líneas 4-16), `request` core (líneas 23-92 — RECORDAR no portar líneas 31-67
      retry + 34-42 throttle), y emisión NDJSON dynamic-import (líneas 76-88).
    - **Re-leer** `23-PATTERNS.md §src/providers/github/client.js` para el sketch concreto del
      constructor, request, y los dos helpers internos.
    - **Re-leer** `23-RESEARCH.md §Error Mapping Table` para la tabla completa status → code.
    - El JSDoc `@param`/`@returns` debe ser correcto para que `// @ts-check` no produzca errores
      al correr `npm test` (Node infiere los tipos JSDoc).
  </read_first>
  <behavior>
    - Importar `{ GitHubClient }` desde el módulo NO lanza.
    - `new GitHubClient({ token: 'ghp_test' })` no lanza.
    - `new GitHubClient({})` sin token y SIN `process.env[<api_key_env>]` lanza
      `'GitHub token not found. Set GITHUB_TOKEN env var.'`.
    - `client.request(path)` interno:
      - construye URL `${baseUrl}${path}` + query params si existen.
      - envía headers `Authorization: token <PAT>`, `Accept: application/vnd.github+json`,
        `X-GitHub-Api-Version: 2022-11-28`, `User-Agent: kodo/0.7.x`.
      - aplica `AbortSignal.timeout(10_000)`.
      - parsea `x-ratelimit-remaining` + `x-ratelimit-reset` (null-safe).
      - **status 304:** devuelve `{status:304, items:[], etag, rate_limit_remaining}` SIN throw
        (envelope listIssues-style — Plan caller decide qué hacer con ello).
      - **status 200-201:** emite `githubApiCall` con `rate_limit_remaining` y devuelve `res.json()`.
      - **`!res.ok`:** emite `githubApiCallFailed`, calcula `.code` via `mapErrorCode`, parsea
        `Retry-After` via `parseRetryAfter`, lanza `Error` enriquecido con `.code`/`.status`/`.retryAfter`.
      - **NO retry, NO proactive throttle, NO console.warn.**
    - 5 métodos públicos `async`:
      - `getIssue(owner, repo, number)` → llama `this.request('/repos/<o>/<r>/issues/<n>')` y
        devuelve raw payload. Owner/repo se sanitizan via `encodeURIComponent` (Security V5).
      - `listIssues(owner, repo, opts?)` → llama request con query params; SI status 200,
        envuelve como `{status:200, items:data, etag:res.headers.get('etag'), rate_limit_remaining}`;
        SI status 304, propaga el envelope que vino del request.
      - `addComment(owner, repo, number, markdownBody)` → POST con body `{body: markdownBody}`.
      - `updateIssue(owner, repo, number, updates)` → PATCH con body=updates.
      - `listLabels(owner, repo)` → GET con `?per_page=100`.
  </behavior>
  <action>
    Crear `src/providers/github/client.js` con el siguiente código EXACTO (~190 LOC):

    ```js
    // @ts-check
    /**
     * GitHubClient — REST wrapper sobre `https://api.github.com` (Phase 23 GH-01).
     *
     * Mirror estructural de `PlaneClient` con 3 divergencias justificadas:
     *
     *   1. **No retry interno (D-11)** — un solo fetch por request. Phase 25 polling (POLL-04)
     *      es la capa de retry con backoff exponencial.
     *
     *   2. **Fetch inyectable (D-06)** — `opts.fetch ?? globalThis.fetch`. Permite testing sin
     *      `globalThis` mutation (anti-pattern del PlaneClient test, ver
     *      `test/plane-provider.test.js:62-77`).
     *
     *   3. **Envelope 304 en `listIssues` (D-19)** — el `request()` privado detecta 304 y devuelve
     *      `{status, items:[], etag, rate_limit_remaining}` SIN throw. Phase 25 persiste etag.
     *
     * Métodos públicos (D-22):
     *   - getIssue(owner, repo, number)        → raw GitHub issue payload
     *   - listIssues(owner, repo, opts?)       → {status, items, etag, rate_limit_remaining}
     *   - addComment(owner, repo, number, md)  → raw comment 201
     *   - updateIssue(owner, repo, number, u)  → raw issue payload tras PATCH
     *   - listLabels(owner, repo)              → array de labels raw
     *
     * Errores canonical (D-12): el cliente lanza `Error` plano con `.code`, `.status`,
     * `.retryAfter`. Códigos: `unauthorized`, `forbidden`, `not_found`, `rate_limit_exceeded`,
     * `github_api_error`.
     *
     * LOG-12 invariant: este módulo importa `getProviderApiKey` (config), `githubApiCall` y
     * `githubApiCallFailed` (logger-events via dynamic import). `src/check.js` NO carga este
     * archivo — `test/check-isolation.test.js` lo verifica.
     */

    import { getProviderApiKey } from '../../config.js';

    /**
     * Parse `Retry-After` header — RFC 7231 permite entero (segundos) o HTTP-date.
     * GitHub históricamente usa entero, pero el spec admite ambos (Pitfall #4).
     *
     * @param {string|null} header
     * @returns {number|undefined}
     */
    function parseRetryAfter(header) {
      if (!header) return undefined;
      const trimmed = header.trim();
      const asInt = parseInt(trimmed, 10);
      if (!isNaN(asInt) && String(asInt) === trimmed) return asInt;
      const asDate = Date.parse(trimmed);
      if (!isNaN(asDate)) return Math.max(0, Math.ceil((asDate - Date.now()) / 1000));
      return undefined;
    }

    /**
     * Mapea HTTP status → código canonical de `Error.code` (D-12 + 23-RESEARCH.md Error Mapping Table).
     *
     * @param {number} status
     * @param {{ get(name: string): string|null }} headers
     * @param {number|undefined} retryAfter
     * @returns {'unauthorized'|'forbidden'|'not_found'|'rate_limit_exceeded'|'github_api_error'}
     */
    function mapErrorCode(status, headers, retryAfter) {
      if (status === 401) return 'unauthorized';
      if (status === 404) return 'not_found';
      if (status === 429) return 'rate_limit_exceeded';
      if (status === 403) {
        const remaining = headers.get('x-ratelimit-remaining');
        if (remaining === '0' || retryAfter !== undefined) return 'rate_limit_exceeded';
        return 'forbidden';
      }
      return 'github_api_error';
    }

    /**
     * @typedef {object} GitHubClientOpts
     * @property {string} [baseUrl] — Default 'https://api.github.com'. Override para fake-server tests.
     * @property {string} [token] — PAT override. Si undefined, llama `getProviderApiKey('github')`.
     * @property {typeof globalThis.fetch} [fetch] — Inyección para test (D-06).
     * @property {import('../../logger.js').Logger} [logger] — Optional. Sin él, NDJSON es no-op.
     */

    export class GitHubClient {
      /** @param {GitHubClientOpts} [opts] */
      constructor(opts = {}) {
        this.baseUrl = (opts.baseUrl || 'https://api.github.com').replace(/\/$/, '');
        this.token = opts.token || getProviderApiKey('github');
        this.fetch = opts.fetch || globalThis.fetch;
        this.logger = opts.logger;

        if (!this.token) {
          throw new Error('GitHub token not found. Set GITHUB_TOKEN env var.');
        }
      }

      /**
       * Internal HTTP transport. Centraliza fetch + auth + timeout + rate-limit headers +
       * NDJSON emission + error mapping. CERO retry (D-11), CERO proactive throttle (D-29).
       *
       * @param {string} path — Path relativo a `baseUrl` (e.g. `/repos/octocat/hello-world/issues/42`).
       * @param {{
       *   method?: string,
       *   body?: object,
       *   params?: Record<string,string>,
       *   etag?: string,
       * }} [opts]
       * @returns {Promise<any>}
       */
      async request(path, opts = {}) {
        const url = new URL(`${this.baseUrl}${path}`);
        if (opts.params) {
          for (const [k, v] of Object.entries(opts.params)) {
            url.searchParams.set(k, v);
          }
        }

        /** @type {Record<string,string>} */
        const headers = {
          'Authorization': `token ${this.token}`,
          'Accept': 'application/vnd.github+json',
          'X-GitHub-Api-Version': '2022-11-28',
          'User-Agent': 'kodo/0.7.x',
        };
        if (opts.body) headers['Content-Type'] = 'application/json';
        if (opts.etag) headers['If-None-Match'] = opts.etag;

        const started = Date.now();
        const method = opts.method || 'GET';
        const res = await this.fetch(url, {
          method,
          headers,
          body: opts.body ? JSON.stringify(opts.body) : undefined,
          signal: AbortSignal.timeout(10_000),
        });

        // Rate-limit header parsing (null-safe — Pitfall #8). String|null → number|undefined.
        const remaining = res.headers.get('x-ratelimit-remaining');
        const reset = res.headers.get('x-ratelimit-reset');
        if (remaining !== null) this._rateRemaining = parseInt(remaining, 10);
        if (reset !== null) this._rateReset = parseInt(reset, 10);

        // 304 path (D-19): envelope, no throw. Solo `listIssues` se aprovecha — los demás métodos
        // no envían `If-None-Match`, así que GitHub no devuelve 304. Pero centralizamos aquí porque
        // los headers ya están parseados.
        if (res.status === 304) {
          const etagHeader = res.headers.get('etag');
          // Emit success-side NDJSON aunque sea 304 (D-15: cuenta como api.call, status=304).
          if (this.logger) {
            try {
              const { githubApiCall } = await import('../../logger-events.js');
              githubApiCall(this.logger, {
                method,
                path,
                status: 304,
                duration_ms: Date.now() - started,
                rate_limit_remaining: this._rateRemaining,
              });
            } catch {
              // silent — nunca interferir con el response flow.
            }
          }
          return {
            status: 304,
            items: [],
            etag: etagHeader ?? opts.etag,
            rate_limit_remaining: this._rateRemaining,
          };
        }

        // Error path: emit failure NDJSON + throw canonical Error.
        if (!res.ok) {
          const text = await res.text().catch(() => '');
          const snippet = text.slice(0, 200);
          const retryAfter = parseRetryAfter(res.headers.get('retry-after'));
          const code = mapErrorCode(res.status, res.headers, retryAfter);

          if (this.logger) {
            try {
              const { githubApiCallFailed } = await import('../../logger-events.js');
              githubApiCallFailed(this.logger, {
                method,
                path,
                status: res.status,
                error: snippet,
              });
            } catch {
              // silent
            }
          }

          const err = /** @type {Error & { code?: string, status?: number, retryAfter?: number }} */ (
            new Error(`GitHub API ${res.status}: ${path} — ${snippet}`)
          );
          err.code = code;
          err.status = res.status;
          if (retryAfter !== undefined) err.retryAfter = retryAfter;
          throw err;
        }

        // Success path: emit NDJSON + return raw json.
        if (this.logger) {
          try {
            const { githubApiCall } = await import('../../logger-events.js');
            githubApiCall(this.logger, {
              method,
              path,
              status: res.status,
              duration_ms: Date.now() - started,
              rate_limit_remaining: this._rateRemaining,
            });
          } catch {
            // silent
          }
        }

        // Stash ETag para que listIssues lo pueda envolver sin re-parsear headers.
        this._lastEtag = res.headers.get('etag') ?? undefined;

        return res.json();
      }

      /**
       * GET /repos/{owner}/{repo}/issues/{number} → raw issue payload.
       * `body` puede ser `null` cuando la issue se creó sin descripción (Pitfall #1).
       *
       * @param {string} owner
       * @param {string} repo
       * @param {number} number
       * @returns {Promise<any>}
       */
      async getIssue(owner, repo, number) {
        const o = encodeURIComponent(owner);
        const r = encodeURIComponent(repo);
        return this.request(`/repos/${o}/${r}/issues/${number}`);
      }

      /**
       * GET /repos/{owner}/{repo}/issues → envelope `{status, items, etag, rate_limit_remaining}`.
       *
       * **Pitfall #2:** GitHub considera PRs como "issues con `pull_request` ≠ null".
       * Este endpoint devuelve ambos intermixed. Phase 24 normalizer filtra los PRs.
       *
       * **Pitfall #6:** El ETag está calculado sobre el conjunto exacto de query params.
       * Si Phase 25 cambia `labels` o `state` entre llamadas, el ETag ya no aplica.
       *
       * @param {string} owner
       * @param {string} repo
       * @param {{
       *   labels?: string[],
       *   state?: 'open'|'closed'|'all',
       *   since?: string,
       *   etag?: string,
       *   per_page?: number,
       * }} [opts]
       * @returns {Promise<{ status: 200 | 304, items: any[], etag: string | undefined, rate_limit_remaining: number | undefined }>}
       */
      async listIssues(owner, repo, opts = {}) {
        const o = encodeURIComponent(owner);
        const r = encodeURIComponent(repo);
        const params = {};
        if (opts.state) params.state = opts.state;
        if (opts.labels && opts.labels.length > 0) params.labels = opts.labels.join(',');
        if (opts.since) params.since = opts.since;
        params.per_page = String(opts.per_page ?? 100);

        const result = await this.request(`/repos/${o}/${r}/issues`, {
          params,
          etag: opts.etag,
        });

        // 304 path: `request()` ya construyó el envelope.
        if (result && typeof result === 'object' && result.status === 304) {
          return result;
        }

        // 200 path: envuelve el array raw en envelope.
        return {
          status: 200,
          items: Array.isArray(result) ? result : [],
          etag: this._lastEtag,
          rate_limit_remaining: this._rateRemaining,
        };
      }

      /**
       * POST /repos/{owner}/{repo}/issues/{number}/comments → raw comment 201.
       * Body **markdown** (no HTML — diferencia con Plane).
       *
       * @param {string} owner
       * @param {string} repo
       * @param {number} number
       * @param {string} markdownBody
       * @returns {Promise<any>}
       */
      async addComment(owner, repo, number, markdownBody) {
        const o = encodeURIComponent(owner);
        const r = encodeURIComponent(repo);
        return this.request(`/repos/${o}/${r}/issues/${number}/comments`, {
          method: 'POST',
          body: { body: markdownBody },
        });
      }

      /**
       * PATCH /repos/{owner}/{repo}/issues/{number} → raw issue payload.
       * `updates.labels` es REPLACE, no merge.
       *
       * @param {string} owner
       * @param {string} repo
       * @param {number} number
       * @param {{ state?: 'open'|'closed', state_reason?: string|null, title?: string, body?: string, labels?: string[], assignees?: string[] }} updates
       * @returns {Promise<any>}
       */
      async updateIssue(owner, repo, number, updates) {
        const o = encodeURIComponent(owner);
        const r = encodeURIComponent(repo);
        return this.request(`/repos/${o}/${r}/issues/${number}`, {
          method: 'PATCH',
          body: updates,
        });
      }

      /**
       * GET /repos/{owner}/{repo}/labels → array de labels raw.
       *
       * @param {string} owner
       * @param {string} repo
       * @returns {Promise<any[]>}
       */
      async listLabels(owner, repo) {
        const o = encodeURIComponent(owner);
        const r = encodeURIComponent(repo);
        return this.request(`/repos/${o}/${r}/labels`, { params: { per_page: '100' } });
      }
    }
    ```

    Notas críticas:
    - **Crear el directorio `src/providers/github/` implícitamente** al escribir el archivo. Si Write
      falla con "directory not found", crear primero con `mkdir -p src/providers/github`.
  </action>
  <verify>
    <automated>node --check src/providers/github/client.js && echo "SYNTAX_OK" && node -e "import('./src/providers/github/client.js').then(m => { if (typeof m.GitHubClient !== 'function') process.exit(1); console.log('GitHubClient class loaded'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `src/providers/github/client.js` existe.
    - `node --check src/providers/github/client.js` exit 0.
    - `grep -c "^export class GitHubClient" src/providers/github/client.js` === 1.
    - `grep -c "async getIssue\|async listIssues\|async addComment\|async updateIssue\|async listLabels" src/providers/github/client.js` === 5.
    - `grep -c "AbortSignal.timeout(10_000)" src/providers/github/client.js` === 1.
    - `grep -c "encodeURIComponent" src/providers/github/client.js` ≥ 10 (5 métodos × 2 args).
    - `grep -c "'Authorization': 'token " src/providers/github/client.js` === 0 (string literal con backticks abajo).
    - `grep -E "'Authorization':\\s*\`token \\\$\\{this\\.token\\}\`" src/providers/github/client.js | wc -l` === 1.
    - `grep -c "X-GitHub-Api-Version': '2022-11-28'" src/providers/github/client.js` === 1.
    - `grep -c "User-Agent': 'kodo/0.7.x'" src/providers/github/client.js` === 1.
    - `grep "maxRetries\|while (true)" src/providers/github/client.js | wc -l` === 0 (D-11 no retry).
    - `grep "console.warn\|console.log" src/providers/github/client.js | wc -l` === 0 (Pitfall #7 — NDJSON only).
    - `grep -E "from\\s+['\"]picocolors" src/providers/github/client.js | wc -l` === 0 (color isolation).
    - `grep -E "from\\s+['\"]\\.\\./\\.\\./logger\\.js" src/providers/github/client.js | wc -l` === 0 (logger via dynamic import only, not static).
    - `node -e "import('./src/providers/github/client.js').then(m => { if (typeof m.GitHubClient !== 'function') process.exit(1); })"` exit 0.
  </acceptance_criteria>
  <done>GitHubClient creado con 5 métodos públicos, `request()` privado (CERO retry, CERO throttle, envelope 304), 2 helpers internos (`parseRetryAfter`, `mapErrorCode`), JSDoc completo, cero imports prohibidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Llenar test/providers/github/client.test.js con los 12 tests de la verification map</name>
  <files>test/providers/github/client.test.js</files>
  <read_first>
    - Re-leer `23-VALIDATION.md §Per-Task Verification Map` filas 23-02-01..12 — son los 12 tests
      a implementar.
    - Re-leer `23-RESEARCH.md §Testing Strategy` (líneas 545-585) — tabla de 12 test cases con
      asserts específicos.
    - Confirmar que Task 2 dejó el `describe('GitHubClient', () => { /* tests aquí */ });` listo
      y que los 3 helpers (`makeFetch`, `makeSpyFetch`, `makeSpyLogger`) están disponibles.
  </read_first>
  <behavior>
    Cada test inyecta `fetch` y NO toca `globalThis`. Asserts específicos (no genéricos
    `assert.ok(result)`). Los 12 tests cubren:
    1. constructor throws sin token (23-02-01 / SC#1)
    2. getIssue retorna raw payload + parsea rate-limit (23-02-02 / SC#1)
    3. listIssues 200 envelope (23-02-03 / SC#3)
    4. listIssues 304 envelope sin throw (23-02-04 / SC#3)
    5. listIssues envía If-None-Match cuando opts.etag (SC#3 wire)
    6. listIssues envía state/labels/since/per_page como query params (SC#1)
    7. 401 → .code='unauthorized' (23-02-10 / SC#1)
    8. 404 → .code='not_found' (23-02-10 / SC#1)
    9. 429 + Retry-After: 60 → .code='rate_limit_exceeded', .retryAfter=60 (23-02-05 / SC#2)
    10. 403 + X-RateLimit-Remaining: 0 → .code='rate_limit_exceeded' (23-02-10 / SC#2)
    11. rate_limit_remaining < 100 → spy logger recibe warn (23-02-06 / SC#2)
    12. addComment POST body markdown intacto (23-02-07 / SC#1)

    Adicional (no en verification map pero útil para cobertura SC#1):
    - updateIssue PATCH body (23-02-08)
    - listLabels GET array (23-02-09)

    Total: 14 it() — más que los 12 requeridos por la verification map.
  </behavior>
  <action>
    Reemplazar la línea `// Task 3 + Task 4 añaden los 12 tests aquí.` dentro de
    `describe('GitHubClient', () => { ... });` por los siguientes 14 tests. Mantener todo lo demás
    del archivo intacto (header, imports, helpers).

    ```js
      // ───────────────────────────────────────────────────────────────────────
      // SC#1 — constructor + auth + headers + 5 método surface
      // ───────────────────────────────────────────────────────────────────────

      it('constructor throws when token unset (row 23-02-01)', () => {
        // No token, no env override. getProviderApiKey('github') returns undefined porque
        // el config v0.6 no tiene `providers.github`. El constructor debe lanzar.
        assert.throws(
          () => new GitHubClient({ fetch: makeFetch({ status: 200, body: {} }) }),
          /GitHub token not found\. Set GITHUB_TOKEN env var\./,
        );
      });

      it('getIssue returns raw payload + parses x-ratelimit-remaining (row 23-02-02)', async () => {
        const { fetch, calls } = makeSpyFetch({
          status: 200,
          body: issueFixture,
          headers: { 'x-ratelimit-remaining': '4998', 'x-ratelimit-reset': '1747200000' },
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        const issue = await client.getIssue('octocat', 'hello-world', 42);

        assert.equal(issue.number, 42);
        assert.equal(issue.title, 'Test issue');
        assert.equal(issue.state, 'open');
        assert.equal(Array.isArray(issue.labels), true);
        assert.equal(issue.labels[0].name, 'kodo');
        assert.equal(client._rateRemaining, 4998);

        // Headers correctos
        assert.equal(calls.length, 1);
        const headers = calls[0].init.headers;
        assert.equal(headers['Authorization'], 'token ghp_test');
        assert.equal(headers['Accept'], 'application/vnd.github+json');
        assert.equal(headers['X-GitHub-Api-Version'], '2022-11-28');
        assert.equal(headers['User-Agent'], 'kodo/0.7.x');
      });

      it('listIssues 200 path returns envelope {status, items, etag, rate_limit_remaining} (row 23-02-03)', async () => {
        const { fetch } = makeSpyFetch({
          status: 200,
          body: issuesListFixture,
          headers: { 'x-ratelimit-remaining': '4500', 'etag': 'W/"abc123"' },
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        const result = await client.listIssues('octocat', 'hello-world');

        assert.equal(result.status, 200);
        assert.equal(Array.isArray(result.items), true);
        assert.equal(result.items.length, 3); // 2 issues + 1 PR (Pitfall #2 — Phase 24 filtra)
        assert.equal(result.etag, 'W/"abc123"');
        assert.equal(result.rate_limit_remaining, 4500);
      });

      it('listIssues 304 path returns envelope {status:304, items:[], etag, rate_limit_remaining} WITHOUT throwing (row 23-02-04 / SC#3)', async () => {
        const { fetch } = makeSpyFetch({
          status: 304,
          headers: { 'x-ratelimit-remaining': '4499', 'etag': 'W/"abc123"' },
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });

        // El test PRINCIPAL: no throw + envelope correcto.
        const result = await client.listIssues('octocat', 'hello-world', { etag: 'W/"abc123"' });

        assert.equal(result.status, 304);
        assert.deepEqual(result.items, []);
        assert.equal(result.etag, 'W/"abc123"');
        assert.equal(result.rate_limit_remaining, 4499);
      });

      it('listIssues sends If-None-Match header when opts.etag provided', async () => {
        const { fetch, calls } = makeSpyFetch({
          status: 200,
          body: [],
          headers: { 'etag': 'W/"new"' },
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        await client.listIssues('octocat', 'hello-world', { etag: 'W/"old"' });

        assert.equal(calls.length, 1);
        assert.equal(calls[0].init.headers['If-None-Match'], 'W/"old"');
      });

      it('listIssues encodes state/labels/since/per_page as query params', async () => {
        const { fetch, calls } = makeSpyFetch({ status: 200, body: [], headers: {} });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        await client.listIssues('octocat', 'hello-world', {
          state: 'open',
          labels: ['kodo', 'priority:high'],
          since: '2026-05-14T00:00:00Z',
          per_page: 100,
        });

        assert.equal(calls.length, 1);
        const url = calls[0].url;
        assert.match(url, /state=open/);
        assert.match(url, /labels=kodo%2Cpriority%3Ahigh/);
        assert.match(url, /since=2026-05-14T00%3A00%3A00Z/);
        assert.match(url, /per_page=100/);
      });

      // ───────────────────────────────────────────────────────────────────────
      // SC#1 / SC#2 — Error mapping table (row 23-02-10)
      // ───────────────────────────────────────────────────────────────────────

      it('401 throws Error with .code === "unauthorized", .status === 401', async () => {
        const client = new GitHubClient({
          token: 'ghp_test',
          fetch: makeFetch({ status: 401, body: unauthorizedFixture }),
        });
        await assert.rejects(
          () => client.getIssue('octocat', 'hello-world', 42),
          (err) => {
            assert.equal(err.code, 'unauthorized');
            assert.equal(err.status, 401);
            assert.match(err.message, /GitHub API 401:/);
            return true;
          },
        );
      });

      it('404 throws Error with .code === "not_found", .status === 404', async () => {
        const client = new GitHubClient({
          token: 'ghp_test',
          fetch: makeFetch({ status: 404, body: notFoundFixture }),
        });
        await assert.rejects(
          () => client.getIssue('octocat', 'missing-repo', 999),
          (err) => {
            assert.equal(err.code, 'not_found');
            assert.equal(err.status, 404);
            return true;
          },
        );
      });

      it('429 with Retry-After: 60 → .code="rate_limit_exceeded", .retryAfter=60 (row 23-02-05 / SC#2)', async () => {
        const client = new GitHubClient({
          token: 'ghp_test',
          fetch: makeFetch({
            status: 429,
            body: rateLimitExceededFixture,
            headers: { 'retry-after': '60' },
          }),
        });
        await assert.rejects(
          () => client.getIssue('octocat', 'hello-world', 42),
          (err) => {
            assert.equal(err.code, 'rate_limit_exceeded');
            assert.equal(err.status, 429);
            assert.equal(err.retryAfter, 60);
            return true;
          },
        );
      });

      it('403 with X-RateLimit-Remaining: 0 → .code="rate_limit_exceeded" (secondary rate limit map)', async () => {
        const client = new GitHubClient({
          token: 'ghp_test',
          fetch: makeFetch({
            status: 403,
            body: forbiddenFixture,
            headers: { 'x-ratelimit-remaining': '0' },
          }),
        });
        await assert.rejects(
          () => client.getIssue('octocat', 'hello-world', 42),
          (err) => {
            assert.equal(err.code, 'rate_limit_exceeded');
            assert.equal(err.status, 403);
            return true;
          },
        );
      });

      // ───────────────────────────────────────────────────────────────────────
      // SC#2 — Rate-limit warn NDJSON (row 23-02-06)
      // ───────────────────────────────────────────────────────────────────────

      it('emits NDJSON at warn level when X-RateLimit-Remaining < 100 (row 23-02-06 / SC#2)', async () => {
        const logger = makeSpyLogger();
        const client = new GitHubClient({
          token: 'ghp_test',
          logger,
          fetch: makeFetch({
            status: 200,
            body: rateLimitLowFixture,
            headers: { 'x-ratelimit-remaining': '50' },
          }),
        });
        await client.getIssue('octocat', 'hello-world', 42);

        // Debe haber un único record con level=warn y rate_limit_remaining=50.
        const warns = logger.records.filter((r) => r.level === 'warn');
        assert.equal(warns.length, 1);
        assert.equal(warns[0].event, 'github.api.call');
        assert.equal(warns[0].rate_limit_remaining, 50);
      });

      it('emits NDJSON at info level when X-RateLimit-Remaining >= 100', async () => {
        const logger = makeSpyLogger();
        const client = new GitHubClient({
          token: 'ghp_test',
          logger,
          fetch: makeFetch({
            status: 200,
            body: issueFixture,
            headers: { 'x-ratelimit-remaining': '4998' },
          }),
        });
        await client.getIssue('octocat', 'hello-world', 42);

        const infos = logger.records.filter((r) => r.level === 'info');
        assert.equal(infos.length, 1);
        assert.equal(infos[0].event, 'github.api.call');
        assert.equal(infos[0].rate_limit_remaining, 4998);
        assert.equal(logger.records.filter((r) => r.level === 'warn').length, 0);
      });

      // ───────────────────────────────────────────────────────────────────────
      // SC#1 — addComment / updateIssue / listLabels (rows 23-02-07, 23-02-08, 23-02-09)
      // ───────────────────────────────────────────────────────────────────────

      it('addComment POSTs markdown body intact (row 23-02-07)', async () => {
        const { fetch, calls } = makeSpyFetch({
          status: 201,
          body: commentCreatedFixture,
          headers: {},
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        const result = await client.addComment('octocat', 'hello-world', 42, 'hello **world**');

        assert.equal(result.id, 123);
        assert.equal(result.author_association, 'OWNER');

        // Verificar el body enviado
        assert.equal(calls.length, 1);
        assert.equal(calls[0].init.method, 'POST');
        const sentBody = JSON.parse(calls[0].init.body);
        assert.equal(sentBody.body, 'hello **world**');
        assert.equal(calls[0].init.headers['Content-Type'], 'application/json');
      });

      it('updateIssue PATCHes with body payload (row 23-02-08)', async () => {
        const { fetch, calls } = makeSpyFetch({
          status: 200,
          body: { ...issueFixture, state: 'closed' },
          headers: {},
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        const result = await client.updateIssue('octocat', 'hello-world', 42, { state: 'closed' });

        assert.equal(result.state, 'closed');
        assert.equal(calls[0].init.method, 'PATCH');
        const sentBody = JSON.parse(calls[0].init.body);
        assert.deepEqual(sentBody, { state: 'closed' });
      });

      it('listLabels returns raw array of {id, name, color} (row 23-02-09)', async () => {
        const { fetch } = makeSpyFetch({
          status: 200,
          body: labelsListFixture,
          headers: {},
        });
        const client = new GitHubClient({ token: 'ghp_test', fetch });
        const labels = await client.listLabels('octocat', 'hello-world');

        assert.equal(Array.isArray(labels), true);
        assert.equal(labels.length, 3);
        assert.equal(labels[0].name, 'kodo');
        assert.equal(labels[0].color, '0e8a16');
        assert.equal(labels[2].name, 'priority:high');
      });
    ```
  </action>
  <verify>
    <automated>node --test test/providers/github/client.test.js 2>&1 | grep -E "^(# pass|# fail|# tests|^not ok)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^      it(" test/providers/github/client.test.js` === 14.
    - `node --test test/providers/github/client.test.js 2>&1 | grep -c "^not ok"` === 0.
    - `node --test test/providers/github/client.test.js 2>&1 | grep "^# pass" | awk '{print $3}'` === 14.
    - `grep -c "globalThis.fetch" test/providers/github/client.test.js` === 0 (zero global mutation).
    - `grep "row 23-02-01\|row 23-02-02\|row 23-02-03\|row 23-02-04\|row 23-02-05\|row 23-02-06\|row 23-02-07\|row 23-02-08\|row 23-02-09\|row 23-02-10" test/providers/github/client.test.js | wc -l` ≥ 10 (todas las filas referenciadas).
  </acceptance_criteria>
  <done>14 tests pasan en verde, cubren las 12 filas del verification map + 2 tests extra (info-level path, listLabels). Zero llamadas a globalThis.fetch.</done>
</task>

<task type="auto">
  <name>Task 5: Verificar invariantes LOG-12, color isolation y full suite green (incluye check-isolation)</name>
  <files></files>
  <read_first>
    - Confirmar que `src/providers/github/client.js` no importa `picocolors` ni `src/logger.js` estáticamente.
    - Confirmar que `src/check.js` NO importa `src/providers/github/client.js` (LOG-12 — verificado
      automáticamente por `test/check-isolation.test.js`).
  </read_first>
  <action>
    Ejecutar las verificaciones invariantes y la suite full:

    1. **LOG-12 canary (existing test):**
       ```bash
       node --test test/check-isolation.test.js
       ```
       Debe pasar verde. Si falla, significa que `src/check.js` empezó a importar transitivamente
       el cliente GitHub o el logger — investigar el grafo.

    2. **Color isolation — `picocolors` solo desde `src/cli/format.js`:**
       ```bash
       grep -rnE "from\s+['\"]picocolors" src/providers/github/ test/providers/github/ test/fixtures/github/ 2>/dev/null && echo "FAIL: picocolors leaked" && exit 1 || echo "OK"
       ```

    3. **GitHub client NO importa src/logger.js estáticamente** (dynamic import OK):
       ```bash
       grep -E "^import.*from\s+['\"]\.\.\/\.\.\/logger\.js" src/providers/github/client.js
       ```
       Debe devolver 0 líneas (el cliente usa dynamic `await import('../../logger-events.js')` para
       los helpers, NO `logger.js` direct).

    4. **Quick run de los 2 test files clave:**
       ```bash
       node --test test/providers/github/client.test.js test/logger-events.test.js test/check-isolation.test.js
       ```
       Todas verdes.

    5. **Suite full:**
       ```bash
       npm test
       ```
       Verde con baseline 614 + 3 (Plan 23-01) + 14 (Plan 23-02) = ≥ 631 pass.

    6. **JSON validity de fixtures:**
       ```bash
       for f in test/fixtures/github/*.json; do node -e "JSON.parse(require('node:fs').readFileSync('$f','utf8'))" || echo "INVALID: $f"; done
       ```
  </action>
  <verify>
    <automated>node --test test/check-isolation.test.js test/providers/github/client.test.js 2>&1 | tail -5; echo "---picocolors leak check---"; grep -rnE "from\\s+['\"]picocolors" src/providers/github/ test/providers/github/ 2>/dev/null | head; echo "---npm test summary---"; npm test 2>&1 | grep -E "^# (pass|fail|tests)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/check-isolation.test.js 2>&1 | grep -c "^not ok"` === 0.
    - `grep -rnE "from\\s+['\"]picocolors" src/providers/github/ test/providers/github/ test/fixtures/github/ 2>/dev/null | wc -l` === 0.
    - `grep -E "^import.*from\\s+['\"]\\.\\.\\/\\.\\.\\/logger\\.js" src/providers/github/client.js` devuelve 0 líneas.
    - `npm test 2>&1 | grep -E "^# fail" | awk '{print $3}'` === `0`.
    - `npm test 2>&1 | grep -E "^# pass" | awk '{print $3}'` ≥ 631 (baseline 614 + 3 Plan 23-01 + 14 Plan 23-02).
    - `for f in test/fixtures/github/*.json; do node -e "JSON.parse(require('node:fs').readFileSync('$f','utf8'))" 2>&1; done | wc -l` === 0 (zero stderr).
  </acceptance_criteria>
  <done>LOG-12 canary verde, picocolors no leakea, logger.js NO importado estáticamente, suite full green con ≥ 631 pass, fixtures todas válidas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node process → GitHub.com (TLS) | Cliente envía PAT en header `Authorization`. Solo `https://api.github.com` por default. `opts.baseUrl` permite override para fake-server testing local. |
| `opts.owner`/`opts.repo` (caller) → URL path | Untrusted input — caller puede pasar `..` o `?`. Mitigación: `encodeURIComponent` antes de interpolar. |
| Error.message → logs/stderr | El snippet del body de error (`text.slice(0, 200)`) puede contener data sensible (raro, pero posible si GitHub echoea). Mitigación: límite 200 chars + redactor regex de Phase 6. |
| GitHub response → Logger NDJSON | Headers parseados (`x-ratelimit-*`) entran al record. NO incluyen secrets. El PAT NUNCA entra al record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Information Disclosure | PAT en `Error.message` o NDJSON | mitigate | El mensaje `GitHub API ${status}: ${path} — ${snippet}` NO interpola headers. `snippet` es body de error truncado a 200 chars (D-14). El redactor de Phase 6 (existing, `src/logger.js`) tiene regex que cubre `ghp_*` / `github_pat_*`. Verificación manual post-suite: `grep -i "ghp_\|github_pat_" ~/.kodo/logs/*.ndjson` debe devolver 0 (per 23-VALIDATION.md §Manual-Only Verifications). |
| T-23-05 | Tampering (SSRF via owner/repo) | URL path construction | mitigate | `encodeURIComponent(owner)` + `encodeURIComponent(repo)` antes de interpolar. Bloquea `..`, `?`, `#`, `/` malicioso. Cubierto en todos los 5 métodos (Task 3 acceptance: ≥ 10 calls a encodeURIComponent). |
| T-23-06 | Tampering (TLS downgrade) | `opts.baseUrl` override | accept | Phase 23 acepta `http://127.0.0.1:<port>` para testing local con fake server. NO validamos `https://` explícitamente — la responsabilidad de no usar `http://` en producción la lleva el config (Phase 26 CFG-02 lockea `https://api.github.com` default). Risk acepted porque Phase 23 no expone configurabilidad pública del baseUrl al usuario final. |
| T-23-07 | Denial of Service (timeout) | request fetch hang | mitigate | `AbortSignal.timeout(10_000)` (D-05) — 10s timeout por request. Si el body streaming se cuelga, `res.json()` rechaza con `AbortError` (Pitfall #9). El test suite no cubre este caso directamente (live behavior, hard to mock fiel-mente), accept residual. |
| T-23-08 | Information Disclosure | rate-limit info en NDJSON | accept | `rate_limit_remaining` es info pública (en headers de la response). Loggearlo es intencional y útil para telemetría. No expone secrets. |
</threat_model>

<verification>
- `node --test test/providers/github/client.test.js` — verde con 14 tests pass (12 rows + 2 extras).
- `node --test test/logger-events.test.js` — sigue verde con 20+ tests (incluye los 3 añadidos por Plan 23-01).
- `node --test test/check-isolation.test.js` — verde (LOG-12 invariant: `src/check.js` no carga `src/providers/github/client.js` transitivamente).
- `grep -rnE "from\\s+['\"]picocolors" src/providers/github/ test/providers/github/` — 0 líneas (color isolation).
- `grep -E "^import.*from\\s+['\"]\\.\\.\\/\\.\\.\\/logger\\.js" src/providers/github/client.js` — 0 líneas (NO static import logger.js).
- `grep "maxRetries\|while (true)" src/providers/github/client.js` — 0 líneas (D-11 no retry).
- `grep "console.warn\|console.log" src/providers/github/client.js` — 0 líneas (Pitfall #7 — NDJSON only).
- `npm test` — verde con ≥ 631 pass (baseline 614 + 3 Plan 23-01 + 14 Plan 23-02).
- 10 fixtures JSON parseables en `test/fixtures/github/`.
- Manual smoke (opcional, no bloqueante): `grep -i "ghp_\|github_pat_" ~/.kodo/logs/*.ndjson 2>/dev/null` → 0 matches (redactor de Phase 6 funciona).
</verification>

<success_criteria>
**ROADMAP SC mapping (Phase 23):**

- **SC#1** — `src/providers/github/client.js` exporta `GitHubClient` con `getIssue`, `listIssues`,
  `addComment`, `updateIssue`, `listLabels`. Header `Authorization: token <PAT>` enviado en cada request.
  Token leído via `getProviderApiKey('github')`. Verificado por test "constructor throws when token unset"
  + test "getIssue returns raw payload + parses x-ratelimit-remaining" (assert headers).
- **SC#2** — Emite `github.api.call` warn-level cuando `X-RateLimit-Remaining < 100` (verificado por
  "emits NDJSON at warn level when X-RateLimit-Remaining < 100"). Rechaza con `Error.code === 'rate_limit_exceeded'`
  en `429` (verificado por test 429 + Retry-After). El helper `githubApiCall` proviene de Plan 23-01.
- **SC#3** — `listIssues({etag})` con respuesta 304 devuelve `{status:304, items:[], etag, rate_limit_remaining}`
  sin throw (verificado por test "listIssues 304 path returns envelope … WITHOUT throwing").
- **SC#4** — 14 tests offline en `test/providers/github/client.test.js` (≥ 8 required), 10 fixtures
  en `test/fixtures/github/*.json`, ZERO `globalThis.fetch` mutation.

**Plan-level success:**
- Clase + 5 métodos + `request()` privado + 2 helpers internos = ~190 LOC en `src/providers/github/client.js`.
- 14 tests verdes en `test/providers/github/client.test.js`.
- 10 fixtures JSON válidas en `test/fixtures/github/`.
- LOG-12 invariant preservada (check-isolation verde).
- Color isolation preservada (picocolors no leakea).
- `npm test` baseline ≥ 631 pass.
- Cross-phase invariant DX-06 (byte-determinism): logger NDJSON añadido NO incluye `Date.now()`
  en payloads no-determinísticos — `duration_ms` se calcula en el caller pero entra al record
  como número plain, sin formato dependiente del environment (asegurar en asserts: `assert.ok(record.duration_ms >= 0)`
  estilo Pitfall #10 si flakea).

**Coverage to D-XX decisions:**
- D-01..D-03 (módulo class, single file, ts-check + JSDoc): Task 3 estructura.
- D-04..D-06 (globalThis.fetch, AbortSignal.timeout 10s, fetch inyectable): Task 3 constructor + request.
- D-07..D-10 (getProviderApiKey, header `token`, opts.token override, env_var from config): Task 3.
- D-11..D-14 (no retry, Error con .code/.status/.retryAfter, mensajes formato): Task 3 + Task 4 tests.
- D-15..D-18 (2 NDJSON events, switch info/warn, helpers exported, logger opcional): Plan 23-01 + Task 3 wiring.
- D-19..D-21 (envelope 304 stateless, no persistencia etag, solo listIssues): Task 3 listIssues + Task 4 tests.
- D-22..D-26 (5 métodos posicionales, raw response, single page): Task 3 método signatures + JSDoc.
- D-27..D-29 (request privado, parse rate headers, no proactive throttle): Task 3 request().
- D-30..D-31 (default baseUrl, GHE out of scope): Task 3 constructor.
- D-32..D-36 (test infra: directory, fixtures, fetch inject, node:test): Tasks 1-4.
- D-37..D-39 (capitalization, User-Agent, header order): Task 3 export + headers.
</success_criteria>

<output>
After completion, create `.planning/phases/23-githubclient-auth-foundation/23-02-SUMMARY.md`
following `$HOME/.claude/get-shit-done/templates/summary.md`. Capture:
- Lines of code in `src/providers/github/client.js` (target ~190).
- Test counts: 14 in client.test.js + 3 (Plan 23-01) + baseline = ≥ 631.
- LOG-12 evidence: `node --test test/check-isolation.test.js` output snippet.
- Color isolation evidence: `grep -rn picocolors src/providers/github/` output (empty).
- Any divergences from CONTEXT.md/RESEARCH.md/PATTERNS.md introduced during execution
  (should be ZERO; if any, justify in retrospective for Phase 24 to absorb).
- Fixtures total bytes (sanity — ~5-10 KB total).
</output>
