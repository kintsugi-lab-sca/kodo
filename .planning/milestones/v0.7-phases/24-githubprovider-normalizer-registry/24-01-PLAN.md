---
phase: 24-githubprovider-normalizer-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/github/normalize.js
  - test/providers/github/normalize.test.js
  - test/fixtures/github/issue-with-priority.json
  - test/fixtures/github/issue-with-kodo.json
  - test/fixtures/github/issue-closed.json
  - test/fixtures/github/issue-no-body.json
  - test/fixtures/github/issue-no-labels.json
autonomous: true
requirements:
  - GH-03
  - TEST-01
requirements_addressed:
  - GH-03
  - TEST-01
tags:
  - github
  - normalizer
  - provider-abstraction

must_haves:
  truths:
    - "A GitHub Issue payload becomes a canonical TaskItem with id=node_id, ref=owner/repo#N, state literal, groups=[]."
    - "Priority extraction from labels prefix 'priority:' returns urgent/high/medium/low or null (case-insensitive, no aliases)."
    - "`body: null` o ausente produce description=''; labels=[] produce labels: [] y priority: null."
    - "extractPriority es export púbico y testeable independiente del normalizer principal."
    - "5 fixtures incrementales existen y son fork mínimo de issue.json (un solo campo cambiado por fixture)."
    - "Project conventions source: .planning/codebase/CONVENTIONS.md (CLAUDE.md project-level not present — checker resolved B4 as false-positive)."
    - "Archivos creados: src/providers/github/normalize.js + 5 fixtures + test/providers/github/normalize.test.js; NO crear src/providers/github/labels.js (parseKodoLabels invariante) (D-03)."
    - "// @ts-check en línea 1 + JSDoc completo en todos los exports públicos del normalizer (CONVENTIONS.md repo) (D-04)."
    - "normalizeIssue(issue, context) es función pura — cero API calls, cero side effects; mirror estructural de normalizeWorkItem (src/providers/plane/normalize.js:64) (D-06)."
    - "TaskItem.id = issue.node_id (string opaco GitHub, estable cross-rename) — NUNCA issue.id numérico (D-07)."
    - "TaskItem.ref = `${context.projectId}#${issue.number}` con projectId='owner/repo' del context (REQ GH-03 literal) (D-08)."
    - "TaskItem.title = issue.title literal (D-09)."
    - "TaskItem.description = issue.body || '' (Markdown crudo, sin strip; body null/undefined → '') (D-10)."
    - "TaskItem.labels = issue.labels.map(l => l.name) con map defensivo para tolerar labels en forma string (D-11)."
    - "TaskItem.projectId = context.projectId literal ('owner/repo') (D-12)."
    - "TaskItem.projectName = context.projectId (mismo string que projectId — GitHub no tiene 'name' separado del slug) (D-13)."
    - "TaskItem.groups = [] hardcoded; milestone GitHub NO se extrae (cierra open question STATE.md) (D-14)."
    - "TaskItem.url = issue.html_url literal (REQ GH-03) (D-15)."
    - "TaskItem.state = issue.state literal ('open'|'closed'), sin transformación — dispatcher consume config-driven (D-16)."
    - "TaskItem.priority extraída via extractPriority(labels) buscando prefix 'priority:' case-insensitive; whitelist urgent/high/medium/low (subset de VALID_PRIORITIES, sin 'none' ni aliases p0/critical/blocker); default null (D-17)."
    - "Zero fugas de campos GitHub-only (assignees, milestone, pull_request, user, comments, locked, state_reason, created_at, updated_at, reactions) en el TaskItem retornado (D-18)."
    - "5 fixtures incrementales nuevas en test/fixtures/github/: issue-with-priority/issue-with-kodo/issue-closed/issue-no-body/issue-no-labels (cada una fork mínimo de issue.json con UN campo mutado) (D-34)."
    - "Cobertura ≥ 90% branches del normalizer cubriendo priority extraction (urgent/high/medium/low + miss + invalid + case-insensitive), body null/undefined/string, labels array/empty/string-form, state open/closed, id vs node_id, groups siempre [] (D-35)."
  artifacts:
    - path: "src/providers/github/normalize.js"
      provides: "normalizeIssue + extractPriority puros, sin side effects, sin API calls"
      exports: ["normalizeIssue", "extractPriority"]
      min_lines: 60
    - path: "test/providers/github/normalize.test.js"
      provides: "Tests del normalizer, ≥90% branches"
      min_lines: 100
    - path: "test/fixtures/github/issue-with-priority.json"
      provides: "Fixture con label priority:high"
    - path: "test/fixtures/github/issue-with-kodo.json"
      provides: "Fixture con labels kodo + kodo:sonnet"
    - path: "test/fixtures/github/issue-closed.json"
      provides: "Fixture state=closed"
    - path: "test/fixtures/github/issue-no-body.json"
      provides: "Fixture body=null"
    - path: "test/fixtures/github/issue-no-labels.json"
      provides: "Fixture labels=[]"
  key_links:
    - from: "src/providers/github/normalize.js"
      to: "src/interface.js"
      via: "import { VALID_PRIORITIES }"
      pattern: "import.*VALID_PRIORITIES.*interface"
    - from: "test/providers/github/normalize.test.js"
      to: "src/providers/github/normalize.js"
      via: "import { normalizeIssue, extractPriority }"
      pattern: "import.*normalizeIssue.*extractPriority"
    - from: "test/providers/github/normalize.test.js"
      to: "test/fixtures/github/issue*.json"
      via: "import json with type: json"
      pattern: "with.*type.*json"
---

<objective>
Implementar el normalizer puro `GitHub Issue → TaskItem` y crear las 5 fixtures incrementales necesarias para cubrir las branches críticas. Phase 24 leaf — sin dependencias del provider/registry.

Purpose: GH-03 lock-in del shape canonical (id=node_id, ref=owner/repo#N, labels strings, priority desde label `priority:*`, groups=[], state literal). El normalizer es función pura — el provider.js (Wave 2) lo consume.

Output:
- `src/providers/github/normalize.js` (~80-100 LOC) — `normalizeIssue` + `extractPriority` exports
- 5 fixtures JSON nuevas (fork mínimo de `issue.json` Phase 23)
- `test/providers/github/normalize.test.js` con ≥90% branches del normalizer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md
@.planning/phases/24-githubprovider-normalizer-registry/24-RESEARCH.md
@.planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md
@.planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md

# Sources of truth para el shape canonical y patterns
@src/interface.js
@src/providers/plane/normalize.js
@test/normalize.test.js
@test/fixtures/github/issue.json

<interfaces>
<!-- TaskItem canonical (src/interface.js:10-24) — destino de normalizeIssue. -->
```typescript
type TaskItem = {
  id: string;             // GitHub node_id (D-07)
  ref: string;            // owner/repo#number (D-08)
  title: string;
  description: string;    // body Markdown crudo, sin strip (D-10); body null → ''
  labels: string[];       // GitHub labels[].name (D-11)
  projectId: string;      // context.projectId = 'owner/repo' (D-12)
  projectName: string;    // mismo string que projectId (D-13)
  groups: string[];       // siempre [] (D-14)
  url: string;            // issue.html_url (D-15)
  priority: 'urgent'|'high'|'medium'|'low'|'none'|null;  // D-17 sin 'none' para GitHub
  state?: string;         // 'open' | 'closed' literal (D-16)
};

// VALID_PRIORITIES = ['urgent','high','medium','low','none'] (frozen)
```

<!-- Plane analog (src/providers/plane/normalize.js:55-80) — template para normalizeIssue. -->
<!-- Phase 24 DROP: stripHtml, resolveWorkItemLabels, parseTriggerEvent (no aplican a GitHub). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 24-01-01: Crear las 5 fixtures incrementales</name>
  <files>
    test/fixtures/github/issue-with-priority.json
    test/fixtures/github/issue-with-kodo.json
    test/fixtures/github/issue-closed.json
    test/fixtures/github/issue-no-body.json
    test/fixtures/github/issue-no-labels.json
  </files>
  <read_first>
    - test/fixtures/github/issue.json (base shape Phase 23 — 20 LOC)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"Fixtures GitHub (5 nuevas)" (líneas ~970-1035) — mutations exactas por fixture
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-34 — lista de fixtures
  </read_first>
  <behavior>
    - Cada fixture mantiene TODOS los campos de issue.json idénticos excepto el campo bajo test.
    - issue-with-priority.json: labels incluye `{name: "priority:high"}` además de `kodo`; number=43, node_id="I_kwTEST002", title="Issue with priority".
    - issue-with-kodo.json: labels = `[{name:"kodo"}, {name:"kodo:sonnet"}]`; number=44, node_id="I_kwTEST003".
    - issue-closed.json: state="closed", state_reason="completed"; number=99, node_id="I_kwTEST099".
    - issue-no-body.json: body=null; number=45, node_id="I_kwTEST004".
    - issue-no-labels.json: labels=[]; number=46, node_id="I_kwTEST005".
  </behavior>
  <action>
    Crear las 5 fixtures siguiendo PATTERNS.md §"Mutations per fixture" (líneas 1001-1034). Cada fixture es un fork mínimo de `test/fixtures/github/issue.json` con UN solo campo mutado (más renombre de `number`/`node_id`/`title` para distinguibilidad).

    Para cada label adicional (priority:high, kodo:sonnet) usar el shape completo de GitHub Issue label: `{ "id": <int>, "node_id": "LA_<TAG>", "name": "<value>", "color": "<hex6>", "default": false, "description": null }` (D-11 — el normalizer hace `.map(l => l.name)` defensivo).

    JSON válido, sin trailing commas, indent 2 spaces, encoding UTF-8.
  </action>
  <verify>
    <automated>node -e "for (const f of ['issue-with-priority','issue-with-kodo','issue-closed','issue-no-body','issue-no-labels']) { const j = require('./test/fixtures/github/'+f+'.json'); if (!j.node_id || !j.number) throw new Error(f+' missing required fields'); }"</automated>
  </verify>
  <acceptance_criteria>
    - `ls test/fixtures/github/issue-with-priority.json test/fixtures/github/issue-with-kodo.json test/fixtures/github/issue-closed.json test/fixtures/github/issue-no-body.json test/fixtures/github/issue-no-labels.json` lista los 5 archivos (exit 0).
    - `node -e "console.log(require('./test/fixtures/github/issue-with-priority.json').labels.filter(l=>l.name==='priority:high').length)"` imprime `1`.
    - `node -e "console.log(require('./test/fixtures/github/issue-with-kodo.json').labels.map(l=>l.name).join(','))"` imprime `kodo,kodo:sonnet`.
    - `node -e "const j=require('./test/fixtures/github/issue-closed.json'); console.log(j.state, j.state_reason)"` imprime `closed completed`.
    - `node -e "const j=require('./test/fixtures/github/issue-no-body.json'); console.log(j.body===null)"` imprime `true`.
    - `node -e "console.log(require('./test/fixtures/github/issue-no-labels.json').labels.length)"` imprime `0`.
    - Cada fixture mantiene los campos GitHub-only originales (`pull_request: null`, `assignees: []`, `user`, `created_at`, `updated_at`, `locked`, `comments`, `html_url`, `id`) — el normalizer debe ignorarlos (D-18).
  </acceptance_criteria>
  <done>5 fixtures JSON existen, son válidas, cada una muta UN solo campo respecto a issue.json (cherry-pick narrativo).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-01-02: Escribir tests del normalizer (RED) cubriendo ≥90% branches</name>
  <files>test/providers/github/normalize.test.js</files>
  <read_first>
    - test/normalize.test.js (analog directo — patrón per-field assertion + edge cases, líneas 1-161)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"5. test/providers/github/normalize.test.js" (líneas 752-880)
    - .planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md §Per-Task Verification Map filas 24-01-02..24-01-06
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-06..D-18 (decisiones del shape), §D-35 (cobertura ≥90%)
    - src/interface.js (typedef TaskItem + VALID_PRIORITIES)
    - test/fixtures/github/issue.json (fixture base — campos disponibles)
  </read_first>
  <behavior>
    Tests obligatorios (cubren D-07..D-18 + D-35 branches):
    - normalizeIssue(issue.json) → result tiene los 11 campos canonical correctos (id=node_id 'I_kwTEST001', ref='octocat/hello-world#42', state='open', groups=[], priority=null porque kodo no es priority).
    - normalizeIssue(issue-no-body.json) → description === '' (D-10).
    - Body undefined inline (delete issue.body) → description === ''.
    - normalizeIssue(issue-no-labels.json) → labels === [] y priority === null.
    - Labels en forma string (mutación inline `issue.labels = ['foo','bar']`) → `.map` defensivo extrae los strings sin crashear (D-11 edge raro).
    - normalizeIssue(issue-closed.json) → state === 'closed' (D-16 literal, sin transformación).
    - normalizeIssue(issue-with-priority.json) → priority === 'high' (D-17).
    - normalizeIssue(issue-with-kodo.json) → labels === ['kodo', 'kodo:sonnet'].
    - Priority 'priority:invalid' inline → priority === null (D-17 whitelist).
    - Priority 'Priority:HIGH' inline (case-insensitive) → priority === 'high'.
    - Priority alias 'priority:p0' inline → priority === null (D-17 sin aliases, lock-in).
    - groups SIEMPRE === [] (incluso si issue tiene `milestone` no-null inline) — D-14.
    - id === node_id, NUNCA === id numérico (D-07).
    - extractPriority(null) → null; extractPriority([]) → null; extractPriority([{name:'priority:p0'}]) → null.
    - extractPriority(['priority:medium']) → 'medium' (string form defensivo).
    - **D-18 leak guard (W9):** un test explícito assert `Object.keys(result).sort()` deepEqual al array canonical `['id','ref','title','description','labels','projectId','projectName','groups','url','priority','state'].sort()` — bloquea cualquier fuga de campos GitHub-only (`pull_request`, `assignees`, `milestone`, `comments_count`, `state_reason`, `user`, `created_at`, `updated_at`, `locked`, `comments`, `id` numérico).
  </behavior>
  <action>
    Crear `test/providers/github/normalize.test.js` siguiendo PATTERNS.md §Pattern A-C (analog `test/normalize.test.js` con path-fix `../../../src/...`).

    Imports:
    - `describe`, `it` desde `node:test`
    - `assert` desde `node:assert/strict`
    - `{ normalizeIssue, extractPriority }` desde `../../../src/providers/github/normalize.js`
    - Las 6 fixtures (issue + 5 nuevas) con `import ... with { type: 'json' }` (ESM JSON imports)

    `const defaultContext = { projectId: 'octocat/hello-world' };`

    Estructurar dos bloques `describe`:
    1. `describe('normalizeIssue', () => { ... })` — cubre todos los asserts per-field por fixture (D-07..D-18) y edge cases (body null/undefined, labels string/empty, milestone presence con groups=[], state closed).
    2. `describe('extractPriority', () => { ... })` — cubre helper independiente (null/[]/whitelist hit/whitelist miss/case-insensitive/string form).

    Mensajes assert descriptivos: `assert.equal(result.id, 'I_kwTEST001', 'D-07: id must be node_id')`. Facilita debug si el normalizer rompe en Wave 2/3.

    Este task escribe los tests ANTES del normalizer (RED — espera fallar con "Cannot find module './normalize.js'"). El siguiente task implementa hasta GREEN.
  </action>
  <verify>
    <!-- B2 fix: guard fails if SUT already exists OR if RED string absent. Both short-circuit propagate non-zero exit. -->
    <automated>test ! -f src/providers/github/normalize.js && node --test test/providers/github/normalize.test.js 2>&1 | grep -qE "Cannot find module|ERR_MODULE_NOT_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `test/providers/github/normalize.test.js` existe.
    - `grep -c "it(" test/providers/github/normalize.test.js` >= 12 (al menos 12 tests cubriendo D-07..D-18 + extractPriority).
    - `grep -c "import.*normalizeIssue" test/providers/github/normalize.test.js` == 1.
    - `grep -c "import.*extractPriority" test/providers/github/normalize.test.js` == 1.
    - `grep -E "issue-with-priority|issue-with-kodo|issue-closed|issue-no-body|issue-no-labels" test/providers/github/normalize.test.js | wc -l` >= 5 (las 5 fixtures importadas).
    - `grep -cE "CANONICAL_KEYS|Object\.keys\(.*\)\.sort\(\)" test/providers/github/normalize.test.js` >= 1 (W9 leak guard test presente con array canonical de 11 campos).
    - Ejecutar `node --test test/providers/github/normalize.test.js` falla porque `src/providers/github/normalize.js` aún no existe (estado RED esperado).
    - Sin `// @ts-check` comment NO se permite — incluir en línea 1 (CONVENTIONS.md repo).
  </acceptance_criteria>
  <done>Test file existe con ≥12 tests, importa las 6 fixtures + ambos exports del normalizer, ejecuta y falla con "module not found" (estado RED esperado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-01-03: Implementar normalize.js (GREEN) — normalizeIssue + extractPriority</name>
  <files>src/providers/github/normalize.js</files>
  <read_first>
    - src/providers/plane/normalize.js (template estructural — D-06 mirror función pura)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"2. src/providers/github/normalize.js" Patterns A-F (líneas 385-535)
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-06..D-18 (decisiones field-por-field)
    - src/interface.js (TaskItem typedef + VALID_PRIORITIES — read-only)
    - test/providers/github/normalize.test.js (creado en task previo — los tests son el spec)
  </read_first>
  <behavior>
    Exports:
    - `normalizeIssue(issue, context)`: función pura GitHub Issue → TaskItem canonical. Sin API calls, sin side effects.
    - `extractPriority(labels)`: helper exportado para tests directos (D-35 cobertura).

    `normalizeIssue` retorna objeto con los 11 campos según D-07..D-18:
      ```
      id: issue.node_id                              // D-07
      ref: `${context.projectId}#${issue.number}`    // D-08
      title: issue.title                             // D-09
      description: issue.body || ''                   // D-10 Markdown crudo, body null/undefined → ''
      labels: array de strings (de issue.labels)     // D-11 defensivo
      projectId: context.projectId                   // D-12
      projectName: context.projectId                  // D-13 mismo slug
      groups: []                                     // D-14 hardcoded
      url: issue.html_url                            // D-15
      priority: extractPriority(issue.labels)        // D-17 derivado de labels
      state: issue.state                             // D-16 'open'|'closed' literal
      ```

    `extractPriority(labels)`:
    - `null`/no-array → `null`
    - Itera labels (tolera objetos `{name}` o strings)
    - Busca prefix `priority:` (case-insensitive vía toLowerCase)
    - Whitelist: solo `urgent`, `high`, `medium`, `low` (D-17 sin `none`, sin aliases p0/critical/blocker)
    - Primer match wins, devuelve el value lowercased
    - Sin match → `null`

    NO importar `parseKodoLabels` (D-32 invariante: dispatcher lo invoca después con `task.labels.map(name => ({name}))`).
  </behavior>
  <action>
    Crear `src/providers/github/normalize.js` siguiendo PATTERNS.md §Patterns A-F líneas 385-535.

    Estructura:
    1. Línea 1: `// @ts-check`
    2. Línea 2: `import { VALID_PRIORITIES } from '../../interface.js';` (Pattern A — DROP parseKodoLabels)
    3. JSDoc typedef `NormalizeContext` simplificado: `{ projectId: string, baseUrl?: string }` (Pattern B / D-06)
    4. Helper exportado `extractPriority(labels)` siguiendo Pattern E literal — tolera string|object|null, whitelist subset de VALID_PRIORITIES sin 'none'
    5. Export `normalizeIssue(issue, context)` siguiendo Pattern F — retorna objeto con los 11 campos D-07..D-18

    Detalles críticos:
    - DROP `stripHtml` (Pattern C — no se importa, no se define)
    - DROP `resolveWorkItemLabels` (Pattern D — no existe en GitHub normalize)
    - Labels extraction inline defensivo: `Array.isArray(issue.labels) ? issue.labels.map(l => typeof l === 'string' ? l : l?.name).filter(Boolean) : []`
    - `extractPriority` whitelist check con string literals (`value === 'urgent' || value === 'high' || ...`) — NO usar `VALID_PRIORITIES.includes(value)` porque incluye `'none'` que no aplica a GitHub priority labels.
    - JSDoc completo en exports públicos (CONVENTIONS.md repo).

    Header doc top-of-file: 5-10 líneas describiendo D-06 (función pura), D-14 (groups=[]), referencia a 24-CONTEXT.md.
  </action>
  <verify>
    <automated>node --test test/providers/github/normalize.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/providers/github/normalize.js` existe.
    - `grep -c "^export function normalizeIssue" src/providers/github/normalize.js` == 1.
    - `grep -c "^export function extractPriority" src/providers/github/normalize.js` == 1.
    - `grep -c "stripHtml\|resolveWorkItemLabels\|parseKodoLabels\|parseTriggerEvent" src/providers/github/normalize.js` == 0 (DROPs intencionales).
    - `grep -c "// @ts-check" src/providers/github/normalize.js` >= 1.
    - `grep -c "import.*VALID_PRIORITIES.*interface" src/providers/github/normalize.js` == 1.
    - `node --test test/providers/github/normalize.test.js` exit 0; todos los tests GREEN.
    - Conteo de branches del normalizer cubiertas en tests >= 90% (verificable visualmente — branches: body null/string, labels array/empty/string-form, state open/closed, priority hit/miss/invalid/case-insensitive, milestone presence).
    - Output del runner Node muestra ≥12 tests pasados (alineado con conteo del task previo).
  </acceptance_criteria>
  <done>normalize.js implementa exports, todos los tests del task 24-01-02 verde, zero side effects, zero API calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API payload → normalizer | Payload no-confiable (fixture en este plan, API response en runtime) pasa por transformación pura. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-N-01 | T (Tampering) | normalizeIssue | accept | Función pura sobre payload ya recibido (validación shape es responsabilidad del cliente Phase 23). Tampering del payload no escala — el TaskItem resultante es consumido solo por flujos internos kodo. |
| T-24-N-02 | I (Information Disclosure) | normalizeIssue | mitigate | D-18 lock-in: zero fugas de campos GitHub-only (`assignees`, `user`, `pull_request`, etc.) hacia TaskItem. Asserts en tests confirman shape exacto. |
| T-24-N-03 | D (Denial of Service) | extractPriority | accept | Loop sobre labels[] de Issue (típicamente <10 labels, GitHub no enforza límite duro pero usage real <20). Sin regex catastrophic backtracking (D-17 usa string.startsWith + comparación literal). |

Phase 24 normalizer no introduce nuevos vectores: V5 Input Validation aplica al `parseRef` del provider (Plan 24-02), no al normalizer (consume payload ya parseado por el cliente Phase 23 — auth-hardened).
</threat_model>

<verification>
- `node --test test/providers/github/normalize.test.js` → exit 0, ≥12 tests green
- `node --test test/normalize.test.js` (Plane) sigue verde — invariante zero cambios en Plane normalizer
- `npm test` global sigue verde (baseline v0.6 + nuevos tests Phase 24-01)
- D-01 doc-correction safety net: `grep -E "listTasks|listLabels|listStates|transitionTask" .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns 0 matches en líneas funcionales (matches en líneas explicativas históricas son aceptables — la corrección 2026-05-14 ya quedó documentada como nota)
- Verificar ausencia de `stripHtml` y `parseKodoLabels` imports en normalize.js (DROPs deliberados)
</verification>

<success_criteria>
- normalize.js existe con 2 exports puros (~80-100 LOC)
- 5 fixtures incrementales válidas, fork mínimo de issue.json
- normalize.test.js con ≥12 tests cubriendo D-07..D-18 + extractPriority edge cases
- Cobertura ≥90% branches del normalizer (D-35)
- Zero modificaciones a `src/labels.js`, `src/providers/plane/*`, `src/interface.js`
- Suite global `npm test` verde sin regresiones
</success_criteria>

<output>
After completion, create `.planning/phases/24-githubprovider-normalizer-registry/24-01-SUMMARY.md` documentando:
- Fixtures creadas (lista + UN-line descriptor de cada mutación)
- Exports del normalizer + LOC count
- Test count + branch coverage estimada
- Cualquier divergencia respecto a PATTERNS.md justificada
</output>
