---
phase: 59-liveness-de-sesiones-adoptadas
plan: 01
retroactive: true
gap_closure: true
status: complete
requirements: [LIVE-04]
subsystem: session
created: 2026-06-19
---

# Phase 59 Plan 01: Liveness de sesiones adoptadas (RETROACTIVO)

> **Plan retroactivo.** El código se implementó y mergeó a `main` vía gap-fix ANTES de escribir este plan. Documenta lo que se hizo, con tasks marcadas [x] y citando los commits reales, para cerrar la trazabilidad GSD. La verificación contra los success criteria está en `59-VERIFICATION.md`.

## Objetivo

`kodo adopt` renombra el workspace cmux a `"<task_ref>: <título>"` justo tras crear la tarea, de modo que el check EXISTENTE `reconcile.liveForSession`/`titleIdentifiesSession` reconoce la sesión adoptada como viva en vez de `dead`/`zombie`. Sin tocar `reconcile.js`; fail-open absoluto; lo cmux-específico solo via `src/host/`.

## Tasks

- [x] **T-59-01-1 — Host layer: exponer `_legacy.rename`** (commit `2544886`)
  - `src/host/cmux.js`: `_legacy.rename(opts)` passthrough a `cmux/client.js:rename` (`workspace rename <ws> --title <t>`), espejo de `setColor`/`send`. `opts = { workspace, title }`.
  - `src/host/interface.js`: `createNullHost` recibe `_legacy.rename` no-op (`async () => {}`) → host non-cmux/null degrada limpio.
  - `test/host/contract.test.js`: cmux host expone `_legacy.rename`; NullHost no-op; aserción de fuente del argv de `client.js`.

- [x] **T-59-01-2 — CLI: rename para liveness en `runAdoptCli`** (commit `2b56f6b`)
  - `src/cli/adopt.js`: PASO 5 — si `result.ok === true` y `result.task.ref` es string no vacío, construye `${result.task.ref}: ${result.task.title}` y llama `renameWorkspaceFn` (DI; default lazy-importa `getHost('cmux')._legacy.rename`). try/catch que se traga el error y NUNCA cambia `exitCodeFor(result)`. `adoptSession` intacto/host-agnóstico; `reconcile.js` intacto.
  - SKIP en non-ok (ALREADY_ADOPTED/INVALID_INPUT/CREATE_FAILED) y cuando `task.ref` falta/vacío.
  - `test/adopt-cli.test.js` (Phase 59 describe, 4 tests): llamada con título ref-word-bounded + exit 0; fail-open (rename lanza → exit 0); NO llamado en non-ok; SKIPPED sin ref.

- [x] **T-59-01-3 — Corrección verbo canónico cmux** (commit `dfd9e71`)
  - `src/cmux/client.js:rename` usa `cmux workspace rename <ws> --title <t>` (la acción `set-title` no existe). Verificado en vivo contra cmux 0.64.16.

## Verificación

Ver `59-VERIFICATION.md`. Suite: `node --test test/adopt-cli.test.js test/host/contract.test.js` → 61 pass / 0 fail (2026-06-19). Walkers `cmux-isolation` + `format-isolation` verdes.

## Commits

- `2544886` feat(59-01): expose cmux host _legacy.rename + NullHost no-op
- `2b56f6b` feat(59-01): kodo adopt renames cmux workspace to carry task_ref (liveness fix, fail-open)
- `dfd9e71` fix(59-01): cmux workspace rename uses canonical verb (set-title action does not exist)
- `d049efc` chore: merge gap-fix worktree (59-01)
</content>
