---
phase: 29-gsd-provider-reporting-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/labels.js
  - src/triggers/dispatcher.js
  - test/labels.test.js
  - test/dispatcher.test.js
  - test/labels-hygiene.test.js
autonomous: true
requirements:
  - REPORT-01
  - REPORT-05
user_setup: []

must_haves:
  truths:
    - "Operador crea/etiqueta una tarea con label `kodo:gsd-child` y al disparar webhook/polling/CLI manual (incluso con `--force`), kodo log emite el literal `[kodo:dispatch] Ignored — kodo:gsd-child filtered (anti-recursion)` SIN llegar a parseKodoLabels / lock acquire / resolver / launch."
    - "Cualquier consumer importa `KODO_LABEL_GSD_CHILD` desde `src/labels.js` (NUNCA inline string `'kodo:gsd-child'`) y usa `isGsdChild(labels)` helper."
    - "Source-hygiene test grep en `src/` retorna 0 matches inline para `'kodo:gsd-child'` fuera de `src/labels.js`."
  artifacts:
    - path: "src/labels.js"
      provides: "`export const KODO_LABEL_GSD_CHILD = 'kodo:gsd-child'` + `export function isGsdChild(labels)` defensive helper"
      contains: "KODO_LABEL_GSD_CHILD"
    - path: "src/triggers/dispatcher.js"
      provides: "Anti-recursion guard antes de `if (!opts.force)` que retorna `{action: 'ignored', code: 'gsd_child'}`"
      contains: "isGsdChild(task.labels)"
    - path: "test/labels.test.js"
      provides: "Describe `REPORT-01 — isGsdChild + KODO_LABEL_GSD_CHILD` con 9 tests"
      contains: "REPORT-01"
    - path: "test/dispatcher.test.js"
      provides: "Describe `REPORT-01 — kodo:gsd-child anti-recursion filter` (6 tests) + describe `REPORT-01 — dispatcher.js source hygiene` (3 tests)"
      contains: "REPORT-01"
    - path: "test/labels-hygiene.test.js"
      provides: "Walker source-hygiene comment-aware sobre `src/` excluyendo `src/labels.js`"
      contains: "REPORT-05"
  key_links:
    - from: "src/triggers/dispatcher.js"
      to: "src/labels.js#isGsdChild"
      via: "import statement"
      pattern: "import \\{ parseKodoLabels, getGsdMode, isGsdChild \\} from '\\.\\./labels\\.js'"
    - from: "src/triggers/dispatcher.js (guard)"
      to: "before `if (!opts.force)` branch"
      via: "structural ordering"
      pattern: "isGsdChild\\(task\\.labels\\)[\\s\\S]*?if\\s*\\(!opts\\.force\\)"
    - from: "test/dispatcher.test.js"
      to: "spies sobre acquireGsdLockFn/resolvePhaseFn/launchWorkItemFn/removeSessionFn"
      via: "DI factory makeDeps"
      pattern: "_inspect\\(\\)"
---

<objective>
Fundación anti-recursión: introducir el constant `KODO_LABEL_GSD_CHILD` + helper `isGsdChild(labels)` en `src/labels.js`, insertar el guard temprano en `src/triggers/dispatcher.js` (fuera del branch `!opts.force` — funciona incluso bajo `--force=true`) y blindar la fuente única con un walker source-hygiene multi-archivo nuevo.

Purpose: cerrar el SC#1 + SC#3 del ROADMAP Phase 29: las sub-issues creadas por el agente con label `kodo:gsd-child` NUNCA pueden disparar otra sesión de Claude, y ningún consumer puede comparar el literal inline fuera de `src/labels.js`. Sin este plan el resto de la phase es inseguro: 29-04 contiene prosa que instruye al agente a crear sub-issues con esa label.

Output: 2 archivos `src/` modificados (cherry-pick `5a41d8f` + `cbd8f9c`) + 2 archivos `test/` modificados via append (cherry-pick de tests heredados, 15 tests) + 1 archivo `test/` nuevo (`test/labels-hygiene.test.js`, net-new, 2-3 tests source-hygiene). Suite global esperada post-plan: 806 baseline + 15 heredados + 2-3 hygiene = **≥823 pass**, 0 nuevos skips, 1 skip pre-existente preservado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PENDING-INTEGRATIONS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md
@.planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md
@.planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-VALIDATION.md

<interfaces>
<!-- Contracts that downstream plans 29-03 / 29-04 / consumers will rely on -->

Exports añadidos a `src/labels.js`:
- `export const KODO_LABEL_GSD_CHILD: string` — valor literal `'kodo:gsd-child'`. Phase 14 D-09 standalone constant (no namespace wrapping).
- `export function isGsdChild(labels: Array<any>): boolean` — defensivo (tolera `string[]`, `Array<{name}>`, `null`, `undefined`, primitives). Case-insensitive. Exact-match.

Discriminated union extendido en `dispatchTrigger` (`src/triggers/dispatcher.js`):
- Return shape adicional: `{action: 'ignored', code: 'gsd_child'}` — reutiliza `action: 'ignored'` existente con field `code` nuevo. NO se introduce nuevo `action`.

Log line literal grep-able (CONTEXT D-07, byte-exact con em-dash U+2014):
- `[kodo:dispatch] Ignored — kodo:gsd-child filtered (anti-recursion)`

Patrón walker source-hygiene reutilizable:
- `test/labels-hygiene.test.js` espeja exactamente la forma de `test/dispatcher-isolation.test.js` (`stripComments` 3-capas + `readdirSync` recursivo + `assert.deepEqual(violations, [])`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cherry-pick `5a41d8f` (labels.js export + 9 tests en test/labels.test.js)</name>
  <files>src/labels.js, test/labels.test.js</files>
  <read_first>
    - .planning/PENDING-INTEGRATIONS.md (orden cronológico exacto de los 9 SHAs)
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Drift Inventory" §labels (drift ZERO — cherry-pick literal aplica)
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 1: src/labels.js" + §"Touch File 6: test/labels.test.js" (excerpts completos del export + 9 tests REPORT-01)
    - src/labels.js (estado actual main: `parseKodoLabels` lines 12-38 + `getGsdMode` lines 53-58 — patrón canónico para append)
    - test/labels.test.js (estado actual main, 169 líneas, 4 describes existentes — patrón QUICK-08/GH-05 prefix `REQ-ID:`)
    - git show 5a41d8f -- src/labels.js test/labels.test.js (excerpt literal del cherry-pick source)
  </read_first>
  <action>
Aplicar `git cherry-pick 5a41d8f` directamente sobre main HEAD (`d9d5964`). Es el primer SHA en orden cronológico (PENDING-INTEGRATIONS §"Cómo integrar"). Drift severity **Zero** según 29-RESEARCH.md §"Drift Inventory" — `src/labels.js` es byte-idéntico entre `ad2cd88` y main HEAD `fc5ceb1`. Debería aplicar sin conflict.

Post cherry-pick, verificar manualmente que el resultado contiene:
1. En `src/labels.js`: `export const KODO_LABEL_GSD_CHILD = 'kodo:gsd-child'` + JSDoc + `export function isGsdChild(labels)` con la forma defensive descrita en PATTERNS §"Touch File 1 → What to Add" (per D-06/D-07/D-08).
2. En `test/labels.test.js`: import line 3 actualizada con `isGsdChild, KODO_LABEL_GSD_CHILD` añadidos al import existente + describe `REPORT-01 — isGsdChild + KODO_LABEL_GSD_CHILD` appended al final con 9 tests (literal const value, empty array, null/undefined/non-array defensive, string form, object form, case-insensitive, child-wins, similar-but-different rejection, mixed-garbage tolerance).
3. Trailer `(cherry picked from commit 5a41d8f...)` preservado en el commit creado por git (default behavior; NO usar `--no-rebase` ni nada que lo elimine).

Si por cualquier razón el cherry-pick falla (improbable per drift inventory), abortar con `git cherry-pick --abort` y aplicar manual reapply per CONTEXT D-24, dejando en el commit message `[manual reapply of 5a41d8f]`.
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -E "(cherry picked from commit 5a41d8f|manual reapply of 5a41d8f)" &amp;&amp; grep -q "export const KODO_LABEL_GSD_CHILD = 'kodo:gsd-child'" src/labels.js &amp;&amp; grep -q "export function isGsdChild" src/labels.js &amp;&amp; npm test -- test/labels.test.js 2>&amp;1 | tail -20 | grep -E "pass [0-9]+"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene el trailer `(cherry picked from commit 5a41d8f...)` o, en su defecto, la nota `[manual reapply of 5a41d8f]`.
    - `grep -c "export const KODO_LABEL_GSD_CHILD = 'kodo:gsd-child'" src/labels.js` retorna 1.
    - `grep -c "export function isGsdChild" src/labels.js` retorna 1.
    - `grep -c "KODO_LABEL_GSD_CHILD" test/labels.test.js` retorna ≥2 (import + assert RC1-style).
    - `npm test -- test/labels.test.js` exit code 0, count de tests pasados aumenta en exactamente +9 vs baseline (4 describes existentes mantienen sus tests intactos).
    - Suite completa `npm test` exit code 0; baseline 806 → ≥815 pass (806 + 9 nuevos).
  </acceptance_criteria>
  <done>Cherry-pick `5a41d8f` aplicado (literal o manual reapply documentado); `KODO_LABEL_GSD_CHILD` + `isGsdChild` exported desde `src/labels.js`; 9 tests REPORT-01 corriendo verdes en `test/labels.test.js`; suite global ≥815 pass; 0 regresiones.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cherry-pick `cbd8f9c` (dispatcher anti-recursion guard + 9 tests en test/dispatcher.test.js)</name>
  <files>src/triggers/dispatcher.js, test/dispatcher.test.js</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Drift Inventory" §dispatcher (drift Minor — import line conflict trivial + guard placement clean) + §"Concrete Code Excerpts" §"Dispatcher guard placement region"
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 2: src/triggers/dispatcher.js" + §"Touch File 7: test/dispatcher.test.js" (10-line guard excerpt + 9 tests structure)
    - src/triggers/dispatcher.js (estado actual main, líneas 55-71 críticas — punto de inserción entre log "Task:" y `if (!opts.force)`)
    - test/dispatcher.test.js (estado actual main, 1038 líneas, 7 describes existentes incluyendo Phase 18 worktree_collision; patrón `makeDeps` factory en lines 758-810)
    - test/dispatcher-isolation.test.js (mirror canónico para los 3 source-hygiene asserts: `stripComments` 3-capas + `readFileSync` + `assert.ok(!stripped.includes(...))`)
    - git show cbd8f9c -- src/triggers/dispatcher.js test/dispatcher.test.js (excerpt literal del cherry-pick)
    - CONTEXT D-06 (guard ANTES de parseKodoLabels/lock/resolver/launch, fuera del `!opts.force` branch), D-07 (log line literal byte-exact con em-dash U+2014), D-08 (`isGsdChild` única fuente de verdad)
  </read_first>
  <action>
Aplicar `git cherry-pick cbd8f9c` sobre el HEAD post-task-1. Drift severity **Minor** per 29-RESEARCH.md: el punto de inserción del guard (entre línea 61 log "Task:" y línea 63 comentario `// 2. Check kodo labels`) está limpio en main; el conflict probable es solo en la línea del import (6 actual) — Git 3-way debería auto-resolver añadiendo `isGsdChild` al import existente `import { parseKodoLabels, getGsdMode } from '../labels.js'`.

Si Git produce conflict marker en el import line (Pitfall 2 documentado en RESEARCH): manual resolve añadiendo `isGsdChild` al import existente (target: `import { parseKodoLabels, getGsdMode, isGsdChild } from '../labels.js';`). Verificar con `node -c src/triggers/dispatcher.js` que no haya errores de sintaxis.

Verificación crítica de Pitfall 5 (RESEARCH §"Pitfall 5"): si `test/dispatcher.test.js` ya tenía `import { readFileSync } from 'node:fs'` antes (puede haber sido añadido por Phase 28 para algún test), evitar import duplicado. Inspeccionar con `head -10 test/dispatcher.test.js` antes y después del cherry-pick.

Post cherry-pick, verificar:
1. En `src/triggers/dispatcher.js`: import line contiene `isGsdChild`; guard insertion entre log "Task:" y `if (!opts.force)` con los 5 comentarios + `if (isGsdChild(task.labels)) { console.log('[kodo:dispatch] Ignored — kodo:gsd-child filtered (anti-recursion)'); return { action: 'ignored', code: 'gsd_child' }; }`. El em-dash debe ser U+2014 byte-exact (CONTEXT D-07).
2. En `test/dispatcher.test.js`: dos describes appended al final — `REPORT-01 — kodo:gsd-child anti-recursion filter` (6 tests: shape `{action:'ignored', code:'gsd_child'}` + spies-all-uncalled sobre 4 callsites + `--force` no bypassea + child-wins + log line capture + control test) + `REPORT-01 — dispatcher.js source hygiene` (3 tests: import contract, anti-inline `task.labels.some(...gsd-child...)`, estructural `filterIdx < forceIdx`).

Si conflict semántico ocurre (improbable per RESEARCH), aplicar manual reapply per CONTEXT D-24-2 y dejar trailer `[cherry-picked from cbd8f9c, manual resolution: <descripción específica>]` en el commit message.
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -E "(cherry picked from commit cbd8f9c|cherry-picked from cbd8f9c)" &amp;&amp; grep -q "isGsdChild" src/triggers/dispatcher.js &amp;&amp; grep -q "kodo:gsd-child filtered (anti-recursion)" src/triggers/dispatcher.js &amp;&amp; grep -q "code: 'gsd_child'" src/triggers/dispatcher.js &amp;&amp; node -c src/triggers/dispatcher.js &amp;&amp; npm test -- test/dispatcher.test.js 2>&amp;1 | tail -20 | grep -E "pass [0-9]+"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene `cherry picked from commit cbd8f9c` o `cherry-picked from cbd8f9c, manual resolution`.
    - `grep -c "isGsdChild" src/triggers/dispatcher.js` retorna ≥2 (import + call).
    - Estructural: posición del guard ANTES del `if (!opts.force)` — `awk '/isGsdChild\(task.labels\)/{found_guard=NR} /if \(!opts.force\)/{found_force=NR} END{exit (found_guard && found_force && found_guard<found_force)?0:1}' src/triggers/dispatcher.js` exit 0.
    - Log line literal byte-exact: `grep -c "\\[kodo:dispatch\\] Ignored — kodo:gsd-child filtered (anti-recursion)" src/triggers/dispatcher.js` retorna 1 (em-dash U+2014 preservado).
    - `node -c src/triggers/dispatcher.js` exit 0 (sintaxis válida).
    - `npm test -- test/dispatcher.test.js` exit 0; +9 tests vs baseline (6 behavior + 3 source-hygiene).
    - Suite completa `npm test` exit 0; ≥824 pass total (815 post-task-1 + 9 nuevos).
  </acceptance_criteria>
  <done>Cherry-pick `cbd8f9c` aplicado; guard `isGsdChild(task.labels)` insertado en dispatcher.js fuera del `!opts.force` branch; log line literal grep-able con em-dash; 9 tests REPORT-01 verdes en `test/dispatcher.test.js` (6 behavior + 3 source-hygiene); suite global ≥824 pass; 0 regresiones.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear `test/labels-hygiene.test.js` (NEW file, walker source-hygiene multi-archivo)</name>
  <files>test/labels-hygiene.test.js</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md §D-17 + §D-18 (test grep en `src/` retorna 0 matches inline `'kodo:gsd-child'` fuera de `src/labels.js`; walker filtra labels.js + tests fixtures)
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 8: test/labels-hygiene.test.js" (excerpt completo del nuevo archivo, 2 tests + helpers)
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Pattern S3" (source-hygiene grep walker comment-aware)
    - test/dispatcher-isolation.test.js (mirror canónico single-file, 70 líneas — `stripComments` 3-capas)
    - test/format-isolation.test.js (mirror canónico multi-archivo, 197 líneas — `listJsFiles` recursivo)
    - src/labels.js (post task 1: file legítimo que el walker debe excluir; sanity assertion verifica que ESTE archivo SÍ contiene el const + helper)
  </read_first>
  <action>
Crear archivo nuevo `test/labels-hygiene.test.js` mirroring exactamente el shape de `test/dispatcher-isolation.test.js` para `stripComments` + el patrón multi-archivo de `test/format-isolation.test.js` para el walker recursivo. Copiar el excerpt literal de PATTERNS §"Touch File 8 → What to Write" (D-17 net-new, NO viene del branch — documentar en commit message `[net-new, NOT cherry-picked — Phase 29 D-17 guard against future inline leaks]`).

Estructura del archivo (descripción para evitar fences inline aquí; el contenido literal está en 29-PATTERNS.md §"Touch File 8 → What to Write" líneas 731-796):
1. Header `// @ts-check` + imports: `node:test`, `node:assert/strict`, `node:fs` (`readFileSync, readdirSync, statSync`), `node:url` (`fileURLToPath`), `node:path` (`dirname, resolve, join, relative`).
2. Constantes derivadas: `__dirname` via `fileURLToPath`, `REPO` resolved 1 nivel arriba, `SRC = join(REPO, 'src')`, `LABELS_FILE = join(SRC, 'labels.js')`.
3. Helper `stripComments(src)`: 3-capas (block `\/\*[\s\S]*?\*\/` + line `//` + JSDoc `*` continuation). Idéntico al de `test/dispatcher-isolation.test.js:30-36`.
4. Helper `listJsFilesExcept(dir, excludePath)`: recursivo, filtra `excludePath`. Mirror `test/format-isolation.test.js:59-71`.
5. Describe `REPORT-05 — labels source hygiene (Phase 29 D-17)` con 2 tests:
   - Test 1: walker `src/` excluyendo `src/labels.js`, recorre todos los `.js`, aplica `stripComments`, verifica `!stripped.includes("'kodo:gsd-child'")` && `!stripped.includes('"kodo:gsd-child"')`; agrega path a `violations[]` si match; `assert.deepEqual(violations, [], 'Inline kodo:gsd-child literal found in: ...')`.
   - Test 2 (sanity): el archivo legítimo `src/labels.js` SÍ contiene el const (`/export\s+const\s+KODO_LABEL_GSD_CHILD\s*=\s*['"]kodo:gsd-child['"]/`) + helper (`/export\s+function\s+isGsdChild\s*\(/`).

NO ejecutar antes el cherry-pick de Plan 02 / 03 / 04 — el walker pasa con 0 violations precisamente porque `src/config.js` y `src/orchestrator/*` aún no introducen consumers que pudieran tener un literal accidental. Si Plan 04 introduce el literal en `src/orchestrator/prompt.md`, el archivo `.md` NO está en el walker (`.endsWith('.js')` filter).

Commit message sugerido: `test(29-01): add labels source-hygiene walker (REPORT-05, net-new)`.
  </action>
  <verify>
    <automated>test -f test/labels-hygiene.test.js &amp;&amp; node -c test/labels-hygiene.test.js &amp;&amp; npm test -- test/labels-hygiene.test.js 2>&amp;1 | tail -10 | grep -E "pass [0-9]+" &amp;&amp; ! grep -rE "'kodo:gsd-child'|\"kodo:gsd-child\"" src/ --include="*.js" | grep -v "src/labels.js" | grep -v "^[[:space:]]*\(\*\|//\|/\*\)"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `test/labels-hygiene.test.js` existe.
    - `npm test -- test/labels-hygiene.test.js` exit 0; ≥2 tests pasando.
    - Walker test 1 reporta `violations === []` (i.e., no hay literal inline fuera de `src/labels.js` post tasks 1-2 — esperado porque `src/triggers/dispatcher.js` usa `isGsdChild(task.labels)`, no el literal).
    - Sanity test 2 confirma que `src/labels.js` DOES contain `KODO_LABEL_GSD_CHILD` const + `isGsdChild` function exports.
    - `grep -rE "'kodo:gsd-child'|\"kodo:gsd-child\"" src/ --include="*.js" | grep -v src/labels.js` retorna vacío (manual smoke).
    - Suite completa `npm test` exit 0; ≥826 pass total (824 post-task-2 + 2 nuevos).
  </acceptance_criteria>
  <done>Archivo `test/labels-hygiene.test.js` creado con walker source-hygiene + sanity test; suite verde con +2 tests; manual smoke grep confirma 0 violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook/polling/CLI → dispatcher | Entrada untrusted: labels arbitrarias controladas por el operador (o por el agente Claude vía MCP create-issue). Las sub-issues creadas por el agente caen en el mismo canal de polling. |
| dispatcher → launchWorkItem / acquireGsdLock / resolvePhase | Frontier interno: una vez cruzado, kodo gasta recursos (lock, worktree, sesión cmux). Mantener fuera de aquí a cualquier task con label `kodo:gsd-child`. |
| `src/labels.js` → consumers | Frontier de fuente de verdad: si un consumer usa literal inline `'kodo:gsd-child'` en lugar del const + helper, queda fuera del walker source-hygiene y puede divergir silenciosamente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Elevation of Privilege (anti-recursion bypass) | `src/triggers/dispatcher.js` guard | mitigate | Insertar guard ANTES del branch `if (!opts.force)` (CONTEXT D-06); test `REPORT-01: filter applies under opts.force: true` (Task 2 test #3) lo verifica; test estructural `filterIdx < forceIdx` (Task 2 test #9) blinda contra refactors futuros que muevan el guard adentro del branch. **HIGH severity si guard se ubica dentro de `!opts.force`** — el agente podría disparar recursión con `--force`. |
| T-29-05 | Tampering (consumer drift) | `src/` con consumers que comparan label inline | mitigate | `test/labels-hygiene.test.js` (Task 3) walker post-stripComments — bloquea cualquier `.js` en `src/` (excepto `src/labels.js`) que contenga `'kodo:gsd-child'` literal. Defensa en profundidad: si Phase 30+ olvida usar `isGsdChild`, CI rojo. **MEDIUM severity.** |
| T-29-PI | Spoofing/Tampering (prompt-injection upstream — alcance limitado) | `task.description_markdown` controlado por el agente que crea sub-issue | accept | Out of scope para este plan (REPORT-04 prosa instruye al agente; el filtro REPORT-01 corta ANTES de leer `description_markdown`). Defensive shape de `isGsdChild` tolera garbage en `labels[]` (test #8 mixed-garbage). |

</threat_model>

<verification>
- Suite global `npm test` exit 0 con ≥826 pass (806 baseline + 9 labels + 9 dispatcher + 2 hygiene), 0 fail, 1 skip pre-existente.
- Cherry-pick traceability: `git log --oneline -3 | grep -E "(cherry picked from commit 5a41d8f|cherry picked from commit cbd8f9c)"` retorna 2 matches.
- Estructural anti-recursion: `awk '/isGsdChild\(task.labels\)/{a=NR} /if \(!opts.force\)/{b=NR} END{exit (a&&b&&a<b)?0:1}' src/triggers/dispatcher.js` exit 0.
- Source hygiene smoke: `grep -rE "'kodo:gsd-child'|\"kodo:gsd-child\"" src/ --include="*.js" | grep -v src/labels.js` retorna vacío.
- Log line byte-exact: `grep -c "kodo:gsd-child filtered (anti-recursion)" src/triggers/dispatcher.js` retorna 1.
</verification>

<success_criteria>
- 2 SHAs aplicados (cherry-pick literal o manual reapply documentado en commit message): `5a41d8f`, `cbd8f9c`.
- 1 archivo nuevo creado: `test/labels-hygiene.test.js`.
- 15 tests heredados + 2-3 tests net-new corriendo verdes → suite global ≥826 pass.
- Guard anti-recursión funcional bajo `--force` (validado por test #3 de `REPORT-01 — kodo:gsd-child anti-recursion filter`).
- `isGsdChild` única fuente de verdad para el check (validado por test #8 source-hygiene + walker multi-archivo del Task 3).
- 0 regresiones, 0 nuevos skips.
</success_criteria>

<output>
After completion, create `.planning/phases/29-gsd-provider-reporting-integration/29-01-SUMMARY.md` documentando:
- SHAs aplicados (2 cherry-picks + 1 net-new file) con commit hashes finales en main.
- Conflicts encontrados (esperado: 0 en `5a41d8f`; trivial import line en `cbd8f9c`).
- Delta de tests: pre-plan vs post-plan (806 → ≥826).
- Cualquier desviación del plan (e.g., manual reapply con justificación per D-25).
- Notas para 29-02: ninguna (plans independientes en código; soft dependency 29-04 → 29-01 vía import `KODO_LABEL_GSD_CHILD` en `test/orchestrator-gsd.test.js`).
</output>
