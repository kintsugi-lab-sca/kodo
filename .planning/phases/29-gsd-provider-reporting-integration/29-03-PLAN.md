---
phase: 29-gsd-provider-reporting-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 29-02
files_modified:
  - src/orchestrator/prompt.md
  - src/orchestrator/launch.js
  - test/launch.test.js
  - test/prompt.test.js
autonomous: false
requirements:
  - REPORT-03
user_setup: []

must_haves:
  truths:
    - "`src/orchestrator/prompt.md` contiene marcadores únicos `<!-- BEGIN reporting -->` y `<!-- END reporting -->` con un heading `## Sub-issue reporting` ENTRE ellos, posicionados DESPUÉS de la sección `## Sesiones GSD`."
    - "`applyReportingGate(prompt, true)` retorna el prompt byte-idéntico al input; `applyReportingGate(prompt, false)` elimina ambos markers + todo el contenido entre ellos sin trailing whitespace fugitivo."
    - "Idempotencia: aplicar `applyReportingGate` dos veces consecutivas con el mismo flag produce bytes idénticos."
    - "`src/orchestrator/launch.js` importa `isReportToProviderEnabled` desde `'../config.js'` (provisto por Plan 29-02) y compone `applyReportingGate(resolvePromptTemplate(...), isReportToProviderEnabled())`."
    - "Placeholder uses canonical phase numbering `29-04` (NOT branch-original `15-02`) — regression-tested in Task 3."
  artifacts:
    - path: "src/orchestrator/prompt.md"
      provides: "Markers BEGIN/END reporting + heading `## Sub-issue reporting` + placeholder (placeholder será reemplazado por prosa ES en Plan 29-04). Placeholder text refleja numeración Phase 29: `_Section body added in plan 29-04._`."
      contains: "BEGIN reporting"
    - path: "src/orchestrator/launch.js"
      provides: "`export function applyReportingGate(prompt, enabled)` pure idempotente + import de `isReportToProviderEnabled` + wire-up wrap del `basePrompt`. Phase 21 syncSkill auto-sync preservado intacto."
      contains: "applyReportingGate"
    - path: "test/launch.test.js"
      provides: "NEW file con 11 tests: 8 LG behavior (applyReportingGate idempotency + strip + identity) + 3 LH source-hygiene"
      contains: "REPORT-03"
    - path: "test/prompt.test.js"
      provides: "Append describe SR1..SR6 (6 tests: markers + topology + heading + gating behavior + sesiones-GSD preservation + PM7)"
      contains: "SR1"
  key_links:
    - from: "src/orchestrator/launch.js (import)"
      to: "src/config.js#isReportToProviderEnabled"
      via: "ESM import"
      pattern: "import \\{ loadConfig, isReportToProviderEnabled \\} from '\\.\\./config\\.js'"
    - from: "src/orchestrator/launch.js#basePrompt"
      to: "applyReportingGate(resolvePromptTemplate(...), isReportToProviderEnabled())"
      via: "wire-up wrap en render path"
      pattern: "applyReportingGate\\([\\s\\S]*?isReportToProviderEnabled\\(\\)"
    - from: "src/orchestrator/prompt.md (markers)"
      to: "después de `## Sesiones GSD`"
      via: "structural topology (D-12)"
      pattern: "Sesiones GSD[\\s\\S]*?BEGIN reporting"
    - from: "src/orchestrator/launch.js (Phase 21 invariant)"
      to: "syncSkill auto-sync block"
      via: "preserved import + call"
      pattern: "await syncSkill\\|syncSkill\\("
---

<objective>
Infraestructura de gating del bloque "Sub-issue reporting" en el prompt del orchestrator. Tres piezas atómicas: (a) markers + heading + placeholder en `prompt.md`, (b) helper `applyReportingGate` + wire-up en `launch.js`, (c) tests `test/launch.test.js` (NEW) + append SR1..SR6 a `test/prompt.test.js`.

Purpose: cumplir REPORT-03. La prosa real entra en Plan 29-04; este plan establece la infraestructura idempotente que la prosa habitará. Sin estos markers + helper, los content asserts RC1..RC15 de 29-04 no pueden extraer `block` via `indexOf BEGIN/END`.

Output: 2 archivos `src/` modificados (prompt.md + launch.js — ambos con manual reapply documentado: prompt.md severa por Phase 999.1 rewrite, launch.js moderada por Phase 21 evolution) + 1 archivo `test/` nuevo (`test/launch.test.js`, 11 tests) + 1 archivo `test/` appended (`test/prompt.test.js` con 6 tests SR1..SR6). Suite global esperada post-plan: ≥818 pass (floor SC#5) — target real D-22 ≥844, sobrecumplido si baseline 29-02 cerró ≥836.

Plan NO autónomo: Task 1 es un read-only decision-recording checkpoint (autonomous: false) que escribe la nota de mapping LG7/LG8 al draft del SUMMARY antes de los cherry-picks subsiguientes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PENDING-INTEGRATIONS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-CONTEXT.md
@.planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md
@.planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md
@.planning/phases/29-gsd-provider-reporting-integration/29-VALIDATION.md
@.planning/phases/29-gsd-provider-reporting-integration/29-01-PLAN.md
@.planning/phases/29-gsd-provider-reporting-integration/29-02-PLAN.md

<interfaces>
<!-- Contracts that 29-04 will consume -->

Markers idempotentes en `src/orchestrator/prompt.md`:
- `<!-- BEGIN reporting -->` y `<!-- END reporting -->` aparecen exactamente 1 vez cada uno (SR1).
- Heading `## Sub-issue reporting` vive ENTRE BEGIN y END (SR3).
- `indexOf('<!-- BEGIN reporting -->') > indexOf('## Sesiones GSD')` (SR2 topología).
- En Plan 29-03, el cuerpo entre markers es un placeholder con numeración canónica (`_Section body added in plan 29-04._` — NO `15-02` del branch). En Plan 29-04 ese placeholder se reemplaza por prosa ES de ~65 líneas.

Export añadido a `src/orchestrator/launch.js`:
- `export function applyReportingGate(prompt: string, enabled: boolean): string` — pure, idempotente.
  - `enabled === true` → return prompt unchanged.
  - `enabled === false` → strip regex `/<!-- BEGIN reporting -->[\s\S]*?<!-- END reporting -->\n?/g`.

Composition contract:
- En `launchOrchestrator`, `applyReportingGate(resolvePromptTemplate(raw, config), isReportToProviderEnabled())` — `resolvePromptTemplate` corre INSIDE para que `{{provider_name}}` se sustituya ANTES del gate (RESEARCH §"{{provider_name}} Substitution" caveat — preserva la prosa provider-agnostic de 29-04).

Phase 21 invariant preservado:
- `await syncSkill(...)` o `syncSkill(...)` sigue presente en `src/orchestrator/launch.js` post wire-up (regression-tested en Task 3 verify).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave 0 — leer branch HEAD para verificar LG7/LG8 dependency on heading (Pitfall 3 mitigation) — autonomous: false (decision-recording)</name>
  <files>.planning/phases/29-gsd-provider-reporting-integration/29-03-SUMMARY-draft.md (write-only output of decision)</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Open Questions (RESOLVED)" Q3 (RESOLVED: LG7/LG8 funcionan desde 29-03 — dependen del heading, no de la prosa) + §"Pitfall 3"
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 10 Sub-touch A → What to Write" (LG7 nota explícita: el heading se inserta en 29-03, no en 29-04 — LG7 funciona desde 29-03)
    - .planning/PENDING-INTEGRATIONS.md (referencia al branch `gsd-provider-reporting`)
  </read_first>
  <action>
Acción de inspección + decision-recording (NO modifica código de producción). Marked `autonomous: false` porque es un read-only checkpoint cuya output es una nota en el draft del SUMMARY (no hay test automatizado para "se leyó el branch y se tomó decisión").

1. Ejecutar `git show gsd-provider-reporting:test/launch.test.js` para obtener el contenido literal de los 11 tests LG1..LG8 + LH1..LH3 que crearía el cherry-pick `38c7a2e`. Mapear cada `it()` a su aserción concreta:

   - LG1..LG6: ¿operan sobre un fixture inline o sobre `readFileSync(src/orchestrator/prompt.md)`?
   - LG7: ¿asserta presencia de la cadena literal `Sub-issue reporting` heading (provisto por 29-03 manual reapply) o asserta presencia de strings específicos de la prosa ES (e.g., `plan-by-plan`, `parent_id`) que solo entran en 29-04?
   - LG8: ¿byte-identidad con `flag=true`?

2. Per PATTERNS §"Touch File 10 Sub-touch A → What to Write" line 966-967: "LG7 funciona desde 29-03" porque el heading `## Sub-issue reporting` se inserta en el cherry-pick `7c28c06` (markers + placeholder + heading), no en `d030547` (prosa). Esta task confirma esa lectura.

3. **Decision-recording (mandatory):** Append una línea explícita al draft del SUMMARY:

   `echo "## Wave 0 LG7/LG8 mapping decision (Task 1, 2026-05-20)" >> .planning/phases/29-gsd-provider-reporting-integration/29-03-SUMMARY-draft.md`
   `echo "Decision: LG7/LG8 funcionan desde 29-03 — dependen del heading, no de la prosa específica. Confirmed via `git show gsd-provider-reporting:test/launch.test.js`." >> .planning/phases/29-gsd-provider-reporting-integration/29-03-SUMMARY-draft.md`

   (Si la inspección revela dependencia de prosa, ajustar el mensaje y postergar el subset afectado a Plan 29-04 con justificación.)

4. Si la inspección revela que algún test LG depende de strings exclusivos de la prosa ES (improbable per RESEARCH §Pitfall 3 + PATTERNS comment): documentar en draft de 29-03-SUMMARY y postergar ese subset de tests a Plan 29-04. Esperado: ningún cambio en el plan; confirmar y avanzar.
  </action>
  <verify>
    <automated>git show gsd-provider-reporting:test/launch.test.js 2>/dev/null | grep -Ec "LG[1-8]|LH[1-3]" | awk '$1 >= 8 { exit 0 } { exit 1 }' && test -f .planning/phases/29-gsd-provider-reporting-integration/29-03-SUMMARY-draft.md && grep -q "LG7/LG8 mapping decision" .planning/phases/29-gsd-provider-reporting-integration/29-03-SUMMARY-draft.md</automated>
  </verify>
  <acceptance_criteria>
    - `git show gsd-provider-reporting:test/launch.test.js` retorna contenido no vacío (verifica que branch + path siguen accesibles).
    - Conteo de `it()` blocks con prefix `LG` o `LH` ≥ 8 (sanity check del shape esperado).
    - Decisión registrada en `29-03-SUMMARY-draft.md`: "LG7/LG8 verificados — funcionan desde 29-03 (dependen del heading, no de la prosa)" O "LG{N,M} re-asignados a 29-04 por dependencia de prosa específica" (con justificación).
  </acceptance_criteria>
  <done>Branch inspeccionado; mapping LG1..LG8 documentado en el draft del SUMMARY; decisión de re-asignación tomada (esperado: NO re-asignar). Sin cambios al código aún.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Manual reapply de `7c28c06` (markers + heading + placeholder en prompt.md, renumbered `15-02` → `29-04`)</name>
  <files>src/orchestrator/prompt.md</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Drift Inventory" §prompt.md (severity SEVERE — Phase 999.1 reescribió prompt.md, cherry-pick literal fallará) + §"Pitfall 1" + §"Prompt.md Section Ordering" + §"Open Questions (RESOLVED)" Q1
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 4: src/orchestrator/prompt.md" (insertion position TRAS línea 38 `## Sesiones GSD`; bloque markers + heading + placeholder excerpt)
    - src/orchestrator/prompt.md (estado actual main, 39 líneas, 4 secciones — última es `## Sesiones GSD` líneas 30-38)
    - git show 7c28c06 -- src/orchestrator/prompt.md (excerpt: los markers + heading + placeholder que branch insertó en la versión vieja del prompt)
    - CONTEXT D-12 (markers literales + idempotencia + provider-agnostic via {{provider_name}})
  </read_first>
  <action>
NO usar `git cherry-pick 7c28c06` — RESEARCH §"Pitfall 1" confirma que fallará con "patch does not apply" porque el patch espera el contexto del prompt.md viejo (80+ líneas, Phase 5 era) que ya no existe (Phase 999.1 lo reescribió a 39 líneas el 2026-05-11).

Manual reapply (D-24-2 conflict semántico):

1. Inspeccionar `git show gsd-provider-reporting:src/orchestrator/prompt.md` para extraer el bloque entre `<!-- BEGIN reporting -->` y `<!-- END reporting -->` con el placeholder (NO la prosa final — esa viene de `d030547` y se aplica en Plan 29-04). En la versión del branch el cherry-pick `7c28c06` específicamente inserta solo el placeholder `_Section body added in plan 15-02._`.

2. **PLACEHOLDER RENAME (critical):** sustituir `15-02` por `29-04` en el placeholder text para reflejar la numeración Phase 29 (CONTEXT D-20: branch usaba Phase 14-15, regenerar con numeración v0.8). El placeholder final debe ser literal `_Section body added in plan 29-04._` — no `15-02`. Esta diferencia es invariante regresional: Task 3 de este plan (verify) Y 29-04 Task 1 (acceptance) chequean que `grep -c "15-02" src/orchestrator/prompt.md` retorna 0. Documentar el rename explícitamente en el commit message para audit trail.

3. Append el bloque al final del archivo `src/orchestrator/prompt.md` (TRAS la línea 38 actual, último item de `## Sesiones GSD`). El bloque a insertar contiene:
   - Línea vacía separadora
   - `<!-- BEGIN reporting -->`
   - `## Sub-issue reporting`
   - Línea vacía
   - `_Section body added in plan 29-04._`
   - `<!-- END reporting -->`
   - Línea final (newline trailing)

4. Verificar invariantes (PATTERNS §"Touch File 4" → Invariants):
   - Markers únicos (exactamente 1 ocurrencia cada uno).
   - Heading `## Sub-issue reporting` entre BEGIN y END (SR3).
   - `indexOf('<!-- BEGIN reporting -->') > indexOf('## Sesiones GSD')` (SR2 topology).
   - NO menciona Plane/GitHub/ClickUp inline (provider-agnostic — el placeholder es agnóstico por construcción).
   - Placeholder renumbered: cero ocurrencias de `15-02`.

5. Crear commit con message: `feat(29-03): markers + heading reporting block in prompt.md [manual reapply of 7c28c06, branch base prompt.md no longer exists in main — Phase 999.1 rewrite; placeholder renumbered 15-02 → 29-04 per CONTEXT D-20]`.

NO insertar la prosa de 65 líneas en este task — eso es responsabilidad de Plan 29-04 (manual reapply de `d030547`). El placeholder es importante porque permite que LG7 test (heading presente) pase desde 29-03 mientras que RA1..RA6 tests (anti-leak content) operan correctamente cuando la prosa entre.
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -q "manual reapply of 7c28c06" && [ "$(grep -c 'BEGIN reporting' src/orchestrator/prompt.md)" = "1" ] && [ "$(grep -c 'END reporting' src/orchestrator/prompt.md)" = "1" ] && grep -q "## Sub-issue reporting" src/orchestrator/prompt.md && awk '/## Sesiones GSD/{a=NR} /BEGIN reporting/{b=NR} END{exit (a&&b&&a<b)?0:1}' src/orchestrator/prompt.md && [ "$(grep -c '15-02' src/orchestrator/prompt.md)" = "0" ] && grep -q "Section body added in plan 29-04" src/orchestrator/prompt.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene `manual reapply of 7c28c06`.
    - `grep -c 'BEGIN reporting' src/orchestrator/prompt.md` retorna 1.
    - `grep -c 'END reporting' src/orchestrator/prompt.md` retorna 1.
    - `grep -c '## Sub-issue reporting' src/orchestrator/prompt.md` retorna 1.
    - Topology check: `awk '/## Sesiones GSD/{a=NR} /BEGIN reporting/{b=NR} END{exit (a&&b&&a<b)?0:1}' src/orchestrator/prompt.md` exit 0 (Sesiones GSD aparece ANTES de BEGIN reporting).
    - Placeholder rename verified: `grep -c "Section body added in plan 29-04" src/orchestrator/prompt.md` retorna 1 (numeración corregida vs `15-02` original).
    - Regression-test rename: `grep -c '15-02' src/orchestrator/prompt.md` retorna 0 (branch-original numeración NO permanece).
    - Suite ≥818 pass (floor SC#5 absolute floor) sin nuevos tests aún — los tests entran en Task 4.
  </acceptance_criteria>
  <done>Markers + heading + placeholder insertados en `src/orchestrator/prompt.md` post `## Sesiones GSD`; placeholder renumbered `15-02` → `29-04`; commit message documenta manual reapply de `7c28c06` + rename rationale; suite preservada en floor ≥818 pass sin regresiones.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cherry-pick `5feb578` (applyReportingGate helper + wire-up en launch.js)</name>
  <files>src/orchestrator/launch.js</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Drift Inventory" §launch.js (severity Moderate — 3 hunks pacíficos, Phase 21 auto-sync NO toca regiones de inserción) + §"Concrete Code Excerpts" §"Launch.js applyReportingGate wire-up region" + §"Pitfall 2" + §"{{provider_name}} Substitution"
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 5: src/orchestrator/launch.js" (excerpts literales de los 3 hunks: import line, helper definition, wire-up wrap)
    - src/orchestrator/launch.js (estado actual main: import line 7, `resolvePromptTemplate` lines 28-36, Phase 21 auto-sync lines 48-87, Phase 18 D-06 comment lines 130-148, wire-up region lines 113-114)
    - git show 5feb578 -- src/orchestrator/launch.js (excerpt literal del cherry-pick)
    - CONTEXT D-13 (wire-up ANTES del template render — patrón Phase 10 verification block) + D-14 (idempotencia SR1..SR6 byte-equal)
  </read_first>
  <action>
Aplicar `git cherry-pick 5feb578` sobre HEAD post-Task-2. Drift severity Moderate per RESEARCH: 3 hunks todos pacíficos. Esperado: Git resuelve automáticamente las 3 inserciones. Si Git produce conflict marker en el import line (Pitfall 2 documentado): manual resolve añadiendo `isReportToProviderEnabled` al import existente `import { loadConfig } from '../config.js'` → `import { loadConfig, isReportToProviderEnabled } from '../config.js'`.

Los 3 hunks que el cherry-pick debe aplicar (RESEARCH §"Concrete Code Excerpts" + PATTERNS §"Touch File 5 → What to Add"):

1. **Import line (línea 7 actual)**: añadir `isReportToProviderEnabled` al import existente desde `'../config.js'`. Phase 29-02 ya exportó este símbolo desde `src/config.js`.

2. **Helper `applyReportingGate` definition**: insertar entre el cierre de `resolvePromptTemplate` (línea ~36) y el JSDoc de `launchOrchestrator` (línea ~38). Slot pacífico — Phase 21 auto-sync vive lines 48-87 (después de helper). Helper signature: `export function applyReportingGate(prompt, enabled)`. Body: `if (enabled) return prompt; return prompt.replace(/<!-- BEGIN reporting -->[\s\S]*?<!-- END reporting -->\n?/g, '');`. JSDoc multi-line documentando composition order (resolvePromptTemplate corre PRIMERO; ver RESEARCH §"{{provider_name}} Substitution").

3. **Wire-up wrap (líneas 113-114 actuales)**: cambiar la asignación de `basePrompt` de `const basePrompt = resolvePromptTemplate(rawPrompt, { provider: config.provider || 'plane' });` a `const basePrompt = applyReportingGate(resolvePromptTemplate(rawPrompt, { provider: config.provider || 'plane' }), isReportToProviderEnabled());`.

Validación post-cherry-pick:
- `node -c src/orchestrator/launch.js` exit 0 (sintaxis).
- Sanity import: `node -e "import('./src/orchestrator/launch.js').then(m => console.log(typeof m.applyReportingGate))"` imprime `function`.
- Composition order: `applyReportingGate(resolvePromptTemplate(...))` — resolvePromptTemplate corre INSIDE para que `{{provider_name}}` se sustituya ANTES del gate. Verificar con grep que el orden sea `applyReportingGate(...resolvePromptTemplate...)` y no al revés.
- **Phase 21 invariant preservation:** `syncSkill` debe seguir importado y llamado. Cherry-pick 3-hunk debe respetar el bloque auto-sync líneas 48-87 (slot ortogonal al wire-up).

Si conflict semántico requiere manual resolution (improbable per RESEARCH), aplicar manual reapply per D-24-2 con trailer `[cherry-picked from 5feb578, manual resolution: 3 hunks rebased onto post-Phase-21 launch.js]`.
  </action>
  <verify>
    <automated>git log -1 --format='%B' | grep -Eq "(cherry picked from commit 5feb578|cherry-picked from 5feb578)" && grep -q "isReportToProviderEnabled" src/orchestrator/launch.js && grep -q "export function applyReportingGate" src/orchestrator/launch.js && grep -Eq "applyReportingGate\(.*resolvePromptTemplate" src/orchestrator/launch.js && node -c src/orchestrator/launch.js && [ "$(grep -cE 'await syncSkill|syncSkill\(' src/orchestrator/launch.js)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%B'` contiene `cherry picked from commit 5feb578` o `cherry-picked from 5feb578, manual resolution`.
    - `grep -c "isReportToProviderEnabled" src/orchestrator/launch.js` retorna ≥2 (import + call en wire-up).
    - `grep -c "export function applyReportingGate" src/orchestrator/launch.js` retorna 1.
    - Composition order: `grep -E "applyReportingGate\(.*resolvePromptTemplate" src/orchestrator/launch.js` retorna ≥1 match (resolvePromptTemplate INSIDE applyReportingGate).
    - Helper body contiene regex global flag: `grep -E "BEGIN reporting.*END reporting.*g" src/orchestrator/launch.js` retorna match (regex `/...END reporting -->\n?/g` presente).
    - `node -c src/orchestrator/launch.js` exit 0.
    - `node -e "import('./src/orchestrator/launch.js').then(m => process.exit(typeof m.applyReportingGate === 'function' ? 0 : 1))"` exit 0.
    - **Phase 21 auto-sync preservado (ISSUE-29-008 regression check):** `grep -cE "await syncSkill|syncSkill\(" src/orchestrator/launch.js` ≥1 (Phase 21 syncSkill call survives the cherry-pick).
    - Phase 18 D-06 comment preservado (línea 130-148 area sin pérdida).
    - Suite ≥818 pass (floor SC#5 sin nuevos tests aún — los tests entran en Task 4).
  </acceptance_criteria>
  <done>Helper `applyReportingGate` exportado desde `src/orchestrator/launch.js` con regex strip + identity fast-path; wire-up envuelve `basePrompt` con composition order correcto (resolvePromptTemplate INSIDE); import `isReportToProviderEnabled` desde `../config.js`; Phase 21 `syncSkill` invariant preserved; sintaxis válida; suite preservada en floor ≥818.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Cherry-pick `38c7a2e` (test/launch.test.js NEW) + `4d67312` (test/prompt.test.js append SR1..SR6)</name>
  <files>test/launch.test.js, test/prompt.test.js</files>
  <read_first>
    - .planning/phases/29-gsd-provider-reporting-integration/29-RESEARCH.md §"Test File Mapping" (path crítico: `test/launch.test.js`, NO `test/orchestrator/launch.test.js`) + §"Pitfall 3" (LG7/LG8 verificados en Task 1)
    - .planning/phases/29-gsd-provider-reporting-integration/29-PATTERNS.md §"Touch File 10 Sub-touch A: test/launch.test.js" (LG1..LG8 + LH1..LH3 descripción) + §"Sub-touch B: test/prompt.test.js append" (SR1..SR6 descripción)
    - test/prompt.test.js (estado actual main: 3 describes existentes; append shape preservado)
    - git show 38c7a2e -- test/launch.test.js
    - git show 4d67312 -- test/prompt.test.js
    - CONTEXT D-14 (idempotencia SR1..SR6 byte-equal asserts)
  </read_first>
  <action>
Aplicar 2 cherry-picks consecutivos sobre HEAD post-Task-3:

1. `git cherry-pick 38c7a2e` — crea `test/launch.test.js` (NEW file). Drift severity Clean (new file). Verificar path correcto: debe ser `test/launch.test.js`, NO `test/orchestrator/launch.test.js` (RESEARCH §"Test File Mapping" lo confirma; si Git crea el path en otra ubicación por algún motivo, mover manualmente).

   Contenido esperado: 11 tests across 2-3 describes:
   - LG1..LG8 (8 behavior tests): `enabled=true` preserve, `enabled=false` strip markers+body, preserve outside content, idempotent doble aplicación, no-op cuando no hay markers, pure (same input→same output), real prompt.md con `flag=false` heading ausente, real prompt.md con `flag=true` byte-idéntico.
   - LH1..LH3 (3 source-hygiene tests): `launch.js` imports `isReportToProviderEnabled`, NO accede `.report_to_provider` directo (post-stripComments), compone `applyReportingGate(...isReportToProviderEnabled())` literal.

2. `git cherry-pick 4d67312` — append SR1..SR6 a `test/prompt.test.js`. Drift severity Clean (append puro a archivo de 3 describes).

   Contenido esperado: 1 describe `REPORT-03 — sub-issue reporting section gating` con 6 tests SR1..SR6:
   - SR1: markers únicos (exactamente 1 ocurrencia cada uno).
   - SR2: topology `indexOf(BEGIN) > indexOf('## Sesiones GSD')`.
   - SR3: heading `## Sub-issue reporting` entre BEGIN y END.
   - SR4: gating behavior — `applyReportingGate(raw, true)` preserva heading + BEGIN; `applyReportingGate(raw, false)` strips ambos.
   - SR5: stripped output preserva `## Sesiones GSD` + `kodo gsd verify <session-id>`.
   - SR6: PM7 invariant — no `you must`/`please`/`execute your` en el raw completo.

Si Git produce conflict en `test/prompt.test.js` (improbable per drift Clean), manual append per CONTEXT D-24-1 (trivial whitespace).

Validación post-cherry-picks:
- `test -f test/launch.test.js` (no en `test/orchestrator/launch.test.js`).
- `npm test -- test/launch.test.js` exit 0 con ≥11 tests pasando.
- `npm test -- test/prompt.test.js` exit 0 con ≥6 tests nuevos pasando (existentes intactos).
- LG7 funcional (RESEARCH §Pitfall 3 mitigation): el heading `## Sub-issue reporting` está presente en prompt.md desde Task 2 → LG7 pasa sin necesidad de la prosa de Plan 29-04.
- Placeholder regression (ISSUE-29-005): `grep -c "15-02" src/orchestrator/prompt.md` retorna 0 (rename de Task 2 sigue stable).

Si Task 1 reveló que algún LG depende de prosa específica (esperado: ninguno), aplicar la decisión documentada — postergar ese subset a Plan 29-04 con justificación en SUMMARY.
  </action>
  <verify>
    <automated>git log -2 --format='%B' | grep -Ec "(cherry picked from commit 38c7a2e|cherry picked from commit 4d67312)" | awk '$1 >= 2 { exit 0 } { exit 1 }' && test -f test/launch.test.js && [ ! -f test/orchestrator/launch.test.js ] && npm test -- test/launch.test.js test/prompt.test.js 2>&1 | tail -20 | grep -Eq "pass [0-9]+" && grep -Eq "applyReportingGate" test/launch.test.js && grep -Eq "SR[1-6]" test/prompt.test.js && [ "$(grep -c '15-02' src/orchestrator/prompt.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - 2 commits cherry-picked: `git log --oneline -2` muestra los 2 nuevos commits con trailers `cherry picked from commit 38c7a2e` y `cherry picked from commit 4d67312`.
    - Archivo `test/launch.test.js` existe en path correcto (NO en `test/orchestrator/launch.test.js`).
    - `test/launch.test.js` contiene ≥11 `it(` blocks con prefijos LG (≥8) + LH (≥3).
    - `test/prompt.test.js` contiene ≥6 nuevos `it(` blocks con prefijo SR (verificar con `grep -c "it('SR" test/prompt.test.js` ≥6 o equivalent).
    - `npm test -- test/launch.test.js` exit 0; ≥11 tests pasando.
    - `npm test -- test/prompt.test.js` exit 0; +6 tests vs baseline.
    - Suite completa `npm test` exit 0 con **≥818 pass + 0 fail + 1 skip preservado** (floor SC#5 D-22; el delta cumulativo +17 vs 29-02 baseline es informativo, no la aserción gate).
    - 0 regresiones.
    - LH3 verificará composition literal: `applyReportingGate(...isReportToProviderEnabled())` presente en `src/orchestrator/launch.js` (ya validado en Task 3, re-asegurado por LH3).
    - Placeholder rename regression (ISSUE-29-005): `grep -c "15-02" src/orchestrator/prompt.md` retorna 0 — rename de Task 2 sigue en HEAD sin reversión.
  </acceptance_criteria>
  <done>2 cherry-picks aplicados; `test/launch.test.js` creado en path correcto con 11 tests verdes (LG1..LG8 + LH1..LH3); `test/prompt.test.js` con 6 tests SR1..SR6 verdes appended; suite global ≥818 pass + 0 fail + 1 skip preservado (D-22 floor); placeholder rename `29-04` sigue en HEAD (regression check); 0 regresiones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/orchestrator/prompt.md` (file) → `launch.js` (readFileSync) | Frontier de carga del template: si los markers desaparecen, `applyReportingGate(flag=false)` se convierte en no-op silencioso y la prosa termina visible aunque el operador no opt-in. |
| `applyReportingGate(prompt, enabled)` → cmux session prompt | Frontier final: lo que sale del helper es lo que el agente Claude lee. Doble aplicación accidental (e.g., refactor que envuelve dos veces) NO debe mutar bytes. |
| `isReportToProviderEnabled()` → `applyReportingGate` (boolean argument) | Frontier de gating: si el helper retorna `true` por mistake (Plan 29-02 mitiga), el agente recibe prosa que en 29-04 instruye crear sub-issues — combinado con T-29-01 mitigation OK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering (gate idempotency violation) | `applyReportingGate` body | mitigate | Test LG4 (doble aplicación con `enabled=false` produce bytes idénticos) + LG6 (pure function — same input + flag → same output). Regex tiene `\n?` opcional al final para evitar trailing whitespace fugitivo. **LOW severity** — solo afecta DX, no security. |
| T-29-PM | Information Disclosure (markers leak) | `src/orchestrator/prompt.md` | mitigate | SR1 (markers únicos — exactamente 1 BEGIN + 1 END) + SR3 (heading ENTRE markers) bloquean estados malformados que harían que el strip dejara prosa colgada. **LOW severity.** |
| T-29-CO | Composition order (placeholder substitution post-gate) | `launchOrchestrator` wire-up | mitigate | Wire-up `applyReportingGate(resolvePromptTemplate(...))` con `resolvePromptTemplate` INSIDE. RESEARCH §"{{provider_name}} Substitution" comenta el caveat explícitamente. LH3 source-hygiene test asserta literal `applyReportingGate(...isReportToProviderEnabled())`. **MEDIUM severity** — si la composition se invierte, las `{{provider_name}}` dentro del bloque reporting nunca se sustituyen → prosa de 29-04 sale literal `{{provider_name}}` al agente. |
| T-29-P21 | Tampering (Phase 21 syncSkill regression) | `src/orchestrator/launch.js` wire-up adjacent regions | mitigate | Task 3 verify asserta `grep -cE "await syncSkill\|syncSkill\(" src/orchestrator/launch.js` ≥1 — si el cherry-pick 3-hunk borra accidentalmente el bloque auto-sync, CI rojo inmediato. **MEDIUM severity** — Phase 21 cross-milestone invariant. |

</threat_model>

<verification>
- Suite global `npm test` exit 0 con **≥818 pass + 0 fail + 1 skip pre-existente preservado** (D-22 floor SC#5). Target real ≥844 si baseline 29-02 cerró en ≥836.
- Cherry-pick traceability: `git log --oneline -4 | grep -Ec "(cherry picked from commit (5feb578|38c7a2e|4d67312)|manual reapply of 7c28c06)"` retorna 4 (3 cherry-picks + 1 manual reapply).
- Markers únicos: `grep -c 'BEGIN reporting' src/orchestrator/prompt.md` retorna 1; `grep -c 'END reporting' src/orchestrator/prompt.md` retorna 1.
- Topology: `awk` confirma `## Sesiones GSD` aparece ANTES de `BEGIN reporting`.
- Composition order: `grep -E "applyReportingGate\(.*resolvePromptTemplate" src/orchestrator/launch.js` retorna ≥1.
- Idempotency runtime: `node -e "const { applyReportingGate } = await import('./src/orchestrator/launch.js'); const p = 'pre\n<!-- BEGIN reporting -->\nbody\n<!-- END reporting -->\npost'; const a = applyReportingGate(p, false); const b = applyReportingGate(a, false); process.exit(a === b ? 0 : 1)"` exit 0.
- Phase 21 invariant: `grep -cE "await syncSkill|syncSkill\(" src/orchestrator/launch.js` ≥1.
- Placeholder rename: `grep -c "15-02" src/orchestrator/prompt.md` retorna 0; `grep -c "Section body added in plan 29-04" src/orchestrator/prompt.md` retorna 1.
</verification>

<success_criteria>
- 3 SHAs aplicados (1 manual reapply documentado + 2 cherry-picks literales): `7c28c06` (manual), `5feb578`, `38c7a2e`, `4d67312`.
- 1 archivo `test/` nuevo: `test/launch.test.js` (NO en subdir `test/orchestrator/`).
- Suite ≥818 pass + 0 fail + 1 skip preservado (D-22 floor SC#5 — canonical assertion, evita arithmetic drift entre plans per ISSUE-29-009).
- Markers + heading + placeholder presentes en `src/orchestrator/prompt.md` (placeholder será reemplazado por prosa en Plan 29-04, numeración Phase 29 NO `15-02`).
- `applyReportingGate` pure idempotente operacional + wire-up con composition order correcto.
- Phase 21 `syncSkill` cross-milestone invariant preservado.
- 0 regresiones, 0 nuevos skips.
</success_criteria>

<output>
After completion, create `.planning/phases/29-gsd-provider-reporting-integration/29-03-SUMMARY.md` documentando:
- SHAs aplicados (1 manual reapply + 3 cherry-picks) con commit hashes finales en main.
- Conflictos: 1 severo (prompt.md, manual reapply); 2 moderados (launch.js, 3 hunks rebased); 1 clean (test/prompt.test.js append).
- Verificación Task 1: LG7/LG8 mapping confirmado en 29-03-SUMMARY-draft.md (esperado: funcionan desde 29-03 por heading-only dependency).
- Placeholder rename documentado: `15-02` → `29-04` (rationale CONTEXT D-20).
- Delta de tests informativo (+17 vs 29-02) pero el gate canonical es suite ≥818 pass + 0 fail + 1 skip preservado.
- Phase 21 syncSkill invariant verificado post-wire-up.
- Notas para 29-04:
  - Placeholder `_Section body added in plan 29-04._` listo para ser reemplazado.
  - `applyReportingGate` + composition order verificados; los content asserts RC1..RC15 de 29-04 pueden usar `applyReportingGate(resolvePromptTemplate(raw, {provider:'plane'}), true)` para extraer el `block`.
  - `KODO_LABEL_GSD_CHILD` import en `test/orchestrator-gsd.test.js` de 29-04 es soft dependency a Plan 29-01 (ya cumplida).
</output>
</output>
