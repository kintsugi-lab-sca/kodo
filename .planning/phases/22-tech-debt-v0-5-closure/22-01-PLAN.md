---
phase: 22-tech-debt-v0-5-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/version-smoke.test.js
  - src/cli/format.js
  - test/format.test.js
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03
user_setup: []

must_haves:
  truths:
    - "DEBT-01: 14-SECURITY.md sigue presente con threats_open: 0 y status: verified (satisfied-by-existing)."
    - "DEBT-02: test/version-smoke.test.js declara timeout explícito (10_000 ms) en el spawnSync de bin/kodo --version."
    - "DEBT-03 IN-01: visibleWidth() en src/cli/format.js usa regex CSI defensiva /\\x1b\\[[\\d;]*[A-Za-z]/g (cubre multi-param y 38;5;200)."
    - "DEBT-03 IN-02: test/format.test.js cubre el caso FORCE_COLOR='' con useColor=true (any non-'0' value forces)."
    - "La matriz NO_COLOR > FORCE_COLOR > stream.isTTY sigue verde; ningún test existente se rompe."
  artifacts:
    - path: ".planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md"
      provides: "Phase 14 security audit, threats_open: 0 (satisfied-by-existing — NO se reescribe)."
      contains: "threats_open: 0"
    - path: "test/version-smoke.test.js"
      provides: "Smoke regression con timeout explícito (WR-01 Phase 14 closed)."
      contains: "timeout: 10_000"
    - path: "src/cli/format.js"
      provides: "visibleWidth() defensiva contra CSI multi-param (IN-01 Phase 14 closed)."
      contains: "\\\\x1b\\\\[[\\\\d;]*[A-Za-z]"
    - path: "test/format.test.js"
      provides: "Caso FORCE_COLOR='' explícito (IN-02 Phase 14 closed)."
      contains: "FORCE_COLOR: ''"
  key_links:
    - from: "test/version-smoke.test.js"
      to: "bin/kodo"
      via: "spawnSync(process.execPath, [KODO_BIN, '--version'], { ..., timeout: 10_000 })"
      pattern: "timeout:\\s*10_000"
    - from: "src/cli/format.js#visibleWidth"
      to: "regex CSI general"
      via: "String.replace(CSI_REGEX, '')"
      pattern: "\\\\x1b\\\\[\\[\\\\d;\\]\\*\\[A-Za-z\\]"
    - from: "test/format.test.js"
      to: "_resolveUseColor"
      via: "case 8: TTY=false + FORCE_COLOR='' → true"
      pattern: "FORCE_COLOR:\\s*''"
---

<objective>
Cerrar la deuda Phase 14 (DEBT-01/02/03). Las 3 tareas son fixes quirúrgicos sin behavior change runtime (golden bytes DX-06 preservados):

1. **DEBT-01 satisfied-by-existing** — `14-SECURITY.md` ya existe con `threats_open: 0` + `status: verified` (verificado por planner). NO se regenera. Se documenta en este plan que el artefacto está vigente.
2. **DEBT-02 (WR-01 Phase 14)** — Añadir `timeout: 10_000` al `spawnSync` de `test/version-smoke.test.js` para que CI falle rápido si el bin cuelga.
3. **DEBT-03 IN-01 + IN-02** — Ampliar regex ANSI de `visibleWidth` para cubrir CSI multi-param + añadir caso de test explícito `FORCE_COLOR=''` en `_resolveUseColor`.

Purpose: Cerrar el Resolution Log Phase 14 (REVIEW.md) — los 3 ítems eran warnings/informational sin block del ship, ahora se documentan en código.

Output:
- `test/version-smoke.test.js` con `timeout: 10_000`.
- `src/cli/format.js` con regex CSI defensiva.
- `test/format.test.js` con caso 8 (`FORCE_COLOR=''`).
- Suite global verde, golden bytes intactos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-tech-debt-v0-5-closure/22-CONTEXT.md
@.planning/phases/22-tech-debt-v0-5-closure/22-RESEARCH.md
@.planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md
@.planning/milestones/v0.5-phases/14-cli-format-foundation/14-REVIEW.md
@src/cli/format.js
@test/version-smoke.test.js
@test/format.test.js

<interfaces>
<!-- Verificado por planner; ejecutor NO necesita re-leer si confía en estos snippets -->

From src/cli/format.js (líneas relevantes):
```js
// Línea 42-50 (función _resolveUseColor; contrato D-02 Phase 14):
//   NO_COLOR (cualquier valor != null) → false
//   FORCE_COLOR != null && FORCE_COLOR !== '0' → true
//   else: stream.isTTY (default false si stream null/undefined)
//
// CONTRATO DOCUMENTADO: cualquier valor non-'0' (incluido '') fuerza true.

// Línea 56-58 (estado actual de visibleWidth):
export function visibleWidth(s) {
  return String(s).replace(/\x1b\[\d+m/g, '').length;
}
// La regex actual cubre solo `\x1b[<N>m` single-param.
```

From test/version-smoke.test.js (líneas 18-22 estado actual):
```js
const result = spawnSync(process.execPath, [KODO_BIN, '--version'], {
  cwd: REPO,
  encoding: 'utf-8',
  // No env override — we want to test the install in its real shape.
});
```

From test/format.test.js (suite `_resolveUseColor precedence (D-02)` casos 1-7):
- case 1: TTY=true + defaults → true
- case 2: TTY=true + NO_COLOR='1' → false
- case 3: TTY=false + FORCE_COLOR='1' → true
- case 4: TTY=true + NO_COLOR='1' + FORCE_COLOR='1' → false (NO_COLOR gana)
- case 5: TTY=true + FORCE_COLOR='0' → false (D-02b: explicit disable)
- case 6: TTY=false + defaults → false
- case 7: TTY=true + NO_COLOR='' → false (NO_COLOR present, any value disables)

FALTA case 8: TTY=false + FORCE_COLOR='' → true (gap IN-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-01 audit — confirmar 14-SECURITY.md vigente (NO regenerar)</name>
  <files>No-op task — documental, sin file changes. Solo verificación.</files>
  <read_first>
    .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md (frontmatter + Sign-Off)
  </read_first>
  <action>
    **DEBT-01 está satisfied-by-existing.** Esta task es un NO-OP de código — solo se ejecutan las verificaciones que siguen para registrar evidencia en el SUMMARY del plan al cierre:

    1. `head -10 .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md` debe mostrar:
       - `phase: 14`
       - `slug: cli-format-foundation`
       - `status: verified`
       - `threats_open: 0`
       - `asvs_level: 1`
       - `created: 2026-05-06`
    2. `grep -c '^- \\[x\\]' .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md` debe devolver `>=4` (los 4 checkbox del Sign-Off).
    3. `grep -E '^\\*\\*Total threats:\\*\\*\\s+13' .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md` debe imprimir la línea (13 threats totales, 13 closed).

    Si las 3 verificaciones pasan: marcar DEBT-01 como satisfied-by-existing en el SUMMARY del plan citando `closed_by_phase_14_secure_phase_run_2026_05_06`. NO crear ni reescribir el archivo (per D-02 Karpathy regla 3 — surgical changes). NO crear un commit dedicado a DEBT-01.

    Si CUALQUIERA falla: detener y reportar — significa que CONTEXT.md/RESEARCH.md tienen estado obsoleto y este plan debe revisarse antes de continuar.
  </action>
  <verify>
    <automated>head -10 .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md | grep -q 'threats_open: 0' && head -10 .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md | grep -q 'status: verified'</automated>
  </verify>
  <done>El frontmatter de 14-SECURITY.md contiene `threats_open: 0` y `status: verified`. DEBT-01 cerrado por audit, no por re-creación.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEBT-02 (WR-01 Phase 14) — timeout explícito en version-smoke</name>
  <files>test/version-smoke.test.js</files>
  <read_first>
    test/version-smoke.test.js (líneas 1-50; contexto del spawnSync)
  </read_first>
  <behavior>
    - Test existente sigue pasando con el mismo exit code (0) y misma assertion de stdout (`assert.match(result.stdout, /^\\d+\\.\\d+\\.\\d+$/)`).
    - Comportamiento nuevo: si `bin/kodo` cuelga >10s, `result.signal === 'SIGTERM'` y el test FALLA explícitamente (vs. quedarse colgado indefinidamente con el wall-clock global de `node --test`).
    - No se añade un test nuevo — solo se modifica la options bag del `spawnSync` existente.
  </behavior>
  <action>
    Edit `test/version-smoke.test.js`. En el llamado a `spawnSync` (líneas 18-22 actuales), añadir la option `timeout: 10_000` ENTRE `encoding: 'utf-8',` y el comentario `// No env override...`. Resultado esperado:

    ```js
    const result = spawnSync(process.execPath, [KODO_BIN, '--version'], {
      cwd: REPO,
      encoding: 'utf-8',
      timeout: 10_000, // WR-01 Phase 14 — fail-fast si el bin cuelga (CI hygiene)
      // No env override — we want to test the install in its real shape.
    });
    ```

    NO modificar nada más en el archivo. NO renombrar el test. NO añadir asserts sobre `result.signal` — el comportamiento default de `node --test` ya reporta el timeout claramente. El comentario inline `// WR-01 Phase 14 — fail-fast...` ata el cambio al ID del Resolution Log per D-09.

    Karpathy regla 3: cambio quirúrgico, una línea. Karpathy regla 2: sin spec-features.
  </action>
  <verify>
    <automated>node --test test/version-smoke.test.js</automated>
  </verify>
  <done>
    `test/version-smoke.test.js` contiene la línea `timeout: 10_000` con comentario `WR-01 Phase 14`; suite del archivo verde; `grep -c 'timeout: 10_000' test/version-smoke.test.js` devuelve 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DEBT-03 IN-01 + IN-02 — regex CSI defensiva + caso FORCE_COLOR=''</name>
  <files>
    src/cli/format.js,
    test/format.test.js
  </files>
  <read_first>
    src/cli/format.js (líneas 40-80: _resolveUseColor + visibleWidth),
    test/format.test.js (líneas 1-90: suite _resolveUseColor con 7 casos)
  </read_first>
  <behavior>
    - **visibleWidth (IN-01):** sigue devolviendo `0` para `''`, `String.length` para texto plano, y AHORA además strip-ea correctamente `\x1b[33;1m` (bold yellow), `\x1b[38;5;200m` (256-color), y cualquier CSI `\x1b[<params>X` con `X` letra ASCII (`m`, `K`, `H`, etc.).
    - Concretamente:
      - `visibleWidth('\\x1b[33;1mbold yellow\\x1b[0m') === 11`.
      - `visibleWidth('\\x1b[38;5;200mpurple\\x1b[0m') === 6`.
      - `visibleWidth('plain') === 5` (regresión).
      - `visibleWidth('\\x1b[33mhello\\x1b[0m') === 5` (regresión single-param).
    - **_resolveUseColor (IN-02):** existe un caso 8 que asserta `_resolveUseColor({ isTTY: false }, { FORCE_COLOR: '' }) === true`. Justifica el contrato D-02 documentado en `src/cli/format.js:44`: `FORCE_COLOR != null && FORCE_COLOR !== '0'` — `''` no es `null`, no es `'0'`, por tanto fuerza true.
  </behavior>
  <action>
    **Sub-task 3a — IN-01 regex defensiva (`src/cli/format.js`):**

    Reemplazar la línea 57 (actual: `return String(s).replace(/\\x1b\\[\\d+m/g, '').length;`) por:
    ```js
    return String(s).replace(/\x1b\[[\d;]*[A-Za-z]/g, '').length;
    ```

    La regex nueva:
    - `\x1b\[` — secuencia CSI inicial.
    - `[\d;]*` — cero o más dígitos/separadores (`33;1`, `38;5;200`, vacío `\x1b[m`).
    - `[A-Za-z]` — letra terminadora (`m` SGR, `K` erase, `H` cursor, etc.).

    NO añadir comentario adicional — el código es self-evident y CLAUDE.md regla 3 prohibe comentario decorativo.

    **Sub-task 3b — extender `test/format.test.js` (IN-01 regression + IN-02 case 8):**

    Localizar la suite `describe('_resolveUseColor precedence (D-02)', ...)` y la suite `describe('visibleWidth', ...)` (si existe; si no, añadirla).

    1. En la suite de `_resolveUseColor`, AÑADIR como último `it` (case 8):
       ```js
       it("case 8: TTY=false + FORCE_COLOR='' => true (any non-'0' value forces, IN-02 Phase 14)", () => {
         assert.equal(_resolveUseColor({ isTTY: false }, { FORCE_COLOR: '' }), true);
       });
       ```

    2. En la suite de `visibleWidth` (o en una NUEVA suite `describe('visibleWidth CSI multi-param (IN-01 Phase 14)', ...)` si no hay suite previa), AÑADIR estos asserts:
       ```js
       it('strip multi-param CSI \\x1b[33;1m', () => {
         assert.equal(visibleWidth('\x1b[33;1mbold yellow\x1b[0m'), 11);
       });
       it('strip 256-color CSI \\x1b[38;5;200m', () => {
         assert.equal(visibleWidth('\x1b[38;5;200mpurple\x1b[0m'), 6);
       });
       it('regression: plain string unchanged', () => {
         assert.equal(visibleWidth('plain'), 5);
       });
       it('regression: single-param CSI still strips', () => {
         assert.equal(visibleWidth('\x1b[33mhello\x1b[0m'), 5);
       });
       ```

    Asegurarse de que el import de `visibleWidth` está en el `top` del archivo (junto a `_resolveUseColor`). Si no está, añadirlo al import existente de `../src/cli/format.js`.

    Karpathy regla 3 — cambios quirúrgicos, sin tocar otros tests.

    **Sub-task 3c — verificar matriz completa:**

    Tras los cambios, correr `node --test test/format.test.js` y confirmar que TODOS los casos (1-8 + visibleWidth regression + new asserts) pasan verdes.
  </action>
  <verify>
    <automated>node --test test/format.test.js</automated>
  </verify>
  <done>
    - `grep -c "\\\\x1b\\\\[\\\\[\\\\\\\\d;\\\\]\\\\*\\\\[A-Za-z\\\\]" src/cli/format.js` devuelve >=1.
    - `grep -c "FORCE_COLOR: ''" test/format.test.js` devuelve >=1.
    - `grep -c "case 8" test/format.test.js` devuelve >=1.
    - `node --test test/format.test.js` exit 0.
    - El test es contract-aware (IN-02 cita Phase 14 by ID per D-09).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.env` → `_resolveUseColor` | Lectura de NO_COLOR / FORCE_COLOR (sin cambios; mismo contrato Phase 14 D-02) |
| `String` user-supplied → `visibleWidth` regex | La regex nueva es estrictamente más permisiva (acepta CSI general); sin nuevas surfaces de input (función pura, sin I/O) |
| `spawnSync` test runner → `bin/kodo` | Mismo boundary que Phase 14 T-14-11; el timeout cierra DoS surface previamente accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Denial of Service | `test/version-smoke.test.js` spawnSync sin timeout | mitigate | Timeout explícito `10_000` ms en `spawnSync` options bag — convierte el accepted risk T-14-11 ("CLI personal, spawnSync síncrono") en active control de fail-fast en CI |
| T-22-01-02 | Denial of Service | `visibleWidth(s)` regex pathological en CSI muy largo (`\x1b[<10000 digits>m`) | accept | El regex engine de V8 es lineal sobre `[\d;]*[A-Za-z]` (no backtracking ambiguo); input máximo realista <100 chars per CSI; CLI personal sin user-uploaded text — sin DoS surface |
| T-22-01-03 | Tampering | Regex CSI más permisiva strip-ea secuencias inesperadas (cursor moves, erase) en strings que las quieren preservar | accept | `visibleWidth` es contract-named para "ancho visible" — strip de CSI es feature, no bug. Consumers de Phase 15 (`formatRow` widths) NUNCA pasan secuencias no-SGR. Documentado en contrato D-02 |
| T-22-01-04 | Information Disclosure | Test FORCE_COLOR='' filtra valor de env en stderr | accept | El test usa fixture `{ FORCE_COLOR: '' }` — `''` no es secreto. Sin trust boundary nueva. |

**threats_open: 0** — todas mitigated o accepted con rationale documentada.
</threat_model>

<verification>
1. `node --test test/version-smoke.test.js` verde — DEBT-02 closed.
2. `node --test test/format.test.js` verde — DEBT-03 IN-01 + IN-02 closed.
3. `npm test` completo verde (regresión Phase 14/15 — golden bytes preservados, formatRow widths consistentes).
4. `head -10 .planning/milestones/v0.5-phases/14-cli-format-foundation/14-SECURITY.md` muestra `threats_open: 0` + `status: verified` — DEBT-01 audited as satisfied-by-existing.
5. Grep sanity:
   - `grep -c 'timeout: 10_000' test/version-smoke.test.js` → 1.
   - `grep -c "\\\\x1b\\\\[\\\\[\\\\\\\\d;\\\\]\\\\*\\\\[A-Za-z\\\\]" src/cli/format.js` → 1.
   - `grep -c "FORCE_COLOR: ''" test/format.test.js` → 1.
</verification>

<success_criteria>
- [x] DEBT-01: 14-SECURITY.md vigente con `threats_open: 0` (audit-only, no rewrite).
- [x] DEBT-02: `test/version-smoke.test.js` con `timeout: 10_000`.
- [x] DEBT-03 IN-01: regex CSI defensiva `/\x1b\[[\d;]*[A-Za-z]/g` en `src/cli/format.js#visibleWidth`.
- [x] DEBT-03 IN-02: caso 8 (`FORCE_COLOR=''` → true) en `test/format.test.js`.
- [x] Suite global verde (~609 tests + asserts nuevos).
- [x] Golden bytes DX-06 preservados (visibleWidth con plain strings no cambia).
- [x] Each commit cita el DEBT/WR/IN ID (per D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/22-tech-debt-v0-5-closure/22-01-SUMMARY.md` with:
- DEBT-01 status: satisfied-by-existing (cita `14-SECURITY.md` con `created: 2026-05-06`).
- DEBT-02 status: closed (cita commit SHA).
- DEBT-03 IN-01 + IN-02 status: closed (cita commit SHA y test casos añadidos).
- Verificación grep cumplida.
- Cita a D-01..D-09 del CONTEXT.md según corresponda.
</output>
