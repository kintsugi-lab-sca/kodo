---
phase: 49-live-progress-spike-hard-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md
autonomous: false
requirements: [PROG-01]
user_setup:
  - service: claude-code-hook-config
    why: "Registrar/desregistrar un hook TaskCreated/TaskCompleted throwaway en ~/.claude/settings.json y lanzar una sesión interactiva real /gsd-execute-phase — un agente autónomo no puede conducir una sesión `claude --worktree` interactiva ni observar sus superficies vivas en primera persona."
    env_vars: []
    dashboard_config:
      - task: "Backup de ~/.claude/settings.json antes de añadir el hook throwaway; restaurar al terminar"
        location: "~/.claude/settings.json (filesystem local)"

must_haves:
  truths:
    - "Existe 49-SPIKE.md con un veredicto VIABLE|INVIABLE explícito en una línea"
    - "El header del doc cita la versión real medida con `claude --version` al momento del spike (no 2.1.174 inferida)"
    - "El doc evalúa las 3 superficies en orden de preferencia con evidencia cruda por cada una intentada (incluidos fallos)"
    - "El Evidence Map tiene una fila por superficie con las 4 condiciones marcadas (a/b/c/d)"
    - "El doc registra una decisión de gate inequívoca para Phase 50 (INVIABLE → corte + PROG-F1; VIABLE → proceder)"
    - "git diff -- src/ test/ bin/ queda vacío (el spike no envía código de producción)"
  artifacts:
    - path: ".planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md"
      provides: "El veredicto empírico con header de versión, Evidence Map de 4 condiciones por superficie, apéndice de evidencia cruda, veredicto explícito y decisión de gate"
      contains: "VIABLE"
      min_lines: 60
  key_links:
    - from: "49-SPIKE.md (correlación condición c)"
      to: "src/session/state.js findSession"
      via: "round-trip session_id → task_id sobre ~/.kodo/state.json real"
      pattern: "findSession"
    - from: "49-SPIKE.md (condición a/b)"
      to: "~/.claude/tasks/<session_id>/ y/o /tmp/kodo-spike-task-*.json"
      via: "evidencia cruda capturada durante una sesión /gsd-execute-phase viva"
      pattern: "tasks/|kodo-spike-task"
---

<objective>
Ejecutar el spike empírico de progreso vivo y emitir el único deliverable de esta fase:
el documento de veredicto `49-SPIKE.md` con un VIABLE/INVIABLE inequívoco respaldado por
un Evidence Map de 4 condiciones por superficie candidata.

Esta fase **ES el research** (espejo de v0.11 Phase 45): empírica, version-specific, no
pre-investigable desde docs. La evidencia DEBE venir de la build instalada en ejecución
(per D-01), observando una sesión `claude --worktree` interactiva real lanzada por kodo.

Purpose: Gobernar el gate duro Phase 49 → Phase 50. INVIABLE es el default esperado (D-04);
si INVIABLE, Phase 50 se corta y PROG-02/03 se difieren a v2 (PROG-F1) sin penalización.

Output: `.planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md`. **NINGÚN código
de producción** — el harness es throwaway (per D-01) y vive solo en `/tmp` + un backup
temporal de `~/.claude/settings.json`.

NO construir el pipeline de captura (PROG-02) ni el display (PROG-03) — eso es Phase 50,
condicional a un veredicto VIABLE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-live-progress-spike-hard-gate/49-CONTEXT.md
@.planning/phases/49-live-progress-spike-hard-gate/49-RESEARCH.md
@.planning/phases/49-live-progress-spike-hard-gate/49-VALIDATION.md

<interfaces>
<!-- Contratos existentes que el round-trip de correlación (condición c) reusa. -->
<!-- Cero código de producción nuevo: el spike SOLO lee. -->

From src/session/state.js:
```javascript
// findSession resuelve session_id → record → task_id contra ~/.kodo/state.json.
// Reusar tal cual para la prueba de correlación condición (c). NO modificar.
export function findSession(query); // query: { sessionId } | { workspaceRef } | { cwd }
                                    // → { id, session, source } | null
                                    // session.task_id es el linchpin para nombrar el artefacto kodo (D-05)
export function loadState();        // → { schema_version, sessions{task_id→record}, history[] }
```

From src/config.js:
```javascript
// KODO_DIR = join(homedir(), '.kodo') — la raíz del artefacto kodo-controlado (D-05).
// El spike NO escribe aquí (throwaway escribe a /tmp); solo documenta la ruta que
// Phase 50 usaría: ~/.kodo/progress/<task_id>.json (mirror de ~/.kodo/plans/<task_id>.md).
const KODO_DIR; // export const
```

Patrón mirror productor↔consumidor (referencia, NO se construye aquí):
- src/hooks/session-start.js  — HOOK-02 golden-bytes (el spike NO lo toca)
- src/cli/dashboard/plan.js   — readLightPlan: ruta filesystem byte-idéntica, never-throws, anti-ReDoS task_id guard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verificar versión instalada + preparar harness throwaway (Wave 0)</name>
  <files>49-SPIKE.md (sección header + apéndice "Setup"), /tmp/kodo-spike-taskhook.sh, ~/.claude/settings.json (backup temporal)</files>
  <read_first>
    - .planning/phases/49-live-progress-spike-hard-gate/49-RESEARCH.md (Pitfall 0 — versión real 2.1.175 no 2.1.174; Code Examples — sonda Surface 1; Wave 0 Gaps)
    - ~/.claude/settings.json (los hooks existentes GSD/codeisland que NO se deben perder)
    - Resultado de `readlink -f "$(which claude)"` (el binario del bundle cmux: /Applications/cmux.app/Contents/Resources/bin/claude)
  </read_first>
  <action>
    Re-ejecutar `claude --version` en primera persona y `readlink -f "$(which claude)"`; pin la versión EXACTA medida hoy en el header de 49-SPIKE.md (per Pitfall 0 — NO citar 2.1.174 sin re-verificar; el research midió 2.1.175 vía el bundle cmux).

    Crear el hook throwaway `/tmp/kodo-spike-taskhook.sh`: vuelca el stdin crudo completo a `/tmp/kodo-spike-task-<timestamp>.json`, never-throws, `exit 0` siempre (nunca `exit 2` → nunca rollback de TaskCreated/TaskCompleted). NO crear nada en src/.

    Hacer backup de `~/.claude/settings.json` (copia a un fichero `.bak` temporal). Registrar el hook throwaway para los eventos `TaskCreated` y `TaskCompleted` preservando todos los hooks existentes (append al bloque hooks, no reemplazar). Confirmar que `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` sigue activo (la maquinaria de TaskCreate).

    Inicializar el esqueleto de 49-SPIKE.md (per D-03): header `claude --version` + ruta del binario → tabla Evidence Map de 4 condiciones × 3 superficies (vacía, a rellenar) → secciones de apéndice por superficie → línea de veredicto placeholder → sección de decisión de gate. NO emitir veredicto todavía.
  </action>
  <verify>
    <automated>test -x /tmp/kodo-spike-taskhook.sh && test -f ~/.claude/settings.json.bak 2>/dev/null && grep -q "TaskCreated" ~/.claude/settings.json && grep -qE "claude --version|Versión instalada|2\.1\.[0-9]+" .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md && echo OK</automated>
  </verify>
  <done>El hook throwaway existe y es ejecutable; settings.json tiene backup y el hook TaskCreated/TaskCompleted registrado sin perder los existentes; 49-SPIKE.md existe con el header de versión real medida y el esqueleto del Evidence Map.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Observar la sesión viva /gsd-execute-phase y capturar evidencia cruda por superficie</name>
  <files>49-SPIKE.md (Evidence Map relleno + apéndice de evidencia cruda), /tmp/kodo-spike-task-*.json (capturas throwaway), ~/.claude/settings.json (restaurado al final)</files>
  <read_first>
    - .planning/phases/49-live-progress-spike-hard-gate/49-RESEARCH.md (Surface Evidence Dossier — las 3 superficies; Pitfall 1 — probar el tipo de sesión correcto; Pitfall 2 — payload puede no llevar identificador; Pitfall 3 — el .lock es de escritor)
    - .planning/phases/49-live-progress-spike-hard-gate/49-VALIDATION.md (Manual-Only Verifications — las 4 condiciones)
    - ~/.kodo/state.json (la fuente de correlación: sessions{task_id → {session_id, worktree_path}})
    - src/session/state.js (findSession :319 — el round-trip de la condición c)
  </read_first>
  <what-built>
    El harness throwaway (Task 1): hook TaskCreated/TaskCompleted que vuelca stdin a /tmp,
    settings.json con backup, y el esqueleto de 49-SPIKE.md con el Evidence Map.
  </what-built>
  <action>
    Tarea operator-driven (checkpoint:human-verify) — un agente autónomo NO puede conducir una sesión `claude --worktree` interactiva ni observar sus superficies vivas. El operador ejecuta los pasos de <how-to-verify>, captura la evidencia cruda en el apéndice de 49-SPIKE.md, rellena cada celda del Evidence Map (4 condiciones × superficie intentada en orden de preferencia), ejecuta el round-trip de correlación, y al terminar restaura `~/.claude/settings.json` desde el `.bak`. El agente NO marca condiciones sin evidencia cruda adjunta (D-04, sin crédito parcial).
  </action>
  <how-to-verify>
    EL CORAZÓN DEL SPIKE — inherentemente operator-driven (un agente no puede conducir una sesión `claude --worktree` interactiva). El operador (humano) debe:

    1. **Lanzar una sesión interactiva REAL que dispare olas de agentes con TaskCreate** (Pitfall 1 — NO una sesión quick/idle). El sujeto debe ser una sesión `claude --worktree` lanzada por kodo que ejecute `/gsd-execute-phase` de una fase con ≥2 plans/waves. Documentar en 49-SPIKE.md qué comando GSD corrió el sujeto.

    2. **Surface 1 (hook, preferida):** durante/tras la sesión, inspeccionar `/tmp/kodo-spike-task-*.json`. ¿Disparó el evento (condición a)? ¿El payload lleva `session_id`/`cwd`/`transcript_path` correlacionable (condición c)? Adjuntar el payload crudo en el apéndice. Si el payload NO lleva identificador → Surface 1 falla (c) → descender a Surface 3.

    3. **Surface 2 (transcript):** comprobar si `~/.claude/projects/<slug>/<session_id>.jsonl` contiene entradas task/todo de las que derivar N/M (research base: NINGUNA → condición b probablemente falla). Adjuntar el resultado del scan.

    4. **Surface 3 (tasks dir, último recurso):** comprobar si `~/.claude/tasks/<session_id>/` se materializó (condición a). Si sí, derivar N/M = (ficheros con `status:"completed"`) / (total ficheros N.json) (condición b). Lectura best-effort never-throws — NO tomar el `.lock` (Pitfall 3). Adjuntar el listado del dir.

    5. **Correlación (condición c) — round-trip real:** tomar un `session_id` real de `~/.kodo/state.json` y resolverlo a su `task_id` vía `findSession({sessionId})` (state.js:319). Adjuntar el mapeo `session_id → task_id → ~/.kodo/progress/<task_id>.json` (la ruta que Phase 50 usaría, D-05). NO escribir el artefacto — solo demostrar la cadena.

    6. **Condición d:** constatar que la sesión completó sin degradación/stall durante el polling de lectura, y que el spike escribió SOLO a /tmp (cero mutación de internos de Claude Code).

    Rellenar cada celda del Evidence Map con evidencia cruda adjunta. Sin evidencia cruda = esa condición FALLA (D-04, sin crédito parcial).

    **Al terminar: restaurar `~/.claude/settings.json` desde el `.bak` (desregistrar el hook throwaway).**
  </how-to-verify>
  <verify>
    <human-check>El Evidence Map tiene las 4 condiciones (a/b/c/d) por superficie intentada con evidencia cruda adjunta; el round-trip session_id→task_id está adjunto; settings.json restaurado.</human-check>
  </verify>
  <acceptance_criteria>
    - El Evidence Map tiene las 4 condiciones (a/b/c/d) marcadas para CADA superficie intentada, en orden de preferencia.
    - Cada celda tiene evidencia cruda adjunta en el apéndice (payload, listado de dir o snippet de transcript con timestamp dentro de la ventana de sesión).
    - El round-trip `session_id → task_id` vía findSession está adjunto para UNA sesión real (condición c).
    - Está documentado qué comando GSD (`/gsd-execute-phase` de qué fase) corrió el sujeto.
    - `~/.claude/settings.json` restaurado al estado pre-spike (hook throwaway desregistrado).
  </acceptance_criteria>
  <done>El Evidence Map está relleno con las 4 condiciones por superficie intentada + evidencia cruda en el apéndice; el round-trip de correlación está adjunto; settings.json restaurado al estado pre-spike.</done>
  <resume-signal>Escribe "evidencia capturada" cuando el Evidence Map esté relleno con evidencia cruda y settings.json restaurado; o describe qué superficie/condición no pudo observarse.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Emitir el veredicto VIABLE/INVIABLE y la decisión de gate</name>
  <files>49-SPIKE.md (línea de veredicto + decisión de gate), 49-VALIDATION.md (frontmatter sign-off)</files>
  <read_first>
    - .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md (el Evidence Map relleno en Task 2)
    - .planning/phases/49-live-progress-spike-hard-gate/49-CONTEXT.md (D-04 — sin crédito parcial; cualquier fallo → INVIABLE)
    - .planning/ROADMAP.md (Phase 49 Success Criteria #4/#5; gate semantics Phase 49 → 50)
    - .planning/REQUIREMENTS.md (PROG-F1 — el fallback INVIABLE)
    - .planning/phases/49-live-progress-spike-hard-gate/49-VALIDATION.md (Validation Sign-Off checklist)
  </read_first>
  <action>
    Aplicar la regla de veredicto de D-04 con sesgo INVIABLE-por-defecto, sin crédito parcial: VIABLE SOLO si la superficie ganadora demuestra las 4 condiciones (a/b/c/d) con evidencia cruda adjunta. Cualquier condición sin evidencia o fallida → INVIABLE.

    Escribir en 49-SPIKE.md la línea de veredicto inequívoca (`Veredicto: VIABLE` | `Veredicto: INVIABLE`), nombrando la superficie ganadora si VIABLE, o el conjunto de fallos por superficie si INVIABLE.

    Escribir la decisión de gate para Phase 50 (Success Criteria #5):
    - Si INVIABLE: Phase 50 se corta entero (sin stub/placeholder); PROG-02/03 → v2 vía PROG-F1; el milestone cierra con OPEN-* + NYQ-03 sin penalización.
    - Si VIABLE: proceder a Phase 50, nombrando la superficie ganadora y la ruta del artefacto `~/.kodo/progress/<task_id>.json` (D-05) que la captura usaría.

    Confirmar que el spike no envió código de producción (`git diff -- src/ test/ bin/` vacío). Marcar `nyquist_compliant: true` y el Validation Sign-Off en 49-VALIDATION.md.
  </action>
  <verify>
    <automated>grep -qE "Veredicto:\s*(VIABLE|INVIABLE)" .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md && grep -qiE "Phase 50|gate|PROG-F1" .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md && test -z "$(git -C /Users/alex/dev/klab/kodo diff --name-only -- src/ test/ bin/)" && echo OK</automated>
  </verify>
  <done>49-SPIKE.md tiene una línea de veredicto VIABLE|INVIABLE inequívoca + la decisión de gate para Phase 50; `git diff -- src/ test/ bin/` está vacío; 49-VALIDATION.md tiene el sign-off marcado y nyquist_compliant: true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code internals → spike (READ-only) | El spike LEE `~/.claude/hooks/`, `~/.claude/projects/<slug>/*.jsonl`, `~/.claude/tasks/<uuid>/`. Estos son superficie de lectura bajo evaluación, NUNCA fuente de verdad ni superficie de escritura (D-04). |
| spike → ~/.claude/settings.json (WRITE temporal) | El único write a territorio Claude Code es el registro/desregistro del hook throwaway, mediado por un backup obligatorio y restaurado al final. |
| spike → /tmp (WRITE throwaway) | El payload crudo del hook se vuelca a `/tmp/kodo-spike-task-*.json`, NO a `~/.kodo/` (el spike es throwaway; la escritura a ~/.kodo es Phase 50). |
| kodo state.json → spike (READ-only) | La correlación condición (c) LEE `~/.kodo/state.json` vía `findSession`; no lo muta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | El hook throwaway en ~/.claude/settings.json podría sobrescribir/perder los hooks GSD/codeisland existentes | mitigate | Backup obligatorio de settings.json a `.bak` ANTES de editar (Task 1); append al bloque hooks preservando los existentes; restauración desde `.bak` al final (Task 2). Verify de Task 1 exige que el `.bak` exista. |
| T-49-02 | Tampering | El spike podría mutar internos de Claude Code (`~/.claude/tasks/`, transcript) y romper la sesión | mitigate | Lectura best-effort never-throws (mirror `readLightPlan`); NUNCA tomar el `.lock` de los dirs `tasks/` (Pitfall 3); el único write a ~/.claude es el hook temporal con backup. Condición (d) verifica cero ruptura. |
| T-49-03 | Denial of Service | El hook throwaway con `exit 2` haría rollback de TaskCreated/TaskCompleted, degradando la sesión sujeto | mitigate | El hook hace `exit 0` SIEMPRE, never-throws; nunca `exit 2` (per Code Examples del research). Condición (d) confirma cero latencia/ruptura. |
| T-49-04 | Information Disclosure | El payload crudo volcado a /tmp podría contener datos sensibles de la sesión | accept | Es `/tmp` local en la máquina del operador, throwaway, borrado tras el spike. Sin PII de terceros; entorno de un único desarrollador. |
| T-49-05 | Elevation of Privilege | Tratar `~/.claude/tasks/` como fuente de verdad violaría el invariante D-04 (internos como write-source) | mitigate | Por construcción: el artefacto persistido vive en `~/.kodo/` (territorio kodo); `~/.claude/*` es SOLO lectura. El veredicto documenta la ruta `~/.kodo/progress/<task_id>.json` como write-owner, no los internos. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | El spike NO instala paquetes (Package Legitimacy Audit del research: cero deps nuevas). Usa solo el binario `claude` instalado, `node --test`, y utilidades de sistema (jq/python3/tail/stat). Sin superficie de slopsquatting. |
</threat_model>

<verification>
- 49-SPIKE.md existe con línea de veredicto VIABLE|INVIABLE inequívoca y header de versión real medida.
- Evidence Map: 4 condiciones (a/b/c/d) por cada superficie intentada en orden de preferencia, con evidencia cruda adjunta.
- Round-trip `session_id → task_id` vía findSession adjunto para una sesión real (condición c).
- Decisión de gate para Phase 50 registrada (INVIABLE → corte + PROG-F1; VIABLE → proceder + ruta del artefacto).
- `git diff -- src/ test/ bin/` vacío — cero código de producción.
- `~/.claude/settings.json` restaurado (hook throwaway desregistrado).
- 49-VALIDATION.md sign-off marcado, nyquist_compliant: true.
</verification>

<success_criteria>
- [ ] `49-SPIKE.md` existe con un VIABLE/INVIABLE explícito y la versión instalada re-verificada en primera persona (no 2.1.174 inferida).
- [ ] Cada una de las 3 superficies evaluada en orden de preferencia con evidencia cruda (incluidos fallos).
- [ ] El Evidence Map tiene las 4 condiciones marcadas por superficie; sin evidencia = condición falla (D-04).
- [ ] Correlación condición (c) demostrada con un round-trip real `session_id → task_id` vía `findSession`.
- [ ] La decisión de gate para Phase 50 es inequívoca.
- [ ] `git diff -- src/ test/ bin/` vacío — el spike es throwaway, cero código de producción (PROG-02/03 NO construidos).
- [ ] PROG-01 satisfecho: el veredicto empírico existe.
</success_criteria>

<output>
Create `.planning/phases/49-live-progress-spike-hard-gate/49-01-SUMMARY.md` when done.
El deliverable de la fase es `49-SPIKE.md` (el veredicto), NO un pipeline de captura construido.
</output>
