---
phase: 05-config-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface.js
  - src/config.js
  - src/cli.js
  - src/providers/plane/provider.js
  - test/interface.test.js
  - test/migration.test.js
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "kodo config wizard asks which provider to use before any provider-specific questions"
    - "Wizard validates connection before saving config"
    - "Wizard lists remote projects from the provider for mapping"
    - "getProviderApiKey(name) returns the correct env var for the active provider"
    - "First run without config.json auto-launches wizard for commands that need a provider"
  artifacts:
    - path: "src/interface.js"
      provides: "listProjects method in TaskProvider typedef and TASK_PROVIDER_METHODS"
      contains: "listProjects"
    - path: "src/config.js"
      provides: "getProviderApiKey(name) generic function"
      exports: ["getProviderApiKey"]
    - path: "src/cli.js"
      provides: "Provider-agnostic interactive config wizard with ensureConfig guard"
      contains: "interactiveConfig"
    - path: "src/providers/plane/provider.js"
      provides: "listProjects() implementation delegating to PlaneClient"
      contains: "listProjects"
  key_links:
    - from: "src/cli.js"
      to: "src/providers/registry.js"
      via: "initRegistry + getProvider in wizard flow"
      pattern: "getProvider\\("
    - from: "src/cli.js"
      to: "src/interface.js"
      via: "TaskProvider.listProjects() called during wizard"
      pattern: "listProjects"
    - from: "src/config.js"
      to: "config.providers[name]"
      via: "getProviderApiKey reads api_key_env from provider config"
      pattern: "api_key_env"
---

<objective>
Provider-agnostic config wizard and API key resolution.

Purpose: Make kodo's configuration layer provider-agnostic so `kodo config` works with any registered provider, not just Plane. Add `listProjects()` to TaskProvider interface, rewrite the wizard to be provider-first, replace `getPlaneApiKey()` with generic `getProviderApiKey(name)`, and add first-run auto-wizard guard.

Output: Working `kodo config` wizard that selects a provider, validates connection, lists projects, and saves provider-agnostic config. Generic API key resolution function. First-run experience for commands needing a provider.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-config-cleanup/05-CONTEXT.md
@.planning/phases/05-config-cleanup/05-RESEARCH.md

@src/interface.js
@src/config.js
@src/cli.js
@src/providers/registry.js
@src/providers/plane/provider.js

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/interface.js (current TASK_PROVIDER_METHODS — must add listProjects):
```javascript
export const TASK_PROVIDER_METHODS = Object.freeze([
  'init', 'getTask', 'updateTaskState', 'addComment',
  'listPendingTasks', 'parseTriggerEvent', 'verifySignature', 'resolveRef',
]);
```

From src/config.js (exports to update):
```javascript
export function loadConfig()          // returns config object
export function saveConfig(config)    // writes config.json
export function getPlaneApiKey()      // TO BE REPLACED by getProviderApiKey(name)
export { KODO_DIR, CONFIG_PATH, PROJECTS_PATH, DEFAULT_CONFIG }
```

From src/providers/registry.js:
```javascript
export function registerProvider(name, factory)
export function getProvider(name)        // returns TaskProvider instance
export async function initRegistry()     // lazy-loads default providers
export function clearRegistry()          // for testing
```

From src/cli.js line 268 (current wizard imports — must be replaced):
```javascript
const { PlaneClient } = await import('./plane/client.js');
const plane = new PlaneClient({ workspaceSlug: config.plane.workspace_slug });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TaskProvider interface + generic getProviderApiKey</name>
  <files>src/interface.js, src/config.js, src/providers/plane/provider.js, test/interface.test.js, test/migration.test.js</files>
  <behavior>
    - Test: TASK_PROVIDER_METHODS includes 'listProjects' (9 methods total)
    - Test: getProviderApiKey('plane') reads env var from config.providers.plane.api_key_env
    - Test: getProviderApiKey() without arg uses config.provider as default
    - Test: getProviderApiKey returns undefined when provider not configured
  </behavior>
  <action>
1. **src/interface.js**: Add `listProjects` to the TaskProvider JSDoc typedef (returns `Promise&lt;Array&lt;{id: string, identifier: string, name: string}&gt;&gt;`). Add `'listProjects'` to TASK_PROVIDER_METHODS array (now 9 methods).

2. **test/interface.test.js**: Update the test `'TASK_PROVIDER_METHODS has the 8 exact methods'` to expect 9 methods including `listProjects`. Run test — should FAIL (RED). Then update interface.js — should PASS (GREEN).

3. **src/config.js**: Add `getProviderApiKey(providerName)` function:
   - If no `providerName` arg, use `config.provider` from loadConfig()
   - Read `config.providers[name].api_key_env` to get the env var name
   - Return `process.env[envVarName]` or undefined
   - Keep `getPlaneApiKey()` as deprecated thin wrapper calling `getProviderApiKey('plane')` for backward compat
   - Export `getProviderApiKey`

4. **test/migration.test.js**: Add test block for `getProviderApiKey`:
   - Mock config with `providers.plane.api_key_env: 'PLANE_API_KEY'`
   - Set env var, call `getProviderApiKey('plane')`, assert returns value
   - Test default (no arg) reads from `config.provider`

5. **src/providers/plane/provider.js**: Add `listProjects()` method to the provider object returned by `createPlaneProvider`. Delegate to `client.listProjects()` and map results to `{id, identifier, name}` shape.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/interface.test.js test/migration.test.js</automated>
  </verify>
  <done>TASK_PROVIDER_METHODS has 9 methods including listProjects. getProviderApiKey(name) exported from config.js and tested. PlaneProvider.listProjects() delegates to client. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite config wizard + first-run auto-wizard guard</name>
  <files>src/cli.js</files>
  <action>
1. **Rewrite `interactiveConfig()` in cli.js** (currently lines ~237-303) to be provider-agnostic:
   - Use `node:readline` createInterface (already imported)
   - Step 1: Show banner "kodo config" 
   - Step 2: List available providers from a static array `['plane']` (extensible later). Ask user to select by number. Default: 1.
   - Step 3: Set `config.provider = selectedProvider`
   - Step 4: Provider-specific prompts — ask for API key env var name (default: `PLANE_API_KEY` for plane), workspace slug, etc. Write to `config.providers[providerName].*` (v2 schema path, NOT `config.plane.*`)
   - Step 5: Validate connection — `await initRegistry()`, `const provider = getProvider(providerName)`, `await provider.init()`. If init() throws, show error, ask to retry or abort. Do NOT save invalid config.
   - Step 6: List projects via `await provider.listProjects()`. Display numbered list. Ask user which projects to track (comma-separated numbers). Map selected projects to `{id, identifier, name}` and save in config.
   - Step 7: `saveConfig(config)`, show success message
   - Import `initRegistry, getProvider` from `./providers/registry.js` (dynamic import inside the function to avoid circular deps)
   - REMOVE the `PlaneClient` direct import from `./plane/client.js` (line 268)

2. **Add `ensureConfig()` guard function** in cli.js:
   - Check `existsSync(CONFIG_PATH)` — if missing, log "Primera vez? Vamos a configurar kodo." and call `await interactiveConfig()`
   - If still no config after wizard, `console.error('Config requerida.')` + `process.exit(1)`

3. **Wire ensureConfig() into commands that need a provider**: `check`, `launch`, `server`, `status`. Add `await ensureConfig()` at the top of each command's action handler. Do NOT add it to `--help`, `--version`, or `config` commands.

4. **After wizard completes in ensureConfig, let the command continue** — the action handler already calls `loadConfig()` which will pick up the newly created config.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node -e "import('./src/cli.js')" && node --test test/**/*.test.js</automated>
  </verify>
  <done>Config wizard is provider-agnostic: selects provider first, validates connection, lists projects via TaskProvider.listProjects(). No direct PlaneClient import in cli.js. ensureConfig() guards check/launch/server/status commands. help/version/config work without config.json.</done>
</task>

</tasks>

<verification>
- `node --test test/**/*.test.js` — full test suite passes
- `grep -r "PlaneClient" src/cli.js` returns no matches (removed direct import)
- `grep "listProjects" src/interface.js` confirms method in typedef
- `grep "getProviderApiKey" src/config.js` confirms new export
- `node -e "import('./src/config.js').then(m => console.log(typeof m.getProviderApiKey))"` prints "function"
</verification>

<success_criteria>
- TASK_PROVIDER_METHODS includes listProjects (9 methods)
- getProviderApiKey(name) works for any configured provider
- kodo config wizard is provider-first, validates connection, lists projects
- No PlaneClient direct import in cli.js
- Commands check/launch/server/status auto-launch wizard on first run
- help/version/config work without config.json
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-cleanup/05-01-SUMMARY.md`
</output>
