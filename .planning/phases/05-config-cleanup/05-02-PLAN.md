---
phase: 05-config-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/orchestrator/prompt.md
  - src/orchestrator/launch.js
  - src/session/state.js
  - src/session/health.js
  - src/labels.js
  - src/providers/plane/labels.js
  - test/prompt.test.js
  - test/state.test.js
  - test/stop.test.js
autonomous: true
requirements:
  - CONF-04

must_haves:
  truths:
    - "Reading prompt.md shows {{provider}} placeholders, not the word Plane"
    - "The resolved prompt at runtime contains the active provider name, not Plane"
    - "state.js function signatures use taskId, not planeId"
    - "health.js HealthReport uses taskId and task_ref, not planeId and plane_identifier"
    - "launch.js reads session.task_ref, not session.plane_identifier"
    - "src/plane/ legacy directory no longer exists"
    - "No generic module imports from src/plane/"
  artifacts:
    - path: "src/orchestrator/prompt.md"
      provides: "Provider-neutral orchestrator prompt with {{placeholders}}"
      contains: "{{provider}}"
    - path: "src/orchestrator/launch.js"
      provides: "resolvePromptTemplate function doing placeholder replacement"
      contains: "resolvePromptTemplate"
    - path: "src/providers/plane/labels.js"
      provides: "Plane-specific resolveLabels() function moved from src/labels.js"
      contains: "resolveLabels"
    - path: "test/prompt.test.js"
      provides: "Test verifying no hardcoded Plane references in resolved prompt"
      exports: []
  key_links:
    - from: "src/orchestrator/launch.js"
      to: "src/orchestrator/prompt.md"
      via: "reads template and applies resolvePromptTemplate()"
      pattern: "resolvePromptTemplate"
    - from: "src/orchestrator/launch.js"
      to: "src/session/state.js"
      via: "reads session.task_ref (not plane_identifier)"
      pattern: "task_ref"
---

<objective>
Neutralize orchestrator prompt and clean up legacy Plane references.

Purpose: Make the orchestrator prompt provider-agnostic via template placeholders, rename lingering `planeId`/`plane_identifier` parameters in generic modules to `taskId`/`task_ref`, move Plane-coupled label resolution to the provider directory, and delete the legacy `src/plane/` directory.

Output: Prompt.md with `{{provider}}` placeholders resolved at runtime. Clean generic modules with no Plane-specific naming. Legacy directory removed.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-config-cleanup/05-CONTEXT.md
@.planning/phases/05-config-cleanup/05-RESEARCH.md
@.planning/phases/05-config-cleanup/05-01-SUMMARY.md

@src/orchestrator/prompt.md
@src/orchestrator/launch.js
@src/session/state.js
@src/session/health.js
@src/labels.js
@src/plane/client.js

<interfaces>
<!-- Current Plane references that must be neutralized -->

From src/orchestrator/prompt.md (8+ Plane references):
- Line 1: "tareas de Plane"
- Line 6: "acceso a Plane via MCP"
- Line 13: "tareas en Plane"
- Line 28: "Actualiza Plane"
- Line 38: "Documentar en Plane"
- Line 52: "Actualizar Plane"
- Line 60: "{sessions: {planeId: {workspace_ref, plane_identifier, ...}}}"
- Line 72: "Plane MCP server de Plane"

From src/session/state.js (parameter names to rename):
```javascript
export function addSession(planeId, session)     // → taskId
export function removeSession(planeId)           // → taskId
export function updateSession(planeId, updates)  // → taskId
export function getSession(planeId)              // → taskId
```

From src/session/health.js (field names to rename):
```javascript
// HealthReport typedef: planeId → taskId, identifier: session.plane_identifier → session.task_ref
```

From src/orchestrator/launch.js line 98:
```javascript
lines.push(`- **${s.plane_identifier}**: ${s.summary}`);  // → s.task_ref
```

From src/labels.js line 44 (JSDoc type import):
```javascript
@param {import('./plane/client.js').PlaneClient} plane  // resolveLabels() is Plane-specific
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrize orchestrator prompt + template resolution in launch.js</name>
  <files>src/orchestrator/prompt.md, src/orchestrator/launch.js, test/prompt.test.js</files>
  <behavior>
    - Test: prompt.md raw text contains "{{provider}}" and "{{provider_name}}" and "{{mcp_tool}}"
    - Test: prompt.md raw text does NOT contain the literal word "Plane" (case-sensitive, outside of placeholders)
    - Test: resolvePromptTemplate(template, {provider: 'plane'}) replaces all placeholders correctly
    - Test: resolved prompt for provider "plane" contains "Plane" (from {{provider_name}})
    - Test: resolved prompt for provider "github" contains "Github" (parametric, not hardcoded)
  </behavior>
  <action>
1. **Create test/prompt.test.js** (Wave 0 — test first):
   - Import `readFileSync` to read `src/orchestrator/prompt.md`
   - Test that raw template contains no literal "Plane" references (use regex `/\bPlane\b/` excluding inside `{{` delimiters)
   - Test that raw template contains `{{provider}}`, `{{provider_name}}`, `{{mcp_tool}}`
   - Import `resolvePromptTemplate` from `src/orchestrator/launch.js`
   - Test that after resolution with `{provider: 'plane'}`, all `{{...}}` are replaced
   - Run tests — should FAIL (RED)

2. **Rewrite src/orchestrator/prompt.md** — replace every Plane reference:
   - "tareas de Plane" → "tareas de {{provider_name}}"
   - "acceso a Plane via MCP" → "acceso a {{provider_name}} via {{mcp_tool}}"
   - "tareas en Plane" → "tareas en {{provider_name}}"
   - "Actualiza Plane" → "Actualiza {{provider_name}}"
   - "Documentar en Plane" → "Documentar en {{provider_name}}"
   - "Actualizar Plane" → "Actualizar {{provider_name}}"
   - "Plane MCP server de Plane" → "{{mcp_tool}}"
   - Update state schema documentation: `planeId` → `taskId`, `plane_identifier` → `task_ref`, add `provider` field
   - Keep the Spanish language and overall structure intact

3. **Add `resolvePromptTemplate(template, config)` to src/orchestrator/launch.js**:
   - Export the function
   - Replacements map: `{{provider}}` → `config.provider`, `{{provider_name}}` → capitalize first letter of provider, `{{mcp_tool}}` → `${config.provider} MCP server`
   - Use `String.replaceAll()` for each placeholder
   - Wire it into the existing prompt-reading code: after `readFileSync(prompt.md)`, call `resolvePromptTemplate(raw, config)` before passing to Claude

4. **Fix launch.js line 98**: change `s.plane_identifier` to `s.task_ref`

5. Run tests — should PASS (GREEN)
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/prompt.test.js</automated>
  </verify>
  <done>prompt.md contains zero literal Plane references. resolvePromptTemplate() replaces all {{placeholders}} at runtime. launch.js uses s.task_ref. test/prompt.test.js passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename planeId params, move labels, delete legacy src/plane/</name>
  <files>src/session/state.js, src/session/health.js, src/labels.js, src/providers/plane/labels.js, test/state.test.js, test/stop.test.js</files>
  <action>
1. **src/session/state.js** — rename all `planeId` parameter names to `taskId`:
   - `addSession(planeId, session)` → `addSession(taskId, session)` (line 79)
   - `removeSession(planeId)` → `removeSession(taskId)` (line 86)
   - `updateSession(planeId, updates)` → `updateSession(taskId, updates)` (line 96)
   - `getSession(planeId)` → `getSession(taskId)` (line 105)
   - Update all JSDoc `@param` annotations accordingly
   - Update internal references: `state.sessions[planeId]` → `state.sessions[taskId]`

2. **src/session/health.js** — rename Plane-specific fields:
   - HealthReport typedef: `planeId: string` → `taskId: string`
   - All occurrences of `planeId` in report objects → `taskId`
   - `session.plane_identifier` → `session.task_ref` (lines 49, 64, 86)
   - `identifier:` field in report → `ref:` to match naming convention
   - Update `removeSession(report.planeId)` → `removeSession(report.taskId)` (line 105)

3. **src/labels.js** — handle Plane coupling:
   - `parseKodoLabels()` is GENERIC — leave in src/labels.js
   - `resolveLabels()` takes a PlaneClient param — it is Plane-specific
   - Move `resolveLabels()` function to new file `src/providers/plane/labels.js`
   - Remove `resolveLabels` from `src/labels.js`, keep `parseKodoLabels` export
   - Remove the JSDoc `@param {import('./plane/client.js').PlaneClient}` type import from `src/labels.js`
   - Note: no external consumer currently imports `resolveLabels`, so no import updates needed beyond the move itself

4. **Delete `src/plane/` directory** — the entire legacy directory:
   - FIRST: verify no remaining imports from `src/plane/` in any file outside `src/plane/` itself (cli.js import was removed in Plan 01, labels.js import removed in step 3 above)
   - Run `grep -rn "plane/client\|src/plane" src/ --include="*.js" | grep -v "providers/plane"` to confirm zero references
   - `rm -rf src/plane/`

5. **Update callers of renamed state.js functions**: grep for `addSession\|removeSession\|updateSession\|getSession` across codebase. These callers already pass a task ID (from Phase 1 state migration), only the parameter NAME was lagging — no caller changes needed, just verify.

6. **Update existing tests** that reference planeId in state/health assertions:
   - Check `test/state.test.js`, `test/stop.test.js` for `planeId` references and update to `taskId`
   - Check health-related tests for `plane_identifier` references
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/**/*.test.js && grep -rn "planeId\|plane_identifier\|src/plane" src/ --include="*.js" | grep -v "providers/plane" | grep -c . | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>state.js uses taskId params. health.js uses taskId and task_ref. resolveLabels() moved to src/providers/plane/labels.js. src/plane/ directory deleted. No generic module references Plane-specific naming. All tests pass.</done>
</task>

</tasks>

<verification>
- `node --test test/**/*.test.js` — full test suite passes
- `grep -rn "planeId" src/ --include="*.js" | grep -v providers/plane` returns nothing
- `grep -rn "plane_identifier" src/ --include="*.js" | grep -v providers/plane` returns nothing
- `grep -rn "src/plane" src/ --include="*.js" | grep -v providers/plane` returns nothing
- `ls src/plane/` returns "No such file or directory"
- `grep -c "Plane" src/orchestrator/prompt.md` returns 0
- `grep "{{provider}}" src/orchestrator/prompt.md` returns matches
</verification>

<success_criteria>
- Orchestrator prompt.md has zero literal Plane references, only {{placeholders}}
- resolvePromptTemplate() in launch.js replaces placeholders at runtime
- launch.js uses session.task_ref (not plane_identifier)
- state.js param names are taskId throughout
- health.js uses taskId and task_ref
- resolveLabels() lives in src/providers/plane/labels.js, not src/labels.js
- src/plane/ legacy directory is deleted
- All existing tests pass with updated references
- test/prompt.test.js validates prompt neutrality
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-cleanup/05-02-SUMMARY.md`
</output>
