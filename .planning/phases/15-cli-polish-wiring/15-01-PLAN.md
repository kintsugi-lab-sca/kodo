---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger.js
  - src/logs/reader.js
  - test/logger.test.js
  - test/logs-reader.test.js
autonomous: true
requirements: [DX-01, DX-02]
user_setup: []

must_haves:
  truths:
    - "`kodo logs` en TTY (sin NO_COLOR) imprime cada record con shape columnar `HH:MM:SS · LEVEL · component · msg[ +k=v...]` con widths fijas timestamp=8 / level=5 / component=12 y separator ` · `; el nivel está coloreado vía fmt.debug/info/warn/error."
    - "`kodo logs` en non-TTY (o con `NO_COLOR=1`) preserva byte-a-byte el shape pre-Phase-15: `HH:MM:SS LEVEL[ component] msg[ +k=v...]` (single space, sin `·`, sin padding)."
    - "`kodo logs --json` produce bytes idénticos a pre-Phase-15 — el formatter no se invoca para `--json` (bypass total preserved)."
    - "`process.stderr` mirror del logger en TTY usa la misma shape columnar que `kodo logs`; ambos consumen el mismo `formatLine`."
    - "Soporte FORCE_COLOR añadido por delegación en `_resolveUseColor` (D-02 Phase 14): `NO_COLOR > FORCE_COLOR > stream.isTTY`."
  artifacts:
    - path: "src/logger.js"
      provides: "formatLine con shape dual condicionado a useColor (D-02 Phase 15) + useColor source via _resolveUseColor"
      contains: "import { createFormatter, _resolveUseColor } from './cli/format.js'"
      min_lines: 280
    - path: "src/logs/reader.js"
      provides: "useColor source unificada via _resolveUseColor de format.js"
      contains: "import { _resolveUseColor } from '../cli/format.js'"
      min_lines: 115
    - path: "test/logger.test.js"
      provides: "Asserts golden bytes en NO_COLOR + nuevos asserts shape columnar en TTY (matriz 5 casos)"
    - path: "test/logs-reader.test.js"
      provides: "Asserts --json bypass byte-idéntico + assert que useColor proviene de _resolveUseColor (FORCE_COLOR support)"
  key_links:
    - from: "src/logger.js"
      to: "src/cli/format.js"
      via: "import { createFormatter, _resolveUseColor }"
      pattern: "from '\\./cli/format\\.js'"
    - from: "src/logs/reader.js"
      to: "src/cli/format.js"
      via: "import { _resolveUseColor }"
      pattern: "from '\\.\\./cli/format\\.js'"
    - from: "src/logger.js#formatLine TTY branch"
      to: "fmt.formatRow"
      via: "delega layout columnar al formatter Phase 14"
      pattern: "formatRow\\("
    - from: "src/logger.js#maybeMirrorToStderr"
      to: "formatLine(record, { useColor })"
      via: "firma preservada (D-04 CONTEXT)"
      pattern: "formatLine\\(record, \\{ useColor"
---

<objective>
Cablear el helper `src/cli/format.js` (Phase 14) en el subsistema `kodo logs` (DX-01 + DX-02). Concretamente:

1. **`src/logger.js#formatLine`** se modifica para producir **shape dual** condicionado a `useColor`:
   - `useColor=false` → bytes IDÉNTICOS al actual (preservación SC#1 byte-a-byte).
   - `useColor=true` → shape columnar nuevo `HH:MM:SS · LEVEL · component · msg[ +k=v...]` con widths fijas (D-05) y separator ` · ` (D-07), color por nivel via `fmt.debug/info/warn/error`.
2. **`src/logger.js:204`** (cómputo inline `useColor`) se sustituye por `_resolveUseColor(process.stderr)` (Pattern A — añade FORCE_COLOR support).
3. **`src/logs/reader.js:68`** (cómputo inline `useColor`) se sustituye por `_resolveUseColor(process.stdout)`. El loop de `printLine` y el early-return de `--json` (D-03 — `lines 74-79`) se preservan intactos.
4. Tests `logger.test.js` y `logs-reader.test.js` se actualizan: golden bytes en NO_COLOR (sin cambio de fixtures), nuevos asserts para shape columnar en TTY, assert que `--json` no introduce bytes nuevos.

Purpose: Cubrir SC#1 (color + columnas + preservación bytes) y SC#2 (`--json` determinismo) del ROADMAP §Phase 15. Los 4 callsites del subsistema logs comparten el mismo `useColor` y la misma `formatLine` — modificarlos juntos minimiza riesgo y churn.

Output: `src/logger.js` y `src/logs/reader.js` modificados; `test/logger.test.js` y `test/logs-reader.test.js` con nuevos asserts. LOG-12 sigue verde (`format.js` no importa `logger.js`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cli-polish-wiring/15-CONTEXT.md
@.planning/phases/15-cli-polish-wiring/15-PATTERNS.md
@.planning/phases/14-cli-format-foundation/14-CONTEXT.md

# Source of truth — read BEFORE editing
@src/cli/format.js
@src/logger.js
@src/logs/reader.js
@test/logger.test.js
@test/logs-reader.test.js
@test/check-isolation.test.js

<interfaces>
<!-- API que este plan consume desde src/cli/format.js (Phase 14, read-only). NO se exploran. -->

From src/cli/format.js (named exports — usar tal cual):
```javascript
export function _resolveUseColor(stream, env = process.env): boolean
// Precedence: NO_COLOR (gana sobre todo) > FORCE_COLOR (>'0') > stream.isTTY.

export function createFormatter(stream, env = process.env): Formatter
// Formatter = {
//   debug, info, warn, error,                  // level chips (color by level)
//   ok, fail,                                   // symbolic (✓/✗ embedded)
//   green, yellow, red, cyan, gray, dim,        // raw color escapes
//   formatRow(cells: string[], widths: number[], opts?: { separator?: string }): string,
//   formatTable(rows: string[][], opts?: { separator?: string, header?: string[] }): string,
// }

export const OK_SYMBOL = '✓'
export const FAIL_SYMBOL = '✗'
```

Mapping de nivel → método del formatter (idéntico a logger.js#COLOR_BY_LEVEL):
- 'debug' → fmt.debug (gray)
- 'info'  → fmt.info  (cyan)
- 'warn'  → fmt.warn  (yellow)
- 'error' → fmt.error (red)

Default separator del formatter (Phase 14 D-11) = ' · ' (espacio + middle dot + espacio).
`padCell` interno hace pad-only sin truncate; un cell que excede `width` se devuelve tal cual.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modificar src/logger.js — shape dual en formatLine + _resolveUseColor</name>
  <files>src/logger.js, test/logger.test.js</files>

  <read_first>
    - src/logger.js (estado actual completo — el branch useColor=false debe reproducir los bytes pre-Phase-15)
    - src/cli/format.js (firma de createFormatter, _resolveUseColor, formatRow; default separator ' · ')
    - .planning/phases/15-cli-polish-wiring/15-PATTERNS.md §"`src/logger.js` — `formatLine` columnar dual" (líneas 38-156)
    - .planning/phases/15-cli-polish-wiring/15-CONTEXT.md §decisions D-01..D-08
    - test/logger.test.js (estado actual — para entender qué fixtures ya pasan en NO_COLOR)
    - test/check-isolation.test.js (LOG-12 guard original — confirmar que sigue verde tras el nuevo import)
  </read_first>

  <behavior>
    - Test 1 (NO_COLOR preservación, golden bytes): `formatLine({ timestamp: '2026-05-05T10:30:45.123Z', level: 'info', msg: 'hello' }, { useColor: false }) === '10:30:45 INFO hello'` (sin component, sin ctx, single space, NO `·`, NO padding).
    - Test 2 (NO_COLOR con component): `formatLine({ timestamp: '2026-05-05T10:30:45.123Z', level: 'info', component: 'dispatcher', msg: 'go' }, { useColor: false }) === '10:30:45 INFO dispatcher go'`.
    - Test 3 (NO_COLOR con ctx extras): `formatLine({ timestamp: '...', level: 'warn', component: 'lock', msg: 'busy', task_id: 'KL-1', extra: 7 }, { useColor: false }) === '10:30:45 WARN lock busy +extra=7'` (task_id excluido por BASE_RECORD_KEYS, extra incluido como `+k=v`).
    - Test 4 (TTY shape columnar): `formatLine({ timestamp: '2026-05-05T10:30:45.123Z', level: 'info', component: 'dispatcher', msg: 'go' }, { useColor: true })` — debe contener (a) `'10:30:45'` literal, (b) separator ` · ` exactamente 3 veces (timestamp·level·component·msg), (c) component padded a 12 chars (`'dispatcher  '` → `dispatcher` tiene 10 chars, padding=2), (d) escape ANSI cyan (`\x1b[36m`) alrededor del nivel.
    - Test 5 (TTY component vacío): cuando `record.component` es falsy en useColor=true, la columna se rellena con 12 espacios (alineación vertical, D-06).
    - Test 6 (TTY component >12 chars no truncado): `component='gsd-bootstrap'` (13 chars) en useColor=true → la celda se devuelve sin truncar (D-05 pad-only).
    - Test 7 (TTY level=ERROR): `formatLine({...level:'error'...}, { useColor: true })` contiene `\x1b[31m` (rojo) alrededor de `'ERROR'`.
    - Test 8 (FORCE_COLOR support): tras la sustitución `useColor = _resolveUseColor(process.stderr)` (línea 204), un test que stub-ee `process.stderr.isTTY=false` + `process.env.FORCE_COLOR='1'` produce un logger con `useColor=true` (assertable observando el output coloreado de un mensaje de warn). Si testear el side-effect directo es engorroso, alternativa: import directo de `_resolveUseColor` y assertar que el módulo lo invoca con `process.stderr` (verificable con grep tras la edición — ver acceptance_criteria).
  </behavior>

  <action>
    1. **Añadir import al header** de `src/logger.js` justo después de `import { KODO_DIR } from './config.js';` (línea 18):
       ```javascript
       import { createFormatter, _resolveUseColor } from './cli/format.js';
       ```
       LOG-12 invariante: `format.js` NO importa `logger.js` (verificado por `test/format-isolation.test.js` Phase 14). El import es unidireccional. **Verificar tras editar:** `node --test test/check-isolation.test.js` sigue verde.

    2. **Añadir constantes a nivel módulo** (justo debajo de `LEVEL_NAMES` línea 28, mismo patrón `Object.freeze`):
       ```javascript
       /** @type {Readonly<{ timestamp: 8, level: 5, component: 12 }>} */
       const COLUMNAR_WIDTHS = Object.freeze({ timestamp: 8, level: 5, component: 12 });
       ```

    3. **Reemplazar el cuerpo de `formatLine` (líneas 82-92 actuales)** con un branch dual condicionado a `useColor`:
       ```javascript
       export function formatLine(record, { useColor }) {
         const time = String(/** @type {any} */ (record).timestamp).slice(11, 19);
         const lvl = String(/** @type {any} */ (record).level).toUpperCase();

         if (!useColor) {
           // BRANCH NON-TTY/NO_COLOR — preservación byte-a-byte (SC#1).
           // NO modificar este return: cualquier cambio rompe golden bytes test.
           const comp = /** @type {any} */ (record).component
             ? ` ${/** @type {any} */ (record).component}`
             : '';
           const ctx = formatCtxInline(record);
           return `${time} ${lvl}${comp} ${/** @type {any} */ (record).msg}${ctx}`;
         }

         // BRANCH TTY+COLOR — columnar (D-02 + D-05 + D-06 + D-07).
         // widths: timestamp=8, level=5, component=12 (D-05). Separator ' · ' (D-07).
         // Component vacío → 12 espacios (D-06, alineación vertical estricta).
         const fmt = createFormatter(process.stderr);
         const levelKey = /** @type {any} */ (record).level;
         const levelMethod =
           levelKey === 'debug' ? fmt.debug
           : levelKey === 'info'  ? fmt.info
           : levelKey === 'warn'  ? fmt.warn
           : levelKey === 'error' ? fmt.error
           : (s) => s;
         const lvlCell = levelMethod(lvl);
         const compRaw = /** @type {any} */ (record).component
           ? String(/** @type {any} */ (record).component)
           : '';
         const cells = [time, lvlCell, compRaw, String(/** @type {any} */ (record).msg)];
         const widths = [
           COLUMNAR_WIDTHS.timestamp,
           COLUMNAR_WIDTHS.level,
           COLUMNAR_WIDTHS.component,
           // 4ª celda (msg) sin width → no se padea (último argumento de cells.map).
         ];
         // formatRow ignora widths[i] cuando es undefined (Phase 14 src/cli/format.js:130).
         const row = fmt.formatRow(cells, widths);
         const ctx = formatCtxInline(record);
         return `${row}${ctx}`;
       }
       ```
       Mantener `formatCtxInline` intacto (líneas 61-72). Mantener firma `formatLine(record, { useColor })` para no tocar `maybeMirrorToStderr` (línea 271) ni `reader.js:99`.

    4. **Reemplazar el cómputo inline de `useColor` (línea 204 actual)**:
       ```javascript
       // ANTES:
       const useColor = Boolean(process.stderr.isTTY) && !process.env.NO_COLOR;
       // DESPUÉS:
       const useColor = _resolveUseColor(process.stderr);
       ```
       Cero churn en `maybeMirrorToStderr` (líneas 263-272) — `useColor` se sigue capturando en closure y `formatLine(record, { useColor })` mantiene la firma.

    5. **NO TOCAR los siguientes elementos de `src/logger.js`**:
       - `formatCtxInline` (líneas 61-72) — pure helper.
       - `ANSI_RESET`, `ANSI_GRAY`, `ANSI_CYAN`, `ANSI_YELLOW`, `ANSI_RED`, `COLOR_BY_LEVEL` (líneas 31-42) — siguen exportándose para `writeNdjson` (línea 253) y por backwards-compat. **Discretion CONTEXT línea 75**: la línea 253 (`process.stderr.write(\`${ANSI_RED}...${ANSI_RESET}\n\`)`) NO es un surface TTY del SC; se deja igual.
       - Bloque de redacción (líneas 94-172) — fuera de scope.
       - `createLogger`, `makeNode`, `emit`, `writeNdjson` — sólo el cómputo de `useColor` (línea 204) cambia.

    6. **Actualizar `test/logger.test.js`** añadiendo los 8 tests del bloque `<behavior>`. Para los tests TTY (4-7), si los tests actuales no usan fake stream, importar `formatLine` directamente y llamarla con `{ useColor: true }`. Para FORCE_COLOR (test 8), patron: `process.env.FORCE_COLOR='1'; delete process.env.NO_COLOR;` antes del `createLogger`, restaurar después.

    7. **Preservar tests existentes verdes**. Si algún test actual asume el shape pre-Phase-15 con `useColor=true` (con escapes ANSI inline en lugar de columnar), actualizarlo al nuevo shape. Los asserts en `useColor=false` NO se tocan — son los golden bytes.
  </action>

  <verify>
    <automated>node --test test/logger.test.js test/logger-redaction.test.js test/logger-events.test.js test/logger-exports.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { createFormatter, _resolveUseColor } from './cli/format.js'" src/logger.js` === 1
    - `grep -c "_resolveUseColor(process.stderr)" src/logger.js` === 1
    - `grep -cE "Boolean\(process\.stderr\.isTTY\) && !process\.env\.NO_COLOR" src/logger.js` === 0  (cómputo inline eliminado)
    - `grep -c "COLUMNAR_WIDTHS" src/logger.js` >= 2  (definición + uso)
    - `grep -c "Object.freeze({ timestamp: 8, level: 5, component: 12 })" src/logger.js` === 1
    - `grep -c "fmt.formatRow" src/logger.js` >= 1  (TTY branch usa formatRow)
    - `grep -cE "if \(!useColor\)" src/logger.js` >= 1  (shape dual present)
    - El golden bytes test pasa: `node --test test/logger.test.js` → 0 fallos en asserts NO_COLOR pre-existentes.
    - `node --test test/check-isolation.test.js` exit 0 (LOG-12 guard original sigue verde — `check.js` no llega a logger.js).
    - Inspección manual: `process.stderr.write(formatLine(record, { useColor: false }) + '\n')` produce exactamente la misma string que pre-Phase-15 (verificar con un fixture de record sintético dentro del test).
  </acceptance_criteria>

  <done>
    `formatLine` produce shape columnar coloreado en TTY y bytes byte-idénticos al actual en NO_COLOR/non-TTY. `useColor` source unificada via `_resolveUseColor` con FORCE_COLOR support. `node --test test/logger.test.js test/logger-redaction.test.js test/check-isolation.test.js` pasa sin fallos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modificar src/logs/reader.js — useColor source unification + preservar --json bypass</name>
  <files>src/logs/reader.js, test/logs-reader.test.js</files>

  <read_first>
    - src/logs/reader.js (estado actual — confirma early-return --json en líneas 74-79)
    - src/cli/format.js (firma de _resolveUseColor)
    - .planning/phases/15-cli-polish-wiring/15-PATTERNS.md §"`src/logs/reader.js` — `useColor` source change" (líneas 160-212)
    - .planning/phases/15-cli-polish-wiring/15-CONTEXT.md D-01, D-03 (`--json` bypass total)
    - test/logs-reader.test.js (estado actual — entender los fixtures de bytes pre-Phase-15)
  </read_first>

  <behavior>
    - Test 1 (--json bypass byte-idéntico): para una línea NDJSON arbitraria `{"timestamp":"2026-05-05T10:30:45.123Z","level":"info","msg":"x"}`, llamar `runLogs({ sessionId, json: true })` debe escribir a stdout exactamente `<raw>\n` SIN parsear, SIN colorear, SIN columnar — independiente del valor de `process.stdout.isTTY` y de `process.env.FORCE_COLOR`. Bytes idénticos a pre-Phase-15.
    - Test 2 (default formatLine consume): cuando `--json` no está activo, `printLine` invoca `formatLine(rec, { useColor })` con `useColor` = `_resolveUseColor(process.stdout)`. En non-TTY/NO_COLOR los bytes coinciden con pre-Phase-15 (delegado a Task 1).
    - Test 3 (FORCE_COLOR support): con `process.stdout.isTTY=false` + `FORCE_COLOR='1'`, `useColor` resuelve a `true` y `formatLine` produce shape columnar. Asegura que `_resolveUseColor` está cableado correctamente (FORCE_COLOR no estaba soportado por el cómputo inline anterior).
  </behavior>

  <action>
    1. **Añadir import** debajo del actual (línea 24):
       ```javascript
       import { LEVELS, formatLine } from '../logger.js';
       import { _resolveUseColor } from '../cli/format.js';
       ```
       NO importar `createFormatter` aquí — `reader.js` no necesita helpers de color directamente; el formatter lo invoca `formatLine` internamente (delegado a Task 1).

    2. **Reemplazar el cómputo de `useColor` (línea 68 actual)**:
       ```javascript
       // ANTES:
       const useColor = Boolean(process.stdout.isTTY) && !process.env.NO_COLOR;
       // DESPUÉS:
       const useColor = _resolveUseColor(process.stdout);
       ```

    3. **NO TOCAR el resto del archivo**:
       - El early-return de `--json` (líneas 74-79) **se preserva exactamente** — el formatter NUNCA se invoca para `--json` (D-03 bypass total). SC#2 cumplido triviamente.
       - El loop de `printLine` (líneas 80-100) — parse + filtros + `formatLine(rec, { useColor })` — NO cambia. La columnar shape entra **vía `formatLine`** (Task 1); reader.js solo consume.
       - Resolución de `--session-of` (líneas 47-56), `--follow` delegation (líneas 102-106), file-existence check (líneas 108-111), readFile loop (líneas 113-116) — sin cambios.

    4. **Actualizar `test/logs-reader.test.js`**:
       - Añadir Test 1 (--json bypass): assert que con `opts.json=true` la salida es exactamente la línea NDJSON cruda + `\n`, independiente de `isTTY` y `FORCE_COLOR`. Usar un fixture mínimo (1 record) y comparar bytes con `assert.equal`.
       - Añadir Test 3 (FORCE_COLOR): stub `process.stdout.isTTY=false` + `FORCE_COLOR='1'`, assert que la salida contiene `\x1b[36m` (cyan) para un record `level:'info'` (delegado al shape columnar de Task 1).
       - Si tests existentes asumen `useColor=true` con shape NDJSON pre-Phase-15 inline (no columnar), actualizarlos al shape columnar (delegado a Task 1).
       - Tests NO_COLOR existentes NO se tocan — son los golden bytes preservados por Task 1.

    5. **Verificar LOG-12 cross-test**: `reader.js` ya está fuera del grafo de `check.js` (sólo se alcanza desde `cli.js`). El nuevo import a `format.js` NO afecta LOG-12. Confirmar con `node --test test/check-isolation.test.js`.
  </action>

  <verify>
    <automated>node --test test/logs-reader.test.js test/logs-follow.test.js test/logs-session-of.test.js test/logs-head-line.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { _resolveUseColor } from '../cli/format.js'" src/logs/reader.js` === 1
    - `grep -c "_resolveUseColor(process.stdout)" src/logs/reader.js` === 1
    - `grep -cE "Boolean\(process\.stdout\.isTTY\) && !process\.env\.NO_COLOR" src/logs/reader.js` === 0  (cómputo inline eliminado)
    - `grep -c "if (opts.json)" src/logs/reader.js` === 1  (early-return preservado)
    - `grep -c "process.stdout.write(raw + '\\\\n')" src/logs/reader.js` === 1  (passthrough crudo intacto)
    - `node --test test/logs-reader.test.js` exit 0
    - `node --test test/check-isolation.test.js` exit 0  (LOG-12 sigue verde — reader.js no afecta el grafo de check.js)
  </acceptance_criteria>

  <done>
    `kodo logs` consume `formatLine` con `useColor` resuelto via `_resolveUseColor(process.stdout)`. `--json` bypass intacto byte-a-byte. FORCE_COLOR funciona en non-TTY. LOG-12 verde.
  </done>
</task>

</tasks>

<verification>
- `node --test` global pasa sin nuevos fallos respecto a la baseline Phase 14.
- `kodo logs <session-id>` en TTY muestra columnas alineadas con color por nivel.
- `kodo logs <session-id>` con `NO_COLOR=1` produce bytes idénticos al pre-Phase-15.
- `kodo logs --json <session-id>` produce bytes idénticos al pre-Phase-15.
- `node --test test/check-isolation.test.js` y `node --test test/format-isolation.test.js` siguen verdes (LOG-12 + single-source-of-color preservados — el plan 15-04 añadirá la aserción positiva de import a `format-isolation.test.js`).
</verification>

<success_criteria>
DX-01 (color por nivel) + DX-02 (columnas alineadas) cubiertos. SC#1 ROADMAP §15: TTY columnar + non-TTY/NO_COLOR bytes idénticos. SC#2 ROADMAP §15: `--json` byte-a-byte preserved.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/15-cli-polish-wiring/15-01-SUMMARY.md` con: archivos modificados, líneas tocadas, golden bytes verificados, FORCE_COLOR support añadido, LOG-12 verde.
</output>
