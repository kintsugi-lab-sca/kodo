---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/check.js
  - test/check.test.js
autonomous: true
requirements: [DX-05]
user_setup: []

must_haves:
  truths:
    - "`src/check.js` ya NO contiene escapes ANSI inline (`\\x1b[33m`, `\\x1b[31m`, `\\x1b[0m`); todos pasan por el formatter de `src/cli/format.js`."
    - "`runCheckAndAct` en TTY muestra `[kodo:check] N pending kodo task(s)...` en amarillo, `Error checking tasks: ...` en rojo, y `All clear` con símbolo `✓` (en verde) anteponiendo el texto."
    - "`runCheckAndAct` en non-TTY (o con NO_COLOR=1) produce bytes sin secuencias ANSI; el símbolo `✓` permanece literal porque `fmt.ok` lo embebe siempre."
    - "`checkPendingTasks` acepta `formatterFn` opcional con default factory `() => createFormatter(process.stdout)` — patrón DI idéntico al `getProviderFn` ya existente."
    - "Test-graph guard LOG-12 sigue verde: `check.js → format.js` está permitido (`format.js` no importa `logger.js`)."
  artifacts:
    - path: "src/check.js"
      provides: "Color/format via fmt.yellow/fmt.red/fmt.ok; ANSI inline eliminados; formatterFn DI"
      contains: "import { createFormatter } from './cli/format.js'"
      min_lines: 130
    - path: "test/check.test.js"
      provides: "Tests 5a/5b actualizados (color via formatter), nuevo test 5c All clear con ✓, nuevo test 5d NO_COLOR sin escapes ANSI"
  key_links:
    - from: "src/check.js"
      to: "src/cli/format.js"
      via: "import { createFormatter }"
      pattern: "from '\\./cli/format\\.js'"
    - from: "checkPendingTasks"
      to: "fmt.yellow / fmt.red"
      via: "DI por formatterFn opcional"
      pattern: "fmt\\.(yellow|red|ok)\\("
    - from: "runCheck → All clear path"
      to: "fmt.ok('All clear')"
      via: "símbolo ✓ embebido por el helper Phase 14"
      pattern: "fmt\\.ok\\('All clear'\\)"
---

<objective>
Cablear el helper `src/cli/format.js` (Phase 14) en `src/check.js` (DX-05). Eliminar los 3 escapes ANSI inline (`ANSI_YELLOW`, `ANSI_RED`, `ANSI_RESET` en `src/check.js:15-17`) y sustituirlos por helpers del formatter (`fmt.yellow`, `fmt.red`, `fmt.ok`). Inyectar `formatterFn` opcional a `checkPendingTasks` siguiendo el mismo patrón DI que `getProviderFn`.

**Cambio de bytes esperado y aceptado** (PATTERNS finding #3 / D-10):
- Línea actual `'[kodo:check] All clear ✓'` → línea nueva `'[kodo:check] ✓ All clear'`. El símbolo `✓` queda **antes** del texto porque `fmt.ok` (Phase 14 `format.js:165`) prepend-ea el símbolo:`${OK_SYMBOL} ${pc.green(s)}`. SC#5 ("colorea OK/FAIL") no constrene la posición del símbolo. **`test/check.test.js` debe actualizarse al nuevo orden**.

**Invariante crítica** (PATTERNS Pattern C, line 184):
- `format.js` NO importa `logger.js` → `check.js → format.js` mantiene LOG-12 verde. `test/check-isolation.test.js` (walker `check.js → logger.js`) sigue ejecutándose y debe seguir pasando.

Purpose: Cubrir DX-05 + SC#5 ROADMAP §Phase 15 (color OK/FAIL en `kodo check` + LOG-12 preserved).
Output: `src/check.js` modificado (3 ANSI → helpers, +1 import, +1 dep opcional `formatterFn`); `test/check.test.js` actualizado (4 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cli-polish-wiring/15-CONTEXT.md
@.planning/phases/15-cli-polish-wiring/15-PATTERNS.md

# Source of truth — read BEFORE editing
@src/cli/format.js
@src/check.js
@test/check.test.js
@test/check-isolation.test.js

<interfaces>
<!-- API que este plan consume desde src/cli/format.js (Phase 14, read-only). -->

From src/cli/format.js:
```javascript
export function createFormatter(stream, env = process.env): Formatter
// El factory invoca _resolveUseColor(stream, env) eager. Cuando useColor=false,
// fmt.yellow/red/green/cyan/gray/dim son identity functions (return s).
// fmt.ok(s) === `${OK_SYMBOL} ${pc.green(s)}`  → símbolo embebido SIEMPRE
//   (incluso con useColor=false, queda `'✓ All clear'` sin escapes ANSI).
// fmt.fail(s) === `${FAIL_SYMBOL} ${pc.red(s)}`
```

Bytes esperados con useColor=false (golden):
- `fmt.yellow('hello')` === `'hello'`  (identity)
- `fmt.red('boom')`    === `'boom'`    (identity)
- `fmt.ok('All clear')` === `'✓ All clear'`  (símbolo embebido, sin color)
- `fmt.fail('FAIL')`    === `'✗ FAIL'`        (símbolo embebido, sin color)

Bytes esperados con useColor=true:
- `fmt.yellow('hello')` contiene `\x1b[33m...hello...\x1b[0m`
- `fmt.red('boom')`    contiene `\x1b[31m...boom...\x1b[0m`
- `fmt.ok('All clear')` contiene `'✓ '` + escape `\x1b[32m` (green) + `'All clear'` + reset
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eliminar ANSI inline + cablear formatter en src/check.js</name>
  <files>src/check.js</files>

  <read_first>
    - src/check.js (estado actual completo — los 3 ANSI literals viven en líneas 15-17, los 3 callsites en líneas 41-44, 47-49, 105)
    - src/cli/format.js (firma de createFormatter, comportamiento de fmt.yellow/red/ok)
    - .planning/phases/15-cli-polish-wiring/15-PATTERNS.md §"`src/check.js` — sustituir ANSI inline por formatter" (líneas 215-330)
    - .planning/phases/15-cli-polish-wiring/15-CONTEXT.md §decisions D-09, D-10, D-11
    - test/check-isolation.test.js (LOG-12 walker — confirmar que sigue verde)
  </read_first>

  <behavior>
    - Test 1 (TTY pending → yellow): `checkPendingTasks` con un fake provider que retorna 1 pendiente y `formatterFn: () => createFormatter({ isTTY: true })` produce una línea con `\x1b[33m` (yellow) alrededor del texto `"N pending kodo task(s), N slot(s) available"`. El prefijo `[kodo:check] ` queda fuera del color.
    - Test 2 (TTY error → red): `checkPendingTasks` con provider que lanza error + `formatterFn: () => createFormatter({ isTTY: true })` produce una línea con `\x1b[31m` (red) alrededor de `"Error checking tasks: <msg>"`.
    - Test 3 (TTY all clear → ✓ + verde): `runCheck()` con state vacío + sin pending + TTY → `summary` contiene `[kodo:check] ✓ All clear` con escape `\x1b[32m` (green) alrededor de `"All clear"`. **Nota**: el símbolo `✓` queda ANTES del texto (no después como antes).
    - Test 4 (NO_COLOR → bytes sin ANSI escapes): `checkPendingTasks` con `formatterFn: () => createFormatter({ isTTY: false })` produce líneas sin ningún `\x1b[` escape. La línea pending: `'[kodo:check] N pending kodo task(s), N slot(s) available'`. La línea error: `'[kodo:check] Error checking tasks: <msg>'`. La línea All clear: `'[kodo:check] ✓ All clear'` (símbolo presente literal pero sin escapes).
    - Test 5 (LOG-12 cross-check): `node --test test/check-isolation.test.js` exit 0 — el walker `check.js → logger.js` no encuentra el ciclo (porque `format.js` no importa logger).
  </behavior>

  <action>
    1. **Añadir import** debajo de los imports existentes (tras línea 13, antes de las constantes):
       ```javascript
       import { createFormatter } from './cli/format.js';
       ```

    2. **Eliminar las 3 líneas de ANSI inline** (líneas 15-17 actuales):
       ```javascript
       // ANTES (eliminar las 3 líneas):
       const ANSI_YELLOW = '\x1b[33m';
       const ANSI_RED = '\x1b[31m';
       const ANSI_RESET = '\x1b[0m';
       ```
       Borrar completamente. **Verificar tras editar**: `grep -c "ANSI_YELLOW\|ANSI_RED\|ANSI_RESET" src/check.js` === 0.

    3. **Extender la firma de `checkPendingTasks`** (línea 31 actual) añadiendo `formatterFn` opcional. Actualizar la JSDoc también (línea 24-30):
       ```javascript
       /**
        * Pure helper: queries the configured provider for pending tasks and returns
        * the lines/reasons to append to runCheck() output. Receives `getProviderFn`
        * for dependency injection in tests.
        *
        * @param {{
        *   config: { provider: string, claude: { max_parallel: number } },
        *   runningCount: number,
        *   getProviderFn: (name: string) => import('./interface.js').TaskProvider,
        *   formatterFn?: () => import('./cli/format.js').Formatter,
        * }} params
        * @returns {Promise<{ lines: string[], reasons: string[] }>}
        */
       export async function checkPendingTasks({ config, runningCount, getProviderFn, formatterFn }) {
         const fmt = (formatterFn || (() => createFormatter(process.stdout)))();
         const lines = [];
         const reasons = [];
         // ... resto del cuerpo
       ```
       **Stream rationale (D-11)**: `runCheckAndAct` escribe via `console.log` (línea 117) → stdout. Por eso `process.stdout` en el factory default. Tests inyectan `formatterFn: () => createFormatter({ isTTY: true })` o `{ isTTY: false }` para asertar bytes determinísticos.

    4. **Sustituir los 3 callsites ANSI por helpers del formatter**:
       - **Líneas 41-43 (pending warning)**:
         ```javascript
         // ANTES:
         lines.push(
           `${ANSI_YELLOW}[kodo:check] ${pending.length} pending kodo task(s), ${available} slot(s) available${ANSI_RESET}`,
         );
         // DESPUÉS:
         lines.push(
           `[kodo:check] ${fmt.yellow(`${pending.length} pending kodo task(s), ${available} slot(s) available`)}`,
         );
         ```
       - **Líneas 47-49 (error)**:
         ```javascript
         // ANTES:
         lines.push(
           `${ANSI_RED}[kodo:check] Error checking tasks: ${err.message}${ANSI_RESET}`,
         );
         // DESPUÉS:
         lines.push(
           `[kodo:check] ${fmt.red(`Error checking tasks: ${err.message}`)}`,
         );
         ```
       - **Línea 105 (All clear)**: el símbolo `✓` queda embebido por `fmt.ok` (Phase 14 `format.js:165`). El `✓` literal del string actual debe eliminarse o queda duplicado:
         ```javascript
         // ANTES:
         lines.push('[kodo:check] All clear ✓');
         // DESPUÉS:
         lines.push(`[kodo:check] ${fmt.ok('All clear')}`);
         ```
         **Bytes resultantes en NO_COLOR**: `'[kodo:check] ✓ All clear'` (símbolo movido a la izquierda, texto sin color). Bytes en TTY: `'[kodo:check] ✓ \x1b[32mAll clear\x1b[0m'`.

    5. **`runCheck()` también necesita un formatter** (porque la línea 105 está en `runCheck`, no en `checkPendingTasks`). Dos opciones:
       - **Opción A (recomendada, simple)**: declarar el `fmt` localmente en `runCheck` antes del primer uso (justo tras `const lines = [];` línea 63):
         ```javascript
         const fmt = createFormatter(process.stdout);
         ```
         Y usar `fmt.ok('All clear')` en línea 105 (renumerada). `checkPendingTasks` recibe el suyo via DI; `runCheck` lo crea localmente. No conflict.
       - **Opción B (DI también para runCheck)**: extender `runCheck` con un parámetro opt `{ formatterFn }`. Más DI pero más churn. **NO se hace** — `runCheck` no recibe `getProviderFn` por parámetro hoy (lo importa directamente), así que añadir `formatterFn` rompería la simetría. Opción A elegida.

       **Importante**: `checkPendingTasks` recibe su propio `fmt` via DI (paso 3); `runCheck` crea uno separado. Ambos `process.stdout`-bound. NO mezclar (no usar `fmt-de-runCheck` para escribir desde `checkPendingTasks` o viceversa — rompería la coherencia useColor/isTTY si tests inyectan diferentes streams).

       En `runCheck`, propagar el `formatterFn` a la llamada de `checkPendingTasks` NO es necesario (el formatterFn de `runCheck` es opcional via DI; en `runCheck` se crea localmente). El test que inyecta `formatterFn` en `checkPendingTasks` lo hace directamente al invocarla.

    6. **NO TOCAR**:
       - Imports existentes (`loadConfig`, `loadState`, `checkHealth`, `actOnHealth`, `initRegistry`, `getProvider`, `launchOrchestrator`).
       - El patrón "compone `lines[]`, luego `lines.join('\n')`" — no se toca; la coloración es por línea individual.
       - `runCheckAndAct` (líneas 115-127) — escribe via `console.log(result.summary)`; el summary ya viene coloreado por línea.
       - El control flow de `runCheck` — únicas modificaciones: declaración de `const fmt = createFormatter(process.stdout)` en line 63, sustitución de la línea 105.
  </action>

  <verify>
    <automated>node --test test/check.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { createFormatter } from './cli/format.js'" src/check.js` === 1
    - `grep -cE "ANSI_YELLOW|ANSI_RED|ANSI_RESET" src/check.js` === 0  (todos los ANSI inline eliminados)
    - `grep -cE "\\\\x1b\\[" src/check.js` === 0  (cero escapes ANSI literales en el código)
    - `grep -c "fmt.yellow(" src/check.js` >= 1
    - `grep -c "fmt.red(" src/check.js` >= 1
    - `grep -c "fmt.ok('All clear')" src/check.js` === 1
    - `grep -c "All clear ✓" src/check.js` === 0  (✓ literal trailing eliminado — ahora viene del helper)
    - `grep -c "formatterFn" src/check.js` >= 2  (firma + uso)
    - `node --test test/check-isolation.test.js` exit 0  (LOG-12 walker `check.js → logger.js` sigue verde)
  </acceptance_criteria>

  <done>
    `src/check.js` libre de ANSI inline. Todos los outputs coloreados pasan por `createFormatter`. `formatterFn` DI inyectable en `checkPendingTasks`. LOG-12 verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Actualizar test/check.test.js — tests 5a/5b/5c/5d (formatter + ✓ reorden + NO_COLOR golden)</name>
  <files>test/check.test.js</files>

  <read_first>
    - test/check.test.js (estado actual completo — Tests 5a y 5b en líneas 119-155 asertan `\x1b[33m` y `\x1b[31m` directamente)
    - src/cli/format.js (verificar comportamiento de fmt.yellow/fmt.red/fmt.ok con `{ isTTY: true }` vs `{ isTTY: false }`)
    - src/check.js (estado tras Task 1 — confirmar callsites de fmt.yellow/red/ok)
    - .planning/phases/15-cli-polish-wiring/15-PATTERNS.md líneas 322-326 (warning sobre el reorden de ✓)
  </read_first>

  <behavior>
    - Test 5a (TTY pending warning yellow — actualizado): `checkPendingTasks(..., formatterFn: () => createFormatter({ isTTY: true }))` con 1 pending → `result.lines.join('\n')` matches `/\x1b\[33m/`. Esencialmente el mismo assert que antes pero con `formatterFn` inyectado.
    - Test 5b (TTY error red — actualizado): `checkPendingTasks(..., formatterFn: () => createFormatter({ isTTY: true }))` con provider que throws → matches `/\x1b\[31m/`.
    - Test 5c (NUEVO, TTY All clear con ✓ + verde): assert que `runCheck()` en condiciones "all clear" produce un summary con `'[kodo:check] ✓ All clear'` Y un escape `\x1b[32m` (green) alrededor de `'All clear'`. Para invocar `runCheck` con TTY, hace falta stub `process.stdout.isTTY=true` antes de la llamada y restaurar después; o expone el `fmt` factory de runCheck (Opción A no lo hace) — alternativa: assertar sólo el orden del símbolo y el texto, omitiendo el assert de color para `runCheck` (que no acepta DI de formatter por Opción A).
    - Test 5d (NUEVO, NO_COLOR / non-TTY → cero escapes ANSI): `checkPendingTasks(..., formatterFn: () => createFormatter({ isTTY: false }))` con 1 pending → `result.lines.join('\n')` NO matches `/\x1b\[/` Y matches `/\[kodo:check\] N pending kodo task\(s\)/`. Mismo assert pattern para el error path.
    - Test 5e (NUEVO, All clear orden ✓-leading): test que invoca `runCheck()` con state vacío + provider sin pending y assert que el summary contiene literalmente `'[kodo:check] ✓ All clear'` (✓ ANTES del texto, no después).
  </behavior>

  <action>
    1. **Inspeccionar tests 5a y 5b actuales** (líneas 119-155 de `test/check.test.js`). Confirmar que asertan `\x1b[33m` y `\x1b[31m` en `result.lines.join('\n')`. Su lógica funcional pasa con o sin `formatterFn` si el factory por default `() => createFormatter(process.stdout)` resuelve `useColor=true` cuando `process.stdout.isTTY=true`. Pero los tests corren en CI con `isTTY=false`. Por eso hay que **inyectar `formatterFn` con `{ isTTY: true }` para asegurar TTY mode determinísticamente**.

    2. **Actualizar Test 5a** (línea 119):
       ```javascript
       it('Test 5a: pending output uses yellow ANSI color (via formatter, TTY)', async () => {
         const provider = createFakeProvider({
           listPendingTasks: async () => [{ id: '1', ref: 'KL-1' }],
         });
         const { createFormatter } = await import('../src/cli/format.js');
         const result = await checkPendingTasks({
           config: BASE_CONFIG,
           runningCount: 0,
           getProviderFn: () => provider,
           formatterFn: () => createFormatter({ isTTY: true }, {}),  // env={} para evitar NO_COLOR del entorno real
         });
         assert.match(
           result.lines.join('\n'),
           /\x1b\[33m/,
           'Expected yellow (\\x1b[33m) for pending warning',
         );
       });
       ```

    3. **Actualizar Test 5b** (línea 137):
       ```javascript
       it('Test 5b: error output uses red ANSI color (via formatter, TTY)', async () => {
         const provider = createFakeProvider({
           listPendingTasks: async () => { throw new Error('boom'); },
         });
         const { createFormatter } = await import('../src/cli/format.js');
         const result = await checkPendingTasks({
           config: BASE_CONFIG,
           runningCount: 0,
           getProviderFn: () => provider,
           formatterFn: () => createFormatter({ isTTY: true }, {}),
         });
         assert.match(
           result.lines.join('\n'),
           /\x1b\[31m/,
           'Expected red (\\x1b[31m) for error',
         );
       });
       ```

    4. **Añadir Test 5d (NO_COLOR golden bytes)**:
       ```javascript
       it('Test 5d: no ANSI escapes when formatter is non-TTY (NO_COLOR-equivalent)', async () => {
         const provider = createFakeProvider({
           listPendingTasks: async () => [{ id: '1', ref: 'KL-1' }],
         });
         const { createFormatter } = await import('../src/cli/format.js');
         const result = await checkPendingTasks({
           config: BASE_CONFIG,
           runningCount: 0,
           getProviderFn: () => provider,
           formatterFn: () => createFormatter({ isTTY: false }, {}),
         });
         const out = result.lines.join('\n');
         assert.doesNotMatch(out, /\x1b\[/, 'No ANSI escapes expected with isTTY=false');
         assert.match(out, /\[kodo:check\] 1 pending kodo task\(s\)/, 'Plain text shape preserved');
       });
       ```

    5. **Añadir Test 5e (All clear → ✓-leading order, sin assert de color)**:
       ```javascript
       it('Test 5e: All clear path uses fmt.ok with ✓-leading order', async () => {
         // runCheck no acepta formatterFn DI (Opción A — fmt local). Esto significa
         // que el color depende de process.stdout.isTTY del entorno test (CI: false).
         // Asserto sólo la presencia del símbolo ✓ y su orden (BEFORE the text).
         // El color (verde) está cubierto por los tests de format.test.js Phase 14.
         const result = await runCheck();
         if (result.reasons.length === 0) {
           assert.match(
             result.summary,
             /\[kodo:check\] ✓ All clear/,
             '✓ must lead "All clear" (fmt.ok prepends symbol — D-10 byte order change)',
           );
           assert.doesNotMatch(
             result.summary,
             /All clear ✓/,
             'Pre-Phase-15 trailing-✓ shape must be gone',
           );
         } else {
           // El environment actual no es "all clear" — skip this assertion.
           // Tests deterministic en runCheck() son responsabilidad de otros tests
           // que stub-ean state.json + provider; aquí sólo confirmamos el orden cuando aplica.
         }
       });
       ```
       **Nota crítica**: si `runCheck()` no es directamente invocable en tests sin más setup (state.json, provider config), reemplazar por un test sintético que arme el `summary` manualmente: importar `runCheck` y stub-ear sus dependencias, O directamente assertar el comportamiento de `fmt.ok('All clear')` (ya cubierto por `test/format.test.js` Phase 14). En ese caso, el test 5e se convierte en una verificación grep del source: `grep -c "fmt.ok('All clear')" src/check.js === 1`, ya cubierto en `acceptance_criteria`. **Decisión**: si el setup de `runCheck()` requiere mocks pesados, mover Test 5e a un grep-source assert dentro del describe de "source invariants" (ya existe en línea 158).

    6. **NO TOCAR** los tests preexistentes que NO asertan color (Tests 1, 2, 3, 4 — líneas pre-119; Test 2 source invariants — línea 158). Siguen verdes porque la lógica de `checkPendingTasks` y `runCheck` no cambia funcionalmente.
  </action>

  <verify>
    <automated>node --test test/check.test.js</automated>
  </verify>

  <acceptance_criteria>
    - Tests 5a y 5b actualizados con `formatterFn` inyectado y `{ isTTY: true }` — ambos pasan.
    - Test 5d nuevo presente y verde (NO_COLOR golden, cero escapes ANSI, shape plain text preservado).
    - Test 5e (o equivalente grep-source) presente y verde (orden ✓-leading documentado).
    - `node --test test/check.test.js` exit 0 — TODOS los tests verdes incluidos los preexistentes.
    - `grep -c "All clear ✓" test/check.test.js` === 0  (asserts pre-Phase-15 con orden trailing eliminados; si quedan, hay que actualizar)
    - `grep -c "✓ All clear" test/check.test.js` >= 1  (orden nuevo asertado al menos una vez)
  </acceptance_criteria>

  <done>
    Tests de `check.test.js` consistentes con la implementación post-Phase-15. Tests 5a/5b funcionan con `formatterFn` inyectado. Tests 5d/5e cubren los nuevos casos (NO_COLOR golden + ✓-leading order).
  </done>
</task>

</tasks>

<verification>
- `node --test test/check.test.js test/check-isolation.test.js` exit 0.
- En TTY real: `KODO_LOG_LEVEL=info kodo check` muestra warnings amarillos, errores rojos, "All clear" con ✓ verde.
- En NO_COLOR: `NO_COLOR=1 kodo check` produce bytes sin escapes ANSI (símbolo ✓ presente literal).
- LOG-12 walker (`test/check-isolation.test.js`) sigue verde.
</verification>

<success_criteria>
DX-05 cubierto. SC#5 ROADMAP §15: `kodo check` colorea OK/FAIL en su tabla y NO carga `src/logger.js` transitivamente. Test-graph guard LOG-12 verde tras los cambios.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/15-cli-polish-wiring/15-02-SUMMARY.md` con: archivos modificados, líneas tocadas, ANSI inline eliminados, callsites migrados, LOG-12 verde, cambio de orden ✓ documentado.
</output>
