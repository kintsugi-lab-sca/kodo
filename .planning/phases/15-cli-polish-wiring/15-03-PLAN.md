---
phase: 15
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/gsd-inspect.js
  - test/gsd-inspect-cli.test.js
autonomous: true
requirements: [DX-03]
user_setup: []

must_haves:
  truths:
    - "`kodo gsd inspect <task-id>` en human mode muestra 4 secciones literales etiquetadas `config`, `fetch`, `roadmap`, `match` (en ese orden), cada una prefijada con `✓ OK` o `✗ FAIL` via `fmt.ok`/`fmt.fail`."
    - "Las secciones `config` y `fetch` siempre muestran `✓ OK` cuando se llega a `renderHuman` — los fallos previos hacen `return 1` o `return 2` antes via `errFn` (PATTERNS finding #4)."
    - "Las únicas dos secciones con `✗ FAIL` posible en human mode son: `roadmap` (cuando `.planning/PROJECT.md` no existe) y `match` (cuando `verdict.action === 'error'`)."
    - "El handler imprime `Exit: N` como última línea (antes del return) en TODAS las ramas que pasan por `renderHuman`, donde N es el code que el proceso retorna (0 para phase|bootstrap, 1 para verdict error)."
    - "Exit codes D-19 NO cambian: 0=phase|bootstrap, 1=verdict-error|config-error, 2=fetch-failure. La línea `Exit: N` es cosmética; el N visible siempre coincide con el N retornado."
    - "`opts.json` mode no se altera — sigue serializando el mismo shape `{ task, project_path, has_planning_dir, verdict, brief }`. La línea `Exit: N` sólo aparece en human mode."
  artifacts:
    - path: "src/cli/gsd-inspect.js"
      provides: "renderHuman refactor a 4 secciones (D-12) + Exit: N visible (D-13) + formatterFn DI"
      contains: "import { createFormatter } from './format.js'"
      min_lines: 165
    - path: "test/gsd-inspect-cli.test.js"
      provides: "Tests actualizados al nuevo shape de 4 secciones + assert Exit: N coincide con return code"
  key_links:
    - from: "src/cli/gsd-inspect.js"
      to: "src/cli/format.js"
      via: "import { createFormatter }"
      pattern: "from '\\./format\\.js'"
    - from: "renderHuman"
      to: "fmt.ok / fmt.fail"
      via: "render por sección con símbolos embebidos"
      pattern: "fmt\\.(ok|fail)\\("
    - from: "Exit: N visible line"
      to: "verdict.action === 'error' ? 1 : 0"
      via: "exitCode declared once and used both for write() and return"
      pattern: "Exit: \\$\\{exitCode\\}"
---

<objective>
Refactorizar `src/cli/gsd-inspect.js#renderHuman` para presentar el verdict del resolver en **4 secciones literales** (`config`, `fetch`, `roadmap`, `match`) con símbolos `✓`/`✗` por sección via `fmt.ok`/`fmt.fail` (D-12), e imprimir `Exit: N` como última línea (D-13) coincidente con el código de retorno del handler. Añadir `formatterFn` DI siguiendo el mismo patrón que `writeFn`/`errFn`/`getProviderFn`.

**Invariantes preservadas**:
- Exit codes D-19 (Phase 9) NO cambian: 0=phase|bootstrap, 1=verdict-error|config-error, 2=fetch-failure.
- `opts.json` shape no cambia (D-17).
- `runGsdInspect` sigue siendo pure read-only (D-18 dry-run): no lock, no state, no cmux.
- El orden de ejecución del handler (fetch primero, luego config, luego resolver, luego roadmap-check) NO cambia. Sólo se reorganiza el render para que coincida con el orden literal del SC (`config / fetch / roadmap / match`) — datos pre-computados ya disponibles cuando se llama `renderHuman`.

**Decisión clave** (PATTERNS finding #4 / D-12 análisis):
- Las secciones `config` y `fetch` SIEMPRE renderizan `✓ OK` en `renderHuman` porque cualquier fallo previo (`resolveProjectPath` throws → return 1, `provider.getTask` throws → return 2) va por `errFn` y NO llega al render. Esto es deliberado: el operador ve `✓ OK` para esas dos secciones cuando el `kodo gsd inspect` llega al render. **No se inyectan flags `configOk`/`fetchOk` en el renderHuman** — quedaría innecesariamente acoplado.
- Para los paths que retornan 1 o 2 sin pasar por `renderHuman`, el `Exit: N` también se imprime (consistencia operador, recomendación PATTERNS línea 427).

Purpose: Cubrir DX-03 + SC#3 ROADMAP §Phase 15 (verdict con ✓/✗ por sección + exit code visible).
Output: `src/cli/gsd-inspect.js` con renderHuman refactorizado + `Exit: N` consistente; `test/gsd-inspect-cli.test.js` actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cli-polish-wiring/15-CONTEXT.md
@.planning/phases/15-cli-polish-wiring/15-PATTERNS.md

# Source of truth — read BEFORE editing
@src/cli/format.js
@src/cli/gsd-inspect.js
@test/gsd-inspect-cli.test.js

<interfaces>
<!-- API que este plan consume desde src/cli/format.js (Phase 14, read-only). -->

From src/cli/format.js:
```javascript
export function createFormatter(stream, env = process.env): Formatter
// Formatter helpers usados aquí:
//   fmt.ok(s)   === `${OK_SYMBOL} ${pc.green(s)}`   → '✓ OK' en TTY (verde) / '✓ OK' en NO_COLOR (sin color)
//   fmt.fail(s) === `${FAIL_SYMBOL} ${pc.red(s)}`   → '✗ FAIL' en TTY (rojo) / '✗ FAIL' en NO_COLOR
```

Verdict shape (de src/gsd/resolver.js — solo lectura):
- `{ action: 'phase', phase_id, match_heading, match_reason }`
- `{ action: 'bootstrap', reason }`
- `{ action: 'error', code, detail?, matches? }`

D-19 exit codes (Phase 9, NO se alteran):
- 0 → verdict.action === 'phase' | 'bootstrap'
- 1 → verdict.action === 'error' OR resolveProjectPath throws (config error)
- 2 → provider.getTask throws (transient fetch failure)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor renderHuman a 4 secciones + Exit: N (src/cli/gsd-inspect.js)</name>
  <files>src/cli/gsd-inspect.js</files>

  <read_first>
    - src/cli/gsd-inspect.js (estado actual completo — render actual en líneas 116-164, exit return en línea 109, error paths en líneas 68-70 y 80-82)
    - src/cli/format.js (firma de createFormatter, comportamiento de fmt.ok / fmt.fail)
    - .planning/phases/15-cli-polish-wiring/15-PATTERNS.md §"`src/cli/gsd-inspect.js` — refactor renderHuman" (líneas 334-432)
    - .planning/phases/15-cli-polish-wiring/15-CONTEXT.md §decisions D-12, D-13 + Discretion línea 77 (orden literal SC)
  </read_first>

  <behavior>
    - Test 1 (verdict pass / phase): output human contiene 4 líneas en orden literal: `config:  ✓ OK`, `fetch:   ✓ OK`, `roadmap: ✓ OK`, `match:   ✓ OK — phase <id>`. Última línea: `Exit: 0`.
    - Test 2 (verdict bootstrap): mismo shape pero línea match es `match:   ✓ OK — bootstrap (<reason>)`. Sigue terminando en `Exit: 0`.
    - Test 3 (verdict error): mismas 3 primeras líneas con `✓ OK`; línea match es `match:   ✗ FAIL — <code>[: <detail>]`. Última línea: `Exit: 1`.
    - Test 4 (roadmap missing): cuando `.planning/PROJECT.md` no existe, `roadmap: ✗ FAIL` aparece. El verdict puede seguir siendo phase/bootstrap/error según el resolver. **Caso típico**: roadmap missing + verdict error (el resolver se queja de no encontrar el doc).
    - Test 5 (resolveProjectPath throws → return 1, NO pasa por renderHuman): el error message va a `errFn`; el output a stdout (`writeFn`) sigue siendo... (decidir abajo si imprimimos `Exit: 1` también en este path).
    - Test 6 (provider.getTask throws → return 2, NO pasa por renderHuman): error message a `errFn`; mismo dilema que Test 5.
    - Test 7 (--json mode): sigue produciendo el shape `{ task, project_path, has_planning_dir, verdict, brief }` — NO se añade ninguna key nueva. Sin línea `Exit: N` en JSON output (sería breaking change para consumers).
    - Test 8 (Exit: N consistencia): para los 3 verdicts que pasan por renderHuman (phase / bootstrap / error), assert que el N de la línea `Exit: N` es exactamente el N que el handler retorna.
  </behavior>

  <action>
    1. **Añadir import** debajo de los imports existentes (tras línea 26):
       ```javascript
       import { createFormatter } from './format.js';
       ```

    2. **Extender la JSDoc de `RunGsdInspectDeps`** (líneas 31-37) con `formatterFn`:
       ```javascript
       /**
        * @typedef {{
        *   getProviderFn?: () => import('../interface.js').TaskProvider,
        *   resolveProjectPathFn?: (task: any) => string,
        *   resolvePhaseFn?: (params: { projectPath: string, task: any }) => import('../gsd/resolver.js').ResolveResult,
        *   writeFn?: (s: string) => void,
        *   errFn?: (s: string) => void,
        *   formatterFn?: () => import('./format.js').Formatter,
        * }} RunGsdInspectDeps
        */
       ```

    3. **Inicializar `fmt` en `runGsdInspect`** (después de los DI defaults, tras línea 61):
       ```javascript
       const fmt = (deps.formatterFn || (() => createFormatter(process.stdout)))();
       ```

    4. **Cambiar el flujo de error paths para imprimir `Exit: N` antes de return** (consistencia operador, PATTERNS línea 427 recomendación (a)):
       - **Línea 68-70 actual** (fetch fail → return 2):
         ```javascript
         } catch (e) {
           err(`Error fetching task ${opts.taskId}: ${/** @type {Error} */ (e).message}\n`);
           write(`Exit: 2\n`);  // NEW (D-13 consistency — visible in stdout)
           return 2;
         }
         ```
       - **Línea 80-82 actual** (config fail → return 1):
         ```javascript
         } catch (e) {
           err(`Error resolving project path: ${/** @type {Error} */ (e).message}\n`);
           write(`Exit: 1\n`);  // NEW (D-13 consistency)
           return 1;
         }
         ```
       **Nota**: en `opts.json` mode el `errFn` se invoca por la excepción, pero el `Exit: N` se escribe a `writeFn` (stdout). En `--json` esto añadiría texto plano al stdout que no es JSON válido. Para evitar romper consumers de `--json`, **no imprimir `Exit: N` en estos paths cuando `opts.json` está set**:
       ```javascript
       } catch (e) {
         err(`Error fetching task ${opts.taskId}: ${...}\n`);
         if (!opts.json) write(`Exit: 2\n`);
         return 2;
       }
       ```
       Mismo patrón para el catch de `resolveProjectPathFn`.

    5. **Refactor `renderHuman` (líneas 116-164)** al nuevo shape de 4 secciones:
       ```javascript
       /**
        * Render the human-readable 4-section report per D-12.
        * Exit: N appended as last line per D-13.
        * @private
        */
       function renderHuman({ task, projectPath, hasPlanning, verdict, brief, write, fmt, exitCode }) {
         // Header (info de contexto, sin OK/FAIL — son hechos)
         write(`Task:         ${task.ref} — ${task.title}\n`);
         write(`Labels:       [${(task.labels || []).join(', ')}]\n`);
         write(`Project path: ${projectPath}\n\n`);

         // 4 secciones literales del SC#3 — orden: config / fetch / roadmap / match.
         // config y fetch siempre OK aquí (cualquier fallo previo retornó antes via errFn).
         write(`config:  ${fmt.ok('OK')}\n`);
         write(`fetch:   ${fmt.ok('OK')}\n`);
         write(`roadmap: ${hasPlanning ? fmt.ok('OK') : fmt.fail('FAIL')}\n`);

         const matchOk = verdict.action !== 'error';
         let matchLine = `match:   ${matchOk ? fmt.ok('OK') : fmt.fail('FAIL')}`;
         if (verdict.action === 'phase') {
           matchLine += ` — phase ${verdict.phase_id}`;
         } else if (verdict.action === 'bootstrap') {
           matchLine += ` — bootstrap (${verdict.reason})`;
         } else if (verdict.action === 'error') {
           matchLine += ` — ${verdict.code}${verdict.detail ? `: ${verdict.detail}` : ''}`;
         }
         write(`${matchLine}\n\n`);

         // Bloque opcional: preview de buildGsdContext sólo para bootstrap (CONTEXT línea 70).
         if (verdict.action === 'bootstrap' && brief) {
           const syntheticSession = /** @type {any} */ ({
             task_ref: task.ref,
             summary: task.title,
             project_path: projectPath,
             session_id: '<dry-run-preview>',
             task_id: task.id,
             project_id: task.projectId,
             gsd: true,
           });
           const preview = buildGsdContext(syntheticSession, { brief });
           write('─── buildGsdContext preview ───\n');
           write(preview);
           if (!preview.endsWith('\n')) write('\n');
           write('───────────────────────────────\n');
         }

         // Última línea: Exit: N (D-13) — el N coincide con el return del handler.
         write(`Exit: ${exitCode}\n`);
       }
       ```

    6. **Actualizar la invocación de `renderHuman`** (líneas 104-106 actuales) para pasar `fmt` y `exitCode`:
       ```javascript
       // 7. Exit code (D-19) — calcular antes de renderizar para que renderHuman lo imprima.
       const exitCode = verdict.action === 'error' ? 1 : 0;

       // 6. Render: --json (D-17) or human (D-12, D-13).
       if (opts.json) {
         write(JSON.stringify({
           task: { ref: task.ref, title: task.title, labels: task.labels },
           project_path: projectPath,
           has_planning_dir: hasPlanning,
           verdict,
           brief,
         }, null, 2) + '\n');
       } else {
         renderHuman({ task, projectPath, hasPlanning, verdict, brief, write, fmt, exitCode });
       }

       return exitCode;
       ```

    7. **NO TOCAR**:
       - `runGsdInspect` el resto del control flow (líneas 50-92): fetch task, resolver project path, llamar resolvePhase, computar `hasPlanning`, computar `brief`.
       - `opts.json` mode: el shape `{ task, project_path, has_planning_dir, verdict, brief }` no cambia.
       - Imports existentes (`existsSync`, `join`, `resolvePhase`, `buildBriefFromTask`, `buildGsdContext`, `resolveProjectPath`, `loadProjects`, `initRegistry`, `getProvider`).
       - El bloque preview de `buildGsdContext` para bootstrap se conserva (Discretion CONTEXT línea 70 — útil para auditoría).
  </action>

  <verify>
    <automated>node --test test/gsd-inspect-cli.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { createFormatter } from './format.js'" src/cli/gsd-inspect.js` === 1
    - `grep -c "formatterFn" src/cli/gsd-inspect.js` >= 2
    - `grep -c "fmt.ok('OK')" src/cli/gsd-inspect.js` >= 3  (config + fetch + roadmap-true + match-true paths)
    - `grep -c "fmt.fail('FAIL')" src/cli/gsd-inspect.js` >= 2  (roadmap-false + match-error paths)
    - `grep -cE "config:\\s+\\$\\{" src/cli/gsd-inspect.js` >= 1  (sección literal `config:`)
    - `grep -cE "fetch:\\s+\\$\\{" src/cli/gsd-inspect.js` >= 1
    - `grep -cE "roadmap:\\s+\\$\\{" src/cli/gsd-inspect.js` >= 1
    - `grep -cE "match:\\s+\\$\\{" src/cli/gsd-inspect.js` >= 1
    - `grep -c "Exit: \${exitCode}" src/cli/gsd-inspect.js` >= 1
    - `grep -c "if (!opts.json) write(\`Exit:" src/cli/gsd-inspect.js` >= 2  (Exit: en error paths, suprimido en json mode)
    - `grep -c "Section 4: preview\\|action:        phase\\|action:        bootstrap\\|action:        error" src/cli/gsd-inspect.js` === 0  (shape antiguo eliminado)
  </acceptance_criteria>

  <done>
    `renderHuman` imprime 4 secciones literales con ✓/✗ por sección. `Exit: N` aparece como última línea en human mode (incluyendo error paths con `if (!opts.json)` guard). Exit codes D-19 invariantes. JSON shape inalterado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Actualizar test/gsd-inspect-cli.test.js — 4 secciones + Exit: N consistency</name>
  <files>test/gsd-inspect-cli.test.js</files>

  <read_first>
    - test/gsd-inspect-cli.test.js (estado actual completo — entender qué asserts dependen del shape antiguo `Section 1:`/`Verdict:`/`action: phase`)
    - src/cli/gsd-inspect.js (estado tras Task 1 — confirmar el shape final del output)
    - src/cli/format.js (comportamiento de fmt.ok / fmt.fail con `{ isTTY: false }`: `'✓ OK'` y `'✗ FAIL'` literal sin color)
  </read_first>

  <behavior>
    - Test 1 (verdict phase, NO_COLOR): captura el output via `writeFn` mock y asserta que contiene exactamente las 4 líneas en orden (`config:  ✓ OK`, `fetch:   ✓ OK`, `roadmap: ✓ OK`, `match:   ✓ OK — phase <id>`) y termina con `Exit: 0\n`.
    - Test 2 (verdict bootstrap): assert linea match contiene `bootstrap (<reason>)`, `Exit: 0`, y el bloque preview de `buildGsdContext` está presente.
    - Test 3 (verdict error): assert linea match contiene `✗ FAIL — <code>`, `Exit: 1`. Bloque preview NO presente.
    - Test 4 (roadmap missing — `hasPlanning=false`): assert linea roadmap es `roadmap: ✗ FAIL`. El N depende del verdict.
    - Test 5 (resolveProjectPath throws → return 1): writeFn captura recibe `Exit: 1\n` (cuando `opts.json` no está set) Y el handler retorna 1.
    - Test 6 (provider.getTask throws → return 2): writeFn captura recibe `Exit: 2\n` (cuando `opts.json` no está set) Y el handler retorna 2.
    - Test 7 (--json mode preservado): el output a writeFn es JSON parsable, contiene exactamente las keys `{ task, project_path, has_planning_dir, verdict, brief }`, sin línea `Exit: N` adicional.
    - Test 8 (Exit: N consistencia): para los 3 verdicts (phase/bootstrap/error), el N de la línea `Exit: N` === el N del return del handler.
  </behavior>

  <action>
    1. **Inspeccionar tests actuales**. Identificar qué tests asertan el shape antiguo (`Verdict:`, `action: phase`, `Section 1:`, `.planning/PROJECT.md: present`, etc.). Estos tests deben actualizarse al nuevo shape de 4 secciones.

    2. **Para cada test que aserte el shape de output**:
       - Sustituir asserts del tipo `assert.match(output, /action:\s+phase/)` por `assert.match(output, /match:\s+✓ OK — phase /)`.
       - Sustituir `assert.match(output, /\.planning\/PROJECT\.md: present/)` por `assert.match(output, /roadmap:\s+✓ OK/)`.
       - Sustituir `assert.match(output, /\.planning\/PROJECT\.md: MISSING/)` por `assert.match(output, /roadmap:\s+✗ FAIL/)`.
       - Para verdict error: `assert.match(output, /action:\s+error/)` → `assert.match(output, /match:\s+✗ FAIL/)`.
       - Pasar `formatterFn: () => createFormatter({ isTTY: false }, {})` en deps cuando los tests quieran asertar bytes plain (NO_COLOR mode). Importar `createFormatter` desde `'../src/cli/format.js'`.

    3. **Añadir Test 8 nuevo (Exit: N consistencia)** — un test parameterizado que cubre los 5 escenarios D-19:
       ```javascript
       it('Test 8: Exit: N visible coincide con el return del handler', async () => {
         const { createFormatter } = await import('../src/cli/format.js');
         const cases = [
           // verdict phase → exit 0
           {
             name: 'phase',
             verdict: { action: 'phase', phase_id: '01', match_heading: 'X', match_reason: 'Y' },
             hasPlanning: true,
             expectedExit: 0,
           },
           // verdict bootstrap → exit 0
           {
             name: 'bootstrap',
             verdict: { action: 'bootstrap', reason: 'no-phase-id-yet' },
             hasPlanning: true,
             expectedExit: 0,
           },
           // verdict error → exit 1
           {
             name: 'error',
             verdict: { action: 'error', code: 'no-match', detail: 'no heading matches' },
             hasPlanning: true,
             expectedExit: 1,
           },
         ];
         for (const tc of cases) {
           let output = '';
           const writeFn = (s) => { output += s; };
           const errFn = () => {};
           const fakeTask = { id: 't1', ref: 'KL-1', title: 'X', labels: [], projectId: 'p1' };
           const code = await runGsdInspect(
             { taskId: 't1', json: false },
             {
               getProviderFn: () => ({ init: async () => {}, getTask: async () => fakeTask, /* ... */ }),
               resolveProjectPathFn: () => '/tmp/dummy',
               resolvePhaseFn: () => tc.verdict,
               writeFn,
               errFn,
               formatterFn: () => createFormatter({ isTTY: false }, {}),
             },
           );
           assert.equal(code, tc.expectedExit, `${tc.name}: return code mismatch`);
           assert.match(output, new RegExp(`Exit: ${tc.expectedExit}\\n$`), `${tc.name}: visible Exit must equal return code`);
         }
       });
       ```
       **Nota**: el setup exacto de `getProviderFn` / `resolveProjectPathFn` / `resolvePhaseFn` depende del contrato actual del handler — si `existsSync` para `.planning/PROJECT.md` se llama directamente (línea 89 de gsd-inspect.js actual: `existsSync(join(projectPath, '.planning', 'PROJECT.md'))`), ese check NO está mockeado y depende del filesystem. **Solución**: el test corre con `projectPath` apuntando a un dir tmp existente con (o sin) un PROJECT.md según el case requiera.

    4. **Añadir Test 5/6 (error paths con `Exit: N`)**:
       ```javascript
       it('Test 5: Exit: 1 visible cuando resolveProjectPath throws (config error)', async () => {
         let output = '';
         const writeFn = (s) => { output += s; };
         const errFn = () => {};
         const code = await runGsdInspect(
           { taskId: 't1', json: false },
           {
             getProviderFn: () => ({ init: async () => {}, getTask: async () => ({ id: 't1', ref: 'KL-1', title: 'X', labels: [] }) }),
             resolveProjectPathFn: () => { throw new Error('no project mapping'); },
             writeFn,
             errFn,
           },
         );
         assert.equal(code, 1);
         assert.match(output, /Exit: 1\n$/);
       });

       it('Test 6: Exit: 2 visible cuando provider.getTask throws (fetch failure)', async () => {
         let output = '';
         const writeFn = (s) => { output += s; };
         const errFn = () => {};
         const code = await runGsdInspect(
           { taskId: 't1', json: false },
           {
             getProviderFn: () => ({ init: async () => {}, getTask: async () => { throw new Error('ECONNREFUSED'); } }),
             writeFn,
             errFn,
           },
         );
         assert.equal(code, 2);
         assert.match(output, /Exit: 2\n$/);
       });

       it('Test 5b: Exit: N suprimido en json mode error paths', async () => {
         let output = '';
         const writeFn = (s) => { output += s; };
         const errFn = () => {};
         const code = await runGsdInspect(
           { taskId: 't1', json: true },  // json=true → no Exit: N en stdout
           {
             getProviderFn: () => ({ init: async () => {}, getTask: async () => { throw new Error('boom'); } }),
             writeFn,
             errFn,
           },
         );
         assert.equal(code, 2);
         assert.doesNotMatch(output, /Exit: \d/, 'Exit: N must be suppressed in --json mode');
       });
       ```

    5. **Test 7 (--json shape preservado)** — si no existe ya, añadir:
       ```javascript
       it('Test 7: --json mode no incluye Exit ni 4-section text', async () => {
         let output = '';
         const writeFn = (s) => { output += s; };
         const code = await runGsdInspect(
           { taskId: 't1', json: true },
           { /* setup happy path */ },
         );
         const parsed = JSON.parse(output);
         assert.deepEqual(Object.keys(parsed).sort(), ['brief', 'has_planning_dir', 'project_path', 'task', 'verdict']);
         assert.doesNotMatch(output, /config:\s+✓/);
         assert.doesNotMatch(output, /Exit:/);
       });
       ```

    6. **NO TOCAR** los tests de invariantes (D-18 dry-run: no lock, no state, no cmux) — siguen verdes porque el control flow del handler no cambia.
  </action>

  <verify>
    <automated>node --test test/gsd-inspect-cli.test.js</automated>
  </verify>

  <acceptance_criteria>
    - Tests existentes adaptados al nuevo shape de 4 secciones (no quedan asserts contra `action:\s+phase` u otros del shape antiguo).
    - Test 5 nuevo presente y verde (Exit: 1 visible cuando config error).
    - Test 6 nuevo presente y verde (Exit: 2 visible cuando fetch failure).
    - Test 5b nuevo presente y verde (Exit: N suprimido en json mode error paths).
    - Test 7 nuevo (o equivalente) presente y verde (--json shape inalterado).
    - Test 8 nuevo presente y verde (Exit: N === return code para los 3 verdicts).
    - `node --test test/gsd-inspect-cli.test.js` exit 0.
    - `grep -c "config:\\s\\+✓\\|fetch:\\s\\+✓\\|roadmap:\\s\\+✓\\|match:\\s\\+✓" test/gsd-inspect-cli.test.js` >= 4 (los 4 labels asertados al menos una vez).
    - `grep -c "Exit: \\d" test/gsd-inspect-cli.test.js` >= 4 (al menos 4 asserts de Exit: N).
  </acceptance_criteria>

  <done>
    Tests reflejan el nuevo shape de 4 secciones. `Exit: N` consistency cubierto en los 3 verdicts + 2 error paths + json suppression. `node --test test/gsd-inspect-cli.test.js` verde.
  </done>
</task>

</tasks>

<verification>
- `node --test test/gsd-inspect-cli.test.js` exit 0.
- Manual: `kodo gsd inspect <task-id>` muestra 4 secciones (`config / fetch / roadmap / match`) con ✓/✗ y termina con `Exit: 0` (o el código correspondiente).
- Manual: `kodo gsd inspect <task-id-with-fetch-error>` retorna 2 y stdout muestra `Exit: 2`.
- Manual: `kodo gsd inspect --json <task-id>` produce JSON sin línea `Exit: N`.
- Exit codes D-19 (Phase 9) verificados: 0=phase|bootstrap, 1=verdict-error|config-error, 2=fetch-failure.
</verification>

<success_criteria>
DX-03 cubierto. SC#3 ROADMAP §15: verdict del resolver con símbolos ✓/✗ por sección + exit code visible al final del output. Semántica D-19 invariante.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/15-cli-polish-wiring/15-03-SUMMARY.md` con: archivos modificados, líneas tocadas, 4 secciones implementadas, Exit: N consistency verificado, exit codes D-19 inalterados, json shape inalterado.
</output>
