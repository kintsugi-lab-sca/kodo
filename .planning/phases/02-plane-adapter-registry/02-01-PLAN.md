---
phase: 02-plane-adapter-registry
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/providers/plane/normalize.js
  - test/fixtures/plane-workitem.json
  - test/fixtures/plane-webhook.json
  - test/fixtures/plane-labels.json
  - test/normalize.test.js
autonomous: true
requirements:
  - PLAN-02
  - PLAN-03
  - PLAN-05
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "A raw Plane API work item converts to a canonical TaskItem with plain text description, string labels, and constructed URL"
    - "A Plane webhook payload parses to a TriggerEvent with taskRef, action, provider='plane', and kodoConfig in raw"
    - "Label UUIDs resolve to human-readable names using a cached label map"
    - "Fixtures represent real Plane API response shapes"
  artifacts:
    - path: "src/providers/plane/normalize.js"
      provides: "normalizeWorkItem, parseTriggerEvent, stripHtml, resolveWorkItemLabels"
      exports: ["normalizeWorkItem", "parseTriggerEvent", "stripHtml", "resolveWorkItemLabels"]
    - path: "test/fixtures/plane-workitem.json"
      provides: "Real Plane API work item response fixture"
    - path: "test/fixtures/plane-webhook.json"
      provides: "Real Plane webhook payload fixture"
    - path: "test/fixtures/plane-labels.json"
      provides: "Project labels array fixture"
    - path: "test/normalize.test.js"
      provides: "Tests for normalization and label parsing"
  key_links:
    - from: "src/providers/plane/normalize.js"
      to: "src/interface.js"
      via: "JSDoc @returns import"
      pattern: "import.*interface\\.js"
    - from: "src/providers/plane/normalize.js"
      to: "src/labels.js"
      via: "parseKodoLabels import for kodo label config extraction"
      pattern: "import.*parseKodoLabels.*labels\\.js"
---

<objective>
Create the Plane normalizer module with pure functions that convert raw Plane API responses to canonical TaskItem and TriggerEvent shapes. Test-driven with fixture JSON files from real Plane API responses.

Purpose: The normalizer is the core data transformation layer — every other adapter component depends on it producing correct canonical shapes. Pure functions make it trivially testable.
Output: `src/providers/plane/normalize.js` with all normalization functions, fixtures, and passing tests.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/interface.js
@src/labels.js
@src/server.js
@src/plane/client.js

<interfaces>
<!-- From src/interface.js — canonical shapes the normalizer must produce -->

```javascript
/** @typedef {{
 *   id: string, ref: string, title: string, description: string,
 *   labels: string[], projectId: string, projectName: string,
 *   groups: string[], url: string,
 *   priority: 'urgent'|'high'|'medium'|'low'|'none'|null,
 * }} TaskItem */

/** @typedef {{
 *   taskRef: string, action: string, provider: string, raw: object,
 * }} TriggerEvent */

export const TASK_PROVIDER_METHODS = Object.freeze([...]);
export const VALID_PRIORITIES = Object.freeze(['urgent','high','medium','low','none']);
```

<!-- From src/labels.js — reuse parseKodoLabels for kodo config extraction -->
```javascript
export function parseKodoLabels(labels) → { isKodo, model, flags }
export async function resolveLabels(plane, projectId, labels) → Array<{name, id}>
```

<!-- From src/server.js lines 51-130 — webhook handling logic to extract -->
```javascript
// handleWebhook extracts: event (issue/work_item), action, data
// handleTriggerState extracts: state_detail.name, project_detail, sequence_id, labels
// Labels in webhook come as UUIDs — need resolution from cached label map
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create fixtures and normalizer tests (RED)</name>
  <files>test/fixtures/plane-workitem.json, test/fixtures/plane-webhook.json, test/fixtures/plane-labels.json, test/normalize.test.js</files>
  <behavior>
    - normalizeWorkItem: given a Plane work item fixture + label context, returns TaskItem with:
      - ref = "{projectIdentifier}-{sequence_id}" (e.g. "KL-42")
      - description = plain text (HTML stripped)
      - labels = string array of names (not UUIDs)
      - url = "{baseUrl}/{workspaceSlug}/browse/{ref}"
      - priority = one of VALID_PRIORITIES or null
      - projectName from project_detail.name
    - normalizeWorkItem edge cases: missing description_html returns "", missing labels returns [], missing priority returns null
    - parseTriggerEvent: given a webhook payload fixture, returns TriggerEvent with:
      - taskRef = "{identifier}-{sequence_id}"
      - action = webhook action string
      - provider = "plane"
      - raw contains kodoConfig from parseKodoLabels (using cached resolved labels)
    - parseTriggerEvent: returns null if event is not "issue" or "work_item"
    - resolveWorkItemLabels: given UUID array + labels map, returns name strings
    - resolveWorkItemLabels: given already-resolved object labels, returns names
    - stripHtml: converts HTML to plain text, collapses whitespace
  </behavior>
  <action>
    1. Create `test/fixtures/plane-workitem.json` — a realistic Plane API work item response. Include fields: id (UUID), name, description_html (with HTML tags), state (UUID), state_detail ({id, name, group}), priority ("medium"), labels (array of UUIDs), project (UUID), project_detail ({id, name, identifier: "KL"}), sequence_id (42), and other realistic fields. Base on the Plane webhook payload structure from RESEARCH.md.

    2. Create `test/fixtures/plane-webhook.json` — a realistic Plane webhook payload with structure: { event: "issue", action: "updated", webhook_id, workspace_id, data: {work item fields like above, with labels as UUIDs} }.

    3. Create `test/fixtures/plane-labels.json` — array of label objects: [{id: "uuid-matching-fixture", name: "kodo"}, {id: "uuid-2", name: "kodo:sonnet"}, {id: "uuid-3", name: "bug"}, {id: "uuid-4", name: "feature"}]. Ensure the UUIDs match those used in the work item and webhook fixtures.

    4. Create `test/normalize.test.js` with tests for:
       - `normalizeWorkItem(fixture, context)` produces correct TaskItem shape
       - HTML stripping in description
       - Label UUIDs resolved to names via context.labels
       - URL construction
       - Priority mapping
       - Edge cases: null/undefined description_html, empty labels, missing priority
       - `parseTriggerEvent(webhookFixture, labelCache)` produces correct TriggerEvent
       - parseTriggerEvent returns null for unknown event types
       - `resolveWorkItemLabels(uuids, labelsMap)` returns string names
       - `stripHtml(html)` returns plain text

    Import from `../src/providers/plane/normalize.js`. Tests MUST fail (module doesn't exist yet).
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/normalize.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>Fixtures contain realistic Plane API data. Tests import from normalize.js and fail because the module does not exist. All behavior expectations are encoded as test assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement normalizer (GREEN)</name>
  <files>src/providers/plane/normalize.js</files>
  <action>
    Create `src/providers/plane/normalize.js` with these exports:

    1. `stripHtml(html)` — pure function. Replaces HTML tags with empty string, collapses whitespace, trims. Adapted from the existing `stripHtml` in `src/session/manager.js` (line ~120): `html.replace(/<[^>]+>/g, '').replace(/\s+/g, ' ').trim()`. Handle null/undefined input → return "".

    2. `resolveWorkItemLabels(labelIds, labelsMap)` — pure function. Takes array of label IDs (strings/UUIDs) or label objects, plus an array of {id, name} objects. Returns string[] of label names. Handle both formats: if labels[0] is an object with .name, extract names directly. If labels are strings (UUIDs), look up each in labelsMap by id. Return empty array for null/empty input.

    3. `normalizeWorkItem(workItem, context)` — pure function.
       - context shape: `{ labels: Array<{id,name}>, projectIdentifier: string, baseUrl: string, workspaceSlug: string }`
       - Returns `TaskItem` with:
         - id: workItem.id
         - ref: `${context.projectIdentifier}-${workItem.sequence_id}`
         - title: workItem.name
         - description: stripHtml(workItem.description_html || '')
         - labels: resolveWorkItemLabels(workItem.labels, context.labels)
         - projectId: workItem.project_detail?.id || workItem.project
         - projectName: workItem.project_detail?.name || ''
         - groups: [] (populated later via getWorkItemModule in provider)
         - url: `${context.baseUrl}/${context.workspaceSlug}/browse/${ref}`
         - priority: workItem.priority if in VALID_PRIORITIES, else null

    4. `parseTriggerEvent(rawPayload, labelCache)` — pure, synchronous function.
       - rawPayload: the webhook body object
       - labelCache: Array<{id, name}> cached during init()
       - Returns TriggerEvent|null
       - If rawPayload.event is not "issue" and not "work_item" → return null
       - Extract data = rawPayload.data
       - Build taskRef: `${data.project_detail?.identifier}-${data.sequence_id}`
       - Resolve labels: resolveWorkItemLabels(data.labels, labelCache)
       - Import and call `parseKodoLabels` from `../../labels.js` with resolved label objects (need [{name: "..."}] format)
       - Return: { taskRef, action: rawPayload.action, provider: "plane", raw: { ...rawPayload, kodoConfig: parseKodoLabels(resolvedLabelObjects) } }

    Use `// @ts-check` at top. Import VALID_PRIORITIES from `../../interface.js`. Import parseKodoLabels from `../../labels.js`.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/normalize.test.js</automated>
  </verify>
  <done>All normalize.test.js tests pass. normalizeWorkItem produces valid TaskItem from Plane fixtures. parseTriggerEvent produces valid TriggerEvent from webhook fixtures. Labels resolve from UUIDs to names. HTML strips to plain text.</done>
</task>

</tasks>

<verification>
- `node --test test/normalize.test.js` — all tests green
- `node --test test/*.test.js` — no regressions in existing tests
</verification>

<success_criteria>
- normalizeWorkItem converts Plane API response to TaskItem with no Plane-specific fields leaking (no UUIDs in labels, no HTML in description)
- parseTriggerEvent converts webhook payload to TriggerEvent with resolved kodoConfig
- All fixture-based tests pass
- Pure functions with no side effects — no API calls, no disk reads
</success_criteria>

<output>
After completion, create `.planning/phases/02-plane-adapter-registry/02-01-SUMMARY.md`
</output>
