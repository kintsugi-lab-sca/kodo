---
phase: 14
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .planning/PROJECT.md
  - test/version-smoke.test.js
autonomous: true
requirements: [DX-07]
user_setup: []

must_haves:
  truths:
    - "PROJECT.md §Constraints documents the picocolors single-source invariant — appended as a sibling bullet after the existing LOG-12 / mode-derived-by-helper bullets."
    - "`node bin/kodo --version` exits 0, prints `0.1.0` to stdout, and produces empty stderr (no deprecation warnings, no install warnings, no missing-module errors triggered by the picocolors import side-effect even though Phase 14 never executes format.js from the CLI entry path)."
    - "The smoke test runs `bin/kodo --version` via `child_process.spawnSync` — process actually spawns, not a unit-mocked invocation."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "New §Constraints bullet documenting picocolors single-source invariant"
      contains: "Color isolation"
    - path: "test/version-smoke.test.js"
      provides: "Spawn-based smoke test that `bin/kodo --version` works after picocolors install"
      min_lines: 20
  key_links:
    - from: "test/version-smoke.test.js"
      to: "bin/kodo"
      via: "spawnSync('node', ['bin/kodo', '--version'])"
      pattern: "spawnSync"
    - from: ".planning/PROJECT.md (§Constraints, new bullet)"
      to: "test/format-isolation.test.js"
      via: "prose reference: '— test/format-isolation.test.js blinda la single-source'"
      pattern: "format-isolation"
---

<objective>
Close the documentation + smoke-test gap of Phase 14: append a §Constraints bullet to `.planning/PROJECT.md` documenting that `picocolors` is a single-source dependency only imported from `src/cli/format.js`, and add a spawn-based smoke test (`test/version-smoke.test.js`) that asserts `node bin/kodo --version` still works without warnings after the picocolors dependency is installed.

Purpose: satisfy ROADMAP SC#4 ("`picocolors` aparece en `package.json` ... PROJECT.md documenta el bump ... y `kodo --version` sigue funcionando sin warnings") fully. The `package.json` + lockfile portion is delivered by Plan 14-01; this plan closes the documentation and CLI-load-path verification.
Output: `.planning/PROJECT.md` modified (one new §Constraints bullet); `test/version-smoke.test.js` created (new file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-cli-format-foundation/14-CONTEXT.md
@.planning/phases/14-cli-format-foundation/14-PATTERNS.md

# Files referenced for the smoke test
@bin/kodo
@src/cli.js
@package.json

<interfaces>
<!-- Existing PROJECT.md §Constraints bullets to mirror in tone/length (verbatim from .planning/PROJECT.md:114-121) -->

```markdown
## Constraints

- **Stack**: Node.js, sin frameworks pesados, mínimas dependencias externas
- **Compatibilidad**: Breaking changes OK (v0.x, no hay usuarios externos)
- **Runtime**: Debe funcionar en macOS con cmux instalado
- **Tokens**: Vigilante/server consumen 0 tokens; solo el orquestador usa LLM
- **Logger aislado del vigilante**: `kodo check` no debe cargar `src/logger.js` transitivamente (LOG-12 guard)
- **Modo derivado por helper, NO inline**: cualquier consumidor de `gsd_mode` o de las flags debe usar `getGsdMode(flags)` / `getSessionMode(session)`. Source-hygiene Phase 13 D-09/D-10/D-11 blindado con tests.
```

<!-- New bullet to append (PATTERNS.md §PROJECT.md modification, verbatim) -->

```markdown
- **Color isolation**: `picocolors` solo se importa desde `src/cli/format.js`. Cualquier callsite que necesite color va por `createFormatter(stream)` — test/format-isolation.test.js blinda la single-source con grep + walker (LOG-12 extension + D-07/D-08 source-hygiene).
```

<!-- bin/kodo current shape (don't modify it — just verify the smoke test path) -->

`bin/kodo` is the CLI shebang entry; running `node bin/kodo --version` causes commander to print the version from `package.json` and exit 0.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Append picocolors single-source bullet to PROJECT.md §Constraints</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 114-122 (current §Constraints — match tone, bold-lead-colon-prose-paren-tags pattern)
    - .planning/phases/14-cli-format-foundation/14-PATTERNS.md §.planning/PROJECT.md (verbatim bullet text + placement directive: append after existing list around line 121-122)
    - .planning/phases/14-cli-format-foundation/14-CONTEXT.md §canonical_refs ("§"Current Milestone: v0.5..." block already mentions picocolors as 2nd dep — needs no edit; only §Constraints gets the new bullet")
  </read_first>
  <behavior>
    - After the edit, `.planning/PROJECT.md` §Constraints contains exactly 7 bullets (was 6), with the new "Color isolation" bullet appended LAST.
    - The new bullet's exact text matches PATTERNS.md §PROJECT.md verbatim.
    - No other section of PROJECT.md is modified — `## Current Milestone: v0.5 ...` block (line 17-29) already references picocolors; verify it does and leave it untouched.
    - The edit preserves trailing newline at EOF (no off-by-one whitespace drift).
  </behavior>
  <action>
    1. Read `.planning/PROJECT.md` lines 114-122 to identify the exact end of the §Constraints bullet list (currently ends with "Modo derivado por helper, NO inline ..." on line 121).

    2. Append (do NOT replace) one new bullet immediately AFTER line 121. Exact text (Spanish, bold lead, colon, prose, parenthetical invariant tags — mirroring existing bullets):

       ```markdown
       - **Color isolation**: `picocolors` solo se importa desde `src/cli/format.js`. Cualquier callsite que necesite color va por `createFormatter(stream)` — test/format-isolation.test.js blinda la single-source con grep + walker (LOG-12 extension + D-07/D-08 source-hygiene).
       ```

    3. Use the Edit tool with the existing line 121 bullet as `old_string` and `<existing_bullet>\n<new_bullet>` as `new_string` to ensure the bullet is appended in the correct position without disturbing the §Key Decisions section that follows.

    4. Verify §Current Milestone block (around line 17-29) ALREADY mentions `picocolors` as the 2nd dep. Per CONTEXT canonical_refs and PATTERNS.md, this section "needs no edit". Confirm by `grep` and DO NOT modify it.

    5. Do NOT add the bullet to §Key Decisions table — the table tracks architectural decisions with rationale columns; the picocolors invariant fits §Constraints (a hard rule) better than §Key Decisions (a recorded rationale). Match the placement of the existing LOG-12 guard bullet, which is also in §Constraints.

    6. Last-updated footer: the file currently ends with `*Last updated: 2026-05-04 — v0.5 CLI Polish & v0.3 Debt Cleanup started*`. Do NOT modify this line in Phase 14 — it was updated when the milestone started; the next bump happens at milestone close.
  </action>
  <verify>
    <automated>grep -c "Color isolation" .planning/PROJECT.md && grep -c "test/format-isolation.test.js" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \*\*Color isolation\*\*:" .planning/PROJECT.md` equals `1`.
    - `grep -c "createFormatter(stream)" .planning/PROJECT.md` equals `1`.
    - `grep -c "test/format-isolation.test.js" .planning/PROJECT.md` equals `1`.
    - `grep -c "^- \*\*Logger aislado del vigilante\*\*" .planning/PROJECT.md` still equals `1` (LOG-12 bullet untouched).
    - `grep -c "^- \*\*Modo derivado por helper" .planning/PROJECT.md` still equals `1` (mode-helper bullet untouched).
    - The new "Color isolation" bullet appears AFTER the "Modo derivado por helper" bullet (verify via `grep -n` and inspect line numbers).
    - The diff is purely additive: `git diff .planning/PROJECT.md | grep -c "^-[^-]"` equals `0` (no content lines removed; only `---` diff hunk headers, which start with `---`, are excluded by the `[^-]` guard). The diff should show ONLY one added bullet line (the new "Color isolation" entry) and possibly unchanged context lines.
  </acceptance_criteria>
  <done>One new §Constraints bullet appended verbatim from PATTERNS.md spec; surrounding sections unchanged; grep audits pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/version-smoke.test.js — spawn bin/kodo --version, assert exit 0 + version + clean stderr</name>
  <files>test/version-smoke.test.js</files>
  <read_first>
    - bin/kodo (full file — verify it's a shebang Node script that delegates to src/cli.js)
    - src/cli.js (just the imports + commander setup — confirm `--version` is wired to package.json's version field)
    - package.json (confirm `"version": "0.1.0"` exists — the smoke test asserts this exact string)
    - test/check-isolation.test.js (analog: imports + describe/it style)
  </read_first>
  <behavior>
    - `node bin/kodo --version` (run via `child_process.spawnSync`) exits with status 0.
    - Stdout (after `.toString().trim()`) equals exactly `'0.1.0'` (commander default `--version` behavior).
    - Stderr is empty (`buffer.length === 0` or `.toString().trim() === ''`). No deprecation warnings, no node experimental warnings, no missing-module errors. This catches: (a) picocolors install deprecation, (b) accidental ESM/CJS warnings from a future build pipeline, (c) any side-effect import in the CLI entry path that breaks under the new dep.
    - The test does NOT import `src/cli/format.js` directly (Phase 14 boundary — no callsite). It only runs the existing `bin/kodo` entry point. The whole point: confirm the picocolors install does NOT break the existing CLI even though it's not yet wired.
    - Test runs in `< 5 seconds` (spawn-based, not a unit test). Use `node:test` default timeout (no explicit timeout needed — kodo --version exits in ms).
  </behavior>
  <action>
    Create `test/version-smoke.test.js`:

    ```javascript
    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';
    import { spawnSync } from 'node:child_process';
    import { fileURLToPath } from 'node:url';
    import { dirname, resolve, join } from 'node:path';
    import { readFileSync } from 'node:fs';

    const __dirname = dirname(fileURLToPath(import.meta.url));
    const REPO = resolve(__dirname, '..');
    const KODO_BIN = join(REPO, 'bin', 'kodo');

    // Read the canonical version from package.json so this test stays in sync
    // if/when version bumps.
    const PKG_VERSION = JSON.parse(readFileSync(join(REPO, 'package.json'), 'utf-8')).version;

    describe('Phase 14 SC#4: kodo --version smoke (post picocolors install)', () => {
      it('node bin/kodo --version exits 0, prints version, no stderr', () => {
        const result = spawnSync(process.execPath, [KODO_BIN, '--version'], {
          cwd: REPO,
          encoding: 'utf-8',
          // No env override — we want to test the install in its real shape.
        });
        assert.equal(
          result.status,
          0,
          `kodo --version exited with status ${result.status}\nstderr: ${result.stderr}\nstdout: ${result.stdout}`,
        );
        assert.equal(
          result.stdout.trim(),
          PKG_VERSION,
          `kodo --version stdout was ${JSON.stringify(result.stdout.trim())}, expected ${JSON.stringify(PKG_VERSION)}`,
        );
        assert.equal(
          result.stderr.trim(),
          '',
          `kodo --version emitted unexpected stderr (Phase 14 picocolors install must not warn):\n${result.stderr}`,
        );
      });
    });
    ```

    Key implementation notes:
    - Use `process.execPath` not the literal string `'node'` — guarantees the test runs under the same Node binary that invoked `node --test`.
    - Read `PKG_VERSION` from `package.json` so the test does not hard-code `'0.1.0'` and won't false-fail on a future minor bump.
    - No `@ts-check` pragma (test files don't use it — confirmed by check-isolation.test.js:1).
    - Use `encoding: 'utf-8'` so `result.stdout` and `result.stderr` are strings, not Buffers.
    - Do NOT pass `env: { ...process.env, NO_COLOR: '1' }` or similar — the test asserts the install is clean in its REAL shape. Future TTY/no-TTY testing belongs to Plan 14-01's unit tests.
    - Do NOT mock `spawnSync` — the whole purpose is real-process verification.
  </action>
  <verify>
    <automated>node --test test/version-smoke.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/version-smoke.test.js` exits 0 with 1 test pass.
    - `grep -c "spawnSync" test/version-smoke.test.js` is `>= 1`.
    - `grep -c "process.execPath" test/version-smoke.test.js` equals `1`.
    - `grep -c "PKG_VERSION" test/version-smoke.test.js` is `>= 2` (assignment + assertion).
    - `grep -c "stderr.trim()" test/version-smoke.test.js` is `>= 1` (clean-stderr assertion).
    - **Manual confirmation** (not automated, document in SUMMARY): `node bin/kodo --version` printed at the executor's terminal shows `0.1.0` followed by a single newline; `node bin/kodo --version 2>&1 1>/dev/null` prints nothing (empty stderr).
    - `node --test test/` (full suite) exits 0 — no other test broken by adding the spawn-based test (spawn is sync, no leaked handles).
  </acceptance_criteria>
  <done>`test/version-smoke.test.js` exists, asserts exit 0 + version match + empty stderr via real process spawn, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `package.json` → spawn-based test | Test reads version from package.json (developer-controlled) and spawns `node bin/kodo --version` (developer-controlled binary). No external input. |
| filesystem → PROJECT.md edit | Documentation-only edit. No code path affected. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering | Documentation drift between PROJECT.md and code | mitigate | The PROJECT.md bullet references `test/format-isolation.test.js` by path. If the test is deleted or renamed, the documentation breaks but no security failure occurs — this is a maintenance concern, not a vuln. Future doc-link checker (out of scope) would close it. |
| T-14-11 | Denial of Service | Spawn-based test hangs CI | accept | `bin/kodo --version` is a commander built-in that exits in milliseconds. No I/O, no network. `spawnSync` is synchronous and returns when the child exits. No timeout needed. |
| T-14-12 | Information Disclosure | spawnSync inherits parent env | accept | The test reads `process.execPath` and inherits `cwd: REPO`. No secret env propagation risk — this is a CLI version flag, not a network call. |
| T-14-13 | Tampering | Picocolors loaded transitively into bin/kodo path warns | accept (verified by test) | This is exactly what the smoke test catches. If a future picocolors version emits deprecation, the test fails and the team revisits the dep range. ASVS L1 covered: dependency confusion / vulnerable component → flagged at install time by `npm install`'s warnings (which the smoke test escalates to test failure). |

**Block conditions:** No `high`-severity threat. ASVS L1 satisfied for this plan's surface.
</threat_model>

<verification>
- `grep -c "Color isolation" .planning/PROJECT.md` returns `1`.
- `node --test test/version-smoke.test.js` exits 0.
- `node --test test/` full suite green.
- Manual: `node bin/kodo --version` prints `0.1.0` with empty stderr.
</verification>

<success_criteria>
- ROADMAP SC#4 fully closed: picocolors in package.json (Plan 14-01) + lockfile (Plan 14-01) + PROJECT.md documents the bump (Plan 14-03 §Constraints bullet) + `kodo --version` confirmed working without warnings (Plan 14-03 smoke test).
- DX-07 fully satisfied (dep + doc).
- ROADMAP SC#5 partial: full suite green after dep install confirms no callsite was affected by introducing picocolors at the package level.
</success_criteria>

<output>
After completion, create `.planning/phases/14-cli-format-foundation/14-03-SUMMARY.md` documenting:
- Diff of `.planning/PROJECT.md` (one bullet appended).
- Smoke test results: exit code, stdout, stderr (should be `0`, `'0.1.0'`, `''`).
- Confirmation that the §Current Milestone block was NOT edited (already references picocolors).
- Any unexpected stderr output from `kodo --version` (would indicate a deprecation or warning to investigate).
</output>
</content>
</invoke>