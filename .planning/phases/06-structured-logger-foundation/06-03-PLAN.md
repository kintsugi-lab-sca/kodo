---
phase: 06-structured-logger-foundation
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/logger.js
autonomous: true
requirements: [LOG-08]
tags: [logging, security, redaction]

must_haves:
  truths:
    - "Ningún secreto del set cerrado (`PLANE_API_KEY`, `plane_api_key`, `authorization`, `x-api-key`, `x-plane-signature`, `password`, `token`, `secret`, `cookie`, `set-cookie`) aparece en el archivo NDJSON persistido"
    - "Valores JWT-like (`eyJ...<≥10chars>.<≥10chars>.<≥10chars>`) se redactan aunque la key sea desconocida"
    - "Valores `Bearer <≥20 chars>` o `plane_<≥20 chars>` se redactan aunque la key sea desconocida"
    - "El placeholder es literal `[REDACTED]` (no hash, no longitud) para keys conocidas y patterns regex"
    - "Deep-walk limita profundidad a 4 y longitud de array a 100; al exceder, reemplaza por `[REDACTED:depth-exceeded]` o trunca con sentinel"
    - "stderr pretty-print también sale redactado (secretos no aparecen en la mirror)"
    - "El redactor NO propaga `__proto__`, `constructor`, `prototype` como claves enumerables"
  artifacts:
    - path: "src/logger.js"
      provides: "Redactor privado + integración en emit() antes de writeNdjson/maybeMirrorToStderr"
      contains: "SENSITIVE_KEYS"
      contains: "redact"
  key_links:
    - from: "src/logger.js:emit"
      to: "src/logger.js:redact"
      via: "record = redact(record) antes de JSON.stringify y de pretty-print"
      pattern: "redact\\("
---

<objective>
Añadir el redactor de secretos a `src/logger.js` con estrategia dual (key-set cerrado + regex genérico JWT/bearer) y deep-walk con límites, aplicado ANTES de cualquier escritura (disco y stderr). Cubre LOG-08 completo.

Purpose: Cerrar la superficie de riesgo de information disclosure (ASVS V7 — core de esta phase). El logger es cruzado por credenciales en transit (headers Authorization, plane_api_key, firmas de webhook) y la regresión es silenciosa — un secreto persistido es un incidente que solo se detecta por audit. El test grep-based de `test/logger-redaction.test.js` es el guardián de referencia.

Output: `src/logger.js` modificado (~280 LoC finales); `test/logger-redaction.test.js` pasa en verde los 3 bloques (grep, depth limit, stderr redaction).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-structured-logger-foundation/06-CONTEXT.md
@.planning/phases/06-structured-logger-foundation/06-RESEARCH.md
@.planning/phases/06-structured-logger-foundation/06-PATTERNS.md
@.planning/phases/06-structured-logger-foundation/06-02-SUMMARY.md
@src/logger.js
@test/logger-redaction.test.js
@test/helpers/logger-fixtures.js

<interfaces>
<!-- Este plan NO cambia el API público. `createLogger`, `LEVELS`, `LEVEL_NAMES`, `noopLogger` se mantienen idénticos. -->

Función privada añadida (no exportada):
```javascript
function redact(value: unknown, depth?: number, keyHint?: string): unknown;
```

Constantes privadas añadidas:
```javascript
const SENSITIVE_KEYS: Set<string>;   // lowercase comparison
const JWT_RE: RegExp;
const BEARERY_RE: RegExp;
const MAX_DEPTH = 4;
const MAX_ARRAY_LEN = 100;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Implementar redactor deep-walk con key-set + regex y cablearlo en emit()</name>
  <files>src/logger.js</files>
  <read_first>
    - src/logger.js (estado actual tras Plan 02 — ubicación de `emit()`)
    - .planning/phases/06-structured-logger-foundation/06-RESEARCH.md líneas 256-294 (Pattern 2 — redactor completo)
    - .planning/phases/06-structured-logger-foundation/06-CONTEXT.md líneas 33-38 (keys sensibles + placeholder)
    - .planning/phases/06-structured-logger-foundation/06-PATTERNS.md líneas 436-441 (advertencias sobre el patrón nuevo)
    - test/logger-redaction.test.js (contrato: los 3 bloques deben pasar)
  </read_first>
  <behavior>
    - `redact({ plane_api_key: 'x' })` devuelve `{ plane_api_key: '[REDACTED]' }`
    - `redact({ request: { headers: { authorization: 'Bearer x' } } })` devuelve `{ request: { headers: { authorization: '[REDACTED]' } } }` (match por key, independiente del valor)
    - `redact({ payload: 'eyJhbGciOiJIUzI1NiJ9.eyJzIjoiMSJ9.aBcDeFg12345' })` devuelve `{ payload: '[REDACTED]' }` (match por regex JWT, aunque `payload` no está en SENSITIVE_KEYS)
    - `redact({ token_value: 'Bearer plane_abc...' })` — key `token_value` NO en set, pero valor matchea BEARERY_RE → redactado
    - `redact({ a: { b: { c: { d: { e: { f: 'leaf' } } } } } })` reemplaza el nivel 5+ por `[REDACTED:depth-exceeded]`
    - `redact([...Array(150).keys()])` devuelve array de 100 items + sentinel `[REDACTED:truncated-50]`
    - `redact(undefined) === undefined`, `redact(null) === null`
    - `redact(42) === 42`, `redact(true) === true`
    - `redact({ __proto__: { polluted: true } })` NO propaga `polluted` al resultado (iteración vía `Object.entries` omite `__proto__`)
    - La comparación de keys es case-insensitive: `Authorization`, `AUTHORIZATION`, `authorization` todos redactan
    - `emit(level, msg, ctx, boundFields)` aplica `redact(record)` ANTES de `writeNdjson` y `maybeMirrorToStderr`
    - El redactor es idempotente: `redact(redact(x))` produce el mismo output que `redact(x)`
  </behavior>
  <action>
  Modificar `src/logger.js` con tres bloques:

  **Bloque 1: Constantes privadas al top del módulo (después de ANSI constants, antes de `createLogger`):**

  ```javascript
  // --- Redaction (LOG-08) ---

  /** Keys cuyo VALOR se redacta siempre (comparación case-insensitive). */
  const SENSITIVE_KEYS = new Set([
    'plane_api_key',
    'authorization',
    'x-api-key',
    'x-plane-signature',
    'password',
    'token',
    'secret',
    'cookie',
    'set-cookie',
  ]);

  /**
   * JWT: `eyJ` (base64 de `{"`) + base64url con ≥2 puntos. Conservador:
   * no matchea UUIDs ni git SHAs ni texto normal.
   */
  const JWT_RE = /^eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}$/;

  /** Bearer token o plane_api_key prefix. Requiere ≥20 chars tras el prefijo. */
  const BEARERY_RE = /^(Bearer\s+|plane_)[A-Za-z0-9_\-]{20,}$/i;

  const REDACTED = '[REDACTED]';
  const REDACTED_DEPTH = '[REDACTED:depth-exceeded]';
  const MAX_DEPTH = 4;
  const MAX_ARRAY_LEN = 100;
  ```

  **Bloque 2: Función privada `redact()` (añadir después de `formatCtxInline` o como helper top-level del módulo — decidir según cohesión, recomendado top-level porque es pure):**

  ```javascript
  /**
   * Deep-walk redactor: reemplaza valores de keys sensibles y strings que
   * matcheen patterns JWT/Bearer por `[REDACTED]`. Aplica límites de profundidad
   * y longitud de array para proteger contra ctx gigantes.
   *
   * Pure function: no muta `value`. Idempotente.
   *
   * @param {unknown} value
   * @param {number} [depth]
   * @param {string} [keyHint]
   * @returns {unknown}
   */
  function redact(value, depth = 0, keyHint = '') {
    if (depth > MAX_DEPTH) return REDACTED_DEPTH;
    if (value === null || value === undefined) return value;

    if (typeof value === 'string') {
      if (keyHint && SENSITIVE_KEYS.has(keyHint.toLowerCase())) return REDACTED;
      if (JWT_RE.test(value) || BEARERY_RE.test(value)) return REDACTED;
      return value;
    }

    if (typeof value !== 'object') return value;

    if (Array.isArray(value)) {
      if (value.length > MAX_ARRAY_LEN) {
        const kept = value.slice(0, MAX_ARRAY_LEN).map(v => redact(v, depth + 1, keyHint));
        kept.push(`[REDACTED:truncated-${value.length - MAX_ARRAY_LEN}]`);
        return kept;
      }
      return value.map(v => redact(v, depth + 1, keyHint));
    }

    // Objeto: iterar con Object.entries (omite __proto__ enumerable por diseño)
    const out = /** @type {Record<string, unknown>} */ ({});
    for (const [k, v] of Object.entries(value)) {
      if (SENSITIVE_KEYS.has(k.toLowerCase())) {
        out[k] = REDACTED;
      } else {
        out[k] = redact(v, depth + 1, k);
      }
    }
    return out;
  }
  ```

  **Bloque 3: Cablear `redact()` en `emit()` dentro del factory:**

  Cambiar:
  ```javascript
  const record = {
    timestamp: new Date().toISOString(),
    level,
    ...boundFields,
    msg: String(msg),
    ...(ctx ?? {}),
  };
  writeNdjson(record);
  maybeMirrorToStderr(level, record);
  ```

  A:
  ```javascript
  const rawRecord = {
    timestamp: new Date().toISOString(),
    level,
    ...boundFields,
    msg: String(msg),
    ...(ctx ?? {}),
  };
  const record = /** @type {Record<string, unknown>} */ (redact(rawRecord));
  writeNdjson(record);
  maybeMirrorToStderr(level, record);
  ```

  **Notas de conformidad:**
  - `redact` NO se exporta. Es interno al módulo.
  - Funcionar como standalone top-level (no dentro del closure del factory) porque es pure y facilita reutilización/testing manual.
  - NO añadir logging de redacciones (metadata leak: un atacante podría correlar patterns).
  - NO usar entropy scoring (falsos positivos en UUIDs, git shas — ver RESEARCH Alternatives).
  - NO hashear el secreto (CONTEXT.md prohíbe explícitamente; facilita side-channel).
  - El test de redacción hace **grep directo** del archivo NDJSON — es la única aserción confiable. Los tests de shape (en logger.test.js) NO deben romperse: los campos base (`timestamp`, `level`, `msg`, `session_id`, `component`, `plane_task_id`, `phase_id`) no están en SENSITIVE_KEYS y no matchean los regex.

  **Verificación de no-regresión en LOG-01..LOG-04:** los 4 describe blocks de `test/logger.test.js` deben seguir verdes. Especial atención a LOG-03 test "child() merges plane_task_id" — `plane_task_id` con valor `'KL-42'` no matchea `BEARERY_RE` (requiere ≥20 chars) ni `JWT_RE` y la key no está en SENSITIVE_KEYS → se preserva.

  Ejecutar la suite completa del phase:
  `node --test test/logger.test.js test/logger-redaction.test.js test/check-isolation.test.js test/startup-budget.test.js`

  Los 4 tests deben pasar en verde tras este task.
  </action>
  <acceptance_criteria>
    - Grep `SENSITIVE_KEYS\\s*=\\s*new Set` en src/logger.js encuentra 1 match
    - Grep `JWT_RE\\s*=\\s*/\\^eyJ` encuentra 1 match
    - Grep `BEARERY_RE\\s*=` encuentra 1 match
    - Grep `function\\s+redact\\s*\\(` encuentra 1 match (no exportado — no debe haber `export` antes)
    - Grep `^export.*redact` NO encuentra match (redact es privado)
    - Grep `redact\\(rawRecord\\)` o equivalente (llamada dentro de emit) encuentra match
    - `node --test test/logger-redaction.test.js` pasa los 3 `it()`: grep assertion, depth limit, stderr redaction
    - `node --test test/logger.test.js` sigue pasando los 4 describe blocks (no regresión LOG-01..LOG-04)
    - `node --test test/check-isolation.test.js test/startup-budget.test.js` siguen en verde
    - Test manual: `node -e "const {createLogger}=await import('./src/logger.js'); const l=createLogger({sessionId:'audit',minLevel:'debug'}); l.info('x',{Authorization:'Bearer plane_aaaaaaaaaaaaaaaaaaaaaaaaa'}); const fs=require('fs'); const raw=fs.readFileSync(require('os').homedir()+'/.kodo/logs/audit.ndjson','utf-8'); if(raw.includes('plane_aaaaaaaaaaaaaaaaaaaaaaaaa')) process.exit(1); fs.unlinkSync(require('os').homedir()+'/.kodo/logs/audit.ndjson');"` exit 0 (si se corre fuera de fixture; principalmente el test grep es el gate real)
    - Test de idempotencia implícito: `JSON.stringify(redact({a:{plane_api_key:'x'}}))` === `JSON.stringify(redact(redact({a:{plane_api_key:'x'}})))` (assert manual opcional)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/logger.test.js test/logger-redaction.test.js test/check-isolation.test.js test/startup-budget.test.js</automated>
  </verify>
  <done>El redactor está integrado en `emit()`, los 3 bloques de `logger-redaction.test.js` pasan, los tests de LOG-01..LOG-04 y LOG-12 no regresan, y `redact` es una función privada pura con límites de profundidad y longitud.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ctx` object from caller → redactor | Caller puede pasar credenciales real-time (headers Plane, webhook payloads) — primary security boundary del phase |
| Redactor → disk NDJSON + stderr | Sink final. Si el redactor falla-open, el secreto se persiste para siempre |

## STRIDE Threat Register (foco LOG-08 — ASVS V7 Logging & Monitoring + V6 Data Protection)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03-01 | Information Disclosure | `redact()` fails-open por typo en SENSITIVE_KEYS | mitigate | Lista hardcodeada en lowercase; comparación con `k.toLowerCase()`; test grep sobre archivo real es el último gate (cubre el caso completo end-to-end) |
| T-6-03-02 | Information Disclosure | JWT legítimo (no secreto) redactado como falso positivo | accept | El cost de FP es cosmético (log menos informativo); el cost de FN es un leak — trade-off explícito en CONTEXT.md. Si en Phase 7+ se detectan FPs problemáticos, añadir allow-list |
| T-6-03-03 | Information Disclosure | Secret embebido dentro de un string más largo (e.g. `"x=Bearer plane_xxx and y=z"`) | accept | Redactor opera por string completo (anchors `^...$`). Documentado como limitación; mitigación es que callsites no concatenen secretos en msgs (convención + futuro lint en Phase 7) |
| T-6-03-04 | Information Disclosure | Secreto en `msg` (primer argumento string) en vez de ctx | accept | `msg` pasa por `String(msg)` y entra al record; el redactor SÍ lo revisa (key hint = `'msg'`, que no está en SENSITIVE_KEYS, pero el regex JWT/Bearer sí se aplica al string). Callsites se instruyen a no poner secretos en `msg` |
| T-6-03-05 | Tampering (prototype pollution) | `{ __proto__: { polluted: true } }` en ctx | mitigate | `Object.entries` NO itera `__proto__` si se asignó con literal `{__proto__:...}`, y spread `{...obj}` no copia `__proto__`. Redactor reconstruye objetos vía `{}` plain |
| T-6-03-06 | DoS | Ctx con profundidad recursiva >1000 | mitigate | `MAX_DEPTH = 4` corta recursión antes del stack overflow |
| T-6-03-07 | DoS | Array con 10M items | mitigate | `MAX_ARRAY_LEN = 100` + sentinel; `slice(0, 100)` es O(1) amortizado |
| T-6-03-08 | Information Disclosure | Redactor se salta stderr mirror y solo aplica a disco | mitigate | Integración ÚNICA en `emit()`, ambos sinks reciben el record ya redactado — verificado por test `'redacts stderr mirror too'` |

**ASVS L1 compliance:**
- V7.1.1 (no log secrets) — directamente satisfecho por este redactor
- V7.3.3 (redact before persistence) — satisfecho porque `redact()` corre antes de `writeNdjson` y `maybeMirrorToStderr`
- V8.3.4 (protect data in logs) — satisfecho para secretos known-set; PII de task titles fuera de scope v0.3 (acknowledged en RESEARCH Security Domain)

**Block gate:** el test grep-based (`'never persists PLANE_API_KEY, headers, JWT-like values'`) es el gate duro. Si falla, el phase no puede avanzar a verificación.
</threat_model>

<verification>
- `node --test test/logger-redaction.test.js` → 3 `it()` verdes
- `node --test test/logger.test.js` → 4 describe blocks verdes (no regresión)
- `node --test test/check-isolation.test.js test/startup-budget.test.js` → verdes
- Grep de secretos: tras correr la suite, el archivo NDJSON temporal de la redaction test NO contiene `plane_abcdef0123456789deadbeefcafe1234`, `eyJhbGciOi...`, ni `sha256=0123...` (asertado inline por el test)
- Manual spot-check opcional: `node -e "import('./src/logger.js').then(({createLogger})=>{const l=createLogger({sessionId:'spot',minLevel:'debug'});l.info('h',{cookie:'abc',Authorization:'Bearer plane_xxxxxxxxxxxxxxxxxxxxxx'});})" && grep -E "(plane_xxxxxx|abc)" ~/.kodo/logs/spot.ndjson && echo FAIL || echo OK`
</verification>

<success_criteria>
- `redact()` existe como función privada en `src/logger.js` con `MAX_DEPTH=4`, `MAX_ARRAY_LEN=100`
- `SENSITIVE_KEYS` contiene al menos los 10 nombres del CONTEXT.md (case-insensitive)
- `JWT_RE` y `BEARERY_RE` matchean los valores del test; no matchean UUIDs ni git SHAs
- `emit()` aplica `redact(rawRecord)` antes de ambos sinks
- `test/logger-redaction.test.js` pasa los 3 bloques
- Ningún test previo regresa
- Redactor no se exporta (API pública intacta)
</success_criteria>

<output>
After completion, create `.planning/phases/06-structured-logger-foundation/06-03-SUMMARY.md` documentando el diff en LoC, los resultados de la suite, y cualquier FP conocido del regex (si se encontró durante testing manual).
</output>
