---
phase: 11-quick-mode-recognition-persistence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger-events.js
  - src/triggers/dispatcher.js
  - test/logger-events.test.js
autonomous: true
requirements:
  - QUICK-01
  - QUICK-02
tags:
  - dispatcher
  - telemetry
  - logger-events
  - ndjson

must_haves:
  truths:
    - "gsd.phase.resolved (success branch, matched:true) lleva campo mode='full'|'quick' en su payload NDJSON"
    - "gsd.bootstrap lleva campo mode='full'|'quick' y se emite vía el helper tipado gsdBootstrap (no via log.info literal)"
    - "Una task quick con resolver verdict 'no-match' emite gsd.phase.resolved {matched:false, code:'no-match', tolerated:true, mode:'quick'} a nivel info y dispatch CONTINÚA al launch (no fail-closed)"
    - "Una task con resolver verdict error y código roadmap-missing/multi-match sigue fallando cerrado independientemente del modo (warn emit + return action:'resolver_failed')"
    - "El warn emit de fail-closed lleva campo mode='full'|'quick' (schema homogéneo D-07 spirit)"
    - "El warn emit conserva el field name error_code; el info tolerated emit usa code+tolerated (divergencia intencional para distinguir fail-closed de tolerated)"
    - "El dispatcher sigue siendo la única fuente de gsd.phase.resolved y gsd.bootstrap (D-14 invariante Phase 9 preservado)"
    - "test/logger-events.test.js verifica que gsdPhaseResolved y gsdBootstrap emiten el campo mode"
  artifacts:
    - path: "src/logger-events.js"
      provides: "gsdPhaseResolved con field mode + gsdBootstrap con fields {project_path, brief_empty, mode}"
      contains: "mode: fields.mode"
    - path: "src/triggers/dispatcher.js"
      provides: "Telemetría con mode en los 4 callsites GSD (success, bootstrap, fail-closed warn, tolerated info)"
      contains: "tolerated: true"
    - path: "test/logger-events.test.js"
      provides: "Cobertura mínima: ambos helpers emiten el field mode"
      contains: "assert.equal(line.mode"
  key_links:
    - from: "src/triggers/dispatcher.js gsd.phase.resolved success emit"
      to: "src/logger-events.js gsdPhaseResolved"
      via: "named consumer"
      pattern: "gsdPhaseResolved\\(log, \\{[^}]*mode: gsdMode"
    - from: "src/triggers/dispatcher.js gsd.bootstrap emit"
      to: "src/logger-events.js gsdBootstrap"
      via: "lift de literal a helper"
      pattern: "gsdBootstrap\\(log, \\{[^}]*mode: gsdMode"
    - from: "src/triggers/dispatcher.js no-match info emit"
      to: "NDJSON record"
      via: "log.info literal con tolerated:true"
      pattern: "tolerated: true"
---

<objective>
Cerrar el control-plane de telemetría QUICK-01/QUICK-02 dentro del dispatcher: añadir el campo `mode` a los tres emits GSD que el dispatcher ya hace (success `gsd.phase.resolved`, fail-closed warn `gsd.phase.resolved` con `error_code`, `gsd.bootstrap`), añadir el cuarto emit que falta (tolerated `info` `gsd.phase.resolved` cuando `quick + no-match`), ampliar las firmas tipadas de `gsdPhaseResolved` y `gsdBootstrap` en `src/logger-events.js`, lift del literal `log.info('gsd.bootstrap', ...)` al helper `gsdBootstrap` (taxonomy single-source D-14), y actualizar `test/logger-events.test.js`.

Purpose: `kodo logs --event-type gsd.phase.resolved --json` y `kodo logs --event-type gsd.bootstrap --json` deben permitir filtrar/agrupar por modo. Hoy una task quick con resolver match success no se distingue de una full en la telemetría, y una task quick con `no-match` no deja rastro (el `break` actual es mudo).
Output: tres archivos modificados; no nuevos eventos NDJSON; reuso de los 8 existentes con el campo `mode` añadido (D-07 schema homogéneo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md
@.planning/phases/11-quick-mode-recognition-persistence/11-PATTERNS.md

@src/logger-events.js
@src/triggers/dispatcher.js
@test/logger-events.test.js

<interfaces>
Existing typed-emitter helpers (this plan extends gsdPhaseResolved + gsdBootstrap signatures):

```javascript
// src/logger-events.js:141-151 (current)
export function gsdPhaseResolved(logger, fields) {
  logger.info(EVENTS.GSD_PHASE_RESOLVED, {
    event: EVENTS.GSD_PHASE_RESOLVED,
    phase_id: fields.phase_id,
    match_heading: fields.match_heading,
  });
}

// src/logger-events.js:153-162 (current — currently unused; this plan wires the dispatcher to it)
export function gsdBootstrap(logger, fields) {
  logger.info(EVENTS.GSD_BOOTSTRAP, {
    event: EVENTS.GSD_BOOTSTRAP,
    project_path: fields.project_path,
  });
}
```

Frozen EVENTS constant (NO change):

```javascript
// src/logger-events.js:30-39
export const EVENTS = Object.freeze({
  ...
  GSD_PHASE_RESOLVED: 'gsd.phase.resolved',
  GSD_BOOTSTRAP:      'gsd.bootstrap',
  ...
});
```

Dispatcher consumer sites this plan modifies:

```javascript
// src/triggers/dispatcher.js:165-201 (error branch — adds tolerated info emit + mode in warn)
case 'error':
  if (gsdMode === 'quick' && resolverVerdict.code === 'no-match') {
    break;  // ← this plan inserts the info emit BEFORE break
  }
  // fail-closed:
  ...
  log.warn('gsd.phase.resolved', { ..., error_code: ..., detail: ..., task_ref });
  // ← this plan adds `mode: gsdMode` to the warn payload

// src/triggers/dispatcher.js:202-225 (success/bootstrap branches — both extend with mode)
if (resolverVerdict.action === 'phase') {
  gsdPhaseResolved(log, { phase_id, match_heading });  // ← add mode: gsdMode
} else if (resolverVerdict.action === 'bootstrap') {
  log.info('gsd.bootstrap', { event, project_path, brief_empty });  // ← lift to gsdBootstrap helper + add mode
}
```

Test fixture pattern (test/logger-events.test.js:142-162 is the section to extend):

```javascript
// current gsdPhaseResolved test — extend with mode field
it('gsdPhaseResolved emits event=gsd.phase.resolved + phase_id/match_heading', () => {
  ...
  gsdPhaseResolved(log, { phase_id: '...', match_heading: '...' });
  ...
});

// current gsdBootstrap test — extend with mode + brief_empty
it('gsdBootstrap emits event=gsd.bootstrap + project_path', () => {
  ...
  gsdBootstrap(log, { project_path: '/tmp/kodo-demo' });
  ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ampliar firmas tipadas gsdPhaseResolved + gsdBootstrap en logger-events.js (D-05/D-07)</name>
  <files>src/logger-events.js, test/logger-events.test.js</files>
  <read_first>
    - src/logger-events.js (todo el archivo — necesitas ver la convención de helpers tipados: stateTransition líneas 113-124 y orchestratorReview líneas 130-139 son los patrones canónicos a copiar; gsdPhaseResolved actual líneas 141-151; gsdBootstrap actual líneas 153-162)
    - test/logger-events.test.js (todo el archivo — necesitas el describe('LOG-09: ...') block completo y las dos it(...) que vas a extender, líneas 142-162; presta atención a readAllLines(logPathFor(sessionId)).pop() y al pattern assert.equal(line.<field>, ...))
    - .planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md (decisions D-05 "gsd.phase.resolved con match exitoso lleva campo mode" + D-07 "gsd.bootstrap también lleva campo mode")
    - .planning/phases/11-quick-mode-recognition-persistence/11-PATTERNS.md (sección 5 "src/logger-events.js — extend gsdPhaseResolved" — bloque exacto a escribir + nota sobre lift de gsdBootstrap)
  </read_first>
  <behavior>
    - Test 1 (existing, extended): gsdPhaseResolved(log, { phase_id, match_heading, mode: 'quick' }) ⇒ NDJSON line con event=gsd.phase.resolved + phase_id + match_heading + mode='quick'
    - Test 2 (new): gsdPhaseResolved(log, { phase_id, match_heading, mode: 'full' }) ⇒ NDJSON line con mode='full'
    - Test 3 (existing, extended): gsdBootstrap(log, { project_path, brief_empty: false, mode: 'full' }) ⇒ NDJSON line con event=gsd.bootstrap + project_path + brief_empty=false + mode='full'
    - Test 4 (new): gsdBootstrap(log, { project_path, brief_empty: true, mode: 'quick' }) ⇒ NDJSON line con brief_empty=true + mode='quick'
  </behavior>
  <action>
Paso A: Modificar gsdPhaseResolved en src/logger-events.js (líneas 141-151).

Reemplazar el bloque actual:
```javascript
/**
 * @param {Logger} logger
 * @param {{ phase_id: string, match_heading: string }} fields
 */
export function gsdPhaseResolved(logger, fields) {
  logger.info(EVENTS.GSD_PHASE_RESOLVED, {
    event: EVENTS.GSD_PHASE_RESOLVED,
    phase_id: fields.phase_id,
    match_heading: fields.match_heading,
  });
}
```

Por:
```javascript
/**
 * Emite el evento `gsd.phase.resolved` (success branch, matched:true).
 * Phase 11 (D-05): añade el campo `mode` para distinguir 'full' vs 'quick'.
 * El dispatcher es la única fuente de este evento (D-14 Phase 9 invariante).
 *
 * @param {Logger} logger
 * @param {{ phase_id: string, match_heading: string, mode: 'full'|'quick' }} fields
 */
export function gsdPhaseResolved(logger, fields) {
  logger.info(EVENTS.GSD_PHASE_RESOLVED, {
    event: EVENTS.GSD_PHASE_RESOLVED,
    phase_id: fields.phase_id,
    match_heading: fields.match_heading,
    mode: fields.mode,
  });
}
```

Paso B: Modificar gsdBootstrap en src/logger-events.js (líneas 153-162).

El helper actualmente acepta solo `{ project_path }`. La realidad del dispatcher (línea 217-221) emite hoy literalmente `log.info('gsd.bootstrap', { event, project_path, brief_empty })` — el `brief_empty` no estaba en el helper. Este plan reconcilia ambos: el helper se vuelve la fuente única (D-14) y acepta los 3 campos.

Reemplazar el bloque actual:
```javascript
/**
 * @param {Logger} logger
 * @param {{ project_path: string }} fields
 */
export function gsdBootstrap(logger, fields) {
  logger.info(EVENTS.GSD_BOOTSTRAP, {
    event: EVENTS.GSD_BOOTSTRAP,
    project_path: fields.project_path,
  });
}
```

Por:
```javascript
/**
 * Emite el evento `gsd.bootstrap`. Phase 11 (D-07): añade `mode` y reconcilia
 * el campo `brief_empty` que el dispatcher ya emitía como literal en Phase 9
 * (lift literal → helper, completa la migración a la taxonomía cerrada D-14).
 *
 * @param {Logger} logger
 * @param {{ project_path: string, brief_empty: boolean, mode: 'full'|'quick' }} fields
 */
export function gsdBootstrap(logger, fields) {
  logger.info(EVENTS.GSD_BOOTSTRAP, {
    event: EVENTS.GSD_BOOTSTRAP,
    project_path: fields.project_path,
    brief_empty: fields.brief_empty,
    mode: fields.mode,
  });
}
```

Paso C: Extender test/logger-events.test.js (líneas 142-162).

Reemplazar el bloque actual:
```javascript
  it('gsdPhaseResolved emits event=gsd.phase.resolved + phase_id/match_heading', () => {
    const sessionId = 'sess-ev-gpr';
    const log = createLogger({ sessionId, minLevel: 'info' });
    gsdPhaseResolved(log, {
      phase_id: '07-kodo-logs-cli',
      match_heading: 'Phase 7: kodo logs CLI + Event Taxonomy',
    });
    const line = readAllLines(logPathFor(sessionId)).pop();
    assert.equal(line.event, EVENTS.GSD_PHASE_RESOLVED);
    assert.equal(line.phase_id, '07-kodo-logs-cli');
    assert.equal(line.match_heading, 'Phase 7: kodo logs CLI + Event Taxonomy');
  });

  it('gsdBootstrap emits event=gsd.bootstrap + project_path', () => {
    const sessionId = 'sess-ev-gb';
    const log = createLogger({ sessionId, minLevel: 'info' });
    gsdBootstrap(log, { project_path: '/tmp/kodo-demo' });
    const line = readAllLines(logPathFor(sessionId)).pop();
    assert.equal(line.event, EVENTS.GSD_BOOTSTRAP);
    assert.equal(line.project_path, '/tmp/kodo-demo');
  });
```

Por:
```javascript
  it('gsdPhaseResolved emits event=gsd.phase.resolved + phase_id/match_heading + mode (Phase 11 D-05)', () => {
    const sessionId = 'sess-ev-gpr';
    const log = createLogger({ sessionId, minLevel: 'info' });
    gsdPhaseResolved(log, {
      phase_id: '07-kodo-logs-cli',
      match_heading: 'Phase 7: kodo logs CLI + Event Taxonomy',
      mode: 'full',
    });
    const line = readAllLines(logPathFor(sessionId)).pop();
    assert.equal(line.event, EVENTS.GSD_PHASE_RESOLVED);
    assert.equal(line.phase_id, '07-kodo-logs-cli');
    assert.equal(line.match_heading, 'Phase 7: kodo logs CLI + Event Taxonomy');
    assert.equal(line.mode, 'full');
  });

  it('gsdPhaseResolved emits mode=quick when quick session matches a phase (Phase 11 D-05)', () => {
    const sessionId = 'sess-ev-gpr-q';
    const log = createLogger({ sessionId, minLevel: 'info' });
    gsdPhaseResolved(log, {
      phase_id: '11-quick-mode-recognition-persistence',
      match_heading: 'Phase 11: Quick Mode Recognition & Persistence',
      mode: 'quick',
    });
    const line = readAllLines(logPathFor(sessionId)).pop();
    assert.equal(line.mode, 'quick');
    assert.equal(line.phase_id, '11-quick-mode-recognition-persistence');
  });

  it('gsdBootstrap emits event=gsd.bootstrap + project_path + brief_empty + mode (Phase 11 D-07)', () => {
    const sessionId = 'sess-ev-gb';
    const log = createLogger({ sessionId, minLevel: 'info' });
    gsdBootstrap(log, { project_path: '/tmp/kodo-demo', brief_empty: false, mode: 'full' });
    const line = readAllLines(logPathFor(sessionId)).pop();
    assert.equal(line.event, EVENTS.GSD_BOOTSTRAP);
    assert.equal(line.project_path, '/tmp/kodo-demo');
    assert.equal(line.brief_empty, false);
    assert.equal(line.mode, 'full');
  });

  it('gsdBootstrap emits brief_empty=true + mode=quick when quick session bootstraps (Phase 11 D-07)', () => {
    const sessionId = 'sess-ev-gb-q';
    const log = createLogger({ sessionId, minLevel: 'info' });
    gsdBootstrap(log, { project_path: '/tmp/kodo-demo-q', brief_empty: true, mode: 'quick' });
    const line = readAllLines(logPathFor(sessionId)).pop();
    assert.equal(line.brief_empty, true);
    assert.equal(line.mode, 'quick');
  });
```

Notas:
- El test `it('EVENTS is frozen and contains the 8 canonical types', ...)` (líneas 45-58) NO se toca — la lista de event types no cambia, solo el shape de los payloads.
- Los tests añadidos no son matriz exhaustiva de QUICK-08 (eso vive en Phase 13). Son cobertura mínima para asegurar que las firmas extendidas funcionan en ambos modos.
- El campo `mode` es REQUERIDO (no optional) por el contrato D-05/D-07. Si un caller olvida pasarlo, el campo en el JSON será `undefined` que `JSON.stringify` omite — produciendo silencio. La acceptance criteria del Task 2 (dispatcher) grepea que los 4 callsites pasen `mode: gsdMode`/`mode: 'quick'` literalmente.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/logger-events.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "phase_id: string, match_heading: string, mode: 'full'|'quick'" src/logger-events.js` matches (firma JSDoc extendida de gsdPhaseResolved)
    - `grep -F "project_path: string, brief_empty: boolean, mode: 'full'|'quick'" src/logger-events.js` matches (firma JSDoc extendida de gsdBootstrap)
    - `grep -c "mode: fields.mode" src/logger-events.js` returns `2` (un mode field en cada body)
    - `grep -F "brief_empty: fields.brief_empty" src/logger-events.js` matches (gsdBootstrap propaga brief_empty)
    - `grep -c "Phase 11" src/logger-events.js` returns at least `2` (comentarios D-05 y D-07)
    - `node --check src/logger-events.js` exits 0
    - `node --check test/logger-events.test.js` exits 0
    - `node --test test/logger-events.test.js` exits 0 con todas las assertions pasando, incluyendo las 5 nuevas/extendidas
    - El test `EVENTS is frozen and contains the 8 canonical types` sigue verde
    - `grep -c "assert.equal(line.mode" test/logger-events.test.js` returns at least `4` (cobertura mode en los 4 nuevos cases)
  </acceptance_criteria>
  <done>
    `gsdPhaseResolved` y `gsdBootstrap` aceptan y emiten el campo `mode: 'full'|'quick'` con tipo JSDoc estricto. `gsdBootstrap` también acepta `brief_empty` (reconcilia el shape que el dispatcher ya emitía literalmente en Phase 9). `test/logger-events.test.js` cubre las 4 nuevas/extendidas assertions: ambos modos para `gsdPhaseResolved`, ambos modos × ambos `brief_empty` para `gsdBootstrap`. La taxonomía cerrada `EVENTS` permanece intacta.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Telemetría dispatcher con mode + tolerated info emit + lift gsdBootstrap (D-05/D-06/D-07)</name>
  <files>src/triggers/dispatcher.js</files>
  <read_first>
    - src/triggers/dispatcher.js (todo el archivo — focal: líneas 5 import, 70-74 derivación gsdMode, 147-226 toda la sección de resolver verdict y emits; cuatro callsites a modificar/añadir conviven en este bloque)
    - src/logger-events.js (después de Task 1, las firmas nuevas de gsdPhaseResolved y gsdBootstrap deben estar ya extendidas — Task 2 las CONSUME, no las define)
    - .planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md (decisions D-05/D-06/D-07; D-14 carry-forward de Phase 9 es invariante a preservar)
    - .planning/phases/11-quick-mode-recognition-persistence/11-PATTERNS.md (sección 4 "src/triggers/dispatcher.js:147+ extend telemetry payloads with mode" — bloques A/B/C/D verbatim a aplicar)
  </read_first>
  <behavior>
    - Test 1: dispatchTrigger sobre task kodo:gsd-quick con resolver verdict 'phase' ⇒ NDJSON line con event=gsd.phase.resolved, phase_id, match_heading, mode='quick'; el Session record persistido descarta phase_id (ya cubierto en WIP línea 157-160)
    - Test 2: dispatchTrigger sobre task kodo:gsd con resolver verdict 'phase' ⇒ NDJSON line con mode='full'; Session record persiste phase_id
    - Test 3: dispatchTrigger sobre task kodo:gsd-quick con resolver verdict error code='no-match' ⇒ NDJSON line info con event=gsd.phase.resolved, matched=false, code='no-match', tolerated=true, mode='quick', task_ref; dispatch CONTINÚA al launch (returns action='launched')
    - Test 4: dispatchTrigger sobre task kodo:gsd con resolver verdict error code='roadmap-missing' ⇒ NDJSON line warn con event=gsd.phase.resolved, matched=false, error_code='roadmap-missing', detail, task_ref, mode='full'; dispatch ABORTA (returns action='resolver_failed')
    - Test 5: dispatchTrigger sobre task kodo:gsd-quick con resolver verdict error code='roadmap-missing' ⇒ NDJSON line warn con error_code='roadmap-missing' + mode='quick'; dispatch ABORTA (no se tolera; solo no-match es tolerado en quick)
    - Test 6: dispatchTrigger sobre task kodo:gsd-quick con resolver verdict 'bootstrap' ⇒ NDJSON line con event=gsd.bootstrap, project_path, brief_empty, mode='quick'; emitido por el helper gsdBootstrap (no por log.info literal)
    (Tests viven en Phase 13 QUICK-08; este plan solo asegura los comportamientos.)
  </behavior>
  <action>
Cuatro modificaciones puntuales, todas dentro del bloque `if (gsdMode && gsdProjectPath) { ... }` (líneas 151-226). NO modificar imports estáticos — `gsdPhaseResolved` ya se importa dinámicamente en línea 205 y se sigue usando vía dynamic import; `gsdBootstrap` se añadirá al mismo dynamic import block.

---

Modificación A: gsd.phase.resolved success branch — añadir mode (D-05).

Líneas 210-214 actuales:
```javascript
      if (resolverVerdict.action === 'phase') {
        gsdPhaseResolved(log, {
          phase_id: resolverVerdict.phase_id,
          match_heading: resolverVerdict.match_heading,
        });
      }
```

Reemplazar por:
```javascript
      if (resolverVerdict.action === 'phase') {
        // D-05: mode in payload — emit phase_id + match_heading even in quick
        // mode (forensic: operator can see "resolver matched phase X but session
        // is phase-agnostic"). Session record itself drops phase_id when quick
        // (see line 157-160 of this file).
        gsdPhaseResolved(log, {
          phase_id: resolverVerdict.phase_id,
          match_heading: resolverVerdict.match_heading,
          mode: gsdMode,  // 'full' | 'quick' — never null inside if(gsdMode && ...)
        });
      }
```

---

Modificación B: gsd.bootstrap literal lift al helper gsdBootstrap con mode (D-07 + D-14 single-source).

Líneas 215-222 actuales:
```javascript
      } else if (resolverVerdict.action === 'bootstrap') {
        // Include brief_empty flag per D-12 for operator visibility.
        log.info('gsd.bootstrap', {
          event: 'gsd.bootstrap',
          project_path: gsdProjectPath,
          brief_empty: isBriefEmpty(task),
        });
      }
```

Reemplazar por:
```javascript
      } else if (resolverVerdict.action === 'bootstrap') {
        // D-07: mode in payload — homogeneous schema for kodo logs filtering.
        // D-14 (Phase 9 invariant) + Phase 11 lift: emit via the typed helper
        // gsdBootstrap (closed taxonomy) instead of the literal log.info — the
        // helper already exists in src/logger-events.js and accepts brief_empty.
        gsdBootstrap(log, {
          project_path: gsdProjectPath,
          brief_empty: isBriefEmpty(task),
          mode: gsdMode,  // 'full' | 'quick'
        });
      }
```

---

Modificación C: añadir gsdBootstrap al dynamic import line 205.

Línea 205 actual:
```javascript
      const { gsdPhaseResolved } = await import('../logger-events.js');
```

Reemplazar por:
```javascript
      const { gsdPhaseResolved, gsdBootstrap } = await import('../logger-events.js');
```

---

Modificación D: warn emit (fail-closed roadmap-missing/multi-match) — añadir mode.

Líneas 185-191 actuales:
```javascript
          log.warn('gsd.phase.resolved', {
            event: 'gsd.phase.resolved',
            matched: false,
            error_code: resolverVerdict.code,
            detail: resolverVerdict.detail,
            task_ref: task.ref,
          });
```

Reemplazar por:
```javascript
          log.warn('gsd.phase.resolved', {
            event: 'gsd.phase.resolved',
            matched: false,
            error_code: resolverVerdict.code,
            detail: resolverVerdict.detail,
            task_ref: task.ref,
            mode: gsdMode,  // D-07 schema homogeneity: warn fail-closed also distinguishes mode
          });
```

Decisión explícita field-name (`error_code` vs `code`): la warn conserva `error_code` (no se cambia a `code`). Los readers existentes (`kodo logs --json` y los tests que graban este evento) consumen `error_code`. La info emit nueva (Modificación E abajo) usará `code: 'no-match'` + `tolerated: true` por dos razones:
1. D-06 lo especifica verbatim ("emite gsd.phase.resolved {matched: false, code: 'no-match', tolerated: true, mode: 'quick'}")
2. Diferencia semántica explícita: `error_code` significa "fail-closed, dispatch abortó"; `code` + `tolerated:true` significa "condición tolerada, dispatch continuó". Reconciliar a un solo nombre rompería esa diferencia.

---

Modificación E: nuevo info emit en quick + no-match (D-06 — el cuarto callsite).

Líneas 165-171 actuales (inicio del case 'error'):
```javascript
      case 'error':
        // Quick mode tolerates 'no-match' — `/gsd-quick` is meant for one-off
        // tasks not necessarily tied to a ROADMAP phase. roadmap-missing and
        // multi-match are still data-quality errors that fail closed.
        if (gsdMode === 'quick' && resolverVerdict.code === 'no-match') {
          break;
        }
```

Reemplazar el `if` (3 líneas) por el bloque que emite info ANTES de hacer break:

```javascript
      case 'error':
        // Quick mode tolerates 'no-match' — `/gsd-quick` is meant for one-off
        // tasks not necessarily tied to a ROADMAP phase. roadmap-missing and
        // multi-match are still data-quality errors that fail closed.
        if (gsdMode === 'quick' && resolverVerdict.code === 'no-match') {
          // D-06: quick + no-match is tolerated, not silent. Emit info-level
          // gsd.phase.resolved {matched:false, code:'no-match', tolerated:true,
          // mode:'quick'} for forensic reconstruction by `kodo logs --session-of`.
          // Dispatcher remains the single source of gsd.phase.resolved (D-14
          // Phase 9 invariant preserved). Field name `code` (not `error_code`)
          // distinguishes this tolerated condition from the fail-closed warn
          // emit below which uses `error_code`.
          try {
            const { createLogger } = await import('../logger.js');
            const log = createLogger({
              sessionId: gsdSessionId || 'dispatch',
              minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
            }).child({ component: 'dispatcher', task_id: task.id });
            log.info('gsd.phase.resolved', {
              event: 'gsd.phase.resolved',
              matched: false,
              code: 'no-match',
              tolerated: true,
              mode: 'quick',
              task_ref: task.ref,
            });
          } catch {
            // silent — never block dispatch on logger failure (mirror existing
            // forensic warn pattern at lines 192-194 of this file)
          }
          break;
        }
```

Notas operativas para el ejecutor:
- El bloque insertado mantiene la semántica del switch: el `break` queda dentro del `if`, el `case 'error'` continúa al fail-closed cuando NO se cumple `quick + no-match`. NO añadir un `break` extra fuera del `if` — eso saldría del switch sin ejecutar el fail-closed (regresión).
- El dynamic import de `createLogger` es paralelo al de las líneas 180-184 (warn emit). NO compartir la variable `log` entre los dos bloques — son `try` independientes y la variable está scope-local. Coste: dos imports (~µs en re-imports cacheados por Node).
- `mode: 'quick'` es literal en este emit (no `mode: gsdMode`). Es correcto: el guard `gsdMode === 'quick'` ya garantiza el valor. Literal facilita greps y deja el contrato auto-documentado.
- Hard-code `task_ref: task.ref` también — espejo del warn emit (línea 190) para que el operador pueda greppear por task ref en ambos eventos.

---

Self-check post-modificaciones (qué debería verse al final):
- Línea 5: import sin cambios (`getGsdMode` ya está importado)
- Línea 74: `const gsdMode = getGsdMode(kodoConfig.flags);` sin cambios
- Líneas 165-198 (aproximadamente, líneas pueden desplazarse por la inserción): el `case 'error'` empieza con el comentario, luego el `if` extendido con el info emit + break, luego cae al fail-closed que ahora también incluye `mode: gsdMode`
- Línea ~205: dynamic import incluye `gsdBootstrap`
- Líneas ~210-216: `gsdPhaseResolved(log, { ..., mode: gsdMode })`
- Líneas ~217-225: `gsdBootstrap(log, { project_path, brief_empty, mode: gsdMode })` (no más `log.info('gsd.bootstrap', ...)` literal)
- Total: 4 ocurrencias del campo `mode` en el archivo dispatcher (success, bootstrap, fail-closed warn, tolerated info)

Lo que NO se toca:
- La derivación de `gsdMode` línea 74
- El switch case 'phase' lógica (líneas 154-160) — el discard de phase_id en quick ya está en WIP
- El switch case 'bootstrap' lógica (líneas 161-164) — buildBriefFromTask sin cambios
- El return 'resolver_failed' (líneas 196-200) — comportamiento fail-closed sin cambios
- Cualquier callsite de acquireGsdLockFn, releaseGsdLockFn, launchWorkItemFn
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --check src/triggers/dispatcher.js && node --test test/dispatcher.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/triggers/dispatcher.js` exits 0 (syntax OK tras todas las inserciones)
    - `grep -cE "mode: (gsdMode|'quick'|'full')" src/triggers/dispatcher.js` returns at least `4` (los 4 callsites: success vía gsdPhaseResolved, bootstrap vía gsdBootstrap, warn fail-closed, info tolerated)
    - `grep -F "{ gsdPhaseResolved, gsdBootstrap } = await import('../logger-events.js')" src/triggers/dispatcher.js` matches (dynamic import extendido)
    - `grep -F "gsdBootstrap(log, {" src/triggers/dispatcher.js` matches (helper en uso, no literal)
    - `grep -cF "log.info('gsd.bootstrap'" src/triggers/dispatcher.js` returns `0` (literal eliminado)
    - `grep -F "tolerated: true" src/triggers/dispatcher.js` matches (D-06 info emit presente)
    - `grep -F "code: 'no-match'" src/triggers/dispatcher.js` matches (D-06 field name)
    - `grep -F "error_code: resolverVerdict.code" src/triggers/dispatcher.js` matches (warn fail-closed conserva error_code — divergencia intencional)
    - `grep -c "matched: false" src/triggers/dispatcher.js` returns `2` (un emit warn fail-closed + un emit info tolerated)
    - `grep -c "log.info('gsd.phase.resolved'" src/triggers/dispatcher.js` returns `1` (solo el nuevo info emit; el success usa el helper)
    - `grep -c "log.warn('gsd.phase.resolved'" src/triggers/dispatcher.js` returns `1` (warn fail-closed sin cambios estructurales)
    - `node --test test/dispatcher.test.js` exits 0 (tests existentes siguen pasando — no rompen al añadir el campo `mode`)
    - `node --test test/logger-events.test.js` exits 0 (Plan 03 Task 1 sigue verde tras Task 2)
    - El bloque `<verify>` automated del Task 2 ejecuta sin errores
  </acceptance_criteria>
  <done>
    El dispatcher emite `mode: 'full'|'quick'` en los 4 callsites GSD (success vía `gsdPhaseResolved`, bootstrap vía `gsdBootstrap`, warn fail-closed, nuevo info tolerated). El literal `log.info('gsd.bootstrap', ...)` queda lifted al helper `gsdBootstrap`, completando la migración a la taxonomía cerrada (D-14). Una task quick con resolver `no-match` deja rastro forense (info emit) en lugar de silencio. La suite de tests existentes (`dispatcher.test.js`, `logger-events.test.js`) sigue verde.
  </done>
</task>

</tasks>

<task_ordering>
Task 1 (logger-events helper signatures) DEBE ejecutarse antes que Task 2 (dispatcher consumers). Si Task 2 corre primero, el dispatcher pasaría `mode` a un helper que aún ignora el campo (silencio en NDJSON) y `gsdBootstrap` rechazaría/ignoraría `brief_empty`. Aplicar Task 1 → validar `node --test test/logger-events.test.js` verde → aplicar Task 2 → validar `node --test test/dispatcher.test.js` verde.
</task_ordering>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry payload (NDJSON sink) | Logger-events helpers escriben en `~/.kodo/logs/<session>.ndjson`. El campo `mode` es un enum constante derivado del label de Plane — no input arbitrario del operador. |
| Dispatcher → logger dynamic imports | `await import('../logger.js')` y `await import('../logger-events.js')` ya estaban en producción; este plan amplía el namespace (añade `gsdBootstrap`) pero no introduce nuevas rutas de carga. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | Telemetry payload extension (`mode` field) | accept | `mode` es un enum constante (`'full'|'quick'`), no PII. Los logs NDJSON son locales (`~/.kodo/logs/`) y ya contienen `task_id`, `task_ref`, `project_path` — añadir un enum no amplía la superficie de información sensible. |
| T-11-08 | Tampering | `mode: gsdMode` en el guard `if (gsdMode && gsdProjectPath)` | mitigate | El emit de `mode` está dentro de un bloque que ya garantiza `gsdMode !== null` (línea 151). Dentro del bloque, `gsdMode` es uno de `'full'|'quick'`. No hay rama donde `mode: undefined` se cuele al NDJSON. El `mode: 'quick'` literal del info emit (no-match) está blindado por el guard `gsdMode === 'quick'`. |
| T-11-09 | Repudiation | `quick + no-match` previamente silencioso, ahora emite info | mitigate | El nuevo emit cumple D-06: deja rastro auditable para `kodo logs --session-of <task>`. Reduce la superficie de repudio ("¿por qué arrancó esta sesión sin phase_id?") sin introducir riesgo nuevo — el evento usa el mismo sink y redactor de Phase 6/7. |
| T-11-10 | Denial of Service | Dynamic import de logger.js dos veces en error branch (warn + nuevo info) | accept | Node cachea los módulos importados — el segundo `await import('../logger.js')` es un lookup en el module cache, no I/O de disco. Coste despreciable. El `try/catch` con silent rescue ya estaba en el patrón Phase 9 y se mantiene: una falla del logger no bloquea dispatch. |
</threat_model>

<verification>
- `node --check src/logger-events.js` exits 0
- `node --check src/triggers/dispatcher.js` exits 0
- `node --check test/logger-events.test.js` exits 0
- `node --test test/logger-events.test.js` exits 0 (5+ assertions nuevas/extendidas pasan)
- `node --test test/dispatcher.test.js` exits 0 (tests existentes siguen pasando con el campo `mode` añadido — los assertions actuales no comprueban `mode`, así que el nuevo field es aditivo)
- `node --test` (suite completa) exits 0 con 0 fallos vs el estado pre-plan
- `grep -F "log.info('gsd.bootstrap'" src/triggers/dispatcher.js` no matches (literal eliminado del archivo)
- `grep -cE "mode:" src/triggers/dispatcher.js` returns at least 4
- `grep -c "tolerated: true" src/triggers/dispatcher.js` returns 1 (un solo info emit tolerated)
- Suite completa: si Plan 02 corrió antes, `node --test test/manager.test.js` también verde — ningún plan rompe a otro.
</verification>

<success_criteria>
- 4 emits GSD en el dispatcher llevan `mode: 'full'|'quick'`: success (vía helper), bootstrap (vía helper, ya no literal), warn fail-closed, nuevo info tolerated
- El info emit `quick + no-match` rompe el silencio: emite event=gsd.phase.resolved nivel info con `tolerated: true`, `code: 'no-match'`, `mode: 'quick'`, `task_ref`
- El warn emit fail-closed conserva `error_code` (compat con readers de Phase 7) y suma `mode`
- El literal `log.info('gsd.bootstrap', ...)` está eliminado; el dispatcher usa exclusivamente el helper `gsdBootstrap`
- Las firmas tipadas de `gsdPhaseResolved` y `gsdBootstrap` aceptan `mode` como required field
- `test/logger-events.test.js` cubre ambos modos para ambos helpers (4+ aserciones nuevas)
- D-14 (dispatcher = única fuente de gsd.phase.resolved y gsd.bootstrap) sigue intacto: ningún otro módulo emite estos eventos
- La taxonomía cerrada `EVENTS` (`src/logger-events.js:30-39`) no añade tipos nuevos — solo schema homogéneo con `mode` (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/11-quick-mode-recognition-persistence/11-03-SUMMARY.md` con:
- Lista de los 4 callsites del dispatcher con su NDJSON shape final (success, bootstrap, warn, info tolerated)
- Confirmación del lift `log.info('gsd.bootstrap', ...)` → `gsdBootstrap(log, ...)`
- Confirmación de la divergencia intencional `error_code` (warn) vs `code`+`tolerated` (info) y por qué se mantiene
- Conteo final de tests verdes (`node --test` total — debería seguir igual que el baseline pre-Phase 11 + las 4 nuevas/extendidas en logger-events)
- Hand-off para Phase 12: "todos los eventos GSD del dispatcher distinguen modo; los hooks de Phase 12 pueden filtrar logs por `mode` para reconstruir comportamiento"
- Hand-off para Phase 13 QUICK-08: "test/dispatcher.test.js necesita añadir matriz quick × {match, no-match, roadmap-missing, multi-match, bootstrap}; test/manager.test.js necesita la matriz quick × {flags variants}"
</output>
