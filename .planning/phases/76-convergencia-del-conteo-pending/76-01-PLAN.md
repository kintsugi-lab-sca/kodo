---
phase: 76-convergencia-del-conteo-pending
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tasks/pending.js
  - test/tasks/pending.test.js
  - test/check-isolation.test.js
autonomous: true
requirements: [ORCH-05, ORCH-06]

must_haves:
  truths:
    - "fetchFreshPending(fn) devuelve el resultado de fn() y propaga cualquier throw sin capturarlo — un solo punto de fetch (convergencia ORCH-05, D-01)."
    - "createPendingResolver({listPendingTasksFn, ttlMs, now}).resolve() sirve la caché dentro de TTL como {stale:false} SIN re-fetch (el call-counter del fake provider no avanza) (D-01, D-03)."
    - "Tras un fetch fallido con caché previa, resolve() devuelve {tasks: last-known-good, stale:true, fetched_at: el del último éxito} — fetched_at NO avanza al momento del fallo (ORCH-06, Pitfall 3)."
    - "En cold-start con provider caído (nunca hubo éxito), resolve() devuelve {tasks:[], fetched_at:null, stale:true} — nunca [] presentado como verdad fresca (ORCH-06, D-04)."
    - "buildPendingStatusFields({tasks, fetched_at, stale}) devuelve {pending, pending_count, pending_stale, pending_fetched_at} con pending_count === pending.length en ambas ramas (D-05, Pitfall 4)."
    - "src/tasks/pending.js es una hoja de CERO imports, verificado por el guard extendido en test/check-isolation.test.js (D-02, D-09)."
  artifacts:
    - src/tasks/pending.js
    - test/tasks/pending.test.js
  key_links:
    - "test/check-isolation.test.js afirma cero imports en src/tasks/pending.js (mismo bloque que el guard de handoff.js, líneas 160-177)."
  prohibitions:
    - "El módulo NO emite console.warn, logs ni eventos — cualquier import de logger/logger-events contaminaría el grafo de kodo check (LOG-12); el caller emite, el módulo devuelve."
    - "El literal del TTL (30s) NO se declara dentro del módulo — se recibe por parámetro ttlMs (no second number, D-03)."
    - "El fetched_at servido en fallo NUNCA es now() — siempre el timestamp del último fetch exitoso (no presentar dato viejo como reciente)."
    - "El módulo NUNCA colapsa el resultado a un único valor de error — la frescura se discrimina en {stale} (D-04), no se pierde información."
---

<objective>
Crear la hoja de cero imports `src/tasks/pending.js` — la fuente única de verdad del carril de lectura de `pending`, con la política de frescura discriminada que ORCH-05 (convergencia de código) y ORCH-06 (fallo etiquetado, no servido como fresco) exigen. Espejo exacto del patrón factory+DI+TTL+fail-open de `src/server/provider-state.js` (Phase 40), adaptado a un fetch de lista completa (no per-sesión) y con resultado `{tasks, fetched_at, stale}` en vez de `{state, reason}` (D-01, D-04).

Este es el productor del milestone-de-la-fase: sin este módulo, ni `server.js` ni `check.js` (Plan 02) tienen de dónde converger. El módulo se construye y se verifica en verde de forma aislada ANTES de tocar cualquier consumidor.

Purpose: Extraer la lógica de fetch+caché+frescura de `pending` a un módulo puro, testeable sin arrancar el servidor HTTP, blindado como hoja para que el grafo de `kodo check` siga pasando LOG-12 (D-02).
Output: `src/tasks/pending.js` (3 funciones puras exportadas), `test/tasks/pending.test.js` (unit completo), y el guard de aislamiento extendido en `test/check-isolation.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-convergencia-del-conteo-pending/76-CONTEXT.md
@.planning/phases/76-convergencia-del-conteo-pending/76-RESEARCH.md
@.planning/phases/76-convergencia-del-conteo-pending/76-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos NUEVOS creados por este plan (la revisión de convergencia los excluye de la verificación de drift):

- `src/tasks/pending.js` — fichero nuevo, hoja de CERO imports.
- `fetchFreshPending(listPendingTasksFn)` — función exportada; `await listPendingTasksFn()`, puede lanzar (punto de convergencia ORCH-05).
- `createPendingResolver({ listPendingTasksFn, ttlMs, now })` — factory exportada; devuelve `{ resolve() }`. `resolve()` nunca lanza; devuelve `{ tasks, fetched_at, stale }`.
- `buildPendingStatusFields({ tasks, fetched_at, stale })` — función pura exportada; devuelve `{ pending, pending_count, pending_stale, pending_fetched_at }` (shaper del payload de `/status`, consumido por server.js en el Plan 02).
- `test/tasks/pending.test.js` — fichero de test nuevo (directorio `test/tasks/` nuevo).
- Bloque `describe('D-... pending contract isolation', ...)` nuevo en `test/check-isolation.test.js`.
</artifacts_this_phase_produces>

<flagged_assumptions>
El reporte de cobertura de edge (spec-less probe fallback) clasificó **ORCH-05 y ORCH-06 como `unclassified → unresolved`** (no hay SPEC.md con `## Edge Coverage`). NO se descartan en silencio: los `must_haves.truths` y `must_haves.prohibitions` de arriba se derivaron a mano de los Success Criteria de la Phase 76 en ROADMAP.md y de las decisiones LOCKED D-01..D-09 de CONTEXT.md. Assunciones abiertas que el ejecutor debe respetar:

- **A1 (shape de tarea):** cada tarea pending tiene `{ref, title, url, state, projectName}` — verificado en `server.js:651`; `buildPendingStatusFields` lo preserva verbatim. Riesgo bajo.
- **A2 (método de contrato):** `provider.listPendingTasks()` es el único método que ambos caminos usan para pending — FROZEN en el contrato TaskProvider. Riesgo bajo.
- **A3 (ubicación):** `src/tasks/` (neutral) elegido sobre `src/server/` para no sugerir acoplamiento al server que `check.js` no tiene (Claude's Discretion explícita, ambas pasan check-isolation si son hoja).
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold RED — test/tasks/pending.test.js</name>
  <files>test/tasks/pending.test.js</files>
  <read_first>
    - test/server/provider-state.test.js (analog EXACTO: estilo DI, makeSpyLogger/makeProvider con spy + call-counter, `now` inyectable como `() => 1000` para TTL determinista — líneas 35-48, 90-112)
    - .planning/phases/76-convergencia-del-conteo-pending/76-PATTERNS.md (sección «test/tasks/pending.test.js (NEW — unit)» — casos requeridos D-08)
    - .planning/phases/76-convergencia-del-conteo-pending/76-RESEARCH.md (Common Pitfalls 1-4; Pattern 1 y Pattern 2)
  </read_first>
  <behavior>
    Casos que el test debe cubrir (todos deben FALLAR ahora — el módulo aún no existe):
    - fetchFreshPending propaga el throw: con un `listPendingTasksFn` que lanza, `fetchFreshPending` rechaza (no captura) — modo crudo para check.js (D-07).
    - fetchFreshPending devuelve la lista tal cual cuando `listPendingTasksFn` resuelve.
    - resolver — TTL fresh hit: dos resolve() consecutivos dentro de ttlMs → el fake `listPendingTasks` se llama UNA vez (call-counter == 1); el segundo devuelve {stale:false}.
    - resolver — TTL expirado: resolve() tras avanzar `now` más allá de ttlMs → re-fetch (call-counter == 2).
    - resolver — catch con caché previa: primer resolve() OK (guarda fetched_at=T0), segundo con `listPendingTasksFn` que lanza y `now` avanzado > ttlMs → devuelve {tasks: los de T0, stale:true, fetched_at === T0} (assert que fetched_at NO avanzó — Pitfall 3).
    - resolver — cold-start caído: primer resolve() con `listPendingTasksFn` que lanza y sin caché → {tasks:[], fetched_at:null, stale:true}.
    - buildPendingStatusFields — rama fresca: input {tasks:[{ref,title,url,state,projectName}...], fetched_at:'ISO', stale:false} → {pending: mapeado, pending_count === pending.length, pending_stale:false, pending_fetched_at:'ISO'}.
    - buildPendingStatusFields — rama stale/cold: input {tasks:[], fetched_at:null, stale:true} → {pending:[], pending_count:0, pending_stale:true, pending_fetched_at:null}.
  </behavior>
  <action>
    Crear el directorio `test/tasks/` y el fichero `test/tasks/pending.test.js`. Importar `describe, it` de `node:test` y `assert` de `node:assert/strict`. Importar `fetchFreshPending, createPendingResolver, buildPendingStatusFields` desde `../../src/tasks/pending.js`. Copiar el estilo de fake+spy de `test/server/provider-state.test.js`: un `makeProvider` adaptado cuyo método espiado es `listPendingTasks` (devuelve array o lanza) con un getter `calls` que cuenta invocaciones, y un `now` mutable inyectado como closure (`let t = 1000; now: () => t`) para avanzar el reloj sin timers reales. Escribir los 8 casos del bloque `<behavior>`. Para el caso Pitfall 3, capturar `fetched_at` del primer resolve() exitoso y assertear con igualdad estricta que el `fetched_at` del segundo (stale) es EL MISMO string. NO usar timers reales ni `setTimeout`. Estos tests deben FALLAR ahora (RED) porque `src/tasks/pending.js` todavía no existe — eso es lo esperado en este task.
  </action>
  <verify>
    <automated>node --test test/tasks/pending.test.js 2>&1 | grep -qE "Cannot find module|not defined|fail" && echo "RED OK (module absent, tests present)" || (echo "expected RED but module resolved" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `test/tasks/pending.test.js` existe e importa las 3 funciones desde `../../src/tasks/pending.js`.
    - Cubre los 8 comportamientos del bloque `<behavior>` (fetchFreshPending propaga throw + resuelve; resolver TTL fresh/expired/catch-labeled/cold-start; buildPendingStatusFields fresh/stale).
    - El caso «catch con caché» assertea con igualdad estricta que `fetched_at` NO avanzó (Pitfall 3).
    - Ejecutar `node --test test/tasks/pending.test.js` falla porque el módulo no existe (estado RED esperado).
    - Cero timers reales / `setTimeout`; el reloj es un `now` inyectado.
  </acceptance_criteria>
  <done>El test unit completo existe y falla en RED por ausencia del módulo bajo prueba.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar src/tasks/pending.js — hoja de cero imports (GREEN)</name>
  <files>src/tasks/pending.js</files>
  <read_first>
    - src/server/provider-state.js (patrón EXACTO a espejar: factory `create*Resolver({deps, ttlMs, now=Date.now})`, closure sobre caché privada, `resolve()` never-throws — líneas 63-122; header/doc-comment líneas 1-21, PERO SIN la línea `import { providerStateFetchFailed }`)
    - src/session/handoff.js (precedente de hoja de CERO imports — el contrato de blindaje que este módulo replica)
    - test/tasks/pending.test.js (el contrato a satisfacer, escrito en el Task 1)
    - .planning/phases/76-convergencia-del-conteo-pending/76-PATTERNS.md (sección «src/tasks/pending.js» con los 3 excerpts)
  </read_first>
  <action>
    Crear `src/tasks/pending.js` como hoja de CERO imports (ni `node:*`, ni relativos — como `src/session/handoff.js`). Cabecera `// @ts-check` + doc-comment que declare: Phase 76 (ORCH-05/ORCH-06), hoja de cero imports para que el grafo de kodo check siga limpio (LOG-12 / check-isolation), fetch + caché TTL + política de frescura del carril pending compartido por server.js (/status) y check.js.

    Exportar tres funciones:
    1. `fetchFreshPending(listPendingTasksFn)` — `async`, hace `return await listPendingTasksFn()`. Punto de convergencia único (ORCH-05, D-01). NO captura el throw — deja que propague (check.js lo consume crudo, D-07).
    2. `createPendingResolver({ listPendingTasksFn, ttlMs, now = Date.now })` — factory que cierra sobre `let cache = null` (`{ tasks, fetched_at }` o null si nunca hubo éxito) y devuelve `{ resolve }`. `resolve()` es `async` y NUNCA lanza:
       (a) cache-hit dentro de TTL (`cache && now() - new Date(cache.fetched_at).getTime() < ttlMs`) → `{ tasks: cache.tasks, fetched_at: cache.fetched_at, stale: false }`.
       (b) fetch fresco vía `await fetchFreshPending(listPendingTasksFn)`; en éxito `fetched_at = new Date(now()).toISOString()`, guarda `cache = { tasks, fetched_at }`, devuelve `{ tasks, fetched_at, stale: false }`.
       (c) en el catch con caché → `{ tasks: cache.tasks, fetched_at: cache.fetched_at, stale: true }` — el `fetched_at` es SIEMPRE `cache.fetched_at` (último éxito), JAMÁS `now()` (Pitfall 3, ORCH-06).
       (d) en el catch sin caché (cold-start caído) → `{ tasks: [], fetched_at: null, stale: true }` (D-04).
    3. `buildPendingStatusFields({ tasks, fetched_at, stale })` — pura, sin estado. Devuelve `{ pending: tasks.map((t) => ({ ref: t.ref, title: t.title, url: t.url, state: t.state, projectName: t.projectName })), pending_count: tasks.length, pending_stale: stale, pending_fetched_at: fetched_at }`. Derivar `pending` y `pending_count` del MISMO `tasks` (Pitfall 4). Preservar el shape de tarea verbatim (A1).

    El resolver NO emite `console.warn` ni logs (D-02, Pitfall 1) — el caller (server.js, Plan 02) inspecciona `stale` y emite el rastro. El dedup in-flight del analog (provider-state.js:66-67,90-118) es OPCIONAL (Claude's Discretion): para un slot único sería un `let inflight = null`; espejarlo solo si se desea, no es requisito.
  </action>
  <verify>
    <automated>node --test test/tasks/pending.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/tasks/pending.js` existe y tiene CERO imports (ninguna línea `import`/`export ... from`).
    - Exporta `fetchFreshPending`, `createPendingResolver`, `buildPendingStatusFields`.
    - `node --test test/tasks/pending.test.js` pasa en verde (los 8 casos).
    - No hay `console.warn`/`console.*` ni literal `30 * 1000` / `30000` dentro del fichero (ttl entra por parámetro).
    - En la rama de fallo con caché, `fetched_at` devuelto === `cache.fetched_at` (test Pitfall 3 verde).
  </acceptance_criteria>
  <done>El módulo implementa las 3 funciones; `test/tasks/pending.test.js` verde; el fichero es hoja de cero imports.</done>
</task>

<task type="auto">
  <name>Task 3: Extender test/check-isolation.test.js — guard de hoja para pending.js</name>
  <files>test/check-isolation.test.js</files>
  <read_first>
    - test/check-isolation.test.js (el fichero completo; reutilizar `extractImports`/`walkImports` YA definidos — NO re-implementar; copiar el bloque `describe('D-13: handoff contract isolation ...')` líneas 160-177 como plantilla)
    - src/tasks/pending.js (el fichero creado en el Task 2, para confirmar la ruta `join(SRC, 'tasks', 'pending.js')`)
  </read_first>
  <action>
    Añadir un nuevo bloque `describe(...)` al final de `test/check-isolation.test.js` que verifique que `src/tasks/pending.js` existe y tiene CERO imports — copiar verbatim la estructura del bloque de `handoff.js` (líneas 161-176), cambiando la ruta a `join(SRC, 'tasks', 'pending.js')` y el mensaje de aserción para citar Phase 76 / D-02 / la razón (que check.js pueda importarlo sin arrastrar deps al grafo LOG-12). Reutilizar `extractImports` (ya en el fichero) y assertear `assert.deepEqual(imports, [])`. NO tocar los bloques existentes (LOG-12 vigilante isolation, handoff D-13) — son invariantes vivos. NO añadir aún la aserción de «pending.js alcanzable desde check.js» — eso llega en el Plan 02 cuando check.js importe el módulo (ahora fallaría porque check.js todavía no lo consume).
  </action>
  <verify>
    <automated>node --test test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El bloque nuevo assertea `existsSync(join(SRC, 'tasks', 'pending.js')) === true` y `extractImports(src) deepEqual []`.
    - Reutiliza `extractImports` existente (no re-declara regex ni walker).
    - Los bloques preexistentes (LOG-12, D-13 handoff) permanecen intactos.
    - `node --test test/check-isolation.test.js` pasa en verde.
  </acceptance_criteria>
  <done>El guard de aislamiento cubre `src/tasks/pending.js` como hoja de cero imports; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider externo (Plane/GitHub) → módulo de lectura | La lista `pending` cruza aquí vía `listPendingTasksFn` (inyectado). El módulo NO llama a red directamente: recibe la función por DI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-01 | Information Disclosure | `src/tasks/pending.js` (fallo del fetch) | low | mitigate | La hoja NUNCA loguea: cero imports, sin `console.*`. El error se colapsa a `stale:true` sin exponer payload ni `err.message`; el caller (Plan 02) emite un warn genérico. Guard de aislamiento verifica cero imports. |
| T-76-04 | Tampering | Semántica de frescura del resolver | medium | mitigate | El `fetched_at` en fallo es SIEMPRE el del último éxito (Pitfall 3), nunca `now()`; `stale` discrimina explícitamente. Test D-08 «catch etiquetado» lo blinda. |
| T-76-SC | Tampering | Instalación npm/pip/cargo | low | accept | N/A — este plan instala CERO paquetes (constraint LOCKED «cero dependencias npm nuevas»). Todo built-in ya presente; sin superficie de supply-chain que auditar. |
</threat_model>

<verification>
- `node --test test/tasks/pending.test.js` verde (8 casos: fetchFreshPending, resolver TTL/stale/cold-start, buildPendingStatusFields ambas ramas).
- `node --test test/check-isolation.test.js` verde (guard de hoja para pending.js + bloques preexistentes intactos).
- Regresión de wave: `node --test test/tasks/pending.test.js test/check.test.js test/check-isolation.test.js` verde.
- Highest severity del threat model = medium (T-76-04), mitigado → sin bloqueo (security_block_on=high).
</verification>

<success_criteria>
- `src/tasks/pending.js` es la fuente única del carril pending: `fetchFreshPending` (fetch), `createPendingResolver` (caché+frescura discriminada), `buildPendingStatusFields` (shaper /status).
- Hoja de cero imports blindada por `test/check-isolation.test.js` (D-02, D-09).
- ORCH-06 cubierto a nivel unidad: fallo con caché → last-known-good etiquetado con `fetched_at` congelado; cold-start caído → `[]`/`null`/`stale:true`.
- Cero deps npm, cero literal de TTL duplicado dentro del módulo.
</success_criteria>

<output>
Create `.planning/phases/76-convergencia-del-conteo-pending/76-01-SUMMARY.md` when done
</output>
