---
phase: 20-hook-01-universal-anti-push-fantasma
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - test/session-start.test.js
  - test/gsd-context.test.js
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
tags:
  - tests
  - hooks
  - golden-bytes
  - coverage-matrix
must_haves:
  truths:
    - "test/session-start.test.js tiene una suite `HOOK-01 — anti-push reminder, no-GSD ES` con asserts de presencia + golden bytes opción B + idempotencia"
    - "test/session-start.test.js QUICK-08 suite extendida con asserts del bloque EN en rama quick"
    - "test/gsd-context.test.js tiene una suite `HOOK-01 — anti-push reminder, GSD EN` con asserts para ramas phase + bootstrap + common-block invariance D-04 + idempotencia"
    - "Los nuevos tests verifican HOOK-01 (presencia × 4 modos), HOOK-02 (prefix bytes opción B), HOOK-03 (matrix completa: no-GSD + quick + phase + bootstrap)"
    - "Todos los tests existentes siguen pasando (no se rompen los QUICK-08 existentes ni los 11 tests buildGsdContext)"
  artifacts:
    - path: "test/session-start.test.js"
      provides: "Suite HOOK-01 (no-GSD ES) + extensión QUICK-08 (quick EN)"
      contains: "describe('HOOK-01 — anti-push reminder, no-GSD ES'"
    - path: "test/gsd-context.test.js"
      provides: "Suite HOOK-01 (GSD EN) cubriendo phase + bootstrap + common-block invariance"
      contains: "describe('HOOK-01 — anti-push reminder, GSD EN'"
  key_links:
    - from: "test/session-start.test.js"
      to: "src/hooks/session-start.js::buildSessionContext"
      via: "import + assert.match con /## Anti-push-fantasma/"
      pattern: "buildSessionContext\\(makeSession\\(\\), makeConfig\\(\\)\\)"
    - from: "test/gsd-context.test.js"
      to: "src/hooks/session-start.js::buildGsdContext"
      via: "import + assert.match con /## No automatic push/ × 3 ramas"
      pattern: "buildGsdContext\\(makeSession\\(\\{[^}]*\\}\\)\\)"
---

<objective>
Extender los 2 archivos de tests existentes (`test/session-start.test.js` y `test/gsd-context.test.js`) con la matrix completa HOOK-01/02/03 para validar el bloque anti-push añadido en Plan 20-01:

- **HOOK-01:** Presencia del bloque en los 4 modos (no-GSD ES + GSD-quick EN + GSD-phase EN + GSD-bootstrap EN). Asserts sobre header, statement y al menos 1 par Bad/Good.
- **HOOK-02:** Golden bytes preservados vía opción B (split-on-header + prefix invariant). Verifica que el bloque va al FINAL y que los bytes anteriores acaban en `'\n\n'` (separador).
- **HOOK-03:** Coverage matrix completa: idempotencia (re-emitir mismo input → mismos bytes), common-block invariance D-04 (3 ramas GSD comparten bloque EN bytes-idéntico), no-emoji enforcement D-02b (slice del bloque sin emojis ni ANSI).

Purpose: Lock-in del comportamiento de Plan 20-01 con asserts deterministas. HOOK-03 es la red de seguridad contra futuras ediciones del prompt que rompan el contrato (ej. mover el bloque a otra posición, eliminar pares Bad/Good, añadir emojis).

Output: ~10-12 tests nuevos repartidos en los 2 archivos. Sin imports nuevos (toda la infra ya existe). Suite global crece de 567 a ~577-579 tests. Tiempo estimado de ejecución del subset: <50ms (builders puros sin I/O).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-CONTEXT.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-RESEARCH.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-PATTERNS.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-01-SUMMARY.md
@src/hooks/session-start.js
@test/session-start.test.js
@test/gsd-context.test.js

<interfaces>
<!-- Fixtures y helpers ya existentes — reusar verbatim, NO inventar -->

test/session-start.test.js (L15-39) — `makeSession()` y `makeConfig()`:
```javascript
function makeSession(overrides = {}) {
  return {
    workspace_ref: 'KL-42', session_id: 'sess-abc', task_id: 'uuid-123',
    task_ref: 'KL-42', provider: 'plane', project_id: 'proj-1',
    summary: 'Fix bug', status: 'running',
    started_at: '2026-04-10T00:00:00.000Z', project_path: '/tmp/kl-42',
    ...overrides,
  };
}

function makeConfig(overrides = {}) {
  return {
    provider: 'plane',
    providers: { plane: { mcp_hint: 'MCP de Plane' } },
    ...overrides,
  };
}
```

test/gsd-context.test.js (L10-25) — `makeSession()` con `gsd: true` default:
```javascript
function makeSession(overrides = {}) {
  return {
    workspace_ref: 'KL-42', session_id: 'sess-abc', task_id: 'uuid-123',
    task_ref: 'KL-42', provider: 'plane', project_id: 'proj-1',
    summary: 'Fix authentication bug', status: 'running',
    started_at: '2026-04-17T00:00:00.000Z', project_path: '/tmp/kl-42',
    gsd: true,
    ...overrides,
  };
}
```

Modos cubiertos por sobrescritura directa (NO crear fixtures nuevas):
- **no-GSD:** `makeSession()` en `test/session-start.test.js` (sin `gsd: true`).
- **GSD quick:** `makeSession({ gsd: true, gsd_mode: 'quick' })` en `test/session-start.test.js` (ya usado en QUICK-08, L101).
- **GSD phase:** `makeSession({ phase_id: '08' })` en `test/gsd-context.test.js` (ya usado, L41).
- **GSD bootstrap:** `makeSession({ phase_id: undefined })` en `test/gsd-context.test.js` (ya usado, L55).

Imports ya presentes (NO añadir nuevos):
- test/session-start.test.js L1-10: `describe`, `it` de `node:test`, `assert` de `node:assert/strict`, `readFileSync` de `node:fs`, `fileURLToPath`/`dirname`/`join` de path, `buildSessionContext`, `buildGsdContext`.
- test/gsd-context.test.js L1-4: `describe`, `it`, `assert`, `buildGsdContext`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender test/session-start.test.js con suite HOOK-01 (no-GSD ES) + extensión QUICK-08 (quick EN)</name>
  <files>test/session-start.test.js</files>
  <read_first>
    - test/session-start.test.js (archivo completo, 299 líneas — entender estructura `describe`/`it` actual)
    - src/hooks/session-start.js (post-Plan 20-01 — verificar bytes exactos del bloque ES y EN)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-PATTERNS.md §"test/session-start.test.js" (esqueleto canónico)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-RESEARCH.md §"Golden Bytes Verification Strategy" (opción B)
  </read_first>
  <behavior>
    - Suite nueva `describe('HOOK-01 — anti-push reminder, no-GSD ES', () => { ... })` contiene 4-5 tests:
      1. Presencia del header `## Anti-push-fantasma`.
      2. Statement + al menos 1 par Bad/Good (D-02 contract).
      3. HOOK-02 golden bytes opción B: lastIndexOf > 0, prefix.endsWith('\n\n'), tail.startsWith(HEADER).
      4. HOOK-03 idempotencia: `buildSessionContext(s, c) === buildSessionContext(s, c)`.
      5. (Opcional D-02b) Slice del bloque sin emojis ni códigos ANSI escape.
    - Suite QUICK-08 existente (línea ~99-180) extendida con 2 tests adicionales:
      1. Presencia del header EN `## No automatic push` + statement EN + ejemplo "Feature deployed".
      2. HOOK-02 golden bytes opción B aplicado a la rama quick.
    - Todos los tests existentes (Test 1-6 + 6 source invariants + QUICK-08 originales) siguen pasando intactos.
  </behavior>
  <action>
APERTURA: leer `test/session-start.test.js` completo. Identificar 3 puntos de inserción:
- **Punto A (línea ~97, después del cierre `});` del `describe('session-start.js — buildSessionContext', ...)`):** insertar la suite nueva `describe('HOOK-01 — anti-push reminder, no-GSD ES', () => { ... });` ANTES del `describe('QUICK-08 — quick mode buildGsdContext', ...)` (línea 99 actual).
- **Punto B (dentro de la suite QUICK-08, al final del `describe` block — línea ~179 actual, justo antes del `});` de cierre de QUICK-08):** añadir los 2 nuevos `it(...)` para HOOK-01 quick + HOOK-02 quick.
- **NO TOCAR** el `describe('session-start.js — source invariants', ...)` (línea 182-299) — sus 6 tests source-hygiene siguen como están.

PUNTO A — insertar literal (después del `});` que cierra la suite buildSessionContext, antes del `describe('QUICK-08 ...)`):

```javascript
describe('HOOK-01 — anti-push reminder, no-GSD ES', () => {
  it('HOOK-01: bloque "## Anti-push-fantasma" presente con header H2', () => {
    const ctx = buildSessionContext(makeSession(), makeConfig());
    assert.match(ctx, /## Anti-push-fantasma/);
  });

  it('HOOK-01 D-02: statement explícito + par Bad/Good presentes', () => {
    const ctx = buildSessionContext(makeSession(), makeConfig());
    assert.match(ctx, /kodo NO hace `git push` automático/);
    assert.match(ctx, /verifica con `git push` real/);
    assert.match(ctx, /Bad: "Feature publicada en producción\."/);
    assert.match(ctx, /Good: "Feature commiteada localmente, pendiente de `git push` al remoto\."/);
    assert.match(ctx, /Bad: "Deploy hecho\."/);
    assert.match(ctx, /Good: "Deploy quedará efectivo una vez se haga `git push origin main`\."/);
  });

  it('HOOK-02 (opción B): bloque al FINAL — prefix bytes intactos + tail starts con header', () => {
    const ctx = buildSessionContext(makeSession(), makeConfig());
    const HEADER = '## Anti-push-fantasma';
    const idx = ctx.lastIndexOf(HEADER);
    assert.ok(idx > 0, `header "${HEADER}" must be present and after byte 0`);
    const tail = ctx.slice(idx);
    assert.ok(tail.startsWith(HEADER), 'header must start the final block');
    const prefix = ctx.slice(0, idx);
    assert.ok(
      prefix.endsWith('\n\n'),
      'prefix must end with blank line separator before HOOK-01 block (D-03)',
    );
  });

  it('HOOK-03 idempotencia: re-emitir produce bytes idénticos', () => {
    const session = makeSession();
    const config = makeConfig();
    const a = buildSessionContext(session, config);
    const b = buildSessionContext(session, config);
    assert.equal(a, b);
    assert.equal(a.length, b.length);
  });

  it('HOOK-01 D-02b: bloque sin emojis ni códigos ANSI escape', () => {
    const ctx = buildSessionContext(makeSession(), makeConfig());
    const HEADER = '## Anti-push-fantasma';
    const block = ctx.slice(ctx.lastIndexOf(HEADER));
    // Sólo verificamos el slice del bloque HOOK-01 — el resto del prompt ES
    // contiene emojis legítimos (✅/📁/⚠️/🔍) en la sección "Comentario final".
    assert.ok(
      !/[\u{1F300}-\u{1FAFF}]/u.test(block),
      'HOOK-01 block must not contain emojis (D-02b)',
    );
    // ESC (\x1B) inicia secuencias ANSI; el bloque es markdown plano.
    // eslint-disable-next-line no-control-regex
    assert.ok(!/\x1B\[/.test(block), 'HOOK-01 block must not contain ANSI escape sequences (D-02b)');
  });
});

```

PUNTO B — dentro del `describe('QUICK-08 — quick mode buildGsdContext', () => { ... })`, antes del `});` final de esa suite (línea ~180 actual), AÑADIR estos 2 `it(...)`:

```javascript
  it('HOOK-01 (quick EN): bloque "## No automatic push" presente con statement + ejemplo', () => {
    const session = makeSession({ gsd: true, gsd_mode: 'quick', summary: 'TASK-X' });
    const ctx = buildGsdContext(session, {});
    assert.match(ctx, /## No automatic push/);
    assert.match(ctx, /kodo does NOT push automatically/);
    assert.match(ctx, /verify with a real `git push`/);
    assert.match(ctx, /Bad: "Feature deployed to production\."/);
    assert.match(ctx, /Good: "Feature committed locally, pending `git push` to remote\."/);
  });

  it('HOOK-02 (quick EN, opción B): bloque al FINAL — prefix bytes intactos', () => {
    const session = makeSession({ gsd: true, gsd_mode: 'quick', summary: 'TASK-X' });
    const ctx = buildGsdContext(session, {});
    const HEADER = '## No automatic push';
    const idx = ctx.lastIndexOf(HEADER);
    assert.ok(idx > 0);
    const tail = ctx.slice(idx);
    assert.ok(tail.startsWith(HEADER), 'header must start the final block (quick branch)');
    const prefix = ctx.slice(0, idx);
    assert.ok(prefix.endsWith('\n\n'), 'prefix must end with blank line separator (D-03)');
  });

```

NO eliminar ningún test existente. NO cambiar la posición del `describe('session-start.js — source invariants', ...)`. NO añadir imports nuevos.

VERIFICAR sintaxis: ejecutar `node --test test/session-start.test.js` y confirmar que el contador total de tests crece en 7 (5 nuevos en HOOK-01 + 2 nuevos en QUICK-08), todos pasan, y los existentes siguen pasando.
  </action>
  <acceptance_criteria>
    - `grep -c "describe('HOOK-01 — anti-push reminder, no-GSD ES'" test/session-start.test.js` devuelve `1`.
    - `grep -c "HOOK-01: bloque \"## Anti-push-fantasma\" presente" test/session-start.test.js` devuelve `1`.
    - `grep -c 'HOOK-02 (opción B): bloque al FINAL' test/session-start.test.js` devuelve `1`.
    - `grep -c 'HOOK-03 idempotencia' test/session-start.test.js` devuelve `1`.
    - `grep -c 'HOOK-01 (quick EN): bloque "## No automatic push"' test/session-start.test.js` devuelve `1`.
    - `grep -c 'HOOK-02 (quick EN, opción B)' test/session-start.test.js` devuelve `1`.
    - `node --test test/session-start.test.js 2>&amp;1 | grep -E '^# tests' | awk '{print $3}'` devuelve un número >= 20 (era 19 pre-Phase 20; añadimos +7 = 26+).
    - `node --test test/session-start.test.js 2>&amp;1 | grep -E '^# pass' | awk '{print $3}'` es igual al total `# tests` (0 fallos).
    - El archivo sigue siendo válido sintácticamente: no errores de parsing del runner.
    - Los 6 tests source invariants en el `describe('session-start.js — source invariants', ...)` (línea 182-299) siguen pasando intactos.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/session-start.test.js 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - Suite `describe('HOOK-01 — anti-push reminder, no-GSD ES', ...)` añadida con 5 tests.
    - Suite `describe('QUICK-08 — quick mode buildGsdContext', ...)` extendida con 2 tests adicionales.
    - Total +7 tests nuevos en este archivo.
    - 0 imports nuevos.
    - Tests existentes (19 pre-Phase 20) intactos en pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender test/gsd-context.test.js con suite HOOK-01 (GSD EN) — ramas phase + bootstrap + common-block invariance + idempotencia</name>
  <files>test/gsd-context.test.js</files>
  <read_first>
    - test/gsd-context.test.js (archivo completo, 110 líneas — entender el `describe('session-start.js — buildGsdContext', ...)`)
    - src/hooks/session-start.js (post-Plan 20-01 — verificar bytes exactos del bloque EN común)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-PATTERNS.md §"test/gsd-context.test.js" (esqueleto canónico)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-CONTEXT.md §D-04 (common-block invariance entre 3 ramas)
  </read_first>
  <behavior>
    - Suite nueva `describe('HOOK-01 — anti-push reminder, GSD EN', () => { ... })` contiene 5-6 tests cubriendo:
      1. HOOK-01 rama phase: presencia del header `## No automatic push` + statement EN.
      2. HOOK-01 rama bootstrap: presencia del header EN.
      3. HOOK-02 rama phase: golden bytes opción B (lastIndexOf > 0, prefix '\n\n', tail starts with header).
      4. HOOK-02 rama bootstrap: golden bytes opción B.
      5. D-04 common-block invariance: tail(quick) === tail(phase) === tail(bootstrap) — los 3 outputs comparten el mismo bloque EN bytes-idéntico.
      6. HOOK-03 idempotencia: `buildGsdContext(session)` re-emitido produce mismos bytes (1 test por rama phase).
    - Todos los tests existentes (11 tests en `describe('session-start.js — buildGsdContext', ...)`) siguen pasando intactos.
    - El test "context is in English (D-04)" existente (L66-71) sigue pasando — el bloque EN nuevo está enteramente en inglés, no contradice "must not contain Spanish".
  </behavior>
  <action>
APERTURA: leer `test/gsd-context.test.js` completo (110 líneas). El archivo tiene 1 sólo `describe` con 11 tests. Punto de inserción: **inmediatamente después del `});` de cierre del describe principal (línea ~110)**.

AÑADIR al FINAL del archivo (después del cierre del describe existente, sin tocar nada arriba):

```javascript

describe('HOOK-01 — anti-push reminder, GSD EN', () => {
  const HEADER = '## No automatic push';

  it('HOOK-01 (phase): bloque "## No automatic push" presente con statement + ejemplo', () => {
    const ctx = buildGsdContext(makeSession({ phase_id: '08' }));
    assert.match(ctx, /## No automatic push/);
    assert.match(ctx, /kodo does NOT push automatically/);
    assert.match(ctx, /verify with a real `git push`/);
    assert.match(ctx, /Bad: "Feature deployed to production\."/);
    assert.match(ctx, /Good: "Feature committed locally, pending `git push` to remote\."/);
    assert.match(ctx, /Bad: "Deploy done\."/);
    assert.match(ctx, /Good: "Deploy will be live once `git push origin main` runs\."/);
  });

  it('HOOK-01 (bootstrap): bloque "## No automatic push" presente', () => {
    const ctx = buildGsdContext(makeSession({ phase_id: undefined }));
    assert.match(ctx, /## No automatic push/);
    assert.match(ctx, /kodo does NOT push automatically/);
    // El bloque común se appendea DESPUÉS de /gsd-new-project (rama bootstrap).
    const cmdIdx = ctx.indexOf('/gsd-new-project');
    const blockIdx = ctx.lastIndexOf(HEADER);
    assert.ok(cmdIdx >= 0, '/gsd-new-project still rendered in bootstrap branch');
    assert.ok(blockIdx > cmdIdx, 'HOOK-01 block must come AFTER bootstrap command');
  });

  it('HOOK-02 (phase, opción B): bloque al FINAL — prefix bytes intactos', () => {
    const ctx = buildGsdContext(makeSession({ phase_id: '08' }));
    const idx = ctx.lastIndexOf(HEADER);
    assert.ok(idx > 0, `header "${HEADER}" must be present and after byte 0`);
    const tail = ctx.slice(idx);
    assert.ok(tail.startsWith(HEADER), 'header must start the final block');
    const prefix = ctx.slice(0, idx);
    assert.ok(
      prefix.endsWith('\n\n'),
      'prefix must end with blank line separator before HOOK-01 block (D-03)',
    );
  });

  it('HOOK-02 (bootstrap, opción B): bloque al FINAL — prefix bytes intactos', () => {
    const ctx = buildGsdContext(makeSession({ phase_id: undefined }));
    const idx = ctx.lastIndexOf(HEADER);
    assert.ok(idx > 0);
    const tail = ctx.slice(idx);
    assert.ok(tail.startsWith(HEADER), 'header must start the final block (bootstrap branch)');
    const prefix = ctx.slice(0, idx);
    assert.ok(prefix.endsWith('\n\n'), 'prefix must end with blank line separator (D-03)');
  });

  it('D-04 common-block invariance: bloque EN bytes-idéntico en las 3 ramas (quick / phase / bootstrap)', () => {
    const ctxQuick = buildGsdContext(makeSession({ gsd_mode: 'quick', summary: 'TASK-X' }));
    const ctxPhase = buildGsdContext(makeSession({ phase_id: '08' }));
    const ctxBoot = buildGsdContext(makeSession({ phase_id: undefined }));
    const tail = (s) => s.slice(s.lastIndexOf(HEADER));
    assert.equal(tail(ctxQuick), tail(ctxPhase), 'quick tail must equal phase tail');
    assert.equal(tail(ctxPhase), tail(ctxBoot), 'phase tail must equal bootstrap tail');
    // Sanidad: el tail no está vacío y empieza con el header.
    assert.ok(tail(ctxPhase).startsWith(HEADER), 'tail must start with HEADER');
    assert.ok(tail(ctxPhase).length > HEADER.length, 'tail must include block body');
  });

  it('HOOK-03 idempotencia (phase): re-emitir produce bytes idénticos', () => {
    const session = makeSession({ phase_id: '08' });
    const a = buildGsdContext(session);
    const b = buildGsdContext(session);
    assert.equal(a, b);
    assert.equal(a.length, b.length);
  });

  it('HOOK-03 idempotencia (bootstrap): re-emitir produce bytes idénticos', () => {
    const session = makeSession({ phase_id: undefined });
    const a = buildGsdContext(session);
    const b = buildGsdContext(session);
    assert.equal(a, b);
  });
});
```

NO modificar:
- El `describe('session-start.js — buildGsdContext', ...)` existente (L27-110) — sus 11 tests permanecen intactos.
- El test "context is in English (D-04)" (L66-71) — el bloque EN nuevo es 100% inglés, NO contradice ese assert.
- El test "does NOT include generic instructions (D-03)" (L73-78) — el bloque EN NO menciona "comenta tu plan", "In Review" ni "mcp_hint", así que sigue verde.
- Los imports L1-4 — todo lo necesario (`describe`, `it`, `assert`, `buildGsdContext`) ya está importado.

VERIFICAR: `node --test test/gsd-context.test.js` muestra todos los tests pasando (11 originales + 7 nuevos = 18 mínimo).
  </action>
  <acceptance_criteria>
    - `grep -c "describe('HOOK-01 — anti-push reminder, GSD EN'" test/gsd-context.test.js` devuelve `1`.
    - `grep -c 'HOOK-01 (phase): bloque "## No automatic push" presente' test/gsd-context.test.js` devuelve `1`.
    - `grep -c 'HOOK-01 (bootstrap): bloque "## No automatic push" presente' test/gsd-context.test.js` devuelve `1`.
    - `grep -c 'D-04 common-block invariance' test/gsd-context.test.js` devuelve `1`.
    - `grep -c 'HOOK-03 idempotencia (phase)' test/gsd-context.test.js` devuelve `1`.
    - `grep -c 'HOOK-03 idempotencia (bootstrap)' test/gsd-context.test.js` devuelve `1`.
    - `node --test test/gsd-context.test.js 2>&amp;1 | grep -E '^# tests' | awk '{print $3}'` devuelve >= 18 (era 11, añadimos +7).
    - `node --test test/gsd-context.test.js 2>&amp;1 | grep -E '^# pass' | awk '{print $3}'` igual al total (0 fallos).
    - Los 11 tests existentes siguen verdes — específicamente `context is in English (D-04)` y `does NOT include generic instructions (D-03)` no son afectados por el append EN.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/gsd-context.test.js 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - Suite `describe('HOOK-01 — anti-push reminder, GSD EN', ...)` añadida al final del archivo con 7 tests (phase presencia, bootstrap presencia, phase HOOK-02, bootstrap HOOK-02, D-04 common-block, idempotencia phase, idempotencia bootstrap).
    - 0 imports nuevos (toda la infra ya estaba).
    - 0 cambios en los 11 tests originales.
    - `node --test test/gsd-context.test.js` muestra 18+ tests verdes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar suite global verde y matrix completa HOOK-01/02/03</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - test/session-start.test.js (post-Task 1)
    - test/gsd-context.test.js (post-Task 2)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-RESEARCH.md §"Validation Architecture" (test framework + sampling rate)
  </read_first>
  <action>
EJECUTAR la suite global y confirmar 0 regresiones:

1. **Subset Phase 20:**
```bash
node --test test/session-start.test.js test/gsd-context.test.js
```
Confirmar: `# tests N`, `# pass N` (cero fallos), `# fail 0`.

2. **Suite global:**
```bash
npm test
```
Confirmar: total de tests pre-Phase 20 (567 según STATE.md) + delta esperado (~14 tests nuevos = 7 en cada archivo) → ~580+ pass. Skip esperado: 1 (startup-budget pre-existente). Fail esperado: 0.

3. **Coverage matrix HOOK-01/02/03 (manual cross-check):**

| Req     | no-GSD ES | GSD quick EN | GSD phase EN | GSD bootstrap EN |
|---------|-----------|--------------|--------------|------------------|
| HOOK-01 (presencia) | session-start.test.js | session-start.test.js (QUICK-08 ext) | gsd-context.test.js | gsd-context.test.js |
| HOOK-02 (prefix bytes opción B) | session-start.test.js | session-start.test.js (QUICK-08 ext) | gsd-context.test.js | gsd-context.test.js |
| HOOK-03 (idempotencia) | session-start.test.js | (cubierto por common-block invariance) | gsd-context.test.js | gsd-context.test.js |
| D-04 common-block | — | cubierto por common-block invariance test | cubierto | cubierto |
| D-02b no-emoji | session-start.test.js | — (opcional, no requerido si planner skipea) | — | — |

Ejecutar `grep -E "HOOK-(01|02|03)" test/session-start.test.js test/gsd-context.test.js | wc -l` y confirmar >= 12 matches (cobertura de la matrix).

4. **Reporte final:**
Escribir en `20-02-SUMMARY.md` un bloque con:
- Tests pre-Phase 20: N (baseline)
- Tests post-Phase 20: N + delta
- Tests fallidos: 0
- Tests skip: 1 (startup-budget Decisión B pre-existente)
- Matrix de coverage (la tabla anterior) con celdas verificadas marcadas ✓.
  </action>
  <acceptance_criteria>
    - `node --test test/session-start.test.js test/gsd-context.test.js 2>&amp;1 | grep -E '^# fail' | awk '{print $3}'` devuelve `0`.
    - `npm test 2>&amp;1 | grep -E '^# fail' | awk '{print $3}'` devuelve `0`.
    - `npm test 2>&amp;1 | grep -E '^# pass' | awk '{print $3}'` devuelve un número >= 575 (baseline 567 + ~14 delta esperado).
    - `npm test 2>&amp;1 | grep -E '^# skipped' | awk '{print $3}'` devuelve `1` (startup-budget pre-existente).
    - `grep -E "HOOK-(01|02|03)" test/session-start.test.js test/gsd-context.test.js | wc -l` devuelve >= 12 (cobertura de matrix).
    - El archivo `20-02-SUMMARY.md` (parte del output del plan) contiene la tabla de coverage matrix con celdas marcadas.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - Suite global verde (0 fail).
    - Matrix HOOK-01/02/03 cubierta para los 4 modos.
    - Idempotencia verificada (no-GSD + phase + bootstrap).
    - D-04 common-block invariance verificada.
    - Reporte en 20-02-SUMMARY.md con conteos exactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → builders | Los tests llaman a `buildSessionContext` y `buildGsdContext` con fixtures sintéticas. Los builders son puros (sin I/O), los tests son deterministas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | Test fixtures sintéticas (`makeSession`/`makeConfig`) | accept | Fixtures locales, no consumen filesystem ni env. Reusan patrón canónico ya probado en 30+ tests existentes. |
| T-20-06 | Repudiation | Tests no aseguran el comportamiento del prompt | mitigate | Asserts deterministas con `assert.match`/`assert.equal` sobre output literal. Opción B golden bytes detecta cualquier futura edición que mueva el bloque o cambie el separador. Idempotencia detecta no-determinismo accidental. |
| T-20-07 | Information Disclosure | Tests emiten output legible en CI logs | accept | Asserts sobre strings de prompt público; no contienen secrets. Los logs del runner Node son seguros. |
| T-20-08 | Denial of Service | Suite extendida ralentiza el test runner | accept | Builders puros sin I/O; +14 tests añade <50ms al runtime total (medido: tests existentes corren en ~3s, delta es despreciable). |

**Threats_open esperado: 0.**
</threat_model>

<verification>
- Plan 20-01 ejecutado primero (wave 1) — el src ya tiene el bloque appended. Si el src no se ha modificado, los nuevos tests fallan inmediatamente (rojo deseable: detectan regresión real).
- `node --test test/session-start.test.js test/gsd-context.test.js` muestra todos los tests nuevos pasando.
- `npm test` global verde con +14 tests netos vs baseline.
- `grep` counts confirman presencia de las nuevas suites en ambos archivos.
- Coverage matrix completa: 4 modos × 3 requirements (HOOK-01/02/03) cubiertos.
</verification>

<success_criteria>
- HOOK-01: 4 tests de presencia cubriendo los 4 modos (no-GSD + quick + phase + bootstrap) — pass.
- HOOK-02: 4 tests golden bytes opción B (uno por modo) — pass.
- HOOK-03: 3 tests idempotencia (no-GSD + phase + bootstrap; quick implícito vía common-block) + 1 test common-block invariance D-04 — pass.
- D-02b no-emoji enforcement: 1 test sobre slice del bloque ES — pass.
- 0 tests pre-existentes rotos.
- Suite global verde: 0 fail, 1 skip (pre-existente).
- 20-02-SUMMARY.md documenta la coverage matrix completa.
</success_criteria>

<output>
After completion, create `.planning/phases/20-hook-01-universal-anti-push-fantasma/20-02-SUMMARY.md` con:
- Tests añadidos por archivo (lista de nombres `it(...)`).
- Conteo: tests pre-Phase 20 / tests post-Phase 20 / delta.
- Tests fallidos (debe ser 0).
- Tests skipped (debe ser 1 — startup-budget pre-existente Decisión B).
- Coverage matrix HOOK-01/02/03 × 4 modos con celdas marcadas ✓.
- Confirmación de common-block invariance D-04 verificada en runtime.
- Confirmación de no-regresiones en tests existentes (QUICK-08 originales + 11 buildGsdContext originales + 6 source invariants).
- Comandos exactos ejecutados (`node --test ...` + `npm test`) y sus exit codes.
</output>
