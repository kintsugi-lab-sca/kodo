---
phase: 20-hook-01-universal-anti-push-fantasma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/session-start.js
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
tags:
  - hooks
  - prompt-injection
  - anti-push
must_haves:
  truths:
    - "buildSessionContext (no-GSD ES) emite un bloque '## Anti-push-fantasma' al final del prompt"
    - "buildGsdContext (3 ramas GSD EN) emite un bloque '## No automatic push' al final del prompt"
    - "Los bytes anteriores al bloque HOOK-01 quedan idénticos a pre-Phase 20 (HOOK-02 satisfied-by-construction)"
    - "Los builders siguen siendo puros (sin I/O, sin globals, sin imports nuevos)"
    - "El bloque EN es bytes-idéntico en las 3 ramas GSD (quick / phase / bootstrap) — D-04"
    - "D-01: split idiomático preservado — buildSessionContext emite el bloque ES, buildGsdContext emite el bloque EN común."
    - "D-02: rigor textual — cada bloque incluye statement + instrucción + 2 pares Bad/Good."
    - "D-03: posición canonical — el bloque va AL FINAL del array `lines`, antes de `lines.join('\\n')`."
    - "D-05: orchestrator EXCLUIDO — ningún cambio en src/orchestrator/launch.js, src/orchestrator/prompt.md, o .claude/skills/kodo-orchestrate/."
  artifacts:
    - path: "src/hooks/session-start.js"
      provides: "buildSessionContext + buildGsdContext con bloque anti-push appended"
      contains: "## Anti-push-fantasma"
    - path: "src/hooks/session-start.js"
      provides: "buildGsdContext con bloque EN común post-if/else"
      contains: "## No automatic push"
  key_links:
    - from: "src/hooks/session-start.js::buildSessionContext"
      to: "Array literal `lines` final"
      via: "append literal de 11 strings antes de ].join('\\n')"
      pattern: "## Anti-push-fantasma"
    - from: "src/hooks/session-start.js::buildGsdContext"
      to: "Array `lines` mutable post-if/else"
      via: "lines.push(...) con 11 items entre el cierre del if/else y `return lines.join('\\n')`"
      pattern: "## No automatic push"
---

<objective>
Modificar los 2 builders puros (`buildSessionContext` ES en L23-68 y `buildGsdContext` EN en L83-155) de `src/hooks/session-start.js` para appendear, al FINAL del array `lines` de cada uno, el bloque "Anti-push-fantasma" (ES) y "No automatic push" (EN) respectivamente. Implementación INLINE (D-04b confirmado por RESEARCH §"Decisión helper vs inline: inline confirmado"). NO se crea helper aislado, NO se modifica el orchestrator (D-05 EXCLUIDO), NO se toca `main()` despacho.

Purpose: Inyectar a TODAS las sesiones lanzadas por kodo el recordatorio explícito de que kodo NO hace `git push` automático, bloqueando por construcción el patrón ROMAN-125/126 (sesiones afirmando "deploy hecho" sin haber pusheado). HOOK-02 (golden bytes) queda satisfied-by-construction: el bloque va al FINAL → los bytes anteriores no mutan.

Output: `src/hooks/session-start.js` con +11 líneas en `buildSessionContext` (línea ~67 actual) y +13 líneas en `buildGsdContext` (entre L152 cierre del if/else y L154 return). Total delta: ~24 líneas. Sin imports nuevos, sin dependencias nuevas, sin cambios al despacho `main()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-CONTEXT.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-RESEARCH.md
@.planning/phases/20-hook-01-universal-anti-push-fantasma/20-PATTERNS.md
@src/hooks/session-start.js

<interfaces>
<!-- Estado actual de las funciones a modificar — el ejecutor NO necesita explorar el codebase -->

Línea 23-68 de src/hooks/session-start.js (`buildSessionContext`):
```javascript
export function buildSessionContext(session, config) {
  const providerName = session.provider || config.provider;
  const providerCfg = (config.providers && config.providers[providerName]) || {};
  const mcpHint = providerCfg.mcp_hint || `MCP de ${providerName}`;
  const reviewState = providerCfg.states?.review || 'In Review';

  return [
    `# kodo ${session.task_ref}`,
    '',
    `Estás trabajando en **${session.task_ref}: ${session.summary}**`,
    `- Proyecto path: ${session.project_path}`,
    `- Session ID: ${session.session_id}`,
    `- Work item ID: ${session.task_id} | Project ID: ${session.project_id}`,
    '',
    '## Tu responsabilidad',
    '',
    `Tú gestionas el ciclo completo de esta tarea: trabajar → documentar → mover a "${reviewState}" → cerrar sesión. Usa ${mcpHint} para todas las interacciones con ${providerName}.`,
    // ... (líneas intermedias con emojis legítimos ✅/📁/⚠️/🔍 en L50-53) ...
    'Si no puedes terminar (falta info, hay blocker, requiere decisión humana): comenta el estado actual con detalle, **no muevas a revisión**, y cierra con `/exit`. La tarea quedará visible en el dashboard para que el humano intervenga.',
  ].join('\n');
}
```

Línea 83-155 de src/hooks/session-start.js (`buildGsdContext`):
```javascript
export function buildGsdContext(session, opts = {}) {
  const lines = [
    `# kodo ${session.task_ref} — GSD Mode`,
    // ... cabecera común L84-94 ...
  ];

  const mode = getSessionMode(session);
  if (mode === 'quick') {
    // ... rama quick L97-121 ...
  } else if (session.phase_id) {
    // ... rama full+phase L122-134 ...
  } else {
    // ... rama bootstrap L135-152 ...
  }

  return lines.join('\n');
}
```

CLAVE: las 3 ramas convergen en `return lines.join('\n')` (L154). El append común va ENTRE el cierre del else `}` (L152) y el `return` (L154). 1 sola inserción → cubre las 3 ramas (D-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append bloque ES "Anti-push-fantasma" al final de buildSessionContext</name>
  <files>src/hooks/session-start.js</files>
  <read_first>
    - src/hooks/session-start.js (líneas 23-68 — función `buildSessionContext`)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-RESEARCH.md §"Proposed Block Text" (bytes canónicos ES) y §"Implementation Sketch — Sitio #1"
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-PATTERNS.md §"src/hooks/session-start.js (hook / context-builder, transform)" (Phase 20 cambio HOOK-01 ES)
  </read_first>
  <behavior>
    - El output de `buildSessionContext(makeSession(), makeConfig())` debe contener el header literal `## Anti-push-fantasma`.
    - El output debe contener el statement `kodo NO hace \`git push\` automático.` y la instrucción con `verifica con \`git push\` real`.
    - El output debe contener al menos 2 pares Bad/Good: uno con "publicada"/"git push al remoto" y otro con "Deploy hecho."/"git push origin main".
    - Los bytes ANTERIORES al header (es decir, el output sin el bloque HOOK-01) deben quedar idénticos a pre-Phase 20.
    - El bloque debe estar al final del output: `output.lastIndexOf('## Anti-push-fantasma')` cae después de la línea `Si no puedes terminar...`.
    - La línea inmediatamente anterior al header debe ser una línea en blanco (separador `'\n\n'`).
  </behavior>
  <action>
LOCALIZAR la línea 66 actual de `src/hooks/session-start.js`:
```javascript
    'Si no puedes terminar (falta info, hay blocker, requiere decisión humana): comenta el estado actual con detalle, **no muevas a revisión**, y cierra con `/exit`. La tarea quedará visible en el dashboard para que el humano intervenga.',
  ].join('\n');
```

INSERTAR exactamente 11 strings dentro del array literal, ENTRE la línea `'Si no puedes terminar...'` (L66) y el `].join('\n');` (L67). El array final debe terminar así (verbatim, copiar carácter a carácter — D-02 statement + D-02 ejemplos Bad/Good + D-02b sin emojis):

```javascript
    'Si no puedes terminar (falta info, hay blocker, requiere decisión humana): comenta el estado actual con detalle, **no muevas a revisión**, y cierra con `/exit`. La tarea quedará visible en el dashboard para que el humano intervenga.',
    '',
    '## Anti-push-fantasma',
    '',
    'kodo NO hace `git push` automático. Antes de afirmar deploy, publicación o cambios remotos, verifica con `git push` real, o redacta la afirmación en condicional ("una vez se haga push…").',
    '',
    'Ejemplos:',
    '- Bad: "Feature publicada en producción."',
    '- Good: "Feature commiteada localmente, pendiente de `git push` al remoto."',
    '- Bad: "Deploy hecho."',
    '- Good: "Deploy quedará efectivo una vez se haga `git push origin main`."',
  ].join('\n');
```

NO añadir comentarios `// Phase 20 HOOK-01 ...` dentro del array (los items son strings literales del prompt — un comentario JS rompería la sintaxis del array). El comentario justificativo va FUERA del array, encima del `return [`. AÑADE este comentario en la línea inmediatamente anterior al `return [` (entre L28 actual `const reviewState = providerCfg.states?.review || 'In Review';` y L29 actual `return [`):

```javascript
  // Phase 20 HOOK-01 (no-GSD ES): bloque "Anti-push-fantasma" al FINAL del array preserva
  // golden bytes anteriores (HOOK-02 satisfied-by-construction).
  return [
```

NO modificar nada más: ni cabeceras, ni emojis previos (✅/📁/⚠️/🔍 en L50-53 son LEGÍTIMOS, no son parte del bloque HOOK-01 y deben permanecer intactos), ni `mcpHint`, ni `reviewState`. Si el linter/ts-check da error, NO desactivar `@ts-check` — los items son strings, son válidos.

VERIFICAR con grep que el statement aparece EXACTAMENTE una vez (no duplicación accidental).
  </action>
  <acceptance_criteria>
    - `grep -c '## Anti-push-fantasma' src/hooks/session-start.js` devuelve `1` (header presente, no duplicado).
    - `grep -c "kodo NO hace \`git push\` automático" src/hooks/session-start.js` devuelve `1` (statement presente exactamente una vez).
    - `grep -c 'Feature publicada en producción' src/hooks/session-start.js` devuelve `1`.
    - `grep -c 'git push origin main' src/hooks/session-start.js` devuelve `1`.
    - `grep -c 'Phase 20 HOOK-01 (no-GSD ES)' src/hooks/session-start.js` devuelve `1` (comentario trazabilidad).
    - El archivo sigue siendo sintácticamente válido: `node -e "import('./src/hooks/session-start.js').then(m => console.log(typeof m.buildSessionContext))"` imprime `function`.
    - El smoke test inline pasa: `node -e "import('./src/hooks/session-start.js').then(m => { const out = m.buildSessionContext({workspace_ref:'KL-1',session_id:'s',task_id:'t',task_ref:'KL-1',provider:'plane',project_id:'p',summary:'x',status:'running',started_at:'2026-01-01T00:00:00Z',project_path:'/tmp'}, {provider:'plane',providers:{plane:{mcp_hint:'MCP de Plane'}}}); if (!out.includes('## Anti-push-fantasma')) process.exit(1); if (!out.endsWith('\"Deploy quedará efectivo una vez se haga \`git push origin main\`.\"')) process.exit(2); console.log('ok'); })"` imprime `ok`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/session-start.test.js 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - Bloque ES "Anti-push-fantasma" appended al final del array `lines` de `buildSessionContext`.
    - Comentario `// Phase 20 HOOK-01 (no-GSD ES): ...` antes del `return [`.
    - Tests existentes de `buildSessionContext` siguen pasando (assert.match `/KL-99/`, `/MCP de Plane/`, etc.) — el append no afecta a esos asserts.
    - `node:test` runner ejecuta `test/session-start.test.js` sin nuevos fallos (los tests HOOK-01 nuevos del Plan 20-02 todavía no existen — los existentes siguen verdes).
    - Sin imports nuevos, sin dependencias nuevas, sin cambios fuera del rango L23-68.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append bloque EN "No automatic push" común post-if/else en buildGsdContext</name>
  <files>src/hooks/session-start.js</files>
  <read_first>
    - src/hooks/session-start.js (líneas 83-155 — función `buildGsdContext` con 3 ramas convergentes)
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-RESEARCH.md §"Proposed Block Text" (bytes canónicos EN) y §"Implementation Sketch — Sitio #2"
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-PATTERNS.md §"Bifurcación 3-ramas + convergencia post-if/else"
    - .planning/phases/20-hook-01-universal-anti-push-fantasma/20-CONTEXT.md §D-04 (1 bloque EN común a 3 ramas)
  </read_first>
  <behavior>
    - El output de `buildGsdContext` en las 3 ramas (quick / phase / bootstrap) debe contener el header literal `## No automatic push`.
    - El output debe contener el statement `kodo does NOT push automatically.` y la instrucción con `verify with a real \`git push\``.
    - El output debe contener al menos 2 pares Bad/Good: uno con "Feature deployed to production."/"`git push` to remote." y otro con "Deploy done."/"git push origin main".
    - El bloque EN debe ser bytes-idéntico en las 3 ramas (common-block invariance D-04): si extraemos `tail = ctx.slice(ctx.lastIndexOf('## No automatic push'))` de las 3 ramas, los 3 tails son `assert.equal`.
    - El bloque debe estar al final del output: después del último item de cada rama existente.
    - La línea inmediatamente anterior al header debe ser una línea en blanco (separador `'\n\n'`).
  </behavior>
  <action>
LOCALIZAR las líneas 151-154 actuales de `src/hooks/session-start.js`:
```javascript
      'This will initialize the project planning structure using the task description as brief.',
    );
  }

  return lines.join('\n');
```

(La línea `)` cierra el último `lines.push(...)` de la rama bootstrap. La línea `}` cierra el bloque `else`. La línea en blanco separa. La línea `return lines.join('\n');` es la convergencia.)

INSERTAR ENTRE el cierre `}` del else (L152 actual) y el `return lines.join('\n');` (L154 actual) un bloque de comentario + un `lines.push(...)` con exactamente 11 items. El resultado verbatim (copiar carácter a carácter):

```javascript
      'This will initialize the project planning structure using the task description as brief.',
    );
  }

  // Phase 20 HOOK-01 (GSD EN): anti-push reminder común a las 3 ramas (quick / phase / bootstrap).
  // D-04: bloque EN único; las 3 ramas convergen aquí post-if/else.
  // HOOK-02 satisfied-by-construction: append al FINAL preserva golden bytes de los bloques anteriores.
  lines.push(
    '',
    '## No automatic push',
    '',
    'kodo does NOT push automatically. Before claiming a deploy, release, or any remote change, verify with a real `git push`, or phrase the claim conditionally ("once pushed…").',
    '',
    'Examples:',
    '- Bad: "Feature deployed to production."',
    '- Good: "Feature committed locally, pending `git push` to remote."',
    '- Bad: "Deploy done."',
    '- Good: "Deploy will be live once `git push origin main` runs."',
  );

  return lines.join('\n');
}
```

NO modificar:
- La cabecera de `buildGsdContext` (L83-94) — los strings `# kodo ${session.task_ref} — GSD Mode`, project_path, etc.
- El `if (mode === 'quick') { ... }` (L97-121) — rama quick, incluyendo `/gsd-quick "${safeTitle}"` y el closing line "Run the slash command and finish — no plan/execute/verify cycle.".
- El `else if (session.phase_id) { ... }` (L122-134) — rama phase con los 3 comandos `/gsd-plan-phase`, `/gsd-execute-phase`, `/gsd-verify-work`.
- El `else { ... }` (L135-152) — rama bootstrap con `/gsd-new-project`.

VERIFICAR que el push común NO se añade DENTRO de ninguna rama del if/else (causaría duplicación 3x). Solo va DESPUÉS del cierre `}` del else.
  </action>
  <acceptance_criteria>
    - `grep -c '## No automatic push' src/hooks/session-start.js` devuelve `1` (header EN aparece exactamente una vez — common-block D-04, NO duplicado en cada rama).
    - `grep -c 'kodo does NOT push automatically' src/hooks/session-start.js` devuelve `1`.
    - `grep -c 'Feature deployed to production' src/hooks/session-start.js` devuelve `1`.
    - `grep -c 'Phase 20 HOOK-01 (GSD EN)' src/hooks/session-start.js` devuelve `1`.
    - `grep -nE "lines\.push\(\s*$" src/hooks/session-start.js | wc -l` devuelve al menos 1 (el `lines.push(` multi-línea nuevo existe; otros `lines.push` previos pueden ser multi-línea también).
    - El archivo sigue siendo sintácticamente válido: `node -e "import('./src/hooks/session-start.js').then(m => console.log(typeof m.buildGsdContext))"` imprime `function`.
    - Smoke test common-block invariance: ejecutar `node -e "import('./src/hooks/session-start.js').then(m => { const base = {workspace_ref:'K',session_id:'s',task_id:'t',task_ref:'K',provider:'p',project_id:'pr',summary:'x',status:'r',started_at:'2026-01-01T00:00:00Z',project_path:'/t',gsd:true}; const H='## No automatic push'; const tail = s => s.slice(s.lastIndexOf(H)); const q = tail(m.buildGsdContext({...base, gsd_mode:'quick'})); const p = tail(m.buildGsdContext({...base, phase_id:'08'})); const b = tail(m.buildGsdContext({...base})); if (q !== p || p !== b) process.exit(1); console.log('ok'); })"` imprime `ok`.
    - Smoke test "rama phase no añade /gsd-plan-phase como duplicado": `grep -c '/gsd-plan-phase' src/hooks/session-start.js` devuelve `1` (el único uso queda en L129 dentro de la rama phase).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/session-start.test.js test/gsd-context.test.js 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - Bloque EN "No automatic push" añadido vía `lines.push(...)` ENTRE el cierre del if/else y el `return lines.join('\n')` (1 sola inserción, cubre las 3 ramas — D-04).
    - 3 líneas de comentario `// Phase 20 HOOK-01 (GSD EN): ...` antes del `lines.push(...)`.
    - Tests existentes de `buildGsdContext` siguen pasando (QUICK-08 7 tests + gsd-context 11 tests) — el append no afecta sus asserts.
    - Common-block invariance verificable: tail(quick) === tail(phase) === tail(bootstrap).
    - Sin cambios a las 3 ramas del if/else.
    - Sin imports nuevos, sin dependencias nuevas.
    - `npm test` corre y los nuevos tests del Plan 20-02 todavía no existen — los existentes (~510+) siguen verdes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Builder pure → prompt stdout | Strings literales estáticas escritas al `additionalContext` del hook payload JSON. Sin interpolación de input externo en el bloque HOOK-01. |
| `session.summary` → builder | Ya defang con `.replace(/"/g, "'")` en rama quick (Phase 12 D-04). NO en scope de Phase 20 — el bloque anti-push no interpola `summary`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `buildSessionContext` / `buildGsdContext` bloque HOOK-01 | accept | El bloque es texto literal estático (sin interpolación de input). No hay superficie de tampering nuevo — el threat surface preexistente (`session.summary` defang) queda intacto. |
| T-20-02 | Information Disclosure | Prompt completo emitido a stdout | accept | El bloque añadido es prosa pública (no contiene secrets, paths, IDs). Phase 20 no añade campos al prompt; añade prosa estática invariante. |
| T-20-03 | Spoofing (agent claims push without verification) | Sesión Claude que parafrasea "deploy hecho" sin haber pusheado (driver ROMAN-125/126) | mitigate | Bloque anti-push instruye explícitamente al agente a verificar `git push` real o reformular en condicional + 2 pares Bad/Good que bloquean la paráfrasis. Mitigación a nivel de prompt; enforcement runtime queda deferido a v0.7+. |
| T-20-04 | Repudiation | Idempotencia del bloque | accept | Bloque estático con bytes-determinismo (mismo input → mismo output). Re-emisiones son trazables vía NDJSON `session.start` ya existente. No se requiere evento dedicado `hook.anti_push.injected` (Deferred). |

**Threats_open esperado: 0** — Phase 20 modifica prompts estáticos, no introduce I/O, no añade deps, no expone nueva input surface.
</threat_model>

<verification>
- `grep -c '## Anti-push-fantasma' src/hooks/session-start.js` == 1
- `grep -c '## No automatic push' src/hooks/session-start.js` == 1
- `grep -c 'kodo NO hace' src/hooks/session-start.js` == 1
- `grep -c 'kodo does NOT push' src/hooks/session-start.js` == 1
- `node --test test/session-start.test.js test/gsd-context.test.js` — suite existente pasa íntegra (los tests HOOK-01 nuevos llegan en Plan 20-02).
- `npm test` (suite global) — no se introducen regresiones; el delta esperado es 0 tests fallidos vs baseline.
- `node -e "..."` smoke common-block invariance: tail(quick) === tail(phase) === tail(bootstrap).
- Sintaxis válida: `node -e "import('./src/hooks/session-start.js')"` no lanza.
</verification>

<success_criteria>
- `buildSessionContext` (no-GSD ES) emite el bloque "## Anti-push-fantasma" al final del prompt — verificable por `assert.match(ctx, /## Anti-push-fantasma/)`.
- `buildGsdContext` (las 3 ramas) emite el bloque "## No automatic push" al final del prompt — verificable por `assert.match(ctx, /## No automatic push/)` en quick/phase/bootstrap.
- HOOK-02 golden bytes preservados: el output sin el bloque (prefix) es bytes-idéntico al output pre-Phase 20 — verificable porque el append es estrictamente al final del array `lines`.
- Common-block invariance D-04: `tail(quick) === tail(phase) === tail(bootstrap)` para el bloque EN.
- Sin imports nuevos en `src/hooks/session-start.js` (mantiene los 4 imports pre-Phase 20: `node:url`, `findSession`, `loadConfig`, `getSessionMode`).
- Sin cambios a `main()` (L169-226), `buildContextSummary` (en `src/orchestrator/launch.js` — D-05 EXCLUIDO), `prompt.md`, ni a la skill `kodo-orchestrate`.
- Suite global de tests sigue en verde (mismo número de pass que baseline pre-Phase 20).
</success_criteria>

<output>
After completion, create `.planning/phases/20-hook-01-universal-anti-push-fantasma/20-01-SUMMARY.md` con:
- Líneas exactas modificadas (números de línea finales).
- Bytes finales del bloque ES y EN (transcribir verbatim del archivo modificado).
- Resultado de los acceptance_criteria (grep counts + smoke tests).
- Resultado del `npm test` (total pass / total tests).
- Confirmación de HOOK-02 satisfied-by-construction (prefix bytes intactos) y D-04 common-block invariance.
- Confirmación de no-cambios en `src/orchestrator/launch.js`, `src/orchestrator/prompt.md`, `.claude/skills/kodo-orchestrate/skill.md` (D-05 EXCLUIDO).
</output>
</output>
