---
phase: 01-interface-state-schema
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/session/state.js
  - src/config.js
  - test/migration.test.js
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - TEST-03

must_haves:
  truths:
    - "Al arrancar kodo con un state.json viejo (sin schema_version), la migración ocurre automáticamente sin intervención del usuario"
    - "Se crea state.json.bak con el contenido original antes de migrar"
    - "El state.json migrado tiene schema_version: 2, sessions: {} y no tiene plane_id ni plane_identifier"
    - "Al arrancar con un config.json viejo (con config.plane.*), la migración ocurre automáticamente"
    - "Se crea config.json.bak antes de migrar config"
    - "El config migrado tiene provider: 'plane' en raíz y providers.plane.* con el mapeo de estados"
    - "Un state.json ya en v2 NO se vuelve a migrar (no se crea .bak redundante)"
    - "Los tests de migración pasan"
  artifacts:
    - path: "src/session/state.js"
      provides: "migrateStateIfNeeded() integrado en loadState() — transparente para consumers"
    - path: "src/config.js"
      provides: "DEFAULT_CONFIG nuevo schema + migrateConfigIfNeeded() integrado en loadConfig() + getPlaneApiKey() actualizada"
    - path: "test/migration.test.js"
      provides: "Tests para STAT-01, STAT-02, STAT-03, STAT-04 (TEST-03)"
  key_links:
    - from: "src/session/state.js loadState()"
      to: "migrateStateIfNeeded()"
      via: "Llamada al inicio de loadState() antes de retornar"
      pattern: "migrateStateIfNeeded"
    - from: "src/config.js loadConfig()"
      to: "migrateConfigIfNeeded()"
      via: "Llamada dentro de loadConfig() antes de retornar el objeto"
      pattern: "migrateConfigIfNeeded"
    - from: "src/config.js getPlaneApiKey()"
      to: "config.providers.plane.api_key_env"
      via: "Acceso al nuevo path del campo (antes: config.plane.api_key_env)"
      pattern: "providers\\.plane\\.api_key_env"
---

<objective>
Implementar la migración automática de state.json y config.json al nuevo schema provider-agnostic de kodo v0.2.

Purpose: Usuarios existentes con kodo v0.1 actualicen sin fricción — kodo detecta el schema viejo al arrancar, hace backup y migra transparentemente. Sin esta migración, cualquier usuario existente vería errores o datos corruptos al actualizar.

Output: Lógica de migración integrada en loadState() y loadConfig(), con tests que verifican los 4 requirements de STAT-*.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-interface-state-schema/01-CONTEXT.md
@.planning/phases/01-interface-state-schema/01-RESEARCH.md
@.planning/phases/01-interface-state-schema/01-01-SUMMARY.md

<interfaces>
<!-- Estado actual DESPUÉS de Plan 01 — estos son los contratos implementados -->

Desde src/interface.js (creado en Plan 01):
```javascript
// TASK_PROVIDER_METHODS y VALID_PRIORITIES exportados
// Typedefs: TaskProvider, TaskItem, TriggerEvent
```

Desde src/session/state.js (typedef actualizada en Plan 01):
```javascript
/**
 * @typedef {{
 *   workspace_ref: string,
 *   session_id: string,
 *   task_id: string,        // antes: plane_id
 *   task_ref: string,       // antes: plane_identifier
 *   provider: string,       // nuevo campo
 *   project_id: string,
 *   summary: string,
 *   status: 'running'|'done'|'error'|'review',
 *   started_at: string,
 *   project_path: string,
 * }} Session
 *
 * @typedef {{
 *   schema_version: number,
 *   sessions: Record<string, Session>
 * }} State
 */
```

Desde src/config.js (DEFAULT_CONFIG actual — PRE-migración):
```javascript
const DEFAULT_CONFIG = {
  plane: {
    base_url: 'https://tasks.kintsugi-lab.com',
    api_key_env: 'PLANE_API_KEY',
    workspace_slug: 'k-lab',
    projects: [],
    trigger_state: 'In Progress',
    done_state: 'Done',
    review_state: 'In review',
  },
  cmux: { ... },
  claude: { ... },
  server: { ... },
};
```

API pública de state.js (NO cambiar firmas):
```javascript
export function loadState() // retorna State
export function saveState(state) // void
export function addSession(taskId, session) // void
export function removeSession(taskId) // void
export function updateSession(taskId, updates) // void
export function getSession(taskId) // Session|null
export function listSessions() // Session[]
export function findSession({ cwd?, workspaceRef? }) // {id, session}|null
```

API pública de config.js (NO cambiar firmas):
```javascript
export function loadConfig() // retorna config
export function saveConfig(config) // void
export function loadProjects() // Record<string, string>
export function saveProjects(projects) // void
export function getPlaneApiKey() // string|undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test/migration.test.js con tests para STAT-01..04</name>
  <files>test/migration.test.js</files>
  <behavior>
    - Test 1: migrateState(oldState) renombra plane_id → task_id y plane_identifier → task_ref (STAT-01)
    - Test 2: migrateState(oldState) añade campo provider: "plane" a cada sesión migrada (STAT-02)
    - Test 3: El resultado de migrateState tiene schema_version: 2 (STAT-03)
    - Test 4: migrateState limpia las sesiones activas (sessions: {}) (STAT-04 — decisión de CONTEXT.md)
    - Test 5: Un state con schema_version: 2 ya no se migra (migrateState retorna el mismo objeto o lo pasa sin cambios)
    - Test 6: La migración de estado crea el archivo .bak antes de escribir el nuevo estado (STAT-04)
    - Test 7: migrateConfig(oldConfig) mueve plane.* a providers.plane.* y añade provider: "plane" en raíz
    - Test 8: migrateConfig preserva los campos states correctamente (trigger, review, done desde trigger_state, review_state, done_state)
    - Test 9: Un config con providers ya existente no se migra de nuevo
  </behavior>
  <action>
    Crear test/migration.test.js con la estructura siguiente. Los tests deben importar funciones puras de migración — separadas del I/O de ficheros — para ser aislados.

    **Diseño importante:** Las funciones de migración pura deben ser exportables desde state.js y config.js para poder testarlas sin tocar el filesystem real. Exportar:
    - `export function migrateState(rawState)` — función pura: recibe objeto state viejo, devuelve objeto state nuevo. NO hace I/O.
    - `export function migrateConfig(rawConfig)` — función pura: recibe objeto config viejo, devuelve objeto config nuevo. NO hace I/O.

    La lógica de I/O (backup + escritura) se queda dentro de `migrateStateIfNeeded()` y `migrateConfigIfNeeded()` que son privadas.

    ```javascript
    // test/migration.test.js
    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';
    import { writeFileSync, existsSync, mkdirSync, rmSync } from 'node:fs';
    import { join } from 'node:path';
    import { tmpdir } from 'node:os';

    import { migrateState } from '../src/session/state.js';
    import { migrateConfig } from '../src/config.js';

    // State migration tests
    describe('state migration', () => {
      const OLD_STATE = {
        sessions: {
          'uuid-abc': {
            workspace_ref: 'workspace:1',
            session_id: 'sess-1',
            plane_id: 'uuid-abc',
            plane_identifier: 'KL-42',
            project_id: 'proj-1',
            summary: 'Fix bug',
            status: 'running',
            started_at: '2026-01-01T00:00:00Z',
            project_path: '/dev/foo',
          },
        },
      };

      it('renombra plane_id a task_id (STAT-01)', () => { ... });
      it('renombra plane_identifier a task_ref (STAT-01)', () => { ... });
      it('añade provider: "plane" a cada sesión (STAT-02)', () => { ... });
      it('schema_version es 2 en resultado migrado (STAT-03)', () => { ... });
      it('limpia las sesiones activas durante migración (STAT-04)', () => { ... });
      it('no migra si ya tiene schema_version: 2', () => { ... });
    });

    // Config migration tests
    describe('config migration', () => {
      const OLD_CONFIG = {
        plane: {
          base_url: 'https://tasks.example.com',
          api_key_env: 'PLANE_API_KEY',
          workspace_slug: 'myslug',
          projects: ['proj-1'],
          trigger_state: 'In Progress',
          done_state: 'Done',
          review_state: 'In review',
        },
        cmux: { binary: '/usr/bin/cmux' },
        claude: { default_model: 'opus' },
      };

      it('mueve plane.* a providers.plane.* y añade provider: "plane"', () => { ... });
      it('mapea trigger_state/done_state/review_state a providers.plane.states.*', () => { ... });
      it('no migra si ya tiene providers definido', () => { ... });
      it('elimina el campo plane del nivel raíz', () => { ... });
    });
    ```

    Los tests deben estar completamente implementados (no solo esquemas). Los casos sin I/O de filesystem son todos puros — solo reciben objetos y verifican objetos resultantes.

    Para el test del .bak, usar tmpdir() como en state.test.js y llamar a una función de migración con I/O que acepte path como parámetro. Si esta función no existe aún, el test fallará (RED) — se implementa en Task 2.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/migration.test.js 2>&1 | head -30</automated>
  </verify>
  <done>Los tests fallan con "migrateState is not a function" o similar (RED esperado). El archivo test/migration.test.js existe con todos los casos de test implementados.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar migraciones en state.js y config.js</name>
  <files>src/session/state.js, src/config.js</files>
  <action>
    Hacer pasar los tests de Task 1 implementando las funciones de migración.

    **En src/session/state.js:**

    1. Añadir import de `writeFileSync` si no está ya (para el backup).

    2. Exportar función pura `migrateState(rawState)`:
    ```javascript
    /** @param {object} rawState @returns {State} */
    export function migrateState(rawState) {
      if (rawState.schema_version === 2) return rawState;
      return {
        schema_version: 2,
        sessions: {},
      };
    }
    ```
    La función limpia las sesiones (decisión de CONTEXT.md: "sesiones activas se limpian durante migración"). No hace I/O.

    3. Añadir función privada `migrateStateIfNeeded()` que hace el I/O real:
    ```javascript
    function migrateStateIfNeeded() {
      if (!existsSync(STATE_PATH)) return;
      const raw = JSON.parse(readFileSync(STATE_PATH, 'utf-8'));
      if (raw.schema_version === 2) return;
      // Backup antes de migrar
      writeFileSync(STATE_PATH + '.bak', JSON.stringify(raw, null, 2) + '\n');
      const newState = migrateState(raw);
      writeFileSync(STATE_PATH, JSON.stringify(newState, null, 2) + '\n');
      console.log('[kodo] State migrado a schema_version 2 (backup: state.json.bak)');
    }
    ```

    4. Llamar `migrateStateIfNeeded()` al inicio de `loadState()` antes de leer el fichero.

    **En src/config.js:**

    1. Actualizar `DEFAULT_CONFIG` al nuevo schema:
    ```javascript
    const DEFAULT_CONFIG = {
      provider: 'plane',
      providers: {
        plane: {
          base_url: 'https://tasks.kintsugi-lab.com',
          api_key_env: 'PLANE_API_KEY',
          workspace_slug: 'k-lab',
          projects: [],
          states: {
            trigger: 'In Progress',
            review: 'In review',
            done: 'Done',
          },
        },
      },
      cmux: {
        binary: '/Applications/cmux.app/Contents/Resources/bin/cmux',
        colors: { running: 'Amber', done: 'Green', error: 'Crimson', review: 'Blue' },
      },
      claude: {
        binary: '/Applications/cmux.app/Contents/Resources/bin/claude',
        default_model: 'opus',
        max_parallel: 3,
        flags: [],
      },
      server: {
        port: 9090,
        idle_threshold_min: 5,
        stuck_threshold_min: 30,
      },
    };
    ```

    2. Exportar función pura `migrateConfig(rawConfig)`:
    ```javascript
    /** @param {object} rawConfig @returns {object} */
    export function migrateConfig(rawConfig) {
      if (rawConfig.providers) return rawConfig; // ya migrado
      const { plane: planeOld = {}, ...rest } = rawConfig;
      return {
        ...rest,
        provider: 'plane',
        providers: {
          plane: {
            base_url: planeOld.base_url,
            api_key_env: planeOld.api_key_env,
            workspace_slug: planeOld.workspace_slug,
            projects: planeOld.projects || [],
            states: {
              trigger: planeOld.trigger_state || 'In Progress',
              review: planeOld.review_state || 'In review',
              done: planeOld.done_state || 'Done',
            },
          },
        },
      };
    }
    ```
    NOTA: Usar destructuring `{ plane: planeOld, ...rest }` para eliminar `plane` del nivel raíz (ver Pitfall 4 en RESEARCH.md — `{ ...config, plane: undefined }` NO elimina la clave en el objeto JS, solo en JSON.stringify).

    3. Añadir función privada `migrateConfigIfNeeded(rawConfig)` que hace I/O:
    ```javascript
    function migrateConfigIfNeeded(rawConfig) {
      if (rawConfig.providers) return rawConfig; // ya migrado
      writeFileSync(CONFIG_PATH + '.bak', JSON.stringify(rawConfig, null, 2) + '\n');
      const newConfig = migrateConfig(rawConfig);
      writeFileSync(CONFIG_PATH, JSON.stringify(newConfig, null, 2) + '\n');
      console.log('[kodo] Config migrada al nuevo schema (backup: config.json.bak)');
      return newConfig;
    }
    ```

    4. Integrar en `loadConfig()`: llamar `migrateConfigIfNeeded(parsed)` sobre el JSON parseado antes de retornarlo.

    5. Actualizar `getPlaneApiKey()` para usar el nuevo path:
    ```javascript
    export function getPlaneApiKey() {
      const config = loadConfig();
      return process.env[config.providers?.plane?.api_key_env];
    }
    ```

    6. Actualizar el JSDoc de `loadConfig()` — el return type ya no es `typeof DEFAULT_CONFIG` directamente (o mantener como está si no rompe).
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/migration.test.js && node --test test/state.test.js && node --test test/interface.test.js</automated>
  </verify>
  <done>Todos los tests del suite pasan. migrateState y migrateConfig son funciones exportadas. loadState() llama migrateStateIfNeeded() internamente. loadConfig() llama migrateConfigIfNeeded() internamente. getPlaneApiKey() usa providers.plane.api_key_env.</done>
</task>

</tasks>

<verification>
Suite completa verde:
```
node --test test/migration.test.js  # ≥ 9 tests pasan
node --test test/state.test.js      # todos los tests existentes pasan
node --test test/interface.test.js  # 3 tests pasan
node --test test/**/*.test.js       # suite completa verde (15 existentes + nuevos)
```

Verificar que la migración no rompe consumidores existentes:
- `node -e "import('./src/config.js').then(m => console.log(Object.keys(m.loadConfig())))"` — debe mostrar keys del nuevo schema
- `node -e "import('./src/session/state.js').then(m => console.log(m.loadState().schema_version))"` — debe mostrar 2 (si ya existe state.json en producción)
</verification>

<success_criteria>
- test/migration.test.js tiene ≥9 tests cubriendo STAT-01, STAT-02, STAT-03, STAT-04
- migrateState() exportada desde state.js: recibe state viejo → devuelve {schema_version: 2, sessions: {}}
- migrateConfig() exportada desde config.js: recibe config con plane.* → devuelve config con providers.plane.* y provider: "plane"
- loadState() llama migrateStateIfNeeded() — al detectar schema viejo, crea .bak y migra
- loadConfig() llama migrateConfigIfNeeded() — al detectar config vieja, crea .bak y migra
- getPlaneApiKey() usa config.providers.plane.api_key_env (nuevo path)
- DEFAULT_CONFIG usa el nuevo schema con providers.plane.states
- Suite completa pasa: `node --test test/**/*.test.js`
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-interface-state-schema/01-02-SUMMARY.md` con lo implementado.
</output>
