---
phase: 999.1
plan: 05
type: execute
wave: 2
depends_on: [999.1-01]
files_modified:
  - .planning/PROJECT.md
autonomous: false
requirements: [D-04, D-05, D-06]
user_setup: []
must_haves:
  truths:
    - "La sección `## Constraints` de PROJECT.md contiene una regla operativa nueva que documenta D-05: `kodo orchestrator` se invoca desde cwd = repo (kodo) para que Claude Code auto-cargue `.claude/skills/kodo-orchestrate/skill.md`"
    - "La sección `Deferred to v0.6 or later` de PROJECT.md contiene una nueva entrada SKILL-01 con `kodo skill sync` CLI"
    - "Tras el checkpoint humano, `~/.claude/skills/kodo-orchestrate/skill.md` no existe (verificado con `test ! -e ~/.claude/skills/kodo-orchestrate/skill.md` en el cierre de la phase)"
    - "La phase 999.1 queda referenciada en la sección Evolution (cierre opcional vía /gsd-transition)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Constraints actualizado con regla D-05/D-06 + Deferred con SKILL-01"
      contains: "kodo orchestrator se invoca desde"
  key_links:
    - from: ".planning/PROJECT.md > Constraints"
      to: ".claude/skills/kodo-orchestrate/skill.md"
      via: "regla operativa que liga cwd=repo a auto-carga de skill"
      pattern: "\\.claude/skills/kodo-orchestrate"
    - from: ".planning/PROJECT.md > Deferred"
      to: "kodo skill sync"
      via: "entrada SKILL-01"
      pattern: "SKILL-01"
    - from: "user filesystem (~/.claude/skills/kodo-orchestrate/)"
      to: "non-existence post-phase"
      via: "checkpoint humano `rm` + verificación `test ! -e`"
      pattern: "test ! -e ~/\\.claude/skills/kodo-orchestrate/skill\\.md"
---

<objective>
State hygiene final de la phase 999.1:

1. **Capturar D-05/D-06 en `.planning/PROJECT.md > Constraints`**: regla operativa nueva
   que documenta que `kodo orchestrator` debe invocarse desde `cwd = repo kodo` para que
   Claude Code auto-cargue la skill local. `cwd ≠ repo` es fallback degradado (prompt.md
   solo).

2. **Capturar SKILL-01 en `.planning/PROJECT.md > Active > Deferred to v0.6 or later`**:
   `kodo skill sync` CLI que empuje la canonical del repo a `~/.claude/skills/` (deferred
   idea del CONTEXT.md, queda para v0.6 cuando se evalúe la fricción de exigir cwd=repo).

3. **Eliminar `~/.claude/skills/kodo-orchestrate/skill.md`** (D-04): archivo fuera del
   repo, eliminación manual a través de un checkpoint humano. Post-phase, la skill solo
   vive en `<repo>/.claude/skills/kodo-orchestrate/skill.md`. **El acceptance gate del
   checkpoint es `test ! -e ~/.claude/skills/kodo-orchestrate/skill.md`** (H4) — el plan
   no cierra si el archivo sigue existiendo.

Purpose: D-04 cierra la fuente de drift entre la skill global y la del repo. D-05/D-06
materializan el contrato operativo nuevo (cwd=repo para skill auto-load). SKILL-01 deja
trazabilidad de la idea diferida.

Output: PROJECT.md actualizado + skill global confirmada como no existente (con
checkpoint humano que ejecuta el `rm` y un gate observable de no-existencia).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md

<interfaces>
<!-- Secciones objetivo de PROJECT.md a editar -->

PROJECT.md > Constraints (líneas ~116–125 del archivo actual):
```markdown
## Constraints

- **Stack**: Node.js, sin frameworks pesados, mínimas dependencias externas
- **Compatibilidad**: Breaking changes OK (v0.x, no hay usuarios externos)
- **Runtime**: Debe funcionar en macOS con cmux instalado
- **Tokens**: Vigilante/server consumen 0 tokens; solo el orquestador usa LLM
- **Logger aislado del vigilante**: `kodo check` no debe cargar `src/logger.js` transitivamente (LOG-12 guard)
- **Modo derivado por helper, NO inline**: ... (D-09/D-10/D-11)
- **Color isolation**: `picocolors` solo se importa desde `src/cli/format.js`. ...
```

→ AÑADIR un bullet nuevo al final de la lista con la regla D-05/D-06.

PROJECT.md > Active > Deferred to v0.6 or later (líneas ~78–84):
```markdown
**Deferred to v0.6 or later:**
- [ ] Adapter de GitHub Issues que implementa TaskProvider
- [ ] Adapter de ClickUp que implementa TaskProvider
- [ ] Adapter local (JSON/Markdown) que implementa TaskProvider
- [ ] Polling trigger channel para providers sin webhook
- [ ] File watcher trigger para provider local
- [ ] Hook prompt: ... (HOOK-01)
```

→ AÑADIR un bullet nuevo SKILL-01 al final de la lista.

Skill global a eliminar (D-04):
- Path: `~/.claude/skills/kodo-orchestrate/skill.md`
- Directorio: `~/.claude/skills/kodo-orchestrate/` (vaciable tras borrar el archivo)
- Acceptance gate post-eliminación: `test ! -e ~/.claude/skills/kodo-orchestrate/skill.md`
  retorna exit 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir D-05/D-06 a PROJECT.md > Constraints y SKILL-01 a Deferred</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - .planning/PROJECT.md (estado actual — verificar líneas exactas de "## Constraints" y "Deferred to v0.6 or later" antes de editar)
    - .planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md (D-04, D-05, D-06 + Deferred Ideas §"kodo skill sync CLI")
  </read_first>

  <action>
    Aplicar EXACTAMENTE 2 ediciones aditivas en `.planning/PROJECT.md`:

    **Edit 1 — Añadir bullet al final de `## Constraints`:**

    Localizar la lista de bullets bajo `## Constraints` (actualmente termina con el bullet
    "Color isolation"). Añadir como último elemento de la lista:

    ```
    - **Orchestrator se lanza desde cwd = repo kodo**: `kodo orchestrator` DEBE invocarse desde el directorio `~/dev/klab/kodo` (o el path real del repo) para que Claude Code auto-cargue `.claude/skills/kodo-orchestrate/skill.md` como skill local. Si se lanza desde otro cwd, la sesión arranca con `src/orchestrator/prompt.md` como render mínimo provider-specific (fallback degradado documentado, no error). La skill canonical solo vive en el repo desde Phase 999.1 (D-04/D-05/D-06); `~/.claude/skills/kodo-orchestrate/` se eliminó al cierre de esa phase.
    ```

    **Edit 2 — Añadir bullet al final de `**Deferred to v0.6 or later:**`:**

    Localizar la lista de bullets bajo `**Deferred to v0.6 or later:**` (actualmente termina
    con HOOK-01). Añadir como último elemento de la lista:

    ```
    - [ ] **SKILL-01**: `kodo skill sync` CLI que empuje la canonical de `<repo>/.claude/skills/` a `~/.claude/skills/`, permitiendo lanzar `kodo orchestrator` desde cualquier `cwd` con la skill auto-cargada. Hoy (post Phase 999.1) se exige cwd = repo (Constraint añadida) en su lugar; si la fricción demuestra ser alta en v0.6+, retomar SKILL-01.
    ```

    Reglas duras:
    - **NO reorganizar** otras secciones de PROJECT.md.
    - **NO tocar** las líneas existentes (incluido el footer `*Last updated: ...*`); las
      dos ediciones son aditivas.
    - **NO añadir** la regla D-05/D-06 a una sección distinta de `## Constraints`.
    - **NO añadir** SKILL-01 a una sección distinta de `Deferred to v0.6 or later`.
    - Idioma: español (consistente con el resto del documento).
  </action>

  <acceptance_criteria>
    - `grep -qE 'Orchestrator se lanza desde cwd = repo kodo' .planning/PROJECT.md` (Edit 1)
    - `grep -q '.claude/skills/kodo-orchestrate/skill.md' .planning/PROJECT.md` (constraint cita el path canonical)
    - `grep -q 'fallback degradado' .planning/PROJECT.md` (constraint nombra el régimen degradado D-02/D-05)
    - `grep -qE '\bSKILL-01\b' .planning/PROJECT.md` (Edit 2: entrada con ID)
    - `grep -q 'kodo skill sync' .planning/PROJECT.md` (SKILL-01 describe el CLI)
    - `[ $(grep -c '^## ' .planning/PROJECT.md) -ge 8 ]` (estructura del documento intacta — al menos 8 H2 headers)
  </acceptance_criteria>

  <verify>
    <automated>bash -c "F=.planning/PROJECT.md; grep -qE 'Orchestrator se lanza desde cwd = repo kodo' \"\$F\" && grep -q '.claude/skills/kodo-orchestrate/skill.md' \"\$F\" && grep -q 'fallback degradado' \"\$F\" && grep -qE '\\bSKILL-01\\b' \"\$F\" && grep -q 'kodo skill sync' \"\$F\" && [ \$(grep -c '^## ' \"\$F\") -ge 8 ]"</automated>
  </verify>

  <done>
    PROJECT.md > Constraints contiene la regla D-05/D-06 con cita al path canonical y al
    régimen degradado. PROJECT.md > Deferred contiene SKILL-01 con la descripción del CLI
    diferido. Estructura general del documento intacta.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Eliminar manualmente ~/.claude/skills/kodo-orchestrate/skill.md (D-04) — gate: archivo NO existe</name>

  <what-built>
    Plan 1 creó `.claude/skills/kodo-orchestrate/skill.md` en el repo (canonical). Plan 5
    Task 1 documentó el contrato operativo nuevo en PROJECT.md. Queda eliminar la fuente
    obsoleta fuera del repo para cerrar la fuente de drift (D-04).

    **Truth a verificar (H4 reformulado)**: tras este checkpoint,
    `~/.claude/skills/kodo-orchestrate/skill.md` NO existe.
  </what-built>

  <how-to-verify>
    El archivo `~/.claude/skills/kodo-orchestrate/skill.md` está FUERA del repo y de
    cualquier sandbox sobre el que el executor pueda actuar autónomamente; lo borra el
    usuario manualmente. Pasos para el humano:

    1. Verificar que la skill canonical existe en el repo (precondición — protege contra
       borrar la global antes de tener la canonical en sitio):
       ```
       test -f .claude/skills/kodo-orchestrate/skill.md && echo "OK: canonical en sitio"
       ```
       Si no imprime "OK", PARAR y reportar — Plan 1 no ha cerrado.

    2. Verificar el estado actual de la skill global:
       ```
       ls -la ~/.claude/skills/kodo-orchestrate/skill.md 2>&1
       ```

    3. Eliminar la skill global (si existe):
       ```
       rm -f ~/.claude/skills/kodo-orchestrate/skill.md
       rmdir ~/.claude/skills/kodo-orchestrate/ 2>/dev/null || true
       ```
       (El `rm -f` no falla si el archivo no existe — escenario "ya no estaba". El
       `rmdir` solo borra el directorio si quedó vacío — si hay otros archivos del
       usuario, los preserva.)

    4. **Acceptance gate H4 obligatorio — verificar no-existencia post-eliminación:**
       ```
       test ! -e ~/.claude/skills/kodo-orchestrate/skill.md && echo "GATE PASS: skill global no existe"
       ```
       El comando DEBE imprimir `GATE PASS: skill global no existe` para que el plan
       cierre. Si imprime otra cosa o `test` retorna exit 1, el checkpoint NO está
       resuelto y el usuario debe investigar (¿permisos? ¿path con espacios? ¿simlink
       persistente?) y reportar.

    5. **Resultado esperado**: la skill canonical vive solo en
       `<repo>/.claude/skills/kodo-orchestrate/skill.md`. Lanzar `kodo orchestrator` desde
       el repo (cwd = repo) sigue funcionando vía auto-carga de la skill local; lanzar
       desde otro cwd cae en el fallback degradado documentado en PROJECT.md > Constraints.

    **Importante**: el acceptance gate de este checkpoint es `test ! -e ...` (paso 4).
    No basta con que el usuario diga "approved" — debe haber ejecutado el comando del
    paso 4 y haber visto `GATE PASS:`. Si el usuario no puede o no quiere eliminar el
    archivo (caso edge: la skill está en uso por otro flujo), reportar para que el
    planner re-evalúe — NO marcar como done sin la verificación.
  </how-to-verify>

  <resume-signal>
    Type "approved — GATE PASS" tras ejecutar el comando del paso 4 y ver
    `GATE PASS: skill global no existe`. Cualquier otra respuesta requiere investigación.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROJECT.md → future planners | Constraints/Deferred guían decisiones de phases futuras |
| User-local filesystem (~/.claude/) | Operación manual fuera del sandbox; riesgo si el usuario teclea mal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1.05-01 | Tampering | rm ~/.claude/skills/ accidental wider scope | mitigate | El paso 3 del checkpoint es explícito y único: `rm -f <path-exacto>` + `rmdir` que falla silenciosamente si el directorio no está vacío. Sin recursión, sin glob, sin variables expandidas. |
| T-999.1.05-02 | Information Disclosure | PROJECT.md público vs path privado del usuario | accept | El path `~/dev/klab/kodo` ya aparece en otras partes de PROJECT.md y en CONTEXT.md; no es información sensible adicional. |
| T-999.1.05-03 | Repudiation | edición sin trazabilidad | mitigate | Las dos ediciones aditivas quedan capturadas en el commit final de la phase (gestionado por el orchestrator GSD); el SUMMARY.md de la phase referencia D-04/D-05/D-06/SKILL-01 explícitamente. |
| T-999.1.05-04 | Tampering / incomplete close | checkpoint cerrado sin eliminación real | mitigate | H4: el resume-signal requiere `GATE PASS` literal tras ejecución observable de `test ! -e ...`. El plan no cierra con "approved" genérico — la verificación es post-condicional. |
</threat_model>

<verification>
- PROJECT.md actualizado con la regla D-05/D-06 en Constraints y SKILL-01 en Deferred
- Skill global eliminada manualmente (checkpoint humano confirma con `test ! -e`)
- Skill canonical sigue presente en `.claude/skills/kodo-orchestrate/skill.md`
- Gate H4: `test ! -e ~/.claude/skills/kodo-orchestrate/skill.md` retorna exit 0 al cierre
</verification>

<success_criteria>
- D-04 cumplida y verificada: `test ! -e ~/.claude/skills/kodo-orchestrate/skill.md` retorna exit 0 al cerrar la phase
- D-05/D-06 capturados como regla operativa en PROJECT.md > Constraints
- SKILL-01 capturado en PROJECT.md > Deferred (trazabilidad)
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-05-SUMMARY.md`.
</output>
