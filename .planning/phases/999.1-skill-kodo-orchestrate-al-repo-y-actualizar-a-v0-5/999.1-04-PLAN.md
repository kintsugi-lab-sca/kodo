---
phase: 999.1
plan: 04
type: execute
wave: 2
depends_on: [999.1-03]
files_modified:
  - test/skill-auto-commit.test.js
autonomous: true
requirements: [D-16]
must_haves:
  truths:
    - "Existe un test que verifica que el hook Stop (al detectar orchestrator session) ejecuta `git commit` cuando hay cambios en `.claude/skills/` del repo tmpdir"
    - "Existe un test que verifica que el hook NO commitea (no-op silencioso) cuando no hay cambios en `.claude/skills/`"
    - "Los tests usan el patrón canónico spawnSync child de Phases 16–17 (LOG-15 + UAT-03): mkdtempSync + HOME override + KODO_ROOT override + git init aislado, NUNCA tocan el repo real del desarrollador ni el state.json del usuario"
    - "El mensaje del commit producido en el escenario A coincide con `skill: orchestrator learnings YYYY-MM-DD` (validado por `git log` en el tmpdir)"
    - "El escenario B verifica el mensaje stderr `no skill changes to commit` en el output del child"
    - "`src/hooks/stop.js` no se modifica en este plan; los tests verifican el comportamiento productivo del hook tras el fix D-14 de Plan 03 sin reescribir API (la firma `async function handleOrchestratorStop()` cero-args se conserva)"
    - "Los tests pasan con `node --test test/skill-auto-commit.test.js` (exit 0) deterministicamente"
  artifacts:
    - path: "test/skill-auto-commit.test.js"
      provides: "Coverage D-16 para handleOrchestratorStop: 2 escenarios (commit / no-op) vía spawnSync child con tmpdir aislado"
      contains: "skill: orchestrator learnings"
  key_links:
    - from: "test/skill-auto-commit.test.js"
      to: "src/hooks/stop.js (vía spawnSync child)"
      via: "spawnSync(process.execPath, [STOP_HOOK], { env: { HOME: tmpHome, KODO_ROOT: tmpRepo, ... }, cwd: tmpRepo, input: JSON.stringify({session_id:'...', cwd: tmpRepo}) })"
      pattern: "spawnSync.*stop\\.js"
---

<objective>
Añadir cobertura D-16 para `handleOrchestratorStop` con `test/skill-auto-commit.test.js`,
usando el **patrón canónico spawnSync child** establecido en Phase 16 LOG-15
(`test/stop-state-transition.test.js`) y Phase 17 UAT-03
(`test/session-of-resolver.test.js`).

Dos escenarios mínimos:
- **Escenario A**: hay cambios sin commitear en `.claude/skills/` del repo tmpdir → el
  hook spawneado ejecuta `git commit` con el mensaje canónico
  `skill: orchestrator learnings YYYY-MM-DD`. Verificación: `git log` del tmpdir tras
  el exit del child.
- **Escenario B**: no hay cambios en `.claude/skills/` → el hook spawneado termina sin
  crear commit (no-op silencioso). Verificación: contador de commits inalterado + el
  stderr del child contiene `no skill changes to commit`.

Purpose: El bloque era dead code mientras el path apuntaba a `skills/` (inexistente). Plan
03 lo re-habilitó + añadió el env override `KODO_ROOT`. Sin tests, una regresión futura
(refactor, rename) puede dejarlo silenciosamente roto otra vez. D-16 lo blinda con el
mismo patrón source-hygiene + spawnSync de Phases 16–17.

**Invariante de scope (H5)**: este plan NO modifica `src/hooks/stop.js`. La firma de
`handleOrchestratorStop` permanece cero-args y no exportada, exactamente como queda tras
Plan 03. El test ejercita el comportamiento productivo del hook a través del CLI
(spawnSync) sin reescribir la API.

Output: `test/skill-auto-commit.test.js` con 2 tests `describe`/`it` pasando
deterministicamente, sin contaminación del repo del desarrollador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-CONTEXT.md

<interfaces>
<!-- Contrato a testear (estado post-Plan 03) -->

From src/hooks/stop.js (post-Plan 03):

L19–21 (KODO_ROOT con env override + SKILL_PATH fixed):
```javascript
const __dirname = dirname(fileURLToPath(import.meta.url));
const KODO_ROOT = process.env.KODO_ROOT || join(__dirname, '..', '..');
const SKILL_PATH = join(KODO_ROOT, '.claude', 'skills', 'kodo-orchestrate', 'skill.md');
```

**Crítico**: KODO_ROOT se evalúa al **import-time** (top-level statement). Para que el
child process lo resuelva al tmpdir, `process.env.KODO_ROOT` DEBE estar set ANTES de que
Node cargue `src/hooks/stop.js`. Como el child es un proceso nuevo, basta con pasar
`env: { KODO_ROOT: tmpRepo, ... }` en `spawnSync` — la env se ve desde el primer import.

L98–122 (runStopHook, detección orchestrator session):
```javascript
export async function runStopHook(input, deps = {}) {
  const findSessionFn = deps.findSessionFn || findSession;
  try {
    const sessionId = input.session_id;
    const cwd = input.cwd || process.cwd();

    let result = findSessionFn({ sessionId, cwd });

    if (!result) {
      const isOrchestratorSession = cwd && (
        cwd === KODO_ROOT ||
        cwd.startsWith(KODO_ROOT + '/')
      );
      if (isOrchestratorSession) {
        await handleOrchestratorStop();
      }
      return;
    }
    // ... resto del flow (no se ejecuta cuando no hay sesión tracked) ...
  }
}
```

**Camino activado por el test**: `input.session_id` apunta a una session inexistente +
`input.cwd === KODO_ROOT` (== tmpRepo cuando el env override está activo). El branch
`!result && isOrchestratorSession` se activa y dispara `handleOrchestratorStop()`. El
hook hace `git status --porcelain .claude/skills/` con `cwd: KODO_ROOT` (== tmpRepo) y
ejecuta commit o no-op según haya cambios.

L224–232 (main entry-point del hook):
```javascript
async function main() {
  const input = JSON.parse(await readStdin());
  await runStopHook(input);
  process.exit(0);
}
// ...
const isMainEntry = process.argv[1] && fileURLToPath(import.meta.url) === process.argv[1];
if (isMainEntry) {
  main();
}
```

El hook como CLI: `node src/hooks/stop.js` lee stdin JSON, ejecuta `runStopHook`, exit 0.

**Patrón canónico de aislamiento (Phase 16 CR-02 + Phase 17 D-01/D-02)**:

```javascript
// 1. mkdtempSync para HOME aislado (state.json/locks no contaminan al user)
// 2. mkdtempSync separado para KODO_ROOT aislado (git workdir de prueba)
// 3. Spawnar el child con env: { HOME: tmpHome, KODO_ROOT: tmpRepo, ...process.env }
// 4. Pasar input por stdin (JSON canónico del hook Stop)
// 5. Cleanup en after() con rmSync recursive force de ambos tmpdirs
// 6. afterEach() limpia entre escenarios si reusan el mismo tmpdir
```

Análogos a copiar:
- `test/session-of-resolver.test.js` L43–72 (spawnSync con HOME override + env explícito
  al child + timeout DoS mitigation)
- `test/stop-state-transition.test.js` (CR-02 fix: mkdtempSync + HOME override antes de
  dynamic import — referencia conceptual; aquí no necesitamos dynamic import porque
  spawnSync arranca un proceso fresh)

Path absoluto al hook (resuelto en `before`):
```javascript
const REPO = resolve(__dirname, '..');
const STOP_HOOK = join(REPO, 'src', 'hooks', 'stop.js');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escribir test/skill-auto-commit.test.js con 2 escenarios spawnSync en tmpdir aislado (NO modificar src/hooks/stop.js)</name>
  <files>test/skill-auto-commit.test.js</files>

  <behavior>
    - **Test A — "commits when .claude/skills/ has uncommitted changes (spawnSync child)"**:
      - Setup: crear `tmpHome` + `tmpRepo` (dos mkdtempSync separados). En `tmpRepo`:
        `git init -q`, configurar user.email/user.name local, `commit.gpgsign false`,
        crear `.claude/skills/kodo-orchestrate/skill.md` con contenido inicial, `git add`
        + `git commit -m "initial"`.
      - Modificar `.claude/skills/kodo-orchestrate/skill.md` (append una línea) → working
        tree dirty.
      - Spawnar `process.execPath` con args `[STOP_HOOK]`, `cwd: tmpRepo`,
        `env: { ...process.env, HOME: tmpHome, KODO_ROOT: tmpRepo }`,
        `input: JSON.stringify({ session_id: 'orch-test-A', cwd: tmpRepo })`,
        `timeout: 10000`, `encoding: 'utf-8'`.
      - Assert: `result.status === 0` (el hook siempre exit 0 — silent-failure
        contract).
      - Assert: `git log --format=%s -n 1` en `tmpRepo` matchea
        `/^skill: orchestrator learnings \d{4}-\d{2}-\d{2}$/`.
      - Assert: `git status --porcelain` en `tmpRepo` devuelve string vacío
        (working tree clean tras el auto-commit).
      - Assert: `git rev-list --count HEAD` ha incrementado en 1 vs el inicial.

    - **Test B — "no-ops silently when .claude/skills/ has no changes (spawnSync child)"**:
      - Setup: igual que Test A pero **NO modificar** `skill.md` tras el `commit -m "initial"`.
      - Spawnar el hook con la misma config.
      - Assert: `result.status === 0`.
      - Assert: `git rev-list --count HEAD` antes y después es idéntico (no commit creado).
      - Assert: `git log --format=%s -n 1` sigue siendo "initial" (no nuevo commit).
      - Assert: `result.stderr` contiene la cadena `'no skill changes to commit'`
        (el hook escribe a stderr este mensaje literal).

    - **Aislamiento (CR-02 Phase 16)**:
      - Dos `mkdtempSync(join(tmpdir(), 'kodo-skill-auto-...'))` por test: uno para
        HOME, otro para el git workdir (KODO_ROOT). Mantenerlos separados aclara
        intención y evita que el hook intente escribir state.json en el mismo dir que
        el git workdir.
      - Cleanup en `afterEach` (o `try/finally`): `rmSync(tmp, { recursive: true, force: true })`
        sobre ambos.
      - **NUNCA** spawnar el hook sin `env: { KODO_ROOT: tmpRepo, HOME: tmpHome }`.
        Hacerlo sin el override haría que el hook commiteara en el repo real del
        desarrollador (regresión a iteración 1).
      - `git config` local al tmpRepo (`-C <tmpRepo> config user.email/user.name` y
        `commit.gpgsign false`) — NUNCA global, NUNCA al gitconfig del usuario.

    - **Patrón de spawnSync (copiar la forma de `test/session-of-resolver.test.js`)**:
      ```javascript
      function runStopHookChild({ tmpHome, tmpRepo, sessionId, cwd }) {
        return spawnSync(
          process.execPath,
          [STOP_HOOK],
          {
            cwd: tmpRepo,
            env: { ...process.env, HOME: tmpHome, KODO_ROOT: tmpRepo },
            input: JSON.stringify({ session_id: sessionId, cwd: cwd ?? tmpRepo }),
            encoding: 'utf-8',
            timeout: 10000, // T-999.1.04-03 DoS mitigation
          },
        );
      }
      ```

    - **Detección orchestrator session**: el hook activa `handleOrchestratorStop()` solo
      cuando `findSession({sessionId, cwd})` retorna `null` Y `cwd === KODO_ROOT` (o
      bajo él). Como el tmpRepo no tiene `~/.kodo/state.json` con esa session, findSession
      retorna null automáticamente. El `cwd === KODO_ROOT` se cumple porque ambos están
      seteados al mismo tmpRepo.
  </behavior>

  <read_first>
    - test/session-of-resolver.test.js L1–135 (patrón canónico spawnSync + HOME override + env explícito al child + timeout — referencia número 1 D-16)
    - test/stop-state-transition.test.js L1–100 (patrón CR-02 mkdtempSync + cleanup robusto — referencia conceptual; nota: usa import dinámico, pero aquí el child process resuelve KODO_ROOT desde env, no necesitamos import dinámico en el runner)
    - src/hooks/stop.js (post Plan 03 — confirmar firma `async function handleOrchestratorStop()` cero-args, NO exportada, KODO_ROOT honra `process.env.KODO_ROOT || join(...)`, paths a `.claude/skills/`, JSDoc actualizado)
  </read_first>

  <action>
    Crear `test/skill-auto-commit.test.js` con la siguiente estructura:

    ```javascript
    // @ts-check
    //
    // test/skill-auto-commit.test.js — Phase 999.1 D-16 coverage.
    //
    // Verifica el contrato auto-commit de handleOrchestratorStop en src/hooks/stop.js:
    //   A) cambios sin commit en .claude/skills/ → git commit con mensaje canónico
    //      (skill: orchestrator learnings YYYY-MM-DD)
    //   B) sin cambios → no-op silencioso + mensaje informativo a stderr
    //
    // Patrón: spawnSync child (canon Phase 16 LOG-15 + Phase 17 UAT-03) con HOME y
    // KODO_ROOT inyectados como env vars al child, repo tmpdir sembrado con git init
    // local. El hook resuelve KODO_ROOT desde process.env (Plan 03 Edit 1: env override
    // aditivo); HOME aísla state.json/locks.
    //
    // Aislamiento (CR-02 Phase 16): mkdtempSync + git init local + git config local.
    // NUNCA toca el repo del desarrollador ni el gitconfig global.
    //
    // Invariante de scope (H5): src/hooks/stop.js NO se modifica en este plan; los
    // tests ejercitan el comportamiento productivo del hook a través del CLI (spawnSync)
    // tras el fix D-14 de Plan 03.

    import { describe, it, afterEach } from 'node:test';
    import assert from 'node:assert/strict';
    import { spawnSync, execSync } from 'node:child_process';
    import { mkdtempSync, rmSync, mkdirSync, writeFileSync, appendFileSync } from 'node:fs';
    import { tmpdir } from 'node:os';
    import { dirname, join, resolve } from 'node:path';
    import { fileURLToPath } from 'node:url';

    const __dirname = dirname(fileURLToPath(import.meta.url));
    const REPO = resolve(__dirname, '..');
    const STOP_HOOK = join(REPO, 'src', 'hooks', 'stop.js');

    /** Setup an isolated git workdir with .claude/skills/ initialized + committed.
     *  Returns { tmpHome, tmpRepo } so the test can clean up both. */
    function makeIsolatedRepo() {
      const tmpHome = mkdtempSync(join(tmpdir(), 'kodo-skill-home-'));
      const tmpRepo = mkdtempSync(join(tmpdir(), 'kodo-skill-repo-'));

      // ~/.kodo/ within the isolated HOME so the hook's findSession() doesn't trip
      // on missing state.json (it tolerates absence, but be explicit).
      mkdirSync(join(tmpHome, '.kodo'), { recursive: true });

      const gitOpts = { cwd: tmpRepo, encoding: 'utf-8', stdio: ['ignore', 'pipe', 'pipe'] };
      const run = (cmd) => execSync(cmd, gitOpts);

      run('git init -q');
      run('git config user.email "test@kodo.local"');
      run('git config user.name "kodo test"');
      // Disable GPG signing locally to avoid prompts in CI.
      run('git config commit.gpgsign false');

      mkdirSync(join(tmpRepo, '.claude', 'skills', 'kodo-orchestrate'), { recursive: true });
      writeFileSync(
        join(tmpRepo, '.claude', 'skills', 'kodo-orchestrate', 'skill.md'),
        '# kodo:orchestrate\n\nInitial skill body.\n',
        'utf-8',
      );
      run('git add .claude/');
      run('git commit -q -m "initial"');

      return { tmpHome, tmpRepo };
    }

    /** Spawn the hook as a child process with HOME + KODO_ROOT overridden.
     *  Mirrors the canonical pattern from test/session-of-resolver.test.js. */
    function runStopHookChild({ tmpHome, tmpRepo, sessionId }) {
      return spawnSync(
        process.execPath,
        [STOP_HOOK],
        {
          cwd: tmpRepo,
          env: { ...process.env, HOME: tmpHome, KODO_ROOT: tmpRepo },
          input: JSON.stringify({ session_id: sessionId, cwd: tmpRepo }),
          encoding: 'utf-8',
          timeout: 10000, // T-999.1.04-03 DoS mitigation
        },
      );
    }

    /** Helper: run git in tmpRepo and return trimmed stdout. */
    function git(tmpRepo, cmd) {
      return execSync(`git ${cmd}`, { cwd: tmpRepo, encoding: 'utf-8' }).trim();
    }

    describe('D-16: handleOrchestratorStop auto-commit (spawnSync canon)', () => {
      let tmpHome;
      let tmpRepo;

      afterEach(() => {
        if (tmpHome) rmSync(tmpHome, { recursive: true, force: true });
        if (tmpRepo) rmSync(tmpRepo, { recursive: true, force: true });
        tmpHome = undefined;
        tmpRepo = undefined;
      });

      it('A: commits when .claude/skills/ has uncommitted changes', () => {
        ({ tmpHome, tmpRepo } = makeIsolatedRepo());
        const headBefore = git(tmpRepo, 'rev-parse HEAD');
        const countBefore = parseInt(git(tmpRepo, 'rev-list --count HEAD'), 10);

        // Dirty the working tree
        appendFileSync(
          join(tmpRepo, '.claude', 'skills', 'kodo-orchestrate', 'skill.md'),
          '- [2026-05-11] new learning\n',
          'utf-8',
        );

        const result = runStopHookChild({
          tmpHome,
          tmpRepo,
          sessionId: 'orch-test-A',
        });

        assert.equal(result.status, 0, `hook should exit 0 (silent-failure); stderr: ${result.stderr}`);

        const lastSubject = git(tmpRepo, 'log --format=%s -n 1');
        assert.match(
          lastSubject,
          /^skill: orchestrator learnings \d{4}-\d{2}-\d{2}$/,
          'commit subject canonical',
        );

        const status = git(tmpRepo, 'status --porcelain');
        assert.equal(status, '', 'working tree clean after auto-commit');

        const countAfter = parseInt(git(tmpRepo, 'rev-list --count HEAD'), 10);
        assert.equal(countAfter, countBefore + 1, 'exactly one new commit created');

        const headAfter = git(tmpRepo, 'rev-parse HEAD');
        assert.notEqual(headAfter, headBefore, 'HEAD advanced');
      });

      it('B: no-ops silently when .claude/skills/ has no changes', () => {
        ({ tmpHome, tmpRepo } = makeIsolatedRepo());
        const headBefore = git(tmpRepo, 'rev-parse HEAD');
        const countBefore = parseInt(git(tmpRepo, 'rev-list --count HEAD'), 10);

        const result = runStopHookChild({
          tmpHome,
          tmpRepo,
          sessionId: 'orch-test-B',
        });

        assert.equal(result.status, 0, `hook should exit 0; stderr: ${result.stderr}`);

        const headAfter = git(tmpRepo, 'rev-parse HEAD');
        const countAfter = parseInt(git(tmpRepo, 'rev-list --count HEAD'), 10);
        assert.equal(headAfter, headBefore, 'HEAD unchanged');
        assert.equal(countAfter, countBefore, 'commit count unchanged');

        const lastSubject = git(tmpRepo, 'log --format=%s -n 1');
        assert.equal(lastSubject, 'initial', 'last commit is still "initial"');

        assert.ok(
          result.stderr.includes('no skill changes to commit'),
          `stderr should mention "no skill changes to commit"; got: ${JSON.stringify(result.stderr)}`,
        );
      });
    });
    ```

    Reglas duras:
    - **NUNCA** spawnar el hook sin `env: { KODO_ROOT: tmpRepo, HOME: tmpHome }`. Sin el
      override KODO_ROOT, el hook usa `join(__dirname, '..', '..')` = el repo real, y
      Test A commitearía sobre `~/dev/klab/kodo` (regresión a iter 1).
    - `git config` debe ser local (sin `--global`).
    - `git config commit.gpgsign false` local para evitar prompts GPG en CI.
    - `afterEach` MUST limpiar ambos tmpdirs incluso si el test falla.
    - El test NO debe importar `src/hooks/stop.js` directamente (la firma es cero-args
      y no exportada — solo se ejercita via spawnSync, honra H5).
    - El test debe pasar `node --test test/skill-auto-commit.test.js` con exit 0
      determinísticamente.
    - **No usar `--global`**: cualquier `git config` con flag global falla la AC.
    - **No tocar `src/hooks/stop.js`** ni ningún otro archivo de `src/` desde este plan.
  </action>

  <acceptance_criteria>
    - `test -f test/skill-auto-commit.test.js` (archivo existe)
    - `grep -q "import { spawnSync" test/skill-auto-commit.test.js` (patrón canon: spawnSync)
    - `! grep -qE "from '\\.\\./src/hooks/stop\\.js'" test/skill-auto-commit.test.js` (H5: NO import directo del módulo del hook)
    - `! grep -q "import { handleOrchestratorStop }" test/skill-auto-commit.test.js` (H5: NO se importa la función — sigue siendo internal)
    - `grep -q 'STOP_HOOK' test/skill-auto-commit.test.js` (path al binario del hook resuelto)
    - `grep -q 'src/hooks/stop.js' test/skill-auto-commit.test.js` (referencia al hook por path, no import)
    - `grep -q 'mkdtempSync' test/skill-auto-commit.test.js` (tmpdir aislado)
    - `grep -q 'git init' test/skill-auto-commit.test.js` (repo aislado, no usa real)
    - `grep -q 'commit.gpgsign false' test/skill-auto-commit.test.js` (sin prompts GPG)
    - `! grep -q -- '--global' test/skill-auto-commit.test.js` (NUNCA touch gitconfig global)
    - `grep -q "KODO_ROOT: tmpRepo" test/skill-auto-commit.test.js` (env override al child — sin esto el test commitearía en el repo real)
    - `grep -q "HOME: tmpHome" test/skill-auto-commit.test.js` (HOME override al child)
    - `grep -qE "describe\\(.+D-16" test/skill-auto-commit.test.js` (suite etiquetada D-16)
    - `grep -c "it\\(" test/skill-auto-commit.test.js | awk '{exit !($1 >= 2)}'` (al menos 2 tests)
    - `grep -q 'skill: orchestrator learnings' test/skill-auto-commit.test.js` (assert canónico mensaje commit)
    - `grep -q 'no skill changes to commit' test/skill-auto-commit.test.js` (assert stderr escenario B)
    - `grep -q "timeout: 10000" test/skill-auto-commit.test.js` (DoS mitigation, T-999.1.04-03)
    - `node --test test/skill-auto-commit.test.js` exits 0 (suite pasa)
    - `node --test` exits 0 (suite global verde — no regresiones)
    - **Invariante H5 (post-execución):** `git diff --quiet src/hooks/stop.js` exits 0
      (este plan NO modifica el archivo del hook respecto a su estado post-Plan 03)
  </acceptance_criteria>

  <verify>
    <automated>bash -c "F=test/skill-auto-commit.test.js; test -f \"\$F\" && grep -q 'import { spawnSync' \"\$F\" && ! grep -qE \"from '\\.\\./src/hooks/stop\\.js'\" \"\$F\" && ! grep -q 'import { handleOrchestratorStop }' \"\$F\" && grep -q 'STOP_HOOK' \"\$F\" && grep -q 'src/hooks/stop.js' \"\$F\" && grep -q 'mkdtempSync' \"\$F\" && grep -q 'git init' \"\$F\" && grep -q 'commit.gpgsign false' \"\$F\" && ! grep -q -- '--global' \"\$F\" && grep -q 'KODO_ROOT: tmpRepo' \"\$F\" && grep -q 'HOME: tmpHome' \"\$F\" && grep -q 'D-16' \"\$F\" && [ \$(grep -c 'it(' \"\$F\") -ge 2 ] && grep -q 'skill: orchestrator learnings' \"\$F\" && grep -q 'no skill changes to commit' \"\$F\" && grep -q 'timeout: 10000' \"\$F\" && node --test test/skill-auto-commit.test.js && node --test"</automated>
  </verify>

  <done>
    `test/skill-auto-commit.test.js` existe con 2 tests (commit + no-op) que spawnean el
    hook como child process con HOME + KODO_ROOT overridden a tmpdirs aislados, NO importan
    el módulo del hook directamente (honra H5), y pasan determinísticamente. `src/hooks/stop.js`
    no fue modificado en este plan. Suite global verde sin regresiones. D-16 cubierta vía
    el patrón canónico spawnSync (Phase 16 LOG-15 + Phase 17 UAT-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → host filesystem | Tests crean tmpdirs y ejecutan git en ellos |
| test suite → host gitconfig | Riesgo de tocar gitconfig global del desarrollador |
| test suite → real kodo repo | Riesgo de commitear en `~/dev/klab/kodo` si el override KODO_ROOT no se aplica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1.04-01 | Tampering | gitconfig global | mitigate | `git config user.email/name` sin `--global` aplica solo al repo local en tmpdir. Acceptance criterion `! grep -q -- '--global'` impide regresión. |
| T-999.1.04-02 | Tampering | repo real del desarrollador | mitigate | El test SIEMPRE pasa `KODO_ROOT: tmpRepo` al child via env. Acceptance criterion `grep -q 'KODO_ROOT: tmpRepo'` impide regresión: si alguien quita el env override en el futuro, el test commitearía en el repo real Y la AC fallaría. Plan 03 fuerza que el código acepte el env (línea aditiva en stop.js L20). |
| T-999.1.04-03 | Denial of Service | child process hung | mitigate | `timeout: 10000` en spawnSync corta cualquier child runaway en 10s. AC grep verifica que el timeout está presente. |
| T-999.1.04-04 | Denial of Service | GPG prompt en CI | mitigate | `git config commit.gpgsign false` local desactiva firmas. Sin esto, un CI con GPG global configurado puede bloquear esperando input. |
| T-999.1.04-05 | Information Disclosure | tmpdir orphan files | mitigate | `afterEach` con `rmSync recursive force` limpia ambos tmpdirs (HOME + repo). Aunque falle el test, los dirs quedan en `/tmp` o equivalente, no en HOME. |
| T-999.1.04-06 | Scope creep | refactor encubierto de stop.js | mitigate | Acceptance criterion `git diff --quiet src/hooks/stop.js` (post-execución) y `! grep -q 'import { handleOrchestratorStop }'` blindan H5: este plan NO modifica el hook ni importa su API. |
</threat_model>

<verification>
- Test file presente, usa spawnSync child (NO import directo del módulo del hook)
- 2 tests pasan en aislamiento dual tmpdir (HOME + repo) con env override KODO_ROOT
- Suite global verde
- `src/hooks/stop.js` intacto en este plan
</verification>

<success_criteria>
- D-16 cubierta vía el patrón canónico spawnSync de Phases 16–17 (CONTEXT.md textual)
- Aislamiento total (mkdtempSync x2 + git local + gpgsign off + timeout)
- H5 honrado: el plan no toca `src/hooks/stop.js`
- Comportamiento productivo del hook intacto (regression: stop.test.js + stop-state-transition.test.js verdes)
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-skill-kodo-orchestrate-al-repo-y-actualizar-a-v0-5/999.1-04-SUMMARY.md`.
</output>
