---
phase: 39-paneles-auxiliares-comentarios-logs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/client.js
  - src/cli/dashboard/select.js
  - .planning/PROJECT.md
  - test/dashboard-client.test.js
  - test/dashboard-select.test.js
autonomous: true
requirements: [TUI-15, TUI-16]

must_haves:
  truths:
    - "fetchComments(baseUrl, taskId) returns {ok:true,data:{comments}} on 200, distinguishes 404 from 5xx/network, treats empty comments as ok"
    - "fetchLogs(baseUrl) returns {ok:true,data:{logs}} on 200, never throws on bad shape/network/corrupt JSON"
    - "grepLogs(logs, session) returns only log entries whose msg substring-matches task_ref or workspace_ref; empty needles -> []; no-match -> []"
    - "PROJECT.md /logs wording reflects best-effort substring grep with no per-line session_id (SC#4)"
  artifacts:
    - path: "src/cli/dashboard/client.js"
      provides: "fetchComments + fetchLogs never-throws clients with 404-discriminable result"
      exports: ["fetchComments", "fetchLogs"]
    - path: "src/cli/dashboard/select.js"
      provides: "grepLogs pure substring filter over shared log buffer"
      exports: ["grepLogs"]
    - path: "test/dashboard-client.test.js"
      provides: "D-07 discriminant matrix for fetchComments/fetchLogs"
    - path: "test/dashboard-select.test.js"
      provides: "grepLogs pure-function unit tests"
  key_links:
    - from: "src/cli/dashboard/client.js"
      to: "GET /comments/<task_id> + GET /logs"
      via: "fetchFn injectable, encodeURIComponent(taskId)"
      pattern: "encodeURIComponent"
    - from: "src/cli/dashboard/select.js#grepLogs"
      to: "String.includes (anti-ReDoS)"
      via: "substring match, never new RegExp"
      pattern: "includes"
---

<objective>
Construir la capa de DATOS y DERIVE de los dos overlays (sin React, sin ink): los clientes
HTTP never-throws `fetchComments`/`fetchLogs` (espejo exacto de `fetchStatus`) y el helper puro
`grepLogs` (substring anti-ReDoS sobre el buffer compartido de `/logs`). Además corrige/verifica
el wording de PROJECT.md sobre `/logs` (D-08, SC#4).

Purpose: Esta es la base testeable sin host del Plan 02 (overlay UI). El invariante "no crash"
de TUI-15/TUI-16 es ESTRUCTURAL aquí — todo modo de fallo colapsa al discriminante `{ok}`, jamás
una excepción que llegue a React. El Plan 02 NO puede empezar sin estas tres funciones.

Output: `fetchComments`/`fetchLogs` exportadas en client.js, `grepLogs` exportada en select.js,
PROJECT.md verificado/corregido, dos suites de test verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-paneles-auxiliares-comentarios-logs/39-CONTEXT.md
@.planning/phases/39-paneles-auxiliares-comentarios-logs/39-PATTERNS.md

<interfaces>
<!-- Contratos exactos extraídos del codebase — el executor NO necesita explorar. -->

Análogo VERBATIM a copiar — `src/cli/dashboard/client.js` lines 47-58 (fetchStatus):
```
export async function fetchStatus(baseUrl, fetchFn = globalThis.fetch, signal) {
  try {
    const res = await fetchFn(`${baseUrl}/status`, { signal });
    if (!res.ok) return { ok: false, error: `HTTP ${res.status}` };
    const data = await res.json();
    if (!Array.isArray(data.sessions)) return { ok: false, error: 'bad shape' };
    return { ok: true, data };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err) };
  }
}
```

Server contract (src/server.js — NO se toca, ya existe):
- GET /comments/<task_id> (lines 421-441): 200 -> `{ comments: [...] }`; 404 -> `{ error:'Session not found' }`; 500 -> `{ error }`.
- GET /logs (lines 415-419): 200 -> `{ logs: [...] }`.
- Log entry shape (src/server.js lines 21-29): `{ ts: ISOString, level: 'info'|'warn'|'error', msg: string }`. Buffer reverse() newest-first, ring de LOG_BUFFER_SIZE líneas, SIN session_id por línea.

Análogo del filtro substring puro — `src/cli/dashboard/select.js#applyFilter` lines 135-152:
usa `String.includes` sobre strings lowercased, NUNCA `new RegExp` (anti-ReDoS, T-36-01).

Análogo de tests — `test/dashboard-client.test.js` lines 31-56: leak guard (`before`/`after`
sustituyen `globalThis.fetch` por un thrower), `makeFetch({status, ok, json})` builder.
`test/dashboard-filter.test.js`: tests puros de select.js sin React/ink.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchComments + fetchLogs never-throws en client.js (404 discriminable)</name>
  <files>src/cli/dashboard/client.js, test/dashboard-client.test.js</files>
  <read_first>
    - src/cli/dashboard/client.js (el archivo a modificar; análogo fetchStatus lines 47-58)
    - test/dashboard-client.test.js (harness leak-guard + makeFetch builder; análogo del test)
    - src/server.js lines 415-441 (contratos /logs y /comments/<task_id>)
  </read_first>
  <behavior>
    fetchComments(baseUrl, taskId, fetchFn = globalThis.fetch, signal):
    - 200 con `{comments:[{...}]}` -> `{ok:true, data:{comments:[...]}}`.
    - 200 con `{comments:[]}` -> `{ok:true, data:{comments:[]}}` (vacío NO es error — D-07).
    - 404 -> `{ok:false, error, code:'not-found', status:404}` (DISCRIMINABLE de 5xx — D-07 crítico).
    - 500 -> `{ok:false, error, code:'http', status:500}`.
    - ECONNREFUSED / fetchFn throws -> `{ok:false, error, code:'network'}`.
    - JSON corrupto (json() lanza) -> `{ok:false, ...}` never-throws.
    - 200 sin `comments` array (bad shape) -> `{ok:false, error:'bad shape'}`.
    fetchLogs(baseUrl, fetchFn = globalThis.fetch, signal):
    - 200 con `{logs:[...]}` -> `{ok:true, data:{logs:[...]}}`.
    - 200 sin `logs` array -> `{ok:false, error:'bad shape'}`.
    - 5xx / network / JSON corrupto -> `{ok:false}` never-throws.
  </behavior>
  <action>
    Añadir DOS funciones exportadas a `src/cli/dashboard/client.js`, copiando la FORMA exacta de
    `fetchStatus` (try/catch never-throws, `fetchFn` inyectable con default `globalThis.fetch`,
    `signal` opcional, discriminante `{ok}`).

    `fetchComments(baseUrl, taskId, fetchFn = globalThis.fetch, signal)`:
    - URL: usa template literal con `${baseUrl}/comments/${encodeURIComponent(taskId)}` — el server
      resuelve por `task_id` (D-02); `encodeURIComponent` es OBLIGATORIO (T-39-01, task_id va en path).
    - CRÍTICO D-07: `fetchStatus` colapsa todo HTTP no-ok a un solo string `HTTP ${status}`, lo cual
      es INSUFICIENTE aquí — App.js debe distinguir 404 ("task not found") de 5xx/red ("error fetching
      comments"). Por eso el resultado de fallo DEBE incluir un discriminante. Cuando `!res.ok`:
      retornar `{ok:false, error: \`HTTP ${res.status}\`, status: res.status, code: res.status === 404 ? 'not-found' : 'http'}`.
      En el `catch` (red/abort/corrupto): retornar `{ok:false, error: msg, code:'network'}`.
    - Shape guard tras `res.json()`: `if (!Array.isArray(data.comments)) return {ok:false, error:'bad shape'}`.
    - Éxito: `{ok:true, data}` (vacío `comments:[]` es ok — emptiness es estado de UI, no error).

    `fetchLogs(baseUrl, fetchFn = globalThis.fetch, signal)`:
    - URL: `${baseUrl}/logs` (sin params — el grep es un paso SEPARADO en select.js, fetchLogs solo trae
      el buffer compartido, nunca filtra).
    - Shape guard: `if (!Array.isArray(data.logs)) return {ok:false, error:'bad shape'}`.
    - Mismo never-throws que fetchStatus; no necesita discriminante de status (no hay caso 404
      semántico — `/logs` siempre existe).

    Documentación del archivo: actualizar el comentario YAGNI de las lines 28-29 ("`fetchComments` /
    `fetchLogs` quedan diferidos a Phases 36/38") — Phase 39 los implementa. Reescribir esa línea del
    header para reflejar que ambas funciones ya existen.

    Tests (Wave 0 RED primero): extender `test/dashboard-client.test.js` reusando `makeFetch` y el
    leak guard existentes. Cubrir la matriz D-07 de AMBAS funciones (ver <behavior>). Para 404 vs 500
    assertar el `code` discriminante (`'not-found'` vs `'http'`), no solo `ok:false`.
  </action>
  <verify>
    <automated>node --test test/dashboard-client.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `client.js` contains `export async function fetchComments` AND `export async function fetchLogs`.
    - `grep -n "encodeURIComponent" src/cli/dashboard/client.js` matches (task_id encoded en el path).
    - `grep -n "new RegExp" src/cli/dashboard/client.js` returns nothing.
    - `node --test test/dashboard-client.test.js` exits 0 con casos: comments 200/empty/404(code=not-found)/500(code=http)/ECONNREFUSED/corrupt/bad-shape, logs 200/bad-shape/500/network.
    - `node --test test/format-isolation.test.js` exits 0 (cero picocolors en client.js — sin imports de color añadidos).
  </acceptance_criteria>
  <done>fetchComments distingue 404 de 5xx/red vía `code`, fetchLogs trae el buffer crudo, ambas never-throw, suite de cliente verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: grepLogs pure substring helper en select.js (anti-ReDoS)</name>
  <files>src/cli/dashboard/select.js, test/dashboard-select.test.js</files>
  <read_first>
    - src/cli/dashboard/select.js (el archivo a modificar; análogo applyFilter lines 135-152)
    - test/dashboard-select.test.js (análogo de tests puros — si no existe, crearlo; mirror de test/dashboard-filter.test.js)
    - src/server.js lines 21-29 (shape de la entrada de log `{ts, level, msg}`)
  </read_first>
  <behavior>
    grepLogs(logs, session) donde logs = `Array<{ts, level, msg}>`, session = `{task_ref?, workspace_ref?}`:
    - Casa por SUBSTRING (lowercased) de `task_ref` O `workspace_ref` contra `entry.msg` (lowercased).
    - needles vacíos (sin task_ref ni workspace_ref) -> `[]` (no inunda con el buffer entero).
    - sin matches -> `[]`.
    - Preserva el orden de entrada de `logs` (no reordena; el buffer ya viene newest-first del server).
    - Un ref con chars regex-especiales (p.ej. `KL-1.*`) se matchea LITERAL como substring, NUNCA se compila.
  </behavior>
  <action>
    Añadir `export function grepLogs(logs, session)` a `src/cli/dashboard/select.js`, copiando la
    disciplina `String.includes` de `applyFilter` (lines 135-152). Implementación:
    - Construir `needles` = `[session.task_ref, session.workspace_ref].filter(Boolean).map(s => s.toLowerCase())`.
    - Si `needles.length === 0` retornar `[]`.
    - `return logs.filter((e) => { const hay = (e.msg ?? '').toLowerCase(); return needles.some((n) => hay.includes(n)); })`.
    - D-03 CRÍTICO: NO parsear `session_id` por línea — el buffer no lo garantiza; substring-only evita
      overlay vacío durante actividad real. NUNCA `new RegExp` (anti-ReDoS, espejo de la decisión de parseFilter).
    - Vive en select.js (capa derive), NO en format.js (presentación) — es un filtro, no un cell projector.
    - JSDoc con la shape `Array<{ts:string, level:string, msg:string}>` y nota de que el buffer es COMPARTIDO
      y best-effort (por eso el header del overlay lo etiqueta — D-04, lo implementa Plan 02).

    Tests (Wave 0 RED primero): crear/extender `test/dashboard-select.test.js` con assertions puras (sin
    ink/host): match por task_ref, match por workspace_ref (OR), needles vacíos -> [], no-match -> [],
    char regex-especial matcheado literal (substring), orden preservado.
  </action>
  <verify>
    <automated>node --test test/dashboard-select.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `select.js` contains `export function grepLogs`.
    - `grep -n "new RegExp" src/cli/dashboard/select.js` returns nothing (anti-ReDoS preservado).
    - `node --test test/dashboard-select.test.js` exits 0 con: match task_ref, match workspace_ref OR, needles vacíos -> [], no-match -> [], char `.*` matcheado literal.
    - `node --test test/format-isolation.test.js` exits 0 (cero picocolors en select.js).
  </acceptance_criteria>
  <done>grepLogs filtra por substring OR de task_ref/workspace_ref sobre msg, never compila regex, suite pura verde.</done>
</task>

<task type="auto">
  <name>Task 3: D-08 — verificar/corregir wording de PROJECT.md sobre /logs (SC#4)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 25-38 (sección Current Milestone v0.9, donde vive el wording de /logs)
    - .planning/phases/39-paneles-auxiliares-comentarios-logs/39-CONTEXT.md (D-08)
  </read_first>
  <action>
    D-08 (SC#4): verificar que TODO el wording de PROJECT.md sobre `/logs` diga "best-effort substring
    grep" sin `session_id` real, NO "filtrado por session_id".

    HALLAZGO del pattern mapper confirmado: PROJECT.md línea 32 YA dice correctamente: "grep best-effort
    sobre el buffer compartido de `GET /logs` — no hay `session_id` real en el buffer". Ese wording es
    correcto y satisface SC#4 — NO tocarlo.

    Acción concreta: escanear el resto de PROJECT.md (`grep -in "session_id\|/logs\|filtrado por"
    .planning/PROJECT.md`) buscando wording residual incorrecto que sugiera tail per-session o filtrado
    por session_id en `/logs`. Si NO se encuentra residuo incorrecto -> marcar SC#4 como ya satisfecho
    en el SUMMARY (cero cambios a PROJECT.md por este sub-criterio). Si SÍ se encuentra residuo
    incorrecto -> corregirlo a "best-effort substring grep (no per-line session_id)".

    NOTA aparte (NO es D-08, NO corregir aquí): la línea 31 menciona `cmux attach <workspace_ref>` (verbo
    viejo, revisado a `select-workspace` en Phase 37). Eso es ortogonal a D-08 (que es SOLO sobre /logs).
    Anotar el drift en el SUMMARY como observación pero NO corregirlo en esta fase (fuera de scope D-08).
  </action>
  <verify>
    <automated>grep -in "session_id" .planning/PROJECT.md | grep -i "/logs\|filtrado" || echo "no incorrect /logs+session_id wording — SC#4 satisfied"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "filtrado por session_id" .planning/PROJECT.md` returns nothing.
    - El wording de /logs en PROJECT.md dice "best-effort" y "no hay session_id real" (o equivalente honesto).
    - SUMMARY documenta si SC#4 estaba ya satisfecho (cero diff) o qué residuo se corrigió.
  </acceptance_criteria>
  <done>PROJECT.md no contiene wording que afirme filtrado por session_id en /logs; SC#4 satisfecho y documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard TUI -> server localhost | El cliente lee `/comments/<task_id>` y `/logs` del server kodo local (HTTP, sin auth, read-only). task_id cruza en la URL. |
| server log buffer -> overlay | El buffer `/logs` es COMPARTIDO entre sesiones (sin session_id por línea); el grep puede exponer líneas de otras sesiones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering/Injection | fetchComments URL (`/comments/<task_id>`) | mitigate | task_id va en el path -> `encodeURIComponent(taskId)` OBLIGATORIO (acceptance criteria Task 1). Evita path traversal / inyección de segmentos de URL. |
| T-39-02 | DoS (ReDoS) | grepLogs substring match | mitigate | `String.includes` only, NUNCA `new RegExp` desde un ref (espejo de applyFilter / T-36-01). Acceptance criteria: grep "new RegExp" vacío en select.js. |
| T-39-03 | Information Disclosure | overlay de logs sobre buffer compartido | accept | El buffer `/logs` es un ring compartido sin session_id; el grep puede mostrar líneas de otras sesiones. Riesgo bajo (logs locales del propio operador, sin PII de terceros). Mitigado por la etiqueta honesta D-04 ("may include other sessions") que implementa el Plan 02 — no se oculta el hecho. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 39 NO añade dependencias (fetch built-in, ink/react ya instalados). Cero install tasks. |
</threat_model>

<verification>
- `node --test test/dashboard-client.test.js` verde (matriz D-07 de ambas funciones, 404 discriminable).
- `node --test test/dashboard-select.test.js` verde (grepLogs puro).
- `node --test test/format-isolation.test.js` verde (cero picocolors en client.js/select.js).
- PROJECT.md sin wording "filtrado por session_id" sobre /logs.
</verification>

<success_criteria>
- fetchComments distingue 404 ("task not found") de 5xx/red ("error fetching comments") vía `code`.
- fetchComments trata `comments:[]` como `{ok:true}` (vacío no es error).
- fetchLogs trae el buffer crudo never-throws; el grep es un paso separado.
- grepLogs casa por substring OR de task_ref/workspace_ref, never compila regex, needles vacíos -> [].
- SC#4: PROJECT.md /logs wording honesto y verificado.
</success_criteria>

<output>
Create `.planning/phases/39-paneles-auxiliares-comentarios-logs/39-01-SUMMARY.md` when done.
</output>
