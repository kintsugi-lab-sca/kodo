---
phase: 10-orchestrator-verification-gate
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/orchestrator/prompt.md
  - src/orchestrator/launch.js
  - src/hooks/stop.js
  - test/orchestrator-gsd.test.js
autonomous: true
requirements: [GSD-07]

must_haves:
  truths:
    - "prompt.md incluye una sección `## Sesiones GSD` en español con instrucciones para detectar session.gsd y ejecutar `kodo gsd verify <session-id>`"
    - "Un operador humano que lea prompt.md renderizado ve los 4 verdicts (pass, fail, missing, malformed) y las rutas a los artefactos (PROJECT.md, ROADMAP.md, PLAN.md, VERIFICATION.md)"
    - "El orquestador ve el tag `[GSD phase N]` junto al task_ref en el contexto cuando la sesión tiene gsd=true y phase_id resuelto"
    - "El orquestador ve el tag `[GSD bootstrap]` en el contexto cuando una sesión GSD carece de phase_id resuelto (fallback Pitfall #4)"
    - "stop.js nudge al orquestador incluye `kodo gsd verify <session-id>` cuando session.gsd === true"
    - "stop.js nudge conserva el texto original cuando session.gsd es false/undefined"
    - "El operador humano que lee un comentario Plane lo recibe en español (D-16); el payload que consume el agente vía buildGsdContext mantiene su inglés previo (Phase 8 D-04 sin regresión)"
  artifacts:
    - path: "src/orchestrator/prompt.md"
      provides: "Sección GSD del prompt del orquestador"
      contains: "## Sesiones GSD"
      min_lines: 85
    - path: "src/orchestrator/launch.js"
      provides: "buildContextSummary con tag [GSD phase N]"
      contains: "s.gsd"
    - path: "src/hooks/stop.js"
      provides: "Nudge condicional GSD"
      contains: "kodo gsd verify"
    - path: "test/orchestrator-gsd.test.js"
      provides: "Tests de las tres modificaciones (prompt.md + launch.js + stop.js)"
      contains: "node:test"
  key_links:
    - from: "src/orchestrator/prompt.md"
      to: "kodo gsd verify CLI"
      via: "texto imperativo en español"
      pattern: "kodo gsd verify"
    - from: "src/orchestrator/launch.js"
      to: "session.gsd + session.phase_id"
      via: "buildContextSummary loop"
      pattern: "s\\.gsd|\\[GSD phase"
    - from: "src/hooks/stop.js"
      to: "kodo gsd verify ${session.session_id}"
      via: "cmux.send text"
      pattern: "kodo gsd verify"
---

<objective>
Integrar el flag GSD en los tres puntos de contacto del orquestador: (1) prompt.md con una sección condicional en español que instruye al orquestador a leer artefactos y correr el verify; (2) `buildContextSummary` en launch.js para que las sesiones GSD aparezcan con tag `[GSD phase N]`; (3) `stop.js` nudge que menciona `kodo gsd verify <session-id>` cuando la sesión que cerró era GSD. Preservar el idioma español del prompt (D-16) y no tocar `buildGsdContext` (inglés, Phase 8 D-04). Cubre GSD-07 (metadata GSD al orquestador + direccionamiento a artefactos).

Purpose: Sin esta integración, el orquestador Claude no sabe cuándo una sesión es GSD ni cómo actuar al cerrarse — el CLI de Plan 10-03 existiría en el vacío. Este plan conecta el ciclo de supervisión con el gate.

Output:
- `src/orchestrator/prompt.md` con sección `## Sesiones GSD` agregada al final.
- `src/orchestrator/launch.js` con `buildContextSummary` modificada para taggear sesiones GSD.
- `src/hooks/stop.js` con nudge condicional GSD.
- `test/orchestrator-gsd.test.js` con tests para prompt.md (readFileSync), launch.js (buildContextSummary) y stop.js (buildStopNudgeText).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md
@.planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md
@.planning/phases/10-orchestrator-verification-gate/10-01-PLAN.md

<interfaces>
<!-- Sin nuevas exports. Este plan modifica archivos existentes. Contratos relevantes: -->

```javascript
// src/session/state.js — Session typedef (existente, sin cambios)
// gsd?: boolean — Phase 8 D-10
// phase_id?: string — Phase 9 D-11 (optional, ausente en bootstrap)

// src/orchestrator/launch.js — buildContextSummary (existente)
function buildContextSummary(sessions, config): string;

// src/hooks/stop.js — handleStop (existente, líneas 116-125 bloque cmux.send)
```

<!-- Paths relevantes (absolutos, evita confusiones): -->
- /Users/alex/dev/klab/kodo/src/orchestrator/prompt.md
- /Users/alex/dev/klab/kodo/src/orchestrator/launch.js (líneas 108-126 es buildContextSummary)
- /Users/alex/dev/klab/kodo/src/hooks/stop.js (líneas 115-125 es el bloque de notify orchestrator)

<!-- Patrón de idiomas (D-16 + D-04 Phase 8): -->
- prompt.md → ESPAÑOL (sin regresión)
- stop.js nudge → ESPAÑOL (consistente con el texto existente "La sesión X ha terminado y está en Review...")
- buildGsdContext (hooks/session-start.js) → INGLÉS (Phase 8 D-04, NO TOCAR)
- Comentario Plane (runGsdVerify) → ESPAÑOL (Plan 10-02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir sección `## Sesiones GSD` a prompt.md + test de contenido</name>
<files>src/orchestrator/prompt.md, test/orchestrator-gsd.test.js</files>
  <read_first>
    - src/orchestrator/prompt.md (archivo completo, 72 líneas)
    - .planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md §D-19 (sección condicional)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §5 (pattern prompt.md modify)
  </read_first>
  <behavior>
    Un test Nyquist-compliant (automatizado) DEBE verificar el contenido renderizado de prompt.md:
    - PM1: `prompt.md` contiene heading literal `## Sesiones GSD`.
    - PM2: contiene el string literal `kodo gsd verify <session-id>`.
    - PM3: contiene los 4 verdicts como palabras literales: `pass`, `fail`, `missing`, `malformed`.
    - PM4: contiene referencias a los 4 artefactos: `PROJECT.md`, `ROADMAP.md`, `PLAN.md`, `VERIFICATION.md`.
    - PM5: conserva el placeholder existente `{{provider_name}}` (no introduce placeholders nuevos).
    - PM6: contiene el string `kodo gsd inspect <task-id>` (referencia debug opcional).
    - PM7: sin frases inglesas típicas de prompts (no `you must`, no `please`, no `execute your`).
  </behavior>
  <action>
    Abrir `src/orchestrator/prompt.md`. Añadir al final del archivo (después de la línea 72, que es la última línea actual `Para {{provider_name}}, usa el {{mcp_tool}} disponible en tu sesión.`) el siguiente bloque literal:

    ```markdown


    ## Sesiones GSD

    Las sesiones con `gsd: true` en `state.json` siguen un flujo estructurado de fase (`PROJECT.md` + `ROADMAP.md` + `PLAN.md` + `VERIFICATION.md`). Cuando una sesión GSD termina y entra a Review:

    1. **Lee los artefactos** — `PROJECT.md`, `ROADMAP.md` y `phases/<n>/PLAN.md` del `project_path` de la sesión (usa la tool `Read` directamente).
    2. **Ejecuta el gate** — `kodo gsd verify <session-id>`. El CLI lee el frontmatter de `VERIFICATION.md`, computa el verdict y postea el comentario en {{provider_name}}.
    3. **Actúa según el verdict del stdout:**
       - `pass` — continúa con tu ronda normal. El CLI ya comentó la tarea y la transicionó a Review.
       - `fail` — el CLI ya comentó el motivo (gaps, must-haves incompletos, o status=failed). Espera a que el humano corrija `VERIFICATION.md` y re-dispare.
       - `missing` — el CLI ya comentó pidiendo que se ejecute `/gsd-verify-work`. No hagas nada manual.
       - `malformed` — el CLI ya comentó con el detalle del error del frontmatter. Espera corrección humana.
    4. **Debugging previo al verify:** si dudas de la resolución de fase, puedes correr `kodo gsd inspect <task-id>` (dry-run del resolver).

    **No dupliques el gate en comentarios manuales.** Todo el lifecycle GSD se orquesta desde el CLI; tu rol es leer los artefactos, ejecutar el verify y continuar con la siguiente ronda de supervisión.
    ```

    **Notas críticas:**
    - El blockquote arriba es EL TEXTO literal que debe quedar en el archivo (con las 2 líneas en blanco iniciales para separar de la sección anterior).
    - NO eliminar ni reordenar secciones existentes. Solo APPEND.
    - NO añadir placeholders nuevos; reusar `{{provider_name}}` (ya resuelto por `resolvePromptTemplate`).
    - Todos los verdicts (`pass`, `fail`, `missing`, `malformed`) aparecen explícitamente (espeja el switch exhaustivo).
    - Referencia a `kodo gsd inspect` es una ayuda opcional — importante para que el orquestador la conozca.

    **Test asociado** — añadir al inicio de `test/orchestrator-gsd.test.js` (Task 2 creará el archivo; esta tarea lo inicializa con el bloque PM1..PM7):

    ```javascript
    // @ts-check
    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';
    import { readFileSync } from 'node:fs';

    describe('prompt.md — sección GSD renderizada', () => {
      const prompt = readFileSync('src/orchestrator/prompt.md', 'utf-8');

      it('PM1: contiene heading literal "## Sesiones GSD"', () => {
        assert.ok(prompt.includes('## Sesiones GSD'),
          'heading "## Sesiones GSD" debe estar presente');
      });

      it('PM2: contiene comando literal "kodo gsd verify <session-id>"', () => {
        assert.ok(prompt.includes('kodo gsd verify <session-id>'));
      });

      it('PM3: menciona los 4 verdicts como palabras literales', () => {
        for (const v of ['pass', 'fail', 'missing', 'malformed']) {
          assert.ok(new RegExp(`\\b${v}\\b`).test(prompt),
            `verdict "${v}" ausente en prompt.md`);
        }
      });

      it('PM4: referencia los 4 artefactos GSD', () => {
        for (const artifact of ['PROJECT.md', 'ROADMAP.md', 'PLAN.md', 'VERIFICATION.md']) {
          assert.ok(prompt.includes(artifact), `artifact "${artifact}" ausente`);
        }
      });

      it('PM5: conserva el placeholder {{provider_name}}', () => {
        assert.ok(prompt.includes('{{provider_name}}'));
      });

      it('PM6: menciona kodo gsd inspect para debugging', () => {
        assert.ok(prompt.includes('kodo gsd inspect <task-id>'));
      });

      it('PM7: sin frases inglesas típicas de prompts', () => {
        for (const phrase of [/\byou must\b/i, /\bplease\b/i, /\bexecute your\b/i]) {
          assert.ok(!phrase.test(prompt), `frase inglesa prohibida encontrada: ${phrase}`);
        }
      });
    });
    ```
  </action>
  <verify>
    <automated>node --test test/orchestrator-gsd.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "## Sesiones GSD" src/orchestrator/prompt.md` → match
    - `grep -qF "kodo gsd verify <session-id>" src/orchestrator/prompt.md` → match
    - `grep -qF "kodo gsd inspect <task-id>" src/orchestrator/prompt.md` → match
    - `grep -qF "PROJECT.md" src/orchestrator/prompt.md && grep -qF "ROADMAP.md" src/orchestrator/prompt.md && grep -qF "PLAN.md" src/orchestrator/prompt.md && grep -qF "VERIFICATION.md" src/orchestrator/prompt.md` → los 4 artefactos
    - `grep -qE "\bpass\b" src/orchestrator/prompt.md && grep -qE "\bfail\b" src/orchestrator/prompt.md && grep -qE "\bmissing\b" src/orchestrator/prompt.md && grep -qE "\bmalformed\b" src/orchestrator/prompt.md` → los 4 verdicts presentes
    - `grep -qF "{{provider_name}}" src/orchestrator/prompt.md` → match (placeholder reusado)
    - `wc -l < src/orchestrator/prompt.md` ≥ 85 (era 72, añade ~15 líneas)
    - `node --test test/orchestrator-gsd.test.js` exits 0 con los 7 tests PM1..PM7 ok
  </acceptance_criteria>
  <done>
    prompt.md contiene la nueva sección `## Sesiones GSD` con 4 bullets numerados, 4 sub-bullets para los verdicts, y la nota final anti-duplicación. Todo en español. No introduce placeholders nuevos. El archivo pasa de 72 a ~87 líneas. El test Nyquist-compliant PM1..PM7 valida el contenido renderizado (no solo que el archivo existe).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modificar buildContextSummary en launch.js + tests</name>
<files>src/orchestrator/launch.js, test/orchestrator-gsd.test.js</files>
  <read_first>
    - src/orchestrator/launch.js (líneas 103-126 — buildContextSummary completa)
    - src/session/state.js (líneas 11-28 — Session typedef con gsd?: boolean, phase_id?: string)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §6 (pattern launch.js modify) + Pitfall #4
  </read_first>
  <behavior>
    Tests en `test/orchestrator-gsd.test.js` DEBEN verificar:

    **launch.js buildContextSummary**
    - L1: sesión con `gsd: true, phase_id: '10'` → línea contiene `**KL-42** [GSD phase 10]`.
    - L2: sesión con `gsd: true` pero `phase_id: undefined` (bootstrap) → línea contiene `[GSD bootstrap]` (fallback Pitfall #4).
    - L3: sesión con `gsd: false` → línea NO contiene `[GSD...]` (comportamiento original preservado).
    - L4: sesión con `gsd: undefined` → línea NO contiene `[GSD...]`.
    - L5: múltiples sesiones mezcladas (GSD y no-GSD) → cada una se tagea correctamente.
    - L6: el resto de la línea (workspace, elapsed, project_path) no se rompe.

    **Import de buildContextSummary:** el archivo launch.js actualmente NO exporta `buildContextSummary` (es privada). Decisión: exportarla (cambio mínimo, compatible con el patrón existente). Opción A del PATTERNS §6.
  </behavior>
  <action>
    Modificar `src/orchestrator/launch.js`:

    **Cambio 1 — exportar la función.** Localizar la declaración actual (línea 108):
    ```javascript
    function buildContextSummary(sessions, config) {
    ```
    Reemplazar por:
    ```javascript
    export function buildContextSummary(sessions, config) {
    ```

    **Cambio 2 — añadir tag GSD en el loop.** Localizar el loop (líneas 118-121 aproximadamente):
    ```javascript
    for (const s of running) {
      const elapsed = Math.floor((Date.now() - new Date(s.started_at).getTime()) / 60_000);
      lines.push(`- **${s.task_ref}**: ${s.summary}`);
      lines.push(`  Workspace: ${s.workspace_ref} | ${elapsed}min | ${s.project_path}`);
    }
    ```
    Reemplazar por:
    ```javascript
    for (const s of running) {
      const elapsed = Math.floor((Date.now() - new Date(s.started_at).getTime()) / 60_000);
      // Phase 10 D-19: taggear sesiones GSD para que el orquestador las identifique.
      // Pitfall #4: phase_id puede estar ausente en modo bootstrap (Phase 9 D-11).
      const gsdTag = s.gsd ? ` \`[GSD ${s.phase_id ? `phase ${s.phase_id}` : 'bootstrap'}]\`` : '';
      lines.push(`- **${s.task_ref}**${gsdTag}: ${s.summary}`);
      lines.push(`  Workspace: ${s.workspace_ref} | ${elapsed}min | ${s.project_path}`);
    }
    ```

    **NO modificar** el resto de la función ni el archivo.

    **Añadir tests a `test/orchestrator-gsd.test.js`** (APPEND al archivo ya iniciado en Task 1):

    ```javascript
    import { buildContextSummary } from '../src/orchestrator/launch.js';

    describe('buildContextSummary — Phase 10 GSD tagging', () => {
      const baseSession = {
        workspace_ref: 'workspace:1',
        session_id: 's1',
        task_id: 'tid',
        task_ref: 'KL-42',
        provider: 'plane',
        project_id: 'p1',
        summary: 'Do work',
        status: 'running',
        started_at: new Date().toISOString(),
        project_path: '/tmp/proj',
      };
      const config = { claude: { max_parallel: 3 } };

      it('L1: sesión GSD con phase_id=10 → tag [GSD phase 10]', () => {
        const sessions = [{ ...baseSession, gsd: true, phase_id: '10' }];
        const out = buildContextSummary(sessions, config);
        assert.match(out, /KL-42\*\*\s*`\[GSD phase 10\]`/);
      });

      it('L2: sesión GSD sin phase_id (bootstrap) → tag [GSD bootstrap]', () => {
        const sessions = [{ ...baseSession, gsd: true, phase_id: undefined }];
        const out = buildContextSummary(sessions, config);
        assert.match(out, /KL-42\*\*\s*`\[GSD bootstrap\]`/);
      });

      it('L3: sesión no-GSD → NO contiene tag [GSD...]', () => {
        const sessions = [{ ...baseSession, gsd: false }];
        const out = buildContextSummary(sessions, config);
        assert.ok(!out.includes('[GSD'));
        assert.match(out, /- \*\*KL-42\*\*: Do work/);
      });

      it('L4: sesión con gsd undefined → NO contiene tag', () => {
        const sessions = [{ ...baseSession }]; // sin gsd
        const out = buildContextSummary(sessions, config);
        assert.ok(!out.includes('[GSD'));
      });

      it('L5: mix de sesiones → cada una tageada correctamente', () => {
        const sessions = [
          { ...baseSession, task_ref: 'KL-1', gsd: true, phase_id: '5' },
          { ...baseSession, task_ref: 'KL-2', gsd: false },
          { ...baseSession, task_ref: 'KL-3', gsd: true, phase_id: undefined },
        ];
        const out = buildContextSummary(sessions, config);
        assert.match(out, /KL-1\*\*\s*`\[GSD phase 5\]`/);
        assert.match(out, /KL-2\*\*:/); // sin tag
        assert.ok(!out.match(/KL-2\*\*.*\[GSD/));
        assert.match(out, /KL-3\*\*\s*`\[GSD bootstrap\]`/);
      });

      it('L6: workspace + elapsed + project_path preservados', () => {
        const sessions = [{ ...baseSession, gsd: true, phase_id: '10' }];
        const out = buildContextSummary(sessions, config);
        assert.match(out, /Workspace: workspace:1/);
        assert.match(out, /\/tmp\/proj/);
      });
    });
    ```
  </action>
  <verify>
    <automated>node --test test/orchestrator-gsd.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "export function buildContextSummary" src/orchestrator/launch.js` → match
    - `grep -qE "s\.gsd" src/orchestrator/launch.js` → match (conditional added)
    - `grep -qF "[GSD phase" src/orchestrator/launch.js && grep -qF "[GSD bootstrap" src/orchestrator/launch.js || grep -qF "'bootstrap'" src/orchestrator/launch.js` → match (ambos formatos presentes)
    - `grep -qF "Pitfall #4" src/orchestrator/launch.js || grep -qF "bootstrap" src/orchestrator/launch.js` → match (documentación del fallback)
    - `grep -c "it(" test/orchestrator-gsd.test.js` ≥ 13 (7 PM + 6 L)
    - `node --test test/orchestrator-gsd.test.js` exits 0
  </acceptance_criteria>
  <done>
    `buildContextSummary` exportada y modificada para taggear sesiones GSD con phase_id o "bootstrap". Tests L1..L6 pasan. Sesiones no-GSD conservan el formato original.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Modificar stop.js nudge condicional + tests</name>
<files>src/hooks/stop.js, test/orchestrator-gsd.test.js</files>
  <read_first>
    - src/hooks/stop.js (líneas 100-131 — bloque de notify orchestrator + session.gsd check)
    - .planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md §D-04 (disparo del verify via nudge)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §7 (pattern stop.js modify) + Pitfall #4
  </read_first>
  <behavior>
    Tests DEBEN verificar:

    **stop.js nudge (buildStopNudgeText — helper puro)**
    - S1: sesión con `gsd: true, phase_id: '10'` → el texto contiene `kodo gsd verify sess-abc-123` Y contiene `fase 10`.
    - S2: sesión con `gsd: true, phase_id: undefined` → texto contiene `kodo gsd verify sess-abc-123` Y contiene `bootstrap` (fallback Pitfall #4).
    - S3: sesión con `gsd: false` → texto es el original ("Revisa el resultado y decide si pasa a Done..."), SIN mención a `kodo gsd verify`.
    - S4: sesión sin `gsd` → mismo que S3.
    - S5: el mensaje siempre empieza con "La sesión ${task_ref}" (prefijo común preservado).
    - S6: el `\n` al final sigue presente (literal `\\n` escapado — ya está así en el código original).
    - S7: idioma español (sin palabras inglesas típicas de prompts).

    **Extracción de la lógica:** refactorizar el texto inline a un helper puro `buildStopNudgeText(session)` exportado, e invocarlo desde el bloque existente. Testear el helper aisladamente (Opción A del PATTERNS §7).
  </behavior>
  <action>
    Modificar `src/hooks/stop.js`.

    **Cambio 1 — añadir helper puro al tope del archivo (después de imports):**

    Localizar la sección de imports (primeras líneas del archivo). Añadir al final de los imports (o justo antes de la primera función exportada) el siguiente helper:

    ```javascript
    /**
     * Build the orchestrator nudge text for a session that just ended.
     * Pure function — no I/O. Exported for testing.
     *
     * Phase 10 D-04: when session.gsd === true, the nudge points the orchestrator
     * at `kodo gsd verify <session-id>`. Non-GSD sessions get the original text.
     *
     * Pitfall #4: session.phase_id may be undefined (bootstrap mode, Phase 9 D-11).
     * Fallback string: "bootstrap".
     *
     * Idioma: español (consistente con el texto original del hook + D-16 Phase 10).
     *
     * @param {import('../session/state.js').Session} session
     * @returns {string}
     */
    export function buildStopNudgeText(session) {
      const base = `La sesión ${session.task_ref} (${session.summary}) ha terminado y está en Review.`;
      if (session.gsd) {
        const phaseLabel = session.phase_id ? `fase ${session.phase_id}` : 'bootstrap';
        return `${base} Es una sesión GSD (${phaseLabel}). Ejecuta \`kodo gsd verify ${session.session_id}\` y actúa según el verdict.\\n`;
      }
      return `${base} Revisa el resultado y decide si pasa a Done o necesita más trabajo.\\n`;
    }
    ```

    **Cambio 2 — reemplazar el texto inline en el bloque cmux.send.**

    Localizar el bloque existente (líneas ~115-125):

    ```javascript
    try {
      const workspaces = await cmux.listWorkspaces();
      const orchMatch = workspaces.match(/(workspace:\d+)\s+kodo-orchestrator/);
      if (orchMatch) {
        await cmux.send({
          workspace: orchMatch[1],
          text: `La sesión ${session.task_ref} (${session.summary}) ha terminado y está en Review. Revisa el resultado y decide si pasa a Done o necesita más trabajo.\\n`,
        });
      }
    } catch {}
    ```

    Reemplazar por:

    ```javascript
    try {
      const workspaces = await cmux.listWorkspaces();
      const orchMatch = workspaces.match(/(workspace:\d+)\s+kodo-orchestrator/);
      if (orchMatch) {
        await cmux.send({
          workspace: orchMatch[1],
          text: buildStopNudgeText(session),
        });
      }
    } catch {}
    ```

    **NO tocar** el resto de la función ni el lock-release logic existente.

    **Añadir tests a `test/orchestrator-gsd.test.js`** (APPEND al archivo):

    ```javascript
    import { buildStopNudgeText } from '../src/hooks/stop.js';

    describe('buildStopNudgeText — Phase 10 nudge condicional GSD', () => {
      const baseSession = {
        workspace_ref: 'workspace:1',
        session_id: 'sess-abc-123',
        task_id: 'tid',
        task_ref: 'KL-42',
        provider: 'plane',
        project_id: 'p1',
        summary: 'Do work',
        status: 'review',
        started_at: new Date().toISOString(),
        project_path: '/tmp/proj',
      };

      it('S1: GSD session con phase_id → incluye `kodo gsd verify <session-id>` y fase', () => {
        const text = buildStopNudgeText({ ...baseSession, gsd: true, phase_id: '10' });
        assert.match(text, /kodo gsd verify sess-abc-123/);
        assert.match(text, /fase 10/);
        assert.match(text, /La sesión KL-42/);
      });

      it('S2: GSD session sin phase_id (bootstrap) → fallback bootstrap', () => {
        const text = buildStopNudgeText({ ...baseSession, gsd: true, phase_id: undefined });
        assert.match(text, /kodo gsd verify sess-abc-123/);
        assert.match(text, /bootstrap/);
      });

      it('S3: non-GSD session → texto original sin kodo gsd verify', () => {
        const text = buildStopNudgeText({ ...baseSession, gsd: false });
        assert.ok(!text.includes('kodo gsd verify'));
        assert.match(text, /Revisa el resultado y decide si pasa a Done/);
      });

      it('S4: session sin gsd (undefined) → texto original', () => {
        const text = buildStopNudgeText({ ...baseSession });
        assert.ok(!text.includes('kodo gsd verify'));
      });

      it('S5: todos los casos arrancan con "La sesión KL-42"', () => {
        const gsd = buildStopNudgeText({ ...baseSession, gsd: true, phase_id: '10' });
        const nonGsd = buildStopNudgeText({ ...baseSession, gsd: false });
        assert.ok(gsd.startsWith('La sesión KL-42'));
        assert.ok(nonGsd.startsWith('La sesión KL-42'));
      });

      it('S6: preserva el \\n literal al final', () => {
        const text = buildStopNudgeText({ ...baseSession, gsd: true, phase_id: '10' });
        assert.ok(text.endsWith('\\n'));
      });

      it('S7: idioma español (no inglés en prosa)', () => {
        const text = buildStopNudgeText({ ...baseSession, gsd: true, phase_id: '10' });
        // No debe contener palabras-clave inglesas típicas del buildGsdContext.
        assert.ok(!/\bplease\b|\byou must\b|\bexecute\b/i.test(text));
        assert.match(text, /Ejecuta/); // español
      });
    });
    ```
  </action>
  <verify>
    <automated>node --test test/orchestrator-gsd.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "export function buildStopNudgeText" src/hooks/stop.js` → match
    - `grep -qF 'kodo gsd verify ${session.session_id}' src/hooks/stop.js` → match (fixed-string, single quotes para evitar expansión de `$`)
    - `grep -qE 'fase \$\{session\.phase_id\}|phaseLabel' src/hooks/stop.js` → match
    - `grep -qF "bootstrap" src/hooks/stop.js` → match (fallback Pitfall #4)
    - `grep -qF "Pitfall #4" src/hooks/stop.js || grep -qF "phase_id may be undefined" src/hooks/stop.js` → match (documentación)
    - `grep -qF "buildStopNudgeText(session)" src/hooks/stop.js` → match (invocación en cmux.send)
    - El texto original `"Revisa el resultado y decide si pasa a Done"` SIGUE presente en el archivo (ahora dentro del helper, rama else):
      `grep -qF "Revisa el resultado y decide si pasa a Done" src/hooks/stop.js` → match
    - `grep -c "it(" test/orchestrator-gsd.test.js` ≥ 20 (7 PM + 6 L + 7 S)
    - `node --test test/orchestrator-gsd.test.js` exits 0
    - `grep -qF "import { buildStopNudgeText }" test/orchestrator-gsd.test.js` → match
  </acceptance_criteria>
  <done>
    `src/hooks/stop.js` exporta `buildStopNudgeText` (helper puro). El bloque de cmux.send consume el helper. Los 7 tests (S1..S7) pasan. Sesiones no-GSD preservan el mensaje original sin regresión. El hook NO toca el lock-release logic ni el resto del archivo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session (state.json) → prompt/nudge/summary | Ya validado upstream (Phase 8 D-10, Phase 9 D-11). |
| phaseLabel string → nudge text | Interpolación directa; `session.phase_id` es string controlado por kodo (Phase 9 resolver). |
| task_ref → prompt/nudge | Ya sanitizado (Phase 2/3 provider). |
| prompt.md → orchestrator Claude | El orquestador interpreta texto; no hay escalación via prompt-injection porque el texto es estático (no interpola input del usuario). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Injection | cmux.send text via session.summary | accept | `session.summary` viene del task provider (Plane). Un título malicioso podría inyectar comandos en el mensaje que Claude interpretaría. Accept: el vector requiere control del backlog Plane (atacante interno); mitigar a nivel provider queda fuera de Phase 10. Documentar en known-risks del milestone. |
| T-10-04-02 | Tampering | session.phase_id en label | mitigate | Si phase_id contiene backticks o markdown, podría romper el formato `[GSD phase X]`. phase_id viene del resolver (Phase 9) que lo extrae por regex `/\d+(?:\.\d+)?/` — sin caracteres especiales. Accept como low-risk; el regex del resolver es la defensa. |
| T-10-04-03 | Injection | prompt.md static text | accept | Texto hardcoded, no interpola input runtime. Zero surface. |
| T-10-04-04 | Spoofing | cmux workspace match regex | accept | `orchMatch = workspaces.match(/(workspace:\d+)\s+kodo-orchestrator/)` — si un atacante crea un workspace con ese nombre podría recibir el nudge. Pero atacante requiere acceso local = ya tiene todo. Accept. |
</threat_model>

<verification>
- `node --test test/orchestrator-gsd.test.js` exits 0 con 20+ tests ok (7 PM + 6 L + 7 S).
- `grep -qF "## Sesiones GSD" src/orchestrator/prompt.md` → match.
- `grep -qF "export function buildStopNudgeText" src/hooks/stop.js` → match.
- `grep -qF "export function buildContextSummary" src/orchestrator/launch.js` → match.
- `grep "kodo gsd verify" src/hooks/stop.js src/orchestrator/prompt.md` → matches en ambos.
- Sin regresión: `buildGsdContext` en session-start.js sigue en inglés (no tocar ese archivo).
</verification>

<success_criteria>
- [ ] `src/orchestrator/prompt.md` tiene `## Sesiones GSD` con 4 bullets, los 4 verdicts explícitos, en español.
- [ ] `src/orchestrator/launch.js`: `buildContextSummary` exportada y tagea `[GSD phase N]` o `[GSD bootstrap]`.
- [ ] `src/hooks/stop.js`: `buildStopNudgeText` helper exportado y usado. Menciona `kodo gsd verify ${session_id}` condicionalmente.
- [ ] `test/orchestrator-gsd.test.js` con 20+ tests (PM1..PM7 + L1..L6 + S1..S7) pasando.
- [ ] Pitfall #4 (phase_id undefined) resuelto con fallback "bootstrap" en ambos puntos (launch.js + stop.js).
- [ ] Sin regresión en idioma: prompt.md en español, buildGsdContext (session-start.js) NO modificado.
- [ ] Task 1 cubre Nyquist Dim 8a: prompt.md tiene test automatizado (no solo grep), validando contenido renderizado.
</success_criteria>

<output>
After completion, create `.planning/phases/10-orchestrator-verification-gate/10-04-SUMMARY.md`
</output>
</content>
</invoke>