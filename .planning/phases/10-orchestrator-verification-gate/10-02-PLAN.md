---
phase: 10-orchestrator-verification-gate
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/gsd/verify.js
  - test/gsd-verify-cli.test.js
  - test/gsd-verify-integration.test.js
autonomous: true
requirements: [GSD-05, GSD-06]

must_haves:
  truths:
    - "runGsdVerify({ sessionId }) resuelve la SessionRecord via findSession y obtiene task_ref + project_path + phase_id"
    - "runGsdVerify descubre el directorio de fase vía readdirSync + prefix-match por padded phase_id"
    - "runGsdVerify retorna { action: 'missing' } si VERIFICATION.md no existe"
    - "runGsdVerify llama provider.addComment SIEMPRE que provider.getTask devuelva un task válido; si getTask falla, se logea plane.api.call.failed{step:getTask}, addComment NO se invoca, y orchestratorReview emite igualmente con legacy='blocked' (fail-open D-17)"
    - "runGsdVerify llama provider.updateTaskState SÓLO en verdict pass, con config.providers[provider].states.review"
    - "runGsdVerify emite orchestratorReview UNA SOLA VEZ por ejecución, mapeando action pass→approved y resto→blocked"
    - "runGsdVerify NO emite plane.api.call (el provider lo emite internamente)"
    - "Los comentarios Plane están en español y son deterministas (mismo verdict → mismo markdown byte-a-byte excepto timestamp)"
    - "Fallo de addComment o updateTaskState NO impide emitir orchestrator.review (fail-open Plane, D-17)"
    - "finalize() obtiene el provider UNA SOLA VEZ vía un hoisted `const provider = await getProviderFn()`; reusa la misma instancia para getTask, addComment y updateTaskState"
  artifacts:
    - path: "src/gsd/verify.js"
      provides: "Orquestación del verification gate: session → verdict → Plane comment + transition + log"
      exports: ["runGsdVerify"]
      contains: "provider.addComment|provider.updateTaskState|orchestratorReview"
    - path: "test/gsd-verify-cli.test.js"
      provides: "Tests CLI-level con TaskProvider mock + DI"
      contains: "node:test"
    - path: "test/gsd-verify-integration.test.js"
      provides: "Tests E2E con tmp .planning/ sintético"
      contains: "mkdtempSync"
  key_links:
    - from: "src/gsd/verify.js"
      to: "src/gsd/verification.js"
      via: "import { parseVerificationFrontmatter, computeVerdict }"
      pattern: "from './verification.js'"
    - from: "src/gsd/verify.js"
      to: "TaskProvider.addComment + updateTaskState"
      via: "getProviderFn() → provider.getTask(task_ref) → addComment/updateTaskState"
      pattern: "addComment\\(|updateTaskState\\("
    - from: "src/gsd/verify.js"
      to: "config.providers[provider].states.review"
      via: "session-start.js:23-27 pattern"
      pattern: "providers\\[.+?\\]\\.states"
    - from: "src/gsd/verify.js"
      to: "orchestratorReview logger event"
      via: "import { orchestratorReview } from '../logger-events.js'"
      pattern: "orchestratorReview\\(log,"
---

<objective>
Implementar `src/gsd/verify.js`: módulo de orquestación que (a) resuelve la sesión GSD desde session-id, (b) localiza y lee `VERIFICATION.md` del proyecto destino, (c) computa el verdict vía `src/gsd/verification.js` (Plan 10-01), (d) postea el comentario Plane determinista en español (D-14, D-15, D-16), (e) transiciona la tarea al estado Review sólo en pass (D-11, D-12), y (f) emite el evento `orchestrator.review` en NDJSON mapeando el discriminated union al contrato legacy `approved|blocked` (D-17, Pitfall #2).

Purpose: Centralizar TODA la I/O y side-effects del gate en un único módulo testeable con DI. El CLI de Plan 10-03 es thin y delega aquí. El parser de Plan 10-01 queda puro. Cubre GSD-05 (inspección + bloqueo) y GSD-06 (comentario con phase_id + resultado).

Output:
- `src/gsd/verify.js` con export `runGsdVerify(opts, deps)`.
- Plantillas de comentario Plane embebidas (pass, fail×3, missing, malformed) en español.
- `test/gsd-verify-cli.test.js` con TaskProvider mock + todos los caminos del switch.
- `test/gsd-verify-integration.test.js` con tmp `.planning/` sintético + SessionRecord mock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md
@.planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md
@.planning/phases/10-orchestrator-verification-gate/10-01-PLAN.md

<interfaces>
<!-- Contratos desde Plan 10-01 (ya existentes al ejecutar este plan): -->

```javascript
// src/gsd/verification.js (Plan 10-01)
export function parseVerificationFrontmatter(md: string): ParsedFrontmatter | { error: string };
export function computeVerdict(parsed, phaseId: string): Verdict;
// Verdict = PassVerdict | FailVerdict | MissingVerdict | MalformedVerdict
// Ver 10-01-PLAN <interfaces> para shape exacto.
```

```javascript
// src/session/state.js (existente)
export function findSession({ sessionId, cwd? }): Session | undefined;
// Session = { session_id, task_id, task_ref, provider, project_id, project_path, gsd?, phase_id?, brief?, ... }
```

```javascript
// src/providers/registry.js (existente)
export async function initRegistry(): Promise<void>;
export function getProvider(taskId?): TaskProvider;
// TaskProvider = { getTask(ref), addComment(task, markdown), updateTaskState(task, stateName), ... }
```

```javascript
// src/logger-events.js (existente, firma Phase 7 D-09)
export function orchestratorReview(logger, { phase_id, verdict: 'approved' | 'blocked', reason: string }): void;
// IMPORTANTE: la firma espera 'approved'|'blocked', NO el discriminated union. Pitfall #2.
```

```javascript
// src/config.js (existente)
export function loadConfig(): { provider, providers: { [name]: { states: { trigger, review, done } } }, ... };
// PITFALL #1: NO existe config.states.review como top-level. Vive en config.providers[providerName].states.review.
```

```javascript
// src/logger.js (existente)
export function createLogger({ sessionId, minLevel }): Logger;
// logger.child({ component, task_id }) retorna sub-logger
```

<!-- Contrato nuevo exportado por ESTE plan: -->

```javascript
// src/gsd/verify.js (NEW)

/**
 * @typedef {{ sessionId: string }} RunGsdVerifyOpts
 * @typedef {{
 *   findSessionFn?: typeof import('../session/state.js').findSession,
 *   getProviderFn?: () => Promise<import('../interface.js').TaskProvider> | import('../interface.js').TaskProvider,
 *   loadConfigFn?: typeof import('../config.js').loadConfig,
 *   readFileFn?: (path: string) => string,
 *   existsFn?: (path: string) => boolean,
 *   readdirFn?: (path: string) => string[],
 *   loggerFactory?: (sessionId: string) => import('../logger.js').Logger,
 * }} RunGsdVerifyDeps
 */

export async function runGsdVerify(opts: RunGsdVerifyOpts, deps?: RunGsdVerifyDeps): Promise<{
  verdict: Verdict,
  plane: { commented: boolean, transitioned: boolean },
  session: { session_id, task_ref, phase_id },
}>;
```
</interfaces>

<patterns>
DI + fallback pattern: `src/cli/gsd-inspect.js:50-62`. Copiar estructura.
Config lookup pattern: `src/hooks/session-start.js:23-27` — `config.providers[session.provider].states.review`.
Fail-open Plane: try/catch individuales en addComment/updateTaskState, orchestratorReview SIEMPRE al final.
Discriminated union exhaustive switch: `src/cli/gsd-inspect.js:127-143`.
Logger DI: `const log = createLogger({ sessionId, minLevel }).child({ component: 'gsd' });`
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar runGsdVerify con DI + plantillas ES embebidas</name>
<files>src/gsd/verify.js</files>
  <read_first>
    - src/gsd/verification.js (Plan 10-01 output — exports que este módulo consume)
    - src/cli/gsd-inspect.js (analog primario de orquestación — imports, DI pattern, estructura; líneas 1-110)
    - src/hooks/session-start.js (líneas 20-50 — patrón EXACTO de `config.providers[providerName].states.review`; resuelve Pitfall #1)
    - src/triggers/dispatcher.js (analog DI secundario; líneas 40-170 — ver cómo pasa logger hijo + maneja fail-open Plane)
    - src/session/state.js (líneas 1-40 — typedef de Session; confirma que phase_id puede ser undefined)
    - src/logger-events.js (líneas 124-137 — firma exacta de orchestratorReview)
    - src/providers/plane/provider.js (líneas 162-185 — shape exacto de addComment + updateTaskState)
    - .planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md §Decisions D-03, D-06, D-08, D-10..D-18
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §2 (pattern assignments de verify.js) + §Metadata (Pitfalls #1..#5)
  </read_first>
  <behavior>
    Tests DEBEN verificar (sub-concerns discretos, el executor marca cada uno al cerrarlo):

    **Sub-concern A — DI + resolución de sesión**
    - T1: session-id no encontrado en state.json → lanza Error con mensaje `session not found: <id>` (el CLI de Plan 10-03 convierte a exit 1).
    - T2: session.gsd=false → lanza Error `session is not GSD: <id>` (invariante: solo verificamos sesiones GSD; CONTEXT §D-18).
    - T3: session sin phase_id (bootstrap) → verdict `{ action: 'malformed', detail: 'session has no phase_id' }`. No lee filesystem. **Resuelve Pitfall #4**. (Nota: addComment SÍ se invoca dentro de finalize, vía getTask; ver Sub-concern D.)

    **Sub-concern B — Descubrimiento de VERIFICATION.md (Pitfall #3)**
    - T4: directorio `.planning/phases/10-foo/` existe pero NO hay archivo `10-VERIFICATION.md` → verdict `{ action: 'missing' }`. addComment llamado con plantilla missing; updateTaskState NO llamado.
    - T5: no existe directorio `.planning/phases/10-*/` (ningún prefix match) → verdict `{ action: 'missing' }`.
    - T6: padded phase_id="03" matchea directorio `03-foundation` (no `30-other`) → usa prefix exacto `${padded}-`.

    **Sub-concern C — Verdict + side-effects Plane**
    - T7: verdict pass → addComment llamado 1x + updateTaskState llamado 1x con `config.providers[session.provider].states.review` (NOT `config.states.review` — **resuelve Pitfall #1**).
    - T8: verdict fail (cualquier reason) → addComment llamado 1x; updateTaskState NO llamado (D-12).
    - T9: verdict missing → addComment llamado 1x; updateTaskState NO llamado.
    - T10: verdict malformed → addComment llamado 1x; updateTaskState NO llamado.

    **Sub-concern D — Fail-open Plane (D-17)**
    - T18: getTask lanza → se loguea `plane.api.call.failed{step:getTask}`; addComment NO se invoca (no tenemos task); updateTaskState NO se invoca; orchestratorReview IGUAL se emite con legacyVerdict='blocked'. Resultado: `{ plane: { commented: false, transitioned: false } }`. Incluso en verdict pass, si getTask falla, transitioned=false y la review se emite como 'blocked' porque el gate no completó el side-effect.
    - T18b (edge case explícito): getTask lanza EN verdict pass → addComment.called=0, updateTaskState.called=0, commented=false, transitioned=false, orchestratorReview emitido exactamente 1x con `{ verdict: 'blocked', reason: 'plane-unreachable:getTask-failed' }` (el legacyVerdict es 'blocked' porque verdict.action===pass pero no pudimos completar side-effect; ver finalize() mapping de legacyVerdict cuando !task).
    - T19: addComment lanza (getTask OK) → se loguea `plane.api.call.failed{step:addComment}`; updateTaskState NO se invoca aunque sea verdict pass (invariante: no transicionar si no comentamos); orchestratorReview emitido; `{ plane: { commented: false, transitioned: false } }`.
    - T19b: addComment OK pero updateTaskState lanza (en verdict pass) → `{ plane: { commented: true, transitioned: false } }`; orchestratorReview emitido con legacyVerdict='approved' (sí se aprobó el verdict, el fallo es transient en Plane).

    **Sub-concern E — Idempotencia (Pitfall #7)**
    - T11: dos invocaciones consecutivas con la misma sesión y mismo VERIFICATION.md → addComment llamado 2x (NO deduplicamos). Comentario byte-a-byte idéntico (excepto timestamp).

    **Sub-concern F — orchestratorReview mapping (Pitfall #2)**
    - T12: verdict pass + side-effects OK → orchestratorReview llamado con `{ verdict: 'approved', reason: 'gate-passed', phase_id }` una vez.
    - T13: verdict fail reason=gaps-found → orchestratorReview con `{ verdict: 'blocked', reason: 'gaps-found:...' }`.
    - T14: verdict missing → orchestratorReview con `{ verdict: 'blocked', reason: 'missing' }`.
    - T15: verdict malformed → orchestratorReview con `{ verdict: 'blocked', reason: 'malformed:...' }`.
    - T16: orchestratorReview emitido EXACTAMENTE UNA VEZ por ejecución (no 0, no 2+).

    **Sub-concern G — NO plane.api.call duplicado (Pitfall #5)**
    - T17: el mock del logger NO recibe evento `plane.api.call` (solo `plane.api.call.failed` en ramas de error). El provider lo emite internamente via su propio logger hijo; verify.js no lo duplica.

    **Sub-concern H — Hoisted provider (reduce llamadas a getProviderFn)**
    - T17b: para un único `runGsdVerify(...)` con verdict pass, el spy sobre `getProviderFn` registra EXACTAMENTE 1 invocación (no 3). El factory se consulta una sola vez y la instancia se reusa para getTask + addComment + updateTaskState.

    **Sub-concern I — Integration (tmp .planning/)**
    - T20 (integration): crear `tmpdir/.planning/phases/10-foo/10-VERIFICATION.md` con frontmatter pass; inyectar findSessionFn mock que retorna SessionRecord; TaskProvider mock; assert verdict.action='pass', addComment llamado con markdown que contiene "✅ Phase 10".
    - T21 (integration): mismo setup pero frontmatter con gaps_count=2 → assert verdict.action='fail', reason='gaps-found', addComment con "❌ Phase 10 bloqueada" y "gaps_count=2".

    **Sub-concern J — Plantillas deterministas (D-15, D-16)**
    - T22: llamar renderizador directamente (si export helper) o vía runGsdVerify dos veces → los primeros N caracteres del markdown (hasta donde no hay timestamp) son idénticos.
  </behavior>
  <action>
    Crear `src/gsd/verify.js`. Seguir el esqueleto canónico abajo — sin variantes "complejas vs simples". Cada sub-concern del <behavior> corresponde a una sección del código.

    **Sub-concern 1 — Header + imports:**

    ```javascript
    // @ts-check
    //
    // src/gsd/verify.js — Orquestación del verification gate GSD (Phase 10, CONTEXT §D-01..§D-17).
    //
    // Responsabilidades:
    //   1. findSession({ sessionId }) → SessionRecord (task_ref, project_path, phase_id, provider).
    //   2. Descubrir .planning/phases/<padded>-<slug>/<padded>-VERIFICATION.md via readdirSync + prefix.
    //   3. Leer + parsear frontmatter (Plan 10-01 verification.js).
    //   4. computeVerdict → Verdict discriminated union.
    //   5. getProvider.getTask(task_ref) → addComment(markdown) → (si pass) updateTaskState(reviewState).
    //   6. orchestratorReview(logger, { phase_id, verdict: mapActionToLegacy(action), reason }).
    //
    // Invariantes:
    //   - addComment se invoca SIEMPRE que getTask devuelva un task válido (D-14). Si getTask falla, NO se comenta.
    //   - updateTaskState SÓLO en verdict pass Y solo si addComment previo tuvo éxito (D-11, D-12).
    //   - orchestratorReview emitido UNA SOLA VEZ, al final, en todas las ramas (D-17).
    //   - NO duplicar plane.api.call — el provider lo emite (Pitfall #5).
    //   - config.providers[provider].states.review (NO config.states.review — Pitfall #1).
    //   - Fail-open Plane: getTask/addComment/updateTaskState en try/catch individuales (D-17).
    //   - Provider obtenido UNA sola vez por ejecución (hoisted const provider).
    //   - Idempotencia NO implementada en v0.3: duplicados aceptados (Pitfall #7).

    import { existsSync, readFileSync, readdirSync } from 'node:fs';
    import { join } from 'node:path';
    import { findSession } from '../session/state.js';
    import { loadConfig } from '../config.js';
    import { initRegistry, getProvider } from '../providers/registry.js';
    import { parseVerificationFrontmatter, computeVerdict } from './verification.js';
    import { orchestratorReview } from '../logger-events.js';
    import { createLogger } from '../logger.js';
    ```

    **Sub-concern 2 — Función principal `runGsdVerify`:**

    ```javascript
    export async function runGsdVerify(opts, deps = {}) {
      const findSessionFn = deps.findSessionFn || findSession;
      const loadConfigFn = deps.loadConfigFn || loadConfig;
      const readFileFn = deps.readFileFn || ((p) => readFileSync(p, 'utf-8'));
      const existsFn = deps.existsFn || existsSync;
      const readdirFn = deps.readdirFn || readdirSync;
      const loggerFactory = deps.loggerFactory || ((sid) =>
        createLogger({ sessionId: sid, minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info') })
          .child({ component: 'gsd' }));

      let getProviderFn = deps.getProviderFn;
      if (!getProviderFn) {
        await initRegistry();
        getProviderFn = () => getProvider(/** @type {any} */ (undefined));
      }

      // 1. Resolver session.
      const session = findSessionFn({ sessionId: opts.sessionId });
      if (!session) throw new Error(`session not found: ${opts.sessionId}`);
      if (!session.gsd) throw new Error(`session is not GSD: ${opts.sessionId}`);

      const log = loggerFactory(session.session_id).child({ task_id: session.task_id });

      // 2. Pitfall #4: phase_id ausente → malformed sin tocar filesystem.
      if (!session.phase_id) {
        const verdict = { action: 'malformed', phase_id: '', detail: 'session has no phase_id (bootstrap?)' };
        return finalize({ verdict, session, log, getProviderFn, loadConfigFn });
      }

      // 3. Descubrir directorio de fase (Pitfall #3).
      // Canónico: entries.find((e) => e.startsWith(`${padded}-`)) — coincidencia por prefijo exacto.
      // (Variantes con regex son innecesarias: readdirSync ya entrega nombres de dir sin separador.)
      const padded = /^\d+$/.test(session.phase_id)
        ? session.phase_id.padStart(2, '0')
        : session.phase_id;  // "02.1" se queda como está
      const phasesRoot = join(session.project_path, '.planning', 'phases');
      let verdict;
      if (!existsFn(phasesRoot)) {
        verdict = { action: 'missing', phase_id: session.phase_id };
      } else {
        const entries = readdirFn(phasesRoot);
        const match = entries.find((e) => e.startsWith(`${padded}-`));
        if (!match) {
          verdict = { action: 'missing', phase_id: session.phase_id };
        } else {
          const verPath = join(phasesRoot, match, `${padded}-VERIFICATION.md`);
          if (!existsFn(verPath)) {
            verdict = { action: 'missing', phase_id: session.phase_id };
          } else {
            const md = readFileFn(verPath);
            const parsed = parseVerificationFrontmatter(md);
            verdict = computeVerdict(parsed, session.phase_id);
          }
        }
      }

      // 4. Finalize: postear + transicionar + loggear.
      return finalize({ verdict, session, log, getProviderFn, loadConfigFn });
    }
    ```

    **Sub-concern 3 — Helper `finalize` con provider hoisted (una sola invocación a getProviderFn):**

    ```javascript
    async function finalize({ verdict, session, log, getProviderFn, loadConfigFn }) {
      const phaseName = session.summary || `Phase ${verdict.phase_id}`;
      const markdown = renderComment(verdict, phaseName, session);

      let commented = false;
      let transitioned = false;
      let task;

      // Hoist provider: una sola llamada a getProviderFn() por ejecución.
      const provider = await getProviderFn();

      try {
        task = await provider.getTask(session.task_ref);
      } catch (err) {
        log.error('plane.api.call.failed', { step: 'getTask', error: err.message });
      }

      if (task) {
        try {
          await provider.addComment(task, markdown);
          commented = true;
        } catch (err) {
          log.error('plane.api.call.failed', { step: 'addComment', error: err.message });
        }

        // updateTaskState: sólo si verdict pass Y addComment tuvo éxito.
        if (verdict.action === 'pass' && commented) {
          // Pitfall #1: config.providers[provider].states.review — NO top-level.
          const config = loadConfigFn();
          const providerName = session.provider || config.provider;
          const providerCfg = (config.providers && config.providers[providerName]) || {};
          const reviewState = (providerCfg.states && providerCfg.states.review) || 'In review';
          try {
            await provider.updateTaskState(task, reviewState);
            transitioned = true;
          } catch (err) {
            log.error('plane.api.call.failed', { step: 'updateTaskState', error: err.message });
          }
        }
      }

      // 5. Pitfall #2: mapear action → legacy verdict.
      // Si verdict.action=pass pero task no se obtuvo (side-effect incompleto), degradar a 'blocked'
      // con reason 'plane-unreachable' — el orquestador puede reintentar.
      let legacyVerdict;
      let reasonForLog;
      if (verdict.action === 'pass') {
        if (!task) {
          legacyVerdict = 'blocked';
          reasonForLog = 'plane-unreachable:getTask-failed';
        } else {
          legacyVerdict = 'approved';
          reasonForLog = 'gate-passed';
        }
      } else {
        legacyVerdict = 'blocked';
        reasonForLog =
          verdict.action === 'fail' ? `${verdict.reason}:${verdict.detail}` :
          verdict.action === 'missing' ? 'missing' :
          /* malformed */ `malformed:${verdict.detail}`;
      }

      orchestratorReview(log, {
        phase_id: verdict.phase_id,
        verdict: legacyVerdict,
        reason: reasonForLog,
      });

      return {
        verdict,
        plane: { commented, transitioned },
        session: { session_id: session.session_id, task_ref: session.task_ref, phase_id: verdict.phase_id },
      };
    }
    ```

    **Sub-concern 4 — Plantillas de comentario (deterministas, en español — D-15, D-16):**

    ```javascript
    function renderComment(verdict, phaseName, session) {
      switch (verdict.action) {
        case 'pass':      return renderPassComment(verdict, phaseName, session);
        case 'fail':      return renderFailComment(verdict, phaseName, session);
        case 'missing':   return renderMissingComment(verdict, phaseName);
        case 'malformed': return renderMalformedComment(verdict, phaseName);
      }
    }

    function renderPassComment(v, phaseName, session) {
      const verPath = `.planning/phases/${String(v.phase_id).padStart(2,'0')}-<slug>/${String(v.phase_id).padStart(2,'0')}-VERIFICATION.md`;
      return [
        `[kodo:gsd] ✅ Phase ${v.phase_id} verificada — ${phaseName}`,
        ``,
        `- Must-haves: ${v.must_haves}/${v.must_haves} verificados`,
        `- Gaps: 0`,
        `- Transicionada a Review`,
        ``,
        `Ver: \`${verPath}\``,
      ].join('\n');
    }

    function renderFailComment(v, phaseName, session) {
      const header = `[kodo:gsd] ❌ Phase ${v.phase_id} bloqueada — ${phaseName}`;
      let body;
      switch (v.reason) {
        case 'gaps-found':
          body = `Motivo: ${v.detail}. Corrige VERIFICATION.md y re-dispara el flujo.`;
          break;
        case 'must-haves-incomplete':
          body = `Motivo: must_haves incompletos (${v.detail}). Completa los must-haves restantes y re-dispara.`;
          break;
        case 'status-failed':
          body = `Motivo: ${v.detail}. Revisa el artefacto, ajusta el status cuando esté passed, y re-dispara.`;
          break;
        default:
          body = `Motivo: ${v.reason} (${v.detail}).`;
      }
      return [header, '', body].join('\n');
    }

    function renderMissingComment(v, phaseName) {
      return [
        `[kodo:gsd] ⚠️ VERIFICATION.md no encontrado para Phase ${v.phase_id}`,
        ``,
        `Ejecuta \`/gsd-verify-work\` en la sesión y re-dispara el flujo.`,
      ].join('\n');
    }

    function renderMalformedComment(v, phaseName) {
      return [
        `[kodo:gsd] ⚠️ VERIFICATION.md presente pero inválido (Phase ${v.phase_id})`,
        ``,
        `Detalle: ${v.detail}`,
        `Corrige el frontmatter y re-dispara el flujo.`,
      ].join('\n');
    }
    ```

    Export `runGsdVerify` como named export. Helpers (`renderPassComment`, etc.) pueden ser exportados también para testear independientemente (recomendado).

    **NO añadir:**
    - Deduplicación de comentarios (Pitfall #7 — deferida).
    - plane.api.call emit (Pitfall #5 — el provider ya lo emite).
    - --dry-run (Claude's Discretion; no añadir en v0.3 para mantener superficie mínima — decisión: omitir).
  </action>
  <verify>
    <automated>node --test test/gsd-verify-cli.test.js test/gsd-verify-integration.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "export async function runGsdVerify" src/gsd/verify.js` → match
    - `grep -qF "config.providers[" src/gsd/verify.js` → match (Pitfall #1 resuelto)
    - `grep -qF "config.states.review" src/gsd/verify.js` → NO match (NO usa el path incorrecto)
    - `grep -qF "readdirSync" src/gsd/verify.js || grep -qF "readdirFn" src/gsd/verify.js` → match (Pitfall #3 resuelto)
    - `grep -qF "plane.api.call.failed" src/gsd/verify.js` → match; y `grep -c "plane.api.call" src/gsd/verify.js` equals `grep -c "plane.api.call.failed" src/gsd/verify.js` (Pitfall #5: solo el evento `.failed`, nunca el success; ambos counts iguales demuestra que no hay `plane.api.call` sin sufijo `.failed`)
    - `grep -c "plane.api.call.failed" src/gsd/verify.js` ≥ 3 (getTask + addComment + updateTaskState error paths)
    - `grep -qF "orchestratorReview(log" src/gsd/verify.js` → match (1 llamada)
    - `grep -c "orchestratorReview(" src/gsd/verify.js` = 2 (1 import + 1 call-site)
    - `grep -qF "approved" src/gsd/verify.js && grep -qF "blocked" src/gsd/verify.js` → match (Pitfall #2: mapping aplicado)
    - `grep -qF "no phase_id" src/gsd/verify.js` → match (Pitfall #4 resuelto)
    - `grep -qF "updateTaskState" src/gsd/verify.js` → match (en rama pass)
    - `grep -qF "plane-unreachable" src/gsd/verify.js` → match (degradación legacyVerdict cuando getTask falla)
    - `grep -qF "✅ Phase" src/gsd/verify.js && grep -qF "❌ Phase" src/gsd/verify.js && grep -qF "⚠️" src/gsd/verify.js` → plantillas ES presentes
    - `grep -qF "[kodo:gsd]" src/gsd/verify.js` → prefijo de comentarios presente
    - **Hoisted provider invariant**: en el cuerpo de `finalize(...)`, `getProviderFn()` se invoca UNA SOLA VEZ. Verificación:
      `grep -c "await getProviderFn()" src/gsd/verify.js` ≤ 1
      (El factory default export puede llamar a `getProvider()` internamente, pero el cuerpo de finalize debe tener exactamente 1 llamada a `await getProviderFn()`. Permitido hasta 1 call-site.)
    - `node --test test/gsd-verify-cli.test.js` exits 0
    - `node --test test/gsd-verify-integration.test.js` exits 0
  </acceptance_criteria>
  <done>
    `src/gsd/verify.js` exporta `runGsdVerify`. Todos los 7 pitfalls resueltos en el código. `finalize()` invoca `getProviderFn()` UNA sola vez (hoisted `const provider`). Tests pasan 100%. Llamadas a getTask/addComment/updateTaskState envueltas en try/catch individuales. orchestratorReview emitido exactamente 1x por ejecución. Plantillas ES deterministas sin LLM.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests CLI (mocks DI) — test/gsd-verify-cli.test.js</name>
<files>test/gsd-verify-cli.test.js</files>
  <read_first>
    - test/gsd-inspect-cli.test.js (analog primario — makeStdoutStub, deps inyectadas, assertions sobre side-effects)
    - src/gsd/verify.js (output de Task 1)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §9 (test pattern CLI)
  </read_first>
  <behavior>
    Ya descrito en Task 1 (T1..T19, T18b, T19b, T17b). Este task materializa esos 22 casos como código ejecutable.
  </behavior>
  <action>
    Crear `test/gsd-verify-cli.test.js`. Estructura:

    ```javascript
    // @ts-check
    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';
    import { runGsdVerify } from '../src/gsd/verify.js';

    // --- Mock factories ---

    function makeSession(overrides = {}) {
      return {
        session_id: 'sess-1',
        task_id: 'task-uuid',
        task_ref: 'KL-42',
        provider: 'plane',
        project_id: 'proj-1',
        project_path: '/tmp/fake-project',
        summary: 'Implement orchestrator gate',
        status: 'review',
        started_at: new Date().toISOString(),
        workspace_ref: 'workspace:1',
        gsd: true,
        phase_id: '10',
        ...overrides,
      };
    }

    function makeProviderMock(overrides = {}) {
      const calls = { getTask: [], addComment: [], updateTaskState: [] };
      const provider = {
        getTask: overrides.getTask || (async (ref) => { calls.getTask.push(ref); return { id: 'task-uuid', ref, title: 'T' }; }),
        addComment: overrides.addComment || (async (task, md) => { calls.addComment.push({ task, md }); }),
        updateTaskState: overrides.updateTaskState || (async (task, state) => { calls.updateTaskState.push({ task, state }); }),
      };
      return { provider, calls };
    }

    function makeLoggerMock() {
      const events = [];
      const logger = {
        info: (msg, fields) => events.push({ level: 'info', msg, fields }),
        warn: (msg, fields) => events.push({ level: 'warn', msg, fields }),
        error: (msg, fields) => events.push({ level: 'error', msg, fields }),
        child: () => logger,
      };
      return { logger, events };
    }

    function makeConfig(overrides = {}) {
      return {
        provider: 'plane',
        providers: {
          plane: { states: { trigger: 'In Progress', review: 'In review', done: 'Done' } },
        },
        ...overrides,
      };
    }

    function makeDeps({ session, verificationMd, phaseExists = true, extraDeps = {} }) {
      const { provider, calls } = makeProviderMock();
      const { logger, events } = makeLoggerMock();
      // Spy sobre getProviderFn para contar invocaciones (Sub-concern H).
      let getProviderCalls = 0;
      const getProviderFn = extraDeps.getProviderFn || (async () => { getProviderCalls++; return provider; });
      return {
        deps: {
          findSessionFn: () => session,
          getProviderFn,
          loadConfigFn: () => makeConfig(),
          existsFn: (p) => {
            if (p.endsWith('/phases')) return phaseExists;
            if (p.endsWith('VERIFICATION.md')) return verificationMd !== null;
            return true;
          },
          readdirFn: () => phaseExists ? ['10-foo', '09-bar'] : [],
          readFileFn: () => verificationMd,
          loggerFactory: () => logger,
          ...extraDeps,
        },
        calls, events,
        getProviderCalls: () => getProviderCalls,
      };
    }

    // --- Tests ---

    describe('runGsdVerify — DI + session resolution', () => {
      it('T1: throws cuando session-id no encontrado', async () => {
        await assert.rejects(
          () => runGsdVerify({ sessionId: 'nope' }, { findSessionFn: () => undefined }),
          /session not found: nope/
        );
      });

      it('T2: throws cuando session.gsd === false', async () => {
        const session = makeSession({ gsd: false });
        await assert.rejects(
          () => runGsdVerify({ sessionId: 'sess-1' }, { findSessionFn: () => session }),
          /session is not GSD/
        );
      });

      it('T3: session sin phase_id → malformed, no filesystem (Pitfall #4)', async () => {
        const session = makeSession({ phase_id: undefined });
        const { deps, calls } = makeDeps({ session, verificationMd: null });
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.verdict.action, 'malformed');
        assert.match(result.verdict.detail, /no phase_id/);
        assert.equal(calls.addComment.length, 1, 'addComment se llama igual (comentario de malformed)');
        assert.equal(calls.updateTaskState.length, 0);
      });
    });

    describe('runGsdVerify — VERIFICATION.md discovery (Pitfall #3)', () => {
      it('T4: archivo no existe → missing', async () => {
        const session = makeSession();
        const { deps, calls } = makeDeps({ session, verificationMd: null, phaseExists: true });
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.verdict.action, 'missing');
        assert.equal(calls.addComment.length, 1);
        assert.equal(calls.updateTaskState.length, 0);
      });

      it('T5: no hay directorio de fase → missing', async () => {
        const session = makeSession();
        const { deps } = makeDeps({ session, verificationMd: null, phaseExists: false });
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.verdict.action, 'missing');
      });

      it('T6: prefix-match exacto: "03" NO matchea "30-other"', async () => {
        const session = makeSession({ phase_id: '03' });
        let readPath;
        const { deps } = makeDeps({
          session,
          verificationMd: '---\nstatus: passed\nmust_haves_total: 1\nmust_haves_verified: 1\ngaps_count: 0\n---',
          extraDeps: {
            readdirFn: () => ['30-other', '03-foundation'],
            existsFn: (p) => p.includes('03-foundation') || p.endsWith('/phases'),
            readFileFn: (p) => { readPath = p; return '---\nstatus: passed\nmust_haves_total: 1\nmust_haves_verified: 1\ngaps_count: 0\n---'; },
          },
        });
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.match(readPath, /03-foundation/);
        assert.ok(!readPath.includes('30-other'));
      });
    });

    describe('runGsdVerify — verdict → Plane side-effects', () => {
      const validPassFrontmatter = ['---','status: passed','must_haves_total: 5','must_haves_verified: 5','gaps_count: 0','---'].join('\n');
      const gapsFrontmatter = ['---','status: passed','must_haves_total: 5','must_haves_verified: 5','gaps_count: 2','---'].join('\n');

      it('T7: pass → addComment 1x + updateTaskState 1x con config.providers[plane].states.review', async () => {
        const session = makeSession();
        const { deps, calls } = makeDeps({ session, verificationMd: validPassFrontmatter });
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.verdict.action, 'pass');
        assert.equal(calls.addComment.length, 1);
        assert.equal(calls.updateTaskState.length, 1);
        assert.equal(calls.updateTaskState[0].state, 'In review'); // Pitfall #1
        assert.match(calls.addComment[0].md, /✅ Phase 10/);
      });

      it('T8: fail → addComment 1x, updateTaskState 0x (D-12)', async () => {
        const session = makeSession();
        const { deps, calls } = makeDeps({ session, verificationMd: gapsFrontmatter });
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.verdict.action, 'fail');
        assert.equal(result.verdict.reason, 'gaps-found');
        assert.equal(calls.addComment.length, 1);
        assert.equal(calls.updateTaskState.length, 0);
        assert.match(calls.addComment[0].md, /❌ Phase 10 bloqueada/);
      });

      it('T9: missing → addComment 1x, updateTaskState 0x', async () => { /* ... */ });
      it('T10: malformed → addComment 1x, updateTaskState 0x', async () => { /* ... */ });
    });

    describe('runGsdVerify — idempotencia (Pitfall #7)', () => {
      it('T11: dos invocaciones → addComment 2x (no dedup)', async () => {
        const session = makeSession();
        const validPassFrontmatter = ['---','status: passed','must_haves_total: 5','must_haves_verified: 5','gaps_count: 0','---'].join('\n');
        const { deps, calls } = makeDeps({ session, verificationMd: validPassFrontmatter });
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(calls.addComment.length, 2);
        assert.equal(calls.addComment[0].md, calls.addComment[1].md, 'idénticos byte-a-byte');
      });
    });

    describe('runGsdVerify — orchestratorReview mapping (Pitfall #2)', () => {
      const validPassFrontmatter = ['---','status: passed','must_haves_total: 5','must_haves_verified: 5','gaps_count: 0','---'].join('\n');

      it('T12: pass → orchestratorReview { verdict: "approved", reason: "gate-passed" }', async () => {
        const session = makeSession();
        const { deps, events } = makeDeps({ session, verificationMd: validPassFrontmatter });
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        const review = events.find((e) => e.msg && String(e.msg).includes('orchestrator.review'));
        assert.ok(review, 'orchestrator.review emitted');
        assert.equal(review.fields.verdict, 'approved');
        assert.equal(review.fields.reason, 'gate-passed');
        assert.equal(review.fields.phase_id, '10');
      });

      it('T13: fail gaps-found → verdict blocked, reason incluye gaps-found', async () => { /* ... */ });
      it('T14: missing → verdict blocked, reason "missing"', async () => { /* ... */ });
      it('T15: malformed → verdict blocked, reason incluye "malformed"', async () => { /* ... */ });
      it('T16: orchestratorReview emitido EXACTAMENTE 1 vez por run', async () => {
        const session = makeSession();
        const { deps, events } = makeDeps({ session, verificationMd: validPassFrontmatter });
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        const reviews = events.filter((e) => e.msg && String(e.msg).includes('orchestrator.review'));
        assert.equal(reviews.length, 1);
      });
    });

    describe('runGsdVerify — NO plane.api.call duplicado (Pitfall #5)', () => {
      const validPassFrontmatter = ['---','status: passed','must_haves_total: 5','must_haves_verified: 5','gaps_count: 0','---'].join('\n');
      it('T17: events NO contienen plane.api.call (success) emitido por verify.js', async () => {
        const session = makeSession();
        const { deps, events } = makeDeps({ session, verificationMd: validPassFrontmatter });
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        const planeSuccess = events.filter((e) => e.msg === 'plane.api.call');
        assert.equal(planeSuccess.length, 0, 'verify.js does NOT emit plane.api.call');
      });

      it('T17b: getProviderFn se invoca EXACTAMENTE 1 vez por runGsdVerify (hoisted provider)', async () => {
        const session = makeSession();
        const { deps, getProviderCalls } = makeDeps({ session, verificationMd: validPassFrontmatter });
        await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(getProviderCalls(), 1, 'finalize debe hoistear provider, no llamarlo 3 veces');
      });
    });

    describe('runGsdVerify — fail-open Plane (D-17)', () => {
      const validPassFrontmatter = ['---','status: passed','must_haves_total: 5','must_haves_verified: 5','gaps_count: 0','---'].join('\n');

      it('T18: getTask lanza → addComment NO invocado, orchestratorReview emitido con blocked+plane-unreachable', async () => {
        const session = makeSession();
        const brokenProvider = {
          getTask: async () => { throw new Error('plane 500 getTask'); },
          addComment: async () => { throw new Error('should not be called'); },
          updateTaskState: async () => { throw new Error('should not be called'); },
        };
        const { deps, events } = makeDeps({ session, verificationMd: validPassFrontmatter, extraDeps: {
          getProviderFn: async () => brokenProvider,
        }});
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.plane.commented, false);
        assert.equal(result.plane.transitioned, false);
        const getTaskFail = events.find((e) => e.msg === 'plane.api.call.failed' && e.fields.step === 'getTask');
        assert.ok(getTaskFail, 'plane.api.call.failed{step:getTask} emitido');
        const review = events.find((e) => e.msg && String(e.msg).includes('orchestrator.review'));
        assert.ok(review, 'review emitido a pesar del fallo');
        assert.equal(review.fields.verdict, 'blocked');
        assert.match(review.fields.reason, /plane-unreachable/);
      });

      it('T18b: getTask lanza (verdict pass) → transitioned=false, commented=false, review=blocked', async () => {
        const session = makeSession();
        const brokenProvider = {
          getTask: async () => { throw new Error('ECONNREFUSED'); },
          addComment: async () => {},
          updateTaskState: async () => {},
        };
        const { deps, events, calls } = makeDeps({ session, verificationMd: validPassFrontmatter, extraDeps: {
          getProviderFn: async () => brokenProvider,
        }});
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.verdict.action, 'pass');  // verdict local sigue siendo pass
        assert.equal(result.plane.commented, false);
        assert.equal(result.plane.transitioned, false);
        const review = events.find((e) => e.msg && String(e.msg).includes('orchestrator.review'));
        assert.equal(review.fields.verdict, 'blocked', 'degrade a blocked porque side-effects no completaron');
        assert.match(review.fields.reason, /plane-unreachable/);
      });

      it('T19: addComment lanza (getTask OK, verdict pass) → updateTaskState NO invocado, review emitido', async () => {
        const session = makeSession();
        const calls = { updateTaskState: [] };
        const brokenProvider = {
          getTask: async () => ({ id: 'task-uuid', ref: 'KL-42', title: 'T' }),
          addComment: async () => { throw new Error('plane 500 addComment'); },
          updateTaskState: async (t, s) => { calls.updateTaskState.push({ t, s }); },
        };
        const { deps, events } = makeDeps({ session, verificationMd: validPassFrontmatter, extraDeps: {
          getProviderFn: async () => brokenProvider,
        }});
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.plane.commented, false);
        assert.equal(result.plane.transitioned, false);
        assert.equal(calls.updateTaskState.length, 0, 'no transicionamos si no comentamos');
        const addFail = events.find((e) => e.msg === 'plane.api.call.failed' && e.fields.step === 'addComment');
        assert.ok(addFail);
      });

      it('T19b: updateTaskState lanza (pass, addComment OK) → commented=true, transitioned=false, review=approved', async () => {
        const session = makeSession();
        const brokenProvider = {
          getTask: async () => ({ id: 'task-uuid', ref: 'KL-42', title: 'T' }),
          addComment: async () => {},
          updateTaskState: async () => { throw new Error('plane 500 updateTaskState'); },
        };
        const { deps, events } = makeDeps({ session, verificationMd: validPassFrontmatter, extraDeps: {
          getProviderFn: async () => brokenProvider,
        }});
        const result = await runGsdVerify({ sessionId: 'sess-1' }, deps);
        assert.equal(result.plane.commented, true);
        assert.equal(result.plane.transitioned, false);
        const review = events.find((e) => e.msg && String(e.msg).includes('orchestrator.review'));
        assert.equal(review.fields.verdict, 'approved', 'el verdict local es pass y addComment OK → approved');
      });
    });
    ```

    Cada `TODO` debe ser implementado. Total 22 tests (T1..T19 + T17b + T18b + T19b).
  </action>
  <verify>
    <automated>node --test test/gsd-verify-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" test/gsd-verify-cli.test.js` ≥ 20
    - `grep -qF "from '../src/gsd/verify.js'" test/gsd-verify-cli.test.js` → match
    - `grep -qF "session not found" test/gsd-verify-cli.test.js` → match (T1)
    - `grep -qF "is not GSD" test/gsd-verify-cli.test.js` → match (T2)
    - `grep -qF "no phase_id" test/gsd-verify-cli.test.js` → match (T3, Pitfall #4)
    - `grep -qF "30-other" test/gsd-verify-cli.test.js && grep -qF "03-foundation" test/gsd-verify-cli.test.js` → match (T6, Pitfall #3)
    - `grep -qF "Pitfall" test/gsd-verify-cli.test.js` → match (comentarios documentando qué pitfall cubre cada test)
    - `grep -qF "approved" test/gsd-verify-cli.test.js && grep -qF "blocked" test/gsd-verify-cli.test.js` → match (T12, Pitfall #2)
    - `grep -qF "plane-unreachable" test/gsd-verify-cli.test.js` → match (T18, T18b)
    - `grep -qF "getProviderCalls" test/gsd-verify-cli.test.js` → match (T17b: spy sobre factory)
    - `grep -qF "T18b" test/gsd-verify-cli.test.js && grep -qF "T19b" test/gsd-verify-cli.test.js` → ambos edge-cases presentes
    - `node --test test/gsd-verify-cli.test.js` exits 0 con todos los tests ok
  </acceptance_criteria>
  <done>
    `test/gsd-verify-cli.test.js` ejecuta 20+ tests con 100% pass. Cada Pitfall (#1..#5, #7) tiene al menos 1 test dedicado. Sub-concern D (fail-open) cubre los 4 vectores: T18 (getTask falla), T18b (getTask falla en pass), T19 (addComment falla), T19b (updateTaskState falla en pass). T17b valida el hoisted provider. DI permite 0 side-effects reales (no filesystem real, no red).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests integración con tmp .planning/ — test/gsd-verify-integration.test.js</name>
<files>test/gsd-verify-integration.test.js</files>
  <read_first>
    - test/gsd-concurrency.test.js (patrón mkdtempSync + rmSync + cleanup)
    - test/gsd-lock.test.js (setup/teardown beforeEach/afterEach)
    - src/gsd/verify.js (output Task 1)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §10 (integration test pattern)
  </read_first>
  <behavior>
    Ya descrito en Task 1 (T20, T21). Materializar como código.
  </behavior>
  <action>
    Crear `test/gsd-verify-integration.test.js`:

    ```javascript
    // @ts-check
    import { describe, it, beforeEach, afterEach } from 'node:test';
    import assert from 'node:assert/strict';
    import { mkdtempSync, rmSync, writeFileSync, mkdirSync } from 'node:fs';
    import { tmpdir } from 'node:os';
    import { join } from 'node:path';
    import { runGsdVerify } from '../src/gsd/verify.js';

    describe('runGsdVerify — integración con filesystem real (.planning/ sintético)', () => {
      let tmpRoot;

      beforeEach(() => {
        tmpRoot = mkdtempSync(join(tmpdir(), 'kodo-verify-'));
        mkdirSync(join(tmpRoot, '.planning', 'phases', '10-orchestrator-verification-gate'), { recursive: true });
      });

      afterEach(() => {
        rmSync(tmpRoot, { recursive: true, force: true });
      });

      function makeSession() {
        return {
          session_id: 'sess-int',
          task_id: 'task-int',
          task_ref: 'KL-99',
          provider: 'plane',
          project_id: 'proj-int',
          project_path: tmpRoot,
          summary: 'Orchestrator gate',
          status: 'review',
          started_at: new Date().toISOString(),
          workspace_ref: 'workspace:1',
          gsd: true,
          phase_id: '10',
        };
      }

      function makeProviderMock() {
        const calls = { getTask: [], addComment: [], updateTaskState: [] };
        return {
          provider: {
            getTask: async (ref) => { calls.getTask.push(ref); return { id: 'task-int', ref, title: 'T' }; },
            addComment: async (task, md) => { calls.addComment.push({ task, md }); },
            updateTaskState: async (task, state) => { calls.updateTaskState.push({ task, state }); },
          },
          calls,
        };
      }

      function makeLogger() {
        const events = [];
        const logger = {
          info: (m, f) => events.push({ level: 'info', msg: m, fields: f }),
          warn: (m, f) => events.push({ level: 'warn', msg: m, fields: f }),
          error: (m, f) => events.push({ level: 'error', msg: m, fields: f }),
          child: () => logger,
        };
        return { logger, events };
      }

      it('T20 E2E: VERIFICATION.md pass → addComment + updateTaskState + orchestratorReview(approved)', () => {
        writeFileSync(
          join(tmpRoot, '.planning', 'phases', '10-orchestrator-verification-gate', '10-VERIFICATION.md'),
          [
            '---',
            'status: passed',
            'must_haves_total: 8',
            'must_haves_verified: 8',
            'gaps_count: 0',
            '---',
            '',
            '# Body ignorado',
          ].join('\n'),
        );
        const session = makeSession();
        const { provider, calls } = makeProviderMock();
        const { logger, events } = makeLogger();
        const deps = {
          findSessionFn: () => session,
          getProviderFn: async () => provider,
          loadConfigFn: () => ({ provider: 'plane', providers: { plane: { states: { review: 'In review' } } } }),
          loggerFactory: () => logger,
        };
        return runGsdVerify({ sessionId: 'sess-int' }, deps).then((result) => {
          assert.equal(result.verdict.action, 'pass');
          assert.equal(result.verdict.must_haves, 8);
          assert.equal(result.plane.commented, true);
          assert.equal(result.plane.transitioned, true);
          assert.equal(calls.addComment.length, 1);
          assert.match(calls.addComment[0].md, /✅ Phase 10 verificada/);
          assert.equal(calls.updateTaskState.length, 1);
          assert.equal(calls.updateTaskState[0].state, 'In review');
        });
      });

      it('T21 E2E: VERIFICATION.md con gaps_count=2 → fail + addComment + NO transition', () => {
        writeFileSync(
          join(tmpRoot, '.planning', 'phases', '10-orchestrator-verification-gate', '10-VERIFICATION.md'),
          [
            '---',
            'status: passed',
            'must_haves_total: 8',
            'must_haves_verified: 8',
            'gaps_count: 2',
            '---',
          ].join('\n'),
        );
        const session = makeSession();
        const { provider, calls } = makeProviderMock();
        const { logger } = makeLogger();
        const deps = {
          findSessionFn: () => session,
          getProviderFn: async () => provider,
          loadConfigFn: () => ({ provider: 'plane', providers: { plane: { states: { review: 'In review' } } } }),
          loggerFactory: () => logger,
        };
        return runGsdVerify({ sessionId: 'sess-int' }, deps).then((result) => {
          assert.equal(result.verdict.action, 'fail');
          assert.equal(result.verdict.reason, 'gaps-found');
          assert.match(result.verdict.detail, /gaps_count=2/);
          assert.equal(calls.addComment.length, 1);
          assert.match(calls.addComment[0].md, /❌ Phase 10 bloqueada/);
          assert.match(calls.addComment[0].md, /gaps_count=2/);
          assert.equal(calls.updateTaskState.length, 0);
        });
      });

      it('T22 E2E: VERIFICATION.md con status desconocido → malformed, comentario warn', () => {
        writeFileSync(
          join(tmpRoot, '.planning', 'phases', '10-orchestrator-verification-gate', '10-VERIFICATION.md'),
          ['---', 'status: in_progress', 'must_haves_total: 8', 'must_haves_verified: 5', 'gaps_count: 3', '---'].join('\n'),
        );
        // ... assert verdict.action === 'malformed' + addComment con "⚠️"
      });

      it('T23 E2E: sin directorio de fase → missing', () => {
        // No crear el directorio 10-*. Solo el tmpRoot/.planning existe.
        rmSync(join(tmpRoot, '.planning', 'phases', '10-orchestrator-verification-gate'), { recursive: true });
        // ... assert verdict.action === 'missing'
      });
    });
    ```

    Total: 4 tests E2E. Usar `writeFileSync` real + `rmSync` cleanup — zero mocks de filesystem para verificar que el path discovery funciona end-to-end.
  </action>
  <verify>
    <automated>node --test test/gsd-verify-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" test/gsd-verify-integration.test.js` ≥ 4
    - `grep -qF "mkdtempSync" test/gsd-verify-integration.test.js && grep -qF "writeFileSync" test/gsd-verify-integration.test.js` → match
    - `grep -qF "rmSync" test/gsd-verify-integration.test.js` → match (cleanup)
    - `grep -qF "10-VERIFICATION.md" test/gsd-verify-integration.test.js` → match (path real)
    - `grep -qF "✅ Phase 10" test/gsd-verify-integration.test.js || grep -qF "❌ Phase 10" test/gsd-verify-integration.test.js` → match (asserts sobre plantillas)
    - `node --test test/gsd-verify-integration.test.js` exits 0
    - Después del test, `ls /tmp/kodo-verify-*` → ningún dir huérfano (cleanup OK)
  </acceptance_criteria>
  <done>
    Integration tests pasan. Tmp dirs creados y limpiados correctamente. Los 4 escenarios E2E (pass, fail, malformed, missing) ejercitan el path real de discovery + parsing + verdict.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-id (CLI arg) → findSession | String arbitrario del invocante. Podría intentar path traversal si lo usáramos como path. NO lo hacemos: findSession lo busca como KEY en state.json. |
| state.json → session.project_path | Ya validado en Phase 8/9 (resolveProjectPath). Asumimos trust (home-dir controlado por usuario). |
| VERIFICATION.md content → parseVerificationFrontmatter | Ver Plan 10-01 threat model. |
| verdict + phaseName → markdown template | Interpolación string directa; `phaseName` viene de `session.summary` (ya sanitizado en state.json). |
| markdown → provider.addComment | Provider lo convierte a HTML; Plane renderiza. Sin XSS porque Plane escapa. |
| reviewState (config) → provider.updateTaskState | String del config local del usuario. No untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Spoofing | session-id param | mitigate | findSession busca la key en state.json (escrito por el propio kodo). Un session-id falso simplemente retorna undefined → throw "session not found". No hay escalación. |
| T-10-02-02 | Tampering | session.project_path | accept | Ya validado upstream. Si el usuario manipula state.json directamente, está atacando su propio sistema (no hay superficie remota). |
| T-10-02-03 | Tampering | VERIFICATION.md path construction | mitigate | Construimos path como `join(project_path, '.planning', 'phases', matchedDir, `${padded}-VERIFICATION.md`)`. `matchedDir` viene de `readdirSync` (no de input del usuario). `padded` es `phase_id` de la session (validado en Phase 9 resolver). No hay path traversal. |
| T-10-02-04 | Injection | markdown → Plane | mitigate | El markdown template es controlado por kodo. Los únicos valores interpolados son `verdict.phase_id` (número, de ROADMAP), `must_haves` (número, del frontmatter), `detail` (string del parseo, limitado a enum statuses + números). Plane escapa HTML al renderizar (ver provider.js:182-185 serialización a `<p>`/`<br>`). Riesgo de injection de markdown: bajo (los valores no incluyen `]`, `[`, `)` peligrosos por construcción). Accept con nota: si futuros campos del frontmatter entran al detail, revisar escape. |
| T-10-02-05 | Info Disclosure | orchestrator.review log | accept | El log NDJSON vive en `~/.kodo/logs/`. No contiene secretos (phase_id, verdict, reason). Accept. |
| T-10-02-06 | DoS | readdirSync en phases/ | accept | `.planning/phases/` tiene decenas de dirs como máximo. No hay vector remoto. |
| T-10-02-07 | Elevation | provider.updateTaskState en pass | mitigate | Solo se llama cuando verdict.action === 'pass' Y addComment previo OK (asserción cubierta por T7/T8/T19 tests + T-10-01 precedence logic). config.providers[provider].states.review es string controlado por config local. |
| T-10-02-08 | Tampering | getProviderFn DI | accept | En producción getProviderFn viene del registry oficial. En tests, DI permite inyectar mocks; es intencional y no es un vector de producción. |
</threat_model>

<verification>
- `node --test test/gsd-verify-cli.test.js` exits 0.
- `node --test test/gsd-verify-integration.test.js` exits 0.
- Los 7 pitfalls resueltos verificados via grep (ver acceptance_criteria Task 1).
- Plantilla español (✅, ❌, ⚠️, "verificada", "bloqueada", "Motivo:") presente.
- orchestratorReview emitido UNA vez, mapeando pass→approved, resto→blocked.
- finalize() invoca getProviderFn() UNA SOLA VEZ (hoisted provider).
</verification>

<success_criteria>
- [ ] `src/gsd/verify.js` creado con export `runGsdVerify`.
- [ ] Los 7 pitfalls resueltos explícitamente en código (#1 providers[].states, #2 mapping, #3 readdirSync, #4 no phase_id, #5 no plane.api.call, #6 exit codes en Plan 10-03, #7 no dedup documentado).
- [ ] `finalize()` invoca `getProviderFn()` UNA SOLA VEZ (hoisted `const provider = await getProviderFn()`).
- [ ] `test/gsd-verify-cli.test.js` con 20+ tests pasando (incluidos T17b, T18b, T19b).
- [ ] `test/gsd-verify-integration.test.js` con 4+ tests E2E pasando.
- [ ] addComment invocado 1x cuando getTask OK (todas las ramas del verdict: pass/fail/missing/malformed).
- [ ] addComment NO invocado cuando getTask falla (fail-open).
- [ ] updateTaskState invocado SÓLO en pass Y con addComment previo OK.
- [ ] orchestratorReview invocado exactamente 1x por run.
- [ ] orchestratorReview degrada a 'blocked' cuando verdict=pass pero task no obtenido.
- [ ] verify.js NO emite `plane.api.call` (solo el provider lo hace).
- [ ] Fail-open: errores de getTask/addComment/updateTaskState no impiden emitir orchestratorReview.
</success_criteria>

<output>
After completion, create `.planning/phases/10-orchestrator-verification-gate/10-02-SUMMARY.md`
</output>
</content>
</invoke>