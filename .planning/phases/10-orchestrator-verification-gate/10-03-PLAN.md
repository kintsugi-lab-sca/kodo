---
phase: 10-orchestrator-verification-gate
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/cli/gsd-verify.js
  - src/cli.js
  - test/gsd-verify-cli-handler.test.js
autonomous: true
requirements: [GSD-05, GSD-06]

must_haves:
  truths:
    - "El comando `kodo gsd verify <session-id>` existe y es invocable desde el CLI principal"
    - "El handler `runGsdVerify` del CLI delega a `src/gsd/verify.js` sin reimplementar lógica"
    - "El CLI emite verdict human-readable por stdout en modo default"
    - "El CLI emite verdict + plane result como JSON cuando --json flag está presente"
    - "Exit code 0 cuando el gate corre entregando cualquier verdict (pass|fail|missing|malformed) — Opción A de Pitfall #6"
    - "Exit code 1 cuando hay error interno (session no encontrada, state.json no legible, session no es GSD)"
    - "Exit code 2 cuando hay error de fetch del provider (transient)"
    - "Idempotencia documentada en el comentario del comando Commander (Pitfall #7 — acepta duplicados)"
  artifacts:
    - path: "src/cli/gsd-verify.js"
      provides: "Thin handler CLI que delega a runGsdVerify"
      exports: ["runGsdVerifyCli"]
      contains: "switch(verdict.action)"
    - path: "src/cli.js"
      provides: "Registro del subcomando gsd verify"
      contains: "gsd.command('verify <session-id>'"
    - path: "test/gsd-verify-cli-handler.test.js"
      provides: "Tests del handler CLI con DI mocks"
      contains: "node:test"
  key_links:
    - from: "src/cli.js"
      to: "src/cli/gsd-verify.js"
      via: "dynamic import + .action()"
      pattern: "import\\('./cli/gsd-verify.js'\\)"
    - from: "src/cli/gsd-verify.js"
      to: "src/gsd/verify.js"
      via: "import { runGsdVerify }"
      pattern: "from '../gsd/verify.js'"
---

<objective>
Crear `src/cli/gsd-verify.js`, el thin CLI handler para `kodo gsd verify <session-id>`. Delega en `runGsdVerify` de Plan 10-02, emite el resultado en formato human-readable (default) o JSON (`--json`), y mapea errores a exit codes deterministas (Pitfall #6, Opción A). Registrar el subcomando en `src/cli.js` tras `kodo gsd inspect`.

Purpose: Separar "routing argv → side-effects" del "orquestador de verificación". El CLI es puro argv→delegación→render; cualquier cambio de semántica del gate vive en `verify.js`. Pattern idéntico al de Phase 9 `gsd-inspect` (D-04 paralelo). Cubre GSD-05 (interfaz invocable) + GSD-06 (entrega del verdict con exit code scriptable).

Output:
- `src/cli/gsd-verify.js` con export `runGsdVerifyCli(opts, deps)`.
- Modificación de `src/cli.js`: bloque `.command('verify <session-id>')` registrado bajo el grupo `gsd`.
- `test/gsd-verify-cli-handler.test.js` con tests para exit codes + JSON output + render human.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md
@.planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md
@.planning/phases/10-orchestrator-verification-gate/10-02-PLAN.md

<interfaces>
<!-- Contrato desde Plan 10-02 (ya existe al ejecutar este plan): -->

```javascript
// src/gsd/verify.js (Plan 10-02)
export async function runGsdVerify({ sessionId }, deps?): Promise<{
  verdict: Verdict,
  plane: { commented: boolean, transitioned: boolean },
  session: { session_id, task_ref, phase_id },
}>;
// Verdict = { action: 'pass' | 'fail' | 'missing' | 'malformed', phase_id, ... }
// Ver 10-01 <interfaces> para shape exacto del Verdict.
```

<!-- Contrato nuevo que exporta este plan: -->

```javascript
// src/cli/gsd-verify.js (NEW)

/**
 * @typedef {{ sessionId: string, json?: boolean }} RunGsdVerifyCliOpts
 * @typedef {{
 *   runVerifyFn?: typeof import('../gsd/verify.js').runGsdVerify,
 *   writeFn?: (s: string) => void,
 *   errFn?: (s: string) => void,
 * }} RunGsdVerifyCliDeps
 */

export async function runGsdVerifyCli(opts: RunGsdVerifyCliOpts, deps?: RunGsdVerifyCliDeps): Promise<number>;
// Exit codes (Pitfall #6, Opción A):
//   0 = gate corrió entregando cualquier verdict
//   1 = error interno (session not found, session not GSD)
//   2 = provider fetch failure (transient)
```
</interfaces>

<patterns>
Clon exacto de `src/cli/gsd-inspect.js` adaptado al nuevo dominio.
Registro en `src/cli.js`: clonar bloque líneas 244-258 (el `gsd inspect`) e insertar justo después como bloque paralelo.
`program.parse()` está en línea 260 — el nuevo `.command` debe registrarse ANTES.
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/cli/gsd-verify.js (thin handler) + tests</name>
<files>src/cli/gsd-verify.js, test/gsd-verify-cli-handler.test.js</files>
  <read_first>
    - src/cli/gsd-inspect.js (analog byte-a-byte — copiar estructura, ajustar semántica)
    - src/gsd/verify.js (output Plan 10-02 — contrato del runGsdVerify)
    - test/gsd-inspect-cli.test.js (patrón de tests CLI con stdout stub)
    - .planning/phases/10-orchestrator-verification-gate/10-CONTEXT.md §Claude's Discretion (exit codes)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §3 (pattern CLI) + §Metadata Pitfall #6
  </read_first>
  <behavior>
    Tests en `test/gsd-verify-cli-handler.test.js` DEBEN verificar:

    **Exit codes (Pitfall #6, Opción A)**
    - C1: verdict pass → exit 0, stdout incluye "action:      pass"
    - C2: verdict fail → exit 0 (gate corrió, entregó verdict), stdout incluye "action:      fail" y "reason:"
    - C3: verdict missing → exit 0, stdout incluye "action:      missing"
    - C4: verdict malformed → exit 0, stdout incluye "action:      malformed"
    - C5: runGsdVerify lanza `Error: session not found` → exit 1, stderr incluye "session not found"
    - C6: runGsdVerify lanza `Error: session is not GSD` → exit 1, stderr incluye "is not GSD"
    - C7: runGsdVerify lanza otro error (simulando "provider fetch failed") → exit 2, stderr incluye el mensaje. **IMPORTANTE**: el handler distingue errores internos (exit 1) vs errores transient (exit 2) por el mensaje. Regla: si error.message matchea `/provider.*fetch|ECONNREFUSED|ETIMEDOUT|network/i` → exit 2. Else → exit 1.

    **JSON output**
    - C8: con `--json` → stdout es JSON parseable con keys `verdict`, `plane`, `session`.
    - C9: sin `--json` → stdout es texto human-readable (no parseable como JSON).

    **Rendering human**
    - C10: renderHuman imprime campos exactos por verdict action (switch exhaustivo):
      - pass: `action`, `phase_id`, `must_haves`
      - fail: `action`, `phase_id`, `reason`, `detail`
      - missing: `action`, `phase_id`
      - malformed: `action`, `phase_id`, `detail`
    - C11: renderHuman imprime línea final `Plane: commented=<bool> transitioned=<bool>`.

    **DI determinismo**
    - C12: runVerifyFn mockeable; el handler no debe importar dependencias reales cuando deps los provee.

    **Registro en cli.js (Task 2)**
    - CLI-STATIC: un test `it('cli.js registra gsd verify')` que lee `src/cli.js` vía `readFileSync` y assert que contiene el string literal `.command('verify <session-id>')` y el import dinámico del handler. Preferido sobre spawnSync por determinismo y velocidad.
  </behavior>
  <action>
    Crear `src/cli/gsd-verify.js`:

    ```javascript
    // @ts-check
    //
    // src/cli/gsd-verify.js — Action handler de `kodo gsd verify <session-id>`.
    //
    // Responsabilidades (CONTEXT §D-20, §D-21, Claude's Discretion exit codes):
    //   1. Invocar runGsdVerify de src/gsd/verify.js (toda la lógica vive ahí).
    //   2. Render human-readable (default) o JSON (--json, scriptable).
    //   3. Exit codes (Pitfall #6, Opción A — alineada con gsd-inspect.js):
    //        0 = gate corrió entregando cualquier verdict (pass/fail/missing/malformed).
    //        1 = error interno (session-id no encontrado, session.gsd false, state.json no legible).
    //        2 = provider fetch failure (transient, retryable por script operador).
    //
    // Idempotencia (Pitfall #7): kodo NO deduplica comentarios Plane. Re-invocar este CLI
    // para la misma sesión postea un comentario adicional byte-idéntico (excepto timestamp).
    // Aceptado en v0.3 para minimizar superficie; ver .planning/.../10-CONTEXT.md §Deferred.

    import { runGsdVerify } from '../gsd/verify.js';

    /**
     * @typedef {{ sessionId: string, json?: boolean }} RunGsdVerifyCliOpts
     * @typedef {{
     *   runVerifyFn?: typeof runGsdVerify,
     *   writeFn?: (s: string) => void,
     *   errFn?: (s: string) => void,
     * }} RunGsdVerifyCliDeps
     */

    const TRANSIENT_PATTERNS = /provider.*fetch|fetch.*failed|ECONNREFUSED|ETIMEDOUT|network|getaddrinfo/i;

    /**
     * Run the GSD verification gate for a session.
     * @param {RunGsdVerifyCliOpts} opts
     * @param {RunGsdVerifyCliDeps} [deps]
     * @returns {Promise<number>} exit code
     */
    export async function runGsdVerifyCli(opts, deps = {}) {
      const write = deps.writeFn || ((s) => process.stdout.write(s));
      const err = deps.errFn || ((s) => process.stderr.write(s));
      const runVerifyFn = deps.runVerifyFn || runGsdVerify;

      let result;
      try {
        result = await runVerifyFn({ sessionId: opts.sessionId });
      } catch (e) {
        const msg = /** @type {Error} */ (e).message || String(e);
        err(`Error verifying session ${opts.sessionId}: ${msg}\n`);
        // Distinguir transient vs internal.
        if (TRANSIENT_PATTERNS.test(msg)) return 2;
        return 1;
      }

      if (opts.json) {
        write(JSON.stringify(result, null, 2) + '\n');
      } else {
        renderHuman(result, write);
      }
      return 0;
    }

    /** @private */
    function renderHuman(result, write) {
      const { verdict, plane, session } = result;
      write(`Session:      ${session.session_id}\n`);
      write(`Task:         ${session.task_ref}\n\n`);
      write('Verdict:\n');
      switch (verdict.action) {
        case 'pass':
          write(`  action:      pass\n`);
          write(`  phase_id:    ${verdict.phase_id}\n`);
          write(`  must_haves:  ${verdict.must_haves}\n`);
          break;
        case 'fail':
          write(`  action:      fail\n`);
          write(`  phase_id:    ${verdict.phase_id}\n`);
          write(`  reason:      ${verdict.reason}\n`);
          write(`  detail:      ${verdict.detail}\n`);
          break;
        case 'missing':
          write(`  action:      missing\n`);
          write(`  phase_id:    ${verdict.phase_id}\n`);
          break;
        case 'malformed':
          write(`  action:      malformed\n`);
          write(`  phase_id:    ${verdict.phase_id}\n`);
          write(`  detail:      ${verdict.detail}\n`);
          break;
      }
      write('\n');
      write(`Plane: commented=${plane.commented} transitioned=${plane.transitioned}\n`);
    }
    ```

    Crear `test/gsd-verify-cli-handler.test.js`:

    ```javascript
    // @ts-check
    import { describe, it } from 'node:test';
    import assert from 'node:assert/strict';
    import { runGsdVerifyCli } from '../src/cli/gsd-verify.js';

    function makeStdoutStub() {
      let buf = '';
      return { write: (s) => { buf += s; }, get: () => buf };
    }

    describe('runGsdVerifyCli — exit codes (Pitfall #6, Opción A)', () => {
      it('C1: verdict pass → exit 0', async () => {
        const stdout = makeStdoutStub();
        const code = await runGsdVerifyCli(
          { sessionId: 's1' },
          {
            runVerifyFn: async () => ({
              verdict: { action: 'pass', phase_id: '10', must_haves: 8 },
              plane: { commented: true, transitioned: true },
              session: { session_id: 's1', task_ref: 'KL-42', phase_id: '10' },
            }),
            writeFn: stdout.write,
            errFn: () => {},
          },
        );
        assert.equal(code, 0);
        assert.match(stdout.get(), /action:\s+pass/);
        assert.match(stdout.get(), /must_haves:\s+8/);
        assert.match(stdout.get(), /commented=true transitioned=true/);
      });

      it('C2: verdict fail → exit 0 (gate corrió)', async () => {
        const stdout = makeStdoutStub();
        const code = await runGsdVerifyCli(
          { sessionId: 's1' },
          {
            runVerifyFn: async () => ({
              verdict: { action: 'fail', phase_id: '10', reason: 'gaps-found', detail: 'gaps_count=2' },
              plane: { commented: true, transitioned: false },
              session: { session_id: 's1', task_ref: 'KL-42', phase_id: '10' },
            }),
            writeFn: stdout.write,
            errFn: () => {},
          },
        );
        assert.equal(code, 0);
        assert.match(stdout.get(), /action:\s+fail/);
        assert.match(stdout.get(), /reason:\s+gaps-found/);
        assert.match(stdout.get(), /detail:\s+gaps_count=2/);
        assert.match(stdout.get(), /transitioned=false/);
      });

      it('C3: verdict missing → exit 0', async () => { /* similar, action missing */ });
      it('C4: verdict malformed → exit 0', async () => { /* similar, action malformed */ });

      it('C5: error "session not found" → exit 1', async () => {
        const stderr = makeStdoutStub();
        const code = await runGsdVerifyCli(
          { sessionId: 'nope' },
          {
            runVerifyFn: async () => { throw new Error('session not found: nope'); },
            writeFn: () => {},
            errFn: stderr.write,
          },
        );
        assert.equal(code, 1);
        assert.match(stderr.get(), /session not found/);
      });

      it('C6: error "is not GSD" → exit 1', async () => { /* ... */ });

      it('C7: error "provider fetch failed" → exit 2 (transient)', async () => {
        const stderr = makeStdoutStub();
        const code = await runGsdVerifyCli(
          { sessionId: 's1' },
          {
            runVerifyFn: async () => { throw new Error('provider fetch failed: ECONNREFUSED'); },
            writeFn: () => {},
            errFn: stderr.write,
          },
        );
        assert.equal(code, 2);
        assert.match(stderr.get(), /provider fetch failed/);
      });
    });

    describe('runGsdVerifyCli — output format', () => {
      it('C8: --json emite JSON parseable', async () => {
        const stdout = makeStdoutStub();
        await runGsdVerifyCli(
          { sessionId: 's1', json: true },
          {
            runVerifyFn: async () => ({
              verdict: { action: 'pass', phase_id: '10', must_haves: 8 },
              plane: { commented: true, transitioned: true },
              session: { session_id: 's1', task_ref: 'KL-42', phase_id: '10' },
            }),
            writeFn: stdout.write,
            errFn: () => {},
          },
        );
        const parsed = JSON.parse(stdout.get());
        assert.equal(parsed.verdict.action, 'pass');
        assert.equal(parsed.plane.commented, true);
        assert.equal(parsed.session.session_id, 's1');
      });

      it('C9: sin --json NO es JSON parseable', async () => {
        const stdout = makeStdoutStub();
        await runGsdVerifyCli(
          { sessionId: 's1' },
          {
            runVerifyFn: async () => ({
              verdict: { action: 'pass', phase_id: '10', must_haves: 8 },
              plane: { commented: true, transitioned: true },
              session: { session_id: 's1', task_ref: 'KL-42', phase_id: '10' },
            }),
            writeFn: stdout.write,
            errFn: () => {},
          },
        );
        assert.throws(() => JSON.parse(stdout.get()));
        assert.match(stdout.get(), /Verdict:/);
      });
    });

    describe('runGsdVerifyCli — renderHuman switch exhaustive', () => {
      it('C10.pass: campos action/phase_id/must_haves', async () => { /* ya cubierto por C1 */ });
      it('C10.fail: campos action/phase_id/reason/detail', async () => { /* ya cubierto por C2 */ });
      it('C10.missing: campos action/phase_id (sin detail)', async () => { /* ... */ });
      it('C10.malformed: campos action/phase_id/detail', async () => { /* ... */ });
    });
    ```

    Total: 10+ tests.

    **IMPORTANTE — Nota sobre idempotencia (Pitfall #7):** el comentario del archivo gsd-verify.js ya documenta que kodo NO deduplica comentarios Plane. Esto es intencional y debe aparecer literal en el JSDoc/header para que un reviewer no añada deduplicación "defensiva".
  </action>
  <verify>
    <automated>node --test test/gsd-verify-cli-handler.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "export async function runGsdVerifyCli" src/cli/gsd-verify.js` → match
    - `grep -qE "TRANSIENT_PATTERNS|ECONNREFUSED" src/cli/gsd-verify.js` → match (distinción exit 1 vs 2)
    - `grep -qF "case 'pass':" src/cli/gsd-verify.js` → match (switch exhaustivo)
    - `grep -qF "case 'malformed':" src/cli/gsd-verify.js` → match (las 4 ramas del Verdict)
    - `grep -qF "Pitfall #6" src/cli/gsd-verify.js || grep -qF "Opción A" src/cli/gsd-verify.js` → match (documentación en header)
    - `grep -qF "Pitfall #7" src/cli/gsd-verify.js || grep -qF "Idempotencia" src/cli/gsd-verify.js` → match (doc idempotencia)
    - `grep -c "it(" test/gsd-verify-cli-handler.test.js` ≥ 10
    - `grep -qF "assert.equal(code, 1)" test/gsd-verify-cli-handler.test.js` → match
    - `grep -qF "assert.equal(code, 2)" test/gsd-verify-cli-handler.test.js` → match
    - `grep -qF "JSON.parse(stdout" test/gsd-verify-cli-handler.test.js` → match (C8)
    - `node --test test/gsd-verify-cli-handler.test.js` exits 0
  </acceptance_criteria>
  <done>
    `src/cli/gsd-verify.js` exporta `runGsdVerifyCli`. Exit codes 0/1/2 según Opción A. --json emite JSON parseable. renderHuman tiene switch exhaustivo sobre los 4 verdict actions. Tests cubren 10+ casos incluyendo el error path transient→exit 2. Documentación explícita de Pitfalls #6 y #7 en el header.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar subcomando en src/cli.js</name>
<files>src/cli.js, test/gsd-verify-cli-handler.test.js</files>
  <read_first>
    - src/cli.js (líneas 241-260 — bloque actual de `gsd inspect` y `program.parse()`)
    - .planning/phases/10-orchestrator-verification-gate/10-PATTERNS.md §4 (pattern de registro en cli.js)
  </read_first>
  <behavior>
    Tests estáticos (lectura del archivo, no spawnSync):
    - CLI1: `src/cli.js` contiene el string literal `.command('verify <session-id>')`.
    - CLI2: `src/cli.js` contiene el string literal `import('./cli/gsd-verify.js')`.
    - CLI3: `src/cli.js` contiene la string `runGsdVerifyCli`.
    - CLI4: la descripción del comando incluye una mención explícita de idempotencia (`idempotent` o `duplicates accepted`).

    Drop spawnSync (evita flakiness en CI). Los tests del handler (Task 1) ya cubren el comportamiento; los tests estáticos aquí confirman el wiring.
  </behavior>
  <action>
    En `src/cli.js`, localizar el bloque:

    ```javascript
    gsd
      .command('inspect <task-id>')
      .description('Dry-run the phase resolver for a task (read-only, no lock/state/cmux)')
      .option('--json', 'Emit structured verdict as JSON (scriptable)')
      .action(async (taskId, opts) => {
        try {
          await ensureConfig();
          const { runGsdInspect } = await import('./cli/gsd-inspect.js');
          const code = await runGsdInspect({ taskId, json: opts.json || false });
          process.exit(code);
        } catch (err) {
          console.error(`Error: ${err.message}`);
          process.exit(1);
        }
      });
    ```

    Insertar INMEDIATAMENTE DESPUÉS (antes de `program.parse()`):

    ```javascript
    gsd
      .command('verify <session-id>')
      .description('Verify phase closure: parses VERIFICATION.md, posts verdict comment and transitions task to Review on pass (idempotent — duplicates accepted, CONTEXT Deferred)')
      .option('--json', 'Emit structured verdict as JSON (scriptable)')
      .action(async (sessionId, opts) => {
        try {
          await ensureConfig();
          const { runGsdVerifyCli } = await import('./cli/gsd-verify.js');
          const code = await runGsdVerifyCli({ sessionId, json: opts.json || false });
          process.exit(code);
        } catch (err) {
          console.error(`Error: ${err.message}`);
          process.exit(1);
        }
      });
    ```

    **Pitfalls a evitar:**
    - NO mover `program.parse()` — debe quedar al final.
    - NO cambiar la descripción del grupo `gsd` (línea 242) — ya dice "GSD subcommands (inspect resolver, etc.)" que es suficientemente genérico.
    - Mantener el nombre del import `runGsdVerifyCli` (NO `runGsdVerify` — ese es el de verify.js, el handler CLI es `runGsdVerifyCli` para evitar colisión).

    **Test estático** (APPEND al archivo `test/gsd-verify-cli-handler.test.js` creado en Task 1, evitando spawnSync por fragilidad en CI):

    ```javascript
    import { readFileSync } from 'node:fs';

    describe('src/cli.js — gsd verify subcommand registration (static)', () => {
      const cli = readFileSync('src/cli.js', 'utf-8');

      it('CLI1: registra .command("verify <session-id>")', () => {
        assert.ok(cli.includes(".command('verify <session-id>')"),
          'expected literal .command(\'verify <session-id>\')');
      });

      it('CLI2: importa dinámicamente ./cli/gsd-verify.js', () => {
        assert.ok(cli.includes("import('./cli/gsd-verify.js')"),
          'expected literal import(\'./cli/gsd-verify.js\')');
      });

      it('CLI3: invoca runGsdVerifyCli', () => {
        assert.ok(cli.includes('runGsdVerifyCli'));
      });

      it('CLI4: descripción documenta idempotencia', () => {
        assert.ok(/idempotent|duplicates accepted/i.test(cli),
          'descripción del comando debe mencionar idempotencia');
      });
    });
    ```
  </action>
  <verify>
    <automated>node --test test/gsd-verify-cli-handler.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF ".command('verify <session-id>')" src/cli.js` → match (literal, fixed-string)
    - `grep -qF "import('./cli/gsd-verify.js')" src/cli.js` → match (literal, fixed-string — evita interpretación de paréntesis y `.` como regex)
    - `grep -qF "runGsdVerifyCli" src/cli.js` → match
    - `grep -qE "idempotent|duplicates accepted" src/cli.js` → match (doc Pitfall #7 en la descripción)
    - `grep -c "program.parse" src/cli.js` = 1 (no se duplicó la llamada)
    - `node --test test/gsd-verify-cli-handler.test.js` exits 0 (incluye los 4 tests estáticos CLI1..CLI4)
  </acceptance_criteria>
  <done>
    `src/cli.js` registra `kodo gsd verify <session-id>` con flag `--json`. El comando invoca `runGsdVerifyCli` del handler. Descripción del comando documenta idempotencia (Pitfall #7). Tests estáticos CLI1..CLI4 pasan. NO se usa spawnSync (decisión explícita: evitar flakiness CI; el handler unit test + lectura estática del archivo son suficientes para el wiring).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv (session-id) → runGsdVerify | Usuario/orquestador pasa session-id; validado downstream en runGsdVerify (findSession). |
| error.message → exit code decision | Matching con regex en texto del error puede ser engañado por un atacante que controle los mensajes, pero los mensajes los genera kodo mismo — no input externo. |
| stdout/stderr → consumidor (pipe) | Si el operador pipea a jq o grep, el JSON de --json debe ser válido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | session-id argv | accept | Downstream validation en runGsdVerify. Un session-id inexistente retorna exit 1, no corrompe estado. |
| T-10-03-02 | Tampering | TRANSIENT_PATTERNS regex bypass | accept | Si un error interno matchea accidentalmente el regex transient (ej. stack trace con "network" en path), el CLI retorna exit 2 cuando debería ser 1. Accept: el operador puede distinguir via stderr message; no hay escalación. |
| T-10-03-03 | Injection | JSON.stringify del result | mitigate | `JSON.stringify(result, null, 2)` sanitiza automáticamente (escape de `"`, `\n`). No hay inyección de JSON. |
| T-10-03-04 | Info Disclosure | stderr con error.message | accept | Los errores de runGsdVerify son "session not found", "is not GSD", "provider fetch failed". No contienen secretos. |
| T-10-03-05 | DoS | sin rate-limit en CLI | accept | CLI invocado por el orquestador o humano; no hay endpoint remoto. Accept. |
</threat_model>

<verification>
- `node --test test/gsd-verify-cli-handler.test.js` exits 0 con 14+ tests ok (10 del handler + 4 estáticos CLI1..CLI4).
- grep estático confirma registro del subcomando y doc de idempotencia.
- grep confirma doc de Pitfalls #6 y #7 en header del handler.
</verification>

<success_criteria>
- [ ] `src/cli/gsd-verify.js` creado exportando `runGsdVerifyCli`.
- [ ] Exit codes 0/1/2 implementados con regex de transient detection.
- [ ] `src/cli.js` registra `gsd verify <session-id>` con `--json`.
- [ ] `test/gsd-verify-cli-handler.test.js` con 14+ tests pasando (10 handler + 4 estáticos CLI1..CLI4).
- [ ] Descripción del comando en cli.js menciona idempotencia.
- [ ] Documentación de Pitfalls #6 (exit codes) y #7 (idempotencia) en el header del archivo handler.
</success_criteria>

<output>
After completion, create `.planning/phases/10-orchestrator-verification-gate/10-03-SUMMARY.md`
</output>
</content>
</invoke>