---
phase: 46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/plan.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-plan.test.js
  - test/dashboard-overlay.test.js
autonomous: true
requirements: [PLAN-04]

must_haves:
  truths:
    - "Con una sesión quick/non-GSD seleccionada (sin phase_id) y artefacto presente, pulsar `p` muestra el plan ligero de ~/.kodo/plans/<task_id>.md con la misma UX que el overlay GSD (fallback dentro de readPlan, D-01; correlación por task_id, D-03)"
    - "Una sesión quick/non-GSD sin artefacto escrito muestra copy honesta distinta ('session has not written a plan yet'), dim, no roja (status/copy nuevo, D-04; mapeo ENOENT→no-light-plan, D-05)"
    - "Una fila sin phase_id Y sin task_id sigue mostrando OVERLAY_PLAN_NO_PHASE (caso terminal preservado, D-06)"
    - "Un artefacto ilegible (EACCES/no-ENOENT) muestra OVERLAY_PLAN_ERROR (rojo); ningún error de fichero crashea el panel (never-throws + anti-ReDoS, D-09; mapeo non-ENOENT→error, D-05)"
    - "Las filas GSD con phase_id siguen leyendo su PLAN.md exactamente igual; el fallback solo dispara en la rama phaseId==null (cero regresión en la rama GSD, D-02)"
    - "Cero endpoints nuevos en src/server.js; el overlay sigue read-only"
  artifacts:
    - path: "src/cli/dashboard/plan.js"
      provides: "Helper privado readLightPlan + rama de fallback en el punto no-phase; import homedir de node:os sin importar config.js (D-07)"
      contains: "readLightPlan"
    - path: "src/cli/dashboard/App.js"
      provides: "Constante de copy exportada OVERLAY_PLAN_NO_LIGHT"
      contains: "OVERLAY_PLAN_NO_LIGHT = 'session has not written a plan yet'"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "Rama de render dim para status no-light-plan"
      contains: "OVERLAY_PLAN_NO_LIGHT"
    - path: "test/dashboard-plan.test.js"
      provides: "Tests DI puros de los 4 outcomes del fallback + never-throws + guard de contención; override de HOME por deps (kodoPlansDir/homedirFn) (D-08)"
      contains: "no-light-plan"
  key_links:
    - from: "src/cli/dashboard/plan.js (readPlan, rama phaseId == null)"
      to: "readLightPlan(row.task_id, deps)"
      via: "llamada cuando row.task_id es truthy"
      pattern: "readLightPlan\\("
    - from: "src/cli/dashboard/plan.js (readLightPlan)"
      to: "~/.kodo/plans/<task_id>.md"
      via: "join(homedir(), '.kodo', 'plans', `${taskId}.md`) + deps.readFileFn"
      pattern: "'plans'"
    - from: "src/cli/dashboard/SessionTable.js (renderOverlay)"
      to: "OVERLAY_PLAN_NO_LIGHT (importado de App.js)"
      via: "rama status === 'no-light-plan'"
      pattern: "no-light-plan"
---

<objective>
Añadir un **fallback read-only de plan ligero** dentro de `readPlan` (`src/cli/dashboard/plan.js`): cuando una fila del dashboard NO resuelve a una fase GSD (`phaseId == null`) pero lleva un `task_id`, leer el artefacto de plan ligero de Phase 45 (`~/.kodo/plans/<task_id>.md`) y mostrarlo en el overlay `p` con la misma UX que el overlay GSD. El caso sin artefacto gana una copy honesta nueva y distinta; el caso sin task_id sigue siendo `no-phase` terminal.

Purpose: Cerrar el lazo de v0.11 "Ventana al plan" — el operador ve el plan de CUALQUIER sesión (GSD full/bootstrap vía PLAN.md de fase, quick/non-GSD vía el artefacto ligero) sin salir de la TUI, con UX indistinguible.

Output:
- `readLightPlan` helper privado en `plan.js` (leaf-only, never-throws, anti-ReDoS) + rama de fallback que estrecha el `no-phase`.
- Constante `OVERLAY_PLAN_NO_LIGHT = 'session has not written a plan yet'` exportada en `App.js`, rama de render dim en `SessionTable.js`.
- Tests DI puros de los 4 outcomes (ok / no-light-plan / error / no-phase) + never-throws + guard de contención; fix de la regresión confirmada del test de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-CONTEXT.md
@.planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-RESEARCH.md
@.planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-PATTERNS.md
@.planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-UI-SPEC.md

<interfaces>
<!-- Contratos clave ya en el codebase. El executor los usa directamente — sin exploración. -->

# src/cli/dashboard/plan.js — contrato actual (Phase 44)
# export function readPlan(row, deps = {}) → { status, lines }
#   status ∈ 'ok' | 'no-phase' | 'no-plan' | 'error'
#   DI: deps.readdirFn, deps.readFileFn, deps.existsFn, deps.resolvePhaseFn (sin default)
#   readFileFn default: (p) => readFileSync(p, 'utf-8')
#   Imports actuales (línea 30-31): readdirSync, readFileSync, existsSync de 'node:fs'; join de 'node:path'
#   Punto de inserción del fallback (línea 69): if (phaseId == null) return { status: 'no-phase', lines: [] };
#   Guard de contención WR-01 ya usado (líneas 98, 109): !e.includes('/') && !e.includes('\\') && !e.includes('..')

# src/config.js:6 — convención de ruta a REPLICAR (NO importar config.js):
#   const KODO_DIR = join(homedir(), '.kodo');   // homedir de 'node:os'; sin env recompute

# src/hooks/session-start.js:85,145 — PRODUCTOR (Phase 45, shipped). Ruta byte-idéntica a leer:
#   join(KODO_DIR, 'plans', `${session.task_id}.md`)   donde KODO_DIR = join(homedir(), '.kodo')

# src/cli/dashboard/App.js:110-112 — bloque de constantes de copy existente (añadir la nueva aquí):
#   export const OVERLAY_PLAN_NO_PHASE = 'not a GSD session / no phase resolved';
#   export const OVERLAY_PLAN_NO_PLAN = 'phase has no PLAN.md yet';
#   export const OVERLAY_PLAN_ERROR = 'error reading plan';
# App.js:482-502 — handler `p`. readPlan(row, { resolvePhaseFn: resolvePhase }) SÍNCRONO. NO cambia.

# src/cli/dashboard/SessionTable.js:34-36 — import block de las OVERLAY_PLAN_* (añadir la nueva).
# SessionTable.js:153-173 — render switch COMPARTIDO (plan/logs/comments). Rama dim sin `color`
#   (no-phase/no-plan) vs rama roja `color='red'` (error). El nuevo no-light-plan va junto a las dim.
#   Mezcla dim-vs-rojo en línea 174: dimColor: color ? undefined : true.

# src/session/state.js:15 — task_id es "UUID del task en el provider activo" (sin separadores de path).

# test/dashboard-plan.test.js:34-52 — makeFs DI builder (readdirFn/readFileFn lanzan err.code='ENOENT').
# test/dashboard-plan.test.js:261-267 — test estructural: plan.js NO debe contener `new RegExp`.
# test/dashboard-overlay.test.js:443-472 — fixture planStatus({}) HARDCODEA task_id: 'a' (línea 448).
#   Test :499 usa planStatus({}) y asserta OVERLAY_PLAN_NO_PHASE → REGRESIÓN tras el fallback.
#   Patrón de aislamiento ya en el fichero: mkdtempSync + writeFileSync (líneas 476, 480).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fallback readLightPlan en plan.js (helper leaf-safe, never-throws, anti-ReDoS) + tests DI puros</name>
  <files>src/cli/dashboard/plan.js, test/dashboard-plan.test.js</files>
  <read_first>
    - src/cli/dashboard/plan.js (TODO el fichero — header de invariantes líneas 1-28, DI línea 47-51, punto de inserción línea 69, guard de contención líneas 97-99/107-109)
    - src/config.js líneas 1-9 (convención homedir() + KODO_DIR — replicar, NO importar)
    - src/hooks/session-start.js líneas 85 y 145 (productor: ruta exacta del artefacto a leer)
    - src/session/state.js línea 15 (forma del task_id: UUID provider, sin separadores)
    - test/dashboard-plan.test.js líneas 1-70 (makeFs DI builder, estructura describe) y 261-267 (test estructural anti-ReDoS)
    - .planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-RESEARCH.md líneas 154-180 y 283-329 (Pattern 1 helper + los 5 casos de test ya redactados)
    - .planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-PATTERNS.md líneas 23-95 (Templates A-D + guard de contención)
  </read_first>
  <action>
    En `src/cli/dashboard/plan.js`:
    1. Añadir al bloque de imports (tras línea 31) `import { homedir } from 'node:os';` — builtin, preserva la leaf-isolation (D-07). NO importar `src/config.js`.
    2. Crear un helper privado (no exportado) `readLightPlan(taskId, deps)` que devuelve `{ status, lines }`. Resuelve el dir de plans con override DI: `const plansDir = deps.kodoPlansDir || join((deps.homedirFn || homedir)(), '.kodo', 'plans');` (D-08, default `homedir()` real). Construye la ruta `join(plansDir, taskId + '.md')` (ruta CONSTRUIDA, no derivada por regex — D-09). Lee con `deps.readFileFn || ((p) => readFileSync(p, 'utf-8'))` envuelto en su PROPIO try/catch. Mapeo D-05: contenido → `{ status: 'ok', lines: md.split('\n') }`; `err.code === 'ENOENT'` → `{ status: 'no-light-plan', lines: [] }`; cualquier otro (incluido error sin `.code`) → `{ status: 'error', lines: [] }`. Espejo estructural exacto del try/catch ENOENT-vs-error de `plan.js:86-92`.
    3. Guard de contención D-09 ANTES de leer: si `taskId.includes('/') || taskId.includes('\\') || taskId.includes('..')` → tratar como task_id no utilizable (mismo trato que falsy, ver paso 4). Usar `String.includes`, NUNCA `new RegExp` (el test estructural :261-267 lo vigila). Aunque task_id es UUID provider sin separadores, el guard hace literalmente cierta la afirmación "fixed root" del threat model.
    4. Reemplazar la línea 69 `if (phaseId == null) return { status: 'no-phase', lines: [] };` por la forma estrechada (Template D): cuando `phaseId == null`, leer `const taskId = row?.task_id;` — si `taskId` es truthy Y pasa el guard de contención, `return readLightPlan(taskId, deps);`; en otro caso `return { status: 'no-phase', lines: [] };` (terminal, D-06). Decidir si el guard vive dentro de `readLightPlan` (degradando a no-phase) o en el call-site — colocarlo donde mantenga un solo punto de decisión; documentar la elección con comentario que cite D-06/D-09.
    5. Actualizar el typedef `PlanResult` (línea 34) y el header de contrato (líneas 11-15) para incluir el nuevo status `'no-light-plan'` y documentar el fallback (qué dispara, mapeo D-05, leaf-isolation preservada). No tocar la rama GSD existente (no-plan/error/ok del flujo de fase).

    En `test/dashboard-plan.test.js`, añadir un `describe('readPlan — fallback plan ligero (D-05/D-08/D-09)')` con estos casos (DI puro, sin disco real, usando `kodoPlansDir` override — patrón RESEARCH:286-328):
    - task_id + artefacto presente → `ok`, `res.lines` incluye el contenido.
    - task_id + ENOENT (readFileFn lanza `err.code='ENOENT'`) → `no-light-plan`.
    - task_id + EACCES (readFileFn lanza `err.code='EACCES'`) → `error`.
    - sin phase_id Y sin task_id (`readPlan({}, { kodoPlansDir })`) → `no-phase` (D-06 terminal).
    - readFileFn lanza un Error plano sin `.code` → `error` AND `assert.doesNotThrow` (never-throws, D-09).
    - task_id de path-traversal (`'../../etc/passwd'`) → `no-phase` (o `no-light-plan` si el guard vive dentro del helper), y `readFileFn` recibe una ruta que NUNCA escapa de plansDir (asertar que no se lee fuera, p. ej. que `readFileFn` no se invoca o se invoca con una ruta contenida).
    El test estructural existente (:261-267, `plan.js` sin `new RegExp`) debe seguir verde — confirmar que el guard usa `String.includes`.
  </action>
  <verify>
    <automated>node --test test/dashboard-plan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "function readLightPlan" src/cli/dashboard/plan.js` devuelve una coincidencia (helper privado, no `export`).
    - `git grep -n "from 'node:os'" src/cli/dashboard/plan.js` devuelve una coincidencia (homedir importado).
    - `git grep -n "from './config.js'\|from '../../config.js'" src/cli/dashboard/plan.js` NO devuelve nada (config.js NO importado — leaf preservado).
    - `git grep -n "new RegExp" src/cli/dashboard/plan.js` NO devuelve nada (anti-ReDoS; test estructural :261-267 verde).
    - `git grep -n "no-light-plan" src/cli/dashboard/plan.js` devuelve coincidencias (mapeo ENOENT).
    - `node --test test/dashboard-plan.test.js` exit 0 con los 6 casos nuevos del fallback presentes (ok / no-light-plan / error / no-phase / never-throws / contención) Y todos los tests GSD existentes verdes (no regresión).
  </acceptance_criteria>
  <done>readLightPlan implementado leaf-safe/never-throws/anti-ReDoS, integrado en la rama no-phase de readPlan estrechándola (D-02/D-03/D-06); los 4 outcomes (ok/no-light-plan/error/no-phase) + never-throws + contención cubiertos por tests DI puros verdes; rama GSD intacta.</done>
</task>

<task type="auto">
  <name>Task 2: Copy constant OVERLAY_PLAN_NO_LIGHT (App.js) + rama de render dim (SessionTable.js)</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/App.js líneas 100-130 (bloque OVERLAY_PLAN_* existente, comentario Phase 44 D-07) y líneas 480-502 (handler `p` — NO cambia, confirmar forma síncrona)
    - src/cli/dashboard/SessionTable.js líneas 28-40 (import block de App.js) y líneas 148-181 (render switch compartido plan/logs/comments: ramas dim sin color vs rama roja error)
    - .planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-UI-SPEC.md (Copywriting Contract: literal locked + tabla de 4 estados; Color: no-light-plan es dim NO rojo)
    - .planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-PATTERNS.md líneas 99-142 (Templates de constante + rama de render)
  </read_first>
  <action>
    En `src/cli/dashboard/App.js`, junto al bloque `OVERLAY_PLAN_*` (tras línea 112), añadir la constante exportada `export const OVERLAY_PLAN_NO_LIGHT = 'session has not written a plan yet';` (literal locked en UI-SPEC Copywriting Contract, D-04). Extender el comentario del bloque (Phase 44 D-07 honest-copy) para documentar el cuarto caso: sesión quick/non-GSD con artefacto ausente (ENOENT), informativo (dim), distinto de NO_PHASE/NO_PLAN/ERROR, legible bajo NO_COLOR. NO tocar el handler `p` (D-01 — sigue `readPlan(row, { resolvePhaseFn })` síncrono + `setOverlaySnapshot`).

    En `src/cli/dashboard/SessionTable.js`:
    1. Añadir `OVERLAY_PLAN_NO_LIGHT` al import block de `./App.js` (junto a OVERLAY_PLAN_NO_PHASE/NO_PLAN/ERROR, líneas 34-36) — mata el drift code/render (la literal vive una sola vez en App.js).
    2. En el render switch (junto a las ramas `no-phase`/`no-plan`, ~línea 158), añadir `else if (snap.status === 'no-light-plan') { copy = OVERLAY_PLAN_NO_LIGHT; }` — SIN asignar `color` (cae a dim vía `dimColor: color ? undefined : true` en línea 174). NO seguir la rama de `error` (que asigna `color = 'red'`): no-light-plan es informativo, no un fallo. Cuidado: el switch es COMPARTIDO entre plan/logs/comments — la rama nueva solo aplica al status `no-light-plan` que solo produce el fallback de plan, así que es seguro añadirla en la cadena común.
  </action>
  <verify>
    <automated>node --test test/dashboard-overlay.test.js test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "OVERLAY_PLAN_NO_LIGHT = 'session has not written a plan yet'" src/cli/dashboard/App.js` devuelve exactamente una coincidencia (constante exportada con el literal locked).
    - `git grep -n "OVERLAY_PLAN_NO_LIGHT" src/cli/dashboard/SessionTable.js` devuelve coincidencias en el import Y en la rama de render (sin literal inline duplicada).
    - `git grep -n "no-light-plan" src/cli/dashboard/SessionTable.js` devuelve una coincidencia (la rama de render).
    - La rama `no-light-plan` en SessionTable.js NO asigna `color = 'red'` (verificar por lectura: cae a dim).
    - `git diff --stat -- src/cli/dashboard/App.js` muestra solo adiciones en el bloque de constantes (handler `p` sin cambios).
    - `node --test test/dashboard-overlay.test.js test/format-isolation.test.js` exit 0 (color-isolation: cero picocolors en dashboard preservado).
  </acceptance_criteria>
  <done>OVERLAY_PLAN_NO_LIGHT exportada en App.js con el literal locked, importada y renderizada dim en SessionTable.js junto a no-phase/no-plan; handler `p` intacto; color-isolation verde.</done>
</task>

<task type="auto">
  <name>Task 3: Fix de la regresión confirmada + test de integración del plan ligero (HOME aislado)</name>
  <files>test/dashboard-overlay.test.js</files>
  <read_first>
    - test/dashboard-overlay.test.js líneas 1-50 (imports: mkdtempSync/writeFileSync/rmSync, OVERLAY_PLAN_* importados) y líneas 440-545 (fixture planStatus con task_id:'a' hardcoded línea 448; tests `p` :474-545)
    - .planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-PATTERNS.md líneas 172-184 (Pitfall 3 CONFIRMADO + Option A/B + HOME isolation)
    - .planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-RESEARCH.md líneas 258-262 (Pitfall 3) y 355-358 (Open Q2 resuelta)
    - src/cli/dashboard/App.js líneas 493-497 (handler `p`: readPlan SIN seam kodoPlansDir → integración usa HOME real)
  </read_first>
  <action>
    Problema CONFIRMADO (no hipotético): `planStatus({})` (test/dashboard-overlay.test.js:448) hardcodea `task_id: 'a'`. El test `:499` ('p sobre fila sin phase_id ... → OVERLAY_PLAN_NO_PHASE') renderiza esa fila a través del handler `p` real, que llama `readPlan(row, { resolvePhaseFn })` SIN override `kodoPlansDir`. Tras Task 1, esa fila (con task_id:'a', sin phase_id) disparará `readLightPlan`, leerá el HOME real `~/.kodo/plans/a.md`, y — ausente el artefacto — producirá `no-light-plan`, rompiendo el assert `OVERLAY_PLAN_NO_PHASE`.

    Elegir e implementar **Option A** (preferida — preserva el caso `no-phase` puro sin depender del estado del HOME real): hacer que `planStatus` pueda emitir una fila SIN `task_id` (p. ej. añadir un parámetro `{ omitTaskId }` o un fixture variant), y ajustar el test `:499` para usar una fila sin `task_id` → el fallback NO dispara → sigue `no-phase`. Mantener el test que asserta `OVERLAY_PLAN_NO_PHASE` verde con esa fila pura. Documentar en comentario por qué la fila no lleva task_id (el fallback exige task_id; sin él, no-phase terminal D-06). Declarar explícitamente en el SUMMARY que se eligió Option A.
    (Si Option A resulta inviable por la forma de la fixture, caer a Option B: actualizar el assert de `:499` a esperar `OVERLAY_PLAN_NO_LIGHT` con HOME aislado a un mkdtemp vacío, Y añadir un test separado con fila sin task_id que produzca `no-phase` puro. Mantener AMBOS casos cubiertos en cualquier opción.)

    Añadir un test de integración NUEVO (Ink, frame assert): una fila con `task_id` cuyo artefacto EXISTE. Como el handler `p` no tiene seam `kodoPlansDir`, aislar el HOME vía el patrón ya en el fichero: `mkdtempSync` para un HOME fake, `process.env.HOME` apuntando a él (guardar/restaurar el valor original en try/finally), escribir `~/.kodo/plans/<task_id>.md` con `mkdirSync({recursive})` + `writeFileSync`, renderizar, pulsar `p`, asertar que el frame muestra `plan · <task_ref>` y el contenido del artefacto. Restaurar `process.env.HOME` y `rmSync` el tmp en finally. (Observaciones 21811/22683 confirman que `homedir()` respeta `process.env.HOME`.)

    Añadir (recomendado) un test de integración del estado vacío: fila con task_id, HOME aislado a un mkdtemp SIN el artefacto → frame muestra `OVERLAY_PLAN_NO_LIGHT`. Confirma el camino end-to-end del nuevo status a través del render real.
  </action>
  <verify>
    <automated>node --test test/dashboard-overlay.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El test `:499` (o su sucesor) que asserta `OVERLAY_PLAN_NO_PHASE` pasa con una fila SIN task_id (Option A) — `git grep -n "OVERLAY_PLAN_NO_PHASE" test/dashboard-overlay.test.js` sigue presente y verde.
    - Existe un test de integración nuevo que asserta el contenido del artefacto de plan ligero en el frame (`assert.match(frame, /<contenido escrito>/)`), con HOME aislado vía `process.env.HOME` + mkdtemp y restaurado en finally.
    - `git grep -n "process.env.HOME" test/dashboard-overlay.test.js` devuelve coincidencias dentro de los tests nuevos (aislamiento + restauración).
    - `git grep -n "OVERLAY_PLAN_NO_LIGHT" test/dashboard-overlay.test.js` devuelve una coincidencia (test del estado vacío end-to-end).
    - `node --test test/dashboard-overlay.test.js` exit 0 — el caso no-phase puro Y los casos nuevos del plan ligero verdes; ningún test toca un `~/.kodo/plans/` real no aislado.
    - `git diff --stat -- src/server.js` vacío (cero endpoints nuevos — el overlay sigue read-only).
  </acceptance_criteria>
  <done>La regresión confirmada está cerrada (Option A: fila sin task_id preserva no-phase puro); el plan ligero se verifica end-to-end vía Ink con HOME aislado (artefacto presente → contenido en frame; ausente → OVERLAY_PLAN_NO_LIGHT); src/server.js sin cambios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `row.task_id` (provider data) → ruta de filesystem | El `task_id` (UUID del provider, `state.js:15`) se interpola en `~/.kodo/plans/<task_id>.md`. Si llevase separadores de path escaparía del root fijo. |
| filesystem (`~/.kodo/plans/`) → render React | El contenido del artefacto y los errores de lectura cruzan a la capa de render; un throw o un fichero ilegible no debe crashear la TUI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering / Information Disclosure | `readLightPlan` path build (`plan.js`) | mitigate | Guard de contención `String.includes('/')||includes('\\')||includes('..')` sobre `task_id` ANTES de leer; falla → no-phase, jamás lee fuera del root fijo `~/.kodo/plans/`. Ruta CONSTRUIDA con `join(homedir(),...)`, no derivada por regex. Reusa la lección WR-01 de Phase 44 (hardening 23076). |
| T-46-02 | Denial of Service | `readLightPlan` regex matching | mitigate | Cero `new RegExp` derivado de input (la ruta se construye, no se matchea). Vigilado estructuralmente por `test/dashboard-plan.test.js:261-267` (plan.js sin `new RegExp`). |
| T-46-03 | Denial of Service | lectura del artefacto (`readFileFn`) | mitigate | never-throws: el `readFile` va en su propio try/catch; un fichero ilegible/huge degrada a status `error`/`no-light-plan`, jamás propaga a React (herencia Phase 44 D-05). Test `assert.doesNotThrow` con readFileFn que lanza. |
| T-46-04 | Information Disclosure | correlación task_id → artefacto | accept | Un fichero por `task_id` (UUID); sin amplificación. El operador ya tiene acceso al filesystem local (dashboard local, sin red). No PII cross-session más allá de lo que el operador ya puede leer. |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | Cero paquetes nuevos (RESEARCH Package Legitimacy Audit: solo builtins de Node — `node:os`, `node:fs`, `node:path`). No hay superficie de supply-chain en esta fase. |

**Nota de proporcionalidad:** la superficie es lectura de filesystem local de un path kodo-controlado, sin red, sin endpoints (`git diff -- src/server.js` queda vacío), read-only. No se añade auth, crypto ni access-control porque el modelo de confianza es "el operador en su propia máquina" (idéntico al overlay GSD de Phase 44 y a `focus.js`).
</threat_model>

<verification>
- `node --test test/dashboard-plan.test.js` — los 4 outcomes del fallback (ok/no-light-plan/error/no-phase) + never-throws + contención verdes; rama GSD intacta.
- `node --test test/dashboard-overlay.test.js` — no-phase puro preservado + plan ligero end-to-end (contenido en frame, estado vacío → OVERLAY_PLAN_NO_LIGHT) con HOME aislado.
- `node --test test/format-isolation.test.js` — color-isolation: cero picocolors bajo `src/cli/dashboard/` (el walker cubre plan.js).
- `node --test` (full suite) — verde antes de `/gsd:verify-work`.
- `git grep -n "from './config.js'\|from '../../config.js'" src/cli/dashboard/plan.js` vacío (leaf preservado).
- `git grep -n "new RegExp" src/cli/dashboard/plan.js` vacío (anti-ReDoS).
- `git diff --stat -- src/server.js` vacío (cero endpoints; read-only).
</verification>

<success_criteria>
1. Una sesión quick/non-GSD seleccionada con artefacto presente muestra, al pulsar `p`, el plan ligero de `~/.kodo/plans/<task_id>.md` con la misma UX que el overlay GSD (snapshot congelado, scroll, `Esc` preserva cursor por task_id) — verificado por el test de integración Ink.
2. Una sesión quick/non-GSD sin artefacto muestra `OVERLAY_PLAN_NO_LIGHT = 'session has not written a plan yet'` dim (no roja), distinta de NO_PHASE/NO_PLAN/ERROR y legible bajo NO_COLOR.
3. Una fila sin phase_id Y sin task_id sigue mostrando `OVERLAY_PLAN_NO_PHASE` (caso terminal D-06 preservado).
4. Un artefacto ilegible (EACCES/no-ENOENT) → `OVERLAY_PLAN_ERROR` (rojo); ningún error de fichero crashea el panel (never-throws verificado por `assert.doesNotThrow`).
5. La rama GSD (filas con phase_id) lee su PLAN.md exactamente igual — cero regresión (tests GSD existentes verdes).
6. Cero endpoints nuevos en `src/server.js`; `plan.js` sigue siendo leaf (no importa config.js) y anti-ReDoS (sin `new RegExp`); overlay read-only.
</success_criteria>

<output>
Create `.planning/phases/46-overlay-del-plan-ligero-para-sesiones-quick-non-gsd/46-01-SUMMARY.md` when done.

En el SUMMARY declarar explícitamente:
- Qué opción de fix de regresión se eligió (A o B) y por qué.
- El literal final de OVERLAY_PLAN_NO_LIGHT (y si difiere del recomendado, justificación dentro de D-04).
- Dónde vive el guard de contención (dentro de readLightPlan vs call-site) y el naming del override DI (kodoPlansDir).
</output>
