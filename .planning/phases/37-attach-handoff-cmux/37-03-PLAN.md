---
phase: 37-attach-handoff-cmux
plan: 03
type: execute
wave: 3
depends_on: [37-01, 37-02]
files_modified:
  - src/cli/dashboard/index.js
  - .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md
autonomous: false
requirements: [TUI-13, TUI-14]
must_haves:
  truths:
    - "El usuario ejecuta `kodo dashboard` en TTY, navega con ↑/↓ hasta una fila `alive===true` y pulsa Enter; el binario cmux es invocado vía `execFile` con args literales `['select-workspace', '--workspace', row.workspace_ref]` y la app cmux GUI cambia el foco al workspace target (TUI-13, validado por UAT escenario #1)"
    - "El dashboard sigue montado durante toda la invocación: `runDashboard` NO toggea alt-screen extra, NO instala ni remueve handlers de SIGINT/SIGTERM adicionales, NO desmonta la instancia ink (TUI-13 criterio #1 — cero handoff TTY)"
    - "NO-ALT-SCREEN-MUTATION: las únicas escrituras de `\\x1b[?1049h/l` en index.js están en líneas 107/127 (Phase 36 alt-screen toggle preservado intacto)"
    - "NO-SIGNAL-HANDLER-MUTATION: las únicas mutaciones de signal handlers en index.js están en líneas 118/128 (SIGTERM handler de Phase 34 D-10); cero `process.on('SIGINT', ...)` añadido, cero `process.removeListener` extra"
    - "`runDashboard` acepta `exec` en `deps` siguiendo el patrón D-DI de Phase 34 (junto a `stdout`/`stdin`/`url`); por default usa `(await import('node:child_process')).execFile` con el mismo patrón lazy del resto de módulos del dashboard"
    - "El binario cmux resuelto desde `loadConfig().cmux.binary` se inyecta a runFocus en la prop `onFocus` que recibe `App.js` (Plan 02)"
    - "`37-HUMAN-UAT.md` existe en `${phase_dir}/` con frontmatter espejo del Phase 36 UAT, los 2 escenarios obligatorios (Focus exitoso visible + Zombie reject) estructurados con setup/steps/expected/result/verified_via, y 2 escenarios bonus opcionales (ENOENT + exit code ≠ 0) (D-08)"
    - "`37-HUMAN-UAT.md` declara `blocking_for_phase_close: true` en el frontmatter — sin sign-off de los 2 obligatorios, `gsd-verify-work` debe bloquear el cierre de fase (D-08 literal)"
  artifacts:
    - path: "src/cli/dashboard/index.js"
      provides: "Extensión DI con `exec` + lazy import de runFocus + cmuxBin resuelto + onFocus prop pass a App"
    - path: ".planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md"
      provides: "UAT artifact bloqueante (D-08, criterio Success #4)"
      contains: "blocking_for_phase_close"
  key_links:
    - from: "src/cli/dashboard/index.js (runDashboard)"
      to: "src/cli/dashboard/focus.js (runFocus)"
      via: "lazy import + prop onFocus"
      pattern: "import.*focus\\.js|runFocus"
    - from: "src/cli/dashboard/index.js (createElement(App))"
      to: "src/cli/dashboard/App.js (props.onFocus)"
      via: "onFocus: async (ref) => runFocus({ exec: execImpl, ref, binary: cmuxBin })"
      pattern: "onFocus.*=>"
    - from: ".planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md"
      to: "gsd-verify-work blocking gate"
      via: "frontmatter blocking_for_phase_close + status: pending"
      pattern: "blocking_for_phase_close:\\s*true"
---

<objective>
Wirear `runDashboard` (`src/cli/dashboard/index.js`) con la inyección de `exec` siguiendo el patrón D-DI de Phase 34 (junto a `stdout`/`stdin`/`url`), resolver el binario cmux desde `loadConfig().cmux.binary` (mismo patrón que `src/cmux/client.js:5-7`), y pasar `onFocus={async (ref) => runFocus({exec, ref, binary})}` como prop a `<App />`. NO modificar el alt-screen toggle (líneas 107/127), NO añadir loop `while(true)`, NO instalar/remover handlers de SIGINT (la versión revisada post-C-01 NO requiere D-12 SIGINT noop), NO tocar el SIGTERM handler de Phase 34 D-10. Crear el artefacto `37-HUMAN-UAT.md` con 2 escenarios obligatorios (Focus exitoso visible + Zombie reject) y 2 bonus opcionales (ENOENT + exit code ≠ 0) (D-08) — bloqueante para el cierre de fase.

Purpose: Cerrar el loop end-to-end: la pieza testeable (runFocus, Plan 01) + la pieza interactiva (App+SessionTable, Plan 02) quedan cableadas al proceso real con DI. El UAT cierra el gap de verificación que tests automatizados no pueden cubrir: que la app cmux GUI cambia foco realmente cuando se invoca el verbo y que el dashboard kodo sigue corriendo intacto en su pane. Cubre TUI-13 (wiring del path completo: Enter → onFocus → runFocus → execFile → cmux GUI focus) y TUI-14 (verificación end-to-end del guard zombie + ENOENT + exit code ≠ 0 en escenarios reales).

Output: 1 archivo modificado (index.js — extension mínima, cero refactor de lifecycle), 1 archivo nuevo (37-HUMAN-UAT.md — artefacto bloqueante). Checkpoint final human-verify para que el operador ejecute el UAT en TTY real con cmux visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-attach-handoff-cmux/37-CONTEXT.md
@.planning/phases/37-attach-handoff-cmux/37-PATTERNS.md
@.planning/phases/37-attach-handoff-cmux/37-VALIDATION.md
@.planning/phases/37-attach-handoff-cmux/37-01-SUMMARY.md
@.planning/phases/37-attach-handoff-cmux/37-02-SUMMARY.md
@.planning/phases/36-tabla-viva-render-seleccion-filtros/36-HUMAN-UAT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From `src/cli/dashboard/index.js:78-79` — Current DI signature to extend with `exec`:
```js
export async function runDashboard(deps = {}) {
  const { stdout = process.stdout, stdin = process.stdin, url } = deps;
```

From `src/cli/dashboard/index.js:93-109` — Lazy imports + render site to extend (NO modificar líneas 107 alt-screen ON ni 127 alt-screen OFF — ambas preservadas):
```js
const { loadConfig } = await import('../../config.js');
const baseUrl = resolveBaseUrl({ url, loadConfig });

const { render } = await import('ink');
const { createElement } = await import('react');
const App = (await import('./App.js')).default;

stdout.write('\x1b[?1049h');  // PRESERVE — alt-screen ON

const app = render(createElement(App, { baseUrl }));
```

From `src/cli/dashboard/index.js:115-130` — Signal handlers + finally (NO MODIFICAR — preservar al pie de la letra):
```js
const onSigterm = () => {
  app.unmount();
};
process.once('SIGTERM', onSigterm);

try {
  await app.waitUntilExit();
} finally {
  stdout.write('\x1b[?1049l');  // PRESERVE — alt-screen OFF
  process.removeListener('SIGTERM', onSigterm);
  process.exitCode = 0;
}
```

From `src/config.js:32-58, 119-127` — `loadConfig` shape (cmux.binary lookup):
```js
const DEFAULT_CONFIG = {
  // ...
  cmux: {
    binary: '/Applications/cmux.app/Contents/Resources/bin/cmux',
  },
  // ...
};

export function loadConfig() {
  if (!existsSync(CONFIG_PATH)) return { ...DEFAULT_CONFIG };
  // ...
}
```

From `src/cli/dashboard/focus.js` (Plan 01 output) — runFocus signature:
```js
export async function runFocus({ exec, ref, binary, timeoutMs = 5_000 }):
  Promise<{ ok: true } | { ok: false, code: 'ENOENT' | 'NON_ZERO_EXIT' | 'SPAWN_ERROR', detail: any }>
```

From `.planning/phases/36-tabla-viva-render-seleccion-filtros/36-HUMAN-UAT.md` — Format analog (frontmatter + Current Test + Tests + Summary + Gaps):
```markdown
---
status: passed
phase: 36-tabla-viva-render-seleccion-filtros
source: [36-VERIFICATION.md]
started: 2026-05-28
updated: 2026-05-28
approved_by: human
approved_at: 2026-05-28
fixture: scripts/dev-dashboard-fixture.mjs
hot_patches_validated: [116cb1e (alt-screen), ca61733 (bold+gutter)]
---

## Current Test

[all passed — UAT closed]

## Tests

### 1. Layout visual de la tabla
expected: ...
result: passed
verified_via: ...

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

(none)
```

ADAPT for 37-HUMAN-UAT.md: same skeleton, `status: pending`, `approved_by: pending`, `blocking_for_phase_close: true`, `total: 2 obligatorios + 2 bonus opcionales`, gaps `(pending — UAT por ejecutar)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender runDashboard con DI de exec + lazy import de runFocus + onFocus prop pass</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - src/cli/dashboard/index.js COMPLETO (líneas 1-132: necesario para identificar TODOS los sitios y NO TOCAR el resto — alt-screen toggle, SIGTERM, try/finally, exitCode)
    - .planning/phases/37-attach-handoff-cmux/37-PATTERNS.md (líneas 97-161: §`src/cli/dashboard/index.js` con los 5 cambios literales + lista de NON-CHANGES dura)
    - .planning/phases/37-attach-handoff-cmux/37-CONTEXT.md (líneas 102-117: §Canonical References punto sobre index.js — "Cambio mínimo: añadir `exec` a `deps` … NO añadir loop while(true), NO tocar alt-screen toggle, NO instalar/remover handlers de señales")
    - src/cmux/client.js (líneas 1-26: patrón espejo para `loadConfig().cmux.binary` lookup — mismo idioma que aquí se replica)
    - .planning/phases/37-attach-handoff-cmux/37-01-SUMMARY.md (signature literal de runFocus exportada en Plan 01)
    - .planning/phases/37-attach-handoff-cmux/37-02-SUMMARY.md (prop signature exacta que App.js espera en `onFocus`)
  </read_first>
  <action>
    Editar `src/cli/dashboard/index.js` con 4 cambios precisos (Edit puntual, NO Write). Lista exhaustiva de NON-CHANGES al final.

    **Cambio 1 — Añadir `exec` al JSDoc + destructuring de deps (líneas 70-79):**

    Actualizar el JSDoc `@param`:
    ```js
    /**
     * Lanza el dashboard TUI de kodo.
     *
     * @param {object} [deps] - Dependencias inyectables (DI para testabilidad).
     * @param {NodeJS.WriteStream} [deps.stdout] - Stream de salida (default process.stdout).
     * @param {NodeJS.ReadStream} [deps.stdin] - Stream de entrada (default process.stdin).
     * @param {string} [deps.url] - Override de baseUrl (flag --url, D-05).
     * @param {(cmd: string, args: string[], opts: object, cb: (err: any, stdout: string, stderr: string) => void) => any} [deps.exec] - Phase 37 D-01: execFile-shaped inyectable para tests. Default lazy `node:child_process.execFile`.
     * @returns {Promise<void>}
     */
    ```

    Y el destructuring (línea 79):
    ```js
    const { stdout = process.stdout, stdin = process.stdin, url, exec } = deps;
    ```

    **Cambio 2 — Lazy import de runFocus + default execImpl + resolución del cmux binary (después de línea 100 `const App = (await import('./App.js')).default;`, ANTES de línea 107 `stdout.write('\\x1b[?1049h');`):**

    Insertar:
    ```js
    // Phase 37: runFocus (orquestador puro never-throws del verbo cmux select-workspace, Plan 01).
    // Lazy import mismo patrón que App/ink/react. Cero overhead en arranque del CLI.
    const { runFocus } = await import('./focus.js');

    // execFile-shaped default cuando exec no fue inyectado. Lazy: solo se carga si se cablea el TUI
    // (post-guard non-TTY), idéntico patrón a los otros lazy imports arriba.
    const execImpl = exec ?? (await import('node:child_process')).execFile;

    // Resolución del binario cmux desde la config (mismo patrón que src/cmux/client.js:5-7). El
    // default `/Applications/cmux.app/Contents/Resources/bin/cmux` viene de DEFAULT_CONFIG y se
    // sobreescribe vía ~/.kodo/config.json si el operador apunta a otro binario. Llamada extra a
    // loadConfig (cero coste real — primera invocación ya cacheó por la lectura de baseUrl arriba;
    // segunda lectura solo re-deserializa el config en memoria).
    const cmuxBin = loadConfig().cmux.binary;
    ```

    **Cambio 3 — Añadir prop `onFocus` al createElement(App) (línea 109):**

    Cambiar:
    ```js
    const app = render(createElement(App, { baseUrl }));
    ```
    a:
    ```js
    const app = render(createElement(App, {
      baseUrl,
      // Phase 37 D-01: fire-and-forget fire al socket cmux. runFocus es never-throws (Plan 01),
      // App.js maneja el discriminado y mapea a footer-error rojo (Plan 02 D-04/D-05). NO toca el
      // lifecycle de runDashboard — ink sigue montado durante toda la invocación (~50ms).
      onFocus: async (ref) => runFocus({ exec: execImpl, ref, binary: cmuxBin }),
    }));
    ```

    **Cambio 4 — Actualizar el header-comment al top del archivo (líneas 3-30) para documentar la extensión Phase 37 al lado de las notas Phase 34/36:**

    Justo después del último comentario (línea 30 aprox., antes de los imports), insertar una línea breve:
    ```js
    //
    // Phase 37 (este archivo): extensión DI mínima — añade `exec` a `deps`, lazy-importa
    // `runFocus` y pasa `onFocus` como prop a `<App />`. CERO modificación al alt-screen toggle
    // (líneas 107/127), CERO loop while(true), CERO mutación de signal handlers (SIGINT/SIGTERM
    // de Phase 34 D-10 preservado al pie de la letra). El verbo `cmux select-workspace` es
    // fire-and-forget RPC al socket Unix (~50ms) — NO toma el TTY (post-C-01).
    ```

    **NON-CHANGES — listado EXHAUSTIVO de líneas que NO deben tocarse (verificable por diff):**
    - Línea 107 `stdout.write('\\x1b[?1049h');` — alt-screen ON (Phase 36 polish).
    - Línea 115-118 `const onSigterm = () => { app.unmount(); }; process.once('SIGTERM', onSigterm);` — SIGTERM handler Phase 34 D-10.
    - Línea 120-122 `try { await app.waitUntilExit(); }` — espera del exit limpio.
    - Línea 122-130 bloque `finally { stdout.write('\\x1b[?1049l'); process.removeListener('SIGTERM', onSigterm); process.exitCode = 0; }` — alt-screen OFF + signal cleanup + exit code.
    - NO añadir `process.on('SIGINT', ...)` o `process.once('SIGINT', ...)` — la versión revisada post-C-01 NO usa SIGINT noop (D-12 antiguo deprecated).
    - NO añadir loop `while(true)` ni iteración explícita — `app.waitUntilExit()` resuelve cuando el usuario pulsa `q` o llega SIGTERM; la invocación de `runFocus` es síncrona desde la perspectiva del lifecycle (cero unmount, cero re-render).
    - NO inyectar nuevas deps que no sean `exec`.
    - NO mover `loadConfig` ni `resolveBaseUrl` ni la resolución de `baseUrl`.
  </action>
  <verify>
    <automated>node --test test/dashboard/focus.test.js test/dashboard/app-focus.test.js test/dashboard-render.test.js test/dashboard-altscreen.test.js test/dashboard-non-tty.test.js test/dashboard-baseurl.test.js test/format-isolation.test.js</automated>
  </verify>
  <done>
    - `runDashboard` acepta `exec` en `deps`: `grep -c "exec" src/cli/dashboard/index.js` ≥ 4 (JSDoc, destructuring, execImpl, prop pass).
    - Lazy import de runFocus presente: `grep -c "from './focus.js'\\|await import('./focus.js')\\|await import(\\\"./focus.js\\\")" src/cli/dashboard/index.js` ≥ 1.
    - `execImpl = exec ?? (await import('node:child_process')).execFile`: `grep -c "execFile" src/cli/dashboard/index.js` ≥ 1.
    - `cmuxBin = loadConfig().cmux.binary`: `grep -c "cmuxBin" src/cli/dashboard/index.js` ≥ 1.
    - Prop `onFocus` al createElement(App): `grep -c "onFocus" src/cli/dashboard/index.js` ≥ 1.
    - **Líneas críticas preservadas (NO-ALT-SCREEN-MUTATION + NO-SIGNAL-HANDLER-MUTATION):**
      - `grep -c "stdout.write('\\\\x1b\\[?1049h')" src/cli/dashboard/index.js` == 1 (exacto, una sola línea).
      - `grep -c "stdout.write('\\\\x1b\\[?1049l')" src/cli/dashboard/index.js` == 1.
      - `grep -c "process.once('SIGTERM'" src/cli/dashboard/index.js` == 1 (no extra installs).
      - `grep -c "process.removeListener('SIGTERM'" src/cli/dashboard/index.js` == 1.
      - `grep -c "process.on('SIGINT'\\|process.once('SIGINT'\\|process.removeListener('SIGINT'" src/cli/dashboard/index.js` == 0 (cero mutación de SIGINT — D-12 antiguo dropped post-C-01).
      - `grep -c "while.*true" src/cli/dashboard/index.js` == 0 (cero loop iteración).
    - Cero regresión en suites Phase 34-36 + Plan 01/02: el bloque de `verify` cubre 7 archivos test que deben pasar todos.
    - Walker color-isolation: `node --test test/format-isolation.test.js` exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear 37-HUMAN-UAT.md con 2 escenarios obligatorios + 2 bonus (D-08)</name>
  <files>.planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/36-tabla-viva-render-seleccion-filtros/36-HUMAN-UAT.md COMPLETO (analog literal — frontmatter, Current Test, Tests, Summary, Gaps)
    - .planning/phases/37-attach-handoff-cmux/37-CONTEXT.md (líneas 52-69: D-08 con los 2 escenarios obligatorios + 2 bonus opcionales literalmente especificados)
    - .planning/phases/37-attach-handoff-cmux/37-PATTERNS.md (líneas 531-644: §`.planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md` con el frontmatter target + los 4 escenarios y el Summary)
    - .planning/phases/37-attach-handoff-cmux/37-VALIDATION.md (líneas 70-77: Manual-Only Verifications — alineado con los 2 obligatorios)
  </read_first>
  <action>
    Crear `.planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md` desde cero con esta estructura literal (espejo del Phase 36 con adaptaciones D-08):

    **1. Frontmatter YAML al top:**
    ```yaml
    ---
    status: pending
    phase: 37-attach-handoff-cmux
    source: [37-CONTEXT.md D-08, 37-VALIDATION.md]
    started: 2026-05-28
    updated: 2026-05-28
    approved_by: pending
    approved_at: pending
    fixture: scripts/dev-dashboard-fixture.mjs
    blocking_for_phase_close: true
    obligatorios: 2
    bonus: 2
    ---
    ```

    **2. Sección `## Current Test`:**
    ```
    [Escenario 1: Focus exitoso visible — pendiente de ejecutar manualmente]
    ```

    **3. Sección `## Tests` con los 2 obligatorios + 2 bonus opcionales:**

    ```markdown
    ## Tests

    ### 1. Focus exitoso visible (obligatorio — bloqueante para cierre de fase)

    setup:
    - cmux.app abierto y visible (no minimizado) con ≥1 workspace activo.
    - server kodo corriendo con ≥1 sesión `alive===true` en `state.json` cuyo `workspace_ref` corresponde a un workspace cmux montado.
    - Terminal TTY interactivo (no pipe / CI).
    - Comando: `kodo dashboard` (o `node bin/kodo dashboard`).

    steps:
    1. La tabla viva muestra ≥1 fila con status verde (alive=true).
    2. Navegar con ↑/↓ hasta posicionar el cursor (`›`) sobre la fila alive target.
    3. Anotar el `workspace_ref` mostrado en `task_ref` y el workspace cmux ANTES de pulsar Enter.
    4. Pulsar Enter.
    5. Observar la pantalla del Mac (la app cmux GUI debe cambiar foco al workspace target en ≤200ms).
    6. Volver al pane del dashboard kodo SIN ALT-TAB ni gestos de window manager — el dashboard debe seguir corriendo en su pane original.

    expected:
    - La app cmux GUI cambia el foco al workspace `workspace:N` (visible en la GUI macOS).
    - El dashboard kodo sigue corriendo en su pane: cursor preservado sobre la fila, indicador `● live` sigue parpadeando, polling activo.
    - El footer del dashboard sigue siendo `↑↓ move · / filter · q quit` (sin `[!]` rojo).
    - Tiempo del Enter al focus visible: ≤200ms percibido.

    result: pending
    verified_via: (pendiente — requiere TTY real + cmux.app visible)

    ### 2. Zombie reject (obligatorio — bloqueante para cierre de fase)

    setup:
    - Mismo arranque que escenario #1: dashboard activo con ≥1 sesión.
    - Forzar un workspace zombie: `cmux close-workspace --workspace workspace:N` en otra terminal donde `workspace:N` es el `workspace_ref` de una sesión activa en `state.json` (sin matar el server kodo).
    - Esperar el próximo poll (~2s); la fila debe pasar a status rojo con marca textual `(zombie)`.

    steps:
    1. Navegar con ↑/↓ hasta posicionar el cursor sobre la fila zombie.
    2. Pulsar Enter.
    3. Observar el footer del dashboard.
    4. En otra terminal: `ps aux | grep cmux | grep select-workspace` (debe NO mostrar ninguna invocación reciente — el guard cortocircuitó).
    5. Pulsar cualquier tecla (ej. 'x').
    6. Verificar que el footer normal se restaura.

    expected:
    - Tras Enter: footer rojo `[!] workspace gone (alive=false) — press any key` reemplaza el footer normal.
    - `ps aux` NO muestra invocación de `cmux select-workspace` (el guard D-02 cortocircuitó ANTES de llamar a `runFocus`).
    - Dashboard intacto: indicador `● live` sigue, polling sigue, cursor preservado.
    - Tras pulsar 'x': footer rojo se limpia, footer normal `↑↓ move · / filter · q quit` se restaura.
    - El comando `cmux` JAMÁS fue invocado durante este escenario.

    result: pending
    verified_via: (pendiente — requiere TTY real + cross-process verification con `ps aux`)

    ### 3. ENOENT (bonus, opcional — no bloqueante)

    setup:
    - Renombrar temporalmente el binario: `mv /Applications/cmux.app/Contents/Resources/bin/cmux /Applications/cmux.app/Contents/Resources/bin/cmux.bak`.
    - Lanzar `kodo dashboard`.

    steps:
    1. Navegar a una fila con `alive===true`.
    2. Pulsar Enter.
    3. Restaurar el binario: `mv /Applications/cmux.app/Contents/Resources/bin/cmux.bak /Applications/cmux.app/Contents/Resources/bin/cmux`.

    expected:
    - Footer rojo `[!] cmux not found in PATH — press any key`.
    - Dashboard sigue funcional, no crashea.

    result: pending (optional, no bloqueante)
    verified_via: (opcional)

    ### 4. Exit code ≠ 0 (bonus, opcional — no bloqueante)

    setup:
    - Forzar ref inválido: editar manualmente `state.json` para que una sesión tenga `workspace_ref: 'workspace:99999'` (id que no existe en cmux).
    - Lanzar `kodo dashboard`.

    steps:
    1. Navegar a la fila con `workspace:99999` (mantendrá `alive:true` artificialmente; recordar restaurar después).
    2. Pulsar Enter.
    3. Restaurar `state.json`.

    expected:
    - Footer rojo `[!] cmux focus failed (code N) — press any key` (con N = exit code real de cmux).
    - Dashboard sigue funcional.

    result: pending (optional, no bloqueante)
    verified_via: (opcional)
    ```

    **4. Sección `## Summary`:**
    ```
    total: 4 (2 obligatorios + 2 bonus opcionales)
    passed: 0
    issues: 0
    pending: 2 obligatorios bloqueantes + 2 bonus opcionales
    skipped: 0
    blocked: 0
    ```

    **5. Sección `## Gaps`:**
    ```
    (pending — UAT por ejecutar; los 2 obligatorios son bloqueantes para `gsd-verify-work` por D-08).
    ```

    **6. Notas finales (opcional, al pie del documento):**
    ```
    ---

    **Notas de cierre (D-08):**
    - Para marcar la fase como completa, los escenarios **1** y **2** deben tener `result: passed`.
    - Los escenarios **3** y **4** pueden quedar como `pending` o `skipped` sin bloquear el cierre.
    - Cuando los 2 obligatorios pasen, actualizar el frontmatter: `status: passed`, `approved_by: <handle>`, `approved_at: <ISO>`. `gsd-verify-work` lee el frontmatter para decidir si bloquear.
    - Si algún escenario obligatorio falla, documentar en `verified_via` y mantener `status: pending`. NO marcar la fase como completa.
    ```

    Verificar tras escribir que:
    - El frontmatter tiene `blocking_for_phase_close: true` literal.
    - Los 2 escenarios obligatorios tienen `setup:` / `steps:` / `expected:` / `result:` / `verified_via:` estructurados.
    - Los mensajes literales en `expected:` coinciden EXACTAMENTE con las constantes D-05 de App.js (Plan 02): `[!] workspace gone (alive=false) — press any key`, `[!] cmux not found in PATH — press any key`, `[!] cmux focus failed (code N) — press any key`.
  </action>
  <verify>
    <automated>test -f .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md &amp;&amp; grep -c "blocking_for_phase_close: true" .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md &amp;&amp; grep -c "### 1\\.\\|### 2\\." .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md &amp;&amp; grep -c "workspace gone (alive=false) — press any key" .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md</automated>
  </verify>
  <done>
    - `37-HUMAN-UAT.md` existe en `.planning/phases/37-attach-handoff-cmux/`.
    - Frontmatter YAML válido con `status: pending`, `phase: 37-attach-handoff-cmux`, `blocking_for_phase_close: true`, `approved_by: pending`.
    - Sección `## Current Test` presente.
    - Sección `## Tests` con 4 escenarios estructurados (`### 1.` Focus exitoso visible — obligatorio, `### 2.` Zombie reject — obligatorio, `### 3.` ENOENT — bonus, `### 4.` Exit code ≠ 0 — bonus).
    - Cada escenario tiene `setup:` + `steps:` + `expected:` + `result:` + `verified_via:`.
    - Los 3 mensajes literal-estables (D-05) aparecen LITERAL en los `expected:` de los escenarios correspondientes.
    - Sección `## Summary` presente con totales correctos.
    - Sección `## Gaps` presente.
    - Notas de cierre documentan el contrato de blocking (sign-off requerido para los 2 obligatorios).
    - `grep -c "blocking_for_phase_close: true"` == 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ejecución del UAT manual (2 escenarios obligatorios) — checkpoint bloqueante</name>
  <files>.planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md</files>
  <what-built>
    El path completo `Enter → onFocus → runFocus → execFile(cmux, ['select-workspace', '--workspace', ref])` está cableado end-to-end. El binario cmux se resuelve desde `loadConfig().cmux.binary`. El dashboard sigue montado durante la invocación (cero alt-screen toggle extra, cero signal handler mutation, cero unmount). El artefacto `37-HUMAN-UAT.md` contiene los 2 escenarios obligatorios estructurados.

    Lo que el operador debe verificar MANUALMENTE (no automatizable):
    - **Escenario #1:** Que la app cmux GUI realmente cambia el foco al workspace target tras pulsar Enter (un test Node no observa estado de la app desktop macOS).
    - **Escenario #2:** Que el guard zombie cortocircuita ANTES de invocar `cmux` (`ps aux` cross-process no muestra invocación), y que el dismiss-on-any-key funciona en TTY real.
  </what-built>
  <how-to-verify>
    1. Asegurarse de que cmux.app está abierto y visible en pantalla (no minimizado, no en otra desktop).
    2. Asegurarse de que existe ≥1 sesión activa en `state.json` con un `workspace_ref` que apunta a un workspace cmux montado. Si no hay sesión real, lanzar `scripts/dev-dashboard-fixture.mjs` y conectar el dashboard a esa fixture (`kodo dashboard --url http://localhost:9091`).
    3. **Ejecutar Escenario #1 (Focus exitoso visible):**
       - Lanzar `kodo dashboard`.
       - Navegar con ↑/↓ hasta una fila alive (status verde, sin marca `(zombie)`).
       - Pulsar Enter.
       - Observar que la app cmux cambia el foco al workspace target visible en la GUI macOS.
       - Volver al pane del dashboard (sin gestos de window manager). Verificar que sigue corriendo (indicador `● live` parpadeando, polling activo, cursor preservado, footer normal sin `[!]`).
       - Anotar el resultado en `37-HUMAN-UAT.md` §1: cambiar `result: pending` a `result: passed` (o `failed` con detalle) + rellenar `verified_via:`.
    4. **Ejecutar Escenario #2 (Zombie reject):**
       - En otra terminal: `cmux close-workspace --workspace workspace:N` para forzar zombie en una sesión activa.
       - Esperar ~2s al próximo poll; la fila debe pasar a rojo con `(zombie)`.
       - Navegar a esa fila + Enter.
       - Verificar que aparece el footer rojo `[!] workspace gone (alive=false) — press any key`.
       - En otra terminal: `ps aux | grep "cmux select-workspace"` — debe NO mostrar invocación reciente.
       - Pulsar cualquier tecla (ej. 'x') — verificar que el footer se restaura a `↑↓ move · / filter · q quit`.
       - Anotar en `37-HUMAN-UAT.md` §2: `result: passed` (o `failed` con detalle) + `verified_via:`.
    5. **Bonus (opcional, no bloqueante):** Ejecutar escenarios #3 (ENOENT) y #4 (exit code ≠ 0) siguiendo los pasos del artefacto; anotar `passed`/`skipped`/`failed`.
    6. **Cuando los 2 obligatorios estén passed:** Actualizar frontmatter de `37-HUMAN-UAT.md`:
       - `status: passed`
       - `approved_by: <tu-handle>`
       - `approved_at: 2026-05-28T<HH:MM>+02:00` (ISO 8601 con timezone).
       - Si los bonus también pasan: `summary.passed: 4`.
  </how-to-verify>
  <action>
    Tras el sign-off del operador en `37-HUMAN-UAT.md`, esta task queda completa. El operador es quien ejecuta el UAT manualmente; el ejecutor del plan SÓLO observa el resultado del artefacto, no automatiza nada. Si los 2 obligatorios pasan, proceder con el SUMMARY (output del plan). Si alguno falla, registrar el fallo y devolver control para gap closure.
  </action>
  <verify>
    <automated>grep -E "^status: (passed|pending|failed)" .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md | head -1 # Debe estar 'passed' tras sign-off humano; si sigue 'pending' el checkpoint no ha sido cerrado</automated>
  </verify>
  <done>
    - `37-HUMAN-UAT.md` tiene `status: passed` en frontmatter.
    - `approved_by:` no es `pending` (un handle real).
    - `approved_at:` no es `pending` (timestamp ISO real).
    - Escenarios `### 1.` y `### 2.` ambos tienen `result: passed` con `verified_via:` rellenado.
    - Si algún escenario obligatorio falla, esta task queda en estado `failed` y abre gap closure (no se procede al SUMMARY).
  </done>
  <resume-signal>
    Una vez ambos escenarios obligatorios estén `passed` en `37-HUMAN-UAT.md` con el frontmatter `status: passed` + `approved_by: <handle>`, escribir "approved" para continuar a la creación del SUMMARY.

    Si algún escenario obligatorio falla:
    - Documentar el fallo concreto en `verified_via:` (lo que se observó vs lo esperado).
    - NO marcar `status: passed` en el frontmatter.
    - Escribir "failed: <escenario> — <observed>" para que el planner abra un gap closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runDashboard → cmux binary | `runFocus` invoca execFile con args literales; binary path viene de loadConfig user-controlled |
| Dashboard process → cmux GUI | Comunicación via Unix socket de cmux (transparente al kodo dashboard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07 | Tampering | DI override de `exec` en producción | accept | `exec` solo se puede inyectar via `runDashboard(deps)`; la entrada del CLI `bin/kodo dashboard` llama `runDashboard()` sin args (deps={}), así que producción siempre usa `(await import('node:child_process')).execFile`. Cero superficie. |
| T-37-08 | Repudiation | UAT sign-off forgery | mitigate | El frontmatter `approved_by` es texto libre; el contrato de la fase exige que el dev firme. Verificable post-hoc por `gsd-verify-work` que solo aprueba si `status: passed` + sign-off coherente. |
| T-37-09 | Denial of Service | runFocus hang bloquea el handler de Enter | mitigate | Timeout 5_000ms en runFocus (Plan 01 D-07); el `await onFocus(...)` en App.js resuelve en ≤5s en el peor caso; ink sigue procesando otros inputs durante el await (useInput async no bloquea el render loop). |
</threat_model>

<verification>
**Plan-level checks:**

1. `node --test test/dashboard/focus.test.js test/dashboard/app-focus.test.js test/dashboard-render.test.js test/dashboard-altscreen.test.js test/dashboard-non-tty.test.js test/dashboard-baseurl.test.js test/dashboard-status-line.test.js test/dashboard-table.test.js test/dashboard-filter.test.js test/dashboard-poll.test.js test/dashboard-client.test.js test/dashboard-select.test.js test/format-isolation.test.js` → exit 0 (suite completa de dashboard + plans 01/02 + walker).
2. `grep -cE "exec|cmuxBin|runFocus|onFocus" src/cli/dashboard/index.js` ≥ 5 (wiring presente).
3. **Cross-cutting invariants (verificable por grep):**
   - `grep -c "stdout.write('\\\\x1b\\[?1049h')\\|stdout.write('\\\\x1b\\[?1049l')" src/cli/dashboard/index.js` == 2 (alt-screen toggle Phase 36 preservado, exactamente 2 escrituras).
   - `grep -c "process.once('SIGTERM'\\|process.removeListener('SIGTERM'" src/cli/dashboard/index.js` == 2 (SIGTERM Phase 34 D-10 preservado, exactamente install + cleanup).
   - `grep -c "SIGINT" src/cli/dashboard/index.js` == 0 (cero mutación SIGINT — D-12 dropped post-C-01).
   - `grep -c "while.*true\\|for (;;)" src/cli/dashboard/index.js` == 0 (cero loop iteración).
4. `test -f .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md` (artefacto existe).
5. `grep -c "blocking_for_phase_close: true" .planning/phases/37-attach-handoff-cmux/37-HUMAN-UAT.md` == 1.
6. Checkpoint Task 3 ejecutado por el operador con los 2 obligatorios `result: passed` y frontmatter `status: passed` + `approved_by:` rellenado.

**Cross-cutting invariants asserted by this plan:**
- NO-ALT-SCREEN-MUTATION (grep estricto, 2 escrituras exactas).
- NO-SIGNAL-HANDLER-MUTATION (grep estricto, cero SIGINT, exactly 2 SIGTERM ops).
- NO-LOOP-INSERTION (cero `while`/`for(;;)`).
- LITERAL-STABLE MESSAGES (UAT cita exactamente las 3 strings de FOCUS_ERR_* de Plan 02).
- BLOCKING-UAT (frontmatter `blocking_for_phase_close: true`).
</verification>

<success_criteria>
- [ ] `src/cli/dashboard/index.js` extendido con DI de `exec`, lazy import de `runFocus`, resolución de `cmuxBin`, prop `onFocus` pass a `<App />`.
- [ ] Líneas críticas del lifecycle Phase 34/36 preservadas literal (alt-screen + SIGTERM) — verificado por grep.
- [ ] Cero `SIGINT` mutation en index.js (D-12 antiguo dropped post-C-01).
- [ ] Cero loop `while(true)` o iteración explícita en runDashboard.
- [ ] `37-HUMAN-UAT.md` existe con frontmatter `blocking_for_phase_close: true`, 2 escenarios obligatorios + 2 bonus.
- [ ] Los mensajes literal-estables del UAT coinciden EXACTAMENTE con `FOCUS_ERR_ZOMBIE` / `FOCUS_ERR_ENOENT` / `focusErrFailed(N)` de Plan 02.
- [ ] Suite completa de tests dashboard pasa GREEN (suites Phase 34-36 + Plan 01 + Plan 02).
- [ ] Walker `format-isolation` pasa (color-isolation preservada).
- [ ] Checkpoint Task 3 ejecutado: los 2 escenarios obligatorios `result: passed` en el UAT, frontmatter `status: passed` + `approved_by: <handle>`.
- [ ] (Bonus, no bloqueante) Escenarios #3 y #4 documentados como `passed` o `skipped` o `pending`.
</success_criteria>

<output>
Crear `.planning/phases/37-attach-handoff-cmux/37-03-SUMMARY.md` al completar con:
- Hash de commit final.
- Comando que verifica green: `node --test test/dashboard/focus.test.js test/dashboard/app-focus.test.js test/format-isolation.test.js test/dashboard-altscreen.test.js`.
- Estado final del UAT: cita literal del frontmatter de `37-HUMAN-UAT.md` (status, approved_by, approved_at) y resultado de los 4 escenarios.
- Cualquier divergencia respecto al plan documentada con justificación.
- Pendientes que queden abiertos (típicamente: gaps detectados durante el UAT manual + opcionales bonus si quedaron como `pending`).

**Phase closure note:** Si los 2 obligatorios pasan + frontmatter `status: passed`, la fase queda LISTA para `/gsd:verify-work 37`. Si algún obligatorio falla, abrir gap closure plan `/gsd:plan-phase 37 --gaps` con el bloque concreto que falló.
</output>
</content>
</invoke>