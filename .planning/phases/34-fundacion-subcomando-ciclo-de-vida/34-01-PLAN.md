---
phase: 34-fundacion-subcomando-ciclo-de-vida
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - test/format-isolation.test.js
  - test/dashboard-non-tty.test.js
  - test/dashboard-render.test.js
autonomous: true
requirements: [TUI-02, TUI-04]
nyquist_role: wave-0

must_haves:
  truths:
    - "El proyecto declara ink@^6.8.0 + react@^19.2.0 como prod deps e ink-testing-library@^4.0.0 como devDep, sin tocar engines.node (sigue >=20.0.0) ni añadir build step."
    - "Existe un test de spawn-piped que afirma exit 1 + mensaje canónico para non-TTY (queda rojo hasta que Plan 02 implemente el guard)."
    - "Existe un test de render con ink-testing-library que afirma el chrome D-01 banner/placeholder/footer (queda rojo hasta que Plan 02 cree App.js)."
    - "El walker de test/format-isolation.test.js verifica cero importadores de picocolors bajo src/cli/dashboard/ (verde desde ya: el directorio aún no existe → lista vacía)."
  artifacts:
    - path: "package.json"
      provides: "Deps ink/react (prod) + ink-testing-library (devDep), engines.node intacto"
      contains: "\"ink\": \"^6.8.0\""
    - path: "test/dashboard-non-tty.test.js"
      provides: "Verificación TUI-02 vía spawnSync piped → exit 1 + mensaje canónico"
      contains: "requires an interactive terminal"
    - path: "test/dashboard-render.test.js"
      provides: "Verificación TUI-01/D-01 + q→exit vía ink-testing-library"
      contains: "ink-testing-library"
    - path: "test/format-isolation.test.js"
      provides: "Walker extendido para cubrir src/cli/dashboard/ (D-13)"
      contains: "cli/dashboard"
  key_links:
    - from: "test/format-isolation.test.js"
      to: "src/cli/dashboard/"
      via: "listJsFiles(SRC).filter por path"
      pattern: "cli/dashboard"
---

<objective>
Preparar la fundación de verificación de Phase 34 ANTES de cualquier implementación: instalar el
stack ink/react (con su devDep de testing), crear los tests de la fase en estado rojo (Wave 0 /
Nyquist gate) y extender el walker de color-isolation para cubrir el directorio TUI.

Este plan NO crea `src/cli/dashboard/` ni toca `src/cli.js`. Su salida son los contratos de
verificación que Plan 02 deberá poner en verde. Es un plan Tier 2 (añade prod deps) por la política
de merge por blast radius.

Purpose: fijar las invariantes de disciplina (refuse non-TTY, color-isolation) como tests
ejecutables desde el primer commit, y bloquear el stack en `ink@^6.8.0` (no `ink@7`, que rompería
`engines.node >=20`).
Output: `package.json`/`package-lock.json` con deps nuevas, `test/dashboard-non-tty.test.js` (nuevo),
`test/dashboard-render.test.js` (nuevo), `test/format-isolation.test.js` (walker extendido).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-CONTEXT.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-RESEARCH.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md

<interfaces>
<!-- Contratos que los tests asertan. Extraídos del codebase y de CONTEXT.md (strings load-bearing). -->

Mensaje canónico non-TTY (D-04) — string EXACTO asertado por test/dashboard-non-tty.test.js:
  "kodo dashboard requires an interactive terminal (TTY). Run it directly in your terminal, not in a pipe or CI."
(implementado en dos líneas concatenadas: "...interactive terminal (TTY). " + "Run it directly...")

Chrome D-01 (mockup CONTEXT.md L127-136) — substrings asertados por test/dashboard-render.test.js:
  banner       → "kodo dashboard"
  placeholder  → "starting…"  (carácter U+2026, NO tres puntos ASCII)
  footer hint  → "q quit"

Helpers reutilizables ya presentes en test/format-isolation.test.js (NO reescribir):
  listJsFiles(dir)        → string[] (absolute paths, recursivo sobre src/)        [L59-71]
  extractImports(src)     → string[] (specifiers import, con y sin binding)        [L23-28]
  REPO, SRC               → constantes de path absolutas                            [L8-9]
  IMPORT_FROM_RE / IMPORT_BARE_RE → NO matchean import() dinámico (verificado L14)

Patrón spawnSync E2E (análogo test/version-smoke.test.js):
  KODO_BIN = join(resolve(__dirname,'..'),'bin','kodo')
  spawnSync(process.execPath, [KODO_BIN, 'dashboard'], { stdio:['pipe','pipe','pipe'], encoding:'utf-8', timeout:10_000 })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar stack ink/react + devDep de testing (pin ink@6, sin build step)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (estado actual: solo commander+picocolors, engines.node >=20.0.0, sin devDependencies, sin scripts.build)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-RESEARCH.md (sección "Standard Stack" L93-143 — versiones verificadas contra npm y Pitfall 4 L332-336: ink@7 rompe Node floor)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md (sección package.json L246-261)
  </read_first>
  <action>
    Añadir a `package.json` exactamente: en `dependencies`, `"ink": "^6.8.0"` y `"react": "^19.2.0"`
    (NUNCA `ink@7` ni rango que lo permita — exige Node >=22 y rompería `engines.node`). Crear la
    sección `devDependencies` con `"ink-testing-library": "^4.0.0"` y `"@types/react": "^19"` (este
    último mantiene `// @ts-check` verde sobre la superficie ink; incluirlo por defecto per Open
    Question 1 del RESEARCH, recomendación = incluir). Ejecutar `npm install` para regenerar
    `package-lock.json` y materializar `node_modules/`.
    NO modificar `engines.node` (debe seguir `>=20.0.0`). NO añadir `scripts.build` ni ningún
    transpiler (constraint "no build step"). NO tocar `commander`/`picocolors`. Verificar que
    `npm install` no emitió warnings de engine/peer incompatibles (si ink resolviese a 7.x abortar).
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node -e "const p=require('./package.json'); const a=require('assert'); a.match(p.dependencies.ink, /\^6\./, 'ink debe ser ^6.x'); a.ok(p.dependencies.react, 'react requerido'); a.ok(p.devDependencies['ink-testing-library'], 'ink-testing-library devDep requerido'); a.equal(p.engines.node, '>=20.0.0', 'engines.node intacto'); a.ok(!p.scripts || !p.scripts.build, 'sin build step'); console.log('OK package.json')" && node -e "require('ink'); require('react'); require('ink-testing-library'); console.log('OK módulos resuelven')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.ink` matchea `^6.` (NO `^7`, NO `7.x`).
    - `package.json` `dependencies.react` presente (`^19.2.0`).
    - `package.json` `devDependencies.ink-testing-library` presente (`^4.0.0`).
    - `package.json` `engines.node === ">=20.0.0"` (sin cambios).
    - No existe `package.json` `scripts.build`.
    - `require('ink')`, `require('react')`, `require('ink-testing-library')` resuelven sin error desde el repo.
    - `package-lock.json` existe y fue actualizado en el mismo commit que `package.json`.
  </acceptance_criteria>
  <done>ink@^6.8.0 + react@^19.2.0 en dependencies; ink-testing-library@^4.0.0 + @types/react@^19 en devDependencies; engines.node intacto; lockfile regenerado; los tres módulos resuelven.</done>
</task>

<task type="auto">
  <name>Task 2: Crear tests Wave 0 (non-TTY guard + chrome render) — rojos hasta Plan 02</name>
  <files>test/dashboard-non-tty.test.js, test/dashboard-render.test.js</files>
  <read_first>
    - test/version-smoke.test.js (análogo EXACTO del boilerplate spawnSync: __dirname/REPO/KODO_BIN, opciones encoding/timeout; nota: ese test afirma stderr==='' — aquí es lo OPUESTO, stderr debe contener el mensaje)
    - test/session-of-resolver.test.js (patrón de asserts de exit code + match de stderr)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-RESEARCH.md (Code Examples L341-398 — ambos tests ya esbozados; "Validation Architecture" L448-481)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md (secciones de ambos tests L149-206)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-CONTEXT.md (D-04 mensaje canónico exacto L137-139; mockup chrome L127-136)
  </read_first>
  <action>
    Crear `test/dashboard-non-tty.test.js` (TUI-02): cabecera `// @ts-check`, imports `node:test`,
    `node:assert/strict`, `spawnSync` de `node:child_process`, y el boilerplate de paths de
    `version-smoke.test.js`. Definir la constante `CANONICAL` con el string EXACTO de D-04 (ver
    `<interfaces>`). Un `it` que ejecute
    `spawnSync(process.execPath, [KODO_BIN, 'dashboard'], { stdio:['pipe','pipe','pipe'], encoding:'utf-8', timeout:10_000 })`
    y aserte: `r.status === 1`, `r.stderr` matchea `/requires an interactive terminal \(TTY\)/`, y
    `r.stderr.trim() === CANONICAL`. Los tres pipes garantizan non-TTY (reproduce `kodo dashboard | cat`/CI).

    Crear `test/dashboard-render.test.js` (TUI-01 + TUI-03 parcial): cabecera `// @ts-check`,
    imports `node:test`, `node:assert/strict`, `render` de `ink-testing-library`, `createElement` de
    `react`, y `App` (default) de `../src/cli/dashboard/App.js`. Un `it` que renderice
    `render(createElement(App, { baseUrl: 'http://localhost:9090' }))` y aserte que `lastFrame()`
    matchea `/kodo dashboard/`, `/starting…/` (con U+2026) y `/q quit/`. Un segundo `it` para `q→exit`:
    `stdin.write('q')` y afirmar la salida limpia observando que `frames` deja de actualizarse / el
    componente desmonta — usar la señal disponible de ink-testing-library (`waitUntilExit` del instance
    o estabilidad de `lastFrame()` tras la pulsación); elegir la aserción concreta según la firma de
    exit que exponga `App` (Assumption A3 del RESEARCH lo deja como criterio de implementación, pero el
    test DEBE asertar algo observable, no quedar vacío).

    Ambos tests quedarán ROJOS hasta que Plan 02 implemente `index.js`/`App.js`/registro — es el
    estado esperado de Wave 0 (Nyquist gate). NO crear `src/cli/dashboard/` en este plan.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && test -f test/dashboard-non-tty.test.js && test -f test/dashboard-render.test.js && node --check test/dashboard-non-tty.test.js && grep -q "requires an interactive terminal" test/dashboard-non-tty.test.js && grep -q "starting…" test/dashboard-render.test.js && grep -q "ink-testing-library" test/dashboard-render.test.js && echo "OK ambos tests existen y son sintácticamente válidos"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-non-tty.test.js` existe, pasa `node --check`, contiene `spawnSync` con `stdio:['pipe','pipe','pipe']` y `[KODO_BIN, 'dashboard']`.
    - Ese test aserta `status === 1` y `stderr.trim()` igual al string canónico D-04 completo.
    - `test/dashboard-render.test.js` existe, importa `render` de `ink-testing-library` y `App` de `../src/cli/dashboard/App.js`.
    - Ese test aserta los tres substrings del chrome (`kodo dashboard`, `starting…`, `q quit`) y tiene un segundo `it` con `stdin.write('q')` que aserta algo observable (no vacío).
    - Ejecutar `node --test test/dashboard-non-tty.test.js test/dashboard-render.test.js` produce fallos (rojo) porque `src/cli/dashboard/` aún no existe — estado Wave 0 esperado.
  </acceptance_criteria>
  <done>Ambos tests creados, sintácticamente válidos, con las aserciones load-bearing (mensaje canónico exacto + chrome D-01). Rojos por diseño hasta Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Extender el walker de color-isolation para cubrir src/cli/dashboard/ (D-13)</name>
  <files>test/format-isolation.test.js</files>
  <read_first>
    - test/format-isolation.test.js (LEER COMPLETO: helpers listJsFiles L59-71 / extractImports L23-28 / constantes REPO,SRC L8-9; el describe "Single source of color" L98-129 que NO se debe modificar)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md (sección format-isolation L209-242 — el bloque NUEVO a añadir, reutilizando helpers)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-CONTEXT.md (D-12/D-13 L61-66)
  </read_first>
  <action>
    Añadir un `describe` NUEVO al final de `test/format-isolation.test.js` titulado
    `'TUI-04 (D-13): cero picocolors bajo src/cli/dashboard/'`. Dentro, un `it` que: tome
    `listJsFiles(SRC)`, filtre por path que incluya `/cli/dashboard/`, y para cada archivo filtrado
    use `extractImports(readFileSync(file,'utf-8'))` afirmando que NINGUNO incluye `'picocolors'`
    (`assert.deepEqual(leakers, [], ...)` con `leakers` = paths relativos de los infractores).
    Reutilizar los helpers existentes (`listJsFiles`, `extractImports`, `REPO`, `SRC`) — NO redefinirlos.
    NO modificar el `describe` existente "Single source of color" ni su aserción
    `assert.deepEqual(importers, ['src/cli/format.js'])` (ink NO es picocolors → esa aserción sigue
    verde). En Wave 1 `src/cli/dashboard/` aún no existe → `listJsFiles` no lo encuentra → la lista
    filtrada es vacía → el nuevo test pasa trivialmente; gana mordida cuando Plan 02 crea los archivos.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --test test/format-isolation.test.js 2>&1 | grep -qE "pass|ok" && node -e "const s=require('fs').readFileSync('test/format-isolation.test.js','utf-8'); const a=require('assert'); a.ok(s.includes('cli/dashboard'), 'walker debe filtrar por cli/dashboard'); a.ok(s.includes(\"['src/cli/format.js']\"), 'aserción existente intacta'); console.log('OK walker extendido + aserción original preservada')"</automated>
  </verify>
  <acceptance_criteria>
    - `test/format-isolation.test.js` contiene un `describe` nuevo con título que referencia `TUI-04` y `src/cli/dashboard/`.
    - El nuevo bloque reutiliza `listJsFiles` y `extractImports` (no redefine helpers).
    - La aserción original `assert.deepEqual(importers, ['src/cli/format.js'], ...)` sigue presente sin modificar.
    - `node --test test/format-isolation.test.js` pasa (verde) en Wave 1 — el directorio TUI aún no existe, lista filtrada vacía.
  </acceptance_criteria>
  <done>Walker extendido con un describe nuevo que verifica cero picocolors bajo src/cli/dashboard/, reutilizando los helpers existentes; las aserciones previas intactas y verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (TTY) → proceso `kodo dashboard` | Entrada confiable local: teclas del TTY y el flag `--url`. En Phase 34 `--url` solo se almacena en memoria; NO alcanza ninguna llamada de red (HTTP llega en Phase 35). |
| stdin/stdout no-TTY (pipe/CI) → proceso | Entrada del entorno: cuando el stream NO es TTY, ink lanzaría `Raw mode is not supported`. El guard pre-render la convierte en exit 1 limpio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | arranque de `runDashboard` en non-TTY (pipe/CI) | mitigate | Guard pre-render (`!stdout.isTTY \|\| !stdin.isTTY` → stderr + `process.exit(1)`) ANTES de `render()`; verificado por `test/dashboard-non-tty.test.js` (este plan crea el test; Plan 02 implementa el guard). |
| T-34-02 | Tampering | inyección de cadena en el flag `--url` | accept | En Phase 34 `--url` no alcanza red ni shell — solo se almacena como baseUrl en memoria. No hay superficie explotable en esta fase. La validación/sanitización de `--url` y de contenido de red se hereda a Phase 35 (cliente HTTP). |

**Nota:** superficie de seguridad de Phase 34 casi nula (sin auth, sin secrets, sin red, cuerpo placeholder estático). No hay amenazas de severidad alta en esta fase — el único riesgo real es de robustez (crash en non-TTY), ya cubierto por T-34-01.
</threat_model>

<verification>
- `node --test test/format-isolation.test.js` pasa (verde) — walker extendido sin romper aserciones previas.
- `node --check test/dashboard-non-tty.test.js && node --check test/dashboard-render.test.js` — ambos sintácticamente válidos.
- `require('ink') && require('react') && require('ink-testing-library')` resuelven.
- `package.json` `engines.node` sigue `>=20.0.0`; `dependencies.ink` matchea `^6.`.
- Estado Wave 0 esperado: `dashboard-non-tty` y `dashboard-render` ROJOS (implementación en Plan 02).
</verification>

<success_criteria>
- Stack instalado y bloqueado: ink@^6.8.0 + react@^19.2.0 (prod), ink-testing-library@^4.0.0 + @types/react@^19 (dev), engines.node intacto, sin build step.
- Dos tests de fase creados en estado rojo (Nyquist gate) con las aserciones load-bearing exactas.
- Walker de color-isolation extendido a `src/cli/dashboard/` reutilizando helpers, aserciones previas preservadas.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-01-SUMMARY.md`
documentando: versiones exactas resueltas por npm, decisión sobre `@types/react`, y el estado rojo
esperado de los dos tests de Plan 02.
</output>
