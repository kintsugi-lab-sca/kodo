---
phase: 34-fundacion-subcomando-ciclo-de-vida
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/cli.js
  - src/cli/dashboard/index.js
  - src/cli/dashboard/App.js
autonomous: false
requirements: [TUI-01, TUI-02, TUI-03, TUI-04]
nyquist_role: implementation

must_haves:
  truths:
    - "El operador ejecuta `kodo dashboard` en un TTY y ve el chrome estático: banner `kodo dashboard`, placeholder `starting…`, footer `q quit`."
    - "D-02: el cuerpo es el placeholder estático `starting…` — NO se renderiza ningún dato real ni counters/header live en Phase 34 (eso se hereda a Phase 36); el marco solo queda listo para que esa fase lo rellene."
    - "D-03: si stdout/stdin NO es TTY (pipe/CI), el guard imprime a stderr el mensaje canónico (D-04) y sale con exit 1 ANTES de render() — sin crash de raw-mode."
    - "D-08: el operador pulsa `q` y la terminal queda intacta (ink desmonta vía useApp().exit(), no process.exit); Ctrl-C usa el exitOnCtrlC default de ink (D-09); D-10: SIGTERM tiene handler explícito propio que invoca el mismo camino de cleanup (app.unmount())."
    - "Esc NO sale en el root (D-11: reservado para overlays de Phase 38)."
    - "D-12: ningún archivo bajo src/cli/dashboard/ importa picocolors; el color sale solo de <Text> de ink."
  artifacts:
    - path: "src/cli.js"
      provides: "Registro del subcomando `kodo dashboard` (D-06 description, D-07 lazy import, D-05 flag --url)"
      contains: "command('dashboard')"
    - path: "src/cli/dashboard/index.js"
      provides: "runDashboard: guard non-TTY (D-03/D-04), resolución baseUrl (D-05), render(), SIGTERM handler (D-10), exit code"
      exports: ["runDashboard"]
      min_lines: 25
    - path: "src/cli/dashboard/App.js"
      provides: "Componente root ink: chrome estático D-01/D-02 + useInput('q' → exit) (D-08)"
      min_lines: 20
  key_links:
    - from: "src/cli.js"
      to: "src/cli/dashboard/index.js"
      via: "await import en .action (lazy, D-07)"
      pattern: "import\\('\\./cli/dashboard/index\\.js'\\)"
    - from: "src/cli/dashboard/index.js"
      to: "src/cli/dashboard/App.js"
      via: "render(createElement(App, { baseUrl }))"
      pattern: "render\\(createElement\\(App"
    - from: "src/cli/dashboard/index.js"
      to: "src/config.js"
      via: "loadConfig().server.port para baseUrl default (D-05)"
      pattern: "loadConfig\\(\\)\\.server\\.port"
---

<objective>
Implementar el subcomando `kodo dashboard` que pone en verde los tests de Plan 01: registro en
`src/cli.js` (lazy import D-07, flag `--url` D-05), el process-owner `runDashboard` (guard non-TTY
D-03/D-04 → render → SIGTERM handler D-10 → exit code) y el componente ink `App.js` (chrome estático
D-01 + `q`→exit D-08).

Esta fase NO renderiza datos reales — el cuerpo es el placeholder estático `starting…` (D-02). Las
primitivas de lifecycle que se fijan aquí (`unmount`/`waitUntilExit`/restauración de terminal) son las
que reusará el handoff de attach en Phase 37, por eso deben quedar correctas desde el primer commit.

Plan no-autónomo: incluye un checkpoint de verificación humana (TUI-03 Ctrl-C/SIGTERM en TTY real no
es automatizable sin PTY).

Purpose: cerrar TUI-01..04 con el esqueleto, los guards y el ciclo de vida limpio.
Output: `src/cli/dashboard/index.js` (nuevo), `src/cli/dashboard/App.js` (nuevo), bloque `command('dashboard')` en `src/cli.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-CONTEXT.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-RESEARCH.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md
@.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-01-SUMMARY.md

<interfaces>
<!-- Contratos que Plan 01 dejó asertados como tests. La implementación DEBE satisfacerlos. -->

Mensaje canónico non-TTY (D-04:) — string EXACTO que test/dashboard-non-tty.test.js compara con stderr.trim():
  "kodo dashboard requires an interactive terminal (TTY). Run it directly in your terminal, not in a pipe or CI."

Chrome D-01 — substrings que test/dashboard-render.test.js busca en lastFrame():
  "kodo dashboard"  ·  "starting…" (U+2026, NO "...")  ·  "q quit"

Patrón de registro en src/cli.js (D-07: lazy import en .action; análogo `status` L249-270 / `logs` L273-298):
  program.command('<name>').description('<desc>').option(...).action(async (opts) => {
    const { runX } = await import('./cli/<path>.js');  // lazy
    await runX({ ... });
  });
  NOTA: NO replicar `await ensureConfig()` (status lo usa; dashboard NO lo necesita en Phase 34).

Resolución de baseUrl (D-05:) — loadConfig().server.port default 9090 (src/config.js:62-63):
  const baseUrl = deps.url ?? `http://localhost:${loadConfig().server.port}`;

Lifecycle ink (CITED ink README, vía RESEARCH Pattern 3 L239-264):
  render(createElement(App, props)) → instance { unmount, waitUntilExit }
  q → useApp().exit()  (D-08: NO process.exit — saltaría el teardown de ink)
  Ctrl-C → exitOnCtrlC:true default de ink (D-09: NO cablear SIGINT propio)
  SIGTERM → process.once('SIGTERM', () => app.unmount()); remover listener tras waitUntilExit()  (D-10:)
  salida limpia → process.exitCode = 0 (NO process.exit — deja drenar stdio)

Convenciones de archivo nuevas (verificadas en src/cli.js:1, src/config.js:1):
  cabecera `// @ts-check`, ESM (`import`/`export`), specifiers con `.js` explícito.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/cli/dashboard/index.js (runDashboard: guard non-TTY, baseUrl, render, SIGTERM, exit code)</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - src/cli.js (bloque `orchestrate` L124-200 — patrón SIGTERM/cleanup idempotente más maduro del repo; gate temprano de `config --set` L37-40 → stderr + process.exit(1))
    - src/config.js:62-66 (server.port default 9090 que alimenta baseUrl)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-RESEARCH.md (Patterns 2-3 L218-264; Anti-Patterns L277-283)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md (sección index.js L66-124 — firma DI, guard, baseUrl, SIGTERM handler, las divergencias respecto a orchestrate)
    - test/dashboard-non-tty.test.js (el test que este archivo debe poner en verde — la firma del mensaje y el exit code)
  </read_first>
  <behavior>
    - D-03: Non-TTY (stdout o stdin sin isTTY): escribe a stderr el mensaje canónico D-04 + `\n`, sale con exit 1, ANTES de cualquier render() → test/dashboard-non-tty.test.js verde.
    - TTY OK: resuelve baseUrl (D-05: deps.url ?? http://localhost:<config.server.port>), monta App con render(createElement(App,{baseUrl})), instala SIGTERM handler que llama app.unmount(), espera waitUntilExit(), remueve el listener, fija process.exitCode = 0.
    - D-10: SIGTERM durante la sesión → app.unmount() (restaura terminal), NO process.exit directo.
    - D-12: el módulo NO importa picocolors (verificado por test/format-isolation.test.js walker extendido).
  </behavior>
  <action>
    Crear `src/cli/dashboard/index.js` con cabecera `// @ts-check` y `export async function runDashboard(deps = {})`.
    Firma DI per constraint PROJECT.md: desestructurar `{ stdout = process.stdout, stdin = process.stdin, url } = deps`
    (stdout/stdin inyectables para testabilidad, igual que los helpers de `src/cli/polling.js`).
    Definir la constante `NON_TTY_MSG` con el string EXACTO de D-04 (ver `<interfaces>`), concatenado en
    dos líneas. Guard PRIMERO (D-03): `if (!stdout.isTTY || !stdin.isTTY) { process.stderr.write(NON_TTY_MSG + '\n'); process.exit(1); }`
    — a stderr (no stdout), exit 1, ANTES de render(). Tras el guard: resolver baseUrl (D-05) con lazy import
    `const { loadConfig } = await import('../../config.js')` → `const baseUrl = url ?? \`http://localhost:${loadConfig().server.port}\``.
    Lazy import de ink/react/App: `const { render } = await import('ink')`, `const { createElement } = await import('react')`,
    `const App = (await import('./App.js')).default`. Montar `const app = render(createElement(App, { baseUrl }))`.
    SIGTERM handler explícito (D-10): `const onSigterm = () => { app.unmount(); }; process.once('SIGTERM', onSigterm);`
    Luego `await app.waitUntilExit(); process.removeListener('SIGTERM', onSigterm); process.exitCode = 0;`.
    NO cablear SIGINT (D-09: lo cubre el exitOnCtrlC default de ink — replicarlo saltaría el teardown, Pitfall 9).
    NO importar picocolors (D-12). NO usar JSX (React.createElement plano). El import de ink/react debe ser
    dinámico (`await import`) para mantener el arranque del CLI ligero y para que el walker estático de
    color-isolation no necesite tratarlos (ink no es picocolors, pero el lazy import es el patrón del repo).
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --check src/cli/dashboard/index.js && node --test test/dashboard-non-tty.test.js 2>&1 | grep -qE "pass 1|# pass 1" && echo "OK index.js: guard non-TTY verde"</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/index.js` existe, pasa `node --check`, exporta `runDashboard`.
    - Contiene el string canónico D-04 exacto y `process.exit(1)` en el path non-TTY.
    - D-03: el guard non-TTY aparece ANTES de cualquier `render(` en el orden del archivo (source assertion: índice de `isTTY` < índice de `render(`).
    - D-05: usa `loadConfig().server.port` para el baseUrl default y respeta `deps.url` como override.
    - D-10: instala SIGTERM vía `process.once('SIGTERM', ...)` que llama `app.unmount()`, y fija `process.exitCode = 0` tras `waitUntilExit()` (NO `process.exit(0)` en el path limpio).
    - NO contiene `import ... 'picocolors'` ni `import 'picocolors'` (D-12).
    - `node --test test/dashboard-non-tty.test.js` pasa (verde).
  </acceptance_criteria>
  <done>runDashboard implementado: guard non-TTY pre-render (D-03, exit 1 + mensaje canónico D-04), baseUrl config-driven con override --url (D-05), render de App, SIGTERM handler vía unmount (D-10), exit code limpio. test/dashboard-non-tty.test.js verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/cli/dashboard/App.js (chrome estático D-01/D-02 + q→exit, Esc no sale) + registrar el subcomando en src/cli.js</name>
  <files>src/cli/dashboard/App.js, src/cli.js</files>
  <read_first>
    - src/cli.js (LEER bloques `status` L249-270 y `logs` L273-298 — patrón de registro a copiar; el comentario de `skill sync` sobre NO usar ensureConfig)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-RESEARCH.md (Patterns 1,3,4 L200-275; mockup en Architecture Diagram L175-186)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-PATTERNS.md (secciones App.js L128-146 y cli.js L24-62 — disciplinas a heredar; el componente es greenfield, sin análogo)
    - .planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-CONTEXT.md (D-01 chrome, D-02 placeholder estático, D-08/D-11 bindings, D-12 color; mockup L127-136)
    - test/dashboard-render.test.js (los substrings exactos que lastFrame() debe contener)
  </read_first>
  <behavior>
    - D-01: render(createElement(App,{baseUrl})).lastFrame() contiene "kodo dashboard", "starting…" (U+2026) y "q quit" → test/dashboard-render.test.js verde (chrome).
    - D-02: el placeholder `starting…` es estático — App NO consume ni renderiza datos reales en Phase 34.
    - D-08: stdin.write('q') dispara useApp().exit() → el componente desmonta limpio (NO process.exit).
    - D-11: Esc NO dispara salida (reservado Phase 38).
    - useInput gateado por useStdin().isRawModeSupported (belt-and-suspenders, Pitfall 1).
    - D-12: color exclusivamente vía <Text> de ink; cero import de picocolors → walker de format-isolation verde.
    - D-05/D-06/D-07: `kodo dashboard --url http://x:1234` registrado en src/cli.js con description D-06 y lazy import a runDashboard.
  </behavior>
  <action>
    Crear `src/cli/dashboard/App.js` con cabecera `// @ts-check`. `import { Box, Text, useApp, useInput, useStdin } from 'ink'`
    y `import { createElement } from 'react'` (static import de ink permitido — ink NO es picocolors, D-12).
    `export default function App({ baseUrl })`. Dentro: `const { exit } = useApp();`,
    `const { isRawModeSupported } = useStdin();`. Registrar
    `useInput((input, key) => { if (input === 'q') exit(); }, { isActive: isRawModeSupported })`
    — `q` sale vía exit() (NO process.exit, D-08); Esc deliberadamente NO se maneja (D-11). Renderizar
    el chrome D-01 con `createElement(Box, ...)` / `createElement(Text, ...)` (NO JSX): banner
    `kodo dashboard` (p. ej. `Text` con `bold`), placeholder central `starting…` (U+2026 exacto, estático
    D-02 — sin datos reales esta fase), footer `q quit`. Markup/borde/padding a discreción (D-01 fija el
    contenido, no el styling), respetando que todo color salga de props de `<Text>` (`color`/`bold`/`dimColor`),
    NUNCA `picocolors` (D-12).

    En `src/cli.js`: añadir un bloque `program.command('dashboard')` siguiendo el patrón de `status`/`logs`
    (lazy import en `.action`, D-07). `.description('Live TUI dashboard of active kodo sessions')` (D-06: sin alias).
    `.option('--url <baseUrl>', 'Base URL del server kodo (default: http://localhost:<config.server.port>)')` (D-05).
    `.action(async (opts) => { const { runDashboard } = await import('./cli/dashboard/index.js'); await runDashboard({ url: opts.url }); })` (D-07).
    NO añadir `await ensureConfig()` (el dashboard no requiere provider configurado en Phase 34). Ubicar
    el bloque junto a los demás subcomandos (p. ej. tras `logs`), sin tocar comandos vecinos (Regla 3 — quirúrgico).
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/kodo && node --check src/cli/dashboard/App.js && node --check src/cli.js && grep -q "command('dashboard')" src/cli.js && node --test test/dashboard-render.test.js test/format-isolation.test.js 2>&1 | grep -qE "pass" && ! grep -rq "picocolors" src/cli/dashboard/ && echo "OK App.js chrome + registro + color-isolation"</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/App.js` existe, pasa `node --check`, default-exporta un componente que usa `useApp`, `useInput`, `useStdin`.
    - D-08/D-11: `useInput` maneja `'q'` con `exit()` y NO maneja `Esc` (source assertion: no hay rama para `key.escape` que llame exit).
    - D-01/D-02: el chrome renderiza los tres substrings exactos (`kodo dashboard`, `starting…` con U+2026, `q quit`), con el cuerpo como placeholder estático.
    - D-12: ningún archivo bajo `src/cli/dashboard/` contiene `picocolors` (`grep -r picocolors src/cli/dashboard/` vacío).
    - D-05/D-06/D-07: `src/cli.js` contiene `command('dashboard')`, `.description('Live TUI dashboard of active kodo sessions')`, `.option('--url <baseUrl>'...)`, y un `.action` con `import('./cli/dashboard/index.js')`; NO contiene `ensureConfig` en ese bloque.
    - `node --test test/dashboard-render.test.js test/format-isolation.test.js` pasa (verde).
  </acceptance_criteria>
  <done>App.js renderiza el chrome estático D-01/D-02 con q→exit (D-08, Esc no sale D-11), color solo de ink (D-12); src/cli.js registra `dashboard` con description D-06, lazy import D-07 y flag --url D-05. test/dashboard-render.test.js y test/format-isolation.test.js verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): UAT manual de TUI-03 — terminal intacta tras q / Ctrl-C / SIGTERM en TTY real</name>
  <what-built>
    El subcomando `kodo dashboard` completo: registro en `src/cli.js`, `runDashboard`
    (guard non-TTY + render + SIGTERM handler + exit code) y `App.js` (chrome estático + lifecycle de
    salida). Los tres tests automáticos de la fase (`dashboard-non-tty`, `dashboard-render`,
    `format-isolation`) están en verde. Falta la verificación de TUI-03 que NO es automatizable sin un
    PTY real: que la terminal quede INTACTA tras salir por `q`, Ctrl-C y SIGTERM (cursor visible, echo
    restaurado, scrollback sin corromper).
  </what-built>
  <how-to-verify>
    En una terminal interactiva real (NO en un pipe, NO en CI):

    1. `node bin/kodo dashboard` → debes ver el chrome: banner `kodo dashboard`, `starting…` centrado, footer `q quit`.
    2. Pulsa `q` → el panel desaparece y vuelves al prompt. Verifica: el cursor es visible, al teclear se ve el echo, y el scrollback (historial de la terminal) NO está corrupto.
    3. Relanza `node bin/kodo dashboard` y pulsa `Ctrl-C` → mismo resultado: terminal intacta (cursor, echo, scrollback).
    4. Relanza `node bin/kodo dashboard` en una terminal, anota el PID (`echo $!` si lo lanzas en background, o `ps`), y desde otra terminal `kill <pid>` (SIGTERM) → la terminal del dashboard debe quedar intacta (cursor visible, echo restaurado), sin dejar raw-mode colgado.
    5. (Negativo, opcional — ya cubierto por test) `node bin/kodo dashboard | cat` → debe imprimir a stderr "kodo dashboard requires an interactive terminal (TTY)..." y salir con código 1, sin stack trace de raw-mode.

    En los 3 casos de salida (q / Ctrl-C / SIGTERM): cursor visible + echo + scrollback intacto = PASS.
  </how-to-verify>
  <resume-signal>Escribe "approved" si los 4 escenarios dejan la terminal intacta, o describe el síntoma (cursor oculto / sin echo / scrollback roto / raw-mode colgado) para corregir.</resume-signal>
  <action>Pausar la ejecución y presentar al operador los pasos de <how-to-verify> para que verifique en un TTY real. NO automatizar este paso (requiere PTY interactivo). Esperar la resume-signal antes de continuar. Si el operador reporta un síntoma (cursor oculto, sin echo, scrollback corrupto, raw-mode colgado), volver a Task 1/Task 2 para corregir el cableado de lifecycle (exit() vs process.exit, SIGTERM handler, unmount) y re-presentar.</action>
  <verify>
    <automated>MANUAL — UAT de TUI-03 en TTY real (no automatizable sin PTY). Los tests automáticos relacionados (dashboard-non-tty, dashboard-render q->exit, format-isolation) ya cubren el resto y deben estar verdes antes de este checkpoint.</automated>
  </verify>
  <done>El operador confirma "approved": tras q, Ctrl-C y SIGTERM la terminal queda intacta (cursor visible, echo restaurado, scrollback sin corromper) en los 4 escenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (TTY) → proceso `kodo dashboard` | Teclas del TTY (`q`, Ctrl-C) y flag `--url`. `--url` solo se almacena como baseUrl en memoria — NO alcanza red en Phase 34 (HTTP llega en Phase 35). |
| stdin/stdout no-TTY (pipe/CI) → proceso | Cuando el stream no es TTY, ink lanzaría `Raw mode is not supported`. El guard pre-render lo convierte en exit 1 limpio. |
| señales del SO (SIGTERM/SIGINT) → ciclo de vida | SIGTERM sin handler dejaría la terminal en raw-mode. Handler explícito → `app.unmount()` restaura. SIGINT lo cubre el `exitOnCtrlC` de ink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | `runDashboard` en non-TTY | mitigate | Guard pre-render (`!stdout.isTTY \|\| !stdin.isTTY` → stderr + `process.exit(1)`) ANTES de `render()`. Verificado por `test/dashboard-non-tty.test.js`. |
| T-34-03 | Denial of Service | terminal dejada en raw-mode tras exit sucio (q/Ctrl-C/SIGTERM) | mitigate | `q`→`useApp().exit()` (no `process.exit`); SIGTERM→handler explícito que llama `app.unmount()`; Ctrl-C→`exitOnCtrlC` default de ink. ink restaura cursor/echo/scrollback en unmount. Verificado por checkpoint human-verify (no automatizable sin PTY). |
| T-34-02 | Tampering | inyección de cadena en `--url` | accept | En Phase 34 `--url` no alcanza red ni shell — solo baseUrl en memoria. Validación/sanitización de `--url` y de contenido de red se hereda a Phase 35. |

**Nota:** sin amenazas de severidad alta. La superficie es de robustez/hygiene (refuse non-TTY + restauración de terminal), ambas mitigadas. La inyección ANSI desde contenido no confiable NO aplica aquí (cuerpo placeholder estático, no se renderizan datos de red) — se hereda a Phases 35-38.
</threat_model>

<verification>
- `node --check src/cli/dashboard/index.js src/cli/dashboard/App.js src/cli.js` — los tres válidos.
- `node --test test/dashboard-non-tty.test.js test/dashboard-render.test.js test/format-isolation.test.js` — los tres VERDES.
- `grep -r picocolors src/cli/dashboard/` — vacío (color-isolation TUI-04, D-12).
- `grep "command('dashboard')" src/cli.js` — presente (registro TUI-01, D-06/D-07).
- `npm test` (suite completa) — verde sobre baseline v0.8 (895 pass + 1 skip); el dashboard no rompe nada existente.
- Checkpoint human-verify: terminal intacta tras q / Ctrl-C / SIGTERM en TTY real (TUI-03).
</verification>

<success_criteria>
- `kodo dashboard` monta el chrome estático D-01/D-02 en TTY (TUI-01).
- non-TTY → exit 1 + mensaje canónico (D-03/D-04), sin crash (TUI-02).
- q / Ctrl-C / SIGTERM dejan la terminal intacta (TUI-03 — D-08/D-09/D-10 — automático parcial + checkpoint manual).
- cero picocolors bajo src/cli/dashboard/, verificado por el walker (TUI-04, D-12).
- Suite completa verde; ningún subcomando vecino tocado (Regla 3).
</success_criteria>

<output>
Tras completar, crear `.planning/phases/34-fundacion-subcomando-ciclo-de-vida/34-02-SUMMARY.md`
documentando: la partición final de archivos en `src/cli/dashboard/`, la firma de `runDashboard`, cómo
quedó cableado el SIGTERM handler, y el resultado del UAT manual de TUI-03 (los 4 escenarios).
</output>
