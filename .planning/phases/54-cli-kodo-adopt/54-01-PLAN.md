---
phase: 54-cli-kodo-adopt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/adopt.js
  - src/cli.js
  - test/adopt-cli.test.js
  - test/format-isolation.test.js
autonomous: true
requirements: [BIDIR-07]

must_haves:
  truths:
    - "D-01: El operador puede correr `kodo adopt --workspace W --cwd C --session-id S --project P` y la sesión se adopta (tarea creada + fila sembrada) vía adoptSession; --session-id required, --project→projectId con projectPath resuelto local desde loadProjects(), --title/--description opcionales delegados al core"
    - "D-02: Cada forma del discriminante de adoptSession deriva el exit code correcto: ok→0, ALREADY_ADOPTED→0, INVALID_INPUT→1, UNSUPPORTED→1, PERSIST_FAILED→1 (LOUD stderr), CREATE_FAILED→2"
    - "D-04: En éxito el feedback muestra task_id + task_url + session_id; en fallo muestra code + detail; --json emite el discriminante byte-determinista sin ANSI (handler espejo de runGsdVerifyCli con DI + createFormatter)"
    - "D-03: Un --project no mapeado en loadProjects() falla fast (exit 1 + lista de projectIds disponibles) ANTES de cualquier POST; --project es required, sin prompt interactivo"
  artifacts:
    - path: "src/cli/adopt.js"
      provides: "runAdoptCli(opts, deps) — thin handler argv→delegación→render"
      contains: "export async function runAdoptCli"
      min_lines: 60
    - path: "test/adopt-cli.test.js"
      provides: "Tests DI-stubbed del exit-code map + render + --json + projectPath"
      contains: "runAdoptCli"
    - path: "src/cli.js"
      provides: "Registro top-level del comando `adopt`"
      contains: "command('adopt')"
  key_links:
    - from: "src/cli.js"
      to: "src/cli/adopt.js"
      via: "lazy import dentro de .action"
      pattern: "import\\('./cli/adopt.js'\\)"
    - from: "src/cli/adopt.js"
      to: "src/adopt.js"
      via: "adoptSession invocado tras resolver provider + projectPath"
      pattern: "adoptSession"
    - from: "src/cli/adopt.js"
      to: "src/cli/format.js"
      via: "createFormatter (única fuente de color, nunca picocolors)"
      pattern: "createFormatter"
---

<objective>
Entregar el CLI `kodo adopt`: el primer consumidor (y referencia de contrato) de la fontanería determinista 0-token `adoptSession` (Phase 53). Es un thin handler argv→delegación→render, espejo 1:1 de `runGsdVerifyCli` (`src/cli/gsd-verify.js`). Cero lógica de negocio nueva: la única lógica original del CLI es (1) resolver `provider`/`providerName` (registry) + `projectPath` (`loadProjects()[projectId]`), (2) el switch discriminante→exit code, (3) los strings de render.

Purpose: Fija el contrato de flags + exit codes que dashboard (Phase 56) y orquestador (Phase 57) shellearán vía `execFile`. Cierra BIDIR-07.
Output: `src/cli/adopt.js` (NEW), bloque `program.command('adopt')` en `src/cli.js` (MODIFIED), `test/adopt-cli.test.js` (NEW), edit aditivo a `test/format-isolation.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-cli-kodo-adopt/54-CONTEXT.md
@.planning/phases/54-cli-kodo-adopt/54-RESEARCH.md
@.planning/phases/54-cli-kodo-adopt/54-PATTERNS.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código. NO explorar el codebase. -->

Discriminante de `adoptSession` (src/adopt.js:135-140, FUENTE DE VERDAD — 5 error codes + ok, 6 SHAPES):
```
{ ok:true, task, session }
{ ok:false, code:'UNSUPPORTED',     detail:{ providerName } }
{ ok:false, code:'INVALID_INPUT',   detail:{ missing } }            // missing: string[]
{ ok:false, code:'ALREADY_ADOPTED', detail:{ task_id } }
{ ok:false, code:'CREATE_FAILED',   detail:{ message } }
{ ok:false, code:'PERSIST_FAILED',  detail:{ task_id, task_url, hint, message } }
```

Firma (src/adopt.js:163-166):
```
adoptSession({ provider, providerName, workspaceRef, cwd, sessionId, projectId, projectPath, title?, description? }, deps?) => Promise<discriminante>
```
En éxito, `task` es un TaskItem canónico (`task.id`, `task.url`); `session` tiene `session.session_id`.

Molde del handler (src/cli/gsd-verify.js:57-85) — clon estructural:
```
export async function runGsdVerifyCli(opts, deps = {}) {
  const write = deps.writeFn || ((s) => process.stdout.write(s));
  const err = deps.errFn || ((s) => process.stderr.write(s));
  const runVerifyFn = deps.runVerifyFn || runGsdVerify;
  const fmt = (deps.formatterFn || (() => createFormatter(process.stdout)))();
  ...
  if (opts.json) { write(JSON.stringify(result, null, 2) + '\n'); }
  else { renderHuman(result, write, fmt); }
  return 0;
}
```

Helpers del formatter (src/cli/format.js): `fmt.ok(s)`, `fmt.green(s)`, `fmt.yellow(s)`, `fmt.red(s)`. `useColor=false` (non-TTY/NO_COLOR) → devuelven el input sin ANSI.

Bloque de registro `gsd verify` (src/cli.js, el molde — pero `adopt` es TOP-LEVEL `program.command('adopt')`, NO bajo `gsd`):
```
.command('verify <session-id>')
.option('--json', '...')
.action(async (sessionId, opts) => {
  try {
    await ensureConfig();
    const { runGsdVerifyCli } = await import('./cli/gsd-verify.js');
    const code = await runGsdVerifyCli({ sessionId, json: opts.json || false });
    process.exit(code);
  } catch (err) { console.error(`Error: ${err.message}`); process.exit(1); }
});
```

Array de callsites (test/format-isolation.test.js:133, `PHASE_15_CALLSITES = Object.freeze([...])`): hoy incluye `src/logger.js`, `src/logs/reader.js`, `src/check.js`, `src/cli/gsd-inspect.js`, `src/cli/gsd-verify.js`. El regex de import relativo `/(\.\.?\/)+(cli\/)?format\.js$/` ya matchea `./format.js` desde `src/cli/adopt.js` — NO tocar el regex.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test DI-stubbed del handler (Wave 0 — escribir ANTES del handler)</name>
  <files>test/adopt-cli.test.js</files>
  <read_first>
    - test/gsd-verify-cli-handler.test.js (el harness a clonar 1:1: makeStdoutStub, builders inline de result, bloques describe/it de exit-code, el par --json parse-vs-throw, las aserciones color-ANSI con formatter TTY inyectado, y el bloque estático CLI-wiring que lee src/cli.js)
    - src/cli/gsd-verify.js (el molde del handler que el test ejercita)
    - src/adopt.js (líneas 135-140 — el discriminante exacto que los stubs de adoptSessionFn devuelven; 163-166 firma)
  </read_first>
  <behavior>
    - ok:true → exit 0; render contiene task_id (T-1), task_url y session_id (mirror C1)
    - ALREADY_ADOPTED → exit 0; mensaje "no-op"/"ya adoptada" con el task_id existente
    - INVALID_INPUT → exit 1; render del detail.missing
    - UNSUPPORTED → exit 1; render con providerName
    - PERSIST_FAILED → exit 1; banner en STDERR (vía errFn stub) con task_id + task_url + hint (NO en stdout)
    - CREATE_FAILED → exit 2 (transient)
    - --json → write byte-igual a `JSON.stringify(result, null, 2) + '\n'`; JSON.parse no lanza; SIN ANSI aun con formatterFn TTY inyectado (mirror JSON1)
    - --project no mapeado (loadProjectsFn devuelve {}) → exit 1 + lista de projectIds disponibles en stderr; adoptSessionFn NUNCA invocado (assert call-count == 0, mirror C12)
    - Estático CLI-wiring: readFileSync('src/cli.js') incluye `command('adopt')`, `import('./cli/adopt.js')` y `runAdoptCli` (mirror CLI1-3)
  </behavior>
  <action>Clonar el harness de test/gsd-verify-cli-handler.test.js. Importar `runAdoptCli` de '../src/cli/adopt.js'. Definir `makeStdoutStub()` (buffer + write/get). Los stubs de result son objetos discriminante planos inline (mirror passResult/failResult) — sin fixtures compartidos. Cada test inyecta deps: `adoptSessionFn` (async devolviendo la shape), `getProviderFn` (devuelve `{ createTask: () => {} }`), `loadProjectsFn` (devuelve `{ P: '/tmp/proj' }` o `{}` para el caso unmapped), `writeFn`/`errFn` (stubs de buffer), `formatterFn` (devuelve un formatter creado con stream non-TTY salvo en el caso --json-con-TTY). Cubrir las 9 conductas del bloque <behavior> en bloques `describe`/`it`. Para el caso unmapped, contar invocaciones de adoptSessionFn y asertar 0. Añadir el bloque estático que hace readFileSync de src/cli.js y `assert.match`/`includes` para las 3 cadenas de wiring. Sin watch-mode. NO bloques de código fenced en el handler — este task SOLO escribe el test; el handler aún no existe y los tests fallarán (RED) hasta Task 2.</action>
  <verify>
    <automated>node --test test/adopt-cli.test.js 2>&1 | grep -qE "fail|Cannot find|ERR_MODULE" && echo "RED-as-expected (handler ausente)" || echo "CHECK: tests deberían fallar hasta Task 2"</automated>
  </verify>
  <acceptance_criteria>
    - test/adopt-cli.test.js existe e importa `runAdoptCli` de '../src/cli/adopt.js'
    - Contiene los 6 casos de exit-code (ok/ALREADY_ADOPTED/INVALID_INPUT/UNSUPPORTED/PERSIST_FAILED/CREATE_FAILED), el caso --json, y el caso --project no mapeado con assert de adoptSessionFn no invocado
    - Contiene el bloque estático que lee src/cli.js y asserta command('adopt') + import('./cli/adopt.js') + runAdoptCli
    - El archivo falla en ejecución porque src/cli/adopt.js aún no existe (RED esperado)
  </acceptance_criteria>
  <done>test/adopt-cli.test.js escrito con cobertura completa de BIDIR-07; falla en RED por ausencia del handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler src/cli/adopt.js + registro del comando en src/cli.js (GREEN)</name>
  <files>src/cli/adopt.js, src/cli.js</files>
  <read_first>
    - src/cli/gsd-verify.js (molde EXACTO del handler: header `// @ts-check`, DI con `*Fn` defaults, formatter lazy, branch --json byte-determinista, renderHuman con color-by-severity)
    - src/cli/gsd-inspect.js (precedente del patrón DI `getProviderFn` — líneas ~58-62)
    - src/adopt.js (líneas 135-140 el discriminante; 163-166 la firma de adoptSession a invocar)
    - src/session/manager.js (líneas 78-103 — las ERROR SEMANTICS de resolveProjectPath a espejar: mensaje "No local path mapped" + hint; NO llamar a la función, toma un `task` inexistente en tiempo de CLI)
    - src/config.js (loadConfig, loadProjects — loadProjects() devuelve Record<projectId, string | {default?, modules?}>)
    - src/cli.js (el bloque `gsd verify` como molde del registro; localizar también `ensureConfig`)
    - .planning/phases/54-cli-kodo-adopt/54-PATTERNS.md (las plantillas de código exactas por sección)
  </read_first>
  <behavior>
    - Tras crear el handler, los tests de Task 1 pasan en GREEN: `node --test test/adopt-cli.test.js` sale 0
    - exitCodeFor: ok→0, ALREADY_ADOPTED→0, INVALID_INPUT→1, UNSUPPORTED→1, PERSIST_FAILED→1, CREATE_FAILED→2, default→1
    - renderHuman: éxito→stdout (task_id verde, task_url, session_id); PERSIST_FAILED→stderr LOUD banner; CREATE_FAILED→fmt.yellow; INVALID_INPUT/UNSUPPORTED→fmt.red
    - --project no mapeado → exit 1 + lista de projectIds ANTES de invocar adoptSession
  </behavior>
  <action>Crear `src/cli/adopt.js` con header `// @ts-check` y comentarios ES (convención del codebase). Exportar `async function runAdoptCli(opts, deps = {})`. Resolver deps con idioma `deps.X || realX`: `writeFn` (default process.stdout.write), `errFn` (default process.stderr.write), `adoptSessionFn` (default `adoptSession` importado de '../adopt.js'), formatter lazy `(deps.formatterFn || (() => createFormatter(process.stdout)))()` importando `createFormatter` de './format.js' (JAMÁS picocolors). Añadir `getProviderFn` y `loadProjectsFn` como deps inyectables (espejo gsd-inspect). PASO 1 resolver provider: lazy-import loadConfig/loadProjects de '../config.js' e initRegistry/getProvider de '../providers/registry.js'; `providerName = loadConfig().provider`; `provider = getProvider(providerName)` (usando los `*Fn` cuando se inyectan). PASO 2 resolver projectPath: `entry = loadProjects()[projectId]`; si `entry === undefined` → escribir vía errFn "No local path mapped for project ..." + "Available projects: " + `Object.keys(projects).join(', ')` + hint `kodo config --map-project`, y `return 1` (fail-fast, SIN POST); `projectPath = typeof entry === 'string' ? entry : (entry.default ?? '')`; si `!projectPath` → errFn + return 1. NO derivar módulo (no hay task en tiempo de CLI; espejar SOLO las error semantics de resolveProjectPath per Pitfall 2). PASO 3 invocar `adoptSessionFn({ provider, providerName, workspaceRef: opts.workspaceRef, cwd: opts.cwd, sessionId: opts.sessionId, projectId: opts.projectId, projectPath, title: opts.title, description: opts.description })`. PASO 4 render: si `opts.json` → `write(JSON.stringify(result, null, 2) + '\n')` y `return exitCodeFor(result)` (bypass total de renderHuman, sin color, sin reshape — byte-determinismo); else `renderHuman(result, write, err, fmt)`. Definir `exitCodeFor(result)`: si `result.ok` return 0; switch sobre `result.code` con el mapeo D-02 (ALREADY_ADOPTED→0, INVALID_INPUT→1, UNSUPPORTED→1, PERSIST_FAILED→1, CREATE_FAILED→2, default→1) — exactamente 5 cases de error, NO añadir un 6º (Pitfall 1). Definir `renderHuman(result, write, err, fmt)`: éxito → fmt.ok('Adopted') + task_id (fmt.green) + task_url + session_id a stdout; ALREADY_ADOPTED → mensaje no-op con detail.task_id a stdout; PERSIST_FAILED → banner a `err` (STDERR) con fmt.red, detail.task_id + detail.task_url + detail.hint (Pitfall 4); CREATE_FAILED → err con fmt.yellow + detail.message; INVALID_INPUT → err con fmt.red + detail.missing.join(', '); UNSUPPORTED → err con fmt.red + detail.providerName. Mensajes ES. NO duplicar el default de título ni el saneo (los hace el core). En `src/cli.js` añadir un bloque TOP-LEVEL `program.command('adopt')` (NO bajo `gsd`) clonando el molde de `gsd verify`: `.requiredOption('--workspace <ref>', ...)`, `.requiredOption('--cwd <path>', ...)`, `.requiredOption('--session-id <id>', ...)`, `.requiredOption('--project <id>', ...)`, `.option('--title <t>', ...)`, `.option('--description <d>', ...)`, `.option('--json', ...)`; en `.action(async (opts) => {...})`: try → `await ensureConfig()` → `const { runAdoptCli } = await import('./cli/adopt.js')` → `const code = await runAdoptCli({ workspaceRef: opts.workspace, cwd: opts.cwd, sessionId: opts.sessionId, projectId: opts.project, title: opts.title, description: opts.description, json: opts.json || false })` → `process.exit(code)`; catch → `console.error('Error: ' + err.message)` + `process.exit(1)`. (`--session-id` → `opts.sessionId` por auto-camelCase de commander.)</action>
  <verify>
    <automated>node --test test/adopt-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/adopt.js` contiene `export async function runAdoptCli`
    - `grep -c picocolors src/cli/adopt.js` devuelve 0 (importa createFormatter de './format.js')
    - `node --test test/adopt-cli.test.js` sale 0 (todos los casos GREEN)
    - `src/cli.js` contiene `command('adopt')` y `import('./cli/adopt.js')`
    - `node bin/kodo adopt --help` imprime usage con --workspace/--cwd/--session-id/--project/--title/--description/--json
    - exitCodeFor tiene exactamente 5 cases de error (no hay 6º)
  </acceptance_criteria>
  <done>Handler + registro implementados; suite del handler en GREEN; --help renderiza; cero importación de picocolors.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar src/cli/adopt.js en el guard de color-isolation</name>
  <files>test/format-isolation.test.js</files>
  <read_first>
    - test/format-isolation.test.js (el array `PHASE_15_CALLSITES` en línea 133 + las dos aserciones: positiva "importa format.js" líneas ~141-163 y negativa "no importa picocolors" líneas ~165-180; el regex de import en ~152 ya matchea, NO tocarlo)
  </read_first>
  <action>Editar `PHASE_15_CALLSITES` (Object.freeze array) añadiendo la entrada `'src/cli/adopt.js'` tras `'src/cli/gsd-verify.js'`. Edit aditivo de una línea — NO modificar el regex de import relativo ni la estructura de las aserciones. Esto bloquea el nuevo callsite bajo ambas aserciones: positiva (importa format.js) y negativa (no importa picocolors).</action>
  <verify>
    <automated>node --test test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/cli/adopt.js" test/format-isolation.test.js` devuelve 1 (presente en el array)
    - `node --test test/format-isolation.test.js` sale 0
    - `npm test` (suite completa) sale 0
  </acceptance_criteria>
  <done>src/cli/adopt.js cubierto por el guard de color-isolation; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → argv (commander) | Flags `--workspace/--cwd/--session-id/--project/--title/--description` cruzan aquí como input no confiable |
| CLI → provider task manager (POST) | `adoptSession` (core) hace el POST; el CLI solo resuelve provider + projectPath |
| CLI → state.json local | `addSession` (dentro del core) escribe la fila; el CLI nunca escribe directo |
| CLI → consumidores aguas abajo (dashboard/orquestador vía execFile) | El contrato de flags + exit codes que esta fase fija es consumido por Phase 56/57 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure | Fuga de ruta absoluta / home dir hacia el task manager (title/description) o en el render | mitigate | Ya mitigado en el core (`sanitizeAdoptionData`, Phase 53 D-06: strip de rutas absolutas + redacción home, nunca embebe transcript). El CLI pasa `title`/`description` SIN tocar y NO duplica/debilita el saneo. El render muestra `task.url`/`detail.task_url` (URL del provider, no ruta local) y `session.session_id`; no imprime `projectPath` ni cwd absoluto. Riesgo residual LOW |
| T-54-02 | Tampering | `--project` hostil escribiendo una fila state.json con un path no mapeado | mitigate | `projectId` validado contra el allowlist de `loadProjects()` ANTES de cualquier POST/escritura (fail-fast exit 1 con lista de projectIds). Path no mapeado → nunca llega a adoptSession |
| T-54-03 | Tampering | Argument injection vía invocación shelleada (dashboard/orquestador) | accept | Concern de Phase 56/57: consumen `execFile` con argv literal (sin shell). El contrato de flags de esta fase es explícito y sin posicionales, lo que lo facilita. El CLI no hace `eval`/shell. Riesgo en esta fase LOW |
| T-54-04 | Information Disclosure | `--json` filtrando algo que el path humano no emitiría | accept | `--json` emite el discriminante EXACTO que devuelve `adoptSession` (`JSON.stringify(result, null, 2)`), la misma superficie que el render humano consume; sin campos extra. El core ya sanea title/description. Riesgo LOW |
| T-54-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — esta fase instala CERO paquetes externos (RESEARCH §Package Legitimacy Audit). Todos los imports son intra-repo + builtins node. Sin checkpoint de legitimidad necesario |
</threat_model>

<verification>
- `npm test` (suite completa) verde antes de `/gsd:verify-work`.
- `node --test test/adopt-cli.test.js` cubre las 6 shapes del discriminante → exit codes, render éxito/fallo, --json byte-determinista, y projectPath unmapped fail-fast (BIDIR-07 SC1/SC2/SC3).
- `node --test test/format-isolation.test.js` verde con el nuevo callsite (invariante color isolation).
- Smoke manual (nicety, no gate): `node bin/kodo adopt --help` muestra usage con todas las flags.
- Invariantes preservadas: cero endpoints nuevos (src/server.js intacto), 0-token determinista (sin LLM/prompts/cmux), FROZEN-9 (createTask sigue typeof-detected — el path UNSUPPORTED del core lo cubre), --json byte-determinismo.
</verification>

<success_criteria>
- BIDIR-07 SC1: `kodo adopt --workspace W --cwd C --session-id S --project P [--title T] [--description D]` crea la tarea y siembra la fila vía adoptSession con input explícito (verificado por el test ok:true + projectPath resuelto).
- BIDIR-07 SC2: los exit codes derivan deterministas del discriminante (espejo Opción A de `gsd verify`): ok/ALREADY_ADOPTED→0, INVALID_INPUT/UNSUPPORTED/PERSIST_FAILED→1, CREATE_FAILED→2.
- BIDIR-07 SC3: éxito muestra task_id + task_url + session_id; fallo muestra code + detail (PERSIST_FAILED LOUD en stderr); --json byte-determinista.
- Color isolation: src/cli/adopt.js importa format.js, nunca picocolors (guard verde).
</success_criteria>

<output>
Create `.planning/phases/54-cli-kodo-adopt/54-01-SUMMARY.md` when done.
</output>
