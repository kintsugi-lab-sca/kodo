---
phase: 19-worktree-cleanup-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/hooks/stop.js
  - src/gsd/verify.js
  - test/stop-worktree-cleanup.test.js
  - test/gsd-verify-integration.test.js
  - test/stop.test.js
autonomous: true
requirements:
  - WT-04
  - WT-05
  - WT-06
tags:
  - worktree-cleanup
  - stop-hook
  - gsd-verify
  - phase-19
user_setup: []

must_haves:
  truths:
    - "Tras stop hook clean: git worktree list no incluye el worktree de la sesión cerrada, branch local borrada, evento worktree.cleanup.ok emitido (SC#1 + D-08)"
    - "D-01: Dirty = git status --porcelain sobre el worktree devuelve output no vacío. SOLO working tree — commits locales no pusheados NO cuentan como dirty (driver ROMAN-113…118: lo que se pierde es trabajo sin commit, no commits unpushed)"
    - "Tras stop hook dirty (D-01): el worktree NO se borra silenciosamente — se mueve a <path>.dirty/ (o .dirty-<timestamp>/ si colisión) y se emite worktree.cleanup.dirty warn (SC#1 + D-02 + Pitfall #1)"
    - "handleOrchestratorStop sigue corriendo con cwd: KODO_ROOT (NO modificado funcionalmente, satisfied-by-design WT-05/D-05)"
    - "kodo gsd verify resuelve phasesRoot con session.worktree_path ?? session.project_path (WT-06 / D-06); sesiones legacy v0.5 sin worktree_path siguen funcionando silent (D-09)"
    - "Si git worktree remove/move falla por razón no-dirty, console.error + continuar — el stop hook nunca crashea (D-03)"
    - "Cleanup ocurre DESPUÉS de releaseGsdLock en stop.js (D-07) y la branch se lee ANTES de worktree remove (D-08 + Pitfall #2)"
    - "Legacy v0.5: si session.worktree_path es falsy, cleanup hace skip silencioso sin emitir warn (D-09)"
    - "git worktree prune oportunista al final del cleanup, fail-open en su propio try/catch (D-04)"
  artifacts:
    - path: "src/hooks/stop.js"
      provides: "Bloque de cleanup del worktree tras releaseGsdLock + gitFn dep inyectable"
      contains: "worktreeCleanupOk"
    - path: "src/gsd/verify.js"
      provides: "phasesRoot resuelto con worktree_path fallback"
      contains: "session.worktree_path ?? session.project_path"
    - path: "test/stop-worktree-cleanup.test.js"
      provides: "Tests unit (gitFn stub) + E2E (git real con tmpdir) para WT-04 (clean/dirty/error/legacy/collision/branch-fail/prune)"
      contains: "describe"
      min_lines: 200
    - path: "test/gsd-verify-integration.test.js"
      provides: "Fixtures con/sin worktree_path + source-hygiene assert sobre verify.js:124"
      contains: "worktree_path"
    - path: "test/stop.test.js"
      provides: "Source-hygiene asserts D-07/D-08/D-05 (orden cleanup>releaseGsdLock, branch read antes de remove, handleOrchestratorStop preserva KODO_ROOT)"
      contains: "Phase 19 D-07"
  key_links:
    - from: "src/hooks/stop.js"
      to: "src/logger-events.js"
      via: "dynamic import { worktreeCleanupOk, worktreeCleanupDirty, worktreeCleanupError }"
      pattern: "worktreeCleanup(Ok|Dirty|Error)"
    - from: "src/hooks/stop.js"
      to: "git CLI"
      via: "gitFn(cwd, args) → execFileSync('git', ['-C', cwd, ...args])"
      pattern: "branch --show-current|worktree remove|worktree move|branch -D|worktree prune"
    - from: "src/gsd/verify.js"
      to: "SessionRecord.worktree_path"
      via: "nullish coalescing en phasesRoot"
      pattern: "session\\.worktree_path \\?\\? session\\.project_path"
    - from: "test/stop-worktree-cleanup.test.js"
      to: "src/hooks/stop.js"
      via: "runStopHook(input, { gitFn, loggerFactory, findSessionFn, removeSessionFn, cmux })"
      pattern: "runStopHook\\("
---

<objective>
Cerrar el ciclo de vida del worktree creado por Phase 18: cablear cleanup fail-open en `src/hooks/stop.js` (WT-04), reapuntar `kodo gsd verify` para leer VERIFICATION.md desde el worktree (WT-06), confirmar que `handleOrchestratorStop` preserva `cwd: KODO_ROOT` (WT-05 satisfied-by-design por D-05) y cubrir todo con tests unit + E2E + source-hygiene.

Purpose: SCs del ROADMAP §Phase 19:
  1. `git worktree list` ya no incluye el worktree de sesiones cerradas con tree limpio; dirty preserva el directorio con marca `.dirty/` + log warn (D-02).
  2. `auto-commit` sigue corriendo con `cwd: KODO_ROOT` (D-05); `KODO_ROOT` env override intacto (Phase 999.1 D-16).
  3. `kodo gsd verify <sid>` localiza VERIFICATION.md en el worktree y mantiene exit codes + bytes Plane comment (Pitfall #6 Opción A invariante).

Output:
- `src/hooks/stop.js` con bloque de cleanup tras `releaseGsdLock` + `gitFn` dep inyectable + 5 try/catch fail-open por sub-paso.
- `src/gsd/verify.js` línea 124 cambiada a `join(session.worktree_path ?? session.project_path, '.planning', 'phases')` + JSDoc actualizada.
- `test/stop-worktree-cleanup.test.js` NUEVO con cobertura de 7+ escenarios.
- `test/gsd-verify-integration.test.js` extendido con fixtures worktree_path / legacy + source-hygiene.
- `test/stop.test.js` extendido con 3 source-hygiene asserts (D-07, D-08, D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md
@.planning/phases/19-worktree-cleanup-integration/19-RESEARCH.md
@.planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md
@.planning/phases/19-worktree-cleanup-integration/19-VALIDATION.md
@.planning/phases/19-worktree-cleanup-integration/19-01-SUMMARY.md
@src/hooks/stop.js
@src/gsd/verify.js
@src/logger-events.js
@src/session/state.js
@test/skill-auto-commit.test.js
@test/gsd-verify-integration.test.js
@test/stop.test.js

<interfaces>
<!-- runStopHook signature (src/hooks/stop.js:97-101) -->

```javascript
export async function runStopHook(input, deps = {}) {
  // W-4: defaults vía OR — runtime productivo usa los imports estáticos.
  const findSessionFn = deps.findSessionFn || findSession;
  const removeSessionFn = deps.removeSessionFn || removeSession;
  const cmuxClient = deps.cmux || cmux;
  // ... Phase 19 añade: const gitFn = deps.gitFn || ((cwd, args) => execFileSync('git', ['-C', cwd, ...args], { encoding: 'utf-8' }).trim());
```

<!-- Sitio de inyección — entre releaseGsdLock y removeSession
     (src/hooks/stop.js, justo después del bloque `if (session.gsd) { ... releaseGsdLock ... }` ~línea 200,
      antes de `removeSessionFn(id)` ~línea 203) -->

<!-- Helpers ya disponibles desde Plan 01 (src/logger-events.js) -->

```javascript
export function worktreeCleanupOk(logger, { session_id, worktree_path, branch_deleted }) { ... }   // info
export function worktreeCleanupDirty(logger, { session_id, worktree_path, moved_to }) { ... }      // warn
export function worktreeCleanupError(logger, { session_id, worktree_path, phase, reason }) { ... } // error
// phase ∈ {'status', 'remove', 'move', 'branch', 'prune'}
```

<!-- verify.js current site (src/gsd/verify.js:121-124) -->

```javascript
const padded = /^\d+$/.test(session.phase_id)
  ? session.phase_id.padStart(2, '0')
  : session.phase_id;
const phasesRoot = join(session.project_path, '.planning', 'phases');
```

<!-- handleOrchestratorStop (src/hooks/stop.js:238-272) — NO TOCAR funcionalmente (D-05).
     Pattern canon: execSync(cmd, { cwd: KODO_ROOT, encoding: 'utf-8' }) -->

<!-- Empirical git behavior validated (RESEARCH §Empirical Git Worktree Behavior):
     - `git worktree move A B`: si B existe como dir, mete A DENTRO de B (Pitfall #1 — pre-check existsSync).
     - `git worktree move A B` CON dirty: ACEPTADO en git 2.51.2 (Test 2b/2c).
     - `git -C <wt> branch --show-current`: requiere worktree existente → leer ANTES de remove (Pitfall #2).
     - `git branch -D X` con branch checked-out por otro worktree: exit 128, fail-open (Pitfall #3).
     - `git worktree prune` sobre repo limpio: exit 0 no-op (Test 5). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test scaffold test/stop-worktree-cleanup.test.js (Wave 0 — TDD RED first)</name>
  <files>test/stop-worktree-cleanup.test.js</files>
  <read_first>
    - test/skill-auto-commit.test.js (líneas 22-99 — patrón mkdtempSync + git init local + spawnSync child con HOME+KODO_ROOT override)
    - test/gsd-verify-integration.test.js (líneas 37-101 — `makeSession()` factory + `makeLogger()` memSink + `makeDeps()`)
    - src/hooks/stop.js (líneas 90-220 — `runStopHook` signature + DI defaults)
    - .planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md §4 "test/stop-worktree-cleanup.test.js" (estructura sugerida verbatim)
    - .planning/phases/19-worktree-cleanup-integration/19-VALIDATION.md (per-task verification map para tasks 19-02-01..07)
    - .planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md §"Política de dirty" (D-01, D-02, D-03, D-04, D-08, D-09)
  </read_first>
  <behavior>
    Suite con 2 secciones:

    **Sección 1 — Unit con gitFn stub (sin spawn child, sin git real):**
    - it CLEAN: `git status --porcelain` devuelve '' → llama `worktree remove` + `branch -D` + emite `worktree.cleanup.ok` con `branch_deleted: true`. Orden de gitCalls: `['branch', '--show-current']` → `['status', '--porcelain']` → `['worktree', 'remove', wt]` → `['branch', '-D', branchName]` → `['worktree', 'prune']`.
    - it DIRTY: `git status --porcelain` devuelve 'M file.txt\n' → NO llama `worktree remove`, llama `worktree move <wt> <wt>.dirty` + emite `worktree.cleanup.dirty` con `moved_to === <wt>.dirty`. Branch preservada (no `branch -D`).
    - it ERROR on remove: gitFn lanza durante `worktree remove` → emite `worktree.cleanup.error` con `phase: 'remove'` + `reason` no vacío. NO crashea (await completa sin throw).
    - it TARGET COLLISION: pre-check detecta `<wt>.dirty` existe → llama `worktree move` con target `<wt>.dirty-<digits>` (timestamp/random suffix). Validar via `gitCalls.some(c => c.args[2].startsWith(<wt>+'.dirty-'))`.
    - it LEGACY (D-09): `session.worktree_path` ausente → `gitCalls.length === 0` y `events` no contiene ningún `worktree.cleanup.*`.
    - it BRANCH-D FAILURE: gitFn lanza solo en `branch -D` → emite `worktree.cleanup.ok` con `branch_deleted: false` (NO emite `cleanup.error` para esta sub-fase; documentado en Pitfall #3 → log warn fail-open).
    - it PRUNE OPPORTUNISTIC: tras el remove ok, gitCalls incluye `['worktree', 'prune']`. Si prune falla, emite `worktree.cleanup.error{phase:'prune'}` pero el cleanup.ok previo ya fue emitido.

    **Sección 2 — E2E con git real (smoke, 1-2 escenarios):**
    - it E2E CLEAN: helper `makeIsolatedRepoWithWorktree()` crea tmpRepo con `git init -q` + 1 commit + `git worktree add <wt> -b sess-test` → llama `runStopHook` con `findSessionFn` mock devolviendo session con `worktree_path` → assert `!existsSync(wt)` + `!gitBranchList.includes('sess-test')` + `events.find(e => e.fields.event === 'worktree.cleanup.ok')`.
    - it E2E DIRTY: como anterior pero `appendFileSync(<wt>/file.txt, 'dirty')` antes del hook → assert `existsSync(<wt>.dirty)` + branch sigue existiendo + `events.find(e => e.fields.event === 'worktree.cleanup.dirty')`.
  </behavior>
  <action>
    Crear `test/stop-worktree-cleanup.test.js` con la estructura siguiente. Este task es RED-first: el archivo se crea ANTES de existir la implementación en stop.js. Esperado: este test FAILS hasta Task 2 lo implementa.

    ```javascript
    // @ts-check
    // Phase 19 WT-04: worktree cleanup en stop hook (fail-open).
    // Cobertura mixta unit (gitFn stub) + E2E smoke (git real con tmpdir).

    import { describe, it, beforeEach, afterEach } from 'node:test';
    import assert from 'node:assert/strict';
    import { execSync } from 'node:child_process';
    import { mkdtempSync, rmSync, mkdirSync, writeFileSync, appendFileSync, existsSync } from 'node:fs';
    import { tmpdir } from 'node:os';
    import { join } from 'node:path';
    import { runStopHook } from '../src/hooks/stop.js';

    function makeMemLogger() {
      const events = [];
      const logger = {
        info: (msg, fields) => events.push({ level: 'info', msg, fields }),
        warn: (msg, fields) => events.push({ level: 'warn', msg, fields }),
        error: (msg, fields) => events.push({ level: 'error', msg, fields }),
        debug: (msg, fields) => events.push({ level: 'debug', msg, fields }),
        child: () => logger,
      };
      return { logger, events };
    }

    function makeSession(overrides = {}) {
      return {
        session_id: 'sess-wt-clean-test',
        task_id: 'task-1',
        task_ref: 'KL-99',
        provider: 'plane',
        project_id: 'proj-1',
        project_path: '/tmp/project',
        worktree_path: '/tmp/project/.bg-shell/sess-wt-clean-test',
        summary: 'Test',
        status: 'review',
        started_at: new Date().toISOString(),
        workspace_ref: 'workspace:1',
        gsd: false,
        ...overrides,
      };
    }

    function makeStubCmux() {
      return {
        setColor: async () => {},
        notify: async () => {},
        listWorkspaces: async () => '',
        send: async () => {},
      };
    }

    function makeGitFnStub(handler) {
      const calls = [];
      const gitFn = (cwd, args) => {
        calls.push({ cwd, args });
        return handler(cwd, args) ?? '';
      };
      return { gitFn, calls };
    }

    describe('Phase 19 WT-04: worktree cleanup — unit (gitFn stub)', () => {
      it('CLEAN: removes worktree + deletes branch + emits cleanup.ok with branch_deleted=true', async () => {
        const session = makeSession();
        const { logger, events } = makeMemLogger();
        const { gitFn, calls } = makeGitFnStub((cwd, args) => {
          if (args.includes('--show-current')) return 'kodo-sess-wt-clean-test';
          if (args.includes('--porcelain')) return '';
          return '';
        });
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );

        const ok = events.find((e) => e.fields?.event === 'worktree.cleanup.ok');
        assert.ok(ok, 'must emit worktree.cleanup.ok');
        assert.equal(ok.level, 'info');
        assert.equal(ok.fields.branch_deleted, true);
        assert.equal(ok.fields.worktree_path, session.worktree_path);

        // Orden de calls: branch --show-current → status --porcelain → worktree remove → branch -D → prune
        const branchReadIdx = calls.findIndex((c) => c.args.includes('--show-current'));
        const statusIdx = calls.findIndex((c) => c.args.includes('--porcelain'));
        const removeIdx = calls.findIndex((c) => c.args[0] === 'worktree' && c.args[1] === 'remove');
        const branchDelIdx = calls.findIndex((c) => c.args[0] === 'branch' && c.args[1] === '-D');
        const pruneIdx = calls.findIndex((c) => c.args[0] === 'worktree' && c.args[1] === 'prune');
        assert.ok(branchReadIdx >= 0 && statusIdx > branchReadIdx, 'branch read BEFORE status (Pitfall #2)');
        assert.ok(removeIdx > statusIdx, 'remove after status');
        assert.ok(branchDelIdx > removeIdx, 'branch -D after remove');
        assert.ok(pruneIdx > branchDelIdx, 'prune oportunista al final (D-04)');
      });

      it('DIRTY: dirty status → move to .dirty/ + emit cleanup.dirty (D-02), branch preserved', async () => {
        const session = makeSession();
        const { logger, events } = makeMemLogger();
        const { gitFn, calls } = makeGitFnStub((cwd, args) => {
          if (args.includes('--show-current')) return 'kodo-sess-wt-clean-test';
          if (args.includes('--porcelain')) return 'M file.txt\n?? new.txt\n';
          return '';
        });
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );

        const dirty = events.find((e) => e.fields?.event === 'worktree.cleanup.dirty');
        assert.ok(dirty, 'must emit worktree.cleanup.dirty');
        assert.equal(dirty.level, 'warn');
        assert.equal(dirty.fields.moved_to, `${session.worktree_path}.dirty`);
        const branchDel = calls.find((c) => c.args[0] === 'branch' && c.args[1] === '-D');
        assert.equal(branchDel, undefined, 'branch must be PRESERVED when dirty (D-02)');
      });

      it('ERROR on remove: gitFn throws → emits cleanup.error{phase:remove} + fail-open', async () => {
        const session = makeSession();
        const { logger, events } = makeMemLogger();
        const gitFn = (cwd, args) => {
          if (args.includes('--show-current')) return 'kodo-sess-wt-clean-test';
          if (args.includes('--porcelain')) return '';
          if (args[0] === 'worktree' && args[1] === 'remove') throw new Error('EBUSY: rmdir failed');
          return '';
        };
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );

        const err = events.find((e) => e.fields?.event === 'worktree.cleanup.error');
        assert.ok(err, 'must emit worktree.cleanup.error');
        assert.equal(err.level, 'error');
        assert.equal(err.fields.phase, 'remove');
        assert.match(err.fields.reason, /EBUSY/);
      });

      it('TARGET COLLISION: <wt>.dirty already exists → suffixed path (Pitfall #1)', async () => {
        // Crear tmp real para que existsSync devuelva true en el target canónico
        const tmpBase = mkdtempSync(join(tmpdir(), 'kodo-collision-'));
        const wt = join(tmpBase, 'wt');
        const dirty = `${wt}.dirty`;
        mkdirSync(wt, { recursive: true });
        mkdirSync(dirty, { recursive: true }); // pre-existing collision target
        const session = makeSession({ worktree_path: wt });
        const { logger, events } = makeMemLogger();
        const { gitFn, calls } = makeGitFnStub((cwd, args) => {
          if (args.includes('--show-current')) return 'sess-x';
          if (args.includes('--porcelain')) return 'M something\n'; // dirty
          return '';
        });
        try {
          await runStopHook(
            { session_id: session.session_id, cwd: session.project_path },
            {
              findSessionFn: () => ({ id: session.task_id, session }),
              removeSessionFn: () => {},
              cmux: makeStubCmux(),
              loggerFactory: () => logger,
              gitFn,
            },
          );
          const moveCall = calls.find((c) => c.args[0] === 'worktree' && c.args[1] === 'move');
          assert.ok(moveCall, 'must call worktree move');
          const target = moveCall.args[3] ?? moveCall.args[moveCall.args.length - 1];
          assert.notEqual(target, dirty, 'target must NOT be the colliding path');
          assert.match(target, new RegExp(`^${wt.replace(/\//g, '\\/')}\\.dirty-`), 'must use suffixed variant');
          const dirtyEv = events.find((e) => e.fields?.event === 'worktree.cleanup.dirty');
          assert.equal(dirtyEv?.fields?.moved_to, target);
        } finally {
          rmSync(tmpBase, { recursive: true, force: true });
        }
      });

      it('LEGACY v0.5: no worktree_path → cleanup skipped silently (D-09)', async () => {
        const session = makeSession({ worktree_path: undefined });
        const { logger, events } = makeMemLogger();
        const { gitFn, calls } = makeGitFnStub(() => '');
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );
        assert.equal(calls.length, 0, 'gitFn must NOT be called for legacy v0.5 (D-09 silent)');
        const cleanupEvents = events.filter((e) => String(e.fields?.event || '').startsWith('worktree.cleanup.'));
        assert.equal(cleanupEvents.length, 0, 'no worktree.cleanup.* events for legacy (silent)');
      });

      it('BRANCH-D FAILURE: branch -D throws → cleanup.ok with branch_deleted=false (Pitfall #3 fail-open)', async () => {
        const session = makeSession();
        const { logger, events } = makeMemLogger();
        const gitFn = (cwd, args) => {
          if (args.includes('--show-current')) return 'sess-x';
          if (args.includes('--porcelain')) return '';
          if (args[0] === 'branch' && args[1] === '-D') throw new Error('cannot delete branch in use');
          return '';
        };
        await runStopHook(
          { session_id: session.session_id, cwd: session.project_path },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            gitFn,
          },
        );
        const ok = events.find((e) => e.fields?.event === 'worktree.cleanup.ok');
        assert.ok(ok, 'cleanup.ok must still be emitted');
        assert.equal(ok.fields.branch_deleted, false);
        const err = events.find((e) => e.fields?.event === 'worktree.cleanup.error');
        assert.equal(err, undefined, 'no cleanup.error for branch -D failure (warn-only per Pitfall #3)');
      });
    });

    describe('Phase 19 WT-04: worktree cleanup — E2E smoke (git real)', () => {
      let tmpBase;

      beforeEach(() => {
        tmpBase = mkdtempSync(join(tmpdir(), 'kodo-wt-e2e-'));
      });

      afterEach(() => {
        try { rmSync(tmpBase, { recursive: true, force: true }); } catch {}
      });

      function makeIsolatedRepoWithWorktree(branchName = 'kodo-sess-e2e') {
        const repo = join(tmpBase, 'repo');
        mkdirSync(repo, { recursive: true });
        const opts = { cwd: repo, encoding: 'utf-8', stdio: ['ignore', 'pipe', 'pipe'] };
        execSync('git init -q', opts);
        execSync('git config user.email "test@kodo.local"', opts);
        execSync('git config user.name "kodo test"', opts);
        execSync('git config commit.gpgsign false', opts);
        writeFileSync(join(repo, 'seed.txt'), 'seed');
        execSync('git add -A && git commit -q -m "seed"', opts);
        const wt = join(repo, '.bg-shell', 'sess-e2e');
        execSync(`git worktree add -b ${branchName} ${wt}`, opts);
        return { repo, wt, branchName };
      }

      it('E2E CLEAN: worktree removed + branch deleted on disk', async () => {
        const { repo, wt, branchName } = makeIsolatedRepoWithWorktree();
        const session = makeSession({ project_path: repo, worktree_path: wt, session_id: 'sess-e2e' });
        const { logger, events } = makeMemLogger();
        await runStopHook(
          { session_id: session.session_id, cwd: repo },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
            // gitFn default (execFileSync real)
          },
        );
        assert.equal(existsSync(wt), false, 'worktree dir must be removed');
        const branches = execSync('git branch', { cwd: repo, encoding: 'utf-8' });
        assert.ok(!branches.includes(branchName), `branch ${branchName} must be deleted`);
        assert.ok(events.find((e) => e.fields?.event === 'worktree.cleanup.ok'), 'cleanup.ok emitted');
      });

      it('E2E DIRTY: worktree moved to .dirty/ + branch preserved', async () => {
        const { repo, wt, branchName } = makeIsolatedRepoWithWorktree('kodo-sess-dirty');
        appendFileSync(join(wt, 'seed.txt'), 'uncommitted change');
        const session = makeSession({
          project_path: repo,
          worktree_path: wt,
          session_id: 'sess-e2e-dirty',
        });
        const { logger, events } = makeMemLogger();
        await runStopHook(
          { session_id: session.session_id, cwd: repo },
          {
            findSessionFn: () => ({ id: session.task_id, session }),
            removeSessionFn: () => {},
            cmux: makeStubCmux(),
            loggerFactory: () => logger,
          },
        );
        const dirtyEv = events.find((e) => e.fields?.event === 'worktree.cleanup.dirty');
        assert.ok(dirtyEv, 'cleanup.dirty emitted');
        assert.equal(existsSync(dirtyEv.fields.moved_to), true, 'moved_to path exists on disk');
        const branches = execSync('git branch', { cwd: repo, encoding: 'utf-8' });
        assert.ok(branches.includes(branchName), `branch ${branchName} preserved`);
      });
    });
    ```

    Notas críticas:
    - Este archivo se crea ANTES de implementar el cleanup en stop.js → tests fallarán (RED). Task 2 los pone verdes.
    - Si `runStopHook` aún no acepta `gitFn` en `deps`, los tests fallarán por undefined call — eso es esperado en RED.
    - Si el cleanup todavía no existe, los tests CLEAN/DIRTY/ERROR/COLLISION fallarán por ausencia de eventos. RED esperado.
    - LEGACY test debería pasar antes y después (no toca el cleanup path).
  </action>
  <verify>
    <automated>node --test test/stop-worktree-cleanup.test.js 2>&1 | grep -E "(ok |not ok |tests )" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/stop-worktree-cleanup.test.js` (archivo existe)
    - `grep -q "Phase 19 WT-04: worktree cleanup — unit" test/stop-worktree-cleanup.test.js`
    - `grep -q "Phase 19 WT-04: worktree cleanup — E2E smoke" test/stop-worktree-cleanup.test.js`
    - `grep -q "TARGET COLLISION" test/stop-worktree-cleanup.test.js`
    - `grep -q "LEGACY v0.5" test/stop-worktree-cleanup.test.js`
    - `grep -q "makeIsolatedRepoWithWorktree" test/stop-worktree-cleanup.test.js`
    - `grep -c "^  it(" test/stop-worktree-cleanup.test.js` ≥ 7 (mínimo 6 unit + 2 E2E)
    - `node --test test/stop-worktree-cleanup.test.js` — TDD RED esperado en este task (la mayoría de tests fallan porque la implementación llega en Task 2). Solo el LEGACY test puede pasar antes de Task 2.
  </acceptance_criteria>
  <done>Archivo creado con 7+ tests cubriendo CLEAN/DIRTY/ERROR/COLLISION/LEGACY/BRANCH-D-FAIL + 2 E2E smoke. Tests están en RED esperado — Task 2 los pasa a GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar cleanup en src/hooks/stop.js (D-01..D-04, D-07..D-10)</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    - src/hooks/stop.js completo (especialmente líneas 90-220 `runStopHook` body, líneas 195-200 `releaseGsdLock` analog, líneas 238-272 `handleOrchestratorStop` NO TOCAR)
    - src/logger-events.js (helpers de Plan 01: `worktreeCleanupOk/Dirty/Error`)
    - .planning/phases/19-worktree-cleanup-integration/19-RESEARCH.md §"Code Examples" Operations 1-5 (snippets verbatim para dirty check, branch read, clean cleanup, dirty move-aside, prune)
    - .planning/phases/19-worktree-cleanup-integration/19-RESEARCH.md §"Common Pitfalls" 1-6 (mitigaciones)
    - .planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md §1 (in-file analogs + restricciones duras D-07/D-05/D-09)
    - test/stop-worktree-cleanup.test.js (los tests creados en Task 1 — son la spec ejecutable)
  </read_first>
  <behavior>
    `runStopHook` debe:
    1. Aceptar `gitFn` en `deps` con default basado en `execFileSync('git', ['-C', cwd, ...args])`.
    2. DESPUÉS de `releaseGsdLock` (D-07), ANTES o ALREDEDOR de `removeSessionFn(id)` (orden discrecional), ejecutar el bloque cleanup.
    3. Si `!session.worktree_path` → return sin emitir nada (D-09 silent).
    4. Leer branch name PRIMERO (`git -C <wt> branch --show-current`), fail-open si throws.
    5. Leer status (`git -C <wt> status --porcelain`); si throws → emit `cleanup.error{phase:'status'}` + abort.
    6. Si CLEAN: `git -C <project> worktree remove <wt>` → si ok: `git -C <project> branch -D <branch>` (fail-open per Pitfall #3 → `branch_deleted: false`) → emit `cleanup.ok`. Si remove falla → emit `cleanup.error{phase:'remove'}`.
    7. Si DIRTY: existsSync check del target `<wt>.dirty`; si existe → usar `<wt>.dirty-<timestamp>`. `git -C <project> worktree move <wt> <newPath>`; si falla → fallback `renameSync + git worktree repair`; si ambos fallan → emit `cleanup.error{phase:'move'}`. Si ok → emit `cleanup.dirty{moved_to}`. NO borrar branch.
    8. Al final (siempre, salvo legacy skip): `git worktree prune` oportunista, fail-open con `cleanup.error{phase:'prune'}` si falla.
    9. NO tocar `handleOrchestratorStop` (líneas 238-272 actuales).

    Todo el bloque envuelto en su propio try/catch top-level que swallow errors (mismo principio que `try/catch` línea 219-221 actual). 5 try/catch internos por sub-fase para emitir eventos correctos.
  </behavior>
  <action>
    1. Añadir dep `gitFn` al destructuring de defaults en `runStopHook` (cerca de líneas 99-101):

    ```javascript
    const findSessionFn = deps.findSessionFn || findSession;
    const removeSessionFn = deps.removeSessionFn || removeSession;
    const cmuxClient = deps.cmux || cmux;
    const gitFn = deps.gitFn || (async (cwd, args) => {
      const { execFileSync } = await import('node:child_process');
      return execFileSync('git', ['-C', cwd, ...args], { encoding: 'utf-8' }).trim();
    });
    ```

    NOTE: si los tests stubbean gitFn como sync (no async), aceptar ambos. Mejor pattern: hacer el default async wrapper y los call sites `await gitFn(...)` — los stubs sync siguen siendo awaitable (Promise.resolve auto-wrap).

    2. Actualizar el JSDoc `@typedef` del deps object para incluir `gitFn?: (cwd: string, args: string[]) => Promise<string> | string`.

    3. Insertar el bloque cleanup ENTRE `releaseGsdLock` block (línea ~200, cierre del `if (session.gsd) { ... }`) y `removeSessionFn(id)` (línea ~203). Estructura recomendada (todo dentro de un try/catch outer para no romper el flow):

    ```javascript
    // Phase 19 WT-04: worktree cleanup fail-open (D-01..D-04, D-07..D-10).
    // Orden: branch read (Pitfall #2) → status → remove|move → branch -D (clean only) → prune.
    if (session.worktree_path) {
      try {
        const { existsSync, renameSync } = await import('node:fs');
        const {
          worktreeCleanupOk,
          worktreeCleanupDirty,
          worktreeCleanupError,
        } = await import('../logger-events.js');
        const cleanupLog = (deps && deps.loggerFactory)
          ? deps.loggerFactory({ session_id: session.session_id, task_id: session.task_id })
          : await (async () => {
              const { createLogger } = await import('../logger.js');
              return createLogger({
                sessionId: session.session_id,
                minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
              }).child({ component: 'hook', task_id: session.task_id });
            })();

        const wt = session.worktree_path;
        const project = session.project_path;

        // 1. Read branch name BEFORE remove (Pitfall #2 / D-08). Fail-open silent.
        let branchName = null;
        try {
          const out = await gitFn(project, ['-C', wt, 'branch', '--show-current']);
          branchName = (out || '').trim() || null;
        } catch (err) {
          console.error(`[kodo:stop] branch --show-current failed: ${err.message}`);
        }

        // 2. Dirty check (D-01).
        let isDirty;
        try {
          const status = await gitFn(project, ['-C', wt, 'status', '--porcelain']);
          isDirty = (status || '').length > 0;
        } catch (err) {
          worktreeCleanupError(cleanupLog, {
            session_id: session.session_id,
            worktree_path: wt,
            phase: 'status',
            reason: err.message,
          });
          // Skip the rest — we cannot decide without status. Still attempt prune at end.
          isDirty = null;
        }

        if (isDirty === false) {
          // 3a. CLEAN path: remove + branch -D.
          let removeOk = false;
          try {
            await gitFn(project, ['worktree', 'remove', wt]);
            removeOk = true;
          } catch (err) {
            worktreeCleanupError(cleanupLog, {
              session_id: session.session_id,
              worktree_path: wt,
              phase: 'remove',
              reason: err.message,
            });
          }
          if (removeOk) {
            let branchDeleted = false;
            if (branchName) {
              try {
                await gitFn(project, ['branch', '-D', branchName]);
                branchDeleted = true;
              } catch (err) {
                // Pitfall #3: branch checked-out by another worktree, race, etc. → warn fail-open.
                console.error(`[kodo:stop] branch -D ${branchName} failed: ${err.message}`);
              }
            }
            worktreeCleanupOk(cleanupLog, {
              session_id: session.session_id,
              worktree_path: wt,
              branch_deleted: branchDeleted,
            });
          }
        } else if (isDirty === true) {
          // 3b. DIRTY path: move-aside to <wt>.dirty (D-02), branch preserved.
          let target = `${wt}.dirty`;
          if (existsSync(target)) {
            // Pitfall #1: collision target → suffixed variant.
            target = `${wt}.dirty-${Date.now()}`;
          }
          let moveOk = false;
          let moveErrMsg = null;
          try {
            await gitFn(project, ['worktree', 'move', wt, target]);
            moveOk = true;
          } catch (err) {
            moveErrMsg = err.message;
            // Fallback (D-02): native rename + git worktree repair.
            try {
              renameSync(wt, target);
              await gitFn(project, ['worktree', 'repair', target]);
              moveOk = true;
            } catch (err2) {
              worktreeCleanupError(cleanupLog, {
                session_id: session.session_id,
                worktree_path: wt,
                phase: 'move',
                reason: `${moveErrMsg} | fallback: ${err2.message}`,
              });
            }
          }
          if (moveOk) {
            worktreeCleanupDirty(cleanupLog, {
              session_id: session.session_id,
              worktree_path: wt,
              moved_to: target,
            });
          }
        }
        // isDirty === null: status read failed → already emitted error, skip remove/move.

        // 4. Opportunistic prune (D-04). Fail-open with cleanup.error{phase:prune}.
        try {
          await gitFn(project, ['worktree', 'prune']);
        } catch (err) {
          worktreeCleanupError(cleanupLog, {
            session_id: session.session_id,
            worktree_path: wt,
            phase: 'prune',
            reason: err.message,
          });
        }
      } catch (outerErr) {
        // Defensive outer catch — runStopHook must NEVER crash Claude Code.
        console.error(`[kodo:stop] worktree cleanup outer error: ${outerErr.message}`);
      }
    }
    ```

    4. NO modificar `handleOrchestratorStop` (líneas 238-272). Confirmar visualmente que sigue con `cwd: KODO_ROOT` y `encoding: 'utf-8'` intactos (D-05).

    5. Mantener el `try/catch` top-level externo (línea 219-221 actual) — el cleanup ya tiene su outer catch propio, pero el top-level final sigue siendo el último seguro.

    6. Patrones críticos:
       - `gitFn(project, ['-C', wt, 'branch', '--show-current'])`: lectura del worktree (cwd del git binary = project, pero opera sobre `-C wt`).
       - `gitFn(project, ['worktree', 'remove', wt])`: cwd = project (NO `-C wt` — el worktree ya está siendo borrado).
       - `gitFn(project, ['branch', '-D', branchName])`: cwd = project (la branch vive en el repo principal).
       - `gitFn(project, ['worktree', 'prune'])`: cwd = project.
  </action>
  <verify>
    <automated>node --test test/stop-worktree-cleanup.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "session.worktree_path" src/hooks/stop.js` (legacy guard + uso)
    - `grep -q "worktreeCleanupOk" src/hooks/stop.js` (helper invocado)
    - `grep -q "worktreeCleanupDirty" src/hooks/stop.js`
    - `grep -q "worktreeCleanupError" src/hooks/stop.js`
    - `grep -q "branch --show-current" src/hooks/stop.js` (Pitfall #2)
    - `grep -q "status', '--porcelain" src/hooks/stop.js` (dirty check)
    - `grep -q "worktree', 'remove" src/hooks/stop.js`
    - `grep -q "worktree', 'move" src/hooks/stop.js`
    - `grep -q "worktree', 'prune" src/hooks/stop.js`
    - `grep -q "existsSync" src/hooks/stop.js` (Pitfall #1 pre-check)
    - `grep -q "deps.gitFn" src/hooks/stop.js` (DI dep)
    - **Orden**: `awk '/releaseGsdLock\(session\.project_path/{lock=NR} /worktree_path/{wt=NR; exit} END{exit !(lock < wt)}' src/hooks/stop.js` exit 0 (cleanup ocurre TRAS releaseGsdLock, D-07).
    - **Orden**: `awk '/branch --show-current/{br=NR} /worktree.+remove/{rm=NR; exit} END{exit !(br < rm)}' src/hooks/stop.js` exit 0 (branch read antes de remove, D-08 / Pitfall #2).
    - `node --test test/stop-worktree-cleanup.test.js` exit code 0 — todos los tests del Task 1 ahora pasan (GREEN).
    - `grep -q "cwd: KODO_ROOT" src/hooks/stop.js` (handleOrchestratorStop intacto, D-05).
  </acceptance_criteria>
  <done>Cleanup cableado fail-open con 5 try/catch internos (status, branch read, remove/move, branch -D, prune). Legacy v0.5 hace skip silencioso. Pitfalls #1 y #2 mitigados. handleOrchestratorStop SIN cambios. Todos los tests de Task 1 pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apuntar verify.js al worktree_path + extender tests gsd-verify-integration</name>
  <files>src/gsd/verify.js, test/gsd-verify-integration.test.js</files>
  <read_first>
    - src/gsd/verify.js completo (header JSDoc 1-31 + función `runGsdVerify` 71-181, especialmente líneas 121-124)
    - test/gsd-verify-integration.test.js completo (estructura `makeSession()` 37-52, `makeDeps()` 73-101, tests 103-fin)
    - .planning/phases/19-worktree-cleanup-integration/19-RESEARCH.md §"Code Examples" Operation 6 (snippet exacto del cambio)
    - .planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md §2 + §6 (verify.js + gsd-verify-integration.test.js)
    - .planning/phases/19-worktree-cleanup-integration/19-CONTEXT.md §"Verify lee VERIFICATION.md" (D-06, D-09)
  </read_first>
  <behavior>
    - `phasesRoot` resuelto con `join(session.worktree_path ?? session.project_path, '.planning', 'phases')` — campo aditivo opcional + fallback nullish coalescing.
    - Sesión con `worktree_path` (v0.6+): verify lee VERIFICATION.md desde el worktree.
    - Sesión legacy v0.5 sin `worktree_path`: verify lee desde project_path silently (D-09 — no emite warn).
    - Exit codes + bytes Plane comment IDÉNTICOS a v0.5 (Pitfall #6 Opción A invariante).
    - JSDoc del header actualizado para documentar el fallback.
  </behavior>
  <action>
    1. Abrir `src/gsd/verify.js`. Localizar líneas 121-124. Cambiar SOLO la línea 124:

    ```javascript
    // ANTES (línea 124):
    const phasesRoot = join(session.project_path, '.planning', 'phases');

    // DESPUÉS (línea 124):
    // Phase 19 D-06: lee del worktree cuando existe (sesiones v0.6+), fallback silent
    // a project_path para sesiones legacy v0.5 sin worktree_path (D-09).
    const phasesRoot = join(session.worktree_path ?? session.project_path, '.planning', 'phases');
    ```

    2. Actualizar el JSDoc del header de la función `runGsdVerify` (cerca de líneas 1-70, donde está documentado el parámetro `session`). Añadir una línea explicando el fallback:

    ```
     *   Phase 19 D-06: si `session.worktree_path` está presente (sesiones v0.6+
     *   tras Phase 18 wiring), phasesRoot resuelve dentro del worktree.
     *   Sesiones legacy v0.5 sin el campo siguen leyendo del project_path
     *   silently (D-09 — no se emite warn de fallback).
    ```

    Insertar esta nota inmediatamente debajo del bullet existente que describe el parámetro `session` o `phasesRoot` en el JSDoc. NO modificar otros parámetros documentados.

    3. NO TOCAR ningún otro código en `verify.js` — el resto de `runGsdVerify` (provider, addComment, finalize, orchestratorReview emit) queda intacto. Pitfall #6 Opción A invariante.

    4. Abrir `test/gsd-verify-integration.test.js`. Añadir fixture helpers después del `makeSession()` existente:

    ```javascript
    function makeSessionWithWorktree(tmpWorktree) {
      return {
        ...makeSession(),
        worktree_path: tmpWorktree, // D-06: phasesRoot resuelve aquí
      };
    }

    function makeLegacySession() {
      // D-09: sesión v0.5 sin worktree_path → fallback silent a project_path.
      return makeSession();
    }
    ```

    5. Añadir 3 `it()` nuevos al final del describe block existente:

    ```javascript
    it('Phase 19 D-06: verify reads VERIFICATION.md from worktree_path when present', async () => {
      // Sembrar VERIFICATION.md SOLO en el worktree (no en project_path).
      const wt = mkdtempSync(join(tmpdir(), 'kodo-verify-wt-'));
      try {
        const phaseDir = join(wt, '.planning', 'phases', '10-test');
        mkdirSync(phaseDir, { recursive: true });
        writeFileSync(
          join(phaseDir, 'VERIFICATION.md'),
          '---\nstatus: pass\n---\n# Phase 10\n## Approved\nAll green.\n',
        );
        const session = makeSessionWithWorktree(wt);
        const { deps, calls, events } = makeDeps(session);
        const verdict = await runGsdVerify({ sessionId: session.session_id }, deps);
        assert.equal(verdict.action, 'pass', 'must read VERIFICATION.md from worktree');
      } finally {
        rmSync(wt, { recursive: true, force: true });
      }
    });

    it('Phase 19 D-09: legacy session without worktree_path falls back to project_path silently', async () => {
      // Sembrar VERIFICATION.md SOLO en project_path; la sesión NO tiene worktree_path.
      const phaseDir = join(tmpRoot, '.planning', 'phases', '10-test');
      mkdirSync(phaseDir, { recursive: true });
      writeFileSync(
        join(phaseDir, 'VERIFICATION.md'),
        '---\nstatus: pass\n---\n# Phase 10\n## Approved\nLegacy OK.\n',
      );
      const session = makeLegacySession();
      assert.equal(session.worktree_path, undefined, 'precondition: legacy session has no worktree_path');
      const { deps, calls, events } = makeDeps(session);
      const verdict = await runGsdVerify({ sessionId: session.session_id }, deps);
      assert.equal(verdict.action, 'pass', 'must fall back to project_path');
      const warns = events.filter((e) => e.level === 'warn' && /fallback|worktree/i.test(String(e.msg || '')));
      assert.equal(warns.length, 0, 'no warn-level events for fallback (D-09 silent)');
    });

    it('Phase 19 D-06 source-hygiene: verify.js resolves phasesRoot with worktree_path nullish coalescing', () => {
      const source = readFileSync(VERIFY_SOURCE_PATH, 'utf-8');
      assert.ok(
        /session\.worktree_path\s*\?\?\s*session\.project_path/.test(source),
        'phasesRoot must use session.worktree_path ?? session.project_path (D-06 + D-09 fallback)',
      );
    });
    ```

    Si el archivo de test no importa `readFileSync` o no define `VERIFY_SOURCE_PATH`, añadir en el top del archivo:

    ```javascript
    import { readFileSync, mkdirSync, writeFileSync, mkdtempSync, rmSync } from 'node:fs';
    import { join, resolve, dirname } from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { tmpdir } from 'node:os';
    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);
    const VERIFY_SOURCE_PATH = resolve(__dirname, '..', 'src', 'gsd', 'verify.js');
    ```

    (Solo añadir lo que falte — no duplicar imports ya presentes.)
  </action>
  <verify>
    <automated>node --test test/gsd-verify-integration.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "session\.worktree_path \?\? session\.project_path" src/gsd/verify.js` (cambio quirúrgico exacto)
    - `grep -q "Phase 19 D-06" src/gsd/verify.js` (JSDoc/comentario referencia la decisión)
    - `grep -c "join(session\.project_path, '\.planning', 'phases')" src/gsd/verify.js` == 0 (la antigua línea ya no existe en su forma original)
    - `grep -q "Phase 19 D-06: verify reads VERIFICATION.md from worktree_path" test/gsd-verify-integration.test.js`
    - `grep -q "Phase 19 D-09: legacy session without worktree_path" test/gsd-verify-integration.test.js`
    - `grep -q "Phase 19 D-06 source-hygiene" test/gsd-verify-integration.test.js`
    - `grep -q "makeSessionWithWorktree" test/gsd-verify-integration.test.js`
    - `node --test test/gsd-verify-integration.test.js` exit code 0 (todos los tests existentes + 3 nuevos pasan)
    - Pitfall #6 Opción A preservada: `grep -q "orchestratorReview" src/gsd/verify.js` sigue devolviendo matches (el invariante de Phase 10 D-04/D-17 no cambia)
  </acceptance_criteria>
  <done>verify.js:124 cambiado a 1 línea con `??` fallback + JSDoc actualizado. 3 tests nuevos en gsd-verify-integration.test.js pasan. Legacy v0.5 sigue funcionando silently. Pitfall #6 Opción A invariante.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Source-hygiene asserts en test/stop.test.js (D-05/D-07/D-08 invariants)</name>
  <files>test/stop.test.js</files>
  <read_first>
    - test/stop.test.js completo (especialmente describe `stop.js source hygiene` y el test existente `releases lock before removeSession` líneas ~41-51)
    - .planning/phases/19-worktree-cleanup-integration/19-PATTERNS.md §7 (analog + diff sugerido)
    - src/hooks/stop.js (resultado de Task 2 — los markers que los grep tests buscan deben existir tras Task 2)
  </read_first>
  <behavior>
    Tres source-hygiene asserts nuevos como "living documentation":
    - D-07: la mención de `worktreeCleanupOk` (o equivalente cleanup marker) aparece DESPUÉS de `releaseGsdLock(session.project_path` en el source.
    - D-08 / Pitfall #2: `branch --show-current` aparece ANTES de `worktree remove` (ya sea como string literal de execSync o como array element de execFileSync).
    - D-05 (WT-05 satisfied-by-design): el bloque `handleOrchestratorStop` (regex match contra la función completa) preserva `cwd: KODO_ROOT`.
  </behavior>
  <action>
    Añadir 3 `it()` al final del describe `stop.js source hygiene` (o equivalente) en `test/stop.test.js`:

    ```javascript
    it('Phase 19 D-07: worktree cleanup happens AFTER releaseGsdLock', () => {
      const source = readFileSync(STOP_SOURCE_PATH, 'utf-8');
      const lockIdx = source.indexOf('releaseGsdLock(session.project_path');
      assert.ok(lockIdx > 0, 'must find releaseGsdLock call site');
      // Cleanup marker: any of the cleanup helpers or the session.worktree_path branch.
      const cleanupIdx = source.indexOf('worktreeCleanupOk');
      assert.ok(cleanupIdx > 0, 'must find worktree cleanup block (worktreeCleanupOk)');
      assert.ok(
        lockIdx < cleanupIdx,
        'cleanup must come AFTER releaseGsdLock (Phase 19 D-07)',
      );
    });

    it('Phase 19 D-08 / Pitfall #2: branch --show-current is read BEFORE worktree remove', () => {
      const source = readFileSync(STOP_SOURCE_PATH, 'utf-8');
      const showCurrentIdx = source.indexOf("'--show-current'");
      // worktree remove as execFileSync array literal: ['worktree', 'remove', ...]
      // Use a regex robust to both 'worktree', 'remove' and 'worktree remove' string forms.
      const removeMatches = [];
      const re = /'worktree',\s*'remove'/g;
      let m;
      while ((m = re.exec(source)) !== null) removeMatches.push(m.index);
      const removeIdx = removeMatches[0] ?? -1;
      assert.ok(showCurrentIdx > 0, 'must reference branch --show-current');
      assert.ok(removeIdx > 0, 'must reference worktree remove');
      assert.ok(
        showCurrentIdx < removeIdx,
        'branch --show-current must precede worktree remove (Pitfall #2 / D-08)',
      );
    });

    it('Phase 19 D-05 (WT-05 satisfied-by-design): handleOrchestratorStop preserves cwd: KODO_ROOT', () => {
      const source = readFileSync(STOP_SOURCE_PATH, 'utf-8');
      const match = source.match(/async function handleOrchestratorStop[\s\S]*?\n}/);
      assert.ok(match, 'must find handleOrchestratorStop function');
      assert.ok(
        match[0].includes('cwd: KODO_ROOT'),
        'handleOrchestratorStop must preserve cwd: KODO_ROOT (Phase 19 D-05; auto-commit excluded from worktree per Phase 18 D-06)',
      );
    });
    ```

    Si `STOP_SOURCE_PATH` o `readFileSync` no están importados en `test/stop.test.js`, añadirlos en el top del archivo:

    ```javascript
    import { readFileSync } from 'node:fs';
    import { resolve, dirname } from 'node:path';
    import { fileURLToPath } from 'node:url';
    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);
    const STOP_SOURCE_PATH = resolve(__dirname, '..', 'src', 'hooks', 'stop.js');
    ```

    (No duplicar — el archivo de test ya tiene este pattern por el test existente "releases lock before removeSession".)

    NO modificar el test existente "releases lock before removeSession (order matters)" — sigue siendo source-of-truth Phase 16.
  </action>
  <verify>
    <automated>node --test test/stop.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 19 D-07: worktree cleanup happens AFTER releaseGsdLock" test/stop.test.js`
    - `grep -q "Phase 19 D-08 / Pitfall #2: branch --show-current is read BEFORE worktree remove" test/stop.test.js`
    - `grep -q "Phase 19 D-05 (WT-05 satisfied-by-design): handleOrchestratorStop preserves cwd: KODO_ROOT" test/stop.test.js`
    - `node --test test/stop.test.js` exit code 0 (los 3 asserts pasan tras Task 2 + el test legacy "releases lock before removeSession" sigue verde)
    - `grep -q "releases lock before removeSession" test/stop.test.js` (test legacy preservado intacto)
  </acceptance_criteria>
  <done>3 source-hygiene asserts añadidos. Pasan tras Task 2. Living documentation de D-05/D-07/D-08 cableada para futuras regresiones.</done>
</task>

</tasks>

<verification>
- `node --test test/stop-worktree-cleanup.test.js` exit 0 — 7+ tests pasan (CLEAN/DIRTY/ERROR/COLLISION/LEGACY/BRANCH-D-FAIL + 2 E2E smoke).
- `node --test test/gsd-verify-integration.test.js` exit 0 — tests existentes + 3 nuevos (D-06 worktree, D-09 fallback, source-hygiene) pasan.
- `node --test test/stop.test.js` exit 0 — 3 source-hygiene asserts nuevos + tests existentes pasan.
- `node --test test/logger-events.test.js` exit 0 — Plan 01 no regrese.
- `node --test test/skill-auto-commit.test.js` exit 0 — auto-commit E2E preservado (WT-05 satisfied-by-design).
- `npm test` (full suite) verde — sin regresiones en el resto del proyecto.
- Source-hygiene grep manual:
  - `awk '/releaseGsdLock\(session\.project_path/{lock=NR} /worktreeCleanupOk/{wt=NR; exit} END{exit !(lock < wt)}' src/hooks/stop.js` → exit 0 (D-07).
  - `awk '/--show-current/{br=NR} /worktree.+remove/{rm=NR; exit} END{exit !(br < rm)}' src/hooks/stop.js` → exit 0 (D-08 / Pitfall #2).
  - `grep -c "cwd: KODO_ROOT" src/hooks/stop.js` ≥ 1 (D-05 preservado).
  - `grep -c "session\.worktree_path \?\? session\.project_path" src/gsd/verify.js` == 1 (D-06).
- Manual smoke (opcional, no bloqueante): `kodo run <task-real>` con worktree, trabajar hasta clean, `cmux stop <sid>`, verificar `git worktree list` no incluye el path (cubierto en 19-VALIDATION.md §Manual-Only Verifications).
</verification>

<success_criteria>
1. **SC#1 cumplido (WT-04):** Stop hook fail-open removes worktree limpio + borra branch, mueve dirty a `.dirty/` (o `.dirty-<ts>/` en colisión), emite los 3 eventos NDJSON dedicados, hace prune oportunista. Cubierto por `test/stop-worktree-cleanup.test.js` (7+ tests unit + 2 E2E git real).
2. **SC#2 cumplido (WT-05 satisfied-by-design):** `handleOrchestratorStop` SIN cambios funcionales — sigue con `cwd: KODO_ROOT` (D-05). `KODO_ROOT` env override intacto. `test/skill-auto-commit.test.js` no requiere cambios y sigue verde. Source-hygiene assert en `test/stop.test.js` blinda el invariante.
3. **SC#3 cumplido (WT-06):** `verify.js:124` resuelve `phasesRoot` con `session.worktree_path ?? session.project_path`. Sesiones v0.6+ leen del worktree; legacy v0.5 siguen leyendo del project_path silently (D-09). Pitfall #6 Opción A (exit codes + bytes Plane comment) invariante.
4. **Compat legacy v0.5:** Cleanup hace skip silencioso si `!session.worktree_path`. Verify mismo. Sin warn extra.
5. **No regresiones:** `npm test` verde end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/19-worktree-cleanup-integration/19-02-SUMMARY.md` con:
- Decisiones aplicadas (D-01..D-04, D-06..D-10 y referencia a D-05 satisfied-by-design)
- Sitio exacto del cleanup en stop.js (líneas resultantes tras la inyección)
- Pitfalls #1 (collision pre-check) y #2 (branch-before-remove) mitigados — referencia a tests que blindan
- Cobertura WT-04 / WT-05 / WT-06 (qué test pasa cada requirement)
- Resultados de `npm test` (count de tests + estado)
- Deuda residual referenciada: bug latente `findSession` no busca en `state.history` (deferido, capturado en 19-CONTEXT.md §Deferred)
</output>
