---
phase: 26-config-wizard-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.js
  - src/cli/polling.js
  - src/config.js
  - test/cli/wizard-github.test.js
  - test/cli/polling.test.js
  - test/migration.test.js
  - test/fixtures/configs/v0.6-no-github.json
  - test/fixtures/configs/v0.7-with-github.json
autonomous: true
requirements:
  - CFG-01
  - CFG-02
requirements_addressed:
  - CFG-01
  - CFG-02
tags:
  - cli
  - wizard
  - config
  - github
  - migration
user_setup: []

must_haves:
  truths:
    - "Operador ejecuta `kodo config`, selecciona provider github, y persiste `providers.github = {api_key_env, repos:[{owner,repo}], poll_interval, mcp_hint, states}` en ~/.kodo/config.json"
    - "GITHUB_TOKEN value NUNCA escrito a config.json (solo se persiste `api_key_env` con el NOMBRE de la variable)"
    - "Wizard auto-detecta el repo origin con `git remote get-url origin` cuando cwd es un repo git con remoto github.com"
    - "Wizard re-prompts (no recursión) ante repo inválido (sin exactamente un `/`); manual loop termina con Enter vacío"
    - "Configs v0.6 sin clave `providers.github` cargan idénticas via `loadConfig` (zero breaking change CFG-02)"
    - "`DEFAULT_CONFIG` NO incluye `providers.github` (D-08 invariante)"
  artifacts:
    - path: "src/cli/polling.js"
      provides: "configureGithubProvider({ask,execGitRemote,providerConfig}) + parseGitHubRemote(url) + detectOriginRepo(exec?) exports"
      contains: "export async function configureGithubProvider"
    - path: "src/config.js"
      provides: "getDefaultGithubProviderConfig() factory export (shape D-06 verbatim)"
      contains: "export function getDefaultGithubProviderConfig"
    - path: "test/cli/wizard-github.test.js"
      provides: "≥6 casos scripted-readline para configureGithubProvider"
    - path: "test/cli/polling.test.js"
      provides: "≥5 casos unit para parseGitHubRemote/detectOriginRepo (fixtures SSH+HTTPS+HTTPS.git+gitlab+empty)"
    - path: "test/fixtures/configs/v0.6-no-github.json"
      provides: "Config fixture v0.6 con provider:plane y SIN providers.github"
    - path: "test/fixtures/configs/v0.7-with-github.json"
      provides: "Config fixture v0.7 con providers.github poblado (shape D-06)"
  key_links:
    - from: "src/cli.js interactiveConfig() branch github"
      to: "src/cli/polling.js configureGithubProvider"
      via: "dynamic import('./cli/polling.js')"
      pattern: "configureGithubProvider\\("
    - from: "src/cli/polling.js configureGithubProvider"
      to: "src/cli/polling.js detectOriginRepo + parseGitHubRemote"
      via: "local import"
      pattern: "detectOriginRepo\\("
    - from: "test/migration.test.js"
      to: "test/fixtures/configs/v0.6-no-github.json"
      via: "JSON.parse(readFileSync(...))"
      pattern: "v0\\.6-no-github\\.json"
    - from: "interactiveConfig availableProviders array"
      to: "['plane', 'github']"
      via: "literal extension D-01"
      pattern: "availableProviders\\s*=\\s*\\['plane',\\s*'github'\\]"
---

<objective>
Extender el wizard `kodo config` con la rama `provider: github` (D-01..05) y sembrar el schema `providers.github` (D-06..08) sin tocar `DEFAULT_CONFIG`. Cubre CFG-01 (wizard) + CFG-02 (zero-breaking-change).

Purpose: El operador puede configurar `provider: github` interactivamente; las configs v0.6 siguen leyéndose idéntico (zero breaking change demostrado por fixture test).

Output:
- `src/cli/polling.js` nuevo con 3 exports: `configureGithubProvider`, `parseGitHubRemote`, `detectOriginRepo` (los handlers CLI del daemon van en Plan 26-02).
- `src/config.js` extendido con `getDefaultGithubProviderConfig()` factory (D-08: NO modificar DEFAULT_CONFIG).
- `src/cli.js` extendido en 2 puntos: `availableProviders` array y rama branch `github` dentro de `interactiveConfig`.
- 2 fixtures de config (v0.6 y v0.7) en `test/fixtures/configs/`.
- 3 test files (wizard + helpers + migration extension) cubriendo ≥10 casos CFG-01+CFG-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md
@.planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md
@.planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md
@.planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md

<interfaces>
<!-- Extraídos del repo actual (verificados con grep). Executor consume estos contratos directamente. -->

From src/config.js (existing exports — DO NOT MODIFY contract):
- `export function migrateConfig(rawConfig)` — line 82, short-circuit at line 83 si `rawConfig.providers` existe
- `export function loadConfig()` — line ~119, retorna `{ ...DEFAULT_CONFIG }` si CONFIG_PATH ausente
- `export function saveConfig(config)` — atomic write
- `export function getProviderApiKey(providerName)` — line 172, provider-agnostic (cubre 'github')
- `export const DEFAULT_CONFIG` — line 32 — D-08: NO modificar
- `export { KODO_DIR, CONFIG_PATH, PROJECTS_PATH, DEFAULT_CONFIG }` — line 176

From src/cli.js (existing structure — extender quirúrgico):
- `interactiveConfig()` — line 344, async function
- `availableProviders` array — line 356 (currently `['plane']` — D-01 extiende a `['plane', 'github']`)
- Plane branch wizard — line 391-400 (mirror para shape, NO modificar)
- `rl.close(); return;` early-return pattern — line 383-386 (Plane apiKey missing)
- `return interactiveConfig()` recursion — line 519 (ANTI-PATTERN para rama github per Pitfall #9)

From src/triggers/polling.js (Phase 25 — referencia, NO modificar):
- `startPolling({provider?, client?, repos, intervalSec?, clock?, logger?, statePath?, dispatchTriggerFn?}) -> {stop}` — line 415
- `saveStateCache` atomic pattern (mirror para Plan 26-02 PID file) — line 149-154

From .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md:
- Pattern A (color isolation) — createFormatter solo desde src/cli/format.js
- Pattern B (DI handler exit codes) — opcional para configureGithubProvider (no exit code, solo mutate providerConfig)
- Pattern H (early-return wizard) — NO `return interactiveConfig()` recursión
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Crear test fixtures + scaffolds de test (RED state)</name>
  <files>test/fixtures/configs/v0.6-no-github.json, test/fixtures/configs/v0.7-with-github.json, test/cli/wizard-github.test.js, test/cli/polling.test.js, test/migration.test.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 685-725 — fixture content verbatim D-06)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 540-553 — parseGitHubRemote FIXTURES verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 600-643 — wizard-github.test.js scripted-readline pattern)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 740-763 — migration.test.js extension pattern)
    - test/migration.test.js (estructura existente — extender no reescribir)
    - test/skill-sync.test.js:18-79 (HOME-isolated boilerplate analog)
  </read_first>
  <behavior>
    - test/cli/wizard-github.test.js debe contener al menos: "happy path: auto-detect + confirm + save", "rejects invalid repo (no slash) → re-prompts", "manual entry succeeds after rejecting auto-detect", "rejects auto-detected repo, add manual one", "token never persisted to providerConfig (security T-26-01 / D-02)", "detectOriginRepo fail-open when execGitRemote throws" (6 casos).
    - test/cli/polling.test.js debe contener describe('parseGitHubRemote') con ≥5 fixtures: `git@github.com:owner/repo.git` → `{owner,repo}`, `https://github.com/owner/repo` → `{owner,repo}`, `https://github.com/owner/repo.git` → `{owner,repo}`, `https://gitlab.com/owner/repo.git` → `null`, `''` → `null`, `https://github.enterprise.example.com/o/r` → `null`. (El resto de casos polling.* van en Plan 26-02.)
    - test/migration.test.js extiende con describe('config v0.6 → v0.7 zero-breaking-change (Phase 26)'): "fixture v0.6 sin providers.github carga idéntica via migrateConfig (mismo reference)", "fixture v0.7 carga idempotente (migrateConfig dos veces === mismo)", "DEFAULT_CONFIG NO contiene providers.github (D-08)".
    - Todos los imports deben apuntar a `../../src/cli/polling.js` (configureGithubProvider, parseGitHubRemote, detectOriginRepo) y `../../src/config.js` (DEFAULT_CONFIG, getDefaultGithubProviderConfig, migrateConfig) — Tasks 2-3 hacen GREEN.
    - Fixture v0.6-no-github.json verbatim per 26-PATTERNS.md líneas 688-703 (provider:plane, providers.plane completo, sin providers.github).
    - Fixture v0.7-with-github.json verbatim per 26-PATTERNS.md líneas 710-724 (D-06 shape: api_key_env GITHUB_TOKEN, repos [{klab,kodo}], poll_interval 60, mcp_hint, states.review closed).
  </behavior>
  <action>Crear el directorio `test/cli/` y `test/fixtures/configs/` (no existen). Escribir las 2 fixtures JSON verbatim per 26-PATTERNS.md. Escribir los 2 test files nuevos (`wizard-github.test.js`, `polling.test.js`) usando node:test + node:assert/strict con la estructura DI definida en 26-PATTERNS.md. Extender `test/migration.test.js` con el describe nuevo (mirror el style de los describes existentes, no reescribir el archivo). Helper `scriptedAsk(answers)` definido en cada test file que lo necesite. Todos los tests deben FAIL en este punto (RED state) porque `configureGithubProvider`/`parseGitHubRemote`/`detectOriginRepo`/`getDefaultGithubProviderConfig` aún no existen — esto es el contrato RED→GREEN del flujo TDD. NO usar `setTimeout` real ni live API calls.</action>
  <verify>
    <automated>node --test test/cli/wizard-github.test.js test/cli/polling.test.js test/migration.test.js 2>&1 | grep -E "fail|error" | head -20 ; ls test/fixtures/configs/v0.6-no-github.json test/fixtures/configs/v0.7-with-github.json</automated>
  </verify>
  <done>Las 2 fixtures existen con shape verbatim; los 3 test files contienen los casos enumerados arriba y FAIL con módulo no encontrado (`Cannot find module '../../src/cli/polling.js'` o similar) — éste es el RED esperado de Wave 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar src/cli/polling.js (helpers parser + wizard branch DI)</name>
  <files>src/cli/polling.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 175-200 — parseGitHubRemote + detectOriginRepo verbatim, RESEARCH §Example 5)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 203-244 — configureGithubProvider verbatim, RESEARCH §Example 1)
    - .planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md (D-02, D-03, D-04, D-06 — shape locked)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Pitfall #6 detectOriginRepo fail-open, Pitfall #9 NO recursión)
    - src/cli.js:344-525 (interactiveConfig Plane branch — referencia de estilo)
  </read_first>
  <behavior>
    - Exports requeridos: `parseGitHubRemote(url) → {owner,repo}|null`, `detectOriginRepo(exec?) → {owner,repo}|null`, `configureGithubProvider({ask, execGitRemote?, providerConfig}) → Promise<void>` (mutates providerConfig in place).
    - `parseGitHubRemote` regex: `/github\.com[:/]([^/]+)\/([^/.\s]+?)(?:\.git)?(?:\/|\s|$)/` — match `[1]` owner, `[2]` repo.
    - `detectOriginRepo` envuelve `execSync('git remote get-url origin', { stdio: ['ignore','pipe','ignore'] })` en try/catch; retorna `null` ante cualquier throw (Pitfall #6 fail-open).
    - `configureGithubProvider` pasos en orden:
      1. `providerConfig.api_key_env = (await ask('  Variable de entorno para API key [GITHUB_TOKEN]: ')).trim() || providerConfig.api_key_env || 'GITHUB_TOKEN'` — NO escribe value del token (T-26-01 / D-02).
      2. `providerConfig.repos = providerConfig.repos || []`.
      3. Auto-detect: `const detected = detectOriginRepo(execGitRemote)`; si presente, ask `'  Detectado: <owner>/<repo> — ¿añadir? [S/n]: '`; aceptar si respuesta vacía o `s`/`S`.
      4. Loop manual: `while (true)`, ask `'  Repo (owner/repo, Enter para terminar): '`; break si trim vacío; split por `/` debe dar exactamente 2 partes no-vacías; si invalido `continue` (NO recursión — Pitfall #9 / Pattern H).
      5. Aplicar defaults D-06: `poll_interval = providerConfig.poll_interval || 60`; `mcp_hint = providerConfig.mcp_hint || 'GitHub MCP server'`; `states = providerConfig.states || { review: 'closed' }`.
    - NO imports de `picocolors` directos (Pattern A — invariante color isolation v0.5). NO escritura de filesystem en este módulo. NO escritura del token en ningún campo.
  </behavior>
  <action>Crear `src/cli/polling.js` con `// @ts-check`, imports mínimos (`node:child_process` para `execSync`), y los 3 exports descritos arriba siguiendo verbatim el shape de 26-PATTERNS.md líneas 175-244. Los handlers CLI del daemon (`runPollingStartCli`, `runPollingStopCli`, `runPollingStatusCli`) NO van en este plan — van en Plan 26-02. Este archivo es el primer drop; Plan 26-02 lo extenderá con los handlers daemon.</action>
  <verify>
    <automated>node --test test/cli/wizard-github.test.js test/cli/polling.test.js 2>&1 | tail -5 ; grep -c "from\s*['\"]picocolors['\"]" src/cli/polling.js | grep -q '^0$' && echo "color-isolation OK" || echo "FAIL: picocolors import detected"</automated>
  </verify>
  <done>`node --test test/cli/wizard-github.test.js test/cli/polling.test.js` pasa 100% de los casos definidos en Task 1 (≥11 casos: 6 wizard + 5+ parser). Source-hygiene grep confirma cero imports de picocolors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Añadir getDefaultGithubProviderConfig() en src/config.js + wire wizard branch en src/cli.js</name>
  <files>src/config.js, src/cli.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 428-450 — getDefaultGithubProviderConfig factory verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 350-375 — wizard branch extension verbatim D-01/D-05)
    - .planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md (D-06, D-08 — shape locked, NO modificar DEFAULT_CONFIG)
    - src/config.js (líneas 32-67 DEFAULT_CONFIG — D-08: NO TOUCH; línea 176 export list — ampliar)
    - src/cli.js (línea 344-525 interactiveConfig completo; línea 356 availableProviders; línea 391-400 Plane branch shape)
  </read_first>
  <behavior>
    - `src/config.js`: añadir `export function getDefaultGithubProviderConfig()` que retorna verbatim el shape D-06: `{ api_key_env: 'GITHUB_TOKEN', repos: [], poll_interval: 60, mcp_hint: 'GitHub MCP server', states: { review: 'closed' } }`. NO modificar `DEFAULT_CONFIG` (D-08 — `'github' in DEFAULT_CONFIG.providers === false`). NO modificar `migrateConfig` (CFG-02 short-circuit existente en línea 83 ya cubre v0.6→v0.7).
    - `src/cli.js`: cambiar línea 356 de `const availableProviders = ['plane'];` a `const availableProviders = ['plane', 'github'];` (D-01).
    - `src/cli.js`: insertar rama `if (selectedProvider === 'github') { ... }` entre el bloque post-selección y la Plane branch (línea ~388-390). La rama debe:
      1. Importar dinámicamente `configureGithubProvider` desde `./cli/polling.js` y `getDefaultGithubProviderConfig` desde `./config.js`.
      2. Inicializar `config.providers.github = config.providers.github || getDefaultGithubProviderConfig()` (D-08 — solo runtime, no en DEFAULT_CONFIG).
      3. Llamar `await configureGithubProvider({ ask, providerConfig: config.providers.github })`.
      4. Asignar `config.provider = 'github'`.
      5. D-05 resumen final: imprimir lista repos + poll_interval; ask `'\n  Guardar? [S/n]: '`; abortar si respuesta `n`/`N` (NO escribir config).
      6. `saveConfig(config)` + mensaje `✓ Configuracion guardada en ~/.kodo/` + `rl.close(); return;` (Pattern H — NO caer al bloque Plane projects listing, NO recursión a interactiveConfig).
    - Cero cambios en la rama Plane existente (Karpathy Rule 3 — surgical, mirror `src/cli.js:383-386` early-return).
  </behavior>
  <action>Editar `src/config.js`: añadir el export `getDefaultGithubProviderConfig()` antes de la línea 176 (export list). Editar `src/cli.js`: cambio quirúrgico de `availableProviders` (D-01) e inserción de la rama github mirror el shape de PATTERNS.md líneas 350-375. NO mover, NO renombrar, NO refactorizar la rama Plane existente. El `ask` helper ya existe localmente en `interactiveConfig` (revisar línea 344+ para su nombre exacto y reusarlo tal cual).</action>
  <verify>
    <automated>node --test test/cli/wizard-github.test.js test/cli/polling.test.js test/migration.test.js 2>&1 | tail -8 ; node -e "import('./src/config.js').then(m => { console.log('has factory:', typeof m.getDefaultGithubProviderConfig === 'function'); console.log('DEFAULT no github:', !('github' in (m.DEFAULT_CONFIG?.providers || {}))); })"</automated>
  </verify>
  <done>Los 3 test files pasan al 100% (Task 1 + Task 2 + Task 3 verdes). El factory `getDefaultGithubProviderConfig` retorna shape D-06 verbatim. `DEFAULT_CONFIG.providers` NO contiene la clave `github`. La suite full `npm test` sigue verde (baseline 715 + ≥10 nuevos = ≥725 pass, 0 fail).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator stdin → wizard | Input no confiable (typo, paste de credencial accidental, paths maliciosos) |
| filesystem ~/.kodo/ | Trust boundary del usuario; otros usuarios del sistema no deben leer secrets |
| git remote URL → parser | URL de origin puede venir de un repo malicioso (clone de URL atacante) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | configureGithubProvider() | mitigate | NUNCA persistir el VALUE de `GITHUB_TOKEN`; solo el NOMBRE de la env var via `api_key_env`. Test guard: `wizard-github.test.js` "token never persisted to providerConfig" — assert `/ghp_\|github_pat_/.test(JSON.stringify(providerConfig)) === false` y `'GITHUB_TOKEN' in providerConfig === false`. |
| T-26-INJ | Injection | parseGitHubRemote regex | mitigate | Regex anchored a `github.com[:/]` + clases de caracteres `[^/]+` para owner y `[^/.\s]+?` para repo. Rechaza `enterprise.github.com`, `gitlab.com`, URLs vacías. Test fixtures verbatim cubren los 6 casos. |
| T-26-SHELL | Injection | detectOriginRepo execSync | mitigate | `execSync` con array-form NO usado (string `'git remote get-url origin'` sin args operator-controlled); no hay shell interpolation porque el comando es literal. `stdio: ['ignore','pipe','ignore']` previene leak de stderr. |
| T-26-MIG | Tampering | migrateConfig backward compat | mitigate | D-07/D-08 — NO añadir nueva migración; CFG-02 invariante demostrada por fixture v0.6 (test "v0.6 zero-breaking-change"). Si emerge regresión: el test fail loud antes del commit. |
| T-26-RECURSION | DoS (UX) | configureGithubProvider manual loop | accept→mitigate | Pitfall #9 — NUNCA `return interactiveConfig()` desde la rama github; `continue` en el while loop ante input inválido. Test guard: "rejects invalid repo (no slash) → re-prompts" assert que tras invalid input + repo válido + Enter, el flow termina exitosamente. |
| T-26-FORK | Information Disclosure (UX) | detectOriginRepo origin auto-detect | accept | Si el operador clonó un fork (origin = `myuser/kodo` en vez de upstream `klab/kodo`), el wizard sugiere el fork. Mitigation: D-03 prompt `[S/n]` permite rechazar y entrar manualmente. Documentado en RESEARCH §Assumption A5. Defer multi-remote auto-detect a v0.8+ per CONTEXT Deferred Ideas. |

</threat_model>

<verification>
- `node --test test/cli/wizard-github.test.js test/cli/polling.test.js test/migration.test.js` — todos los casos green (≥10 casos: 6 wizard + 5 parser + 3 migration).
- `npm test` — full suite ≥725 pass, 0 fail.
- `grep -v '^//\|^\s*\*' src/cli/polling.js | grep -c "from\s*['\"]picocolors['\"]"` — debe ser `0` (Pattern A color isolation invariante).
- `node -e "import('./src/config.js').then(m => process.exit('github' in (m.DEFAULT_CONFIG?.providers || {}) ? 1 : 0))"` — exit 0 (D-08 invariante).
- Manual smoke (out-of-band, no en CI): `HOME=$(mktemp -d) bin/kodo config` → elegir github → confirmar 2 prompts → verificar `~/.kodo/config.json` contiene `providers.github` con shape D-06 y NO contiene `GITHUB_TOKEN` literal.
</verification>

<success_criteria>
- [ ] CFG-01: `kodo config` rama `provider: github` ejecuta sin crash y persiste `providers.github = {api_key_env, repos:[{owner,repo}], poll_interval:60, mcp_hint, states.review:'closed'}` — assert via wizard-github.test.js "happy path".
- [ ] CFG-01: Token value NUNCA escrito a providerConfig — assert via wizard-github.test.js "token never persisted".
- [ ] CFG-02: fixture v0.6 sin providers.github carga via `migrateConfig` sin inyectar la clave — assert via migration.test.js "v0.6 zero-breaking-change".
- [ ] CFG-02: `DEFAULT_CONFIG.providers` NO contiene `github` — assert via migration.test.js "DEFAULT_CONFIG sin github".
- [ ] CFG-02: fixture v0.7 idempotente (migrate dos veces === primero) — assert via migration.test.js "v0.7 idempotent".
- [ ] `src/cli/polling.js` cero imports directos de picocolors — assert via source-hygiene grep.
- [ ] Suite full `npm test` baseline 715 + ≥10 nuevos casos, 0 fail.
</success_criteria>

<output>
After completion, create `.planning/phases/26-config-wizard-cli-integration/26-01-SUMMARY.md` documentando:
- Casos green count (target ≥10 nuevos sobre baseline 715).
- Decisiones tomadas en discretion areas (regex inline vs función dedicada; UX add-only vs reorder/delete).
- Notas sobre el shape exacto persistido al config.json (verbatim D-06 o variantes).
- Validación de que la rama Plane existente NO fue tocada (diff `src/cli.js:391-400` debe ser vacío).
</output>
