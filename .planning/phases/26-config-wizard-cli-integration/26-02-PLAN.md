---
phase: 26-config-wizard-cli-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - src/cli.js
  - src/cli/polling.js
  - src/cli/polling-daemon.js
  - test/cli/polling.test.js
  - test/cli/polling-daemon.test.js
autonomous: true
requirements:
  - CFG-03
requirements_addressed:
  - CFG-03
tags:
  - cli
  - daemon
  - pid-file
  - polling
  - exit-codes
user_setup: []

must_haves:
  truths:
    - "`kodo polling start` (default daemon) spawn child detached + unref(); padre exit 0 tras confirmar PID file escrito (timeout 2s)"
    - "`kodo polling start --no-daemon` arranca foreground; SIGINT/SIGTERM detienen `startPolling.stop()` y borran PID file"
    - "`kodo polling stop` envía SIGTERM al PID; espera hasta 5s; si vivo, SIGKILL; borra PID file"
    - "`kodo polling status` reporta `running` (pid + started_at) o `idle`; exit 0 siempre"
    - "`kodo polling status --json` byte-determinístico TTY/no-TTY con 4 keys siempre (status/pid/started_at/repos; null cuando idle)"
    - "Exit codes deterministas: 0 ok, 1 ya corriendo, 2 no config, 3 stop sin daemon vivo (D-14)"
    - "PID file shape `{pid, started_at, repos:[\"owner/repo\",...]}` JSON con chmod 0o600 + atomic tmp+rename"
    - "`isPidAlive` importado de src/gsd/lock.js — NO duplicado"
    - "Windows daemon mode emite warn + sugiere `--no-daemon` (Pitfall #8); behavior verificado en test"
  artifacts:
    - path: "src/cli/polling-daemon.js"
      provides: "writePidFile + readPidFile + removePidFile + PID_PATH exports"
      contains: "export const PID_PATH"
    - path: "src/cli/polling.js"
      provides: "runPollingStartCli + runPollingStopCli + runPollingStatusCli handlers (extends Plan 26-01 file)"
      contains: "export async function runPollingStartCli"
    - path: "src/cli.js"
      provides: "kodo polling parent command + 3 subcommands (start/stop/status) registered"
      contains: "polling.command('start')"
    - path: "test/cli/polling.test.js"
      provides: "≥14 casos integration spawnSync HOME-isolated + --json byte-determinism + Windows fallback (extends Plan 26-01 file)"
    - path: "test/cli/polling-daemon.test.js"
      provides: "≥2 casos unit atomic write + fail-open read"
  key_links:
    - from: "src/cli/polling-daemon.js"
      to: "src/config.js KODO_DIR"
      via: "import { KODO_DIR } from '../config.js'"
      pattern: "from\\s+['\"]\\.\\./config\\.js['\"]"
    - from: "src/cli/polling.js"
      to: "src/gsd/lock.js isPidAlive"
      via: "import"
      pattern: "isPidAlive"
    - from: "src/cli/polling.js runPollingStartCli daemon path"
      to: "spawn(process.execPath, [KODO_BIN, 'polling', 'start', '--no-daemon'], { detached, stdio:'ignore' })"
      via: "spawn + child.unref()"
      pattern: "detached:\\s*true"
    - from: "src/cli/polling.js runPollingStartCli --no-daemon handler"
      to: "src/triggers/polling.js startPolling"
      via: "dynamic import('../triggers/polling.js') — lazy load (W-6)"
      pattern: "await import\\(['\"]\\.\\./triggers/polling\\.js['\"]\\)"
    - from: "src/cli.js polling subcommands"
      to: "src/cli/polling.js runPollingStartCli/StopCli/StatusCli"
      via: "dynamic import"
      pattern: "polling\\.command\\("
---

<objective>
Implementar el daemon CLI `kodo polling start/stop/status` (CFG-03 / D-09..15) con exit codes deterministas, PID file atomic con chmod 0o600, y `--json` byte-determinístico.

Purpose: El operador puede arrancar polling como daemon detached o foreground, detenerlo via PID file, consultar status — todo cross-platform en macOS/Linux (Windows degrada a `--no-daemon` per Pitfall #8 con test acceptance).

Output:
- `src/cli/polling-daemon.js` nuevo (PID file lifecycle: writePidFile/readPidFile/removePidFile/PID_PATH).
- `src/cli/polling.js` extendido (Plan 26-01 dejó configureGithubProvider/parseGitHubRemote/detectOriginRepo) con 3 handlers exit-code-deterministic: runPollingStartCli/runPollingStopCli/runPollingStatusCli.
- `src/cli.js` extendido con `kodo polling <subcmd>` parent + 3 subcomandos mirror el patrón `kodo gsd <sub>`.
- `test/cli/polling.test.js` extendido (Plan 26-01 dejó parseGitHubRemote casos) con ≥14 casos integration spawnSync (13 CFG-03 originales + 1 Windows fallback W-2).
- `test/cli/polling-daemon.test.js` nuevo con ≥2 casos unit (atomic write, fail-open read).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md
@.planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md
@.planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md
@.planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md
@.planning/phases/26-config-wizard-cli-integration/26-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos del repo + Plan 26-01 (file state previa al Plan 26-02). -->

From src/gsd/lock.js (existing — REUSE, NO duplicar):
- `export function isPidAlive(pid)` — line 67-74; POSIX semantics: returns true for EPERM (conservatively alive), false para ESRCH (dead).

From src/config.js (existing exports — REUSE):
- `export { KODO_DIR, CONFIG_PATH, PROJECTS_PATH, DEFAULT_CONFIG }` — line 176.
- `export function getProviderApiKey(providerName)` — line 172.
- `export function loadConfig()` — line ~119.

From src/triggers/polling.js (Phase 25 — REUSE, NO modificar):
- `export function startPolling({provider?, client?, repos, intervalSec?, clock?, logger?, statePath?, dispatchTriggerFn?}) → { stop }` — line 415.
- `repos` shape: `Array<{owner: string, repo: string}>`.

From src/cli.js (existing structure — extender):
- `kodo gsd <sub>` parent + subcomandos pattern — line 241-275 (mirror para `kodo polling <sub>`).
- `kodo skill <sub>` parent + subcomandos pattern — line 277-296 (mirror para `kodo polling <sub>`).
- `program.command('orchestrate')` — line 126 (Plan 26-03 extiende con `--polling` flag).

From src/server.js (existing — analog reference, NO modificar):
- PID file write + cleanup — line 482, 491.
- SIGTERM/SIGINT cleanup pattern — line 494-495.
- Stop pattern (kill PID + ESRCH cleanup) — line 503-523.

From src/cli/format.js (existing — REUSE):
- `export function createFormatter(stream, env?)` — line 114.

From src/cli/polling.js (created by Plan 26-01 — extender, NO reescribir):
- `export async function configureGithubProvider({ask, execGitRemote, providerConfig})` — del Plan 26-01.
- `export function parseGitHubRemote(url)` — del Plan 26-01.
- `export function detectOriginRepo(exec?)` — del Plan 26-01.

From test/fixtures/configs/ (Plan 26-01 creó — REUSE):
- `v0.7-with-github.json` (filename canonical N-1; NO `v0.7-github.json`).
- `v0.6-no-github.json`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Crear scaffolds test daemon + extender polling.test.js (RED state)</name>
  <files>test/cli/polling.test.js, test/cli/polling-daemon.test.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 460-535 — spawnSync HOME-isolated test boilerplate)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 560-590 — polling-daemon.test.js unit pattern)
    - .planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md (líneas 41-72 — 13 casos CFG-03 mapeados con grep names)
    - test/skill-sync.test.js:18-79 (HOME-isolated boilerplate analog)
    - test/skill-sync.test.js:346-353 (`--json` byte-determinism assertion verbatim)
  </read_first>
  <behavior>
    - `test/cli/polling.test.js` extiende con ≥14 casos integration (cada caso usa `mkdtempSync` + `HOME=tmpdir` + `NO_COLOR=1` + `spawnSync` con timeout 10s):
      1. **'start sin config' → exit 2** (gate config missing): no providers.github.repos → exit 2 + stderr match `/providers\.github\.repos is empty/`.
      2. **'--no-daemon SIGINT exits 0'** (foreground happy path + SIGINT): `start --no-daemon` arranca + SIGINT detiene cleanly (spawn pipe + setTimeout(kill, 500) + assert exit 0).
      3. **'start daemon writes PID file'**: daemon → PID file escrito ≤2s + padre exit 0 + PID file shape `{pid:number, started_at:string, repos:array}`.
      4. **'start con PID file vivo' → exit 1** (already running): segundo spawn cuando PID file ya existe + proceso vivo → exit 1 + stderr match `/already running/`.
      5. **'status idle'** → exit 0 (sin PID file): stdout match `/idle/` + exit 0.
      6. **'status running'** → exit 0 (con PID file + process.pid vivo). El fake PID file DEBE incluir `started_at` (W-4):
         ```
         writeFileSync(PID_PATH, JSON.stringify({
           pid: process.pid,
           started_at: new Date().toISOString(),
           repos: ['foo/bar']
         }, null, 2));
         chmodSync(PID_PATH, 0o600);
         ```
         Stdout match `/running/` + match pid:N + exit 0.
      7. **'status --json stale → idle'** → exit 0 (PID file stale, PID 999999 muerto): stdout match `/idle/` + exit 0 (NO crash).
      8. **'status --json idle' byte-deterministic** → exit 0 + regex exacto `/^\{"status":"idle","pid":null,"started_at":null,"repos":null\}\n$/` + assert NO `\x1b\[` (ANSI escape).
      9. **'status --json running' byte-deterministic** → exit 0 + regex exacto (W-3 — FIXTURE LITERAL match, NO permissive): `/^\{"status":"running","pid":\d+,"started_at":"[^"]+","repos":\["foo\/bar"\]\}\n$/` (el fixture siempre siembra `repos: ['foo/bar']`; regex literal previene falsos verdes ante `[]`). Assert NO `\x1b\[`.
      10. **'stop sin PID file' → exit 3**: stderr match `/no polling daemon running/`.
      11. **'stop sends SIGTERM and removes PID'**: spawn `--no-daemon` luego stop; assert PID file ausente post-stop + exit 0.
      12. **'status --json idle byte-determinism (4 keys consistency)'** → 4 keys siempre (Pitfall #10 D-21).
      13. **'stop con PID stale (ESRCH)'** → exit 0 + borra PID file (NO crash).
      14. **W-2 'Windows daemon emits warn and falls back to foreground'** (cross-platform Pitfall #8 acceptance):
          - Test unit in-process (NO spawnSync — process.platform es read-only en child para macOS dev box). Importa `runPollingStartCli` y mock `process.platform`:
            ```
            const originalPlatform = process.platform;
            Object.defineProperty(process, 'platform', { value: 'win32', configurable: true });
            try {
              const errs = [];
              const errFn = (s) => errs.push(s);
              // setup fixture HOME tmpdir + config + token env
              const code = await runPollingStartCli({}, { errFn, ...stubs });
              // Asserts:
              // a) stderr contains "Windows daemon unsupported" o "use --no-daemon"
              assert.match(errs.join(''), /Windows daemon unsupported|use --no-daemon/i);
              // b) Behavior: warn emitido. Exit code aceptable:
              //    - 0 si fallback automático a foreground (preferido si simple)
              //    - 1 si refusa con guidance (también aceptable)
              //    Plan executor decide; documentar en SUMMARY.
              assert.ok(code === 0 || code === 1, `windows path returned ${code}`);
            } finally {
              Object.defineProperty(process, 'platform', { value: originalPlatform });
            }
            ```
    - `test/cli/polling-daemon.test.js` con ≥2 casos unit:
      1. `writePidFile escribe atomic con chmod 0o600`: setear `HOME=tmpdir`, dynamic-import del módulo, llamar `writePidFile({pid:42, started_at:'2026-05-14T19:00:00Z', repos:['a/b']})`, assert `statSync(PID_PATH).mode & 0o777 === 0o600`, assert tmp file ausente, assert JSON parseable.
      2. `readPidFile fail-open: corrupted JSON → null`: escribir `'not valid json'` a PID_PATH, assert `readPidFile() === null` (no throw).
      3. (extra recomendado) `readPidFile shape check: pid not number → null` (defensive Security V14).
    - Todos los tests deben FAIL en este punto — `runPollingStartCli`/`writePidFile`/etc. aún no existen.
    - Spawn timeout 10s + DoS guard. Cero live API calls. Cero `setTimeout` real > 1s.
  </behavior>
  <action>Extender `test/cli/polling.test.js` con los ≥14 casos integration (después de los describes parseGitHubRemote del Plan 26-01). Crear `test/cli/polling-daemon.test.js` nuevo con los 2-3 casos unit. Helper local `makeFixture()` que siembra `~/.kodo/config.json` con `providers.github = {api_key_env: 'GITHUB_TOKEN', repos:[{owner:'foo',repo:'bar'}], poll_interval:60}` y `~/.kodo/.env` con `GITHUB_TOKEN=fake_token_for_test`. Helper `runCli({tmpHome, args})` que invoca `spawnSync(process.execPath, [KODO_BIN, 'polling', ...args], {env: {...process.env, HOME: tmpHome, NO_COLOR: '1'}, encoding: 'utf-8', timeout: 10000})`. Helper `writeFakePidFile({tmpHome, payload})` que asegura siempre incluir `started_at` (W-4). afterEach() limpia tmpHome con `rmSync({recursive, force})`. Para el caso Windows (14), test in-process con `Object.defineProperty(process, 'platform', { value: 'win32', configurable: true })` + cleanup en finally.</action>
  <verify>
    <automated>node --test test/cli/polling.test.js test/cli/polling-daemon.test.js 2>&1 | grep -E "tests|fail" | tail -5 ; ls test/cli/polling-daemon.test.js</automated>
  </verify>
  <done>Los 2 test files contienen ≥16 casos nuevos (14 integration + 2-3 unit, incluyendo W-2 Windows + W-3 regex literal + W-4 fake PID con started_at) y todos FAIL con `Cannot find module` o `runPollingStartCli is not a function` — RED state Wave 0 esperado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar src/cli/polling-daemon.js (PID file lifecycle)</name>
  <files>src/cli/polling-daemon.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 253-298 — writePidFile/readPidFile/removePidFile verbatim, RESEARCH §Example 3)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Pitfall #7 chmod cross-fs, Pitfall #11 KODO_DIR consistency)
    - .planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md (D-15 shape, T-26-02 PID file symlink ataque)
    - src/triggers/polling.js:149-154 (saveStateCache atomic analog verbatim)
    - src/config.js:176 (KODO_DIR export confirmation)
  </read_first>
  <behavior>
    - Exports: `PID_PATH` const, `writePidFile(payload)`, `readPidFile() → payload|null`, `removePidFile()`.
    - `PID_PATH = join(KODO_DIR, 'polling.pid')` (Pitfall #11 — KODO_DIR imported from `../config.js`, NO redefinir).
    - `writePidFile(payload)`:
      1. `mkdirSync(dirname(PID_PATH), { recursive: true })`.
      2. `const tmp = PID_PATH + '.tmp'`.
      3. `writeFileSync(tmp, JSON.stringify(payload, null, 2) + '\n')`.
      4. `chmodSync(tmp, 0o600)` — PRE-rename para que concurrent reads vean 0o600 inmediato (Security V14 / T-26-02).
      5. `renameSync(tmp, PID_PATH)` — POSIX atomic (mismo FS).
    - `readPidFile()`:
      1. `if (!existsSync(PID_PATH)) return null`.
      2. try/catch JSON.parse del readFileSync.
      3. Defensive shape check: `typeof parsed?.pid !== 'number' || typeof parsed?.started_at !== 'string' → null` (Security: PID injection mitigation).
      4. Cualquier throw → `return null` (fail-open mirror loadStateCache).
    - `removePidFile()`: `unlinkSync(PID_PATH)` en try/catch (may not exist).
    - JSDoc typedef: `@typedef {{pid: number, started_at: string, repos: string[]}} PidFilePayload` (D-15 — repos human-readable `["owner/repo", ...]`, NO objects).
    - Cero referencias a `GITHUB_TOKEN`, `api_key`, o `token` (excluyendo comentarios — W-7 LOCKED grep regex).
  </behavior>
  <action>Crear `src/cli/polling-daemon.js` con `// @ts-check`, imports `node:fs` (writeFileSync, readFileSync, renameSync, unlinkSync, mkdirSync, existsSync, chmodSync) y `node:path` (join, dirname), import `KODO_DIR` from `../config.js`. Implementar los 4 exports verbatim per 26-PATTERNS.md líneas 253-298. NO añadir lógica adicional. NO importar de `src/cli/format.js` (este módulo no produce output — solo I/O). NO citar literalmente `GITHUB_TOKEN`/`api_key`/`token` en código ni en comentarios JSDoc del módulo (mantiene grep source-hygiene limpio — W-7).</action>
  <verify>
    <automated>node --test test/cli/polling-daemon.test.js 2>&1 | tail -5 ; grep -E "ghp_|github_pat_" src/cli/polling-daemon.js | wc -l | grep -q '^0$' && echo "no token literals OK" || echo "FAIL: literal token detected" ; grep -v '^\s*\*\|^\s*//' src/cli/polling-daemon.js | grep -cE "GITHUB_TOKEN|api_key" | grep -q '^0$' && echo "no token refs in code OK" || echo "FAIL: token reference in non-comment code"</automated>
  </verify>
  <done>`test/cli/polling-daemon.test.js` pasa los 2-3 casos unit. Source-hygiene grep (W-7 LOCKED — excluyendo comentarios `/^\s*\*/` y `/^\s*\/\//`): cero referencias a `GITHUB_TOKEN`/`api_key` en código no-comentario; cero literales `ghp_`/`github_pat_` en todo el archivo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar 3 handlers CLI en src/cli/polling.js + registrar `kodo polling <sub>` en src/cli.js</name>
  <files>src/cli/polling.js, src/cli.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 47-172 — los 3 handlers verbatim con DI deps, exit codes D-14)
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 305-346 — kodo polling parent + 3 subcmd registration verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Pitfalls #2 unref, #3 race, #4 SIGINT propagation, #5 stdio:'ignore' diagnostics, #8 cross-platform, #10 --json shape)
    - .planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md (D-10..15, D-21 --json determinism, T-26-03..06)
    - src/cli/skill-sync.js:1-112 (CLI handler pattern: exit codes + --json + DI deps + createFormatter)
    - src/cli.js:241-275 (kodo gsd <sub> registration analog verbatim)
    - src/server.js:482-523 (PID lifecycle + SIGTERM kill + ESRCH cleanup analog)
  </read_first>
  <behavior>
    - `src/cli/polling.js` extiende (NO reescribe lo que dejó Plan 26-01) con 3 nuevos exports:
      - `export async function runPollingStartCli(opts, deps = {})` returning `Promise<number>` exit code.
        - Default writeFn/errFn/formatterFn/configFn injectables.
        - Pre-flight: `loadConfig()`; si `providers.github.repos.length === 0` → stderr `Error: providers.github.repos is empty. Run \`kodo config\` first.\n` + return 2.
        - Pre-flight: si `!getProviderApiKey('github')` → stderr `Error: GITHUB_TOKEN not set. Export it or add to ~/.kodo/.env.\n` + return 2.
        - W-6 LOCKED — import strategy explicit (lazy):
          - `--no-daemon` (foreground) handler: dynamic import `const { startPolling } = await import('../triggers/polling.js')` — lazy load, el parent `start/stop/status` no carga `polling.js` (Phase 25 module) si no se ejecuta foreground.
          - daemon path (spawn detached): NO necesita import directo de `startPolling` — el hijo es el que ejecuta `--no-daemon` internamente y allí se hace la import dinámica.
        - Branch `opts.noDaemon === true` (foreground):
          - Dynamic `const { initRegistry, getProvider } = await import('../providers/registry.js')`; `await initRegistry()`; `const provider = getProvider('github')`.
          - Dynamic `const { startPolling } = await import('../triggers/polling.js')` (W-6 LOCKED).
          - `handle = startPolling({provider, repos, intervalSec: config.providers.github.poll_interval || 60})`.
          - `writePidFile({pid: process.pid, started_at: new Date().toISOString(), repos: repos.map(r => \`${r.owner}/${r.repo}\`)})`.
          - SIGINT + SIGTERM handler: `handle.stop(); removePidFile(); process.exit(0)` (Pattern D).
          - Block forever: `await new Promise(() => {})`.
        - Branch default (daemon):
          - Cross-platform Windows check FIRST (Pitfall #8 + W-2 LOCKED): `if (process.platform === 'win32') { err('Error: Windows daemon unsupported. Use \`kodo polling start --no-daemon\` instead.\n'); return 1; }` (plan: refuse-with-guidance variant; alternative `fall-through to foreground` también aceptable según executor — documentar en SUMMARY cual se eligió; ambos cumplen test W-2 acceptance).
          - Pre-flight (Pitfall #3 — check en padre, no en hijo): `const existing = readPidFile(); if (existing && isPidAlive(existing.pid)) → stderr "already running" + return 1`; si existing pero pid muerto, `removePidFile()` (stale cleanup).
          - `spawn(process.execPath, [KODO_BIN, 'polling', 'start', '--no-daemon'], {detached: true, stdio: 'ignore', env: process.env})` + `child.unref()` (Pitfall #2 crítico). NO import directo de `startPolling` aquí (W-6 LOCKED — el daemon path delega al hijo que ejecuta `--no-daemon`).
          - `KODO_BIN = join(dirname(fileURLToPath(import.meta.url)), '..', '..', 'bin', 'kodo')` — absolute (Security elevation of privilege: NO PATH lookup).
          - Bounded wait (D-10 timeout 2s): loop `while (Date.now() < deadline)` polling `readPidFile()` + `isPidAlive`; success → return 0.
          - Timeout → stderr "daemon failed to write PID file within 2s" + return 1.
      - `export async function runPollingStopCli(opts, deps = {})` returning `Promise<number>`:
        - `payload = readPidFile()`; si null → stderr `Error: no polling daemon running\n` + return 3.
        - try `process.kill(payload.pid, 'SIGTERM')`; espera hasta 5s con `isPidAlive` loop (sleep 100ms); si vivo, `process.kill(payload.pid, 'SIGKILL')`.
        - `removePidFile(); return 0`.
        - catch ESRCH → `removePidFile(); return 0` (mirror src/server.js:516-519 stale cleanup).
      - `export async function runPollingStatusCli(opts, deps = {})` returning `Promise<number>`:
        - `payload = readPidFile(); alive = payload && isPidAlive(payload.pid)`.
        - JSON shape SIEMPRE 4 keys (Pitfall #10 / D-21): `{status: alive?'running':'idle', pid: alive?payload.pid:null, started_at: alive?payload.started_at:null, repos: alive?payload.repos:null}`.
        - `if (opts.json) write(JSON.stringify(json) + '\n')` (NO createFormatter — byte-determinism DX-06; single-line, NO ANSI).
        - else `fmt = createFormatter(stream); if (alive) write(\`${fmt.ok('running')} pid: ${payload.pid}, started: ${payload.started_at}\n\`); else write(\`${fmt.dim('idle')}\n\`)`.
        - return 0 SIEMPRE (D-13).
    - `src/cli.js` extiende con bloque mirror `kodo gsd <sub>` (líneas 241-275):
      - `const polling = program.command('polling').description('GitHub polling daemon (start/stop/status)')`.
      - `polling.command('start').option('--no-daemon', '...').option('--json', '...').action(async opts => { const {runPollingStartCli} = await import('./cli/polling.js'); process.exit(await runPollingStartCli({noDaemon: opts.noDaemon, json: opts.json})); })` con try/catch que exit 1 ante throw.
      - `polling.command('stop').action(...)` análogo con `runPollingStopCli`.
      - `polling.command('status').option('--json', '...').action(...)` análogo con `runPollingStatusCli`.
      - NO añadir `ensureConfig()` wrapping en `polling start` — el pre-flight de exit 2 ya cubre el caso config ausente.
    - Cero imports directos de `picocolors` (Pattern A). `isPidAlive` importado de `../gsd/lock.js` (Pattern E — NO duplicar).
    - `--json` flag NO usa createFormatter (byte-determinism DX-06: el output JSON viaja sin ANSI escapes ni dependencia de TTY).
  </behavior>
  <action>Editar `src/cli/polling.js`: AÑADIR los 3 exports `runPollingStartCli`, `runPollingStopCli`, `runPollingStatusCli` (en orden); imports estáticos nuevos `node:child_process` (spawn), `node:url` (fileURLToPath), `node:path` (dirname, join), `node:timers/promises` (setTimeout as sleep), local imports `./format.js` (createFormatter), `../gsd/lock.js` (isPidAlive), `../config.js` (loadConfig, getProviderApiKey), `./polling-daemon.js` (writePidFile, readPidFile, removePidFile, PID_PATH). IMPORT DINÁMICO LAZY (W-6 LOCKED) DENTRO del `--no-daemon` branch del handler: `await import('../triggers/polling.js')` para `startPolling` y `await import('../providers/registry.js')` para `initRegistry/getProvider` — esto evita que el parent `polling.js` cargue Phase 25 polling.js cuando solo se llama `status` o `stop`. Editar `src/cli.js`: AÑADIR el bloque `kodo polling <sub>` siguiendo verbatim el shape de PATTERNS.md líneas 305-346, insertado adyacente al bloque `kodo skill <sub>` (línea ~277-296). NO modificar la rama Plane ni el bloque interactiveConfig.</action>
  <verify>
    <automated>node --test test/cli/polling.test.js test/cli/polling-daemon.test.js 2>&1 | tail -10 ; grep -v '^//\|^\s*\*' src/cli/polling.js | grep -c "from\s*['\"]picocolors['\"]" | grep -q '^0$' && echo "color-isolation OK" || echo "FAIL picocolors leak"</automated>
  </verify>
  <success_criteria>
    - **Exit code 0 deterministic** (D-14): verificado verbatim por test names:
      - caso 2 `'--no-daemon SIGINT exits 0'` (foreground happy + SIGINT cleanup)
      - caso 3 `'start daemon writes PID file'` (daemon path success)
      - caso 5 `'status idle'` (status query siempre 0 D-13)
      - caso 6 `'status running'` (status query siempre 0 D-13)
      - caso 7 `'status --json stale → idle'` (status query no falla por stale PID D-13)
      - caso 11 `'stop sends SIGTERM and removes PID'` (stop happy path)
      - caso 12 `'status --json idle byte-determinism (4 keys consistency)'` (D-21)
    - **Exit code 1 deterministic** (D-14 already-running): verificado verbatim por:
      - caso 4 `'start con PID file vivo'` (daemon detecta existing PID alive → exit 1)
    - **Exit code 2 deterministic** (D-14 no config): verificado verbatim por:
      - caso 1 `'start sin config'` (providers.github.repos vacío → stderr canonical + exit 2)
    - **Exit code 3 deterministic** (D-14 no daemon for stop): verificado verbatim por:
      - caso 10 `'stop sin PID file'` (stop sin daemon vivo → stderr canonical + exit 3)
    - **Cross-platform fallback** (Pitfall #8 / W-2 LOCKED): verificado por:
      - caso 14 `'Windows daemon emits warn and falls back to foreground'` (assert stderr contiene `Windows daemon unsupported|use --no-daemon` y exit code es 0 o 1 según variante elegida).
    - **--json byte-determinism** (D-21 / DX-06): verificado por casos 8, 9, 12 (regex literal de fixture per W-3; NO regex permissive `[^\]]*`).
    - **PID file shape** (D-15 + W-4): verificado por caso 6 (fake PID file con `started_at` siempre presente).
    - **Color isolation** (Pattern A): cero imports directos de picocolors en `src/cli/polling.js` y `src/cli/polling-daemon.js`.
    - **Token isolation source-hygiene** (W-7 LOCKED): `grep -E "ghp_|github_pat_" src/cli/polling-daemon.js | wc -l` retorna `0` (ningún valor literal de token); `grep -v '^\s*\*\|^\s*//' src/cli/polling-daemon.js | grep -cE "GITHUB_TOKEN|api_key"` retorna `0` (excluyendo comentarios — daemon-utility no maneja tokens).
    - **Import strategy** (W-6 LOCKED): `grep "await import\(.*triggers/polling" src/cli/polling.js` retorna ≥1 match (lazy load); el import estático top-level NO debe contener `from '../triggers/polling.js'`.
    - **Suite verde**: `npm test` baseline 725 + ≥16 nuevos = ≥741 pass, 0 fail.
  </success_criteria>
  <done>`test/cli/polling.test.js` + `test/cli/polling-daemon.test.js` ambos verde con ≥16 casos (14 integration + 2-3 unit). Source-hygiene grep cero picocolors. Source-hygiene W-7 cero token literals en polling-daemon.js. Lazy import W-6 confirmado via grep. `bin/kodo polling --help` muestra los 3 subcomandos. Suite full `npm test` ≥741 pass, 0 fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → kodo polling CLI | argv y env vars no confiables |
| ~/.kodo/polling.pid → readPidFile | Otro proceso podría escribir un payload malicioso |
| spawn child detached → PID OS-level | El hijo corre desligado; el PID puede ser reusado tras crash |
| SIGTERM/SIGINT → cleanup handler | Race entre signal y `removePidFile` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering | PID file `~/.kodo/polling.pid` | mitigate | `chmod 0o600` PRE-rename en `writePidFile` (Security V14 — token-adjacent metadata; only owner reads/writes). `readPidFile` defensive shape check (`typeof pid === 'number'`, `typeof started_at === 'string'` → null) previene PID injection. Test source-hygiene W-7 LOCKED: `grep -E "ghp_|github_pat_" src/cli/polling-daemon.js` retorna 0; `grep -v '^\s*\*\|^\s*//' src/cli/polling-daemon.js | grep -cE "GITHUB_TOKEN|api_key"` retorna 0. |
| T-26-03 | DoS (false positive) | runPollingStatusCli isPidAlive check | accept | Tras crash, el OS puede reciclar el PID. `kodo polling status` podría reportar `running` a un PID ajeno (e.g. vim). Probabilidad real en Mac/Linux dev box trivial (PID space 32-bit, segundos entre crash y rearranque). Aceptar como known limitation per RESEARCH Pitfall #1; documentar en summary. Reusar `isPidAlive` de `src/gsd/lock.js:67` sin enrichment. |
| T-26-04 | Availability | runPollingStartCli daemon path | mitigate | `child.unref()` INMEDIATO post-spawn (Pitfall #2 — sin esto el padre cuelga). `detached: true` + `stdio: 'ignore'` desliga al hijo del process group del padre. Si el padre crashea durante setup, el hijo lee config + arranca solo (sin rely on parent). Test integration: caso 2 `'--no-daemon SIGINT exits 0'`. |
| T-26-05 | Tampering (race) | runPollingStopCli SIGTERM/SIGKILL escalation | mitigate | Cleanup idempotente: `removePidFile()` envuelto en try/catch (may not exist). Catch ESRCH del kill → return 0 + removePidFile (mirror src/server.js:516-519). Test casos 11 + 13 cubren happy stop + ESRCH stale. |
| T-26-06 | Information Disclosure | runPollingStartCli NDJSON startup logging | mitigate | NO loggear token en runPollingStartCli (pre-flight solo verifica `!!getProviderApiKey('github')`, NUNCA imprime el value). Reusar invariante Phase 25 T-25-02: `polling.dispatch` NDJSON excluye `issue.body` y token. El `--no-daemon` path hereda esta propiedad. Source-hygiene: `grep -c "process.env.GITHUB_TOKEN\|payload.api_key" src/cli/polling.js === 0` (solo `getProviderApiKey('github')` legítimo). |
| T-26-EOP | Elevation of Privilege | spawn detached child | mitigate | `process.execPath` (Node absolute path) + `KODO_BIN` absolute (resolved via `fileURLToPath(import.meta.url) + '../../bin/kodo'`) — cero PATH lookup. argv array form sin shell. `env: process.env` propaga env del padre (no escalation; cualquier env malicioso ya estaba presente en el padre). |
| T-26-WIN | Availability (cross-platform) | runPollingStartCli on Windows | mitigate | Pitfall #8 / W-2 LOCKED — `detached: true` en Windows crea nueva ventana visible; `process.kill(pid, 'SIGTERM')` es no-op (translation a `TerminateProcess`). Mitigation: detectar `process.platform === 'win32'` en daemon path y emitir warn `Windows daemon unsupported. Use \`kodo polling start --no-daemon\` instead.` + exit 1 (refuse-with-guidance variant) O fallback automático a foreground (exit 0). Test caso 14 acepta ambas variantes. v0.7 prioriza darwin/linux. |
| T-26-DIAG | Availability (operability) | daemon stdio:'ignore' diagnostics | accept | Pitfall #5 — el child error va a /dev/null. Aceptar para v0.7 (operador con dudas hace `kodo polling start --no-daemon` para ver stderr). Defer a v0.8: redirigir stdout/stderr a `~/.kodo/polling.log` con `fs.openSync` + `stdio: ['ignore', logFd, logFd]` + chmod 0o600. |

</threat_model>

<verification>
- `node --test test/cli/polling.test.js test/cli/polling-daemon.test.js` — ≥16 casos green (14 integration CFG-03 + 2-3 unit daemon, incluyendo W-2 Windows caso 14).
- `npm test` — full suite ≥741 pass, 0 fail.
- `grep -v '^//\|^\s*\*' src/cli/polling.js src/cli/polling-daemon.js | grep -c "from\s*['\"]picocolors['\"]"` — `0` (Pattern A).
- W-7 LOCKED token isolation (regex refinado — excluye comentarios, busca literales):
  - `grep -E "ghp_|github_pat_" src/cli/polling-daemon.js | wc -l` — `0` (ningún valor literal de token).
  - `grep -v '^\s*\*\|^\s*//' src/cli/polling-daemon.js | grep -cE "GITHUB_TOKEN|api_key"` — `0` (excluyendo comentarios; daemon-utility no maneja tokens).
- W-6 LOCKED import strategy:
  - `grep -E "^import.*from\s+['\"]\.\./triggers/polling\.js['\"]" src/cli/polling.js | wc -l` — `0` (NO static top-level import).
  - `grep -E "await import\(.*triggers/polling" src/cli/polling.js | wc -l` — `≥1` (dynamic import lazy).
- `bin/kodo polling --help` — lista los 3 subcomandos start/stop/status.
- `bin/kodo polling start --help` — muestra `--no-daemon` y `--json` flags.
- Manual smoke (out-of-band): `HOME=$(mktemp -d) && mkdir -p $HOME/.kodo && cp test/fixtures/configs/v0.7-with-github.json $HOME/.kodo/config.json && echo "GITHUB_TOKEN=fake" > $HOME/.kodo/.env && bin/kodo polling start --no-daemon &` → tras 1s `bin/kodo polling status` → `running`; luego `bin/kodo polling stop` → exit 0 + PID file ausente.
</verification>

<success_criteria>
- [ ] CFG-03: `kodo polling start` (default daemon) exit 0 tras PID file escrito ≤2s.
- [ ] CFG-03: `kodo polling start --no-daemon` arranca + SIGINT detiene cleanly + PID file borrado.
- [ ] CFG-03 / D-14: Exit codes enumerados verbatim por test name (B-2 LOCKED):
  - **Exit 0**: caso 2 (`'--no-daemon SIGINT exits 0'`), caso 3 (`'start daemon writes PID file'`), caso 5 (`'status idle'`), caso 6 (`'status running'`), caso 7 (`'status --json stale → idle'`), caso 11 (`'stop sends SIGTERM and removes PID'`), caso 12 (`'status --json idle byte-determinism'`).
  - **Exit 1**: caso 4 (`'start con PID file vivo'`).
  - **Exit 2**: caso 1 (`'start sin config'`).
  - **Exit 3**: caso 10 (`'stop sin PID file'`).
- [ ] CFG-03: PID file shape `{pid, started_at, repos:[\"owner/repo\",...]}` con chmod 0o600 — assert via polling-daemon.test.js statSync; fake PID file en tests siempre incluye `started_at` (W-4).
- [ ] CFG-03: `kodo polling status --json` byte-determinístico — regex LITERAL en running case `\["foo\/bar"\]` (NO permissive `[^\]]*`, W-3).
- [ ] Pitfall #8 / W-2 LOCKED: `kodo polling start` en Windows emite warn cita `Windows daemon unsupported|use --no-daemon` — verificable via caso 14.
- [ ] W-6 LOCKED import strategy: `startPolling` cargado vía `await import('../triggers/polling.js')` lazy dentro del `--no-daemon` handler; NO static top-level import.
- [ ] W-7 LOCKED source-hygiene: `grep -E "ghp_|github_pat_" src/cli/polling-daemon.js` retorna 0; `grep -v '^\s*\*\|^\s*//' src/cli/polling-daemon.js | grep -cE "GITHUB_TOKEN|api_key"` retorna 0.
- [ ] `isPidAlive` reutilizado desde `src/gsd/lock.js` (NO duplicado en polling.js o polling-daemon.js).
- [ ] Cero imports directos de picocolors en `src/cli/polling.js` y `src/cli/polling-daemon.js` (Pattern A).
- [ ] Suite full `npm test` ≥741 pass, 0 fail.
</success_criteria>

<output>
After completion, create `.planning/phases/26-config-wizard-cli-integration/26-02-SUMMARY.md` documentando:
- Casos green count (target ≥16 nuevos sobre baseline post-Plan-26-01).
- Decisión Windows daemon path (W-2): refuse-with-guidance (exit 1) o fallback-to-foreground (exit 0) — citar variante elegida + stderr canonical exacto.
- Confirmación W-3: regex literal `\["foo\/bar"\]` usado en caso 9 (NO permissive).
- Confirmación W-4: fake PID file en caso 6 incluye `started_at`.
- Confirmación W-6: dynamic import lazy `await import('../triggers/polling.js')` dentro de `--no-daemon` handler.
- Confirmación W-7: grep source-hygiene refinado pasa (comentarios excluidos, literales `ghp_/github_pat_` ausentes).
- Confirmación N-1: filename `v0.7-with-github.json` reusado del Plan 26-01.
- Decisiones tomadas en discretion areas (status muestra `last_tick` o solo `running|idle` + pid).
- Validación T-26-06: ningún path imprime el value de GITHUB_TOKEN (grep en source confirma).
- Pitfall observations: si emergió flake en el test de spawn detached (timing sensitive).
- Nota borderline scope (N-2 acceptable): Task 3 acumula 3 handlers + parent + 3 subcmd + Windows guard — si emergió presión de contexto durante ejecución, documentar para futura extracción de "CLI registration" a un Task 4 en revisiones siguientes.
</output>
</output>
