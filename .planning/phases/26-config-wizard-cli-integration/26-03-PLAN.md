---
phase: 26-config-wizard-cli-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "26-01"
  - "26-02"
files_modified:
  - src/cli.js
  - test/cli/orchestrate-polling.test.js
autonomous: true
requirements:
  - CFG-04
requirements_addressed:
  - CFG-04
tags:
  - cli
  - orchestrate
  - polling
  - sigint-cleanup
  - mutex
user_setup: []

must_haves:
  truths:
    - "`kodo orchestrate --polling` valida config (providers.github.repos no vacío + GITHUB_TOKEN set); exit 2 si falla con stderr canonical"
    - "`kodo orchestrate --polling` arranca `startPolling({provider, repos, intervalSec})` integrado en el mismo proceso del orchestrator (NO daemon separado, NO fork)"
    - "SIGINT/SIGTERM en el proceso orchestrator llama `pollingHandle.stop()` antes de `process.exit(0)` (D-18 — evita timer huérfano)"
    - "Sin `--polling`, `kodo orchestrate` se comporta idéntico a hoy (zero breaking change D-19)"
    - "`kodo orchestrate --help` cita el contrato mutex implícito (D-17): NO usar con `kodo polling start` simultáneo sobre el mismo repo"
    - "El contrato `cwd=repo` Phase 999.1 se preserva — el polling vive en el mismo proceso, no en un worktree"
  artifacts:
    - path: "src/cli.js"
      provides: "kodo orchestrate --polling flag + SIGINT/SIGTERM cleanup + pre-flight exit 2 gates"
      contains: "\\.option\\('--polling'"
    - path: "test/cli/orchestrate-polling.test.js"
      provides: "≥4 casos: sin repos exit 2, sin GITHUB_TOKEN exit 2, startPolling invoked (DI spy), SIGINT cleanup exit 0"
  key_links:
    - from: "src/cli.js orchestrate action --polling branch"
      to: "src/triggers/polling.js startPolling"
      via: "dynamic import"
      pattern: "import\\('\\./triggers/polling\\.js'\\)"
    - from: "src/cli.js orchestrate action --polling branch"
      to: "src/providers/registry.js getProvider('github')"
      via: "dynamic import + initRegistry()"
      pattern: "getProvider\\(['\"]github['\"]\\)"
    - from: "src/cli.js orchestrate SIGINT handler"
      to: "pollingHandle.stop()"
      via: "process.on('SIGINT', cleanup) + cleanup() body"
      pattern: "pollingHandle\\.stop\\("
    - from: "kodo orchestrate --help"
      to: "mutex implícito doc string"
      via: "Commander .option description"
      pattern: "mutex implícito|lock per-repo Phase 8"
---

<objective>
Añadir flag `--polling` a `kodo orchestrate` que arranca `startPolling` integrado en el mismo proceso (CFG-04 / D-16..19). El flag es ortogonal a `kodo polling start` daemon path — la mutex es implícita vía lock per-repo Phase 8 GSD-10 (D-17), documentada en `--help`.

Purpose: El operador que ya lanza orchestrator desde el repo (Phase 999.1 contract) puede añadir `--polling` para tener un único proceso que cubre orchestrator + polling sin daemon separado. Para uso personal donde "elige uno u otro por repo" es suficiente.

Output:
- `src/cli.js` extendido con `.option('--polling', ...)` en el comando orchestrate + lógica de arranque + SIGINT/SIGTERM cleanup mirror src/server.js:490-495.
- `test/cli/orchestrate-polling.test.js` nuevo con ≥4 casos cubriendo CFG-04 exit codes + SIGINT cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md
@.planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md
@.planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md
@.planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md
@.planning/phases/26-config-wizard-cli-integration/26-02-SUMMARY.md

<interfaces>
<!-- Contratos del estado del repo tras Plan 26-02 ejecutado. -->

From src/cli.js (existing — extender):
- `program.command('orchestrate').description('Launch the orchestrator Claude session')` — line 126.
- Action handler existente — line ~127-141 (preserve la lógica `launchOrchestrator`).

From src/triggers/polling.js (Phase 25 — REUSE):
- `export function startPolling({provider, repos, intervalSec, ...}) → { stop }` — line 415.
- `repos` shape: `Array<{owner: string, repo: string}>`.

From src/providers/registry.js (Phase 24 — REUSE):
- `export async function initRegistry()`.
- `export function getProvider(name) → TaskProvider`.

From src/config.js (existing — REUSE):
- `export function loadConfig()`.
- `export function getProviderApiKey(providerName)`.

From src/server.js (existing — analog SIGINT pattern, NO modificar):
- SIGTERM/SIGINT cleanup — line 490-495.

From Plan 26-02 deliverables (state pre-execution):
- `src/cli/polling.js` con `runPollingStartCli/StopCli/StatusCli` + `configureGithubProvider/parseGitHubRemote/detectOriginRepo` exports (Plan 26-01 + 26-02).
- `src/cli/polling-daemon.js` con PID file lifecycle (Plan 26-02).
- `kodo polling <sub>` subcommands registrados en `src/cli.js` (Plan 26-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Crear test/cli/orchestrate-polling.test.js (RED state, ≥4 casos)</name>
  <files>test/cli/orchestrate-polling.test.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 655-678 — orchestrate-polling.test.js SIGINT pattern verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md (líneas 66-69 — 4 casos CFG-04 mapeados)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Pitfall #5 daemon stdio diagnostics)
    - test/skill-sync.test.js:69-79 (spawn child + timeout analog)
    - test/cli/polling.test.js (creado en Plan 26-02 — reusar makeFixture helper si está exportable)
  </read_first>
  <behavior>
    - ≥4 casos integration spawnSync HOME-isolated:
      1. **sin repos → exit 2**: makeFixture con `providers.github.repos = []`; `spawnSync(node, [KODO_BIN, 'orchestrate', '--polling'], {env: {...process.env, HOME: tmpHome}, timeout: 5000})`; assert `result.status === 2` + `result.stderr` match `/providers\.github\.repos is empty/`.
      2. **sin GITHUB_TOKEN → exit 2**: makeFixture con `providers.github.repos = [{owner:'foo',repo:'bar'}]` PERO sin `~/.kodo/.env` y `env` scrubbed (`GITHUB_TOKEN: undefined`); assert `result.status === 2` + stderr match `/GITHUB_TOKEN not set/`.
      3. **SIGINT cleanup → exit 0**: makeFixture válida (repos + token); `spawn(... , {stdio:'pipe'})`; `await sleep(500)` para dar tiempo a montar el timer; `process.kill(child.pid, 'SIGINT')`; `exitCode = await new Promise(r => child.on('exit', r))`; assert `exitCode === 0` (no timer huérfano, no hang). Bounded timeout 3s. Si exitCode === null (timeout/crash), test fail loud.
      4. **startPolling invoked (DI spy)**: unit in-process — extraer la lógica del `--polling` branch a un helper exportado `runOrchestratePollingSetup(opts, deps = {})` con `deps.startPollingFn` injectable (similar al patrón skill-sync DI). Test importa el helper, pasa `startPollingFn` spy + `loadConfigFn` stub + `getProviderApiKeyFn` stub + `getProviderFn` stub; assert spy invoked con `{provider, repos: [...], intervalSec: 60}`. Si el refactor para exposure es prohibitivo, alternar a integration test que verifica vía `kodo orchestrate --polling` + assertion de que polling hizo al menos una iteración (e.g. NDJSON event match) — el planner-executor decide; documentar en summary.
      5. (extra recomendado) **Sin `--polling`, `kodo orchestrate` comportamiento idéntico**: assert `--help` muestra `--polling` flag con texto que cita "mutex" o "lock per-repo" (regex match en `result.stdout`).
    - Todos los tests FAIL en este punto porque el flag `--polling` aún no existe en `src/cli.js`.
    - makeFixture helper local: similar al de polling.test.js (Plan 26-02) — siembra `~/.kodo/config.json` + `~/.kodo/.env` con tokens fake.
    - NO live API calls. NO `setTimeout` > 1s salvo timeouts de spawn (bounded 5s).
  </behavior>
  <action>Crear `test/cli/orchestrate-polling.test.js` con `node:test` + `node:assert/strict` + `node:child_process` (spawn + spawnSync) + `node:timers/promises` (sleep). Helper `makeFixture({withRepos, withToken})` que devuelve `{tmpHome}`. afterEach() limpia tmpHome con `rmSync({recursive, force})`. Para el caso SIGINT, capturar `child.pid` antes del kill y bound el `child.on('exit')` con `Promise.race` contra timeout 3s para evitar hang del test. Si el caso 4 (DI spy in-process) requiere refactor del action handler, dejar comentario `// TODO Plan 26-03 Task 2: refactor for DI` y caer al integration variant.</action>
  <verify>
    <automated>node --test test/cli/orchestrate-polling.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>El test file contiene ≥4 casos y todos FAIL con `Unknown option '--polling'` o equivalente — RED state Wave 0 esperado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar flag --polling en kodo orchestrate (src/cli.js)</name>
  <files>src/cli.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 378-419 — orchestrate --polling action verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Code Example #4 verbatim líneas 355-413; Pitfalls #4 SIGINT race, #6 errors)
    - .planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md (D-16..19, T-26-04..06)
    - src/cli.js:126-141 (orchestrate command actual — extender, NO reescribir)
    - src/server.js:490-495 (SIGINT/SIGTERM cleanup analog)
  </read_first>
  <behavior>
    - Extender el comando `kodo orchestrate` (línea 126 en src/cli.js) con:
      - `.option('--polling', 'Arranca polling integrado en el orchestrator. NO usar con \`kodo polling start\` simultáneo sobre el mismo repo (mutex implícito vía lock per-repo Phase 8 GSD-10).')` — el description text DEBE contener literalmente la frase "mutex implícito" o "lock per-repo" para que el test del `--help` matche (D-17).
    - Action handler extendido:
      1. Preservar la lógica existente `launchOrchestrator()` — el flag `--polling` es estrictamente aditivo (D-19).
      2. Si `opts.polling === true`:
         - `const config = loadConfig()`; `const repos = config.providers?.github?.repos || []`; si `repos.length === 0`: `console.error('Error: providers.github.repos is empty. Run \`kodo config\` first.')` + `process.exit(2)`.
         - Si `!getProviderApiKey('github')`: `console.error('Error: GITHUB_TOKEN not set. Export it or add to ~/.kodo/.env.')` + `process.exit(2)`.
         - Dynamic import + `await initRegistry()` + `const provider = getProvider('github')`.
         - Dynamic import + `const pollingHandle = startPolling({provider, repos, intervalSec: config.providers.github.poll_interval || 60})`.
         - SIGINT + SIGTERM handler: `const cleanup = () => { if (pollingHandle) pollingHandle.stop(); process.exit(0); }; process.on('SIGINT', cleanup); process.on('SIGTERM', cleanup);` (Pattern D — D-18). NO `removePidFile` (orchestrate path NO maneja PID file; eso es exclusivo de `kodo polling start`).
      3. Sin `opts.polling`, action handler existente sigue idéntico (D-19 zero breaking change).
    - El contrato `cwd=repo` Phase 999.1 NO se altera — `launchOrchestrator()` ya carga skill desde cwd; el polling vive en el mismo proceso async paralelo, NO en worktree.
    - Output formatting: NO usar createFormatter para el cleanup path (mensajes de error son stderr canonical, no necesitan TTY-awareness; alineado con el style del orchestrate handler existente).
    - El cleanup handler debe ser idempotente — si `pollingHandle` es `null` (e.g. crash entre validation y start), `cleanup()` no debe crashear.
  </behavior>
  <action>Editar `src/cli.js` con cambio quirúrgico: AÑADIR `.option('--polling', '...')` al comando orchestrate y EXTENDER el action handler con el bloque `if (opts.polling) { ... }` siguiendo verbatim 26-PATTERNS.md líneas 378-419 + 26-RESEARCH.md líneas 355-413. NO mover, NO renombrar, NO refactorizar la lógica existente de `launchOrchestrator()`. Los imports nuevos (`loadConfig`, `getProviderApiKey`, `startPolling`, `initRegistry`, `getProvider`) van como dynamic `await import('...')` dentro del handler para mantener carga lazy y no afectar startup del orchestrator-sin-polling. Si el Task 1 dejó un comentario `// TODO Plan 26-03 Task 2: refactor for DI`, esta tarea evalúa si el refactor es de bajo costo (5-10 LOC, exponer `runOrchestratePollingSetup`); si sí, hacerlo + actualizar el test 4 para DI; si no, dejar el integration variant del Task 1.</action>
  <verify>
    <automated>node --test test/cli/orchestrate-polling.test.js 2>&1 | tail -10 ; bin/kodo orchestrate --help 2>&1 | grep -E "polling|mutex|lock per-repo" | head -3</automated>
  </verify>
  <done>`test/cli/orchestrate-polling.test.js` pasa los ≥4 casos. `bin/kodo orchestrate --help` muestra el flag `--polling` con descripción conteniendo "mutex implícito" o "lock per-repo". Suite full `npm test` baseline 740 + ≥4 nuevos = ≥744 pass, 0 fail. La regresión de `kodo orchestrate` sin flag debe seguir funcional (smoke manual o test existente — verificar que el handler preserva la lógica `launchOrchestrator()` previa).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → kodo orchestrate --polling | argv y env vars no confiables |
| same process: orchestrator + polling | Crash en polling no debe llevarse al orchestrator (y viceversa) |
| SIGINT/SIGTERM → cleanup handler | Race: si SIGINT llega antes de `pollingHandle = startPolling(...)`, cleanup() referencia `null` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Availability | --polling SIGINT race / orphan timer | mitigate | D-18 — `cleanup()` envuelve `pollingHandle.stop()` con check `if (pollingHandle)` para idempotencia (cubre el caso SIGINT pre-start). El test `SIGINT cleanup → exit 0` valida wall-clock (sleep 500ms antes del kill garantiza handle inicializado en happy path). |
| T-26-05 | Tampering (mutex) | --polling vs kodo polling start daemon simultáneos | accept | D-17 — mutex implícito vía lock per-repo Phase 8 GSD-10 (Karpathy Rule 2 simplicity-first). Si el operador arranca AMBOS sobre el mismo repo, el lock per-repo coalesce las sesiones — no doble dispatch. Documentar en `--help` text + summary. NO añadir mutex explícito (overkill v0.7). |
| T-26-06 | Information Disclosure | --polling startup logging | mitigate | El pre-flight gate solo verifica `!!getProviderApiKey('github')` — NUNCA imprime el value del token. Mensajes de error son stderr canonical sin token leak. Reusar invariante T-25-02 Phase 25 (polling.dispatch NDJSON excluye token). Source-hygiene: `grep -cE "process.env.GITHUB_TOKEN(?!.*payload|.*event)" src/cli.js === 0` excepto en pre-flight check legítimo. |
| T-26-CWD | Tampering (contract violation) | cwd=repo Phase 999.1 invariante | mitigate | El polling vive en el mismo proceso del orchestrator (D-16) — NO spawn child, NO worktree. `launchOrchestrator()` ya carga skill desde cwd; el flag `--polling` solo añade un timer async paralelo. Test acceptance: `orchestrate --polling` exit clean tras SIGINT — implica que el proceso es uno solo. |
| T-26-ZBC | Availability (regression) | --polling default off (D-19 zero breaking change) | mitigate | Sin `--polling`, action handler comportamiento IDÉNTICO a hoy. Tests existentes de `kodo orchestrate` (si existen en `test/`) deben seguir pasando sin modificación. Acceptance: `npm test` full suite baseline 740 → 744 pass (4 nuevos), 0 fail. |
| T-26-CRASH | Availability | startPolling throw durante init | mitigate | Si `startPolling(...)` throws (e.g. provider.init() falla por token revocado), el throw propaga al action handler outer try/catch → stderr + exit 1. Cleanup handler instalado DESPUÉS del startPolling exitoso — si crashea pre-install, no hay handler que llamar (correcto: el proceso muere clean por throw). |

</threat_model>

<verification>
- `node --test test/cli/orchestrate-polling.test.js` — ≥4 casos green.
- `npm test` — full suite ≥744 pass, 0 fail.
- `bin/kodo orchestrate --help` — contiene `--polling` flag + texto matching `/mutex implícito|lock per-repo/`.
- Smoke manual (out-of-band): `bin/kodo orchestrate --polling` con config válida → arranca; Ctrl+C → exit 0 clean (no hang, no timer huérfano warning).
- Regresión: `bin/kodo orchestrate` (sin flag) debe comportarse idéntico (D-19) — verificar que existing tests/UATs no se rompen.
- `grep -c "removePidFile\|polling.pid" $(git diff --cached --name-only -- src/cli.js)` — `0` (el --polling path NO toca PID file; eso es exclusivo de `kodo polling start`).
</verification>

<success_criteria>
- [ ] CFG-04: `kodo orchestrate --polling` sin repos → exit 2 + stderr canonical.
- [ ] CFG-04: `kodo orchestrate --polling` sin GITHUB_TOKEN → exit 2 + stderr canonical.
- [ ] CFG-04: `kodo orchestrate --polling` arranca `startPolling` integrado (DI spy o integration NDJSON match).
- [ ] CFG-04 / D-18: SIGINT en el proceso orchestrator llama `pollingHandle.stop()` antes de `process.exit(0)` — assert wall-clock exit 0 dentro de 3s.
- [ ] D-17: `--help` text del flag cita "mutex implícito" o "lock per-repo".
- [ ] D-19: Sin `--polling`, `kodo orchestrate` comportamiento idéntico (regresión libre).
- [ ] Phase 999.1 contract preservado: el polling vive en el mismo proceso, NO worktree, NO spawn child.
- [ ] Suite full `npm test` ≥744 pass, 0 fail.
</success_criteria>

<output>
After completion, create `.planning/phases/26-config-wizard-cli-integration/26-03-SUMMARY.md` documentando:
- Casos green count (target ≥4 nuevos sobre baseline post-Plan-26-02).
- Decisión final sobre Task 1 caso 4 (DI spy in-process vs integration NDJSON variant).
- Validación de la regresión D-19: si existe test previo de `kodo orchestrate` sin flag, citar su nombre y confirmar que sigue verde.
- Cita literal del texto del `--help` text para auditoría del mutex implícito (D-17).
- Notas sobre el invariante cwd=repo Phase 999.1: confirmar que el polling integrado NO requiere worktree changes.
- Resumen del milestone v0.7: CFG-01..04 all green, Phase 26 completo, ready for Phase 27 (cross-provider contract matrix).
</output>
