---
phase: 26-config-wizard-cli-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "26-01"
  - "26-02"
files_modified:
  - src/cli.js
  - src/cli/orchestrate.js
  - test/cli/orchestrate-polling.test.js
autonomous: true
requirements:
  - CFG-04
requirements_addressed:
  - CFG-04
tags:
  - cli
  - orchestrate
  - polling
  - sigint-cleanup
  - mutex
  - di-extraction
user_setup: []

must_haves:
  truths:
    - "`kodo orchestrate --polling` valida config (providers.github.repos no vacío + GITHUB_TOKEN set); exit 2 si falla con stderr canonical"
    - "`kodo orchestrate --polling` arranca `startPolling({provider, repos, intervalSec})` integrado en el mismo proceso del orchestrator (NO daemon separado, NO fork)"
    - "SIGINT/SIGTERM en el proceso orchestrator llama `pollingHandle.stop()` antes de `process.exit(0)` (D-18 — evita timer huérfano)"
    - "Sin `--polling`, `kodo orchestrate` se comporta idéntico a hoy (zero breaking change D-19)"
    - "`kodo orchestrate --help` cita el contrato mutex implícito (D-17): NO usar con `kodo polling start` simultáneo sobre el mismo repo"
    - "El contrato `cwd=repo` Phase 999.1 se preserva — el polling vive en el mismo proceso, no en un worktree"
    - "Polling setup extraído a helper exportado `runOrchestratePollingSetup(opts, deps)` (DI canonical pattern, Phase 25 precedent) — habilita test in-process spy sin integration NDJSON"
  artifacts:
    - path: "src/cli/orchestrate.js"
      provides: "runOrchestratePollingSetup({startPollingFn, configLoader, initRegistryFn, getProviderFn, getProviderApiKeyFn}, opts) exported helper for DI testing"
      contains: "export async function runOrchestratePollingSetup"
    - path: "src/cli.js"
      provides: "kodo orchestrate --polling flag + SIGINT/SIGTERM cleanup + delegates to runOrchestratePollingSetup"
      contains: "\\.option\\('--polling'"
    - path: "test/cli/orchestrate-polling.test.js"
      provides: "≥4 casos: sin repos exit 2, sin GITHUB_TOKEN exit 2, startPolling invoked (DI spy in-process), SIGINT cleanup exit 0"
  key_links:
    - from: "src/cli.js orchestrate action --polling branch"
      to: "src/cli/orchestrate.js runOrchestratePollingSetup"
      via: "dynamic import"
      pattern: "runOrchestratePollingSetup\\("
    - from: "src/cli/orchestrate.js runOrchestratePollingSetup"
      to: "src/triggers/polling.js startPolling (injectable as deps.startPollingFn)"
      via: "dynamic import (default) o DI spy (tests)"
      pattern: "deps\\.startPollingFn"
    - from: "src/cli/orchestrate.js runOrchestratePollingSetup"
      to: "src/providers/registry.js getProvider('github')"
      via: "dynamic import (default) o DI stub (tests)"
      pattern: "getProvider\\(['\"]github['\"]\\)"
    - from: "src/cli.js orchestrate SIGINT handler"
      to: "pollingHandle.stop()"
      via: "process.on('SIGINT', cleanup) + cleanup() body"
      pattern: "pollingHandle\\.stop\\("
    - from: "kodo orchestrate --help"
      to: "mutex implícito doc string"
      via: "Commander .option description"
      pattern: "mutex implícito|lock per-repo Phase 8"
---

<objective>
Añadir flag `--polling` a `kodo orchestrate` que arranca `startPolling` integrado en el mismo proceso (CFG-04 / D-16..19). El flag es ortogonal a `kodo polling start` daemon path — la mutex es implícita vía lock per-repo Phase 8 GSD-10 (D-17), documentada en `--help`.

**B-3 LOCKED — Decisión arquitectónica del Task 1 caso 4 cerrada AHORA (no diferida al executor):** la lógica de setup `--polling` se extrae a un helper exportado `runOrchestratePollingSetup(opts, deps = {})` en `src/cli/orchestrate.js` (mismo pattern DI usado en Phase 25). El handler `kodo orchestrate` delega a este helper. El test caso 4 hace DI spy in-process: importa el helper, inyecta `startPollingFn` spy, y assert `spy.callCount === 1` con args correctos. **Elimina** la opción alternativa "integration NDJSON event count" — siempre DI in-process.

Purpose: El operador que ya lanza orchestrator desde el repo (Phase 999.1 contract) puede añadir `--polling` para tener un único proceso que cubre orchestrator + polling sin daemon separado. Para uso personal donde "elige uno u otro por repo" es suficiente.

Output:
- `src/cli/orchestrate.js` nuevo con `runOrchestratePollingSetup(opts, deps)` exportado (DI-zable per B-3 LOCKED).
- `src/cli.js` extendido con `.option('--polling', ...)` en el comando orchestrate + lógica de arranque que delega al helper + SIGINT/SIGTERM cleanup mirror src/server.js:490-495.
- `test/cli/orchestrate-polling.test.js` nuevo con ≥4 casos cubriendo CFG-04 exit codes + DI spy + SIGINT cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md
@.planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md
@.planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md
@.planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md
@.planning/phases/26-config-wizard-cli-integration/26-02-SUMMARY.md

<interfaces>
<!-- Contratos del estado del repo tras Plan 26-02 ejecutado. -->

From src/cli.js (existing — extender):
- `program.command('orchestrate').description('Launch the orchestrator Claude session')` — line 126.
- Action handler existente — line ~127-141 (preserve la lógica `launchOrchestrator`).

From src/triggers/polling.js (Phase 25 — REUSE):
- `export function startPolling({provider, repos, intervalSec, ...}) → { stop }` — line 415.
- `repos` shape: `Array<{owner: string, repo: string}>`.

From src/providers/registry.js (Phase 24 — REUSE):
- `export async function initRegistry()`.
- `export function getProvider(name) → TaskProvider`.

From src/config.js (existing — REUSE):
- `export function loadConfig()`.
- `export function getProviderApiKey(providerName)`.

From src/orchestrator/launch.js (existing — REUSE, NO modificar):
- `export async function launchOrchestrator() → { existing|launched }` (signature per src/cli.js:126 usage).

From src/server.js (existing — analog SIGINT pattern, NO modificar):
- SIGTERM/SIGINT cleanup — line 490-495.

From Plan 26-02 deliverables (state pre-execution):
- `src/cli/polling.js` con `runPollingStartCli/StopCli/StatusCli` + `configureGithubProvider/parseGitHubRemote/detectOriginRepo` exports (Plan 26-01 + 26-02).
- `src/cli/polling-daemon.js` con PID file lifecycle (Plan 26-02).
- `kodo polling <sub>` subcommands registrados en `src/cli.js` (Plan 26-02).

From test/fixtures/configs/ (Plan 26-01 creó — REUSE):
- `v0.7-with-github.json` (filename canonical N-1; NO `v0.7-github.json`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Crear test/cli/orchestrate-polling.test.js (RED state, ≥4 casos; caso 4 cerrado a DI spy in-process B-3 LOCKED)</name>
  <files>test/cli/orchestrate-polling.test.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 655-678 — orchestrate-polling.test.js SIGINT pattern verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-VALIDATION.md (líneas 66-69 — 4 casos CFG-04 mapeados)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Pitfall #5 daemon stdio diagnostics)
    - test/skill-sync.test.js:69-79 (spawn child + timeout analog)
    - test/cli/polling.test.js (creado en Plan 26-02 — reusar makeFixture helper si está exportable; sino, copy local con `v0.7-with-github.json` fixture)
  </read_first>
  <behavior>
    - ≥4 casos test:
      1. **sin repos → exit 2** (integration spawnSync): makeFixture con `providers.github.repos = []`; `spawnSync(node, [KODO_BIN, 'orchestrate', '--polling'], {env: {...process.env, HOME: tmpHome}, timeout: 5000})`; assert `result.status === 2` + `result.stderr` match `/providers\.github\.repos is empty/`.
      2. **sin GITHUB_TOKEN → exit 2** (integration spawnSync): makeFixture con `providers.github.repos = [{owner:'foo',repo:'bar'}]` PERO sin `~/.kodo/.env` y `env` scrubbed (`env: { ...process.env, GITHUB_TOKEN: undefined, HOME: tmpHome }` — quitar la var explícitamente); assert `result.status === 2` + stderr match `/GITHUB_TOKEN not set/`.
      3. **SIGINT cleanup → exit 0** (integration spawn + signal): makeFixture válida (repos + token); `spawn(... , {stdio:'pipe'})`; `await sleep(500)` para dar tiempo a montar el timer; `process.kill(child.pid, 'SIGINT')`; `exitCode = await new Promise(r => child.on('exit', r))` envuelto en Promise.race contra timeout 3s; assert `exitCode === 0` (no timer huérfano, no hang).
      4. **startPolling invoked (DI spy in-process) — B-3 LOCKED** (NO integration NDJSON variant):
         ```javascript
         import { runOrchestratePollingSetup } from '../../src/cli/orchestrate.js';

         it('CFG-04: runOrchestratePollingSetup invokes startPolling with correct args (DI spy)', async () => {
           let callCount = 0;
           let capturedArgs = null;
           const startPollingFn = (args) => {
             callCount++;
             capturedArgs = args;
             return { stop: () => {} };  // fake handle
           };
           const configLoader = () => ({
             providers: {
               github: {
                 api_key_env: 'GITHUB_TOKEN',
                 repos: [{ owner: 'foo', repo: 'bar' }],
                 poll_interval: 60,
               },
             },
           });
           const getProviderApiKeyFn = () => 'fake-token';
           const initRegistryFn = async () => {};
           const getProviderFn = (name) => ({ name, init: async () => {} });

           const handle = await runOrchestratePollingSetup(
             { polling: true },
             { startPollingFn, configLoader, getProviderApiKeyFn, initRegistryFn, getProviderFn },
           );

           assert.equal(callCount, 1, 'startPollingFn should be called once');
           assert.equal(capturedArgs.repos.length, 1);
           assert.deepEqual(capturedArgs.repos[0], { owner: 'foo', repo: 'bar' });
           assert.equal(capturedArgs.intervalSec, 60);
           assert.ok(capturedArgs.provider, 'provider must be passed');
           assert.ok(handle && typeof handle.stop === 'function', 'returned handle has stop()');
         });
         ```
      5. (extra recomendado) **Sin `--polling`, comportamiento idéntico** + **--help mutex doc**: assert `--help` muestra `--polling` flag con texto que cita "mutex" o "lock per-repo" (regex match en `result.stdout`).
    - Todos los tests FAIL en este punto porque:
      - El flag `--polling` aún no existe en `src/cli.js` (casos 1, 2, 3, 5).
      - `src/cli/orchestrate.js` con `runOrchestratePollingSetup` aún no existe (caso 4).
    - makeFixture helper local: similar al de polling.test.js (Plan 26-02) — siembra `~/.kodo/config.json` con providers.github + `~/.kodo/.env` con tokens fake. El config file content puede copiarse del fixture `v0.7-with-github.json` (filename canonical N-1).
    - NO live API calls. NO `setTimeout` > 1s salvo timeouts de spawn (bounded 5s).
  </behavior>
  <action>Crear `test/cli/orchestrate-polling.test.js` con `node:test` + `node:assert/strict` + `node:child_process` (spawn + spawnSync) + `node:timers/promises` (sleep). Helper `makeFixture({withRepos, withToken})` que devuelve `{tmpHome}` — content basado en `v0.7-with-github.json`. afterEach() limpia tmpHome con `rmSync({recursive, force})`. Para el caso SIGINT (3), capturar `child.pid` antes del kill y bound el `child.on('exit')` con `Promise.race` contra timeout 3s para evitar hang del test. Para el caso 4 (DI spy in-process B-3 LOCKED), importar `runOrchestratePollingSetup` desde `../../src/cli/orchestrate.js` (módulo que el Task 2 creará); el test FAIL hasta GREEN con `Cannot find module './../../src/cli/orchestrate.js'`. NO incluir alternativa integration NDJSON — caso 4 ES DI spy verbatim.</action>
  <verify>
    <automated>node --test test/cli/orchestrate-polling.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>El test file contiene ≥4 casos (incluyendo el DI spy in-process per B-3 LOCKED) y todos FAIL con `Cannot find module './../../src/cli/orchestrate.js'` o `Unknown option '--polling'` o equivalente — RED state Wave 0 esperado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extraer runOrchestratePollingSetup a src/cli/orchestrate.js + wire flag --polling en src/cli.js (B-3 + W-5 LOCKED order)</name>
  <files>src/cli/orchestrate.js, src/cli.js</files>
  <read_first>
    - .planning/phases/26-config-wizard-cli-integration/26-PATTERNS.md (líneas 378-419 — orchestrate --polling action verbatim)
    - .planning/phases/26-config-wizard-cli-integration/26-RESEARCH.md (Code Example #4 verbatim líneas 355-413; Pitfalls #4 SIGINT race, #6 errors)
    - .planning/phases/26-config-wizard-cli-integration/26-CONTEXT.md (D-16..19, T-26-04..06)
    - src/cli.js:126-141 (orchestrate command actual — extender, NO reescribir)
    - src/server.js:490-495 (SIGINT/SIGTERM cleanup analog)
    - src/cli/skill-sync.js:1-112 (DI pattern Phase 25 precedent para el helper export)
  </read_first>
  <behavior>
    - **B-3 LOCKED — Crear `src/cli/orchestrate.js` con DI export:**
      ```javascript
      // @ts-check

      /**
       * @typedef {{ polling?: boolean }} OrchestrateOpts
       * @typedef {{
       *   startPollingFn?: (args: any) => { stop: () => void },
       *   configLoader?: () => any,
       *   getProviderApiKeyFn?: (name: string) => string | undefined,
       *   initRegistryFn?: () => Promise<void>,
       *   getProviderFn?: (name: string) => any,
       *   errFn?: (s: string) => void,
       * }} Deps
       */

      /**
       * Setup helper for `kodo orchestrate --polling`. Exported for DI testing.
       *
       * Pre-flight gates:
       * - Exit 2 (caller throws/process.exit) si providers.github.repos vacío.
       * - Exit 2 si !getProviderApiKey('github').
       *
       * Returns:
       * - { stop: () => void } el polling handle (caller installs SIGINT/SIGTERM cleanup).
       * - null si --polling no estaba activado.
       *
       * Throws:
       * - Error con .exitCode (2 para config issues) — el handler en src/cli.js captura .exitCode y process.exit().
       *
       * @param {OrchestrateOpts} opts
       * @param {Deps} [deps]
       * @returns {Promise<{ stop: () => void } | null>}
       */
      export async function runOrchestratePollingSetup(opts, deps = {}) {
        if (!opts.polling) return null;

        const configLoader = deps.configLoader
          || (await import('../config.js')).then(m => m.loadConfig);  // pseudo: el executor resuelve dynamic import correcto
        // (el executor escribirá: const { loadConfig, getProviderApiKey } = await import('../config.js'))

        const config = (deps.configLoader || configLoader)();
        const repos = config.providers?.github?.repos || [];
        if (repos.length === 0) {
          const err = new Error('providers.github.repos is empty. Run `kodo config` first.');
          /** @type {any} */ (err).exitCode = 2;
          throw err;
        }
        const getProviderApiKeyFn = deps.getProviderApiKeyFn
          || (await import('../config.js')).getProviderApiKey;
        if (!getProviderApiKeyFn('github')) {
          const err = new Error('GITHUB_TOKEN not set. Export it or add to ~/.kodo/.env.');
          /** @type {any} */ (err).exitCode = 2;
          throw err;
        }

        const initRegistryFn = deps.initRegistryFn
          || (await import('../providers/registry.js')).initRegistry;
        const getProviderFn = deps.getProviderFn
          || (await import('../providers/registry.js')).getProvider;
        await initRegistryFn();
        const provider = getProviderFn('github');

        const startPollingFn = deps.startPollingFn
          || (await import('../triggers/polling.js')).startPolling;
        const handle = startPollingFn({
          provider,
          repos,
          intervalSec: config.providers.github.poll_interval || 60,
        });
        return handle;
      }
      ```
      Nota: La estructura exacta de `await import(...).then(...)` la decide el executor — el contrato clave es que cada dep (configLoader, getProviderApiKeyFn, initRegistryFn, getProviderFn, startPollingFn) es INYECTABLE y, si no se pasa, se resuelve por dynamic import desde el módulo canonical.

    - **W-5 LOCKED — Orden de ejecución en src/cli.js orchestrate handler:**
      Extender el comando `kodo orchestrate` (línea 126 en src/cli.js) con:
      - `.option('--polling', 'Arranca polling integrado en el orchestrator. NO usar con \`kodo polling start\` simultáneo sobre el mismo repo (mutex implícito vía lock per-repo Phase 8 GSD-10).')` — el description text DEBE contener literalmente la frase "mutex implícito" o "lock per-repo" para que el test del `--help` matche (D-17).
      - Action handler con ORDEN ESTRICTO (W-5 LOCKED):
        ```
        action(async (opts) => {
          /** @type {{ stop: () => void } | null} */
          let pollingHandle = null;
          // PASO 0: Instalar SIGINT/SIGTERM handler ANTES de cualquier setup.
          //         Incluso si SIGINT llega durante setup del orchestrator o del polling,
          //         el handler ya está listo. Idempotente: chequea pollingHandle != null.
          const cleanup = () => {
            try { if (pollingHandle) pollingHandle.stop(); } catch {}
            process.exit(0);
          };
          process.on('SIGINT', cleanup);
          process.on('SIGTERM', cleanup);

          try {
            // PASO 1: Iniciar polling ANTES de launchOrchestrator (W-5 LOCKED).
            //         Razón: si SIGINT llega durante launchOrchestrator setup,
            //         cleanup() ya limpia polling. Si polling falla, no se llega a launch.
            if (opts.polling) {
              const { runOrchestratePollingSetup } = await import('./cli/orchestrate.js');
              try {
                pollingHandle = await runOrchestratePollingSetup({ polling: true });
              } catch (e) {
                // exitCode propagated por el helper (2 si config issue).
                if (e?.exitCode) {
                  console.error('Error: ' + e.message);
                  process.exit(e.exitCode);
                }
                throw e;
              }
            }

            // PASO 2: launchOrchestrator() — el polling YA está corriendo en este punto.
            const { launchOrchestrator } = await import('./orchestrator/launch.js');
            const result = await launchOrchestrator(opts);

            // PASO 3: Output existing/launched logic existente preservada.
            // (handler original sigue funcional sin --polling — D-19)
          } catch (err) {
            // W-5 LOCKED — capture global: si launchOrchestrator throws,
            // limpiar pollingHandle ANTES de re-throw / exit.
            try { if (pollingHandle) pollingHandle.stop(); } catch {}
            console.error(`Error: ${err.message}`);
            process.exit(1);
          }
        });
        ```

    - **Resumen de orden (W-5 LOCKED):**
      1. Instalar SIGINT/SIGTERM handler PRIMERO (antes de cualquier setup async).
      2. `pollingHandle = await runOrchestratePollingSetup({...})` ANTES de `launchOrchestrator()`.
      3. `const result = await launchOrchestrator(opts)` DESPUÉS de polling ya activo.
      4. Si `launchOrchestrator` throws → catch outer llama `pollingHandle?.stop()` antes de re-throw / exit 1.
      5. SIGINT en cualquier momento → cleanup llama `pollingHandle?.stop()` + exit 0 (idempotente).

    - Sin `opts.polling`, action handler comportamiento IDÉNTICO a hoy (D-19 zero breaking change — los pasos de polling se saltan; SIGINT handler instalado pero `pollingHandle === null` → `cleanup()` solo hace exit 0).
    - El contrato `cwd=repo` Phase 999.1 NO se altera — `launchOrchestrator()` ya carga skill desde cwd; el polling vive en el mismo proceso async paralelo, NO en worktree.
    - Output formatting: NO usar createFormatter para el cleanup path (mensajes de error son stderr canonical, no necesitan TTY-awareness; alineado con el style del orchestrate handler existente).
    - El cleanup handler debe ser idempotente — si `pollingHandle` es `null` (e.g. crash entre validation y start), `cleanup()` no debe crashear (try/catch envuelve `pollingHandle.stop()`).
  </behavior>
  <action>**B-3 LOCKED:** Crear `src/cli/orchestrate.js` exportando `runOrchestratePollingSetup(opts, deps)` con TODAS las deps inyectables (`startPollingFn, configLoader, getProviderApiKeyFn, initRegistryFn, getProviderFn`). En el default path (sin DI), resolver vía `await import('../config.js')`, `await import('../providers/registry.js')`, `await import('../triggers/polling.js')`. **W-5 LOCKED:** Editar `src/cli.js` con cambio quirúrgico: AÑADIR `.option('--polling', '...')` al comando orchestrate Y reemplazar el action handler siguiendo el ORDEN ESTRICTO descrito (SIGINT handler instalado paso 0; pollingHandle paso 1; launchOrchestrator paso 2; outer catch que limpia polling paso 4). NO mover, NO renombrar, NO refactorizar la lógica existente de `launchOrchestrator()`. Si el handler existente ya tiene un try/catch outer, integrar el cleanup `pollingHandle?.stop()` dentro de ese catch existente (NO crear segundo try/catch).</action>
  <verify>
    <automated>node --test test/cli/orchestrate-polling.test.js 2>&1 | tail -10 ; bin/kodo orchestrate --help 2>&1 | grep -E "polling|mutex|lock per-repo" | head -3</automated>
  </verify>
  <success_criteria>
    - **B-3 LOCKED — DI extraction confirmed**: `src/cli/orchestrate.js` exporta `runOrchestratePollingSetup` con 5 deps inyectables; el test caso 4 in-process spy invoca el helper directamente y assert `callCount === 1` con args correctos. NO integration NDJSON variant.
    - **W-5 LOCKED — order verified**: el handler de `src/cli.js` ejecuta:
      - PASO 0: `process.on('SIGINT', cleanup); process.on('SIGTERM', cleanup);` (ANTES de cualquier import async dentro del action).
      - PASO 1: `pollingHandle = await runOrchestratePollingSetup({...})` ANTES de `launchOrchestrator(opts)`.
      - PASO 2: `const result = await launchOrchestrator(opts)` DESPUÉS de polling activo.
      - PASO 3+4: outer `catch` llama `pollingHandle?.stop()` antes de `process.exit(1)`.
      - Acceptance grep: `grep -n "process.on('SIGINT'" src/cli.js` aparece ANTES de `grep -n "runOrchestratePollingSetup" src/cli.js` que aparece ANTES de `grep -n "launchOrchestrator(opts)" src/cli.js` dentro del bloque del action.
    - **D-17 mutex doc**: `bin/kodo orchestrate --help` contiene `--polling` flag + texto matching `/mutex implícito|lock per-repo/`.
    - **D-19 zero breaking change**: `kodo orchestrate` sin flag se comporta idéntico; el SIGINT handler instalado pero `pollingHandle === null` → `cleanup()` solo hace exit 0.
    - **Cleanup idempotente**: si SIGINT llega antes de que `pollingHandle` esté seteado, `try { if (pollingHandle) pollingHandle.stop(); } catch {}` no crashea.
    - **Test casos**: ≥4 casos green incluyendo el DI spy in-process caso 4.
    - **Suite verde**: `npm test` baseline 741 + ≥4 nuevos = ≥745 pass, 0 fail.
  </success_criteria>
  <done>`test/cli/orchestrate-polling.test.js` pasa los ≥4 casos (incluyendo DI spy in-process caso 4 per B-3 LOCKED). `src/cli/orchestrate.js` exporta `runOrchestratePollingSetup` con DI completo. `src/cli.js` orchestrate handler sigue el orden estricto W-5 LOCKED (SIGINT handler primero, polling antes de launch, outer catch limpia polling). `bin/kodo orchestrate --help` muestra el flag `--polling` con descripción conteniendo "mutex implícito" o "lock per-repo". Suite full `npm test` ≥745 pass, 0 fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → kodo orchestrate --polling | argv y env vars no confiables |
| same process: orchestrator + polling | Crash en polling no debe llevarse al orchestrator (y viceversa) |
| SIGINT/SIGTERM → cleanup handler | Race: si SIGINT llega antes de `pollingHandle = startPolling(...)`, cleanup() referencia `null` |
| runOrchestratePollingSetup helper | DI deps inyectables; en producción resuelven a módulos canonical via dynamic import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Availability | --polling SIGINT race / orphan timer | mitigate | W-5 LOCKED — SIGINT handler instalado en PASO 0 ANTES de cualquier setup async. `cleanup()` envuelve `pollingHandle?.stop()` con check `if (pollingHandle)` para idempotencia (cubre el caso SIGINT pre-start). El test `SIGINT cleanup → exit 0` valida wall-clock (sleep 500ms antes del kill garantiza handle inicializado en happy path). |
| T-26-05 | Tampering (mutex) | --polling vs kodo polling start daemon simultáneos | accept | D-17 — mutex implícito vía lock per-repo Phase 8 GSD-10 (Karpathy Rule 2 simplicity-first). Si el operador arranca AMBOS sobre el mismo repo, el lock per-repo coalesce las sesiones — no doble dispatch. Documentar en `--help` text + summary. NO añadir mutex explícito (overkill v0.7). |
| T-26-06 | Information Disclosure | --polling startup logging | mitigate | El pre-flight gate solo verifica `!!getProviderApiKey('github')` — NUNCA imprime el value del token. Mensajes de error son stderr canonical sin token leak. Reusar invariante T-25-02 Phase 25 (polling.dispatch NDJSON excluye token). Source-hygiene: `grep -cE "process.env.GITHUB_TOKEN(?!.*payload|.*event)" src/cli/orchestrate.js === 0` excepto en pre-flight check legítimo. |
| T-26-CWD | Tampering (contract violation) | cwd=repo Phase 999.1 invariante | mitigate | El polling vive en el mismo proceso del orchestrator (D-16) — NO spawn child, NO worktree. `launchOrchestrator()` ya carga skill desde cwd; el flag `--polling` solo añade un timer async paralelo. Test acceptance: `orchestrate --polling` exit clean tras SIGINT — implica que el proceso es uno solo. |
| T-26-ZBC | Availability (regression) | --polling default off (D-19 zero breaking change) | mitigate | Sin `--polling`, action handler comportamiento IDÉNTICO a hoy. SIGINT handler instalado pero `pollingHandle === null` → `cleanup()` solo hace exit 0 (no-op stop). Tests existentes de `kodo orchestrate` deben seguir pasando sin modificación. Acceptance: `npm test` full suite baseline 741 → 745 pass (4 nuevos), 0 fail. |
| T-26-CRASH | Availability | startPolling throw durante init | mitigate | W-5 LOCKED — Si `runOrchestratePollingSetup(...)` throws (e.g. provider.init() falla por token revocado), el throw se captura en el inner try/catch del handler: si tiene `.exitCode` se hace `process.exit(.exitCode)`; sino se re-throw al outer catch que llama `pollingHandle?.stop()` (puede ser null — idempotente) y exit 1. SIGINT handler instalado en PASO 0 garantiza que incluso un crash durante setup no deja proceso colgando. |
| T-26-DI | Tampering (DI helper) | runOrchestratePollingSetup deps inyectables | accept | B-3 LOCKED — Los 5 deps (`startPollingFn`, `configLoader`, etc.) son inyectables por tests pero NO por el operador (CLI handler siempre llama sin segundo arg). El default path resuelve a módulos canonical via dynamic import. No hay vector de injection del operador hacia el helper. |

</threat_model>

<verification>
- `node --test test/cli/orchestrate-polling.test.js` — ≥4 casos green (incluyendo DI spy in-process caso 4 per B-3 LOCKED).
- `npm test` — full suite ≥745 pass, 0 fail.
- `bin/kodo orchestrate --help` — contiene `--polling` flag + texto matching `/mutex implícito|lock per-repo/`.
- B-3 LOCKED acceptance: `grep -n "export async function runOrchestratePollingSetup" src/cli/orchestrate.js` retorna 1 línea (helper exportado).
- W-5 LOCKED acceptance (orden en handler de `src/cli.js`):
  - `awk '/program\.command\(.orchestrate.\)/,/^\}/' src/cli.js | grep -n "process\.on\(.SIGINT" | head -1` retorna línea ANTES de
  - `awk '/program\.command\(.orchestrate.\)/,/^\}/' src/cli.js | grep -n "runOrchestratePollingSetup" | head -1` que es ANTES de
  - `awk '/program\.command\(.orchestrate.\)/,/^\}/' src/cli.js | grep -n "launchOrchestrator(opts)" | head -1`.
- Smoke manual (out-of-band): `bin/kodo orchestrate --polling` con config válida → arranca; Ctrl+C → exit 0 clean (no hang, no timer huérfano warning).
- Regresión: `bin/kodo orchestrate` (sin flag) debe comportarse idéntico (D-19) — verificar que existing tests/UATs no se rompen.
- `grep -c "removePidFile\|polling.pid" $(git diff --cached --name-only -- src/cli.js src/cli/orchestrate.js)` — `0` (el --polling path NO toca PID file; eso es exclusivo de `kodo polling start`).
</verification>

<success_criteria>
- [ ] CFG-04: `kodo orchestrate --polling` sin repos → exit 2 + stderr canonical (caso 1).
- [ ] CFG-04: `kodo orchestrate --polling` sin GITHUB_TOKEN → exit 2 + stderr canonical (caso 2).
- [ ] CFG-04 / B-3 LOCKED: `runOrchestratePollingSetup` exportado desde `src/cli/orchestrate.js` con 5 deps inyectables; caso 4 in-process DI spy assert `callCount === 1` con args correctos.
- [ ] CFG-04 / D-18: SIGINT en el proceso orchestrator llama `pollingHandle.stop()` antes de `process.exit(0)` — caso 3 assert wall-clock exit 0 dentro de 3s.
- [ ] D-17: `--help` text del flag cita "mutex implícito" o "lock per-repo".
- [ ] D-19: Sin `--polling`, `kodo orchestrate` comportamiento idéntico (regresión libre).
- [ ] W-5 LOCKED: orden estricto en handler — SIGINT handler instalado paso 0; polling antes de launch; outer catch limpia polling.
- [ ] Phase 999.1 contract preservado: el polling vive en el mismo proceso, NO worktree, NO spawn child.
- [ ] N-1: filename `v0.7-with-github.json` usado si el test reusa fixture (canonical).
- [ ] Suite full `npm test` ≥745 pass, 0 fail.
</success_criteria>

<output>
After completion, create `.planning/phases/26-config-wizard-cli-integration/26-03-SUMMARY.md` documentando:
- Casos green count (target ≥4 nuevos sobre baseline post-Plan-26-02).
- B-3 LOCKED — confirmación: caso 4 usa DI spy in-process (NO integration NDJSON). Citar las primeras 10 líneas de `runOrchestratePollingSetup` para auditoría del shape DI.
- W-5 LOCKED — confirmación del orden:
  - Cita literal (line numbers) de las 3 líneas clave en `src/cli.js`: instalación del SIGINT handler (paso 0), llamada a `runOrchestratePollingSetup` (paso 1), llamada a `launchOrchestrator(opts)` (paso 2).
  - Confirmar que outer catch limpia `pollingHandle?.stop()` antes de `process.exit(1)`.
- Validación de la regresión D-19: si existe test previo de `kodo orchestrate` sin flag, citar su nombre y confirmar que sigue verde.
- Cita literal del texto del `--help` text para auditoría del mutex implícito (D-17).
- Notas sobre el invariante cwd=repo Phase 999.1: confirmar que el polling integrado NO requiere worktree changes.
- N-1: confirmación de filename `v0.7-with-github.json` reusado del Plan 26-01.
- Resumen del milestone v0.7: CFG-01..04 all green, Phase 26 completo, ready for Phase 27 (cross-provider contract matrix).
</output>
</output>
