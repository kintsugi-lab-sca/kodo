---
phase: 21-skill-sync-cli-auto-sync
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/orchestrator/launch.js
  - test/orchestrator-auto-sync.test.js
autonomous: true
requirements: [SKILL-02, SKILL-03]
tags: [auto-sync, orchestrator, fail-open, ndjson, cwd-preservation]

must_haves:
  truths:
    - "T-21.02-01 (D-03): launchOrchestrator ejecuta syncSkill ANTES de cmux.listWorkspaces() (entre L40 y L42 actuales); si falla emite skill.sync.auto.error y CONTINÚA fail-open"
    - "T-21.02-02 (D-03b): drift detectado → emit skill.sync.auto con { source, dest, files_changed }; status=noop → SIN evento (sin ruido por cada launch)"
    - "T-21.02-03 (D-05c): auto-sync NUNCA pasa prune=true a syncSkill (prune es solo flag manual del CLI)"
    - "T-21.02-04 (SKILL-03 + D-10): Constraint cwd=repo Phase 999.1 D-04/D-05/D-06 preservada — el bloque NO modifica process.cwd() ni los args de cmux.newWorkspace; orchestrator NO lee ~/.claude/skills/kodo-orchestrate/skill.md"
    - "T-21.02-05 (Pattern C): KODO_ROOT_FOR_SKILL = process.env.KODO_ROOT || process.cwd() permite test isolation con tmpdir"
    - "T-21.02-06 (D-08b): syncSkill importado desde EXACTAMENTE 2 callsites — src/cli/skill-sync.js (Plan 01) + src/orchestrator/launch.js (Plan 02)"
  artifacts:
    - path: "src/orchestrator/launch.js"
      provides: "Auto-sync fail-open block insertado entre L40 (log start) y L42 (workspace check)"
      contains: "syncSkill"
    - path: "test/orchestrator-auto-sync.test.js"
      provides: "5 escenarios — auto-sync ok/noop/error + source-hygiene cross-callsite + SKILL-03 invariante"
      min_lines: 120
  key_links:
    - from: "src/orchestrator/launch.js"
      to: "src/skill/sync.js"
      via: "static import { syncSkill } at top"
      pattern: "from\\s+['\\\"]\\.\\./skill/sync\\.js['\\\"]"
    - from: "src/orchestrator/launch.js"
      to: "src/logger-events.js"
      via: "static import { skillSyncAuto, skillSyncAutoError } at top"
      pattern: "skillSyncAuto.*skillSyncAutoError"
    - from: "auto-sync block (between L40 and L42)"
      to: "cmux.listWorkspaces() (L45 actual)"
      via: "executes BEFORE first cmux side-effect"
      pattern: "syncSkill[\\s\\S]*?cmux\\.listWorkspaces"
---

<objective>
Cablear el hook auto-sync fail-open en `launchOrchestrator` (entre L40 y L42 — ANTES de `cmux.listWorkspaces()` L45) consumiendo el módulo `syncSkill` de Plan 01. Emite `skill.sync.auto` / `skill.sync.auto.error` en NDJSON sin bloquear el launch. Crea test file con 3 escenarios auto-sync in-process + 1 source-hygiene cross-callsite + 1 SKILL-03 invariante.

Purpose: Materializar SKILL-02 (drift detection auto-sync con event) + SKILL-03 (Constraint cwd=repo preservada — el sync es side-effect previo al launch, NO modifica el contrato Phase 999.1 D-04..D-06).

Output: 1 archivo modificado + 1 archivo nuevo. Suite verde con 5 escenarios nuevos (3 auto-sync + 1 source-hygiene + 1 SKILL-03 invariante).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-CONTEXT.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-RESEARCH.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md
@.planning/phases/21-skill-sync-cli-auto-sync/21-01-SUMMARY.md

# Source canonical (read-only — NUNCA modificar)
@.claude/skills/kodo-orchestrate/skill.md

# Analogs directos
@src/hooks/stop.js
@src/orchestrator/launch.js
@src/logger.js
@test/gsd-verify-integration.test.js
@test/orchestrator-launch.test.js

# Artifacts del Plan 01 (módulo + events disponibles tras Plan 01)
@src/skill/sync.js
@src/cli/skill-sync.js
@src/logger-events.js

<interfaces>
<!-- Contratos consumidos. Plan 01 los crea — Plan 02 los IMPORTA tal cual.
     NO re-explorar — el contrato está aquí. -->

From src/skill/sync.js (Plan 01 — disponible al iniciar Plan 02):
```js
/**
 * @typedef {{ source: string, dest: string, prune?: boolean, logger?: Logger }} SyncSkillOpts
 * @typedef {{ status: 'ok'|'noop'|'error', files_changed: number, files_pruned?: number,
 *             symlink_replaced?: boolean, error?: string }} SyncSkillResult
 */
export function syncSkill(opts: SyncSkillOpts): SyncSkillResult
```

From src/logger-events.js (Plan 01 — disponible al iniciar Plan 02):
```js
export const EVENTS = { SKILL_SYNC_AUTO: 'skill.sync.auto', SKILL_SYNC_AUTO_ERROR: 'skill.sync.auto.error', ... }
export function skillSyncAuto(logger, { source, dest, files_changed }): void  // logger.info
export function skillSyncAutoError(logger, { source, dest, error }): void     // logger.error
```

From src/orchestrator/launch.js (CURRENT STATE — leído línea por línea 1-157):
```js
// Header lines 1-13 (imports + __dirname + PROMPT_PATH + ORCHESTRATOR_WORKSPACE_NAME)
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { loadConfig } from '../config.js';
import { listSessions } from '../session/state.js';
import * as cmux from '../cmux/client.js';
import { getSessionMode } from '../labels.js';

// Función signature L37 — bloque actual:
export async function launchOrchestrator(opts = {}) {
  const config = loadConfig();                                              // L38
  const log = opts.logger?.child({ component: 'orchestrator' });            // L39
  log?.info('orchestrator.launch.start', { provider: config.provider });    // L40
                                                                            // L41 ← INSERTION POINT
  // Check if orchestrator is already running                               // L42
  let workspaceList;                                                        // L43
  try {                                                                     // L44
    workspaceList = await cmux.listWorkspaces();                            // L45
    // ...resto del flujo intacto
```

From src/hooks/stop.js (KODO_ROOT pattern — analog literal — líneas 19-20):
```js
const __dirname = dirname(fileURLToPath(import.meta.url));
const KODO_ROOT = process.env.KODO_ROOT || join(__dirname, '..', '..');
// Tests inyectan: env: { ...process.env, HOME: tmpHome, KODO_ROOT: tmpRepo }
```

From src/hooks/stop.js (outer try/catch fail-open pattern — análogo Phase 19):
```js
try {
  // inner ops with event emission per failure mode
  if (errorCondition) {
    if (log) eventEmitterError(log, { ... });
  }
} catch (err) {
  // defense in depth: console.error, never throw
  console.error(`[kodo:component] op failed: ${err.message}`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test/orchestrator-auto-sync.test.js con 5 escenarios in-process (RED phase — Tests D + E fallarán hasta Task 2)</name>
  <files>test/orchestrator-auto-sync.test.js</files>
  <read_first>
    - .planning/phases/21-skill-sync-cli-auto-sync/21-RESEARCH.md (§3 escenarios auto-sync líneas 397-425)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md (§test/orchestrator-auto-sync.test.js líneas 537-579 — memSink + DI patrón)
    - test/gsd-verify-integration.test.js (líneas 72-119 — makeMemSink logger factory + makeDeps)
    - test/skill-auto-commit.test.js (líneas 22-115 — makeFixture + HOME override + chmod cleanup)
    - test/skill-sync.test.js (Plan 01 — reusar makeFixture si fue extraído a helpers/; si no, duplicar inline)
    - src/skill/sync.js (Plan 01 — leer signature + SyncSkillResult typedef)
    - src/logger-events.js (Plan 01 — leer skillSyncAuto + skillSyncAutoError signature)
  </read_first>
  <behavior>
    Tests in-process consumiendo `syncSkill` + `skillSyncAuto/Error` directamente (NO spawn — auto-sync vive dentro del proceso orchestrator):

    - Test A (drift detectado → ok event): tmpRepo con skill canonical sembrada; tmpHome con dest vacío; `result = syncSkill({ source, dest })` → `status: 'ok', files_changed: 2`; simular caller emitiendo `skillSyncAuto(memLogger, { source, dest, files_changed: 2 })`; assert memSink.records contiene UN record con `event: 'skill.sync.auto'`, `source`, `dest`, `files_changed: 2`, `level: 'info'`.

    - Test B (no drift → SIN event): primer run completo + segundo run de syncSkill sobre el mismo dest; `result.status === 'noop'`; caller simulado correcto NO debe llamar `skillSyncAuto` ni `skillSyncAutoError`; assert memSink.records SIN entries de `skill.sync.*`. Test BLINDA D-03b (silencio total en noop para evitar ruido por launch).

    - Test C (sync error → error event + fail-open): chmodSync(dest, 0o500) para forzar EACCES; `result = syncSkill({ source, dest })` → status='error' (outer try/catch en syncSkill) O syncSkill throw. El caller simulado captura ambos y emite `skillSyncAutoError(memLogger, { source, dest, error })`. Assert memSink.records contiene UN record con `event: 'skill.sync.auto.error'`, `error` truthy, `level: 'error'`. AfterEach restaura permisos.

    - Test D (D-08b source-hygiene cross-callsite): readFileSync de src/cli/skill-sync.js + src/orchestrator/launch.js con stripComments; assert AMBOS importan `from '../skill/sync.js'`; cross-repo grep `execSync('grep -rl from.*skill/sync src/')` → exactamente `[src/cli/skill-sync.js, src/orchestrator/launch.js]` (sorted). RECHAZA cualquier tercer importer no autorizado.

    - Test E (SKILL-03 invariante): readFileSync de `src/orchestrator/launch.js` + stripComments; assert NO contiene lectura de `~/.claude/skills/.../skill.md` (regex negativo); assert SÍ contiene la palabra `syncSkill` (la invocación del hook Plan 02 está presente).

    Tests A, B, C deben PASAR post-Plan 01 (syncSkill + helpers existen).
    Tests D, E deben FALLAR en este punto (RED — launch.js aún no tiene el wiring; Task 2 los pone GREEN).
  </behavior>
  <action>
**Paso 1.A — Crear `test/orchestrator-auto-sync.test.js` con header + imports + helpers.**

Header literal + imports (mirror de test/gsd-verify-integration.test.js + test/skill-auto-commit.test.js):

```javascript
// @ts-check
//
// test/orchestrator-auto-sync.test.js — Phase 21 SKILL-02 + SKILL-03.
//
// Tests in-process del hook auto-sync que Plan 02 cabela en src/orchestrator/launch.js
// ANTES de cmux.listWorkspaces() (entre L40 y L42 actuales). Consumimos syncSkill +
// skillSyncAuto/Error directamente (no spawn — auto-sync vive dentro del proceso).
//
// Cobertura:
//   A — drift detected → skill.sync.auto event (D-03b)
//   B — no drift → SIN event (D-03b silencio total)
//   C — sync error → skill.sync.auto.error event + caller fail-open (D-03)
//   D — source-hygiene cross-callsite (D-08b — exactamente 2 importers de syncSkill)
//   E — SKILL-03 invariante (orchestrator NO lee ~/.claude/skills/.../skill.md)

import { describe, it, afterEach } from 'node:test';
import assert from 'node:assert/strict';
import { execSync } from 'node:child_process';
import {
  mkdtempSync, rmSync, mkdirSync, writeFileSync, readFileSync, chmodSync,
} from 'node:fs';
import { tmpdir } from 'node:os';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { syncSkill } from '../src/skill/sync.js';
import { skillSyncAuto, skillSyncAutoError } from '../src/logger-events.js';

const __dirname = dirname(fileURLToPath(import.meta.url));
const REPO = resolve(__dirname, '..');
```

memSink + makeFixture + helpers (mirror exacto de Pattern §memSink lines 544-554 + §makeFixture):

```javascript
function makeMemSink() {
  const records = [];
  const logger = {
    info:  (msg, fields) => records.push({ level: 'info',  msg, fields: fields || {} }),
    warn:  (msg, fields) => records.push({ level: 'warn',  msg, fields: fields || {} }),
    error: (msg, fields) => records.push({ level: 'error', msg, fields: fields || {} }),
    debug: (msg, fields) => records.push({ level: 'debug', msg, fields: fields || {} }),
    child: () => logger,
  };
  return { logger, records };
}

function makeFixture() {
  const tmpHome = mkdtempSync(join(tmpdir(), 'kodo-auto-sync-home-'));
  const tmpRepo = mkdtempSync(join(tmpdir(), 'kodo-auto-sync-repo-'));
  const skillDir = join(tmpRepo, '.claude', 'skills', 'kodo-orchestrate');
  mkdirSync(skillDir, { recursive: true });
  writeFileSync(join(skillDir, 'skill.md'), '# kodo:orchestrate\n\nv1\n', 'utf-8');
  writeFileSync(join(skillDir, 'extra.md'), 'extra\n', 'utf-8');
  return { tmpHome, tmpRepo };
}

function sourceFor(tmpRepo) { return join(tmpRepo, '.claude', 'skills', 'kodo-orchestrate'); }
function destFor(tmpHome) { return join(tmpHome, '.claude', 'skills', 'kodo-orchestrate'); }

function stripComments(src) {
  return src
    .replace(/\/\*[\s\S]*?\*\//g, '')
    .split('\n')
    .filter((l) => !l.trim().startsWith('//') && !l.trim().startsWith('*'))
    .join('\n');
}

let _tmpHome, _tmpRepo;
afterEach(() => {
  if (_tmpHome) {
    try { chmodSync(destFor(_tmpHome), 0o755); } catch {}
    rmSync(_tmpHome, { recursive: true, force: true });
  }
  if (_tmpRepo) rmSync(_tmpRepo, { recursive: true, force: true });
  _tmpHome = undefined; _tmpRepo = undefined;
});
```

**Paso 1.B — Implementar los 5 tests A..E.**

Test A (drift detectado → ok event):

```javascript
describe('auto-sync (SKILL-02)', () => {
  it('A: drift detected → emit skill.sync.auto with files_changed > 0', () => {
    ({ tmpHome: _tmpHome, tmpRepo: _tmpRepo } = makeFixture());
    const { logger, records } = makeMemSink();
    const source = sourceFor(_tmpRepo);
    const dest = destFor(_tmpHome);

    const result = syncSkill({ source, dest });
    assert.equal(result.status, 'ok');
    assert.equal(result.files_changed, 2);

    // Simula el caller (orchestrator): emite skill.sync.auto en status='ok'.
    skillSyncAuto(logger, { source, dest, files_changed: result.files_changed });

    const skillRecords = records.filter((r) => r.fields.event === 'skill.sync.auto');
    assert.equal(skillRecords.length, 1);
    assert.equal(skillRecords[0].level, 'info');
    assert.equal(skillRecords[0].fields.source, source);
    assert.equal(skillRecords[0].fields.dest, dest);
    assert.equal(skillRecords[0].fields.files_changed, 2);
  });

  it('B: no drift → silencio total (D-03b — sin .noop event para evitar ruido)', () => {
    ({ tmpHome: _tmpHome, tmpRepo: _tmpRepo } = makeFixture());
    const { logger, records } = makeMemSink();
    const source = sourceFor(_tmpRepo);
    const dest = destFor(_tmpHome);

    syncSkill({ source, dest }); // primer run: ok
    const result = syncSkill({ source, dest }); // segundo run: noop
    assert.equal(result.status, 'noop');

    // Caller correcto: NO emite event en noop (simulando lógica del bloque Plan 02).
    if (result.status === 'error') {
      skillSyncAutoError(logger, { source, dest, error: result.error || 'x' });
    } else if (result.status === 'ok') {
      skillSyncAuto(logger, { source, dest, files_changed: result.files_changed });
    }

    const skillRecords = records.filter((r) => /^skill\.sync\./.test(r.fields.event || ''));
    assert.equal(skillRecords.length, 0,
      `expected 0 skill.sync.* records, got: ${JSON.stringify(skillRecords)}`);
  });

  it('C: sync error → emit skill.sync.auto.error + caller continues fail-open', () => {
    ({ tmpHome: _tmpHome, tmpRepo: _tmpRepo } = makeFixture());
    const { logger, records } = makeMemSink();
    const source = sourceFor(_tmpRepo);
    const dest = destFor(_tmpHome);

    syncSkill({ source, dest }); // primer run para crear dest dir
    chmodSync(dest, 0o500);      // forzar EACCES
    writeFileSync(join(source, 'skill.md'), '# kodo:orchestrate\n\nv2 modified\n', 'utf-8');

    let result;
    try {
      result = syncSkill({ source, dest });
    } catch (e) {
      result = { status: 'error', files_changed: 0, error: e.message };
    }
    assert.equal(result.status, 'error');
    assert.ok(result.error, 'expected error string truthy');

    skillSyncAutoError(logger, { source, dest, error: result.error });

    const errorRecords = records.filter((r) => r.fields.event === 'skill.sync.auto.error');
    assert.equal(errorRecords.length, 1);
    assert.equal(errorRecords[0].level, 'error');
    assert.equal(errorRecords[0].fields.source, source);
    assert.equal(errorRecords[0].fields.dest, dest);
    assert.ok(errorRecords[0].fields.error);
  });
});

describe('source-hygiene D-08b (Phase 21)', () => {
  it('D: syncSkill imported from exactly 2 callsites (CLI handler + orchestrator launch)', () => {
    const cliHandler = readFileSync(join(REPO, 'src', 'cli', 'skill-sync.js'), 'utf-8');
    const launchMod  = readFileSync(join(REPO, 'src', 'orchestrator', 'launch.js'), 'utf-8');

    assert.match(stripComments(cliHandler), /from\s+['"]\.\.\/skill\/sync\.js['"]/,
      'src/cli/skill-sync.js must import from ../skill/sync.js');
    assert.match(stripComments(launchMod), /from\s+['"]\.\.\/skill\/sync\.js['"]/,
      'src/orchestrator/launch.js must import from ../skill/sync.js (Plan 02 wiring)');

    const grepOutput = execSync('grep -rl "from.*skill/sync" src/ || true', {
      cwd: REPO, encoding: 'utf-8',
    });
    const importers = grepOutput.trim().split('\n').filter(Boolean).sort();
    assert.deepEqual(
      importers,
      ['src/cli/skill-sync.js', 'src/orchestrator/launch.js'].sort(),
      `expected exactly 2 importers, got: ${importers.join(', ')}`,
    );
  });
});

describe('SKILL-03 invariante (Phase 999.1 D-04..D-06 preserved)', () => {
  it('E: src/orchestrator/launch.js NO reads ~/.claude/skills/kodo-orchestrate/skill.md', () => {
    const launchMod = stripComments(readFileSync(join(REPO, 'src', 'orchestrator', 'launch.js'), 'utf-8'));

    // Negativo 1: ningún readFileSync sobre .claude/skills/... + skill.md combo.
    assert.equal(
      /readFileSync\s*\([^)]*\.claude\/skills[\s\S]{0,80}skill\.md/.test(launchMod), false,
      'launch.js must NOT readFileSync from .claude/skills/.../skill.md (auto-load by cwd, not direct read)',
    );

    // Negativo 2: ningún homedir() + skill.md combo (lectura desde home).
    assert.equal(
      /homedir\s*\(\)[\s\S]{0,80}skill\.md/.test(launchMod), false,
      'launch.js must NOT read homedir-based skill.md (Phase 999.1 D-04 invariant)',
    );

    // Positivo: el bloque Plan 02 SÍ invoca syncSkill.
    assert.match(launchMod, /syncSkill\s*\(/,
      'launch.js must invoke syncSkill (Plan 02 hook present)');
  });
});
```
  </action>
  <verify>
    <automated>node --test test/orchestrator-auto-sync.test.js 2>&1 | tail -25 | tee /tmp/p21-02-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/orchestrator-auto-sync.test.js` ejecuta sin syntax errors.
    - Tests A, B, C pass (syncSkill + helpers ya existen post-Plan 01).
    - Tests D, E FAIL en este punto (RED phase — launch.js aún no tiene el wiring; Task 2 los pone GREEN).
    - `grep -c "describe(" test/orchestrator-auto-sync.test.js` → 3 (auto-sync, source-hygiene, SKILL-03).
    - `grep -cE "it\\(.A:|it\\(.B:|it\\(.C:|it\\(.D:|it\\(.E:" test/orchestrator-auto-sync.test.js` → 5.
    - `grep -cE "from\\s+['\\\"]\\.\\.\\/src\\/skill\\/sync\\.js['\\\"]" test/orchestrator-auto-sync.test.js` → 1.
    - `grep -cE "from\\s+['\\\"]\\.\\.\\/src\\/logger-events\\.js['\\\"]" test/orchestrator-auto-sync.test.js` → 1.
  </acceptance_criteria>
  <done>
    test/orchestrator-auto-sync.test.js creado con 5 tests cubriendo D-03b (drift + noop + error event), D-08b (cross-callsite grep), SKILL-03 (no home read). A/B/C pass; D/E fail por diseño (RED phase — Task 2 los pone GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear bloque auto-sync fail-open en src/orchestrator/launch.js entre L40 y L42 (GREEN para Tests D + E)</name>
  <files>src/orchestrator/launch.js</files>
  <read_first>
    - .planning/phases/21-skill-sync-cli-auto-sync/21-RESEARCH.md (§Inserción en launchOrchestrator líneas 143-209 — bloque literal + análisis L44 vs L70)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-PATTERNS.md (§src/orchestrator/launch.js líneas 336-385 — bloque literal a insertar)
    - .planning/phases/21-skill-sync-cli-auto-sync/21-CONTEXT.md (§D-03 fail-open + D-05c no prune + D-10 cwd preservation)
    - src/orchestrator/launch.js (líneas 1-157 — INSERTION POINT entre L40 y L42, ANTES del comentario "Check if orchestrator is already running")
    - src/hooks/stop.js (líneas 19-20 — KODO_ROOT pattern literal; outer try/catch fail-open patrón)
  </read_first>
  <behavior>
    Tras Task 2:
    - Test D pasa: src/orchestrator/launch.js contiene `import { syncSkill } from '../skill/sync.js'` (stripComments-friendly).
    - Test E pasa: launch.js contiene `syncSkill(` invocation pero NO lee `~/.claude/skills/.../skill.md` (los regex negativos pasan).
    - `launchOrchestrator()` ejecuta el bloque auto-sync ANTES del primer `cmux.listWorkspaces()` (línea 45 original).
    - `KODO_ROOT_FOR_SKILL = process.env.KODO_ROOT || process.cwd()` permite test isolation via env override.
    - Bloque NO modifica `process.cwd()` ni los args de `cmux.newWorkspace({ cwd: process.cwd() })` línea 72 — Phase 999.1 D-04 intacta.
    - Si syncSkill throw (defense in depth) → console.error + continuar (sin crash del orchestrator).
    - Comentario Phase 18 D-06 sobre exclusión `--worktree` (líneas 83-101 originales) PRESERVADO byte-a-byte (verificable por grep).
    - Suite global no regresiona: tests pre-existentes de orchestrator launch siguen verdes (el bloque está dentro de try/catch fail-open → si syncSkill falla en tests legacy sin HOME isolation, console.error en stderr pero el test continúa).
  </behavior>
  <action>
**Paso 2.A — Añadir 3 imports + 1 constante al top de `src/orchestrator/launch.js`.**

EDICIÓN 1 — Bloque de imports (líneas 1-9 actuales).

ANTES:
```javascript
// @ts-check
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { loadConfig } from '../config.js';
import { listSessions } from '../session/state.js';
import * as cmux from '../cmux/client.js';
import { getSessionMode } from '../labels.js';
```

DESPUÉS (añadir `homedir`, `syncSkill`, `skillSyncAuto/Error`):
```javascript
// @ts-check
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { homedir } from 'node:os';
import { loadConfig } from '../config.js';
import { listSessions } from '../session/state.js';
import * as cmux from '../cmux/client.js';
import { getSessionMode } from '../labels.js';
import { syncSkill } from '../skill/sync.js';
import { skillSyncAuto, skillSyncAutoError } from '../logger-events.js';
```

EDICIÓN 2 — Constante KODO_ROOT_FOR_SKILL (después de la línea actual `const ORCHESTRATOR_WORKSPACE_NAME = 'kodo-orchestrator';`):

ANTES (líneas 11-13 actuales):
```javascript
const __dirname = dirname(fileURLToPath(import.meta.url));
const PROMPT_PATH = join(__dirname, 'prompt.md');
const ORCHESTRATOR_WORKSPACE_NAME = 'kodo-orchestrator';
```

DESPUÉS (añadir constante al final del bloque):
```javascript
const __dirname = dirname(fileURLToPath(import.meta.url));
const PROMPT_PATH = join(__dirname, 'prompt.md');
const ORCHESTRATOR_WORKSPACE_NAME = 'kodo-orchestrator';
// Phase 21 D-08 + Pattern C: KODO_ROOT override aditivo para test isolation
// (mismo patrón que src/hooks/stop.js:20; permite spawnSync con env.KODO_ROOT=tmpRepo).
const KODO_ROOT_FOR_SKILL = process.env.KODO_ROOT || process.cwd();
```

**Paso 2.B — Insertar el bloque auto-sync entre L40 y L42** (ANTES del comentario `// Check if orchestrator is already running`).

ANTES (líneas 37-45 actuales):
```javascript
export async function launchOrchestrator(opts = {}) {
  const config = loadConfig();
  const log = opts.logger?.child({ component: 'orchestrator' });
  log?.info('orchestrator.launch.start', { provider: config.provider });

  // Check if orchestrator is already running
  let workspaceList;
  try {
    workspaceList = await cmux.listWorkspaces();
```

DESPUÉS (insertar el bloque entre L40 y L42; sin tocar el resto del flujo):
```javascript
export async function launchOrchestrator(opts = {}) {
  const config = loadConfig();
  const log = opts.logger?.child({ component: 'orchestrator' });
  log?.info('orchestrator.launch.start', { provider: config.provider });

  // ─── PHASE 21 D-03 fail-open auto-sync ──────────────────────────────────
  // Sincroniza canonical skill <repo>/.claude/skills/kodo-orchestrate/ → home
  // ANTES del primer side-effect cmux (D-08 SoSoT: mismo módulo que kodo skill sync).
  //
  // Insertado aquí (no antes de cmux.newWorkspace L70) para cubrir el caso
  // "orchestrator ya existe": el operador hace `kodo orchestrate` para refrescar
  // y home debe quedar coherente — RESEARCH §Inserción L44 vs L70.
  //
  // Si syncSkill falla: emit skill.sync.auto.error + continuar (D-03 fail-open —
  // la skill local del repo gana por construcción Phase 999.1 D-04, así el
  // orchestrator funciona aunque home quede stale). NUNCA prune (D-05c).
  //
  // SKILL-03 invariante: este bloque NO toca process.cwd() ni los args de
  // cmux.newWorkspace({ cwd: process.cwd() }) (línea ~72). La skill canonical
  // sigue siendo la del repo (cwd=repo Phase 999.1 D-04/D-05/D-06 intacto).
  try {
    const skillSource = join(KODO_ROOT_FOR_SKILL, '.claude', 'skills', 'kodo-orchestrate');
    const skillDest = join(homedir(), '.claude', 'skills', 'kodo-orchestrate');
    const skillResult = syncSkill({ source: skillSource, dest: skillDest }); // prune NEVER true (D-05c)
    if (skillResult.status === 'error') {
      if (log) skillSyncAutoError(log, { source: skillSource, dest: skillDest, error: skillResult.error || 'unknown' });
    } else if (skillResult.status === 'ok') {
      if (log) skillSyncAuto(log, { source: skillSource, dest: skillDest, files_changed: skillResult.files_changed });
    }
    // status === 'noop' → silencio total (D-03b — sin .noop event para evitar ruido).
  } catch (err) {
    // Defense in depth: si syncSkill throws inesperado, fail-open silencioso.
    // Mismo patrón que stop.js outer catch del worktree cleanup (Phase 19 D-03).
    console.error(`[kodo:orchestrator] skill sync failed: ${/** @type {Error} */ (err).message}`);
  }
  // ────────────────────────────────────────────────────────────────────────

  // Check if orchestrator is already running
  let workspaceList;
  try {
    workspaceList = await cmux.listWorkspaces();
```

**Paso 2.C — Verificar comentario Phase 18 D-06 intacto.**

El bloque de comentarios sobre exclusión `--worktree` (líneas 83-101 originales) DEBE preservarse byte-a-byte. Tras la inserción, las líneas se desplazan ~30 posiciones hacia abajo. Verificar:

```bash
grep -c "Phase 18 D-06: launchOrchestrator EXCLUIDO de --worktree" src/orchestrator/launch.js
```

Debe seguir devolviendo `1`. Si devuelve `0`, el comentario se rompió — restaurar.

**Paso 2.D — Verificar Tests D + E ahora pasan (GREEN).**

```bash
node --test test/orchestrator-auto-sync.test.js 2>&1 | tail -15
```

Esperar: A, B, C, D, E todos pass.

**Posibles fallos y soluciones:**

- Test E `regex negativo /homedir\s*\(\)[\s\S]{0,80}skill\.md/` falla porque el bloque contiene `homedir()` y luego `skill.md` aparece más adelante (en el log o en algún string adyacente):
  - Verificar: el bloque insertado contiene `homedir()` + path al DIR `kodo-orchestrate` (NO al archivo `skill.md`). Si el archivo `launch.js` no contiene literal `skill.md` cerca del `homedir()`, el regex no matcha.
  - Si falla: revisar que el path string usado sea `join(homedir(), '.claude', 'skills', 'kodo-orchestrate')` SIN `'skill.md'` al final.

- Test D fallaría si algún otro archivo en `src/` también importa `from '../skill/sync.js'` o `from './skill/sync.js'`. Verificar:
  ```bash
  grep -rln "from.*skill/sync" src/
  ```
  Output esperado:
  ```
  src/cli/skill-sync.js
  src/orchestrator/launch.js
  ```

**Paso 2.E — Verificar suite global sin regresión.**

```bash
npm test 2>&1 | tail -10
```

Esperar suite verde. Si un test legacy de orchestrator falla por interacción con el filesystem real del runner (ej. `test/orchestrator-launch.test.js` sin HOME tmpdir), la salvaguarda es:
1. Si `cwd` del runner es el repo kodo real → `syncSkill` corre contra `<repo>/.claude/skills/kodo-orchestrate/` (real) y `~/.claude/skills/kodo-orchestrate/` (real del usuario). Es exactamente el efecto previsto en producción.
2. Si el test estaba mockeando `cmux.listWorkspaces` y asertando sobre el flujo → el bloque auto-sync NO debe romperlo porque va antes y es fail-open.
3. Si aparece un crash genuino: añadir el patch mínimo al test legacy inyectando `process.env.KODO_ROOT = tmpdir` y `process.env.HOME = tmpHome` en el setup.

NO modificar tests legacy a menos que el fallo sea real y no relacionado con HOME pollution aceptable.
  </action>
  <verify>
    <automated>node --test test/orchestrator-auto-sync.test.js 2>&1 | tail -15; echo "---grep checks---"; grep -c "from '../skill/sync.js'" src/orchestrator/launch.js; grep -c "Phase 18 D-06: launchOrchestrator EXCLUIDO" src/orchestrator/launch.js; grep -c "KODO_ROOT_FOR_SKILL" src/orchestrator/launch.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/orchestrator-auto-sync.test.js` → 5/5 tests pass (A, B, C, D, E).
    - `grep -c "import { syncSkill } from '../skill/sync.js'" src/orchestrator/launch.js` → exactamente 1.
    - `grep -c "import { skillSyncAuto, skillSyncAutoError } from '../logger-events.js'" src/orchestrator/launch.js` → exactamente 1.
    - `grep -c "KODO_ROOT_FOR_SKILL" src/orchestrator/launch.js` → ≥2 (1 const + 1 uso en el bloque).
    - `grep -c "Phase 18 D-06: launchOrchestrator EXCLUIDO de --worktree" src/orchestrator/launch.js` → exactamente 1 (comentario preservado).
    - `grep -cE "process\\.cwd\\(\\)" src/orchestrator/launch.js` → ≥2 (1 en KODO_ROOT_FOR_SKILL fallback + 1 en cmux.newWorkspace cwd línea ~72). El bloque auto-sync NUNCA llama `process.chdir` ni modifica cwd.
    - `grep -c "console.error(\`\\[kodo:orchestrator\\] skill sync failed" src/orchestrator/launch.js` → exactamente 1 (outer catch fail-open).
    - `grep -cE "syncSkill\\([^)]*prune:\\s*true" src/orchestrator/launch.js` → 0 (D-05c — auto-sync NUNCA prune).
    - `npm test 2>&1 | tail -5 | grep -E "(fail 0|0 failures)"` → suite global verde.
  </acceptance_criteria>
  <done>
    Bloque auto-sync fail-open insertado entre L40 y L42 con 3 imports + 1 constante. Tests A/B/C/D/E pass. Phase 18 D-06 comment preservado. Suite global verde. SKILL-02 + SKILL-03 + D-03 + D-03b + D-05c + D-08b + D-10 implementados como código verificable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `kodo orchestrate` CLI invocation → process | El operador lanza desde cwd controlado (típicamente repo kodo); cwd determina `KODO_ROOT_FOR_SKILL` salvo override env. |
| process.env.KODO_ROOT override | Test-only path; permite spawnSync con tmpdir aislado (mismo trust level que `HOME` override Phase 999.1 D-16). |
| Auto-sync block → syncSkill (Plan 01) | Mismo trust level que CLI manual: el módulo es puro, sin emit de eventos, retorna SyncSkillResult. |
| syncSkill error → console.error fallback | Defense in depth: si syncSkill THROW inesperado, el orchestrator NO crashea (fail-open). |
| Auto-sync block → cmux.listWorkspaces() | Side-effect cmux corre DESPUÉS del sync; si sync falla, cmux ejecuta normalmente (la skill local del repo gana por Phase 999.1 D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.02-01 | Tampering | Auto-sync block before cmux side-effects | accept | Bloque corre antes de cualquier mutación; el orchestrator NO depende del éxito del sync para funcionar (skill local gana por cwd Phase 999.1 D-04). |
| T-21.02-02 | Denial of Service | syncSkill bloquea launch en cada `kodo orchestrate` | accept | RESEARCH §latency penalty: ~5ms por launch (hash 8KB + compare). Despreciable vs el overhead de cmux + Claude startup. |
| T-21.02-03 | Denial of Service | syncSkill throw inesperado crashea orchestrator | mitigate | Outer try/catch con `console.error` + continuar (D-03 fail-open + defense in depth). Test C cubre el path. |
| T-21.02-04 | Information Disclosure | Event NDJSON expone paths absolutos | accept | `source` + `dest` son paths locales (skill canonical del repo + home del operador); ningún secreto. Phase 19 worktreeCleanupOk emite mismo shape. |
| T-21.02-05 | Tampering | process.env.KODO_ROOT permite redirigir source | accept | Override es opt-in (test-only); operador con KODO_ROOT malicioso es self-DoS. Phase 999.1 D-16 ya validó este pattern. |
| T-21.02-06 | Elevation of Privilege | Auto-sync escribe en ~/.claude/skills/ sin confirmación | accept | Es el comportamiento esperado: `kodo orchestrate` mantiene home coherente para cross-cwd futuros. Operador puede borrar `~/.claude/skills/kodo-orchestrate/` si no lo desea; el próximo run lo recrea (idempotente). NUNCA prune en auto-sync (D-05c). |
| T-21.02-07 | Spoofing | Race entre 2 `kodo orchestrate` simultáneos | accept | RESEARCH §Risks: caso teórico (operador humano lanza uno a la vez). Última escritura gana; ambas escriben el mismo contenido canónico → convergente. NO file-lock added (Karpathy Regla 2 simplicity). |
| T-21.02-08 | Repudiation | Sync fallido sin auditoría visible | mitigate | Emit `skill.sync.auto.error` via NDJSON (logger-events); operador puede `kodo logs --event-type skill.sync.auto.error` para audit. Test C blinda el path. |

**threats_open: 0** — todos los threats tienen disposition (2 mitigate, 6 accept con rationale explícito reusando precedentes Phase 19 + 999.1).
</threat_model>

<verification>
- `node --test test/orchestrator-auto-sync.test.js` → 5/5 pass.
- `node --test test/skill-sync.test.js test/orchestrator-auto-sync.test.js test/logger-events.test.js` → suite Phase 21 completa verde.
- `npm test 2>&1 | tail -5` → suite global verde (baseline 567 + Plan 01 ~22 + Plan 02 ~5 = ~594 tests).
- `grep -rln "from.*skill/sync" /Users/alex/dev/klab/kodo/src/` → exactamente 2 archivos: `src/cli/skill-sync.js` + `src/orchestrator/launch.js`.
- `grep -c "Phase 18 D-06" src/orchestrator/launch.js` → 1 (comentario preservado).
- `grep -cE "(\.cwd\\(\\)|chdir)" src/orchestrator/launch.js` → patrón solo en `KODO_ROOT_FOR_SKILL` fallback + `cwd: process.cwd()` de cmux.newWorkspace; NUNCA `process.chdir`.
- Manual integration: `cd ~/dev/klab/kodo && KODO_ROOT=$PWD node bin/kodo orchestrate` (si tiene cmux + config) → log NDJSON contiene `skill.sync.auto` con `files_changed: N` la primera vez, silencio en segundas invocaciones (noop).
</verification>

<success_criteria>
- SKILL-02 satisfied: `kodo orchestrate` ejecuta syncSkill ANTES de cmux.listWorkspaces (L45); drift → emit `skill.sync.auto` (Test A); noop → SIN event (Test B); error → `skill.sync.auto.error` + fail-open (Test C).
- SKILL-03 satisfied: el bloque NO modifica `process.cwd()` ni los args de `cmux.newWorkspace({ cwd: process.cwd() })`; orchestrator NO lee `~/.claude/skills/.../skill.md` (Test E regex negativos).
- D-03 fail-open: outer try/catch defense in depth con `console.error` (acceptance criteria grep).
- D-03b: silencio total en noop (Test B asertando 0 records de `skill.sync.*`).
- D-05c: auto-sync NUNCA pasa `prune: true` (acceptance criteria grep negativo).
- D-08b: source-hygiene cross-callsite (Test D: exactly 2 importers).
- D-10: SKILL-03 invariante (Test E).
- Phase 18 D-06 comentario `--worktree EXCLUIDO` preservado byte-a-byte (grep acceptance criteria).
- Suite global verde sin regresión cross-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-sync-cli-auto-sync/21-02-SUMMARY.md` documentando:
- Files modified: src/orchestrator/launch.js (+~35 LOC: 3 imports + 1 constante + ~28 LOC bloque auto-sync con outer try/catch)
- Files created: test/orchestrator-auto-sync.test.js (5 tests A..E in-process DI + memSink)
- Decisions implemented: D-03 (fail-open con event), D-03b (silencio en noop), D-05c (auto-sync sin prune), D-08b (cross-callsite source-hygiene), D-10 (cwd preservation)
- Phase 18 D-06 comentario `--worktree EXCLUIDO` preservado (verificado por grep en acceptance criteria)
- Suite global delta (Plan 01 ~589 → Plan 02 ~594 tests pass; baseline 567)
- SKILL-01..04 = COMPLETE. Phase 21 cierra requirements al 100%.
- Bloque inserted entre L40 y L42 (offset original; tras Plan 02 ~L70 en archivo modificado).
- Drift residual symlink legacy en `~/.claude/skills/kodo-orchestrate` (driver del usuario) será resuelto en la PRIMERA ejecución real de `kodo orchestrate` o `kodo skill sync` tras merge (D-04 idempotente).
</output>
