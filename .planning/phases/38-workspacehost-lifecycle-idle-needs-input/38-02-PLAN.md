---
phase: 38
plan: 38-02
wave: 2
depends_on: [38-01]
autonomous: true
objective: |
  Introducir los 4 estados nuevos del ciclo de vida (`idle`, `needs-input`,
  `dead`, `closed`) en `markSessionStatus` con compat shim `'done' → 'idle'`,
  migrar el schema v2→v3 idempotente con backup automático, y rewire del
  único caller real `src/hooks/stop.js:202` (D-12 CORREGIDO post-R-6 — verify.js
  emite `'review'`, NO se toca). SIN tocar dashboard render todavía (Plan 03)
  ni reconciliación con host (Plan 04). Load-bearing: cumple TUI-18 / SC#2.
files_modified:
  - test/state/migration.test.js
  - src/session/manager.js
  - src/session/state.js
  - src/hooks/stop.js
  - src/logger-events.js
  - .planning/phases/38-workspacehost-lifecycle-idle-needs-input/38-02-SUMMARY.md
task_count: 4
---

## Objective

Cerrar TUI-18 / SC#2 de CONTEXT.md: `markSessionStatus` acepta los nuevos
estados con compat shim, el schema v2→v3 migra preservando sessions con
backup timestamped, y el caller real de `'done'` (`stop.js:202`) emite
`'idle'` con reason `'session-stop:lock-released'`. Cero impacto al render —
los nuevos campos del SessionRecord v3 son aditivos opcionales (D-11) y los
consumers actuales (`findSession` dual-scan, dashboard select.js) siguen
funcionando sin tocar.

**Referencias load-bearing:**
- CONTEXT.md D-04 (5 estados + reglas de transición), D-05 (migración
  idempotente + backup timestamped + lazy en `loadState`), D-11 (dimensiones
  independientes del SessionRecord v3), D-12 (caller migration CORREGIDO —
  solo `stop.js:202`; `verify.js:274` emite `'review'`, NO se toca),
  D-13 (logger event `state.migration.v2_to_v3`).
- RESEARCH.md §S1 (pattern migration existente — NO copiar el destructive
  clear de v1→v2), §S2 (inventory completo de callers, 5 archivos test a
  actualizar), §S3 (fixture pattern), §P-2/R-6 (verify.js empíricamente emite
  `'review'`), §P-1/R-1 (NO destructive migration), §P-9 (5 test fixture
  archivos legacy).
- PATTERNS.md: análogos exactos `test/migration.test.js:16-86` (fixture style),
  `src/session/state.js:46-94` (estructura migration, NO copiar el clear),
  `src/session/manager.js:366-397` (markSessionStatus non-throwing),
  `src/logger-events.js` (helper pattern para nuevos NDJSON).

## Tasks

### Task 1: Wave 0 RED — migration.test.js + manager.test extension (fallan limpio)

**Files:** `test/state/migration.test.js`

**Action:**

1. **Crear `test/state/migration.test.js`** mirror del estilo de
   `test/migration.test.js:16-86` (PATTERNS.md). Estructura: `describe('state
   migration v2 → v3', ...)` con fixtures inline como objeto literal
   (no archivos JSON externos).

2. **Fixtures requeridas (D-05 + RESEARCH.md §S3):**

   - **F1 `STATE_V2_EMPTY`** — `{schema_version: 2, sessions: {}, history: []}`.
     Migración a v3 sin cambios efectivos (no hay sessions a derivar).

   - **F2 `STATE_V2_WITH_RUNNING`** — 1 session con `status: 'running'` y 1
     entry en history reciente (`ended_at < 30 días`) con `workspace_ref` que
     APARECE en `hostSnapshot`. Asserts:
     - `migrated.schema_version === 3`.
     - `migrated.sessions['<id>'].state === 'running'` (preservado).
     - `migrated.sessions['<id>'].process_alive === true` (derivado de status).
     - `migrated.sessions['<id>'].tab_alive === false` (default — rescate vive
       en reconciliación, no en migración pura per RESEARCH.md §S1 punto 2).
     - `migrated.sessions['<id>'].needs_input === false` (default).
     - `migrated.sessions['<id>'].last_seen_alive === null` (default).
     - `migrated.history` igual al input (rescate NO ocurre en migrate puro).

   - **F3 `STATE_V2_DONE_LEGACY`** — 1 session con `status: 'done'` (caso
     degenerate — sesión legacy quedó con done en sessions). Asserts:
     - `migrated.sessions['<id>'].state === 'idle'` (mapping legacy D-04
       último párrafo).
     - `migrated.sessions['<id>'].process_alive === false`.

   - **F4 `STATE_V2_REVIEW_PRESERVED`** — 1 session con `status: 'review'`.
     Asserts: `migrated.sessions['<id>'].state === 'review'` preservado (D-12:
     `review` es ortogonal a la división Phase 38, queda como pre-existente).

   - **F5 `IDEMPOTENCE`** — Llamar `migrateStateV2toV3(migrateStateV2toV3(F2))`.
     Assertar que `result.schema_version === 3` (estable) y deep-equal al
     primer migrate (no cambia nada). Mismo idiom que
     `test/migration.test.js:68-85`.

   - **F6 `ALREADY_V3_NO_OP`** — Input con `schema_version: 3` → retorna mismo
     objeto referencialmente (`assert.equal(result, input)`). Idempotencia
     trivial.

3. **Backup test** — Test separado con `tmpdir` fixture (idiom de
   `test/state/find-session.test.js:101+` per RESEARCH.md §S3):
   - Crear state.json v2 en `tmpdir/state.json`.
   - Invocar `migrateStateIfNeeded()` (la versión I/O).
   - Assertar existencia de `tmpdir/state.json.bak.YYYYMMDDTHHMMSS` (regex
     match — formato sortable per D-05).
   - Assertar que `tmpdir/state.json` tiene `schema_version: 3`.
   - Re-invocar migrate → assertar que NO se crea un segundo backup
     (idempotencia I/O — second call detecta v3 y retorna).

4. **markSessionStatus compat shim test** — agregar a `test/session/mark-status.test.js`
   (extender el archivo existente per RESEARCH.md §S2):
   - Llamar `markSessionStatus(id, 'done', reason, logger, sid)`.
   - Assertar que `result.ok === true` y `result.to === 'idle'` (shim aplicado).
   - Assertar que `logger.warn` fue invocado con clave que contiene
     `'DEPRECATED'` y `status: 'done'` + `mapped_to: 'idle'` + `session_id`.
   - Test SC#3 negative: llamar con `'idle'` directo → NO warn (shim solo
     dispara con `'done'`).

5. **stop.js caller test** — extender `test/stop.test.js` (uno de los 5
   archivos a actualizar per RESEARCH.md §P-9) para assertar:
   - Cuando `stop.js` recibe input con `session.gsd`, llama a `markSessionStatus`
     con `'idle'` (NO `'done'`) y reason `'session-stop:lock-released'`.

   **Initially FAILS rojo:**
   - `migrateStateV2toV3` no existe en `state.js`.
   - `markSessionStatus` rechaza `'idle'` (no en allowlist v2 actual).
   - El shim no existe.
   - `stop.js:202` aún emite `'done'`.

**Verify:**
- `node --test test/state/migration.test.js` rojo con ERR_MODULE_NOT_FOUND
  (esperado).
- `node --test test/session/mark-status.test.js` rojo en los nuevos asserts.

**Done:** 6 fixtures + backup test + shim test + stop caller test
committed RED.

### Task 2: Extender `markSessionStatus` con nuevos estados + compat shim

**Files:** `src/session/manager.js`

**Action:**

1. **Extender JSDoc `@param nextStatus`** (línea 366 per PATTERNS.md):
   de `'running'|'done'|'error'|'review'|'interrupted'` a
   `'running'|'idle'|'needs-input'|'dead'|'closed'|'done'|'error'|'review'|'interrupted'`.

2. **Insertar compat shim** ANTES del guard `!taskId` (línea 371 per
   RESEARCH.md §S2 + PATTERNS.md):
   ```
   if (nextStatus === 'done') {
     if (logger) logger.warn('markSessionStatus.deprecated', {
       input_status: 'done', mapped_to: 'idle',
       task_id: taskId, session_id: sessionId, reason,
     });
     nextStatus = 'idle';
   }
   ```
   Comentario inline: `// Phase 38 D-12: compat shim 'done' → 'idle'. Eliminado en v0.10.`

3. **Ampliar allowlist de estados** en el body de la función — donde se
   valida `nextStatus` contra el set permitido, añadir `'idle'`, `'needs-input'`,
   `'dead'`, `'closed'`. Preservar `'review'`, `'error'`, `'interrupted'`,
   `'running'` (legacy). NO añadir `'done'` al allowlist persistente — el shim
   lo mapea ANTES de la validación.

4. **Preservar contract non-throwing** — el discriminated union
   `{ok: true, from, to}` / `{ok: false, reason}` retornado se conserva
   intacto. `to` ahora puede ser `'idle'` cuando el caller pasó `'done'`
   (post-shim).

5. **Preservar `findSession` dual-scan** — el cuerpo de `markSessionStatus`
   no cambia su lookup (sigue invocando `findSession` que escanea sessions +
   history per Phase 30). Cero cambios estructurales.

**Verify:**
- `node --test test/session/mark-status.test.js` — verde (incluye el shim test).
- `node --test test/session/` completo — sin regresiones.
- `grep -n "Eliminado en v0.10" src/session/manager.js` → match (TODO marker).

**Done:** Shim instalado, allowlist ampliada, contract non-throwing intacto.

### Task 3: Schema v2→v3 migration + backup automático + lazy en loadState

**Files:** `src/session/state.js`

**Action:**

1. **Función pura `migrateStateV2toV3(rawState, hostSnapshot = [])`** — agregar
   junto a `migrateState` (cerca de líneas 46-52 per PATTERNS.md). NO copiar
   el destructive `sessions: {}` del v1→v2 (RESEARCH.md §S1 P-1 — anti-pattern).

   Algoritmo:
   - Idempotencia: `if (rawState.schema_version === 3) return rawState`.
   - Preservar `rawState.sessions`: para cada session, calcular los 5 campos
     nuevos derivados (D-11):
     - `state` derivado de `status` actual: `'running' → 'running'`,
       `'done' → 'idle'` (compat shim mapping de D-04 último párrafo),
       `'error' → 'dead'`, `'review' → 'review'` (preserved), `'interrupted' → 'dead'`.
     - `process_alive = (status === 'running')`.
     - `tab_alive = false` por default (rescate vive en reconciliación Plan 04,
       NO en migrate puro per RESEARCH.md §S1 punto 2).
     - `needs_input = false` por default.
     - `last_seen_alive = null` por default.
     - Mantener `alive` derived field (`alive = state === 'running' || state === 'idle' || state === 'needs-input'`)
       para compat con consumers que ya lo leen.
   - Preservar `rawState.history` SIN modificar (rescate desde history a
     sessions vive en Plan 04 reconciliación, NO en migrate puro).
   - Return `{schema_version: 3, sessions: newSessions, history: rawState.history}`.

2. **Extender `@typedef Session`** (líneas 11-36 per PATTERNS.md) con los
   campos aditivos opcionales (`?`):
   ```
   *   state?: 'running'|'idle'|'needs-input'|'dead'|'closed'|'review'|'error',
   *   needs_input?: boolean,
   *   process_alive?: boolean,
   *   tab_alive?: boolean,
   *   last_seen_alive?: string|null,
   ```
   Comentario inline `// Phase 38 D-04/D-11` en cada uno.

3. **`migrateStateIfNeeded` extension** — extender la función existente
   (líneas 81-94) para cubrir v1→v2 (legacy) + v2→v3 (nuevo). Después del
   guard `if (raw.schema_version === 3) return`:
   - Backup timestamped: `const ts = new Date().toISOString().replace(/[:.]/g, '').slice(0, 15);`
     (formato `YYYYMMDDTHHMMSS` sortable per D-05 y PATTERNS.md sección
     `src/session/state.js`).
   - `writeFileSync(STATE_PATH + ".bak." + ts, JSON.stringify(raw, null, 2) + '\n')`.
   - Si `raw.schema_version === undefined` (v1): primero migrar a v2 con la
     función existente, luego encadenar a v3.
   - Si `raw.schema_version === 2`: migrar directo a v3 con
     `migrateStateV2toV3(raw, [])` (hostSnapshot vacío — rescate diferido a
     Plan 04).
   - `writeFileSync(STATE_PATH, JSON.stringify(newState, null, 2) + '\n')`.
   - Emit log via logger inyectado (NO importar logger desde state.js —
     LOG-12 walker): si el caller pasó logger, invoca `stateMigrationV3(logger, {...})`
     helper (lo crearemos en Task 4). Si no, console.log fallback como el v1→v2
     existente.

4. **NO añadir flock** (R-8 / P-3 — punt al backlog per RESEARCH.md
   recommendation #10). Idempotencia previene corruption; backup ruido es
   cosmético.

**Verify:**
- `node --test test/state/migration.test.js` — todos los fixtures + idempotencia
  + backup test verdes.
- `node --test test/migration.test.js` — pre-existing v1→v2 sigue verde sin
  regresión.
- `node --test test/state/` completo — verde.

**Done:** Schema v3 migration funcional, idempotente, con backup timestamped,
sin destructive clear, sin flock.

### Task 4: Caller migration `stop.js:202` + logger event `state.migration.v2_to_v3`

**Files:** `src/hooks/stop.js`, `src/logger-events.js`

**Action:**

1. **`src/hooks/stop.js:202`** (PATTERNS.md sección stop.js — líneas exactas):
   cambiar:
   ```
   markSessionStatus(session.task_id, 'done', 'session-stop', log, session.session_id)
   ```
   a:
   ```
   markSessionStatus(session.task_id, 'idle', 'session-stop:lock-released', log, session.session_id)
   ```
   Cambios:
   - String `'done'` → `'idle'` (D-12 caller migration plan + R-6 CORREGIDO).
   - Reason `'session-stop'` → `'session-stop:lock-released'` (D-12 — el stop
     hook NO significa "sesión muerta" sino "lock released, esperando humano").
   - Patrón de consumo del return (líneas 203-208) NO cambia — sigue el
     `if (!result?.ok) log.warn('markSessionStatus.skipped', ...)` heredado
     de Phase 30.

2. **NO TOCAR `src/gsd/verify.js:274`** — empíricamente emite `'review'`, NO
   `'done'` (R-6 / P-2 corregido en CONTEXT.md D-12). El status `'review'`
   queda como estado válido pre-existente, ortogonal a Phase 38.

3. **`src/logger-events.js`** — añadir 1 evento nuevo (el de migración —
   los otros 3 host events viven en Plan 04 reconciliación per D-13).
   Pattern de extensión per PATTERNS.md sección logger-events.js:
   - Añadir al objeto `EVENTS` (líneas 47-67):
     `STATE_MIGRATION_V3: 'state.migration.v2_to_v3',`
   - Helper `stateMigrationV3(logger, fields)` con whitelist explícita
     (NO spread — pattern de `pollingTick` líneas 422-431):
     campos `{from_count, to_sessions, to_history, rescued, sealed}` (D-13).
     Nivel `info`. `rescued` y `sealed` serán 0 en Plan 02 (rescate vive en
     Plan 04) — quedan como `0` hasta entonces, lo cual es semánticamente
     correcto.
   - Actualizar header (líneas 1-16) sumando 1 evento (taxonomy 19 → 20 en
     Plan 02; los otros 3 host events suman a 23 en Plan 04).
   - **NO añadir imports** — LOG-12 walker invariante (RESEARCH.md
     §Patterns to Follow + PATTERNS.md sección).

4. **5 test fixture files** que llaman `markSessionStatus('done', ...)` (per
   RESEARCH.md §P-9): `test/stop.test.js`, `test/stop-state-transition.test.js`,
   `test/gsd-verify-integration.test.js`, `test/session/mark-status.test.js`,
   `test/stop-worktree-cleanup.test.js`. Buscar cada `'done'` invocation y:
   - Si el test verifica la transición observable: actualizar el expected a
     `'idle'` con reason `'session-stop:lock-released'` (refleja el cambio
     real).
   - Si el test verifica el shim path explícitamente (deprecated input): dejar
     `'done'` y assertar que el shim emite warn + mapea a `'idle'`.
   - Cualquier fixture de log existente con `'done'` queda como
     **observation only** (no falla — el shim lo acepta), pero documentar en
     el SUMMARY del plan que esos fixtures se eliminarán en v0.10.

**Verify:**
- `node --test test/stop.test.js test/stop-state-transition.test.js test/gsd-verify-integration.test.js test/session/mark-status.test.js test/stop-worktree-cleanup.test.js`
  — todos verdes.
- `grep -nE "markSessionStatus\\(.*'done'" src/` → 0 matches en src/ (sólo
  el shim acepta el input, ningún caller real lo emite).
- `grep -n "STATE_MIGRATION_V3" src/logger-events.js` → match.
- Suite global — sin regresiones.

**Done:** Caller migrado, shim cubriendo callers externos legacy, logger event
emitido, 5 test fixtures actualizadas.

## Constraints (load-bearing)

- **NO copiar el destructive clear** de la migración v1→v2 (`sessions: {}`).
  La v2→v3 PRESERVA sessions y añade campos aditivos (R-1 + RESEARCH.md §S1
  P-1).
- **NO tocar `verify.js:274`** — empíricamente emite `'review'`, no `'done'`
  (R-6 corregido). El shim cubre cualquier caller legacy externo.
- **NO importar logger** desde `src/session/state.js` ni `src/host/*`
  (LOG-12 walker invariante).
- **`markSessionStatus` non-throwing contract** (v0.8 invariante): firma intacta,
  discriminated union return `{ok, reason}` preservado.
- **`findSession` dual-scan** (Phase 30 invariante): sigue escaneando sessions
  + history. Los nuevos estados `idle`/`needs-input`/`dead` viven en sessions;
  solo `closed` vive en history (D-04).
- **Idempotencia obligatoria** — F5 test cubre el caso. Sin idempotencia hay
  riesgo R-1 de corrupción.
- **Backup ANTES de migrar** — D-05 + R-1. Si falla la migración, el operador
  puede restaurar manualmente.
- **NO añadir flock** — R-8 / P-3 documentado como known minor, punt al
  backlog.
- **NO tocar dashboard render** — Plan 03 hace eso. Los nuevos campos del
  SessionRecord v3 son aditivos opcionales (D-11); los consumers actuales
  siguen funcionando sin tocar.
- **NO añadir reconciliación / rescate desde history** — Plan 04 hace eso.
  La migración pura solo bumpea schema + deriva states inline; el rescate
  cross-host requiere el host de Plan 01 + el reconciliador de Plan 04.

## Verification

| Check | Comando | Esperado |
|---|---|---|
| SC#2 migration | `node --test test/state/migration.test.js` | verde, 6 fixtures + backup |
| Idempotencia | F5 + F6 dentro de migration.test.js | verde |
| markSessionStatus shim | `node --test test/session/mark-status.test.js` | verde con shim warn assert |
| stop.js caller | `node --test test/stop.test.js` | verde con `'idle'` esperado |
| Legacy v1→v2 intacto | `node --test test/migration.test.js` | verde sin regresión |
| Sin `'done'` callers reales | `grep -rnE "markSessionStatus\\(.*['\"]done['\"]" src/` | 0 matches |
| Logger event nuevo | `grep -n "STATE_MIGRATION_V3" src/logger-events.js` | 1 match |
| LOG-12 walker | `node --test test/logger-isolation.test.js` (o equivalente) | verde sin regresión |
| Suite global | `npm test` | 0 regresiones, +N tests netos |

## Risks and Deviations

- **R-1 (Tier 3):** migración destructive si schema bump no idempotente.
  Mitigación: F5 idempotence test + F6 already-v3 no-op + backup ANTES de
  migrar.
- **R-3 (Tier 2):** `'done' → 'idle'` rompe test fixtures legacy. Mitigación:
  compat shim con warn DEPRECATED + 5 test files actualizados explícitamente
  en Task 4.
- **R-6 (Tier 1, RESUELTO):** D-12 cita verify.js como caller de `'done'`,
  empíricamente emite `'review'`. Mitigación: D-12 CORREGIDO en CONTEXT.md
  + plan NO toca verify.js + grep negativo en verify post-implementación.
- **R-8 (Tier 3, NO mitigado por decisión):** race condition migración en
  arranques paralelos. Mitigación: idempotencia previene corruption; backup
  ruido cosmético. Documentar como known minor en SUMMARY.
- **Deviation NO permitida:** tocar verify.js, tocar dashboard render, añadir
  reconciliación, copiar el destructive clear de v1→v2, añadir flock.
- **Deviation permitida:** si un fixture legacy con `'done'` no se puede
  actualizar sin cambiar la semántica del test, documentar como observation
  de Plan 04 cleanup pre-v0.10.
