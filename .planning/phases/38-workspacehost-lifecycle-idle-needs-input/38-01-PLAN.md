---
phase: 38
plan: 38-01
wave: 1
depends_on: []
autonomous: true
objective: |
  Foundation del WorkspaceHost provider — contrato + impl `CmuxHost` + walker
  cmux-isolation. Establece la base intercambiable cmux/orca/… sin tocar todavía
  estados de sesión ni render del dashboard. Load-bearing: cumple SC#1 (TUI-17)
  y SC#5 (cmux-isolation) de CONTEXT.md D-01/D-02/D-03/D-08/D-09. Wave 1 sin
  side-effects observables al usuario más allá del refactor estructural.
files_modified:
  - test/host/contract.test.js
  - test/host/cmux-isolation.test.js
  - test/fixtures/cmux/list-workspaces.json
  - test/fixtures/cmux/notification-list.json
  - src/host/interface.js
  - src/host/cmux.js
  - src/session/manager.js
  - src/session/health.js
  - src/cli/dashboard/index.js
  - .planning/phases/38-workspacehost-lifecycle-idle-needs-input/38-01-SUMMARY.md
task_count: 4
---

## Objective

Crear el contrato `WorkspaceHost` (4 métodos, D-03), implementar `CmuxHost`
delegando a `runFocus` Phase 37 (D-08, R-Q1/Q2/Q3 del research) y blindar el
aislamiento de cmux con un walker estructural análogo a
`test/format-isolation.test.js` (D-09). Cero cambios en `state.json` schema,
`markSessionStatus`, ni dashboard render — todo eso es Plans 02/03/04.

**Referencias load-bearing:**
- CONTEXT.md D-01 (nombre `WorkspaceHost`), D-02 (`src/host/`), D-03 (4 métodos
  + shape `WorkspaceInfo`), D-08 (CmuxHost reusa `runFocus` + `cmux/client.js`),
  D-09 (walker SC#5).
- RESEARCH.md Q1 (`cmux list-workspaces --json` existe, evidencia empírica
  capturada en fixture), Q2 (`needs_input` derivado de `notification.list`
  con `subtitle === 'Waiting'`), Q3 (`last_activity` ← `latest_submitted_at`).
- RESEARCH.md §S4 + §S7 + R-5: **orden secuencial obligatorio dentro del
  plan** — refactor de callers ANTES del walker, sino el walker rompe rojo y
  bloquea.
- PATTERNS.md: análogos exactos `src/interface.js` (HOST_METHODS),
  `src/providers/plane/provider.js` (factory createCmuxHost),
  `test/providers/contract.test.js` (matrix loop), `test/format-isolation.test.js`
  (walker estructural).

## Tasks

### Task 1: Wave 0 RED — contract test + walker stub (falla limpio)

**Files:** `test/host/contract.test.js`, `test/host/cmux-isolation.test.js`,
`test/fixtures/cmux/list-workspaces.json`, `test/fixtures/cmux/notification-list.json`

**Action:**

1. **`test/fixtures/cmux/list-workspaces.json`** — pegar verbatim el output
   capturado en RESEARCH.md §Q1 (window_ref + workspaces[] con `ref`, `title`,
   `selected`, `latest_submitted_at`, etc.). Este fixture es golden-byte: si
   cmux cambia el shape en una versión futura, los tests fallan loud (R-7
   pitfall).

2. **`test/fixtures/cmux/notification-list.json`** — pegar verbatim el output
   capturado en RESEARCH.md §Q2 (notifications[] con `workspace_ref`, `is_read`,
   `subtitle: "Waiting"`). Documenta el assumption R-7 (literal match fragile).

3. **`test/host/contract.test.js`** — mirror de `test/providers/contract.test.js`
   líneas 369-466 (loop `for (const implName of IMPLS)`). IMPLS = `['cmux', 'null']`.
   Asserts core por implementación (todos los `it()` viven DENTRO del describe del
   loop — pitfall #3 de Phase 27):
   - `HOST_METHODS.length === 4` y los 4 son strings literales
     `['listWorkspaces', 'selectWorkspace', 'isAlive', 'needsInput']` en orden.
   - `getHost('cmux')` retorna objeto donde `HOST_METHODS.every(m => typeof host[m] === 'function')`.
   - `listWorkspaces()` retorna array donde cada item satisface
     `assertWorkspaceInfoShape` (espejo de `assertTaskItemShape` líneas 139-187):
     campos requeridos `workspace_ref` (string), `alive` (boolean), `needs_input`
     (boolean), `last_activity` (string|null).
   - `selectWorkspace(ref)` retorna discriminated union `{ok}` (never-throws —
     stub exec retorna `{stdout: '', stderr: '', code: 0}`).
   - `isAlive(ref)` retorna boolean; `needsInput(ref)` retorna boolean.
   - **Leak guard exec** (mirror líneas 54-63): `before()` sobreescribe el exec
     inyectable con stub loud-on-call si la impl olvidó usar el inyectable.
   - **`instantiateHost(name)` helper** — espejo de `instantiateProvider`
     (líneas 287-343): para `cmux` inyecta `exec` fake que carga los fixtures
     JSON; para `null` instancia directa sin DI.

4. **`test/host/cmux-isolation.test.js`** — copia literal del walker de
   `test/format-isolation.test.js` líneas 199-220 (bloque TUI-04 D-13).
   `SCANNED_DIRS = [join(SRC, 'cli', 'dashboard'), join(SRC, 'session'),
   join(SRC, 'cli', 'polling.js')]` (las 3 carpetas de D-09 — `polling.js` es
   archivo, el walker debe aceptar tanto dir como file).
   Detectar tres formas: regex `/\/cmux[./]/` sobre specifiers retornados por
   `extractImports`. Excepciones documentadas en el assert message:
   `src/host/cmux.js` y `src/cli/dashboard/focus.js` (Phase 37 — existe pero
   NO importa `cmux/`).

   **Initially FAILS rojo:**
   - `contract.test.js`: `src/host/interface.js` no existe → ERR_MODULE_NOT_FOUND.
   - `cmux-isolation.test.js`: `src/session/manager.js:6,7` importa `'../cmux/client.js'`
     y `src/session/health.js:4-5` también → walker encuentra leaks → assert falla.

**Verify:**
- `node --test test/host/contract.test.js` muestra ERR_MODULE_NOT_FOUND (esperado RED).
- `node --test test/host/cmux-isolation.test.js` muestra leaks en manager.js + health.js (esperado RED).

**Done:** 2 tests RED + 2 fixtures committed. Documentar en el assert message
del walker que la Task 3 desbloquea el verde.

### Task 2: Crear contrato `src/host/interface.js` (GREEN parcial — contract.test pasa shape)

**Files:** `src/host/interface.js`

**Action:**

Copiar exactamente el patrón de `src/interface.js:1-62` (PATTERNS.md sección
correspondiente). Cero imports (módulo puro). Exportar:

1. **`HOST_METHODS`** — `Object.freeze(['listWorkspaces', 'selectWorkspace', 'isAlive', 'needsInput'])`.
   Mismo patrón que `TASK_PROVIDER_METHODS` (líneas 51-62 de `src/interface.js`).

2. **`@typedef WorkspaceInfo`** JSDoc — modelar como `@typedef TaskItem` de
   `src/interface.js:11-26`. Campos: `workspace_ref` (string), `alive` (boolean),
   `needs_input` (boolean), `last_activity` (string|null). Comentar el origen
   D-03 inline.

3. **`getHost(name)` factory** — función pequeña inline. Acepta `'cmux'` y
   `'null'` (NullHost mock only, per D-10). Para `'cmux'`: lazy `await import('./cmux.js')`
   + `createCmuxHost(loadConfig())`. Para `'null'`: retorna stub con los 4
   métodos retornando `[]/{ok:true}/false/false` para uso solo en tests
   (per CONTEXT.md D-10 — NO se publica como export de runtime; vive en
   `interface.js` solo para alimentar el contract test). Si `name` no es
   reconocido, throw `Error("Unknown host: ${name}")`.

4. **Test de validación shape de impl** — exportar `validateHost(host)` que
   itera `HOST_METHODS` y assert `typeof host[m] === 'function'`. Mirror de
   cualquier validador del registry de providers.

**Verify:**
- `node --test test/host/contract.test.js` — los asserts de shape (HOST_METHODS
  + getHost retorna objeto con los 4 métodos) pasan. Asserts de runtime de
  CmuxHost siguen rojos hasta Task 3.

**Done:** Contrato exportado, sintaxis válida, `HOST_METHODS` matchea D-03.

### Task 3: Implementar `src/host/cmux.js` + refactorizar callers + walker GREEN

**Files:** `src/host/cmux.js`, `src/session/manager.js`, `src/session/health.js`,
`src/cli/dashboard/index.js`

**Action (orden estricto W-1 load-bearing — R-5 del research):**

1. **`src/host/cmux.js`** — `createCmuxHost(config, opts = {})` factory siguiendo
   el patrón de `src/providers/plane/provider.js:23-31` (PATTERNS.md). Imports:
   `execFile` de `node:child_process`, `runFocus` de `../cli/dashboard/focus.js`,
   `HOST_METHODS` de `./interface.js`. Resolver binary desde
   `config.cmux.binary` (mismo patrón que `index.js:122` per PATTERNS.md y
   `src/cmux/client.js:5-7` per RESEARCH.md S7).

   Métodos:
   - `listWorkspaces()` — `Promise.all([run(['list-workspaces', '--json']), run(['rpc', 'notification.list'])])`
     en paralelo (RESEARCH.md §S5 — budget ~50ms paralelo vs ~100ms secuencial).
     Parse JSON. Por cada workspace del primer JSON: normaliza a `WorkspaceInfo`
     mapeando `workspace_ref ← ref`, `alive ← true` (presencia = vivo),
     `needs_input ← notifications.some(n => n.workspace_ref === ref && !n.is_read && n.subtitle === 'Waiting')`
     (RESEARCH.md §Q2), `last_activity ← latest_submitted_at` (RESEARCH.md §Q3).
     Cachear el snapshot en `lastSnapshot` (Map per ref) para que `isAlive`/`needsInput`
     consuman sin nueva I/O (D-08 — caché 1-tick). Timeout 5s mismo que
     `runFocus` (D-08). Never-throws: si falla → emite warn vía `opts.logger?.warn`
     y retorna array vacío (RESEARCH.md §S5 — no romper reconciliador downstream).
     JSDoc debe documentar el assumption R-7 verbatim ("Mapping derivado de
     notification.list con subtitle === 'Waiting'. Si cmux cambia este literal
     en versiones futuras, este host requiere actualización — el test contract
     con fixture JSON real lo detectará.").

   - `selectWorkspace(ref)` — wrapper de 3 líneas que delega a `runFocus({exec, ref, binary})`
     de Phase 37 (D-08 reuso DIRECTO). El shape `{ok, code?, detail?}` retornado
     es idéntico al de `runFocus` porque lo re-exporta sin transformar
     (RESEARCH.md §S7 — cero changes en App.js handleFocusResult).

   - `isAlive(ref)` — lee `lastSnapshot.get(ref)?.alive ?? false`. Si snapshot
     vacío, retorna `false`. Never-throws.

   - `needsInput(ref)` — lee `lastSnapshot.get(ref)?.needs_input ?? false`.
     Never-throws.

2. **Refactor `src/session/manager.js`** (líneas 6-7, 280 — per RESEARCH.md §S4):
   - Mantener `import { colorForStatus } from '../cmux/colors.js'` (es pure
     helper, no leak).
   - Reemplazar SOLO la línea 280 (`cmux.listWorkspaces`) por una llamada al
     host obtenido via `getHost('cmux')`. **Conservar** `cmux.newWorkspace`,
     `cmux.setColor`, `cmux.send`, `cmux.notify` en `cmux/client.js` — esos
     NO migran al contract D-03 (per CONTEXT.md §Recommendations #6 y
     RESEARCH.md §S4 row manager.js — son Cmux-specific lifecycle management,
     no observation). Si la única línea que migra es `cmux.listWorkspaces`,
     el `import * as cmux from '../cmux/client.js'` permanece para los otros
     métodos, **pero el walker debe pasar verde** — porque el walker SC#5
     prohíbe import de `'../cmux/*'` desde `src/session/`. Esto fuerza un
     refactor más amplio: extraer los métodos no-contract de cmux/client.js
     que manager.js necesita (newWorkspace/setColor/send/notify) a un helper
     intermediario en `src/host/cmux.js` que los re-exporte, O aceptar que
     el walker SC#5 excluye explícitamente `manager.js` con justification
     documentada en el assert message.

     **Decisión load-bearing del plan:** seguir el camino estricto de D-09 —
     el walker SC#5 NO permite `manager.js` ni `health.js` con import de
     `cmux/`. Por tanto `CmuxHost` debe exponer (como dimension extension,
     fuera de `HOST_METHODS`) los métodos legacy de Cmux que manager/health
     necesitan: `_legacy: { newWorkspace, setColor, send, notify }` (prefijo
     `_` marca scope interno, no contract). Esto evita romper el alcance del
     refactor Phase 38 a la vez que cumple SC#5. Documentar en JSDoc de
     `createCmuxHost` que `_legacy` es transición temporal hasta que un futuro
     contract los absorba o queden out-of-scope con NullHost stubs.

3. **Refactor `src/session/health.js`** (líneas 4-5, 33 per RESEARCH.md §S4):
   reemplazar `import * as cmux from '../cmux/client.js'` + `cmux.listWorkspaces()`
   por `import { getHost } from '../host/interface.js'` + `await getHost('cmux').listWorkspaces()`.

4. **Refactor `src/cli/dashboard/index.js`** (PATTERNS.md sección
   `src/cli/dashboard/index.js`): tras línea 122 (donde se resuelve `cmuxBin`),
   construir `const host = createCmuxHost(loadConfig())` via lazy `await import('../../host/cmux.js')`.
   Pasarlo a `<App>` como prop nueva `host` (que App.js usará en Plan 02/03/04
   sin breaking del Phase 37 wiring). **NO mutar** líneas 129/155 (alt-screen
   toggle) ni 143 (SIGTERM handler) ni 149 (waitUntilExit) — invariantes
   Phase 36/34 preservadas literalmente.

5. **Ejecutar el walker** (`node --test test/host/cmux-isolation.test.js`) y
   verificar que pasa verde. Si falla, identificar el leak residual antes de
   cerrar la task.

**Verify:**
- `node --test test/host/contract.test.js` — verde (CmuxHost real satisface
  contrato + shape + never-throws).
- `node --test test/host/cmux-isolation.test.js` — verde (3 carpetas limpias).
- `node --test test/format-isolation.test.js` — sigue verde (color-isolation
  Phase 34 D-12 intacto).
- `grep -rE "from ['\\\"]\\.\\./cmux/" src/session/ src/cli/dashboard/ src/cli/polling.js 2>/dev/null` → 0 matches.

**Done:** CmuxHost implementado, callers refactorizados, walker SC#5 verde,
Phase 37 `runFocus` siendo invocado via `CmuxHost.selectWorkspace` sin cambios
de shape.

### Task 4: Verificación cruzada — Phase 37 parity smoke + contract verde end-to-end

**Files:** (no source changes — solo verifica)

**Action:**

1. Ejecutar `node --test test/dashboard/` completo — asegurar que ningún test
   de Phase 35/36/37 rompió (focus.test.js, app-focus.test.js, status-line,
   table-render). Si rompe, el wiring de `host` prop en `index.js` debe estar
   intacto sin mutar el callback `onFocus` legacy (que `App.js` invoca como
   antes).

2. Suite global: `npm test` (o equivalente runner del proyecto) — esperado
   delta neto: +2 archivos test (contract + isolation), +2 fixtures, 0
   regresiones.

3. Verificar invariantes PROJECT.md preservadas via greps:
   - `grep -rE "import.*picocolors" src/cli/dashboard/ 2>/dev/null` → 0 (D-12 Phase 34).
   - `grep -rE "render\\(.*createSafeBuffer\\(stdout.*altScreen" src/cli/dashboard/index.js` → match en línea ~129
     (alt-screen toggle intacto).
   - `grep -nE "process\\.once\\('SIGTERM'" src/cli/dashboard/index.js` → match (SIGTERM handler intacto).
   - `markSessionStatus` signature en `src/session/manager.js:366-397` — sin
     cambios (Plan 02 lo extiende).

**Verify:**
- Suite global verde.
- 0 regressions de tests preexistentes.
- Invariantes greps OK.

**Done:** SC#1 (TUI-17) + SC#5 (cmux-isolation) cubiertos. Cero impacto
observable al usuario — el dashboard Phase 37 sigue funcionando idéntico
porque `CmuxHost.selectWorkspace` delega a `runFocus` sin transformar el
shape (RESEARCH.md §S7).

## Constraints (load-bearing)

- **NO tocar** `state.json` schema, `markSessionStatus` signature, ni dashboard
  render. Plan 02/03 hacen eso. Phase 38 se ship en 4 increments seguros.
- **NO tocar** alt-screen toggle (`src/cli/dashboard/index.js` líneas 129/155 —
  invariante Phase 34/36 documentada en PROJECT.md "Plug del dashboard").
- **NO tocar** SIGTERM handler línea 143, `waitUntilExit` línea 149, `runFocus`
  (Phase 37 `src/cli/dashboard/focus.js` queda byte-identical — `selectWorkspace`
  lo delega).
- **NO importar** `picocolors` en ningún archivo nuevo bajo `src/host/` ni
  `src/cli/dashboard/` (color-isolation D-12 Phase 34, walker `test/format-isolation.test.js`).
- **NO importar** `src/logger.js` desde `src/host/*` (LOG-12 walker —
  `kodo check` no debe cargar logger transitivamente). `opts.logger` se inyecta
  por el caller, NO se importa.
- **`markSessionStatus` non-throwing contract** (v0.8 invariante) intacto —
  no se toca aquí.
- **`findSession` dual-scan** (Phase 30 invariante) intacto — sigue
  escaneando `state.sessions` + `state.history`.
- **Orden W-1 estricto** dentro de Task 3 (R-5 research): refactor callers
  ANTES de añadir walker, sino el walker rompe rojo. Si el plan se ejecuta
  fuera de orden, Task 3 falla.

## Verification

| Check | Comando | Esperado |
|---|---|---|
| SC#1 contract | `node --test test/host/contract.test.js` | verde, 4 métodos × 2 impls × shape asserts |
| SC#5 walker | `node --test test/host/cmux-isolation.test.js` | verde, 0 leaks en 3 carpetas |
| D-12 Phase 34 | `node --test test/format-isolation.test.js` | verde sin regresión |
| Phase 37 parity | `node --test test/dashboard/focus.test.js test/dashboard/app-focus.test.js` | verde sin cambios |
| Suite global | `npm test` o runner del proyecto | +2 archivos test, 0 regresiones |
| Fixture golden | `cat test/fixtures/cmux/list-workspaces.json | jq .workspaces[0].ref` | `"workspace:1"` (byte-stable) |

## Risks and Deviations

- **R-5 (Tier 2):** orden de Task 3 load-bearing. Si el executor añade el
  walker antes del refactor de callers, falla rojo. Mitigación: Task 3 está
  ordenada explícitamente y el verify command incluye el grep negativo.
- **R-7 (Tier 2):** `needs_input` literal match `subtitle === 'Waiting'` es
  fragile. Mitigación: fixture golden + JSDoc verbatim del assumption.
  Si cmux cambia el literal, contract test rompe loud.
- **R-9 (Tier 2):** multi-window cmux retorna solo workspaces del window
  activo. Documentar como known limitation en JSDoc de `listWorkspaces` (per
  RESEARCH.md §P-4). Out-of-scope hard de Phase 38.
- **Deviation permitida:** `CmuxHost._legacy` para preservar
  `newWorkspace/setColor/send/notify` que manager/health invocan. Documentado
  con prefijo `_` como transición temporal. Si el executor encuentra otro
  camino más limpio (e.g., manager.js delega a `host.send` via contract
  extendido), debe pararse y consultar — fuera del scope de Phase 38.
- **NO desviación permitida:** romper el orden W-1, mutar alt-screen, importar
  picocolors, importar logger en src/host/.
