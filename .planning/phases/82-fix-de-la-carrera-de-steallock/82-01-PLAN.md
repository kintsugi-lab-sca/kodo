---
phase: 82-fix-de-la-carrera-de-steallock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gsd/lock.js
  - test/gsd-lock-guard.test.js
  - .gitignore
autonomous: true
requirements: [LOCK-01, LOCK-02]
must_haves:
  truths:
    # Probe item 1 (LOCK-01, unclassified → covered)
    - "Con N≥2 procesos robando el MISMO lock GSD muerto, exactamente uno adquiere `{acquired:true}` y el resto recibe `{acquired:false, holder}` — la ventana no-atómica move-aside→`O_EXCL` de `stealLock` queda cerrada POR CONSTRUCCIÓN (D-01), no por reducción probabilística (LOCK-01)."
    # Probe item 3 (LOCK-03 adjacency → covered)
    - "Dos stealers cuyas secciones críticas se solapan quedan serializados por el steal-guard `O_EXCL`: jamás renombran ambos sobre `lockPath` a la vez — se separan (uno gana el guard, el otro re-contiende), nunca colisionan en doble adquisición."
    # Probe item 4 (LOCK-03 empty → covered)
    - "Si `lockPath` queda AUSENTE dentro del guard (el holder original liberó a mitad del steal), el cuerpo crítico respeta a un creador fresco vía `writeFileSync({flag:'wx'})` en lugar de clobbearlo con un rename incondicional — nunca doble adquisición (Pitfall 2)."
    # Probe item 5 (LOCK-03 ordering → covered)
    - "El invariante se asevera sobre el AGREGADO (cardinalidad = 1), nunca sobre QUÉ stealer gana: el ganador queda sin especificar por diseño, pero exactamente-uno es estable e invariante en ejecuciones repetidas."
    # Probe item 6 (LOCK-03 concurrency → covered)
    - "Si un stealer es interrumpido dentro del guard (crash), el guard huérfano es breakable por PID muerto o edad>umbral corto (D-05) y un stealer posterior recupera un estado consistente; un crash entre `writeFileSync(tmp)` y `renameSync(tmp→lockPath)` deja el lock previo intacto (nunca dos locks)."
  prohibitions:
    - statement: "NO reintroducir el move-aside del lock (`renameSync(lockPath → aside)`) — es la causa raíz de la ventana briefly-empty; el fix la elimina del todo."
      status: flagged-unverified
    - statement: "NO usar tmp de nombre FIJO (`writeFileAtomic`/`path+'.tmp'`) para escribir el lock — dos escritores lo comparten y se pisan bytes (anti-patrón WR-02); usar SIEMPRE tmp con `randomUUID()`."
      status: flagged-unverified
    - statement: "NO romper un guard cuyo poseedor está vivo Y dentro del umbral de edad — podría estar a mitad del rename y romperlo reintroduce la doble adquisición (Pitfall 1)."
      status: flagged-unverified
    - statement: "NO añadir exports nuevos al módulo para testear helpers privados — viola D-08; testear el guard vía API pública `acquireGsdLock` + seeding de ficheros en disco."
      status: flagged-unverified
    - statement: "NO añadir dependencias npm nuevas — solo `node:fs`/`node:crypto` built-in (invariante cross-milestone)."
      status: flagged-unverified
    - statement: "NO cambiar el shape `AcquireResult`, el formato JSON del lock, los exports del módulo, ni la semántica de Cases 1-5; NO tocar consumidores (`doctor.decideLock`, dispatch, release) — D-08 LOCKED."
      status: flagged-unverified
    - statement: "NO debilitar el assert `exactly one`, ni `.skip`, ni retries, ni subir timeouts en `test/gsd-lock-race.test.js`/`test/helpers/lock-race-child.mjs` — quedan byte-idénticos (D-07); enmascarar la carrera es el TOP THREAT (T-82-02)."
      status: flagged-unverified
    - statement: "NO aceptar un fix que sólo REDUZCA la probabilidad de la ventana en vez de eliminarla por construcción — viola D-01."
      status: flagged-unverified
  artifacts:
    - "src/gsd/lock.js — `stealLock` reescrito (steal-guard `O_EXCL` + reemplazo in-place atómico) + helpers privados nuevos + docblock reescrito (D-11)"
    - "test/gsd-lock-guard.test.js — unit tests dirigidos del guard vía API pública + seeding"
    - ".gitignore — entradas defensivas para el nuevo artefacto guard/tmp del lock"
  key_links:
    - "`acquireGsdLock` (Cases 2/3/5) sigue llamando a `stealLock` con la misma firma — el cambio queda contenido dentro de `stealLock` + helpers privados"
    - "La propiedad del GUARD la confiere ÚNICAMENTE su `O_EXCL`-create; la propiedad del LOCK la confiere ÚNICAMENTE `renameSync(tmp→lockPath)` — esa separación es el pivote de la corrección"
    - "`isStaleLock`/`isPidAlive` reutilizados verbatim para el re-check ABA dentro del guard y para la staleness del guard (no re-derivar)"
---

<objective>
Cerrar con un fix real la ventana no-atómica move-aside→`O_EXCL` de `stealLock` (`src/gsd/lock.js:283-351`): con N≥2 procesos robando el mismo lock GSD muerto, exactamente uno adquiere (LOCK-01). El mecanismo (D-02, LOCKED) sustituye el move-aside por reemplazo in-place atómico (`writeFileSync(tmp)` + `renameSync(tmp→lockPath)`) serializado por un steal-guard `O_EXCL` breakable — `lockPath` jamás queda ausente por construcción (D-01). Se añaden unit tests dirigidos del guard vía la API pública + seeding, sin nuevos exports (D-08).

Purpose: eliminar la doble adquisición del lock per-repo GSD (dos agentes GSD sobre un repo → corrupción de estado; TOP THREAT T-82-01). Resuelve R-81-01 (mantenedor 2026-07-24 = fix real).
Output: `src/gsd/lock.js` con `stealLock` reescrito + docblock actualizado, `test/gsd-lock-guard.test.js` nuevo, `.gitignore` con el artefacto guard cubierto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-fix-de-la-carrera-de-steallock/82-CONTEXT.md
@.planning/phases/82-fix-de-la-carrera-de-steallock/82-RESEARCH.md
@.planning/phases/82-fix-de-la-carrera-de-steallock/82-PATTERNS.md
@.planning/debug/gsd-lock-race-cr01.md
@src/gsd/lock.js
@src/hooks/session-end.js
@test/gsd-lock.test.js
@test/gsd-lock-race.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests dirigidos del steal-guard (Wave 0 scaffold, vía API pública + seeding)</name>
  <files>test/gsd-lock-guard.test.js</files>
  <read_first>
    - `test/gsd-lock.test.js` — estructura del harness unit: imports (`node:test`, `node:assert/strict`, `mkdtempSync`/`rmSync` en `beforeEach`/`afterEach`), el helper `writeLockDirect` (`:43-47`) que siembra el lock en disco, y las aserciones de resultado + estado final en disco. ES el analog directo de estructura.
    - `test/gsd-lock-race.test.js` — el harness de procesos reales CR-01: `raceGsdStealDeadHolder` (`:74-118`) siembra un lock stale de PID muerto (`pid: 99999999`) y asevera sobre el agregado. NO se toca; se lee para replicar la convención de PID muerto y la intención de carrera.
    - `src/gsd/lock.js` — superficie pública a importar (`acquireGsdLock`, `readLock`, `isPidAlive`, `LOCK_FILE`, `DEFAULT_TTL_HOURS`); confirmar que NO se importan helpers privados (D-08).
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-RESEARCH.md` §"Validation Architecture" (Wave 0 Gaps) y §"Open Questions" #1 (testear vía API pública + seeding, no exponer).
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-PATTERNS.md` §`test/gsd-lock-guard.test.js` — los 4 casos requeridos y el patrón `writeGuardDirect`.
  </read_first>
  <action>
    Crear `test/gsd-lock-guard.test.js`, un fichero de unit tests que ejercita los estados del steal-guard SOLO a través de la API pública `acquireGsdLock` + seeding de ficheros en disco (sin importar ni exportar helpers privados — D-08). Reutilizar el estilo de `test/gsd-lock.test.js`: sandbox `mkdtempSync(join(tmpdir(), 'kodo-lock-guard-'))` en `beforeEach`, `rmSync(..., {recursive:true, force:true})` en `afterEach`. Añadir un helper `writeGuardDirect(projectPath, {pid, ts})` que escribe `.planning/.kodo.lock.steal-guard` (fichero hermano del lock, ruta `LOCK_FILE + '.steal-guard'`) con JSON `{pid, ts}`, análogo a `writeLockDirect`. Reutilizar/adaptar `writeLockDirect` para sembrar el lock stale (PID muerto `99999999`, convención de `test/gsd-lock.test.js:99-106`).

    Cubrir estos casos deterministas (asertando resultado del API + estado final en disco):
    - (a) Guard HUÉRFANO de PID muerto (`pid: 99999999`) + lock stale de PID muerto → `acquireGsdLock` devuelve `{acquired:true}`; el `.kodo.lock` final pertenece al llamante (`session_id` propio, `pid: process.pid`); NO queda `.kodo.lock.steal-guard` ni ficheros `.tmp.*`/`.steal.*` residuales.
    - (b) Guard VIVO+FRESCO (`pid: process.pid`, `ts: Date.now()`) + lock stale de PID muerto → `acquireGsdLock` devuelve `{acquired:false}` (NO roba pasando por encima de un guard válidamente tomado); el `.kodo.lock.steal-guard` seguirá existiendo (NO se rompe); el `.kodo.lock` final queda consistente (un solo fichero de lock, sin doble adquisición). Determinista porque `process.pid` está garantizado vivo y `ts` es fresco.
    - (c) Guard VIVO pero MUY VIEJO (`pid: process.pid`, `ts: Date.now() - 3600_000` = 1 hora atrás, holgadamente > cualquier umbral "de segundos") + lock stale → `acquireGsdLock` devuelve `{acquired:true}` (roto por edad, agnóstico al umbral exacto elegido por el implementador); estado final = un solo lock del llamante.
    - (d) Crash simulado mid-steal: guard huérfano de PID muerto + lock stale + (opcional) un `.tmp.*` residual sembrado → `acquireGsdLock` recupera a un estado consistente: exactamente un `.kodo.lock` del llamante, sin dobles.

    Documentar en una cabecera del fichero (SIN reproducir literales que otras gates negativo-grepeen) que la propiedad concurrente "dos breakers del mismo guard huérfano → exactamente uno posee" (Assumption A1) la cubre el harness de procesos reales `test/gsd-lock-race.test.js` (CR-01) + el loop de estrés del Plan 82-02, no un unit test in-process (que requeriría concurrencia real y sería flaky). Estos unit tests son deterministas por diseño.

    Nota de ordenación TDD: contra la implementación actual (move-aside, sin guard) algunos de estos casos NO pasan todavía — describen el comportamiento objetivo del guard. La Task 2 los pone en verde. Ejecutarlos aquí para confirmar que compilan y fallan de forma legible (RED), no para que pasen.
  </action>
  <verify>
    <automated>node --test test/gsd-lock-guard.test.js 2>&1 | grep -qE '# tests [1-9]' && grep -qE "from '\.\./src/gsd/lock\.js'" test/gsd-lock-guard.test.js && grep -q 'acquireGsdLock' test/gsd-lock-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/gsd-lock-guard.test.js` existe y el runner descubre ≥1 test bajo `node --test` (compila; describe/it válidos) — verificado por `# tests [1-9]`.
    - El fichero importa de `../src/gsd/lock.js` la superficie pública (referencia `acquireGsdLock`) y NO importa helpers privados del guard (garantizado por el gate de export-count == 5 de la Task 2: los helpers no existen como exports, luego no son importables).
    - Contiene los 4 casos (a)(b)(c)(d) con seeding de guard + lock stale y aserciones sobre resultado del API y estado final en disco (existencia/ausencia de `.kodo.lock` y `.kodo.lock.steal-guard`).
    - Caso (c) usa `ts` = 1 hora atrás (agnóstico al umbral exacto), no un valor hardcodeado al umbral del implementador.
    - La cabecera documenta que A1 (dos breakers concurrentes) se cubre por el harness CR-01 + estrés, no in-process — sin reproducir literales de assert.
  </acceptance_criteria>
  <done>El fichero de unit tests del guard existe, compila, ejercita los 4 estados vía API pública + seeding, no añade exports, y falla de forma legible contra la implementación actual (RED) a la espera de la Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescribir `stealLock` — steal-guard `O_EXCL` + reemplazo in-place atómico + docblock (D-02/D-05/D-06/D-08/D-11)</name>
  <files>src/gsd/lock.js, .gitignore</files>
  <read_first>
    - `src/gsd/lock.js` (fichero completo) — `stealLock` actual (`:283-351`, el move-aside a reemplazar), su docblock (`:258-282`, describe el CAS move-aside a reescribir por D-11), `acquireGsdLock` (`:108-151`, los call-sites de `stealLock` que NO cambian), `writeLockFile` (`:203-208`, `O_EXCL` `{flag:'wx'}`), `serializeLockContent` (`:216-227`), `readLockContent` (`:236-242`), `isStaleLock` (`:251-256`), `isPidAlive` (`:72-79`), `MAX_STEAL_ATTEMPTS` (`:57`), y los imports (`:2-12`, ya incluyen `writeFileSync`/`renameSync`/`unlinkSync`/`existsSync`/`mkdirSync`/`dirname`/`randomUUID` — NO hacen falta imports nuevos).
    - `src/hooks/session-end.js:368-381` — el patrón de escritura atómica de referencia (WR-02, en producción): tmp de nombre único `planPath + '.tmp.' + process.pid + '.' + randomUUID()`, `writeFileSync(tmp)` + `renameSync(tmp, dest)`, `rmSync(tmp, {force:true})` en el `catch`. Es el analog exacto para el owner-op del LOCK.
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-RESEARCH.md` §"Architecture Patterns" (Patterns 1-3), §"Common Pitfalls" (1-4, especialmente Pitfall 2 rama `lockPath` ausente), §"Esqueleto de `stealLock` reescrito" (referencia de diseño, NO prescripción literal), y §"Assumptions Log" (A1/A2).
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-CONTEXT.md` — decisiones LOCKED D-01/D-02/D-05/D-06/D-08/D-11 y §"Claude's Discretion".
    - `.planning/phases/82-fix-de-la-carrera-de-steallock/82-PATTERNS.md` §`stealLock` rewrite y §"Shared Patterns".
    - `.gitignore` (raíz) — hoy sólo `node_modules/`; confirmar antes de añadir entradas.
  </read_first>
  <action>
    Reescribir la función `stealLock` en `src/gsd/lock.js` para cerrar la ventana move-aside→`O_EXCL` por construcción (D-01), manteniendo la firma `stealLock(lockPath, sessionInfo, reason)` y el contrato `AcquireResult` intactos (D-08). Sustituir el move-aside actual (`:288-292`, `renameSync(lockPath → aside)`) por el mecanismo D-02 con estos elementos (nombres exactos = discreción del implementador, sugeridos entre paréntesis):

    1. Steal-guard `O_EXCL` (fichero hermano en `guardPath = ${lockPath}.steal-guard`): un helper privado (p. ej. `acquireStealGuard(guardPath)`) hace `writeFileSync(guardPath, JSON.stringify({pid: process.pid, ts: Date.now()}), {flag:'wx'})`; éxito = poseedor, `EEXIST` = perdedor, cualquier otro `code` re-lanza. La propiedad del guard la confiere SOLO este `O_EXCL`-create exitoso (Pattern 2). El guard vive en el mismo directorio `.planning/` que el lock (mismo filesystem → evita `EXDEV`, Pitfall 3).

    2. Cuerpo crítico serializado por el guard: SOLO el poseedor del guard ejecuta re-read → confirmar stale → escribir → rename. Re-leer `lockPath` con `readLockContent`; tres ramas (Pitfall 2): (a) holder presente y `!isStaleLock(holder)` → `return {acquired:false, holder}`; (b) holder presente y stale → reemplazo in-place: `writeFileSync(tmp, serializeLockContent(sessionInfo))` con `tmp = ${lockPath}.tmp.${process.pid}.${randomUUID()}` seguido de `renameSync(tmp, lockPath)` (atómico, `lockPath` nunca ausente); (c) holder AUSENTE → respetar a un creador fresco vía `writeFileSync(lockPath, serializeLockContent(sessionInfo), {flag:'wx'})` directo (no rename); en `EEXIST`, re-leer holder y decidir. En error de escritura, `unlinkSync(tmp)` best-effort y re-lanzar. Liberar el guard con `unlinkSync(guardPath)` en un `finally` (camino feliz y de error).

    3. Guard breakable (D-05): un helper privado (p. ej. `guardIsStale(guard, thresholdMs)`) devuelve `true` si `!guard` (corrupt/partial) O `!isPidAlive(guard.pid)` (poseedor muerto — criterio primario, siempre seguro) O `Date.now() - guard.ts > thresholdMs` (edad > umbral). El umbral (constante nueva, p. ej. `STEAL_GUARD_STALE_MS`) es de orden de segundos (margen ≥1000× sobre la sección crítica de ~1ms, A2) — discreción del implementador. La rotura (p. ej. `breakStaleGuard(guardPath)`): NO confiere propiedad; sólo limpia el guard stale (`unlinkSync` best-effort) para que alguien pueda `O_EXCL`-crear uno fresco. CRÍTICO: nunca romper un guard vivo+dentro-de-umbral (Pitfall 1 — reintroduce doble adquisición).

    4. Contención acotada de los perdedores (D-06): reutilizar el esqueleto `for (let attempt = 0; attempt < MAX_STEAL_ATTEMPTS; attempt++)`. El perdedor del guard: si `guardIsStale` → romperlo y re-contender (`continue`); si no → re-leer `lockPath` (holder vivo → `return {acquired:false, holder}`; stale → backoff acotado corto y `continue`). Nunca espera indefinida ni spin sin límite. Fallback de agotamiento de presupuesto (D-04, re-evaluado bajo D-01): el fallback final ACTUAL (`src/gsd/lock.js:339-350`, `writeLockFile` tras agotar attempts) NO puede reabrir la ventana briefly-empty — se sustituye por rechazo contra el holder actual (`readLockContent(lockPath)` → `{acquired:false, holder}`) o, a lo sumo, una última adquisición vía op atómica (`writeFileSync({flag:'wx'})` u `renameSync(tmp→lockPath)`), JAMÁS un move-aside final ni un create sobre un path que se dejó ausente. El camino patológico nunca deja `lockPath` ausente ni «gana» sin una op atómica.

    5. Docblock (D-11): reescribir el bloque JSDoc de `stealLock` (`:258-282`) — hoy describe el CAS move-aside que el fix elimina. El nuevo docblock describe el mecanismo guard-`O_EXCL` + reemplazo in-place atómico (propiedad del guard vía `O_EXCL`, propiedad del lock vía `renameSync(tmp→lockPath)`, guard breakable por PID/edad, `lockPath` nunca ausente por construcción). Mantener el tail `@param`/`@returns` (`:278-281`) porque la firma no cambia (D-08). Eliminar la mención al ABA-guard obsoleto.

    6. `.gitignore` (hygiene del nuevo artefacto): añadir entradas para el guard y el tmp del lock si no están presentes — `.planning/.kodo.lock`, `.planning/.kodo.lock.steal-guard`, y `.planning/.kodo.lock.tmp.*` (patrón glob). Previene que el artefacto efímero nuevo que introduce esta fase se commitee accidentalmente bajo `.planning/` (que sí está trackeado). No mover ni renombrar `node_modules/`.

    NO añadir exports nuevos (D-08 — los helpers del guard son privados, testeados vía API pública). NO cambiar `serializeLockContent`/formato JSON del lock. NO tocar `acquireGsdLock`/`releaseGsdLock`/`readLock`/consumidores.
  </action>
  <verify>
    <automated>node --test test/gsd-lock.test.js test/gsd-lock-guard.test.js test/gsd-lock-race.test.js && test "$(grep -cE '^export ' src/gsd/lock.js)" = "5" && git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs && git diff --quiet -- package.json && node -e "require('node:fs').readFileSync('.gitignore','utf8').includes('steal-guard')||process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/gsd-lock.test.js` verde: las Cases 1-5 + release + readLock del contrato D-08 siguen pasando (15 tests) — el shape `AcquireResult` y la semántica no cambian.
    - `node --test test/gsd-lock-guard.test.js` verde: los 4 casos del guard (a)(b)(c)(d) pasan (RED→GREEN).
    - `node --test test/gsd-lock-race.test.js` verde: los casos CR-01 (N=2/N=5) pasan — la doble adquisición está cerrada.
    - `grep -cE '^export ' src/gsd/lock.js` == 5 (sin exports nuevos — D-08).
    - `git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs` limpio: harness byte-idéntico (D-07).
    - `git diff --quiet -- package.json` limpio: cero deps nuevas.
    - `.gitignore` contiene una entrada con `steal-guard` (el nuevo artefacto queda cubierto).
    - El docblock de `stealLock` describe el mecanismo guard+rename-in-place y ya NO menciona move-aside/ABA-guard (D-11).
    - `stealLock` ya no contiene `renameSync(lockPath, <algo-aside>)` (el move-aside del lock queda eliminado; el único rename del lock es `renameSync(tmp, lockPath)`).
    - El fallback de agotamiento de attempts (D-04) no reabre la ventana: no hay `renameSync(lockPath → aside)` ni un create sobre un path deliberadamente dejado ausente en el camino patológico; a lo sumo rechaza contra el holder o adquiere vía op atómica (`{flag:'wx'}`/`renameSync(tmp→lockPath)`).
  </acceptance_criteria>
  <done>`stealLock` cierra la ventana por construcción con guard `O_EXCL` + reemplazo in-place atómico; los tests del guard, del contrato y del harness CR-01 pasan; sin exports/deps nuevas; harness byte-idéntico; docblock reescrito; `.gitignore` cubre el guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proceso ↔ proceso | N agentes GSD concurrentes contienden por el mismo lock per-repo (`.planning/.kodo.lock`) vía filesystem — no hay canal de coordinación fuera del propio filesystem |
| proceso ↔ filesystem | Los artefactos de lock/guard/tmp (`.kodo.lock`, `.kodo.lock.steal-guard`, `.kodo.lock.tmp.*`) son el único estado compartido; su atomicidad la garantiza el kernel (`rename(2)`, `O_EXCL`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-82-01 | Tampering | `stealLock` (`src/gsd/lock.js`) | high | mitigate | Reemplazo in-place atómico (`renameSync(tmp→lockPath)`) + steal-guard `O_EXCL` serializa el cuerpo crítico → `lockPath` nunca ausente por construcción (D-01/D-02); exactamente un stealer gana. Verificado por CR-01 harness verde. |
| T-82-02 | Tampering (integridad de la garantía) | `test/gsd-lock-race.test.js` + assert `exactly one` | high | mitigate | Harness byte-idéntico (D-07, `git diff --quiet`); assert intacto; verde determinista bajo estrés (Plan 82-02). Enmascarar la carrera está prohibido (prohibitions). |
| T-82-03 | Denial of Service | steal-guard huérfano | medium | mitigate | Guard breakable por PID muerto (`isPidAlive`, primario) o edad>umbral corto (D-05); cleanup best-effort en `finally`. Un poseedor que crashea no bloquea steals para siempre. |
| T-82-04 | Denial of Service (menor) | tmp/guard huérfanos como basura | low | accept | Nombres únicos por escritor (`randomUUID()`) + `unlink` best-effort en camino feliz y de error; `.gitignore` evita que se commiteen. Basura funcional despreciable; aceptado. |
| T-82-SC | Tampering | npm/pip/cargo installs | low | accept | Sin instalaciones esta fase — invariante cero-deps (`git diff --quiet -- package.json`). Sin superficie de supply-chain; nada que verificar. |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (surfaced, no silent drops)

- **A1 (RESEARCH [ASSUMED], no probe-item):** La ventana breve de `guardPath` durante una rotura es inocua porque la propiedad del guard la confiere SOLO el `O_EXCL`-create posterior (nunca la rotura). NO se valida con un unit test in-process determinista (requeriría concurrencia real → flaky); se valida con el harness de procesos reales `test/gsd-lock-race.test.js` (CR-01, N=5) + el loop de estrés ≥50× del Plan 82-02. Riesgo si falla: doble adquisición un nivel abajo (dos breakers del mismo guard huérfano acabando ambos como poseedores). Mitigación: los casos CR-01 deben quedar verdes deterministas bajo estrés.
- **A2 (RESEARCH [ASSUMED]):** Un umbral de edad del guard "de segundos" es holgadamente mayor que el peor caso de la sección crítica (~1ms: read+write+rename). Riesgo si falla: bajo carga extrema la sección crítica podría exceder el umbral y romperse un guard vivo. Mitigación: PID-muerto es el criterio primario (siempre seguro); el umbral por edad es respaldo con margen ≥1000×.

> No-silent-drop: los 6 items del edge-probe quedan representados como `must_haves.truths` (5 en este plan — items 1/3/4/5/6; 1 en el Plan 82-02 — item 2). 0 items del probe quedan sin resolver. A1/A2 son assumptions de research adicionales surfaced aquí para transparencia (no son probe-items).
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces

Símbolos y ficheros CREADOS o REESCRITOS por esta fase (excluir de la verificación de drift — son nuevos por construcción):

**Producción (`src/gsd/lock.js`):**
- Helpers PRIVADOS nuevos (no exportados — D-08; nombres = discreción): `acquireStealGuard(guardPath)`, `guardIsStale(guard, thresholdMs)`, `breakStaleGuard(guardPath)`, `readGuard(guardPath)` (o equivalentes).
- Constante nueva del umbral de edad del guard (p. ej. `STEAL_GUARD_STALE_MS`), orden de segundos.
- `stealLock` reescrito (misma firma, mismo `AcquireResult`) + su docblock reescrito.

**Artefactos de filesystem efímeros nuevos (runtime, no persistentes):**
- `${lockPath}.steal-guard` — fichero de guard efímero (contenido `{pid, ts}`), best-effort-cleanup + breakable.
- `${lockPath}.tmp.<pid>.<uuid>` — tmp efímero del reemplazo in-place.

**Test:**
- `test/gsd-lock-guard.test.js` + su helper `writeGuardDirect(projectPath, {pid, ts})`.

**Sin cambios (D-08):** exports del módulo, `AcquireResult`, formato JSON del lock, Cases 1-5, `acquireGsdLock`/`releaseGsdLock`/`readLock`/`isPidAlive`, consumidores. Sin CLI flags nuevos. Sin endpoints. Sin deps npm.
</artifacts_produced>

<verification>
- `node --test test/gsd-lock.test.js` → 15 tests verdes (contrato D-08 intacto).
- `node --test test/gsd-lock-guard.test.js` → los 4 casos del guard verdes.
- `node --test test/gsd-lock-race.test.js` → CR-01 N=2/N=5 verdes (una pasada; el determinismo bajo estrés lo evidencia el Plan 82-02).
- `grep -cE '^export ' src/gsd/lock.js` == 5.
- `git diff --quiet -- test/gsd-lock-race.test.js test/helpers/lock-race-child.mjs` → sin cambios (D-07).
- `git diff --quiet -- package.json` → sin cambios (cero deps).
- `.gitignore` contiene la entrada `steal-guard`.
</verification>

<success_criteria>
- La ventana move-aside→`O_EXCL` de `stealLock` queda cerrada por construcción (D-01): el único rename del lock es `renameSync(tmp, lockPath)`; el move-aside desaparece.
- Con N≥2 stealers del mismo lock muerto, exactamente uno adquiere (CR-01 verde) — LOCK-01.
- Los unit tests dirigidos del guard cubren huérfano-PID-muerto, vivo-fresco-bloquea, viejo-por-edad-rompe, y crash-consistente.
- Contrato D-08 intacto (Cases 1-5, exports, JSON, consumidores); harness byte-idéntico (D-07); cero deps nuevas.
- Docblock de `stealLock` reescrito (D-11): describe guard+rename-in-place, sin move-aside/ABA.
</success_criteria>

<output>
Create `.planning/phases/82-fix-de-la-carrera-de-steallock/82-01-SUMMARY.md` when done
</output>
