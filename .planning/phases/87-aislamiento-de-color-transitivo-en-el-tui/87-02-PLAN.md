---
phase: 87-aislamiento-de-color-transitivo-en-el-tui
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - test/format-isolation.test.js
  - src/cli/dashboard/inbox-count.js
  - test/dashboard-inbox-count.test.js
autonomous: true
requirements: [ISO-03, ISO-04]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`src/cli/dashboard/format.js` queda congelado por test como hoja: cero imports **relativos**, y los builtins que importa están todos dentro de una allowlist explícita de un elemento (`node:path`) escrita con su medición (ISO-03 / UF-02, D-13). Comando: `node --test test/format-isolation.test.js`."
    - "La clausura transitiva de `src/cli/dashboard/format.js` es **exactamente 1** (él mismo) — el assert de alcanzabilidad, que es el que de verdad muerde frente a los dos de forma (ISO-03, D-13). Comando: `node --test test/format-isolation.test.js`."
    - "`src/cli/dashboard/select.js` sigue consumiendo `./format.js`: aserto **positivo** de convergencia, espejo de ORCH-05, sin el cual ISO-03 congelaría un módulo huérfano y podría regresar en silencio moviendo `nextCell` a otro sitio (ISO-03, D-14). Comando: `node --test test/format-isolation.test.js`."
    - "`test/format-isolation.test.js` ya no declara ningún punto ciego en falso: la premisa retirada de `:14` y `:33` no queda ni citada. Comando: `grep -c \"el repo no lo usa\\|el repo no los usa\" test/format-isolation.test.js` → `0` (HEAD `61a5c95`: 2)."
    - "La cabecera reescrita enumera lo que el fichero **CUBRE** (imports estáticos con y sin binding, re-exports, e `import()` dinámico con specifier literal fuera de comentarios), nombra el punto ciego **residual** (`import()` con specifier computado) sin presentarlo como cerrado, y fecha su medición sobre `src/` (ISO-04, D-12). Comando de forma: `grep -c 'CUBRE' test/format-isolation.test.js` ≥ 1 y `grep -cE '2026-0[0-9]-[0-9]{2}' test/format-isolation.test.js` ≥ 1."
    - "El helper `stripComments` con el orden corregido **no ciega** al guard: un meta-test asevera que recupera los **4** imports estáticos de `src/cli/dashboard/markdown.js`, el fichero que es a la vez uno de los tres cegados por el orden verbatim y un leaker primario de esta fase (ISO-04, D-09/D-10). Comando: `node --test test/format-isolation.test.js`."
    - "Los tres comentarios que ISO-01/ISO-02 vuelven falsos quedan corregidos, no omitidos: `src/cli/dashboard/inbox-count.js` (bloque de prohibición) y `test/dashboard-inbox-count.test.js` (cabecera) dejan de afirmar que el guard solo mira imports directos y que el store del inbox alcanza el paquete de color. La **prohibición en sí se conserva**, con sus motivos vigentes. Comando: `grep -c 'imports DIRECTOS' src/cli/dashboard/inbox-count.js test/dashboard-inbox-count.test.js` → `0` en ambos (HEAD `61a5c95`: 2 y 1)."
    - "Cero regresión: `node --test test/dashboard-inbox-count.test.js` sigue en `# tests 14 · # fail 0` y `npm test` sigue verde con `# fail 0` — los cambios de esta tarea son de comentario en producción y aditivos en test."
    - statement: "La declaración honesta pasa la revisión de honestidad: enumera qué cubre, nombra `import()` con specifier computado como no cubierto, fecha la medición con las cifras que el ejecutor **re-midió en esta sesión** (no las transcritas de la investigación), y no contiene la premisa retirada ni citada. Ningún assert puede comprobar que un texto es honesto — se revisa leyendo la cabecera y se cita la sección en el VERIFICATION."
      verification: backstop
  prohibitions:
    - "(sonda §B — transparencia) NO sustituir la premisa falsa por otra afirmación no verificable. Cada cifra de la declaración honesta se re-mide en la sesión que la escribe y va fechada; el punto ciego residual (`import()` con specifier computado) **NO** puede presentarse como cerrado, mitigado ni improbable. Escribir «hoy hay 0 casos» es una foto; escribir «no puede pasar» es reincidir en el pecado que ISO-04 retira."
    - "(sonda §B — integridad de los guards) NO poner verde ningún guard debilitándolo: la única allowlist admitida en toda la fase es la de D-13 (`node:path` en el guard de hoja de `src/cli/dashboard/format.js`), justificada por medición. Ninguna otra excepción entra «para que pase» (DEBT-04 LOCKED, D-16)."
    - "NO citar la premisa retirada verbatim dentro del fichero al explicar qué se retiró: reintroduciría el literal y el criterio de fuente volvería a contarlo. Se describe por su contenido."
    - "NO tocar `src/cli/dashboard/format.js` ni `src/cli/dashboard/select.js`: ISO-03 los **congela** tal como están, no los modifica. Si el guard sale rojo sobre ellos, es un hallazgo que se mide, no un fichero que se ajusta."
    - "NO ampliar la allowlist de builtins más allá de `node:path`, ni convertirla en una lista abierta o derivada del propio fichero: una allowlist que se calcula del sujeto no aserta nada."
    - "NO retirar la prohibición de importar `src/inbox/store.js` desde `src/cli/dashboard/inbox-count.js`: sigue siendo correcta por sus otros motivos (arrastraría `withFileLock` y `resolveProjectId` a un módulo que solo cuenta líneas). Lo que se corrige es su **premisa**, no su conclusión."
    - "NO generalizar los guards más allá de `src/cli/dashboard/`: fuera de alcance declarado."
    - "NO corregir `stripComments` en `test/check-isolation.test.js` ni en `test/dispatcher-isolation.test.js`, ni extraer un helper compartido: diferido con trigger escrito."
    - "NO añadir ninguna dependencia npm — invariante cross-milestone de `.planning/STATE.md`."
  artifacts:
    - "`test/format-isolation.test.js` — `ALLOWED_BUILTINS`: constante congelada de un elemento (`node:path`), la única allowlist de la fase."
    - "`test/format-isolation.test.js` — suite `ISO-03 (Phase 87 / UF-02): src/cli/dashboard/format.js es una HOJA pura`, con los casos `cero imports relativos; builtins solo los de la allowlist; clausura de 1` y `select.js consume ./format.js (convergencia, D-14)`."
    - "`test/format-isolation.test.js` — suite `ISO-04 (Phase 87): stripComments no ciega al guard`, con el caso `stripComments recupera los 4 imports estáticos de src/cli/dashboard/markdown.js`."
    - "`test/format-isolation.test.js` — bloque de **declaración honesta** en la cabecera (CUBRE / NO CUBRE — punto ciego residual / MEDICIÓN FECHADA, NO GARANTÍA), sustituye a `:10-16`; y la línea equivalente reescrita en el JSDoc de `walkImports` (`:30-34`)."
  key_links:
    - "`extractImports(dashboard/format.js)` → filtro de specifiers relativos (vacío) → filtro contra `ALLOWED_BUILTINS` (vacío) → `walkImports(...).size === 1`: los dos primeros asserts son de FORMA, el tercero es de ALCANZABILIDAD y sobreviviría a una sintaxis de import que el regex no viera."
    - "`walkImports(select.js)` contiene `dashboard/format.js` → el guard de pureza deja de ser decoración: sin este aserto positivo, mover `nextCell` a otro sitio dejaría ISO-03 verde sobre un módulo que no consume nadie (D-14)."
    - "`stripComments(markdown.js)` → `extractImports(...)` = 4 → el meta-test ata el helper corregido al fichero concreto que el orden verbatim ciega; si alguien «arregla» la divergencia devolviendo el bug, este caso cae a 0 y la suite lo dice."
    - "Bloque de prohibición de `src/cli/dashboard/inbox-count.js` → su premisa (`store.js` alcanza el paquete de color / el guard solo mira imports directos) → ambas mitades las invalidan ISO-02 e ISO-01 respectivamente; la conclusión (no importar el store) se conserva con sus motivos restantes."
---

<objective>
Cerrar las dos mitades que el plan `87-01` deja abiertas y que hacen que la invariante deje de depender de la disciplina: **congelar por test la pureza de `src/cli/dashboard/format.js`** —la premisa sobre la que descansa que `select.js` lo importe sin arrastrar color, y que hasta hoy ningún test asevera (ISO-03 / UF-02)— y **retirar la premisa falsa de la cabecera del propio fichero de guard**, sustituyéndola por una declaración honesta con medición fechada y punto ciego residual nombrado (ISO-04).

Es la tarea de expansión del tracer: el plan `87-01` demostró el carril completo (detectar → cortar → morder); éste añade los dos guards que impiden que lo demostrado regrese en silencio, y salda de paso los tres comentarios que ISO-01/ISO-02 vuelven falsos en otros dos ficheros.

Purpose: esta fase existe porque un comentario declaraba un punto ciego que no tapaba. Cerrarla dejando tres comentarios nuevos declarando algo falso sería reabrir el mismo pecado en otro fichero — y la Phase 88 los encontraría.
Output: `test/format-isolation.test.js` con las suites ISO-03 (pureza + convergencia) e ISO-04 (meta-test de `stripComments`) y la cabecera reescrita; `src/cli/dashboard/inbox-count.js` y `test/dashboard-inbox-count.test.js` con su premisa corregida y su prohibición intacta.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-CONTEXT.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-PATTERNS.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-VALIDATION.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-01-SUMMARY.md
</context>

<!-- planner-discipline-allow: cli/format -->
<!-- planner-discipline-allow: el repo no lo usa -->
<!-- planner-discipline-allow: el repo no los usa -->
<!-- planner-discipline-allow: imports DIRECTOS -->

<tasks>

<task type="auto">
  <name>Tarea 1: Congelar la pureza de `dashboard/format.js` y su convergencia con `select.js`</name>

  <files>test/format-isolation.test.js</files>

  <read_first>
    - `test/format-isolation.test.js` tal como lo dejó el plan `87-01` — el fichero que se extiende. En particular: `extractImports`, `walkImports` (que se reutiliza tal cual, **sin** `stripComments` dentro), `listJsFiles`, y las suites `ISO-01` e `ISO-02` recién añadidas, cuyo estilo hay que continuar.
    - `test/check-isolation.test.js:241-258` (`handoff.js`) y `:269-285` (`pending.js`) — el molde **doble y literal** del guard de hoja. La estructura de 4 partes que hay que replicar: comentario con la razón dura de por qué existe el guard y qué degradación previene · `assert.equal(existsSync(p), true, '… otherwise this isolation test passes trivially')` como anti-vacuidad · `assert.deepEqual(imports, [], msg)` con `found: …` en el mensaje · el mensaje nombrando al **consumidor** que la pureza protege.
    - `test/check-isolation.test.js:287-300` — ORCH-05, el molde **literal** del aserto positivo de convergencia, incluido su comentario con la estructura «los guards de prohibición de arriba prueban X; este prueba Y, y por eso la convergencia no puede regresar en silencio».
    - `src/cli/dashboard/format.js` — el sujeto. Verificado hoy contra `61a5c95`: **un único import**, `basename` de `node:path`, en `:25`; su bloque de comentario de color-isolation está en `:22-23`. **No se modifica.**
    - `src/cli/dashboard/select.js:30-35` — el consumidor. Su comentario de `:30-34` afirma literalmente que `./format.js` es puro y que no arrastra la capa de color; el import está en `:35`. Es la afirmación que este plan convierte en assert. **No se modifica.**
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md` §Code 4 — el código completo de las dos suites, **ya ejecutado y verde contra el árbol real** (congelan el estado actual, que es su función). §Patrón 3 y §Patrón 4.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-PATTERNS.md` §guard de hoja ISO-03 y §aserto positivo de convergencia.
  </read_first>

  <action>
Añadir a `test/format-isolation.test.js` una suite `describe('ISO-03 (Phase 87 / UF-02): src/cli/dashboard/format.js es una HOJA pura')` con dos casos, sin tocar nada de lo ya existente en el fichero.

**1) La constante de allowlist.** Dentro de la suite, `ALLOWED_BUILTINS` como array congelado de **un solo elemento**: el specifier del builtin de rutas. Congelado literalmente, no derivado del fichero sujeto — una allowlist calculada a partir de lo que el sujeto importa no asevera nada.

**2) Comentario de cabecera de la suite (Patrón compartido 5: el comentario lleva la MEDICIÓN, no la preferencia).** Debe decir: que `src/cli/dashboard/format.js` es la capa de presentación pura del dashboard; que su pureza es la **premisa** sobre la que descansa que `select.js` pueda importarlo sin arrastrar color —cableado por DEBT-06 en la Phase 85, afirmado por el comentario de `select.js:30-34`, y **hasta hoy sin ningún test que lo asevere**—; que el molde de redacción son los guards de hoja de `src/session/handoff.js` y `src/tasks/pending.js`; y que la **única divergencia** respecto de ese molde es la allowlist, porque aquéllos exigen cero imports **incluidos builtins** y este fichero importa uno. La divergencia se documenta **con su medición**: ese builtin no arrastra nada y la clausura transitiva del sujeto es exactamente 1 (medido). D-16 admite esta allowlist y ninguna otra en toda la fase.

**3) `it('cero imports relativos; builtins solo los de la allowlist; clausura de 1')`.** Tres asserts, en este orden:
   - Anti-vacuidad: `assert.equal(existsSync(formatPath), true, …)` con el mensaje del molde, que dice que si el fichero no existe el test pasa trivialmente.
   - Forma A: los specifiers extraídos que empiezan por punto deben ser lista vacía, con el mensaje explicando que debe ser una hoja para que `select.js` lo importe sin arrastrar grafo ni color, y listando los encontrados.
   - Forma B: los specifiers extraídos que **no** estén en `ALLOWED_BUILTINS` deben ser lista vacía, con el mensaje nombrando la allowlist y listando todos los imports.
   - Alcanzabilidad: `assert.equal(walkImports(formatPath).size, 1, …)`. **Éste es el que de verdad muerde**: los dos anteriores son de forma y sobrevivirían a una sintaxis de import que el regex no viera; éste no. El comentario debe decirlo.

**4) `it('select.js consume ./format.js (convergencia, D-14)')`.** `assert.ok(walkImports(selectPath).has(formatPath), msg)`, con el grafo completo de `select.js` interpolado en el mensaje, exactamente como el molde ORCH-05. El comentario que lo precede explica que sin este aserto positivo la premisa que ISO-03 protege se puede regresar **en silencio** moviendo `nextCell` a otro sitio: el guard de pureza seguiría verde sobre un módulo huérfano. Un guard de prohibición sobre un módulo que no usa nadie es verde y vacío.

Las dos suites salen **verdes ya contra el árbol actual** — congelan el estado, que es su función; su valor no es descubrir nada hoy sino que la regresión de mañana sea un fallo de suite y no una degradación silenciosa. Ni `src/cli/dashboard/format.js` ni `src/cli/dashboard/select.js` se modifican en esta tarea.
  </action>

  <verify>
    <automated>node --test test/format-isolation.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    Valores de HEAD medidos en vivo contra `61a5c95`; el conteo de partida del fichero de guard es el que dejó el plan `87-01`.

    - `node --test test/format-isolation.test.js` da `# fail 0` y su `# tests` crece en **2** respecto del valor que registró el `87-01-SUMMARY.md`. **Muerde** — las dos suites no existen.
    - La suite nueva se llama `ISO-03 (Phase 87 / UF-02): src/cli/dashboard/format.js es una HOJA pura` y contiene exactamente los dos casos nombrados en `<artifacts_produced>`. **Muerde.**
    - `grep -c 'ALLOWED_BUILTINS' test/format-isolation.test.js` es ≥ 2 (declaración y uso) y la declaración contiene **un solo** specifier de builtin. **Muerde** — HEAD: `0`.
    - El caso de pureza contiene un assert de **alcanzabilidad** además de los dos de forma: `grep -c 'walkImports' test/format-isolation.test.js` crece respecto del valor post-`87-01`, y el mensaje del assert menciona la clausura. **Muerde.**
    - `git diff --exit-code -- src/cli/dashboard/format.js src/cli/dashboard/select.js` sale con código 0: ISO-03 congela, no modifica. **Anti-regresión** por diseño — delimita el alcance y no acredita trabajo.
    - `node --test test/check-isolation.test.js` da `# tests 12 · # fail 0` sin haber tocado el fichero: los moldes que se copian siguen intactos y su helper con bug sigue sin corregir (diferido). **Anti-regresión** — HEAD: 12/0.
    - `git diff --exit-code -- package.json package-lock.json` sale con código 0. **Anti-regresión** — invariante de cero dependencias nuevas.
  </acceptance_criteria>

  <done>
    `test/format-isolation.test.js` congela por test que `src/cli/dashboard/format.js` no tiene imports relativos, que su único builtin está en una allowlist de un elemento escrita con su medición, y que su clausura transitiva es exactamente 1; y asevera positivamente que `select.js` sigue consumiéndolo, de modo que la pureza no puede degradar a decoración. Ninguno de los dos ficheros de producción se ha tocado.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: La declaración honesta y los tres comentarios que la fase vuelve falsos</name>

  <files>test/format-isolation.test.js, src/cli/dashboard/inbox-count.js, test/dashboard-inbox-count.test.js</files>

  <read_first>
    - `test/format-isolation.test.js:10-16` y `:30-34` — las dos ubicaciones a reescribir. `:10-13` es la explicación (correcta, se conserva) de por qué hay dos regex de import; `:14` es la línea de premisa falsa. `:30-32` y `:34` son JSDoc correcto de `walkImports`; `:33` es la segunda línea de premisa falsa. Las dos líneas falsas son las que dan nombre a OQ-1.
    - `test/check-isolation.test.js:10-17` — **el mismo comentario ya corregido** en la Phase 85, en el fichero hermano. Es el molde exacto de redacción: nombra los ficheros y líneas reales que hacen carga dinámica, y declara el punto ciego como deliberado remitiendo al guard que lo cubre.
    - `test/check-isolation.test.js:51-59` — su reflejo en el JSDoc del walker, que además cita el dato tranquilizador con su verificación.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md` §Code 5 (el texto de la declaración honesta, con la estructura de tres bloques), §Hallazgo 6 (las mediciones y sus **dos correcciones**) y §Runtime State Inventory (la tabla de los 5 sitios que la fase vuelve falsos, con la nota de disciplina para el planner).
    - `src/cli/dashboard/inbox-count.js:1-23` — el bloque de prohibición a corregir. Su premisa ocupa `:9-16` y tiene **dos** mitades falsas: que el store del inbox alcanza el paquete de color a través del módulo de color, y que el guard de este fichero no lo detectaría por mirar solo imports de primer nivel. El resto del bloque (`:17-22`) es correcto y se conserva.
    - `test/dashboard-inbox-count.test.js:1-14` — la cabecera a corregir. Su premisa ocupa `:5-11`, con la misma doble falsedad.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-01-SUMMARY.md` — para tomar de ahí el conteo de tests con el que se compara, y para no repetir mediciones que el plan hermano ya registró.
  </read_first>

  <action>
Tres bloques de trabajo. El primero es el que da nombre al requisito; los otros dos impiden que la fase cierre reabriendo su propio pecado en otros ficheros.

**1) La declaración honesta (ISO-04 / D-12).** Reescribir el bloque de comentario que hoy ocupa `:10-16`, conservando verbatim sus tres primeras líneas explicativas (por qué hay dos regex) y sustituyendo la línea de premisa falsa por un bloque de **tres partes**, en el registro que las Phases 85 y 86 ya establecieron:

   - **Por qué se retira.** Que hasta esta fase esas dos líneas declaraban un punto ciego apoyándose en una afirmación sobre el uso de la carga dinámica en el repo que es **falsa**, y nombrar las pruebas: `src/providers/registry.js:27,28,57,58` y `src/session/state.js:247` hacen carga dinámica desde antes de que ese comentario se escribiera. **Describir la afirmación retirada por su contenido, jamás citarla literalmente**: citarla la reintroduce en el fichero y el criterio de fuente vuelve a contarla. Un fichero no puede declarar un punto ciego apoyándose en una premisa que no se sostiene.
   - **CUBRE.** Imports estáticos (con binding, sin binding, y re-exports) más `import()` dinámico con specifier **literal** fuera de comentarios, remitiendo al guard de source-grep que el plan `87-01` añadió y a la cabecera de `stripComments`.
   - **NO CUBRE — punto ciego RESIDUAL, nombrado y no cerrado.** `import()` con specifier **computado**: ningún regex lo resuelve sin ejecutar el módulo, y ejecutar módulos dentro de un guard de test es justo lo que D-06 evita.
   - **MEDICIÓN FECHADA, NO GARANTÍA.** Aquí van las cifras, y son cifras que **el ejecutor re-mide en su propia sesión**, no transcritas. El método, fijado para que sea reproducible: recorrer los ficheros `.js` de `src/`, aplicar `stripComments` al fuente, contar las coincidencias de `import(` con specifier entrecomillado (literales) y las coincidencias de `import(` que no lo son (computados), y contar los ficheros distintos con al menos un literal. Escribir el resultado con la **fecha de la sesión**. Valores de referencia: la investigación midió 128 literales en 26 ficheros y 0 computados el 2026-08-05; una re-medición del 2026-08-09 con el método de arriba da **129 literales en 26 ficheros y 0 computados** sobre 98 ficheros. **La divergencia es el argumento, no un problema**: demuestra que la cifra caduca y por eso se escribe fechada. Cerrar el bloque diciendo que es una foto y no una promesa, y que mañana puede aparecer el primer computado sin que este fichero lo vea.
   - **Prohibido** presentar el punto ciego residual como cerrado, mitigado o improbable.

   Reescribir además la línea de premisa falsa del JSDoc de `walkImports` (`:33`) por la versión de `87-RESEARCH.md` §Code 5: que **no** sigue carga dinámica **a propósito** (D-06, precedente Phase 85 / WR-03), porque seguir aristas dinámicas ahí ensancharía la clausura y pondría rojos guards vecinos por motivos espurios; que el punto ciego lo cubre el source-grep sobre esa misma clausura; y que el residual queda declarado en la cabecera del fichero. `:30-32` y `:34` del JSDoc se conservan.

**2) El meta-test de `stripComments` (ISO-04 / D-09/D-10).** Añadir `describe('ISO-04 (Phase 87): stripComments no ciega al guard')` con `it('stripComments recupera los 4 imports estáticos de src/cli/dashboard/markdown.js')`: aplicar `stripComments` al fuente de ese fichero, pasar el resultado por `extractImports` y asertar que la longitud es **4** (medido hoy: 4 imports estáticos, y el movimiento del plan `87-01` cambia el specifier de uno de ellos, no su número). El comentario explica que este fichero es a la vez uno de los tres que el orden verbatim ciega **y** un leaker primario de esta fase, así que el caso ata el helper corregido al sujeto concreto: si alguien «arregla» la divergencia devolviendo el orden del molde, este número cae a 0 y la suite lo dice en vez de quedarse verde y ciega. Es el guard del guard.

**3) Los tres comentarios de zona gris — DECISIÓN: entran en alcance.** `87-RESEARCH.md` §Open Questions 2 dejó abierto si corregirlos aquí o diferirlos. Se corrigen **en esta fase**, y la razón se escribe en el commit: son tres ediciones de comentario, cero comportamiento, en dos ficheros que ningún otro plan de la fase toca; el coste de scope es despreciable frente al de cerrar una fase cuyo objeto es retirar una premisa falsa **dejando tres premisas falsas recién creadas** en el mismo repo. Diferirlas exigiría entrada en `deferred-items.md` con trigger, lo que cuesta más que arreglarlas.

   - `src/cli/dashboard/inbox-count.js:9-16` — reescribir **la premisa, no la conclusión**. La prohibición de importar el store del inbox **se conserva** y sigue siendo correcta: arrastraría `withFileLock` y `resolveProjectId` a un módulo que solo tiene que contar líneas. Lo que se corrige es su argumento principal: el store del inbox **ya no** alcanza el paquete de color (su única vía era el módulo de color, y la Phase 87 la cortó — efecto colateral verificado en `87-RESEARCH.md` §Hallazgo 7), y el guard de `test/format-isolation.test.js` **ya no** se limita a los imports de primer nivel. Dejar constancia de que la prohibición sobrevive a la evaporación de su premisa original, con sus motivos restantes nombrados — no borrarla ni debilitarla.
   - `test/dashboard-inbox-count.test.js:5-11` — misma corrección en la cabecera, que hoy repite la doble falsedad. Lo que se conserva íntegro es el argumento de fondo del fichero: el aislamiento cuesta una duplicación de la gramática de la línea y este fichero es la contrapartida que impide la deriva.
   - Ningún assert de esos dos ficheros cambia. Son ediciones de comentario.

**4) Cierre.** Ejecutar `npm test` y comprobar `# fail 0`. Registrar en el SUMMARY el comando de re-medición y su salida literal, y la cita de la sección de la cabecera reescrita para la revisión de honestidad del `VERIFICATION`.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    Valores de HEAD medidos en vivo contra `61a5c95`.

    - `grep -c "el repo no lo usa\|el repo no los usa" test/format-isolation.test.js` devuelve `0`. **Muerde** — HEAD: `2`. Cuenta las dos líneas de `:14` y `:33`; el criterio incluye la variante en plural porque las dos líneas la redactan distinto.
    - `grep -c 'NO CUBRE' test/format-isolation.test.js` es ≥ 1 y `grep -c 'CUBRE' test/format-isolation.test.js` es ≥ 2 (el bloque afirmativo y el negativo). **Muerde** — HEAD: `0`.
    - La cabecera contiene una fecha con forma `2026-MM-DD` y, junto a ella, el conteo de literales, el de ficheros y el de computados que el ejecutor re-midió. `grep -cE '2026-[0-9]{2}-[0-9]{2}' test/format-isolation.test.js` ≥ 1. **Muerde** — HEAD: `0`.
    - La cabecera nombra `src/providers/registry.js` y `src/session/state.js` como las pruebas de que la premisa retirada era falsa. `grep -c 'providers/registry.js' test/format-isolation.test.js` ≥ 1. **Muerde** — HEAD: `0`.
    - `node --test test/format-isolation.test.js` da `# fail 0` y su `# tests` crece en **1** respecto del valor tras la Tarea 1 (el meta-test). **Muerde.**
    - El meta-test asevera literalmente el valor `4` sobre `src/cli/dashboard/markdown.js`. **Muerde** — HEAD: el caso no existe; el conteo real de imports estáticos de ese fichero es `4`, medido en vivo.
    - `grep -c 'imports DIRECTOS' src/cli/dashboard/inbox-count.js` devuelve `0` y `grep -c 'imports DIRECTOS' test/dashboard-inbox-count.test.js` devuelve `0`. **Muerde** — HEAD: `2` y `1` respectivamente.
    - La prohibición **se conserva**: `grep -c 'PROHIBIDO importar' src/cli/dashboard/inbox-count.js` es ≥ 2 (el bloque del store y el de la config). **Anti-regresión** — HEAD: ya se cumple. Existe para que «corregir la premisa» no se convierta en «borrar la prohibición».
    - `git diff -U0 -- src/cli/dashboard/inbox-count.js test/dashboard-inbox-count.test.js` toca **solo** líneas de comentario: ninguna línea del diff añade o quita código ejecutable. **Muerde** — es el gate de que la corrección es doc-only.
    - `node --test test/dashboard-inbox-count.test.js` da `# tests 14 · # fail 0`. **Anti-regresión** — HEAD: 14/0.
    - `npm test` sale con `# fail 0` y `# tests` ≥ 2589. **Anti-regresión** respecto del estado que dejó el plan `87-01`; existe para que esta tarea no rompa nada.
    - `git diff --exit-code -- test/check-isolation.test.js test/dispatcher-isolation.test.js src/cli/dashboard/format.js src/cli/dashboard/select.js src/cli/dashboard/SessionTable.js src/cli/dashboard/index.js` sale con código 0. **Anti-regresión** — gate combinado del diferido de `stripComments`, de D-13/D-14 (congelar no es modificar) y de D-04.
    - El SUMMARY contiene el comando de re-medición y su salida literal, y la cita de la sección de la cabecera reescrita. **Muerde.**
  </acceptance_criteria>

  <done>
    La cabecera de `test/format-isolation.test.js` declara con honestidad qué cubre, nombra el punto ciego residual sin venderlo como cerrado y fecha una medición re-tomada en la sesión; el JSDoc del walker dice por qué no sigue aristas dinámicas en vez de negar que existan; el meta-test ata `stripComments` al fichero que el orden verbatim cegaría; y los tres comentarios que ISO-01/ISO-02 vuelven falsos quedan corregidos conservando íntegra la prohibición que argumentaban. `npm test` verde.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artefactos que produce esta fase

Inventario completo de la fase; los de este plan van marcados `[87-02]` y los del plan hermano `[87-01]`. La tabla es la misma en los dos documentos a propósito, para que el inventario esté completo en cualquiera de ellos.

| Artefacto | Tipo | Dónde vive | Plan |
|-----------|------|-----------|------|
| `src/cli/sanitize.js` | **fichero nuevo** — módulo-contrato hoja de cero imports | `src/cli/` | `[87-01]` |
| `stripControlChars(s) → string` | función exportada (movida verbatim de `format.js:80-87`) | `src/cli/sanitize.js` | `[87-01]` |
| `stripForKeystroke(s) → string` | función exportada (movida verbatim de `format.js:114-123`) | `src/cli/sanitize.js` | `[87-01]` |
| `stripComments(src) → string` | helper de test con el **orden corregido** (D-10), duplicado a propósito | `test/format-isolation.test.js` | `[87-01]` |
| `DYNAMIC_PICOCOLORS_RE` | regex constante anclada al patrón `import(...)` con specifier literal | `test/format-isolation.test.js` | `[87-01]` |
| `importsPicocolors(file) → boolean` | predicado de import directo | `test/format-isolation.test.js` | `[87-01]` |
| `findChainToPicocolors(entry) → string[]\|null` | BFS con mapa de padres; cadena MÁS CORTA; solo en el camino de fallo | `test/format-isolation.test.js` | `[87-01]` |
| suite `ISO-01 (Phase 87): cero picocolors TRANSITIVO bajo src/cli/dashboard/` | `describe` nuevo | `test/format-isolation.test.js` | `[87-01]` |
| caso `ningún fichero del TUI alcanza picocolors por ninguna cadena de imports estáticos` | `it` | `test/format-isolation.test.js` | `[87-01]` |
| caso `ningún fichero del grafo del TUI hace import() DINÁMICO de picocolors` | `it` | `test/format-isolation.test.js` | `[87-01]` |
| suite `ISO-02 (Phase 87): src/cli/sanitize.js es una HOJA de cero imports` | `describe` nuevo | `test/format-isolation.test.js` | `[87-01]` |
| caso `src/cli/sanitize.js existe y tiene cero imports (incluidos builtins)` | `it` | `test/format-isolation.test.js` | `[87-01]` |
| `ALLOWED_BUILTINS` | constante congelada de un elemento (`node:path`), la única allowlist de la fase (D-13) | `test/format-isolation.test.js` | `[87-02]` |
| suite `ISO-03 (Phase 87 / UF-02): src/cli/dashboard/format.js es una HOJA pura` | `describe` nuevo | `test/format-isolation.test.js` | `[87-02]` |
| caso `cero imports relativos; builtins solo los de la allowlist; clausura de 1` | `it` | `test/format-isolation.test.js` | `[87-02]` |
| caso `select.js consume ./format.js (convergencia, D-14)` | `it` | `test/format-isolation.test.js` | `[87-02]` |
| suite `ISO-04 (Phase 87): stripComments no ciega al guard` | `describe` nuevo (meta-test) | `test/format-isolation.test.js` | `[87-02]` |
| caso `stripComments recupera los 4 imports estáticos de src/cli/dashboard/markdown.js` | `it` | `test/format-isolation.test.js` | `[87-02]` |
| Bloque **declaración honesta** (CUBRE / NO CUBRE — punto ciego residual / MEDICIÓN FECHADA, NO GARANTÍA) | cabecera reescrita, sustituye a `:10-16` | `test/format-isolation.test.js` | `[87-02]` |
| Línea reescrita del JSDoc de `walkImports` sobre por qué no sigue aristas dinámicas | comentario | `test/format-isolation.test.js` | `[87-02]` |
</artifacts_produced>

<flagged_assumptions>
## Supuestos marcados — sondas de requisito sin resolver

La tabla completa de los cuatro supuestos de la sonda spec-less está en `87-01-PLAN.md` §flagged_assumptions y **no se repite aquí para no duplicar el recuento**: la igualdad de no-descarte-silencioso es de fase, 4 items surgidos == 4 marcados, 0 autorizados a `must_haves` desde la sonda.

Lo que corresponde a este plan de aquella tabla: **ISO-03** e **ISO-04**, ambos `unclassified` / `unresolved`. Su cobertura no viene de la sonda sino de `87-VALIDATION.md` §Per-Task Verification Map, cuyas filas se levantaron a `must_haves.truths` con su comando automatizado; y la revisión de honestidad de la cabecera va como **backstop**, no como truth verificable, porque ningún assert puede comprobar que un texto es honesto.

**Supuesto adicional propio de este plan, declarado:** las cifras de la declaración honesta se re-miden en la sesión de ejecución. La investigación midió 128 literales en 26 ficheros el 2026-08-05 y una re-medición del 2026-08-09 da 129 en 26. La divergencia en cuatro días es exactamente por qué la cifra se escribe **fechada** y por qué el plan prohíbe transcribirla: una cifra heredada sin re-medir sería otra premisa no verificada en el mismo comentario que retira una.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fuente del repo → regex y walker de los guards | Los guards leen ficheros arbitrarios del propio árbol. Las regex son constantes y nunca se compilan desde input. |
| `src/cli/dashboard/format.js` → `node:path` | Única arista admitida por la allowlist de D-13. Builtin sin efectos de módulo; clausura medida = 1. |
| Comentario del repo → lector humano futuro | Frontera de **veracidad**, no de datos. Un comentario que declara una garantía que no tiene induce a no verificar; es la frontera que ISO-04 repara. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-07 | Repudiation | declaración honesta con cifras heredadas | medium | mitigate | El ejecutor **re-mide** en su sesión con el método fijado y escribe su propio resultado fechado; el SUMMARY registra comando y salida literal. La divergencia 128→129 en cuatro días está citada como prueba de que la transcripción no vale (Tarea 2). |
| T-87-08 | Tampering | guard de pureza degradado a decoración | medium | mitigate | Aserto positivo de convergencia D-14: sin consumidor, el guard de prohibición es verde y vacío. Más el assert de alcanzabilidad (clausura = 1) junto a los dos de forma (Tarea 1). |
| T-87-09 | Tampering | allowlist de builtins usada como vía de escape | medium | mitigate | Congelada a **un** elemento, declarada literalmente y jamás derivada del sujeto; escrita con su medición. Es la única allowlist admitida en toda la fase (D-13/D-16, Tarea 1). |
| T-87-10 | Tampering | helper `stripComments` revertido al orden con bug | high | mitigate | Meta-test que asevera el conteo de imports recuperados sobre el fichero concreto que el orden verbatim ciega, más el bloque de comentario con la medición encima del helper (Tarea 2). Sin el meta-test, la reversión pasaría verde. |
| T-87-11 | Repudiation | prohibición borrada al corregir su premisa | medium | mitigate | Criterio de conteo de las prohibiciones que sobreviven, más el gate de que el diff de esos dos ficheros toca solo líneas de comentario (Tarea 2). |
| T-87-06 | Information disclosure | mensajes de fallo de los guards | low | accept | Imprimen paths relativos del propio repo, ya públicos en el árbol. Sin secreto que redactar. |
| T-87-SC | Tampering | instalaciones npm/pip/cargo | low | accept | **Cero instalaciones en esta fase** (`87-RESEARCH.md` §Package Legitimacy Audit: no aplica; `[SLOP]` 0, `[SUS]` 0). Invariante cross-milestone de cero dependencias npm nuevas, aseverado por `git diff --exit-code -- package.json package-lock.json`. No procede checkpoint de legitimidad porque no hay paquete que verificar. |
</threat_model>

<verification>
**Baselines medidos en vivo contra `61a5c95` al planificar.** `src/cli/dashboard/format.js` = 1 import (`node:path`, `:25`) · `src/cli/dashboard/select.js:35` importa `./format.js` · `src/cli/dashboard/markdown.js` = 4 imports estáticos · premisa falsa en `test/format-isolation.test.js` = 2 coincidencias · premisa falsa en `src/cli/dashboard/inbox-count.js` = 2 y en `test/dashboard-inbox-count.test.js` = 1 · `test/dashboard-inbox-count.test.js` = 14 tests · `test/check-isolation.test.js` = 12 tests. Re-medición de carga dinámica sobre `src/` el 2026-08-09: 98 ficheros, 129 literales en 26 ficheros, 0 computados.

1. `node --test test/format-isolation.test.js` → `# fail 0`, con **3** tests más que el valor registrado en `87-01-SUMMARY.md` (2 de la Tarea 1 + 1 de la Tarea 2).
2. `grep -c "el repo no lo usa\|el repo no los usa" test/format-isolation.test.js` → `0`.
3. `grep -c 'imports DIRECTOS' src/cli/dashboard/inbox-count.js test/dashboard-inbox-count.test.js` → `0` en los dos.
4. `grep -c 'PROHIBIDO importar' src/cli/dashboard/inbox-count.js` → ≥ 2 (la prohibición sobrevive a la corrección de su premisa).
5. `git diff -U0` sobre los dos ficheros de zona gris toca solo líneas de comentario.
6. `git diff --exit-code -- src/cli/dashboard/format.js src/cli/dashboard/select.js src/cli/dashboard/SessionTable.js src/cli/dashboard/index.js test/check-isolation.test.js test/dispatcher-isolation.test.js package.json package-lock.json` → código 0.
7. `npm test` → `# fail 0`, `# tests` ≥ 2589.
8. El SUMMARY registra el comando de re-medición con su salida literal y la cita de la sección de la cabecera reescrita, para la revisión de honestidad del `VERIFICATION`.
</verification>

<success_criteria>
- La pureza de `src/cli/dashboard/format.js` queda congelada por test —cero imports relativos, builtins dentro de una allowlist de un elemento con su razón medida, clausura transitiva exactamente 1— y una regresión que la rompa falla (ISO-03 / UF-02, criterio 3 del ROADMAP).
- `select.js` sigue consumiendo `./format.js` por aserto positivo, de modo que ISO-03 no puede degradar a guard sobre un módulo huérfano (D-14).
- `test/format-isolation.test.js` no declara ningún punto ciego en falso: la premisa retirada no aparece ni citada, y en su lugar hay una declaración que enumera lo cubierto, nombra el residual sin venderlo como cerrado y fecha una medición re-tomada en la sesión (ISO-04, criterio 4).
- El helper `stripComments` corregido queda atado por un meta-test al fichero que el orden del molde cegaría: revertirlo al orden con bug es un fallo de suite, no un silencio.
- Los tres comentarios que ISO-01/ISO-02 vuelven falsos quedan corregidos, conservando íntegras las prohibiciones que argumentaban: la fase no cierra dejando premisas falsas recién creadas.
- Ningún assert se debilitó y la única allowlist de toda la fase es la de D-13, justificada por medición (DEBT-04 LOCKED, D-16).
</success_criteria>

<output>
Crear `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-02-SUMMARY.md` al terminar, incluyendo obligatoriamente: el comando de re-medición de carga dinámica con su salida literal y la fecha escrita en la cabecera; la cita de la sección de la declaración honesta para la revisión de honestidad; el conteo de tests del fichero de guard antes y después; y el párrafo que justifica por qué los tres comentarios de zona gris entraron en alcance en vez de diferirse.
</output>
