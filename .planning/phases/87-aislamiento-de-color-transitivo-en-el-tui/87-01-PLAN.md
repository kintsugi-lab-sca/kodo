---
phase: 87-aislamiento-de-color-transitivo-en-el-tui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/format-isolation.test.js
  - src/cli/sanitize.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/markdown.js
  - src/cli/inbox.js
  - src/cli/capture.js
  - src/hooks/stop.js
  - src/inbox/store.js
  - src/session/manager.js
  - test/dashboard-format.test.js
  - test/manager.test.js
autonomous: true
requirements: [ISO-01, ISO-02, ISO-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ningún fichero de `src/cli/dashboard/` alcanza `picocolors` por ninguna cadena de imports estáticos: el guard itera los 16 ficheros del TUI como entry point y su lista de cadenas es vacía (ISO-01 / ISO-02, D-05). Comando: `node --test test/format-isolation.test.js`."
    - "El mensaje de fallo del guard transitivo imprime la **cadena** de imports (fichero del TUI → … → importador de `picocolors`), no el conjunto del grafo (ISO-01, D-07). Evidencia: la salida roja capturada en la Tarea 1 contiene `src/cli/dashboard/markdown.js` seguido de `src/cli/format.js` en la misma cadena."
    - "Un `import()` **dinámico** de `picocolors` en cualquier fichero de la unión de las clausuras del TUI pone el guard rojo, con `stripComments` de orden corregido aplicado antes del match y anclaje al patrón de import (ISO-01 / ISO-04, D-06/D-10/D-11). Comando: `node --test test/format-isolation.test.js`."
    - "Los saneadores puros de texto viven en `src/cli/sanitize.js`, una hoja de **cero imports** (ni relativos ni builtins), y ningún fichero los importa ya desde el módulo de color (ISO-02, D-01/D-02). Comando: `grep -rnE \"^import .*(stripControlChars|stripForKeystroke)\" src test | grep -c \"cli/format\"` → `0` (HEAD `61a5c95`: 8)."
    - "El movimiento de los dos saneadores es **byte-idéntico**: el bloque copiado coincide carácter a carácter con las líneas 60-123 de `src/cli/format.js` en `61a5c95` (ISO-02, D-17 / Pitfall 7). Comando: `diff <(git show 61a5c95:src/cli/format.js | sed -n '60,123p') <(sed -n '<rango>p' src/cli/sanitize.js)` → salida vacía."
    - "Cero regresión de comportamiento (criterio 5 del ROADMAP): los cinco ficheros que D-17 nombra pasan **sin tocarse** con los conteos idénticos al baseline medido en `61a5c95` — `test/dashboard-format.test.js` 58, `test/dashboard-markdown.test.js` 9, `test/format.test.js` 44, `test/stop.test.js` 33, `test/inbox-format-golden.test.js` 22 — y ningún golden se actualiza. Comando: `node --test` sobre los cinco."
    - "Los 8 tests y 5 suites que `test/format-isolation.test.js` ya tenía en `61a5c95` (D-18) siguen verdes: el guard endurecido es **aditivo** y el test directo TUI-04 (`:200-221`) queda intacto (D-08). Comando: `node --test test/format-isolation.test.js` con `# fail 0` y `# tests` > 8."
    - "Los dos guards source-grep de `test/manager.test.js` siguen exigiendo import canónico y saneo de los mismos tres campos; solo cambia cuál es el carril canónico. Los asserts de interpolación y los negativos de regresión del mismo bloque quedan **intactos** (D-16 / RESEARCH §A3). Comando: `node --test test/manager.test.js` con `# tests 62` y `# fail 0`."
    - "La suite completa queda verde tras el movimiento: `npm test` con `# fail 0` y `# tests` ≥ 2589 (baseline de la simulación de `87-RESEARCH.md` §Hallazgo 4)."
    - statement: "La mordida está registrada como evidencia citada en el SUMMARY: mordida A (reintroducir el import estático de `markdown.js:27`) pone el guard transitivo ROJO con sus cadenas, y mordida B (un `import()` dinámico de `picocolors` en ese mismo fichero) pone ROJO el guard dinámico; revertidas ambas, verde. Se cita diff exacto, mensaje de fallo y conteo (D-15, manual-only: D-15 prohíbe infraestructura de mutation testing)."
      verification: backstop
  prohibitions:
    - "(sonda §B — integridad del refactor) NO alterar, «limpiar», reordenar ni re-derivar los cuerpos ni las regex de `stripControlChars` / `stripForKeystroke` al moverlos. El movimiento es byte-idéntico o no es el movimiento que se decidió: esas regex tienen tres fases afinadas en las Phases 72 y 78 (WR-02) y sanean contenido de un LLM y de un proveedor externo antes del terminal y del carril de keystroke."
    - "(sonda §B — integridad de los guards) NO poner verde ningún guard debilitándolo: ni un assert menos, ni un umbral movido, ni una allowlist añadida «para que pase», ni un golden actualizado, ni un shim de re-export. Un guard rojo se cierra cerrando el leak (DEBT-04 LOCKED, D-16 / D-02)."
    - "NO añadir `export { stripControlChars } from './sanitize.js'` ni ninguna otra forma de re-export en `src/cli/format.js` (D-02). Los 8 call sites se actualizan; el atajo no existe."
    - "NO mover `visibleWidth` ni ninguna otra función de `src/cli/format.js` (D-03): cero consumidores fuera del propio fichero, medido."
    - "NO tocar `src/cli/dashboard/SessionTable.js` ni `src/cli/dashboard/index.js` (D-04). Son leakers derivativos; si alguno sigue rojo tras el fix, se **mide** antes de tocar nada."
    - "NO meter `stripComments` —ni el corregido— dentro de `walkImports` (Pitfall 4). Medido: es un no-op hoy con el corregido y una catástrofe con el verbatim (la clausura de `markdown.js` cae de 3 a 1)."
    - "NO copiar `stripComments` verbatim de `test/check-isolation.test.js:23-29` ni de `test/dispatcher-isolation.test.js:24-30` (D-09): su orden ciega el guard justo sobre `markdown.js`."
    - "NO corregir `stripComments` en `test/check-isolation.test.js` ni en `test/dispatcher-isolation.test.js`: está diferido con trigger escrito y esta fase solo endurece `test/format-isolation.test.js`."
    - "NO extraer el walker ni `stripComments` a un helper de test compartido: se duplica **con la razón escrita** (RESEARCH §OQ-1); un helper compartido obligaría a tocar los tres ficheros de guard, que es justo lo que el diferido evita."
    - "NO añadir ninguna dependencia npm (parser AST incluido) — invariante cross-milestone de `.planning/STATE.md`."
    - "NO construir infraestructura de mutation testing (D-15): la mordida es manual y su evidencia va citada."
  artifacts:
    - "`src/cli/sanitize.js` — módulo NUEVO, hoja de cero imports. Exporta `stripControlChars(s)` y `stripForKeystroke(s)`, copiados verbatim del bloque contiguo `src/cli/format.js:60-123` de `61a5c95`."
    - "`test/format-isolation.test.js` — `stripComments(src)`: helper de filtrado de comentarios con el **orden corregido** (líneas `//` → bloques `/* */` → líneas `*`), con el bloque de medición encima."
    - "`test/format-isolation.test.js` — `DYNAMIC_PICOCOLORS_RE`: regex constante anclada al patrón `import(...)` con specifier literal."
    - "`test/format-isolation.test.js` — `importsPicocolors(file)`: predicado de import directo sobre un fichero."
    - "`test/format-isolation.test.js` — `findChainToPicocolors(entry)`: BFS con mapa de padres que devuelve la cadena MÁS CORTA; aditivo, no toca `walkImports`."
    - "`test/format-isolation.test.js` — suite `ISO-01 (Phase 87): cero picocolors TRANSITIVO bajo src/cli/dashboard/` con dos casos: `ningún fichero del TUI alcanza picocolors por ninguna cadena de imports estáticos` y `ningún fichero del grafo del TUI hace import() DINÁMICO de picocolors`."
    - "`test/format-isolation.test.js` — suite `ISO-02 (Phase 87): src/cli/sanitize.js es una HOJA de cero imports` con el caso `src/cli/sanitize.js existe y tiene cero imports (incluidos builtins)`."
  key_links:
    - "`listJsFiles(SRC)` filtrado a `/cli/dashboard/` → `walkImports(file)` por CADA fichero como entry → `importsPicocolars`… → `findChainToPicocolors(file)` solo en el camino de fallo: iterar los 16 como entry es lo que hace innecesario seguir aristas dinámicas dentro del walker (D-05). El leak de `index.js` queda cubierto porque `App.js` también es entry."
    - "unión de las 16 clausuras → `stripComments(readFileSync(file))` → `DYNAMIC_PICOCOLORS_RE`: el saneo de comentarios va ANTES del match y solo en este carril, nunca dentro de `walkImports` (D-06, Pitfall 4)."
    - "`src/cli/format.js:60-123` → `src/cli/sanitize.js`: corte de un bloque contiguo. Cortar por rangos separados (`:80-97` + `:114-125`, los que cita D-01) partiría el JSDoc y rompería la byte-identidad."
    - "`src/cli/inbox.js:36` y `src/cli/capture.js:38` → DOS imports: `createFormatter` se queda en `./format.js`, el saneador pasa a `./sanitize.js`. Es la única forma del lote que no es sustitución de path."
    - "`src/inbox/store.js:46`, `src/hooks/stop.js:16`, `src/cli/capture.js:38` → `src/cli/sanitize.js`: sin shim (D-02), no actualizarlos es un fallo en **tiempo de carga** en tres carriles vivos (escritura del inbox, nudge de keystroke, captura CLI), no una degradación silenciosa."
    - "`test/manager.test.js` regex `\\.\\.\\/cli\\/format\\.js` → `\\.\\.\\/cli\\/sanitize\\.js`: el assert conserva TODAS sus condiciones; solo cambia el carril canónico al que se ancla."
---

<objective>
Cerrar de punta a punta el carril completo del aislamiento de color transitivo: **detectar** el leak (guard endurecido que ve la clausura, no el import directo, y que imprime la cadena), **cortarlo** (los dos saneadores puros salen de `src/cli/format.js` a una hoja nueva de cero imports) y **probar que el detector muerde** (reintroducción manual del leak, en sus dos variantes, con evidencia citada).

Este plan es el **tracer** de la fase: la rebanada vertical más fina que atraviesa todas las capas que la fase toca —helper de test, walker existente, aserción, mensaje de fallo, módulo de producción nuevo, 8 call sites y los guards hermanos que se anclaban al carril viejo—, de calidad final y con `npm test` verde al terminar. La pureza de `src/cli/dashboard/format.js` (ISO-03) y la declaración honesta de la cabecera (ISO-04) se expanden desde aquí en el plan `87-02`.

Purpose: hoy el guard de `test/format-isolation.test.js:200-221` mira **imports directos** y sale verde mientras tres ficheros del TUI alcanzan `picocolors` por vía transitiva; el walker que lo detectaría lleva 150 líneas más arriba en ese mismo fichero, sin usar. Una invariante que no se puede medir no es una invariante.
Output: `test/format-isolation.test.js` con el guard transitivo, el guard dinámico y `findChainToPicocolors`; `src/cli/sanitize.js` con los dos saneadores byte a byte; los 8 call sites re-apuntados; los 2 asserts source-grep de `test/manager.test.js` re-anclados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-CONTEXT.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-PATTERNS.md
@.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-VALIDATION.md
</context>

<!-- planner-discipline-allow: cli/format -->

<tasks>

<task type="tracer" tdd="true">
  <name>Tarea 1: El guard que ve la cadena — transitivo + dinámico, escrito y ROJO</name>

  <files>test/format-isolation.test.js</files>

  <read_first>
    - `test/format-isolation.test.js` **íntegro** (221 líneas) — el fichero que se modifica. Hay que tener en la cabeza antes de escribir nada: las dos regex de import (`:15-16`), `extractImports` (`:23-28`), `walkImports` (`:40-52`, **se reutiliza tal cual, leyendo el fuente crudo**), `listJsFiles` (`:59-71`), el guard de single-source de `picocolors` (`:98-129`, que sigue verde y no se toca) y el guard **directo** TUI-04 (`:200-221`), que se conserva intacto (D-08). Los comentarios de `:14` y `:33` **no se tocan en este plan** — son del plan `87-02`.
    - `test/check-isolation.test.js:192-228` — el precedente **locked** (Phase 85 D-09 / WR-03) del source-grep de `import()` dinámico sobre la lista que el walker devuelve, **con su justificación escrita** de por qué no se mete en el walker. Es la fuente primaria de D-06 y el molde del comentario que hay que adaptar de `logger` a `picocolors`.
    - `test/check-isolation.test.js:23-29` — `stripComments`. **ANTI-ANÁLOGO**: se lee para entender la forma, jamás para copiar el orden (D-09). Su origen con el mismo bug está en `test/dispatcher-isolation.test.js:24-30`.
    - `test/check-isolation.test.js:31-34` — el molde de las regex **constantes** (anti-ReDoS: jamás compiladas desde input) y su comentario.
    - `test/check-isolation.test.js:241-258` (`handoff.js`) y `:269-285` (`pending.js`) — el molde de redacción del guard de **hoja de cero imports**, incluido el assert anti-vacuidad `existsSync(...)` con el mensaje «otherwise this isolation test passes trivially».
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md` §Code Examples 1, 2 y 3 — el código completo de `stripComments` corregido, de `findChainToPicocolors` y de la suite ISO-01, **ya ejecutado en las dos direcciones** contra el árbol real. §Hallazgo 5 (evidencia del bug de orden) y §Common Pitfalls 3, 4, 5 y 6.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-PATTERNS.md` §guard transitivo ISO-01, §source-grep dinámico, §`stripComments` (ANTI-ANÁLOGO) y §Patrones compartidos 1-5.
    - `src/cli/dashboard/markdown.js:11-27` y `src/cli/dashboard/enrich.js:20-30` — los comentarios de línea cuya glob abre el bloque falso que ciega al helper verbatim. Es la evidencia que el comentario del helper corregido debe citar.
  </read_first>

  <behavior>
    Este es el paso RED del tracer: los guards se escriben **antes** que el fix y se comprueba que salen rojos contra `61a5c95`. Ese rojo, con sus cadenas impresas, es la primera evidencia de mordida y se transcribe al SUMMARY.

    - Test 1 — `ningún fichero del TUI alcanza picocolors por ninguna cadena de imports estáticos`: contra `61a5c95` debe salir **ROJO** listando exactamente **tres** cadenas: `src/cli/dashboard/App.js → src/cli/format.js`, `src/cli/dashboard/SessionTable.js → src/cli/dashboard/markdown.js → src/cli/format.js` y `src/cli/dashboard/markdown.js → src/cli/format.js`. Tres, no cuatro: `index.js` no aparece porque su arista a `App.js` es dinámica y el walker es estático a propósito (D-06); queda cubierto porque `App.js` es entry por sí mismo (D-05).
    - Test 2 — `ningún fichero del grafo del TUI hace import() DINÁMICO de picocolors`: contra `61a5c95` sale **VERDE** por construcción (medido: 0 `import()` dinámicos de `picocolors` en `src/`). Su mordida no la da este paso sino la mordida B de la Tarea 3; el test existe porque sin él la cobertura de aristas dinámicas sería una afirmación sin assert.
    - Test 3 — `src/cli/sanitize.js existe y tiene cero imports (incluidos builtins)`: contra `61a5c95` sale **ROJO** en el assert anti-vacuidad, porque el fichero aún no existe. Lo crea la Tarea 2.
    - Los 8 casos previos del fichero siguen verdes en el paso RED: los guards nuevos son aditivos y ninguno de los existentes se toca.
  </behavior>

  <action>
Escribir en `test/format-isolation.test.js` los cuatro elementos nuevos, en este orden, sin modificar una sola línea del código ya existente en el fichero.

**1) `stripComments(src)` con el orden corregido (D-09/D-10).** Función auxiliar de módulo, colocada junto a `extractImports`. El orden es: filtrar las líneas cuyo `trim()` empieza por dos barras → **después** borrar los bloques `/* … */` con una sustitución global → **después** filtrar las líneas cuyo `trim()` empieza por asterisco. El código exacto está en `87-RESEARCH.md` §Code 1 y ya se ejecutó. **Encima de la función va un bloque de comentario obligatorio** (Pitfall 3) que diga: que diverge a propósito del helper de `test/check-isolation.test.js:23-29` y de su origen `test/dispatcher-isolation.test.js:24-30`; que aquéllos borran los bloques antes de filtrar las líneas de comentario, de modo que una línea de comentario que contenga la secuencia de apertura de bloque abre un bloque falso que se traga el fichero hasta el siguiente cierre; que medido sobre los 98 ficheros de `src/` el 2026-08-05, **tres** ficheros pierden el **100 %** de sus imports estáticos con el orden verbatim —`src/cli/dashboard/enrich.js` (3→0), `src/cli/dashboard/markdown.js` (4→0) y `src/logs/session-lookup.js` (5→0)—, con sus disparadores citados por línea (`markdown.js:14`, `enrich.js:26`, `session-lookup.js:14`); y que el argumento decisivo es que `markdown.js` es a la vez uno de los tres cegados **y** un leaker primario de esta fase: con un `import()` de `picocolors` inyectado ahí, el verbatim da **0 hits (guard ciego)** y el corregido **1 (rojo)**. Sin ese bloque, un lector futuro «arregla» la divergencia devolviendo el bug.

**2) `DYNAMIC_PICOCOLORS_RE` (D-11, Pitfall 6).** Constante de módulo junto a las dos regex ya existentes, con el comentario del molde de `test/check-isolation.test.js:31-34` sobre que las regex son constantes y jamás se compilan desde input. Debe casar la llamada `import(...)` con specifier **literal** entre comillas que contenga `picocolors`, capturando el specifier en el grupo 1, con flag global. El anclaje es al **patrón de import**, nunca al identificador suelto: hay prosa que nombra el paquete en `src/cli/dashboard/format.js:17`, `src/cli/dashboard/markdown.js:13` y `src/cli/dashboard/inbox-count.js:21`, y esa prosa no puede poner roja la suite. Texto exacto en `87-RESEARCH.md` §Code 2.

**2 bis) El filtro de línea de D-11, implementado además de `stripComments`.** D-11 exige que el match del `import()` dinámico descarte, además de los specifiers no literales, las líneas cuyo `trim()` empiece por dos barras, por asterisco o por barra-asterisco. Se implementa: es un `.filter()` sobre las líneas del fuente ya saneado, antes del `matchAll`. Va acompañado de un comentario que fija la relación entre los dos mecanismos, medida el 2026-08-05: **los dos dan exactamente el mismo resultado, 128 coincidencias**, así que quien hace el trabajo es `stripComments`; el filtro de línea se implementa para satisfacer D-11 **literalmente** y porque cuesta una línea. El comentario debe dejar escrito que `stripComments` es **estrictamente más fuerte** —también neutraliza un bloque `/* … */` multilínea cuyas líneas interiores no empiecen por ninguno de esos tres prefijos, que el filtro de línea dejaría pasar como arista real—, para que un lector futuro no concluya que el filtro de línea basta por sí solo y elimine el saneo de comentarios. La intención de D-11 —que los imports de **tipo** no cuentan como aristas— queda satisfecha por ambos.

**3) `importsPicocolors(file)` y `findChainToPicocolors(entry)` (D-07).** `importsPicocolors` es un predicado de una línea: `extractImports` sobre el fuente del fichero incluye el specifier del paquete. `findChainToPicocolors` es un BFS de ~14 líneas con `Map` de padres que devuelve el array de paths relativos al repo desde `entry` hasta el primer fichero que importa el paquete, o `null`. Va **separado** de `walkImports`, que devuelve un `Set` sin información de padres y **no se reescribe** (D-05); se invoca **solo en el camino de fallo**, para construir el mensaje, así que la suite verde no lo paga. BFS y no DFS porque la cadena más corta es la que nombra la arista que hay que cortar. Código completo en `87-RESEARCH.md` §Code 2; su JSDoc debe decir esas dos cosas (aditivo, y solo en fallo).

**4) Las tres suites nuevas.** Todas usan `assert.deepEqual(violadores, [], msg)`, nunca `assert.equal(x.length, 0)`: el mensaje de `deepEqual` sobre un array lista los violadores, que es lo que hace el guard accionable (Patrón compartido 1).

   - `describe('ISO-01 (Phase 87): cero picocolors TRANSITIVO bajo src/cli/dashboard/')`, con su comentario de cabecera explicando que el guard TUI-04 de `:200-221` mira imports directos y salía verde mientras tres ficheros del TUI alcanzaban el paquete por vía transitiva; que el directo se conserva porque es aditivo y su mensaje es más legible cuando el leak es de primer nivel (D-08); y que el ancla es el **paquete**, no `src/cli/format.js`, porque un segundo importador futuro escaparía a un ancla al fichero (D-07).
     - `it('ningún fichero del TUI alcanza picocolors por ninguna cadena de imports estáticos')`: `listJsFiles(SRC)` filtrado por `/cli/dashboard/`; para cada fichero, si algún elemento de `walkImports(file)` importa el paquete, se añade `findChainToPicocolors(file)` unida por un separador visible de flecha a la lista de cadenas. El mensaje de fallo enumera las cadenas y termina con la instrucción accionable: cortar la primera arista de cada cadena porque el saneador de texto vive en `src/cli/sanitize.js`, una hoja sin color. Código en `87-RESEARCH.md` §Code 3.
     - `it('ningún fichero del grafo del TUI hace import() DINÁMICO de picocolors')`: construye la **unión** de las clausuras de los 16 ficheros del dashboard acumulando en un único `Set` pasado como segundo argumento a `walkImports`; recorre esa unión aplicando `stripComments` al fuente **antes** del `matchAll` de `DYNAMIC_PICOCOLORS_RE`; acumula violaciones con la forma `<path relativo> → import('<specifier>')`. Lleva el comentario de justificación adaptado del precedente (`test/check-isolation.test.js:198-207`): por qué es un grep aparte y no una mejora del walker —seguir aristas dinámicas dentro del walker ensancha la clausura y pone rojos guards vecinos por motivos espurios, y la reacción natural a un rojo espurio es debilitarlos—, y por qué `stripComments` es obligatorio y va antes del match: sin él se cuelan aristas fantasma de anotaciones de tipo, que son imports borrados en runtime, no aristas. Cita la medición fechada del 2026-08-05: 139 coincidencias que bajan a **128** al descartar las de una sola línea de bloque de documentación, **11** fantasmas de los cuales **9** con specifier relativo, entre ellos `src/cli/polling.js:417 → '../logger.js'`, que apunta justo al módulo que el guard hermano prohíbe. **Sin allowlist**: aquí no hay equivalente legítimo de `logger-noop.js` (D-16).
   - `describe('ISO-02 (Phase 87): src/cli/sanitize.js es una HOJA de cero imports')` con `it('src/cli/sanitize.js existe y tiene cero imports (incluidos builtins)')`: estructura de 4 partes del molde (`test/check-isolation.test.js:241-258`) — comentario con la razón dura de por qué existe el guard y qué degradación previene (si el módulo deja de ser hoja, reabre por la puerta de atrás la arista que esta fase cierra); `assert.equal(existsSync(path), true, …)` anti-vacuidad cuyo mensaje diga que debe existir tras el plan `87-01` porque si no el test pasa trivialmente; `assert.deepEqual(extractImports(src), [], …)` con `found: …` en el mensaje —**cero imports a secas, sin allowlist**, a diferencia de `dashboard/format.js`, que sí necesitará una en el plan `87-02`—; y el mensaje nombrando a los consumidores que la pureza protege.

**5) Comprobar el ROJO y transcribirlo.** Ejecutar el fichero, comprobar que falla, y copiar al SUMMARY: las **tres** cadenas impresas verbatim, el mensaje del assert anti-vacuidad de `src/cli/sanitize.js`, y el `# tests` / `# fail` de la corrida. Este rojo intermedio **no** sustituye a la mordida canónica de D-15, que la Tarea 3 produce sobre el HEAD final.

**6) SHA base de la fase.** Antes de la primera edición, ejecutar `git rev-parse HEAD` y registrar el valor en el SUMMARY bajo el rótulo `phase_base_sha`. Todos los valores medidos de este plan se tomaron contra `61a5c95`; si el SHA base difiere, re-medirlos antes de dar un criterio por bueno.
  </action>

  <verify>
    <automated>node --test test/format-isolation.test.js > "${TMPDIR:-/tmp}/87-01-red.log" 2>&1; [ $? -ne 0 ] && grep -q 'src/cli/dashboard/markdown.js' "${TMPDIR:-/tmp}/87-01-red.log" && grep -q 'src/cli/format.js' "${TMPDIR:-/tmp}/87-01-red.log" && grep -q 'src/cli/sanitize.js' "${TMPDIR:-/tmp}/87-01-red.log"</automated>
  </verify>

  <acceptance_criteria>
    Todos los valores de HEAD se midieron en vivo contra `61a5c95` al planificar. Cada criterio lleva su clase: **muerde** (hoy no se cumple; solo la tarea puede satisfacerlo) o **anti-regresión** (hoy ya se cumple; existe para que la tarea no lo rompa y **no** acredita trabajo hecho).

    - `node --test test/format-isolation.test.js` sale con código distinto de 0 y su salida contiene, en la misma cadena, `src/cli/dashboard/markdown.js` y `src/cli/format.js`. **Muerde** — HEAD: la corrida sale con `# tests 8 · # suites 5 · # pass 8 · # fail 0`, es decir verde y sin ninguna cadena impresa.
    - Esa misma salida contiene exactamente **tres** cadenas, encabezadas por `src/cli/dashboard/App.js`, `src/cli/dashboard/SessionTable.js` y `src/cli/dashboard/markdown.js`. **Muerde.** Si aparecieran cuatro (con `index.js`), el walker está siguiendo aristas dinámicas y viola D-06.
    - Esa misma salida contiene el mensaje anti-vacuidad de `src/cli/sanitize.js`. **Muerde** — HEAD: el fichero no existe y el guard tampoco.
    - `grep -c 'stripComments' test/format-isolation.test.js` es ≥ 1. **Muerde** — HEAD: `0`.
    - El caso dinámico aplica **los dos** mecanismos de D-11: `stripComments` antes del match **y** el filtro de línea sobre los tres prefijos de comentario, con el comentario que explica que dan el mismo resultado medido y que el primero es estrictamente más fuerte. **Muerde** — HEAD: ninguno de los dos existe.
    - El bloque de comentario de `stripComments` contiene las tres cifras de la medición (`enrich.js` 3→0, `markdown.js` 4→0, `session-lookup.js` 5→0) y el argumento del guard ciego sobre `markdown.js`. **Muerde** — HEAD: el helper no existe.
    - `grep -c 'walkImports' test/format-isolation.test.js` **no** cambia respecto de HEAD salvo por los usos nuevos, y el cuerpo de `walkImports` (`:40-52`) queda **byte-idéntico**: `diff <(git show 61a5c95:test/format-isolation.test.js | sed -n '40,52p') <(sed -n '<rango nuevo>p' test/format-isolation.test.js)` da salida vacía. **Anti-regresión** — trivialmente cierto en HEAD; es el gate de D-05 y de Pitfall 4 (`stripComments` no entra en el walker).
    - El bloque `describe` de TUI-04 (`:200-221` en HEAD) sigue presente sin cambios: `diff <(git show 61a5c95:test/format-isolation.test.js | sed -n '200,221p') <(sed -n '<rango nuevo>p' test/format-isolation.test.js)` da salida vacía. **Anti-regresión** — gate de D-08.
    - `git diff --exit-code -- src/ package.json package-lock.json` sale con código 0: esta tarea no toca producción ni añade dependencias. **Anti-regresión** por diseño — delimita el alcance, no acredita trabajo.
    - El SUMMARY registra `phase_base_sha`, las tres cadenas verbatim y el `# tests`/`# fail` de la corrida roja. **Muerde.**
  </acceptance_criteria>

  <done>
    `test/format-isolation.test.js` contiene `stripComments` con el orden corregido y su medición citada, la regex constante de `import()` dinámico, el BFS `findChainToPicocolors` y las tres suites nuevas; el guard transitivo sale **ROJO** contra `61a5c95` imprimiendo las tres cadenas; `walkImports` y TUI-04 quedan byte-idénticos; producción intacta.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: La hoja `src/cli/sanitize.js` y los OCHO call sites</name>

  <files>src/cli/sanitize.js, src/cli/dashboard/App.js, src/cli/dashboard/markdown.js, src/cli/inbox.js, src/cli/capture.js, src/hooks/stop.js, src/inbox/store.js, src/session/manager.js, test/dashboard-format.test.js</files>

  <read_first>
    - `src/cli/format.js` **íntegro** (243 líneas) — el fichero del que se corta. Los límites verificados en vivo contra `61a5c95`: `:56-58` cuerpo y cierre de `visibleWidth` (**no se mueve**, D-03); `:59` línea en blanco; `:60-79` JSDoc de `stripControlChars`; `:80-87` su cuerpo; `:88` línea en blanco; `:89-113` JSDoc de `stripForKeystroke`; `:114-123` su cuerpo; `:124` línea en blanco; `:125` arranque del JSDoc de `padCell` (**no se mueve**). El bloque movible es **contiguo: 60-123**. Los rangos que cita `87-CONTEXT.md` D-01 (`:80-97` y `:114-125`) están desalineados y partirían el JSDoc.
    - `src/session/handoff.js:1-27` y `src/tasks/pending.js:1-21` — los **dos precedentes exactos** del repo de módulo-contrato hoja de cero imports. De ahí sale la estructura de cabecera; `pending.js` además nombra a `handoff.js` como su molde, y la cabecera nueva debe nombrar a los dos.
    - Los ocho call sites, en su línea verificada hoy: `src/cli/dashboard/App.js:73` · `src/cli/dashboard/markdown.js:27` · `src/cli/inbox.js:36` · `src/cli/capture.js:38` · `src/hooks/stop.js:16` · `src/inbox/store.js:46` · `src/session/manager.js:12` · `test/dashboard-format.test.js:27`.
    - Los consumos que no cambian, para confirmar que el movimiento es puro: `src/cli/capture.js:96` · `src/hooks/stop.js:59,83` · `src/inbox/store.js:311,329,344`.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md` §Hallazgo 2 (la tabla de los 8 call sites), §Hallazgo 3 (los límites del bloque), §Code 6 (la cabecera propuesta del módulo nuevo) y §Common Pitfalls 1 y 7.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-PATTERNS.md` §`src/cli/sanitize.js` — los cinco elementos obligatorios de la cabecera y lo que **no** se copia de los análogos.
  </read_first>

  <reversibility rating="costly">
    D-01 mueve dos funciones exportadas y toca 8 call sites en cuatro subsistemas (TUI, CLI de captura, hook de Stop, escritura del inbox); deshacerlo los toca otra vez. No es one-way: no hay formato en disco ni contrato publicado que migrar — la salida de ambas funciones es byte-idéntica y ningún dato persistido nombra el módulo.
  </reversibility>

  <action>
Crear la hoja y re-apuntar los ocho consumidores. El orden importa: primero el módulo, después los call sites, y solo entonces se corta el bloque del fichero de color — así ningún estado intermedio del árbol tiene una función referenciada y ausente.

**1) `src/cli/sanitize.js` (D-01).** Fichero nuevo. Primera línea, la anotación de comprobación de tipos que usan los análogos. Cabecera con los **cinco elementos obligatorios** del molde: (a) esa anotación; (b) path + fase + plan + requisito, con la forma «Phase 87 Plan 01 (ISO-02)»; (c) un bloque titulado como restricción estructural no negociable de **cero imports** —ni builtins ni relativos— con la **razón dura**, no solo la regla: estas funciones vivían en el único importador del paquete de color, y eso hacía que dos ficheros del TUI que solo querían sanear texto arrastrasen ese paquete al grafo, rompiendo la invariante de color-isolation con el guard directo en verde; (d) la mención explícita de que el contrato es el mismo que el de `src/session/handoff.js`, `src/tasks/pending.js` y `src/logger-noop.js`, y de que el guardián es `test/format-isolation.test.js`; (e) un bloque propio de esta fase declarando que el re-export desde el módulo de color está **prohibido** (D-02), porque mantener viva esa arista dejaría un atajo legítimo que dispara la alarma sin ser un error, y el objetivo es que el camino correcto sea el único disponible. Texto de referencia en `87-RESEARCH.md` §Code 6. **No** se copia de `handoff.js:18-22` la prohibición de construir regex: este módulo **es** regex por definición y copiarla sería falsa.

   El **cuerpo** es el bloque `60-123` de `src/cli/format.js` **copiado carácter a carácter**: JSDoc y cuerpo de `stripControlChars`, la línea en blanco intermedia, JSDoc y cuerpo de `stripForKeystroke`. **Ni una regex se reescribe, ni un comentario se reformatea, ni el orden de las dos funciones se altera** — `stripForKeystroke` llama a `stripControlChars`, así que el orden actual también es el correcto de lectura. Las tres fases documentadas de `stripControlChars` (secuencias CSI completas; bytes de control con preservación de tabulador y salto de línea; rango C1) y las dos de `stripForKeystroke` (colapso de los reales, colapso de las secuencias de escape literales) se afinaron en las Phases 72 y 78 con WR-02: son superficie de seguridad, no estilo.

**2) Los ocho call sites (Hallazgo 2, Pitfall 1).** **Seis son sustitución de path pura**, cambiando el specifier al módulo nuevo y conservando el resto de la línea: `src/cli/dashboard/App.js:73` y `src/cli/dashboard/markdown.js:27` pasan de `'../format.js'` a `'../sanitize.js'`; `src/hooks/stop.js:16`, `src/inbox/store.js:46` y `src/session/manager.js:12` pasan de `'../cli/format.js'` a `'../cli/sanitize.js'`; `test/dashboard-format.test.js:27` pasa de `'../src/cli/format.js'` a `'../src/cli/sanitize.js'` (importa **las dos** funciones).

   **Los dos restantes parten el import en dos líneas**, y es la única forma del lote que no es buscar-y-reemplazar: en `src/cli/inbox.js:36` y en `src/cli/capture.js:38`, `createFormatter` **se queda** apuntando a `'./format.js'` y el saneador (`stripControlChars` en el primero, `stripForKeystroke` en el segundo) pasa a una línea propia apuntando a `'./sanitize.js'`.

   Tres de los ocho —`src/cli/capture.js`, `src/hooks/stop.js` y `src/inbox/store.js`— **no aparecen en la lista de D-01**, que contaba cinco. Los tres consumen `stripForKeystroke`, el consumidor menos visible, en carriles vivos: saneo del texto de la captura CLI, nudge de keystroke al orquestador y saneo de los tres campos de la línea del inbox. Sin shim (D-02), omitir cualquiera de ellos es un fallo en **tiempo de carga**, no una degradación silenciosa.

**3) El corte en `src/cli/format.js`.** Eliminar el bloque `60-123` completo, dejando `visibleWidth` (`:56-58`) y el JSDoc de `padCell` separados por una única línea en blanco, igual que hoy separa a los vecinos. Ninguna otra función del fichero llama a las dos que se van (verificado). `createFormatter`, `_resolveUseColor`, `visibleWidth`, `formatRow` y `formatTable` se quedan donde están, y el import del paquete de color de `:18` **no se toca**: el fichero sigue siendo su único importador y el guard de single-source (`test/format-isolation.test.js:98-129`) debe seguir verde.

**4) Verificar la byte-identidad antes de continuar.** Comparar el bloque copiado contra el original de `61a5c95` con `diff` sobre `git show`; si la salida no es vacía, el movimiento dejó de ser puro y hay que rehacerlo, no ajustar el criterio.

**Prohibido en esta tarea:** actualizar un golden porque cambió. Si `test/dashboard-markdown.test.js`, `test/inbox-format-golden.test.js`, `test/format.test.js` o `test/stop.test.js` se mueve un solo test, el movimiento dejó de ser puro (D-17) — se revisa el movimiento. La simulación completa de `87-RESEARCH.md` §Hallazgo 4 demuestra que ninguno cambia.
  </action>

  <verify>
    <automated>node --test test/format-isolation.test.js test/dashboard-format.test.js test/dashboard-markdown.test.js test/format.test.js test/stop.test.js test/inbox-format-golden.test.js test/inbox-cli.test.js test/check-isolation.test.js</automated>
  </verify>

  <acceptance_criteria>
    Valores de HEAD medidos en vivo contra `61a5c95`.

    - Las dos suites nuevas de la Tarea 1 (`ISO-01` transitivo y la hoja `src/cli/sanitize.js`) salen **verdes**, y `node --test test/format-isolation.test.js` da `# fail 0` con `# tests` estrictamente mayor que **8**. **Muerde** — HEAD: 8 tests, y tras la Tarea 1 el fichero está rojo.
    - `grep -rnE "^import .*(stripControlChars|stripForKeystroke)" src test | grep -c "cli/format"` devuelve `0`. **Muerde** — HEAD: `8`. Este es el gate de Pitfall 1: un plan dimensionado a cinco ficheros deja tres sin tocar y falla aquí.
    - `grep -rlnE "^import .*(stripControlChars|stripForKeystroke)" src test | wc -l` devuelve `8`: los ocho consumidores siguen existiendo y ninguno se perdió por el camino. **Anti-regresión** — HEAD: `8`. Existe para que «cerrar el carril viejo» no se confunda con «borrar un import».
    - `diff <(git show 61a5c95:src/cli/format.js | sed -n '60,123p') <(sed -n '<rango>p' src/cli/sanitize.js)` da **salida vacía**. **Muerde** — HEAD: el fichero destino no existe. Es el control de seguridad de la fase, no burocracia (§Security Domain: el mayor riesgo es que alguien «aproveche» el movimiento para limpiar las regex de saneo).
    - `node -e "import('./src/cli/sanitize.js').then(m=>{if(typeof m.stripControlChars!=='function'||typeof m.stripForKeystroke!=='function')process.exit(1)})"` sale con código 0. **Muerde** — HEAD: el módulo no existe.
    - Los cinco ficheros de D-17 dan conteos **idénticos** al baseline y ninguno se ha modificado salvo el que D-01 ya contaba: `test/dashboard-format.test.js` 58, `test/dashboard-markdown.test.js` 9, `test/format.test.js` 44, `test/stop.test.js` 33, `test/inbox-format-golden.test.js` 22, todos con `# fail 0`. **Anti-regresión** — HEAD: los mismos cinco valores, medidos en vivo. Es el gate del criterio 5.
    - `git diff --exit-code -- test/dashboard-markdown.test.js test/format.test.js test/stop.test.js test/inbox-format-golden.test.js` sale con código 0: ningún golden se tocó. **Anti-regresión** — trivialmente cierto en HEAD; gate de D-17.
    - `git diff --exit-code -- src/cli/dashboard/SessionTable.js src/cli/dashboard/index.js` sale con código 0 (D-04), y aun así ninguno aparece en las cadenas del guard: los leakers derivativos se cierran solos. **Anti-regresión** para el diff, **muerde** para la ausencia en las cadenas.
    - `node --test test/inbox-cli.test.js` da `# tests 75 · # fail 0` sin haber tocado el fichero: su assert de `:866` está anclado al **identificador**, no al path, y sobrevive al movimiento. **Anti-regresión** — HEAD: 75/0.
    - `node --test test/check-isolation.test.js` da `# tests 12 · # fail 0` sin haber tocado el fichero. **Anti-regresión** — HEAD: 12/0. Gate del diferido: el helper con bug de ese fichero no se corrige en esta fase.
    - `git diff --exit-code -- package.json package-lock.json` sale con código 0. **Anti-regresión** — gate de la invariante de cero dependencias nuevas.
  </acceptance_criteria>

  <done>
    `src/cli/sanitize.js` existe como hoja de cero imports con los dos saneadores byte a byte; los ocho consumidores importan desde ahí y ninguno desde el módulo de color; `src/cli/format.js` sigue siendo el único importador del paquete de color y no exporta ya los saneadores; el guard transitivo y el de la hoja están verdes; los cinco ficheros de D-17 conservan sus conteos sin una sola edición.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: Re-anclar los dos guards source-grep de `manager.test.js` y morder el guard</name>

  <files>test/manager.test.js</files>

  <read_first>
    - `test/manager.test.js:852-895` — el bloque completo que se toca. Los dos asserts anclados al path viven **hoy** en `:858` (`stripForKeystroke`, dentro del caso «Phase 78 (WR-01/WR-02)…» que empieza en `:852`) y `:890` (`stripControlChars`, caso «Phase 78 (IN-04)…» que empieza en `:885`), con sus comentarios explicativos en `:854-857` y `:887-888`. **Ojo:** `87-CONTEXT.md` D-17, `87-RESEARCH.md` §Hallazgo 4 y `87-PATTERNS.md` citan `:835`/`:867` — números de una lectura anterior. Medido en vivo contra `61a5c95`: son **`:858` y `:890`**. Anclar por **contenido**, nunca por número de línea.
    - En ese mismo bloque, lo que **NO** se toca: los asserts de interpolación (`:866` y el homólogo del segundo caso) y los **negativos de regresión** (`:871` y `:881`). Leerlos para confirmar que quedan intactos.
    - `src/cli/dashboard/markdown.js:27` tal como quedó tras la Tarea 2 — es el sujeto de las dos mordidas.
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-RESEARCH.md` §Hallazgo 4 (por qué actualizar estos dos asserts no viola DEBT-04) y §Hallazgo 8 (las dos mordidas, ya ejecutadas: A estática con 3 cadenas y 9 líneas de mensaje, B dinámica con una violación impresa).
    - `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-VALIDATION.md` §Manual-Only Verifications — el procedimiento de 5 pasos de la mordida.
  </read_first>

  <action>
Cerrar los dos guards hermanos que se anclaban al carril viejo y producir la evidencia de mordida.

**1) Los dos asserts de path (D-16 / RESEARCH §A3).** En cada uno de los dos casos, cambiar **únicamente** el fragmento de path dentro del regex —de la ruta escapada del módulo de color a la del módulo nuevo— y el texto del mensaje del assert en consonancia. Actualizar también los dos comentarios explicativos que nombran cuál es el carril canónico (`:854-857` y `:887-888`), que quedarían describiendo un carril que ya no existe.

   **Nada más cambia.** Los asserts de interpolación siguen exigiendo que los tres campos derivados del proveedor pasen por el saneador; los negativos de regresión siguen prohibiendo el campo crudo y el saneador de render dentro del carril de keystroke. El guard conserva **todas** sus condiciones: lo único que se mueve es cuál es el carril canónico al que se ancla. Esa distinción va escrita en el mensaje del commit, porque un revisor que vea «tocar un guard» sin contexto leerá relajación — y DEBT-04 está LOCKED (D-16).

**2) Suite completa verde.** Ejecutar `npm test` y comprobar `# fail 0`. Los dos fallos que la simulación de `87-RESEARCH.md` §Hallazgo 4 aisló son exactamente los dos asserts de este paso; los otros dos de aquella corrida eran artefactos del directorio scratch (ausencia de repo git y de la carpeta de skills) y no se reproducen aquí.

**3) Mordida A — estática, la de D-15.** Reintroducir a mano en `src/cli/dashboard/markdown.js` el import del saneador desde el módulo de color, exactamente en la forma que tenía en `61a5c95`. Ejecutar el fichero de guard: debe salir **ROJO** con **tres** cadenas —`src/cli/dashboard/markdown.js`, `src/cli/dashboard/SessionTable.js → src/cli/dashboard/markdown.js` y `src/cli/dashboard/App.js → src/cli/dashboard/SessionTable.js → src/cli/dashboard/markdown.js`—. Copiar al SUMMARY el **diff exacto** de la reintroducción, el **mensaje de fallo íntegro** con las tres cadenas y el **conteo** de la corrida. Revertir y re-ejecutar: verde.

   La elección de `markdown.js` es LOCKED (D-15), pero el `VERIFICATION` **no** debe justificarla por el tamaño del muro de salida: con el mensaje basado en cadenas de D-07 esa razón desapareció (medido: la mordida de `App.js` produce 2 cadenas / 5 líneas y la de `markdown.js` 3 cadenas / 9). Se justifica por lo que de verdad es: es el leak con la cadena más corta y el único que además ejercita la herencia por `SessionTable.js`.

**4) Mordida B — dinámica.** Con el árbol ya restaurado, inyectar en `src/cli/dashboard/markdown.js` una carga dinámica del paquete de color con specifier literal. Ejecutar el fichero de guard: el caso dinámico debe salir **ROJO** con la violación impresa en la forma `src/cli/dashboard/markdown.js → import('picocolors')`. Copiar diff, mensaje y conteo al SUMMARY. Revertir y re-ejecutar: verde. Sin esta mordida el guard dinámico es un assert que nunca se ha visto fallar; con ella queda demostrado además que `stripComments` con el orden corregido **no** lo ciega, que es el argumento que cierra ISO-04 (§Hallazgo 5: el helper verbatim daría 0 hits sobre este mismo fichero).

**5) Estado final del árbol.** Tras las dos mordidas, `git status --porcelain` sobre `src/cli/dashboard/markdown.js` debe estar limpio respecto del estado dejado por la Tarea 2. Una mordida que se queda a medio revertir es un leak reintroducido.

**Prohibido:** automatizar las mordidas como test permanente de la suite. D-15 es explícito y el milestone es saneo puro, sin feature nueva: la evidencia es citada, no infraestructura.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    Valores de HEAD medidos en vivo contra `61a5c95`.

    - `npm test` sale con `# fail 0` y `# tests` ≥ **2589**. **Muerde** — tras la Tarea 2, `test/manager.test.js` está rojo por sus dos asserts de path.
    - `node --test test/manager.test.js` da `# tests 62 · # fail 0`. **Muerde** para el estado post-Tarea-2; **anti-regresión** respecto al conteo, que no puede bajar de 62 (HEAD: 62).
    - `grep -c "cli\\\\/format\\\\.js" test/manager.test.js` devuelve `0` y `grep -c "cli\\\\/sanitize\\\\.js" test/manager.test.js` devuelve `2`. **Muerde** — HEAD: `2` y `0` respectivamente.
    - Los cuatro asserts que **no** cambian siguen presentes: el fichero conserva sus dos asserts de interpolación y sus dos negativos de regresión, y `node --test test/manager.test.js` los ejercita. Comprobable con `git diff -U0 -- test/manager.test.js`, cuyo diff debe tocar **solo** las dos líneas de regex, sus dos mensajes y los dos bloques de comentario. **Muerde** — es el gate anti-relajación de DEBT-04: si el diff toca cualquier otra aserción, el guard se debilitó.
    - El SUMMARY contiene, para la **mordida A**: el diff exacto de la reintroducción, el mensaje de fallo íntegro con las **tres** cadenas y el conteo de la corrida roja. **Muerde.**
    - El SUMMARY contiene, para la **mordida B**: el diff exacto, la línea de violación con la forma `<path> → import('<specifier>')` y el conteo. **Muerde.**
    - Tras ambas mordidas, `git status --porcelain -- src/cli/dashboard/markdown.js` no muestra diferencias respecto al estado dejado por la Tarea 2, y `node --test test/format-isolation.test.js` vuelve a `# fail 0`. **Muerde.**
    - `git diff --exit-code -- test/check-isolation.test.js test/dispatcher-isolation.test.js` sale con código 0: el helper con bug de los ficheros hermanos sigue **sin corregir**, como manda el diferido. **Anti-regresión** — trivialmente cierto en HEAD; delimita el alcance.
  </acceptance_criteria>

  <done>
    Los dos guards source-grep de `test/manager.test.js` se anclan al carril canónico nuevo conservando todas sus condiciones; `npm test` está verde con ≥ 2589 tests; las dos mordidas —estática y dinámica— están registradas en el SUMMARY con diff, mensaje y conteo, y el árbol quedó restaurado tras ambas.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artefactos que produce esta fase

Símbolos, ficheros y nombres de suite nuevos. Los de este plan van marcados `[87-01]`; los del plan hermano, `[87-02]` — se listan aquí para que el inventario de la fase sea completo en cualquiera de los dos documentos.

| Artefacto | Tipo | Dónde vive | Plan |
|-----------|------|-----------|------|
| `src/cli/sanitize.js` | **fichero nuevo** — módulo-contrato hoja de cero imports | `src/cli/` | `[87-01]` |
| `stripControlChars(s) → string` | función exportada (movida verbatim de `format.js:80-87`) | `src/cli/sanitize.js` | `[87-01]` |
| `stripForKeystroke(s) → string` | función exportada (movida verbatim de `format.js:114-123`) | `src/cli/sanitize.js` | `[87-01]` |
| `stripComments(src) → string` | helper de test con el **orden corregido** (D-10), duplicado a propósito | `test/format-isolation.test.js` | `[87-01]` |
| `DYNAMIC_PICOCOLORS_RE` | regex constante anclada al patrón `import(...)` con specifier literal | `test/format-isolation.test.js` | `[87-01]` |
| `importsPicocolors(file) → boolean` | predicado de import directo | `test/format-isolation.test.js` | `[87-01]` |
| `findChainToPicocolors(entry) → string[]\|null` | BFS con mapa de padres; cadena MÁS CORTA; solo en el camino de fallo | `test/format-isolation.test.js` | `[87-01]` |
| suite `ISO-01 (Phase 87): cero picocolors TRANSITIVO bajo src/cli/dashboard/` | `describe` nuevo | `test/format-isolation.test.js` | `[87-01]` |
| caso `ningún fichero del TUI alcanza picocolors por ninguna cadena de imports estáticos` | `it` | `test/format-isolation.test.js` | `[87-01]` |
| caso `ningún fichero del grafo del TUI hace import() DINÁMICO de picocolors` | `it` | `test/format-isolation.test.js` | `[87-01]` |
| suite `ISO-02 (Phase 87): src/cli/sanitize.js es una HOJA de cero imports` | `describe` nuevo | `test/format-isolation.test.js` | `[87-01]` |
| caso `src/cli/sanitize.js existe y tiene cero imports (incluidos builtins)` | `it` | `test/format-isolation.test.js` | `[87-01]` |
| suite `ISO-03 (Phase 87 / UF-02): src/cli/dashboard/format.js es una HOJA pura` | `describe` nuevo | `test/format-isolation.test.js` | `[87-02]` |
| caso `cero imports relativos; builtins solo los de la allowlist; clausura de 1` | `it` | `test/format-isolation.test.js` | `[87-02]` |
| caso `select.js consume ./format.js (convergencia, D-14)` | `it` | `test/format-isolation.test.js` | `[87-02]` |
| `ALLOWED_BUILTINS` | constante congelada de un elemento (`node:path`), la única allowlist de la fase (D-13) | `test/format-isolation.test.js` | `[87-02]` |
| suite `ISO-04 (Phase 87): stripComments no ciega al guard` | `describe` nuevo (meta-test) | `test/format-isolation.test.js` | `[87-02]` |
| caso `stripComments recupera los 4 imports estáticos de src/cli/dashboard/markdown.js` | `it` | `test/format-isolation.test.js` | `[87-02]` |
| Bloque **declaración honesta** (qué CUBRE / qué NO CUBRE / medición fechada) | cabecera reescrita, sustituye a `:10-16` | `test/format-isolation.test.js` | `[87-02]` |
</artifacts_produced>

<flagged_assumptions>
## Supuestos marcados — sondas de requisito sin resolver

Esta fase no tiene `SPEC.md`, así que se disparó el fallback de sonda spec-less. La sonda determinista de edges recorrió ISO-01..04 y devolvió **4 aplicables / 0 resueltos / 4 sin resolver**, las cuatro filas `unclassified`. Una fila `unclassified` **no** se auto-resuelve, **no** se auto-degrada a backstop y **no** se descarta en silencio, así que las cuatro quedan aquí como supuestos explícitos.

**Igualdad de no-descarte-silencioso:** 4 items surgidos de la sonda == 0 autorizados a `must_haves` desde la sonda + **4** marcados aquí. Los `must_haves.truths` de este plan y del `87-02` **no** vienen de la sonda: vienen de `87-VALIDATION.md` §Per-Task Verification Map, que ya traía el comando automatizado resuelto y verificado por requisito.

| Requisito | Estado de la sonda | Supuesto que asume esta fase |
|-----------|--------------------|------------------------------|
| ISO-01 | `unclassified` / `unresolved` | La cobertura sale de `87-VALIDATION.md`, filas ISO-01 (guard transitivo, mensaje con cadena, `import()` dinámico, mordida) → `must_haves.truths` de este plan, con la mordida como **backstop** por ser manual-only (D-15). |
| ISO-02 | `unclassified` / `unresolved` | Ídem, filas ISO-02 (los 3 ficheros dejan de alcanzar el paquete, cero imports residuales, movimiento byte-idéntico, cero regresión) → `must_haves.truths` de este plan. |
| ISO-03 | `unclassified` / `unresolved` | Ídem, filas ISO-03 (hoja + allowlist, clausura 1, convergencia D-14) → `must_haves.truths` del plan `87-02`. |
| ISO-04 | `unclassified` / `unresolved` | Ídem, filas ISO-04 (la premisa retirada, `stripComments` corregido, declaración honesta) → repartidas entre este plan (el guard dinámico y el helper) y el `87-02` (la declaración), con la revisión de honestidad como **backstop** por ser revisión de prosa. |

**Lectura práctica, sin adornos:** el clasificador de la sonda no reconoció la forma de estos cuatro requisitos porque **no son formas de petición/respuesta**: son invariantes de higiene sobre un grafo de imports del propio código fuente, un eje que la taxonomía de edges no modela. Eso es una limitación del clasificador, **no** evidencia de que los requisitos no tengan casos límite. Lo que sostiene la cobertura es el mapa humano por requisito con comando automatizado; lo que sostiene que el mapa no es decorativo son las dos mordidas de la Tarea 3.

**Breadcrumb de canon (sonda §B):** el endurecimiento genérico contra inyección de terminal (secuencias CSI, OSC-52, C1 de un byte) es canon — lo cubren `/gsd-secure-phase` y el `<threat_model>` de abajo; **no se acuña** como prohibición. Lo que sí se acuña es la prohibición bespoke de esta fase: alterar los cuerpos durante el movimiento.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contenido de proveedor / LLM → `stripControlChars` → render del TUI | Comentarios de Plane, títulos de tarea y el `next` escrito por un LLM cruzan aquí sin confianza. El saneador es el control; se **mueve de fichero**, y ese movimiento es la superficie de riesgo de la fase. |
| Contenido de proveedor / LLM → `stripForKeystroke` → `cmux send` (carril de keystroke) | Mismo modelo de amenaza más el vector salto de línea (Enter espurio). Tres consumidores vivos: captura CLI, hook de Stop y escritura del inbox. |
| Fuente del repo → regex de los guards | El guard lee ficheros del propio árbol. Las regex nunca se compilan desde input, pero recorren texto arbitrario del repo. |
| `src/cli/format.js` → `picocolors` | Única arista legítima hacia el paquete de color (invariante cross-milestone). La fase la **preserva**; lo que corta son las aristas del TUI hacia ese fichero. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Tampering | bloque `60-123` movido a `src/cli/sanitize.js` (ASVS V5) | high | mitigate | Copia carácter a carácter de un bloque **contiguo**; criterio `diff` contra `git show 61a5c95:src/cli/format.js` con salida vacía; prohibición explícita de reescribir regex; los 5 ficheros de D-17 con conteos idénticos (Tarea 2). |
| T-87-02 | Tampering | los 8 call sites — 3 de ellos ausentes de D-01 (ASVS V5) | high | mitigate | Los ocho se actualizan en el mismo paso; gate `grep` de cero imports residuales desde el módulo de color **y** gate de que siguen siendo 8 consumidores; `npm test` verde (Tarea 2 y 3). |
| T-87-03 | Tampering | guard de color-isolation en falso verde | medium | mitigate | Mordida A (estática) y mordida B (dinámica) con diff, mensaje y conteo citados; assert anti-vacuidad `existsSync`; assert de alcanzabilidad además de los de forma (Tareas 1 y 3). |
| T-87-04 | Denial of Service | `DYNAMIC_PICOCOLORS_RE` sobre fuentes del repo | low | mitigate | Regex **constante**, jamás compilada desde input; clases de caracteres negadas que no retroceden sobre sí mismas; molde de `test/check-isolation.test.js:31-34` (Tarea 1). |
| T-87-05 | Repudiation | actualización de los 2 asserts de `test/manager.test.js` leída como relajación | medium | mitigate | El diff toca **solo** las dos líneas de regex, sus mensajes y sus comentarios; los asserts de interpolación y los negativos de regresión quedan intactos, con criterio `git diff -U0` que lo comprueba; la distinción va escrita en el commit (Tarea 3, D-16 / §A3). |
| T-87-06 | Information disclosure | mensaje de fallo del guard | low | accept | Imprime paths relativos del propio repo, ya públicos en el árbol. La cadena es precisamente el dato accionable que D-07 exige; no hay secreto que redactar. |
| T-87-SC | Tampering | instalaciones npm/pip/cargo | low | accept | **Cero instalaciones en esta fase** (`87-RESEARCH.md` §Package Legitimacy Audit: no aplica; `[SLOP]` 0, `[SUS]` 0). Invariante cross-milestone de cero dependencias npm nuevas, aseverado por `git diff --exit-code -- package.json package-lock.json`. No procede checkpoint de legitimidad porque no hay paquete que verificar. |
</threat_model>

<verification>
**Baselines medidos en vivo contra `61a5c95` al planificar.** `test/format-isolation.test.js` = 8 tests / 5 suites / 0 fail · `test/dashboard-format.test.js` = 58 · `test/dashboard-markdown.test.js` = 9 · `test/format.test.js` = 44 · `test/stop.test.js` = 33 · `test/inbox-format-golden.test.js` = 22 · `test/manager.test.js` = 62 · `test/inbox-cli.test.js` = 75 · `test/check-isolation.test.js` = 12 · `test/dashboard-inbox-count.test.js` = 14. Call sites que importan un saneador desde el módulo de color = **8**.

1. `node --test test/format-isolation.test.js` → `# fail 0`, `# tests` > 8, cero cadenas impresas.
2. `grep -rnE "^import .*(stripControlChars|stripForKeystroke)" src test | grep -c "cli/format"` → `0`; el conteo de ficheros consumidores sigue siendo `8`.
3. `diff` del bloque movido contra `git show 61a5c95:src/cli/format.js` recortado a `60,123` → salida vacía.
4. `npm test` → `# fail 0`, `# tests` ≥ 2589.
5. Los cinco ficheros de D-17 con conteos idénticos y sin edición (`git diff --exit-code` limpio en los cuatro goldens).
6. `git diff -U0 -- test/manager.test.js` toca únicamente dos líneas de regex, dos mensajes y dos bloques de comentario.
7. El SUMMARY registra `phase_base_sha`, la salida roja de la Tarea 1 con sus tres cadenas, y las dos mordidas con diff, mensaje y conteo.
</verification>

<success_criteria>
- Un fichero del TUI que arrastre `picocolors` por una **cadena transitiva** de imports pone el guard rojo, y el mensaje imprime la cadena, no el conjunto (ISO-01, criterio 1 del ROADMAP).
- Los 3 leaks medidos están cerrados: `App.js`, `markdown.js` y `SessionTable.js` dejan de alcanzar `src/cli/format.js`; el guard pasa verde **con** el fix y rojo sin él, sin haberse relajado (ISO-02, criterio 2).
- Un `import()` dinámico del paquete de color dentro del grafo del TUI también pone el guard rojo, con `stripComments` de orden corregido delante del match (ISO-01 / ISO-04).
- Los saneadores viven en una hoja de cero imports y llegan a sus ocho consumidores sin shim; el módulo de color sigue siendo el único importador del paquete (D-01/D-02/D-07).
- Cero regresión de comportamiento: los cinco ficheros de D-17 idénticos, ningún golden actualizado, `npm test` verde (criterio 5).
- Ningún assert se debilitó: los dos guards de `test/manager.test.js` conservan todas sus condiciones y solo cambian de carril canónico (DEBT-04 LOCKED, D-16).
</success_criteria>

<output>
Crear `.planning/phases/87-aislamiento-de-color-transitivo-en-el-tui/87-01-SUMMARY.md` al terminar, incluyendo obligatoriamente: `phase_base_sha`; la salida roja de la Tarea 1 con las tres cadenas verbatim y su conteo; el diff, el mensaje de fallo y el conteo de la **mordida A** (estática) y de la **mordida B** (dinámica); y el párrafo que explica por qué actualizar los dos asserts de `test/manager.test.js` no debilita ningún guard.
</output>
