---
phase: 43-render-provider-state-en-el-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/select.js
  - src/cli/dashboard/App.js
  - test/dashboard-select.test.js
autonomous: true
requirements: [PSTATE-06]
must_haves:
  truths:
    - "El operador filtra por provider_state con el prefijo dedicado `ps:` (ej. ps:review)"
    - "El match es por substring case-insensitive: ps:rev casa 'in_review' ('in_review'.includes('rev'))"
    - "El prefijo `s:` sigue filtrando SOLO el estado local v3 (match exacto) — ejes de filtro separados y explícitos"
    - "Una fila con provider_state === null (unsupported/fetch-failed) NUNCA casa con ps:"
    - "El footer de hints documenta el prefijo ps:"
  artifacts:
    - path: "src/cli/dashboard/select.js"
      provides: "parseFilter reconoce ps: + applyFilter ramifica por provider_state con String.includes"
      contains: "ps:"
    - path: "src/cli/dashboard/App.js"
      provides: "Footer de hints documenta ps:"
      contains: "ps:"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/select.js"
      via: "applyFilter(sorted, parseFilter(query), deriveRepo) — wiring ya existente en línea 266"
      pattern: "applyFilter"
    - from: "src/cli/dashboard/select.js applyFilter"
      to: "provider_state"
      via: "String.includes case-insensitive sobre el string crudo (anti-ReDoS)"
      pattern: "provider_state"
---

<objective>
Añadir un **prefijo de filtro dedicado `ps:`** a la capa de derive pura del dashboard (`select.js`) que acote por `provider_state` vía `String.includes` case-insensitive sobre el string crudo (criterio 3, PSTATE-06, D-06/D-07/D-09). El `ps:` es un eje SEPARADO del `s:` existente (que filtra el estado local v3 con match exacto) — no se extiende `s:` con OR. Documentar el prefijo en el footer de hints (D-06).

Purpose: cerrar la parte de filtro de la cadena provider_state — el operador acota la tabla por estado de la tarea en su sistema de gestión, separado del eje de proceso local. Anti-ReDoS por construcción (jamás RegExp sobre input del operador — T-36-01).
Output: `select.js` con la rama `ps:` en parseFilter/applyFilter; `App.js` con el hint actualizado; tests de select extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-render-provider-state-en-el-dashboard/43-CONTEXT.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código — no explorar el codebase. -->

Patrón espejo existente en select.js (replicar casi 1:1 para `ps:`):
- `parseFilter(query)` (línea 101) → retorna `{ repo, status, text }`. Ya baja a minúsculas y reconoce `r:`/`s:` por prefijo. Añadir el campo `provider_state` y reconocer `ps:`.
- `applyFilter(rows, parsed, deriveRepo)` (línea 139) → filtra haciendo AND vía `String.includes` (anti-ReDoS). Añadir la rama `provider_state`.
- ASIMETRÍA DELIBERADA (documentar inline): `s:` hace match EXACTO (`st === parsed.status`, línea 147); `ps:` hace match por SUBSTRING (`String.includes`) — son ejes distintos con semánticas distintas (criterio 3, specifics CONTEXT.md). NO "alinear" ambos.

ORDEN de parsing crítico: `ps:` empieza por `p` pero contiene `s:` como substring — el check de prefijo debe probar `ps:` ANTES que `s:` (o usar startsWith exacto), si no `s:` capturaría 'ps:review' mal. `'ps:review'.toLowerCase().startsWith('s:')` es false (empieza por 'p'), así que el orden actual r:/s: no lo captura por accidente — pero AÑADIR la rama ps: con startsWith('ps:') es obligatorio y debe ir antes de la rama s: para legibilidad/seguridad.

Contrato del dato (Phase 40, ya en cada fila): `provider_state: string | null`. Las filas con `provider_state === null` (unsupported/fetch-failed) NUNCA casan con ps: (D-09).

Wiring ya existente (NO tocar): App.js:266 `applyFilter(sorted, parseFilter(query), deriveRepo)` — la rama ps: entra ahí sin cambiar el wiring.
Footer de hints: App.js:575 `'↑↓ move · / filter · d dismiss · q quit'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rama ps: en parseFilter/applyFilter de select.js</name>
  <files>src/cli/dashboard/select.js, test/dashboard-select.test.js</files>
  <read_first>
    - src/cli/dashboard/select.js (TODO el archivo — `parseFilter` línea 101 y `applyFilter` línea 139 como patrón espejo r:/s:; nota anti-ReDoS T-36-01 línea 124-128)
    - test/dashboard-select.test.js (estructura de tests puros; el bloque `applyFilter(full, parsed, deriveRepo)` línea 97 como patrón de aserción de filtro)
    - .planning/phases/43-render-provider-state-en-el-dashboard/43-CONTEXT.md §D-06/D-07/D-09 (prefijo ps: dedicado, substring case-insensitive, filas null nunca casan)
    - .planning/phases/40-provider-state-contrato-providers-enrichment/40-CONTEXT.md §D-05 (provider_state: string|null — la forma del campo que ps: matchea)
  </read_first>
  <behavior>
    - Test: parseFilter('ps:review') → { provider_state: 'review', repo: null, status: null, text: '' } (baja a minúsculas, reconoce el prefijo)
    - Test: parseFilter('PS:Review') → provider_state 'review' (prefijo y valor case-insensitive)
    - Test: parseFilter('s:running') → status 'running', provider_state null (el ps: NO se confunde con s:)
    - Test: applyFilter con parsed.provider_state 'rev': fila { provider_state: 'in_review' } CASA ('in_review'.includes('rev')) — substring, no exacto
    - Test: applyFilter con parsed.provider_state 'review': fila { provider_state: null } NO casa (D-09 — degradadas nunca casan)
    - Test: applyFilter con parsed.provider_state 'done': fila { provider_state: 'done' } casa; fila { provider_state: 'in_review' } no casa
    - Test: applyFilter combina AND — `s:running ps:review` filtra por estado local exacto Y provider_state substring simultáneamente
    - Test: anti-ReDoS — un valor ps con chars regex-especiales (ej. ps:.*) se matchea LITERAL como substring, jamás compila RegExp
  </behavior>
  <action>
    En `parseFilter` (select.js:101) añadir el campo `provider_state: null` al objeto `out` y, dentro del loop de words, reconocer el prefijo `ps:` con `lower.startsWith('ps:')` ANTES de la rama `s:` (el check de ps: debe preceder al de s: por claridad y para blindar el parsing; documentar inline por qué el orden importa). El valor se extrae con `w.slice(3)` y se baja a minúsculas. En `applyFilter` (select.js:139) añadir una rama: `if (parsed.provider_state) { const ps = (r.provider_state ?? '').toLowerCase(); if (!ps.includes(parsed.provider_state)) return false; }` — substring vía `String.includes`, JAMÁS RegExp (anti-ReDoS T-36-01). Documentar inline la ASIMETRÍA deliberada: `s:` es match EXACTO del estado local v3, `ps:` es match por SUBSTRING del provider_state (criterio 3) — ejes distintos, no alinear. La fila con `provider_state === null` (o ausente) colapsa a `''.includes(termNoVacío)` === false → nunca casa (D-09), comportamiento correcto por construcción. Actualizar el JSDoc de ambas funciones para incluir el nuevo campo. Escribir los tests del bloque <behavior> en test/dashboard-select.test.js espejando el estilo existente. Color-isolation: cero picocolors, cero import de format.js que no exista ya (deriveRepo es estable, no se toca).
  </action>
  <verify>
    <automated>node --test test/dashboard-select.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-select.test.js` exits 0
    - `parseFilter('ps:review').provider_state === 'review'`
    - `parseFilter('s:running').provider_state === null` (ps: no captura s:)
    - applyFilter: fila `{ provider_state: 'in_review' }` con parsed.provider_state 'rev' → incluida (substring)
    - applyFilter: fila `{ provider_state: null }` con parsed.provider_state 'review' → excluida (D-09)
    - select.js usa `String.includes` para la rama ps:, cero `RegExp`/`.match`/`.test` sobre el término (grep gate: `grep -v '^//' src/cli/dashboard/select.js | grep -cE 'new RegExp|\.match\(|\.test\('` == 0)
    - select.js NO importa picocolors (`grep -v '^//' src/cli/dashboard/select.js | grep -c picocolors` == 0)
  </acceptance_criteria>
  <done>parseFilter reconoce ps: como eje separado; applyFilter filtra por provider_state vía substring anti-ReDoS; filas null nunca casan; tests de select verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar ps: en el footer de hints de App.js</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - src/cli/dashboard/App.js (línea 575 — el footer de hints `'↑↓ move · / filter · d dismiss · q quit'`; línea 266 — wiring applyFilter ya existente que NO se toca)
    - .planning/phases/43-render-provider-state-en-el-dashboard/43-CONTEXT.md §D-06 (documentar ps: en el footer; Claude's Discretion: probablemente sí, minimal)
  </read_first>
  <action>
    Actualizar la cadena del footer de hints en App.js (línea 575) para documentar el nuevo prefijo de filtro `ps:` de forma mínima. El footer actual es `'↑↓ move · / filter · d dismiss · q quit'`. Añadir una pista compacta del filtro provider_state — opción minimal: cambiar `/ filter` por `/ filter (ps:state)` o añadir un segmento `· ps:state filter` SIN saturar la línea (el footer es estrecho; mantenerlo legible). NO añadir contador de provider_state al header countsLabel (D-Claude's-Discretion: NO en v1, el header se mantiene centrado en el eje local). NO tocar el wiring de applyFilter (línea 266 ya invoca parseFilter cada render — la rama ps: entra automáticamente). Verificar que el render no rompe: el footer es un `<Text dimColor>` plano, cero color nuevo.
  </action>
  <verify>
    <automated>node --test test/dashboard-table.test.js test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-table.test.js` exits 0 (el render sigue verde con el footer actualizado)
    - `node --test test/format-isolation.test.js` exits 0 (cero picocolors)
    - App.js:575 footer menciona `ps:` (grep: `grep -c 'ps:' src/cli/dashboard/App.js` >= 1)
    - El countsLabel del header NO se modificó (sin contador provider_state — D-discretion)
  </acceptance_criteria>
  <done>El footer documenta el prefijo ps: de forma compacta; el render sigue verde; el wiring de applyFilter intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador (input de filtro `ps:`) → select.js parseFilter/applyFilter | El término del filtro lo teclea el operador — UNTRUSTED. Cruza a la lógica de match. |
| GET /status (server) → applyFilter | `provider_state` de cada fila viene de Phase 40 (provider autenticado). El filtro solo lo LEE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Denial of Service (ReDoS) | applyFilter rama ps: | mitigate | Match por `String.includes` sobre el `provider_state` lowercased — JAMÁS `RegExp`/`.match`/`.test` sobre el término tecleado (criterio 3, D-07, espejo de T-36-01 para r:/s:). Un término `ps:.*` se matchea LITERAL como substring, cero backtracking catastrófico. Grep gate verifica cero RegExp en select.js. |
| T-43-05 | Information Disclosure | rama ps: / D-09 boundary | mitigate | La rama ps: SOLO lee `provider_state` ya presente en las filas de GET /status (Phase 40). No accede a campos nuevos, no escribe a state.json. Las filas degradadas (provider_state null) nunca casan — el filtro no expone el reason. |
| T-43-06 | Tampering | parsing ps: vs s: | mitigate | `ps:` se reconoce con `startsWith('ps:')` ANTES de la rama `s:`, evitando que un `ps:` se interprete como `s:` (eje equivocado). Ambos ejes permanecen separados y explícitos (D-06). |
</threat_model>

<verification>
- `node --test test/dashboard-select.test.js` exits 0 (ps: substring, case-insensitive, filas null no casan, AND con s:)
- `node --test test/dashboard-table.test.js` exits 0 (render con footer actualizado)
- `node --test test/format-isolation.test.js` exits 0 (cero picocolors)
- grep gate: cero `new RegExp`/`.match`/`.test` sobre el término ps: en select.js
</verification>

<success_criteria>
- PSTATE-06 satisfecho: filtro por provider_state con prefijo dedicado `ps:` vía String.includes anti-ReDoS — criterio 3
- Eje separado del `s:` (estado local exacto) — semánticas distintas documentadas inline
- Filas con provider_state null nunca casan con ps: (D-09)
- Footer documenta ps: (D-06); countsLabel sin tocar (D-discretion)
</success_criteria>

<output>
Create `.planning/phases/43-render-provider-state-en-el-dashboard/43-02-SUMMARY.md` when done
</output>
