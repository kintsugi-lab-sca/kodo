---
phase: 43-render-provider-state-en-el-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/format.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-format.test.js
  - test/dashboard-table.test.js
autonomous: true
requirements: [PSTATE-05]
must_haves:
  truths:
    - "El dashboard muestra el provider_state en una columna dedicada 'task', separada de la columna 'status' (eje local v3) — columna dedicada D-01, posición entre status y age D-02, header 'task' D-03"
    - "Una sesión con provider_state 'in_review' es visible verbatim en la columna 'task' (cierra ROMAN-150)"
    - "Una fila unsupported (reason 'unsupported') muestra '—' en dim; una fila fetch-failed (reason 'fetch-failed') muestra '?' en dim — distinguibles SIN color (render por reason D-04, texto plano + dim D-05)"
    - "El valor crudo 'unknown' (reason null) se muestra verbatim como cualquier ok-value, NO como glyph degradado"
    - "Un estado renombrado por el provider se muestra verbatim sin cambios de código (cero tabla de mapeo — verbatim+truncate D-08)"
  artifacts:
    - path: "src/cli/dashboard/format.js"
      provides: "Derivación pura de la celda task (taskCell) + rowCells extendido con la clave task"
      contains: "task"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "Columna task renderizada entre status y age (header + celda dim para degradados)"
      contains: "task"
  key_links:
    - from: "src/cli/dashboard/SessionTable.js"
      to: "src/cli/dashboard/format.js"
      via: "rowCells(session).task + taskCell derivación"
      pattern: "rowCells"
    - from: "src/cli/dashboard/format.js"
      to: "provider_state / provider_state_reason"
      via: "lectura de los campos enriquecidos por GET /status (Phase 40)"
      pattern: "provider_state"
---

<objective>
Renderizar `provider_state` en una **columna dedicada nueva keyed/headered `task`**, insertada entre `status` y `age` en la tabla viva del dashboard ink — separando inequívocamente el eje "tarea en el sistema de gestión" del eje "proceso local v3" (criterio 1, PSTATE-05, D-01/D-02/D-03). La celda distingue los **tres reason-states** sin color (D-04/D-05): valor crudo verbatim (ok), `—` dim (unsupported), `?` dim (fetch-failed).

Purpose: cerrar la parte visual de la cadena provider_state end-to-end del milestone v0.10 (driver ROMAN-150: sesión "In Review" en Plane invisible tras `/exit`). La sola presencia del valor en una columna propia resuelve el caso.
Output: `format.js` con derivación pura de la celda `task`; `SessionTable.js` con la columna renderizada; tests de format y de tabla extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-render-provider-state-en-el-dashboard/43-CONTEXT.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código — no explorar el codebase. -->

Contrato de datos consumido (Phase 40, ya presente en cada fila de GET /status):
- `provider_state: string | null`   — valor crudo del provider ('in_review', 'done', 'unknown', …) o null
- `provider_state_reason: null | 'unsupported' | 'fetch-failed'`
  - reason null  + provider_state presente → OK (valor crudo verbatim)
  - reason null  + provider_state === 'unknown' → OK (verbatim, NO degradado — D-08/specifics)
  - reason 'unsupported' (provider_state null, permanente) → glyph '—' dim
  - reason 'fetch-failed' (provider_state null, transitorio) → glyph '?' dim

Helpers existentes a reusar (NO reimplementar):
- format.js `rowCells(session)` (línea 181) → proyecta a `{ task_ref, repo, phasemode, status, age }`. Añadir la clave `task`.
- SessionTable.js `cell({ width, text, color, dim, bold, truncate })` (línea 60) → celda de ancho fijo, `truncate-end` nativo de ink.
- SessionTable.js `COLS` (línea 44): `{ gutter: 2, state: 16, task_ref: 10, repo: 18, phasemode: 11, status: 18, age: 7 }`.
- SessionTable.js `columnHeader` (línea 280) y `dataRows.map` (línea 293): donde se insertan la cabecera y la celda.
- Patrón espejo: `statusLabel` (format.js:119) deriva `(zombie)` de forma pura — la celda `task` se deriva igual desde `provider_state` + `provider_state_reason`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derivación pura de la celda task en format.js</name>
  <files>src/cli/dashboard/format.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/dashboard/format.js (TODO el archivo — patrón de funciones puras; `statusLabel` línea 119 como espejo de derivación; `rowCells` línea 181)
    - test/dashboard-format.test.js (líneas 1-60 — estructura de tests puros sin React; patrón de aserciones con mensaje explícito)
    - .planning/phases/43-render-provider-state-en-el-dashboard/43-CONTEXT.md §D-04/D-05/D-08/specifics (los tres reason-states + el caso 'unknown')
    - .planning/phases/40-provider-state-contrato-providers-enrichment/40-CONTEXT.md §D-05/D-06/D-07 (forma exacta del contrato provider_state/provider_state_reason)
  </read_first>
  <behavior>
    - Test: provider_state 'in_review' + reason null → celda text 'in_review', sin dim (ok verbatim)
    - Test: provider_state 'unknown' + reason null → celda text 'unknown', sin dim (verbatim, NO degradado)
    - Test: provider_state null + reason 'unsupported' → celda text '—', dim true
    - Test: provider_state null + reason 'fetch-failed' → celda text '?', dim true
    - Test: provider_state ausente/undefined + reason ausente → celda text '—' (fallback seguro, trata ausencia como sin-dato — NO crashea)
    - Test: rowCells(session) ahora incluye la clave `task` con la forma { text, dim } de la derivación
    - Test: cero transformación del vocabulario — un valor inventado 'wibble' se muestra 'wibble' verbatim (criterio 4, sin tabla de mapeo)
  </behavior>
  <action>
    Añadir una función pura exportada `taskCell(session)` a `format.js` que retorne `{ text: string, dim: boolean }` derivando SOLO de `session.provider_state` y `session.provider_state_reason`. Lógica por reason: cuando `provider_state_reason === 'unsupported'` → `{ text: '—', dim: true }`; cuando `=== 'fetch-failed'` → `{ text: '?', dim: true }`; en cualquier otro caso (reason null/ausente) → el valor crudo verbatim `{ text: provider_state, dim: false }`, con fallback `'—'` (sin dim) si `provider_state` es null/undefined. NUNCA transformar el string (cero guiones-a-espacios, cero tabla de mapeo — criterio 4, D-08). Documentar inline que `'unknown'` es un ok-value verbatim (reason null), DISTINTO de null+unsupported/fetch-failed (specifics CONTEXT.md). Extender `rowCells` (línea 181) para incluir la clave `task` con el resultado de `taskCell(session)`. Mantener color-isolation: cero picocolors, cero import de src/cli/format.js — `dim` es un bool plano que SessionTable mapea a `dimColor` de ink. Escribir los tests del bloque <behavior> en test/dashboard-format.test.js (importar `taskCell` y verificar `rowCells().task`), espejando el estilo de aserción-con-mensaje existente.
  </action>
  <verify>
    <automated>node --test test/dashboard-format.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-format.test.js` exits 0
    - format.js exporta `taskCell` y `taskCell({ provider_state: 'in_review', provider_state_reason: null })` retorna `{ text: 'in_review', dim: false }`
    - `taskCell({ provider_state: null, provider_state_reason: 'unsupported' })` retorna `{ text: '—', dim: true }`
    - `taskCell({ provider_state: null, provider_state_reason: 'fetch-failed' })` retorna `{ text: '?', dim: true }`
    - `taskCell({ provider_state: 'unknown', provider_state_reason: null })` retorna `{ text: 'unknown', dim: false }` (NO degradado)
    - `rowCells(session).task` existe y es la forma `{ text, dim }`
    - format.js NO contiene `import` de 'picocolors' ni de '../format.js' (grep: `grep -v '^//' src/cli/dashboard/format.js | grep -c picocolors` == 0)
  </acceptance_criteria>
  <done>taskCell deriva los 3 reason-states de forma pura y testeable sin ink; rowCells expone la celda task; tests de format verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Render de la columna task en SessionTable.js</name>
  <files>src/cli/dashboard/SessionTable.js, test/dashboard-table.test.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js (TODO el archivo — `COLS` línea 44, `cell()` línea 60, `columnHeader` línea 280, `dataRows.map` línea 293; patrón de la celda `state` línea 308 como espejo de celda derivada)
    - src/cli/dashboard/format.js (la `taskCell`/`rowCells` extendidos en Task 1 — la fuente del dato de la celda)
    - test/dashboard-table.test.js (líneas 1-70 — harness ink-testing-library, lastFrame, drenado de microtasks)
    - .planning/phases/43-render-provider-state-en-el-dashboard/43-CONTEXT.md §D-01/D-02/D-03 (columna dedicada, posición entre status y age, header 'task')
  </read_first>
  <action>
    Insertar la columna `task` en `SessionTable.js` ENTRE `status` y `age` (orden resultante: gutter · state · task_ref · repo · phasemode · status · TASK · age — D-02). Añadir `task` a la constante `COLS` (línea 44) con width 12 (Claude's Discretion: ~12 para que `in_progress` quepa; `truncate-end` nativo de ink como red de seguridad si desborda — D-08). Añadir la celda de cabecera en `columnHeader` (línea 280) entre la celda `status` y la celda `age`, con label literal `task` y `dimColor` (espejando las demás cabeceras). En `dataRows.map` (línea 293), entre la celda `status` (línea 319) y la celda `age` (línea 320), insertar `cell({ width: COLS.task, text: cells.task.text, dim: cells.task.dim, bold: selected, truncate: true })` — consumiendo `rowCells(session).task` (importar `rowCells` ya existe en línea 25). El `dim` de la celta degradada va a `dimColor` de ink vía el helper `cell()`; el valor ok va en texto plano SIN color propio (D-05: NO una segunda paleta semántica — el red queda reservado al zombie del eje local). Color-isolation: solo `dimColor` de ink, cero picocolors. Extender test/dashboard-table.test.js para cubrir byte-stable: (a) la cabecera incluye `task` entre `status` y `age`; (b) una fila ok renderiza el valor crudo verbatim; (c) una fila unsupported renderiza `—` y una fetch-failed renderiza `?`. Actualizar cualquier snapshot byte-stable existente que la columna nueva desplace.
  </action>
  <verify>
    <automated>node --test test/dashboard-table.test.js test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-table.test.js` exits 0
    - `node --test test/format-isolation.test.js` exits 0 (cero picocolors en los archivos tocados — el walker cubre src/cli/dashboard/ automáticamente)
    - SessionTable.js `COLS` contiene la clave `task`
    - El frame renderizado de la tabla incluye el header `task` posicionado entre `status` y `age`
    - Una fila con provider_state 'in_review' muestra 'in_review' verbatim en la columna task
    - Una fila unsupported muestra '—' y una fetch-failed muestra '?' en la columna task
    - grep gate: `grep -v '^//' src/cli/dashboard/SessionTable.js | grep -c picocolors` == 0
  </acceptance_criteria>
  <done>La columna task se renderiza entre status y age con header 'task'; los 3 reason-states son visualmente distinguibles sin color; tests de tabla e isolation verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /status (server) → dashboard (ink render) | El dashboard LEE `provider_state`/`provider_state_reason` ya enriquecidos por Phase 40. No los computa ni los escribe. |
| provider_state crudo → render de celda | El string del provider se muestra verbatim; podría contener cualquier carácter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | columna task / format.js | mitigate | La columna SOLO lee `provider_state`/`provider_state_reason` ya presentes en las filas de GET /status (Phase 40). Cero campos nuevos, cero acceso a datos que el dashboard no recibiera ya. NO se escribe a state.json (carril read-only — invariante D-09 boundary). |
| T-43-02 | Tampering | render verbatim del string del provider | accept | `provider_state` se muestra verbatim con `truncate-end` (ancho fijo 12); ink no interpreta el string como markup. Un valor inesperado se trunca, no rompe el layout ni inyecta render. Riesgo bajo: el string viene del provider autenticado de Phase 40, no de input del operador. |
| T-43-03 | Denial of Service | celda de ancho fijo | mitigate | width 12 + `wrap:'truncate-end'` acota cualquier string largo del provider; un `provider_state` de 10k chars se trunca a la columna, no desborda la tabla. |
</threat_model>

<verification>
- `node --test test/dashboard-format.test.js` exits 0 (derivación pura de los 3 reason-states + caso 'unknown')
- `node --test test/dashboard-table.test.js` exits 0 (columna renderizada entre status y age, byte-stable)
- `node --test test/format-isolation.test.js` exits 0 (cero picocolors en los archivos tocados)
- Inspección: la columna `task` aparece entre `status` y `age` en el frame; ok-value verbatim, unsupported `—`, fetch-failed `?`
</verification>

<success_criteria>
- PSTATE-05 satisfecho: provider_state renderizado en columna dedicada `task`, separada de la columna `status` (eje local) — criterio 1
- Los tres reason-states distinguibles SIN color (criterio 2): verbatim / `—` dim / `?` dim
- Estado tratado como dato crudo (criterio 4): cero tabla de mapeo, un renombrado se muestra verbatim sin tocar código
- Color-isolation intacta: cero picocolors en src/cli/dashboard/, solo `dimColor` de ink
</success_criteria>

<output>
Create `.planning/phases/43-render-provider-state-en-el-dashboard/43-01-SUMMARY.md` when done
</output>
