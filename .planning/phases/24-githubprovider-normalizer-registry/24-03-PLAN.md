---
phase: 24-githubprovider-normalizer-registry
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/providers/registry.js
  - test/registry.test.js
  - test/check-isolation.test.js
autonomous: true
requirements:
  - GH-04
  - GH-05
  - TEST-01
requirements_addressed:
  - GH-04
  - GH-05
  - TEST-01
tags:
  - github
  - registry
  - invariants
  - log-12

must_haves:
  truths:
    - "getProvider('github') retorna instance que pasa el gate de los 9 TASK_PROVIDER_METHODS sin crashear."
    - "registry.js registra factory 'github' lazy-init después del bloque 'plane', con error isolation (try separado)."
    - "Config v0.6 sin clave 'github' carga sin error (D-31 — el fallo del github factory NO debe abortar el plane factory)."
    - "parseKodoLabels(githubTaskItem.labels.map(name => ({name}))) reconoce 'kodo' y 'kodo:sonnet' (GH-05 invariante / D-32 zero cambios src/labels.js)."
    - "LOG-12: kodo check (src/check.js) NO importa transitivamente src/providers/github/provider.js NI src/providers/github/normalize.js."
    - "ROADMAP.md + REQUIREMENTS.md NO mencionan los métodos fantasma listTasks/listLabels/listStates/transitionTask en líneas funcionales (D-01 doc-correction safety net)."
    - "W6 alignment: VALIDATION.md filas 24-03-01..24-03-03 renumeradas para alinear con PLAN.md tasks 24-03-01..24-03-03 (LOG-12 → GH-05 → registry test). Renumbering aplicado en 24-VALIDATION.md (commit junto a esta revisión); el plan mantiene su orden RED→GREEN→registry."
    - "Project conventions source: .planning/codebase/CONVENTIONS.md (CLAUDE.md project-level not present — checker B4 false-positive)."
  artifacts:
    - path: "src/providers/registry.js"
      provides: "Factory 'github' añadida en registerDefaults, error-isolated"
      contains: "factories.set('github'"
    - path: "test/registry.test.js"
      provides: "Test extendido con caso getProvider('github') via factory real"
      contains: "getProvider('github')"
    - path: "test/check-isolation.test.js"
      provides: "Tests LOG-12 extendidos para github/provider.js + github/normalize.js"
      contains: "providers/github/provider.js"
  key_links:
    - from: "src/providers/registry.js"
      to: "src/providers/github/provider.js"
      via: "dynamic import { createGitHubProvider }"
      pattern: "import.*createGitHubProvider.*github/provider"
    - from: "test/registry.test.js"
      to: "src/providers/github/provider.js"
      via: "dynamic import + registerProvider injection con fakeClient"
      pattern: "createGitHubProvider.*client"
    - from: "test/check-isolation.test.js"
      to: "src/check.js"
      via: "walkImports static graph walk + filter github/*.js"
      pattern: "providers/github/(provider|normalize)\\.js"
---

<objective>
Cerrar Phase 24: registrar la factory `github` en el registry (GH-04), validar invariantes (LOG-12 isolation + GH-05 cross-provider labels), y completar el net de safety D-01 (doc-correction grep).

Purpose: Una vez Wave 1 (normalizer) y Wave 2 (provider) están verdes, este plan **publica** el provider al sistema vía `getProvider('github')` y demuestra que (a) supera el gate de validación del registry, (b) `parseKodoLabels` funciona sobre labels GitHub sin tocar `src/labels.js`, (c) Phase 24 no rompe el árbol de `src/check.js` (LOG-12).

Output:
- `src/providers/registry.js` modificado — block paralelo a 'plane' para 'github' con try/catch aislado (D-29)
- `test/registry.test.js` extendido — caso `getProvider('github')` que invoca el factory real con fakeClient (D-38 variante alternativa fuerte)
- `test/check-isolation.test.js` extendido — 2 filas nuevas para `provider.js` y `normalize.js` (LOG-12 invariante)
- Test de GH-05 (D-32 invariante) integrado en este plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md
@.planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md
@.planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md
@.planning/phases/24-githubprovider-normalizer-registry/24-01-SUMMARY.md
@.planning/phases/24-githubprovider-normalizer-registry/24-02-SUMMARY.md

# Source-of-truth
@src/providers/registry.js
@src/interface.js
@src/labels.js
@test/registry.test.js
@test/check-isolation.test.js

<interfaces>
<!-- Registry public API (src/providers/registry.js). -->
```typescript
function registerProvider(name: string, factory: () => TaskProvider): void;
function getProvider(name: string): TaskProvider;  // throws "Unknown provider" o "missing method: X"
function clearRegistry(): void;
function initRegistry(): Promise<void>;  // dispara registerDefaults internamente
```

<!-- parseKodoLabels REAL shape (src/labels.js:12-37 — GH-05 invariante, zero cambios). -->
<!-- B1 fix: shape ficticio del checker anterior reemplazado por shape REAL verificado en src/labels.js. -->
```typescript
function parseKodoLabels(labels: Array<{name: string}> | any[]): {
  isKodo: boolean;                       // true si alguna label.name === 'kodo' o starts 'kodo:'
  model: 'sonnet' | 'haiku' | null;      // de 'kodo:sonnet' / 'kodo:haiku' (whitelist hard, sin opus)
  flags: string[];                       // resto de 'kodo:<tag>' donde tag NO es sonnet/haiku (e.g. 'gsd', 'gsd-quick', 'review')
};

// getGsdMode(flags: string[]) → 'full' | 'quick' | null
//   flags incluye 'gsd-quick' → 'quick' (precedencia más específica)
//   flags incluye 'gsd'       → 'full'
//   ninguno                   → null

// Internal toLowerCase aplicado a label names — case-insensitive matching.
// Strings sueltos (no objetos) en el array son ignorados (test/labels.test.js:58-61).
```

<!-- LOG-12 invariant: src/check.js cargado por `kodo check` debe ser ligero. -->
<!-- Cualquier import transitivo de github/provider.js (que carga client → config) lo rompe. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 24-03-01: Extender check-isolation.test.js (LOG-12 invariant) — github/provider.js + github/normalize.js fuera del grafo de check.js</name>
  <files>test/check-isolation.test.js</files>
  <read_first>
    - test/check-isolation.test.js (estructura existente — walkImports, IMPORT_FROM_RE, 108 LOC)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"7. test/check-isolation.test.js" (líneas 932-967)
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §LOG-12 invariant en STATE.md cross-phase
    - src/labels.js (parseKodoLabels — leer signature exacto para test de GH-05 in-place)
    - src/check.js (target del walk — confirmar entry point existe)
  </read_first>
  <behavior>
    Añadir 3 tests al describe `'LOG-12: vigilante isolation (import-graph)'` existente:

    1. **`kodo check does not import src/providers/github/provider.js transitively`** — walkImports desde `src/check.js`, filtrar `/providers\/github\/provider\.js$/`, assert deepEqual []. Mensaje de error muestra el path violator + relative graph.

    2. **`kodo check does not import src/providers/github/normalize.js transitively`** — mismo patrón con `/providers\/github\/normalize\.js$/`.

    **Scope del task:** este task añade SOLO las 2 filas LOG-12 al check-isolation.test.js. El test GH-05 (parseKodoLabels invariante con shape REAL `{isKodo, model, flags}`) vive en task 24-03-02 contra `test/labels.test.js` (B1 corrección).
  </behavior>
  <action>
    Editar `test/check-isolation.test.js` añadiendo 2 nuevos `it(...)` blocks dentro del describe `'LOG-12: vigilante isolation (import-graph)'` (después del último test existente, antes del cierre del describe).

    Patrón literal (PATTERNS.md §7 Pattern A líneas 952-963):
    ```
    it('kodo check does not import src/providers/github/provider.js transitively', () => {
      const graph = walkImports(join(SRC, 'check.js'));
      const violators = [...graph].filter((p) => p.endsWith('/providers/github/provider.js'));
      assert.deepEqual(
        violators,
        [],
        `check.js transitively imports github/provider.js via:\n  ${violators.map((p) => relative(REPO, p)).join('\n  ')}`,
      );
    });

    it('kodo check does not import src/providers/github/normalize.js transitively', () => {
      const graph = walkImports(join(SRC, 'check.js'));
      const violators = [...graph].filter((p) => p.endsWith('/providers/github/normalize.js'));
      assert.deepEqual(
        violators,
        [],
        `check.js transitively imports github/normalize.js via:\n  ${violators.map((p) => relative(REPO, p)).join('\n  ')}`,
      );
    });
    ```

    Re-utilizar las constantes `SRC`, `REPO`, helper `walkImports` ya definidas en el archivo. NO duplicar imports — todos están ya disponibles desde el head del archivo.

    Si Phase 23 ya añadió un test similar para `github/client.js`, dejarlo intacto — los nuevos cubren `provider.js` y `normalize.js` específicamente (cargan config + dependencies más pesadas que `client.js` solo).
  </action>
  <verify>
    <automated>node --test test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "providers/github/provider.js" test/check-isolation.test.js` >= 1.
    - `grep -c "providers/github/normalize.js" test/check-isolation.test.js` >= 1.
    - `grep -c "it('kodo check does not import" test/check-isolation.test.js` >= 3 (los 2 nuevos + el existente de logger.js).
    - `node --test test/check-isolation.test.js` exit 0; los 2 tests nuevos GREEN (porque actualmente check.js no toca github/*).
    - Conteo total de `it(` en el archivo >= 5 (los 3-4 originales + los 2 nuevos).
    - Sin cambios a constantes `SRC`/`REPO`/`walkImports` (zero duplication).
  </acceptance_criteria>
  <done>2 tests LOG-12 nuevos añadidos al check-isolation.test.js, GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-03-02: Test GH-05 — parseKodoLabels (shape REAL) reconoce labels GitHub via TaskItem</name>
  <files>test/labels.test.js</files>
  <read_first>
    - test/labels.test.js (estructura existente — 127 LOC; los tests existentes muestran el shape REAL: isKodo/model/flags)
    - src/labels.js (líneas 12-37 parseKodoLabels + líneas 53-58 getGsdMode — shape REAL canonical)
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-32 (invariante GH-05 zero cambios)
    - .planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md (después del renombre W6: fila 24-03-02 = GH-05)
    - src/triggers/dispatcher.js (líneas 65, 74 — uso real `task.labels.map(name => ({name}))`)
  </read_first>
  <behavior>
    Añadir un nuevo `describe('GH-05 — GitHub TaskItem cross-provider (parseKodoLabels invariant)', () => { ... })` block al final de `test/labels.test.js` (después del último describe `'QUICK-08 — getSessionMode 4-state matrix'`).

    **B1 corrección — shape REAL canonical:**

    `parseKodoLabels(labels)` retorna `{ isKodo: boolean, model: 'sonnet'|'haiku'|null, flags: string[] }`. NO existe `result.kodo`, NO existe `result.gsdMode`. El modo GSD se deriva via `getGsdMode(result.flags)` que retorna `'full'|'quick'|null`.

    **Cases obligatorios (importar `parseKodoLabels` y `getGsdMode` desde `../src/labels.js`):**

    1. `it('recognizes kodo label from GitHub-style string labels mapped via dispatcher pattern')`:
       ```js
       const ghLabels = ['kodo'];
       const labelObjs = ghLabels.map((name) => ({ name }));
       const result = parseKodoLabels(labelObjs);
       assert.deepEqual(result, { isKodo: true, model: null, flags: [] });
       ```

    2. `it('extracts model from kodo:sonnet label (GitHub provenance)')`:
       ```js
       const result = parseKodoLabels(['kodo:sonnet'].map((name) => ({ name })));
       assert.equal(result.isKodo, true);
       assert.equal(result.model, 'sonnet');
       assert.deepEqual(result.flags, []);
       ```

    3. `it('detects kodo:gsd-quick → flags contains gsd-quick → getGsdMode returns quick')`:
       ```js
       const result = parseKodoLabels(['kodo:gsd-quick'].map((name) => ({ name })));
       assert.equal(result.isKodo, true);
       assert.deepEqual(result.flags, ['gsd-quick']);
       assert.equal(getGsdMode(result.flags), 'quick');
       ```

    4. `it('detects kodo:gsd → flags contains gsd → getGsdMode returns full')`:
       ```js
       const result = parseKodoLabels(['kodo:gsd'].map((name) => ({ name })));
       assert.equal(result.isKodo, true);
       assert.deepEqual(result.flags, ['gsd']);
       assert.equal(getGsdMode(result.flags), 'full');
       ```

    5. `it('returns isKodo:false for GitHub labels without kodo presence')`:
       ```js
       const result = parseKodoLabels(['bug', 'priority:high'].map((name) => ({ name })));
       assert.deepEqual(result, { isKodo: false, model: null, flags: [] });
       ```

    6. `it('handles empty labels array (defensive)')`:
       ```js
       const result = parseKodoLabels([]);
       assert.deepEqual(result, { isKodo: false, model: null, flags: [] });
       ```

    Cada test demuestra que el dispatcher pattern `task.labels.map(name => ({name}))` funciona idéntico con labels provenientes de GitHub. Cierre GH-05 / D-32 invariante: cero cambios en `src/labels.js`.
  </behavior>
  <action>
    Editar `test/labels.test.js` añadiendo un nuevo bloque `describe` AL FINAL del archivo (después del cierre del describe `'QUICK-08 — getSessionMode 4-state matrix'`).

    Imports — el archivo ya importa `parseKodoLabels, getGsdMode, getSessionMode` (línea 3). Verificar que `getGsdMode` ya está disponible; NO duplicar imports.

    Bloque a añadir (literal, sin placeholders):
    ```javascript
    // GH-05 invariant: src/labels.js es provider-agnostic; dispatcher.js:65,74 hace
    // .map(name => ({name})) — Phase 24 valida que el flow funciona sin tocar labels.js.
    describe('GH-05 — GitHub TaskItem cross-provider (parseKodoLabels invariant)', () => {
      it('recognizes kodo label from GitHub-style string labels mapped via dispatcher pattern', () => {
        const labelObjs = ['kodo'].map((name) => ({ name }));
        const result = parseKodoLabels(labelObjs);
        assert.deepEqual(result, { isKodo: true, model: null, flags: [] });
      });

      it('extracts model from kodo:sonnet label (GitHub provenance)', () => {
        const result = parseKodoLabels(['kodo:sonnet'].map((name) => ({ name })));
        assert.equal(result.isKodo, true);
        assert.equal(result.model, 'sonnet');
        assert.deepEqual(result.flags, []);
      });

      it('detects kodo:gsd-quick → flags has gsd-quick → getGsdMode returns quick', () => {
        const result = parseKodoLabels(['kodo:gsd-quick'].map((name) => ({ name })));
        assert.equal(result.isKodo, true);
        assert.deepEqual(result.flags, ['gsd-quick']);
        assert.equal(getGsdMode(result.flags), 'quick');
      });

      it('detects kodo:gsd → flags has gsd → getGsdMode returns full', () => {
        const result = parseKodoLabels(['kodo:gsd'].map((name) => ({ name })));
        assert.equal(result.isKodo, true);
        assert.deepEqual(result.flags, ['gsd']);
        assert.equal(getGsdMode(result.flags), 'full');
      });

      it('returns isKodo:false for GitHub labels without kodo presence', () => {
        const result = parseKodoLabels(['bug', 'priority:high'].map((name) => ({ name })));
        assert.deepEqual(result, { isKodo: false, model: null, flags: [] });
      });

      it('handles empty labels array (defensive)', () => {
        const result = parseKodoLabels([]);
        assert.deepEqual(result, { isKodo: false, model: null, flags: [] });
      });
    });
    ```

    **B1 critical:** los asserts usan `isKodo` (NO `kodo`), `flags` array (NO `gsdMode` campo), y `getGsdMode(flags)` para derivar el modo. Esto es el shape REAL verificado en `src/labels.js:12-37` y `test/labels.test.js:1-67`.
  </action>
  <verify>
    <automated>node --test test/labels.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "GH-05|GitHub.*cross-provider" test/labels.test.js` >= 1 (comment + describe name).
    - `grep -cE "\.map\(\(name\) => \(\{ name \}\)\)|\.map\(name => \(\{name\}\)\)" test/labels.test.js` >= 5 (dispatcher pattern en ≥5 tests).
    - `grep -c "getGsdMode(result.flags)" test/labels.test.js` >= 2 (B1: tests usan getGsdMode con shape real, NO result.gsdMode ficticio).
    - `grep -cE "result\.isKodo|isKodo:\s*true|isKodo:\s*false" test/labels.test.js` >= 6 (B1: shape real isKodo, NO result.kodo).
    - `grep -c "result.kodo,\|result.gsdMode" test/labels.test.js` == 0 (B1 hard-gate: shape ficticio del checker anterior NO debe aparecer).
    - `grep -c "it(" test/labels.test.js` >= (count previo + 6) — añadir 6 tests al baseline existente.
    - `node --test test/labels.test.js` exit 0; los 6 tests nuevos GREEN; los existentes siguen GREEN.
    - `git diff src/labels.js` returns empty (D-32 zero cambios — INVARIANTE BLOQUEANTE).
    - `git diff src/triggers/dispatcher.js` returns empty (Phase 24 NO toca dispatcher).
  </acceptance_criteria>
  <done>6 tests GH-05 invariant añadidos a labels.test.js con shape REAL (isKodo/model/flags + getGsdMode), GREEN; src/labels.js sin modificar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-03-03: Extender registry.test.js con caso getProvider('github') via factory real</name>
  <files>test/registry.test.js</files>
  <read_first>
    - test/registry.test.js (estructura existente — 84 LOC, ya incluye `createFakeProvider` helper)
    - src/providers/registry.js (factories Map, registerProvider, getProvider gate de 9 métodos)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"6. test/registry.test.js" Pattern A (líneas 886-928) — variante alternativa fuerte
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-38 (test de github registry)
    - .planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md fila 24-03-01
    - src/providers/github/provider.js (creado en Wave 2 — factory real)
    - test/providers/github/provider.test.js (referencia para makeFakeClient pattern reutilizable)
  </read_first>
  <behavior>
    Añadir 2 tests al describe `'Provider Registry'` existente:

    1. **`getProvider("github") via createGitHubProvider passes TASK_PROVIDER_METHODS gate`** (PATTERNS.md alternativa fuerte / D-38):
       - Dynamic import del factory real `createGitHubProvider`.
       - Construir un fakeClient minimal (5 métodos stubeados — reutilizar o re-crear como helper local).
       - `registerProvider('github', () => createGitHubProvider(MOCK_GITHUB_CONFIG, {client: fakeClient}))`.
       - `const provider = getProvider('github')`.
       - Loop sobre `TASK_PROVIDER_METHODS`: `assert.equal(typeof provider[method], 'function', \`Missing method: \${method}\`)`.

    2. **`getProvider("github") caches instance (singleton)`** — reutilizar el patrón ya existente para 'test' (registry.test.js:39-45), aplicado a 'github' con factory real.

    Este test es **la prueba definitiva de GH-04**: si el provider real cumple el gate, el registry funciona end-to-end con GitHub.

    Importante: NO disparar `registerDefaults` real (porque haría `loadConfig()` que falla sin config.json válido con `providers.github`). El test usa `registerProvider` directo (manual injection) en lugar de invocar `initRegistry`.
  </behavior>
  <action>
    Editar `test/registry.test.js` añadiendo 2 nuevos `it(...)` blocks dentro del describe `'Provider Registry'` (después del último test existente, antes del cierre del describe).

    Antes de los nuevos tests, definir constantes locales al describe:
    ```javascript
    /** Mock minimal GitHub config (snake_case — raw del registry, D-29). */
    const MOCK_GITHUB_CONFIG = {
      base_url: 'https://api.github.com',
      api_key_env: 'GITHUB_TOKEN',
      repos: [{owner: 'octocat', repo: 'hello-world'}],
      states: {trigger: 'open', review: 'closed', done: 'closed'},
    };

    /** Stub minimal del GitHubClient (5 métodos consumidos por createGitHubProvider). */
    function createFakeGitHubClient() {
      return {
        async getIssue() { return { node_id: 'I_x', number: 1, title: '', body: '', labels: [], state: 'open', html_url: '' }; },
        async listIssues() { return { status: 200, items: [], etag: '', rate_limit_remaining: 5000 }; },
        async addComment() { return { id: 1 }; },
        async updateIssue(o, r, n, u) { return { number: n, state: u.state }; },
        async listLabels() { return []; },
      };
    }
    ```

    Tests nuevos:
    ```javascript
    it('getProvider("github") via createGitHubProvider passes TASK_PROVIDER_METHODS gate', async () => {
      const { createGitHubProvider } = await import('../src/providers/github/provider.js');
      registerProvider('github', () =>
        createGitHubProvider(MOCK_GITHUB_CONFIG, { client: createFakeGitHubClient() }),
      );
      const provider = getProvider('github');
      for (const method of TASK_PROVIDER_METHODS) {
        assert.equal(typeof provider[method], 'function', `Missing method: ${method}`);
      }
    });

    it('getProvider("github") returns cached singleton across calls', async () => {
      const { createGitHubProvider } = await import('../src/providers/github/provider.js');
      registerProvider('github', () =>
        createGitHubProvider(MOCK_GITHUB_CONFIG, { client: createFakeGitHubClient() }),
      );
      const first = getProvider('github');
      const second = getProvider('github');
      assert.equal(first, second, 'Expected same instance (singleton)');
    });
    ```

    El `beforeEach` existente (`clearRegistry()` + dynamic import del módulo) ya cubre el setup.
  </action>
  <verify>
    <automated>node --test test/registry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getProvider..github.." test/registry.test.js` >= 2 (los 2 tests nuevos).
    - `grep -c "createGitHubProvider" test/registry.test.js` >= 2 (dynamic import en ambos tests).
    - `grep -c "createFakeGitHubClient" test/registry.test.js` >= 3 (definición + 2 usos).
    - `grep -c "TASK_PROVIDER_METHODS" test/registry.test.js` >= 2 (existente + nuevo loop).
    - `node --test test/registry.test.js` exit 0; los 2 tests nuevos GREEN; los 5 existentes siguen GREEN.
    - `git diff src/providers/registry.js` aún vacío en este momento (este task NO modifica registry.js — eso es task 24-03-04).
  </acceptance_criteria>
  <done>2 tests `getProvider('github')` añadidos via factory real + fakeClient; GREEN sin necesidad de modificar registry.js todavía.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-03-04: Añadir bloque factory 'github' en registry.js (D-29) con try aislado</name>
  <files>src/providers/registry.js</files>
  <read_first>
    - src/providers/registry.js (estructura actual — `registerDefaults` async function con try plane, 102 LOC)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"3. src/providers/registry.js (MOD)" Pattern A (líneas 538-595)
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-29 (block paralelo), §D-30 (sin cambios de lógica), §D-31 (sin cambios config.js)
    - .planning/phases/24-githubprovider-normalizer-registry/24-RESEARCH.md §"Common Pitfalls" Pitfall #6 — registry try/catch swallow oculta errores
    - test/registry.test.js (los tests existentes deben seguir GREEN tras este cambio)
  </read_first>
  <behavior>
    Modificar `src/providers/registry.js:registerDefaults` añadiendo un **try/catch separado** del bloque `plane` para el factory `github` (D-29 + Pitfall #6: aislar fallos para que un error de import en github NO aborte el registro de plane).

    Comportamiento esperado:
    - `await initRegistry()` (o disparo lazy vía `getProvider('plane')`) → ambos factories registrados.
    - Si `import('./github/provider.js')` falla (ej. módulo no existe aún en una checkout previa), `factories.set('plane', ...)` debe haberse ejecutado igualmente.
    - El factory `github` consume el sub-objeto raw `config.providers.github` (snake_case `base_url`/`api_key_env`/`repos`/`states`) — NO transforma a camelCase (divergencia justificada vs plane block).
    - NO se pasa `getProviderApiKey('github')` aquí — el `GitHubClient` constructor (Phase 23) lo resuelve internamente cuando `token === undefined`.
    - NO se pasa `webhookSecret` — D-27 verifySignature es no-op, sin HMAC.
    - Sin logger construido aquí (precedente PlaneProvider — callers pasan logger via opts).
  </behavior>
  <action>
    Editar `src/providers/registry.js:registerDefaults` añadiendo un nuevo `try/catch` block después del cierre del try existente de plane (línea ~42).

    Patrón exacto (PATTERNS.md líneas 575-588 + ajuste defensivo D-31):
    ```javascript
    try {
      const { loadConfig } = await import('../config.js');
      const { createGitHubProvider } = await import('./github/provider.js');

      factories.set('github', () => {
        const config = loadConfig();
        const github = config.providers?.github;
        // D-31: si config v0.6 sin clave github, el factory aún se registra pero
        // construir el provider en getProvider('github') fallará con mensaje canónico
        // del GitHubClient constructor (Phase 23 D-04). Comportamiento aceptable:
        // Phase 24 verde implica config con github presente.
        return createGitHubProvider(github);
        // NO se transforma config (D-29) — el factory consume snake_case raw.
        // logger se inyecta vía opts en callers (precedente Plane).
      });
    } catch {
      // Config or provider module not available — skip github registration
    }
    ```

    **Crítico:**
    - Este try es **separado** del try de plane (Pitfall #6 — fail isolation).
    - `loadConfig` se re-importa (no se reutiliza del try anterior) porque cada try debe ser auto-contenido en caso de error del primero.
    - `config.providers?.github` con optional chaining (D-31 — no crashea si la clave falta).

    Tras el cambio, agregar comment header al `registerDefaults` mencionando los 2 providers default registrados (plane + github).

    **Verificación previa al commit:** ejecutar el test extendido del task 24-03-03 — debe SEGUIR verde sin cambios (los tests inyectan el provider manualmente, no dependen de `registerDefaults`). Los tests existentes del registry (test, bad, unknown, clearRegistry, singleton) tampoco deben romperse.
  </action>
  <verify>
    <automated>node --test test/registry.test.js test/providers/github/provider.test.js test/providers/github/normalize.test.js test/check-isolation.test.js test/labels.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "factories.set('github'" src/providers/registry.js` == 1.
    - `grep -c "createGitHubProvider" src/providers/registry.js` == 1 (single dynamic import).
    - `grep -cE "try \{" src/providers/registry.js` >= 2 (try aislado plane + try aislado github — W10: regex sin anchor de indent para tolerar refactor de whitespace).
    - `grep -c "config.providers?.github\|config.providers\.github" src/providers/registry.js` >= 1.
    - `grep -c "getProviderApiKey.*github\|webhook_secret.*github\|webhookSecret.*github" src/providers/registry.js` == 0 (no transformación adicional — D-29).
    - `node --test test/registry.test.js` exit 0; todos los tests GREEN incluyendo los 2 nuevos.
    - `node --test test/providers/github/provider.test.js test/providers/github/normalize.test.js` siguen GREEN (Wave 1 + 2 invariantes).
    - `node --test test/check-isolation.test.js` GREEN — añadir un import al registry NO debe romper LOG-12 porque el path de check.js no toca el registry.
    - `node --test test/plane-provider.test.js` GREEN — invariante cero cambios a Plane registration.
    - `node --test test/labels.test.js` GREEN — GH-05 invariante.
    - El bloque github es estructuralmente posterior al bloque plane en el orden del archivo (parseable visualmente).
  </acceptance_criteria>
  <done>registry.js registra factory 'github' con try/catch aislado; suite completa Phase 24 GREEN; zero regresiones en Plane/labels/check-isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-03-05: D-01 doc-correction safety net + suite global verde</name>
  <files>
    .planning/phases/24-githubprovider-normalizer-registry/24-03-SUMMARY.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (verificar que ya no quedan menciones funcionales a métodos fantasma — corrección 2026-05-14)
    - .planning/REQUIREMENTS.md (mismo)
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-01 (background del rewrite)
    - .planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md §Manual-Only Verifications (D-01 doc grep)
  </read_first>
  <behavior>
    Verificación final del plan:

    1. **Suite global verde:** `npm test` exit 0 con baseline v0.6 + Phase 24 tests añadidos (target: ≥ baseline + ~25 tests nuevos).

    2. **D-01 doc-correction safety net (grep):** validar que ROADMAP.md y REQUIREMENTS.md NO mencionan los 4 métodos fantasma (`listTasks`, `listLabels`, `listStates`, `transitionTask`) en líneas FUNCIONALES del contrato. Matches aceptables: líneas con marcadores históricos (`_Corregido 2026-05-14...`, `la lista original era fantasía...`) que explícitamente documentan el error pasado. Cero matches en frases de definición.

    3. **Crear SUMMARY del plan** documentando cierre de Phase 24.
  </behavior>
  <action>
    1. Ejecutar `npm test` en el repo. Si falla, identificar regresiones y fixearlas ANTES de continuar. El criterio de éxito es exit code 0.

    2. Ejecutar `grep -nE "listTasks|listLabels|listStates|transitionTask" .planning/ROADMAP.md .planning/REQUIREMENTS.md`. Analizar cada match:
       - Si la línea contiene `Corregido`, `fantasía`, `error`, `original`, `rechazaría` (marcador histórico) → OK, dejar.
       - Si la línea define el contrato como si fuera válido (sin marcador histórico) → FIXEAR la línea con un commit `docs(24-03): close D-01 doc-correction remnant`.

    3. Crear `.planning/phases/24-githubprovider-normalizer-registry/24-03-SUMMARY.md` documentando:
       - `getProvider('github')` GREEN — gate de 9 métodos pasado por factory real
       - registry.js modificado con try aislado (LOC delta + diff summary)
       - check-isolation.test.js extendido (2 filas LOG-12 nuevas)
       - labels.test.js extendido (5 tests GH-05 invariant)
       - registry.test.js extendido (2 tests singleton + contract via factory real)
       - Suite global: baseline v0.6 (614 tests) + Phase 24 delta (estimate count from npm test output) = total final
       - D-01 safety net: greps ejecutados, status (clean / fixed N líneas / N/A si ya estaban limpios)
       - Invariantes preservados: zero diff en `src/labels.js`, `src/triggers/dispatcher.js`, `src/providers/plane/*`, `src/interface.js`
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exit 0 (suite global verde).
    - `grep -nE "listTasks|listLabels|listStates|transitionTask" .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -vE "Corregido|fantasía|original|rechazaría|error|incorrect"` returns 0 lines (no remnants funcionales).
    - `git diff --name-only src/labels.js src/triggers/dispatcher.js src/interface.js src/providers/plane/` returns empty (invariantes preservados — D-32 + STATE.md cross-phase).
    - `.planning/phases/24-githubprovider-normalizer-registry/24-03-SUMMARY.md` existe con las secciones listadas en `<action>`.
    - El test count global de npm test es ≥ 614 (baseline v0.6) + 20 (estimate Phase 24 deltas: 12 normalize + 12 provider + 2 registry + 5 labels + 2 check-isolation = 33 ish, range 25-40).
  </acceptance_criteria>
  <done>Suite global verde, D-01 safety net validado, SUMMARY de cierre creado, invariantes preservados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → dynamic import('./github/provider.js') | Provider module load — fallar aquí no debe abortar plane registration (Pitfall #6) |
| Caller → getProvider('github') | Gate de 9 métodos valida shape antes de exponer instance — defensa frente a refactor que rompa contrato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-R-01 | T (Tampering / Contract) | getProvider gate | mitigate | `TASK_PROVIDER_METHODS` frozen array enforcement (registry.js:73-77 existente) + test 24-03-03 ejerce el gate con factory real. Refactor que rompa los 9 métodos rompe los tests RED loud. |
| T-24-R-02 | D (Denial of Service / Boot failure) | registerDefaults | mitigate | Try/catch aislado per-provider (Pitfall #6) — fallo del github dynamic import no aborta plane registration. Test fixture verifica que `kodo` arranca con config v0.6 (sin github) idéntico. |
| T-24-R-03 | T (Doc drift) | ROADMAP / REQUIREMENTS | mitigate | Task 24-03-05 grep safety net. Matches funcionales (sin marcador histórico) son FAIL — el plan los fixea antes de cerrar. |
| T-24-R-04 | I (Information Disclosure / Internal isolation) | LOG-12 invariant | mitigate | Tests 24-03-01 walkImports verifican que `kodo check` NO carga github/provider.js (que carga config.js + logger via opts) ni normalize.js. Mantiene `kodo check` light-weight como en v0.5. |

Phase 24 no introduce nuevos vectores de seguridad. Los threats son sobre la invariante del contrato (registry gate) y aislación de módulos (LOG-12). Sin threats high-severity.
</threat_model>

<verification>
- `node --test test/registry.test.js` → GREEN, 7 tests (5 existentes + 2 nuevos getProvider github)
- `node --test test/check-isolation.test.js` → GREEN, ≥5 tests (3-4 existentes + 2 nuevos github isolation)
- `node --test test/labels.test.js` → GREEN, count previo + 5 (GH-05 invariant)
- `node --test test/providers/github/provider.test.js test/providers/github/normalize.test.js` → GREEN (Waves 1+2 invariantes)
- `node --test test/plane-provider.test.js test/normalize.test.js` → GREEN (Plane invariantes zero cambios)
- `npm test` global → exit 0, total tests ≥ baseline v0.6 + Phase 24 deltas
- `git diff src/labels.js src/triggers/dispatcher.js src/interface.js src/providers/plane/` → empty (invariantes locked)
- D-01 grep safety net → 0 matches funcionales en ROADMAP/REQUIREMENTS
</verification>

<success_criteria>
- `getProvider('github')` valida los 9 métodos del contrato y retorna provider funcional (GH-04)
- `parseKodoLabels` reconoce labels GitHub vía dispatcher pattern sin cambios a `src/labels.js` (GH-05 / D-32)
- LOG-12 invariant extendido: `kodo check` no toca github/provider.js ni github/normalize.js
- registry.js modificado con try aislado (Pitfall #6) — plane registration robusto a fallos de github
- D-01 doc-correction safety net pasado (zero remnants funcionales en ROADMAP/REQUIREMENTS)
- Suite global `npm test` exit 0, sin regresiones
- 24-03-SUMMARY.md creado documentando el cierre de Phase 24
</success_criteria>

<output>
After completion, create `.planning/phases/24-githubprovider-normalizer-registry/24-03-SUMMARY.md` documentando:
- registry.js diff summary (líneas añadidas, ubicación del bloque github vs plane)
- Total de tests nuevos en Phase 24 (suma de los 3 plans)
- Estado de invariantes (GH-05 zero cambios, LOG-12, Plane unchanged, dispatcher unchanged, interface.js unchanged)
- Verificación D-01 doc-correction (grep result)
- Próximo phase: Phase 25 Polling Trigger Channel (POLL-01..04 + TEST-02)
</output>
