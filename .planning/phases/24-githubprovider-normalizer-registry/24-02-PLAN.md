---
phase: 24-githubprovider-normalizer-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/providers/github/provider.js
  - test/providers/github/provider.test.js
autonomous: true
requirements:
  - GH-02
  - GH-03
  - TEST-01
requirements_addressed:
  - GH-02
  - TEST-01
tags:
  - github
  - provider
  - taskprovider-contract

must_haves:
  truths:
    - "createGitHubProvider(config, opts?) retorna objeto con los 9 métodos TASK_PROVIDER_METHODS como functions."
    - "init() es no-op (cero API calls, no throw)."
    - "getTask('owner/repo#N') parsea ref, llama client.getIssue, retorna TaskItem normalizado."
    - "resolveRef('owner/repo#N') retorna node_id del issue (D-21)."
    - "parseRef rechaza KL-42, #42, URLs con throw `Invalid GitHub ref: ...`."
    - "updateTaskState passthrough hard: 'open'/'closed' directo, otros buscan en config.states, unknown → throw."
    - "addComment postea Markdown literal (sin HTML wrap como Plane — D-24)."
    - "listPendingTasks itera config.repos, server-side filter labels=kodo+state=open, filtra PRs (Pitfall #2), retorna TaskItem[]."
    - "parseTriggerEvent retorna null determinístico (D-26 GitHub polling-only)."
    - "verifySignature retorna false determinístico (D-27)."
    - "listProjects retorna config.repos.map(...) — cero API calls (D-28)."
    - "opts.client inyectable para tests (D-36); sin inyección construye new GitHubClient(...)."
    - "Live-fetch leak guard: si test olvida inyectar opts.client, globalThis.fetch lanza loud (D-37)."
  artifacts:
    - path: "src/providers/github/provider.js"
      provides: "createGitHubProvider factory + 9 métodos TaskProvider"
      exports: ["createGitHubProvider"]
      min_lines: 140
    - path: "test/providers/github/provider.test.js"
      provides: "Contract tests + per-method coverage con fakeClient injection"
      min_lines: 200
  key_links:
    - from: "src/providers/github/provider.js"
      to: "src/providers/github/client.js"
      via: "import { GitHubClient }"
      pattern: "import.*GitHubClient.*client"
    - from: "src/providers/github/provider.js"
      to: "src/providers/github/normalize.js"
      via: "import { normalizeIssue }"
      pattern: "import.*normalizeIssue.*normalize"
    - from: "test/providers/github/provider.test.js"
      to: "src/providers/github/provider.js"
      via: "dynamic import + opts.client injection"
      pattern: "createGitHubProvider.*client.*makeFakeClient"
    - from: "test/providers/github/provider.test.js"
      to: "src/interface.js"
      via: "import { TASK_PROVIDER_METHODS }"
      pattern: "TASK_PROVIDER_METHODS"
---

<objective>
Implementar el `createGitHubProvider(config, opts?)` factory con los 9 métodos REALES del contrato `TaskProvider` (D-01) + suite de tests contract con `fakeClient` injection (D-36) y live-fetch leak guard (D-37).

Purpose: GH-02 / TEST-01 — el provider satisface el gate `TASK_PROVIDER_METHODS` del registry (Wave 3 lo registra), consume el normalizer de Wave 1 y el `GitHubClient` shipped en Phase 23. parseTriggerEvent/verifySignature no-op funcional (GitHub polling-only en v0.7).

Output:
- `src/providers/github/provider.js` (~150-180 LOC) — factory + 9 métodos
- `test/providers/github/provider.test.js` (~250 LOC) — contract + per-method tests con fakeClient injection
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md
@.planning/phases/24-githubprovider-normalizer-registry/24-RESEARCH.md
@.planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md
@.planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md
@.planning/phases/24-githubprovider-normalizer-registry/24-01-SUMMARY.md

# Source-of-truth para contrato y patterns directos
@src/interface.js
@src/providers/plane/provider.js
@src/providers/github/client.js
@src/providers/github/normalize.js
@test/plane-provider.test.js
@test/providers/github/client.test.js

<interfaces>
<!-- TaskProvider canonical contract (src/interface.js:36-46) — los 9 métodos REALES (D-01). -->
```typescript
type TaskProvider = {
  init: () => Promise<void>;
  getTask: (ref: string) => Promise<TaskItem>;
  updateTaskState: (task: TaskItem, stateName: string) => Promise<void>;
  addComment: (task: TaskItem, markdownText: string) => Promise<void>;
  listPendingTasks: () => Promise<TaskItem[]>;
  parseTriggerEvent: (rawPayload: object) => TriggerEvent | null;  // D-26 → null
  verifySignature: (rawBody: string, headers: object) => boolean;  // D-27 → false
  resolveRef: (humanRef: string) => Promise<string>;
  listProjects: () => Promise<Array<{id: string, identifier: string, name: string}>>;
};

// TASK_PROVIDER_METHODS frozen array — gate del registry valida estos 9 nombres.
```

<!-- GitHubClient (Phase 23) — 5 métodos públicos consumidos por el provider. -->
```typescript
class GitHubClient {
  constructor(opts: { baseUrl?: string, token?: string, logger?: Logger, fetch?: fetch });
  async getIssue(owner: string, repo: string, number: number): Promise<Issue>;
  async listIssues(owner: string, repo: string, opts?: {labels?: string[], state?: string, since?: string, etag?: string}): Promise<{status: number, items: Issue[], etag: string, rate_limit_remaining: number}>;
  async addComment(owner: string, repo: string, number: number, body: string): Promise<{id: number}>;
  async updateIssue(owner: string, repo: string, number: number, updates: {state?: string, ...}): Promise<Issue>;
  async listLabels(owner: string, repo: string): Promise<Label[]>;
}
// Errores: throw Error con .code='not_found'|'unauthorized'|'rate_limit_exceeded' (Phase 23 D-12).
```

<!-- normalizeIssue (Wave 1) — consumed por getTask y listPendingTasks. -->
```typescript
function normalizeIssue(issue: object, context: {projectId: string, baseUrl?: string}): TaskItem;
```

<!-- GitHubProviderConfig — snake_case (raw del config, sin transformación registry, D-29). -->
```typescript
type GitHubProviderConfig = {
  base_url: string;
  api_key_env: string;
  repos: Array<{owner: string, repo: string}>;
  states: {trigger: string, review: string, done: string};
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 24-02-01: Escribir provider.test.js (RED) — contract + 9 métodos + leak guard</name>
  <files>test/providers/github/provider.test.js</files>
  <read_first>
    - test/plane-provider.test.js (analog directo — patrón contract + per-method, líneas 1-125)
    - test/providers/github/client.test.js (analog secundario — makeFakeClient pattern + live-fetch leak guard líneas 32-43, 79-103)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"4. test/providers/github/provider.test.js" Patterns A-E (líneas 599-749)
    - .planning/phases/24-githubprovider-normalizer-registry/24-VALIDATION.md §Per-Task Verification Map filas 24-02-01..24-02-12
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-19..D-28 (los 9 métodos), D-36..D-37 (test infra)
    - src/interface.js (TASK_PROVIDER_METHODS — gate canonical)
  </read_first>
  <behavior>
    Test file estructura:

    1. **Top-level imports + dynamic load del SUT** (Pattern A): describe/it/before/after de `node:test`, assert/strict, TASK_PROVIDER_METHODS desde `../../../src/interface.js`, dynamic import del factory en `beforeEach`.

    2. **Live-fetch leak guard** (Pattern D, D-37): `before` overrides `globalThis.fetch` para throw `live fetch leak: ...`; `after` restaura el original. Si algún test olvida inyectar `opts.client`, revienta loud.

    3. **makeFakeClient helper** (Pattern C, D-36): construye stub con los 5 métodos del GitHubClient, captura llamadas en `calls[methodName]`, soporta `overrides` per-método para casos específicos.

    4. **Contract test** (Pattern B / 24-02-01): `createGitHubProvider(MOCK_CONFIG, {client: makeFakeClient()})` retorna objeto con los 9 métodos del `TASK_PROVIDER_METHODS` como functions. Loop sobre el frozen array.

    5. **Per-method tests** (Pattern E / 24-02-02..24-02-11) — uno por método:
       - 24-02-02 / `init()`: invocar, no throw, cero calls al fakeClient
       - 24-02-03 / `getTask('octocat/hello-world#42')`: fakeClient.getIssue invocado con `('octocat','hello-world',42)`, retorna TaskItem normalizado (asserts sobre `id`/`ref`/`projectId`)
       - 24-02-04 / `resolveRef('octocat/hello-world#42')`: retorna `'I_kwTEST001'` (node_id del fixture)
       - 24-02-05 / `parseRef` rechaza: `getTask('KL-42')` throw `Invalid GitHub ref: KL-42. Expected owner/repo#number`; mismo para `#42`, `https://github.com/...`, `octocat/hello-world` (sin #N)
       - 24-02-06 / `updateTaskState(task, 'closed')`: fakeClient.updateIssue invocado con `{state: 'closed'}`; mismo para `'open'`; `updateTaskState(task, 'Done')` con config.states `{done:'closed'}` → passthrough `'closed'`; `updateTaskState(task, 'whatever')` con whatever NO en config.states → throw `Unknown state: whatever`
       - 24-02-07 / `addComment(task, '**md**')`: fakeClient.addComment invocado con body literal `'**md**'` (NO `<p><br>` wrap)
       - 24-02-08 / `listPendingTasks()`: invocar por repo, fakeClient.listIssues con `{labels:['kodo'], state:'open'}`, filtrar `pull_request != null` (test con un items list que contenga PR + issue), retornar normalizado
       - 24-02-09 / `parseTriggerEvent({...anything...})` → `null`; también `parseTriggerEvent(null)` → `null`
       - 24-02-10 / `verifySignature('body', {})` → `false`; `verifySignature('', {'x-github-signature': 'whatever'})` → `false`
       - 24-02-11 / `listProjects()`: retorna `config.repos.map(r => ({id, identifier, name}))`; assert `fakeClient.calls.listIssues.length === 0` después (cero API calls)

    6. **MOCK_CONFIG** snake_case (D-29):
       ```javascript
       const MOCK_CONFIG = {
         base_url: 'https://api.github.com',
         api_key_env: 'GITHUB_TOKEN',
         repos: [{owner: 'octocat', repo: 'hello-world'}],
         states: {trigger: 'open', review: 'closed', done: 'closed'},
       };
       ```
  </behavior>
  <action>
    Crear `test/providers/github/provider.test.js` (~250 LOC) siguiendo PATTERNS.md §Patterns A-E líneas 604-749.

    Imports + setup:
    ```
    // @ts-check
    import { describe, it, beforeEach, before, after } from 'node:test';
    import assert from 'node:assert/strict';
    import { TASK_PROVIDER_METHODS } from '../../../src/interface.js';
    import issueFixture from '../../fixtures/github/issue.json' with { type: 'json' };
    import issuesListFixture from '../../fixtures/github/issues-list.json' with { type: 'json' };
    ```

    NO importar `createHmac` (D-27 verifySignature → false, sin HMAC).

    Estructura `makeFakeClient` (Pattern C — copiar de PATTERNS.md líneas 676-703 literal, ajustando paths). Cada método del fake:
    - Push a `calls[name]` el argumento recibido
    - Si `overrides[name]` es función, ejecutarla y retornar su resultado
    - Default returns: getIssue → `{node_id:'I_kwTEST001', number, title:'t', body:'', labels:[], state:'open', html_url:''}`; listIssues → `{status:200, items:[], etag:undefined}`; addComment → `{id:1}`; updateIssue → `{number, state: updates.state}`; listLabels → `[]`.

    Estructura `before/after` (Pattern D — copiar literal de client.test.js:32-43):
    ```
    const _originalFetch = globalThis.fetch;
    before(() => { globalThis.fetch = () => { throw new Error('live fetch leak: test must inject opts.client'); }; });
    after(() => { globalThis.fetch = _originalFetch; });
    ```

    `beforeEach(async () => { ({ createGitHubProvider } = await import('../../../src/providers/github/provider.js')); });`

    Para test 24-02-08 listPendingTasks con PR filtering: usar override de listIssues que retorne `{status:200, items:[issueFixture, {...issueFixture, pull_request:{url:'...'}, number:50, node_id:'I_PR050'}], etag:''}`. Assert que el resultado tiene 1 elemento (PR filtrado), con id='I_kwTEST001'.

    Para test 24-02-06 updateTaskState con state desconocido: invocar `updater.updateTaskState({ref:'octocat/hello-world#42', projectId:'octocat/hello-world'}, 'NoSuchState')`. Capturar throw con `assert.throws(..., {message: /Unknown state: NoSuchState/})`.

    Mensajes de assert descriptivos referenciando IDs (`D-19 init no-op`, `D-23 passthrough`, etc.).

    Este task escribe los tests ANTES del provider (RED — espera fallar con "Cannot find module").
  </action>
  <verify>
    <automated>node --test test/providers/github/provider.test.js 2>&1 | grep -E "Cannot find module|ERR_MODULE_NOT_FOUND" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `test/providers/github/provider.test.js` existe.
    - `grep -c "it(" test/providers/github/provider.test.js` >= 12 (contract + 11 per-method coverage).
    - `grep -c "TASK_PROVIDER_METHODS" test/providers/github/provider.test.js` >= 1 (contract test loop).
    - `grep -c "live fetch leak" test/providers/github/provider.test.js` == 1 (D-37 guard message).
    - `grep -c "makeFakeClient" test/providers/github/provider.test.js` >= 5 (helper definido + usado en múltiples tests).
    - `grep -c "createHmac\|x-plane-signature\|webhookSecret" test/providers/github/provider.test.js` == 0 (D-26/D-27 no-op — no hay HMAC en GitHub).
    - Ejecutar `node --test test/providers/github/provider.test.js` falla con "Cannot find module './provider.js'" o equivalente (RED esperado).
    - Imports: `TASK_PROVIDER_METHODS`, fixtures con `with { type: 'json' }`, paths con `../../../src/...` correctos.
  </acceptance_criteria>
  <done>Test file completo con ≥12 tests, leak guard, makeFakeClient injection, dynamic SUT load; ejecuta RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 24-02-02: Implementar provider.js (GREEN) — factory + 9 métodos</name>
  <files>src/providers/github/provider.js</files>
  <read_first>
    - src/providers/plane/provider.js (template estructural 1:1 — leer en full para Patterns C-M, 253 LOC)
    - src/providers/github/client.js (Phase 23 — constructor + 5 métodos públicos)
    - src/providers/github/normalize.js (Wave 1 — normalizeIssue consumido)
    - .planning/phases/24-githubprovider-normalizer-registry/24-PATTERNS.md §"1. src/providers/github/provider.js" Patterns A-M (líneas 36-382)
    - .planning/phases/24-githubprovider-normalizer-registry/24-CONTEXT.md §D-01..D-28, §D-39..D-42 (header doc, opts.client typed, orden métodos)
    - test/providers/github/provider.test.js (creado en task previo — spec ejecutable)
    - src/interface.js (TaskProvider typedef + TASK_PROVIDER_METHODS)
  </read_first>
  <behavior>
    Export: `createGitHubProvider(config, opts?)` (Pattern C / D-02 / D-36).

    Estructura del factory:
    1. Header doc (Pattern A / D-39): 10-15 líneas describiendo D-01 (contrato real), D-19 (init no-op), D-24 (markdown literal), D-26/D-27 (webhook no-op), referencia a 24-CONTEXT.md.
    2. Imports (Pattern B): `GitHubClient` desde `./client.js`, `normalizeIssue` desde `./normalize.js`. DROP `createHmac/timingSafeEqual` (no HMAC). DROP `parseTriggerEvent` import (D-26 inline).
    3. JSDoc typedef `GitHubProviderConfig` snake_case (Pattern B / D-29 / D-41).
    4. Factory signature con `opts?: {logger?, client?}` (Pattern C / D-36 / D-41). Logger optional vía `?.child({component: 'github'})` (D-05). Client optional: `opts.client || new GitHubClient({baseUrl: config.base_url, logger})` (token undefined → GitHubClient lee `getProviderApiKey('github')` internamente — Phase 23 D-04).
    5. Helper privado `parseRef(ref)` (Pattern D / D-22): regex `/^([^/]+)\/([^#]+)#(\d+)$/`, retorna `{owner, repo, number}`, throw `Invalid GitHub ref: ${ref}. Expected owner/repo#number` si no matchea.
    6. NO `findProject` helper (DROP — D-08 ref ya trae owner/repo).
    7. Sin closures de cache (D-19): no `labelCache`, `stateCache`, `initTimestamp`, `INIT_TTL_MS`.

    Provider literal (Pattern E / D-42 — orden de TASK_PROVIDER_METHODS):
    ```
    /** @type {import('../../interface.js').TaskProvider} */
    const provider = {
      async init() {},                                         // D-19 (Pattern F)
      async getTask(ref) { ... },                              // D-20 (Pattern G)
      async updateTaskState(task, stateName) { ... },          // D-23 (Pattern H)
      async addComment(task, markdownText) { ... },            // D-24 (Pattern I)
      async listPendingTasks() { ... },                        // D-25 (Pattern J)
      parseTriggerEvent(_rawPayload) { return null; },         // D-26 (Pattern K)
      verifySignature(_rawBody, _headers) { return false; },   // D-27 (Pattern K)
      async resolveRef(humanRef) { ... },                      // D-21 (Pattern L)
      async listProjects() { ... },                            // D-28 (Pattern M)
    };
    return provider;
    ```

    Detalles per-método:
    - `getTask(ref)` (D-20 / Pattern G): `const {owner, repo, number} = parseRef(ref); const issue = await client.getIssue(owner, repo, number); return normalizeIssue(issue, {projectId: \`${owner}/${repo}\`});`
    - `updateTaskState(task, stateName)` (D-23 / Pattern H): parseRef(task.ref); si stateName !== 'open'/'closed' y NO está en `Object.values(config.states || {})` → throw `Unknown state: ${stateName}. Configured: ${Object.values(states).join(', ')}`; `await client.updateIssue(owner, repo, number, {state: stateName})`.
    - `addComment(task, md)` (D-24 / Pattern I): parseRef(task.ref); `await client.addComment(owner, repo, number, md)` — Markdown literal, sin transformación.
    - `listPendingTasks()` (D-25 / Pattern J): `for (const r of config.repos || []) { const result = await client.listIssues(r.owner, r.repo, {labels:['kodo'], state:'open'}); for (const issue of result.items) { if (issue.pull_request) continue; allTasks.push(normalizeIssue(issue, {projectId: '${r.owner}/${r.repo}'})); } }`
    - `resolveRef(humanRef)` (D-21 / Pattern L): parseRef + client.getIssue, retornar `issue.node_id`.
    - `listProjects()` (D-28 / Pattern M): `return (config.repos || []).map(r => ({id: \`${r.owner}/${r.repo}\`, identifier: \`${r.owner}/${r.repo}\`, name: \`${r.owner}/${r.repo}\`}));` — cero API calls.
  </behavior>
  <action>
    Crear `src/providers/github/provider.js` (~150-180 LOC) siguiendo PATTERNS.md §Patterns A-M líneas 36-382.

    Punto crítico: el orden de métodos en el objeto retornado DEBE seguir `TASK_PROVIDER_METHODS` (D-42) — facilita diff side-by-side con PlaneProvider y el contract test del task previo lo verifica iterando ese array.

    JSDoc `/** @type {import('../../interface.js').TaskProvider} */` antes del literal del provider para que `// @ts-check` valide el shape en compile-time.

    Prefix `_` en params no-usados (`_rawPayload`, `_rawBody`, `_headers`) — convención repo para silenciar linter no-unused-vars y señalar intencionalidad (Pattern K).

    Pitfall #6 RESEARCH.md: el `try/catch` en registry.js NO está aquí — el provider NO atrapa errores del client. `getTask` con issue inexistente → client.getIssue throw Error.code='not_found' y el provider lo propaga directo (los callers como verify.js / dispatcher deciden cómo reaccionar — Pattern Error Handling RESEARCH.md líneas 1044-1066).
  </action>
  <verify>
    <automated>node --test test/providers/github/provider.test.js test/providers/github/normalize.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/providers/github/provider.js` existe.
    - `grep -c "^export function createGitHubProvider" src/providers/github/provider.js` == 1.
    - `grep -c "// @ts-check" src/providers/github/provider.js` >= 1.
    - `grep -c "import.*GitHubClient.*client.js" src/providers/github/provider.js` == 1.
    - `grep -c "import.*normalizeIssue.*normalize.js" src/providers/github/provider.js` == 1.
    - `grep -c "createHmac\|timingSafeEqual" src/providers/github/provider.js` == 0 (D-27 no-op, sin HMAC).
    - `grep -cE "parseTriggerEvent\(.*\) \{$|parseTriggerEvent\(_rawPayload\)" src/providers/github/provider.js` >= 1 con cuerpo `return null`.
    - `grep -E "return false" src/providers/github/provider.js | wc -l` >= 1 (verifySignature).
    - `grep -c "labelCache\|stateCache\|initTimestamp\|INIT_TTL_MS" src/providers/github/provider.js` == 0 (D-19 sin cache).
    - `grep -c "<p>.*<br>" src/providers/github/provider.js` == 0 (D-24 sin HTML wrap).
    - `grep -c "issue.pull_request" src/providers/github/provider.js` >= 1 (Pitfall #2 PR filter en listPendingTasks).
    - `grep -c "Invalid GitHub ref" src/providers/github/provider.js` >= 1 (parseRef error message canonical).
    - `node --test test/providers/github/provider.test.js` exit 0, todos los tests del task 24-02-01 GREEN.
    - `node --test test/providers/github/normalize.test.js` exit 0 (Wave 1 sigue verde).
    - Verbo del leak guard: ningún test produjo el error `live fetch leak: ...` (ningún test toca globalThis.fetch — confirma D-37 funcionando).
    - Orden de métodos del provider (output del contract test) coincide con `['init', 'getTask', 'updateTaskState', 'addComment', 'listPendingTasks', 'parseTriggerEvent', 'verifySignature', 'resolveRef', 'listProjects']`.
  </acceptance_criteria>
  <done>provider.js implementa los 9 métodos siguiendo D-19..D-28; test suite verde; live-fetch leak guard sin trigger; mirror estructural de PlaneProvider con divergencias justificadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (CLI / dispatcher / polling) → provider | `ref` y `stateName` arguments arriving de userland (CLI args) o de fuentes Plane-shape (KL-42) deben rechazarse |
| Provider → GitHubClient (Phase 23 auth-hardened) | Provider propaga errores del client tal cual; cliente owns auth + rate-limit handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-P-01 | T (Tampering / Input Validation V5) | parseRef | mitigate | Regex strict `^([^/]+)\/([^#]+)#(\d+)$` rechaza Plane refs (`KL-42`), URLs completas, partial refs (`#42`). Test 24-02-05 verifica los 4 patterns inválidos. Mensaje de error fijo para grep en logs. |
| T-24-P-02 | T (State injection) | updateTaskState | mitigate | Solo acepta 'open'/'closed' literal o valores presentes en `config.states` map. Cualquier otro stateName → throw `Unknown state: ${stateName}` antes de tocar GitHub API. |
| T-24-P-03 | I (Information Disclosure) | parseTriggerEvent / verifySignature | accept | Webhook ingress fuera de scope v0.7 (REQUIREMENTS §Out of Scope). Ambos métodos no-op funcional (D-26 null, D-27 false). Sin HMAC code path → sin riesgo timing-attack. |
| T-24-P-04 | D (DoS) | listPendingTasks | accept | Iteración server-side filter (`labels=kodo`) reduce payload. Sin pagination (D-25): si un repo tiene >100 issues con label `kodo` open → primera página solamente, defer pagination a v0.8+ (deferred ideas). Acceptable para personal use kodo. |
| T-24-P-05 | E (Elevation of Privilege) | provider methods | mitigate | Provider NO mantiene secrets — `GitHubClient` (Phase 23) owns el token. Provider invoca client passthrough. Sin scope-creep de credenciales. |

Phase 24 no introduce nuevos auth code paths. V5 (Input Validation) cubierto por `parseRef` strict + `updateTaskState` whitelist. Sin threats high-severity → no blocker.
</threat_model>

<verification>
- `node --test test/providers/github/provider.test.js` → exit 0, ≥12 tests green
- `node --test test/providers/github/normalize.test.js` → sigue verde (Wave 1 invariante)
- `node --test test/plane-provider.test.js` → sigue verde (zero cambios a Plane)
- `npm test` global verde, baseline v0.6 + Phase 24 Wave 1 + Wave 2 (target ≥ 614 + N tests)
- Live-fetch leak guard activo: ningún test del archivo provoca `live fetch leak: ...` (todos inyectan `opts.client`)
- `grep -E "listTasks|listLabels|listStates|transitionTask" src/providers/github/provider.js` returns 0 matches (D-01 safety net — el provider implementa SOLO los 9 reales)
</verification>

<success_criteria>
- provider.js (~150-180 LOC) implementa los 9 métodos exactos según D-19..D-28
- Contract test `createGitHubProvider(...).METHOD` is function para los 9 nombres de TASK_PROVIDER_METHODS — GREEN
- Per-method tests verifican parseRef strict, state passthrough, markdown literal, PR filter, no-op trigger/verify, cero API calls listProjects — todos GREEN
- Zero modificaciones a `src/providers/github/client.js`, `src/providers/plane/*`, `src/labels.js`, `src/interface.js`
- Suite global `npm test` sin regresiones
- Live-fetch leak guard funciona (D-37): cero entries en stderr indicando inyección olvidada
</success_criteria>

<output>
After completion, create `.planning/phases/24-githubprovider-normalizer-registry/24-02-SUMMARY.md` documentando:
- provider.js LOC final + lista de métodos por orden de aparición (debe matchear TASK_PROVIDER_METHODS)
- Divergencias respecto a PlaneProvider justificadas (init no-op, sin caches, markdown literal, no-op trigger/verify, listProjects sin API)
- Test count + cobertura por método
- Cualquier desviación de PATTERNS.md anotada (no debería haber ninguna sustantiva)
</output>
