---
phase: 81-saneo-de-deuda-v0-17
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gsd-lock-race.test.js
  - test/helpers/lock-race-child.mjs
  - .planning/debug/gsd-lock-race-cr01.md
  - .planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md
autonomous: true
requirements: [DEBT-04]
must_haves:
  truths:
    - "El flaky `test/gsd-lock-race.test.js` («concurrent dead-holder steal», CR-01, :142) tiene un diagnóstico de causa raíz DOCUMENTADO vía `/gsd-debug` (repro intentado bajo carga con condiciones y verdicts registrados) — el entregable es el artefacto, no un test verde (DEBT-04, D-08)"
    - "`src/gsd/lock.js` queda SIN modificar salvo que la causa raíz esté confirmada Y el cambio no altere la semántica de locks de v0.16 (`acquireGsdLock` atómico, steal CAS tmp+rename, guarda ABA) — verificable con `git diff` vacío sobre lock.js (D-09, invariante v0.16 LOCKED)"
    - "Si la causa es del harness del test (timing de arranque de procesos), el fix legítimo es al TEST, nunca a `lock.js`; si tras un intento honesto no reproduce, el outcome válido es documentar la no-repro + instrumentación barata en el test/helper (volcar verdicts al fallar), sin tocar producción (D-09/D-10)"
    - "El invariante 'exactamente-uno-adquiere' del harness `raceGsdStealDeadHolder` es order-independent: cuál de los N stealers concurrentes gana NO está especificado ni necesita ser estable; solo se asevera el conteo (=1) (edge ordering, DEBT-04)"
    - { statement: "El diagnóstico de DEBT-04 registra si dos stealers concurrentes de un dead-holder pueden AMBOS adquirir (colisión en la ventana move-aside→O_EXCL create) vs. exactamente-uno-adquiere — la adyacencia/ventana de carrera queda documentada con evidencia verificada, no asumida en silencio", verification: backstop }
    - { statement: "El diagnóstico de DEBT-04 anota el caso degenerado de un solo stealer (N=1 adquiere trivialmente; el invariante solo se estresa con N≥2 stealers concurrentes) — el harness cubre N=2 (:143) y N=5 (:153)", verification: backstop }
  artifacts:
    - ".planning/debug/gsd-lock-race-cr01.md — artefacto `/gsd-debug` con la estructura canónica (# Debug: título → Fecha/Gap/Metodología → ## Síntoma → ## Evidencia (verificada, no inferida) → ## Root Cause → ## Files Involved → ## Suggested Fix Direction)"
    - ".planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md — nota de resolución en el directorio de fase (outcome: causa confirmada / no-repro instrumentado; condiciones intentadas)"
    - "test/gsd-lock-race.test.js y/o test/helpers/lock-race-child.mjs — SOLO si se justifica: instrumentación barata (volcar verdicts / rama CAS de cada hijo al fallar) o, si la causa confirmada es de harness, el fix del test — nunca `lock.js`"
  key_links:
    - "harness `raceGsdStealDeadHolder` (:74-118) → siembra dead-PID 99999999 en `<realpath repo>/.planning/.kodo.lock` → spawnea N hijos (`lock-race-child.mjs --hold 500`) → asevera exactamente un `acquired` (:146)"
    - "`stealLock` CAS (`src/gsd/lock.js:283-351`, READ-ONLY): move-aside `renameSync` → guarda ABA → O_EXCL create `{flag:'wx'}`"
  prohibitions:
    - statement: "MUST NOT debilitar ni enmascarar la corrección del lock cross-proceso para poner verde el flaky: prohibidos a ciegas los retries, `.skip`/`it.skip`, subir timeouts, o editar `src/gsd/lock.js` sin causa raíz confirmada — enmascarar una carrera real (doble adquisición → corrupción de estado) es la violación de seguridad; el entregable es el diagnóstico, no un test verde (D-09, seguridad/integridad, invariante v0.16 LOCKED)"
      status: resolved
      verification: judgment
---

<objective>
Cerrar DEBT-04: diagnóstico de causa raíz del flaky `test/gsd-lock-race.test.js` («concurrent dead-holder steal», CR-01, :142) vía `/gsd-debug`. El entregable es el DIAGNÓSTICO DOCUMENTADO, no un fix — `src/gsd/lock.js` es READ-ONLY salvo causa raíz confirmada que NO altere la semántica de locks de v0.16. Prohibido a ciegas: retries, `.skip`, subir timeouts, tocar `lock.js` sin causa. Si la causa es del harness (timing de arranque de procesos), el fix legítimo es al test; si no reproduce tras un intento honesto, el outcome válido es documentar la no-repro + instrumentación barata en el test/helper.

Purpose: proteger el invariante de locks de v0.16 (top threat de la fase — un «arreglo» descuidado del flaky que debilite el CAS es lo peor que puede pasar) entendiendo la causa antes de tocar nada.

Output: artefacto de diagnóstico + nota de resolución; a lo sumo instrumentación barata o un fix de test si la causa es de harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-CONTEXT.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-RESEARCH.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-PATTERNS.md
@.planning/debug/resolved/state-tasks-missing-live04.md
</context>

## Artifacts this phase produces (source-grounding — read before drift verification)

Ficheros que ESTE plan crea (no marcar como drift):
- `.planning/debug/gsd-lock-race-cr01.md` — nuevo artefacto de diagnóstico `/gsd-debug`
- `.planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md` — nueva nota de resolución de fase
- (condicional) instrumentación en `test/gsd-lock-race.test.js` / `test/helpers/lock-race-child.mjs` — SOLO si se justifica por D-10 (no-repro) o D-09 (causa de harness confirmada); `src/gsd/lock.js` NO se toca

<tasks>

<task type="auto">
  <name>Task 1: Intento de reproducción del flaky CR-01 (cold + bajo carga)</name>
  <files>test/gsd-lock-race.test.js</files>
  <read_first>
    - test/gsd-lock-race.test.js — leer el `describe` CR-01 (:142) y los casos :143 (2 procesos) y :153 (5 procesos); el harness `raceGsdStealDeadHolder` (:74-118): siembra un dead-PID (`99999999`) en `<realpath repo>/.planning/.kodo.lock` (:79-95), spawnea N hijos con `--hold 500`, y asevera exactamente un `acquired` (:146); el mensaje de fallo ya imprime `verdicts.join(',')`
    - test/helpers/lock-race-child.mjs — el contrato del hijo (qué imprime, cómo señala `acquired`)
    - src/gsd/lock.js — leer (SOLO lectura) `stealLock` CAS (:283-351): move-aside `renameSync(lockPath, aside)`, guarda ABA (restore+reject si los bytes movidos son un lock vivo fresco), O_EXCL create `writeFileSync(..., {flag:'wx'})`, `MAX_STEAL_ATTEMPTS = 8`
    - .planning/phases/81-saneo-de-deuda-v0-17/81-RESEARCH.md §DEBT-04 Diagnostic Guidance (comandos de repro cold + load, hipótesis: ventana briefly-empty, timing de harness/hold-expiry, cold-vs-load — precedente 12 runs verdes en frío 2026-07-06)
  </read_first>
  <action>
    Correr los loops de repro documentados en 81-RESEARCH.md SIN modificar producción: (1) cold, el fichero flaky en loop — 50 iteraciones de `node --test test/gsd-lock-race.test.js`, parando y anotando en la primera que falle; (2) bajo carga, la suite completa en loop — 20 iteraciones de `node --test $(find test -name '*.test.js' -type f)`, parando y anotando la primera que falle. Registrar para el artefacto (Task 2): número de iteraciones, cuántas fallaron, los `verdicts` exactos de cualquier fallo, y las condiciones (versión de Node, carga de máquina, cold vs load). NO aplicar ningún remedio en este task — es puramente observación. Si NO reproduce en ninguno de los dos regímenes, registrarlo explícitamente como no-repro con las condiciones intentadas (outcome válido per D-10). NO tocar `src/gsd/lock.js`.
  </action>
  <verify>
    <automated>for i in $(seq 1 50); do node --test test/gsd-lock-race.test.js || { echo "FAILED cold run $i"; break; }; done</automated>
  </verify>
  <acceptance_criteria>
    - El loop cold (50x el fichero) y el loop de carga (20x la suite) se ejecutaron; el resultado (repro/no-repro), el conteo de fallos y los `verdicts` de cualquier fallo quedan capturados para el artefacto
    - `git diff --quiet -- src/gsd/lock.js` (exit 0: `lock.js` SIN modificar en este task)
    - Las condiciones (versión de Node, régimen cold vs load) quedan anotadas
  </acceptance_criteria>
  <done>Repro intentado en ambos regímenes (cold + carga); outcome (repro con verdicts / no-repro con condiciones) capturado; `lock.js` intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Artefacto de diagnóstico /gsd-debug + nota de resolución (+ instrumentación/fix condicional)</name>
  <files>.planning/debug/gsd-lock-race-cr01.md, .planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md, test/gsd-lock-race.test.js, test/helpers/lock-race-child.mjs</files>
  <read_first>
    - .planning/debug/resolved/state-tasks-missing-live04.md — la estructura canónica del artefacto `/gsd-debug` a replicar (# Debug: título → Fecha / Gap / Metodología → ## Síntoma → ## Evidencia (verificada, no inferida) → ## Root Cause → ## Files Involved → ## Suggested Fix Direction)
    - .planning/phases/81-saneo-de-deuda-v0-17/81-RESEARCH.md §DEBT-04 (hipótesis a evaluar y su encuadre: ventana briefly-empty = posible causa de PRODUCTO; hold-expiry/spawn-jitter = causa de HARNESS → fix legítimo al test; cold-vs-load) y §Cheap instrumentation option (D-10)
    - .planning/phases/81-saneo-de-deuda-v0-17/81-CONTEXT.md D-08/D-09/D-10 (gate del fix y outcomes válidos)
    - test/gsd-lock-race.test.js y test/helpers/lock-race-child.mjs (por si se justifica instrumentación barata: volcar verdicts / la rama CAS de cada hijo al fallar)
  </read_first>
  <action>
    Escribir el artefacto de diagnóstico en `.planning/debug/gsd-lock-race-cr01.md` con la estructura canónica de `state-tasks-missing-live04.md`, usando los datos del Task 1: Síntoma (el flaky CR-01 bajo carga), Evidencia (verificada, no inferida — los verdicts/condiciones reales del repro o la no-repro), Root Cause (la causa confirmada, o las hipótesis abiertas con cuál se descartó y cuál no, encuadrando ventana-briefly-empty como causa de producto vs hold-expiry/spawn-jitter como causa de harness), Files Involved (`test/gsd-lock-race.test.js`, `test/helpers/lock-race-child.mjs`, `src/gsd/lock.js:283-351` READ-ONLY) y Suggested Fix Direction. Aplicar el GATE del fix (D-09/D-10): (a) si la causa raíz queda confirmada Y es del HARNESS (timing de arranque de procesos / hold-expiry), aplicar el fix SOLO al test/helper (p. ej. hold coordinado/mayor), NUNCA a `lock.js`; (b) si NO reproduce, documentar la no-repro (condiciones intentadas, hipótesis abiertas) y, si es barato, añadir instrumentación de bajo coste al test/helper (volcar verdicts / rama CAS de cada hijo al fallar) para la próxima manifestación; (c) PROHIBIDO a ciegas: retries, `.skip`, subir timeouts, o cualquier edición de `src/gsd/lock.js` sin causa confirmada. Escribir la nota de resolución en `.planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md` (outcome: «causa confirmada + fix de test» O «diagnosticado: no reproducible en frío; instrumentado», con las condiciones). Si el artefacto queda cerrado (causa entendida), moverlo a `.planning/debug/resolved/`.
  </action>
  <verify>
    <automated>{ test -f .planning/debug/gsd-lock-race-cr01.md || test -f .planning/debug/resolved/gsd-lock-race-cr01.md; } && test -f .planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md && git diff --quiet -- src/gsd/lock.js && node --test test/gsd-lock-race.test.js</automated>
    <human-check>Revisar que el artefacto de diagnóstico tenga Root Cause con evidencia verificada (no inferida) y que el gate del fix (D-09/D-10) se respetó — repro no determinista, verificación manual per VALIDATION.md</human-check>
  </verify>
  <acceptance_criteria>
    - Existe `.planning/debug/gsd-lock-race-cr01.md` (o su versión en `resolved/`) con las secciones canónicas: Síntoma, Evidencia (verificada, no inferida), Root Cause, Files Involved, Suggested Fix Direction
    - Existe la nota de resolución `.planning/phases/81-saneo-de-deuda-v0-17/81-DEBT-04-DIAGNOSIS.md` con el outcome (causa confirmada + fix de test, o no-repro instrumentado) y las condiciones intentadas
    - `git diff --quiet -- src/gsd/lock.js` (exit 0: `src/gsd/lock.js` SIN modificar — invariante v0.16 LOCKED, D-09)
    - `node --test test/gsd-lock-race.test.js` verde (si se añadió instrumentación o un fix de harness, la suite del fichero sigue pasando; los casos CR-01 NO quedan `.skip`)
    - Behavior/judgment assertion: no se añadieron remedios a ciegas (retries, skip, subir timeouts) — verificable por revisión del diff del test
  </acceptance_criteria>
  <done>Artefacto de diagnóstico con Root Cause y evidencia verificada + nota de resolución escritos; `lock.js` intacto; si hubo cambio, fue al test/helper (instrumentación o fix de harness) y la suite del fichero sigue verde; cero remedios a ciegas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agentes GSD concurrentes (procesos separados) → `.kodo.lock` compartido | Múltiples procesos contienden por el mismo fichero de lock; el CAS steal debe garantizar exactamente-uno-adquiere |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-03-01 | Tampering | `src/gsd/lock.js` CAS steal (doble adquisición) | high | mitigate | `lock.js` READ-ONLY: DEBT-04 solo DIAGNOSTICA; un fix está gated por causa raíz confirmada Y cero cambio a la semántica v0.16; `git diff --quiet` prueba que `lock.js` no se tocó. TOP THREAT de la fase |
| T-81-03-02 | Tampering | remediación del flaky que enmascara una carrera real | high | mitigate | Prohibido a ciegas: retries/`.skip`/subir timeouts; el entregable es el artefacto de diagnóstico, no un test verde; si la causa confirmada es de harness, el fix va SOLO al test |
| T-81-03-SC | Tampering | instalaciones npm/pip/cargo | n/a | accept | Sin instalaciones en esta fase — no aplica |
</threat_model>

<verification>
- El artefacto de diagnóstico existe con Root Cause y evidencia verificada; la nota de resolución existe con el outcome
- `git diff --quiet -- src/gsd/lock.js` (invariante v0.16 protegido)
- `node --test test/gsd-lock-race.test.js` verde (los casos CR-01 no quedan skipped)
- Suite completa verde al cierre del wave: `node --test $(find test -name '*.test.js' -type f)`
</verification>

<success_criteria>
DEBT-04 cerrado como diagnóstico: causa raíz documentada (o no-repro instrumentado) vía `/gsd-debug`, con evidencia verificada; `src/gsd/lock.js` intacto; cualquier cambio limitado al test/helper y sin remedios a ciegas; el invariante de locks de v0.16 protegido.
</success_criteria>

<output>
Create `.planning/phases/81-saneo-de-deuda-v0-17/81-03-SUMMARY.md` when done
</output>