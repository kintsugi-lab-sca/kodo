---
phase: 81-saneo-de-deuda-v0-17
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/format.js
  - test/dashboard-format.test.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
autonomous: true
requirements: [DEBT-02, DEBT-03]
must_haves:
  truths:
    - "`nextCell` colapsa toda secuencia de whitespace (`/\\s+/g`: `\\n` `\\t` `\\r` + espacios múltiples) a un espacio único + `trim` en el RENDER de fila; un `next` hand-editado en state.json ya no descuadra la tabla (DEBT-03, D-05)"
    - "El cell de la columna `next` renderiza `''` (celda vacía) para `next` ausente/`null`/`undefined`/`''`/solo-whitespace/no-string — sin placeholder glyph (SC5), nunca lanza (D-07) (edge empty + UI-SPEC empty/error)"
    - "El colapso de `nextCell` usa `/\\s+/g` (clase `\\s` de JS: espacio, tab, CR, LF, FF, VT y separadores Unicode); no normaliza Unicode; el truncado por ancho lo sigue haciendo la columna Ink de ancho fijo (~40, `truncate-end`), inalterada por esta fase (edge encoding + UI-SPEC long-text)"
    - "El dato persistido en state.json queda VERBATIM: el colapso es render-only; NO se mueve a `upsertTaskHandoff` ni al enrich de App.js (que sigue aplicando solo `stripControlChars`, carril Phase 78 intacto) (DEBT-03, D-06)"
    - "`nextCell` sigue pura: sin color propio (color-isolation D-12/D-07), sin efectos, consumida por `rowCells` sin cambios — un solo punto de cambio"
    - "El comentario de App.js (aprox. :735) deja de afirmar que el bloque `tasks` se lee una vez por tick del poll; se reescribe para describir la realidad del render que 75/WR-02 señaló (una lectura síncrona en cada render, piggyback sobre el tick de usePoll) — doc-only, sin cambio de comportamiento (DEBT-02a, D-11)"
    - "El typedef del prop `overlaySnapshot` en SessionTable.js (aprox. :817) gana `render?: 'markdown'|'plain'`, espejo del `PlanResult` de plan.js:48 — JSDoc puro, cero runtime (DEBT-02b, D-11)"
    - "Un `next` string no-vacío renderiza verbatim tras colapso + trim, siempre una sola línea (UI-SPEC populated)"
    - "No existe estado de carga async por diseño: `readTasksFn` es lectura local síncrona piggyback en el tick del poll; hasta que hay dato el valor está ausente → misma celda vacía `''`, sin spinner/skeleton (UI-SPEC loading)"
    - "Las filas degradan de forma independiente: una sesión cuyo `task_id` no tiene entrada en `tasks` (o cuya entrada no tiene `next`) renderiza `''` mientras las filas hermanas renderizan su valor — sin acoplamiento cross-fila (UI-SPEC partial)"
    - "Embebidos `\\n`/`\\t`/`\\r`/multi-espacio (state.json hand-editado) colapsan a un espacio único → la fila no puede romper en múltiples líneas ni descuadrar la tabla de ancho fijo — el fix núcleo de DEBT-03 (UI-SPEC overflow)"
    - "Comportamiento de columna condicional de Phase 75 inalterado: cero sesiones con `next` → columna oculta; una/muchas → celdas independientes por fila; sin copy singular/plural (UI-SPEC zero-one-many)"
    - { statement: "DEBT-02 edge `unclassified` (auto-probe no clasificable): DEBT-02 es doc-only (comentario + typedef); no aplica ningún edge de comportamiento runtime — la evidencia de cero-cambio es la suite completa verde SIN modificación (D-12). Si se implicara un comportamiento runtime que el planner no vio, NO queda cubierto.", verification: backstop }
  artifacts:
    - "src/cli/dashboard/format.js — `nextCell` con colapso `/\\s+/g`→' ' + trim, no-string→'', empty→'' (sin placeholder) + JSDoc actualizado notando el colapso"
    - "test/dashboard-format.test.js — casos de whitespace-collapse añadidos al describe `nextCell` existente (aprox. :509)"
    - "src/cli/dashboard/App.js — comentario aprox. :735 corregido a la realidad del render (75/WR-02), doc-only"
    - "src/cli/dashboard/SessionTable.js — typedef `overlaySnapshot` (aprox. :817) con `render?: 'markdown'|'plain'`, doc-only"
  key_links:
    - "state.json `next` (verbatim) → enrich App.js (`stripControlChars` only, :753) → `rowCells` → `nextCell` (colapso layout) → celda Ink ancho fijo `truncate-end`"
    - "SessionTable.js typedef `overlaySnapshot` ← espejo de plan.js:48 `PlanResult` (`render?: 'markdown'|'plain'`)"
  prohibitions:
    - statement: "MUST NOT mover el colapso de whitespace al write ni al enrich de App.js, ni eliminar/reemplazar la capa `stripControlChars` (defensa terminal-injection de Phase 78): el colapso es una capa de LAYOUT complementaria, no un sustituto de la sanitización de control-chars; el dato persistido queda verbatim (D-06, defensa en profundidad)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT introducir un placeholder glyph (p. ej. `—`) ni color propio en el estado vacío de `nextCell`: la celda vacía es `''` byte-idéntica a un `next` nunca seteado (SC5 LOCKED, color-isolation D-12/D-07) — a diferencia de `progCell`, la asimetría es intencional"
      status: resolved
      verification: judgment
    - statement: "MUST NOT cambiar el comportamiento de render en los arreglos doc-only de DEBT-02: la suite existente debe seguir verde SIN modificación como prueba de cero-cambio (D-12) — sin memoización, sin nuevos tests"
      status: resolved
      verification: judgment
---

<objective>
Cerrar DEBT-02 y DEBT-03, ambos en el subsistema TUI/dashboard, ficheros disjuntos:
- **DEBT-03** (render): `nextCell` (`src/cli/dashboard/format.js`) colapsa `\n`/`\t`/`\r`/multi-espacio a un espacio único + `trim` en el punto de proyección al render, de modo que un `next` hand-editado en state.json no rompa la fila de ancho fijo y descuadre la tabla. El dato persistido queda verbatim (D-06); es un fix de LAYOUT complementario a `stripControlChars` (Phase 78), no un sustituto.
- **DEBT-02** (doc-drift, Tier 1 cero-comportamiento): (a) el comentario de `App.js` (aprox. :735) que afirma que el bloque `tasks` se lee «una vez por tick» se corrige a la realidad del render (75/WR-02); (b) el typedef del prop `overlaySnapshot` en `SessionTable.js` (aprox. :817) gana `render?: 'markdown'|'plain'`, espejo de `plan.js:48`.

Purpose: la tabla del dashboard resiste un `next` malformado sin descuadrarse, y la documentación deja de mentir sobre el render — sin tocar comportamiento (la suite verde sin modificación es la prueba).

Output: `nextCell` con colapso + tests; dos ediciones doc-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-CONTEXT.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-RESEARCH.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-PATTERNS.md
@.planning/phases/81-saneo-de-deuda-v0-17/81-UI-SPEC.md
@.planning/milestones/v0.17-phases/75-superficie-del-next-dashboard-y-nudge/75-REVIEW.md
</context>

## Artifacts this phase produces (source-grounding — read before drift verification)

Símbolos/ficheros que ESTE plan crea o refina (no marcar como drift):
- `nextCell` (mismo símbolo exportado en `src/cli/dashboard/format.js`) — refinamiento de comportamiento: añade colapso `/\s+/g`→' ' + trim
- Nuevos casos de test de whitespace-collapse en el describe `nextCell` existente de `test/dashboard-format.test.js`
- Campo de typedef `render?: 'markdown'|'plain'` añadido al JSDoc del prop `overlaySnapshot` en `src/cli/dashboard/SessionTable.js` (espeja `plan.js:48`, símbolo ya existente en el repo)
- Reword del comentario `:735` de `src/cli/dashboard/App.js` (doc-only, sin símbolo nuevo)

## Flagged assumption (edge no clasificable — no silenciar)

- **DEBT-02 / edge `unclassified`** (auto-probe no lo clasificó): DEBT-02 es doc-only (comentario + typedef); el planner asume que NO aplica ningún edge de comportamiento runtime. La evidencia de cero-cambio es la suite completa verde SIN modificación (D-12). Si un revisor detecta un comportamiento runtime implicado que este plan no cubre, es una asunción explícita a resolver, no un drop silencioso.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Colapso de whitespace en nextCell + tests (DEBT-03)</name>
  <files>src/cli/dashboard/format.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/dashboard/format.js — leer `nextCell` (aprox. :258-260): hoy `return typeof session.next === 'string' && session.next.length > 0 ? session.next : ''`; el JSDoc (aprox. :248-256); el consumidor `rowCells` (aprox. :271); los moldes de celda pura hermanos (`progCell`, `taskCell`) para replicar el estilo never-throws
    - test/dashboard-format.test.js — leer el describe `nextCell` existente (aprox. :509, «LIVE-05 (SC5): nextCell proyecta el NEXT: por tarea») y sus casos actuales (:511-525): passthrough de string, `null`/`undefined`/`''`/`{}` → `''`, no-string (`42`, `{}`) → `''`; la forma one-liner `assert.equal(nextCell({ next: ... }), ...)`
    - .planning/phases/81-saneo-de-deuda-v0-17/81-UI-SPEC.md §Render Contract — `nextCell` (los 5 pasos de la transformación) y §Copywriting Contract (empty vs placeholder)
    - .planning/phases/81-saneo-de-deuda-v0-17/81-PATTERNS.md §format.js (forma objetivo) y §81-RESEARCH.md Pattern 1 (collapse-at-projection) + Pitfall 4
  </read_first>
  <behavior>
    - `nextCell({ next: 'a\nb\tc' })` → `'a b c'`
    - `nextCell({ next: '  multiple   spaces  ' })` → `'multiple spaces'` (trim + colapso)
    - `nextCell({ next: '\n\t \r' })` → `''` (solo-whitespace → vacío, SIN placeholder)
    - `nextCell({ next: 'plan ok' })` → `'plan ok'` (passthrough sin whitespace interno)
    - `nextCell({ next: null })`, `nextCell({ next: undefined })`, `nextCell({ next: '' })`, `nextCell({ next: 42 })`, `nextCell({})` → `''` (never-throws, no-string → '')
  </behavior>
  <action>
    Reescribir `nextCell` para: si `session.next` NO es string → `return ''` (never-throws, D-07); si lo es → colapsar con `session.next.replace(/\s+/g, ' ').trim()` y devolver el resultado (vacío → `''`, SIN placeholder — no introducir `—` ni ningún glyph, SC5/Pitfall 4). Mantener la celda PURA: sin color propio (color-isolation D-12), sin efectos secundarios. NO mover el colapso al enrich de App.js ni al write (D-06: el dato persistido queda verbatim; el enrich sigue con `stripControlChars` únicamente). Actualizar el JSDoc (aprox. :248-256) para notar el colapso de whitespace de layout. `rowCells` (aprox. :271) consume `nextCell` sin cambios (un solo punto de cambio). En el test, añadir al describe `nextCell` existente los casos de behavior de arriba, siguiendo la forma one-liner `assert.equal(nextCell({ next: ... }), ...)`.
  </action>
  <verify>
    <automated>node --test test/dashboard-format.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-format.test.js` verde con los casos: `'a\nb\tc'`→`'a b c'`, `'  multiple   spaces  '`→`'multiple spaces'`, `'\n\t \r'`→`''`, no-string→`''`
    - Source assertion: `grep -c 'replace(/\\s+/g' src/cli/dashboard/format.js` ≥ 1 (colapso presente en `nextCell`)
    - Source assertion: `grep -c 'trim()' src/cli/dashboard/format.js` ≥ 1 en el cuerpo de `nextCell`
    - Behavior assertion: `nextCell` no introduce ningún placeholder para vacío (el resultado vacío es `''`, byte-idéntico al estado no-seteado)
    - Source assertion: el enrich de App.js (`src/cli/dashboard/App.js`) sigue aplicando SOLO `stripControlChars` — `grep -c 'stripControlChars' src/cli/dashboard/App.js` ≥ 1 y el diff de App.js en este task es vacío (el colapso de layout vive únicamente en `format.js`, NO migra a persistencia/enrich — D-06)
  </acceptance_criteria>
  <done>`nextCell` colapsa whitespace a un espacio + trim en el render, empty→'' sin placeholder, never-throws, pura y sin color; el dato persistido intacto; tests de whitespace verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Correcciones doc-only de Phase 75 (DEBT-02)</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - .planning/milestones/v0.17-phases/75-superficie-del-next-dashboard-y-nudge/75-REVIEW.md — leer el texto EXACTO de WR-02 (el comentario de App.js impreciso) y WR-04 (el typedef sin `render`) ANTES de reescribir (D-11)
    - src/cli/dashboard/App.js — leer el comentario aprox. :735-739 (la afirmación «Phase 75 (LIVE-05, D-02): lee el bloque tasks …») y la lectura `readTasksFn({})` (aprox. :739); el enrich con `stripControlChars` (aprox. :753) para confirmar que NO se toca
    - src/cli/dashboard/SessionTable.js — leer el typedef del prop `overlaySnapshot` (aprox. :817), hoy `{ kind, taskRef, status, lines }|null` sin `render`
    - src/cli/dashboard/plan.js — leer el typedef `PlanResult` (aprox. :48) que YA declara `render?: 'markdown'|'plain'` (la fuente-espejo)
  </read_first>
  <action>
    (a) DEBT-02a — Reescribir el comentario de `App.js` (aprox. :735) para que deje de afirmar que el bloque `tasks` se lee una única vez por tick del poll y describa la realidad que 75/WR-02 señaló: la lectura `readTasksFn({})` ocurre en CADA render (una lectura síncrona por render, piggyback sobre el tick de `usePoll` que refresca `/status`). Doc-only: NO cambiar el comportamiento del render, NO memoizar, NO tocar el enrich `stripControlChars`. (b) DEBT-02b — Añadir `render?: 'markdown'|'plain'` al typedef inline del prop `overlaySnapshot` en `SessionTable.js` (aprox. :817), espejando literalmente el campo que `plan.js:48` `PlanResult` ya declara. JSDoc puro, cero runtime, sin nuevos tests (D-12: la suite verde sin modificación ES la prueba de cero-cambio).
  </action>
  <verify>
    <automated>node --test $(find test -name '*.test.js' -type f)</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "render?: 'markdown'|'plain'" src/cli/dashboard/SessionTable.js` ≥ 1 (typedef con `render` añadido)
    - Source assertion: el comentario reescrito de App.js menciona el render (p. ej. `grep -ci 'render' src/cli/dashboard/App.js` ≥ 1 en la zona del comentario) y ya NO afirma la lectura «una vez por tick» que 75/WR-02 marcó como imprecisa
    - Source assertion: el enrich sigue aplicando `stripControlChars` — `grep -c 'stripControlChars' src/cli/dashboard/App.js` ≥ 1 (capa Phase 78 intacta, cero-cambio de comportamiento)
    - `node --test $(find test -name '*.test.js' -type f)` verde SIN modificación de ningún test (prueba de cero-cambio, D-12)
  </acceptance_criteria>
  <done>Comentario de App.js corregido a la realidad del render (WR-02); typedef `overlaySnapshot` con `render?` espejando plan.js:48 (WR-04); suite completa verde sin tocar tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.json `next` (editable por operador) → render terminal | Contenido no confiable proyectado a la tabla TUI del dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-02-01 | Tampering (DoS-of-display) | `nextCell` render (`format.js`) | low | mitigate | Colapso `/\s+/g`→' ' + `trim` en el punto de proyección: `\n`/`\t`/`\r` no pueden romper la fila de ancho fijo ni descuadrar la tabla |
| T-81-02-02 | Tampering (terminal injection OSC-52/CSI) | enrich de App.js | low | accept | `stripControlChars` (Phase 78) existente e INALTERADO; injection es canon → breadcrumb a `/gsd-secure-phase`; DEBT-03 NO debe removerlo ni sustituirlo |
| T-81-02-03 | Tampering | dato persistido en state.json | low | accept | El dato queda verbatim (D-06); fix render-only, sin mutación del write; DEBT-02 es doc-only con cero superficie runtime |
| T-81-02-SC | Tampering | instalaciones npm/pip/cargo | n/a | accept | Sin instalaciones en esta fase — no aplica |
</threat_model>

<verification>
- `node --test test/dashboard-format.test.js` verde (colapso `nextCell`)
- `node --test $(find test -name '*.test.js' -type f)` verde SIN modificación de tests (prueba doc-only D-12)
- El enrich de App.js conserva `stripControlChars` y no gana el colapso (capas complementarias, D-06)
</verification>

<success_criteria>
DEBT-03 cerrado: `nextCell` colapsa whitespace en el render, un `next` malformado no descuadra la tabla, dato persistido verbatim, celda pura never-throws sin placeholder. DEBT-02 cerrado: comentario de App.js y typedef `overlaySnapshot` corregidos, suite verde sin modificación como prueba de cero-cambio.
</success_criteria>

<output>
Create `.planning/phases/81-saneo-de-deuda-v0-17/81-02-SUMMARY.md` when done
</output>
