---
phase: 12-hook-orchestrator-bifurcation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/launch.js
  - src/orchestrator/prompt.md
autonomous: true
requirements:
  - QUICK-07
tags:
  - orchestrator
  - launch
  - prompt
  - quick-mode
  - visibility

must_haves:
  truths:
    - "src/orchestrator/launch.js importa getSessionMode desde ../labels.js"
    - "buildContextSummary computa gsdTag con prioridad mode-first: quick > phase_id > bootstrap"
    - "Una sesión con gsd_mode === 'quick' renderiza `[GSD quick]` aunque tenga phase_id residual"
    - "Una sesión con gsd_mode === 'full' y phase_id renderiza `[GSD phase N]`"
    - "Una sesión con gsd_mode === 'full' sin phase_id renderiza `[GSD bootstrap]`"
    - "Sesiones no-GSD (gsd falsy) siguen sin tag (cadena vacía) — status quo Phase 10 D-19"
    - "src/orchestrator/prompt.md sección `## Sesiones GSD` incluye párrafo nuevo aclarando quick"
    - "El párrafo cita `[GSD quick]` y `kodo:gsd-quick` y usa el placeholder `{{provider_name}}`"
    - "Los 4 pasos numerados del flujo full y el cierre 'No dupliques el gate' quedan intactos verbatim"
  artifacts:
    - path: "src/orchestrator/launch.js"
      provides: "buildContextSummary con gsdTag mode-first emitiendo 3 etiquetas: [GSD quick], [GSD phase N], [GSD bootstrap]"
      contains: "mode === 'quick' ? 'quick'"
    - path: "src/orchestrator/prompt.md"
      provides: "Sección `## Sesiones GSD` con párrafo nuevo `**Sesiones quick.**` al final"
      contains: "Sesiones quick"
  key_links:
    - from: "src/orchestrator/launch.js"
      to: "src/labels.js getSessionMode"
      via: "named import (primer consumer)"
      pattern: "import \\{ getSessionMode \\} from '\\.\\./labels\\.js'"
---

<objective>
Bifurcar dos puntos del orchestrator-side para distinguir sesiones quick:

1. `buildContextSummary` en `src/orchestrator/launch.js`: el cómputo del `gsdTag` cambia de ternary `phase_id ? phase N : bootstrap` a switch mode-first que emite tres etiquetas: `[GSD quick]`, `[GSD phase N]`, `[GSD bootstrap]`. Defensa en profundidad: si una sesión quick tuviera `phase_id` residual (no debería, dispatcher lo descarta), igualmente se renderiza como quick.

2. `src/orchestrator/prompt.md` sección `## Sesiones GSD`: insertar un solo párrafo nuevo al final aclarando que las sesiones `[GSD quick]` no se verifican via `kodo gsd verify` y se revisan manualmente.

Cumple QUICK-07 y success criteria 3 y 4 de Phase 12.

Purpose: el orchestrator humano ve la pizarra (output de `buildContextSummary`) y lee `prompt.md` como guía. Sin estos cambios, una sesión quick aparece taggeada igual que full+bootstrap (`[GSD bootstrap]`) y el orchestrator intenta ejecutar `kodo gsd verify` que falla. Este plan da visibilidad explícita al modo quick en ambos lugares.
Output: 4 líneas modificadas en launch.js (1 import + 4 líneas reemplazando 1 ternary) + 2 líneas nuevas en prompt.md (línea en blanco + párrafo). Diff total <30 líneas. Idioma ES preservado en prompt.md (D-16 Phase 10), tag EN literal en launch.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-hook-orchestrator-bifurcation/12-CONTEXT.md
@.planning/phases/12-hook-orchestrator-bifurcation/12-PATTERNS.md
@.planning/phases/11-quick-mode-recognition-persistence/11-CONTEXT.md

@src/orchestrator/launch.js
@src/orchestrator/prompt.md
@src/labels.js

<interfaces>
<!-- Helper Phase 11 a consumir: -->

```javascript
// src/labels.js:82
export function getSessionMode(session) {
  if (!session?.gsd) return null;
  return session.gsd_mode || 'full';
}
```

<!-- launch.js:122 forma actual del cómputo del gsdTag: -->

```javascript
// src/orchestrator/launch.js:120-122
// Phase 10 D-19: taggear sesiones GSD para que el orquestador las identifique.
// Pitfall #4: phase_id puede estar ausente en modo bootstrap (Phase 9 D-11).
const gsdTag = s.gsd ? ` \`[GSD ${s.phase_id ? `phase ${s.phase_id}` : 'bootstrap'}]\`` : '';
```

<!-- prompt.md sección actual `## Sesiones GSD` (líneas 75-88): -->

```markdown
## Sesiones GSD

Las sesiones con `gsd: true` en `state.json` siguen un flujo estructurado de fase (...). Cuando una sesión GSD termina y entra a Review:

1. **Lee los artefactos** — ...
2. **Ejecuta el gate** — `kodo gsd verify <session-id>`. ...
3. **Actúa según el verdict del stdout:** ...
4. **Debugging previo al verify:** ...

**No dupliques el gate en comentarios manuales.** Todo el lifecycle GSD se orquesta desde el CLI; tu rol es leer los artefactos, ejecutar el verify y continuar con la siguiente ronda de supervisión.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Importar getSessionMode en launch.js</name>
  <files>src/orchestrator/launch.js</files>
  <read_first>
    - src/orchestrator/launch.js (líneas 1-10 — verás los 7 imports actuales: readFileSync, fileURLToPath, dirname/join, randomUUID, loadConfig, listSessions, cmux)
    - src/labels.js (líneas 82-85 — confirma signature de getSessionMode)
    - .planning/phases/12-hook-orchestrator-bifurcation/12-PATTERNS.md (§"Site 3" — el bloque "Imports actuales" e "Import a añadir")
  </read_first>
  <behavior>
    - El archivo importa getSessionMode como named import desde '../labels.js'
    - Los 7 imports existentes quedan intactos
    - Es el PRIMER import de `../labels.js` desde launch.js
  </behavior>
  <action>
Añadir el import en `src/orchestrator/launch.js` después de la línea 8 (`import * as cmux from '../cmux/client.js';`). Línea exacta a insertar:

```javascript
import { getSessionMode } from '../labels.js';
```

Resultado del bloque de imports (líneas 2-9 después del cambio):

```javascript
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { loadConfig } from '../config.js';
import { listSessions } from '../session/state.js';
import * as cmux from '../cmux/client.js';
import { getSessionMode } from '../labels.js';
```

NO modificar imports existentes. Mantener la directiva `// @ts-check` en línea 1.
  </action>
  <verify>
    <automated>node --check src/orchestrator/launch.js && node -e "import('./src/orchestrator/launch.js').then(() => console.log('OK')).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { getSessionMode } from '../labels.js'" src/orchestrator/launch.js` returns `1`
    - `head -1 src/orchestrator/launch.js | grep -q "// @ts-check"` (directiva preservada)
    - `grep -E "^import" src/orchestrator/launch.js | wc -l` returns `8` (7 originales + 1 nuevo)
    - `node --check src/orchestrator/launch.js` exits 0
  </acceptance_criteria>
  <done>
    `launch.js` importa `getSessionMode` desde `../labels.js`. Es el primer import desde `labels.js` en este archivo. La directiva `// @ts-check` y los 7 imports existentes están intactos.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reemplazar gsdTag ternary por switch mode-first (D-11, D-12, D-13)</name>
  <files>src/orchestrator/launch.js</files>
  <read_first>
    - src/orchestrator/launch.js (líneas 108-129 completas — `buildContextSummary`, específicamente el ternary actual en línea 122)
    - .planning/phases/12-hook-orchestrator-bifurcation/12-CONTEXT.md (D-11 con bloques verbatim del before/after, D-12 sobre YAGNI, D-13 sobre status quo no-GSD)
    - .planning/phases/12-hook-orchestrator-bifurcation/12-PATTERNS.md (§"Site 3" subsección "Reemplazo objetivo" — el bloque verbatim a copiar)
  </read_first>
  <behavior>
    - Sesión gsd_mode='quick' → tag ` \`[GSD quick]\``
    - Sesión gsd_mode='full' con phase_id → tag ` \`[GSD phase <N>]\``
    - Sesión gsd_mode='full' sin phase_id → tag ` \`[GSD bootstrap]\``
    - Sesión legacy gsd:true sin gsd_mode con phase_id → tag ` \`[GSD phase <N>]\`` (D-08 Phase 11: legacy lee como full)
    - Sesión gsd:false / sin campo gsd → cadena vacía (status quo D-13)
    - Defensa en profundidad: gsd_mode='quick' con phase_id residual → renderiza `[GSD quick]`, NO `[GSD phase N]`
    - El cómputo queda inline en `buildContextSummary` (D-12: YAGNI, no extraer helper)
  </behavior>
  <action>
Modificar `src/orchestrator/launch.js` líneas 120-122. La línea 122 es la única que cambia funcionalmente, pero por claridad se actualiza también el comentario en líneas 120-121 (obsoleto al introducir D-11 Phase 12).

**Reemplazar líneas 120-122:**

```javascript
      // Phase 10 D-19: taggear sesiones GSD para que el orquestador las identifique.
      // Pitfall #4: phase_id puede estar ausente en modo bootstrap (Phase 9 D-11).
      const gsdTag = s.gsd ? ` \`[GSD ${s.phase_id ? `phase ${s.phase_id}` : 'bootstrap'}]\`` : '';
```

**Por:**

```javascript
      // Phase 12 D-11: prioridad mode-first. Una sesión quick con phase_id
      // residual (no debería existir — dispatcher lo descarta — pero defensa
      // en profundidad) renderiza [GSD quick], no [GSD phase N].
      // D-12: cómputo inline (YAGNI — un solo callsite, no se extrae helper).
      // D-13: sesiones no-GSD siguen sin tag (status quo Phase 10 D-19).
      let gsdTag = '';
      if (s.gsd) {
        const mode = getSessionMode(s);
        const inner = mode === 'quick' ? 'quick' : (s.phase_id ? `phase ${s.phase_id}` : 'bootstrap');
        gsdTag = ` \`[GSD ${inner}]\``;
      }
```

**Validaciones críticas:**

1. **NO tocar líneas 123-124** (los `lines.push` que consumen `gsdTag`). El template literal externo sigue siendo `` `- **${s.task_ref}**${gsdTag}: ${s.summary}` ``.

2. **NO tocar el `for (const s of running)`** ni el resto de `buildContextSummary`.

3. **El `let gsdTag = ''` inicial** importa: si no entra al `if (s.gsd)`, `gsdTag` queda como cadena vacía y el template literal lo concatena sin afectar la línea (status quo D-13).

4. **El backtick en `[GSD ${inner}]`**: usar template literal regular con `\`` escapado al inicio y final del tag. NO confundir con un código backtick de Markdown — el output es markdown que el orquestador renderiza. El patrón ya estaba así en la línea 122 original.

5. **D-12 — NO extraer helper**: NO crear `function buildGsdTag(session)` ni similar. Un solo callsite hoy. Si Phase 13 necesita testearlo aislado, se extrae entonces.

Por qué se hace así:
- D-11: mode-first garantiza que `getSessionMode === 'quick'` gane sobre `phase_id` (defensa en profundidad).
- D-13: sesiones no-GSD (`s.gsd` falsy) no entran al `if`, `gsdTag` queda `''` → `${gsdTag}` no añade nada al template literal externo. Idéntico al output Phase 10 status quo.
- El comentario actualizado cita D-11 + D-12 + D-13 explícitamente.
  </action>
  <verify>
    <automated>node --input-type=module -e "
import { buildContextSummary } from './src/orchestrator/launch.js';

const baseSession = (overrides) => ({
  task_ref: 'TASK-X', summary: 's', workspace_ref: 'w', project_path: '/p',
  status: 'running', started_at: new Date().toISOString(), ...overrides
});
const config = { claude: { max_parallel: 5 } };

// Test 1: quick session
const out1 = buildContextSummary([baseSession({ gsd: true, gsd_mode: 'quick' })], config);
console.assert(out1.includes('\`[GSD quick]\`'), 'quick tag missing: ' + out1);
console.assert(!out1.includes('\`[GSD bootstrap]\`'), 'should NOT show bootstrap');
console.assert(!out1.includes('\`[GSD phase'), 'should NOT show phase');

// Test 2: full+phase
const out2 = buildContextSummary([baseSession({ gsd: true, gsd_mode: 'full', phase_id: '7' })], config);
console.assert(out2.includes('\`[GSD phase 7]\`'), 'phase tag missing: ' + out2);
console.assert(!out2.includes('\`[GSD quick]\`'), 'should NOT show quick');

// Test 3: full+bootstrap (sin phase_id)
const out3 = buildContextSummary([baseSession({ gsd: true, gsd_mode: 'full' })], config);
console.assert(out3.includes('\`[GSD bootstrap]\`'), 'bootstrap tag missing: ' + out3);
console.assert(!out3.includes('\`[GSD quick]\`'), 'should NOT show quick');

// Test 4: legacy gsd:true sin gsd_mode con phase_id (Phase 11 D-08: lee como full)
const out4 = buildContextSummary([baseSession({ gsd: true, phase_id: '3' })], config);
console.assert(out4.includes('\`[GSD phase 3]\`'), 'legacy should be tagged as phase');

// Test 5: defensa en profundidad — quick con phase_id residual
const out5 = buildContextSummary([baseSession({ gsd: true, gsd_mode: 'quick', phase_id: '99' })], config);
console.assert(out5.includes('\`[GSD quick]\`'), 'quick with residual phase_id should still show quick');
console.assert(!out5.includes('\`[GSD phase 99]\`'), 'should NOT leak phase_id');

// Test 6: no-GSD — sin tag
const out6 = buildContextSummary([baseSession({ gsd: false })], config);
console.assert(!out6.includes('[GSD'), 'non-GSD should not have tag: ' + out6);
console.assert(out6.includes('TASK-X'), 'non-GSD should still render task_ref');

// Test 7: mixed sessions
const out7 = buildContextSummary([
  baseSession({ task_ref: 'A', gsd: true, gsd_mode: 'quick' }),
  baseSession({ task_ref: 'B', gsd: true, gsd_mode: 'full', phase_id: '5' }),
  baseSession({ task_ref: 'C' })
], config);
console.assert(out7.includes('\`[GSD quick]\`'), 'mixed: quick tag missing');
console.assert(out7.includes('\`[GSD phase 5]\`'), 'mixed: phase tag missing');
console.assert(out7.split('[GSD').length === 3, 'mixed: expected exactly 2 GSD tags (1 quick + 1 phase), got: ' + out7);

console.log('OK');
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "let gsdTag = '';" src/orchestrator/launch.js`
    - `grep -q "if (s.gsd) {" src/orchestrator/launch.js`
    - `grep -q "const mode = getSessionMode(s);" src/orchestrator/launch.js`
    - `grep -q "mode === 'quick' ? 'quick'" src/orchestrator/launch.js`
    - `grep -q "Phase 12 D-11" src/orchestrator/launch.js` (comentario actualizado)
    - `grep -c "const gsdTag = s.gsd ?" src/orchestrator/launch.js` returns `0` (ternary viejo eliminado — el reemplazo usa `let gsdTag = ''` + bloque `if`)
    - `node --check src/orchestrator/launch.js` exits 0
    - El bloque `<verify>` automated imprime `OK` y exits 0
  </acceptance_criteria>
  <done>
    `buildContextSummary` computa `gsdTag` con prioridad mode-first. Tres etiquetas posibles: `[GSD quick]`, `[GSD phase N]`, `[GSD bootstrap]`. Sesiones no-GSD siguen sin tag. Defensa en profundidad: quick con phase_id residual aún se taggea como quick. Cómputo inline (YAGNI). Comentario actualizado a D-11/D-12/D-13.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Insertar párrafo `**Sesiones quick.**` al final de prompt.md (D-14, D-15, D-16, D-17)</name>
  <files>src/orchestrator/prompt.md</files>
  <read_first>
    - src/orchestrator/prompt.md (líneas 75-89 completas — sección `## Sesiones GSD` actual con sus 4 pasos numerados y el cierre `**No dupliques el gate…**`)
    - .planning/phases/12-hook-orchestrator-bifurcation/12-CONTEXT.md (D-14, D-15, D-16, D-17 — patch incremental, anchor de inserción, contenido verbatim, placeholder existente)
    - .planning/phases/12-hook-orchestrator-bifurcation/12-PATTERNS.md (§"Site 4" subsección "Párrafo a insertar" — el contenido verbatim copiado de CONTEXT.md D-16)
    - src/orchestrator/launch.js (líneas 21-29 — confirma que `resolvePromptTemplate` ya resuelve `{{provider_name}}`)
  </read_first>
  <behavior>
    - El archivo conserva las líneas 1-88 verbatim (sin un solo cambio en los 4 pasos numerados ni en el cierre `**No dupliques el gate…**`)
    - Se añade UN solo párrafo nuevo después de la línea 88, separado por una línea en blanco
    - El párrafo empieza con `**Sesiones quick.**` (negrita)
    - El párrafo cita `[GSD quick]` y `kodo:gsd-quick` literales
    - El párrafo usa el placeholder existente `{{provider_name}}` (sin introducir nuevos)
    - El párrafo lleva negrita en `**NO ejecutes \`kodo gsd verify\`**`
    - Idioma ES (D-16 Phase 10)
  </behavior>
  <action>
Añadir al final de `src/orchestrator/prompt.md`, después de la línea 88 (`**No dupliques el gate en comentarios manuales.** ... ronda de supervisión.`).

**Pre-condición:** la línea 88 actual termina con `... ronda de supervisión.` (revisa el archivo). Confirma que NO hay líneas adicionales después (el archivo termina en EOF justo después de esa línea, sin trailing newline forzado o con uno solo).

**Insertar EXACTAMENTE estas 2 líneas al final del archivo (línea 89 = blanco, línea 90 = párrafo):**

```markdown

**Sesiones quick.** Las sesiones lanzadas por `kodo:gsd-quick` aparecen en la pizarra como `[GSD quick]`. Son one-shot (sin `VERIFICATION.md`), por eso **NO ejecutes `kodo gsd verify`** sobre ellas — el CLI no las soporta. Revísalas manualmente como cualquier sesión no-GSD: lee el comentario final del agente, valida en {{provider_name}} y decide si pasa a Done o necesita más trabajo.
```

**Validaciones críticas:**

1. **Línea en blanco entre línea 88 y el párrafo nuevo**: Markdown necesita una línea vacía entre párrafos para renderizarlos como tales. Si no, el párrafo nuevo se concatenaría al cierre `**No dupliques…**` y se rompería la lectura.

2. **NO añadir un h3 ni cambiar la sección a sub-secciones**: D-15 lo prohíbe explícitamente. Es un párrafo más al final de la misma h2 `## Sesiones GSD`.

3. **NO modificar las líneas 1-88**: ni los 4 pasos numerados, ni el cierre `**No dupliques el gate…**`, ni el inicio `Las sesiones con \`gsd: true\`…`. D-14 patch incremental.

4. **NO introducir placeholders nuevos**: usar `{{provider_name}}` que `resolvePromptTemplate` ya resuelve (D-17). NO escribir literalmente "Plane" — el proyecto soporta multiples providers.

5. **Negrita selectiva (D-15)**: solo `**Sesiones quick.**` y `**NO ejecutes \`kodo gsd verify\`**`. NO poner negrita en otras frases — preserva la economía visual del resto de la sección.

6. **Backticks**: `\`kodo:gsd-quick\``, `` `[GSD quick]` ``, `` `VERIFICATION.md` ``, `` `kodo gsd verify` ``. Coinciden con el resto de la sección.

7. **Trailing newline**: si el archivo actual termina con `\n` final, mantenerlo. Si termina sin newline, mantener sin newline. NO normalizar.

Por qué se hace así:
- D-14: patch incremental — mínimo riesgo de romper redacción probada Phase 10. Diff claro.
- D-15: párrafo único al final, lectura natural (modo principal full primero, excepción quick después).
- D-16: contenido del párrafo es literal de CONTEXT.md — incorpora identificación + exclusión + acción + justificación en una sola frase.
- D-17: `{{provider_name}}` ya existe en líneas 80, 82 — la sustitución la hace `resolvePromptTemplate` en `launch.js`. NO se toca `launch.js` para esto.
  </action>
  <verify>
    <automated>grep -q "Sesiones quick" src/orchestrator/prompt.md && grep -q "kodo:gsd-quick" src/orchestrator/prompt.md && grep -q "\\[GSD quick\\]" src/orchestrator/prompt.md && grep -q "NO ejecutes \`kodo gsd verify\`" src/orchestrator/prompt.md && grep -q "{{provider_name}}" src/orchestrator/prompt.md && grep -q "No dupliques el gate" src/orchestrator/prompt.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Sesiones quick" src/orchestrator/prompt.md`
    - `grep -q "\`kodo:gsd-quick\`" src/orchestrator/prompt.md`
    - `grep -q "\\[GSD quick\\]" src/orchestrator/prompt.md`
    - `grep -q "NO ejecutes \`kodo gsd verify\`" src/orchestrator/prompt.md`
    - `grep -q "Revísalas manualmente" src/orchestrator/prompt.md`
    - `grep -q "{{provider_name}}" src/orchestrator/prompt.md` (placeholder presente — debería estar más de una vez en total)
    - `grep -q "No dupliques el gate en comentarios manuales" src/orchestrator/prompt.md` (cierre Phase 10 preservado)
    - `grep -c "## Sesiones GSD" src/orchestrator/prompt.md` returns `1` (la sección sigue siendo única, sin sub-h3)
    - `grep -q "kodo gsd verify <session-id>" src/orchestrator/prompt.md` (paso 2 del flujo full preservado verbatim)
    - `grep -q "1\\. \\*\\*Lee los artefactos\\*\\*" src/orchestrator/prompt.md` (paso 1 preservado)
    - El archivo es Markdown válido (sin errores de syntax al parsear)
  </acceptance_criteria>
  <done>
    `src/orchestrator/prompt.md` tiene un párrafo nuevo al final de `## Sesiones GSD` aclarando el modo quick. Los 4 pasos numerados y el cierre `**No dupliques el gate**` están intactos. El placeholder `{{provider_name}}` se usa sin introducir nuevos. Negrita aplicada solo a `**Sesiones quick.**` y `**NO ejecutes \`kodo gsd verify\`**`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `state.json` → buildContextSummary | El cómputo del `gsdTag` consume `s.gsd`, `s.gsd_mode`, `s.phase_id` desde `state.json`. Todos escritos por kodo (no input externo directo). |
| `prompt.md` → resolvePromptTemplate | El template se renderiza al iniciar el orchestrator. `{{provider_name}}` se sustituye por valor de config (controlado). NO se procesan otros placeholders en este patch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | gsdTag computation con mode-first prioridad | mitigate | El cambio de ternary a switch mode-first **es ella misma** una mitigación: si una sesión malformada (escritura corrupta a `state.json`, race condition) tuviera `gsd_mode='quick'` con `phase_id='99'` residual, el render correcto es `[GSD quick]`. La forma anterior (ternary) habría priorizado `phase_id` y emitido `[GSD phase 99]` — falso positivo que llevaría al orchestrator a ejecutar verify sobre una sesión no verificable. |
| T-12-08 | Information Disclosure | prompt.md párrafo cita placeholder | accept | El párrafo usa `{{provider_name}}` resuelto en runtime — no hardcodea "Plane" ni filtra info de provider. Usuarios con providers custom (GitHub Issues, Linear) reciben el placeholder correcto. |
| T-12-09 | Repudiation | sesiones no-GSD sin tag (D-13 status quo) | accept | Status quo Phase 10 — sesiones sin `gsd === true` no llevan tag. NO se introduce `[no-GSD]` ni similar (out of scope per CONTEXT.md `<deferred>`). El orchestrator distingue por ausencia de tag. |
| T-12-10 | Elevation of Privilege | switch sin default explícito | accept | El `if (s.gsd)` actúa como guard. Si `getSessionMode` devolviera un valor inesperado (futuro modo `'research'`), el ternary interno cae a `s.phase_id ? phase N : bootstrap` — falso negativo seguro: la sesión se taggea como full, no se rompe el render. La directiva TS-check del archivo advertiría en compile time si el typing del helper cambiara. |
</threat_model>

<verification>
- `node --check src/orchestrator/launch.js` exits 0
- `grep -E "^import" src/orchestrator/launch.js | wc -l` returns 8
- El bloque `<verify>` automated del Task 2 imprime `OK`
- El bloque `<verify>` automated del Task 3 imprime `OK`
- `grep -c "## Sesiones GSD" src/orchestrator/prompt.md` returns 1 (no se introduce sub-sección)
- Los 4 pasos numerados de prompt.md siguen presentes (`grep -E "^[1-4]\\." src/orchestrator/prompt.md | wc -l` returns 4)
- El cierre Phase 10 `**No dupliques el gate…**` sigue presente
</verification>

<success_criteria>
- Import de `getSessionMode` desde `../labels.js` añadido en launch.js (Task 1) — primer import de labels.js en este archivo
- `buildContextSummary` computa `gsdTag` con prioridad mode-first inline (Task 2)
- Tres etiquetas posibles: `[GSD quick]`, `[GSD phase N]`, `[GSD bootstrap]`
- Defensa en profundidad: sesión `gsd_mode='quick'` con `phase_id` residual aún renderiza `[GSD quick]`
- Sesiones no-GSD siguen sin tag (cadena vacía) — D-13 status quo
- Cómputo inline en `buildContextSummary` (D-12 YAGNI — no se extrae helper)
- `prompt.md` sección `## Sesiones GSD` con párrafo nuevo `**Sesiones quick.**` al final (Task 3)
- Líneas 1-88 de prompt.md preservadas verbatim (D-14 patch incremental)
- Párrafo cita `[GSD quick]`, `kodo:gsd-quick`, `{{provider_name}}` (placeholder existente)
- Negrita aplicada solo a `**Sesiones quick.**` y `**NO ejecutes \`kodo gsd verify\`**` (D-15)
- Idioma ES preservado en prompt.md (D-16 Phase 10)
- Tag EN literal en launch.js (palabra `quick` dentro del tag, sin frase)
</success_criteria>

<output>
After completion, create `.planning/phases/12-hook-orchestrator-bifurcation/12-03-SUMMARY.md` con:
- Línea exacta donde se añadió el import de launch.js (`src/orchestrator/launch.js:NN`)
- Líneas donde vive el nuevo cómputo del gsdTag (`src/orchestrator/launch.js:NN-MM`)
- Confirmación verbatim del párrafo añadido a prompt.md (las 2 líneas insertadas)
- Confirmación de que las líneas 1-88 de prompt.md NO se tocaron
- Nota de hand-off para Phase 13: "test/launch.test.js puede importar `buildContextSummary` y assert los 5 estados (quick, full+phase, full+bootstrap, legacy, no-GSD) + el caso defensa-en-profundidad (quick + phase_id residual)"
</output>
