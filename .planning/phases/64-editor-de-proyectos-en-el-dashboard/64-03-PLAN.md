---
phase: 64
plan: 03
type: execute
wave: 3
depends_on:
  - 64-02
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-projects.test.js
autonomous: true
requirements:
  - PROJ-04
must_haves:
  truths:
    - "Una tecla en mode:'projects' abre los módulos: projects-modules-loading dispara listModulesFn(projectId) con el MISMO guard projectsReqRef (2º hop); éxito → projects-modules (PROJ-04, D-05)"
    - "Mapear la ruta de un módulo persiste projects[id] = {default, modules:{[mod.name]:ruta}} preservando default y los otros módulos, vía setModulePath + saveProjectsFn (PROJ-04, D-05/D-06)"
    - "Si listModulesFn devuelve lista vacía (GitHub / provider sin módulos) → footer PROJECTS_NO_MODULES, no-op, never-throws (PROJ-04, D-05)"
    - "La ruta de un módulo se valida con validateExistingDir antes de guardar (reuso del text-input y validador del carril base); inválida → footer rojo, NO escribe (PROJ-04, D-05)"
    - "Esc durante projects-modules-loading invalida el fetch en vuelo (projectsReqRef) y vuelve a projects (PROJ-04 race / Pitfall 3)"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "modos projects-modules-loading/projects-modules + module-edit, handler de tecla módulos, 2º hop async con projectsReqRef, copy PROJECTS_NO_MODULES + PROJECTS_MODULES_TITLE"
      contains: "projects-modules"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "render del sub-overlay de módulos (loading/lista con mapeo/edit) reusando el text-input"
      contains: "renderModulesOverlay"
    - path: "test/dashboard-projects.test.js"
      provides: "cobertura integration PROJ-04: tecla módulos → listModulesFn ok → {default,modules}; github/vacío → footer no-módulos no-op; staleness 2º hop"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "listModulesFn (prop DI)"
      via: "handler de módulos await listModulesFn(projectId) (discriminado {ok:true,modules}|{ok:false,error})"
      pattern: "listModulesFn"
    - from: "src/cli/dashboard/App.js"
      to: "src/projects-shape.js"
      via: "import setModulePath/getModuleMap para mutar el snapshot al mapear un módulo"
      pattern: "setModulePath|getModuleMap"
    - from: "src/cli/dashboard/App.js"
      to: "src/path-validate.js"
      via: "validateExistingDir reusado para la ruta del módulo"
      pattern: "validateExistingDir"
---

<objective>
Añadir el **soporte mínimo de módulos** (PROJ-04 / D-05): los modos `projects-modules-loading` / `projects-modules` en App.js, el segundo hop async `listModulesFn(projectId)` con el mismo guard `projectsReqRef` (Pitfall 3 — dos hops, un ref dedicado), el editor de ruta de módulo reusando el text-input + `validateExistingDir` del carril base, y la persistencia como `{ default, modules: { [mod.name]: ruta } }` vía `setModulePath` (Plan 01). Espejo del soporte de módulos del wizard (`cli.js:700-740`). Provider sin módulos (GitHub / lista vacía) → footer informativo no-op, never-throws.

Purpose: PROJ-04 es criterio de éxito de la fase (no opcional). Se aísla en su propio plan/ola (recomendación RESEARCH) porque añade un SEGUNDO hop async + la asimetría de `listModules` (vive solo en PlaneClient, no en el contrato), reduciendo el blast radius del carril base ya verificado en Plan 02.
Output: `App.js` + `SessionTable.js` ampliados con los modos de módulos y `test/dashboard-projects.test.js` extendido. Opera con DI fakes (el cableado real de `listModulesFn` lo hace Plan 04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-CONTEXT.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md
@src/projects-shape.js
@src/path-validate.js
</context>

## Artifacts this phase produces (Plan 03)

- **Nuevos `mode`:** `'projects-modules-loading'` (transitorio async, 2º hop), `'projects-modules'` (lista de módulos del proyecto con su mapeo + edición de ruta reusando el text-input).
- **Nuevo handler de tecla** dentro de `mode === 'projects'`: tecla de módulos (`m` — coherente "m = módulos", no colisiona porque la rama `mode:'projects'` se evalúa antes que `mode:'list'`; Pitfall 0). Dispara `listModulesFn(projectId)` token-guarded.
- **Reuso del estado del Plan 02:** `projectsReqRef` (mismo ref para el 2º hop — cada incremento invalida el anterior, Pitfall 3), `buffer`/`cursor` (text-input de ruta de módulo), `fieldCursor` (cursor de la lista de módulos), `projectsEditError` (validación inline). Nuevo estado mínimo si hace falta: el `projectId` activo y la lista de módulos congelada (p.ej. dentro de `projectsSnapshot` o un `modulesSnapshot`).
- **Nuevas constantes de copy EXPORTADAS:** `PROJECTS_MODULES_TITLE`, `PROJECTS_NO_MODULES` (`este provider no tiene módulos`, espejo `cli.js:711-714`).
- **Nueva prop DI en App:** `listModulesFn` (async → `{ok:true,modules}|{ok:false,error}`). Default fake para tests.
- **Nueva función de render** en SessionTable.js: `renderModulesOverlay(...)`.
- **Test extendido:** `test/dashboard-projects.test.js` con la cobertura de PROJ-04.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modos de módulos + 2º hop async + persistencia en App.js</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - `src/cli/dashboard/App.js` (los modos `projects`/`projects-edit` añadidos en Plan 02 — molde exacto a extender; el handler `m` y `projectsReqRef`; el bloque de constantes PROJECTS_*; el text-input de projects-edit)
    - `src/cli.js` (~líneas 700-740: el wizard mapea módulos — `listModules` vía PlaneClient directo ~704-710, forma `{default, modules}` con key `mod.name` ~728/737, y el caso "sin módulos" ~711-714)
    - `src/projects-shape.js` (setModulePath/getModuleMap/getProjectPath — Plan 01)
    - `src/path-validate.js` (validateExistingDir — reuso para la ruta de módulo)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Pattern 4 "listModules variancia de provider — discriminado {ok}"; Pitfall 3 "dos hops async, un ref dedicado"; §Code Examples copy PROJECTS_NO_MODULES)
  </read_first>
  <behavior>
    - tecla módulos en mode:'projects' → `setMode('projects-modules-loading')`, `reqId = ++projectsReqRef.current`, `await listModulesFn(items[fieldCursor].id)`; staleness check; `{ok:true, modules}` con `modules.length>0` → congelar la lista + `setMode('projects-modules')`; `{ok:true, modules:[]}` (github / sin módulos) → footer PROJECTS_NO_MODULES + volver a projects (no-op); `{ok:false}` → footer error + volver a projects (never-throws).
    - mode:'projects-modules-loading': Esc → `projectsReqRef.current++` + volver a projects; traga el resto.
    - mode:'projects-modules': ↑/↓ navegan la lista de módulos (clamp sin wrap); Enter → precarga la ruta actual del módulo (getModuleMap(map[id])[mod.name] ?? '') en el text-input → edición; Esc → vuelve a projects.
    - edición de ruta de módulo: mismo molde de text-input; Enter → validateExistingDir; inválido → projectsEditError + no escribe; válido → `setModulePath(map, id, mod.name, value)` + saveProjectsFn + aviso de reinicio + volver a la lista de módulos.
  </behavior>
  <action>
Modificar `src/cli/dashboard/App.js` (cambio quirúrgico, preservar TODO lo de Plan 02):

1. **Prop DI:** añadir `listModulesFn` al destructuring de `App({...})` con default inerte `listModulesFn = async () => ({ ok: true, modules: [] })` (discriminado {ok}, espejo de listProjectsFn — Pattern 4). Documentar la asimetría en JSDoc: `listModules` NO está en el contrato TaskProvider (solo en PlaneClient) — el cableado condicional vive en index.js (Plan 04); App solo consume el discriminado.
2. **Constantes de copy EXPORTADAS:** `PROJECTS_MODULES_TITLE` (título del sub-overlay), `PROJECTS_NO_MODULES` (`este provider no tiene módulos`, espejo `cli.js:711-714`).
3. **Estado mínimo:** la lista de módulos congelada y el `projectId` activo. Reusar `projectsSnapshot` añadiéndole campos (p.ej. `modules` y `activeProjectId`) o un estado `modulesSnapshot` dedicado — discreción del planner; preferir extender `projectsSnapshot` para no proliferar estado. `fieldCursor` se reutiliza para la lista de módulos (se reinicia a 0 al entrar).
4. **Handler de tecla módulos** dentro de la rama `mode === 'projects'` (añadir junto a Enter/quitar): tecla `m` (coherente con "m = módulos"; NO colisiona — la rama `mode:'projects'` se evalúa ANTES de `mode:'list'`, Pitfall 0). Implementar el 2º hop de RESEARCH Pattern 1/4: `const id = items[fieldCursor]?.id; if(!id) return; setMode('projects-modules-loading'); const reqId = ++projectsReqRef.current; const result = await listModulesFn(id); if (projectsReqRef.current !== reqId) return;` luego ramificar: si `result.ok && result.modules.length` → congelar `{ modules: result.modules, activeProjectId: id }` + `setFieldCursor(0)` + `setMode('projects-modules')`; si `result.ok && !result.modules.length` → `setFocusError(PROJECTS_NO_MODULES)` + `setMode('projects')` (no-op, never-throws); si `!result.ok` → `setFocusError(PROJECTS_LOAD_FAILED(result.error))` + `setMode('projects')` (never-throws, no escribe).
5. **Ramas de modo nuevas** insertadas junto a las de Plan 02 (entre projects-error y filter):
   - `mode === 'projects-modules-loading'`: Esc → `projectsReqRef.current++` + `setMode('projects')`; traga el resto.
   - `mode === 'projects-modules'`: ↑/↓ → fieldCursor clamp [0, modules.length-1] sin wrap; Enter → `const mod = modules[fieldCursor]; if(!mod) return; const cur = getModuleMap(map[activeProjectId])[mod.name] ?? ''; setBuffer(cur); setCursor(cur.length); setProjectsEditError(null);` y entrar a un sub-estado de edición de módulo (reusar `mode:'projects-edit'` con un flag de contexto, o un `mode:'projects-modules-edit'` — discreción; preferir un modo dedicado `projects-modules-edit` para que el Enter sepa que debe llamar setModulePath, no setProjectPath); Esc → `setMode('projects')`.
   - `mode === 'projects-modules-edit'` (si se opta por modo dedicado): mismo molde de text-input que projects-edit; Enter → `const res = validateExistingDir(buffer)`; inválido → `setProjectsEditError(res.error)` + return; válido → `const next = setModulePath(map, activeProjectId, mod.name, res.value); try { saveProjectsFn(next); setProjectsSnapshot(s=>({...s, map: next})); setProjectsEditError(null); setFocusError(PROJECTS_SAVED_RESTART); setFooterColor('yellow'); setMode('projects-modules'); } catch { setProjectsEditError(...) }`; Esc → vuelve a projects-modules.
6. **Render:** pasar a `SessionTable` las props nuevas que necesite el sub-overlay de módulos (la lista de módulos congelada + activeProjectId, accesibles vía `projectsSnapshot` extendido).

Reusar `validateExistingDir` y `setModulePath`/`getModuleMap` de Plan 01 — NO duplicar lógica. El carril de módulos respeta los mismos invariantes que el base: never-throws, color-isolation, forma dual preservada (setModulePath materializa `{default, modules}` sin perder el default ni otros módulos).
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `App.js` contiene `mode === 'projects-modules-loading'` y `'projects-modules'` (+ el modo/contexto de edición de módulo).
    - `App.js` declara la prop DI `listModulesFn` (discriminado {ok}) y exporta `PROJECTS_MODULES_TITLE`, `PROJECTS_NO_MODULES`.
    - El 2º hop usa el MISMO `projectsReqRef` con check de staleness (Pitfall 3); Esc durante loading invalida el fetch.
    - El caso lista-vacía (github/sin módulos) muestra `PROJECTS_NO_MODULES` y es no-op (no escribe, never-throws).
    - El guardado de un módulo usa `setModulePath` (forma `{default,modules}` preservando default/otros módulos) y `validateExistingDir` pre-escritura.
    - `npm test` no regresiona los tests existentes (incl. el carril base de Plan 02).
  </acceptance_criteria>
  <done>El sub-editor de módulos abre con un 2º hop async guard-eado, valida y persiste la ruta de cada módulo como {default,modules} preservando la forma dual, y degrada con gracia si el provider no tiene módulos (PROJ-04, D-05/D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render del sub-overlay de módulos en SessionTable.js</name>
  <files>src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - `src/cli/dashboard/SessionTable.js` (`renderProjectsOverlay` añadido en Plan 02 — molde a espejar; `renderConfigOverlay` para el cursor `<Text inverse>`; los early-returns por modo)
    - `src/cli/dashboard/App.js` (constantes PROJECTS_MODULES_TITLE/PROJECTS_NO_MODULES; la lista de módulos congelada en projectsSnapshot)
    - `src/projects-shape.js` (getModuleMap — para el estado de mapeo de cada módulo)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (Pitfall 6 ANSI del inverse — assertar por contenido)
  </read_first>
  <behavior>
    - mode:'projects-modules-loading' → título + texto de carga.
    - mode:'projects-modules' → PROJECTS_MODULES_TITLE + lista de módulos con su mapeo (ruta de getModuleMap(map[activeProjectId])[mod.name] o "sin mapear"); fila en fieldCursor resaltada.
    - mode:'projects-modules-edit' → la fila activa renderiza el text-input (cursor invertido); projectsEditError → línea roja.
    - Todo color vía <Text>; el sub-overlay no muestra secretos (solo módulos del provider + rutas).
  </behavior>
  <action>
Modificar `src/cli/dashboard/SessionTable.js` (cambio quirúrgico):

1. **Imports:** añadir `PROJECTS_MODULES_TITLE`, `PROJECTS_NO_MODULES` al import desde `./App.js`; importar `getModuleMap` de `../../projects-shape.js`.
2. **`renderModulesOverlay(snapshot, fieldCursor, mode, buffer, cursor, projectsEditError)`** (molde `renderProjectsOverlay` de Plan 02): cabecera `PROJECTS_MODULES_TITLE`; lista de `snapshot.modules` — cada fila `mod.name` + el estado de mapeo derivado de `getModuleMap(snapshot.map[snapshot.activeProjectId])[mod.name]` (ruta o "sin mapear"); fila en `fieldCursor` resaltada. En el modo de edición de módulo, la fila activa renderiza el text-input con el cursor por `<Text inverse>` (molde renderConfigOverlay). Si `projectsEditError != null`, línea de footer roja. Pista de teclas (Enter editar · Esc volver).
3. **Early-return:** añadir junto a los de projects (Plan 02): `if (mode === 'projects-modules-loading') return <loading>`; `if ((mode === 'projects-modules' || mode === 'projects-modules-edit') && projectsSnapshot?.modules) return renderModulesOverlay(...)`.

Assertar por CONTENIDO (Pitfall 6). NO usar picocolors (color-isolation — cursor `<Text inverse>`).
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `SessionTable.js` contiene `renderModulesOverlay` y early-returns para los modos de módulos.
    - El sub-overlay muestra el estado de mapeo por módulo vía `getModuleMap` y NO muestra secretos.
    - El cursor del text-input de módulo se pinta con `<Text inverse>` (no picocolors); format-isolation sigue verde.
    - `node --test test/dashboard-projects.test.js` exits 0.
  </acceptance_criteria>
  <done>El sub-overlay de módulos renderiza la lista de módulos con su mapeo, el text-input de ruta con cursor y los estados loading, reusando el molde del overlay base sin romper color-isolation (PROJ-04, D-05).</done>
</task>

<task type="auto">
  <name>Task 3: Extender test/dashboard-projects.test.js con la cobertura de módulos (PROJ-04)</name>
  <files>test/dashboard-projects.test.js</files>
  <read_first>
    - `test/dashboard-projects.test.js` (el harness y los fakes ya creados en Plan 02 — injectProps con los 3 *Fn; añadir listModulesFn)
    - `src/cli/dashboard/App.js` (constantes PROJECTS_NO_MODULES/PROJECTS_MODULES_TITLE; la tecla de módulos y los modos)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md` (Per-Task map fila PROJ-04: tecla módulos → {default,modules}; github/vacío → footer no-módulos)
  </read_first>
  <action>
Extender `test/dashboard-projects.test.js` (añadir tests, no romper los de Plan 02). Ampliar `injectProps` con `listModulesFn`:
- caso plane: fake async `{ ok:true, modules:[{id:'mod1', name:'core'},{id:'mod2', name:'web'}] }`.
- caso github/vacío: fake async `{ ok:true, modules:[] }`.
Importar `PROJECTS_NO_MODULES` y `PROJECTS_MODULES_TITLE` de App.js.

Cubrir (cada test con `unmount()` en finally, `drain()` para settle):
1. **PROJ-04 (mapear módulo):** `m` (abre projects) → drain → sobre la fila p1 pulsar la tecla de módulos → `projects-modules-loading` → drain → `lastFrame()` contiene `PROJECTS_MODULES_TITLE` y `core`/`web`. Enter en `core` → editar a un dir REAL → `\r` → `saveProjectsFn` llamado con `p1 = { default: <ruta original>, modules: { core: <dir nuevo> } }` (forma `{default,modules}` correcta — assertar la estructura del argumento del spy).
2. **PROJ-04 (sin módulos):** con `listModulesFn` devolviendo `{ok:true, modules:[]}`, abrir proyectos, pulsar módulos sobre una fila → `lastFrame()` contiene `PROJECTS_NO_MODULES`, el modo sigue en projects (no abre sub-overlay), `saveProjectsFn` NO llamado (no-op never-throws).
3. **PROJ-04 (staleness 2º hop):** pulsar módulos (entra modules-loading) y antes del drain `\x1b` (Esc) → vuelve a projects; el resultado tardío de listModulesFn se descarta (no entra a projects-modules).

Mantener verdes los tests del carril base de Plan 02.
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-projects.test.js` exits 0 (carril base de Plan 02 + módulos de Plan 03).
    - El test demuestra que mapear un módulo persiste la forma `{default, modules:{[name]:ruta}}` (assert sobre el argumento del spy saveProjectsFn).
    - El test demuestra el caso sin-módulos (footer PROJECTS_NO_MODULES, no-op, saveProjectsFn NO llamado).
    - El test demuestra la staleness del 2º hop (Esc durante modules-loading descarta el resultado tardío).
    - `npm test` verde completo.
  </acceptance_criteria>
  <done>La cobertura integration de PROJ-04 verifica el mapeo de módulos en forma {default,modules}, la degradación sin-módulos y la staleness del 2º hop, todo con DI fakes (PROJ-04, D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listModulesFn (red, solo plane) → estado de la TUI | el 2º hop async entra como estado; un rechazo NO debe tumbar el árbol ink (never-throws) |
| ruta de módulo del operador → validateExistingDir | la ruta cruza el validador antes de setModulePath/saveProjectsFn |
| nombre de módulo (mod.name del provider) → key del mapa | la key del mapa de módulos proviene del provider, no del teclado (superficie de inyección mínima) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-10 | DoS | useInput projects-modules-loading / 2º hop | high | mitigate | listModulesFn never-throws discriminado; {ok:false}/vacío → footer + vuelve a projects, panel ink montado (PROJ-04/never-throws) |
| T-64-11 | Tampering | path traversal en mod.name como key | medium | mitigate | La key del mapa de módulos es `mod.name` del provider (cli.js:728), NO input libre del operador; el único input libre es la ruta (validada FS) |
| T-64-12 | DoS | projectsReqRef staleness (2 hops) | medium | mitigate | Un único ref dedicado; cada apertura (lista Y módulos) captura su reqId y descarta resultados tardíos (Pitfall 3) |
| T-64-13 | Tampering | setModulePath (forma dual) | medium | mitigate | setModulePath materializa {default,modules} preservando default y otros módulos; nunca colapsa la forma (D-06) |
| T-64-SC | Tampering | npm installs | low | accept | Cero dependencias nuevas (D-09 — package.json intacto); sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/dashboard-projects.test.js` verde (base + módulos).
- `npm test` verde completo (sin regresión).
- Inspección: el 2º hop reusa `projectsReqRef` (no un ref nuevo); `setModulePath` preserva default/otros módulos; cursor `<Text inverse>` (color-isolation).
</verification>

<success_criteria>
El operador abre los módulos de un proyecto con un 2º hop async, mapea/edita la ruta de cada módulo (validada) persistida como {default,modules}, y un provider sin módulos degrada con un footer informativo no-op — todo never-throws con guard de staleness y DI fakes (PROJ-04, D-05/D-06).
</success_criteria>

<output>
Create `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-03-SUMMARY.md` when done
</output>
