---
phase: 64
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-projects.test.js
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-05
must_haves:
  truths:
    - "El operador pulsa `m` en mode:'list' y entra a projects-loading; listProjectsFn se dispara con guard projectsReqRef y en éxito muestra la lista CONGELADA de proyectos con su estado [ruta]/[sin mapear] (PROJ-01, D-01/D-02/D-10)"
    - "Enter sobre una fila precarga la ruta actual (default si objeto, string si string, vacío si sin mapear) en el text-input; Enter valida con validateExistingDir; inválido → footer rojo, NO escribe, sigue en projects-edit (PROJ-02, D-03)"
    - "Una tecla quita el mapeo: saveProjectsFn se llama con el mapa sin la key (removeProjectMapping) + aviso de reinicio (PROJ-03, D-03/D-06)"
    - "Si listProjectsFn devuelve {ok:false} → mode:'projects-error'; `r` reintenta, `Esc` sale a list; saveProjectsFn NUNCA se llama; panel ink montado (PROJ-05, D-07)"
    - "Esc durante projects-loading invalida el fetch en vuelo vía projectsReqRef (resultado tardío descartado) y vuelve a list preservando selectedTaskId (PROJ-05 race, UX-03)"
    - "El poll /status sigue corriendo por debajo sin tocar el snapshot congelado del overlay (D-01, molde overlaySnapshot)"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "modos projects/projects-loading/projects-edit/projects-error, handler `m`, projectsReqRef dedicado, estado projectsSnapshot/projectsError/projectsEditError, copy PROJECTS_*, footer hint `m projects`"
      contains: "projects-loading"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "renderProjectsOverlay (lista con estado de mapeo + text-input de ruta) y estados loading/error"
      contains: "renderProjectsOverlay"
    - path: "test/dashboard-projects.test.js"
      provides: "cobertura integration PROJ-01/02-UI/03/05 + race: m abre, lista, edita+valida, quita, error+retry, Esc-cancela-fetch"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/path-validate.js"
      via: "import validateExistingDir; Enter de projects-edit lo corre ANTES de saveProjectsFn"
      pattern: "validateExistingDir"
    - from: "src/cli/dashboard/App.js"
      to: "src/projects-shape.js"
      via: "import setProjectPath/removeProjectMapping/getProjectPath para mutar el snapshot"
      pattern: "setProjectPath|removeProjectMapping"
    - from: "src/cli/dashboard/App.js"
      to: "listProjectsFn/loadProjectsFn/saveProjectsFn (props DI)"
      via: "handler `m` await listProjectsFn (discriminado {ok}); Enter/quitar llaman saveProjectsFn"
      pattern: "listProjectsFn|saveProjectsFn"
    - from: "src/cli/dashboard/SessionTable.js"
      to: "src/cli/dashboard/App.js"
      via: "consume props mode/projectsSnapshot/buffer/cursor/fieldCursor/projectsError/projectsEditError + constantes PROJECTS_*"
      pattern: "projectsSnapshot|PROJECTS_"
---

<objective>
Construir el **carril async base** del editor de proyectos: los modos `projects` / `projects-loading` / `projects-edit` / `projects-error` en la máquina de `useInput` de App.js (D-02), el fetch async de `listProjectsFn` con guard de request-token dedicado `projectsReqRef` (D-01, primer rechazo async convertido en estado — PROJ-05), la navegación two-level + acciones por tecla (D-03), la validación de ruta reusando el text-input de Phase 63 + `validateExistingDir` (PROJ-02), quitar mapeo (PROJ-03), y la degradación never-throws con retry (D-07/PROJ-05). Probado end-to-end con `test/dashboard-projects.test.js` y DI fakes.

Purpose: es el corazón de la fase y el carril de mayor riesgo del milestone — la primera fuente de datos ASYNC de red surfaced como estado de la TUI. Reusa toda la maquinaria de 63 (text-input buffer/cursor, snapshot congelado, footer transitorio, cursor clamp-sin-wrap) y solo añade los modos nuevos + la fuente async. NO incluye módulos (PROJ-04 → Plan 03) ni el cableado real (Plan 04 lo conecta; aquí se opera con DI fakes).
Output: `App.js` + `SessionTable.js` modificados y `test/dashboard-projects.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-CONTEXT.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-CONTEXT.md
@src/path-validate.js
@src/projects-shape.js
</context>

## Artifacts this phase produces (Plan 02)

- **Nuevos `mode`:** `'projects'` (lista navegable con estado de mapeo), `'projects-loading'` (transitorio async), `'projects-edit'` (text-input de ruta reusando el de 63), `'projects-error'` (degradación PROJ-05).
- **Nuevo handler de tecla:** `m` en la rama `mode === 'list'` de `useInput` (dispara `listProjectsFn`, token-guarded).
- **Nuevo ref:** `projectsReqRef = useRef(0)` (DEDICADO — NO reusa `overlayReqRef` que c/l/adopt/deriving comparten, Anti-pattern RESEARCH).
- **Nuevo estado en App.js:** `projectsSnapshot` ({ remote, map } congelado, null si cerrado), `projectsError` (string|null — mensaje del fallo de fetch, dirige projects-error), `projectsEditError` (string|null — error de validación de ruta inline, estado DEDICADO, Pitfall 2). Reusa `buffer`/`cursor` (text-input) y `fieldCursor` (cursor de la lista).
- **Nuevas constantes de copy EXPORTADAS** (molde `CONFIG_*`/`OVERLAY_*`): `PROJECTS_OVERLAY_TITLE`, `PROJECTS_LOADING`, `PROJECTS_UNMAPPED` (`[sin mapear]`), `PROJECTS_SAVED_RESTART`, `PROJECTS_REMOVED(ref)`, `PROJECTS_LOAD_FAILED(reason)`.
- **Nuevas props DI en App:** `listProjectsFn` (async → `{ok:true,projects}|{ok:false,error}`), `loadProjectsFn` (() => map), `saveProjectsFn` (map => void). Defaults fakes para tests del módulo.
- **Nueva función de render** en SessionTable.js: `renderProjectsOverlay(...)` + estados loading/error.
- **Test nuevo:** `test/dashboard-projects.test.js`.

<tasks>

<task type="auto">
  <name>Task 1: Test RED de integración del editor de proyectos (test/dashboard-projects.test.js) — fija el contrato</name>
  <files>test/dashboard-projects.test.js</files>
  <read_first>
    - `test/dashboard-config.test.js` (MOLDE directo: makeFakeClock/injectProps/render/stdin.write/lastFrame/flushTick + `unmount()` en finally; cómo inyecta `loadConfigFn`/`onSaveConfig`)
    - `test/dashboard-overlay.test.js` (helper `drain()` = 2x setImmediate para settle async; códigos de tecla `\r` Enter, `\x1b` Esc, `\x7f` backspace, `\x1b[D`/`\x1b[C` ←/→, `\x1b[A`/`\x1b[B` ↑/↓)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Code Examples "Test hermético del editor de proyectos"; §Validation Per-Task map PROJ-01/02-UI/03/05 + race)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md` (Per-Task Verification Map + Observabilidad: spy de saveProjectsFn, discriminante {ok}, projectsReqRef)
    - `src/cli/dashboard/App.js` (props actuales de App ~líneas 320-330; render de SessionTable ~1185-1214 — qué props se pasan)
  </read_first>
  <action>
Crear `test/dashboard-projects.test.js` en RED (debe FALLAR: importa props/constantes/modos aún inexistentes). Reusar VERBATIM el harness de `test/dashboard-config.test.js` (makeFakeClock, injectProps, render, drain de `dashboard-overlay.test.js`), extendiendo `injectProps` con los 3 `*Fn` DI de este plan:
- `listProjectsFn`: fake async que devuelve `{ ok:true, projects:[{id:'p1',identifier:'KL',name:'k-lab'},{id:'p2',identifier:'XX',name:'otro'}] }` en los casos de éxito y `{ ok:false, error:'ECONNREFUSED' }` en los de fallo.
- `loadProjectsFn`: fake síncrono que devuelve un mapa conocido (p.ej. `{ p1: '<dir-real-de-mkdtemp>' }`) — usar un dir REAL creado con `mkdtempSync` para que `validateExistingDir` lo acepte en el caso válido.
- `saveProjectsFn`: spy síncrono que registra el mapa recibido (punto de muestreo de PROJ-03 y de "el carril de error NUNCA escribe", PROJ-05).
Importar de `../src/cli/dashboard/App.js` las constantes nuevas `PROJECTS_OVERLAY_TITLE`, `PROJECTS_SAVED_RESTART`, `PROJECTS_UNMAPPED`, `PROJECTS_LOAD_FAILED`.

Cubrir (cada test con `unmount()` en finally; `await flushTick()` para el primer poll y `await drain()` para settle del await async):
1. **PROJ-01 / D-01/D-02/D-10:** `stdin.write('m')` → `projects-loading` → drain → `lastFrame()` contiene `PROJECTS_OVERLAY_TITLE`, `k-lab`, y el estado de mapeo (la fila mapeada muestra su ruta; la no mapeada muestra `PROJECTS_UNMAPPED`).
2. **PROJ-02 UI (válido) / D-03:** `m` → drain → `\r` (Enter en la fila p1 → projects-edit con la ruta precargada) → editar a un dir REAL existente → `\r` → `saveProjectsFn` llamado UNA vez con el mapa donde `p1` apunta al dir nuevo; `lastFrame()` matchea `/reinicia|reiniciar/i` (PROJECTS_SAVED_RESTART).
3. **PROJ-02 UI (inválido) / CFG-05-mol:** en projects-edit, teclear una ruta inexistente, `\r` → footer rojo con el error de validación, `saveProjectsFn` NO llamado, el modo sigue siendo projects-edit (la siguiente tecla edita — Pitfall 2).
4. **PROJ-03 / D-03/D-06:** en mode:'projects' sobre una fila mapeada, pulsar la tecla de quitar → `saveProjectsFn` llamado con el mapa SIN esa key; el frame refleja la fila como `PROJECTS_UNMAPPED`.
5. **PROJ-05 (error) / D-07:** con `listProjectsFn` devolviendo `{ok:false}`, `m` → drain → `lastFrame()` contiene el mensaje de `PROJECTS_LOAD_FAILED` (footer/panel rojo); `r` → re-dispara el fetch (puede inyectarse un 2º fake que ahora resuelve ok → vuelve a `projects`); `Esc` → vuelve a list; `saveProjectsFn` NUNCA llamado en todo el carril de error.
6. **PROJ-05 (race) / UX-03:** `m` (entra projects-loading) y antes del drain `\x1b` (Esc) → vuelve a list; al resolverse el fetch tardío el resultado se DESCARTA (no entra a projects); `selectedTaskId` intacto (la misma fila sigue marcada en la tabla).

El test es el CONTRATO: bloquea los nombres de props DI (`listProjectsFn`/`loadProjectsFn`/`saveProjectsFn`), las constantes (`PROJECTS_*`) y los modos. Tasks 2 y 3 lo llevan a GREEN.
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js 2>&1 | grep -q "fail\|Error\|not ok" && echo "RED OK (esperado: el test falla antes de implementar)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-projects.test.js` existe y, ejecutado ANTES de Tasks 2/3, FALLA (RED) por props/constantes/modos inexistentes.
    - El test reusa el harness de `test/dashboard-config.test.js` + `drain()` de `dashboard-overlay.test.js` (render + stdin.write + lastFrame + unmount en finally).
    - El test inyecta `listProjectsFn`/`loadProjectsFn`/`saveProjectsFn` por DI (no toca `~/.kodo/` ni la red); usa `mkdtempSync` para el dir real del caso válido.
    - Cubre los 6 comportamientos PROJ-01 / PROJ-02-UI (válido+inválido) / PROJ-03 / PROJ-05 (error) / PROJ-05 (race).
  </acceptance_criteria>
  <done>El contrato de interacción del editor de proyectos (incl. el carril async y la degradación) está fijado como test RED ejecutable (Nyquist Wave 0 para la UI); Tasks 2/3 lo vuelven verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modos async + handlers + validación/guardado en App.js</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - `src/cli/dashboard/App.js` (líneas 320-330 props/destructuring; 358-374 estado focusError/footerColor; 409-426 doc del snapshot congelado + `overlayReqRef`; 632-690 rama `deriving` = MOLDE de await+token+Esc-cancela; 785-886 ramas `config`/`config-edit` = MOLDE EXACTO de los nuevos projects/projects-edit; 887 rama `filter` — punto de inserción ENTRE config-edit y filter; 913-922 inicio de la rama `list` y handler `e`; 1088 handler `a`; 1113-1124 arrows/Enter de list; 1185-1214 render de SessionTable + props; 1216 footer hint; 229-231 constantes CONFIG_* exportadas — molde de copy)
    - `src/path-validate.js` (validateExistingDir — Plan 01)
    - `src/projects-shape.js` (setProjectPath/removeProjectMapping/getProjectPath — Plan 01)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Pattern 1 fetch+token; §Pattern 3 orden de gating; §Pattern 5 forma dual; Anti-patterns; Pitfalls 1/3/5; Pitfall 0 verificación de `m` libre)
  </read_first>
  <behavior>
    - `m` en mode:'list' → `setMode('projects-loading')`, `reqId = ++projectsReqRef.current`, `await listProjectsFn()`; tras el await si `projectsReqRef.current !== reqId` → return (staleness, T5); si `{ok:true}` → `setProjectsSnapshot({ remote: result.projects, map: loadProjectsFn() })`, `setFieldCursor(0)`, `setMode('projects')`; si `{ok:false}` → `setProjectsError(result.error)`, `setMode('projects-error')`.
    - mode:'projects-loading': Esc → `projectsReqRef.current++` (invalida el fetch en vuelo) + `setMode('list')`; traga el resto.
    - mode:'projects': ↑/↓ mueven fieldCursor clamp [0, remote.length-1] sin wrap; Enter → precarga `getProjectPath(map[remote[fieldCursor].id])` en buffer + cursor al final → projects-edit; tecla quitar → `saveProjectsFn(removeProjectMapping(map, id))` + actualizar snapshot.map + aviso de reinicio; Esc → mode:'list' (selectedTaskId intacto).
    - mode:'projects-edit': edición de texto (mismo molde que config-edit); Enter → `validateExistingDir(buffer)`; inválido → `setProjectsEditError(error)` + sigue en projects-edit (NO escribe); válido → `saveProjectsFn(setProjectPath(map, id, value))` + actualizar snapshot.map + aviso de reinicio + mode:'projects'; Esc → cancela sin guardar → projects.
    - mode:'projects-error': `r` → re-dispara el fetch (mismo flujo que `m`); Esc → mode:'list'; traga el resto. saveProjectsFn NUNCA se llama en este carril.
  </behavior>
  <action>
Modificar `src/cli/dashboard/App.js` (cambio quirúrgico, preservar TODO lo existente):

1. **Props DI (D-08/D-09):** añadir `listProjectsFn`, `loadProjectsFn`, `saveProjectsFn` al destructuring de `App({...})` (~líneas 320-330), con defaults inertes para tests del módulo (`listProjectsFn = async () => ({ ok: true, projects: [] })`, `loadProjectsFn = () => ({})`, `saveProjectsFn = () => {}`). Documentar en JSDoc espejo de `onAdopt`/`onSaveConfig`. NOTA: `listProjectsFn` devuelve un DISCRIMINADO `{ok}` (NO fail-open a `[]` como onDerive) — necesario para distinguir 0-proyectos de error de red (PROJ-05, A4).
2. **Ref dedicado:** `const projectsReqRef = useRef(0)` junto a `overlayReqRef` (~426). NO reusar `overlayReqRef` (lo comparten c/l/adopt/deriving — Anti-pattern; un Esc en projects-loading que avanzara overlayReqRef invalidaría un overlay c/l legítimo en vuelo).
3. **Estado nuevo (useState):** `projectsSnapshot` (null — { remote, map } congelado, D-01), `projectsError` (null — mensaje del fallo de fetch), `projectsEditError` (null — error de validación de ruta inline, estado DEDICADO Pitfall 2; NO usar focusError ni configEditError). Reusar `buffer`/`cursor`/`fieldCursor` existentes.
4. **Constantes de copy EXPORTADAS** (molde ~229-231, en español, estables): `PROJECTS_OVERLAY_TITLE` (título overlay), `PROJECTS_LOADING` (cargando), `PROJECTS_UNMAPPED` (`[sin mapear]`, espejo `cli.js:667`), `PROJECTS_SAVED_RESTART` (aviso de reinicio tras guardar, PERSIST-03), `PROJECTS_REMOVED(ref)` (feedback de quitar), `PROJECTS_LOAD_FAILED(reason)` (mensaje de error con pista `r reintentar · Esc salir`, PROJ-05).
5. **Handler `m`** en la rama `mode === 'list'` (junto a `e`/`c`/`l`/`p`/`d`/`o`/`a`, ~línea 922): implementar el flujo de RESEARCH Pattern 1 — `setMode('projects-loading')`, `const reqId = ++projectsReqRef.current`, `const result = await listProjectsFn()`, check staleness `if (projectsReqRef.current !== reqId) return`, ramificar a `projects` (snapshot congelado con `loadProjectsFn()`) o `projects-error` (`setProjectsError`). NO tocar `selectedTaskId` (UX-03). Pitfall 0: `m` está LIBRE en mode:'list' (re-verificado: q//e c l p d o a + arrows/Enter; Esc ignorado) — confirmar en lectura antes de añadir.
6. **Ramas de modo nuevas** insertadas ENTRE el bloque `config-edit` (~886) y `filter` (~887), espejo del orden D-02 ("antes del mode-gate de filtro"):
   - `mode === 'projects-loading'`: Esc → `projectsReqRef.current++` + `setMode('list')` (cancela e invalida, molde deriving ~682); traga el resto mientras carga.
   - `mode === 'projects'`: `const items = projectsSnapshot?.remote ?? []`; Esc → `setMode('list')` (selectedTaskId intacto, UX-03); ↑/↓ → `setFieldCursor` clamp `Math.max(0,i-1)`/`Math.min(items.length-1,i+1)` sin wrap; Enter → `const id = items[fieldCursor]?.id; if(!id) return; const cur = getProjectPath(projectsSnapshot.map[id]); setBuffer(cur); setCursor(cur.length); setProjectsEditError(null); setMode('projects-edit')`; tecla quitar (elegir `d` o `x` — ver discreción abajo) → `const next = removeProjectMapping(projectsSnapshot.map, id); saveProjectsFn(next); setProjectsSnapshot(s => ({...s, map: next})); setFocusError(PROJECTS_REMOVED(items[fieldCursor].identifier)); setFooterColor('yellow')`; tragar el resto.
   - `mode === 'projects-edit'`: idéntico molde de text-input que `config-edit` (~818-886): Esc → projects (cancela sin guardar); ←/→ clamp cursor; backspace||delete (juntos, Pitfall 3) borra char anterior; char imprimible (`input && !key.ctrl && !key.meta`) inserta en cursor; Enter → `const res = validateExistingDir(buffer)`; si `!res.ok` → `setProjectsEditError(res.error)` + return (sigue en projects-edit, NO escribe — PROJ-02); si ok → `const next = setProjectPath(projectsSnapshot.map, id, res.value); try { saveProjectsFn(next); setProjectsSnapshot(s=>({...s,map:next})); setProjectsEditError(null); setFocusError(PROJECTS_SAVED_RESTART); setFooterColor('yellow'); setMode('projects'); } catch { setProjectsEditError(PROJECTS_LOAD_FAILED... o un PROJECTS_SAVE_FAILED) }` (saveProjectsFn de Plan 04 es síncrono atómico; try/catch defensivo never-throws). El `id` se re-deriva de `items[fieldCursor].id` al entrar a edit (capturarlo en el estado o re-leerlo del snapshot).
   - `mode === 'projects-error'`: `r` → re-ejecuta el flujo de fetch (extraer el cuerpo del handler `m` a una función local reutilizable, o duplicar el patrón); Esc → `setMode('list')`; tragar el resto. saveProjectsFn JAMÁS se llama aquí (carril de LECTURA, projects.json intacto — PROJ-05).
7. **Render:** pasar a `SessionTable` (~1185-1214) las props nuevas `projectsSnapshot`, `projectsError`, `projectsEditError` (reusa `buffer`/`cursor`/`fieldCursor` ya pasadas para config). Actualizar el footer hint (~1216) añadiendo `· m projects` a la línea de ayuda.

**Discreción resuelta por el planner:** tecla de quitar = `x` (evita colisión con `d` que en este sub-modo no aplica pero reserva claridad; `x` no se usa dentro de projects). Quitar es DIRECTO (sin modal de confirmación) con footer `PROJECTS_REMOVED` — simplicidad, quitar es re-mapeable (no destructivo). En `projects-error` v1 = error+retry simple (NO degradación parcial con mapeo local cacheado — diferido). Cap del buffer de ruta: sin límite explícito en v1 (el validador rechaza lo inválido).

NO añadir endpoint, NO shell-out, NO tocar el poll ni `resolveSelection`. El clear-on-any-input (~562) se conserva — por eso `projectsEditError` y `projectsError` son estados DEDICADOS, no focusError (Pitfall 2). El aviso de reinicio transitorio SÍ va en focusError/footerColor (transitorio, ya de vuelta en projects — molde config D-10).
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `App.js` contiene `mode === 'projects'`, `'projects-loading'`, `'projects-edit'`, `'projects-error'` y el handler `input === 'm'` en la rama list.
    - `App.js` declara `projectsReqRef` con `useRef` (dedicado, NO reusa overlayReqRef) y lo usa para el check de staleness post-await.
    - `App.js` exporta `PROJECTS_OVERLAY_TITLE`, `PROJECTS_LOADING`, `PROJECTS_UNMAPPED`, `PROJECTS_SAVED_RESTART`, `PROJECTS_LOAD_FAILED`.
    - El error de validación de ruta se almacena en `projectsEditError` y el fallo de fetch en `projectsError` (NO en focusError — Pitfall 2).
    - El carril `projects-error` NO llama a `saveProjectsFn` (verificable por el spy del test — PROJ-05).
    - El footer hint (~1216) incluye `m projects`.
    - `npm test` no regresiona los tests existentes de App.js (filter/overlay/select/dismiss/adopt/config).
  </acceptance_criteria>
  <done>Los modos async projects/projects-loading/projects-edit/projects-error, el handler `m`, el guard projectsReqRef, la validación de ruta y quitar mapeo están en App.js sin romper los modos existentes; el fetch distingue éxito de fallo (PROJ-05) y el carril de error nunca escribe (PROJ-01/02/03/05, D-01/02/03/06/07/10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render del overlay de proyectos + estados loading/error en SessionTable.js</name>
  <files>src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - `src/cli/dashboard/SessionTable.js` (líneas 27-43 import de constantes desde App.js; 135-215 `renderOverlay` — molde de render de overlay; 229-279 `renderAdoptPicker` — molde de lista navegable con cursor; 280-336 `renderConfigOverlay` + render del cursor `<Text inverse>` = MOLDE EXACTO del text-input; 309-336 firma del componente; 421-432 early-returns por modo incl. config/config-edit)
    - `src/cli/dashboard/App.js` (constantes PROJECTS_* exportadas por Task 2; props nuevas projectsSnapshot/projectsError/projectsEditError)
    - `src/projects-shape.js` (getProjectPath — para mostrar el estado de mapeo de cada fila)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Pattern "Render del cursor"; Pitfall 6 ANSI del inverse — assertar por contenido, no por bytes)
  </read_first>
  <behavior>
    - mode:'projects-loading' → muestra PROJECTS_OVERLAY_TITLE + PROJECTS_LOADING (spinner/texto).
    - mode:'projects' → lista de projectsSnapshot.remote: cada fila `identifier — name` + estado de mapeo (la ruta de getProjectPath(map[id]) si está mapeado, o PROJECTS_UNMAPPED); la fila en fieldCursor resaltada (inverse/bold vía <Text>).
    - mode:'projects-edit' → la fila activa renderiza el text-input (buffer con el char bajo cursor invertido, molde renderConfigOverlay); si projectsEditError != null, línea de footer roja con el mensaje.
    - mode:'projects-error' → panel/footer rojo con PROJECTS_LOAD_FAILED(projectsError) + pista r/Esc.
    - El overlay NUNCA muestra API keys ni base_url/workspace_slug (solo lista proyectos del provider + rutas locales — PERSIST-04 por construcción).
    - Todo color vía props de <Text> (color-isolation; cursor con inverse, NO picocolors).
  </behavior>
  <action>
Modificar `src/cli/dashboard/SessionTable.js` (cambio quirúrgico):

1. **Imports:** añadir al import desde `./App.js` (~27-43) las constantes `PROJECTS_OVERLAY_TITLE`, `PROJECTS_LOADING`, `PROJECTS_UNMAPPED`, `PROJECTS_SAVED_RESTART`, `PROJECTS_LOAD_FAILED`; importar `getProjectPath` de `../../projects-shape.js`.
2. **Props nuevas** en la firma del componente (~309-336): `projectsSnapshot`, `projectsError`, `projectsEditError` (defaults seguros null). Reusa `buffer`/`cursor`/`fieldCursor` ya recibidas.
3. **`renderProjectsOverlay(snapshot, fieldCursor, mode, buffer, cursor, projectsEditError)`** (molde `renderAdoptPicker` para la lista + `renderConfigOverlay` para el cursor): cabecera `PROJECTS_OVERLAY_TITLE`; lista de `snapshot.remote` — cada fila `identifier — name` + el estado de mapeo derivado de `getProjectPath(snapshot.map[item.id])` (ruta mapeada, truncando rutas largas — discreción del planner) o `PROJECTS_UNMAPPED`; la fila en `fieldCursor` resaltada. En `mode==='projects-edit'`, la fila activa renderiza el text-input con `left=buffer.slice(0,cursor)`, `under=buffer[cursor] ?? ' '`, `right=buffer.slice(cursor+1)`, montando el cursor por `<Text inverse>` (molde renderConfigOverlay ~293-298, color-isolation). Si `projectsEditError != null`, una línea de footer en rojo con el mensaje. Añadir una línea de pista de teclas (Enter editar · x quitar · Esc cerrar) — discreción del layout.
4. **Estados loading/error:** un helper de render (o ramas dentro de renderProjectsOverlay) para `mode==='projects-loading'` (título + PROJECTS_LOADING) y `mode==='projects-error'` (panel rojo con `PROJECTS_LOAD_FAILED(projectsError)`).
5. **Early-return:** añadir, junto al `if ((mode === 'config' || mode === 'config-edit') && configSnapshot)` (~431), las ramas: `if (mode === 'projects-loading') return <render loading>`; `if (mode === 'projects-error') return <render error>`; `if ((mode === 'projects' || mode === 'projects-edit') && projectsSnapshot) return renderProjectsOverlay(...)`.

Assertar (vía el test GREEN del Task 1) por CONTENIDO de texto, no por bytes ANSI del cursor (Pitfall 6). NO usar picocolors (color-isolation — el cursor es `<Text inverse>`).
  </action>
  <verify>
    <automated>node --test test/dashboard-projects.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-projects.test.js` exits 0 (los 6 comportamientos del Task 1 en verde).
    - `SessionTable.js` contiene `renderProjectsOverlay` y early-returns para `mode === 'projects'`/`'projects-edit'`/`'projects-loading'`/`'projects-error'`.
    - El overlay renderiza el estado de mapeo por fila vía `getProjectPath` (ruta o `PROJECTS_UNMAPPED`) y NO muestra ningún secreto (PERSIST-04 por construcción — solo lista del provider + rutas).
    - El cursor del text-input se pinta con `<Text inverse>` (no picocolors); `npm test` de format-isolation sigue verde.
    - `npm test` verde completo.
  </acceptance_criteria>
  <done>El overlay de proyectos renderiza la lista con su estado de mapeo, el text-input de ruta con cursor visible y los estados loading/error, sin exponer secretos y sin romper color-isolation (PROJ-01/02/05, D-01/07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teclado del operador → useInput | toda la entrada de navegación/edición cruza por useInput (única superficie de input de la TUI) |
| listProjectsFn (red) → estado de la TUI | el resultado del fetch async entra como estado; un rechazo NO debe tumbar el árbol ink (PROJ-05) |
| snapshot de proyectos → render | el overlay muestra proyectos del provider + rutas locales; ninguna API key entra al snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-05 | DoS | useInput projects-loading / fetch async | high | mitigate | listProjectsFn es never-throws discriminado; un {ok:false} ramifica a projects-error con el panel ink montado (PROJ-05/D-07); jamás un throw no capturado cuelga el handler |
| T-64-06 | Tampering | mode:'projects-edit' (validación pre-escritura) | high | mitigate | validateExistingDir corre ANTES de saveProjectsFn; una ruta inválida nunca alcanza el disco (PROJ-02/D-03) |
| T-64-07 | Tampering | quitar mapeo / setProjectPath en el snapshot | medium | mitigate | Mutación vía helpers puros de forma dual (Plan 01) sobre un clon; la forma string\|{default,modules} se preserva (D-06) |
| T-64-08 | DoS | projectsReqRef staleness (race) | medium | mitigate | Ref dedicado por apertura; un resultado tardío tras Esc/2ª apertura se descarta (Pitfall 3); evita aplicar estado obsoleto |
| T-64-09 | Information Disclosure | renderProjectsOverlay | high | mitigate | El overlay solo itera snapshot.remote (proyectos del provider) + rutas locales; api_key_env/base_url/workspace_slug nunca entran al snapshot ni al render (PERSIST-04) |
| T-64-SC | Tampering | npm installs | low | accept | Cero dependencias nuevas (D-09 — package.json intacto); sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/dashboard-projects.test.js` verde (PROJ-01/02-UI/03/05 + race).
- `npm test` verde completo (sin regresión en filter/overlay/select/dismiss/adopt/config/format-isolation).
- Inspección: `App.js` no introduce import de picocolors; el cursor es `<Text inverse>`; `projectsReqRef` es dedicado.
</verification>

<success_criteria>
El operador abre el editor con `m`, ve la lista en vivo con estado de mapeo, edita una ruta (validada, no-escribe si inválida), quita un mapeo, y ante un fallo de fetch ve projects-error con retry/salir sin crash ni escritura — todo sobre un snapshot congelado con guard de staleness y DI fakes (PROJ-01/02/03/05, D-01/02/03/06/07/10).
</success_criteria>

<output>
Create `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-02-SUMMARY.md` when done
</output>
