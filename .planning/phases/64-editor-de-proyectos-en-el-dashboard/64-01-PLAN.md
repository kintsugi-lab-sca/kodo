---
phase: 64
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/path-validate.js
  - src/projects-shape.js
  - test/path-validate.test.js
  - test/projects-shape.test.js
autonomous: true
requirements:
  - PROJ-02
  - PROJ-03
  - PROJ-04
must_haves:
  truths:
    - "validateExistingDir rechaza ruta vacía, inexistente, ruta-a-archivo, y symlink roto / sin permisos con {ok:false,error} y NUNCA lanza (PROJ-02, D-04, Pitfall 2)"
    - "validateExistingDir acepta un directorio existente con {ok:true,value} (la ruta trim-eada) (PROJ-02, D-04)"
    - "Editar la ruta de una entrada-objeto preserva su `modules` (solo cambia `default`); editar una entrada-string la deja como string (forma dual D-06 intacta — manager.js:88 / adopt.js:126 no se rompen)"
    - "Quitar un mapeo elimina SOLO esa key del mapa sin tocar las demás entradas (PROJ-03, D-06)"
    - "Mapear un módulo materializa projects[id] = {default, modules:{[mod.name]:ruta}} preservando default y los módulos previos (PROJ-04, D-05)"
  artifacts:
    - path: "src/path-validate.js"
      provides: "validateExistingDir(raw) never-throws con contrato {ok:true,value}|{ok:false,error} — el ÚNICO validador con I/O del milestone (adyacente a config-validate.js para preservar su invariante 0-I/O)"
      exports: ["validateExistingDir"]
    - path: "src/projects-shape.js"
      provides: "Helpers PUROS de mutación/lectura del mapa projects.json preservando la forma dual string|{default,modules}"
      exports: ["setProjectPath", "removeProjectMapping", "setModulePath", "getProjectPath", "getModuleMap"]
    - path: "test/path-validate.test.js"
      provides: "Tabla dir-existe→ok / archivo→error / no-existe→error / vacío→error / symlink-roto→error never-throws (mkdtempSync, DI puro sin tocar HOME)"
    - path: "test/projects-shape.test.js"
      provides: "Preservación de forma dual al editar (objeto conserva modules) + remove (delete key) + set módulo, todo sobre funciones puras sin ink ni FS"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "src/path-validate.js"
      via: "Plan 02 importa validateExistingDir y la corre ANTES de saveProjectsFn en mode:'projects-edit'"
      pattern: "validateExistingDir"
    - from: "src/cli/dashboard/App.js"
      to: "src/projects-shape.js"
      via: "Plan 02/03 importan setProjectPath/removeProjectMapping/setModulePath/getProjectPath para mutar el snapshot antes de saveProjectsFn"
      pattern: "setProjectPath|removeProjectMapping|setModulePath"
---

<objective>
Construir la **fundación pura y determinista** del editor de proyectos: el validador de ruta-directorio `src/path-validate.js` (PROJ-02 / D-04) y el módulo de helpers puros de forma dual `src/projects-shape.js` (PROJ-03/PROJ-04 / D-05/D-06). Carril 100% local, sin ink, sin red, 0 tokens — se construye y se prueba en aislamiento para que la UI (Plan 02/03) lo consuma ya verificado.

Purpose: estas dos piezas son las de menor riesgo y máxima testabilidad de la fase. El validador formaliza el `existsSync` plano del wizard (`cli.js:689,727`) añadiendo `isDirectory` y never-throws; los helpers de forma dual extraen la lógica de mutación del wizard (`cli.js:683-737`) a funciones puras testeables (espejo de `setByPath`/`mapDismissResult`), garantizando que `manager.js:88` y `adopt.js:126` (consumidores de la forma dual) nunca se rompan.
Output: `src/path-validate.js` (nuevo), `src/projects-shape.js` (nuevo), y sus tests unitarios. NO toca App.js, SessionTable.js ni index.js (eso es Plan 02-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-CONTEXT.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md
@.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md
@.planning/phases/63-editor-de-configuraci-n-en-el-dashboard-fundaci-n-ajustes-co/63-CONTEXT.md
</context>

## Artifacts this phase produces (Plan 01)

- **Módulo nuevo:** `src/path-validate.js` (`// @ts-check`, never-throws, ÚNICO módulo con I/O — adyacente a `config-validate.js`, NO dentro, para preservar el invariante 0-I/O declarado en `config-validate.js:14-15`).
- **Función nueva:** `validateExistingDir(raw)` → `{ok:true,value}|{ok:false,error}` (error en español, copy estable).
- **Módulo nuevo:** `src/projects-shape.js` (`// @ts-check`, puro, sin FS, sin ink).
- **Funciones nuevas:** `setProjectPath(map, id, path)`, `removeProjectMapping(map, id)`, `setModulePath(map, id, moduleName, path)`, `getProjectPath(entry)`, `getModuleMap(entry)`.
- **Tests nuevos:** `test/path-validate.test.js`, `test/projects-shape.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validador de ruta-directorio never-throws (src/path-validate.js)</name>
  <files>src/path-validate.js, test/path-validate.test.js</files>
  <read_first>
    - `src/config-validate.js` (cabecera con el invariante 0-I/O declarado en ~líneas 14-15 — explica POR QUÉ el validador de ruta NO va aquí; y el contrato canónico `{ok:true,value}|{ok:false,error}` que se replica)
    - `src/cli.js` (~líneas 686, 689, 727: el wizard valida la ruta con `existsSync` plano — este validador lo formaliza añadiendo `statSync().isDirectory()` + never-throws)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Code Examples "Validador de ruta-directorio"; Pitfall 2 "statSync lanza"; Pitfall 4 "no es 0-I/O")
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-VALIDATION.md` (Nota de aislamiento: el validador recibe la ruta por argumento → testeable con `mkdtempSync(os.tmpdir())` SIN tocar HOME)
    - `test/config-validate.test.js` (molde de test unitario `node:test` + `node:assert/strict` del repo)
  </read_first>
  <behavior>
    - `validateExistingDir('/ruta/a/dir-existente')` → {ok:true, value:'/ruta/a/dir-existente'}.
    - `validateExistingDir('')` y `'   '` → {ok:false, error} (ruta vacía tras trim).
    - `validateExistingDir('/ruta/inexistente')` → {ok:false, error} (no existe; `existsSync` false silencioso).
    - `validateExistingDir('/ruta/a/un-archivo')` → {ok:false, error} (existe pero no es directorio).
    - `validateExistingDir('/ruta/symlink-roto')` → {ok:false, error} (statSync LANZA → try/catch lo convierte; never-throws, Pitfall 2).
    - Recorta espacios: `' /tmp/x '` valida `/tmp/x`.
    - NUNCA lanza ante ningún input (incluso no-string): `String(raw)` defensivo.
  </behavior>
  <action>
Crear `src/path-validate.js` con cabecera `// @ts-check` y comentarios densos en español (estilo del repo). Importar `existsSync` y `statSync` de `node:fs`. Documentar en la cabecera por qué este módulo es ADYACENTE a `config-validate.js` y no dentro: `config-validate.js` declara el invariante 0-I/O en su cabecera (`:14-15`); meter `existsSync`/`statSync` ahí rompería ese invariante y contaminaría su test hermético (D-04 concesión consciente, RESEARCH Pitfall 4 / A2).

Implementar y exportar `validateExistingDir(raw)`:
1. `const s = String(raw).trim()` (defensivo never-throws ante cualquier tipo).
2. Si `s.length === 0` → `{ ok: false, error: 'la ruta no puede estar vacía' }`.
3. Envolver el resto en `try { ... } catch { return { ok:false, error:`no se pudo acceder a "${s}"` } }` — `statSync` LANZA en symlink roto / permisos denegados / race (Pitfall 2); `existsSync` es false-silencioso pero `statSync` propaga. El catch garantiza never-throws (contrato D-04).
4. Dentro del try: si `!existsSync(s)` → `{ ok:false, error:`"${s}" no existe` }`; si `!statSync(s).isDirectory()` → `{ ok:false, error:`"${s}" no es un directorio` }`; en otro caso → `{ ok:true, value: s }`.

Contrato idéntico al de los validadores de `config-validate.js` (`{ok:true,value}|{ok:false,error}`) para que el handler de Plan 02 los trate igual. Copy de error estable en español (los tests pueden assertarla por substring).

Crear `test/path-validate.test.js` (molde `test/config-validate.test.js`, `node:test` + `node:assert/strict`) PRIMERO en RED, luego implementar hasta GREEN. Usar `mkdtempSync(join(tmpdir(),'kodo-pathval-'))` para crear un directorio REAL y un archivo real dentro (DI puro, sin tocar HOME — VALIDATION isolation note). Tabla: dir-existe→{ok:true}; el archivo creado→{ok:false}; una ruta inexistente bajo el tmpdir→{ok:false}; `''` y `'   '`→{ok:false}; un symlink colgante (`symlinkSync` a un target borrado) →{ok:false} **sin lanzar** (envolver el caso en una aserción de que `validateExistingDir` retorna en vez de throw). Limpiar el tmpdir en un `after`/`finally`.
  </action>
  <verify>
    <automated>node --test test/path-validate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/path-validate.test.js` exits 0.
    - `src/path-validate.js` exporta `validateExistingDir`.
    - El test demuestra que un symlink roto / ruta inaccesible devuelve `{ok:false}` sin lanzar (never-throws, Pitfall 2).
    - El test usa `mkdtempSync` para un dir real y NO toca `process.env.HOME` ni `~/.kodo` (aislamiento).
    - `src/path-validate.js` NO importa ink ni picocolors (módulo de dominio puro salvo el I/O de `node:fs`).
  </acceptance_criteria>
  <done>El validador de ruta-directorio acepta directorios existentes y rechaza vacío/inexistente/archivo/symlink-roto con mensaje en español, sin lanzar nunca, en un módulo adyacente que preserva el invariante 0-I/O de config-validate.js (PROJ-02, D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helpers puros de forma dual de projects.json (src/projects-shape.js)</name>
  <files>src/projects-shape.js, test/projects-shape.test.js</files>
  <read_first>
    - `src/cli.js` (~líneas 683-737: el wizard `kodo config` — estado de mapeo `[ruta]`/`[sin mapear]` (~666-668), asignación de ruta (~721), forma `{default, modules}` con key `mod.name` (~728,737). ES la lógica EXACTA a extraer a funciones puras)
    - `src/session/manager.js` (~líneas 78-103: `resolveProjectPath` consume la forma dual — `typeof entry === 'string'` ~88 y `entry.modules?.[name]` ~92. ROMPER la forma rompe la resolución de rutas de TODAS las sesiones de ese proyecto)
    - `src/cli/adopt.js` (~líneas 115-135: segundo consumidor de `loadProjects()` forma dual)
    - `src/cli/dashboard/App.js` (~líneas 233-249 zona de helpers/constantes; el patrón de helper PURO de mutación tipo `setByPath`/`mapDismissResult` como molde de estilo)
    - `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-RESEARCH.md` (§Pattern 5 "Preservación de la forma dual + quitar mapeo"; Anti-pattern "Colapsar la forma dual")
  </read_first>
  <behavior>
    - getProjectPath(entry): `'/x'` → `'/x'`; `{default:'/x',modules:{}}` → `'/x'`; `undefined`/sin mapear → `''`.
    - getModuleMap(entry): `{default,modules:{a:'/m'}}` → `{a:'/m'}`; `'/x'` (string) → `{}`; `undefined` → `{}`.
    - setProjectPath(map, id, path): entrada-objeto con modules → `{default:path, modules: prev.modules}` (modules INTACTO, solo cambia default); entrada-string o ausente → string `path` plano (legacy). NUNCA escribe `{default:undefined}` ni colapsa un objeto-con-módulos a string.
    - removeProjectMapping(map, id): devuelve un mapa SIN la key `id`; las demás entradas idénticas; map original no mutado (clon).
    - setModulePath(map, id, moduleName, path): asegura forma objeto `{default, modules}` (preservando el default actual vía getProjectPath); setea `modules[moduleName] = path` preservando los otros módulos.
    - Todas las funciones son PURAS: reciben el mapa y devuelven uno NUEVO (no mutan el argumento) — verificable comparando referencias.
  </behavior>
  <action>
Crear `src/projects-shape.js` con cabecera `// @ts-check` y comentarios densos en español. Módulo PURO: sin `node:fs`, sin ink, sin red. Documentar en la cabecera que es la extracción testeable de la lógica de forma dual del wizard (`cli.js:683-737`) y que sus consumidores de runtime son `manager.js:resolveProjectPath` y `adopt.js` — por eso la forma `string | {default, modules}` debe preservarse EXACTAMENTE (Anti-pattern RESEARCH).

Exportar funciones puras (cada una devuelve un valor nuevo, nunca muta su argumento — el consumidor de Plan 02 ya pasa el snapshot, pero la pureza se garantiza aquí con un clon superficial del mapa: `const next = { ...map }`):
1. `getProjectPath(entry)`: `typeof entry === 'string' ? entry : (entry?.default ?? '')` (lectura para precarga del text-input, espejo `cli.js:685`).
2. `getModuleMap(entry)`: `(entry && typeof entry === 'object') ? (entry.modules ?? {}) : {}`.
3. `setProjectPath(map, id, path)`: `const next = { ...map }; const prev = next[id];` — si `prev && typeof prev === 'object' && prev.modules` → `next[id] = { default: path, modules: prev.modules }` (preserva modules, D-06); en otro caso → `next[id] = path` (string plano). `return next`.
4. `removeProjectMapping(map, id)`: `const next = { ...map }; delete next[id]; return next` (PROJ-03 / D-06).
5. `setModulePath(map, id, moduleName, path)`: `const next = { ...map }; const def = getProjectPath(next[id]); const modules = { ...getModuleMap(next[id]), [moduleName]: path }; next[id] = { default: def, modules }; return next` (PROJ-04 / D-05, key = `mod.name` del provider — `cli.js:728`).

Crear `test/projects-shape.test.js` (molde `node:test`) PRIMERO en RED, luego GREEN. Sin filesystem (funciones puras). Cubrir: (a) `setProjectPath` sobre `{p1:{default:'/old',modules:{m:'/mm'}}}` con id `p1` y ruta `/new` → `{p1:{default:'/new',modules:{m:'/mm'}}}` (modules preservado); (b) `setProjectPath` sobre `{p1:'/old'}` → `{p1:'/new'}` (string sigue string); (c) `setProjectPath` sobre `{}` (sin mapear) → `{p1:'/new'}`; (d) `removeProjectMapping({p1:'/x',p2:'/y'},'p1')` → `{p2:'/y'}` (las demás intactas); (e) `setModulePath` sobre `{p1:'/d'}` con módulo `core`→`/c` → `{p1:{default:'/d',modules:{core:'/c'}}}`; (f) `setModulePath` preserva otros módulos existentes; (g) ninguna función muta el mapa de entrada (assert la referencia original sin cambios); (h) `getProjectPath`/`getModuleMap` para string, objeto y sin-mapear.
  </action>
  <verify>
    <automated>node --test test/projects-shape.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/projects-shape.test.js` exits 0.
    - `src/projects-shape.js` exporta `setProjectPath`, `removeProjectMapping`, `setModulePath`, `getProjectPath`, `getModuleMap`.
    - El test demuestra que editar la ruta de una entrada-objeto preserva su `modules` (forma dual D-06) y que remove elimina solo la key indicada.
    - El test demuestra que ninguna función muta su mapa de entrada (pureza — referencia original sin cambios).
    - `src/projects-shape.js` NO importa `node:fs`, ink ni picocolors (módulo puro).
  </acceptance_criteria>
  <done>Los helpers puros de forma dual editan/quitan/mapean-módulos preservando exactamente la forma `string | {default, modules}` que consumen manager.js y adopt.js, sin mutar su entrada (PROJ-03, PROJ-04, D-05/D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruta cruda del operador → validateExistingDir | string del text-input entra al validador antes de tocar el mapa o el disco |
| mapa projects.json → helpers de forma | el mapa (de loadProjects, never-throws) se muta vía funciones puras antes de saveProjects (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | validateExistingDir / path-validate.js | high | mitigate | Validación FS pre-escritura: una ruta inexistente/no-dir nunca alcanza saveProjects (PROJ-02/D-04) |
| T-64-02 | DoS | validateExistingDir (statSync lanza) | medium | mitigate | try/catch envuelve statSync → {ok:false} ante symlink roto/permisos; never-throws por contrato (Pitfall 2) |
| T-64-03 | Tampering | setProjectPath/setModulePath (forma dual) | high | mitigate | Preservar string\|{default,modules} exacto; nunca colapsar objeto-con-módulos ni escribir default:undefined (no rompe manager.js:88/adopt.js:126) |
| T-64-04 | DoS | validateExistingDir (ReDoS) | low | mitigate | Solo FS calls + comparaciones de string + trim; nunca regex compilada desde el input (anti-ReDoS) |
| T-64-SC | Tampering | npm installs | low | accept | Cero dependencias nuevas esta fase (D-09 — package.json intacto); sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --test test/path-validate.test.js` y `node --test test/projects-shape.test.js` verdes.
- `npm test` verde (sin regresión en la suite existente).
- Inspección: `src/projects-shape.js` no importa `node:fs`/ink/picocolors; `src/path-validate.js` solo importa `node:fs` (no ink/picocolors) — preserva color-isolation y mantiene el I/O aislado fuera de config-validate.js (0-I/O intacto).
</verification>

<success_criteria>
El validador de ruta-directorio y los helpers de forma dual cubren PROJ-02/PROJ-03/PROJ-04 a nivel de lógica pura, never-throws, con la forma `string|{default,modules}` preservada exactamente, todo verificado en aislamiento sin la TUI ni la red (D-04/D-05/D-06).
</success_criteria>

<output>
Create `.planning/phases/64-editor-de-proyectos-en-el-dashboard/64-01-SUMMARY.md` when done
</output>
