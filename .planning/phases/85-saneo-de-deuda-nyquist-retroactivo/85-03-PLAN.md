---
phase: 85-saneo-de-deuda-nyquist-retroactivo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md
  - .planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-VALIDATION.md
  - .planning/milestones/v0.18-phases/79-sidebar-doctor/79-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "Las tres fases de v0.18 (79, 80, 81) tienen su `VALIDATION.md` con `status: validated` y `nyquist_compliant: true`, y cada fila de su Per-Task Verification Map cita un fichero de evidencia CONCRETO con su resultado (`{N}-VERIFICATION.md` + sección + conteo `**X pass / 0 fail**`), nunca una afirmación sin fichero detrás."
    - "El backfill es citation-based: CERO ficheros de test nuevos y CERO re-ejecución de la suite (D-12). La cobertura ES la cita a la evidencia empírica ya en disco."
    - "El veredicto se decide por fase de forma independiente contra su propia evidencia; ninguna de las tres hereda el veredicto de otra, y dos fases con evidencia distinta pueden divergir (D-17)."
    - "Si el `VERIFICATION.md` de una fase no aporta evidencia citable para alguna dimensión, esa fila se declara manual-only o el frontmatter queda `nyquist_compliant: false` con la razón escrita (D-17) — nunca `true` sin cita. RESEARCH §6c verificó que las tres admiten `true` honestamente, pero el veredicto se re-comprueba contra el fichero, no se asume."
    - "La forma de salida es el molde de `41-VALIDATION.md` (D-13): blockquote de cabecera de 3 líneas declarando el backfill, fila `Evidencia citada` en Test Infrastructure, `Sampling Rate` con la política de backfill explícita, Per-Task Verification Map con la columna «Evidencia citada (fichero + resultado)», Manual-Only Verifications, Validation Sign-Off y `## Reconstruction Audit {fecha} (Phase 85 NYQ-01)` con sus 6 métricas — **no** el audit trail genérico que prescribe el §6 del workflow."
    - "El frontmatter conserva los dos comentarios de lifecycle que 79/80/81 ya traen y la fecha `created` original de cada fase; `wave_0_complete` se deja como está salvo que el backfill pueda citarlo (81 llega en `true`, 79 y 80 en `false`)."
    - "Los `MILESTONE-AUDIT.md` archivados de v0.18 quedan INTACTOS (D-15): son snapshots de lo que era cierto al cerrar el milestone. El cierre se registra en la cabecera del propio `VALIDATION.md` y en la fila de `STATE.md` §Deferred Items (esta última la aplica el plan 85-05)."
  artifacts:
    - "`.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md` — reescrito con el molde 41, `nyquist_compliant: true`"
    - "`.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-VALIDATION.md` — ídem"
    - "`.planning/milestones/v0.18-phases/79-sidebar-doctor/79-VALIDATION.md` — ídem, con el mapa reconstruido desde cero sobre sus 4 planes"
  key_links:
    - "`{N}-VALIDATION.md` → `{N}-VERIFICATION.md`: la cita que constituye la cobertura; sin ella el `true` es greenear enmascarando"
    - "`{N}-VALIDATION.md` → los `{N}-NN-SUMMARY.md` de cada plan: la segunda fuente de la reconstrucción del mapa por-tarea"
    - "`status: validated` + `nyquist_compliant: true` → `audit-milestone §5.5`: el consumidor que distingue NOT-VALIDATED (`draft`) de PARTIAL (`validated` + `false`) de COMPLIANT"
  prohibitions:
    - "Ninguna fase recibe `nyquist_compliant: true` sin una cita concreta (fichero + sección + resultado) por dimensión. Un `true` sin evidencia es greenear enmascarando con disfraz documental; D-17 (`validated` + `nyquist_compliant: false` con la razón escrita) es la salida honesta y `audit-milestone §5.5` ya sabe leerla."
    - "El bloque NYQ no genera ni un solo fichero de test nuevo ni re-ejecuta la suite. Un fichero sin trackear bajo `test/` o un commit con prefijo de test durante este plan es la señal de que el gate §4 del workflow se respondió mal."
    - "No se reescribe la prosa de artefactos archivados que son snapshots históricos: los `MILESTONE-AUDIT.md` de v0.18, los `SUMMARY` y los `REVIEW` de 79/80/81 quedan intactos. Solo se edita el `VALIDATION.md` de cada fase."
    - "No se re-deriva evidencia que no existe. Si una dimensión no tiene resultado empírico en disco, la salida es una fila manual-only con su razón — jamás una corrida nueva ni una estimación redactada como si fuera medición."
---

<objective>
Saldar **NYQ-01**: los `VALIDATION.md` de las Phases **79**, **80** y **81** (milestone v0.18) pasan de `draft` / `nyquist_compliant: false` a `validated` / `nyquist_compliant: true`, **citation-based** — la cobertura es la cita a la evidencia empírica ya en disco, no una re-derivación.

Purpose: la columna Nyquist de v0.18 quedó en draft porque el mapa por-tarea nunca se reconcilió, no porque faltara cobertura real: los tres `VERIFICATION.md` están en `status: passed` con la evidencia mapeada truth→fichero de test. El backfill convierte una deuda de bookkeeping en un contrato legible por `audit-milestone §5.5`.

Output: tres `VALIDATION.md` reescritos in-place en su directorio archivado, con el molde de `41-VALIDATION.md`.

Decisiones que este plan implementa: **D-11** (vehículo `/gsd-validate-phase {N}`, una invocación por fase, sobre el directorio archivado sin mover nada), **D-12** (cero tests nuevos, cero re-ejecución de la suite — el guardarraíl que mantiene esta fase dentro de su promesa de barrido ligero), **D-13** (molde `41-VALIDATION.md`), **D-14** (`status: validated`, no `approved`; `wave_0_complete` como está ante duda), **D-15** (los `MILESTONE-AUDIT.md` archivados no se reescriben), **D-16** (bloque v0.18 primero), **D-17** (si la evidencia no da, `false` con la razón escrita — no se fuerza).

**El riesgo real de esta fase vive aquí, no en el bloque DEBT.** Los DEBT están acotados a ficheros y líneas concretas; el backfill toca 6 fases de 2 milestones con una skill que **sabe generar tests**. D-12 es lo que impide que «backfill documental» se convierta en «generación de tests sobre código de tres milestones».
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-CONTEXT.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md
</context>

## Supuestos declarados del planner

- **A2 — NYQ-01 quedó `unclassified` en el edge probe determinista.** No es un vacío: el requisito es **documental** (frontmatter + citas) y no tiene superficie ejecutable propia, así que ningún edge de comportamiento le aplica. Su verificación es la inspección del frontmatter resultante más la comprobación de que cada fila del mapa cite fichero **y** resultado. Se registra como nudge de revisión manual, no como gap resuelto automáticamente.

## Protocolo de invocación (LOCKED — leer antes de la Task 1)

El vehículo es `/gsd-validate-phase {N}`, una invocación por fase (D-11). `init.phase-op` **resuelve fases archivadas**: las tres apuntan a su directorio bajo `.planning/milestones/v0.18-phases/` y traen `has_verification: true`. La skill opera in-place, sin mover nada.

Mecánica del workflow y dónde está el peligro (verificado en RESEARCH §Sonda 5c, workflow leído íntegro):

| Paso | Qué hace | Instrucción vinculante para este plan |
|---|---|---|
| §0 Initialize | init + resolución de hooks | El hook `nyquist` está ACTIVO (`workflow.nyquist_validation: true`); el §0 no aborta |
| §1 Detect Input State | `ls *-VALIDATION.md` | Las tres son **State A** (el fichero existe) → ruta «audit existing» |
| §2 Discovery | lee PLAN/SUMMARY, cruza requirement↔test por nombre/imports/descripciones | **No ejecuta nada.** Es exactamente el trabajo citation-based que D-12 quiere |
| §3 Gap Analysis | clasifica COVERED / PARTIAL / MISSING | Si no ve gaps, salta a §6 con `nyquist_compliant: true` — ruta ideal. El criterio «runs green» de COVERED se satisface **por CITA** a `{N}-VERIFICATION.md` |
| §4 Present Gap Plan | gate interactivo con 3 opciones | **SIEMPRE la opción 2 — «Skip — mark manual-only».** NUNCA la opción 1. Este es EL mecanismo de supresión de D-12 |
| §5 Spawn auditor | **genera ficheros de test** | **PROHIBIDO.** No se alcanza si §3 no ve gaps o si §4 responde opción 2 |
| §6 Generate/Update | actualiza mapa + frontmatter + audit trail | Aquí se aplica el molde de D-13, **no** la plantilla genérica del workflow |
| §7 Commit | `git add {test_files}` + commit de tests, luego commit `docs(...)` | **Omitir la primera mitad** (no habrá ficheros de test). Solo el commit `docs(...)` vía `gsd-tools` |

Si `mode: yolo` o el auto-chain atraviesan el gate §4 sin preguntar, la respuesta fijada por este plan es **opción 2**. Ningún agente elige la opción 1 por defecto.

**Divergencia a resolver a favor de D-13:** el §6 del workflow prescribe un audit trail genérico titulado `## Validation Audit {date}` con tabla `Gaps found / Resolved / Escalated`. El molde de 41 usa `## Reconstruction Audit {fecha}` con métricas más ricas. **D-13 es LOCKED ⇒ prevalece el molde 41.**

## Artifacts this phase produces

Este plan **no crea ningún fichero nuevo y no añade ningún símbolo de código**. Lo que genuinamente produce:

| Artefacto | Naturaleza |
|---|---|
| `81-VALIDATION.md` reescrito | frontmatter + 6 filas del mapa completadas con su columna de evidencia citada + `## Reconstruction Audit` |
| `80-VALIDATION.md` reescrito | ídem, con la fila de `80-02` que hoy falta |
| `79-VALIDATION.md` reescrito | ídem, con el mapa **reconstruido desde cero** sobre 4 planes y SDR-01..06 |

**Cero código, cero tests, cero deps, cero ficheros nuevos.**

<tasks>

<task type="auto">
  <name>Task 1 (85-03-01): backfill de Phase 81 — fija el molde para las otras cinco</name>
  <files>.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md</files>
  <read_first>
    - `.planning/milestones/v0.10-phases/41-doctor-m-dulo-puro-de-saneo-cli/41-VALIDATION.md` (completo, 89 líneas) — **el molde de D-13**. Copiar su estructura, no su contenido
    - `.planning/milestones/v0.11-phases/44-overlay-de-plan-gsd-pulido-de-dashboard/44-VALIDATION.md:1-30` — segundo precedente, con la fórmula de cita reutilizable
    - `.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md` — el estado de partida: **6 filas reales** con Requirement/Threat/Command y `File Exists ✅`; es el caso más barato de los seis
    - `.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VERIFICATION.md` — `status: passed`, **26/26** must-haves, `behavior_unverified: 0`, suite citada 2364/2364. La evidencia primaria
    - Los 3 `81-NN-SUMMARY.md` del mismo directorio
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §6 (6a a 6f) — frontmatter, blockquote, fila `Evidencia citada`, bullets de Sampling Rate, cabecera y leyenda exactas del mapa, y las secciones restantes
  </read_first>
  <action>
Invocar `/gsd-validate-phase 81` siguiendo el §Protocolo de invocación de este plan al pie de la letra: gate §4 respondido **opción 2** si aparece, §5 nunca invocado, §7 sin `git add` de ficheros de test.

Reescribir `81-VALIDATION.md` con el molde de `41-VALIDATION.md` (D-13), en este orden:

1. **Frontmatter.** `status: validated` (no `approved` — el `approved` de los backfills de v0.10/v0.11 es anterior a la convención vigente). `nyquist_compliant: true`. **Conservar** los dos comentarios de lifecycle que el fichero ya trae encima de `status`, la clave `phase`, la clave `slug` y la fecha `created` original — `created` es la fecha de la fase, no la del backfill. `wave_0_complete` llega en `true` y su Wave 0 está marcado con confirmaciones: **se deja en `true`**.

2. **Blockquote de cabecera, 3 líneas** (molde `41-VALIDATION.md:12-14`): la primera declara el contrato reconstruido retroactivamente nombrando `backfill Nyquist Phase 85, NYQ-01`; la segunda declara la cobertura **citada** de `81-VERIFICATION.md` con su score real (26/26 must-haves) y el número real de SUMMARY de plan (3); la tercera declara que **no se re-ejecuta la suite** — cada dimensión referencia el resultado empírico ya registrado (D-12).

3. **`## Test Infrastructure`.** La tabla ya existe; el delta es **añadir** la última fila `Evidencia citada` con el fichero, la fecha, el `status passed` y el score.

4. **`## Sampling Rate`.** Tres bullets: *Evidencia primaria* (el `VERIFICATION.md` con su score desglosado); *Política Nyquist (backfill)* — la cobertura ES la cita a la evidencia preexistente, no se re-corre la suite, con ref a **D-12** (no a D-03/D-05, que es la ref del molde original); *UAT humano* con su resultado real (81 trae 1/1 pass).

5. **`## Per-Task Verification Map (dimensión → cobertura citada)`.** Cabecera de 7 columnas y leyenda exactas del molde: `Requirement | Plan | Dimensión / Secure Behavior | Test Type | Automated Command | Evidencia citada (fichero + resultado) | Status`, con la leyenda `⬜ pending · ✅ green · ❌ red · ⚠️ flaky / manual-only`. La columna clave es la sexta. Convención de esa celda, obligatoria en cada fila: el fichero de evidencia entre backticks + la sección citada + el **conteo en negrita** + el `Truth #N ✓ VERIFIED` correspondiente. **Nunca una afirmación sin fichero:sección detrás.**

6. **`## Wave 0 Requirements`** (prosa breve: infraestructura nativa suficiente), **`## Manual-Only Verifications`** con columnas `Behavior | Requirement | Why Manual | Evidencia` — aquí cae el item humano de 81 (la disposición de WR-01/WR-02, que es justo lo que la Phase 85 cierra; anotarlo así) — y **`## Validation Sign-Off`** con los 6 checkboxes marcados y la línea de aprobación con la fecha y `(backfill Phase 85, NYQ-01)`, usando **`validated`**.

7. **`## Reconstruction Audit {fecha} (Phase 85 NYQ-01)`** — el título del molde 41, **no** el audit trail genérico que prescribe el §6 del workflow. Tabla con las 6 métricas: `Requirements audited`, `COVERED (automated unit)`, `PARTIAL`, `MISSING`, `Manual-only (by design)`, `Tests citados (no re-corridos)`. Cerrar con el párrafo **Nota Nyquist**.

**Comprobar el veredicto contra el fichero, no asumirlo.** RESEARCH §6c dictamina que 81 es el caso más limpio (26/26, `behavior_unverified: 0`) y que `true` es defendible; aun así, si al reconstruir el mapa alguna dimensión queda sin cita, la salida es una fila manual-only con su razón o, en el límite, `validated` + `nyquist_compliant: false` con la razón escrita (D-17) — nunca un `true` sin cita.

**No tocar** el `MILESTONE-AUDIT.md` de v0.18 ni ningún `SUMMARY`/`REVIEW` archivado (D-15).
  </action>
  <verify>
    <automated>P=.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md; grep -c 'nyquist_compliant: true' "$P" | grep -qx 1 && grep -c 'status: validated' "$P" | grep -qx 1 && grep -c 'Reconstruction Audit' "$P" | grep -qvx 0 && grep -c 'Validation Audit' "$P" | grep -qx 0 && grep -c 'Evidencia citada' "$P" | grep -qvx 0 && test "$(git status --porcelain test/ | grep -c '^??')" -eq 0 && echo "NYQ-01/81 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' .planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md` devuelve `1`
    - `grep -c 'status: validated' <path>` devuelve `1` y `grep -c 'status: approved' <path>` devuelve `0`
    - `grep -c '^# status lifecycle:' <path>` devuelve `1` y `grep -c '^# audit-milestone' <path>` devuelve `1` — los dos comentarios de lifecycle se conservan
    - `git diff <path> | grep -c '^-created:'` devuelve `0` — la fecha original de la fase no se ha tocado
    - `grep -c 'wave_0_complete: true' <path>` devuelve `1` (llegaba en `true` y se queda)
    - `grep -c 'Reconstruction Audit' <path>` devuelve `>= 1` y `grep -c 'Validation Audit' <path>` devuelve `0` — prevalece el molde 41 sobre la plantilla genérica del workflow
    - `grep -c 'Evidencia citada' <path>` devuelve `>= 2` (la fila de Test Infrastructure + la cabecera del mapa)
    - `grep -c '81-VERIFICATION.md' <path>` devuelve `>= 3` — la evidencia se cita repetidamente, no una vez de adorno
    - **Toda fila del Per-Task Verification Map con `Status` verde cita un fichero**: no existe ninguna fila del mapa cuya celda de evidencia esté vacía o contenga solo prosa sin `.md` ni conteo
    - **D-12 respetado:** `git status --porcelain test/ | grep -c '^??'` devuelve `0` (cero ficheros de test nuevos) y `git log --oneline -10 | grep -c 'test(phase-'` devuelve `0`
    - `git diff --exit-code .planning/milestones/v0.18-MILESTONE-AUDIT.md` sale 0 (D-15)
  </acceptance_criteria>
  <done>`81-VALIDATION.md` es un contrato de validación completo, con `validated` + `nyquist_compliant: true` y cada dimensión respaldada por una cita a `81-VERIFICATION.md` con fichero, sección y conteo. El molde queda fijado para las cinco fases restantes.</done>
</task>

<task type="auto">
  <name>Task 2 (85-03-02): backfill de Phase 80 — completar el mapa con la fila de `80-02`</name>
  <files>.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-VALIDATION.md</files>
  <read_first>
    - `.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md` — **el resultado de la Task 1**: el molde ya instanciado en este repo y en esta fase. Es la referencia más cercana
    - `.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-VALIDATION.md` — estado de partida: 1 fila real (`80-01-01`, ORCH-07) + una nota «a rellenar»; Manual-Only ya tiene 1 fila (ORCH-08)
    - `.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-VERIFICATION.md` — `status: passed`, **11/12** must-haves, `behavior_unverified: 1` (SC1: convergencia ≤1 pase contra cmux vivo). Cita líneas exactas de test: `test/check.test.js:338-355 / 357-369 / 371-395 / 397-426 / 428-439`, `test/check-isolation.test.js:156`, `test/cmux/sidebar-doctor.test.js:218`
    - Los 2 `80-NN-SUMMARY.md` del mismo directorio
    - `.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-UAT.md` — 1/1 pass
  </read_first>
  <action>
Invocar `/gsd-validate-phase 80` con el mismo protocolo (gate §4 → opción 2; §5 nunca; §7 sin `git add` de tests) y reescribir `80-VALIDATION.md` con el molde ya instanciado en la Task 1.

Cobertura a reconstruir: **ORCH-07** y **ORCH-08** sobre 2 planes. El delta principal respecto al estado de partida es la fila que hoy falta, la de `80-02`; la de `80-01-01` ya existe y se completa con su columna de evidencia citada.

El `VERIFICATION.md` de 80 es especialmente rico para citar: mapea truths a **rangos de línea exactos** de `test/check.test.js`. Reproducir esas citas en la columna de evidencia — fichero, rango, conteo — en vez de resumirlas.

El único `behavior_unverified` (SC1, convergencia ≤1 pase contra un cmux vivo) va a **`## Manual-Only Verifications`**, no se cuenta como gap de cobertura: el cableado sí está cubierto por los casos A/B/C/D/D2/E. Precedente que lo autoriza: `41-VALIDATION.md` es `nyquist_compliant: true` **con** una fila Manual-Only.

Frontmatter: `status: validated`, `nyquist_compliant: true`, los dos comentarios de lifecycle conservados, `created` intacta. `wave_0_complete` llega en `false` y **se deja en `false`** (D-14: ante duda se deja como está; el campo que importa para NYQ-01 es `nyquist_compliant`).

Audit trail: `## Reconstruction Audit {fecha} (Phase 85 NYQ-01)` con las 6 métricas del molde 41.

Nota para el SUMMARY: la fila Manual-Only de ORCH-07/SC1 es **la misma** que `STATE.md` §Deferred Items registra como «Evidencia en vivo» — sigue abierta y esta fase **no la cierra**; solo la deja correctamente contabilizada como manual-only en el contrato de validación.

**Comprobar el veredicto contra el fichero** (D-17): si alguna dimensión queda sin cita, fila manual-only con su razón o `nyquist_compliant: false` con la razón escrita. **No tocar** `MILESTONE-AUDIT.md` ni ningún artefacto archivado que no sea este `VALIDATION.md` (D-15).
  </action>
  <verify>
    <automated>P=.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-VALIDATION.md; grep -c 'nyquist_compliant: true' "$P" | grep -qx 1 && grep -c 'status: validated' "$P" | grep -qx 1 && grep -c 'Reconstruction Audit' "$P" | grep -qvx 0 && grep -c 'Validation Audit' "$P" | grep -qx 0 && grep -c '80-VERIFICATION.md' "$P" | grep -qvx 0 && test "$(git status --porcelain test/ | grep -c '^??')" -eq 0 && echo "NYQ-01/80 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' <path>` devuelve `1`; `grep -c 'status: validated' <path>` devuelve `1`
    - `grep -c '^# status lifecycle:' <path>` y `grep -c '^# audit-milestone' <path>` devuelven ambos `1`
    - `git diff <path> | grep -c '^-created:'` devuelve `0`
    - `grep -c 'wave_0_complete: false' <path>` devuelve `1` (se deja como estaba, D-14)
    - `grep -c 'Reconstruction Audit' <path>` devuelve `>= 1`; `grep -c 'Validation Audit' <path>` devuelve `0`
    - El Per-Task Verification Map tiene fila para **ORCH-07** y fila para **ORCH-08**, y ninguna contiene el texto de placeholder «a rellenar»: `grep -ci 'a rellenar\|TBD\|se rellena' <path>` devuelve `0`
    - `grep -c 'check.test.js' <path>` devuelve `>= 1` — las citas a rangos de línea del `VERIFICATION.md` están reproducidas
    - `## Manual-Only Verifications` contiene una fila para el `behavior_unverified` de SC1 con su columna `Evidencia`
    - **D-12 respetado:** `git status --porcelain test/ | grep -c '^??'` devuelve `0`
    - `git diff --exit-code .planning/milestones/v0.18-MILESTONE-AUDIT.md` sale 0 (D-15)
  </acceptance_criteria>
  <done>`80-VALIDATION.md` cubre ORCH-07 y ORCH-08 sobre sus 2 planes, con la fila de `80-02` que faltaba, las citas a rangos de línea de `80-VERIFICATION.md`, y el único item no verificable automáticamente contabilizado como manual-only en vez de como gap.</done>
</task>

<task type="auto">
  <name>Task 3 (85-03-03): backfill de Phase 79 — mapa reconstruido desde cero sobre 4 planes</name>
  <files>.planning/milestones/v0.18-phases/79-sidebar-doctor/79-VALIDATION.md</files>
  <read_first>
    - `.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md` y `.../80-VALIDATION.md` — los resultados de las Tasks 1 y 2: el molde ya instanciado dos veces
    - `.planning/milestones/v0.18-phases/79-sidebar-doctor/79-VALIDATION.md` — estado de partida: **1 fila placeholder** para SDR-01..06 y una Wave 0 con 3 ficheros sin marcar. El mapa se reconstruye entero
    - `.planning/milestones/v0.18-phases/79-sidebar-doctor/79-VERIFICATION.md` — `status: passed`, **5/6** must-haves tras la re-verificación del gap G-79-1, `behavior_unverified: 1` (SDR-05, round-trip real vía `kodo sidebar doctor --fix`). Cada truth cita fichero de test: `test/cli/sidebar-doctor-cli.test.js`, `test/sidebar-doctor-hygiene.test.js` (17/17), `test/cmux/sidebar-doctor.test.js` (22/22), `test/manager.test.js`, `test/session/group-resolve.test.js`
    - Los 4 `79-NN-SUMMARY.md` del mismo directorio
    - `.planning/milestones/v0.18-phases/79-sidebar-doctor/79-UAT.md` — 4/4 pass
  </read_first>
  <action>
Invocar `/gsd-validate-phase 79` con el mismo protocolo (gate §4 → opción 2; §5 nunca; §7 sin `git add` de tests) y reconstruir `79-VALIDATION.md` con el molde ya instanciado dos veces.

Es el caso más caro de este plan: el Per-Task Verification Map se escribe **desde cero** sobre 4 planes y los requisitos **SDR-01..06**, sustituyendo la única fila placeholder actual. Una fila por dimensión, cada una con su celda de evidencia citada apuntando a `79-VERIFICATION.md` (sección + conteo) y al fichero de test concreto que el propio VERIFICATION nombra.

El `behavior_unverified` de **SDR-05** (round-trip real de `--fix` sobre una sesión suelta) va a **`## Manual-Only Verifications`** con su razón y su evidencia parcial: el verbo crudo ya se validó en `79-UAT.md` A2 (pass) y el argv exacto está probado con spy. Anotar además que ya figura como fila abierta en `STATE.md` §Deferred Items («Evidencia en vivo») y que **esta fase no la cierra**.

`## Wave 0 Requirements`: la lista actual tiene 3 ficheros con checkbox sin marcar. Los ficheros existen hoy, pero **el campo `wave_0_complete` no es citable sin re-derivar**, así que se deja en `false` (D-14) y la sección se redacta como prosa que declara ese estado, sin marcar checkboxes que la evidencia no respalda.

Frontmatter: `status: validated`, `nyquist_compliant: true`, los dos comentarios de lifecycle conservados, `created` intacta, `wave_0_complete: false`.

Audit trail: `## Reconstruction Audit {fecha} (Phase 85 NYQ-01)` con las 6 métricas del molde 41, cerrando con el párrafo **Nota Nyquist**.

**Comprobar el veredicto contra el fichero** (D-17). RESEARCH §6c lo declara defendible como `true` (5/6, con el 1 restante manual-only por naturaleza), pero el juicio se hace leyendo `79-VERIFICATION.md`, no heredando el veredicto de las tareas anteriores: cada fase se decide de forma independiente contra su propia evidencia.

**No tocar** `MILESTONE-AUDIT.md` ni ningún artefacto archivado que no sea este `VALIDATION.md` (D-15).
  </action>
  <verify>
    <automated>P=.planning/milestones/v0.18-phases/79-sidebar-doctor/79-VALIDATION.md; grep -c 'nyquist_compliant: true' "$P" | grep -qx 1 && grep -c 'status: validated' "$P" | grep -qx 1 && grep -c 'Reconstruction Audit' "$P" | grep -qvx 0 && grep -c 'Validation Audit' "$P" | grep -qx 0 && grep -c 'SDR-0' "$P" | grep -qvx 0 && test "$(git status --porcelain test/ | grep -c '^??')" -eq 0 && echo "NYQ-01/79 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' <path>` devuelve `1`; `grep -c 'status: validated' <path>` devuelve `1`
    - `grep -c '^# status lifecycle:' <path>` y `grep -c '^# audit-milestone' <path>` devuelven ambos `1`
    - `git diff <path> | grep -c '^-created:'` devuelve `0`
    - `grep -c 'wave_0_complete: false' <path>` devuelve `1` (D-14: no es citable, se deja como está)
    - `grep -c 'Reconstruction Audit' <path>` devuelve `>= 1`; `grep -c 'Validation Audit' <path>` devuelve `0`
    - El Per-Task Verification Map tiene una fila por cada uno de **SDR-01..SDR-06** y ninguna contiene texto de placeholder: `grep -ci 'se rellena\|a rellenar\|TBD' <path>` devuelve `0`
    - `grep -c '79-VERIFICATION.md' <path>` devuelve `>= 3`
    - `grep -c 'sidebar-doctor' <path>` devuelve `>= 2` — los ficheros de test que el VERIFICATION nombra están citados
    - `## Manual-Only Verifications` contiene la fila de **SDR-05** con su columna `Evidencia` (`79-UAT.md` A2 pass + argv probado con spy) y la nota de que la fila de `STATE.md` «Evidencia en vivo» **no** se cierra aquí
    - **D-12 respetado:** `git status --porcelain test/ | grep -c '^??'` devuelve `0` y `git log --oneline -10 | grep -c 'test(phase-'` devuelve `0`
    - `git diff --exit-code .planning/milestones/v0.18-MILESTONE-AUDIT.md` sale 0 (D-15)
    - Las tres fases del plan quedan con veredicto propio: `grep -l 'nyquist_compliant: true' .planning/milestones/v0.18-phases/{79-sidebar-doctor/79,80-carril-orquestador-reconciliaci-n-documental/80,81-saneo-de-deuda-v0-17/81}-VALIDATION.md | wc -l` devuelve `3`
  </acceptance_criteria>
  <done>`79-VALIDATION.md` tiene un Per-Task Verification Map completo sobre SDR-01..06 y sus 4 planes, cada fila con evidencia citada, y SDR-05 correctamente contabilizado como manual-only. Con esto NYQ-01 queda saldado: las tres fases de v0.18 son `validated` + `nyquist_compliant: true`, cada una decidida contra su propia evidencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(sin frontera de red ni de proceso)* | El plan edita tres ficheros Markdown en directorios archivados del propio repo. Cero código, cero endpoints, cero parsing de entrada, cero instalaciones. |
| Agente de backfill → árbol de artefactos archivados | El único vector real es de **integridad documental**: un agente diligente puede «terminar el trabajo» reescribiendo snapshots históricos o afirmando cobertura que no existe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-03-01 | Repudiation | `nyquist_compliant: true` sin evidencia | high | mitigate | Prohibición explícita en `must_haves.prohibitions` + criterio de aceptación por fase: cada fila con `Status` verde cita fichero, sección y conteo; `grep -c '{N}-VERIFICATION.md'` ≥ 3 por fichero; `grep -ci 'a rellenar\|TBD\|se rellena'` = 0. D-17 (`validated` + `false` con razón escrita) es la salida honesta y `audit-milestone §5.5` ya la lee. |
| T-85-03-02 | Tampering | Artefactos archivados de v0.18 (`MILESTONE-AUDIT.md`, `SUMMARY`, `REVIEW`) | medium | mitigate | D-15: son snapshots históricos. Criterio de aceptación `git diff --exit-code` sobre `v0.18-MILESTONE-AUDIT.md` en las tres tareas; `files_modified` acota el plan a los tres `VALIDATION.md`. |
| T-85-03-03 | Tampering | Suite de tests del repo | high | mitigate | D-12 con supresión explícita en el gate §4 del workflow (siempre opción 2) + criterio de aceptación `git status --porcelain test/ \| grep -c '^??'` = 0 y `git log \| grep -c 'test(phase-'` = 0 en las tres tareas. §5 (spawn del auditor generador de tests) nunca se alcanza. |
| T-85-03-04 | Information Disclosure | Citas de evidencia en los `VALIDATION.md` | low | accept | Las citas son paths de test del propio repo y conteos; los `VALIDATION.md` viven en `.planning/` del mismo repo. Sin secretos, sin tokens, sin rutas de máquina del operador. |
| T-85-03-SC | Tampering | Supply chain (npm) | low | accept | **Cero instalaciones.** Constraint LOCKED «cero deps npm nuevas»; `85-RESEARCH.md` §Package Legitimacy Audit devuelve tabla vacía. No procede checkpoint de legitimidad. |
</threat_model>

<verification>
- Los tres `VALIDATION.md` con `status: validated` + `nyquist_compliant: true`
- Los tres con `## Reconstruction Audit {fecha} (Phase 85 NYQ-01)` y sin el audit trail genérico del workflow
- Los tres con la columna «Evidencia citada (fichero + resultado)» poblada en todas las filas verdes
- `git status --porcelain test/ | grep -c '^??'` en 0 — cero ficheros de test nuevos (D-12)
- `git diff --exit-code .planning/milestones/v0.18-MILESTONE-AUDIT.md` en 0 (D-15)
- Cero ejecuciones de `npm test` atribuibles a este plan; el SUMMARY lo declara explícitamente
</verification>

<success_criteria>
1. Phases 79, 80 y 81 tienen `VALIDATION.md` con `nyquist_compliant: true` citation-based, con la evidencia de la suite existente y **sin re-derivar** (NYQ-01).
2. Cada dimensión de cada mapa cita fichero + sección + resultado; ninguna fila verde carece de cita.
3. Cero tests generados, cero re-ejecución de la suite (D-12).
4. Los `MILESTONE-AUDIT.md` y el resto de artefactos archivados de v0.18 quedan intactos (D-15).
5. El veredicto de cada fase se decidió contra su propia evidencia, sin herencia entre tareas (D-17).
</success_criteria>

<output>
Create `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-03-SUMMARY.md` when done.
</output>