---
phase: 85-saneo-de-deuda-nyquist-retroactivo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/check.js
  - test/check.test.js
  - test/check-isolation.test.js
autonomous: true
requirements: [DEBT-07]

must_haves:
  truths:
    - "Cuando hay a la vez acciones aplicadas y fallidas se emiten AMBAS líneas — `aplicadas` por `logFn` (stdout) y `fallida(s)` por `errorFn` (stderr) — sin fundirse ni sustituirse. El orden ENTRE canales no es contractual: el test asevera pertenencia dentro de `logs[]` y de `errs[]` por separado, nunca una secuencia mezclada."
    - "Con `(r.errors || []).length === 0` NO se emite ninguna línea `fallida` por `errorFn`; un `r` sin campo `errors` (undefined) se trata como cero fallos por el guard `|| []`, nunca un throw. La rama de silencio tiene su propio assert (los casos A/D existentes pasan `errorFn: () => {}` y descartan el output, así que hoy nadie la asevera)."
    - "La línea de fallos se emite inmediatamente después de la línea `aplicadas` y ANTES de la rama `hasAdvisories` (agrupa las dos líneas derivadas de `r`/execute y separa la que deriva de `report`/scan). El orden dentro de cada canal es determinista."
    - "`failed === 0` ⇒ cero líneas por `errorFn`; `failed >= 1` ⇒ exactamente UNA línea con el conteo. Sin ramificación singular/plural: `acción(es) fallida(s)` con el paréntesis, igual que la hermana `acción(es) aplicadas` — coherencia con la convención ya establecida en el fichero."
    - "La línea sale por `errorFn` (stderr) y NO por `logFn`. El test asevera las dos mitades: pertenencia en `errs[]` Y ausencia de la palabra `fallida` en `logs[]` — sin la segunda mitad, una regresión de canal pasaría en verde."
    - "El fail-open no cambia: la línea informa, jamás bloquea el check ni el launch, jamás re-entra a `reasons` ni al gate `needsOrchestrator` (D-04 de Phase 80), y no altera el exit code. LOG-12 intacto: `deps = {}` sigue produciendo el `noopLogger` y no se inyecta el logger real."
    - "El conteo `applied` sigue siendo `added + ungrouped` (NUNCA `created`), y el test lo congela con tres números distintos por diseño (`applied = 3`, `failed = 2`, `advisories = 1`) para que ningún falso verde pase por coincidencia numérica."
    - "El comentario de `test/check-isolation.test.js:14` y `:33` deja de afirmar que el repo no usa `import()` dinámico — afirmación falsa desde que `src/providers/registry.js:27,28,57,58` y `src/session/state.js:247` la introdujeron — y declara el alcance real de los guards: el grafo de MODULE-LOAD de `kodo check`, no la alcanzabilidad en runtime."
    - "Un `import()` DINÁMICO de un `logger*.js` que no sea `logger-events.js` ni `logger-noop.js`, en cualquier fichero del grafo que `walkImports` ya devuelve, pone el guard ROJO. El guard sale VERDE a HEAD (único hit: `src/session/state.js → import('../logger-events.js')`, en allowlist) — verificado en RESEARCH §1d antes de escribir el assert."
    - "El guard filtra comentarios con `stripComments` ANTES de aserverar. Sin ese filtro produce 13 falsos positivos, 5 apuntando literalmente a `../logger.js`, todos imports de TIPO en JSDoc borrados en runtime."
  artifacts:
    - "`src/check.js` — rama `if (failed > 0) errorFn(...)` dentro del `try` existente del piggyback"
    - "`test/check.test.js` — dos `it()` nuevos (Test F: advisories + fallos por-item; Test G: rama de silencio) dentro del `describe` ORCH-07 existente"
    - "`test/check-isolation.test.js` — `stripComments` copiado verbatim con su línea de procedencia, las constantes `DYNAMIC_LOGGER_IMPORT_RE` / `LOGGER_ALLOWLIST_RE`, un `it()` de guard nuevo, y los comentarios de `:14` y `:33` corregidos"
  key_links:
    - "`executeFn` → `r.errors` → `errorFn` (stderr): el canal de observabilidad del piggyback, que hoy nunca inspecciona `r.errors`"
    - "`walkImports(join(SRC,'check.js'))` → lista de 23 ficheros → `stripComments` → `DYNAMIC_LOGGER_IMPORT_RE`: el guard nuevo consume la salida del walker, **sin modificar el walker**"
    - "`deps = {}` → `noopLogger` (LOG-12): la razón por la que la observabilidad tiene que salir por stdout/stderr y no por `src/logger.js`"
  prohibitions:
    - "`walkImports`, `extractImports`, `IMPORT_FROM_RE` e `IMPORT_BARE_RE` NO se modifican. Extender el walker para seguir `import()` dinámico pone ROJOS dos guards existentes (`github/provider.js`, `github/normalize.js` entran en la clausura dinámica vía `check.js:103 → initRegistry()`), y la reacción a un rojo espurio es debilitar el assert — exactamente lo que el constraint anti-greenear prohíbe."
    - "Si el guard nuevo sale ROJO tras aplicar `stripComments`, es un hallazgo REAL de LOG-12 y se ESCALA. No se relaja la regex, no se amplía la allowlist, no se estrecha la lista de ficheros. Un guard verde comprado debilitando su assert es peor que no tener guard."
    - "La línea nueva de stderr emite un conteo entero y nada más. No se filtran `err.message`, `.target` ni `.reason` de los items fallidos: son refs de workspace y rutas del operador, y el canal es un carril automático sin control sobre dónde termina."
    - "El resultado del doctor del sidebar no re-entra al gate. La línea nueva no toca `reasons`, no altera `needsOrchestrator` y no cambia el exit code — el sidebar NO es trigger (D-04 de Phase 80, vigente e intacto)."
---

<objective>
Resolver los **tres** warnings de `80-REVIEW.md` — ninguno se re-acepta (D-07/D-08/D-09):

- **WR-01** — el piggyback de `src/check.js:156-166` lee `r.added`/`r.ungrouped` pero nunca inspecciona `r.errors`; con `deps = {}` el logger es el `noopLogger` obligado por LOG-12, así que 3 `addToWorkspaceGroup` fallidos son indistinguibles de «no había nada que arreglar». Se resuelve con una línea por `errorFn`.
- **WR-02** — ni la línea de aplicadas, ni el cálculo `applied`, ni la rama de advisories tienen un solo assert en toda la suite (confirmado por grep: las tres cadenas literales aparecen **exclusivamente** en `src/check.js`). Se resuelve con el test que cruza `needsOrchestrator: true` + `hasAdvisories: true`, la combinación que ninguno de los 6 casos existentes alcanza.
- **WR-03** — el guard LOG-12 declara en dos comentarios que el repo no usa `import()` dinámico. Es falso. Y el guard es decorativo: un `await import('../logger.js')` en cualquier fichero del grafo rompería LOG-12 **en verde**. Se resuelve en sus dos mitades — corregir el comentario y añadir un source-grep sobre la lista que `walkImports` ya devuelve.

Purpose: WR-01 es el warning que más duele en operación — `Sidebar: 0 acción(es) aplicadas` significa hoy dos cosas opuestas y el operador no puede distinguirlas. WR-03 convierte un guard de aislamiento decorativo en uno con mordida real.

Output: `src/check.js` con la rama de fallos, `test/check.test.js` con la cobertura que WR-02 reclama, y `test/check-isolation.test.js` con el comentario honesto y el guard reforzado.

Decisiones que este plan implementa: **D-07** (WR-01 por `errorFn`, nunca `logFn`), **D-08** (el test nuevo cubre WR-01 y WR-02 a la vez y el fix no se mergea sin él), **D-09** (las dos mitades de WR-03, con `stripComments` obligatorio y `walkImports` intacto). Fences: **D-10** — `IN-01` **no entra** (está clasificado *info* en el propio review, el criterio literal dice «los 3 warnings»); se re-registra como diferido en el plan 85-05. **D-18** — `test/format-isolation.test.js` **no se modifica**, aunque el refuerzo de WR-03 produzca un patrón reutilizable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-CONTEXT.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-UI-SPEC.md
</context>

## Artifacts this phase produces

Este plan **no crea ningún fichero nuevo**. Símbolos y bloques que genuinamente añade:

| Artefacto | Fichero | Naturaleza |
|---|---|---|
| Rama `if (failed > 0) errorFn(...)` + `const failed` | `src/check.js` | 4 líneas de código + comentario de porqué; **cero funciones nuevas** |
| `it('Test F: …')` | `test/check.test.js` | cubre WR-01 y WR-02 a la vez (4 asserts) |
| `it('Test G: …')` | `test/check.test.js` | rama de silencio: `errors: []` y `errors` ausente ⇒ cero líneas por `errorFn` |
| `function stripComments(src)` | `test/check-isolation.test.js` | **10.ª copia verbatim** en el repo, con su línea de procedencia (decisión ya tomada 9 veces) |
| `const DYNAMIC_LOGGER_IMPORT_RE` | `test/check-isolation.test.js` | regex CONSTANTE (anti-ReDoS), nunca compilada desde input |
| `const LOGGER_ALLOWLIST_RE` | `test/check-isolation.test.js` | regex CONSTANTE de allowlist (`logger-events.js` / `logger-noop.js`) |
| `it('… import() DINÁMICO … (WR-03)')` | `test/check-isolation.test.js` | el guard source-hygiene nuevo |
| Comentarios corregidos de `:14` y `:33-34` | `test/check-isolation.test.js` | prosa; retiran una premisa falsa y declaran el alcance real de los guards |

**Cero ficheros nuevos, cero exports nuevos, cero deps npm.**

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (85-02-01): WR-01 + WR-02 RED — Test F (advisories + fallos) y Test G (rama de silencio)</name>
  <files>test/check.test.js</files>
  <read_first>
    - `test/check.test.js:320-440` — el `describe('check.js — runCheckAndAct sidebar doctor piggyback (ORCH-07)')` completo: los helpers `cleanReport()` / `emptyResult()` (`:322-336`) y los 6 casos A/B/C/D/D2/E. **Se leen para imitar la shape DI y el naming; NO se modifican**
    - `src/check.js:140-175` — el piggyback entero: `deps = {}`, `applied = added + ungrouped`, las 3 líneas literales, la rama `hasAdvisories`, el `catch` fail-open
    - `src/cmux/sidebar-doctor.js:320-341` — el typedef `SidebarResult`: `errors` es `Array<{ category, target, reason }>`
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-UI-SPEC.md` §S-2 — la familia completa de 4 líneas del piggyback y la advertencia de contrato sobre el orden cross-canal
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §2 — shape DI canónica, patrón de captura por array, naming del `it`
  </read_first>
  <behavior>
    - **Test F** — `needsOrchestrator: true` + `scanFn` que devuelve `hasAdvisories: true` con `missing_group` de **1** elemento + `executeFn` que devuelve `{ created: 0, added: 2, ungrouped: 1, errors: [2 items] }`:
      - `logs` incluye `[kodo:check] Sidebar: 3 acción(es) aplicadas` (applied = added + ungrouped, NO created)
      - `logs` incluye `[kodo:check] Sidebar advisories: 1 (acción de operador)`
      - `errs` contiene una línea con `2 acción(es) fallida(s)` ← **RED hoy**
      - `logs` NO contiene ninguna línea con `fallida` (mitad de canal; sin ella una regresión de canal pasa en verde)
    - **Test G** — mismo gate ON, `cleanReport()`, `executeFn` que devuelve `{ created: 0, added: 0, ungrouped: 0, errors: [] }`:
      - `errs` queda **vacío** (cero líneas de fallos)
      - Con un `executeFn` que devuelve un objeto SIN campo `errors`, la llamada no lanza y `errs` sigue vacío
  </behavior>
  <action>
Añadir **dos** `it()` nuevos al final del `describe` ORCH-07 de `test/check.test.js`, después del Test E (`~:439`), dentro del mismo bloque. Los 6 casos existentes y los helpers `cleanReport()` / `emptyResult()` **no se tocan ni se reformatean**.

**Test F** — el que D-08 prescribe; cubre WR-01 y WR-02 a la vez. Naming coherente con el bloque (`'Test X: <resumen> (<ref de decisión>)'`): `'Test F: gate ON + advisories + fallos por-item — las 3 líneas salen por su canal (WR-01/WR-02)'`.

Shape: las **6 inyecciones siempre**, en el orden canónico del bloque (`runCheckFn`, `scanFn`, `executeFn`, `launchFn`, `logFn`, `errorFn`). Dos arrays `const` declarados antes del `await` — `logs` y `errs` — alimentados desde `logFn: (m) => logs.push(m)` y `errorFn: (m) => errs.push(m)`. **Dos arrays SEPARADOS, jamás uno mezclado**: stdout y stderr son streams independientes y su interleaving no está garantizado; un assert de secuencia cross-canal sería flaky por construcción (UI-SPEC §S-2, «Advertencia de contrato para el test»).

`scanFn` devuelve un report sucio inline (no `cleanReport()`, que lo trae limpio): `missing_group` con **1** elemento con la forma `{ name, anchor, members }`, `loose_workspace: []`, `empty_group: []`, `protected: { sessions: [] }`, `hasActions: true`, `hasAdvisories: true`. `executeFn` devuelve `{ created: 0, added: 2, ungrouped: 1, errors: [2 items con la forma { category, target, reason }] }`.

`created: 0` es **deliberado**: hace que el test detecte la regresión concreta que WR-02 nombra (usar `r.created` en lugar de `r.added` al calcular `applied`). Los tres números son distintos por diseño — `applied = 3`, `failed = 2`, `advisories = 1` — para que ningún falso verde pase por coincidencia numérica.

Los 4 asserts del bloque `&lt;behavior&gt;`, en ese orden. Los dos primeros usan los literales exactos tomados verbatim de `src/check.js:158` y `:161`; el tercero comprueba pertenencia por substring del conteo y el sustantivo; el cuarto es el assert negativo de canal, con mensaje explicando por qué existe (un fallo escrito en el canal del éxito sigue siendo invisible en un pipe).

**Test G** — la rama de silencio, que hoy nadie asevera porque los casos A/D pasan `errorFn: () => {}` y descartan el output. Naming: `'Test G: gate ON sin fallos — errors vacío o ausente NO emite ninguna línea por errorFn (WR-01)'`. Dos escenarios en el mismo `it()`: (a) `executeFn` que devuelve `emptyResult()` ⇒ `errs` vacío; (b) `executeFn` que devuelve un objeto **sin** campo `errors` ⇒ la llamada no lanza y `errs` sigue vacío. Este segundo escenario congela el guard defensivo que la Task 2 va a escribir.

Nota honesta para el SUMMARY: **Test F está RED hoy** (falla el tercer assert, porque la línea de fallos no existe) y es el que dicta la forma del fix. **Test G está VERDE hoy** por vacuidad — no es driver del fix, es el guard de regresión que impide que la Task 2 emita una línea espuria cuando no hay fallos.

**No tocar `src/check.js` en esta tarea.**
  </action>
  <verify>
    <automated>if node --test test/check.test.js; then echo "FALLO: Test F NO esta RED — revisa que asevere la linea de fallos por errorFn"; exit 1; else echo "RED confirmado (WR-01/WR-02)"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/check.test.js` termina con **exit code distinto de 0** y su salida nombra `Test F` entre los fallidos; `Test G` aparece entre los que pasan
    - `git diff test/check.test.js` añade exactamente dos `it()` y **cero líneas eliminadas o modificadas** en el rango `:320-440` preexistente
    - `grep -cF '[kodo:check] Sidebar: 3 acción(es) aplicadas' test/check.test.js` devuelve `>= 1`
    - `grep -cF '[kodo:check] Sidebar advisories: 1 (acción de operador)' test/check.test.js` devuelve `>= 1`
    - `grep -c 'Test F' test/check.test.js` y `grep -c 'Test G' test/check.test.js` devuelven ambos `>= 1`
    - El Test F declara `const logs` y `const errs` como arrays separados; `grep -c 'errs' test/check.test.js` devuelve `>= 4`
    - `git diff --exit-code src/check.js` sale 0 — el fix todavía no existe
  </acceptance_criteria>
  <done>El `describe` ORCH-07 cubre por fin la combinación `needsOrchestrator: true` + `hasAdvisories: true` que ninguno de los 6 casos previos alcanzaba, asevera el conteo `applied = added + ungrouped`, y exige la línea de fallos por `errorFn` con su mitad negativa en `logFn`. La rama de silencio tiene su propio assert. El commit registra el estado RED del Test F.</done>
</task>

<task type="auto">
  <name>Task 2 (85-02-02): WR-01 GREEN — la línea de fallos del piggyback por `errorFn`</name>
  <files>src/check.js</files>
  <read_first>
    - `src/check.js:140-175` — el piggyback completo. El código nuevo va **dentro** del `try` existente, entre el `logFn` de aplicadas y la rama `hasAdvisories`
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-UI-SPEC.md` §S-2 — el literal contractual, su anclaje, su canal, su condición, su orden y la política de singular/plural
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §3 — el estilo a igualar (`|| 0` / `|| []` defensivos, template literal, prefijo `[kodo:*]`, texto plano)
    - `.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-REVIEW.md` §Warnings — WR-01 con su fix propuesto
    - `.planning/milestones/v0.18-phases/80-carril-orquestador-reconciliaci-n-documental/80-CONTEXT.md` — D-04 (el sidebar NO es trigger; el resultado del doctor jamás re-entra al gate) y D-05 (orden del piggyback). **Ambos siguen vigentes: esta tarea no los toca**
  </read_first>
  <action>
Añadir la rama de observabilidad de **D-07** dentro del `try` del piggyback de `src/check.js`:

Calcular `const failed = (r.errors || []).length;` — el guard `|| []` es obligatorio: un `r` sin campo `errors` se trata como cero fallos, nunca como un throw (lo congela el Test G). Y si `failed > 0`, emitir por **`errorFn`** exactamente la línea `[kodo:check] Sidebar: ${failed} acción(es) fallida(s) (fail-open)`.

**Canal: `errorFn`, jamás `logFn`.** Es la restricción vinculante de D-07 y el punto entero del fix: un fallo escrito en el mismo canal que el éxito sigue siendo invisible en un pipe. Nota: la prosa de 80-REVIEW WR-01 dice «emitirlo por `logFn`» pero su propio snippet de fix usa `errorFn`; D-07 resuelve la contradicción a favor de `errorFn`.

**Ubicación exacta:** inmediatamente después del `logFn` de acciones aplicadas y **antes** de la rama `hasAdvisories`. Así las dos líneas derivadas de `r` (el resultado de `executeFn`) quedan juntas, y la de advisories — que deriva de `report`, el resultado de `scanFn` — queda separada.

**Sin ramificación singular/plural.** `acción(es) fallida(s)` con los paréntesis, exactamente como la hermana `acción(es) aplicadas`. Con `failed === 1` se renderiza `1 acción(es) fallida(s) (fail-open)`. Coherencia con la convención ya establecida en el fichero por encima de la gramática.

**Texto plano.** Sin `picocolors`, sin ANSI, sin emoji, sin `src/logger.js`. No se inyecta el logger real: `deps = {}` sigue produciendo el `noopLogger` obligado por LOG-12, y la observabilidad sale por el canal que el piggyback ya usa.

Acompañarlo de un comentario de porqué, en el estilo del fichero, que diga las tres cosas que un lector futuro necesita: (1) por qué el canal es stderr y no stdout; (2) que el fail-open no cambia — la línea informa, jamás bloquea el check ni el launch; (3) que el resultado jamás re-entra a `reasons` ni al gate `needsOrchestrator` (D-04 de Phase 80).

**Solo se emite el conteo.** Nada de `r.errors[i].reason`, `.target` ni `.category`: son refs de workspace y rutas del operador, y este es un carril automático sobre cuyo destino no tenemos control.

Fences: el gate, el orden `execute` → `launch`, el `catch` fail-open y las 3 líneas existentes **no se tocan**.
  </action>
  <verify>
    <automated>node --test test/check.test.js test/check-isolation.test.js && grep -cF 'acción(es) fallida(s) (fail-open)' src/check.js | grep -qx 1 && grep -n 'fallida' src/check.js | grep -q 'errorFn(' && echo "WR-01 GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/check.test.js` pasa con exit code 0, incluidos Test F y Test G
    - `grep -cF 'acción(es) fallida(s) (fail-open)' src/check.js` devuelve exactamente `1`
    - La línea que contiene `fallida` en `src/check.js` contiene también `errorFn(`: `grep -n 'fallida' src/check.js | grep -q 'errorFn('` sale 0
    - `grep -c 'logFn(.*fallida' src/check.js` devuelve `0` — el canal no es stdout
    - `grep -cF '(r.errors || []).length' src/check.js` devuelve `>= 1` — el guard defensivo existe
    - **Orden en el código verificado:** `awk '/acción\(es\) aplicadas/{if(!a)a=NR} /acción\(es\) fallida\(s\)/{if(!f)f=NR} /hasAdvisories/{if(!h)h=NR} END{exit !(a && f && h && a<f && f<h)}' src/check.js` sale 0
    - `grep -c 'picocolors' src/check.js` no aumenta respecto a HEAD, y `node --test test/check-isolation.test.js` sigue verde (LOG-12 intacto)
    - `git diff src/check.js` no modifica ninguna de las 3 líneas literales existentes ni el `catch`, no toca `reasons`, no toca `needsOrchestrator` y no altera ningún `process.exit` / código de retorno
    - `git diff --exit-code test/check.test.js` sale 0 respecto al commit de la Task 1 — el fix no se ha comprado tocando el test
  </acceptance_criteria>
  <done>`kodo check` distingue por fin «no había nada que arreglar» de «cmux caído y N acciones fallidas», y lo hace por el canal donde el operador lo verá aunque filtre stdout. El fail-open, el gate y las 3 líneas existentes están intactos.</done>
</task>

<task type="auto">
  <name>Task 3 (85-02-03): WR-03 — comentario honesto + guard source-grep con mordida real</name>
  <files>test/check-isolation.test.js</files>
  <read_first>
    - `test/check-isolation.test.js` (completo, 200+ líneas) — en particular `:10-16` (los dos comentarios de las regex y las constantes `IMPORT_FROM_RE` / `IMPORT_BARE_RE`), `:30-52` (el docblock de `walkImports` y el walker), `:54` (el `describe` destino), `:82-87` (el molde de mensaje de assert que imprime el grafo completo), `:156-176` y `:196-204` (guards vecinos y su convención de comentario-de-porqué)
    - `test/skill-sync.test.js:105-135` y `:815-842` — **el molde de D-09**: el guard source-hygiene de Phase 84, con `stripComments` y su línea de procedencia, la regex constante, el bucle sobre ficheros y el assert negativo explícito
    - `src/providers/registry.js:20-60` — los `await import()` reales de `:27,:28,:57,:58` que desmienten la premisa del comentario actual
    - `src/session/state.js:240-250` — el `import('../logger-events.js')` de `:247`: el único hit del guard a HEAD, y está en la allowlist
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-RESEARCH.md` §Sonda 1 (completa) y §Code Examples — la regex verificada, la tabla de los 13 falsos positivos y el texto propuesto para el comentario
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §1
  </read_first>
  <action>
**Mitad (a) — el comentario.** Reescribir los dos comentarios de `test/check-isolation.test.js` que hoy afirman que el repo no usa `import()` dinámico: el de `:14` (sobre las constantes de regex) y el de `:33-34` (dentro del docblock de `walkImports`). Retirar la afirmación y su atribución a la sonda A3 de la investigación de la Phase 06.

El texto nuevo debe decir exactamente cuatro cosas: (1) el walker sigue **solo** imports estáticos, a propósito; (2) el repo **SÍ** usa `import()` dinámico, con los paths concretos `src/providers/registry.js:27,28,57,58` y `src/session/state.js:247`; (3) el punto ciego lo cubre el guard de source-grep de la mitad (b), citando `Phase 85 / DEBT-07 WR-03`; (4) el alcance real de estos guards es el grafo de **MODULE-LOAD** de `kodo check` — coste de arranque y prohibición de `logger.js` — y **NO** una afirmación sobre alcanzabilidad en runtime, porque `check.js:103` llama `initRegistry()`, que carga `providers/{plane,github}/provider.js` con `await import()`. Cerrar con el dato tranquilizador verificado: ninguno de esos dos alcanza `logger.js`.

No citar la frase antigua textualmente — se retira, no se glosa.

**Mitad (b) — el guard.** Añadir, dentro del `describe('LOG-12: vigilante isolation (import-graph)')`, tres piezas:

1. `stripComments` copiado **verbatim** de `test/dispatcher-isolation.test.js:24-30`, con su línea de procedencia encima (el patrón exacto de `test/skill-sync.test.js:113-114`). Es la 10.ª copia verbatim en el repo: `test/helpers/` aloja fixtures y procesos hijo, no helpers de aserción, y el repo ya tomó esta decisión nueve veces. **No extraer a un módulo compartido** y, por D-18, **no aplicarlo a `test/format-isolation.test.js`** en esta fase.

2. Dos regex **CONSTANTES** (constraint anti-ReDoS: jamás compiladas desde input externo), con los valores ya verificados por RESEARCH §1d: `DYNAMIC_LOGGER_IMPORT_RE` capturando el specifier de un `import(...)` cuyo path contenga `logger` y termine en `.js`, con flag global; y `LOGGER_ALLOWLIST_RE` anclada al final para `logger-events.js` y `logger-noop.js`.

3. Un `it()` con el guard. Su forma: obtener la lista de ficheros con `walkImports(join(SRC, 'check.js'))` — **la misma llamada que los guards vecinos ya usan**; para cada fichero, `stripComments(readFileSync(file, 'utf-8'))` y recorrer `matchAll` de la regex; saltar los hits que la allowlist acepte; acumular el resto en un array `violations` con la forma `<path relativo> → import('<specifier>')`; y cerrar con `assert.deepEqual(violations, [], <mensaje>)` — assert negativo explícito, no `assert.ok`. El mensaje debe imprimir por qué camino entra la violación, siguiendo el molde de `:82-87` que ya imprime el grafo completo.

Encima del `it()`, el comentario-de-porqué que la convención del fichero exige: qué invariante custodia y **qué regresión concreta entraría en silencio sin él** (un `await import('../logger.js')` en `config.js`, `manager.js`, `state.js` o `client.js` rompería LOG-12 con el guard estático en verde). Y la razón de que sea un grep aparte y no una mejora del walker.

**`stripComments` es obligatorio y va ANTES del match.** Sin él el guard sale ROJO a HEAD con 13 falsos positivos, cinco apuntando literalmente a `../logger.js`, todos ellos imports de TIPO en JSDoc que se borran en runtime. Aceptar un guard rojo por comentarios obliga a debilitarlo después, que es exactamente lo que el constraint anti-greenear prohíbe.

**`walkImports`, `extractImports`, `IMPORT_FROM_RE` e `IMPORT_BARE_RE` NO se modifican.** Cualquier diff que los toque es la señal de alarma del Pitfall 1: seguir aristas dinámicas dentro del walker mete `github/provider.js` y `github/normalize.js` en la clausura y pone ROJOS dos guards vecinos.

**Si el guard sale ROJO al ejecutarlo por primera vez tras aplicar `stripComments`: NO se toca el assert.** RESEARCH §Sonda 1 verificó que a HEAD sale VERDE (único hit: `src/session/state.js → import('../logger-events.js')`, en allowlist). Un rojo inesperado es un hallazgo real de LOG-12 y se **escala** al operador con el path del violador.
  </action>
  <verify>
    <automated>node --test test/check-isolation.test.js && grep -c 'DYNAMIC_LOGGER_IMPORT_RE' test/check-isolation.test.js | grep -qvx 0 && grep -c 'stripComments' test/check-isolation.test.js | grep -qvx 0 && grep -c '06-RESEARCH A3' test/check-isolation.test.js | grep -qx 0 && echo "WR-03 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/check-isolation.test.js` pasa con exit code 0 y su salida incluye el `it()` nuevo con la ref `WR-03`
    - `grep -c 'DYNAMIC_LOGGER_IMPORT_RE' test/check-isolation.test.js` devuelve `>= 2` (declaración + uso) y `grep -c 'LOGGER_ALLOWLIST_RE'` devuelve `>= 2`
    - `grep -c 'stripComments' test/check-isolation.test.js` devuelve `>= 2` (definición + uso) y la definición va precedida de una línea de procedencia que nombra `dispatcher-isolation.test.js`
    - `grep -c '06-RESEARCH A3' test/check-isolation.test.js` devuelve `0` — la premisa falsa y su atribución han desaparecido
    - `grep -c 'registry.js' test/check-isolation.test.js` devuelve `>= 1` — el comentario nuevo cita los `await import()` reales
    - `grep -c 'MODULE-LOAD' test/check-isolation.test.js` devuelve `>= 1` — el alcance real de los guards queda declarado
    - **El walker está intacto:** `git diff -U0 test/check-isolation.test.js | grep -E '^-' | grep -cE 'function walkImports|function extractImports|const IMPORT_FROM_RE|const IMPORT_BARE_RE'` devuelve `0`
    - **Mordida real del guard** (verificación destructiva reversible; si falla, revertir igualmente): `printf "\nawait import('./logger.js');\n" >> src/labels.js; node --test test/check-isolation.test.js; RC=$?; git checkout -- src/labels.js; test $RC -ne 0` sale 0 — con la violación inyectada el guard se pone ROJO
    - Tras la verificación de mordida, `git diff --exit-code src/labels.js` sale 0 — el fichero quedó revertido
    - `git diff --exit-code test/format-isolation.test.js` sale 0 (D-18: no se toca, ni siquiera para copiar el patrón)
    - `npm test` sin regresiones: los guards vecinos de `github/provider.js` y `github/normalize.js` siguen verdes
  </acceptance_criteria>
  <done>El comentario del guard LOG-12 dice la verdad sobre los imports dinámicos del repo y declara el alcance real (module-load, no runtime). El guard tiene mordida demostrada: con un `import()` dinámico de `logger.js` inyectado en un fichero del grafo se pone rojo, y a HEAD sale verde con el único hit en allowlist. `walkImports` y `format-isolation.test.js` están intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cmux` (proceso externo) → `executeFn` → `r.errors` → stderr | Los items fallidos traen `target` y `reason` procedentes de un proceso externo; cruzan a un canal de salida sobre cuyo destino no tenemos control. |
| Árbol de fuentes del repo → `readFileSync` del guard | El guard nuevo lee ficheros **del propio repo** (la lista que `walkImports` devuelve). No hay input del operador, no hay red, no hay path derivado de entrada externa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-02-01 | Denial of Service | `DYNAMIC_LOGGER_IMPORT_RE` / `LOGGER_ALLOWLIST_RE` (`test/check-isolation.test.js`) | low | mitigate | Ambas declaradas como **constantes**, nunca compiladas desde input externo (constraint anti-ReDoS LOCKED). Sin alternancia anidada ni cuantificadores solapados: las clases son `[^'"]*`, que no retroceden sobre sí mismas. Operan solo sobre fuentes del repo. |
| T-85-02-02 | Information Disclosure | Línea nueva de `src/check.js` → stderr | medium | mitigate | La línea emite **`${failed}`, un entero, y nada más**. Prohibición explícita en `must_haves.prohibitions` y criterio de aceptación: `grep` confirma que la línea no interpola `reason`, `target` ni `category`. Los items fallidos traen refs de workspace del operador y no cruzan al canal. |
| T-85-02-03 | Repudiation | El guard LOG-12 reforzado | high | mitigate | Prohibición explícita: si sale rojo tras `stripComments`, se escala — no se relaja la regex ni se amplía la allowlist. Verificación de **mordida** obligatoria en los criterios de aceptación (inyectar violación → ROJO → revertir), de modo que un guard vacuamente verde no puede pasar por cumplido. RESEARCH §Sonda 1 verificó el veredicto GREEN **antes** de escribir el assert, eliminando la presión de debilitarlo. |
| T-85-02-04 | Tampering | `walkImports` / `extractImports` | high | mitigate | Prohibición explícita + criterio de aceptación con `git diff -U0` filtrado: modificar el walker pone ROJOS dos guards vecinos (`github/provider.js`, `github/normalize.js`) y la reacción esperada sería debilitarlos. D-09 acota el refuerzo a un grep sobre la salida existente. |
| T-85-02-05 | Elevation of Privilege | Piggyback del sidebar → gate `needsOrchestrator` | low | mitigate | La línea nueva no re-entra a `reasons`, no altera el gate y no cambia el exit code (D-04 de Phase 80, vigente). Criterio de aceptación: `git diff` de `src/check.js` no toca `reasons`, `needsOrchestrator` ni ningún retorno. |
| T-85-02-06 | Tampering | Verificación de mordida (mutación temporal de `src/labels.js`) | medium | mitigate | La mutación es una sola línea appendeada, se revierte con `git checkout --` en el mismo comando **incluso si el test falla** (se captura el RC antes de revertir), y un criterio de aceptación posterior exige `git diff --exit-code src/labels.js` en 0. `check-isolation.test.js` lee ficheros como texto: no importa `labels.js` en runtime. |
| T-85-02-SC | Tampering | Supply chain (npm) | low | accept | **Cero instalaciones.** Constraint LOCKED «cero deps npm nuevas»; `85-RESEARCH.md` §Package Legitimacy Audit devuelve tabla vacía (0 paquetes, 0 `[SLOP]`, 0 `[SUS]`). No procede checkpoint de legitimidad. |
</threat_model>

<verification>
- `node --test test/check.test.js` — verde, incluidos Test F y Test G
- `node --test test/check-isolation.test.js` — verde, incluido el guard WR-03
- Verificación de mordida ejecutada y revertida, con su resultado escrito en el SUMMARY
- `npm test` — suite completa sin regresiones respecto al baseline de 2586 tests · 0 fail
- `git diff --exit-code test/format-isolation.test.js` — sale 0 (D-18)
- `git diff -U0 test/check-isolation.test.js` — sin eliminaciones sobre `walkImports` / `extractImports` / las dos constantes de import
- SUMMARY con la disposición individual de los 3 warnings de 80-REVIEW (los 3 RESUELTOS, ninguno re-aceptado) y la nota de que `IN-01` queda diferido por D-10
</verification>

<success_criteria>
1. WR-01 resuelto: `kodo check` emite por stderr una línea con el conteo de acciones fallidas del doctor del sidebar, con el fail-open y el gate intactos (DEBT-07).
2. WR-02 resuelto: el `describe` ORCH-07 asevera el conteo `applied = added + ungrouped`, la rama de advisories y el canal de la línea de fallos en sus dos mitades (DEBT-07).
3. WR-03 resuelto en sus dos mitades: el comentario deja de afirmar una premisa falsa y declara el alcance real; el guard tiene mordida demostrada sobre la lista que `walkImports` ya devuelve, con `walkImports` sin modificar (DEBT-07).
4. Ningún assert existente debilitado; `test/format-isolation.test.js` sin tocar.
5. `IN-01` explícitamente **fuera** con su razón (D-10), listo para registrarse como diferido en el plan 85-05.
</success_criteria>

<output>
Create `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-02-SUMMARY.md` when done.
</output>
