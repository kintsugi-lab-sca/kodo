---
phase: 85-saneo-de-deuda-nyquist-retroactivo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md
  - .planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VALIDATION.md
  - .planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VALIDATION.md
autonomous: true
requirements: [NYQ-02]

must_haves:
  truths:
    - "El veredicto `nyquist_compliant` se decide por fase de forma independiente contra su propia evidencia; ninguna fase hereda el veredicto de otra, y dos fases con evidencia distinta pueden divergir (D-17)."
    - "Si el `VERIFICATION.md` de una fase no aporta evidencia citable para alguna dimensión, esa fila se declara manual-only o el frontmatter queda `nyquist_compliant: false` con la razón escrita (D-17) — nunca `true` sin cita."
    - "Las 6 fases se procesan en el orden 79→80→81→69→71→72 (D-16), pero el resultado de cada una es independiente del orden: ninguna depende del estado de otra."
    - "Las tres fases de v0.16 (69, 71, 72) tienen su `VALIDATION.md` con `status: validated` y `nyquist_compliant: true`, y cada fila de su Per-Task Verification Map cita un fichero de evidencia CONCRETO con su resultado (`{N}-VERIFICATION.md` + sección + conteo), nunca una afirmación sin fichero detrás."
    - "El backfill es citation-based: CERO ficheros de test nuevos y CERO re-ejecución de la suite (D-12), sobre código de un milestone cerrado hace tres milestones."
    - "La forma de salida es el molde de `41-VALIDATION.md` (D-13), con `## Reconstruction Audit {fecha} (Phase 85 NYQ-02)` y sus 6 métricas — **no** el audit trail genérico que prescribe el §6 del workflow."
    - "`72-VALIDATION.md` deja de ser la plantilla literal sin rellenar: los marcadores de framework, comando, requirement y fichero de test desaparecen, sustituidos por los valores reales de la fase sobre sus 5 planes y HYG-01..08."
    - "69, 71 y 72 NO traen los dos comentarios de lifecycle en su frontmatter (son anteriores a esa convención) y **no se les añaden**: se edita `status` y `nyquist_compliant` sin inventar estructura que el fichero original no tenía. La fecha `created` de cada fase queda intacta."
    - "Los `MILESTONE-AUDIT.md` archivados de v0.16 quedan INTACTOS (D-15). El cierre se registra en la cabecera del propio `VALIDATION.md` y en la fila de `STATE.md` §Deferred Items (esta última la aplica el plan 85-05)."
  artifacts:
    - "`.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md` — mapa reconstruido sobre NET-01..06 y 4 planes"
    - "`.planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VALIDATION.md` — las 4 filas `TBD` completadas sobre DELIV-01..04 y 5 planes"
    - "`.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VALIDATION.md` — plantilla literal sustituida por el contrato real sobre HYG-01..08 y 5 planes"
  key_links:
    - "`{N}-VALIDATION.md` → `{N}-VERIFICATION.md`: la cita que constituye la cobertura; sin ella el `true` es greenear enmascarando"
    - "`{N}-VALIDATION.md` → los `{N}-NN-SUMMARY.md` y, en 71/72, el `{N}-UAT.md`: las fuentes de la reconstrucción del mapa"
    - "`status: validated` + `nyquist_compliant: true` → `audit-milestone §5.5`: el consumidor que distingue NOT-VALIDATED (`draft`) de PARTIAL (`validated` + `false`) de COMPLIANT"
  prohibitions:
    - "Ninguna fase recibe `nyquist_compliant: true` sin una cita concreta (fichero + sección + resultado) por dimensión. Un `true` sin evidencia es greenear enmascarando con disfraz documental; D-17 (`validated` + `nyquist_compliant: false` con la razón escrita) es la salida honesta."
    - "El bloque NYQ no genera ni un solo fichero de test nuevo ni re-ejecuta la suite. Un fichero sin trackear bajo `test/` o un commit con prefijo de test durante este plan es la señal de que el gate §4 del workflow se respondió mal — y aquí el riesgo es máximo, porque el código auditado es de v0.16."
    - "No se reescribe la prosa de artefactos archivados que son snapshots históricos: los `MILESTONE-AUDIT.md` de v0.16, los `SUMMARY`, los `UAT` y los `SECURITY` de 69/71/72 quedan intactos. Solo se edita el `VALIDATION.md` de cada fase."
    - "No se re-deriva evidencia que no existe. Si una dimensión no tiene resultado empírico en disco, la salida es una fila manual-only con su razón — jamás una corrida nueva ni una estimación redactada como si fuera medición."
    - "No se añade al frontmatter de 69/71/72 estructura que esos ficheros nunca tuvieron (los dos comentarios de lifecycle). Homogeneizar por estética reescribe la historia del fichero sin aportar nada al consumidor."
---

<objective>
Saldar **NYQ-02**: los `VALIDATION.md` de las Phases **69**, **71** y **72** (milestone v0.16) pasan de `draft` / `nyquist_compliant: false` a `validated` / `nyquist_compliant: true`, **citation-based**. Con esto se cierra la columna Nyquist de v0.16 en `STATE.md` §Deferred Items.

Purpose: son las tres fases más antiguas del backfill y las de mayor riesgo de fricción — por eso van últimas (D-16) y por eso `72` recibe su propia tarea con presupuesto explícito. La evidencia existe y es rica en las tres; lo que varía brutalmente es el **coste de reconstrucción**, y tratarlas como trabajo equivalente es el error de estimación más probable de este bloque.

Output: tres `VALIDATION.md` reescritos in-place en su directorio archivado bajo `.planning/milestones/v0.16-phases/`, con el molde de `41-VALIDATION.md`.

Decisiones que este plan implementa: **D-11** (vehículo `/gsd-validate-phase {N}`, una invocación por fase, sobre el directorio archivado), **D-12** (cero tests nuevos, cero re-ejecución de la suite), **D-13** (molde `41-VALIDATION.md`), **D-14** (`status: validated`; `wave_0_complete` como está ante duda), **D-15** (los `MILESTONE-AUDIT.md` archivados no se reescriben), **D-16** (bloque v0.16 después del de v0.18), **D-17** (si la evidencia no da, `false` con la razón escrita).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-CONTEXT.md
@.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md
</context>

## Protocolo de invocación (LOCKED — idéntico al del plan 85-03, leer antes de la Task 1)

El vehículo es `/gsd-validate-phase {N}`, una invocación por fase (D-11). `init.phase-op` **resuelve fases archivadas**: las tres apuntan a su directorio bajo `.planning/milestones/v0.16-phases/` y traen `has_verification: true`. La skill opera in-place, sin mover nada.

| Paso | Qué hace | Instrucción vinculante para este plan |
|---|---|---|
| §0 Initialize | init + resolución de hooks | El hook `nyquist` está ACTIVO; el §0 no aborta |
| §1 Detect Input State | `ls *-VALIDATION.md` | Las tres son **State A** → ruta «audit existing» |
| §2 Discovery | lee PLAN/SUMMARY, cruza requirement↔test por nombre/imports/descripciones | **No ejecuta nada.** Es el trabajo citation-based que D-12 quiere |
| §3 Gap Analysis | clasifica COVERED / PARTIAL / MISSING | Sin gaps ⇒ salta a §6 con `nyquist_compliant: true`. El criterio «runs green» se satisface **por CITA** a `{N}-VERIFICATION.md` |
| §4 Present Gap Plan | gate interactivo con 3 opciones | **SIEMPRE la opción 2 — «Skip — mark manual-only».** NUNCA la opción 1. Es EL mecanismo de supresión de D-12 |
| §5 Spawn auditor | **genera ficheros de test** | **PROHIBIDO.** No se alcanza si §3 no ve gaps o si §4 responde opción 2 |
| §6 Generate/Update | actualiza mapa + frontmatter + audit trail | Aquí se aplica el molde de D-13, **no** la plantilla genérica del workflow |
| §7 Commit | `git add {test_files}` + commit de tests, luego commit `docs(...)` | **Omitir la primera mitad.** Solo el commit `docs(...)` vía `gsd-tools` |

**Divergencia a resolver a favor de D-13:** el §6 del workflow prescribe un audit trail genérico con tabla `Gaps found / Resolved / Escalated`. El molde de 41 usa `## Reconstruction Audit {fecha}` con métricas más ricas. **D-13 es LOCKED ⇒ prevalece el molde 41.**

**Diferencia de frontmatter respecto al plan 85-03:** 79/80/81 traen embebidos dos comentarios de lifecycle encima de `status`. **69, 71 y 72 NO los traen y no se les añaden.** Se edita `status` y `nyquist_compliant`, se deja `created` y `wave_0_complete` como están, y no se inventa estructura que el fichero original no tenía.

## Presupuesto relativo (RESEARCH §6a — no tratar las tres como equivalentes)

| Fase | Estado de partida del mapa | Coste |
|---|---|---|
| **71** | 4 filas con `TBD (planner fills)`, pero requirement, secure behavior y comando **ya correctos** (DELIV-01..04); Manual-Only ya tiene 1 fila | MEDIO |
| **69** | **1 fila placeholder** para NET-01..06; Manual-Only ya tiene 1 fila | MEDIO-ALTO |
| **72** | **PLANTILLA LITERAL SIN RELLENAR** — marcadores de framework, comando, requirement y fichero de test — sobre 5 planes y 8 requirements | **ALTO (~4× el de 81)** |

## Artifacts this phase produces

Este plan **no crea ningún fichero nuevo y no añade ningún símbolo de código**. Lo que genuinamente produce:

| Artefacto | Naturaleza |
|---|---|
| `69-VALIDATION.md` reescrito | mapa reconstruido sobre NET-01..06 y 4 planes, con evidencia citada por fila |
| `71-VALIDATION.md` reescrito | las 4 filas `TBD` completadas con su columna de evidencia; 2 items humanos como manual-only |
| `72-VALIDATION.md` reescrito | plantilla literal sustituida por el contrato real sobre HYG-01..08 y 5 planes |

**Cero código, cero tests, cero deps, cero ficheros nuevos.**

<tasks>

<task type="auto">
  <name>Task 1 (85-04-01): backfill de Phase 69 — mapa reconstruido sobre NET-01..06</name>
  <files>.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md</files>
  <read_first>
    - `.planning/milestones/v0.10-phases/41-doctor-m-dulo-puro-de-saneo-cli/41-VALIDATION.md` (completo) — **el molde de D-13**
    - `.planning/milestones/v0.18-phases/81-saneo-de-deuda-v0-17/81-VALIDATION.md` — el molde ya instanciado en esta misma fase por el plan 85-03; es la referencia más cercana en tono y en convención de citas
    - `.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md` — estado de partida: **1 fila placeholder** para NET-01..06; Manual-Only ya trae 1 fila
    - `.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VERIFICATION.md` — `status: passed`, **12/12** must-haves, `behavior_unverified: 0`, sin `human_verification`. Cada truth cita fichero: `test/server-bind.test.js`, `test/server-auth.test.js`, `test/server/auth.test.js` (39 subtests / 122 asserts), `test/server-body-limit.test.js`, `test/server-error-hygiene.test.js`, `test/server-malformed-request.test.js`, `test/logs-reader.test.js`, `test/dashboard-client.test.js`, `test/format-isolation.test.js`
    - Los 4 `69-NN-SUMMARY.md` del mismo directorio
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-PATTERNS.md` §6 (6a a 6f)
  </read_first>
  <action>
Invocar `/gsd-validate-phase 69` siguiendo el §Protocolo de invocación al pie de la letra: gate §4 respondido **opción 2** si aparece, §5 nunca invocado, §7 sin `git add` de ficheros de test.

Reescribir `69-VALIDATION.md` con el molde de `41-VALIDATION.md`, en el mismo orden de secciones que fijó el plan 85-03: frontmatter · blockquote de cabecera de 3 líneas · `## Test Infrastructure` con la fila `Evidencia citada` añadida · `## Sampling Rate` con sus 3 bullets (el segundo con ref a **D-12**) · `## Per-Task Verification Map (dimensión → cobertura citada)` con las 7 columnas y la leyenda del molde · `## Wave 0 Requirements` · `## Manual-Only Verifications` · `## Validation Sign-Off` · `## Reconstruction Audit {fecha} (Phase 85 NYQ-02)` con sus 6 métricas y el párrafo **Nota Nyquist**.

**Frontmatter.** `status: validated` (no `approved`) y `nyquist_compliant: true`. `created` intacta. `wave_0_complete` llega en `false` y **se deja en `false`** (D-14). **No añadir** los dos comentarios de lifecycle: este fichero es anterior a esa convención y homogeneizarlo reescribiría su historia sin beneficio.

**El mapa se reconstruye entero**, sustituyendo la única fila placeholder actual: una fila por dimensión de **NET-01..NET-06** sobre los 4 planes, cada una con su celda de evidencia citada apuntando a `69-VERIFICATION.md` (sección + conteo en negrita + `Truth #N ✓ VERIFIED`) y al fichero de test concreto que el propio VERIFICATION nombra. 69 es el caso más rico del bloque: 12 truths, `behavior_unverified: 0`, cada NET-0x mapeando a ≥1 fichero nombrado — hay material para no dejar ninguna celda floja.

69 **no tiene `UAT.md`** ni items de `human_verification`. El tercer bullet de `## Sampling Rate` (UAT humano) se redacta declarando eso explícitamente, no se omite en silencio ni se rellena con un resultado inventado. `## Manual-Only Verifications` conserva su fila existente con la columna `Evidencia` completada.

**Comprobar el veredicto contra el fichero, no asumirlo** (D-17). El veredicto de esta fase se decide contra `69-VERIFICATION.md` y solo contra él: ninguna fase hereda el veredicto de otra, y dos fases con evidencia distinta pueden divergir.

**No tocar** el `MILESTONE-AUDIT.md` de v0.16 ni ningún `SUMMARY` archivado (D-15).
  </action>
  <verify>
    <automated>P=.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md; grep -c 'nyquist_compliant: true' "$P" | grep -qx 1 && grep -c 'status: validated' "$P" | grep -qx 1 && grep -c 'Reconstruction Audit' "$P" | grep -qvx 0 && grep -c 'Validation Audit' "$P" | grep -qx 0 && grep -c 'NET-0' "$P" | grep -qvx 0 && test "$(git status --porcelain test/ | grep -c '^??')" -eq 0 && echo "NYQ-02/69 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' <path>` devuelve `1`; `grep -c 'status: validated' <path>` devuelve `1`; `grep -c 'status: approved' <path>` devuelve `0`
    - `git diff <path> | grep -c '^-created:'` devuelve `0` — la fecha original de la fase no se ha tocado
    - `grep -c '^# status lifecycle:' <path>` devuelve `0` — no se ha añadido estructura ajena al fichero
    - `grep -c 'wave_0_complete: false' <path>` devuelve `1` (D-14)
    - `grep -c 'Reconstruction Audit' <path>` devuelve `>= 1`; `grep -c 'Validation Audit' <path>` devuelve `0`
    - El Per-Task Verification Map tiene una fila por cada uno de **NET-01..NET-06**; `grep -ci 'a rellenar\|por el planner\|TBD' <path>` devuelve `0`
    - `grep -c '69-VERIFICATION.md' <path>` devuelve `>= 3`
    - `grep -c 'server-auth.test.js\|server-bind.test.js' <path>` devuelve `>= 2` — los ficheros de test que el VERIFICATION nombra están citados
    - `grep -c 'Evidencia citada' <path>` devuelve `>= 2`
    - **D-12 respetado:** `git status --porcelain test/ | grep -c '^??'` devuelve `0` y `git log --oneline -10 | grep -c 'test(phase-'` devuelve `0`
    - `git diff --exit-code .planning/milestones/v0.16-MILESTONE-AUDIT.md` sale 0 (D-15)
  </acceptance_criteria>
  <done>`69-VALIDATION.md` tiene un Per-Task Verification Map completo sobre NET-01..06 y sus 4 planes, cada fila con evidencia citada a `69-VERIFICATION.md` y al fichero de test nombrado, `validated` + `nyquist_compliant: true`, con el veredicto decidido contra su propia evidencia.</done>
</task>

<task type="auto">
  <name>Task 2 (85-04-02): backfill de Phase 71 — completar las 4 filas `TBD` de DELIV-01..04</name>
  <files>.planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VALIDATION.md</files>
  <read_first>
    - `.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md` — **el resultado de la Task 1**: el molde ya instanciado para el bloque v0.16
    - `.planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VALIDATION.md` — estado de partida: 4 filas con `TBD (planner fills)` pero requirement, secure behavior y comando ya correctos (DELIV-01..04); Manual-Only ya trae 1 fila
    - `.planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VERIFICATION.md` — `status: passed`, **4/4** must-haves tras cerrar 2 gaps BLOCKER (el propio informe declara haber corrido los tests), `behavior_unverified: 0`, **2** items de `human_verification` (Plane real, GitHub real). Evidencia: `test/triggers/polling.test.js` (5+5 casos), `test/adopt-cli.test.js` (34/34, E2E `:529-601`), `test/hooks/session-end.test.js:285-321 / 323-346`
    - `.planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-UAT.md` — 2 items totales: 1 pass, 1 **skipped**
    - Los 5 `71-NN-SUMMARY.md` del mismo directorio
    - `.planning/STATE.md` §Deferred Items — la fila «Backstop GitHub real (nunca cierra issues) — skip reconocido por el operador 2026-07-09; mock de 3 capacidades como cobertura compensatoria»
  </read_first>
  <action>
Invocar `/gsd-validate-phase 71` con el mismo protocolo (gate §4 → opción 2; §5 nunca; §7 sin `git add` de tests) y reescribir `71-VALIDATION.md` con el molde ya instanciado en la Task 1.

El delta principal es **completar las 4 filas que hoy llevan el marcador de placeholder del seed**: requirement, secure behavior y comando ya son correctos, así que el trabajo se concentra en la columna «Evidencia citada (fichero + resultado)» de cada una — `71-VERIFICATION.md` con su sección, el conteo en negrita y el `Truth #N ✓ VERIFIED`, más el fichero de test concreto (incluidos los rangos de línea que el VERIFICATION nombra para `session-end.test.js`).

Los **2 items de `human_verification`** (Plane real, GitHub real) van a `## Manual-Only Verifications` con su razón: son manual-only **por construcción**, exigen servicios externos vivos. Su columna `Evidencia` cita el `71-UAT.md`.

El **`skipped: 1` del UAT** se declara explícitamente en el tercer bullet de `## Sampling Rate` y en la fila Manual-Only correspondiente, reproduciendo la formulación que `STATE.md` §Deferred Items ya usa: skip reconocido por el operador con fecha, con el mock de 3 capacidades como cobertura compensatoria. **No se maquilla como pass.** Anotar además que esa fila de `STATE.md` sigue abierta y que **esta fase no la cierra** — solo la deja correctamente contabilizada.

Frontmatter: `status: validated`, `nyquist_compliant: true`, `created` intacta, `wave_0_complete` en `false` (D-14), **sin añadir** los comentarios de lifecycle.

Audit trail: `## Reconstruction Audit {fecha} (Phase 85 NYQ-02)` con las 6 métricas del molde 41.

**Comprobar el veredicto contra el fichero** (D-17): RESEARCH §6c lo declara defendible como `true` (4/4 tras gap closure, los 2 items humanos manual-only por construcción, precedente aceptado en `41-VALIDATION.md`, que es `true` **con** fila Manual-Only). El juicio se hace leyendo `71-VERIFICATION.md`, no heredándolo de la Task 1.

**No tocar** `MILESTONE-AUDIT.md`, `71-UAT.md` ni ningún otro artefacto archivado (D-15).
  </action>
  <verify>
    <automated>P=.planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VALIDATION.md; grep -c 'nyquist_compliant: true' "$P" | grep -qx 1 && grep -c 'status: validated' "$P" | grep -qx 1 && grep -c 'Reconstruction Audit' "$P" | grep -qvx 0 && grep -c 'Validation Audit' "$P" | grep -qx 0 && grep -c 'TBD' "$P" | grep -qx 0 && test "$(git status --porcelain test/ | grep -c '^??')" -eq 0 && echo "NYQ-02/71 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' <path>` devuelve `1`; `grep -c 'status: validated' <path>` devuelve `1`
    - `grep -c 'TBD' <path>` devuelve `0` — las 4 filas placeholder están completadas
    - `git diff <path> | grep -c '^-created:'` devuelve `0`; `grep -c '^# status lifecycle:' <path>` devuelve `0`
    - `grep -c 'wave_0_complete: false' <path>` devuelve `1` (D-14)
    - `grep -c 'Reconstruction Audit' <path>` devuelve `>= 1`; `grep -c 'Validation Audit' <path>` devuelve `0`
    - El Per-Task Verification Map tiene una fila por cada uno de **DELIV-01..DELIV-04**, todas con celda de evidencia no vacía
    - `grep -c '71-VERIFICATION.md' <path>` devuelve `>= 3`
    - `grep -c 'adopt-cli.test.js\|polling.test.js\|session-end.test.js' <path>` devuelve `>= 2`
    - `## Manual-Only Verifications` contiene **2** filas para los items de `human_verification` (Plane real y GitHub real), cada una con su columna `Evidencia`
    - El fichero declara explícitamente el `skipped: 1` del UAT: `grep -ci 'skip' <path>` devuelve `>= 1`, y en ningún sitio se cuenta ese item como `pass`
    - **D-12 respetado:** `git status --porcelain test/ | grep -c '^??'` devuelve `0`
    - `git diff --exit-code .planning/milestones/v0.16-MILESTONE-AUDIT.md .planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-UAT.md` sale 0 (D-15)
  </acceptance_criteria>
  <done>`71-VALIDATION.md` cubre DELIV-01..04 sobre sus 5 planes con evidencia citada por fila, los 2 items de verificación humana contabilizados como manual-only por construcción, y el `skipped: 1` del UAT declarado tal cual — sin maquillarlo como pass.</done>
</task>

<task type="auto">
  <name>Task 3 (85-04-03): backfill de Phase 72 — la plantilla literal se sustituye por el contrato real (el más caro de los seis)</name>
  <files>.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VALIDATION.md</files>
  <read_first>
    - `.planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md` y `.../71-VALIDATION.md` — los resultados de las Tasks 1 y 2: el molde ya instanciado dos veces en este bloque
    - `.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VALIDATION.md` — estado de partida: **la plantilla literal sin rellenar**, con marcadores de framework, de comando rápido, de requirement y de fichero de test. Todo se sustituye
    - `.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VERIFICATION.md` — `status: passed`, **5/5** must-haves (verificado post-review a `2adfebd`), `behavior_unverified: 0`, 1 item de `human_verification` (propagación real de `KODO_ORCHESTRATOR=1`). Evidencia: `test/hooks/stop-idempotency.test.js`, `test/skill-auto-commit.test.js` (A/B/C), `test/config-hardening.test.js` (22/22), `test/config.test.js`, `test/config-migration-atomic.test.js`, `test/dashboard-format.test.js`, `test/dashboard-table.test.js`, `test/hooks/session-end.test.js`, más tests por hallazgo (labels, gsd-verification, gsd-roadmap, plane-provider, hooks/install, registry)
    - `.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-UAT.md` — 1/1 pass
    - `.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-SECURITY.md` — fuente adicional de dimensiones de *Secure Behavior* para la tercera columna del mapa
    - Los 5 `72-NN-SUMMARY.md` del mismo directorio
    - `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-RESEARCH.md` §Sonda 6a y §Pitfall 7 — por qué esta tarea tiene su propio presupuesto
  </read_first>
  <action>
Invocar `/gsd-validate-phase 72` con el mismo protocolo (gate §4 → opción 2; §5 nunca; §7 sin `git add` de tests). **Esta es la tarea más cara de la fase — presupuestarla en consecuencia** (RESEARCH §6a la estima en ~4× el coste de `81`; §Pitfall 7 nombra «tratar las 6 fases como trabajo equivalente» como el error de estimación de este bloque).

`72-VALIDATION.md` es hoy **la plantilla literal sin rellenar**: los marcadores de framework, de comando rápido, de requirement y de fichero de test siguen ahí tal cual salieron del seed. **Todos desaparecen**, sustituidos por los valores reales de este repo y de esta fase:

- `## Test Infrastructure`: framework `node:test` (runner nativo) + `node:assert/strict`, sin fichero de config (convención `test/**/*.test.js`), comando de suite completa `npm test`, más la fila **`Evidencia citada`** con `72-VERIFICATION.md`, su fecha, su `status passed` y su score 5/5.
- `## Per-Task Verification Map (dimensión → cobertura citada)`: una fila por dimensión de **HYG-01..HYG-08** sobre los **5 planes**, con las 7 columnas y la leyenda del molde. La tercera columna (*Dimensión / Secure Behavior*) se nutre también de `72-SECURITY.md`. La sexta columna cita `72-VERIFICATION.md` (sección + conteo en negrita + `Truth #N ✓ VERIFIED`) y el fichero de test concreto.
- El resto de secciones en el orden del molde 41, con `## Reconstruction Audit {fecha} (Phase 85 NYQ-02)` y sus 6 métricas.

El único item de `human_verification` (propagación real de `KODO_ORCHESTRATOR=1` a través de `cmux.send`) va a `## Manual-Only Verifications` con su razón — es manual-only **por naturaleza**, con modo de fallo seguro documentado — y su columna `Evidencia` cita `72-UAT.md` (1/1 pass). Coste alto de reconstrucción no significa evidencia débil: son cosas distintas y el audit debe reflejarlo.

Frontmatter: `status: validated`, `nyquist_compliant: true`, `created` intacta, `wave_0_complete` en `false` (D-14), **sin añadir** los comentarios de lifecycle.

**Comprobar el veredicto contra el fichero** (D-17), leyendo `72-VERIFICATION.md` y no heredando el de las tareas anteriores. Si al reconstruir el mapa alguna de las 8 dimensiones queda sin cita utilizable, la salida honesta es una fila manual-only con su razón o, en el límite, `validated` + `nyquist_compliant: false` con la razón escrita — que es el estado PARTIAL que `audit-milestone §5.5` sabe leer. Un `true` sin cita sería greenear enmascarando con disfraz documental.

**No tocar** `MILESTONE-AUDIT.md`, `72-UAT.md`, `72-SECURITY.md` ni ningún otro artefacto archivado (D-15).
  </action>
  <verify>
    <automated>P=.planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VALIDATION.md; grep -c 'nyquist_compliant: true' "$P" | grep -qx 1 && grep -c 'status: validated' "$P" | grep -qx 1 && grep -c 'Reconstruction Audit' "$P" | grep -qvx 0 && grep -c 'Validation Audit' "$P" | grep -qx 0 && grep -c 'pytest' "$P" | grep -qx 0 && grep -c 'HYG-0' "$P" | grep -qvx 0 && test "$(git status --porcelain test/ | grep -c '^??')" -eq 0 && echo "NYQ-02/72 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' <path>` devuelve `1`; `grep -c 'status: validated' <path>` devuelve `1`
    - **La plantilla ha desaparecido:** `grep -c 'pytest' <path>` devuelve `0`, `grep -c 'jest' <path>` devuelve `0`, `grep -c 'REQ-{' <path>` devuelve `0`, `grep -c 'tests/test_file' <path>` devuelve `0`, y `grep -c '{' <path>` no contiene ningún marcador de plantilla sin sustituir
    - `grep -c 'node:test' <path>` devuelve `>= 1` — el framework real está declarado
    - `git diff <path> | grep -c '^-created:'` devuelve `0`; `grep -c '^# status lifecycle:' <path>` devuelve `0`
    - `grep -c 'wave_0_complete: false' <path>` devuelve `1` (D-14)
    - `grep -c 'Reconstruction Audit' <path>` devuelve `>= 1`; `grep -c 'Validation Audit' <path>` devuelve `0`
    - El Per-Task Verification Map tiene una fila por cada uno de **HYG-01..HYG-08**, todas con celda de evidencia no vacía
    - `grep -c '72-VERIFICATION.md' <path>` devuelve `>= 3`
    - `grep -c 'config-hardening.test.js\|stop-idempotency.test.js\|skill-auto-commit.test.js' <path>` devuelve `>= 2`
    - `## Manual-Only Verifications` contiene la fila de propagación de `KODO_ORCHESTRATOR=1` con su columna `Evidencia` citando `72-UAT.md`
    - **D-12 respetado:** `git status --porcelain test/ | grep -c '^??'` devuelve `0` y `git log --oneline -10 | grep -c 'test(phase-'` devuelve `0`
    - `git diff --exit-code .planning/milestones/v0.16-MILESTONE-AUDIT.md .planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-UAT.md .planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-SECURITY.md` sale 0 (D-15)
    - **Cierre de NYQ-02:** las tres fases del plan tienen el flag — `grep -l 'nyquist_compliant: true' .planning/milestones/v0.16-phases/69-red-y-autenticaci-n/69-VALIDATION.md .planning/milestones/v0.16-phases/71-fiabilidad-de-entrega-y-backstop/71-VALIDATION.md .planning/milestones/v0.16-phases/72-higiene-dx-y-verdad-documental/72-VALIDATION.md | wc -l` devuelve `3`
  </acceptance_criteria>
  <done>`72-VALIDATION.md` deja de ser una plantilla y pasa a ser un contrato de validación real sobre HYG-01..08 y sus 5 planes, con evidencia citada por dimensión y el único item humano contabilizado como manual-only. Con esto NYQ-02 queda saldado y la columna Nyquist de v0.16 lista para cerrarse en `STATE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(sin frontera de red ni de proceso)* | El plan edita tres ficheros Markdown en directorios archivados del propio repo. Cero código, cero endpoints, cero parsing de entrada, cero instalaciones. |
| Agente de backfill → árbol de artefactos archivados de v0.16 | El único vector real es de **integridad documental**, y aquí es máximo: el código auditado cerró hace tres milestones y `72` parte de una plantilla vacía, lo que crea presión hacia el relleno plausible sin cita. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-04-01 | Repudiation | `nyquist_compliant: true` sin evidencia (agravado en `72`, que parte de plantilla) | high | mitigate | Prohibición explícita + criterios por fase: `grep -c '{N}-VERIFICATION.md'` ≥ 3, cero marcadores de plantilla (`pytest`, `jest`, `REQ-{`, `tests/test_file`), cero `TBD`, y una fila por requirement sin celda de evidencia vacía. D-17 (`validated` + `false` con razón escrita) es la salida honesta. |
| T-85-04-02 | Tampering | Suite de tests del repo | high | mitigate | D-12 con supresión explícita en el gate §4 (siempre opción 2) + criterio `git status --porcelain test/ \| grep -c '^??'` = 0 y `git log \| grep -c 'test(phase-'` = 0 en las tres tareas. §5 (auditor generador de tests) nunca se alcanza. Riesgo máximo del plan: el auditor operaría sobre código de v0.16. |
| T-85-04-03 | Tampering | Artefactos archivados de v0.16 (`MILESTONE-AUDIT.md`, `UAT.md`, `SECURITY.md`, `SUMMARY`) | medium | mitigate | D-15: son snapshots históricos. Criterios `git diff --exit-code` explícitos sobre `v0.16-MILESTONE-AUDIT.md`, `71-UAT.md`, `72-UAT.md` y `72-SECURITY.md`; `files_modified` acota el plan a los tres `VALIDATION.md`. |
| T-85-04-04 | Repudiation | El `skipped: 1` del UAT de 71 | medium | mitigate | Criterio de aceptación explícito: el fichero declara el skip y en ningún sitio lo cuenta como `pass`, reproduciendo la formulación que `STATE.md` ya usa (skip reconocido con fecha + mock compensatorio). Un skip maquillado es la versión sutil del `true` sin cita. |
| T-85-04-05 | Information Disclosure | Citas de evidencia en los `VALIDATION.md` | low | accept | Son paths de test del propio repo y conteos, dentro de `.planning/` del mismo repo. Sin secretos, sin tokens, sin credenciales de Plane/GitHub (los items de servicio real se citan por su `UAT.md`, no por sus credenciales). |
| T-85-04-SC | Tampering | Supply chain (npm) | low | accept | **Cero instalaciones.** Constraint LOCKED «cero deps npm nuevas»; `85-RESEARCH.md` §Package Legitimacy Audit devuelve tabla vacía. No procede checkpoint de legitimidad. |
</threat_model>

<verification>
- Los tres `VALIDATION.md` con `status: validated` + `nyquist_compliant: true`
- Los tres con `## Reconstruction Audit {fecha} (Phase 85 NYQ-02)` y sin el audit trail genérico del workflow
- `72-VALIDATION.md` sin ningún marcador de plantilla y con `node:test` como framework declarado
- `71-VALIDATION.md` sin ningún `TBD` y con el `skipped: 1` del UAT declarado tal cual
- `git status --porcelain test/ | grep -c '^??'` en 0 — cero ficheros de test nuevos (D-12)
- `git diff --exit-code` en 0 sobre `v0.16-MILESTONE-AUDIT.md`, `71-UAT.md`, `72-UAT.md` y `72-SECURITY.md` (D-15)
- Cero ejecuciones de `npm test` atribuibles a este plan; el SUMMARY lo declara explícitamente
</verification>

<success_criteria>
1. Phases 69, 71 y 72 tienen `VALIDATION.md` con `nyquist_compliant: true` citation-based, con la evidencia de la suite existente y **sin re-derivar** (NYQ-02).
2. `72-VALIDATION.md` deja de ser la plantilla literal: HYG-01..08 sobre 5 planes, con evidencia citada por dimensión.
3. Cada dimensión de cada mapa cita fichero + sección + resultado; ninguna fila verde carece de cita.
4. Cero tests generados, cero re-ejecución de la suite (D-12).
5. Los `MILESTONE-AUDIT.md`, `UAT.md` y `SECURITY.md` archivados de v0.16 quedan intactos (D-15).
6. El veredicto de cada fase se decidió contra su propia evidencia, sin herencia entre tareas ni entre bloques (D-17).
</success_criteria>

<output>
Create `.planning/phases/85-saneo-de-deuda-nyquist-retroactivo/85-04-SUMMARY.md` when done.
</output>
