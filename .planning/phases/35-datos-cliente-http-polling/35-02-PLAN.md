---
phase: 35-datos-cliente-http-polling
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/cli/dashboard/usePoll.js
  - test/dashboard-poll.test.js
autonomous: true
requirements: [TUI-05]
must_haves:
  truths:
    - "El poll es self-scheduling: el siguiente tick se programa SOLO tras resolver el actual (single-flight, ≤1 request en vuelo)"
    - "El intervalo hace backoff 2.5→5→10s cap ante fallos consecutivos y resetea a 2.5s al primer ok"
    - "El teardown del efecto limpia el timer y aborta el controller (no setState tras unmount)"
  artifacts:
    - path: "src/cli/dashboard/usePoll.js"
      provides: "Hook self-scheduling cancelable con clock/fetch inyectables (D-03/04/05/09)"
      contains: "export function usePoll"
    - path: "test/dashboard-poll.test.js"
      provides: "Tests single-flight, backoff, teardown con DI clock+fetch"
      contains: "maxInFlight"
  key_links:
    - from: "src/cli/dashboard/usePoll.js"
      to: "recursive setTimeout (NO setInterval)"
      via: "re-arma el timer SOLO tras await del tick"
      pattern: "setTimeout\\(tick"
---

<objective>
Crear el hook `usePoll` — loop de polling **self-scheduling** con `setTimeout` recursivo
(NUNCA `setInterval`, Anti-Pattern 3) que garantiza **single-flight** (≤1 request en vuelo,
D-03), **backoff** `2.5→5→10s` cap con reset a 2.5s al recuperar (D-04), **timeout de 5s** vía
`AbortController` re-creado cada tick (D-05) y **teardown limpio** (cancelled flag +
clearTimeout + abort, D-09/Pitfall 9). Clock y fn inyectables para tests herméticos (Pitfall 11).

Purpose: La capa de scheduling (timing puro) de TUI-05, desacoplada del render. Consume el `fn`
que `App` (Plan 03) envuelve sobre `fetchStatus` (Plan 01).
Output: `src/cli/dashboard/usePoll.js` (NUEVO) + `test/dashboard-poll.test.js` (NUEVO).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-datos-cliente-http-polling/35-CONTEXT.md
@.planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md
@.planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md
@.planning/phases/35-datos-cliente-http-polling/35-01-SUMMARY.md

<interfaces>
<!-- usePoll NO importa client.js: recibe `fn` por DI (el `fn` envuelve fetchStatus con AbortSignal). -->
<!-- Contrato del resultado que el `fn` resuelve (de Plan 01 client.js): { ok:true, data } | { ok:false, error } -->

<!-- Análogo del loop self-scheduling YA en producción (src/triggers/polling.js:478-575): -->
<!--   - tick() async: hace await del trabajo y re-arma `timer = clock.setTimeout(tick, intervalMs)` SOLO al final (línea 552-554) -->
<!--   - cancel flag: `if (stopped) return;` al entrar (línea 495) + en stop() (línea 571-574) -->
<!--   - clock DI (DEFAULT_CLOCK, líneas 96-100): { setTimeout, clearTimeout, now } -->
<!-- Constraint backoff (D-04): interval = result.ok ? baseMs : Math.min(interval*2, maxMs); baseMs=2500, maxMs=10000 -->
<!--   → secuencia esperada de intervals ante fallos: [2500, 5000, 10000, 10000, …], reset a 2500 al ok -->
<!-- Timeout (D-05): AbortController re-creado por tick + setTimeout(() => ac.abort(), 5000); el handle vive en el hook para abort-on-unmount (D-09) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Wave 0 de usePoll (single-flight, backoff, teardown) con DI clock+fetch</name>
  <files>test/dashboard-poll.test.js</files>
  <read_first>
    - src/triggers/polling.js (líneas 478-575: estructura tick recursivo + clock DI + cancel — análogo de loop a copiar; líneas 96-100 DEFAULT_CLOCK)
    - test/providers/github/client.test.js (líneas 52-88: patrón de fetch fake/spy a adaptar)
    - test/dashboard-render.test.js (líneas 21-26: constraint ink-testing-library@4 SIN waitUntilExit; runner node:test 32-36)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (sección test/dashboard-poll.test.js, líneas 230-248; "No Analog Found" líneas 308-314)
    - .planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md (test single-flight líneas 354-366; constraint harness líneas 329-335)
  </read_first>
  <behavior>
    - single-flight (TUI-05): `fn` lento (incrementa inFlight, await fake delay, decrementa) inyectado → avanzar el clock varios intervalos → assert `maxInFlight === 1` (nunca >1 request en vuelo); el siguiente `schedule` NO se invoca hasta resolver el `fn` actual
    - backoff sube (D-04): `fn` que siempre resuelve `{ok:false}` → capturar los `interval` pasados a la fake `schedule` → assert secuencia comienza [2500, 5000, 10000, 10000, …] (cap a 10000)
    - backoff resetea (D-04): `fn` que resuelve `{ok:false}` ×2 luego `{ok:true}` → tras el ok el siguiente `interval` vuelve a 2500
    - teardown (Pitfall 9/D-09): tras invocar el cleanup del efecto, spy verifica `clearTimeout` llamado y `abort()` del controller; ningún `onResult` se invoca tras `cancelled`
  </behavior>
  <action>
    Crear `test/dashboard-poll.test.js` con `import { describe, it } from 'node:test'` +
    `import assert from 'node:assert/strict'`. Ensamblar dos patrones (No Analog Found en PATTERNS.md):
    la DI de `fn` (un fake async que devuelve `{ok}`) del estilo `makeFetch` de
    `test/providers/github/client.test.js`, y un **fake clock/schedule** del estilo `DEFAULT_CLOCK`
    de `src/triggers/polling.js:96-100`. El fake `schedule` debe (a) registrar cada `interval` recibido
    en un array para las aserciones de backoff y (b) permitir disparar el callback manualmente para
    avanzar el loop de forma determinista (sin timers reales). Importar `{ usePoll } from
    '../src/cli/dashboard/usePoll.js'`.

    IMPORTANTE (constraint verificado): `ink-testing-library@4` NO expone `waitUntilExit()`. Para testear
    el hook de forma determinista SIN React, el implementador debe poder ejercitar la mecánica del loop
    vía las opciones inyectables de `usePoll` (clock/schedule/cancel + el `fn`). Si la firma del hook
    no permite drivear el loop sin un host React, extraer el scheduler a una función pura testeable
    (decisión de Discretion del implementador en Task 2). Escribir un `it(...)` por cada comportamiento
    del bloque <behavior>: single-flight (`maxInFlight === 1`), backoff sube (`[2500,5000,10000,10000]`),
    backoff resetea (vuelve a `2500` tras ok), teardown (spy de `clearTimeout` + `abort`). Estado Wave 0:
    ROJO hasta Task 2.
  </action>
  <verify>
    <automated>node --test test/dashboard-poll.test.js 2>&1 | grep -qE "Cannot find module|ERR_MODULE_NOT_FOUND|fail" && echo "RED-as-expected (usePoll.js missing)" || echo "check"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-poll.test.js` existe e importa `usePoll` (o el scheduler extraído) desde `../src/cli/dashboard/usePoll.js`
    - Contiene un `it(...)` para cada uno de: single-flight, backoff-sube, backoff-resetea, teardown (`grep -c "it(" test/dashboard-poll.test.js` ≥ 4)
    - El test de single-flight asserta `maxInFlight === 1` (`grep -q "maxInFlight" test/dashboard-poll.test.js`)
    - El test de backoff asserta la secuencia `2500`/`5000`/`10000` y el reset a `2500`
    - NO usa `setInterval` ni `await sleep(...)` con timers reales (`grep -c "setInterval" test/dashboard-poll.test.js` == 0)
    - Antes de Task 2 el test falla por módulo ausente (RED esperado)
  </acceptance_criteria>
  <done>El archivo de test cubre single-flight, backoff (sube+reset) y teardown con DI clock+fetch; ROJO porque usePoll.js no existe aún.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar usePoll (recursive setTimeout + single-flight + backoff + teardown) hasta verde</name>
  <files>src/cli/dashboard/usePoll.js</files>
  <read_first>
    - src/triggers/polling.js (líneas 478-575: tick recursivo + cancel flag + re-arma timer SOLO al final; 96-100 clock shape)
    - src/cli/dashboard/App.js (líneas 1-26: header @ts-check + import desde 'react'; useInput/useStdin host)
    - test/dashboard-poll.test.js (el test de Task 1 — el contrato a satisfacer)
    - .planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md (Pattern 2, líneas 211-246; Discretion Open Question 2 líneas 415-418)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (sección usePoll.js, líneas 76-133)
  </read_first>
  <action>
    Crear `src/cli/dashboard/usePoll.js`. Primera línea `// @ts-check` + bloque de comentario-cabecera
    documentando responsabilidad (hook de scheduling, pure-ish, clock inyectable), las decisiones
    D-03/D-04/D-05/D-09 y la invariante de color-isolation (NO picocolors). Importar
    `{ useEffect, useRef } from 'react'`. Constantes `BASE_MS = 2500`, `MAX_MS = 10000` (D-03/D-04).

    Exportar `usePoll(fn, onResult, deps, opts)` donde `opts` permite inyectar `baseMs`/`maxMs` y el
    clock/`schedule`/`cancel` (default `setTimeout`/`clearTimeout`; ver el shape Clock de
    `src/triggers/polling.js:96-100`). Guardar `fn` y `onResult` en `useRef` (no re-armar el efecto en
    cada render). Dentro de `useEffect`: variables locales `cancelled = false`, `timer`, `interval = baseMs`,
    `ac`. Un `tick` async que: (1) `if (cancelled) return;` (cf. polling.js:495); (2) crea
    `ac = new AbortController()` (re-creado cada tick, D-05) y `const to = schedule(() => ac.abort(), 5000)`;
    (3) `const result = await savedFn.current(ac.signal);` (el `await` ANTES de re-armar = single-flight,
    D-09); (4) `cancel(to)`; (5) `if (cancelled) return;`; (6) `savedOn.current(result);`; (7)
    `interval = result.ok ? baseMs : Math.min(interval * 2, maxMs);` (backoff/reset, D-04); (8)
    `timer = schedule(tick, interval);` (re-arma SOLO ahora, cf. polling.js:552-554). Lanzar `tick()` una
    vez. El cleanup `return () => { cancelled = true; cancel(timer); ac?.abort(); };` (D-09, Pitfall 9).
    NUNCA usar `setInterval` (Anti-Pattern 3). NO importar `picocolors`. Discretion (Open Question 2): el
    timing del backoff (interval actual) vive en el hook; el keep-last-good/connection state vive en App
    (Plan 03) — el hook solo reporta cada `{ok}` vía `onResult`.
  </action>
  <verify>
    <automated>node --test test/dashboard-poll.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/usePoll.js` existe y exporta `usePoll` (`grep -q "export function usePoll" src/cli/dashboard/usePoll.js`)
    - Usa `setTimeout`/`schedule` recursivo y NO `setInterval` (`grep -v '^//' src/cli/dashboard/usePoll.js | grep -c setInterval` == 0)
    - El `await savedFn` ocurre ANTES de re-armar el timer (single-flight); el `tick` re-arma con `schedule(tick, interval)` al final
    - El backoff usa `Math.min(interval * 2, maxMs)` y resetea a `baseMs` cuando `result.ok`
    - El cleanup del `useEffect` setea `cancelled = true`, llama `cancel(timer)` y `ac?.abort()`
    - Crea un `AbortController` nuevo por tick con `setTimeout(abort, 5000)` (D-05)
    - Ningún `import` de `picocolors` (`grep -v '^//' src/cli/dashboard/usePoll.js | grep -c picocolors` == 0)
    - `node --test test/dashboard-poll.test.js` pasa (GREEN: single-flight, backoff sube/resetea, teardown)
    - `node --test test/format-isolation.test.js` verde
  </acceptance_criteria>
  <done>usePoll implementado con recursive setTimeout, single-flight, backoff (2.5→5→10 cap + reset) y teardown; tests de poll verdes; sin setInterval; sin picocolors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usePoll → cliente (vía `fn`) | El hook no toca la red directamente: recibe un `fn` que envuelve `fetchStatus`. La única superficie es el timing (no apilar requests) y el teardown (no fugar timers/fetch tras unmount). Sin input de usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Denial of Service (self-inflicted, resource exhaustion) | `usePoll` scheduling | mitigate | Single-flight estricto (recursive `setTimeout` con `await` antes de re-armar) + `AbortController` 5s por tick: un `/status` lento (endpoint caro: `listPendingTasks` TTL + `cmux.listWorkspaces`) NO apila requests ni martillea el puerto. Backoff con cap 10s ante server caído. Cubierto por el test single-flight (`maxInFlight === 1`) y backoff. |
| T-35-04 | Denial of Service (fetch zombi / setState-tras-unmount) | cleanup de `useEffect` | mitigate | `cancelled` flag + `clearTimeout(timer)` + `ac.abort()` en el cleanup (D-09, Pitfall 9): al desmontar (`q`, o el remount del attach en Phase 37) no queda timer activo ni fetch pegándole al server. Cubierto por el test teardown (spy de `clearTimeout` + `abort`). |
| T-35-SC | Tampering | npm installs | accept | Esta fase NO instala paquetes. Ver RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- `node --test test/dashboard-poll.test.js` — single-flight (`maxInFlight === 1`), backoff sube/resetea, teardown verdes (TUI-05)
- `node --test test/format-isolation.test.js` — color-isolation verde (cubre usePoll.js automáticamente)
</verification>

<success_criteria>
- El poll es single-flight (≤1 request en vuelo) y self-scheduling con recursive `setTimeout` — nunca `setInterval`.
- El backoff sube `2.5→5→10s` cap ante fallos y resetea a 2.5s al primer ok.
- El teardown limpia timer + aborta el controller — sin fetch zombi ni setState tras unmount.
- Sin `picocolors`; suite de poll + color-isolation verdes.
</success_criteria>

<output>
Crear `.planning/phases/35-datos-cliente-http-polling/35-02-SUMMARY.md` al terminar.
</output>
