---
phase: 35-datos-cliente-http-polling
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/index.js
  - test/dashboard-baseurl.test.js
autonomous: true
requirements: [TUI-06]
must_haves:
  truths:
    - "runDashboard resuelve el baseUrl sin TypeError aunque loadConfig() devuelva un config sin la clave `server` (v1 migrado)"
    - "Cuando server.port falta, el baseUrl usa el default conocido 9090"
    - "El override --url sigue teniendo prioridad sobre el default config-driven"
    - "Implementa D-10: cierra el advisory WR-01 de 34-REVIEW.md (guard de server.port undefined con fallback al default conocido 9090)"
  artifacts:
    - path: "src/cli/dashboard/index.js"
      provides: "Resolución de baseUrl con guard WR-01 (optional chaining + fallback a DEFAULT_CONFIG.server.port)"
      contains: "server?.port"
    - path: "test/dashboard-baseurl.test.js"
      provides: "Test de la resolución de baseUrl con loadConfig fake (config sin `server`)"
      contains: "9090"
  key_links:
    - from: "src/cli/dashboard/index.js"
      to: "DEFAULT_CONFIG.server.port (src/config.js)"
      via: "fallback cuando cfg.server?.port es undefined"
      pattern: "DEFAULT_CONFIG\\.server\\.port"
---

<objective>
Cerrar el advisory **WR-01** de `34-REVIEW.md` (D-10): `src/cli/dashboard/index.js:49` hace
`loadConfig().server.port` **sin guardia**. `migrateConfig` (`src/config.js:82-102`) reconstruye el
config **sin** la clave `server`, así que un usuario con config v1 migrado dispara un `TypeError`
al resolver el `baseUrl`. El fix es optional-chaining + fallback al default conocido
`DEFAULT_CONFIG.server.port` (9090).

Purpose: Hardening arrastrado de Phase 34 — Phase 35 es donde el cliente consume el `baseUrl`, así
que el guard debe quedar resuelto aquí antes de que el polling lo use. Independiente del slice de
datos (toca solo `index.js`), corre en paralelo a Plan 01.
Output: `src/cli/dashboard/index.js` (MODIFICADO) + `test/dashboard-baseurl.test.js` (NUEVO).

NOTA VERIFICADA EN PLANNING (resuelve Assumption A1): `DEFAULT_CONFIG` **ya está exportado** de
`src/config.js` (`export { …, DEFAULT_CONFIG }` en la línea 228). NO hace falta tocar `config.js`:
el fix importa `DEFAULT_CONFIG` directamente. El default `server.port: 9090` está en `src/config.js:62-66`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-datos-cliente-http-polling/35-CONTEXT.md
@.planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md
@.planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md

<interfaces>
<!-- Línea actual a reemplazar (VERIFICADO src/cli/dashboard/index.js:48-49): -->
<!--   const { loadConfig } = await import('../../config.js'); -->
<!--   const baseUrl = url ?? `http://localhost:${loadConfig().server.port}`;  ← TypeError si config v1 migrado -->

<!-- Causa raíz (VERIFICADO): migrateConfig (src/config.js:82-102) reconstruye el config SIN la clave `server` -->
<!--   → loadConfig().server es undefined → .port lanza TypeError. -->

<!-- DEFAULT_CONFIG exportado (VERIFICADO src/config.js:228) → import directo, sin tocar config.js. -->
<!--   DEFAULT_CONFIG.server.port == 9090 (VERIFICADO src/config.js:62-66). -->

<!-- runDashboard ya inyecta deps para tests (src/cli/dashboard/index.js:37-38): { stdout, stdin, url }. -->
<!--   El guard non-TTY (42-45) hace process.exit(1) si stdout/stdin no son TTY — el test debe inyectar -->
<!--   stdout/stdin con isTTY:true para llegar a la resolución de baseUrl, O el implementador extrae la -->
<!--   resolución de baseUrl a un helper puro testeable (Discretion). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Wave 0 de la resolución de baseUrl con guard WR-01 (config sin `server`)</name>
  <files>test/dashboard-baseurl.test.js</files>
  <read_first>
    - src/cli/dashboard/index.js (archivo COMPLETO, 75 líneas: runDashboard, deps inyectables 37-38, guard non-TTY 42-45, resolución baseUrl 47-49)
    - test/dashboard-non-tty.test.js (patrón de test de runDashboard con deps inyectadas — cómo se ejercita el subcomando sin TTY real)
    - src/config.js (líneas 62-66 default server.port:9090; 82-102 migrateConfig sin `server`; 228 export DEFAULT_CONFIG)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (sección index.js líneas 176-204)
    - .planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md (fix WR-01 líneas 339-351)
  </read_first>
  <behavior>
    - config v1 migrado (sin `server`): un `loadConfig` fake que devuelve `{ provider:'…' }` (SIN `server`) → la resolución de baseUrl NO lanza TypeError y produce `http://localhost:9090` (fallback al default)
    - config normal (con `server.port`): `loadConfig` fake devuelve `{ server:{ port: 7777 } }` → baseUrl `http://localhost:7777`
    - override --url: `deps.url = 'http://example:1234'` → baseUrl `http://example:1234` (prioridad sobre el config, aunque `server` falte)
  </behavior>
  <action>
    Crear `test/dashboard-baseurl.test.js` con runner `node:test` + `node:assert/strict`. El objetivo es
    ejercitar la resolución de baseUrl con un `loadConfig` que devuelve un config SIN la clave `server`
    (simula v1 migrado) y assertar que NO lanza TypeError y cae al default 9090.

    Decisión de testabilidad (Discretion, alinear con Task 2): la resolución de baseUrl vive hoy inline en
    `runDashboard` tras un guard non-TTY que hace `process.exit(1)`. Hay dos vías herméticas — el
    implementador elige UNA y la Task 2 la respeta: (a) extraer la resolución a un helper puro exportado
    `resolveBaseUrl({ url, loadConfig })` y testearlo directamente con un `loadConfig` fake; (b) testear
    `runDashboard` inyectando `deps.stdout`/`deps.stdin` con `isTTY:true` y un `loadConfig` inyectable
    (requiere que Task 2 añada `loadConfig` a los deps inyectables). La vía (a) es más simple y evita
    arrancar ink — recomendada. Escribir un `it(...)` por cada escenario del <behavior>: v1-migrado→9090,
    config-normal→7777, override-url→prioridad. Estado Wave 0: ROJO hasta Task 2.
  </action>
  <verify>
    <automated>node --test test/dashboard-baseurl.test.js 2>&1 | grep -qE "Cannot find module|ERR_MODULE_NOT_FOUND|fail|not ok" && echo "RED-as-expected" || echo "check"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-baseurl.test.js` existe con runner `node:test` + `node:assert/strict`
    - Contiene un `it(...)` para cada uno de: v1-migrado→9090, config-normal→7777, override-url (`grep -c "it(" test/dashboard-baseurl.test.js` ≥ 3)
    - El test v1-migrado usa un `loadConfig` fake SIN la clave `server` y asserta `http://localhost:9090` (`grep -q "9090" test/dashboard-baseurl.test.js`)
    - El test NO depende de un server real ni de un TTY real (config/loadConfig inyectado)
    - Antes de Task 2 el test está ROJO
  </acceptance_criteria>
  <done>El test cubre los 3 escenarios de resolución de baseUrl (v1-migrado→9090, normal, override) con loadConfig fake; ROJO hasta el fix de Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aplicar el guard WR-01 (optional chaining + fallback a DEFAULT_CONFIG.server.port) hasta verde</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - src/cli/dashboard/index.js (líneas 47-49 a parchear; preservar el lazy import 48 y todo el resto del lifecycle 51-74)
    - src/config.js (línea 228 export DEFAULT_CONFIG; 62-66 server.port:9090)
    - test/dashboard-baseurl.test.js (el test de Task 1 — contrato a satisfacer)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (fix pattern líneas 192-204)
  </read_first>
  <action>
    Modificar `src/cli/dashboard/index.js` aplicando el fix WR-01 (D-10). Reemplazar las líneas 48-49
    actuales (`const { loadConfig } = await import('../../config.js');` +
    `const baseUrl = url ?? `http://localhost:${loadConfig().server.port}`;`) por: importar también
    `DEFAULT_CONFIG` en el destructuring del lazy import (`const { loadConfig, DEFAULT_CONFIG } = await
    import('../../config.js');` — VERIFICADO que `DEFAULT_CONFIG` se exporta, A1 resuelta); luego
    `const cfg = loadConfig();`, `const port = cfg.server?.port ?? DEFAULT_CONFIG.server.port;`,
    `const baseUrl = url ?? `http://localhost:${port}`;`. El optional chaining `cfg.server?.port` evita el
    TypeError cuando `migrateConfig` omitió la clave `server`; el fallback usa el default conocido 9090.
    Preservar el lazy import (no cargar config en el arranque del CLI), el guard non-TTY (42-45), el render
    de ink (53-57) y todo el lifecycle (SIGTERM/exit, 59-73).

    Si la Task 1 eligió la vía (a) (helper puro `resolveBaseUrl`): extraer la lógica de resolución
    (url override + cfg.server?.port + fallback) a una función exportada `resolveBaseUrl({ url, loadConfig
    = realLoadConfig })` que `runDashboard` invoca, y que el test importa directamente. Si eligió la vía
    (b): añadir `loadConfig` a los deps inyectables de `runDashboard`. Respetar la vía que fijó Task 1.
    Actualizar el comentario de la línea 47 (resolución de baseUrl, D-05) para mencionar el guard WR-01/D-10.
    NO importar `picocolors`.
  </action>
  <verify>
    <automated>node --test test/dashboard-baseurl.test.js test/dashboard-non-tty.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/index.js` usa optional chaining sobre `server` (`grep -q "server?.port" src/cli/dashboard/index.js`)
    - El fallback referencia `DEFAULT_CONFIG.server.port` (`grep -q "DEFAULT_CONFIG.server.port" src/cli/dashboard/index.js`)
    - El lazy import destructura `DEFAULT_CONFIG` junto a `loadConfig`
    - Ya NO existe el acceso sin guardia `loadConfig().server.port` (`grep -c "loadConfig().server.port" src/cli/dashboard/index.js` == 0)
    - El override `url ??` se preserva con prioridad
    - Ningún `import` de `picocolors` (`grep -v '^//' src/cli/dashboard/index.js | grep -c picocolors` == 0)
    - `node --test test/dashboard-baseurl.test.js` pasa (v1-migrado→9090 sin TypeError, normal, override) — GREEN
    - `node --test test/dashboard-non-tty.test.js` sigue verde (guard non-TTY de Phase 34 intacto)
    - `node --test test/format-isolation.test.js` verde
  </acceptance_criteria>
  <done>WR-01 cerrado: la resolución de baseUrl usa cfg.server?.port ?? DEFAULT_CONFIG.server.port; config v1 migrado → 9090 sin TypeError; non-TTY + color-isolation verdes; override --url preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config en disco → runDashboard | `loadConfig()` lee `~/.kodo/config.json` (puede ser un config v1 migrado sin `server`). El `--url` lo provee el operador (flag CLI). El `baseUrl` resultante apunta a un destino HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08 | Denial of Service (crash de arranque) | resolución de baseUrl en `runDashboard` | mitigate | Optional chaining `cfg.server?.port ?? DEFAULT_CONFIG.server.port`: un config v1 migrado (sin `server`) ya NO dispara TypeError al arrancar el dashboard. Cubierto por el test "v1-migrado→9090" (cierra WR-01/D-10). |
| T-35-09 | Spoofing / SSRF (--url a destino arbitrario) | `deps.url` override | accept | El `--url` es un flag CLI que el operador local controla deliberadamente (apunta su propio dashboard a otro server kodo). El cliente es read-only (`GET /status`), sin auth ni secretos en el path; no reenvía credenciales ni hace requests derivados a la URL. Riesgo SSRF inexistente: no hay input remoto que controle el destino, solo el operador. Superficie local. |
| T-35-SC | Tampering | npm installs | accept | Esta fase NO instala paquetes. Ver RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- `node --test test/dashboard-baseurl.test.js` — v1-migrado→9090 sin TypeError, config normal, override --url (D-10/WR-01)
- `node --test test/dashboard-non-tty.test.js` — guard non-TTY de Phase 34 intacto
- `node --test test/format-isolation.test.js` — color-isolation verde
</verification>

<success_criteria>
- `runDashboard` resuelve el baseUrl sin TypeError aunque el config carezca de la clave `server` (config v1 migrado), cayendo al default 9090.
- El override `--url` mantiene prioridad.
- WR-01 cerrado; non-TTY guard y color-isolation preservados.
</success_criteria>

<output>
Crear `.planning/phases/35-datos-cliente-http-polling/35-04-SUMMARY.md` al terminar.
</output>
