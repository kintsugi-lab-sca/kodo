---
phase: 35-datos-cliente-http-polling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/client.js
  - test/dashboard-client.test.js
autonomous: true
requirements: [TUI-05, TUI-06]
must_haves:
  truths:
    - "fetchStatus devuelve {ok:true,data} con un payload /status válido"
    - "fetchStatus devuelve {ok:false,error} ante ECONNREFUSED, HTTP no-ok, JSON corrupto y shape inválida — nunca lanza"
    - "El test de cliente verifica los 5 escenarios (ok / HTTP no-ok / JSON corrupto / throw / bad shape)"
  artifacts:
    - path: "src/cli/dashboard/client.js"
      provides: "fetchStatus(baseUrl, fetchFn?, signal?) puro never-throws"
      contains: "export async function fetchStatus"
    - path: "test/dashboard-client.test.js"
      provides: "5 escenarios del discriminante {ok}"
      contains: "bad shape"
  key_links:
    - from: "src/cli/dashboard/client.js"
      to: "GET {baseUrl}/status"
      via: "fetchFn(`${baseUrl}/status`, { signal })"
      pattern: "fetchFn\\(`\\$\\{baseUrl\\}/status`"
---

<objective>
Crear el cliente HTTP puro `fetchStatus` (data layer, React-free) que consume `GET /status`
del server kodo y **nunca lanza**: colapsa ECONNREFUSED, HTTP no-ok, JSON corrupto y shape
inválida al discriminante `{ok:true,data}` / `{ok:false,error}` (D-07, Pattern 1). El invariante
"no crash" de TUI-06 es **estructural aquí**, no en los componentes React.

Purpose: Establecer el contrato de datos sobre el que `usePoll` (Plan 02) y `App` (Plan 03)
construyen. Es la capa más baja del slice de datos de Phase 35.
Output: `src/cli/dashboard/client.js` (NUEVO) + `test/dashboard-client.test.js` (NUEVO).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-datos-cliente-http-polling/35-CONTEXT.md
@.planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md
@.planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md

<interfaces>
<!-- Contrato del payload GET /status (VERIFICADO src/server.js:397-411) -->
<!-- { sessions: [{…, alive, elapsed_min}], count: enriched.length, pending, pending_count, history, metrics, uptime } -->
<!-- Para "N sessions": data.count (== enriched.length) o data.sessions.length (D-01 los acepta equivalentes). -->
<!-- src/server.js es READ ONLY — cero endpoints nuevos (constraint dura del milestone). -->

<!-- Forma del fetch inyectable a copiar (src/providers/github/client.js:76,85): opts.fetch || globalThis.fetch -->
<!-- DIVERGENCIA INTENCIONAL a documentar: los clients existentes LANZAN -->
<!-- (src/providers/plane/client.js:72 `throw new Error`). Este cliente hace lo CONTRARIO -->
<!-- (never-throws {ok}). Copiar la FORMA del fetch + lectura de res.ok/res.status/res.json(), -->
<!-- NO la propagación de excepciones. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Wave 0 de fetchStatus (5 escenarios del discriminante {ok})</name>
  <files>test/dashboard-client.test.js</files>
  <read_first>
    - test/providers/github/client.test.js (líneas 32-88: leak guard + helpers makeFetch/makeSpyFetch — patrón exacto a copiar)
    - test/dashboard-render.test.js (runner node:test + node:assert/strict, líneas 32-36)
    - .planning/phases/35-datos-cliente-http-polling/35-PATTERNS.md (sección test/dashboard-client.test.js, líneas 208-227)
  </read_first>
  <behavior>
    - ok: fetchFn resuelve Response-like { status:200, ok:true, json: async () => ({ sessions:[{}], count:1 }) } → resultado { ok:true, data: { count:1, sessions:[{}] } }
    - HTTP no-ok: { status:500, ok:false } → { ok:false } con error que contiene "500" (D-07)
    - JSON corrupto: json() lanza SyntaxError → { ok:false } (NO propaga el throw, Pitfall 12)
    - throw (ECONNREFUSED): fetchFn lanza Error('ECONNREFUSED') → { ok:false } con error que contiene "ECONNREFUSED"
    - bad shape: { status:200, ok:true, json: async () => ({ count:3 }) } (sin sessions array) → { ok:false, error:'bad shape' }
  </behavior>
  <action>
    Crear `test/dashboard-client.test.js` con `import { describe, it, before, after } from 'node:test'`
    y `import assert from 'node:assert/strict'`. Copiar el fetch-leak guard de
    `test/providers/github/client.test.js:32-43` (reemplaza `globalThis.fetch` por un thrower en
    `before()`, restaura en `after()`) para que cualquier test que olvide inyectar el fake falle loud.
    Copiar el helper `makeFetch(scenario)` de las líneas 52-70 del mismo análogo, adaptado: el
    Response-like debe exponer `ok` (bool), `status` (number) y `json()` (async, puede lanzar).
    Importar `{ fetchStatus } from '../src/cli/dashboard/client.js'`. Escribir un `it(...)` por cada
    uno de los 5 escenarios del bloque <behavior>, inyectando el `fetchFn` fake como 2º argumento de
    `fetchStatus(baseUrl, fakeFetch)`. Para el escenario JSON corrupto, el fake `json()` debe
    `throw new SyntaxError('Unexpected token')`. Para ECONNREFUSED, el `fetchFn` mismo lanza. Aserciones
    con `assert.equal(result.ok, ...)` y `assert.match(result.error, /…/)`. Estado Wave 0: ROJO hasta
    que la Task 2 cree `client.js`.
  </action>
  <verify>
    <automated>node --test test/dashboard-client.test.js 2>&1 | grep -qE "Cannot find module|ERR_MODULE_NOT_FOUND|fail" && echo "RED-as-expected (client.js missing)" || echo "check"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard-client.test.js` existe con 5 bloques `it(...)` (uno por escenario del <behavior>)
    - El archivo importa `fetchStatus` desde `../src/cli/dashboard/client.js`
    - El leak guard reemplaza `globalThis.fetch` por un thrower en `before()` y lo restaura en `after()`
    - `grep -c "it(" test/dashboard-client.test.js` ≥ 5
    - Antes de Task 2, `node --test test/dashboard-client.test.js` falla por módulo ausente (RED esperado)
  </acceptance_criteria>
  <done>El archivo de test existe, cubre los 5 escenarios e importa fetchStatus; está ROJO porque client.js aún no existe (Nyquist gate).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar fetchStatus (never-throws {ok}) hasta verde</name>
  <files>src/cli/dashboard/client.js</files>
  <read_first>
    - src/providers/github/client.js (líneas 1-31 header @ts-check + JSDoc; 76,85 fetch inyectable)
    - src/providers/plane/client.js (líneas 46-90: lectura res.ok/res.status/res.json — CONTRASTE: lanzan, NO copiar el throw)
    - src/cli/dashboard/App.js (líneas 1-23: estilo de comentario-cabecera de src/cli/dashboard/**)
    - test/dashboard-client.test.js (el test recién creado en Task 1 — el contrato a satisfacer)
    - .planning/phases/35-datos-cliente-http-polling/35-RESEARCH.md (Pattern 1, líneas 189-209)
  </read_first>
  <action>
    Crear `src/cli/dashboard/client.js`. Primera línea `// @ts-check`, seguida de un bloque de
    comentario-cabecera (estilo `src/cli/dashboard/App.js:1-23`) que documente: responsabilidad
    (data layer puro, React-free), la decisión D-07/Pattern 1 (never-throws `{ok}`), la **divergencia
    intencional** respecto a los clients existentes que sí lanzan (`src/providers/plane/client.js:72`),
    y la invariante de color-isolation (NO `picocolors`). Exportar
    `export async function fetchStatus(baseUrl, fetchFn = globalThis.fetch, signal)`. Cuerpo dentro de
    un `try/catch`: (1) `const res = await fetchFn(`${baseUrl}/status`, { signal });`
    (2) `if (!res.ok) return { ok: false, error: `HTTP ${res.status}` };` (D-07);
    (3) `const data = await res.json();` (puede lanzar JSON corrupto → cae al catch, Pitfall 12);
    (4) `if (!Array.isArray(data.sessions)) return { ok: false, error: 'bad shape' };` (validación
    mínima de shape, Discretion); (5) `return { ok: true, data };`. El `catch (err)` devuelve
    `{ ok: false, error: err.message }` (cubre ECONNREFUSED / abort / parse). NO importar `picocolors`
    ni ningún módulo de color. NO añadir `fetchComments`/`fetchLogs` (Deferred a Phases 36/38, YAGNI).
  </action>
  <verify>
    <automated>node --test test/dashboard-client.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/dashboard/client.js` existe y exporta `fetchStatus` (`grep -q "export async function fetchStatus" src/cli/dashboard/client.js`)
    - El cuerpo está envuelto en `try/catch` y el `catch` devuelve `{ ok: false, error: ... }`
    - La validación de shape usa `Array.isArray(data.sessions)` y devuelve `error: 'bad shape'`
    - Ningún `import`/`require` de `picocolors` en el archivo (`grep -v '^//' src/cli/dashboard/client.js | grep -c picocolors` == 0)
    - El archivo NO contiene `fetchComments` ni `fetchLogs` (Deferred)
    - `node --test test/dashboard-client.test.js` pasa los 5 tests (GREEN)
    - `node --test test/format-isolation.test.js` sigue verde (walker color-isolation cubre el archivo nuevo automáticamente)
  </acceptance_criteria>
  <done>fetchStatus implementado never-throws; los 5 tests pasan; format-isolation verde; sin picocolors; sin fetchComments/fetchLogs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client.js → kodo server (localhost) | El TUI lee JSON de `GET /status` sin auth; el server es local y read-only. Entrada no confiable: el cuerpo de la respuesta (puede ser JSON corrupto, HTML de error 500, o body parcial). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Denial of Service (crash) | `fetchStatus` parse de `res.json()` | mitigate | `try/catch` alrededor de `res.json()` + validación `Array.isArray(data.sessions)`; JSON corrupto/parcial/HTML → `{ok:false}`, jamás un throw que llegue a React (Pitfall 12, D-07). Cubierto por los escenarios "JSON corrupto" y "bad shape" del test. |
| T-35-02 | Information Disclosure | `error` en el resultado `{ok:false}` | accept | El `error` es `err.message`/`HTTP <status>` sobre un endpoint localhost sin secretos; no se loggea a scrollback (ink posee la pantalla). `baseUrl` apunta a localhost. Superficie local read-only. |
| T-35-SC | Tampering | npm installs | accept | Esta fase NO instala paquetes (todo el stack instalado/auditado en Phase 34; `fetch` built-in Node 20+). Ver RESEARCH §"Package Legitimacy Audit": N/A. |
</threat_model>

<verification>
- `node --test test/dashboard-client.test.js` — 5 escenarios verdes (TUI-05 contrato + TUI-06 never-throws/JSON corrupto)
- `node --test test/format-isolation.test.js` — walker color-isolation verde (cubre client.js automáticamente, Open Question 3)
</verification>

<success_criteria>
- `fetchStatus` devuelve `{ok:true,data}` ante payload válido y `{ok:false,error}` ante los 4 modos de fallo (HTTP no-ok, JSON corrupto, throw, bad shape) — never-throws.
- El cliente es React-free, sin `picocolors`, sin `fetchComments`/`fetchLogs`.
- Suite de cliente + color-isolation verdes.
</success_criteria>

<output>
Crear `.planning/phases/35-datos-cliente-http-polling/35-01-SUMMARY.md` al terminar.
</output>
