---
phase: 57-orquestador-asistido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/kodo-orchestrate/skill.md
  - src/orchestrator/prompt.md
autonomous: true
requirements: [ORCH-01]

must_haves:
  truths:
    - "D-05: El skill kodo-orchestrate contiene una sección §'Adopción asistida (sesión → tarea)' (fuente canónica) que instruye al orquestador a derivar un título inteligente, confirmarlo con el operador y shellear kodo adopt --title."
    - "D-01: Las coordenadas de la sesión (workspaceRef/cwd/sessionId) son input explícito — sin auto-discovery; el orquestador rellena desde cat ~/.kodo/state.json, con escape-hatch al dashboard (tecla a) y NUNCA llama a cmux directamente."
    - "D-02: El título inteligente lo deriva el LLM del contexto real (basename(cwd) + git log --oneline -N + transcript opcional), no el basename(cwd) determinista del dashboard."
    - "El mandato de invocación shell-segura (charset seguro + título como UN argumento entre comillas simples + summarizar, no copiar, los subjects de commit) está presente EN AMBOS archivos (skill.md y prompt.md), no solo en el skill."
    - "D-03: El paso de confirmación humana (proponer título + proyecto destino y ESPERAR aprobación/edición; nunca crear silenciosamente) está presente en el skill."
    - "D-04: El saneo del título lo hace el núcleo (sanitizeAdoptionData, BIDIR-08) — el orquestador NO lo duplica; solo se pasa --title esta fase (--description diferido a BIDIR-F2)."
    - "D-05: src/orchestrator/prompt.md contiene un espejo condensado de la sección de adopción colocado en el cuerpo always-on, ANTES del marcador <!-- BEGIN reporting -->; src/orchestrator/launch.js queda intacto."
    - "No se añadió lógica de negocio nueva: src/orchestrator/launch.js, src/adopt.js, src/cli/adopt.js, src/cli.js quedan byte-idénticos (git diff vacío para esos paths)."
    - "La suite de invariantes pasa: node --test test/prompt.test.js test/skill-sync.test.js test/orchestrator-launch-isolation.test.js sale con exit 0."
    - "Decisions covered: D-01, D-02, D-03, D-04, D-05 (see 57-CONTEXT.md)."
  artifacts:
    - path: ".claude/skills/kodo-orchestrate/skill.md"
      provides: "Sección canónica §'Adopción asistida (sesión → tarea)' con flujo numerado + mandato shell-seguro + exit codes de kodo adopt"
      contains: "Adopción asistida"
    - path: "src/orchestrator/prompt.md"
      provides: "Espejo condensado de adopción en el cuerpo always-on + cross-ref al skill"
      contains: "Adopción asistida"
  key_links:
    - from: ".claude/skills/kodo-orchestrate/skill.md §Adopción asistida"
      to: "kodo adopt --title '<derived>'"
      via: "instrucción de shell-out con título single-quoted literal"
      pattern: "kodo adopt.*--title"
    - from: "src/orchestrator/prompt.md §Adopción asistida"
      to: ".claude/skills/kodo-orchestrate/skill.md §Adopción asistida"
      via: "cross-ref 'Fuente canonical extendida'"
      pattern: "skill"
    - from: "src/orchestrator/prompt.md §Adopción asistida"
      to: "cuerpo always-on (antes del reporting gate)"
      via: "colocación antes de <!-- BEGIN reporting -->"
      pattern: "Adopción asistida"
---

<objective>
Añadir el tercer consumidor de la fontanería de adopción: el **orquestador** (el único carril con LLM). Dada una sesión `claude` ad-hoc con coordenadas explícitas, el orquestador deriva un **título inteligente** del contexto real (cwd / `git log` / transcript opcional), lo confirma con el operador, y shellea el mismo `kodo adopt --title '<derived>'` de forma **shell-segura**.

El deliverable es **prosa de instrucción** en dos `.md`: la sección canónica en el skill `kodo-orchestrate` + un espejo condensado en el fallback `src/orchestrator/prompt.md`. CERO lógica de negocio nueva (SC3): el núcleo determinista 0-token (`adoptSession` / `kodo adopt` / `sanitizeAdoptionData`) y `src/orchestrator/launch.js` quedan intactos.

Purpose: ORCH-01 — el valor del orquestador es el título derivado por LLM (mejor que el `basename(cwd)` determinista del dashboard), no descubrir surfaces. El LLM vive estrictamente en el consumidor; el carril 0-token del núcleo se preserva.
Output: edición de `.claude/skills/kodo-orchestrate/skill.md` (nueva sección) + `src/orchestrator/prompt.md` (espejo condensado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-orquestador-asistido/57-CONTEXT.md
@.planning/phases/57-orquestador-asistido/57-RESEARCH.md
@.planning/phases/57-orquestador-asistido/57-PATTERNS.md
@.planning/phases/57-orquestador-asistido/57-VALIDATION.md

<interfaces>
<!-- Contratos que el orquestador CONSUME (NO se tocan — referencia, no edición). -->

Flags exactas de `kodo adopt` (Source: src/cli.js:248-277):
```text
kodo adopt
  --workspace <ref>      (required)
  --cwd <path>           (required)
  --session-id <id>      (required)
  --project <id>         (required; mapeado en ~/.kodo/projects.json)
  --title <t>            (optional; default basename(cwd) lo aplica el CORE)
  --description <d>      (optional; OMITIR esta fase — D-04)
  --json                 (optional)
```

Exit codes de `kodo adopt` (Source: src/cli/adopt.js:144-160, 5 casos):
```text
0 — adopted | ALREADY_ADOPTED  (éxito o no-op idempotente)
1 — config | input | persist   (error interno; no retryable sin corregir)
2 — transient POST             (red/timeout al provider; retryable)
```

Saneo del título (Source: src/adopt.js:82-90 — redactPaths SOLO, NO escapa metacaracteres shell):
```js
// El default basename(cwd) y la redacción de home/rutas absolutas viven AQUÍ.
// NO es defensa contra shell-injection (corre POST-parse del shell del orquestador).
export function sanitizeAdoptionData({ cwd, title, description }, homedirFn = homedir) { /* … */ }
```

Path del transcript opcional (Source: src/logger-events.js:107-109):
```text
~/.claude/projects/<encodeURIComponent(cwd).replace(/%2F/g,'-')>/<sessionId>.jsonl
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sección canónica §"Adopción asistida (sesión → tarea)" en skill.md</name>
  <files>.claude/skills/kodo-orchestrate/skill.md</files>
  <read_first>
    - `.claude/skills/kodo-orchestrate/skill.md` (el archivo a editar — 180 líneas). Analogs intra-archivo a IMITAR:
      - §"Proceso de inicio" (skill.md:14-35) — pasos numerados, imperativo segunda persona, cada paso abre con label en negrita, cita comandos/`~/.kodo/*.json` por nombre, embebe el caveat provider-agnostic ("NO asumas un provider concreto").
      - §"Mapeo de proyectos" (skill.md:51-64) — el patrón `cat ~/.kodo/projects.json` + "pregunta al usuario antes de"; REUSA por referencia (no re-documentar) para resolver `--project`.
      - §"Cuándo correr kodo gsd verify" (skill.md:83-97) — el estilo de documentar un `kodo <cmd>` shelleado: nombrar el comando, listar comportamiento, enumerar exit codes deterministas como sub-lista bullet.
      - §"Sesiones GSD" (skill.md:66-109) — sección de flujo de lifecycle adyacente al punto de inserción.
    - `.planning/phases/57-orquestador-asistido/57-PATTERNS.md` (§"skill.md — NEW §Adopción asistida", líneas 18-73 — placement, voz, las 4 reglas de defensa-en-profundidad, fuentes de derivación del título).
    - `.planning/phases/57-orquestador-asistido/57-RESEARCH.md` (Pitfall 1 líneas 138-145 — el mandato shell-seguro; Code Examples líneas 190-206 — patrones SAFE/UNSAFE de invocación; D-01 Resolution líneas 208-229 — input explícito + lookup en state.json).
    - Source-of-truth (REFERENCIAR, NO editar): `src/cli.js:248-277` (flags), `src/cli/adopt.js:144-160` (exit codes), `src/adopt.js:82-90` (sanitizeAdoptionData = paths-only).
  </read_first>
  <action>
    Inserta una NUEVA sección `## Adopción asistida (sesión → tarea)` ENTRE §"Sesiones GSD" (termina en skill.md:109) y §"Diagnóstico" (empieza en skill.md:111). Es un flujo de lifecycle, no un síntoma→comando de diagnóstico — va adjacente a las secciones de ciclo de vida, ARRIBA de §"Diagnóstico".

    Estructura: un párrafo intro breve (el orquestador propone proactivamente adoptar una sesión ad-hoc — su valor es el título inteligente, no descubrir surfaces) seguido de pasos numerados imperativos (espejo de §"Proceso de inicio"):

    1. **Obtener las coordenadas (input explícito — D-01)** — el orquestador NO auto-descubre surfaces (eso es la tecla del dashboard, Phase 56). El operador nombra la sesión por su ancla humana (p. ej. "la sesión en ~/dev/foo"); el orquestador rellena `workspace_ref` / `session_id` haciendo `cat ~/.kodo/state.json` y matcheando por `cwd` (reusa el patrón de skill.md:23-24). Escape hatch en UNA frase: si la sesión ad-hoc nunca fue sembrada en `state.json` y no puedes resolver las coordenadas, pide al operador que adopte desde el dashboard (tecla `a`). NUNCA llames a `cmux` directamente (invariante LOCKED: todo cmux entra por `src/host/`).

    2. **Derivar el título inteligente (D-02)** — ancla en `basename(cwd)` + señal primaria `git log --oneline -N` en el cwd (los subjects de commit son la mejor señal de "qué es este trabajo"); opcionalmente enriquece con un resumen del transcript leyendo `~/.claude/projects/<encoded-cwd>/<sessionId>.jsonl` (path computable; trátalo como opcional — `git log` es siempre la señal primaria). Compón UNA línea concisa estilo título de tarea. NO reimplementes el default ni el saneo — solo produce un string mejor que `basename(cwd)`.

    3. **⚠ Restringir el título a un charset seguro ANTES de invocar (mandato LOAD-BEARING — T-57-01)** — el título es una frase humana de una línea (≤ ~80 chars). Prohíbe/elimina del título derivado: `\ $ \` " ' ; | & < > {newline}`. **Summariza** los subjects de commit, nunca los copies verbatim (un subject `feat(x): add $FOO via \`bar\`` se vuelve `Añadir FOO via bar`). Documenta explícitamente que `sanitizeAdoptionData` (src/adopt.js:82-90) redacta rutas/home pero NO neutraliza metacaracteres shell y corre DENTRO de `kodo adopt`, DESPUÉS de que el shell del orquestador ya parseó el comando — por eso el saneo del núcleo NO protege contra la inyección y NO debes apoyarte en él para la seguridad shell. NO dupliques la redacción de paths en prosa (eso es la única fuente de verdad del núcleo, D-04).

    4. **Proponer + esperar aprobación (D-03)** — propón el título derivado + el proyecto destino al operador y ESPERA su aprobación/edición. Nunca crees silenciosamente. El operador ve el título antes de que corra (backstop humano de la mitigación).

    5. **Resolver el proyecto destino** — reusa §"Mapeo de proyectos" (apunta a ella, NO la re-documentes): `cat ~/.kodo/projects.json` para resolver `--project <id>`; si el mapping no existe, pregunta al operador antes de crear.

    6. **Shellear kodo adopt de forma shell-segura** — pasa el título como UN argumento literal entre comillas SIMPLES: `kodo adopt --workspace "$WS" --cwd "$CWD" --session-id "$SID" --project "$PROJ" --title 'Título seguro aquí'`. Dentro de comillas simples nada se interpola. Incluye un ejemplo SAFE y un ejemplo UNSAFE explícito (`--title "$(git log -1 --format=%s)"` ejecuta command substitution; backticks + `;` ejecutan). Solo `--title` esta fase — OMITE `--description` (D-04: diferido a BIDIR-F2). Cierra con los exit codes deterministas de `kodo adopt` como sub-lista bullet (espejo de §"Cuándo correr kodo gsd verify"): `0` adopted|ALREADY_ADOPTED (idempotente, re-run seguro), `1` config|input|persist, `2` transient POST (retryable).

    Voz: imperativa, segunda persona, provider-agnostic (NUNCA nombres un provider concreto — `--project` es un id genérico; la creación de tarea la hace el `createTask` del núcleo invocado por `kodo adopt`, no el orquestador). NO inventes prosa sobre lógica del núcleo: el orquestador deriva el título y shellea; el resto es reuso.
  </action>
  <verify>
    <automated>node --test test/prompt.test.js test/skill-sync.test.js test/orchestrator-launch-isolation.test.js</automated>
    Además (grep-asserts de presencia):
    - `grep -q "Adopción asistida" .claude/skills/kodo-orchestrate/skill.md`
    - `grep -q "kodo adopt" .claude/skills/kodo-orchestrate/skill.md` y que aparezca `--title` con comilla simple en el ejemplo SAFE.
    - La sección aparece entre §"Sesiones GSD" y §"Diagnóstico": el índice de "## Adopción asistida" está entre el de "## Sesiones GSD" y el de "## Diagnóstico".
  </verify>
  <acceptance_criteria>
    - Existe `## Adopción asistida (sesión → tarea)` en skill.md, ubicada entre §"Sesiones GSD" y §"Diagnóstico".
    - La sección contiene los 6 pasos: coordenadas explícitas (con escape hatch al dashboard + "nunca cmux directo"), derivación del título, mandato de charset seguro, proponer+esperar aprobación (D-03), resolución de `--project` por referencia a §"Mapeo de proyectos", y el shell-out con `--title` entre comillas simples + ejemplo UNSAFE + exit codes.
    - El mandato shell-seguro (charset seguro + single-quote literal + "el saneo del núcleo NO protege la shell") está presente y explícito.
    - Provider-agnostic: ningún nombre de provider concreto aparece en la sección nueva.
    - `git diff` NO toca `src/orchestrator/launch.js`, `src/adopt.js`, `src/cli/adopt.js`, `src/cli.js`.
    - `node --test test/prompt.test.js test/skill-sync.test.js test/orchestrator-launch-isolation.test.js` sale con exit 0.
  </acceptance_criteria>
  <done>La sección canónica de adopción existe en skill.md con el flujo numerado completo + el mandato shell-seguro LOAD-BEARING + los exit codes; la suite de invariantes pasa; ningún archivo `src/` de lógica cambia.</done>
</task>

<task type="auto">
  <name>Task 2: Espejo condensado "Adopción asistida" en src/orchestrator/prompt.md</name>
  <files>src/orchestrator/prompt.md</files>
  <read_first>
    - `src/orchestrator/prompt.md` (el archivo a editar — 109 líneas). Analogs intra-archivo a IMITAR:
      - §"Sesiones GSD" condensado (prompt.md:30-38) — 4-8 líneas + bullets que enuncian el flujo + deferencia al skill canónico. Esta es la densidad a igualar.
      - Header cross-ref (prompt.md:3) — "Fuente canonical extendida: la skill … contiene el comportamiento completo"; cierra la subsección nueva con la misma deferencia.
      - Marcador `<!-- BEGIN reporting -->` (prompt.md:40) — TODO lo que va entre BEGIN/END reporting lo strippea `applyReportingGate` cuando `report_to_provider` es false. La subsección nueva DEBE ir ANTES de la línea 40 (cuerpo always-on), idealmente justo tras §"Sesiones GSD" (prompt.md:38).
    - `.planning/phases/57-orquestador-asistido/57-PATTERNS.md` (§"prompt.md — NEW condensed Adopción asistida", líneas 77-100 — placement, analog condensed-mirror, cross-ref, convención de placeholders).
    - `.planning/phases/57-orquestador-asistido/57-RESEARCH.md` (Pattern 2 líneas 105-111; Pitfall 3 líneas 152-154 — no caer en el bloque reporting gated).
    - El nuevo §"Adopción asistida" del skill (Task 1) — esta subsección es su espejo CONDENSADO, no una re-explicación.
    - Source-of-truth (REFERENCIAR, NO editar): `src/orchestrator/launch.js` `resolvePromptTemplate` (solo sustituye `{{provider_name}}`/`{{provider}}`/`{{mcp_tool}}`) + `applyReportingGate`. launch.js NO se toca.
  </read_first>
  <action>
    Inserta una NUEVA subsección `## Adopción asistida` en el cuerpo ALWAYS-ON, ANTES del marcador `<!-- BEGIN reporting -->` (prompt.md:40) — slot natural: justo después de §"Sesiones GSD" (tras prompt.md:38). Si cae dentro de los marcadores reporting, desaparece cuando `report_to_provider` es false (regresión Pitfall 3).

    Densidad: 4-6 líneas + bullets, estilo §"Sesiones GSD" condensado. Contenido mínimo que DEBE sobrevivir la condensación:
    - El flujo en una línea: derivar un título inteligente del contexto real (cwd / `git log` / transcript) → proponer + esperar confirmación del operador → shellear `kodo adopt --title '<literal>'`.
    - **El mandato shell-seguro (LOAD-BEARING, debe estar AQUÍ porque el skill no siempre se auto-carga):** pasar el título como UN argumento entre comillas SIMPLES + restringirlo a charset seguro (sin `\ $ \` " ' ; | & < > newline`); summarizar los subjects de commit, no copiarlos.
    - Deferencia al skill: "ver §'Adopción asistida' en la skill `.claude/skills/kodo-orchestrate/skill.md` para el detalle" (espejo del cross-ref de prompt.md:3).

    Restricciones DURAS (verificadas contra test/prompt.test.js):
    - NO uses la palabra literal "Plane" (el test prohíbe `\bPlane\b` fuera de `{{ }}`).
    - NO introduzcas frases imperativas en inglés (`you must` / `please` / `execute your` — PM7/SR6 fallan). La prosa es en español, imperativa.
    - Puedes usar `{{provider_name}}`/`{{provider}}`/`{{mcp_tool}}` (los únicos placeholders que `resolvePromptTemplate` sustituye) pero NO introduzcas un placeholder nuevo ni lógica nueva — para esta subsección probablemente no necesitas ningún placeholder (la adopción es provider-agnostic; `--project` es un id genérico).
    - launch.js NO se modifica (SC3).
  </action>
  <verify>
    <automated>node --test test/prompt.test.js test/skill-sync.test.js test/orchestrator-launch-isolation.test.js</automated>
    Además (grep-asserts):
    - `grep -q "Adopción asistida" src/orchestrator/prompt.md`
    - La subsección está ANTES del marcador reporting: el índice de "## Adopción asistida" es menor que el de "<!-- BEGIN reporting -->".
    - El título single-quote aparece: `grep -q "kodo adopt" src/orchestrator/prompt.md` y el ejemplo usa `--title '` (comilla simple).
  </verify>
  <acceptance_criteria>
    - Existe `## Adopción asistida` en prompt.md ubicada en el cuerpo always-on, ANTES de `<!-- BEGIN reporting -->`.
    - El mandato shell-seguro (single-quote literal + charset seguro) está presente en prompt.md (no solo en el skill).
    - Cierra con cross-ref deferente al skill canónico.
    - No contiene la palabra literal "Plane" (fuera de placeholders) ni frases en inglés `you must`/`please`/`execute your`.
    - `src/orchestrator/launch.js` queda byte-idéntico (git diff vacío para ese path).
    - `node --test test/prompt.test.js test/skill-sync.test.js test/orchestrator-launch-isolation.test.js` sale con exit 0 (placeholders presentes, marcadores reporting exactamente 1, bloque reporting después de §"Sesiones GSD", sin frases inglés).
  </acceptance_criteria>
  <done>El espejo condensado de adopción existe en prompt.md en el cuerpo always-on con el mandato shell-seguro + cross-ref al skill; la suite de invariantes pasa; launch.js intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-derived title → orquestador's Bash tool | El título es input no confiable (derivado por LLM de subjects de commit, que pueden contener metacaracteres) que cruza hacia un comando shell construido por el orquestador. |
| orquestador → `kodo adopt` (proceso hijo) | El orquestador es un LLM en un shell — NO puede usar `execFile` con argv literal (a diferencia del dashboard, src/cli/dashboard/adopt.js:102-118). La frontera de inyección es la construcción del comando, ANTES de que el núcleo vea el string. |
| `kodo adopt` → provider (POST createTask) | El título/descripción saneados (rutas/home redactadas por el núcleo, BIDIR-08) salen al gestor de tareas externo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering / Elevation of Privilege | Orquestador shelleando `kodo adopt --title '<LLM-derived>'` vía su Bash tool | mitigate | **CENTERPIECE.** Mitigación PROSA en AMBOS archivos (el skill no siempre se auto-carga): (1) restringir el título derivado a charset seguro — prohibir `\ $ \` " ' ; | & < > newline` — y SUMMARIZAR los subjects de commit, no copiarlos; (2) pasar el título como UN argumento entre comillas SIMPLES (`--title '<literal>'`, nada se interpola dentro); (3) confirmación humana (D-03) como backstop — el operador ve el título antes de que corra. Defensa-en-profundidad; (1) y (2) son load-bearing. NOTA explícita en la prosa: `sanitizeAdoptionData` (src/adopt.js:82-90) redacta rutas/home pero NO escapa metacaracteres shell y corre DENTRO de `kodo adopt`, POST-parse del shell del orquestador — por eso NO es defensa contra esta inyección. |
| T-57-02 | Information Disclosure | Fuga de secretos/rutas al título de la tarea externa | accept (mitigado por el núcleo) | El núcleo `sanitizeAdoptionData` redacta home/rutas absolutas automáticamente (BIDIR-08, Phase 53). El orquestador NO duplica el saneo (única fuente de verdad); la prosa NO lo re-documenta. Severidad baja — el saneo de paths ya existe y es transversal. |
| T-57-03 | Information Disclosure | Exfiltración del body crudo del transcript hacia la tarea | mitigate | Estructuralmente imposible en el núcleo (sin parámetro transcript, src/adopt.js:75-77); la prosa PROHÍBE usar un body crudo de transcript como `--description` y recomienda solo-`--title` esta fase (D-04, BIDIR-08). El transcript solo se LEE para resumir, nunca se embebe. |
| T-57-SC | Tampering | Instalaciones npm/pip/cargo | n/a | No aplica — Phase 57 instala CERO paquetes (deliverable = dos `.md`). Sin Package Legitimacy Gate requerido. |

**ASVS L1:** V5 (Input Validation) es la categoría central — el título derivado por LLM en la frontera del shell. T-57-01 sería `high` si quedara sin mitigar; la mitigación prosa (charset seguro + single-quote literal + confirmación humana) es explícita y verificable (presente en AMBOS archivos). El dashboard sortea esto con `execFile` sin shell; el orquestador no puede, por eso la disciplina prosa es la única defensa.
</threat_model>

<verification>
**Invariantes (preservación, no nueva cobertura — fase pure-prose):**
1. `node --test test/prompt.test.js test/skill-sync.test.js test/orchestrator-launch-isolation.test.js` → exit 0.
   - `prompt.test.js`: placeholders presentes, sin literal "Plane", marcadores reporting exactamente 1, bloque reporting tras §"Sesiones GSD", sin frases inglés.
   - `orchestrator-launch-isolation.test.js`: launch.js sin `--worktree` runtime + `cwd: process.cwd()` preservado.
   - `skill-sync.test.js`: usa fixtures sintéticos (no el skill.md real) → editar el real es seguro.
2. SC3 — `git diff src/orchestrator/launch.js src/adopt.js src/cli/adopt.js src/cli.js` VACÍO.
3. `git diff --name-only` muestra SOLO `.claude/skills/kodo-orchestrate/skill.md` + `src/orchestrator/prompt.md` (la suite añade nada en `src/` de lógica).
4. El mandato shell-seguro (single-quote literal + charset seguro) presente en AMBOS archivos.
5. Full suite verde antes de `/gsd:verify-work`: `node --test $(find test -name '*.test.js' -type f)`.

**HUMAN-UAT (LLM-driven, inherentemente manual):** lanzar el orquestador, apuntarlo a una sesión ad-hoc, verificar que deriva un título mejor que `basename(cwd)`, pide confirmación, y crea la tarea con título saneado. Cubierto por 57-VALIDATION.md §"Manual-Only Verifications".
</verification>

<success_criteria>
- ✅ SC1: el orquestador propone adoptar una sesión ad-hoc y deriva un título inteligente del contexto real (cwd / commits / transcript) — sección del skill + espejo.
- ✅ SC2: el título derivado pasa por el sanitizador del núcleo (BIDIR-08) y se confirma (humano/CLI) antes de crear — paso D-03 + nota de que `kodo adopt` sanea automáticamente.
- ✅ SC3: shellea el mismo `kodo adopt --title '<derived>'` (carril 0-token preservado; LLM solo en el consumidor) — prosa del skill actualizada, CERO lógica nueva (launch.js + núcleo byte-idénticos).
- ✅ T-57-01 mitigado: mandato shell-seguro presente y verificable en AMBOS archivos.
- ✅ Suite de invariantes verde + full suite verde.
</success_criteria>

<output>
Create `.planning/phases/57-orquestador-asistido/57-01-SUMMARY.md` when done.
</output>
