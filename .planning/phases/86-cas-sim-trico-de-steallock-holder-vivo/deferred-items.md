# Deferred items — Phase 86

Ítems que esta fase **decide no cerrar**, cada uno con su razón y su trigger real. Un ítem sin
trigger es una intención, no un diferido: por eso la columna nunca queda vacía.

| Ítem | Qué se difiere | Por qué no aquí | Trigger |
|---|---|---|---|
| **WR-03 — `isPidAlive` clasifica un lock `{}` como holder vivo para siempre** (hallazgo del code review de la fase, `86-REVIEW.md` §WR-03) | Endurecer `isPidAlive` (`src/gsd/lock.js`) frente a PIDs no enteros. Medido: `isPidAlive(undefined \| NaN \| 0 \| -1) === true`, y `new Date(undefined)` es `NaN`, así que `isStaleLock` tampoco corta por TTL. Consecuencia: un lock `{}` (JSON válido pero sin campos) se clasifica como **holder vivo y fresco de forma permanente** — `acquireGsdLock` devuelve `{"acquired":false,"holder":{}}` y `doctor.decideLock` dice `keep`. Sin salida salvo borrar el fichero a mano. | **Es pre-existente: esta fase no lo introdujo.** `isPidAlive` no se toca en ningún commit de la 86 (`git diff 120e5e9d..HEAD` lo confirma). Y arreglarlo **no es local**: `isPidAlive` e `isStaleLock` son el predicado compartido de `acquireGsdLock`, `stealLock` y `doctor.decideLock` — el `decideLock` está declarado espejo EXACTO del primero (D-13 de su fase). Cambiar la clasificación de un lock cambia la máquina de estados observable de los tres, que es justo lo que el **criterio 5 de esta fase prohíbe** («los consumidores no cambian de comportamiento, el camino caliente queda intacto»). Un fix correcto necesita su propio boundary y su propio harness. | Que aparezca un lock `{}` o con `pid` no entero en uso real (un `.kodo.lock` truncado por un crash a mitad de escritura, o hand-editado), **o** el próximo trabajo que abra `isPidAlive` / `isStaleLock` por otra razón. Al abrirlo: validar `Number.isInteger(pid) && pid > 0` antes del `process.kill`, y decidir explícitamente si un lock sin `pid` es robable (probablemente sí: es la misma clase que el corrupto del Caso 5). |
| **La mitad conductual de WR-02 — el `finally` suelta el steal-guard sin comprobar propiedad** (`86-REVIEW.md` §WR-02) | Que `stealLock` solo borre en su `finally` el guard **propio**, y que `guardIsStale` no rompa por edad un guard cuyo dueño sigue vivo. Hoy `guardIsStale:413` rompe un guard vivo a los 5 s y el `finally:667` hace `unlinkSync(guardPath)` sin mirar quién lo escribió: dos stealers pueden coexistir en la sección crítica. | La fase **sí cerró la mitad documental**: el commit `7a3579f` retiró el «by construction» de la cabecera de `stealLock` y el «never broken by age» del comentario de `STEAL_GUARD_STALE_MS`, porque afirmaban más de lo que el código sostiene — exactamente el defecto que D-18 y LOCK-07 existen para eliminar. La mitad **conductual** es otra cosa: es el primitivo del guard, pre-existente a esta fase, y tocarlo es la clase de cambio que LOCK-F1 agrupa. La redacción honesta actual **nombra** la limitación en vez de taparla, que es lo que esta fase debía entregar. | El mismo que `LOCK-F1`: que la coexistencia de dos stealers se manifieste en uso real. Al abrirlo, va junto con `LOCK-F1` — comparten causa (serialización por presupuesto de tiempo en vez de por construcción) y por tanto se corrigen en la misma pasada. |
| **Criterios de aceptación por `grep -c` que miden el token, no la semántica** (aprendizaje de la fase, registrado por los dos ejecutores) | Sustituir los gates de conteo literal por gates acotados al bloque de código relevante. Dos casos medidos en esta fase: `grep -c 'mtimeMs' src/gsd/lock.js = 3` se puso rojo al **documentar** por qué `mtimeMs` queda fuera de la comparación (el conteo subía a 4 por una frase que dice lo contrario de lo que el gate teme); y `grep -c 'waitUntil' >= 8` quedó en 7 porque la secuencia del propio plan solo prescribe 5 llamadas. | No es un defecto del código de la fase sino del **contrato de verificación** de GSD, y arreglarlo bien es trabajo de tooling (acotar el grep a un rango de líneas o a un bloque nombrado), no de una fase de saneo del lock. Los dos ejecutores lo declararon en vez de maquillarlo, que es lo que DEBT-04 exige. | El próximo `/gsd-plan-phase` que vuelva a emitir un criterio de conteo literal sobre un fichero grande. El fix barato ya está descrito: acotar al cuerpo de la función (`awk` entre marcadores) o exigir que las N apariciones vivan dentro de un símbolo concreto. |

---

## Lo que esta fase SÍ cerró y no debe leerse como diferido

- **R-82-01** — la carrera de 2º orden con holder VIVO queda **cerrada con fix real**, no aceptada.
  LOCK-04..07 completos, mordida reproducida de forma independiente por el verificador.
- **`LOCK-F1`** sigue diferido a v2 **por decisión del mantenedor (2026-08-02)**, no por omisión de
  esta fase: la ventana residual de 2 syscalls está declarada en el JSDoc de `stealLock` y en
  `STATE.md`, con su clase de riesgo nombrada y sin presentarse jamás como cierre por construcción.
  Su trigger original sigue vigente: que esa ventana se manifieste en uso real.
