---
phase: 86-cas-sim-trico-de-steallock-holder-vivo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gsd/lock.js
  - test/gsd-lock-guard.test.js
autonomous: true
requirements: [LOCK-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Con el seam disparado dentro de la sección crítica del steal para sustituir el lock por uno fresco y VIVO de otro creador, `acquireGsdLock` devuelve `{ acquired: false, reason: 'lock-replaced-mid-steal' }` y `result.holder.session_id` es el del creador — no el del stealer (LOCK-04 a/b, D-06). Comando: `node --test test/gsd-lock-guard.test.js`."
    - "En ese mismo escenario el fichero en disco conserva el `session_id` del creador Case-1: el `renameSync` destructivo NO se ejecuta (LOCK-04 a). Comando: `node --test test/gsd-lock-guard.test.js`."
    - "El camino del abort no deja residuo: tras el escenario, `.planning/` no contiene ninguna entrada que case `.tmp.` y el `.kodo.lock.steal-guard` ha desaparecido (D-03, `finally` de `src/gsd/lock.js:484-490` intacto). Comando: `node --test test/gsd-lock-guard.test.js`."
    - "El CAS **no** aborta cuando nada cambió: los 4 casos de `test/gsd-lock-race.test.js`, los 6 de `test/gsd-lock-guard.test.js` y los 8 de `test/gsd-lock.test.js` siguen verdes sin tocarlos (LOCK-04 c). Comando: `node --test test/gsd-lock-race.test.js test/gsd-lock-guard.test.js test/gsd-lock.test.js`."
    - "Un lock corrupto sigue siendo robable con el CAS puesto — `content: null` no bloquea la rama PRESENT (LOCK-04 d, IN-01 no empeora). Comando: `node --test test/gsd-lock.test.js` (caso `steals lock when file contains corrupt JSON`, `:136-148`)."
    - "La premisa falsa de `src/gsd/lock.js:455-457` desaparece y la frase verdadera se conserva (D-18). Comandos: `grep -c 'Case-1 creator can race here' src/gsd/lock.js` = 0 · `grep -c 'guard fully serializes us' src/gsd/lock.js` = 0 · `grep -c 'rename swaps the inode atomically' src/gsd/lock.js` = 1."
    - "El seam es superficie de test, no característica: ningún fichero de `src/` fuera de `src/gsd/lock.js` lo nombra ni lo pasa (D-10). Comando: `grep -rln '_afterCriticalReadFn' src/` devuelve exactamente `src/gsd/lock.js`."
    - "El tercer parámetro de `acquireGsdLock` es opcional y aditivo: las 23 llamadas existentes de 2 argumentos siguen compilando y verdes, y el guard cross-callsite de `test/gsd-concurrency.test.js:483-520` sigue verde (D-08/D-11, criterio 5). Comando: `node --test test/gsd-concurrency.test.js test/dispatcher.test.js`."
    - "No se debilitó ningún umbral en este plan (D-07/D-16). Comandos: `grep -v '^\\s*//' src/gsd/lock.js | grep -c 'MAX_STEAL_ATTEMPTS = 8'` = 1 · `grep -v '^\\s*//' src/gsd/lock.js | grep -c 'STEAL_GUARD_STALE_MS = 5_000'` = 1."
    - statement: "El `console.error` del abort interpola únicamente `task_ref` — nunca el cuerpo crudo del `.kodo.lock`, que es editable a mano y sería un vector de control chars hacia el terminal (T-86-04, V5)."
      verification: backstop
  prohibitions:
    - "No greenear nada enmascarando: ni debilitando asserts, ni subiendo timeouts, ni ampliando presupuestos de reintento (DEBT-04, LOCKED · D-16)."
    - "No subir `MAX_STEAL_ATTEMPTS` (hoy 8) ni `STEAL_GUARD_STALE_MS` (hoy 5_000). Si el presupuesto se agota, el epílogo de `src/gsd/lock.js:493-507` ya rechaza sin clobbear — no se toca (D-07)."
    - "No rediseñar el primitivo de lock: serializar Case-1/`release` con el steal-guard es LOCK-F1 y está diferido a v2."
    - "No añadir ninguna dependencia npm — invariante cross-milestone de `.planning/STATE.md`."
    - "No modificar `src/triggers/dispatcher.js`, `src/gsd/doctor.js`, `src/hooks/`, `src/cli/polling.js`, `src/daemon/lifecycle.js` ni `src/session/state-lock.js` (criterio 5)."
    - "No arreglar WR-01, WR-02, IN-01 ni IN-02 de `82-REVIEW.md` en este viaje — v0.20 es saneo puro y esos hallazgos están fuera de LOCK-04..07."
    - "No introducir `rmSync`, `realpathSync` sobre el `lockPath`, ni `mtimeMs` en la comparación: las tres son divergencias medidas del análogo del inbox que esta fase rechaza deliberadamente (D-04, Pitfall 4, `86-PATTERNS.md` §Divergencias)."
    - "No inventar una taxonomía de `reason`: un único valor nuevo, `lock-replaced-mid-steal` (D-09)."
  artifacts:
    - "`src/gsd/lock.js` — `readLockIdentity(path)`: lector interno de UNA pasada que devuelve `{ raw: Buffer|null, content: LockContent|null, ino: number|null }`, vecino de `readLockContent` (que se conserva intacto)."
    - "`src/gsd/lock.js` — CAS en la rama PRESENT: sustituye el bloque `:453-471` (sonda fresca tras el `tmp`, comparación `Buffer.equals` + `ino`, abort conservador, `continue` o corte con `reason`)."
    - "`src/gsd/lock.js` — `AcquireResult` (`:52`) ampliado aditivamente con `reason?: string`."
    - "`src/gsd/lock.js` — tercer parámetro `deps = {}` en `acquireGsdLock`, propagado a `stealLock` en sus tres call sites (`:137`, `:142`, `:154`), con el campo `_afterCriticalReadFn` documentado en JSDoc como seam de test."
    - "`test/gsd-lock-guard.test.js` — `describe` nuevo con el caso in-process determinista del abort del CAS (más el caso de corrupto-con-seam)."
  key_links:
    - "`readLockIdentity(lockPath)` (baseline) → `writeFileSync(tmp)` → `readLockIdentity(lockPath)` (sonda fresca) → `renameSync` o abort: el ORDEN INAMOVIBLE heredado de `src/inbox/store.js:787-789`. Comparar antes de escribir el `tmp` dejaría fuera de la ventana vigilada el propio coste de la escritura."
    - "`fresh.content` → `isStaleLock(fresh.content)` → corte con `reason` vs `continue`: el parse de la sonda fresca sirve a la vez para decidir `changed` y para evaluar D-06; no hace falta una tercera lectura."
    - "`continue` desde DENTRO del `try` → `finally` de `:484-490` → `unlinkSync(guardPath)`: el guard debe soltarse entre intentos o el siguiente se estrella contra sí mismo (Pitfall 5)."
    - "`acquireGsdLock(projectPath, sessionInfo, deps)` → `stealLock(lockPath, sessionInfo, reason, deps)` → `deps._afterCriticalReadFn` con guard `attempt === 0`: sin el guard, cada vuelta retiene el steal-guard más tiempo y se acerca al techo de `STEAL_GUARD_STALE_MS` (Pitfall 6)."
---

<objective>
Cerrar la carrera de **segundo orden** de `stealLock` (R-82-01, `82-REVIEW.md` §CR-01) con un **compare-and-swap simétrico** en la rama PRESENT de `src/gsd/lock.js:453-471`: re-validar la identidad del `lockPath` (bytes crudos + `ino`, baseline tomado de la lectura de la propia sección crítica) inmediatamente antes del `renameSync` destructivo, y **abortar** con un `reason` discriminado en vez de clobbear al creador Case-1 legítimo.

Este plan es el **tracer** de la fase: una rebanada vertical de producción a test, end-to-end y de calidad final, sobre el carril **in-process determinista** — el más fino que atraviesa todas las capas que la fase toca. El harness de procesos reales (LOCK-05) y la declaración honesta (LOCK-06/07) se expanden desde aquí en el plan `86-02`.

Purpose: el invariante «nunca dos owners» solo puede sostenerse donde vive el `renameSync` destructivo. Ningún consumidor puede repararlo desde fuera.
Output: `src/gsd/lock.js` con el lector de una pasada, el seam de test, el typedef ampliado y el CAS; `test/gsd-lock-guard.test.js` con el caso que muerde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-CONTEXT.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-PATTERNS.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarea 1: CAS simétrico end-to-end — de la rama PRESENT al caso in-process que muerde</name>

  <files>src/gsd/lock.js, test/gsd-lock-guard.test.js</files>

  <read_first>
    - `src/gsd/lock.js` **íntegro** — el fichero que se modifica. Puntos que hay que tener en la cabeza antes de tocar nada: typedef `AcquireResult` (`:52`), constantes (`:55-65`), `acquireGsdLock` (`:116-159`) con sus tres call sites de `stealLock` (`:137` corrupto, `:142` PID muerto, `:154` TTL vencido), `readLockContent` (`:244-250`), `isStaleLock` (`:259-264`), `stealLock` (`:424-508`) con la rama PRESENT (`:453-471`, el objetivo), la rama ABSENT (`:473-483`, el espejo a imitar en forma), el `finally` del guard (`:484-490`) y el epílogo (`:493-507`).
    - `src/inbox/store.js:590-839` — **el análogo exacto, ya en producción**. Leer en concreto: el JSDoc del guard CAS (`:651-709`) con la regla de orden del baseline (`:662-665`) y la declaración de ventana residual (`:678-683`); el seam `_afterReadFn` (`:698-703`); el baseline (`:731-744`) con su degradación `baseIno = null`; el disparo guardado por `attempt === 0` (`:776`); y la implementación del guard (`:787-822`) con su ORDEN INAMOVIBLE y su `catch { changed = true }`.
    - `src/triggers/dispatcher.js:60-85` y `:195-205` — el patrón de DI por tercer parámetro opcional (`deps = {}`) y el **único** consumidor de la variante rechazada (`lockResult.holder`), que no cambia.
    - `test/gsd-lock-guard.test.js` **íntegro** — el fichero de test que se extiende: cabecera y su declaración de alcance (`:1-16`), `makeSessionInfo` (`:41-50`), `writeLockDirect` (`:52-`), `writeStaleDeadLock`, `planningEntries`, y la forma de los 6 casos (a)-(f).
    - `test/gsd-lock.test.js:95-160` — las recetas de siembra: dead-PID, **TTL vencido con PID vivo** (`:117-127`, el molde literal del holder stale-pero-VIVO) y corrupto.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md` §Code Examples — los cuatro esqueletos adaptables (lector, CAS, typedef, seam). §Common Pitfalls 1-6.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-PATTERNS.md` §1-§4 y §Divergencias deliberadas — incluye **verbatim** el bloque `:453-471` que se sustituye y las dos frases falsas que se retiran.
  </read_first>

  <behavior>
    Escribir PRIMERO el caso in-process en `test/gsd-lock-guard.test.js` contra el seam ya existente pero **sin** el CAS, comprobar que sale **ROJO**, y solo entonces implementar el CAS hasta ponerlo verde. Ese rojo intermedio es la primera evidencia de mordida y se anota en el SUMMARY (la mordida canónica de LOCK-06, con reversión manual sobre el HEAD final, la produce el plan `86-02`).

    - Test 1 — «el steal aborta cuando el lock es reemplazado por un creador VIVO en plena sección crítica»: sembrado un lock stale-pero-VIVO (`pid: process.pid`, `acquired_at` retrodatado 5 h, `ttl_hours: 4`, `session_id: 'sess-live-holder'`), se llama a `acquireGsdLock` pasando por `deps._afterCriticalReadFn` un callback que hace `unlinkSync(lockPath)` y escribe acto seguido un lock nuevo de otro creador (`session_id: 'sess-creator'`, `pid: process.pid`, `acquired_at` de ahora, `ttl_hours: 4`). Esperado: `result.acquired === false`, `result.reason === 'lock-replaced-mid-steal'`, `result.holder.session_id === 'sess-creator'`.
    - Test 2 — el mismo escenario, aserción sobre el disco: el `.kodo.lock` final parsea y su `session_id` sigue siendo `sess-creator`; en `.planning/` hay exactamente **una** entrada `.kodo.lock`, **ninguna** entrada que contenga `.tmp.`, y **ninguna** `.kodo.lock.steal-guard`.
    - Test 3 — «un lock corrupto sigue siendo robable con el CAS puesto»: sembrado un lock con JSON inválido y un seam **no-op**, `acquireGsdLock` devuelve `{ acquired: true }` y el `.kodo.lock` final lleva el `session_id` del llamante. El CAS no debe convertir `content: null` en un bloqueo.
    - Test 4 — «sin seam, cero cambio de comportamiento»: los 6 casos (a)-(f) existentes del fichero siguen pasando sin tocarse, igual que los 8 de `test/gsd-lock.test.js` y los 4 de `test/gsd-lock-race.test.js`.
  </behavior>

  <reversibility rating="costly">
    D-10 y D-11 abren superficie nueva (un seam y un tercer parámetro) en un módulo de concurrencia consumido por dispatcher, orchestrator y polling; deshacerlas obligaría a reescribir el harness de `86-02` que las consume. Ambas son **aditivas y opcionales**, así que el coste de revertirlas es de reescritura de test, no de cambio de contrato: ningún call site de producción las usa.
  </reversibility>

  <action>
Implementar el CAS simétrico en cuatro movimientos sobre `src/gsd/lock.js`, en este orden, y luego el test que lo muerde. Los identificadores concretos quedaban a discreción del planner (`86-CONTEXT.md` §Claude's Discretion) y se fijan aquí — **no se renombran**.

**1) Lector de una pasada `readLockIdentity(path)` (D-01, D-02).** Función privada del módulo, colocada junto a `readLockContent` (`:244-250`), que se **conserva intacto** para el resto de call sites. Devuelve `{ raw, content, ino }`: `raw` es el `Buffer` del `readFileSync(path)` **sin encoding** (Pitfall 2 — leer con `'utf-8'` colapsaría bytes inválidos a U+FFFD y dos contenidos distintos compararían iguales), o `null` si la lectura falla; `content` es `JSON.parse(raw.toString('utf-8'))` dentro de su propio `try/catch`, o `null` si es corrupto — al mismo nivel de validación que `readLockContent`, sin empeorar IN-01; `ino` es `statSync(path).ino` tomado **inmediatamente después** de la lectura, o `null` si el stat falla (degradación del molde `src/inbox/store.js:740-744`: sin componente de inodo, la comparación de bytes sigue vigente). **Orden crítico heredado del precedente** (`src/inbox/store.js:662-665`): los bytes salen de LA LECTURA, jamás de un `statSync` tomado por separado. JSDoc en español para el bloque explicativo del orden, con la cita de procedencia; el resto del fichero está en inglés y se respeta su estilo.

**2) Tercer parámetro de deps y seam (D-10, D-11).** Cambiar la firma a `acquireGsdLock(projectPath, sessionInfo, deps = {})` y propagar `deps` como **cuarto** argumento de `stealLock` en sus **tres** call sites: `:137` (corrupto), `:142` (PID muerto) y `:154` (TTL vencido). La firma de `stealLock` pasa a `stealLock(lockPath, sessionInfo, reason, deps = {})`. El campo del seam se llama `_afterCriticalReadFn` y se invoca **dentro del `try` de la sección crítica, inmediatamente después de la lectura del baseline y ANTES de la comprobación de frescura del baseline**, guardado por `attempt === 0` — el mismo guard literal de `src/inbox/store.js:776` y por la razón que su JSDoc explica en `:701-703`, que aquí pesa más: cada vuelta extra retiene el steal-guard más tiempo y se acerca al techo de `STEAL_GUARD_STALE_MS` (Pitfall 6/8). Documentarlo en el JSDoc de `acquireGsdLock` **como seam de inyección del test de concurrencia, no como característica**, con la nota de default no-op. Ningún call site de producción pasa `deps`: dispatcher, orchestrator, polling y `doctor.js` no se tocan.

**3) Typedef aditivo (D-08).** Ampliar `AcquireResult` (`:52`) a la unión `{ acquired: true } | { acquired: false, holder: LockContent, reason?: string }`, con una línea de JSDoc que fije que `reason` es **opcional y aditivo**, que su único valor por ahora es `lock-replaced-mid-steal`, y que el único consumidor de la variante rechazada (`src/triggers/dispatcher.js:202`) lee solo `.holder`. **No** tocar el typedef `DispatchDeps` de `src/triggers/dispatcher.js:51`: un tercer parámetro opcional sigue siendo asignable a una firma de dos parámetros.

**4) El CAS en la rama PRESENT (D-02, D-03, D-05, D-06, D-09, D-18).** Sustituir el bloque `:453-471` completo. Dentro de la sección crítica, el baseline pasa a venir de `readLockIdentity(lockPath)` en lugar del `readLockContent(lockPath)` de `:448`, y la comprobación de frescura de `:451` sigue operando sobre el `content` de ese baseline. Dentro de la rama PRESENT, tras `writeFileSync(tmp, ...)` y **antes** del `renameSync`, tomar una sonda **fresca** con `readLockIdentity(lockPath)` y calcular `changed` así: si la sonda fresca o el baseline tienen `raw === null`, `changed = true` (conservador — si no se puede comprobar, NO se publica, Pitfall 3); en otro caso `changed` es cierto cuando los `Buffer` no son iguales con `.equals()`, **o** cuando ambos `ino` son no nulos y difieren (D-02, Pitfall 1: la reutilización de inodo tras `unlink`+create ciega a un detector que solo mire `ino`; el peso lo aguanta la componente de bytes). `mtimeMs` queda **fuera** (D-04). Cuando `changed`: borrar el `tmp` con `try { unlinkSync(tmp); } catch {}` best-effort — convención vigente del módulo (`:463-467`), **no** `rmSync`; luego, si el `content` de la sonda fresca parsea y `isStaleLock` sobre él es falso, es el corte de D-06 — emitir un `console.error` con el prefijo `[kodo:lock]` ya establecido en `:425`, en **inglés** como el resto de mensajes del módulo, interpolando **solo** `task_ref` (nunca el cuerpo crudo del lock: es editable a mano y sería un vector de control chars, T-86-04) — y devolver `{ acquired: false, holder: <content de la sonda fresca>, reason: 'lock-replaced-mid-steal' }`; en cualquier otro caso (no parsea, es stale, o la sonda falló) hacer **`continue`** desde DENTRO del `try`, de modo que el `finally` de `:484-490` suelte el guard y la siguiente vuelta re-contienda (D-05, Pitfall 5). Cuando `!changed`, `renameSync(tmp, lockPath)` y `return { acquired: true }` como hoy. La sonda apunta a `lockPath` **tal cual**: no añadir una segunda resolución de symlink, porque `lockPathFor` (`:199-201`) ya resolvió el projectPath y sondear un path distinto del que el `rename` toca reintroduciría el desajuste que el CAS cierra (Pitfall 4). **`MAX_STEAL_ATTEMPTS` se queda como está**: el `continue` consume un intento del presupuesto existente y el epílogo de `:493-507` no se toca (D-07). Reescribir el comentario del bloque: la primera frase de `:455` sobre la atomicidad del `rename` es **cierta y se conserva verbatim**; las dos frases que la siguen en `:455-457` son la premisa falsa de D-18 y **se retiran** — su texto verbatim está transcrito en `86-PATTERNS.md` §2 y en `86-RESEARCH.md` §Patrón 6, y no debe reaparecer en el fichero ni citado. En su lugar, describir el hecho real: el holder vivo puede liberar aquí mismo y un creador Case-1 fresco aterrizar en el hueco, y eso es exactamente lo que el CAS detecta.

**5) El caso in-process (RED primero).** Extender `test/gsd-lock-guard.test.js` con un `describe` propio, reutilizando su `beforeEach`/`afterEach` y sus helpers (`makeSessionInfo`, `writeLockDirect`, `planningEntries`). La siembra del holder stale-pero-VIVO es el molde literal de `test/gsd-lock.test.js:117-127`: `pid: process.pid` con `acquired_at` retrodatado 5 h sobre `ttl_hours: 4`. **Nunca** el dead-PID: ese sesgo es precisamente lo que esta fase corrige. Escribir los cuatro comportamientos del bloque `<behavior>` y comprobar el rojo antes de aplicar el punto 4; anotar en el SUMMARY el mensaje del assert que falló.

**6) SHA base de la fase.** Antes de la primera edición, ejecutar `git rev-parse HEAD` y registrar el valor en el SUMMARY de este plan bajo el rótulo `phase_base_sha`. El plan `86-02` lo consume para el gate de consumidores intactos (criterio 5); sin él, ese gate no es comprobable.
  </action>

  <verify>
    <automated>node --test test/gsd-lock-guard.test.js test/gsd-lock.test.js test/gsd-lock-race.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/gsd-lock-guard.test.js test/gsd-lock.test.js test/gsd-lock-race.test.js` sale con 0 fail y el conteo de tests es **estrictamente mayor** que el de HEAD (33 + 4 = 37 casos hoy en esos tres ficheros).
    - El caso nuevo asevera literalmente `result.reason === 'lock-replaced-mid-steal'` y `result.holder.session_id === 'sess-creator'`, y que el `.kodo.lock` en disco parsea con `session_id === 'sess-creator'`.
    - Higiene del abort: en el escenario del abort, `planningEntries(tmpDir)` no contiene ninguna entrada que incluya `.tmp.` ni `.kodo.lock.steal-guard`.
    - `grep -c 'Case-1 creator can race here' src/gsd/lock.js` devuelve `0`.
    - `grep -c 'guard fully serializes us' src/gsd/lock.js` devuelve `0`.
    - `grep -c 'rename swaps the inode atomically' src/gsd/lock.js` devuelve `1` (la frase verdadera se conserva).
    - `grep -c 'lock-replaced-mid-steal' src/gsd/lock.js` es ≥ 1 y el valor aparece como literal, no construido por concatenación.
    - `grep -rln '_afterCriticalReadFn' src/` devuelve exactamente una línea: `src/gsd/lock.js`.
    - `grep -c 'attempt === 0' src/gsd/lock.js` es ≥ 1 (el seam solo se dispara en el primer intento).
    - `grep -v '^\s*//' src/gsd/lock.js | grep -c 'MAX_STEAL_ATTEMPTS = 8'` devuelve `1`; `grep -v '^\s*//' src/gsd/lock.js | grep -c 'STEAL_GUARD_STALE_MS = 5_000'` devuelve `1`.
    - `grep -c 'mtimeMs' src/gsd/lock.js` sigue devolviendo `1` (solo el uso pre-existente de `guardIsStale:352`); el CAS no introduce `mtimeMs`.
    - `grep -c 'rmSync' src/gsd/lock.js` devuelve `0`.
    - `git diff --exit-code -- src/triggers/dispatcher.js src/gsd/doctor.js src/hooks/ src/cli/polling.js src/daemon/lifecycle.js src/session/state-lock.js package.json package-lock.json` sale con código 0 (ningún consumidor tocado, cero deps nuevas).
    - El SUMMARY registra el `phase_base_sha` y el mensaje del assert que falló en el paso RED.
  </acceptance_criteria>

  <done>
    `acquireGsdLock` acepta un tercer parámetro de deps opcional; la rama PRESENT de `stealLock` re-valida bytes + `ino` justo antes del `renameSync` y aborta con `reason: 'lock-replaced-mid-steal'` cuando el lock fue reemplazado por un holder vivo; el caso in-process determinista lo asevera de punta a punta; la premisa falsa ha desaparecido del código; los 37 casos previos de los tres ficheros de lock siguen verdes y ningún consumidor cambió.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: No-regresión del contrato — corrupto robable, superficie de consumidores congelada</name>

  <files>test/gsd-lock-guard.test.js</files>

  <read_first>
    - `test/gsd-lock-guard.test.js` — el fichero recién extendido en la Tarea 1; se le añaden los asserts de contrato que faltan.
    - `test/gsd-concurrency.test.js:478-521` — el guard source-hygiene cross-callsite WT-03, que hace `/acquireGsdLockFn?\s*\([^)]*worktree/i` sobre `dispatcher.js`, `manager.js` y `stop.js`. Hay que comprobar que sigue verde tras el cambio de firma, no modificarlo.
    - `test/dispatcher.test.js` — los stubs `acquireGsdLockFn` que no devuelven `reason`; ninguno debe necesitar cambio.
    - `src/gsd/doctor.js:228-277` — `decideLock`, espejo declarado de `acquireGsdLock`. Es un predicado puro sobre un `LockContent`: decide **si un lock es robable**, no qué hace el stealer cuando el fichero cambió bajo sus pies. Verificar que sigue siendo espejo, no modificarlo.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md` §Blast Radius — el inventario completo de importadores y qué lee cada uno del resultado.
  </read_first>

  <action>
Cerrar las dos filas de la `86-VALIDATION.md` §Mapa Requisito → Test que la Tarea 1 deja parcialmente cubiertas, sin tocar producción.

**a) LOCK-04 (d) — el corrupto sigue siendo robable con el seam en medio.** Añadir al `describe` nuevo de `test/gsd-lock-guard.test.js` un caso donde el lock sembrado es JSON inválido **y** el seam lo sustituye por otro contenido igualmente inválido: el resultado esperado es `{ acquired: true }` con el `session_id` del llamante en disco cuando el contenido no cambió, y el `continue` de D-05 (no el corte de D-06) cuando sí cambió pero el nuevo contenido tampoco parsea. Aseverar la rama por su efecto observable, nunca inspeccionando estado interno: un contenido corrupto **no** puede producir `reason`, porque D-06 exige `content` parseado y no-stale. Este caso es el que impide que el CAS convierta `content: null` en un bloqueo y es el guard de que IN-01 no empeora.

**b) Criterio 5 — superficie de consumidores congelada.** Ejecutar los ficheros de test que consumen la firma vieja de 2 argumentos y comprobar que ninguno necesita cambio: `test/gsd-concurrency.test.js` (incluido su guard source-hygiene de `:478-521`), `test/dispatcher.test.js`, `test/stop.test.js` y `test/gsd-inspect-cli.test.js`. Si alguno rompe, el fallo está en el cambio de firma —no en el test—: revisar que el tercer parámetro sea **opcional** y que no se haya tocado el typedef `DispatchDeps` de `src/triggers/dispatcher.js:51`. **Prohibido** ajustar un test existente para acomodar el cambio.

**c) Espejo de `doctor.decideLock`.** Dejar constancia en el SUMMARY, con la cita de `src/gsd/doctor.js:240-258` y `:349`/`:556`, de por qué el CAS no rompe el espejo declarado: `decideLock` opera sobre un snapshot de `LockContent` leído por `d.readLock` y decide robabilidad; el CAS cambia qué hace el stealer cuando el fichero cambió bajo sus pies, que es un estado que `decideLock` nunca observa. Es una comprobación de razonamiento con cita, no un test nuevo — no inventar uno.
  </action>

  <verify>
    <automated>node --test test/gsd-lock-guard.test.js test/gsd-lock.test.js test/gsd-lock-race.test.js test/gsd-concurrency.test.js test/dispatcher.test.js test/stop.test.js test/gsd-inspect-cli.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/gsd-lock-guard.test.js test/gsd-lock.test.js test/gsd-lock-race.test.js test/gsd-concurrency.test.js test/dispatcher.test.js test/stop.test.js test/gsd-inspect-cli.test.js` sale con 0 fail.
    - `git diff --exit-code -- test/gsd-concurrency.test.js test/dispatcher.test.js test/stop.test.js test/gsd-inspect-cli.test.js test/gsd-lock.test.js test/gsd-lock-race.test.js` sale con código 0: ninguno de esos ficheros se ha modificado para acomodar el cambio de firma.
    - El caso de corrupto-con-seam existe en `test/gsd-lock-guard.test.js` y asevera que un contenido corrupto **nunca** produce `reason`.
    - `git diff --exit-code -- src/` sale con código 0 respecto al estado dejado por la Tarea 1: esta tarea no toca producción.
    - El SUMMARY contiene el párrafo del espejo de `decideLock` con las tres citas de línea (`src/gsd/doctor.js:240-258`, `:349`, `:556`).
  </acceptance_criteria>

  <done>
    Un lock corrupto sigue siendo robable con el CAS puesto y no puede producir `reason`; los siete ficheros de test que consumen la superficie del lock pasan sin una sola modificación; el espejo de `doctor.decideLock` queda justificado con cita en el SUMMARY.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artefactos que produce esta fase

Símbolos, valores y ficheros nuevos. Los de este plan van marcados `[86-01]`; los del plan hermano, `[86-02]` — se listan aquí para que el inventario de la fase sea completo en cualquiera de los dos documentos.

| Artefacto | Tipo | Dónde vive | Plan |
|-----------|------|-----------|------|
| `readLockIdentity(path) → { raw, content, ino }` | lector interno de una pasada | `src/gsd/lock.js` (junto a `readLockContent`) | `[86-01]` |
| `'lock-replaced-mid-steal'` | valor canónico del `reason` (único valor nuevo, D-09) | `src/gsd/lock.js` | `[86-01]` |
| `AcquireResult.reason?: string` | campo aditivo y opcional del typedef (`:52`) | `src/gsd/lock.js` | `[86-01]` |
| `deps = {}` | tercer parámetro opcional de `acquireGsdLock`, cuarto de `stealLock` | `src/gsd/lock.js` | `[86-01]` |
| `_afterCriticalReadFn` | nombre del campo del seam de test (default no-op, `attempt === 0`) | `src/gsd/lock.js` | `[86-01]` |
| `--kind gsd-holder` | rol holder stale-pero-VIVO que libera bajo barrera; veredicto `written`/`failed` | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--kind gsd-seam` | rol stealer que se aparca en el seam; veredicto `acquired`/`blocked` | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--release <goFile>` | flag de argv: barrera de liberación del holder | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--resume <goFile>` | flag de argv: barrera de salida del seam del stealer | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--hold-until <goFile>` | flag de argv: el ganador espera una barrera en vez de dormir `--hold` | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `holder-seeded` | fichero marcador: el holder ya sembró el lock stale-pero-vivo | sandbox del test | `[86-02]` |
| `stealer-parked` | fichero marcador escrito **desde dentro del seam** | sandbox del test | `[86-02]` |
| `steal-reasons.log` | marcador de canal lateral con el `reason` de cada stealer | sandbox del test | `[86-02]` |
| `go-stealer`, `go-extras`, `go-release`, `go-creator`, `go-resume`, `go-teardown` | go-files de barrera, **uno por etapa** (nunca compartidos) | sandbox del test | `[86-02]` |
| Sección **Ventana residual** | bloque de JSDoc de `stealLock` con la clase de riesgo nombrada | `src/gsd/lock.js:388-423` | `[86-02]` |
| Bullet de ventana residual | entrada en §Critical Invariants (cross-milestone) | `.planning/STATE.md` | `[86-02]` |
| `raceGsdStealLiveHolder(extraStealers)` | orquestación de los tres tiempos en el padre | `test/gsd-lock-race.test.js` | `[86-02]` |
| `waitUntil(pred, timeoutMs)` | espera acotada sobre estado de disco | `test/gsd-lock-race.test.js` | `[86-02]` |
| `readStealReasons(dir)` | lector never-throws del marcador lateral | `test/gsd-lock-race.test.js` | `[86-02]` |
| `assertCasExercised(dir, ctx)` | guard de cobertura anti-degradación silenciosa | `test/gsd-lock-race.test.js` | `[86-02]` |
</artifacts_produced>

<flagged_assumptions>
## Supuestos marcados — sondas de requisito sin resolver

Esta fase no tiene `SPEC.md`, así que se disparó el fallback de sonda spec-less. La sonda determinista de edges recorrió LOCK-04..07 y devolvió **4 aplicables / 0 resueltos / 4 sin resolver**, las cuatro filas `unclassified`. Una fila `unclassified` **no** se auto-resuelve ni se descarta en silencio, así que quedan aquí como supuestos explícitos:

| Requisito | Estado de la sonda | Supuesto que asume este plan |
|-----------|--------------------|------------------------------|
| LOCK-04 | `unclassified` / `unresolved` | La cobertura real no viene de la sonda sino de `86-VALIDATION.md` §Mapa Requisito → Test, filas LOCK-04 (a)-(d), levantadas verbatim a `must_haves.truths` de este plan con su comando automatizado. |
| LOCK-05 | `unclassified` / `unresolved` | Ídem, filas LOCK-05 (a)-(c) → `must_haves.truths` del plan `86-02`. |
| LOCK-06 | `unclassified` / `unresolved` | Ídem, filas LOCK-06 y LOCK-06 (b) → `86-02`, con la mordida como **manual-only con evidencia citada** (D-15), no como automatización. |
| LOCK-07 | `unclassified` / `unresolved` | Ídem, filas LOCK-07 (a)-(c) → `86-02`. |

**Lectura práctica, declarada sin adornos:** el motor de la sonda clasifica prosa de requisito en inglés y estos cuatro requisitos están escritos en español, que es la causa más probable del `unclassified` en bloque. Eso **no** autoriza a darlos por resueltos — de ahí que sigan marcados aquí. Lo que sí hay es un mapa humano por requisito con comandos automatizados, y es ese mapa el que sostiene la cobertura.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.kodo.lock` en disco → `stealLock` | El cuerpo del lock es JSON **no confiable**: el fichero es editable a mano por el operador. Se parsea siempre dentro de `try/catch` y un fallo degrada a «robable». |
| Proceso A ↔ proceso B sobre el mismo `lockPath` | Frontera multi-escritor. `unlink`, `O_EXCL` create y `rename` de terceros pueden aterrizar en cualquier hueco entre dos syscalls del stealer. Es la frontera que esta fase endurece. |
| Llamante in-process → `deps._afterCriticalReadFn` | Callback inyectado que se ejecuta **dentro** de la sección crítica. Solo alcanzable por quien ya controla el proceso; ningún call site de producción lo pasa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-01 | Tampering | `stealLock` rama PRESENT, `renameSync` de `src/gsd/lock.js:461` | high | mitigate | CAS de identidad (`Buffer.equals` sobre los bytes crudos + `ino`) tomado con sonda **fresca** tras escribir el `tmp` y antes del `rename`; degradación conservadora a `changed = true` ante cualquier sonda fallida (Tarea 1, D-02/D-03). |
| T-86-02 | Tampering | detector de cambio del CAS | medium | mitigate | Comparar bytes crudos **además** del `ino`: la reutilización de inodo tras `unlink`+create cegaría a un detector que solo mirase `ino` (Tarea 1, D-02, Pitfall 1). |
| T-86-03 | Spoofing | `session_id` como token de propiedad (ASVS V3) | high | mitigate | El CAS impide que un stealer se convierta en owner sobre un `lockPath` cuya identidad ya cambió; `releaseGsdLock` (`:187-189`) sigue borrando solo con `session_id` coincidente y no se toca. |
| T-86-04 | Information disclosure | `console.error` del abort (ASVS V5) | medium | mitigate | Interpolar **solo** `task_ref` —campo ya interpolado hoy en `:151`— y nunca el cuerpo crudo del lock, que es editable a mano y podría inyectar control chars hacia el terminal (Tarea 1). |
| T-86-05 | Tampering | fichero `tmp` del steal | low | mitigate | Nombre único por escritor (`${lockPath}.tmp.${process.pid}.${randomUUID()}`, `:458`) conservado sin cambios, más `unlinkSync` best-effort en **todos** los caminos, incluido el nuevo camino de abort (Tarea 1, D-03). |
| T-86-06 | Elevation of privilege | seam `_afterCriticalReadFn` en código de producción | low | mitigate | Default no-op; disparo guardado por `attempt === 0`; ningún call site de producción pasa `deps`; aseverado por `grep -rln '_afterCriticalReadFn' src/` = solo `src/gsd/lock.js` (Tarea 1). |
| T-86-07 | Tampering | JSON del lock no validado como objeto (IN-01, ASVS V5) | low | accept | Pre-existente y fuera de LOCK-04..07. El lector nuevo degrada a `content: null` ante parse fallido, **al mismo nivel** que `readLockContent` — la fase no lo empeora, y el caso de corrupto-con-seam de la Tarea 2 lo congela. |
| T-86-SC | Tampering | instalaciones npm/pip/cargo | low | accept | **Cero instalaciones en esta fase** (`86-RESEARCH.md` §Package Legitimacy Audit: no aplica). Invariante cross-milestone de cero deps npm nuevas, aseverado por `git diff --exit-code -- package.json package-lock.json`. No procede checkpoint de legitimidad porque no hay paquete que verificar. |
</threat_model>

<verification>
1. `node --test test/gsd-lock-guard.test.js test/gsd-lock.test.js test/gsd-lock-race.test.js` → 0 fail, conteo > 37.
2. `node --test test/gsd-concurrency.test.js test/dispatcher.test.js test/stop.test.js test/gsd-inspect-cli.test.js` → 0 fail, sin haber modificado ninguno.
3. Los seis greps de fuente de `must_haves.truths` devuelven los valores exactos indicados.
4. `git diff --exit-code -- src/triggers/dispatcher.js src/gsd/doctor.js src/hooks/ src/cli/polling.js src/daemon/lifecycle.js src/session/state-lock.js package.json package-lock.json` → código 0.
5. El SUMMARY registra `phase_base_sha`, el mensaje del assert del paso RED, y el párrafo del espejo de `decideLock` con sus tres citas.
</verification>

<success_criteria>
- La rama PRESENT de `stealLock` re-valida la identidad del `lockPath` inmediatamente antes del `renameSync` destructivo y aborta con `reason: 'lock-replaced-mid-steal'` cuando cambió (LOCK-04).
- El escenario está aseverado in-process de forma determinista, con el lock del creador sobreviviendo en disco y cero residuo de `tmp` ni de guard.
- La premisa falsa de `:455-457` ha desaparecido y la frase verdadera sobre la atomicidad del `rename` se conserva (D-18).
- El seam y el tercer parámetro de deps son aditivos: cero cambios en dispatcher, orchestrator, polling y `doctor.js`; cero deps npm nuevas.
- `MAX_STEAL_ATTEMPTS` y `STEAL_GUARD_STALE_MS` intactos: ningún umbral se movió para greenear nada (DEBT-04).
</success_criteria>

<output>
Crear `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-01-SUMMARY.md` al terminar, incluyendo obligatoriamente: `phase_base_sha`, el mensaje del assert del paso RED de la Tarea 1, y el párrafo justificativo del espejo de `doctor.decideLock`.
</output>
