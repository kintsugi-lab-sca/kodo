---
phase: 86-cas-sim-trico-de-steallock-holder-vivo
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - test/helpers/lock-race-child.mjs
  - test/gsd-lock-race.test.js
  - src/gsd/lock.js
  - .planning/STATE.md
autonomous: true
requirements: [LOCK-05, LOCK-06, LOCK-07]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "El harness siembra un holder **VIVO**, no un dead-PID: el kind `gsd-holder` escribe el lock con el PID del propio proceso hijo y `acquired_at` retrodatado por encima de `ttl_hours` (LOCK-05 a, D-12). Comando: `grep -v '^\\s*//' test/helpers/lock-race-child.mjs | grep -c 'pid: process.pid'` ≥ 1. **Muerde** — HEAD `dcddc80`: 0. Éste es el gate que acredita el trabajo del holder VIVO."
    - "El sesgo dead-PID no se replica en el fichero de carrera. Comando: `grep -c 'DEAD_PID' test/gsd-lock-race.test.js` = 0. **Anti-regresión** — ya vale 0 en HEAD `dcddc80`: el token nunca ha existido en ese fichero (solo vive en `test/gsd-lock-guard.test.js` y `test/state/state-lock.test.js`). Se conserva como salvaguarda de que el harness nuevo no lo introduce; **no** es evidencia de trabajo hecho. El gate que sí mide trabajo es el de `pid: process.pid` en el helper."
    - "La única siembra de PID muerto del fichero de carrera sigue siendo la pre-existente de `raceGsdStealDeadHolder`, intacta. Comando: `grep -v '^\\s*//' test/gsd-lock-race.test.js | grep -c '9999'` = 1. **Anti-regresión** — ya vale 1 en HEAD `dcddc80`; su función es que el `describe` nuevo no añada una segunda siembra de PID muerto ni toque la existente."
    - "Con un holder stale-pero-VIVO que ejecuta `releaseGsdLock` en plena sección crítica del steal y un creador Case-1 legítimo, exactamente **un** proceso imprime `acquired` — con N=3 y con N=5 (LOCK-05 b, D-14). Comando: `node --test test/gsd-lock-race.test.js`."
    - "En el caso N=3 el lock que sobrevive en disco lleva el `session_id` del creador Case-1, no el del stealer (criterio 1 de la ROADMAP). Comando: `node --test test/gsd-lock-race.test.js`."
    - "El escenario **prueba** que ejerció la rama del CAS: `assertCasExercised` exige ≥ 1 marcador `lock-replaced-mid-steal` en `steal-reasons.log` por iteración y falla con mensaje propio si no aparece — la cobertura no puede apagarse en silencio (LOCK-05 c, precedente medido en 83-06). Comando: `node --test test/gsd-lock-race.test.js`."
    - "El holder demuestra que liberó de verdad: su veredicto por stdout es `written`, nunca `acquired`, de modo que el conteo de cardinalidad sobre el agregado sigue siendo seguro (Pitfall 9). Comando: `node --test test/gsd-lock-race.test.js`."
    - "La ventana residual está declarada en el JSDoc de `stealLock` con su clase de riesgo nombrada (LOCK-07 a, D-17). Comandos: `grep -c 'TOCTOU residual' src/gsd/lock.js` ≥ 1 · `grep -c 'Phase 83' src/gsd/lock.js` ≥ 1. **Ambos muerden** — HEAD `dcddc80`: 0 y 0."
    - "La ventana residual está declarada también en `.planning/STATE.md` (LOCK-07 b, D-17). Comandos: `grep -c 'ventana residual' .planning/STATE.md` ≥ 1 (**muerde** — HEAD: 0) · `node <gsd-tools> query state.validate` → `valid: true` sin drift (**anti-regresión** — válido en HEAD; el gate es que la edición no lo rompa)."
    - "No se debilitó ningún umbral para greenear (LOCK-06 b, D-16/DEBT-04). Comandos: `grep -v '^\\s*//' src/gsd/lock.js | grep -c 'MAX_STEAL_ATTEMPTS = 8'` = 1 · `grep -v '^\\s*//' src/gsd/lock.js | grep -c 'STEAL_GUARD_STALE_MS = 5_000'` = 1. Ambos **anti-regresión**: ya valen 1 en HEAD `dcddc80`. Aquí además cumplen una segunda función que sí muerde en la Tarea 3 — comprobar que la reversión manual de la mordida quedó **restaurada**."
    - "La suite completa crece y sigue en cero fallos: `npm test` → **`# tests` estrictamente mayor que 2590** y `# fail` = 0. El baseline `2590` está medido en vivo en HEAD `dcddc80` (`# pass` 2589 · `# skipped` 1 · `# fail` 0), así que `≥ 2590` **no** muerde y el umbral operativo es `≥ 2591`."
    - "Los consumidores del lock no cambiaron (criterio 5). Comando: `git diff --name-only <phase_base_sha>..HEAD -- src/ test/` lista exactamente `src/gsd/lock.js`, `test/gsd-lock-guard.test.js`, `test/gsd-lock-race.test.js` y `test/helpers/lock-race-child.mjs`, y ninguno de `src/triggers/dispatcher.js`, `src/gsd/doctor.js`, `src/hooks/`, `src/cli/polling.js`, `src/daemon/lifecycle.js`, `src/session/state-lock.js`."
    - statement: "La mordida del guard queda **verificada y registrada con evidencia citada**: diff exacto de la reversión manual del CAS, salida `# fail ≥ 1` del runner con el mensaje del assert que rompió, y salida verde tras restaurar (LOCK-06, D-15). Manual-only por decisión: automatizarlo sería mutation testing, infraestructura nueva prohibida en v0.20."
      verification: backstop
    - statement: "La redacción de la ventana residual es **honesta**, no solo presente: contiene los cuatro elementos de D-17 (qué es · clase de riesgo nombrada · qué cambia de verdad · nunca cierre por construcción). Un grep verifica presencia, no honestidad."
      verification: backstop
    - statement: "El aparcamiento del stealer en el seam se mantiene holgadamente por debajo de `STEAL_GUARD_STALE_MS`, de modo que ningún stealer extra rompe el guard por edad y el escenario no degrada a medir la carrera de PRIMER orden ya cerrada en Phase 82 (A1, Pitfall 8)."
      verification: backstop
  prohibitions:
    - "No greenear el harness enmascarando: ni debilitando asserts, ni subiendo timeouts, ni ampliando presupuestos de reintento (DEBT-04, LOCKED · D-16). Si el harness sale rojo con el CAS puesto, el fallo está en el CAS o en la secuenciación del harness — nunca en el umbral."
    - "No subir `MAX_STEAL_ATTEMPTS` (hoy 8) ni `STEAL_GUARD_STALE_MS` (hoy 5_000). Subir el segundo para que el escenario quepa es literalmente «subir un timeout para que el test pase» (Pitfall 8)."
    - "No presentar la ventana residual como cierre por construcción. El cierre por construcción es LOCK-F1 y está diferido a v2 con su trigger propio."
    - "No borrar ni relajar `assertCasExercised` ante un rojo. La reacción correcta a un rojo de cobertura es revisar la SECUENCIACIÓN de los tres tiempos."
    - "No construir infraestructura de mutation testing para automatizar la mordida (D-15, diferido con trigger)."
    - "No modificar los 4 casos existentes de `test/gsd-lock-race.test.js` ni `raceGsdChildren`/`raceGsdStealDeadHolder`: el `describe` nuevo se añade al final (D-13)."
    - "No contaminar el contrato de stdout de los hijos: un único `process.stdout.write` por hijo; toda señal extra va por canal lateral en fichero, en su propio `try/catch`, después del veredicto lógico (Pitfall 9)."
    - "No asertar sobre quién gana la carrera. La aserción canónica es la de cardinalidad sobre el agregado; la de identidad en disco solo se admite con el comentario que justifique por qué los roles son asimétricos **por construcción** (D-14, Pitfall 10)."
    - "No añadir ninguna dependencia npm ni tocar `package.json`."
  artifacts:
    - "`test/helpers/lock-race-child.mjs` — kind `gsd-holder`: siembra el lock stale-pero-VIVO, escribe el marcador `holder-seeded`, espera `--release` y ejecuta `releaseGsdLock`; veredicto `written`/`failed`."
    - "`test/helpers/lock-race-child.mjs` — kind `gsd-seam`: llama a `acquireGsdLock` con `deps._afterCriticalReadFn`, que escribe `stealer-parked` y espera `--resume`; veredicto `acquired`/`blocked` + marcador de `reason` por canal lateral."
    - "`test/helpers/lock-race-child.mjs` — flags de argv nuevas `--release`, `--resume` y `--hold-until`, documentadas en el bloque de cabecera junto a los kinds nuevos."
    - "`test/gsd-lock-race.test.js` — `raceGsdStealLiveHolder(extraStealers)`, `waitUntil`, `readStealReasons`, `assertCasExercised` y el `describe` nuevo con los casos N=3 y N=5."
    - "`src/gsd/lock.js` — sección **Ventana residual** en el JSDoc de `stealLock` (`:388-423`), con la clase de riesgo nombrada."
    - "`.planning/STATE.md` — entrada de la ventana residual en §Critical Invariants to Preserve (cross-milestone)."
  key_links:
    - "`holder-seeded` → `go-stealer` → `stealer-parked` → `go-extras` → `go-release` → (lock del holder desaparece) → `go-creator` → (lock no-holder presente) → `go-resume` → `steal-reasons.log` → `go-teardown`: la cadena de **tres tiempos** del padre. Cada flecha es un `waitUntil` sobre estado de disco, no una espera de duración fija."
    - "Seam del stealer (aparcado, reteniendo el steal-guard) ↔ `STEAL_GUARD_STALE_MS`: mientras el stealer está aparcado retiene el guard; si el aparcamiento superase el umbral, un stealer extra lo rompería por edad y entrarían dos a la sección crítica — el test pasaría a medir la carrera de primer orden."
    - "`assertCasExercised` ← `steal-reasons.log` ← `result.reason` del hijo `gsd-seam` ← `reason: 'lock-replaced-mid-steal'` del corte D-06 de `86-01`: la cadena completa de observabilidad que hace la cobertura demostrable en vez de supuesta."
    - "Reversión manual del CAS → dos rojos independientes: `acquired === 2` (cardinalidad) y `assertCasExercised` sin marcador. La canónica es la de cardinalidad (D-14)."
---

<objective>
Expandir el tracer de `86-01` en las dos direcciones que la fase todavía debe: (a) el **harness de procesos reales** que siembra un holder stale-pero-VIVO y demuestra cardinalidad exacta con un release concurrente (LOCK-05), y (b) la **honestidad como entregable** — la mordida del guard verificada y registrada con evidencia citada (LOCK-06) más la declaración sin adornos de la ventana residual en los dos sitios que D-17 exige (LOCK-07).

Purpose: tres de los cuatro requisitos de esta fase se pueden cumplir a medias sin que nadie lo note. LOCK-06 y LOCK-07 existen para que no se haga. Un comentario que afirma más de lo que el código garantiza cuenta como defecto aquí, igual que en la Phase 85 y en la 87.
Output: `test/helpers/lock-race-child.mjs` con dos roles nuevos; `test/gsd-lock-race.test.js` con la orquestación de tres tiempos y el guard de cobertura; el JSDoc de `stealLock` y `.planning/STATE.md` con la ventana residual declarada; el SUMMARY con la evidencia de la mordida.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-CONTEXT.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-PATTERNS.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-VALIDATION.md
@.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Harness de holder VIVO — dos roles nuevos y la orquestación de tres tiempos (N=3)</name>

  <files>test/helpers/lock-race-child.mjs, test/gsd-lock-race.test.js</files>

  <read_first>
    - `test/helpers/lock-race-child.mjs` **íntegro** — el fichero que se extiende. Interiorizar: el contrato de stdout de la cabecera (`:14-18`), la documentación por-kind del bloque `:20-95` y la lista de `argv` (`:72-94`) —convención del fichero: cada kind nuevo añade su párrafo y sus flags ahí—, `parseArgs` (`:98-109`), `sleepSync` (`:111-118`), `waitForBarrier` (`:120-130`), el marcador de canal lateral con su comentario (`:241-252`), el kind `mark` como molde del seam inyectado (`:264-282`), el kind `gsd` (`:396-413`) y el bloque de `--hold` del ganador (`:415-420`).
    - `test/gsd-lock-race.test.js` **íntegro** — cabecera con la disciplina «asserts on the AGGREGATE, never on which child wins» (`:8`), el sandbox `beforeEach`/`afterEach` (`:24-33`), `raceGsdChildren` (`:41-64`), `raceGsdStealDeadHolder` (`:74-118`) con su resolución de symlink (`:79`) y los 4 casos existentes (`:120-162`). Ninguno se modifica.
    - `test/inbox-concurrency.test.js:1-255` — el molde de la orquestación por etapas: la disciplina de cabecera (`:1-62`), `waitUntil` (`:133-140`), `raceChildren` con `gate` y su JSDoc de por qué existe la liberación en dos tiempos (`:142-192`, en especial `:145-160`), `readBranchCounts` never-throws (`:210-224`) y `assertFailopenExercised` (`:226-253`).
    - `test/gsd-lock.test.js:117-127` — la receta literal de siembra stale-pero-VIVO (`acquired_at` retrodatado 5 h, `pid: process.pid`).
    - `src/gsd/lock.js` — el CAS y el seam recién construidos por `86-01`; en concreto la firma de `acquireGsdLock`, el nombre del campo del seam, el punto exacto donde se dispara, y las constantes `MAX_STEAL_ATTEMPTS` y `STEAL_GUARD_STALE_MS` (`:55-65`).
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md` §Patrón 3 (la tabla t0-t10 y el modo de fallo silencioso del barrier único), §Patrón 4 (seam por barrera y el techo de 5.000 ms) y §Pitfalls 7-10.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-01-SUMMARY.md` — el `phase_base_sha` y los nombres exactos que `86-01` fijó.
  </read_first>

  <action>
Construir el harness que hoy no existe: el repo solo sabe sembrar holders con PID muerto, y ése es exactamente el sesgo por el que esta carrera es invisible.

**a) Kind `gsd-holder` en `test/helpers/lock-race-child.mjs`.** Rol: holder stale-pero-VIVO que libera bajo barrera. No recibe `--barrier` de arranque: siembra nada más bootear. Secuencia: resolver `join(realpathSync(args.repo), '.planning')` y crearlo con `mkdirSync({recursive:true})` —la resolución de symlink es obligatoria porque `lockPathFor` resuelve el projectPath y en macOS `mkdtempSync` entrega rutas bajo un symlink (Pitfall 4)—; escribir `.kodo.lock` con `session_id: 'sess-live-holder'`, `task_id`, `task_ref`, **`pid: process.pid`** (vivo, es la razón de ser del rol), `acquired_at` retrodatado 5 horas y `ttl_hours: 4`, serializado con `JSON.stringify(..., null, 2) + '\n'` como hace `serializeLockContent`; escribir el fichero marcador `holder-seeded` en `args.sandbox`; `waitForBarrier(args.release, 3000)`; importar **dinámicamente** `../../src/gsd/lock.js` y llamar a `releaseGsdLock(args.repo, 'sess-live-holder')`. Veredicto por stdout: `written` en el camino feliz, `failed` en cualquier error — **nunca** `acquired`, para que el conteo de cardinalidad sobre todos los hijos siga siendo seguro (Pitfall 9). El import se mantiene dinámico aunque aquí la fuga de HOME no aplique: es la norma del fichero (`:166-168`, `:204-207`) y romperla invita a copiarla mal en el siguiente kind.

**b) Kind `gsd-seam` en el mismo fichero.** Rol: el stealer que se aparca dentro de la sección crítica. Espera `--barrier`; importa dinámicamente `acquireGsdLock` y lo llama con el tercer parámetro de deps de `86-01`, pasando por el campo del seam un callback que (1) escribe el marcador `stealer-parked` en `args.sandbox` y (2) hace `waitForBarrier(args.resume, 3000)` — **barrera, no `sleepSync`**: a diferencia del precedente del inbox, aquí el padre puede observar en disco que el lock desapareció y reapareció, así que el interleaving se hace determinista **por construcción** y no por anchura de ventana. El timeout de 3.000 ms es un techo de fallo ruidoso, claramente por debajo del umbral de edad del steal-guard: mientras el stealer está aparcado **retiene el guard**, y si el aparcamiento lo superase, un stealer extra lo rompería por edad y entrarían dos a la sección crítica — el test pasaría a medir la carrera de primer orden que la Phase 82 ya cerró. Veredicto: `acquired`/`blocked`. Después del veredicto lógico y en su propio `try/catch`, apendar `String(result.reason ?? 'no-reason')` a `steal-reasons.log` en `args.sandbox` — canal **lateral**, molde literal de `:241-252`: su fallo jamás cambia lo que el hijo imprime ni lo hace lanzar.

**c) Flags nuevas y documentación.** Añadir `--release <goFile>` (barrera de liberación del holder), `--resume <goFile>` (barrera de salida del seam) y `--hold-until <goFile>` a la lista de `argv` del bloque de cabecera, más un párrafo por kind nuevo siguiendo el estilo de los ocho ya documentados. `--hold-until` es una extensión **aditiva** del bloque de hold del ganador (`:415-420`): cuando está presente, el ganador espera esa barrera con `waitForBarrier(..., 3000)` en lugar de dormir `--hold` milisegundos. Los ocho kinds existentes y sus seis consumidores no se tocan: siguen pasando `--hold` y su comportamiento es idéntico.

**d) Orquestación de tres tiempos en `test/gsd-lock-race.test.js`.** `describe` nuevo **al final del fichero**, reutilizando el `beforeEach`/`afterEach` existente (D-13). Copiar `waitUntil(pred, timeoutMs)` de `test/inbox-concurrency.test.js:133-140` con su línea de procedencia. Añadir `readStealReasons(dir)` (never-throws, devuelve `[]` si el fichero falta, molde de `readBranchCounts`) y `assertCasExercised(dir, ctx)` (molde literal de `assertFailopenExercised`, `:226-253`): exige ≥ 1 entrada `lock-replaced-mid-steal` y falla con un mensaje que diga explícitamente que la reacción correcta es revisar la secuenciación de los tres tiempos, **jamás** borrar ni relajar la aserción.

`raceGsdStealLiveHolder(extraStealers)` spawnea **todos** los hijos antes de la primera liberación —así el `spawn` y el import dinámico quedan fuera de la ventana y dentro solo queda el trabajo real, un `unlink` y un `O_EXCL` create— y después secuencia, con un go-file **por etapa**, nunca compartido: (1) `waitUntil` a que exista `holder-seeded`, con assert propio si vence el margen; (2) soltar el stealer con seam; (3) `waitUntil` a que exista `stealer-parked`, con assert propio — es un indicador estrictamente más fuerte que la existencia del `.steal-guard`, que podría venir de un intento previo; (4) soltar los stealers extra, **solo aquí y nunca antes**: si se soltasen junto al stealer con seam, uno de ellos podría ganar el steal-guard en su primer intento, robar legítimamente el lock del holder, y entonces el stealer con seam no entraría en la sección crítica en su intento 0 y el seam no se dispararía jamás; (5) soltar el release del holder; (6) `waitUntil` a que el `session_id` en disco deje de ser el del holder —predicado deliberadamente más laxo que «el fichero no existe», porque con stealers extra sueltos uno podría ocupar el hueco antes de que el padre lo observe—; (7) soltar el creador; (8) `waitUntil` a que haya en disco un lock parseable cuyo `session_id` no sea el del holder; (9) soltar el resume del seam; (10) `waitUntil` a que aparezca `steal-reasons.log`, sin assert aquí —el assert es `assertCasExercised`—; (11) escribir el go-file de teardown para que los ganadores con `--hold-until` salgan; (12) esperar el cierre de todos los hijos y devolver veredicitos, el `session_id` final en disco y el PID del hijo creador. **Todas** las esperas son acotadas y, al vencer, se continúa igualmente: dejar hijos colgados enmascararía el fallo, misma disciplina que `raceChildren:159-160`.

El creador y los stealers extra son el kind `gsd` **existente, sin modificar**, con su propio `--barrier` y con `--hold-until`. Su `session_id` es `'sess-' + process.pid`, así que el padre puede identificar al creador por el `pid` del hijo que spawneó — sin tocar el kind.

**e) El caso N=3.** `it` con `raceGsdStealLiveHolder(0)` — holder + creador + stealer con seam. Aserciones: el veredicto del holder es `written` (prueba que liberó de verdad; el escenario no ocurrió si no); `verdicts.filter(v => v === 'acquired').length === 1` con el mensaje que enumera los veredictos, forma canónica de `:143-151` (D-14); `assertCasExercised`; y el `session_id` en disco igual al del creador. Esta última aserción **exige un comentario que justifique por qué no viola D-14**: los roles son asimétricos **por construcción** —el stealer está aparcado en una barrera que el padre controla y no hay ningún otro contendiente en el hueco—, de modo que el resultado es determinista y no una coincidencia del scheduler. Sin ese comentario un revisor futuro la leerá como una infracción y la borrará.
  </action>

  <verify>
    <automated>node --test test/gsd-lock-race.test.js test/gsd-lock-guard.test.js test/gsd-lock.test.js</automated>
  </verify>

  <acceptance_criteria>
    **Todos los criterios de fuente de abajo se midieron en vivo contra `dcddc80` al planificar.** Cada uno lleva su valor real en HEAD y su clase: **muerde** (hoy falla; solo la tarea puede satisfacerlo) o **anti-regresión** (hoy ya se cumple; existe para que la tarea no lo rompa y **no** cuenta como evidencia de trabajo hecho).

    - `node --test test/gsd-lock-race.test.js test/gsd-lock-guard.test.js test/gsd-lock.test.js` sale con 0 fail y `test/gsd-lock-race.test.js` pasa de **4** (`# tests` medido en HEAD) a 5 casos. **Muerde.**
    - `grep -v '^\s*//' test/helpers/lock-race-child.mjs | grep -c 'pid: process.pid'` es ≥ 1 (el holder siembra un PID vivo). **Muerde** — HEAD: `0`. Es el gate central de LOCK-05 (a).
    - `grep -c 'DEAD_PID' test/gsd-lock-race.test.js` devuelve `0`. **Anti-regresión** — HEAD: `0`, ya satisfecho sin trabajo alguno: el token nunca existió en ese fichero (vive solo en `test/gsd-lock-guard.test.js` y `test/state/state-lock.test.js`). Se conserva como salvaguarda de que el harness nuevo no introduce el sesgo; **no lo cites como evidencia de la fase**.
    - `grep -v '^\s*//' test/gsd-lock-race.test.js | grep -c '9999'` devuelve `1`: la siembra de PID muerto pre-existente sigue siendo la única, intacta. **Anti-regresión** — HEAD: `1`, ya satisfecho.
    - `grep -c 'gsd-holder' test/helpers/lock-race-child.mjs` es ≥ 2 (implementación + párrafo de la cabecera) y lo mismo para `gsd-seam`. **Ambos muerden** — HEAD: `0` y `0`.
    - `grep -c 'process.stdout.write' test/helpers/lock-race-child.mjs` devuelve exactamente **9** — los `7` medidos en HEAD más uno por kind nuevo: un único veredicto por hijo (Pitfall 9). **Muerde.**
    - `grep -c 'lock-replaced-mid-steal' test/gsd-lock-race.test.js` es ≥ 1 (el guard de cobertura lo exige por nombre). **Muerde** — HEAD: `0`.
    - `grep -c 'waitUntil' test/gsd-lock-race.test.js` es ≥ 8: cada transición de etapa espera sobre estado de disco, no sobre una duración fija. **Muerde** — HEAD: `0` (el helper aún no está copiado desde `test/inbox-concurrency.test.js`).
    - `grep -c 'sleepSync' test/gsd-lock-race.test.js` devuelve `0` y el seam del kind `gsd-seam` usa `waitForBarrier`, no `sleepSync`. **Anti-regresión** — HEAD: `0`, ya satisfecho: el fichero de carrera nunca ha usado `sleepSync`. El gate impide que la orquestación nueva caiga en esperas de duración fija; la mitad que sí muerde es la del `waitForBarrier` en el seam, verificable en `test/helpers/lock-race-child.mjs`.
    - `git diff --exit-code -- src/` sale con código 0: esta tarea no toca producción. **Anti-regresión** por diseño (delimita alcance).
    - Las 4 pruebas pre-existentes de `test/gsd-lock-race.test.js` y las funciones `raceGsdChildren`/`raceGsdStealDeadHolder` no aparecen modificadas en el diff de la tarea.
    - El SUMMARY registra el tiempo real de aparcamiento medido en la corrida (delta entre el marcador `stealer-parked` y el go-file de resume) y confirma que queda holgadamente por debajo del umbral de edad del steal-guard.
  </acceptance_criteria>

  <done>
    Existen dos roles nuevos de proceso hijo (holder stale-pero-VIVO que libera bajo barrera, y stealer que se aparca en el seam); la orquestación de tres tiempos reproduce el interleaving de 5 pasos de forma determinista; el caso N=3 asevera cardinalidad exacta, veredicto `written` del holder, identidad del creador en disco y cobertura demostrada del CAS.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: Caso N=5 — presión de stealers extra sin degradar el escenario</name>

  <files>test/gsd-lock-race.test.js</files>

  <read_first>
    - `test/gsd-lock-race.test.js` — el `describe` recién creado en la Tarea 1 y `raceGsdStealLiveHolder`, que ya acepta el parámetro `extraStealers`.
    - `src/gsd/lock.js:429-444` — la rama de guard ocupado del bucle de `stealLock`: `guardIsStale`, `readLockContent`, `sleepShort(2 * (attempt + 1))`. Es el camino que recorren los stealers extra, y su presupuesto acumulado (~72 ms sobre 8 intentos) es lo que determina cuándo llegan al epílogo de `:493-507`.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md` §Open Questions punto 2 (composición del N=5) y §Assumptions Log A1.
  </read_first>

  <action>
Añadir el `it` de N=5 al `describe` de la Tarea 1: `raceGsdStealLiveHolder(2)` — holder + creador + stealer con seam + **2 stealers extra**, cinco procesos hijo en total.

**Composición deliberada: exactamente un stealer lleva el seam.** Que varios se aparquen a la vez es imposible por construcción —el steal-guard los serializa— y multiplicaría el riesgo de que el aparcamiento se acerque al umbral de edad del guard. Los extra son el kind `gsd` sin seam: entran por Case-3, pierden el guard contra el aparcado (vivo y en ventana, así que `guardIsStale` es falso), agotan su presupuesto acotado y llegan al epílogo, donde rechazan contra el holder que encuentren o hacen un `O_EXCL` create legítimo si el hueco sigue abierto. En ambos desenlaces la cardinalidad se mantiene en uno: si un extra ocupa el hueco, el creador encuentra `EEXIST` sobre un holder vivo y fresco —el extra espera el teardown por `--hold-until`— y sale `blocked`.

**Aserciones del caso N=5:** veredicto `written` del holder; `verdicts.filter(v => v === 'acquired').length === 1` (D-14); `assertCasExercised`. La aserción de identidad en disco **NO** se replica aquí, y hay que escribir el comentario que explica por qué: con contendientes extra sueltos, quién ocupa el hueco entre el `unlink` del holder y el create del creador **sí** depende del scheduler, y afirmar la identidad del superviviente sería exactamente asertar sobre quién gana la carrera —lo que D-14 prohíbe—. Que un extra gane ese hueco no es un defecto: es el comportamiento correcto del `O_EXCL` sobre un path ausente, y el CAS sigue mordiendo igual porque el nuevo holder es vivo y fresco en ambos casos.

**Regla de reacción ante un rojo, escrita en el fichero:** si este caso sale rojo, la causa a investigar es la secuenciación de las etapas o el propio invariante en producción. Subir el umbral de edad del steal-guard para que el escenario quepa, o ampliar el presupuesto de reintentos, están **prohibidos** — es el enmascaramiento que DEBT-04 cierra y por el que el repo ya revirtió una vez en la Phase 83.
  </action>

  <verify>
    <automated>node --test test/gsd-lock-race.test.js</automated>
  </verify>

  <acceptance_criteria>
    Misma convención de clases que la Tarea 1: **muerde** vs **anti-regresión**, medido contra `dcddc80` (donde `test/gsd-lock-race.test.js` tiene `# tests` = 4).

    - `node --test test/gsd-lock-race.test.js` sale con 0 fail y el fichero pasa de 5 a **6** casos (4 en HEAD + 1 de la Tarea 1 + 1 de ésta). **Muerde.**
    - El caso N=5 spawnea exactamente 5 procesos hijo y asevera `acquired === 1` sobre el agregado de los cinco veredictos. **Muerde.**
    - `assertCasExercised` se invoca también en el caso N=5. **Muerde.**
    - El caso N=5 **no** contiene ninguna aserción sobre el `session_id` en disco, y el comentario que explica por qué está presente en el fichero. **Muerde** (el comentario es artefacto nuevo; la parte negativa sola no bastaría).
    - `grep -v '^\s*//' src/gsd/lock.js | grep -c 'MAX_STEAL_ATTEMPTS = 8'` devuelve `1` y `grep -v '^\s*//' src/gsd/lock.js | grep -c 'STEAL_GUARD_STALE_MS = 5_000'` devuelve `1`: ninguna constante se movió para que el caso pasara. **Anti-regresión** — HEAD: `1` y `1`, ya satisfechos. Es el gate de DEBT-04, no evidencia de trabajo.
    - Tres corridas consecutivas de `node --test test/gsd-lock-race.test.js` salen verdes (comprobación de flakiness; un rojo intermitente aquí se escala, no se enmascara). **Muerde** (el caso N=5 no existe en HEAD).
    - `git diff --exit-code -- src/ test/helpers/` sale con código 0: esta tarea solo toca el fichero de test. **Anti-regresión** por diseño (delimita alcance).
  </acceptance_criteria>

  <done>
    El caso N=5 existe, asevera cardinalidad exacta y cobertura del CAS con dos contendientes extra en presión real, no asevera identidad del superviviente, y las tres corridas de comprobación salen verdes sin haber movido ningún umbral.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: Honestidad como entregable — ventana residual declarada y mordida registrada</name>

  <files>src/gsd/lock.js, .planning/STATE.md</files>

  <read_first>
    - `src/gsd/lock.js:388-423` — el JSDoc actual de `stealLock` con sus tres pasos numerados; la sección nueva va ahí, como sección propia.
    - `src/inbox/store.js:678-683` — la **plantilla literal** de declaración honesta, transcrita verbatim en `86-PATTERNS.md` §4: qué es · qué NO cierra · qué cambia de verdad. Se calca la estructura y se le añade el elemento que el precedente no nombra: la clase de riesgo.
    - `.planning/STATE.md:149-163` — §Critical Invariants to Preserve (cross-milestone), donde ya vive el invariante del lock del inbox y el de cero deps npm. Es el destino de la entrada de D-17b.
    - `.planning/STATE.md` §Deferred Items — el aviso operativo heredado de 85-05: `state.patch`/`state.update` resuelven por `tableRowPattern` y solo casan filas de 2 celdas; la mutación de aquella fase se hizo por `Edit` con `state.validate` saliendo `valid: true` sin drift.
    - `.planning/milestones/v0.19-phases/83-*/83-VERIFICATION.md` (la línea del registro de mordida) y `.planning/STATE.md:113` — el formato de registro de mordida que esta fase replica.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-RESEARCH.md` §Patrón 5 y §Patrón 6, y `86-VALIDATION.md` §Manual-Only Verifications.
    - `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-01-SUMMARY.md` — el `phase_base_sha` necesario para el gate de consumidores.
  </read_first>

  <action>
**a) Declaración en el JSDoc de `stealLock` (LOCK-07 a, D-17).** Añadir una sección propia al JSDoc de `:388-423`, junto a los tres pasos numerados, con los cuatro elementos que D-17 exige y **ninguno más**: (1) **qué es** — entre la sonda de identidad del CAS y el `renameSync` quedan **dos syscalls contiguos**, y un creador Case-1 que aterrice justo ahí sigue siendo clobbeable; (2) **la clase de riesgo, nombrada** — TOCTOU residual, no cerrable sin soporte atómico del sistema de ficheros (el `rename` POSIX no admite condición y la variante condicional de Linux no está expuesta por `node:fs`), **la misma clase** que la ventana residual aceptada en el guard del inbox de la **Phase 83**; (3) **qué cambia de verdad** — la magnitud: deja de ser toda la sección crítica del steal, que dura lo que dure el proceso vivo del holder, y pasa a ser el hueco entre dos syscalls contiguos, sin depender de ningún presupuesto de tiempo; (4) **qué NO es** — no es cierre por construcción, y no debe leerse como si lo fuera; el cierre por construcción es LOCK-F1 y está diferido a v2 con su trigger propio. Añadir además una línea sobre el residual del residual documentado en `86-RESEARCH.md` §Pitfall 1: un contenido nuevo byte-idéntico **y** con reutilización de inodo cegaría al CAS; implica el mismo `session_id` y el mismo `acquired_at` al milisegundo entre dos procesos distintos, así que merece una línea, no una defensa. Los literales `TOCTOU residual` y `Phase 83` deben aparecer tal cual: son los anclajes de los gates de fuente.

**b) Declaración en `.planning/STATE.md` (LOCK-07 b, D-17).** Añadir un bullet a §Critical Invariants to Preserve (cross-milestone), inmediatamente después del bullet del lock del inbox de v0.19 —son la misma clase de riesgo y leerlos juntos es la mitad del valor—, con la misma sustancia que el JSDoc, condensada, incluyendo el literal `ventana residual`. Mutar el fichero con `Edit` de alcance quirúrgico sobre esa sección, **nunca** con un `Write` de fichero completo, y validar después con el handler `state.validate` de `gsd-tools`: debe salir `valid: true` sin drift. Si algún handler de `state.*` direccionase la sección, usarlo; el precedente de 85-05 documenta que para secciones curadas de este fichero la vía verificada es `Edit` + `state.validate`.

**c) Mordida canónica (LOCK-06, D-15) — manual con evidencia citada.** Sobre el HEAD final de la fase, revertir **a mano** el CAS con el cambio más pequeño que lo desactive: neutralizar el cálculo de `changed` en la rama PRESENT de modo que el `renameSync` vuelva a ser incondicional. Ejecutar `node --test test/gsd-lock-race.test.js` y `node --test test/gsd-lock-guard.test.js` y capturar la salida **roja** completa: el runner debe reportar `# fail ≥ 1` con el mensaje del assert que rompió. Se esperan **dos rojos independientes** —`acquired === 2` en el caso del holder vivo, que es la evidencia canónica por D-14, y `assertCasExercised` sin marcador— más el rojo del caso in-process de `86-01`. Restaurar con `git checkout -- src/gsd/lock.js` y volver a correr para capturar la salida **verde**. Registrar en el SUMMARY, con el formato del precedente de la Phase 83: el `diff` exacto de la reversión, la salida `# fail` con su mensaje, la salida verde tras restaurar, y el conteo de la suite completa antes y después. **Prohibido** construir infraestructura de mutation testing para automatizarlo, y prohibido tocar un solo assert, timeout o presupuesto en el proceso: si el harness no se pone rojo al revertir, el hallazgo es que el harness no muerde — se escala, no se ajusta.

**d) Cierre de fase (criterio 5).** Ejecutar `npm test` completo y registrar el conteo: el baseline está **medido en vivo en `dcddc80`** y es exactamente `# tests 2590` (`# pass` 2589 · `# skipped` 1 · `# fail` 0), así que el umbral que muerde es **`# tests` ≥ 2591**, con 0 fail. Registrar el conteo antes y después; si el `after` no supera 2590, los casos nuevos no están corriendo dentro de `npm test` y eso es el hallazgo. Ejecutar el gate de consumidores intactos usando el `phase_base_sha` que `86-01-SUMMARY.md` registró: `git diff --name-only <phase_base_sha>..HEAD -- src/ test/` debe listar exactamente `src/gsd/lock.js`, `test/gsd-lock-guard.test.js`, `test/gsd-lock-race.test.js` y `test/helpers/lock-race-child.mjs`, y **ninguno** de `src/triggers/dispatcher.js`, `src/gsd/doctor.js`, `src/hooks/`, `src/cli/polling.js`, `src/daemon/lifecycle.js`, `src/session/state-lock.js`. Comprobar además que `package.json` y `package-lock.json` no aparecen en el diff de la fase. Registrar los tres resultados en el SUMMARY.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    Misma convención de clases que las Tareas 1 y 2: **muerde** vs **anti-regresión**, medido contra `dcddc80`.

    - `npm test` sale con 0 fail y `# tests` **≥ 2591** — estrictamente mayor que el baseline `2590` medido en vivo en HEAD (`# pass` 2589 · `# skipped` 1 · `# fail` 0). **Muerde**; ojo: el `≥ 2590` de la redacción anterior era ya cierto en HEAD y no medía nada.
    - `grep -c 'TOCTOU residual' src/gsd/lock.js` es ≥ 1. **Muerde** — HEAD: `0`.
    - `grep -c 'Phase 83' src/gsd/lock.js` es ≥ 1. **Muerde** — HEAD: `0`.
    - `grep -c 'ventana residual' .planning/STATE.md` es ≥ 1. **Muerde** — HEAD: `0`.
    - El handler `state.validate` de `gsd-tools` devuelve `valid: true` sin drift tras la edición de `.planning/STATE.md`. **Anti-regresión** — válido en HEAD; el gate es que la edición quirúrgica no lo rompa (precedente 85-05).
    - El texto de la ventana residual —en el JSDoc y en `STATE.md`— contiene los cuatro elementos de D-17 y **no** afirma en ningún punto que la carrera quede cerrada por construcción. **Muerde** (revisión manual: un grep verifica presencia, no honestidad).
    - `git diff --name-only <phase_base_sha>..HEAD -- src/ test/` lista exactamente los cuatro ficheros esperados y ninguno de los seis consumidores prohibidos. **Muerde en la parte positiva** (los cuatro ficheros deben aparecer; en HEAD el diff está vacío) y **anti-regresión** en la negativa.
    - `git diff --name-only <phase_base_sha>..HEAD -- package.json package-lock.json` no devuelve nada. **Anti-regresión** — trivialmente cierto en HEAD; gate de cero deps npm.
    - `grep -v '^\s*//' src/gsd/lock.js | grep -c 'MAX_STEAL_ATTEMPTS = 8'` devuelve `1` y `grep -v '^\s*//' src/gsd/lock.js | grep -c 'STEAL_GUARD_STALE_MS = 5_000'` devuelve `1` tras restaurar la reversión. **Anti-regresión con mordida propia aquí**: valen `1` y `1` en HEAD, pero en esta tarea son la comprobación de que la reversión manual del apartado (c) quedó **efectivamente restaurada** y de que ningún umbral se tocó durante la mordida.
    - El SUMMARY contiene, citado literalmente: el `diff` de la reversión manual, la salida `# fail` con el mensaje del assert, la salida verde tras restaurar, y los conteos de suite antes y después.
    - `git status --porcelain -- src/gsd/lock.js` sale vacío tras restaurar: la reversión manual no queda en el árbol de trabajo.
  </acceptance_criteria>

  <done>
    Quien lea el JSDoc de `stealLock` o `.planning/STATE.md` encuentra declarada la ventana residual de dos syscalls contiguos con su clase de riesgo nombrada y sin presentarla como cierre por construcción; la mordida del guard está verificada a mano y registrada con evidencia citada; la suite completa está por encima del baseline con cero fallos y ningún consumidor del lock cambió.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artefactos que produce esta fase

Inventario completo de la fase; los de este plan van marcados `[86-02]`.

| Artefacto | Tipo | Dónde vive | Plan |
|-----------|------|-----------|------|
| `readLockIdentity(path) → { raw, content, ino }` | lector interno de una pasada | `src/gsd/lock.js` | `[86-01]` |
| `'lock-replaced-mid-steal'` | valor canónico del `reason` (único valor nuevo) | `src/gsd/lock.js` | `[86-01]` |
| `AcquireResult.reason?: string` | campo aditivo y opcional del typedef | `src/gsd/lock.js` | `[86-01]` |
| `deps = {}` | tercer parámetro opcional de `acquireGsdLock`, cuarto de `stealLock` | `src/gsd/lock.js` | `[86-01]` |
| `_afterCriticalReadFn` | campo del seam de test (default no-op, `attempt === 0`) | `src/gsd/lock.js` | `[86-01]` |
| Sección **Ventana residual** | bloque de JSDoc con la clase de riesgo nombrada | `src/gsd/lock.js:388-423` | `[86-02]` |
| Bullet de ventana residual | entrada en §Critical Invariants (cross-milestone) | `.planning/STATE.md` | `[86-02]` |
| `--kind gsd-holder` | rol holder stale-pero-VIVO que libera bajo barrera; veredicto `written`/`failed` | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--kind gsd-seam` | rol stealer aparcado en el seam; veredicto `acquired`/`blocked` | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--release <goFile>` | flag de argv: barrera de liberación del holder | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--resume <goFile>` | flag de argv: barrera de salida del seam | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `--hold-until <goFile>` | flag de argv: el ganador espera una barrera en vez de dormir `--hold` | `test/helpers/lock-race-child.mjs` | `[86-02]` |
| `holder-seeded` | marcador: el holder ya sembró el lock stale-pero-vivo | sandbox del test | `[86-02]` |
| `stealer-parked` | marcador escrito **desde dentro del seam** | sandbox del test | `[86-02]` |
| `steal-reasons.log` | marcador de canal lateral con el `reason` de cada stealer | sandbox del test | `[86-02]` |
| `go-stealer`, `go-extras`, `go-release`, `go-creator`, `go-resume`, `go-teardown` | go-files de barrera, **uno por etapa** | sandbox del test | `[86-02]` |
| `raceGsdStealLiveHolder(extraStealers)` | orquestación de los tres tiempos en el padre | `test/gsd-lock-race.test.js` | `[86-02]` |
| `waitUntil(pred, timeoutMs)` | espera acotada sobre estado de disco | `test/gsd-lock-race.test.js` | `[86-02]` |
| `readStealReasons(dir)` | lector never-throws del marcador lateral | `test/gsd-lock-race.test.js` | `[86-02]` |
| `assertCasExercised(dir, ctx)` | guard de cobertura anti-degradación silenciosa | `test/gsd-lock-race.test.js` | `[86-02]` |
</artifacts_produced>

<flagged_assumptions>
## Supuestos marcados — sondas de requisito sin resolver

Esta fase no tiene `SPEC.md`; se disparó el fallback de sonda spec-less. La sonda determinista recorrió LOCK-04..07 y devolvió **4 aplicables / 0 resueltos / 4 sin resolver**, las cuatro `unclassified`. Una fila `unclassified` no se auto-resuelve ni se descarta en silencio:

| Requisito | Estado de la sonda | Supuesto que asume este plan |
|-----------|--------------------|------------------------------|
| LOCK-04 | `unclassified` / `unresolved` | Cubierto por `86-01`: filas LOCK-04 (a)-(d) de `86-VALIDATION.md` levantadas a `must_haves.truths` con su comando. |
| LOCK-05 | `unclassified` / `unresolved` | Cubierto aquí: filas LOCK-05 (a)-(c) → `must_haves.truths` de este plan, Tareas 1 y 2. |
| LOCK-06 | `unclassified` / `unresolved` | Cubierto aquí: fila LOCK-06 (b) automatizada por gates de fuente; la mordida propiamente dicha es **manual-only con evidencia citada** (D-15) y va como truth con `verification: backstop`. |
| LOCK-07 | `unclassified` / `unresolved` | Cubierto aquí: filas LOCK-07 (a)-(c); la (c) —desaparición de la premisa falsa— la cierra `86-01` con el CAS. |

**Lectura práctica, declarada sin adornos:** el motor de la sonda clasifica prosa de requisito en inglés y estos cuatro están escritos en español, que es la causa más probable del `unclassified` en bloque. Eso **no** los da por resueltos; la cobertura real la sostiene el mapa humano por requisito de `86-VALIDATION.md`, no la sonda.

**Supuesto adicional heredado del research (A1), aquí explícito:** se asume que el aparcamiento del stealer en el seam se mantiene holgadamente por debajo del umbral de edad del steal-guard en la máquina donde corra la suite. Si no lo hiciera, un stealer extra rompería el guard por edad y el escenario mediría la carrera de primer orden en vez de la de segundo — el guard de cobertura lo detectaría (dejaría de aparecer el marcador), y la salida admitida sería reducir el caso N=5 a un solo stealer con seam más observadores no-stealer. **Subir el umbral no es una salida.**
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.kodo.lock` en disco → `stealLock` | Cuerpo JSON no confiable, editable a mano; se parsea siempre en `try/catch` y degrada a «robable». Este plan documenta la ventana que queda abierta en esa frontera, no la amplía. |
| Proceso A ↔ proceso B sobre el mismo `lockPath` | Frontera multi-escritor. La ventana residual de 2 syscalls vive aquí, y este plan la **declara** en los dos sitios que D-17 exige. |
| Procesos hijo del harness ↔ sandbox del test | Marcadores y go-files. Superficie exclusiva de test, bajo un `mkdtempSync` borrado en el `afterEach`; no toca `$HOME` ni el repo del operador. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-08 | Tampering | ventana residual de 2 syscalls entre la sonda de identidad y el `renameSync` | medium | accept | **Aceptada y declarada** (LOCK-07): TOCTOU residual no cerrable sin soporte atómico del FS. Declarada en el JSDoc de `stealLock` y en `.planning/STATE.md` con su clase nombrada y sin presentarla como cierre por construcción. Trigger de revisión: LOCK-F1 en v2, si la ventana se manifiesta en uso real. |
| T-86-09 | Tampering | residual del residual: contenido byte-idéntico **con** reutilización de inodo | low | accept | Implicaría mismo `session_id` y mismo `acquired_at` al milisegundo entre dos procesos distintos; el clobber resultante sería semánticamente un no-op. Declarado en una línea del JSDoc, sin defensa dedicada (Pitfall 1). |
| T-86-10 | Denial of service | steal-guard retenido por el stealer aparcado durante el escenario de test | low | mitigate | El aparcamiento espera una **barrera** con techo de 3.000 ms, claramente por debajo del umbral de edad del guard; el seam es no-op en producción y ningún call site real lo pasa. |
| T-86-11 | Repudiation | cobertura del CAS apagándose en silencio en una futura edición del harness | medium | mitigate | `assertCasExercised` exige ≥ 1 marcador `lock-replaced-mid-steal` por iteración y falla con mensaje propio; perder cobertura pasa a ser un fallo, no un silencio (precedente medido en 83-06). |
| T-86-12 | Tampering | mutación de `.planning/STATE.md` por herramienta equivocada | low | mitigate | `Edit` de alcance quirúrgico sobre la sección elegida —nunca `Write` de fichero completo— seguido de `state.validate` con `valid: true` sin drift (precedente 85-05). |
| T-86-SC | Tampering | instalaciones npm/pip/cargo | low | accept | **Cero instalaciones en esta fase** (`86-RESEARCH.md` §Package Legitimacy Audit: no aplica). Aseverado por la ausencia de `package.json`/`package-lock.json` en el diff de la fase. No procede checkpoint de legitimidad porque no hay paquete que verificar. |
</threat_model>

<verification>
**Baseline de la fase, medido en vivo contra `dcddc80`:** `npm test` = `# tests 2590` (`# pass` 2589 · `# skipped` 1 · `# fail` 0) · `test/gsd-lock-race.test.js` = 4 tests · `process.stdout.write` en el helper = 7 · `DEAD_PID` en el fichero de carrera = 0 · `sleepSync` en el fichero de carrera = 0 · `TOCTOU residual`, `Phase 83`, `ventana residual`, `pid: process.pid` (helper), `waitUntil`, `lock-replaced-mid-steal` = 0 todos.

1. `node --test test/gsd-lock-race.test.js` → 0 fail, 6 casos (4 en HEAD + 2 de la fase), tres corridas consecutivas verdes.
2. `npm test` → `# tests` ≥ **2591**, estrictamente mayor que el baseline medido `2590`, 0 fail.
3. Gates de fuente que **muerden**: `TOCTOU residual` ≥ 1 y `Phase 83` ≥ 1 en `src/gsd/lock.js`; `ventana residual` ≥ 1 en `.planning/STATE.md`; `pid: process.pid` ≥ 1 (sin comentarios) en el helper. Gates **anti-regresión** —ya ciertos en HEAD, salvaguardas y no evidencia de trabajo—: `DEAD_PID` = 0 y `sleepSync` = 0 en `test/gsd-lock-race.test.js`, `9999` = 1 en el mismo fichero, y los dos umbrales del lock intactos.
4. `state.validate` → `valid: true` sin drift.
5. Gate de consumidores: `git diff --name-only <phase_base_sha>..HEAD -- src/ test/` lista exactamente los cuatro ficheros de la fase.
6. **Manual-only, con evidencia citada en el SUMMARY:** (a) la mordida —diff de la reversión, salida `# fail` con su mensaje, salida verde tras restaurar, conteos antes/después—; (b) la revisión de honestidad del texto de la ventana residual contra los cuatro elementos de D-17.
</verification>

<success_criteria>
- El harness siembra un holder VIVO —TTL vencido con PID vivo, nunca un PID muerto— y demuestra cardinalidad exacta con un release concurrente, con N=3 y con N=5 (LOCK-05).
- El escenario prueba por marcador que ejerció la rama del CAS; perder esa cobertura es un fallo con mensaje propio, no un silencio (LOCK-05 c).
- Revertir a mano el CAS pone el harness rojo, y la evidencia queda registrada con diff, salida roja y salida verde restaurada; ningún assert, timeout ni presupuesto se tocó (LOCK-06).
- La ventana residual de dos syscalls contiguos está declarada en el JSDoc de `stealLock` y en `STATE.md`, con su clase de riesgo nombrada y nunca como cierre por construcción (LOCK-07).
- La suite completa termina en o por encima del baseline con 0 fallos y los consumidores del lock no cambiaron de comportamiento (criterio 5).
</success_criteria>

<output>
Crear `.planning/phases/86-cas-sim-trico-de-steallock-holder-vivo/86-02-SUMMARY.md` al terminar, incluyendo obligatoriamente el bloque de evidencia de la mordida (diff de la reversión · salida `# fail` con su mensaje · salida verde restaurada · conteos de suite antes y después), el tiempo real de aparcamiento medido, y el resultado del gate de consumidores intactos.
</output>
