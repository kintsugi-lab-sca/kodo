---
id: 31-01
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skill/sync.js
  - test/skill-sync.test.js
autonomous: true
requirements: [ADVISORY-01]
requirements_addressed: [ADVISORY-01]
tags: [skill-sync, di, callback, advisory]

must_haves:
  truths:
    - "syncSkill({...opts, onConsoleWarn}) acepta callback opcional y lo invoca en lugar de console.warn cuando se inyecta."
    - "Cuando onConsoleWarn no se provee (undefined), syncSkill llama console.warn directamente — back-compat byte-exact."
    - "Test captura el warning del prune sin monkey-patch global de console.warn ni spy de proceso."
    - "Source-hygiene mantenida: src/skill/sync.js NO importa picocolors; el callback recibe string ya formateado."
  artifacts:
    - path: "src/skill/sync.js"
      provides: "syncSkill con parámetro onConsoleWarn opcional"
      contains: "onConsoleWarn"
    - path: "test/skill-sync.test.js"
      provides: "Describe block 'syncSkill onConsoleWarn DI' con 2 tests netos (callback path + default fallback path)"
      contains: "syncSkill onConsoleWarn DI"
  key_links:
    - from: "src/skill/sync.js#117"
      to: "opts.onConsoleWarn || console.warn"
      via: "destructuring + nullish coalescing"
      pattern: "onConsoleWarn"
    - from: "test/skill-sync.test.js"
      to: "syncSkill({source, dest, prune, onConsoleWarn: capture})"
      via: "invocación in-process Suite 1 style"
      pattern: "onConsoleWarn:"
---

<objective>
ADVISORY-01: refactorizar `syncSkill` para aceptar `onConsoleWarn` callback opcional inyectable. El único `console.warn` directo del módulo (línea 117, foreign-prune en `--prune`) pasa a invocar `onConsoleWarn(msg)` cuando se provee, fallback a `console.warn(msg)` cuando es `undefined`. Tests capturan el warning sin monkey-patch global.

Purpose: cierra Phase 21 WR-04 ("pureza del módulo skill/sync"). El módulo era pure-but-not-fully — `console.warn` directo lo hacía no-mockable sin spy global. ADVISORY-01 elimina la última fuga de side-effect global del módulo "puro" (D-08 SoSoT).

Output: `src/skill/sync.js` con `onConsoleWarn` añadido al typedef + destructuring + warn callable; `test/skill-sync.test.js` con describe block `'syncSkill onConsoleWarn DI'` con 2 tests netos (captura DI + default fallback byte-exact). Suite global preserva los 7 tests pre-existentes de syncSkill Suite 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-phase-21-22-advisory-cleanup/31-CONTEXT.md
@.planning/phases/31-phase-21-22-advisory-cleanup/31-PATTERNS.md
@src/skill/sync.js
@test/skill-sync.test.js

<interfaces>
<!-- Contracts the executor needs. Extracted from src/skill/sync.js + 31-PATTERNS.md. -->

Current SyncSkillOpts (src/skill/sync.js:25-31):
  - source: string
  - dest: string
  - prune?: boolean
  - logger?: import('../logger.js').Logger

Current return shape SyncSkillResult (src/skill/sync.js:33-39):
  - status: 'ok' | 'noop' | 'error'
  - files_changed: number
  - files_pruned?: number
  - symlink_replaced?: boolean
  - error?: string

Target signature addition (D-01):
  - onConsoleWarn?: (msg: string) => void
  - Default semantics: when undefined, use console.warn (preserves Phase 21 Test 6 behavior byte-exact).
  - Per CONTEXT D-01: "Cuando undefined, default = console.warn." → patrón A (default observable) explícitamente permitido aquí porque console.warn ya era el side-effect previo (no introduce nuevo observable, solo permite override).

Callsite to modify (src/skill/sync.js:111-122):
  - Inside `if (prune === true)` branch
  - Line 117: `console.warn(\`[kodo skill sync --prune] removing foreign: ${relPath}\`);`
  - Replace with reference to local `warn` variable (resolved at top of function from opts).

Test fixture analog (test/skill-sync.test.js:38-79):
  - makeFixture() returns { tmpHome, tmpRepo, skillDir }
  - destOf(tmpHome) / sourceOf(tmpRepo) helpers
  - afterEach with chmodSync restore + rmSync recursive

Existing Test 6 to preserve (test/skill-sync.test.js:~197-223):
  - 'Test 6: prune=true → foráneo borrado con console.warn previo'
  - Uses spy: `console.warn = (msg) => { warns.push(...) }` + try/finally restore
  - This test MUST remain passing (it now exercises the default fallback path) — do not delete it.
</interfaces>

<patterns>
DI Default Pattern (nullish coalescing for observable side-effect):
  - `const warn = onConsoleWarn ?? console.warn;`
  - Diferencia con `cleanupFn` (Plan 31-02) que usa `if (fn) await fn()` — aquí podemos usar default porque console.warn es la behavior previa (preserva back-compat byte-exact).

HOME-isolation fixture (reusable verbatim):
  - makeFixture + afterEach pattern documentado en 31-PATTERNS.md líneas 247-263, 588-596.

Source-hygiene assert (preservar tras cambios, ya presente test/skill-sync.test.js:382-384):
  - `assert.equal(/picocolors/.test(stripComments(syncMod)), false);`
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir onConsoleWarn DI a syncSkill</name>
  <files>src/skill/sync.js</files>
  <read_first>
    - src/skill/sync.js (estado actual — typedef líneas 25-31, función líneas 50-138, callsite warn línea 117)
    - .planning/phases/31-phase-21-22-advisory-cleanup/31-PATTERNS.md (sección 'src/skill/sync.js — ADVISORY-01', líneas 19-87)
    - .planning/phases/31-phase-21-22-advisory-cleanup/31-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Implementa la extensión D-01/D-02/D-03 (`onConsoleWarn` callback opcional) en `src/skill/sync.js`:

    1. **Typedef `SyncSkillOpts` (líneas 25-31)** — añadir campo opcional `onConsoleWarn?: (msg: string) => void` después de `logger?`. Mantener orden documentado (source, dest, prune, logger, onConsoleWarn).
    2. **Función `syncSkill(opts)` línea 50** — extender el destructuring para incluir `onConsoleWarn`. Crear variable local `warn` resuelta vía nullish coalescing: `warn = onConsoleWarn ?? console.warn`. Resolver la variable DESPUÉS del destructuring (línea ~51), ANTES del `try { ... }` (línea 56) para que esté en scope del prune block.
    3. **Callsite línea 117** — reemplazar `console.warn(\`[kodo skill sync --prune] removing foreign: ${relPath}\`);` por `warn(\`[kodo skill sync --prune] removing foreign: ${relPath}\`);`. Mensaje literal byte-exact preservado.
    4. **Invariantes:** NO añadir nuevos imports (callback es runtime-only). NO importar `picocolors`. NO modificar `walkFiles`, hash logic, symlink-replace logic, ni prune logic — solo el callsite del warn. NO cambiar el shape del return.
    5. **JSDoc de la función** (líneas 42-49) — actualizar el comentario "Función pura" si es necesario para mencionar que el callback opcional sustituye `console.warn` (literal: añadir frase tipo "Cuando `opts.onConsoleWarn` se inyecta, reemplaza la llamada a `console.warn` del prune; D-01 ADVISORY-01."). Mantener el resto del JSDoc verbatim.

    El default `console.warn` (vía `??`) es explícitamente correcto aquí porque `console.warn` ya era la behavior pre-Phase-31 — no introduce un side-effect nuevo, solo permite override (per CONTEXT D-01).
  </action>
  <verify>
    <automated>node --test test/skill-sync.test.js 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "onConsoleWarn" src/skill/sync.js` retorna ≥3 matches (typedef + destructuring + warn resolution).
    - `grep -n "console.warn" src/skill/sync.js` retorna exactamente 0 matches en runtime (puede aparecer en JSDoc/comentarios; stripComments-friendly check: `grep -v '^\s*[/*]' src/skill/sync.js | grep -c "console.warn"` = 0).
    - `grep -c "removing foreign:" src/skill/sync.js` retorna exactamente 1 (mensaje literal preservado).
    - `node --test test/skill-sync.test.js` pasa los 36+ tests pre-existentes incluyendo Test 6 (default fallback) sin modificación.
    - El módulo NO importa picocolors: `grep -c "picocolors" src/skill/sync.js` retorna 0.
  </acceptance_criteria>
  <done>
    src/skill/sync.js acepta `opts.onConsoleWarn`, lo usa con default `console.warn` byte-exact, los tests Phase 21 (incl. Test 6) siguen verdes sin cambios.
  </done>
</task>

<task type="auto">
  <name>Task 2: Añadir describe 'syncSkill onConsoleWarn DI' a test/skill-sync.test.js</name>
  <files>test/skill-sync.test.js</files>
  <read_first>
    - test/skill-sync.test.js (estado actual — fixture makeFixture líneas 38-79, Suite 1 syncSkill líneas 93-250, Test 6 prune-warn pre-existente ~líneas 197-223)
    - .planning/phases/31-phase-21-22-advisory-cleanup/31-PATTERNS.md (sección 'test/skill-sync.test.js — ADVISORY-01 + ADVISORY-02', líneas 239-382 — especialmente el bloque 'ADVISORY-01 — patrón nuevo (sin spy global)' líneas 286-307)
    - src/skill/sync.js (post-Task-1: para confirmar que onConsoleWarn ya está disponible)
  </read_first>
  <action>
    Añade un nuevo describe block `'syncSkill onConsoleWarn DI (ADVISORY-01)'` al archivo `test/skill-sync.test.js`. Insertarlo **después** del bloque Suite 1 `describe('syncSkill (unit, in-process)', ...)` actual (~línea 250) y **antes** de Suite 2 (CLI integration spawnSync).

    El bloque contiene exactamente 2 tests `it()` netos (D-15 → ~2-3 tests por advisory):

    1. **Test A — "captura warning vía callback sin spy global"**:
       - Reusa `makeFixture()` + afterEach pattern del Suite 1 (variables `_tmpHome`, `_tmpRepo`).
       - Setup: `({ tmpHome: _tmpHome, tmpRepo: _tmpRepo } = makeFixture())`; computa `source = sourceOf(_tmpRepo)`, `dest = destOf(_tmpHome)`.
       - Primera invocación: `syncSkill({ source, dest })` — siembra dest sin prune.
       - Crea archivo foráneo en dest: `writeFileSync(join(dest, 'foreign.md'), 'local override\n', 'utf-8')`.
       - Array local `const warns = []`. Segunda invocación con prune + callback: `syncSkill({ source, dest, prune: true, onConsoleWarn: (msg) => warns.push(msg) })`.
       - Asserts: `result.files_pruned === 1`; `warns.length === 1`; `assert.match(warns[0], /\[kodo skill sync --prune\] removing foreign: foreign\.md/)`.
       - NO modifica `console.warn` (ni global ni shadow) — confirma que el patrón DI evita la mutación global.
       - Assertion explícita: verifica que `console.warn` real NO fue llamado. Patrón: snapshot inicial `const before = console.warn`, ejecutar, assert `console.warn === before` (referencia inmutable).

    2. **Test B — "default fallback usa console.warn cuando onConsoleWarn no se inyecta"**:
       - Mismo setup fixture.
       - Sin callback: `syncSkill({ source, dest, prune: true })` (con foreign sembrado igual que Test A).
       - Captura via override transitorio del Suite 1 Test 6 pattern: `const orig = console.warn; const warns = []; console.warn = (m) => warns.push(String(m)); try { ... } finally { console.warn = orig }`.
       - Asserts: `result.files_pruned === 1`; `warns.some(w => /removing foreign/.test(w))`.
       - Este test BLINDA que la back-compat por default (D-03) está intacta — si alguien rompe el `??` default, este test falla.

    **Imports a verificar/añadir al top del archivo (sin duplicar):** `syncSkill` ya importado línea 28. `makeFixture`, `destOf`, `sourceOf` ya son funciones top-level. `mkdtempSync`, `writeFileSync`, `join`, `tmpdir`, `chmodSync`, `rmSync`, `assert` ya importados.

    **NO añadir:** spawnSync invocations (el test es in-process unit, Suite 1 style). NO tocar Suite 2 (CLI integration) ni los tests Test 1-8 existentes.
  </action>
  <verify>
    <automated>node --test test/skill-sync.test.js 2>&amp;1 | grep -E "(syncSkill onConsoleWarn DI|tests|pass|fail)" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "syncSkill onConsoleWarn DI" test/skill-sync.test.js` retorna exactamente 1 (describe block name).
    - `grep -c "onConsoleWarn:" test/skill-sync.test.js` retorna ≥1 (callback en invocación de syncSkill).
    - `node --test test/skill-sync.test.js` exit code 0 con +2 tests netos vs baseline (baseline reportado: 36+ → ahora ≥38). Output incluye describe `'syncSkill onConsoleWarn DI (ADVISORY-01)'` con 2 `pass`.
    - Test A nunca mutates `console.warn` (verificable: ejecutar `node --test test/skill-sync.test.js --test-name-pattern "captura warning vía callback"` y confirmar pass sin warnings stderr).
    - Test B falla si se elimina el default `?? console.warn` en src/skill/sync.js (regression guard).
  </acceptance_criteria>
  <done>
    Describe block `'syncSkill onConsoleWarn DI (ADVISORY-01)'` añadido con 2 tests pasando. Suite global sube ≥+2 tests. Tests pre-existentes intactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `syncSkill` opts → callback | El callback `onConsoleWarn` es invocado con string formateado por syncSkill. Caller controla la función — sin input externo. |
| Test fixture → filesystem | makeFixture crea tmpdirs vía `mkdtempSync` (POSIX-safe). afterEach limpia. No hay path traversal exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Information disclosure | `onConsoleWarn` callback recibe path foráneo | accept | El path es relative al dest sembrado por el propio caller (no input externo); el mensaje ya era visible via console.warn pre-Phase-31. Sin cambio de surface. |
| T-31-01-02 | Tampering | Inyección de callback malicioso | accept | El caller es código de usuario in-process (no IPC ni RPC). Quien controla los opts controla todo el proceso — sin escalación. |
| T-31-01-03 | Repudiation | Pérdida de log del prune | mitigate | Default fallback (`?? console.warn`) preserva el side-effect observable para callers que NO inyectan callback (D-03 back-compat). Test B blinda regresión. |

ASVS L1: este plan no introduce nuevo input externo, ni cambia el threat surface efectivo. El callback es interno (Node module-level), sin crossing de trust boundaries reales.
</threat_model>

<verification>
- `node --test test/skill-sync.test.js` exit 0 con describe `'syncSkill onConsoleWarn DI (ADVISORY-01)'` pasando (+2 tests netos).
- `node --test` global suite no regresiona (baseline post-Phase-30 ≥884 → ≥886 post-Task-2).
- `grep -v '^\s*[/*]' src/skill/sync.js | grep -c "console.warn"` retorna 0 (no runtime console.warn directo).
- `grep -c "picocolors" src/skill/sync.js` retorna 0 (color isolation preservado).
</verification>

<success_criteria>
SC#1 ROADMAP byte-exact satisfacha:
- `syncSkill({onConsoleWarn})` acepta callback ✓
- Default = console.warn cuando no se inyecta ✓
- Test captura warning sin monkey-patch global ✓ (Test A del nuevo describe)
- Back-compat preservada para callers existentes ✓ (Test B blinda el default; `src/cli/skill-sync.js#43` y `src/orchestrator/launch.js#90` continúan invocando syncSkill sin opts.onConsoleWarn sin cambio observable)
</success_criteria>

<output>
After completion, create `.planning/phases/31-phase-21-22-advisory-cleanup/31-01-SUMMARY.md` documentando:
- Modificaciones byte-level a `src/skill/sync.js` (typedef + destructuring + warn variable + callsite)
- 2 nuevos tests añadidos a `test/skill-sync.test.js` con line ranges
- Conteo pre/post de tests del archivo y suite global
- Confirmación de invariantes preservados (color isolation, no nuevos imports, walkFiles/hash intactos)
</output>
