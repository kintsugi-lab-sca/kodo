---
id: 31-03
phase: 31
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/launch.js
  - test/launch.test.js
autonomous: true
requirements: [ADVISORY-03]
requirements_addressed: [ADVISORY-03]
tags: [orchestrator, launch, di, spawn, lifecycle, ndjson, advisory]

must_haves:
  truths:
    - "launchOrchestrator(opts) acepta `opts.spawnFn` opcional como lifecycle simulator hook (Opción A — ver decisión en <context>)."
    - "En producción (callers existentes que no pasan spawnFn), el comportamiento de launchOrchestrator es byte-exact pre-Phase-31: cmux.newWorkspace + setColor + send + notify — el lifecycle real lo hace claude binary dentro del workspace cmux vía cmux.send."
    - "Cuando opts.spawnFn se provee, launchOrchestrator lo invoca DESPUÉS de cmux.send y ANTES del return, pasando `{ workspaceRef, sessionId, projectPath, kodoDir, taskRef }` (taskRef='kodo-orchestrator' para esta rama)."
    - "Test del ADVISORY-03 inyecta spawnFn que ejecuta `node -e <inline-script>` real con env HOME=tmpHome aislado. Inline script simula el lifecycle: addSession + sessionStart emit (NDJSON head-line con session.start + transcript_path)."
    - "Post-await launchOrchestrator + await spawn-child exit, observables verificables: state.json contiene el SessionRecord; ~/.kodo/logs/<sessionId>.ndjson primera línea parseable con event=session.start + transcript_path no vacío + task_id correcto."
    - "cmux client es stubeado vía SHIM BINARY canónico (`${_tmpHome}/bin/cmux` con chmod 0o755) + `${_tmpHome}/.kodo/config.json` con `cmux.binary` apuntando al shim. cmux real NUNCA se invoca en el test."
    - "Test setup setea `process.env.HOME = _tmpHome` ANTES de cualquier dynamic import de módulos src/ que toquen homedir() (CR-02 pattern, replicado de test/session-start-event.test.js). NO usa `process.env.KODO_DIR` (no existe en src/config.js — KODO_DIR se deriva de homedir() join '.kodo')."
  artifacts:
    - path: "src/orchestrator/launch.js"
      provides: "launchOrchestrator con opts.spawnFn DI hook (default undefined → no-op)"
      contains: "spawnFn"
    - path: "test/launch.test.js"
      provides: "Describe block 'launchOrchestrator real spawn observables (ADVISORY-03)' con 1 test integration end-to-end"
      contains: "launchOrchestrator real spawn observables"
  key_links:
    - from: "src/orchestrator/launch.js (post-cmux.send, pre-return)"
      to: "opts.spawnFn invocation si presente"
      via: "if-guard, sin default"
      pattern: "spawnFn"
    - from: "test/launch.test.js spawnFn DI"
      to: "child_process.spawn('node', ['-e', inlineScript], {env: {HOME: _tmpHome, KODO_DIR: ...}})"
      via: "real spawn → addSession + sessionStart emit"
      pattern: "process.execPath"
    - from: "Inline script (node -e)"
      to: "addSession + sessionStart (dynamic import desde REPO root)"
      via: "src/session/state.js#addSession + src/logger-events.js#sessionStart"
      pattern: "session.start"
    - from: "Test assertions"
      to: "loadState() state.sessions[taskId] + readFirstLine(NDJSON path) + JSON.parse"
      via: "filesystem read post-spawn exit"
      pattern: "transcript_path"
---

<objective>
ADVISORY-03: añadir `spawnFn` DI hook opcional a `launchOrchestrator` y crear un test integration que valida observables post-launch reales (state.json + NDJSON head-line con `session.start`).

Purpose: cierra Phase 21 WR-06 ("test launchOrchestrator real, no mockSpawn-only"). Pre-Phase-31, `test/launch.test.js` cubre `applyReportingGate` (helpers puros) + source-hygiene de `launch.js`, pero NO ejecutaba `launchOrchestrator` runtime con observables reales. ADVISORY-03 añade un test integration end-to-end que simula el lifecycle downstream que claude haría dentro del cmux workspace — sin requerir claude/cmux reales y sin tocar el HOME del usuario.

Output: `src/orchestrator/launch.js` con `opts.spawnFn` añadido a la JSDoc/signature (default `undefined` → no-op invariante para callers de producción); `test/launch.test.js` con describe `'launchOrchestrator real spawn observables (ADVISORY-03)'` con 1 test integration que asserta state.json + NDJSON observables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

## DECISIÓN ARQUITECTÓNICA — Opción A (Lifecycle Simulator Hook)

CONTEXT.md D-09/D-10/D-14 asume que `launchOrchestrator` hace `child_process.spawn`. **Realidad verificada**: `launchOrchestrator` solo invoca `cmux.*` (newWorkspace, listWorkspaces, send, setColor, notify) — NO hay `child_process.spawn`/`exec`/`fork` en `src/orchestrator/launch.js`. El lifecycle real lo hace el binario `claude` que cmux arranca dentro del workspace tras `cmux.send`.

**Adoptada por este plan:** **Opción A — "Lifecycle simulator hook"** (recomendada por el orquestador en critical_planning_input).

`opts.spawnFn` se interpreta como un hook DI OPCIONAL que `launchOrchestrator` invoca **DESPUÉS** de `cmux.send` y **ANTES** del `return { workspace: workspaceRef, existing: false }`. Recibe un objeto con `{ workspaceRef, sessionId, projectPath, kodoDir, taskRef }`. En producción (callers existentes), `spawnFn` es `undefined` → el `if (opts.spawnFn)` lo elide silenciosamente: el lifecycle real lo hace claude binary dentro del workspace cmux (vía la canónica `cmux.send`). En tests, `spawnFn` es inyectado y ejecuta el `node -e <inline-script>` que simula `addSession` + NDJSON `session.start` con `transcript_path` populated.

**Justificación de la elección:**
1. Preserva la intención de SC#3 (validar observables reales post-lifecycle).
2. Mantiene el patrón DI ya establecido en Phase 31 (`onConsoleWarn` en 31-01, `cleanupFn` en 31-02 — todos NULL+if-guard).
3. Default `undefined` (no `child_process.spawn` real default) preserva back-compat byte-exact: el bundle de prod no carga `child_process` desde `launch.js`.
4. Produce un test que valida que `launchOrchestrator` engancha correctamente al sistema de sesiones — verifica el contrato observable, no la fidelidad del runtime cmux.
5. NO requiere refactor de cmux client ni mock de proceso real claude.

**Opción B descartada** (test fixture externo que ejecuta spawn fuera del SUT): más limpio para `src/launch.js` pero artificial en test — no valida que `launchOrchestrator` engancha al lifecycle.

**Opción C descartada** (mover ADVISORY-03 a v0.9+ backlog): violaría ROADMAP SC#4 (deferred section → 0 items).

## Referencias canónicas

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-phase-21-22-advisory-cleanup/31-CONTEXT.md
@.planning/phases/31-phase-21-22-advisory-cleanup/31-PATTERNS.md
@src/orchestrator/launch.js
@test/launch.test.js
@test/orchestrator-launch-isolation.test.js
@src/session/state.js
@src/logger-events.js
@src/logs/head-line.js
@src/hooks/session-start.js

<interfaces>
<!-- Contracts the executor needs. Extracted from src/orchestrator/launch.js + 31-PATTERNS.md. -->

Current launchOrchestrator signature (src/orchestrator/launch.js:62-67):
```
/**
 * Launch the orchestrator Claude session in a dedicated cmux workspace.
 * @param {{ logger?: import('../logger.js').Logger }} [opts]
 */
export async function launchOrchestrator(opts = {}) { ... }
```

Target addition (Opción A):
```
@param {{
  logger?: import('../logger.js').Logger,
  spawnFn?: (ctx: { workspaceRef: string, sessionId: string, projectPath: string, kodoDir: string, taskRef: string }) => Promise<void> | void,
}} [opts]
```

Call site insertion: post-`await cmux.send(...)` (línea ~184), post-`await cmux.notify(...)` (línea ~191), pre-`console.log(... Orchestrator launched...)` (línea ~193). Pattern:
```
if (opts.spawnFn) {
  await opts.spawnFn({
    workspaceRef,
    sessionId,
    projectPath: process.cwd(),
    kodoDir: join(homedir(), '.kodo'),
    taskRef: ORCHESTRATOR_WORKSPACE_NAME,
  });
}
```
(Variables ya presentes en scope: `workspaceRef`, `sessionId`, `process.cwd()`, `homedir()`, `ORCHESTRATOR_WORKSPACE_NAME`. `homedir` ya importado línea 6, `join` desde 'node:path' línea 4. **CRÍTICO: `src/config.js#6` define `const KODO_DIR = join(homedir(), '.kodo')` SIN env var override. NO usar `process.env.KODO_DIR` — no existe en config.js. El override de `process.env.HOME` es lo que reroutea todo.**)

addSession signature (src/session/state.js:117-125 — target del inline-script):
```
export function addSession(taskId, session, logger = noopLogger)
```
Session shape (typedef líneas 11-30): { workspace_ref, session_id, task_id, task_ref, provider, project_id, summary, status: 'running'|'done'|'error'|'review', started_at, project_path, gsd?: boolean, ... }

sessionStart emitter (src/logger-events.js:108-120):
```
export function sessionStart(logger, fields) {
  const transcript_path = fields.transcript_path ?? resolveTranscriptPath(fields.project_path, fields.session_id);
  logger.info(EVENTS.SESSION_START, { event: EVENTS.SESSION_START, session_id, task_id, provider, project_path, transcript_path, started_at });
}
```

readFirstLine helper (src/logs/head-line.js:33-52): lee primera línea bounded 64KB, retorna string.

cmux stub pattern existente (test/orchestrator-launch-isolation.test.js:19-39):
- KODO_ROOT env override + stripComments source-hygiene
- ESTE TEST PRE-EXISTENTE NO INVOCA launchOrchestrator runtime — solo grep-asserta source-hygiene de --worktree absence.
- Para ADVISORY-03 necesitamos invocar launchOrchestrator runtime, lo cual implica mockear cmux client. Estrategia: usar Node's experimental loader `--import` con un module mock, O reemplazar el cmux module via `import.meta.resolve` override (no soportado), O — más práctico — usar un **wrapper helper** dentro del test que importa launch.js + reescribe el cmux client global via setter (no posible — ESM imports son immutable).

cmux client API (src/cmux/client.js — read-only inspect):
- `newWorkspace({name, cwd}) → Promise<workspaceRef>` retorna string tipo `workspace:N`.
- `listWorkspaces() → Promise<string>` retorna salida textual.
- `send({workspace, text}) → Promise<void>`.
- `setColor({workspace, color}) → Promise<void>`.
- `notify({title, body, workspace}) → Promise<void>`.

**Estrategia cmux stub para test ADVISORY-03 (D-12):**
cmux client comunica con un binario cmux externo. En tests, si la binary no está disponible, cmux throws. Estrategia: stub vía env var `KODO_CMUX_BIN_OVERRIDE` ó variable de entorno que la propia cmux/client.js respete. Verifica `src/cmux/client.js` para descubrir si tiene env var override (tipo `KODO_CMUX_FAKE=1`). Si NO existe override, esta task requiere añadir uno mínimo en cmux/client.js — pero eso sale del scope (Phase 31 NO refactoriza cmux). **Fallback aceptable:** crear un stub mediante un binario shim escrito a `tmpHome/bin/cmux` que retorna outputs canónicos a stdin/stdout y poner ese path al frente de `PATH`. Documenta esto en la ejecución y descubre durante Task 1 lectura de cmux/client.js si hay path simpler.

KODO_DIR override mechanism (CORRECTO, verificado contra src/config.js#6): `KODO_DIR = join(homedir(), '.kodo')` — derivado puramente de `homedir()` (que respeta `process.env.HOME` en Node 18+). **NO hay env var `KODO_DIR` consumida por config.js.** Por lo tanto el test override es:
1. `process.env.HOME = _tmpHome` ANTES de cualquier dynamic import de state.js/config.js (CR-02 pattern).
2. El subprocess `node -e` hereda `HOME=_tmpHome` via `env: { ...process.env, HOME: _tmpHome }`. NO setear `KODO_DIR` en el env del child — sería inerte.
3. Resultado: todos los reads/writes a `~/.kodo/` se redirigen a `_tmpHome/.kodo/`.

Existing test/launch.test.js structure (líneas 1-125):
- Imports: describe, it, readFileSync, applyReportingGate.
- 2 describe blocks: 'REPORT-03 applyReportingGate' (LG1-LG8) y 'REPORT-03 launch.js source hygiene' (LH1-LH3).
- NO existe mockSpawn pre-existente (a pesar de lo que CONTEXT.md insinúa — verificado leyendo el archivo completo).
- Inserta el nuevo describe AL FINAL del archivo (después del describe LH3).
</interfaces>

<patterns>
DI Default Pattern (NULL + if-guard):
```
const spawnFn = opts.spawnFn; // sin default
// ... cuerpo entero ...
if (spawnFn) await spawnFn(ctx);  // post-cmux.send, pre-return
```

Inline-script idiom (analog src/hooks/session-start.js:219-233 — dynamic import + try/catch fail-open):
```
const inlineScript = `
  process.env.HOME = ${JSON.stringify(_tmpHome)};
  const { addSession } = await import(${JSON.stringify('file://' + join(REPO, 'src/session/state.js'))});
  const { createLogger } = await import(${JSON.stringify('file://' + join(REPO, 'src/logger.js'))});
  const { sessionStart } = await import(${JSON.stringify('file://' + join(REPO, 'src/logger-events.js'))});
  const sessionId = '...';
  const taskId = '...';
  addSession(taskId, { ...session shape... });
  const log = createLogger({ sessionId }).child({ component: 'test' });
  sessionStart(log, { session_id: sessionId, task_id: taskId, provider: 'plane', project_path: process.cwd(), started_at: new Date().toISOString() });
  process.exit(0);
`;
```

NOTA crítica: el inline script DEBE usar `file://` URLs en `await import(...)` porque Node ESM requiere URLs no-relativas para dynamic imports desde un -e script. NO usar paths relativos.

HOME-isolation fixture (verbatim del 31-PATTERNS.md líneas 580-595):
```
const _tmpHome = mkdtempSync(join(tmpdir(), 'kodo-launch-'));
const _kodoDir = join(_tmpHome, '.kodo');
mkdirSync(_kodoDir, { recursive: true });
mkdirSync(join(_kodoDir, 'logs'), { recursive: true });
// afterEach: rmSync(_tmpHome, { recursive: true, force: true });
```

Real spawn helper:
```
import { spawn as realSpawn } from 'node:child_process';
async function runInlineNode(script, env) {
  return new Promise((resolve, reject) => {
    const child = realSpawn(process.execPath, ['-e', script], { env, stdio: ['ignore', 'pipe', 'pipe'] });
    let stdout = '', stderr = '';
    child.stdout.on('data', d => stdout += d);
    child.stderr.on('data', d => stderr += d);
    child.on('error', reject);
    child.on('exit', code => resolve({ code, stdout, stderr }));
  });
}
```
</patterns>

<noopLogger>
Patrón `noopLogger` (src/logger-noop.js — referencia, asumido existente per 31-PATTERNS.md):
```
import { noopLogger } from '../src/logger-noop.js';
await launchOrchestrator({ logger: noopLogger, spawnFn });
```
Si `src/logger-noop.js` no existe (verificar en Task 2 reading), crear inline en el test un mock con `{ info: () => {}, warn: () => {}, error: () => {}, child: () => mock }`.
</noopLogger>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir spawnFn DI hook a launchOrchestrator</name>
  <files>src/orchestrator/launch.js</files>
  <read_first>
    - src/orchestrator/launch.js (estado actual — JSDoc líneas 62-66, función launchOrchestrator líneas 67-195, callsite post-cmux.send líneas 184-194)
    - .planning/phases/31-phase-21-22-advisory-cleanup/31-PATTERNS.md (sección 'src/orchestrator/launch.js — ADVISORY-03', líneas 182-237)
    - .planning/phases/31-phase-21-22-advisory-cleanup/31-CONTEXT.md (D-09 a D-14)
    - El bloque DECISIÓN ARQUITECTÓNICA — Opción A en el <context> de este PLAN.md (above)
  </read_first>
  <action>
    Refactor mínimo de `src/orchestrator/launch.js` para añadir el DI hook `opts.spawnFn` (Opción A — Lifecycle Simulator Hook):

    1. **JSDoc de `launchOrchestrator` (líneas 62-66)** — extender el typedef inline:
       - Añadir `spawnFn?: (ctx: { workspaceRef: string, sessionId: string, projectPath: string, kodoDir: string, taskRef: string }) => Promise<void> | void` al param object.
       - Añadir comentario in-file explicativo (4-6 líneas) sobre la decisión Opción A: "DI hook opcional invocado post-cmux.send/notify. Default undefined preserves prod behavior (lifecycle real lo hace claude binary en cmux workspace). Tests inyectan spawnFn para simular addSession + NDJSON session.start observables — ADVISORY-03 / Plan 31-03 / Opción A."

    2. **Cuerpo de `launchOrchestrator` (línea 67 onwards)** — añadir la invocación en el branch new-workspace:
       - Posición: en la rama "no existing" (la que SÍ crea workspace nuevo via `cmux.newWorkspace`), DESPUÉS de `await cmux.notify({...})` (línea ~191) y DESPUÉS del `console.log(...launched...)` (línea ~193), pero ANTES del `return { workspace: workspaceRef, existing: false }` (línea ~194).
       - Inserta un `if (opts.spawnFn) { ... }` guard que invoca `await opts.spawnFn(ctx)` donde `ctx` es un objeto con 5 fields:
         * `workspaceRef` — la variable ya en scope (línea ~144, return de `cmux.newWorkspace`).
         * `sessionId` — la variable ya en scope (línea ~153, `randomUUID()`).
         * `projectPath` — `process.cwd()` (mismo valor que `cmux.newWorkspace` recibe, línea ~146).
         * `kodoDir` — derivado de `join(homedir(), '.kodo')`. **NOTA: `src/config.js#6` define `KODO_DIR = join(homedir(), '.kodo')` SIN env var override. NO usar `process.env.KODO_DIR` (no existe en config.js). `homedir` ya importado línea 6 de launch.js; `join` ya importado línea 4.**
         * `taskRef` — el constant `ORCHESTRATOR_WORKSPACE_NAME` (línea 16).
       - **NO invocar spawnFn en la rama "existing"** (línea ~128 `return { workspace: match[1], existing: true }`). El hook simula el primer lifecycle; refresh-nudge no crea sesión nueva.

    3. **Invariantes:**
       - NO añadir `child_process` al top-level imports (la decisión Opción A es explícita: producción no usa spawn; tests proveen su propio spawn).
       - NO modificar el bloque auto-sync fail-open (líneas 87-110) — INTOCABLE per CONTEXT.md.
       - NO modificar cmux.* invocations (newWorkspace, setColor, send, notify, listWorkspaces) — preserved byte-exact.
       - NO modificar applyReportingGate / resolvePromptTemplate / buildContextSummary.
       - NO modificar source-hygiene blocked patterns (--worktree absent, cwd: process.cwd() preserved, Phase 18 D-06 comment preserved — blindado por test/orchestrator-launch-isolation.test.js).
       - NO añadir new top-level constants — los valores ya están en scope (`ORCHESTRATOR_WORKSPACE_NAME` line 16, `workspaceRef` línea 144, `sessionId` línea 153, `homedir` import línea 6, `join` import línea 4).

    4. **Comentario in-file Opción A** — añadir un bloque comment (5-8 líneas) JUSTO ANTES del `if (opts.spawnFn) { ... }` insertion explicando:
       - Por qué default undefined (prod no necesita spawn; cmux + claude binary hacen el lifecycle real)
       - Para qué sirve el hook (tests ADVISORY-03 inyectan spawnFn para simular addSession + sessionStart emitir NDJSON head-line)
       - Referenciar Plan 31-03 y la decisión Opción A
  </action>
  <verify>
    <automated>node --test test/launch.test.js test/orchestrator-launch-isolation.test.js 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spawnFn" src/orchestrator/launch.js` retorna ≥2 (JSDoc + invocación).
    - `grep -c "child_process" src/orchestrator/launch.js` retorna 0 (NO añadir import — Opción A).
    - `grep -E "if \(opts\.spawnFn\)" src/orchestrator/launch.js | wc -l` retorna 1 (única invocación, en branch new-workspace).
    - `grep -c "Opción A" src/orchestrator/launch.js` retorna ≥1 (comentario in-file documenta la decisión).
    - Tests pre-existentes NO regresionan: `node --test test/launch.test.js test/orchestrator-launch-isolation.test.js` exit code 0 (REPORT-03 LG1-LG8 + LH1-LH3 + source-hygiene D-06 todos pasan).
    - Source-hygiene `--worktree` absent en runtime: `grep -v '^\s*[/*]' src/orchestrator/launch.js | grep -c -- '--worktree'` retorna 0.
    - El bloque auto-sync (líneas ~87-110) intacto: `grep -c "skill.sync.auto" src/orchestrator/launch.js` retorna ≥1 (skillSyncAuto + skillSyncAutoError refs preservados).
  </acceptance_criteria>
  <done>
    `launchOrchestrator` acepta `opts.spawnFn`, lo invoca post-cmux.send/notify pre-return en branch new-workspace, comentario in-file documenta Opción A. Callers existentes (bin/kodo orchestrate) reciben byte-exact behavior. Source-hygiene tests pre-existentes pasan sin cambio.
  </done>
</task>

<task type="auto">
  <name>Task 2: Añadir describe 'launchOrchestrator real spawn observables' a test/launch.test.js</name>
  <files>test/launch.test.js</files>
  <read_first>
    - test/launch.test.js (estado actual — 2 describe blocks LG1-LG8 + LH1-LH3, imports en líneas 1-5, NO mockSpawn pre-existente)
    - src/orchestrator/launch.js (post-Task-1: spawnFn DI hook presente)
    - src/cmux/client.js (READ para descubrir si tiene env var override / mock hook que permita stub sin spawnar cmux binary real)
    - src/session/state.js (addSession signature líneas 117-125, loadState, KODO_DIR env var canónica)
    - src/logger-events.js (sessionStart helper líneas 108-120)
    - src/logs/head-line.js (readFirstLine líneas 33-52)
    - src/logger.js (createLogger signature — para inline script usage)
    - src/hooks/session-start.js (líneas 216-236, analog inline-script idiom)
    - .planning/phases/31-phase-21-22-advisory-cleanup/31-PATTERNS.md (sección 'test/launch.test.js — ADVISORY-03', líneas 386-543)
    - test/orchestrator-launch-isolation.test.js (cmux stub pattern + stripComments)
  </read_first>
  <action>
    Añade un nuevo describe block `'launchOrchestrator real spawn observables (ADVISORY-03)'` al **final** de `test/launch.test.js`, después del describe `'REPORT-03 — launch.js source hygiene (Phase 14 D-05 forward-looking)'` (~línea 125).

    **Estrategia cmux stub (Issue #2 — DEFINIDA, no descubrimiento):**

    `src/cmux/client.js` invoca el binario cmux via `execFile(loadConfig().cmux.binary, args)` (verificable en líneas 2-26 de cmux/client.js). Por lo tanto el stub se logra inyectando un binario shim en disco + un `config.json` que apunte a él. Pasos:

    1. Crea directorio `${_tmpHome}/bin/` (mkdirSync recursive).
    2. Escribe shim ejecutable en `${_tmpHome}/bin/cmux` con shebang `#!/usr/bin/env node` (NO bash — Node es portable y el repo ya depende de él). El shim parsea `process.argv[2]` (la subcommand cmux: `new-workspace`, `send`, `workspace-action`, `list-workspaces`, `notify`) y devuelve outputs canónicos a stdout. Para `new-workspace` imprime literal `OK workspace:99` (el cliente extrae el ref via regex `(workspace:\d+)` línea 38 cmux/client.js — DEBE matchear ese regex con dígitos reales). Para `list-workspaces` imprime cadena vacía (string vacío hace que el branch "ya existe orchestrator" del launchOrchestrator NO se active — preserva el flow new-workspace). Para `send`, `workspace-action`, `notify` el shim no necesita output: basta `process.exit(0)`. Marca el shim con `chmodSync(path, 0o755)`.
    3. Escribe `${_tmpHome}/.kodo/config.json` con un objeto que contiene como mínimo: `provider: "plane"`, `cmux: { binary: <absoluteShimPath> }`, `claude: { default_model: "test", flags: [], max_parallel: 1 }`, y `providers: { plane: { base_url: "x", api_key_env: "X", workspace_slug: "x", projects: [], states: {} } }`. El path al shim DEBE ser el absoluto computado en runtime via `join(_tmpHome, 'bin', 'cmux')`, no un literal hardcoded. Serializa via `JSON.stringify(configObj)`.
    4. Plumbing concreto a invocar en `before()`: `mkdirSync(join(_tmpHome, 'bin'), { recursive: true })` + `mkdirSync(join(_tmpHome, '.kodo', 'logs'), { recursive: true })` + `writeFileSync(shimPath, shimContent, 'utf-8')` + `chmodSync(shimPath, 0o755)` + `writeFileSync(configPath, JSON.stringify(configObj), 'utf-8')`.
    5. Cleanup en `after()`: `rmSync(_tmpHome, { recursive: true, force: true })` + restore de `process.env.HOME` desde snapshot.

    **CR-02 pattern — HOME isolation ANTES de cualquier import dinámico (Issue #3):**

    `src/config.js#6` define `const KODO_DIR = join(homedir(), '.kodo')` al **module load time** (sin env var override `KODO_DIR`). Si el test importa `launchOrchestrator` al top del archivo con HOME=real, el module cache freeze del usuario contamina TODO el suite y los reads/writes irían a `~/.kodo` del operador real. Replica verbatim el pattern de `test/session-start-event.test.js:64-83`:

    - **NO importes `launchOrchestrator` al top del archivo.** Sí imports estáticos de cosas que NO tocan homedir (`describe, it, before, after` de node:test; `assert`; `spawn` de node:child_process; primitivos de node:fs, node:os, node:path, node:url).
    - El existing top-level `import { applyReportingGate } from '../src/orchestrator/launch.js'` (línea 5 del archivo actual) QUEDA porque `applyReportingGate` es función pura (líneas 54-60 launch.js) — la importación del módulo ejecuta el module body pero las refs a `homedir()` están todas dentro de la función `launchOrchestrator` (lazy), no en top-level del módulo (verificar en read del archivo). Si emerge contaminación, mover ese import también a dynamic dentro de los nuevos describe blocks suite, dejando los existing describes intactos.
    - En un `before()` async del nuevo describe block:
      1. Snapshot `origHome = process.env.HOME`.
      2. Crear `_tmpHome` via `mkdtempSync(join(tmpdir(), 'kodo-launch-advisory-03-'))`.
      3. `process.env.HOME = _tmpHome`.
      4. Crear plumbing (bin/cmux shim + .kodo/config.json + .kodo/logs/ — ver bloque "Estrategia cmux stub" arriba).
      5. Crear state.json inicial vacío: `writeFileSync(join(_tmpHome, '.kodo', 'state.json'), JSON.stringify({sessions: {}, history: []}, null, 2), 'utf-8')`.
      6. **Dynamic imports DESPUÉS de fijar HOME**: `const launchMod = await import('../src/orchestrator/launch.js')`; `const stateMod = await import('../src/session/state.js')`. Guarda `launchOrchestrator = launchMod.launchOrchestrator` y `loadState = stateMod.loadState` en `let` declaradas en el scope del describe.
    - En `after()`: si `origHome === undefined` haz `delete process.env.HOME`, sino `process.env.HOME = origHome`. Luego `rmSync(_tmpHome, { recursive: true, force: true })`.
    - **Code comment dentro del `before()` que documente literal**: "// CR-02 pattern — HOME override BEFORE imports to prevent state.js/config.js cache freeze on user real ~/.kodo. Replica de test/session-start-event.test.js:64-83."

    **Imports estáticos a añadir al top del archivo (extender existing líneas 1-5):**

    El archivo ya importa `describe, it` (línea 2) y `readFileSync` (línea 4) y `applyReportingGate` (línea 5). Añade:
    - `before, after` al import de `node:test`.
    - `import { spawn } from 'node:child_process';`
    - `import { mkdtempSync, mkdirSync, rmSync, writeFileSync, chmodSync, existsSync } from 'node:fs';`
    - `import { tmpdir } from 'node:os';`
    - `import { dirname, join, resolve } from 'node:path';`
    - `import { fileURLToPath, pathToFileURL } from 'node:url';`

    **NO importes `launchOrchestrator` al top** — sigue siendo dynamic import dentro del nuevo `before()`. NO añadas `from 'node:os'` a la línea de `tmpdir` si también importas `homedir` — `homedir()` NO se usa en el test (todo deriva de `_tmpHome`).

    **Constantes top-level (después de los imports):**
    - `const __dirname = dirname(fileURLToPath(import.meta.url));`
    - `const REPO = resolve(__dirname, '..');`

    **Helper `runInlineNode(script, env)` top-level del archivo:**

    Wrapper sobre `spawn(process.execPath, ['-e', script], { env, stdio: ['ignore', 'pipe', 'pipe'] })`. Retorna `Promise<{code, stdout, stderr}>`. Captura stdout/stderr via `child.stdout.on('data', d => stdout += d.toString())` y `child.stderr.on('data', ...)`. Resuelve la promesa en el evento `'exit'` con `{code, stdout, stderr}`. Timeout interno opcional 5000ms vía `setTimeout` que rechaza con `Error('inline script timeout 5s')` (mismo pattern de CR-02 analog línea 147).

    **El test único** `'SC#3: spawnFn DI ejecuta inline lifecycle → state.json + NDJSON head-line observables'`:

    a) **Inline script construction (Warning #5 — sin fenced code block):**

    El test construye una variable string `inlineScript` que será pasada a `runInlineNode(inlineScript, env)` via `node -e <script>`. El string DEBE incluir los pasos del lifecycle simulator en orden:
    1. Primera línea: setea `process.env.HOME` al `_tmpHome` (literal embedded vía `${JSON.stringify(_tmpHome)}` para escapar paths con caracteres especiales).
    2. Dynamic imports vía `await import(...)` de 3 módulos: `src/session/state.js` (para `addSession`), `src/logger.js` (para `createLogger`), `src/logger-events.js` (para `sessionStart`). Cada path computado en runtime via `pathToFileURL(join(REPO, 'src/...')).href` para evitar issues de path-style. **NO uses paths relativos en dynamic import** — Node ESM exige URLs absolutas en `node -e` scripts.
    3. Constantes literales: `sessionId = 'test-advisory-03-uuid'`, `taskId = 'TEST-ADVISORY-03'`.
    4. Invocación `stateMod.addSession(taskId, sessionRecord)` donde `sessionRecord` es objeto literal con los 11 campos canónicos del SessionRecord shape (ver bloque `<interfaces>` arriba): `workspace_ref: 'workspace:test'`, `session_id`, `task_id`, `task_ref: 'kodo-orchestrator'`, `provider: 'plane'`, `project_id: 'p-test'`, `summary: 'inline lifecycle simulator'`, `status: 'running'`, `started_at: new Date().toISOString()`, `project_path: process.cwd()`, `gsd: false`.
    5. Logger construction: `baseLog = loggerMod.createLogger({ sessionId })`; `log = typeof baseLog.child === 'function' ? baseLog.child({ component: 'test' }) : baseLog`.
    6. `eventsMod.sessionStart(log, { session_id: sessionId, task_id: taskId, provider: 'plane', project_path: process.cwd(), started_at: new Date().toISOString() })` — emite la NDJSON head-line.
    7. `process.exit(0)` explícito al final.

    **Excerpts completos del shape están en bloque `<patterns>` del plan (sección "Inline-script idiom") — el executor LEE esos excerpts para implementar; NO replica el body completo en `<action>`.** Nota crítica vs versión anterior: el inline script ahora setea `process.env.HOME` (NO `KODO_DIR`) — `KODO_DIR` no existe como env var en src/config.js.

    b) **spawnFn DI construction (Warning #5 — sin fenced code block):**

    Define `const spawnCalls = []` en el scope del test. Define `const spawnFn = async (ctx) => { ... }` que:
    - Push `ctx` al array `spawnCalls`.
    - Construye env para el child: `{ ...process.env, HOME: _tmpHome }` — solo `HOME` es necesario (`KODO_DIR` no existe en config.js). El proceso padre del test ya tiene HOME=_tmpHome desde `before()`, pero el override explícito es defensive.
    - Invoca `const result = await runInlineNode(inlineScript, env)`.
    - Si `result.code !== 0`, throw `new Error('inline script exit ' + result.code + ': ' + result.stderr)` para fail-loud.

    c) **Logger noop para el await launchOrchestrator:**

    Inline mock literal definido en el scope del test: objeto con métodos `info()`, `warn()`, `error()`, `debug()` que son no-op (`() => {}`), y `child()` que retorna el mismo mock (self-referencial). Si descubres durante read_first que `src/logger-noop.js` existe y exporta una función equivalente, impórtalo dinámicamente DESPUÉS del HOME override y úsalo en vez del inline mock.

    d) **Await launchOrchestrator (Warning #5 — sin fenced code block):**

    Invocación sincrónica de la fn ya importada dinámicamente en `before()`: `await launchOrchestrator({ logger: noopLog, spawnFn })`. El binding `launchOrchestrator` está en el closure scope del describe block.

    e) **Assertions sobre observables post-launch:**

    Las assertions son las mismas del plan original — preservar literalmente (todas usan formato prosa-con-identifiers, sin fenced):
    - **Assertion 1 — spawnFn fue invocado** con context payload correcto:
      * `assert.equal(spawnCalls.length, 1)`.
      * `assert.equal(spawnCalls[0].taskRef, 'kodo-orchestrator')`.
      * `assert.ok(spawnCalls[0].workspaceRef, 'workspaceRef present')`.
      * `assert.ok(spawnCalls[0].sessionId, 'sessionId present')`.
      * `assert.equal(spawnCalls[0].kodoDir, join(_tmpHome, '.kodo'))` — kodoDir computado por launchOrchestrator usando homedir() override.
    - **Assertion 2 — state.json contiene la sesión** (usar binding `loadState` del closure, importado dinámicamente en `before()`):
      * `const state = loadState()`.
      * `assert.ok(state.sessions['TEST-ADVISORY-03'], 'session present in state')`.
      * `assert.equal(state.sessions['TEST-ADVISORY-03'].session_id, 'test-advisory-03-uuid')`.
    - **Assertion 3 — NDJSON head-line parseable con event=session.start**:
      * `const ndjsonPath = join(_tmpHome, '.kodo', 'logs', 'test-advisory-03-uuid.ndjson')`.
      * `assert.ok(existsSync(ndjsonPath), 'NDJSON logfile created')`.
      * Leer primera línea: `const lines = readFileSync(ndjsonPath, 'utf-8').split('\n').filter(Boolean)`; `const head = lines[0]`.
      * `const rec = JSON.parse(head)` envuelto en try/catch que asserta con fail-loud message si parsing falla (D-10 pattern de CR-02).
      * `assert.equal(rec.event, 'session.start')`.
      * `assert.equal(rec.task_id, 'TEST-ADVISORY-03')`.
      * `assert.ok(rec.transcript_path, 'transcript_path populated')`.
      * `assert.ok(typeof rec.transcript_path === 'string' && rec.transcript_path.length > 0)`.

    **Source-hygiene assertion adicional (NET PLUS, opcional dentro del mismo describe):**

    Test extra dentro del mismo describe que verifica que `src/orchestrator/launch.js` NO importa `child_process` (blinda la Opción A invariante). Patrón: lee `readFileSync(join(REPO, 'src', 'orchestrator', 'launch.js'), 'utf-8')`; assert `!source.includes("from 'node:child_process'")` y `!source.includes('from "child_process"')`.

    **Restricciones explícitas (Issue #1 — sin fallback unit-only):**
    - NO spawnar `claude` ni `cmux` binarios reales — el shim node `${_tmpHome}/bin/cmux` resuelve toda invocación cmux.
    - NO tocar el HOME del usuario — verificado por CR-02 HOME override + dynamic imports.
    - NO depender de network/IPC externo.
    - NO modificar tests pre-existentes (LG1-LG8 + LH1-LH3 del archivo + D-06 source-hygiene de `test/orchestrator-launch-isolation.test.js`).
    - NO refactorizar `src/cmux/client.js` como parte de Phase 31 — el shim binary stub es zero-source-change.
    - **El test integration SE EJECUTA. No hay fallback "unit-only" legal.** Si emerge un blocker técnico durante execute-phase (e.g., el shim no satisface el regex `(workspace:\d+)` por error de tipeo, o el config.json shape requiere campos adicionales), el executor lo corrige en su Task — NO degrada a unit test. Phase 31 cierra ADVISORY-03 con un integration test real per Opción A.
  </action>
  <verify>
    <automated>node --test test/launch.test.js 2>&amp;1 | grep -E "(launchOrchestrator real spawn|tests|pass|fail)" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "launchOrchestrator real spawn observables" test/launch.test.js` retorna exactamente 1 (describe name).
    - `grep -c "spawnFn" test/launch.test.js` retorna ≥3 (helper + DI injection + assertion).
    - `grep -c "session.start" test/launch.test.js` retorna ≥2 (inline script + assertion).
    - `grep -c "transcript_path" test/launch.test.js` retorna ≥2 (assertion present).
    - `node --test test/launch.test.js` exit code 0. Tests pre-existentes (LG1-LG8 + LH1-LH3) pasan + nuevo describe pasa con ≥1 test (≥2 si se incluyó la source-hygiene `child_process`-absent variant).
    - El test integration valida los 3 niveles de observables: spawnFn invocado (Assertion 1), state.json mutado (Assertion 2), NDJSON head-line con session.start + transcript_path (Assertion 3).
    - El test NO degrada a unit-only. Si emerge un blocker técnico (regex mismatch, config shape incompleto, etc.), el executor corrige el shim/config en su Task antes de declarar la advisory cerrada.
    - El test NO toca `~/.kodo` real del usuario (verificable: ejecutar el test y confirmar que `~/.kodo/state.json` del usuario no se mutó — manual check, no auto).
  </acceptance_criteria>
  <done>
    Describe block `'launchOrchestrator real spawn observables (ADVISORY-03)'` añadido con ≥1 test integration validando state.json + NDJSON head-line + session.start + transcript_path observables. cmux stubeado sin invocar binary real. HOME-isolated. Suite global sube ≥+1 test (≥+2 si se añadió source-hygiene variant).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `launchOrchestrator` opts → `spawnFn` hook | Caller-controlled; en producción es `undefined`. En tests, la function es código in-process del repo. Sin crossing de trust boundary externo. |
| Test → `realSpawn('node', ['-e', inlineScript])` | El inline script es string literal definido EN el test source — no hay interpolation desde input externo ni variables runtime no-controladas (sessionId/taskId son constantes literales). |
| Test → `tmpHome` filesystem | mkdtempSync POSIX-safe; afterEach rmSync limpia. cmux shim (si se usa) escrito a `tmpHome/bin/cmux` con chmod 0o755, no a path del usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Injection (test fixture) | Inline script con interpolation | mitigate | `JSON.stringify` aplicado a TODOS los valores embedded (`_kodoDir`, paths de import). Esto blinda contra injection vía paths con quotes/backslashes. |
| T-31-03-02 | Information disclosure | Producción ejecuta spawnFn malicioso | accept | `spawnFn` default `undefined` en producción — no hay path de ejecución para callers de bin/kodo. Solo invocado si caller explícitamente lo provee (test-only). |
| T-31-03-03 | Tampering | spawnFn modifica state.json del usuario | mitigate | Test usa HOME-isolated `_tmpHome`; KODO_DIR override apunta a `_tmpHome/.kodo`. Verifica explícitamente en assertions que las mutaciones ocurren en tmpHome, no en ~. afterEach rmSync limpia. |
| T-31-03-04 | Denial of Service | spawnFn cuelga indefinidamente en producción | accept | Default undefined. Tests usan timeout implícito de node --test (60s default). Sin escalación. |
| T-31-03-05 | Elevation of privilege | spawnFn ejecuta arbitrary code | accept | El callback es definido por el caller in-process (mismo runtime, mismos privilegios). Sin escalación. En tests, el inline script ejecuta dynamic imports de paths absolutos resueltos vía `pathToFileURL(join(REPO, ...))` — no input externo. |
| T-31-03-06 | Repudiation | cmux real invocado por accidente en el test | mitigate | Stub explícito documentado en code-comment. afterEach restaura env. Assertions sobre `spawnCalls.length === 1` blindan que el hook se invocó (no que cmux se haya escapado). |

ASVS L1: el surface efectivo es 0 en producción (default undefined). Tests son ASVS-out-of-scope por convención (no son codepaths reachables por consumers externos). El inline script con JSON.stringify-everything es la única superficie post-trust-boundary; mitigado por construcción.
</threat_model>

<verification>
- `node --test test/launch.test.js` exit 0 con describe `'launchOrchestrator real spawn observables (ADVISORY-03)'` pasando.
- `node --test test/orchestrator-launch-isolation.test.js` exit 0 (Phase 18 D-06 source-hygiene preservado).
- Suite global no regresiona: `node --test` exit 0 con count ≥890 (baseline post-31-02 + ≥1 test del 31-03).
- `grep -c "child_process" src/orchestrator/launch.js` retorna 0 (Opción A invariante: prod no importa child_process desde launch.js).
- `grep -c "Opción A" src/orchestrator/launch.js` retorna ≥1 (comentario in-file documenta la decisión).
- `grep -c "spawnFn" src/orchestrator/launch.js` retorna ≥2 (JSDoc + invocación).
- El test NO muta `~/.kodo` del usuario (verificable manual: `cat ~/.kodo/state.json` antes y después del test → idéntico).
</verification>

<success_criteria>
SC#3 ROADMAP byte-exact satisfacha:
- Test `test/launch.test.js` (ampliado) ejecuta `launchOrchestrator` con spawn real (NO mockSpawn — el spawnFn DI ejecuta `node -e <inlineScript>` real vía `child_process.spawn`) ✓
- Stdin canónico no requerido — el inline script es self-contained vía `node -e` (no necesita stdin) ✓
- Observables post-launch verificados:
  * `state.json` contiene la nueva session record ✓ (Assertion 2)
  * NDJSON contiene evento `session.start` con `transcript_path` populated ✓ (Assertion 3)
- Adopta explícitamente Opción A (Lifecycle Simulator Hook), documentada in-file + en este PLAN.md ✓
- Producción byte-exact: callers existentes (`bin/kodo orchestrate`) no pasan spawnFn → if-guard lo elide → comportamiento idéntico pre-Phase-31 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/31-phase-21-22-advisory-cleanup/31-03-SUMMARY.md` documentando:
- Modificaciones byte-level a `src/orchestrator/launch.js` (JSDoc + invocación post-cmux.send/notify pre-return + comentario in-file Opción A)
- 1 (o 2 si se incluyó variante source-hygiene) test añadido a `test/launch.test.js` con line ranges
- **Estrategia de cmux stub** utilizada (cuál de las 3 opciones — env var override / shim binary / mock socket — y por qué)
- Conteo pre/post de tests del archivo y suite global
- Confirmación de invariantes preservados (auto-sync block intocado, --worktree absent runtime, cwd=process.cwd() preserved, Phase 18 D-06 comment preserved)
- **Confirmación que el test integration corre y pasa** (no fallback unit-only) — documenta exit code 0 del test integration y los 3 niveles de observable verificados.
</output>
