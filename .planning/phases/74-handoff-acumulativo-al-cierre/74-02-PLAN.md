---
phase: 74-handoff-acumulativo-al-cierre
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/state.js
  - test/state/handoff-state.test.js
  - test/session/reconcile-lock.test.js
autonomous: true
requirements: [LIVE-04]

must_haves:
  truths:
    - "Tras el cierre, `state.json` refleja para la tarea el puntero al plan y el `NEXT:` de una línea (LIVE-04)"
    - "El dato sobrevive a `removeSession` — vive en `state.tasks`, no en el registro de sesión, así que la SIGUIENTE sesión de la misma tarea lo encuentra (D-05)"
    - "Un lock-timeout escribe un warn y devuelve el fail-safe: jamás lanza, jamás bloquea al caller (D-06)"
    - "`state.tasks` sobrevive intacta a un `reconcileTick` (el spread preserva claves top-level desconocidas)"
    - "`reconcileTick` sigue siendo el único escritor de `alive` — el mutator nuevo no lo toca (D-04 cross-milestone)"
  artifacts:
    - src/session/state.js
    - test/state/handoff-state.test.js
  key_links:
    - "`withStateLock` carga fresco DENTRO del lock — ésa es la clave anti-clobber, no el rename"
    - "`schema_version` NO se toca: `tasks` es aditivo opcional, precedente `history` (Phase 30) y `worktree_path` (Phase 18 D-03c)"
    - "Los logs llevan solo `{task_id, reason}` — nunca el contenido del `next` (T-71-18)"
---

<objective>
Añadir a `src/session/state.js` el escritor de la clave top-level `tasks` (D-05) bajo `withStateLock`
(D-06), y blindar con tests las dos propiedades que hacen que el dato sobreviva: la aditividad sin
bump de `schema_version` y la no-interferencia con `alive`.

Purpose: el `NEXT:` es un dato de la **tarea**, no de la sesión. Su valor entero es sobrevivir a la
sesión que lo produjo. `performTerminalCleanup` llama a `removeSession`, que archiva la sesión a
`history` (cap FIFO 50) y borra la fila de `state.sessions` — un `NEXT:` guardado ahí desaparecería
de la lista al cerrar y sería desalojable a los 50 cierres (D-05, razón dura verificada en
`state.js:355-376`).
Output: `upsertTaskHandoff` + el typedef `State` extendido + dos suites que vigilan la supervivencia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-PATTERNS.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Símbolos y ficheros NUEVOS creados por la Phase 74 (ninguno existe hoy — no son deriva):

**Plan 01** — `src/session/handoff.js`, exports: `HANDOFF_REASONS`, `normalizeReason`,
`sanitizeInline`, `isSafeTaskId`, `buildPlanHeader`, `buildHandoffBlock`, `findSessionBlock`,
`hasSessionHandoff`, `extractNext`. Fichero nuevo: `test/session/handoff.test.js`.

**Plan 02 (este plan)** — `upsertTaskHandoff` en `src/session/state.js`; clave `tasks?` en el typedef
`State`. Fichero nuevo: `test/state/handoff-state.test.js`.

**Plan 03** — sin símbolos nuevos (dos strings de instrucción en `src/hooks/session-start.js`).

**Plan 04** — `writeHandoff` (export nuevo de `src/hooks/session-end.js`); claves nuevas en el shape
`deps` de `runSessionEndHook`: `plansDir`, `fs`, `stateWriterFn`, `now`. Fichero nuevo:
`test/hooks/session-end-handoff.test.js`.

**Plan 05** — modo `--kind handoff` en `test/helpers/lock-race-child.mjs`. Fichero nuevo:
`test/state/handoff-concurrency.test.js`.

**Shape de `state.tasks` (D-05)** — `state.tasks[task_id] = { plan_path: string, next: string|null, updated_at: string }`,
clave top-level aditiva y opcional, SIN bump de `schema_version`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `upsertTaskHandoff` — escritor de `state.tasks` bajo `withStateLock`</name>

  <read_first>
    - `src/session/state.js:331-348` (`addSession`) — el analog EXACTO de shape a espejar; y `:355-376` (`removeSession`) — de donde sale el guard defensivo del campo aditivo
    - `src/session/state.js:317-323` (`withStateLock`) — el contrato que consume: carga fresca DENTRO del lock, mutator SÍNCRONO
    - `src/session/state.js:51-56` — el typedef `State` a extender; y `:41` (`worktree_path`) — la frase "NO bump de schema_version" a copiar
    - `src/session/state.js:1-10` — LOG-12: solo se importa `logger-noop`, el logger real lo inyecta el caller
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md` §decisions — D-05 y D-06
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-PATTERNS.md` §`src/session/state.js`
  </read_first>

  <files>src/session/state.js</files>

  <behavior>
    - `upsertTaskHandoff('t1', {plan_path:'/p/t1.md', next:'Arreglar X', updated_at:'2026-07-15T09:00:00.000Z'})` deja `loadState().tasks.t1` igual a ese objeto de tres claves.
    - Llamarlo dos veces para el mismo `task_id` deja UNA entrada, con los valores de la segunda llamada (upsert, no append).
    - Llamarlo para dos `task_id` distintos deja DOS entradas y ninguna pisa a la otra.
    - Sobre un state que no tiene la clave `tasks`, la crea; sobre uno que ya la tiene, la preserva y añade.
    - `next: null` se persiste como `null` (caso del bloque mecánico de D-03) y no como `undefined` ni ausente.
    - Omitir `updated_at` hace que se persista un ISO-8601 generado; el campo nunca falta.
    - Con el lock ocupado (`withStateLock` devuelve `{ok:false}`), devuelve ese mismo objeto, emite un `logger.warn` y NO lanza.
    - En el carril de fallo NO se emite la telemetría de éxito (WR-01: nunca reclamar un éxito falso).
    - El logger por defecto es el noop: llamarlo sin `logger` no lanza.
    - El mutator no introduce ni modifica ninguna clave `alive` en ninguna sesión (D-04 cross-milestone).
    - Ninguna llamada modifica `state.schema_version`.
  </behavior>

  <action>
    Añadir a `src/session/state.js` el export `upsertTaskHandoff(taskId, entry, logger = noopLogger)`,
    colocado junto a los demás mutators (`addSession`/`removeSession`/`updateSession`).

    Espejar los 5 elementos NO negociables de `addSession:331-348`:
    1. `logger = noopLogger` como default param (LOG-12: `state.js` jamás importa `logger.js`).
    2. `withStateLock` con un mutator **síncrono** que muta en sitio y devuelve `undefined`.
    3. `if (!r.ok)` → `logger.warn` → **`return r`** — fail-safe propagado, jamás throw (D-06:
       lock-timeout → warn y seguir; el cierre JAMÁS se bloquea).
    4. Telemetría de éxito **gated** tras ese guard (WR-01).
    5. `return r` final.

    Dentro del mutator, antes de escribir: guard defensivo del campo aditivo, espejo de
    `if (!Array.isArray(state.history)) state.history = [];` (`:361`) → crear `state.tasks` como
    objeto vacío si falta. Luego asignar `state.tasks[taskId]` con exactamente tres claves:
    `plan_path`, `next` (con `null` como valor por defecto cuando venga ausente — el bloque mecánico
    de D-03 no tiene `NEXT:`) y `updated_at` (ISO-8601, generado si no viene). **El mutator toca SOLO
    `state.tasks`** — jamás `alive`, cuyo único escritor sigue siendo `reconcileTick` (D-04
    cross-milestone, invariante de STATE.md).

    Nombres de evento del logger: `state.task.handoff_saved` para el éxito y
    `state.task.handoff_failed` para el fallo. **Campos: solo `{task_id, reason}`.** El `next` NO se
    loguea nunca — es contenido redactado por un LLM y los logs no llevan contenido de usuario
    (precedente T-71-18: el backstop loguea solo `{session_id, task_id, state}`). Mitigación T-74-08.

    Extender el typedef `State` (`:51-56`) con `tasks?: Record<string, {plan_path: string, next: string|null, updated_at: string}>`
    y un comentario en el mismo estilo que el de `history`: cita Phase 74 (D-05), dice que es aditivo
    opcional, que los lectores usan el guard defensivo `state.tasks || {}`, y cierra con la frase
    literal del precedente `worktree_path` (`:41`): **NO bump de `schema_version`**. No tocar
    `schema_version` ni `migrateStateV2toV3` — la evidencia de seguridad del aditivo está verificada
    en RESEARCH (`reconcile.js:238`/`:396` reconstruyen con spread y preservan claves top-level
    desconocidas; `migrateStateV2toV3:122` es inalcanzable por el early-return de idempotencia).
  </action>

  <verify>
    <automated>node --test test/state/handoff-state.test.js</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c 'export function upsertTaskHandoff' src/session/state.js` == 1.
    - Source assertion: el typedef `State` contiene la clave `tasks?` — `grep -c 'tasks?:' src/session/state.js` >= 1.
    - Source assertion: `git diff src/session/state.js | grep -c '^[-+].*schema_version'` == 0 (el aditivo no toca el schema).
    - Behavior assertion: todos los casos de `<behavior>` verdes vía `node --test test/state/handoff-state.test.js` (Task 2).
    - Behavior assertion: en el carril `{ok:false}` la función devuelve el objeto de fail-safe y el logger registra un `warn`, sin excepción propagada.
  </acceptance_criteria>

  <done>`upsertTaskHandoff` existe, escribe solo `state.tasks` bajo `withStateLock`, propaga el fail-safe sin lanzar y no loguea contenido de usuario.</done>
</task>

<task type="auto">
  <name>Task 2: Suite de persistencia con HOME aislado y semilla v3</name>

  <read_first>
    - `test/state/save-state-atomic.test.js:67-90` — el analog EXACTO: `process.env.HOME = tmpHome` en `before()` y **dynamic import POST-HOME** de `state.js`; restauración de HOME en `after()`; reset a v3 limpio en `afterEach()`
    - `test/state/state-writers-concurrency.test.js:41-60` — `seedV3()`, la semilla canónica `{schema_version: 3, sessions: {}, history: []}`
    - `src/session/state.js:255-263` (`loadState`) y `:121-143` (`migrateStateV2toV3`) — la cadena exacta de la trampa v2
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md` §Pitfall 5 y §Pitfall 6
  </read_first>

  <files>test/state/handoff-state.test.js</files>

  <action>
    Crear `test/state/handoff-state.test.js` cubriendo todos los casos de `<behavior>` del Task 1.

    **Aislamiento de HOME — obligatorio y con una trampa concreta.** `config.js:11` evalúa
    `join(homedir(), '.kodo')` en **module-load**, y `state.js:14` deriva `STATE_PATH` de ahí. Un
    `import` estático de `state.js` en la cabecera del fichero haría que el test escriba en el
    `~/.kodo` REAL del operador. Patrón obligatorio (RESEARCH §Pitfall 6): `process.env.HOME = tmpHome`
    **antes** de un `await import('../../src/session/state.js')` dinámico dentro de `before()`. **Ningún
    import estático de `state.js`.** Restaurar HOME en `after()` y borrar el tmpdir.

    **Sembrar v3 SIEMPRE (RESEARCH §Pitfall 5) — el fallo silencioso de este plan.** Si `state.json`
    no existe, `loadState():257` devuelve la forma **v2** `{schema_version: 2, sessions: {}}`;
    `withStateLock` mutaría eso y `saveState` escribiría en disco un fichero v2 **con** `tasks`; el
    siguiente `loadState` dispararía `migrateStateV2toV3`, cuya reconstrucción exhaustiva
    (`:139-143`) **descarta toda clave desconocida, incluida `tasks`**. Ese carril es inalcanzable
    desde el hook real (sin fichero → `findSession` no matchea → `session-end.js:72-75` retorna),
    pero es **totalmente alcanzable desde este test**. Sembrar `{schema_version: 3, sessions: {}, history: []}`
    en `beforeEach`/`afterEach` como hace `state-writers-concurrency.test.js:47`.

    Incluir además un caso explícito anti-regresión: tras `upsertTaskHandoff`, un `loadState()`
    posterior sigue devolviendo `tasks` intacta y `schema_version` sigue siendo 3 — es la prueba
    directa de que la aditividad no muere en la migración.

    Para el caso de lock ocupado, seguir la técnica ya usada en el repo para forzar `{ok:false}`:
    crear a mano el fichero de lock del state con un pid vivo y un mtime fresco antes de llamar, de
    modo que se agoten los reintentos (defaults `retries=8, backoffMs=20` → ~160 ms) y limpiarlo
    después. Assertar el `warn` y la ausencia de excepción.
  </action>

  <verify>
    <automated>node --test test/state/handoff-state.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/state/handoff-state.test.js` exits 0.
    - Source assertion: `grep -c "^import .*src/session/state.js" test/state/handoff-state.test.js` == 0 (el import es dinámico y post-HOME; un import estático rompería el aislamiento).
    - Source assertion: el fichero siembra un state v3 explícito — `grep -c 'schema_version: 3' test/state/handoff-state.test.js` >= 1.
    - Behavior assertion: tras `upsertTaskHandoff`, `loadState().schema_version` sigue siendo 3 y `loadState().tasks` contiene la entrada (la aditividad sobrevive a la relectura).
    - Behavior assertion: con el lock ocupado, la llamada devuelve el fail-safe y registra un `warn`, sin lanzar.
    - CLI: `ls ~/.kodo/plans/ 2>/dev/null | wc -l` no cambia y `git status --porcelain` no muestra ficheros fuera del repo — el test no ha tocado el HOME real.
  </acceptance_criteria>

  <done>La persistencia de `state.tasks` está testeada en un HOME aislado con semilla v3, incluidos el carril de fallo y la supervivencia a la relectura.</done>
</task>

<task type="auto">
  <name>Task 3: Regresión anti-drop — `state.tasks` sobrevive a `reconcileTick`</name>

  <read_first>
    - `test/session/reconcile-lock.test.js:1-80` — HOME aislado + dynamic import POST-HOME de `state.js`/`reconcile.js`, con `loadState`/`saveState` inyectados en memoria: el andamiaje exacto que este caso reutiliza
    - `src/session/reconcile.js:237-239` — `return { state: { ...state, sessions, history }, ... }`: el spread que PRESERVA las claves top-level desconocidas
    - `src/session/reconcile.js:396` — `if (changed) state = { ...state, sessions };`: el segundo spread
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md` §"La reconstrucción del state que PRESERVA `tasks`"
  </read_first>

  <files>test/session/reconcile-lock.test.js</files>

  <action>
    Añadir a `test/session/reconcile-lock.test.js` un caso de regresión anti-drop: partiendo de un
    state en memoria que contiene una clave top-level `tasks` con una entrada, correr
    `runReconcileTick` y assertar que el state guardado (capturado por el `saveState` espía que el
    fichero ya inyecta) **sigue conteniendo `tasks` con la misma entrada, byte a byte**.

    Por qué este caso vale su coste: toda la decisión D-05 se apoya en que `reconcile.js:238` y `:396`
    reconstruyen el state con spread y por tanto preservan claves desconocidas. Eso está verificado
    hoy por lectura de código, pero **no está vigilado por ningún test**. El día que alguien cambie
    ese spread por una reconstrucción explícita (exactamente lo que hace `migrateStateV2toV3:139-143`),
    el `NEXT:` de todas las tareas desaparecería en silencio en el siguiente tick — sin que rompa
    ningún test, y sin que nadie lo note hasta que el dashboard de la Phase 75 muestre celdas vacías.

    Añadir en el mismo caso la contra-assert del invariante cross-milestone: el tick sigue siendo
    quien escribe `alive` y `upsertTaskHandoff` no participa en esa transición — reutilizar la
    sesión muerta que el fichero ya modela y assertar que su transición de estado sigue ocurriendo
    con `tasks` presente (es decir, que la clave nueva no interfiere con la máquina de estados).

    Reutilizar los helpers y el andamiaje ya presentes en el fichero; no crear un HOME nuevo ni
    duplicar los stubs.
  </action>

  <verify>
    <automated>node --test test/session/reconcile-lock.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/session/reconcile-lock.test.js` exits 0, con los casos preexistentes verdes.
    - Behavior assertion: el state capturado por el `saveState` espía tras `runReconcileTick` contiene `tasks` con la entrada sembrada, con `plan_path`, `next` y `updated_at` idénticos a los de entrada.
    - Behavior assertion: el caso falla si se sustituye el spread de `reconcile.js:238` por una reconstrucción explícita de `{schema_version, sessions, history}` — verificable con una edición temporal revertida después.
    - Behavior assertion: la transición de estado preexistente de la sesión muerta sigue ocurriendo con `tasks` presente (la clave nueva no interfiere).
  </acceptance_criteria>

  <done>La aditividad de `state.tasks` frente al único reescritor periódico del state está vigilada por un test, no solo por una lectura de código.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (hook de cierre) → `state.json` | El hook es un escritor más de un fichero compartido por daemon, server y reconcile |
| Contenido redactado por el LLM (`next`) → logs | El `next` es texto libre y no debe acabar en la telemetría |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-05 | Tampering | `upsertTaskHandoff` → `state.json` | high | mitigate | Escritura exclusivamente vía `withStateLock`, que hace `loadState()` **fresco dentro** del lock (`state.js:319`) — la clave anti-clobber. Mutator síncrono que toca SOLO `state.tasks`; jamás `alive` (D-04: `reconcileTick` sigue siendo su único escritor). Invariante v0.16 Phase 70 |
| T-74-06 | DoS | `withStateLock` lock-timeout | medium | mitigate | `{ok:false}` → `logger.warn` → `return r`. Nunca throw, nunca bloqueo indefinido; peor caso ~160 ms (`state-lock.js:34-36`). El caller (Plan 04) sigue el cierre igualmente (D-06) |
| T-74-08 | Information Disclosure | Campos del logger | low | mitigate | `state.task.handoff_saved` / `state.task.handoff_failed` llevan solo `{task_id, reason}`. El `next` (contenido redactado por un LLM) NUNCA se loguea. Precedente T-71-18 |
| T-74-10 | Tampering | Pérdida silenciosa de `state.tasks` en la migración | medium | mitigate | Aditivo SIN bump de `schema_version` (precedentes `history`/`worktree_path`); tests que siembran v3 explícito (Task 2) y regresión anti-drop sobre el spread de `reconcileTick` (Task 3) |
| T-74-SC | Tampering | npm/pip/cargo installs | high | mitigate | Esta fase instala CERO paquetes (`74-RESEARCH.md` §Package Legitimacy Audit: *"No aplica"*). Sin tasks de install → sin checkpoint de legitimidad. Verificable: `git diff --stat package.json package-lock.json` vacío |
</threat_model>

<verification>
- `node --test test/state/handoff-state.test.js` exits 0
- `node --test test/session/reconcile-lock.test.js` exits 0
- `npm test` — suite completa verde (baseline 2027 tests, cero regresiones)
- `git diff --stat package.json package-lock.json` — vacío
- El HOME real del operador intacto: los tests nuevos no crean nada en `~/.kodo`
</verification>

<success_criteria>
- `upsertTaskHandoff` existe en `src/session/state.js` y escribe `state.tasks[task_id] = {plan_path, next, updated_at}` bajo `withStateLock`
- `schema_version` sin tocar; el typedef `State` documenta `tasks?` como aditivo opcional
- El mutator no toca `alive` (asserted)
- Lock-timeout → warn + fail-safe, sin excepción (asserted)
- `state.tasks` sobrevive a `runReconcileTick` (asserted)
- Cero dependencias npm nuevas
</success_criteria>

<output>
Create `.planning/phases/74-handoff-acumulativo-al-cierre/74-02-SUMMARY.md` when done
</output>
