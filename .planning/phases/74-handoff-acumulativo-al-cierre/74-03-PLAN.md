---
phase: 74-handoff-acumulativo-al-cierre
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/session-start.js
  - test/session-start.test.js
  - test/gsd-context.test.js
autonomous: true
requirements: [LIVE-02]

must_haves:
  truths:
    - "La instrucción al LLM deja de ordenar sobrescribir el plan y pasa a preservar-y-appendear (LIVE-02, SC#2)"
    - "El LLM recibe el formato EXACTO del bloque de handoff de D-01, con su session_id ya resuelto, para que el detector de D-04 lo reconozca"
    - "Las DOS ramas productoras cambian — la no-GSD (ES) y la GSD quick (EN): son el mismo bug (D-10)"
    - "Las ramas GSD full y bootstrap SIGUEN sin instrucción — las cubre el backstop mecánico de D-03 (D-10)"
  artifacts:
    - src/hooks/session-start.js
  key_links:
    - "El slice del guard de emojis va desde el heading del bloque HOOK-01 hasta el FINAL del string — la instrucción nueva cae dentro (Pitfall 2)"
    - "El prefijo de la instrucción se conserva: es lo que assertan los tests de presencia y de exclusión phase/bootstrap"
    - "La ruta se resuelve con join(KODO_DIR, 'plans', `${session.task_id}.md`) — nunca un literal templated"
---

<objective>
Invertir la semántica de las dos instrucciones de plan de `src/hooks/session-start.js` (D-10): de
«sobrescribe si ya existe» a preservar-y-appendear, y añadir a ambas la instrucción de escribir el
bloque de handoff al cierre con el formato exacto de D-01.

Purpose: LIVE-02 (SC#2) exige que una segunda sesión de la misma tarea acumule un segundo bloque con
el primero íntegro. Mientras la instrucción ordene sobrescribir, el LLM destruye el historial en el
arranque de cada sesión — antes incluso de que el hook de cierre tenga nada que acumular. Es la mitad
optimista del patrón LLM+backstop: esta instrucción es la optimización, el bloque mecánico del Plan
04 es la garantía.

**D-11, re-alcanzado tras el research:** la premisa original («tocar `:85`/`:145` rompe los golden
bytes de HOOK-02, hay que repararlos») es **falsa**. El grep exhaustivo del researcher encontró CERO
tests que asserten los literales de sobrescritura; las asserts reales son de prefijo, ruta resuelta y
orden. Conservando el prefijo no rompe ningún test existente. El objeto de este plan cambia en
consecuencia: **añadir** tests de la semántica nueva, no reparar bytes.

Output: dos instrucciones invertidas + dos instrucciones de handoff + la cobertura de test de la
semántica nueva en ambos idiomas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-PATTERNS.md
@.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Símbolos y ficheros NUEVOS creados por la Phase 74 (ninguno existe hoy — no son deriva):

**Plan 01** — `src/session/handoff.js`, exports: `HANDOFF_REASONS`, `normalizeReason`,
`sanitizeInline`, `isSafeTaskId`, `buildPlanHeader`, `buildHandoffBlock`, `findSessionBlock`,
`hasSessionHandoff`, `extractNext`. Fichero nuevo: `test/session/handoff.test.js`.

**Plan 02** — `upsertTaskHandoff` en `src/session/state.js`; clave `tasks?` en el typedef `State`.
Fichero nuevo: `test/state/handoff-state.test.js`.

**Plan 03 (este plan)** — sin símbolos nuevos. Dos strings de instrucción invertidos y dos strings de
instrucción de handoff añadidos en `src/hooks/session-start.js` (ramas no-GSD ES y GSD quick EN).

**Plan 04** — `writeHandoff` (export nuevo de `src/hooks/session-end.js`); claves nuevas en el shape
`deps` de `runSessionEndHook`: `plansDir`, `fs`, `stateWriterFn`, `now`. Fichero nuevo:
`test/hooks/session-end-handoff.test.js`.

**Plan 05** — modo `--kind handoff` en `test/helpers/lock-race-child.mjs`. Fichero nuevo:
`test/state/handoff-concurrency.test.js`.

**Formato del marcador de handoff (D-01)** — heading + marcador HTML en la MISMA línea:
`## Handoff <YYYY-MM-DD HH:MM> <!-- kodo:handoff v=1 session=<session_id> author=<llm|auto> at=<ISO-8601 UTC> -->`
El bloque mecánico añade el sufijo ` — automático` al heading, antes del marcador.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Invertir las dos instrucciones y añadir el contrato de handoff al prompt</name>

  <read_first>
    - `src/hooks/session-start.js:81-86` — la rama no-GSD (ES) en `buildSessionContext`: el array literal, el separador `''`, la ruta resuelta con `join(KODO_DIR, ...)`, y el comentario `:83` que queda OBSOLETO con D-10
    - `src/hooks/session-start.js:140-146` — la rama GSD quick (EN) en `buildGsdContext`, DENTRO del `if (mode === 'quick')`; `session.session_id` ya está en scope (se usa en `:108`)
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md` §decisions — D-01 (formato exacto), D-10, D-11
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-PATTERNS.md` §`src/hooks/session-start.js` — las restricciones de forma no negociables
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-RESEARCH.md` §Pitfall 2 — el guard de emojis, el riesgo REAL
  </read_first>

  <files>src/hooks/session-start.js</files>

  <action>
    **Rama no-GSD, ES (`:85`).** Sustituir el paréntesis final de la instrucción de plan, que hoy dice
    «(sobrescribe si ya existe)», por una que ordene preservar-y-appendear: si el fichero ya existe,
    no sobrescribirlo, añadir el plan al final. **Conservar el prefijo literal** «Además, al empezar
    escribe un plan corto» y la ruta resuelta con `join(KODO_DIR, 'plans', ...)` — son lo que assertan
    los tests de presencia y de ruta.

    Añadir a continuación, como elementos nuevos del mismo array literal (con su separador `''`), la
    instrucción de handoff: al cerrar la sesión, añadir al final de ese mismo fichero un bloque de
    handoff sin borrar los bloques anteriores; primera línea con el formato exacto de D-01
    (`## Handoff ` + fecha-hora local + ` ` + el marcador HTML `<!-- kodo:handoff v=1 session=` +
    **`session.session_id` interpolado y resuelto** + ` author=llm at=` + timestamp ISO-8601 UTC + ` -->`);
    debajo, tres líneas con las etiquetas `**Hecho:**`, `**Pendiente:**` y `**NEXT:**`, siendo `NEXT:`
    una única línea con la acción concreta siguiente.

    **El `session_id` DEBE ir resuelto, no templated.** Es lo que hace que `findSessionBlock` del Plan
    01 reconozca el bloque como de esta sesión (D-04). Si el LLM escribe un placeholder, la detección
    de autoría falla y el hook appendea un bloque mecánico duplicado.

    **Rama GSD quick, EN (`:145`).** El mismo cambio, en inglés, **DENTRO del `if (mode === 'quick')`**
    — el bloque común «## No automatic push» vive fuera del if/else y su invariancia está asserted
    (`gsd-context.test.js:229`). Conservar el prefijo literal «Also, at the start write a short plan».
    Las **etiquetas del formato siguen en español** (`**Hecho:**` / `**Pendiente:**` / `**NEXT:**`) en
    ambos idiomas: el idioma que alterna por rama es el de la **instrucción** (D-08 Phase 45), no el
    del formato — el parser de D-02 busca `**NEXT:**` y el bloque mecánico de D-03 escribe español.

    **Ramas GSD full y bootstrap: NO tocar.** Siguen sin instrucción, por diseño (D-10) — las cubre el
    backstop mecánico de D-03 y esas sesiones ya tienen continuidad propia vía GSD.

    **Restricción dura (RESEARCH §Pitfall 2 — el riesgo REAL de este plan).** Las instrucciones nuevas
    deben ser **markdown plano, sin emojis y sin secuencias ANSI**. `test/session-start.test.js:174-190`
    toma el slice desde `lastIndexOf('## Anti-push-fantasma')` **hasta el final del string** y prohíbe
    emojis y ANSI en él; el equivalente EN hace lo mismo desde `'## No automatic push'`
    (`gsd-context.test.js:189-201`). La cola del prompt ya incluye la instrucción de plan y va a
    incluir la de handoff. El resto del prompt ES sí tiene emojis legítimos — la cola no.

    **Actualizar los comentarios obsoletos**, que son parte del cambio (regla de cambios quirúrgicos:
    tocar solo lo que se debe, pero el comentario que justifica la semántica que se invierte SÍ se
    debe). En `:83` y en `:142-143`, la justificación «D-06 escribir al empezar (re-dispatch
    sobrescribe, latest-wins)» queda muerta con D-10: sustituirla por la razón nueva, citando D-10 y
    LIVE-02 (preservar-y-appendear; el historial de la tarea es el dato) y D-01 (el formato del
    bloque). No reformatear ni «mejorar» nada más del fichero.
  </action>

  <verify>
    <automated>node --test test/session-start.test.js test/gsd-context.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/session-start.test.js test/gsd-context.test.js` exits 0 — los tests preexistentes de prefijo, ruta resuelta, orden y exclusión phase/bootstrap siguen verdes sin modificarlos (D-11 re-alcanzado).
    - Source assertion: `grep -c "Además, al empezar escribe un plan corto" src/hooks/session-start.js` == 1 (prefijo ES conservado).
    - Source assertion: `grep -c "Also, at the start write a short plan" src/hooks/session-start.js` == 1 (prefijo EN conservado).
    - Source assertion: `grep -c 'kodo:handoff v=1 session=' src/hooks/session-start.js` == 2 (una instrucción de handoff por rama productora).
    - Source assertion: `grep -c 'session.session_id' src/hooks/session-start.js` >= 3 (el `:108` preexistente + las dos interpolaciones nuevas: el session_id va resuelto, no templated).
    - Behavior assertion: el guard de emojis/ANSI sobre el slice de cola sigue verde en ambos idiomas (es el caso que RESEARCH §Pitfall 2 identifica como el que romperá si la instrucción lleva emojis).
    - `git diff src/hooks/session-start.js` toca solo las dos ramas productoras y sus comentarios — las ramas phase y bootstrap sin cambios.
  </acceptance_criteria>

  <done>Las dos instrucciones ordenan preservar-y-appendear y entregan el formato de D-01 con el session_id resuelto; los comentarios obsoletos citan D-10 en vez de la semántica muerta.</done>
</task>

<task type="auto">
  <name>Task 2: Cobertura de la semántica nueva en ambos idiomas</name>

  <read_first>
    - `test/session-start.test.js:174-190` — el guard de emojis/ANSI del slice de cola (ES)
    - `test/gsd-context.test.js:189-201` — el guard equivalente (EN) y `:204-227` — el bloque `describe` PLAN-03 con los casos de presencia, ruta resuelta y exclusión phase/bootstrap: el analog exacto del par a añadir
    - `src/hooks/session-start.js` — el fichero modificado en el Task 1
    - `.planning/phases/74-handoff-acumulativo-al-cierre/74-CONTEXT.md` §decisions — D-10, D-11
  </read_first>

  <files>test/session-start.test.js, test/gsd-context.test.js</files>

  <behavior>
    - ES: el contexto de `buildSessionContext` NO contiene ya el paréntesis que ordenaba sobrescribir el plan — la semántica vieja desapareció del string construido.
    - ES: el contexto ordena explícitamente no sobrescribir y añadir al final (preservar-y-appendear, LIVE-02).
    - ES: el contexto contiene el marcador de handoff con el `session_id` **resuelto** (el de la sesión inyectada), y NO contiene un placeholder templated de session_id.
    - ES: el contexto contiene las tres etiquetas del formato: `**Hecho:**`, `**Pendiente:**` y `**NEXT:**`.
    - EN (rama quick): mismos cuatro casos sobre `buildGsdContext` con `gsd_mode: 'quick'`, conservando el prefijo EN.
    - EN: la rama **phase** (`phase_id` presente) NO recibe la instrucción de handoff — espejo del caso de exclusión que ya existe para la instrucción de plan (D-10).
    - EN: la rama **bootstrap** NO recibe la instrucción de handoff (D-10).
    - Ambos idiomas: el slice de cola (desde el heading del bloque común hasta el final del string) sigue sin emojis y sin secuencias ANSI, ahora que incluye también la instrucción de handoff.
  </behavior>

  <action>
    Extender `test/session-start.test.js` (ES) y `test/gsd-context.test.js` (EN) con los casos de
    `<behavior>`. **No modificar los casos existentes** — D-11 quedó re-alcanzado: no hay golden bytes
    que reparar, solo semántica nueva que cubrir.

    Para las asserts de ausencia de la semántica vieja, assertar sobre el **string construido** que
    devuelven `buildSessionContext` / `buildGsdContext`, no sobre el source del fichero: lo que
    importa es lo que recibe el LLM.

    Para el par de exclusión phase/bootstrap, copiar la forma de los casos ya presentes en
    `gsd-context.test.js:219` y `:224` (`assert.ok(!ctx.includes(INSTR), ...)` con un mensaje que diga
    por qué), usando como constante el prefijo de la instrucción de handoff. Esos dos casos son la
    regresión que vigila la mitad de D-10 que dice qué **no** cambia: full y bootstrap se cubren con
    el backstop mecánico, no con instrucción.

    Añadir a los casos nuevos un `describe` que cite LIVE-02 y D-10 en el nombre, para que quien los
    rompa entienda que la sobrescritura es el bug, no la feature.
  </action>

  <verify>
    <automated>node --test test/session-start.test.js test/gsd-context.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test test/session-start.test.js test/gsd-context.test.js` exits 0 y cubre todos los casos de `<behavior>`.
    - Behavior assertion: los casos nuevos fallan si se revierte el Task 1 — verificable con `git stash` del cambio de `src/hooks/session-start.js`, corriendo los tests (deben fallar) y restaurando.
    - Behavior assertion: el marcador presente en el contexto ES/EN contiene el `session_id` de la sesión inyectada (`sess-abc` en el `makeSession` de `gsd-context.test.js:15`), no un placeholder.
    - Behavior assertion: los casos de exclusión phase/bootstrap para la instrucción de handoff pasan (D-10: esas ramas se cubren con el backstop mecánico).
    - `git diff test/session-start.test.js test/gsd-context.test.js` solo añade casos; los preexistentes intactos.
  </acceptance_criteria>

  <done>La semántica preservar-y-appendear y el contrato de handoff en el prompt están cubiertos por tests en ambos idiomas, con la exclusión de las ramas full/bootstrap vigilada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider remoto → prompt | `session.summary` / `task_ref` se interpolan en el contexto que recibe el LLM (superficie preexistente, no ampliada por este plan) |
| Hook → LLM | El prompt entrega un formato que el LLM escribirá en un fichero del filesystem del operador |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-11 | Tampering | Ruta del plan en la instrucción | medium | mitigate | La ruta se **resuelve** con `join(KODO_DIR, 'plans', `${session.task_id}.md`)`, nunca se emite como literal templated; asserted por `gsd-context.test.js:212-217`. El hook del Plan 04 no confía en la ruta del prompt: reconstruye la suya y la valida con `isSafeTaskId` (T-74-01) |
| T-74-12 | Tampering | Contenido del bloque escrito por el LLM | low | accept | El LLM escribe `Hecho`/`Pendiente`/`NEXT:` con texto libre en un fichero markdown local del propio operador. Aceptado: no es HTML ni shell, y el `next` se trunca a 200 en el contrato (D-02) antes de llegar a `state.json`. El defanging del bloque escrito por el LLM no está en alcance — el LLM opera ya con permisos de escritura sobre el repo |
| T-74-13 | Tampering | Marcador forjado por el LLM con un `session_id` ajeno | low | accept | El LLM recibe SU `session_id` resuelto; podría escribir otro, pero eso solo afectaría a la detección de autoría de su propia tarea (peor caso: un bloque mecánico redundante o ausente). Sin cruce de privilegio: el fichero es local y de esa misma tarea |
| T-74-SC | Tampering | npm/pip/cargo installs | high | mitigate | Esta fase instala CERO paquetes (`74-RESEARCH.md` §Package Legitimacy Audit: *"No aplica"*). Sin tasks de install → sin checkpoint de legitimidad. Verificable: `git diff --stat package.json package-lock.json` vacío |
</threat_model>

<verification>
- `node --test test/session-start.test.js test/gsd-context.test.js` exits 0
- `npm test` — suite completa verde (baseline 2027 tests, cero regresiones)
- `git diff --stat package.json package-lock.json` — vacío
- El guard de emojis/ANSI del slice de cola verde en ambos idiomas (RESEARCH §Pitfall 2)
</verification>

<success_criteria>
- Las dos ramas productoras (no-GSD ES `:85`, GSD quick EN `:145`) ordenan preservar-y-appendear
- Ambas entregan el formato del bloque de D-01 con el `session_id` resuelto
- Las ramas GSD full y bootstrap siguen sin instrucción (asserted)
- Cero tests existentes modificados; los prefijos conservados (D-11 re-alcanzado)
- Instrucciones en markdown plano, sin emojis ni ANSI (asserted por el guard de cola)
- Cero dependencias npm nuevas
</success_criteria>

<output>
Create `.planning/phases/74-handoff-acumulativo-al-cierre/74-03-SUMMARY.md` when done
</output>
