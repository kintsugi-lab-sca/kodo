---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger-events.js
  - test/logger-events.test.js
autonomous: true
requirements:
  - TEST-02
requirements_addressed:
  - TEST-02
tags:
  - logger-events
  - ndjson-taxonomy
  - polling
must_haves:
  truths:
    - "El operador puede observar cada tick del polling en `~/.kodo/logs/*.ndjson` con shape estable"
    - "El evento `polling.dispatch` NO filtra `issue.body` (T-25-02 — invariante de seguridad)"
    - "La taxonomía cerrada de EVENTS pasa de 15 → 18 entradas sin romper consumers existentes (`kodo logs`, redactor)"
    - "Los 3 nuevos helpers están exportados y emiten al nivel correcto (info / info / warn)"
  artifacts:
    - path: "src/logger-events.js"
      provides: "Frozen EVENTS extendido con POLLING_TICK/POLLING_DISPATCH/POLLING_ERROR + 3 helpers tipados"
      contains: "export function pollingTick"
    - path: "test/logger-events.test.js"
      provides: "Asserts contractuales sobre los 3 nuevos helpers + ampliación del array de 15 → 18 events"
      contains: "pollingTick("
  key_links:
    - from: "src/logger-events.js (EVENTS frozen)"
      to: "src/triggers/polling.js (Plan 25-02 consumer)"
      via: "import { EVENTS, pollingTick, pollingDispatch, pollingError } from '../logger-events.js'"
      pattern: "POLLING_TICK|POLLING_DISPATCH|POLLING_ERROR"
    - from: "test/logger-events.test.js"
      to: "src/logger-events.js"
      via: "dynamic import + helper invocation + readAllLines assertion"
      pattern: "pollingTick|pollingDispatch|pollingError"
---

<objective>
Extender la taxonomía cerrada NDJSON con tres nuevos eventos de ciclo de polling: `polling.tick`, `polling.dispatch`, `polling.error`. Esta es la dependencia Wave 0 que Plan 25-02 (core polling.js) consume.

Purpose: Mantener el invariante LOG-12 (closed taxonomy + cero imports nuevos) y preservar la seguridad T-25-02 (cero leak de `issue.body` en NDJSON), siguiendo verbatim el patrón establecido en Phase 23 (`githubApiCall` / `githubApiCallFailed`). Sin esta extensión, `polling.js` no podría emitir eventos al sink sin violar la taxonomía cerrada documentada en `src/logger-events.js:3-11`.

Output:
- 3 entradas nuevas en `EVENTS` frozen object + JSDoc typedef ampliado
- 3 funciones helper exportadas (`pollingTick`, `pollingDispatch`, `pollingError`)
- Ampliación del array canónico en `test/logger-events.test.js` (15 → 18 events)
- ≥ 6 nuevos casos de test asegurando shape, level, y exclusión de campos sensibles
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-polling-trigger-channel/25-RESEARCH.md
@.planning/phases/25-polling-trigger-channel/25-PATTERNS.md
@.planning/phases/25-polling-trigger-channel/25-VALIDATION.md

@src/logger-events.js
@test/logger-events.test.js
@test/helpers/logger-fixtures.js

<interfaces>
<!-- Contratos extraídos del codebase que el executor consumirá. NO explorar más. -->

Existing EVENTS frozen object (src/logger-events.js:41-57) — 15 entries:
  SESSION_START, SESSION_END, STATE_TRANSITION, ORCHESTRATOR_REVIEW,
  GSD_PHASE_RESOLVED, GSD_BOOTSTRAP,
  PLANE_API_CALL, PLANE_API_CALL_FAILED,
  WORKTREE_CLEANUP_OK, WORKTREE_CLEANUP_DIRTY, WORKTREE_CLEANUP_ERROR,
  SKILL_SYNC_AUTO, SKILL_SYNC_AUTO_ERROR,
  GITHUB_API_CALL, GITHUB_API_CALL_FAILED

Header comment inventory (src/logger-events.js:3-11) — listing all events as prose; MUST be updated to include the 3 new ones.

Imports actuales del módulo (src/logger-events.js:21-22) — `node:os`, `node:path`. **Invariante LOG-12: cero imports nuevos.**

Precedente verbatim (src/logger-events.js:242-276) — `githubApiCall(logger, fields)` retorna logger.info|warn según rate_limit_remaining; `githubApiCallFailed(logger, fields)` retorna logger.error.

Test fixture helper (test/helpers/logger-fixtures.js) — `makeTmpHome({ sessionId, label })` + `readAllLines(path)` ya en uso por test/logger-events.test.js (líneas 21-22, 47-49).

Test pattern verbatim (test/logger-events.test.js:51-72) — `it('EVENTS is frozen and contains the N canonical types', () => { ... assert.deepEqual(types, [sorted list]) })`. Este test array debe pasar de 15 → 18 entradas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender EVENTS frozen + JSDoc typedef + header con 3 nuevos eventos de polling</name>
  <files>src/logger-events.js</files>
  <read_first>
    - src/logger-events.js (file completo; ver header lines 3-11, typedef lines 24-40, EVENTS lines 41-57, githubApiCall precedente lines 242-276)
    - .planning/phases/25-polling-trigger-channel/25-PATTERNS.md (§ src/logger-events.js (taxonomy, MODIFY) — precedente Phase 23 lines 372-440)
    - .planning/phases/25-polling-trigger-channel/25-RESEARCH.md (§ Code Examples → Example 1: NDJSON event helpers lines 569-635)
  </read_first>
  <behavior>
    - Header comment line 3-11 ampliado para mencionar `polling.tick`, `polling.dispatch`, `polling.error` en el inventario textual (precedente: Phase 23 añadió `github.api.call` + `github.api.call.failed` en la misma lista)
    - JSDoc typedef de EVENTS (lines 24-40) ampliado de 15 a 18 entradas — añadir `POLLING_TICK: 'polling.tick'`, `POLLING_DISPATCH: 'polling.dispatch'`, `POLLING_ERROR: 'polling.error'`
    - `EVENTS` frozen object (lines 41-57) ampliado con las 3 entradas; alineación visual respetada (colón + 11 espacios, igual que el resto)
    - 3 funciones helper exportadas al final del archivo, después de `skillSyncAutoError` (lines 365-372):
      - `pollingTick(logger, fields)` → `logger.info(EVENTS.POLLING_TICK, { event, owner, repo, status, dispatched, ...(fields.first_tick ? { first_tick: true } : {}) })`
      - `pollingDispatch(logger, fields)` → `logger.info(EVENTS.POLLING_DISPATCH, { event, owner, repo, ref, pattern })` — **SOLO esos 4 campos de identificación, NUNCA `issue.body`/title/raw** (invariante T-25-02)
      - `pollingError(logger, fields)` → `logger.warn(EVENTS.POLLING_ERROR, { event, owner, repo, status, attempt, ...(fields.error ? { error: fields.error } : {}) })`
    - Cero imports nuevos — el archivo sigue importando solo `node:os` y `node:path` (invariante LOG-12)
    - `Object.freeze(EVENTS)` preservado tras la ampliación
  </behavior>
  <action>
    Editar `src/logger-events.js`. Actualizar el header comment (líneas 3-11) añadiendo `polling.tick, polling.dispatch, polling.error` en el inventario y la cita "Phase 25 (polling trigger channel)". Ampliar el JSDoc typedef de `EVENTS` (líneas 24-40) con las 3 nuevas keys. Ampliar el frozen object con las 3 entradas manteniendo el sufijo de string `'polling.tick'`/`'polling.dispatch'`/`'polling.error'` y la alineación visual existente. Añadir tres funciones helper al final del archivo siguiendo verbatim el patrón de `githubApiCall` / `githubApiCallFailed`: `pollingTick` (level `info`, payload `{event, owner, repo, status, dispatched, first_tick?}`), `pollingDispatch` (level `info`, payload `{event, owner, repo, ref, pattern}` — invariante de seguridad T-25-02: cero `issue.body`/title/raw), `pollingError` (level `warn`, payload `{event, owner, repo, status, attempt, error?}`). Conservar los dos únicos imports stdlib existentes; cero imports nuevos para no violar LOG-12. JSDoc completo en cada helper (typedef Logger, fields shape) siguiendo la prosa de los helpers Phase 23.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "POLLING_TICK\|POLLING_DISPATCH\|POLLING_ERROR" src/logger-events.js` ≥ 6 (3 en typedef + 3 en frozen object, y luego cada uno se usa también dentro de su helper — total ≥ 9)
    - `grep -c "^export function polling" src/logger-events.js` == 3
    - `grep -n "polling.tick\|polling.dispatch\|polling.error" src/logger-events.js` muestra al menos las 3 string literals como values del frozen object
    - `grep -v '^#' src/logger-events.js | grep -c "issue\.body\|issue\.title\|raw:" ` == 0 (invariante de seguridad T-25-02 — los helpers de polling no acceden a esos campos)
    - `grep -c "^import " src/logger-events.js` ≤ 2 (solo `node:os` + `node:path`; invariante LOG-12)
    - `Object.isFrozen` sigue siendo true sobre `EVENTS` (cubierto por test EVENTS frozen contract)
    - `node -e "const m = await import('./src/logger-events.js'); console.log(typeof m.pollingTick, typeof m.pollingDispatch, typeof m.pollingError);"` imprime `function function function`
  </acceptance_criteria>
  <done>
    `src/logger-events.js` exporta 3 nuevos helpers + EVENTS ampliado a 18 entradas; cero imports nuevos; archivo compila bajo `node --check src/logger-events.js`; el invariante T-25-02 (cero `issue.body` en helpers de polling) es verificable por grep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ampliar test/logger-events.test.js con cobertura contractual de los 3 helpers</name>
  <files>test/logger-events.test.js</files>
  <read_first>
    - test/logger-events.test.js (file completo; ver imports lines 27-46, contract test EVENTS lines 51-72, sessionStart pattern lines 74+ para shape)
    - src/logger-events.js (post-Task 1, para confirmar shape exacta de los 3 helpers)
    - test/helpers/logger-fixtures.js (helpers `makeTmpHome` + `readAllLines`)
    - .planning/phases/25-polling-trigger-channel/25-VALIDATION.md (§ Per-Task Verification Map — fila `TEST-02` para `polling.tick/dispatch/error` y la fila `T-25-02 token leak`)
  </read_first>
  <behavior>
    - Tests añadidos dentro del `describe` existente (`logger-events taxonomy`); cabecera del describe actualizada para añadir "+ Phase 25 polling trigger channel"
    - Bloque de import (lines 27-46) ampliado con `pollingTick`, `pollingDispatch`, `pollingError` desde `'../src/logger-events.js'`
    - Test `EVENTS is frozen and contains the N canonical types` (lines 51-72): el array sorted pasa de 15 a 18 entradas — debe incluir literales `'polling.tick'`, `'polling.dispatch'`, `'polling.error'` en orden lexicográfico; el comentario del título pasa de "15" a "18"
    - 6 nuevos tests `it(...)` al final del describe, cada uno con su propio `sessionId` aislado (sufijo `pollev-tick`, `pollev-dispatch`, `pollev-error-warn`, `pollev-tick-first`, `pollev-dispatch-redaction`, `pollev-error-no-error-field`):
      1. `pollingTick emits event with {owner, repo, status, dispatched}` — invocar con `{owner:'octocat', repo:'hello', status:200, dispatched:3}` y assert que la línea NDJSON tiene `event === 'polling.tick'`, `level === 'info'`, los 4 campos verbatim
      2. `pollingTick includes first_tick:true when set` — invocar con `{owner, repo, status:200, dispatched:0, first_tick:true}` y assert `first_tick === true` en la línea; assert que cuando `first_tick` no se pasa, la key NO aparece (truthy-spread)
      3. `pollingDispatch emits event with {owner, repo, ref, pattern}` — invocar con `{owner:'octocat', repo:'hello', ref:'octocat/hello#42', pattern:'a-new'}` y assert los 4 campos verbatim; assert `level === 'info'`
      4. `pollingDispatch does NOT include issue.body / title / raw (T-25-02 invariant)` — pasar un `fields` extendido con propiedades extra `{issueBody:'secret', title:'leak', raw:{token:'xxx'}}` y assert que la línea NDJSON parsed NO contiene ninguna de esas keys (whitelist-strict shape)
      5. `pollingError emits at warn level with {owner, repo, status, attempt}` — invocar con `{owner, repo, status:429, attempt:2}` y assert `level === 'warn'`, los 4 campos verbatim
      6. `pollingError includes error field only when provided` — invocar primero sin `error`, assert `error` key ausente; luego con `error:'rate limited'`, assert `error === 'rate limited'`
  </behavior>
  <action>
    Editar `test/logger-events.test.js`. Ampliar la lista de imports dinámicos (líneas 27-46) añadiendo `pollingTick, pollingDispatch, pollingError` al destructuring del import de `'../src/logger-events.js'`. Actualizar el título del `describe` (línea 51) para mencionar "Phase 25 polling trigger channel". Modificar el test existente `EVENTS is frozen and contains the N canonical types` cambiando el numeral en el título de 15 a 18 y añadiendo `'polling.dispatch'`, `'polling.error'`, `'polling.tick'` en orden lexicográfico al array literal del `assert.deepEqual`. Añadir 6 nuevos `it(...)` al final del describe siguiendo verbatim el patrón de tests existentes (sessionId único + `createLogger` + invoke helper + `readAllLines(logPathFor(sessionId))` + JSON.parse + asserts). El test #4 (`pollingDispatch does NOT include issue.body`) es el guardián explícito del invariante T-25-02 — incluir el comentario `// T-25-02: invariante de seguridad — polling.dispatch NDJSON NO debe filtrar contenido de usuario`. Reutilizar `logPathFor(sessionId)` ya definido en línea 47. Cero nuevos helpers; cero nuevos imports salvo los 3 nombres añadidos al destructuring existente.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/logger-events.test.js` reporta `pass` con count incrementado (≥ baseline + 6)
    - `grep -c "pollingTick\|pollingDispatch\|pollingError" test/logger-events.test.js` ≥ 9 (3 en import + 6 invocaciones mínimo)
    - El test `EVENTS is frozen and contains the 18 canonical types` pasa, con array verbatim que incluye los 3 nuevos literales
    - El test #4 (T-25-02 invariant) falla si en producción se añadiese accidentalmente un campo como `body` o `raw` al payload de `pollingDispatch` — verificable corriendo el test y luego mutando `pollingDispatch` para añadir `body: fields.issueBody` y confirmar fallo
    - El test `pollingError emits at warn level` falla si el helper se cambiase a `logger.error` o `logger.info` — verificable por mutación
    - Wall-time del test file < 1s (todos clock-mock o sync writes a tmp NDJSON)
    - Cero `setTimeout(..., > 0)` real en los nuevos tests (no aplica clock mock aquí; los tests escriben NDJSON sincrónicamente y leen — pattern existente)
  </acceptance_criteria>
  <done>
    Suite `test/logger-events.test.js` verde con ≥ 6 nuevos casos cubriendo los 3 helpers + el invariante de seguridad T-25-02; ampliación del contract test del array de EVENTS (15 → 18). Esto cierra Wave 0: Plan 25-02 puede ahora importar y consumir los 3 helpers sin Wave-0-MISSING.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| polling.js → logger sink → ~/.kodo/logs/*.ndjson | Datos de polling cruzan a archivo en disco (potencial leak si payload incluye contenido de usuario) |
| logger sink → `kodo logs` consumer (tail/grep) | NDJSON puede ser inspeccionado a posteriori; cualquier secreto persiste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | I (Information disclosure) | `pollingDispatch` helper en `src/logger-events.js` | **mitigate** | El helper SOLO acepta y emite `{event, owner, repo, ref, pattern}` — whitelist estricta sin spread del input. Cualquier campo extra del caller queda silenciosamente descartado. Test #4 del Task 2 documenta y verifica el invariante (asserts ausencia de `body`/`title`/`raw`). No se accede a `issue.body` ni en la firma JSDoc ni en el cuerpo del helper. |
| T-25-04 (parcial) | D (DoS via log flood) | `pollingTick`/`pollingError` | **accept** | Helpers no rate-limitan emisión; el rate-limit emerge de la lógica de loop en Plan 25-02 (max 3 retries × N repos × 1 tick/intervalSec). Aceptable porque kodo es uso personal y el sink NDJSON es append-only file. |
</threat_model>

<verification>
- `node --test test/logger-events.test.js` verde con count ≥ baseline + 6
- `grep -c "issue\.body\|issue\.title\|fields\.raw" src/logger-events.js` == 0 (T-25-02 source assertion)
- `grep -c "^import " src/logger-events.js` ≤ 2 (LOG-12 source assertion)
- `node --check src/logger-events.js` exit 0
- Sin regresiones: `npm test` corre y baseline+6 tests verdes (post-task)
</verification>

<success_criteria>
- `src/logger-events.js` exporta `pollingTick`, `pollingDispatch`, `pollingError` y EVENTS contiene `POLLING_TICK`, `POLLING_DISPATCH`, `POLLING_ERROR` (3 string literals `polling.tick`/`polling.dispatch`/`polling.error`)
- `test/logger-events.test.js` valida shape, level, y ausencia de campos sensibles (T-25-02)
- Invariante LOG-12 preservado: cero imports nuevos en `src/logger-events.js`
- Plan 25-02 puede ahora `import { EVENTS, pollingTick, pollingDispatch, pollingError } from '../logger-events.js'` sin Wave-0-MISSING
</success_criteria>

<output>
After completion, create `.planning/phases/25-polling-trigger-channel/25-01-SUMMARY.md` con:
- 3 helpers + 3 EVENTS añadidos (con shape exacta documentada)
- Confirmación del invariante T-25-02 (whitelist verificada por test)
- Confirmación del invariante LOG-12 (cero imports nuevos)
- Count de tests añadidos (≥ 6)
- Referencia a Plan 25-02 como next-step consumer
</output>
