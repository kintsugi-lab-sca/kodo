---
phase: 25
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/triggers/polling.js
  - test/triggers/polling.test.js
  - test/check-isolation.test.js
autonomous: true
requirements:
  - POLL-01
  - POLL-02
  - POLL-03
  - POLL-04
  - TEST-02
requirements_addressed:
  - POLL-01
  - POLL-02
  - POLL-03
  - POLL-04
  - TEST-02
tags:
  - polling
  - trigger-channel
  - github
  - clock-mock
  - state-cache
must_haves:
  truths:
    - "`startPolling({provider?, client?, repos, intervalSec, clock?, logger?, statePath?, dispatchTriggerFn?})` arranca un loop async recursivo y retorna `{stop}` cancelable"
    - "El primer tick NO dispara dispatch — solo puebla el cursor (evita storm de 50 sesiones en repos con backlog histórico, Pitfall #7)"
    - "Issues con `pull_request !== null` se filtran (Pitfall #2, Phase 24 invariant)"
    - "Respuesta 304 Not Modified no actualiza el cursor; respuesta 200 actualiza a `max(updated_at)` de los items"
    - "Errores transitorios (429, 500-504, AbortError, ETIMEDOUT) entran en backoff exponencial 2s base × 3 retries y emiten `polling.error{owner, repo, status, attempt}`"
    - "Tras 3 retries agotados: warn-and-continue — el siguiente tick reintenta, el proceso parent nunca crashea"
    - "`dispatchTrigger` se invoca fire-and-forget (sin `await`); cualquier rejection se loggea pero NO propaga al loop"
    - "Cache cargado desde JSON corrupto o ausente → return `{}` (POLL-02 fail-open); cache guardado vía `tmp + rename` atómico (POSIX)"
    - "Cero `Date.now()` directo en `polling.js` (Pitfall #5 — clock.now() exclusivo)"
    - "`src/check.js` NO importa `src/triggers/polling.js` transitivamente (LOG-12 extendido)"
    - "Wall-time de toda la suite `test/triggers/polling.test.js` < 1s (clock-mock; zero real setTimeout en happy path)"
    - "Cero `globalThis.fetch` calls en tests (live-fetch leak guard)"
  artifacts:
    - path: "src/triggers/polling.js"
      provides: "startPolling({...}) -> {stop} — tercer canal trigger; loop async cancelable + state cache + retry backoff + fire-and-forget dispatch"
      contains: "export function startPolling"
      min_lines: 150
    - path: "test/triggers/polling.test.js"
      provides: "Suite TEST-02: ~22-25 casos cubriendo POLL-01..04 + clock-mock + live-fetch leak guard + first-tick skip + PR filter + T-25-02 polling.dispatch redaction"
      contains: "createTestClock"
      min_lines: 400
    - path: "test/check-isolation.test.js"
      provides: "+1 it() row asserting check.js NO importa polling.js transitivamente (LOG-12 extension)"
      contains: "triggers/polling.js"
  key_links:
    - from: "src/triggers/polling.js (processRepo)"
      to: "src/providers/github/client.js (listIssues envelope {status, items, etag})"
      via: "client.listIssues(owner, repo, {labels:['kodo'], state:'open', since, etag}) — direct optimized path"
      pattern: "client\\.listIssues"
    - from: "src/triggers/polling.js (processRepo fallback)"
      to: "src/interface.js TaskProvider.listPendingTasks"
      via: "provider.listPendingTasks() — fallback when only opts.provider injected (Phase 27 cross-provider path)"
      pattern: "provider\\.listPendingTasks"
    - from: "src/triggers/polling.js (dispatch site)"
      to: "src/triggers/dispatcher.js (dispatchTrigger)"
      via: "dispatchFn({taskRef, action:'polling', provider:'github', raw:issue}, {}).catch(...) — fire-and-forget"
      pattern: "dispatchFn|dispatchTrigger"
    - from: "src/triggers/polling.js (state cache)"
      to: "~/.kodo/polling-state.json (filesystem)"
      via: "loadStateCache / saveStateCache using KODO_DIR import + tmp + rename"
      pattern: "polling-state\\.json"
    - from: "src/triggers/polling.js (normalize)"
      to: "src/providers/github/normalize.js (normalizeIssue)"
      via: "normalizeIssue(issue, {projectId: `${owner}/${repo}`})"
      pattern: "normalizeIssue"
    - from: "src/triggers/polling.js (events)"
      to: "src/logger-events.js (Plan 25-01 helpers)"
      via: "import { pollingTick, pollingDispatch, pollingError } from '../logger-events.js'"
      pattern: "pollingTick|pollingDispatch|pollingError"
    - from: "test/check-isolation.test.js"
      to: "src/check.js (walkImports graph)"
      via: "new it() row filtering for '/triggers/polling.js'"
      pattern: "triggers/polling\\.js"
---

<objective>
Crear el tercer canal de trigger (`src/triggers/polling.js`) — loop async recursivo cancelable que descubre issues con label `kodo` en repos configurados, dispara `dispatchTrigger` con `TaskItem` normalizado fire-and-forget, persiste cursor + etag en `~/.kodo/polling-state.json`, recupera de errores transitorios con backoff exponencial, y nunca crashea el proceso parent. Acompañar con suite TEST-02 completa (clock-mock, ~22-25 casos, <1s wall-time) y extensión del guardián LOG-12.

Purpose: Cierra los 5 requirements de Phase 25 (POLL-01..04 + TEST-02). Es **la pieza central de v0.7** porque demuestra el invariante provider-agnostic con el primer trigger no-webhook (espejo arquitectónico de `webhook.js`). La complejidad está en el clock-mock testing y la persistencia atómica con reset fail-open — todo lo demás es orquestación de Phase 23 (`GitHubClient.listIssues` envelope), Phase 24 (`getProvider('github')` + `normalizeIssue`), `dispatchTrigger` (v0.2), y los helpers Plan 25-01.

Output:
- `src/triggers/polling.js` con `startPolling(opts) -> {stop}` y helpers internos `loadStateCache`/`saveStateCache`/`processRepo`/`shouldDispatch`/`classifyPattern`
- `test/triggers/polling.test.js` con `createTestClock()` controller + `makeFakeClient()` + `makeFakeLogger()` + ~22-25 `it(...)` casos
- `test/check-isolation.test.js` con `+1 it()` row para LOG-12 polling.js
- Directorio `test/triggers/` creado (no existía)
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-polling-trigger-channel/25-RESEARCH.md
@.planning/phases/25-polling-trigger-channel/25-PATTERNS.md
@.planning/phases/25-polling-trigger-channel/25-VALIDATION.md
@.planning/phases/25-polling-trigger-channel/25-01-SUMMARY.md

@src/triggers/webhook.js
@src/triggers/dispatcher.js
@src/providers/github/client.js
@src/providers/github/provider.js
@src/providers/github/normalize.js
@src/config.js
@src/logger-events.js
@test/webhook.test.js
@test/providers/github/provider.test.js
@test/dispatcher.test.js
@test/check-isolation.test.js
@test/fixtures/github/issues-list.json

<interfaces>
<!-- Contratos extraídos del codebase que el executor consumirá directamente. NO explorar más. -->

GitHubClient.listIssues envelope (Phase 23 D-19, verified src/providers/github/client.js):
  client.listIssues(owner, repo, { labels: string[], state: 'open'|'closed', since?: string, etag?: string })
  Returns Promise<{ status: 200|304, items: Issue[], etag?: string, rate_limit_remaining?: number }>
  Throws Error with .status (429/4xx/5xx) or { code: 'ETIMEDOUT' } / name 'AbortError' on transient

normalizeIssue (Phase 24, src/providers/github/normalize.js):
  normalizeIssue(issue, { projectId: string }) -> TaskItem
  TaskItem shape: { id, ref, title, description, labels, projectId, projectName, url, state, groups, priority }
  ref format: `${owner}/${repo}#${number}`

TaskProvider.listPendingTasks (src/interface.js:41):
  provider.listPendingTasks() -> Promise<TaskItem[]>
  (Phase 24 GitHubProvider already filters PRs internally — Pitfall #2)

TriggerEvent (src/interface.js:27-32):
  { taskRef: string, action: string, provider: string, raw: object }

dispatchTrigger (src/triggers/dispatcher.js:43):
  dispatchTrigger(event, opts = {}, deps = {}) -> Promise<any>
  Polling MUST call as fire-and-forget: dispatchFn(event, {}).catch(err => ...)

KODO_DIR export (src/config.js:6 — note: NOT currently exported; verify before import):
  const KODO_DIR = join(homedir(), '.kodo')
  ⚠ Verify: line 105+ has `export { KODO_DIR, CONFIG_PATH, PROJECTS_PATH, DEFAULT_CONFIG }` per PATTERNS.md line 105.
  If not exported, executor must export it (additive change to config.js — Rule 3 quirúrgico).
  Alternative (preferred if ambiguous): re-compute inline as `join(homedir(), '.kodo')` to avoid mutating config.js in this plan.

Logger events helpers (Plan 25-01 output, src/logger-events.js):
  pollingTick(logger, {owner, repo, status, dispatched, first_tick?})
  pollingDispatch(logger, {owner, repo, ref, pattern})  // T-25-02: NO body / no raw
  pollingError(logger, {owner, repo, status, attempt, error?})

webhook.js fire-and-forget precedent (src/triggers/webhook.js:46-48):
  dispatchFn(triggerEvent).catch((err) => console.error(`[kodo] Dispatch error: ${err.message}`));

provider.test.js live-fetch leak guard (test/providers/github/provider.test.js:40-49):
  const _originalFetch = globalThis.fetch;
  before(() => { globalThis.fetch = () => { throw new Error('live fetch leak: test must inject opts.client'); }; });
  after(() => { globalThis.fetch = _originalFetch; });

check-isolation row precedent (test/check-isolation.test.js:113-131):
  it('kodo check does not import src/providers/github/provider.js transitively', () => {
    const graph = walkImports(join(SRC, 'check.js'));
    const violators = [...graph].filter((p) => p.endsWith('/providers/github/provider.js'));
    assert.deepEqual(violators, [], `...`);
  });
</interfaces>

<critical_invariants>
- LOG-12: `src/check.js` NO importa `src/triggers/polling.js` transitivamente → assertable via Task 4 row
- Lock per-repo Phase 8 GSD-10: polling delega idempotencia al dispatcher; NO introduce nuevo mecanismo de dedup
- Dispatcher fire-and-forget: `dispatchFn(...).catch(...)` — NEVER `await`, NEVER propagate rejection
- Worktree path Phase 18: polling NO toca cómputo de worktree
- HOOK-01 universal Phase 20: sesiones disparadas por polling heredan el bloque anti-push-fantasma sin trabajo nuevo
- Clock injection (Pitfall #5): polling.js usa `clock.now()` exclusivamente; cero `Date.now()` directo (lint-able)
- T-25-02: `polling.dispatch` NDJSON payload SOLO {owner, repo, ref, pattern}; cero `issue.body`/title/raw
- First-tick skip (Pitfall #7): primer poll por repo popula cursor SIN dispatch
- POSIX-only atomic write: `tmp + rename` documentado como Mac/Linux only (Pitfall #6)
- Zero new deps: solo stdlib + módulos ya existentes
</critical_invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/triggers/polling.js — startPolling + state cache + retry + fire-and-forget dispatch</name>
  <files>src/triggers/polling.js</files>
  <read_first>
    - src/triggers/webhook.js (file completo — precedente fire-and-forget, dispatchFn fallback, DI typedef)
    - src/triggers/dispatcher.js (lines 18-55 — DI deps typedef + getProviderFn/launchWorkItemFn pattern)
    - src/providers/github/client.js (lines 1-31 header style + 42-69 transient status set + 147-148 dynamic import precedent; usar este último SOLO como referencia — polling.js usa imports estáticos porque NO está en check.js graph)
    - src/providers/github/provider.js (lines 133-147 — listPendingTasks envelope consumption + PR filter line)
    - src/providers/github/normalize.js (firma `normalizeIssue(issue, {projectId})` y TaskItem shape)
    - src/config.js (lines 1-9 + 69-73 + 105 final exports — verificar si KODO_DIR está exportado; si no, computar inline)
    - src/logger-events.js (post-Plan-25-01 — confirmar firma de los 3 helpers + EVENTS values)
    - src/interface.js (TriggerEvent typedef + TaskProvider typedef)
    - .planning/phases/25-polling-trigger-channel/25-RESEARCH.md (§ Architecture Patterns 1-4 + § Pitfalls 1-7 + § Code Examples 1-3 + § Open Questions Resolved)
    - .planning/phases/25-polling-trigger-channel/25-PATTERNS.md (file completo — todos los analogs aplicados aquí)
  </read_first>
  <behavior>
    Estructura del módulo (top → bottom):
    - Header `// @ts-check` + JSDoc módulo doc-comment estilo Phase 23 client.js (referenciar `webhook.js` como mirror + `Plane client.js` retry como divergence motive + Phase 24 D-25 + Pitfalls #2/#5/#6/#7 + invariantes LOG-12, T-25-02)
    - Imports estáticos: `dispatchTrigger` de `./dispatcher.js`; `normalizeIssue` de `../providers/github/normalize.js`; `EVENTS, pollingTick, pollingDispatch, pollingError` de `../logger-events.js`; `readFileSync, writeFileSync, renameSync, existsSync, mkdirSync` de `node:fs`; `join, dirname` de `node:path`; `homedir` de `node:os` (o importar `KODO_DIR` de `../config.js` SI está exportado; verificar primero)
    - JSDoc typedef `Clock`: `{ setTimeout: (fn, ms) => any, clearTimeout: (h) => void, now: () => number }`
    - Const `DEFAULT_CLOCK` con los 3 métodos delegando a `globalThis.setTimeout` / `globalThis.clearTimeout` / `() => Date.now()` (este `Date.now()` es la **única ocurrencia permitida** en todo el archivo, dentro de DEFAULT_CLOCK — el resto del módulo usa `clock.now()`)
    - Constantes módulo: `RETRY_BASE_MS = 2000`, `RETRY_MAX_ATTEMPTS = 3`, `TRANSIENT_STATUSES = new Set([429, 500, 502, 503, 504])`, `DEFAULT_STATE_PATH = join(homedir(), '.kodo', 'polling-state.json')`
    - `loadStateCache(path = DEFAULT_STATE_PATH)`: si !existsSync → return `{}`. Try readFileSync + JSON.parse + defensive `if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) return parsed; return {}`. Catch → return `{}` (POLL-02 fail-open, cero throw)
    - `saveStateCache(cache, path = DEFAULT_STATE_PATH)`: `mkdirSync(dirname(path), {recursive: true})` defensive; const `tmp = path + '.tmp'`; `writeFileSync(tmp, JSON.stringify(cache, null, 2) + '\n')`; `renameSync(tmp, path)` (atomic POSIX, documentar Pitfall #6 en comment inline)
    - `shouldDispatch(issue, prev)`: si `!prev.last_updated_at` return `false` (first-tick: NO dispatch — Pitfall #7); else return `issue.updated_at > prev.last_updated_at`
    - `classifyPattern(issue, prev)`: si `!prev.last_updated_at` return `'first-tick'`; si `issue.created_at && issue.created_at > prev.last_updated_at` return `'a-new'`; else return `'b-or-c-updated'` (forensic hint — NO contract)
    - `sleep(clock, ms)`: `return new Promise((resolve) => clock.setTimeout(resolve, ms))` — bridge para que retry sea testeable
    - `processRepo({owner, repo, cache, client, provider, dispatchFn, clock, logger, isFirstTick})`: while loop con `attempt ≤ RETRY_MAX_ATTEMPTS`. Dentro del try: si hay `client` → llamar `client.listIssues(owner, repo, {labels:['kodo'], state:'open', since: prev.last_updated_at, etag: prev.etag})`. Else (solo provider) → llamar `provider.listPendingTasks()` y construir un envelope sintético `{status: 200, items: filtered_for_this_repo, etag: undefined}` (filtrar items cuyo `projectId === ${owner}/${repo}`). Si `result.status === 304` → emit `pollingTick({owner, repo, status:304, dispatched:0})` y return. Si 200 → iterar `result.items`, skip `if (issue.pull_request) continue` (Pitfall #2, comment inline citando Phase 24 D-25), calcular `shouldDispatch`, si true llamar `normalizeIssue` + `pollingDispatch({owner, repo, ref:task.ref, pattern:classifyPattern(...)})` + `dispatchFn({taskRef:task.ref, action:'polling', provider:'github', raw:issue}, {}).catch(err => logger?.error('polling.dispatch.failed', ...))` (fire-and-forget, citar webhook.js:46-48). Tras el for, calcular `maxUpdatedAt` y actualizar `cache[key] = {last_updated_at: maxUpdatedAt, etag: result.etag}`. Llamar `saveStateCache(cache, statePath)`. Emit `pollingTick({owner, repo, status:200, dispatched, ...(isFirstTick ? {first_tick: true} : {})})`. Return. Catch: extraer `status` de err; `isTransient = TRANSIENT_STATUSES.has(status) || err.code === 'ETIMEDOUT' || err.name === 'AbortError'`. `attempt++`. Emit `pollingError({owner, repo, status, attempt, error: err.message})`. Si `!isTransient || attempt > RETRY_MAX_ATTEMPTS` → return (warn-and-continue, NO emite `polling.stopped` — Open Question resolved). Else → `await sleep(clock, RETRY_BASE_MS * Math.pow(2, attempt - 1))`
    - `startPolling(opts)`: const `clock = opts.clock || DEFAULT_CLOCK`, `intervalMs = (opts.intervalSec ?? 60) * 1000`, `dispatchFn = opts.dispatchTriggerFn || dispatchTrigger`, `statePath = opts.statePath || DEFAULT_STATE_PATH`. Validar que al menos uno de `opts.provider` u `opts.client` esté presente (sino throw `Error('startPolling requires opts.provider or opts.client')` — fail-fast, no en runtime del tick). `let stopped = false`; `let timer = null`; `const firstTickPerRepo = new Set()` (track which repos have done their first tick). Función interna async `tick()`: si `stopped` return. const `cache = loadStateCache(statePath)`. For cada `{owner, repo}` en `opts.repos`: si stopped break. `const isFirstTick = !firstTickPerRepo.has(\`${owner}/${repo}\`)`. `await processRepo({owner, repo, cache, client: opts.client, provider: opts.provider, dispatchFn, clock, logger: opts.logger, isFirstTick, statePath})`. `firstTickPerRepo.add(\`${owner}/${repo}\`)`. Tras el for, si !stopped → `timer = clock.setTimeout(tick, intervalMs)`. Kick-off: `Promise.resolve().then(tick).catch(err => opts.logger?.error('polling.loop.error', {error: err.message}))` (fail-open al loop level). Return `{stop() { stopped = true; if (timer) clock.clearTimeout(timer); }}`
    - JSDoc completo en `startPolling`: typedef del opts object (provider?, client?, repos, intervalSec?, clock?, logger?, statePath?, dispatchTriggerFn?), retorno `{stop: () => void}`
  </behavior>
  <action>
    Crear `src/triggers/polling.js`. Empezar con `// @ts-check` + header doc-comment estilo Phase 23 client.js (referenciar webhook.js como mirror estructural + dispatcher.js como DI precedent + Phase 24 D-25 PR filter + Pitfalls #2/#5/#6/#7 explícitos + invariantes LOG-12 / T-25-02 / first-tick-skip / fire-and-forget / clock-injection). Imports estáticos siguiendo PATTERNS § "Dynamic import of logger-events helpers" — usar **estático** porque polling.js no está en check.js graph (Task 4 lo guarda). Si `KODO_DIR` está exportado en `src/config.js` (verificar línea 105+), importarlo; de lo contrario computar `DEFAULT_STATE_PATH = join(homedir(), '.kodo', 'polling-state.json')` inline sin tocar config.js (regla 3 quirúrgico). Definir typedef Clock + DEFAULT_CLOCK (la **única** ocurrencia de `Date.now()` permitida en el archivo está dentro de DEFAULT_CLOCK.now); constantes RETRY_BASE_MS=2000, RETRY_MAX_ATTEMPTS=3, TRANSIENT_STATUSES=Set([429,500,502,503,504]). Funciones internas no exportadas: `loadStateCache`, `saveStateCache`, `shouldDispatch`, `classifyPattern`, `sleep`, `processRepo`. Función exportada: `startPolling(opts) -> {stop}`. La implementación sigue el sketch documentado en RESEARCH § Code Examples Example 1+2+3 y PATTERNS § Pattern 1-5, aplicando: first-tick skip (NO dispatch en primer tick por repo, solo poblar cursor), PR filter `if (issue.pull_request) continue` con comentario citing Pitfall #2, fire-and-forget `dispatchFn(...).catch(...)` (espejo `webhook.js:46-48`), retry exponencial via `sleep(clock, RETRY_BASE_MS * 2^(attempt-1))`, warn-and-continue tras retry agotado (NO emite `polling.stopped` event — Open Question resolved a simplicidad), `saveStateCache` con `tmp + rename` atómico documentando POSIX-only en comentario inline, validación fail-fast de `opts.provider || opts.client` al inicio de startPolling. Hybrid path: si `opts.client` presente → ruta optimizada `client.listIssues` con `since` + `etag` (envelope `{status, items, etag, rate_limit_remaining}`). Si solo `opts.provider` → `provider.listPendingTasks()` + filtrar items por `projectId === \`${owner}/${repo}\`` + construir envelope sintético `{status:200, items, etag:undefined}` (Phase 27 cross-provider path). Emisión NDJSON via los 3 helpers Plan 25-01: `pollingTick`/`pollingDispatch`/`pollingError`. Solo el helper `pollingDispatch` recibe `{owner, repo, ref, pattern}` — NUNCA pasarle `issue.body` o `raw` (T-25-02; el helper en sí también lo blinda, esto es defensa en profundidad).
  </action>
  <verify>
    <automated>node --check src/triggers/polling.js && node -e "const m = await import('./src/triggers/polling.js'); console.log('startPolling:', typeof m.startPolling);"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/triggers/polling.js` exit 0
    - `node -e "const m = await import('./src/triggers/polling.js'); console.log(typeof m.startPolling)"` imprime `function`
    - `grep -v '^//' src/triggers/polling.js | grep -v '^\s*\*' | grep -c "Date\.now()"` == 1 (única ocurrencia es dentro de DEFAULT_CLOCK.now — Pitfall #5)
    - `grep -c "issue\.body\|raw:\s*issue\.body" src/triggers/polling.js` == 0 (el campo `raw: issue` completo SÍ se pasa a `dispatchTrigger` porque el dispatcher lo necesita; pero `pollingDispatch` helper NO recibe body — T-25-02)
    - `grep -c "if (issue\.pull_request)" src/triggers/polling.js` ≥ 1 (PR filter — Pitfall #2)
    - `grep -c "renameSync" src/triggers/polling.js` ≥ 1 (atomic write — Pitfall #6)
    - `grep -c "RETRY_BASE_MS\s*=\s*2000\|RETRY_MAX_ATTEMPTS\s*=\s*3" src/triggers/polling.js` ≥ 2 (constantes lock-in)
    - `grep -c "\.catch(" src/triggers/polling.js` ≥ 1 (fire-and-forget dispatchFn)
    - `grep -E "await\s+dispatchFn|await\s+dispatchTrigger" src/triggers/polling.js | wc -l` == 0 (NUNCA await dispatch — invariante fire-and-forget)
    - `grep -c "first_tick\|firstTick" src/triggers/polling.js` ≥ 2 (Pitfall #7 first-tick skip)
    - `grep -c "import " src/triggers/polling.js` ≥ 6 y ≤ 12 (imports razonables; estáticos)
    - `grep -c "^import.*from 'picocolors'" src/triggers/polling.js` == 0 (color isolation invariant)
    - El archivo declara `export function startPolling` exactamente 1 vez; cero otras exportaciones (helpers internos privados al módulo)
  </acceptance_criteria>
  <done>
    `src/triggers/polling.js` existe, exporta `startPolling`, compila bajo `node --check`, respeta los 10 invariantes críticos (LOG-12, T-25-02, Pitfalls #2/#5/#6/#7, fire-and-forget, retry constants, color isolation, first-tick skip). Listo para Task 2 (tests).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test/triggers/polling.test.js — suite TEST-02 con clock-mock controller</name>
  <files>test/triggers/polling.test.js</files>
  <read_first>
    - src/triggers/polling.js (post-Task 1 — confirmar signaturas exactas)
    - test/webhook.test.js (lines 1-50 — fakeProvider helper + dispatch spy + fire-and-forget assertion pattern)
    - test/providers/github/provider.test.js (lines 37-100 — live-fetch leak guard + makeFakeClient + calls collector)
    - test/dispatcher.test.js (lines 1-71 — node:test imports + beforeEach reset pattern)
    - test/fixtures/github/issues-list.json (fixture existente: 2 issues + 1 PR — Phase 23/24)
    - .planning/phases/25-polling-trigger-channel/25-RESEARCH.md (§ Code Examples Example 2 — createTestClock controller líneas 645-715; § Validation Architecture § Option A — makeFakeLogger)
    - .planning/phases/25-polling-trigger-channel/25-VALIDATION.md (§ Per-Task Verification Map — los ~23 contratos detallados)
    - .planning/phases/25-polling-trigger-channel/25-PATTERNS.md (§ test/triggers/polling.test.js analogs A/B/C/D)
  </read_first>
  <behavior>
    Estructura del archivo de test:
    - `// @ts-check` + comentario doc explicando que el archivo cubre TEST-02 (POLL-01..04) + clock-mock <1s wall
    - Imports: `describe, it, before, after, beforeEach, afterEach` de `node:test`; `assert` de `node:assert/strict`; `existsSync, readFileSync, writeFileSync, mkdirSync, rmSync` de `node:fs`; `join` de `node:path`; `tmpdir` de `node:os`; `randomUUID` de `node:crypto`; `startPolling` de `'../../src/triggers/polling.js'`
    - **Live-fetch leak guard** (lift verbatim de provider.test.js:37-49): `before` sustituye `globalThis.fetch` por throwing stub, `after` restaura
    - Helper `createTestClock()` (lift de RESEARCH Example 2 verbatim): queue ordenada + `setTimeout(fn, ms)` retorna handle + `clearTimeout(handle)` splice + `now()` returns virtualNow + `async advance(ms)` drena queue ejecutando fns en orden + `await setImmediate` para drenar microtasks + `pendingCount()`
    - Helper `makeFakeClient(overrides = {})`: retorna `{calls: {listIssues: []}, async listIssues(owner, repo, opts) { calls.listIssues.push({owner, repo, opts}); if (overrides.listIssues) return overrides.listIssues(owner, repo, opts); return {status: 200, items: [], etag: 'E0', rate_limit_remaining: 5000}; }}`
    - Helper `makeFakeProvider(overrides = {})`: 9-method shim (init, getTask, updateTaskState, addComment, listPendingTasks: async () => [], parseTriggerEvent: () => null, verifySignature: () => false, resolveRef, listProjects) — lifted from webhook.test.js:9-21 extended
    - Helper `makeFakeLogger(captureArray)`: `{info, warn, error, debug}` que push `{level, msg, ...ctx}` a captureArray
    - Helper `tempStatePath()`: `join(tmpdir(), \`polling-state-${randomUUID()}.json\`)` — cleanup en afterEach
    - Helper `makeIssue(overrides = {})`: factory `{id, node_id, number, title, body, state:'open', labels:[{name:'kodo'}], updated_at:'2026-05-14T10:00:00Z', created_at:'2026-05-14T09:00:00Z', html_url, pull_request: null, ...overrides}`
    - `beforeEach`: limpiar captureArray (events), resetear dispatchCalls=[], statePath aislado
    - `afterEach`: si test creó `{stop}` handle, llamarlo; rmSync del statePath si existe
    
    Casos `it(...)` (~22-25, agrupados por requirement):
    
    **POLL-01 — Loop signature & scheduling (5 cases):**
    1. `startPolling returns {stop}` — startPolling con fakeClient + 1 repo + clock; assert `result.stop` es function
    2. `startPolling throws if neither provider nor client given` — `assert.throws(() => startPolling({repos:[...], clock}), /requires/)`
    3. `schedules next tick after intervalSec` — fakeClient devuelve 304; advance(60_000) tras primer tick → assert pendingCount > 0 antes y después de advance; assert calls.listIssues.length crece de 1 a 2
    4. `stop cancels pending loop` — startPolling + advance algo; stop(); advance(60_000) más; assert calls.listIssues.length NO incrementa tras stop
    5. `multiple repos polled in single tick` — repos: 3 elementos; primer tick → assert calls.listIssues.length === 3 con owner/repo distintos en cada uno
    
    **POLL-02 — State cache (4 cases):**
    6. `loadStateCache fail-open on corrupted JSON` — escribir `'not valid json{'` a statePath; startPolling con fakeClient devolviendo 304; advance microtask; assert calls.listIssues[0].opts.since === undefined (cache reset a `{}`); NO throw
    7. `loadStateCache fail-open on missing file` — statePath inexistente; mismo assert: NO throw, since undefined
    8. `atomic write: tmp file gone post-tick` — fakeClient devuelve 200 con issues; advance microtask; assert `!existsSync(statePath + '.tmp')` && `existsSync(statePath)` && JSON.parse del contenido es plain object
    9. `304 preserves cursor` — pre-poblar statePath con `{owner/repo: {last_updated_at: '2026-05-14T10:00:00Z', etag: 'E1'}}`; fakeClient devuelve `{status:304, items:[], etag:'E1'}`; advance microtask; readFileSync(statePath) sigue siendo equivalente al inicial (cursor inmutable)
    10. `200 advances cursor to max(updated_at)` — pre-poblar cursor a `'2026-05-14T08:00:00Z'`; fakeClient devuelve 200 con 2 issues `updated_at` `'2026-05-14T11:00:00Z'` y `'2026-05-14T12:00:00Z'`; advance microtask; assert JSON.parse(readFileSync(statePath)).['owner/repo'].last_updated_at === '2026-05-14T12:00:00Z'
    
    **POLL-03 — Dispatch patterns + idempotency (7 cases):**
    11. `first tick skips dispatch (Pitfall #7)` — cursor vacío; fakeClient devuelve 200 con 5 issues kodo; advance microtask; assert dispatchCalls.length === 0 && cursor poblado (statePath JSON tiene last_updated_at = max de los 5)
    12. `pattern (a): new issue after cursor fires dispatch` — pre-poblar cursor `'2026-05-14T08:00:00Z'`; fakeClient devuelve 200 con 1 issue `updated_at:'2026-05-14T11:00:00Z'`, `created_at:'2026-05-14T11:00:00Z'`; advance; assert dispatchCalls.length === 1 && dispatchCalls[0].event.action === 'polling' && .provider === 'github' && .taskRef === 'octocat/hello#42' (verbatim del issue)
    13. `pattern (b/c): existing issue updated since cursor fires dispatch` — pre-poblar cursor `'2026-05-14T08:00:00Z'`; fakeClient devuelve issue `created_at:'2026-05-13T00:00:00Z'` (pre-cursor) y `updated_at:'2026-05-14T10:00:00Z'` (post-cursor); assert dispatch fired
    14. `dispatchTrigger is fire-and-forget (no await)` — dispatchFn devuelve `new Promise(r => setTimeout(r, 5000))` (un dispatch lento); advance microtask; assert el loop completó el tick (assert que el cache fue saved, NO bloqueó por 5s)
    15. `dispatch rejection does not crash loop` — dispatchFn rechaza con `new Error('boom')`; advance microtask; advance(60_000); assert loop reschedules (next tick fired); assert capturedEvents contiene `polling.dispatch.failed`
    16. `PR filter (Pitfall #2)` — fakeClient devuelve 200 con 2 items, uno con `pull_request: { url: '...' }` y otro con `pull_request: null`; cursor pre-poblado; advance microtask; assert dispatchCalls.length === 1 (solo el non-PR)
    17. `provider-only path: listPendingTasks used when no client` — sin opts.client; opts.provider con listPendingTasks devolviendo `[{ref:'octocat/hello#1', projectId:'octocat/hello', ...}]`; advance microtask; assert provider.listPendingTasks llamado (spy en provider) y dispatch eventualmente fired (segundo tick — first-tick skip aplica también a path provider)
    
    **POLL-04 — Retry backoff (6 cases):**
    18. `429 triggers retry with 2s base backoff` — fakeClient en primer call throws `{status:429}`; tras backoff devuelve `{status:200, items:[]}`; advance microtask → attempt 1 visible en events; advance(2000) → attempt 2; assert events contains `polling.error{attempt:1, status:429}`
    19. `5xx triggers retry path (500/502/503/504)` — repetir test 18 con cada status (parametrizado o 4 sub-its); para cada uno: primer call throws, segundo call ok; assert retry fired tras 2s
    20. `network error (ETIMEDOUT, AbortError) triggers retry` — fakeClient throws `Object.assign(new Error('timeout'), {code:'ETIMEDOUT'})`; assert retry; idem `new Error('aborted')` con name='AbortError'
    21. `exponential backoff sequence: 2s, 4s, 8s` — fakeClient throws 429 indefinidamente; advance microtask (attempt 1); advance(2000) (attempt 2); advance(4000) (attempt 3); advance(8000) (attempt 4); assert listIssues.calls.length === 4 (3 retries + initial)
    22. `warn-and-continue after 3 retries exhausted` — fakeClient throws 429 indefinidamente; correr la secuencia completa de retries; advance(60_000) (next tick); assert listIssues.calls.length === 5 (next tick after exhaustion fires another initial); assert events incluyen `polling.error{attempt:3}` pero NO `polling.stopped`
    23. `non-transient error (401/404) does NOT retry` — fakeClient throws `{status:401}`; advance microtask; advance(2000); assert listIssues.calls.length === 1 (cero retry); assert events incluyen `polling.error{attempt:1, status:401}` solo una vez
    
    **TEST-02 — NDJSON shape + invariantes (3 cases):**
    24. `emits polling.tick per repo with {owner, repo, status, dispatched}` — fakeClient 200 con 1 nuevo issue; cursor pre-poblado; advance microtask; assert capturedEvents incluye `{event:'polling.tick', owner, repo, status:200, dispatched: 1}`
    25. `polling.dispatch NDJSON does NOT include issue.body (T-25-02 invariant)` — fakeClient 200 con issue cuyo body es `'SECRET TOKEN ghp_xxx'`; cursor pre-poblado; advance microtask; assert dispatch event capturado; assert `JSON.stringify(capturedEvent).indexOf('SECRET TOKEN')` === -1 && el evento NO tiene keys `body` / `title` / `raw`
    26. `wall-time of full suite < 1s` — meta-test: el archivo entero corre con clock mock, cero `setTimeout` real con ms > 0 fuera de `setImmediate` para microtask drain
  </behavior>
  <action>
    Crear directorio `test/triggers/` (no existe) y dentro `test/triggers/polling.test.js`. Empezar con `// @ts-check` + doc-comment explicando que cubre TEST-02. Imports node:test + node:assert/strict + node:fs + node:path + node:os + node:crypto + startPolling de `'../../src/triggers/polling.js'`. Implementar **live-fetch leak guard** verbatim de `test/providers/github/provider.test.js:37-49`. Implementar `createTestClock()` verbatim de RESEARCH § Code Examples Example 2 (líneas 645-715) — queue ordenada + setTimeout/clearTimeout/now + advance async que drena queue ejecutando fns + microtask flush via setImmediate. Implementar `makeFakeClient(overrides)` con `calls.listIssues` spy collector y default return `{status:200, items:[], etag:'E0', rate_limit_remaining:5000}`. Implementar `makeFakeProvider(overrides)` con 9-method shim. Implementar `makeFakeLogger(captureArray)` que push `{level, event, ...ctx}` para cada nivel. Helper `tempStatePath()` usando `tmpdir()` + `randomUUID()`. Helper factory `makeIssue(overrides)` con shape mínima GitHub issue. Estructurar tests en grupos por requirement (5 POLL-01 + 4 POLL-02 + 7 POLL-03 + 6 POLL-04 + 3 TEST-02 = ~25 casos). Cada test sigue pattern: `beforeEach` resetea capturedEvents/dispatchCalls; arrange (clock, client/provider, statePath); arrange cache (writeFileSync si necesita estado pre-poblado); act (startPolling → drain primer tick via setImmediate → advance virtual time según el escenario); assert (calls.listIssues.length, dispatchCalls.length, capturedEvents shape, statePath contents); `afterEach` llama `stop()` + `rmSync(statePath, {force:true})`. El test 25 es el guardián T-25-02 — debe escribir un `body: 'SECRET TOKEN ghp_xxx'` en el issue fixture y verificar via `JSON.stringify(capturedEvent).indexOf('SECRET TOKEN') === -1`. El test 14 es el guardián fire-and-forget — el dispatchFn devuelve una promise que NO resuelve en el wall-time del test pero el tick completa de todas formas. El test 21 es el guardián de las constantes RETRY_BASE_MS=2000/RETRY_MAX_ATTEMPTS=3 — si el implementor cambiase a 1000ms base o 5 retries, este test falla loud. Sin `await new Promise(r => setTimeout(r, ms))` con ms > 0 en ningún test (solo microtask drain via setImmediate). El archivo completo debe correr en < 1s wall-time.
  </action>
  <verify>
    <automated>node --test test/triggers/polling.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/triggers/polling.test.js` reporta pass ≥ 22
    - Wall-time del archivo < 1000ms (verificable con `time node --test test/triggers/polling.test.js`)
    - `grep -c "globalThis\.fetch" test/triggers/polling.test.js` ≥ 2 (live-fetch leak guard before/after)
    - `grep -c "createTestClock\|advance(" test/triggers/polling.test.js` ≥ 10 (clock mock usado activamente)
    - `grep -E "setTimeout\(r, [1-9]" test/triggers/polling.test.js` retorna 0 lines (cero real setTimeout > 0 ms — solo setImmediate o clock-injected)
    - `grep -c "SECRET TOKEN\|T-25-02" test/triggers/polling.test.js` ≥ 1 (test guardian de invariante seguridad)
    - `grep -c "polling.dispatch.failed\|polling\\.error" test/triggers/polling.test.js` ≥ 3 (asserts sobre eventos de retry/failure)
    - `grep -c "pull_request" test/triggers/polling.test.js` ≥ 2 (PR filter test)
    - `grep -c "first_tick\|firstTick\|first tick" test/triggers/polling.test.js` ≥ 2 (Pitfall #7 test)
    - `grep -c "RETRY_BASE_MS\|2000\b.*backoff\|backoff.*2000\b\|2s.*4s.*8s" test/triggers/polling.test.js` ≥ 1 (retry constants regression guard)
    - Sin regresiones: `npm test` corre completo con baseline+~25 tests verdes
    - El test 25 (T-25-02) falla loud si en `src/triggers/polling.js` se cambiase `pollingDispatch(... )` para pasar body — verificable por mutación manual
  </acceptance_criteria>
  <done>
    `test/triggers/polling.test.js` verde con ≥ 22 casos, wall-time < 1s, cero live API, cero `setTimeout` real con ms > 0. Cubre POLL-01..04 + TEST-02 + invariante T-25-02 + Pitfalls #2/#5/#7. Plan 25 funcional behavior contract cerrado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender test/check-isolation.test.js con LOG-12 row para polling.js</name>
  <files>test/check-isolation.test.js</files>
  <read_first>
    - test/check-isolation.test.js (file completo — lines 109-131 es el precedente Phase 24 para provider.js y normalize.js; precedente exact en patrón)
    - .planning/phases/25-polling-trigger-channel/25-PATTERNS.md (§ test/check-isolation.test.js — la sección "Apply to check-isolation.test.js MODIFY" con el código verbatim para añadir)
    - src/check.js (verificar que NO importa polling.js — assertion pasiva, debería pasar tras Task 1 sin cambios)
  </read_first>
  <behavior>
    - Añadir un nuevo `it(...)` dentro del `describe('LOG-12: vigilante isolation (import-graph)', ...)` existente, después de la línea 131 (último `it` actual de `github/normalize.js`)
    - Estructura del nuevo test: comentario inline explicando "Phase 25 LOG-12 extension: polling.js carga dispatcher (manager.js, gsd/lock.js), GitHubClient, normalize, logger-events helpers — todo fuera del árbol de check.js" + título `'kodo check does not import src/triggers/polling.js transitively'` + cuerpo `const graph = walkImports(join(SRC, 'check.js')); const violators = [...graph].filter((p) => p.endsWith('/triggers/polling.js')); assert.deepEqual(violators, [], \`check.js transitively imports triggers/polling.js via:\\n  ${violators.map((p) => relative(REPO, p)).join('\\n  ')}\`);`
    - Cero cambios al walker (líneas 32-52); cero cambios a los imports del archivo; cero cambios a tests existentes
    - ~12 LOC añadidas total
  </behavior>
  <action>
    Editar `test/check-isolation.test.js` añadiendo un solo `it(...)` row tras la línea 131 (antes del cierre `});` del describe). Copiar verbatim el patrón de `it('kodo check does not import src/providers/github/normalize.js transitively', ...)` y sustituir el path suffix `/providers/github/normalize.js` por `/triggers/polling.js`. Incluir el comentario inline `// Phase 25 LOG-12 extension: polling.js carga dispatcher (→ manager.js + gsd/lock.js), GitHubClient, normalize, logger-events helpers — fuera del árbol permitido de \`kodo check\`. El dispatcher (ya excluido transitivamente) importa machinery cmux/state. Polling es trigger channel, no check primitive (invariante cross-phase STATE.md).` antes del título del test. Cero cambios al walker, imports del archivo, ni tests existentes — solo +12 LOC.
  </action>
  <verify>
    <automated>node --test test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/check-isolation.test.js` reporta pass para el nuevo `it` y todos los anteriores siguen verdes
    - `grep -c "triggers/polling.js" test/check-isolation.test.js` ≥ 2 (1 en filter + 1 en mensaje de error / título)
    - `grep -c "^\s*it(" test/check-isolation.test.js` == 6 (baseline 5 + 1 nuevo)
    - Si por hipótesis `src/check.js` empezara a importar `src/triggers/polling.js` (directa o transitivamente), este test fallaría loud — verificable por mutación
    - Sin regresiones a los 5 tests existentes
  </acceptance_criteria>
  <done>
    `test/check-isolation.test.js` con 6 tests verdes incluyendo el nuevo guardián LOG-12 para `src/triggers/polling.js`. El invariante cross-phase STATE.md "check.js no importa polling.js" queda asegurado por test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → `client.listIssues` → polling.js | Issues externos (label-controllables por terceros con write access al repo) cruzan a la lógica de dispatch |
| polling.js → dispatchTrigger → launchWorkItem → cmux spawn | Cada dispatch crea un worktree + sesión Claude con acceso al filesystem y al GitHub repo |
| polling.js → ~/.kodo/polling-state.json | Cursor + etag persisten entre procesos; corrupción podría inducir re-dispatch o skip |
| polling.js → logger sink → ~/.kodo/logs/*.ndjson | Cualquier campo emitido a NDJSON queda en disco — inspeccionable post-hoc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T (Tampering) | `loadStateCache` en `src/triggers/polling.js` | **mitigate** | Try/catch envuelve `readFileSync` + `JSON.parse`; defensive check `parsed && typeof parsed === 'object' && !Array.isArray(parsed)` antes de retornar el objeto. Cualquier corrupción (JSON inválido, archivo binario, array, null) retorna `{}` fail-open. Test #6 + #7 del Task 2 cubren ambos paths. |
| T-25-02 | I (Information disclosure) | `polling.js` dispatch site + `pollingDispatch` helper | **mitigate** | `pollingDispatch(logger, {owner, repo, ref, pattern})` — el callsite en polling.js NUNCA pasa `issue.body` ni `raw`. El helper Plan 25-01 ya hace whitelist estricta. Defensa en profundidad: dos capas. Test #25 del Task 2 verifica via grep en el JSON serializado del evento capturado. El `raw: issue` SÍ se pasa a `dispatchTrigger` porque el dispatcher lo necesita downstream (no es leak a NDJSON; es paso interno de proceso). |
| T-25-03 | D (DoS via unbounded retry) | `processRepo` retry loop | **mitigate** | Constantes módulo `RETRY_BASE_MS=2000`, `RETRY_MAX_ATTEMPTS=3`. Tras 3 retries: warn-and-continue (return); el siguiente tick se schedula via `intervalSec * 1000`. Máximo `1 (initial) + 3 (retries) = 4` calls por repo por tick. Test #21 + #22 del Task 2 verifican la secuencia exacta 2s/4s/8s y la salida warn-and-continue. |
| T-25-04 | D (Dispatch storm on first tick) | `startPolling` + `processRepo` first-tick logic | **mitigate** | First-tick skip: `shouldDispatch` retorna `false` cuando `!prev.last_updated_at`; el cursor se popula con `max(updated_at)` pero NO dispara. Test #11 del Task 2 verifica que un repo con 5 issues kodo + cursor vacío produce `dispatchCalls.length === 0` && cursor poblado. Defensa adicional: dispatcher tiene `inFlight + lock per-repo` (Phase 8 GSD-10) que coalesce concurrent dispatches — el polling.js NO duplica este mecanismo. |
| T-25-05 (nuevo) | E (Elevation via PR dispatch) | `processRepo` PR filter | **mitigate** | `if (issue.pull_request) continue;` con comentario citing Pitfall #2 y Phase 24 D-25. Sin este filtro, un PR con label `kodo` (controlable por cualquier contribuidor con permiso push a una rama de PR) podría disparar un worktree + sesión Claude con permisos sobre el repo. Test #16 del Task 2 verifica con fixture mixto (1 PR + 1 issue, ambos con label kodo) que solo el issue dispatcha. |
</threat_model>

<verification>
- `node --test test/triggers/polling.test.js test/check-isolation.test.js test/logger-events.test.js` verde
- `npm test` verde con baseline (682 post-Phase-24) + ~25 polling + ~6 logger-events + 1 check-isolation = ~714 tests pass; cero regresiones
- `grep -v '^//' src/triggers/polling.js | grep -v '^\s*\*' | grep -c "Date\.now()"` == 1 (única ocurrencia en DEFAULT_CLOCK.now — Pitfall #5 lint gate)
- `node -e "import('./src/check.js')"` sigue funcionando (LOG-12 sanity: check.js no crashea al cargarse)
- `time node --test test/triggers/polling.test.js` reporta total time < 1s
- Cero `import 'picocolors'` en polling.js (color isolation)
</verification>

<success_criteria>
- `src/triggers/polling.js` exporta `startPolling(opts) -> {stop}` cumpliendo POLL-01..04
- `~/.kodo/polling-state.json` se crea atómicamente vía tmp + rename; corrupted/missing → reset fail-open
- Patrones de dispatch (a)/(b)/(c) cubiertos por cursor + filter `labels=kodo`; first-tick skip implementado (Pitfall #7)
- Errores transitorios (429/5xx/network) → backoff exponencial 2s/4s/8s × 3 retries → warn-and-continue
- `dispatchTrigger` fire-and-forget; rejection loggeada como `polling.dispatch.failed`, NUNCA propaga al loop
- `test/triggers/polling.test.js` ≥ 22 casos pass con wall-time < 1s, cero live fetch, cero `setTimeout` real > 0
- `test/check-isolation.test.js` extendido con guardián LOG-12 para polling.js (Task 3)
- Invariantes preservados: LOG-12, T-25-02, T-25-03 (retry bound), T-25-04 (first-tick skip), T-25-05 (PR filter), color isolation, fire-and-forget, lock per-repo no duplicado, worktree path no tocado
- 5/5 requirements de Phase 25 cerrados: POLL-01 + POLL-02 + POLL-03 + POLL-04 + TEST-02
</success_criteria>

<output>
After completion, create `.planning/phases/25-polling-trigger-channel/25-02-SUMMARY.md` con:
- Exports + signature de `startPolling` (verbatim)
- Constantes lock-in (RETRY_BASE_MS, RETRY_MAX_ATTEMPTS, TRANSIENT_STATUSES, DEFAULT_STATE_PATH)
- Decisión hybrid resuelta: provider-fallback vs client-direct (cuándo se elige cada uno, con código de ejemplo)
- Open Questions resueltas (warn-and-continue confirmado, clock-mock duck-typed, hybrid signature) + sus tradeoffs ejercidos
- Count de tests añadidos por archivo (polling.test.js ~25, check-isolation +1)
- Wall-time de la suite polling.test.js medido
- Listado verificado de los 5 invariantes T-25-XX + Pitfalls #2/#5/#6/#7 con referencia al test que los guarda
- Confirmación que `node --test` total (~714 tests) verde post-merge
</output>
