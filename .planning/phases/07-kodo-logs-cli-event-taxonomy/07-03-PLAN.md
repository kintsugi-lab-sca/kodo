---
phase: 07-kodo-logs-cli-event-taxonomy
plan: 03
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/logger.js
  - src/logs/reader.js
  - src/logs/follow.js
autonomous: true
requirements: [LOG-05, LOG-06, LOG-07]

must_haves:
  truths:
    - "`src/logger.js` exporta aditivamente `ANSI_RESET`, `COLOR_BY_LEVEL` y `formatLine(record, { useColor })` sin cambiar comportamiento existente"
    - "`src/logs/reader.js` exporta `runLogs(opts)` que lee NDJSON de `~/.kodo/logs/<id>.ndjson`, filtra client-side, y printea pretty (default) o raw (`--json`)"
    - "`src/logs/follow.js` exporta `followFile(path, onLine)` con dump completo + tail via `fs.watchFile` + wait-until-exists + partial-line buffer"
    - "Filtros `--level`, `--component`, `--event-type` descartan antes de printear"
    - "`test/logger.test.js` sigue verde (stderr mirror intocado)"
  artifacts:
    - path: "src/logger.js"
      provides: "Additive exports formatLine, COLOR_BY_LEVEL, ANSI_RESET"
      contains: "export function formatLine"
    - path: "src/logs/reader.js"
      provides: "runLogs action handler (dump + filter + follow delegation)"
      contains: "export async function runLogs"
      min_lines: 60
    - path: "src/logs/follow.js"
      provides: "followFile(path, onLine) tail watcher"
      contains: "watchFile"
      min_lines: 40
  key_links:
    - from: "src/logs/reader.js"
      to: "src/logger.js"
      via: "import { LEVELS, formatLine } from '../logger.js'"
      pattern: "LEVELS.*formatLine"
    - from: "src/logs/reader.js"
      to: "src/logs/follow.js"
      via: "dynamic import cuando opts.follow"
      pattern: "import.*follow"
    - from: "src/logs/reader.js"
      to: "src/config.js"
      via: "import { KODO_DIR }"
      pattern: "KODO_DIR"
---

<objective>
Wave 1 (paralelo con Plan 02): implementar el CLI reader — promociona `formatLine` de logger.js a export público, crea `src/logs/reader.js` (dump + filtros + delega follow) y `src/logs/follow.js` (watcher con buffer partial-line).

Purpose: cubrir LOG-05 (dump), LOG-06 (--follow) y LOG-07 (--level filter). Reutiliza `formatLine` compartido → UNA SOLA fuente de verdad visual entre `stderr mirror` del logger y `kodo logs`.

Output: 2 módulos nuevos + 3 exports aditivos en logger.js. Tests `test/logs-reader.test.js` verdes para las partes no-`--follow`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md
@src/logger.js
@src/config.js
@test/logs-reader.test.js
@test/logger.test.js

<interfaces>
<!-- API actual de src/logger.js (NO cambiar comportamiento; solo añadir exports) -->

From src/logger.js (existentes — intocables):
```javascript
export const LEVELS = Object.freeze({ debug: 10, info: 20, warn: 30, error: 40 });
export const LEVEL_NAMES = Object.freeze(['debug','info','warn','error']);
export function createLogger({ sessionId, minLevel }): Logger;
export { noopLogger };
```

From src/logger.js (actuales privados que se promueven a export SIN cambios de valor):
```javascript
// Líneas 31-42:
const ANSI_RESET = '\x1b[0m';
const COLOR_BY_LEVEL = Object.freeze({ debug: '\x1b[90m', info: '\x1b[36m', warn: '\x1b[33m', error: '\x1b[31m' });

// Líneas 246-257:
function formatCtxInline(record): string; // pure, excluye BASE_RECORD_KEYS
```

<!-- Nuevas exports a añadir -->

To export from src/logger.js:
```javascript
export const ANSI_RESET: string;
export const COLOR_BY_LEVEL: Readonly<{debug,info,warn,error}>;
export function formatLine(record, { useColor }): string; // `HH:MM:SS LEVEL component msg +k=v`
```

<!-- API producida por este plan -->

From src/logs/reader.js (NEW):
```javascript
/**
 * @param {{
 *   sessionId?: string,
 *   follow?: boolean,
 *   level?: 'debug'|'info'|'warn'|'error',
 *   component?: string,
 *   eventType?: string[],
 *   json?: boolean,
 *   sessionOf?: string,
 * }} opts
 */
export async function runLogs(opts): Promise<void>;
```

From src/logs/follow.js (NEW):
```javascript
export const FOLLOW_INTERVAL_MS: number;  // 200
export function followFile(filePath: string, onLine: (line: string) => void): void;
```

<!-- Config existente -->

From src/config.js:
```javascript
export const KODO_DIR: string;  // ~/.kodo (o $HOME/.kodo si HOME está override)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive exports en src/logger.js — formatLine + COLOR_BY_LEVEL + ANSI_RESET</name>
  <files>src/logger.js</files>
  <read_first>
    - src/logger.js (completo — verás líneas 31-42 para ANSI/COLOR y 222-257 para maybeMirrorToStderr + formatCtxInline)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 430-500 — Change 1/2/3 diff verbatim)
    - test/logger.test.js (ver tests que NO deben romperse — mirror stderr sigue con output idéntico)
    - test/logger-redaction.test.js (ídem)
  </read_first>
  <behavior>
    - Test 1 (existente en test/logger.test.js): stderr mirror sigue produciendo exactamente `HH:MM:SS LEVEL component msg +ctx` — SIN CAMBIOS.
    - Test 2 (nuevo implícito, vía test/logs-reader.test.js): `formatLine(record, { useColor: false })` devuelve mismo string que el mirror stderr con `NO_COLOR=1`.
    - Test 3 (acceptance): `import { formatLine, COLOR_BY_LEVEL, ANSI_RESET } from './logger.js'` compila.
  </behavior>
  <action>
    Refactor ADITIVO de `src/logger.js`. Los cambios:

    **Change 1** — promover constantes a export (líneas 31-42 actuales):

    Antes (actual):
    ```javascript
    const ANSI_RESET = '\x1b[0m';
    const ANSI_GRAY = '\x1b[90m';
    // ...
    const COLOR_BY_LEVEL = Object.freeze({ ... });
    ```

    Después (aditivo — mismos valores, solo añadir `export`):
    ```javascript
    export const ANSI_RESET = '\x1b[0m';
    const ANSI_GRAY = '\x1b[90m';
    const ANSI_CYAN = '\x1b[36m';
    const ANSI_YELLOW = '\x1b[33m';
    const ANSI_RED = '\x1b[31m';

    export const COLOR_BY_LEVEL = Object.freeze({
      debug: ANSI_GRAY,
      info: ANSI_CYAN,
      warn: ANSI_YELLOW,
      error: ANSI_RED,
    });
    ```

    **Change 2** — extraer `formatCtxInline` del closure de `createLogger` a top-level module scope, y extraer `formatLine` como pure function top-level.

    El `formatCtxInline` actual (líneas 246-257) está definido dentro de `createLogger`. Muévelo AL TOPE del módulo (antes de `createLogger`), porque `formatLine` lo necesita:

    ```javascript
    const BASE_RECORD_KEYS = new Set([
      'timestamp', 'level', 'msg', 'session_id', 'component', 'plane_task_id', 'phase_id',
    ]);

    /**
     * Formatea campos extra inline como `+k=v k2=v2`.
     * @param {object} record
     * @returns {string}
     */
    function formatCtxInline(record) {
      const parts = [];
      for (const [k, v] of Object.entries(record)) {
        if (BASE_RECORD_KEYS.has(k)) continue;
        if (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean') {
          parts.push(`${k}=${v}`);
        } else {
          parts.push(`${k}=${JSON.stringify(v)}`);
        }
      }
      return parts.length ? ` +${parts.join(' ')}` : '';
    }

    /**
     * Pretty-format a log record. Shared by stderr mirror AND `kodo logs` CLI.
     * Pure — no I/O.
     * @param {object} record
     * @param {{ useColor: boolean }} opts
     * @returns {string}
     */
    export function formatLine(record, { useColor }) {
      const time = String(/** @type {any} */ (record).timestamp).slice(11, 19);
      const lvl = String(/** @type {any} */ (record).level).toUpperCase();
      const c = useColor ? COLOR_BY_LEVEL[/** @type {any} */ (record).level] || '' : '';
      const r = useColor ? ANSI_RESET : '';
      const comp = /** @type {any} */ (record).component ? ` ${/** @type {any} */ (record).component}` : '';
      const ctx = formatCtxInline(record);
      return `${time} ${c}${lvl}${r}${comp} ${/** @type {any} */ (record).msg}${ctx}`;
    }
    ```

    NOTA: el `BASE_RECORD_KEYS` ya existe a nivel módulo (líneas 44-52) — NO duplicarlo. Si `formatCtxInline` está en top-level, puede usar el existente.

    **Change 3** — actualizar `maybeMirrorToStderr` (líneas 222-238) para reutilizar `formatLine`:

    Antes:
    ```javascript
    function maybeMirrorToStderr(level, record) {
      const isTTY = Boolean(process.stderr.isTTY);
      const mirror = /* ... */;
      if (!mirror) return;
      const time = record.timestamp.slice(11, 19);
      const c = useColor ? COLOR_BY_LEVEL[level] : '';
      // ... construye string manualmente
      process.stderr.write(`${time} ${c}${level.toUpperCase()}${r}${comp} ${msg}${ctxStr}\n`);
    }
    ```

    Después:
    ```javascript
    function maybeMirrorToStderr(level, record) {
      const isTTY = Boolean(process.stderr.isTTY);
      const mirror =
        level === 'error' ||
        level === 'warn' ||
        (level === 'info' && isTTY && minLevelNum <= LEVELS.info) ||
        (level === 'debug' && isTTY && minLevelNum <= LEVELS.debug);
      if (!mirror) return;
      process.stderr.write(formatLine(record, { useColor }) + '\n');
    }
    ```

    NOTA crítica: `formatLine` debe producir output IDÉNTICO al actual — si difiere en un espacio, los tests existentes fallarán. Ver Change 2 y compara literal con lo que había antes en `maybeMirrorToStderr`.

    **Eliminar** el `formatCtxInline` interno duplicado dentro de `createLogger` (líneas 246-257 actuales). Dejar solo el top-level.

    Los exports existentes (`LEVELS`, `LEVEL_NAMES`, `createLogger`, `noopLogger`) NO cambian.
  </action>
  <verify>
    <automated>node --test test/logger.test.js test/logger-redaction.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export const ANSI_RESET" src/logger.js` returns 1.
    - `grep -c "^export const COLOR_BY_LEVEL" src/logger.js` returns 1.
    - `grep -c "^export function formatLine" src/logger.js` returns 1.
    - `grep -c "^function formatCtxInline" src/logger.js` returns 1 (top-level, no dentro de `createLogger`).
    - `grep -c "function formatCtxInline" src/logger.js` returns 1 (NO duplicado — solo existe la top-level).
    - `node --test test/logger.test.js` exits 0 (comportamiento intocado).
    - `node --test test/logger-redaction.test.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0 (LOG-12 intacto — solo añadir exports no cambia grafo).
    - `node -e "import('./src/logger.js').then(m => { if (typeof m.formatLine !== 'function') process.exit(1); if (!m.COLOR_BY_LEVEL) process.exit(1); if (!m.ANSI_RESET) process.exit(1); })"` exits 0.
  </acceptance_criteria>
  <done>Exports aditivos `formatLine`, `COLOR_BY_LEVEL`, `ANSI_RESET` disponibles; tests existentes intocados; LOG-12 guardián verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/logs/follow.js — tail watcher con fs.watchFile + partial-line buffer</name>
  <files>src/logs/follow.js</files>
  <read_first>
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 322-385 — Pattern 3 verbatim, VERIFIED empírico)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 235-291 — template completo)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-04, D-05 — semántica watchFile 200ms polling + dump+tail)
  </read_first>
  <behavior>
    - Test 1 (manual verify, ver VALIDATION.md): `followFile('/nonexistent', onLine)` imprime `"waiting for session log to appear..."` una vez a stderr, no crashea, sigue hasta que el archivo aparezca.
    - Test 2 (manual): con archivo existente, dumpea byte 0..EOF al arrancar, luego reporta cada append.
    - Test 3 (manual): partial line `{"a":1}\n{"b":2` → onLine("`{\"a\":1}`") pero NO onLine("`{\"b\":2`"); cuando llegue `1}\n`, onLine("`{\"b\":21}`"). **CRÍTICO**: el buffer guarda el fragmento sin newline.
    - Test 4 (manual): SIGINT → `unwatchFile` + `process.exit(0)`.
    - Test 5 (manual): truncate (`curr.size < prev.size`) → `readFrom = 0`, `buffer = ''`.
  </behavior>
  <action>
    Crea `src/logs/follow.js`:

    ```javascript
    // @ts-check
    //
    // src/logs/follow.js — Tail-follow de NDJSON logs.
    //
    // Semántica (D-04, D-05):
    //   - Polling con fs.watchFile (NO fs.watch — edge cases inotify/FSEvents por plataforma).
    //   - Dump completo desde byte 0 + tail en vivo (como `tail -f`).
    //   - Si el archivo no existe aún, espera hasta que aparezca (poll).
    //   - Buffer de partial-line: guarda fragmento tras último '\n' para reparsear cuando llegue más.
    //   - SIGINT → unwatchFile + exit(0).
    //

    import { watchFile, unwatchFile, openSync, readSync, closeSync,
             existsSync, statSync } from 'node:fs';

    /** Interval en ms del polling de watchFile. Exportado para override en tests. */
    export const FOLLOW_INTERVAL_MS = 200;

    /**
     * Tail-follow con buffer para líneas parciales.
     * @param {string} filePath absolute
     * @param {(line: string) => void} onLine llamado una vez por línea completa
     */
    export function followFile(filePath, onLine) {
      let readFrom = 0;
      let buffer = '';
      let waitingWarned = false;

      if (existsSync(filePath)) {
        drainFrom(0);
        readFrom = statSync(filePath).size;
      } else {
        process.stderr.write(`waiting for session log to appear...\n`);
        waitingWarned = true;
      }

      watchFile(filePath, { interval: FOLLOW_INTERVAL_MS }, (curr, prev) => {
        // Archivo aún no existe
        if (curr.size === 0 && prev.size === 0 && !existsSync(filePath)) return;
        // Truncate / rename — reset
        if (curr.size < prev.size) {
          readFrom = 0;
          buffer = '';
        }
        if (curr.size > readFrom) {
          drainFrom(readFrom);
          readFrom = curr.size;
        }
      });

      process.on('SIGINT', () => {
        unwatchFile(filePath);
        process.exit(0);
      });

      /**
       * Lee bytes [start..EOF], añade al buffer, corta por '\n' y emite líneas.
       * @param {number} start
       */
      function drainFrom(start) {
        if (!existsSync(filePath)) return;
        const fd = openSync(filePath, 'r');
        try {
          const size = statSync(filePath).size - start;
          if (size <= 0) return;
          const buf = Buffer.alloc(size);
          readSync(fd, buf, 0, size, start);
          buffer += buf.toString('utf8');
          const parts = buffer.split('\n');
          buffer = /** @type {string} */ (parts.pop()); // guarda trailing parcial
          for (const line of parts) if (line) onLine(line);
        } finally {
          closeSync(fd);
        }
      }
    }
    ```

    NOTAS:
    - El `waitingWarned` declarado pero no usado en el snippet inicial: no es necesario si solo escribimos el warn una vez antes del loop. Quítalo.
    - Buffer como `parts.pop()` — si la última parte es `''` (archivo termina en `\n`), buffer = ''. Correcto.
    - No importar `logger.js` (aislamiento lateral — este módulo NO entra al grafo de check.js vía reader.js, que sí podría ser importado por cli.js, pero cli.js no entra en check.js). Verificar que `test/check-isolation.test.js` siga verde.
  </action>
  <verify>
    <automated>node --check src/logs/follow.js && node --test test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/logs/follow.js` existe, ≥40 líneas.
    - `grep -c "^export const FOLLOW_INTERVAL_MS" src/logs/follow.js` returns 1.
    - `grep "FOLLOW_INTERVAL_MS = 200" src/logs/follow.js` retorna match.
    - `grep -c "^export function followFile" src/logs/follow.js` returns 1.
    - `grep "watchFile" src/logs/follow.js` retorna match (no `fs.watch` directo).
    - `grep -c "unwatchFile" src/logs/follow.js` returns >= 1 (SIGINT handler).
    - `grep "waiting for session log to appear" src/logs/follow.js` retorna match.
    - `grep "buffer.split" src/logs/follow.js` retorna match (partial-line).
    - `grep "parts.pop" src/logs/follow.js` retorna match (guarda fragment).
    - `grep "from 'fs'" src/logs/follow.js` returns 0 (solo `node:fs` — ESM pure).
    - `node --check src/logs/follow.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0.
  </acceptance_criteria>
  <done>Módulo `follow.js` creado, compila, LOG-12 intacto. Testing manual/integración en Plan 07 via `kodo logs --follow`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear src/logs/reader.js — runLogs(opts) con dump + filtros + delega follow</name>
  <files>src/logs/reader.js</files>
  <read_first>
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 601-648 — template runLogs completo)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 138-229 — imports + patterns)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-01..D-06, D-22)
    - test/logs-reader.test.js (contract exacto: behavior por flag)
    - src/logger.js (API ya expuesta tras Task 1: LEVELS, formatLine)
    - src/config.js (KODO_DIR)
  </read_first>
  <behavior>
    - Test 1 (LOG-05): `runLogs({ sessionId: 'sess-X' })` imprime TODAS las líneas del fichero `~/.kodo/logs/sess-X.ndjson` a stdout, pretty-format.
    - Test 2 (LOG-07): `runLogs({ sessionId, level: 'warn' })` descarta líneas con `LEVELS[rec.level] < LEVELS.warn`.
    - Test 3 (D-02 component): `runLogs({ sessionId, component: 'plane' })` descarta líneas con `rec.component !== 'plane'`.
    - Test 4 (D-02 event-type): `runLogs({ sessionId, eventType: ['session.start','session.end'] })` descarta si `!arr.includes(rec.event)`.
    - Test 5 (D-02 json): `runLogs({ sessionId, json: true })` imprime la línea NDJSON cruda + '\n', sin pretty.
    - Test 6 (D-06 malformed): línea no-JSON → imprime `[malformed] <raw>\n`, continúa con la siguiente.
    - Test 7 (archivo inexistente): `process.exit(1)` con error claro a stderr. NOTA: los tests usan mock de exit; el código usa `process.exit` de verdad.
    - Test 8 (LOG-06 delegación): `runLogs({ sessionId, follow: true })` llama `followFile(path, printLine)` — en este plan no hay test automatizado para follow, solo acceptance estructural.
    - `--session-of`: NO se implementa en este plan — runLogs recibe `sessionId` ya resuelto. La resolución va en Plan 04.
  </behavior>
  <action>
    Crea `src/logs/reader.js`:

    ```javascript
    // @ts-check
    //
    // src/logs/reader.js — Action handler de `kodo logs`.
    //
    // Responsabilidades:
    //   1. Resolver el file path desde sessionId (o sessionOf → Plan 04).
    //   2. Dump default: readFileSync + split '\n' + filtros cliente + formatLine.
    //   3. --follow: delega en ./follow.js#followFile.
    //   4. --json: imprime NDJSON crudo sin formatear.
    //
    // Filtros (D-06, cliente-side):
    //   - level: LEVELS[rec.level] >= LEVELS[flag]
    //   - component: rec.component === flag
    //   - eventType: eventType.includes(rec.event)
    //

    import { readFileSync, existsSync } from 'node:fs';
    import { join } from 'node:path';
    import { KODO_DIR } from '../config.js';
    import { LEVELS, formatLine } from '../logger.js';

    /**
     * @typedef {{
     *   sessionId?: string,
     *   follow?: boolean,
     *   level?: 'debug'|'info'|'warn'|'error',
     *   component?: string,
     *   eventType?: string[],
     *   json?: boolean,
     *   sessionOf?: string,
     * }} RunLogsOpts
     */

    /**
     * @param {RunLogsOpts} opts
     */
    export async function runLogs(opts) {
      let sessionId = opts.sessionId;

      if (opts.sessionOf) {
        const { resolveSessionIdFromTaskId } = await import('./session-lookup.js');
        sessionId = /** @type {string|null|undefined} */ (
          await resolveSessionIdFromTaskId(opts.sessionOf)
        ) ?? undefined;
        if (!sessionId) {
          process.stderr.write(`No session found for task ${opts.sessionOf}\n`);
          process.exit(1);
        }
      }

      if (!sessionId) {
        process.stderr.write('Usage: kodo logs <session-id> | kodo logs --session-of <task-id>\n');
        process.exit(2);
      }

      const filePath = join(KODO_DIR, 'logs', `${sessionId}.ndjson`);
      const minLevelNum = opts.level && opts.level in LEVELS
        ? LEVELS[opts.level]
        : LEVELS.debug;
      const useColor = Boolean(process.stdout.isTTY) && !process.env.NO_COLOR;

      const printLine = (raw) => {
        if (opts.json) {
          process.stdout.write(raw + '\n');
          return;
        }
        /** @type {any} */
        let rec;
        try {
          rec = JSON.parse(raw);
        } catch {
          process.stdout.write(`[malformed] ${raw}\n`);
          return;
        }
        const recLevel = rec.level in LEVELS ? LEVELS[rec.level] : LEVELS.debug;
        if (recLevel < minLevelNum) return;
        if (opts.component && rec.component !== opts.component) return;
        if (opts.eventType && opts.eventType.length > 0 && !opts.eventType.includes(rec.event)) return;
        process.stdout.write(formatLine(rec, { useColor }) + '\n');
      };

      if (opts.follow) {
        const { followFile } = await import('./follow.js');
        followFile(filePath, printLine);
        return;
      }

      if (!existsSync(filePath)) {
        process.stderr.write(`No log file at ${filePath}\n`);
        process.exit(1);
      }

      const raw = readFileSync(filePath, 'utf-8');
      for (const line of raw.split('\n')) {
        if (line) printLine(line);
      }
    }
    ```

    NOTAS:
    - `KODO_DIR` ya se deriva de `process.env.HOME` en `src/config.js` — `makeTmpHome` en los tests setea HOME antes de cualquier import, así el tests funcionan.
    - `process.exit(1)` bloquea el test normalmente, pero los tests de error path usan mocks. El happy path (archivo existe, dump ok) no llega a exit.
    - `import.*session-lookup.js` es dinámico — session-lookup.js se crea en Plan 04. Este import solo se dispara si `opts.sessionOf` — los tests de Plan 03 NO lo usan.
  </action>
  <verify>
    <automated>node --check src/logs/reader.js && node --test test/logs-reader.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/logs/reader.js` existe, >= 60 líneas.
    - `grep -c "^export async function runLogs" src/logs/reader.js` returns 1.
    - `grep "from '../logger.js'" src/logs/reader.js` retorna match e importa `LEVELS` y `formatLine`.
    - `grep "from '../config.js'" src/logs/reader.js` retorna match (imports `KODO_DIR`).
    - `grep "'./follow.js'" src/logs/reader.js` retorna match (dynamic import para --follow).
    - `grep "'./session-lookup.js'" src/logs/reader.js` retorna match (dynamic import para --session-of).
    - `grep "\\[malformed\\]" src/logs/reader.js` retorna match (D-06 graceful handling).
    - `grep "process.stdout.isTTY" src/logs/reader.js` retorna match (color detection).
    - `grep "NO_COLOR" src/logs/reader.js` retorna match.
    - `node --check src/logs/reader.js` exits 0.
    - `node --test test/logs-reader.test.js` exits 0 (6 tests verdes: dump, level, component, event-type, json, malformed).
    - `node --test test/check-isolation.test.js` exits 0.
  </acceptance_criteria>
  <done>`runLogs` funciona para dump + filtros + json + malformed. `--follow` estructuralmente listo (delega a follow.js); `--session-of` estructuralmente listo (delega a session-lookup.js, se crea en Plan 04). Tests de reader PASS.</done>
</task>

</tasks>

<verification>
```bash
# Tests específicos de la wave
node --test test/logs-reader.test.js

# Tests de fase previa intocados
node --test test/logger.test.js test/logger-redaction.test.js

# LOG-12 guardián
node --test test/check-isolation.test.js

# Suite completa sigue verde (excepto logs-session-of y logger-events, que aún no tienen su módulo — plan 02/04)
node --test 2>&1 | tail -10

# No nuevas deps
grep -c '"dependencies"' package.json
```
</verification>

<success_criteria>
- `src/logger.js` exporta aditivamente `formatLine`, `COLOR_BY_LEVEL`, `ANSI_RESET` sin cambiar comportamiento de mirror stderr.
- `src/logs/reader.js` implementa `runLogs(opts)` con dump + 3 filtros + json + malformed.
- `src/logs/follow.js` implementa `followFile(path, onLine)` + `FOLLOW_INTERVAL_MS = 200`.
- `test/logs-reader.test.js` PASS 100%.
- `test/logger.test.js`, `test/logger-redaction.test.js`, `test/check-isolation.test.js` siguen PASS.
- Zero nuevas deps.
</success_criteria>

<output>
After completion, create `.planning/phases/07-kodo-logs-cli-event-taxonomy/07-03-SUMMARY.md`.
</output>
