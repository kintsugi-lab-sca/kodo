---
phase: 07-kodo-logs-cli-event-taxonomy
plan: 06
type: execute
wave: 2
depends_on: [07-02, 07-03]
files_modified:
  - src/hooks/session-start.js
  - src/hooks/stop.js
  - src/session/state.js
  - src/session/manager.js
  - src/providers/plane/client.js
  - src/providers/plane/provider.js
  - src/cmux/client.js
  - src/orchestrator/launch.js
autonomous: true
requirements: [LOG-09, LOG-10]

must_haves:
  truths:
    - "`hooks/session-start.js` emite `sessionStart(log, fields)` con los 6 campos D-10 incluyendo `transcript_path` del payload Claude"
    - "`hooks/stop.js` emite `sessionEnd(log, { session_id, plane_task_id, status, ended_at })` antes de `removeSession`"
    - "`providers/plane/client.js` emite `planeApiCall(logger, { method, path, status, duration_ms })` tras cada request exitosa"
    - "`session/manager.js` expone helpers que emiten `stateTransition` al cambiar `session.status`"
    - "DI logger opcional cableado en 7 consumers; todos los callsites son silent-failure (nunca throw si el logger falla)"
    - "`src/session/state.js` acepta `logger` opcional con default `noopLogger` — NUNCA importa logger.js a nivel módulo (mantiene LOG-12)"
    - "`test/check-isolation.test.js` sigue PASS sin modificación"
  artifacts:
    - path: "src/hooks/session-start.js"
      provides: "Emisor de session.start con contrato D-10"
      contains: "sessionStart(log"
    - path: "src/hooks/stop.js"
      provides: "Emisor de session.end antes de removeSession"
      contains: "sessionEnd(log"
    - path: "src/providers/plane/client.js"
      provides: "Emisor de plane.api.call en request()"
      contains: "planeApiCall"
    - path: "src/session/state.js"
      provides: "DI logger opcional con noopLogger default"
      contains: "noopLogger"
  key_links:
    - from: "src/hooks/session-start.js"
      to: "src/logger-events.js"
      via: "dynamic import + sessionStart helper"
      pattern: "sessionStart"
    - from: "src/session/state.js"
      to: "src/logger-noop.js"
      via: "import { noopLogger } — zero-import module"
      pattern: "from '../logger-noop.js'"
    - from: "src/providers/plane/client.js"
      to: "src/logger-events.js"
      via: "dynamic import + planeApiCall helper"
      pattern: "planeApiCall"
---

<objective>
Wave 2: cablear DI del logger en los 7 consumers (session/state, session/manager, hooks/session-start, hooks/stop, providers/plane/client, providers/plane/provider, cmux/client, orchestrator/launch) y emitir los eventos tipados en los callsites identificados.

Purpose: hacer que un `kodo logs <id>` muestre eventos reales en runtime — `session.start`, `session.end`, `plane.api.call`, `state.transition`. Sin este plan, los 7 módulos de logger-events.js están pero nadie los llama.

Output: 8 archivos modificados. `src/check.js` → `src/session/state.js` sin importar `logger.js` (LOG-12 guardián verde). Emisión de 4 tipos de evento en callsites canónicos (los 3 restantes —`orchestrator.review`, `gsd.phase.resolved`, `gsd.bootstrap`— quedan con el logger cableado para Fases 9-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md
@src/hooks/session-start.js
@src/hooks/stop.js
@src/session/state.js
@src/session/manager.js
@src/providers/plane/client.js
@src/providers/plane/provider.js
@src/cmux/client.js
@src/orchestrator/launch.js
@test/check-isolation.test.js

<interfaces>
<!-- Disponibles al entrar este plan -->

From src/logger.js:
```javascript
export function createLogger({ sessionId, minLevel }): Logger;
export { noopLogger };
```

From src/logger-noop.js (zero-import, siempre seguro):
```javascript
export const noopLogger: {
  debug: () => void, info: () => void, warn: () => void, error: () => void,
  child: (bindings) => NoopLogger,
};
```

From src/logger-events.js (Plan 02):
```javascript
export const EVENTS: Readonly<{...7 types}>;
export function resolveTranscriptPath(path, id): string;
export function sessionStart(log, fields): void;
export function sessionEnd(log, fields): void;
export function stateTransition(log, { from, to, reason }): void;
export function planeApiCall(log, { method, path, status, duration_ms }): void;
// orchestratorReview / gsdPhaseResolved / gsdBootstrap no se emiten en Phase 7
```

<!-- Callsites identificados en 07-PATTERNS.md -->

| Event              | Callsite (file:line approx)                                | Firma del helper |
|--------------------|-----------------------------------------------------------|------------------|
| session.start      | src/hooks/session-start.js#main (entre findSession y stdout.write) | sessionStart(log, {session_id, plane_task_id, provider, project_path, transcript_path?, started_at}) |
| session.end        | src/hooks/stop.js (justo antes de removeSession)          | sessionEnd(log, {session_id, plane_task_id, status, ended_at}) |
| state.transition   | src/session/manager.js#markSessionDone (nuevo wrapper)    | stateTransition(log, {from, to, reason}) |
| plane.api.call     | src/providers/plane/client.js#request (tras res.ok)       | planeApiCall(log, {method, path, status, duration_ms}) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DI logger opcional en src/session/state.js (LOG-12 safe)</name>
  <files>src/session/state.js</files>
  <read_first>
    - src/session/state.js (completo — 149 líneas, ver líneas 79-99 para `addSession`/`removeSession`)
    - test/check-isolation.test.js (REGLA CRÍTICA: `src/check.js` → `src/session/state.js` es camino real; si state.js importa logger.js → test 3 FALLA)
    - src/logger-noop.js (zero-import, siempre safe)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 610-640 — pattern exacto D-14 + pitfall 6)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 740-743 — Open Question 5 recommendation)
  </read_first>
  <behavior>
    - Test (guardián): `test/check-isolation.test.js` sigue PASS — NO debe aparecer `logger.js` en el grafo transitivo de `check.js`.
    - Test existente: `test/state.test.js` sigue PASS (las firmas cambian solo con parámetro opcional al final).
    - Nuevo comportamiento: `addSession(taskId, session, logger?)` — si logger no se pasa, `noopLogger` suplanta; si se pasa, emite `state.session.added` a info level.
    - Análogo en `removeSession(taskId, logger?)` → emite `state.session.removed`.
  </behavior>
  <action>
    En `src/session/state.js`:

    1. **Añadir import aditivo** (arriba, junto a los imports existentes):

    ```javascript
    import { noopLogger } from '../logger-noop.js';
    ```

    **CRÍTICO**: NO añadir `import { createLogger } from '../logger.js'`. `logger-noop.js` tiene zero imports y está explícitamente permitido en el grafo de `check.js` (ver `test/check-isolation.test.js` test 4).

    2. **Extender firma de `addSession`** (líneas 79-83 aprox):

    Antes:
    ```javascript
    export function addSession(taskId, session) {
      const state = loadState();
      state.sessions[taskId] = session;
      saveState(state);
    }
    ```

    Después:
    ```javascript
    /**
     * @param {string} taskId
     * @param {import('./state.js').SessionRecord} session
     * @param {import('../logger-noop.js').NoopLogger} [logger]
     */
    export function addSession(taskId, session, logger = noopLogger) {
      const state = loadState();
      state.sessions[taskId] = session;
      saveState(state);
      logger.info('state.session.added', {
        task_id: taskId,
        status: session.status,
      });
    }
    ```

    3. **Extender firma de `removeSession`** análogo (buscar la función existente):

    ```javascript
    /**
     * @param {string} taskId
     * @param {import('../logger-noop.js').NoopLogger} [logger]
     */
    export function removeSession(taskId, logger = noopLogger) {
      const state = loadState();
      delete state.sessions[taskId];
      saveState(state);
      logger.info('state.session.removed', { task_id: taskId });
    }
    ```

    4. **`updateSession`** (si existe en el archivo) — misma extensión con logger opcional + log de cambios.

    5. NO tocar otras funciones (`loadState`, `saveState`, `findSession`, `listSessions`, migraciones) salvo para añadir tipo al argumento opcional si lo requieren.

    NOTA sobre la nota del logger: `noopLogger.info` y `noopLogger.child` son no-ops, así que la llamada a `logger.info(...)` cuando no se pasa logger es gratis en runtime.
  </action>
  <verify>
    <automated>node --test test/state.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from '../logger-noop.js'" src/session/state.js` retorna match.
    - `grep "from '../logger.js'" src/session/state.js` returns 0 (NO importa logger.js — crítico para LOG-12).
    - `grep "noopLogger" src/session/state.js` retorna al menos 2 matches (import + default).
    - `grep "logger = noopLogger" src/session/state.js` retorna al menos 2 matches (addSession + removeSession).
    - `grep "state.session.added\|state.session.removed" src/session/state.js` retorna al menos 2 matches.
    - `node --test test/state.test.js` exits 0 (firmas retrocompatibles).
    - `node --test test/check-isolation.test.js` exits 0 (LOG-12 guardián — test 3 sigue PASS).
  </acceptance_criteria>
  <done>state.js extendido con DI logger opcional, LOG-12 guardián verde, tests de state PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emisor `session.start` en src/hooks/session-start.js</name>
  <files>src/hooks/session-start.js</files>
  <read_first>
    - src/hooks/session-start.js (completo — 114 líneas, ver `main()` y silent-failure pattern en catch)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 505-571 — diff completo con wrapper silent-failure)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-10, D-18, D-19)
    - src/logger-events.js (API de sessionStart + campos requeridos)
    - test/session-start.test.js (NO debe romperse — silent failure preservation)
  </read_first>
  <behavior>
    - Test (existente): `test/session-start.test.js` sigue PASS (silent-failure externo preservado).
    - Nuevo comportamiento: tras `findSession` y ANTES de `process.stdout.write(output)`, emite `sessionStart(log, {...})` con los 6 campos D-10. Si falla, swallowed.
    - `transcript_path` viene del payload Claude (`input.transcript_path`). Si no está en el payload, `sessionStart` auto-resuelve con `resolveTranscriptPath(session.project_path, session.session_id)`.
    - Logger raíz creado dentro del hook (el hook es proceso separado de `cli.js`): `createLogger({ sessionId: session.session_id, minLevel: process.env.KODO_LOG_LEVEL || 'info' }).child({ component: 'hook', plane_task_id: session.task_id })`.
  </behavior>
  <action>
    En `src/hooks/session-start.js`, dentro de la función `main()`:

    Antes del `process.stdout.write(output)` (o del `JSON.stringify(context)` equivalente), AÑADE un bloque try/catch anidado (NUNCA crashear Claude Code — ver silent-failure pattern del archivo). El bloque debe:

    1. Importar dinámicamente `createLogger` y `sessionStart`.
    2. Crear logger raíz bindeado a session_id.
    3. `.child({ component: 'hook', plane_task_id: session.task_id })`.
    4. Invocar `sessionStart(log, {...6 campos D-10...})`.
    5. Swallow cualquier error — jamás throw.

    Template:

    ```javascript
    // NEW: emisión typed (best effort — silent on failure)
    try {
      const { createLogger } = await import('../logger.js');
      const { sessionStart } = await import('../logger-events.js');
      const log = createLogger({
        sessionId: session.session_id,
        minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
      }).child({ component: 'hook', plane_task_id: session.task_id });
      sessionStart(log, {
        session_id: session.session_id,
        plane_task_id: session.task_id,
        provider: session.provider,
        project_path: session.project_path,
        transcript_path: input.transcript_path,  // puede ser undefined; helper hace fallback
        started_at: new Date().toISOString(),
      });
    } catch {
      // silent — nunca crashear Claude Code
    }
    ```

    Inserción: justo antes de que el hook envíe el context a stdout (buscar el `process.stdout.write(...)` o `console.log(JSON.stringify(...))` del hook). El bloque try/catch outer existente del `main()` se preserva tal cual — este try/catch está ANIDADO dentro.

    NO TOCAR: `buildSessionContext`, `findSession`, la forma del payload stdout, `isMainEntry` guard.

    **KODO_LOG_LEVEL**: variable de entorno leída aquí por primera vez (D-13 menciona este como entrypoint alternativo). Si está unset, default `'info'`.
  </action>
  <verify>
    <automated>node --check src/hooks/session-start.js && node --test test/session-start.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sessionStart" src/hooks/session-start.js` retorna match.
    - `grep "from '../logger-events.js'\|'../logger-events.js'" src/hooks/session-start.js` retorna match.
    - `grep "createLogger" src/hooks/session-start.js` retorna match.
    - `grep "component: 'hook'" src/hooks/session-start.js` retorna match.
    - `grep "KODO_LOG_LEVEL" src/hooks/session-start.js` retorna match.
    - `grep -c "try {" src/hooks/session-start.js` returns >= 2 (outer silent-failure + nuevo inner).
    - `grep "transcript_path" src/hooks/session-start.js` retorna match.
    - `node --check src/hooks/session-start.js` exits 0.
    - `node --test test/session-start.test.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0.
  </acceptance_criteria>
  <done>`session.start` emitido en cada invocación del hook con contrato D-10; silent-failure outer preservado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emisor `session.end` en src/hooks/stop.js</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    - src/hooks/stop.js (completo — 144 líneas, ver `main()` + llamada a `removeSession`)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 575-605 — diff + insertion point)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-18, D-09 — sessionEnd acepta status)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 720-724 — Open Question 1: stop.js es emisor canónico)
    - test/stop.test.js (NO debe romperse)
  </read_first>
  <behavior>
    - Test (existente): `test/stop.test.js` sigue PASS.
    - Nuevo: tras la lógica existente del hook (setColor, etc.) y ANTES de `removeSession(id)`, emite `sessionEnd(log, { session_id, plane_task_id, status: session.status, ended_at })`.
    - Misma estrategia silent-failure anidada que `session-start.js`.
    - `session.status` viene del state antes de remover — los valores posibles (`running`, `done`, `error`, `review`, `interrupted`) se pasan tal cual.
  </behavior>
  <action>
    En `src/hooks/stop.js`, dentro de la función `main()` (o equivalente entrypoint del hook), JUSTO ANTES de la llamada `removeSession(id)`:

    ```javascript
    // NEW: emisión typed (best effort — silent on failure)
    try {
      const { createLogger } = await import('../logger.js');
      const { sessionEnd } = await import('../logger-events.js');
      const log = createLogger({
        sessionId: session.session_id,
        minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
      }).child({ component: 'hook', plane_task_id: session.task_id });
      sessionEnd(log, {
        session_id: session.session_id,
        plane_task_id: session.task_id,
        status: session.status,
        ended_at: new Date().toISOString(),
      });
    } catch {
      // silent — nunca crashear Claude Code
    }

    removeSession(id);
    ```

    NO TOCAR: `setColor`, `findSession`, `removeSession`, la forma de respuesta stdout.
  </action>
  <verify>
    <automated>node --check src/hooks/stop.js && node --test test/stop.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sessionEnd" src/hooks/stop.js` retorna match.
    - `grep "from '../logger-events.js'\|'../logger-events.js'" src/hooks/stop.js` retorna match.
    - `grep "component: 'hook'" src/hooks/stop.js` retorna match.
    - `grep "status: session.status" src/hooks/stop.js` retorna match.
    - La llamada `sessionEnd(...)` aparece en el archivo ANTES de `removeSession(` — verificar con `grep -n "sessionEnd\|removeSession" src/hooks/stop.js | head -5`.
    - `node --check src/hooks/stop.js` exits 0.
    - `node --test test/stop.test.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0.
  </acceptance_criteria>
  <done>`session.end` emitido antes de remover la sesión; silent-failure preservado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Emisor `plane.api.call` en src/providers/plane/client.js + DI logger</name>
  <files>src/providers/plane/client.js, src/providers/plane/provider.js</files>
  <read_first>
    - src/providers/plane/client.js (completo — 195 líneas, ver constructor + `request()` method)
    - src/providers/plane/provider.js (factory que instancia PlaneClient)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 644-696 — pattern client+provider DI)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-09 planeApiCall contract, D-14 DI)
    - src/logger-events.js (planeApiCall)
  </read_first>
  <behavior>
    - `PlaneClient` constructor acepta `opts.logger` opcional y lo guarda como `this.logger`.
    - `PlaneClient.request(path, opts)`: mide `Date.now()` antes del `fetch`, emite `planeApiCall` tras `res.ok` check, antes de `return res.json()`.
    - Emisión silent-failure: try/catch interno que no bloquea la request.
    - `createPlaneProvider(config, opts?)` acepta segundo arg con `{ logger }`, hace `logger.child({ component: 'plane' })` y pasa al `new PlaneClient({...existingConfig, logger: childLog})`.
    - Test plane-provider/client existentes deben seguir PASS (firma retrocompatible — el segundo arg es opcional).
  </behavior>
  <action>
    **En `src/providers/plane/client.js`:**

    1. Constructor: añadir `this.logger = opts.logger` (asignar undefined si no se pasa — evita I/O inneceario). NO forzar a noopLogger aquí (la emisión ya usa `this.logger?.` optional chain).

    ```javascript
    // en el constructor existente, junto a this.baseUrl, this.apiKey, this.workspaceSlug:
    this.logger = opts.logger; // undefined si no se pasa — emisión es optional
    ```

    2. Método `async request(path, opts = {})`: envolver el flujo con medición + emisión. Buscar el loop `while (true)` con `fetch` y el bloque `if (!res.ok)`. INSERTAR la medición antes del fetch y la emisión justo tras `if (!res.ok)`:

    ```javascript
    async request(path, opts = {}) {
      const url = new URL(/* existente */);
      // ... headers existentes
      while (true) {
        const started = Date.now();
        const res = await fetch(url, { /* existente */ });
        // ... throttle/rate-limit lógico existente
        if (!res.ok) {
          const text = await res.text().catch(() => '');
          throw new Error(`Plane API ${res.status}: ${path} — ${text}`);
        }
        // NEW: emisión typed (best effort)
        if (this.logger) {
          try {
            const { planeApiCall } = await import('../../logger-events.js');
            planeApiCall(this.logger, {
              method: opts.method || 'GET',
              path,
              status: res.status,
              duration_ms: Date.now() - started,
            });
          } catch {}
        }
        return res.json();
      }
    }
    ```

    NOTA: si `request()` tiene retry loop, medir `started` DENTRO del loop para que cada intento tenga su `duration_ms`. Si el retry ocurre (rate-limit), el primer intento NO emite (falla en `if (!res.ok)`); solo el exitoso final emite.

    **En `src/providers/plane/provider.js`:**

    1. Factory `createPlaneProvider(config, opts = {})`: aceptar segundo arg con `{ logger }`.

    ```javascript
    /**
     * @param {PlaneProviderConfig} config
     * @param {{ logger?: import('../../logger.js').Logger }} [opts]
     */
    export function createPlaneProvider(config, opts = {}) {
      const logger = opts.logger?.child({ component: 'plane' });
      const client = new PlaneClient({
        baseUrl: config.baseUrl,
        apiKey: config.apiKey,
        workspaceSlug: config.workspaceSlug,
        logger,
      });
      // ... resto sin tocar
    }
    ```

    NO TOCAR: API de `PlaneClient.request`, firma pública del provider, parser de `task_ref`, `listProjects`, etc.

    Verificar: si otros archivos llaman `createPlaneProvider(config)` (sin segundo arg), sigue funcionando (opts default `{}`).
  </action>
  <verify>
    <automated>node --check src/providers/plane/client.js && node --check src/providers/plane/provider.js && node --test test/plane-provider.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "planeApiCall" src/providers/plane/client.js` retorna match.
    - `grep "from '../../logger-events.js'\|'../../logger-events.js'" src/providers/plane/client.js` retorna match.
    - `grep "this.logger = opts.logger" src/providers/plane/client.js` retorna match.
    - `grep "Date.now()" src/providers/plane/client.js` retorna al menos 2 matches (started + duration_ms calculation).
    - `grep "duration_ms" src/providers/plane/client.js` retorna match.
    - `grep "opts.logger\|opts = {}" src/providers/plane/provider.js` retorna al menos 1 match (nueva firma).
    - `grep "component: 'plane'" src/providers/plane/provider.js` retorna match.
    - `node --test test/plane-provider.test.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0.
    - `node --test` exits 0 (suite completa).
  </acceptance_criteria>
  <done>`plane.api.call` emitido en requests exitosas; DI del logger cableado desde createPlaneProvider; tests existentes PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: DI logger en session/manager.js, cmux/client.js, orchestrator/launch.js + wrapper stateTransition</name>
  <files>src/session/manager.js, src/cmux/client.js, src/orchestrator/launch.js</files>
  <read_first>
    - src/session/manager.js (completo — 243 líneas)
    - src/cmux/client.js (completo — 85 líneas)
    - src/orchestrator/launch.js (completo — 122 líneas)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 731-812 — patterns cmux + orchestrator + manager)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 725-729 — Open Question 2: state.transition = session.status change)
    - src/logger-events.js (stateTransition contract)
    - test/manager.test.js (NO debe romperse)
  </read_first>
  <behavior>
    - `session/manager.js`: exportar nuevo wrapper `markSessionDone(taskId, logger?)` que lee state antes, actualiza status, y emite `stateTransition(childLog, { from, to: 'done', reason })`. Análogamente `markSessionError`, `markSessionReview` — los que tengan sentido según el state.js actual.
    - `cmux/client.js`: `run(args, logger?)` añadir arg opcional. Si logger, emite `logger.debug('cmux.exec', { cmd })` y `logger.warn('cmux.fail', { cmd, stderr })` en error path. No evento tipado (cmux no está en los 7).
    - `orchestrator/launch.js`: `launchOrchestrator(opts?)` acepta `opts.logger` y hace `.child({ component: 'orchestrator' })`. NO emite `orchestrator.review` aún — eso es Fase 10. Solo cablear el logger.
    - Todos los cambios retrocompatibles (args opcionales al final).
  </behavior>
  <action>
    **`src/session/manager.js`:**

    Añadir (o extender) un helper exportado para transiciones de estado. Buscar función existente que llame `updateSession` para cambiar `session.status`. Si ya existe un wrapper (`markSessionDone` o similar), extender; si no, añadir uno:

    ```javascript
    import { stateTransition } from '../logger-events.js';

    /**
     * Marca una sesión como finalizada y emite state.transition si hay logger.
     * @param {string} taskId
     * @param {'done' | 'error' | 'review' | 'interrupted'} nextStatus
     * @param {string} reason
     * @param {import('../logger.js').Logger} [logger]
     */
    export function markSessionStatus(taskId, nextStatus, reason, logger) {
      const prev = findSession({ sessionId: undefined, cwd: undefined }); // Ajustar según API real
      // NOTA: usa la API real de session/state.js para obtener el session actual
      // (puede ser listSessions().find(...) o getSession(taskId), según lo que exista).
      const current = listSessions().find(s => s.task_id === taskId || s.task_ref === taskId);
      const fromStatus = current?.status || 'unknown';
      updateSession(taskId, { status: nextStatus });
      if (logger) {
        const log = logger.child({ component: 'session', plane_task_id: taskId });
        stateTransition(log, { from: fromStatus, to: nextStatus, reason });
      }
    }
    ```

    NOTA para el implementador: la lectura del `prev.status` depende de la API exacta de `src/session/state.js` — usar la que EXISTA (probablemente `listSessions()` + `find()` o `getSession(taskId)`). Adaptar al nombre correcto.

    NO rewiring forzado: los callsites actuales que llaman directamente a `updateSession(taskId, {status: ...})` no se modifican masivamente en Phase 7. Este wrapper queda disponible para Fase 8/9 al usarlo en el dispatcher.

    **`src/cmux/client.js`:**

    Extender `run()` con arg opcional logger. Buscar función existente:

    ```javascript
    /**
     * @param {string[]} args
     * @param {import('../logger.js').Logger} [logger]
     */
    function run(args, logger) {
      return new Promise((resolve, reject) => {
        logger?.debug('cmux.exec', { cmd: args[0], argc: args.length });
        execFile(getCmuxBinary(), args, { timeout: 15_000 }, (err, stdout, stderr) => {
          if (err) {
            logger?.warn('cmux.fail', { cmd: args[0], stderr: String(stderr || '').slice(0, 200) });
            reject(new Error(`cmux ${args[0]} failed: ${stderr || err.message}`));
            return;
          }
          resolve(stdout.trim());
        });
      });
    }
    ```

    Y los helpers wrappers (`newWorkspace`, `send`, `setColor`, etc.) aceptan opcionalmente `logger` al final y lo pasan a `run`.

    **`src/orchestrator/launch.js`:**

    Extender la firma de `launchOrchestrator` con `opts.logger`:

    ```javascript
    /**
     * @param {{ logger?: import('../logger.js').Logger }} [opts]
     */
    export async function launchOrchestrator(opts = {}) {
      const log = opts.logger?.child({ component: 'orchestrator' });
      // ... flujo existente SIN modificaciones de comportamiento
      // log?.info('orchestrator.launched', { ... }) — opcional, log libre (no typed event en Phase 7)
    }
    ```

    NO EMITIR `orchestrator.review` aún — es Fase 10. Solo cablear.

    Los tests de manager/cmux/orchestrator existentes siguen PASS (firmas retrocompatibles).
  </action>
  <verify>
    <automated>node --check src/session/manager.js && node --check src/cmux/client.js && node --check src/orchestrator/launch.js && node --test test/manager.test.js test/check-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "stateTransition" src/session/manager.js` retorna match.
    - `grep "from '../logger-events.js'" src/session/manager.js` retorna match.
    - `grep "component: 'session'" src/session/manager.js` retorna match.
    - `grep "logger?.debug\|logger?.warn" src/cmux/client.js` retorna al menos 2 matches.
    - `grep "cmux.exec\|cmux.fail" src/cmux/client.js` retorna al menos 2 matches.
    - `grep "component: 'orchestrator'" src/orchestrator/launch.js` retorna match.
    - `grep "opts.logger" src/orchestrator/launch.js` retorna match.
    - `node --check src/session/manager.js` exits 0.
    - `node --check src/cmux/client.js` exits 0.
    - `node --check src/orchestrator/launch.js` exits 0.
    - `node --test test/manager.test.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0.
    - `node --test` (suite completa) exits 0.
  </acceptance_criteria>
  <done>DI logger cableado en manager/cmux/orchestrator; state.transition wrapper disponible; LOG-12 guardián verde; suite completa PASS.</done>
</task>

</tasks>

<verification>
```bash
# Suite completa verde — prueba definitiva de este plan
node --test

# Guardián LOG-12 explícitamente
node --test test/check-isolation.test.js

# Smoke end-to-end: arrancar logger vía hook y verificar session.start
# (manual verification — ver 07-VALIDATION.md sección Manual-Only Verifications)

# Smoke: kodo logs contra un log real
mkdir -p ~/.kodo/logs && cp test/fixtures/events-golden.ndjson ~/.kodo/logs/sess-golden-01.ndjson
node bin/kodo logs sess-golden-01 --json | head -1 | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const r=JSON.parse(d); if(r.event!=='session.start'||!r.transcript_path)process.exit(1);})"
rm ~/.kodo/logs/sess-golden-01.ndjson
```
</verification>

<success_criteria>
- 4 eventos tipados emitidos en sus callsites canónicos: `session.start` (hook), `session.end` (hook), `plane.api.call` (plane client), `state.transition` (manager wrapper disponible para dispatcher).
- 3 eventos restantes (`orchestrator.review`, `gsd.phase.resolved`, `gsd.bootstrap`) tienen el logger cableado — Fases 9-10 los emiten.
- DI logger opcional funcional en 7 consumers: state, manager, session-start, stop, plane/client, plane/provider, cmux, orchestrator.
- `test/check-isolation.test.js` PASS — LOG-12 guardián intacto.
- Suite completa (`node --test`) exits 0.
- Zero nuevas dependencias runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/07-kodo-logs-cli-event-taxonomy/07-06-SUMMARY.md`.
</output>
