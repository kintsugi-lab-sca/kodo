---
phase: 07-kodo-logs-cli-event-taxonomy
plan: 04
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/logs/session-lookup.js
  - src/logs/head-line.js
autonomous: true
requirements: [LOG-11]

must_haves:
  truths:
    - "`resolveSessionIdFromTaskId(taskId)` hace step 1 (loadState) antes de escanear logs/"
    - "Step 2: lee solo la primera línea de cada `.ndjson` (head-line bounded ≤64KB) sin consumir el archivo entero"
    - "Multi-match devuelve la más reciente por timestamp DESC y warn stderr con descartados"
    - "Archivo corrupto o JSON malformed en head-line → skip, no crash"
    - "Sin match en ambos pasos → devuelve `null`"
  artifacts:
    - path: "src/logs/session-lookup.js"
      provides: "resolveSessionIdFromTaskId(taskId) — two-step lookup"
      contains: "export async function resolveSessionIdFromTaskId"
      min_lines: 50
    - path: "src/logs/head-line.js"
      provides: "readFirstLine(filePath) bounded head-line reader"
      contains: "export function readFirstLine"
      min_lines: 25
  key_links:
    - from: "src/logs/session-lookup.js"
      to: "src/session/state.js"
      via: "import { loadState } from '../session/state.js'"
      pattern: "loadState"
    - from: "src/logs/session-lookup.js"
      to: "src/logs/head-line.js"
      via: "import { readFirstLine } from './head-line.js'"
      pattern: "readFirstLine"
    - from: "src/logs/session-lookup.js"
      to: "src/config.js"
      via: "import { KODO_DIR }"
      pattern: "KODO_DIR"
---

<objective>
Wave 1 (paralelo con Plans 02 y 03): implementar el resolver `--session-of <task-id>` — dos pasos (state.json → fallback head-line scan) con multi-match warn.

Purpose: cubrir LOG-11. `kodo logs --session-of KL-42` localiza el session-id sin que el dev lo conozca, permitiendo pivotar desde un task Plane directo al log.

Output: 2 módulos nuevos, tests `test/logs-session-of.test.js` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md
@test/logs-session-of.test.js
@src/config.js

<interfaces>
<!-- API consumida -->

From src/session/state.js:
```javascript
export function loadState(): { schema_version: number, sessions: Record<string, SessionRecord> };
// SessionRecord shape (schema v2): { session_id, task_id, task_ref, provider, project_path, status, ... }
```

From src/config.js:
```javascript
export const KODO_DIR: string;
```

<!-- API producida -->

From src/logs/head-line.js (NEW):
```javascript
/** Cap bytes leídos si no se encuentra '\n'. */
export const MAX_HEADLINE_BYTES: number;  // 65536
/** Devuelve la primera línea (sin '\n') o null si no hay newline dentro del cap o archivo vacío. */
export function readFirstLine(filePath: string): string | null;
```

From src/logs/session-lookup.js (NEW):
```javascript
/**
 * Dos pasos:
 *   1. loadState() busca por task_id o task_ref en ~/.kodo/state.json.
 *   2. Scan ~/.kodo/logs/*.ndjson — head-line parse — match por session.start.plane_task_id.
 *      Multi-match: pick timestamp DESC, warn descarted a stderr.
 * @param {string} taskId
 * @returns {Promise<string | null>} sessionId o null
 */
export async function resolveSessionIdFromTaskId(taskId): Promise<string | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/logs/head-line.js — readFirstLine bounded</name>
  <files>src/logs/head-line.js</files>
  <read_first>
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 290-320 — Pattern 2 verbatim, VERIFIED)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 329-354 — head-line pattern)
  </read_first>
  <behavior>
    - Test 1 (acceptance): archivo con `{"a":1}\n{"b":2}\n` → `readFirstLine` returns `{"a":1}` (sin '\n').
    - Test 2: archivo vacío → `null`.
    - Test 3: archivo de 1KB sin '\n' → `null` (no acumula más allá del cap).
    - Test 4: archivo de 100KB con '\n' en byte 50KB → returns la línea truncada a 50KB.
    - Test 5: archivo de 100KB sin '\n' dentro de 64KB → `null` (MAX_HEADLINE_BYTES alcanzado).
  </behavior>
  <action>
    Crea `src/logs/head-line.js`:

    ```javascript
    // @ts-check
    //
    // src/logs/head-line.js — Lectura bounded de la primera línea de un archivo.
    //
    // Optimización sobre readline.createInterface: NO abre stream que dispara
    // 'data' events para todo el archivo. Lee 4KB chunks hasta encontrar '\n'
    // o alcanzar MAX_HEADLINE_BYTES (64KB). Útil para escaneo del directorio
    // logs/ donde cada archivo puede ser grande pero solo importa la cabecera.
    //

    import { openSync, readSync, closeSync } from 'node:fs';

    /** Cap total de bytes antes de abortar la búsqueda de '\n'. */
    export const MAX_HEADLINE_BYTES = 65536;

    /** Tamaño de cada read syscall. */
    const READ_CHUNK = 4096;

    /**
     * Lee solo la primera línea de un archivo (sin el '\n').
     * Devuelve null si:
     *   - archivo vacío (EOF inmediato)
     *   - no hay '\n' en los primeros MAX_HEADLINE_BYTES bytes
     *
     * @param {string} filePath
     * @returns {string | null}
     */
    export function readFirstLine(filePath) {
      const fd = openSync(filePath, 'r');
      try {
        const buf = Buffer.alloc(READ_CHUNK);
        let acc = '';
        let pos = 0;
        while (acc.length < MAX_HEADLINE_BYTES) {
          const n = readSync(fd, buf, 0, buf.length, pos);
          if (n === 0) return null; // EOF sin '\n'
          const chunk = buf.slice(0, n).toString('utf8');
          const nl = chunk.indexOf('\n');
          if (nl !== -1) return acc + chunk.slice(0, nl);
          acc += chunk;
          pos += n;
        }
        return null;
      } finally {
        closeSync(fd);
      }
    }
    ```

    NOTA: no se importa `logger.js` ni `logger-noop.js` — módulo puro, no entra en grafo de check.js.
  </action>
  <verify>
    <automated>node --check src/logs/head-line.js && node -e "const { readFirstLine } = await import('./src/logs/head-line.js'); const fs = await import('node:fs'); fs.writeFileSync('/tmp/hl-test.ndjson', '{\"a\":1}\n{\"b\":2}\n'); if (readFirstLine('/tmp/hl-test.ndjson') !== '{\"a\":1}') process.exit(1); fs.unlinkSync('/tmp/hl-test.ndjson');" --input-type=module</automated>
  </verify>
  <acceptance_criteria>
    - `src/logs/head-line.js` existe, ≥25 líneas.
    - `grep -c "^export const MAX_HEADLINE_BYTES" src/logs/head-line.js` returns 1.
    - `grep "MAX_HEADLINE_BYTES = 65536" src/logs/head-line.js` retorna match.
    - `grep -c "^export function readFirstLine" src/logs/head-line.js` returns 1.
    - `grep -c "openSync\|readSync\|closeSync" src/logs/head-line.js` returns >= 3.
    - `grep "readline" src/logs/head-line.js` returns 0 (NO usa readline).
    - `grep "try {" src/logs/head-line.js` retorna match, `grep "finally {" src/logs/head-line.js` retorna match (closeSync garantizado).
    - `node --check src/logs/head-line.js` exits 0.
    - Smoke test: archivo con `{"a":1}\n{"b":2}\n` → `readFirstLine` returns `{"a":1}` exactamente.
  </acceptance_criteria>
  <done>Helper `readFirstLine` creado, bounded a 64KB, libera fd con try/finally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/logs/session-lookup.js — resolveSessionIdFromTaskId (2 pasos + multi-match)</name>
  <files>src/logs/session-lookup.js</files>
  <read_first>
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 651-689 — resolver template completo)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 298-371 — two-step pattern)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-20, D-21, D-22)
    - src/session/state.js (API de loadState — ya existe de Fase 1/3)
    - test/logs-session-of.test.js (contract exacto)
    - src/config.js (KODO_DIR)
  </read_first>
  <behavior>
    - Test 1: `state.json` tiene `{sessions: {"KL-42": {session_id:"sess-abc", task_id:"KL-42"}}}` → returns `"sess-abc"`, no escanea.
    - Test 2: `state.json` vacío + `~/.kodo/logs/sess-xyz.ndjson` con primera línea que tiene `event:"session.start"` y `plane_task_id:"KL-99"` → returns `"sess-xyz"`.
    - Test 3: Dos `.ndjson` con `plane_task_id:"KL-DUP"`, timestamps `09:00` y `11:00` → returns el de `11:00` + warn a stderr con el de `09:00`.
    - Test 4: Archivo con primera línea JSON malformada → skip sin crash.
    - Test 5: Sin match en ningún paso → returns `null`.
    - Test 6: También matchea `task_ref` además de `task_id` (D-20 literal).
  </behavior>
  <action>
    Crea `src/logs/session-lookup.js`:

    ```javascript
    // @ts-check
    //
    // src/logs/session-lookup.js — Resolver de session-id desde task-id.
    //
    // Dos pasos (D-20):
    //   1. loadState() — match rápido en ~/.kodo/state.json por task_id/task_ref.
    //   2. Fallback: scan de ~/.kodo/logs/*.ndjson.
    //      Para cada archivo: head-line-read (readFirstLine) → parse → match
    //      session.start con plane_task_id === taskId.
    //
    // Multi-match (D-21): ordenar por timestamp DESC, devolver el más reciente,
    // warn a stderr con los session_id descartados.
    //

    import { readdirSync, existsSync } from 'node:fs';
    import { join } from 'node:path';
    import { KODO_DIR } from '../config.js';
    import { loadState } from '../session/state.js';
    import { readFirstLine } from './head-line.js';

    /**
     * @param {string} taskId
     * @returns {Promise<string | null>}
     */
    export async function resolveSessionIdFromTaskId(taskId) {
      // Step 1 — state.json index
      const state = loadState();
      const sessions = state.sessions || {};
      for (const s of Object.values(sessions)) {
        /** @type {any} */
        const sess = s;
        if (sess.task_id === taskId || sess.task_ref === taskId) {
          return sess.session_id;
        }
      }

      // Step 2 — scan logs/ head-line
      const logsDir = join(KODO_DIR, 'logs');
      if (!existsSync(logsDir)) return null;

      /** @type {Array<{ sessionId: string, timestamp: string }>} */
      const matches = [];
      for (const fn of readdirSync(logsDir)) {
        if (!fn.endsWith('.ndjson')) continue;
        let first;
        try {
          first = readFirstLine(join(logsDir, fn));
        } catch {
          continue;
        }
        if (!first) continue;
        /** @type {any} */
        let rec;
        try {
          rec = JSON.parse(first);
        } catch {
          continue;
        }
        if (rec && rec.event === 'session.start' && rec.plane_task_id === taskId) {
          matches.push({
            sessionId: fn.replace(/\.ndjson$/, ''),
            timestamp: String(rec.timestamp || ''),
          });
        }
      }

      if (matches.length === 0) return null;

      // Sort DESC por timestamp ISO-8601 (lexicográfico === cronológico)
      matches.sort((a, b) => b.timestamp.localeCompare(a.timestamp));

      if (matches.length > 1) {
        process.stderr.write(`Multiple sessions for task ${taskId}:\n`);
        for (const m of matches.slice(1)) {
          process.stderr.write(`  ${m.sessionId}  ${m.timestamp}\n`);
        }
        process.stderr.write(`Using most recent: ${matches[0].sessionId}\n\n`);
      }

      return matches[0].sessionId;
    }
    ```

    NOTAS:
    - Imports `loadState` de `src/session/state.js` — que existe desde Fase 1/3. No se toca en este plan.
    - `readdirSync` determinístico por entrada (no por fecha) — por eso el sort manual por `rec.timestamp`.
    - El warn stderr sigue fixture literal: `"Multiple sessions for task KL-DUP:\n  sess-old  2026-...\nUsing most recent: sess-new\n\n"`.
  </action>
  <verify>
    <automated>node --test test/logs-session-of.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/logs/session-lookup.js` existe, ≥50 líneas.
    - `grep -c "^export async function resolveSessionIdFromTaskId" src/logs/session-lookup.js` returns 1.
    - `grep "loadState" src/logs/session-lookup.js` retorna match (step 1).
    - `grep "readFirstLine" src/logs/session-lookup.js` retorna match (step 2).
    - `grep "readdirSync" src/logs/session-lookup.js` retorna match.
    - `grep "session.start" src/logs/session-lookup.js` retorna match (D-20 step 2 filter).
    - `grep "plane_task_id" src/logs/session-lookup.js` retorna match.
    - `grep "task_ref" src/logs/session-lookup.js` retorna match (D-20 literal: también matchea task_ref).
    - `grep "localeCompare" src/logs/session-lookup.js` retorna match (sort DESC).
    - `grep "Multiple sessions" src/logs/session-lookup.js` retorna match (warn text).
    - `grep "Using most recent" src/logs/session-lookup.js` retorna match.
    - `node --test test/logs-session-of.test.js` exits 0.
    - `node --test test/check-isolation.test.js` exits 0 (LOG-12 intacto; session-lookup.js NO está en grafo de check.js).
  </acceptance_criteria>
  <done>Resolver completo, 4 tests de logs-session-of.test.js PASS (step1, step2, multi-match, null), LOG-12 guardián verde.</done>
</task>

</tasks>

<verification>
```bash
# Tests específicos
node --test test/logs-session-of.test.js

# LOG-12 guardián — session-lookup.js importa state.js y config.js, NO logger.js directo.
# state.js no debe importar logger.js a nivel módulo (Plan 05 añade DI opcional con noopLogger).
node --test test/check-isolation.test.js

# Tests de state.js (fase 1/3) no deben romperse por este plan (no se modifica state.js aún).
node --test test/state.test.js
```
</verification>

<success_criteria>
- `src/logs/head-line.js` creado con `readFirstLine` bounded a 64KB.
- `src/logs/session-lookup.js` creado con `resolveSessionIdFromTaskId(taskId)` dos pasos + multi-match.
- `test/logs-session-of.test.js` PASS 100%.
- `test/check-isolation.test.js` sigue verde.
- `test/state.test.js` sigue verde (no se modifica state.js en este plan).
</success_criteria>

<output>
After completion, create `.planning/phases/07-kodo-logs-cli-event-taxonomy/07-04-SUMMARY.md`.
</output>
