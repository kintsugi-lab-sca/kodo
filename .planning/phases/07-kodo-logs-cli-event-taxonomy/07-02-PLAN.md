---
phase: 07-kodo-logs-cli-event-taxonomy
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/logger-events.js
autonomous: true
requirements: [LOG-09, LOG-10]

must_haves:
  truths:
    - "Existen las 7 constantes `EVENTS.*` frozen y los 7 helpers correspondientes"
    - "`sessionStart(log, fields)` emite una línea con los 6 campos obligatorios (D-10) incluyendo `transcript_path` auto-resuelto si falta"
    - "`resolveTranscriptPath(path, id)` es pura (sin I/O) y determinista"
    - "test/logger-events.test.js y test/transcript-path.test.js pasan en verde"
  artifacts:
    - path: "src/logger-events.js"
      provides: "EVENTS const + 7 helpers + resolveTranscriptPath"
      contains: "export const EVENTS"
      min_lines: 100
  key_links:
    - from: "src/logger-events.js"
      to: "src/logger.js"
      via: "import typedef Logger via JSDoc + logger.info() call dentro de cada helper"
      pattern: "logger\\.(info|warn|error)"
    - from: "src/logger-events.js"
      to: "node:os + node:path"
      via: "homedir() + join() en resolveTranscriptPath"
      pattern: "homedir\\(\\).*join"
---

<objective>
Wave 1: crear `src/logger-events.js` — módulo puro con las 7 constantes frozen, los 7 helpers tipados y el resolver de transcript path. Hace pasar `test/logger-events.test.js` y `test/transcript-path.test.js` del Plan 01.

Purpose: cerrar el contrato de taxonomía de eventos (LOG-09, LOG-10). Los consumers de Wave 2 y Fases 9-10 importan de aquí — una sola fuente de verdad.

Output: `src/logger-events.js` (~120 líneas), tests LOG-09 + LOG-10 verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md
@src/logger.js
@test/logger-events.test.js
@test/transcript-path.test.js
@test/fixtures/events-golden.ndjson

<interfaces>
<!-- API consumida del logger ya entregado en Fase 6 -->

From src/logger.js:
```javascript
/** @typedef {{
 *   debug(msg: string, ctx?: object): void,
 *   info(msg: string, ctx?: object): void,
 *   warn(msg: string, ctx?: object): void,
 *   error(msg: string, ctx?: object): void,
 *   child(bindings: object): Logger,
 * }} Logger */
export function createLogger({ sessionId, minLevel }): Logger;
```

El redactor corre dentro de `emit()` → `redact()`. Los helpers pasan `ctx` a `logger.info(msg, ctx)` sin redactar ellos mismos.

<!-- API que este plan produce -->

To be exported from src/logger-events.js:
```javascript
export const EVENTS: Readonly<{
  SESSION_START: 'session.start',
  SESSION_END: 'session.end',
  STATE_TRANSITION: 'state.transition',
  ORCHESTRATOR_REVIEW: 'orchestrator.review',
  GSD_PHASE_RESOLVED: 'gsd.phase.resolved',
  GSD_BOOTSTRAP: 'gsd.bootstrap',
  PLANE_API_CALL: 'plane.api.call',
}>;
export function resolveTranscriptPath(projectPath: string, sessionId: string): string;
export function sessionStart(logger, { session_id, plane_task_id, provider, project_path, transcript_path?, started_at }): void;
export function sessionEnd(logger, { session_id, plane_task_id, status, ended_at }): void;
export function stateTransition(logger, { from, to, reason }): void;
export function orchestratorReview(logger, { phase_id, verdict, reason }): void;
export function gsdPhaseResolved(logger, { phase_id, match_heading }): void;
export function gsdBootstrap(logger, { project_path }): void;
export function planeApiCall(logger, { method, path, status, duration_ms }): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/logger-events.js — EVENTS const + resolveTranscriptPath + 7 helpers</name>
  <files>src/logger-events.js</files>
  <read_first>
    - src/logger.js (estilo: `Object.freeze`, JSDoc `@typedef` Logger, export patterns)
    - test/logger-events.test.js (contrato exacto que debe satisfacer este módulo)
    - test/transcript-path.test.js (expectativas de resolveTranscriptPath)
    - test/fixtures/events-golden.ndjson (shape exacto de cada tipo de evento)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 42-130 — template literal para el módulo completo)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-07..D-12, D-17, D-18)
  </read_first>
  <behavior>
    - `EVENTS` es `Object.freeze`: `Object.isFrozen(EVENTS)` returns true.
    - `resolveTranscriptPath('/Users/alex/dev/klab/kodo', 'uuid')` returns `${homedir()}/.claude/projects/-Users-alex-dev-klab-kodo/uuid.jsonl` — sin I/O.
    - `sessionStart(log, fields)`: si `fields.transcript_path` es `undefined`, se auto-resuelve via `resolveTranscriptPath(project_path, session_id)`.
    - Cada helper llama `logger.info(msg, ctx)` (o `.warn`/`.error` para `orchestratorReview`) con `msg` = el event type y `ctx` conteniendo `event: EVENTS.<X>` + los campos del contrato.
    - Helpers NO validan campos obligatorios con throw — el test de contrato valida presencia, no el helper.
    - No I/O de disco, no `fs.*`, no `fetch`, no `child_process` — módulo puro.
  </behavior>
  <action>
    Crea `src/logger-events.js` (ES module, `@ts-check`). Contenido:

    ```javascript
    // @ts-check
    //
    // src/logger-events.js — Taxonomía cerrada de 7 eventos de ciclo de vida.
    //
    // Contrato fijo por ROADMAP §Phase 7:
    //   session.start, session.end, state.transition, orchestrator.review,
    //   gsd.phase.resolved, gsd.bootstrap, plane.api.call
    //
    // Los helpers delegan en logger.info/warn/error — el sink NDJSON y el redactor
    // siguen siendo los de src/logger.js (Fase 6). Este archivo es pure transform:
    // campos → record. No abre archivos, no hace I/O.
    //
    // LOG-12 invariant: ningún consumer en el grafo de src/check.js importa este
    // módulo. Los imports son solo `node:os` + `node:path` (stdlib).
    //

    import { homedir } from 'node:os';
    import { join } from 'node:path';

    /** @type {Readonly<{
     *   SESSION_START: 'session.start',
     *   SESSION_END: 'session.end',
     *   STATE_TRANSITION: 'state.transition',
     *   ORCHESTRATOR_REVIEW: 'orchestrator.review',
     *   GSD_PHASE_RESOLVED: 'gsd.phase.resolved',
     *   GSD_BOOTSTRAP: 'gsd.bootstrap',
     *   PLANE_API_CALL: 'plane.api.call',
     * }>} */
    export const EVENTS = Object.freeze({
      SESSION_START:        'session.start',
      SESSION_END:          'session.end',
      STATE_TRANSITION:     'state.transition',
      ORCHESTRATOR_REVIEW:  'orchestrator.review',
      GSD_PHASE_RESOLVED:   'gsd.phase.resolved',
      GSD_BOOTSTRAP:        'gsd.bootstrap',
      PLANE_API_CALL:       'plane.api.call',
    });

    /**
     * Path determinista del transcript de Claude Code. Pure — no I/O.
     *
     * Convención empíricamente verificada:
     *   encodeURIComponent('/Users/alex/dev/klab/kodo').replace(/%2F/g, '-')
     *   === '-Users-alex-dev-klab-kodo'
     *
     * Limitación (Pitfall 3 de 07-RESEARCH): paths con caracteres no-ASCII
     * o espacios producen encodings que Claude Code puede no respetar. El
     * campo se persiste tal cual; si el transcript no existe en disco, es
     * responsabilidad del dev tool consumer, no del logger.
     *
     * @param {string} projectPath absolute
     * @param {string} sessionId UUID v4
     * @returns {string}
     */
    export function resolveTranscriptPath(projectPath, sessionId) {
      const encoded = encodeURIComponent(projectPath).replace(/%2F/g, '-');
      return join(homedir(), '.claude', 'projects', encoded, `${sessionId}.jsonl`);
    }

    /**
     * @typedef {import('./logger.js').Logger} Logger
     */

    /**
     * Emite la línea con el contrato mínimo D-10 (6 campos obligatorios).
     * Si falta `transcript_path`, se auto-resuelve con resolveTranscriptPath.
     *
     * @param {Logger} logger
     * @param {{
     *   session_id: string,
     *   plane_task_id: string | null,
     *   provider: string,
     *   project_path: string,
     *   transcript_path?: string,
     *   started_at: string,
     * }} fields
     */
    export function sessionStart(logger, fields) {
      const transcript_path = fields.transcript_path
        ?? resolveTranscriptPath(fields.project_path, fields.session_id);
      logger.info(EVENTS.SESSION_START, {
        event: EVENTS.SESSION_START,
        session_id: fields.session_id,
        plane_task_id: fields.plane_task_id,
        provider: fields.provider,
        project_path: fields.project_path,
        transcript_path,
        started_at: fields.started_at,
      });
    }

    /**
     * @param {Logger} logger
     * @param {{
     *   session_id: string,
     *   plane_task_id: string | null,
     *   status: 'done' | 'error' | 'review' | 'interrupted' | 'running',
     *   ended_at: string,
     * }} fields
     */
    export function sessionEnd(logger, fields) {
      logger.info(EVENTS.SESSION_END, {
        event: EVENTS.SESSION_END,
        session_id: fields.session_id,
        plane_task_id: fields.plane_task_id,
        status: fields.status,
        ended_at: fields.ended_at,
      });
    }

    /**
     * @param {Logger} logger
     * @param {{ from: string, to: string, reason: string }} fields
     */
    export function stateTransition(logger, fields) {
      logger.info(EVENTS.STATE_TRANSITION, {
        event: EVENTS.STATE_TRANSITION,
        from: fields.from,
        to: fields.to,
        reason: fields.reason,
      });
    }

    /**
     * @param {Logger} logger
     * @param {{ phase_id: string, verdict: 'approved' | 'blocked', reason: string }} fields
     */
    export function orchestratorReview(logger, fields) {
      // verdict !== 'approved' → warn para espejar a stderr también
      const level = fields.verdict === 'approved' ? 'info' : 'warn';
      logger[level](EVENTS.ORCHESTRATOR_REVIEW, {
        event: EVENTS.ORCHESTRATOR_REVIEW,
        phase_id: fields.phase_id,
        verdict: fields.verdict,
        reason: fields.reason,
      });
    }

    /**
     * @param {Logger} logger
     * @param {{ phase_id: string, match_heading: string }} fields
     */
    export function gsdPhaseResolved(logger, fields) {
      logger.info(EVENTS.GSD_PHASE_RESOLVED, {
        event: EVENTS.GSD_PHASE_RESOLVED,
        phase_id: fields.phase_id,
        match_heading: fields.match_heading,
      });
    }

    /**
     * @param {Logger} logger
     * @param {{ project_path: string }} fields
     */
    export function gsdBootstrap(logger, fields) {
      logger.info(EVENTS.GSD_BOOTSTRAP, {
        event: EVENTS.GSD_BOOTSTRAP,
        project_path: fields.project_path,
      });
    }

    /**
     * @param {Logger} logger
     * @param {{ method: string, path: string, status: number, duration_ms: number }} fields
     */
    export function planeApiCall(logger, fields) {
      logger.info(EVENTS.PLANE_API_CALL, {
        event: EVENTS.PLANE_API_CALL,
        method: fields.method,
        path: fields.path,
        status: fields.status,
        duration_ms: fields.duration_ms,
      });
    }
    ```

    Notas implementación:
    - El primer argumento a `logger.info()` es el event string — también sirve como `msg` (ya que el record base del logger hace `msg: String(msg)`). Eso asegura que el NDJSON tiene tanto `msg: "session.start"` como `event: "session.start"` — consistente con el golden fixture.
    - `orchestratorReview` usa `logger[level]` para elegir `info`/`warn` según `verdict` — alineado con el golden fixture (línea 6 usa level=warn para `verdict: "blocked"`).
    - NO se importan ni `logger.js`, ni `logger-noop.js`, ni ningún módulo del repo. Los únicos imports son `node:os` y `node:path`.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js test/transcript-path.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/logger-events.js` existe con al menos 100 líneas.
    - `grep -c "export function" src/logger-events.js` returns >= 8 (`resolveTranscriptPath` + 7 helpers).
    - `grep -c "export const EVENTS" src/logger-events.js` returns 1.
    - `grep -c "Object.freeze" src/logger-events.js` returns >= 1.
    - `grep -cE "^import" src/logger-events.js` returns exactly 2 (solo `node:os` y `node:path`).
    - `grep "from 'node:fs'" src/logger-events.js` returns 0 (pure module, no I/O).
    - `grep "from './logger" src/logger-events.js` returns 0 (no importa logger.js ni logger-noop.js — solo `import('./logger.js').Logger` en JSDoc, que no es import real).
    - `node --test test/logger-events.test.js test/transcript-path.test.js` exits 0 (todos los tests verdes).
    - `node --test test/check-isolation.test.js` exits 0 (LOG-12 intacto).
  </acceptance_criteria>
  <done>`src/logger-events.js` creado, `test/logger-events.test.js` + `test/transcript-path.test.js` PASS, `test/check-isolation.test.js` sigue PASS.</done>
</task>

</tasks>

<verification>
```bash
# Tests específicos de esta wave en verde
node --test test/logger-events.test.js test/transcript-path.test.js

# LOG-12 intocable
node --test test/check-isolation.test.js

# Ningún test existente rompe
node --test test/logger.test.js test/logger-redaction.test.js

# Zero nuevas dependencias runtime
test "$(node -e 'console.log(Object.keys(JSON.parse(require(\"fs\").readFileSync(\"package.json\"))).includes(\"dependencies\") ? Object.keys(JSON.parse(require(\"fs\").readFileSync(\"package.json\")).dependencies).length : 0)')" = "1"
```
</verification>

<success_criteria>
- `src/logger-events.js` creado, 7 helpers + EVENTS const + resolveTranscriptPath exportados.
- `test/logger-events.test.js` pasa al 100% (todos los `it(...)` verdes).
- `test/transcript-path.test.js` pasa al 100%.
- `test/check-isolation.test.js` sigue verde (LOG-12 mantiene).
- `package.json` sin nuevas deps runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/07-kodo-logs-cli-event-taxonomy/07-02-SUMMARY.md`.
</output>
