---
phase: 07-kodo-logs-cli-event-taxonomy
plan: 05
type: execute
wave: 2
depends_on: [07-03, 07-04]
files_modified:
  - src/cli.js
autonomous: true
requirements: [LOG-05, LOG-06, LOG-07, LOG-11]

must_haves:
  truths:
    - "`kodo logs <session-id>` ejecuta `runLogs` con los flags del CLI"
    - "El sub-comando acepta `-f`, `-l`, `-c`, `-e` (variadic), `--json`, `--session-of`"
    - "El sub-comando queda registrado entre `status` y `program.parse()` sin romper otros sub-comandos"
  artifacts:
    - path: "src/cli.js"
      provides: "Registro de `kodo logs [session-id]` sub-comando"
      contains: ".command('logs [session-id]')"
  key_links:
    - from: "src/cli.js"
      to: "src/logs/reader.js"
      via: "dynamic import dentro de .action"
      pattern: "import\\('\\./logs/reader\\.js'\\)"
---

<objective>
Wave 2: registrar el sub-comando `kodo logs [session-id]` en `src/cli.js` con todos los flags de D-02. Action delega en `runLogs` ya implementado en Plan 03.

Purpose: exponer al usuario los LOG-05/06/07/11 a través del binario `kodo`. Sin este plan, el código está pero no es invocable via CLI.

Output: `src/cli.js` editado, `kodo logs --help` muestra los 6 flags, invocación funcional end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md
@.planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md
@src/cli.js

<interfaces>
From src/logs/reader.js (creado en Plan 03):
```javascript
/**
 * @param {{
 *   sessionId?: string, follow?: boolean, level?: string,
 *   component?: string, eventType?: string[], json?: boolean,
 *   sessionOf?: string,
 * }} opts
 */
export async function runLogs(opts): Promise<void>;
```

Patrón commander ya establecido en src/cli.js:
- 8 sub-comandos existentes: config, start, stop, check, install, uninstall, orchestrate, launch, status.
- `program.parse()` en línea 213 — el registro nuevo debe ir ANTES.
- Cada action handler usa `await import('./<module>.js')` para lazy-load (líneas 25, 73, 82, 94, 111, 120, 130, 153, 194).
- Patrón error handling: `try { ... } catch (err) { console.error(`Error: ${err.message}`); process.exit(1); }` (ver `launch` líneas 183-186).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar `kodo logs [session-id]` en src/cli.js</name>
  <files>src/cli.js</files>
  <read_first>
    - src/cli.js (completo — especialmente líneas 143-187 para `launch` como analog, 189-211 para `status`, 213 para `program.parse()`)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-PATTERNS.md (líneas 374-427 — Register pattern + insertion point)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-CONTEXT.md (D-01, D-02)
    - .planning/phases/07-kodo-logs-cli-event-taxonomy/07-RESEARCH.md (líneas 267-280, Pattern 1 commander variadic)
  </read_first>
  <action>
    En `src/cli.js`, INSERTA un nuevo bloque `.command('logs [session-id]')` ENTRE el comando `status` (que termina cerca de línea 211) y `program.parse()` (línea 213). El bloque debe ir EXACTAMENTE así (copia textual, preserva indentación y comillas simples):

    ```javascript
    // --- kodo logs ---
    program
      .command('logs [session-id]')
      .description('Inspect a session log (dump, tail, filter)')
      .option('-f, --follow', 'Tail live output (like tail -f)')
      .option('-l, --level <level>', 'Min log level: debug|info|warn|error')
      .option('-c, --component <name>', 'Filter by component')
      .option('-e, --event-type <type...>', 'Filter by event type (repeatable)')
      .option('--json', 'Emit raw NDJSON (pipe-friendly)')
      .option('--session-of <task-id>', 'Resolve session-id from task id')
      .action(async (sessionId, opts) => {
        try {
          const { runLogs } = await import('./logs/reader.js');
          await runLogs({
            sessionId,
            follow: opts.follow || false,
            level: opts.level,
            component: opts.component,
            eventType: opts.eventType,
            json: opts.json || false,
            sessionOf: opts.sessionOf,
          });
        } catch (err) {
          console.error(`Error: ${err.message}`);
          process.exit(1);
        }
      });
    ```

    DETALLES:
    - Positional `[session-id]` (corchetes) — opcional porque `--session-of` lo reemplaza (D-01).
    - `-e, --event-type <type...>` variadic: `kodo logs X -e session.start -e plane.api.call` → `opts.eventType === ['session.start','plane.api.call']` (VERIFIED RESEARCH A4).
    - Action handler es async — commander 13 lo soporta.
    - NO usar `await ensureConfig()` — `runLogs` no necesita provider config (solo lee `~/.kodo/logs/`).
    - El mapping de opts lo hacemos explícito (`follow: opts.follow || false`, etc.) para que runLogs reciba shape consistente incluso si commander deja algún flag como `undefined`.

    NO TOCAR:
    - Ningún otro sub-comando (`config`, `start`, `stop`, `check`, `install`, `uninstall`, `orchestrate`, `launch`, `status`).
    - `program.parse()` debe quedar DESPUÉS del nuevo bloque.
    - Helpers como `ensureConfig`, `timeSince`, `setNestedValue` intocados.
  </action>
  <verify>
    <automated>node --check src/cli.js && node bin/kodo logs --help 2>&1 | grep -qE "(--follow|--event-type|--session-of)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\.command('logs \\[session-id\\]')" src/cli.js` returns 1.
    - `grep "'--follow'" src/cli.js` retorna match nuevo (no duplicado en otros sub-commands).
    - `grep "'--event-type <type...>'" src/cli.js` retorna match.
    - `grep "'--session-of <task-id>'" src/cli.js` retorna match.
    - `grep "'./logs/reader.js'" src/cli.js` retorna match (dynamic import).
    - `grep -c "program.parse()" src/cli.js` returns 1 (sigue siendo único, no duplicado).
    - `node --check src/cli.js` exits 0.
    - `node bin/kodo logs --help` imprime help con los 6 flags: `-f, --follow`, `-l, --level`, `-c, --component`, `-e, --event-type`, `--json`, `--session-of`.
    - `node bin/kodo --help` sigue mostrando los 9 sub-comandos anteriores + `logs`.
    - Suite completa: `node --test` exits 0 (no rompe nada previo).
  </acceptance_criteria>
  <done>Sub-comando `logs` registrado, `--help` lo muestra, suite completa verde, smoke manual: `node bin/kodo logs inexistente-sess` imprime `No log file at ...` y exit 1.</done>
</task>

</tasks>

<verification>
```bash
# Help del sub-comando
node bin/kodo logs --help

# Help general (debe mostrar `logs` entre los sub-comandos)
node bin/kodo --help

# Smoke — session inexistente
node bin/kodo logs inexistente-sess-999; echo "exit=$?"

# Smoke con fixture golden como archivo real
mkdir -p ~/.kodo/logs && cp test/fixtures/events-golden.ndjson ~/.kodo/logs/sess-golden-01.ndjson
node bin/kodo logs sess-golden-01 --json | wc -l  # debe ser 7
node bin/kodo logs sess-golden-01 --level warn --json | wc -l  # debe ser 1 (solo orchestrator.review con level warn)
node bin/kodo logs sess-golden-01 --event-type session.start session.end --json | wc -l  # debe ser 2
# Cleanup
rm ~/.kodo/logs/sess-golden-01.ndjson

# Suite completa
node --test
```
</verification>

<success_criteria>
- `kodo logs --help` imprime los 6 flags + el positional `[session-id]`.
- `kodo logs <id>` funciona end-to-end contra un log real en `~/.kodo/logs/`.
- Filtros `--level`, `--component`, `--event-type`, `--json` operativos.
- `--session-of <task-id>` resuelve correctamente vía state.json o head-line scan.
- Suite completa verde.
</success_criteria>

<output>
After completion, create `.planning/phases/07-kodo-logs-cli-event-taxonomy/07-05-SUMMARY.md`.
</output>
