---
phase: 78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/manager.js
  - src/host/cmux.js
  - test/session/group-resolve.test.js
  - test/manager.test.js
autonomous: true
requirements: ["77/WR-01", "77/WR-02", "77/IN-01", "77/IN-02", "77/IN-03", "77/IN-04", "77/IN-05", "77/IN-06"]

must_haves:
  truths:
    - "`deriveExpectedGroupName` deriva sobre el ref TRIMEADO y devuelve `null` para refs que colapsan a identifier vacío: `'#7'`→null, `'-9'`→null; y `'KODO-9 '` (whitespace de borde) → match limpio `KODO` en vez de perder el grupo (WR-01+IN-01)."
    - "`resolveWorkspaceGroup` solo devuelve `g.ref` cuando cumple `/^workspace_group:\\d+$/` — un ref anómalo de cmux (shape distinto, con `\\n` u otro carácter) → null (IN-02, defensa contra forja de líneas de log)."
    - "`resolveWorkspaceGroup` matchea un `name` en NFD (`ç` = `c`+U+0327) contra un `expectedName` en NFC → devuelve el ref; borrar `.normalize('NFC')` pone el test rojo (WR-02, red de regresión del invariante Unicode)."
    - "En `launchWorkItem`, `host._legacy.listWorkspaceGroups()` NO se ejecuta cuando `expectedName` es null — la llamada cmux (garantizada-inútil) queda tras el guard `if (expectedName)` (IN-04)."
    - "El log de degradación de la resolución de grupo incluye el motivo del error (`String(err?.message).slice(0,80)`) sin filtrar contenido de usuario: el mensaje viene de cmux/JSON.parse, nunca del título de tarea (IN-03, D-11 preservado)."
    - "El JSDoc de `_legacy.newWorkspace` (src/host/cmux.js) documenta `group?: string` en `opts` (IN-06, coherencia doc↔código: el passthrough ya recibe `group`)."
    - "El fixture live (Kodo/SCRIBBA/SCP-CMRi) sigue resolviendo `KODO-9`→`workspace_group:1`; los 19 tests existentes de group-resolve.test.js y los source-hygiene de manager.test.js:786-855 siguen verdes (fail-open GRP-03 y GRP-04 intactos)."
  prohibitions:
    - statement: "No re-cablear el retry TOCTOU de la capa 2 (`newWorkspaceWithGroupFallback`) — D-10 LOCKED, IN-07 es riesgo residual conocido fuera de scope."
      status: resolved
      verification: "git diff de src/session/manager.js no toca `newWorkspaceWithGroupFallback` (líneas 209-217)"
    - statement: "Ningún verbo de gestión de grupos cmux (create/rename/delete/add/ungroup) — solo `workspace-group list` es admisible (GRP-04)."
      status: resolved
      verification: "node --test test/manager.test.js (guard GRP-04 existente en :829 sigue verde)"
    - statement: "`src/session/manager.js` no importa `src/cmux/client.js` directo — todo lo cmux-específico va por `host._legacy` (walker cmux-isolation)."
      status: resolved
      verification: "grep: manager.js no contiene `from '../cmux/client.js'`"
  backstop:
    - statement: "El assert GRP-04 de test/manager.test.js falla si un futuro reorden de funciones rompe el slice: `end > start` verificado ANTES de usar el `body` (si no, el regex negativo pasaría vacuo con guard apagado)."
      verification: backstop
  artifacts:
    - src/session/manager.js
    - src/host/cmux.js
    - test/session/group-resolve.test.js
    - test/manager.test.js
  key_links:
    - "deriveExpectedGroupName → resolveWorkspaceGroup → newWorkspaceWithGroupFallback (cadena fail-open GRP-03 intacta: un null en cualquier eslabón lanza la sesión SIN --group)"
    - "launchWorkItem → host._legacy.listWorkspaceGroups (única llamada cmux extra por lanzamiento, ahora guardada por expectedName)"
---

<objective>
Saldar los 8 hallazgos accionables de 77-REVIEW sobre la resolución de grupos cmux (0 critical · 2 warning · 6 info). Todos son deuda menor verificada como «NO gap» (el fail-open queda intacto), pero con fixes concretos ya escritos en el review. Endurecen las funciones puras de derivación/resolución y su cableado en `launchWorkItem`, más la red de regresión de tests. IN-07 (retry D-10) queda explícitamente FUERA (LOCKED).

Purpose: Eliminar tres modos de fallo silencioso — (a) un identifier bogus `''` que matchea un grupo whitespace-only y aterriza la tarea en grupo arbitrario; (b) un ref anómalo de cmux que forja líneas de log; (c) pérdida de grupo por whitespace de borde en el ref — más trabajo cmux inútil cuando no hay grupo esperado, un log de degradación sin causa diagnosticable, un JSDoc desalineado y dos asserts de test frágiles.
Output: `manager.js` endurecido (WR-01/IN-01/IN-02/IN-04/IN-03), JSDoc alineado (IN-06), regresión Unicode + guard de slice (WR-02/IN-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review/78-RESEARCH.md
@.planning/phases/78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review/78-PATTERNS.md
</context>

## Artifacts this phase produces

Este plan MODIFICA símbolos existentes (fase de deuda técnica — cero símbolos nuevos):

- `deriveExpectedGroupName` (`src/session/manager.js`) — deriva sobre el ref trimeado (IN-01) y añade guard del identifier derivado vacío → null (WR-01). Firma y contrato de retorno sin cambios.
- `resolveWorkspaceGroup` (`src/session/manager.js`) — añade el predicado de shape `/^workspace_group:\d+$/` al type-check por campo (IN-02).
- Bloque de resolución de grupo en `launchWorkItem` (`src/session/manager.js`:388-396) — guard `if (expectedName)` sobre la llamada cmux (IN-04) y `catch (err)` con motivo acotado en el log (IN-03).
- JSDoc de `_legacy.newWorkspace` (`src/host/cmux.js`:358) — añade `group?: string` a `opts` (IN-06).
- Casos de test nuevos en `test/session/group-resolve.test.js` (WR-01/IN-01/IN-02/WR-02) y un assert de robustez en `test/manager.test.js` (IN-05 + source-hygiene IN-04).

No se crean ficheros, funciones, flags CLI ni campos de struct nuevos. `newWorkspaceWithGroupFallback` (D-10) NO se toca.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Endurecer las funciones puras — deriveExpectedGroupName (WR-01+IN-01) y resolveWorkspaceGroup (IN-02+WR-02)</name>
  <files>src/session/manager.js, test/session/group-resolve.test.js</files>
  <read_first>
    - src/session/manager.js (líneas 143-162: `deriveExpectedGroupName`, guarda de entrada actual en :146 y derivación sobre ref crudo en :151-153; líneas 178-190: `resolveWorkspaceGroup`, `norm` con `.normalize('NFC')` en :179 y type-check por campo en :185)
    - test/session/group-resolve.test.js (líneas 20-66: imports + fixtureLive + `makeTask`; líneas 68-131: describe de `deriveExpectedGroupName` con el bloque de ref degenerado; líneas 132-190: describe de `resolveWorkspaceGroup`, casos de norm y empate — replicar el estilo de aserción)
    - .planning/phases/78-.../78-PATTERNS.md (líneas 76-119: fixes exactos WR-01/IN-01 e IN-02 con Pitfall 3 — el fixture live debe seguir resolviendo KODO-9→workspace_group:1)
  </read_first>
  <behavior>
    - `deriveExpectedGroupName(makeTask({ref:'#7'}), '/x', '/x')` → null (identifier derivado `''` tras `split('#')[0]`).
    - `deriveExpectedGroupName(makeTask({ref:'-9'}), '/x', '/x')` → null (identifier derivado `''` tras `replace(/-\d+$/,'')`).
    - `deriveExpectedGroupName(makeTask({ref:'KODO-9 '}), '/klab/kodo', '/klab/kodo')` → `'KODO'` (trim del ref antes de derivar; hoy pierde el grupo).
    - `deriveExpectedGroupName(makeTask({ref:'KODO-9'}), '/klab/kodo', '/klab/kodo')` → `'KODO'` (no-regresión del caso limpio).
    - `resolveWorkspaceGroup({groups:[{name:'K', ref:'grupo-malo'}]}, 'K')` → null (ref no cumple `workspace_group:\d+`).
    - `resolveWorkspaceGroup({groups:[{name:'K', ref:'workspace_group:5\ninject'}]}, 'K')` → null (ref con `\n` rechazado).
    - `resolveWorkspaceGroup` con name en NFD (`Traça` = `Traça`) y expected en NFC `'Traça'` → devuelve el ref del grupo.
    - `resolveWorkspaceGroup(fixtureLive, 'KODO')` → `'workspace_group:1'` (no-regresión del fixture live).
  </behavior>
  <action>
    Primero AÑADIR los casos de regresión a test/session/group-resolve.test.js (RED), luego aplicar los fixes a src/session/manager.js (GREEN):

    Tests (group-resolve.test.js):
    1. En el describe de `deriveExpectedGroupName`, añadir casos: `ref:'#7'`→null, `ref:'-9'`→null (identifier derivado colapsa a vacío), y `ref:'KODO-9 '` (whitespace de borde) → `'KODO'`. Reutilizar `makeTask` y el estilo `assert.equal`.
    2. En el describe de `resolveWorkspaceGroup`, añadir: un grupo cuyo `g.ref` NO case `/^workspace_group:\d+$/` (p.ej. `'grupo-malo'`, y otro con `\n` embebido) → null; y un caso WR-02 Unicode: un grupo con `name` en forma NFD (`c` + U+0327 combinando cedilla) matcheando un `expectedName` en NFC → devuelve el ref. Dientes WR-02: documentar en el test que borrar `.normalize('NFC')` de manager.js:179 lo pone rojo.

    Fixes (manager.js):
    3. `deriveExpectedGroupName` (fusionar WR-01+IN-01, un solo cambio cohesivo): derivar el ref con `const ref = String(task?.ref ?? '').trim();` y `if (ref === '') return null;` (IN-01: trim primero); tras derivar `identifier` con el `split('#')`/`replace(/-\d+$/,'')` existente sobre ESE ref trimeado, añadir `if (!identifier || identifier.trim() === '') return null;` (WR-01: identifier derivado vacío → fail-open null). Conservar `deriveModuleName`, la lógica `isFlat`/`usesModulePath` y el return compuesto tal cual.
    4. `resolveWorkspaceGroup` (IN-02): añadir el predicado `/^workspace_group:\d+$/.test(g.ref)` al type-check por campo existente (la condición del `if` dentro del `for`), sin tocar `norm`, el early-return de shape ni el first-match-wins.

    Pitfall 3: verificar que el fixture live (`Kodo`/`SCRIBBA`/`SCP-CMRi`) sigue resolviendo `KODO-9`→`workspace_group:1` y que los casos legítimos cortos no caen en el guard nuevo. NO tocar `deriveModuleName` ni `newWorkspaceWithGroupFallback`.
  </action>
  <verify>
    <automated>node --test test/session/group-resolve.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/session/group-resolve.test.js contiene casos `ref:'#7'`→null, `ref:'-9'`→null, `ref:'KODO-9 '`→`'KODO'`, un ref que no cumple `workspace_group:\d+`→null, y un caso NFD↔NFC que resuelve el ref.
    - src/session/manager.js `deriveExpectedGroupName` deriva sobre `String(task?.ref ?? '').trim()` y devuelve null cuando el identifier derivado es vacío.
    - src/session/manager.js `resolveWorkspaceGroup` incluye `/^workspace_group:\d+$/` en el type-check por campo.
    - `node --test test/session/group-resolve.test.js` sale 0; los 19 tests preexistentes (incl. el fixture live y el empate D-03) siguen verdes.
  </acceptance_criteria>
  <done>Las funciones puras rechazan refs degenerados y anómalos, respetan el trim y el invariante Unicode tiene red de regresión; fixture live intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear el guard IN-04 + motivo IN-03 en launchWorkItem, JSDoc IN-06 y robustez de test IN-05</name>
  <files>src/session/manager.js, src/host/cmux.js, test/manager.test.js</files>
  <read_first>
    - src/session/manager.js (líneas 380-408: bloque de resolución de grupo en `launchWorkItem` — el try en :390-396 con `expectedName`, `listWorkspaceGroups()`, `resolveWorkspaceGroup(JSON.parse(raw), ...)` y el `catch { console.log('...resolucion_fallo') }`)
    - src/host/cmux.js (líneas 357-361: JSDoc actual de `_legacy.newWorkspace`, hoy `{ name, cwd?, command? }` sin `group`)
    - test/manager.test.js (líneas 786-810: source-hygiene existente que grepa `listWorkspaceGroups()`, `resolveWorkspaceGroup(JSON.parse(raw), expectedName)` y `group_skipped — resolucion_fallo` — los fixes deben preservar esos regexes; líneas 844-857: el assert GRP-04 con el slice frágil)
    - .planning/phases/78-.../78-PATTERNS.md (líneas 121-153: fixes exactos IN-04/IN-03/IN-06; líneas 169-182: fix IN-05 con Pitfall 4)
  </read_first>
  <action>
    Aplicar cuatro fixes quirúrgicos, ninguno cambia comportamiento observable para inputs limpios:

    1. IN-04 (manager.js, bloque `launchWorkItem`): dentro del try existente, envolver la llamada cmux en un guard — derivar `const expectedName = deriveExpectedGroupName(task, entry, projectPath);` y solo si `expectedName` es truthy ejecutar `const raw = await host._legacy.listWorkspaceGroups();` y `groupRef = resolveWorkspaceGroup(JSON.parse(raw), expectedName);`. Cuando `expectedName` es null, `groupRef` queda null sin llamar a cmux (evita ~50ms/timeout garantizado-inútil). La llamada a `listWorkspaceGroups()` sigue existiendo textualmente dentro del try (preserva el regex de manager.test.js:790).
    2. IN-03 (mismo bloque): cambiar el `catch {` a `catch (err) {` y el log a incluir el motivo acotado: el mensaje debe conservar la subcadena literal `group_skipped — resolucion_fallo` (para no romper el regex de manager.test.js:809) y añadir a continuación `String(err?.message).slice(0,80)`. D-11 preservado: el mensaje viene de cmux/JSON.parse, nunca del título de tarea. NO tocar `newWorkspaceWithGroupFallback` (D-10 LOCKED).
    3. IN-06 (src/host/cmux.js:358): actualizar el JSDoc de `_legacy.newWorkspace` para incluir `group?: string` en el shape de `opts` (`{ name: string, cwd?: string, command?: string, group?: string }`), coherente con que el passthrough ya recibe `group` vía `newWorkspaceWithGroupFallback`.
    4. IN-05 (test/manager.test.js, assert GRP-04 ~:844-857): añadir `assert.ok(end > start, 'resolveProjectPath debe seguir a buildSessionFromTask (delimitador del slice)')` INMEDIATAMENTE tras calcular `end` y ANTES de `const body = source.slice(start, end)`. Añadir también un assert de source-hygiene para IN-04: verificar que la llamada `listWorkspaceGroups()` en manager.js está dentro de un guard `if (expectedName)` (regex sobre el source de manager.js que exija el patrón `if (expectedName)` envolviendo la llamada).

    Pitfall 4: el assert `end > start` debe ir ANTES de usar `body`, y ponerse rojo si el orden de funciones se rompe. NO modificar los regexes existentes de manager.test.js:786-810 (deben seguir matcheando).
  </action>
  <verify>
    <automated>node --test test/manager.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/session/manager.js: `listWorkspaceGroups()` y `resolveWorkspaceGroup(...)` están dentro de `if (expectedName) { ... }`; el `catch (err)` loguea `group_skipped — resolucion_fallo` seguido de `String(err?.message).slice(0,80)`.
    - src/host/cmux.js:358 JSDoc de `newWorkspace` incluye `group?: string`.
    - test/manager.test.js contiene `assert.ok(end > start, ...)` antes del `source.slice(start, end)` del test GRP-04, y un assert que exige el guard `if (expectedName)` sobre la llamada `listWorkspaceGroups()`.
    - `node --test test/manager.test.js` sale 0; los source-hygiene existentes (:786-810, :829, :844) siguen verdes.
    - `newWorkspaceWithGroupFallback` (manager.js:209-217) no aparece en el diff.
  </acceptance_criteria>
  <done>El guard IN-04 evita la llamada cmux inútil, el log IN-03 diagnostica la causa, el JSDoc IN-06 está alineado y el assert IN-05 protege el slice — sin romper los source-hygiene existentes ni D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de `cmux workspace-group list --json` → log de kodo / routing de grupo | El JSON de cmux (`g.name`, `g.ref`) es entrada externa; un shape anómalo puede forjar líneas de log o enrutar a un grupo arbitrario. |
| `task.ref` (provider) → identifier de grupo | Un ref degenerado colapsa a identifier vacío que podría matchear un grupo whitespace-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-02 | Tampering (log injection) | `resolveWorkspaceGroup` (src/session/manager.js:178) | low | mitigate | Guard `/^workspace_group:\d+$/` sobre `g.ref` (Task 1, IN-02) — un ref con `\n` u otro shape queda rechazado antes de devolverse/loguearse. |
| T-78-03 | Tampering (routing) | `deriveExpectedGroupName` (src/session/manager.js:143) | low | mitigate | Guard del identifier derivado vacío → null (Task 1, WR-01) + trim del ref (IN-01). Un ref bogus ya no produce `''` que matchee un grupo whitespace-only; fail-open a lanzar sin --group. |
| T-78-04 | Information disclosure | log de degradación en `launchWorkItem` (src/session/manager.js:395) | low | accept→mitigate | IN-03 añade `String(err?.message).slice(0,80)`; el mensaje viene de cmux/JSON.parse, nunca del título de tarea (D-11 preservado, verificado en RESEARCH §Security). |

Nota: cero instalaciones npm/pip/cargo en este plan → sin threat T-78-SC (package legitimacy) aplicable. IN-07 (retry D-10 duplica workspace ante timeout) es riesgo residual LOCKED, fuera de scope (RESEARCH §Out of Scope).
</threat_model>

<verification>
- `node --test test/session/group-resolve.test.js` en verde (WR-01/IN-01/IN-02/WR-02 + los 19 preexistentes).
- `node --test test/manager.test.js` en verde (IN-05 + source-hygiene IN-04 + los guards GRP-04 existentes).
- `git diff` de manager.js no toca `newWorkspaceWithGroupFallback` (D-10 LOCKED).
- `npm test` en verde antes del cierre (re-correr `test/gsd-lock-race.test.js` si falla — flaky preexistente, deferred Phase 74, NO regresión de esta fase).
</verification>

<success_criteria>
Los tres modos de fallo silencioso (identifier bogus, ref anómalo, whitespace de borde) quedan cerrados con fail-open explícito; la llamada cmux inútil se evita cuando no hay grupo esperado; el log de degradación es diagnosticable; el invariante Unicode y el slice GRP-04 tienen red de regresión; D-10 y GRP-04 intactos; cero dependencias nuevas.
</success_criteria>

<output>
Create `.planning/phases/78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review/78-02-SUMMARY.md` when done
</output>
