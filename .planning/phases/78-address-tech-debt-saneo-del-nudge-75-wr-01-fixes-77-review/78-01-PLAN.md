---
phase: 78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/stop.js
  - test/stop.test.js
autonomous: true
requirements: ["75/WR-01"]

must_haves:
  truths:
    - "El nudge del orquestador emite `next`, `summary` y `task_ref` pasados por `stripControlChars` — ninguna secuencia de escape de terminal (CSI, OSC, C0/C1, DEL, CR) sobrevive en el texto que llega a `cmuxClient.send`."
    - "Para inputs limpios (ASCII sin bytes de control) el texto del nudge queda byte-idéntico al de la Phase 75 (D-09) — los 5 goldens por-modo de test/stop.test.js (QUICK-08 quick/full/full-bootstrap/legacy/no-GSD) no cambian."
    - "`buildStopNudgeText` sigue siendo una función pura (cero I/O) — el test de pureza `LIVE-07: buildStopNudgeText permanece PURA` de test/stop.test.js sigue verde."
    - "Los tres modos (quick/full/no-GSD) siguen añadiendo la línea del `NEXT:` cuando `next` es string no vacío, y siguen degradando byte-idéntico cuando `next` es null/''/undefined/no-string (LIVE-07 intacto)."
  prohibitions:
    - statement: "Cero nuevas dependencias npm — el único import añadido es `stripControlChars` desde el módulo interno `src/cli/format.js`."
      status: resolved
      verification: "grep de package.json sin cambios; import relativo `../cli/format.js` (no un paquete)"
    - statement: "`buildStopNudgeText` nunca lee state.json ni hace I/O — el saneo solo aplica un helper puro sobre valores ya en memoria."
      status: resolved
      verification: "node --test test/stop.test.js (test de pureza: cuerpo sin readFileSync/writeFileSync/readFileFn)"
    - statement: "No se toca `src/hooks/session-end.js` — se elige la Opción 1 (sanear en el punto de composición), así el threading de `handoffNext` queda intacto."
      status: resolved
      verification: "git diff no lista src/hooks/session-end.js"
  artifacts:
    - src/hooks/stop.js
    - test/stop.test.js
  key_links:
    - "buildStopNudgeText → stripControlChars (import desde src/cli/format.js, mismo patrón que src/cli/dashboard/App.js:752-753 y markdown.js)"
---

<objective>
Cerrar la asimetría de saneo del nudge del orquestador (hallazgo 75/WR-01, riesgo aceptado R-75-02): el carril de render del dashboard ya blinda el contenido LLM con `stripControlChars` (App.js:752-753), pero el carril del nudge lo emite crudo hacia el terminal del orquestador. Este plan aplica el MISMO saneo a los tres campos LLM (`next`, `summary`, `task_ref`) en el único punto donde se interpolan: `buildStopNudgeText` (Opción 1 de RESEARCH §Scope A — mantiene la pureza de la función).

Purpose: Neutralizar la inyección de secuencias de escape de terminal (OSC-52 = escritura al portapapeles, CSI de reposicionamiento) desde contenido no confiable (LLM / state.json hand-editable) que cruza al terminal del orquestador. Cierra R-75-02 en un solo diff.
Output: `buildStopNudgeText` sanea los 3 campos; regresión que prueba el strip con dientes y la no-regresión byte-idéntica (D-09).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review/78-RESEARCH.md
@.planning/phases/78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review/78-PATTERNS.md
</context>

## Artifacts this phase produces

Este plan MODIFICA símbolos existentes (fase de deuda técnica — cero símbolos nuevos):

- `buildStopNudgeText` (`src/hooks/stop.js`) — ahora sanea `task_ref`, `summary` y `next` con `stripControlChars` en el punto de interpolación. Firma, exportación y contrato LIVE-07 sin cambios.
- Import nuevo en `src/hooks/stop.js`: `stripControlChars` desde `../cli/format.js` (símbolo ya existente, no se crea).
- Casos de test nuevos en `test/stop.test.js` dentro del describe `QUICK-08 — buildStopNudgeText switch`.

No se crean ficheros, funciones, flags CLI ni campos de struct nuevos.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regresión RED — el nudge sanea los 3 campos LLM y sigue byte-idéntico para inputs limpios</name>
  <files>test/stop.test.js</files>
  <read_first>
    - test/stop.test.js (líneas 204-343: describe `QUICK-08 — buildStopNudgeText switch`, fixture `makeQuickSession`, goldens por-modo y bloque LIVE-07 — replicar el estilo de aserción y la carga dinámica POST-HOME de `buildStopNudgeText`)
    - src/hooks/stop.js (líneas 48-73: `buildStopNudgeText` — los 3 campos que se interpolan: `session.task_ref`, `session.summary`, `next`)
    - src/cli/format.js (líneas 80-87: `stripControlChars` — qué elimina: CSI completas, C0 incl. ESC/BEL/CR, DEL, C1 `\x80-\x9f`; qué PRESERVA: solo `\t` y `\n`)
  </read_first>
  <action>
    Añadir al describe existente `QUICK-08 — buildStopNudgeText switch` (test/stop.test.js) nuevos casos de regresión, usando `makeQuickSession` y la `buildStopNudgeText` ya cargada dinámicamente en el `before()` del bloque:

    1. Caso "sanea `task_ref` inyectado": construir una sesión con `task_ref` que contenga una secuencia CSI (p.ej. `\x1b[31m`) y un byte C0 (`\x07` BEL). Assert que el texto devuelto NO contiene `\x1b`, NO contiene `\x07`, y NO contiene la subcadena `[31m` (el CSI completo desapareció). El identifier visible (parte ASCII limpia del ref) SÍ permanece.
    2. Caso "sanea `summary` inyectado": análogo con un `summary` que lleve un OSC-52 simulado (`\x1b]52;c;...\x07`) y un CR (`\x0d`). Assert ausencia de `\x1b`, `\x07`, `\x0d` en el output.
    3. Caso "sanea `next` inyectado": pasar como 2º argumento un `next` con CSI + C1 (`\x9b`, `\x9d`). Assert que el output no contiene ninguno de esos bytes de control, pero SÍ la parte textual limpia del next.
    4. Caso "no-regresión D-09 con inputs limpios": para los tres modos (quick/full/no-GSD) con `makeQuickSession` (task_ref `KL-42`, summary `Quick task` — ASCII limpio), assert que el texto es EXACTAMENTE igual antes y después (comparar contra el literal esperado actual o contra un snapshot capturado en el propio test). El saneo debe ser la identidad sobre ASCII limpio.

    Teeth: estos casos deben ponerse ROJOS con la implementación actual (los bytes de control sobreviven hoy). NO modificar los 5 goldens por-modo existentes ni el bloque LIVE-07 — deben seguir pasando sin tocarlos.
  </action>
  <verify>
    <automated>node --test test/stop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/stop.test.js contiene al menos 3 casos nuevos que inyectan bytes de control en `task_ref`, `summary` y `next` y afirman su ausencia en el output de `buildStopNudgeText`.
    - Con la implementación actual (sin el fix de Task 2) esos casos nuevos FALLAN (`node --test test/stop.test.js` sale con código != 0 por los casos de saneo) — dientes verificados.
    - Los 5 tests golden por-modo (QUICK-08 quick/full/full-bootstrap/legacy/no-GSD) y los 4 tests LIVE-07 NO se modifican y siguen pasando.
  </acceptance_criteria>
  <done>Los casos de regresión del saneo existen y están rojos sin el fix; los goldens y LIVE-07 intactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — sanear los 3 campos LLM en buildStopNudgeText (Opción 1, mantiene pureza)</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    - src/hooks/stop.js (líneas 12-16: bloque de imports; líneas 48-73: cuerpo de `buildStopNudgeText`)
    - src/cli/format.js (líneas 80-87: `stripControlChars`, pura y never-throws — el helper a reutilizar)
    - src/cli/dashboard/App.js (líneas 752-753: patrón EXACTO a replicar — el carril de render ya sanea `next` con `stripControlChars`)
    - test/stop.test.js (líneas 334-343: test de pureza que grepa el cuerpo de `buildStopNudgeText` por readFileSync/writeFileSync/readFileFn — el fix no debe introducir ninguno)
  </read_first>
  <behavior>
    - `buildStopNudgeText(session, next)` con `session.task_ref = 'A\x1b[31mB'` → el output no contiene `\x1b` ni `[31m`, sí contiene `AB` en la frase base.
    - `buildStopNudgeText(session, next)` con `session.summary = 'x\x07y'` → el output no contiene `\x07`, sí `xy`.
    - `buildStopNudgeText(session, 'paso\x9bmalo')` → el output no contiene `\x9b`, sí el texto limpio del next.
    - `buildStopNudgeText(makeQuickSession({gsd:false}))` (input limpio) → byte-idéntico al output actual (D-09).
    - El cuerpo de la función no contiene readFileSync/writeFileSync (pureza).
  </behavior>
  <action>
    Aplicar la Opción 1 de RESEARCH §Scope A (sanear en el único punto de composición, no en cada sink):

    1. Añadir el import `import { stripControlChars } from '../cli/format.js';` junto a los imports existentes de `src/hooks/stop.js` (ruta relativa correcta: stop.js está en src/hooks/, format.js en src/cli/). Este import NO viola `format-isolation` — el walker solo prohíbe importar `picocolors` directo, no `stripControlChars`; es el mismo patrón que ya usan App.js y markdown.js.
    2. En la construcción de `const base`, envolver los dos campos interpolados: usar `stripControlChars(session.task_ref)` y `stripControlChars(session.summary)` en lugar de `session.task_ref` y `session.summary` crudos.
    3. En la rama del `next` (dentro del guard existente `typeof next === 'string' && next.length > 0`), interpolar `stripControlChars(next)` en lugar de `next` crudo. Mantener el guard tal cual — sólo se envuelve el valor.

    NO cambiar la firma, el switch por-modo, los textos base, ni el escape literal `\\n`. NO tocar `src/hooks/session-end.js` (el threading de `handoffNext` sigue igual). `stripControlChars` es pura → la función SIGUE pura (el test de pureza sigue verde). Sobre los valores ASCII limpios de los goldens, `stripControlChars` es la identidad → D-09 preservado.
  </action>
  <verify>
    <automated>node --test test/stop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/stop.js importa `stripControlChars` desde `../cli/format.js`.
    - En `buildStopNudgeText`, `session.task_ref`, `session.summary` y `next` se interpolan a través de `stripControlChars(...)`.
    - `node --test test/stop.test.js` sale 0: los casos de saneo de Task 1 pasan (verdes), los 5 goldens por-modo y los 4 LIVE-07 siguen verdes (byte-idénticos), y el test de pureza sigue verde.
    - `git diff --name-only` NO lista `src/hooks/session-end.js`.
  </acceptance_criteria>
  <done>Los 3 campos LLM del nudge están saneados; suite stop.test.js verde; goldens byte-idénticos; función pura.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenido LLM / state.json (hand-editable) → terminal del orquestador | `next`/`summary`/`task_ref` cruzan de datos no confiables al sink `cmuxClient.send`, que el terminal del orquestador interpreta (OSC/CSI). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-01 | Tampering | `buildStopNudgeText` (src/hooks/stop.js) → `cmuxClient.send` (session-end.js:254) | medium | mitigate | Aplicar `stripControlChars` a `next`/`summary`/`task_ref` en el punto de composición (Task 2). Neutraliza CSI/OSC/C0/C1/DEL. Simetría con el carril de render ya cerrado (App.js:752-753). Severidad medium: el sink es un terminal local del propio operador, no un canal remoto (RESEARCH §Security Domain). |

Nota: cero instalaciones npm/pip/cargo en este plan → sin threat T-78-SC (package legitimacy) aplicable.
</threat_model>

<verification>
- `node --test test/stop.test.js` en verde (saneo + goldens + pureza + LIVE-07).
- `git diff --name-only` no incluye `src/hooks/session-end.js` ni `package.json`.
- Los 5 goldens por-modo permanecen byte-idénticos (D-09).
</verification>

<success_criteria>
El nudge del orquestador ya no puede transportar secuencias de escape de terminal desde los 3 campos LLM; el comportamiento observable para inputs limpios es idéntico al de la Phase 75; `buildStopNudgeText` sigue pura; cero dependencias nuevas.
</success_criteria>

<output>
Create `.planning/phases/78-address-tech-debt-saneo-del-nudge-75-wr-01-fixes-77-review/78-01-SUMMARY.md` when done
</output>
