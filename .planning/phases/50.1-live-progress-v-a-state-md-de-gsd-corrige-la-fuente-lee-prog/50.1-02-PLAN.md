---
phase: 50.1-live-progress-via-gsd-state
plan: 02
type: execute
wave: 2
depends_on: ["50.1-01"]
files_modified:
  - src/cli/dashboard/App.js
autonomous: true
requirements: [PROG-03]
must_haves:
  truths:
    - "DG-03: solo sesiones con row.gsd === true se enriquecen con progreso; no-GSD → {status:'no-progress'} → '—'"
    - "DG-04: el enrich deriva la ruta con computeRealWorktreePath(project_path, session_id), NUNCA confía en row.worktree_path persistido (apunta a .bg-shell incorrecto)"
    - "DG-05: la columna prog, los 4 estados (progCell), la condicionalidad (deriveAnyProgress) y cero-color se reusan INTACTOS — solo cambia la fuente del enrich"
    - "DG-06: enrich CLIENT-SIDE en App.js, CERO cambios en src/server.js, cero endpoints, cero deps"
    - "DG-07: keep-last-good keyed por session_id; fallo transiente con last-good → N/M, sin él → '?'"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "enrich repuntado a readGsdProgress + computeRealWorktreePath, gate row.gsd, keep-last-good por session_id"
      contains: "readGsdProgress"
  key_links:
    - from: "src/cli/dashboard/App.js"
      to: "computeRealWorktreePath + readGsdProgress"
      via: "enrich client-side gateado por row.gsd === true"
      pattern: "computeRealWorktreePath"
    - from: "src/cli/dashboard/App.js"
      to: "progCell/deriveAnyProgress"
      via: "session.progress consumido por la columna prog (reuso intacto)"
      pattern: "deriveAnyProgress"
---

<objective>
Repunta la fuente del display de progreso al lector GSD del Plan 01: el enrich client-side de
`App.js` ahora gateado por `row.gsd === true`, deriva la ruta real con `computeRealWorktreePath`
y lee `readGsdProgress`. Reusa intactos `progCell`, `deriveAnyProgress`, la columna `prog`, el
cero-color y el keep-last-good (re-keyed a `session_id`).

Purpose: cerrar PROG-03 con la fuente correcta. El display de Phase 50 era trabajo bueno; lo único
equivocado era de dónde leía. Cero server.js, cero deps (DG-06).
Output: una sola región del enrich de App.js repuntada; display completo reusado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-CONTEXT.md
@.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-RESEARCH.md
@.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos del codebase + Plan 01. El executor usa estos directamente. -->

readGsdProgress (creado en Plan 01 — src/cli/dashboard/progress.js):
```javascript
export function readGsdProgress(worktreeBase, deps = {}); // → {status:'ok',n,m,completed}|{status:'no-progress'}|{status:'error'}
```

computeRealWorktreePath (creado en Plan 01 — src/session/state.js):
```javascript
export function computeRealWorktreePath(projectPath, sessionId); // → join(projectPath,'.claude','worktrees',sessionId)
```

Enrich ACTUAL a repuntar (src/cli/dashboard/App.js:346-366) — el delta vive AQUÍ:
```javascript
const lastGood = progressLastGoodRef.current;
const enriched = sorted.map((row) => {
  const taskId = row?.task_id;
  const usable = taskId && !taskId.includes('/') && !taskId.includes('\\') && !taskId.includes('..');
  if (!usable) return { ...row, progress: { status: 'no-progress' } };
  const res = readProgress(taskId, {});            // ← cambia a readGsdProgress(base, {})
  if (res.status === 'ok') { lastGood.set(taskId, {...}); return { ...row, progress: res }; }
  if (res.status === 'error') { const prev = lastGood.get(taskId); return {...}; }
  return { ...row, progress: res };
});
const anyProgress = deriveAnyProgress(enriched);   // ← REUSADO intacto
```

Import actual (App.js:71): `import { readProgress } from './progress.js';` → cambiar a `readGsdProgress`
computeRealWorktreePath se importa desde '../../session/state.js' (verificar la ruta relativa real desde App.js).

progCell (src/cli/dashboard/format.js:240-246) — REUSADO, consume {status,n,m,completed} idéntico.
deriveAnyProgress (src/cli/dashboard/select.js:236) — REUSADO sin cambios.
COLS.prog = 7 (SessionTable.js:58) — dejar en 7 (Pitfall 3: menor blast radius, no toca golden-bytes de test/dashboard-table.test.js).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repuntar el enrich de App.js a la fuente GSD (gate row.gsd + ruta real + keep-last-good por session_id)</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - src/cli/dashboard/App.js:71 (import readProgress) y :346-366 (bloque enrich a modificar)
    - RESEARCH.md "Repunte del enrich en App.js" (delta mínimo) + Pitfall 1 (no confiar en row.worktree_path) + Pitfall 4 (cero server.js) + Open Question 3 (keep-last-good por session_id)
    - 50.1-01-SUMMARY.md (firmas exactas de readGsdProgress / computeRealWorktreePath y la ruta de import del helper)
  </read_first>
  <action>
    En src/cli/dashboard/App.js cambia el import de línea 71 a `import { readGsdProgress } from './progress.js';`
    y añade el import de `computeRealWorktreePath` desde el módulo de session/state.js (verifica la ruta
    relativa correcta desde App.js, p.ej. `'../../session/state.js'`).

    Reescribe el bloque enrich (~346-366) así (DG-03 + DG-04 + DG-07):
    - Gate DG-03: si `row?.gsd !== true` → `return { ...row, progress: { status: 'no-progress' } }` (no-GSD no contribuye; la columna lo tolera como '—').
    - Para filas GSD: lee `const projectPath = row.project_path; const sessionId = row.session_id;`.
      NO uses `row.worktree_path` (Pitfall 1 — apunta a .bg-shell incorrecto).
    - Guard anti-traversal del sessionId ANTES de construir la ruta (defensa en profundidad, T-501-traversal):
      `sessionId.includes('/')`/`'\\'`/`'..'` (String.includes, NO regex). Falta projectPath/sessionId o
      no usable → `{ status: 'no-progress' }`.
    - `const base = computeRealWorktreePath(projectPath, sessionId); const res = readGsdProgress(base, {});`
    - Keep-last-good re-keyed a `session_id` (Open Question 3): el `progressLastGoodRef` Map ahora se keya por
      `sessionId` en vez de `taskId`. Conserva la lógica exacta: 'ok' → set + return res; 'error' → prev presente
      → return {status:'ok', ...prev}, ausente → return {status:'error'}; 'no-progress' → return res.

    Actualiza el comentario del bloque (líneas ~338-345) para reflejar la fuente STATE.md en vez de
    `~/.kodo/progress/<task_id>.json`. CERO cambios en src/server.js (Pitfall 4 — el `...s` ya pasa
    gsd/project_path/session_id al cliente). NO toques COLS.prog ni format.js/select.js/SessionTable.js
    (DG-05 reuso intacto).
  </action>
  <acceptance_criteria>
    - `grep -c "readGsdProgress" src/cli/dashboard/App.js` >= 1 y `grep -c "readProgress\b" src/cli/dashboard/App.js` == 0 (sin import muerto)
    - `grep -c "computeRealWorktreePath" src/cli/dashboard/App.js` >= 1
    - `grep -c "row.worktree_path\|worktree_path ??" src/cli/dashboard/App.js` no introduce uso nuevo para localizar el STATE.md (Pitfall 1)
    - `git diff --quiet src/server.js` == 0 (DG-06, acceptance DURO)
    - `git diff --quiet package.json` == 0 (cero deps, acceptance DURO)
    - `npm test` verde (suite completa, cero regresiones — incl. progCell/deriveAnyProgress/dashboard-table reusados intactos)
  </acceptance_criteria>
  <verify>
    <automated>git diff --quiet src/server.js && git diff --quiet package.json && [ "$(grep -c 'computeRealWorktreePath' src/cli/dashboard/App.js)" -ge 1 ] && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>El enrich de App.js lee la fuente GSD (STATE.md vía ruta real, gate row.gsd, keep-last-good por session_id); display reusado intacto; cero server.js, cero deps; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionRecord del cliente (`gsd`, `project_path`, `session_id`) → construcción de la ruta del STATE.md | input que viaja en `/status` cruza a un `join` de filesystem en el render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-501-traversal | Tampering | enrich App.js construye la ruta del worktree desde session_id | mitigate | Guard anti-traversal en el CALLER (este enrich) ANTES de `computeRealWorktreePath`/`readGsdProgress`: `sessionId.includes('/')`/`'\\'`/`'..'` (String.includes, NO regex — anti-ReDoS, mold plan.js:120-121). El session_id es UUID por construcción (manager.js); el guard es defensa en profundidad. Severidad high. |
| T-501-server | Tampering | tentación de enriquecer server-side | accept (prevent) | El enrich es client-side; `git diff --quiet src/server.js` == 0 es acceptance duro. No se abre superficie en el server. |
| T-501-render-dos | Denial of Service | lectura síncrona de N STATE.md por render | accept | Gate `row.gsd === true` limita la lectura a filas GSD; STATE.md es pequeño; mismo perfil que el legacy readProgress/readPlan (Pitfall 5). Memoización por mtime DEFERRED. |
</threat_model>

<verification>
- `npm test` verde (suite completa)
- `git diff --quiet src/server.js` == 0 (DG-06 duro)
- `git diff --quiet package.json` == 0 (cero deps duro)
- `grep -c "computeRealWorktreePath" src/cli/dashboard/App.js` >= 1 y sin `readProgress` muerto
- Manual (VALIDATION.md): abrir dashboard con sesión GSD viva → columna `prog` = completed_phases/total_phases, `✓` al completar
</verification>

<success_criteria>
- Enrich client-side repuntado a readGsdProgress vía computeRealWorktreePath, gateado por row.gsd (DG-03/DG-04)
- Display (progCell/deriveAnyProgress/columna prog/cero-color) reusado intacto (DG-05)
- Keep-last-good por session_id; estados degradados honestos (DG-07)
- CERO src/server.js, cero endpoints, cero deps (DG-06) — acceptance duros verdes
- Suite completa verde sin regresiones
</success_criteria>

<output>
Create `.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-02-SUMMARY.md` when done
</output>
