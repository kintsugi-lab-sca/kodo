---
phase: 50.1-live-progress-via-gsd-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/progress.js
  - src/session/state.js
  - src/hooks/task-progress.js
  - src/hooks/install.js
  - test/dashboard-progress.test.js
  - test/state.test.js
  - test/hooks/task-progress.test.js
  - test/hooks/task-progress-install.test.js
autonomous: true
requirements: [PROG-02]
must_haves:
  truths:
    - "DG-01: readGsdProgress deriva N/M = completed_phases/total_phases (fases, no tareas), con ✓ cuando completed_phases === total_phases"
    - "DG-02: la fuente es el bloque progress: del STATE.md del worktree GSD, parseado con mini-parser regex constante (sin dep YAML), NO ~/.claude/tasks/ ni ~/.kodo/progress/"
    - "DG-04: computeRealWorktreePath devuelve <projectPath>/.claude/worktrees/<sid> sin tocar computeWorktreePath"
    - "DG-07: STATE.md ausente/parcial → no-progress; corrupto/EACCES → error; never-throws"
    - "DG-08: task-progress.js eliminado + registro TaskCreated/TaskCompleted removido de install.js + tests del hook eliminados/reescritos"
  artifacts:
    - path: "src/cli/dashboard/progress.js"
      provides: "readGsdProgress + parseProgressBlock (mini-parser regex constante)"
      contains: "readGsdProgress"
    - path: "src/session/state.js"
      provides: "computeRealWorktreePath helper (aditivo, no toca computeWorktreePath)"
      contains: "computeRealWorktreePath"
    - path: "test/dashboard-progress.test.js"
      provides: "contrato readGsdProgress: ok/no-progress(ENOENT)/error(corrupto)/parcial, DI readFileFn"
      contains: "readGsdProgress"
  key_links:
    - from: "src/cli/dashboard/progress.js"
      to: "<worktree>/.planning/STATE.md"
      via: "readFileFn(join(worktreeBase, '.planning', 'STATE.md'))"
      pattern: "join.*planning.*STATE"
    - from: "src/hooks/install.js"
      to: "settings.json hooks"
      via: "addHook solo SessionStart/Stop (sin TaskCreated/TaskCompleted)"
      pattern: "addHook.*SessionStart"
---

<objective>
Corrige la FUENTE del progreso vivo: crea el lector `readGsdProgress` que lee el bloque
`progress:` del `STATE.md` del worktree GSD real, el helper `computeRealWorktreePath` para
localizarlo, y demota el hook de captura 50-02 (que leía la superficie equivocada
`~/.claude/tasks/`, vacía en sesiones GSD reales que usan `Agent`, no `TaskCreate`).

Purpose: Las sesiones GSD reales no usan `TaskCreate` → la columna `prog` de Phase 50 mostraría
siempre `—`. El dato canónico vive en el `progress:` del `STATE.md` que GSD mantiene por disk-scan.
Output: lector nuevo + helper de ruta + hook demotado + tests Wave 0 al nuevo contrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-CONTEXT.md
@.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-RESEARCH.md
@.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-VALIDATION.md

<interfaces>
<!-- Contratos extraídos del codebase. El executor usa estos directamente — sin exploración. -->

ProgressResult (forma que progCell/select.js/App.js consumen HOY, NO cambia — src/cli/dashboard/progress.js:22):
```javascript
/** @typedef {{ status: 'ok', n: number, m: number, completed: boolean } | { status: 'no-progress' } | { status: 'error' }} ProgressResult */
```

readProgress LEGACY a REEMPLAZAR (src/cli/dashboard/progress.js:44-58) — mold del nuevo lector:
- DI: `deps.readFileFn || ((p) => readFileSync(p, 'utf-8'))`
- try → 'ok'; catch ENOENT → 'no-progress'; catch otro → 'error' (never-throws)

readLightPlan (mold de robustez — src/cli/dashboard/plan.js:65-78):
- mismo patrón DI readFileFn, mismo mapeo ENOENT→ausente / otro→error

computeWorktreePath — NO TOCAR (src/session/state.js:153):
```javascript
export function computeWorktreePath(projectPath, sessionId) {
  return join(projectPath, '.bg-shell', sessionId); // 5 consumidores acoplados a .bg-shell
}
```

Bloque progress: real del STATE.md (verificado en .planning/STATE.md:9-14) — indentado a 2 espacios:
```
progress:
  total_phases: 5
  completed_phases: 3
  total_plans: 7
  completed_plans: 7
  percent: 60
```
Las keys son CONDICIONALES (el generador GSD añade cada una con `if (x !== null)`) → tolerar ausencia.

install.js registro a DEMOTAR (src/hooks/install.js):
- línea 19: `const taskProgressCmd = ...task-progress.js`
- líneas 42-43: `addHook(..., 'TaskCreated'/'TaskCompleted', taskProgressCmd)`
- línea 50: `console.log(... TaskCreated/TaskCompleted ...)`
- uninstall línea 74: el array de events incluye 'TaskCreated','TaskCompleted' — quitar (uninstall ya filtra por `.includes('kodo')`, mantenerlos es inerte pero se limpia por higiene)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — reescribir tests al contrato STATE.md + helper de ruta + demote del hook</name>
  <files>src/cli/dashboard/progress.js, src/session/state.js, src/hooks/task-progress.js, src/hooks/install.js, test/dashboard-progress.test.js, test/state.test.js, test/hooks/task-progress.test.js, test/hooks/task-progress-install.test.js</files>
  <read_first>
    - src/cli/dashboard/progress.js (readProgress legacy, mold) + test/dashboard-progress.test.js (contrato JSON actual a reescribir)
    - test/state.test.js:87-113 (analog: estructura del describe de computeWorktreePath)
    - test/hooks/task-progress-install.test.js:1-50 (cubre install/uninstall de SessionStart/Stop + Task* — reescribir para preservar la cobertura SessionStart/Stop SIN Task*)
    - RESEARCH.md "Wave 0 Gaps" + Pattern 2 (mini-parser) + Pitfall 2 (keys condicionales)
  </read_first>
  <behavior>
    - readGsdProgress: bloque progress: completo → {status:'ok', n:completed_phases, m:total_phases, completed: m>0 && n===m}
    - readGsdProgress: completed_phases===total_phases (p.ej. 5/5) → completed:true
    - readGsdProgress: STATE.md sin total_phases (parcial) → {status:'no-progress'}
    - readGsdProgress: bloque parcial con total_phases pero sin completed_phases → n=0, m=total_phases, ok
    - readGsdProgress: readFileFn lanza ENOENT → {status:'no-progress'}
    - readGsdProgress: readFileFn lanza EACCES / contenido corrupto sin frontmatter → {status:'error'} (no lanza)
    - computeRealWorktreePath('/proj','sid') → '/proj/.claude/worktrees/sid'
    - install/uninstall: SessionStart y Stop siguen registrándose/limpiándose; NINGÚN TaskCreated/TaskCompleted aparece tras installHooks
  </behavior>
  <action>
    REESCRIBE test/dashboard-progress.test.js del contrato JSON (`~/.kodo/progress`) al contrato
    STATE.md: importar `readGsdProgress` desde progress.js, DI `readFileFn` que devuelve markdown
    sintético (HOME-isolation pura, sin disco real — sigue el patrón DI del test actual). Cubre los
    6 behaviors de readGsdProgress de arriba. Usa fixtures de markdown con frontmatter `---\nprogress:\n  total_phases: 5\n  completed_phases: 3\n---` y variantes (parcial, completo 5/5, corrupto sin `---`).

    AÑADE a test/state.test.js un `describe('computeRealWorktreePath')` (mold del describe de
    computeWorktreePath:87) verificando `('/proj','sid') → '/proj/.claude/worktrees/sid'` y
    determinismo (mismo input → mismo output).

    ELIMINA test/hooks/task-progress.test.js. REESCRIBE test/hooks/task-progress-install.test.js →
    renómbralo conceptualmente a cobertura de install/uninstall de SessionStart/Stop: mantén los
    behaviors 2 (idempotente), 3 (no clobber de hooks ajenos), 4 (uninstall limpia kodo, preserva
    ajenos) PERO sobre SessionStart/Stop, y AÑADE un assert de que tras installHooks `settings.hooks`
    NO contiene clave `TaskCreated` ni `TaskCompleted`. Si prefieres, mueve estos asserts a un
    test/hooks/install.test.js nuevo y borra el de task-progress-install — Claude's Discretion, pero
    la cobertura de SessionStart/Stop NO debe quedar huérfana.

    ELIMINA src/hooks/task-progress.js. En src/hooks/install.js quita: la línea 19 (taskProgressCmd),
    las líneas 42-43 (addHook TaskCreated/TaskCompleted), la línea 50 (console.log Task*), y de la
    lista de events del uninstall (línea 74) quita 'TaskCreated','TaskCompleted'. NO toques los bloques
    de SessionStart/Stop (golden-bytes HOOK-02 de session-start.js — no se perturba ese archivo).

    Estos tests fallarán hasta que la Task 2 cree readGsdProgress y computeRealWorktreePath (RED).
  </action>
  <acceptance_criteria>
    - `node --test test/state.test.js` PASA (el describe de computeRealWorktreePath GREEN tras Task 2)
    - `node --test test/dashboard-progress.test.js` cubre los 6 behaviors (GREEN tras Task 2)
    - `node --test test/hooks/` PASA: SessionStart/Stop testeados, assert de ausencia de Task* verde
    - `test/hooks/task-progress.test.js` ya NO existe
    - `src/hooks/task-progress.js` ya NO existe
    - `grep -v '^[[:space:]]*//' src/hooks/install.js | grep -c 'TaskCreated\|TaskCompleted'` == 0
  </acceptance_criteria>
  <verify>
    <automated>test ! -f src/hooks/task-progress.js && test ! -f test/hooks/task-progress.test.js && [ "$(grep -v '^[[:space:]]*//' src/hooks/install.js | grep -c 'TaskCreated\|TaskCompleted')" = "0" ] && echo OK</automated>
  </verify>
  <done>Tests Wave 0 reescritos al contrato STATE.md (RED, esperando Task 2), helper testeado, hook 50-02 eliminado limpiamente, install/uninstall siguen cubriendo SessionStart/Stop sin Task*.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: readGsdProgress + parseProgressBlock + computeRealWorktreePath</name>
  <files>src/cli/dashboard/progress.js, src/session/state.js</files>
  <read_first>
    - src/cli/dashboard/progress.js (readProgress a reemplazar) + RESEARCH.md Pattern 1 (lector) y Pattern 2 (mini-parser anti-ReDoS)
    - src/session/state.js:153 (computeWorktreePath — analog de firma, NO tocar) + Code Examples del RESEARCH
    - Tests escritos en Task 1 (contrato a satisfacer)
  </read_first>
  <behavior>
    - parseProgressBlock(md): aísla el primer frontmatter (--- ... ---) con regex CONSTANTE; lee las keys de una allowlist literal fija (total_phases, completed_phases, total_plans, completed_plans) con regex constante por key; devuelve solo las keys encontradas; sin frontmatter → null
    - readGsdProgress(worktreeBase, deps): lee join(worktreeBase,'.planning','STATE.md') vía readFileFn; total_phases ausente → no-progress; presente → {status:'ok', n: completed_phases ?? 0, m: total_phases, completed: m>0 && n===m}; ENOENT → no-progress; otro error → error; never-throws
    - computeRealWorktreePath(projectPath, sessionId) → join(projectPath,'.claude','worktrees',sessionId)
  </behavior>
  <action>
    REEMPLAZA el cuerpo de src/cli/dashboard/progress.js: sustituye `readProgress` por
    `readGsdProgress(worktreeBase, deps = {})` con la forma del Code Example del RESEARCH (DI readFileFn,
    mapeo ENOENT→no-progress / otro→error, never-throws). Añade `parseProgressBlock(md)` como función
    module-local: regex de frontmatter CONSTANTE `/^---\r?\n([\s\S]*?)\r?\n---/`, y por cada key de la
    allowlist literal fija un `new RegExp(\`^\\\\s+${key}:\\\\s*(\\\\d+)\`, 'm')` (la key es literal de
    allowlist → NO es input externo → sin vector ReDoS, espejo del `/^\d+$/` constante de plan.js:131).
    N/M = FASES (DG-01): m=total_phases, n=completed_phases ?? 0, completed = m>0 && n===m. NO uses
    `percent` (semántica distinta, anti-pattern del RESEARCH). NO leas ~/.claude/tasks/ ni ~/.kodo/progress/.
    Actualiza el comentario de cabecera del archivo al nuevo contrato STATE.md.

    AÑADE a src/session/state.js (junto a computeWorktreePath:153, SIN tocarla) el helper exportado
    `computeRealWorktreePath(projectPath, sessionId)` que devuelve
    `join(projectPath, '.claude', 'worktrees', sessionId)`. JSDoc citando la convención confirmada
    empíricamente (TENDERIO-9 + git worktree list) y DG-04 (helper SEPARADO, blast radius mínimo).

    No cambies el nombre del export consumido por App.js todavía — App.js se repunta en el Plan 02.
    Exporta `readGsdProgress` (App.js Plan 02 cambiará su import).
  </action>
  <acceptance_criteria>
    - `node --test test/dashboard-progress.test.js` GREEN (6 behaviors)
    - `node --test test/state.test.js` GREEN (computeRealWorktreePath)
    - `grep -c "computeWorktreePath" src/session/state.js` muestra la función original intacta (firma .bg-shell sin cambios)
    - `grep -c "homedir\|\.kodo\|claude.*tasks" src/cli/dashboard/progress.js` == 0 (la fuente ya no es HOME-relative)
    - `git diff --quiet package.json` (cero deps)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dashboard-progress.test.js test/state.test.js && git diff --quiet package.json && grep -q "return join(projectPath, '.bg-shell'" src/session/state.js && echo OK</automated>
  </verify>
  <done>readGsdProgress lee el STATE.md real con mini-parser anti-ReDoS, computeRealWorktreePath resuelve la ruta real sin tocar computeWorktreePath, suite del lector + helper verde, cero deps nuevas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionRecord (`project_path`, `session_id`) → ruta del STATE.md | input persistido cruza a una construcción de path filesystem |
| Contenido del STATE.md → mini-parser | markdown del disco cruza a regex de extracción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-501-traversal | Tampering | construcción de la ruta del STATE.md desde session_id/project_path | mitigate | El guard anti-traversal (`session_id.includes('/')`/`'\\'`/`'..'`, String.includes NO regex) vive en el CALLER (App.js, Plan 02, mold plan.js:120-121). En este plan readGsdProgress recibe `worktreeBase` ya validado; la ruta se construye con `join(worktreeBase, '.planning', 'STATE.md')` con raíz fija. Severidad high. |
| T-501-redos | Denial of Service | parseProgressBlock sobre contenido del STATE.md | mitigate | Regex de frontmatter y de keys son CONSTANTES (literales fijas en el código), nunca compiladas desde input externo. Las keys son una allowlist literal fija. Espejo de la disciplina `/^\d+$/` de plan.js:131. |
| T-501-info | Information Disclosure | lectura de STATE.md fuera del worktree esperado | accept | El STATE.md vive en el árbol del proyecto del propio usuario (no PII de terceros); el guard de traversal acota la ruta al worktree derivado. Lectura local, single-user. |
</threat_model>

<verification>
- `node --test test/dashboard-progress.test.js test/state.test.js test/hooks/` verde
- `test ! -f src/hooks/task-progress.js && test ! -f test/hooks/task-progress.test.js`
- `git diff --quiet package.json` (cero deps)
- `grep -q "return join(projectPath, '.bg-shell'" src/session/state.js` (computeWorktreePath intacta)
</verification>

<success_criteria>
- readGsdProgress parsea el bloque `progress:` del STATE.md → `{status,n,m,completed}`, never-throws, N/M = fases (DG-01)
- computeRealWorktreePath resuelve `.claude/worktrees/<sid>` sin tocar computeWorktreePath (DG-04)
- Hook 50-02 (task-progress.js + registro en install.js + tests) eliminado (DG-08)
- install/uninstall siguen cubriendo SessionStart/Stop; cero Task* en settings tras install
- Cero dependencias nuevas (mini-parser hand-rolled — DG-02)
</success_criteria>

<output>
Create `.planning/phases/50.1-live-progress-v-a-state-md-de-gsd-corrige-la-fuente-lee-prog/50.1-01-SUMMARY.md` when done
</output>
