---
phase: 79-sidebar-doctor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cmux/client.js
  - test/sidebar-doctor-hygiene.test.js
autonomous: true
requirements: [SDR-02, SDR-04]
must_haves:
  truths:
    - "src/cmux/client.js exporta createWorkspaceGroup, addToWorkspaceGroup, setGroupAnchor y ungroupWorkspaceGroup, cada uno delegando en run() con argv plano (ref y nombre viajan como elementos de array, jamás interpolados en un string) — espejo de buildNewWorkspaceArgs (client.js:38)"
    - "src/cmux/client.js exporta listWorkspacesJson() que devuelve el stdout crudo de `workspace list --json` (sin parsear — el parseo defensivo vive en la función pura, Plan 02)"
    - "El comentario GRP-04 de listWorkspaceGroups (client.js:99-106) queda re-fronterizado: la gestión de grupos se permite SOLO en el carril doctor con allowlist no-destructivo (create/add/set-anchor/ungroup); el verbo destructivo, remove y rename siguen fuera"
    - "El guard test/sidebar-doctor-hygiene.test.js escanea src/ (comment-stripped, walker recursivo espejo de test/hygiene-api-key.test.js:59-68) y FALLA si aparece cableado el verbo destructivo de workspace-group (tokens argv adyacentes) o un export de gestión con ese verbo"
    - "El guard incluye un bloque detector-no-trivial (espejo hygiene-api-key.test.js:174-198): un fixture sintético CON el cableado prohibido DEBE marcarse y uno limpio NO"
    - "SDR-02 adjacency: el guard detecta el verbo destructivo aunque los tokens `workspace-group` y el verbo aparezcan pegados/con separadores en el argv (regex de adyacencia sobre fuente stripeada)"
    - "SDR-02 empty: con src/ sin ningún cableado del verbo destructivo, el guard reporta 0 violaciones y pasa"
    - "SDR-04 adjacency: dos sesiones kodo que resuelven al MISMO grupo en el launch path se comportan como hoy (fail-open) — el golden GRP-01/GRP-03 de test/manager.test.js pasa sin modificación; el launch nunca gestiona grupos"
    - "SDR-04 empty: con expectedName null (ref degenerado) el launch path lanza sin --group idéntico a hoy — golden group-resolve.test.js (ref degenerado) intacto"
    - "SDR-04 ordering: el orden de flags de buildNewWorkspaceArgs (--name -> --cwd -> --command -> --group) queda byte-idéntico; ningún golden del argv cambia"
    - "El launch path (src/session/manager.js:400-440, newWorkspaceWithGroupFallback :229-237, buildNewWorkspaceArgs client.js:38-44) NO se edita — los nuevos exports son aditivos (D-15)"
  artifacts:
    - "src/cmux/client.js con 4 passthroughs del allowlist + listWorkspacesJson + comentario GRP-04 actualizado"
    - "test/sidebar-doctor-hygiene.test.js (nuevo) — guard SDR-02 + aserción de forma del launch path SDR-04"
  key_links:
    - "Los 4 passthroughs son el ÚNICO canal de mutación cmux del doctor; run() (client.js:14-26) aporta timeout 15s + reject-en-error = fail-open gratis"
    - "El guard es la evidencia mecánica de SDR-02 (no revisión humana); protege la ausencia del verbo destructivo en TODA la fase, no solo en client.js"
  prohibitions:
    - statement: "MUST NOT cablear el verbo destructivo de workspace-group (el que cierra todos los workspaces del grupo) — el allowlist es exclusivamente create/add/set-anchor/ungroup; ni siquiera 'por completitud'"
      status: unverified
    - statement: "MUST NOT editar el launch path (manager.js:400-440, newWorkspaceWithGroupFallback, buildNewWorkspaceArgs, call-site manager.js:411) ni modificar los tests golden GRP-01..03 para que pasen — la re-fronterización de GRP-04 vive SOLO en el carril doctor"
      status: unverified
---

<objective>
Sentar la base cmux del sidebar doctor: los 4 passthrough del allowlist no-destructivo (`create`, `add`, `set-anchor`, `ungroup`) más un lector `listWorkspacesJson`, todos en `src/cmux/client.js` vía el `run()` existente (execFile, argv plano, timeout 15s). Y blindar SDR-02/SDR-04 con un guard source-hygiene que falla si alguien cablea el verbo destructivo del grupo, más la evidencia de que el launch path sigue byte-idéntico.

Purpose: Aislar la gestión de grupos —hasta hoy prohibida (GRP-04)— en un allowlist mínimo y verificado mecánicamente, ANTES de que la lógica del doctor (Plan 02) o el CLI (Plan 03) la consuman. El guard es la puerta que garantiza que la re-fronterización de GRP-04 no abre la puerta al verbo destructivo.
Output: `src/cmux/client.js` (+4 mutaciones allowlist, +1 lector JSON, comentario GRP-04 re-fronterizado) y `test/sidebar-doctor-hygiene.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/79-sidebar-doctor/79-CONTEXT.md
@.planning/phases/79-sidebar-doctor/79-RESEARCH.md
@.planning/phases/79-sidebar-doctor/79-PATTERNS.md

# Analogs de solo lectura (patrón a calcar)
@src/cmux/client.js
@test/hygiene-api-key.test.js
</context>

## Artifacts this phase produces

Símbolos NUEVOS creados en la Phase 79 (para que la verificación de deriva excluya lo recién creado):

- Exports cmux (client.js): `createWorkspaceGroup`, `addToWorkspaceGroup`, `setGroupAnchor`, `ungroupWorkspaceGroup`, `listWorkspacesJson`
- Módulo puro (Plan 02): `src/cmux/sidebar-doctor.js` con exports `scan`, `execute`; helper interno `taskLikeFrom`; typedefs `SidebarReport`, `SidebarResult`
- Categorías del report: `missing_group`, `loose_workspace`, `empty_group`, `protected`, `hasActions`
- Eventos logger (Plan 02, discreción): `sidebarDoctorScan`, `sidebarDoctorFix`, `sidebarDoctorFixError` + constantes `EVENTS.SIDEBAR_DOCTOR_*`
- CLI (Plan 03): `src/cli/sidebar-doctor.js` con export `runSidebarDoctor`; comando `kodo sidebar` + subcomando `doctor`; flags `--fix`, `--json`
- Ficheros de test nuevos: `test/sidebar-doctor-hygiene.test.js`, `test/cmux/sidebar-doctor.test.js`, `test/cli/sidebar-doctor-cli.test.js`

## Multi-Source Coverage Audit (phase 79)

Auditoría de las 4 fuentes — cada item COVERED por un plan:

| Fuente | Item | Plan | Estado |
|--------|------|------|--------|
| GOAL (ROADMAP §79) | Doctor determinista scan+execute, dry-run/`--fix`, espejo `gsd doctor` | 02 (motor) + 03 (CLI) | COVERED |
| GOAL | Allowlist no-destructivo `create`/`add`/`set-anchor`/`ungroup`, sin el verbo destructivo | 01 (passthroughs+guard) | COVERED |
| GOAL | Launch path byte-idéntico (GRP-01..03 fail-open) | 01 (guard SDR-04) | COVERED |
| REQ SDR-01 | Dry-run lista acciones clasificadas sin ejecutar | 02 (scan) + 03 (render) | COVERED |
| REQ SDR-02 | `--fix` usa solo el allowlist; verbo destructivo ausente + guard | 01 (guard) + 02 (execute) | COVERED |
| REQ SDR-03 | 100% determinista, 0 tokens; reutiliza deriveExpectedGroupName + resolveWorkspaceGroup | 02 (scan + no-import provider/LLM) | COVERED |
| REQ SDR-04 | Launch path byte-idéntico, GRP-01..03 intactos | 01 (guard + golden) | COVERED |
| REQ SDR-05 | Sesiones adoptadas/lanzadas convergen al grupo esperado (frontera D-13 Phase 77) | 02 (loose_workspace -> add) | COVERED |
| REQ SDR-06 | CLI espejo `gsd doctor` — `--json` byte-determinista, exit codes deterministas | 03 (runSidebarDoctor) | COVERED |
| RESEARCH | 4 passthroughs argv-plano; re-list tras create (OQ1); reverse-lookup módulo (D-02) | 01 + 02 | COVERED |
| RESEARCH | Fail-open per item + TOCTOU re-check | 02 (execute) | COVERED |
| CONTEXT D-01..D-15 | Todas las decisiones LOCKED | 01/02/03 (citadas por task) | COVERED |

Exclusiones (no gaps): Phase 80 (ORCH-07/08), Phase 81 (DEBT-01..04), FUT-01/02/03, verbo destructivo (NI SE CABLEA), sidebar como trigger, renombrar grupos — todos fuera de scope por CONTEXT/REQUIREMENTS §Out of Scope.

<tasks>

<task type="auto">
  <name>Task 1: Passthroughs del allowlist + lector JSON en client.js</name>
  <files>src/cmux/client.js</files>
  <read_first>
    - src/cmux/client.js (estado actual: run() :14-26, buildNewWorkspaceArgs :38-44, listWorkspaces :95-97, listWorkspaceGroups :99-110 con el comentario GRP-04)
    - .planning/phases/79-sidebar-doctor/79-RESEARCH.md §Standard Stack (sintaxis verificada en vivo cmux 0.64.20) + §Pattern 4
    - .planning/phases/79-sidebar-doctor/79-PATTERNS.md §src/cmux/client.js
  </read_first>
  <action>
    Añadir a `src/cmux/client.js` cuatro funciones async del allowlist (D-12: las funciones cmux nuevas viven en client.js vía run(), exclusivamente el allowlist), cada una delegando en el `run()` existente con un argv plano de strings (nunca shell, nunca interpolación de ref/nombre — V5/Tampering, T-79-01). La sintaxis exacta de cada subcomando está verificada en vivo (D-10: cmux 0.64.20, RESEARCH §Standard Stack). Calcadas del idiom de buildNewWorkspaceArgs:
      - `createWorkspaceGroup({ name, from })`: construye `['workspace-group','create']` y añade `'--name', name` si name es truthy y `'--from', from.join(',')` si `from` es un array no vacío (from = refs `workspace:N`). Devuelve el stdout crudo (el ref se obtiene por re-list en el Plan 02, OQ1 — NO parsear aquí).
      - `addToWorkspaceGroup({ group, workspace })`: `run(['workspace-group','add','--group', group, '--workspace', workspace])`.
      - `setGroupAnchor({ group, workspace })`: `run(['workspace-group','set-anchor','--group', group, '--workspace', workspace])`.
      - `ungroupWorkspaceGroup({ group })`: `run(['workspace-group','ungroup', group])` (grupo posicional).
    Añadir además un lector `listWorkspacesJson()` que haga `run(['workspace','list','--json'])` y devuelva el stdout crudo (JSON sin parsear) — necesario para la liveness de workspace_refs del scan (D-04). NO tocar la `listWorkspaces()` existente (:95-97, devuelve texto plano — la usa otro carril).
    Actualizar el docstring de `listWorkspaceGroups` (:99-106): hoy dice que la familia workspace-group queda fuera por GRP-04; re-fronterizar a que la gestión (create/add/set-anchor/ungroup) se permite SOLO en el carril doctor de v0.18 con allowlist no-destructivo, y que el verbo destructivo, `remove` y `rename` siguen fuera del código. NO añadir ningún quinto passthrough para el verbo destructivo del grupo (el que cierra todos los workspaces) — el guard de la Task 2 lo verifica.
    Reglas de estilo del repo: `// @ts-check` ya presente; JSDoc con @param/@returns como el resto del fichero; cero deps npm nuevas; cero color/ANSI.
  </action>
  <verify>
    <automated>node --test test/sidebar-doctor-hygiene.test.js test/host/cmux-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./src/cmux/client.js').then(m => process.exit(['createWorkspaceGroup','addToWorkspaceGroup','setGroupAnchor','ungroupWorkspaceGroup','listWorkspacesJson'].every(k => typeof m[k]==='function') ? 0 : 1))"` sale 0 (los 5 exports existen y son funciones)
    - Ninguna de las 4 funciones de mutación parsea JSON ni usa template strings para el ref/nombre — todos los argumentos entran a `run()` como elementos de array (source assertion sobre el diff)
    - El docstring de listWorkspaceGroups menciona explícitamente que la gestión se permite SOLO en el carril doctor (re-fronterización GRP-04)
    - `node --test test/host/cmux-isolation.test.js` sigue verde (client.js no rompe el walker)
  </acceptance_criteria>
  <done>Los 4 passthroughs del allowlist + listWorkspacesJson existen y delegan en run() con argv plano; el comentario GRP-04 re-fronterizado; cero cableado del verbo destructivo; walker de aislamiento verde.</done>
</task>

<task type="auto">
  <name>Task 2: Guard source-hygiene (SDR-02) + evidencia launch path byte-idéntico (SDR-04)</name>
  <files>test/sidebar-doctor-hygiene.test.js</files>
  <read_first>
    - test/hygiene-api-key.test.js (analog exacto: stripComments :50-56, listJsFiles recursivo :59-68, bloque "detector no es trivial" :174-198, patrón deepEqual de violaciones)
    - src/cmux/client.js (el fichero recién modificado en la Task 1 — el guard debe pasar contra él)
    - src/session/manager.js:400-440 (launch path — solo lectura; el guard afirma su forma, no lo edita)
    - .planning/phases/79-sidebar-doctor/79-RESEARCH.md §Validation Architecture (Wave 0 Gaps) + §Common Pitfall 4
  </read_first>
  <action>
    Crear `test/sidebar-doctor-hygiene.test.js` calcando el walker de `test/hygiene-api-key.test.js`: reutilizar `stripComments` (elimina comentarios de bloque, línea y continuaciones JSDoc `*` para no marcar menciones legítimas) y `listJsFiles` (recursivo sobre `src/`).
    Guard SDR-02 (D-14: guard source-hygiene automático que falla si aparece el verbo destructivo cableado): escanear todo `src/**/*.js` (comment-stripped) y afirmar `deepEqual(violaciones, [])` para dos patrones — (a) los tokens `workspace-group` y el verbo destructivo del grupo como elementos argv adyacentes (regex de adyacencia sobre la fuente stripeada; el verbo destructivo es el que en el `--help` de cmux "cierra todos los workspaces del grupo"), y (b) un identificador de export que combine gestión de grupo con ese verbo. El patrón exacto de la regex vive en el analog `hygiene-api-key.test.js`; leerlo para calcar la forma `SECRET`/`SINKS` y el mensaje de fallo accionable. <!-- planner-discipline-allow: workspace-group -->
    Bloque detector-no-trivial OBLIGATORIO (espejo :174-198): un fixture sintético string CON el cableado prohibido debe producir ≥1 violación (assert que marca), y un fixture limpio (con solo create/add/set-anchor/ungroup) debe producir 0 (assert que no marca). Esto prueba que el guard no pasa trivialmente.
    Evidencia SDR-04 en el MISMO fichero: (1) un test que lee `src/session/manager.js` y afirma que el bloque del launch path (la llamada a `newWorkspaceWithGroupFallback` con `host._legacy.newWorkspace` y `groupRef`) sigue presente sin cambios de forma (aserción de que las líneas clave existen literalmente), y (2) documentar que los golden GRP-01..03 son el criterio real — se validan corriendo `test/manager.test.js` y `test/session/group-resolve.test.js` sin modificación (comando en <verify>).
    Framework: `node:test` + `node:assert/strict`, imports `describe`/`it`, sin watch-mode.
  </action>
  <verify>
    <automated>node --test test/sidebar-doctor-hygiene.test.js test/manager.test.js test/session/group-resolve.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/sidebar-doctor-hygiene.test.js` pasa: 0 violaciones sobre src/ real
    - El bloque detector-no-trivial demuestra que un fixture con el cableado prohibido SÍ se marca y uno limpio NO (ambos asserts pasan)
    - `test/manager.test.js` y `test/session/group-resolve.test.js` pasan SIN modificación (golden GRP-01..03 intactos — SDR-04)
    - El test de forma del launch path afirma que las líneas de newWorkspaceWithGroupFallback en manager.js no cambiaron
    - El guard usa stripComments (una mención en comentario del verbo destructivo no cuenta como violación)
  </acceptance_criteria>
  <done>El guard SDR-02 falla ante cualquier cableado futuro del verbo destructivo (probado por el bloque no-trivial) y pasa hoy; los golden del launch path GRP-01..03 pasan sin tocar (SDR-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kodo -> cmux CLI | Los 4 passthroughs del allowlist mutan el estado vivo del sidebar cmux vía execFile (argv plano) |
| ref/nombre de grupo -> argv | El ref (`workspace_group:N`, `workspace:N`) y el nombre esperado cruzan hacia la línea de comando |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Tampering | createWorkspaceGroup/add/set-anchor/ungroup argv | high | mitigate | `run()` usa execFile con argv array (nunca shell); refs y nombres viajan como elementos de array, sin interpolación (espejo buildNewWorkspaceArgs client.js:38) |
| T-79-02 | Denial of Service / Destruction | verbo destructivo de workspace-group | high | mitigate | El verbo destructivo (cierra todos los workspaces) NI SE CABLEA (LOCKED); guard source-hygiene automático (Task 2) FALLA si aparece — evidencia mecánica, no revisión humana |
| T-79-06 | Denial of Service | run() contra cmux headless/colgado | low | mitigate | timeout 15s en run() (client.js:17) -> reject -> fail-open per item en el caller (Plan 02) |
| T-79-SC | Tampering | supply chain (npm) | low | accept | Cero deps npm nuevas (invariante cross-milestone); no hay instalación que auditar (RESEARCH §Package Legitimacy Audit: no aplica) |
</threat_model>

<verification>
- `node --test test/sidebar-doctor-hygiene.test.js` verde (guard SDR-02 + no-trivial)
- `node --test test/manager.test.js test/session/group-resolve.test.js` verde sin modificar (SDR-04 golden GRP-01..03)
- `node --test test/host/cmux-isolation.test.js` verde (client.js no rompe aislamiento)
- Los 5 exports nuevos de client.js resuelven como funciones
</verification>

<success_criteria>
- src/cmux/client.js expone create/add/set-anchor/ungroup + listWorkspacesJson, todos argv-plano, sin el verbo destructivo
- El comentario GRP-04 refleja la re-fronterización (gestión SOLO en el carril doctor)
- El guard source-hygiene pasa hoy y fallaría ante un cableado destructivo futuro (probado)
- El launch path y sus golden GRP-01..03 quedan byte-idénticos
</success_criteria>

<output>
Create `.planning/phases/79-sidebar-doctor/79-01-SUMMARY.md` when done
</output>
