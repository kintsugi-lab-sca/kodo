---
phase: 79-sidebar-doctor
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - src/cmux/sidebar-doctor.js
  - src/logger-events.js
  - test/cmux/sidebar-doctor.test.js
autonomous: true
requirements: [SDR-01, SDR-02, SDR-03, SDR-05]
must_haves:
  truths:
    - "D-01: el scan deriva el grupo esperado 100% offline (state.json + projects.json + cmux workspace-group list --json + workspace list) sin ninguna llamada al provider"
    - "D-05: ungroup solo se propone para grupos con 0 miembros en workspace-group list --json (acción no destructiva) y el dry-run lista todos los candidatos antes de cualquier --fix"
    - "src/cmux/sidebar-doctor.js exporta scan(deps) puro (never-throws): cada input (loadState, loadProjects, listWorkspaceGroupsRaw, listWorkspacesRaw) va en try/catch con fallback y clasifica en missing_group / loose_workspace / empty_group + protected + hasActions (espejo estructural de src/gsd/doctor.js:405)"
    - "scan reutiliza deriveExpectedGroupName (manager.js:144) y resolveWorkspaceGroup (manager.js:189) VERBATIM — no re-implementa el strip de identifier ni el match nombre->ref (SDR-03)"
    - "taskLikeFrom(session, projects) reconstruye { ref: session.task_ref, groups: [moduleName] } por reverse-lookup determinista offline (D-02/D-03: NO se persiste expected_group en el session record — el reverse-lookup evita tocar el launch path): moduleName = primer name de entry.modules cuyo path === session.project_path (first-match estable); path === entry.default -> groups vacío -> identifier a secas"
    - "SDR-03: la detección es 100% determinista y 0 tokens — sidebar-doctor.js NO importa ningún cliente de provider ni módulo LLM (verificable por source assertion sobre los imports)"
    - "SDR-01 adjacency: dos sesiones kodo vivas que derivan al MISMO expectedName convergen a una sola entrada missing_group (se agrupan juntas, no colisionan en grupos separados) con anchor = la de started_at más antiguo (D-08)"
    - "SDR-01 empty: con state.json vacío / sin sesiones vivas / sin workspace_ref vivo, scan devuelve las 3 categorías vacías, hasActions=false (never-throws, D-06)"
    - "SDR-01 ordering: cuando varias sesiones convergen al mismo grupo, los miembros se ordenan por started_at ISO-8601 ascendente con desempate por orden estable de la lista; execute emite acciones en orden D-09 create -> add(s) -> set-anchor"
    - "SDR-01 idempotency: ejecutar scan dos veces sobre el mismo input produce un report idéntico (scan es read-only puro; no muta state.json ni el sidebar)"
    - "SDR-01 concurrency: scan() no escribe ningún fichero ni muta el sidebar; un scan interrumpido o dos scans en paralelo no dejan estado parcial (read-only puro)"
    - "SDR-05: una sesión kodo cuyo workspace_ref no está en member_workspace_refs del grupo esperado (que SÍ existe) se clasifica loose_workspace -> add; una sesión suelta sin grupo (o cuyo grupo se disolvió al cerrarse su anchor) se clasifica missing_group con el mismo remedio create+add+set-anchor (D-07: 'grupo disuelto' == grupo faltante con ≥1 sesión viva; resuelve la frontera D-13 de Phase 77)"
    - "src/cmux/sidebar-doctor.js exporta execute(deps, opts): con opts.fix falsy es no-op; con fix true RE-detecta llamando scan(deps) fresco (D-06 TOCTOU, NO consume el report externo) y emite el allowlist por grupo en orden D-09, cada acción en su try/catch (fail-open per item), never-throws top-level"
    - "SDR-02 ordering: execute emite las acciones del allowlist en orden determinista D-09 (create -> add(s) -> set-anchor por grupo; luego loose add; luego empty ungroup)"
    - "SDR-02 idempotency: un segundo execute({fix:true}) sobre un estado ya convergido re-detecta (scan fresco) y ejecuta 0 acciones del allowlist (hasActions=false) — verificable por DI del estado"
    - "Tras createWorkspaceGroup, execute obtiene el ref del grupo nuevo por re-list (listWorkspaceGroupsRaw + resolveWorkspaceGroup) — NO depende de parsear el stdout de create (OQ1/Pitfall 2); create SIEMPRE pasa --from con el/los workspace_ref convergentes (nunca omitido — Pitfall 1)"
    - "Ningún ref workspace_group:N se persiste en state.json ni config (GRP-04); sidebar-doctor.js no importa ningún escritor de state (saveState/withStateLock/upsertTaskHandoff)"
    - statement: "Dos procesos `kodo sidebar doctor --fix` en paralelo mutando el sidebar cmux vivo no dejan grupos duplicados ni miembros perdidos — execute() re-detecta (D-06 TOCTOU) y cada acción es fail-open per item; la ausencia de corrupción bajo carrera REAL de procesos OS no es confirmable en unit (cmux serializa fuera del control de kodo)"
      verification: backstop
  artifacts:
    - "src/cmux/sidebar-doctor.js (nuevo) con scan, execute, taskLikeFrom, typedefs SidebarReport/SidebarResult, resolveDeps con defaults lazy DI (LOG-12: noopLogger default)"
    - "src/logger-events.js con eventos sidebarDoctorScan / sidebarDoctorFix / sidebarDoctorFixError + constantes EVENTS.SIDEBAR_DOCTOR_*"
    - "test/cmux/sidebar-doctor.test.js (nuevo) — unit puro con fixtures del JSON live + DI del run/list (spy de argv)"
  key_links:
    - "scan -> deriveExpectedGroupName/resolveWorkspaceGroup (manager.js) es donde la re-derivación offline debe coincidir byte-a-byte con la del launch; si diverge, sesiones se agrupan mal o quedan sueltas"
    - "execute re-list tras create es el punto frágil (OQ1): si el ref no se resuelve, add/set-anchor van con ref undefined -> fail-open lo salta pero el grupo queda a medio poblar"
    - "el spy de argv en el test es la evidencia unit de SDR-02: ninguna acción emitida contiene el verbo destructivo"
  prohibitions:
    - statement: "MUST NOT tocar, mover ni re-anclar workspaces del operador — solo workspace_ref presentes en state.json y vivos (cruzados con workspace list); un add/set-anchor jamás apunta a un ref no-kodo (D-04)"
      status: unverified
    - statement: "MUST NOT consultar ningún LLM ni provider ni introducir latencia/coste de red en el scan — la detección es 100% determinista y 0 tokens (SDR-03); el scan es 0 red salvo las llamadas cmux locales"
      status: unverified
    - statement: "MUST NOT persistir ningún ref workspace_group:N en state.json o config, ni importar un escritor de state (saveState/withStateLock/upsertTaskHandoff) — el doctor es solo-lectura sobre state.json (GRP-04 + invariante withStateLock)"
      status: unverified
    - statement: "MUST NOT consumir el report de scan como plan dentro de execute — execute RE-detecta con datos frescos antes de actuar (un grupo pudo crearse/disolverse entre scan y execute, D-06)"
      status: unverified
---

<objective>
El motor del doctor: `src/cmux/sidebar-doctor.js`, una mitad pura `scan()` + `execute()` con DI never-throws, espejo arquitectónico exacto de `src/gsd/doctor.js`. `scan` compara las sesiones kodo vivas de `state.json` contra el sidebar real (`workspace-group list --json` + `workspace list --json`) y clasifica en `missing_group` / `loose_workspace` / `empty_group`. `execute` re-detecta (TOCTOU) y emite el allowlist en orden D-09, fail-open per item. La re-derivación del grupo esperado es offline (reverse-lookup de módulo en `projects.json`) y reutiliza verbatim `deriveExpectedGroupName` + `resolveWorkspaceGroup`.

Purpose: Es el núcleo del milestone y el 0-token determinista (SDR-03). Toda la política de agrupación vive aquí; el CLI (Plan 03) solo la renderiza. Reutiliza los passthroughs del Plan 01.
Output: `src/cmux/sidebar-doctor.js`, eventos en `src/logger-events.js`, y `test/cmux/sidebar-doctor.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/79-sidebar-doctor/79-CONTEXT.md
@.planning/phases/79-sidebar-doctor/79-RESEARCH.md
@.planning/phases/79-sidebar-doctor/79-PATTERNS.md

# Analogs de solo lectura (patrón a calcar VERBATIM)
@src/gsd/doctor.js
@src/session/manager.js
@src/logger-events.js
</context>

## Artifacts this phase produces

Ver el listado completo en `79-01-PLAN.md §Artifacts this phase produces`. Símbolos nuevos de ESTE plan: `scan`, `execute`, `taskLikeFrom`, typedefs `SidebarReport`/`SidebarResult`, eventos `sidebarDoctorScan`/`sidebarDoctorFix`/`sidebarDoctorFixError` + `EVENTS.SIDEBAR_DOCTOR_*`. Consumen (no crean) los exports de client.js del Plan 01 y las funciones puras `deriveExpectedGroupName`/`resolveWorkspaceGroup` (Phase 77, ya existentes).

## Supuestos señalados (edge probe spec-less — sin drops silenciosos)

Filas `unclassified` del edge probe que NO se auto-resuelven (se surface como supuestos explícitos, per §C del fallback):

- **SDR-03 (unclassified):** el probe no clasificó una categoría de borde para "detección 100% determinista, 0 tokens". Supuesto del planner: se cubre con una source assertion de que `sidebar-doctor.js` no importa provider/LLM (autorizada como truth arriba). Si el executor detecta un import de red inesperado, señalarlo antes de cerrar.
- **SDR-05 (unclassified):** el probe no clasificó un borde para "convergencia de sesiones adoptadas/lanzadas". Supuesto: la convergencia en unit se prueba con `loose_workspace -> add` (truth arriba); la convergencia REAL en la GUI cmux es manual-only (VALIDATION.md) y se confirma en el checkpoint del Plan 03. No es un drop silencioso: queda trazado aquí y en Manual-Only Verifications.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scan() puro + taskLikeFrom (reverse-lookup módulo) + clasificación</name>
  <files>src/cmux/sidebar-doctor.js, test/cmux/sidebar-doctor.test.js</files>
  <read_first>
    - src/gsd/doctor.js (calco arquitectónico: resolveDeps con defaults lazy :188-208, scan estructura :405-442, detect* helpers never-throws :267-385, pushError :618-621)
    - src/session/manager.js:144-210 (deriveExpectedGroupName + resolveWorkspaceGroup — reuso verbatim; leer el contrato de deriveModuleName -> task.groups[0])
    - src/cmux/client.js (los exports del Plan 01: listWorkspaceGroups, listWorkspacesJson, createWorkspaceGroup, addToWorkspaceGroup, setGroupAnchor, ungroupWorkspaceGroup)
    - src/session/state.js:23-63 (shape del Session record: workspace_ref, task_ref, project_id, project_path, started_at, alive) + loadState :262
    - src/config.js:323 (loadProjects — shape Record<UUID, string | {default, modules}>)
    - .planning/phases/79-sidebar-doctor/79-RESEARCH.md §Pattern 1 + §Pattern 2 + §Shape live de workspace-group list --json (:446-467) + §Shape live de workspace list --json
  </read_first>
  <behavior>
    - Test: state con 2 sesiones vivas mismo expectedName sin grupo -> 1 entrada missing_group, members ordenados por started_at asc, anchor = el más antiguo (D-08)
    - Test: sesión viva cuyo workspace_ref NO está en member_workspace_refs del grupo existente -> loose_workspace con group=ref del grupo (SDR-05)
    - Test: state vacío / loadState throws -> fallback {sessions:{}}, 3 categorías vacías, hasActions=false (never-throws, D-06)
    - Test: listWorkspaceGroupsRaw devuelve JSON malformado -> JSON.parse en try/catch -> groups=[] -> no crash, categorías coherentes
    - Test: taskLikeFrom con session.project_path === entry.default -> groups=[] -> deriveExpectedGroupName da identifier a secas; con project_path === un entry.modules[name] -> groups=[name] -> "IDENTIFIER/Módulo" (D-02)
    - Test: sesión con alive===false o cuyo workspace_ref no está vivo (ausente de workspace list --json) -> excluida (D-04)
    - Test: grupo con member_count===0 -> empty_group (defensivo, D-05)
  </behavior>
  <action>
    Crear `src/cmux/sidebar-doctor.js` con `// @ts-check`. Imports para defaults lazy DI (el módulo vive en src/cmux/, así que PUEDE importar ./client.js — no lo escanea el walker cmux-isolation, RESEARCH §Nota de aislamiento): `noopLogger` de `../logger-noop.js` (LOG-12, JAMÁS logger.js), `loadState` de `../session/state.js`, `deriveExpectedGroupName`+`resolveWorkspaceGroup` de `../session/manager.js`, `loadProjects` de `../config.js`, y del `./client.js` los exports del Plan 01. Eventos de `../logger-events.js` (Task 2 los añade; importarlos aquí).
    `resolveDeps(deps)` espejo de doctor.js:188-208: cada primitiva DI con su default real lazy — `loadState`, `loadProjects`, `listWorkspaceGroupsRaw` (default = listWorkspaceGroups), `listWorkspacesRaw` (default = listWorkspacesJson), los 4 verbos del allowlist, `now` (default Date.now), `logger` (default noopLogger — never-throws al emitir eventos).
    `taskLikeFrom(session, projects)`: reverse-lookup determinista (D-02) — si `projects[session.project_id]` es objeto con `.modules` y `session.project_path !== entry.default`, buscar el primer `[name,path]` de `Object.entries(entry.modules)` con `path === session.project_path` y usar ese name; devolver `{ ref: session.task_ref, groups: moduleName ? [moduleName] : [] }`. Contrato verificado contra manager.js:166 (deriveModuleName -> task.groups[0]).
    `scan(deps)`: patrón de RESEARCH §Pattern 1 (implementación concreta ya escrita ahí). Cada input en try/catch con fallback (state={sessions:{}}, projects={}, groupsJson={groups:[]}, workspacesJson={workspaces:[]}). Construir `liveWorkspaceRefs` (Set de w.ref de workspacesJson.workspaces) y `memberOf` (índice workspace:N -> [group refs] desde member_workspace_refs). Agrupar sesiones vivas (alive !== false Y workspace_ref en liveWorkspaceRefs, D-04) por `deriveExpectedGroupName(taskLikeFrom(s,projects), projects[s.project_id], s.project_path)`; saltar expectedName null (fail-open). Por cada expectedName: si `resolveWorkspaceGroup(groupsJson, expectedName)` es null -> `missing_group` con members ordenados por started_at asc (helper sortByOldest, empate estable) y anchor=members[0]; si existe -> por cada sesión cuyo workspace_ref no esté en los miembros del groupRef -> `loose_workspace`. `empty_group` = grupos con member_count===0. `hasActions = missing_group.length + loose_workspace.length + empty_group.length > 0`. `protected` (workspaces bien agrupados / operador) NO afecta hasActions. Emitir `sidebarDoctorScan(logger, {...})` si hay logger. never-throws: cada helper de detección aislado en try/catch con warn (espejo detect* de doctor.js).
    NO importar ningún cliente de provider ni módulo LLM (SDR-03). NO importar ningún escritor de state (GRP-04).
  </action>
  <verify>
    <automated>node --test test/cmux/sidebar-doctor.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Los 7 tests de <behavior> pasan
    - `scan()` con deps vacíos (defaults reales) no lanza aunque cmux/state falten (fail-open -> hasActions=false)
    - Source assertion: `src/cmux/sidebar-doctor.js` no importa ningún path de provider (`../provider`, `../plane`, `../github`) ni logger.js — grep del fichero
    - missing_group.members está ordenado por started_at ISO asc; anchor === members[0]
    - Correr scan dos veces sobre el mismo fixture devuelve deepEqual idéntico (idempotencia read-only, SDR-01)
  </acceptance_criteria>
  <done>scan() clasifica missing/loose/empty desde fixtures del JSON live, never-throws, 0 imports de provider/LLM, reverse-lookup de módulo correcto; ordenación por oldest verificada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: execute() (orden D-09, TOCTOU re-detect, fail-open) + eventos logger</name>
  <files>src/cmux/sidebar-doctor.js, src/logger-events.js, test/cmux/sidebar-doctor.test.js</files>
  <read_first>
    - src/gsd/doctor.js (execute estructura :483-608: emptyResult, if(!opts.fix) return, RE-detección vía scan interno, try/catch por acción, pushError; errMsg :610-613)
    - src/logger-events.js:709-798 (doctorScan/doctorFixWorktree/doctorFixLock/doctorFixError — patrón a calcar para los eventos sidebar; EVENTS obj :86-90)
    - src/cmux/sidebar-doctor.js (scan de la Task 1 — execute lo re-invoca)
    - .planning/phases/79-sidebar-doctor/79-RESEARCH.md §Code Examples execute() (:409-443) + §Common Pitfall 1 y 2 (Open Question 1)
  </read_first>
  <behavior>
    - Test: execute({fix:false}) -> emptyResult, NO llama ningún verbo del allowlist (spy de argv vacío)
    - Test: report con 1 missing_group (2 members) -> spy captura, en orden: createWorkspaceGroup con --from=[oldest], luego addToWorkspaceGroup del segundo member, luego setGroupAnchor al oldest (D-09/D-08); ninguna llamada contiene el verbo destructivo
    - Test: execute RE-invoca scan(deps) con datos frescos — si entre el scan externo y el interno el grupo ya existe (DI de un groupsJson que lo incluye), NO se crea duplicado (idempotencia SDR-02, TOCTOU D-06)
    - Test: una llamada del allowlist rechaza (run stub throws) -> pushError registra + emite sidebarDoctorFixError + continúa con las demás (fail-open per item)
    - Test: tras createWorkspaceGroup el ref se resuelve por re-list (listWorkspaceGroupsRaw devuelve el grupo nuevo) -> add/set-anchor usan ese ref (OQ1)
    - Test: loose_workspace -> addToWorkspaceGroup(group,workspace); empty_group -> ungroupWorkspaceGroup(ref)
  </behavior>
  <action>
    Primero, añadir a `src/logger-events.js` los eventos del doctor sidebar calcando doctorScan/doctorFix*/doctorFixError (:709-798): constantes `EVENTS.SIDEBAR_DOCTOR_SCAN='sidebar.doctor.scan'`, `SIDEBAR_DOCTOR_FIX='sidebar.doctor.fix'`, `SIDEBAR_DOCTOR_FIX_ERROR='sidebar.doctor.fix.error'` (en ambos bloques donde EVENTS se declara, :53-57 typedef y :86-90 objeto) y las funciones `sidebarDoctorScan(logger, fields)` (info; fields: mode, missing, loose, empty), `sidebarDoctorFix(logger, fields)` (info; fields: created, added, ungrouped) y `sidebarDoctorFixError(logger, fields)` (error; fields: category, reason, target). Naming/taxonomía es discreción (D-11) — seguir el estilo doctor*.
    Luego, en `src/cmux/sidebar-doctor.js` añadir `execute(deps, opts)` async espejo de doctor.js:483. `emptyResult()` = { created:0, added:0, ungrouped:0, errors:[] }. Si `!opts.fix` -> return emptyResult (dry-run usa scan). Bajo fix: outer try/catch (never-throws top-level). RE-detectar llamando `scan(deps)` FRESCO (D-06 — NO consumir un report externo). Por cada `missing_group` (orden D-09): `createWorkspaceGroup({ name, from:[anchor] })` (from SIEMPRE explícito con el oldest — Pitfall 1, nunca omitido); re-list vía `resolveWorkspaceGroup(JSON.parse(listWorkspaceGroupsRaw()), name)` para el ref (OQ1/Pitfall 2); si el ref no resuelve -> pushError + continue; por cada member != anchor -> `addToWorkspaceGroup({group:ref, workspace})` en try/catch; luego `setGroupAnchor({group:ref, workspace:anchor})` en try/catch (idempotente, D-08). Por cada `loose_workspace` -> `addToWorkspaceGroup`. Por cada `empty_group` -> `ungroupWorkspaceGroup`. Cada acción en su try/catch -> `pushError(result, logger, categoria, target, errMsg)` + `sidebarDoctorFixError`, y continúa (fail-open per item). Emitir `sidebarDoctorFix` al final con los contadores. NO cablear el verbo destructivo del grupo bajo ninguna rama.
    Documentar en el executor: A1/A2/A5 (RESEARCH §Assumptions) son MEDIUM/BAJO — verificar A1 (¿create --json devuelve el ref?) opcionalmente contra un grupo throwaway; el default robusto (re-list) no depende de ello.
  </action>
  <verify>
    <automated>node --test test/cmux/sidebar-doctor.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Los 6 tests de <behavior> pasan
    - Spy de argv: en NINGUNA ejecución aparece el verbo destructivo de workspace-group (SDR-02 a nivel unit)
    - execute con fix:false no invoca ningún verbo del allowlist
    - Orden capturado por el spy para un missing_group: create (con --from=oldest) -> add(s) -> set-anchor (D-09)
    - Un rechazo de una acción no aborta el pase (las demás siguen; error queda en result.errors + evento emitido)
    - `src/logger-events.js` exporta sidebarDoctorScan/sidebarDoctorFix/sidebarDoctorFixError
  </acceptance_criteria>
  <done>execute() emite el allowlist en orden D-09 con re-detección TOCTOU y fail-open per item; eventos sidebar añadidos; spy confirma que el verbo destructivo nunca se emite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmux JSON (workspace-group/workspace list) -> scan | Output de shape no confiable parseado en la función pura defensiva |
| state.json / projects.json -> scan | Leídos por el doctor (escritos por kodo bajo lock; solo lectura aquí) |
| execute -> cmux allowlist | `--fix` muta el estado vivo del sidebar (propósito de la fase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-03 | Tampering | add/set-anchor apuntando a un workspace del operador | high | mitigate | D-04: solo workspace_ref presentes en state.json y vivos (cruzados con workspace list --json); un ref no-kodo jamás entra a una acción |
| T-79-04 | Tampering / DoS | JSON malformado/malicioso de cmux | medium | mitigate | JSON.parse en try/catch dentro de la función pura; shape inesperado -> categoría vacía + warn (never-throws D-06); resolveWorkspaceGroup valida `^workspace_group:\d+$` |
| T-79-05 | Tampering | escritura corruptiva de state.json | medium | mitigate | El doctor NO escribe state.json (solo lectura); no importa saveState/withStateLock; invariante intacto por construcción — verificado por source assertion |
| T-79-02 | Denial of Service / Destruction | verbo destructivo emitido por execute | high | mitigate | execute solo emite create/add/set-anchor/ungroup; spy de argv en test lo prueba; guard source-hygiene del Plan 01 lo respalda |
| T-79-SC | Tampering | supply chain (npm) | low | accept | Cero deps npm nuevas; nada que instalar |
</threat_model>

<verification>
- `node --test test/cmux/sidebar-doctor.test.js` verde (scan + execute, todos los <behavior>)
- Spy de argv: el verbo destructivo nunca se emite
- Source assertion: 0 imports de provider/LLM/escritor-de-state en sidebar-doctor.js
- scan idempotente (deepEqual en dos corridas); execute fail-open per item
- `npm test` verde (suite completa) antes de cerrar el plan
</verification>

<success_criteria>
- scan() clasifica correctamente missing/loose/empty desde el JSON live, 0 tokens, never-throws
- execute() re-detecta (TOCTOU) y emite el allowlist en orden D-09, fail-open per item
- Reverse-lookup de módulo (D-02) coincide con la derivación del launch
- Ningún ref workspace_group:N persistido; ningún workspace del operador tocado
</success_criteria>

<output>
Create `.planning/phases/79-sidebar-doctor/79-02-SUMMARY.md` when done
</output>
