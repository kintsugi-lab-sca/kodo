---
phase: 75-superficie-del-next-dashboard-y-nudge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/tasks.js
  - src/cli/dashboard/select.js
  - src/cli/dashboard/format.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-tasks.test.js
  - test/dashboard-select.test.js
  - test/dashboard-format.test.js
  - test/dashboard-table.test.js
autonomous: true
requirements: [LIVE-05]

must_haves:
  truths:
    - "El dashboard muestra el NEXT: por tarea en la lista, leído de state.json por un reader leaf filesystem — la TUI no abre N ficheros de plan para pintar la tabla ni añade endpoint en src/server.js (LIVE-05, D-01, SC1)."
    - "readTasks tolera escritores concurrentes: los escritores usan temp+rename atómico, así que una lectura nunca ve un fichero a medias; una lectura fallida o parcial en un tick colapsa never-throws a {} (celdas vacías) y el siguiente tick se recupera; el reader NUNCA escribe (nunca loadState, que migra+escribe — RESEARCH Pitfall 1) (LIVE-05, concurrency, edge covered)."
    - "readTasks sobre state.json ausente / JSON corrupto / sin clave tasks colapsa a {} never-throws → celdas vacías, cero ruido, TUI never-throws (SC5, UI-SPEC error)."
    - "La celda next de una tarea sin NEXT: es la cadena vacía '' — sin placeholder, sin guion, sin N/A (SC5, UI-SPEC empty)."
    - "La columna next solo se emite si deriveAnyNext(enriched) es true (≥1 fila con next no vacío); si ninguna fila tiene dato la columna no se renderiza e ink recupera el ancho vía flex (D-03, UI-SPEC empty)."
    - "deriveAnyNext se computa sobre el set SIN filtrar (enriched), NO sobre filtered → la columna no parpadea al teclear una query / (RESEARCH Pitfall 4, UI-SPEC partial)."
    - "La columna next va al FINAL del orden de columnas (tras age) y trunca al ancho con cell({truncate:true}) → ellipsis nativo …; doble acotado con el cap de 200 chars que ya trae state.json (D-04, RESEARCH Pitfall 6, UI-SPEC overflow)."
    - "El valor next proveniente de state.json (contenido LLM) pasa por stripControlChars en el enrich de App.js — el mismo saneo que task_ref/summary (App.js:737-741) — ANTES de proyectarse a la celda (V5 Input Validation, T-75-01)."
  artifacts:
    - "src/cli/dashboard/tasks.js — nuevo módulo leaf con readTasks(deps)"
    - "src/cli/dashboard/select.js — deriveAnyNext exportada"
    - "src/cli/dashboard/format.js — nextCell exportada + clave next en rowCells"
    - "src/cli/dashboard/App.js — enrich row.next + anyNext + prop a SessionTable"
    - "src/cli/dashboard/SessionTable.js — COLS.next + header y celda condicionales al final"
    - "test/dashboard-tasks.test.js — nuevo (Wave 0), cubre readTasks"
  key_links:
    - "state.json clave tasks (filesystem) → readTasks → enrich por task_id en App.js → deriveAnyNext/nextCell → columna next de SessionTable"
    - "El merge next se hace por task_id en la capa derive (pura, React-free), nunca en el render (D-02)"
  prohibitions:
    - statement: "NUNCA añadir un endpoint nuevo en src/server.js para servir el NEXT: — el dato viaja en state.json que la TUI ya lee (invariante cross-milestone «cero endpoints nuevos desde v0.10», SC1)."
      status: unverified
    - statement: "NUNCA escribir/mutar state.json desde el reader de la TUI, ni importar loadState() (migra + escribe .bak en cada tick de poll) — el reader es lectura pura never-throws (RESEARCH Pitfall 1)."
      status: unverified
---

<objective>
Superficie LIVE-05: el operador ve el `NEXT:` por tarea en la lista del dashboard, leído de
`~/.kodo/state.json` por un reader leaf filesystem que se engancha al tick de poll existente
(piggyback), sin abrir N ficheros de plan y sin endpoint nuevo en `src/server.js`.

Purpose: cablear a la tabla el dato vivo (`state.tasks[task_id].next`) que la Phase 74 ya produce,
replicando tres patrones ya probados del propio directorio: el reader síncrono never-throws
(`readLightPlan`), el enrich por tick (`progress`) y la columna condicional (`prog`, Phase 50 D-06).

Output: módulo `tasks.js` (nuevo), `deriveAnyNext`, `nextCell`, el enrich `row.next` en App.js y la
columna condicional `next` al final de la tabla. Degradación limpia en cinco caminos de dato ausente
(SC5): celda vacía, cero ruido, TUI never-throws.
</objective>

<artifacts_produced>
Símbolos nuevos/modificados que crea este plan (para el pase de source-grounding del review):
- `readTasks(deps)` — NUEVO fichero `src/cli/dashboard/tasks.js` (reader leaf never-throws).
- `deriveAnyNext(rows)` — NUEVO export en `src/cli/dashboard/select.js`.
- `nextCell(session)` — NUEVO export en `src/cli/dashboard/format.js`; nueva clave `next` en el objeto que devuelve `rowCells`.
- `anyNext` (variable derivada) + `next` en el objeto `enriched` — en `src/cli/dashboard/App.js`; nueva prop `anyNext` a `SessionTable`.
- `COLS.next` + header/celda condicionales `next` — en `src/cli/dashboard/SessionTable.js`.
- `test/dashboard-tasks.test.js` — NUEVO fichero de test (Wave 0).
</artifacts_produced>

<planner_assumptions>
Ninguna suposición sin resolver para LIVE-05: la edge de concurrencia se resolvió `covered`
(ver el truth de tolerancia a escritores concurrentes). Sin huecos flagged en este plan.
</planner_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-CONTEXT.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-RESEARCH.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-PATTERNS.md
@.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-UI-SPEC.md
@src/cli/dashboard/plan.js
@src/cli/dashboard/progress.js
@src/cli/dashboard/select.js
@src/cli/dashboard/format.js
@src/cli/dashboard/SessionTable.js
@src/cli/dashboard/App.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reader leaf readTasks de state.tasks (nuevo módulo tasks.js)</name>
  <files>src/cli/dashboard/tasks.js, test/dashboard-tasks.test.js</files>
  <read_first>
    - src/cli/dashboard/plan.js (readLightPlan :65-78 — molde LITERAL del reader: never-throws + DI de HOME `kodoDir`/`homedirFn`/`readFileFn`; imports SOLO builtins node:fs/node:path/node:os, jamás config.js)
    - src/session/state.js (typedef TaskHandoff :44-63 y el guard defensivo `state.tasks || {}` :61 — la forma de state.tasks[task_id] = { plan_path, next, updated_at })
    - src/cli/dashboard/client.js (fetchStatus :51 — la forma never-throws discriminada a replicar)
    - test/dashboard-plan.test.js (patrón de fixtures + aislamiento de HOME vía deps, para modelar el test nuevo)
  </read_first>
  <behavior>
    - readTasks() sobre un state.json con clave `tasks` → devuelve exactamente ese objeto tasks.
    - readTasks() sobre state.json inexistente (ENOENT) → {} (no lanza).
    - readTasks() sobre JSON corrupto (parse falla) → {} (no lanza).
    - readTasks() sobre state.json SIN clave `tasks` (o tasks null / no-objeto) → {} (no lanza).
    - DI: pasar `readFileFn` y `kodoDir` (o `homedirFn`) aísla el HOME real — el test NUNCA lee el ~/.kodo del operador.
    - El módulo importa SOLO builtins (node:fs, node:path, node:os); NO importa src/config.js ni loadState.
  </behavior>
  <action>
    Crear `src/cli/dashboard/tasks.js` con una única función exportada `readTasks(deps = {})`, modelada
    byte-a-byte sobre la forma de `readLightPlan` (plan.js:65-78). Resolver `readFileFn = deps.readFileFn
    || ((p) => readFileSync(p, 'utf-8'))` y `kodoDir = deps.kodoDir || join((deps.homedirFn || homedir)(),
    '.kodo')` (misma convención que plan.js:69). Dentro de un try: `JSON.parse(readFileFn(join(kodoDir,
    'state.json')))`, y devolver el objeto tasks SOLO si es un objeto no-null (guard idéntico al de
    state.js:61 — `state && typeof state.tasks === 'object' && state.tasks !== null ? state.tasks : {}`);
    en el catch devolver `{}`. Importar EXCLUSIVAMENTE builtins (`readFileSync` de node:fs, `join` de
    node:path, `homedir` de node:os). PROHIBIDO importar `loadState`/`src/config.js`: `loadState` llama
    `migrateStateIfNeeded()` que ESCRIBE en disco (`.bak`) en cada tick de poll (RESEARCH Pitfall 1) — el
    reader es lectura pura never-throws. Añadir JSDoc con el typedef del return
    `Record<string, { plan_path: string, next: string|null, updated_at: string }>`.
    Crear `test/dashboard-tasks.test.js` (Wave 0) cubriendo los cuatro caminos de la sección behavior con
    DI de HOME (fixtures de state.json mínimos: con tasks, sin tasks, corrupto; ENOENT vía kodoDir a un
    directorio vacío temporal). Escribir primero los tests (deben fallar por módulo ausente), luego el módulo.
  </action>
  <verify>
    <automated>node --test test/dashboard-tasks.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-tasks.test.js` pasa (los 4+ casos: tasks presente, ENOENT→{}, corrupto→{}, sin-tasks→{}).
    - `grep -nE "^import" src/cli/dashboard/tasks.js` muestra SOLO líneas import de builtins (node:fs / node:path / node:os); revisar que ninguna línea import trae state.js, config.js ni el migrador (el reader no arrastra el grafo — la garantía mecánica de «no I/O de escritura» es el check conductual de abajo).
    - Ejecutar `npm test` no crea ningún fichero `state.json.bak.*` (el reader nunca escribe — prueba conductual de que no pasa por el migrador de estado).
  </acceptance_criteria>
  <done>readTasks lee state.tasks never-throws con DI de HOME, colapsa todo fallo a {}, no escribe nunca y no importa loadState/config.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveAnyNext (select.js) + nextCell (format.js)</name>
  <files>src/cli/dashboard/select.js, src/cli/dashboard/format.js, test/dashboard-select.test.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/dashboard/select.js (deriveAnyProgress :241-243 y deriveAnyGsd :217-219 con sus docstrings :209-212 / :232-236 sobre computar SIEMPRE sobre el set SIN filtrar — molde LITERAL de deriveAnyNext)
    - src/cli/dashboard/format.js (progCell :240-246, taskCell :214-221 y rowCells :257-267 — la forma de celda y el punto donde añadir la clave next al FINAL, tras age)
    - test/dashboard-select.test.js y test/dashboard-format.test.js (para extender con los casos nuevos siguiendo el estilo existente)
  </read_first>
  <behavior>
    - deriveAnyNext(rows) → true si ≥1 fila tiene `next` string de longitud > 0; false si ninguna (o next null/vacío/no-string en todas).
    - deriveAnyNext sobre un set con filas next presentes pero filtradas fuera: el consumidor lo llama sobre el set SIN filtrar (documentado en el docstring, espejo de deriveAnyProgress).
    - nextCell(session) → el string `session.next` si es no-vacío; '' si next es null/undefined/'' (SC5, sin placeholder).
    - rowCells(session).next existe y es igual a nextCell(session); va como última clave del objeto (tras age).
  </behavior>
  <action>
    En `select.js`, añadir `export function deriveAnyNext(rows)` como espejo LITERAL de `deriveAnyProgress`:
    `return rows.some((r) => typeof r.next === 'string' && r.next.length > 0)`. Copiar el docstring del
    invariante estructural de deriveAnyProgress (el consumidor la computa sobre `enriched` SIN filtrar, no
    sobre `filtered`, para que la columna no parpadee al teclear `/` — RESEARCH Pitfall 4).
    En `format.js`, añadir `export function nextCell(session)` que devuelve `typeof session.next === 'string'
    && session.next.length > 0 ? session.next : ''` (celda vacía sin placeholder ruidoso, SC5); es texto
    plano — SIN color propio (color-isolation: el color queda reservado, no se pinta acento en la celda next).
    En `rowCells` (:257-267), añadir la clave `next: nextCell(session)` como ÚLTIMA propiedad del objeto,
    tras `age` (D-04, última posición). NO tocar ninguna otra celda.
    Extender `test/dashboard-select.test.js` (deriveAnyNext: true con un next, false con ninguno, false con
    next '' / null) y `test/dashboard-format.test.js` (nextCell con y sin dato; rowCells.next presente).
    Escribir los asserts nuevos primero (fallan por símbolo ausente), luego implementar.
  </action>
  <verify>
    <automated>node --test test/dashboard-select.test.js test/dashboard-format.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-select.test.js test/dashboard-format.test.js` pasa con los casos nuevos.
    - `grep -n "export function deriveAnyNext" src/cli/dashboard/select.js` devuelve una línea.
    - `grep -n "export function nextCell" src/cli/dashboard/format.js` devuelve una línea; `grep -n "next:" src/cli/dashboard/format.js` muestra la clave next dentro de rowCells.
    - `node --test test/format-isolation.test.js` sigue verde (nextCell no introduce picocolors/ANSI).
  </acceptance_criteria>
  <done>deriveAnyNext y nextCell existen, son puros React-free, la celda vacía es '' y rowCells expone next como última clave.</done>
</task>

<task type="auto">
  <name>Task 3: Enrich por tick en App.js + columna condicional next en SessionTable.js</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js, test/dashboard-table.test.js</files>
  <read_first>
    - src/cli/dashboard/App.js (enrich de progress :729-776 — el molde del enrich por tick; anyGsd :780 / anyProgress :783; applyFilter :784; la prop pasada a SessionTable :2013-2027; el saneo stripControlChars de task_ref/summary :737-741 que el next debe replicar)
    - src/cli/dashboard/SessionTable.js (COLS :95; cell :111 con wrap:'truncate-end'; columnHeader :982-1000 con el header condicional prog :996; dataRows :1003 con la celda condicional prog :1051-1052; la firma de props del componente :814-815 con anyGsd/anyProgress defaults)
    - src/cli/dashboard/tasks.js (readTasks — creado en Task 1)
    - src/cli/dashboard/select.js (deriveAnyNext — creado en Task 2)
    - src/cli/format.js (stripControlChars :80 — el saneo a aplicar al next)
    - test/dashboard-table.test.js (para extender con el render de la columna y la ausencia condicional)
  </read_first>
  <action>
    En `App.js`, en el cuerpo del render y ANTES del `.map` del enrich (~:730), leer las tareas UNA vez por
    tick: `const tasks = readTasks({})` (never-throws → {}; piggyback sobre el tick de usePoll que ya
    refresca /status — D-02, cero loop nuevo, cero watcher). Dentro del map del enrich añadir a cada fila el
    campo `next`: tomar `tasks[row.task_id]?.next` SOLO si `row.task_id` es truthy, y saneаrlo con
    `stripControlChars` cuando sea un string no vacío, colapsando cualquier otro caso a `null`
    (el dato es de la TAREA por task_id, no de la sesión; ausente → null → celda vacía). Importar `readTasks`
    de `./tasks.js` y `deriveAnyNext` de `./select.js`. Tras el enrich, computar `const anyNext =
    deriveAnyNext(enriched)` sobre `enriched` SIN filtrar (NO sobre `filtered` — RESEARCH Pitfall 4, espejo
    de anyProgress :783). Pasar `anyNext` como prop a `SessionTable` (junto a anyGsd/anyProgress :2026-2027).
    En `SessionTable.js`: añadir la clave `next` al objeto `COLS` (:95) como ÚLTIMA columna tras `age`
    (ancho base ~40, columna flexible — Claude's Discretion respetando COLS). Recibir la prop `anyNext = false`
    en la firma del componente (default false, retro-compat, espejo invertido de anyProgress :815). En
    `columnHeader` (:982-1000), añadir el header condicional al FINAL tras el de `age`:
    `...(anyNext ? [h(Box, { width: COLS.next }, h(Text, { dimColor: true }, 'next'))] : [])`. En
    `dataRows.map` (:1003+), añadir la celda condicional al FINAL:
    `...(anyNext ? [cell({ width: COLS.next, text: cells.next, bold: selected, truncate: true })] : [])`
    — `truncate:true` da el ellipsis nativo `…` de ink (D-04, doble acotado con el cap de 200 de state.json,
    Pitfall 6). NO tocar ninguna columna existente ni su orden.
    Extender `test/dashboard-table.test.js`: (a) con ≥1 fila con next, la columna aparece y la celda muestra
    el valor truncado; (b) sin ninguna fila con next, ni el header `next` ni la celda se emiten; (c) un next
    con caracteres de control se sanea antes de pintarse; (d) el enrich mergea el next por task_id.
  </action>
  <verify>
    <automated>node --test test/dashboard-table.test.js test/dashboard-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard-table.test.js` pasa: columna presente con dato, ausente sin dato, merge por task_id, saneo del next.
    - `grep -n "readTasks\|deriveAnyNext\|anyNext" src/cli/dashboard/App.js` muestra el reader, el derive y la prop.
    - `grep -n "next:" src/cli/dashboard/SessionTable.js` muestra la clave next en COLS; `grep -n "anyNext" src/cli/dashboard/SessionTable.js` muestra header y celda condicionales.
    - `git diff --stat src/server.js` está VACÍO (cero endpoints nuevos, SC1) tras el plan.
    - `node --test test/format-isolation.test.js` sigue verde (la columna next no introduce picocolors/ANSI).
    - `npm test` verde (excepto el flaky preexistente test/gsd-lock-race.test.js «CR-01», que NO lo causa esta fase — STATE.md §Deferred).
  </acceptance_criteria>
  <done>La columna next aparece condicionalmente al final de la tabla leyendo state.json por tick, mergea por task_id, sanea el contenido LLM, trunca al ancho y no toca server.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.json (fichero local) → capa de datos de la TUI | El valor `next` es contenido escrito por un LLM; cruza al render del terminal. |
| filesystem → reader leaf | Un fichero ausente/corrupto/parcial cruza a la cadena de render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-01 | Tampering | celda `next` (App.js enrich → SessionTable) | high | mitigate | El `next` de state.json (contenido LLM) pasa por `stripControlChars` (src/cli/format.js, el mismo que sanea task_ref/summary en App.js:737-741) ANTES de proyectarse a la celda — neutraliza OSC-52/CSI/C1 (terminal escape injection). |
| T-75-04 | Denial of Service | readTasks (tasks.js) | medium | mitigate | Reader leaf never-throws: ENOENT/JSON corrupto/sin clave `tasks` → {}; ningún throw llega a React (SC5). |
| T-75-05 | Tampering | ruta de state.json | low | mitigate | Ruta con root FIJO `join(homedir(),'.kodo','state.json')`, jamás derivada de input; el `next` no se usa como ruta. |
| T-75-SC | Tampering | instalaciones npm/pip/cargo | low | accept | Esta fase instala CERO paquetes (RESEARCH §Package Legitimacy Audit N/A; invariante «cero deps npm nuevas») — sin superficie supply-chain. |
</threat_model>

<verification>
- `node --test test/dashboard-tasks.test.js test/dashboard-select.test.js test/dashboard-format.test.js test/dashboard-table.test.js` — verde.
- `node --test test/format-isolation.test.js` — verde (color-isolation intacta con la columna y el reader nuevos).
- `git diff --stat src/server.js package.json package-lock.json` — VACÍO (cero endpoints nuevos, cero deps nuevas).
- `npm test` — verde salvo el flaky preexistente CR-01 (no causado por esta fase).
</verification>

<success_criteria>
- El dashboard muestra el `NEXT:` por tarea leyéndolo de state.json, sin abrir N ficheros de plan y sin endpoint nuevo en src/server.js (LIVE-05 / SC1).
- La columna next es condicional (aparece solo con dato), va al final, trunca al ancho y degrada a celda vacía sin ruido cuando falta el dato (D-03/D-04, SC5).
- El contenido LLM del next se sanea antes de renderizarse (T-75-01).
</success_criteria>

<output>
Crear `.planning/phases/75-superficie-del-next-dashboard-y-nudge/75-01-SUMMARY.md` al terminar.
</output>
