---
phase: 45-spike-captura-de-plan-no-gsd-v-a-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-SPIKE.md
autonomous: false
requirements: [PLAN-03]
user_setup: []

must_haves:
  truths:
    - "Existe `45-SPIKE.md` con un veredicto binario (VIABLE | INVIABLE) en su primera sección."
    - "El documento transcribe el comando exacto lanzado y el payload crudo capturado (o su ausencia documentada) para cada combinación de la matriz D-02."
    - "El documento cita la versión de `claude` instalada (`claude --version`) — el veredicto está anclado a esa versión."
    - "Si VIABLE: el documento especifica el contrato de captura propio de kodo con los 5 puntos de D-04. Si INVIABLE: registra la decisión de diferir PLAN-04 a v2 sin bloquear el milestone."
    - "Tras el experimento, `~/.claude/settings.json` queda restaurado byte-idéntico al backup y los hooks producción de kodo (SessionStart/Stop) persisten."
    - "Tras el experimento, `git diff -- src/ test/ bin/` está vacío (cero código de producción commiteado)."
  artifacts:
    - path: ".planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-SPIKE.md"
      provides: "Documento de spike: veredicto binario + método + evidencia reproducible + contrato/diferimiento"
      contains: "VIABLE"
      min_lines: 40
  key_links:
    - from: "spike-dump.js (hook de prueba temporal)"
      to: "/tmp/kodo-spike-*.log"
      via: "appendFileSync del payload crudo de stdin"
      pattern: "appendFileSync"
    - from: "~/.claude/settings.json (entrada temporal de prueba)"
      to: "spike-dump.js"
      via: "command 'KODO_SPIKE_LOG=... node .../spike-dump.js' en hooks[event]"
      pattern: "KODO_SPIKE_LOG"
    - from: "45-SPIKE.md sección (3) evidencia"
      to: "/tmp/kodo-spike-*.log"
      via: "transcripción literal del payload crudo capturado"
      pattern: "session_id"
---

<objective>
Alcanzar un **veredicto binario empírico** (VIABLE / INVIABLE) sobre si una sesión `claude --dangerously-skip-permissions` emite un plan **capturable** vía un hook **soportado** de Claude Code (`PostToolUse:ExitPlanMode` o equivalente del barrido D-02), mediante un experimento real y reproducible. El entregable es el documento `45-SPIKE.md`.

Purpose: Este veredicto es un **gate duro** del milestone v0.11 — gobierna si Phase 46 (PLAN-04, captura no-GSD) se ejecuta o se difiere a v2. El roadmapper de Phase 46 lee la primera línea del documento para decidir ejecutar vs cortar.

Output: `45-SPIKE.md` con las 4 secciones de D-05 (veredicto binario · hipótesis y método · evidencia reproducible · contrato de captura si VIABLE | decisión de diferir si INVIABLE). Cero código de producción commiteado (D-06): el script de volcado es throwaway y se desinstala en teardown.

**Naturaleza spike (cercos duros — NO violar):**
- Cero implementación de producción. `git diff -- src/ test/ bin/` debe quedar **vacío** al cerrar. El único artefacto de código es `spike-dump.js` (~15 líneas, throwaway, fuera de `src/`).
- Solo caminos soportados. PROHIBIDO parsear transcript JSONL crudo, `~/.claude/plans/`, `~/.claude/todos/` (D-07). Si ningún hook soportado porta el plan → INVIABLE, **no** se busca workaround vía rutas internas.
- Teardown obligatorio y seguro: backup byte-idéntico de `~/.claude/settings.json` antes de instrumentar; restore después; verificar que los hooks producción de kodo (SessionStart/Stop) persisten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-CONTEXT.md
@.planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-RESEARCH.md
@.planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-PATTERNS.md

<interfaces>
<!-- Andamiaje kodo a IMITAR (NO modificar — spike puro). Extraído del codebase. -->
<!-- El executor copia readStdin verbatim al spike-dump.js y replica el shape de addHook en el settings.json de prueba. -->

From src/hooks/session-start.js:185-195 (readStdin — COPIAR VERBATIM al spike-dump.js):
```javascript
const STDIN_TIMEOUT = 3000;
async function readStdin() {
  return new Promise((resolve) => {
    const timer = setTimeout(() => resolve('{}'), STDIN_TIMEOUT);
    const chunks = [];
    process.stdin.on('data', (chunk) => chunks.push(chunk));
    process.stdin.on('end', () => {
      clearTimeout(timer);
      resolve(Buffer.concat(chunks).toString());
    });
  });
}
```

From src/hooks/session-start.js:197-253 (contrato never-throws — try/catch externo silencioso):
"Silent failure — never break Claude Code startup". El spike-dump.js envuelve TODO en try/catch silencioso.

From src/hooks/install.js:6,91-109 (shape de addHook — REPLICAR en el settings.json de prueba, NO invocar):
```javascript
const SETTINGS_PATH = join(homedir(), '.claude', 'settings.json'); // línea 6
// Shape de inserción no-clobber (aditivo .push, preserva entradas existentes):
hooks[event].push({ hooks: [{ type: 'command', command }] });
// Para PostToolUse/PreToolUse se añade un campo matcher: "ExitPlanMode".
```

From src/session/state.js:319-364 (findSession — SOLO referencia para el contrato D-04 si VIABLE; NO se invoca en el spike):
```javascript
export function findSession(query) { /* dual-scan: state.sessions + state.history, never-throws, prioridad sessions; matchea por query.sessionId y por query.cwd → session.project_path */ }
```
</interfaces>

<spike_dump_model>
<!-- Modelo del spike-dump.js (RESEARCH Pattern 1, líneas 138-167). NO va en src/. ~15 líneas. -->
```javascript
import { appendFileSync } from 'node:fs';
const LOG = process.env.KODO_SPIKE_LOG || '/tmp/kodo-spike.log';
const STDIN_TIMEOUT = 3000;
// ... readStdin verbatim (arriba) ...
(async () => {
  try {
    const raw = await readStdin();
    appendFileSync(LOG, `\n===== ${new Date().toISOString()} =====\n${raw}\n`);
  } catch { /* never break Claude Code */ }
})();
```
El valor del spike es el payload **crudo**: NO parsear `input.tool_input.plan` ciegamente (RESEARCH Pitfall 4 / Assumption A1 — el nombre del campo solo se confirma leyendo el log).
</spike_dump_model>

<settings_matrix_model>
<!-- Matriz de eventos a instrumentar a la vez en el settings.json de prueba (D-02 + RESEARCH Pattern 2, líneas 175-198). -->
<!-- AÑADIR junto a los hooks producción de kodo (SessionStart/Stop), NO reemplazarlos. -->
- PostToolUse  matcher "ExitPlanMode"  → KODO_SPIKE_LOG=/tmp/kodo-spike-posttool-exitplan.log  (HIPÓTESIS PRIMARIA D-02)
- PreToolUse   matcher "ExitPlanMode"  → KODO_SPIKE_LOG=/tmp/kodo-spike-pretool-exitplan.log
- UserPromptSubmit (sin matcher)       → KODO_SPIKE_LOG=/tmp/kodo-spike-userprompt.log
- Stop (sin matcher)                   → KODO_SPIKE_LOG=/tmp/kodo-spike-stop.log
- Notification (sin matcher)           → KODO_SPIKE_LOG=/tmp/kodo-spike-notification.log
</settings_matrix_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup del experimento — gate `claude` + backup byte-idéntico + script de volcado + instrumentación de la matriz</name>
  <files>
    /tmp/spike-dump.js (throwaway — NO en src/)
    ~/.claude/settings.json (mutación temporal — entradas de prueba añadidas)
    ~/.claude/settings.json.spike-backup (backup byte-idéntico)
  </files>
  <read_first>
    - src/hooks/session-start.js:185-195 (readStdin — copiar verbatim) y :197-253 (contrato never-throws)
    - src/hooks/install.js:6 (SETTINGS_PATH), :17-23 (carga tolerante a ausencia), :91-109 (shape addHook no-clobber)
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-RESEARCH.md §"Pattern 1" (modelo spike-dump.js), §"Pattern 2" (matriz settings.json), §"Environment Availability" (gate command -v claude)
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-PATTERNS.md §"spike-dump.js" y §"~/.claude/settings.json"
  </read_first>
  <action>
    **(Gate bloqueante primero)** Ejecutar `command -v claude` y `claude --version`. Si `claude` no está en PATH → ABORTAR el plan con el mensaje "claude CLI ausente — sin Claude Code no hay experimento" (Environment Availability: bloqueante sin fallback). Transcribir la versión exacta para anclar el veredicto (RESEARCH Pitfall 2 / Open Question #4 — el comportamiento de hooks es version-specific).

    **Backup byte-idéntico:** `cp ~/.claude/settings.json ~/.claude/settings.json.spike-backup`. Si `~/.claude/settings.json` no existe, crear `{}` (espejo del fallback de install.js, RESEARCH §371) y registrar esa condición. Este backup es la red de seguridad del teardown (T-45-01).

    **Script de volcado throwaway:** crear `/tmp/spike-dump.js` (NO en `src/` — D-06) según `<spike_dump_model>`: `readStdin` verbatim de session-start.js + `appendFileSync(LOG, raw)` envuelto en try/catch silencioso. Una sola responsabilidad: volcar el payload **crudo** + timestamp. NO parsear, NO correlacionar, NO emitir output (RESEARCH Anti-Pattern: "volcar payload parseado en vez de crudo").

    **Instrumentar la matriz:** editar `~/.claude/settings.json` añadiendo (aditivo `.push`, sin clobber-ear las entradas producción de kodo) las 5 entradas de hook de `<settings_matrix_model>`, cada una con su `KODO_SPIKE_LOG=<ruta> node /tmp/spike-dump.js`. `PostToolUse`/`PreToolUse` llevan `matcher: "ExitPlanMode"`; `UserPromptSubmit`/`Stop`/`Notification` sin matcher. CRÍTICO: la entrada `Stop` de prueba se AÑADE al array `Stop` existente — NO reemplaza la entrada `Stop` producción de kodo.
  </action>
  <verify>
    <automated>command -v claude && node -e "const s=require(require('os').homedir()+'/.claude/settings.json'); const c=JSON.stringify(s.hooks); const prod=c.includes('session-start.js')&&c.includes('stop.js'); const spike=c.includes('spike-dump'); if(!prod) throw new Error('hooks producción kodo ausentes'); if(!spike) throw new Error('hooks de prueba no instrumentados'); console.log('OK: producción intactos + matriz instrumentada')"</automated>
  </verify>
  <acceptance_criteria>
    - `command -v claude` resuelve y `claude --version` queda transcrito para el documento.
    - `~/.claude/settings.json.spike-backup` existe y es byte-idéntico al settings.json previo (`diff` vacío en este punto, antes de instrumentar — comparar contra una copia previa a la edición).
    - `/tmp/spike-dump.js` existe, hace `appendFileSync` del payload crudo, envuelto en try/catch silencioso, sin parsear `tool_input`.
    - `~/.claude/settings.json` contiene las 5 entradas de prueba (`spike-dump`) Y conserva las entradas producción de kodo (`session-start.js` + `stop.js`).
  </acceptance_criteria>
  <done>Gate `claude` pasado y versión registrada; backup byte-idéntico creado; spike-dump.js throwaway escrito; matriz de 5 eventos instrumentada en settings.json sin clobber de los hooks producción.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ejecutar el experimento — matriz de 3 formas de entrar a plan mode bajo skip-permissions</name>
  <read_first>
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-RESEARCH.md §"Code Examples" (comandos de la matriz), §"Common Pitfalls" (1: skip-permissions evita el gate; 3: NO hacer clear context), §"Open Questions"
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-CONTEXT.md §D-02 (matriz de eventos), §D-03 (criterio "capturable")
  </read_first>
  <what-built>
    El setup (Task 1) instaló el hook de volcado `/tmp/spike-dump.js` instrumentado sobre 5 eventos soportados (`PostToolUse:ExitPlanMode` primario + barrido `PreToolUse:ExitPlanMode`/`UserPromptSubmit`/`Stop`/`Notification`). Cada evento vuelca su payload crudo a `/tmp/kodo-spike-<event>.log`.

    Esta verificación requiere una **sesión interactiva real de Claude Code** que NO es automatizable (depende de un proceso `claude` interactivo con plan mode + interacción humana real). Por eso es un checkpoint human-action.
  </what-built>
  <how-to-verify>
    Ejecutar la matriz de 3 formas de entrar a plan mode bajo skip-permissions (RESEARCH Code Examples). Para CADA combinación, en una sesión limpia, lanzar un prompt que fuerce un plan (p. ej. "Investiga el codebase y proponme un plan para refactorizar X. No edites nada todavía."), **aceptar el plan SIN hacer clear context** (RESEARCH Pitfall 3), y salir:

    1. `claude --dangerously-skip-permissions` (skip-permissions solo; toggle a plan mode con Shift+Tab si hace falta).
    2. `claude --dangerously-skip-permissions --permission-mode plan` (plan mode al arrancar; ojo bug #17544 — el bypass podría anular plan mode: documentar lo que observes).
    3. `claude --dangerously-skip-permissions`, luego `Shift+Tab` a plan mode dentro de la sesión (bug #32934).

    Tras cada corrida, observar: ¿se imprimió un plan en pantalla? ¿qué ficheros `/tmp/kodo-spike-*.log` se crearon/crecieron?

    NO descender a parsear el transcript JSONL ni rutas internas si los hooks fallan (D-07) — un fallo de todos los hooks soportados ES el resultado INVIABLE.
  </how-to-verify>
  <acceptance_criteria>
    - Las 3 combinaciones de la matriz se ejecutaron, cada una forzando un plan visible en pantalla, sin clear context.
    - Para cada combinación se registró qué logs `/tmp/kodo-spike-*.log` aparecieron y cuáles quedaron vacíos.
    - Los payloads crudos capturados (o la ausencia de fichero) están disponibles en `/tmp/` para que Task 3 los transcriba — NO se borran antes del análisis.
  </acceptance_criteria>
  <resume-signal>Escribe "experimento ejecutado" cuando las 3 corridas estén hechas y los logs `/tmp/kodo-spike-*.log` (o su ausencia) estén listos para análisis. Si `--dangerously-skip-permissions` no produjo ningún plan capturable en ninguna combinación, indícalo — es un resultado válido (INVIABLE).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Análisis + veredicto binario + escribir `45-SPIKE.md`</name>
  <files>.planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-SPIKE.md</files>
  <read_first>
    - /tmp/kodo-spike-*.log (los payloads crudos capturados en Task 2 — la evidencia)
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-CONTEXT.md §D-03 (criterio "capturable": (a) payload contiene el plan Y (b) correlacionable vía session_id/cwd), §D-04 (los 5 puntos del contrato si VIABLE), §D-05 (las 4 secciones del documento)
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-RESEARCH.md §"Pattern 3" / §"Shared Patterns" (modelo del contrato de captura), §"Pitfall 4" (confirmar el nombre real del campo del plan en el log crudo)
    - src/session/state.js:319-364 (findSession — solo para describir la correlación (4) del contrato si VIABLE; NO se invoca)
  </read_first>
  <action>
    Inspeccionar cada `/tmp/kodo-spike-*.log`: para cada evento de la matriz, determinar si el payload (a) **contiene el texto del plan** y (b) es **correlacionable** (`session_id` y/o `cwd` presentes en el payload). Confirmar el **nombre literal** del campo que porta el plan leyéndolo del log crudo — NO asumir `tool_input.plan` (RESEARCH Pitfall 4 / Assumption A1).

    Aplicar el criterio D-03: un evento es "capturable" SOLO si cumple (a) Y (b). Disparar sin portar el plan, o sin correlación = INVIABLE para ese evento. El veredicto global es **VIABLE** si al menos un evento soportado es capturable; **INVIABLE** si ninguno lo es (tras agotar el barrido D-02 — no declarar INVIABLE sin haber revisado todos los logs del barrido, RESEARCH Anti-Pattern).

    Escribir `45-SPIKE.md` con las 4 secciones de D-05:
    1. **Veredicto binario** (VIABLE | INVIABLE) en la primera línea, anclado a la versión de `claude` (de Task 1).
    2. **Hipótesis y método:** qué hooks se instalaron (la matriz), qué comandos se lanzaron (las 3 combinaciones).
    3. **Evidencia reproducible:** los comandos exactos + los payloads crudos transcritos literalmente del log (o la ausencia documentada para los logs vacíos), por combinación de la matriz.
    4. Si **VIABLE**: el contrato de captura propio de kodo con los 5 puntos de D-04 — (1) qué evento, (2) el campo del plan **verificado en el log** (no el asumido), (3) dónde persiste kodo (su propio side, NUNCA `~/.claude/plans/`), (4) correlación vía `findSession` (`session_id`/`cwd` → `task_id`), (5) cómo el overlay `mode:'overlay'` de Phase 44 lo reusa. Si **INVIABLE**: la decisión explícita de **diferir PLAN-04 a v2** (PLAN-F1/PLAN-F2 lo anticipan) sin bloquear el cierre del milestone (Phases 44/45/47 cierran v0.11).

    NO escribir código de producción ni proponer parsing de transcript/rutas internas (D-06/D-07). El documento ESPECIFICA el contrato; no lo implementa.
  </action>
  <verify>
    <automated>test -f .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-SPIKE.md && grep -E -q 'VIABLE|INVIABLE' .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-SPIKE.md && echo "OK: documento con veredicto presente"</automated>
  </verify>
  <acceptance_criteria>
    - `45-SPIKE.md` existe con un veredicto binario (VIABLE | INVIABLE) explícito en la primera sección, anclado a la versión de `claude`.
    - El documento tiene las 4 secciones de D-05; la sección (3) transcribe los payloads crudos (o ausencia) por combinación de la matriz.
    - El criterio D-03 ((a) plan + (b) correlación) se aplica explícitamente por evento; el nombre del campo del plan es el verificado en el log, no el asumido.
    - Si VIABLE: los 5 puntos de D-04 están presentes. Si INVIABLE: la decisión de diferir PLAN-04 a v2 está registrada sin bloquear el milestone.
    - El documento es reproducible: un tercero puede repetir el experimento siguiéndolo.
  </acceptance_criteria>
  <done>Veredicto binario alcanzado con evidencia transcrita; `45-SPIKE.md` completo con las 4 secciones de D-05; contrato (si VIABLE) o diferimiento (si INVIABLE) documentado.</done>
</task>

<task type="auto">
  <name>Task 4: Teardown obligatorio — restore byte-idéntico + verificar hooks producción + limpiar evidencia</name>
  <files>
    ~/.claude/settings.json (restaurado byte-idéntico al backup)
    /tmp/spike-dump.js (eliminado)
    /tmp/kodo-spike-*.log (eliminados tras transcribir la evidencia)
    ~/.claude/settings.json.spike-backup (eliminado tras restore)
  </files>
  <read_first>
    - .planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-RESEARCH.md §"Teardown obligatorio (D-06)" (líneas 313-321), §"Runtime State Inventory" (qué estado retirar), §"Security Domain" (T-45-01)
    - src/hooks/install.js:65-73 (filtro de desinstalación selectivo — fallback si el restore byte-idéntico no aplica)
  </read_first>
  <action>
    Restaurar `~/.claude/settings.json` desde el backup byte-idéntico: `cp ~/.claude/settings.json.spike-backup ~/.claude/settings.json` (enfoque byte-idéntico preferido, RESEARCH §200). Verificar con `diff` que el settings.json restaurado == backup. Confirmar que los hooks producción de kodo (`SessionStart` con `session-start.js` + `Stop` con `stop.js`) están intactos y que NINGUNA entrada `spike-dump` sobrevive (mitigación T-45-01).

    Limpiar la evidencia efímera SOLO tras confirmar que Task 3 ya la transcribió al documento (el log `/tmp/kodo-spike-*.log` puede contener texto de planes potencialmente sensible — RESEARCH Runtime State Inventory / security note): `rm -f /tmp/kodo-spike-*.log /tmp/spike-dump.js ~/.claude/settings.json.spike-backup`.

    Verificar el invariante spike puro: `git diff -- src/ test/ bin/` debe estar VACÍO (D-06 — cero código de producción commiteado; el único cambio versionado es `45-SPIKE.md` + artefactos GSD).
  </action>
  <verify>
    <automated>node -e "const s=require(require('os').homedir()+'/.claude/settings.json'); const c=JSON.stringify(s.hooks); if(c.includes('spike-dump')) throw new Error('teardown incompleto: entrada de prueba sobrevive'); if(!c.includes('session-start.js')||!c.includes('stop.js')) throw new Error('hooks producción kodo dañados'); console.log('OK: teardown limpio, producción intactos')" && test -z "$(git -C /Users/alex/dev/klab/kodo diff -- src/ test/ bin/)" && echo "OK: git diff src/test/bin vacío"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.claude/settings.json` restaurado byte-idéntico al backup (`diff` vacío); ninguna entrada `spike-dump` sobrevive.
    - Los hooks producción de kodo (`session-start.js` + `stop.js`) están intactos (T-45-01 mitigado).
    - `/tmp/spike-dump.js`, `/tmp/kodo-spike-*.log` y `settings.json.spike-backup` eliminados (tras transcribir la evidencia al documento).
    - `git diff -- src/ test/ bin/` vacío (D-06).
  </acceptance_criteria>
  <done>settings.json restaurado byte-idéntico sin clobber de hooks producción; evidencia efímera limpiada; cero código de producción en el repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code (host externo) → hook script | El payload por stdin es output del modelo (texto arbitrario, no confiable); el spike lo vuelca crudo sin parsear (cero superficie de inyección por parseo en esta fase). |
| spike (proceso temporal) → `~/.claude/settings.json` | El experimento muta un fichero del usuario FUERA del repo que también contiene los hooks producción de kodo — principal superficie de riesgo de la fase. |
| spike → `/tmp/kodo-spike-*.log` | El log puede contener texto de planes potencialmente sensible; debe limpiarse tras el análisis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `~/.claude/settings.json` (teardown) | mitigate | **Bloqueante (severidad high).** Backup byte-idéntico antes de instrumentar (Task 1) + restore verificado con `diff` (Task 4) + asserción de que `SessionStart`/`Stop` producción de kodo persisten y ninguna entrada `spike-dump` sobrevive. Teardown incompleto dejaría el hook de volcado activo → fuga de payloads a `/tmp` en sesiones futuras y/o clobber de hooks producción. |
| T-45-02 | Denial of Service | `/tmp/spike-dump.js` (hook de prueba) | mitigate | El script es never-throws (try/catch externo silencioso, espejo del contrato kodo): un fallo del hook nunca crashea `claude`. No parsea el payload (solo `appendFileSync`). |
| T-45-03 | Information Disclosure | `/tmp/kodo-spike-*.log` | mitigate | El log puede contener texto de planes sensible. Task 4 lo elimina (`rm -f`) tras transcribir la evidencia al documento. No se commitea ningún log al repo. |
| T-45-04 | Tampering / Injection | plan text como dato no confiable (Phase 46) | accept | Fuera del scope del spike (no se persiste/renderiza nada aquí). Nota para D-04: el plan es output del modelo; el overlay read-only never-throws de Phase 44 lo trataría como texto plano. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No aplica — spike puro, cero paquetes nuevos (RESEARCH §67: "no hay npm install"). No hay `## Package Legitimacy Audit` porque no hay instalaciones. |
</threat_model>

<verification>
- `45-SPIKE.md` existe con veredicto binario explícito (VIABLE | INVIABLE) en la primera sección y las 4 secciones de D-05.
- El veredicto está anclado a una versión concreta de `claude` (`claude --version` transcrita).
- La evidencia (comandos + payloads crudos o ausencia) es reproducible por un tercero.
- `~/.claude/settings.json` restaurado byte-idéntico; hooks producción de kodo (`session-start.js` + `stop.js`) intactos; ninguna entrada `spike-dump` sobrevive.
- `git diff -- src/ test/ bin/` vacío (cero código de producción — D-06).
- `npm test` (suite del repo) sigue verde — el experimento no rompió nada (no debería: spike puro no toca `src/`/`test/`).
- Cero tareas que parseen transcript JSONL / `~/.claude/plans/` / `~/.claude/todos/` (D-07 honrado).
</verification>

<success_criteria>
1. (PLAN-03 SC#1) Existe `45-SPIKE.md` que concluye, con evidencia reproducible, si `ExitPlanMode` (u otro evento soportado del barrido) dispara bajo `--dangerously-skip-permissions` y produce un payload con el plan.
2. (PLAN-03 SC#2) La conclusión es un veredicto binario accionable: VIABLE (con el mecanismo de hook concreto) o INVIABLE (con la evidencia que lo justifica), en la primera línea.
3. (PLAN-03 SC#3) Si VIABLE, el documento especifica el contrato de captura propio (los 5 puntos de D-04); si INVIABLE, registra la decisión de diferir PLAN-04 a v2 sin bloquear el cierre del milestone.
4. (D-06) Teardown completo: settings.json byte-idéntico, hooks producción intactos, `git diff -- src/ test/ bin/` vacío.
</success_criteria>

<output>
Create `.planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-01-SUMMARY.md` when done.
El artefacto principal de la fase es `.planning/phases/45-spike-captura-de-plan-no-gsd-v-a-hook/45-SPIKE.md` (el veredicto binario que Phase 46 lee como gate).
</output>
