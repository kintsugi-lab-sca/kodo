---
phase: 41-doctor-m-dulo-puro-de-saneo-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/worktree-cleanup.js
  - src/hooks/stop.js
  - src/logger-events.js
  - test/stop-worktree-cleanup.test.js
  - test/worktree-cleanup.test.js
autonomous: true
requirements: [DOCTOR-02, DOCTOR-04]
must_haves:
  truths:
    - "stop.js worktree cleanup behaves identically before and after the factorization (no regression)"
    - "A single shared helper performs branch-read → status → remove|move-to-.dirty → prune, fail-open"
    - "doctor.* events are registered in the EVENTS registry and emit at info/warn/error levels with token=0"
    - "Decisions implemented in this plan: D-11 — worktree cleanup mirrors stop.js:251-402 verbatim (branch-before-remove, no --force, dirty→.dirty, no rm -rf)"
  artifacts:
    - path: "src/hooks/worktree-cleanup.js"
      provides: "Shared fail-open worktree cleanup helper (branch read, remove vs move-to-.dirty, prune) consumed by stop.js and doctor.js"
      exports: ["cleanupWorktree"]
      min_lines: 60
    - path: "src/logger-events.js"
      provides: "doctor.* event helpers mirroring worktreeCleanup* shape"
      contains: "DOCTOR_FIX_WORKTREE"
    - path: "test/worktree-cleanup.test.js"
      provides: "Direct unit coverage of the extracted helper (clean/dirty/error paths, gitFn stub, never-throws)"
  key_links:
    - from: "src/hooks/stop.js"
      to: "src/hooks/worktree-cleanup.js"
      via: "import cleanupWorktree"
      pattern: "worktree-cleanup"
    - from: "src/hooks/worktree-cleanup.js"
      to: "src/logger-events.js"
      via: "worktreeCleanupOk/Dirty/Error injected logger calls"
      pattern: "worktreeCleanup(Ok|Dirty|Error)"
---

<objective>
Extract the hardened worktree-cleanup block from `src/hooks/stop.js` (lines 251-402) into a shared, DI'd, fail-open helper `src/hooks/worktree-cleanup.js` consumed by both `stop.js` and (in Plan 02) `doctor.js` — the "una sola fuente de saneo" mandated by D-11. Also register the `doctor.*` observability events in `logger-events.js`.

This plan is sequenced FIRST and isolated deliberately: factoring production happy-path cleanup code is the single highest-regression-risk action in the phase (per planner guidance / pattern-mapper finding). `stop.js`'s existing contractual behavior MUST remain byte-for-byte equivalent — verified by `test/stop-worktree-cleanup.test.js` staying green before AND after the change.

Purpose: Give Plan 02's `doctor.js` a tested, reusable cleanup primitive so doctor never reimplements `git worktree remove/move/prune` and never introduces `rm -rf`.
Output: `src/hooks/worktree-cleanup.js` (new), refactored `src/hooks/stop.js` call site (behavior verbatim), `doctor.*` events in `logger-events.js`, new direct unit test for the helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-doctor-m-dulo-puro-de-saneo-cli/41-CONTEXT.md
@.planning/phases/41-doctor-m-dulo-puro-de-saneo-cli/41-PATTERNS.md

<interfaces>
<!-- Current cleanup block in stop.js:251-402 (the source to factor). gitFn signature: -->
<!-- gitFn(projectPath, argsArray) => Promise<string>. It internally prepends `-C <project>`; -->
<!-- git accepts composable `-C`, so the block passes `['-C', wt, ...]` for worktree-scoped reads. -->

From src/logger-events.js (event helper shape to mirror):
```javascript
export function worktreeCleanupError(logger, fields) {
  logger.error(EVENTS.WORKTREE_CLEANUP_ERROR, {
    event: EVENTS.WORKTREE_CLEANUP_ERROR,
    session_id: fields.session_id,
    worktree_path: fields.worktree_path,
    phase: fields.phase,   // 'status'|'remove'|'move'|'branch'|'prune'
    reason: fields.reason,
  });
}
```
EVENTS is `Object.freeze({...})` at logger-events.js:52-77. WORKTREE_CLEANUP_* live at :61-63.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract cleanupWorktree() into src/hooks/worktree-cleanup.js</name>
  <files>src/hooks/worktree-cleanup.js, test/worktree-cleanup.test.js</files>
  <read_first>
    - src/hooks/stop.js (lines 244-402 — the exact block being extracted; read the full sequence and its outer try/catch)
    - src/logger-events.js (worktreeCleanupOk/Dirty/Error at :305-351 — the event helpers the block calls)
    - src/session/reconcile.js (header banner :1-27 + DI runner signature :254 — the pure+DI+never-throws doc convention to replicate)
  </read_first>
  <action>
    Create `src/hooks/worktree-cleanup.js` exporting `async function cleanupWorktree({ project, worktree, sessionId, gitFn, logger })`. Move the stop.js:272-397 sequence VERBATIM into it: (1) read branch via `gitFn(project, ['-C', worktree, 'branch', '--show-current'])` BEFORE remove, fail-open silent (Pitfall #2); (2) `gitFn(project, ['-C', worktree, 'status', '--porcelain'])` dirty check — on failure emit `worktreeCleanupError({phase:'status'})` and set isDirty=null (skip remove/move, still prune); (3) CLEAN path: `gitFn(project, ['worktree', 'remove', worktree])` WITHOUT `--force`, then `gitFn(project, ['branch', '-D', branchName])` fail-open (Pitfall #3 → cleanup.ok with branch_deleted=false), emit `worktreeCleanupOk`; (4) DIRTY path: move-aside to `${worktree}.dirty` with `lstatSync` collision pre-check forcing `${worktree}.dirty-${Date.now()}` on any non-ENOENT (Pitfall #1), fallback `renameSync` + `gitFn(project,['worktree','repair',target])`, emit `worktreeCleanupDirty({moved_to})`, NEVER delete dirty; (5) opportunistic `gitFn(project, ['worktree', 'prune'])` fail-open with `worktreeCleanupError({phase:'prune'})`. Import `lstatSync`/`renameSync` from `node:fs` and the three event helpers from `./logger-events.js` at module top (static import OK — this is the hook path, NOT the LOG-12-restricted no-logger path; the `logger` itself stays injected per `deps`, the module never imports `logger.js`). Replicate the `reconcile.js` doc-banner declaring fail-open/never-throws. Return `{ removed: boolean, moved_to: string|null, branch_deleted: boolean }` so doctor (Plan 02) can report the exact per-item action (D-08). NO `rm -rf`, NO `unlinkSync` of the worktree dir.
  </action>
  <verify>
    <automated>node --test test/worktree-cleanup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/worktree-cleanup.js exports cleanupWorktree`
    - `grep -c "rm -rf" src/hooks/worktree-cleanup.js` returns 0
    - helper never imports `../logger.js`: `grep -v '^//' src/hooks/worktree-cleanup.js | grep -c "logger.js'" ` returns 0 (the injected `logger` arg is used, logger-events.js is allowed)
    - `git worktree remove` is invoked WITHOUT `--force`: no `'--force'` token appears adjacent to a `'remove'` worktree call
    - dirty path uses `${worktree}.dirty` target and never deletes it
  </acceptance_criteria>
  <done>cleanupWorktree exists with the full clean/dirty/error/prune sequence verbatim from stop.js, fail-open, returns a structured per-item result, contains no rm -rf.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire stop.js to consume cleanupWorktree (behavior verbatim) + register doctor.* events</name>
  <files>src/hooks/stop.js, src/logger-events.js</files>
  <read_first>
    - src/hooks/stop.js (lines 244-402 — the call site to replace; preserve the outer defensive try/catch and the legacy-session skip-without-warn at :249-251)
    - src/hooks/worktree-cleanup.js (the helper created in Task 1 — its exact signature and return shape)
    - src/logger-events.js (EVENTS registry :52-77 + worktreeCleanup* helpers :305-351 — the registration and helper-function molds)
    - test/stop-worktree-cleanup.test.js (the contractual test that MUST stay green — read its assertions on emitted events)
  </read_first>
  <action>
    In `src/hooks/stop.js`, replace the inlined cleanup block (lines ~272-397) with a single `await cleanupWorktree({ project: session.project_path, worktree: session.worktree_path, sessionId: session.session_id, gitFn, logger: cleanupLog })`. KEEP the existing `if (session.worktree_path) { ... }` guard, the legacy-session silent skip (D-09), the `cleanupLog` construction (loggerFactory DI or lazy createLogger child), and the outer defensive try/catch (runStopHook must NEVER crash) verbatim. Add `import { cleanupWorktree } from './worktree-cleanup.js'` (or keep the existing dynamic-import style if that is what stop.js uses for siblings — match the file's convention). Then in `src/logger-events.js`: add to the frozen `EVENTS` object (next to WORKTREE_CLEANUP_*): `DOCTOR_SCAN: 'doctor.scan'`, `DOCTOR_FIX_WORKTREE: 'doctor.fix.worktree'`, `DOCTOR_FIX_LOCK: 'doctor.fix.lock'`, `DOCTOR_FIX_LOG: 'doctor.fix.log'`, `DOCTOR_FIX_ERROR: 'doctor.fix.error'`. Add helper functions mirroring `worktreeCleanupError`: `doctorFixWorktree(logger, fields)` (info, fields: session_id, worktree_path, action ['remove'|'prune'|'moved'], moved_to), `doctorFixLock(logger, fields)` (info/warn, fields: project_path, decision ['stolen'|'kept'], pid, reason), `doctorFixLog(logger, fields)` (info, fields: log_path, session_id), `doctorFixError(logger, fields)` (error, fields: category ['worktree'|'lock'|'log'|'zombie'], reason, target). All token-free (filesystem/git ops, no model call — these helpers do NOT add a `tokens` field, matching worktreeCleanup*).
  </action>
  <verify>
    <automated>node --test test/stop-worktree-cleanup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/stop-worktree-cleanup.test.js` passes unchanged (no assertion edits needed) — proves stop.js behavior is verbatim
    - `src/hooks/stop.js` references `cleanupWorktree` and no longer inlines the remove/move/prune sequence: `grep -c "worktree.*remove\|worktree.*move\|worktree.*prune" src/hooks/stop.js` returns 0 (those tokens now live only in the helper)
    - `src/logger-events.js` EVENTS contains `DOCTOR_FIX_WORKTREE`, `DOCTOR_FIX_LOCK`, `DOCTOR_FIX_LOG`, `DOCTOR_FIX_ERROR`, `DOCTOR_SCAN`
    - `src/logger-events.js exports doctorFixWorktree` / `doctorFixLock` / `doctorFixLog` / `doctorFixError`
  </acceptance_criteria>
  <done>stop.js delegates cleanup to the shared helper with no behavior change (contractual test green), and doctor.* events + helpers are registered for Plan 02 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stop hook → git/filesystem | The cleanup helper deletes/moves worktree directories on disk via git |
| refactor → production happy-path | Extracting hardened code risks silently changing stop.js cleanup semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | cleanupWorktree (refactor of stop.js:251-402) | mitigate | `test/stop-worktree-cleanup.test.js` MUST stay green with zero assertion edits — proves byte-equivalent behavior. New `test/worktree-cleanup.test.js` covers the helper directly (clean/dirty/error paths). |
| T-41-02 | Denial of Service | dirty worktree handling | mitigate | DIRTY path NEVER deletes — moves to `${worktree}.dirty` with `lstatSync` collision pre-check (Pitfall #1). `git worktree remove` WITHOUT `--force` (git acts as second guard against removing dirty trees). Acceptance criteria assert no `rm -rf`, no `--force`. |
| T-41-03 | Repudiation | doctor.* events | mitigate | doctor.* helpers emit at info/warn/error with a fixed whitelist of fields (no `...spread`), token-free — every destructive action in Plan 02 is auditable in NDJSON. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — extraction + event registration only, all stdlib (`node:fs`). No third-party dependency added. |
</threat_model>

<verification>
- `node --test test/worktree-cleanup.test.js` passes (helper covered directly)
- `node --test test/stop-worktree-cleanup.test.js` passes UNCHANGED (regression gate)
- `grep -c "rm -rf" src/hooks/worktree-cleanup.js` == 0
- EVENTS registry contains all 5 doctor.* keys
</verification>

<success_criteria>
- Shared `cleanupWorktree` helper exists, fail-open, returns structured per-item result, no rm -rf, remove without --force, dirty never deleted.
- stop.js consumes the helper with verbatim behavior (contractual test green).
- doctor.* events registered with token-free, fixed-field helpers ready for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/41-doctor-m-dulo-puro-de-saneo-cli/41-01-SUMMARY.md` when done
</output>
