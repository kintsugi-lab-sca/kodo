---
phase: 52-createtask-contrato-anti-recursi-n
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/providers/plane/client.js
  - src/providers/plane/provider.js
autonomous: false
requirements: [BIDIR-01]
user_setup: []

must_haves:
  truths:
    - "El adapter Plane crea una work-item vía createTask y devuelve un TaskItem canónico shape-idéntico a uno fetcheado (url + state válidos)"
    - "El TaskItem devuelto tiene task_id en formato ${identifier}-${sequence_id} y url web_url/browse válida"
    - "La tarea se crea en el estado in-progress/trigger configurado (D-04), nunca en Backlog"
    - "La tarea creada lleva el marker kodo:adopted aplicado en Plane antes de devolver (sobrevive --force en el siguiente poll tick)"
    - "TASK_PROVIDER_METHODS permanece FROZEN en 9 — createTask NO se añade a la lista"
  artifacts:
    - path: "src/providers/plane/client.js"
      provides: "createWorkItem(projectId, fields) transport POST + createLabel(projectId, name) para el marker UUID"
      contains: "createWorkItem"
    - path: "src/providers/plane/provider.js"
      provides: "createTask({projectId, title, description}) typeof-detected, fuera de los 9 FROZEN"
      contains: "async createTask"
  key_links:
    - from: "src/providers/plane/provider.js"
      to: "src/providers/plane/client.js"
      via: "client.createWorkItem"
      pattern: "client\\.createWorkItem"
    - from: "src/providers/plane/provider.js"
      to: "src/providers/plane/normalize.js"
      via: "normalizeWorkItem con contexto completo de 6 campos"
      pattern: "normalizeWorkItem\\("
    - from: "src/providers/plane/provider.js"
      to: "src/labels.js"
      via: "import KODO_LABEL_ADOPTED (no inline)"
      pattern: "KODO_LABEL_ADOPTED"
---

<objective>
Entregar `createTask` como método **opcional typeof-detected** en el adapter Plane (BIDIR-01), reusando el transporte POST autenticado existente. La work-item se crea en el estado in-progress/trigger configurado (D-04), se le aplica el marker `kodo:adopted` (label-UUID lookup-or-create — la pieza de Plane de la decisión de Open Q1), y la respuesta 201 se normaliza al `TaskItem` canónico vía `normalizeWorkItem` con el **contexto completo de 6 campos** (Pitfall 2).

Purpose: Primera capacidad de creación de tareas de kodo en su historia, sin romper el contrato FROZEN-at-9. Phase 53 (`adoptSession`) consumirá este método vía `typeof`-gate.
Output: `src/providers/plane/client.js` (+`createWorkItem`, +`createLabel`), `src/providers/plane/provider.js` (+`createTask`).

Depende de Plan 01 (Wave 1): consume `KODO_LABEL_ADOPTED` exportado desde `src/labels.js` (NO inline — source-hygiene REPORT-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-CONTEXT.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-RESEARCH.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md

<interfaces>
<!-- Contratos extraídos del codebase — verificados contra HEAD 2026-06-16. -->

Plane client transport (analog — src/providers/plane/client.js:175-180 createComment):
  async createComment(projectId, workItemId, commentHtml) {
    return this.request(`/projects/${projectId}/work-items/${workItemId}/comments/`, { method: 'POST', body: { comment_html: commentHtml } });
  }
  request() (:23-92) ya hace auth X-API-Key + 10s timeout + rate-limit retry + error throw (:70-72 lanza Error('Plane API ${status}: ...')); devuelve res.json() crudo.
  Path list/create work-items (trailing slash LOAD-BEARING, Pitfall 3): `/projects/${projectId}/work-items/` (idéntico a listWorkItems :107)

Plane provider caches del closure (src/providers/plane/provider.js):
  stateByName: Map<projectId, Map<stateName, stateId>>  (:41; refresh-on-miss en updateTaskState :200-216)
  stateCache: Map<stateId, stateName>
  labelCache: Map de labels existentes {id,name}
  config: { baseUrl, webUrl, workspaceSlug, projects:[{id, identifier}], states:{ trigger } }

updateTaskState state-resolve a espejar (:200-216):
  let stateId = stateByName.get(task.projectId)?.get(stateName);
  if (!stateId) { const states = await client.listStates(task.projectId); ... stateByName.set(...); stateId = byName.get(stateName); }

normalizeWorkItem(raw, context) — src/providers/plane/normalize.js:65-97 (REUSE, NO editar).
  context REQUERIDO (6 campos, Pitfall 2 — listPendingTasks :271-278):
    { labels: labelCache, projectIdentifier: proj.identifier, baseUrl, webUrl, workspaceSlug, stateMap: stateCache }
  Parcial → url:undefined o state:undefined (rompe D-06).

getTaskState (precedente método opcional — :235-263):
  // OPTIONAL method (NOT in TASK_PROVIDER_METHODS — FROZEN at 9, D-13). Detected at
  // the call site via `typeof provider.getTaskState === 'function'`.

KODO_LABEL_ADOPTED — exportado por Plan 01 desde src/labels.js (= 'kodo:adopted').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createWorkItem + createLabel en plane/client.js</name>
  <read_first>
    - src/providers/plane/client.js (archivo modificado; createComment :175-180, request() :23-92, listWorkItems path :107, listStates :95-98)
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (sección "src/providers/plane/client.js")
  </read_first>
  <files>src/providers/plane/client.js</files>
  <action>
    Añadir `createWorkItem(projectId, fields)` espejo EXACTO de `createComment` (:175-180): `return this.request(\`/projects/${projectId}/work-items/\`, { method: 'POST', body: fields })`. El path es byte-idéntico a `listWorkItems` (:107) — trailing slash obligatorio (Pitfall 3: Plane es trailing-slash-strict; sin `/` da 404 solo en create). `fields` es `{ name, description_html?, state?, labels? }`: `name` required, `state` es un state UUID (no nombre), `labels` es array de UUIDs, `description_html` es HTML. NO envolver en try/catch que trague — `request()` ya lanza en non-ok (:70-72) y create es una mutación que falla LOUD (D-08).

    Añadir `createLabel(projectId, name, color?)` con el mismo patrón POST: `return this.request(\`/projects/${projectId}/labels/\`, { method: 'POST', body: { name, color: color || '#6b7280' } })`. Resuelve la pieza Plane de Open Q1: el marker `kodo:adopted` es un label-UUID que debe existir antes del POST de la work-item. JSDoc en ambos espejando el de createComment.
  </action>
  <verify>
    <automated>node -e "import('./src/providers/plane/client.js').then(m => { const C = m.PlaneClient || m.default; if (typeof C.prototype.createWorkItem !== 'function') throw new Error('createWorkItem missing'); if (typeof C.prototype.createLabel !== 'function') throw new Error('createLabel missing'); console.log('OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - source: `createWorkItem` y `createLabel` existen en `PlaneClient.prototype`, ambos delegan en `this.request` con `method: 'POST'`
    - source: el path de `createWorkItem` es `/projects/${projectId}/work-items/` con trailing slash
    - behavior: ninguno contiene un `catch` que devuelva un default (propagación LOUD load-bearing, D-08)
    - test-command: `node --test test/providers/contract.test.js` sigue verde (el it() de createTask llega en Plan 03)
  </acceptance_criteria>
  <done>`createWorkItem` y `createLabel` añadidos a PlaneClient, reusando `request()` con POST, sin swallow, paths con trailing slash.</done>
</task>

<task type="auto">
  <name>Task 2: createTask typeof-detected en plane/provider.js</name>
  <read_first>
    - src/providers/plane/provider.js (archivo modificado; getTaskState :235-263 incl. comentario :235-236, updateTaskState state-resolve :200-218, listPendingTasks contexto normalize :265-284, caches :41 + labelCache, config.states.trigger)
    - src/providers/plane/normalize.js (normalizeWorkItem :65-97 — REUSE; resolveWorkItemLabels :41-54 para el shape UUID de labels)
    - src/labels.js (KODO_LABEL_ADOPTED exportado por Plan 01)
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (sección "src/providers/plane/provider.js")
  </read_first>
  <files>src/providers/plane/provider.js</files>
  <action>
    Importar `KODO_LABEL_ADOPTED` desde `labels.js` (verificar la profundidad real del path relativo desde `src/providers/plane/provider.js` hacia `src/labels.js`; NO literal inline — source-hygiene REPORT-05).

    Añadir `async createTask({ projectId, title, description })` dentro del object literal `provider`, OUTSIDE de los 9, al mismo nivel que `getTaskState`. Copiar verbatim el comentario de método opcional de :235-236 cambiando `getTaskState`→`createTask`.

    Cuerpo (D-04, D-05, D-06, D-02, Open Q1):
    1. `proj = config.projects.find((p) => p.id === projectId)`.
    2. `const html = description ? '<p>' + description.replace(/\n/g, '<br>') + '</p>' : ''`.
    3. Resolver el state UUID del estado trigger (D-04) espejando updateTaskState refresh-on-miss contra `stateByName`: leer `stateByName.get(projectId)?.get(config.states.trigger)`; si falta, `await client.listStates(projectId)`, poblar `stateCache` (id→name) y `byName` (name→id), `stateByName.set(projectId, byName)`, tomar `byName.get(config.states.trigger)` (puede quedar undefined → Plane usa el default del proyecto; NO lanzar).
    4. Resolver/crear el label UUID del marker: buscar en `labelCache` el `{id,name}` cuyo name (case-insensitive) === `KODO_LABEL_ADOPTED` → usar su `id`; si no existe, `await client.createLabel(projectId, KODO_LABEL_ADOPTED)`, registrar el `{id,name}` devuelto en `labelCache`, usar el nuevo `id`. (Open Q1 decidido: lookup-or-create ANTES del POST para que el marker sobreviva --force en el siguiente tick.)
    5. POST: `const raw = await client.createWorkItem(projectId, { name: title, description_html: html, ...(stateId ? { state: stateId } : {}), labels: [adoptedLabelId] })`.
    6. Normalizar con el CONTEXTO COMPLETO de 6 campos (Pitfall 2, copiado de listPendingTasks :271-278): `return normalizeWorkItem(raw, { labels: labelCache, projectIdentifier: proj?.identifier || 'UNKNOWN', baseUrl: config.baseUrl, webUrl: config.webUrl, workspaceSlug: config.workspaceSlug, stateMap: stateCache })`.

    NO envolver el POST en try/catch que trague: los errores propagan con contexto (D-08; la taxonomía de `code` la posee Phase 53).
  </action>
  <verify>
    <automated>node -e "import('./src/providers/plane/provider.js').then(()=>console.log('plane provider imports OK')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - source: `async createTask` existe en el object literal del provider, con el comentario "OPTIONAL method (NOT in TASK_PROVIDER_METHODS — FROZEN at 9..." copiado de getTaskState
    - source: `createTask` llama a `client.createWorkItem` y a `normalizeWorkItem` con un contexto que incluye las 6 claves: labels, projectIdentifier, baseUrl, webUrl, workspaceSlug, stateMap
    - source: el marker se referencia vía `KODO_LABEL_ADOPTED` importado de labels.js, NO un literal `'kodo:adopted'` inline (verificado por el hygiene test de Plan 01)
    - source: el state trigger se resuelve refresh-on-miss espejando updateTaskState (no lanza si no resuelve)
    - test-command: `node --test test/providers/contract.test.js test/labels-hygiene.test.js` verde
  </acceptance_criteria>
  <done>`createTask` añadido al provider Plane, typeof-detected fuera de los 9, crea en estado trigger, aplica el marker kodo:adopted vía label-UUID lookup-or-create, normaliza el 201 con contexto completo de 6 campos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>`createTask` en el adapter Plane: transporte POST `/work-items/` + creación en estado trigger + marker `kodo:adopted` + normalización del 201. El shape del 201 de Plane CE es el único ítem MEDIUM-confidence de la fase (Assumption A1): reads/PATCH/comments ya están verificados-por-uso, pero el create-201 NO. D-07 exige un POST manual de ~5 min para fijar el shape (`id` + `sequence_id`) antes de confiar en la normalización.</what-built>
  <how-to-verify>
    1. Con credenciales Plane CE reales (`PLANE_API_KEY`, base URL, workspace slug, un projectId de prueba), ejecutar un POST manual contra `POST {baseUrl}/api/v1/workspaces/{slug}/projects/{projectId}/work-items/` con header `X-API-Key` y body `{ "name": "kodo adopt smoke test" }`.
    2. Confirmar respuesta 201 y que el JSON incluye los campos que `normalizeWorkItem` consume: `id`, `sequence_id` (numérico), `project_detail`/`state_detail` o al menos `project`/`state` resolubles vía las caches del provider.
    3. Anotar el `sequence_id` observado y si `state`/`project_detail` vienen embebidos o requieren las caches.
    4. (Opcional) `POST .../labels/` con `{ "name": "kodo:adopted" }` devuelve un label con `id` (UUID) — confirma createLabel para Open Q1.
    5. Si el shape diverge de lo asumido (sequence_id ausente, state no resoluble), pegar el JSON crudo para ajustar la normalización antes de marcar la fase verde.
  </how-to-verify>
  <resume-signal>Escribe "approved" con el sequence_id observado, o pega el JSON 201 crudo si el shape diverge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kodo → Plane CE REST API | POST autenticado (X-API-Key) que MUTA el gestor remoto (crea work-item + posiblemente un label). Primera mutación de creación de la historia de kodo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-1 | Denial of Service | `createTask` Plane / marker `kodo:adopted` | mitigate | El marker se aplica en el create (label-UUID lookup-or-create ANTES de devolver) para que el guard `isAdopted` (Plan 01) corte el re-despacho incluso bajo `--force`. Sin el marker presente antes del siguiente poll tick, D-02 no aplicaría en Plane. |
| T-52-3 | Tampering / Repudiation | `createTask`/`createWorkItem`/`createLabel` Plane (mutaciones) | mitigate | Ninguno envuelve el POST en un catch que trague — el `Error` de `request()` (:70-72) propaga con contexto. Never-throws es solo para lectura; una mutación que el operador pidió debe surfacear el fallo (D-08). |
| T-52-SC | Tampering | npm installs | accept | Cero paquetes nuevos (RESEARCH.md §Package Legitimacy Audit). Sin superficie supply-chain. |

> V5 Input Validation (sanitización de title/description, path/home-dir redaction) es responsabilidad de Phase 53 (BIDIR-08). Phase 52 recibe `{projectId, title, description}` ya resueltos — NO añadir derivación no saneada aquí.
</threat_model>

<verification>
- `node --test test/providers/contract.test.js test/labels-hygiene.test.js` verde.
- `npm test` (suite completa 894+) verde tras el merge de wave.
- D-07 manual Plane CE 201 POST registrado como evidencia de una línea (checkpoint Task 3).
- `createTask` NO aparece en `TASK_PROVIDER_METHODS` (asegurado por el negative-assert de Plan 03).
</verification>

<success_criteria>
- El adapter Plane crea una work-item vía `createTask` (`POST .../work-items/`, `name` required) en el estado trigger (D-04).
- El 201 se normaliza a un `TaskItem` canónico shape-idéntico vía `normalizeWorkItem` con contexto completo de 6 campos.
- El marker `kodo:adopted` se aplica en el create (label-UUID lookup-or-create), referenciado vía `KODO_LABEL_ADOPTED` (no inline).
- `createTask` typeof-detected, fuera de los 9 FROZEN; `interface.js`/`registry.js` intactos.
- Shape del 201 de Plane CE validado manualmente (D-07).
</success_criteria>

<output>
Create `.planning/phases/52-createtask-contrato-anti-recursi-n/52-02-SUMMARY.md` when done
</output>
