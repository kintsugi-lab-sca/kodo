---
phase: 52-createtask-contrato-anti-recursi-n
plan: 03
type: execute
wave: 3
depends_on: ["52-01", "52-02"]
files_modified:
  - src/providers/github/client.js
  - src/providers/github/provider.js
  - test/providers/contract.test.js
autonomous: true
requirements: [BIDIR-02, BIDIR-01]
user_setup: []

must_haves:
  truths:
    - "El adapter GitHub crea una issue vía createTask (POST /repos/{o}/{r}/issues, title required, body Markdown) y devuelve un TaskItem canónico"
    - "La issue creada lleva el label kodo:adopted aplicado en el create (GitHub labels son strings — sin resolución UUID)"
    - "createTask falla LOUD ante 403/404 (scope PAT insuficiente / repo inexistente) — nunca silencioso"
    - "Un it() capability-gated en contract.test.js asserta que createTask round-trippea un 201 mockeado a un TaskItem canónico, para AMBOS providers"
    - "TASK_PROVIDER_METHODS.length === 9 y createTask NO está en la lista (negative-assert)"
  artifacts:
    - path: "src/providers/github/client.js"
      provides: "createIssue(owner, repo, fields) transport POST"
      contains: "createIssue"
    - path: "src/providers/github/provider.js"
      provides: "createTask({projectId, title, description}) typeof-detected, fuera de los 9 FROZEN"
      contains: "async createTask"
    - path: "test/providers/contract.test.js"
      provides: "it() capability-gated createTask (mirror B8) + getCreateTaskArg + negative-assert FROZEN-9"
      contains: "createTask"
  key_links:
    - from: "src/providers/github/provider.js"
      to: "src/providers/github/client.js"
      via: "client.createIssue"
      pattern: "client\\.createIssue"
    - from: "src/providers/github/provider.js"
      to: "src/labels.js"
      via: "import KODO_LABEL_ADOPTED (no inline)"
      pattern: "KODO_LABEL_ADOPTED"
    - from: "test/providers/contract.test.js"
      to: "provider.createTask"
      via: "capability-gated it() round-trip"
      pattern: "provider\\.createTask"
---

<objective>
Entregar `createTask` como método **opcional typeof-detected** en el adapter GitHub (BIDIR-02) y el test de contrato compartido que cierra BIDIR-01 + BIDIR-02. La issue se crea vía `POST /repos/{o}/{r}/issues` (`title` required, body **Markdown** — divergencia conocida del split de addComment), con el label `kodo:adopted` aplicado en el create (GitHub labels son strings planos — simple, sin resolución UUID). El fallo ante 403/404 es **LOUD** (D-08, Pitfall 4). El 201 se normaliza vía `normalizeIssue`. Más el `it()` capability-gated (espejo B8) que ejerce `createTask` para AMBOS providers + el negative-assert que `createTask` NO está en `TASK_PROVIDER_METHODS`.

Purpose: Completar la capacidad de creación en ambos adapters preservando el contrato FROZEN-at-9, con la red de seguridad del contract matrix (espejo v0.10).
Output: `src/providers/github/client.js` (+`createIssue`), `src/providers/github/provider.js` (+`createTask`), `test/providers/contract.test.js` (it() + helpers + negative-assert).

Depende de Plan 01 (`KODO_LABEL_ADOPTED`) y Plan 02 (el contract test ejerce también el Plane provider — ambos deben tener `createTask` antes de que el it() corra).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-CONTEXT.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-RESEARCH.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md

<interfaces>
<!-- Contratos extraídos del codebase — verificados contra HEAD 2026-06-16. -->

GitHub client transport (analog — src/providers/github/client.js:293-300 addComment):
  async addComment(owner, repo, number, markdownBody) {
    const o = encodeURIComponent(owner); const r = encodeURIComponent(repo);
    return this.request(`/repos/${o}/${r}/issues/${number}/comments`, { method: 'POST', body: { body: markdownBody } });
  }
  request() (:106-217) ya hace auth (token PAT), API version pin 2022-11-28 (:118), y LANZA un Error canónico con .code ('forbidden'/'not_found') + .status en non-ok (:168-195, mapErrorCode :60-70).
  Path issues (sin number, sin trailing slash): `/repos/${o}/${r}/issues` (base de listIssues :264).

GitHub provider (src/providers/github/provider.js):
  getTaskState precedente método opcional (:167-180), comentario :167-168:
    // OPTIONAL method (NOT in TASK_PROVIDER_METHODS — FROZEN at 9, D-13). Detected via
    // `typeof provider.getTaskState === 'function'` at the call site.
  getTask owner/repo split + normalize (:121-125):
    async getTask(ref) { const { owner, repo, number } = parseRef(ref); const issue = await client.getIssue(owner, repo, number); return normalizeIssue(issue, { projectId: `${owner}/${repo}` }); }
  listProjects shape (:201-204): projectId llega como 'owner/repo'.

normalizeIssue(raw, { projectId }) — src/providers/github/normalize.js:84-108 (REUSE, NO editar).
  id=node_id (:94), ref=owner/repo#number (:95), url=html_url (:102), state='open'|'closed' (:104). Contexto trivial: { projectId: 'owner/repo' }.

KODO_LABEL_ADOPTED — exportado por Plan 01 desde src/labels.js (= 'kodo:adopted'). GitHub labels son strings → pasar [KODO_LABEL_ADOPTED] directo en el body create.

Contract test (test/providers/contract.test.js):
  B8 getTaskState it() (:498-505) — patrón capability-gated a espejar.
  getTaskStateArg (:375-381) — per-provider arg helper a espejar.
  instantiateProvider / stubPlaneFetch (:287-347), route '/work-items/' (:317) match por path-suffix method-agnostic (:258).
  makeFakeGitHubClient (:195-239), getIssue default (:208-216): { node_id, number, title, body, labels, state, html_url }.
  assertTaskItemShape (:139-187) — 13-field shape assert.
  B1 9-method test (:431-439).
  PROVIDERS matrix loop (:393).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createIssue en github/client.js</name>
  <read_first>
    - src/providers/github/client.js (archivo modificado; addComment :293-300, request() :106-217, mapErrorCode :60-70, API version :118, listIssues base path :264)
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (sección "src/providers/github/client.js")
  </read_first>
  <files>src/providers/github/client.js</files>
  <action>
    Añadir `createIssue(owner, repo, fields)` espejo EXACTO de `addComment` (:293-300): `const o = encodeURIComponent(owner); const r = encodeURIComponent(repo); return this.request(\`/repos/${o}/${r}/issues\`, { method: 'POST', body: fields })`. Path sin number ni trailing slash (convención GitHub, base de listIssues :264). `fields` es `{ title, body?, labels? }`: `title` required, `body` es **Markdown** (NO HTML — divergencia conocida de Plane), `labels` array de strings.

    Error handling LOAD-BEARING (D-08 / Pitfall 4): `request()` ya lanza un `Error` canónico con `.code` (`'forbidden'`/`'not_found'`) + `.status` en non-ok (:168-195). `createIssue` debe dejar que PROPAGUE — nunca catch-and-default a un TaskItem vacío. Never-throws es solo para los carriles de lectura. JSDoc espejando el de addComment.
  </action>
  <verify>
    <automated>node -e "import('./src/providers/github/client.js').then(m => { const C = m.GitHubClient || m.default; if (typeof C.prototype.createIssue !== 'function') throw new Error('createIssue missing'); console.log('OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - source: `createIssue` existe en `GitHubClient.prototype`, delega en `this.request` con `method: 'POST'`, encodeURIComponent en owner y repo
    - source: el path es `/repos/${o}/${r}/issues` (sin number, sin trailing slash)
    - behavior: NO contiene un catch que devuelva un default (propagación LOUD load-bearing, D-08/Pitfall 4)
    - test-command: `node --test test/providers/contract.test.js` verde
  </acceptance_criteria>
  <done>`createIssue` añadido a GitHubClient, reusando `request()` con POST Markdown, sin swallow (LOUD on 403/404).</done>
</task>

<task type="auto">
  <name>Task 2: createTask typeof-detected en github/provider.js</name>
  <read_first>
    - src/providers/github/provider.js (archivo modificado; getTaskState :167-180 incl. comentario :167-168, getTask :121-125, parseRef :84, listProjects :201-204)
    - src/providers/github/normalize.js (normalizeIssue :84-108 — REUSE)
    - src/labels.js (KODO_LABEL_ADOPTED exportado por Plan 01)
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (sección "src/providers/github/provider.js")
  </read_first>
  <files>src/providers/github/provider.js</files>
  <action>
    Importar `KODO_LABEL_ADOPTED` desde `labels.js` (verificar la profundidad del path relativo desde `src/providers/github/provider.js`; NO literal inline — source-hygiene REPORT-05).

    Añadir `async createTask({ projectId, title, description })` dentro del object literal `provider`, OUTSIDE de los 9, al mismo nivel que `getTaskState`. Copiar verbatim el comentario de método opcional de :167-168 cambiando `getTaskState`→`createTask`.

    Cuerpo (D-04, D-05, D-06, D-02):
    1. `const [owner, repo] = projectId.split('/')` (projectId llega como 'owner/repo', listProjects :201-204).
    2. `const raw = await client.createIssue(owner, repo, { title, body: description || '', labels: [KODO_LABEL_ADOPTED] })` — GitHub labels son strings, el marker se adjunta en el create (D-02/D-03). D-04: la issue queda `open` por defecto, sin campo de state.
    3. `return normalizeIssue(raw, { projectId: \`${owner}/${repo}\` })` (contexto trivial, D-06).

    NO envolver en try/catch que trague: el `Error` con `.code`/`.status` propaga (D-08).
  </action>
  <verify>
    <automated>node -e "import('./src/providers/github/provider.js').then(()=>console.log('github provider imports OK')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - source: `async createTask` existe en el object literal del provider, con el comentario "OPTIONAL method (NOT in TASK_PROVIDER_METHODS — FROZEN at 9..." copiado de getTaskState
    - source: `createTask` llama a `client.createIssue` con `labels: [KODO_LABEL_ADOPTED]` y a `normalizeIssue` con `{ projectId: 'owner/repo' }`
    - source: el marker se referencia vía `KODO_LABEL_ADOPTED` importado de labels.js, NO inline
    - source: NO hay catch que trague (el error 403/404 propaga LOUD)
    - test-command: `node --test test/providers/contract.test.js test/labels-hygiene.test.js` verde
  </acceptance_criteria>
  <done>`createTask` añadido al provider GitHub, typeof-detected fuera de los 9, aplica el marker kodo:adopted en el create, normaliza el 201 vía normalizeIssue, falla LOUD ante 403/404.</done>
</task>

<task type="auto">
  <name>Task 3: it() capability-gated createTask + FROZEN-9 negative-assert en contract.test.js</name>
  <read_first>
    - test/providers/contract.test.js (B8 getTaskState it() :498-505, getTaskStateArg :375-381, instantiateProvider/stubPlaneFetch :287-347 route '/work-items/' :317 + match por suffix :258, makeFakeGitHubClient :195-239 getIssue default :208-216, assertTaskItemShape :139-187, B1 9-method :431-439, PROVIDERS loop :393)
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (sección "test/providers/contract.test.js")
  </read_first>
  <files>test/providers/contract.test.js</files>
  <action>
    1. Añadir `getCreateTaskArg(name)` espejo de `getTaskStateArg` (:375-381): plane → `{ projectId: 'p0p0p0p0-1111-2222-3333-444444444444', title: 'adopt smoke', description: 'desc' }` (matching el UUID del stub `/projects/` :294); github → `{ projectId: 'octocat/hello-world', title: 'adopt smoke', description: 'desc' }`.

    2. Añadir a `makeFakeGitHubClient` (:195-239) un override `createIssue(owner, repo, fields)` que devuelva un raw issue shape como el `getIssue` default (:208-216): `{ node_id, number, title: fields.title, body: fields.body, labels: fields.labels, state: 'open', html_url }`.

    3. Stub de Plane (A3): el `stubPlaneFetch` ya matchea por path-suffix method-agnostic (:258) y la route `/work-items/` (:317) devuelve `{ results: [planeWorkItem] }` a status:200. Un POST a `/work-items/` routea ahí; el round-trip asserta SHAPE no status, así que funciona as-is. Si el round-trip necesita el work item crudo (no envuelto en `results`), extender la route para devolver `planeWorkItem` directo ante method POST (low risk per A3) — decidir leyendo cómo `createWorkItem` consume `request()` (devuelve `res.json()` crudo, sin desempaquetar `results`). Asegurar que el stub devuelve la forma cruda que `normalizeWorkItem` espera (`planeWorkItem` con `id`, `sequence_id`, `state`, `project_detail`/`state_detail`). También añadir un stub `/labels/` POST que devuelva `{ id: 'label-uuid', name: 'kodo:adopted' }` para que `createLabel` resuelva en el test.

    4. Añadir el it() capability-gated DENTRO del `for (const providerName of PROVIDERS)` matrix loop (:393), espejo de B8 (:498-505):
       `it('createTask (if supported) round-trips a 201 to a canonical TaskItem', async () => { if (typeof provider.createTask !== 'function') return; const task = await provider.createTask(getCreateTaskArg(providerName)); assertTaskItemShape(task, providerName); });`

    5. FROZEN-9 negative-assert: en (o junto a) el B1 test (:431), añadir `assert.ok(!TASK_PROVIDER_METHODS.includes('createTask'), 'createTask must NOT be in TASK_PROVIDER_METHODS (FROZEN at 9)')`.
  </action>
  <verify>
    <automated>node --test test/providers/contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test: el it() de createTask corre para AMBOS providers (plane + github) y pasa `assertTaskItemShape` (13 campos, url + state válidos)
    - test: el negative-assert `!TASK_PROVIDER_METHODS.includes('createTask')` pasa; B1 sigue asertando exactamente 9 métodos
    - source: `makeFakeGitHubClient` tiene un `createIssue` override; el stub de Plane sirve el create-201 crudo (no envuelto en `results`) + un `/labels/` POST
    - test-command: `node --test test/providers/contract.test.js` verde
  </acceptance_criteria>
  <done>it() capability-gated de createTask verde para ambos providers + negative-assert FROZEN-9; helpers y stubs añadidos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kodo → GitHub REST API | POST autenticado (PAT) que MUTA el repo remoto (crea una issue). La issue creada es visible públicamente según el repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-2 | Repudiation / Tampering | `createIssue` / `createTask` GitHub (scope PAT) | mitigate | Scope PAT insuficiente → 403, o repo inexistente → 404, NO deben leerse como éxito. `request()` ya lanza un `Error` canónico con `.code='forbidden'`/`'not_found'` + `.status` (:168-195); `createIssue`/`createTask` lo dejan PROPAGAR (D-08/Pitfall 4) — nunca catch-and-default a un TaskItem vacío. Scope mínimo a documentar en el SUMMARY: `issues:write` (fine-grained) / `repo` (classic). |
| T-52-1 | Denial of Service | marker `kodo:adopted` GitHub | mitigate | El marker se adjunta en el create (`labels: [KODO_LABEL_ADOPTED]`, string plano) para que el guard `isAdopted` (Plan 01) corte el re-despacho incluso bajo `--force`. |
| T-52-3 | Tampering | `createTask` GitHub (mutación) | mitigate | Sin try/catch que trague — la mutación que el operador pidió surfacea el fallo (D-08). |
| T-52-SC | Tampering | npm installs | accept | Cero paquetes nuevos (RESEARCH.md §Package Legitimacy Audit). Sin superficie supply-chain. |

> V5 Input Validation (sanitización de title/description) es Phase 53 (BIDIR-08). Phase 52 recibe datos ya resueltos.
</threat_model>

<verification>
- `node --test test/providers/contract.test.js test/labels-hygiene.test.js` verde.
- `npm test` (suite completa 894+) verde tras el merge de wave — guarda contra regresión.
- El it() de createTask corre para plane + github; `TASK_PROVIDER_METHODS.length === 9` y createTask no está en la lista.
- Scope PAT mínimo (`issues:write`/`repo`) documentado en el SUMMARY (D-08).
</verification>

<success_criteria>
- El adapter GitHub crea una issue vía `createTask` (`POST /repos/{o}/{r}/issues`, `title` required, body Markdown) con el marker `kodo:adopted`.
- `createTask` falla LOUD ante 403/404 (scope insuficiente / repo inexistente).
- El 201 se normaliza a un `TaskItem` canónico vía `normalizeIssue`.
- Un it() capability-gated espejo de B8 asserta el round-trip para AMBOS providers; el negative-assert confirma FROZEN-9.
- `createTask` typeof-detected, fuera de los 9; `interface.js`/`registry.js` intactos.
</success_criteria>

<output>
Create `.planning/phases/52-createtask-contrato-anti-recursi-n/52-03-SUMMARY.md` when done
</output>
