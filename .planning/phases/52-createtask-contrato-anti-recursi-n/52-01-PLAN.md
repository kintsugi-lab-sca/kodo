---
phase: 52-createtask-contrato-anti-recursi-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/labels.js
  - src/triggers/dispatcher.js
  - test/labels.test.js
  - test/labels-hygiene.test.js
  - test/dispatcher.test.js
autonomous: true
requirements: [BIDIR-06]
user_setup: []

must_haves:
  truths:
    - "labels.js exporta KODO_LABEL_ADOPTED = 'kodo:adopted' y un helper isAdopted(labels) tolerante a string[] y {name}[], case-insensitive"
    - "El dispatcher descarta una tarea con label kodo:adopted devolviendo {action:'ignored', code:'adopted'} ANTES de lock/resolver/launch"
    - "El corte isAdopted NO es bypasseado por --force (cuts incluso bajo opts.force:true)"
    - "Una tarea kodo:gsd normal SIGUE llegando al resolver (sin falso positivo)"
    - "Ningún literal 'kodo:adopted' vive fuera de labels.js (source-hygiene)"
  artifacts:
    - path: "src/labels.js"
      provides: "KODO_LABEL_ADOPTED const + isAdopted helper (mirror KODO_LABEL_GSD_CHILD/isGsdChild :99-123)"
      contains: "KODO_LABEL_ADOPTED"
    - path: "src/triggers/dispatcher.js"
      provides: "corte isAdopted antes de if(!opts.force)"
      contains: "isAdopted"
  key_links:
    - from: "src/triggers/dispatcher.js"
      to: "src/labels.js"
      via: "import isAdopted"
      pattern: "import.*isAdopted.*from.*labels"
    - from: "src/triggers/dispatcher.js"
      to: "anti-recursion cut"
      via: "isAdopted cut antes de if(!opts.force)"
      pattern: "isAdopted\\(task\\.labels\\)"
---

<objective>
Entregar la **anti-recursión** (BIDIR-06) que viaja con `createTask` como propiedad de corrección del núcleo: la constante `KODO_LABEL_ADOPTED` + el helper `isAdopted` en `labels.js`, y un corte `isAdopted(task.labels)` en `dispatcher.js` insertado **ANTES** del bloque `if (!opts.force)` — espejo exacto del guard `isGsdChild` (cuts incluso bajo `--force`). Más los tests de corrección (truth-table de `isAdopted`, comportamiento del corte, ordering hygiene, no-inline-literal).

Purpose: Una tarea recién adoptada NUNCA debe ser re-despachada por el poller/webhook. El marker `kodo:adopted` (aplicado por `createTask` en los Plans 02/03) es el lever; este plan entrega el guard que lo respeta. Precedente: la anti-recursión `kodo:gsd-child` shipped CON el reporting en Phase 29, no después.
Output: `src/labels.js`, `src/triggers/dispatcher.js`, `test/labels.test.js`, `test/labels-hygiene.test.js`, `test/dispatcher.test.js`.

**Pitfall 1 (crítico — leer):** `parseKodoLabels` trata CUALQUIER `kodo:*` (incluido `kodo:adopted`) como `isKodo:true`. Por eso la "capa primaria" (D-01, label trigger ausente) NO suprime por sí sola una tarea adoptada una vez el marker está presente — el corte `isAdopted` es el guard LOAD-BEARING, no redundante. Por eso DEBE ir antes de `if (!opts.force)`. NO tocar `parseKodoLabels`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-CONTEXT.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-RESEARCH.md
@.planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md

<interfaces>
<!-- Bloques exactos a espejar — verificados contra HEAD 2026-06-16. -->

labels.js — bloque a espejar (src/labels.js:99-123):
  export const KODO_LABEL_GSD_CHILD = 'kodo:gsd-child';
  export function isGsdChild(labels) {
    if (!Array.isArray(labels)) return false;
    return labels.some((l) => {
      const name = typeof l === 'object' && l !== null ? l.name : typeof l === 'string' ? l : null;
      return typeof name === 'string' && name.toLowerCase() === KODO_LABEL_GSD_CHILD;
    });
  }

dispatcher.js — import a extender (src/triggers/dispatcher.js:6):
  import { parseKodoLabels, getGsdMode, isGsdChild } from '../labels.js';

dispatcher.js — corte existente a espejar (src/triggers/dispatcher.js:69-72), y el bloque
`if (!opts.force)` que sigue (:74) ante el cual el nuevo corte debe ir ANTES:
  if (isGsdChild(task.labels)) {
    console.log(`[kodo:dispatch] Ignored — kodo:gsd-child filtered (anti-recursion)`);
    return { action: 'ignored', code: 'gsd_child' };
  }
  // 2. Check kodo labels (skip if force=true)
  if (!opts.force) { ... }

CRITICAL: NO tocar parseKodoLabels (:12-38) — trata kodo:adopted como isKodo:true (Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KODO_LABEL_ADOPTED + isAdopted en labels.js (+ truth-table + hygiene tests)</name>
  <read_first>
    - src/labels.js (archivo modificado; bloque KODO_LABEL_GSD_CHILD/isGsdChild :99-123 a espejar; parseKodoLabels :12-38 — NO tocar)
    - test/labels.test.js (truth-table de isGsdChild :171-214 a espejar; import line :3)
    - test/labels-hygiene.test.js (REPORT-05 :41-65 — listJsFilesExcept, stripComments, asserts de export)
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (secciones "src/labels.js", "test/labels.test.js", "test/labels-hygiene.test.js")
  </read_first>
  <files>src/labels.js, test/labels.test.js, test/labels-hygiene.test.js</files>
  <behavior>
    - isAdopted(['kodo:adopted']) === true (forma string)
    - isAdopted([{name:'kodo:adopted'}]) === true (forma objeto)
    - isAdopted(['KODO:ADOPTED']) === true (case-insensitive)
    - isAdopted([]) / null / undefined / no-array === false
    - isAdopted(['kodo:adopted-x']) === false (rechaza similar-pero-distinto)
    - isAdopted(['kodo:gsd', {name:'kodo:adopted'}]) === true (tolera garbage mixto)
    - KODO_LABEL_ADOPTED === 'kodo:adopted'
    - hygiene: ningún src/**/*.js excepto labels.js contiene el literal 'kodo:adopted'/"kodo:adopted"
    - hygiene: labels.js exporta `const KODO_LABEL_ADOPTED = 'kodo:adopted'` + `function isAdopted(`
  </behavior>
  <action>
    En `src/labels.js`, append después del bloque `isGsdChild` (:99-123) byte-por-byte el mismo cuerpo, swapping la const: `export const KODO_LABEL_ADOPTED = 'kodo:adopted';` + `export function isAdopted(labels) { ... toLowerCase() === KODO_LABEL_ADOPTED ... }`. JSDoc espejando el de isGsdChild (única fuente de verdad para el check `adopted`; callsites usan el helper, no inline). NO tocar `parseKodoLabels`.

    En `test/labels.test.js`: extender el import (:3) para traer `isAdopted, KODO_LABEL_ADOPTED`, y añadir un `describe('BIDIR-06 — isAdopted + KODO_LABEL_ADOPTED')` clonando la truth-table de isGsdChild (:171-214) swapping `gsd-child`→`adopted` — cubrir los casos de <behavior>.

    En `test/labels-hygiene.test.js`: extender REPORT-05 (:42-58) para también asertar que ningún `src/**/*.js` excepto `labels.js` contiene el literal `'kodo:adopted'`/`"kodo:adopted"` (reutilizar `listJsFilesExcept`/`stripComments`), y los dos source-export asserts paralelos (:60-64): `assert.match(source, /export\s+const\s+KODO_LABEL_ADOPTED\s*=\s*['"]kodo:adopted['"]/)` + `assert.match(source, /export\s+function\s+isAdopted\s*\(/)`.
  </action>
  <verify>
    <automated>node --test test/labels.test.js test/labels-hygiene.test.js</automated>
  </verify>
  <acceptance_criteria>
    - source: `labels.js` exporta `KODO_LABEL_ADOPTED` (= 'kodo:adopted') e `isAdopted`, cuerpo idéntico a isGsdChild salvo la const
    - source: `parseKodoLabels` sin cambios (diff no toca :12-38)
    - test-command: `node --test test/labels.test.js test/labels-hygiene.test.js` verde, incluyendo todos los casos de <behavior>
    - test: la truth-table cubre string/objeto/case-insensitive/null/no-array/similar-pero-distinto/garbage-mixto
  </acceptance_criteria>
  <done>`KODO_LABEL_ADOPTED` + `isAdopted` exportados desde labels.js con truth-table y hygiene tests verdes; parseKodoLabels intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: corte isAdopted en dispatcher.js (+ behavior + ordering tests)</name>
  <read_first>
    - src/triggers/dispatcher.js (archivo modificado; import :6, corte isGsdChild :69-72, bloque if(!opts.force) :74-82)
    - test/dispatcher.test.js (REPORT-01 behavior :1040-1151, source-hygiene/ordering :1153-1189, makeDeps harness :1056-1090 con _inspect())
    - .planning/phases/52-createtask-contrato-anti-recursi-n/52-PATTERNS.md (secciones "src/triggers/dispatcher.js", "test/dispatcher.test.js")
  </read_first>
  <files>src/triggers/dispatcher.js, test/dispatcher.test.js</files>
  <behavior>
    - tarea con labels ['kodo:adopted'] → dispatch devuelve {action:'ignored', code:'adopted'}
    - el corte ocurre ANTES de acquireGsdLock / resolvePhase / launchWorkItem (esas fns NO se llaman)
    - el corte ocurre incluso con opts.force:true (--force NO bypasea)
    - control: una tarea ['kodo:gsd'] normal SÍ llega al resolver (sin falso positivo)
    - source-hygiene: source.indexOf('isAdopted(task.labels)') < source.search(/if\s*\(!opts\.force\)/)
    - dispatcher importa isAdopted desde ../labels.js
  </behavior>
  <action>
    En `src/triggers/dispatcher.js`: extender el import (:6) a `import { parseKodoLabels, getGsdMode, isGsdChild, isAdopted } from '../labels.js';`. Insertar, INMEDIATAMENTE después del bloque `isGsdChild` (:72) y ANTES del comentario `// 2. Check kodo labels` + `if (!opts.force)` (:74), el corte espejo:
    `if (isAdopted(task.labels)) { console.log('[kodo:dispatch] Ignored — kodo:adopted filtered (anti-recursion)'); return { action: 'ignored', code: 'adopted' }; }`
    El ordering es LOAD-BEARING: antes de `if (!opts.force)` para que `--force` no lo bypasee (D-02), y la hygiene test asserta `filterIdx < forceIdx`. Añadir un comentario breve espejo del de isGsdChild (:63-67) explicando la propiedad de seguridad (anti-recursión, sobrevive --force).

    En `test/dispatcher.test.js`: añadir `describe('BIDIR-06 — kodo:adopted anti-recursion filter')` clon estructural de REPORT-01 (:1040-1151) + source-hygiene (:1153-1189), swapping `gsd-child`→`adopted`, `code:'gsd_child'`→`code:'adopted'`. Reutilizar el harness `makeDeps` (:1056-1090) con `task.labels = ['kodo:adopted']`. Cubrir los casos de <behavior> incluyendo el assert de import y el de ordering.
  </action>
  <verify>
    <automated>node --test test/dispatcher.test.js</automated>
  </verify>
  <acceptance_criteria>
    - source: dispatcher importa `isAdopted` de `../labels.js`; el corte `isAdopted(task.labels)` devuelve `{action:'ignored', code:'adopted'}`
    - source: el corte está físicamente ANTES de `if (!opts.force)` (verificado por la ordering hygiene test `filterIdx < forceIdx`)
    - behavior: el corte dispara bajo `opts.force:true`; lock/resolver/launch NO se invocan; control `kodo:gsd` sí llega al resolver
    - test-command: `node --test test/dispatcher.test.js` verde
  </acceptance_criteria>
  <done>Corte `isAdopted` insertado antes de `if(!opts.force)`, devuelve `code:'adopted'`, sobrevive --force; behavior + ordering hygiene tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poller/webhook → dispatcher | Una tarea (posiblemente recién adoptada, marker `kodo:adopted`) entra al dispatcher; el corte decide si lanza una sesión. Una tarea adoptada re-despachada lanzaría una segunda sesión colisionando con la sesión ad-hoc viva. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-1 | Denial of Service | `dispatcher.js` corte `isAdopted` | mitigate | Corte `isAdopted(task.labels)` insertado ANTES de `if (!opts.force)`, espejo de `isGsdChild`: descarta la tarea adoptada antes de lock/resolver/launch, devolviendo `{action:'ignored', code:'adopted'}` incluso bajo `--force`. Es el guard LOAD-BEARING (Pitfall 1: la capa primaria de label-ausente NO suprime por sí sola una vez el marker está presente). Verificado por behavior + ordering tests. |
| T-52-SC | Tampering | npm installs | accept | Cero paquetes nuevos (RESEARCH.md §Package Legitimacy Audit). Sin superficie supply-chain. |

> Source-hygiene (REPORT-05 + ordering) blinda el invariante: el literal `kodo:adopted` solo vive en `labels.js`; el corte solo vía `isAdopted`; el orden `filterIdx < forceIdx` es testeado estáticamente.
</threat_model>

<verification>
- `node --test test/labels.test.js test/labels-hygiene.test.js test/dispatcher.test.js` verde.
- `npm test` (suite completa) verde tras el merge — el hygiene test escanea TODO src/ (no debe haber inline `kodo:adopted`; los Plans 02/03 importan `KODO_LABEL_ADOPTED`).
- `parseKodoLabels` sin cambios.
</verification>

<success_criteria>
- `labels.js` exporta `KODO_LABEL_ADOPTED` + `isAdopted` (mirror exacto de gsd-child).
- El dispatcher descarta tareas `kodo:adopted` con `{action:'ignored', code:'adopted'}` antes de lock/resolver/launch, incluso bajo `--force`.
- Truth-table, behavior, ordering hygiene y no-inline-literal tests verdes.
- Una tarea `kodo:gsd` normal sigue llegando al resolver.
</success_criteria>

<output>
Create `.planning/phases/52-createtask-contrato-anti-recursi-n/52-01-SUMMARY.md` when done
</output>
