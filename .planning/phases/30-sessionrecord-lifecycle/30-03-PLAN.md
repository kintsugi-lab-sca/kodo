---
phase: 30-sessionrecord-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [30-01, 30-02]
gap_closure: true
gap_source: 30-REVIEW.md CR-01
files_modified:
  - src/hooks/stop.js
  - test/hooks/stop-idempotency.test.js
autonomous: true
requirements:
  - LIFE-01
requirements_addressed:
  - LIFE-01
tags:
  - stop-hook
  - idempotency
  - cr-01-closure
  - gap-closure
must_haves:
  truths:
    - "Stop hook descarta entradas de history (source==='history') ANTES de cualquier side-effect — preserva idempotencia natural del hook anterior a Phase 30"
    - "El discriminator vive en stop.js inmediatamente después de findSessionFn, antes del setColor/markSessionStatus/sessionEnd/worktree-cleanup/buildStopNudgeText"
    - "console.error log line por consistencia con el resto del hook (línea 131, 135, 158, 192)"
    - "El short-circuit es 'return' temprano, no 'throw' — preserva fail-open behavior del hook (nunca crashea Claude Code)"
    - "Test idempotencia: runStopHook 2 veces con mismo session_id, segunda llamada NO emite worktree.cleanup.* ni state.transition ni session.end ni nudge al orchestrator"
    - "Suite global post-fix ≥881 pass + 0 fail (no degradación respecto a Wave 1 baseline)"
  artifacts:
    - path: "src/hooks/stop.js"
      provides: "Discriminator early-return para entradas de history"
      contains: "result.source === 'history'"
    - path: "test/hooks/stop-idempotency.test.js"
      provides: "Test de idempotencia: dos invocaciones consecutivas, segunda es no-op"
      exports: "describe block 'stop hook — Phase 30 idempotency (CR-01)'"
      min_lines: 80
  key_links:
    - from: "src/hooks/stop.js#~147 (post-findSessionFn)"
      to: "source field de findSession"
      via: "discriminator if (result.source === 'history') { console.error(...); return; }"
      pattern: "result\\.source === 'history'"
---

<objective>
Cerrar CR-01 del code review: el doble-scan de findSession (Phase 30-01) introdujo un bug de idempotencia en el stop hook. Cuando Claude Code re-dispara el hook (restart, reload, race), la segunda invocación encuentra la sesión en `state.history` y re-ejecuta todo el cleanup pipeline (cmux setColor sobre workspace potencialmente reasignado, markSessionStatus con `from='unknown'`, segundo `session.end`, worktree cleanup destructivo, segundo nudge al orchestrator).

El fix es quirúrgico: añadir un discriminator inmediatamente después de `findSessionFn` que descarte entradas de `history`. Preserva la idempotencia natural del hook pre-Phase-30 sin tocar `findSession` (que sigue útil para los flujos READ de SC#1).

Output: 5-LOC fix en `src/hooks/stop.js` + test de idempotencia en `test/hooks/stop-idempotency.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-sessionrecord-lifecycle/30-REVIEW.md
@.planning/phases/30-sessionrecord-lifecycle/30-VERIFICATION.md
@src/hooks/stop.js
@src/session/state.js
@test/stop-state-transition.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test idempotencia — runStopHook 2 veces, segunda es no-op</name>
  <files>test/hooks/stop-idempotency.test.js</files>
  <read_first>
    - test/stop-state-transition.test.js (líneas 1-180 — analog scaffold + makeLogger memSink)
    - src/hooks/stop.js (líneas 1-260 — entender el flow completo)
  </read_first>
  <behavior>
    - Escenario único (RED inicialmente): construir un SessionRecord, llamar runStopHook (primera vez), verificar archivos en state.history. Llamar runStopHook **segunda vez** con mismo session_id. Assert: segunda llamada NO emite `state.transition` (markSessionStatus no se ejecuta), NO emite `session.end`, NO llama `cmuxClient.setColor`, NO llama `buildStopNudgeText`. Solo emite el console.error informativo "already archived — skip" y retorna.
  </behavior>
  <action>
    Crear `test/hooks/stop-idempotency.test.js` siguiendo el patrón de `test/stop-state-transition.test.js`:

    1. **Imports**: `describe, it, before, after, afterEach` de `node:test`. `assert` de `node:assert/strict`. `mkdtempSync, rmSync, mkdirSync` de `node:fs`. `tmpdir` de `node:os`. `join` de `node:path`.

    2. **HOME-isolation scaffold** + `makeLogger()` memSink copiado verbatim de `test/stop-state-transition.test.js#65-80`.

    3. **Stub deps** para runStopHook:
       - `cmuxClient`: fake con `setColor` y `notify` que push a un `callsLog` array.
       - `loggerFactory`: retorna el fakeLogger.
       - **Opcional**: stubbear `worktreeRemove` / `releaseGsdLock` si el hook los exporta como deps (revisar la signature actual de runStopHook). Si no son injectables, usar SessionRecord SIN `worktree_path` ni `gsd: true` para evitar disparar esos paths.

    4. **describe block**: `'stop hook — Phase 30 idempotency (CR-01)'`.

    5. **it() único**: `'second invocation skips cleanup when session is in history'`:
       - addSession(taskId, sessionRecord) con status='running'.
       - `const stopHook = await import('../../src/hooks/stop.js')` (dynamic POST-HOME).
       - Primera invocación: `await stopHook.runStopHook({session_id, cwd: tmpPath}, {loggerFactory: () => fakeLogger, cmuxClient: fakeCmux})`. Assert state.sessions vacío + state.history tiene 1 entry.
       - Reset events / callsLog entre invocaciones.
       - Segunda invocación con misma input.
       - Assertions:
         - `events.filter(e => e.msg === 'state.transition').length === 0` (markSessionStatus no se llamó en la segunda).
         - `events.filter(e => e.msg === 'session.end').length === 0` (sessionEnd no se llamó).
         - `callsLog.filter(c => c.fn === 'setColor').length === 0` (cmux color skipped).
         - `state.history.length === 1` (no se duplicó la entry).
       - Permitido: console.error con substring "already archived" — usar `process.stderr` capture o no testearlo directamente (cosmético).

    **NO añadir** tests sobre setColor/notify/worktree si el hook no los expone como deps inyectables — el test no debe requerir refactor del production code.
  </action>
  <verify>
    <automated>node --test test/hooks/stop-idempotency.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `test/hooks/stop-idempotency.test.js` existe y tiene ≥80 líneas
    - `grep -c "it('" test/hooks/stop-idempotency.test.js` retorna ≥1
    - El test FALLA con código actual (RED): describe expect = 1, actual = 0 (la segunda invocación SÍ emite state.transition pre-fix)
    - Después de Task 2, el test PASA (GREEN)
  </acceptance_criteria>
  <done>
    Test RED escrito. Demuestra el bug CR-01: segunda invocación emite state.transition cuando el código actual lo hace via findSession+history match.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discriminator source==='history' en stop.js</name>
  <files>src/hooks/stop.js</files>
  <read_first>
    - src/hooks/stop.js (líneas 125-150 — el bloque post-findSessionFn)
  </read_first>
  <behavior>
    - Pre-fix: runStopHook con sesión en history ejecuta cleanup completo (regression vs comportamiento pre-Phase-30).
    - Post-fix: runStopHook con sesión en history emite console.error informativo y retorna early — sin side-effects.
  </behavior>
  <action>
    Edit `src/hooks/stop.js` inmediatamente DESPUÉS de la línea `let result = findSessionFn({ sessionId, cwd });` (línea ~132) y ANTES del `if (!result)` (línea ~134):

    **Estructura del nuevo bloque** (sin código fenced — descripción directiva):

    Después de `let result = findSessionFn({ sessionId, cwd });` añadir:

    ```
    // Phase 30 LIFE-01 CR-01: findSession ahora escanea state.history. El stop
    // hook NO debe re-procesar sesiones archivadas — el primer trigger ya hizo
    // cleanup. Re-procesar emite eventos duplicados (state.transition, session.end,
    // segundo nudge) y puede tocar workspaces reasignados o worktrees ya removidos.
    if (result && result.source === 'history') {
      console.error(`[kodo:stop] Session ${result.session.task_ref} already archived — skip`);
      return;
    }
    ```

    NO tocar el `if (!result)` block ni los blocks subsiguientes (`const { id, session } = result;` etc).

    **NO** añadir el discriminator a `session-start.js` u otros callers — solo stop.js. Los otros callers (`verify.js#84`, `session-start.js#203`) tienen casos de uso legítimos para leer entradas de history (verify lee VERIFICATION.md del worktree, session-start solo arranca si la sesión existe).
  </action>
  <verify>
    <automated>node --test test/hooks/stop-idempotency.test.js test/stop-state-transition.test.js test/session/find-session.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "result.source === 'history'" src/hooks/stop.js` retorna exactamente 1
    - `grep -c "already archived" src/hooks/stop.js` retorna 1
    - `node --test test/hooks/stop-idempotency.test.js` exits 0 (GREEN)
    - `node --test test/stop-state-transition.test.js` exits 0 (regression: tests existentes de stop hook intactos — sesión activa sigue siendo procesada)
    - `node --test test/session/find-session.test.js` exits 0 (regression LIFE-01 intacto)
    - `npm test` total ≥881 pass + 0 fail (no degradación vs Wave 1)
  </acceptance_criteria>
  <done>
    CR-01 cerrado: discriminator implementado en 5 LOC, idempotencia natural del stop hook restaurada. findSession sigue retornando source field — caller-side filtering preserva su utilidad para verify.js y kodo logs.
  </done>
</task>

</tasks>

<verification>
- `npm test` GREEN, ≥881 pass + 0 fail
- `test/hooks/stop-idempotency.test.js` GREEN (RED→GREEN ciclo TDD)
- `test/stop-state-transition.test.js` GREEN (regression — sesión activa procesada normal)
- `test/session/find-session.test.js` GREEN (regression LIFE-01)
- `grep -c "result.source === 'history'" src/hooks/stop.js` retorna 1
</verification>

<success_criteria>
CR-01 del REVIEW.md cerrado: stop hook recupera idempotencia natural; entradas de history descartadas con console.error informativo; tests cubren idempotencia con un escenario explícito. Suite global no degradada.
</success_criteria>

<output>
After completion, create `.planning/phases/30-sessionrecord-lifecycle/30-03-SUMMARY.md` documentando:
- CR-01 closure: discriminator early-return en stop.js#~134
- Test idempotencia añadido (RED→GREEN cycle)
- Suite global delta (pre vs post)
- Confirmación de que findSession sigue retornando entradas de history (LIFE-01 SC#1 flujos READ intactos)
</output>
