---
phase: 30-sessionrecord-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/manager.js
  - src/gsd/verify.js
  - src/hooks/stop.js
  - test/session/mark-status.test.js
autonomous: true
requirements:
  - LIFE-02
requirements_addressed:
  - LIFE-02
tags:
  - session-lifecycle
  - observability
  - logger-warn
must_haves:
  truths:
    - "`markSessionStatus(taskId, status, reason, logger)` con taskId falsy (null/undefined/'') emite `logger.warn('markSessionStatus: missing task_id', {session_id, status, reason})` BYTE-EXACT y retorna `{ok: false, reason: 'missing-task-id'}`"
    - "`markSessionStatus(taskId, status, reason, logger)` con taskId presente retorna `{ok: true, from: <prevStatus>, to: <nextStatus>}` y preserva la transición de status existente"
    - "Nueva firma: `markSessionStatus(taskId, nextStatus, reason, logger, sessionId?)` — 5º parámetro opcional (D-07). Cuando ausente, warn registra `session_id: 'unknown'`"
    - "Callers existentes `verify.js#267` y `stop.js#188` actualizan al nuevo signature pasando `session.session_id` como 5º arg. Try/catch silent envelope preservado (D-06)"
    - "Tests `test/session/mark-status.test.js` GREEN con 4 escenarios (task_id presente OK + 3 falsy paths con warn)"
    - "Callers que NO destructuran return value (verify.js + stop.js fire-and-forget) preservan semántica externa intacta"
  artifacts:
    - path: "src/session/manager.js"
      provides: "markSessionStatus refactor con falsy guard + discriminated union return + signature extendida"
      contains: "missing task_id"
      contains_also: "missing-task-id"
    - path: "test/session/mark-status.test.js"
      provides: "4 escenarios LIFE-02 con fakeLogger memSink + warn payload assertions"
      exports: "describe block 'LIFE-02 — markSessionStatus falsy task_id observability'"
      min_lines: 130
    - path: "src/gsd/verify.js"
      provides: "callsite actualizado con 5º arg session.session_id"
      contains: "markSessionStatus(session.task_id, 'review', 'gate-passed', log, session.session_id)"
    - path: "src/hooks/stop.js"
      provides: "callsite actualizado con 5º arg session.session_id"
      contains: "markSessionStatus(session.task_id, 'done', 'session-stop', log, session.session_id)"
  key_links:
    - from: "src/session/manager.js markSessionStatus falsy path"
      to: "logger.warn"
      via: "literal message 'markSessionStatus: missing task_id' + keys {session_id, status, reason}"
      pattern: "logger\\.warn\\('markSessionStatus: missing task_id'"
    - from: "src/gsd/verify.js#267"
      to: "markSessionStatus new signature"
      via: "5º positional arg session.session_id"
      pattern: "markSessionStatus\\(session\\.task_id, 'review', 'gate-passed', log, session\\.session_id\\)"
    - from: "src/hooks/stop.js#188"
      to: "markSessionStatus new signature"
      via: "5º positional arg session.session_id"
      pattern: "markSessionStatus\\(session\\.task_id, 'done', 'session-stop', log, session\\.session_id\\)"
    - from: "test/session/mark-status.test.js fakeLogger"
      to: "makeLogger pattern"
      via: "copia verbatim de test/stop-state-transition.test.js#70"
      pattern: "child: \\(\\) => logger"
---

<objective>
Refactor `markSessionStatus` en `src/session/manager.js` (líneas 352-360) para reemplazar el bail-out silencioso del falsy `task_id` path con una emisión `logger.warn` observable + return shape determinístico `{ok: false, reason: 'missing-task-id'}`. Path success retorna `{ok: true, from: <prev>, to: <next>}`. Añade 5º parámetro opcional `sessionId` para observability del warn payload.

Cierra WR-07 Phase 22 deferred.

Purpose: Resolver SC#2 del ROADMAP. Cuando un caller pasa task_id falsy (drift futuro), el sistema emite warn observable en vez de no-op silencioso. Callers existentes (`verify.js#267`, `stop.js#188`) preservan semántica externa (fire-and-forget try/catch silencioso intacto).

Output: `src/session/manager.js` refactorizado + `test/session/mark-status.test.js` con 4 escenarios verdes + 2 callsites (verify.js + stop.js) actualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sessionrecord-lifecycle/30-CONTEXT.md
@.planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md
@src/session/manager.js
@src/gsd/verify.js
@src/hooks/stop.js
@test/stop-state-transition.test.js

<interfaces>
<!-- Contracts extraídos del código fuente para evitar exploración del executor -->

From src/session/manager.js (current line 352-360 — TO BE REFACTORED):
```javascript
/**
 * @param {string} taskId
 * @param {'running'|'done'|'error'|'review'|'interrupted'} nextStatus
 * @param {string} reason
 * @param {import('../logger.js').Logger} [logger]
 */
export function markSessionStatus(taskId, nextStatus, reason, logger) {
  const current = listSessions().find((s) => s.task_id === taskId || s.task_ref === taskId);
  const fromStatus = current?.status || 'unknown';
  updateSession(taskId, { status: nextStatus });
  if (logger) {
    const log = logger.child({ component: 'session', task_id: taskId });
    stateTransition(log, { from: fromStatus, to: nextStatus, reason });
  }
}
```

From src/gsd/verify.js#266-270 (callsite TO UPDATE):
```javascript
try {
  markSessionStatus(session.task_id, 'review', 'gate-passed', log);
} catch {
  // intencionalmente vacío — ver comentario CR-01 arriba.
}
```

From src/hooks/stop.js#186-193 (callsite TO UPDATE):
```javascript
try {
  const { markSessionStatus } = await import('../session/manager.js');
  markSessionStatus(session.task_id, 'done', 'session-stop', log);
} catch (err) {
  console.error(`[kodo:stop] markSessionStatus failed: ${(err).message}`);
}
```

From test/stop-state-transition.test.js#70 (makeLogger pattern to COPY VERBATIM):
```javascript
function makeLogger() {
  const events = [];
  const logger = {
    info: (m, f) => events.push({ level: 'info', msg: m, fields: f }),
    warn: (m, f) => events.push({ level: 'warn', msg: m, fields: f }),
    error: (m, f) => events.push({ level: 'error', msg: m, fields: f }),
    debug: (m, f) => events.push({ level: 'debug', msg: m, fields: f }),
    child: () => logger,  // CRITICAL: child() returns same logger
  };
  return { logger, events };
}
```

Locked literal (SC#2 ROADMAP byte-exact):
- message: `'markSessionStatus: missing task_id'` (single space colon-space-missing)
- fields keys: `{session_id, status, reason}` — order documented pero JS objects no preservan order; tests match por existence

Locked return shapes (D-05):
- Falsy: `{ok: false, reason: 'missing-task-id'}` (note: kebab-case dentro del string)
- Success: `{ok: true, from: <prevStatus>, to: <nextStatus>}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor markSessionStatus — falsy guard observable + discriminated union return + 5º param sessionId</name>
  <files>src/session/manager.js</files>
  <read_first>
    - src/session/manager.js (líneas 342-361 — bloque markSessionStatus completo)
    - .planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md sección "src/session/manager.js — refactor markSessionStatus (LIFE-02)" (líneas 117-196)
    - .planning/phases/30-sessionrecord-lifecycle/30-CONTEXT.md decisiones D-05..D-09
    - src/labels.js (función isGsdChild líneas 114-123 — defensive prefix guard analog)
  </read_first>
  <behavior>
    - Test 1 (success path): markSessionStatus('task-1', 'done', 'reason', logger) con session activa retorna `{ok: true, from: 'running', to: 'done'}` + emite state.transition event (preservado)
    - Test 2 (success, no caller logger): markSessionStatus('task-1', 'done', 'reason') sin logger retorna `{ok: true, ...}` sin emit (back-compat path preserved)
    - Test 3 (falsy null): markSessionStatus(null, 'done', 'reason', logger, 'sess-abc') retorna `{ok: false, reason: 'missing-task-id'}` + emite warn con payload `{session_id: 'sess-abc', status: 'done', reason: 'reason'}`
    - Test 4 (falsy undefined): markSessionStatus(undefined, 'done', 'reason', logger) sin 5º arg → warn con `session_id: 'unknown'` (D-07 fallback)
    - Test 5 (falsy empty string): markSessionStatus('', 'done', 'reason', logger) → warn + ok:false
  </behavior>
  <action>
    Refactor de `markSessionStatus` (líneas 352-360 de src/session/manager.js). Mantener orden de parámetros existentes; añadir 5º opcional.

    **Nueva firma** (D-07):
    `markSessionStatus(taskId, nextStatus, reason, logger, sessionId?)`

    **Estructura del nuevo body** (sin código fenced — descripción directiva):

    1. **Falsy guard PRIMERO** (D-09: early return BEFORE listSessions/updateSession). Check: `if (!taskId)`. Esto cubre `null`, `undefined`, `''` simultáneamente (mismo patrón defensive prefix de `isGsdChild` en src/labels.js línea 114).
       - Dentro del guard: si `logger` está provisto, llamar `logger.warn('markSessionStatus: missing task_id', { session_id: sessionId || 'unknown', status: nextStatus, reason })`. Notar:
         - **Message byte-exact** (SC#2 lockea): `'markSessionStatus: missing task_id'` con single space después del colon.
         - **Fallback D-07**: `session_id: sessionId || 'unknown'` (string literal 'unknown').
         - **Key D-08**: `status` (NO `next_status`). `reason` directo. `session_id` snake_case (alinea con convención state model — ver state.js typedef línea 13-30).
         - **NO** llamar `logger.child()` aquí — no hay task_id válido para el child context.
       - Return: `{ok: false, reason: 'missing-task-id'}`.
       - **Si `logger` es undefined**: skip el warn pero igual retorna `{ok: false, reason: 'missing-task-id'}` (path back-compat sin observability).

    2. **Success path** (D-05): preservar el código actual con dos cambios:
       - Capturar `fromStatus` igual que ahora (`current?.status || 'unknown'`).
       - Llamar `updateSession(taskId, {status: nextStatus})` igual.
       - Block `if (logger)` preservado con `logger.child({component: 'session', task_id: taskId})` + `stateTransition(log, {from, to, reason})` (PATTERN preserved — solo el success path emite state.transition).
       - Al final: `return {ok: true, from: fromStatus, to: nextStatus}`.

    **JSDoc updates** (Claude's Discretion):
    - Añadir `@param {string} [sessionId]` documentado como "Phase 30 D-07: opcional, para observability del falsy-taskId warn payload. Defaults a 'unknown' string literal."
    - Añadir `@returns {{ok: true, from: string, to: string} | {ok: false, reason: 'missing-task-id'}}`.
    - Mantener doc string original "Retrocompatible" — el cambio es additive para callers que no destructuran (verify.js + stop.js).

    **NO tocar** (out of scope per CONTEXT.md §domain):
    - `addSession`, `removeSession`, `updateSession` en state.js.
    - `listSessions().find(...)` lookup actual (pitfall #3: ONLY scans state.sessions — out of scope para Phase 30 success path `fromStatus`).
    - `stateTransition` event helper (preservado).

    Cambios per D-05..D-09 + WR-07 Phase 22 closure.
  </action>
  <verify>
    <automated>node --test test/session/mark-status.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "markSessionStatus: missing task_id" src/session/manager.js` retorna exactamente 1 match (literal byte-exact)
    - `grep -n "missing-task-id" src/session/manager.js` retorna ≥1 match dentro del cuerpo de markSessionStatus (kebab-case en return shape)
    - `grep -n "ok: true" src/session/manager.js` y `grep -n "ok: false" src/session/manager.js` retornan ≥1 match cada uno
    - La firma actualizada contiene 5 parámetros: `grep -E "export function markSessionStatus\\(taskId, nextStatus, reason, logger, sessionId\\)" src/session/manager.js` matchea
    - `node --test test/session/mark-status.test.js` exits 0 con ≥4 tests pasando
    - `node --test test/stop-state-transition.test.js test/gsd-verify-integration.test.js` exits 0 (callers regression GREEN)
    - `npm test` total pass ≥ 825 + 0 fail (D-14 floor)
  </acceptance_criteria>
  <done>
    markSessionStatus refactorizado: falsy path emite warn observable + return shape determinístico; success path preserva semántica externa (verify.js + stop.js que no destructuran siguen funcionando idénticos); JSDoc actualizado; WR-07 Phase 22 cerrado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar callsites verify.js#267 + stop.js#188 con 5º arg session.session_id</name>
  <files>src/gsd/verify.js, src/hooks/stop.js</files>
  <read_first>
    - src/gsd/verify.js líneas 250-280 (contexto del markSessionStatus callsite + try/catch envelope)
    - src/hooks/stop.js líneas 180-200 (contexto del markSessionStatus callsite + WR-03 catch)
    - .planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md secciones "src/gsd/verify.js — update callsite line ~267" + "src/hooks/stop.js — update callsite line ~188" (líneas 198-260)
    - .planning/phases/30-sessionrecord-lifecycle/30-CONTEXT.md decisión D-07 (opcional 5º param)
  </read_first>
  <action>
    Dos micro-edits — añadir `session.session_id` como 5º argumento positional. `session` ya está en scope en ambos callsites (verify.js#106 + stop.js#132).

    **src/gsd/verify.js línea 267**:
    - Antes: `markSessionStatus(session.task_id, 'review', 'gate-passed', log);`
    - Después: `markSessionStatus(session.task_id, 'review', 'gate-passed', log, session.session_id);`
    - Preservar el try/catch envelope (líneas 266-270 — intencionalmente silencioso, comentario CR-01 referenciado).

    **src/hooks/stop.js línea 188**:
    - Antes: `markSessionStatus(session.task_id, 'done', 'session-stop', log);`
    - Después: `markSessionStatus(session.task_id, 'done', 'session-stop', log, session.session_id);`
    - Preservar el try/catch envelope (líneas 187-193). WR-03 console.error catch SIN cambio.

    **NO tocar** (D-06 — invariante crítico):
    - El try/catch envelope en ambos callsites (verify silent, stop con WR-03 console.error).
    - El return value NO se captura (ambos callers son fire-and-forget). El cambio de return shape de markSessionStatus a discriminated union es additive — NO afecta semántica externa.
    - El logger `log` ya en scope: NO crear uno nuevo, NO renombrar.

    **NO añadir** otros callsites — sólo estos dos (CONTEXT.md §code_context Integration Points). Si grep encuentra más callsites de `markSessionStatus`, parar y reportar al usuario.
  </action>
  <verify>
    <automated>grep -E "markSessionStatus\\(.*, log, session\\.session_id\\)" src/gsd/verify.js src/hooks/stop.js | wc -l | grep -E "^\\s*2\\s*$" && node --test test/gsd-verify-integration.test.js test/stop-state-transition.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "markSessionStatus.*session.session_id" src/gsd/verify.js` retorna exactamente 1
    - `grep -c "markSessionStatus.*session.session_id" src/hooks/stop.js` retorna exactamente 1
    - `grep -c "markSessionStatus" src/` (sin subdirs) retorna mismo conteo que antes del refactor (no se añadieron callsites nuevos)
    - El try/catch envelope en verify.js#266-270 preservado (lectura visual: comentario CR-01 sigue presente)
    - El try/catch + WR-03 console.error en stop.js#187-193 preservado
    - `node --test test/gsd-verify-integration.test.js test/stop-state-transition.test.js` exits 0 (regression tests GREEN)
    - `npm test` total pass ≥ 825 + 0 fail
  </acceptance_criteria>
  <done>
    2 callsites actualizan a la nueva firma pasando `session.session_id` como 5º arg; semántica externa preservada (D-06); regression tests de los callers GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear test/session/mark-status.test.js con 4 escenarios LIFE-02 + fakeLogger memSink</name>
  <files>test/session/mark-status.test.js</files>
  <read_first>
    - test/stop-state-transition.test.js (líneas 1-100 — analog fakeLogger memSink + test scaffold)
    - test/session-of-resolver.test.js (líneas 101-180 — HOME-isolation scaffold reusable)
    - .planning/phases/30-sessionrecord-lifecycle/30-PATTERNS.md sección "test/session/mark-status.test.js (NEW — 4 scenarios LIFE-02)" (líneas 373-427)
    - src/session/manager.js (después del refactor de Task 1, para confirmar firma y shapes)
  </read_first>
  <behavior>
    - Escenario 1 (success): addSession + markSessionStatus(taskId, 'done', 'reason', logger, 'sess-id') → assert `result === {ok: true, from: 'running', to: 'done'}` + warn events length === 0 + state.transition event emitted (preserved)
    - Escenario 2 (null): markSessionStatus(null, 'done', 'session-stop', logger, 'sess-abc') → assert `result === {ok: false, reason: 'missing-task-id'}` + warns[0].msg byte-exact + warns[0].fields === `{session_id: 'sess-abc', status: 'done', reason: 'session-stop'}`
    - Escenario 3 (undefined, NO 5º arg): markSessionStatus(undefined, 'done', 'reason', logger) → warns[0].fields.session_id === 'unknown' (D-07 fallback)
    - Escenario 4 (empty string): markSessionStatus('', 'done', 'reason', logger, 'sess-empty') → warn fires + result.ok === false + fields.session_id === 'sess-empty'
  </behavior>
  <action>
    Crear NEW file `test/session/mark-status.test.js`. Directorio `test/session/` ya creado por Plan 30-01 Task 2 (mkdir idempotente — si no existe, crear).

    **Estructura del file** (sin código fenced):

    1. **Imports**: `describe, it, before, after, afterEach` de `node:test`. `assert` de `node:assert/strict`. `mkdtempSync, rmSync, mkdirSync, writeFileSync` de `node:fs`. `tmpdir` de `node:os`. `join` de `node:path`.

    2. **HOME-isolation scaffold** (mismo patrón que find-session.test.js):
       - `let tmpHome, origHome;`
       - `let markSessionStatus, addSession;` (dynamic-imported POST-HOME — markSessionStatus de manager.js, addSession de state.js).
       - `before(async () => {...})`: guarda origHome, crea tmpHome con `mkdtempSync(join(tmpdir(), 'kodo-life02-'))`, set HOME, mkdir `.kodo/`, dynamic-import `../../src/session/manager.js` + `../../src/session/state.js`.
       - `after(() => {...})`: restore HOME, rmSync tmpHome.
       - `afterEach(() => {...})`: rewrite state.json a `{schema_version: 2, sessions: {}}`.

    3. **fakeLogger memSink** (copiar VERBATIM de test/stop-state-transition.test.js línea 65-80):
       Function `makeLogger()` retorna `{logger, events}`. Logger tiene métodos `info, warn, error, debug` que push a events array con `{level, msg, fields}`. Método `child: () => logger` retorna el mismo logger (CRITICAL — eventos sobreviven .child() chains).

    4. **describe block** (D-10 convention): `'LIFE-02 — markSessionStatus falsy task_id observability'`.

    5. **4 it() escenarios**:

       - **`it('returns {ok:true, from, to} when task_id present and session exists', ...)`**:
         - Construye SessionRecord con `status: 'running'` (shape de session-of-resolver.test.js#142-154).
         - addSession(taskId, session).
         - `const {logger, events} = makeLogger();`
         - `const result = markSessionStatus(taskId, 'done', 'review-gate', logger, 'sess-abc');`
         - `assert.deepEqual(result, {ok: true, from: 'running', to: 'done'});`
         - `assert.equal(events.filter(e => e.level === 'warn').length, 0);` (no warn en success path).
         - Optional: assertions sobre `state.transition` event emission (verificable inspeccionando events que NO sean warn — el preserved emit path).

       - **`it('warns + returns {ok:false} when task_id is null', ...)`**:
         - `const {logger, events} = makeLogger();`
         - `const result = markSessionStatus(null, 'done', 'session-stop', logger, 'sess-abc');`
         - `assert.deepEqual(result, {ok: false, reason: 'missing-task-id'});`
         - `const warns = events.filter(e => e.level === 'warn');`
         - `assert.equal(warns.length, 1);`
         - `assert.equal(warns[0].msg, 'markSessionStatus: missing task_id');` (BYTE-EXACT SC#2).
         - `assert.deepEqual(warns[0].fields, {session_id: 'sess-abc', status: 'done', reason: 'session-stop'});`

       - **`it('warns with session_id "unknown" when task_id is undefined and no 5th arg', ...)`**:
         - `const {logger, events} = makeLogger();`
         - `const result = markSessionStatus(undefined, 'done', 'reason', logger);` // NO 5th arg
         - `assert.deepEqual(result, {ok: false, reason: 'missing-task-id'});`
         - `const warns = events.filter(e => e.level === 'warn');`
         - `assert.equal(warns[0].fields.session_id, 'unknown');` (D-07 fallback).

       - **`it('warns + returns {ok:false} when task_id is empty string', ...)`**:
         - `const {logger, events} = makeLogger();`
         - `const result = markSessionStatus('', 'review', 'gate-passed', logger, 'sess-empty');`
         - `assert.deepEqual(result, {ok: false, reason: 'missing-task-id'});`
         - `assert.equal(events.filter(e => e.level === 'warn')[0].fields.session_id, 'sess-empty');`

    **Prefijo describe `'LIFE-02 — '`** per established pattern (Phase 29 REPORT-NN, QUICK-08).

    **NO añadir**: tests de no-logger path (back-compat sin observability). Los 4 escenarios SC#3 son suficientes; si surge necesidad de cobertura adicional, abrir follow-up phase. Mantener el file enfocado.
  </action>
  <verify>
    <automated>node --test test/session/mark-status.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `test/session/mark-status.test.js` existe y tiene ≥130 líneas
    - `grep -c "it('" test/session/mark-status.test.js` retorna 4 (4 escenarios)
    - `grep "describe('LIFE-02" test/session/mark-status.test.js` matchea exactamente 1
    - `grep "markSessionStatus: missing task_id" test/session/mark-status.test.js` retorna ≥1 match (byte-exact)
    - `grep "missing-task-id" test/session/mark-status.test.js` retorna ≥1 match (kebab-case return shape)
    - `grep "session_id: 'unknown'" test/session/mark-status.test.js` retorna ≥1 match (D-07 fallback test)
    - `node --test test/session/mark-status.test.js` exits 0 con 4 tests pasando
    - `node --test test/session/find-session.test.js test/session/mark-status.test.js` exits 0 (subdirectorio test/session/ funciona — pitfall #4)
    - `npm test` total pass ≥ 825 con 0 fail (D-14 floor)
  </acceptance_criteria>
  <done>
    4 escenarios LIFE-02 GREEN; fakeLogger memSink captura warn payloads byte-exact; D-07 sessionId fallback 'unknown' verificado; pitfall #4 (test/session/ subdirectorio descubierto por test runner) confirmado vía `npm test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 30-02 modifica function in-process (markSessionStatus) + 2 callsites + logger emission. Sin nueva superficie externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Spoofing | markSessionStatus signature | N/A | 5º param `sessionId` es positional opcional — no nuevo auth surface. Caller-controlled string passthrough al warn payload |
| T-30-08 | Tampering | logger.warn payload | accept | Warn fields `{session_id, status, reason}` provienen del caller-scope (ya trusted en flow GSD verify + stop hook). Sin nueva user-input surface |
| T-30-09 | Repudiation | observability vía warn emit | mitigate | El refactor MEJORA repudiation defense — antes el falsy-taskId path era no-op silencioso; ahora emite warn observable. Net-positive para audit |
| T-30-10 | Information disclosure | session_id en warn | accept | session_id es UUID interno (no PII). Logger sink ya autorizada para state events (ver state.transition emit preservado en success path) |
| T-30-11 | Denial of service | logger.warn cost | N/A | Logger es injected (fakeLogger en tests, real logger.js en prod) — backpressure handling delegado al logger impl. Falsy-taskId path es single warn fire-and-forget |
| T-30-12 | Elevation of privilege | new return shape `{ok, ...}` | N/A | Discriminated union additive. Callers existentes (verify.js#267 + stop.js#188) NO destructuran — semántica externa intacta (D-06 invariante) |

**Net assessment**: Phase 30-02 surface es refactor in-process + warn emit + 2 callsite arg passthrough. Mayor STRIDE dimensions N/A o accept. Repudiation MEJORADA (mitigate). Zero net-new attack surface.
</threat_model>

<verification>
- Suite global ≥825 pass + 0 fail (D-14 floor)
- `node --test test/session/mark-status.test.js` GREEN con 4 tests
- `node --test test/gsd-verify-integration.test.js test/stop-state-transition.test.js` GREEN (callers regression intacta)
- `grep -n "markSessionStatus: missing task_id" src/session/manager.js` retorna exactamente 1 línea (literal byte-exact SC#2)
- `grep -E "markSessionStatus\\(.*, log, session\\.session_id\\)" src/gsd/verify.js src/hooks/stop.js` retorna 2 líneas (1 por callsite)
- Try/catch envelopes preservados en verify.js#266-270 + stop.js#187-193 (D-06)
</verification>

<success_criteria>
SC#2 ROADMAP (warn payload byte-exact + return shape `{ok: false, reason: 'missing-task-id'}`): satisfied via markSessionStatus refactor.
SC#3 (4 escenarios mark-status test): satisfied por test/session/mark-status.test.js.
SC#4 suite ≥825 + WR-07 Phase 22 closed: closure documentada en SUMMARY + STATE.md update (post-phase).
</success_criteria>

<output>
After completion, create `.planning/phases/30-sessionrecord-lifecycle/30-02-SUMMARY.md` documentando:
- LIFE-02 entrega: markSessionStatus refactor (falsy guard observable + discriminated union return + 5º param sessionId)
- 2 callsites actualizados (verify.js#267, stop.js#188) con `session.session_id` como 5º arg
- 4 escenarios test añadidos
- WR-07 Phase 22 closure note
- Suite global delta (pre vs post)
- Pitfall #2 cita: D-04 en CONTEXT.md decía `archived_at` pero el código fuente usa `ended_at` — Phase 30 mantiene `ended_at` por autoritatividad del source. (Solo relevante para Plan 30-01, pero documentar aquí si Task 3 tocó history shape para tests.)
</output>
