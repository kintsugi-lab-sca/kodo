---
phase: 39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta
plan: 04
type: execute
wave: 3
depends_on: ["39.1-01", "39.1-02", "39.1-03"]
files_modified:
  - src/server.js
  - src/cli/dashboard/client.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-table.test.js
autonomous: true
requirements: [TUI-15]
must_haves:
  truths:
    - "GET /comments/{taskId} responde {comments:[], supported:false} cuando el provider no tiene listComments"
    - "GET /comments/{taskId} responde {comments, supported:true} cuando el provider sí soporta (aditivo, byte-compatible)"
    - "El overlay ink muestra 'comments not supported by this provider' (distinto de 'no comments yet') cuando supported===false"
    - "El mensaje unsupported es distinguible bajo NO_COLOR (redundancia textual, no solo color)"
  artifacts:
    - path: "src/server.js"
      provides: "Handler /comments/ con campo aditivo `supported`"
      contains: "supported"
    - path: "src/cli/dashboard/App.js"
      provides: "Handler `c` que mapea supported===false a status 'unsupported' + constante OVERLAY_COMMENTS_UNSUPPORTED exportada"
      contains: "OVERLAY_COMMENTS_UNSUPPORTED"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "renderOverlay que pinta OVERLAY_COMMENTS_UNSUPPORTED para status 'unsupported'"
      contains: "OVERLAY_COMMENTS_UNSUPPORTED"
  key_links:
    - from: "src/server.js (/comments handler)"
      to: "src/cli/dashboard/App.js (handler c)"
      via: "campo supported en la respuesta JSON"
      pattern: "supported"
    - from: "src/cli/dashboard/App.js"
      to: "src/cli/dashboard/SessionTable.js (renderOverlay)"
      via: "overlaySnapshot.status === 'unsupported'"
      pattern: "OVERLAY_COMMENTS_UNSUPPORTED"
---

<objective>
Cerrar TUI-15 (D-07/D-08): el overlay de comentarios no distingue "provider sin soporte de listComments" de "tarea sin comentarios" — ambos muestran `OVERLAY_COMMENTS_EMPTY` ("no comments yet"). El server señala la diferencia con un campo ADITIVO `supported` en la respuesta de `/comments/{taskId}` (NO se crea endpoint nuevo — solo cambia la shape, byte-compatible). La TUI renderiza un mensaje distinto cuando `supported === false`.

Purpose: honestidad de UI — el operador debe saber si el provider no soporta comentarios (estado permanente) vs la tarea no tiene comentarios aún (estado transitorio). Cambio aditivo: clientes viejos ignoran `supported` (invariante v0.9 "respuestas JSON aditivas/byte-compatibles").

Output: handler `/comments/` con `supported` en `src/server.js`; typedef y consumo en `src/cli/dashboard/client.js`; handler `c` + constante `OVERLAY_COMMENTS_UNSUPPORTED` en `src/cli/dashboard/App.js`; render del mensaje en `src/cli/dashboard/SessionTable.js`; tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-CONTEXT.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md
@.planning/v0.9-MILESTONE-AUDIT.md

<interfaces>
<!-- server.js:421-440 — handler GET /comments/ (server-side real) -->
const comments = typeof provider.listComments === 'function'
  ? await provider.listComments({ id: session.task_id, projectId: session.project_id })
  : [];
res.writeHead(200, { 'Content-Type': 'application/json' });
res.end(JSON.stringify({ comments }));
<!-- El 404 'Session not found' (426-429) y el 500 catch (436-438) NO cambian. -->

<!-- client.js — fetchComments typedef (66-70) y return (104-105) -->
@typedef {( { ok: true, data: { comments: any[] } } | { ok:false, ... code?: 'not-found'|'http'|'network' } )} FetchCommentsResult
...
if (!Array.isArray(data.comments)) return { ok: false, error: 'bad shape' };
return { ok: true, data };
<!-- `data` ya pasa-through entero → `data.supported` estará disponible si el server lo añade. Extender el typedef a { comments: any[], supported?: boolean }. -->

<!-- App.js handler `c` (329-364) — mapeo actual del discriminante a status -->
if (res.ok) {
  const comments = res.data.comments;
  if (comments.length > 0) { status = 'ok'; lines = ... }
  else { status = 'empty'; }
} else if (res.code === 'not-found') { status = 'not-found'; }
else { status = 'error'; }
setOverlaySnapshot({ kind: 'comments', taskRef: ..., status, lines });

<!-- App.js constantes overlay EXPORTADAS (95-100) — patrón literal-stable a seguir -->
export const OVERLAY_COMMENTS_EMPTY = 'no comments yet';
export const OVERLAY_COMMENTS_NOT_FOUND = 'task not found';
export const OVERLAY_COMMENTS_ERROR = 'error fetching comments';

<!-- SessionTable.js renderOverlay (135-149) — switch de status para el body del overlay -->
if (snap.status === 'not-found') { copy = OVERLAY_COMMENTS_NOT_FOUND; color = 'red'; }
else if (snap.status === 'error') { copy = isLogs ? OVERLAY_LOGS_ERROR : OVERLAY_COMMENTS_ERROR; color = 'red'; }
else { /* 'empty' */ copy = isLogs ? OVERLAY_LOGS_EMPTY : OVERLAY_COMMENTS_EMPTY; }
<!-- SessionTable.js importa OVERLAY_COMMENTS_* desde App.js (26-34). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir el campo aditivo `supported` al handler /comments/ (server.js)</name>
  <files>src/server.js</files>
  <read_first>
    - src/server.js (leer líneas 421-441 — handler GET /comments/ completo, incl. 404 y 500)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección D-07 /comments aditivo supported)
  </read_first>
  <behavior>
    - provider sin listComments (typeof !== 'function') → respuesta 200 `{ comments: [], supported: false }`
    - provider con listComments que devuelve [] → respuesta 200 `{ comments: [], supported: true }`
    - provider con listComments que devuelve [c1, c2] → respuesta 200 `{ comments: [c1, c2], supported: true }`
    - sesión inexistente → 404 `{ error: 'Session not found' }` (SIN cambio)
    - listComments lanza → 500 `{ error: ... }` (SIN cambio)
  </behavior>
  <action>
    En el handler GET /comments/ (server.js:421-440): calcular `const supported = typeof provider.listComments === 'function';`. Cuando `supported`, llamar `await provider.listComments(...)` para `comments`; cuando no, `comments = []`. Cambiar el `res.end(JSON.stringify({ comments }))` (435) a `res.end(JSON.stringify({ comments, supported }))`. El campo `supported` es ADITIVO (byte-compatible: clientes viejos lo ignoran). NO crear endpoint nuevo (constraint v0.9). NO tocar el 404 'Session not found' (426-429) ni el 500 catch (436-438). NO tocar otros handlers. Si existe un test de server para /comments, extenderlo con los dos casos (supported true/false); si no existe en una suite hermética sencilla, documentar el comportamiento en el SUMMARY y dejar la verificación E2E al overlay (Task 3) — pero preferir un test si la infra de server-test ya existe.
  </action>
  <acceptance_criteria>
    - `grep -c "supported" src/server.js` es ≥ 2 (cálculo + serialización)
    - `grep -c "JSON.stringify({ comments, supported })" src/server.js` es exactamente 1
    - `grep -c "'Session not found'" src/server.js` es ≥ 1 (404 intacto)
    - `node --check src/server.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/server.js && test "$(grep -c 'JSON.stringify({ comments, supported })' src/server.js)" = "1"</automated>
  </verify>
  <done>El handler /comments/ devuelve `supported` (false sin listComments, true si soporta); 404/500 intactos; byte-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Extender el typedef de fetchComments para pasar `supported` (client.js)</name>
  <files>src/cli/dashboard/client.js</files>
  <read_first>
    - src/cli/dashboard/client.js (leer líneas 60-110 — typedef FetchCommentsResult y la función fetchComments)
    - src/server.js (shape de la respuesta tras Task 1)
  </read_first>
  <action>
    Actualizar el `@typedef FetchCommentsResult` (client.js:66-70): cambiar `data: { comments: any[] }` a `data: { comments: any[], supported?: boolean }` para que el consumo de `res.data.supported` en App.js (Task 3) sea type-safe. El cuerpo de `fetchComments` ya hace pass-through de `data` entero (`return { ok: true, data }`, línea 105) tras la guard `Array.isArray(data.comments)` (104) — `data.supported` ya viaja sin cambios de runtime. NO modificar la lógica de runtime (la guard de shape, los discriminantes 404/http/network). Solo el typedef + actualizar el JSDoc de la función para mencionar el campo `supported` aditivo (D-07).
  </action>
  <acceptance_criteria>
    - `grep -c "supported" src/cli/dashboard/client.js` es ≥ 1 (typedef extendido)
    - `grep -c "Array.isArray(data.comments)" src/cli/dashboard/client.js` es exactamente 1 (guard de shape de runtime sin cambio)
    - `node --check src/cli/dashboard/client.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/cli/dashboard/client.js && test "$(grep -c 'supported' src/cli/dashboard/client.js)" -ge "1"</automated>
  </verify>
  <done>El typedef de fetchComments expone `data.supported?: boolean`; runtime sin cambio (pass-through).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mapear supported===false a 'unsupported' en el handler `c` + constante + render</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js, test/dashboard-table.test.js</files>
  <read_first>
    - src/cli/dashboard/App.js (leer líneas 90-100 constantes overlay + 329-365 handler `c`)
    - src/cli/dashboard/SessionTable.js (leer líneas 26-34 imports + 135-149 switch de status en renderOverlay)
    - test/dashboard-table.test.js (leer estructura de describes + el patrón de assert de overlay)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección D-07 downstream TUI + Shared Patterns "Constantes literal-estables EXPORTADAS")
  </read_first>
  <behavior>
    - handler `c` con res.ok && res.data.supported === false → overlaySnapshot.status = 'unsupported'
    - handler `c` con res.ok && comments vacíos && supported !== false → status = 'empty' (sin regresión)
    - handler `c` con res.ok && comments.length > 0 → status = 'ok' (sin regresión)
    - renderOverlay con status 'unsupported' (kind comments) → body muestra OVERLAY_COMMENTS_UNSUPPORTED, distinto de OVERLAY_COMMENTS_EMPTY
    - OVERLAY_COMMENTS_UNSUPPORTED es literal-estable, exportada, redundancia textual (legible bajo NO_COLOR)
  </behavior>
  <action>
    En App.js: añadir la constante exportada `export const OVERLAY_COMMENTS_UNSUPPORTED = 'comments not supported by this provider';` junto al resto de `OVERLAY_COMMENTS_*` (cerca de App.js:95), siguiendo el patrón literal-stable exportado (redundancia textual NO_COLOR, sin depender de color). En el handler `c` (329-364), dentro de la rama `if (res.ok)`, ANTES de la lógica `comments.length > 0 ? 'ok' : 'empty'`, añadir: `if (res.data.supported === false) { status = 'unsupported'; }` y solo si no es unsupported aplicar la lógica ok/empty existente (un `else` que envuelva el bloque actual de comments). El snapshot final `setOverlaySnapshot({ kind: 'comments', taskRef, status, lines })` no cambia de forma. En SessionTable.js: importar `OVERLAY_COMMENTS_UNSUPPORTED` desde App.js (añadir al bloque de import 26-34) y, en el switch de `renderOverlay` (135-149), añadir una rama `else if (snap.status === 'unsupported')` que asigne `copy = OVERLAY_COMMENTS_UNSUPPORTED;` (dejar `color` undefined → dimColor como el caso empty, NO red — no es un error, es un estado informativo). En test/dashboard-table.test.js: añadir un describe que (a) assere `OVERLAY_COMMENTS_UNSUPPORTED` byte-stable contra el literal exacto, y (b) verifique que renderOverlay con un snapshot `{kind:'comments', status:'unsupported', ...}` pinta el copy unsupported y NO el empty (mismo patrón de assert de overlay ya usado en el archivo). NO romper los describes existentes de overlay empty/ok/error.
  </action>
  <acceptance_criteria>
    - `grep -c "OVERLAY_COMMENTS_UNSUPPORTED" src/cli/dashboard/App.js` es ≥ 1 (declarada/exportada)
    - `grep -c "res.data.supported === false" src/cli/dashboard/App.js` es exactamente 1
    - `grep -c "OVERLAY_COMMENTS_UNSUPPORTED" src/cli/dashboard/SessionTable.js` es ≥ 2 (import + uso en renderOverlay)
    - `grep -c "snap.status === 'unsupported'" src/cli/dashboard/SessionTable.js` es exactamente 1
    - `node -e "const m=await import('./src/cli/dashboard/App.js'); console.log(m.OVERLAY_COMMENTS_UNSUPPORTED)" --input-type=module` imprime `comments not supported by this provider`
    - `node --test test/dashboard-table.test.js` exit 0 (incl. el nuevo describe unsupported)
    - `node --test test/format-isolation.test.js` exit 0 (color-isolation intacta)
  </acceptance_criteria>
  <verify>
    <automated>node --check src/cli/dashboard/App.js && node --check src/cli/dashboard/SessionTable.js && test "$(grep -c 'res.data.supported === false' src/cli/dashboard/App.js)" = "1" && node --test test/dashboard-table.test.js && node --test test/format-isolation.test.js</automated>
  </verify>
  <done>El overlay distingue unsupported de empty; constante exportada literal-stable y legible NO_COLOR; tests verdes; color-isolation intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider → server /comments handler | datos de comentarios del provider (ya manejados por el handler existente) |
| server /comments → cliente TUI/web | respuesta JSON local con campo aditivo `supported` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-04 | Information disclosure | campo `supported` en /comments | accept | `supported` es un booleano derivado de `typeof provider.listComments` — no expone datos sensibles ni rutas internas. Aditivo byte-compatible; no añade endpoint ni entrada de usuario. El task_id del path ya se sanea con encodeURIComponent/decodeURIComponent simétrico (T-39-01 Phase 39, sin cambio). |
| T-39.1-SC | Tampering | npm/pip/cargo installs | n/a | Sin instalaciones de paquetes (cero deps nuevas). |
</threat_model>

<verification>
- `node --test test/dashboard-table.test.js` verde (describe unsupported + empty/ok/error sin regresión)
- `node --test test/format-isolation.test.js` verde (cero picocolors bajo src/cli/dashboard/)
- La respuesta de `/comments/` incluye `supported` y sigue incluyendo `comments` (byte-compatible)
- El overlay muestra dos mensajes distinguibles: 'no comments yet' (empty) vs 'comments not supported by this provider' (unsupported), ambos legibles bajo NO_COLOR
- Suite completa sin regresión: `npm test` verde
</verification>

<success_criteria>
- TUI-15 cerrado: server señala unsupported vs vacío con campo aditivo; overlay ink renderiza mensajes distintos
- Byte-compatibilidad preservada (clientes viejos ignoran `supported`); cero endpoints nuevos
- Constante OVERLAY_COMMENTS_UNSUPPORTED exportada, literal-stable, legible sin color
- NOTA (D-09): la vista HTML web (toggleComments, server.js:167-195) puede recibir el mismo trato — documentar en SUMMARY si se aplicó o se dejó como deuda acotada (criterio defensivo: prioridad es el overlay ink, no la web HTML)
</success_criteria>

<output>
Create `.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-04-SUMMARY.md` when done.
Documentar la decisión sobre D-09 (vista HTML web): aplicada o deuda acotada con justificación.
</output>
