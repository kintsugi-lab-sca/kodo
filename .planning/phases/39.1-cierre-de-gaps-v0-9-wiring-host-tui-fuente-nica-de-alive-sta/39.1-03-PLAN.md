---
phase: 39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta
plan: 03
type: execute
wave: 2
depends_on: ["39.1-01"]
files_modified:
  - src/cli/dashboard/format.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-format.test.js
autonomous: true
requirements: [TUI-10]
must_haves:
  truths:
    - "statusColor colorea la columna status de sesiones v3 (idle/needs-input) cuyo status v2 es null"
    - "La paleta LOCKED se respeta: idle=yellow, needs-input=cyan, dead=red (reusados de STATE_BADGES, sin colores nuevos)"
    - "statusColor sigue devolviendo nombres de color ink (nunca ANSI); función pura (byte-determinismo --json/NO_COLOR)"
  artifacts:
    - path: "src/cli/dashboard/format.js"
      provides: "statusColor v3-aware derivando del estado v3 igual que stateBadge"
      contains: "STATE_BADGES"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "call-site de statusColor con el argumento de estado v3 propagado desde la fila"
      contains: "statusColor"
  key_links:
    - from: "src/cli/dashboard/SessionTable.js"
      to: "statusColor (format.js)"
      via: "session.state propagado al call-site"
      pattern: "statusColor"
---

<objective>
Cerrar TUI-10 (fix mecánico, Claude's Discretion): `statusColor` solo ve `session.status` (v2). Sesiones v3 con `status` v2 `null` (idle / needs-input) → la celda `status` queda sin color (solo el badge izquierdo, vía `stateBadge`, colorea). Hacer `statusColor` v3-aware: que derive también del estado v3 (`session.state`), exactamente como `stateBadge` ya hace con `state ?? status`.

Purpose: el patrón ya está establecido por `stateBadge` (format.js:136, `STATE_BADGES[state ?? '']`). Reutilizar la paleta LOCKED (idle=yellow, needs-input=cyan, dead=red) sin duplicar nombres de color ni inventar colores. Preserva color-isolation (nombres ink, cero picocolors) y byte-determinismo `--json`/NO_COLOR (función pura).

Output: `statusColor` v3-aware en `format.js`; call-site en `SessionTable.js` actualizado para propagar `session.state`; tests de `dashboard-format.test.js` extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-CONTEXT.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md
@.planning/v0.9-MILESTONE-AUDIT.md

<interfaces>
<!-- Analog de referencia (patrón a replicar) — format.js:121-138 -->
export const STATE_BADGES = Object.freeze({
  running: { glyph: '▶', color: 'green', label: 'running' },
  idle: { glyph: '⏸', color: 'yellow', label: 'idle' },
  'needs-input': { glyph: '🔔', color: 'cyan', label: 'needs-input' },
  dead: { glyph: '✗', color: 'red', label: 'dead' },
});
export function stateBadge(state) { return STATE_BADGES[state ?? ''] ?? {}; }

<!-- Código a modificar — format.js:91-98 (firma actual) -->
export function statusColor(status, alive) {
  if (status === 'running' && !alive) return { color: 'red' };
  if (status === 'running') return { color: 'green' };
  if (status === 'review') return { color: 'cyan' };
  if (status === 'error') return { color: 'magenta' };
  if (status === 'done') return { dim: true };
  return {};
}

<!-- Call-site — SessionTable.js:277 (dentro de rows.map((session, i) => {...})) -->
const sc = statusColor(session.status ?? '', session.alive);
<!-- `session.state` está disponible en la fila (ya se usa en SessionTable.js:290 para stateBadge). -->

<!-- Invariantes LOCKED (D-08): nombres de color ink ('green'|'red'|'cyan'|'magenta'|'yellow') o {dim:true},
     JAMÁS ANSI. red reservado a muerto/zombie; magenta para error (distinto del red). Función pura, sin I/O. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hacer statusColor v3-aware (format.js)</name>
  <files>src/cli/dashboard/format.js, test/dashboard-format.test.js</files>
  <read_first>
    - src/cli/dashboard/format.js (leer líneas 75-138 — statusColor, statusLabel, STATE_BADGES, stateBadge)
    - test/dashboard-format.test.js (leer líneas 1-90 — imports + describe de statusColor existente para extenderlo)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección format.js TUI-10: Opción A alineada con stateBadge)
  </read_first>
  <behavior>
    - statusColor('running', true) → { color: 'green' } (sin cambio)
    - statusColor('running', false) → { color: 'red' } (zombie, sin cambio)
    - statusColor('review', true) → { color: 'cyan' } (sin cambio)
    - statusColor('error', true) → { color: 'magenta' } (sin cambio)
    - statusColor('done', true) → { dim: true } (sin cambio)
    - statusColor(null, true, 'idle') → { color: 'yellow' } (NUEVO: deriva del estado v3, reusa STATE_BADGES.idle.color)
    - statusColor(null, true, 'needs-input') → { color: 'cyan' } (NUEVO: reusa STATE_BADGES['needs-input'].color)
    - statusColor(null, false, 'dead') → { color: 'red' } (NUEVO: reusa STATE_BADGES.dead.color)
    - statusColor(null, true, undefined) → {} (sin status v2 ni state v3 reconocido → vacío)
    - statusColor('running', true, 'idle') → { color: 'green' } (status v2 GANA cuando matchea una rama v2 — no regresión)
  </behavior>
  <action>
    Modificar `statusColor` a la firma `statusColor(status, alive, state)` (Opción A del PATTERNS, alineada con stateBadge). Mantener PRIMERO el switch v2 existente (running+!alive→red, running→green, review→cyan, error→magenta, done→dim) para que el status v2 tenga precedencia cuando matchee. Si ninguna rama v2 matchea Y `STATE_BADGES[state]` existe, devolver `{ color: STATE_BADGES[state].color }` (reusa la paleta YA LOCKED — NO duplicar literales de color; leer el color desde STATE_BADGES). Si nada matchea, devolver `{}`. Actualizar el JSDoc de `statusColor` para documentar el tercer parámetro `state` y la derivación v3. PRESERVAR: nombres de color ink (nunca ANSI), función pura sin I/O (byte-determinismo). En `test/dashboard-format.test.js`, extender el describe "TUI-10 (D-08)" con los casos v3 nuevos del bloque <behavior> (idle→yellow, needs-input→cyan, dead→red, precedencia v2, fallback {}), importando STATE_BADGES si conviene assertar contra la paleta en vez de literales hardcoded.
  </action>
  <acceptance_criteria>
    - `node --test test/dashboard-format.test.js` exit 0 con los casos v3 nuevos
    - `node -e "const {statusColor}=await import('./src/cli/dashboard/format.js'); console.log(JSON.stringify(statusColor(null,true,'idle')))" --input-type=module` imprime `{"color":"yellow"}`
    - `node -e "const {statusColor}=await import('./src/cli/dashboard/format.js'); console.log(JSON.stringify(statusColor('running',true,'idle')))" --input-type=module` imprime `{"color":"green"}` (precedencia v2)
    - `grep -vc '^[[:space:]]*//\|^[[:space:]]*\*' src/cli/dashboard/format.js` — el cuerpo de statusColor referencia `STATE_BADGES` (paleta reusada, no literales nuevos): `grep -n "STATE_BADGES\[state" src/cli/dashboard/format.js` devuelve ≥ 1
    - `node --test test/format-isolation.test.js` exit 0 (cero picocolors bajo src/cli/dashboard/ — color-isolation intacto)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dashboard-format.test.js && node --test test/format-isolation.test.js && test "$(grep -c 'STATE_BADGES\[state' src/cli/dashboard/format.js)" -ge "1"</automated>
  </verify>
  <done>statusColor deriva del estado v3 reusando STATE_BADGES; status v2 conserva precedencia; paleta LOCKED y color-isolation intactas; tests v3 verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Propagar session.state al call-site de statusColor (SessionTable.js)</name>
  <files>src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js (leer líneas 273-303 — el rows.map con `const sc = statusColor(...)` y el render de la celda status)
    - src/cli/dashboard/format.js (firma final de statusColor tras Task 1)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección "Call-sites de statusColor a actualizar")
  </read_first>
  <action>
    Actualizar el call-site `const sc = statusColor(session.status ?? '', session.alive);` (SessionTable.js:277) para pasar el tercer argumento `session.state`: `const sc = statusColor(session.status ?? '', session.alive, session.state);`. `session.state` ya está disponible en la fila (se usa en la línea 290 para `stateBadge(session.state ?? session.status ?? '')`). NO tocar el render de la celda `status` (300, `cell({ width: COLS.status, text: cells.status, color: sc.color, dim: sc.dim, ... })`) — sigue consumiendo `sc.color`/`sc.dim` igual. NO tocar la celda `state` del badge (289-293). Verificar que `node --test test/dashboard-table.test.js` siga verde tras el cambio de call-site.
  </action>
  <acceptance_criteria>
    - `grep -c "statusColor(session.status ?? '', session.alive, session.state)" src/cli/dashboard/SessionTable.js` es exactamente 1
    - `grep -c "sc.color\|sc.dim" src/cli/dashboard/SessionTable.js` es ≥ 1 (render de celda status sin cambio)
    - `node --check src/cli/dashboard/SessionTable.js` exit 0
    - `node --test test/dashboard-table.test.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/cli/dashboard/SessionTable.js && test "$(grep -c "statusColor(session.status ?? '', session.alive, session.state)" src/cli/dashboard/SessionTable.js)" = "1" && node --test test/dashboard-table.test.js</automated>
  </verify>
  <done>El call-site propaga session.state; el render de la celda status colorea idle/needs-input v3; suite de la tabla verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de /status → presentación pura (format.js) | transformación in-process; sin entrada de usuario nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-03 | (n/a) | statusColor (función pura de presentación) | accept | Cambio de presentación puro: añade un parámetro derivado de datos ya presentes en la fila. Sin I/O, sin red, sin entrada de usuario. Color-isolation preservada (verificada por test/format-isolation.test.js). Byte-determinismo --json/NO_COLOR intacto (función pura). Sin amenaza nueva. |
| T-39.1-SC | Tampering | npm/pip/cargo installs | n/a | Sin instalaciones de paquetes (cero deps nuevas). |
</threat_model>

<verification>
- `node --test test/dashboard-format.test.js` verde con casos v3 (idle/needs-input/dead colorean)
- `node --test test/format-isolation.test.js` verde (cero picocolors bajo src/cli/dashboard/)
- `node --test test/dashboard-table.test.js` verde (render de la tabla sin regresión)
- Paleta LOCKED respetada: ningún color nuevo introducido (statusColor lee de STATE_BADGES)
</verification>

<success_criteria>
- TUI-10 cerrado: la columna `status` de sesiones idle/needs-input (status v2 null) colorea según el estado v3
- Paleta LOCKED y color-isolation intactas; status v2 conserva precedencia (sin regresión de zombie/running/review/error/done)
- Byte-determinismo --json/NO_COLOR preservado (función pura)
</success_criteria>

<output>
Create `.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-03-SUMMARY.md` when done.
</output>
