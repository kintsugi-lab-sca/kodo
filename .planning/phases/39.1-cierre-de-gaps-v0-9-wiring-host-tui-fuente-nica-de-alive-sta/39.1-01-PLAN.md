---
phase: 39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/index.js
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - test/dashboard-table.test.js
autonomous: true
requirements: [TUI-17]
must_haves:
  truths:
    - "El dashboard ink no construye ni recibe la prop `host` (código muerto eliminado)"
    - "App.js no exporta HOST_ERR_UNAVAILABLE ni HOST_ERR_TIMEOUT"
    - "El dashboard sigue arrancando, renderizando la tabla y aceptando Enter/focus sin regresión"
  artifacts:
    - path: "src/cli/dashboard/index.js"
      provides: "runDashboard sin wiring de host muerto; cmuxBin preservado para onFocus"
      contains: "onFocus"
    - path: "src/cli/dashboard/App.js"
      provides: "App sin hostError/setHostError/HOST_ERR_*; focusError intacto"
      contains: "FOCUS_ERR_ZOMBIE"
    - path: "src/cli/dashboard/SessionTable.js"
      provides: "SessionTable sin prop hostError; footer-error solo de focusError"
  key_links:
    - from: "src/cli/dashboard/index.js"
      to: "App (createElement)"
      via: "props sin host; onFocus presente"
      pattern: "onFocus"
    - from: "src/cli/dashboard/App.js"
      to: "SessionTable (createElement)"
      via: "prop hostError eliminada"
      pattern: "focusError"
---

<objective>
Cerrar el BLOCKER TUI-17 por ELIMINACIÓN (D-01): el wiring de la prop `host` del WorkspaceHost al dashboard ink es código muerto de Phase 38 (la prop se descarta silenciosamente, `setHostError` solo se llama con `null`, `HOST_ERR_*` nunca se emiten). Se elimina todo el rastro de ese camino sin introducir consulta host directa desde el dashboard.

Purpose: el camino del server (`startReconcileLoop` → `state.json` → `GET /status` → tabla) ya entrega idle/needs-input al operador, cumpliendo la promesa funcional de Phase 38. Cablear la consulta host directa crearía una segunda fuente de verdad para `alive` — exactamente la divergencia que TUI-13/14 (Plan 02) corrige. Simplicidad + una sola fuente de verdad (D-01/D-03).

Output: `index.js`, `App.js`, `SessionTable.js` y `test/dashboard-table.test.js` limpios del rastro de `host`/`hostError`/`HOST_ERR_*`; dashboard sin regresión funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-CONTEXT.md
@.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md
@.planning/v0.9-MILESTONE-AUDIT.md

<interfaces>
<!-- Patrón ESPEJO a CONSERVAR como modelo (NO tocar): focusError SÍ se emite y se queda.
     hostError solo se seteaba a null → muere. -->

App.js (lo que SE QUEDA — modelo de lo que es código vivo):
- export const FOCUS_ERR_ZOMBIE / FOCUS_ERR_ENOENT / focusErrFailed(code)  (App.js:73-81)
- const [focusError, setFocusError] = useState(null)                       (App.js:162)
- dismiss de focusError dentro de useInput                                  (App.js:259-262)
- emisión real de focusError en el handler de Enter                         (App.js:422/431/437)

App.js (lo que MUERE — código muerto TUI-17):
- export const HOST_ERR_UNAVAILABLE / HOST_ERR_TIMEOUT                      (App.js:87-88)
- const [hostError, setHostError] = useState(null)                         (App.js:167)
- dismiss de hostError dentro de useInput                                  (App.js:263-267)
- prop hostError pasada a SessionTable                                     (App.js:481)

index.js (lo que SE QUEDA — NO tocar): cmuxBin (122) lo usa onFocus (144).
index.js (lo que MUERE): import dinámico getHost de host/interface.js + const host + prop host (124-130, 145-147).

SessionTable.js: usa la prop `hostError` (param 196) y `footerError = focusError ?? hostError` (235). NO importa HOST_ERR_* (confirmado por grep — ver Task 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar el wiring de host en index.js</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - src/cli/dashboard/index.js (estado actual real — leer ANTES de editar)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección `index.js` D-01/D-02: qué borrar y qué conservar)
  </read_first>
  <action>
    Eliminar el bloque de construcción del WorkspaceHost (D-02): el comentario "Phase 38 (Task 3 / D-09)" y las dos sentencias `const { getHost } = await import('../../host/interface.js');` y `const host = getHost('cmux', { binary: cmuxBin });` (index.js:124-130). Eliminar también la prop `host,` y su comentario "Phase 38 (Task 3): WorkspaceHost inyectado..." del objeto pasado a `createElement(App, {...})` (index.js:145-147). NO eliminar `cmuxBin` (línea 122): lo sigue usando `onFocus` en `runFocus({ exec: execImpl, ref, binary: cmuxBin })` (línea 144). NO tocar: guard non-TTY (90-93), resolveBaseUrl (69-73), alt-screen toggle (137/166), SIGTERM handler (154-157), try/finally con `process.exitCode = 0` (159-169), lazy import de runFocus (111). Tras el borrado, `../../host/interface.js` ya no se importa en este archivo (verificar con grep en acceptance).
  </action>
  <acceptance_criteria>
    - `grep -c "getHost\|host/interface" src/cli/dashboard/index.js` devuelve 0
    - `grep -c "cmuxBin" src/cli/dashboard/index.js` es ≥ 1 (cmuxBin preservado para onFocus)
    - `grep -c "onFocus" src/cli/dashboard/index.js` es ≥ 1 (wiring de focus intacto)
    - `node -e "require('node:fs').readFileSync('src/cli/dashboard/index.js','utf8')"` sin error de sintaxis; `node --check src/cli/dashboard/index.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/cli/dashboard/index.js && test "$(grep -c 'getHost\|host/interface' src/cli/dashboard/index.js)" = "0"</automated>
  </verify>
  <done>index.js no construye ni pasa `host`; cmuxBin y onFocus intactos; import de host/interface.js eliminado.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar hostError/setHostError/HOST_ERR_* en App.js</name>
  <files>src/cli/dashboard/App.js</files>
  <read_first>
    - src/cli/dashboard/App.js (leer al menos líneas 73-100, 160-170, 255-270, 460-490 — estado real)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección `App.js` D-02 eliminación + D-06 sin cambio)
  </read_first>
  <action>
    Eliminar las constantes exportadas `HOST_ERR_UNAVAILABLE` y `HOST_ERR_TIMEOUT` junto a su comentario "Phase 38 D-06: errores del WorkspaceHost..." (App.js:83-88). Eliminar el estado `const [hostError, setHostError] = useState(...)` junto a su comentario "Phase 38 D-06/D-07: estado del footer-error del WorkspaceHost..." (App.js:164-167). Eliminar el bloque de dismiss dentro de `useInput` `if (hostError != null) { setHostError(null); return; }` junto a su comentario "Phase 38 D-06: el host-error usa el MISMO dismiss modal que focusError." (App.js:263-267). Eliminar la prop `hostError,` y su comentario del `createElement(SessionTable, {...})` (App.js:481). PRESERVAR LITERALMENTE (D-06, NO CAMBIAR): el guard de Enter `if (row.alive === false) { setFocusError(FOCUS_ERR_ZOMBIE); return; }` (App.js:418) — `=== false` es deliberado fail-open, NO cambiar a `!row.alive`. PRESERVAR el espejo vivo `focusError`/`setFocusError`/su dismiss (259-262)/`FOCUS_ERR_*` (73-81). El borrado es exactamente "deshacer el espejo de focusError que nunca se cableó".
  </action>
  <acceptance_criteria>
    - `grep -c "HOST_ERR\|hostError\|setHostError" src/cli/dashboard/App.js` devuelve 0
    - `grep -c "FOCUS_ERR_ZOMBIE\|setFocusError" src/cli/dashboard/App.js` es ≥ 2 (focusError vivo preservado)
    - `grep -c "row.alive === false" src/cli/dashboard/App.js` es exactamente 1 (guard D-06 fail-open intacto, sin cambio a `!row.alive`)
    - `node --check src/cli/dashboard/App.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>node --check src/cli/dashboard/App.js && test "$(grep -c 'HOST_ERR\|hostError\|setHostError' src/cli/dashboard/App.js)" = "0" && test "$(grep -c 'row.alive === false' src/cli/dashboard/App.js)" = "1"</automated>
  </verify>
  <done>App.js sin hostError/HOST_ERR_*; focusError y el guard de Enter D-06 intactos.</done>
</task>

<task type="auto">
  <name>Task 3: Quitar la prop hostError de SessionTable.js y reconciliar el test</name>
  <files>src/cli/dashboard/SessionTable.js, test/dashboard-table.test.js</files>
  <read_first>
    - src/cli/dashboard/SessionTable.js (leer líneas 184-240 — params del componente + bloque footerError)
    - test/dashboard-table.test.js (leer líneas 26-32 e 565-580 — import de HOST_ERR_* y el describe que los assera)
    - .planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-PATTERNS.md (sección `SessionTable.js` D-02: confirmar vía grep antes de editar)
  </read_first>
  <action>
    Primero confirmar con `grep -n "HOST_ERR" src/cli/dashboard/SessionTable.js`: el bloque de imports (26-34) NO importa `HOST_ERR_*` (importa solo `OVERLAY_*`/`OVERLAY_VIEWPORT`), así que NO hay import que quitar — registrar este hecho en el SUMMARY. SÍ hay que: (a) eliminar el parámetro `hostError = null,` de la firma del componente `SessionTable({...})` (SessionTable.js:196); (b) simplificar `const footerError = focusError ?? hostError;` (SessionTable.js:235) a `const footerError = focusError;` y actualizar el comentario adyacente "Phase 38 D-06..." para que ya no mencione el host-error (mantener la explicación de focusError). NO tocar el render de `errorLine` (236-239) ni la precedencia errorLine/filterLine. Segundo, en `test/dashboard-table.test.js`: el archivo importa `HOST_ERR_UNAVAILABLE, HOST_ERR_TIMEOUT` desde App.js (línea 29) y los assera en el describe "Phase 38 SC#3: HOST_ERR_* constantes literal-stable (D-06)" (líneas 571-577). Eliminar ese import de las dos constantes (dejar `import App from '../src/cli/dashboard/App.js';`) y eliminar el `describe` completo de HOST_ERR_* (esas constantes ya no existen — el test fallaría al importar undefined). NO eliminar ningún otro describe del archivo.
  </action>
  <acceptance_criteria>
    - `grep -c "hostError" src/cli/dashboard/SessionTable.js` devuelve 0
    - `grep -c "footerError = focusError" src/cli/dashboard/SessionTable.js` es exactamente 1 (sin `?? hostError`)
    - `grep -c "HOST_ERR" test/dashboard-table.test.js` devuelve 0
    - `node --check src/cli/dashboard/SessionTable.js` exit 0
    - `node --test test/dashboard-table.test.js` exit 0 (la suite de la tabla pasa sin el describe de HOST_ERR_*)
  </acceptance_criteria>
  <verify>
    <automated>node --check src/cli/dashboard/SessionTable.js && test "$(grep -c 'hostError' src/cli/dashboard/SessionTable.js)" = "0" && test "$(grep -c 'HOST_ERR' test/dashboard-table.test.js)" = "0" && node --test test/dashboard-table.test.js</automated>
  </verify>
  <done>SessionTable.js sin prop hostError (footer-error solo de focusError); test/dashboard-table.test.js sin import ni describe de HOST_ERR_*; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → dashboard ink (stdin) | input de teclado en proceso local; sin red entrante |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-01 | (n/a) | eliminación de código muerto en dashboard local | accept | Cambio puramente sustractivo (borrar host/hostError/HOST_ERR_*). No añade superficie de entrada, no toca red, auth ni datos. Reduce superficie de ataque al eliminar un import dinámico de host. Sin amenaza nueva de severidad media/alta. |
| T-39.1-SC | Tampering | npm/pip/cargo installs | n/a | No hay instalaciones de paquetes en este plan (cero deps nuevas — invariante stack v0.9). |
</threat_model>

<verification>
- `node --test test/dashboard-table.test.js` verde tras la limpieza
- `grep -rn "HOST_ERR\|hostError" src/cli/dashboard/` devuelve 0 líneas (drift code↔render eliminado en todo el dashboard)
- `grep -rn "host/interface" src/cli/dashboard/` devuelve 0 líneas
- Suite completa sin regresión: `npm test` (o el runner del proyecto) verde
</verification>

<success_criteria>
- TUI-17 cerrado por eliminación: cero rastro de `host`/`hostError`/`HOST_ERR_*` bajo `src/cli/dashboard/` y en `test/dashboard-table.test.js`
- `cmuxBin`, `onFocus`, `focusError` y el guard de Enter D-06 (`row.alive === false`) intactos — sin regresión funcional
- El camino del server (reconcileLoop → /status → tabla) sigue siendo la única fuente de idle/needs-input (documentar en SUMMARY que cumple la promesa funcional de Phase 38, D-03)
</success_criteria>

<output>
Create `.planning/phases/39.1-cierre-de-gaps-v0-9-wiring-host-tui-fuente-nica-de-alive-sta/39.1-01-SUMMARY.md` when done.
Documentar explícitamente (D-03): el blocker se cierra por eliminación; el camino del server cumple la promesa funcional de Phase 38, no se introduce consulta host directa. Registrar que SessionTable.js NO importaba HOST_ERR_* (solo usaba la prop).
</output>
