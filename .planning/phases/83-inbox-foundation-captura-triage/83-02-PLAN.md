---
phase: 83-inbox-foundation-captura-triage
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - src/cli/capture.js
  - src/cli/inbox.js
  - src/cli.js
  - test/inbox-cli.test.js
autonomous: true
requirements: [CAPT-01, CAPT-03, CAPT-04, CAPT-06]

must_haves:
  truths:
    - "`kodo capture \"idea\"` desde cualquier proyecto appendea una línea a `~/.kodo/inbox.md` y sale con código 0 (CAPT-01)"
    - "El tag-proyecto se deriva del cwd por nearest-ancestor sobre projects.json y cae a basename(cwd) cuando no hay match ni cuando el match es ambiguo (D-15)"
    - "`kodo capture` con texto vacío o solo whitespace tras el saneo sale con código 2 y NO escribe ninguna línea"
    - "`kodo capture --origin skill` produce una línea byte-idéntica salvo el campo de origen — el flag existe ya en esta fase para que Phase 84 shellee al mismo writer (D-16)"
    - "`kodo inbox` lista solo las capturas abiertas; `kodo inbox --all` incluye también las cerradas con su estado (CAPT-03, D-12)"
    - "`kodo inbox --json` emite una única línea de JSON parseable, sin ANSI y byte-determinista entre ejecuciones (DX-06)"
    - "`kodo inbox` sobre un inbox inexistente o vacío sale con código 0 y una copy explícita, jamás con un throw (D-18)"
    - "El render human sanea el texto y el destino con el carril de render, de modo que una secuencia de escape de terminal pegada a mano en el fichero nunca se ejecuta en el terminal del operador"
    - "`kodo inbox route <id>` marca enrutada; con `--dest <ref>` añade el trace pointer; sin `--dest` cierra igual y sale 0 (CAPT-06, D-10)"
    - "`kodo inbox discard <id>` marca descartada sin borrar la línea (CAPT-03)"
    - "route/discard sobre un id inexistente o sobre una captura ya cerrada salen con código 2; un lock-timeout o un error de filesystem salen con código 1 (D-13)"
    - "Ningún módulo del inbox importa `node:child_process` ni ninguna ruta del skill de enrutado — el seam es documental (CAPT-04, D-09)"
    - "Los handlers CLI retornan el exit code y nunca invocan el helper de salida del runtime; el registro de commander en `src/cli.js` es quien hace el exit"
    - "`package.json` sigue teniendo exactamente 4 dependencias de producción (invariante cero deps)"
  artifacts:
    - path: "src/cli/capture.js"
      provides: "Thin handler de `kodo capture`: gate de texto, derivación de tag/fecha/origen, delegación al store y exit codes"
      exports: ["runCaptureCli"]
      min_lines: 70
    - path: "src/cli/inbox.js"
      provides: "Thin handler de `kodo inbox` (listado human/JSON) y de `route`/`discard`"
      exports: ["runInboxListCli", "runInboxMarkCli"]
      min_lines: 110
    - path: "src/cli.js"
      provides: "Registro commander de `capture` y del grupo `inbox` con acción propia + subcomandos"
      contains: "kodo capture"
    - path: "test/inbox-cli.test.js"
      provides: "Integración por spawnSync de bin/kodo con HOME sandbox: exit codes, --json determinista y gates source-hygiene"
      min_lines: 150
  key_links:
    - from: "src/cli/capture.js"
      to: "src/inbox/store.js"
      via: "encodeLine + appendCapture + newCaptureId + todayLocal + deriveTag"
      pattern: "appendCapture|encodeLine"
    - from: "src/cli/inbox.js"
      to: "src/inbox/store.js"
      via: "listCaptures para el render y markCapture para route/discard"
      pattern: "listCaptures|markCapture"
    - from: "src/cli/inbox.js"
      to: "src/cli/format.js"
      via: "createFormatter para el render coloreado y stripControlChars para el saneo de lectura"
      pattern: "createFormatter|stripControlChars"
    - from: "src/cli.js"
      to: "src/cli/capture.js"
      via: "await import() perezoso dentro del .action() de commander, sin ensureConfig()"
      pattern: "await import\\('\\./cli/(capture|inbox)\\.js'\\)"
  prohibitions:
    - statement: "MUST NOT reinterpretar, auto-etiquetar, auto-enrutar, reescribir ni resumir el texto capturado — se persiste verbatim salvo el saneo a una sola línea y el recorte de bordes"
      category: transparency
      requirement_id: CAPT-01
    - statement: "MUST NOT borrar una captura por ninguna ruta de código — el cierre es solo una transición de estado; la traza permanente es el feature"
      category: safety
      requirement_id: CAPT-03
    - statement: "MUST NOT transmitir, subir ni copiar capturas fuera de la máquina local, ni emitir el texto capturado a ningún log compartido o de telemetría"
      category: privacy
      requirement_id: CAPT-01
    - statement: "MUST NOT decidir, validar ni interpretar el destino de una captura — el «a dónde va» es competencia exclusiva del skill de enrutado; kodo solo persiste la ref opaca que le den"
      category: values
      requirement_id: CAPT-04
    - statement: "D-14: MUST NOT registrar ninguna opción `--project` ni `--open` en el comando `inbox` ni en sus subcomandos — el comando `inbox` registrado en `src/cli.js` no expone ningún filtro; CAPT-F1 está diferido a v2 y la superficie no se adelanta"
      category: values
      requirement_id: CAPT-03
    - statement: "D-17: MUST NOT registrar una opción `--project` de override en `kodo capture` — el comando `capture` registrado en `src/cli.js` no expone ninguna opción de proyecto; el tag-proyecto se deriva exclusivamente del cwd (D-15)"
      category: values
      requirement_id: CAPT-01
---

<objective>
Cablear la superficie CLI del inbox: `kodo capture` (append) y `kodo inbox` con sus subcomandos `route`/`discard`, como thin handlers sobre `src/inbox/store.js`, más su suite de integración por proceso real con HOME sandbox.

Purpose: es la única superficie que el operador toca, y la que Phase 84 shelleará (`--origin`). Los exit codes y el formato de `--json` que se fijan aquí son contrato.
Output: `src/cli/capture.js`, `src/cli/inbox.js`, el registro en `src/cli.js` y `test/inbox-cli.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-inbox-foundation-captura-triage/83-CONTEXT.md
@.planning/phases/83-inbox-foundation-captura-triage/83-PATTERNS.md
@.planning/phases/83-inbox-foundation-captura-triage/83-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
</context>

<!-- planner-discipline-allow: writeFileAtomic -->
<!-- planner-discipline-allow: picocolors -->
<!-- planner-discipline-allow: node:child_process -->
<!-- planner-discipline-allow: gsd-capture -->

## Artifacts this phase produces

Ver el listado canónico en `83-01-PLAN.md` §Artifacts this phase produces. Los símbolos que **este plan** crea: `src/cli/capture.js` (`runCaptureCli`), `src/cli/inbox.js` (`runInboxListCli`, `runInboxMarkCli`), `test/inbox-cli.test.js`, y la superficie CLI `kodo capture` · `--origin` · `kodo inbox` · `--all` · `--json` · `kodo inbox route <id>` · `--dest` · `kodo inbox discard <id>`. Todos son nuevos: ninguno existe en el código actual y ninguno debe verificarse contra el árbol previo.

## Superficie CLI LOCKED (D-12/D-13) — copia literal, no ampliar ni recortar

| Invocación | Efecto | Exit codes |
|------------|--------|-----------|
| `kodo capture "<texto>"` | Appendea una línea abierta al inbox | 0 ok · 2 texto vacío tras saneo · 1 error de filesystem |
| `kodo capture "<texto>" --origin <valor>` | Igual, con el campo origen fijado (flag **interno**, no anunciado en el help principal; default `cli`) | ídem |
| `kodo inbox` | Lista las capturas **abiertas** | 0 siempre (never-throws) |
| `kodo inbox --all` | Incluye también las cerradas con su estado | 0 |
| `kodo inbox --json` | Una sola línea de JSON, byte-determinista, sin ANSI | 0 |
| `kodo inbox route <id>` | Marca `enrutada` sin destino | 0 ok · 2 id inexistente o ya cerrada · 1 lock-timeout / error de filesystem |
| `kodo inbox route <id> --dest <ref>` | Marca `enrutada` con trace pointer | ídem |
| `kodo inbox discard <id>` | Marca `descartada` | ídem |

**Sin** `--project`, **sin** `--open`, **sin** ningún filtro (D-14: CAPT-F1 está diferido a v2). No adelantar superficie.

## Contract decisions applied here

Las siete decisiones de contrato están registradas en `83-01-PLAN.md` §Contract decisions recorded here. Las que este plan implementa directamente:

- **Decisión 4 — texto vacío tras el saneo → exit 2** y cero escritura. El gate vive en `src/cli/capture.js`, después de aplicar el saneo, sobre el resultado recortado.
- **Decisión 3 — el marcado no hace fail-open:** el handler mapea `reason: 'lock-timeout'` y `reason: 'fs'` a exit **1** con un stderr canónico; `not-found` y `already-closed` a exit **2**.
- **Decisión 2 — `- [x]` hand-editado sin sufijo** llega como `already-closed` desde el store: exit 2, y el listado con `--all` lo muestra con estado desconocido.
- **Decisión 6 — un solo warn en el fail-open de la captura:** el handler no añade un segundo mensaje; el warn ya lo emite `appendCapture`.
- **Decisión 7 — los paths salen de `defaultInboxPaths()` del store**, invocado en el call-site del handler; `src/cli/capture.js` sí puede importar `src/config.js` (para `loadProjects`), pero `src/inbox/store.js` no.

## Flagged Assumptions — spec-less probe

Las 4 filas del edge probe (CAPT-01, CAPT-03, CAPT-04, CAPT-06) volvieron `unclassified` → **unresolved** y están surfaceadas como supuestos explícitos en `83-01-PLAN.md` §Flagged Assumptions, que es la tabla canónica de la fase. Este plan asume la clasificación manual allí registrada y la ejercita en `test/inbox-cli.test.js`; no la duplica ni descarta ninguna fila.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `src/cli/capture.js` — thin handler de la captura</name>
  <files>src/cli/capture.js</files>
  <read_first>
    - `src/cli/skill-sync.js` (fichero completo) — el thin handler canónico: cabecera de responsabilidades + invariante de color isolation, `@typedef` de opts separado del de deps, defaults de `writeFn`/`errFn`/`formatterFn` resueltos en el cuerpo, gate → delegar → render → **retornar** el código (nunca salir del proceso)
    - `src/inbox/store.js` — las firmas de `encodeLine`, `newCaptureId`, `todayLocal`, `deriveTag`, `defaultInboxPaths`, `appendCapture`, `MAX_TEXT_LEN`
    - `src/config.js` líneas 315-335 — `loadProjects()`, que ya hace `ensureDir()` y colapsa un JSON corrupto a `{}`
    - `src/cli/format.js` líneas 110-130 — `stripForKeystroke` (el saneo del carril de escritura)
    - `.planning/phases/83-inbox-foundation-captura-triage/83-CONTEXT.md` — D-02, D-03, D-13, D-15, D-16, D-17
    - `.planning/phases/83-inbox-foundation-captura-triage/83-RESEARCH.md` §Pitfall 3 y §Pitfall 8
  </read_first>
  <behavior>
    - `runCaptureCli({text:'una idea'}, deps)` devuelve 0 e invoca `appendFn` con una línea terminada en newline que parsea a una captura abierta.
    - Con `text` igual a la cadena vacía, a solo espacios, o a una secuencia que el saneo colapsa a espacios, devuelve **2**, escribe un mensaje a `errFn` y **no** invoca `appendFn`.
    - `origin` por defecto es `cli`; `runCaptureCli({text:'x', origin:'skill'})` produce una línea cuyo último campo es `skill` y que es idéntica en todo lo demás.
    - El tag sale de `deriveTag(cwd, projects)`; con `projects` vacío, el tag es el basename del cwd inyectado.
    - Un texto por encima de `MAX_TEXT_LEN` se trunca y se emite un aviso por `errFn`, pero el exit code sigue siendo 0 (la idea no se pierde).
    - `appendFn` devolviendo `{ok:false, reason:'fs'}` produce exit **1** y un mensaje canónico en `errFn`.
    - `appendFn` devolviendo `{ok:true, coordinated:false}` produce exit **0** y el handler **no** añade ningún mensaje extra (el warn ya lo emitió el store — decisión de contrato 6).
    - En la ruta feliz, `writeFn` recibe una confirmación que incluye el id corto de la captura para que el operador pueda usarlo como handle de `route`.
  </behavior>
  <action>
Crear `src/cli/capture.js` con `// @ts-check` en la línea 1 y la cabecera de responsabilidades del molde de `src/cli/skill-sync.js:1-19`, incluyendo la declaración explícita del invariante de color isolation (este fichero solo puede tocar el color a través de `createFormatter`).

Declarar dos typedefs separados: `RunCaptureCliOpts` con `{ text?: string, origin?: string }` y `RunCaptureCliDeps` con `{ appendFn?, writeFn?, errFn?, formatterFn?, cwdFn?, projectsFn?, idFn?, clockFn?, pathsFn? }`. Todas las deps son opcionales con default resuelto en el cuerpo: `appendFn` → `appendCapture`, `writeFn` → escritura a stdout, `errFn` → escritura a stderr, `formatterFn` → `createFormatter(process.stdout)`, `cwdFn` → `process.cwd()`, `projectsFn` → `loadProjects` de `src/config.js`, `idFn` → `newCaptureId`, `clockFn` → `todayLocal`, `pathsFn` → `defaultInboxPaths`. La DI de `idFn` y `clockFn` es lo que hace el handler testeable sin reloj real.

`runCaptureCli(opts, deps = {})` devuelve `number` (0 | 1 | 2) y **nunca** invoca el helper de salida del runtime — ese contrato es de `src/cli.js` (precedente `skill-sync.js:44-49`). Secuencia:

1. Sanear el texto de entrada con `stripForKeystroke` y recortar los bordes. **Gate (decisión de contrato 4):** si el resultado es la cadena vacía, escribir a `errFn` el mensaje canónico `Error: capture text is empty after sanitization` terminado en newline y devolver **2**. Nada se escribe en el inbox.
2. Si el texto saneado excede `MAX_TEXT_LEN`, dejar que `encodeLine` lo recorte y avisar por `errFn` con un mensaje de prefijo `[kodo:inbox]` indicando el recorte. El exit code no cambia.
3. Derivar el tag con `deriveTag(cwdFn(), projectsFn())`, la fecha con `clockFn()` y el id con `idFn()`. El origen es `opts.origin` saneado o `cli` por defecto.
4. Construir la captura abierta (`open: true`, `estado: null`, `dest: null`) y codificarla con `encodeLine`, añadiendo el newline final.
5. Resolver `{inboxPath, lockPath}` con `pathsFn()` e invocar `appendFn(line, { inboxPath, lockPath })` dentro de un `try/catch`; cualquier excepción se mapea a exit 1 con un mensaje canónico de error de filesystem.
6. `{ok:false}` → exit 1 con mensaje canónico. `{ok:true}` → escribir por `writeFn` una confirmación que incluya el id corto y devolver 0. No emitir ningún mensaje adicional cuando `coordinated` es `false`.

Este handler **no** invoca la comprobación de configuración de proveedor: el inbox es filesystem local (mismo precedente que `skill sync`, `gsd doctor` y `sidebar doctor`). Documentarlo en comentario.
  </action>
  <verify>
    <automated>node --test test/inbox-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function runCaptureCli\|^export async function runCaptureCli" src/cli/capture.js` devuelve 1.
    - `grep -nE "^import .*picocolors" src/cli/capture.js` no devuelve coincidencias; `node --test test/format-isolation.test.js` en verde.
    - `grep -nE "^import .*'node:child_process'" src/cli/capture.js` no devuelve coincidencias (gate CAPT-04).
    - `grep -nE "process\.exit" src/cli/capture.js` no devuelve coincidencias (el handler retorna el código).
    - `head -1 src/cli/capture.js` es `// @ts-check`.
    - Comportamiento (unit por DI, dentro de `test/inbox-cli.test.js`): `runCaptureCli({text:'   '}, {appendFn: spy})` devuelve 2 y `spy` no fue invocado.
    - Comportamiento: `runCaptureCli({text:'idea', origin:'skill'}, {idFn:()=>'a3f9k2', clockFn:()=>'2026-07-25', cwdFn:()=>'/x/kodo', projectsFn:()=>({}), appendFn: spy})` hace que `spy` reciba una línea cuyo último campo estructurado es `skill`.
  </acceptance_criteria>
  <done>`kodo capture` tiene su handler con gate de texto vacío en exit 2, `--origin` inyectable, tag derivado del cwd y cero mensajes duplicados en el fail-open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `src/cli/inbox.js` — listado human/JSON y handlers de route/discard</name>
  <files>src/cli/inbox.js</files>
  <read_first>
    - `src/cli/skill-sync.js` (fichero completo) — en particular la separación de la rama `--json` **antes** de tocar el formatter (bytes deterministas, DX-06), el `JSON.stringify(payload) + '\n'` de una sola línea, las claves opcionales añadidas condicionalmente en orden fijo, y el render human en función privada que solo usa métodos del formatter
    - `src/inbox/store.js` — firmas de `listCaptures`, `markCapture`, `defaultInboxPaths`, `MAX_DEST_LEN` y el typedef `Capture`
    - `src/cli/format.js` líneas 75-100 y 160-210 — `stripControlChars` (carril de render) y los métodos que expone `createFormatter` (`ok`, `fail`, `yellow`, `dim`, `gray`, `cyan`, `formatRow`, `formatTable`)
    - `.planning/phases/83-inbox-foundation-captura-triage/83-CONTEXT.md` — D-09 (el seam es documental), D-10/D-11 (`--dest` opcional y no validado), D-12, D-13, D-18
    - `.planning/phases/83-inbox-foundation-captura-triage/83-RESEARCH.md` §Pitfall 6 (el saneo de escritura no protege el render — el fichero es human-editable)
  </read_first>
  <behavior>
    - `runInboxListCli({}, {listFn})` con 2 abiertas y 1 cerrada muestra solo las 2 abiertas y devuelve 0.
    - `runInboxListCli({all:true}, {listFn})` muestra las 3, con la cerrada acompañada de su estado; una cerrada con `estado: null` se muestra como cierre desconocido.
    - `runInboxListCli({json:true}, {listFn})` escribe exactamente **una** línea que `JSON.parse` acepta; dos ejecuciones con la misma entrada producen bytes idénticos; la salida no contiene ninguna secuencia ANSI aunque el formatter tenga color activado.
    - El payload JSON tiene claves en orden fijo y expone por captura al menos `id`, `text`, `tag`, `date`, `origin`, `open` y, en `--all`, `estado` y `dest`.
    - Inbox vacío o inexistente → exit 0 con una copy explícita (nunca un throw ni una tabla vacía sin texto).
    - Un texto de captura que contiene una secuencia de escape de terminal pegada a mano sale del render **sin** esa secuencia (el carril de render la neutraliza).
    - `runInboxMarkCli('a3f9k2','enrutada',{dest:'.planning/todos/TODO-012.md'},{markFn})` devuelve 0 y pasa el destino al store.
    - Sin `dest`, devuelve 0 igualmente (CAPT-06 best-effort).
    - `markFn` devolviendo `not-found` o `already-closed` → exit **2** con mensajes distinguibles en stderr.
    - `markFn` devolviendo `lock-timeout` o `fs` → exit **1** con mensaje canónico en stderr.
    - Un `dest` que excede `MAX_DEST_LEN` se recorta sin fallar.
  </behavior>
  <action>
Crear `src/cli/inbox.js` con `// @ts-check` y la cabecera del molde de `src/cli/skill-sync.js:1-19`, declarando el invariante de color isolation.

Typedefs separados: `RunInboxListCliOpts` con `{ all?: boolean, json?: boolean }`; `RunInboxListCliDeps` con `{ listFn?, writeFn?, errFn?, formatterFn?, pathsFn? }`; `RunInboxMarkCliDeps` con `{ markFn?, writeFn?, errFn?, formatterFn?, pathsFn? }`. Defaults resueltos en el cuerpo, igual que en `capture.js`.

`runInboxListCli(opts, deps = {})` → `number`:

1. Resolver paths con `pathsFn()` e invocar `listFn({ inboxPath })`. `listCaptures` nunca lanza, así que no hace falta `try/catch` alrededor, pero sí envolver el render para garantizar el never-throws de extremo a extremo (D-18).
2. Filtrar: por defecto solo las que tienen `open === true`; con `opts.all` todas.
3. **Rama `--json` primero, antes de instanciar el formatter** (DX-06 byte-determinismo): construir un objeto con claves en orden fijo — primero el conteo de abiertas, luego el conteo de no parseables, luego el array de capturas; cada captura con sus claves en el orden `id`, `text`, `tag`, `date`, `origin`, `open` y, solo cuando `opts.all` está activo, `estado` y `dest`. Emitir `JSON.stringify(payload) + '\n'` en una sola llamada a `writeFn`. Sin ANSI por construcción: esta rama nunca toca el formatter.
4. Rama human en una función privada: instanciar el formatter, y **sanear con `stripControlChars` el texto y el destino de cada captura antes de pintarlos** (Pitfall 6 — el fichero es human-editable, y el saneo de escritura no cubre las líneas pegadas a mano). Pintar con `formatTable` una fila por captura con las columnas id, texto, tag, fecha y, en modo `--all`, estado. No numerar filas: el handle que el operador copia es el id corto, y numerar induciría a usar el número como handle.
5. Listado vacío → escribir una copy explícita (distinguiendo «el inbox está vacío» de «no hay capturas abiertas; usa `--all` para ver la traza») y devolver 0.
6. Si `unparsed` es mayor que cero, añadir una línea informativa indicando cuántas líneas no parseables se han **omitido del listado y conservado en el fichero** — el operador debe saber que kodo no las ha tocado.
7. Devolver siempre 0.

`runInboxMarkCli(id, estado, opts, deps = {})` → `number`, donde `estado` es `'enrutada'` o `'descartada'` y `opts` es `{ dest?: string }`:

1. Resolver paths con `pathsFn()`.
2. Invocar `markFn(id, estado, { dest: opts.dest ?? null, inboxPath, lockPath })` dentro de un `try/catch` que colapse cualquier excepción a exit 1.
3. Mapear el resultado a exit code (D-13 + decisión de contrato 3): `{ok:true}` → 0 con una confirmación por `writeFn`; `not-found` → 2 con `Error: capture <id> not found`; `already-closed` → 2 con `Error: capture <id> is already closed`; `lock-timeout` → 1 con un mensaje que indique que el marcado **no** se ha aplicado y que se puede reintentar; `fs` → 1 con el error de filesystem canónico.
4. `--dest` es una string **libre**: se pasa tal cual al store, que la sanea y la recorta. Este handler **no** comprueba que el destino exista, no lo resuelve contra el filesystem y no interpreta su forma — el «a dónde va» es competencia exclusiva del skill de enrutado (D-11, CAPT-04). Dejarlo escrito en comentario junto al parámetro.

Este fichero **no** invoca la comprobación de configuración de proveedor, **no** importa `node:child_process` y **no** ejecuta ningún proceso externo: el seam de enrutado es puramente documental (D-09). Documentarlo en la cabecera describiendo el flujo de tres pasos por el que el operador enruta manualmente, sin nombrar ningún módulo importable.
  </action>
  <verify>
    <automated>node --test test/inbox-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function runInboxListCli\|^export function runInboxMarkCli" src/cli/inbox.js` devuelve 2.
    - `grep -nE "^import .*picocolors" src/cli/inbox.js` no devuelve coincidencias; `node --test test/format-isolation.test.js` en verde.
    - `grep -nE "^import .*'node:child_process'" src/cli/inbox.js` no devuelve coincidencias (gate CAPT-04).
    - `grep -nE "process\.exit" src/cli/inbox.js` no devuelve coincidencias.
    - `grep -c "stripControlChars" src/cli/inbox.js` es al menos 1 (carril de render saneado, Pitfall 6).
    - Comportamiento: `runInboxListCli({json:true}, {listFn:()=>fixture})` produce una salida que `JSON.parse` acepta y que es byte-idéntica en dos invocaciones consecutivas.
    - Comportamiento: con un fixture cuyo texto contiene una secuencia de escape de terminal, la salida human no contiene ningún byte del rango de control.
    - Comportamiento: `runInboxMarkCli('zzz','enrutada',{},{markFn:()=>({ok:false,reason:'not-found'})})` devuelve 2; con `reason:'lock-timeout'` devuelve 1.
  </acceptance_criteria>
  <done>`kodo inbox` lista abiertas y cerradas con render saneado y `--json` determinista; route/discard mapean los cuatro reasons del store a los exit codes de D-13.</done>
</task>

<task type="auto">
  <name>Task 3: Registro en `src/cli.js` y suite de integración por proceso real</name>
  <files>src/cli.js, test/inbox-cli.test.js</files>
  <read_first>
    - `src/cli.js` líneas 470-525 — el molde de registro de `sidebar` y `skill sync`: comentario separador, `await import()` perezoso dentro del `.action()`, el comentario que justifica no invocar la comprobación de configuración, normalización de flags, salida con el código retornado y `try/catch` que mapea una excepción a exit 1
    - `.planning/phases/83-inbox-foundation-captura-triage/83-RESEARCH.md` §Pattern 4 — la forma verificada de commander 13.1.0 para un comando padre con acción propia **y** subcomandos, con las 6 invocaciones que resuelve
    - `.planning/phases/83-inbox-foundation-captura-triage/83-PATTERNS.md` §`test/inbox-cli.test.js` — el analog `test/version-smoke.test.js:1-35` (spawnSync de `bin/kodo` con `timeout: 10_000` y assert que incluye stderr) y el analog del assert source-hygiene de `test/gsd-doctor-cli.test.js:265`
    - `.planning/phases/83-inbox-foundation-captura-triage/83-RESEARCH.md` §Pitfall 5 — `~/.kodo` se resuelve al cargar el módulo de config: cada `spawnSync` debe llevar su propio HOME sandbox
    - `src/cli/capture.js` y `src/cli/inbox.js` — las firmas recién escritas
    - `package.json` — la lista actual de dependencias de producción (4 claves)
  </read_first>
  <action>
**A. `src/cli.js`.** Añadir dos bloques al final del área de registro, cada uno precedido de su comentario separador al estilo de los existentes.

Primero `kodo capture`: un comando de primer nivel con el argumento posicional del texto y la opción `--origin <valor>`. La descripción de la opción debe dejar claro que es de uso **interno** (la produce el skill de Phase 84) y que su default es `cli`. Dentro del `.action()`, `await import()` perezoso de `./cli/capture.js`, invocación de `runCaptureCli` con los flags normalizados, salida con el código retornado, y `try/catch` que mapea la excepción a un mensaje `Error: …` y exit 1. Incluir el comentario que justifica **no** invocar la comprobación de configuración de proveedor, con el mismo precedente citado en los bloques de `sidebar` y `skill`.

Después el grupo `kodo inbox`: a diferencia de todos los grupos existentes en este fichero, el padre lleva **acción propia** además de subcomandos. Esta es la única desviación del molde y hay que comentarla explícitamente en el código, citando que la forma quedó verificada sobre commander 13.1.0 en `83-RESEARCH.md` §Pattern 4. El padre declara `--all` y `--json` y su `.action()` delega en `runInboxListCli`. Colgando del padre, `route <id>` con la opción `--dest <ref>` (descripción: trace pointer best-effort, kodo no lo valida) delegando en `runInboxMarkCli` con estado `enrutada`, y `discard <id>` delegando con estado `descartada`. Los tres `.action()` siguen el mismo patrón de import perezoso, exit con el código retornado y `try/catch`.

**B. `test/inbox-cli.test.js`.** Crear la suite de integración con `// @ts-check`, `node:test` y `node:assert/strict`, resolviendo la ruta del binario igual que `test/version-smoke.test.js`. Sandbox por test con `mkdtempSync` y `rmSync` en los hooks; **cada** `spawnSync` lleva `env: { ...process.env, HOME: sandbox }`, `encoding: 'utf-8'` y `timeout: 10_000`, y los asserts incluyen `stderr` en su mensaje. Casos:

1. Primer run sobre un HOME limpio: `kodo capture "una idea"` sale 0, crea el directorio del inbox y el fichero, y el fichero tiene exactamente 1 línea cuyo primer byte es `-` (sin cabecera, D-19).
2. `kodo capture ""` y `kodo capture "   "` salen **2** y no crean ni modifican el fichero.
3. `kodo capture "idea" --origin skill` produce una línea cuyo último campo estructurado es `skill`.
4. Tres capturas seguidas → 3 líneas, en orden de invocación.
5. `kodo inbox` tras 2 capturas lista 2 filas y sale 0; `kodo inbox` sobre un HOME limpio sale 0 con la copy de inbox vacío.
6. `kodo inbox --json` sale 0, su stdout es **una sola línea** que `JSON.parse` acepta, no contiene el byte de escape ANSI, y dos invocaciones consecutivas sobre el mismo estado producen stdout byte-idéntico.
7. `kodo inbox route <id> --dest .planning/todos/TODO-012.md` sale 0; el fichero conserva el mismo número de líneas y esa línea pasa a cerrada con el trace pointer; `kodo inbox` deja de listarla y `kodo inbox --all` la sigue mostrando.
8. `kodo inbox route <id>` sin `--dest` sale 0 y cierra la línea sin destino (CAPT-06).
9. `kodo inbox discard <id>` sale 0 y la línea sigue en el fichero con el sufijo de descarte.
10. `kodo inbox route <id-inexistente>` sale **2**; repetir `route` sobre una captura ya cerrada sale **2**; ambos dejan el fichero byte-idéntico (comparar el hash antes y después).
11. Una línea hand-editada a la forma cerrada **sin** sufijo de estado: `route` sobre su id sale 2 y esa línea no cambia (decisión de contrato 2).
12. Un fichero sembrado con una captura válida + un heading + una línea escrita a mano: `kodo inbox` lista solo la captura, informa de las líneas omitidas, y tras un `route` las otras dos líneas siguen byte a byte iguales.
13. `kodo capture --help` y `kodo inbox --help` salen 0 y el help de `inbox` menciona los subcomandos `route` y `discard`.
14. **Gate source-hygiene CAPT-04:** leer `src/inbox/store.js`, `src/cli/capture.js` y `src/cli/inbox.js` y assertar que ninguno contiene una sentencia de import del módulo de procesos hijo de Node. Anclar la aserción al patrón de import a principio de línea, **no** al nombre suelto: un comentario que explique la ausencia de acoplamiento no puede hacer roja la suite. Dejar ese razonamiento escrito en un comentario del test.
15. **Gate source-hygiene cero-deps:** leer `package.json` y assertar que `dependencies` tiene exactamente 4 claves. El mensaje de fallo debe nombrar el invariante cross-milestone.
16. **Unit por DI** de los dos handlers (los casos marcados como «Comportamiento» en las tareas 1 y 2 de este plan), invocando `runCaptureCli` / `runInboxListCli` / `runInboxMarkCli` directamente con deps espía — sin proceso y sin tocar HOME.
  </action>
  <verify>
    <automated>node --test test/inbox-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/inbox-cli.test.js` sale con código 0.
    - `npm test` completa en verde.
    - `grep -c "await import('./cli/capture.js')\|await import('./cli/inbox.js')" src/cli.js` es al menos 4 (capture + list + route + discard).
    - `grep -n "ensureConfig" src/cli.js` no muestra ninguna llamada dentro de los bloques nuevos de `capture` e `inbox`.
    - CLI: `node bin/kodo inbox --help` sale 0 y su stdout menciona `route` y `discard`.
    - CLI (D-14 — superficie diferida no adelantada): `node bin/kodo inbox --help | grep -cE '\-\-project|\-\-open'` cuenta 0, y lo mismo para `node bin/kodo inbox route --help` y `node bin/kodo inbox discard --help`.
    - CLI (D-17 — sin override del tag): `node bin/kodo capture --help | grep -c '\-\-project'` cuenta 0.
    - CLI: con un HOME sandbox limpio, `node bin/kodo capture "x"` sale 0 y `node bin/kodo capture ""` sale 2.
    - CLI: `node bin/kodo inbox --json` con HOME sandbox produce stdout que `JSON.parse` acepta, de una sola línea, byte-idéntico entre dos ejecuciones.
    - `git diff --stat package.json package-lock.json` sale vacío.
    - `node --test test/format-isolation.test.js` en verde.
  </acceptance_criteria>
  <done>Las ocho invocaciones de la tabla LOCKED funcionan de extremo a extremo con sus exit codes, `--json` es determinista, y los dos gates source-hygiene (seam sin acoplamiento y cero deps) están cableados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv del operador → `src/cli/capture.js` | Texto arbitrario y sin cota cruza hacia un fichero persistido |
| `~/.kodo/inbox.md` (human-editable) → render de `kodo inbox` → terminal del operador | Contenido no confiable, potencialmente pegado a mano por un tercero, se pinta en un terminal interactivo |
| argv del operador → `route`/`discard` (`<id>`, `--dest`) | Identificador y ref opacos cruzan hacia el marcado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-09 | Tampering | Render human de `kodo inbox` (escapes de terminal desde líneas pegadas a mano; escritura al portapapeles del operador) | high | mitigate | `stripControlChars` aplicado al texto y al destino de cada captura en la función privada de render de `src/cli/inbox.js`; el saneo del carril de escritura no basta porque el fichero es human-editable (Pitfall 6). Aserción dedicada en `test/inbox-cli.test.js` |
| T-83-10 | Tampering | `<id>` de `route`/`discard` (path traversal) | medium | mitigate | El identificador solo se usa para matchear una línea en memoria; ningún path se compone a partir de él. Los paths salen de `defaultInboxPaths()`, construidos desde el home y dos basenames fijos |
| T-83-11 | Tampering | `--dest` (ref opaca del operador) | medium | mitigate | Saneada con el carril de escritura y recortada a la cota antes de persistirse; kodo no la resuelve contra el filesystem, no la interpreta y no la ejecuta (D-11) |
| T-83-12 | Elevation of Privilege | Acoplamiento del inbox al skill de enrutado (ejecución de un proceso externo desde el CLI) | high | mitigate | Ningún módulo del inbox importa el módulo de procesos hijo de Node; gate source-hygiene anclado al patrón de import en `test/inbox-cli.test.js` (CAPT-04/D-09) |
| T-83-13 | Information Disclosure | `kodo inbox --json` en un pipeline | low | accept | La salida contiene el texto capturado por diseño (es el propósito del flag). Riesgo tolerado: superficie local, el operador decide dónde tubea. No se emite a ningún log ni destino remoto |
| T-83-14 | Denial of Service | Texto de captura sin cota desde argv | low | accept | Recorte a la cota de longitud antes de codificar; atomicidad del append medida muy por encima de esa cota. Riesgo tolerado: es el propio operador quien pasa el argumento |
| T-83-SC | Tampering | Instalaciones npm/pip/cargo | high | mitigate | Cero instalaciones. Gate source-hygiene en `test/inbox-cli.test.js`: `dependencies` de `package.json` se pina a exactamente 4 claves. No existen paquetes `[ASSUMED]`/`[SUS]` que verificar |

**Canon-referral (no minteado como prohibición):** la inyección de escapes de terminal y el path traversal son canon → cubiertos por `/gsd-secure-phase` y por las filas T-83-09/T-83-10 de este registro.
</threat_model>

<verification>
- `node --test test/inbox-cli.test.js` en verde.
- `node --test test/format-isolation.test.js` en verde.
- `npm test` completa en verde.
- `node bin/kodo inbox --help` sale 0 y lista `route` y `discard`.
- `git diff --stat package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Las ocho invocaciones de la tabla LOCKED se comportan según sus exit codes.
- `kodo inbox --json` es una línea, parseable, sin ANSI y byte-determinista.
- El render human neutraliza secuencias de control provenientes del fichero.
- Los gates source-hygiene de CAPT-04 y de cero-deps están cableados y anclados al patrón de import (no al nombre suelto).
- Ningún handler invoca el helper de salida del runtime; el exit lo hace el registro de commander.
</success_criteria>

<output>
Create `.planning/phases/83-inbox-foundation-captura-triage/83-02-SUMMARY.md` when done
</output>
