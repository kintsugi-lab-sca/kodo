---
phase: 83-inbox-foundation-captura-triage
plan: 07
type: execute
wave: 4
depends_on: ["83-04", "83-05"]
gap_closure: true
files_modified:
  - src/inbox/store.js
  - src/cli/inbox.js
  - src/cli/capture.js
  - test/inbox-store.test.js
  - test/inbox-cli.test.js
  - README.md
  - .claude/skills/kodo-orchestrate/skill.md
autonomous: true
requirements: [CAPT-01, CAPT-03, CAPT-04, CAPT-06]

must_haves:
  truths:
    - "El tag de una captura es legible para el operador en la configuración REAL del instalador: cuando el identificador de proyecto tiene forma de UUID, el tag es un nombre humano derivado de la ruta mapeada, nunca la cadena de 36 caracteres (CAPT-01, D-15)"
    - "Existe cobertura con la forma REAL del mapa de proyectos —claves UUID con valores que llevan la ruta— y no solo con claves legibles inventadas"
    - "Un identificador de proyecto ya legible sigue produciendo exactamente el mismo tag que hoy: el cambio no altera ninguna configuración que ya funcionaba"
    - "El carril de datos neutraliza los bytes de control que el serializador JSON NO escapa (rango C1 y DEL), igual que ya hace el carril human — el carril que la skill del orquestador manda usar deja de ser el hueco del modelo de amenaza (CAPT-03)"
    - "El operador recibe un mensaje distinguible cuando el marcado no se aplica por una escritura concurrente frente a cuando no se aplica por lock ocupado; ambos salen con código 1 y dejan el fichero intacto (D-13)"
    - "El aviso del fail-open de la captura viaja por el mismo seam inyectable que el resto de la salida de error del handler, y hay una prueba de integración que lo observa en el binario real con código de salida 0 (CAPT-01, D-03)"
    - "La documentación del orquestador no describe el fichero con una garantía de concurrencia que el marcado no da"
    - "La documentación declara la forma segura de capturar un texto que empieza por guion, en el mismo sitio donde el orquestador aprende a usar el CLI (CAPT-04)"
  artifacts:
    - path: "src/inbox/store.js"
      provides: "deriveTag proyecta un identificador de proyecto con forma de UUID a un nombre humano derivado de la ruta mapeada, con el nombre del directorio como último recurso"
      exports: ["deriveTag"]
      contains: "deriveTag"
    - path: "src/cli/inbox.js"
      provides: "Saneo del rango de control que el serializador no escapa en el carril de datos, y mapeo honesto del reason de escritura concurrente a su copy y su exit code"
      exports: ["runInboxListCli", "runInboxMarkCli"]
      contains: "concurrent-write"
    - path: "src/cli/capture.js"
      provides: "El aviso del fail-open se propaga por el seam de inyección del handler en vez de escribir directo al stream de error del proceso"
      exports: ["runCaptureCli"]
      contains: "warnFn"
    - path: ".claude/skills/kodo-orchestrate/skill.md"
      provides: "Sección de triage con la descripción correcta del modelo del fichero, el modo de fallo por escritura concurrente y la forma segura de capturar texto con guion inicial"
      contains: "kodo inbox"
  key_links:
    - from: "src/inbox/store.js"
      to: "src/cli/dashboard/select.js"
      via: "resolveProjectId sigue siendo la única resolución cwd→proyecto; deriveTag solo PROYECTA su resultado a un nombre legible"
      pattern: "resolveProjectId"
    - from: "src/cli/capture.js"
      to: "src/inbox/store.js"
      via: "appendCapture recibe el seam de aviso del handler junto con los paths"
      pattern: "warnFn"
    - from: "src/cli/inbox.js"
      to: "src/inbox/store.js"
      via: "el reason de escritura concurrente que markCapture devuelve se mapea a copy y exit code aquí"
      pattern: "concurrent-write"
  prohibitions:
    - statement: "MUST NOT emitir por el carril de datos contenido del fichero human-editable que el carril human sí neutraliza — el saneo se aplica a los dos carriles o el modelo de amenaza tiene un agujero por diseño"
      category: safety
      requirement_id: CAPT-03
    - statement: "MUST NOT decidir, validar ni interpretar el destino de una captura — el «a dónde va» es competencia exclusiva del skill de enrutado; kodo solo persiste la ref opaca que le den"
      category: values
      requirement_id: CAPT-04
    - statement: "MUST NOT describir en la documentación una garantía que el código no da — el fichero no es de solo-append: el marcado lo reescribe entero bajo lock, y un agente que asuma lo contrario infiere garantías de concurrencia inexistentes"
      category: transparency
      requirement_id: CAPT-04
    - statement: "MUST NOT transmitir, subir ni copiar capturas fuera de la máquina local, ni emitir el texto capturado ni la ruta del proyecto derivada a ningún log compartido o de telemetría"
      category: privacy
      requirement_id: CAPT-01
---

<objective>
Cerrar GAP-3 (CR-03, blocker de calidad de producto) y los tres warnings que comparten su superficie de saneo, mapeo y copy.

Purpose: el campo que existe para comunicar al operador a qué proyecto pertenece una captura es, en la instalación real, un UUID de 36 caracteres — las 10 claves del mapa de proyectos del operador lo son, y una captura desde este mismo repo escribe hoy `7246e3fe-3dc4-4f24-9078-1911ad477e0d` como tag (reproducido por el verificador y por este planificador). Ningún test lo detecta porque todos los fixtures inventan claves legibles. En el mismo ciclo se cierran: el carril de datos que emite verbatim los bytes de control que el serializador JSON no escapa (WR-02, el mismo vector que el carril human sí cierra y precisamente el carril que la skill del orquestador manda usar), el aviso del fail-open que esquiva el seam de inyección del handler (WR-08, relevante ahora que el Plan 83-04 devolvió el presupuesto al default y esa rama vuelve a ser alcanzable en producción), y la copy del skill que llama de solo-append a un fichero que el marcado reescribe entero (IN-02, la asunción exacta que produce el escenario de CR-02).
Output: `deriveTag` con proyección legible, el carril de datos saneado, el aviso del fail-open por el seam, la copy corregida en el skill y en el README, y cobertura para todo ello.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-inbox-foundation-captura-triage/83-CONTEXT.md
@.planning/phases/83-inbox-foundation-captura-triage/83-VERIFICATION.md
@.planning/phases/83-inbox-foundation-captura-triage/83-REVIEW.md
@.planning/phases/83-inbox-foundation-captura-triage/83-04-SUMMARY.md
@.planning/phases/83-inbox-foundation-captura-triage/83-05-SUMMARY.md
</context>

<!-- planner-discipline-allow: append-only -->

## Artifacts this phase produces

Ver el listado canónico de la fase en `83-01-PLAN.md` §Artifacts this phase produces. Los símbolos que **este plan** crea (nuevos: excluir de toda verificación de drift contra el código actual):

**Helpers privados nuevos en `src/inbox/store.js`:** `isUuidLike(s)` — reconoce la forma canónica de 36 caracteres de un identificador de proveedor; `mappedProjectPath(projects, projectId)` — extrae del mapa de proyectos la ruta asociada a un identificador, aceptando tanto un valor que sea la ruta directamente como un valor objeto con una clave de ruta por defecto.

**Helper privado nuevo en `src/cli/inbox.js`:** `sanitizeJsonField(s)` — elimina del contenido de un campo el rango de control que el serializador no escapa.

**Literal de contrato consumido (creado en el Plan 83-04):** el `reason` `'concurrent-write'` de `markCapture`. Este plan añade su rama en el mapeo a copy y exit code.

## Decisiones de contrato fijadas aquí (NO delegar al ejecutor)

**Decisión A — la proyección del tag es CONDICIONAL, no incondicional.** Solo se proyecta cuando el identificador de proyecto tiene forma de UUID. Un identificador ya legible se devuelve tal cual, exactamente como hoy. Razón: el comportamiento actual es correcto para toda configuración con claves legibles, hay cobertura verde que lo fija, y el cierre de gaps no reabre lo que ya funciona (`83-VERIFICATION.md` deja las truths 1, 2, 4 y 5 verificadas; solo se toca lo que las hace falsas). Un cambio incondicional además haría depender el tag de la forma del valor del mapa, que es operator-editable.

**Decisión B — la ruta legible sale del MAPA, no del cwd.** Con forma de UUID, el tag es el último segmento de la ruta mapeada a ese identificador. El nombre del directorio actual es el ÚLTIMO recurso, no el primero: capturar desde un subdirectorio del proyecto (lo normal) daría el nombre del subdirectorio, que informa peor que el nombre del proyecto. Verificado sobre la configuración real de este operador: los valores del mapa son objetos con una clave de ruta por defecto (y una tabla de módulos), no cadenas sueltas; el extractor tiene que aceptar ambas formas y filtrar todo lo que no sea cadena — el fichero es operator-editable y `resolveProjectId` ya paga ese peaje por la misma razón.

**Decisión C — el saneo del carril de datos NO toca lo que el serializador ya cubre.** Se elimina exclusivamente el rango que el serializador deja pasar verbatim (los controles C1 y DEL). No se re-escapa nada de lo que ya escapa, no se introduce color y no se altera el orden ni el conjunto de claves del objeto emitido: el byte-determinismo de DX-06 y el gate de ausencia de color siguen intactos.

## Corrección de contexto acumulado que este plan invalida

`.planning/STATE.md` §Accumulated Context registra hoy de 83-02: «el carril `--json` emite el texto VERBATIM (`JSON.stringify` ya escapa los bytes C0); el saneo agresivo vive solo en el render human». La premisa es **factualmente falsa** y el review la refutó con una medición: el serializador escapa C0 pero no el rango C1 ni DEL, y ambos salen verbatim. El `83-07-SUMMARY.md` DEBE registrar la reversión para que el carril de estado del proyecto deje de propagar la afirmación falsa. Este plan **no edita `STATE.md` directamente**: ese fichero lo escribe el workflow.

## Flagged Assumptions — spec-less probe

Las 4 filas del edge probe volvieron `unclassified` → **unresolved** y están surfaceadas en la tabla canónica del ciclo de cierre, en `83-04-PLAN.md` §Flagged Assumptions. Este plan ejercita el borde de **CAPT-01** (el tag tiene que ser informativo con la forma REAL del mapa de proyectos, y el aviso del fail-open tiene que ser observable en el binario), el de **CAPT-03** (el carril de datos es contenido de un fichero human-editable que llega a un terminal), el de **CAPT-04** (el borde es documental: una copy que prometa garantías inexistentes) y el de **CAPT-06** (el trace pointer viaja saneado por el carril de datos). No duplica la tabla ni descarta ninguna fila.

## Diferidos con razón explícita

- **WR-04 — el codificador de línea no valida su propio contrato** (puede emitir líneas que su propio parser rechaza): diferido. El arreglo correcto cambia el contrato del codificador de «nunca lanza» a «lanza ante entrada degradada», y ese codificador es el contrato inter-fase byte-exacto que Phase 84 consume. Cambiarlo dentro de un cierre de gaps, sin que Phase 84 exista todavía para absorber el cambio, tiene peor relación riesgo/beneficio que dejarlo registrado. Hoy el único call-path de producción pasa por el gate de texto vacío del handler.
- **WR-06 — CRLF y BOM rompen el parser en silencio:** diferido. Es un defecto real de robustez del fichero human-editable, pero requiere tocar el bucle de lectura Y el de localización del marcado manteniendo el round-trip byte a byte de D-04 intacto — es decir, la misma superficie que el Plan 83-04 acaba de reescribir. Meterlo en el mismo ciclo multiplica el riesgo sobre el invariante que este cierre existe para asegurar. Se registra como deuda propia.
- **IN-01, IN-03, IN-04** (redacción del comentario del seam de test, deduplicación de la cadena de saneo, vocabulario del flag de origen): diferidos por ser higiene sin impacto observable; ninguno bloquea una truth de la fase.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: El tag comunica el proyecto también con la configuración real del operador</name>
  <files>src/inbox/store.js, test/inbox-store.test.js</files>
  <read_first>
    - `src/inbox/store.js` — `deriveTag` completa (su JSDoc, el fallback al nombre del directorio y el catch de cinturón de seguridad) y el sanitizador de campos estructurados, que es el que normaliza el resultado al codificar la línea
    - `src/cli/dashboard/select.js` líneas 375-450 — `resolveProjectId`: su retorno real, sus DOS modos de fallo, y sobre todo `candidatesOf`, que ya resuelve el problema de «el valor del mapa puede ser una cadena o un objeto con ruta por defecto y tabla de módulos» y filtra todo candidato que no sea cadena. Espejar esa semántica, no reinventarla
    - `src/cli/adopt.js` línea 43 — `resolveProjectPath(cwd, entry)`: la semántica hermana ya establecida en el repo para extraer una ruta de una entrada del mapa
    - `src/cli/capture.js` líneas 105-118 — el call-site: de dónde salen el cwd y el mapa de proyectos
    - `.planning/phases/83-inbox-foundation-captura-triage/83-REVIEW.md` §CR-03 — la reproducción y el snippet mínimo (que usa el nombre del directorio como proyección; la Decisión B de la cabecera de este plan lo mejora usando la ruta mapeada y deja el directorio como último recurso)
    - `.planning/phases/83-inbox-foundation-captura-triage/83-CONTEXT.md` — D-15: el propósito declarado del campo («un solo campo, siempre poblado, siempre informativo»), que es exactamente lo que el defecto incumple
  </read_first>
  <behavior>
    - Con un mapa de la forma REAL del operador —clave UUID de 36 caracteres, valor objeto con la ruta del proyecto en su clave por defecto y una tabla de módulos— y un cwd que es esa misma ruta, `deriveTag` devuelve el último segmento de la ruta del proyecto, no el UUID.
    - Con el mismo mapa y un cwd que es un SUBDIRECTORIO profundo del proyecto, `deriveTag` sigue devolviendo el último segmento de la ruta del PROYECTO, no el del subdirectorio.
    - Con un mapa cuyo valor es directamente la cadena de la ruta (la otra forma admitida), el resultado es el mismo.
    - Con una clave UUID en mayúsculas, el reconocimiento funciona igual (insensible a mayúsculas).
    - Con una clave UUID cuyo valor no aporta ninguna ruta utilizable (valor nulo, numérico, array, objeto sin ruta por defecto), `deriveTag` cae al nombre del directorio actual y nunca lanza.
    - Con una clave LEGIBLE, el resultado es exactamente el de hoy: la clave. Todos los casos previos de la suite siguen verdes sin ajustar asserts.
    - Sin match, con match ambiguo o con un mapa corrupto (nulo, numérico, array), el resultado sigue siendo el nombre del directorio actual y nunca lanza.
    - Una cadena que se parece a un UUID pero no lo es (longitud distinta, guiones mal colocados, caracteres fuera del alfabeto hexadecimal) NO se proyecta: se devuelve tal cual.
  </behavior>
  <action>
Modificar `deriveTag` en `src/inbox/store.js`. La firma, el fallback y la garantía de never-throws se conservan; lo que se añade es la proyección condicional.

1. **Helper privado `isUuidLike(s)`.** Reconoce la forma canónica de 36 caracteres de un identificador de proveedor: cinco grupos hexadecimales separados por guiones con las longitudes canónicas 8-4-4-4-12, anclado a los dos extremos e insensible a mayúsculas. Declarar la expresión como **constante de módulo**, jamás compilada desde input, igual que la del parser de línea. Coaccionar la entrada con `String(...)`.

2. **Helper privado `mappedProjectPath(projects, projectId)`.** Devuelve la ruta asociada a ese identificador en el mapa, o cadena vacía si no hay ninguna utilizable. Acepta las dos formas del valor —una cadena que ya es la ruta, o un objeto con una clave de ruta por defecto— y descarta cualquier candidato que no sea una cadena no vacía, exactamente por la misma razón por la que `candidatesOf` de `resolveProjectId` lo hace: el mapa es operator-editable y un valor degradado no puede hacer lanzar a un carril never-throws. **No** recorrer la tabla de módulos: el tag identifica el PROYECTO, y el módulo más específico no es lo que el operador necesita ver en una línea de una lista de triage. Todo el cuerpo en `try/catch`.

3. **Cablearlo en `deriveTag`.** Tras obtener el identificador de proyecto: si NO tiene forma de UUID, devolverlo tal cual (comportamiento actual, Decisión A). Si la tiene, obtener la ruta mapeada; si hay una ruta no vacía, devolver su último segmento; si no la hay, devolver el fallback del nombre del directorio actual. Todo dentro del `try/catch` existente.

4. **Reescribir el JSDoc de la función** para que documente el defecto que cierra y la decisión: la clave del mapa es un identificador de PROVEEDOR, no un nombre; en la instalación real del operador las 10 claves comprobadas son UUIDs de 36 caracteres, así que devolver la clave cruda deformaba la columna del listado y no comunicaba nada — que es exactamente la función del campo según D-15. Documentar también por qué la proyección es condicional (Decisión A) y por qué la ruta sale del mapa y no del cwd (Decisión B).

**Tests.** Ampliar `test/inbox-store.test.js` con un `describe` para la forma REAL del mapa de proyectos, cubriendo el bloque `<behavior>`. El fixture de ese bloque tiene que usar **claves UUID y valores objeto con ruta por defecto y tabla de módulos** — la forma que el operador tiene en disco, no una clave legible inventada. Encabezar el bloque con un comentario que diga por qué existe: todos los fixtures previos usaban claves legibles y por eso el defecto era invisible a una suite verde, reproducible a la vez en la máquina del operador. Mantener intactos todos los casos previos de `deriveTag`.
  </action>
  <verify>
    <automated>node --test test/inbox-store.test.js test/inbox-format-golden.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/inbox-store.test.js test/inbox-format-golden.test.js` sale con código 0.
    - `grep -c "isUuidLike" src/inbox/store.js` es al menos 2 (declaración + uso).
    - `grep -c "mappedProjectPath" src/inbox/store.js` es al menos 2 (declaración + uso).
    - `grep -c "resolveProjectId" src/inbox/store.js` sigue siendo al menos 1 — la resolución cwd→proyecto NO se reimplementa, solo se proyecta su resultado.
    - Comportamiento decisivo: con un mapa `{'7246e3fe-3dc4-4f24-9078-1911ad477e0d': {default: '/Users/x/dev/klab/kodo', modules: {DEV: '/Users/x/dev/klab/kodo'}}}` y cwd `/Users/x/dev/klab/kodo/src/inbox`, `deriveTag` devuelve `kodo`. Hoy devuelve el UUID.
    - Comportamiento: con `deriveTag('/x/y/kodo', {kodo: '/x/y/kodo'})` el resultado sigue siendo `kodo` (ninguna configuración legible cambia).
    - Comportamiento: con una clave UUID cuyo valor es `null`, `deriveTag` devuelve el nombre del directorio del cwd y no lanza.
    - Comportamiento (extremo a extremo, HOME sandbox): sembrando un mapa de proyectos con la forma real y capturando desde una ruta mapeada, el campo de tag de la línea escrita tiene menos de 36 caracteres y coincide con el último segmento de la ruta del proyecto.
    - `npm test` completa en verde.
  </acceptance_criteria>
  <done>El campo que existe para decirle al operador de qué proyecto es una captura dice el nombre del proyecto, también en la configuración que el operador tiene realmente en disco, y hay cobertura con esa forma real.</done>
</task>

<task type="auto">
  <name>Task 2: El carril de datos sanea lo que el serializador no escapa, y el marcado explica por qué no se aplicó</name>
  <files>src/cli/inbox.js, test/inbox-cli.test.js</files>
  <read_first>
    - `src/cli/inbox.js` — fichero completo. En concreto: la rama del carril de datos con su comentario (el que afirma que el serializador escapa todo control C0 y que por eso el texto puede ir verbatim — la premisa que el review refutó), el orden fijo de claves del objeto emitido, el render human y su uso del saneador de controles, y el mapeo de los `reason` del store a copy y exit code
    - `src/cli/format.js` líneas 75-130 — `stripControlChars` y `stripForKeystroke`: qué cubre exactamente cada uno y por qué el carril human ya está cerrado. El saneador de este plan es más estrecho a propósito (Decisión C)
    - `src/inbox/store.js` — la unión de `reason` de `markCapture` tras el Plan 83-04, con el valor nuevo de escritura concurrente y su semántica: el marcado no se aplicó y el fichero quedó intacto
    - `test/inbox-cli.test.js` líneas 440-500 — el test que asserta el saneo en el carril human con vectores escritos en notación de escape; el caso nuevo del carril de datos es su espejo y tiene que respetar la misma higiene (cero bytes de control literales en el source)
    - `.planning/phases/83-inbox-foundation-captura-triage/83-REVIEW.md` §WR-02 — la medición con volcado de bytes y por qué el modelo de amenaza del carril human se aplica igual al carril de datos
  </read_first>
  <action>
**A. Saneo del carril de datos (WR-02).**

Añadir a `src/cli/inbox.js` un helper privado `sanitizeJsonField(s)` que elimine del valor el rango de controles que el serializador deja pasar verbatim: DEL y el bloque C1. Escribir el rango con notación de escape, nunca con bytes literales (higiene declarada en la cabecera del fichero de test). Aplicarlo, en la construcción del objeto del carril de datos, a los campos que provienen del fichero human-editable: el texto, el tag, el origen y —en la rama que incluye las capturas cerradas— el destino, respetando que el destino puede ser nulo. El identificador y la fecha **no** lo necesitan: el parser los restringe a alfabetos que no contienen controles; dejarlos fuera mantiene el cambio mínimo y verificable.

No cambiar el orden ni el conjunto de claves del objeto, no instanciar el formateador en esa rama y no tocar el render human. Sustituir el comentario que hoy justifica el paso verbatim por uno que diga la verdad medida: el serializador escapa los controles C0 pero **no** DEL ni el bloque C1, y ambos salían verbatim; el fichero es human-editable por diseño, así que una línea pegada a mano con una secuencia de escritura al portapapeles llegaría íntegra al consumidor del carril de datos — que es además el carril que la skill del orquestador manda usar y cuya salida un modelo reemite hacia el terminal. Es el mismo modelo de amenaza que el carril human ya cierra.

**B. Mapeo honesto del modo de fallo por escritura concurrente.**

Añadir al mapeo de `reason` del handler de marcado la rama del valor nuevo que el Plan 83-04 introdujo. Exit code **1** (reintentable, igual que el lock ocupado) y una copy que distinga las dos causas: aquí el lock SÍ se obtuvo, pero una captura concurrente aterrizó mientras se marcaba, así que el marcado se abortó **para no destruirla** y el fichero quedó intacto. La acción del operador es la misma —reintentar—, pero la causa no, y conflar ambas dejaría al siguiente mantenedor pensando que hay contención de lock donde hay un guard funcionando exactamente como debe. Mantener la rama por defecto como está: sigue siendo la red para cualquier `reason` inesperado.

**C. Tests en `test/inbox-cli.test.js`.**

1. Espejo del test de saneo del carril human, sobre el carril de datos: sembrar el inbox con una línea cuyo texto, tag y destino contengan vectores escritos en **notación de escape** (un control del bloque C1, otro del mismo bloque y DEL), invocar el listado en modo datos y assertar que la salida parseada no contiene ninguno de esos puntos de código en ningún campo, y que el resto del texto sobrevive íntegro. Assertar también que sigue siendo una sola línea y sin secuencias de color.
2. Un caso de determinismo: dos invocaciones consecutivas sobre el mismo fixture producen salidas byte-idénticas (DX-06 no se rompe con el saneo).
3. Un caso por inyección de dependencia sobre el handler de marcado que devuelva el `reason` de escritura concurrente y asserte exit 1 más un mensaje de error que mencione la concurrencia y distinto del mensaje de lock ocupado.
  </action>
  <verify>
    <automated>node --test test/inbox-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/inbox-cli.test.js` sale con código 0.
    - `grep -c "sanitizeJsonField" src/cli/inbox.js` es al menos 5 (declaración + los campos a los que se aplica en las dos ramas).
    - `grep -c "concurrent-write" src/cli/inbox.js` es al menos 1.
    - `grep -nE "^import .*picocolors" src/cli/inbox.js` sigue sin devolver coincidencias y `node --test test/format-isolation.test.js` sigue en verde.
    - `grep -c "x7f\|u007f" src/cli/inbox.js` es al menos 1 y no hay ningún byte de control literal en el fichero (comprobable con una inspección del diff).
    - Comportamiento decisivo: con un inbox sembrado con los tres vectores en notación de escape, la salida del carril de datos no contiene ninguno de esos puntos de código, mientras que hoy sí los contiene.
    - Comportamiento: dos ejecuciones consecutivas del carril de datos sobre el mismo fixture producen salidas byte-idénticas.
    - Comportamiento: el handler de marcado ante el `reason` de escritura concurrente devuelve 1 y su mensaje difiere del de lock ocupado.
    - `npm test` completa en verde.
  </acceptance_criteria>
  <done>El carril que la skill manda usar deja de ser el hueco del modelo de amenaza, y un marcado que se aborta para proteger una captura concurrente se lo dice al operador con esas palabras.</done>
</task>

<task type="auto">
  <name>Task 3: El aviso del fail-open pasa por el seam del handler y la documentación deja de prometer lo que no hay</name>
  <files>src/cli/capture.js, test/inbox-cli.test.js, README.md, .claude/skills/kodo-orchestrate/skill.md</files>
  <read_first>
    - `src/cli/capture.js` — la resolución de dependencias al principio del handler (dónde se define el seam de salida de error) y la llamada a la función de append, que hoy solo le pasa los dos paths
    - `src/inbox/store.js` — la firma de `appendCapture`: el tercer parámetro opcional de aviso y su default, que escribe directo al stream de error del proceso. Y el JSDoc reescrito por el Plan 83-04 sobre por qué esa rama vuelve a ser alcanzable
    - `test/inbox-cli.test.js` — el carril unit por inyección de dependencias del handler de captura (el molde de los espías) y el carril de integración con HOME sandbox (para el caso del lock ocupado sobre el binario real)
    - `test/inbox-store.test.js` líneas 380-420 — cómo se toma el lock desde el propio test para forzar la rama fail-open
    - `.claude/skills/kodo-orchestrate/skill.md` líneas 379-422 — la sección de triage completa: la frase que describe el fichero, el bloque de tres pasos y las reglas duras con sus exit codes
    - `README.md` líneas 166-240 y 370-380 — el bloque de comandos, la subsección del inbox y el árbol de ficheros
    - `.planning/phases/83-inbox-foundation-captura-triage/83-REVIEW.md` §WR-08 e §IN-02
  </read_first>
  <action>
**A. `src/cli/capture.js` — propagar el seam (WR-08).**

Pasar el seam de salida de error del handler a la función de append junto con los dos paths, de modo que el aviso del fail-open salga por el mismo canal inyectable que el resto de la salida de error del handler en vez de escribir directo al stream del proceso. Es un cambio de una línea. Añadir un comentario de una frase: la rama del fail-open es la única señal que el operador recibe cuando su captura se escribió sin coordinación, y desde que el Plan 83-04 devolvió el presupuesto de reintentos al default de la primitiva esa rama vuelve a ser alcanzable en producción — así que tiene que ser observable por el mismo carril que todo lo demás, no solo por los tests que inyectan el seam a mano.

No cambiar el contrato de «exactamente un aviso»: el store sigue emitiendo uno solo y el handler sigue sin añadir ningún mensaje propio en esa rama.

**B. `test/inbox-cli.test.js` — observarlo en el binario real.**

Añadir un caso de integración con HOME sandbox que tome el lockfile del inbox antes de invocar el binario (escribiendo un lockfile con el contenido que la primitiva espera y un identificador de proceso VIVO, para que no se considere robable), invoque el subcomando de captura, y asserte tres cosas: el código de salida es 0, la línea SÍ está en el fichero, y la salida de error contiene el aviso del fail-open con su prefijo. Liberar el lock en la limpieza. Es la primera prueba de que esa rama funciona en el binario y no solo en un unit con el seam inyectado a mano.

**C. `.claude/skills/kodo-orchestrate/skill.md` — tres ediciones quirúrgicas en la sección de triage, sin tocar ninguna otra sección.**

1. **IN-02.** Sustituir la descripción del fichero que hoy afirma que es de solo-append por una que describa el modelo real: el fichero **solo crece** —cerrar es una transición de estado, nunca un borrado— y la traza es permanente. Motivo, que conviene dejar explícito en el propio texto o en la regla dura correspondiente: el marcado reescribe el fichero entero bajo lock, así que un agente que asuma solo-append infiere garantías de concurrencia que no existen. Sustituir la frase; **no** dejar una nota de cambio ni mencionar la redacción anterior.
2. **Modo de fallo nuevo.** En la regla dura que hoy explica que el marcado puede fallar y la captura no, añadir que el marcado también sale con código 1 —y deja el fichero intacto— cuando una captura concurrente aterriza mientras se marcaba: el comando se aborta para no destruirla y la acción es la misma, reintentar.
3. **WR-05.** Añadir una regla dura corta: si alguna vez shelleas el comando de captura, antepón siempre el separador de argumentos antes del texto; un texto que empiece por guion sin ese separador aborta la captura y la idea se pierde.

**D. `README.md` — una edición quirúrgica.**

En la subsección del inbox, junto a la forma de invocar la captura, añadir una línea que declare la forma segura para un texto que empiece por guion (anteponer el separador de argumentos). No documentar el flag interno de origen, no describir filtros que no existen y no reescribir ninguna otra parte del README.

**Coherencia:** los tres pasos del seam de enrutado, los nombres de comando y los exit codes tienen que seguir siendo idénticos entre el README y el skill. Este plan no cambia el flujo de tres pasos ni la afirmación de delegación de CAPT-04: solo corrige la descripción del modelo del fichero y añade dos notas.
  </action>
  <verify>
    <automated>node --test test/inbox-cli.test.js test/inbox-store.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/inbox-cli.test.js test/inbox-store.test.js` sale con código 0.
    - `grep -c "warnFn" src/cli/capture.js` es al menos 1 — el seam se propaga desde el handler.
    - `grep -ci "append-only" .claude/skills/kodo-orchestrate/skill.md` devuelve 0.
    - `grep -c "solo crece" .claude/skills/kodo-orchestrate/skill.md` es al menos 1.
    - `grep -c "kodo inbox" .claude/skills/kodo-orchestrate/skill.md` sigue siendo al menos 2 y el bloque de tres pasos sigue presente en el mismo orden: listar, enrutar fuera de kodo, marcar.
    - `grep -c "\-\-origin" README.md` sigue devolviendo 0 (el flag es interno — D-16).
    - `grep -c "\-\-project\|\-\-open" README.md` no aumenta respecto al valor previo (no se documentan filtros inexistentes — D-14).
    - Comportamiento: con el lockfile del inbox tomado por un proceso vivo y un HOME sandbox, el binario de captura sale con 0, la línea está en el fichero, y la salida de error contiene el aviso del fail-open.
    - `npm test` completa en verde.
  </acceptance_criteria>
  <done>La única señal del fail-open viaja por el carril inyectable del handler y está probada sobre el binario; la documentación del orquestador describe el modelo real del fichero, su modo de fallo nuevo y la forma segura de capturar un texto con guion inicial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.kodo/inbox.md` (human-editable) → carril de datos → consumidor automatizado y su terminal | El fichero es editable a mano por diseño; su contenido no es confiable y el carril de datos lo reemite hacia un orquestador que lo devuelve al terminal |
| `~/.kodo/projects.json` (operator-editable) → `deriveTag` → línea persistida | El mapa puede tener cualquier forma tras una edición manual; su contenido acaba en un campo estructurado del fichero |
| Documentación del orquestador → comportamiento de un agente | Una copy que prometa garantías de concurrencia inexistentes induce al agente a asumir exactamente el escenario del lost-update |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-37 | Tampering | Controles DEL y del bloque C1 emitidos verbatim por el carril de datos hacia el terminal de un consumidor | high | mitigate | `sanitizeJsonField` elimina exactamente el rango que el serializador no escapa, aplicado a los campos que provienen del fichero; test espejo del carril human con vectores en notación de escape |
| T-83-38 | Information Disclosure | El tag persistido pasa a derivar de una ruta del filesystem del operador | low | accept | Solo se persiste el ÚLTIMO SEGMENTO de la ruta del proyecto, nunca la ruta completa; el fichero es local, no se transmite, y el fallback previo (nombre del directorio actual) ya exponía el mismo grado de información. Riesgo tolerado y consistente con D-20 |
| T-83-39 | Denial of Service | Expresión regular del reconocedor de identificadores sobre un valor patológico del mapa | low | accept | La expresión es una constante de módulo con longitudes fijas y sin cuantificadores anidados, anclada a los dos extremos; jamás se compila desde input. Riesgo tolerado: fichero local, un solo usuario |
| T-83-40 | Tampering | Valor degradado del mapa de proyectos (nulo, numérico, array) haciendo lanzar a un carril never-throws | medium | mitigate | El extractor de ruta filtra todo candidato que no sea cadena no vacía, espejando `candidatesOf` de la resolución cwd→proyecto, y va envuelto en `try/catch`; casos de mapa corrupto en la suite |
| T-83-41 | Repudiation | Modo de fallo por escritura concurrente presentado al operador como contención de lock | medium | mitigate | Rama propia en el mapeo de `reason` con copy que nombra la causa real y mantiene el exit code reintentable; test por inyección que comprueba que ambos mensajes difieren |
| T-83-42 | Repudiation | Rama del fail-open sin observabilidad real en producción: nadie verifica el comportamiento del binario en ese camino | medium | mitigate | El aviso se propaga por el seam inyectable del handler y hay un test de integración que lo observa sobre el binario con el lock tomado por un proceso vivo |
| T-83-43 | Elevation of Privilege | Copy que induzca a un agente a escribir el fichero a mano o a automatizar el paso de enrutado | medium | mitigate | Las reglas duras existentes de no escribir el fichero y de no automatizar el enrutado se conservan intactas; la corrección del modelo del fichero refuerza el motivo en vez de debilitarlo |
| T-83-SC | Tampering | Instalaciones npm/pip/cargo | high | mitigate | Cero instalaciones: solo built-ins de Node y código del repo. `package.json` `dependencies` sigue en 4 claves y el gate vive en `test/inbox-cli.test.js`. No hay paquetes `[ASSUMED]`/`[SUS]` que verificar |

**Canon-referral (no minteado como prohibición):** path traversal vía el identificador o el destino sigue cubierto por construcción (el identificador solo matchea líneas y nunca compone un path; el destino es texto opaco que kodo no resuelve) y por `/gsd-secure-phase`.
</threat_model>

<verification>
- `node --test test/inbox-store.test.js test/inbox-format-golden.test.js test/inbox-cli.test.js` en verde.
- `node --test test/inbox-concurrency.test.js` en verde (este plan no toca la superficie de concurrencia; es la red de no-regresión sobre los Planes 83-04 y 83-06).
- `node --test test/format-isolation.test.js` en verde (color isolation intacta).
- `npm test` completa en verde.
- `git diff --stat package.json package-lock.json` vacío (invariante cero deps).
- Comprobación manual de una línea: capturar desde este mismo repo con la configuración real y comprobar que el campo de tag es legible y no un identificador de 36 caracteres.

## Cierre de la fase — comprobación agregada de los tres gaps

Tras aterrizar este plan, los cuatro planes del ciclo cierran los tres gaps de `83-VERIFICATION.md`:

| Gap | Plan que lo cierra | Evidencia |
|-----|--------------------|-----------|
| GAP-1 — captura concurrente destruida por el marcado | 83-04 (guard compare-and-swap + presupuesto + JSDoc) y 83-06 (escenario por encima del presupuesto + guard de cobertura) | `node --test test/inbox-concurrency.test.js` con el escenario 3 en verde y con la comprobación de mordida registrada |
| GAP-2 — carril de datos truncado en 64 KB | 83-05 | Regresión con inbox por encima de 64 KB que parsea la salida canalizada y falla sin el arreglo |
| GAP-3 — tag ilegible en la instalación real | 83-07 Task 1 | Cobertura con la forma real del mapa de proyectos y comprobación extremo a extremo del campo persistido |
</verification>

<success_criteria>
- El tag comunica el proyecto con la configuración que el operador tiene en disco, y hay un test que usa esa forma real.
- El carril de datos neutraliza exactamente lo que el serializador no cubre, sin perder byte-determinismo ni introducir color.
- Un marcado abortado para proteger una captura concurrente se distingue de un lock ocupado, con el mismo exit code reintentable.
- La única señal del fail-open es observable por el carril inyectable del handler y está probada sobre el binario real.
- La documentación del orquestador describe el modelo real del fichero y la forma segura de capturar texto con guion inicial.
</success_criteria>

<output>
Create `.planning/phases/83-inbox-foundation-captura-triage/83-07-SUMMARY.md` when done
</output>
