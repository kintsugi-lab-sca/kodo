---
phase: 68-dashboard-setup-mode-cfgf-03-first-run
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - src/cli/dashboard/index.js
  - test/dashboard/app-setup.test.js
autonomous: true
requirements: [SETUP-01, SETUP-02]
must_haves:
  truths:
    - "En first-run el dashboard entra en mode:'setup' (pantalla guiada lineal), no en la tabla (D-01/D-04)"
    - "El paso 1/4 es un selector de provider ['plane','github']; elegir plane escribe config.provider vía saveConfig (D-05)"
    - "Elegir github muestra SETUP_GITHUB_REDIRECT y NO continúa el guiado (D-06)"
    - "Los pasos base_url y workspace_slug persisten a ~/.kodo/config.json vía saveConfig (SETUP-02)"
    - "El paso 4/4 (API key) se enmascara como '•' y persiste a ~/.kodo/.env vía onSaveApiKey→writeEnvVar (D-11)"
    - "Al completar el setup se muestra el aviso de reinicio honesto SETUP_COMPLETE_RESTART + SETUP_WEBHOOK_NOTE (D-08/D-12)"
    - "En non-TTY (isRawModeSupported=false) el overlay renderiza SETUP_NO_RAWMODE y remite a kodo config, never-throws (D-13)"
    - "Las 16 constantes SETUP_* están exportadas de App.js (copy literal-estable en español)"
  artifacts:
    - "src/cli/dashboard/App.js — 16 constantes SETUP_* exportadas + mode:'setup' con sub-pasos provider/base_url/workspace_slug/apikey"
    - "src/cli/dashboard/SessionTable.js — renderSetupOverlay + rama de dispatch mode==='setup'"
    - "src/cli/dashboard/index.js — wiring de la decisión de render setup (flag setup / needsSetupFn) reusando onSaveConfig/onSaveApiKey"
    - "test/dashboard/app-setup.test.js — state-machine del modo setup (ink-testing-library)"
  key_links:
    - "El paso apikey reusa LITERAL el flujo de Phase 67 (onSaveApiKey→writeEnvVar + setBuffer('')+setMaskValue(false))"
    - "renderSetupOverlay es molde EXACTO de renderConfigOverlay (gutter width:2 + label width:24 + cursor inverse + máscara '•')"
    - "SessionTable.js importa las constantes SETUP_* de App.js (mata el drift code/render)"
  prohibitions:
    - "El VALOR de la API key NUNCA se renderiza raw (siempre '•'.repeat), ni se loguea, ni entra a config.json / snapshot / argv / /status (PERSIST-04/Pitfall 5/11)"
    - "El buffer del secreto se limpia (setBuffer('') + setMaskValue(false)) al guardar y al cancelar — no persiste en memoria"
    - "El modo setup NO introduce colores/anchos nuevos — reusa la paleta ink LOCKED (cyan/yellow/red/dim) y el molde de renderConfigOverlay"
    - "NO se convierte el guard non-TTY de index.js:90-93 (D-13 — se mantiene tal cual); la degradación non-TTY vive DENTRO del render vía isRawModeSupported"
    - "El text-input NO compila regex desde el input (substring puro / String.includes — anti-ReDoS)"
    - "El paso github NO intenta escribir repos[]/poll_interval (fuera de scope, D-06) — solo remite a kodo config"
    - "El modo setup aparece SOLO auto en first-run (vía el flag setup / needsSetupFn); NO se añade ninguna tecla que re-lance el guiado post-first-run (D-07 — los cambios posteriores usan el editor de config existente de Phase 63/64)"
---

<objective>
Materializar el corazón de la fase: un nuevo `mode:'setup'` en la state-machine del dashboard (`App.js`), pantalla guiada lineal de 4 pasos (provider → base_url → workspace_slug → API key enmascarada, D-04), su render (`renderSetupOverlay` en `SessionTable.js`, molde exacto de `renderConfigOverlay`), el wiring de la decisión de render en `index.js`, y la degradación non-TTY honesta (D-13). Reuso ~90% de piezas enviadas en Phases 63/67; el único código nuevo es la rama `setup`, el selector de provider y las 16 constantes `SETUP_*`.

Purpose: cierra la mitad "render + edición" de SETUP-01 (servir el setup mode sin exit(1) en terminal real) y todo SETUP-02 (editar provider/base_url/workspace_slug → config.json + aviso de reinicio honesto). El boundary de seguridad de la fase (PERSIST-04) se juega aquí: el render del secreto no puede abrir un nuevo vector de fuga.
Output: modo setup funcional en el dashboard + `test/dashboard/app-setup.test.js` verde (selector, saves, máscara, aviso, non-TTY).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-CONTEXT.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-RESEARCH.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-PATTERNS.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-UI-SPEC.md
@.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-VALIDATION.md
@src/cli/dashboard/App.js
@src/cli/dashboard/SessionTable.js
@src/cli/dashboard/index.js
@test/dashboard/app-dismiss.test.js
</context>

## Artifacts this phase produces (para excluir en source-grounding)

Símbolos NUEVOS creados en este plan (sin consumidor previo — no tratar como fuente huérfana):
- Las 16 constantes `SETUP_*` exportadas de `App.js`: `SETUP_OVERLAY_TITLE`, `SETUP_INTRO`, `SETUP_STEP_PROVIDER`, `SETUP_STEP_BASE_URL`, `SETUP_STEP_WORKSPACE`, `SETUP_STEP_APIKEY`, `SETUP_PROVIDER_LABEL`, `SETUP_PROVIDER_HINT`, `SETUP_GITHUB_REDIRECT`, `SETUP_BASE_URL_LABEL`, `SETUP_WORKSPACE_LABEL`, `SETUP_COMPLETE_RESTART`, `SETUP_WEBHOOK_NOTE`, `SETUP_NO_RAWMODE`, `SETUP_INVALID`, `SETUP_SAVE_FAILED`.
- `mode:'setup'` + los sub-pasos `provider`/`base_url`/`workspace_slug`/`apikey` en la state-machine de `App.js`.
- `renderSetupOverlay` en `SessionTable.js`.
- El flag `setup` propagado por `runDashboard`/prop de `App` (origen en `runUp`, plan 68-01).
- Constantes REUSADAS de Phase 67 (NO redeclarar): `API_KEY_LABEL`, `API_KEY_CONFIGURED`, `API_KEY_UNSET`, `API_KEY_INVALID`, `API_KEY_NO_RAWMODE`.

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test/dashboard/app-setup.test.js (state-machine del modo setup, RED)</name>
  <files>test/dashboard/app-setup.test.js</files>
  <read_first>
    - `test/dashboard/app-dismiss.test.js` — harness completo: `render` de ink-testing-library + `createElement(App, {...})` + import de constantes exportadas (app-dismiss.test.js:22-31), fake clock, `stdin.write(...)` → `lastFrame()`, y la nota RESEARCH Pitfall 5 (ink NO awaitea el handler async → un save puede necesitar DOS `await drain()`).
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-VALIDATION.md` §Per-Task Verification Map — filas SETUP-02 (selector, base_url/slug, API key enmascarada, aviso, non-TTY).
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-UI-SPEC.md` §Interaction Contract (los 4 pasos + degradación non-TTY) y §Copywriting (constantes `SETUP_*`).
  </read_first>
  <action>
    Crear `test/dashboard/app-setup.test.js` como scaffold Wave 0 (RED — falla hasta que Task 2 exporte las `SETUP_*` y añada `mode:'setup'`). Copiar el harness de `app-dismiss.test.js` (render + fake clock + stdin.write + drain). Importar de `App.js` las constantes `SETUP_*` y las reusadas `API_KEY_*`. Casos (levantados del Test Map de VALIDATION.md, lift a asserts concretos):
    (a) App montado con la prop de setup activa (flag `setup:true` o `needsSetupFn` que devuelve true) → `lastFrame()` muestra `SETUP_OVERLAY_TITLE` + `SETUP_STEP_PROVIDER` (entra en modo setup, no en la tabla);
    (b) selector de provider: ↑/↓ mueven el cursor; Enter en `plane` → `onSaveConfig` recibe un config con `provider: 'plane'` y el frame avanza a `SETUP_STEP_BASE_URL`;
    (c) Enter en `github` → frame muestra `SETUP_GITHUB_REDIRECT` y NO avanza (cero onSaveConfig de estructurales, D-06);
    (d) paso base_url y workspace_slug: teclear + Enter → `onSaveConfig` recibe el valor en `providers.plane.base_url` / `.workspace_slug`;
    (e) paso apikey: teclear la key → el frame muestra `'•'` repetido (NUNCA el valor literal en `lastFrame()`); Enter → `onSaveApiKey` recibe `(api_key_env, valorTecleado)` y el frame muestra `SETUP_COMPLETE_RESTART` (aviso honesto);
    (f) non-TTY: montar con `isRawModeSupported` falso (stdin sin raw-mode, molde de los tests de Phase 67) → el frame muestra `SETUP_NO_RAWMODE` y no lanza (never-throws).
    Los callbacks `onSaveConfig`/`onSaveApiKey` son fakes que registran sus argumentos (molde de los routers/fakes de app-dismiss.test.js). Assert clave de seguridad (T-PERSIST-04): en el caso (e), `lastFrame()` NUNCA contiene el string del valor tecleado de la key.
  </action>
  <acceptance_criteria>
    - `test/dashboard/app-setup.test.js` existe e importa `App` + constantes `SETUP_*` de `../../src/cli/dashboard/App.js`.
    - Cubre los 6 casos (a)-(f) del `<action>`.
    - Incluye el assert held-out de seguridad: el valor de la key nunca aparece en `lastFrame()` (solo `'•'`).
    - `node --test test/dashboard/app-setup.test.js` corre en RED (falla por import/mode ausente) — documenta el gap Wave 0 antes de implementar.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dashboard/app-setup.test.js 2>&1 | grep -qiE 'fail|cannot|undefined|not a function' && echo "RED OK (gap documentado)"</automated>
  </verify>
  <done>test/dashboard/app-setup.test.js creado con los 6 casos + el assert de no-fuga del secreto; corre en RED (import/mode ausente) — Wave 0 scaffold listo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mode:'setup' + 16 constantes SETUP_* (App.js) + renderSetupOverlay (SessionTable.js) → GREEN</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - `src/cli/dashboard/App.js` — bloque de constantes exportadas (App.js:239-260, molde `CONFIG_*`/`API_KEY_*`), `useState` del mode (App.js:509), el handler `mode==='config-edit'` (App.js:993-1104) con: guardado del masked field (App.js:1029-1057, reuso literal — `onSaveApiKey`→`setBuffer('')`+`setMaskValue(false)`+`setFocusError(API_KEY_SAVED_RESTART)`), guardado de campo con deep-clone (App.js:1071-1094 — `structuredClone` + `setByPath` + `onSaveConfig`), inserción-en-cursor (App.js:1097-1102). Gate `isRawModeSupported` (App.js:435, 1653). Props: `onSaveConfig`/`onSaveApiKey`/`isApiKeyConfiguredFn` (App.js:426-428). Render dispatch payload (App.js:1700-1710).
    - `src/cli/dashboard/SessionTable.js` — `renderConfigOverlay` (SessionTable.js:296-359, molde), render enmascarado `'•'.repeat` + cursor inverse (SessionTable.js:342-346), degradación non-TTY `if (!rawModeSupported)` (SessionTable.js:339-340), dispatch por modo (SessionTable.js:692-693).
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-UI-SPEC.md` §Copywriting (valores literales de las 16 `SETUP_*`, español, literal D-08 preservado) + §Interaction Contract (4 pasos).
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-PATTERNS.md` §App.js y §SessionTable.js (moldes a copiar).
  </read_first>
  <behavior>
    - Con la prop de setup activa, el mode inicial es 'setup' con sub-paso 'provider'.
    - Selector provider: Enter en 'plane' → onSaveConfig con provider='plane', avanza a base_url; Enter en 'github' → SETUP_GITHUB_REDIRECT, no avanza.
    - base_url / workspace_slug: text-input controlado (inserción-en-cursor, backspace/delete, ←/→); Enter válido → onSaveConfig del campo, avanza; vacío/inválido → SETUP_INVALID, no avanza.
    - apikey: máscara '•'; Enter → onSaveApiKey(api_key_env, buffer) → éxito limpia buffer+máscara y muestra SETUP_COMPLETE_RESTART + SETUP_WEBHOOK_NOTE; fallo I/O → SETUP_SAVE_FAILED (rojo).
    - Esc en cualquier paso → cierra sin escribir (never-throws); en el paso apikey limpia buffer+máscara.
    - non-TTY (isRawModeSupported=false) → renderSetupOverlay pinta SETUP_NO_RAWMODE (dim), never-throws.
  </behavior>
  <action>
    En `src/cli/dashboard/App.js`: (1) declarar y EXPORTAR las 16 constantes `SETUP_*` junto al bloque existente (App.js:239-260), con los valores literales de UI-SPEC §Copywriting (español; preservar el literal de D-08 en `SETUP_COMPLETE_RESTART`; NUNCA incluir el valor del secreto ni el nombre de la env var en ninguna cadena user-facing — Pitfall 11); reusar `API_KEY_LABEL`/`API_KEY_CONFIGURED`/`API_KEY_UNSET`/`API_KEY_INVALID`/`API_KEY_NO_RAWMODE` de Phase 67 (NO redeclarar). (2) Extender el union de `mode` (App.js:509) con `'setup'` y añadir un estado de sub-paso (`setupStep` ∈ `provider`/`base_url`/`workspace_slug`/`apikey`) y un cursor de selección de provider. (3) Inicializar `mode='setup'` cuando la prop de setup está activa (D-01 — la decisión de render vive local en App; mecanismo exacto a discreción: consumir el flag `setup` propagado desde runUp o una prop `needsSetupFn` — respetando D-01/D-04). (4) Añadir la rama `if (mode === 'setup')` en el `useInput` mode-gated: paso provider = lista `['plane','github']` con cursor clamp-sin-wrap (molde cli.js:588 adaptado a ↑/↓+Enter), Enter en 'github' → `setFocusError(SETUP_GITHUB_REDIRECT)` sin avanzar (D-06), Enter en 'plane' → `structuredClone(configSnapshot)` + `setByPath(next,'provider','plane')` + `onSaveConfig(next)` (molde App.js:1071-1094) → avanzar a base_url; pasos base_url/workspace_slug = text-input controlado (reuso App.js:1097-1102 + validación no-vacío → `SETUP_INVALID` si falla, no avanza) que en Enter válido guardan vía `setByPath(next,'providers.plane.base_url'|'workspace_slug', value)` + `onSaveConfig`; paso apikey = REUSO LITERAL del bloque de Phase 67 (App.js:1029-1057: `onSaveApiKey(apiKeyEnv, buffer)` → éxito `setBuffer('')`+`setMaskValue(false)`+aviso `SETUP_COMPLETE_RESTART`/`SETUP_WEBHOOK_NOTE`; fallo `SETUP_SAVE_FAILED`). Esc = never-throws (cierra; en apikey limpia buffer+máscara, molde App.js:1002-1004). (5) En el payload de dispatch del render (App.js:1700-1710) propagar `setupStep`, el provider-cursor, `mask`, `rawModeSupported` al SessionTable.
    En `src/cli/dashboard/SessionTable.js`: añadir `renderSetupOverlay(...)` como molde EXACTO de `renderConfigOverlay` (SessionTable.js:296-359): gutter `Box width:2` (`'› '`/`'  '`), label `Box width:24`, título `SETUP_OVERLAY_TITLE` (cyan/bold), cabecera de paso `SETUP_STEP_*` (dim), cursor `inverse`, y para el paso apikey la máscara `'•'.repeat(buffer.length)` + cursor inverse sobre la máscara (reuso literal SessionTable.js:342-346). Añadir la degradación non-TTY: `if (!rawModeSupported) → SETUP_NO_RAWMODE` (dim, never-throws, molde SessionTable.js:339-340). Añadir la rama de dispatch `if (mode === 'setup') return renderSetupOverlay(...)` en el bloque de dispatch (SessionTable.js:692), importando las constantes `SETUP_*` de App.js (mata el drift). Objetivo: `test/dashboard/app-setup.test.js` en GREEN.
  </action>
  <acceptance_criteria>
    - `src/cli/dashboard/App.js` exporta las 16 constantes `SETUP_*` (source assertion: `export const SETUP_OVERLAY_TITLE` … `export const SETUP_SAVE_FAILED`).
    - `SETUP_COMPLETE_RESTART` preserva el literal de D-08 (menciona reiniciar kodo / `kodo up`).
    - El union de `mode` incluye `'setup'` y existe la rama `if (mode === 'setup')` en el `useInput`.
    - `SessionTable.js` define `renderSetupOverlay` y lo despacha con `mode === 'setup'`, importando las `SETUP_*` de App.js (no strings duplicados).
    - El render del paso apikey deriva `'•'.repeat` (source assertion) y en non-TTY pinta `SETUP_NO_RAWMODE`.
    - `node --test test/dashboard/app-setup.test.js` exits 0 (los 6 casos GREEN, incluido el assert de no-fuga del valor).
    - Regresión: `node --test test/dashboard/app-dismiss.test.js` sigue verde (la state-machine existente no se rompe).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dashboard/app-setup.test.js test/dashboard/app-dismiss.test.js</automated>
  </verify>
  <done>Modo setup lineal de 4 pasos funcional (provider/base_url/workspace_slug/apikey), 16 constantes SETUP_* exportadas, renderSetupOverlay molde-exacto con máscara '•' + degradación non-TTY; app-setup.test.js verde y sin regresión de app-dismiss.</done>
</task>

<task type="auto">
  <name>Task 3: wiring en index.js (decisión de render setup + reuso de wrappers) + full green</name>
  <files>src/cli/dashboard/index.js, test/dashboard/app-setup.test.js</files>
  <read_first>
    - `src/cli/dashboard/index.js` — `runDashboard` (index.js:85-333): guard non-TTY (index.js:88-93, NO se toca — D-13), lazy import compartido `const { loadConfig, saveConfig, writeEnvVar, isApiKeyConfigured } = await import('../../config.js')` (index.js:110), `render(createElement(App, {...}))` (index.js:240), wrappers `onSaveConfig`/`onSaveApiKey`/`isApiKeyConfiguredFn` YA cableados (index.js:279-302).
    - `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-PATTERNS.md` §index.js (wiring a añadir; Assumption A3: `needsSetupFn` como prop es la ruta más coherente con D-01; alternativa el flag `setup:true` ya propagado desde runUp).
    - `src/cli/up.js` — la rama setup de plan 68-01 ya llama `runDashboardFn({ url: baseUrl, setup: true })`.
  </read_first>
  <action>
    En `src/cli/dashboard/index.js`, aceptar el flag `setup` en la firma de `runDashboard({ url, setup })` (propagado por `runUp`, plan 68-01) y pasarlo como prop a `render(createElement(App, {...}))` (index.js:240). Añadir `needsSetup` al destructuring del lazy import compartido (index.js:110) y pasar `needsSetupFn: (providerName) => needsSetup(providerName)` como prop, de modo que App pueda decidir el render setup local (D-01) — el flag `setup` de runUp es la señal primaria, `needsSetupFn` la comprobación coherente-con-D-01. REUSAR sin cambios los wrappers `onSaveConfig`/`onSaveApiKey`/`isApiKeyConfiguredFn` ya cableados (index.js:279-302) — el modo setup los consume tal cual (cero writer nuevo). NO tocar el guard non-TTY (index.js:88-93): D-13 lo mantiene; la degradación non-TTY del setup vive dentro del render (`isRawModeSupported` → `SETUP_NO_RAWMODE`, Task 2). Cerrar `test/dashboard/app-setup.test.js` cubriendo el arranque en modo setup vía el flag/`needsSetupFn` (montar App con la prop y assert de que entra en modo setup). Ejecutar la suite tocada (app-setup + up) para confirmar el circuito completo runUp→index→App.
  </action>
  <acceptance_criteria>
    - `runDashboard` acepta `setup` y lo pasa como prop a `App` (source assertion).
    - `index.js` reusa los wrappers existentes `onSaveConfig`/`onSaveApiKey`/`isApiKeyConfiguredFn` (no crea escritores nuevos de config/.env).
    - `needsSetup` se añade al destructuring del import compartido de `config.js` (index.js:110) y se pasa como `needsSetupFn`.
    - El guard non-TTY (index.js:88-93) queda intacto (source assertion: sin cambios en ese bloque — D-13).
    - `node --test test/dashboard/app-setup.test.js test/cli/up.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dashboard/app-setup.test.js test/cli/up.test.js</automated>
  </verify>
  <done>index.js cablea la decisión de render setup (flag setup + needsSetupFn) reusando los wrappers de Phase 63/67 sin escritores nuevos; guard non-TTY intacto (D-13); circuito runUp→index→App verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (teclado TUI) → proceso | El valor de la API key se teclea en el masked field. Cruza en-proceso a `writeEnvVar`. Nunca debe cruzar a render raw / logger / argv / config.json. |
| proceso → filesystem (~/.kodo) | `saveConfig` (config.json) y `writeEnvVar` (.env 0600) son los únicos escritores; el modo setup los dispara, no escribe directo. |
| stdin (TTY vs pipe/CI) → render | El render debe degradar sin crashear cuando falta raw-mode (never-throws). |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-04 | Information Disclosure | render del paso apikey (App.js/SessionTable.js) | high | mitigate | Máscara `'•'.repeat(buffer.length)` (nunca el valor); `setBuffer('')`+`setMaskValue(false)` al guardar/cancelar; valor solo a `onSaveApiKey`→`writeEnvVar`. Acceptance: test app-setup.test.js asserta que el valor NUNCA aparece en `lastFrame()` (held-out); el grep de higiene de Phase 67 (plan 68-03) re-verifica los 5 sinks. |
| T-68-05 | Tampering | valor de config/base_url/slug → config.json | medium | mitigate | Escritura solo vía `saveConfig` (atómico, no-corruptivo); deep-clone antes de mutar (`structuredClone`). Acceptance: test app-setup.test.js verifica el payload de onSaveConfig. |
| T-68-06 | Denial of Service | render non-TTY / handlers async | medium | mitigate | Degradación `SETUP_NO_RAWMODE` never-throws (isRawModeSupported); guard non-TTY de index.js intacto (D-13). Acceptance: caso (f) de app-setup.test.js. |
| T-68-07 | Denial of Service | text-input (ReDoS) | low | mitigate | Substring puro (`String.includes`), nunca compila regex desde input (invariante del dashboard). Acceptance: source assertion (sin `new RegExp` sobre buffer). |

Nota de supply-chain: sin `npm/pip/cargo install` en la fase (invariante LOCKED). No aplica `T-68-SC`.
</threat_model>

<verification>
- `node --test test/dashboard/app-setup.test.js test/dashboard/app-dismiss.test.js test/cli/up.test.js` verde.
- Source assertions: 16 `SETUP_*` exportadas; `renderSetupOverlay` con `'•'.repeat`; guard non-TTY de index.js sin cambios (D-13); `SETUP_COMPLETE_RESTART` con el literal de D-08.
- Held-out de seguridad: el valor de la key nunca aparece en `lastFrame()` (T-68-04).
</verification>

<success_criteria>
- El dashboard entra en modo setup en first-run (SETUP-01, render) y edita provider/base_url/workspace_slug → config.json + aviso de reinicio honesto (SETUP-02).
- La API key se enmascara (`•`) y persiste vía onSaveApiKey→writeEnvVar; el valor nunca se filtra (PERSIST-04).
- Degradación non-TTY honesta (D-13); paleta/molde reusados sin reinvención.
</success_criteria>

<output>
Create `.planning/phases/68-dashboard-setup-mode-cfgf-03-first-run/68-02-SUMMARY.md` when done
</output>
