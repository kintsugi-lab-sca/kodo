---
phase: 62-adopci-n-inteligente-desde-el-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/adopt.js
  - test/dashboard/adopt.test.js
autonomous: true
requirements: [ORCH-02]

must_haves:
  truths:
    - "runAdopt inserta el par --description <d> en el argv cuando description es string no vacío"
    - "runAdopt omite --description cuando es undefined o cadena vacía (core cae al cuerpo por defecto)"
    - "--description se inserta como par literal tras --title y antes de --json"
    - "una description con metacaracteres viaja como un solo arg literal (injection-inerte, execFile sin shell)"
  artifacts:
    - path: "src/cli/dashboard/adopt.js"
      provides: "runAdopt con --description simétrico a --title (espejo argv literal)"
      contains: "--description"
    - path: "test/dashboard/adopt.test.js"
      provides: "tests del par --description (presente/omitido/vacío/injection-inerte)"
      contains: "--description"
  key_links:
    - from: "src/cli/dashboard/adopt.js (runAdopt)"
      to: "kodo adopt --description (CLI ya registrado src/cli.js:250)"
      via: "argv literal — el flag downstream YA está enhebrado a adoptSession→createTask"
      pattern: "'--description'"
---

<objective>
Extender `runAdopt` (`src/cli/dashboard/adopt.js`) para insertar el par `--description <d>` en el argv, espejo EXACTO del par `--title` existente (adopt.js:123). Implementa el carril shell de ORCH-02 SC#4 (D-10/D-13).

Purpose: El flag `kodo adopt --description` YA existe y está enhebrado downstream (cli.js:250 → cli/adopt.js:174 → adoptSession → createTask, RESEARCH A1 VERIFIED). El ÚNICO trabajo es que `runAdopt` lo inserte en el argv — la descripción derivada por Haiku (Plan 01) viaja como cuerpo at-adopt, NO como comentario post-hoc (D-10).
Output: `runAdopt` con `--description` + tests unit (presente/omitido/vacío/injection-inerte).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-CONTEXT.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-RESEARCH.md

<interfaces>
<!-- Estado actual verificado (2026-06-25). El executor extiende esto. -->

src/cli/dashboard/adopt.js:91 — firma actual de runAdopt:
  export function runAdopt({ exec, execPath, kodoBin, workspaceRef, cwd, sessionId, projectId, title, timeoutMs = 5_000 })
  // → añadir `description` al destructuring + @param JSDoc espejo del de title (líneas 85-87)

src/cli/dashboard/adopt.js:123 — bloque --title actual (el par a espejar):
  ...(typeof title === 'string' && title.length > 0 ? ['--title', title] : []),
  // El nuevo par --description va INMEDIATAMENTE tras este, ANTES de '--json' (último elemento).

Cadena downstream YA verificada (NO tocar — RESEARCH A1):
  src/cli.js:250        .option('--description <d>', 'Task description (optional)')  [VERIFIED]
  src/cli/adopt.js:174  description: opts.description → adoptSession                  [VERIFIED]
  adoptSession          omite si undefined; la enhebra a createTask                  [VERIFIED]

Test harness existente (test/dashboard/adopt.test.js:45-152):
  const exec = (cmd, args, opts, cb) => { captured = { args }; setImmediate(() => cb(null, '', '')); };
  // base = { execPath: EXEC_PATH, kodoBin: KODO_BIN, workspaceRef: WORKSPACE_REF, cwd: CWD, sessionId: SESSION_ID, projectId: PROJECT_ID }
  // Los 4 tests de --title (líneas 83-151) son el molde EXACTO a clonar para --description.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — extender test/dashboard/adopt.test.js con tests de --description (RED)</name>
  <files>test/dashboard/adopt.test.js</files>
  <read_first>
    - test/dashboard/adopt.test.js (líneas 45-60: fakeExec + base; 83-113: title presente; 115-127: title injection-inerte; 129-139: title ausente; 141-151: title vacío — clonar EXACTO con --description)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-VALIDATION.md (filas ORCH-02 de --description + injection-inerte)
  </read_first>
  <behavior>
    - description no vacía → argv incluye `['--description', desc]` insertado tras `--title` y antes de `--json`
    - description + title presentes → orden argv: ...--project, PROJECT_ID, --title, T, --description, D, --json
    - description ausente (sin pasar) → ningún `--description` en el argv
    - description vacía `''` → omitido (no fuerza descripción vacía)
    - description con metacaracteres (p.ej. `'$(rm -rf /); `backtick`'`) → un solo arg literal tras `--description` (injection-inerte, execFile sin shell, D-13)
  </behavior>
  <action>
    Clonar los 4 tests de `--title` (adopt.test.js:83-151) sustituyendo `title`→`description` y `--title`→`--description`, dentro del mismo `describe` (o uno nuevo `describe('Phase 62 (ORCH-02): runAdopt --description')`). Reusar el `base` y el patrón de `exec` capturador existentes. Añadir un quinto test que pase title Y description juntos y asserte el ORDEN exacto del argv (`--title` antes de `--description`, `--json` último). El test de injection-inerte usa una description con `$()`/backtick/`;` y asserta que `captured.args[i+1]` (donde `i = indexOf('--description')`) es la cadena literal completa (un solo elemento del array). Falla en RED (description aún no en la firma de runAdopt).
  </action>
  <verify>
    <automated>node --test test/dashboard/adopt.test.js 2>&1 | grep -qi "description" && echo "tests presentes" ; node --test test/dashboard/adopt.test.js 2>&1 | grep -q "fail" && echo "RED OK" || echo "verificar RED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'--description'\|--description" test/dashboard/adopt.test.js` ≥ 5 (los 5 comportamientos)
    - Los nuevos tests cubren: presente, orden con title, ausente, vacío, injection-inerte (1:1 con VALIDATION.md)
    - En RED los tests de description fallan (firma de runAdopt aún sin `description`)
  </acceptance_criteria>
  <done>Los tests de --description existen y fallan en RED por la firma incompleta de runAdopt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runAdopt inserta el par --description (GREEN)</name>
  <files>src/cli/dashboard/adopt.js</files>
  <read_first>
    - src/cli/dashboard/adopt.js:80-130 (firma runAdopt + JSDoc de title + bloque --title + posición de --json)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md (§adopt.js Cambio 1 + Cambio 2 — excerpts exactos)
  </read_first>
  <action>
    En `src/cli/dashboard/adopt.js`:
    (Cambio 1) Añadir `description` al destructuring de la firma de `runAdopt` (línea 91): `runAdopt({ exec, execPath, kodoBin, workspaceRef, cwd, sessionId, projectId, title, description, timeoutMs = 5_000 })`.
    (Cambio 2) Añadir un `@param {string} [args.description]` al JSDoc, espejo del de `title` (líneas 85-87), explicando: cuando es string no vacío se inserta como `--description <description>` (literal argv, injection-safe sin shell); cuando es absent/empty se OMITE y el core deja el cuerpo at-adopt por defecto.
    (Cambio 3) Insertar el par en el argv INMEDIATAMENTE tras el bloque `--title` (línea 123) y ANTES de `'--json'`: `...(typeof description === 'string' && description.length > 0 ? ['--description', description] : []),`. NO tocar el resto del argv (el `--json` final, el leak guard del `exec`, el discriminante de resultado — todo intacto).
    NO modificar nada downstream (cli.js / cli/adopt.js / adoptSession — RESEARCH A1: ya enhebrado).
  </action>
  <verify>
    <automated>node --test test/dashboard/adopt.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard/adopt.test.js` pasa (los 5 tests de description + los de title preexistentes)
    - `grep -c "'--description'" src/cli/dashboard/adopt.js` ≥ 1
    - El par --description está entre el bloque --title y `'--json'` (verificable: en los tests, el argv capturado pone `--description` tras `--title` y `--json` último)
    - `npm test` verde (cero regresión)
    - La firma de runAdopt incluye `description` (no rompe el wiring existente de index.js, que se actualiza en Plan 03)
  </acceptance_criteria>
  <done>runAdopt inserta --description como par literal espejo de --title; suite verde; downstream intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard (runAdopt) → CLI `kodo adopt` | argv literal cruza vía execFile (sin shell) hacia el proceso hijo `node bin/kodo adopt` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Tampering | inyección shell vía `description` | mitigate | `--description <d>` se inserta como par argv LITERAL (cada valor precedido de su flag explícito, T-56-01); `runAdopt` usa `execFile` con argv literal (NO shell) → la description es un único argumento, metacaracteres inertes (D-13). T-57-01 NO aplica. Cubierto por el test de injection-inerte. |
| T-62-08 | Information Disclosure | leak de rutas/home en `description` hacia el provider | mitigate | El cuerpo viaja vía `--description` a `adoptSession`, donde `sanitizeAdoptionData` (BIDIR-08) redacta home/rutas ANTES del POST (D-12). runAdopt no re-sanea (backstop estructural downstream). |
| T-62-SC | Tampering | npm installs | mitigate | N/A — cero dependencias npm nuevas (solo edición de un archivo existente + tests builtin). RESEARCH §Package Legitimacy Audit: gate N/A. |
</threat_model>

<verification>
- `node --test test/dashboard/adopt.test.js` verde (title preexistente + description nuevo)
- `npm test` verde
- El par `--description` es literal, tras `--title`, antes de `--json`
</verification>

<success_criteria>
- ORCH-02 SC#4 (shellea `kodo adopt --title '<t>' --description '<d>'` con argv literal injection-inerte) — carril shell cubierto
- `sanitizeAdoptionData` (BIDIR-08) sigue aplicando aguas abajo sin cambios (D-12)
- Cadena downstream intacta (cero cambios en cli.js / cli/adopt.js / adoptSession)
</success_criteria>

<output>
Create `.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-02-SUMMARY.md` when done
</output>
