---
phase: 62-adopci-n-inteligente-desde-el-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dashboard/enrich.js
  - test/dashboard/enrich.test.js
  - test/dashboard/fixtures/transcript-variable-line.jsonl
  - test/dashboard/fixtures/transcript-tool-result-only.jsonl
autonomous: true
requirements: [ORCH-02]

must_haves:
  truths:
    - "Un envelope feliz de claude -p (con --json-schema) se parsea a {title, description}"
    - "Cualquier fallo (ENOENT, timeout, exit≠0, is_error, parse-fail, sync-throw) devuelve {} sin lanzar"
    - "Un proyecto GSD lee PROJECT.md+ROADMAP.md+STATE.md capeados; un non-GSD usa git log + primer prompt del transcript"
    - "El primer prompt del transcript se localiza vía resolveTranscriptPath y salta líneas no-user y tool_result-only"
    - "spawnFn omitido produce TypeError visible (leak guard), nunca toca el claude real"
  artifacts:
    - path: "src/cli/dashboard/enrich.js"
      provides: "deriveAdoptionMeta + spawnDerive + firstUserPrompt (derivador LLM never-throws, DI)"
      exports: ["deriveAdoptionMeta", "spawnDerive", "firstUserPrompt"]
      min_lines: 90
    - path: "test/dashboard/enrich.test.js"
      provides: "cobertura unit de parse/fail-open/ramas/transcript con fakes (NO invoca claude real)"
      contains: "deriveAdoptionMeta"
  key_links:
    - from: "src/cli/dashboard/enrich.js"
      to: "src/logger-events.js (resolveTranscriptPath)"
      via: "import — NO reinventar el path del .jsonl"
      pattern: "resolveTranscriptPath"
    - from: "src/cli/dashboard/enrich.js"
      to: "src/adopt.js (isGsdProject)"
      via: "import — rama D-04 vs D-05, NO existsSync ad-hoc"
      pattern: "isGsdProject"
---

<objective>
Crear `src/cli/dashboard/enrich.js`: el derivador LLM one-shot never-throws que produce `{title, description}` a partir de la memoria del proyecto (GSD: PROJECT.md/ROADMAP.md/STATE.md; non-GSD: git log + primer prompt del transcript), spawneando `claude -p --model claude-haiku-4-5 --output-format json --json-schema <SCHEMA>` con fail-open a `{}` ante cualquier fallo. Implementa el núcleo de ORCH-02 (D-01, D-02, D-03, D-04, D-05, D-06, D-07).

Purpose: Aísla todo el carril LLM en un único módulo DI-inyectable (preserva el suelo 0-token del core, D-11/D-14). Es el ÚNICO código genuinamente nuevo de la fase — el resto es wiring.
Output: `enrich.js` + suite unit con fakes (`spawnFn`/`readFileFn`/`existsSyncFn`) que NO invoca `claude` real + fixtures `.jsonl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-CONTEXT.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-RESEARCH.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-VALIDATION.md

<interfaces>
<!-- Contratos a reusar (extraídos del codebase, verificados 2026-06-25). El executor los IMPORTA, no los reimplementa. -->

From src/logger-events.js:109 (REUSAR, NO reinventar el path — D-06):
  export function resolveTranscriptPath(projectPath: string, sessionId: string): string
  // → ~/.claude/projects/<encodeURIComponent(projectPath).replace(/%2F/g,'-')>/<sessionId>.jsonl

From src/adopt.js:38 (REUSAR para la rama GSD/non-GSD — D-04/D-05):
  export function isGsdProject(projectPath: string, existsSyncFn = existsSync): boolean
  // → true si existe .planning/PROJECT.md o .planning/STATE.md; never-throws

Patrón leak guard estructural (espejo EXACTO de runAdopt, src/cli/dashboard/adopt.js:95-100):
  if (typeof spawnFn !== 'function') {
    throw new TypeError('...: `spawnFn` is required (no default — leak guard).');
  }
  // Va ANTES del `new Promise` para que el TypeError propague síncronamente.

Patrón never-throws / fail-open (runAdopt, adopt.js:101-170):
  return new Promise((resolve) => { try { ...; resolve(OUT) } catch { resolve(FALLBACK) } });
  // JAMÁS reject. Aquí FALLBACK = {} (no {ok:false,code} — solo importa "hay/no hay derivación").

Envelope real de `claude -p --output-format json` (VERIFIED probe 2026-06-25):
  { type:'result', subtype:'success', is_error:false, result:<string>, session_id, total_cost_usd, ... }
  // Con --json-schema, `result` = '{"title":"...","description":"..."}' (JSON estricto, SIN fence markdown).

Patrón de fakes en tests (DI shape execFile, test/dashboard/adopt.test.js:51-54):
  const exec = (cmd, args, opts, cb) => { setImmediate(() => cb(err, stdout, stderr)); };
From test/adopt.test.js:437 (fakes de fs):
  existsSyncFn: (p) => p.endsWith('.planning/PROJECT.md');  readFileFn: () => '...' | () => { throw }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test/dashboard/enrich.test.js + fixtures .jsonl (RED)</name>
  <files>test/dashboard/enrich.test.js, test/dashboard/fixtures/transcript-variable-line.jsonl, test/dashboard/fixtures/transcript-tool-result-only.jsonl</files>
  <read_first>
    - test/dashboard/adopt.test.js (fakeExec `(cmd,args,opts,cb)` líneas 45-60, 83-152; leak-guard test 236-243; sync-throw test 214-234 — molde never-throws)
    - test/adopt.test.js (líneas ~437: `existsSyncFn`/`readFileFn` fakes)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-VALIDATION.md (Per-Task Verification Map — los 11 escenarios obligatorios de enrich)
    - src/logger-events.js:109 (forma exacta del path que el fixture debe satisfacer)
  </read_first>
  <behavior>
    - spawnDerive: envelope feliz (`{type:'result', is_error:false, result:'{"title":"X scope","description":"Y"}'}`) → `{title:'X scope', description:'Y'}`
    - spawnDerive: `is_error:true` → `{}`
    - spawnDerive: stdout no-JSON → `{}`; `result` no-JSON (`result:'not json'`) → `{}`
    - spawnDerive: cb con `{code:'ENOENT'}` → `{}` (claude ausente en PATH)
    - spawnDerive: cb con `{killed:true}` (timeout) → `{}`
    - spawnDerive: spawnFn sync-throw → `{}` (assert NO reject — molde adopt.test.js:214-234)
    - spawnDerive: omitir spawnFn → assert.rejects TypeError (leak guard, molde adopt.test.js:236-243)
    - spawnDerive: title/description con metacaracteres en el prompt → un solo arg literal en argv (injection-inerte D-13; capturar args como adopt.test.js)
    - firstUserPrompt: primer `type:'user'` en línea VARIABLE (≥3), salta `queue-operation` y líneas no-parseables
    - firstUserPrompt: content array tool_result-only → saltado; content string vs array → ambos extraen texto; cap a 1500 chars
    - firstUserPrompt: transcript ausente (readFileFn throw ENOENT) → `''`
    - deriveAdoptionMeta: rama GSD (`existsSyncFn`=true) → readFileFn invocado con paths .planning/{PROJECT,ROADMAP,STATE}.md (assert por captura)
    - deriveAdoptionMeta: rama non-GSD (`existsSyncFn`=false) → usa git log (spawnFn 'git') + firstUserPrompt
  </behavior>
  <action>
    Crear `test/dashboard/enrich.test.js` con `import { describe, it } from 'node:test'` + `assert from 'node:assert/strict'`, importando `{ deriveAdoptionMeta, spawnDerive, firstUserPrompt }` de `../../src/cli/dashboard/enrich.js`. Fakes: `okExec` que produce el envelope feliz vía `setImmediate(() => cb(null, envelope, ''))`; variantes de fallo (ENOENT, killed, sync-throw, is_error, bad-json). Para las ramas GSD/non-GSD inyectar `existsSyncFn` y `readFileFn` capturadores (espejo test/adopt.test.js:437). Crear DOS fixtures en `test/dashboard/fixtures/`: `transcript-variable-line.jsonl` (primer `user` en línea ≥3, con una línea `{"type":"queue-operation"}` intercalada y content string) y `transcript-tool-result-only.jsonl` (un turno `user` con content array tool_result-only seguido de un turno `user` con texto real). El test que lee fixtures usa el `readFileFn` real (`readFileSync`) contra esos paths — el resto inyecta fakes. NO invocar `claude` real en NINGÚN test (todo vía spawnFn fake). Cubrir los 13 comportamientos del bloque `<behavior>` (mapean 1:1 a las filas ORCH-02 de VALIDATION.md). Tests fallan en RED (enrich.js aún no existe).
  </action>
  <verify>
    <automated>node --test test/dashboard/enrich.test.js 2>&1 | grep -qE "Cannot find module|fail" && echo "RED OK (enrich.js no existe aún)" || echo "ERROR: debería fallar en RED"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard/enrich.test.js` existe y cubre los 13 comportamientos del bloque `<behavior>` (1:1 con las filas ORCH-02 de VALIDATION.md §Per-Task Verification Map)
    - Existen `test/dashboard/fixtures/transcript-variable-line.jsonl` y `transcript-tool-result-only.jsonl`
    - El test usa fakes DI (`spawnFn`/`readFileFn`/`existsSyncFn`); `grep -c "import.*child_process" test/dashboard/enrich.test.js` retorna 0 (cero spawn real de claude)
    - En RED el comando falla porque `enrich.js` no existe aún
  </acceptance_criteria>
  <done>El test y los fixtures existen; la suite de enrich falla en RED por ausencia del módulo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: enrich.js — spawnDerive + firstUserPrompt + deriveAdoptionMeta (GREEN)</name>
  <files>src/cli/dashboard/enrich.js</files>
  <read_first>
    - src/cli/dashboard/adopt.js:91-170 (molde never-throws + leak guard estructural a copiar EXACTO)
    - src/logger-events.js:109 (resolveTranscriptPath — importar, NO reinventar)
    - src/adopt.js:38 (isGsdProject — importar)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md (§enrich.js: SCHEMA, spawnDerive, firstUserPrompt, gsdContext, buildDerivePrompt — todos los excerpts)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-RESEARCH.md (Pitfall 1 timeout, Pitfall 4 caps, Code Examples)
    - test/format-isolation.test.js (escanea src/cli/dashboard/** — cero picocolors/ANSI inline)
  </read_first>
  <action>
    Crear `src/cli/dashboard/enrich.js` con `// @ts-check`. Imports SOLO `node:path` (`join`) + `resolveTranscriptPath` de `../../logger-events.js` + `isGsdProject` de `../../adopt.js`. CERO import de `node:child_process` (se inyecta por DI — leak guard), CERO `picocolors`/ANSI (color-isolation D-12, verificado por test/format-isolation.test.js).

    Exportar `SCHEMA` (string `JSON.stringify` del schema `{type:'object', properties:{title:{type:'string'},description:{type:'string'}}, required:['title','description'], additionalProperties:false}`).

    Exportar `spawnDerive({ spawnFn, prompt, timeoutMs = 25_000 })`: leak guard estructural (`if (typeof spawnFn !== 'function') throw new TypeError(...)`) ANTES del `new Promise`. Dentro: `argv = ['-p','--model','claude-haiku-4-5','--output-format','json','--json-schema', SCHEMA, prompt]`; resolver `'claude'` por PATH (NO `config.claude.binary` — Pitfall 3, apunta a binario inexistente); `spawnFn('claude', argv, { timeout: timeoutMs }, (err, stdout) => ...)`. Parse doble capa: si `err` → `resolve({})`; `JSON.parse(stdout)` → si `is_error` o `typeof result !== 'string'` → `{}`; `JSON.parse(env.result)` → extraer `title`/`description` solo si `typeof === 'string'` y `.trim()` no vacío. Cualquier throw (parse o spawn sync) → `resolve({})`. Timeout default 25_000 (NO 8s — Pitfall 1, evidencia empírica 8.7-21.9s).

    Exportar `firstUserPrompt({ cwd, sessionId, readFileFn })`: `readFileFn(resolveTranscriptPath(cwd, sessionId), 'utf8')` (ENOENT → catch → `''`); iterar líneas, `JSON.parse` por línea con try/catch (saltar `queue-operation` y no-parseables); primer `o.type==='user'` con texto real; content string o array (filtrar bloques tool_result-only); cap `.slice(0, 1500)`.

    Helpers internos (NO exportados): `readCapped(readFileFn, path, cap)` never-throws; `gsdContext({ cwd, readFileFn })` lee PROJECT.md≤3000, ROADMAP.md≤2000, STATE.md≤2000 (caps Pitfall 4) join'd; `gitLog({ cwd, spawnFn })` con `spawnFn('git', ['-C', cwd, 'log', '--oneline', '-20'], {timeout:3000}, ...)` never-throws → `''` on err, cap 2000; `buildDerivePrompt({ contextLabel, contextBody })` — prompt NUEVO mínimo en inglés (NO copiar la prosa de kodo-orchestrate/skill.md, D-07): pide derivar `{title, description}` que reflejen el ALCANCE del proyecto, NO el directorio NI el último commit; SIN mandato charset/single-quote (shell-safety por execFile argv, D-13).

    Exportar `deriveAdoptionMeta({ spawnFn, readFileFn, existsSyncFn, cwd, sessionId })` (never-throws, fail-open a `{}`): ramificar por `isGsdProject(cwd, existsSyncFn)` (D-04 GSD vs D-05 non-GSD); construir `contextBody` (gsdContext o gitLog+firstUserPrompt); `prompt = buildDerivePrompt(...)`; `return spawnDerive({ spawnFn, prompt, timeoutMs })`. Si la rama es non-GSD, await `gitLog` antes de construir el prompt. Cualquier excepción → `{}`.
  </action>
  <verify>
    <automated>node --test test/dashboard/enrich.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard/enrich.test.js` pasa (todos los comportamientos GREEN)
    - `grep -v '^#' src/cli/dashboard/enrich.js | grep -cE "require\\(.node:child_process|from .node:child_process" ` retorna 0 (DI, no import directo del spawn)
    - `grep -v '^#' src/cli/dashboard/enrich.js | grep -cE "picocolors|\\\\x1b\\[" ` retorna 0 (color-isolation D-12)
    - `grep -c "claude-haiku-4-5" src/cli/dashboard/enrich.js` ≥ 1 y `grep -c "json-schema" src/cli/dashboard/enrich.js` ≥ 1
    - `grep -c "25_000\|25000" src/cli/dashboard/enrich.js` ≥ 1 (timeout ~25s, NO 8s — Pitfall 1)
    - `grep -c "resolveTranscriptPath\|isGsdProject" src/cli/dashboard/enrich.js` ≥ 2 (assets reusados, no reinventados)
    - `npm test` sigue verde (cero regresión)
  </acceptance_criteria>
  <done>enrich.js exporta deriveAdoptionMeta/spawnDerive/firstUserPrompt; la suite enrich pasa; cero color/child_process inline; timeout 25s; assets reusados por import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (tecla) → subproceso `claude -p` | El prompt inline (memoria del proyecto + transcript) cruza al subproceso LLM local |
| contenido del transcript (primer prompt del usuario) → prompt de derivación | Texto no confiable inyectado como contexto de summarización |
| salida del LLM `{title, description}` → tarea creada (Plane/GitHub) | El output del modelo cruza hacia el task tracker externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | `spawnDerive` argv (prompt/cwd) | mitigate | `spawnFn` es execFile-shaped con argv LITERAL (sin shell). El prompt va como un único arg; metacaracteres inertes (D-13). T-57-01 NO aplica a este carril. |
| T-62-02 | Information Disclosure | prompt inline → subproceso | accept | El subproceso `claude -p` NO lleva tools (sin Read/Bash, D-02); contexto pre-leído y CAPEADO (≤8000 chars). El transcript se inyecta SOLO en el prompt local de Haiku, nunca se persiste ni se reenvía al task tracker. |
| T-62-03 | Tampering | output LLM hostil (prompt-injection vía transcript) | accept | El output se usa SOLO como texto (`title`/`description`); el subproceso no ejecuta nada (sin tools). Peor caso: un título raro — saneado aguas abajo por `sanitizeAdoptionData` (BIDIR-08, fuera de este plan, en adoptSession). |
| T-62-04 | Information Disclosure | leak de home/rutas en el output | mitigate | El `{title, description}` derivado pasa por `sanitizeAdoptionData` (BIDIR-08) DENTRO de `adoptSession` aguas abajo (D-12). enrich.js NO re-sanea (sin saneo nuevo). |
| T-62-05 | DoS | derivación colgada | mitigate | `timeout` (~25s) en el `spawnFn` + fail-open a `{}`. enrich never-throws; el panel ink (Plan 03) nunca se bloquea. |
| T-62-06 | Tampering | binario `claude` resuelto por path inexistente | mitigate | Resolver `'claude'` por PATH (NO `config.claude.binary` que apunta a binario inexistente, Pitfall 3). ENOENT → fail-open a `{}`. |
| T-62-SC | Tampering | npm installs | mitigate | N/A — cero dependencias npm nuevas (solo `node:*` builtins + CLI `claude` preexistente, D-15). RESEARCH §Package Legitimacy Audit: gate N/A. Sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `node --test test/dashboard/enrich.test.js` verde (cobertura completa de parse/fail-open/ramas/transcript)
- `npm test` verde (cero regresión en la suite global)
- enrich.js: cero `child_process`/`picocolors`/ANSI inline; timeout 25s; assets reusados por import
</verification>

<success_criteria>
- ORCH-02 SC#2 (fuentes de memoria GSD/non-GSD correctas) cubierto por las ramas de `deriveAdoptionMeta`
- ORCH-02 SC#3 (fail-open: timeout/parse-error/claude ausente → {}; never-throws) cubierto por `spawnDerive`
- Suelo determinista 0-token del core intacto (el LLM vive SOLO en enrich.js, D-11)
- Cero dependencias npm nuevas
</success_criteria>

<output>
Create `.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-01-SUMMARY.md` when done
</output>
