---
phase: 62-adopci-n-inteligente-desde-el-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["62-01", "62-02"]
files_modified:
  - src/cli/dashboard/App.js
  - src/cli/dashboard/SessionTable.js
  - src/cli/dashboard/index.js
  - test/dashboard/app-derive.test.js
autonomous: true
requirements: [ORCH-02]

must_haves:
  truths:
    - "Al armar el adopt (primer `a` con projectId resuelto), App entra en mode 'deriving' y espera onDerive antes del confirm"
    - "onDerive never-throws resuelve {title?,description?}: el resultado se fusiona en armedSurface; fail-open a {} conserva surface.title"
    - "El confirm muestra el {title, description} propuestos (o el prompt degradado cuando no hay derivación)"
    - "Esc en 'deriving' cancela e invalida la derivación en vuelo (token de generación); el resultado tardío no reabre el confirm"
    - "index.js cablea onDerive = deriveAdoptionMeta(...) con execFile real + fs DI, y onAdopt pasa description a runAdopt"
  artifacts:
    - path: "src/cli/dashboard/App.js"
      provides: "estado 'deriving' + await onDerive + fusión en armedSurface + constantes copy español exportadas"
      contains: "deriving"
    - path: "src/cli/dashboard/index.js"
      provides: "wiring onDerive (deriveAdoptionMeta) + onAdopt con description"
      contains: "onDerive"
    - path: "test/dashboard/app-derive.test.js"
      provides: "tests del estado deriving + fusión + fail-open + Esc-invalida + render confirm"
      contains: "deriving"
  key_links:
    - from: "src/cli/dashboard/index.js (onDerive)"
      to: "src/cli/dashboard/enrich.js (deriveAdoptionMeta)"
      via: "wiring DI never-throws — execFile real + readFileSync/existsSync"
      pattern: "deriveAdoptionMeta"
    - from: "src/cli/dashboard/App.js (handler `a` overlay)"
      to: "onDerive prop"
      via: "await onDerive entre setMode('deriving') y setMode('confirm')"
      pattern: "onDerive"
    - from: "src/cli/dashboard/index.js (onAdopt)"
      to: "src/cli/dashboard/adopt.js (runAdopt description)"
      via: "pasa description al runAdopt extendido (Plan 02)"
      pattern: "description"
---

<objective>
Integrar el derivador (Plan 01) y el `--description` (Plan 02) en el flujo de la tecla `a` del dashboard ink: añadir el estado `mode==='deriving'` entre el armado (primer `a`) y el confirm, esperar `onDerive` (fail-open a `{}`), fusionar `{title, description}` en `armedSurface`, mostrar la propuesta en el confirm, y cablear `onDerive`/`onAdopt(description)` en `index.js`. Implementa la UX derive-then-confirm de ORCH-02 SC#1 (D-08, D-09) y conecta SC#4.

Purpose: Cierra el lazo end-to-end. El dashboard YA tiene las coordenadas (`workspaceRef`/`cwd`/`sessionId`) vía `listAgentSurfaces()` — por eso la derivación vive aquí y no en el orquestador (disuelve el fallo de UAT de ORCH-01).
Output: App.js (estado deriving + constantes copy español) + SessionTable.js (render del confirm con propuesta + precedencia de footer) + index.js (wiring) + tests de integración ink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-CONTEXT.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-UI-SPEC.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-RESEARCH.md
@.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-VALIDATION.md

<interfaces>
<!-- Puntos de inserción verificados (2026-06-25). El executor extiende el código existente. -->

src/cli/dashboard/App.js:339 — unión mode actual (añadir 'deriving'):
  const [mode, setMode] = useState(/** @type {'list'|'filter'|'overlay'|'confirm'} */ ('list'));

src/cli/dashboard/App.js:347 — overlayReqRef (token de generación a reusar para T5/Esc):
  const overlayReqRef = useRef(0);

src/cli/dashboard/App.js:512-549 — handler `a` en overlay (PUNTO DE INSERCIÓN):
  // tras resolveProjectId OK (línea ~524) y setArmedSurface, ANTES de setMode('confirm') (~548):
  // → setMode('deriving') → const reqId=++overlayReqRef.current → await onDerive?.({cwd,sessionId}) ?? {}
  // → if (overlayReqRef.current !== reqId) return → fusionar en setArmedSurface → setMode('confirm')
  // El handler useInput YA es async y usa `await onAdopt` (línea 591) → await onDerive es legal.

src/cli/dashboard/App.js:580-623 — handler confirm (segunda `a` → onAdopt(armedSurface); Esc/otra cancela).
  // armedSurface ya viaja COMPLETO a onAdopt → solo asegurar que lleva description (Cambio 2).

src/cli/dashboard/App.js:182-197 — constantes ADOPT_* exportadas (molde para las nuevas, en español).

src/cli/dashboard/SessionTable.js:332-387 — precedencia de footer (extender a derivingLine):
  const filterLine = ...; const errorLine = ...; const confirmLine = ...;
  return h(Box, ..., (confirmLine ?? errorLine ?? filterLine));   // → (derivingLine ?? confirmLine ?? errorLine ?? filterLine)
  // confirmLine usa ADOPT_CONFIRM(armedSurfaceRef) — extender para mostrar título:/desc: cuando hay propuesta.

src/cli/dashboard/index.js:124 — execImpl (execFile real ya resuelto, lazy):
  const execImpl = exec ?? (await import('node:child_process')).execFile;
src/cli/dashboard/index.js:175-176 — onAdopt actual (añadir description):
  onAdopt: async ({ workspaceRef, cwd, sessionId, projectId, title }) =>
    runAdopt({ exec: execImpl, execPath: process.execPath, kodoBin, workspaceRef, cwd, sessionId, projectId, title }),

From src/cli/dashboard/enrich.js (Plan 01 — wiring target):
  export function deriveAdoptionMeta({ spawnFn, readFileFn, existsSyncFn, cwd, sessionId }): Promise<{title?,description?}>

Copy literal-estable OBLIGATORIO (UI-SPEC §Copywriting, español, ellipsis `…` un char):
  DERIVE_PROGRESS = 'derivando título…'                                              (dimColor, estado deriving)
  ADOPT_DERIVED_CONFIRM(ref) = `adoptar ${ref}? pulsa a de nuevo · Esc cancela`      (cyan, confirm con propuesta)
  ADOPT_DERIVED_CONFIRM_FALLBACK(ref) = `adoptar ${ref} (título por defecto)? pulsa a de nuevo · Esc cancela`  (cyan, confirm degradado)
  prefijo título propuesto = 'título: '   (normal/bold)
  prefijo descripción propuesta = 'desc: ' (dimColor)

Test harness ink (test/dashboard/app-adopt.test.js): injectProps + fake clock + drain() 80ms (load-bearing).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test/dashboard/app-derive.test.js (RED)</name>
  <files>test/dashboard/app-derive.test.js</files>
  <read_first>
    - test/dashboard/app-adopt.test.js (harness completo: injectProps, makeFakeClock, drain() 80ms, escRe; contratos a-h del flujo adopt — molde EXACTO)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-UI-SPEC.md (§Máquina de estados T1-T7; §Keybindings; §Copywriting — las 5 constantes)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-VALIDATION.md (filas App.js: estado deriving, fusión, Esc, sanitize)
  </read_first>
  <behavior>
    - T1/T3: `a` sobre surface con projectId resuelto → frame con DERIVE_PROGRESS ('derivando título…') → tras drain, onDerive resuelve {title,description} → confirm muestra la propuesta (título:/desc:)
    - T4 (fail-open): onDerive resuelve {} → confirm en modo degradado (ADOPT_DERIVED_CONFIRM_FALLBACK, sin líneas título:/desc:); NO error rojo
    - fusión: el armedSurface pasado a onAdopt (segundo `a`) lleva title y description derivados (capturar el arg de onAdopt)
    - T5 (Esc en deriving): onDerive en vuelo, Esc → mode vuelve a list; el resultado tardío NO reabre el confirm (token de generación)
    - keybinding: `a` durante deriving → tragada (no encola un segundo onDerive)
    - onDerive never-throws: si onDerive lanzara, el panel ink sigue montado (assert no-crash); pero el contrato es fail-open a {} (no debe lanzar)
    - T2: projectId none/ambiguous → ADOPT_NO_PROJECT (rojo), NO entra en deriving, cero onDerive (regresión Phase 56 preservada)
    - onDerive llamado EXACTAMENTE una vez por armado (no duplicado)
  </behavior>
  <action>
    Crear `test/dashboard/app-derive.test.js` clonando el harness de `app-adopt.test.js` (injectProps con `onDerive` añadido + `onAdopt` capturador + fake clock + `drain()`). Importar de App.js las nuevas constantes `{ DERIVE_PROGRESS, ADOPT_DERIVED_CONFIRM, ADOPT_DERIVED_CONFIRM_FALLBACK }` (assert por equality, no duplicar strings). `onDerive` fake configurable: variante feliz (`async () => ({title:'kodo bidireccional', description:'...'})`), fail-open (`async () => ({})`), y lenta (resuelve tras un await para el caso Esc-en-deriving). Verificar los 8 comportamientos del bloque `<behavior>` usando `lastFrame()` + `escRe` para los frames y capturando el arg de `onAdopt` para la fusión. Para T5 usar una `onDerive` que resuelva en una promesa controlada y enviar Esc entre el primer `a` y la resolución. Falla en RED (App.js aún sin 'deriving'/onDerive/constantes nuevas).
  </action>
  <verify>
    <automated>node --test test/dashboard/app-derive.test.js 2>&1 | grep -qE "Cannot find|is not a function|fail|DERIVE_PROGRESS" && echo "RED OK" || echo "verificar RED"</automated>
  </verify>
  <acceptance_criteria>
    - `test/dashboard/app-derive.test.js` existe y cubre los 8 comportamientos (1:1 con VALIDATION.md filas App.js)
    - Importa `DERIVE_PROGRESS`/`ADOPT_DERIVED_CONFIRM`/`ADOPT_DERIVED_CONFIRM_FALLBACK` de App.js (assert por equality)
    - Usa el harness ink (drain() 80ms load-bearing) — `grep -c "drain" test/dashboard/app-derive.test.js` ≥ 2
    - En RED falla por ausencia de las constantes/estado en App.js
  </acceptance_criteria>
  <done>El test de integración del flujo derive-then-confirm existe y falla en RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App.js — estado 'deriving' + await onDerive + constantes copy español; SessionTable.js — render confirm + precedencia footer (GREEN)</name>
  <files>src/cli/dashboard/App.js, src/cli/dashboard/SessionTable.js</files>
  <read_first>
    - src/cli/dashboard/App.js:182-197 (constantes ADOPT_* — molde de export); :339 (mode); :347 (overlayReqRef); :512-549 (handler `a` overlay — punto de inserción); :580-623 (confirm handler)
    - src/cli/dashboard/SessionTable.js:332-387 (filterLine/errorLine/confirmLine + cadena de precedencia del return; ADOPT_CONFIRM import línea 40)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md (§App.js Cambios 1-5; §SessionTable confirm render)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-UI-SPEC.md (§Spinner; §Confirm render; §Color — cyan reservado al prompt, spinner dimColor; §Máquina de estados T1-T7)
    - test/format-isolation.test.js (cero picocolors/ANSI inline en src/cli/dashboard/**)
  </read_first>
  <action>
    En `src/cli/dashboard/App.js`:
    (Cambio 1) Añadir `'deriving'` a la unión de `mode` (línea 339): `'list'|'filter'|'overlay'|'confirm'|'deriving'`.
    (Cambio 2) Exportar las constantes copy NUEVAS junto a las `ADOPT_*` (líneas 182-197), en ESPAÑOL, con ellipsis `…` (un char, NUNCA `...`): `DERIVE_PROGRESS = 'derivando título…'`; `ADOPT_DERIVED_CONFIRM(ref) = `adoptar ${ref}? pulsa a de nuevo · Esc cancela``; `ADOPT_DERIVED_CONFIRM_FALLBACK(ref) = `adoptar ${ref} (título por defecto)? pulsa a de nuevo · Esc cancela``. (Las `ADOPT_*` de Phase 56 quedan en inglés — mezcla consciente aceptada por UI-SPEC.)
    (Cambio 3) Punto de inserción del derive en el handler `a` del overlay (entre la resolución de projectId OK ~524 y `setMode('confirm')` ~548): tras `setArmedSessionId`/`setArmedSurface`, hacer `setOverlayKind(null)` → `setMode('deriving')` → `const reqId = ++overlayReqRef.current` → `const derived = await onDerive?.({ cwd: surface.cwd, sessionId: surface.sessionId }) ?? {}` → `if (overlayReqRef.current !== reqId) return` (T5: derivación obsoleta tras Esc) → `setArmedSurface({ workspaceRef, cwd, sessionId, projectId: r.projectId, title: derived.title ?? surface.title, description: derived.description })` (T4 fail-open conserva surface.title) → `setMode('confirm')` → return. El `await onDerive` es legal (handler ya async); never-throws (fail-open a {}).
    (Cambio 4) Keybindings: `a` mientras `mode==='deriving'` → tragada (return sin efecto); `Esc` en `deriving` → `overlayReqRef.current++` + `setMode('list')` (T5). El confirm (segunda `a`) NO cambia su lógica; solo asegurar que `armedSurface` lleva `description` (ya cubierto por Cambio 3) y que `onAdopt(armedSurface)` la incluye.
    (Cambio 5) NO setear footer al entrar en deriving/confirm desde App.js (el copy se DERIVA en SessionTable por mode/armedSurface — espejo del patrón Phase 56 Pitfall 4).

    En `src/cli/dashboard/SessionTable.js`:
    (Cambio 6) Importar las constantes nuevas de App.js (junto a `ADOPT_CONFIRM`, línea 40).
    (Cambio 7) Añadir `derivingLine`: cuando el prop `mode==='deriving'`, render `<Box marginTop=1><Text dimColor>{DERIVE_PROGRESS}</Text></Box>` (mismo molde que filterLine/errorLine; spinner NEUTRAL dimColor, NO cyan). Pasar `mode` como prop a SessionTable si no llega ya.
    (Cambio 8) Extender `confirmLine` (líneas 357-366) para el caso adopt-con-propuesta: cuando `armedSessionId != null` y hay `armedSurface.title`, render multi-línea: `título: <title truncado…>` (normal/bold) + `desc: <description 1-2 líneas truncadas…>` (dimColor) + `ADOPT_DERIVED_CONFIRM(ref)` (cyan). Cuando `armedSessionId != null` SIN título derivado (fail-open T4), render `ADOPT_DERIVED_CONFIRM_FALLBACK(ref)` (cyan, sin líneas título:/desc:). Truncar con `…` (un char). NO mostrar error rojo en fail-open.
    (Cambio 9) Extender la cadena de precedencia del footer (líneas 378/387): `(derivingLine ?? confirmLine ?? errorLine ?? filterLine)`.
    CERO `picocolors`/ANSI inline en ambos archivos (color-isolation D-12). Color solo por prop `color`/`dimColor`/`bold` de `<Text>`. Timeout del derive lo fija el wiring (Plan 03 Task 3), NO App.js.
  </action>
  <verify>
    <automated>node --test test/dashboard/app-derive.test.js && node --test test/format-isolation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/dashboard/app-derive.test.js` pasa (los 8 comportamientos GREEN)
    - `node --test test/format-isolation.test.js` pasa (cero picocolors/ANSI inline en App.js/SessionTable.js)
    - `grep -c "deriving" src/cli/dashboard/App.js` ≥ 2 (unión mode + handler/keybinding)
    - `grep -c "DERIVE_PROGRESS\|ADOPT_DERIVED_CONFIRM" src/cli/dashboard/App.js` ≥ 3 (las 3 constantes exportadas)
    - `grep -c "derivando título…" src/cli/dashboard/App.js` ≥ 1 (copy literal español con ellipsis `…`)
    - `grep -c "\\.\\.\\." src/cli/dashboard/App.js` no introduce `...` en las constantes nuevas (ellipsis correcto `…`)
    - `grep -c "derivingLine" src/cli/dashboard/SessionTable.js` ≥ 2 (definición + precedencia)
    - `grep -c "overlayReqRef" src/cli/dashboard/App.js` ≥ 1 dentro del flujo deriving (token de generación T5)
    - `npm test` verde (cero regresión en app-adopt/app-dismiss/etc.)
  </acceptance_criteria>
  <done>App.js tiene el estado deriving + await onDerive + constantes español; SessionTable renderiza el spinner y el confirm con propuesta/degradado; precedencia de footer extendida; color-isolation intacta; suite verde.</done>
</task>

<task type="auto">
  <name>Task 3: index.js — wiring onDerive (deriveAdoptionMeta) + onAdopt con description</name>
  <files>src/cli/dashboard/index.js</files>
  <read_first>
    - src/cli/dashboard/index.js:120-176 (execImpl lazy ~124; host ~136; onFocus/onOpen/onAdoptDiscover/onAdopt ~163-176 — molde de wiring DI never-throws)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-PATTERNS.md (§index.js Cambios 1-2 — excerpts)
    - .planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-RESEARCH.md (Pitfall 1 timeout 25s, Pitfall 3 binario por PATH)
    - src/cli/dashboard/enrich.js (Plan 01 — firma de deriveAdoptionMeta)
    - src/cli/dashboard/adopt.js (Plan 02 — runAdopt con description)
  </read_first>
  <action>
    En `src/cli/dashboard/index.js`:
    (Cambio 1) Importar `deriveAdoptionMeta` de `./enrich.js` y `readFileSync`/`existsSync` de `node:fs` (SOLO aquí — en enrich.js van por DI). El `execImpl` (execFile real, línea 124) ya está resuelto: es el spawnFn DI para enrich (execFile-shaped, NO config.claude.binary — Pitfall 3; `'claude'` se resuelve por PATH dentro de spawnDerive).
    (Cambio 2) Añadir el prop `onDerive` junto a `onAdopt` (índole de onFocus/onAdoptDiscover): `onDerive: async ({ cwd, sessionId }) => deriveAdoptionMeta({ spawnFn: execImpl, readFileFn: readFileSync, existsSyncFn: existsSync, cwd, sessionId })`. Never-throws (deriveAdoptionMeta fail-open a {}) → App.js cae a surface.title/basename. El timeout (~25s) vive dentro de spawnDerive (default 25_000, Plan 01) — no se cablea aquí salvo override explícito.
    (Cambio 3) Extender `onAdopt` (líneas 175-176) para destructurar y pasar `description`: `onAdopt: async ({ workspaceRef, cwd, sessionId, projectId, title, description }) => runAdopt({ exec: execImpl, execPath: process.execPath, kodoBin, workspaceRef, cwd, sessionId, projectId, title, description })`.
    Comentario explicativo (espejo de los comentarios Phase 56/57 existentes): el LLM vive SOLO en este carril (D-11/D-14); fail-open a basename; `execFile` argv injection-inerte (D-13).
    NO tocar el lifecycle de runDashboard (alt-screen, SIGTERM, waitUntilExit — todo intacto).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onDerive" src/cli/dashboard/index.js` ≥ 1 y `grep -c "deriveAdoptionMeta" src/cli/dashboard/index.js` ≥ 1
    - `grep -c "description" src/cli/dashboard/index.js` ≥ 1 (onAdopt pasa description)
    - `grep -c "readFileSync\|existsSync" src/cli/dashboard/index.js` ≥ 1 (fs DI cableado en index, NO en enrich)
    - onDerive usa `execImpl` como spawnFn (NO re-importa child_process; NO usa config.claude.binary)
    - `npm test` verde end-to-end (suite global completa, cero regresión)
  </acceptance_criteria>
  <done>index.js cablea onDerive=deriveAdoptionMeta con execFile+fs DI y onAdopt pasa description; el flujo end-to-end queda conectado; suite global verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (tecla `a`) → onDerive → subproceso `claude -p` | El armado dispara la derivación LLM local con contexto inline |
| onDerive (output LLM) → armedSurface → onAdopt → runAdopt → CLI | El `{title, description}` derivado cruza hacia la creación de la tarea |
| panel ink (TUI) → handlers async never-throws | Un fallo del derivador/adopt no debe crashear el panel montado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-09 | Denial of Service | derivación colgada bloquea el panel ink | mitigate | onDerive es never-throws (fail-open a {}); el timeout (~25s) vive en spawnDerive (Plan 01); el poll de /status sigue corriendo bajo `deriving`; el spinner ocupa solo el slot de footer (no overlay bloqueante). Esc en deriving cancela (T5). |
| T-62-10 | Tampering | derivación obsoleta reabre el confirm tras Esc | mitigate | Token de generación (`overlayReqRef`, espejo App.js:347): `if (overlayReqRef.current !== reqId) return` tras el await — el resultado tardío se descarta. Cubierto por el test T5. |
| T-62-11 | Information Disclosure | leak de home/rutas en title/description hacia el provider | mitigate | El `{title, description}` viaja vía onAdopt→runAdopt→`kodo adopt --title/--description`→adoptSession, donde `sanitizeAdoptionData` (BIDIR-08) redacta home/rutas (D-12). El dashboard no re-sanea (backstop downstream). |
| T-62-12 | Tampering | inyección shell vía title/description en el spawn | mitigate | onAdopt usa `execFile` (execImpl) con argv literal (sin shell); onDerive pasa cwd/sessionId como datos a deriveAdoptionMeta, que spawnea con argv literal. Metacaracteres inertes (D-13). T-57-01 NO aplica. |
| T-62-SC | Tampering | npm installs | mitigate | N/A — cero dependencias npm nuevas (solo `node:fs` builtin + imports internos + el CLI `claude` preexistente, D-15). RESEARCH §Package Legitimacy Audit: gate N/A. |
</threat_model>

<verification>
- `node --test test/dashboard/app-derive.test.js` verde (estado deriving + fusión + fail-open + Esc-invalida + render)
- `node --test test/format-isolation.test.js` verde (color-isolation D-12)
- `npm test` verde end-to-end
- **Manual/UAT obligatorio (VALIDATION.md §Manual-Only):** la calidad del título/descripción derivados por Haiku y la UX derive-then-confirm en vivo NO son deterministas → requiere HUMAN-UAT contra una sesión ad-hoc real, reproduciendo el caso origen (ROMAN-194 / scp-cmri: el título DEBE reflejar el proyecto, NO `basename(cwd)`). Confirmar que el timeout ~25s no dispara fallback en derivaciones normales.
</verification>

<success_criteria>
- ORCH-02 SC#1 (derive-then-confirm: pick → "derivando…" → propuesta → segunda `a` confirma; propuesta ANTES de crear) cubierto por el estado deriving + render confirm
- ORCH-02 SC#4 (al confirmar shellea `kodo adopt --title --description`; LLM solo en derivación; suelo 0-token intacto) cerrado por el wiring onDerive/onAdopt
- never-throws preservado en el carril del dashboard (panel ink siempre montado, D-03/D-13)
- v1 no-editable (Esc cancela; escape-hatch = `kodo adopt --title` manual, D-09)
</success_criteria>

<output>
Create `.planning/phases/62-adopci-n-inteligente-desde-el-dashboard/62-03-SUMMARY.md` when done
</output>
