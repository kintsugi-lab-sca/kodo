---
phase: 65-daemon-lifecycle-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/polling-daemon.js
  - src/daemon/provider-uses-polling.js
  - test/daemon/pid-name-param.test.js
  - test/daemon/provider-uses-polling.test.js
autonomous: true
requirements: [UP-04]

must_haves:
  truths:
    - "getPidPath('kodo') resolves to ~/.kodo/kodo.pid, distinct from server.pid and polling.pid"
    - "getPidPath() with no arg still resolves to ~/.kodo/polling.pid (back-compat)"
    - "writePidFile(payload, 'kodo') writes ~/.kodo/kodo.pid atomically with mode 0o600"
    - "A daemon payload {pid, started_at, kind:'daemon'} (no repos) survives readPidFile('kodo') shape-check"
    - "providerUsesPolling({provider:'github'}) === true; plane, undefined, and malformed configs === false"
  artifacts:
    - src/cli/polling-daemon.js
    - src/daemon/provider-uses-polling.js
    - test/daemon/pid-name-param.test.js
    - test/daemon/provider-uses-polling.test.js
  key_links:
    - "The optional trailing name param defaults to 'polling' in all four PID primitives — existing zero/one-arg callers in src/cli/polling.js stay byte-identical (D-04 back-compat)"
    - "run.js (Plan 03) imports these four PID primitives from src/cli/polling-daemon.js passing 'kodo', and providerUsesPolling from src/daemon/provider-uses-polling.js"
---

<objective>
Deliver the two pure, dependency-free primitives that the composed daemon (Plan 03) builds on: (1) generalize the shipped PID module `src/cli/polling-daemon.js` so each primitive accepts an optional `name` (default `'polling'`) — the daemon will use `'kodo'` → `~/.kodo/kodo.pid`, distinct from the legacy `server.pid` and standalone `polling.pid`; and (2) add the pure `providerUsesPolling(config)` helper that decides whether the daemon should run the polling loop.

Purpose: Both are leaf primitives with zero dependencies and clear input→output contracts, so they land first (Wave 1) and unblock `run.js`/`lifecycle.js` in Wave 2. Generalizing the PID module additively (default `name='polling'`) is the lowest-churn path that preserves every current caller and existing test (D-04).
Output: A name-parametrized PID module (back-compat), a `providerUsesPolling` pure function, and their two unit test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-daemon-lifecycle-foundation/65-CONTEXT.md
@.planning/phases/65-daemon-lifecycle-foundation/65-RESEARCH.md

@src/cli/polling-daemon.js
@test/cli/polling-daemon.test.js
</context>

## Artifacts this phase produces (phase-wide surface — for parallel executors)

- `src/cli/polling-daemon.js` — PID primitives generalized by `name` (this plan): `getPidPath(name='polling')`, `writePidFile(payload, name='polling')`, `readPidFile(name='polling')`, `removePidFile(name='polling')`.
- `src/daemon/provider-uses-polling.js` — `providerUsesPolling(config)` pure helper (this plan).
- `src/server.js` — `startServer({ managed, _loadConfig, _provider })` option; managed returns `{ server, stopReconcile }` (Plan 02).
- `src/daemon/lifecycle.js` — generic `startDaemon`/`stopDaemon`/`statusDaemon` (Plan 03).
- `src/daemon/run.js` — `runDaemon()` foreground funnel (Plan 03).
- `src/cli.js` — `kodo daemon run` hidden subcommand (Plan 04).
- `~/.kodo/kodo.pid` — new runtime PID file (JSON, 0600, `kind:'daemon'`).
- 7 new test files under `test/daemon/` + `test/server-managed.test.js` + `test/cli/kodo-start-regression.test.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize the PID module by name (additive, back-compat)</name>
  <files>src/cli/polling-daemon.js, test/daemon/pid-name-param.test.js</files>
  <read_first>
    - src/cli/polling-daemon.js (whole file — getPidPath :51, writePidFile :76, readPidFile :95, removePidFile :113, PID_PATH legacy alias :129)
    - test/cli/polling-daemon.test.js:1-57 (HOME-isolation molde: mkdtempSync + process.env.HOME + dynamic import cachebust + statSync mode check)
  </read_first>
  <behavior>
    - getPidPath('kodo') === join(homedir(), '.kodo', 'kodo.pid'); getPidPath() (no arg) === join(homedir(), '.kodo', 'polling.pid')
    - writePidFile({pid:7, started_at:'...', kind:'daemon'}, 'kodo') creates ~/.kodo/kodo.pid, mode 0o600, tmp file absent post-rename
    - readPidFile('kodo') on a {pid, started_at, kind:'daemon'} payload returns it (shape-check passes without repos); malformed pid → null
    - removePidFile('kodo') is idempotent (no throw when file absent)
    - Back-compat: existing zero/one-arg calls (getPidPath(), writePidFile(payload), readPidFile(), removePidFile()) resolve to the polling.pid path unchanged — existing test/cli/polling-daemon.test.js stays green
  </behavior>
  <action>
    Per D-04: make `name` an optional TRAILING parameter defaulting to `'polling'` in all four primitives so no current caller changes. Signatures become `getPidPath(name = 'polling')`, `writePidFile(payload, name = 'polling')`, `readPidFile(name = 'polling')`, `removePidFile(name = 'polling')` — payload stays FIRST in writePidFile precisely to preserve the single existing caller in src/cli/polling.js:374 and the existing single-arg test at test/cli/polling-daemon.test.js:43 (both keep passing an object as the only positional arg and get `name='polling'`). Replace the hardcoded `'polling.pid'` at polling-daemon.js:52 with `` `${name}.pid` ``. Preserve the atomic write recipe verbatim (writeFileSync tmp → chmodSync(tmp, 0o600) PRE-rename → renameSync) at :76-83 — do NOT switch to config.js writeFileAtomic (it does NOT chmod). Preserve the defensive readPidFile shape-check at :100 unchanged (pid:number + started_at:string) — a daemon payload `{pid, started_at, kind:'daemon'}` (no `repos`) already passes it, which is exactly why the kodo daemon can reuse this module. Do NOT rename the exports and do NOT touch the `PID_PATH` legacy alias. Write test/daemon/pid-name-param.test.js mirroring the HOME-isolation molde of test/cli/polling-daemon.test.js (mkdtempSync + set process.env.HOME + dynamic `import('...polling-daemon.js?cachebust=' + Date.now())` AFTER setting HOME + statSync mode & 0o777). This is additive generalization only — no behavior change for the polling daemon (avoids the Pitfall-1 naive-PID trap by keeping the mature primitive).
  </action>
  <verify>
    <automated>node --test test/daemon/pid-name-param.test.js test/cli/polling-daemon.test.js test/cli/polling.test.js</automated>
  </verify>
  <acceptance_criteria>
    - getPidPath('kodo')/getPidPath() resolve to kodo.pid / polling.pid respectively
    - writePidFile(payload, 'kodo') is atomic + 0600 + distinct path from server.pid/polling.pid
    - Existing polling-daemon and polling tests remain green (zero regression on current callers)
  </acceptance_criteria>
  <done>getPidPath('kodo')→kodo.pid (0600) and getPidPath()→polling.pid; daemon payload passes readPidFile('kodo'); existing polling PID tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add providerUsesPolling(config) pure helper</name>
  <files>src/daemon/provider-uses-polling.js, test/daemon/provider-uses-polling.test.js</files>
  <read_first>
    - src/providers/registry.js:20-80 (only 'plane' + 'github' are registered)
    - src/config.js:253-261 (getDefaultGithubProviderConfig — github has repos[] + poll_interval; polling-based)
  </read_first>
  <behavior>
    - providerUsesPolling({provider:'github'}) === true
    - providerUsesPolling({provider:'plane'}) === false (plane is webhook/server ingress, not polling)
    - providerUsesPolling(undefined) === false; providerUsesPolling({}) === false; providerUsesPolling({provider:42}) === false
  </behavior>
  <action>
    Per D-06: create src/daemon/provider-uses-polling.js exporting a `// @ts-check` pure function `providerUsesPolling(config)` that returns `config?.provider === 'github'`. GitHub is polling-based (repos + poll_interval, config.js:253-261); Plane uses webhook ingress on the server (server.js /webhook), so it does NOT need the polling loop. Use an explicit allowlist against `'github'` with a dense Spanish comment noting it is the only polling provider among the two registered today (registry.js) and is extensible if a third polling provider ships. Follow the repo never-throws / fail-open convention: any malformed or missing `config` returns `false` (NOT starting polling is the safe failure — the server keeps serving). No fenced logic beyond the single equality; no imports required. Write test/daemon/provider-uses-polling.test.js as a pure unit (no HOME isolation needed — the function takes config as an argument) covering github→true, plane→false, and the three malformed cases.
  </action>
  <verify>
    <automated>node --test test/daemon/provider-uses-polling.test.js</automated>
  </verify>
  <acceptance_criteria>
    - github → true, plane → false, malformed/missing → false
    - Function is pure (no filesystem/network), never throws
  </acceptance_criteria>
  <done>providerUsesPolling: github→true, plane→false, malformed/missing→false; pure and never-throws.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → filesystem (~/.kodo) | PID metadata written at rest; readable by other local processes |
| config object → daemon decision | untrusted/malformed config must not crash the polling decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-01 | Information Disclosure | writePidFile(payload,'kodo') | medium | mitigate | chmod 0o600 PRE-rename (polling-daemon.js:81) so any concurrent post-rename read sees restrictive perms immediately; payload carries only pid/started_at/kind — no secrets |
| T-65-02 | Tampering | readPidFile shape-check | low | mitigate | defensive shape-check (pid:number, started_at:string) at polling-daemon.js:100 rejects injected/corrupt payloads; fail-open null |
| T-65-03 | Denial of Service | providerUsesPolling(config) | low | mitigate | never-throws / fail-safe false on malformed config — a bad config cannot crash the daemon's start decision |
| T-65-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit = N/A; zero-new-deps invariant LOCKED) |
</threat_model>

<verification>
- `node --test test/daemon/pid-name-param.test.js test/daemon/provider-uses-polling.test.js` green
- `node --test test/cli/polling-daemon.test.js test/cli/polling.test.js` green (no regression on existing PID callers — proves the generalization is byte-compatible for `name='polling'`)
</verification>

<success_criteria>
- PID module accepts an optional trailing `name` (default `'polling'`) across all four primitives; `'kodo'` yields `~/.kodo/kodo.pid` at 0600 (SC4 prerequisite, UP-04)
- Existing polling callers/tests unchanged (back-compat)
- `providerUsesPolling` correctly gates polling to github, fail-safe false (D-06, UP-04)
</success_criteria>

<output>
Create `.planning/phases/65-daemon-lifecycle-foundation/65-01-SUMMARY.md` when done.
</output>
