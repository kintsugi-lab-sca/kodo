---
phase: 69-red-y-autenticaci-n
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logs/reader.js
  - src/logger.js
  - test/logs-reader.test.js
  - README.md
autonomous: true
requirements: [NET-05, NET-06]
must_haves:
  truths:
    - "A sessionId with any character outside /^[A-Za-z0-9_-]+$/ is rejected before any filesystem path is built"
    - "The reconcile loop and legitimate loggers (synthetic id 'reconcile', UUID session ids) keep working after the guard is added"
    - "The README documents the multi-node topology: default 127.0.0.1 bind, explicit exposure via config.server.bind + ACL for the Plane webhook"
  artifacts:
    - "SESSION_ID_RE guard in src/logs/reader.js before the log-path join"
    - "Soft (non-throwing) sessionId format guard in src/logger.js"
    - "New README section: Topología multi-nodo"
  key_links:
    - "The hard reject lives at the CLI edge (reader.js, untrusted `kodo logs <id>` input); logger.js uses defense-in-depth WITHOUT a process-killing throw (Pitfall 3)"
---

<objective>
Neutralize the path-traversal vector where a `sessionId` becomes a log filename (audit finding B6), and document the multi-node network topology the default-bind change makes operationally relevant. Per D-10 (validate `/^[A-Za-z0-9_-]+$/` at the edge before touching the filesystem; soft defense-in-depth in logger.js) and D-11 (a single new README section only).

Purpose: NET-05 closes the last untrusted-input → filesystem path in this phase; NET-06 tells the operator how to safely re-expose the server (now loopback-only by default) so the Plane webhook can still arrive from another node. Both are independent of the server.js and dashboard work and can land in parallel.

Output: hardened `sessionId` handling + a "Topología multi-nodo" README section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-red-y-autenticaci-n/69-CONTEXT.md
@.planning/phases/69-red-y-autenticaci-n/69-RESEARCH.md
@.planning/phases/69-red-y-autenticaci-n/69-PATTERNS.md
@src/logs/reader.js
@src/logger.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate sessionId before it becomes a log path (NET-05, D-10)</name>
  <files>src/logs/reader.js, src/logger.js, test/logs-reader.test.js</files>
  <read_first>
    - src/logs/reader.js (line 66 join(KODO_DIR, 'logs', `${sessionId}.ndjson`) — the untrusted `kodo logs <session-id>` edge; the existing process.exit(2)/process.exit(1) rejections at 54/62 to mirror)
    - src/logger.js (createLogger at 240-250: the existing empty-sessionId throw guard at 241-243, and the path join at 250 — CAUTION: createLogger is called with the synthetic id 'reconcile' at server.js:431/693 and with real session ids; a strict throw here could kill the reconcile loop — Pitfall 3)
    - test/logs-reader.test.js (the makeTmpHome fixture + seedLog + captureStderr harness to extend)
    - src/config.js (validateEnvValue at 366 — the same allowlist-by-rejection spirit; the regex here is a positive allowlist /^[A-Za-z0-9_-]+$/)
    - .planning/phases/69-red-y-autenticaci-n/69-RESEARCH.md (Pattern 8 sessionId guard; Pitfall 3; Open Question 2 — hard reject at the edge, soft defense in logger.js)
  </read_first>
  <behavior>
    - runLogs called with a sessionId containing a path separator or traversal (e.g. `../../etc/passwd`, `a/b`, `a..b` with a slash, an empty-after-resolve id) writes an "Invalid session id" line to stderr and exits with code 2, BEFORE any readFileSync / path join
    - runLogs called with a valid id (letters, digits, `_`, `-`, e.g. a UUID like `2f9c-...` or `sess-reader-1`) proceeds exactly as today (dumps the seeded ndjson)
    - createLogger with sessionId 'reconcile' still returns a working logger (synthetic id passes the allowlist — no regression to the reconcile loop)
    - createLogger with a real UUID sessionId still returns a working logger
    - createLogger with a sessionId containing a filesystem-hostile character does NOT throw a process-killing error; it degrades softly (warns and neither builds nor writes to a traversal path)
  </behavior>
  <action>
    In `src/logs/reader.js`, define `const SESSION_ID_RE = /^[A-Za-z0-9_-]+$/` at module scope. Immediately BEFORE the `join(KODO_DIR, 'logs', ...)` at line 66 (after the `sessionId` is resolved and the existing empty-id guard at 59-64), add a hard reject: if `!SESSION_ID_RE.test(sessionId)`, `process.stderr.write('Invalid session id\n')` and `process.exit(2)` — mirroring the existing exits at 54/62. This is the untrusted CLI edge (`kodo logs <session-id>`), so the reject is hard.

    In `src/logger.js`, apply defense-in-depth at the `join(logDir, `${sessionId}.ndjson`)` (line 250) WITHOUT a hard throw (Pitfall 3 — createLogger runs with the synthetic id 'reconcile' and real ids; a throw could kill the reconcile loop). Validate with the same `/^[A-Za-z0-9_-]+$/` allowlist; if the id fails, `console.warn` a redacted notice and disable the disk sink for that logger (make writeNdjson a no-op / skip the append) so a hostile id can never resolve into a traversal path. Do NOT touch the existing empty-sessionId throw at 241-243 (that contract stays). Note: 'reconcile' and UUIDs pass the allowlist, so the reconcile loop and normal loggers are unaffected — only a genuinely hostile id degrades.

    Extend `test/logs-reader.test.js`: add cases for a traversal/path-separator sessionId → stderr 'Invalid session id' + exit code 2 (assert via the captureStderr + exit-capture harness), and confirm a valid id still dumps its seeded log. Add (here or alongside logger tests) an assertion that createLogger('reconcile') and a UUID id still produce a working logger, and that a hostile id does not throw.
  </action>
  <verify>
    <automated>node --test test/logs-reader.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/logs-reader.test.js` passes: a traversal sessionId is rejected with 'Invalid session id' + exit 2 before any file read; a valid id still dumps its log
    - `grep -c "A-Za-z0-9_-" src/logs/reader.js` is >= 1 and `grep -c "A-Za-z0-9_-" src/logger.js` is >= 1 (allowlist present at both anchors)
    - createLogger('reconcile') returns a logger without throwing (asserted / smoke: `node -e "import('./src/logger.js').then(m => { m.createLogger({sessionId:'reconcile'}); console.log('ok'); })"` prints ok)
    - The logger.js empty-sessionId throw at 241-243 is unchanged (grep confirms the existing guard message still present)
    - Full suite green: `npm test` (server-reconcile-logger + logger tests must not regress)
  </acceptance_criteria>
  <done>A hostile sessionId is hard-rejected at the CLI edge (exit 2) before any path is built; logger.js soft-guards the same allowlist without throwing; 'reconcile' and UUID ids keep working.</done>
</task>

<task type="auto">
  <name>Task 2: Document the multi-node topology in the README (NET-06, D-11)</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing section structure/heading style to match; find where server/config or deployment topics live so the new section is placed coherently)
    - .planning/phases/69-red-y-autenticaci-n/69-CONTEXT.md (D-11: only this one section is added — the full README pass is HYG-08 in Phase 72)
    - .planning/phases/69-red-y-autenticaci-n/69-RESEARCH.md (Runtime State Inventory: the bind change is breaking for existing multi-node deploys that received the webhook via an open bind — this is exactly what NET-06 documents)
  </read_first>
  <action>
    Add ONE new README section titled "Topología multi-nodo" (match the existing heading level/style). It must state: (1) the server binds to `127.0.0.1` by default, so it is unreachable from other interfaces/nodes; (2) to receive the Plane webhook from another node (Plane delivers from an external host), the operator sets `config.server.bind` to the machine's tailscale IP (or the intended interface) AND restricts access with an ACL/firewall — exposing the bind is a conscious opt-in; (3) the non-webhook rail still requires the bearer token even when exposed, and `/webhook` keeps its HMAC while `/health` stays open. Keep it to this single section — do NOT do a broader README pass (that is HYG-08, Phase 72). Do not invent flags or config keys beyond `config.server.bind` and the existing webhook secret env var.
  </action>
  <verify>
    <automated>grep -c "Topología multi-nodo" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Topología multi-nodo" README.md` returns 1 (the section exists exactly once)
    - `grep -c "config.server.bind" README.md` is >= 1 and `grep -c "127.0.0.1" README.md` is >= 1 (the default + the exposure key are documented)
    - The section mentions the bearer requirement on the non-webhook rail and that /webhook keeps HMAC / /health stays open (grep for "bearer" or "token" and "webhook" within the added content)
    - No other README section is restructured (diff touches only the added section)
  </acceptance_criteria>
  <done>A single "Topología multi-nodo" README section documents the loopback default, the explicit config.server.bind + ACL exposure path for the Plane webhook, and the retained bearer/HMAC/health semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg / untrusted id → filesystem | `kodo logs <session-id>` and createLogger take an id that becomes a file path; the allowlist blocks traversal here |
| operator → network exposure decision | The README section is the control that makes re-exposing the bind a documented, deliberate act rather than an accidental open surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-06 | Tampering / Information Disclosure | sessionId → log file path (reader.js:66, logger.js:250) | high | mitigate | Allowlist `/^[A-Za-z0-9_-]+$/` — hard reject (exit 2) at the CLI edge before the path join; soft non-throwing guard in logger.js (defense-in-depth, no reconcile-loop regression) (NET-05, B6) |
| T-69-10 | Information Disclosure (operational) | multi-node exposure without ACL | low | mitigate | README documents that exposing `config.server.bind` is opt-in and must be paired with an ACL/firewall; bearer + HMAC remain in force (NET-06) |
| T-69-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs; `node:fs`/`node:path` built-ins only — zero-new-dependency invariant |
</threat_model>

<verification>
- `node --test test/logs-reader.test.js` green; a traversal id → exit 2 before any file read; valid ids unaffected
- createLogger('reconcile') + UUID ids still return working loggers; hostile id degrades softly (no throw)
- README has exactly one "Topología multi-nodo" section citing config.server.bind + 127.0.0.1 + bearer/HMAC/health
- Full suite green: `npm test` (baseline 1788 pass + 1 skip must not regress)
</verification>

<success_criteria>
A `sessionId` outside `/^[A-Za-z0-9_-]+$/` is rejected before any filesystem path is built (hard at the CLI edge, soft in logger.js), with no regression to the reconcile loop or normal loggers; the README documents the multi-node topology in a single new section. Zero new npm dependencies.
</success_criteria>

## Artifacts this phase produces

New symbols introduced by THIS plan (exclude from source-drift verification):
- `src/logs/reader.js`: module-scope `SESSION_ID_RE = /^[A-Za-z0-9_-]+$/` and a hard reject (stderr + exit 2) before the log-path join
- `src/logger.js`: a soft (non-throwing) `/^[A-Za-z0-9_-]+$/` guard at the log-path join that disables the disk sink for a hostile id
- `README.md`: a new "Topología multi-nodo" section
- Extended tests in `test/logs-reader.test.js`

<output>
Create `.planning/phases/69-red-y-autenticaci-n/69-04-SUMMARY.md` when done.
</output>
