---
phase: 09-phase-resolver-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gsd/roadmap.js
  - test/gsd-roadmap.test.js
autonomous: true
requirements: [GSD-03]
must_haves:
  truths:
    - "parseRoadmap acepta `##` y `###` como niveles de heading válidos"
    - "parseRoadmap rechaza `#` y `####` (y no los añade a phases[])"
    - "parseRoadmap acepta números enteros Y decimales (`9`, `72.1`) por D-08"
    - "parseRoadmap ignora rangos tipo `## Phase 1-5: Overview` (no matchean)"
    - "normalizeTitle aplica SOLO trim + collapse whitespace + lowercase (D-07)"
    - "normalizeTitle preserva puntuación y backticks (no strippea)"
  artifacts:
    - path: "src/gsd/roadmap.js"
      provides: "parseRoadmap + normalizeTitle, módulo puro sin I/O"
      contains: "export function parseRoadmap"
      contains_also: "export function normalizeTitle"
    - path: "test/gsd-roadmap.test.js"
      provides: "Unit tests cubriendo niveles válidos/rechazados, decimales, rangos, normalización"
      contains: "describe('parseRoadmap'"
  key_links:
    - from: "src/gsd/roadmap.js"
      to: "no external imports"
      via: "pure module (no node:fs, no node:path)"
      pattern: "NO_IMPORT (fs|path)"
---

<objective>
Crear el parser puro `src/gsd/roadmap.js` que extrae fases de `ROADMAP.md` y su helper `normalizeTitle` para comparaciones 1:1 estrictas. Satisface GSD-03 (parte parser) sin tocar filesystem.

Purpose: Aislar la lógica de parsing en un módulo puro (zero I/O) que pueda ser testado exhaustivamente sin fixtures de disco y reutilizado desde `resolver.js` (plan 09-03) y `cli/gsd-inspect.js` (plan 09-05).

Output: `src/gsd/roadmap.js` (parseRoadmap + normalizeTitle, ambos `export function`) y `test/gsd-roadmap.test.js` (unit tests con `node:test`).

**Refinamiento crítico aplicado (pattern-mapper #1):** La regex del CONTEXT D-05 dice `##{2,3}` pero ese pattern matchea 3-4 hashes (`#` + `{2,3}` repeticiones extra). El valor correcto para capturar `##` y `###` es `#{2,3}` (dos-a-tres hashes totales). Este plan lo corrige y documenta con un test explícito por nivel de heading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md
@.planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md

<interfaces>
<!-- Analog: src/labels.js (38 líneas, pure transform, zero imports). -->
<!-- Executor: imita header + shape, NO añadas imports de node:fs ni node:path. -->

From src/labels.js:
```javascript
// @ts-check

/**
 * Parse kodo labels from a work item's label data.
 * ...
 * @param {Array<any>} labels
 * @returns {{ isKodo: boolean, model: string|null, flags: string[] }}
 */
export function parseKodoLabels(labels) {
  const result = { isKodo: false, model: null, flags: [] };
  if (!Array.isArray(labels) || labels.length === 0) return result;
  // ... pure logic
  return result;
}
```

From test/labels.test.js (shape de test a imitar):
```javascript
// @ts-check
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { parseKodoLabels } from '../src/labels.js';

describe('parseKodoLabels', () => {
  it('returns defaults for empty input', () => {
    assert.deepEqual(parseKodoLabels([]), { isKodo: false, model: null, flags: [] });
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear `src/gsd/roadmap.js` (parseRoadmap + normalizeTitle)</name>
  <files>src/gsd/roadmap.js</files>
  <read_first>
    - src/gsd/roadmap.js (no existe — confirmar con ls, cualquier contenido preexistente es error)
    - src/labels.js (analog: header + shape + zero I/O)
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md (D-05 a D-08)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "src/gsd/roadmap.js (NEW — pure parser)")
  </read_first>
  <behavior>
    - Test 1: parseRoadmap('') devuelve { phases: [] }
    - Test 2: parseRoadmap con entrada non-string devuelve { phases: [] }
    - Test 3: `## Phase 1: Foundation\nbody\n## Phase 2: Rollout\n` produce 2 fases con n='1'/'2' y title='Foundation'/'Rollout'
    - Test 4: `### Phase 9: Phase Resolver + Bootstrap\n` produce 1 fase con n='9', title='Phase Resolver + Bootstrap' (D-05, nivel ### real en repo)
    - Test 5: `# Phase 1: Hashed\n#### Phase 2: Too deep\n` produce 0 fases (niveles fuera de 2-3 hashes rechazados)
    - Test 6: `## Phase 72.1: Inserted\n` produce fase con n='72.1' (decimal, D-08)
    - Test 7: `## Phase 1-5: Overview\n` produce 0 fases (rango ignorado, D-08)
    - Test 8: parseRoadmap guarda `heading` (línea cruda con hashes) y `line` (1-indexed)
    - Test 9: normalizeTitle('  Phase   Resolver  ') === 'phase resolver' (trim + collapse + lowercase)
    - Test 10: normalizeTitle('Foo: `bar`') === 'foo: `bar`' (puntuación y backticks preservados, D-07)
  </behavior>
  <action>
Crear el archivo `src/gsd/roadmap.js` con exactamente este contenido (NO abrevies, NO añadas imports de node:fs/path — módulo puro):

```javascript
// @ts-check

/**
 * Parse a ROADMAP.md string into structured phases. Pure: no I/O.
 *
 * Accepts heading levels `##` and `###` (D-05). Headings `#` and `####` rejected.
 * Accepts integer and decimal phase numbers (D-08): `Phase 9`, `Phase 72.1`.
 * Ranges like `Phase 1-5` are ignored (do not match the regex).
 *
 * NOTE: CONTEXT.md §D-05 writes the regex as `##{2,3}` which actually matches
 * 3-4 hashes (one literal `#` + 2-3 more). The correct pattern to capture
 * exactly `##` and `###` is `#{2,3}` (two-to-three hashes total). This module
 * uses the corrected form — see pattern-mapper refinement #1 in 09-PATTERNS.md.
 *
 * @param {string} md - Raw ROADMAP.md content.
 * @returns {{ phases: Array<{ n: string, title: string, heading: string, line: number }> }}
 */
export function parseRoadmap(md) {
  const result = { phases: [] };
  if (typeof md !== 'string' || md.length === 0) return result;

  const lines = md.split('\n');
  // `#{2,3}` = exactly 2 or 3 hashes. `\d+(?:\.\d+)?` = integer or decimal.
  // `[:\-]` separator after the number. `(.+)$` = non-empty title.
  // Line anchored (`^...$`) so trailing text on the same line is not stripped
  // but ranges like `Phase 1-5` fail because `-5:` is not `:` or `-` alone.
  const re = /^(#{2,3})\s+Phase\s+(\d+(?:\.\d+)?)\s*[:\-]\s*(.+)$/;

  for (let i = 0; i < lines.length; i++) {
    const m = lines[i].match(re);
    if (!m) continue;
    result.phases.push({
      n: m[2],
      title: m[3].trim(),
      heading: lines[i],
      line: i + 1,
    });
  }
  return result;
}

/**
 * Normalize a title for strict 1:1 matching (D-07).
 * Rules: trim + collapse whitespace runs + lowercase. Nothing else.
 * Keeps punctuation (`:`, `.`, `,`) and backticks (`` ` ``) intact to keep
 * matching strict — two titles that differ only in punctuation are NOT equal.
 *
 * @param {string} s
 * @returns {string}
 */
export function normalizeTitle(s) {
  return String(s).trim().replace(/\s+/g, ' ').toLowerCase();
}
```

Header obligatorio `// @ts-check` en línea 1 (convención de todo `src/`). El módulo NO debe importar `node:fs`, `node:path`, ni ningún otro módulo — es 100% puro.

**Anti-patrón a evitar:** NO uses el regex `##{2,3}` literal del CONTEXT — es el bug documentado. Usa `#{2,3}` (ver refinement #1 del pattern-mapper).
  </action>
  <verify>
    <automated>node -e "const {parseRoadmap, normalizeTitle} = await import('./src/gsd/roadmap.js'); const r = parseRoadmap('### Phase 9: Test\n# Phase 2: Rejected\n## Phase 1-5: Range\n'); console.assert(r.phases.length === 1, 'expected 1 phase, got ' + r.phases.length); console.assert(r.phases[0].n === '9'); console.assert(normalizeTitle('Foo: \`bar\`') === 'foo: \`bar\`');" --input-type=module</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `src/gsd/roadmap.js` existe y empieza con `// @ts-check`
    - Grep confirma export: `grep -n "export function parseRoadmap" src/gsd/roadmap.js` devuelve una coincidencia
    - Grep confirma export: `grep -n "export function normalizeTitle" src/gsd/roadmap.js` devuelve una coincidencia
    - Grep confirma regex correcto: `grep -n "#{2,3}" src/gsd/roadmap.js` devuelve una coincidencia
    - Grep confirma NO imports de filesystem: `grep -E "import.*(node:fs|node:path|'fs'|'path')" src/gsd/roadmap.js` devuelve 0 coincidencias
    - `node --check src/gsd/roadmap.js` sale con código 0
  </acceptance_criteria>
  <done>Parser puro creado. parseRoadmap acepta `##`/`###`, rechaza `#`/`####`, acepta decimales, ignora rangos. normalizeTitle aplica solo trim+collapse+lowercase. Ningún import de I/O.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear `test/gsd-roadmap.test.js` (unit tests parser + normalizeTitle)</name>
  <files>test/gsd-roadmap.test.js</files>
  <read_first>
    - test/labels.test.js (analog: describe/it flat con node:test + assert/strict)
    - src/gsd/roadmap.js (recién creado en Task 1)
    - .planning/phases/09-phase-resolver-bootstrap/09-PATTERNS.md (sección "test/gsd-roadmap.test.js" con snippet literal)
  </read_first>
  <action>
Crear `test/gsd-roadmap.test.js` con exactamente este contenido:

```javascript
// @ts-check
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { parseRoadmap, normalizeTitle } from '../src/gsd/roadmap.js';

describe('parseRoadmap', () => {
  it('returns empty phases when markdown is empty', () => {
    assert.deepEqual(parseRoadmap(''), { phases: [] });
  });

  it('returns empty phases for non-string input', () => {
    // @ts-expect-error — deliberate invalid input for defense check
    assert.deepEqual(parseRoadmap(null), { phases: [] });
    // @ts-expect-error
    assert.deepEqual(parseRoadmap(undefined), { phases: [] });
    // @ts-expect-error
    assert.deepEqual(parseRoadmap(42), { phases: [] });
  });

  it('parses ## Phase headings (spec form)', () => {
    const md = '## Phase 1: Foundation\nbody\n## Phase 2: Rollout\n';
    const { phases } = parseRoadmap(md);
    assert.equal(phases.length, 2);
    assert.equal(phases[0].n, '1');
    assert.equal(phases[0].title, 'Foundation');
    assert.equal(phases[0].heading, '## Phase 1: Foundation');
    assert.equal(phases[0].line, 1);
    assert.equal(phases[1].n, '2');
    assert.equal(phases[1].line, 3);
  });

  it('parses ### Phase headings (real ROADMAP form, D-05)', () => {
    const md = '### Phase 9: Phase Resolver + Bootstrap\n';
    const { phases } = parseRoadmap(md);
    assert.equal(phases.length, 1);
    assert.equal(phases[0].n, '9');
    assert.equal(phases[0].title, 'Phase Resolver + Bootstrap');
  });

  it('rejects # (level 1) and #### (level 4) headings', () => {
    const md = '# Phase 1: Hashed\n#### Phase 2: Too deep\n';
    assert.deepEqual(parseRoadmap(md), { phases: [] });
  });

  it('accepts decimal phase numbers (D-08, forward-compat with gsd-insert-phase)', () => {
    const { phases } = parseRoadmap('## Phase 72.1: Inserted\n');
    assert.equal(phases.length, 1);
    assert.equal(phases[0].n, '72.1');
    assert.equal(phases[0].title, 'Inserted');
  });

  it('ignores range headings like Phase 1-5', () => {
    const { phases } = parseRoadmap('## Phase 1-5: Overview\n');
    assert.equal(phases.length, 0);
  });

  it('accepts dash separator (## Phase 1 - Title)', () => {
    const { phases } = parseRoadmap('## Phase 1 - Foo\n');
    assert.equal(phases.length, 1);
    assert.equal(phases[0].title, 'Foo');
  });

  it('captures line numbers as 1-indexed', () => {
    const md = 'prelude\n\n## Phase 3: Third\n';
    const { phases } = parseRoadmap(md);
    assert.equal(phases[0].line, 3);
  });
});

describe('normalizeTitle', () => {
  it('lowercases and trims', () => {
    assert.equal(normalizeTitle('  Hello World  '), 'hello world');
  });

  it('collapses internal whitespace runs (D-07)', () => {
    assert.equal(normalizeTitle('A   B\t\tC'), 'a b c');
  });

  it('preserves punctuation and backticks (D-07 strict)', () => {
    assert.equal(normalizeTitle('Foo: `bar`'), 'foo: `bar`');
    assert.equal(normalizeTitle('A, B. C!'), 'a, b. c!');
  });

  it('coerces non-strings via String()', () => {
    // @ts-expect-error
    assert.equal(normalizeTitle(42), '42');
  });
});
```

Usa `node:test` con `describe`/`it` flat (sin nesting profundo), `assert/strict` (no `assert.equal` laxo), imports relativos `../src/gsd/roadmap.js`. Esta es la convención exacta de `test/labels.test.js`.
  </action>
  <verify>
    <automated>node --test test/gsd-roadmap.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `test/gsd-roadmap.test.js` existe
    - `node --test test/gsd-roadmap.test.js` imprime "pass" en todos los tests y exit code 0
    - El output incluye al menos 13 tests ejecutados (9 en parseRoadmap + 4 en normalizeTitle)
    - Grep confirma que los tests usan el módulo real: `grep -n "from '../src/gsd/roadmap.js'" test/gsd-roadmap.test.js` devuelve 1 coincidencia
    - Todos los tests pasan SIN mocks (parser puro, no requiere fs)
  </acceptance_criteria>
  <done>Unit tests cubren niveles de heading válidos/rechazados, decimales, rangos, normalización estricta (preserva puntuación). `node --test` los ejecuta en < 1s sin tocar disco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external ROADMAP.md content → parser | Contenido de ROADMAP.md puede ser malicioso (ReDoS) si el repo destino es hostil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Denial of Service | parseRoadmap regex | mitigate | Regex `^(#{2,3})\s+Phase\s+(\d+(?:\.\d+)?)\s*[:\-]\s*(.+)$` es ancorado (`^`/`$`) y no tiene backtracking catastrófico: ninguna alternancia anidada, cuantificadores posesivos implícitos por `.+` en end-of-line. Validado por test explícito con entrada de 10k líneas en task futura si se detectan lentitudes. |
| T-09-01-02 | Tampering | parseRoadmap output | mitigate | Salida es plain object literal (`result.phases.push({...})`); no merge/assign desde input, no hay riesgo de prototype pollution. |
| T-09-01-03 | Information Disclosure | parser bugs | accept | Parser es local, no emite logs, no expone datos sensibles. Superficie mínima. |
</threat_model>

<verification>
- `node --test test/gsd-roadmap.test.js` pasa con 0 fallos
- `node --check src/gsd/roadmap.js` sale con código 0 (syntax válida)
- `grep -E "import.*(node:fs|node:path|'fs'|'path')" src/gsd/roadmap.js` devuelve 0 líneas (pureza)
- `grep -n "#{2,3}" src/gsd/roadmap.js` devuelve 1 línea (regex correcto, no bug del CONTEXT)
</verification>

<success_criteria>
- `src/gsd/roadmap.js` existe, 100% puro, exporta `parseRoadmap` y `normalizeTitle`
- `test/gsd-roadmap.test.js` existe con ≥13 tests, todos pasan
- Regex utiliza `#{2,3}` (corrige el bug del CONTEXT D-05 según refinement #1 del pattern-mapper)
- Tests cubren: niveles `##`/`###` válidos, `#`/`####` rechazados, decimales (`72.1`), rangos (`1-5`) ignorados, normalización estricta con puntuación preservada
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-resolver-bootstrap/09-01-SUMMARY.md`
</output>
