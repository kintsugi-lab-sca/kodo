---
id: 09-06
phase: 09-phase-resolver-bootstrap
plan: 06
type: execute
wave: 1
autonomous: true
gap_closure: true
depends_on: [09-04, 09-05]
files_modified:
  - src/hooks/session-start.js
  - test/session-start.test.js
  - src/cli/gsd-inspect.js
  - test/gsd-inspect-cli.test.js
requirements: []   # gap closure does not introduce new requirements

must_haves:
  truths:
    - "Tras un dispatch bootstrap, sólo existe UNA entrada NDJSON con event='gsd.bootstrap' por sesión (fuente única = dispatcher)"
    - "El hook src/hooks/session-start.js NO invoca gsdBootstrap (pattern-mapper #3 aplicado a bootstrap igual que a phase.resolved)"
    - "runGsdInspect retorna exit code 1 (no 2) cuando resolveProjectPath lanza — preserva el contrato D-19 literal: 2 es exclusivamente provider fetch failure"
    - "Un test anti-regresión impide que el bloque gsdBootstrap vuelva al hook (análogo al test existente para gsdPhaseResolved)"
  artifacts:
    - path: "src/hooks/session-start.js"
      provides: "hook sin bloque duplicado de gsd.bootstrap"
      grep_must_be_zero: "gsdBootstrap|'gsd\\.bootstrap'"
    - path: "test/session-start.test.js"
      provides: "anti-regresión invertido — grep-based assertion que gsdBootstrap NO es invocado desde el hook"
    - path: "src/cli/gsd-inspect.js"
      provides: "exit code 1 en rama resolveProjectPath catch (D-19 clarificado en comentario)"
    - path: "test/gsd-inspect-cli.test.js"
      provides: "test nuevo: resolveProjectPathFn throw → exit code 1 (config error ≠ fetch failure)"
  key_links:
    - from: "src/triggers/dispatcher.js"
      to: "NDJSON log"
      via: "única emisión gsd.bootstrap"
      pattern: "log\\.info.*gsd\\.bootstrap"
    - from: "src/cli/gsd-inspect.js:~73"
      to: "exit code 1"
      via: "catch de resolveProjectPathFn"
      pattern: "return 1"
---

<objective>
Cerrar los dos hallazgos HIGH del code review Phase 9 (09-REVIEW.md) y la única falla de VERIFICATION.md:

1. **GAP-01 / HI-01 (BLOCKING, gap verificado):** El dispatcher y el hook `session-start.js` emiten ambos `gsd.bootstrap` por cada dispatch bootstrap — doble entrada NDJSON. Pattern-mapper #3 ya se aplicó para `gsd.phase.resolved` en Plan 09-04 pero `gsd.bootstrap` quedó sin migrar. Eliminar el bloque emisor del hook (líneas 188-200) e invertir el test en `test/session-start.test.js:156` que actualmente cementa el bug pidiendo activamente que el hook emita.

2. **HI-02 (non-blocking pero cheap-to-fix):** `runGsdInspect` en `src/cli/gsd-inspect.js:75` retorna exit code 2 cuando `resolveProjectPathFn(task)` lanza — mismo código que `provider.getTask` throw (línea 66). D-19 (09-CONTEXT.md) reserva exit 2 EXCLUSIVAMENTE para "provider fetch failure"; un mapping ausente es config error (semántico) y debe retornar 1. Clarifica el contrato D-19 sin inventar un cuarto código.

Purpose: Una sola fuente emite cada evento de dispatch (invariante D-14 completado) y los exit codes del CLI `kodo gsd inspect` reflejan el contrato D-19 literal — scripts con retry-on-2 dejan de reintentar infinitamente config errors.

Output: 4 archivos modificados, ~20 LOC netos. Un commit único tipo `fix(09-06): eliminar doble emisión gsd.bootstrap + clarificar exit codes kodo gsd inspect`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md
@.planning/phases/09-phase-resolver-bootstrap/09-VERIFICATION.md
@.planning/phases/09-phase-resolver-bootstrap/09-REVIEW.md
@.planning/phases/09-phase-resolver-bootstrap/09-04-SUMMARY.md

<interfaces>
<!-- Contratos que los consumidores usan. Extraídos de los archivos a modificar. -->

From src/hooks/session-start.js (bloque actual a eliminar, líneas 183-200):
```js
// Phase 9 (pattern-mapper refinement #3): gsd.phase.resolved is emitted by
// the DISPATCHER now (single source of truth), not the hook. The hook only
// emits gsd.bootstrap for bootstrap sessions (no phase_id). If we kept the
// phase-resolved emit here, every GSD dispatch would produce TWO matching
// NDJSON entries — `kodo logs --event gsd.phase.resolved` would double-count.
if (session.gsd && !session.phase_id) {
  try {
    const { createLogger } = await import('../logger.js');
    const { gsdBootstrap } = await import('../logger-events.js');
    const log = createLogger({
      sessionId: session.session_id,
      minLevel: /** @type {any} */ (process.env.KODO_LOG_LEVEL || 'info'),
    }).child({ component: 'hook', task_id: session.task_id });
    gsdBootstrap(log, { project_path: session.project_path });
  } catch {
    // silent — never crash Claude Code
  }
}
```

From test/session-start.test.js (test actual a invertir, líneas 156-164):
```js
it('Phase 9: still invokes gsdBootstrap from hook for bootstrap sessions', () => {
  // Companion assertion: removing the phase-resolved emit must NOT remove the
  // bootstrap emit. Bootstrap events still originate from the hook.
  assert.match(
    source,
    /gsdBootstrap\s*\(/,
    'session-start.js must still invoke gsdBootstrap for bootstrap sessions',
  );
});
```

From test/session-start.test.js (patrón de strip de comentarios a reutilizar, líneas 142-153):
```js
const invocationRe = /gsdPhaseResolved\s*\(/;
const stripped = source
  .replace(/\/\*[\s\S]*?\*\//g, '')
  .split('\n')
  .filter((line) => !line.trim().startsWith('//'))
  .join('\n');
assert.ok(
  !invocationRe.test(stripped),
  'src/hooks/session-start.js must NOT invoke gsdPhaseResolved — ...',
);
```

From src/cli/gsd-inspect.js (bloques actuales, líneas 59-76):
```js
// 1. Fetch task — best-effort; on fetch failure, exit 2 with message.
let task;
try {
  const provider = getProviderFn();
  task = await provider.getTask(opts.taskId);
} catch (e) {
  err(`Error fetching task ${opts.taskId}: ${/** @type {Error} */ (e).message}\n`);
  return 2;   // ← correcto per D-19
}

// 2. Resolve project path (may throw if task's project has no mapping).
let projectPath;
try {
  projectPath = resolveProjectPathFn(task);
} catch (e) {
  err(`Error resolving project path: ${/** @type {Error} */ (e).message}\n`);
  return 2;   // ← INCORRECTO (HI-02): config error, no fetch failure
}
```

From src/cli/gsd-inspect.js (JSDoc actual, línea 44):
```js
 * @returns {Promise<number>} exit code (0 success, 1 resolver error, 2 fetch failure)
```

From src/cli/gsd-inspect.js (comentario del header, línea 11):
```js
//   6. Exit code 0 si phase|bootstrap, 1 si error (D-19).
```

From test/gsd-inspect-cli.test.js (patrón de DI para tests de error path, líneas 166-172):
```js
// Ejemplo existente para getTask failure:
const exitCode = await runGsdInspect({ taskId: 'KL-0' }, {
  getProviderFn: () => ({
    init: async () => {},
    getTask: async () => { throw new Error('Task KL-0 not found'); },
  }),
  resolveProjectPathFn: () => '/tmp/fake-project',
  writeFn: () => {},
  errFn: () => {},
});
assert.strictEqual(exitCode, 2);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — GAP-01 src: eliminar bloque duplicado gsdBootstrap del hook</name>
  <files>src/hooks/session-start.js</files>
  <read_first>
    - src/hooks/session-start.js (archivo a modificar — sección 150-220)
    - .planning/phases/09-phase-resolver-bootstrap/09-04-SUMMARY.md (rationale del pattern-mapper #3 — misma migración ya hecha para gsd.phase.resolved)
    - .planning/phases/09-phase-resolver-bootstrap/09-VERIFICATION.md (GAP-01 detalle y líneas exactas)
  </read_first>
  <action>
    En `src/hooks/session-start.js`, **eliminar el bloque completo desde la línea 183 hasta la línea 200 inclusive** (ambos endpoints inclusive). Este bloque es:

    1. El comentario multilinea `// Phase 9 (pattern-mapper refinement #3): gsd.phase.resolved is emitted by / the DISPATCHER now ...` (líneas 183-187).
    2. El bloque `if (session.gsd && !session.phase_id) { try { ... gsdBootstrap(log, { project_path: session.project_path }); } catch { ... } }` (líneas 188-200).

    Tras la eliminación, la línea 181 (que cierra el try/catch del `sessionStart` emit) queda seguida directamente del comentario `// Output context for Claude Code to inject` (antes en línea 202). El bloque completo de emisión GSD desaparece del hook — la emisión de `gsd.bootstrap` queda en el dispatcher como única fuente (ya presente en `src/triggers/dispatcher.js:198-204`).

    **Añadir un comentario mínimo de 3 líneas justo antes de `// Output context for Claude Code to inject`** documentando la invariante (para operadores que lean el hook y se pregunten por qué no hay emisión GSD aquí):

    ```js
    // Phase 9 (pattern-mapper refinement #3, completado en 09-06): ni
    // gsd.phase.resolved ni gsd.bootstrap se emiten desde este hook. El
    // dispatcher es la fuente única (src/triggers/dispatcher.js).
    ```

    **NO tocar** ninguna otra parte del archivo: `buildGsdContext`, `main()`, el emit de `sessionStart`, ni los imports. El import de `gsdBootstrap` desde `logger-events.js` **NO existe en este archivo** (es un dynamic import dentro del try eliminado), por lo que no hay imports que remover.

    Verificar tras la edición con `grep -nE "gsdBootstrap|'gsd\\.bootstrap'" src/hooks/session-start.js` → debe retornar 0 matches (el comentario nuevo NO contiene esas cadenas literales).
  </action>
  <verify>
    <automated>
      bash -c "
        test \$(grep -cE \"gsdBootstrap|'gsd\\\\.bootstrap'\" src/hooks/session-start.js) -eq 0 &&
        node --check src/hooks/session-start.js
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "gsdBootstrap|'gsd\.bootstrap'" src/hooks/session-start.js` → **0**
    - `grep -c "session.gsd && !session.phase_id" src/hooks/session-start.js` → **0** (el guard desaparece junto al bloque)
    - `grep -c "pattern-mapper refinement #3" src/hooks/session-start.js` → **1** (el nuevo comentario documenta la invariante)
    - `node --check src/hooks/session-start.js` → **exit 0** (sintaxis válida tras eliminar)
    - `grep -c "buildGsdContext" src/hooks/session-start.js` → **≥ 2** (función no tocada, export + call en main)
    - El archivo pasa de ~220 líneas a ~205 líneas (eliminación neta ~18 líneas menos 3 añadidas = ~15 líneas net).
  </acceptance_criteria>
  <done>
    El bloque `if (session.gsd && !session.phase_id) { ... gsdBootstrap(...) }` ya no existe en `src/hooks/session-start.js`. El hook sólo emite `sessionStart`. La sintaxis es válida. Un comentario explicativo preserva la invariante para futuros lectores.
  </done>
</task>

<task type="auto">
  <name>Task 2 — GAP-01 test: invertir la aserción en test/session-start.test.js:156</name>
  <files>test/session-start.test.js</files>
  <read_first>
    - test/session-start.test.js (archivo a modificar — sección 120-174)
    - .planning/phases/09-phase-resolver-bootstrap/09-REVIEW.md (HI-01 muestra el test target y el refactor propuesto)
    - .planning/phases/09-phase-resolver-bootstrap/09-VERIFICATION.md (missing items #2: reemplazar test que pide activamente la emisión)
  </read_first>
  <action>
    En `test/session-start.test.js`, **reemplazar el test actual de las líneas 156-164** (actualmente afirma que el hook SIGUE invocando `gsdBootstrap`) por un test anti-regresión **análogo al existente de `gsdPhaseResolved` en líneas 134-154**. El reemplazo literal:

    Bloque a eliminar (líneas 156-164):
    ```js
      it('Phase 9: still invokes gsdBootstrap from hook for bootstrap sessions', () => {
        // Companion assertion: removing the phase-resolved emit must NOT remove the
        // bootstrap emit. Bootstrap events still originate from the hook.
        assert.match(
          source,
          /gsdBootstrap\s*\(/,
          'session-start.js must still invoke gsdBootstrap for bootstrap sessions',
        );
      });
    ```

    Bloque de reemplazo (mismo rango, paridad estructural con el test de `gsdPhaseResolved`):
    ```js
      it('Phase 9 (09-06 gap closure): does NOT emit gsd.bootstrap from hook (moved to dispatcher, pattern-mapper #3)', () => {
        // Anti-regression guard completing pattern-mapper #3 for gsd.bootstrap.
        // The dispatcher is now the single source for gsd.bootstrap emission
        // (src/triggers/dispatcher.js:198-204). Duplicating from the hook would
        // make `kodo logs --event gsd.bootstrap` double-count every bootstrap
        // dispatch — the exact regression GAP-01 / REVIEW HI-01 identified.
        //
        // Strip single-line and block comments to avoid false positives from
        // explanatory comments that may legitimately mention gsdBootstrap.
        const invocationRe = /gsdBootstrap\s*\(/;
        const stripped = source
          .replace(/\/\*[\s\S]*?\*\//g, '')
          .split('\n')
          .filter((line) => !line.trim().startsWith('//'))
          .join('\n');
        assert.ok(
          !invocationRe.test(stripped),
          'src/hooks/session-start.js must NOT invoke gsdBootstrap — that emission moved to src/triggers/dispatcher.js in Phase 9 (09-06) to avoid duplicate NDJSON entries',
        );
      });
    ```

    **NO tocar** los tests `Phase 9: does NOT emit gsd.phase.resolved from hook ...` (líneas 134-154) ni `Phase 9: buildGsdContext signature accepts opts with brief field` (líneas 166-173). Sólo se reemplaza el bloque 156-164.
  </action>
  <verify>
    <automated>node --test test/session-start.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "does NOT emit gsd.bootstrap from hook" test/session-start.test.js` → **1**
    - `grep -c "still invokes gsdBootstrap" test/session-start.test.js` → **0** (el test viejo desapareció)
    - `grep -c "assert.match" test/session-start.test.js` con posterior ripgrep de `gsdBootstrap`: el test antiguo (assert.match con `/gsdBootstrap\s*\(/`) ya no aparece. El test nuevo usa `assert.ok(!invocationRe.test(stripped))`.
    - `node --test test/session-start.test.js` → **exit 0** (todos los tests del archivo pasan, incluido el nuevo anti-regresión).
    - El test nuevo hace paridad estructural con el existente para `gsdPhaseResolved`: mismo strip de comentarios, misma forma de la regex, misma `assert.ok(!...)`.
  </acceptance_criteria>
  <done>
    `test/session-start.test.js` ya NO cementa el bug de doble emisión — asserta lo contrario. Los demás tests del archivo (incluido el de `gsdPhaseResolved`) siguen pasando. Todos los tests del archivo (`node --test test/session-start.test.js`) pasan.
  </done>
</task>

<task type="auto">
  <name>Task 3 — HI-02 src+test: clarificar exit codes de runGsdInspect (D-19)</name>
  <files>src/cli/gsd-inspect.js, test/gsd-inspect-cli.test.js</files>
  <read_first>
    - src/cli/gsd-inspect.js (archivo a modificar — sección 40-76)
    - test/gsd-inspect-cli.test.js (sección 160-180 como referencia del patrón DI para tests de error path)
    - .planning/phases/09-phase-resolver-bootstrap/09-CONTEXT.md D-19 (contrato literal: "exit 2 = provider fetch failure"; exit 1 = error semántico)
    - .planning/phases/09-phase-resolver-bootstrap/09-REVIEW.md HI-02 (recomendación primaria: `return 2 → return 1` en el catch de resolveProjectPathFn)
  </read_first>
  <action>
    **Cambio 1 — `src/cli/gsd-inspect.js` línea 75:** reemplazar `return 2;` por `return 1;` en el catch del `try { projectPath = resolveProjectPathFn(task); }`. Resultado:
    ```js
      // 2. Resolve project path. A missing mapping is a config error (semantic
      //    failure) — exit 1 per D-19, NOT 2. Exit code 2 is reserved for
      //    provider fetch failure (transient). Scripts with retry-on-2 must
      //    not reintentar cuando el operador necesita corregir config.
      let projectPath;
      try {
        projectPath = resolveProjectPathFn(task);
      } catch (e) {
        err(`Error resolving project path: ${/** @type {Error} */ (e).message}\n`);
        return 1;
      }
    ```

    Mantén la línea 66 (`return 2;` del catch de `provider.getTask`) **sin cambios** — ese caso SÍ es fetch failure y cumple D-19 literal.

    **Cambio 2 — `src/cli/gsd-inspect.js` JSDoc línea 44:** actualizar `@returns` para reflejar que `1` cubre tanto resolver error como config error. Reemplazar:
    ```js
     * @returns {Promise<number>} exit code (0 success, 1 resolver error, 2 fetch failure)
    ```
    por:
    ```js
     * @returns {Promise<number>} exit code per D-19:
     *   0 = verdict 'phase' or 'bootstrap' (happy path)
     *   1 = verdict 'error' (resolver) OR config error (project mapping missing)
     *   2 = provider.getTask fetch failure (transient, retryable)
    ```

    **Cambio 3 — `src/cli/gsd-inspect.js` comentario del header línea 11:** actualizar:
    ```js
    //   6. Exit code 0 si phase|bootstrap, 1 si error (D-19).
    ```
    por:
    ```js
    //   6. Exit codes (D-19): 0=phase|bootstrap, 1=verdict error OR config
    //      error, 2=provider fetch failure (transient).
    ```

    **Cambio 4 — `test/gsd-inspect-cli.test.js`:** añadir UN test nuevo que asserte el nuevo contrato. Localización: al final del archivo, antes del último `});` de cierre del `describe` principal. Contenido:

    ```js
      it('09-06: resolveProjectPath throw returns exit code 1 (config error ≠ fetch failure, D-19)', async () => {
        // HI-02 gap closure: distinguimos config error (exit 1) de fetch failure
        // (exit 2). Un project mapping ausente no es transient — reintentar
        // nunca va a funcionar hasta que el operador arregle la config.
        const exitCode = await runGsdInspect({ taskId: 'KL-42' }, {
          getProviderFn: () => ({
            init: async () => {},
            getTask: async () => ({ ref: 'KL-42', title: 'x', labels: [], project_id: 'p-missing' }),
          }),
          resolveProjectPathFn: () => {
            throw new Error('No local path mapped for project p-missing');
          },
          writeFn: () => {},
          errFn: () => {},
        });
        assert.strictEqual(exitCode, 1,
          'config error (resolveProjectPath throw) must return 1, not 2 (D-19 reserves 2 for fetch failure)');
      });
    ```

    **Cuidado:** usa `async () => ({ ... })` para `getTask` que retorna el task (NO throw); el throw sólo viene de `resolveProjectPathFn`. El objeto task minimal (`ref, title, labels, project_id`) basta porque el flujo aborta antes de llegar al resolver.

    **NO tocar** el test existente que asserta `exitCode === 2` para `getTask` throw (líneas ~166-180 del archivo) — sigue siendo válido y necesario para cubrir la rama de fetch failure.
  </action>
  <verify>
    <automated>node --test test/gsd-inspect-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "return 1;" src/cli/gsd-inspect.js | grep -c ":" → **≥ 2**` (uno en el catch de resolveProjectPath, y el return al final del función en verdict error — ya existente).
    - `grep -c "return 2;" src/cli/gsd-inspect.js` → **1** (sólo el del catch de provider.getTask sigue).
    - `grep -c "config error (project mapping missing)" src/cli/gsd-inspect.js` → **1** (JSDoc actualizado).
    - `grep -c "09-06: resolveProjectPath throw returns exit code 1" test/gsd-inspect-cli.test.js` → **1**.
    - `node --test test/gsd-inspect-cli.test.js` → **exit 0** (el test nuevo pasa + los 9 existentes siguen pasando → 10 tests total).
    - El test nuevo para fetch failure existente (`assert.strictEqual(exitCode, 2)` en líneas ~166-180) no ha sido tocado.
    - `node --check src/cli/gsd-inspect.js` → **exit 0**.
  </acceptance_criteria>
  <done>
    `runGsdInspect` retorna **1** para config error (resolveProjectPath throw) y **2** sólo para `provider.getTask` throw. El JSDoc y el comentario del header documentan los tres códigos. Un test anti-regresión impide reintroducir el overload. Los tests del archivo pasan todos (10/10 incluyendo el nuevo).
  </done>
</task>

</tasks>

<verification>
**Gap-closure verification (tras las tres tasks):**

1. **Regresión cero cross-file:**
   ```bash
   node --test test/*.test.js
   ```
   Debe terminar con `pass: 263+` (una suite subió de 9 a 10 tests en gsd-inspect-cli, resto invariante; tests antiguos eliminados compensados por nuevos). `fail: 0`.

2. **GAP-01 closed — grep invariants:**
   ```bash
   grep -cE "gsdBootstrap|'gsd\.bootstrap'" src/hooks/session-start.js   # → 0
   grep -c  "gsdBootstrap"                     src/triggers/dispatcher.js # → 0 (usa log.info directo, ya era así)
   grep -c  "log\.info.*gsd\.bootstrap"        src/triggers/dispatcher.js # → 1 (fuente única)
   grep -c  "does NOT emit gsd.bootstrap"      test/session-start.test.js # → 1
   grep -c  "still invokes gsdBootstrap"       test/session-start.test.js # → 0 (test invertido)
   ```

3. **HI-02 closed — exit-code contract:**
   ```bash
   grep -c "return 2;" src/cli/gsd-inspect.js                             # → 1 (solo fetch failure)
   grep -c "09-06: resolveProjectPath throw" test/gsd-inspect-cli.test.js # → 1
   ```

4. **Sanity re-run Phase 9 suites:**
   ```bash
   node --test test/dispatcher.test.js                # → 21/21 pass (sin regresión)
   node --test test/gsd-context.test.js               # → 12/12 pass
   node --test test/session-start.test.js             # → test antiguo reemplazado, suite entera pass
   node --test test/gsd-inspect-cli.test.js           # → 10/10 pass (incluye el nuevo)
   ```
</verification>

<success_criteria>
- `grep -cE "gsdBootstrap|'gsd\.bootstrap'" src/hooks/session-start.js` = **0** (GAP-01 src fix).
- `test/session-start.test.js` asserta que el hook NO invoca `gsdBootstrap` (antes cementaba lo contrario).
- `src/cli/gsd-inspect.js` retorna **1** para resolveProjectPath throw (**2** reservado para provider.getTask throw, D-19 literal).
- `test/gsd-inspect-cli.test.js` cubre el nuevo contrato con un test dedicado.
- `node --test test/*.test.js` — todos los tests pasan. Cero regresiones en Phase 6/7/8/9.
- `grep` invariants del bloque `<verification>` todos verdes.
- **NO se introduce scope de ME-01/ME-02/ME-03/LO-01..04** — ese scope queda explícitamente deferido.
</success_criteria>

<output>
Tras completar los tres tasks, crear `.planning/phases/09-phase-resolver-bootstrap/09-06-SUMMARY.md` documentando:

- Los dos gaps cerrados (GAP-01 / HI-01 y HI-02) con evidencia grep antes/después.
- Los 4 archivos modificados, LOC netos (~+25 / -20 ≈ +5 net).
- El commit (uno sólo — `fix(09-06): eliminar doble emisión gsd.bootstrap + clarificar exit codes kodo gsd inspect`).
- Invariante pattern-mapper #3 ahora completa para AMBOS eventos (`gsd.phase.resolved` + `gsd.bootstrap`).
- Confirmación de que `kodo logs --event gsd.bootstrap --count` ahora cuenta 1 por dispatch bootstrap (antes: 2).
- Defer explícito de ME-01/ME-02/ME-03 a Phase 10 y LO-01..04 a backlog indefinido.
- Re-verificación de Phase 9 ready (`/gsd-verify-phase 09` debe cerrar los 8/8 must-haves tras este plan).
</output>
