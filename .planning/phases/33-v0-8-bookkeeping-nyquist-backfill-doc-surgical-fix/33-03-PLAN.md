---
phase: 33-v0-8-bookkeeping-nyquist-backfill-doc-surgical-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gsd/verify.js
  - src/hooks/stop.js
  - test/gsd-verify-integration.test.js
  - test/stop.test.js
autonomous: true
requirements: []   # Phase-local marker: LIFE-02-FOLLOWUP es robustness gap (no nuevo REQ-ID). Contrato vinculante = CONTEXT D-01.

must_haves:
  truths:
    - "src/gsd/verify.js captura el return discriminado de markSessionStatus y, cuando result.ok === false, emite log.warn('markSessionStatus.skipped', {reason, session_id}) y continua (cero throws)"
    - "src/hooks/stop.js hace lo mismo DENTRO del try block existente (lineas ~195-202), cero throws nuevos"
    - "Tests netos (+2 a +4) asseran el evento NDJSON markSessionStatus.skipped con {reason, session_id} cuando ok === false, por callsite"
    - "Cero cambio E2E observable: markSessionStatus mismo NO se modifica (contrato Phase 30 LIFE-02 inmutable); task_id siempre presente en sesiones activas -> comportamiento runtime hoy identico"
    - "D-01: consumo simetrico log+continue en ambos callers con event name markSessionStatus.skipped (patron <componente>.<situacion>)"
  artifacts:
    - path: "src/gsd/verify.js"
      provides: "Callsite #1 consume el return y emite warn observable"
      contains: "markSessionStatus.skipped"
    - path: "src/hooks/stop.js"
      provides: "Callsite #2 consume el return y emite warn observable"
      contains: "markSessionStatus.skipped"
  key_links:
    - from: "src/gsd/verify.js + src/hooks/stop.js"
      to: "log.warn NDJSON event markSessionStatus.skipped"
      via: "if (!result?.ok) tras capturar const result = markSessionStatus(...)"
      pattern: "markSessionStatus\\.skipped"
---

<objective>
Cerrar el robustness gap LIFE-02-FOLLOWUP (Bloque C): los 2 callers de `markSessionStatus` (`src/gsd/verify.js:267` y `src/hooks/stop.js:197`) actualmente descartan el return discriminado `{ok, reason}`. Capturar el return y, cuando `ok === false`, emitir `log.warn('markSessionStatus.skipped', {reason, session_id})` y continuar — consumo simetrico log+continue, cero throws.

Purpose: Hacer observable en NDJSON el drift `missing-task-id` si emerge en runtime, sin cambiar el comportamiento E2E (hoy `task_id` siempre presente). Tier 1 extendido (toca src/ pero es robustness gap puro sin path E2E hot).
Output: 2 callsites actualizados + 2-4 tests netos de drift-observability. Target suite >=896 pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-v0-8-bookkeeping-nyquist-backfill-doc-surgical-fix/33-CONTEXT.md

<interfaces>
<!-- Contrato de markSessionStatus (Phase 30 LIFE-02) — INMUTABLE, el surgical fix lo CONSUME, no lo muta. -->
<!-- src/session/manager.js:362-396 -->

markSessionStatus(taskId, nextStatus, reason, logger, sessionId)
  retorna: {ok: true, from: string, to: string}
         | {ok: false, reason: 'missing-task-id'}
  (la rama ok:false ya emite internamente log.warn('markSessionStatus: missing task_id', ...) — el surgical fix anade observabilidad EN EL CALLER vía un event name distinto markSessionStatus.skipped)

<!-- INSIGHT CRITICO sobre verify.js:267 (leer el archivo, NO aplicar a ciegas el snippet de CONTEXT): -->
<!-- A diferencia de lo que dice CONTEXT (línea 96 "NO esta envuelto en try/catch"), -->
<!-- la llamada en verify.js SI esta dentro de un try { markSessionStatus(...) } catch {} (lineas ~266-270). -->
<!-- El catch silencia fallos de fs (CR-01 Phase 16, preserva invariante D-17 orchestratorReview). -->
<!-- El fix: capturar `const result = markSessionStatus(...)` DENTRO de ese try existente y anadir -->
<!-- el `if (!result?.ok) log.warn(...)` ahi mismo. NO anadir un try/catch nuevo. NO remover el existente. -->
<!-- markSessionStatus es non-throwing por contrato, asi que el warn no puede disparar el catch. -->

<!-- stop.js:197 SI esta dentro de un try block existente (lineas ~195-202) que ya captura -->
<!-- excepciones del dynamic import; el log.warn nuevo cabe DENTRO del mismo try (no anadir wrapper). -->
</interfaces>

# Wording sugerido del fix (CONTEXT <specifics> lineas 157-173): usar optional chaining `result?.ok`
# y `result?.reason` (defensivo contra mocks que retornan undefined; produccion siempre retorna el union).
# Event name normalizado al patron <componente>.<situacion> ya en uso (worktree.cleanup.dirty, markSessionStatus.failed).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consumir el return en verify.js:267 + test de drift-observability</name>
  <files>src/gsd/verify.js, test/gsd-verify-integration.test.js</files>
  <read_first>
    - src/gsd/verify.js (leer lineas ~240-280: la rama `verdict.action === 'pass' && commented`, el try/catch existente lineas ~266-270 que envuelve la llamada markSessionStatus, y el comentario CR-01 lineas ~258-265 que explica por que el catch silencia fs)
    - src/session/manager.js lineas ~362-396 (return shape de markSessionStatus — NO editar, solo confirmar el union {ok,from,to} | {ok,reason})
    - test/gsd-verify-integration.test.js (estructura de tests del flujo verify.js#finalize — como se inyecta el logger/memSink, como se mockea markSessionStatus o el provider, como se asseran eventos NDJSON)
  </read_first>
  <behavior>
    - Test 1 (drift observable): cuando markSessionStatus retorna `{ok: false, reason: 'missing-task-id'}` en la rama pass de finalize, el logger (memSink) contiene un evento `markSessionStatus.skipped` con `{reason: 'missing-task-id', session_id: <session.session_id>}`.
    - Test 2 (happy path no-regresion): cuando markSessionStatus retorna `{ok: true, ...}`, NO se emite `markSessionStatus.skipped` (el evento no aparece en el memSink).
  </behavior>
  <action>
    En `src/gsd/verify.js`, dentro del try existente (~266-270, NO anadir uno nuevo), cambiar la linea `markSessionStatus(session.task_id, 'review', 'gate-passed', log, session.session_id);` por capturar el return en `const result = markSessionStatus(...)` con los MISMOS argumentos, y debajo (aun dentro del try) anadir `if (!result?.ok) { log.warn('markSessionStatus.skipped', { reason: result?.reason, session_id: session.session_id }); }`. Usar optional chaining `result?.ok` / `result?.reason` (defensivo contra mocks que retornan undefined). NO modificar el catch silencioso ni el comentario CR-01. NO modificar markSessionStatus en manager.js. Anadir los 2 tests descritos en <behavior> a `test/gsd-verify-integration.test.js` reutilizando el patron de inyeccion de logger/memSink ya presente en ese archivo.
  </action>
  <verify>
    <automated>node --test test/gsd-verify-integration.test.js 2>&1 | grep -E '^# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `src/gsd/verify.js` contiene `const result = markSessionStatus(` en el callsite de la rama pass
    - `src/gsd/verify.js` contiene `markSessionStatus.skipped`
    - El try/catch existente (CR-01) sigue presente sin modificar; cero try/catch nuevos
    - `node --test test/gsd-verify-integration.test.js` reporta `# fail 0` y los 2 tests nuevos pasan
    - El test 1 assera el evento NDJSON `markSessionStatus.skipped` con `{reason, session_id}` cuando ok === false
  </acceptance_criteria>
  <done>verify.js consume el return y emite warn observable cuando ok===false; 2 tests netos GREEN; cero throws nuevos; contrato markSessionStatus intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consumir el return en stop.js:197 + test de drift-observability simetrico</name>
  <files>src/hooks/stop.js, test/stop.test.js</files>
  <read_first>
    - src/hooks/stop.js (leer lineas ~180-218: la construccion del logger ~185-193, el try block existente ~195-202 que hace el dynamic import + invoca markSessionStatus y captura excepciones con console.error, y el bloque sessionEnd ~207-217)
    - src/session/manager.js lineas ~362-396 (return shape — NO editar)
    - test/stop.test.js (estructura de tests del stop hook — como se construye el logger/memSink, como se mockea markSessionStatus o el dynamic import, como se asseran eventos NDJSON; tambien existe test/stop-state-transition.test.js como referencia de assert de eventos)
  </read_first>
  <behavior>
    - Test 1 (drift observable simetrico): cuando markSessionStatus retorna `{ok: false, reason: 'missing-task-id'}` en runStopHook, el logger contiene un evento `markSessionStatus.skipped` con `{reason: 'missing-task-id', session_id: <session.session_id>}`.
    - Test 2 (happy path no-regresion): cuando markSessionStatus retorna `{ok: true, ...}`, NO se emite `markSessionStatus.skipped`.
  </behavior>
  <action>
    En `src/hooks/stop.js`, DENTRO del try block existente (~195-202, no anadir wrapper), cambiar `markSessionStatus(session.task_id, 'done', 'session-stop', log, session.session_id);` por `const result = markSessionStatus(...)` con los MISMOS argumentos, y debajo (aun dentro del try) anadir `if (!result?.ok) { log.warn('markSessionStatus.skipped', { reason: result?.reason, session_id: session.session_id }); }`. Optional chaining `result?.ok` / `result?.reason`. NO tocar el `catch (err) { console.error(...) }` existente (WR-03), ni el bloque sessionEnd, ni el dynamic import. NO modificar markSessionStatus. Anadir los 2 tests descritos en <behavior> a `test/stop.test.js` reutilizando el patron de logger/memSink del archivo (referenciar test/stop-state-transition.test.js si necesita patron de assert de eventos).
  </action>
  <verify>
    <automated>node --test test/stop.test.js 2>&1 | grep -E '^# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/stop.js` contiene `const result = markSessionStatus(` dentro del try existente
    - `src/hooks/stop.js` contiene `markSessionStatus.skipped`
    - El catch existente (WR-03 console.error) y el bloque sessionEnd siguen sin modificar
    - `node --test test/stop.test.js` reporta `# fail 0` y los 2 tests nuevos pasan
    - El test 1 assera el evento NDJSON `markSessionStatus.skipped` con `{reason, session_id}` cuando ok === false
  </acceptance_criteria>
  <done>stop.js consume el return y emite warn observable simetrico cuando ok===false; 2 tests netos GREEN; cero throws nuevos; fail-open contract preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna nueva. El cambio es consumo log+continue del return de una funcion ya existente, dentro de callsites ya existentes. No introduce input handling, parsing, red, auth, ni exposicion de datos. `session_id` y `reason` ya estan en scope local; el `reason` es un literal kebab-case del enum interno (`'missing-task-id'`), no input externo.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33C-01 | N/A | log.warn observable en verify.js + stop.js | accept | No new input, network, or auth surface; log+continue only — cero throws nuevos, sin injection, sin privilege change. `reason` es enum interno, no input externo. No hay threats high. |
| T-33C-02 | I (Info Disclosure) | NDJSON event markSessionStatus.skipped | accept | El payload {reason, session_id} ya se loggea en otros eventos (markSessionStatus.failed, session.end). Sin PII nueva, mismo sink NDJSON local. |
</threat_model>

<verification>
- `src/gsd/verify.js` y `src/hooks/stop.js` capturan el return y emiten `markSessionStatus.skipped` cuando `!result?.ok`.
- Cero throws nuevos; try/catch existentes (CR-01 verify, WR-03 stop) intactos.
- `src/session/manager.js` (markSessionStatus) SIN modificar — contrato Phase 30 LIFE-02 inmutable.
- GATE DEL PLAN: correr SOLO los tests modificados, NO la suite global (ya verde a 894).
  - `node --test test/gsd-verify-integration.test.js` -> `# fail 0`
  - `node --test test/stop.test.js` -> `# fail 0`
- Delta neto: +2 a +4 tests. Target suite global post-phase >=896 (894 baseline + >=2). NO se gatea sobre la suite completa.
- Cero cambio E2E observable: task_id siempre presente en sesiones activas -> runtime hoy identico.
</verification>

<success_criteria>
- LIFE-02-FOLLOWUP: ambos callsites consumen el return discriminado con log+continue simetrico (D-01).
- Event name `markSessionStatus.skipped` (patron <componente>.<situacion>).
- +2 a +4 tests netos GREEN; gate corre solo los archivos modificados.
- Cero throws, cero cambio E2E, contrato markSessionStatus inmutable.
</success_criteria>

<output>
After completion, create `.planning/phases/33-v0-8-bookkeeping-nyquist-backfill-doc-surgical-fix/33-03-SUMMARY.md`
</output>
