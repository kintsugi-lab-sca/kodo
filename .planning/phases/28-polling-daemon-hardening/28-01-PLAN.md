---
phase: 28-polling-daemon-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface.js
  - src/providers/github/normalize.js
  - src/providers/plane/normalize.js
  - src/triggers/polling.js
  - test/providers/contract.test.js
  - test/providers/github/normalize.test.js
  - test/triggers/polling.test.js
autonomous: true
requirements:
  - POLL-FIX-01
requirements_addressed:
  - POLL-FIX-01
tags: [polling, normalize, taskitem, provider-only-path]

must_haves:
  truths:
    - "`getProvider('github').listPendingTasks()` retorna TaskItems con `updated_at` y `created_at` como strings ISO no-nulos"
    - "`getProvider('plane').listPendingTasks()` retorna TaskItems con `updated_at` y `created_at` como strings ISO no-nulos (paridad cross-provider)"
    - "`shouldDispatch(task, prev)` en path provider-only evalúa contra `task.updated_at` real (no `undefined`) y dispara cuando `task.updated_at > prev.last_updated_at`"
    - "`extractMaxUpdatedAt` (cursor cache) en path provider-only lee `task.updated_at` correctamente"
    - "Contract matrix Phase 27 ahora valida 9 asserts × 2 providers = 18 cases (antes 7 × 2 = 14)"
    - "Suite global ≥780 pass + 0 fail (777 baseline + ≥2 tests nuevos en este plan)"
  artifacts:
    - path: "src/interface.js"
      provides: "TaskItem typedef con 13 fields canónicos (11 + updated_at + created_at)"
      contains: "updated_at: string"
    - path: "src/providers/github/normalize.js"
      provides: "normalizeIssue emite updated_at + created_at"
      contains: "updated_at: issue.updated_at"
    - path: "src/providers/plane/normalize.js"
      provides: "normalizeWorkItem emite updated_at + created_at (paridad)"
      contains: "updated_at: workItem.updated_at"
    - path: "test/providers/contract.test.js"
      provides: "CANONICAL_TASK_ITEM_KEYS extendido a 13 + 2 type asserts en assertTaskItemShape"
      contains: "'updated_at'"
    - path: "test/providers/github/normalize.test.js"
      provides: "2 asserts passthrough literal + leak guard actualizado a 13 keys"
      contains: "result.updated_at"
    - path: "test/triggers/polling.test.js"
      provides: "caso provider-only path GREEN con TaskItem.updated_at disparando dispatch"
      contains: "provider-only"
  key_links:
    - from: "src/triggers/polling.js#shouldDispatch"
      to: "task.updated_at (TaskItem normalizado)"
      via: "string comparison"
      pattern: "task\\.updated_at"
    - from: "src/triggers/polling.js#processRepo (provider-only branch)"
      to: "maxUpdatedAt accumulator"
      via: "TaskItem.updated_at"
      pattern: "updated_at && .* > maxUpdatedAt"
    - from: "test/providers/contract.test.js"
      to: "assertTaskItemShape"
      via: "Type asserts en helper compartido"
      pattern: "typeof task\\.updated_at"
---

<objective>
Cerrar el v0.7 tech debt POLL-FIX-01: `normalizeIssue` y `normalizeWorkItem` excluían `updated_at`/`created_at` del TaskItem canónico (D-18 leak guard Phase 24), provocando que `shouldDispatch` en el provider-only path evaluara contra `undefined`. Esta phase extiende el contrato TaskItem de 11 a 13 fields canónicos (D-01 override) y propaga el cambio simétricamente a ambos normalizers + el contract matrix Phase 27.

Purpose: Garantizar que cualquier consumer que llame `getProvider('<x>').listPendingTasks()` y luego `shouldDispatch(task)` evalúe contra timestamps reales, sin importar el provider.
Output: TaskItem 13-field typedef, ambos normalizers emiten timestamps, contract matrix 18 cases, 3 archivos de test extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-polling-daemon-hardening/28-CONTEXT.md
@.planning/phases/28-polling-daemon-hardening/28-PATTERNS.md

@src/interface.js
@src/providers/github/normalize.js
@src/providers/plane/normalize.js
@src/triggers/polling.js
@test/providers/contract.test.js
@test/providers/github/normalize.test.js
@test/triggers/polling.test.js
@test/fixtures/github/issue.json
@test/fixtures/plane-workitem.json

<interfaces>
<!-- Contracts touched by this plan, extracted from existing codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From src/interface.js (current TaskItem typedef, lines 10-24 — pre-change shape):
- 11 canonical fields: id, ref, title, description, labels, projectId, projectName, groups, url, priority, state?
- Post-change shape: ADD `updated_at: string` + `created_at: string` (REQUIRED, NO `?` optional).

From src/providers/github/normalize.js#normalizeIssue (lines 83-105):
- Signature: `normalizeIssue(issue, context) → TaskItem`
- Current return omits `updated_at`/`created_at` (D-18 leak guard Phase 24).
- GitHub fixture `test/fixtures/github/issue.json` lines 16-17 already contain `created_at: "2026-05-14T07:00:00Z"`, `updated_at: "2026-05-14T08:00:00Z"`.

From src/providers/plane/normalize.js#normalizeWorkItem (lines 64-80):
- Signature: `normalizeWorkItem(workItem, context) → TaskItem`
- Current return omits `updated_at`/`created_at`.
- Plane fixture `test/fixtures/plane-workitem.json` lines 31-32 already contain `created_at: "2026-04-01T10:00:00.000Z"`, `updated_at: "2026-04-08T14:30:00.000Z"`.

From src/triggers/polling.js#shouldDispatch (lines 167-170):
- Signature: `shouldDispatch(issue, prev) → boolean`
- Body: `if (!prev.last_updated_at) return false; return issue.updated_at > prev.last_updated_at;`
- Post-change: rename param `issue` → `task` (semantic dualidad raw issue / TaskItem).

From src/triggers/polling.js#processRepo (lines 223-380):
- maxUpdatedAt accumulator at lines 286-290 reads `issue.updated_at`.
- En path provider-only, `issue` es ahora TaskItem normalizado (mismo shape post-D-02/D-03).

From test/providers/contract.test.js#CANONICAL_TASK_ITEM_KEYS (lines 77-89):
- Freeze array de 11 keys actualmente.
- `assertTaskItemShape(task, providerName)` helper recorre type asserts (lines 155-167).

From test/providers/github/normalize.test.js#CANONICAL_KEYS (lines 20-34):
- 11 keys actualmente. Leak guard test lines 57-65 mensaje "EXACTAMENTE 11 canonical TaskItem keys".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskItem 13 fields + normalizers simétricos</name>
  <files>src/interface.js, src/providers/github/normalize.js, src/providers/plane/normalize.js</files>
  <read_first>
    - src/interface.js (TaskItem typedef líneas 10-24, formato JSDoc canónico del repo)
    - src/providers/github/normalize.js (normalizeIssue líneas 83-105, JSDoc header 73-78, comentario línea 19 sobre "EXACTAMENTE 11 canonical fields")
    - src/providers/plane/normalize.js (normalizeWorkItem líneas 64-80)
    - test/fixtures/github/issue.json líneas 16-17 (verificar que el fixture trae los timestamps ISO)
    - test/fixtures/plane-workitem.json líneas 31-32 (verificar paridad)
  </read_first>
  <behavior>
    - Test 1 (D-01): TaskItem typedef declara `updated_at: string` y `created_at: string` REQUIRED (no opcional con `?`).
    - Test 2 (D-02): `normalizeIssue(issue, ctx).updated_at === issue.updated_at` (passthrough literal).
    - Test 3 (D-02): `normalizeIssue(issue, ctx).created_at === issue.created_at` (passthrough literal).
    - Test 4 (D-03): `normalizeWorkItem(item, ctx).updated_at === item.updated_at` (passthrough literal).
    - Test 5 (D-03): `normalizeWorkItem(item, ctx).created_at === item.created_at` (passthrough literal).
    - Test 6 (leak guard reformulado): TaskItem retornado por ambos normalizers tiene EXACTAMENTE 13 keys (zero leaks `pull_request`/`assignees`/`milestone`/`reactions`).
  </behavior>
  <action>
    En `src/interface.js` (líneas 10-24): extender el TaskItem typedef añadiendo dos campos REQUIRED `updated_at: string` y `created_at: string` justo después de `state?: string`. Añadir comentario inline `// D-01 Phase 28: REQUIRED ISO 8601 UTC string` en cada uno (mirror del estilo de los comentarios D-XX existentes en el repo). NO usar `?` opcional — D-01 dice REQUIRED.

    En `src/providers/github/normalize.js#normalizeIssue` (función líneas 82-102): añadir 2 properties al return object literal después de `state: issue.state`, en este orden: `updated_at: issue.updated_at,` (D-02 passthrough) seguido de `created_at: issue.created_at,` (D-02 passthrough). Mantener el estilo de comentarios trailing con `// D-02 Phase 28: ISO 8601 (always present)`. Actualizar el JSDoc header (líneas 73-78): cambiar la enumeración a 13 fields. Actualizar el comentario línea 19 "EXACTAMENTE 11 canonical fields" → "EXACTAMENTE 13 canonical fields (Phase 28 D-01)". Remover `created_at`/`updated_at` del enumeration de "leaks" en líneas 76-77 (ya NO son leaks).

    En `src/providers/plane/normalize.js#normalizeWorkItem` (función líneas 63-79): mirror simétrico — añadir 2 properties al return object literal después de `state: workItem.state_detail?.name || ...`: `updated_at: workItem.updated_at,` (D-03 paridad cross-provider) y `created_at: workItem.created_at,` (D-03 paridad cross-provider). SIN guard defensivo `|| ''` — D-01 REQUIRED prefiere fail-loud downstream que mascarar undefined.

    Implementación: editar los 3 archivos. No modificar el resto del comportamiento — cambios puramente aditivos sobre el return shape de funciones puras.
  </action>
  <verify>
    <automated>node --test test/providers/github/normalize.test.js test/providers/plane/normalize.test.js 2>&1 | grep -E "(pass|fail|ok)" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updated_at" src/interface.js` ≥ 1 (campo añadido al typedef).
    - `grep -c "created_at" src/interface.js` ≥ 1.
    - `grep "updated_at: issue.updated_at" src/providers/github/normalize.js` retorna 1 match.
    - `grep "created_at: issue.created_at" src/providers/github/normalize.js` retorna 1 match.
    - `grep "updated_at: workItem.updated_at" src/providers/plane/normalize.js` retorna 1 match.
    - `grep "created_at: workItem.created_at" src/providers/plane/normalize.js` retorna 1 match.
    - `grep -v '^[[:space:]]*[/*#]' src/providers/github/normalize.js | grep -c "EXACTAMENTE 11"` retorna 0 (texto reformulado a 13).
    - El test suite de ambos normalizers pasa (los tests actuales se actualizan en Task 2; este task NO debe romper tests que aún tienen `CANONICAL_KEYS` con 11 — por eso Task 2 corrige los tests inmediatamente después).
  </acceptance_criteria>
  <done>
    TaskItem 13-field typedef escrito. `normalizeIssue` y `normalizeWorkItem` emiten `updated_at` + `created_at` passthrough literal del payload raw. JSDoc header de `normalize.js` actualizado a 13 fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests normalize + contract matrix actualizados a 13 fields</name>
  <files>test/providers/github/normalize.test.js, test/providers/contract.test.js</files>
  <read_first>
    - test/providers/github/normalize.test.js (CANONICAL_KEYS líneas 20-34, primer test "normalizeIssue produces..." líneas 37-55, leak guard líneas 57-65)
    - test/providers/contract.test.js (CANONICAL_TASK_ITEM_KEYS líneas 77-89, assertTaskItemShape helper líneas 131-168, comment líneas 71-76 sobre "Los 11 fields canonical")
    - test/fixtures/github/issue.json líneas 16-17 (timestamps a usar como valores literales)
    - test/fixtures/plane-workitem.json líneas 31-32 (timestamps)
  </read_first>
  <behavior>
    - Test 1: `CANONICAL_KEYS` y `CANONICAL_TASK_ITEM_KEYS` ambas tienen length 13.
    - Test 2: assert literal `result.updated_at === '2026-05-14T08:00:00Z'` en github/normalize.test.js (valor del fixture).
    - Test 3: assert literal `result.created_at === '2026-05-14T07:00:00Z'` en github/normalize.test.js.
    - Test 4: leak guard `Object.keys(result).length === 13` (NO 11).
    - Test 5 (D-04 contract matrix): assertTaskItemShape valida `typeof task.updated_at === 'string'` para ambos providers.
    - Test 6 (D-04): assertTaskItemShape valida `typeof task.created_at === 'string'` para ambos providers.
  </behavior>
  <action>
    En `test/providers/github/normalize.test.js`:
    1. Extender `CANONICAL_KEYS` (líneas 20-34) añadiendo dos entries finales: `'updated_at'` y `'created_at'` (mismo formato literal-string del resto).
    2. En el primer test "normalizeIssue produces canonical 11-field TaskItem" (renombrar a "13-field"): justo después del assert sobre `result.priority` (línea ~50), añadir 2 nuevos asserts: `assert.equal(result.updated_at, '2026-05-14T08:00:00Z', 'D-02 Phase 28: updated_at passthrough literal')` y `assert.equal(result.created_at, '2026-05-14T07:00:00Z', 'D-02 Phase 28: created_at passthrough literal')`. Los valores literales DEBEN coincidir con el fixture `test/fixtures/github/issue.json` líneas 16-17.
    3. En el leak guard test (líneas 57-65): cambiar mensaje "EXACTAMENTE 11 canonical TaskItem keys" → "EXACTAMENTE 13 canonical TaskItem keys"; remover `'created_at'` y `'updated_at'` del enumeration de leaks listadas como ejemplo (líneas 63-64) — ya NO son leaks. Si el assert es `Object.keys(result).length === 11` cambiarlo a `=== 13`.

    En `test/providers/contract.test.js`:
    1. Extender `CANONICAL_TASK_ITEM_KEYS` (líneas 77-89) añadiendo `'updated_at'` y `'created_at'` al final, con comentario trailing `// D-04 Phase 28`.
    2. En el comentario JSDoc líneas 71-76 (o equivalente): reformular "Los 11 fields canonical" → "Los 13 fields canonical (Phase 28 D-01)".
    3. En `assertTaskItemShape(task, providerName)` (líneas 131-168): después del último type assert existente sobre los campos canónicos (e.g., del que valida `state`), añadir 2 type asserts nuevos en el mismo estilo: `assert.equal(typeof task.updated_at, 'string', \`[${providerName}] updated_at must be string ISO\`);` y `assert.equal(typeof task.created_at, 'string', \`[${providerName}] created_at must be string ISO\`);`.

    NO añadir nuevos `it()` top-level — los asserts viven dentro del helper compartido que se invoca en los `it()` existentes del for-loop (Pitfall #3 del patterns map).
  </action>
  <verify>
    <automated>node --test test/providers/github/normalize.test.js test/providers/contract.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'updated_at'" test/providers/github/normalize.test.js` ≥ 1 (key en CANONICAL_KEYS).
    - `grep -c "'created_at'" test/providers/github/normalize.test.js` ≥ 1.
    - `grep "result.updated_at" test/providers/github/normalize.test.js` retorna ≥ 1 match (assert passthrough literal).
    - `grep -c "'updated_at'" test/providers/contract.test.js` ≥ 1 (key en CANONICAL_TASK_ITEM_KEYS).
    - `grep "typeof task.updated_at" test/providers/contract.test.js` retorna 1 match (assertTaskItemShape type assert).
    - `grep "typeof task.created_at" test/providers/contract.test.js` retorna 1 match.
    - `node --test test/providers/github/normalize.test.js test/providers/contract.test.js` exit 0.
    - Plane normalizer test (si existe `test/providers/plane/normalize.test.js`) NO debe regresar — si tiene su propio CANONICAL_KEYS hay que actualizarlo también; verificar con `grep -l "CANONICAL_KEYS\|CANONICAL_TASK_ITEM_KEYS" test/providers/plane/` y actualizar igual si aplica.
  </acceptance_criteria>
  <done>
    Ambos tests pasan con 13 fields. Contract matrix Phase 27 valida `updated_at`/`created_at` como string ISO para Plane y GitHub. Leak guard reformulado a "EXACTAMENTE 13 canonical TaskItem keys".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shouldDispatch + processRepo provider-only GREEN + test polling caso provider-only</name>
  <files>src/triggers/polling.js, test/triggers/polling.test.js</files>
  <read_first>
    - src/triggers/polling.js líneas 167-170 (shouldDispatch), 223-380 (processRepo), 286-290 (maxUpdatedAt accumulator)
    - test/triggers/polling.test.js — pattern de `makeFakeProvider`, `makeFakeClient`, `createTestClock`, `drainMicrotasks` (usar grep para localizar)
  </read_first>
  <behavior>
    - Test 1 (D-05): cuando provider mock retorna TaskItem con `updated_at: '2026-05-15T10:00:00Z'` y el cursor cache tiene `last_updated_at: '2026-05-15T08:00:00Z'`, `shouldDispatch` retorna true y `dispatchTriggerFn` se invoca 1 vez.
    - Test 2 (first-tick skip preservado): cuando el cursor cache no tiene `last_updated_at` para el repo, el TaskItem con `updated_at` no dispara (first-tick skip T-25-04).
    - Test 3 (D-06 paridad): `extractMaxUpdatedAt` después del tick actualiza el cursor cache a `task.updated_at` del item más reciente.
  </behavior>
  <action>
    En `src/triggers/polling.js#shouldDispatch` (líneas 167-170): renombrar el parámetro `issue` → `task` y el cuerpo `issue.updated_at` → `task.updated_at`. NO cambiar el comportamiento — string comparison sobre `updated_at` sigue igual; el rename refleja la dualidad semántica (raw issue path client / TaskItem path provider). Mantener el comentario "first-tick skip (T-25-04)" en el `if (!prev.last_updated_at)`. Actualizar también el call site en línea 292 si pasa `issue` (puede quedar como `task` o `issue` — argumentos posicionales no requieren rename, solo se documenta semánticamente).

    En `src/triggers/polling.js#processRepo` (líneas 286-290): NO cambiar el código del accumulator — `issue.updated_at` sigue siendo correcto porque en el path client `issue` es raw issue (con `updated_at` GitHub-native) y en el path provider-only `issue` es ahora TaskItem con `updated_at` D-02/D-03. Mantener el guard `issue.pull_request continue` como defensa en profundidad (TaskItem nunca tendrá `pull_request`, pero el guard no daña).

    En `test/triggers/polling.test.js`: añadir un nuevo `it()` dentro del describe existente sobre el provider-only path (o crear un nuevo describe `'startPolling — POLL-FIX-01 provider-only path'`). Pattern:
    1. Setup: `const { clock, advance } = createTestClock();` (o equivalente al helper existente del file).
    2. Construir un TaskItem mock con los 13 campos canónicos: `id, ref, title, description, labels, projectId, projectName, groups, url, priority, state, updated_at: '2026-05-15T10:00:00Z', created_at: '2026-05-15T09:00:00Z'`. La `ref` debe ser parseable como `<projectId>#<number>` (e.g., `'octocat/hello#1'`).
    3. Pre-poblar el cursor cache (`statePath`) con `JSON.stringify({ 'octocat/hello': { last_updated_at: '2026-05-15T08:00:00Z' } })` usando `writeFileSync` para evitar el first-tick skip.
    4. Construir el provider mock: `const provider = makeFakeProvider({ listPendingTasks: async () => [taskItem] });` (usar el mismo factory que el resto del file).
    5. Construir el dispatch capture: `const dispatched = []; const dispatchTriggerFn = async (event) => { dispatched.push(event); };`.
    6. Llamar `handle = startPolling({ provider, repos: [{ owner: 'octocat', repo: 'hello' }], intervalSec: 60, clock, statePath, dispatchTriggerFn });`.
    7. `await drainMicrotasks()` (o el helper equivalente).
    8. Asserts: `assert.equal(dispatched.length, 1, 'TaskItem updated_at > cursor → dispatch fires');` y `assert.equal(dispatched[0].taskRef, 'octocat/hello#1');` (ajustar la shape del event según el código actual del file).
    9. Cleanup: `handle.stop()`.

    Si el helper `makeFakeProvider` no existe en el file, inspeccionar los tests del provider path existentes y replicar el factory inline (provider con métodos `listPendingTasks`, `init`, etc., implementados como stubs async que retornan los valores configurados).
  </action>
  <verify>
    <automated>node --test test/triggers/polling.test.js 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep "function shouldDispatch" src/triggers/polling.js` muestra signature renombrado a `(task, prev)` (o documentado el rename con comment si argumentos posicionales).
    - El nuevo test "provider-only path: TaskItem con updated_at dispara correctamente" (o nombre equivalente) existe en `test/triggers/polling.test.js` y pasa.
    - `node --test test/triggers/polling.test.js` exit 0, los casos provider path existentes siguen verdes (no-regresión).
    - `grep -c "kodo:" test/triggers/polling.test.js` o equivalente: el TaskItem mock incluye los 13 fields canónicos (zero "campos faltantes" en el mock).
  </acceptance_criteria>
  <done>
    `shouldDispatch` renombrado y semántica documentada como dual-mode. Test caso provider-only path GREEN existe y pasa, demostrando que `task.updated_at` (no `undefined`) es comparado contra el cursor cache. Path client (raw issue) sigue verde sin cambios.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → normalizeIssue | Untrusted payload field `updated_at`/`created_at` passthrough sin validación de shape. |
| Plane API → normalizeWorkItem | Untrusted payload field `updated_at`/`created_at` passthrough sin validación. |
| TaskItem → shouldDispatch | String comparison sobre `task.updated_at` confiando en ISO 8601 monotonic ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | T (Tampering) | normalizeIssue passthrough sin validar formato ISO | accept | GitHub API garantiza ISO 8601 UTC en `issue.created_at`/`updated_at` (documentado en GitHub REST API spec). Validación adicional añadiría coste sin proteger contra un GitHub comprometido. Si el campo es malformado, `shouldDispatch` string-compare degrada graceful (jamás dispara) — fail-closed natural. |
| T-28-02 | I (Information disclosure) | TaskItem leak guard contra campos GitHub-only/Plane-only adicionales | mitigate | Leak guard test reformulado en `test/providers/github/normalize.test.js` y `test/providers/contract.test.js` valida `Object.keys(result).length === 13` (no menos, no más). Bloquea regresión donde un normalizer pase `pull_request`, `assignees`, `milestone`, `reactions`, etc. |
| T-28-03 | D (DoS) | TaskItem mock test consume bytes sin límite | accept | Tests son sintéticos (fixture < 5KB). Cero exposure a payloads adversariales. |
| T-28-04 | R (Repudiation) | shouldDispatch usa string compare sobre updated_at | mitigate | Caso first-tick skip (T-25-04) preservado: si `prev.last_updated_at` falsy, retorna false — evita disparos espurios en el primer tick post-restart cuando el cursor cache aún no se ha sembrado. |

Block on: T-28-02 (high severity — leak guard es invariante crítico Phase 24 D-18 reformulado).
</threat_model>

<verification>
- Suite global ≥780 pass + 0 fail (777 baseline + ≥3 nuevos asserts cubiertos por este plan: github/normalize +2, contract +2 type asserts en 2 providers, polling provider-only +1 test = ≥5 nuevos asserts a nivel test count).
- 1 skip pre-existente preservado.
- `node --test` exit code 0.
</verification>

<success_criteria>
1. `getProvider('github').listPendingTasks()` retorna TaskItems con `updated_at`/`created_at` strings ISO no-nulos.
2. `getProvider('plane').listPendingTasks()` paridad — mismo shape.
3. `shouldDispatch(task, prev)` en path provider-only evalúa contra `task.updated_at` real, dispara correctamente cuando `task.updated_at > prev.last_updated_at`.
4. Contract matrix Phase 27: 18 cases (9 asserts × 2 providers) vs 14 previo.
5. Leak guard test reformulado a "EXACTAMENTE 13 canonical TaskItem keys".
6. ROADMAP Success Criterion 3 verificable: cualquier consumer del provider-only path obtiene timestamps reales.
</success_criteria>

<output>
After completion, create `.planning/phases/28-polling-daemon-hardening/28-01-SUMMARY.md`
</output>
