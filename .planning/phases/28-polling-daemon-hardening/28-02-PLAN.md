---
phase: 28-polling-daemon-hardening
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/logger-events.js
  - src/triggers/polling.js
  - src/cli/polling.js
  - src/cli.js
  - test/triggers/polling.test.js
  - test/cli/polling-verbose.test.js
autonomous: true
requirements:
  - DAEMON-01
requirements_addressed:
  - DAEMON-01
tags: [polling, daemon, verbose, observability, taxonomy]

must_haves:
  truths:
    - "Operador ejecuta `kodo polling start --no-daemon --verbose` y ve stdout estructurado por tick (timestamp ISO, repos polled, dispatch decisions, rate-limit remaining)"
    - "Sin `--verbose`, comportamiento silent/quiet actual se preserva (logger NDJSON sigue al sink raíz, sin output stdout adicional)"
    - "`polling.tick.summary` se emite AL FINAL del tick (después de iterar todos los repos), una vez por tick"
    - "TTY → columnar humano con colores via `createFormatter`; no-TTY o `--json` → NDJSON byte-determinístico (DX-06 preservado)"
    - "`rate_limit_remaining` es el mínimo cross-repo del tick; `null` si ningún repo retornó rate-limit header"
    - "Closed event taxonomy crece de 18 a 19 eventos en `src/logger-events.js`"
  artifacts:
    - path: "src/logger-events.js"
      provides: "EVENTS.POLLING_TICK_SUMMARY + helper pollingTickSummary"
      contains: "POLLING_TICK_SUMMARY: 'polling.tick.summary'"
    - path: "src/triggers/polling.js"
      provides: "Emisión de polling.tick.summary al final del tick + processRepo retorna {dispatched, rate_limit_remaining}"
      contains: "pollingTickSummary"
    - path: "src/cli/polling.js"
      provides: "Foreground subscriber para polling.tick.summary cuando --verbose activo"
      contains: "wrapLoggerForSummary"
    - path: "src/cli.js"
      provides: "Flag Commander --verbose (default false) en `kodo polling start`"
      contains: "'--verbose'"
    - path: "test/cli/polling-verbose.test.js"
      provides: "Integration test spawn foreground con --verbose verificando ≥1 línea por tick"
      contains: "polling.tick.summary"
  key_links:
    - from: "src/triggers/polling.js#startPolling tick loop"
      to: "pollingTickSummary helper"
      via: "Logger info emission al final del tick"
      pattern: "pollingTickSummary\\(.*logger"
    - from: "src/cli/polling.js#runForegroundPolling"
      to: "createFormatter(process.stdout)"
      via: "Logger wrapper que duplica polling.tick.summary a stdout"
      pattern: "createFormatter\\(process\\.stdout"
    - from: "src/cli.js (Commander)"
      to: "src/cli/polling.js#runPollingStartCli (opts.verbose)"
      via: "Commander flag --verbose propagation"
      pattern: "--verbose"
---

<objective>
Cerrar el v0.7 tech debt DAEMON-01: el operador no tenía visibilidad del comportamiento per-tick del daemon de polling. Esta phase añade el flag ortogonal `--verbose` que activa emisión de un evento cross-repo agregado `polling.tick.summary` (D-10) por tick. En foreground (`--no-daemon --verbose`), el evento se renderiza también a stdout via `createFormatter` (D-09) — TTY columnar humano con colores o NDJSON byte-determinístico según TTY-detect.

Purpose: Operador puede diagnosticar el comportamiento del polling sin parsear el NDJSON sink raíz — útil para shakedown post-config y troubleshooting interactive.
Output: Nuevo evento `polling.tick.summary` en la closed taxonomy, helper `pollingTickSummary`, emisión en el tick loop, foreground subscriber con TTY-aware rendering, flag Commander `--verbose`, test integration spawn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-polling-daemon-hardening/28-CONTEXT.md
@.planning/phases/28-polling-daemon-hardening/28-PATTERNS.md
@.planning/phases/28-polling-daemon-hardening/28-01-SUMMARY.md

@src/logger-events.js
@src/triggers/polling.js
@src/cli/polling.js
@src/cli.js
@src/cli/format.js
@test/triggers/polling.test.js
@test/cli/polling.test.js

<interfaces>
<!-- Contracts touched by this plan, extracted from existing codebase. -->

From src/logger-events.js (closed taxonomy):
- `EVENTS` is `Object.freeze({...})` with 18 entries (lines 46-65).
- Pattern: each event has uppercase const (e.g., `POLLING_TICK: 'polling.tick'`) + helper function (e.g., `pollingTick(logger, fields)`).
- Helper shape: `logger.info(EVENTS.X, { event: EVENTS.X, ...whitelisted_fields })`.
- Header comment line 13 lists 18 events — must update to 19.

From src/triggers/polling.js#startPolling (line 415 onwards):
- `tick()` async closure (lines 431-457) — iterates `opts.repos`, calls `processRepo` per repo, schedules next via `clock.setTimeout`.
- `processRepo({owner, repo, client, provider, logger, statePath, ...})` (lines 223-380) — currently returns void; emits `pollingTick` per-repo.

From src/cli/polling.js:
- `runPollingStartCli(opts, deps)` (line 204) — daemon-or-foreground dispatch.
- `runForegroundPolling({config, reposRaw})` (line 275) — imports startPolling lazy, wires logger, sets PID, SIGINT cleanup. No verbose subscriber yet.

From src/cli/format.js#createFormatter (lines 114-178):
- Factory: `createFormatter(stream, env?) → {cyan, dim, red, formatRow, formatTable, useColor, ...}`.
- Eager `useColor` resolution: NO_COLOR > FORCE_COLOR > stream.isTTY.
- DX-06 invariant: byte-determinism TTY/no-TTY when `--json`.

From src/cli.js (Commander registration, lines 67-130):
- `kodo polling start` command. Existing `.option(...)` patterns shown e.g. line 21-23.
- Must add `.option('--verbose', '...', false)` to the polling start command.

From test/cli/polling.test.js#makeFixture (lines 87-110, integration spawn pattern lines 179-185):
- Helper sets up HOME-isolated `~/.kodo/config.json` + `~/.kodo/.env` con `GITHUB_TOKEN=fake_token_for_test`.
- Spawn pattern: `spawn(process.execPath, [KODO_BIN, 'polling', 'start', '--no-daemon'], { env: {...process.env, HOME: tmpHome, GITHUB_TOKEN: 'fake', NO_COLOR: '1'} })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evento polling.tick.summary en taxonomy + helper pollingTickSummary</name>
  <files>src/logger-events.js, test/logger-events.test.js</files>
  <read_first>
    - src/logger-events.js header líneas 1-21 (enumeración de 18 eventos), typedef líneas 26-45, EVENTS líneas 46-65, helper `pollingTick` líneas 420-429 (template para el nuevo helper)
    - test/logger-events.test.js (si existe — buscar con `ls test/logger-events.test.js`; si existe, leer para entender el pattern de validación de inmutabilidad del map)
  </read_first>
  <behavior>
    - Test 1 (D-10): `EVENTS.POLLING_TICK_SUMMARY === 'polling.tick.summary'`.
    - Test 2: `EVENTS` sigue siendo `Object.frozen` y crece a 19 entries.
    - Test 3 (D-10): `pollingTickSummary(logger, {repos_polled, total_dispatches, rate_limit_remaining, repos})` invoca `logger.info(EVENTS.POLLING_TICK_SUMMARY, {event: 'polling.tick.summary', repos_polled, total_dispatches, rate_limit_remaining, repos})` — los 4 fields canónicos pasados al record + ZERO leaks (no body/title/payload).
    - Test 4 (T-25-02 invariant): el helper SOLO emite contadores + lista de repos string keys, jamás body/title/ref/payload raw.
  </behavior>
  <action>
    En `src/logger-events.js`:
    1. Header líneas 1-21: actualizar el comentario línea 13-14 (enumeración de 18 eventos) — añadir `polling.tick.summary` a la lista. El comentario línea 3 "18 eventos de ciclo de vida" → "19 eventos de ciclo de vida".
    2. Typedef JSDoc líneas 26-45 (Readonly map): añadir `POLLING_TICK_SUMMARY: 'polling.tick.summary',` después de `POLLING_ERROR` en el typedef.
    3. `EVENTS` const (líneas 46-65): añadir entry `POLLING_TICK_SUMMARY:    'polling.tick.summary',` después de `POLLING_ERROR:           'polling.error',`.
    4. Después del helper `pollingTick` (líneas 420-429 — usar como template estructural), añadir el nuevo helper `pollingTickSummary` con JSDoc completo que cubra: invariante T-25-02 (zero info disclosure), D-10 shape, D-12 (rate_limit null fallback), D-11 (per-repo `pollingTick` preservado sin cambios). El cuerpo del helper: `logger.info(EVENTS.POLLING_TICK_SUMMARY, { event: EVENTS.POLLING_TICK_SUMMARY, repos_polled: fields.repos_polled, total_dispatches: fields.total_dispatches, rate_limit_remaining: fields.rate_limit_remaining, repos: fields.repos })`. Whitelisted explicit fields — no spread.

    En `test/logger-events.test.js` (si existe; si NO existe, este task NO crea el archivo — solo asegura que cualquier test que enumere events incluya el nuevo; verificar con `grep -l "POLLING_TICK\b" test/`):
    - Si el test file existe y enumera el frozen map: añadir assert sobre `EVENTS.POLLING_TICK_SUMMARY === 'polling.tick.summary'` y `Object.keys(EVENTS).length === 19`.
    - Si el test enumera helpers: añadir assert de invocación `pollingTickSummary(mockLogger, {...4 fields...})` → mockLogger.info recibe el shape D-10 exacto.

    NO modificar el helper `pollingTick` existente (D-11: per-repo preservado).
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | tail -20; node -e "const {EVENTS, pollingTickSummary} = require('./src/logger-events.js'); console.log(EVENTS.POLLING_TICK_SUMMARY === 'polling.tick.summary' && Object.keys(EVENTS).length === 19 && typeof pollingTickSummary === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "POLLING_TICK_SUMMARY:" src/logger-events.js` retorna ≥ 2 matches (typedef + EVENTS const).
    - `grep "polling.tick.summary" src/logger-events.js` retorna ≥ 3 matches (typedef value + EVENTS value + helper invocation).
    - `grep "export function pollingTickSummary" src/logger-events.js` retorna 1 match.
    - `Object.keys(EVENTS).length === 19` (verificado vía node one-liner).
    - El helper `pollingTick` original (líneas 420-429) NO debe haber sido modificado — `git diff src/logger-events.js` muestra additions only.
    - Cero stdout/stderr writes desde el helper (LOG-12: este archivo no importa `console.*` ni FS).
  </acceptance_criteria>
  <done>
    Closed taxonomy crece a 19 eventos. Helper `pollingTickSummary` implementado con whitelist explícito de 4 fields (T-25-02 information-disclosure invariant). Tests de taxonomía verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emisión de polling.tick.summary al final del tick + test caso summary</name>
  <files>src/triggers/polling.js, test/triggers/polling.test.js</files>
  <read_first>
    - src/triggers/polling.js líneas 1-30 (imports), 223-380 (processRepo signature + return), 415-457 (startPolling + tick closure)
    - test/triggers/polling.test.js (pattern `makeFakeLogger(captureArray)`, `makeFakeClient`, `createTestClock`, `drainMicrotasks` — buscar con grep)
  </read_first>
  <behavior>
    - Test 1 (D-10): tras un tick con 2 repos exitosos, el logger captura EXACTAMENTE 1 evento `polling.tick.summary` (no más, no menos).
    - Test 2 (D-10 shape): el record contiene `repos_polled: 2`, `total_dispatches: 0`, `repos: ['a/b', 'c/d']`.
    - Test 3 (D-12): cuando el client retorna `rate_limit_remaining: 4823` para ambos repos, `summary.rate_limit_remaining === 4823`.
    - Test 4 (D-12): cuando un repo retorna `rate_limit_remaining: 4823` y otro `1500`, el summary contiene el MÍNIMO `1500` (cross-repo conservative).
    - Test 5 (D-12 null fallback): cuando ningún repo retorna rate-limit (e.g., todos 304 cache hit), `summary.rate_limit_remaining === null`.
    - Test 6 (D-11): el evento `polling.tick` per-repo se sigue emitiendo (no se ha roto el helper existente).
  </behavior>
  <action>
    En `src/triggers/polling.js`:
    1. Imports: añadir `pollingTickSummary` al import existente de `./logger-events.js` o `../logger-events.js` (el path correcto se ve en los imports actuales).
    2. `processRepo` (líneas 223-380): cambiar el return shape de void a `{dispatched: number, rate_limit_remaining: number|null}`. Acumular un contador `let dispatched = 0;` al inicio de la función, incrementar `dispatched++` en cada path donde se llama `dispatchTriggerFn`. Capturar `rate_limit_remaining` del response del client cuando esté disponible (en el path client directo, `apiResp.rate_limit_remaining`; en path provider-only no hay rate-limit accessible directamente — retornar `null`). Return `{dispatched, rate_limit_remaining}` al final de las branches success; en error branches retornar `{dispatched: 0, rate_limit_remaining: null}`.
    3. `tick()` closure (líneas 431-457): antes del `for (const {owner, repo} of opts.repos)`, declarar acumuladores: `let totalDispatched = 0; let minRateLimit = null; const reposPolled = [];`. Dentro del loop: tras `await processRepo({...})` capturar `const repoSummary = await processRepo(...);`, push `reposPolled.push(\`${owner}/${repo}\`);` ANTES del await (para que aparezca incluso si processRepo throws). Acumular: `totalDispatched += repoSummary.dispatched;` y si `repoSummary.rate_limit_remaining != null`, `minRateLimit = minRateLimit == null ? repoSummary.rate_limit_remaining : Math.min(minRateLimit, repoSummary.rate_limit_remaining);`. Después del `for` loop, antes del `if (!stopped) timer = clock.setTimeout(tick, intervalMs);`: emitir `if (opts.logger && !stopped) { pollingTickSummary(opts.logger, { repos_polled: reposPolled.length, total_dispatches: totalDispatched, rate_limit_remaining: minRateLimit, repos: reposPolled }); }`.

    En `test/triggers/polling.test.js`: añadir nuevo `it()` (o describe) `'emite polling.tick.summary AL FINAL del tick con shape D-10'`:
    1. Setup clock virtual + `events = []; const logger = makeFakeLogger(events);` (usar el helper existente; si la API del logger en tests es `makeFakeLogger(arr)`, replicar).
    2. Construir fake client que retorna `{status: 200, items: [], etag: undefined, rate_limit_remaining: 4823}` para `listIssues`.
    3. `handle = startPolling({ client, repos: [{owner:'a', repo:'b'}, {owner:'c', repo:'d'}], intervalSec: 60, clock, statePath, logger });`.
    4. `await drainMicrotasks();` (helper existente o equivalente).
    5. Asserts:
       - `const summaries = events.filter(e => e.msg === 'polling.tick.summary');` (ajustar `msg` al field correcto que use el makeFakeLogger del file — puede ser `event`, `msg`, etc.).
       - `assert.equal(summaries.length, 1, 'exactamente 1 summary por tick');`
       - `assert.equal(summaries[0].repos_polled, 2);`
       - `assert.equal(summaries[0].total_dispatches, 0);`
       - `assert.equal(summaries[0].rate_limit_remaining, 4823);`
       - `assert.deepEqual(summaries[0].repos, ['a/b', 'c/d']);`
    6. Caso adicional (D-12 mínimo): variant del test donde el fake client returns distinto `rate_limit_remaining` por repo (4823 vs 1500) → assert `summaries[0].rate_limit_remaining === 1500`.
    7. Caso null fallback: fake client returns sin field rate_limit → assert `summaries[0].rate_limit_remaining === null`.
    8. Cleanup: `handle.stop();`.

    NO tocar el helper `pollingTick` per-repo emission existente. NO tocar el path provider-only del test del Plan 28-01 — su test sigue verde porque el provider-only branch retorna `{dispatched, rate_limit_remaining: null}` que se acumula correctamente.
  </action>
  <verify>
    <automated>node --test test/triggers/polling.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pollingTickSummary" src/triggers/polling.js` retorna ≥ 2 matches (import + call).
    - `grep "totalDispatched\|reposPolled\|minRateLimit" src/triggers/polling.js` retorna ≥ 3 matches (los 3 acumuladores).
    - `grep -A 3 "function processRepo" src/triggers/polling.js | grep -c "return"` ≥ 1 (processRepo ahora returns).
    - El nuevo test `'emite polling.tick.summary AL FINAL del tick'` pasa con 3 sub-casos (sample, mínimo cross-repo, null fallback).
    - `node --test test/triggers/polling.test.js` exit 0; los casos pre-existentes siguen verdes (no-regresión del helper `pollingTick` per-repo).
    - El test del Plan 28-01 (provider-only path GREEN) NO regresa.
  </acceptance_criteria>
  <done>
    `processRepo` retorna `{dispatched, rate_limit_remaining}`. `tick()` acumula cross-repo y emite `polling.tick.summary` exactamente 1 vez por tick AL FINAL del loop. Per-repo `polling.tick` preservado (D-11). Tests cubren happy path + D-12 mínimo + null fallback.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flag --verbose Commander + foreground subscriber + test integration spawn</name>
  <files>src/cli.js, src/cli/polling.js, test/cli/polling-verbose.test.js</files>
  <read_first>
    - src/cli.js (registración de `kodo polling start` — buscar con `grep -n "polling.*start\|--no-daemon" src/cli.js`)
    - src/cli/polling.js líneas 204-225 (runPollingStartCli entry point), 275-307 (runForegroundPolling)
    - src/cli/format.js#createFormatter (líneas 114-178 — métodos disponibles: cyan, dim, formatRow)
    - test/cli/polling.test.js (makeFixture líneas 87-110, spawn pattern líneas 179-185)
  </read_first>
  <behavior>
    - Test 1 (D-08): `kodo polling start --help` muestra `--verbose` con default false.
    - Test 2 (D-07 ortogonalidad): `--verbose` se acepta junto a `--no-daemon` y junto a daemon-default.
    - Test 3 (AC#1 ROADMAP): spawn `kodo polling start --no-daemon --verbose` con HOME-isolated config + fake GitHub token, esperar ≥1 tick, capturar stdout, verificar ≥1 línea contiene `polling.tick.summary` o las keys del shape D-10 (repos_polled, total_dispatches).
    - Test 4 (sin --verbose silencio): spawn `kodo polling start --no-daemon` (sin verbose) → stdout NO contiene `polling.tick.summary` (logger NDJSON sigue al sink raíz, no a stdout).
    - Test 5 (no-TTY bypass): cuando el spawn redirige stdout (no-TTY), la línea de summary es NDJSON byte-deterministic (`JSON.stringify(record) + '\n'`).
  </behavior>
  <action>
    En `src/cli.js`: localizar la registración del comando `kodo polling start` (probablemente línea ~120-130 según el grep del context). Añadir un nuevo `.option('--verbose', 'Emit polling.tick.summary line per tick to stdout (foreground) or logfile (daemon)', false)` al `.command('polling start')` o equivalente. Mantener el orden y formato de las option ya existentes (e.g., `.option('--no-daemon', ...)`).

    En `src/cli/polling.js`:
    1. `runPollingStartCli(opts, deps)` (línea 204): asegurar que `opts.verbose` se propaga. En la llamada a `runForegroundPolling(...)` (línea 225), añadir `verbose: opts.verbose` al objeto argumento: `return runForegroundPolling({ config, reposRaw, verbose: opts.verbose });`. En la branch daemon (línea ~247-252), añadir `...(opts.verbose ? ['--verbose'] : [])` al array de argumentos pasados a `spawn` para que el daemon child reciba la flag.
    2. `runForegroundPolling({config, reposRaw, verbose})` (línea 275): aceptar el nuevo `verbose` parameter. Importar `createFormatter` lazy desde `./format.js`. Antes de crear el handle, si `verbose === true`, wrappear el logger:
       - Definir helper local `wrapLoggerForSummary(baseLogger, fmt, stream)` que retorne un proxy del logger. El proxy.info delega al baseLogger.info y, si `event === 'polling.tick.summary'`, también escribe a `stream`:
         - Si `stream.isTTY && !process.env.KODO_JSON`: formatear via `fmt.formatRow([fmt.dim(record.ts || new Date().toISOString()), fmt.cyan('polling.tick.summary'), \`repos=${record.repos_polled}\`, \`dispatched=${record.total_dispatches}\`, \`rl=${record.rate_limit_remaining ?? '—'}\`], [], { separator: ' · ' })` (verificar el method signature actual de `formatRow` antes de adoptarlo; si la API difiere, usar `formatTable` o construir la línea manualmente con `fmt.dim`/`fmt.cyan`).
         - Sino: `stream.write(JSON.stringify(record) + '\n')` (NDJSON byte-determinístico, DX-06).
       - Otros métodos del proxy (warn, error, debug) delegan transparentemente al baseLogger.
     3. Inyectar `const fmt = createFormatter(process.stdout);` y `const logger = verbose ? wrapLoggerForSummary(baseLogger, fmt, process.stdout) : baseLogger;` antes de `startPolling(...)`.
     4. Pasar `logger` al `startPolling` (no `baseLogger`).

    Crear `test/cli/polling-verbose.test.js` (nuevo file):
    1. Pattern mirror de `test/cli/polling.test.js` (importar `makeFixture` helper o duplicar inline).
    2. `before/afterEach` HOME-isolated: `mkdtempSync(join(tmpdir(), 'kodo-verbose-'))`, set `process.env.HOME = tmpHome`, restore after.
    3. Caso 1 — foreground verbose emite summary a stdout:
       - `makeFixture({tmpHome, githubToken: 'fake_token_for_test'})` → escribe `~/.kodo/config.json` con un repo válido y stub fetch.
       - `const child = spawn(process.execPath, [KODO_BIN, 'polling', 'start', '--no-daemon', '--verbose'], { env: {...process.env, HOME: tmpHome, GITHUB_TOKEN: 'fake_token_for_test', NO_COLOR: '1'} });`
       - Acumular stdout: `let out = ''; child.stdout.on('data', d => out += d.toString());`.
       - Esperar ≤5s (timeout claro): `await new Promise(r => setTimeout(r, 3000));`.
       - `child.kill('SIGINT');` y esperar `child.on('exit')`.
       - Assert: `assert.match(out, /polling\.tick\.summary/, 'stdout contiene summary event name');` y `assert.match(out, /repos_polled|repos=/);` (cubre tanto NDJSON `"repos_polled":` como columnar `repos=`).
    4. Caso 2 — sin verbose, stdout NO contiene summary:
       - Mismo setup pero spawn sin `--verbose`.
       - Assert `assert.doesNotMatch(out, /polling\.tick\.summary/, 'sin --verbose: silent stdout');`.
    5. Caso 3 — `--verbose --json` byte-determinístico:
       - Spawn con `--no-daemon --verbose` + `KODO_JSON=1` (o equivalent flag para forzar NDJSON).
       - Assert: cada línea con `polling.tick.summary` parsea como JSON válido (`JSON.parse(line).event === 'polling.tick.summary'`).

    Si el spawn real con fake token requiere mocking de GitHub HTTP, usar nock o stub similar — si ya hay un pattern en `test/cli/polling.test.js`, replicarlo. Si no hay fetch interception y la suite real falla por 401, marcar el test como integration-skip y dejarlo en TODO documentado (preferible: stub con MSW o similar wrapper si existe en el repo).
  </action>
  <verify>
    <automated>node --test test/cli/polling-verbose.test.js 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'--verbose'" src/cli.js` retorna ≥ 1 match (option registrada).
    - `node bin/kodo.js polling start --help 2>&1 | grep -c "verbose"` ≥ 1 (flag visible en help).
    - `grep "wrapLoggerForSummary\|createFormatter" src/cli/polling.js` retorna ≥ 2 matches (helper + factory invocation).
    - `grep "verbose" src/cli/polling.js` retorna ≥ 3 matches (option dispatch + foreground + daemon spawn).
    - `test/cli/polling-verbose.test.js` existe con ≥ 2 casos (foreground verbose ON, foreground sin verbose silent).
    - `node --test test/cli/polling-verbose.test.js` exit 0.
    - Test integration NO debe demorar > 30s (timeouts claros con `child.kill`).
    - `picocolors` NO importado en `src/cli/polling.js` (color isolation D-07 Phase 14) — `grep "picocolors" src/cli/polling.js` retorna 0.
  </acceptance_criteria>
  <done>
    `--verbose` flag registrado en Commander, propagado de daemon spawn al child, y wired en foreground subscriber. Logger wrapper duplica `polling.tick.summary` a stdout via `createFormatter` (TTY columnar) o NDJSON (no-TTY). Test integration spawn con HOME-isolated config valida AC#1: ≥1 línea por tick en stdout cuando `--verbose --no-daemon`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `polling.tick.summary` record → logger.info | Whitelisted field list (4 keys); ningún field de la API raw entra al record. |
| Foreground spawn stdout → operator terminal | Output puede contener `repos` lista (owner/repo strings) — info publicación del operador, no untrusted. |
| Daemon spawn args → child process | `--verbose` propagado como argument literal, sin interpolación de input usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | I (Information disclosure) | `pollingTickSummary` whitelist | mitigate | Helper usa whitelist explícito de 4 fields (`repos_polled`, `total_dispatches`, `rate_limit_remaining`, `repos`). JAMÁS spread `...fields`. Test de taxonomy (Task 1) assertea zero leaks. T-25-02 invariant Phase 25 preservado. |
| T-28-06 | T (Tampering) | wrapLoggerForSummary stream write | accept | Wrapper escribe a `process.stdout` solo. No FS, no exec, no eval. Stream redirection se hace por el shell del operador. |
| T-28-07 | E (Elevation of privilege) | Daemon child recibe `--verbose` arg | accept | Argument literal estático, sin interpolación. No introduce nueva vector de injection. |
| T-28-08 | D (DoS) | Verbose stdout buffer overflow en tick rate alto | accept | Default `intervalSec=60` impide tick rate alto. Stdout buffer del SO maneja backpressure. Si el operador sustituye con `--interval 1`, el problema es operacional, no técnico. |
| T-28-09 | I (Information disclosure) | `repos` lista en summary contiene owner/repo strings | accept | Owner/repo son public-by-config (`~/.kodo/config.json` set por el operador). No es PII. Mismo nivel de disclosure que el evento `polling.tick` per-repo existente (Phase 25). |

Block on: T-28-05 (high severity — la whitelist es invariante crítico de la closed taxonomy + T-25-02).
</threat_model>

<verification>
- `kodo polling start --no-daemon --verbose` foreground emite ≥1 línea `polling.tick.summary` a stdout en formato TTY columnar o NDJSON según `isTTY`.
- `kodo polling start --no-daemon` (sin verbose) stdout silent preservado (logger NDJSON sigue al sink raíz).
- Closed taxonomy ahora tiene 19 eventos. `pollingTick` per-repo emisión preservada (D-11).
- Test suite global ≥781 pass (777 baseline + 1 nuevo de Plan 28-01 + ≥3 nuevos de este plan).
- 0 regresiones en tests Phase 25 (polling channel) y Phase 26 (config wizard + CLI).
</verification>

<success_criteria>
1. ROADMAP Success Criterion 1: operador ve línea estructurada por tick en stdout cuando ejecuta `--no-daemon --verbose`, con timestamp ISO + repos_polled + total_dispatches + rate_limit_remaining.
2. Daemon + `--verbose` propaga la flag al child spawn (los summary lines aparecen en el logfile vía fd redirect del Plan 28-03).
3. Format consistente con `kodo logs` (TTY columnar humano / no-TTY NDJSON byte-determinístico).
4. `pollingTickSummary` whitelist field-by-field preservada — zero info disclosure leaks.
</success_criteria>

<output>
After completion, create `.planning/phases/28-polling-daemon-hardening/28-02-SUMMARY.md`
</output>
