---
phase: 45-inyecci-n-de-plan-ligero-universal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/session-start.js
  - test/session-start.test.js
  - test/gsd-context.test.js
autonomous: true
requirements: [PLAN-03]

must_haves:
  truths:
    - "En sesiones non-GSD, el contexto de buildSessionContext incluye una instrucción ES de escribir un plan corto a ~/.kodo/plans/<task_id>.md con la ruta absoluta resuelta (homedir expandido + task_id real), no el literal <task_id> (D-01, D-02, D-05, D-08, D-09)"
    - "En sesiones quick (/gsd-quick), la rama mode === 'quick' de buildGsdContext incluye una instrucción EN equivalente con la misma ruta resuelta (D-04, D-08)"
    - "Las ramas phase y bootstrap de buildGsdContext NO reciben la instrucción y su bloque común '## No automatic push' permanece byte-idéntico en las 3 ramas (D-04, HOOK-02)"
    - "La instrucción es append-al-final (golden-bytes preservados): los bloques previos de ambos builders quedan byte-idénticos a la baseline (HOOK-02)"
    - "El hook no realiza I/O: la ruta se computa con join(KODO_DIR, 'plans', `${session.task_id}.md`) dentro del builder puro; la escritura del fichero la hace la sesión (D-03)"
    - "La instrucción inyectada es una sola línea imperativa, sin ceremonia — mantiene quick ligero (D-07)"
    - "La instrucción dirige a la sesión a escribir el plan al empezar; un re-dispatch de la misma task sobrescribe ~/.kodo/plans/<task_id>.md (latest-wins, D-06)"
  artifacts:
    - path: "src/hooks/session-start.js"
      provides: "Inyección de instrucción de plan ligero ES (buildSessionContext) + EN (rama quick de buildGsdContext) con ruta resuelta bajo KODO_DIR"
      contains: "import { KODO_DIR }"
    - path: "test/session-start.test.js"
      provides: "Cobertura non-GSD: presencia de instrucción ES, ruta resuelta, ausencia del literal <task_id>"
      contains: "plans"
    - path: "test/gsd-context.test.js"
      provides: "Cobertura quick: instrucción EN presente en quick, ausente en phase/bootstrap; invariancia del bloque común sigue verde"
      contains: "write a short plan"
  key_links:
    - from: "src/hooks/session-start.js"
      to: "src/config.js (KODO_DIR)"
      via: "import { KODO_DIR } from '../config.js'"
      pattern: "import \\{ KODO_DIR \\} from '\\.\\./config\\.js'"
    - from: "src/hooks/session-start.js"
      to: "~/.kodo/plans/<task_id>.md (ruta inyectada como texto)"
      via: "join(KODO_DIR, 'plans', `${session.task_id}.md`)"
      pattern: "join\\(KODO_DIR, 'plans'"
---

<objective>
Inyectar en `src/hooks/session-start.js` la instrucción de escribir un plan ligero a `~/.kodo/plans/<task_id>.md` en los dos sitios que hoy NO producen `PLAN.md`: (a) `buildSessionContext` entero (non-GSD, ES); (b) la rama `mode === 'quick'` de `buildGsdContext` (quick, EN). Las ramas phase y bootstrap quedan excluidas y byte-idénticas (D-04). La adición es append-al-final preservando golden-bytes (HOOK-02). El hook no escribe nada: solo computa el string con la ruta resuelta; la sesión de Claude escribe el fichero en runtime (D-03).

Purpose: Cierra PLAN-03 — habilita que el overlay de Phase 46 lea un plan ligero universal correlacionado por `task_id`, sin depender de hooks/rutas no documentados de Claude Code.
Output: `src/hooks/session-start.js` extendido + casos de test en `test/session-start.test.js` y `test/gsd-context.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-inyecci-n-de-plan-ligero-universal/45-CONTEXT.md
@.planning/phases/45-inyecci-n-de-plan-ligero-universal/45-RESEARCH.md
@.planning/phases/45-inyecci-n-de-plan-ligero-universal/45-PATTERNS.md
@src/hooks/session-start.js
@src/config.js
@test/session-start.test.js
@test/gsd-context.test.js

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase. No explorar de nuevo. -->

From src/hooks/session-start.js (firmas — funciones PURAS, sin I/O):
  export function buildSessionContext(session, config): string   // bloque ES non-GSD; return [...].join('\n')
  export function buildGsdContext(session, opts = {}): string     // GSD; usa lines.push(...); bifurca por getSessionMode(session)

  Imports actuales (L8-11) — NO incluyen join ni KODO_DIR:
    import { fileURLToPath } from 'node:url';
    import { findSession } from '../session/state.js';
    import { loadConfig } from '../config.js';
    import { getSessionMode } from '../labels.js';

  Anclas de edición:
    - buildSessionContext: el array `return [...]` termina en la línea Good de Anti-push:
        '   - Good: "Deploy quedará efectivo una vez se haga `git push origin main`."',   // última línea actual, justo antes de `].join('\n')`
    - buildGsdContext: la rama `if (mode === 'quick')` hace un único `lines.push(...)` cuya última línea actual es:
        'Run the slash command and finish — no plan/execute/verify cycle.',
      El bloque común `lines.push('', '## No automatic push', ...)` va FUERA del if/else, DESPUÉS de las 3 ramas — NO se toca.

From src/config.js:
  export { KODO_DIR, ... };           // KODO_DIR = join(homedir(), '.kodo')  — const evaluada al import, NO I/O en runtime
                                       // exportado verificado en src/config.js:228

From src/labels.js:
  getSessionMode(session) === 'quick'  // solo si session.gsd === true && session.gsd_mode === 'quick'

From test/session-start.test.js:
  import { dirname, join } from 'node:path';   // `join` YA importado (L6); `KODO_DIR` NO — añadir import
  function makeSession(overrides) → { task_id: 'uuid-123', summary: 'Fix bug', gsd?: ..., ... }  // fixture reusable
  function makeConfig(overrides) → { provider: 'plane', providers: { plane: { mcp_hint } } }

From test/gsd-context.test.js:
  const HEADER = '## No automatic push';
  const tail = (s) => s.slice(s.lastIndexOf(HEADER));        // patrón de invariancia
  // Test EXISTENTE a NO romper — D-04 common-block invariance (L160-169):
  //   assert.equal(tail(ctxQuick), tail(ctxPhase));  assert.equal(tail(ctxPhase), tail(ctxBoot));
  // makeSession en este fichero ya pone gsd:true por defecto.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inyectar instrucción ES en buildSessionContext (non-GSD) + tests</name>
  <files>src/hooks/session-start.js, test/session-start.test.js</files>
  <read_first>
    - src/hooks/session-start.js (L8-11 imports actuales; L29-79 bloque buildSessionContext + comentario HOOK-02 a replicar)
    - src/config.js:228 (export de KODO_DIR), src/config.js:6 (definición KODO_DIR = join(homedir(), '.kodo'))
    - src/session/state.js:3-4 (precedente exacto del par de imports `join` de node:path + `KODO_DIR` de ../config.js)
    - test/session-start.test.js (L1-39 imports + fixtures makeSession/makeConfig; L41-120 estilo de los it() y asserts golden-bytes HOOK-02)
    - 45-PATTERNS.md (§"bloque ES" — patrón append-al-final y assert de ruta resuelta)
    - 45-RESEARCH.md (§Common Pitfalls 1 y 3 — task_id falsy y caching de KODO_DIR en tests)
  </read_first>
  <behavior>
    - Test ES presencia: buildSessionContext(makeSession({ task_id: 'uuid-abc' }), makeConfig()) hace match con /Además, al empezar escribe un plan corto/ (texto en español, D-08).
    - Test ruta resuelta: el output contiene exactamente join(KODO_DIR, 'plans', 'uuid-abc.md') (calculado en el test con el MISMO KODO_DIR importado, NO hardcodeando HOME — Pitfall 3).
    - Test sin literal: el output NO contiene la subcadena '<task_id>' (la ruta está resuelta, no plantillada).
    - Test golden-bytes HOOK-02: el bloque previo no cambia — el prefijo hasta '## Anti-push-fantasma' sigue intacto; la instrucción nueva está DESPUÉS de la última línea Good de Anti-push, separada por una línea vacía (produce el '\n\n').
    - Test complementariedad (D-09): la instrucción es distinta del comentario-al-provider — el output sigue conteniendo '**1. Al empezar** — comenta tu plan de acción' Y ADEMÁS la línea nueva con la ruta de fichero.
  </behavior>
  <action>
    Añadir al top de `src/hooks/session-start.js` (junto a los imports L8-11) dos imports nuevos: `import { join } from 'node:path';` y `import { KODO_DIR } from '../config.js';` — patrón verbatim de `src/session/state.js:3-4` (D-Discretion: KODO_DIR reusa convención, DRY; KODO_DIR es const al import, no dispara I/O en runtime del builder). En `buildSessionContext`, añadir como ELEMENTOS FINALES del array `return [...]` (después de la última línea Good de Anti-push, antes de `].join('\n')`): una línea vacía `''` y luego la instrucción ES interpolando `join(KODO_DIR, 'plans', \`${session.task_id}.md\`)` dentro de backticks en el texto — wording de referencia "Además, al empezar escribe un plan corto (qué vas a hacer + pasos previstos) en `<ruta_resuelta>` (sobrescribe si ya existe)." (D-05 markdown plano sin frontmatter, D-06 escribir al empezar → un re-dispatch sobrescribe latest-wins, D-07 una sola línea imperativa, D-08 español, D-09 redacción "Además" complementaria al comentario-al-provider). Replicar el comentario inline de estilo HOOK-02 ("Phase 45 PLAN-03: append al FINAL preserva golden bytes (HOOK-02 satisfied-by-construction)"). NO pre-crear el directorio, NO añadir I/O, NO defensa `?? 'unknown'` (confiar en el invariante de findSession — main() hace exit(0) sin sesión; ensuciar golden-bytes con defensa es over-engineering, Pitfall 1). En `test/session-start.test.js`, añadir `import { KODO_DIR } from '../src/config.js';` (join ya está en L6) y los casos de la sección <behavior> dentro del describe existente de buildSessionContext, copiando el estilo de assert.match/assert.ok(!ctx.includes(...)) ya presente.
  </action>
  <verify>
    <automated>node --test test/session-start.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/session-start.test.js` exits 0.
    - `grep -q "import { KODO_DIR } from '../config.js'" src/hooks/session-start.js` y `grep -q "import { join } from 'node:path'" src/hooks/session-start.js` (imports añadidos).
    - `grep -q "join(KODO_DIR, 'plans'" src/hooks/session-start.js` (ruta compuesta con join+KODO_DIR, sin `~` literal ni concat de process.env.HOME).
    - `grep -q "Además, al empezar escribe un plan corto" src/hooks/session-start.js` (wording D-06/D-07 "al empezar", una línea, ES).
    - El output de buildSessionContext(makeSession({ task_id: 'uuid-abc' }), makeConfig()) contiene `join(KODO_DIR, 'plans', 'uuid-abc.md')` resuelto y NO contiene el literal `<task_id>`.
    - El test existente HOOK-02 de golden-bytes en session-start.test.js sigue verde (prefijo hasta '## Anti-push-fantasma' intacto).
    - `git diff src/hooks/session-start.js` NO muestra cambios en `buildGsdContext` (esta task solo toca imports + buildSessionContext).
  </acceptance_criteria>
  <done>buildSessionContext inyecta la instrucción ES con ruta resuelta bajo KODO_DIR; los imports join/KODO_DIR están añadidos; los tests non-GSD nuevos y los golden-bytes existentes pasan; buildGsdContext aún sin tocar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inyectar instrucción EN en la rama quick de buildGsdContext + tests de invariancia</name>
  <files>src/hooks/session-start.js, test/gsd-context.test.js</files>
  <read_first>
    - src/hooks/session-start.js (L108-133 rama `if (mode === 'quick')` con su `lines.push(...)`; L134-164 ramas phase/bootstrap — NO TOCAR; L166-180 bloque común "## No automatic push" — NO TOCAR; imports ya añadidos en Task 1)
    - src/labels.js:82-85 (getSessionMode → 'quick' requiere gsd:true && gsd_mode:'quick')
    - test/gsd-context.test.js (L113 HEADER; L160-169 test "D-04 common-block invariance" que NO debe romperse; L172-189 idempotencia; fixture makeSession con gsd:true por defecto)
    - 45-PATTERNS.md (§"rama quick" — append DENTRO del if; §"gsd-context.test.js" — patrón de invariancia de ramas excluidas)
    - 45-RESEARCH.md (§Common Pitfalls 2 — romper la invariancia del bloque común; §Anti-Patterns to Avoid)
  </read_first>
  <behavior>
    - Test quick presencia: buildGsdContext(makeSession({ gsd_mode: 'quick', summary: 'X' })) hace match con /Also, at the start write a short plan/ (texto en inglés, D-08).
    - Test quick ruta resuelta: ese output contiene join(KODO_DIR, 'plans', `${task_id}.md`) resuelto (mismo KODO_DIR importado en el test) y NO contiene el literal '<task_id>'.
    - Test exclusión phase: buildGsdContext(makeSession({ phase_id: '08' })) NO contiene 'Also, at the start write a short plan'.
    - Test exclusión bootstrap: buildGsdContext(makeSession({ phase_id: undefined })) NO contiene 'Also, at the start write a short plan'.
    - Test invariancia (existente, debe seguir verde): tail(quick) === tail(phase) === tail(boot) sobre HEADER '## No automatic push' — la instrucción quick va DENTRO del if, antes del bloque común, así que el tail compartido no cambia.
  </behavior>
  <action>
    En `src/hooks/session-start.js`, dentro de la rama `if (mode === 'quick')`, añadir como ÚLTIMOS argumentos del `lines.push(...)` existente (después de `'Run the slash command and finish — no plan/execute/verify cycle.'`, todavía DENTRO del if): una cadena vacía `''` y luego la instrucción EN interpolando `join(KODO_DIR, 'plans', \`${session.task_id}.md\`)` dentro de backticks — wording de referencia "Also, at the start write a short plan (what you'll do + planned steps) to `<ruta_resuelta>` (overwrite if it exists)." (D-05, D-06 escribir al empezar → re-dispatch sobrescribe latest-wins, D-07 una línea, D-08 inglés porque el bloque GSD es EN, D-09 "Also" complementario). CRÍTICO: NO appendear después del bloque común `## No automatic push` (que va fuera del if/else, L166-180) — eso rompería el test "D-04 common-block invariance" (gsd-context.test.js:160-169). NO tocar las ramas phase (L134-146) ni bootstrap (L147-164): D-04 las excluye y sus bytes están pineados. Replicar el comentario inline de estilo HOOK-02 dentro de la rama quick. En `test/gsd-context.test.js`, añadir `import { KODO_DIR } from '../src/config.js';` y `join` de node:path si no está, y los casos de la sección <behavior> copiando el patrón tail()/assert de los tests de invariancia ya presentes; el test existente D-04 common-block invariance se conserva intacto.
  </action>
  <verify>
    <automated>node --test test/session-start.test.js test/gsd-context.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/session-start.test.js test/gsd-context.test.js` exits 0 (incluye el test existente D-04 common-block invariance en gsd-context.test.js:160 — debe seguir pasando).
    - `grep -q "Also, at the start write a short plan" src/hooks/session-start.js` (wording D-06/D-07 "at the start", una línea, EN).
    - buildGsdContext(makeSession({ gsd_mode: 'quick', summary: 'X' })) contiene `Also, at the start write a short plan` y la ruta `join(KODO_DIR, 'plans', `${task_id}.md`)` resuelta, sin el literal `<task_id>`.
    - buildGsdContext(makeSession({ phase_id: '08' })) NO contiene `Also, at the start write a short plan`; buildGsdContext(makeSession({ phase_id: undefined })) NO contiene `Also, at the start write a short plan`.
    - El tail desde `## No automatic push` es byte-idéntico en las 3 ramas (test existente verde).
    - `git diff src/hooks/session-start.js` NO muestra cambios en las ramas `else if (session.phase_id)`, `else` (bootstrap), ni en el `lines.push('', '## No automatic push', ...)` común.
    - `npm test` exits 0 (suite completa verde — sin regresiones más allá de los dos test files).
  </acceptance_criteria>
  <done>La rama quick de buildGsdContext inyecta la instrucción EN con ruta resuelta dentro del if; phase/bootstrap y el bloque común quedan byte-idénticos; los tests de quick/exclusión/invariancia pasan y la suite completa sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kodo state → instrucción inyectada (texto) | `session.task_id` (UUID generado por kodo/provider, no input de usuario libre) se interpola en una ruta de fichero que se MUESTRA al modelo como texto. El hook NO ejecuta ni escribe esa ruta (D-03). |
| Instrucción inyectada → artefacto escrito por la sesión | La sesión de Claude (runtime) crea `~/.kodo/plans/<task_id>.md`. El hook solo emite texto. |
| Artefacto → overlay de Phase 46 (lectura) | `plan.js` (Phase 44/46) lee el artefacto con contrato never-throws / anti-ReDoS (`String.includes`, sin RegExp). El formato D-05 (plano, sin frontmatter) debe respetar ese contrato. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `task_id` interpolado en la ruta mostrada al modelo (buildSessionContext / buildGsdContext) | accept | `task_id` es un UUID controlado por kodo/provider, no input externo arbitrario. El hook NO realiza I/O con la ruta (D-03) — solo la emite como texto; ningún path traversal ocurre en el hook. La escritura la resuelve el tool Write de la sesión. Riesgo bajo, L1. |
| T-45-02 | Tampering | Formato del artefacto consumido por plan.js (Phase 46) | mitigate | D-05 fija markdown plano sin frontmatter; la correlación vive en el nombre de fichero `<task_id>.md` resuelto por plan.js con `String.includes` anti-ReDoS (sin RegExp). La instrucción no pide frontmatter ni contenido que rompa el render plano/never-throws del consumidor. |
| T-45-03 | Tampering | Prompt injection vía `summary`/título en la rama quick | accept | Fuera de scope: el título ya se inyecta hoy y la rama quick ya hace `replace(/"/g, "'")` en `safeTitle` (L122). La instrucción de plan nueva no añade superficie: es texto estático salvo la ruta resuelta. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No se instala ningún paquete — la fase es 100% builtins de Node (`node:path`, `node:os` vía KODO_DIR). Audit no aplicable (RESEARCH §Package Legitimacy Audit). Sin checkpoint de legitimidad requerido. |

> Proporcional a L1 / baja superficie: el único componente de producción es un string-builder puro sin I/O. No se inventan amenazas de alta severidad donde no las hay.
</threat_model>

<verification>
- `node --test test/session-start.test.js test/gsd-context.test.js` → 48 baseline + casos nuevos, todos verdes (0 fail).
- `npm test` → suite completa verde, sin regresiones.
- Inyección presente en los dos sitios D-04 (ES en buildSessionContext, EN en rama quick) con ruta resuelta bajo KODO_DIR; literal `<task_id>` ausente.
- Ramas phase/bootstrap y bloque común "## No automatic push" byte-idénticos (test D-04 common-block invariance verde).
- Sin paquetes nuevos; sin I/O en el hook (D-03).
</verification>

<success_criteria>
1. (SC-1) En non-GSD y quick, el contexto inyectado incluye la instrucción de escribir un plan corto a una ruta kodo-controlada y estable correlacionada por `task_id` — verificado por los tests de presencia/ruta resuelta.
2. (SC-2) La ruta es propia de kodo (`~/.kodo/plans/<task_id>.md` vía KODO_DIR), legible por el overlay sin endpoints nuevos (esta fase no toca `src/server.js`).
3. (SC-3) Golden-bytes preservados (HOOK-02): bloques previos byte-idénticos, ramas phase/bootstrap intactas — verificado por los tests de invariancia existentes que siguen verdes.
</success_criteria>

<output>
Create `.planning/phases/45-inyecci-n-de-plan-ligero-universal/45-01-SUMMARY.md` when done
</output>
</output>
