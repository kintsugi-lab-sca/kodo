---
phase: 80-carril-orquestador-reconciliaci-n-documental
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/kodo-orchestrate/skill.md
  - src/orchestrator/prompt.md
autonomous: true
requirements: [ORCH-08]
must_haves:
  truths:
    - "grep confirma que tanto `.claude/skills/kodo-orchestrate/skill.md` como `src/orchestrator/prompt.md` mencionan `kodo sidebar doctor` y las 4 features v0.17 — handoff+`NEXT:` (74), superficie dashboard/nudge (75), `pending_stale`/`pending_fetched_at` (76), agrupación `--group` (77) (ORCH-08, edge empty)"
    - "El bloque `<!-- BEGIN reporting -->`/`<!-- END reporting -->` y los placeholders `{{provider}}`/`{{provider_name}}`/`{{mcp_tool}}` de prompt.md siguen presentes e intactos tras la edición (D-12)"
    - "La skill recibe el detalle (nuevo § de higiene del sidebar + flujo 5 en §Diagnóstico) y prompt.md recibe menciones concisas + referencia a la skill — la asimetría de jerarquía se preserva (D-09)"
    - "Ni la skill ni el prompt prometen features borradas — no aparece el nudge de refresh eliminado (Phase 73) ni `missing_group` como acción ejecutable del doctor (ahora advisory) (D-11, disciplina anti-deriva HYG-08)"
    - statement: "skill.md (canónica, detalle) y prompt.md (fallback degradado, conciso) describen el carril del sidebar sin contradicción — el prompt mantiene su rol subordinado y referencia la skill"
      verification: backstop
    - statement: "Reconciliación quirúrgica: el diff de skill.md/prompt.md queda acotado a las secciones tocadas; el bloque BEGIN/END reporting y los placeholders quedan intactos"
      verification: backstop
  artifacts:
    - ".claude/skills/kodo-orchestrate/skill.md — nuevo § de higiene del sidebar + flujo 5 en §Diagnóstico + reflejo de las 4 features v0.17 con detalle"
    - "src/orchestrator/prompt.md — mención concisa del carril del sidebar + referencia a la skill + reflejo conciso de las 4 features v0.17, bloque reporting intacto"
  key_links:
    - "skill.md (canónica) ↔ prompt.md (fallback degradado): asimetría de jerarquía preservada (D-09) — el prompt no se nivela con la skill"
    - "syncSkill auto-propaga skill.md a `~/.claude/skills/` en el próximo `launchOrchestrator` (mecanismo existente; el plan NO añade paso manual de copia)"
  prohibitions:
    - statement: "NO reescribir los documentos enteros: la reconciliación es quirúrgica (D-11) — se corrige lo desfasado y se añade lo ausente, sin re-redactar secciones intactas ni nivelar el rol del prompt con el de la skill"
      status: resolved
      verification: judgment
    - statement: "NO tocar el bloque `<!-- BEGIN/END reporting -->` ni los placeholders `{{provider}}`/`{{provider_name}}`/`{{mcp_tool}}` de prompt.md: la reconciliación no altera el mecanismo de gating (`applyReportingGate`/`resolvePromptTemplate`) (D-12)"
      status: resolved
      verification: judgment
---

<objective>
Reconciliación documental (ORCH-08): el skill `kodo-orchestrate` (fuente
canónica) y `src/orchestrator/prompt.md` (fallback degradado) dejan de estar
desfasados — mencionan `kodo sidebar doctor` y reflejan toda la realidad
post-v0.17 que hoy no aparece (handoff acumulativo + `NEXT:`, superficie
dashboard/nudge, `pending_stale`/`pending_fetched_at`, agrupación `--group`), con
la misma disciplina anti-deriva que HYG-08 aplicó al README en v0.16: sin
prometer features borradas, sin omitir las nuevas.

Purpose: que el orquestador (LLM) opere con documentación fiel — el detalle en la
skill, la mención mínima en el prompt — preservando la jerarquía declarada (la
skill manda; el prompt es fallback subordinado) y el mecanismo de reporting gated
intacto.

Output: skill.md con un § de higiene del sidebar + flujo 5 de diagnóstico + las 4
features v0.17; prompt.md con menciones concisas + referencia a la skill; bloque
reporting y placeholders intactos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-carril-orquestador-reconciliaci-n-documental/80-CONTEXT.md
@.planning/phases/80-carril-orquestador-reconciliaci-n-documental/80-RESEARCH.md
@.planning/phases/80-carril-orquestador-reconciliaci-n-documental/80-PATTERNS.md

# Documentos a reconciliar
@.claude/skills/kodo-orchestrate/skill.md
@src/orchestrator/prompt.md

# Referencia de comportamiento del carril (para redactar la higiene del sidebar con precisión)
@src/cli/sidebar-doctor.js
@src/cmux/sidebar-doctor.js

# Qué entregó cada fase v0.17 (para reflejar las features con exactitud)
@.planning/milestones/v0.17-ROADMAP.md
</context>

## Artifacts this phase produces

Secciones NUEVAS que este plan crea (excluir del drift-check de convergencia de
reviews):

- Nuevo `## Higiene del sidebar` (o equivalente) en `.claude/skills/kodo-orchestrate/skill.md`.
- Nuevo flujo `### 5. Sidebar desalineado ...` dentro de `## Diagnóstico` en skill.md.
- Nuevas menciones de las 4 features v0.17 en skill.md (handoff/`NEXT:`, dashboard/nudge, `pending_stale`/`pending_fetched_at`, `--group`).
- Nueva mención concisa del carril del sidebar + referencia a la skill + reflejo conciso de las features v0.17 en `src/orchestrator/prompt.md` (fuera del bloque reporting).

<tasks>

<task type="auto">
  <name>Task 1: skill.md canónica — § higiene del sidebar + flujo 5 de diagnóstico + 4 features v0.17 (detalle)</name>
  <files>.claude/skills/kodo-orchestrate/skill.md</files>
  <read_first>
    - .claude/skills/kodo-orchestrate/skill.md — estructura completa: `## Diagnóstico` (4 flujos numerados síntoma→comando; el flujo 4 es «Phase terminó y entró a Review»), `## Sesiones GSD`, `## Adopción asistida`, `## Cómo actualizar este skill`. El nuevo flujo 5 y el nuevo § copian la forma existente.
    - src/cli/sidebar-doctor.js — semántica EXACTA del CLI: dry-run por defecto lista acciones; `--fix` converge; `missing_group` es ADVISORY (acción del operador, el doctor no ancla en sesión viva); `loose→add`, `empty→ungroup`; allowlist no-destructivo; exit codes deterministas.
    - src/cmux/sidebar-doctor.js — `hasActions` (loose+empty) vs `hasAdvisories` (missing_group); el carril automático converge lo auto-arreglable en `kodo check`.
    - .planning/milestones/v0.17-ROADMAP.md — qué entregó cada fase 74-77 para reflejar las features con exactitud (sin inventar).
    - .planning/phases/80-carril-orquestador-reconciliaci-n-documental/80-RESEARCH.md §State of the Art — deprecated a NO reintroducir: nudge de refresh (borrado Phase 73), `missing_group` como acción ejecutable (ahora advisory), doc web cmux de disolución de grupos.
  </read_first>
  <action>
    Edición QUIRÚRGICA (D-11): añade lo ausente y corrige lo desfasado, sin
    re-redactar secciones intactas. Cambios concretos:

    1. **Nuevo § de higiene del sidebar** (p. ej. `## Higiene del sidebar`; ubícalo
       tras `## Diagnóstico` o donde encaje jerárquicamente). Debe explicar: (a) el
       sidebar lo mantiene un **carril automático** de `kodo check` — cuando un pase
       está motivado (stuck/review/pending) el doctor converge las acciones
       auto-arreglables de piggyback, ANTES de lanzar al orquestador, in-process y
       0-token; (b) el sidebar **NO** es trigger (consistencia eventual: una sesión
       recién lanzada suelta se agrupa en el SIGUIENTE pase motivado, no de
       inmediato); (c) `kodo sidebar doctor` (dry-run, sin `--fix`) es la herramienta
       de **diagnóstico bajo demanda** que el orquestador puede correr para
       inspeccionar sin mutar; (d) las acciones auto-arreglables son `loose→add` y
       `empty→ungroup` (allowlist NO-destructivo — `workspace-group delete` no
       existe); (e) `missing_group` es **advisory = acción del operador** (el doctor
       NO crea ni ancla grupos en una sesión viva; el operador crea el grupo
       conscientemente); (f) el launch path sigue byte-idéntico (la gestión de
       grupos solo ocurre en el carril doctor).

    2. **Nuevo flujo 5 en `## Diagnóstico`** («### 5. Sidebar desalineado (grupos
       vacíos / workspaces sueltos)»), copiando la forma «síntoma → secuencia
       numerada de comandos» de los flujos 1-4: paso 1 `kodo sidebar doctor`
       (dry-run) para diagnosticar sin mutar; paso 2 interpretar acciones
       auto-arreglables (loose→add, empty→ungroup) vs advisories (missing_group =
       acción del operador, kodo no ancla en sesión viva); paso 3 recordar que el
       carril automático de `kodo check` ya converge las acciones en pases motivados
       — el dry-run es solo diagnóstico.

    3. **Reflejar las 4 features v0.17** (D-10, con detalle en la canónica), donde
       encaje: handoff acumulativo + `NEXT:` en `state.tasks` (74 — el orquestador
       puede leer `NEXT:` como contexto de qué sigue en cada tarea); superficie del
       `NEXT:` en el dashboard y en el nudge con contexto (75); frescura
       discriminada del conteo pending `pending_stale`/`pending_fetched_at` con
       convergencia vía `kodo check` (76); agrupación `--group` de workspaces al
       lanzar (77).

    4. **Disciplina anti-deriva (D-11, HYG-08)**: NO introduzcas prosa que prometa
       features borradas — en particular NO describas un nudge de refresh al
       orquestador ya-existente (se eliminó en Phase 73) ni `missing_group` como
       acción ejecutable/creable del doctor (es advisory). Si al leer encuentras
       prosa vieja que las prometa, corrígela.

    NO reescribas los documentos enteros. NO toques la lógica de `## Adopción
    asistida` ni sus mandatos shell-seguros. El commit lo hace el hook Stop
    automáticamente (`## Cómo actualizar este skill`) — no añadas paso manual de
    copia; `syncSkill` propaga a home en el próximo launch.
  </action>
  <verify>
    <automated>grep -q "kodo sidebar doctor" .claude/skills/kodo-orchestrate/skill.md && grep -q "NEXT:" .claude/skills/kodo-orchestrate/skill.md && grep -q "pending_stale" .claude/skills/kodo-orchestrate/skill.md && grep -q -- "--group" .claude/skills/kodo-orchestrate/skill.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "kodo sidebar doctor" .claude/skills/kodo-orchestrate/skill.md` — el carril/comando del sidebar doctor está mencionado.
    - Existe un nuevo § de higiene del sidebar Y un flujo `### 5.` bajo `## Diagnóstico` (la numeración de flujos previos 1-4 queda intacta).
    - Las 4 features v0.17 aparecen: `grep -q "NEXT:"`, `grep -q "pending_stale"`, `grep -q -- "--group"`, y una mención a la superficie dashboard/nudge del `NEXT:`.
    - Anti-deriva (revisión manual, D-11): ninguna prosa promete un nudge de refresh al orquestador ya-existente (eliminado en Phase 73); toda mención de `missing_group` lo describe como advisory / acción del operador, nunca como acción ejecutable del doctor.
    - `git diff --stat` muestra cambios acotados a las secciones tocadas; `## Adopción asistida` y sus mandatos shell-seguros sin cambios.
  </acceptance_criteria>
  <done>
    La skill canónica menciona `kodo sidebar doctor`, tiene un § de higiene del sidebar + flujo 5 de diagnóstico, refleja las 4 features v0.17 con detalle y no promete features borradas — edición quirúrgica, resto intacto.
  </done>
</task>

<task type="auto">
  <name>Task 2: prompt.md fallback — mención concisa del carril + referencia a la skill + features v0.17 (bloque reporting intacto)</name>
  <files>src/orchestrator/prompt.md</files>
  <read_first>
    - src/orchestrator/prompt.md — `## Loop de supervisión` (pasos numerados 1-8, donde el paso 5 revisa Review y el 6 lanza tareas), `## Sesiones GSD`, `## Adopción asistida`, y el bloque `<!-- BEGIN reporting -->` … `<!-- END reporting -->` (L47-116) con placeholders `{{provider}}`/`{{provider_name}}`/`{{mcp_tool}}`. El prompt DECLARA su jerarquía subordinada respecto a la skill (L3) — preservarla.
    - src/orchestrator/launch.js — `resolvePromptTemplate` / `applyReportingGate` consumen el bloque reporting y los placeholders; las ediciones van FUERA de ese bloque y no tocan los placeholders.
    - .claude/skills/kodo-orchestrate/skill.md — la fuente canónica que el prompt referencia para el detalle (mantener el reparto asimétrico D-09).
  </read_first>
  <action>
    Edición QUIRÚRGICA y CONCISA (D-09: el prompt es fallback degradado, no se
    nivela con la skill). Cambios concretos, todos FUERA del bloque
    `<!-- BEGIN reporting -->` … `<!-- END reporting -->`:

    1. **Una línea en `## Loop de supervisión`** (p. ej. un paso nuevo o un inciso):
       el sidebar lo mantiene automáticamente el carril de `kodo check` (converge
       grupos vacíos / workspaces sueltos en pases motivados, in-process, 0-token);
       para diagnóstico bajo demanda, `kodo sidebar doctor` (dry-run). Detalle en la
       skill `.claude/skills/kodo-orchestrate/skill.md`.

    2. **Reflejo CONCISO de las 4 features v0.17** (D-10, profundidad de fallback):
       una mención breve (2-3 líneas o incisos, deferiendo el detalle a la skill) de:
       handoff acumulativo + `NEXT:` en `state.json` como contexto de qué sigue por
       tarea (74); superficie del `NEXT:` en dashboard y nudge (75); frescura del
       conteo pending `pending_stale`/`pending_fetched_at` con convergencia vía `kodo
       check` (76); agrupación `--group` de workspaces al lanzar (77). No dupliques el
       detalle de la skill — enumera y remite.

    3. **Anti-deriva (D-11)**: no prometas el nudge de refresh eliminado (Phase 73)
       ni `missing_group` como acción ejecutable del doctor.

    **D-12 — NO TOCAR**: el bloque `<!-- BEGIN reporting -->` … `<!-- END reporting
    -->` (incl. su granularidad fase=sub-issue) ni los placeholders
    `{{provider}}`/`{{provider_name}}`/`{{mcp_tool}}` — `resolvePromptTemplate` y
    `applyReportingGate` dependen de ellos byte-a-byte. NO reescribas el prompt
    entero; preserva su rol reducido y su referencia a la skill como canónica.
  </action>
  <verify>
    <automated>grep -q "kodo sidebar doctor" src/orchestrator/prompt.md && grep -q "NEXT:" src/orchestrator/prompt.md && grep -q "pending_stale" src/orchestrator/prompt.md && grep -q -- "--group" src/orchestrator/prompt.md && grep -q "BEGIN reporting" src/orchestrator/prompt.md && grep -q "END reporting" src/orchestrator/prompt.md && grep -q "{{provider}}" src/orchestrator/prompt.md && grep -q "{{mcp_tool}}" src/orchestrator/prompt.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "kodo sidebar doctor" src/orchestrator/prompt.md` y una referencia explícita a la skill como fuente canónica del detalle del sidebar.
    - Las 4 features v0.17 mencionadas concisamente: `grep -q "NEXT:"`, `grep -q "pending_stale"`, `grep -q -- "--group"`, y una mención a la superficie dashboard/nudge.
    - D-12 preservado: `grep -q "BEGIN reporting"`, `grep -q "END reporting"`, `grep -q "{{provider}}"`, `grep -q "{{provider_name}}"`, `grep -q "{{mcp_tool}}"` — todos presentes; `git diff` NO muestra cambios dentro del bloque BEGIN/END reporting.
    - Anti-deriva (revisión manual, D-11): no se promete el nudge de refresh eliminado; `missing_group` no aparece como acción ejecutable del doctor.
    - Las ediciones son concisas (fallback degradado): el prompt no se convierte en una copia del detalle de la skill (rol subordinado preservado, D-09).
  </acceptance_criteria>
  <done>
    prompt.md menciona el carril del sidebar y refiere a la skill, refleja concisamente las 4 features v0.17, mantiene su rol de fallback degradado y deja el bloque reporting + placeholders intactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edición documental → skill propagada a `~/.claude/skills/` | `syncSkill` copia skill.md a home en el próximo `launchOrchestrator` (fail-open, mecanismo existente) — el contenido editado se convierte en instrucciones que el orquestador LLM ejecuta |
| prompt.md → `resolvePromptTemplate`/`applyReportingGate` | los placeholders y el bloque reporting son consumidos byte-a-byte al spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-05 | Tampering | prosa nueva en skill.md/prompt.md que induzca un verbo destructivo o el sidebar como trigger | low | mitigate | Cambios quirúrgicos (D-11) + checklist anti-deriva; NO se documenta `workspace-group delete` ni el sidebar como trigger; `missing_group` solo como advisory (acción del operador). |
| T-80-06 | Tampering | romper el bloque reporting gated o los placeholders `{{provider}}` de prompt.md al editar | low | mitigate | Ediciones FUERA del bloque `<!-- BEGIN/END reporting -->`; grep verifica que los marcadores y los 3 placeholders siguen presentes; `git diff` confirma cero cambios dentro del bloque (D-12). |
| T-80-SC | Tampering | supply-chain (npm/pip/cargo installs) | low | accept | Esta fase NO instala ningún paquete (cero deps; solo edita `.md`). Superficie supply-chain sin cambios. |

Ninguna amenaza de severidad **high** → el gate «block on high» (ASVS L1) queda satisfecho sin checkpoint bloqueante.
</threat_model>

<verification>
Checklist manual anti-deriva features↔docs (D-11, precedente HYG-08 v0.16 Phase
72) — se ejecuta en el gate de fase (`/gsd-verify-work`), NO como test automático
de docs:

- [ ] **Presente (docs → features)**: skill.md y prompt.md mencionan `kodo sidebar doctor`, handoff+`NEXT:`, superficie dashboard/nudge, `pending_stale`/`pending_fetched_at`, `--group` (grep de las tareas cubre la presencia mecánica).
- [ ] **Fiel (features → docs)**: cada mención describe el comportamiento REAL (missing_group advisory, no ejecutable; carril de piggyback en `kodo check`, no trigger; launch path byte-idéntico).
- [ ] **Sin promesas muertas**: ninguna prosa promete el nudge de refresh eliminado (Phase 73), `missing_group` como acción creable del doctor, ni la disolución de grupos según el doc web de cmux (el `--help` del binario manda).
- [ ] **Asimetría D-09**: la skill lleva el detalle; el prompt es conciso y referencia la skill; el prompt no se nivela.
- [ ] **D-12 intacto**: bloque BEGIN/END reporting y placeholders sin cambios (`git diff`).

Automático (por tarea): grep de presencia de tokens en ambos docs + presencia de
marcadores/placeholders del bloque reporting.
</verification>

<success_criteria>
El skill y el prompt reflejan toda la realidad post-v0.17 (sidebar doctor + las 4
features), sin prometer features borradas ni omitir las nuevas; la skill lleva el
detalle y el prompt la mención concisa con referencia a la skill; el bloque
reporting y los placeholders quedan intactos.
</success_criteria>

<output>
Create `.planning/phases/80-carril-orquestador-reconciliaci-n-documental/80-02-SUMMARY.md` when done
</output>
