---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/session/manager.js
  - test/manager.test.js
autonomous: true
requirements: [WT-01, WT-02]
user_setup: []

must_haves:
  truths:
    - "Toda invocación de `launchWorkItem(identifier, opts)` para sesiones full / quick / no-GSD construye un comando claude que incluye `--worktree <sessionId>` (D-01: argumento explícito, jamás sin nombre)."
    - "El path del worktree se computa con `computeWorktreePath(projectPath, sessionId)` de Plan 01 — single source of truth para el cómputo (D-03 + Claude's Discretion)."
    - "`worktree_path` se persiste en `SessionRecord` vía `addSession(taskId, session)` ANTES de `cmux.send(claudeCmd)` (D-03 PRE-spawn ordering: persist → send), con `buildSessionFromTask` recibiendo el path por conditional spread `...(worktreePath ? { worktree_path: worktreePath } : {})` siguiendo el idiom existente para `phase_id`/`brief`/`gsd_mode`."
    - "El comando claude resultante preserva el orden actual de flags `--model X --session-id Y --worktree Z [--dangerously-skip-permissions] '<prompt>'` — `--worktree <sessionId>` se inserta INMEDIATAMENTE DESPUÉS de `--session-id <sessionId>` y ANTES de `[--dangerously-skip-permissions]` (golden-bytes contract Phase 12 QUICK-07: las tags `[GSD quick]`/`[GSD phase N]`/`[GSD bootstrap]` viven en el prompt, NO en el header del cmd; añadir `--worktree` aquí no muta esos bytes)."
    - "`cmux.newWorkspace({ cwd: projectPath })` en línea 194–197 se mantiene IDÉNTICO — D-04 lockeado: el shell de cmux abre en el repo principal; el worktree lo materializa `claude --worktree` del lado de claude."
    - "Sesiones legacy v0.5 sin `worktree_path` siguen leyéndose (D-03c — campo aditivo opcional). Tests de `buildSessionFromTask` sin worktreePath no rompen: el conditional spread omite el campo."
    - "TODAS las sesiones de `launchWorkItem` llevan `--worktree` (D-06b explícito): no hay branch GSD/no-GSD; el flag es universal en este callsite. La exclusión de `launchOrchestrator` se valida en Plan 03."
  artifacts:
    - path: "src/session/manager.js"
      provides: "launchWorkItem cablea computeWorktreePath PRE-spawn, persiste worktree_path en addSession PRE-cmux.send, buildClaudeCommand emite --worktree <sessionId>"
      contains: "computeWorktreePath"
      min_lines: 290
    - path: "test/manager.test.js"
      provides: "Asserts de buildSessionFromTask con worktree_path; asserts del shape del cmd claude (--worktree presente y posicionado)"
      contains: "worktree_path"
      min_lines: 1
  key_links:
    - from: "src/session/manager.js"
      to: "src/session/state.js#computeWorktreePath"
      via: "named import"
      pattern: "import\\s+\\{[^}]*\\bcomputeWorktreePath\\b[^}]*\\}\\s+from\\s+['\"]\\./state\\.js['\"]"
    - from: "src/session/manager.js#launchWorkItem"
      to: "computeWorktreePath"
      via: "const worktreePath = computeWorktreePath(projectPath, sessionId)"
      pattern: "computeWorktreePath\\(\\s*projectPath\\s*,\\s*sessionId\\s*\\)"
    - from: "src/session/manager.js#buildSessionFromTask"
      to: "Session.worktree_path"
      via: "conditional spread `...(worktreePath ? { worktree_path: worktreePath } : {})`"
      pattern: "worktreePath\\s*\\?\\s*\\{\\s*worktree_path:\\s*worktreePath\\s*\\}"
    - from: "src/session/manager.js#buildClaudeCommand"
      to: "claude --worktree <sessionId>"
      via: "string interpolation in template"
      pattern: "--worktree\\s+\\$\\{sessionId\\}"
    - from: "src/session/manager.js#launchWorkItem"
      to: "addSession (PRE-cmux.send ordering)"
      via: "addSession(task.id, session) before cmux.send(claudeCmd)"
      pattern: "addSession\\([^)]+\\)[^;]*;[\\s\\S]{0,500}cmux\\.send"
---

<objective>
Cablear `--worktree <sessionId>` en `launchWorkItem` para TODAS las sesiones (full + quick + no-GSD por D-06b) y persistir `worktree_path` en `SessionRecord` ANTES del spawn de cmux. Single source of truth: `computeWorktreePath` de Plan 01.

**3 cambios atómicos en `src/session/manager.js`:**
1. Import de `computeWorktreePath` desde `./state.js` (junto al import existente de `addSession`/`listSessions`/`updateSession` línea 8).
2. `buildSessionFromTask` (líneas 27–57) acepta nuevo param opcional `worktreePath` y lo emite con conditional spread `...(worktreePath ? { worktree_path: worktreePath } : {})`. Patrón idéntico a `phase_id`/`brief` (líneas 53–55) y `gsd_mode` (líneas 50).
3. `launchWorkItem` (líneas 154–250) — orden exacto:
   - Línea ~206: `const sessionId = opts.sessionId || randomUUID();` (sin cambios)
   - **NUEVA línea**: `const worktreePath = computeWorktreePath(projectPath, sessionId);` (PRE-cmux.send, PRE-buildClaudeCommand)
   - Línea ~209: `buildClaudeCommand(...)` recibe `sessionId` (sin cambios — el `--worktree` se compone INTERNO al builder usando el `sessionId` que ya tiene)
   - Línea ~228: `addSession(task.id, session)` PRE-spawn, sin cambios estructurales — la `session` ya tiene `worktree_path` por (2)
   - Línea ~212: `cmux.send({ workspace: workspaceRef, text: claudeCmd })` (sin cambios)
4. `buildClaudeCommand` (líneas 261–274) inserta `--worktree ${sessionId}` después de `--session-id ${sessionId}` y antes de `${cliFlags}`.

**Order ortogonal a respetar (CONTEXT.md §canonical_refs):**
- `cmux.newWorkspace({ cwd: projectPath })` línea 194–197 — IDÉNTICO (D-04).
- `launchOrchestrator` en `src/orchestrator/launch.js` — INTACTO (Plan 03 verifica la exclusión D-06).
- `cmux.notify`, `cmux.setColor`, `inFlight` guard, error handling — INTACTOS.

Purpose: cubrir WT-01 (`--worktree` en TODAS las sesiones de `launchWorkItem`) y WT-02 (path persistido en `SessionRecord`).
Output: `manager.js` con +1 import + +1 param en `buildSessionFromTask` + +2 líneas en `launchWorkItem` + +1 fragmento de string en `buildClaudeCommand`. Tests cubren shape de la session y shape del cmd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md
@.planning/phases/18-worktree-runtime-wiring/18-01-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Source of truth — read BEFORE editing
@src/session/manager.js
@src/session/state.js
@test/manager.test.js
@src/triggers/dispatcher.js

<interfaces>
<!-- Contrato de Plan 01 (ya completado en Wave 1, depends_on: [18-01]): -->

```javascript
// src/session/state.js (named export, pure)
export function computeWorktreePath(projectPath: string, sessionId: string): string;
// Devuelve `<projectPath>/.bg-shell/<sessionId>` (sin trailing slash).
// NO realpathSync, NO mkdir — pure path.join.

// src/session/state.js typedef (campo aditivo opcional):
type Session = {
  // ...existing fields...
  worktree_path?: string;  // Phase 18 D-03c
};
```

<!-- API actual de buildSessionFromTask (líneas 27–57) — el spread condicional es el patrón establecido: -->

```javascript
// Líneas 50–55 actuales (patrón a clonar para worktreePath):
...(gsdMode ? { gsd: true, gsd_mode: gsdMode } : {}),
...(phaseId ? { phase_id: phaseId } : {}),
...(brief ? { brief } : {}),
// ↓ NUEVO (sigue el mismo idiom):
...(worktreePath ? { worktree_path: worktreePath } : {}),
```

<!-- Cmd claude actual (línea 273) — el orden de flags es contractual para golden-bytes Phase 12 QUICK-07: -->

```javascript
// ANTES:
return `claude --model ${model} --session-id ${sessionId} ${cliFlags} '${escapeShell(prompt)}'`.replace(/\s+/g, ' ').trim();

// DESPUÉS (única mutación: insertar --worktree entre --session-id y cliFlags):
return `claude --model ${model} --session-id ${sessionId} --worktree ${sessionId} ${cliFlags} '${escapeShell(prompt)}'`.replace(/\s+/g, ' ').trim();
```

Verificación golden-bytes: las tags `[GSD quick]`/`[GSD phase N]`/`[GSD bootstrap]` viven en el PROMPT (escapado, último arg). El header (`--model X --session-id Y --worktree Z [--dangerously-skip-permissions]`) precede al prompt — NO entra al cómputo de offset relativo de las tags. Phase 20 (HOOK-01) operará sobre `buildSessionContext`/`buildGsdContext`, no sobre el header del cmd. Compatibilidad bidireccional preservada.

<!-- Caller actual (dispatcher.js líneas 282–292 / 313–322) — NO cambia signature de launchWorkItem: -->

```javascript
// dispatcher pasa opts = { model, flags, sessionId?, phase_id?, brief? }
// launchWorkItem internamente computa worktreePath desde projectPath + sessionId
// y lo persiste solo. El dispatcher NO necesita conocer el path; lo lee de
// state.json via findSession() cuando lo necesite (Phase 19+).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cablear computeWorktreePath + --worktree en launchWorkItem y buildClaudeCommand</name>
  <files>src/session/manager.js, test/manager.test.js</files>

  <read_first>
    - src/session/manager.js (estado completo — confirmar líneas exactas: imports 1-9, buildSessionFromTask 27-57, launchWorkItem 154-250, buildClaudeCommand 261-274)
    - src/session/state.js (confirmar que `computeWorktreePath` está exportado por Plan 01 — Wave 1 dependency)
    - test/manager.test.js (estado completo — patrón de `makeTask()`, `describe('buildSessionFromTask')`, asserts existentes sobre `phase_id`/`gsd_mode` para CLONAR el shape para worktree_path)
    - src/triggers/dispatcher.js líneas 280-325 (caller actual de launchWorkItem — confirmar que opts shape NO cambia: dispatcher sigue pasando `{model, flags, sessionId?, phase_id?, brief?}` y NO necesita worktree_path)
    - .planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md §decisions D-01, D-03, D-04
    - .planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md §code_context (Reusable Assets, Established Patterns)
  </read_first>

  <behavior>
    - Test 1 (`buildSessionFromTask` añade worktree_path con conditional spread): pasar `worktreePath: '/repo/.bg-shell/abc'` produce `session.worktree_path === '/repo/.bg-shell/abc'`. Pasar `worktreePath: undefined` produce `session.worktree_path === undefined` (campo omitido por spread, no `null`).
    - Test 2 (`buildSessionFromTask` no rompe call sites legacy): llamadas SIN `worktreePath` (las 4 existentes en `launchWorkItem` antes de Plan 02 y todos los tests de Phase 11/9) producen el mismo shape que antes — `session.worktree_path` undefined, los demás campos intactos byte-a-byte.
    - Test 3 (`buildClaudeCommand` emite `--worktree <sessionId>`): el string resultante para `sessionId='abc-123'`, `model='sonnet'` contiene exactamente `--session-id abc-123 --worktree abc-123` en ese orden, separados por un solo espacio. NO `--worktree=abc-123` (D-01: nombre como arg posicional), NO `--worktree` solo (D-01: explícito).
    - Test 4 (`buildClaudeCommand` orden de flags preservado): `--model` precede `--session-id` precede `--worktree` precede `--dangerously-skip-permissions` (cuando aplica) precede al prompt escapado entre `'`. Validable con regex sobre el output completo.
    - Test 5 (orden PRE-spawn en `launchWorkItem`): `addSession(task.id, session)` se ejecuta ANTES de `cmux.send({ workspace: workspaceRef, text: claudeCmd })`. Esto se asegura ya en el código actual (línea 212 send, línea 228 addSession — está INVERTIDO actualmente: send PRECEDE addSession). **CAMBIO REQUERIDO**: mover `addSession(task.id, session)` ANTES de `cmux.send` para cumplir D-03 estricto. Test asserta orden via mocking de `cmux.send` y `addSession` (DI vía `opts.depsCmux` o spy en spy-able layer).
    - Test 6 (worktree_path persistido en state real): integration mínimo con `tmpdir+HOME override` (patrón Phase 16 LOG-13..15) — invocar `launchWorkItem` con stubs de cmux (que no spawnean nada) y aserir que `loadState().sessions[task.id].worktree_path === computeWorktreePath(projectPath, sessionId)`.
  </behavior>

  <action>
    1. **Añadir import** en `src/session/manager.js` línea 8 (modificar import existente, NO añadir nuevo):

       ```javascript
       // ANTES (línea 8):
       import { addSession, listSessions, updateSession } from './state.js';

       // DESPUÉS:
       import { addSession, listSessions, updateSession, computeWorktreePath } from './state.js';
       ```

    2. **Extender `buildSessionFromTask`** (líneas 27–57) — añadir param opcional `worktreePath` al destructuring del primer arg + spread condicional al final. JSDoc también recibe el nuevo campo:

       ```javascript
       /**
        * Build the session record saved to state from a resolved TaskItem.
        * Pure function — no I/O.
        *
        * @param {{
        *   task: import('../interface.js').TaskItem,
        *   providerName: string,
        *   projectPath: string,
        *   workspaceRef: string,
        *   sessionId: string,
        *   flags?: string[],
        *   phaseId?: string,
        *   brief?: string,
        *   worktreePath?: string,  // Phase 18 D-03: persisted via conditional spread (mismo patrón que phaseId/brief/gsdMode)
        * }} params
        * @returns {import('./state.js').Session}
        */
       export function buildSessionFromTask({ task, providerName, projectPath, workspaceRef, sessionId, flags, phaseId, brief, worktreePath }) {
         const gsdMode = getGsdMode(flags);
         return {
           workspace_ref: workspaceRef,
           session_id: sessionId,
           task_id: task.id,
           // ... resto de campos sin cambios ...
           ...(gsdMode ? { gsd: true, gsd_mode: gsdMode } : {}),
           ...(phaseId ? { phase_id: phaseId } : {}),
           ...(brief ? { brief } : {}),
           // Phase 18 D-03c: aditivo opcional, falsy permitido. Mismo idiom que los anteriores.
           ...(worktreePath ? { worktree_path: worktreePath } : {}),
         };
       }
       ```

       NO modificar el orden de los campos pre-existentes; AÑADIR el spread al final, después de `brief`.

    3. **Modificar `launchWorkItem`** (líneas 154–250):

       a. Tras la línea 206 (`const sessionId = opts.sessionId || randomUUID();`) y ANTES de la línea 209 (`const claudeCmd = buildClaudeCommand(...)`), añadir:

          ```javascript
          // Phase 18 D-01, D-02, D-03: compute deterministic worktree path PRE-spawn.
          // Single source of truth: computeWorktreePath de state.js. Plan 03 valida la
          // unicidad del path (D-05 fail-fast canonical error).
          const worktreePath = computeWorktreePath(projectPath, sessionId);
          ```

       b. En la llamada a `buildSessionFromTask` (líneas 215–227), añadir el nuevo arg `worktreePath`:

          ```javascript
          const session = buildSessionFromTask({
            task,
            providerName: config.provider,
            projectPath,
            workspaceRef,
            sessionId,
            flags: combinedFlags,
            phaseId: opts.phase_id,
            brief: opts.brief,
            worktreePath,  // ← Phase 18 D-03 (persisted PRE-spawn)
          });
          ```

       c. **CRÍTICO — orden PRE-spawn (D-03)**: revisar el orden actual entre `cmux.send` (línea 212) y `addSession` (línea 228). El código actual envía a cmux ANTES de persistir en state. Por D-03 el orden debe ser `addSession → cmux.send`. **Mover el bloque `addSession(task.id, session)` ANTES de `cmux.send`**:

          Estructura objetivo (líneas 209–228 reordenadas):
          ```javascript
          const claudeCmd = buildClaudeCommand(config, sessionId, task, description, modelOverride, combinedFlags, moduleName);

          // Build session record (pure)
          const session = buildSessionFromTask({
            task, providerName: config.provider, projectPath, workspaceRef,
            sessionId, flags: combinedFlags,
            phaseId: opts.phase_id, brief: opts.brief, worktreePath,
          });

          // Phase 18 D-03: persist BEFORE cmux.send so kodo logs --session-of and
          // other consumers see worktree_path immediately. Single source of truth
          // in kodo, not in claude (precedente Phase 8 GSD-10 lock path).
          addSession(task.id, session);

          // Send Claude command to workspace
          await cmux.send({ workspace: workspaceRef, text: claudeCmd });
          ```

          Reordenar requiere mover el bloque `cmux.send` (línea 212 actual) DESPUÉS del `addSession` (línea 228 actual). El `cmux.notify` y el `try/catch` para orchestrator notify (líneas 231–247) quedan después de cmux.send sin cambios.

    4. **Modificar `buildClaudeCommand`** (líneas 261–274) — la única mutación es insertar `--worktree ${sessionId}` en el template:

       ```javascript
       // ANTES (línea 273):
       return `claude --model ${model} --session-id ${sessionId} ${cliFlags} '${escapeShell(prompt)}'`.replace(/\s+/g, ' ').trim();

       // DESPUÉS:
       // Phase 18 D-01: --worktree <sessionId> en TODAS las sesiones de launchWorkItem
       // (D-06b: full + quick + no-GSD). claude --worktree con nombre explícito (NO sin
       // arg, perderíamos el path determinístico). Orden de flags preservado para
       // golden-bytes Phase 12 QUICK-07: las tags [GSD *] viven en el prompt, NO en
       // el header del cmd — añadir --worktree aquí no muta los offsets de prompt.
       return `claude --model ${model} --session-id ${sessionId} --worktree ${sessionId} ${cliFlags} '${escapeShell(prompt)}'`.replace(/\s+/g, ' ').trim();
       ```

       Mantener intacto: `model`, `cliFlags` (compute de skipPerms), `escapeShell`, el `.replace(/\s+/g, ' ').trim()`.

    5. **Tests en `test/manager.test.js`** — añadir asserts en los `describe` existentes y un `describe` nuevo para el cmd shape:

       ```javascript
       // Dentro de describe('buildSessionFromTask'):
       it('persists worktree_path when worktreePath param is provided (Phase 18 D-03)', () => {
         const task = makeTask();
         const session = buildSessionFromTask({
           task, providerName: 'test', projectPath: '/tmp/proj',
           workspaceRef: 'workspace:42', sessionId: 'sess-uuid',
           worktreePath: '/tmp/proj/.bg-shell/sess-uuid',
         });
         assert.equal(session.worktree_path, '/tmp/proj/.bg-shell/sess-uuid');
       });

       it('omits worktree_path when worktreePath is undefined (legacy compat — D-03c aditivo opcional)', () => {
         const task = makeTask();
         const session = buildSessionFromTask({
           task, providerName: 'test', projectPath: '/tmp/proj',
           workspaceRef: 'workspace:42', sessionId: 'sess-uuid',
           // worktreePath omitido
         });
         assert.equal('worktree_path' in session, false);
       });

       // Nuevo describe — buildClaudeCommand es función file-local; testear vía
       // re-export para test, o vía spy del cmd que llega a cmux.send. Estrategia
       // más simple: exportar buildClaudeCommand para testing (CONVENTIONS.md
       // permite exports adicionales si están @ts-checked) — añadir
       // `export { buildClaudeCommand };` al final del archivo. Si el equipo
       // prefiere NO exportar internals, alternativa: spy en cmux.send y aserir
       // sobre el text recibido en un launchWorkItem integration test.
       describe('buildClaudeCommand cmd shape (Phase 18 WT-01)', () => {
         it('includes --worktree <sessionId> after --session-id', () => {
           const config = { provider: 'plane', claude: { default_model: 'sonnet' } };
           const task = { ref: 'KL-42', title: 'fix bug' };
           const cmd = buildClaudeCommand(config, 'abc-123', task, 'desc', null, [], null);
           assert.match(cmd, /--session-id abc-123 --worktree abc-123/);
         });

         it('--worktree precedes --dangerously-skip-permissions when both present', () => {
           const config = { provider: 'plane', claude: { default_model: 'sonnet' } };
           const task = { ref: 'KL-42', title: 'fix bug' };
           const cmd = buildClaudeCommand(config, 'abc-123', task, 'desc', null, ['yolo'], null);
           assert.match(cmd, /--worktree abc-123 --dangerously-skip-permissions/);
         });

         it('worktree arg is the sessionId verbatim (D-01: explicit, never bare --worktree)', () => {
           const config = { provider: 'plane', claude: { default_model: 'sonnet' } };
           const task = { ref: 'KL-42', title: 'fix bug' };
           const cmd = buildClaudeCommand(config, 'sess-uuid-xyz', task, 'desc', null, [], null);
           assert.ok(cmd.includes('--worktree sess-uuid-xyz'));
           assert.ok(!cmd.includes('--worktree=')); // no = syntax
           assert.ok(!cmd.match(/--worktree(?!\s+sess-uuid-xyz)/)); // no bare --worktree
         });
       });
       ```

       Si `buildClaudeCommand` no estaba exportado (file-local), añadir `export { buildClaudeCommand };` al final de `src/session/manager.js` para permitir testing — esto es consistente con `escapeShell`/`truncate` ya internas pero NO exportadas; aceptable lift por testabilidad (precedente Phase 16 LOG-15 D-08 que exportó `runStopHook` para DI test).

    6. **NO TOCAR** (invariantes a preservar):
       - `cmux.newWorkspace({ cwd: projectPath })` línea 194–197 (D-04 IDÉNTICO).
       - `inFlight` guard en dispatcher (responsabilidad de Plan 03 / Phase 8 invariant).
       - Lock acquisition/release — sigue en el dispatcher con `gsdProjectPath` (Plan 03 lo verifica).
       - `launchOrchestrator` en `src/orchestrator/launch.js` (Plan 03 lo verifica).
       - El bloque `cmux.notify` (líneas 231–235) y la notificación al orchestrator (237–247) — siguen al final, después de `cmux.send`.
  </action>

  <verify>
    <automated>node --test test/manager.test.js test/dispatcher.test.js test/state.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "computeWorktreePath" src/session/manager.js` >= 2 (1 import + 1 invocación en launchWorkItem)
    - `grep -c "worktree_path:\\s*worktreePath" src/session/manager.js` == 1 (conditional spread en buildSessionFromTask)
    - `grep -c -- "--worktree \\${sessionId}" src/session/manager.js` == 1 (template del cmd en buildClaudeCommand)
    - `grep -c -- "--worktree" src/session/manager.js` >= 1 (al menos el del cmd; tests adicionales pueden añadir más)
    - Orden PRE-spawn verificable con grep multilinea: `addSession(task.id, session)` aparece ANTES de `cmux.send({ workspace: workspaceRef, text: claudeCmd })` en `launchWorkItem`. Test automatizado: `node -e "const s = require('fs').readFileSync('src/session/manager.js', 'utf-8'); const i = s.indexOf('addSession(task.id'); const j = s.indexOf('cmux.send({ workspace: workspaceRef, text: claudeCmd'); process.exit(i > 0 && j > 0 && i < j ? 0 : 1)"` exits 0
    - `node --test test/manager.test.js` exits 0 — todos los `describe`s pasan, incluido el nuevo `buildClaudeCommand cmd shape`
    - `node --test test/dispatcher.test.js` exits 0 — el dispatcher sigue pasando opts sin cambios; ninguna regresión
    - `npm test` exits 0 — suite global verde
    - `grep -n "cmux.newWorkspace.*cwd: projectPath" src/session/manager.js` reporta 1 línea sin mutación (D-04 invariante)
    - `grep -c "schema_version" src/session/manager.js` == 0 (no se introduce migration aquí)
    - El cmd resultante para una sesión quick (`flags: ['gsd-quick']`) preserva `--dangerously-skip-permissions` AL FINAL (post-`--worktree`): regex `/--worktree [a-f0-9-]+ --dangerously-skip-permissions/` debe matchear.
  </acceptance_criteria>

  <done>
    `launchWorkItem` cablea `computeWorktreePath` PRE-spawn, persiste `worktree_path` en state via `addSession` ANTES de `cmux.send`, y `buildClaudeCommand` emite `--worktree <sessionId>` para TODAS las sesiones (full + quick + no-GSD). Tests cubren shape de cmd, persistencia condicional, y compat con sesiones sin worktreePath. Suite verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → `launchWorkItem` | sessionId proviene de `randomUUID()` (dispatcher línea 129) o `opts.sessionId` threaded del lock (también UUID). Trust elevado — input controlado. |
| `launchWorkItem` → `cmux.send(text=claudeCmd)` | El claudeCmd contiene el sessionId interpolado en `--worktree ${sessionId}`. Si sessionId fuera tampered, podría inyectar shell. UUIDs cierran este vector. |
| `launchWorkItem` → `addSession` → `~/.kodo/state.json` | El `worktree_path` se persiste a disco. Si filesystem es shared (no en uso solo personal), otro user podría leer. Mismo modelo de threat que `project_path` ya persistido. |
| TOCTOU pre-spawn | Entre `addSession` y `cmux.send`, otra dispatch concurrente podría intentar el mismo sessionId. El `inFlight` guard del dispatcher (línea 109) y el lock per-repo (Phase 8 GSD-10) cierran este vector ANTES de llegar a `launchWorkItem`. Plan 03 añade fail-fast adicional sobre el path físico. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | sessionId malicioso → shell injection en `--worktree ${sessionId}` | accept | sessionId proviene de `randomUUID()` o `opts.sessionId` (también UUID por flow del dispatcher). Sin entrada de red directa al string. UUIDs `/^[a-f0-9-]+$/i` no contienen `;`, `&`, `$`, `\``, espacios. Si Phase 19+ acepta input externo al sessionId, validar allí; aquí trust-by-construction. |
| T-18-04 | Information Disclosure | `worktree_path` con separators inesperados leakea via cmux/logger | mitigate | El path proviene de `computeWorktreePath` (Plan 01) que es `path.join` puro sobre projectPath (config humano) + `'.bg-shell'` (literal) + sessionId (UUID). Sin chars especiales. Logger NDJSON serializa con `JSON.stringify` (escape automático). Sin nuevo vector vs. `project_path` ya persistido. |
| T-18-05 | Repudiation | `addSession` falla silenciosa entre persist y cmux.send → sesión arranca sin trace | mitigate | `addSession` ya emite `state.session.added` evento en `~/.kodo/logs/<sessionId>.ndjson` (línea 93–96 state.js). Si falla, throw propagado al try/catch del dispatcher (línea 326–334) que libera el lock — la sesión NO arranca. La nueva ordenación PRE-cmux refuerza: si addSession falla, cmux.send NO se llama. |
| T-18-06 | DoS | sessionId colisiona con worktree existente en `.bg-shell/` → cmux.send arranca pero claude --worktree falla | mitigate | Plan 03 implementa fail-fast canonical error en dispatcher ANTES de `launchWorkItem`. Plan 02 NO valida existencia (D-05 lockeado en CONTEXT.md: la verificación va en el dispatcher para mantener `launchWorkItem` enfocado en spawn). |
</threat_model>

<verification>
- `npm test` exits 0 (suite global) — sin regresiones en dispatcher.test.js, gsd-concurrency.test.js, gsd-context.test.js, gsd-verify-integration.test.js, orchestrator-gsd.test.js.
- Smoke manual (post-execute): lanzar una sesión real con `kodo launch <task>` (si setup disponible) y verificar:
  - `cat ~/.kodo/state.json | jq '.sessions[] | .worktree_path'` muestra el path determinístico.
  - El cmd visible en cmux contiene `--worktree <uuid>`.
  - `git -C <projectPath>/.bg-shell/<sessionId> rev-parse --show-toplevel` (desde el agente claude) devuelve el worktree path, no el repo principal — SC#1.
  - Smoke OPCIONAL — Plan 03 cubre esto via test automatizado E2E si el setup local de cmux está disponible.
- Golden bytes Phase 12 QUICK-07 invariante: `node --test test/orchestrator-gsd.test.js test/gsd-context.test.js` exits 0 — las tags `[GSD quick]`/`[GSD phase N]`/`[GSD bootstrap]` siguen byte-idénticas (viven en el prompt, no en el header del cmd que mutamos).
</verification>

<success_criteria>
- `--worktree <sessionId>` presente en TODAS las invocaciones de `launchWorkItem` (no solo GSD).
- `worktree_path` persistido en state.json PRE-spawn (orden `addSession → cmux.send` invariante).
- `cmux.newWorkspace({ cwd: projectPath })` IDÉNTICO (D-04).
- `buildSessionFromTask` retro-compatible con call sites SIN worktreePath (campo aditivo opcional D-03c).
- Tests cubren: shape session con/sin worktree_path, shape cmd con `--worktree` en posición correcta, orden de flags preservado.
- Plan 03 puede asumir que toda sesión llamada via `launchWorkItem` ya lleva el flag y el path persistido.
</success_criteria>

<output>
Crear `.planning/phases/18-worktree-runtime-wiring/18-02-SUMMARY.md` documentando: (a) líneas exactas modificadas en manager.js, (b) reorden PRE-spawn confirmado (addSession ANTES de cmux.send), (c) export adicional de buildClaudeCommand para testing (si se decidió), (d) confirmación de que dispatcher/launchOrchestrator quedan intactos (Plan 03 los valida).
</output>
