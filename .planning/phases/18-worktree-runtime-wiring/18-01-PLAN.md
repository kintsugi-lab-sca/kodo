---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/state.js
  - test/state.test.js
autonomous: true
requirements: [WT-02]
user_setup: []

must_haves:
  truths:
    - "`src/session/state.js` exporta `computeWorktreePath(projectPath, sessionId)` como named export, función pura (sin I/O), determinística — mismo (projectPath, sessionId) siempre devuelve mismo string (D-03 Claude's Discretion: factorizar porque Phase 19 stop-hook lo consumirá para `git worktree remove`)."
    - "`computeWorktreePath('/repo', 'abc-123')` devuelve `'/repo/.bg-shell/abc-123'` (path.join nativo, sin normalize artificial, sin trailing slash) — convención `<projectPath>/.bg-shell/<sessionId>/` documentada en CONTEXT.md §domain (D-01, D-02 lockeadas)."
    - "El typedef `Session` en `src/session/state.js` (líneas 11–32) añade `worktree_path?: string` como **campo aditivo opcional** (D-03c — mismo patrón que `gsd_mode` Phase 11 D-08, `phase_id`/`brief` Phase 9). NO bump de `schema_version` (sigue siendo 2). Sesiones legacy v0.5 sin `worktree_path` se leen como `undefined` y los consumers deben tolerarlo."
    - "El helper NO normaliza ni resuelve symlinks (NO `realpathSync`) — es pure en sentido estricto, sin I/O. La resolución de symlinks es responsabilidad del caller si la necesita (precedente: `gsd/lock.js` SÍ usa `realpathSync` porque opera sobre el lock path; el worktree path no necesita simetría con el lock — son dos invariantes distintos por D-04 y SC#3)."
    - "Existe `test/state.test.js` con al menos 4 asserts sobre `computeWorktreePath`: (a) shape canónico `<projectPath>/.bg-shell/<sessionId>`, (b) determinismo (call dos veces → mismo string), (c) UUID seguro como sessionId (no escape needed), (d) NO usa `realpathSync` (no resuelve symlinks — pure)."
  artifacts:
    - path: "src/session/state.js"
      provides: "Helper puro `computeWorktreePath` + typedef `Session.worktree_path?` aditivo opcional"
      contains: "export function computeWorktreePath"
      min_lines: 175
    - path: "test/state.test.js"
      provides: "4 asserts sobre computeWorktreePath (shape, determinismo, UUID-safe, no I/O)"
      contains: "computeWorktreePath"
      min_lines: 1
  key_links:
    - from: "src/session/state.js"
      to: "node:path"
      via: "import { join } from 'node:path' (existing line 3)"
      pattern: "import\\s+\\{[^}]*\\bjoin\\b[^}]*\\}\\s+from\\s+['\"]node:path['\"]"
    - from: "src/session/state.js#computeWorktreePath"
      to: "node:path#join"
      via: "join(projectPath, '.bg-shell', sessionId)"
      pattern: "join\\(\\s*projectPath\\s*,\\s*['\"]\\.bg-shell['\"]\\s*,\\s*sessionId\\s*\\)"
    - from: "src/session/state.js#typedef Session"
      to: "JSDoc field worktree_path"
      via: "@typedef adds `worktree_path?: string`"
      pattern: "worktree_path\\?:\\s*string"
---

<objective>
Crear el helper puro `computeWorktreePath(projectPath, sessionId)` en `src/session/state.js` y extender el typedef `Session` con el campo aditivo opcional `worktree_path?: string`. Este es el contrato base que consume Plan 02 (cableado en `launchWorkItem`) y consumirá Phase 19 (stop hook `git worktree remove`). El helper se vive en `state.js` por simetría: `STATE_PATH` (constante export), `Session` typedef y ahora `computeWorktreePath` viven juntos.

Decisión de la Claude's Discretion (CONTEXT.md §decisions): factorizamos a helper porque Phase 19 lo consumirá explícitamente para `git worktree remove <worktreePath>` y `kodo gsd verify` (WT-05/WT-06 leyendo `VERIFICATION.md` desde el worktree). Mantener inline en `launchWorkItem` obligaría a duplicar el `join(projectPath, '.bg-shell', sessionId)` o a importar desde `manager.js`, lo que rompería la separación de capas (state ↔ manager).

Purpose: cubrir la base de WT-02 (path determinístico derivado del session-id). Este plan NO toca `launchWorkItem` ni `state.json` runtime — solo añade la signatura/typedef que Plan 02 consumirá.
Output: `src/session/state.js` con +1 export named (`computeWorktreePath`) y +1 campo aditivo en typedef `Session`. `test/state.test.js` con 4 asserts del helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Source of truth — read BEFORE editing
@src/session/state.js
@test/state.test.js

<interfaces>
<!-- Estado actual: src/session/state.js exporta una API funcional sin clases. -->
<!-- Convención del archivo (líneas 1-9): -->

```javascript
// @ts-check
import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { join } from 'node:path';                       // ← `join` ya importado (reusable)
import { KODO_DIR } from '../config.js';
import { noopLogger } from '../logger-noop.js';

const STATE_PATH = join(KODO_DIR, 'state.json');         // ← precedente de helper export
```

```javascript
// Typedef actual (líneas 11–32) — añadimos UNA línea más al final, antes del `}`:
/**
 * @typedef {{
 *   workspace_ref: string,
 *   session_id: string,
 *   task_id: string,
 *   ...
 *   gsd_mode?: 'full'|'quick',
 *   phase_id?: string,
 *   brief?: string,
 *   worktree_path?: string,    // ← NUEVO (D-03c: aditivo, falsy permitido, NO schema_version bump)
 * }} Session
 */
```

```javascript
// Exports al final (línea 172): `export { STATE_PATH };` — añadir `computeWorktreePath`
// como export named en su propia declaración (no junto a STATE_PATH para mantener
// JSDoc completo). Posición: tras `migrateState` (línea 41) o tras `findSession`
// (línea 156); preferencia: cerca de los demás helpers puros, antes de los I/O.
```

Patrón análogo (precedente Phase 8 GSD-10): `src/gsd/lock.js#lockPathFor` — helper pure path-builder que consume `realpathSync` (símbolo del lock; nuestro helper NO lo usa porque el worktree no requiere collapse de symlinks per D-04 y la simetría se prefiere romper aquí).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir helper computeWorktreePath + extender typedef Session.worktree_path?</name>
  <files>src/session/state.js, test/state.test.js</files>

  <read_first>
    - src/session/state.js (estado completo — confirmar typedef Session líneas 11–32, posición exacta donde insertar `worktree_path?`, exports finales línea 172)
    - test/state.test.js (estado actual — confirmar shape `describe`/`it` para añadir bloque `describe('computeWorktreePath')`)
    - .planning/phases/18-worktree-runtime-wiring/18-CONTEXT.md §decisions D-01, D-02, D-03c
    - src/gsd/lock.js líneas 178–181 (`lockPathFor` precedente: helper pure path-builder con realpathSync — leer para CONTRASTAR; nuestro helper NO usa realpathSync)
  </read_first>

  <behavior>
    - Test 1 (shape canónico): `computeWorktreePath('/repo', 'abc-123-uuid')` devuelve exactamente `'/repo/.bg-shell/abc-123-uuid'` (sin trailing slash, sin normalize artificial, segmentos unidos por `path.join`).
    - Test 2 (determinismo): llamar `computeWorktreePath('/repo', 'abc')` dos veces consecutivas devuelve strings byte-idénticos. No hay state interno.
    - Test 3 (UUID-safe sin escape): `computeWorktreePath('/repo', randomUUID())` produce un string parseable como path absoluto sin caracteres especiales (UUIDs son `/^[a-f0-9-]+$/i`).
    - Test 4 (NO realpathSync): el helper NO resuelve symlinks. `computeWorktreePath('/tmp/foo', 'abc')` en macOS devuelve `'/tmp/foo/.bg-shell/abc'` (NO `/private/tmp/foo/.bg-shell/abc`). Esto se afirma SIN crear filesystem real — basta verificar que el output preserva el `projectPath` literal del input. Si el helper hiciera `realpathSync`, el primer segmento cambiaría.
    - Test 5 (typedef): `grep -c "worktree_path?:" src/session/state.js` >= 1 (típicamente == 1 dentro del typedef; el `?:` exacto es el shape JSDoc TypeScript-checked).
    - Test 6 (export): `grep -c "export function computeWorktreePath" src/session/state.js` == 1 (signature específica, no `export const` ni anonymous default).
  </behavior>

  <action>
    1. **Localizar el typedef Session** en `src/session/state.js` (líneas 11–32). Justo antes del cierre `* }} Session` (línea actual ~31), añadir UNA línea con comentario JSDoc del nuevo campo:

       ```javascript
       *   worktree_path?: string,    // Phase 18 (D-03c, aditivo opcional — mismo patrón que gsd_mode Phase 11 D-08). Path determinístico derivado del session-id (`<projectPath>/.bg-shell/<sessionId>`) computado por computeWorktreePath. Sesiones legacy v0.5 sin este campo se leen como undefined; consumers downstream deben tolerar falsy. NO bump de schema_version.
       ```

       NO modificar ningún otro campo del typedef. El orden importa: insertar al final, después de `brief?: string,` para reforzar visualmente que es un campo aditivo nuevo.

    2. **Añadir el helper `computeWorktreePath`** como export named en `src/session/state.js`. Posición sugerida: justo después de `migrateState` (línea 41) y antes de `migrateStateIfNeeded` (línea 53), porque agrupa "helpers puros sin I/O" antes de los helpers con side-effects. Implementación exacta:

       ```javascript
       /**
        * Compute the deterministic worktree path for a session.
        *
        * Phase 18 (D-01, D-02, D-03 — Claude's Discretion factor-into-helper).
        * Pure function: NO realpathSync, NO mkdirSync, NO existsSync — solo `path.join`.
        * Determinístico por (projectPath, sessionId): mismo input → mismo output.
        *
        * Convención: `<projectPath>/.bg-shell/<sessionId>` (CONTEXT.md §domain).
        * `.bg-shell/` es la convención claude para worktrees de sesión (ver
        * `claude --help`). El directorio NO se crea aquí — eso lo hace
        * `claude --worktree <sessionId>` durante el spawn (Plan 02).
        *
        * Phase 19 consumirá este helper para `git worktree remove <worktreePath>`
        * en el stop hook (WT-04). Mantener la firma estable.
        *
        * @param {string} projectPath - Repo principal (no symlinked-resolved aquí; ver D-04).
        * @param {string} sessionId - UUID de la sesión (mismo que `Session.session_id`).
        * @returns {string} Path absoluto sin trailing slash.
        */
       export function computeWorktreePath(projectPath, sessionId) {
         return join(projectPath, '.bg-shell', sessionId);
       }
       ```

       Notas:
       - `join` ya está importado en línea 3 (`import { join } from 'node:path'`) — NO añadir import.
       - NO usar `realpathSync` aquí. El lock (`src/gsd/lock.js#lockPathFor`) sí lo usa para colapsar symlinks (Pitfall 3 macOS), pero el worktree path no requiere esa simetría: el lock identifica el repo como "key" y necesita normalización para coalescer; el worktree path es un destino físico que se crea/destruye literal. Si el caller pasa `/tmp/foo` y macOS lo resuelve a `/private/tmp/foo`, eso es responsabilidad del caller.
       - JSDoc completo con `@param` + `@returns` por convención (CONVENTIONS.md §JSDoc/TSDoc requerido para exports).

    3. **Añadir el bloque de tests** en `test/state.test.js`. Si el archivo no existe o no tiene `import` de `computeWorktreePath`, añadir el import y un `describe` nuevo:

       ```javascript
       import { computeWorktreePath } from '../src/session/state.js';
       import { randomUUID } from 'node:crypto';

       describe('computeWorktreePath', () => {
         it('returns the canonical <projectPath>/.bg-shell/<sessionId> shape', () => {
           assert.equal(
             computeWorktreePath('/repo', 'abc-123-uuid'),
             '/repo/.bg-shell/abc-123-uuid',
           );
         });

         it('is deterministic: same inputs → byte-identical output', () => {
           const a = computeWorktreePath('/repo', 'abc');
           const b = computeWorktreePath('/repo', 'abc');
           assert.equal(a, b);
         });

         it('handles real UUIDs without escaping', () => {
           const id = randomUUID(); // /^[a-f0-9-]+$/i — safe path segment
           const out = computeWorktreePath('/Users/alex/dev/klab/kodo', id);
           assert.equal(out, `/Users/alex/dev/klab/kodo/.bg-shell/${id}`);
           assert.ok(!out.includes('..'), 'no traversal'); // T-18-01 mitigation
         });

         it('does NOT resolve symlinks (no realpathSync)', () => {
           // /tmp en macOS es symlink a /private/tmp. Si el helper hiciera
           // realpathSync, el primer segmento cambiaría a /private/tmp.
           // Verificamos sin filesystem real: el output debe preservar el
           // projectPath literal del input.
           const out = computeWorktreePath('/tmp/foo', 'abc');
           assert.equal(out, '/tmp/foo/.bg-shell/abc');
           assert.ok(!out.startsWith('/private/'), 'must NOT collapse symlink');
         });
       });
       ```

       Si `test/state.test.js` no existe, crearlo con el header estándar (`// @ts-check`, `import { describe, it } from 'node:test'`, `import assert from 'node:assert/strict'`).

    4. **NO TOCAR**: `addSession`, `removeSession`, `updateSession`, `loadState`, `saveState`, `findSession`, `migrateState`, `STATE_PATH`. La typedef se modifica AÑADIENDO una línea, no reordenando ni cambiando tipos existentes.
  </action>

  <verify>
    <automated>node --test test/state.test.js test/migration.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function computeWorktreePath" src/session/state.js` == 1
    - `grep -c "worktree_path?:" src/session/state.js` >= 1
    - `grep -c "schema_version: 3\|schema_version === 3" src/session/state.js` == 0 (NO bump — D-03c invariante)
    - `node --test test/state.test.js` exits 0 (4+ asserts del nuevo describe)
    - `node --test test/migration.test.js` exits 0 (no se rompe la migration v1→v2 existente)
    - Suite completa sin regresiones: `npm test` exits 0
    - `grep -c "realpathSync" src/session/state.js` == 0 (helper NO importa realpathSync — D-04 invariante: el lock sí lo usa, el worktree NO)
    - `grep -n "from 'node:path'" src/session/state.js` reporta exactamente 1 línea (no añade segundo import — `join` ya importado)
  </acceptance_criteria>

  <done>
    `src/session/state.js` exporta `computeWorktreePath` puro y el typedef `Session` incluye `worktree_path?: string`. Tests pasan, no hay schema bump, no hay regresiones.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `computeWorktreePath` | El sessionId proviene de `randomUUID()` (UUIDs v4 son `/^[a-f0-9-]+$/i`, no contienen separators ni `..`). El projectPath proviene de `loadProjects()` (config humano, no untrusted input runtime). Trust boundary baja — no entra input de red. |
| consumer downstream → `Session.worktree_path` | El campo se persiste a `~/.kodo/state.json` y se loggea. Si en el futuro entra input untrusted al sessionId (no en Phase 18), el helper preservaría literal — la sanitización tendría que correr antes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `computeWorktreePath` con sessionId malicioso (`'../etc/passwd'`) | accept | sessionId proviene de `randomUUID()` con regex `/^[a-f0-9-]+$/i`. Path traversal imposible en producción. Test 3 afirma `!out.includes('..')` como defensa-en-profundidad. Si Phase 19+ acepta sessionIds del dispatcher con input externo, añadir validación allí, no aquí. |
| T-18-02 | Information Disclosure | `worktree_path` con `\n` u otros separators leakea via logger | mitigate | El helper devuelve un string que el logger NDJSON serializa con `JSON.stringify` (escape automático de `\n`, `"`, etc.). No se usa `console.log` directo sobre el campo desde Plan 01 (eso es responsabilidad de logger.js). El campo nunca contiene chars peligrosos porque ambos inputs son sanitized-by-construction (UUIDs + paths absolutos). |
| T-18-03 | Disclosure | typedef `worktree_path?` expuesto en `state.json` legible por otros usuarios del sistema | accept | `~/.kodo/state.json` está en `$HOME` con permisos default (700/750 según umask del usuario). Mismo modelo de threat que `project_path` ya persistido (no introduce regresión). Sin escalar permisos: no hay nueva superficie. |
</threat_model>

<verification>
- `npm test` (suite global) exits 0 — no regresiones en `migration.test.js`, `manager.test.js`, ni en consumers de `Session`.
- `node -e "import('./src/session/state.js').then(m => console.log(typeof m.computeWorktreePath))"` imprime `function`.
- TypeScript checking via JSDoc (@ts-check al top de state.js) no reporta errores nuevos: `npx tsc --noEmit --allowJs --checkJs --module nodenext --moduleResolution nodenext src/session/state.js` (best-effort si tsc disponible; opcional si no).
</verification>

<success_criteria>
- Helper puro `computeWorktreePath` exportado y testeado (4 asserts mínimos).
- Typedef `Session` extendido con `worktree_path?: string` aditivo opcional.
- Sin schema_version bump (D-03c).
- Suite global verde.
- Plan 02 puede importar `computeWorktreePath` desde `'../session/state.js'` sin necesidad de tocar este archivo de nuevo.
</success_criteria>

<output>
Crear `.planning/phases/18-worktree-runtime-wiring/18-01-SUMMARY.md` documentando: (a) signature exacta del helper, (b) typedef extension exacta, (c) decisión NO realpathSync (vs lock.js que sí), (d) confirmación de que Plan 02 importará desde state.js.
</output>
