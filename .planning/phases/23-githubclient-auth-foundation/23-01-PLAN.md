---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger-events.js
  - test/logger-events.test.js
autonomous: true
requirements:
  - GH-01
goal: >-
  Extender la taxonomía cerrada `EVENTS` en `src/logger-events.js` con dos eventos GitHub
  (`github.api.call`, `github.api.call.failed`) más sus helpers (`githubApiCall` con switch
  info/warn por `rate_limit_remaining < 100`, `githubApiCallFailed` a nivel error). Actualizar
  `test/logger-events.test.js` de 13 → 15 eventos preservando LOG-12 (logger-events.js solo
  importa stdlib) y sin tocar `picocolors`. Foundation para que Plan 23-02 (GitHubClient)
  pueda emitir NDJSON estructurado.
must_haves:
  truths:
    - "Object.values(EVENTS).length === 15 — los 13 existentes + 'github.api.call' + 'github.api.call.failed'"
    - "Importar { githubApiCall, githubApiCallFailed, EVENTS } desde src/logger-events.js no lanza"
    - "githubApiCall(logger, { rate_limit_remaining: 4998, ... }) emite NDJSON con level='info'"
    - "githubApiCall(logger, { rate_limit_remaining: 50, ... }) emite NDJSON con level='warn'"
    - "githubApiCallFailed(logger, {...}) emite NDJSON con level='error', event='github.api.call.failed'"
    - "test/logger-events.test.js verde con 15 eventos asserted (alfabético)"
    - "test/check-isolation.test.js sigue verde (LOG-12 preservada — logger-events.js no añade imports)"
  artifacts:
    - path: "src/logger-events.js"
      provides: "EVENTS.GITHUB_API_CALL + EVENTS.GITHUB_API_CALL_FAILED + githubApiCall + githubApiCallFailed"
      contains: "github.api.call"
    - path: "test/logger-events.test.js"
      provides: "Asserts 15 eventos + 3 tests nuevos (info/warn/error)"
      contains: "github.api.call.failed"
  key_links:
    - from: "src/logger-events.js"
      to: "EVENTS frozen object"
      via: "Object.freeze({...})"
      pattern: "GITHUB_API_CALL:\\s*'github\\.api\\.call'"
    - from: "src/logger-events.js (githubApiCall)"
      to: "switch info/warn"
      via: "level = fields.rate_limit_remaining < 100 ? 'warn' : 'info'"
      pattern: "rate_limit_remaining.*<\\s*100"
    - from: "test/logger-events.test.js"
      to: "EVENTS array assertion"
      via: "assert.deepEqual(types, [...15 entries...])"
      pattern: "github\\.api\\.call"
---

<objective>
Añadir dos eventos nuevos a la taxonomía cerrada de NDJSON (`src/logger-events.js`) junto con sus
helpers de emisión, y actualizar el test contract correspondiente. Es la única dependencia
upstream de Plan 23-02 (GitHubClient), que importa estos helpers desde su `request()` privado.

**Purpose:**
- Permitir que Plan 23-02 emita `github.api.call` (info por default; warn cuando
  `rate_limit_remaining < 100` per D-15/D-16) y `github.api.call.failed` (error level per D-15)
  sin inventar literales fuera del frozen object.
- Preservar la invariante LOG-12: `src/logger-events.js` sigue importando exclusivamente
  stdlib (`node:os` + `node:path`) — los dos helpers nuevos son pure transform (fields → record)
  igual que los 13 existentes.

**Output:**
- `src/logger-events.js` extendido con 2 entradas en `EVENTS` + 2 funciones exportadas.
- `test/logger-events.test.js` con array de 15 entradas (alfabético) + 3 tests nuevos
  (info path, warn path, failed path).

**Per D-15, D-16, D-17, D-18, D-39 (CONTEXT.md):** estos helpers son la única forma legítima de
emitir los eventos GitHub. Plan 23-02 los consume vía `dynamic await import('../../logger-events.js')`
(patrón establecido por `PlaneClient` líneas 76-88).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-githubclient-auth-foundation/23-CONTEXT.md
@.planning/phases/23-githubclient-auth-foundation/23-RESEARCH.md
@.planning/phases/23-githubclient-auth-foundation/23-PATTERNS.md
@.planning/phases/23-githubclient-auth-foundation/23-VALIDATION.md
@src/logger-events.js
@test/logger-events.test.js

<interfaces>
<!-- Contratos extraídos del codebase. El executor NO debe explorar más. -->

Desde `src/logger-events.js` (HEAD, antes de este plan):
```js
// Imports stdlib ONLY (LOG-12 invariant):
import { homedir } from 'node:os';
import { join } from 'node:path';

// EVENTS frozen actual — 13 entries:
export const EVENTS = Object.freeze({
  SESSION_START:           'session.start',
  SESSION_END:             'session.end',
  STATE_TRANSITION:        'state.transition',
  ORCHESTRATOR_REVIEW:     'orchestrator.review',
  GSD_PHASE_RESOLVED:      'gsd.phase.resolved',
  GSD_BOOTSTRAP:           'gsd.bootstrap',
  PLANE_API_CALL:          'plane.api.call',
  PLANE_API_CALL_FAILED:   'plane.api.call.failed',
  WORKTREE_CLEANUP_OK:     'worktree.cleanup.ok',
  WORKTREE_CLEANUP_DIRTY:  'worktree.cleanup.dirty',
  WORKTREE_CLEANUP_ERROR:  'worktree.cleanup.error',
  SKILL_SYNC_AUTO:         'skill.sync.auto',
  SKILL_SYNC_AUTO_ERROR:   'skill.sync.auto.error',
});

// Patrón A — helper plano (líneas 188-200):
export function planeApiCall(logger, fields) {
  logger.info(EVENTS.PLANE_API_CALL, {
    event: EVENTS.PLANE_API_CALL,
    method: fields.method,
    path: fields.path,
    status: fields.status,
    duration_ms: fields.duration_ms,
  });
}

// Patrón B — helper con SWITCH level info/warn por field (líneas 143-152):
export function orchestratorReview(logger, fields) {
  const level = fields.verdict === 'approved' ? 'info' : 'warn';
  logger[level](EVENTS.ORCHESTRATOR_REVIEW, {
    event: EVENTS.ORCHESTRATOR_REVIEW,
    phase_id: fields.phase_id,
    verdict: fields.verdict,
    reason: fields.reason,
  });
}

// Patrón C — helper a error level (líneas 211-217):
export function planeApiCallFailed(logger, fields) {
  logger.error(EVENTS.PLANE_API_CALL_FAILED, {
    event: EVENTS.PLANE_API_CALL_FAILED,
    step: fields.step,
    error: fields.error,
  });
}
```

Desde `test/logger-events.test.js` (HEAD, antes de este plan):
```js
// Líneas 28-43 — import block actual (13 helpers + EVENTS):
const {
  EVENTS,
  sessionStart, sessionEnd, stateTransition, orchestratorReview,
  gsdPhaseResolved, gsdBootstrap, planeApiCall, planeApiCallFailed,
  worktreeCleanupOk, worktreeCleanupDirty, worktreeCleanupError,
  skillSyncAuto, skillSyncAutoError,
} = await import('../src/logger-events.js');

// Línea 49 — describe label (a actualizar):
describe('logger-events taxonomy (Phase 7 LOG-09 + Phase 19 worktree cleanup + Phase 21 skill sync)', ...

// Líneas 50-68 — assertion del array sort alfabético (13 entries hoy):
it('EVENTS is frozen and contains the 13 canonical types', () => {
  assert.equal(Object.isFrozen(EVENTS), true);
  const types = Object.values(EVENTS).sort();
  assert.deepEqual(types, [
    'gsd.bootstrap',
    'gsd.phase.resolved',
    'orchestrator.review',
    'plane.api.call',
    'plane.api.call.failed',
    'session.end',
    'session.start',
    'skill.sync.auto',
    'skill.sync.auto.error',
    'state.transition',
    'worktree.cleanup.dirty',
    'worktree.cleanup.error',
    'worktree.cleanup.ok',
  ]);
});

// Helper de fixture compartido por toda la suite (líneas 19-26):
const fixture = makeTmpHome({ sessionId: '_bootstrap', label: 'events' });
function logPathFor(sessionId) {
  return join(fixture.homeDir, '.kodo', 'logs', `${sessionId}.ndjson`);
}
// readAllLines(...) viene de './helpers/logger-fixtures.js'.
// El logger real (createLogger from '../src/logger.js') escribe NDJSON al sink;
// los tests leen el archivo y hacen pop() de la última línea.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender EVENTS frozen y JSDoc header con github.api.call + github.api.call.failed</name>
  <files>src/logger-events.js</files>
  <read_first>
    - Leer `src/logger-events.js` líneas 1-52 (header + JSDoc @type + EVENTS frozen actual de 13 entries).
    - Confirmar que los imports actuales son SOLO `node:os` + `node:path` (líneas 20-21). NO añadir imports nuevos.
  </read_first>
  <behavior>
    - El módulo debe seguir teniendo cero imports nuevos tras el cambio (invariante LOG-12).
    - `EVENTS.GITHUB_API_CALL === 'github.api.call'`
    - `EVENTS.GITHUB_API_CALL_FAILED === 'github.api.call.failed'`
    - `Object.values(EVENTS).length === 15`
    - `Object.isFrozen(EVENTS) === true` (el `Object.freeze(...)` ya lo cubre).
  </behavior>
  <action>
    1. Actualizar el bloque comment header (líneas 1-18) — añadir los 2 eventos a la lista enumerada
       (líneas 7-10). Resultado esperado: la lista de eventos en el comment incluye
       `github.api.call, github.api.call.failed` justo después de `plane.api.call.failed,`
       y antes de `worktree.cleanup.ok,`. Añadir parenthetical al cierre: `// + Phase 23 (github client)`.

       Texto exacto a insertar (preservando indent existente):
       ```
       //   gsd.phase.resolved, gsd.bootstrap, plane.api.call, plane.api.call.failed,
       //   github.api.call, github.api.call.failed,
       //   worktree.cleanup.ok, worktree.cleanup.dirty, worktree.cleanup.error,
       ```
       Y en línea 6 (cierre del comment de contrato): añadir
       `// + Phase 23 (github client)` antes del cierre `//`.

    2. Actualizar el JSDoc `@type` (líneas 23-37) — añadir DOS líneas al final del object type literal,
       después de `SKILL_SYNC_AUTO_ERROR: 'skill.sync.auto.error',` y antes del cierre `}>`:
       ```
       *   GITHUB_API_CALL: 'github.api.call',
       *   GITHUB_API_CALL_FAILED: 'github.api.call.failed',
       ```

    3. Extender el `Object.freeze({...})` (líneas 38-52) — añadir DOS entries al final del objeto,
       después de `SKILL_SYNC_AUTO_ERROR: 'skill.sync.auto.error',` y antes del `});`:
       ```js
         GITHUB_API_CALL:         'github.api.call',
         GITHUB_API_CALL_FAILED:  'github.api.call.failed',
       ```
       Alinear las columnas con espacios (mismo padding que `SKILL_SYNC_AUTO_ERROR:` que usa 8 espacios
       entre el `:` y el valor — copiar visualmente el patrón existente).

    **NO añadir imports nuevos.** El array de imports en líneas 20-21 (`node:os`, `node:path`) NO se toca.
    **NO modificar** ninguno de los 13 helpers existentes.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | grep -E "fail|pass" | tail -5</automated>
    <!-- NOTA: este verify intencionalmente FALLA hasta que Task 4 actualice el test contract. -->
    <!-- Verificación que SÍ debe pasar tras esta task:                                     -->
    <!-- node -e "import('./src/logger-events.js').then(({EVENTS}) => { console.log(Object.values(EVENTS).length, Object.isFrozen(EVENTS)); })" -->
    <!-- Output esperado: "15 true"                                                          -->
  </verify>
  <acceptance_criteria>
    - `grep -c "GITHUB_API_CALL" src/logger-events.js` ≥ 4 (1 en JSDoc + 1 en frozen + 1 en JSDoc del 2nd + 1 frozen del 2nd; los uses en helpers añaden más en Task 2/3).
    - `grep "^import" src/logger-events.js` devuelve EXACTAMENTE: `import { homedir } from 'node:os';` y `import { join } from 'node:path';` (sin nuevos imports).
    - `node -e "import('./src/logger-events.js').then(({EVENTS}) => { if (Object.values(EVENTS).length !== 15) process.exit(1); if (!Object.isFrozen(EVENTS)) process.exit(2); if (EVENTS.GITHUB_API_CALL !== 'github.api.call') process.exit(3); if (EVENTS.GITHUB_API_CALL_FAILED !== 'github.api.call.failed') process.exit(4); })"` exit 0.
  </acceptance_criteria>
  <done>EVENTS frozen tiene 15 entries, JSDoc @type y comment header reflejan los 2 nuevos eventos, cero imports nuevos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir helper githubApiCall (info/warn switch por rate_limit_remaining < 100)</name>
  <files>src/logger-events.js</files>
  <read_first>
    - Leer `src/logger-events.js` líneas 139-152 (`orchestratorReview` — patrón de switch por field).
    - Leer `src/logger-events.js` líneas 188-200 (`planeApiCall` — patrón de helper plano método/path/status/duration_ms).
    - Combinar ambos patrones: shape de payload como `planeApiCall` + level switch como `orchestratorReview`,
      pero el predicate es numeric `rate_limit_remaining < 100`.
  </read_first>
  <behavior>
    - Llamar `githubApiCall(logger, { rate_limit_remaining: 4998, ... })` emite UNA línea con `level: 'info'`.
    - Llamar `githubApiCall(logger, { rate_limit_remaining: 50, ... })` emite UNA línea con `level: 'warn'`.
    - Llamar `githubApiCall(logger, { rate_limit_remaining: undefined, ... })` emite `level: 'info'`
      (defensive: `typeof remaining === 'number' && remaining < 100`, undefined NO es < 100).
    - La línea emitida contiene: `event: 'github.api.call'`, `method`, `path`, `status`, `duration_ms`,
      `rate_limit_remaining` (5 fields propios + `event`).
  </behavior>
  <action>
    Insertar la función EXPORTADA inmediatamente después de `planeApiCallFailed` (línea 217 cierre `}` del helper).
    Insertar ANTES de `worktreeCleanupOk` (línea 226) — es decir, al final del bloque de helpers Plane y antes
    de los helpers Worktree, manteniendo el agrupamiento cronológico/temático del archivo.

    Código exacto a insertar (per D-15, D-16, D-17, D-18 — pattern de `orchestratorReview` línea 145):

    ```js
    /**
     * Emitido cuando una llamada a la GitHub API completa exitosamente (Phase 23 D-15/D-16).
     * El nivel del record cambia a `warn` cuando `rate_limit_remaining < 100`; default `info`.
     * Pattern espejo: `orchestratorReview` (switch por field) + `planeApiCall` (shape de payload).
     *
     * El cliente (`src/providers/github/client.js`, Plan 23-02) invoca este helper vía dynamic
     * `await import('../../logger-events.js')` para preservar la invariante LOG-12 (el cliente
     * solo conoce `logger.js`; los helpers viven en una entry del grafo separada).
     *
     * @param {Logger} logger
     * @param {{
     *   method: string,
     *   path: string,
     *   status: number,
     *   duration_ms: number,
     *   rate_limit_remaining: number | undefined,
     * }} fields
     */
    export function githubApiCall(logger, fields) {
      const level =
        typeof fields.rate_limit_remaining === 'number' && fields.rate_limit_remaining < 100
          ? 'warn'
          : 'info';
      logger[level](EVENTS.GITHUB_API_CALL, {
        event: EVENTS.GITHUB_API_CALL,
        method: fields.method,
        path: fields.path,
        status: fields.status,
        duration_ms: fields.duration_ms,
        rate_limit_remaining: fields.rate_limit_remaining,
      });
    }
    ```

    Notas:
    - El `typeof === 'number'` guard es intencional (handles `undefined` cuando GitHub no devuelve
      el header en alguna respuesta — Pitfall #8 de RESEARCH.md).
    - NO refactorizar `orchestratorReview` ni `planeApiCall` para extraer un helper común — YAGNI;
      los 3 switches divergen en predicate (verdict/threshold/no switch).
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | tail -3</automated>
    <!-- Este verify pasa tras Task 4 (que añade los tests). Antes de Task 4, validar manual: -->
    <!-- node -e "import('./src/logger-events.js').then(({githubApiCall}) => console.log(typeof githubApiCall))" -->
    <!-- Esperado: "function"                                                                    -->
  </verify>
  <acceptance_criteria>
    - `grep -c "export function githubApiCall(" src/logger-events.js` === 1.
    - `grep -c "rate_limit_remaining < 100" src/logger-events.js` === 1 (sólo dentro de `githubApiCall`).
    - `grep -c "logger\[level\](EVENTS.GITHUB_API_CALL," src/logger-events.js` === 1.
    - `node -e "import('./src/logger-events.js').then(m => { if (typeof m.githubApiCall !== 'function') process.exit(1); })"` exit 0.
    - `grep "^import" src/logger-events.js | grep -v "node:os\|node:path"` NO devuelve líneas (LOG-12 invariant).
  </acceptance_criteria>
  <done>`githubApiCall` exportado con switch info/warn por `rate_limit_remaining < 100`, JSDoc completo, cero imports nuevos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Añadir helper githubApiCallFailed (error level)</name>
  <files>src/logger-events.js</files>
  <read_first>
    - Leer `src/logger-events.js` líneas 202-217 (`planeApiCallFailed` — patrón de helper error-level).
    - Divergencia respecto a `planeApiCallFailed`: en lugar del campo `step` (gate-step-level), GitHub
      emite la tripleta HTTP `{method, path, status}` per D-15 y per `23-RESEARCH.md §NDJSON Event Additions`.
  </read_first>
  <behavior>
    - Llamar `githubApiCallFailed(logger, { method: 'GET', path: '/repos/o/r/issues/42', status: 404, error: 'Not Found' })`
      emite UNA línea con `level: 'error'`, `event: 'github.api.call.failed'`, los 4 fields presentes.
    - Siempre `level: 'error'` (no hay switch).
    - El campo `error` es un string snippet del body de error (≤ 200 chars — el cliente Plan 23-02 hace el slice).
  </behavior>
  <action>
    Insertar la función EXPORTADA inmediatamente después de `githubApiCall` (helper añadido en Task 2),
    antes de `worktreeCleanupOk`. Mantiene el agrupamiento github.* contiguo.

    Código exacto:

    ```js
    /**
     * Emitido cuando una llamada a GitHub API falla (HTTP `!res.ok`) — Phase 23 D-15.
     * Complementa `github.api.call`: el cliente emite uno u otro por request (nunca ambos).
     *
     * Divergencia respecto a `planeApiCallFailed`: en lugar de `step` (gate-step-level),
     * GitHub usa la tripleta HTTP `{method, path, status}` + `error` snippet del body
     * (truncado a 200 chars por el caller para evitar fugas de payload sensible).
     *
     * @param {Logger} logger
     * @param {{ method: string, path: string, status: number, error: string }} fields
     */
    export function githubApiCallFailed(logger, fields) {
      logger.error(EVENTS.GITHUB_API_CALL_FAILED, {
        event: EVENTS.GITHUB_API_CALL_FAILED,
        method: fields.method,
        path: fields.path,
        status: fields.status,
        error: fields.error,
      });
    }
    ```

    NO modificar `planeApiCallFailed` ni cambiarle el shape (los consumers Plane siguen pasando `step`).
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | tail -3</automated>
    <!-- Tras Task 4. Pre-Task-4: -->
    <!-- node -e "import('./src/logger-events.js').then(m => console.log(typeof m.githubApiCallFailed))" → "function" -->
  </verify>
  <acceptance_criteria>
    - `grep -c "export function githubApiCallFailed(" src/logger-events.js` === 1.
    - `grep -c "logger.error(EVENTS.GITHUB_API_CALL_FAILED," src/logger-events.js` === 1.
    - `grep "step:" src/logger-events.js | grep -c "githubApiCallFailed"` === 0 (NO usa `step` — usa `method/path/status`).
    - `node -e "import('./src/logger-events.js').then(m => { if (typeof m.githubApiCallFailed !== 'function') process.exit(1); })"` exit 0.
    - `grep "^import" src/logger-events.js | grep -v "node:os\|node:path"` NO devuelve líneas (LOG-12 invariant).
  </acceptance_criteria>
  <done>`githubApiCallFailed` exportado, level=error fijo, payload `{method, path, status, error}`, JSDoc completo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Actualizar test/logger-events.test.js — imports + array de 13 → 15 + describe label</name>
  <files>test/logger-events.test.js</files>
  <read_first>
    - Leer `test/logger-events.test.js` líneas 28-43 (destructured import block actual).
    - Leer líneas 49-68 (describe label + assertion del array de 13 sorted).
    - Confirmar que el array está sorted alfabético — la inserción de `'github.api.call'` y
      `'github.api.call.failed'` va AL INICIO del array (`g` < `gsd` lexicográficamente porque
      el sort ordena char-by-char y `gi` < `gs`).
  </read_first>
  <behavior>
    - El test `'EVENTS is frozen and contains the … canonical types'` espera 15 entries (no 13).
    - Los nombres alfabéticos quedan: `github.api.call`, `github.api.call.failed`, `gsd.bootstrap`, …
      (verificar: `['github.api.call', 'github.api.call.failed', 'gsd.bootstrap'].sort()` confirma
      que `github` viene antes que `gsd` porque `i(105) < s(115)` al comparar `gi` vs `gs`).
    - El destructured import bloque incluye `githubApiCall, githubApiCallFailed` después de
      `planeApiCallFailed`.
  </behavior>
  <action>
    1. **Líneas 28-43 — extender el destructured import:**
       Después de `planeApiCallFailed,` (línea 37), antes de `worktreeCleanupOk,` (línea 38),
       insertar:
       ```js
         githubApiCall,
         githubApiCallFailed,
       ```

    2. **Línea 49 — actualizar describe label:**
       Cambiar de:
       ```js
       describe('logger-events taxonomy (Phase 7 LOG-09 + Phase 19 worktree cleanup + Phase 21 skill sync)', () => {
       ```
       A:
       ```js
       describe('logger-events taxonomy (Phase 7 LOG-09 + Phase 19 worktree cleanup + Phase 21 skill sync + Phase 23 github client)', () => {
       ```

    3. **Líneas 50-68 — actualizar el array assertion:**
       Cambiar la línea 50 (string del `it(...)`):
       ```js
       it('EVENTS is frozen and contains the 13 canonical types', () => {
       ```
       Por:
       ```js
       it('EVENTS is frozen and contains the 15 canonical types', () => {
       ```

       Cambiar el array de `assert.deepEqual(types, [...])` para que incluya las 2 entries
       nuevas al inicio (mantener sort alfabético):
       ```js
       assert.deepEqual(types, [
         'github.api.call',
         'github.api.call.failed',
         'gsd.bootstrap',
         'gsd.phase.resolved',
         'orchestrator.review',
         'plane.api.call',
         'plane.api.call.failed',
         'session.end',
         'session.start',
         'skill.sync.auto',
         'skill.sync.auto.error',
         'state.transition',
         'worktree.cleanup.dirty',
         'worktree.cleanup.error',
         'worktree.cleanup.ok',
       ]);
       ```

    **NO tocar los otros 17 tests** del archivo — siguen verdes con los helpers existentes.
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | grep -E "^(# pass|# fail|# tests|^not ok)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "githubApiCall" test/logger-events.test.js` ≥ 2 (1 en import + 1+ en tests añadidos en Task 5).
    - `grep -c "githubApiCallFailed" test/logger-events.test.js` ≥ 2.
    - `grep "EVENTS is frozen and contains the 15" test/logger-events.test.js` devuelve 1 línea.
    - `grep "'github.api.call'," test/logger-events.test.js` devuelve 1 línea (la del array sort).
    - `node --test test/logger-events.test.js 2>&1 | grep -c "^ok "` ≥ 18 (los 17 existentes + el bumped a 15).
  </acceptance_criteria>
  <done>Test contract actualizado: imports incluyen los 2 nuevos helpers, array assertion espera 15 entries en orden alfabético, describe label refleja Phase 23.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Añadir 3 tests nuevos — githubApiCall info+warn + githubApiCallFailed</name>
  <files>test/logger-events.test.js</files>
  <read_first>
    - Leer `test/logger-events.test.js` líneas 200-226 (tests existentes `planeApiCall` + `planeApiCallFailed`).
      Son los analogs directos.
    - Leer líneas 137-150 (`orchestratorReview` test — patrón de assertion del level switch).
    - El logger real (`createLogger from '../src/logger.js'`) ya escribe NDJSON al sink del fixture
      HOME — `readAllLines(logPathFor(sessionId)).pop()` devuelve la última línea con `.level`
      añadido por `src/logger.js` (NO lo añade el helper).
  </read_first>
  <behavior>
    - **Test A (info path):** `githubApiCall` con `rate_limit_remaining: 4998` emite NDJSON con
      `event: 'github.api.call'`, `level: 'info'`, `rate_limit_remaining: 4998`, los demás fields presentes.
    - **Test B (warn path):** `githubApiCall` con `rate_limit_remaining: 50` emite con `level: 'warn'`,
      mismo `event`, mismo shape, `rate_limit_remaining: 50`.
    - **Test C (failed):** `githubApiCallFailed` emite `event: 'github.api.call.failed'`, `level: 'error'`,
      los 4 fields `method/path/status/error` correctos.
  </behavior>
  <action>
    Insertar los 3 tests inmediatamente DESPUÉS del test `'planeApiCallFailed emits event=plane.api.call.failed + step/error at error level'`
    (línea 226 cierre `});`) y ANTES del test `'worktreeCleanupOk emits …'` (línea 228).

    Código a insertar (3 `it(...)` consecutivos):

    ```js
      // ─── Phase 23 D-15/D-16: github api call helpers ─────────────────────────

      it('githubApiCall emits at info level when rate_limit_remaining >= 100', () => {
        const sessionId = 'sess-ev-ghac-info';
        const log = createLogger({ sessionId, minLevel: 'info' });
        githubApiCall(log, {
          method: 'GET',
          path: '/repos/octocat/hello-world/issues/42',
          status: 200,
          duration_ms: 123,
          rate_limit_remaining: 4998,
        });
        const line = readAllLines(logPathFor(sessionId)).pop();
        assert.equal(line.event, EVENTS.GITHUB_API_CALL);
        assert.equal(line.level, 'info');
        assert.equal(line.method, 'GET');
        assert.equal(line.path, '/repos/octocat/hello-world/issues/42');
        assert.equal(line.status, 200);
        assert.equal(line.duration_ms, 123);
        assert.equal(line.rate_limit_remaining, 4998);
      });

      it('githubApiCall emits at warn level when rate_limit_remaining < 100 (D-16 threshold)', () => {
        const sessionId = 'sess-ev-ghac-warn';
        const log = createLogger({ sessionId, minLevel: 'info' });
        githubApiCall(log, {
          method: 'GET',
          path: '/repos/octocat/hello-world/issues',
          status: 200,
          duration_ms: 80,
          rate_limit_remaining: 50,
        });
        const line = readAllLines(logPathFor(sessionId)).pop();
        assert.equal(line.event, EVENTS.GITHUB_API_CALL);
        assert.equal(line.level, 'warn');
        assert.equal(line.rate_limit_remaining, 50);
      });

      it('githubApiCallFailed emits event=github.api.call.failed + method/path/status/error at error level', () => {
        const sessionId = 'sess-ev-ghacf';
        const log = createLogger({ sessionId, minLevel: 'info' });
        githubApiCallFailed(log, {
          method: 'GET',
          path: '/repos/octocat/hello-world/issues/42',
          status: 404,
          error: 'Not Found',
        });
        const line = readAllLines(logPathFor(sessionId)).pop();
        assert.equal(line.event, EVENTS.GITHUB_API_CALL_FAILED);
        assert.equal(line.level, 'error');
        assert.equal(line.method, 'GET');
        assert.equal(line.path, '/repos/octocat/hello-world/issues/42');
        assert.equal(line.status, 404);
        assert.equal(line.error, 'Not Found');
      });
    ```

    Nota: los `sessionId` son únicos por test (`-info`, `-warn`, `-ghacf`) para evitar colisiones
    de NDJSON entre tests (patrón establecido en todo el archivo).
  </action>
  <verify>
    <automated>node --test test/logger-events.test.js 2>&1 | grep -E "^ok|^not ok" | wc -l</automated>
    <!-- Esperado: ≥ 20 (17 baseline + 3 nuevos). Si "not ok" aparece, fallar. -->
  </verify>
  <acceptance_criteria>
    - `grep -c "githubApiCall emits at info level" test/logger-events.test.js` === 1.
    - `grep -c "githubApiCall emits at warn level" test/logger-events.test.js` === 1.
    - `grep -c "githubApiCallFailed emits event=github.api.call.failed" test/logger-events.test.js` === 1.
    - `node --test test/logger-events.test.js 2>&1 | grep -c "^not ok"` === 0.
    - `node --test test/logger-events.test.js 2>&1 | grep "^# pass" | head -1` muestra ≥ 20.
  </acceptance_criteria>
  <done>3 tests nuevos pasan en verde. Suite completa de logger-events sigue con cero fallos.</done>
</task>

<task type="auto">
  <name>Task 6: Verificar invariantes globales — LOG-12, color isolation, npm test full suite</name>
  <files></files>
  <read_first>
    - Repasar `23-RESEARCH.md §LOG-12 invariant preservation argument`.
    - Confirmar que las modificaciones de Tasks 1-3 NO añadieron imports a `src/logger-events.js`.
    - Confirmar que las modificaciones de Tasks 4-5 NO añadieron imports nuevos a `test/logger-events.test.js`.
  </read_first>
  <action>
    Ejecutar las 3 verificaciones invariantes y la suite full:

    1. **LOG-12 invariant guard (canary existente):**
       ```bash
       node --test test/check-isolation.test.js
       ```
       Debe terminar con `# pass N / # fail 0`.

    2. **Color isolation — `picocolors` NO importado desde logger-events.js:**
       ```bash
       grep -nE "picocolors|from\s+['\"]picocolors" src/logger-events.js test/logger-events.test.js && echo "FAIL: picocolors leaked" && exit 1 || echo "OK: no picocolors"
       ```
       Debe imprimir `OK: no picocolors`.

    3. **Imports stdlib-only en logger-events.js:**
       ```bash
       grep "^import" src/logger-events.js
       ```
       Esperado EXACTAMENTE 2 líneas: `import { homedir } from 'node:os';` y `import { join } from 'node:path';`.

    4. **Suite completa:**
       ```bash
       npm test
       ```
       Debe terminar verde con baseline ≥ 614 tests + los 3 nuevos = ≥ 617.

    Si CUALQUIER paso falla, NO commitear — investigar el fallo y corregir la task afectada antes
    de continuar a Plan 23-02.
  </action>
  <verify>
    <automated>node --test test/check-isolation.test.js test/logger-events.test.js 2>&1 | tail -5 && grep "^import" src/logger-events.js | grep -v "node:os\|node:path"; echo "---npm test summary---"; npm test 2>&1 | grep -E "^# (pass|fail|tests)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/check-isolation.test.js 2>&1 | grep -c "^not ok"` === 0.
    - `grep "^import" src/logger-events.js | grep -vE "node:os|node:path"` devuelve 0 líneas.
    - `grep -nE "picocolors" src/logger-events.js test/logger-events.test.js` devuelve 0 líneas.
    - `npm test 2>&1 | grep -E "^# fail" | awk '{print $3}'` === `0`.
    - `npm test 2>&1 | grep -E "^# pass" | awk '{print $3}'` ≥ 617 (baseline 614 + 3 nuevos).
  </acceptance_criteria>
  <done>LOG-12 canary verde, picocolors no aparece en archivos modificados, suite completa green con ≥ 617 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logger-events.js → logger.js sink | El helper invoca `logger[level](event, fields)` que escribe NDJSON a disco. `fields` viene del cliente Plan 23-02; el redactor regex de Phase 6 ya filtra secrets — Phase 23 NO añade nueva surface de redacción. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `error` field en `githubApiCallFailed` (snippet de body de error) | mitigate | El caller (Plan 23-02 client) trunca a 200 chars antes de invocar; tests verifican shape pero no payload sensible (D-14). Documentado en JSDoc de Task 3. |
| T-23-02 | Tampering | `EVENTS` frozen object | mitigate | `Object.freeze(...)` ya activo; el test contract (Task 4) asserts `Object.isFrozen(EVENTS) === true`. |
| T-23-03 | Tampering (invariant LOG-12) | import graph de `logger-events.js` | mitigate | Tasks 1-3 explícitamente prohíben imports nuevos; Task 6 valida via `grep "^import"` y via `check-isolation.test.js` canary. |
</threat_model>

<verification>
- `node --test test/logger-events.test.js` — verde con ≥ 20 tests (17 baseline + 3 nuevos).
- `node --test test/check-isolation.test.js` — verde (LOG-12 invariant preservada).
- `grep "^import" src/logger-events.js` — EXACTAMENTE 2 líneas (`node:os`, `node:path`).
- `grep -nE "picocolors" src/logger-events.js test/logger-events.test.js` — 0 líneas.
- `npm test` — verde con baseline ≥ 614 → ≥ 617 (3 nuevos).
- `node -e "import('./src/logger-events.js').then(m => { if (Object.values(m.EVENTS).length !== 15 || typeof m.githubApiCall !== 'function' || typeof m.githubApiCallFailed !== 'function') process.exit(1); })"` exit 0.
</verification>

<success_criteria>
**ROADMAP SC mapping (Phase 23):**

- **SC#2 (rate-limit warn NDJSON):** este plan provee la infrastructure de emisión (`githubApiCall` con switch info/warn por `rate_limit_remaining < 100`). El cliente Plan 23-02 hace el wire-up.
- **SC#2 (`github.api.call.failed`):** este plan provee el helper error-level.

(SC#1 y SC#3 y SC#4 — los cubre Plan 23-02; SC#2 wiring del cliente también en Plan 23-02.)

**Plan-level success:**
- 15 events en `EVENTS` frozen, alphabetically asserted en el test contract.
- 2 helpers exportados con JSDoc completo y shape de fields documentado.
- 3 tests nuevos verdes (info/warn/error paths).
- LOG-12 invariant: cero imports nuevos en `src/logger-events.js`.
- Color isolation: cero `picocolors` en archivos tocados.
- `npm test` baseline + 3 = ≥ 617 pass.
</success_criteria>

<output>
After completion, create `.planning/phases/23-githubclient-auth-foundation/23-01-SUMMARY.md`
following `$HOME/.claude/get-shit-done/templates/summary.md`. Capture:
- Final state of `EVENTS` (15 entries) and the 2 new helpers' signatures.
- Test counts before/after (baseline → +3 = 617+).
- LOG-12 invariant evidence (grep output proving stdlib-only imports).
- Any pitfalls encountered (e.g., test sessionId collisions, NDJSON sort artifacts).
</output>
