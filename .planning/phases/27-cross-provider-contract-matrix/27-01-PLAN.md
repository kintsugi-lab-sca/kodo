---
phase: 27-cross-provider-contract-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/providers/contract.test.js
autonomous: true
requirements: [TEST-03]
requirements_addressed: [TEST-03]
tags: [test-only, cross-provider, contract-matrix, invariant-v0.2]

must_haves:
  truths:
    - "El test file itera estructuralmente sobre ['plane', 'github'] con un único for...of loop alrededor de describe(...)"
    - "Cada describe corre los mismos N=7 asserts core contra el provider correspondiente"
    - "El TaskItem retornado por getTask() en ambos providers tiene EXACTAMENTE los 11 campos canonical (D-18 leak guard cross-provider)"
    - "getTask(badRef) lanza Error con .message string en ambos providers (contract negativo simétrico, SIN equality .code o mensaje literal)"
    - "parseTriggerEvent({}) returns null en ambos providers (nunca throw, nunca undefined)"
    - "verifySignature('', {}) returns false en ambos providers (nunca throw)"
    - "listProjects() retorna Array<{id, identifier, name}> con cada campo string, en ambos providers"
    - "Zero llamadas a la red real — file-level live-fetch leak guard ataja cualquier path no-stubbeado"
    - "El test count del archivo es derivado por construcción: PROVIDERS.length × N_asserts (nunca hardcoded)"
    - "Suite global termina en ≥ 763 + 14 pass (baseline post-Phase-26 + 7 asserts × 2 providers), 0 fail, 1 skipped preexistente"
  artifacts:
    - path: "test/providers/contract.test.js"
      provides: "Matrix runner — itera ['plane', 'github'] × 7 asserts core; único test file de la phase"
      min_lines: 200
      contains: "for (const providerName of PROVIDERS)"
    - path: "test/providers/contract.test.js"
      provides: "File-level live-fetch leak guard (before/after que sustituye globalThis.fetch por thrower)"
      contains: "live fetch leak"
  key_links:
    - from: "test/providers/contract.test.js"
      to: "src/interface.js#TASK_PROVIDER_METHODS (lines 50-60)"
      via: "import + for-of loop validando typeof provider[method] === 'function'"
      pattern: "import.*TASK_PROVIDER_METHODS.*from.*interface"
    - from: "test/providers/contract.test.js"
      to: "src/providers/plane/provider.js#createPlaneProvider"
      via: "import directo + stubFetch routes + await provider.init()"
      pattern: "createPlaneProvider"
    - from: "test/providers/contract.test.js"
      to: "src/providers/github/provider.js#createGitHubProvider"
      via: "import directo + opts.client injection (fakeGitHubClient) + await provider.init() no-op"
      pattern: "createGitHubProvider.*client:"
    - from: "test/providers/contract.test.js"
      to: "test/fixtures/plane-workitem.json + plane-labels.json"
      via: "import attributes with { type: 'json' } — fixture nativo Plane (identifier KL, sequence_id 42)"
      pattern: "from.*fixtures/plane-workitem"
    - from: "test/providers/contract.test.js"
      to: "test/fixtures/github/issue.json"
      via: "import attributes with { type: 'json' } — override fakeClient.getIssue retorna este fixture"
      pattern: "from.*fixtures/github/issue"
---

<objective>
Crear `test/providers/contract.test.js` — un único matrix runner que itera estructuralmente sobre `['plane', 'github']` ejecutando los mismos 7 asserts contract core contra cada provider con fixtures offline. Demuestra empíricamente el invariante v0.2 ("cambiar de provider no requiere reescribir lógica") con dos adapters reales en producción.

Purpose: Cerrar TEST-03 — el último requirement pendiente del milestone v0.7. Sin este test, el roadmap dice "provider-agnostic" pero no hay prueba ejecutable cross-adapter; con este test, cualquier regresión que rompa simetría (e.g., GitHub normalizer empieza a emitir `description: null` cuando Plane emite `''`) revienta loud en CI.

Output:
- `test/providers/contract.test.js` (~250-300 LOC) con file-level live-fetch leak guard + matrix loop estructural + 7 asserts core + helpers inline (`CANONICAL_TASK_ITEM_KEYS`, `assertTaskItemShape`, `instantiateProvider`, `getValidRef`, `getInvalidRef`, `makeFakeGitHubClient`, `stubPlaneFetch`).
- Suite global delta: +14 nuevos casos pass (7 × 2 providers), test count derivado por construcción.
- Zero cambios a `src/**`, `test/plane-provider.test.js`, `test/providers/github/provider.test.js`, fixtures existentes (test-only phase, Karpathy Rule 3 cambios quirúrgicos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-cross-provider-contract-matrix/27-RESEARCH.md
@.planning/phases/27-cross-provider-contract-matrix/27-PATTERNS.md
@.planning/phases/27-cross-provider-contract-matrix/27-VALIDATION.md

<interfaces>
<!-- Single source of truth — el matrix IMPORTA estos symbols, NO los redeclara. -->

From `src/interface.js:11-24` (TaskItem canonical typedef — 11 fields, anclado en D-18 leak guard Phase 24):
```
TaskItem = {
  id: string,
  ref: string,
  title: string,
  description: string,
  labels: string[],
  projectId: string,
  projectName: string,
  groups: string[],
  url: string,
  priority: 'urgent'|'high'|'medium'|'low'|'none'|null,
  state?: string,   // OPTIONAL — algunos providers lo omiten; el matrix NO lo asserta requerido
}
```

From `src/interface.js:50-60` (TASK_PROVIDER_METHODS frozen array — los 9 nombres canonical):
```
['init', 'getTask', 'updateTaskState', 'addComment', 'listPendingTasks',
 'parseTriggerEvent', 'verifySignature', 'resolveRef', 'listProjects']
```

From `src/providers/plane/provider.js:24` (firma factory):
```
createPlaneProvider(config: PlaneProviderConfig, opts?: { logger? }): TaskProvider
```
- `config.projects` array de `{ id, identifier, name }` — el helper Plane DEBE pasar `identifier: 'KL'` matching `test/fixtures/plane-workitem.json` (sequence_id 42). Pitfall #5.
- `init()` llama 3 endpoints por proyecto (`/labels/`, `/states/`, `/modules/`). Sin init, `listPendingTasks` y `getTask` fallan porque `stateCache` está vacío.

From `src/providers/github/provider.js:67` (firma factory):
```
createGitHubProvider(config: GitHubProviderConfig, opts?: { logger?, client?: GitHubClient }): TaskProvider
```
- `opts.client` injection (D-36 Phase 24) — fakeClient con 5 métodos (`getIssue`, `listIssues`, `addComment`, `updateIssue`, `listLabels`).
- `init()` es no-op (D-19), zero API calls, zero side-effects.

From `test/fixtures/plane-workitem.json` (datos clave):
- `sequence_id: 42` → `ref` resuelto: `'KL-42'`
- `project.identifier: 'KL'` → debe matchear `config.projects[0].identifier`
- `state: 'd1e2f3a4-b5c6-7890-1234-567890abcdef'` (UUID) → `stateCache` debe mappear este UUID a `'In Progress'` (matching `config.states.trigger`)

From `test/fixtures/github/issue.json` (datos clave):
- `number: 42` → `ref` resuelto: `'octocat/hello-world#42'` (matching helper input)
- `node_id`, `labels`, `state: 'open'`, `html_url` — todos campos consumidos por `normalizeIssue`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test/providers/contract.test.js con matrix loop + leak guard + 7 asserts</name>

  <read_first>
    - /Users/alex/dev/klab/kodo/.planning/phases/27-cross-provider-contract-matrix/27-PATTERNS.md (TODO el documento — define los 10 patterns A-J literalmente listos para copiar)
    - /Users/alex/dev/klab/kodo/test/providers/github/provider.test.js líneas 1-105 (Pattern A header + Pattern B leak guard + Pattern H fakeClient — copiar adaptado al matrix)
    - /Users/alex/dev/klab/kodo/test/plane-provider.test.js líneas 60-77 (Pattern I — `stubFetch` route table, adaptar para tirar throw en miss en lugar de devolver `{ results: [] }` default — Pitfall #7 fail-loud)
    - /Users/alex/dev/klab/kodo/src/interface.js líneas 11-24, 50-60 (TaskItem typedef + TASK_PROVIDER_METHODS — import, NO redeclarar)
    - /Users/alex/dev/klab/kodo/src/providers/plane/provider.js líneas 24-32, 67-130, 197-241 (factory signature + init() ciclos + listPendingTasks + listProjects — entender qué endpoints stubear)
    - /Users/alex/dev/klab/kodo/src/providers/github/provider.js líneas 67-110 (factory signature + opts.client injection + init() no-op)
    - /Users/alex/dev/klab/kodo/test/fixtures/plane-workitem.json (identifier 'KL', sequence_id 42, state UUID `d1e2f3a4...`)
    - /Users/alex/dev/klab/kodo/test/fixtures/plane-labels.json (4 labels — el matrix necesita un fixture parseable para `/labels/` endpoint)
    - /Users/alex/dev/klab/kodo/test/fixtures/github/issue.json (override de fakeClient.getIssue retorna esto)
  </read_first>

  <files>test/providers/contract.test.js</files>

  <behavior>
    El archivo cubre 7 asserts core (un `it(...)` por behavior) DENTRO del loop `for (const providerName of PROVIDERS) describe(...)`. NUNCA hay un `it()` top-level. El test count derivado: 7 × 2 = 14 nuevos casos pass.

    Behaviors a cubrir (Pattern letter del PATTERNS.md entre paréntesis):

    - **B1 (Pattern D — contract loop):** `it('exposes 9 TASK_PROVIDER_METHODS as functions')` — itera `TASK_PROVIDER_METHODS` y asserta `typeof provider[method] === 'function'`. Error message: `[${providerName}] missing method: ${method}`.
    - **B2 (Pattern E — TaskItem shape):** `it('getTask(validRef) returns TaskItem with exactly 11 canonical fields')` — llama `provider.getTask(getValidRef(providerName))`, invoca `assertTaskItemShape(task, providerName)`. Asserta `Object.keys(task).sort()` deepEqual a `[...CANONICAL_TASK_ITEM_KEYS].sort()`. NOTA: si `task.state` está presente (campo opcional `state?` del typedef), el assert exact-11 fallaría — `assertTaskItemShape` debe IGNORAR `state` del diff (filter del set comparado) o asertar `subset ⊆ CANONICAL_KEYS ∪ {state}`. Verificar shape real con un `console.log` durante RED si hace falta — el helper queda definitivo en GREEN.
    - **B3 (Pattern F — error contract negativo):** `it('getTask(invalidRef) throws Error with message string')` — `await assert.rejects(() => provider.getTask(getInvalidRef(providerName)), (err) => err instanceof Error && typeof err.message === 'string')`. NO comparar `.code` ni mensaje literal (Plane lanza `Work item KL-9999 not found`; GitHub lanza Error con `.code='not_found'` del client). Pitfall #6.
    - **B4 (Pattern F — parseTriggerEvent simétrico):** `it('parseTriggerEvent({}) returns null')` — asserta `provider.parseTriggerEvent({}) === null` (Plane: payload sin `event` field → null por normalize.parseTriggerEvent; GitHub: D-26 always-null).
    - **B5 (Pattern F — verifySignature simétrico):** `it('verifySignature("", {}) returns false')` — asserta `provider.verifySignature('', {}) === false` (Plane: missing `x-plane-signature` header early-returns false (provider.js:223); GitHub: D-27 always-false).
    - **B6 (Pattern G — listProjects shape):** `it('listProjects returns Array<{id, identifier, name}>')` — asserta `Array.isArray(projects)`, `projects.length >= 1`, y para cada `p`: `typeof p.id === 'string'`, `typeof p.identifier === 'string'`, `typeof p.name === 'string'`. Para Plane, el stub debe mockear `/projects/` endpoint con un payload `[{ id, identifier, name }]`.
    - **B7 (init no-throw):** `it('init() does not throw')` — `await provider.init()` envuelto en `assert.doesNotReject`. Símétrico: Plane requiere init para warmup; GitHub es no-op (D-19); ambos deben resolver sin throw.

    Estructura del archivo (orden estricto top-to-bottom):

    1. Header docblock con `@ts-check` + nota explicativa del invariante estructural Pitfall #3 (cita textual al top: "TODOS los it() viven DENTRO del for-of loop. Plan-checker valida con grep.")
    2. Imports `node:test` + `node:assert/strict` + `TASK_PROVIDER_METHODS` desde `../../src/interface.js` + factories desde `../../src/providers/{plane,github}/provider.js` + fixtures con import attributes (`with { type: 'json' }`)
    3. `const PROVIDERS = Object.freeze(['plane', 'github'])` (frozen para que un `.push()` rompa loud, NO con `/** @type {const} */` JSDoc que permite mutation)
    4. `const CANONICAL_TASK_ITEM_KEYS = Object.freeze([...11 fields...])` — single source of truth
    5. Helper `assertTaskItemShape(task, providerName)` — keys diff + tipos por campo (con filtrado de `state?` opcional)
    6. Helper `makeFakeGitHubClient(overrides)` — lift de `test/providers/github/provider.test.js:61-105`. Override `getIssue` retorna `issueFixture`; `listIssues` retorna `{ status: 200, items: [issueFixture], etag: undefined }` para que `listPendingTasks` retorne ≥0 items sin requerir state filtering (GitHub provider lista sin filter por state).
    7. Helper `stubPlaneFetch(routes)` — route table; cualquier path no-mapeado hace `throw new Error('plane stub miss: ' + path)` (Pitfall #7 fail-loud). Retorna `{ restore }`.
    8. Helper `instantiateProvider(name)` — retorna `{ provider, cleanup }`. Plane: monta `stubPlaneFetch` con 5 rutas (`/projects/`, `/labels/`, `/states/`, `/modules/`, `/work-items/` o equivalente `/issues/`), llama `createPlaneProvider(MOCK_PLANE_CONFIG)`. GitHub: llama `createGitHubProvider(MOCK_GITHUB_CONFIG, { client: makeFakeGitHubClient({ getIssue: () => issueFixture }) })`, `cleanup = () => {}`.
    9. `getValidRef(name)` → `'KL-42'` | `'octocat/hello-world#42'`; `getInvalidRef(name)` → `'KL-9999'` | `'octocat/hello-world#9999'`
    10. File-level live-fetch leak guard `before(() => { globalThis.fetch = () => { throw new Error('live fetch leak: contract matrix must stub or inject') } })` + `after(() => { globalThis.fetch = _originalFetch })`. Crítico: `_originalFetch = globalThis.fetch` capturado ANTES del `before` (top-level const).
    11. `for (const providerName of PROVIDERS) describe(`TaskProvider contract — ${providerName}`, () => { ... })` con `let provider; let cleanup;` + `beforeEach({provider, cleanup} = await instantiateProvider(providerName); await provider.init())` + `afterEach(() => cleanup())` + los 7 `it(...)` declarados arriba.

    Stubs Plane necesarios (cubrir TODAS las rutas que `provider.init()` + `listProjects` + `getTask('KL-42')` tocan; el resto se cae al guard fail-loud):
    - `/projects/` → `[{ id: 'p0p0p0p0-1111-2222-3333-444444444444', identifier: 'KL', name: 'Kodo Lab' }]` (consumido por `listProjects` provider.js:235)
    - `/labels/` → `planeLabelsFixture` (consumido por `init()` provider.js:88)
    - `/states/` → `{ results: [{ id: 'd1e2f3a4-b5c6-7890-1234-567890abcdef', name: 'In Progress' }, { id: 'state-rev', name: 'In review' }, { id: 'state-done', name: 'Done' }] }` (UUID matching fixture's `state` field — sin esto, `getTask` falla en `stateCache.get(workItem.state)`)
    - `/modules/` → `{ results: [] }` (init() tolera vacío, provider.js:108)
    - work-item lookup: `getWorkItemBySequence` invoca `client.request(`/workspaces/{slug}/projects/{id}/issues/?sequence_id={N}`)`. El stub debe matchear `/issues/` o `/projects/.../issues/` con `{ results: [planeWorkitemFixture] }`. Confirmar el path exacto leyendo `src/providers/plane/client.js` durante el RED step si el primer intento falla con `plane stub miss`.

    Naming convention para messages: TODOS los `assert.*` messages prefijan `[${providerName}]` (Pattern shared "Method-error grep-friendly messages"). Cuando un test falle, `node:test` muestra el message — el prefix hace el failure localizable sin abrir el código.

    Comentario top del archivo (literal, ya descrito en Pattern A) cita el invariante estructural Pitfall #3: "TODOS los `it(...)` viven DENTRO del `for-of` loop. Plan-checker valida con grep. Test count = PROVIDERS.length × N_asserts (derivado por construcción)."
  </behavior>

  <action>
    1. Crear `test/providers/contract.test.js` siguiendo la estructura descrita en `<behavior>` paso 1-11. Header docblock cita TEST-03 + Phase 27 + Pitfall #3 invariante estructural literal.

    2. Implementar el live-fetch leak guard FILE-LEVEL como primera cosa después de imports (Pattern B literal de `test/providers/github/provider.test.js:40-49`). Capturar `_originalFetch = globalThis.fetch` como `const` top-level ANTES del `before()`. El thrower message: `live fetch leak: contract matrix must stub or inject`.

    3. Declarar `PROVIDERS = Object.freeze(['plane', 'github'])` y `CANONICAL_TASK_ITEM_KEYS = Object.freeze(['id','ref','title','description','labels','projectId','projectName','groups','url','priority','state'])` (los 11 fields del typedef, incluyendo `state` para diff exact-match — si Plane normalizer NO emite `state`, el assert falla y se debe ajustar el helper para filtrarlo). Anchor en `src/interface.js:11-24`.

    4. Implementar `assertTaskItemShape(task, providerName)`:
       - `assert.equal(typeof task, 'object')` + `assert.notEqual(task, null)`
       - `const keys = Object.keys(task).sort()`
       - `const expected = [...CANONICAL_TASK_ITEM_KEYS].sort()` — pero `state` es opcional (`state?` en typedef): hacer `assert.ok(keys.every(k => CANONICAL_TASK_ITEM_KEYS.includes(k)))` (subset check, NO equality) + `assert.ok(CANONICAL_TASK_ITEM_KEYS.filter(k => k !== 'state').every(k => keys.includes(k)))` (todos los required presentes).
       - Type asserts por campo: `id`/`ref`/`title`/`description`/`projectId`/`projectName`/`url` son `string`; `labels`/`groups` son arrays; `priority` es `null` o uno de `['urgent','high','medium','low','none']`.
       - Cada message prefija `[${providerName}]`.

    5. Implementar `makeFakeGitHubClient(overrides)` lift textual de `test/providers/github/provider.test.js:61-105`. Default `getIssue` retorna `issueFixture` (override en `instantiateProvider`); default `listIssues` retorna `{ status: 200, items: [issueFixture], etag: undefined, rate_limit_remaining: 5000 }` para que `listPendingTasks` tenga al menos 1 item, ayudando si más tarde se añade un assert sobre shape de items.

    6. Implementar `stubPlaneFetch(routes)` — variante de `test/plane-provider.test.js:62-77` con diferencia clave: cuando ningún `suffix` matchea el `path`, **throw** en lugar de devolver `{ results: [] }` default (Pitfall #7 fail-loud). Retorna `{ calls, restore }`. NO restaura el `globalThis.fetch` original dentro — eso lo hace `cleanup()` del `instantiateProvider`.

    7. Implementar `instantiateProvider(name)`:
       - **plane:** llamar `stubPlaneFetch` con las 5 rutas listadas en `<behavior>` paso "Stubs Plane necesarios". Llamar `createPlaneProvider({ baseUrl: 'https://plane.test', apiKey: 'test-key', workspaceSlug: 'test', projects: [{ id: 'p0p0p0p0-1111-2222-3333-444444444444', identifier: 'KL', name: 'Kodo Lab' }], states: { trigger: 'In Progress', review: 'In review', done: 'Done' }, webhookSecret: 'test-secret' })`. Retornar `{ provider, cleanup: stub.restore }`.
       - **github:** llamar `createGitHubProvider({ base_url: 'https://api.github.com', api_key_env: 'GITHUB_TOKEN', repos: [{ owner: 'octocat', repo: 'hello-world' }], states: { trigger: 'open', review: 'closed', done: 'closed' } }, { client: makeFakeGitHubClient({ getIssue: () => issueFixture }) })`. Retornar `{ provider, cleanup: () => {} }`.

    8. Implementar `getValidRef(name)`/`getInvalidRef(name)` — lookup tables triviales con throw default para "unknown provider".

    9. Loop estructural `for (const providerName of PROVIDERS) describe(...)`:
       - `beforeEach(async () => { ({ provider, cleanup } = await instantiateProvider(providerName)); await provider.init(); })` — `init()` es Pitfall #1 mitigation; siempre llamado, Plane lo necesita, GitHub es no-op.
       - `afterEach(() => { cleanup?.(); })` — restaura `globalThis.fetch` después de cada test Plane.
       - 7 `it(...)` declarados según los behaviors B1-B7. NO hay `it()` fuera de este loop.

    10. RED step: ejecutar `node --test test/providers/contract.test.js` y verificar que **14 tests pass** (7 × 2). Si alguno falla por mocks mal calibrados (e.g., `plane stub miss: /workspaces/...`), leer `src/providers/plane/client.js` para descubrir el path exacto del endpoint y añadir la ruta al stub. Iterar hasta verde. Karpathy Rule 4 ejecución dirigida por objetivo: el éxito es 14 pass, no un set específico de stubs.

    11. GREEN verification adicional: ejecutar `npm test` full y confirmar **delta ≥ 14 pass** sobre baseline 763 (target: 777 pass, 0 fail, 1 skipped). Si Plane describe contamina otro test del suite via `globalThis.fetch` no restaurado, leer `node --test` output en busca de errores `plane stub miss` lanzados desde tests que NO son contract.test.js — eso indica leak. Mitigation: revisar que `cleanup()` se invoca en `afterEach` para AMBOS providers (no solo Plane).

    12. NO crear `test/providers/contract-helpers.js` en este task. RESEARCH (Open Question 5 + line count target ~250-300) sugiere todo inline. Si en GREEN se observa LOC > 300, abrir un follow-up task de refactor (NO bloqueante para TEST-03 closure).

    13. NO modificar `test/plane-provider.test.js` ni `test/providers/github/provider.test.js` (Karpathy Rule 3 cambios quirúrgicos; RESEARCH Open Question 5 RESOLVED — no reorganizar).

    14. NO añadir `it.skip` ni `test.todo` — toda assertion ejecuta. Si una sub-assertion no se puede satisfacer dentro de un provider sin live API, eso es indicador de mock incompleto, no de "test no aplicable" — fix el mock.
  </action>

  <verify>
    <automated>node --test test/providers/contract.test.js 2>&1 | grep -E "^# (tests|pass|fail|skipped)"</automated>
  </verify>

  <acceptance_criteria>
    1. **Archivo existe + estructura mínima:** `test -f test/providers/contract.test.js && wc -l test/providers/contract.test.js | awk '{print ($1 >= 200)}'` retorna `1`.

    2. **Matrix loop estructural:** `grep -cE "for \(const providerName of PROVIDERS\)" test/providers/contract.test.js` retorna `>= 1`. Y `grep -cE "^  it\(" test/providers/contract.test.js` (top-level `it()` con 2-space indent fuera de un `describe`) retorna `0` — TODOS los `it()` viven dentro del loop.

    3. **Test count derivado:** `node --test test/providers/contract.test.js 2>&1 | grep -E "^# pass" | awk '{print ($2 == 14)}'` retorna `1`. Si la cuenta es ≠14, el matrix tiene asimetría (algún `it` solo en un provider) o el loop no itera 2 veces.

    4. **Zero hardcoded counts:** `grep -cE "providers.length \* [0-9]+|assert.*=== 14|tests.* 14" test/providers/contract.test.js` retorna `0` (NO se asserta el count en runtime — Pitfall #3).

    5. **Live-fetch leak guard presente:** `grep -cE "live fetch leak" test/providers/contract.test.js` retorna `>= 1`, y `grep -cE "globalThis\.fetch = " test/providers/contract.test.js` retorna `>= 2` (uno para el guard `before()`, uno para el stub Plane interno).

    6. **CANONICAL_TASK_ITEM_KEYS contiene los 11 fields:** `grep -A 13 "CANONICAL_TASK_ITEM_KEYS = Object.freeze" test/providers/contract.test.js | grep -E "'id'|'ref'|'title'|'description'|'labels'|'projectId'|'projectName'|'groups'|'url'|'priority'|'state'" | wc -l` retorna `11`.

    7. **Import attributes correctos (Pattern shared):** `grep -cE "from.*\.json' with \{ type: 'json' \}" test/providers/contract.test.js` retorna `>= 3` (al menos issue.json + plane-workitem.json + plane-labels.json).

    8. **Imports canonical de interface.js:** `grep -cE "import.*TASK_PROVIDER_METHODS.*from '\.\./\.\./src/interface\.js'" test/providers/contract.test.js` retorna `1`. NO se redeclara la lista de 9 métodos.

    9. **Suite global sin regresiones (D-19 cross-phase invariant):** `npm test 2>&1 | grep -E "^# (tests|pass|fail|skipped)"` retorna `tests >= 777`, `pass >= 777`, `fail 0`, `skipped 1`. Baseline post-Phase-26: 763 pass. Delta esperado: +14.

    10. **Zero live API calls (file-level guard verifies):** `node --test test/providers/contract.test.js 2>&1 | grep -cE "live fetch leak|ENOTFOUND|ECONNREFUSED|api\.github\.com|plane\.app"` retorna `0` (cero fugas, cero hits a paths reales).

    11. **Provider-prefixed error messages (grep-friendly Pattern):** `grep -cE "\\[\\\$\\{providerName\\}\\]" test/providers/contract.test.js` retorna `>= 7` (al menos un message por `it()`, idealmente más).

    12. **No tocados (Karpathy Rule 3):** `git diff --name-only HEAD | grep -E "^src/|test/plane-provider\.test\.js|test/providers/github/provider\.test\.js|test/fixtures/"` retorna VACÍO. Solo `test/providers/contract.test.js` aparece en el diff.

    13. **Suite contract aislada corre en <2s:** `time node --test test/providers/contract.test.js 2>&1 | tail -3 | grep -E "real" | awk '{print ($2 < "0m2.000s")}'` retorna `1` (target validation strategy "feedback latency <1s targeted").
  </acceptance_criteria>

  <done>
    14 nuevos tests pass (7 asserts × 2 providers); suite global ≥ 777 pass / 0 fail / 1 skipped; archivo `test/providers/contract.test.js` ~250-300 LOC con matrix loop estructural; cero touch a `src/**` y a tests existentes; cero live API calls; CANONICAL_TASK_ITEM_KEYS y TASK_PROVIDER_METHODS importados desde `src/interface.js` (single source of truth); el plan-checker grep encuentra el `for-of` loop y zero `it()` top-level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → red real | El test corre dentro del proceso `node --test`; el único riesgo de fuga es `globalThis.fetch` invocando `api.github.com` / `plane.app` reales si un stub está incompleto. |
| Test runtime → otros tests del suite | `node --test` ejecuta archivos en el mismo proceso JS (default sequential, mismo `globalThis`). Mutar `globalThis.fetch` sin restaurar contamina tests posteriores. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | `test/providers/contract.test.js` — live fetch leak hacia `api.github.com` o Plane real | **mitigate** | File-level `before()` reemplaza `globalThis.fetch` por thrower (`throw new Error('live fetch leak: ...')`). Cualquier path no-stubbeado revienta loud antes de hacer DNS. Pattern B literal de Phase 24 D-37 (provider.test.js:40-49). Acceptance criterion #5 valida la presencia del guard via grep; criterion #10 valida cero mensajes de live fetch en runtime. |
| T-27-02 | Tampering | `globalThis.fetch` mutation cross-contamina otros archivos del suite (`test/plane-provider.test.js`, `test/triggers/polling.test.js`, etc.) | **mitigate** | `instantiateProvider('plane')` retorna `cleanup = stub.restore`; `afterEach(() => cleanup?.())` invocado para CADA test (no solo el último). Adicional: el file-level `after()` restaura `_originalFetch` capturado al inicio del archivo — doble red de seguridad. Pitfall #2 cubierto. |
| T-27-03 | Denial of Service | Test wall-time excesivo por timeout en stubs faltantes (network retry, timeouts default node http) | **accept** | El leak guard hace `throw` síncrono — sin retry, sin timeout, fail-loud en <1ms. Sin live fetch path, no hay riesgo de DoS por timeouts. Acceptance criterion #13 valida wall-time <2s. |
| T-27-04 | Repudiation | Test count asimétrico oculto — un `it()` solo en un provider pasa silenciosamente | **mitigate** | Acceptance criterion #2 grep `^  it\(` count = 0 fuera del loop. Acceptance criterion #3 pass count = 14 exacto (7×2). Invariante estructural por construcción — RESEARCH Pitfall #3, Open Question 7 RESOLVED. |
| T-27-05 | Spoofing | Fixture cross-provider compartido oculta divergencia legítima de payloads | **accept** | RESEARCH Open Question 3 RESOLVED: per-provider fixtures (Plane usa `plane-workitem.json`, GitHub usa `issue.json`). NO crear fixtures shared. El matrix valida convergencia del OUTPUT (TaskItem), no uniformidad del INPUT. |
| T-27-06 | Elevation of Privilege | `test/providers/contract.test.js` ejecuta código de producción que podría tener side-effects (file writes, env mutations) | **accept** | `createPlaneProvider` y `createGitHubProvider` son factories puras — no escriben archivos, no mutan env. El único side-effect intencional es `globalThis.fetch` mutation, cubierto por T-27-02. `provider.init()` es read-only (HTTP GETs). |

## Critical Invariant (test-only phase)

**"Cero live API calls" es EL invariante de Phase 27.** Toda la threat model apunta a este norte. El file-level leak guard + per-test cleanup garantizan estructuralmente que ningún provider toca red real durante el test run. Acceptance criterion #10 verifica empíricamente.
</threat_model>

<verification>
**Phase-level acceptance gate** (después de Task 1 completo):

1. **Suite delta:** `npm test 2>&1 | grep -E "^# pass"` retorna pass count ≥ 777 (baseline 763 + 14). Si pass count = baseline + ≠14, el matrix tiene asimetría (revisar criterion #3).

2. **TEST-03 closure:** `cat .planning/REQUIREMENTS.md | grep "TEST-03"` muestra el bullet aún pending — el verification step se completa con `/gsd-verify-work 27` que confirma observable + actualiza traceability tabla.

3. **Invariante v0.2 empíricamente demostrado:** los 14 nuevos tests pasan, lo que significa que tanto `createPlaneProvider` como `createGitHubProvider` satisfacen el contract `TaskProvider` con el mismo set de 7 asserts. Cualquier futura regresión que rompa simetría (e.g., GitHub normalizer emite `description: null` en lugar de `''`) hará fallar este suite.

4. **Cero regresión cross-suite:** `npm test 2>&1 | grep -E "^# fail"` retorna 0. Confirma que el `globalThis.fetch` mutation no contaminó tests posteriores (T-27-02 mitigation efectiva).

5. **File ownership clean (parallel-safe):** `git diff --name-only` muestra ÚNICAMENTE `test/providers/contract.test.js` modificado/creado. Cero diff en `src/**` (Karpathy Rule 3).
</verification>

<success_criteria>
**ROADMAP Phase 27 Success Criteria (verbatim):**

- [x] **SC#1:** `test/providers/contract.test.js` itera sobre `['plane', 'github']` ejecutando la misma batería de asserts contra cada `getProvider(name)` instance — mismas signatures, mismos error shapes, mismos campos en `TaskItem` devuelto.
  - **Implementation:** Task 1 paso 9 — `for (const providerName of PROVIDERS) describe(...)` con 7 `it()` declarativos compartidos.
  - **Note:** Se invoca el FACTORY directo (`createPlaneProvider`, `createGitHubProvider`) en lugar de `getProvider(name)` del registry — RESEARCH Open Question 6 RESOLVED: `getProvider()` requiere `loadConfig()` real que crashea sin config v0.7 completo. La distinción no afecta SC#1 (el contract es del adapter, no del wiring del registry).

- [x] **SC#2:** El test usa fixtures offline para ambos providers (zero live API calls) y falla loud si cualquier provider devuelve un shape inconsistente.
  - **Implementation:** Task 1 paso 2 (file-level leak guard) + paso 6 (stubPlaneFetch throw-on-miss) + paso 7 (fakeClient injection para GitHub). Fixtures: `plane-workitem.json`, `plane-labels.json`, `issue.json`. `assertTaskItemShape` falla loud con prefix `[${providerName}]`.

- [x] **SC#3:** Suite global v0.7 termina en ≥ 614 + N tests pass (baseline v0.6) sin regresiones; el matrix añade un test count derivado (`providers.length × asserts`); zero skip nuevos.
  - **Implementation:** Acceptance criterion #9 valida pass ≥ 777 (baseline 763 + 14, supera 614+N por amplio margen). Criterion #2 + #4 garantizan derivación estructural. Cero `it.skip`/`test.todo`.

**Plan-level done:** Task 1 acceptance criteria 1-13 todos green; commits firmados; ready for `/gsd-verify-work 27`.
</success_criteria>

<output>
After completion:

1. Crear `.planning/phases/27-cross-provider-contract-matrix/27-01-SUMMARY.md` siguiendo el template summary del ejecutor — debe incluir:
   - Frontmatter con `phase: 27-cross-provider-contract-matrix`, `plan: 01`, `requirements-completed: [TEST-03]`, `key-files.created: [test/providers/contract.test.js]`, `key-files.modified: []`.
   - **Confirmation Checks** sección citando:
     - Test count derivado: `node --test test/providers/contract.test.js` → 14 pass exactos.
     - Live-fetch leak guard verbatim (líneas exactas del `before()`/`after()`).
     - Suite delta: baseline 763 → post-plan ≥777, 0 fail, 1 skipped.
     - Grep verification del invariante estructural (zero `it()` top-level, 1+ `for-of` loop).
   - **Threat Mitigations Verified** tabla referenciando T-27-01..06.
   - **Deviations from Plan** sección — si hubo auto-fixes (e.g., añadir una ruta stub adicional que el plan no anticipó por desconocer el path exacto de `getWorkItemBySequence`), documentarlos como Rule 2 con `Found during:`, `Issue:`, `Fix:`, `Files modified:`, `Verification:`, `Committed in:`.

2. Update `.planning/ROADMAP.md` Phase 27 plans table: marcar `[x] 27-01-PLAN.md — Matrix runner + 7 asserts core` y actualizar **Plans Complete** count `1/1`. Status → `Complete`. Completed → `2026-05-14`.

3. Commit (Karpathy Rule 4 — pre-verify, post-task atómico):
   - Task 1 commit: `feat(27-01): cross-provider contract matrix (TEST-03)` con cuerpo citando 14 nuevos tests pass + invariante v0.2 demostrado.
</output>
