---
phase: 17-phase-7-uat-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/session-start-event.test.js
autonomous: true
requirements: [UAT-02]
tags: [uat, integration-test, session-start, hook, fail-loud]

must_haves:
  truths:
    - "Spawn de `node src/hooks/session-start.js` con HOME aislado, stdin JSON `{session_id, transcript_path}` y `addSession()` pre-poblada produce un archivo `<tmpHome>/.kodo/logs/<session-id>.ndjson` cuya primera línea es un `session.start` con los 6 campos canónicos D-10"
    - "Los asserts comparan contra el contrato del helper `EVENTS.SESSION_START` y la signature de `sessionStart()` (D-09): cambiar el contrato del helper rompe el test (objetivo SC#2)"
    - "Fail-loud cubre el outer try/catch silent del hook (D-10): si NDJSON file ausente, primera línea no parsea, o event !== EVENTS.SESSION_START, el test falla con mensaje específico"
    - "Sesión sintética no-GSD (`session.gsd = false`, D-11) aísla UAT-02 del builder GSD/quick context — session.start es invariante al modo según líneas 184-186 del hook"
    - "tmpHome creado con mkdtempSync 'kodo-uat-session-start-' y limpiado en after() (D-02 isolation)"
  artifacts:
    - path: "test/session-start-event.test.js"
      provides: "Integration test UAT-02: spawn hook + addSession setup + assert 6 keys canónicas + fail-loud"
      contains: "EVENTS.SESSION_START"
      contains_alt: "spawn(process.execPath"
      min_lines: 100
  key_links:
    - from: "test/session-start-event.test.js"
      to: "src/hooks/session-start.js"
      via: "spawn(process.execPath, [hookPath], { env: { ...process.env, HOME: tmpHome }, stdio: ['pipe', 'pipe', 'pipe'] })"
      pattern: "spawn.*session-start\\.js"
    - from: "test/session-start-event.test.js"
      to: "src/logger-events.js (EVENTS.SESSION_START)"
      via: "import estático para asserts contra contrato"
      pattern: "EVENTS\\.SESSION_START"
    - from: "test/session-start-event.test.js"
      to: "src/session/state.js (addSession)"
      via: "dynamic import DESPUÉS de fijar HOME en process.env (CR-02 pattern)"
      pattern: "await import\\('\\.\\./src/session/state\\.js'\\)"
---

<objective>
Convertir el UAT-02 humano de Phase 7 (`07-HUMAN-UAT.md` test #2) en integration test que dispara una emisión real de `session.start` desde el hook `src/hooks/session-start.js` con stdin canónico, y assertea contra el CONTRATO del helper `EVENTS.SESSION_START` + `sessionStart()` — no contra un fixture estático. Cambiar el contrato del helper rompe el test (objetivo D-09 + SC#2).

Purpose: Cierra SC#2 de Phase 17. Sustituye el TODO humano de "session.start con campos D-10 reales" con cobertura programática que (a) ejerce el path real del hook (no import directo de helpers), (b) compensa el silent-on-failure outer try/catch del hook con fail-loud externo (D-10), (c) usa state.json sintético para reproducir Plane → Claude Code start sin Plane vivo.

Output: `test/session-start-event.test.js` con 1 test `it()` que cubre el path full no-GSD del hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-phase-7-uat-automation/17-CONTEXT.md

<!-- Source of truth: contrato del evento + path del hook -->
@src/logger-events.js
@src/hooks/session-start.js

<!-- State persistence: addSession lee/escribe state.json relativo a HOME -->
@src/session/state.js

<!-- Patrones canónicos a replicar -->
@test/stop-state-transition.test.js

<interfaces>
<!-- Contratos relevantes. El executor NO debe explorar el codebase para confirmarlos. -->

From src/logger-events.js:
```js
export const EVENTS = Object.freeze({
  SESSION_START: 'session.start',
  // ... otros 7 eventos
});

// Contrato D-10: 6 campos canónicos requeridos en NDJSON record.
export function sessionStart(logger, fields: {
  session_id: string,
  task_id: string | null,
  provider: string,
  project_path: string,
  transcript_path?: string,    // auto-resuelto si ausente
  started_at: string,
}): void
```

From src/hooks/session-start.js (líneas 188-208 — el path que UAT-02 ejerce):
```js
// Emite session.start con los 6 campos D-10. El outer try/catch líneas 223-225 traga
// errores silenciosamente. UAT-02 fail-loud externo compensa (D-10).
sessionStart(log, {
  session_id: session.session_id,
  task_id: session.task_id,
  provider: session.provider,
  project_path: session.project_path,
  transcript_path: input.transcript_path,
  started_at: new Date().toISOString(),
});
```

From src/hooks/session-start.js (líneas 169-225 — main()):
```js
// Lee stdin JSON con timeout 3s. Necesita session_id + transcript_path.
// Llama findSession({sessionId, cwd}) que delega a state.js.
// Si la sesión NO está en state.json → exit 0 SIN emitir session.start.
```

From src/session/state.js:
```js
export const KODO_DIR = join(homedir(), '.kodo');  // computado al import-time
export function addSession(taskId, session): void;  // escribe a ~/.kodo/state.json
export function loadState(): { sessions: { [taskId: string]: Session } };
// findSession({sessionId, cwd}) → busca en state.json
```

D-XX decisions del CONTEXT.md aplicables a este plan:
- D-01: spawn `src/hooks/session-start.js` directamente (NO via bin/kodo) — ese es el entry point que Claude Code invoca en producción.
- D-02: HOME override via env del child + mkdtempSync 'kodo-uat-session-start-' + cleanup en after(). Import dinámico de `state.js` DESPUÉS de fijar HOME (CR-02 pattern, replica de stop-state-transition.test.js).
- D-08: state.json sintético + stdin con session_id. Pre-poblar via `addSession(taskId, session)` desde el test runner (importado dinámicamente). Spawn child con `stdin: 'pipe'` y escribir JSON `{session_id, transcript_path}`.
- D-09: imports estáticos `EVENTS.SESSION_START` (no depende de HOME). Asserts: `record.event === EVENTS.SESSION_START` + presencia de las 6 keys + tipos correctos.
- D-10: fail-loud externo:
  - `existsSync(ndjsonPath) === false` → `assert.fail('hook did not emit session.start NDJSON file')`.
  - `JSON.parse(firstLine)` throws → `assert.fail('first line malformed: ' + firstLine)`.
  - `record.event !== EVENTS.SESSION_START` → `assert.fail('first event is not session.start: ' + JSON.stringify(record))`.
- D-11: sesión sintética con `gsd: false` (no-GSD).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear test/session-start-event.test.js con setup state.json sintético + spawn hook + fail-loud asserts</name>
  <files>test/session-start-event.test.js</files>
  <read_first>
    - test/session-start-event.test.js (confirmar ausencia)
    - .planning/phases/17-phase-7-uat-automation/17-CONTEXT.md (D-08..D-11 son los binding decisions)
    - test/stop-state-transition.test.js (patrón mkdtempSync + HOME override + dynamic import de state.js dentro de before)
    - src/logger-events.js (EVENTS.SESSION_START + signature de sessionStart)
    - src/hooks/session-start.js líneas 169-225 (main flow + outer try/catch silent)
    - src/session/state.js (addSession signature + KODO_DIR)
  </read_first>
  <action>
Crear `test/session-start-event.test.js` con 1 test `it()` que valida UAT-02 end-to-end. Estructura concreta:

1. Imports estáticos:
   - `describe, it, before, after` from `node:test`.
   - `assert` from `node:assert/strict`.
   - `spawn` from `node:child_process`.
   - `mkdtempSync, rmSync, mkdirSync, existsSync, readFileSync` from `node:fs`.
   - `tmpdir` from `node:os`.
   - `join, resolve, dirname` from `node:path`.
   - `fileURLToPath` from `node:url`.
   - `EVENTS` from `'../src/logger-events.js'` — IMPORT ESTÁTICO (D-09: no depende de HOME, son constantes puras stdlib-only).

2. Constantes: `__dirname = dirname(fileURLToPath(import.meta.url))`; `REPO = resolve(__dirname, '..')`; `HOOK_PATH = join(REPO, 'src', 'hooks', 'session-start.js')`.

3. Variables de scope `tmpHome`, `origHome`, `addSession` (resuelta dinámicamente, mismo patrón CR-02).

4. `describe('UAT-02 SC#2: session.start emite 6 campos canónicos D-10', ...)`:
   - `before` async:
     a. `origHome = process.env.HOME`.
     b. `tmpHome = mkdtempSync(join(tmpdir(), 'kodo-uat-session-start-'))`.
     c. `process.env.HOME = tmpHome` (D-02).
     d. `mkdirSync(join(tmpHome, '.kodo', 'logs'), { recursive: true })` — el hook escribe a `<HOME>/.kodo/logs/<sid>.ndjson`; el dir debe existir o el logger crashea silenciosamente bajo el try/catch del hook.
     e. **Dynamic import de state.js DESPUÉS de fijar HOME** (CR-02 pattern, idéntico a stop-state-transition.test.js:108-114):
        ```js
        const stateMod = await import('../src/session/state.js');
        addSession = stateMod.addSession;
        ```
   - `after`:
     a. Restaurar HOME: si `origHome === undefined` → `delete process.env.HOME`; else `process.env.HOME = origHome`.
     b. `rmSync(tmpHome, { recursive: true, force: true })`.

5. El test `it('emits session.start as first NDJSON line with 6 canonical D-10 keys (D-08..D-11, fail-loud per D-10)', async () => { ... })`:

   a. **Sesión sintética no-GSD (D-11)**:
      ```js
      const session = {
        session_id: 'uat02-' + process.pid + '-' + Date.now(),
        task_id: 'kodo-uat02-task-' + process.pid,
        task_ref: 'KL-uat02',
        gsd: false,                              // D-11: aísla del builder GSD
        status: 'running',
        provider: 'plane',
        project_id: 'p-uat02',
        project_path: tmpHome,                   // cwd del child es tmpHome → findSession matchea
        workspace_ref: 'workspace:uat02',
        started_at: new Date().toISOString(),
        summary: 'UAT-02 integration session.start',
      };
      addSession(session.task_id, session);      // D-08: state.json pre-poblado
      ```
      (Limpieza después del test: el `rmSync(tmpHome)` en `after()` borra state.json junto con todo. NO se necesita removeSession explícito aquí — distinto a stop-state-transition.test.js porque allí HOME se compartía entre tests del mismo describe.)

   b. **Spawn del hook (D-01)**:
      ```js
      const child = spawn(
        process.execPath,
        [HOOK_PATH],
        {
          cwd: tmpHome,                          // input.cwd default = process.cwd() del child
          env: { ...process.env, HOME: tmpHome },
          stdio: ['pipe', 'pipe', 'pipe'],
        }
      );
      ```

   c. **Stdin JSON (D-08)** — escribir y cerrar para que `readStdin()` resuelva:
      ```js
      const stdinPayload = JSON.stringify({
        session_id: session.session_id,
        transcript_path: '/tmp/fake-transcript-' + session.session_id + '.jsonl',
        cwd: tmpHome,
      });
      child.stdin.write(stdinPayload);
      child.stdin.end();
      ```

   d. **Esperar exit del hook**:
      ```js
      let stdout = '', stderr = '';
      child.stdout.on('data', (b) => { stdout += b.toString('utf8'); });
      child.stderr.on('data', (b) => { stderr += b.toString('utf8'); });
      const exitInfo = await new Promise((resolveP, rejectP) => {
        const t = setTimeout(() => rejectP(new Error('hook timeout 5s. stderr=' + stderr)), 5000);
        child.on('exit', (code, signal) => { clearTimeout(t); resolveP({ code, signal }); });
      });
      assert.equal(exitInfo.code, 0, 'hook should exit 0 (silent on failure pattern). stderr=' + stderr);
      ```

   e. **Fail-loud asserts (D-10)** — leer NDJSON post-exit:
      ```js
      const ndjsonPath = join(tmpHome, '.kodo', 'logs', session.session_id + '.ndjson');
      if (!existsSync(ndjsonPath)) {
        assert.fail('D-10 fail-loud: hook did not emit session.start NDJSON file at ' + ndjsonPath + '. The outer try/catch (src/hooks/session-start.js:223-225) likely swallowed an error. stderr=' + stderr + ' stdout=' + stdout);
      }
      const raw = readFileSync(ndjsonPath, 'utf-8').split('\n').filter(Boolean);
      assert.ok(raw.length >= 1, 'D-10 fail-loud: NDJSON file empty');
      let record;
      try {
        record = JSON.parse(raw[0]);
      } catch (e) {
        assert.fail('D-10 fail-loud: first line malformed: ' + raw[0] + ' err=' + e.message);
      }
      ```

   f. **Asserts contra contrato del helper (D-09)** — 6 keys canónicas + tipos:
      ```js
      assert.equal(record.event, EVENTS.SESSION_START, 'D-09: first event must equal EVENTS.SESSION_START');
      // 6 campos canónicos D-10:
      assert.equal(record.session_id, session.session_id, 'session_id matches input');
      assert.equal(record.task_id, session.task_id, 'task_id from session');
      assert.equal(record.provider, session.provider, 'provider from session');
      assert.equal(record.project_path, session.project_path, 'project_path from session');
      assert.equal(record.transcript_path, '/tmp/fake-transcript-' + session.session_id + '.jsonl', 'transcript_path from stdin');
      assert.equal(typeof record.started_at, 'string', 'started_at is ISO string');
      assert.match(record.started_at, /^\d{4}-\d{2}-\d{2}T/, 'started_at ISO-8601 format');
      // Sanity: las 6 keys exactas (D-09) — ningún campo D-10 ausente.
      const requiredKeys = ['session_id', 'task_id', 'provider', 'project_path', 'transcript_path', 'started_at'];
      for (const k of requiredKeys) {
        assert.ok(k in record, 'D-09: required key "' + k + '" missing from session.start record');
      }
      ```

6. NO assertar contenido de stderr (CONTEXT discretion: stderr es prose, no contrato). NO usar `--json` (no aplica al hook). NO mockear `findSession` ni `createLogger` — se ejerce el path real (D-01).

7. NO crear helper compartido `test/_helpers.js` (D-02 + Discretion: no proactivo). El helper de exit-await va inline.

8. Idioma del test: comentarios y mensajes en inglés/español según costumbre del repo (`stop-state-transition.test.js` mezcla); preferir inglés en assertion messages para grep-ability futura.
  </action>
  <verify>
    <automated>node --test test/session-start-event.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/session-start-event.test.js` exists.
    - File contains `EVENTS.SESSION_START` (D-09: assert contra contrato del helper, no contra fixture estático).
    - File contains `import.*EVENTS.*from.*'\.\./src/logger-events\.js'` (import estático del contrato).
    - File contains `spawn(process.execPath` (D-01: subprocess real).
    - File contains `'src'.*'hooks'.*'session-start.js'` OR `HOOK_PATH` (target del spawn).
    - File contains `mkdtempSync` AND `'kodo-uat-session-start-'` (D-02).
    - File contains `HOME: tmpHome` (D-02 child env override).
    - File contains `await import('../src/session/state.js')` (D-02 + CR-02: dynamic import POST-HOME).
    - File contains `addSession(` (D-08: state.json sintético).
    - File contains `child.stdin.write(` AND `child.stdin.end()` (D-08: stdin JSON).
    - File contains `gsd: false` (D-11: sesión no-GSD).
    - File contains `assert.fail(` AT LEAST 2 times (D-10 fail-loud para file ausente + JSON malformed).
    - File contains `'session_id'` AND `'task_id'` AND `'provider'` AND `'project_path'` AND `'transcript_path'` AND `'started_at'` (D-10: 6 keys canónicas).
    - File contains `requiredKeys` OR equivalent loop iterando las 6 keys.
    - File does NOT contain `mock` OR `stub` for findSession/createLogger (D-01: path real, no mocks).
    - File does NOT contain `import.*sessionStart.*from` (D-01: NO import directo del helper bajo test).
    - `node --test test/session-start-event.test.js` exits 0.
    - `node --test` (full suite) exits 0 — sin regresiones.
    - Test es deterministic en run repetido: `for i in 1 2 3; do node --test test/session-start-event.test.js || exit 1; done` exits 0.
  </acceptance_criteria>
  <done>UAT-02 automatizado: 1 test it() que valida los 6 campos D-10 en el primer NDJSON record, asserteando contra `EVENTS.SESSION_START` (contrato del helper). Fail-loud cubre el silent try/catch del hook. Suite global verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner ↔ hook child | Spawned hook puede crashear silenciosamente (outer try/catch líneas 223-225); fail-loud externo (D-10) compensa. |
| test runner ↔ state.js (HOME) | `addSession` escribe a `~/.kodo/state.json` resuelto al import-time desde `homedir()`; sin override correcto de HOME, contamina state.json del usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information Disclosure | HOME override timing | mitigate | `process.env.HOME = tmpHome` se fija ANTES del dynamic import de `state.js` (CR-02 pattern, replicado de stop-state-transition.test.js:100-114). El módulo cachea `KODO_DIR = join(homedir(), '.kodo')` al evaluarse — fijar HOME después del import expondría `~/.kodo` real. |
| T-17-02-02 | Tampering | state.json contamination | mitigate | `addSession` escribe a `tmpHome/.kodo/state.json` — `rmSync(tmpHome, recursive: true, force: true)` en `after()` lo limpia. Si `origHome` es `undefined` (test runner sin HOME), `delete process.env.HOME` restaura el estado exacto previo. |
| T-17-02-03 | Repudiation | silent hook failure | mitigate | El hook tiene outer try/catch que traga errores. **D-10 fail-loud externo** convierte cada modo de falla en `assert.fail` con mensaje específico (file ausente, JSON malformed, event mismatch). Imposibilita merge de regresión que silencie session.start. |
| T-17-02-04 | Denial of Service | hook stdin timeout | mitigate | El hook tiene `STDIN_TIMEOUT = 3000ms`. El test cierra stdin con `child.stdin.end()` inmediatamente tras write, así readStdin resuelve en <50ms. Timeout externo del test (5000ms) cubre worst case. |
| T-17-02-05 | Spoofing | sentinel session_id collision | accept | `session_id = 'uat02-' + process.pid + '-' + Date.now()` es único por test run; colisión con sesiones reales del dev imposible bajo HOME aislado. |
| T-17-02-06 | Information Disclosure | fixture path leak | accept | `transcript_path: '/tmp/fake-transcript-...'` es un path no-existente intencional (el hook no toca el archivo, solo lo persiste en NDJSON). No filtra info sensible. |

Severity gate: 0 high. T-17-02-03 es load-bearing — el plan REQUIERE D-10 fail-loud asserts (verificado en acceptance_criteria). Plan procede.
</threat_model>

<verification>
- `node --test test/session-start-event.test.js` exit 0.
- Full suite: `node --test` exit 0 — sin regresiones.
- Grep checks de acceptance_criteria todos green.
- 3 ejecuciones consecutivas verdes.
</verification>

<success_criteria>
- 1 archivo nuevo: `test/session-start-event.test.js`.
- 1 test `it()` cubre SC#2 de Phase 17 (6 keys D-10 + assert contra contrato del helper).
- Sin regresiones en suite existente.
- Sin nuevas dependencias externas.
- Fail-loud cubre el outer try/catch del hook (D-10 invariante).
</success_criteria>

<output>
After completion, create `.planning/phases/17-phase-7-uat-automation/17-02-SUMMARY.md` con:
- file created (path)
- D-decisions covered (D-08..D-11 + D-01/D-02 transversales)
- shape del NDJSON record observado (las 6 keys verificadas)
- fail-loud paths exercised vs not (cuál triggerear en regresión)
- regresiones detectadas (debe ser ninguna)
</output>
