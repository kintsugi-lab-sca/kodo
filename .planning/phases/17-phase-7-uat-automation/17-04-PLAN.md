---
phase: 17-phase-7-uat-automation
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - .planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [UAT-01, UAT-02, UAT-03]
tags: [doc, redirect, milestones, cleanup, sc4]

must_haves:
  truths:
    - "`07-HUMAN-UAT.md` reducido a redirect con frontmatter `status: superseded` (D-15) — preserva enlaces inversos y trazabilidad histórica, NO se borra"
    - "El nuevo `07-HUMAN-UAT.md` apunta a los 3 archivos test creados en plans 01/02/03 (logs-follow-integration, session-start-event, session-of-resolver)"
    - "MILESTONES.md v0.3 entry quita la mención de los 3 UATs como deferred (D-16)"
    - "MILESTONES.md v0.3 entry añade referencia a Phase 17 como cierre del UAT debt"
    - "Touch mínimo: solo se modifican 2 archivos (D-16)"
  artifacts:
    - path: ".planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md"
      provides: "Redirect con status: superseded a los 3 integration tests"
      contains: "status: superseded"
      contains_alt: "test/logs-follow-integration.test.js"
      max_lines: 30
    - path: ".planning/MILESTONES.md"
      provides: "v0.3 entry actualizada — UATs movidos de deferred a closed via Phase 17"
      contains: "Phase 17"
  key_links:
    - from: ".planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md"
      to: "test/logs-follow-integration.test.js, test/session-start-event.test.js, test/session-of-resolver.test.js"
      via: "lista de archivos en el redirect"
      pattern: "test/.*\\.test\\.js"
---

<objective>
Cerrar el UAT debt en documentación: reducir `07-HUMAN-UAT.md` a un redirect con frontmatter `status: superseded` (D-15) apuntando a los 3 integration tests creados en plans 01/02/03, y actualizar la entrada `v0.3` de `MILESTONES.md` para mover los 3 UATs de "deferred" a "closed via Phase 17" (D-16).

Purpose: Cierra SC#4 de Phase 17 — "07-HUMAN-UAT.md se elimina o se reduce a una nota apuntando a los integration tests; MILESTONES.md v0.3 deja de listar los UATs como deferred". CONTEXT D-15 escoge "redirect" sobre "elimina" para preservar enlaces inversos y trazabilidad histórica.

Output: 2 archivos modificados in-place. Sin nuevos archivos, sin tocar test/ ni src/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-phase-7-uat-automation/17-CONTEXT.md

<!-- Archivos a modificar — read full state before editing -->
@.planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md
@.planning/MILESTONES.md

<!-- SUMMARYs upstream — confirmar que los 3 tests se crearon antes de cerrar el doc redirect -->
@.planning/phases/17-phase-7-uat-automation/17-01-SUMMARY.md
@.planning/phases/17-phase-7-uat-automation/17-02-SUMMARY.md
@.planning/phases/17-phase-7-uat-automation/17-03-SUMMARY.md

<interfaces>
<!-- Estado actual de los 2 docs (se modifica IN-PLACE, no se reemplaza). -->

Estado actual `07-HUMAN-UAT.md` (líneas 1-37 — full file, ~30 líneas):
```markdown
---
status: partial
phase: 07-kodo-logs-cli-event-taxonomy
source: [07-VERIFICATION.md]
started: 2026-04-16T13:27:00Z
updated: 2026-04-16T13:27:00Z
---

## Current Test
[awaiting human testing]

## Tests
### 1. Live `--follow` tail behaviour ...
### 2. `session.start` real con todos los campos D-10 ...
### 3. `--session-of` end-to-end con sesión real ...

## Summary
total: 3, passed: 0, issues: 0, pending: 3, ...

## Gaps
```

Después del cambio (D-15 redirect):
```markdown
---
status: superseded
superseded_by: phase-17-uat-automation
phase: 07-kodo-logs-cli-event-taxonomy
source: [07-VERIFICATION.md]
started: 2026-04-16T13:27:00Z
updated: <fecha del commit, ISO 8601>
---

# Superseded — UATs automatizados en Phase 17

Los 3 UATs humanos pendientes de Phase 7 se convirtieron en integration tests automatizados durante Phase 17 (v0.5 milestone).

## Reemplazos 1:1

- **UAT #1 (live `--follow` tail)** → `test/logs-follow-integration.test.js`
- **UAT #2 (`session.start` campos D-10)** → `test/session-start-event.test.js`
- **UAT #3 (`--session-of` E2E)** → `test/session-of-resolver.test.js`

Ver `.planning/phases/17-phase-7-uat-automation/` para spec, decisiones (D-01..D-16) y SUMMARYs.

## Estado original

Los 3 UATs originalmente requerían sesión Plane viva + sesión real Claude Code. Phase 17 reemplaza ambas dependencias con fixtures sintéticos (state.json + logs NDJSON pre-poblados) y subprocess spawn de `bin/kodo` / `src/hooks/session-start.js`. Cobertura equivalente sin coste humano recurrente.
```

Estado actual `MILESTONES.md` v0.3 entry, sección "Known deferred items" (línea ~29 según preview):
```markdown
**Known deferred items (accepted as tech debt):**

- Phase 7 `07-HUMAN-UAT.md` — 3 tests manuales pendientes (live --follow, session.start real fields, --session-of E2E)
- INT-MED-01 — `dispatcher.js` usa literales ...
- INT-LOW-01 — `markSessionStatus` exportado pero sin callsites de producción ...
- ...
```

Después del cambio (D-16 touch mínimo):
- Eliminar la línea "Phase 7 `07-HUMAN-UAT.md` — 3 tests manuales pendientes...".
- Añadir, ya sea en otra sección "Closed retroactivamente en v0.5" o como un bullet en "Key accomplishments" del v0.3 entry, una nota: "Phase 7 UATs humanos cerrados retroactivamente en v0.5 Phase 17 — 3 integration tests sustituyen los 3 TODO manuales (test/logs-follow-integration, test/session-start-event, test/session-of-resolver)."
- Si la entrada v0.3 tiene también un bullet "INT-MED-01" que se cerró en Phase 16 (LOG-13 + LOG-14 + LOG-15), opcionalmente eliminar también — pero ESO ESTÁ FUERA DE SCOPE (D-16 touch mínimo, solo UAT). Si el ejecutor detecta que INT-MED-01 / INT-LOW-01 quedaron obsoletos pero su cierre ya fue documentado en otra parte, NO LOS TOCA — ese cleanup pertenece a `/gsd-complete-milestone v0.5`.

D-XX decisions del CONTEXT.md aplicables:
- D-15: 07-HUMAN-UAT.md → redirect, NO delete. Frontmatter status: superseded. Lista de los 3 reemplazos test.
- D-16: MILESTONES.md v0.3 entry → quitar mención UAT-deferred + referencia a Phase 17. Touch mínimo, scope solo a UAT (no tocar otros bullets de deferred).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reducir 07-HUMAN-UAT.md a redirect con status: superseded</name>
  <files>.planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md (estado actual, partial 3-pending tests)
    - .planning/phases/17-phase-7-uat-automation/17-CONTEXT.md (D-15 redirect, no delete)
    - .planning/phases/17-phase-7-uat-automation/17-01-SUMMARY.md (confirmar test/logs-follow-integration.test.js creado)
    - .planning/phases/17-phase-7-uat-automation/17-02-SUMMARY.md (confirmar test/session-start-event.test.js creado)
    - .planning/phases/17-phase-7-uat-automation/17-03-SUMMARY.md (confirmar test/session-of-resolver.test.js creado)
  </read_first>
  <action>
Sobreescribir `07-HUMAN-UAT.md` con el contenido del redirect (D-15). Estructura literal:

```markdown
---
status: superseded
superseded_by: phase-17-uat-automation
phase: 07-kodo-logs-cli-event-taxonomy
source: [07-VERIFICATION.md]
started: 2026-04-16T13:27:00Z
updated: <USAR LA FECHA-HORA ISO 8601 DEL MOMENTO DEL COMMIT, p.ej. 2026-05-10T16:50:00Z>
---

# Superseded — UATs automatizados en Phase 17

Los 3 UATs humanos pendientes de Phase 7 se convirtieron en integration tests automatizados durante Phase 17 (v0.5 milestone).

## Reemplazos 1:1

- **UAT #1 (live `--follow` tail behaviour)** → `test/logs-follow-integration.test.js`
- **UAT #2 (`session.start` real con campos D-10)** → `test/session-start-event.test.js`
- **UAT #3 (`--session-of` end-to-end)** → `test/session-of-resolver.test.js`

Ver `.planning/phases/17-phase-7-uat-automation/` para spec, decisiones (D-01..D-16) y SUMMARYs por plan.

## Estado original

Los 3 UATs originalmente requerían sesión Plane viva + sesión real Claude Code. Phase 17 reemplaza ambas dependencias con fixtures sintéticos (`state.json` + logs NDJSON pre-poblados) y subprocess spawn de `bin/kodo` / `src/hooks/session-start.js`. Cobertura equivalente sin coste humano recurrente.

## Cambio mecánico vs cambio de contrato

- **UAT #1 (`--follow`)**: el integration test escribe 3 batches NDJSON con `setInterval ~250ms` y verifica orden estricto vía `awaitLine` + SIGINT cleanup en <2s. Ejerce el path real de `src/logs/follow.js` (FOLLOW_INTERVAL_MS=200, watcher fs.watchFile, SIGINT handler).
- **UAT #2 (`session.start`)**: el integration test spawna `src/hooks/session-start.js` con stdin JSON canónico, lee `<HOME>/.kodo/logs/<sid>.ndjson` post-exit y assertea contra `EVENTS.SESSION_START` + las 6 keys D-10. Cambiar el contrato del helper rompe el test (objetivo SC#2).
- **UAT #3 (`--session-of`)**: 4 escenarios E2E (step-1 hit / step-2 hit / not-found / state-points-to-missing-log) con exit codes deterministas observados del CLI actual (D-13 — descubrir, no rediseñar).
```

Reglas:
1. Usar la herramienta Write para sobrescribir el archivo (más simple que Edit en este caso porque el reemplazo es completo).
2. La fecha `updated:` debe ser la del momento del cambio (el ejecutor puede usar `date -u +"%Y-%m-%dT%H:%M:%SZ"` para obtener ISO 8601 UTC).
3. NO eliminar el archivo (D-15: redirect, no delete — preserva enlaces inversos).
4. NO modificar otros archivos en `milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/` — solo este.
5. NO tocar `test/` ni `src/` en este task.
  </action>
  <verify>
    <automated>grep -q "status: superseded" .planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md && grep -q "test/logs-follow-integration.test.js" .planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md && grep -q "test/session-start-event.test.js" .planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md && grep -q "test/session-of-resolver.test.js" .planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v0.3-phases/07-kodo-logs-cli-event-taxonomy/07-HUMAN-UAT.md` exists (D-15: not deleted).
    - File contains `status: superseded` in frontmatter.
    - File contains `superseded_by: phase-17-uat-automation` in frontmatter.
    - File contains `test/logs-follow-integration.test.js` (reemplazo UAT #1).
    - File contains `test/session-start-event.test.js` (reemplazo UAT #2).
    - File contains `test/session-of-resolver.test.js` (reemplazo UAT #3).
    - File does NOT contain `status: partial` (regresión guard — frontmatter actualizado).
    - File does NOT contain `result: [pending]` (regresión guard — original 3-test placeholder removido).
    - File line count <= 50 (touch mínimo, redirect doc).
    - File contains `Phase 17` (referencia explícita a la fase que la supersedió).
    - File mentions D-XX (al menos `D-01..D-16` o similar) para trazabilidad.
  </acceptance_criteria>
  <done>07-HUMAN-UAT.md reducido a redirect: frontmatter `status: superseded`, lista de los 3 reemplazos test, referencia a Phase 17. Archivo conservado (no borrado) per D-15.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar MILESTONES.md v0.3 entry — UATs movidos de deferred a closed</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (estado actual completo — leer las 2 entries v0.3 + v0.4 en su totalidad para localizar la línea exacta del UAT-deferred bullet)
    - .planning/phases/17-phase-7-uat-automation/17-CONTEXT.md (D-16 touch mínimo, solo scope UAT)
  </read_first>
  <action>
Modificar `.planning/MILESTONES.md` en 2 lugares dentro de la entrada `## v0.3 GSD Integration + Structured Logging (Shipped: 2026-04-22)`:

1. **Eliminar el bullet UAT-deferred** de la sección "Known deferred items (accepted as tech debt):". Linea exacta a eliminar:
   ```
   - Phase 7 `07-HUMAN-UAT.md` — 3 tests manuales pendientes (live --follow, session.start real fields, --session-of E2E)
   ```
   Usar Edit (no Write) — el resto del archivo se preserva intacto.

2. **Añadir un bullet de cierre retroactivo** en la sección "Key accomplishments" de la entrada v0.3, al FINAL de la lista de bullets existentes (después del bullet "Post-audit cleanup..."), con texto literal:
   ```
   - **UAT debt closure (Phase 17, v0.5 milestone)** — los 3 UATs humanos pendientes de Phase 7 (live `--follow` tail, `session.start` con campos D-10, `--session-of` E2E) se automatizaron en `test/logs-follow-integration.test.js`, `test/session-start-event.test.js`, `test/session-of-resolver.test.js`. Cobertura equivalente sin coste humano recurrente. Ver `.planning/phases/17-phase-7-uat-automation/`.
   ```

Reglas:
1. Usar la herramienta Edit para los 2 cambios (ambos son edits in-place de un archivo grande — Write completo arriesga regresar bullets de otras milestones).
2. **NO TOCAR** ningún otro bullet de la sección "Known deferred items":
   - INT-MED-01 (cerrado en Phase 16 — pertenece a v0.5 closure cleanup, no a este plan).
   - INT-LOW-01 (cerrado en Phase 16 — idem).
   - INT-LOW-02 (sin cambio).
   - Nyquist drafts (sin cambio).
   - GSD-07 (sin cambio).
   D-16 touch mínimo: solo UAT. El cleanup de los otros bullets pertenece a `/gsd-complete-milestone v0.5`.
3. **NO TOCAR** la entrada v0.4 ni v0.2.
4. **NO AÑADIR** una entrada v0.5 — esta fase no cierra el milestone, solo cubre Phase 17. v0.5 entry se añadirá al disparar `/gsd-complete-milestone v0.5` después de que las 4 fases (14, 15, 16, 17) estén todas completas.
  </action>
  <verify>
    <automated>! grep "Phase 7 .07-HUMAN-UAT.md. — 3 tests manuales pendientes" .planning/MILESTONES.md && grep -q "UAT debt closure (Phase 17" .planning/MILESTONES.md && grep -q "test/logs-follow-integration.test.js" .planning/MILESTONES.md && grep -q "test/session-start-event.test.js" .planning/MILESTONES.md && grep -q "test/session-of-resolver.test.js" .planning/MILESTONES.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/MILESTONES.md` does NOT contain `Phase 7 .07-HUMAN-UAT.md. — 3 tests manuales pendientes` (el bullet original eliminado).
    - File contains `UAT debt closure (Phase 17` (nuevo bullet añadido).
    - File contains `test/logs-follow-integration.test.js` AND `test/session-start-event.test.js` AND `test/session-of-resolver.test.js` (los 3 archivos test referenciados en el bullet de cierre).
    - File still contains `INT-MED-01` (NO modificado — touch mínimo D-16).
    - File still contains `INT-LOW-01` (NO modificado).
    - File still contains `## v0.4 GSD Quick Mode` (entrada v0.4 intacta).
    - File still contains `## v0.2 Provider Abstraction` (entrada v0.2 intacta).
    - File does NOT contain `## v0.5 ` heading (NO se añade entrada v0.5 — pertenece a /gsd-complete-milestone).
    - Total line count del archivo cambia en ≤ 4 líneas vs estado pre-cambio (1 eliminada + 1 añadida + posibles ajustes de blank lines).
    - `git diff --stat .planning/MILESTONES.md` muestra solo este archivo modificado.
  </acceptance_criteria>
  <done>MILESTONES.md v0.3 entry: UAT-deferred bullet eliminado de "Known deferred items"; nuevo bullet "UAT debt closure (Phase 17, v0.5 milestone)" añadido en "Key accomplishments" referenciando los 3 archivos test. Otros bullets intactos (touch mínimo D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner ↔ doc files | Edits a .md files solo; sin código ejecutable, sin riesgo de runtime injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering | scope creep en MILESTONES.md | mitigate | D-16 touch mínimo enforced en acceptance_criteria: file aún contiene `INT-MED-01`, `INT-LOW-01`, `## v0.4`, `## v0.2`. Cleanup de otros bullets pertenece a `/gsd-complete-milestone v0.5`. |
| T-17-04-02 | Information Disclosure | broken inverse links | mitigate | D-15 explícito: `07-HUMAN-UAT.md` se conserva con `status: superseded` (no delete). Cualquier link inverso desde otros docs sigue resolviendo. |
| T-17-04-03 | Repudiation | trazabilidad histórica perdida | mitigate | El redirect documenta los 3 reemplazos 1:1 + estado original + razón del cambio. SUMMARYs por plan (17-01/02/03) preservan los detalles técnicos. |

Severity gate: 0 high, 0 medium. Todo mitigado vía acceptance_criteria explícitos. Plan procede.
</threat_model>

<verification>
- Grep checks de acceptance_criteria todos green.
- `git diff` muestra solo 2 archivos modificados (`07-HUMAN-UAT.md` rewritten, `MILESTONES.md` con 1 línea eliminada + 1 añadida).
- Sin tests modificados, sin src/ modificado.
- Suite global sigue verde: `node --test` exit 0.
</verification>

<success_criteria>
- 2 archivos modificados in-place.
- `07-HUMAN-UAT.md` con frontmatter `status: superseded` + lista de los 3 reemplazos test.
- `MILESTONES.md` v0.3 entry sin UAT-deferred bullet + con bullet de cierre retroactivo.
- Otros bullets de deferred y otras entries de milestones INTACTOS (touch mínimo D-16).
</success_criteria>

<output>
After completion, create `.planning/phases/17-phase-7-uat-automation/17-04-SUMMARY.md` con:
- archivos modificados (2 paths)
- diff resumido (1 archivo rewrite, 1 archivo edit con 1 línea eliminada + 1 línea añadida)
- D-decisions cubiertas (D-15, D-16)
- regresiones detectadas (debe ser ninguna)
</output>
