---
phase: 50-live-progress-display-condicional-solo-si-phase-49-viable
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Existe evidencia cruda de que TaskCreate/TaskCreated dispara durante un execute-phase real en worktree (cwd = .bg-shell/<sid>/), no solo en el orquestador"
    - "Existe una medición de latencia del hook que confirma que NO añade latencia perceptible a la sesión (o que async:true/asyncRewake:true la elimina si la build lo soporta)"
    - "El plan documenta el branch de decisión explícito: A2 confirma → proceder; A2 falla → cortar la fase vía PROG-F1 (D-02) sin penalización al milestone"
  artifacts:
    - path: ".planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-A2-GATE.md"
      provides: "Veredicto A2 con evidencia cruda (cwd worktree + medición de latencia + modo sync/async elegido) y decisión de gate"
      contains: "VEREDICTO A2"
  key_links:
    - from: "execute-phase real (claude --worktree)"
      to: "~/.claude/tasks/<session_id>/"
      via: "TaskCreate disparado por los agentes de la sesión worktree"
      pattern: "cwd.*\\.bg-shell"
---

<objective>
Confirmar EMPÍRICAMENTE el supuesto A2 (riesgo load-bearing) ANTES de invertir en captura+persist+display: que `TaskCreate` dispara los eventos hook `TaskCreated`/`TaskCompleted` durante un `claude --worktree` real de `execute-phase` (cwd = `.bg-shell/<sid>/`, no el repo del orquestador) **Y** que el hook de captura NO añade latencia perceptible a la sesión.

Purpose: el spike (Phase 49) probó las 4 condiciones VIABLE con evidencia cruda, pero disparó `TaskCreate` en PRIMERA PERSONA en el orquestador (cwd = repo). Lo único inferido —NO re-medido— es A2: que el flujo `/gsd-execute-phase` de kodo **invoque** `TaskCreate` en sus olas de agentes worktree, y que el hook síncrono no tartamudee la sesión (Pitfall 1 del research: `TaskCreated`/`TaskCompleted` disparan SÍNCRONAMENTE dentro del agentic loop; "slow hooks add latency to the session"). A2 es load-bearing para TODO el valor de la fase (D-01).

Output: `50-A2-GATE.md` — veredicto A2 con evidencia cruda (cwd worktree real poblándose + medición de latencia + modo sync/async elegido) y la decisión de gate. Si A2 confirma → procede el resto de la fase. Si A2 falla → corta vía PROG-F1 (D-02), gastando UNA corrida en vez de la fase entera.

Este gate es load-bearing y NO saltable (D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gate, mecanismo y riesgo
@.planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-VALIDATION.md

# El mold del hook throwaway que usó el spike (restaurar settings byte-idéntico al terminar)
@src/hooks/install.js
@src/hooks/session-start.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Instrumentar un execute-phase real worktree y confirmar disparo + latencia (gate A2)</name>
  <read_first>
    - .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md (Header: hook throwaway `/tmp/kodo-spike-taskhook.sh`, payload crudo con session_id+cwd+transcript_path, Nota de cobertura A2 — qué NO se re-midió)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md (Pitfall 1 latencia síncrona; Pitfall 6 re-verificar versión; Open Questions 1 async:true/asyncRewake:true; Assumptions A1/A2/A3)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md (D-01/D-02 — gate de apertura + branch de corte)
    - src/hooks/install.js (addHook: envoltura `{hooks:[{type:'command',command}]}`; cómo registrar/desregistrar un evento sin clobber)
  </read_first>
  <action>
    Instrumentación empírica throwaway (NO código de producción — espejo del harness del spike Phase 49):

    1. **Re-verificar la versión** (Pitfall 6 / Pitfall 0 del spike): ejecutar `claude --version` AHORA y anotarla. El research vale para 2.1.175; si rotó, anotar la build real medida.

    2. **Registrar un hook throwaway** para `TaskCreated` y `TaskCompleted` en `~/.claude/settings.json` que vuelque a `/tmp/kodo-a2-<event>-<ts>.json`: el payload crudo (`session_id`, `cwd`, `hook_event_name`, `task_id`) + un timestamp de entrada y otro de salida del propio hook (para medir su coste de cold-start Node). HACER BACKUP `.bak` de settings.json ANTES y RESTAURARLO byte-idéntico al terminar (igual que el spike). El hook throwaway escribe SOLO a `/tmp`, nunca a `~/.kodo/` ni `src/`.

    3. **Lanzar un `execute-phase` real vía el flujo de kodo** (no en el orquestador): una sesión `claude --worktree` cuyo cwd sea `.bg-shell/<sid>/`, con agentes que invoquen `TaskCreate`. Confirmar EMPÍRICAMENTE:
       - (A1) que `~/.claude/tasks/<session_id>/` se puebla con `N.json` durante las olas (verificar con `ls -la`), y que los eventos hook DISPARAN en esa sesión worktree (volcado en `/tmp`). El payload debe traer `cwd = .bg-shell/<sid>/` (no el repo) — confirma A4 de paso.
       - (A2/latencia) MEDIR la latencia del hook: con cuerpo mínimo (readdir + 1 writeFile), ¿el delta entrada→salida es perceptible? ¿la sesión "tartamudea" entre tasks/olas? Probar `async:true` + `asyncRewake:true` en el registro del hook si la build 2.1.175 lo soporta (Open Question 1 / A3); anotar si elimina la latencia o si hay que caer a `timeout` bajo + cuerpo mínimo.

    4. **Emitir `50-A2-GATE.md`** con: versión medida, evidencia cruda (rutas `/tmp` de los volcados + un payload de ejemplo con `cwd` worktree + medición de latencia), el modo elegido (sync+timeout vs async) como recomendación load-bearing para el Plan 02, y el **VEREDICTO A2: CONFIRMA / FALLA** con la decisión de gate.

    5. **Branch de decisión EXPLÍCITO** (documentarlo en el veredicto, D-02):
       - **A2 CONFIRMA** (dispara en worktree + sin latencia perceptible, o async la elimina) → proceder a los Plans 02/03. Anotar el modo de registro recomendado.
       - **A2 FALLA** (cero disparo en execute-phase real, o latencia inaceptable sin mitigación) → CORTAR la fase: PROG-02/03 difieren a v2 vía PROG-F1; el milestone cierra con OPEN-* + NYQ-03 sin penalización. NO ejecutar los Plans 02/03.

    Restaurar `~/.claude/settings.json` byte-idéntico al backup al terminar. Confirmar `git diff -- src/ test/ bin/` vacío (instrumentación throwaway, cero producción).
  </action>
  <what-built>
    Instrumentación throwaway (hook en `/tmp`, registrado temporalmente en settings.json con backup) que observa un execute-phase real worktree disparando eventos `Task*` y mide la latencia del hook.
  </what-built>
  <how-to-verify>
    1. Anotar `claude --version` ejecutado en el momento.
    2. Lanzar un execute-phase real (claude --worktree, cwd `.bg-shell/<sid>/`) con agentes que usen TaskCreate.
    3. `ls -la ~/.claude/tasks/<session_id>/` muestra `N.json` poblándose durante las olas.
    4. Los volcados `/tmp/kodo-a2-*.json` contienen payloads con `cwd` = `.bg-shell/<sid>/` (no el repo) + timestamps de entrada/salida del hook.
    5. La sesión NO tartamudea de forma perceptible al crear/completar tasks (o async:true lo resuelve).
    6. `50-A2-GATE.md` existe con `VEREDICTO A2: CONFIRMA` o `VEREDICTO A2: FALLA` y la decisión de gate.
    7. `~/.claude/settings.json` restaurado byte-idéntico; `git diff -- src/ test/ bin/` vacío.
  </how-to-verify>
  <acceptance_criteria>
    - `50-A2-GATE.md` existe y contiene la cadena `VEREDICTO A2` con CONFIRMA o FALLA explícito.
    - Si CONFIRMA: el documento cita ≥1 payload crudo con `cwd` apuntando a un worktree `.bg-shell/<sid>/` + una medición de latencia + el modo de registro recomendado (sync+timeout o async).
    - Si FALLA: el documento invoca PROG-F1 (D-02) y declara los Plans 02/03 NO ejecutables; la fase se corta.
    - `~/.claude/settings.json` restaurado byte-idéntico (verificable: `diff settings.json settings.json.bak` vacío antes de borrar el backup).
    - `git diff -- src/ test/ bin/` vacío (instrumentación throwaway, cero código de producción).
  </acceptance_criteria>
  <resume-signal>Escribe "A2 confirma" (procede a Plan 02) o "A2 falla" (corta la fase vía PROG-F1), o describe lo observado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code runtime → hook throwaway | el payload del evento `Task*` (untrusted shape) cruza a un script throwaway en `/tmp` |
| hook throwaway → `~/.claude/settings.json` | mutación temporal de config de Claude Code (debe restaurarse byte-idéntico) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-A2-01 | Tampering | `~/.claude/settings.json` mutado para registrar el hook throwaway | mitigate | Backup `.bak` ANTES; restaurar byte-idéntico al terminar (espejo del spike Phase 49); verificar `diff` vacío |
| T-50-A2-02 | DoS (self-inflicted) | hook síncrono que ralentiza la sesión worktree | mitigate | Esta es PRECISAMENTE la medición del gate: cuerpo mínimo + medir latencia + probar async:true/timeout (Pitfall 1) |
| T-50-A2-03 | Information Disclosure | payload del hook volcado a `/tmp` (world-readable) | accept | datos efímeros throwaway (session_id, cwd, task subject) sin secretos; borrados al cerrar el gate; misma postura que el spike |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — esta fase no instala paquetes (research §Package Legitimacy Audit: cero dependencias nuevas) |
</threat_model>

<verification>
- `50-A2-GATE.md` existe con veredicto A2 explícito (CONFIRMA/FALLA) y decisión de gate.
- `claude --version` re-verificado y anotado en el documento.
- `~/.claude/settings.json` restaurado byte-idéntico (cero residuo del hook throwaway).
- `git diff -- src/ test/ bin/` vacío.
</verification>

<success_criteria>
- El supuesto A2 queda CERRADO con evidencia cruda (disparo en worktree real + medición de latencia), no inferido.
- La decisión de gate es inequívoca: procede (con modo de registro recomendado) o corta (PROG-F1).
- Cero código de producción tocado; settings.json restaurado.
</success_criteria>

<output>
Create `.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-01-SUMMARY.md` when done.
Also create `.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-A2-GATE.md` (el veredicto del gate).
</output>
