---
phase: 50-live-progress-display-condicional-solo-si-phase-49-viable
plan: 02
type: execute
wave: 1
depends_on: ["50-01"]
files_modified:
  - src/hooks/task-progress.js
  - src/hooks/install.js
  - test/hooks/task-progress.test.js
  - test/hooks/task-progress-install.test.js
autonomous: true
requirements: ["PROG-02"]
user_setup: []

must_haves:
  truths:
    - "En cada disparo TaskCreated/TaskCompleted, el hook recuenta el tasks-dir plano de Claude Code y escribe N/M autoritativo al artefacto kodo (self-healing, sobrevive eventos perdidos)"
    - "El hook es never-throws fire-and-forget: ENOENT/JSON corrupto/sesión no rastreada/payload basura nunca crashean Claude Code"
    - "El hook NUNCA toma el .lock del tasks-dir y filtra .lock/.highwatermark del recuento"
    - "El artefacto se nombra con found.session.task_id (UUID kodo), NUNCA con input.task_id (índice de Claude Code), validado anti-traversal antes de construir la ruta"
    - "install/uninstall registra y limpia TaskCreated/TaskCompleted sin perturbar los golden-bytes de session-start.js (HOOK-02) ni los hooks SessionStart/Stop existentes"
  artifacts:
    - path: "src/hooks/task-progress.js"
      provides: "Hook de captura separado: lee ~/.claude/tasks/<session_id>/ → escribe ~/.kodo/progress/<task_id>.json"
      contains: "import.meta.url"
      min_lines: 40
    - path: "src/hooks/install.js"
      provides: "Registro/limpieza de TaskCreated/TaskCompleted vía addHook (sin clobber)"
      contains: "TaskCreated"
    - path: "test/hooks/task-progress.test.js"
      provides: "Tests del hook: recuento, filtrado .lock/.highwatermark, never-throws, anti-traversal, status estricto completed"
    - path: "test/hooks/task-progress-install.test.js"
      provides: "Tests install/uninstall de los 2 eventos sin tocar SessionStart/Stop"
  key_links:
    - from: "src/hooks/task-progress.js"
      to: "~/.claude/tasks/<session_id>/"
      via: "readdirSync never-throws (sin .lock)"
      pattern: "readdirSync"
    - from: "src/hooks/task-progress.js"
      to: "findSession({sessionId})"
      via: "import dinámico lazy de ../session/state.js"
      pattern: "findSession"
    - from: "src/hooks/task-progress.js"
      to: "~/.kodo/progress/<task_id>.json"
      via: "writeFileSync con found.session.task_id"
      pattern: "progress.*task"
    - from: "src/hooks/install.js"
      to: "~/.claude/settings.json"
      via: "addHook(settings.hooks, 'TaskCreated'/'TaskCompleted', cmd)"
      pattern: "addHook.*Task"
---

<objective>
Implementar el hook de captura SEPARADO (`src/hooks/task-progress.js`) que, en cada disparo `TaskCreated`/`TaskCompleted`, lee autoritativamente el tasks-dir plano de Claude Code (`~/.claude/tasks/<session_id>/`), deriva `N = count(status==="completed")` / `M = total`, correlaciona `session_id → task_id` (UUID kodo) vía `findSession`, y escribe el snapshot a `~/.kodo/progress/<task_id>.json`. Registrar el hook vía `installHooks()` sin perturbar los golden-bytes de `session-start.js` (HOOK-02).

Purpose: cierra PROG-02 (captura + persiste). Es el productor del seam byte-idéntico productor↔consumidor (espejo de `~/.kodo/plans/<task_id>.md` de v0.11). TODO el acoplamiento a internals de Claude Code vive EXCLUSIVAMENTE aquí (1 archivo) — el dashboard nunca lee `~/.claude/`.

Output: `src/hooks/task-progress.js` (nuevo, never-throws fire-and-forget), `src/hooks/install.js` (extendido), y los tests Wave 0.

Pre-requisito: este plan SOLO se ejecuta si `50-01` concluyó **A2 CONFIRMA**. El veredicto `50-A2-GATE.md` recomienda el modo de registro (sync+timeout vs async:true/asyncRewake:true) — honrarlo en `install.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Mecanismo locked + schema + decisiones
@.planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-CONTEXT.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-PATTERNS.md
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-VALIDATION.md

# El veredicto del gate (modo de registro recomendado)
@.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-A2-GATE.md

# Analogs verbatim (read_first de cada tarea apunta a líneas concretas)
@src/hooks/session-start.js
@src/hooks/install.js
@src/cli/dashboard/plan.js
@src/session/state.js

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase. Úsalos directamente. -->

findSession (src/session/state.js:319) — correlación session_id → task_id:
  findSession({ sessionId }) → { id, session, source } | null
  El task_id UUID kodo se accede vía `found.session.task_id` (mismo shape que session-start.js:238).
  NOTA: el payload del hook trae `input.task_id` = índice "1" de Claude Code (DISTINTO del UUID).
  USAR found.session.task_id para el nombre del artefacto, NUNCA input.task_id (Open Question 2 research).

readStdin (mold session-start.js:198-208) — copiar VERBATIM:
  async readStdin() con setTimeout(()=>resolve('{}'), STDIN_TIMEOUT=3000) + chunks + on('end').

Guard de invocación directa (mold session-start.js:269-272) — copiar VERBATIM:
  if (import.meta.url === `file://${process.argv[1]}`) { main(); }

addHook (src/hooks/install.js:91-109) — NO se toca. Ya envuelve en { hooks:[{type:'command',command}] }
  e idempotencia vía entry.hooks.some(h => h.command?.includes('kodo')). Retorna true si añadió.

Anti-traversal guard (mold plan.js:120-121) — String.includes, NO regex (anti-ReDoS):
  taskId && !taskId.includes('/') && !taskId.includes('\\') && !taskId.includes('..')

Schema de ~/.claude/tasks/<session_id>/N.json (verificado 2.1.175):
  { id, subject, description, status: "completed"|"pending", blocks:[], blockedBy:[] }
  Load-bearing: solo `status`. El dir también contiene `.lock` (0B) y `.highwatermark` → filtrar.

Forma del artefacto ~/.kodo/progress/<task_id>.json:
  { n, m, completed, updated_at }  donde completed === (m > 0 && n === m)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook task-progress.js — recuento autoritativo + escritura del artefacto (never-throws)</name>
  <files>src/hooks/task-progress.js, test/hooks/task-progress.test.js</files>
  <read_first>
    - src/hooks/session-start.js (líneas 8-15 imports + STDIN_TIMEOUT; 198-208 readStdin VERBATIM; 210-267 main() never-throws con try/catch externo; 233-234 import dinámico lazy; 269-272 guard de invocación directa)
    - src/cli/dashboard/plan.js (líneas 65-78 readLightPlan: forma never-throws + DI; 120-121 guard anti-traversal String.includes)
    - src/session/state.js (líneas 316-342 findSession: firma `{ id, session, source }`, acceso `found.session.task_id`)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md (líneas 116-163 Pattern 1 cuerpo verbatim; Pitfalls 2/3/4 — trigger vs verdad, .lock, status estricto; Open Question 2 — task_id namespaces)
    - .planning/phases/49-live-progress-spike-hard-gate/49-SPIKE.md (schema N.json real; payload crudo; Pitfall 3 NO tomar .lock)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-A2-GATE.md (modo recomendado — si async, el hook debe escribir a tiempo)
  </read_first>
  <behavior>
    - Test 1 (recuento): tasks-dir sintético con 3 `N.json` (1 completed, 2 pending) → artefacto `{ n:1, m:3, completed:false }`.
    - Test 2 (completado): 3 de 3 completed → `{ n:3, m:3, completed:true }`.
    - Test 3 (filtrado): el tasks-dir contiene `.lock` y `.highwatermark` además de los `N.json` → NO se cuentan en M (M = solo los `*.json` que no empiezan por `.`).
    - Test 4 (status estricto): un `N.json` con status `cancelled`/`blocked`/`in_progress` NO infla N — N = count exacto de `status==="completed"` (igualdad estricta, Pitfall 4). N nunca > M.
    - Test 5 (never-throws / tasks-dir ausente): session_id sin tasks-dir (ENOENT) → salida silenciosa, no escribe artefacto, no lanza.
    - Test 6 (never-throws / JSON corrupto): un `N.json` a medio escribir (JSON inválido) → no cuenta como completed, no lanza, los demás sí cuentan (self-heal).
    - Test 7 (sesión no rastreada): findSession → null → no-op silencioso, no escribe.
    - Test 8 (anti-traversal): un task_id derivado con `/`, `\` o `..` → NO escribe (guard antes de construir la ruta).
    - Test 9 (payload basura): stdin no-JSON o sin session_id → salida silenciosa, never-throws.
  </behavior>
  <action>
    Crear `src/hooks/task-progress.js` clonando la FORMA de `session-start.js` (es un hook SEPARADO — NO append, preserva golden-bytes HOOK-02). El cuerpo verbatim está en RESEARCH.md líneas 116-163 (Pattern 1).

    Estructura (mold session-start.js):
    - Imports: `readdirSync, readFileSync, writeFileSync, mkdirSync` de `node:fs`; `join` de `node:path`; `homedir` de `node:os`. `const STDIN_TIMEOUT = 3000` (copia de session-start.js:15). LEAF: solo builtins arriba; `findSession` se importa LAZY dentro de main (`const { findSession } = await import('../session/state.js')`, mold session-start.js:233-234) — el research (Pitfall 1, latencia) manda cuerpo mínimo, import diferido.
    - `readStdin()`: copiar VERBATIM session-start.js:198-208.
    - `main()` (mold session-start.js:210-267, try/catch externo never-throws):
      1. parse stdin → `sessionId = input.session_id`; si falsy → return silencioso.
      2. **Recuento AUTORITATIVO del tasks-dir (D-04, NO acumular eventos — Pitfall 2):** `join(homedir(), '.claude', 'tasks', sessionId)`; readdirSync en try/catch (ENOENT/EACCES → return); por cada entry, filtrar `f.endsWith('.json') && !f.startsWith('.')` (ignora `.lock`/`.highwatermark` — Pitfall 3); `m++`; leer+parsear cada `N.json` en su PROPIO try/catch (JSON corrupto → no cuenta, self-heal); `if (t.status === 'completed') n++` (igualdad ESTRICTA — Pitfall 4). NUNCA tomar el `.lock`.
      3. **Correlación:** `findSession({ sessionId })`; si null → return. `taskId = found.session.task_id` (UUID kodo — NUNCA `input.task_id`, Open Question 2).
      4. **Anti-traversal ANTES de la ruta** (mold plan.js:120-121, String.includes NO regex): `if (!taskId || taskId.includes('/') || taskId.includes('\\') || taskId.includes('..')) return`.
      5. **Escritura write-owner kodo:** `mkdirSync(progDir, {recursive:true})` en try/catch; `snapshot = { n, m, completed: m > 0 && n === m, updated_at: new Date().toISOString() }`; `writeFileSync(join(progDir, \`${taskId}.json\`), JSON.stringify(snapshot) + '\n')`. Ruta byte-idéntica a la que leerá el consumidor (Plan 03): `join(homedir(), '.kodo', 'progress', \`${taskId}.json\`)`.
    - Guard de invocación directa: copiar VERBATIM session-start.js:269-272 (`if (import.meta.url === \`file://${process.argv[1]}\`) main();`) — hace el módulo testeable sin spawn. EXPORTAR `main` (o un helper puro `deriveProgress(tasksDir, deps)` y `writeProgress`) para inyectar DI en tests (HOME isolation: `tasksRoot`/`progressDir`/`findSessionFn`/`readdirFn` inyectables, mold del DI de readLightPlan plan.js:65-69). NO depender de spawn real en los tests.

    Crear `test/hooks/task-progress.test.js` (tests puros, sin spawn): fixtures de tasks-dir sintético (mkdtemp en /tmp o DI de `readdirFn`/`readFileFn`) + HOME isolation vía DI (`progressDir`/`tasksRoot`/`findSessionFn`). Cubrir los 9 behaviors.

    NO usar regex sobre nombres de archivo (anti-ReDoS D-13). NO importar picocolors. NO tocar session-start.js.
  </action>
  <verify>
    <automated>node --test test/hooks/task-progress.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/hooks/task-progress.test.js` exits 0 con los 9 behaviors verdes.
    - `src/hooks/task-progress.js` contiene `import.meta.url` (guard de invocación directa) y `findSession` (correlación).
    - `grep -c "input.task_id" src/hooks/task-progress.js` devuelve 0 fuera de comentarios (el nombre del artefacto usa found.session.task_id, NUNCA input.task_id). Comando: `grep -v '^[[:space:]]*//' src/hooks/task-progress.js | grep -c 'input\.task_id'` == 0.
    - El recuento filtra `.lock`/`.highwatermark` (M cuenta solo `*.json` sin `.` inicial) — verificado por Test 3.
    - `git diff --quiet src/hooks/session-start.js` (golden-bytes HOOK-02 intactos — el hook es separado).
  </acceptance_criteria>
  <done>El hook recuenta el tasks-dir autoritativo, escribe el artefacto con el UUID kodo, es never-throws fire-and-forget, filtra `.lock`/`.highwatermark`, cuenta status `completed` estricto, y no toca session-start.js. Los 9 tests pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar TaskCreated/TaskCompleted en install.js sin clobber</name>
  <files>src/hooks/install.js, test/hooks/task-progress-install.test.js</files>
  <read_first>
    - src/hooks/install.js (líneas 12-47 installHooks: declaración de cmds 14-15, addHook existentes 32-35, bloque console.log 39-41; 52-83 uninstallHooks: loop de eventos línea 65; 91-109 addHook — NO tocar)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-RESEARCH.md (líneas 274-287 diff exacto del registro; Runtime State Inventory — coexisten 5 hooks SessionStart de distintas herramientas, NO clobber)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-PATTERNS.md (sección install.js: declarar taskProgressCmd junto a sessionStartCmd, extender el array de uninstall)
    - .planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-A2-GATE.md (si el veredicto recomienda async:true/asyncRewake:true, reflejarlo en la forma del hook registrado — ver action)
  </read_first>
  <behavior>
    - Test 1 (install añade): settings.json sin hooks Task* → tras installHooks, `settings.hooks.TaskCreated` y `.TaskCompleted` contienen una entry kodo con el command a `task-progress.js`.
    - Test 2 (idempotente): correr installHooks dos veces → no duplica (addHook ya es idempotente vía `.includes('kodo')`).
    - Test 3 (no clobber): settings.json con hooks SessionStart ajenos (gsd/codeisland/orca) + Stop existente → installHooks NO los altera; solo añade los 2 nuevos eventos.
    - Test 4 (uninstall limpia): tras uninstallHooks, `TaskCreated`/`TaskCompleted` quedan sin la entry kodo, PERO los SessionStart ajenos y los hooks no-kodo permanecen.
    - Test 5 (golden-bytes): el contenido de session-start.js no cambia (este plan no lo toca — verificable por git).
  </behavior>
  <action>
    Extender `installHooks()` y `uninstallHooks()` en `src/hooks/install.js` (el diff exacto está en RESEARCH.md líneas 274-287). NO tocar el helper `addHook` (líneas 91-109) — ya envuelve `{ hooks:[{type:'command',command}] }` e idempotencia.

    En `installHooks()`:
    - Declarar `const taskProgressCmd = \`node "${join(kodoRoot, 'src', 'hooks', 'task-progress.js')}"\`;` junto a `sessionStartCmd`/`stopCmd` (mismo patrón node-path-quote, líneas 14-15).
    - Tras los `addHook` de SessionStart/Stop (líneas 32-35): `changed = addHook(settings.hooks, 'TaskCreated', taskProgressCmd) || changed;` y lo mismo para `'TaskCompleted'`.
    - Añadir los nuevos cmds al bloque `console.log` (39-41) por simetría.

    En `uninstallHooks()`: extender el array de eventos del loop (línea 65) a `['SessionStart', 'Stop', 'TaskCreated', 'TaskCompleted']`. El cuerpo del loop (filtra por `h.command?.includes('kodo')`) NO cambia — ya limpia cualquier hook kodo de cada evento.

    **Modo async (condicional al gate):** si `50-A2-GATE.md` recomienda `async:true`/`asyncRewake:true` para mitigar la latencia (Pitfall 1), el registro debe incluir esas claves en el shape del hook. Como `addHook` hoy hardcodea `{ type:'command', command }`, esto requiere un tratamiento: o (a) un parámetro opcional `extra` en `addHook` que se haga merge en el objeto del hook (preservando idempotencia), o (b) un shape de registro específico para los eventos Task*. Elegir la opción mínima alineada con el veredicto del gate; documentar la elección en el SUMMARY. Si el gate recomendó sync+timeout, añadir `timeout` bajo (2-3s) en el shape en vez de async. Si el gate no exigió ningún modo especial, registrar plano como SessionStart/Stop.

    Crear `test/hooks/task-progress-install.test.js` (NO existe un test de install hoy — es Wave 0 nuevo, no "extender"). Usar un settings.json sintético (DI del path o un fixture temporal en /tmp con HOME isolation) y cubrir los 5 behaviors. Mold del patrón de tests del repo (`node --test`, fixtures en /tmp, restaurar).
  </action>
  <verify>
    <automated>node --test test/hooks/task-progress-install.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/hooks/task-progress-install.test.js` exits 0 con los 5 behaviors verdes.
    - `grep -c "TaskCreated" src/hooks/install.js` >= 2 (install + uninstall).
    - `grep -c "TaskCompleted" src/hooks/install.js` >= 2.
    - El loop de uninstall barre los 4 eventos: `grep -c "'TaskCreated', 'TaskCompleted'" src/hooks/install.js` >= 1 (o equivalente en el array).
    - `git diff --quiet src/hooks/session-start.js` (golden-bytes HOOK-02 intactos).
    - El helper `addHook` preserva idempotencia (Test 2 verde).
  </acceptance_criteria>
  <done>installHooks registra TaskCreated/TaskCompleted apuntando a task-progress.js sin clobbear hooks ajenos; uninstallHooks los limpia; el modo (async/timeout/plano) sigue la recomendación del gate; session-start.js intacto. Los 5 tests pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code runtime → hook task-progress.js | el payload `Task*` (untrusted: session_id, task_id "1", cwd) cruza al hook kodo vía stdin |
| ~/.claude/tasks/<session_id>/ → hook | datos on-disk de Claude Code (N.json, .lock, .highwatermark) leídos por el hook (READ surface) |
| hook → ~/.kodo/progress/<task_id>.json | el hook construye una ruta de escritura desde un task_id derivado (write-owner kodo) |
| install.js → ~/.claude/settings.json | mutación de config compartida con hooks de terceros (gsd/codeisland/orca) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-traversal | Tampering | nombre del artefacto `~/.kodo/progress/<task_id>.json` | mitigate (HIGH) | Guard anti-traversal `String.includes('/')`/`'\\'`/`'..'` (NO regex, mold plan.js:120-121) sobre `found.session.task_id` ANTES de construir la ruta; ruta construida con root FIJO `join(homedir(),'.kodo','progress')`. NUNCA usar `input.task_id` del payload (Open Question 2) |
| T-50-redos | DoS | matching de path en el hook | mitigate | Cero compilación de regex desde input — solo `String.endsWith/startsWith/includes` (mold plan.js D-13) |
| T-50-dos-internals | DoS / Info-disc | lectura de `~/.claude/tasks/` ajeno | mitigate | Lectura never-throws SIN tomar el `.lock` (Pitfall 3 spike); filtra `.lock`/`.highwatermark`; ENOENT/EACCES → return silencioso (no escala) |
| T-50-latency | DoS (self-inflicted) | hook síncrono ralentiza la sesión | mitigate | Cuerpo mínimo (readdir + 1 writeFile), import lazy de findSession, modo async/timeout según `50-A2-GATE.md` (Pitfall 1) |
| T-50-corrupt | DoS | N.json a medio escribir crashea el hook | mitigate | Cada readFile+JSON.parse en su propio try/catch → no cuenta, no lanza (self-heal próximo evento) |
| T-50-clobber | Tampering | settings.json de hooks de terceros | mitigate | addHook idempotente filtra por `.includes('kodo')`; install/uninstall solo tocan entries kodo; session-start.js golden-bytes intactos |
| T-50-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — cero paquetes nuevos (research §Package Legitimacy Audit). Solo Node builtins + state.js ya presente |
</threat_model>

<verification>
- `node --test test/hooks/task-progress.test.js` y `node --test test/hooks/task-progress-install.test.js` verdes.
- `npm test` sin regresiones (cero fail — el repo lo exige).
- `git diff --quiet src/hooks/session-start.js` (HOOK-02 golden-bytes).
- El hook usa `found.session.task_id` (UUID), nunca `input.task_id`; anti-traversal antes de la ruta; filtra `.lock`/`.highwatermark`.
</verification>

<success_criteria>
- PROG-02 cumplido: el hook captura y persiste `N/M` autoritativo a `~/.kodo/progress/<task_id>.json`, correlacionado por task_id, never-throws, sin tomar el `.lock`, sin perturbar HOOK-02.
- install/uninstall registra y limpia los 2 eventos sin clobber, con el modo (async/timeout/plano) que el gate recomendó.
- Todo el acoplamiento a internals de Claude Code vive en este único archivo.
</success_criteria>

<output>
Create `.planning/phases/50-live-progress-display-condicional-solo-si-phase-49-viable/50-02-SUMMARY.md` when done.
</output>
